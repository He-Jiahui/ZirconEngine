"""Durable supervision and lifecycle support for the coordinator daemon.

Public contracts are imported from their owning modules so schema migration can
load this package without creating a migration/runtime-descriptor cycle.
"""
