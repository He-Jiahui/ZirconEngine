---
related_code:
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/shell.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/rails.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/document/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/document/close.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/close_prompt.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/close_prompt/action.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/close_prompt_hit.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/primary_overlays.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/release_capture.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/close_prompt.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/primary_overlays.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/release.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/text_focus.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/fallback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/menu_press.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/reference.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/reference/click.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/tree.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/fallback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/kind.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes/welcome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/reference.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/tree.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/callback_targets.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run/callback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run/fallback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run/result.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/native.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/template_nodes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar/click.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar/damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/primary_press.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/release.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/text_focus.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/text_focus/activation.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/text_focus/clear.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/viewport_button.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/entry/pressed.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/entry/released.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary/activation.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary/damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary/text_input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/release.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/secondary.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/constants.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture/arm.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture/finish.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture/move_event.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/arm.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/finish.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event/active.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event/start.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/document/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/document/root.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer/bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer/left.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer/right.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page/status.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page/template_nodes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/dock.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/dock/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/bar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/popup_bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/popup_bottom/root.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/stack.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/stack/level.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/stack/level/frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/chrome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/content/blocking.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/content/dimensions.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/popup.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/row.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/submenu.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/blocking.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/root.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/root/frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/root/source.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/nested.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/nested/hit.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/nested/level.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/submenu_frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/clear.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/entry/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/entry/capture.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/menu.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/asset.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/entry/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/native.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/workbench/hover.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/union/optional.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/chrome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/hierarchy/row.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/hierarchy/scroll.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/tab_drag.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/resize_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/floating/header.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/rails.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/rails/frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/rails/hit.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/document/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/document/close.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/docked.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/drawers.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/drawers/bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/drawers/side.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/side_rails.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/geometry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/geometry/containment.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/geometry/frames.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/pane_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/pane_route/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/pane_route/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local/bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local/side.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/mode.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body/model.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/menu.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset/reference.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset/tree.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/browser.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/panels.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/welcome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/viewport_target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/state.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/entry/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/floating/group.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/floating/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/groups.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/regions.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/regions/center_status.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/regions/same_group.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/template_hover_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/status.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/union/optional.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/redraw.rs
  - zircon_editor/src/ui/retained_host/host_contract/globals.rs
  - zircon_editor/src/ui/retained_host/host_contract/data/host_interaction.rs
  - zircon_editor/src/ui/retained_host/host_contract/window.rs
  - zircon_editor/src/ui/retained_host/app/host_lifecycle.rs
  - zircon_editor/src/ui/retained_host/app/viewport.rs
  - zircon_editor/src/ui/retained_host/app/workbench_pointer.rs
  - zircon_editor/src/ui/retained_host/app/workspace_docking.rs
  - zircon_editor/src/ui/retained_host/app/detail_scroll_pointer.rs
  - zircon_editor/src/ui/retained_host/app/hierarchy_pointer.rs
  - zircon_editor/src/ui/retained_host/app/presentation_cache.rs
  - zircon_editor/src/ui/retained_host/app/welcome_recent_pointer.rs
  - zircon_editor/src/ui/retained_host/activity_rail_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/document_tab_pointer/host_document_tab_pointer_bridge_sync.rs
  - zircon_editor/src/ui/retained_host/drawer_header_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/host_page_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/viewport_toolbar_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/menu_pointer/host_menu_pointer_bridge_sync.rs
  - zircon_editor/src/ui/retained_host/detail_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/scroll_surface_host.rs
  - zircon_editor/src/ui/retained_host/hierarchy_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/welcome_recent_pointer/welcome_recent_pointer_bridge_sync.rs
  - zircon_editor/src/ui/retained_host/asset_pointer/tree/bridge.rs
  - zircon_editor/src/ui/retained_host/asset_pointer/content/bridge.rs
  - zircon_editor/src/ui/retained_host/asset_pointer/reference/bridge.rs
  - zircon_editor/src/scene/viewport/pointer/overlay_router/viewport_overlay_pointer_router_sync.rs
  - zircon_editor/src/ui/retained_host/ui/pane_data_conversion/pane_component_projection/mod.rs
  - zircon_editor/src/ui/workbench/layout/manager/apply.rs
  - zircon_editor/src/tests/host/retained_window/native_host_contract.rs
  - zircon_editor/src/tests/host/retained_window/shell_window.rs
  - zircon_editor/src/tests/host/retained_activity_rail_pointer/pointer_bridge.rs
  - zircon_editor/src/tests/host/retained_document_tab_pointer/dispatch.rs
  - zircon_editor/src/tests/host/retained_drawer_header_pointer/pointer_bridge.rs
  - zircon_editor/src/tests/host/retained_host_page_pointer/pointer_bridge.rs
  - zircon_editor/src/tests/host/retained_viewport_toolbar_pointer/pointer_bridge.rs
  - zircon_editor/src/tests/host/retained_menu_pointer/pointer_bridge.rs
  - zircon_editor/src/tests/host/retained_detail_pointer/scroll_bridge.rs
  - zircon_editor/src/tests/host/retained_list_pointer/bridge_dispatch.rs
  - zircon_editor/src/tests/host/retained_asset_pointer.rs
  - zircon_editor/src/tests/editing/viewport.rs
  - zircon_editor/src/tests/workbench/layout/drawer_attachment.rs
implementation_files:
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/shell.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/rails.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/document/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/document/close.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/chrome_press/tabs/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/close_prompt.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/close_prompt/action.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/close_prompt_hit.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/primary_overlays.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/release_capture.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/close_prompt.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/primary_overlays.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/release.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/sequence/steps/text_focus.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/fallback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/entry/body_routes/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/menu_press.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/reference.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/reference/click.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/tree.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/fallback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/kind.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes/welcome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/reference.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/tree.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/callback_targets.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run/callback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run/fallback.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run/result.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/native.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/template_nodes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar/click.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar/damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/pane_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/primary_press.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/release.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/text_focus.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/text_focus/activation.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/text_focus/clear.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/viewport_button.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/entry/pressed.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/entry/released.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary/activation.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary/damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/primary/text_input.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/release.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/button_dispatch/workbench/secondary.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/constants.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture/arm.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture/finish.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/resize_capture/move_event.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/arm.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/finish.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event/active.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event/start.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/document/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/document/root.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer/bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer/left.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/drawer/right.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/drag_resize/tab_drag/payload/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/close_prompt_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page/status.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/host_page/template_nodes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/dock.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/dock/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/route/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/chrome_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/bar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/popup_bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/popup_bottom/root.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/stack.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/stack/level.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/damage/stack/level/frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/chrome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/content/blocking.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/content/dimensions.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/popup.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/row.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/frames/submenu.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/blocking.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/root.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/root/frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/entry/root/source.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/nested.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/nested/hit.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/nested/level.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/menu_geometry/popup/submenu_frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/clear.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/entry/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/entry/capture.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/menu.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/asset.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/entry/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/native.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/pane/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/move_dispatch/workbench/hover.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/union/optional.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/pane_button_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/chrome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/hierarchy/row.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/hierarchy/scroll.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/pointer_move/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/tab_drag.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/redraw_result/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/resize_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/floating/header.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/rails.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/rails/frame.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/rails/hit.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/document/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/document/close.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/drawer.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/tabs/host_page.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/docked.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/drawers.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/drawers/bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/drawers/side.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/resize.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/chrome/top_level/side_rails.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/geometry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/geometry/containment.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/geometry/frames.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/pane_route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/pane_route/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/pane_route/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local/bottom.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local/document.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/local/side.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/entry/route.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/mode.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/template.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body/body.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body/model.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/entry/viewport_body/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/pane/toolbar.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/panes/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/routing/workbench.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/menu.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset/content.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset/reference.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/asset/tree.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/browser.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/hierarchy.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/panels.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/native/welcome.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/passive.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/pane/viewport_target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/scroll_dispatch/viewport.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/state.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/entry/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/floating.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/floating/group.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/floating/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/groups.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/regions.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/regions/center_status.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/regions/same_group.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/tab_drag_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/template_hover_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/entry.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/status.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/target.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/union.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/union/optional.rs
  - zircon_editor/src/ui/retained_host/host_contract/native_pointer/viewport_toolbar_damage/union/visible.rs
  - zircon_editor/src/ui/retained_host/host_contract/redraw.rs
  - zircon_editor/src/ui/retained_host/host_contract/globals.rs
  - zircon_editor/src/ui/retained_host/app/host_lifecycle.rs
  - zircon_editor/src/ui/retained_host/app/viewport.rs
  - zircon_editor/src/ui/retained_host/app/workbench_pointer.rs
  - zircon_editor/src/ui/retained_host/app/workspace_docking.rs
  - zircon_editor/src/ui/retained_host/app/detail_scroll_pointer.rs
  - zircon_editor/src/ui/retained_host/app/hierarchy_pointer.rs
  - zircon_editor/src/ui/retained_host/app/presentation_cache.rs
  - zircon_editor/src/ui/retained_host/app/welcome_recent_pointer.rs
  - zircon_editor/src/ui/retained_host/activity_rail_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/document_tab_pointer/host_document_tab_pointer_bridge_sync.rs
  - zircon_editor/src/ui/retained_host/drawer_header_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/host_page_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/viewport_toolbar_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/menu_pointer/host_menu_pointer_bridge_sync.rs
  - zircon_editor/src/ui/retained_host/detail_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/scroll_surface_host.rs
  - zircon_editor/src/ui/retained_host/hierarchy_pointer/sync.rs
  - zircon_editor/src/ui/retained_host/welcome_recent_pointer/welcome_recent_pointer_bridge_sync.rs
  - zircon_editor/src/ui/retained_host/asset_pointer/tree/bridge.rs
  - zircon_editor/src/ui/retained_host/asset_pointer/content/bridge.rs
  - zircon_editor/src/ui/retained_host/asset_pointer/reference/bridge.rs
  - zircon_editor/src/scene/viewport/pointer/overlay_router/viewport_overlay_pointer_router_sync.rs
  - zircon_editor/src/ui/retained_host/ui/pane_data_conversion/pane_component_projection/mod.rs
  - zircon_editor/src/ui/workbench/layout/manager/apply.rs
plan_sources:
  - user: 2026-05-07 editor input, split bar drag, drawer drag selection, and single-select regressions
tests:
  - zircon_editor/src/tests/host/retained_window/native_host_contract.rs
  - zircon_editor/src/tests/workbench/layout/drawer_attachment.rs
  - cargo test -p zircon_editor template_assets -- --nocapture (2026-05-11: passed, 10 passed)
  - cargo test -p zircon_editor pointer_handlers_do_not_force_slow_path_rebuilds -- --nocapture (2026-05-11: passed, 1 passed)
  - cargo test -p zircon_editor native_host_repeated_hierarchy_hover_moves_do_not_rebuild_presentation -- --nocapture (2026-05-11: passed, 1 passed)
  - cargo test -p zircon_editor native_host_viewport_button_and_scroll_wait_for_viewport_image_repaint -- --nocapture (2026-05-11: passed, 1 passed)
  - cargo test -p zircon_editor --lib shared_activity_rail_pointer_bridge_skips_rebuild_for_unchanged_layout -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_document_tab_pointer_bridge_skips_rebuild_for_unchanged_layout -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_drawer_header_pointer_bridge_skips_rebuild_for_unchanged_layout -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_host_page_pointer_bridge_skips_rebuild_for_unchanged_layout -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_viewport_toolbar_pointer_bridge_skips_rebuild_for_unchanged_layout -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib viewport_overlay_pointer_router_skips_rebuild_for_unchanged_layout -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_menu_pointer_bridge_skips_rebuild_for_unchanged_layout_and_state -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_scroll_surface_bridge_skips_rebuild_for_unchanged_layout_and_state -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_welcome_recent_pointer_bridge_skips_rebuild_for_unchanged_layout_and_state -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_hierarchy_pointer_bridge_skips_rebuild_for_unchanged_layout_and_state -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib shared_asset_pointer_bridges_skip_rebuild_for_unchanged_layout_and_state -- --nocapture (2026-05-11)
  - cargo test -p zircon_editor --lib retained_activity_rail_pointer -- --nocapture (2026-05-11: passed, 7 passed)
  - cargo test -p zircon_editor --lib retained_document_tab_pointer -- --nocapture (2026-05-11: passed, 6 passed)
  - cargo test -p zircon_editor --lib retained_drawer_header_pointer -- --nocapture (2026-05-11: passed, 5 passed)
  - cargo test -p zircon_editor --lib retained_host_page_pointer -- --nocapture (2026-05-11: passed, 6 passed)
  - cargo test -p zircon_editor --lib retained_viewport_toolbar_pointer -- --nocapture (2026-05-11: passed, 6 passed)
  - cargo test -p zircon_editor --lib viewport_overlay_pointer_router -- --nocapture (2026-05-11: passed, 4 passed)
  - cargo test -p zircon_editor --lib retained_menu_pointer -- --nocapture (2026-05-11: passed, 22 passed, 4 ignored)
  - cargo test -p zircon_editor --lib retained_detail_pointer -- --nocapture (2026-05-11: passed, 7 passed)
  - cargo test -p zircon_editor --lib retained_list_pointer -- --nocapture (2026-05-11: passed, 7 passed)
  - cargo test -p zircon_editor --lib retained_asset_pointer -- --nocapture (2026-05-11: passed, 7 passed)
  - cargo check -p zircon_editor --lib --tests --locked --message-format=short
  - cargo test -p zircon_editor --lib native_host_menu_click_unions_text_focus_damage_without_full_frame --offline --message-format=short
  - cargo test -p zircon_editor --lib redraw_region_can_request_frame_update_without_losing_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_viewport_toolbar_only_dispatches_primary_press --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_pointer_click_routes_late_viewport_toolbar_controls --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_resize_splitter_forwards_move_and_release_after_capture --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_document_tab_drag_releases_capture_and_forwards_drop --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_document_tab_drag_cross_dock_release_uses_center_status_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_document_tab_drag_document_edge_release_uses_center_status_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_document_tab_drag_floating_window_release_uses_floating_center_status_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_close_prompt_button_press_uses_overlay_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_pointer_click_routes_document_tab_with_document_region_origin --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_floating_document_tab_press_uses_floating_window_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_floating_window_header_press_uses_floating_layer_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_drawer_header_tab_press_uses_drawer_region_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_activity_rail_press_uses_center_band_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_pointer_click_routes_host_page_tabs_with_tab_local_point --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_hierarchy_press_uses_pane_center_status_damage --offline --message-format=short
  - cargo test -p zircon_editor --lib native_host_hierarchy_move_prefers_native_hover_when_template_node_overlaps --locked --jobs 1 --message-format=short (2026-05-15: passed)
  - cargo test -p zircon_editor --lib native_host_template_node_move_updates_hover_without_rebuilding_presentation --locked --jobs 1 --message-format=short (2026-05-15: passed)
  - cargo test -p zircon_editor --lib native_host_hierarchy_move --locked --jobs 1 --message-format=short (2026-05-15: passed, 2 passed)
  - tools/ui-profile-capture.ps1 -Scenario idle_hover -AutoInteract -RequireScenarioEvidence -AutoCloseSeconds 3 -SkipBuild (20260515-211644-idle_hover: redraw_region_count=1, gpu_draw_calls=10, gpu_visible_draw_items=12, zero alerts/fallback)
  - cargo test -p zircon_editor --lib frame_update_region_queues_external_redraw_with_frame_update --locked --jobs 1 --message-format=short (2026-05-16: passed)
  - cargo test -p zircon_editor --lib close_requested_callback_can_mutate_host_state_without_reentrant_borrow --locked --jobs 1 --message-format=short (2026-05-16: passed)
  - tools/ui-profile-capture.ps1 -Scenario viewport_image -AutoInteract -RequireScenarioEvidence -AutoCloseSeconds 3 -SkipBuild (20260516-000208-viewport_image: dirty_paint_only_count=1, redraw_region_count=1, gpu_draw_calls=16, gpu_visible_draw_items=21, zero alerts/fallback)
  - cargo test -p zircon_editor --lib retained_window --offline --message-format=short
  - ./.codex/skills/zircon-dev/scripts/validate-matrix.ps1 -TargetDir target/codex-shared-a
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer routing/redraw ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer routing/redraw responsibility split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome rail frame/hit ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome rail split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer redraw result pointer/workbench/resize/chrome/tab-drag/hierarchy ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after redraw result split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pointer-move redraw entry/hierarchy/template ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pointer-move redraw split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pointer-move hierarchy redraw row/scroll ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pointer-move hierarchy redraw split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer drag/resize ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer drag/resize responsibility split: passed with existing warning noise only; `native_pointer.rs` is 732 lines and `native_pointer/drag_resize.rs` is 242 lines)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer move-dispatch ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer move-dispatch responsibility split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer move dispatch entry/menu/workbench/pane/clear ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after move dispatch split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer move dispatch entry body/capture ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after move dispatch entry body/capture split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer move dispatch workbench hover ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after workbench move hover split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane move target native/asset/template/viewport/passive ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane move target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane move entry target dispatch ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane move entry target split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer viewport toolbar damage entry/status/target/union ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after viewport toolbar damage split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer viewport toolbar union optional/visible ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after viewport toolbar union split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane button damage entry/union ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane button damage split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane button union optional/visible ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane button union split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer close prompt damage entry/union ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after close prompt damage split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer close prompt damage union visible ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after close prompt damage union visible split: passed; full tests deferred)
- cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button/scroll dispatch ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer button/scroll responsibility split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer scroll dispatch entry/menu/pane/viewport ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after scroll dispatch split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer scroll pane target ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after scroll pane target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer asset pane scroll tree/content/reference ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after asset pane scroll split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer native pane scroll hierarchy/welcome/panels/browser ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after native pane scroll split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button subtree ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer button subtree split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch entry/release/menu/chrome/workbench ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer close prompt action dispatch ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after close prompt action dispatch split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer text focus activation/clear ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after text focus split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer workbench button entry/primary/secondary/release ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after workbench button split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer workbench button entry pressed/released ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after workbench button entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer workbench primary activation/text-input/damage ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after workbench primary split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome press entry/rails/tabs/floating/resize ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome press split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome press entry tabs/shell ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome press entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome press tab route host/document/drawer ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome press tab route split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome press tabs host/document/drawer ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome press tabs split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome press document tab body/close callback ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome press document tab callback split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch root entry primary/pane-route ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch root entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch body route ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch body route split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch root entry input/release/primary-overlay ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch root entry input/release/primary-overlay split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch root entry sequence ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch root entry sequence split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch root entry sequence entry/steps ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch root entry sequence child split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch root entry sequence steps body/close-prompt/text-focus ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch root entry sequence steps split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button dispatch root entry sequence steps release/primary-overlay ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button dispatch root entry sequence release/primary-overlay split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer button body route entry/input/sequence/workbench/pane/fallback ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after button body route subtree split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback target/kind/fallback ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane callback target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer native pane button hierarchy/welcome ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after native pane button split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback target domain ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane callback target domain split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback viewport target toolbar/body ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane callback viewport target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback target entry callback/result ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane callback target entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback target entry input/sequence/run ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane callback target entry sequence split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback target sequence run callback/result/fallback ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane callback target sequence run split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer result target entry/input/sequence/viewport/template/passive ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after result target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer asset pane target entry/tree/content/reference ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after asset pane target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer asset pane target tree/content/reference route ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after asset pane target route split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer viewport pane callback toolbar/body ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after viewport pane callback split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer viewport toolbar entry/click/damage ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after viewport toolbar callback split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer asset pane callback tree/content/reference ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after asset pane callback split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer asset pane reference callback click ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after asset pane reference callback click split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome damage entry/route/floating/host-page/union ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome damage split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome damage route dock/document/floating/host-page ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome damage route split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome damage dock drawer ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome damage dock drawer split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome damage union visible ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome damage union visible split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome host-page tabs/template/status ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome host-page damage split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane callback subtree ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer pane callback subtree split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer routing subtree ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer routing subtree split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer routing route payload ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after routing route payload split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer routing pane route route/target ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after routing pane route payload split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer routing geometry containment/frames ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after routing geometry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome routing top-level/tabs/rails/floating ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome routing subtree split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome floating header ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome floating header split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome tabs document/drawer/host-page ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome tabs split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome document tab body/close ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome document tab split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome top-level entry/drawer/resize ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome top-level split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome top-level drawer side/bottom ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome top-level drawer split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer chrome top-level docked/side-rail ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after chrome top-level docked/side-rail split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane routing entry/mode/pane/target ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane routing subtree split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane routing entry route/floating/local ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane routing entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane routing local dock document/side/bottom ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane routing local dock split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane route entry/toolbar ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane route split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane route viewport body ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane route viewport body split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane route viewport body toolbar/body/model ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane route viewport body child split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer pane route entry template-hit ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after pane route entry template-hit split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry subtree ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer menu geometry subtree split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry frame chrome/content/popup/row/submenu ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry frame split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry content blocking/dimensions ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry content split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry damage entry/stack ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry damage split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry damage stack level ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry damage stack split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry damage stack level frame ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry damage stack level frame split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry damage popup-bottom ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry damage popup-bottom split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry damage popup-bottom root ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry damage popup-bottom root split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry popup entry/nested ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry popup split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry popup entry root/blocking ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry popup entry split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry popup root source/frame ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry popup root split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry popup submenu-frame ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu popup submenu-frame split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry nested popup level ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry nested level split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer menu geometry nested hit payload ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after menu geometry nested hit payload split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer drag/resize capture subtree ownership scan; cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never (2026-06-18 after native pointer drag/resize capture subtree split: passed with existing warning noise only)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer resize capture arm/move/finish ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after resize capture split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag lifecycle/payload ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag lifecycle/payload split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag lifecycle arm/move/finish ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag lifecycle split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag move event active/start ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag move event split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag payload route/document/drawer ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag payload split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag payload document root/floating and drawer side ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag payload child split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag damage entry/floating/groups/regions/union ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag damage split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag damage entry cross-group target ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag damage entry target split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag damage floating group/target ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag damage floating split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag damage regions same-group/center-status ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag damage regions split: passed; full tests deferred)
  - cargo fmt -p zircon_editor; cargo fmt -p zircon_editor --check; native pointer tab-drag damage union visible ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-21 after tab-drag damage union visible split: passed; full tests deferred)
  - cargo fmt -p zircon_editor --check; native pointer constants/state ownership scan; scoped trailing-whitespace scan; scoped git diff --check (2026-06-20 after constants/state split: passed; full tests deferred)
doc_type: module-detail
---

# Native Pointer Host Contract

The native retained host contract is the editor-side boundary that translates root window pointer and keyboard input into editor shell callbacks. `native_pointer.rs` now owns only native pointer subtree assembly and re-exports. `native_pointer/constants.rs` owns shared host/viewport pointer event ids and viewport button ids, while `native_pointer/state.rs` owns `NativePointerButtonState`. `native_pointer/button_dispatch.rs` is a structural pointer-button dispatch entry and delegates event ordering, capture release, menu/close-prompt handling, Workbench template presses, pane callback fanout, chrome callback fanout, text-focus activation, close-prompt hit resolution, and viewport button id conversion to its folder-backed children. `native_pointer/move_dispatch.rs` owns the root pointer-move flow. `native_pointer/scroll_dispatch.rs` owns root pointer scroll dispatch. `native_pointer/drag_resize.rs` owns transient resize capture and tab-drag state progression. `native_pointer/routing.rs` owns route payload types and delegates hit routing for top-level chrome, pane surfaces, workbench template nodes, viewport toolbar targets, floating-window content, and shell resize splitters to its folder-backed children. `window.rs` owns keyboard/text dispatch after the native pointer subtree has established a focused text-input target.

## Routing Ownership

`native_pointer/routing.rs` is the structural hit-routing entry. It exposes route helpers for top-level chrome, default pane hit testing, pointer-move pane hit testing, and componentized Workbench template-node hit testing, while delegating shared route payload declarations to `routing/chrome_route.rs` and `routing/pane_route.rs`.

The concrete route construction now lives under the routing subtree. `routing/chrome_route.rs` owns the shared `ChromePointerRoute` payload, and `routing/pane_route.rs` is a structural pane-route payload entry: `pane_route/route.rs` owns `PanePointerRoute`, while `pane_route/target.rs` owns `PanePointerTarget`. `routing/chrome.rs` is a structural entry for top-level chrome hit testing; `routing/chrome/top_level.rs` is now a structural top-level route entry with `top_level/entry.rs` owning route order, `top_level/resize.rs` owning splitter hit routing, `top_level/docked.rs` owning document-dock tab routing, `top_level/side_rails.rs` owning left/right activity-rail routing, and `top_level/drawers.rs` owning left/right/bottom drawer-header route sequencing while `drawers/side.rs` owns side-dock drawer header routing and `drawers/bottom.rs` owns bottom-dock drawer header routing. `routing/chrome/tabs.rs` is a structural tab-route entry with `tabs/document.rs`, `tabs/drawer.rs`, and `tabs/host_page.rs` owning document tab, drawer header, and host-page tab route construction respectively. `routing/chrome/rails.rs` is now a structural activity-rail route entry with `rails/frame.rs` owning rail-frame containment and `rails/hit.rs` owning translated button hit testing. `routing/chrome/floating.rs` owns floating-window iteration, while `floating/header.rs` owns header containment, document-tab priority, and floating-header fallback route construction. `routing/panes.rs` is a structural entry for pane hit routing; `routing/panes/entry.rs` is a structural pane-route entry with `entry/route.rs` owning the default/pointer-move public entry points, `entry/floating.rs` owning floating-window pane priority, and `entry/local.rs` owning local dock route sequence while `entry/local/{document,side,bottom}.rs` own document, side, and bottom dock pane routing respectively. `routing/panes/pane.rs` is now a structural pane route entry with `pane/entry.rs` owning pane route sequencing, `pane/entry/template.rs` owning pointer-move template-node hit routing, `pane/entry/viewport_body.rs` owning Scene/Game viewport body routing sequence, `viewport_body/toolbar.rs` owning toolbar eligibility/frame/hit routing, `viewport_body/body.rs` owning body-frame adjustment below the toolbar, `viewport_body/model.rs` owning the route result DTO, and `pane/toolbar.rs` owning viewport toolbar route construction. `routing/panes/target.rs` owns pane-kind to target mapping, and `routing/panes/mode.rs` owns pointer-move template-hit gating. `routing/geometry.rs` is now a structural route-geometry entry with `geometry/containment.rs` owning point-in-frame hit testing and `geometry/frames.rs` owning translation, side-dock content frames, and floating-window content geometry. `routing/workbench.rs` owns componentized Workbench template-node routing. The parent dispatcher consumes those route results but no longer defines tab/header/activity-rail/floating-window/pane route construction itself.

Within document-tab routing, `routing/chrome/tabs/document.rs` now owns only close-before-body iteration order. `tabs/document/close.rs` owns close-button hit route construction, and `tabs/document/body.rs` owns tab-body hit route construction.

## Redraw Result Ownership

`native_pointer/redraw_result.rs` is now a structural wrapper layer that turns route-specific damage calculations into `NativePointerDispatchResult` values. `redraw_result/pointer_move.rs` is a structural pane pointer-move redraw entry with `pointer_move/entry.rs` owning generic short-circuit decisions, `pointer_move/hierarchy.rs` owning hierarchy pointer-move redraw selection, `pointer_move/hierarchy/scroll.rs` owning hierarchy scroll damage, `pointer_move/hierarchy/row.rs` owning hierarchy row-hover damage, and `pointer_move/template.rs` owning non-hierarchy template-hover damage merging. `workbench.rs` owns Workbench template-node hover redraw, `resize.rs` owns resize redraw, `chrome.rs` owns chrome press redraw, `tab_drag.rs` owns tab-drag release redraw, and `hierarchy.rs` owns hierarchy row hover geometry. The specialized damage geometry remains in `chrome_damage.rs`, `resize_damage.rs`, `tab_drag_damage.rs`, and `template_hover_damage.rs`; the parent dispatcher now chooses when those redraw helpers apply without mixing their damage details.

## Move Dispatch Ownership

`native_pointer/move_dispatch.rs` is now a structural pointer-move entry after winit input reaches the retained host. `move_dispatch/entry.rs` owns the perf scope and top-level capture-before-body ordering. `move_dispatch/entry/capture.rs` owns resize capture and tab-drag capture priority, while `move_dispatch/entry/body.rs` owns the normal hover route order: menu hover, componentized Workbench template hover, pane hover and viewport move events, then final hover clearing.

The helper responsibilities live under the move-dispatch subtree. `move_dispatch/menu.rs` owns menu hover callback dispatch and menu damage, `move_dispatch/workbench.rs` owns Workbench template hover hit routing and redraw assembly while `workbench/hover.rs` owns structured-row versus template-node hover state writes, `move_dispatch/pane.rs` is now a structural pane-move entry with `pane/entry.rs` owning route/redraw sequencing and `pane/{native,asset,template,viewport,passive}.rs` owning native pane callbacks, asset pane callbacks, template hover state, viewport move forwarding, and inert hover cleanup respectively. `move_dispatch/clear.rs` owns final hovered-template cleanup damage. Keeping these helpers in named children keeps high-frequency hover orchestration separate from button press, scroll, text focus, and chrome click behavior.

## Button Dispatch Ownership

`native_pointer/button_dispatch.rs` is now a structural entry for pointer button dispatch after winit input reaches the retained host. `button_dispatch/entry.rs` is a structural root-entry export; `entry/sequence.rs` is a structural sequencing entry. `entry/sequence/entry.rs` owns click performance scope setup and root input capture, while `entry/sequence/steps.rs` preserves release, close-prompt, text-focus, primary-overlay, and body-route ordering. `steps/release.rs` owns release-capture completion, `steps/close_prompt.rs` owns close-prompt blocking dispatch, `steps/text_focus.rs` owns focused-text cleanup, `steps/primary_overlays.rs` owns primary-overlay dispatch handoff, and `steps/body.rs` owns final body-route handoff. Focused entry responsibilities live in child owners: `entry/input.rs` owns button defaulting, presentation capture, and viewport button-id lookup; `entry/release_capture.rs` owns primary-release resize/tab-drag completion; `entry/primary_overlays.rs` owns primary-press menu/outside-popup/chrome overlay gating; `entry/body_routes.rs` owns Workbench template hits, pane body button callbacks, and cleared-text fallback damage.

`entry/body_routes.rs` is now a structural body-route entry. `body_routes/entry.rs` owns public body-route input capture, `body_routes/input.rs` owns the shared body-route input model, `body_routes/sequence.rs` owns Workbench-first then pane-route ordering, `body_routes/workbench.rs` owns Workbench body dispatch handoff, `body_routes/pane.rs` owns pane-route button handoff, and `body_routes/fallback.rs` owns cleared-text-input fallback damage.

The helper responsibilities now live under the button-dispatch subtree. `button_dispatch/release.rs` owns primary capture release, `button_dispatch/close_prompt.rs` owns close-prompt button blocking/dispatch, `button_dispatch/primary_press.rs` owns the primary-press menu, outside-popup, and top-level chrome ordering, `button_dispatch/menu_press.rs` owns menu primary-click damage and callback dispatch, `button_dispatch/chrome_route.rs` owns top-level chrome route arming/redraw sequencing, `button_dispatch/chrome_press.rs` is a structural chrome-press entry with `chrome_press/entry.rs` now serving as a structural route-family entry, `chrome_press/entry/tabs.rs` serving as a structural tab-route entry whose `entry/tabs/{host_page,document,drawer}.rs` children own host-page, document, and drawer tab route dispatch, `chrome_press/entry/shell.rs` owning activity-rail/floating-header/resize dispatch, `chrome_press/{rails,floating,resize}.rs` owning activity-rail callbacks, floating header callbacks, and resize arming respectively, and `chrome_press/tabs.rs` now serving as a structural tab callback entry whose `tabs/host_page.rs` owns host-page tab callbacks, `tabs/document.rs` owns body-versus-close selection, `tabs/document/body.rs` owns document tab body callbacks, `tabs/document/close.rs` owns document tab close callbacks, and `tabs/drawer.rs` owns drawer header tab callbacks. `button_dispatch/workbench.rs` is now a structural Workbench button entry with `workbench/entry.rs` owning hit routing and button-state ordering, `workbench/entry/pressed.rs` owning primary/secondary pressed dispatch selection, `workbench/entry/released.rs` owning release dispatch handoff, `workbench/primary.rs` serving as the structural primary-press entry, `primary/text_input.rs` owning text-input focus and idle results, `primary/activation.rs` owning template primary activation, `primary/damage.rs` owning hit damage union, `workbench/secondary.rs` owning context menu request/damage, and `workbench/release.rs` owning release damage. `button_dispatch/pane_route.rs` owns pane hit routing handoff from the button dispatcher. `button_dispatch/pane_callbacks.rs` is a structural pane-callback entry; `pane_callbacks/target.rs` is now a structural target-routing subtree with `target/entry.rs` now a 5-line structural entry, `target/entry/input.rs` owning pane-button dispatch input capture, `target/entry/callback_targets.rs` owning native/asset callback-only target dispatch, `target/entry/result_targets.rs` owning viewport/template/passive result-producing target dispatch, `target/entry/sequence.rs` serving as a structural sequencing entry, `target/entry/sequence/entry.rs` owning public pane-button input capture, and `target/entry/sequence/run.rs` now a structural callback/result/fallback ordering entry with `run/{callback,result,fallback}.rs` owning callback-target fallback, result-target dispatch, and final fallback damage respectively. `target/{native,asset,viewport,template,passive}.rs` own pane-family dispatch selection. `target/asset.rs` is now a structural asset-target entry with `asset/entry.rs` owning route sequencing and `asset/{tree,content,reference}.rs` owning AssetTree, AssetContent, and AssetReference target matching plus concrete callback handoff. `pane_callbacks/kind.rs` owns native/viewport button event id mapping, and `pane_callbacks/fallback.rs` owns passive pane handling plus final pane-press damage fallback, while `pane_callbacks/native_panes.rs`, `pane_callbacks/asset_panes.rs`, `pane_callbacks/viewport.rs`, and `pane_callbacks/template_nodes.rs` own the concrete pane-family callback fanout. `pane_callbacks/viewport.rs` is now a structural viewport callback entry with `viewport/toolbar.rs` owning viewport toolbar button clicks/damage and `viewport/viewport.rs` owning viewport body pointer event forwarding. `pane_callbacks/asset_panes.rs` is now a structural asset callback entry with `asset_panes/{tree,content,reference}.rs` owning asset tree clicks, asset content events/clicks, and asset reference events/clicks respectively. `button_dispatch/text_focus.rs` is now a structural text-focus entry with `text_focus/activation.rs` owning template text-input focus activation and `text_focus/clear.rs` owning focused-text cleanup on primary press. `button_dispatch/close_prompt_hit.rs` owns close-prompt button hit resolution. `button_dispatch/viewport_button.rs` owns native-to-viewport button id conversion. Keeping these helpers in named children prevents `button_dispatch.rs` from becoming a second mixed native-pointer root.

Within the close-prompt button subtree, `button_dispatch/close_prompt.rs` now owns hit blocking and public dispatch, while `button_dispatch/close_prompt/action.rs` owns primary-press gating, close-prompt callback invocation, and close-prompt damage result mapping.

Within the asset callback subtree, `asset_panes/reference.rs` now owns only AssetReference pointer-event callback fanout and preserves the public `dispatch_asset_reference_button` entry. `asset_panes/reference/click.rs` owns primary-button pressed gating plus AssetReference click callback fanout.

Within the pane-callback target subtree, `target/viewport.rs` now only dispatches viewport target kinds. `target/viewport/toolbar.rs` owns viewport-toolbar target handoff to the viewport toolbar callback subtree, and `target/viewport/body.rs` owns viewport-body target handoff to the viewport body callback subtree.

Native pane button callbacks are now split below the native-pane callback entry. `pane_callbacks/native_panes.rs` is a structural entry, `native_panes/hierarchy.rs` owns hierarchy pointer event/click callbacks, and `native_panes/welcome.rs` owns welcome recent click callbacks.

Result-producing pane targets are now split below the target entry. `target/entry/result_targets.rs` is a structural result-target entry, `result_targets/entry.rs` owns public result target input capture, `result_targets/input.rs` owns the shared route input model, `result_targets/sequence.rs` owns viewport/template/passive result ordering, `result_targets/viewport.rs` owns viewport result handoff, `result_targets/template.rs` owns template-node result handoff, and `result_targets/passive.rs` owns passive target fallback.

Viewport toolbar button callbacks are now split below the viewport callback entry. `pane_callbacks/viewport/toolbar.rs` is a structural toolbar entry, `toolbar/entry.rs` owns primary-press gating and dispatch order, `toolbar/click.rs` owns viewport-toolbar click callback fanout, and `toolbar/damage.rs` owns toolbar click damage result mapping.

## Scroll Dispatch Ownership

`native_pointer/scroll_dispatch.rs` is now a structural scroll input entry after winit dispatch. `scroll_dispatch/entry.rs` owns the root scroll ordering and perf scope setup, `scroll_dispatch/menu.rs` owns menu scroll callbacks and menu damage, and `scroll_dispatch/pane.rs` is now a structural pane-scroll entry. Under that pane subtree, `pane/entry.rs` owns pane hit routing plus result ordering, `pane/native.rs` is a structural native-pane callback entry with `native/entry.rs` owning target selection and `native/{hierarchy,welcome,panels,browser}.rs` owning hierarchy, welcome, console/inspector, and browser asset details scroll fanout respectively. `pane/asset.rs` is now a structural asset-pane callback entry with `asset/{tree,content,reference}.rs` owning AssetTree, AssetContent, and AssetReference scroll fanout respectively. `pane/viewport_target.rs` owns viewport scroll handoff, and `pane/passive.rs` owns non-scrollable template, toolbar, UI asset, and fallback pane target classification. `scroll_dispatch/viewport.rs` owns viewport scroll event forwarding with the shared viewport pointer constants.

## Drag And Resize Ownership

`native_pointer/drag_resize.rs` owns the capture-style lifecycle entry points for shell splitters and document/drawer tab drags. The concrete state machines now live in children: `drag_resize/resize_capture.rs` is a structural resize-capture entry, with `resize_capture/arm.rs` arming splitter capture, `resize_capture/move_event.rs` short-circuiting subsequent root moves while resize is active and forwarding move callbacks, and `resize_capture/finish.rs` forwarding up events, clearing resize state, and asking the redraw-result layer for bounded resize damage.

`drag_resize/tab_drag.rs` is now a structural entry for tab-drag capture. `drag_resize/tab_drag/lifecycle.rs` is a structural lifecycle entry: `lifecycle/arm.rs` arms tab drag state from payload data, `lifecycle/move_event.rs` owns move-event eligibility and drag perf scope, `move_event/start.rs` waits for the drag-start distance threshold and sends the initial drag-down callback, `move_event/active.rs` forwards active drag move callbacks, and `lifecycle/finish.rs` forwards drag up events, computes release redraw, and clears drag state. `drag_resize/tab_drag/payload.rs` is now a structural payload entry: `payload/route.rs` owns chrome-route matching, `payload/document.rs` is a structural document-payload entry with `document/root.rs` owning root document tab lookup and `document/floating.rs` owning floating document tab lookup, and `payload/drawer.rs` is a structural drawer-payload entry with `drawer/{left,right,bottom}.rs` owning side-specific drawer tab payload/source-group lookup. Keeping these state machines outside `native_pointer.rs` leaves the root dispatcher focused on event ordering, route selection, and callback timing.

## Text Input Focus

Template text focus is intentionally keyed by an edit target, not only by the visual control id. Existing showcase controls provide `edit_action_id` directly, while generic template surfaces often provide only a binding id for `UiEventKind::Change`.

The focus target resolution order is:

1. Use `TemplateNodePointerHit.edit_action_id` when projection already supplied one.
2. Use the welcome text action id for the legacy welcome text route.
3. For generic `input-field` and `number-field` nodes, fall back to `binding_id` when there is no explicit edit action.

`pane_component_projection::host_template_node(...)` now derives generic template edit and commit targets from bindings. `UiEventKind::Change` becomes the edit target, and `UiEventKind::Submit` becomes the commit target. Showcase-specific preferred action discovery still wins first, so existing curated showcase controls keep their explicit action mapping.

This keeps binding-only template inputs typable: pointer press can focus the node, and `window.rs` can dispatch typed text through `on_surface_control_edited(control_id, edit_target_id, new_value)` instead of dropping the key event because the focus target was empty.

## Menu Damage

Menu hit testing and popup damage math are isolated in the `native_pointer/menu_geometry` subtree. `native_pointer.rs` still owns pointer routing and callback dispatch, while `menu_geometry.rs` re-exports the menu entry points consumed by button, move, and scroll dispatch.

`menu_geometry/bar.rs` owns top-bar and scrolled menu-control containment. `menu_geometry/popup.rs` is now a structural popup hit entry: `popup/entry.rs` owns root/nested/blocking hit-order composition, `popup/entry/root.rs` owns root-menu popup result assembly, `root/source.rs` owns opened root-menu source lookup, `root/frame.rs` owns root popup frame construction, `popup/entry/blocking.rs` owns popup blocking-region containment, `popup/nested.rs` owns nested submenu path traversal, `popup/nested/level.rs` owns single-level submenu branch advancement and hit classification, `popup/nested/hit.rs` owns the `NestedMenuLevelHit` payload returned between traversal levels, and `popup/submenu_frame.rs` owns nested submenu popup-frame construction. `menu_geometry/damage.rs` is now a structural damage entry: `damage/entry.rs` owns current/explicit menu damage rectangle assembly, `damage/popup_bottom.rs` owns open-menu guard and base-height fallback, `damage/popup_bottom/root.rs` owns opened root-menu lookup, root popup frame construction, and stack-bottom handoff, `damage/stack.rs` owns submenu stack-bottom traversal, and `damage/stack/level/frame.rs` owns one stack-level popup frame calculation. `menu_geometry/frames.rs` is now a structural frame-geometry entry: `frames/chrome.rs` owns menu chrome and top-bar fallback frames, `frames/content.rs` re-exports content-frame helpers while `content/blocking.rs` owns popup blocking bounds and `content/dimensions.rs` owns shell extents, `frames/popup.rs` owns root popup placement constraints, `frames/row.rs` owns menu row/scrolled-frame metrics, and `frames/submenu.rs` owns submenu popup placement.

Primary menu clicks measure menu damage before and after `HostMenuStateData` changes, then request a regional repaint for the union of those rectangles. If the same click clears a focused text input, the old edit frame is unioned into that region as well. This keeps menu open/close and focus-outline cleanup on the paint-only path instead of requesting a full host frame update.

## Viewport Toolbar Damage

Viewport toolbar hit routing takes priority over the viewport body, but it no longer treats every primary press as a full host-frame repaint. Common mode and toggle controls dispatch their Rust callback, request a presentation frame update, and preserve the toolbar rectangle as the damage region. That lets the presenter repaint the changed toolbar labels or selected states without repainting the full editor window.

Play-mode, frame-selection, and view-alignment controls can affect session state, status text, and viewport camera state. `native_pointer/viewport_toolbar_damage.rs` is now a structural damage entry: `viewport_toolbar_damage/entry.rs` owns the public `viewport_toolbar_press_damage_frame(...)` route, `target.rs` owns the control-id classification, `status.rs` owns the center-band/status-bar damage assembly, and `union.rs` is a structural union entry whose `union/optional.rs` child owns optional damage merging while `union/visible.rs` owns visible-frame filtering and visible damage accumulation. The callback can still patch presentation state before repaint, but menu/title chrome no longer gets redrawn.

## Viewport Frame Update Retry

Viewport rendering can need more than one retained-host tick during startup. The first tick may only start the lazy render-framework resolver; no viewport exists yet, so `submit_extract_with_ui` returns without a frame. `RetainedEditorHost::tick()` now keeps `render_dirty` in that case and queues a frame-update redraw for the committed viewport content region through `UiHostWindow::request_frame_update_region`.

That redraw is queued as an external request and drained by the native event loop, so it does not call the frame callback while `RetainedEditorHost` is already mutably borrowed. When the resolver finishes, the next redraw invokes the normal frame update, submits the extract, polls the viewport image, and then the existing paint-only viewport-image patch repaints only the changed viewport rectangle. If the committed viewport rectangle is not visible yet, the queued request falls back to a full frame-update redraw so startup cannot stall on a zero-sized bootstrap frame.

## Pane Button Damage

Generic pane button presses now use `native_pointer/pane_button_damage.rs` as a structural damage entry. `pane_button_damage/entry.rs` owns pane press damage assembly, and `pane_button_damage/union.rs` is now a structural union entry with `union/optional.rs` owning optional damage merging and `union/visible.rs` owning visible-frame filtering plus union accumulation through shared host frame geometry. The dispatcher still calls the pane-specific callback first, then requests a frame update with the pane body, center band, and status bar as damage. This keeps selection and status changes visible without repainting the entire native window.

Pure release remains paint-local to the pane frame, and inert panes without a callback-visible change can still return idle. The regional press path is intentionally broader than the row/control hit because pane callbacks can update sibling pane chrome or status text.

## Close Prompt Damage

Close prompt action buttons are routed before pane and chrome hit testing. `native_pointer/close_prompt_damage.rs` is now a structural damage entry. `close_prompt_damage/entry.rs` returns the overlay/dialog union, `close_prompt_damage/union.rs` is a structural union entry, and `close_prompt_damage/union/visible.rs` owns visible-frame filtering plus rectangle union math through the shared frame-geometry helpers. The dispatcher keeps the required frame update attached to that bounded region. This removes the old unconditional full-frame request from save/discard/cancel button presses while still covering the modal overlay disappearing or changing state.

## Chrome Press Damage

Top-level chrome press dispatch no longer treats every successful click as a full native frame repaint. `native_pointer/chrome_damage.rs` owns the damage mapping for controls whose effect stays inside a known region: root document-tab press and close use the document dock, floating document-tab press and close use the owning floating-window frame, drawer-header tab press uses the corresponding drawer dock, and activity-rail press uses the center band because opening or swapping a drawer can move the document region.

`native_pointer/chrome_damage.rs` is now a structural chrome-press damage entry. `chrome_damage/entry.rs` preserves the public damage helper, `route.rs` owns the `ChromePointerRoute` dispatch entry, `route/dock.rs` owns activity-rail and document-dock frame selection plus visible-frame gating, `route/dock/drawer.rs` owns drawer-dock frame selection, `route/document.rs` owns root-document versus floating-document tab routing, `route/floating.rs` owns floating-window-header routing, `route/host_page.rs` owns host-page tab routing, `floating.rs` owns floating document-tab and floating-window-header damage, `host_page.rs` is now a structural host-page damage entry with `host_page/tabs.rs` owning host-page tab/project damage, `host_page/template_nodes.rs` owning page chrome template-node damage, and `host_page/status.rs` owning center-band/status damage. `union.rs` is now a structural union entry, and `union/visible.rs` owns visible-frame filtering plus frame union math through the shared frame-geometry helpers.

Floating-window header focus uses the union of all floating-window frames. The previous focused floating window is tracked in the retained app layer, not in the native host contract presentation packet, and focusing can reorder floating windows. The floating-layer union is therefore the conservative regional damage for z-order/focus changes: it avoids repainting menu, document, drawer, and status regions while still covering stacked floating-window pixels that can change.

The dispatcher still requests a frame update for these paths, so Rust callbacks can patch presentation state before repaint. It preserves the region damage so the presenter avoids redrawing menu chrome, title chrome, and other unaffected pixels. Host-page tabs now use a conservative multi-region union: page tab row, project path, page chrome template nodes, center band, and status bar. This covers page activation and status changes while keeping the menu/title chrome out of the repaint.

## Resize Capture

Resize splitters live in `HostResizeLayerData` and are hit by `route_top_level_chrome(...)` as `ChromePointerRoute::Resize`. A splitter press enters `native_pointer/drag_resize/resize_capture.rs`, starts a host resize session by storing `HostResizeStateData` in `HostContractState`, and emits `HOST_POINTER_DOWN`.

While that resize state is active, root pointer move dispatch short-circuits before tab dragging, menus, and pane routing. Every move emits `HOST_POINTER_MOVE`, updates the stored pointer coordinates, and requests a frame update with regional damage. Primary release emits `HOST_POINTER_UP`, clears the resize state, and uses the same regional damage path.

This is a capture-style contract: after the user presses the split bar, later move/up events do not need to remain physically over the splitter hitbox. The layout resize implementation in the retained host app can therefore receive the complete down/move/up sequence needed to adjust drawer and workspace bounds.

The resize damage rectangle is the committed center band, computed in `native_pointer/resize_damage.rs`. Layout changes can move drawer, document, splitter, and viewport frames, so the damage is intentionally broader than the splitter itself; it still avoids repainting menu chrome and status bar pixels that resize capture does not mutate.

## Tab Drag Release Damage

Tab drag release still emits `HOST_POINTER_UP` before clearing capture, but it no longer blindly requests a full native frame for every active drag. `native_pointer/drag_resize/tab_drag.rs` forwards the release callback, reads the updated `HostDragStateData.active_drag_target_group`, and asks the redraw-result layer to compute release damage from `native_pointer/tab_drag_damage.rs`.

Resolvable dock releases use this regional path. When `drag_source_group` and `active_drag_target_group` are the same local dock group, the redraw request keeps the required frame update and limits damage to that dock plus status. When both groups are known but different local docks, or when the target is a `document-left/right/top/bottom` edge split, the damage widens to center band plus status because the move can rearrange drawers and document chrome.

`native_pointer/tab_drag_damage.rs` is now a structural entry for tab-drag release damage. `tab_drag_damage/entry.rs` owns source/target capture plus same-group fast path, `entry/target.rs` owns cross-group target-family release damage, `groups.rs` owns local dock and document-edge group recognition, `floating.rs` is a structural floating-damage entry, `floating/group.rs` owns floating-window target lookup, `floating/target.rs` owns floating-target damage assembly, `regions.rs` owns same-group and center/status regional damage, and `union.rs` owns visible-frame filtering and frame union math.

Floating-window target groups are matched against the `FloatingWindowData` target-group fields in the committed presentation. A drop onto an existing floating window repaints that floating frame and also widens to center/status when the source was a local dock. Detach/drop-empty targets remain full-frame because the newly created floating-window bounds are not present in the old presentation packet used by native pointer dispatch.

## Pointer Rebuild Guard

Retained host pointer callbacks now call `RetainedEditorHost::use_committed_pointer_layout()` instead of `recompute_if_dirty()`. Pointer routing uses the last committed bridge frames, while presentation/layout/window dirty flags remain queued for `tick()` or explicit `refresh_ui()`. This prevents native pointer down/move/up/scroll callbacks from recursively rebuilding the full editor UI tree during hit routing.

The boundary test `pointer_handlers_do_not_force_slow_path_rebuilds` scans the pointer modules for this contract. It covers viewport, viewport toolbar, menu, workbench tab/header, asset tree/content/reference, hierarchy, detail scrollers, welcome recent-project pointers, and drag/resize docking pointer paths.

Bridge-local pointer surfaces also avoid redundant rebuilds. Activity rail, document tabs, drawer headers, host page tabs, viewport toolbar, and viewport overlay sync paths compare the incoming layout with the committed layout and return `false` without rebuilding when the layout is unchanged. Menu, hierarchy, detail scroll surfaces, and asset tree/content/reference lists use the stricter `layout + state` equality check, so hover, scroll, popup, and open-submenu state changes still rebuild while identical projection packets are ignored. The welcome recent list reads recent-project paths from `HostPresentationCache`; click syncs the cached paths and high-frequency move/scroll callbacks use a size-only sync that preserves them instead of rebuilding `EditorChromeSnapshot` on every hover event. This keeps high-frequency pointer routing from paying a fresh surface build when the projection pass re-sends identical frames, while still rebuilding immediately after real geometry, candidate, or state changes.

## Template Hover Damage

Template-backed pane bodies can now update visible hover state without rebuilding the retained presentation. Pointer move dispatch stores the active template control id and frame in `HostPaneInteractionStateData`, then `window.rs` overlays that hover bit when publishing the next host presentation. The base presentation cache remains unchanged, so repeated hover frames do not increment `presentation_rebuild_count`.

`native_pointer/template_hover_damage.rs` computes the union of the old and new template hover frames. Pointer moves that enter, leave, or switch template nodes request only that regional repaint, while repeated moves over the same node return idle. When the move also touches a native-hover pane such as Hierarchy, Welcome, Assets, or AssetBrowser, the move-specific pane router prefers the native hover target over overlapping `TemplateNode` hits, preserving the existing row-hover callbacks and damage semantics for those panes.

The profile closeout `20260515-211644-idle_hover` passed the automated interaction gate with one regional redraw, `gpu_draw_calls=10`, `gpu_visible_draw_items=12`, zero hotspot alerts, and no software fallback. This gives `idle_hover` real GPU patch evidence rather than only pointer-frame samples.

## Drawer Active Selection Normalization

Drawer headers and tab dragging can expose stale layout states when `drawer.tab_stack.active_tab` and `drawer.active_view` diverge. `LayoutManager::apply(...)` now normalizes all activity-window drawers after every changed layout command and before syncing legacy drawer mirrors.

For each drawer, the normalization rule is:

1. If the drawer is collapsed or has no tabs, both `active_tab` and `active_view` become `None`.
2. Otherwise, keep the existing valid `active_tab` when possible.
3. If `active_tab` is missing or stale, fall back to a valid `active_view`.
4. Write the same resolved id back to both fields.

The resulting invariant is zero or one active drawer item per drawer. This prevents a selected drawer title drag/drop path from leaving one active id in the tab stack and another in the drawer view field, which looked like multi-selection at the target location.

The 2026-06-18 button/scroll dispatch split reduced `native_pointer.rs` to 33 lines. `native_pointer/button_dispatch.rs` is 460 lines, `native_pointer/scroll_dispatch.rs` is 109 lines, and `native_pointer/move_dispatch.rs` remains 133 lines. The parent now stays structural: it declares the subtree, re-exports dispatch entries, and keeps shared pointer constants/state; button, move, scroll, drag/resize, routing, damage, and geometry responsibilities live in named children.

The 2026-06-21 move dispatch split reduced `native_pointer/move_dispatch.rs` from 141 lines to a 7-line structural entry. `move_dispatch/entry.rs` is 40 lines and owns root move ordering and perf scope setup, `menu.rs` is 27 lines and owns menu hover dispatch/damage, `workbench.rs` is 40 lines and owns Workbench template hover/structured-row mapping, `pane.rs` is 82 lines and owns pane/asset/template/viewport move callback fanout, and `clear.rs` is 13 lines and owns final hovered-template cleanup damage. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a move dispatch entry/menu/workbench/pane/clear ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 move dispatch entry body/capture split reduced `native_pointer/move_dispatch/entry.rs` from 35 lines to a 21-line perf-scope and top-level ordering entry. `entry/capture.rs` is 15 lines and owns resize/tab-drag capture priority, while `entry/body.rs` is 23 lines and owns presentation capture plus menu/Workbench/pane/final-clear hover route order. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a move dispatch entry body/capture ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 Workbench move hover split reduced `native_pointer/move_dispatch/workbench.rs` from 40 lines to a 25-line Workbench move route/redraw owner. `workbench/hover.rs` is 21 lines and owns structured-row hover state writes for Workbench options/menu items plus template-node hover state writes for ordinary Workbench nodes. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a Workbench move hover ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane move target split reduced `native_pointer/move_dispatch/pane.rs` from 82 lines to an 8-line structural entry. `pane/entry.rs` is 44 lines and owns pane pointer-move route/redraw sequencing, `pane/native.rs` is 24 lines and owns Hierarchy/Welcome move callbacks, `pane/asset.rs` is 35 lines and owns asset tree/content/reference move callbacks, `pane/template.rs` is 6 lines and owns template hover state update, `pane/viewport.rs` is 20 lines and owns viewport move forwarding, and `pane/passive.rs` is 5 lines and owns inert hover cleanup. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane move target native/asset/template/viewport/passive ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane move entry target split reduced `native_pointer/move_dispatch/pane/entry.rs` from 44 lines to a 25-line route/redraw entry. `pane/entry/target.rs` is 29 lines and owns target-family dispatch across native, asset, template, viewport, and passive move handling. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane move entry target dispatch ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 viewport toolbar damage split reduced `native_pointer/viewport_toolbar_damage.rs` from 76 lines to a 6-line structural entry. `viewport_toolbar_damage/entry.rs` is 19 lines and owns public toolbar damage routing, `target.rs` is 6 lines and owns viewport/status-affecting control classification, `status.rs` is 19 lines and owns center-band/status damage assembly, and `union.rs` is 47 lines and owns frame visibility plus union math. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a viewport toolbar damage entry/status/target/union ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 viewport toolbar union split reduced `native_pointer/viewport_toolbar_damage/union.rs` from 47 lines to a 5-line structural entry. `union/optional.rs` is 9 lines and owns optional damage merging via shared host frame geometry, while `union/visible.rs` is 21 lines and owns visible-frame filtering plus visible damage accumulation. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a viewport toolbar union optional/visible ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane button damage split reduced `native_pointer/pane_button_damage.rs` from 61 lines to a 4-line structural entry. `pane_button_damage/entry.rs` is 23 lines and owns pane press damage assembly, while `pane_button_damage/union.rs` is 47 lines and owns visible-frame filtering plus union math. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane button damage entry/union ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane button union split reduced `native_pointer/pane_button_damage/union.rs` from 47 lines to a 5-line structural entry. `union/optional.rs` is 9 lines and owns optional damage merging via shared host frame geometry, while `union/visible.rs` is 15 lines and owns visible-frame filtering plus visible damage accumulation. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane button union optional/visible ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 close prompt damage split reduced `native_pointer/close_prompt_damage.rs` from 46 lines to a 4-line structural entry. `close_prompt_damage/entry.rs` is 16 lines and owns close-prompt overlay/dialog damage assembly, while `close_prompt_damage/union.rs` is 36 lines and owns visible-frame filtering plus union math. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a close prompt damage entry/union ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 close prompt damage union visible split reduced `native_pointer/close_prompt_damage/union.rs` from 36 lines to a 3-line structural entry. `union/visible.rs` is 21 lines and owns visible-frame filtering plus union accumulation through the shared `frame_geometry` helpers. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a close prompt damage union visible ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 redraw result split reduced `native_pointer/redraw_result.rs` from 132 lines to a 12-line structural entry. `redraw_result/pointer_move.rs` is 63 lines and owns pane pointer-move redraw decisions, `workbench.rs` is 20 lines and owns Workbench template-node redraw, `resize.rs` is 15 lines and owns resize redraw, `chrome.rs` is 21 lines and owns chrome press redraw, `tab_drag.rs` is 16 lines and owns tab-drag release redraw, and `hierarchy.rs` is 20 lines and owns hierarchy row damage geometry. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a redraw result pointer/workbench/resize/chrome/tab-drag/hierarchy ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pointer-move redraw split reduced `native_pointer/redraw_result/pointer_move.rs` from 63 lines to a 5-line structural entry. `pointer_move/entry.rs` is 33 lines and owns generic viewport/no-change short-circuits plus target-family routing, `pointer_move/hierarchy.rs` is 38 lines and owns hierarchy scroll/row hover damage, and `pointer_move/template.rs` is 20 lines and owns non-hierarchy template-hover damage merging. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pointer-move redraw entry/hierarchy/template ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pointer-move hierarchy redraw split reduced `native_pointer/redraw_result/pointer_move/hierarchy.rs` from 35 lines to a 20-line hierarchy-redraw selector. `hierarchy/row.rs` is 27 lines and owns hierarchy row-hover damage union, while `hierarchy/scroll.rs` is 11 lines and owns scroll-change damage union. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pointer-move hierarchy redraw row/scroll ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 scroll dispatch split reduced `native_pointer/scroll_dispatch.rs` from 114 lines to a 6-line structural entry. `scroll_dispatch/entry.rs` is 27 lines and owns root scroll ordering and perf scope setup, `menu.rs` is 25 lines and owns menu scroll callback/damage, `pane.rs` is 96 lines and owns native pane scroll callback fanout, and `viewport.rs` is 20 lines and owns viewport scroll event forwarding. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a scroll dispatch entry/menu/pane/viewport ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 scroll pane target split reduced `native_pointer/scroll_dispatch/pane.rs` from 96 lines to a 7-line structural entry. `pane/entry.rs` is 35 lines and owns pane scroll routing/result ordering, `pane/native.rs` is 63 lines and owns native pane scroll callbacks, `pane/asset.rs` is 47 lines and owns asset pane scroll callbacks, `pane/viewport_target.rs` is 21 lines and owns viewport scroll handoff, and `pane/passive.rs` is 11 lines and owns inert pane target classification. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a scroll pane target ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 asset pane scroll split reduced `native_pointer/scroll_dispatch/pane/asset.rs` from 45 lines to a 17-line structural entry. `asset/tree.rs` is 20 lines and owns AssetTree scroll callbacks, `asset/content.rs` is 20 lines and owns AssetContent scroll callbacks, and `asset/reference.rs` is 21 lines and owns AssetReference scroll callbacks. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, an asset pane scroll tree/content/reference ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 native pane scroll split reduced `native_pointer/scroll_dispatch/pane/native.rs` from 63 lines to a 7-line structural entry. `native/entry.rs` is 24 lines and owns target selection, `native/hierarchy.rs` is 18 lines and owns hierarchy scroll callbacks, `native/welcome.rs` is 18 lines and owns welcome scroll callbacks, `native/panels.rs` is 33 lines and owns console/inspector scroll callbacks, and `native/browser.rs` is 18 lines and owns browser asset details scroll callbacks. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a native pane scroll hierarchy/welcome/panels/browser ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-18 button subtree split reduced `native_pointer/button_dispatch.rs` to 173 lines. The new `button_dispatch/chrome_press.rs`, `button_dispatch/pane_callbacks.rs`, `button_dispatch/text_focus.rs`, `button_dispatch/close_prompt_hit.rs`, and `button_dispatch/viewport_button.rs` keep callback fanout, text focus, close-prompt hit testing, and button-id conversion out of the root button event-ordering module.

The 2026-06-21 button dispatch entry split reduced `native_pointer/button_dispatch.rs` from 173 lines to a 12-line structural entry. `button_dispatch/entry.rs` is 92 lines and owns the root button event order; `release.rs` is 13 lines and owns active capture release; `close_prompt.rs` is 35 lines and owns close-prompt dispatch/blocking; `menu_press.rs` is 29 lines and owns menu primary-click damage; `chrome_route.rs` is 24 lines and owns top-level chrome arming/redraw sequencing; `workbench.rs` is 57 lines and owns Workbench context menu/template primary press/text focus routing; `text_focus.rs` is 44 lines and owns focused text activation/cleanup. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch entry/release/menu/chrome/workbench ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 close prompt action dispatch split reduced `native_pointer/button_dispatch/close_prompt.rs` from 36 lines to a 35-line hit-blocking/public dispatch owner. `close_prompt/action.rs` is 27 lines and owns primary-press gating, `UiHostContext` close-prompt callback invocation, and region/full-frame damage result mapping. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a close prompt action dispatch ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 text focus split reduced `native_pointer/button_dispatch/text_focus.rs` from 44 lines to a 5-line structural entry. `text_focus/activation.rs` is 26 lines and owns template text-input focus target resolution plus `HostTextInputFocusData` assembly, while `text_focus/clear.rs` is 24 lines and owns primary-press focused-text cleanup. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a text focus activation/clear ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 Workbench button split reduced `native_pointer/button_dispatch/workbench.rs` from 59 lines to a 6-line structural entry. `workbench/entry.rs` is 42 lines and owns Workbench hit routing plus button-state ordering, `workbench/primary.rs` is 38 lines and owns text-input focus plus template primary activation, `workbench/secondary.rs` is 24 lines and owns context menu request/damage, and `workbench/release.rs` is 8 lines and owns release damage. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a Workbench button entry/primary/secondary/release ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 Workbench button entry split reduced `native_pointer/button_dispatch/workbench/entry.rs` from 42 lines to a 31-line hit/state entry. `workbench/entry/pressed.rs` is 33 lines and owns primary/secondary pressed dispatch selection, while `workbench/entry/released.rs` is 10 lines and owns release dispatch handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a Workbench button entry pressed/released ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 Workbench primary split reduced `native_pointer/button_dispatch/workbench/primary.rs` from 35 lines to a 21-line primary-press entry. `primary/text_input.rs` is 21 lines and owns text-input focus detection/result mapping, `primary/activation.rs` is 19 lines and owns template primary callback dispatch plus frame-update result, and `primary/damage.rs` is 10 lines and owns hit damage union. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a Workbench primary activation/text-input/damage ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome press split reduced `native_pointer/button_dispatch/chrome_press.rs` from 82 lines to a 7-line structural entry. `chrome_press/entry.rs` is 71 lines and owns route-family dispatch, `chrome_press/rails.rs` is 13 lines and owns activity rail callback fanout, `chrome_press/tabs.rs` is 68 lines and owns host-page/document/drawer tab callbacks, `chrome_press/floating.rs` is 7 lines and owns floating header callbacks, and `chrome_press/resize.rs` is 7 lines and owns resize arming. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome press entry/rails/tabs/floating/resize ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome press entry split reduced `native_pointer/button_dispatch/chrome_press/entry.rs` from 71 lines to an 18-line structural entry. `entry/tabs.rs` is 62 lines and owns host-page/document/drawer tab route dispatch, while `entry/shell.rs` is 27 lines and owns activity-rail, floating-window header, and resize route dispatch. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome press entry tabs/shell ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome press tab route split reduced `native_pointer/button_dispatch/chrome_press/entry/tabs.rs` from 62 lines to a 13-line structural entry. `entry/tabs/host_page.rs` is 20 lines and owns host-page tab route dispatch, `entry/tabs/document.rs` is 31 lines and owns document tab route dispatch, and `entry/tabs/drawer.rs` is 32 lines and owns drawer header tab route dispatch. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome press tab route host/document/drawer ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome press tabs split reduced `native_pointer/button_dispatch/chrome_press/tabs.rs` from 68 lines to a 7-line structural entry. `tabs/host_page.rs` is 14 lines and owns host-page tab callback fanout, `tabs/document.rs` is 36 lines and owns document tab/close callback fanout, and `tabs/drawer.rs` is 23 lines and owns drawer header callback fanout. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome press tabs host/document/drawer ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome press document tab callback split reduced `native_pointer/button_dispatch/chrome_press/tabs/document.rs` from 35 lines to a 25-line callback-selector entry. `tabs/document/body.rs` is 23 lines and owns document tab body callback fanout, while `tabs/document/close.rs` is 23 lines and owns document tab close callback fanout. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome press document tab body/close callback ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch root-entry split reduced `native_pointer/button_dispatch/entry.rs` from 102 lines to 86 lines. `button_dispatch/primary_press.rs` is 31 lines and owns primary-press menu/outside-popup/chrome ordering, while `button_dispatch/pane_route.rs` is 31 lines and owns pane hit routing handoff from the root button dispatcher. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch root entry primary/pane-route ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch body route split reduced `native_pointer/button_dispatch/entry.rs` from 86 lines to 68 lines. `entry/body_routes.rs` is 49 lines and owns Workbench body routing, pane route handoff, and cleared-text fallback damage. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch body route ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch root-entry input/release/primary-overlay split moved focused input, release-capture, and primary-overlay responsibilities into child owners. `entry/input.rs` is 22 lines and owns button defaulting, presentation capture, and viewport button-id lookup; `entry/release_capture.rs` is 17 lines and owns primary-release resize/tab-drag completion gating; `entry/primary_overlays.rs` is 20 lines and owns primary-press overlay dispatch gating. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch root entry input/release/primary-overlay ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch root-entry sequence split reduced `native_pointer/button_dispatch/entry.rs` from 70 lines to a 6-line structural entry. `entry/sequence.rs` is now a structural sequencing entry, with `entry/sequence/entry.rs` owning performance scope setup and root input capture, while `entry/sequence/steps.rs` owns release, close-prompt, text-focus, primary-overlay, and body-route ordering. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch root entry sequence ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch root-entry sequence child split reduced `native_pointer/button_dispatch/entry/sequence.rs` from 57 lines to a 3-line structural entry. `entry/sequence/entry.rs` is 23 lines and owns performance scope setup plus root input capture; `entry/sequence/steps.rs` is 48 lines and owns ordered dispatch through release capture, close prompt, text focus, primary overlays, and body routes. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch root entry sequence entry/steps ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch root-entry sequence steps split reduced `native_pointer/button_dispatch/entry/sequence/steps.rs` from 50 lines to a 42-line ordering entry. `steps/close_prompt.rs` is 16 lines and owns close-prompt blocking dispatch, `steps/text_focus.rs` is 14 lines and owns focused-text cleanup dispatch, and `steps/body.rs` is 27 lines and owns final body-route handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch root entry sequence steps body/close-prompt/text-focus ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button dispatch root-entry sequence release/primary-overlay split reduced `native_pointer/button_dispatch/entry/sequence/steps.rs` from 42 lines to a 38-line ordering entry. `steps/release.rs` is 16 lines and owns release-capture completion, while `steps/primary_overlays.rs` is 26 lines and owns primary overlay dispatch handoff with cleared-text damage propagation. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button dispatch root entry sequence steps release/primary-overlay ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 button body route subtree split reduced `native_pointer/button_dispatch/entry/body_routes.rs` from 49 lines to an 8-line structural entry. `body_routes/entry.rs` is 30 lines and owns input capture, `body_routes/input.rs` is 16 lines and owns the route input model, `body_routes/sequence.rs` is 18 lines and owns Workbench-first routing order, `body_routes/workbench.rs` is 18 lines and owns Workbench handoff, `body_routes/pane.rs` is 19 lines and owns pane-route handoff, and `body_routes/fallback.rs` is 11 lines and owns cleared-text fallback damage. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a button body route entry/input/sequence/workbench/pane/fallback ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome damage split reduced `native_pointer/chrome_damage.rs` from 141 lines to a 7-line structural entry. `chrome_damage/entry.rs` is 11 lines and owns the dispatch-facing helper, `route.rs` is 41 lines and owns route-family selection, `floating.rs` is 35 lines and owns floating document-tab/header damage, `host_page.rs` is 47 lines and owns host-page tab/project/template/status union damage, and `union.rs` is 36 lines and owns visible-frame filtering plus union math. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome damage entry/route/floating/host-page/union ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome damage route split reduced `native_pointer/chrome_damage/route.rs` from 41 lines to a 29-line route-kind dispatch entry. `route/dock.rs` is 34 lines and owns dock-region damage selection, `route/document.rs` is 17 lines and owns root-document versus floating-document tab routing, `route/floating.rs` is 8 lines and owns floating-window-header routing handoff, and `route/host_page.rs` is 7 lines and owns host-page tab routing handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome damage route dock/document/floating/host-page ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome damage dock drawer split reduced `native_pointer/chrome_damage/route/dock.rs` from 39 lines to a 35-line dock damage owner. `route/dock/drawer.rs` is 17 lines and owns drawer surface-key to dock-frame mapping plus visible-frame gating handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome damage dock drawer ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome damage union visible split reduced `native_pointer/chrome_damage/union.rs` from 36 lines to a 3-line structural entry. `union/visible.rs` is 21 lines and owns visible-frame filtering plus union accumulation through the shared `frame_geometry` helpers. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome damage union visible ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome host-page damage split reduced `native_pointer/chrome_damage/host_page.rs` from 47 lines to a 19-line structural entry. `host_page/tabs.rs` is 22 lines and owns page tab/project damage, `host_page/template_nodes.rs` is 28 lines and owns page chrome template-node damage conversion, and `host_page/status.rs` is 15 lines and owns center-band/status damage accumulation. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome host-page tabs/template/status ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-18 pane callback subtree split reduced `native_pointer/button_dispatch/pane_callbacks.rs` to 126 lines. The new `pane_callbacks/native_panes.rs`, `pane_callbacks/asset_panes.rs`, `pane_callbacks/viewport.rs`, and `pane_callbacks/template_nodes.rs` keep concrete pane-family callback fanout out of the pane button ordering entry.

The 2026-06-21 native pane button split reduced `native_pointer/button_dispatch/pane_callbacks/native_panes.rs` from 47 lines to a 5-line structural entry. `native_panes/hierarchy.rs` is 31 lines and owns hierarchy pointer event/click callback dispatch, while `native_panes/welcome.rs` is 21 lines and owns welcome recent click dispatch. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a native pane button hierarchy/welcome ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane callback target split reduced `native_pointer/button_dispatch/pane_callbacks.rs` from 126 lines to a 9-line structural entry. `pane_callbacks/target.rs` is 106 lines and owns pane-button target routing, `fallback.rs` is 34 lines and owns passive pane/final damage fallback, and `kind.rs` is 18 lines and owns native/viewport button event id mapping. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane callback target/kind/fallback ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane callback target-domain split reduced `native_pointer/button_dispatch/pane_callbacks/target.rs` from 106 lines to an 8-line structural entry. `target/entry.rs` is 74 lines and owns pane-button target ordering, while `target/native.rs`, `asset.rs`, `viewport.rs`, `template.rs`, and `passive.rs` own native pane, asset pane, viewport, template-node, and passive target selection respectively. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane callback target domain ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane callback viewport target split reduced `native_pointer/button_dispatch/pane_callbacks/target/viewport.rs` from 37 lines to a 39-line viewport-target dispatch entry. `target/viewport/toolbar.rs` is 30 lines and owns viewport-toolbar target handoff to the viewport toolbar callback subtree, while `target/viewport/body.rs` is 19 lines and owns viewport-body target handoff to the viewport body callback subtree. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane callback viewport target toolbar/body ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane callback target entry split reduced `native_pointer/button_dispatch/pane_callbacks/target/entry.rs` from 74 lines to 54 lines. `entry/callback_targets.rs` is 19 lines and owns native/asset callback-only target dispatch, while `entry/result_targets.rs` is 49 lines and owns viewport/template/passive result-producing target dispatch. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane callback target entry callback/result ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane callback target entry sequence split reduced `native_pointer/button_dispatch/pane_callbacks/target/entry.rs` from 54 lines to a 5-line structural entry. `entry/input.rs` is 14 lines and owns pane-button dispatch input, `entry/sequence.rs` is 3 lines and owns sequence module wiring, `entry/sequence/entry.rs` is 27 lines and owns public pane-button input capture, and `entry/sequence/run.rs` is 48 lines and owns callback/result/fallback ordering. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane callback target entry input/sequence/run ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane callback target sequence-run split reduced `native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run.rs` from 48 lines to a 24-line structural ordering entry. `run/callback.rs` is 27 lines and owns callback-only target fallback damage, `run/result.rs` is 21 lines and owns result-producing target dispatch, and `run/fallback.rs` is 13 lines and owns final pane-button fallback damage. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane callback target sequence run callback/result/fallback ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 result target split reduced `native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets.rs` from 49 lines to an 8-line structural entry. `result_targets/entry.rs` is 34 lines and owns public input capture, `result_targets/input.rs` is 19 lines and owns the route input model, `result_targets/sequence.rs` is 18 lines and owns viewport/template/passive result ordering, `result_targets/viewport.rs` is 19 lines and owns viewport result handoff, `result_targets/template.rs` is 17 lines and owns template-node result handoff, and `result_targets/passive.rs` is 14 lines and owns passive target fallback. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a result target entry/input/sequence/viewport/template/passive ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 asset pane target split reduced `native_pointer/button_dispatch/pane_callbacks/target/asset.rs` from 50 lines to a 6-line structural entry. `asset/entry.rs` is 50 lines and owns AssetTree/AssetContent/AssetReference target matching, `asset/tree.rs` is 16 lines and owns asset-tree target handoff, `asset/content.rs` is 20 lines and owns asset-content target handoff, and `asset/reference.rs` is 21 lines and owns asset-reference target handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, an asset pane target entry/tree/content/reference ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 asset pane target route split reduced `native_pointer/button_dispatch/pane_callbacks/target/asset/entry.rs` from 50 lines to 25 lines. `asset/tree.rs` is 19 lines and owns AssetTree target matching, `asset/content.rs` is 29 lines and owns AssetContent target matching, and `asset/reference.rs` is 30 lines and owns AssetReference target matching; `asset/entry.rs` now only sequences those target routes. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, an asset pane target tree/content/reference route ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 viewport pane callback split reduced `native_pointer/button_dispatch/pane_callbacks/viewport.rs` from 57 lines to a 5-line structural entry. `viewport/toolbar.rs` is 43 lines and owns viewport toolbar button clicks plus toolbar damage, while `viewport/viewport.rs` is 19 lines and owns viewport body pointer event forwarding and cleared text-input damage. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a viewport pane callback toolbar/body ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 viewport toolbar callback split reduced `native_pointer/button_dispatch/pane_callbacks/viewport/toolbar.rs` from 43 lines to a 5-line structural entry. `toolbar/entry.rs` is 31 lines and owns primary-press gating plus click/damage ordering, `toolbar/click.rs` is 23 lines and owns viewport-toolbar click callback fanout, and `toolbar/damage.rs` is 21 lines and owns toolbar damage result mapping. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a viewport toolbar entry/click/damage ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 asset pane callback split reduced `native_pointer/button_dispatch/pane_callbacks/asset_panes.rs` from 85 lines to a 7-line structural entry. `asset_panes/tree.rs` is 24 lines and owns asset tree primary-click fanout, `asset_panes/content.rs` is 35 lines and owns asset content pointer event/click fanout, and `asset_panes/reference.rs` is 38 lines and owns asset reference pointer event/click fanout. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, an asset pane callback tree/content/reference ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 asset pane reference callback click split reduced `native_pointer/button_dispatch/pane_callbacks/asset_panes/reference.rs` from 38 lines to a 33-line AssetReference pointer-event fanout owner. `reference/click.rs` is 27 lines and owns primary-click state/button gating plus click callback fanout. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, an asset pane reference callback click ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-18 routing subtree split reduced `native_pointer/routing.rs` to 88 lines. The new `routing/chrome.rs`, `routing/panes.rs`, `routing/geometry.rs`, and `routing/workbench.rs` keep chrome hit routing, pane hit routing, route geometry, and Workbench template-node route entry separate from shared route payload declarations.

The 2026-06-21 routing route payload split reduced `native_pointer/routing.rs` from 95 lines to a 17-line structural entry. `routing/chrome_route.rs` is 37 lines and owns `ChromePointerRoute`, while `routing/pane_route.rs` is 48 lines and owns `PanePointerRoute` plus `PanePointerTarget`. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a routing route payload ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 routing pane route payload split reduced `native_pointer/routing/pane_route.rs` from 45 lines to a 4-line structural entry. `pane_route/route.rs` is 27 lines and owns `PanePointerRoute` geometry/local-coordinate construction, while `pane_route/target.rs` is 19 lines and owns `PanePointerTarget`. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a routing pane route route/target ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 routing geometry split reduced `native_pointer/routing/geometry.rs` from 50 lines to a 7-line structural entry. `geometry/containment.rs` is 14 lines and owns point-in-frame hit testing, while `geometry/frames.rs` is 37 lines and owns translated frames, side-dock content frames, and floating-window content frames. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a routing geometry containment/frames ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome routing subtree split reduced `native_pointer/routing/chrome.rs` from 232 lines to a 5-line structural entry. `chrome/top_level.rs` is 94 lines and owns the root route order plus resize splitter checks; `chrome/tabs.rs` is 86 lines and owns document tab, drawer header, and host-page tab route construction; `chrome/rails.rs` is 44 lines and owns activity-rail containment; `chrome/floating.rs` is 36 lines and owns floating-window header/tab routing. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome routing top-level/tabs/rails/floating ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome floating header split reduced `native_pointer/routing/chrome/floating.rs` from 36 lines to a 19-line floating-window iteration owner. `floating/header.rs` is 28 lines and owns translated header containment, document-tab hit priority, and `FloatingWindowHeader` fallback route construction. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome floating header ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome rail split reduced `native_pointer/routing/chrome/rails.rs` from 46 lines to a 26-line route entry. `rails/frame.rs` is 31 lines and owns rail-frame containment, while `rails/hit.rs` is 20 lines and owns translated activity-rail button hit testing. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome rail frame/hit ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome tabs route split reduced `native_pointer/routing/chrome/tabs.rs` from 90 lines to a 7-line structural entry. `tabs/document.rs` is 44 lines and owns document tab/close hit routes, `tabs/drawer.rs` is 33 lines and owns drawer-header tab routes, and `tabs/host_page.rs` is 24 lines and owns host-page tab routes. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome tabs document/drawer/host-page ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome document tab split reduced `native_pointer/routing/chrome/tabs/document.rs` from 44 lines to a 28-line route-order entry. `tabs/document/close.rs` is 29 lines and owns close-button hit route construction, while `tabs/document/body.rs` is 29 lines and owns tab-body hit route construction. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome document tab body/close ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome top-level route split reduced `native_pointer/routing/chrome/top_level.rs` from 98 lines to a 5-line structural entry. `top_level/entry.rs` is 64 lines and owns root chrome route ordering, `top_level/drawers.rs` is 39 lines and owns drawer-header route sequencing, and `top_level/resize.rs` is 20 lines and owns splitter hit routing. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome top-level entry/drawer/resize ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome top-level drawer split reduced `native_pointer/routing/chrome/top_level/drawers.rs` from 39 lines to a 20-line drawer-header sequence owner. `drawers/side.rs` is 21 lines and owns side-dock drawer header routing for left/right docks, while `drawers/bottom.rs` is 18 lines and owns bottom-dock drawer header routing. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome top-level drawer side/bottom ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 chrome top-level docked/side-rail split reduced `native_pointer/routing/chrome/top_level/entry.rs` from 64 lines to 38 lines. `top_level/docked.rs` is 23 lines and owns document-dock tab routing, while `top_level/side_rails.rs` is 30 lines and owns left/right activity-rail routing. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a chrome top-level docked/side-rail ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane routing subtree split reduced `native_pointer/routing/panes.rs` from 164 lines to a 7-line structural entry. `panes/entry.rs` is 80 lines and owns floating-window/local dock route iteration, `panes/pane.rs` is 66 lines and owns pane body route construction plus viewport toolbar/template-node priority, `panes/mode.rs` is 17 lines and owns pointer-move template-hit gating, and `panes/target.rs` is 18 lines and owns pane-kind target mapping. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane routing entry/mode/pane/target ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane routing entry split reduced `native_pointer/routing/panes/entry.rs` from 84 lines to an 8-line structural entry. `entry/route.rs` is 32 lines and owns the public default/pointer-move route entry points plus floating-before-local ordering, `entry/floating.rs` is 32 lines and owns floating-window reverse hit priority, and `entry/local.rs` is 50 lines and owns document/left/right/bottom dock pane routing. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane routing entry route/floating/local ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane routing local dock split reduced `native_pointer/routing/panes/entry/local.rs` from 50 lines to 33 lines. `entry/local/document.rs`, `entry/local/side.rs`, and `entry/local/bottom.rs` are 23 lines each and own document, side, and bottom dock content-frame routing into `pane_route_from_pane(...)` respectively. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane routing local dock document/side/bottom ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane route split reduced `native_pointer/routing/panes/pane.rs` from 67 lines to a 4-line structural entry. `pane/entry.rs` is 47 lines and owns pane body/template-node route construction, while `pane/toolbar.rs` is 25 lines and owns viewport toolbar route construction. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane route entry/toolbar ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane route viewport-body split reduced `native_pointer/routing/panes/pane/entry.rs` from 45 lines to 37 lines. `entry/viewport_body.rs` is 42 lines and owns Scene/Game toolbar hit priority plus body-frame adjustment before template-node and pane-kind target routing continue. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane route viewport body ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane route viewport-body child split reduced `native_pointer/routing/panes/pane/entry/viewport_body.rs` from 42 lines to a 22-line structural route sequence. `viewport_body/toolbar.rs` is 26 lines and owns Scene/Game toolbar eligibility, toolbar frame construction, and toolbar hit routing; `viewport_body/body.rs` is 10 lines and owns body-frame adjustment below the toolbar; `viewport_body/model.rs` is 20 lines and owns `ViewportBodyRoute`. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane route viewport body toolbar/body/model ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 pane route entry template-hit split reduced `native_pointer/routing/panes/pane/entry.rs` from 37 lines to a 29-line pane route sequencing owner. `entry/template.rs` is 22 lines and owns pointer-move template-node eligibility, hit-test handoff, and `PanePointerTarget::TemplateNode` route construction. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a pane route entry template-hit ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-18 menu geometry subtree split reduced `native_pointer/menu_geometry.rs` to 8 lines. The new `menu_geometry/bar.rs`, `menu_geometry/popup.rs`, `menu_geometry/damage.rs`, and `menu_geometry/frames.rs` keep menu-bar containment, popup containment, damage calculation, and shared popup frame math separated while preserving the original menu dispatch entry points.

The 2026-06-21 menu geometry frame split reduced `native_pointer/menu_geometry/frames.rs` from 167 lines to a 17-line structural entry. `frames/chrome.rs` is 35 lines and owns menu chrome/top-bar fallback frames, `frames/content.rs` is 49 lines and owns shell content extents plus popup blocking bounds, `frames/popup.rs` is 34 lines and owns root popup constraints, `frames/row.rs` is 34 lines and owns scrolled menu frames plus popup row metrics, and `frames/submenu.rs` is 35 lines and owns submenu popup constraints. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry frame chrome/content/popup/row/submenu ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry content split reduced `native_pointer/menu_geometry/frames/content.rs` from 46 lines to a 6-line structural entry. `content/blocking.rs` is 22 lines and owns popup blocking frame construction, while `content/dimensions.rs` is 22 lines and owns shell content width/height calculation. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry content blocking/dimensions ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry damage split reduced `native_pointer/menu_geometry/damage.rs` from 91 lines to a 6-line structural entry. `damage/entry.rs` is 54 lines and owns menu damage rectangle assembly, while `damage/stack.rs` is 41 lines and owns submenu stack-bottom traversal. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry damage entry/stack ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry damage stack split reduced `native_pointer/menu_geometry/damage/stack.rs` from 41 lines to a 30-line stack-bottom accumulation owner. `damage/stack/level.rs` is 40 lines and owns single-level submenu branch validation, scroll offset selection, anchor row construction, constrained popup frame construction, and child item handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry damage stack level ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry damage stack level frame split reduced `native_pointer/menu_geometry/damage/stack/level.rs` from 43 lines to a 40-line branch/child handoff owner. `damage/stack/level/frame.rs` is 29 lines and owns scroll-offset selection, anchor row construction, constrained submenu popup-frame construction, and child-count height conversion. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry damage stack level frame ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry damage popup-bottom split reduced `native_pointer/menu_geometry/damage/entry.rs` from 54 lines to 25 lines. `damage/popup_bottom.rs` is 39 lines and owns opened root-menu popup lookup, root popup frame construction, and handoff to submenu stack-bottom traversal; `damage/entry.rs` now only assembles the final menu damage frame. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry damage popup-bottom ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry damage popup-bottom root split reduced `native_pointer/menu_geometry/damage/popup_bottom.rs` from 37 lines to a 13-line open-menu guard and fallback owner. `damage/popup_bottom/root.rs` is 30 lines and owns opened root-menu lookup, root popup frame construction, and stack-bottom handoff. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry damage popup-bottom root ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry popup split reduced `native_pointer/menu_geometry/popup.rs` from 87 lines to a 4-line structural entry. `popup/entry.rs` is 44 lines and owns root popup/blocking-region containment, while `popup/nested.rs` is 50 lines and owns nested submenu hit traversal. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry popup entry/nested ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry popup entry split reduced `native_pointer/menu_geometry/popup/entry.rs` from 44 lines to a 21-line hit-order entry. `popup/entry/root.rs` is 42 lines and owns opened root-menu lookup plus root popup frame construction, while `popup/entry/blocking.rs` is 12 lines and owns popup blocking-region containment. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry popup entry root/blocking ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry popup root split reduced `native_pointer/menu_geometry/popup/entry/root.rs` from 42 lines to a 22-line root popup result assembly owner. `root/source.rs` is 34 lines and owns open-menu index validation, menu chrome row lookup, popup sizing capture, and menu item handoff; `root/frame.rs` is 15 lines and owns scrolled menu-frame conversion plus constrained root popup frame construction. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry popup root source/frame ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu popup submenu-frame split reduced `native_pointer/menu_geometry/popup/nested.rs` from 50 lines to 41 lines. `popup/submenu_frame.rs` is 24 lines and owns submenu scroll offset, row anchor, popup height, and constrained submenu popup-frame construction. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu popup submenu-frame ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry nested level split moved single-level submenu branch advancement out of `native_pointer/menu_geometry/popup/nested.rs`. `nested.rs` is now a 42-line path traversal owner, while `nested/level.rs` is 40 lines and owns branch child validation, nested popup frame construction, and point containment classification for one submenu depth. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry nested popup level ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 menu geometry nested hit payload split moved the `NestedMenuLevelHit` DTO out of `native_pointer/menu_geometry/popup/nested/level.rs`. `nested/level.rs` is now 35 lines and owns only branch child validation, nested popup frame construction, and point containment classification, while `nested/hit.rs` is an 8-line payload owner for the per-depth traversal result. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a menu geometry nested hit payload ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-18 drag/resize capture subtree split reduced `native_pointer/drag_resize.rs` to 9 lines. The new `drag_resize/resize_capture.rs` and `drag_resize/tab_drag.rs` keep splitter resize capture and document/drawer/floating tab-drag capture in separate state-machine owners while preserving the original dispatch-facing function names.

The 2026-06-21 resize capture split reduced `native_pointer/drag_resize/resize_capture.rs` from 64 lines to a 7-line structural entry. `resize_capture/arm.rs` is 19 lines and owns splitter capture arming, `resize_capture/move_event.rs` is 30 lines and owns active resize move forwarding plus resize redraw, and `resize_capture/finish.rs` is 27 lines and owns pointer-up forwarding, resize-state cleanup, and resize redraw. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a resize capture arm/move/finish ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag lifecycle/payload split reduced `native_pointer/drag_resize/tab_drag.rs` from 182 lines to a 5-line structural entry. `tab_drag/lifecycle.rs` is 89 lines and owns tab-drag arming/move/release lifecycle; `tab_drag/payload.rs` is 98 lines and owns route-to-tab/source-group resolution. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag lifecycle/payload ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag lifecycle split reduced `native_pointer/drag_resize/tab_drag/lifecycle.rs` from 96 lines to a 7-line structural entry. `lifecycle/arm.rs` is 30 lines and owns tab-drag arming, `lifecycle/move_event.rs` is 44 lines and owns drag-start threshold plus down/move forwarding, and `lifecycle/finish.rs` is 34 lines and owns release redraw plus cleanup. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag lifecycle arm/move/finish ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag move event split reduced `native_pointer/drag_resize/tab_drag/lifecycle/move_event.rs` from 44 lines to a 31-line move-event entry. `move_event/start.rs` is 28 lines and owns drag-start threshold gating plus initial drag-down callback dispatch, while `move_event/active.rs` is 20 lines and owns active drag pointer update plus move callback dispatch. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag move event active/start ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag payload split reduced `native_pointer/drag_resize/tab_drag/payload.rs` from 100 lines to a 5-line structural entry. `payload/route.rs` is 32 lines and owns chrome-route matching, `payload/document.rs` is 45 lines and owns document/floating document tab lookup, and `payload/drawer.rs` is 45 lines and owns drawer tab lookup. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag payload route/document/drawer ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag payload child split reduced `native_pointer/drag_resize/tab_drag/payload/document.rs` from 45 lines to a 16-line structural entry and `payload/drawer.rs` from 45 lines to a 20-line structural entry. `document/root.rs` is 22 lines and owns root document tab lookup, `document/floating.rs` is 27 lines and owns floating document tab lookup, while `drawer/{left,right,bottom}.rs` are 18 lines each and own side-specific drawer tab lookup. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag payload document root/floating and drawer side ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag damage split reduced `native_pointer/tab_drag_damage.rs` from 145 lines to a 7-line structural entry. `tab_drag_damage/entry.rs` is 37 lines and owns release-damage group routing; `floating.rs` is 42 lines and owns floating-window lookup/target damage; `groups.rs` is 25 lines and owns local/document-edge group recognition; `regions.rs` is 45 lines and owns same-group and center/status damage; `union.rs` is 36 lines and owns visible-frame filtering plus union math. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag damage entry/floating/groups/regions/union ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag damage entry target split reduced `native_pointer/tab_drag_damage/entry.rs` from 35 lines to a 16-line source/target capture and same-group owner. `entry/target.rs` is 29 lines and owns cross-group local/local, document-edge, floating-target, and floating-to-local release-damage classification. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag damage entry cross-group target ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag damage floating split reduced `native_pointer/tab_drag_damage/floating.rs` from 42 lines to a 5-line structural entry. `floating/group.rs` is 24 lines and owns floating-window target-group/edge-group frame lookup, while `floating/target.rs` is 21 lines and owns floating-target damage assembly and target-frame unioning. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag damage floating group/target ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag damage regions split reduced `native_pointer/tab_drag_damage/regions.rs` from 45 lines to a 6-line structural entry. `regions/same_group.rs` is 17 lines and owns same-group release damage, while `regions/center_status.rs` is 26 lines and owns center-band/status damage plus floating-source unioning. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag damage regions same-group/center-status ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-21 tab-drag damage union visible split reduced `native_pointer/tab_drag_damage/union.rs` from 36 lines to a 3-line structural entry. `union/visible.rs` is 21 lines and owns visible-frame filtering plus union accumulation through the shared `frame_geometry` helpers. Validation used `cargo fmt -p zircon_editor`, `cargo fmt -p zircon_editor --check`, a tab-drag damage union visible ownership scan, scoped trailing-whitespace scan, and scoped `git diff --check`; package-level Cargo check was not relaunched because the current editor package check lane has been timing out before actionable editor diagnostics, and full Cargo tests remain deferred per the user's instruction.

The 2026-06-20 constants/state split reduced `native_pointer.rs` from 33 lines to a 25-line structural entry. `native_pointer/constants.rs` is 11 lines and owns host/viewport pointer event ids plus viewport button ids; `native_pointer/state.rs` is 5 lines and owns `NativePointerButtonState`. The 2026-06-20 owner visibility sweep kept drag/resize, routing, menu geometry, constants, and state re-exports inside the `host_contract` owner boundary. Validation used `cargo fmt -p zircon_editor --check`, a root ownership scan confirming raw constants and enum definition no longer live in `native_pointer.rs`, scoped `git diff --check`, and `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never`, which passes with existing warning noise only. Full Cargo test matrix remains deferred to the milestone validation stage per the user's instruction.

## Focused Regression Coverage

`native_host_binding_only_template_text_field_accepts_keyboard_input` covers a template input that has `component_role = "input-field"` and `binding_id`, but no explicit edit action id. It focuses the node and expects a typed character to route through `on_surface_control_edited`.

`native_host_resize_splitter_forwards_move_and_release_after_capture` covers splitter press, move, and release. The expected callback sequence is down, move, up with the root pointer positions forwarded to host resize callbacks.

`drawer_selection_is_normalized_to_one_active_item_after_layout_commands` creates a drawer state with conflicting `active_tab` and `active_view`, applies a layout command, and verifies both fields are normalized to the same valid item.

The shared pointer bridge rebuild guards have focused tests for unchanged activity rail, document tab, drawer header, host page, viewport toolbar, viewport overlay, menu, detail scroll, welcome recent, hierarchy, and asset-list inputs. The drawer header dispatch regression now derives its expected `left_top` instance from the projected pointer layout because current editor presets can place Hierarchy before Project in that runtime harness, while the routing contract is the projected tab, not a fixed legacy instance id.

The 2026-05-09 retained-host validation rerun exposed two host-scoped test-build residues after the Slint build dependency was removed. Retained host tests still called `i_retained_backend_testing::init_no_event_loop()`, and one hierarchy projection test still built `ModelRc` from a borrowed slice. Both were deleted or converted to the Rust-owned `model_rc(Vec<T>)` path because `UiHostWindow::new()` now initializes pure Rust host-contract state and no backend bootstrap crate is part of the retained contract.

The focused editor test-build check `cargo check -p zircon_editor --lib --tests --locked --message-format=short` now compiles with warnings only. The workspace validator gets past `cargo build --workspace --locked` and fails later in `cargo test --workspace --locked` only if a new active-worktree blocker appears outside this native pointer contract; do not restore a backend-testing crate to hide stale test calls.
