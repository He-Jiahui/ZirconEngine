# Editor 模块结构规范镜像文档

> 本文是 [Editor UI 10](../../plans/zircon_editor/editor_ui/10-code-structure-and-module-conventions.md) 的镜像文档，记录 editor `module_convention_gate` 的结构审计入口；当前事实以 `audit_editor_structure.py --json` 输出为准。上游规范：[`engine-code-structure-convention.md`](../../plans/engine-code-structure-convention.md)。
>
> 状态：active（`audit_editor_structure.py --json` 与 `zircon_editor/src/tests/structure_convention/` 已落地；当前 `module_convention_gate.m1_gate_status = migration-debt-present`，债务为 1 个超大生产文件和 1 个 UI owner 边界违例）。

## 治理范围

`zircon_editor/src`（`core/` `scene/` `ui/`）的：`ui/` owner 边界（R1.1/R1.2）、超大投影 / 转换文件 owner 化（R1.3/R1.4）、重复测试树消除（R4.3）、命名去 `_inner`（R2.3）、façade/prelude 友好度（R3.1/R3.3）。

## `module_convention_gate` 当前实测

字段同 [Runtime 镜像文档](../../zircon_runtime/structure/module-convention.md)，加 editor owner 分类（对齐 `large-file-ownership-m1.md` 的 `editor-retained-host` / `editor-ui`）。2026-08-02 `audit_editor_structure.py --json` 输出：

| 字段 | 当前 | 目标 |
|---|---|
| `oversized_production_files`（投影 / 转换巨型文件） | 1 | 0 |
| `production_dead_code_suppressions` | 0 | → 0 |
| `duplicate_test_trees`（`src/tests/**` ↔ `src/ui/**` 双写） | 0 | → 0 |
| `banned_name_modules`（`_inner` 等） | 0 | → 0 |
| `ui_module_owner_boundary_violations`（顶层单文件未归 owner） | 1 | → 0 |
| `module_convention_gate.m1_gate_status` | `migration-debt-present` | `classified-and-clear` |
| `migration_debt_count` | 2 | 0 |

## 已锁定的清零项

- `editor_ui_10_visual_style_owner_tree_is_hard_cut_over`：`pane_component_projection/visual_style.rs` 旧单文件 owner 已删除，`visual_style/{mod,button,component,model,surface}.rs` 全部存在。
- `editor_ui_10_module_convention_audit_report_has_expected_shape`：审计 JSON 暴露 `module_convention_gate`、`migration_debt_count` 与结构债分类字段。
- 2026-06-22 验证：M1.T1 前的 `cargo test -p zircon_editor --lib structure_convention --locked --message-format short --color never -- --test-threads=1` 通过（3 passed，2066 filtered out）；测试入口编译漂移已通过 owner-local import / `cfg(test)` helper 收束修复。
- 2026-06-22 M1.T1：`ui/activity.rs`、`ui/control.rs`、`ui/reflection.rs` 顶层单文件 owner 已硬切为 folder-backed owner tree，`ui_module_owner_boundary_violation_count` 清零。
- 2026-07-17 M1.T1 回归关闭：结构审计先精确复现 `component_registry.rs`、`preferences.rs` 两项 root owner debt（`migration_debt_count = 2`），随后硬切为 `component_registry/{mod,registry,tests}.rs` 与 `preferences/{mod,appearance,persistence,startup,typography_migration,tests/**}`；旧文件不存在，最新 Python audit 为 `classified-and-clear`、`migration_debt_count = 0`、`ui_module_owner_boundary_violation_count = 0`。文件级 rustfmt 与 scoped diff 已通过；受管偏好 12/12、组件 1/1、结构 3/3 全部 exit 0，并已完成 Editor07 fixed return。
- 2026-06-22 M1.T1 后续验证：同一 focused structure guard 重新尝试时被 runtime/plugin 侧 `RuntimePluginDescriptor` 私有字段访问错误阻断在 `zircon_runtime` 编译阶段，尚未进入 editor guard；脚本级审计仍通过并记录 `migration_debt_count = 31`。
- 2026-06-22 M4.T1：`scene/viewport/handles/{move,rotate,scale}_handle_tool_impl.rs` 禁名模块已硬切为 `*_handle_tool_behavior.rs`，旧路径扫描清零，`banned_name_module_count` 降为 5，`migration_debt_count` 降为 28。
- 2026-06-22 M4.T1：`ui/workbench/project/runtime_asset_helpers.rs` 与 `ui/retained_host/app/host_lifecycle/invalidation_bridge/mark_helpers.rs` 已硬切为 `runtime_asset_resolution.rs` 与 `dirty_marking.rs`，旧 helper 源文件不存在，调用方与 owner 说明已改向新路径，`banned_name_module_count` 降为 3，`migration_debt_count` 降为 26；focused `structure_convention` guard 本次 240s 超时无诊断，未声明通过。
- 2026-06-22 M4.T1：`core/editor_event/runtime/editor_event_runtime_inner.rs` 已硬切为 `editor_event_runtime_state.rs`，`EditorEventRuntimeInner` 改名为 `EditorEventRuntimeState`，源码旧 runtime-inner 引用清零，`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，`banned_name_module_count` 降为 2，`migration_debt_count` 降为 25；focused `structure_convention` guard 本次 300s 超时无诊断，未声明通过。
- 2026-06-22 M4.T1：`ui/retained_host/host_contract/paint_workbench_impl.rs` 与 `paint_workbench_impl/` 已硬切为 `paint_workbench_renderer.rs` 与 `paint_workbench_renderer/`，源码旧 impl 引用清零，owner 文档路径已改向 renderer，`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，`banned_name_module_count` 清零，`migration_debt_count` 降为 23。
- 2026-06-22 M5.T2：生产 `#[allow(dead_code)]` 审计命中已清零；`chrome_template_projection.rs` 删除未使用 `DOCK_HEADER_ASSET`，`host_data.rs`/`pane_presentation.rs`/`showcase_demo_state.rs` 移除剩余抑制，上一轮已清理 palette、softbuffer、viewport、template catalog 与 component drawer owner；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，`cargo test -p zircon_editor --lib structure_convention --locked --message-format short --color never -- --test-threads=1` 通过（3 passed，2066 filtered out），`production_dead_code_suppression_count` 降为 0，`migration_debt_count` 降为 13。
- 2026-06-22 M3.T1：`src/tests/host/template_runtime/viewport_toolbar.rs` 已硬切为 `scene_viewport_toolbar_runtime_projection.rs`，crate-level 测试名表达内建模板运行时投影职责，`ui/retained_host/host_contract/surface_hit_test/viewport_toolbar_tests.rs` 保留为私有 hit-test owner；focused runtime projection test 通过（2 passed，2067 filtered out），`duplicate_test_tree_count` 降为 0，`migration_debt_count` 降为 12。
- 2026-06-22 M2：`ui/template_runtime/showcase_demo_state.rs` 已拆出 `showcase_demo_state/events.rs`，事件输入、错误和 `component_event_for_action(...)` 由事件 owner 管理，根文件降为 486 行；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，`oversized_production_file_count` 降为 11，`migration_debt_count` 降为 11。
- 2026-06-22 M2：`ui/host/editor_manager_asset_editor.rs` 已拆为 5 行 wiring root 与 `editor_manager_asset_editor/{binding,designer,document,presentation,style}.rs` 子 owner，117 个 `EditorManager` UI Asset Editor façade 方法按 authoring/presentation 责任分区，旧根文件退出 oversized 清单；`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 10，`migration_debt_count` 降为 10；同轮后续 `cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，仅有既有 warning。
- 2026-06-22 M2：`ui/animation_editor/session.rs` 已拆为 48 行根 owner 与 `session/{lifecycle,presentation,sequence,graph,state_machine,parameters,support}.rs` 子 owner，加载/保存、pane presentation、sequence authoring、graph authoring、state-machine authoring、literal parsing 与共享 helper 分别落到具名 owner；同步修正 `host_cutover` thin-wrapper 结构测试以读取 folder-backed child modules；`cargo fmt -p zircon_editor --check`、`cargo check -p zircon_editor --lib --locked --message-format short --color never`、`cargo test -p zircon_editor --lib animation_editor --locked --target-dir E:\cargo-targets\zircon-editor-ui-animation-session-0622 --message-format short --color never -- --test-threads=1`、`cargo test -p zircon_editor --lib structure_convention --locked --target-dir E:\cargo-targets\zircon-editor-structure-0622 --message-format short --color never -- --test-threads=1` 与脚本审计通过，`oversized_production_file_count` 降为 9，`migration_debt_count` 降为 9。
- 2026-06-22 M2：`ui/host/editor_manager_plugins_export/export_build/wizard/tests.rs` 已拆为 `wizard/tests/{mod,support,pipeline_plan,pipeline_execution,job,panel_session,view_model}.rs` owner tree；共享 runner/helper、pipeline plan/execution、job、panel session、view-model/template-state 测试分别归属到具名文件，旧巨型测试文件不存在；同步修正 export wizard 取消分类与 v2 面板导入注册。`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 8，`migration_debt_count` 降为 8；`cargo test -p zircon_editor --lib export_wizard --locked --target-dir E:\cargo-targets\zircon-editor-export-wizard-0622 --message-format short --color never -- --test-threads=1` 在修复后曾被 runtime 侧 `GpuMeshResource::indirect_order_signature` 私有字段访问错误阻断于 `zircon_runtime` 编译阶段，文档同步后的最新复跑 304 秒超时且无诊断输出，残留 cargo/rustc 进程已停止，尚未声明通过。
- 2026-06-22 M2：`ui/retained_host/app/tests.rs` 已硬切为 `app/tests/mod.rs` + `app/tests/support.rs` + `app/tests/{command_palette,child_window_focus,menu_pointer,projection_geometry,viewport_template_bridge,root_pointer_fallbacks,child_window_tabs}.rs`；shared retained-host harness、root command palette、child-window focus、menu/popup、projection geometry、viewport/template bridge、root pointer fallback 与 child-window tab/header 回归分别落到具名 owner，旧 root 测试文件不存在。`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 7，`migration_debt_count` 降为 7；首次 focused Cargo run 暴露测试模块可见性缺口，已修正 `tests/mod.rs`/`support.rs` wiring；随后 lib-test 编译完成但 exact filter 匹配 0 tests，非 exact 复跑 184 秒超时无诊断，残留 cargo/rustc 进程已停止，尚未声明执行通过。
- 2026-06-22 M2：`ui/retained_host/ui/tests.rs` 已硬切为 `ui/tests/mod.rs` + `ui/tests/support.rs` + `ui/tests/{host_scene_projection,apply_presentation_shell,workbench_layout_frames,welcome_presentation,scene_document_pane,floating_windows,component_showcase}.rs`，其中 host-scene projection 断言段继续下沉到 `host_scene_projection/assertions.rs`；shared retained-host fixture、host-scene conversion、root shell apply-presentation、workbench layout frames、welcome mount projection、scene document pane、floating window projection 和 component showcase 回归分别落到具名 owner，旧 root 测试文件不存在。`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 6，`migration_debt_count` 降为 6；focused retained-ui Cargo exact-filter run 两次分别约 305 秒 / 304 秒超时且无诊断输出，匹配本次 target 的残留进程已检查/清理，尚未声明执行通过。
- 2026-06-22 M2：`ui/asset_editor/preview/preview_mock.rs` 已拆出 `preview_mock/entries.rs`；root 现在只保留 preview mock DTO、公开编辑入口、override 应用入口和 expression/suggestion/value-resolution wiring，子 owner 管理 subject/property/nested entry selection、expression resolution、literal/inline formatting、override mutation 与 nested path collection helper。`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 5，`migration_debt_count` 降为 5；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-preview-mock-0622 --message-format short --color never` 304 秒超时且无诊断输出，匹配该 target 的残留进程已检查/清理，尚未声明编译通过。
- 2026-06-22 M2：`ui/asset_editor/session/lifecycle.rs` 已拆出 `lifecycle/v2_projection.rs`；root 保留 session lifecycle/load/save/recompile/source-buffer 方法，子 owner 管理 source parse、v2/legacy projection、resource report、structured diagnostics 和 palette index helper。`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 4，`migration_debt_count` 降为 4；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-asset-lifecycle-0622 --message-format short --color never` 304 秒超时且无诊断输出，匹配该 target 的残留进程已检查/清理，尚未声明编译通过。
- 2026-06-22 M2：`ui/asset_editor/binding/binding_inspector.rs` 已拆出 `binding_inspector/payload_editing.rs`；root 保留 binding inspector DTO、公开 selection/editing 入口、event/action/route/schema projection helper，子 owner 管理 payload suggestion/schema entries、route/action suggestions、payload path resolution、action-state application、identifier/path normalization 和 item formatting helper。`cargo fmt -p zircon_editor --check` 与脚本审计通过，`oversized_production_file_count` 降为 3，`migration_debt_count` 降为 3；因本机 plugin/runtime Cargo lanes 活跃，本切片未重启 editor package check，尚未声明编译通过。

- 2026-06-22 M2：`ui/asset_editor/style/theme_authoring.rs` 已拆出 `theme_authoring/action_projection.rs` 与 `theme_authoring/merge.rs`；root 保留公开 theme authoring API，action projection 和 imported-theme merge/rewrite 分别落到具名 owner。`cargo fmt -p zircon_editor --check` 与脚本审计通过，后续 editor package check 通过。
- 2026-06-22 M2：`ui/layouts/windows/workbench_host_window/chrome_template_projection.rs` 已拆出 `chrome_template_projection/{activity_rail,dock_header,status_bar,tests}.rs`；root 保留 source-contract 可见入口和通用 template/frame helper，activity rail、dock header、status bar、module-local tests 分别归属到具名 owner。`cargo fmt -p zircon_editor --check` 与脚本审计通过，后续 editor package check 通过。
- 2026-06-22 M2：`ui/retained_host/ui/apply_presentation.rs` 已拆出 `apply_presentation/{pane_conversion,scene_conversion}.rs`；root 保留 apply flow 和基础 helper，pane payload conversion 与 host scene/native surface conversion 分别归属到具名 owner。`cargo fmt -p zircon_editor --check`、`audit_editor_structure.py --json` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-architecture-0622 --message-format short --color never` 通过；最新审计报告 `m1_gate_status = classified-and-clear`、`oversized_production_file_count = 0`、`migration_debt_count = 0`。

## 联动

与 `large-file-ownership-m1.md` 的 `editor-retained-host`(1) / `editor-ui`(2) owner 类共享 hotspot 清单；editor UI 计划 08/09 触及超大文件时按本治理 owner 边界落地。
