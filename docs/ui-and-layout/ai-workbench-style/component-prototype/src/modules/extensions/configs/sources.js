import { extensionReferenceSamples } from "../../../foundation/reference-samples.js";

export const extensionSources = extensionReferenceSamples.map(({ source, category, glyph }) => [
  source,
  category,
  glyph
]);
