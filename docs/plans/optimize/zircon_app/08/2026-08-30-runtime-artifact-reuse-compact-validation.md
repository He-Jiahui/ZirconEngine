# Runtime Artifact Reuse And Compact Validation

## Problem

Parallel Codex Sessions validate independent source snapshots, but repeated Rust dependency builds and per-job target trees can consume hundreds of gigabytes. Serial validation avoids target locking but leaves available CPU idle and increases queue latency.

## Accepted Architecture

- Exact validation inputs are coalesced by the existing ticket dedupe key while they are queued or running.
- The validation worker advances at most two immutable workspace copies concurrently. FIFO claim order remains authoritative.
- Cargo keeps one retained warm target per compatibility key. A second retained target for the same key is forbidden.
- The existing resource-budget service may admit one disposable burst target for eligible scoped CPU validation. Burst output is deleted after release.
- `reuse` is the default validator storage mode: persistent Cargo fingerprints and compiled dependencies, `CARGO_INCREMENTAL=0`, dev/test debug disabled, and a shared bounded `sccache`.
- `compact` keeps the same compiler cache but places Cargo build-script output in per-job scratch and deletes that scratch after the run.
- `diagnostic` is opt-in for symbolized investigation and preserves caller/Cargo defaults.
- Validation refuses to start when the target drive cannot preserve a `35 GiB` reserve. It never automatically cleans a hot pool immediately before rebuilding it.

## Dynamic Dependency Boundary

- Native third-party libraries with a stable C ABI may remain external DLLs so multiple host/runtime artifacts can reference the same installed bytes. NativeDynamic export staging now publishes immutable files into a shared SHA-256 content store and hardlinks each same-volume stage to that store; cross-volume staging falls back to a regular copy.
- Rust third-party crates remain statically linked into the versioned `zircon_runtime.dll`. Splitting each Rust crate into a DLL is rejected because Rust ABI, enabled features, panic strategy, and toolchain identity are not a stable cross-build contract.
- Hosts communicate with `zircon_runtime.dll` only through the versioned runtime C ABI. A future artifact manifest must assign independent `RuntimeArtifactId` and `HostArtifactId` values so host-only changes do not invalidate an unchanged runtime DLL.
- The NativeDynamic store is bounded to 4 GiB by default. Oldest unreferenced blobs are reclaimed after a run, while blobs with live stage hardlinks are retained. Signing and notarization runs disable hardlink reuse so mutable external tools cannot alter a shared blob.

## 2026-08-30 Live Baseline

- Coordinator database: 919 non-archived historical Session rows plus 2,971 archived rows.
- Fresh nonterminal Sessions in the last 30 minutes: 5.
- Validation queue at inspection: 1 materializing and 3 queued tickets.
- Active Cargo jobs and pending CPU/GPU reservations at inspection: 0.
- Validation ticket history: 1,267 failed, 326 snapshot-stale, 107 passed.
- Drive free space: C 26.2 GiB, D 58.3 GiB, E 139.4 GiB, F 90.0 GiB.

## Acceptance

- Two independent tickets can materialize concurrently; a third stays queued and backfills immediately when a slot terminates.
- Default storage keeps the compatible target hot and uses bounded `sccache` while per-job temporary scratch is deleted.
- Compact storage deletes its build scratch.
- Low-space admission fails without invoking `cargo clean`.
- Existing validation-ticket, control boundary, and validator regression suites pass.

## 2026-08-31 Runtime Evidence

- Codex reported 17 active ZirconEngine tasks in the desktop task list.
- Coordinator Session history contained 124 `registered`, 116 `stale`, 13 `waiting_validation`, 12 `resolving_failure`, 12 `finalizing`, and 8 `active` rows. These are workflow states, not 285 runnable workers; stale and terminal-adjacent rows must not be interpreted as compile demand.
- The live validation queue simultaneously held two `materializing` tickets and two `queued` tickets after daemon instance `5ce1cc87d0e341d182fd1216a3ac17de` loaded the two-slot worker. The two claims were committed within the same scheduler tick while later tickets remained FIFO queued.
- The managed compiler cache was live at `E:\cargo-targets\zircon-engine\cache\sccache`, bounded to 12 GiB. Its first real cold population observed 68 compiler requests, 48 Rust cache misses written, zero cache errors, and 37 MiB stored. A second compatible build is still required for a measured warm-hit timing; no speedup is inferred from the cold fill alone. Shared Cargo home and job scratch also resolved below `E:\cargo-targets\zircon-engine`; no compiler cache was placed on C.
- The Cargo runner now keeps job scratch until the complete process tree is terminal and the durable job release succeeds. The targeted regression first reproduced premature deletion and then passed after the lifecycle correction.
- The related coordinator regression set passed 123 tests. The post-correction Cargo runner module passed 12 tests, Python bytecode compilation passed, and the managed-storage Pester suite passed 4 tests.

## Remaining Throughput Gate

The latest 48-hour window contained 352 tickets: 323 failed, 22 became `snapshot_stale`, 3 passed, 2 were materializing, and 2 were queued. Median queue time was 3.38 minutes, but p90 was 87.65 minutes. Median active time after claim was only 2.32 minutes. This confirms that queue delay and late admission rejection dominate real compilation time.

The failure distribution was led by 213 `validation_copy_compile_time_resource_missing`, 59 `unmanaged_artifacts_detected`, 14 missing dependency-root declarations, 14 forbidden source states, and 10 unowned overlays. These checks must converge in submit-time admission before a ticket consumes a queue slot or creates a source copy. The currently leased validation-baseline work owns that boundary; this milestone does not duplicate its edits.

One terminal retention leak remains as an explicit cross-task handoff: failed ticket `9429d8340a6a4a258e57b28aa02878b2` references materialized copy `3e3c814b120646c9ada6c8ced4ef2d83`, whose Cargo job `5222293d80114b049eb94d482e626e50` is already released with deleted target output. Durable audit references currently prevent physical copy cleanup forever. The validation-copy owner received the exact reproduction; the fix must preserve manifests and audit rows while allowing physical source-copy removal once every run, reservation, ticket, and Cargo target is terminal.

## 2026-08-31 Quantified Validation Cost

- The latest 24-hour sample contained 287 tickets: 249 failed, 30 became `snapshot_stale`, 6 passed, and 2 were active. Ticket elapsed time was p50 1,049 seconds, mean 2,035 seconds, p90 6,067 seconds, and maximum 8,249 seconds. The reported "about 30 minutes and mostly failed" condition is therefore real.
- The same window contained 22 Cargo runs: 8 succeeded, 12 failed, and 2 were nonterminal or lacked an exit result. Cargo duration was p50 164 seconds, p90 1,242 seconds, and maximum 2,678 seconds. It also contained 22 validation-copy runs: 6 succeeded and 16 failed, with p50 28 seconds, p90 399 seconds, and maximum 2,687 seconds.
- After the loaded baseline/resource fixes, no new `validation_copy_compile_time_resource_missing` occurred after 16:02:49Z and no new dependency-root omission was observed. The leading post-fix rejection became `unmanaged_artifacts_detected`, including an empty `zircon-engine\scratch` structural directory. Artifact governance now treats that directory as a managed container: it preserves the root, recurses through it, skips active job children, and still reports stale children. The complete governance module passed 35 tests.

## Manifest Retention And Physical Database Cost

- The coordinator database occupied 2,742,210,560 bytes. Its logical JSON payload included 1,147,672,579 bytes of baseline manifests, 659,359,122 bytes of validation-copy manifests, and 20,326,533 bytes of ticket source manifests.
- Automatic retention is archive-first: it writes a bounded gzip JSONL batch, verifies readback, then transactionally clears eligible terminal manifest bodies. It excludes active/nonterminal owners and the latest baseline, rejects zero limits, and performs no routine database backup or `VACUUM`.
- The observed database had 96,230 free pages at 4,096 bytes, approximately 376 MiB physically reclaimable by a separately coordinated `VACUUM`. Online compaction remains owned by the daemon-maintenance Session; this record does not issue a competing control mutation.

## R5 Rejection And R6 Sccache Lifecycle

- Frameworks01 R4 started sccache PID 1660 with `TEMP` below job `84f3507f1dee480184e94f5cbaf9fdb2`. After that scratch was retired, the R5 exact return reused PID 1660 and failed in 18 seconds while trying to create `sccacheY8m77e` below the deleted R4 path. `SCCACHE_CLIENT_SIDE=1` alone was therefore insufficient, and the first metadata-only probe was rejected.
- R6 assigns a distinct endpoint to each of the nine approved D/E/F `cargo-targets`, `targets`, and `ZirconBuilds` roots (`42260` through `42268`). Only the daemon initializer inherits the persistent, guardian-protected `zircon-engine\cache\sccache-temporary`; Cargo, rustc, build scripts, and sccache clients inherit `scratch\<job-id>\temporary`. `SCCACHE_CLIENT_SIDE=1` connects those isolated clients to the stable endpoint, and `SCCACHE_IDLE_TIMEOUT=0` avoids idle respawn under an arbitrary caller environment.
- A PID, UTC-start-ticks, cache directory, cache size, executable, port, and stable-TEMP marker makes server reuse explicit. A matching marker uses a fast PID/TCP health path. A stale production binding refuses restart while any Cargo or rustc process is active; in an idle window it stops and rebinds the server. This also prevents a daemon auto-started inside an atomic Cargo Job Object from extending that job's process-tree lifetime.
- The deterministic regression starts an isolated daemon under a temporary directory, deletes that directory, rebinds the daemon, and then compiles a cacheable Rust request with `--emit=dep-info,metadata,link`. It asserts `.d` and `.rlib` outputs, then calls the initializer again and verifies the same PID is reused without another restart.
- Final focused evidence: managed-storage Pester 7/7 in 34.73 seconds; affected validator lifecycle 7/7 in 4.99 seconds; artifact governance 35/35 in 224.67 seconds; storage mapping 4/4. The online E endpoint was PID 31088 on port 42261, legacy port 4226 was closed, its marker matched the live process, and the stable directory survived the guardian cycle with no Cargo or rustc process remaining.
- Frameworks01 R6 then ran the exact origin command as outer job `96c7732d445d4596b5e86f662d8333ed`. It started at 20:57:00.893Z, finished at 21:00:13.739Z, released at 21:00:25.025Z, and reused the R5 compatibility target from `680c28eeb45f44ada781073ea28a3e50`. Real `zircon_runtime_interface` and `zr_resource` `dep-info,metadata,link` requests used endpoint 42261 and the stable temporary directory without reproducing the retired-job `TEMP` or `deps.d` defect.
- The R6 wrapper still exited 1 because a Frameworks-owned test read `self_record.id` after moving `self_record` in `readiness_projection/tests/behavior_red.rs` (`E0382`). Frameworks01 captured the id before the move, passed rustfmt and diff-check, and retained ownership of the identical FIFO rerun. The infrastructure defect is production-verified; the canonical Failure remains open only until the origin command reaches its requested profile artifacts.

## Symmetric Temp Boundary And Loaded Runner

- The final client-isolation regression first failed exactly two storage-mode cases (`5 passed, 2 failed`): the old implementation exposed `cache\sccache-temporary` as Cargo's process TEMP. After the environment boundary was corrected, all seven managed-storage cases passed, including the realistic stale-daemon `dep-info,metadata,link` probe. PowerShell AST parsing reported zero errors and diff checking was clean apart from the repository's existing CRLF warning.
- The Python Cargo runner invokes the same PowerShell lifecycle helper before placing Cargo in its Windows Job Object, validates the helper's binding-marker receipt, and then launches the child with job-scoped TEMP. Scratch removal occurs only after terminal process-tree observation and durable job release. This keeps one implementation of daemon rebind safety while retaining independent parallel compiler clients.
- Controlled rollover loaded daemon instance `8198e5b10a2846b4bef9a697af236925` at 05:36:11 +08 with no Cargo or rustc process alive. A FIFO reservation and its unstarted leased job were allowed to reconcile normally; no reservation was cancelled, duplicated, or consumed by the rollover.
- The live E cache footprint after the lifecycle tests was 0.45 GiB for shared Cargo home, 0.55 GiB for bounded sccache, one zero-sized binding marker in stable sccache TEMP, and an empty managed scratch root. These bytes are shared across compatible validations rather than copied per Session.
- Live observation then found an equivalent-path defect before closeout: a marker written with `\\?\E:\...` and a later Python initializer using `E:\...` were compared as raw strings, so endpoint 42261 restarted from PID 33892 to 31980 while Cargo/rustc were idle. The RED regression reproduced the same false restart on an isolated port (`6 passed, 1 failed`, PID 6736 to 16892).
- Binding comparison now resolves the cache directory, stable TEMP, and sccache executable through the Windows path contract before comparison. The complete Pester suite returned 7/7, Python Cargo runner returned 15/15, and online extended/display probes both returned `Restarted=false` with PID 14596 unchanged. Current helper hashes are `7d1eb4fe2bad2fb7bc124efcac272c187226b9a6f52dbdf9c86e4cd5342f74d9` and `4798293a9503186b1917aa5dc5074bbbc005dacd866868366f4eb529d1502cc9` for implementation and tests respectively.
- Automatic ticket `8b0d0f42f3854ce28982fb3400d2583e` exercised the loaded worker end to end: `queued -> materializing -> running -> passed`. Materialization took approximately 56 seconds, real Cargo compile/link took 10.63 seconds, and the managed command returned exit code 0. The ticket crossed overlay ownership, dependency closure, source-copy, FIFO admission, shared helper, job-scoped scratch, and terminal cleanup without the earlier dependency-root, compile-time-resource, or sccache failures.

## Loaded Queue And Failure-Shift Evidence

- The 06:31 +08 Codex discovery projection reported 1,003 records and was explicitly truncated: 217 were idle, 633 unavailable, 153 archived, and none were actively sampling at that instant. The coordinator independently reported 938 non-archived Session rows, of which only 8 were `active`; the largest categories were 557 `cancelled`, 126 `registered`, 102 `stale`, and 95 `completed`. Neither total is a runnable compiler count.
- The loaded validation worker simultaneously held one `running` ticket and one `materializing` ticket. Tooling15 ticket `2b993f52bd394d039361d81edc433dad` passed in approximately 115 seconds while later work continued to claim a materialization slot. Normal asynchronous Sessions must use this durable ticket path so FIFO waiting survives client timeouts without repeated acquisition attempts.
- A client-side timeout of the read-only artifact audit was reconciled through command request `566da6faec054f38a91e717153c36836`; it completed in 10.4 seconds with `unmanaged=[]`. Frameworks01's independent audit `0943b0b4b4e847029b52a894ae86a81b` had the same terminal result. No duplicate audit or cleanup mutation was issued.
- Frameworks01 exact-return request `16c82d55152a49f685c381263053016a` was durably terminal `failed` with `cargo_cpu_lane_reserved`, pointing to reservation `7551b566d19540438e8d2992d61b40e0`. It created no Frameworks Cargo job or profile artifact, so it is a FIFO admission result rather than a compiler or storage failure. The canonical Failure remains open until Frameworks01's unchanged origin command reaches its requested profile artifact.
- At 06:39 +08 the only live CPU reservation was `0150e485acb44feba0cd487eeb26611c`, running job `04e0bd610ee144c2890f0aae067cedde` in the single retained compatibility pool `1ee780aca270edf6d2084becaa48160d29e11c0797940111602464682cc2456b`. The managed scratch root contained only that job, and endpoint 42261 still used sccache PID 14596. The reservation and its process tree are allowed to terminate normally; this milestone does not cancel, consume, or replay them.
- Post-storage production failures have shifted to earlier workflow contracts. Tickets `a50f01434d9a4c02833e7d62e5a48267`, `b81e1cd1ded749d5b07f0f3ccb0ae8e3`, and `7db5a0680a0f445999f1dba29795661d` spent 43 to 83 seconds materializing before `validation_copy_overlay_not_owned`; ticket `aea3f8f71ed9494c9f69a4cd6e52bd43` reached `running` but later failed `cargo_cpu_reservation_command_mismatch` during `run_restart`. These two admission/restart defects were returned to the current validator owner with exact ticket evidence. They must be fixed at submit/claim or exact-command normalization, not hidden by retrying Cargo.

## Exact Return Closeout

- Frameworks01 independently verified the final managed-storage helper hashes: `7d1eb4fe2bad2fb7bc124efcac272c187226b9a6f52dbdf9c86e4cd5342f74d9` for `managed-cargo-storage.ps1` and `4798293a9503186b1917aa5dc5074bbbc005dacd866868366f4eb529d1502cc9` for its regression suite. Pre-run artifact audit request `b2559d6fdd2841d6894ec2f972f99189` completed with `unmanaged=[]`.
- The unchanged origin harness compiled through `zr_resource` as job `9acd911f7caf4aa583fa31000f66be0a`, then exposed a harness omission: `ZR_RESOURCE_MANAGEMENT_PROFILE_DIR` was unset and the directly reproduced E-drive binary panicked at `profile.rs:188` in 0.00 seconds. This was classified as an origin-harness contract failure, not a compiler-cache, storage, or coordinator infrastructure regression.
- Frameworks01 preserved the non-C-drive contract and reran the same managed validator, package, release profile, test filter, and target, adding only `ZR_RESOURCE_MANAGEMENT_PROFILE_DIR=E:\cargo-targets\frameworks01-resource-management-current-r4\profiles\resource-management-current`. Managed job `f2f3280096d64ca699bdd9c9e4800e97` ran from 07:03:07 to 07:07:19 +08, released at 07:07:25, reused the compatible target, and returned exit code 0.
- Endpoint `127.0.0.1:42261` remained on PID 14596 throughout the real `zircon_runtime_interface` and `zr_resource` compile/link path. No deleted job TEMP, `deps.d`, OS error 3, or false rebind recurrence was observed.
- The exact return produced a 345-byte/10-line metadata artifact (`f7cc3e2bc196d316790d4f590cb16394c7d2b1135dab3407ecb2b0338ba79a07`), a 27,125-byte/435-line raw artifact (`8c8bb282a3d3c051f85ff1b8f5198b66e4fb4d3ed69b79adae6bbbbad2701230`), and a 3,126-byte/15-line summary artifact (`1244bf20c9b30bf0fca4bd7f3bf850c7502a36c0f911823079718583923d05cd`).
- The canonical Frameworks01 failure record `docs/plans/zircon_runtime/frameworks/01/failure-2026-08-31-managed-cargo-sccache-temporary-path-lifecycle.md` is now `fixed`. This exact return supersedes the earlier in-progress/open status snapshots in this record.

## Validation Admission Closeout

- The loaded worker now rejects an unowned overlay before creating a validation copy. Production ticket `1c247655...` terminated at `queue_claim` with `validation_copy_overlay_not_owned` in approximately 80 milliseconds and created no copy link, replacing the earlier 43-to-83-second materialize-then-fail path.
- Cargo compile-pool identity now excludes immutable source-copy identity so compatible targets remain reusable, while every pending reservation remains bound to its exact source-copy job. A second ticket from the same Session can no longer overwrite the first ticket's approved command; it remains `waiting` until its own reservation can advance.
- A materialized Cargo copy remains `materializing` when `start()` reports `waiting`; it does not emit a false running link. The legacy recovery path is characterized separately so a previously running ticket can wait without creating a second run link or a fabricated failure.
- Dead leased pre-start supervisors are reconciled after the durable empty-process-tree evidence instead of occupying the FIFO lane until an unrelated client repairs them. The opposite transient-empty observation remains covered so a live launch is not collected prematurely.
- The complete ticket/Cargo-copy modules passed independently as 43/43 tests after the final failure-detail projection. The owner also reported 14/14 focused Cargo/workspace tests, Python compilation, and a clean diff check. Automatic success ticket `8b0d0f42f3854ce28982fb3400d2583e` remained `passed` after the changes.
- Controlled rollover loaded instance `936580b0f0c54f42889aa55c09fab16d` on PID 12412 with health and UI both returning HTTP 200. This instance contains the claim-time ownership, reservation/source-copy isolation, waiting-state, and dead-lease recovery fixes.

## Rollover Cargo-Start Fence

- The load operation exposed a separate race: rollover action `0a9b1df...` initially observed no Cargo while general admission remained open, then ticket `3c720...` started job `2ef1fd...` before shutdown. Durable recovery closed run `078c61...` as `cargo_run_reconciled_from_orphaned_job`, but a validation task was still interrupted. This is not an acceptable steady-state behavior for independent asynchronous validation.
- The corrected boundary serializes `arm_rollover` with the existing managed launch transition covering authorization, process creation, and durable registration. `BEGIN IMMEDIATE` then establishes one database order between rollover arming, reservation consumption, reserved-start acknowledgement, managed authorization, and the legacy registered-PID start path.
- Once this daemon commits `awaiting_restart`, only new Cargo start/consume operations receive `cargo_start_rollover_pending`. General Session, lease, queue, read-only, and UI admission remains open. Validation maps the temporary fence to `waiting`, preserving its exact reservation and source copy for the successor.
- Rollover also treats durable `cargo_start_requests.status='start_pending'` as an active launch and waits for it before arming. The fence is scoped to `source_daemon_instance_id`, so the successor does not inherit its predecessor's stop condition.
- Five deterministic regressions first failed against the old behavior and then passed 5/5. They cover pending-start deferral, launch-transition serialization, Cargo-only admission fencing, reserved-start rejection before acknowledgement, validation waiting, successor isolation, and the compatible registered-PID path. Python compilation and `git diff --check` passed.
- The broader affected batch passed 25/31. The remaining six tests stop before spawn because concurrent storage-policy work rejects their historical temporary `...\\drive\\cargo-targets` or `C:\\cargo-targets` fixture roots as unsupported; they do not exercise the rollover code and are retained as a precise storage-fixture handoff rather than reported green.
- The final Cargo-start fence loaded only after managed job `42419d28b8254edf816b6b125bfa3eeb` finished naturally, released, and reported an empty process tree. Rollover action `087b83f057f74401ac07c156838e237c` observed zero queued/materializing/running tickets, running jobs, nonterminal reservations, and pending starts, then loaded successor `7ede9895abae48ab94dfbae7b55b0ebe`. A later cleanup-selection correction used the same zero-load gate and action `24985b59e7bc40cb83e79b5390456edf` to load successor `df4f7483c9c2404597a63a1a93f6ea1d`. Neither action cancelled, replayed, or consumed another Session's work.

## Final Operational Snapshot

- Codex desktop discovery returned 29 recent tasks: 20 `active` and 9 `notLoaded`; 21 used the ZirconEngine checkout and 18 of those were active. This is a task-presence projection, not 18 simultaneous compiler processes.
- The final durable snapshot contained zero nonterminal validation tickets, zero nonterminal lane reservations, and zero pending Cargo starts. Historical validation totals were 1,406 `failed`, 123 `passed`, and 358 `snapshot_stale`; the remaining high failure ratio is therefore an admission/input-quality problem, not a reason to multiply target trees.
- The coordinator is healthy and writable on instance `df4f7483c9c2404597a63a1a93f6ea1d`, PID 30008, with the control UI returning HTTP 200. A new independent `zircon_runtime_interface` release check began only after the loaded cleanup had converged, demonstrating that normal asynchronous work resumed without a retained maintenance hold.
- Current drive free space is C 25.97 GiB, D 58.30 GiB, E 128.16 GiB, and F 90.03 GiB. The reusable E cache footprint measured earlier in this milestone was approximately 1.00 GiB across shared Cargo home and sccache; the 2.74 GiB coordinator database still has approximately 376 MiB reclaimable only in a separately coordinated maintenance window.

## Submit Admission, Copy Reclamation, And Scheduler Closeout

- A 12-hour production sample contained 148 tickets, 147 terminal results, and only 10 passes (`6.8%`). Thirty unowned-overlay failures spent 215.43 aggregate minutes in materialization and another 13 spent 44.44 minutes at queue claim. Twelve missing dependency-root failures spent 67.64 aggregate minutes. These 55 invalid submissions are now rejected before a new ticket or request row is inserted; existing successful `request_id` replay remains authoritative even if ownership later expires.
- Submit admission validates the Session and numbered Plan first, requires `coverage.dependencyRoots` or `coverage.dependency_roots` for non-Cargo commands, and calls the same normalized overlay-ownership contract used at claim time. The claim-time check remains as a race fence for ownership changes after submission. Cargo tickets and valid camelCase/snake_case non-Cargo tickets retain their existing behavior.
- Physical audit found 42 terminal ticket-linked validation copies still present, approximately 2.57 GiB and 364,369 files under `E:\cargo-targets\verify`. The old guard incorrectly required `cleanup_status='deleted'` and an absent Cargo target, which permanently blocked the normal shared-pool state (`status='released'`, `cleanup_status='retained'`, target present).
- Terminal source-copy cleanup now requires a terminal ticket, an explicit same-Session ticket/copy link, every linked Cargo run in `completed` or `launch_failed`, every reservation in `released` or `expired`, and every linked Cargo job exactly `released` with `process_tree_live_pids_json=[]`. It deliberately ignores retained target existence and never deletes Cargo targets or audit rows. Immediate terminal cleanup is best effort; periodic recovery retries at most four historical copies per tick in deterministic order.
- Production timing showed a completed Cargo run waiting about 309 seconds for ticket projection while the maintenance thread synchronously prepared a filesystem-heavy workspace scan. Workspace observation preparation is now a single background flight. Validation advances before reconciliation and immediately after reconciliation on every watch interval, completed observations are applied serially, and a blocked observation cannot stop validation ticks or hold coordinator shutdown beyond a 100 ms join bound.
- The owner and App08 verification sets passed 44/44 ticket tests, 13/13 copy-lifecycle tests, and 13/13 server tests; App08 independently repeated 44/44 ticket tests, 11/11 focused copy cleanup tests, 5/5 scheduler tests, and the final 10/10 terminal-cleanup safety batch. The full workspace-copy module exceeded a 15-minute local I/O ceiling without an assertion failure, so it is not reported as a complete green run.
- Production request `app08-submit-preflight-unowned-20260831-v1` submitted an intentionally unowned `README.md` overlay through the loaded daemon. It returned `validation_copy_overlay_not_owned` immediately; before and after counts remained exactly 1,883 tickets, 1,886 ticket requests, 5,132 Cargo jobs, and 1,259 lane reservations, with no request row for the probe. This proves the rejection no longer spends a queue slot, creates a source copy, or invokes Cargo.
- The first loaded recovery reduced historical copies but then stopped behind the oldest non-releasable Cargo evidence. A RED with `batch_size=1` reproduced the queue-head defect: an `orphaned` Cargo job returned zero recovery and hid a later fully releasable copy. Candidate selection now excludes active tickets, nonterminal runs/reservations, and any Cargo job not exactly `released` with a parsed empty process tree before applying the batch limit; the transactional deletion path repeats the full safety check.
- The corrected production recovery reduced `materialized` copies from 32 to 7 and drove eligible terminal copies to zero. It removed 35 historical reclaimable copies while retaining seven copies whose linked Cargo jobs remain `orphaned`; all seven have empty observed process trees and expired reservations, but remain intentionally blocked until job lifecycle is reconciled to `released`. E free space increased from approximately 127.05 GiB to 128.16 GiB, and 359 released Cargo jobs still report `cleanup_status='retained'`, so source-copy reclamation did not delete warm targets or audit rows.

## Reuse Limits And Remaining Independence Work

- Warm Cargo targets are the primary Rust reuse mechanism. Compatible production evidence reduced a previously cold approximately 19-minute build to approximately 49 seconds, and the focused automatic success ticket compiled and linked in 10.63 seconds after a 56-second source materialization. Rust crates remain rlibs inside the versioned runtime/plugin `cdylib` boundary; converting ordinary Rust dependencies into per-crate DLLs would trade stable fingerprints for unstable ABI and deployment coupling.
- Cross-copy `sccache` reuse remains low (approximately `0.16%`) because compiler keys still include distinct source roots. The cache is useful as a bounded shared fallback, but this milestone does not claim high cache-hit reuse or rely on an unproven `SCCACHE_BASEDIRS` Rust-key contract.
- The former path-hash-only ticket boundary is closed below: new production tickets seal content-addressed overlay bytes and deletion tombstones at submit time. Legacy tickets retain the previous fail-closed ownership and drift behavior rather than being silently reinterpreted.

## Sealed Ticket Sources And Writer-Lock Boundary

- The next milestone now seals every submitted overlay byte into the existing zlib-compressed, SHA-256-addressed coordinator object store. Null manifest entries remain deletion tombstones. An internal `validation-ticket-source:<ticket-id>` snapshot pins those objects while the ticket is queued, materializing, or running; terminal projection releases the pin and existing retention performs eventual deduplicated collection.
- Submit still replays an existing `request_id` before any mutable admission check. New requests validate Session/Plan, dependency roots, and current overlay ownership before reading source bytes, then repeat the mutable admission checks in the commit transaction. Source reading and hashing occur outside the global SQLite writer transaction; object registration, ticket insertion, source-pin insertion, and the sealed event commit atomically.
- A multi-file stale submission first reproduced an orphan-object leak: an earlier valid file could create a compressed object before a later stale file rolled back its database row. The corrected path validates every path and hash before writing any object. A separate deterministic concurrency RED held a source read open and showed an unrelated database writer blocked; after the transaction split, that writer completed within the 0.5-second assertion window while the source read remained blocked.
- Generic and Cargo validation copies now accept the immutable overlay manifest and materialize those paths from object bytes instead of the live worktree or current attribution. This covers changed files, newly created files, and deletion tombstones. Legacy tickets without a sealed event retain the prior fail-closed live ownership and drift checks.
- Cargo durable materialization requests persist the sealed overlay manifest so daemon recovery uses the same bytes. `PinnedCargoPlanner` constructs Cargo manifests, lockfiles, toolchain/config files, and zero-byte source topology from the ticket's baseline commit, then applies the sealed overlay and tombstones before invoking metadata inside the registered copy job. A submit-time manifest scan also records every sibling Git dependency's exact commit and package include roots in `coverage.externalSources`; the worker consumes that immutable descriptor after restart instead of reading a later sibling `HEAD`. Legacy tickets without the version marker retain live discovery only for compatibility.
- Each new ticket also records the submitting Session's `baseline_epoch` and `base_head` in its immutable submitted event. Both participate in the dedupe key, and the source-object snapshot references the same baseline epoch. A Session baseline change during source capture rejects the request before any ticket, request row, snapshot, or object is written.
- The validation worker passes the ticket's `base_head` into both generic and Cargo materialization. Generic dependency enumeration now uses `git ls-tree` against that commit rather than the live index; the durable Cargo JSON stores the same commit and recovery preserves it after HEAD advances. Two otherwise identical submissions from different baselines cannot coalesce.
- The production sample used to size the boundary contained 1,000 recent tickets: source manifests had p50 7 paths, p90 10, p99 40, and maximum 345; manifest JSON reached 43,816 bytes. Current overlay bytes were p50 approximately 27.6 KiB, p90 approximately 90.7 KiB, and maximum approximately 3.43 MiB. Keeping those reads outside the writer transaction directly protects heartbeat, request terminalization, and parallel submission latency.
- Final local evidence before production load: validation-ticket module 53/53 in 98.75 seconds; pinned-baseline and sealed-overlay real Git materialization 5/5 in 57.63 seconds; production server validation boundary 5/5 in 57.12 seconds; background scan/scheduler boundary 5/5 in 0.77 seconds; Python bytecode compilation and diff checking passed. The full workspace-copy module previously exceeded its local I/O timeout, so only the affected real materialization and legacy regression batches are reported green.
- The workspace observation single-flight was extracted to `tools/session_coordinator/workspace_scan.py`; the already oversized server remains the orchestration boundary instead of absorbing another background-worker responsibility.

## Managed Convention Cargo Boundary

- Production observation found `tools/check_conventions.py --only structure --json` running `cargo +1.94.1 test -p zircon_runtime --lib structure_convention --locked --jobs 1` directly in the shared checkout. Rustc wrote `target\debug` with incremental compilation and full development debuginfo. The process occupied approximately 46 minutes and E free space fell by approximately 2.65 GiB during the observation. It completed naturally; no process was killed.
- On unmanaged Windows, the structure and clippy convention gates now delegate to `validate-matrix.ps1`. The validator acquires the normal test/check FIFO lane, uses the retained compatibility target, stable sccache endpoint, job-scoped temporary directory, `CARGO_INCREMENTAL=0`, and the existing disk-reserve admission. Linux/CI and an already managed child retain the canonical raw Cargo command, so recursion is impossible and CI command identity does not change.
- The managed-environment detector requires an approved D/E/F target root, matching `zircon-engine\scratch\<job>\temporary`, the shared sccache directory, client-side sccache, and disabled incremental compilation. The dedicated validator switches render the exact locked structure or clippy command and reject package/test selectors, a non-root manifest, `-NoLocked`, a non-development profile, and ephemeral storage rather than silently ignoring them.
- The complete validator Pester suite passed 108/108 in 171.76 seconds without launching real Cargo. The Python convention suite passed 32/33; its only failure reports seven current `zircon_runtime_interface` Rust files without required exemption markers, owned by concurrent source Sessions and unrelated to the managed Cargo delegation.

## Production Load And Sealed-Source Receipt

- With zero queued/materializing/running tickets, zero leased/running Cargo jobs, zero active lane reservations, zero pending starts, and no Cargo/rustc process, controlled rollover action `06e46e52658948cdb40c461b6a9dd6ad` loaded successor `0e5f56aaa6214c2c93ac7a94f660eed2` on PID 36308. The action succeeded through preview/confirm, schema remained 69, mode is `read_write`, supervision is healthy, and the health endpoint returned HTTP 200. No Session lease, FIFO request, or mutation was cancelled or replayed.
- Successor reconciliation confirmed the previously delayed commands `a210b549f3804a7886f91a1df5332a40`, `cb00e7fc611c4f6d97875fe3c8e0947e`, and `69a6fda72bc04917a5e36377d7ca5eef` are terminal `completed`. This closes the command-journal terminalization concern without repeating Frameworks01's status mutation.
- Production ticket `4570b7afe9eb49a0b41bbd49bec2faef` sealed the current 34,312-byte `validation_tickets.py` object at SHA-256 `ce44249f0f4b09456cb800067b4a3e9ed93b481b71a66cb834d5552f4d9afaa8`. Its immutable event recorded `baseline_epoch=574`, `base_head=399f2318150ae4fa0df3a2543133b03b80099288`, one object, and source-manifest hash `597c766a5b1ef0746d5e548926304a72c113903b66f26c868e937d3fc5b22189`.
- The worker advanced `queued -> materializing -> running -> passed` in 72.34 seconds: approximately 28.1 seconds waiting for the next worker tick, 43.7 seconds for a real pinned Git archive plus overlay, and 0.57 seconds for execution and terminal projection. Stdout returned the exact sealed SHA-256. It invoked no Cargo or rustc process.
- Copy `0a0309b8ed6d4897920c62c0314419ec` was tied to the old base commit, returned exit code 0, and was physically removed less than one second after terminal projection. Its source pin count is zero, and at that receipt the managed scratch and pool roots contained zero files with no active Cargo/rustc process. The shared cache measured 2.829 GiB and E had 124.21 GiB free. Concurrent tasks can change drive free space, so this snapshot is evidence of bounded App08 output, not an attribution of all drive movement.
- After the App08 receipt, independent Session `01a019a5-b15f-7461-a1b0-ce4b6aa8e710` acquired test job `92465a3663f24c319eccd35b02743c0d` under approved root `E:\ZirconBuilds\shader06-p1-1-exact-plan` with `delete_on_release`. Its focused Cargo process is parented by successor PID 36308 while the App08 ticket remains passed and its copy remains removed. This is direct evidence that later asynchronous validation proceeds without reopening or sharing the completed ticket's source tree.

## Pinned Cargo Topology And External Sources

- The planner no longer asks the live checkout which packages belong to a submitted Cargo ticket. Its topology view is identified by `base_head + sealed overlay + deletion tombstones`, and the final metadata command runs only after that view has been placed below the durable validation-copy root.
- Submit-time external discovery is Cargo-free and reads baseline manifests through one bounded Git archive. On the production checkout it completed in 2.837 seconds and pinned sibling repository `E:\Git\zr_vm` at `45843227a477ab6274d1026e2621e03318199f9e`, including only `zr_vm_rust_binding/rust/zr_vm_rust_binding` and `zr_vm_rust_binding/rust/zr_vm_rust_binding_sys`.
- The focused planner suite passed 9/9. External-pin and ticket suites passed 59/59, including a restart case where the sibling repository advanced after submission and a new sealed Cargo manifest that introduces a sibling dependency. The affected workspace-copy and validation-copy paths passed 12/12 targeted regressions covering baseline advance, sealed overlays, durable acknowledgement, external pins, and shared baseline analysis.

## Native Dynamic Artifact CAS

- Managed Cargo environments expose one stable NativeDynamic CAS below the selected D/E/F storage root. DLLs and file sidecars are addressed by full SHA-256 content, published atomically, and guarded by per-digest thread/process locks so independent validation tasks can publish the same bytes concurrently.
- A real NTFS measurement staged one 32 MiB DLL into four independent output roots. All four paths shared the CAS blob's file identity, the blob link count was five, and 128 MiB of logical stage output required one 32 MiB blob: 100,663,296 bytes of duplicate stage content avoided, or 75%. After all four stages were released, zero-byte-limit GC removed the blob and reported no retained bytes.
- The CAS suite passed 11/11, including corruption replacement, cross-volume fallback, concurrent publication, linked-blob retention, and bounded reclamation. A verified cache hit hashes the source and validates the existing blob once, then compares its file identity under the digest lock instead of reading the blob a second time. A separate stress loop completed 50 rounds with six concurrent publishers and four concurrent pruners per round. Native build, copy-error, signing, and notarization regressions passed 36/36.

## Current Capacity Snapshot

- Codex desktop returned 22 recent ZirconEngine tasks: 16 `active` and six `notLoaded`. This remains task presence, not compiler concurrency.
- The coordinator held 46 nonterminal validation tickets: one `running`, one `materializing`, and 44 `queued`, with one managed Cargo job running. Three Sessions owned 44 of those 46 tickets; the largest Session owned 26. The daemon also had a foreign Git finalize in progress, so rollover was intentionally deferred rather than interrupting either operation.
- The latest 48-hour completed population contained 419 `failed`, 92 `snapshot_stale`, and 30 `passed` tickets. A passed ticket's median total time was 158.6 seconds: 38.6 seconds queued, 47.0 seconds materializing, and 51.0 seconds running where all phase timestamps were available. Failed tickets averaged 1,851.7 seconds end to end, but 347 of 419 failures occurred before a test command ran. The leading early failures were 162 missing compile-time resources, 70 unmanaged-artifact rejections, and 62 unowned-overlay rejections. Adding target trees cannot correct those inputs.
- The next throughput priority is submission batching and early source/command preflight, not more retained Cargo pools. A Session should submit one coherent validation matrix per sealed snapshot, while exact duplicate commands remain coalesced and independent snapshots keep separate receipts. Per-Session queue observability should warn before dozens of fine-grained tickets accumulate; it must not silently cancel a task's requested evidence.
