# Frameworks 02 Current Corrections Output Records

> 本文件归档 `02-module-kernel-and-lifecycle-unification.md` 的 2026-07-13 当前修正产出；计划定义仍由父计划持有。

| 里程碑 | 完成项目 | 状态锚 | 日期 | 产出与验证 |
|---|---|---|---|---|
| M1 内核语义修正 | Build-success activation rollback cleanup | `frameworks_02_m1_activation_rollback_cleanup_focused_passed_full_runtime_timeout` | 2026-07-13 | 单模块在 build 成功后的 service/ready/finish/activation 失败路径执行 cleanup；批量模块仅回滚成功 build 的前缀，并按依赖逆序 best-effort cleanup。新增 `CoreError::ModuleActivationRollback` 与 `CoreError::ModuleBatchActivationRollback`，cleanup 失败时同时保留主激活错误和回滚错误，不回落旧的静默重置语义。focused lifecycle 8/8 passed；scoped rustfmt、`git diff --check` passed。Windows managed Runtime package test 完成编译但 Cargo 返回 101；直接复用同一 fresh lib-test executable 的 focused lifecycle 8/8 passed。全 Runtime lib executable 在 20 分钟预算内未完成，已停止本会话进程，因此不声明全量 Runtime green。 |
| M2 模块发现与诊断硬切 | RuntimeModuleLoadReport typed single-source diagnostics | `frameworks_02_m2_module_load_report_typed_diagnostics_hard_cut_windows_build_focused_no_ui_probe_passed_full_test_profile_blocked` | 2026-07-13 | 删除并行 `warnings`/`errors`/private required-missing、`RuntimeRequiredPluginMissing`、`effective_*` 合并与 externalized warning helper；report 仅保留 modules、structured availability、private `RuntimeModuleLoadDiagnostic`。Core/unknown-plugin/feature/asset-importer 错误保持类型，App/动态 ABI 仅在显示边界调用 `warning_messages()`/`fatal_messages()`；known required provider 只由 `RuntimePluginAvailabilityReport::missing_required` 持有。UI 未编译时由 availability 结构化标记 unavailable，不恢复 loader 字符串真相。Windows managed `cargo build -p zircon_runtime --locked` 冷构建通过（11m22s，既有 bin dead-code warnings）；当前 7,859-test lib binary 成功生成。runtime module 当前 binary 先通过 11 项；更新后的 direct-owner grouped-import structure guard standalone 1/1；optional VirtualGeometry structured warning projection 独立 current-rlib probe 通过；production 旧字段/helper 扫描为零，scoped rustfmt/diff-check 通过。后续 Windows `core-min/no-ui` build-and-run probe 退出码 0，证明 Client2d/Client3d 均只把 UI 投影为 required missing；正式全 test profile 未声明 green，因为默认 `--tests` 在无关 `material_shader_redirect_dependency_contract` 集成目标的既有 API 漂移处停止。 |
| M3 测试阶段支持修复 | VM host-interface test-tree compile propagation | `frameworks_02_m3_runtime_lib_test_vm_host_interface_support_compile_repaired_focused_passed` | 2026-07-13 | Runtime lib-test 首轮编译在并发 VM host-interface 变更处失败：hot-reload fixture 缺 `VmHostInterfaceRegistry` import，新 sibling `tests/host_interfaces.rs` 未按既有 child pattern 导入 `super::support::*`。系统化定位到测试支持传播缺口后仅补两处 import，不改生产行为；重新生成当前 7,859-test binary，证明 4 个 E0433/E0425 编译错误清零。focused `script::vm::tests::host_interfaces` 4/4、`hot_reload_coordinator::tests` 5/5；poison-lock 用例的 panic 文本为预期捕获，测试进程退出码 0。 |
| M3 / Editor M1 | Plugin provider identity fixed-handoff current-source revalidation | `frameworks_02_m3_editor_m1_plugin_provider_lookup_windows_exact_passed` | 2026-07-13 | 按 `fixed-2026-07-11-editor-m1-plugin-provider-lookup.md` 自下而上复验：Runtime `package_feature_definitions` external-provider/owner-provider 2/2，runtime plugin catalog feature/provider 11/11；09:02 新生成的 Windows Editor test binary 上，builtin `editor_manager_plugin_status_lists_owner_optional_feature_dependencies` 1/1、native `native_aware_catalog_enables_external_feature_extension_provider` 1/1（各 0.07s，3,133 filtered）。definition、completed selection、dependency enablement 与 native-aware lookup 继续使用准确 `feature_id@provider_package_id`，未恢复 feature-id fallback、别名键、重复 catalog truth 或 Editor 特例。Editor 01 fixed handoff 已为 `status: fixed`；本 Frameworks 02 会话因 output ownership 规则不重复改写来源计划记录。 |
| M3 / Editor M1 | Provider package identity fallback hard cut | `frameworks_02_m3_editor_m1_provider_package_identity_hard_cut_windows_default_lib_no_ui_probe_passed_external_integration_blocked` | 2026-07-13 | 复审发现 profile availability 与 target assembly 仍把 `project_selection.id`/`RuntimePluginId::key()` 当作 provider 兼容身份。已删除两类 fallback 及第二套 `plugin_modules/availability.rs` predicate：registration 只贡献 `package_manifest.id`，linked/native 只匹配 `descriptor.package_id()`，target assembly 只消费 structured availability 的 `Linked`/`NativeDynamic` 分类。新增 linked/native runtime-key mismatch、linked/native registration package mismatch、single-source target loading 负例，并同步全部 Client2d UI/no-UI assertions。Windows 默认 `cargo check --lib --tests` 的 production lib 通过（460 个既有 warning），随后由无关 material/shader integration test 的 3 个既有编译错误阻塞；Windows `core-min/no-ui` 实际 probe 16m11s、exit 0，验证 package identity 与唯一 required-UI 结果。structure guard standalone 1/1；两轮只读复审最终 0 Critical / 0 Important。 |
| M3 测试阶段 | Normalized project selection overlay keeps one module owner | `frameworks_02_m3_normalized_project_selection_overlay_windows_fresh_libtest_focused_passed` | 2026-07-13 | current-source builtin module 回归先 12/13，`target_module_loading_does_not_treat_selection_spelling_as_provider_identity` 暴露 `manifest_with_mode_baseline` 会把 baseline `ui` 与 override `UI` 作为两项，生成重复 `UiModule` 并被排序器正确拒绝。最低层修正在 `ProjectPluginManifest::set_enabled`：精确文本未命中时按解析后的 `RuntimePluginId` 语义 upsert，同一选择只保留一个 owner；provider availability 仍只认 package manifest id，不增加 selection/runtime-key fallback。旧二进制稳定复现 RED；协调器生成当前源码 Windows Runtime lib-test（7,863 tests inventory，compile exit 0）后，该 exact 1/1、provider package mismatch exact 2/2、profile maturity 18/18、builtin runtime modules 13/13 全部通过；scoped rustfmt 与 diff-check 通过。 |
| M3 测试阶段 | Full Runtime lib gate failure routed to Runtime 04 | `frameworks_02_m3_full_runtime_lib_asset_reference_repair_failure_handoff_open` | 2026-07-13 | 当前源码 7,863-test Windows binary 的完整门在 Asset/Project 簇确定失败；`.codex/tmp/frameworks02-runtime-lib-full-20260713.log` 于 620/7863 前已记录多项失败。本会话向下复现最早失败：`importer_outcome_exposes_complete_guid_repair` 0/1、exit 101，普通 `resolution_reports_guid_path_repair_dangling_and_conflict_states` 1/1。最低故障收束到 Runtime 04 `reference_resolver::entry_by_hint(...)` 在 base registry lookup 前拼入 stale `#Mesh0`，错误落入 `Dangling`。已创建并索引 [`Runtime 04 stale-subasset failure`](fixed-2026-07-14-stale-subasset-reference-repair.md)，来源/修复计划均保留 relative link；Frameworks 02 不加 importer fallback，也不声明 broad Runtime green。 |
| M3 插件结构硬切 | Navigation Editor registration owner convergence | `frameworks_02_m3_navigation_registration_hard_cut_static_review_passed_cargo_queued` | 2026-07-13 | 全仓插件结构审计暴露仅余两处 registration compatibility debt，均来自 Navigation Editor 的 crate-root `mod registration` 与根级 `registration/mod.rs`。已将完整注册实现硬切到 `plugin/registration/`，由 `plugin.rs` 直接拥有，删除旧目录且不保留 re-export/forwarding shim。插件结构审计 exit 0，`m3_hard_cut_gate_status=registration-hard-cut-clean`、compatibility shim sites 2→0；registration audit 单测 10/10，scoped rustfmt 通过；独立只读复审 0 Critical / 0 Important / 0 Minor，结论 Ready。当前源包级 Cargo 复验因 3 个其他会话的受管任务占用通道而排队，详见 [`Plugins 05 output record`](../../../zircon_plugins/05/2026-07-13-navigation-registration-hard-cut-output-records.md)。 |
| M1–M3 当前二进制聚焦验收 | Lifecycle/order/profile/plugin convergence | `frameworks_02_m1_m3_current_runtime_binary_focused_35_passed` | 2026-07-13 | 复用本会话从当前源码生成的 Windows Runtime lib-test binary（7,863 tests）补齐 Frameworks 02 直接验收：lifecycle rollback/ready 8/8、InitLevel 与依赖排序 6/6、builtin profile/availability/order 13/13、RuntimePlugin descriptor/lifecycle/native projection/order 8/8，共 35/35；runtime plugin lifecycle folder-backed structure guard 1/1。核心扫描未发现 compat/legacy/shim/deprecated 迁移模块；`plugin/bridge` 为正式 ABI bridge，未误删。完整 Runtime 门仍由已交接的 Runtime 04 Asset 故障阻断，因此本行只声明 Frameworks 聚焦门通过。 |
| M2 架构决策收口 | Disabled plugin-group anchor contract | `frameworks_02_m2_disabled_anchor_contract_documented_app_test_pending` | 2026-07-13 | 核对 `PluginGroupBuilder::enabled_anchor_index` 与现有 `plugin_group_builder_reports_disabled_anchor_reordering` 后，关闭 profile 文档中遗留的 G1 未决项：禁用 entry 不贡献 membership 或 ordering authority，`add_before/add_after` 对禁用 anchor 返回类型化 `PluginGroupError::DisabledAnchor`；调用方只能启用 anchor 或通过参与模块的 descriptor dependency 声明顺序。同步 `docs/zircon_app/plugins.md` 与 `docs/runtime-plugins/profile-selection.md`，不增加 Bevy 兼容分支；当前源码 App 包测试待受管 Cargo 通道。 |
| M1 计划/代码类型名收敛 | Draft kernel-error name hard cut to CoreError | `frameworks_02_m1_core_error_plan_source_converged` | 2026-07-13 | Frameworks 02 正文与开发规范仍残留旧草案错误类型名及非当前 variant 名。已按生产唯一类型 `CoreError` 硬切文档：明确 `DuplicateModule`、`MissingModuleDependency`、`ModuleInitLevelViolation`、`ModuleDependencyCycle`、`ModuleReadyTimeout`、`ModuleActivationRollback`、`ModuleBatchActivationRollback`，并明确 App 只在日志/ABI 展示边界投影字符串；全 Frameworks 计划集不再保留第二套内核错误类型词汇。 |
| M1 InitLevel 命名硬切 | Retired Servers plan vocabulary removed | `frameworks_02_m1_init_level_services_plan_source_converged_python_8_passed` | 2026-07-13 | 生产枚举已按结构规范使用 `InitLevel::Services`，但 Frameworks 02、总览与架构图仍残留旧 `Servers` 目标词汇。已将目标层级统一为 `Kernel→Services→Scene→Editor→Post`，明确非网络语义不得恢复 `Servers` variant；Godot 参考源码路径中的 `servers/` 仅保留为上游事实。`test_runtime_init_level_naming` 与 manifest schema module 组合验证 8/8 通过，覆盖 production `Servers`/`InitLevel::Servers` 清零、serde `services` 接受及退役 `servers` 拒绝。 |
| M1/M3 描述符所有权收口 | Module vs RuntimePlugin descriptor fields reconciled | `frameworks_02_m1_m3_descriptor_ownership_plan_source_converged` | 2026-07-13 | 计划草案把 systems/features 平铺进 `ModuleDescriptor`，与当前分层和生产类型不一致。核对 module descriptor、RuntimePlugin descriptor、registration report 与 extension registry 后，将最终契约写回计划：内核 descriptor 只拥有通用 lifecycle/order/service 声明；RuntimePlugin descriptor 组合它并拥有 capability/system-set/feature/packaging 元数据；实际扩展经 `RuntimeExtensionRegistry` 注册，report 自动投影唯一 module row。该决策避免 core 反向依赖 scene/plugin 类型，也不恢复 provider 自注册第二份 descriptor。 |
| 优先代码评审门 | engine-code-review-findings current Runtime binary sweep | `frameworks_02_priority_code_review_findings_current_binary_80_passed` | 2026-07-13 | 按用户优先级直接执行当前 7,863-test Windows Runtime binary 的 `tests::runtime_absorption::code_review_findings`：80/80、0 failed，覆盖 P0 robustness、F8 API convergence、plugin/importer DX、typed-error convergence、dead-code 与 late API cleanup；无需重新编译。 |
| 优先结构约定门 | structure-convention broad sweep and current-owner correction | `frameworks_02_priority_structure_convention_1288_passed_16_red_current_owner_static_repaired_recompile_pending` | 2026-07-13 | 同一 binary 的 structure-convention 宽门 1,288/1,304，通过项包含 module boundaries、production/test file budget、lock poison、root/facade 与 status ownership。16 项 RED 收束为并发当前源码 owner 漂移：hot-reload 新增第 6 个 generation retry 测试、UI asset 新增第 33 个 layout regression、`BuildConfig` 从 `zircon_build.py` 拆到 `zircon_build_config.py` 后 shader-prewarm guards 仍只读入口。UI guard 已由并行改动同步；本会话让 hot-reload guard 显式锁定新测试，并新增 `read_zircon_build_sources()` 聚合入口+配置 owner，迁移 13 个失败读取点，不把字段镜像回旧入口。静态 15 个缺失 anchor 已清零，hot-reload count=6，scoped rustfmt/diff-check passed。独立只读复审逐一确认 13 个配置 owner 聚合点与剩余 8 个入口专属读取，结论 0 Critical / 0 Important / 0 Minor、Ready；需新 lib-test 编译后再声明 1,304/1,304。 |
| 优先计划状态门 | Runtime subplan frontmatter date convergence | `frameworks_02_priority_runtime_plan_status_current_binary_48_passed` | 2026-07-13 | current binary 的 `tests::runtime_absorption::plan_status` 首轮 47/48，依次暴露 Runtime 02、Runtime 11 已归档 2026-07-13 记录但 `last_refined` 仍为 2026-07-12。一次性枚举 15 个 runtime 子计划及编号归档后确认仅这两项漂移；在无活动 lease 下只将两份 frontmatter 更新到 2026-07-13，不改业务计划内容。复跑 48/48、0 failed，scoped diff-check passed。 |
