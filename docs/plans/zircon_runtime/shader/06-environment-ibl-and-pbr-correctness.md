---
related_code:
  - zircon_runtime/src/core/framework/render/environment/source_cubemap.rs
  - zircon_runtime/src/core/framework/render/environment/source_cubemap/pmrem.rs
  - zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_compute_executor.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export/frame_assertions.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export/scene_fixtures.rs
  - zircon_runtime/src/core/framework/render/material/standard_material.rs
  - zircon_runtime/src/core/framework/render/material/texture_transform.rs
  - zircon_runtime/src/asset/assets/material/material_control.rs
  - zircon_runtime/src/asset/assets/material/default_pbr.rs
  - zircon_runtime/src/asset/importer/gltf_texture_semantics.rs
  - zircon_runtime/src/asset/importer/ingest/gltf_labeled_subassets.rs
  - zircon_runtime/src/asset/importer/ingest/gltf_labeled_subassets/material.rs
  - zircon_plugins/gltf_importer/runtime/Cargo.toml
  - zircon_plugins/gltf_importer/runtime/src/lib.rs
  - zircon_plugins/gltf_importer/runtime/src/subassets.rs
  - zircon_plugins/gltf_importer/runtime/src/tests.rs
  - zircon_runtime/src/asset/tests/assets/artifact_store/material_data.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs
  - zircon_runtime/src/graphics/scene/gpu_scene/bindless_material_payload.rs
  - zircon_runtime/src/graphics/shader/template/material_surface.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/shaders/fallback_mesh.wgsl
  - zircon_runtime/src/graphics/shader/includes/zr_pbr_extras.wgsl
  - zircon_runtime/src/graphics/shader/template/material_surface.rs
  - zircon_runtime/src/graphics/shader/template/tests/environment_specular_occlusion.rs
  - zircon_runtime/src/core/framework/render/shader/variant_key.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/mesh_pipeline_variant_registry.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs
  - zircon_runtime/src/graphics/shader/wgsl/zr_gbuffer_encode_standard_pbr.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_shading_environment_only_pbr.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_forward_environment_only_pbr.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/shaders/deferred_lighting.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/shaders/fallback_mesh.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/post_process/shaders/post_process_screen_space_reflection.wgsl
  - zircon_runtime/src/core/framework/render/environment/skybox.rs
  - zircon_runtime/src/core/framework/render/environment/cubemap_projection.rs
  - zircon_runtime/tests/runtime_environment_cubemap_projection_contract.rs
  - zircon_runtime/src/core/framework/render/image/dimension.rs
  - zircon_runtime/src/asset/assets/texture/descriptor.rs
  - zircon_runtime/src/core/framework/render/image/sampler.rs
  - zircon_runtime/src/asset/assets/texture/upload_support.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_texture/gpu_texture_resource_from_asset.rs
  - zircon_runtime/tests/runtime_texture_cube_resource_contract.rs
  - zircon_runtime/src/graphics/shader/wgsl/zr_environment.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_environment_core.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_environment_generic_api.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_environment_only_pbr.wgsl
  - zircon_runtime/src/core/framework/render/backend_types.rs
  - zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/update_stats/base_stats.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_render_with_pipeline/render_frame_with_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/capacity.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/report.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/resources.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/selection.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/slot_allocator.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/tests/resources.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer/tests/slot_allocator.rs
  - zircon_runtime/tests/runtime_environment_reflection_probe_product_contract.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/shaders/skybox_procedural.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/overlay/viewport_overlay_renderer/construct/create_sky_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/primitives/scene_uniform/from_frame.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_texture/mod.rs
  - dev/cmft/src/cmft/cubemapfilter.cpp
  - dev/cmft/src/cmft/image.cpp
  - dev/cmft/src/cmft/cubemaputils.h
  - dev/cmftStudio/src/shaders/fs_mesh.shdr
  - dev/cmftStudio/src/shaders/fs_skybox.sc
  - dev/cmftStudio/src/shaders/vs_skybox.sc
  - dev/cmftStudio/src/shaders/utils.shdr
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentCapture.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentRealTimeCapture.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentDiffuseIrradiance.cpp
  - dev/UnrealEngine/Engine/Shaders/Private/ReflectionEnvironmentShaders.usf
  - dev/UnrealEngine/Engine/Shaders/Private/ReflectionEnvironmentShared.ush
  - dev/UnrealEngine/Engine/Shaders/Private/MonteCarlo.ush
  - dev/UnrealEngine/Engine/Shaders/Private/BRDF.ush
  - dev/UnrealEngine/Engine/Shaders/Private/SkyLightingShared.ush
  - dev/UnrealEngine/Engine/Source/Developer/TextureCompressor/Private/TextureCompressorModule.cpp
milestone_status:
  M5: in_progress
  M6: in_progress
  M7: in_progress
  M8: in_progress
---

# 06 环境 IBL 资产链与 PBR 正确性验收

本子计划是 shader 计划集对 render 计划 11(环境光照)的**算法定稿与验收闭环补充**:计划 11 的"工程落地细化"仍是文件落点/契约类型/帧时序的实施权威;本计划定稿其中 IBL 相关 compute 的**具体算法**(equirect→cube、GGX 预滤波、SH9、BRDF LUT)、`zr_environment.wgsl` v2 的采样端契约,以及"8×8 金属度/光滑度材质球矩阵正确"的可量化验收标准。跨计划扩展按 shader index §4 惯例在计划 11/13 状态表登记镜像条目。

```zircon-workflow
{
  "schema": 1,
  "workflow_id": "zircon-runtime-shader-environment-ibl-pbr-correctness",
  "goal": "完成真实环境 IBL 资产链、GGX PMREM、PBR 采样与多视角产品验收",
  "milestones": [
    {"id": "M1", "title": "EC-M1 方向与 PBR 数学正确性", "depends_on": []},
    {"id": "M2", "title": "EC-M2 cubemap 资源与环境绑定", "depends_on": ["M1"]},
    {"id": "M3", "title": "EC-M3 离线 IBL 资产与派生物", "depends_on": []},
    {"id": "M4", "title": "EC-M4 实时 IBL 与产品验收", "depends_on": ["M3"]},
    {"id": "M5", "title": "EC-M5 当前源码交互查看器交付", "depends_on": ["M1", "M2", "M3", "M4"]},
    {"id": "M6", "title": "EC-M6 环境专用 PBR 性能前向收口", "depends_on": ["M1", "M2", "M3", "M4"]},
    {"id": "M7", "title": "EC-M7 全局 IBL 核心与环境专用 Provider 收口", "depends_on": ["M6"]},
    {"id": "M8", "title": "EC-M8 持久化 IBL recipe 收束与可测量启动性能", "depends_on": ["M3", "M4", "M5"]}
  ]
}
```

## 1. 问题定位:当前渲染为什么全是马赛克

2026-07-04 的 SH04/Plan 11 状态行(index §4.1)所验证的"sampled environment"路径存在三个结构性缺陷,与截图观察(天空盒马赛克、mip 无模糊、PBR 矩阵不可信)一一对应:

1. **没有环境贴图,只有 128 个数**。`SkyboxSettings.sampled_equirectangular` 是 16×8=128 个 vec4 的 CPU 数组(`skybox.rs` 的 `SAMPLED_EQUIRECT_ENVIRONMENT_WIDTH/HEIGHT`),经 scene uniform 上传;`zr_environment.wgsl` 的 `zr_environment_sampled_equirect_color` 做**最近邻**索引。天空盒的物理分辨率就是 16×8——马赛克是数据形态的必然,不是滤波 bug。
2. **没有预滤波,粗糙度是假的**。`zr_environment_reflection_color` 用 `mix(sky(reflect_dir), sky(normal), roughness)` 两方向插值冒充模糊;不存在 GGX 卷积 mip 链,"mipmap 全是马赛克"是因为根本没有生成过预滤波 mip。
3. **没有 split-sum,能量是拍的**。`zr_environment_pbr_indirect` 用 `0.35`/`0.20`/`1.0 - roughness*0.65` 经验常数近似 diffuse/specular 权重;无 SH irradiance、无环境 BRDF 项,金属度/光滑度扫描不可能呈现正确梯度。

前置缺口:`gpu_texture` 无 `Cube`/`D2Array` 维度,`CubemapAsset`(计划 13 TX-M3)未开工。

2026-07-06 复核结论:用户指出的 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 问题成立。该图必须标记为 **rejected baseline**:它只能证明 16x8 sampled-equirect 路径不可用,不能证明 cubemap/PMREM 方案完成。当前可继续验收的路径从 2026-07-05 source-cubemap/2K PMREM 图开始,但 EC-M3 仍需补严格高频/roughness 单调截图、SSIM 与 GPU/offline bake 对拍。

## 2. 参考实现精读结论(cmft / cmftStudio / UE5)

动手前必读下表源文件(index §5 第 8 条参考对照纪律)。以下为已完成的精读结论,实现期直接对照。

### 2.1 cmft:equirect→cubemap 与离线滤波

| 机制 | 结论 | 出处 |
|------|------|------|
| 面尺寸 | `dst_face_size = (equirect_H + 1) / 2`(2:1 全景图 → 面 = H/2;1K HDRI 512 高 → 面 256) | `image.cpp:3143` |
| texel→方向 | `uu,vv ∈ [-1,1]`,`dir = normalize(uu*faceU[f] + vv*faceV[f] + faceN[f])`;六面轴表见 `cubemaputils.h:38–70` | `cubemaputils.h:149–161` |
| 方向→equirect UV | `u = (π + atan2(x,z)) / 2π`,`v = acos(y) / π`,**双线性**采样 | `cubemaputils.h:221–228`,`image.cpp:3185–3219` |
| 逐 texel 立体角 | `areaElement(x,y)=atan2(x·y, √(x²+y²+1))` 四角差分(Rory Driscoll 精确式),用作滤波/SH 权重 | `cubemaputils.h:291–313` |
| radiance 滤波 | 逐 texel `weight = solidAngle · dotᵖ`(p=specular power)锥角裁剪 + per-face bbox 加速;gloss→power 为指数映射 `2^(scale·g+bias)` | `cubemapfilter.cpp:774–862, 1811–1855, 2100–2108` |
| 末级 mip | 1×1 时六面取平均,消除方向噪声 | `cubemapfilter.cpp:2181–2199` |
| edge fixup(Warp) | 烘焙端 `u' = u + warp·u³` 三次扭曲(nvtt 出身),采样端 `fixCubeLookup` 把非主轴分量按 `1 - 2^lod/size` 压缩——**为无 seamless filtering 的旧 GL 准备** | `cubemaputils.h:163–185`,`utils.shdr:51–63` |
| SH | 5 阶 25 系数,立体角加权累加后 `norm = 4π/Σω`;重建乘 cosine lobe `A_l = 1, 2/3, 1/4, 0, -1/24`;**无 windowing** | `cubemapfilter.cpp:124–350` |
| 消费端 | `lod = (1-gloss)·(mipCount-1)` 线性;`envSpec = fresnel(F0, NoV) · radiance`——**无 split-sum LUT**,是简化 IBL | `fs_mesh.shdr:194–225` |
| 容器 | DDS face-major / KTX mip-major;RGBA16F/RGBA32F 全支持 | `image.cpp:1416–1432, 5236, 5243–5362` |

**2026-07-07 运行时验证链取舍修订（历史，已被 2026-07-11/13 UE GGX FIS 硬切取代）**:用户指出镜面/天空盒仍不对后,当时的 CPU source-cubemap PMREM 与采样端先按 cmft/cmftStudio 消费模型对齐。方向/立体角/双线性公式与"末级 mip 平均"继续原样采纳;PMREM mip1..N 使用 `specularPowerFor(mip,mipCount,glossScale=10,glossBias=3)` + `BlinnBrdf(power/4+1)` 的 cosine-power radiance lobe,以 Hammersley 锥采样近似 cmft 的逐 texel solid-angle 积分;采样端 roughness→LOD 改为 cmftStudio 的线性 `roughness * (mipCount - 1)`,并在 skybox/specular/IEM/source cube lookup 前加入 `fixCubeLookup` 压非主轴分量。该段只保留迁移历史,不得作为当前 PMREM 实现依据。

### 2.2 UE5:捕获管线、split-sum 与实时环境

| 机制 | 结论 | 出处 |
|------|------|------|
| 捕获流程 | 逐面渲染 → mip0 premultiply alpha → `CreateCubeMips` 降采样链 → `ComputeDiffuseIrradiance`(SH9)→ `FilterCubeMap`(GGX) → 拷入 `TextureCubeArray` | `ReflectionEnvironmentCapture.cpp:490` |
| 分辨率/格式 | 128 或 256,`PF_FloatRGBA`(FP16);mip 数 = `CeilLogTwo(size)+1` | `ReflectionEnvironmentCapture.cpp:274` |
| roughness↔mip | `LevelFrom1x1 = 1.0 - 1.2·log2(max(r, 0.001))`,`mip = maxMip - 1 - LevelFrom1x1`;逆映射 `r = exp2((1.0 - LevelFrom1x1)/1.2)`——mip↔roughness 与总 mip 数无关 | `ReflectionEnvironmentShared.ush:16–39` |
| GGX 重要性采样 | Hammersley 序列,`E.y *= 0.995` 防掠射;`CosTheta = √((1-E.y)/(1+(a²-1)E.y))`,`a² = r⁴`(α=r² 的平方);`L = 2(H·V)H - V` | `MonteCarlo.ush:347–363`,`ReflectionEnvironmentShaders.usf:612–648` |
| **filtered importance sampling** | 当 `V=N` 时按样本 PDF 选源 mip:`PDF_L = D·NoH/(4·VoH) = D/4`,`Ω_s = 1/(N·PDF_L)`,`Ω_p = 4π/(6·size²)·2`,`srcMip = 0.5·log2(Ω_s/Ω_p)`;NoL 加权累加——这是 32/64 样本就能干净的关键 | `ReflectionEnvironmentShaders.usf:612–648` |
| 样本数 | 桌面 `r<0.1 ? 32 : 64`;参考路径 1024 | `ReflectionEnvironmentShaders.usf:561–580` |
| split-sum LUT | `PreIntegratedGF` 默认 128×32、128 样本,UV=(NoV, roughness),RG 双通道;reference 开关才使用 128×128;`GF = F0·A + saturate(50·F0.g)·B`(F90 项) | `SystemTextures.cpp:534–670`,`BRDF.ush:559–573` |
| 无 LUT 近似 | Lazarov 多项式 `EnvBRDFApprox`(常数 c0/c1 见出处),移动端用 | `BRDF.ush:583–604` |
| SH9 计算 | 实时路径:8×8=64 线程均匀球采样 mip2,树形归约,输出 8×float4(预乘 diffuse 卷积系数);离线路径:32³ 中间 cubemap 三 pass 归约 | `ReflectionEnvironmentShaders.usf:759–892`,`ReflectionEnvironmentDiffuseIrradiance.cpp:88–230` |
| SH9 重建 | `GetSkySHDiffuse`:9 点积无分支(L0+L1 一组 dot4、L2 双线性项、L2 m=±2 项),`max(0, …)` 截断 | `SkyLightingShared.ush:84–103` |
| 探针混合 | light grid 剔除后按覆盖半径大→小 under-operator 累加:`rgb += s.rgb·a_acc`,`a_acc *= 1-s.a`,`a<0.001` 早退;剩余 alpha 给 skylight | `ReflectionEnvironmentComposite.ush:12–188` |
| parallax | box:局部空间 AABB 最远交点,`DistanceAlpha = 1-smoothstep(0, 0.7·transition, boxDist)`;sphere:二次方程远交点 | `ReflectionEnvironmentShared.ush:136–201` |
| SSR/RT/Lumen 组合 | 高频源(SSR/Lumen/RT)先写,`Color.a = 1 - hit.a`,探针吃剩余 alpha,skylight 兜底,最后统一乘 `EnvBRDF` | `ReflectionEnvironmentPixelShader.usf:113–281` |
| 实时天空捕获 | 时间切片:每帧渲染 N 面(云默认 2 面/帧)→ `FDownsampleCubeFaceCS`(8×8)→ `FConvolveSpecularFaceCS` 分帧卷积 → SH 一次性 64 线程 | `ReflectionEnvironmentRealTimeCapture.cpp:340, 84–153` |

**取舍**:mip 映射常数(1.0/1.2)、GGX 采样式、FIS、样本数、LUT 尺寸/公式、SH9 归约与重建、SSR→探针→天空的 alpha under 组合契约全部原样采纳;探针混合沿用计划 11 已定稿的 top-2 截断(URP 量级)而非 UE 的 grid 全列表;light grid 剔除、Lumen 本体不在本计划范围。filtered 分支的 source LOD 只由上述 PDF 公式和 source 最大 mip 约束,不得再以 destination texel footprint 作下限;destination footprint 只用于 mip0 直接缩放/普通 source mip 构建。

### 2.3 2026-07-06 cubemap/PMREM 设计修订

- **source 与 PMREM 分离**:source cubemap mip0 是天空盒显示源;specular PMREM 是反射卷积结果。任何把 PMREM 粗 mip 用作天空背景的实现都会把反射模糊直接变成背景马赛克。
- **分辨率规则**:按 cmft 的 equirect height 规则,`polyhaven_lakes_1k.hdr`(1024x512)的 source face 是 256,`polyhaven_lakes_2k.hdr`(2048x1024)的 source face 是 512。文档和测试不得再写"1K 默认 512 face"。
- **PMREM mip 生成**:当前 CPU `pmrem.rs` 与 GPU `ibl_prefilter.wgsl` 均采用 UE ReflectionEnvironment GGX filtered importance sampling:Hammersley GGX/cosine sampling、`V=N` 下 `PDF_L=D/4`、PDF 选择 source mip、UE roughness↔mip 映射和末级六面平均。cmft/cmftStudio 继续提供 face order、投影、跨面采样、edge/final-average 与 source/PMREM 角色边界参考,不再作为生产 PMREM 的 cosine-power/线性 LOD 算法。
- **mip 模糊纪律**:source mip chain 只是 FIS 的输入 pyramid,不能当 PMREM 结果。`roughness >= 0.99` 的高粗糙度尾部按 UE `FilterPS` 的 cosine hemisphere branch 从 source cubemap 金字塔卷积采样,不能从上一层 PMREM 普通降采样;末级 1x1 六面平均;验收要覆盖高频方差下降、cube seam 下降和截图级 roughness 单调。
- **cmft/cmftStudio 保留价值**:face order、lat-long、exact solid angle、邻面 edge 处理、final mip averaging 与 IEM/PMREM 双贴图消费分工继续作为实现纪律;Warp fixup 仅作旧 GL/调试参考,wgpu 原生 cube seamless filtering 下不作为默认算法。

### 2.4 UE TextureCompressor:普通 cubemap mip 与 PMREM 的边界

| 机制 | 结论 | 出处 |
|------|------|------|
| long-lat base cube | 源图先线性化;每个 face texel 用 texel center 得到 world direction,再转 longitude/latitude 采样源 HDRI。该阶段只生成 source cubemap mip0,不做反射卷积。 | `TextureCompressorModule.cpp:2254–2315` |
| 普通纹理 mip | `GenerateMipChain` 用可分离 Gaussian/sharpen kernel 与可选 2x2 average intermediate chain;这是普通 2D 纹理 mip,不是 cubemap angular mip。 | `TextureCompressorModule.cpp:1799–2057` |
| cubemap angular mip | `GenerateAngularFilteredMips` 先构建简单平均 mip 作为输入加速,再按每个输出 mip 的 cone angle 选择输入 mip,跨六面在角锥内积分并用平滑核归一化。该算法解决 cubemap 缩小/低 mip 的角域模糊与接缝一致性。 | `TextureCompressorModule.cpp:2473–2651` |
| 角色边界 | angular cubemap mip 是 source cubemap 的 mip pyramid/texture minification/FIS source LOD,不是 specular BRDF PMREM。标准 PBR 反射仍使用 §4.2 的 UE ReflectionEnvironment GGX FIS。 | `ReflectionEnvironmentShaders.usf:612–648` + `TextureCompressorModule.cpp:2473–2651` |

设计取舍:
- `source_cubemap_mipmap` 必须成为独立 owner:输入 source mip0,输出 source mip pyramid;它可服务 skybox 缩小、FIS 源 LOD、诊断 atlas 与资产 `.zcube` source mip,但不写 PMREM。
- 当前 `source_cubemap::mipmap` 已切到 UE angular-filtered 结构：先建平均输入链，按输出 mip 角面积与 quality bias 选输入 mip，跨六面 cone 积分，末级 1x1 六面平均；旧 face-aware bilinear/box pyramid 不再是当前 owner。
- `pmrem.rs` / `ibl_prefilter.wgsl` 继续只拥有 GGX/cosine radiance convolution;不得把 ordinary/angular source mip downsample 当作 PMREM 高粗糙度结果。
- `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 继续作为 rejected baseline;下一张有效截图必须同时给 source skybox、PMREM mip diagnostic 和 8x8 roughness/metallic matrix 的定量指标。

## 3. 目标数据链(总览)

```
.hdr/.exr equirect(≥1K,建议 2K)
   │ 导入:计划 13 TX-M3 .zcube(RGBA16F,face-major,含全 mip)——就绪前由 EC-M1 的运行期路径顶替
   ▼
[env.equirect_to_cube]   compute/EC-M1 CPU bridge:双线性重投影 → 源 cubemap mip0(面尺寸 = min(pow2((H+1)/2), 1024))
   ▼
[env.source_cubemap::mipmap] UE angular filtered cubemap mip pyramid(过渡期可用 face-aware CPU pyramid)
   ▼                                + EC-M2a CPU GGX FIS PMREM bridge(正式 GPU/RGBA16F PMREM 前过渡)
GpuEnvironmentMap(计划 11 落点):
   ├─ [env.ibl_prefilter]      compute:128³×8mip GGX 重要性采样 + FIS(mip0 直拷,末级 6 面平均)
   ├─ [env.ibl_irradiance_sh]  compute:SH9 立体角加权树形归约(源取 32³ 档 mip)
   └─ [env.brdf_lut]           CPU init:128×32 RG16F split-sum LUT(设备生命周期一次,与环境内容无关)
   ▼
采样端 zr_environment.wgsl v2:
   skybox pass   → 源 cubemap(全分辨率 trilinear)          ← 消灭天空马赛克
   specular 项   → prefiltered 链 mip=对数映射 × EnvBRDF(LUT) ← 消灭假粗糙度
   diffuse 项    → SH9 求值 × albedo × (1-metallic)          ← 消灭 0.35 常数
```

关键分离:**天空盒显示采样源 cubemap,不采样 128³ 预滤波链**。当前"天空盒分辨率极低"的另一半原因是显示与 IBL 共用同一低分辨率数据;UE/Unity 的天空背景都走全分辨率源(或天空材质),预滤波链只服务反射。

烘焙产物(PMREM mip 链、SH9、可选 IEM)按 §4.7 三来源优先序取用:资产伴生离线产物 → 运行期回写缓存 → 现场 compute;命中离线产物时运行期环境 compute dispatch 为 0。

## 4. 算法定稿

### 4.1 equirect → cubemap(`equirect_to_cube.wgsl`)

- 每 texel 一线程(8×8 workgroup,z=face)。texel 中心寻址:`uu = (2(x+0.5)/size) - 1`(修正 cmft 的非中心寻址)。
- 方向公式与六面轴表照抄 cmft(`cubemaputils.h:38–70, 149–161`);方向→UV 用 `u=(π+atan2(x,z))/2π, v=acos(clamp(y,-1,1))/π`,源图双线性(equirect 源以 `texture_2d` + linear sampler 上传,直接硬件双线性)。
- 面尺寸:`clamp(next_pow2((H+1)/2), 64, 1024)`。该节点只承诺 source cubemap mip0 的正确重投影;source mip pyramid 归 `env.source_cubemap_mipmap`/`source_cubemap_mipmap.rs` 后续节点所有,避免 equirect 投影、普通 mip 与 PMREM 混在同一 owner。

### 4.1a source cubemap angular mip(`source_cubemap/mipmap.rs` / `env.source_cubemap::mipmap`)

- 输入:source cubemap mip0(RGBA16F face-major,+X,-X,+Y,-Y,+Z,-Z)。输出:source cubemap 全 mip pyramid,供 skybox trilinear minification、GGX FIS `src_mip` 采样、离线 `.zcube` source mip 与诊断 atlas 使用。
- 算法标准:参照 UE `GenerateAngularFilteredMips`。先从 mip0 构建简单平均链作为积分输入;对每个输出 mip/face/texel,以该 texel direction 为 cone axis,按 mip texel angular extent 得到 cone angle;依据球面覆盖面积和 quality bias 选择输入 mip;遍历六面中落入 cone 的 texel,用 smoothstep 类核和 cubemap solid angle 加权,最后归一化写出。
- 末级纪律:source 最终 1x1 mip 做六面共同平均,与 cmft final mip averaging 一致;这样 skybox 低 mip 与 FIS 源 LOD 不保留 face-local 方向噪声。
- 当前实现（2026-08-01）：`source_cubemap/mipmap.rs` 已从 mip0 构建 average input chain，按输出角域和 quality bias 选输入 mip，跨六面做 cone traversal，以 cubemap solid angle 与平滑核归一化，并对最终 1x1 六面共同平均；face 级任务使用既有并行执行器。该 owner 已不再是 face-aware bilinear/box bridge，后续只按定量质量与吞吐门禁继续收敛。
- 测试:高频棋盘/条纹 cubemap 的 source mip luma variance 必须随 mip 单调下降;六面边界平均差随 mip 下降;`runtime_shader_pbr_real_hdri_lakes_*_mip_diagnostic_*.png` 中 source mip 区域不允许出现 nearest-neighbor 块状放大。

### 4.2 GGX 预滤波(`ibl_prefilter.wgsl`)

固定 128³×8 mip(计划 11 定稿),每 mip 一 dispatch,特化常量 = 该 mip 的 roughness 与样本数:

```
// 每 mip roughness(采样端映射的逆,常数 1.0/1.2,mip 数无关)
level_from_1x1 = (num_mips - 1) - mip
roughness(mip) = exp2((1.0 - level_from_1x1) / 1.2)
// mip0:roughness≈0,直拷源 cubemap(重采样到 128)

// 每 texel(法线 N = texel 方向,V=N 假设,切空间 Z=N):
alpha = roughness²;a2 = alpha²                    // UE Pow4 约定
for i in 0..N_samples:
    E = hammersley(i, N);E.y *= 0.995
    cos_theta = sqrt((1 - E.y) / (1 + (a2 - 1) * E.y))
    H = spherical(2π·E.x, cos_theta)               // 切空间半向量
    L = 2·H.z·H - vec3(0,0,1)                      // 反射方向
    NoL = L.z
    if NoL > 0:
        pdf = D_GGX(a2, H.z) * 0.25
        omega_s = 1.0 / (f32(N) * pdf)
        omega_p = 4π / (6 · src_size²) * 2.0
        src_mip = clamp(0.5 * log2(omega_s / omega_p), 0.0, src_max_mip)   // FIS
        color += textureSampleLevel(src_cube, world(L), src_mip).rgb * NoL
        weight += NoL
out = color / max(weight, 1e-4)
// 末级 mip(1³ 或 2³)dispatch 后追加六面平均 pass(cmft 纪律)
```

样本数档位沿用计划 11 表(Fast 32 / Normal 64 / High 128,`roughness<0.1` 减半),**FIS 是硬性前提**——没有按 PDF 选源 mip 时这些样本数不足以消噪,这正是当前直觉"需要几千样本"与 UE 实际 32/64 的差距来源。累加在 f32 域进行,写出 RGBA16F。

### 4.3 SH9 投影(`ibl_irradiance_sh.wgsl`)与重建

- 源取源 cubemap 的 32³ 档 mip(UE `GDiffuseIrradianceCubemapSize=32` 同量级)。
- 每面 8×8 线程读全部 texel,权重用 cmft 精确 `texelSolidAngle`(非均匀球近似);workgroup 共享内存树形归约(64→1),6 面结果二段归约,`norm = 4π/Σω`。
- 输出即计划 11 的 `array<vec4<f32>, 9>` 布局(L00,L1-1,L10,L11,L2-2,L2-1,L20,L21,L22),**烘焙端预乘** cosine lobe `A_l ∈ {1, 2/3, 1/4}` 与基函数常数(Stupid SH Tricks);采样端 `zr_environment_sh9_eval(n)` 为 9 项多项式无分支求值 + `max(vec3(0), …)` 截断(数学等价于 UE `GetSkySHDiffuse` 的 dot4 重排)。无 windowing(3 阶 + 低频信号,ringing 不构成问题;若未来夹具出现负瓣再补 Hanning)。
- CPU 参考实现(测试用)与 compute 用同一权重公式,逐系数对拍。

### 4.4 环境 BRDF LUT(`brdf_lut.wgsl`)

- 128×32 `rg16float`,UV=(NoV, roughness),128 样本 Hammersley GGX 重要性采样积分 Karis split-sum 的 A/B 两项;几何项与 UE `SystemTextures.cpp` 相同,使用 `Vis_SmithJointApprox` 后累加 `NoL * Vis * (4 * VoH / NoH)`，不得替换为可分离 Schlick-IBL `G1(V)*G1(L)`。设备生命周期 CPU 生成一次(不进 `IblBakeKey`)。该规模对齐 UE 默认 `PreintegratedGF`,相对旧 128×128×1024 工作量减少 32 倍;同一 joint-Smith 积分器的 16×16 稀疏 128-vs-4096 样本对比为 RG 平均绝对误差 0.00302355、最大 0.02227185,仍须由 fresh 托管启动计时与画面验收确认。
- Forward/basic、fallback 与 deferred 的基础各向同性直接光统一消费 `zr_pbr_extras_core.wgsl` 的单一 GGX owner，并与 LUT 使用同一 UE joint-Smith 近似可见性，避免渲染路径及 split-sum 预积分之间的模型分叉。共享 core 自包含半角向量归一化；environment-only deferred profile 不引入该直射光源码。原精确标量式仅迁入高级 include 以保持当前各向异性行为，基础编译闭包不得携带该高级实现；真正的各向异性 joint-Smith 方向项属于高级能力后续，不在 MVP 内扩张。
- 采样端 `GF = F0 · A + saturate(50·F0.g) · B`(F90 白色项,UE 式)。
- 同时在 `zr_environment.wgsl` 保留 `zr_environment_env_brdf_approx(F0, roughness, NoV)` Lazarov 多项式(常数照抄 `BRDF.ush:583–604`)作为低端档候选；当前 MVP 未建立对应的全局 define/permutation，Standard PBR 环境反射固定走 LUT。只有计划 08 同时关闭全局质量档、PSO 预热与启动成本门禁后才允许接线，不得成为材质局部 option。

### 4.5 采样端契约(`zr_environment.wgsl` v2)

当前 scene 环境绑定固定为 group0：source cube/sampler/BRDF LUT/specular PMREM/IEM/SH9 分别在 binding 1..6；group1 binding 16..18 只供反射探针，binding 29/30 供 planar reflection。删除 `scene.environment_samples` 后接口:

```wgsl
fn zr_environment_mip_from_roughness(roughness: f32, max_mip: f32) -> f32; // §2.2 对数式
fn zr_environment_sh9_eval(n: vec3<f32>) -> vec3<f32>;                     // 预乘 SH9,9 点积
fn zr_environment_sky_color(dir: vec3<f32>) -> vec3<f32>;                   // skybox:源 cube trilinear
fn zr_environment_pbr_indirect(
    world_position, normal_ws, view_dir_ws, roughness, metallic,
    diffuse_color, base_color, occlusion, is_standard_pbr,
) -> vec3<f32>;
// 内部:
//   F0 = mix(vec3(0.04), base_color, metallic);F90 = saturate(50·F0.g)
//   specular = prefiltered(R, mip(roughness)) · (F0·A + F90·B)
//   diffuse  = zr_environment_sh9_eval(n) · diffuse_color · (1 - metallic)
//   diffuse *= occlusion
//   specular *= saturate(pow(NoV + occlusion, roughness²) - 1 + occlusion)
//   return (diffuse + specular) · intensity                  // intensity/rotation 采样期,不进 bake key
```

roughness 定义统一:材质 smoothness → `perceptual_roughness = 1 - smoothness`;预滤波/映射/LUT 全部消费 perceptual_roughness,GGX 内部 `α = r²`。source-cubemap 与 realtime-IBL 的 PBR 分支必须删除 0.35/0.20/0.65 经验常数和两方向 lerp；无 cubemap 的 procedural-sky 回退只采样单次 reflected direction，不能作为 HDRI/PMREM 验收路径或标准 PBR 的正确性依据，必须由 EC-M4 的实时 capture/PMREM 链替换而非扩散到 source 路径。

AO 契约按 glTF、cmftStudio 与 UE 的职责边界收束：glTF `occlusionTexture.strength` 是材质 owner 的归一化属性，内置 ingest 与 stable `gltf_importer.gltf` 插件共用 canonical property key，导入后贯通 artifact property、`StandardMaterialDescriptor`、`MaterialRuntime`、runtime-prepare/Hybrid GI card capture 与既有 GPU `data0.z`。光栅和 card capture 都使用 `mix(1, texture.r, strength)`，默认 strength=1 且不增加 uniform 大小、纹理采样或 shader permutation。AO 只影响 ambient/indirect；diffuse IBL 线性乘 AO，specular IBL 与 clearcoat IBL 复用 UE `GetSpecularOcclusion(NoV, roughness², AO)` 形式，直接光不乘 AO。AO=0 在环境纹理采样前退出，AO=1 与 roughness≈0 走无 `pow` 快路径。该消费端修改不改变 PMREM/SH9/IEM artifact 内容或 roughness→LOD 映射，因此不提升 `IBL_BAKE_ALGORITHM_VERSION`。

### 4.6 探针、实时捕获与光追(契约对齐,实施归计划 11)

- **探针捕获**:6 面渲染 → 降采样 → 复用 §4.2/§4.3 同一 compute 节点(UE `FilterReflectionEnvironment` 结构);混合用计划 11 top-2 + skybox fallback，影响域支持 box/sphere，视差校正只采用计划 11 已定稿的 box projection。球形 influence 当前直接使用反射方向采样，不能误写为已实现 UE sphere parallax。本计划只保证 prefilter/SH 节点以"源 cube 任意尺寸输入"为参数化前提,探针 128³ 与天空源共用。
- **实时环境捕获**(登记为 EC-M4,对齐 UE 时间切片):每帧预算 = 渲染 N 面(默认 2)+ 降采样该面 + 分帧 GGX 卷积(每帧 1 mip)+ 末帧一次性 SH;`IblBakeKey` 增加 `realtime_slice_state` 不参与——实时模式下 bake key 判定退化为"每 6+8 帧一轮"。V1 只做 procedural sky 的实时重烘(参数拖动即触发),场景几何入捕获留 EL-M2 之后。
- **当前 procedural sun 身份与热路径合同（2026-08-01）**：`IblBakeKey.source_hash` 消费有效太阳的归一化方向、RGB、强度和钳制后内外余弦边界；方向向量缩放保持同键，禁用/无效太阳保持零 hash，最终采样 `intensity` / `rotation_radians` 继续不进 bake key。CPU scene/capture upload 统一解析太阳并预计算方向与余弦；直接 procedural scene uniform 在 CPU 侧应用采样旋转，realtime capture 保留未旋转源方向，三条 WGSL invocation 热路径不再重复 `length` / 除法 / `cos`。`SceneUniform` 的 availability 位只做 mode/source 结构判断，不为布尔值构造 BLAKE3 bake key；完整 key 仅由真实 artifact/scheduler 身份路径消费。最终环境强度统一作用于 gradient 与 sun 的完整 radiance，保证 direct fallback 与 realtime cube sampling 一致。
- **光追/SSR 组合契约**:预留 alpha under 组合序 = 高频源(SSR/RT)→ 探针 → 天空,`indirect_specular.a` 表示未覆盖率,统一末端乘 EnvBRDF(UE `ReflectionEnvironmentPixelShader.usf` 结构)。V1 不实现 RT；当前可复用的分量入口是 `zr_environment_pbr_components(...)`，其 `ZrEnvironmentPbrComponents.specular` 承载镜面 IBL。若后续 pass 需要以剩余 alpha 组合，须在该结构化入口上扩展，不能声称已有未实现的 `zr_env_specular_ibl(...)` 符号。

### 4.7 离线烘焙镜面反射(PMREM)与三来源持久化

术语:PMREM = prefiltered mipmapped radiance environment map,即 §4.2 GGX 预滤波 mip 链的持久化形态(cmft CLI 的同名产物)。HDR 镜面反射的离线烘焙**不引入第二套算法**——离线与运行期跑同一份 compute WGSL(headless wgpu)或其 CPU 参考实现,产物一致性由 `render_env_prefilter_cpu_gpu_match_16` 保证。

`GpuEnvironmentMap` 装配时按三来源优先序取产物,以 `IblBakeKey` + 算法版本号判定命中:

| 优先 | 来源 | 产出时机 | 说明 |
|---|---|---|---|
| 1 | 资产伴生 derived artifact | 导入期 / staged build 期离线烘焙 | importer(计划 13 texture_importer 插件)或 `tools/zircon_build.py` staged 阶段 headless 跑 equirect→cube + prefilter + SH(+IEM),随资产持久化;镜像 shader prewarm 纪律 |
| 2 | `.zircon-cache` 回写缓存 | 首次运行期烘焙后提交后 readback 回写(计划 11 既有机制,由"仅 SH"扩为 PMREM+SH9+IEM 全产物) | 编辑器内拖 procedural 参数 / 换 HDR 的迭代路径 |
| 3 | 现场 compute | prepare 期(计划 11 帧时序 §1) | 前两级全 miss 兜底;烘完即回写为来源 2 |

- 持久化格式:face-major RGBA16F 全 mip(cmft DDS 布局纪律,`image.cpp:1416` 同序)+ SH9 144B + 可选 IEM 32³;头部携带 `IblBakeKey` 与**算法版本号**——mip 映射常数(1.0/1.2)、FIS、样本档位、SH 权重公式全部受 `IBL_BAKE_ALGORITHM_VERSION` 的同一维护纪律约束。该版本是显式维护常数；任何影响 PMREM 内容或其采样 LOD 映射的变更必须在同一补丁提升它，使旧产物自动失效重烘，不能静默混用两代产物。
- 当前 payload 合同:`IblBakeArtifactPayload` 固定按 PMREM RGBA16F 全 mip → SH9(9×vec4 f32 little-endian,144B)→ 可选 IEM 32×32×6 RGBA16F(alpha=1.0) 写入;`expected_payload_size_bytes()` 与三段 byte-range helper 是 importer/cache/file container 的共同 offset 来源。
- 当前 blob 合同:`IblBakeArtifactBlob` 固定 header + payload 完整字节容器,`decode_current_for_request(...)` 同时验证 header 魔数/格式、payload 长度和 request/key/layout/content/algorithm-version 当前性。
- 2026-08-09 canonical recipe 收束后,`.zribl` format v3 header 同时持久化 source face/mip、derived PMREM face/mip、recipe identity 与 producer provenance；asset-derived 只接受 `AssetImporterCpu`，runtime cache 只接受 `RendererGpuRuntime`。cache hash、asset-derived path、WGPU command plan 和 runtime writeback 必须从同一 `IblBakeArtifactRequest` 构造 descriptor。相同 key/PMREM 但 source layout、recipe 或 producer 不同必须拒绝命中;旧 format v1/v2 直接失效,不保留升级 shim。
- 当前 runtime cache 合同:`IblBakeArtifactCacheStore` 在 `.zircon-cache/render/ibl/v{IBL_BAKE_ALGORITHM_VERSION}/{request_hash}/face_####_mips_##.zribl` 读写 raw blob;missing/rejected 非 fatal,只有 current blob 会转成 `IblBakeArtifactCandidate::runtime_cache(...)`。
- 当前 runtime readback writeback 合同:`IblBakeArtifactReadbackSections` 以 descriptor 为真理接收 PMREM RGBA16F、SH9 与 optional IEM RGBA16F 分段字节,拒绝缺失/多余/长度错误分段;`write_ibl_bake_artifact_runtime_readback(...)` 对 stale descriptor 不写文件,current descriptor 则组装 payload/blob 并写入 `IblBakeArtifactCacheStore`。首次 cache miss 在 frame compile 前登记 request-keyed pending state，并仅为该请求保留一次 `environment_ibl_bake_request`;pending 的后续 frame 不再重建 IBL bake graph。compiled-scene 将 copy command 合入同一帧主 submit，submit 后才开始 map，并由后续非阻塞 `device.poll(Poll)` 完成 readback/writeback；其间不执行第二个 submit 或 `wait_indefinitely`。runtime cache payload 出现后才 hydration 并移除 pending state。
- 当前 WGPU RGBA16F readback helper:`read_texture_rgba16float_region(...)` 可按 mip/origin/extent 读取 `Rgba16Float` texture region 并剥离 row padding;`read_texture_rgba16float_cube_mip_chain(...)` 输出 face-major、face 内 mip 顺序的 cubemap bytes,与 PMREM artifact section layout 对齐;`SceneEnvironmentCubemap` 的 source/specular/IEM cube texture 已具备 `COPY_SRC` usage。静态环境 artifact compute 已由 compiled-scene graph 驱动；procedural gradient 则走独立 `RealtimeIblRuntime` 的双缓冲分帧 graph（每帧最多两张 capture face、后续 source mip/PMREM/SH9），在同一 command encoder 录制、提交后完成 scheduler token 并可采集 GPU timestamp。该 runtime 的 GPU textures、capture recorder 与 timestamp collector 延后至首个 procedural frame 初始化，因此 HDRI-only PBR preview 的 renderer 构造不再分配两套 realtime cubemap 资源或创建其 capture pipeline。
- 当前 resolved payload 合同:`resolve_ibl_bake_artifact_payload(...)` 消费来源 1/2 的 blob-backed candidates,复用 asset-derived > runtime-cache > runtime-compute 优先级,命中时返回可直接应用的 payload 且 dispatch=0,miss 时 `payload()==None` 并保留 required contents dispatch 计数。文件 IO 仍归 asset artifact cache/importer/staged build owner。
- 当前 runtime dispatch/cache 集成合同:`resolve_ibl_bake_artifact_runtime_dispatch(...)` 在 asset artifact 边界读取 runtime cache,合并 asset-derived blob 候选,并返回 resolved payload 与 dispatch count;`write_ibl_bake_artifact_runtime_dispatch_readback(...)` 只在该 dispatch report 需要 runtime compute 时写入 readback sections,首次 miss -> writeback -> 下一次同 request runtime cache hit 且 dispatch=0 已有合同测试,asset-derived/cache hit 时 stray readback 被跳过。该 asset 边界只负责候选决议和持久化；GPU bake resource、graph record 与帧内调度由 graphics runtime 拥有。
- 当前 source-cubemap 应用合同:`source_cubemap_mip_chain_with_bake_artifact(...)` 保留 source/display mips,仅把 decoded PMREM/SH9 payload 应用到 specular PMREM 链与 diffuse SH9;`source_cubemap_environment_with_bake_artifact(...)` 进一步保留环境 source revision/hash、intensity、rotation,并在 payload 含 IEM 时附加 `SourceCubemapIrradianceCube`;这保证天空盒仍采全分辨率源 cubemap,反射/漫反射可来自 derived/runtime cache artifact。frame compile 先接受与该 source request 当前匹配的 CPU artifact descriptor，随后才查询 runtime cache，因此 asset-derived IEM 不会被 PMREM/SH9 fallback 覆盖。
- 当前 WGPU upload identity 合同:`SourceCubemapEnvironment::ibl_bake_key()` 继续只描述 source 输入,artifact 应用不会改变 bake key;完整 artifact hash 留作资产/provenance,不进入 GPU upload key。`texture_upload_key()` 仅包含构造期缓存的 PMREM section hash 与 IEM content hash。`SceneEnvironmentCubemap::ensure_uploaded(...)` 在同一 source 命中不同 PMREM/SH9/IEM artifact、或直接替换同尺寸 IEM 时仅重传受影响的 source/specular/IEM cube;SH9-only 更新不重传 texture,IEM-only 更新不重传 source/PMREM,且帧热路径不重算哈希。
- 命中来源 1/2 时运行期环境 compute dispatch 数为 0(验收断言),与计划 08 MS-M4"二次启动 miss=0"同构。
- **外部预滤波 cubemap 的处理**:cmft/cmftStudio 产出的含 mip DDS/KTX 导入时,其 mip 链默认只作为源 cubemap 的降采样链消费;PMREM 仍由引擎按当前算法版本重新生成。2026-07-07 的 cmftStudio Blinn/cosine-power radiance + 线性 LOD 验证链已被 2026-07-11/13 的 UE GGX FIS 硬切取代,不得作为当前 PMREM 算法依据。外部容器仍缺 Zircon 的 `IblBakeKey`、算法版本、SH9/IEM payload 与 artifact header,因此不能静默声明为可直接消费的 PMREM artifact;`.zcube` 暂不设 `prefiltered` 声明位。
- 开发者使用说明:导入 `.hdr` → importer 自动产 `.zcube` + 伴生 PMREM/SH(来源 1);编辑器改 procedural 参数 → 运行期切片重烘 + 回写(来源 2/EC-M4);发布走 staged build 全量预烘,产品运行期零环境烘焙成本。

### 4.8 IEM(irradiance cubemap)替代 SH 的 diffuse 消费选项

cmftStudio 的 diffuse 消费端是 32³ irradiance cubemap 直采(`fs_mesh.shdr:210` 的 `textureCube(u_texIem, n)`),不是 SH 求值。引擎提供两种 diffuse 消费路径,由 source 环境的 IEM availability uniform 在运行时选择:当前 artifact/环境携带 IEM 时采样它,否则回退 SH9。该选择不使用 pipeline define,不新增 PSO 或材质变体。

| 路径 | 求值 | 优势 | 默认 |
|---|---|---|---|
| SH9 | `zr_environment_sh9_eval(n)` 9 项多项式 | 零纹理带宽;与 ambient/probe grid 契约同一表示(计划 11) | artifact/环境不含 IEM 时回退 |
| IEM | `zr_environment_irradiance_cube_color(n)` 1 fetch | 无 3 阶截断 ringing;强方向性环境下更准;带宽换 ALU | source artifact/环境含 IEM 时 |

- 生成:32³ RGBA16F cubemap,余弦加权重要性采样卷积(复用 §4.2 compute 骨架,kernel 换 cosine lobe)。**不采用** cmft 的"SH 重建回 cubemap"式(`imageIrradianceFilterSh`)作为主路径——直接卷积无阶数截断,精度更高;SH 重建式仅作 Fast 档快捷路径。随 §4.7 三来源链持久化,同一 `IblBakeKey`。
- SH9 无条件仍产出:它是 ambient 三模式(计划 11 EL-M4)与 probe grid(EL-M3)契约的权威表示,IEM 只是 standard PBR diffuse 项的可选消费端,不替代契约层。
- 绑定:当前 scene group0 环境段 binding5 为 `zr_environment_irradiance_cube`(`texture_cube<f32>`),主渲染路径无 IEM 时绑定 1x1 黑色 fallback 且 availability uniform 保持 SH9 分支不采样它,有 IEM 时上传 32x32x6 RGBA16F 并在同一 pipeline 中选择 IEM。
- 一致性纪律:IEM 与 SH9 求值在低频上必须一致(球面 64 方向对拍,均值/低频差异 < 容差);高频差异是 IEM 的合法优势,不设上限。

## 5. 里程碑与切片

依赖:计划 13 TX-M3(`.zcube` 资产)未就绪前,EC-M1 以"运行期从已加载 HDR 像素生成源 cubemap"打通(数据已在 `docs/tests/runtime/shader/assets/polyhaven_lakes_1k.hdr` 链路上);TX-M3 落地时导入期直接产出含 mip 的 `.zcube`,运行期 equirect_to_cube 节点仅保留给"源是 equirect 资产"的形态,**不留双轨**(同一 `GpuEnvironmentMap` 入口,来源判别切换)。

**EC-M1 源 cubemap + skybox 全分辨率(先消灭天空马赛克)**
- 触碰:`gpu_texture`(Cube 维度 + 逐面逐 mip 上传/视图)、`equirect_to_cube.wgsl`、`gpu_environment_map.rs`(计划 11 落点)、skybox 采样改走 cube 纹理;删除 `SAMPLED_EQUIRECT_ENVIRONMENT_*`、`SkyboxMode::SampledEquirectangular` 的 sample table 形态、scene uniform `environment_samples` 字段与 `zr_environment_sampled_equirect_*` 函数(硬切换)。
- 完成判据:`.\.codex\skills\zircon-dev\scripts\validate-matrix.ps1 -Package zircon_runtime -SkipTest` 受管构建通过;真实 HDRI 天空盒截图无 16×8 块状(与 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 对比);仓库内 `environment_samples`/`SAMPLED_EQUIRECT` 零命中。

**EC-M2 GGX 预滤波 + SH9 + BRDF LUT + 采样端 v2(消灭假粗糙度与经验常数)**
- 切片 1(算法与采样端):`ibl_prefilter.wgsl`/`ibl_irradiance_sh.wgsl`/`brdf_lut.wgsl` 与对应 Rust 构建(计划 11 落点 `ibl_prefilter.rs` + 新增 `env_brdf_lut.rs`)、`zr_environment.wgsl` v2、binding 接线、`IblBakeKey` 消费(procedural 参数/HDR 内容哈希触发重烘)。
- 切片 2(离线持久化,§4.7):derived artifact 读取 + readback 回写(PMREM+SH9)+ 三来源优先序 + 算法版本失效;importer/staged build 预烘入口。
- 切片 3(IEM 选项,§4.8):余弦卷积 compute、IEM availability uniform 接线、`zr_environment_irradiance_cube` 槽绑定、与 SH9 低频对拍。
- 完成判据:`render_env_mip_from_roughness_roundtrip`、SH 常色→band0、CPU/GPU prefilter 16³ 小尺寸对拍、LUT 角点特值(NoV→1,r→0 时 A→1,B→0)全绿;粗糙度阶梯抓帧模糊单调;bake key 命中来源 1/2 时二次启动环境 compute dispatch 数 = 0;IEM/SH9 低频对拍绿。

**EC-M3 8×8 材质球矩阵验收闭环(直到正确)**
- 触碰:升级 ignored export 用例为 IBL 路径 + 新增非 ignored 定量断言测试(离屏 readback,§6 判据);截图归档 `docs/tests/runtime/shader/`。
- 完成判据:§6 六条判据全部量化通过,截图与 Unity/UE 校准图目视一致。

**EC-M4 探针捕获接线 + 实时天空捕获时间切片(镜像计划 11 EL-M2)**
- 2026-08-29 P0-8 hard-cut 复审确认：`7a20f921b` 已删除 `SceneRenderer::render_scene_color_hdr` / `last_transient_resource_pool_report`，retained HDR capture 已迁至 `RenderFramework + viewport`，但可选 probe runtime/editor 仍调用旧接口并让 editor 持有 raw renderer，故当前首先是插件编译边界过期，而非可在旧函数内微调的性能问题。拒绝恢复旧 API、兼容 shim 或机械创建六个 viewport：后者仍会六次 `finish_submission + wait/readback`，并保留六面 RGBA32F、CPU mip/PMREM/SH9 与同步持久化。旧路线在 face size `S` 下仅六面 decoded capture 就是 `96*S^2` bytes，连同新 source mip storage 的保守下界为 `224*S^2`，128/256/512/1024 分别至少 3.5/14/56/224 MiB，尚未计 staging、PMREM/IEM 与 artifact。Unreal 参考的可迁移结构是一个 builder 下的六 view、共享 GPU cube、GPU filter 与直接发布到 scene cube array；其当前分支仍含逐面 GPU idle，不能冒充无 stall 基线。cmftStudio 只证明长滤波应有后台 job 与显式终态。目标硬切按 neutral request/handle/status DTO → bounded runtime scheduler（revision/cancel/failure/last-good）→ GPU-resident six-view capture → 既有 GPU IBL graph/atomic residency → optional async readback/persistence/editor-cook 共用 facade 的顺序实施；editor 不再接触 `SceneRenderer`。128/256 五冷五热及 CPU/GPU、submission/wait、readback、RSS/VRAM、RenderDoc、WPR/WPA 门禁见预优化审计。当前插件无法形成有效 before binary，且共享树仍有外部编译错误，所以本轮只登记结构结论，不声称优化收益，不前移 M5-M8、Failure、manifest 或 coordinator commit。
- 触碰:归计划 11 EL-M2 实施,本计划交付参数化 prefilter/SH 节点与时间切片调度表;procedural sky 参数拖动实时重烘走切片路径。
- 完成判据:计划 11 EL-M2 判据 + 拖动 procedural 参数时帧时间无尖峰(切片生效)。

**EC-M5 当前源码交互查看器交付(关闭手动复核入口漂移)**
- 触碰:`zircon_shader_pbr_viewer` 只通过真实 runtime module descriptor 和 `ProjectAssetManagerAccess` 创建生产 `SceneRenderer`;不恢复直接 concrete manager 构造器,不增加 viewer-only renderer 旁路。
- 完成判据:当前源码 Windows production build 通过;`--help` 通过;Lakes HDRI DX12 窗口在后台烘焙期间持续响应并进入 `Ready`;鼠标左键 orbit/滚轮 zoom 保持可用;当前窗口截图和一次性 DX12 RenderDoc capture/replay 证据归档到 `docs/tests/runtime/shader`,可执行文件位于协调器管理的外部构建目录而非仓库 `target`。

执行顺序 EC-M1 → EC-M2 → EC-M3 → EC-M4 → EC-M5;EC-M1/M2 各自切片内硬切换删除被取代路径。

## 6. "8×8 材质球矩阵正确"的量化判据(全局验收)

矩阵布局沿用现有 export 用例:列 = metallic 0→1,行 = smoothness 0→1,真实 HDRI 天空。六条判据(离屏 readback 自动断言 + 截图人工复核):

1. **镜面正确性**:smoothness=1、metallic=1 球面中心区域与"按反射向量直接采样源 cubemap"的参考渲染 SSIM ≥ 0.95(天空镜像清晰可辨,湖面/地平线不糊)。
2. **粗糙度单调性**:任一 metallic 列沿 smoothness 递减方向,球面高频能量(Laplacian 方差)严格单调下降,且相邻档差异 > 噪声阈值(证明 mip 链在被使用且逐级模糊)。
3. **电介质行正确**:metallic=0 行球面主体颜色与 `SH9(n)·albedo` 参考着色的平均色差 ΔE < 阈值,且仍带 4% F0 的弱边缘高光(Fresnel 掠射变亮可见)。
4. **金属能量合理**:metallic=1、smoothness=0 球的平均亮度 ∈ [SH 均值×F0 分量下限, 源图均值×1.05]——不为黑(当前实现的典型失败),也无能量爆炸。
5. **无块状伪影**:天空背景与球面反射的块边界检测(16×8 网格对齐的梯度峰)命中数 = 0。
6. **无 cube 接缝**:预滤波链每 mip 六面共 12 条边的跨边亮度差 < 阈值(替代 cmft Warp 的防回归断言)。

判据 1–4 同时构成"调节金属度/光滑度示意图"与 Unity 校准图表 / UE 材质示例的对照基础;截图命名 `runtime_shader_pbr_ibl_metallic_smoothness_matrix_<date>.png` 归档。

## 7. 测试清单

| 测试 | 断言 | 位置 |
|---|---|---|
| `runtime_environment_cubemap_projection_contract_matches_cmft_face_axes` / `runtime_environment_equirect_uv_contract_matches_cmft_latlong_axes` | ±X/±Y/±Z 面中心与四角 texel 的方向→UV 与 cmft 公式闭式对拍 | `zircon_runtime/tests/runtime_environment_cubemap_projection_contract.rs` + `core/framework/render/environment/cubemap_projection.rs` |
| `runtime_texture_cube_resource_contract` | `dimension = "cube"` 默认 6 面、`cubemap` 别名拒绝非 6 倍数层数、完整 RGBA8 六面 payload upload-ready | `zircon_runtime/tests/runtime_texture_cube_resource_contract.rs` + `asset/assets/texture/*` + `graphics/scene/resources/gpu_texture/*` |
| `render_env_prefilter_cpu_gpu_match_16` | 16³ 源、全 mip CPU 参考(同 FIS/同序列)与 compute 输出逐 texel 容差对拍 | `ibl_prefilter.rs` |
| `render_env_prefilter_constant_env_is_identity` | 常色环境预滤波后所有 mip 仍为常色(权重归一正确性) | `ibl_prefilter.rs` |
| `render_env_sh9_matches_cpu_reference` | compute SH9 与 CPU 立体角参考逐系数容差对拍;常色→仅 band0 | `ibl_prefilter.rs` |
| `render_env_brdf_lut_corner_values` | (NoV→1, r→0) A→1,B→0;(任意, r→1) A+B < 1(能量守恒上界) | `env_brdf_lut.rs` |
| `render_env_mip_from_roughness_roundtrip` | 已在计划 11 清单,常数 1.0/1.2 双端一致 | 计划 11 |
| `runtime_environment_source_cubemap_pmrem_rough_mips_reduce_cube_seam_energy` | §6 判据 6；逐 face/edge 比较 PMREM 边界亮度，并约束中等与粗糙 mip 的 mean/max seam energy | `zircon_runtime/tests/runtime_environment_source_cubemap_contract.rs` |
| `render_product_environment_pbr_matrix_quantitative` | §6 判据 1–5 | `zircon_runtime/tests/runtime_shader_pbr_hdri_export/pbr_matrix.rs` + `pbr_matrix_quantitative.rs` |
| `render_env_derived_cache_second_launch_zero_dispatch` | bake key 命中来源 1/2 时环境 compute dispatch 计数 = 0;算法版本号变更后失效重烘 | `gpu_environment_map.rs` |
| `render_env_pmrem_artifact_roundtrip` | PMREM+SH9(+IEM) 持久化 → 重载逐字节等值;`IblBakeKey`/版本号不匹配拒绝命中 | `gpu_environment_map.rs` |
| `render_env_external_prefiltered_container_is_source_only` | 含 mip DDS/KTX 导入后 PMREM 仍由引擎生成(源 mip 只作降采样链) | 计划 13 importer 用例 |
| `render_env_iem_matches_sh9_low_frequency` | 球面 64 方向 IEM 采样与 SH9 求值低频差异 < 容差 | `ibl_prefilter.rs` |

命令沿计划 11:`.\.codex\skills\zircon-dev\scripts\validate-matrix.ps1 -Package zircon_runtime -SkipBuild -LibTests -TestFilter environment`、`.\.codex\skills\zircon-dev\scripts\validate-matrix.ps1 -Package zircon_runtime -SkipBuild -LibTests -TestFilter render_product_environment`。

## 8. 跨计划登记

- **计划 11**(实施权威):本计划 §4 为其 EL-M1 切片 2 的算法定稿来源;新增差异需在其状态表登记——skybox 显示采样源 cubemap(非 128³ 链)、BRDF LUT 节点与 binding、实时捕获时间切片(EC-M4)、16×8 sample table 删除清单、readback 回写由"仅 SH"扩为 PMREM+SH9+IEM 全产物(§4.7)、IEM 新增 `zr_environment_irradiance_cube` 一槽(§4.8)。
- **计划 13**(资产权威):`.zcube` 需满足 RGBA16F、face-major、全 mip、equirect/六面/十字三形态导入(TX-M3 原有范围),源面尺寸上限 1024;SH 与预滤波产物不进 `.zcube` 本体;derived artifact(PMREM/SH9/IEM)按 §4.7 作为资产伴生产物由 importer/staged build 产出;外部含 mip 容器(cmft DDS/KTX)只作源消费,`.zcube` 无 `prefiltered` 声明位。
- **计划 08**:当前无变体键增维；`zr_environment.wgsl` 仍经既有 include 注册表分发，MVP 固定使用 BRDF LUT，低端近似仅保留未接线函数。未来若启用，只能由计划 08 建立引擎全局质量维度并同时验证 PSO 预热/启动成本，不新增材质局部 option。

## 9. 状态与产出记录

> 请将产出记录放置在子计划中，此处仅展示当前现状的概述

- 迁入记录：[`06/2026-08-01-validation-and-performance-handoffs.md`](06/2026-08-01-validation-and-performance-handoffs.md)（52 条原始验证/性能记录）
- 当前状态：M5/M6/M7 均保持 `in_progress`；2026-08-03 的二次代码审查已收紧 WGSL 注释剥离后的源码契约与 EnvironmentOnly PBR 调用点断言，并使零贡献 Clearcoat 环境分支在 EnvBRDF LUT 前退出；GPU local-probe selector 也会在空间权重前排除非正强度条目，保留天空回退并避免无效 cubemap 路径。该审查随后发现 disabled-probe 测试只要求区间内存在任意 `continue`，现已收紧为 gate 分支体必须直接退出；修复复核结果为 `Critical 0 / Important 0 / Minor 0`。首个 procedural realtime IBL frame 测试也不再在 offscreen backend 不可用时静默成功，必须真实初始化 GPU 并启动 full batch；该测试加固的独立复审结果为 `Critical 0 / Important 0 / Minor 0`。历史 PNG/RDC、source-raced/compile-blocked run 与静态审查不替代 fresh managed Rust/WGSL、量化导出、DX12 timing/screenshot 和 RenderDoc replay。
 - 2026-08-29 P0-8/C0 非验收基础设施已实现：`RenderFramework` 新增中立的非阻塞 environment-capture request/poll/cancel 默认能力，`RenderEnvironmentCaptureRequest` 固定 capture id、scene/environment revision、output generation、捕获变换、face size 与 prefilter quality，状态 DTO 收敛 queued/capturing/filtering/persisting 与 terminal phases。reflection-probes runtime plugin 仅负责 JSON 到中立合同的映射，editor trigger 仅提交/轮询/取消；旧 `SceneRenderer` 同步六面读回、CPU texel 聚合、cache-root 持久化和 editor 资产注册入口已删除，持久化消费保留为后续 C4 的 typed input。精确 `rustfmt --check`、scoped `git diff --check`、旧 API 清零和资源所有权静态合同通过；未运行 Cargo/Naga/WGPU、DX12 PNG、`D:\Tools\renderdoc`、GPU/RSS/功耗，故 C0 不计入 M5-M8 验收；C1 已在下一条记录实现。
 - 2026-08-29 P0-8/C1 控制面已实现为非验收基础设施：graphics runtime 新增固定上界 scheduler，最多保留 `1` 个 pending 场景快照、`1` 个 active work item 与 `64` 个 terminal status；同一完整 request 复用句柄，同 capture id 的更高 `output_generation` supersede 旧 pending/active 工作，取消、失败和终态淘汰都 fail-closed，不得发布 output identity。Framework request/poll/cancel 只访问 scheduler mutex，不 drain frame submission、不获取 renderer/state lock；telemetry 记录 accepted/duplicate/capacity/stale/superseded/cancel/success/failure/eviction。新增合同测试覆盖容量、代际、取消、终态上界、进度回退和锁边界，精确 `rustfmt --check`、scoped `git diff --check` 与源码静态检查通过。C1 尚未把 work item 接入 `SceneRenderer` 的 GPU command recording，故没有 GPU/WGPU、DX12 PNG、`D:\Tools\renderdoc`、RSS/功耗或性能收益数据；C2 仍负责复用现有 realtime-IBL 双缓冲/分帧 graph 做六视图 GPU capture。
- 2026-08-29 P0-8/C2 前置投影与 CPU 批次合同已实现为非验收基础设施：`CubemapFace::projection_axes` 直接复用既有 cmft/D3D 面序 `FACE_UVN`，只返回每个 cube layer 的纹理 `U/V/forward` 投影轴，不再把纹理投影轴伪装成 Zircon 的右手相机姿态。复核 `zr_math::Transform` 证明 Zircon 相机以 `-Z` 为 forward，`looking_at` 按 `forward x up` 构造 screen-right；当 image-up 取 `-V` 时 screen-right 必为 `-U`，因此普通六次 `looking_at` 会把 cubemap 水平镜像。新增 `cubemap_capture_view_from_world` 与 request-scoped `cubemap_capture_camera`：前者从唯一轴 owner 构造 determinant=`-1` 的反射感知 view matrix，后者以固定 90°/1:1/HDR/无抖动相机包装请求 near/far/face size；两者均以 `reverses_winding=true` 强制调用方选择反向 front-face。材质 draw construction 新增 capture view-winding 维度，与既有 negative-determinant model flag 做 XOR 后写入 `PipelineKey::reverse_raster_winding`，不扩张 `ShaderVariantKey`。renderer 内部 `EnvironmentCaptureSceneBatch` 进一步把 `SceneViewportRenderPacket` 一次 move 成单一 `RenderFrameExtract`，清除 editor overlay/virtual-geometry debug sideband，并在同一 `ViewportRenderFrame`/`Arc` 上依次切换六面 camera；后续 recorder 的结构目标固定为一次资源准备、一次 mesh draw 构建、六个 layer pass 和一次 queue submission，CPU 规模为 `O(6 + M)` 而不是六次场景深拷贝或 `O(6M)` draw 构建。现有 realtime IBL 双槽 source texture 固定为 128-face 且缺少 `RENDER_ATTACHMENT`，不能承载任意 request；新增独立 `EnvironmentCaptureGpuTargetPlan` 按 face size 量化完整 RGBA16F 六面 source mip 链和一个顺序复用 Depth32 attachment（128 face=`1,114,096 B`，1024 face=`71,303,152 B`），并提供 sampled Cube、逐 mip D2Array storage、逐面 D2 color view，避免 capture 别名到已发布 last-good IBL。六面 pass 也不能连续覆写现有唯一 scene uniform buffer，否则一次 submission 中所有 pass 都会读取最后一面；新增 `EnvironmentCaptureSceneUniformPlan/Workspace` 以一份连续 CPU payload 派生六条 upload range，并持有六个稳定 uniform buffer/bind group，共享既有环境贴图与 layout，不采用 dynamic offset、不增加 shader permutation，GPU uniform 容量严格为 `6 * sizeof(SceneUniform)`。`build_environment_capture_mesh_draws` 专用入口现在显式只开启 opaque、alpha-mask 与 advanced-PBR-opaque，关闭 base-transparency、transmission、shadow、velocity 与 OIT；这与 Unreal `ReflectionEnvironmentCapture.cpp` 的 runtime translucency 默认关闭策略一致，也避免把依赖每面深度排序的透明 draw 错误地复用到六层。未来启用透明需为 `T` 个透明 draw 做每面排序，至少新增 `O(6T)` 排序/构建成本并单独计入预算。纯合同测试锁定六面中心、轴正交、单位长度、`U x V = -forward`、原点平移、U/V 到 clip XY 的映射、投影矩阵等价、winding XOR、反射实例、单 extract identity、无 temporal previous-camera/UI/overlay、六面 uniform range 连续且互不重叠、capture feature set 的不透明/透明边界；C2 不得复用未修正的点阴影 face camera。该切片仍未录制 GPU command、未推进 scheduler accepted submission，也未运行 Cargo/Naga/WGPU、DX12 PNG、`D:\Tools\renderdoc`、RSS/功耗或性能测量；C2 的六层 GPU 记录与现有 IBL graph 接线继续实施中，M5-M8 状态不变。
- 2026-08-29 C2 scheduler-to-recorder ownership boundary 已补齐：`EnvironmentCaptureWorkItem::into_parts` consuming API 原子移交 handle、scene snapshot 与 request，`EnvironmentCaptureSceneBatch::from_work_item` 在 scheduler mutex 释放后建立单一 moved extract；recorder 不再需要借用 queued item 或在资源准备期间持有控制面锁。该切片当时的 `SceneUniform` ABI 为 `480 B`、六面连续 payload 为 `2,880 B`；2026-08-31 P1-18 新增 lightmapped ambient lane 后，当前合同已同步为 `496 B` / `2,976 B`。该 slice 仍不宣称 GPU command recording 或 accepted submission 已实现。
- 2026-08-29 C2 framework consumer boundary 已补齐：`WgpuRenderFramework::begin_environment_capture_work_item` 只访问 `environment_captures` mutex 并返回 consuming work item，不 drain frame submission、不获取 operation/state lock；后续 renderer recorder 可在锁外完成 target/uniform/draw 准备，再由 accepted submission 驱动 scheduler progress。该 slice 仍不宣称 GPU command recording 或 accepted submission 已实现。
- 2026-08-23 realtime IBL P1-2（M4 性能前向）已完成源码级绑定生命周期收口：双槽 GPU 资源保持常驻，PMREM slice/SH9 的 immutable params buffer、pipeline 与 bind group 以物理槽和 command slice 缓存；三代产品 ticket 的第三代回到已填充槽，要求十个 PMREM 加一个 SH9 命中、零 params/bind-group 新建和零 cache reset。缓存只复用尺寸/拓扑参数，天空 revision 仍驱动 source cubemap 重写和完整 PMREM/SH9 重烘。其性能设计、profile、当前产品 evidence 和验收门见 [`06/2026-08-22-realtime-sh9-dispatch-parity-performance-plan.md`](06/2026-08-22-realtime-sh9-dispatch-parity-performance-plan.md)。此前一次 Windows 托管 `--locked` 产品导出曾取得三代共 63 个单 pass/dispatch GPU timestamp，第三代 B 槽有 11 次模板命中、零 params/bind-group 新建、零 cache reset；非 RenderDoc 截图与 sidecar 已归档在 `docs/tests/runtime/shader`。该结果只保留为历史拓扑/暖槽证据，不能表述为当前源码通过：当前 `zr_rhi_wgpu` 编译门禁会在产品夹具前终止。P1-2、M4 及关联 M5/M6/M7/M8 仍保持 `in_progress`，因为 focused recorder regression、当前源码的 RenderDoc final-SH9 capture/replay 和 Frameworks01 RHI/WGPU 编译修复尚未完成；历史 realtime PNG/timing/RDC 不得代替这些门禁。只有这些门禁通过后，才能比较重复稳定 warm run 的 CPU construction，且不得从现有同步读回样本推断总帧时或功耗。
- 2026-08-24 PBR fallback 路径复核发现 glTF 合法 `roughnessFactor=0` 会被 `fallback_mesh.wgsl` 重新解释成 `1.0`，与主材质模板及 GPU material uploader 的默认值语义不一致。协调器补丁 `#143` 已将该路径统一为 `clamp(factor * texture.g, 0.001, 1.0)`，保留缺失贴图/默认材质的 `1.0`，并新增静态回归以禁止重引入该分支。当前 Windows 托管产品导出已不再受此前 Asset Registry E0599 阻塞；不过现有 procedural 产品场景没有携带 `roughnessFactor=0` 的外部 glTF 输入，故仍需该专项运行时 WGSL/产品截图验证，且不将现有静态证据计入 M4/M5 图形验收。
- 2026-08-24 PBR 输入语义链已补齐可执行源码约束：最小 glTF `roughnessFactor=0` 被导入为 `MaterialAsset/StandardMaterialDescriptor` 的零值，GPU uniform 将其裁剪到 `STANDARD_MATERIAL_MIN_ROUGHNESS`，主材质模板和 fallback mesh 均在乘以 packed texture 的 G 通道后应用相同下限。内建 ingest 与 stable `gltf_importer` 均有内存 glTF 回归，锁定 `metallic=1.0`、`roughness=0.0` 到材质描述符的投影，防止未来默认值修正使两条导入链重新分叉。该覆盖仍是源码级契约，尚未形成外部 glTF 场景到像素的受管产品证据。
- 2026-08-24 PBR 输入链复审还发现 stable `gltf_importer` 将材质 shader 依赖写为历史单文件 URI `res://shaders/default_pbr.zshader`，而内建 ingest、`ProjectManager` 的 compound-source resolver、最小项目夹具和 `examples/vampire` 都以 `res://shaders/default_pbr` 作为 `default_pbr.zmeta` 的逻辑资产根。解析器不会把单文件 URI 自动折叠为 compound 根，故插件产出的显式及默认 glTF 材质可能无法解析标准 PBR shader 包。现在由 `asset::assets` material API（source owner `asset/assets/material/default_pbr.rs`）唯一拥有 compound 根与已解析 `AssetReference`；内建 ingest 与 stable 插件均调用该访问器，并通过 `OnceLock` 避免在批量导入中反复解析稳定 URI。含 N 个显式材质的单个 glTF 文档此前会构造 N+1 次默认 URI 解析，现在进程首次只解析一次并仍为每个材质复制 value reference；这是可证明的构造次数收敛，不宣称耗时、功耗或 GPU 提升。此修复只恢复依赖可解析性，不改变环境 IBL 算法或截图、RDC 结果；仍需与外部 glTF 零粗糙度产品夹具一起完成受管 Rust/WGPU、PNG 与 RenderDoc 验证，M4/M5/M6/M7/M8 保持 `in_progress`。
- 2026-08-24 通用环境 WGSL 的 `zr_environment_env_brdf_approx` 已与 Unreal `EnvBRDFApprox` 和运行时 LUT 路径统一为 `F90 = saturate(50 * F0.g)`，不再保留固定 `F90=1` 的后备公式；其 include 内容哈希仍参与变体 cache key，故修改不会复用同一 key 下的旧通用环境源码。该 helper 当前不在主 Standard-PBR LUT 路径上，修复只消除未来 custom/fallback consumer 的语义分叉，不宣称既有截图或性能变化；依据与未验收门见 [`06/2026-08-22-realtime-sh9-dispatch-parity-performance-plan.md`](06/2026-08-22-realtime-sh9-dispatch-parity-performance-plan.md)。
- 2026-08-24 已新增独立的 ignored WGPU 产品夹具 `export_realtime_ibl_external_gltf_zero_roughness_mirror_png`：它在 `docs/tests/runtime/shader/.work` 内生成外部 glTF 材质、项目级 `default_pbr` alias 和镜面 sphere 场景；导入后先断言同一 `MaterialAsset` 保持 `metallic=1.0`、`roughness=0.0`，再完成完整 21-slice realtime IBL ticket。截图前必须取得 21 条 GPU timestamp，并断言一次 terminal SH9 发布、三次 CaptureSky 与七次 source-mip 动态绑定归因；方向性 procedural sky 的零粗糙度球还必须保留球体内的局部太阳高亮而非大面积过曝。该专项阈值由同场景历史基线（最亮 RGB=765、阈值 700 以上 63 像素）校准，但不作为当前源码验收。以 `profiling` feature 且无预存 core capture 运行时，夹具还会写入独立 CPU command-recording sidecar；外部 capture 不会被 drain、停止或重置，feature-disabled 路径则真实 drain 并断言为空。每次运行先清理同名 CPU sidecar，避免将历史 CPU 数据误作本次证据。PNG、逐分片 GPU timestamp 与 CPU sidecar 均输出到 `docs/tests/runtime/shader`，并可通过 `ZR_RENDERDOC_CAPTURE_GLTF_ZERO_ROUGHNESS_FINAL_SH9=1` 捕获 terminal SH9；RDC 文件路径仍须在受管运行启动前由 RenderDoc/捕获器显式指定为该证据目录。该夹具尚未运行，尚无 PNG、TXT、RDC 或性能数据，不能计入 M4/M5 验收。
- 2026-08-24 的历史 v13 启动归因曾以 `payload_build` 记录进程级 CPU integration/RG16F encode，并将其与包含该区间的 `cache_wait` 作为不可相加的并列归因候选；upload enqueue 也不代表 GPU completion。2026-08-31 该运行时积分路径和字段已被 §10.18 的 versioned-builtin/v17 hard cut 取代，v16 仅保留显式 legacy 读取，新的 materialization 数据不得与历史 build 数据混算。当前仍无 fresh managed Windows/WPR、PNG 或 RenderDoc 数据，故不调整 LUT 分辨率/采样算法，不宣称产品性能、功耗或视觉改善，M8 继续为 `in_progress`；完整测量方案见 [`06/2026-08-09-m8-canonical-ibl-recipe-and-performance-plan.md`](06/2026-08-09-m8-canonical-ibl-recipe-and-performance-plan.md)。
- 2026-08-25 PMREM roughness-LOD 坐标复审澄清：scene uniform 的 W 分量是 mip count，WGSL 先转换为最后合法索引 `mip_count - 1`。Unreal `SceneRendering.cpp` 也将 `ReflectionCubemapMaxMip` 设为 `FloorLog2(capture_size)`，故 `ReflectionEnvironmentShared.ush` 的 `maxMip - 1 - (1.0 - 1.2*log2(r))` 中第二个 `-1` 必须保留。8 mip 链的 `roughness=1` 正确取 mip 5；这是该 lookup 的最高目标 mip，mip 6/7 不会被该 PMREM lookup 选中。CPU recipe、GPU bake 参数和采样 WGSL 的 v7 正反映射因此正确，不建立 v8 cache identity 或重烘要求。源码锚点锁定 `r=1 -> mip 5`、`mip 4 < 1`、`mip 5 = 1`；Rust/WGPU、Windows cold/WPR、GPU timestamp、PNG 与 RenderDoc 仍待受管运行，M8 保持 `in_progress`。
- 2026-08-24 PBR 材质链复审发现 glTF `normalTexture.scale` 在内建 ingest 和 stable `gltf_importer` 均被读取后丢弃，Standard-PBR、fallback mesh 与 Hybrid GI card capture 因而固定按 `1.0` 解释合法法线贴图。现在该值作为材质自有 `normal_scale`（缺失时 serde/资产默认 `1.0`）贯穿两个导入器、`MaterialAsset`、`StandardMaterialDescriptor`、运行时 capture DTO 和 Hybrid GI；两条 GPU 路径统一为“先解码法线，缩放切线空间 XY，再归一化”，避免 fallback 在缩放后重算 Z 而与 Standard-PBR 分叉。标准 material uniform 追加 `data12.x`，从 192B 增至 208B；未接入 shader 变体的 bindless payload 也同步从 12 行/224B 到 13 行/240B，以保持其标准 uniform 镜像可编译。没有增加 binding、贴图槽或 permutation；上述各自 +16B 是静态 ABI 容量，不是带宽、帧时间、功耗或视觉效果测量。内存 glTF、uniform 打包和 WGSL/Hybrid GI 回归已经加入源码树，但外部 glTF 法线 scale 的受管 Rust/WGPU、PNG 和 RenderDoc 验收尚未执行，M5/M6/M7/M8 继续为 `in_progress`。
- 2026-08-24 `KHR_texture_transform.rotation` 也已从两个 glTF 导入器连续投影到 `MaterialAsset`、`StandardMaterialDescriptor` 与运行时材质；此前该合法字段被静默丢弃。遵循 Unreal `InterchangeGLTFMaterial.cpp` 的逐槽策略，CPU uploader 将每个槽的 rotation 预计算为 `(cos, sin)`，Standard-PBR 模板和 fallback mesh 统一执行 `offset + rotate(uv * scale)`，避免片元三角函数且禁止两条渲染路径对同一材质产生不同 UV。五个标准槽的十个旋转标量占用 `data13..data15`；共享 group-2 binding 下限随之从 208B 增至 256B，因此 Standard-PBR 与 custom property payload 都会满足 256B（后者只零填充，不获得新语义），bindless 镜像从 13 行/240B 增至 16 行/288B。未增加 binding、贴图槽或 permutation。两个内存 glTF fixture、rotation sin/cos、uniform/bindless 打包、模板/fallback source guard 及 custom-payload 下限回归均已纳入源码；格式、差异完整性和静态 ABI 检查通过。该 +48B/+48B 只是静态 ABI 容量（共享 uniform 下限 +23.1%，bindless payload +20.0%），不是带宽、帧时间、功耗或视觉效果测量；外部 glTF rotation 的受管 Rust/WGPU、PNG 与 RenderDoc 验收仍待当前源码产物，M5/M6/M7/M8 保持 `in_progress`。
- 2026-08-24 PBR 输入色彩空间复审确认：两条 glTF 导入器此前均以 `TextureAsset::new_rgba8` 的默认 sRGB 描述符输出全部纹理，因而 normal、metallic-roughness 与 occlusion 也会以 sRGB 采样，破坏数据贴图及最终 IBL 反射语义。现在先按五个已投影 Standard-PBR 槽统计每个 glTF texture 的 transfer usage：base color/emissive 为 sRGB，normal/metallic-roughness/occlusion 为 linear；只含一种 transfer 的纹理继续输出既有 `TextureN` 标签且不复制载荷。若同一合法 texture 同时被颜色槽和数据槽使用，则显式输出 `TextureN/Srgb` 与 `TextureN/Linear`，材质槽路由到匹配子资源，避免单一视图静默污染另一个槽。冲突输入会产生两份 RGBA8 CPU 载荷与两次后续资源入队；这是当前 asset-id/GPU-view 模型下的正确性优先后果，尚未把它宣称为内存、带宽、帧时间或功耗数据。两个导入器的混用 glTF fixture 已断言五槽引用与描述符 transfer，且 scoped rustfmt、diff 完整性和静态路由检查通过；受管 Rust/WGPU、外部 glTF 像素截图、RenderDoc 与当前机器性能/功耗剖析仍未执行，M5/M6/M7/M8 保持 `in_progress`。
- 2026-08-24 同次复审还确认现有 `RenderSamplerDescriptor` 默认 clamp-to-edge，而 glTF texture 的 `wrapS/wrapT` 默认为 repeat；此前两条导入器完全忽略显式 glTF sampler，UV tiling、边缘和 minification 结果会偏离资产。现在 texture 子资源从 `gltf::Texture::sampler()` 投影 `wrapS/wrapT` 到 U/V，保留 glTF 2D 的未用 W=repeat，并完整映射 `magFilter` 与六种 `minFilter` 到现有 min/mipmap 二段 filter；缺失 filter 沿用引擎原有 linear 默认。该投影不增加 texture binding、sampler binding 或 shader permutation，sampler cache 已将这六个描述符字段纳入 key，因此不同 glTF sampler 不会错误复用同一 WGPU sampler。当前内嵌 glTF `TextureAsset` 仍为单 mip RGBA8，故 `NEAREST/LINEAR` 的“仅 base mip”与未来多 mip LOD clamp 需要由 Runtime92 的 sampler LOD contract 统一解决，不能在此冒充已经支持。混用 transfer fixture 同时锁定 MIRRORED_REPEAT、CLAMP_TO_EDGE、nearest mag、nearest+linear-mip 的 descriptor 值；受管 Rust/WGPU、外部 glTF 截图/RenderDoc 和性能数据尚未运行，M5/M6/M7/M8 继续为 `in_progress`。
- 2026-08-24 对 embedded/external glTF texture 的 mip/cook 路径复审确认，`texture_importer` 已有离线 mip、linear/sRGB kernel、normal 重归一化和 BC5 transcode，但它是插件 owner；内建 `asset::importer` 不能依赖插件，stable glTF plugin 也不能成为内建路径的共享服务。当前 glTF 子资源仍直接输出一层 RGBA8，不能仅修改 `mip_policy` 假装已经生成正确 mip 链。将这类处理复制到两个 glTF importer 会形成第三套 cook authority，故正确的后续是按 [`Runtime09D P1-6`](../../optimize/zircon_runtime/09d-render-asset-streaming-residency-review.md) 与 Runtime92 提取 runtime-owned `TextureBuildService`，把 decoded pixels 或保留的 encoded container payload、source locator、sampler、transfer/normal semantic 和 material slot 提交到唯一 cook 链；后者是 `KHR_texture_basisu`/KTX2 进入预算化平台转码的前提，不能由 importer 伪装为 RGBA8。该结构性缺口不阻塞本次已完成的 transfer/sampler MVP 修复，也不作为性能优化或验收通过；M5/M6/M7/M8 保持 `in_progress`。
- 2026-08-24 shared-image import ownership 复审：stable `gltf_importer.gltf` 曾借用整个 `images` 列表，使每个 texture index 都从同一 decoded image 重新复制 RGBA8，并在所有 texture subasset 输出后继续保留原始 image。现与内建 ingest 对齐：先统计每个 source 的 texture-index 引用数，最后一次使用移动 `GltfImageData`；RGBA8 fast path 直接移动 `image.pixels`，非 RGBA 格式维持原有验证与转换。插件改为消费 workspace 唯一的 `gltf` 依赖及其 `allow_empty_texture` feature 契约，因此 `EXT_texture_webp.source` 在没有 core `source` 时可合法解析，真正无 source 才返回 typed parse error，且不会再与内建路径发生版本或 feature 漂移。两个 texture index 共享一个 image 和 WebP-only source 的回归均锁定输出。对一个共享 RGBA8 source，已输出 K 份 `TextureAsset` payload 时，旧路径保留 `(K + 1) * S` decoded RGBA8 字节，新路径只保留不可省的 `K * S` 输出字节：静态减少一份 `S` source buffer 和一次深复制；若同一 texture 同时要求 sRGB/linear，K 的两份输出仍是语义必需，不能被该优化删除。这是源码级内存所有权计数，不是 RSS、上传带宽、帧时间、GPU 时间或功耗数据；受管 Rust/WGPU 与性能剖析仍未执行，M5/M6/M7/M8 继续为 `in_progress`。
- 2026-08-24 transfer-to-GPU 定向复审：`TextureAsset::with_descriptor` 的 normalizer 会将 linear RGBA8 从默认 `rgba8unorm_srgb` 改为 `rgba8unorm`；`TextureUploadPlan` 保留该 format，`GpuTextureResource::from_rgba8_asset` 分别创建 `Rgba8Unorm` 与 `Rgba8UnormSrgb`，普通材质绑定和 bindless slab 都直接消费同一 resource view。因此本次 five-slot glTF transfer 修复实际决定硬件 sRGB decode，而非孤立 metadata。两个导入器的混用 fixture 现同时断言 descriptor color space 和规范化 format；该结论来自静态路径复核，未替代 WGPU device、外部 glTF PNG、RenderDoc 或帧时/功耗验证，M5/M6/M7/M8 保持 `in_progress`。
- 2026-08-24 `KHR_texture_basisu` 当前仍未支持：stable plugin 在 `gltf::import` 前、内建 ingest 在 image decode 前一致拒绝该 extension 的 texture。可选 texture 返回 `KTX2/BasisU transcoder is required` typed error；内建 ingest 的 `extensionsRequired` 继续由 required-extension policy 返回 typed unsupported。该行为阻止可选 extension 静默丢失或把 KTX2 伪装为 RGBA8；它不保留 encoded payload、不会转码，也不构成 Runtime09D P1-6 或 M5/M6/M7/M8 验收完成。
- 2026-08-24 stable `gltf_importer` 与内建 ingest 的 material-extension 投影已收束：`KHR_materials_unlit`、`ior`、`transmissionFactor`、volume 的 thickness/attenuation、`emissiveStrength` 现在都进入既有 `MaterialAsset` property owner，因而生成相同的 Standard-PBR descriptor 和既有 GPU material fields；`KHR_materials_specular` 仍没有 factor/color owner，两个路径均保留明确 diagnostic，不静默伪装为已支持。JSON 验证/投影唯一归 `runtime asset::importer::project_gltf_material_extensions` 所有，内建 ingest 与 stable plugin 都直连该 owner；这删除了两条导入链各自复制 extension policy 的漂移源，也避免继续扩张超过千行的插件导入编排文件。内存 glTF 回归同时锁定支持字段和降级诊断。没有新 uniform、binding、texture slot 或 shader permutation，也没有 material-extension 外部 glTF、Rust/WGPU、PNG、RenderDoc 或性能/功耗数据，M5/M6/M7/M8 继续为 `in_progress`。
- 2026-08-24 `KHR_materials_ior` 的普通 `IOR >= 1` opaque PBR 实现已完成源码暂存：CPU 首版在既有 256 B material uniform 的 `data12.yzw` 预计算三通道 dielectric F0；2026-08-28 clearcoat ABI 收敛后改为 `data12.y` 单标量并由 WGSL splat，Standard-PBR surface、fallback 和 direct/IBL 路径继续统一使用该值。non-default IOR 进入既有 late forward opaque route，default 1.5 IOR 保持 deferred。route-only key 不进入 `ShaderFeatureBits`，并在 mesh PSO identity 前被剥离，因而不会增加 shader permutation 或重复 PSO；它也不请求 scene-color copy。当时新的 WGSL common owner 还使 direct、environment、deferred、fallback 和 baked diffuse 使用 Fresnel remaining energy；该部分已由 2026-08-26 的完整 Unreal/Bevy 复审否决并被本计划末尾的 source-independent metallic decomposition 覆盖，F0 owner 与 IOR 路由继续保留。`KHR_materials_specular` 因完整 factor/color texture owner 尚缺仍保持 rejected。Khronos 的 `IOR = 0` specular-glossiness compatibility mode 需要有效无穷 IOR 和透射路径共同语义；当前 glTF 投影明确拒绝该零值，防御性 normalizer 将其他小于 `1` 的有限值映射为 `1`，因此不得从本条推断完整 `KHR_materials_ior` conformance。此项是 source/static 阶段，受管 Windows Rust/WGPU、外部 glTF IOR PNG、GPU timestamp、RenderDoc 与 WPR/能耗数据尚未执行；M5/M6/M7/M8 继续为 `in_progress`，不得从本条推断视觉或性能验收。
- 2026-08-24 stable plugin 的 `extensionsRequired` preflight 也已按实际能力收束：仅允许 WebP、mesh quantization、已投影的 material extensions 与 texture transform；它明确拒绝没有完整 owner 的 `KHR_materials_specular`，也不会把 stable plugin 未实现 decode 的 `EXT_meshopt_compression` 伪列为支持。preflight document 同时供 external-buffer 和 BasisU 检查复用，消除之前这两项之间的一次重复 glTF parse；真实 `gltf::import`/buffer/image decode 仍是唯一导入执行路径。该项是 source-level 失败语义和无效预检工作的收束，不是已测导入耗时、RSS、GPU、功耗或截图证据，M5/M6/M7/M8 状态不变。
- 2026-08-24 `KHR_materials_specular` 的完整 owner/ABI/PSO 复审已记录在 `docs/plans/optimize/zircon_runtime/09c-material-shader-pipeline-pso-review.md` §12.8；2026-08-28 clearcoat packing 使其旧 `data12.yzw` 容量假设与 304 B/336 B 数字失效，ABI design 已重新开放。按当前仅余 1 个 reserve scalar 的布局，若把 CPU 合并的 RGB dielectric F0 与两张 texture 的 14 个 metadata scalar 全部内联，最低 vec4 对齐会把 uniform 扩至 320 B（+25%）、bindless row 扩至 352 B（+22.22%），因此仍禁止直接扩大默认行。feature-only auxiliary storage 只保留为待评审方向，必须重做 constant-only、per-material/bindless layout 与 profile；预检继续拒绝该 required extension，M5/M6/M7/M8 状态不变。
- 2026-08-24 glTF texture role ABI 收束完成、受管验证待执行：`asset::importer::gltf_texture_semantics` 现在唯一推导 material-slot 的 sRGB/linear、Normal/Data 语义与稳定子资源标签，内建 ingest 和 stable `gltf_importer` 只消费该 owner。若同一 linear texture 同时被 normal 与 metallic-roughness/occlusion 使用，导入器明确生成 `TextureN/Normal` 与 `TextureN/Data`，材质 slot 及 dependency 都路由到角色匹配的 URI；二者分别通过 `decoded_rgba8_for_import_usage` 取得既有 normal convention、color-space 和 mip filter，但显式保持单层 `FromSource + Uncompressed`，不伪造 offline mip 或 BC 压缩产物。该条替代“仅一个 linear transfer 视图即可共享”的旧实现含义，但没有生成 mip 链、引入 BC5/KTX2 或实现 Runtime09D `TextureBuildService`；因而不主张性能、功耗、GPU 或像素验收，M5/M6/M7/M8 继续为 `in_progress`。
- 2026-08-25 skinned Standard-PBR 权重收束：内建与 stable glTF importer 都保留读取到的 `WEIGHTS_0` 原值；CPU skinning 与 fallback mesh 已先按有效 joint weight sum 归一化，但两个 generated Standard-PBR geometry source 过去只检测该 sum 是否为零，随后直接以原权重混合 current/previous joint matrix。于是同一非归一权重输入会在 GPU Standard-PBR 中改变位置、法线、切线和 IBL 反射，而 CPU/fallback 结果不同。两个 skinned WGSL source 现在仅在已有的非零 sum guard 后计算一次 reciprocal，并将四个有效权重乘到该 reciprocal；静态/非法 joint 过滤、零权重 identity fallback、joint palette ABI、shader feature、binding 和 PSO identity 均未改变。Unreal `GpuSkinVertexFactory.ush` 同样先构造单个 blend matrix 再生成 tangent frame；本修复保持该结构，同时不假设 Zircon importer 已验证输入权重归一。新增 assembled-template contract 覆盖 skinned 与 skinned-morphed source 的四分量归一化，source closure 12/12、scoped `rustfmt --check` 与 `git diff --check` 通过。该修复只给既有 GPU skinning vertex path 增加一次 reciprocal 和四次标量乘法，尚无 managed Rust/WGPU、DX12、GPU timestamp、功耗或外部 skinned glTF 截图数据，不能将其描述为性能结论，M5/M6/M7/M8 继续为 `in_progress`。
- fixed lifecycle：[rich inline provider export](06/fixed-2026-07-11-rich-inline-provider-export-name.md) · [Editor10 runtime layout method](06/fixed-2026-07-11-editor10-runtime-layout-method-name.md) · [deferred-lighting nested include](06/fixed-2026-07-15-deferred-lighting-nested-include-resolution.md) · [runtime operation phase matcher](06/fixed-2026-07-15-runtime-operation-phase-terminal-matcher.md)
- fixed cross-plan lifecycle：[EditorLayout15 realtime IBL recorder](../../zircon_editor/editor_layout/15/fixed-2026-07-12-realtime-ibl-graph-recorder-type-errors.md) · [Editor08 realtime IBL option](../../zircon_editor/editor/08/fixed-2026-07-12-realtime-ibl-option-then-type-errors.md) · [Editor03 external cubemap contract](../../zircon_editor/editor/03/fixed-2026-07-15-external-source-cubemap-contract-api-drift.md) · [Runtime02 core-filter fixture](../runtime/02/fixed-2026-07-12-core-filter-runtime-fixture-contracts.md)

## 10. 2026-08-25 PBR vertex normal transform: architecture and performance plan

### 10.1 Current-source finding and scope

This is a rendering-correctness prerequisite for the IBL/PBR matrix, not a claim
that PMREM or the viewer acceptance gate has passed. `zr_surface_types.wgsl` and
the fallback mesh shader transform object-space normals with `world_from_local`
as though every instance were rigid or uniformly scaled. That is incorrect for a
non-uniform scale or affine shear: a normal must use the inverse transpose of the
linear transform. It perturbs direct light, normal mapping, SH diffuse, reflection
direction, specular occlusion, and therefore every IBL product image. The legacy
built-in PBR source has the same defect and must not retain a divergent rule.

The current GPU scene ABI is `GpuInstanceData = 176 B`: two 4x4 matrices consume
128 B and the remaining 48 B is fully assigned. Group 3 already owns bindings
0--11. A blanket `mat3x3` normal matrix would raise every instance row to 224 B
(+48 B, +27.27% resident/upload traffic); a separate buffer would consume another
shared GPU-scene binding. Neither is acceptable as a default-path repair without
evidence. Conversely, computing a matrix inverse at every vertex creates a
per-vertex ALU cost for the common uniform case.

UE's `LocalVertexFactory.ush` supplies `InvNonUniformScale` and
`DeterminantSign`, strips scale before building `TangentToWorld`, and multiplies
the tangent sign by the instance determinant sign. Zircon cannot copy only the
inverse-scale part: its matrix extraction can represent parent non-uniform scale
and therefore shear (see the transform policy in Runtime23). The implementation
must remain correct for the general affine linear block.

### 10.2 Selected structure

1. Keep the 176 B instance layout and the 12-entry group-3 layout unchanged.
   The producer classifies the 3x3 linear block once per changed instance and
   writes three existing `flags` bits: general-normal-transform required,
   negative determinant, and a degenerate-transform safety state.
2. For an orthogonal equal-length basis, keep the existing normalized
   `world_from_local * normal` fast path. This includes normal rotation and
   uniform scale, so it adds no instance-buffer bytes or additional resource
   binding. A negative determinant still flips tangent handedness.
3. For non-uniform scale or shear, form the inverse-transpose direction from the
   adjugate columns (`cross(c1,c2)`, `cross(c2,c0)`, `cross(c0,c1)`) and normalize
   it. The reciprocal determinant is deliberately omitted because normalization
   cancels its magnitude; the determinant-sign bit restores the correct direction
   for mirrored transforms. Tangents continue to use the linear transform and
   receive the same handedness correction, after which the existing fragment
   Gram--Schmidt step rebuilds a coherent TBN basis.
4. A non-finite or near-singular affine block is explicitly marked degenerate and
   produces a zero normal rather than NaN/Inf. Rendering the position remains
   defined; a later transform-policy task owns rejection/diagnostics at authoring
   time. No normal-matrix data is hidden in the previous-transform field because
   velocity history is an independent contract.

### 10.3 Cost model and required measurements

The common path consumes zero extra GPU-scene bytes and performs only the
per-instance-uniform branch plus the normalization already present. The exceptional
path adds three cross products and their weighted sum per transformed normal; it
avoids storage fetches, matrix inversion, and all-instance bandwidth growth. This
is a design estimate, not a measured timing result.

Before marking this performance-safe, the managed Windows/DX12 run must capture
the identical mesh/material corpus twice (uniform versus non-uniform/sheared
instances) with RenderDoc at `D:\Tools\renderdoc`. The report must record: scene
instance count, submitted vertex count, GPU vertex-stage duration or equivalent
timestamp range, total frame GPU time, CPU GPU-scene upload bytes/ranges, and
pipeline statistics. It must compare a mixed-instancing scene as well as isolated
cases, state driver/device/clock conditions, report median and p95 across at least
three cold and three warm runs, and publish the raw capture identifiers. Until a
current managed receipt exists, this section intentionally reports no bottleneck,
power, or frame-time improvement.

### 10.4 Verification contract and status

The implementation begins with CPU math contracts for uniform, non-uniform,
sheared, mirrored, and degenerate matrices; they compare the fast/general result
with an inverse-transpose oracle where that oracle is finite. Shader source
contracts then require all material-template and fallback routes to call the same
normal/tangent helper, and reject the legacy direct normal transform. Product
acceptance adds a normal-map PBR sphere under non-uniform scale and a mirrored
instance to the existing HDR visual corpus, with normal/tangent and reflection
oracles plus screenshots in `docs/tests/runtime/shader`.

Status: structural research, source implementation, and static closure checks are
complete. The producer now caches the derived flags from the matching GPU-scene
shadow row, so unchanged affine transforms do not repeat the CPU classification;
the 176 B ABI and group-3 binding count remain unchanged. `rustfmt --check`,
source-closure, and scoped diff-integrity checks passed. Rust/WGPU execution,
current screenshots, RenderDoc capture, timing, and power measurements remain
pending. This does not advance M5--M8 or any existing screenshot, RenderDoc,
power, or timing acceptance claim.

### 10.5 2026-08-25 Realtime IBL Binding-Cache and CPU-Profile Audit

The PMREM/SH9 binding-template cache is valid only under its current lifetime
owner. `RealtimeIblRuntime::ensure_gpu_resources` creates
`RealtimeIblGpuResources` and `RealtimeIblWgpuRecorder` together once, while
the runtime never replaces resources independently. The cache key therefore
needs the request layout plus ping-pong work slot, not a synthetic texture-view
identity: every cached bind group still names the live resources for that
recorder. A future resource reset/recreation must recreate or clear the
recorder in the same transition; replacing resources alone would invalidate
cached bind groups and is prohibited by this owner contract.

CPU recording evidence is deliberately separate from GPU timestamps. A CPU
report is created only for an active profiling epoch, admitted only after the
matching submission succeeds, and drained through
`WgpuRenderFramework::take_realtime_ibl_cpu_timing_reports`, which first
finishes pending submission. It consequently remains available on adapters
without timestamp-query support. `RealtimeIblGpuTimingReport` remains
timestamp-readback-only so it cannot mislabel host recording time as GPU time.

The execution-resource cache now records one explicit hit-or-miss outcome,
the current entry count, and the configured topology capacity on every CPU
sample. Aggregated CPU evidence sums outcomes and retains peak entry/capacity
values, so a product capture can prove reuse without inferring it from zero
optional bindings. These are scalar control-plane observations from the
existing cache branch and `HashMap::len`; they do not create GPU resources,
change command encoding, or constitute a timing, energy, or power result. The
profile provenance closure includes both the cache and CPU timing owners.
The checked source closure is 18 viewer-owned modules plus 74 cross-module
files, for 92 unique files; the report-contract and GPU-sidecar exclusion
guards pass at the source level.

The present execution-resource map is safe only because its key is the closed
time-slice tuple `(ready slot, work slot, operation)`: the default scheduler
enumerates exactly 42 possible topologies, and the compiled-graph cache uses
that same capacity. The resource map currently carries a debug-only capacity
assertion rather than release-mode eviction. Do not add a dynamic key field or
claim general bounded-cache behavior on this basis. If the scheduler's closed
topology proof changes, first add a release-enforced eviction/lifetime policy
and a resource-destruction test, then profile its hit and miss behavior.

This source audit does not authorize a further binding-cache, `(mip, face)`
dispatch, or default PMREM-size change. Before any such change, collect the
declared current-source cold/warm matrix with the CPU drain, GPU timestamps when
available, worker utilization, visual equivalence, cache bytes, and an energy
counter with explicit units. No managed WGPU profiling, screenshot, RenderDoc
replay, or power sample was run for this audit; M8 remains
`implementation_complete_pending_managed_validation`.

### 10.6 2026-08-26 material publication camera-boundary review

`SceneRenderer::generation` advances for every camera submission and therefore cannot represent a logical-frame publication boundary. `ViewportCameraStackOutputPolicy` now marks the viewport start as well as the terminal owner, so a new submission first clears state left by an aborted camera loop. The staged material candidate then aggregates observed/deferred PSO admission across the viewport camera sequence with two booleans and commits only at the viewport-terminal camera, when at least one real draw referenced it and every observed requirement is Ready. An unreferenced candidate remains staged instead of bypassing admission through an empty requirement set, then leaves the active census until a later `ensure_material` cache hit reactivates it. Stable submissions still short-circuit on the empty active-candidate set; candidate work is `O(P + D + R)` per camera submission.

This source-level guarantee is limited to one viewport camera sequence. A later viewport can introduce an unseen graph, shader-quality, fog, or geometry context. Precompiling `4 qualities x 2 fog states x pass x geometry` without usage evidence was rejected as a compile-storm design; the remaining direction is context-qualified last-good selection plus usage/cook prewarm under the unified PipelineService. The retirement audit also found append-only variant/module/per-pass PSO stores; existing miss-report gauges are sufficient to quantify 300-frame growth, but safe deletion must wait for exact command-buffer variant usage to be attached to direct and compiled submission tickets. No managed Cargo/WGPU, screenshot, RenderDoc, timing, RSS, or power evidence was produced, so P0-4 and M5/M6/M7/M8 remain open at their existing statuses.

### 10.7 2026-08-30 environment rotation direction-domain review

The current direction chain is algebraically consistent without another shader change. Cubemap, PMREM, irradiance-cube, and SH9 consumers apply the same positive-angle Y rotation to the lookup direction, while the CPU scene-uniform owner applies the inverse rotation to the procedural sun. Their dot products are therefore identical; the Y-only sky/ground gradient is invariant under that rotation. Realtime capture intentionally bakes the unrotated resolved sun, then applies rotation at final PMREM/SH9 sampling, so changing only `rotation_radians` neither needs nor should trigger a rebake.

Forward Standard PBR, environment-only Forward, all three Deferred profiles, and fallback mesh assemble the same procedural-sky and environment-core owners. Source-cubemap skybox display uses the same lookup matrix, while procedural skybox display consumes the inverse-rotated scene sun. The zero-rotation flag returns before reading sine/cosine and the nonzero path uses CPU-precomputed values, so the review found no per-pixel trigonometry, duplicate normalization, extra texture access, dispatch, binding, PSO, or permutation cost. No sign reversal, double rotation, or renderer-path divergence was found.

The existing source contract already encodes both matrix signs, but it currently includes the old `environment/skybox.rs` path while a foreign hard-cut has deleted that file and materialized the `skybox/` directory. This session does not cross ownership to repair that in-flight refactor and does not report the unexecuted contract as passing. The review status is `review_complete_pending_foreign_hard_cut_and_managed_validation`; current-source `0/90/180` degree asymmetric-sun PNG and RenderDoc evidence remains an acceptance requirement.

### 10.8 2026-08-30 realtime IBL GPU lifetime and cache-bound review

Realtime IBL texture residency is fixed after lazy initialization: two slots each own one 128x128x6 RGBA16F source cube, one equal-layout PMREM cube, and a 144-byte SH9 buffer. Across all eight mips this is `4,194,528 B` of logical texel/buffer storage for both slots, excluding view and driver metadata; generations only exchange ready/work roles. The compiled-graph and execution-resource topology spaces are both bounded at 42 default entries. PMREM/SH9 binding state is bounded at 50 entries across two slots and produces zero new parameter buffers or bind groups after warming the complete topology.

The remaining P1-2 cost is isolated rather than guessed away: every 21-slice generation still creates three capture and seven source-downsample uniform/bind-group pairs. Existing reports already record their counts and creation microseconds separately, and the new generation provenance distinguishes warm stability from continuous input churn. A persistent binding/parameter redesign is not authorized until an E-drive current-source profile proves these 10 pairs per generation are a material CPU, native-allocation, memory-watermark, or energy bottleneck. This review changes no production code and leaves M8 `implementation_complete_pending_managed_validation`.

### 10.9 2026-08-30 reflection-probe assignment architecture review

The current camera-level preparation filters layers, missing cubemaps, non-positive intensity, and distance before admitting at most 64 probes, but every relevant fragment still ranks every active probe. The shader now reads each probe layer mask, correcting an older plan statement, yet it only repeats the camera mask: Forward object data and the Deferred GBuffer still expose no object reflection mask. Active clearcoat after a planar miss invokes the same position-only selector a second time, so the existing `pixels * active probes` workload metric describes one selector invocation and understates the clearcoat worst case by up to 2x. At 64 probes this is 265,420,800 candidate visits at 1080p and 1,061,683,200 at 4K; these are static bounds, not measured GPU time.

Unreal's reflection environment reads a culled capture offset/count from the light-grid cell before ranking captures. Zircon can reuse its light-grid sphere projection and view mapping, but not its fixed-budget bitmask storage or per-call allocation lifecycle: at 1k probes the existing word budgets would force roughly 128/256 px tiles at 1080p/4K and only 120 z bins. The M9 target remains a persistent 09B world index plus a versioned per-view sparse `offset/count + packed indices + spill` assignment. The dense group1 layout leaves only binding 19 free between the current probe/light/lightmap/volumetric/planar ranges, so the reviewed ABI expands the versioned probe header at binding 17 and packs assignment data into one storage buffer at binding 19. A common query carries fragment coordinates, world position, and a newly versioned object reflection mask; base and clearcoat reuse one selection while preserving exact sphere/box weight, priority, blend, top-two, box projection, and sky remainder.

No production algorithm was changed. Before M9 implementation, the E-drive managed profile must cover Forward/Deferred, clearcoat off/on, planar hit/miss, 0/1/8/32/64 active probes, 1/64/1k global probes, 1080p/4K, and sparse/dense overlap. Required evidence is selector invocations/candidate visits, GPU and CPU p50/p95/p99, assignment build/rebuild and allocation, upload bytes, RSS/VRAM, frame time, WPR/WPA energy, pixel equivalence, and `D:\Tools\renderdoc` binding/loop inspection. Status is `architecture_review_complete_pending_managed_profile`; M5/M6/M7 remain `in_progress` and M8 remains `implementation_complete_pending_managed_validation`.

### 10.10 2026-08-30 reflection-probe cold-residency and upload-admission review

The current probe upload uses eight mip writes per PMREM, each covering all six cubemap layers; the older 48-write description was incorrect. The larger fault is admission order. `prepare` synchronously calls `load_texture_asset`, which can perform cold artifact read/decode and always returns a cloned `TextureAsset`; upload preparation then copies the validated bytes again into a shared upload payload before the RHI has accepted the complete frame batch. One probe is `1,048,560 B`. Sixty-four cold textures are `67,107,840 B`; adding the probe/header/planar buffers produces a minimum `67,114,176 B` packet, already `5,312 B` beyond the default 64 MiB staging budget. Sixty-three probes leave only `1,043,344 B` for every other frame upload. The source-derived CPU payload high-water can reach `135,264,240 B` across resident assets, retained upload payloads, and one temporary cloned probe, excluding decode/native overhead; this is not measured RSS.

Admission rejection does not become automatic timeslicing. The graphics submission and probe commit are skipped, `discard_pending_uploads` clears only the pending vector, and the previous pending epoch becomes unavailable. The next frame therefore loads and schedules the same eight writes again; the existing uncommitted-upload retry test explicitly requires that behavior. A revised cubemap also overwrites the allocator's single ready revision before upload acceptance, so the current state model cannot preserve a distinct last-good generation while deferring the replacement.

The reviewed M4/P0-6 target is an asynchronous 09D residency ticket, immutable shared bulk payload, unified RHI byte/count admission before payload materialization, bounded cross-frame probe scheduling, and transactional `ready + pending` slot generations. Rejection, cancel, and supersede preserve last-good; probes without a ready generation fall back to sky and do not fail the frame. Unreal's update/load queue, render-command upload, memory-bounded cubemap array, and final-upload source release support this ownership shape without requiring Zircon to copy Unreal's upload primitive. No production source was changed. E-drive ETW/WPA plus RHI telemetry must measure 1/8/32/63/64 cold probes, warm reuse, revision replacement, low headroom, and failure injection before implementation; current status is `architecture_review_complete_pending_residency_owner_and_managed_profile`, and no latency, memory, GPU, or power improvement is claimed.

### 10.11 2026-08-30 environment extract and runtime-cache hydration boundary review

The first cache miss is still synchronous in `build_frame_submission_context_from_source`: `resolve_and_rehydrate_environment_ibl_cache` reads the runtime `.zribl` path, verifies and decodes its payload, reconstructs PMREM f32 texels, and rebuilds padded source/PMREM/irradiance upload rows before the submission context is returned. A pending bake reservation only suppresses duplicate graph compilation; it does not suppress the next frame's cache read, because `read_runtime_cache` runs before `begin_runtime_bake`. The hydration cache's capacity of four bounds entry count only. At the common 1024 source / 128 PMREM layout, one entry retains about `204,536,256 B` of source/PMREM f32 and padded upload rows (plus fixed coefficients), and four entries retain `818,145,024 B` (about 780.24 MiB) of logical CPU payload. This is a static payload model, not RSS or VRAM measurement.

The GPU environment owner also publishes replacement textures and views immediately after staging encode, while only the upload key remains pending until outer submission. `discard_pending_upload` clears the key but cannot restore the previously committed physical resources. A submission or later admission failure can therefore leave an uninitialized replacement as the active sampling object even though the committed key still names the old generation. M4 must converge this to one environment submission ticket that owns replacement textures, views, staging reservation and upload identity; publish only after accepted submission, keep last-good visible on failure, and retire old objects through the 09A fence owner.

The selected structure is a 09D asynchronous typed residency ticket and shared bulk owner, a three-stage cache probe/request/publish flow with single-flight pending requests, byte/priority/fence-bounded hydration storage, and a transactional `last_good + pending` environment resource generation. No shader or production renderer source changed in this review. E-drive ETW/WPA and RHI telemetry must cover warm hit, cold miss, stale/rejected cache, a 60-frame repeated pending miss, 1/2/4/5 distinct 1024 environments, submission failure, device generation change, and eviction/fence retirement; record read/decode/rehydrate micros, per-owner bytes, cache probes, frame prepare/submit p50/p95/p99, RSS/VRAM, first-visible latency, dropped frames, and WPR/WPA energy. The exact current-source physical-resource/bind-group rollback defect and hard-cut acceptance contract are handed to [`Render11 environment cubemap rebind last-good transaction`](../render/11/failure-2026-08-31-environment-cubemap-rebind-last-good-transaction.md). Status is `architecture_review_complete_render11_rebind_failure_open_pending_residency_owner_and_managed_profile`; no performance or power improvement is claimed.

### 10.12 2026-08-31 reflection-probe capture physical transaction review

The existing `CapturePending` metadata is not a physical rollback boundary:
capture copies overwrite the same cube-array slot before terminal settlement,
so a changed-revision failure cannot restore last-good texels. Unreal's runtime
refresh uses reserved array storage for old/new separation; Unity HDRP's in-place
atlas and Fyrox's per-probe target confirm why neither alternative directly
satisfies Zircon's shared-array rollback contract. The authorized MVP hard cut
is 64 logical probe entries over 65 physical PMREM slots. One free landing slot
is kept outside the ready LRU; capture copies there, terminal commit rotates it
into the entry and frees the old slot, and all failure paths return the reserved
slot without changing the old entry. The single
`Option<EnvironmentCaptureSubmission>` owner bounds concurrency to one such
slot; a new target at full logical capacity and a second concurrent reservation
fail closed. At 128-face RGBA16F/8 mips the fixed source-derived texture cost is
`1,048,560 B`, with no additional probe metadata or shader work. The hard cut is
implemented: capture ownership is outside the ready LRU, changed-revision
refresh keeps the old Ready slot visible, commit rotates the physical spare,
and cancel/ordinary pressure cannot replace last-good. Production-source
allocator regressions pass `12/12`; static descriptor/view, stale-state,
formatting, diff, and owner-size gates pass, with `resources.rs` reduced from
941 to 791 lines through capacity/selection/report owners. Managed
WGPU/DX12/RenderDoc failure injection and measured CPU/GPU/RSS/VRAM/power
evidence remain pending, so M5-M8 stay `in_progress`.

### 10.13 2026-08-31 capture terminal publication atomicity repair

源码复审确认 completion 曾先在 scheduler mutex 下发布 `Succeeded` 和可取
source payload，再另行获取 renderer state 提交 probe physical spare 与 resident
output；control-plane poll 因而可能观察到“成功但物理结果尚不存在”。简单倒序又会让
cancel/supersede 在两步之间插入并发布 stale output。当前已硬切为 scheduler-owned
publication gate：验证 handle/phase/progress/terminal intent/payload 后，在 scheduler
锁内执行一次 `Publish` 或 `Discard` callback；probe/residency commit 或 reservation
cancel 完成后才写 source payload 与 terminal status。锁序固定为
`environment_captures -> RenderFrameworkState`，request/poll/cancel 仍只访问 scheduler，
不存在 state-to-scheduler 反向路径。旧 bool success API 与终态后 publish 路径已删除。
transient source/depth 到 resident output 的 move/drop 在取得 scheduler 锁前完成，锁内只保留
常数级 probe commit/cancel 与 bounded residency map publication。
调度器同时拆出 `control_plane.rs`/`completion.rs`，加入 mailbox 回归后主文件从
1,135 行降到 892 行。
focused `rustfmt --check`、source-order 与旧 API absence contract 通过；E 盘最小
`rustc --test` harness 对真实 scheduler/control-plane/completion 模块报告 `16/16`，
receipt 位于 `E:/zircon-profiles/shader06-environment-capture-atomicity-20260831/`。
它同时锁定 runtime-only success 不会清除未消费的 persistence mailbox。该 harness
不等同于 crate compile ticket。managed Cargo、
WGPU/DX12、产品截图、RenderDoc、timing、RSS/VRAM 与功耗仍待协调器恢复后验证，
因此 M5-M8 和 realtime-SH9 Failure 不前移。

### 10.14 2026-08-31 capture scene-source and probe-origin LOD review

当前 capture 请求携带的是已由 viewport camera 选定 model/mesh/material/primitive
资源的 `SceneViewportRenderPacket`；六面 batch 只换 camera descriptor，因此 probe
可能错误复用主视口 LOD。`mesh_lod` 只是结果标记，清空它不能恢复已经丢失的 base/all-LOD
资源。Unreal 的 reflection capture 为 capture position 建真实 view，并让该 view 进入
visibility/LOD；Zircon 六面共享同一 origin，可在命令构建前每 primitive 只选一次 LOD，
无需六次全场景提取。

正确硬切依赖 Render03 已实现但尚未产品接线的持久 `RenderScene`：其
`RenderComponentChangeArtifact`/`RenderSceneMeshSource` 已以 immutable `Arc` 保存
base/all-LOD 绑定。renderer 接入该唯一 scene generation 后，capture work item 应持有
generation-qualified read view，在 probe origin 做一次 selection，构建一次命令并六面
replay；随后删除旧 viewport-packet capture overload。禁止把 all LOD 塞回
`RenderMeshSnapshot`、复制 `MeshRenderer` 或新增 capture-only cache。

目标 CPU 规模为 `O(M log L + K + 6)`，稳定 generation 的 source projection、all-LOD
payload clone、重复 residency admission 均为 0。E 盘验收需使用 viewport/probe 分离且
至少三档 LOD、各档不同 primitive/material 的场景，记录 visits/selections/bindings、
command builds、allocation bytes、CPU/GPU p50/p95/p99、RSS/VRAM 与 WPR/WPA，并由
RenderDoc 证明六面使用 probe-origin selection。当前仅完成架构审阅，无实现和性能结论，
状态为 `architecture_review_complete_blocked_on_render03_persistent_scene_wiring`。

### 10.15 2026-08-31 capture cold-procedural specular fallback repair

capture full-roughness policy 曾在 realtime IBL 首次发布前落入 raw procedural
reflected-direction fallback，导致 roughness=1 仍可能把清晰太阳镜面项烘进 probe。唯一
environment core 现保持 source/realtime PMREM 与普通 viewport fallback 不变，只在
capture policy 已启用且无 PMREM 时把未卷积 specular fail-closed 为 0；diffuse sky、
ambient、direct、emissive 与 sky background 均走独立路径。无 ABI/binding/permutation/
PSO/资源变化；静态 RED/GREEN、focused rustfmt 和 diff check 通过，managed Naga/WGPU、
PNG、RenderDoc、timing、memory、power 仍 pending。状态为
`implemented_static_contract_passed_pending_managed_gpu_profile`。

### 10.16 2026-08-31 capture direct-light shadow ownership review

当前 capture 虽构建六面 light grid，却不构建 shadow command，receiver 也绑定 fallback
shadow atlas，因此所有 direct light 在 probe 中无阴影。UE reflection capture 从 Game
show flags 出发且未关闭 shadows。不能复用 caller viewport directional cascade，也不能按六面
重复 generic shadow plan。正确结构需发布 generation-qualified capture lighting/shadow
product：light-owned point/spot/static 产品可按 generation 复用，directional 需要一次 bounded
probe-volume policy；缺失/陈旧 shadow 必须 typed/observable，不能静默退化为 unshadowed。
目标为每 unique light product 一次 caster census/preparation 加六次 color replay，拒绝
`O(6M)` shadow rebuild。当前仅完成架构审阅，无源码或性能结论，状态为
`architecture_review_complete_pending_capture_shadow_product_owner`。

### 10.17 2026-08-31 source-independent direct diffuse convergence

完整模块复审确认当前实现仍是混合状态：environment、ambient 与 lightmap 已复用
`base_color * (1 - metallic)` 的材质分解，而 Forward Basic/Advanced、Deferred 与
standalone fallback 又逐灯乘以 `1 - F(VoH)`。这使已准备的 diffuse BRDF 随光线与视角
变化，并让 direct 与 indirect 对同一材质使用不同漫反射反照率。

以 Unreal 为主参考重新核对 `BasePassPixelShader.usf`、`ShadingModels.ush` 与
`ForwardLightingCommon.ush` 后，MVP 统一采用 Default Lit 的 source-independent
metallic diffuse owner；Bevy direct-light 组合作为同方向交叉证据。生产硬切删除
`ZrPbrSpecularComponents`、`zr_pbr_isotropic_ggx_components` 与
`zr_aniso_ggx_components`，共享 GGX 只返回 specular BRDF；Forward Basic/Advanced、
Deferred 与 fallback 直接组合已在 light-grid 边界准备的 metallic Lambert 项。
transmission 与 clearcoat 的独立 Fresnel 能量门不变。

本次不增加 ABI、binding、texture sample、feature bit、permutation、PSO identity、循环或
bake recipe；复杂度仍为 `O(visited lights)`，且 diffuse material factor 仍只在光循环外
准备一次。组件 struct/wrapper 删除仅是源码与潜在 IR 简化，尚无 GPU 指令、耗时或功耗
收益声明。静态 RED/GREEN 已确认旧组件符号从 production WGSL 归零、六个 owner/consumer
合同存在；managed Cargo/Naga/WGPU、HDRI PNG、`D:\Tools\renderdoc`、GPU
p50/p95/p99、RSS/VRAM 与 WPR/WPA energy 仍待执行，因此 M5-M8、Failure、manifest 与
coordinator commit 状态保持不变。

### 10.18 2026-08-31 versioned builtin BRDF LUT startup profile and hard-cut plan

完整启动链复审确认 `SceneRenderer` 已不再自行创建 BRDF LUT，而是消费 backend-owned
`SystemTextureGenerationLease`；但 `SystemTextureGenerationOwner::acquire` 的首次 device
generation 仍在发布任何 lease 前同步执行 `128 * 32 * 128 = 524,288` 次 GGX 积分并编码
RG16F。31 个独立进程直接编译/调用当前 production integrator，测得 build wall time
min/p50/p95/p99/max 为 `20.967/22.086/36.618/40.505/40.505 ms`，每次固定 1 次
`32,768 B` 分配、4,096 texel 和相同 checksum。该证据只覆盖 CPU 积分，不包含 WGPU
texture create/upload，也不构成功耗数据。

Unreal `SystemTextures.cpp` 证明相同 128x32、128-sample、system-texture lifetime 与
`PreIntegratedGF` consumer contract；其同步初始化循环是算法/格式参考，不是 Zircon 启动
预算的性能豁免。`EnvBRDFApproxLazarov` 会改变 base/clearcoat 的既有数值合同，不能作为
无画质证据的替换；cmft/cmftStudio 不拥有 split-sum LUT runtime publication，也不能成为
第二套 owner。选定 hard cut 是把当前可信 integrator 的 RG16F 输出生成为带显式 algorithm
version、extent、sample count、format、byte length 和 content hash 的 builtin artifact；产品
路径只 materialize/上传该 immutable payload，动态 integrator 仅保留给 artifact 生成与
byte-for-byte 认证测试，不保留 runtime fallback。每个 device generation 仍由现有唯一 owner
创建/上传 GPU texture，device-independent builtin bytes 跨 generation 复用。

2026-08-31 source hard cut 已把该目标落到唯一 system-texture owner。production acquisition
不再引用 integrator、half encoder 或 runtime sample count，只从
`environment_brdf_lut_rg16float.bin` 物化一个 `Arc<[u8]>` 并沿原有单 ticket 上传。checked-in
artifact 的 algorithm version/extent/sample count/format/byte length/content hash 与认证测试绑定；
测试期仍调用 canonical generator 做 byte-for-byte 比较，production 不保留动态重建 fallback。
artifact 长度为 `16,384 B`，SHA-256 为
`406956356B136BD079CDCCE8DCB86F9E20D596681F7457AD38D91A7EE472674D`，与两次独立生成一致。

诊断链也同步 hard cut，而不是把新的复制成本继续伪装成旧 `payload_build`：runtime report、
viewer startup、ready-frame sidecar、validator 与 profile summarizer 统一使用
`builtin_payload_materialized` / `builtin_payload_cache_wait` /
`builtin_payload_materialization`。ready-frame schema 升为 v17；v16 只允许显式 legacy 读取，
不得与新 profile 汇总。全工作区 Rust 检索的旧 build/built 符号为 0，Python evidence/summary/
visual-oracle 回归 `63/63` 通过（`220.033 s`，工作区 E 盘临时根）。

对已接入 production artifact 的精确 `Arc::from(&BUILTIN[..])` 路径重新执行 31 个独立冷进程，
materialization min/p50/p95/p99/max 为 `26.0/31.1/63.9/188.0/188.0 us`，mean
`37.852 us`；每次固定 1 allocation、allocator charge `16,400 B`、payload `16,384 B`、checksum
`6197230336633806570`。相对优化前 isolated production integrator p50 `22.086 ms` 的 p50 ratio
为 `710.16x`；复杂度从 `O(width * height * samples)` 收敛为 `O(payload bytes)`，且没有新增
shader sample、binding、PSO、permutation、draw、dispatch 或 GPU pass。该数字只证明 CPU
积分工作已移出 runtime；它不包含 WGPU texture create/upload、完整 renderer startup、GPU 或
功耗，不能外推产品启动或 energy 收益。

当前状态为
`implementation_complete_static_and_tooling_green_pending_managed_rust_wgpu_product_timing_power_validation`。
仍需 coordinator managed Rust tests、同源 product startup report、current-source DX12 screenshot、
`D:\Tools\renderdoc` replay 与相同 binary/interval 的 WPR/WPA before/after，才能确认 GPU/upload
瓶颈和功耗是否满足经验预算。M5-M8、manifest、coordinator commit 与性能/功耗验收均不因本次
source/tooling evidence 提前推进。

### 10.19 2026-08-31 realtime procedural policy and single-source recheck

P1-3/P1-4/P1-5 的历史描述已按当前 source 重判。默认 realtime scheduler 每 frame 最多处理
2 个 capture face，并以 21 个 accepted slice 完成 capture、source mips、PMREM 与 SH9；首次
generation 不再把全部 GPU work 放进一帧。调度按包含 source revision、有效 sun 参数和共享
capture shader identity 的 bake key 执行；published key 无 active ticket 时相同静态输入不会再次
请求 generation，连续变化只保留 latest snapshot。因此 MVP 默认语义已经是 OnChange，静态
gradient 的一次首代 bake 是产生正确 roughness PMREM/SH9 所需工作，不应为了“零 bake”退回
未卷积 fallback。Static cook、minimum interval、threshold、importance 或 EveryFrame 是后续产品
策略，不进入当前最小 ABI。

display/fallback 通过 `zr_environment_core.wgsl` 调用 `zr_procedural_sky_radiance`，realtime
capture 也把同一个 `zr_procedural_sky.wgsl` 与 capture wrapper 拼接；颜色、sun disk 和有效 sun
identity 不再有第二套公式 owner。上述是 source/lifecycle closure，不是 GPU timing、画质或功耗
验收；P1-3/P1-4/P1-5 的 MVP source finding 标为 closed，managed Naga/WGPU、current-source
PNG/RenderDoc 与 timing/energy gates 保持开放。

### 10.20 2026-08-31 general IBL command-plan O(P^2) profile and owner decision

P1-1 复审确认 realtime slice 已有 bounded compiled-graph topology cache，但通用 artifact bake 仍在
每个 WGPU pass 调用 `ibl_bake_wgpu_command_plan_for_request`，构造全部 8 个 PMREM、SH9 和 IEM
command，再线性查找当前 pass。完整 `PMREM_SH9_IEM` 图有 `P=10` 个 pass、每份 plan 有 10 个
command 和 55 个 readback copy，因此一次 bake 在 record 阶段实际执行 10 次 full-plan build，
算法规模为 `O(P^2)`，并重复创建 pipeline label、resource name、parameter `BTreeMap`/`Vec` 和
readback `Vec`。这不是 WGPU pipeline cache 命中能够消除的工作。

优化前在 `E:/zircon-profiles/shader06-ibl-command-plan-20260831/` 直接编译当前 production
`compiler.rs`、`ibl_bake_shader_plan.rs` 与 `ibl_bake_wgpu_command_plan.rs`；只有 artifact 尺寸和
graph 常量由 harness 提供，command-plan profile copy 仅把 `realtime_slice` 子模块改为绝对路径。
请求固定为 source 512/10 mip、PMREM 128/8 mip、`PMREM_SH9_IEM`。单份 plan 固定产生 396 次
allocation、41,570 B allocator charge；当前十 pass 路径为 3,960 次、415,700 B，而一次 full-plan
build 后十次 lookup 为 396 次、41,570 B。31 个独立进程、每进程 128 次的 current/candidate
p50 为 1,716,197.7/141,324.2 ns，isolated p50 ratio 为 12.14x；current p95/p99 为
2,848,416.4/3,053,585.2 ns，candidate p95/p99 为 472,246.9/481,593.8 ns。该时间链接现有 debug
dependency artifact，只是 production-source CPU 结构证据，不是 renderer frame、WGPU、GPU 或功耗数据。

Unreal `RenderGraphUtils.h` 的 `FComputeShaderUtils::AddPass` 在 graph authoring 时把当前 pass 的
shader、parameters 与 group count 捕获进 RDG pass；`ReflectionEnvironmentCapture.cpp` 也按 mip/face
逐 pass `AllocParameters`，不会在执行每个 pass 时重建同一组全部 pass descriptor。Zircon 长期应让
compiled graph 持有同等的 immutable pass payload，但当前通用 `RenderGraphComputePassMetadata` 只能
表达 shader/binding schema，不能表达 IBL uniform words、mip-scoped storage view 和 artifact readback
copy。MVP hard cut 因此先让通用 IBL executor 只构建当前 pass 的 exact kernel/command，把总规模从
`O(P^2)` 降为 `O(P)`；不得增加全局 cache、不得复用无 readback 的 realtime compatibility path，
revision/content/readback 语义必须保持不变。后续 render-graph payload ABI 收敛是独立高级里程碑。

2026-08-31 已完成 exact-pass hard cut。dispatcher 先严格解析 canonical `(executor_id, pass_name)`，再调用
`ibl_bake_wgpu_command_plan_for_kind` 只构建当前 requested kernel；未请求、越界、非规范或 executor/pass 不匹配均
fail closed。完整 `PMREM_SH9_IEM` 的 10 个 pass 仍产生 10 个 command 和 55 个 readback copy，逐项
readback offset/length 与 full plan 相同；realtime slice/runtime-kernel 路径未复用或改变。四文件 atomic
dependency set 已取得 Shader06 coordinator lease，最终源码哈希为：command plan
`22B8D474D3F97B960292D935A861AFCA35C438C815C53BA08C6B4F1D77C5ACBF`、command-plan tests
`858103365E8CC0B352EB3A3F7555801DE60A4EDB0C38C98E9E25F344E7D3D053`、dispatcher
`0A588EC75A1B7E20AAACBAC55E7078F58E5A81EBCFD035A56F414F069D83E64B`、dispatcher tests
`EB85A431242C7F1D20ACE43476288A1CE389C3BE04F536C364BEF8943EB4B556`。

最终源码绑定的隔离 profile 位于 `E:/zircon-profiles/shader06-ibl-command-plan-20260831/`，31 个独立进程各执行
128 次 legacy/exact scenario。legacy allocation/bytes 为 `3960/415700`，exact 为 `393/27498`，即下降
`90.08%/93.39%`；command/readback 固定为 `10/55`，checksum 全部为 `0`。按每 scenario 归一化，legacy
min/p50/p95/p99/max/mean 为 `1539145.3/1831436.7/3150285.2/3354645.3/3354645.3/2006622.7 ns`，
exact 为 `123973.4/175880.5/298189.1/318781.3/318781.3/189672.3 ns`，p50 ratio `10.41x`。最终
harness source/exe/raw/summary SHA-256 为 `49535C6A1FF16F274B939515B08B18ECE172FBE494452FED17D0DCF3E335AAB3` /
`98BD1C8F9BC620B47FE3A37F086241119613AA4DC0B11E2D34AB2405E57B5460` /
`EB58658A8A40D5490369EBC04A5EA83A27B4F3C0AC97CCD3609A662122E2D5FD` /
`B7E69FC4FF1225AD51625D1DC4BA748E7BB46122BBFF0D04B15284479D9D508C`。这是 isolated production-source CPU
结构证据；尚不能推导产品帧、GPU、RSS/VRAM 或功耗收益。

当前状态为 `implementation_complete_isolated_profile_green_pending_managed_rust_wgpu_product_validation`。
managed Cargo/WGPU、产品 CPU scope、DX12 PNG/RenderDoc 与 matched WPR/WPA 仍未执行，因此不推进
M5/M6/M7/M8 或性能/功耗验收。

### 10.21 2026-08-31 AmbientLight/lightmap source-policy closure

`affects_lightmapped_meshes`现在从scene component进入render snapshot。`SceneUniform::from_frame`在
同一个O(A) fold内生成all-mesh与lightmapped-mesh两个ambient radiance，ABI新增16 B。Forward、
generated和fallback使用现有GPU Scene lightmap bit选择；Deferred复用`Rgba16Float` emissive alpha
传递同一bit，并从已有emissive load解码。没有新增MRT、binding、sample、feature bit、permutation、
PSO key、allocation或光源/探针循环。Rust回归锁定uniform总大小496 B，以及ambient/new ambient/
previous matrix偏移192/208/224 B；所有包含ambient后续字段的WGSL ABI mirror已静态核对。

E盘隔离`naga 29.0.3`已经验证full Deferred、environment-only Deferred、fallback、两个skybox variant
和六个normalizer delegate全部通过；probe source/binary SHA-256为
`1738DA460B1CF5535F45297C5E70EFAD0A8FD6AA561ACE34A971E29413DB37AC`/
`D8E7D5E589B67B875A1F36405D00B11090751B2D536B713A97CDB10E5AC64EAC`。状态为
`implementation_complete_static_and_isolated_naga_green_pending_managed_wgpu_and_product_validation`。
managed Cargo/WGPU、current-source PNG、`D:\Tools\renderdoc`、timing、RSS/VRAM和功耗仍开放，
因此M5-M8不推进。

### 10.22 2026-08-31 cold procedural fallback/bootstrap disposition

粗糙度无关的procedural fallback仍是确认缺陷，但六态status已把它标成fallback而非Ready。默认首代
包含21个单operation batch；把all-face工作合并后仍约有17个GPU pass，只减少frame boundary，不
减少capture/mip/PMREM/SH9滤波。batch array、topology identity、attempt/stale token、retry和timing
均以单operation为原子合同，因此没有GPU/power基线前不做局部合批。状态为
`algorithm_defect_confirmed_structural_bootstrap_cut_pending_current_source_gpu_and_power_profile`。

### 10.23 2026-08-31 realtime IBL dynamic-binding ownership recheck

原P1-2把全部realtime dispatch描述为动态binding创建，current source已不是如此。默认ticket的10个
PMREM与1个SH9 pass由`RealtimeIblWgpuBindingCache`按work slot和command key持久化；stable-slot重放
是11 hit、0新params buffer和0新bind group。剩余3个capture与7个source-mip pass仍每代创建10个
小uniform buffer与10个bind group；cold slot另需11组cacheable对象。现有ignored WGPU profiler已经
分别记录三类creation count/micros和adapter identity，但尚无current-source profile receipt。

recorder/capture/binding文件都是共享工作树中的foreign modified source，本轮不改实现。必须先获取
cold/warm p50/p95/p99、CPU graph/record scope和匹配WPR/WPA，才能在persistent parameter arena、
dynamic offsets或per-slot immutable templates间选择。状态为
`realtime_pmrem_sh9_source_closed_dynamic_capture_source_mip_profile_pending`。

### 10.24 2026-08-31 base-lobe IBL recipe disposition

current source已经有versioned `IblBakeRecipe`，统一拥有PMREM sample tiers、roughness/mip双向映射、
solid-angle scale、full-roughness cosine gate、diffuse source mip、RGBA16F output format，以及asset CPU
和runtime GPU两种diffuse integrator identity。P1-8不再描述成“没有recipe”。

剩余边界是128x32x128 BRDF LUT domain/integrator没有进入该identity，且base-lobe
multiple-scattering没有mode、artifact version或误差门。仓内没有multiple-scattering consumer。
MVP保留已对齐Unreal joint-Smith的single-scatter baseline，先完成managed image/error/performance验收，
不为关闭P1提前引入高级能量补偿。状态为
`base_single_scatter_mvp_recipe_present_brdf_lut_identity_and_multiscatter_contract_pending`。

### 10.25 2026-08-31 reflection capture layer-culling contract

P1-16的layer request/key与packet-local基础设施已实施，但完整capture extract尚未闭合。
`RenderEnvironmentCaptureRequest`现在拥有独立
`RenderLayerSet`；默认覆盖scene schema v1的32层，显式空集合保留sky-only语义。该集合进入
renderer-owned capture bake key，防止不同可见集复用同一runtime-cache artifact。该mask在唯一一次
mesh draw census之前写入初始`CameraRenderDescriptor::culling_mask`，并由六面capture camera继续持有；
因此对已提交packet的几何筛选维持一次`O(M)`构建，而不会退化为六次draw重建。placement的`layer_mask`仍只决定成品probe影响哪些
receiver，不能作为capture可见集替身。
同一mask也进入`RenderEnvironmentCaptureOutputIdentity`并通过只读getter对status消费者开放，使scheduler/status
终态比较和诊断不会把不同capture可见集当作同一输出；layer count固定按`u64`、layer id按`u32`写入key，
避免host pointer width污染durable identity。

reflection-probe runtime/editor JSON合同从schema v1提升到v2，新增`capture_layer_mask`且不为旧
v1提供隐式默认迁移；constructor默认`u32::MAX`，与neutral request的v1-layer默认一致。空mask、
跨32位block的neutral layer set、draw构建前安装、六面保留、volume mask不变、output identity和bake-key分离均有源码回归。
结构复审同时确认当前framework边界仍接收已由viewport camera layer过滤、并按viewport位置选择LOD的
`SceneViewportRenderPacket`；后置mask只能收窄，不能恢复被上游移除的对象。因此本切片不声称完整layer selection
已关闭，也不解决self exclusion、transparent/emissive/sky/exposure/LOD策略或light layer/channel；后者仍归09B/09E共享ABI。
`rustfmt`、静态合同和scoped `git diff --check`通过，managed
Cargo/WGPU、PNG、RenderDoc、timing和功耗仍待执行。状态为
`capture_layer_request_key_and_packet_local_culling_implemented_static_green_full_capture_extract_pending`。

### 10.26 2026-08-31 reflection-probe scheduling authority hard-cut review

Current-source tracing confirms that `ProbeBakeTiming::EditorManual/RuntimeManual` is a public render-snapshot label with no behavior: only the default, getter/builder, an offline-bake default override, reflection-probes plugin placement JSON/runtime projection, and roundtrip tests consume it. Renderer, World, Editor, and the environment-capture scheduler never read it. The plugin exposes nonblocking request/poll/cancel wrappers, but no Workbench, Inspector, World, or other product consumer invokes them. Expanding the enum would therefore create a second policy surface without execution authority.

The Unreal reference keeps static reflection captures in dirty/on-load queues and drives runtime captures through explicit refresh requests, frame budgets, timeslicing, and one-shot request flags. Every-frame refresh is diagnostic or a separate SceneCapture capability, and redundant manual capture is explicitly treated as a major inefficiency. Shader06 therefore selects a direct MVP cut: remove `ProbeBakeTiming` from `ReflectionProbeData` and both public exports without aliases or shims; the future authored scene-component lifecycle translates manual/change/load events into revisioned `RenderEnvironmentCaptureRequest` values; the existing RenderFramework scheduler remains the sole owner of admission, progress, cancellation, failure, publication, and last-good state. Every-frame reflection capture stays out of MVP until a dynamic-capture GPU/power before-profile and a shared budget owner exist.

The six-file dependency set was reconciled atomically at coordinator epoch 584 and the hard cut is now implemented. `ProbeBakeTiming`, the `ReflectionProbeData` field/getter/builder, both public exports, the offline default override, and the plugin placement field/projection are removed without serde aliases or compatibility shims. Core probe JSON and plugin placement JSON no longer emit `bake_timing`; the placement contract retains `deny_unknown_fields` and explicitly rejects a legacy payload containing that field. Production Rust has zero `ProbeBakeTiming` matches, while the only `bake_timing` matches are the two negative regression assertions. TDD RED snapshot `2475`, focused rustfmt, static contract search, and scoped diff integrity are green. The remaining product gap is unchanged: a future scene/Editor lifecycle owner must turn authored manual/change/load events into revisioned requests and prove terminal status through the existing scheduler. Status: `fake_snapshot_policy_hard_cut_implemented_static_green_product_scene_consumer_pending`; managed Cargo and product validation remain pending, so M5-M8 do not advance.

### 10.27 2026-08-31 environment/probe transition ownership disposition

The capture identity and last-good baseline are already present: output identity separates scene, environment, and output generations; the scheduler publishes only a current generation; the local-probe allocator uses one physical spare so capture refresh keeps sampling the old ready slot until atomic commit and discards the candidate on cancel/failure. The spare is released immediately after commit, so it is not a transition pool. Ordinary global-environment rebind still has the Render11 rollback failure recorded by this plan and must be corrected before adding dual resources.

Unreal implements runtime reflection-capture smooth blending as an explicit one-shot request coupled to runtime budgets, timeslicing, fade state, and a bounded blend-slot pool; captures without a slot switch immediately. Shader06 therefore keeps atomic last-good replacement as the MVP contract and does not add unconditional dual PMREM/probe sampling, a second bind group, or a generic history dependency. Advanced crossfade requires a dynamic day/night or streaming before-profile with GPU percentiles, extra resident bytes, slot pressure/fallback counts, and WPR/WPA power, followed by one owner for bounded transition slots, old/new generation, pre-exposure blend domain, duration, supersede/cancel, and reclamation. Camera-cut invalidation applies only if that design later consumes screen-space temporal history. Status: `mvp_atomic_last_good_identity_present_advanced_crossfade_deferred_pending_render11_transaction_and_dynamic_profile`; no implementation or performance claim is made.

## Code Review Disposition (2026-07-31)

The plan now mirrors the current source: environment resources occupy group0 bindings 1..6; probe and planar resources retain their separate group1 bindings; the public sampling names and full PBR indirect signature use the implemented `zr_environment_*` forms; and the source mip owner is `source_cubemap::mipmap`. The SSR/RT future hook is described through the existing `ZrEnvironmentPbrComponents.specular` output rather than a nonexistent `zr_env_specular_ibl` symbol.

`IBL_BAKE_ALGORITHM_VERSION` is an explicit cache-invalidating maintenance value, so a PMREM content or sampling-LOD mapping change must raise it in the same source change. The prior `max_mip - 1.0 + 1.2 * log2(roughness)` lookup advanced it to `2026_07_31_0004`; the GPU IEM source-mip/provenance contract advanced it to `2026_08_09_0006`; the Unreal-equivalent `max_mip - 1.0 - roughest_mip_offset + 1.2 * log2(roughness)` repair advanced it to `2026_08_24_0007`; and the low-roughness PMREM D_GGX/PDF repair advances the current value to `2026_08_26_0008`. `max_mip` is the last index in both the Zircon WGSL helper and Unreal's `ReflectionCubemapMaxMip`, so the second subtraction is intentional. Bake artifacts use the current source constant. The manual quantitative export combines that value with the BLAKE3 of the running lib-test executable and the bytes of the 2K Lakes HDR input. The executable hash is frozen before rendering and is the code provenance for the pixels; the HDR hash is frozen before rendering and rechecked immediately before the immutable PNG/TXT pair commits. The TXT report records both values, so an unbuilt worktree edit cannot masquerade as code that produced the image, while a PBR/material/renderer/shader correction that is actually rebuilt obtains a new executable identity. The non-export quantitative render path performs neither hashing step.

The 2026-07-31 follow-up independent review found the realtime IBL lazy initialization clean: both render paths initialize before bind-group sampling, the first procedural frame still writes the work slot before scene draws, and submission/failed-record token handling is unchanged. HDRI-only frames leave diagnostics as safe no-ops and do not allocate realtime GPU resources.

M5 acceptance still requires current-source executions of `runtime_environment_source_cubemap_pmrem_rough_mips_reduce_cube_seam_energy`, `render_product_environment_pbr_matrix_quantitative`, `realtime_runtime_defers_gpu_resource_creation_until_a_procedural_frame`, and `first_procedural_frame_initializes_realtime_gpu_resources_and_starts_full_batch`, in addition to the managed direct-present gates above. Existing screenshots and historical RenderDoc artifacts remain visual baselines only, not substitutes for those quantitative readback assertions.

The 2026-08-01 procedural-sun convergence closes a stale realtime-bake identity hole and removes uniform normalization plus two cosine evaluations from each direct/capture shader invocation. It also makes sampling-time intensity and rotation consistent before and after realtime IBL publication. Scoped Rust formatting, diff integrity, and legacy per-invocation sun-math scans pass. The required second independent review reports 0 critical / 0 important / 0 minor after verifying bake identity, the 112-byte capture ABI, CPU/WGSL rotation signs, direct/realtime intensity semantics, lazy availability checks, and the invalid-direction regression. M5 remains `in_progress`: these source changes still require fresh managed tests, quantitative export, DX12 viewer timing, screenshot, and RenderDoc replay before accepted closeout.

The 2026-08-01 startup follow-up confirms that the historical 82--100 second load is DX12 synchronous WGSL/PSO compilation, not HDR decode, PMREM generation, cache restore, screenshot encoding, or CPU readback. The completed infrastructure work removes eager deferred-pipeline construction from the environment-only viewer path, keeps the SSS deferred variant lazy, splits the fullscreen vertex module, prewarms the exact Base material key, and uses GPU direct present outside screenshot mode. The remaining no-shadow Base Forward variant now substitutes a binding-free `zr_gpu_light_shadow_visibility` API stub for the 7,850-character shadow atlas/PCF module when `RECEIVE_SHADOWS` is absent; the shader input for this module falls to 165 characters (97.9% reduction), while shadow-enabled variants retain the complete implementation and the canonical include token/content-hash cache contract. The required second review found one Important performance issue: the first draft constructed and hashed shadow support before pass dispatch, including non-Forward passes. The forward fix moves both shadow and volumetric specialization inside the Forward match arm; post-fix formatting, scoped diff integrity, Forward-only source-selection contracts, and the existing Python environment/prewarm contracts pass. M5 remains `in_progress` until a fresh managed Cargo/WGSL execution, quantitative export, current-source DX12 timing/screenshot, and RenderDoc replay are accepted; queued/running coordinator work is not polled and historical PNG/RDC files remain non-acceptance baselines.

The latest M7 provider-boundary forward repair preserves the global-only specialized Base shader only while the environment preview retains its one-slot local-provider placeholder. If a visible baked probe or planar provider expands that resource, the variant registry switches subsequent compatible Base requests to the generic environment key before draw construction; the generic key then remains selected to avoid visibility-driven variant thrashing. This prevents provider radiance from being silently omitted by the specialized WGSL while retaining provider-free startup savings. Focused resource/variant regression contracts, Rust source parsing, source-order checks, and diff integrity pass. M7 remains `in_progress`; this is not fresh Cargo/WGPU, DX12 timing, screenshot, or RenderDoc acceptance evidence.

The mandatory independent review of that repair found the async Base PSO transition could still produce a SkipDraw placeholder for the new generic key even though BaseScenePass requires a concrete pipeline. The forward repair is provider-event scoped: FullScene resource capacity does not enable it, while an actual environment-preview upgrade makes subsequent Base pipeline resolution synchronous and finishes any already pending target. The generic fallback test exercises this exact path with async compilation enabled and requires a concrete pipeline plus no pending work. Rust source parsing and diff integrity pass. M7 stays `in_progress` pending post-repair independent review and the existing managed Cargo/WGSL, WGPU, DX12, screenshot, and RenderDoc gates.

The post-repair independent review reports `Critical 0 / Important 0 / Minor 1`. The Minor is limited to the absence of a full `SceneRendererCore::write_scene_uniform` fixture that crosses unrelated renderer subsystems; the actual provider resource upgrade, generic cache fallback, async concrete-pipeline path, and core source ordering are covered independently. It does not change failure ownership or acceptance requirements. M7 remains `in_progress` pending managed Cargo/WGSL, WGPU, DX12 timing/screenshot, and RenderDoc replay evidence.

The core ordering portion of that Minor is now retained in the Rust regression suite: it requires reflection-provider preparation before the generic-key fallback, and scene-uniform writing before direct mesh draw construction. The resource and pipeline tests retain their real WGPU coverage, while the source-order test avoids coupling this narrow fallback behavior to a full deferred/overlay/IBL core fixture. M7 remains `in_progress` under the same managed evidence requirements.

The same current-worktree regression sweep exposed a Python-version-dependent shader-prewarm infrastructure defect before shader execution: the checked-in `native_dynamic_fixture` owns real `assets/shader.wgsl` files, but Python 3.12 `Path.glob("assets/**")` yields only the directory, which the validator then filters out as non-file. The distribution-assets owner now treats only a terminal `**` as recursive contents (`**/*`), preserving every other glob, root-confinement, retired-UI, and `.zui` document validation rule. A red/green nested-asset regression passes on Python 3.12 and 3.14; the affected prewarm/permutation/cache/provenance/resource/dimension/wrapper set now reports 127 passed with one intentional skip, and the environment/lightmap contracts pass 9 tests. This is a forward infrastructure repair discovered by validation, not M5 visual acceptance; the milestone remains `in_progress` under the same current-source Cargo/GPU/screenshot/RenderDoc requirements.

The expanded 2026-08-01 review then found that source selection alone was insufficient: material assembly still constructed, copied, dependency-scanned, and hashed the complete builtin module registry before resolving the selected roots. Runtime material assembly now extracts roots first, returns immediately for an empty root set, and constructs only the reachable builtin/custom dependency closure. Deferred-lighting assembly uses the same root-scoped registry and supplies only the builtin shading-model sources selected by `FullScene`, `StandardPbrPreview`, or `EnvironmentOnlyPbrPreview`; the full registry remains available for IDE/all-builtin enumeration and tests. A source contract rejects reintroducing eager `with_builtin_modules()` in deferred runtime assembly. This removes avoidable CPU source work from both no-shadow Base Forward and environment-only startup without changing canonical include tokens, content hashes, custom include precedence, or the generic FullScene shader contract.

That review also exposed two validator portability gaps. Windows drive-relative globs such as `C:outside/**` could reach `Path.glob` and raise `NotImplementedError`, while Windows root-relative globs such as `\\outside\\**` were not rejected by native-path checks on POSIX hosts. Validation now applies both native `Path` and `PureWindowsPath` anchor/parent semantics before matching and converts platform glob exceptions into diagnostics. Red/green regressions cover terminal recursive contents, drive-relative, root-relative, and parent traversal forms; the distribution owner set passes 31/31 on bundled Python 3.12, the real fixture CLI reports `fatal=false` with no diagnostics, and plugin discovery reports all 38 plugins. These are infrastructure results, not GPU acceptance. M5 stays `in_progress` pending fresh managed Rust/WGSL and quantitative executions plus current-source DX12 timing, screenshot, and RenderDoc replay.

The final incremental independent review reports `Critical 0 / Important 0 / Minor 0`. It verified that environment-only deferred assembly no longer constructs the unreachable disabled-volumetric override, the source contract locks that root condition, and the evidence/test counts match that candidate. A subsequent PBR hot-path audit found that global sky/source, SH/IEM, and PMREM work still ran when the global environment intensity was non-positive, while local reflection probes and planar reflections retained their own independent enable/intensity semantics. The current source therefore returns zero components before normalization or texture access only when the global intensity is non-positive, the probe set is empty, and planar reflection is disabled. The first draft incorrectly skipped valid local providers and received `Critical 0 / Important 1 / Minor 0`; after the provider-aware forward fix, the follow-up independent review reports `Critical 0 / Important 0 / Minor 0`. The assembled-source ordering/condition gate and 9 environment/lightmap contracts pass. None of these implementation results advance M5 beyond `in_progress` or replace the outstanding managed product gates.

The 2026-08-01 environment-only Base follow-up removes the remaining unreachable direct-light shader graph from the exact viewer prewarm. A dedicated `ENVIRONMENT_ONLY_PBR` variant bit is applied only to compatible fallback Standard-PBR opaque Base keys; GBuffer, custom shaders, shadows, alpha, unlit, advanced PBR, and volumetric variants retain the generic source. The feature-aware source owner selects a Forward pass containing only material evaluation plus environment IBL, and substitutes a minimal Standard-PBR shading body under the canonical include token with a distinct content hash. Light grid, cookie, irradiance-volume/lightmap, shadow, volumetric, and advanced-PBR modules are absent from both WGSL and the include-hash manifest. The regression requires at least a 25% assembled-WGSL reduction and the exact WGPU prewarm gate requires the reduced bit in the reused cache identity. A conservative comparable-source inventory reports 72,564 bytes for the generic Forward lower bound and 39,435 bytes for the reduced path, a 45.7% reduction; this is source-volume evidence, not DX12 PSO timing. Scoped formatting, diff integrity, new-file whitespace, and source guards pass. The required independent second review reports `Critical 0 / Important 0 / Minor 0 / Ready` after checking profile boundaries, cache identity, WGSL ABI, Standard-PBR equivalence, assembled-source regressions, and asynchronous pipeline completion. The immutable M5 managed attempt is terminal `failed` at workspace materialization with `validation_copy_attribution_stale`: its path set cannot absorb the post-snapshot owners and must not be rewritten or rolled back. Append-only M6 therefore owns the complete post-snapshot delta as a sibling of M5 after M1-M4. M6 must first integrate the new WGSL dependencies and performance owners into the current baseline; M5 then reruns its original immutable manifest against that forward baseline so both milestones can receive accepted succeeded attempts. Fresh managed Rust/WGSL, quantitative, DX12 screenshot/timing, and RenderDoc replay evidence remain required. M5 and M6 remain `in_progress`; historical artifacts are not acceptance evidence.

The next immutable M6 Windows attempt (`job 3b27c26867f2457e898972f9297c1e91`, `run 808f1cf31210424189ba4542dcb70953`) also reached a terminal failure before any Shader06 test executed: clean `main@322a03acfec7c8527cec593a4165af3ae31437b5` declares `meshopt` in `Cargo.toml` and `zircon_runtime/Cargo.toml`, but its checked-in `Cargo.lock` contains neither the `meshopt` package nor the `zircon_runtime` dependency edge, so `cargo --locked` correctly refuses the snapshot. This is the same lowest shared cause already owned by `docs/plans/zircon_runtime/runtime/04/failure-2026-07-17-woc-gltf-meshopt-webp-import.md` (`status: open`), not a new Shader06 lifecycle. Shader06 keeps the integrated/current implementation, does not edit the foreign lockfile or use `--unlocked`, and remains active through independent review/static work until the Runtime04 forward fix lands. The fresh incremental review found no additional Critical/Important/Minor production finding; M5/M6 stay `in_progress`, and no Rust/WGPU, quantitative image, DX12 timing, screenshot, or RenderDoc acceptance is inferred from this pre-test failure.

The 2026-08-08 CPU bake hot-path follow-up removes per-mip face-pixel staging from both the UE angular source-mip builder and cmft-derived GGX PMREM builder, and per-face staging from the parallel equirect-to-cubemap base projection. The face-major owner now borrows six final-output mip slices as a fixed array; serial and budgeted parallel executors fill those non-overlapping slices directly, so neither path allocates six temporary pixel vectors or copies them back. PMREM-only reconfiguration also retains the immutable source pyramid through its `Arc` rather than copying every source texel, plus its canonical source-derived SH9 cache. Artifact application may replace active diffuse coefficients, but it preserves that cache; a later PMREM layout reconfiguration restores the cached source result without copying or re-integrating the source pyramid, so artifact SH9 cannot leak into the new layout. The helper clamps a requested mip to the terminal chain level, matching the public layout offset contract. New regressions cover final-storage mapping, terminal-mip clamping, PMREM serial/parallel equality, the retained source-mip parallel schedule, the exact projection-plus-PMREM executor schedule, and artifact-SH9/cache reconfiguration equivalence. This is output-equivalent CPU memory-traffic reduction only: equirect UV projection, GGX/FIS sampling, angular weighting, face order, artifact bytes, and `IBL_BAKE_ALGORITHM_VERSION` remain unchanged. `rustfmt --check`, scoped diff integrity, helper-use/legacy-staging scans pass; managed Rust/WGPU, DX12 timing, screenshot, and RenderDoc gates remain required, so M5/M6/M7 stay `in_progress`.

The required independent second review of the CPU bake change reports `Critical 0 / Important 0 / Minor 0`. It verified the six disjoint face/mip borrows satisfy the executor `Send` boundary, `mip_count == 0` and terminal-mip clamping preserve the established layout contract, and the source-mip, PMREM, and parallel equirect projection filters retain their previous pixel loops and scheduling gates while eliminating only temporary vectors and copy-back traffic. A follow-up review found that PMREM-only reconfiguration had accidentally retained artifact SH9; the forward repair retains a separate canonical source-SH9 cache across artifact application and uses it for later PMREM reconfiguration, matching the canonical builder's PMREM and SH9 output even when artifact coefficients deliberately differ. The independent review of that cache-retention follow-up reports `Critical 0 / Important 0 / Minor 0`. Because final-storage writes and copy removal are output-equivalent, that CPU-only refactor retained `IBL_BAKE_ALGORITHM_VERSION = 2026_08_02_0005`. M8 later advances the version to `2026_08_09_0006` for the non-equivalent GPU IEM source-mip/provenance repair; this invalidates stale runtime-cache IEM output rather than attributing the rebuild to the final-storage refactor. Its independent review reports `Critical 0 / Important 0 / Minor 0`. This is static/source review rather than managed product acceptance; M5/M6/M7 therefore remain `in_progress` pending the existing locked Rust/WGPU, DX12 timing/screenshot, and RenderDoc replay gates.

M8 is now `in_progress` under [2026-08-09-m8-canonical-ibl-recipe-and-performance-plan.md](06/2026-08-09-m8-canonical-ibl-recipe-and-performance-plan.md). The structural review separates the historical DX12 PSO startup from cache-miss IBL work; M8 now shares the source-mip policy while storing CPU/GPU producer provenance in the persistent artifact descriptor, because their integration rules remain different. A process-cold CPU asset-cache hit now combines staged source mips with validated PMREM/SH9/IEM artifact sections directly instead of rebuilding and immediately discarding PMREM; it transfers the decoded source-mip allocation without a second full-pyramid copy, while separately retaining source-derived SH9 for later PMREM reconfiguration. Import reports attribute equirect projection, source mip, PMREM, SH9, IEM, and bundle-write time without double counting the cubemap envelope, and viewer Ready evidence v12 inherits those diagnostic CPU IBL subphases plus direct-IEM source-sample visits. Fresh Windows CPU/GPU measurements, screenshot, and RenderDoc evidence under `E:` remain required before a GPU integrator can be treated as canonical. This planning result is not M5/M6/M7 acceptance evidence.

The initial 2026-08-10 structural review was nonaccepting at `Critical 0 / Important 9 / Minor 2`. Its cross-frame artifact hydration, runtime writeback, optional-IEM policy, recipe/hash authority, BRDF LUT startup, source-stage identity, duplicate hashing, viewer work-root, and stale skybox-test findings were forward-repaired in M8. The subsequent required post-repair second review reports `Critical 0 / Important 0`; managed Rust/WGPU parity and current-machine DX12 timing, screenshot, and RenderDoc evidence remain mandatory, so this source review is not acceptance.

The 2026-08-10 forward implementation batch now also addresses the cross-frame hydration churn, synchronous runtime writeback, default optional-IEM cost, source-stage cache identity, and BRDF LUT startup work identified by that review. Runtime writeback joins the frame's single GPU submission and completes through a bounded nonblocking queue; `.zcube` has a source-only key and is neither rebuilt nor rewritten on a derived-only miss; and the BRDF LUT uses Unreal's 128×32×128 initialization scale, reducing CPU integration iterations from 16,777,216 to 524,288. The same-integrator sparse comparison measured mean/max RG absolute error 0.00238490/0.01624132. These changes remain `in_progress` and validation pending; the review count is intentionally not rewritten until the required second review and managed gates run.

The same M8 batch now centralizes the canonical IBL recipe in the framework: CPU solid-angle and renderer GPU Hammersley paths have distinct descriptor identities, while source LOD, PMREM samples, roughness mapping, optional-IEM extent, and RGBA16F layout come from one contract. The renderer's three IBL WGSL pipeline identities are derived from their included source bytes, so a shader edit cannot silently retain a stale manual content-hash constant. Validated artifact hydration records its accepted descriptor on `SourceCubemapEnvironment`, rechecks the descriptor against the concrete environment request, and clears provenance when optional IEM content changes while keeping source identity stable. These are implemented source contracts, not CPU/GPU equivalence evidence: M8 remains `in_progress` pending the fresh second review, managed Rust/WGPU parity, DX12 timing/screenshot, and RenderDoc replay.

The subsequent M8 forward repair gives each runtime-cache miss an owned reservation that moves from frame compilation through scene submission into the bounded GPU readback queue. Any build, prepare, graph, device-poll, mapping, or cache-write error drops that handle and releases the pending key, allowing the next frame to retry instead of suppressing IBL indefinitely; a device-poll failure drains all queued writebacks before it propagates. A successful queued readback retains the handle only until it persists or fails. When no project cache store exists, the source CPU environment remains the live MVP fallback and the compiler deliberately omits the unconsumed runtime bake graph on both the first and following frames. This remains implementation evidence only; M8 stays `in_progress` pending the renewed independent review and managed Rust/WGPU, DX12 screenshot/timing, and RenderDoc gates.

The 2026-08-13 PBR material/occlusion forward batch closes a non-visual infrastructure gap independently of the outstanding validation queues. glTF occlusion strength now survives both the built-in ingest owner and the stable importer plugin, binary artifact reload, material ownership/validation, standard descriptor projection, runtime streaming/capture, Hybrid GI card capture, and the existing 48-float GPU material uniform without a new binding or permutation. Surface and card-capture evaluation use the glTF `mix(1, sample, strength)` rule instead of multiplying or consuming the texture unscaled. Environment diffuse remains linearly AO-masked, while base and clearcoat specular IBL share the Unreal roughness-squared specular-occlusion function; direct lighting remains AO-independent. Zero AO skips clearcoat reflection texture work and AO boundary/near-mirror cases avoid `pow`. Focused source contracts cover defaults, invalid values, artifact-preserved property ownership, both importer paths, capture DTO propagation, GPU packing, WGSL assembly/validation, formula boundaries, and removal of the old linear specular-AO path. Scoped diff-integrity checks pass; the repository-wide formatter still reports pre-existing recursive formatting drift, while the newly touched expressions are formatter-conformant. The managed Windows command produced no terminal receipt within the invocation window and was neither retried nor polled; therefore M5/M6/M7/M8 remain `in_progress`, and no current screenshot or RenderDoc acceptance is claimed.

The same 2026-08-13 infrastructure pass verifies that a process-cold runtime `.zribl` hit is no longer treated as a graph-only decision: frame extraction resolves the cache payload, hydrates PMREM/SH9 into the source environment, memoizes the hydrated value by request/source identity, and only then removes the bake request. The staged `.zcube` direct path separately retains canonical source-derived SH9 while exposing artifact SH9 as the active diffuse result, so a later PMREM reconfiguration is equivalent to rebaking from immutable source mips; GPU runtime artifacts are rejected from that CPU direct-hydration API. These forward repairs and their regressions were already present in the integrated worktree and were preserved rather than duplicated.

Shader startup profiling now has source-bound structural and creation-cost layers before any PSO identity rewrite. `ShaderVariantMissReport` and `DiagnosticStore` expose full registered pipeline variants, unique shader variants, variants normalized only across base-color/metallic-roughness/occlusion/emissive texture-presence booleans, the resulting texture-presence-equivalent expansion, live mesh pipeline/module cache residency, cumulative WGPU creation calls, CPU microseconds spent inside shader-module/render-pipeline creation, and separate async Base queue-wait sample/elapsed totals from successful admission to worker job start. Registry updates occur only when a new variant is inserted; report reads do not rescan the registry or add per-draw work. The background Base worker shares a rare-event locked metric snapshot, so cache-hit draw paths remain untouched; live-manifest prewarm uses the same owner and records primary, companion, and OIT creations before validation-scope resolution. Pure contracts lock the current 16 full / 1 normalized / 15 equivalent texture-presence result, verify that TAA pipeline kind and normal-map shader-feature differences remain distinct, and require synchronous/background/live-manifest creation to count while repeated cache hits do not increase count or time. A blocked-predecessor regression requires one actual Base job to publish a nonzero queue wait without counting duplicate/full/unavailable attempts. The retained M5 evidence still attributes the historical 82--100 s delay to synchronous DX12 WGSL/PSO construction rather than the approximately 1.08 s IBL restore, but the 16-way result is only a multi-material scale-risk bound, not the historical single-material cause. GPU timestamps, power telemetry, three-run cold DX12 corpus, current screenshot, and RenderDoc replay remain required before choosing normalization versus bounded texture specialization; M5/M6/M7/M8 stay `in_progress`.

The required incremental review of that profiling layer first identified one live-manifest accounting gap: prewarm created ordinary, companion TAA, and OIT WGPU objects without publishing the corresponding creation totals. The integrated forward repair records each actual call before validation-scope resolution and leaves a complete cache-hit replay at zero metric delta. The renewed review reports `Critical 0 / Important 0`; scoped formatting and diff integrity pass. This remains nonaccepting source evidence, so all current milestones retain `in_progress` status and the managed WGPU/DX12/image gates remain unchanged.

The 2026-08-25 PBR viewer Ready-frame current evidence schema is now v14. It retains the twelve `ShaderVariantMissReport` structural, residency, WGPU creation-cost, and async Base queue-wait metrics introduced by v12, adds v13 BRDF-LUT startup provenance, and adds v14 host capability fields. This lets a process-cold DX12 capture separate shader/PSO creation from admitted-worker queue delay while proving the actual diagnostic/native host mode. The standalone verifier permits v2-v13 only behind explicit legacy inspection, requires all current v14 fields, rejects u64 overflow, zero registered variants or capture-ready GPU residency, inconsistent texture-presence equivalence, cache residency greater than creation events, and nonzero queue time without an admitted job. A successful v14 CLI result emits the structural metrics plus renderer initialization, deferred Standard PSO, IBL restore, total startup, one-shot Base wait, viewer Ready, Ready-render durations, BRDF-LUT startup fields, and host capability as structured JSON for cold-run comparison. This is source-wired static infrastructure, not WGPU evidence; it does not replace the pending current-source screenshot/RenderDoc gate.

2026-08-25 的当前 IOR 与性能证据源契约已收敛。非默认 dielectric IOR 由 `pbr_ior_override` 路由到 `advanced_pbr_opaque` 的通用 Forward 路径，不进入 shader feature 或 PSO identity；同帧 `RenderStats`/`render.mesh.queue.*` 公开 ordinary-opaque、advanced-PBR-opaque、dynamic-command、cache-hit 与 rebuild 计数，且不增加逐 draw 或 GPU 同步工作。viewer 通过封闭参数 `--material-fixture <metal-mirror|dielectric-ior>` 选择默认 `viewer-project-v4` 镜面材质或隔离的 IOR=2.0 项目树；IOR 路径只等待实际 static material 的 `generic-forward-pbr-ior` Base PSO，绝不以环境专用镜面预热作为就绪代理。Ready sidecar 为 v16，绑定 fixture、实际 Base PSO 和 capture-time readiness；未使用的 environment-only 预热必须记录为 `not_requested` 与 false/zero。GPU timing sidecar v3 的 31 个样本仅在 frame generation 精确匹配时携带五项 mesh submission 计数，验证器与汇总器拒绝缺失、代际不匹配或采样期漂移。profile runner 将 fixture 和 v16 PSO admission 语义绑定到 cache-seed、cold、warm、RenderDoc 命令、display oracle 与汇总重放，避免 IOR 与镜面结果混合。关键 source provenance 闭包为 18 个 viewer 模块加 74 个跨模块所有者，共 92 个文件，并包含 realtime IBL execution-resource cache 与 CPU timing owner。以上均为源码/静态契约：未运行 Cargo、WGPU、DX12、WPR、截图、RenderDoc 或功耗采集；M5/M6/M7 仍为 `in_progress`，M8 源码实施为 `implementation_complete_pending_managed_validation`，不得把历史图像、静态计数或默认镜面结果当作 IOR、性能或功耗验收。

2026-08-26 的 split-sum 输入域与 LUT 积分器复审完成源码修复。共享 `zr_pbr_material_f0` 现在与 Unreal `GetMaterialBaseColor()` 的 `saturate` 边界一致，将 metallic base-color F0 限制在 `0..1`；Standard/basic/fallback/deferred 的直接光与环境光均消费该唯一所有者。BRDF LUT 则从可分离 Schlick-IBL geometry 改为 UE `SystemTextures.cpp` 的 joint-Smith visibility；相同 128 点 Hammersley 扫描中，旧式与 UE joint-Smith 的网格通道 MAE/最大误差为 `0.022225/0.300572`，最差点 `NoV=0.02, roughness=1` 的 A 项为 `0.581606` 对 `0.882178`。新实现的锚点 `NoV=0.1, roughness=1 -> A/B=0.754189/0.025194` 与 `NoV=0.5, roughness=0.6 -> 0.671656/0.013660` 已进入回归。基础各向同性直接光同步采用 UE `Vis_SmithJointApprox`；旧精确式与该近似式在 256³ `(roughness, NoV, NoL)` 网格的平均/最大相对可见性差异为 `7.322394%/29.151215%`，代表点 `roughness=0.5, NoV=NoL=0.5` 为 `0.917663` 对 `0.800000`。Forward、fallback、deferred 已收敛到自包含的单一 GGX core；精确式迁入高级 include，仅保持现有各向异性路径，基础/environment-only 编译闭包不携带对应高级或直射光源码。该复审还移除了 D_GGX 对整个 `π*d²` 的 `1e-6` 地板：已有 `alpha>=0.001` 保证合法点积域分母为正，而旧地板会影响 `roughness<0.154119`；`roughness=0.08, NoH=1` 的 D 项由错误的 `40.96` 恢复为 UE 公式的 `7771.237456`。该变化不增加 binding、材质 ABI、feature bit、shader permutation、PSO key 或 `IblBakeKey`；虽然基础公式静态少两次 `sqrt` 且删除两份 GGX 源码副本，但没有 GPU profile，不能声明性能收益。限定格式、差异与源码闭包检查通过，但未运行 Cargo/WGPU、Windows 启动计时、PNG 或 RenderDoc，因此 M5/M6/M7 仍为 `in_progress`，M8 仍为 `implementation_complete_pending_managed_validation`。

同日 PMREM FilterCS 完整调用链复审先否决了一个错误候选：Unreal 的通用 `ImportanceSampleGGX` 虽直接消费 `E.y`，但 `ReflectionEnvironmentShaders.usf::FilterCS` 调用前明确执行 `E.y *= 0.995`，并使用 texel solid-angle `*2`、`D/4` PDF 与 roughness>0.99 cosine 分支；Zircon CPU/GPU 正确保留这些规则。真正缺陷位于 PDF 的 `distribution_ggx`：CPU/GPU 都把完整 `PI*d²` 地板到 `1e-6`。默认 128×8 PMREM 的 mip1 roughness 为 `0.0992126`，`NoH=1` 时 D 由 Unreal/稳定等价公式的 `3285.3633` 被压成 `96.8873`；32 个样本中 27 个受影响，source face 256 的 PDF source LOD 平均偏高 `1.3430` mip，中心样本偏高 `2.1676` mip。当前实现用正定等价式 `d=(1-NoH²)+NoH²*a²` 消除相消与完整分母地板，CPU/GPU 同步，样本数、FIS 规则、资源 ABI 和 dispatch 均不变。持久化 PMREM 内容因此推进 canonical recipe 与 viewer 当前证据门到 `2026_08_26_0008`；旧 `.zribl`/sidecar 仅可作为历史证据。静态红/绿契约、限定 `rustfmt` 与 diff integrity 通过，但未运行 Cargo/WGPU、PNG、RenderDoc 或功耗测量，故所有里程碑状态不变。

同日 Standard-PBR 材质输入域复审继续关闭了 F0 修复之外的漫反射缺口。材质采样仍保留 `texture × tint × vertex color` 原始值，避免改变 Unlit、Blinn-Phong、自定义模型和共享 GBuffer 语义；物理消费端则统一通过 `zr_pbr_base_color` 把 Standard-PBR RGB 限制到 `[0,1]`。Forward/basic、environment-only、fallback、deferred、baked lightmap、共享环境核心与 diffuse transmission 已覆盖，直接光循环只复用每像素 `diffuse_color`，没有新增逐光源钳位。以 normal incidence、`metallic=0`、`F0=0.04`、RGB=`[2,-0.5,0.5]` 为公式锚点，修复前漫反射能量乘积为 `[1.92,-0.48,0.48]`，修复后为 `[0.96,0,0.48]`。该正确性修复不增加 binding、材质/GBuffer ABI、feature bit、permutation、PSO identity 或 bake recipe；静态红/绿与源码闭包检查通过，但未运行 Cargo/WGPU、PNG、RenderDoc、GPU timing 或功耗采集，因此 M5/M6/M7 仍为 `in_progress`，M8 仍为 `implementation_complete_pending_managed_validation`。

同日对 direct/ambient/environment/lightmap 的完整能量分解复审推翻了此前 S3 的“按对应视角 Fresnel 扣除 diffuse”设计。修复前 direct 使用 `(1-F(VoH))*(1-metallic)`、environment 使用 `(1-F(NoV))*(1-metallic)`、baked lightmap 使用 `(1-F0)*(1-metallic)`，导致同一材质按光源类型拥有不同漫反射反照率，且 `VoH->0` 时直接光 diffuse 趋零。Unreal Default Lit 默认路径先形成 `DiffuseColor=BaseColor-BaseColor*Metallic` 并由各光源复用；其可选 energy-conservation 路径使用 `Roughness/NoV/F0` directional-albedo LUT/解析拟合，而不是逐光 `1-F(VoH)`；Bevy/Filament 系也复用 source-independent metallic diffuse color。Shader06 MVP 因此把 `zr_surface_metallic_diffuse_energy_scale` 唯一 owner 从通用 `zr_surface_types.wgsl` 硬迁移到 `zr_pbr_common.wgsl`，不保留旧别名，统一为 `clamp(base_color,0,1)*(1-clamp(metallic,0,1))`，并删除错误设计新增的重复 `zr_pbr_diffuse_energy_scale`；F0 只服务 specular，direct、ambient、environment 与 lightmap 共用同一材质分解。以 `F0=0.04, metallic=0` 为公式锚点，旧 baked scale=`0.96`、旧 environment 在 `NoV=0.5/0.1` 为 `0.93/约0.39313`、旧 direct 在 `VoH=0` 为 `0`，新 MVP scale 均为 `1.0`。因此前段 base-color clamp 的最终 MVP 锚点为 `[1,0,0.5]`；`[0.96,0,0.48]` 仅记录 clamp 完成但能量分解尚未更正的中间状态。Unreal 式 directional-albedo/multiple-scattering 补偿保留为需要当前 source RenderDoc/GPU timing/WPR/功耗证据的高级候选。本更正不增加 binding、ABI、feature bit、permutation、PSO identity 或 bake recipe；状态与托管验收门禁不变。

上述 diffuse decomposition 已完成 source 实施：主 shader 与独立 fallback/deferred 产品共 15 个 production 消费点统一复用 `zr_pbr_common.wgsl` 的 scalar owner，重复 helper、无消费者 `ZrPbrGgxTerms` 以及基础/各向异性 terms wrapper 已删除；Fresnel 只留在 specular、transmission 与 clearcoat。direct 的 `direct_diffuse_brdf` 在 light-grid 入口按像素准备一次再传入逐光 owner，Blinn-Phong 仍消费原始颜色。N 个遍历光源时，源码表达的 metallic clamp/subtract、RGB scale 与 `/PI` 从 N 次收敛为 1 次，减少 N-1 次；编译器最终指令、GPU 耗时与功耗尚未测量。静态主 source closure `6/6 red->green`、独立产品 closure `5/5 red->green`、GGX result 清理 `5/5 red->green`、逐光不变量 `4/4 red->green`、签名/legacy-model `8/8`、fallback/deferred 11 组关键 WGSL arity 与公共 owner `3/3`（唯一声明、精确公式、无 Fresnel 输入）检查通过，限定 Rust 格式和 diff integrity 通过；未运行 Cargo/Naga/WGPU、PNG、RenderDoc、GPU timing、WPR 或功耗采集，因此 M5/M6/M7 仍为 `in_progress`，M8 仍为 `implementation_complete_pending_managed_validation`。

同日继续完成 09C P0-5 的 raw shader identity 基础闭包。通用 WGSL/GLSL/SPIR-V importer 统一产出不参与材质变体的 `ShaderAssetKind::Module`；`.zshader v2` 的 Surface/Include/Compute/Fullscreen 产品域保持显式，builtin PBR 保持带 `standard_pbr` 的 Surface。readiness 以同一 canonical entry rows 做 O(E) kind contract，material 与 pipeline 诊断消费结构化 `kind_diagnostic`，material consumer 明确只接受 Surface。非 Surface 的缓存恢复不再生成或保留 material property layout、option table 与 generated WGSL，从而同时关闭新导入假 Surface、旧 raw Surface 假 ready 和 Module cache hydration 材质化三条入口。本切片不增加 Naga parse、binding、permutation、PSO、GPU submission 或成功路径字符串分配；静态闭包 `16/16`、限定 Rust 格式与 diff integrity 通过，cache 往返测试已实施但 Cargo/Windows 动态数据仍待协调器运行。M5/M6/M7/M8 状态及现有 realtime-SH9 failure 均不变。

同日继续完成 09C P0-2 的 generated-WGSL source-cache 身份子边界。prewarm source ID 由 WGSL hash、include hashes、template revision、Naga/WGPU version 决定，`source_label` 仅保留 provenance；manifest schema 推进到 v3。磁盘 schema v2 的 key/metadata 绑定完整 source ID、payload hash 与版本字段，写入前和解压后均重算 payload hash，runtime 因此删除 hit 后的全文 WGSL 比较；动态 builtin manifest 对同内容 source 做去重。该 cache lookup 仍发生在 WGSL assembly 与 validation queue 之后，也不复用 renderer device 的 module/PSO，所以它不是性能闭环，甚至可能增加 warm I/O。下一受管 profile 必须拆分 assembly/hash/meta read/read bytes/decompress/rehash/Naga/module/PSO 的 p50/p95/p99、RSS 与 1/100/10k variant 规模后，才决定保留、降级或由 compiled artifact/shared compile service 替换。静态契约 `17/17`、限定格式与 diff integrity 通过；没有运行 Cargo/WGPU、PNG、RenderDoc、WPR 或功耗采集。M5/M6/M7 继续 `in_progress`，M8 继续 `implementation_complete_pending_managed_validation`，realtime-SH9 failure 继续 `open`。

同一基础身份层还移除了 `packed_dims()` 的插件 geometry 碰撞：旧布局只保留 4-bit geometry，却公开完整 `u8` ID；current source 以 8-bit geometry、8-bit shading、4-bit pass、32-bit features、2-bit quality 组成 54-bit 内存键，`canonical_string_v1` 和磁盘 identity 不变。回归锁定 geometry=16 不再与 shading=1 冲突；未运行 Cargo，状态门禁不变。

同日继续关闭 09C P0-5 在离线预热工具中的绕过入口。`zircon_shader_prewarm` 不再把 standalone 或 single-unit `.zmeta` WGSL 提升为 Surface/完整材质 pass，而是保留 `Module + empty pass set`；source 枚举和 `.zmaterial` join 均以 material-variant kind predicate fail-closed，显式材质引用 Module 会返回结构化 `MaterialShaderKindMismatch`。资源 revision 导出仍保留，但裸 Module 的材质 prewarm source/variant 数为 0；原先每个错误 raw source 的 `6 x quality x geometry` 请求规模被消除。几何与 registry 测试改用显式 Surface package，raw/module 与 material-kind regressions 已实施。离线静态契约 `8/8`、限定格式和 diff integrity 通过；Cargo、Windows CLI、WGPU、1/100/10k timing/RSS、PNG、RenderDoc 与功耗仍待协调器托管，M5/M6/M7/M8 及 realtime-SH9 failure 状态均不变。

同日继续收敛 09C P0-5 的 Surface 源形态边界。资产层新增 typed `ShaderSurfaceSourceContract`：唯一、未与 executable entry 混用、接收一个 `ZrVertexOutput`（或公开 `ZrSurfaceInput` alias）且返回 `ZrSurfaceOutput` 的 `zr_material_surface` 为 `MaterialFunction`；精确 `vs_main` vertex + `fs_main` fragment pair 暂记为 `LegacyFullPass`；缺失、重复、签名不匹配、混用和非 canonical pair 均 fail-closed。分类使用注释感知、零分配单趟扫描，readiness 与 streaming 共用该 owner；streaming 发布枚举后，mesh pipeline source resolution 从逐请求 O(WGSL bytes) `str::contains` 改为 O(1) 枚举读取。builtin PBR 同步发布其实际 stage rows，历史 full-pass 行为未删除；三个错误使用不存在 `ZrMaterialSurface` 返回类型的 streaming 夹具已修正。该项仅为结构/算法规模结论，未测 CPU、GPU 或功耗；reflection stage IO、bind group/layout hash 与 legacy full-pass 的独立 kind/删除决策仍未完成，故 P0-5 和 M5/M6/M7/M8 状态不变。

同日继续完成 09C P0-5 的 compiler-reflection 基础切片。template validation 复用同一遍 Naga `Module + ModuleInfo`，发布 canonical entry stage IO、每 entry 可达资源、模块级合并 stage visibility、每-entry resource-layout hash 及模块 interface/resource-layout hash；没有第二次 parse/validate。资源 identity 覆盖 group/binding、address-space/access、memory decorations 与递归 type layout，buffer member name 参与 host/material ABI，resource variable name 仅作诊断而不参与身份；同 stage 多 entry 保持独立资源集，相同 binding location 的不同 ABI shape 不会静默折叠。新增工作界为 `O(entry points x globals + reflected type graph)`，type hash 每 Naga type memoize 一次，不向 frame thread 新增 Naga、I/O、WGPU 或 wait。Naga 29 的 workgroup override 与 override-sized type 现在发布 module override count、逐维 workgroup dependency 及 interface/resource specialization flags，这些 flags 进入 layout hash，禁止 raw Naga handle index 进入稳定身份；未来 exact `PipelineDescriptorId` 仍必须包含 selected constant values。当前 tracked WGSL `override` 为 0，139 个 WGPU `compilation_options` 行全部为 default，因此不新增当前 permutation/PSO 维度。静态源码闭包 `14/14`、specialization contract `10/10` 与限定格式检查通过，八项 Naga 单元契约已暂存但未在本地执行。异步 diagnostic worker 尚未成为 WGPU fail-closed admission gate；因此 P0-5、M5/M6/M7/M8 与 realtime-SH9 failure 状态均不变，Cargo/Naga、WGPU、profile、PNG、RenderDoc 和功耗仍由协调器托管。

后续 pipeline admission 复审已否决“只把 reflection 放进 diagnostic completion”的局部修补。Mesh runtime 允许 64 个 source-validation job 与 64 个 Base PSO job 分属两个无依赖 worker；同步 Base 以及 Depth/GBuffer/OIT/Shadow/Velocity/TAA 路径在 validation 完成前即可创建 WGPU 对象。离线 prewarm 虽按 source 去重做 Naga/WGPU validation，磁盘层仍只保存 compressed WGSL 与版本 metadata，runtime hit 仍重复排队 Naga 和创建 module/PSO。结构方向固定为单一 content-addressed `ValidatedShaderSourceArtifact -> specialized pipeline compile -> validated module/PSO publication` 服务，覆盖所有 pass；frame 只观察 pending/ready/failed 与 last-good/error policy，不新增 Naga、I/O 或隐式 wait。实施 worker/cache/default-async 变更前必须按 1/100/10k 请求采集 assembly、Naga/reflection、disk、WGPU module、PSO、queue wait 的 p50/p95/p99，duplicate validation/create count、queue outcome、in-flight bytes、peak RSS 与 Ready latency；静态审查不授权队列或缓存优化，里程碑状态不变。

同日 Surface 源形态完成硬切换，不再保留上一段记录的过渡 `LegacyFullPass`。仓库内 6 个 tracked Surface package 均已使用 canonical `zr_material_surface`；唯一旧 full-pass Surface 是 builtin PBR，而 mesh fallback 运行时本来就通过 fallback key 组装标准材质模板，并不消费该 raw full-pass。当前 readiness、streaming、prewarm 与 runtime assembly 因此只接受 `MaterialFunction`，带 executable entries 的完整 WGSL 必须归入 Module/Fullscreen 等 executable kind。builtin PBR 也改为无 entry rows 的最小 material function：拼接载荷由 24,220 bytes（includes 19,149 + inline 5,068）降为 225 bytes，减少 23,995 bytes / 99.07%；考虑 `source` 与 `wgsl_source` 两份持有，直接 resident string 减少约 47,990 bytes，尚未计 prepared/cache clone。以上是源码规模数据，不是 CPU/GPU/功耗收益结论。静态硬切换契约 `15/15`、限定 `rustfmt` 与 tracked diff integrity 通过；Cargo/Naga/WGPU、1/100/10k profile、PNG、RenderDoc、RSS、GPU timing 与功耗仍由协调器托管，P0-5 和 M5/M6/M7/M8 状态不变。

随后对手写 `ShaderAsset.pipeline_layout` 的 owner 调查确认它不能作为 compiler reflection 的对拍权威：`.zshader v2` 已禁止 authored `pipeline_layout`，当前 Surface package 均导入为空 DTO，material ABI diagnostic 又将空值视为 opt-out；生产代码仅缓存/报告该 DTO，没有用 `pipeline_layout_descriptor()` 构造 WGPU layout，字符串形式的 push-constant ranges 也没有运行时消费者。正确边界应是 specialized reflection 在统一 compile service 内对拍实际 pass pipeline-layout owner，然后才允许 WGPU module/PSO publication；shared layout 中 reflection 未引用的额外 binding 不应被误判。DTO 删除属于独立序列化 schema/cache 硬迁移，需先做迁移清单，本切片不擅自删除。里程碑与托管 profile/验收门保持不变。
- 同日完成 09C P0-7 的全 mesh pipeline admission 源码级前置实施：旧 `PipelinePlaceholderPolicy/SkipDraw` 与模糊 `Option` 返回被 typed `Ready/Deferred/Failed` 取代，queue/pending/saturation 保持可恢复，worker/job/source/variant/WGPU failure 为终止状态；原因变化重置 age。Opaque/transparent Base consumer 将 variant、canonical shader/material identity、entity、consumer、state/action/reason/age/occurrence 写入每帧最多 8 条的有界报告，重复上下文原地合并，ready path 不生成诊断字符串；容量已满后的新上下文只累计总数，在 canonical key/consumer/reason 分配前返回，已有上下文仍更新次数与最大 age。共享 source-cache selector 已由 `Option<String>` 硬切为 `String`，8 个 mesh pass 不再以 `?` 伪造 cache-source failure。失败/age 身份以 `(PipelineCreationTarget, MeshPipelineVariantId)` 为键，避免 OIT 复用透明 Base variant id 时互相污染；GBuffer、DepthPrepass、ShadowDepth/ShadowDepthAlphaMask、Velocity、TAA reactive/material mask 与 OIT consumer 均处理 typed admission，失败时记录各自 consumer / `RejectDraw` 并重置 replay state，不再使用生产帧路径 `expect`。Shadow/TAA 以 exact kind 隔离共享容器内的子域；TAA 重复创建流程已合并；OIT entry 缺失分类为 `oit_fragment_store_unavailable`，且仍在记录诊断后返回图执行错误。依据 Unreal material fallback proxy 规则，error PSO 不能脱离对应 material binding 单独替换，因此当前 terminal action 是可见 `RejectDraw`，不是伪造 error material；last-good generation、原子 error material+binding、view identity、required startup deadline 和统一 cross-pass artifact publication 仍开放。没有调整 queue/cache/worker 或 PBR/IBL 算法，没有运行 Cargo/WGPU/PNG/RenderDoc/profile/功耗，M5/M6/M7/M8 与 realtime-SH9 failure 状态不变；完整依据与量测门见 [`06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md`](06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md)。

- 2026-08-28 glTF normal-map 基础几何 owner 已完成源码级收敛，但尚未形成 managed/product 验收。builtin 与默认 priority-120 stable importer 共用 runtime indexed projection；缺失 normal 按 glTF flat policy 展开全部 base/morph/skin stream，缺失 tangent 由单一 MeshAsset MikkTSpace owner 按 normal texture 的有效 UV0/UV1 生成。Mikk 的 `(face, corner)` 输出不再 last-corner-wins，而是按内部已确定 tangent group 最小拆分 `V+S` render vertex；只有新增 `S` 行跨 base/morph attributes 追加，附加搬运为 `O((A+M)*S)`。morph target 从 absolute target position 重建 flat normal/Mikk tangent delta，不可表达的 handedness/corner group fail closed；VG page/ordinal 与 SDF hash 只在最终 split 后 cook，并保留带 skin owner 的 authored joint slots。Khronos `KHR_texture_transform` 仍只选择 tangent UV channel 并按 `translation*rotation*scale` 作用于 sampling UV，未擅自二次烘入 authored/generated TBN。限定 rustfmt/diff/metadata 通过，workspace/target 在 E 盘；ticket `6032100d054f4e05bc97b2ad75ec231e` 早于最终 cook-order，已失效。fresh managed Cargo、Khronos TextureTransformMultiTest normal-row 产品图、`docs/tests/runtime/shader` PNG、`D:\Tools\renderdoc`、cold/warm CPU、RSS/WPR/功耗均待完成，所以 M5/M6/M7/M8 与 realtime-SH9 failure 状态不变。

- 同日继续实施 09C P0-4 的 mesh 范围 cross-pass material publication。热更新材质的 revision、直接/传递 shader 身份、六项纹理依赖与 upload support、runtime key/readiness 以及 custom/standard uniform 现在作为一个 staged bundle；direct/compiled scene 在当前帧仍用完整旧 bundle 构造 draws，同时对当前 scene 实际 material/geometry/quality 和当前 graph executor 集合生成潜在 pass requirement。瞬时 culling 与 previous-velocity-history 不参与裁剪，避免隐藏对象恢复可见或下一帧刚产生 history 后才补 PSO；disabled pass、alpha/forward/transmission phase、shadow policy、geometry eligibility 和 graph feature 仍精确裁剪。Base/GBuffer/Depth/两类 Shadow/Velocity/两类 TAA/OIT 全部 Ready 后候选才一次发布并在下一帧可见；Deferred 保留旧代，terminal Failed 保留完整候选身份与结构化诊断，同身份后续命中不再重建两项 uniform。cold success 也不再绕过门槛：无 published generation 时 draw 成对使用 default pipeline、fallback bindings/textures 与洋红 error-proxy tint，真实 candidate 只在全 Ready 后发布。稳定帧由 active-candidate `HashSet::is_empty()` 同时短路 graph pass scan、draw census 与全 material 遍历；候选期间规模为 `O(P + D + R)`，发布顺序按 `ResourceId` 确定化。限定 `rustfmt`、源码契约与 scoped diff integrity 通过；两次托管 Windows 尝试分别在 `cargo.acquire` reconciliation timeout 与既有 D/E/F `unmanaged_artifacts_detected` 阶段停止，均未启动 Cargo，也未删除其它会话产物或绕过 validator。Cargo/WGPU、300-frame invalid/recovery、PNG、`D:\Tools\renderdoc`、GPU timing、RSS 和功耗仍未运行，统一 PipelineService 与 submission-ticket PSO retirement 仍开放，因此 M5/M6/M7/M8 状态不变。

- 同日 clearcoat normal 的独立材质合同完成源码实施。共享 glTF extension projection 被 builtin 与 priority-120 stable importer 共用，`clearcoatNormalTexture` 的 texture reference、`texCoord`/`KHR_texture_transform` 与 `scale` 贯穿 texture slot、asset、runtime、256 B uniform 和 Standard-PBR shader；base/coat 可使用不同 UV0/UV1 和 affine transform，但继续复用 glTF 唯一 vertex TBN。几何 admission 同时按 Khronos 边界收紧：两张法线贴图引用的 UV attribute 都必须存在；缺 authored tangent 时只允许 base normal 的有效 UV 作为 Mikk owner；clearcoat-only normal 没有 authored `NORMAL+TANGENT` 时直接 fail closed，不再发布默认 tangent。五个 UV selector 压成 6-bit mask，dielectric F0 从三个重复通道收敛为一个标量，释放的 8 个 scalar 容纳 clearcoat 元数据，所以 uniform 保持 256 B、bindless row 保持 288 B；相对直接增加两个 vec4 分别避免 +12.5% 与 +11.11% 常驻行开销。既有 clearcoat texture sample、binding 11/12 和 feature permutation 数均不增加；material rebuild 只新增一次 `sin_cos`。factor/roughness texture 仍未实现，optional extension 输出诊断，required `KHR_materials_clearcoat` 继续 fail closed。当前只有源码测试与静态合同，fresh managed Cargo/Naga/WGPU、Khronos 产品图、`docs/tests/runtime/shader` PNG、`D:\Tools\renderdoc`、GPU timing、RSS/WPR/功耗尚未完成，故 M5/M6/M7/M8 与 realtime-SH9 failure 状态不变。

- 同日对 clearcoat 分层语义完成规范级复审。当前用一次 `1 - clearcoat * Fresnel(NoV)` 衰减完整 base direct/indirect 的做法符合 Khronos 简化分层合同；Unreal/Bevy 对 base specular 使用额外 transmission 属于不同高级材质模型，不能作为无身份迁移的“优化”直接替换。完整 binding 追踪同时撤销“缺 coat normal 时继承 base mapped normal”的初步判断：clearcoat permutation 会无条件用 binding 11 覆盖该初值，ResourceStreamer 在 descriptor 无贴图时绑定 `[128,128,255]` normal fallback，因此视觉上近似几何法线而非 base map。真实结构缺口是无贴图 clearcoat 仍承担一次 texture sample、UV transform 与 TBN rebuild，并带入 UNORM flat-normal 的微小 XY 量化；Bevy 用独立 `STANDARD_MATERIAL_CLEARCOAT_NORMAL_MAP` specialization，Godot 则直接传 vertex normal。是否新增 presence identity 与静态几何法线路由必须先对有/无 coat normal 的代表语料 profile，不能未经测量增加 permutation。clearcoat emission 缺口现已完成源码修复：advanced Standard-PBR 将 `surface.emissive` 复用既有 `clearcoat_base_energy`，精确实现 Khronos `emission * (1 - clearcoat * Fresnel(NoV))`；basic/environment-only 无 clearcoat 路由不变，unlit early return 不受 coating。合同锁定 `0.96/0/0.98` 三个数值锚点；热路径仅新增一次 emission scale，不新增分支、sample、binding、permutation、helper 或 Fresnel 计算。fresh managed Naga/WGPU、产品图、RenderDoc 与功耗仍待完成，因此里程碑状态保持不变，也不声明产品或性能验收。

- 同日完成 runtime-owned normal convention 与 Mikk shader 逆变换的源码级原子修复。引擎 cook canonical 硬切为 right-handed/GL：decoded DX 只在 import/build 阶段翻 green，GL 与 glTF normal 保持像素，compressed DX fail closed，BC5 只接收 canonical GL；shared WGSL normal ABI 删除 convention 参数/常量/分支。Standard base/clearcoat 与 fallback 按 Bevy/Mikk 合同保留插值 N/T、以 `B=cross(N,T)*w` 构造 frame，仅归一化最终 mapped normal；anisotropy 仍使用独立正交 frame。静态 hot-path 从每 normal sample 1 个 convention conditional 降为 0；Standard Mikk frame 删除 3 次预归一化/Gram-Schmidt，fallback 删除 geometric/tangent/bitangent 与 tangent-normal 预归一化，但这些只是源码操作计数，不是 GPU timing/功耗结论。asymmetric DX/GL、compressed payload、generated WGSL 与 fallback source guards 已覆盖；decoded build、builtin/stable glTF、builtin image 和 stable texture importer 版本同步失效旧 artifact。限定 rustfmt、diff integrity、生产 shader contract 与 locked metadata 通过；fresh managed Cargo/Naga/WGPU、PNG、`D:\Tools\renderdoc`、timing/RSS/WPR/功耗仍待完成，M5/M6/M7/M8 与 realtime-SH9 failure 状态不变。

- 同日继续把 glTF texture-subasset 基础设施从 builtin/stable 双实现收敛到 runtime 单一 owner。BasisU admission、core/`EXT_texture_webp` source 选择、decoded image 长度校验与 RGBA8 展开、颜色空间/用途 variant 发布以及 glTF sampler 映射现在只有一套生产定义；builtin decode/label path 直接调用它，priority-120 stable importer 仅 re-export，并继续拥有自身 material/mesh/scene 装配。生产 authority 从三处 partial/full owner 收敛为一处完整 owner。该变更保留独立 `TextureAsset` 所需的当前像素 clone 行为，import 规模仍为 `O(T + Q)`，其中 `T` 为 texture 数、`Q` 为全部发布 variant 的像素总量；runtime sample、binding、shader permutation、Naga/WGPU 与 frame scan 增量均为 0。未取得 cold/warm CPU/RSS profile 前不实施共享 decoded payload 或 copy-on-write，也不宣称性能收益。限定 rustfmt、diff integrity 与 workspace metadata 已通过；fresh managed compile/product-route tests、PNG、RenderDoc 与功耗 gate 仍开放，M5/M6/M7/M8 和 realtime-SH9 failure 状态不变。

- 同日继续关闭 glTF 单 mip 基础缺口。共享 texture-subasset owner 现在通过 runtime 无状态 decoded-RGBA8 build v2 生成完整 offline mip chain：upload shape、payload 长度、compression/runtime-mip 能力均 fail closed；sRGB 在线性域过滤，normal 先 canonicalize 为 GL 再逐级重归一化；packed level-major 输出只扩展一个最终 buffer，不再为每级创建 pixel `Vec`。显式 glTF `minFilter=NEAREST/LINEAR` 按规范保持 base-only，只有 mipmap filter 与未指定的 engine default 构建完整链，避免生成未消费数据和改变采样语义。算法保持 `O(P)`，persistent output 为 `O(P)`、filter scratch 为 `O(W+H)`，不新增 runtime sample/binding/permutation。public admission 已收紧到 single-layer D2；2D array、cube 与 cube array 在专用 array mip/cube seam owner 和产品图像 oracle 出现前 fail closed。builtin/stable glTF importer version 包含 build v2，builtin image 与 stable texture importer 也提升版本，旧单 mip/错误 convention artifact 必须失效。当前只完成 source/static closure；fresh managed compile、产品像素、`docs/tests/runtime/shader` PNG、`D:\Tools\renderdoc`、cold/warm CPU/RSS、WPR 与功耗仍待完成，M5/M6/M7/M8 及 realtime-SH9 failure 状态不变。

- 同日按 `docs/plans/optimize/zircon_runtime/09c-material-shader-pipeline-pso-review.md` 的 canonical P1-10 对 material binding 做 E3 级结构复审。当前每个未被 compiled static command cache 完整命中的 residual draw 都执行 6 次 sampler-variant preparation，并创建 custom/standard 两个 13-entry WGPU bind group，即静态计数为 `6R`、`2R` 与 `26R`；material-property override 还逐 draw 创建一个 256 B logical uniform buffer。direct scene 与 hit-proxy 无 command cache，多 camera 会继续放大 `R`。Bevy 在 material prepare 阶段发布 `MaterialBindingId`，Unreal 在 `FMaterialRenderProxy` 上维护可失效/异步重建的 uniform-expression cache；Zircon 已有的 `PreparedMaterialBundle` 也具备 `draw_generation`、完整纹理与双 uniform，故目标 owner 是 device/anisotropy/resolved-resource-qualified 的 generation binding bundle，而不是 draw 或无界 pointer cache。实施前必须先加入 bounded profiler scopes/counters，并以 1/100/10,000 draws、共享/独立 material、override、direct/compiled/hit-proxy、多 camera、streaming/reload/device-change 矩阵采集 300-frame warm p50/p95/p99、create/query counts、RSS、GPU timing 与 WPR/WPA 功耗；优化后的 unchanged warm path须为 0 sampler query、0 uniform create、0 bind-group create。KHR clearcoat factor/roughness 两张独立纹理若直接加入固定 ABI，会使每组 13→17 entries、当前 residual projection `26R→34R`（+30.77%），因此在 prepared-binding owner 与实测基线验收前不得扩展这四个 binding；这不阻塞 MVP base PBR、clearcoat emissive 衰减、normal convention 与 decoded-D2 正确性修复。完整报告见 [`06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md`](06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md)。当前 low E: workspace capacity 与缺少 exact passed current-source ticket 仍阻止受管 before/after profile；immutable Session scope 下新增 shared ABI 路径必须走 audited ownership transfer，故 M5/M6/M7/M8 与 realtime-SH9 failure 状态不变。

- 同日完成 material binding 的历史 RenderDoc 诊断基线与 production profiler slice，但没有开始缓存优化。2026-08-23 real-HDRI export capture 的 963 actions 中有 208 copy、54 clear，294 个 GPU duration sample 合计 5.630272 ms；top-25 copy 已占 3.945536 ms（至少 70.08%），且包含 48 次 PMREM readback destination 与 74 个 WGPU internal clear，故它是 export/acceptance frame 而非 steady game frame。final-SH9 capture 的 200 个 sample 合计 2.322656 ms，其中单次最终 `CopyImageToBuffer` 为 1.17 ms；top-25 compute 为 0.296960 ms、draw 为 0.404320 ms。两份 Vulkan 历史 capture 都是 diagnostic only，不能冒充 current-source/DX12/功耗/优化后证据。residual draw builder 现以单次聚合 counter 发布 `R`、`6R` sampler query、`2R` bind-group create、`26R` entry projection 与 `O` override uniform create，并用一个 `material/binding.build_residual_draws` CPU span 覆盖构造阶段；10,000 draw 的单元合同固定为 60,000/20,000/260,000，不改变 shader、binding layout 或输出。下一步必须先通过协调器生成 current-source profiling build 并跑既定矩阵，再决定三代 bundle 内有界 cache；M5/M6/M7/M8 与 realtime-SH9 failure 状态不变。

- 同日对 Standard-PBR core Fresnel 与 transmission/clearcoat 组合完成结构复审，暂未把局部乘法当作修复提交。Basic/Advanced Forward、Deferred 与 fallback 当前都以 `diffuse*(1-metallic)+GGX(F)` 合成直射，缺少 glTF 核心 `diffuse*(1-F)`；Advanced 又把 ambient、base specular、clearcoat 和 environment 先聚合，再整体乘 `1-specular_transmission`，错误削弱反射高光与 coat 顶层，同时让透射绕过 coat、缺少 baseColor 染色，并把 custom diffuse transmission 与 specular transmission 相加。规范与 Unreal/Bevy 参考共同要求先建立共享分量 owner：公共 GGX 一次返回 specular BRDF 与已计算 Fresnel，四条渲染路径原子应用同一 diffuse complement；Advanced 仅在末端按 `coat * (Rs + (1-t)*mix(Rd,Td,d) + (1-metallic)*t*Ts + emission) + clearcoat` 合成，environment 保持 diffuse/specular 分量，scene-copy 缺失时不得再把已着色反射 aggregate 当透射背景。当前 transmission scene copy 只有单 mip，本轮 MVP 只授权能量、染色与层所有权修复，不宣称 rough microfacet BTDF；不新增 binding、uniform、PSO key 或 permutation。完整算法、复杂度预算与 managed/profile/PNG/RenderDoc/功耗门见 [`06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md`](06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md)，M5/M6/M7/M8 与 realtime-SH9 failure 状态保持不变。

- 2026-08-29 已完成被拒 M6 的 transmission-frame successor 源码修复，但尚未通过精确验收。screen scene-copy 与 environment fallback 共用 Snell exit/direction/frame：thin thickness=0 保持直穿，volume thickness>0 才产生宏观折射位移；透射基色使用 canonical F0、entry `N dot V` 的 `1-F` 与 metallic complement，反射 specular 不再被透射因子削弱。scene-copy 可用性改由 group1 binding38 的 16 B generation uniform 表达，合法黑色/alpha-zero 像素不再被误判，命中 copy 时不执行 environment fallback；1080p 全覆盖下旧路径的静态上限是每帧 2,073,600 次多余 fallback query，但尚无 current-source GPU/功耗量测，不能宣称实际收益。六份 Forward/deferred/fallback view-direction 算法已硬切为 `zr_pbr_common.wgsl` 单一 owner，保留透视/正交 endpoint early return；Forward lightmap 与 shading 是否进一步做到每像素只归一化一次，须先看编译器输出，避免给 unlit 或 custom shading ABI 引入无证据成本。managed lib-test job `ab2519bf94ac43eea728f9e8223fe03f` 在 foreign `zircon_runtime_interface` 漂移期间以 425 个 Rust 错误终止且未执行目标测试；production retry request `40ca2a3a16dc409493f6578bf9f81725` 又因未知 `D:\cargo-targets\zircon-engine\python-temp` 被 coordinator 以 `unmanaged_artifacts_detected` 拒绝，未启动 Cargo。当前仅有唯一 owner/零 legacy call 的源码合同、精确 rustfmt 与 scoped diff integrity；fresh Cargo/Naga/WGPU、advanced transmission 产品 PNG、`docs/tests/runtime/shader`、`D:\Tools\renderdoc`、GPU p50/p95/p99、RSS/WPR/WPA 与独立复审仍开放，因此 M5/M6/M7/M8、realtime-SH9 Failure、manifest 释放和 coordinator milestone commit 均不前移。详细记录见 [`06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md`](06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md)。

- 同日完成 double-sided surface-frame 与 mirrored-raster 的源码闭环。更深的 WGPU/Unreal 复审推翻了片元阶段再次读取 GPUScene flags 并 XOR 的初案：pending instance 现在只复用或计算一次既有 normal-transform flags，`PipelineKey::reverse_raster_winding` 仅进入 draw/batch/command-cache/PSO identity、不进入 `ShaderVariantKey`，八类 mesh raster pass 统一以 CW/CCW 选择把 `front_facing` 逻辑化；formal Forward/deferred/environment-only/debug GBuffer、OIT、hit-proxy-alpha 与 standalone fallback 再在完整 normal-map surface 后翻转 base/clearcoat normal 和 anisotropy bitangent。一面镜像实例因此保留 back-face early reject，片元侧不新增 GPUScene 读取、binding、sample 或 shader permutation；相同材质在镜像/非镜像同时存在时，必要的 runtime PSO 基数上界为 2 倍。缺失 owner 已通过协调器 transfer preview `6f46e6902b2045219503d2038bc46d24`（fingerprint `902678591581b39a97bdd127b8f06a8ab248192529ded4375b41640727b94b3f`）和 apply `661c86e1bcf54afb9271d22c2ef3c348` 审计转入 Shader06。当前仅完成 exact-file `rustfmt --check` 与 scoped source/whitespace contract（最终结果见同一 architecture audit）；fresh Cargo/Naga/WGPU、DX12 PNG/RenderDoc、PSO/draw counter、耗时/RSS 与 WPR/WPA 功耗证据仍开放，不宣称性能收益。M5/M6/M7/M8、realtime-SH9 Failure、manifest 与提交状态不变。

- 同日继续完成 volume-thickness 空间与吸收 owner 的源码修复。glTF `thicknessFactor` 现在通过已有 flat `instance_index` 读取 GPUScene `world_from_local`，按 Khronos sample-renderer 的三列基向量长度把 mesh-space thickness 投影为 world-space transmission ray；同一 ray/length 同时驱动 screen exit、environment fallback direction 与 Beer-Lambert，因此 scale-2 实例的折射位移和光程均为 2 倍，非均匀缩放保持 rotation-independent 近似。`attenuationColor=0` 的合法通道不再被 epsilon 抬高：零光程显式返回 1，正光程严格得到 0。体积 attenuation 也从 specular scene helper 提升为单一 owner，diffuse BTDF、反向环境漫透射与 specular transmission 都消费该结果；diffuse 的 indirect 项只在 lobe 活跃时从 `-N` 查询现有 SH9/irradiance-cube owner，并与无 occlusion 的 scene ambient 合成，不触发 reflection-probe selection、PMREM specular 或 BRDF LUT。两种 transmission 在现有材质模型中互斥，所以每个活跃像素最多计算 1 次 frame 与 1 次三通道 `pow`，clustered light loop 内只增加 attenuation 乘法；薄壁 early return 为 0 次 `pow`、0 次 GPUScene 矩阵读取、0 次 `refract`、0 次 basis length。该修复不新增 binding、varying、material ABI、PSO key 或 permutation；`rustfmt`、scoped diff integrity、唯一 owner/旧公式静态检查已通过，fresh managed Cargo/Naga/WGPU、缩放玻璃与黑色吸收产品图、`docs/tests/runtime/shader` PNG、`D:\Tools\renderdoc`、GPU p50/p95/p99、RSS/WPR/WPA 与独立复审仍开放，因此 M5/M6/M7/M8、realtime-SH9 Failure、manifest 释放和 milestone commit 状态不变。详细决策见 [`06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md`](06/2026-08-25-pbr-ibl-preoptimization-architecture-audit.md)。

- 同日继续关闭 screen-space transmission 的可见域合同。旧 helper 对 `w<=0` 与 NDC 越界出口仍生成并钳位 UV，使镜头后方或屏幕外的折射错误读取中心/边缘 scene copy；当前投影返回显式 `{uv, valid}`，要求正 `clip.w`、XY 位于视口且 Z 位于 WGPU 深度范围。只有合法投影才读取 scene copy 并立即返回，因此该命中路径不执行 environment query；不可用或无效投影从同一 transmission frame 的 `exit_position` 与 `environment_direction` 回退，不再用 entry position 选择 probe。活跃路径最多增加一次布尔边界判定，不新增 texture sample、binding、varying、material ABI、PSO key 或 permutation；源码合同拒绝旧 `safe_w`/UV clamp 路径，`rustfmt` 与 scoped diff integrity 通过。scene depth/rough-refraction depth rejection 仍属于后续高级能力，不能在缺少 depth owner、代表场景 profile 与 RenderDoc 证据时扩张 MVP binding。fresh managed Cargo/Naga/WGPU、产品 PNG、`D:\Tools\renderdoc`、GPU timing 与功耗证据仍开放，里程碑、Failure、manifest 和提交状态不变。

- 同日继续修复 anisotropic environment reflection 的模块断层。此前 anisotropy 只进入 clustered direct-light GGX，global PMREM 与 local reflection probe 仍按 isotropic `reflect(-V,N)` 取样；当前 Advanced Standard-PBR 按 Khronos 单样本近似生成显式 bent specular normal，environment-components owner 只在 provider active 后从该 normal 生成 dominant direction。diffuse irradiance、原始 `N dot V`、Fresnel/BRDF LUT、AO、planar precedence 与 clearcoat 仍由原 normal 拥有，退化 tangent frame 回退原 normal；anisotropic 活跃像素新增两次 cross、tangent/bent-normal normalization 与标量 bend mix，但仍为一次 provider sample，不新增 binding、uniform、varying、PSO key 或 permutation。审查同时记录 direct anisotropy 当前每灯重复 tangent 旋转/归一化的 `O(L)` 性能风险；在 1/8/32-light shader disassembly、GPU p50/p95/p99 与功耗 before profile 前不实施 once-per-pixel frame 优化。当前仅有源码/数值合同，fresh managed Naga/WGPU、anisotropy HDRI 产品图、RenderDoc 和性能证据仍开放，里程碑、Failure、manifest 与提交状态不变。

- 同日收敛 formal Forward、deferred、clearcoat 与 reduced environment-only 共用的 rough-IBL dominant direction。旧实现对全部 roughness 都直接使用 perfect-mirror `reflect(-V,N)`，使高粗糙度掠射 PMREM/probe lobe 的中心落到切平面下方；当前 environment-core 对 source-cubemap、realtime-IBL PMREM 与 local probe 统一采用 Khronos/Unreal 对齐的 `normalize(mix(reflect(-V,N),N,roughness^2))`，并保留 roughness 0 的精确镜面端点与 roughness 1 的法线端点。首次实现复审还发现，无 PMREM 的 procedural-sky fallback 被过早弯折会违反本计划的单次 perfect-reflection 降级合同；最终 radiance API 因此分离 sky/probe direction，同一像素只计算一次 perfect reflection，仅在 PMREM/probe 可消费时增加 dominant mix/normalize，probe 与 procedural sky 混合时也各取正确方向。planar hit 与无 global/probe provider 的早退都发生在该 ALU 之前；anisotropic 路径只传 bent normal，不能在调用端提前生成方向。活跃中间粗糙度 PMREM/probe 像素增加一次 vector mix 与 normalization，procedural-only fallback 不增加 normalization；texture sample、binding、uniform、varying、PSO key 和 permutation 数均不增加。源码顺序、provider-specific direction、唯一 direct-reflect owner、端点数值、`rustfmt` 与 scoped diff integrity 作为静态合同保留。fresh managed Naga/WGPU、shader disassembly、代表场景 GPU p50/p95/p99、RSS/WPR/WPA、产品 PNG 与 RenderDoc replay 仍开放，因此该结果不前移里程碑、Failure、manifest 或 coordinator commit。

- 同日完成 direct anisotropic GGX 的整体公式复审与源码修复。旧 Advanced lobe 以 `base_alpha*(1+strength)` / `base_alpha*(1-strength)` 构造两轴并把 `sqrt(alpha_t*alpha_b)` 送入各向同性 Smith visibility，既不符合 Khronos `KHR_materials_anisotropy`，也不等价于 Unreal `D_GGXaniso` / `Vis_SmithJointAniso`。当前 strength-axis 改为 `mix(material_alpha,1,strength^2)`，垂直轴保持 `material_alpha`；NDF 使用不压平低粗糙度峰值的 Burley 稳定式，visibility 消费 view/light 在 tangent/bitangent 上的四个方向投影。perceptual roughness 0.5、strength 1、`H=N` 的旧/新 NDF 峰值约为 `255.93/1.27`，旧值放大约 `201.01x`；anisotropic 的最小 alpha 也从私有 `0.002` 收敛到共享 isotropic owner 的 `0.001`，避免零粗糙度 lobe 在 strength 跨过 epsilon 时突变。strength 0 保持精确 isotropic endpoint，零 strength 与退化 tangent frame 都直接回退共享 isotropic owner，`V+L=0` 只对精确零 NDF 向量返回零。新 visibility 与旧 exact scalar helper 都执行两次 vector length/sqrt，但增加方向 dot/multiply；不新增 sample、binding、uniform、varying、PSO key、permutation 或循环阶数。把 tangent rotation/frame 从每灯 `O(L)` 提升为 once-per-pixel 仍需 1/8/32-light disassembly、GPU p50/p95/p99 与功耗 before profile 后再做。当前公式/唯一 owner/旧公式拒绝、数值 endpoint、`rustfmt` 和 scoped diff integrity 已形成静态合同；fresh managed Naga/WGPU、anisotropic 产品图、RenderDoc 与量化性能/功耗证据仍开放，里程碑、Failure、manifest 和 coordinator commit 不前移。

- 同日关闭 volume 默认无界衰减距离与精确黑色 attenuation color 的交叉边界。旧 CPU sentinel 为 `f32::MAX`，一单位世界光程产生约 `2.938736e-39` 的正指数；非零颜色近似 1，但黑色通道执行 `0^positive` 得到 0，违反 glTF 缺省无限 `attenuationDistance` 应不衰减的语义。现有 `STANDARD_PBR_NO_ATTENUATION_DISTANCE` 收敛为可序列化/GPU 安全的 `1.0e30`，CPU normalization 同时把旧 `f32::MAX`/更大有限值 canonicalize 到该值，避免材质与 cache identity 分裂；WGSL 由 `zr_surface_types.wgsl` 唯一声明同名 sentinel，默认 surface 与生成 Standard-PBR projection 共同消费。Beer-Lambert helper 对零光程或 `distance >= sentinel` 在三通道 `pow` 前返回 1，`>=` 也防御尚未重建的旧 GPU payload，有限正距离仍保持黑色通道严格吸收。该修复不新增 uniform slot、binding、sample、feature bit、permutation 或循环，默认 volume 路径增加一次标量比较并静态省去三次 `pow`；这不是 GPU/功耗实测收益。红/绿源码合同、数值边界、旧身份收敛、唯一 owner、`rustfmt` 与 scoped diff integrity 已通过，fresh managed Cargo/Naga/WGPU、产品 PNG、`D:\Tools\renderdoc`、GPU p50/p95/p99、RSS/WPR/WPA 仍开放，因此 M5/M6/M7/M8、realtime-SH9 Failure、manifest 与 coordinator commit 状态不变。

- 同日完成 required glTF 材质扩展的 fail-closed 基础设施收口。builtin 与 priority-120 stable importer 的 allowlist 此前允许 `KHR_materials_transmission`、`KHR_materials_volume`、`KHR_materials_ior` 出现在 `extensionsRequired`，但 `transmissionTexture`、`thicknessTexture` 与 `ior=0` compatibility mode 只产生诊断后继续使用 fallback，导致“必须支持”的资产静默改变材质。现在 runtime 唯一 preflight owner 在 buffer/image decode 前消费原始 required set；仅当相应扩展 required 时，以 `O(M)` 常数探针扫描材质，命中上述三类未实现语义即返回含 extension field/material index 的 typed parse error。factor-only transmission/volume 与普通 `ior>=1` 继续接受，optional unsupported field 保持既有显式诊断；该切片仍保守地整体拒绝 required clearcoat，后续 field-aware 复审再处理其两张缺失纹理。不新增解码、材质 ABI、binding、sample、feature bit、permutation 或帧内工作。两个 importer 入口均加入三类拒绝测试，stable 入口另锁定 factor-only 正向 admission；静态红/绿、唯一 owner/双调用、`rustfmt` 与 scoped diff integrity 通过，但 fresh managed Cargo、产品截图、RenderDoc 与性能/功耗证据未执行，M5/M6/M7/M8、Failure、manifest 与提交状态不变。

- 同日补齐 `KHR_materials_anisotropy` 的 factor-only 资产入口。此前强度/旋转已由 `StandardPbrMaterialFeatures`、GPU uniform、feature bit、direct GGX 与 environment reflection 全链路拥有，但共享 glTF projector 完全忽略该扩展，使资产静默退化为 isotropic。当前唯一 runtime owner 按 Khronos 默认值投影 `[0,1]` strength 与有限弧度 rotation；空扩展保持 strength 0，不激活 Forward feature。builtin 与 stable required allowlist 只在共享 preflight 能拒绝 `anisotropyTexture` 后开放该扩展，因此 required factor-only 可导入，required texture 在外部 image decode 前 fail closed，optional texture 保留显式诊断。实现不新增 WGSL、material ABI、binding、sample、uniform slot、PSO key 或 permutation，导入扫描仍为 `O(M)` 常数探针；双入口红/绿源码合同、默认/author 值、optional 诊断、required 拒绝、`rustfmt` 与 scoped diff integrity 已通过。fresh managed Cargo/Naga/WGPU、anisotropy HDRI 产品图、`D:\Tools\renderdoc`、GPU timing/RSS/功耗仍开放，故里程碑、Failure、manifest 与提交状态不变。

- 同日把 required clearcoat 从 blanket rejection 收敛为逐字段 admission。Khronos/Unreal 复审确认 factor、roughness、normal texture 是独立输入；Zircon 已拥有 scalar 默认/投影、linear normal slot、UV/`KHR_texture_transform`/scale、几何 UV/tangent 校验与 Standard-PBR coat 层，唯一缺口是 `clearcoatTexture.r` 与 `clearcoatRoughnessTexture.g`。当前双 importer allowlist 已开放 `KHR_materials_clearcoat`，runtime preflight 的固定字段表在 required 资产使用任一缺失纹理时于 image decode 前返回精确 `extension.field` error；factor-only 与已有 normal-texture 子集可导入，optional 两纹理继续产生显式诊断。导入仍为 `O(M)` 常数探针，不新增 shader ALU、sample、binding、uniform、ABI、PSO key 或 permutation。双入口 positive/negative 红绿合同、唯一 guard 表、normal owner 保留、`rustfmt` 与 diff integrity 通过；managed Cargo/Naga/WGPU、clearcoat 产品 PNG/RenderDoc/GPU/RSS/功耗仍未完成，里程碑、Failure、manifest 与提交不变。同期复审拒绝把仅有的 `diffuse_transmission` 标量冒充 `KHR_materials_diffuse_transmission` 支持：该扩展仍为 Release Candidate 且要求独立 color factor 与两张纹理，当前 ABI 不完整，required 继续 fail closed。

- 同日关闭 optional 高级 glTF 材质扩展的静默降级。Khronos 允许非 required 材质扩展回退 core PBR，但此前只有 `KHR_materials_specular` 产生明确 diagnostic；dispersion、iridescence、sheen、Release-Candidate diffuse transmission、Initial-Draft subsurface 与 archived specular-glossiness 均无提示丢失。共享 projector 现在以一个固定六项注册表为两条 importer 路径生成精确扩展名与 core-PBR fallback 诊断，只有实际命中才分配字符串；required 政策仍由双 allowlist 独立 fail closed，六项均未进入 allowlist。导入规模保持 `O(M)` 的六个常数探针，不新增材质字段、texture slot、WGSL、binding、sample、feature、PSO key 或 permutation。双入口红绿合同、唯一注册表/模板、allowlist 零命中、`rustfmt` 与 diff integrity 已通过；managed Cargo 与产品证据仍开放，里程碑、Failure、manifest 与提交状态不变。

- 同日按 `docs/plans/optimize/zircon_runtime/09f1-environment-sky-ibl-reflection-probe-review.md` 的 P0-7 重审 reflection-probe layer/selection 边界。当前 CPU 仅按 camera layer 筛选并按距离/priority 截到 64 个 probe；GPU `misc.w` 虽保存 `RenderLayerSet` 的 lossy scene-v1 低 32 位，WGSL 完全不读，`zr_environment_select_probes` 仍按 fragment 线性扫描全部已上传 probe。对象侧完整 layer 是 growable `Vec<u64>`，而 GPUScene primitive/instance ABI 与 fragment environment API 都没有 receiver mask；因此直接借用 primitive padding 或读取 `misc.w` 会丢弃 32 以上 layer，并让 Forward/Deferred/particle/capture 继续各自为政，属于假修复。结构性方向保持 visibility-owned persistent `ReflectionProbeSpatialAssignment`：在完整 object/camera/capture/reflection mask 交集后生成 bounded local candidate list，Forward 与 Deferred 共享 offset/count/index ABI，再在局部列表内执行现有 top-two/priority/blend/box projection。该优化必须先取得同场景 1/8/32/64 probe GPU、fragment/list visits、occupancy/overflow、upload、VRAM/RSS 与功耗 before 数据；本轮只完成源码取证和报告，不修改 shader 热路径，不前移 M5-M8、Failure、manifest 或 coordinator commit。

- 同日完成 `KHR_materials_specular` 的结构性 admission 复审。Khronos 的 factor 同时改变 dielectric F0/F90，color 只改变 F0 且允许大于 1，metal 分支不受影响，diffuse 能量需按 RGB Fresnel 最大分量回收；现有 ABI 只有 IOR 推导的 achromatic F0，shared GGX 先混合 metal F0、固定白 F90 并使用逐通道 complement，Deferred 还固定 `vec3(0.04)`。因此不能把 factor/color 任一项映射成 IOR 或当前 F0 而宣称支持；required 继续 fail closed，optional 保持显式 diagnostic。完整实现需先建立 Forward factor/color/F90 ABI、dielectric/metal Fresnel 分离、direct/IBL/transmission 共享 max-Fresnel energy owner，再处理两张纹理与 GBuffer。当前只记录架构与 Khronos SpecularTest/IOR/transmission 验收矩阵，不改生产 shader/ABI，不前移里程碑。

- 同日确定 reflection-probe before-profile 的最小遥测切片。现有 prepare 已生成但丢弃 `ReflectionProbeUploadReport`；目标是沿既有 `RenderStats` 输出 extracted、camera candidate、attempted、active/resident、capacity dropped、new upload count/bytes 与 rejection，并以 saturating `u64` 暴露 `render pixels * active probes` 的明确全屏访问上界。该值不是实际 shaded-fragment 或 GPU work 计数；实现不得增加 shader atomic、GPU buffer/readback、binding、sample、PSO key、permutation 或新分配。后续只有把这份 workload context 与现有 scene-pass GPU timestamp、1/8/32/64 probe 同场景、RenderDoc、RSS/VRAM 与功耗结合后，才允许进入 local-list 算法优化。

- 同日完成上述 reflection-probe workload 遥测的源码实施。`RenderReflectionProbeWorkloadReport` 现由探针 prepare 的既有控制流填充并在提交后投影到 `RenderStats`：camera-layer candidate 只统计 layer 相交，attempted 只统计进入 asset/slot 解析的 eligible 候选，capacity dropped 由具备 baked cubemap 且强度有效的候选数减 attempted 得到，因此资产失败触发的 overflow replacement 不会被误记为容量截断；upload bytes 是本帧新排入队列的 PMREM payload 字节，不代表 GPU completion。全分辨率访问上界使用 `u64` 饱和乘法。热路径只增加常数计数、一次 subtraction 和一次 stats copy，不增加 allocation、shader atomic、GPU buffer/readback、binding、sample、PSO key 或 permutation。测试先行的源码红态确认 5 项合同均缺失，资源级测试锁定精确 payload bytes 与 invalid-nearest/healthy-overflow 计数，既有 WGPU 产品测试也从公共 `query_stats` 锁定 enabled 2-probe、feature-off 2-probe 与 sky-only 三帧不会残留历史值；实施后的静态链检查、精确 `rustfmt` 与 scoped `git diff --check` 已通过。managed Cargo/WGPU、1/8/32/64 probe GPU timing、RenderDoc、RSS/VRAM 与功耗仍开放，故 M5-M8、Failure、manifest 与 coordinator commit 状态不变。

- 同一产品合同新增 `ignored` 的 1080p reflection-probe before-profile 入口。它固定 framework、viewport、camera、mesh、material、quality profile 与一份 shared PMREM，只把 active probe 数切为 1/8/32/64；每档先预热 16 帧，再按 `frame_generation` 归属并去重收集 120 份异步 `RenderFrameProfile`，同时保存全部 pass 记录和 GPU frame p50/p95/p99。定时样本完成后才允许通过 `ZR_RENDERDOC_CAPTURE_REFLECTION_PROBE_64=1` 请求最终 64-probe 帧 capture，避免 RenderDoc 污染样本。JSON 与 PNG 固定输出到 `docs/tests/runtime/shader/runtime_environment_reflection_probe_linear_scan_before_profile_20260829.json` 和 `...linear_scan_64_20260829.png`，不使用 C 盘。该入口仅已完成源码设计、`rustfmt` 与 diff 检查，尚未由受管 current-source 二进制执行，不能登记为 before 数据或视觉验收。

- 同日完成 P0-6 reflection-probe 冷驻留调用链取证。slot miss 会从 probe `prepare` 同步进入 `load_texture_asset -> load_typed -> ensure_resident`；资源尚未位于 `ResourceManager` 时，后者在当前渲染调用线程执行 project artifact `read()`，随后才克隆 texture payload 并排入 PMREM upload。resident slot hit 不触发此路径，而 1/8/32/64 稳态 fixture 共享并预热一份 PMREM，因此不能用它证明冷 I/O 延迟。结构性方向必须由 Runtime09D 提供异步 residency/prefetch 与 ready-revision admission，Shader06 只消费 ready snapshot 并保留 last-good/environment fallback；在独立 process-cold profile 量化 artifact read/decode、首帧 CPU、upload bytes 与 time-to-first-resident 前不实施猜测性缓存或迁移。本轮仅更新架构审计，没有生产代码或性能收益声明，里程碑、Failure、manifest 与 coordinator commit 状态不变。

- 同日受管 Windows locked development gate 已在 `D:/cargo-targets/zircon-engine/pool/f9fef644bf8e441a49ad1c139495499657f126cd246ffca80d13868db535561d` 执行 `zircon_runtime` build 与聚焦 probe product target，但 library 在测试启动前以 32 个诊断终止。2 个 Shader06 诊断同属新 workload report 未从 render facade 重导出，现已补入既有 `backend_types` 公共列表，并通过精确 `rustfmt --check` 与 scoped diff integrity；其余 30 个为当前共享工作树的 `zr_contracts::random` manifest wiring、text/font、post-process、UI、GPUScene、material streamer 与 platform host 外部错误。由于已知快照仍不可编译，本轮未重复提交 Cargo。WGPU 产品合同、ignored 1080p profile、PNG、RenderDoc 与功耗验证均未执行，M5-M8、Failure、manifest 与 coordinator commit 状态不变。

- 同日补齐 P0-6 冷加载量化字段。workload report 的 `asset_load_call_count` 只在 probe cubemap revision 未命中 slot 并进入 `load_texture_asset` 时增加，`asset_load_cpu_time_us` 只包围该同步调用，因而包含潜在 `ensure_resident` artifact read 与 texture clone；load failure 同样计时，registry revision 缺失不冒充 load。resident hit、feature-off 与 sky-only 路径均不读取时钟。每个 cold slot miss 仅新增两次 CPU clock read，不新增 allocation、GPU query、shader ALU、binding、sample、PSO 或 permutation。资源合同覆盖 first/reuse/disabled、rejected 与 overflow，公共产品合同把成功 load count 与 scheduled upload 对齐并要求 no-call frame timing 为零，manual JSON 保留两字段。当前只有静态/源码合同，没有非零冷延迟样本或收益结论，里程碑、Failure、manifest 与提交状态不变。

- 同日按当前 RHI 重新量化 P0-6 upload：旧报告的 48 次 face/mip 写已不再成立，probe 现在共享一份 immutable payload，每个 mip 以六个 array layer 合并成一条 `WgpuTextureUpload`，并与 frame buffer setup 共用一个逻辑 resource-upload ticket；但 production flush 仍逐条执行每 cubemap 8 次 `queue.write_texture`，并非 staging-buffer copy encoder。workload 新增 `scheduled_texture_write_count`，合同锁定成功单 PMREM 为 8、resident/disabled/rejected/capacity-dropped 为 0，manual JSON 同步输出。复用 staging arena 与 copy command 的结构性迁移归 09A/09D，需先取得 cold upload CPU/GPU/submission 数据；Shader06 不跨 owner 重写后端，也不宣称性能收益。

- 同日完成 probe resource-upload transaction 生命周期复审。direct/compiled 两条路径都先接受合并 resource-upload ticket，只有 scene command buffers 成功取得 submission ticket 后才调用 `commit_pending_uploads` 把 slot 标为 Ready；upload enqueue、receipt 或 scene submit 失败均不会误发布驻留，下一帧 `prepare` 会清除 stale pending、推进 epoch 并重新排入 PMREM。为避免性能报告把 prepare 阶段请求误称为 GPU completion，公共与内部字段在进入基线前统一改为 `scheduled_cubemap_upload_count/bytes`，`scheduled_texture_write_count` 也仅表示 probe owner 生成的 RHI 写记录。新增资源合同显式丢弃首批 pending 并要求下一 epoch 再排 1 个 cubemap/8 个 mip writes，既有 direct/compiled source-order 合同保持 submit 后 commit。该更名和合同不增加帧内算法工作；“resource upload 已被 queue 接受但后续 scene submit 失败”可能产生的重复带宽需由 09A/09D failure-injection profile 量化后再决定事务重构，当前不宣称收益，M5-M8、Failure、manifest 与提交状态不变。

- 2026-08-29 增加 environment-capture recorder 的 CPU 计划与 WGPU 编码合同。`EnvironmentCaptureRenderPlan` 固定六个 `CubemapFace` 顺序，把每个 face 映射到唯一 mip0 array layer 与独立 scene-uniform slot，并锁定 capture view 的 winding reversal 与 opaque-only profile；它复用 `EnvironmentCaptureGpuTargetPlan` 的完整 RGBA16F mip-chain/单深度预算，确保工作项数量、目标容量和六面录制不会各自漂移。`EnvironmentCaptureWgpuRecorder` 在一次 command build 后，按六面复用 opaque、alpha-mask、advanced-PBR-opaque 三条流，只替换 face attachment、scene bind group 与 camera；sky pass 负责每面 clear，mesh pass 只做 load/store。capture 专用 builder 每个 mesh batch 只运行一次 `OpaqueBasePassProcessor`，相对 generic builder 的 depth/shadow/opaque/transparent/velocity/TAA 六次 processor visits，把 CPU 计划规模从 `6M` 收敛到 `M`，并从命令列表中排除 transparency/transmission。静态合同覆盖 face/layer/slot 一一对应、1024-face `71,303,152 B` 上限、`6 == RENDER_ENVIRONMENT_CAPTURE_WORK_ITEM_COUNT`、单次 command build 与三条允许流；renderer-owned upload/submission、accepted progress、source-mip/PMREM/SH9 handoff、产品 PNG、RenderDoc 和性能/功耗证据仍待完成，M5-M8、Failure、manifest 与 coordinator commit 状态不变。

- 同日补充 capture scene packet 的语义边界。`EnvironmentCaptureSceneBatch` 只消费 `SceneViewportRenderPacket` 已表达的网格、光源、环境与相机数据；清除 overlays 和 virtual-geometry debug 是 capture MVP 的固定过滤，不代表 recorder 已经覆盖动画、粒子、sprite 或其它未进入该 packet 的 sideband。六面循环不得为这些 sideband 各自重新抽取或在缺失时静默伪造；若产品要求完整场景反射，必须先扩展共享 packet/extract owner，并为该扩展单独建立 capture 与普通 viewport 的一致性合同，再接入同一六面 GPU submission。当前 batch 因此只承诺 `O(M + 6)` CPU 计划规模（一次 `M` mesh census 加六个 camera/uniform 选择），不宣称完整场景渲染或视觉验收通过。

- 2026-08-29 C2/C3 非验收生产链继续实现到 GPU 过滤与有界 last-good 工件。renderer 在同一 graphics command buffer 中顺序记录六个 RGBA16F mip0 raster pass、完整 HDR source mip chain、8 个 GGX PMREM mip 和 1 个 SH9 dispatch；capture direct-light owner 现一次打包光源并为六个视图生成 immutable light grids，共享一份 CPU payload、18 个 upload ranges，空光源路径保持零 grid allocation/upload；mesh processor census 保持 `M`，必要 draw replay 保持 `6K`。capture scene uniform 通过当前 496-byte ABI 中原有的保留 lane 启用 full-roughness surface policy，Forward base/clearcoat 与 standalone fallback 共用同一材质边界，不增加 shader permutation 或 PSO identity；六面 immutable payload 随 P1-18 同步为 2,976 B。由于 source radiance 已改变而 PMREM/SH 数学未变，runtime cache key 独立哈希 capture-raster algorithm version `2026_08_31_0001`，只失效旧场景捕获，不推进 canonical IBL v8 或误伤 imported HDRI。128-face 的源码计划为 `6 raster + 7 source-mip + 8 PMREM + 1 SH9`，即 6 个 raster pass 和 16 个 compute dispatch；1024-face 为 6 个 raster pass 和 19 个 compute dispatch。质量档只缩放 canonical PMREM sample budget：Fast=`0.5x`（下限 16）、Normal=`1x`、High=`2x`，不复制第二套 kernel。peak GPU allocation 已由仅 source/depth 的旧 C2 数值更新为 source+PMREM+SH9+depth：128=`2,162,800 B`，1024=`72,351,856 B`；两个 backend ticket 都完成后立即释放 source/depth scratch，每项 resident PMREM+SH9=`1,048,704 B`，64 项 owner 上界=`67,117,056 B`。completion 只观察 renderer 唯一 poll 已发布的 ticket 状态，不新增 `device.poll`/wait；取消或 supersede 不发布，失败保留 last-good。scheduler 在 accepted submission 后发布 `Filtering 6/6`，仅当前代 GPU 完成才进入 terminal success。typed request 现已携带生产 probe residency 所需的 `ResourceId`/numeric probe target identity，并由插件的 placement 绑定；capture output 仍只在 ticket 完成后发布到 probe array，不以字符串解析或位置猜测提前激活。该身份合同、直接 probe-array copy/slot commit、可选 C4 async persistence、managed Cargo/Naga/WGPU、DX12 PNG、`D:\Tools\renderdoc`、GPU timing、RSS/VRAM、WPR/WPA 功耗仍开放；上述 pass/byte 均为源码计划量，不是性能实测，M5-M8、realtime-SH9 Failure、manifest 与 coordinator commit 不前移。

- 2026-08-31 scene-capture 证据与 Base-profile 状态复审：现有 PBR viewer v16 只覆盖 imported-HDRI 的 `metal-mirror`/`dielectric-ior`，1/8/32/64 probe ignored 产品入口只测已驻留 shared PMREM 的采样扩展性；两者都不执行本轮 scene-capture raster，因此不能证明 capture direct-light、full-roughness 或 raster-key 失效，历史 PNG/RDC 也不升级为 current evidence。lit capture 在构建一次性 generic Forward command set 前会关闭 environment-only Base profile；此前没有像普通 viewport owner 一样清除旧 profile 的 cached mesh commands，后续帧可能在 registry 已切换后复用旧 command stream。当前只在 enabled-to-disabled 转换时执行一次 cache invalidation，不增加逐面或逐帧工作。精确 RED/GREEN source guard、focused `rustfmt --check` 与 scoped diff integrity 通过；managed Cargo/WGPU、fresh scene-capture PNG、`D:\Tools\renderdoc` replay、GPU timing、RSS/VRAM 与 WPR/WPA 功耗仍开放，M5-M8、manifest 归属与 coordinator commit 状态不变。

- 2026-08-31 capture request identity 复审关闭 signed-zero 漂移：scheduler duplicate 判定使用 request 的浮点数值相等，而 runtime bake key 哈希 position bit；旧实现会把 `-0.0`/`+0.0` 当成同一 live request，却生成不同 cache key。构造边界现将所有有限零分量规范化为 `+0.0`，非零有限值保留原 bit、非有限值继续拒绝。回归锁定 request equality、bake-key equality 与 canonical bit；这是常数级 admission 工作，不增加帧、draw、dispatch、binding 或 allocation。focused `rustfmt --check`、source RED/GREEN guard 与 scoped diff integrity 通过；受管 runtime 验收状态不变。

- 2026-08-31 environment-capture 优化前测量接线：recorder/filter 早已计算但未外发的 command/filter report 现在在成功 capture 事件发布 17 个 profiler counter，覆盖 face pass、单次 command build、每面及三类 command、draw/state/bind skip、source-mip/PMREM/SH9 dispatch、source-mip 与 IBL 参数 buffer/bind-group 创建数，以及已有的 source-mip binding 创建微秒。源码门禁确认未新增 `Instant`、GPU query/readback、遍历、allocation 或 `device.poll`；这是后续关联 GPU timestamp、RenderDoc、RSS/VRAM 与 WPR/WPA 的测量基础设施，不是性能收益数据。RED/GREEN source guard、focused `rustfmt --check`、17/17 counter inventory 与 scoped diff integrity 通过；在 current-source profile 前不改 command/binding/filter 算法，M5-M8 状态不变。

- 2026-08-31 typed probe target success 语义改为 fail-closed，并取代 2026-08-29 “capacity pressure 可跳过 publication”的旧表述。旧 `Option::and_then` 会把 target resource revision 缺失或 `CapturePending` slot reservation 拒绝静默折叠为 `None`，随后 scheduler 仍可能发布 `Succeeded`，但没有任何 probe-array copy。当前显式 target 必须同时取得 revision 与 reservation，否则在 resource-upload/graphics submit 前失败；无 target 的通用 resident-output capture 保持不变。后续 admission-order 复审又将无副作用的 revision 解析前移到 provider 扩容和 capture-target 分配之前：环境专用 renderer 的无效 target 因而不会先创建静态纹理布局下界 `78,292,648 B` 的 full probe/planar provider，也不会创建 face-128/1024 分别 `2,162,800 B`/`72,351,856 B` 的 capture target。可变 reservation 仍留在后续 fallible 编码之后，避免在缺少统一 rollback guard 时扩散事务错误路径。以上均为源码容量模型，不是 RSS/VRAM 或性能实测；managed WGPU、产品截图、RenderDoc、GPU timing 与功耗仍待验证。

- 2026-08-29 typed target boundary review: reflection-probe placement now forwards its numeric `probe_id` together with a stable `ResourceId` derived from the persisted PMREM URI through the capture request/output identity. The direct cube-array publication experiment was removed after review because it mutated slot state before copy completion and could race ordinary frame prepare; the accepted path publishes only bounded filtered GPU output. Probe-array copy/slot commit remains a separate completion-ticket milestone and must not be reintroduced as a same-call `submit -> commit -> activate` shortcut. No string parsing, position matching, or early live-target activation is permitted.
- 2026-08-29 completed the ticket-owned probe-array copy boundary. A typed target resolves the registry revision, reserves an LRU slot as `CapturePending`, records all eight PMREM mip copies into the same capture encoder, and carries the reservation with the source submission. The slot becomes `Ready` only after both upload/capture tickets complete and the scheduler accepts the current generation; cancellation, supersession, and submission failure release the reservation without publishing it as Ready, while explicit target-revision lookup or reservation-admission failure terminates before submission. Ordinary viewport prepare never treats a pending capture without matching last-good content as valid; a same-revision pending capture may continue queue-ordered sampling of its existing Ready array content. This is still source-contract complete only: managed Cargo/WGPU, product PNG, RenderDoc, GPU timing and power evidence remain open.
- 2026-08-29 tightened capture-slot rollback semantics. A first capture with no prior content is unavailable to viewport sampling while `CapturePending`; a same-revision capture may continue serving its existing Ready array contents in queue order. Cancelling a same-revision reservation restores `Ready`, while cancelling a revision change or a new slot leaves `Pending(0)` so the next prepare must reload before publication. Duplicate reservations for one active cubemap fail closed. This preserves last-good data without mislabeling old GPU bytes as the requested revision; it is covered by focused allocator contracts only, with managed runtime validation still open.
- 2026-08-29 closed the environment-only placeholder boundary for capture publication. The deferred local-provider constructor owns a `1x1x1` placeholder array, which cannot receive the fixed 128-face PMREM chain. A typed capture target therefore expands the provider before reservation/copy; the placeholder invariant requires no local-provider upload to be in flight, so the upgrade cannot discard an accepted upload or race a copy. Generic environment-PBR selection remains sticky after expansion. This is a resource-size/order contract only; managed GPU validation and screenshots remain open.
- 2026-08-30 C4 async-persistence review now closes the runtime-cache subphase while retaining the editor asset boundary. The neutral `RenderEnvironmentCaptureRequest` and `RenderEnvironmentCaptureOutputIdentity` carry an optional validated persistence output URI plus an explicit `IblBakeArtifactRequest`; the reflection-probe plugin forwards its existing `output_uri`, while an artifact request must be supplied by the asset/editor owner rather than inferred from the capture snapshot key. The plugin request now accepts an optional explicit source hash and constructs that canonical artifact request only when the owner provides the hash; an absent hash preserves the non-persistent capture path. Request admission rejects source face/mip mismatches, non-canonical PMREM layout, empty content masks, and unsupported IEM content before a queue can reserve GPU readback. For an admitted request, the capture source reads PMREM/SH9 directly from its filtered target, encodes those copies into the capture command buffer's product-diagnostic tail, submits one shared ticket, and commits the existing bounded writeback queue only after submission. The normal completion poll assembles the sections and writes the runtime cache; duplicate requests and the four-entry cap remain fail-closed. Preparation, encoding, submission, completion readback, and runtime-cache write failures abandon only the optional writeback and never invalidate the visible capture or publish a partial artifact. The URI remains a destination locator and is intentionally excluded from `ibl_bake_key`; editor asset-derived staging/output-URI publication remains pending. Managed Cargo/WGPU, product PNG, RenderDoc, timing, RSS/VRAM, WPR/WPA power evidence, and M5-M8 status are unchanged.
- 2026-08-31 C3/C4 current-state reconciliation: the open list at the end of the 2026-08-29 C2/C3 historical slice is superseded by the ticket-owned probe-array copy in the 2026-08-29 follow-up and the bounded persistence/runtime-cache writeback in the 2026-08-30 follow-up. Current source carries the typed target reservation through the submission ticket, copies all eight PMREM mips in the capture encoder, commits the slot only after accepted terminal completion, and keeps persistence in the same `EnvironmentCaptureSubmission::{Capturing, Persisting}` owner. C3 is therefore `typed_target_copy_and_atomic_slot_commit_implemented_managed_gpu_validation_pending`; C4 runtime writeback is `bounded_runtime_cache_writeback_implemented_editor_staging_and_bulk_profile_pending`. Remaining gates are managed Cargo/Naga/WGPU, PNG/RenderDoc, failure injection, timing/RSS/VRAM/power, and the separately owned editor background-job/undo/bulk-data boundary; neither direct copy nor runtime-cache writeback should be reimplemented.

- 2026-08-30 camera/probe layer ABI repair: the existing 16-byte reflection-probe header now carries the selected camera's scene-schema-v1 mask in its former reserved word, while `GpuReflectionProbe.misc.w` retains the probe mask. WGSL rejects non-intersecting probes after the intensity gate and before influence weighting; the legacy header constructor defaults to `u32::MAX` to preserve all-layer visibility for unmigrated callers. This adds no binding, allocation, shader permutation, sample, or PSO key and is not the M9 spatial-assignment optimization: object receiver-mask input and bounded local-list publication remain owned by the 09B/09C follow-up. Focused source contracts, rustfmt, scoped diff integrity, and 18 Python contracts pass; managed WGPU/Cargo, current-source screenshots, RenderDoc, timing, RSS/VRAM, and power evidence remain pending.

- 2026-08-30 reflection-probe runtime-cache consumer boundary: the runtime plugin now exposes `register_captured_reflection_probe_from_runtime_cache`, which requires the request's explicit source hash, resolves only a current `RendererGpuRuntime` artifact through `IblBakeArtifactCacheStore`, and reuses the existing PMREM-to-TextureAsset registration owner in memory. Missing identity, cache miss, stale/rejected blob, or invalid PMREM fails closed. The path never creates or overwrites an asset-derived `.zcube`; editor scene/catalog/undo publication remains a separate pending owner. Focused Rust source contracts, rustfmt and scoped diff checks pass; managed Cargo/WGPU, current-source PNG, RenderDoc, timing, RSS/VRAM and power evidence remain open.

- 2026-08-30 shader/IBL cost model and M9 gate review: fixed PMREM/SH9 capture costs, the current global probe-selection upper bound (`132,710,400` candidate visits at 1080p and `530,841,600` at 4K), and CPU candidate preparation complexity are now recorded in 09F1 with CMFT/Unreal capture-time filtering references. No PMREM sample-budget or probe-list algorithm change is authorized without same-scene GPU timestamps, visit proxy, list overflow, CPU p50/p95/p99, RSS/VRAM and WPR/WPA data; M9 remains `implementation_status: pending`. This is an architecture/measurement update only, so managed Cargo/WGPU, `docs/tests/runtime/shader` PNG, RenderDoc, timing and power evidence remain open.

- 2026-08-30 historical visual spot-check: `zircon_shader_pbr_viewer_m5_current_source_20260727_dx12.png` shows a nonblank HDRI reflection sphere with coherent sky/ground reflection, and `runtime_shader_pbr_real_hdri_lakes_2k_8x8_cmft_pmrem_reflection_20260710.png` shows a nonblank roughness matrix with visible PMREM variation. Both artifacts predate this source slice and are retained as visual sanity evidence only; they are not current-source acceptance screenshots.
- 2026-08-30 diffuse-energy owner re-alignment: current WGSL had drifted back to a view-Fresnel helper for standard/environment diffuse and baked lightmap energy. Forward, Deferred, environment-only, fallback-mesh, and lightmap paths now share `zr_surface_metallic_diffuse_energy_scale` (`1 - clamp(metallic, 0, 1)`); the Fresnel complement is retained only as the explicitly named `zr_pbr_transmission_energy_scale`. This removes one ordinary diffuse Schlick evaluation and its vector arithmetic without adding a sample, binding, permutation, or PSO key. Static source contracts are updated; managed Naga/WGPU, current-source PNG/RenderDoc, timing, RSS/VRAM, and power evidence remain open.
- 2026-08-30 managed Cargo recheck: `cargo test --manifest-path zircon_runtime/Cargo.toml --lib graphics::shader::template --no-default-features --offline --jobs 1` reached `zircon_runtime` compilation but failed before test execution with 235 pre-existing workspace errors, including unresolved `zr_contracts` manifest wiring and missing mirrored source paths. This is external to the shader owner slice; no Cargo pass or product acceptance is claimed, and all build output stayed under `E:\Git\ZirconEngine\target\shader06-contract-check`.
- 2026-08-30 fullscreen parameter ABI review: `FullscreenPassPlan::write_parameter_bytes()` now serializes each existing 32-bit word with explicit little-endian bytes before uniform-buffer upload; source-contract coverage rejects host-endian `to_ne_bytes()`. This changes no allocation, capacity, binding, sample, branch, loop, permutation, Naga/WGPU module, or PSO identity. Scoped rustfmt/static checks pass; managed shader assembly, product PNG/RenderDoc, timing, RSS/VRAM, and power evidence remain pending.
- 2026-08-30 screenshot audit: `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_ambientcg_metal009_texture_maps_20260823_renderdoc_r2.png` is nonblank and shows a visible HDRI lake reflection on the metallic sphere, but it predates the current source snapshot. It remains historical visual context only; a fresh managed Windows/WGPU capture is required before M6-M8 acceptance.
- 2026-08-30 compute indirect fixture audit: the three test writes that construct the WebGPU 12-byte indirect-dispatch argument buffer now use `u32::to_le_bytes()` as well. This keeps the test oracle aligned with the fixed GPU ABI; it adds no production dispatch work or runtime cost.
- 2026-08-30 SH9 format audit: cmft's legacy SH9 order/sign convention differs from Zircon's canonical Y-up order, but the current CPU projection, `.zribl` payload codec, and WGSL reconstruction all share Zircon's same nine-term basis. External cmft DDS/KTX inputs remain source-cubemap mip data only and are not accepted as direct Zircon PMREM/SH9 artifacts, so no coefficient swizzle is introduced without an explicit artifact-format contract.
- 2026-08-30 SH9 producer parity audit: `ibl_irradiance_sh.wgsl` uses the same basis terms, solid-angle normalization, and cosine-lobe scales as the CPU `irradiance_sh9` owner; the existing GPU parity contract remains the authoritative runtime check. `cargo metadata --locked --no-deps` also parses the current workspace with all target output under `E:\Git\ZirconEngine\target\shader06-metadata`; this is manifest/graph evidence only, not a compile or product acceptance result.
- 2026-08-30 C4 persistence identity hard-cut: the capture request now keeps the asset/editor-supplied `persistence_artifact_request` separate from `runtime_cache_artifact_request`. Renderer GPU readback/writeback derives the latter from `RenderEnvironmentCaptureRequest::ibl_bake_key()`, so capture id, position, clip range, face size, Fast/Normal/High quality, and scene/environment/output generations participate in the runtime-cache key; it no longer writes GPU bytes under the external source hash. Successful output retains both identities, while the reflection-probe runtime-cache consumer requires explicit persistence intent but resolves only the renderer-owned request. This changes no dispatch, readback section, binding, sample, PSO, permutation, or GPU allocation. `rustfmt --check`, scoped diff integrity, the identity source contract, and the existing environment/RenderDoc Python matrix (`18/18`) pass. Editor asset-derived publication remains pending because the bounded readback currently contains PMREM/SH9/IEM but not the source cubemap needed for a truthful `.zcube`, and no catalog/undo owner is wired; managed Cargo/WGPU, fresh PNG/RenderDoc, timing, RSS/VRAM, and power evidence remain open.
- 2026-08-30 C4 source-publication foundation now separates `.zcube` source data from filtered IBL data. `texture_asset_from_source_cubemap_zcube_mips` accepts only a validated complete source face/mip pyramid, while `texture_asset_from_source_cubemap_zcube_rgba16f_mips` accepts the same canonical face-major RGBA16F bytes produced by GPU readback; both reject incomplete layouts, and the byte path does not expand the source to `f32x4` or quantize it a second time. The first readback draft's one-callback-per-face/mip shape was rejected during budget review: 1024 needs 66 requests before PMREM/SH9, exceeding the RHI default `64` requests/frame, while one full packed request would exceed the `16 MiB` request and `32 MiB` frame limits. The replacement packs all mips of one face into one buffer and permits only one budget-derived batch in flight. At 1024 one padded face is `11,190,016 B`, the default batch is two faces / `22,380,032 B`, and six face requests complete over three diagnostic frames; each callback strips row padding directly into the single `67,108,848 B` canonical result and releases that face delivery. The canonical result plus the largest current callback batch is therefore `89,488,880 B`, not the former `134,217,696 B` f32 result plus a complete byte chain; GPU/native staging still requires later measured accounting. The batch API remains intentionally unwired from capture submission: current runtime GPU dispatch, copy/readback bandwidth, allocation, binding, sample, PSO and permutation deltas are all zero. Ten focused Rust regressions now cover byte equivalence, invalid layouts, padding removal, 1024 budget batching and in-flight serialization, but the known 235-error workspace blocker prevents executing them; scoped formatting, diff integrity and static source/behavior guards are the current evidence. Scheduler admission/cancel/supersede, editor output-URI/catalog/undo publication, current-source PNG/RenderDoc and measured CPU/GPU/RSS/VRAM/power acceptance remain pending.
- 2026-08-30 C4 bounded persistence wiring supersedes the preceding unwired status. The framework now retains one typed `EnvironmentCaptureSubmission::{Capturing, Persisting}` owner: capture completion starts source readback only when the request carries a persistence output URI, submits at most one budget-derived face batch per successful render pump, never calls `device.poll` or waits for a ticket, and keeps the probe-array reservation plus last-good resident output unpublished until the scheduler accepts the terminal generation. Cancel, supersede, diagnostic admission failure, submission failure, invalid payload, and payload-mailbox rejection all release the reservation without replacing last-good. Plain scheduler success now rejects persistence work with `PersistenceSourcePayloadRequired`; the only accepted path consumes one `RenderEnvironmentCaptureSourcePayload`, so a successful persisted request cannot lose its source bytes silently.
- 2026-08-30 C4 project source transaction foundation is also wired. Reflection-probe runtime consumes the scheduler payload, encodes canonical RGBA16F bytes into `.zcube` without a `f32x4` chain, and the editor publication adapter passes the owned byte vector to `ProjectAssetManager::publish_generated_project_source`. The project owner prepares source, sidecar, any importer-produced artifacts, registry/resource mutation and watcher echo as one existing durable transaction; it does not use direct `fs::write`. A follow-up memory review found the targeted importer still cloned the generated source snapshot once. `take_source_bytes_for_import` now moves that buffer into `AssetImportContext` and later into `PreparedFileWrite`, eliminating one persistent `67,108,864 B` `.zcube` clone at 1024. The logical live source plus largest callback batch remains `89,488,880 B` before encoding; the encoded container is `67,108,864 B`. Allocator reallocation during the 16-byte header insertion, importer-private temporaries, native staging, RSS and VRAM remain unmeasured and are not claimed as peak-memory results.
- 2026-08-30 editor undo boundary review: the reflection-probe editor crate currently exposes capture submit/poll/cancel/take and project publication, but it has no registered `OperationCommandFactory` or background job owner. `EditorTransactionEngine` replays synchronous `EditCommand::apply/revert` against `EditContext`, while `ProjectAssetManager` owns the durable source/catalog/resource transaction outside that context. A correct undo cannot retain only the receipt's previous hash, and copying the previous 64 MiB source into every history record would be the wrong memory architecture. The remaining design is a background capture/publication job plus a project-owned content-addressed reversible source-swap token that `EditCommand` can apply/revert through the same durable transaction. Until that owner exists, editor undo, retry after post-take publication failure, scene dirty/placement publication, current-source PNG/RenderDoc, and measured CPU/GPU/RSS/VRAM/power acceptance remain pending; no M6-M9 acceptance or performance gain is claimed.
- 2026-08-30 C4 `.zcube` importer hard cut: end-to-end review found that project `.zcube` was previously owned only by `texture_importer.cubemap`, which treated every file as a TOML six-file/cross/equirectangular manifest. A captured `ZRZCUBE1` binary source would therefore fail before the project transaction could commit. The existing importer is now the single extension owner for both forms: it probes the eight-byte magic before UTF-8/TOML parsing, validates/adopts the binary header plus complete RGBA16F payload without `f32x4` expansion or ordinary texture mip/transcode work, and preserves all manifest layouts. Only the cubemap importer recipe advances from version 2 to 3 so cached artifacts are invalidated without churning unrelated texture importers. The focused binary/manifest Rust contracts are written but not executed because the same 235-error workspace blocker remains; static branch-order/export/format/diff checks pass.
- 2026-08-30 C4 bulk-data cost correction: zero-copy decode does not mean zero-copy project publication. With current `Vec<u8>` contracts, binary import clones the `67,108,864 B` encoded source into `TextureAsset`, and `ArtifactCacheAsset::from_imported` clones it again while bincode/zstd streams the cache artifact. Together with the source transaction buffer, the preparation stage can therefore own three equal payloads, `201,326,592 B`, before serializer/compressor overhead; after artifact preparation, source write plus live ready payload still retain `134,217,728 B`. These are source-derived logical byte counts, not measured RSS. No further optimization is authorized until an E-drive ETW/WPA allocation/I/O profile identifies the dominant phase. The reviewed structural target follows Unreal `FEditorBulkData`: immutable shared bulk payload/content identity, asynchronous/lazy retrieval and unload, plus a project transaction that can consume shared or already-staged bulk data and issue the reversible token needed by editor undo. Current-source Cargo/WGPU/PNG/RenderDoc, timing, RSS/VRAM and power remain pending.
- 2026-08-30 C4 cold/cook boundary correction: accepting binary `.zcube` closes source/catalog/resource publication only. The existing external-cubemap IBL staging path recognizes DDS/KTX, while the captured binary importer deliberately returns a source-only `TexturePayload::Container`; it does not automatically produce project asset-derived PMREM/SH9/IEM. The active renderer still publishes and consumes its separately keyed runtime-cache PMREM/SH9 result, so live-session last-good behavior remains independent of source publication. Cold restart, cook/package rebuild, cache eviction recovery and source-to-derived recipe ownership remain pending and must not be inferred from the successful binary import contract.
- 2026-08-30 C4 cold/cook source-to-derived owner now supersedes the preceding pending status. Project scan/import calls one `prepare_source_cubemap_texture` owner after texture import: a header-only classifier accepts either captured `ZRZCUBE1` or the existing linear-float DDS/KTX inputs, derives the same content/layout request identity, reuses a current `.zcube`/asset-derived `.zribl` pair without pixel decode or PMREM work, and on a miss decodes the captured source mips and rebuilds PMREM/SH9 through the existing asset-importer CPU recipe. The old `stage_external_source_cubemap_texture` entry remains compatible; the implementation module hard-cuts from `external_source` to `source_cubemap_texture` so the generic owner is explicit. Prepared cache files remain part of the existing project durable transaction, so cold restart/cache eviction/cook have a reproducible asset-derived source instead of depending only on renderer runtime cache. The focused classifier/project-owner Rust contracts are written but cannot execute under the known 235-error workspace blocker; no Cargo or product acceptance is claimed.
- 2026-08-30 C4 cold/cook memory gate: the new miss path is functionally necessary but not accepted as performant. At 1024, project context source, imported source-only `TextureAsset`, re-encoded cache `.zcube`, and decoded `f32x4` source alone total `3 * 67,108,864 + 134,217,696 = 335,544,288 B`; this excludes PMREM (`2,097,120 B` as an in-memory default 128-face f32 chain), encoded `.zribl`, artifact-cache serialization, allocator growth and native staging. A warm pair still hashes the source bytes but returns before f32 decode/PMREM reconstruction. E-drive ETW/WPA profiling must decide between immutable shared bulk ownership, referencing the durable project `.zcube` instead of duplicating it in cache, and moving cache-miss bake/publication into the background job; no memory, latency or power improvement is claimed.
- 2026-08-30 C4 durable recovery identity guard: the project recovery policy previously resolved each journal target into `ResolvedProjectPathIdentity` but passed the original raw path to `IblSourceCubemapStagingStore::validate_bundle_target`, whose checks are lexical and do not canonicalize aliases. It now validates `target.operation_path()` while keeping project path identity out of the asset-artifact layer. The updated project IBL transaction contracts pass `4/4`; the combined IBL/importer ownership matrix passes `21/21`.
- 2026-08-30 editor background-job admission gate: the encoded 1024 captured `.zcube` is exactly `67,108,864 B`, which consumes `100%` of Editor09's default `64 MiB` pending byte budget before the publication job's URI, access handle, channel and control allocation are counted. Reporting only `Vec::len()` would under-account retained memory, while raising the global queue limit or bypassing admission without a measured workload would weaken a shared editor invariant. The background job and operation command therefore remain pending until the payload can be handed off through a shared/staged bulk owner or an evidence-backed Editor09 admission contract. Unreal `FEditorBulkData` confirms the required shape: content payload identity, shared immutable buffer ownership, async retrieval, disk/virtualized backing and explicit unregister/unload semantics. No speculative permanent blob store, unbounded undo cache or false job byte estimate was added.
- 2026-08-30 cold/cook profiling foundation: source IBL staging now separates `source_classify`, `source_identity`, `cache_probe`, `source_decode`, `cubemap_build`, optional `irradiance_cube_build`, `bundle_encode` and `bundle_commit` in production timeline scopes and timing reports. `bundle_write()` remains a compatibility sum of encode plus commit, while `total()` counts each owner phase once and does not double-add the PMREM/SH9 subphases already contained by `cubemap_build`. Review found that the compatibility `decode_external_source_cubemap` API also performed PMREM/SH9 construction internally, which would charge the full DDS/KTX bake to `source_decode` and leave `cubemap_build` at zero. A crate-private decode-only source-mip API now feeds the same timed rebuild owner used by captured `.zcube`; the public compatibility API retains its original complete-cubemap behavior. A profiling capture also emits source/derived bytes, face/mip layout, PMREM/SH9/source-mip subphase microseconds, work-item count and IEM visit upper bound. Static contracts pass `40/40`, focused `rustfmt --check` and scoped diff integrity pass; no Cargo, ETW/WPA, WGPU, PNG, RenderDoc, RSS/VRAM or power run was made, so this is measurement infrastructure only and M5-M8 remain `in_progress`.
- 2026-08-30 procedural sky generation identity review: display sky, realtime capture, deferred, fallback and generated-material paths already consume the same `zr_procedural_sky.wgsl`. The remaining CPU `procedural_environment.rs` has no production caller and implements only horizon-to-zenith interpolation while omitting ground and sun, so it is explicitly not a display/capture reference truth. Its hard deletion was reviewed but withdrawn from this slice because the tracked path and root module are outside the immutable Shader06 write scope; a future audited owner transfer must remove it. Realtime source generation now derives a 128-bit identity at compile time from the complete capture WGSL (including the common sky module) and downsample WGSL, then combines it with the parameter key using four `u32` XOR operations. Any byte change in either producer creates a new scheduler/runtime-cache generation without runtime source scans or allocation. The capture identity owner remains local and deliberately does not refactor the still Frameworks01-owned compute-plan hash helper. `intensity` and `rotation_radians` intentionally remain final-sampling parameters: display, SH9, irradiance and PMREM lookup apply them after bake, so changing either reuses the same radiance artifact. Focused identity contracts, `rustfmt --check` and scoped diff integrity pass. This adds no binding, dispatch, sample, PSO or GPU allocation; Cargo/WGPU/PNG/RenderDoc and performance/power measurements remain pending, so M4-M8 stay `in_progress`.
- 2026-08-30 realtime IBL generation-starvation repair: architecture review found that `prepare_frame` previously called `request_rebake` with the newest sky key every frame. Because a key change replaces the active ticket at `CaptureSky { first_face: 0 }`, a continuously moving sun could complete `0` generations regardless of runtime duration; the current 8-mip/2-face scheduler requires a finite 21 accepted slices. Unreal's `FRealTimeSlicedReflectionCapture` instead preserves a work cubemap until the terminal diffuse-irradiance state and only then swaps the ready/work target. Zircon now freezes one `ProceduralSkyParams` snapshot for the active 21-slice generation and coalesces all intervening changes into one latest-wins slot. The active generation therefore publishes within at most 21 successful submissions, then immediately starts the newest pending key; per-frame request work and queue storage remain `O(1)`, with no allocation, binding, dispatch, sample, PSO, permutation or GPU-resource increase. A behavioral Rust regression drives a distinct source revision before every slice, requires the original generation to publish within 64 attempts, and requires only the final revision to remain pending. The RED source contract observed all four state/transition owners absent; GREEN reports `6/6`, focused `rustfmt --check` and scoped diff integrity pass. This is a correctness/liveness repair and an algorithmic bound, not measured latency, power or GPU throughput evidence; the known workspace compile blocker still prevents Cargo/WGPU execution, and current-source PNG, `D:\Tools\renderdoc`, GPU p50/p95/p99, RSS/VRAM and WPR/WPA remain open.
- 2026-08-30 PMREM roughness/BRDF no-change review: the apparent unused tail after roughness 1 maps to mip 5 in an 8-mip chain is intentional Unreal parity, not an off-by-two error. Zircon's CPU recipe/inverse, GPU dispatch parameters and Forward/Deferred/fallback sample helper all retain `roughest_mip=1` and `roughness_mip_scale=1.2`; mips 6/7 are the clamped full-roughness and terminal-average tail. The roughness-1 cosine path is the same normalized expectation as the GGX `V=N, alpha=1` estimator while reducing variance. The RG16F LUT uses `(NoV, Roughness)` and the same `F0 * A + saturate(50 * F0.g) * B` gate as Unreal `BRDF.ush`; its clamp-to-edge linear sampler preserves boundary texel centers. No sampling constant or production code changes are authorized from this review. Fresh managed Naga/WGPU image parity and GPU/power evidence remain required before changing recipe resolution, sample budgets or tail layout.
- 2026-08-30 realtime IBL liveness measurement contract: before any further slice-budget or sampling optimization, each accepted CPU recording sample must identify its generation start frame, elapsed generation frames, number of distinct latest-slot replacements coalesced while that generation remained active, and whether a next generation is currently queued. The terminal SH9 sample is the publication-interval authority; it must expose `21` elapsed frames for the default no-retry schedule, while retry/stall runs may exceed that value without losing attribution. These counters are `O(1)` runtime state and add no clock read, allocation, GPU query, dispatch, binding, sample, PSO, permutation or GPU resource. Current-source Cargo/WGPU execution, continuous-sun sidecar data, PNG/RenderDoc, GPU p50/p95/p99, RSS/VRAM and WPR/WPA remain required before an optimization claim or milestone advancement.
- 2026-08-30 Deferred environment-only PBR signature closure: the `EnvironmentOnlyPbrPreview` deferred template passes authored `dielectric_f0` (10 arguments), while the generic environment module's legacy `zr_environment_pbr_indirect` entry remains a 9-argument compatibility API. The template now calls the existing normalized `zr_environment_pbr_indirect_with_dielectric_f0_normalized` entry, preserving local-probe/planar providers and material F0 without changing the legacy entry. The source contract now checks the specialized call and declaration in the assembled deferred closure; this is a WGSL arity correctness repair only, with no new binding, sample, permutation, PSO, allocation, or performance claim. Managed Cargo/Naga/WGPU, current-source PNG/RenderDoc, timing, RSS/VRAM, and power evidence remain pending, so M5-M8 stay `in_progress`.
- 2026-08-30 Deferred diffuse-energy stale-symbol closure: after the diffuse-energy owner was consolidated to `zr_surface_metallic_diffuse_energy_scale`, the generic and environment-only Deferred templates still referenced the removed `zr_pbr_diffuse_energy_scale`. Those calls now use the scalar metallic diffuse owner, matching Forward/fallback/lightmap decomposition and eliminating undefined WGSL symbols in both Deferred paths. Static production-source search reports zero remaining calls to the removed helper; this is a compile/correctness repair with no binding, sample, permutation, PSO, allocation, or performance change. Managed Cargo/Naga/WGPU and current-source product evidence remain pending, so M5-M8 stay `in_progress`.
- 2026-08-30 WGSL non-finite normalizer closure: the shared `zr_pbr_common_normalize_or_zero` owner now accepts only `epsilon < length < 3.4e38`, so zero, NaN, and infinite lengths select the zero vector without relying on backend-specific `isNan`/`isInf` builtins. Environment, PBR-extras, surface, both Deferred profiles, and fallback-mesh normalizer entry points delegate to that owner. The module registry makes the `zr_surface_types.wgsl -> zr_pbr_common.wgsl` dependency explicit, while Forward, GBuffer, and TAA-reactive fixed assembly sequences emit the common owner before surface consumers. This removes six duplicated length/division implementations and keeps Forward/Deferred/fallback handling aligned; it adds no binding, sample, permutation, PSO, allocation, or loop. The source contract covers the module-level bound, finite predicate, six delegating entry points, and the three fixed assembly orders. An isolated E-drive crate using the locked `naga 29.0.3` reads the production files, extracts the exact shared owner plus each delegator, and reports parse+validation `6/6`; its build stayed under `target/shader06-naga-probe` and did not load the workspace manifests. Full assembled-template/WGPU compilation and current-source PNG/RenderDoc remain required before acceptance; status is `implementation_complete_pending_managed_shader_validation`, and M5-M8, manifest attribution, and coordinator commit status remain unchanged.

- 2026-08-31 realtime IBL bounded-failure closure: the source review traced both recorder errors and failed submissions into an unbounded boolean retry loop, so the scheduler now owns a typed `Recording`/`Submission` report, a per-stage three-attempt budget, one- then two-complete-frame retry backoff, and terminal same-key suppression. A successful retry resets the stage budget; terminal failure preserves the published key and ready slot, clears runtime active/queued snapshots, and a different source revision starts a new generation. Requesting the preserved published key clears an obsolete failure for another key and restores `Ready`; requesting the failed key remains suppressed. Retry eligibility uses the same wrapping `u64` frame-sequence model as the runtime counter, so a failure at `u64::MAX` skips frame `0` and resumes at frame `1` rather than becoming permanently deferred. Each submitted attempt also carries its frame identity, so a delayed completion from the failed attempt is `Stale` and cannot complete the retry that has the same logical stage/substep. The initial failure-only facade was rejected during product-boundary review: the scheduler is the unique six-state readiness owner, while one `RealtimeIblStatusReport` adds current/start frame, published/pending/queued keys, coalesced update count, and optional failure through `SceneRenderer` and `WgpuRenderFramework` after pending submission completion; no parallel failure getter remains. This is a liveness/fault-isolation repair, not a measured performance optimization: steady successful work keeps the same `O(1)` scheduler state and slice count, and status projection/failures add only constant state/branch/copy work without new binding, dispatch, sample, allocation, PSO, or permutation. Focused source contracts, leaf `rustfmt`, scoped diff integrity, and an E-drive `rustc -O` harness that directly compiles the production scheduler and verifies all six readiness states, retry/backoff, delayed-completion rejection, last-good, failed-key suppression, published-key restoration, new-key recovery, bounded frame age, and rollover pass; harness source/binary SHA-256 are `C0BF937A1F9A51FDB031834515B46821DABBA9E0F830683281BB3764015A6EA3` / `1A015F79BC5731A716E82291687415F1001C88F3280A90FC9B592FC691E22718`. The post-repair independent review reports `Critical 0 / Important 0` for attempt identity and stale completion. Managed Cargo/WGPU fault injection, public status query execution, current-source PNG/RenderDoc, timing, RSS/VRAM, and power evidence remain open, so status is `implementation_complete_static_and_isolated_behavior_green_pending_managed_fault_product_validation`; M5-M8, manifest attribution, and coordinator commit status do not advance.
- 2026-08-31 realtime IBL freshness closure: review against Unreal `USkyLightComponent`'s capture status/age ownership found that `RefreshingLastGood` and `FailedLastGood` exposed a key but no freshness, forcing each diagnostic consumer to build another timer. `RealtimeIblRuntime` now records the successful publication frame and projects `published_generation_frame_number`, `last_good_age_frame_count`, and inclusive `active_generation_elapsed_frame_count` through the existing single status report. The shared half-range wrapping frame-sequence helper returns active elapsed `0` before the first slice, publication age `0` in the publishing frame, and correct age across `u64` rollover; retries, terminal failures, and published-key restoration retain the timestamp, while only a later successful publication replaces it. Production behavior tests cover fallback/baking/ready/refresh/failed-last-good, restoration, rollover, and replacement, but remain unexecuted until managed Cargo is available. The E-drive scheduler harness covers the shared sequence primitive and remains GREEN with the hashes above. This adds three fixed `Option<u64>` fields and constant integer work only, with no wall clock, allocation, lock, GPU query, binding, dispatch, sample, PSO, permutation, timing, power, or performance claim. Status is `implementation_complete_static_and_isolated_sequence_green_pending_managed_status_product_validation`; M5-M8, manifest attribution, and coordinator commit status do not advance.
- 2026-08-31 freshness follow-up review reports `Critical 0 / Important 0 / Minor 0`, confirming pre-first-slice zero elapsed, publication-frame zero age, rollover, terminal/restoration timestamp retention, and replacement only by a successful successor publication.
- 2026-08-31 P1-9 direct-cosine IEM path disposition: a whole-repository production-call review found one non-test caller, `asset/importer/environment_ibl.rs`. The default import contract requests `PMREM_SH9`; direct cosine IEM is built only when `environment_ibl_irradiance_cube=true` requests `PMREM_SH9_IEM`, and a current staged bundle returns before source rebuild or IEM convolution. Runtime cache misses use the existing GPU PMREM/SH9/IEM render-graph path, while hydration decodes the versioned artifact and prepares upload bytes. This already establishes the intended product split: GPU runtime, cached opt-in CPU cook, and CPU direct integration as oracle/high-quality offline capability. cmft independently uses an offline SH projection/reconstruction filter, while Unreal reflection capture records convolution and diffuse-irradiance work as GPU RDG passes. No renderer algorithm change or unmeasured optimization was made. P1-9 is reclassified as `runtime_path_closed_opt_in_cook_profile_retained`; an SH/GPU cook replacement is authorized only after a representative opt-in import profile shows `irradiance_cube_build` dominates and includes error, latency, RSS, and energy evidence. Reviewed source/importer/settings SHA-256 values are `7C859E43E4F60C70306BF0DCE76DE09B4170A732E9A9CE555C0177A35063B937` / `128157450A2C9D5078552E93F1912E0AC3C5260089C6CCAD7C455C607589DB40` / `491F0AC4724265CDA9112D2005239179E5BB2018856CAB43E5372B5F0DD3ED46`; managed Cargo, product image, RenderDoc, timing, RSS, and power gates remain unchanged.
- 2026-08-31 P1-18 AmbientLight/lightmap source-policy closure: the render snapshot now carries `affects_lightmapped_meshes`, and `SceneUniform::from_frame` produces all-mesh and lightmapped-mesh ambient sums in one O(A) pass. Forward/generated/fallback select with the existing GPU Scene lightmap bit. Deferred stores that bit in the unused alpha of the existing `Rgba16Float` emissive GBuffer and consumes it from the existing emissive load in both full and environment-only lighting. The hard cut adds 16 B to `SceneUniform` but adds no MRT, binding, texture sample, feature bit, permutation, PSO key, allocation, or per-light/per-probe loop. Full SceneUniform ABI mirrors and source contracts are statically GREEN. An isolated E-drive `naga 29.0.3` probe validates full Deferred, environment-only Deferred, fallback, two skybox variants, and six normalizer delegates; probe source/binary SHA-256 are `1738DA460B1CF5535F45297C5E70EFAD0A8FD6AA561ACE34A971E29413DB37AC` / `D8E7D5E589B67B875A1F36405D00B11090751B2D536B713A97CDB10E5AC64EAC`. Managed Cargo/WGPU, current-source PNG under `docs/tests/runtime/shader`, `D:\Tools\renderdoc`, timing, RSS/VRAM, and power remain pending. Status is `implementation_complete_static_and_isolated_naga_green_pending_managed_wgpu_and_product_validation`; M5-M8 do not advance.

- 2026-08-31 P1-8 recipe identity closure: current-source review against Unreal `PreintegratedGF` and cmft/cmftStudio confirms the 128x32 RG16F joint-Smith split-sum LUT is a device-global system texture, while `.zribl` remains a producer-specific PMREM/diffuse artifact. A new `EnvironmentPbrRecipe` composes the existing `IblBakeRecipe`, an independent versioned `EnvironmentBrdfLutRecipe` (algorithm `2026_08_31_0001`, extent `128x32`, 128 samples, `GgxJointSmithSplitSum`, `Rg16Float`), and an explicit `SingleScatterSplitSum` energy mode. Asset/runtime composite identities still differ only in their IBL diffuse producer identity; the LUT identity is shared and is intentionally excluded from the serialized IBL artifact header so a device-global LUT change cannot invalidate unrelated environment blobs. System-texture extent, row stride, WGPU format, and expected payload length now derive from this canonical recipe; payload retains only the non-derivable builtin byte SHA and bytes. The production path remains builtin-materialized with no runtime LUT integration, no new binding/sample/permutation/PSO, and no multiple-scattering consumer. TDD RED snapshot `2460` captured the new public contract before implementation; focused rustfmt, static source guards, and scoped diff checks are green. Managed Cargo/Naga/WGPU and current-source product image/RenderDoc/error/power evidence remain pending, so status is `implementation_complete_static_recipe_green_pending_managed_runtime_validation` and M5-M8 do not advance.
- 2026-08-31 P1-18 current-source ABI follow-up: a repository-wide `SceneUniform` declaration audit found that Render07 subsequently added `ssao_spatial_denoise.wgsl` and `ssao_bilateral_upsample.wgsl` without the 16-byte `lightmapped_ambient_color` field. Both builtin shaders read fields after `ambient_color`, so their camera position/direction offsets are stale even though the Shader06 Forward/Deferred/fallback set covered by the isolated probe remains valid. The two shaders and their descriptor are part of a foreign in-flight Render07 half-resolution AO change and were not split-edited here; the exact hashes, root cause, forbidden workarounds and Naga/WGPU return gate are recorded in [`Render07 SSAO SceneUniform lightmapped ambient offset drift`](../render/07/failure-2026-08-31-ssao-scene-uniform-lightmapped-ambient-offset.md). P1-18 production policy remains implemented, but the broad current-source ABI gate is reopened with status `implementation_complete_core_naga_green_cross_plan_ssao_mirror_failure_open`; M5-M8 do not advance.
- 2026-08-31 P1-6 cold-fallback/bootstrap architecture disposition: the roughness-independent procedural fallback remains a confirmed visual defect but is now explicitly reported as fallback rather than ready output. The default first generation is 21 one-operation batches (3 capture, 7 source-mip, 3 PMREM-mip0 face batches, 7 remaining PMREM mips, 1 SH9). Batch storage, topology identity, retry/completion token, and timing all treat one operation as atomic. A one-submission bootstrap would still contain about 17 GPU passes and does not remove filtering work, so no speculative merge or sample-budget reduction was made without current-source GPU/power evidence. The required baseline is per-pass GPU timestamps, CPU command/submission cost, publication latency, RSS/VRAM and matched WPR/WPA energy; any implementation must hard-cut scheduler, topology cache, retry/completion, and timing together. Status is `algorithm_defect_confirmed_structural_bootstrap_cut_pending_current_source_gpu_and_power_profile`.
- 2026-08-31 shared-model prepared-cache infrastructure closure: full call-graph review corrected the Runtime02 handoff premise. `ensure_model` directly loads from `AssetManager` and owns one `Arc` publication boundary; the actual per-instance deep copy was `ResourceStreamer::load_model_asset`, whose prepared-cache hit cloned the complete `ModelAsset` before `extend_pending_draws_for_mesh_instance` consumed it. The helper now returns `Option<Arc<ModelAsset>>`; a current revision uses `Arc::clone`, repeated hits preserve `Arc::ptr_eq`, a stale revision loads a distinct latest `Arc`, and a miss wraps the fallback exactly once. No deep-copy compatibility path or second cache owner remains, so repeated instances move from `O(I * model_payload)` copies to `O(I)` Arc reference-count operations with one prepared payload. The archived Render18 three-file transfer was applied with fingerprint `3f808bab3f337a59f67e446fd6978cdbde597a8e37733f3131b8ebe90ecdcc9d` / request `1b9b625640c8476bb842a7032dfe5d4b`, then the five-file set was leased as request `2bd4614f0023460d987f0a64cf82cfc6`; RED snapshot is `2502`. Final helper/ensure SHA-256 values are `8C42C235FB5886CF51959E8BEA4EF29E753007BE185F3DAF0F54F45CCB53C567` / `050EEF76803472FAD0BB3AFF67BCAA2C1150156210661A40BCC42C7748AE8BEF`; focused rustfmt and diff integrity are green. The fresh ownership matrix at `baselineEpoch=584` confirms all five dependency paths remain attributed to Shader06 with no competing owner; the lease has since expired and no transfer or replacement owner is active. Managed Cargo and a realistic repeated-instance allocation/profile run remain blocked by the reserved coordinator CPU lane, so this is `implementation_complete_static_pointer_identity_green_pending_managed_rust_and_current_source_profile`; it does not advance M5-M8 or claim timing, RSS, GPU, or power acceptance.
- 2026-08-31 shared-model ensure follow-up: the first Arc hard cut removed the per-instance prepared-cache deep clone, but a second full-chain review found `ensure_model` still bypassed the helper. A model with unchanged source revision and a changed external mesh dependency therefore reloaded the complete source payload before rebuilding geometry. Ensure now consumes `self.load_model_asset(id)` and publishes that Arc directly; current-source dependency rebuilds add one Arc reference instead of reconstructing vertex/index/virtual-geometry payload, while the independent GPU resource Arc remains unchanged. The five-file exact lease/attribution receipts are `1038d1ed77ac4f48aee4c647b18011e1` / `068d3a896e294552bbd950bcb1c3e7f6`; final load/ensure/accessors/extend/prepared SHA-256 values are `22B1A68EB2D9189FC12CB01645F42BD0915CA1B46EB3BDD2BC5A10132B944922` / `74805DD382F15A31CE1CA75C6FAB09724EF569767B5684A8C2B7AE9FD16AD2DE` / `9D938CD8B168E6B11E5FF302AEB317E23A7A618708D6E4F61EBCBDC6AB9DD92C` / `961D196F8E5008EC1B60BF7FAA9990F5368420D525998721863F18E496708B32` / `C4134746BE41B8AA433ACD9C7BA64249E863D99CDEB2C24CF07DE340384E98C7`. An ignored profile defines 262,144 vertices + 786,432 indices (`28,311,552 B` by current layout) and 16,384 pointer-identity hits with zero fallback, but it has not run because managed Cargo is still reserved; no elapsed/allocation/RSS/power result is claimed and M5-M8 do not advance.
- 2026-08-31 historical screenshot inspection: the newest existing real-HDRI PNG in `docs/tests/runtime/shader` (2026-08-23 RenderDoc export) renders the lake background and metal sphere, but inspection still shows horizontal banding across the sphere and clipped/overexposed sky/highlight regions. The 2026-07 metallic/smoothness matrix shows the same exposure/banding signature. These files belong to older binaries/RDCs and were not regenerated from the current Shader06 tree; they are retained as defect context only. Current-source DX12 viewer capture, PNG pixel checks, `D:\Tools\renderdoc` replay and matched timing/power evidence remain mandatory before any visual or PBR-correctness claim.
- 2026-08-31 Runtime02 ownership reconciliation: the requested Arc<ModelAsset> handoff is not transferable to a live Runtime02 coordinator session (`01a04c62-01c7-7ea1-8328-87fa4eb2b125` has no session record). The fresh Shader06 ownership matrix at `baselineEpoch=584` keeps all five dependency paths attributed to this session with no competing owner; the earlier lease is expired, so no new lease or ownership transfer is fabricated. Shader06 remains the single integration owner for the helper/ensure/accessor/mesh-instance closure, while the Cargo manifests remain foreign and untouched. Static pointer identity and formatting evidence stay green; managed Cargo and realistic allocation/profile execution remain blocked by the reserved CPU lane, so no timing, RSS, GPU, power, commit, or acceptance claim is advanced.
- 2026-08-31 source-fingerprint baseline: the viewer production-source closure contains `18` files and was regenerated at `E:\zircon-profiles\shader06-source-closure-manifest-20260831.json` with SHA-256 `6BC104E6BA2C32FF0B9830825CD09F612FB36547FA15C160086A34653E20DDE5` (`3159` bytes). This is a source identity anchor only; no Cargo/WGPU/rendering or performance result is implied. Any current-source PNG/RenderDoc/timing profile must bind this manifest before it can advance M5-M8.
- 2026-08-31 critical-source identity baseline: the full Shader PBR cross-module critical set contains `117` existing source files and was normalized into `E:\zircon-profiles\shader06-critical-source-manifest-20260831.json` with canonical SHA-256 `97464d01e8f213a1f1e41cefe5161f6033f8caa407f8495a9546fd643d4bd238` and file SHA-256 `D5121000D70A0EDCC85C5D16276DCB5B809E22B730CC266BE92F620D87AEB663` (`17187` bytes). The set includes environment/deferred/realtime-IBL/resource-streamer/pipeline owners beyond the viewer closure. This is identity evidence only; it does not advance managed compilation, WGPU, screenshots, RenderDoc, timing, RSS/VRAM, power, or milestone status.
- 2026-08-31 realtime IBL graph/command fail-closed closure: production review found that graph reports and cached WGPU commands could independently carry dispatch extents even though pure regressions currently agreed. The recorder now passes each fixed PMREM/SH9 graph extent into the binding cache and rejects a mismatch on both cache hits and misses before compute encoding, pipeline creation, or parameter/bind-group creation. Canonical SH9 `[1,1,1]` is accepted; legacy graph `[4,4,6]` against command `[1,1,1]` is rejected with both extents and the command key. The steady success path adds one allocation-free three-lane equality check per cacheable dispatch, retains the complete 4,124-workgroup ticket budget, and does not add a binding, sample, allocation, PSO, permutation, or GPU resource. Focused `rustfmt --check`, structural ordering contracts, and scoped diff integrity are green; managed Rust/WGPU execution, current-source PNG, `D:\Tools\renderdoc`, GPU timing, RSS/VRAM, and power remain pending. The local SH9 failure therefore stays `open`, and M5-M8 do not advance.
- 2026-08-31 P0-9 warm-cache and source-bundle infrastructure closure: the pre-implementation profile recorded 31 cold-source runs x8 samples (248 samples) for the Poly Haven 2K source: decode `p50=139.50 ms/p95=278.55 ms`, staged-content `p50=15.11 ms/p95=23.94 ms`, combined `p50=156.21 ms/p95=294.35 ms`; each sample performed 21 decode allocations / 58,728,995 B and the cache probe performed 6 allocations / 21,070,832 B. Following Unreal `TextureDerivedData` metadata-first cache queries and cmft/cmftStudio's explicit offline bake boundary, the importer now resolves source dimensions/decoder identity without RGBA32F materialization, probes a fixed 252-byte little-endian `ZRIBLBND` manifest under versioned `render/ibl-source-bundle`, and hydrates source/derived payloads only after a current-generation snapshot. Source, asset-derived and manifest files publish through one durable transaction with the manifest last; missing/old-schema/request-mismatch bundles are rebuildable misses, and hydration verifies both BLAKE3 payload stamps before decoding. The equirect identity includes resolved decoder format while captured ZCUBE/DDS/KTX identities retain their container-specific stable key domain; source-cubemap staging recovery/target validation is split into a bounded child module. Focused manifest/metadata/source-identity/recovery contracts and scoped `rustfmt --check`/`git diff --check` are green; no Cargo/WGPU/ETW/WPA run was started for this slice because the coordinator Cargo lane is occupied by another session. Target after-profile is combined p50/p95 <= `39.05/73.59 ms`, warm manifest <= `512 B`, warm probe payload reads `0`, and warm probe allocation < `64 KiB`; these are acceptance gates, not achieved results. Status is `implementation_complete_static_bundle_contract_green_pending_managed_rust_and_current_source_profile`; M5-M8, current-source PNG, `D:\Tools\renderdoc`, timing, RSS/VRAM, power and milestone commit remain pending.
- 2026-08-31 P0-9 evidence-identity closure: the Shader PBR critical-source contract now includes every production owner added by the hard cut (bundle manifest/recovery, warm probe/restore, source identity and metadata decoder) instead of only their root modules. It resolves to `133` existing files with zero duplicate/missing paths; `tools/tests/zircon_profile_shader_pbr_viewer.Tests.ps1` and `shader-pbr-profile-source-closure.Tests.ps1` pass `30/30 + 4/4`. The non-overwriting current-source manifests are `E:\zircon-profiles\shader06-critical-source-manifest-p0-9-20260831.json` (canonical SHA-256 `6763f7906021af18edcce68a2c96f4cd1ebe413d8e8a132fec2693ec1b314973`, file SHA-256 `A5C3C154E430E7C86DA55574997B2D26DEC65EF9B1EA1DBA14EA3F12251A4A58`, `19384` bytes) and `E:\zircon-profiles\shader06-source-closure-manifest-p0-9-20260831.json` (`18` viewer files, file SHA-256 `83BD5E68A4EF3B71352E3105802853F415EBC00692BFAA55E6CB45A9EF2616AE`, `3157` bytes). These supersede the earlier 117-file identity for future evidence without overwriting historical baseline files; they still make no Cargo, WGPU, visual, timing or power claim.

- 2026-08-31 P0-9 caller-decoded warm-hit follow-up: the caller-provided decoded-image API bypassed the metadata-first entry probe, so its shared builder decoded cached `.zcube/.zribl` payloads before discovering a current manifest. An explicit `EnvironmentIblBundleProbeState` now distinguishes a prior miss from a required probe; only the caller-decoded path performs `current_bundle_manifest_matches` before payload hydration, while ordinary entries preserve their existing source-only rebuild path without a redundant manifest probe. Probe timing accumulates instead of overwriting earlier metadata work. Static RED/GREEN, focused rustfmt and scoped diff integrity pass; orchestrator/warm-cache SHA-256 values are `D82D6D65DCA2F3A0CBBA52BC28DAE491A95F0879781E87AACF61BEB0A3618099` / `36BBE79883BB60876F555C9E15D7F350196B309814EAD917DA81D0F0F4FB12A9`. Managed Cargo and the 31-run after profile remain pending, so P0-9/M5-M8 and performance/power claims do not advance.
- 2026-08-31 P0-9b evidence-identity refresh: the critical-source contract now explicitly includes the Arc hard-cut load/ensure/prepared/mesh-instance owners in addition to accessors. The new non-overwriting manifest `E:\zircon-profiles\shader06-critical-source-manifest-p0-9b-20260831.json` contains `137` unique/existing paths; canonical/file SHA-256 values are `94F4D07B4D96F7DBF3E990B475382C7ABF8A88EABE4E31F0356078F9A2B02B0F` / `3A0149719B04ABC916C2D6F1F533F5AA39A97ACD956E44C4B34DABDB7BA7E211` (`20046 B`). Source-closure Pester is green `4/4`; the 18-file viewer-only manifest is unchanged. This supersedes the 133-file critical identity for future validation but is not Cargo/WGPU/image/performance/power evidence, so milestone status is unchanged.
- 2026-08-31 P0-10 telemetry/readiness architecture review: current source already has three distinct truth cadences: `RealtimeIblStatusReport` owns six-state readiness and generation age; `RenderStats.last_frame_profile` and `last_reflection_probe_workload` own frame-matched CPU/GPU, staging/persistent bytes, probe occupancy, upload and rejection counters; the scene submission journal owns source submission identity and `DeviceLost`; importer reports own cache decision, phase timing and bundle publication. The defect is that callers can combine these reports at different lock/cadence points, not that a second renderer-local state machine is missing. The P0-10 hard-cut design is therefore an immutable `EnvironmentRuntimeSnapshot` composed under one framework operation/state lock after pending submission completion, plus a bounded versioned `EnvironmentGenerationTrace` for asset/cook and delayed GPU/scene events keyed by generation/ticket. Snapshot construction shares the existing `Arc<RenderFrameProfile>`, validates frame/profile generation identity, returns no profile when no current frame exists, and never drains timing collectors or stores filesystem paths. Trace entries preserve cache hit/miss reason, artifact provenance, CPU/GPU operation identity, capture latency and terminal failure without backfilling delayed samples into current-frame state. Unreal `ReflectionEnvironmentCapture.cpp` confirms the useful shape is one owner for budget admission, hysteresis, timeslice progress and fast-render/eviction decisions; it does not justify merging delayed timing, import timing and scene readiness into one mutable struct. Public `graphics` facade ownership is currently held by Runtime07, so Shader06 did not edit that boundary or split ownership. RED/GREEN gates and the implementation sequence are recorded in 09F1 §12.36. Status: `architecture_review_complete_snapshot_trace_implementation_pending_facade_owner_and_managed_validation`; M5-M8, current-source PNG/RenderDoc, managed Cargo/WGPU, timing, RSS/VRAM, power and milestone commit remain pending.
- 2026-08-31 P0-10 immutable runtime-snapshot first slice: after the architecture/ownership review, the pure status DTO, snapshot/error and trait contract were placed in `core::framework::render`; graphics retains only a public re-export of the same status type, while WGPU owns one backend projection. The query finishes pending submission, takes exactly one operation/state lock pair, and combines current generation, shared `Arc<RenderFrameProfile>`, probe workload, scene-completion identity and realtime IBL readiness. No current frame yields no profile; generation mismatch fails closed; delayed completion retains its own generation/ticket. The path does not call deep-copying `query_stats` or drain CPU/GPU timing collectors, so its structural cost is `O(1)` fixed report copies plus one Arc reference operation. Focused rustfmt, single-owner/single-lock/no-drain source checks, diff integrity and source-closure Pester pass (`34/34`); the defined 16,384-iteration `Arc::ptr_eq` Rust regression awaits managed Cargo. The 148-path non-overwriting identity is `E:\zircon-profiles\shader06-critical-source-manifest-p0-10-20260831.json`, canonical/file SHA-256 `5F233BE3C035E553ECD85B9DC8E5F8C77065E6CD8DA7781FA16ABA66C3FCC125` (`21627 B`). The versioned generation trace/store remains unimplemented and Cargo/WGPU/PNG/`D:\Tools\renderdoc`/RSS/VRAM/WPR-WPA evidence remains pending. Status: `runtime_snapshot_first_slice_complete_static_green_trace_store_and_managed_product_validation_pending`; P0-10 and M5-M8 do not advance.
- 2026-08-31 P0-10 coordinator ownership closure: the four new DTO/query paths entered Shader06 through audited transfer `1d58777b555f47518c1dd6d39249c5e7`; seven existing facade/wiring paths could not self-transfer while already attributed, so they crossed a planless no-edit scope bridge and returned at identical hashes through receipts `f62060c84f5c4bd6ac63c88b7eaad94a` and `13aa4ef7069a46c489b7d541512e4b43`. The complete 16-path P0-10 dependency set is now inside the immutable Shader06 write scope and attributed to the same integration owner. Separately, Shader06 had no manifest write scope or live manifest lease, so current `Cargo.toml`, `Cargo.lock`, and `zircon_runtime/Cargo.toml` blobs were atomically transferred to Frameworks01 with fingerprint `0CB0B50193C3B7AAA5840C3B74AD0ADDEC7EDCC578B60E2235D6F58530A464CE` / receipt `91616B0CD88F486CB94DAE68E1BCE9F5`; no manifest content was edited by Shader06. A second source review reconfirmed the query lock order, fail-closed generation check, delayed-event identity and Arc-sharing boundary. The managed workspace lane is still occupied by foreign job `957b46a1eb8c4e61b3a7aa5d53aecaca`, so no Cargo retry, milestone commit, WeCom notification, PNG, RenderDoc, timing or power claim is made; P0-10 and M5-M8 remain pending their existing validation gates.
