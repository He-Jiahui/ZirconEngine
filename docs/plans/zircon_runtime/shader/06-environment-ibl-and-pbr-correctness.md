---
related_code:
  - zircon_runtime/src/core/framework/render/environment/source_cubemap.rs
  - zircon_runtime/src/core/framework/render/environment/source_cubemap/pmrem.rs
  - zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_compute_executor.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export/frame_assertions.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export/scene_fixtures.rs
  - zircon_runtime/src/core/framework/render/material/standard_material.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs
  - zircon_runtime/src/graphics/shader/template/material_surface.rs
  - zircon_runtime/src/graphics/shader/wgsl/zr_gbuffer_encode_standard_pbr.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/shaders/deferred_lighting.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/shaders/fallback_mesh.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/post_process/shaders/post_process_screen_space_reflection.wgsl
  - zircon_runtime/src/core/framework/render/environment/skybox.rs
  - zircon_runtime/src/core/framework/render/environment/cubemap_projection.rs
  - zircon_runtime/tests/runtime_environment_cubemap_projection_contract.rs
  - zircon_runtime/src/core/framework/render/image/dimension.rs
  - zircon_runtime/src/asset/assets/texture/descriptor.rs
  - zircon_runtime/src/asset/assets/texture/upload_support.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_texture/gpu_texture_resource_from_asset.rs
  - zircon_runtime/tests/runtime_texture_cube_resource_contract.rs
  - zircon_runtime/src/graphics/shader/wgsl/zr_environment.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/shaders/skybox_procedural.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/overlay/viewport_overlay_renderer/construct/create_sky_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/primitives/scene_uniform/from_frame.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_texture/mod.rs
  - dev/cmft/src/cmft/cubemapfilter.cpp
  - dev/cmft/src/cmft/image.cpp
  - dev/cmft/src/cmft/cubemaputils.h
  - dev/cmftStudio/src/shaders/fs_mesh.shdr
  - dev/cmftStudio/src/shaders/fs_skybox.sc
  - dev/cmftStudio/src/shaders/vs_skybox.sc
  - dev/cmftStudio/src/shaders/utils.shdr
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentCapture.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentRealTimeCapture.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentDiffuseIrradiance.cpp
  - dev/UnrealEngine/Engine/Shaders/Private/ReflectionEnvironmentShaders.usf
  - dev/UnrealEngine/Engine/Shaders/Private/ReflectionEnvironmentShared.ush
  - dev/UnrealEngine/Engine/Shaders/Private/MonteCarlo.ush
  - dev/UnrealEngine/Engine/Shaders/Private/BRDF.ush
  - dev/UnrealEngine/Engine/Shaders/Private/SkyLightingShared.ush
  - dev/UnrealEngine/Engine/Source/Developer/TextureCompressor/Private/TextureCompressorModule.cpp
---

# 06 环境 IBL 资产链与 PBR 正确性验收

本子计划是 shader 计划集对 render 计划 11(环境光照)的**算法定稿与验收闭环补充**:计划 11 的"工程落地细化"仍是文件落点/契约类型/帧时序的实施权威;本计划定稿其中 IBL 相关 compute 的**具体算法**(equirect→cube、GGX 预滤波、SH9、BRDF LUT)、`zr_environment.wgsl` v2 的采样端契约,以及"8×8 金属度/光滑度材质球矩阵正确"的可量化验收标准。跨计划扩展按 shader index §4 惯例在计划 11/13 状态表登记镜像条目。

## 1. 问题定位:当前渲染为什么全是马赛克

2026-07-04 的 SH04/Plan 11 状态行(index §4.1)所验证的"sampled environment"路径存在三个结构性缺陷,与截图观察(天空盒马赛克、mip 无模糊、PBR 矩阵不可信)一一对应:

1. **没有环境贴图,只有 128 个数**。`SkyboxSettings.sampled_equirectangular` 是 16×8=128 个 vec4 的 CPU 数组(`skybox.rs` 的 `SAMPLED_EQUIRECT_ENVIRONMENT_WIDTH/HEIGHT`),经 scene uniform 上传;`zr_environment.wgsl` 的 `zr_environment_sampled_equirect_color` 做**最近邻**索引。天空盒的物理分辨率就是 16×8——马赛克是数据形态的必然,不是滤波 bug。
2. **没有预滤波,粗糙度是假的**。`zr_environment_reflection_color` 用 `mix(sky(reflect_dir), sky(normal), roughness)` 两方向插值冒充模糊;不存在 GGX 卷积 mip 链,"mipmap 全是马赛克"是因为根本没有生成过预滤波 mip。
3. **没有 split-sum,能量是拍的**。`zr_environment_pbr_indirect` 用 `0.35`/`0.20`/`1.0 - roughness*0.65` 经验常数近似 diffuse/specular 权重;无 SH irradiance、无环境 BRDF 项,金属度/光滑度扫描不可能呈现正确梯度。

前置缺口:`gpu_texture` 无 `Cube`/`D2Array` 维度,`CubemapAsset`(计划 13 TX-M3)未开工。

2026-07-06 复核结论:用户指出的 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 问题成立。该图必须标记为 **rejected baseline**:它只能证明 16x8 sampled-equirect 路径不可用,不能证明 cubemap/PMREM 方案完成。当前可继续验收的路径从 2026-07-05 source-cubemap/2K PMREM 图开始,但 EC-M3 仍需补严格高频/roughness 单调截图、SSIM 与 GPU/offline bake 对拍。

## 2. 参考实现精读结论(cmft / cmftStudio / UE5)

动手前必读下表源文件(index §5 第 8 条参考对照纪律)。以下为已完成的精读结论,实现期直接对照。

### 2.1 cmft:equirect→cubemap 与离线滤波

| 机制 | 结论 | 出处 |
|------|------|------|
| 面尺寸 | `dst_face_size = (equirect_H + 1) / 2`(2:1 全景图 → 面 = H/2;1K HDRI 512 高 → 面 256) | `image.cpp:3143` |
| texel→方向 | `uu,vv ∈ [-1,1]`,`dir = normalize(uu*faceU[f] + vv*faceV[f] + faceN[f])`;六面轴表见 `cubemaputils.h:38–70` | `cubemaputils.h:149–161` |
| 方向→equirect UV | `u = (π + atan2(x,z)) / 2π`,`v = acos(y) / π`,**双线性**采样 | `cubemaputils.h:221–228`,`image.cpp:3185–3219` |
| 逐 texel 立体角 | `areaElement(x,y)=atan2(x·y, √(x²+y²+1))` 四角差分(Rory Driscoll 精确式),用作滤波/SH 权重 | `cubemaputils.h:291–313` |
| radiance 滤波 | 逐 texel `weight = solidAngle · dotᵖ`(p=specular power)锥角裁剪 + per-face bbox 加速;gloss→power 为指数映射 `2^(scale·g+bias)` | `cubemapfilter.cpp:774–862, 1811–1855, 2100–2108` |
| 末级 mip | 1×1 时六面取平均,消除方向噪声 | `cubemapfilter.cpp:2181–2199` |
| edge fixup(Warp) | 烘焙端 `u' = u + warp·u³` 三次扭曲(nvtt 出身),采样端 `fixCubeLookup` 把非主轴分量按 `1 - 2^lod/size` 压缩——**为无 seamless filtering 的旧 GL 准备** | `cubemaputils.h:163–185`,`utils.shdr:51–63` |
| SH | 5 阶 25 系数,立体角加权累加后 `norm = 4π/Σω`;重建乘 cosine lobe `A_l = 1, 2/3, 1/4, 0, -1/24`;**无 windowing** | `cubemapfilter.cpp:124–350` |
| 消费端 | `lod = (1-gloss)·(mipCount-1)` 线性;`envSpec = fresnel(F0, NoV) · radiance`——**无 split-sum LUT**,是简化 IBL | `fs_mesh.shdr:194–225` |
| 容器 | DDS face-major / KTX mip-major;RGBA16F/RGBA32F 全支持 | `image.cpp:1416–1432, 5236, 5243–5362` |

**2026-07-07 运行时验证链取舍修订**:用户指出镜面/天空盒仍不对后,当前 CPU source-cubemap PMREM 与采样端先按 cmft/cmftStudio 消费模型对齐。方向/立体角/双线性公式与"末级 mip 平均"继续原样采纳;PMREM mip1..N 使用 `specularPowerFor(mip,mipCount,glossScale=10,glossBias=3)` + `BlinnBrdf(power/4+1)` 的 cosine-power radiance lobe,以 Hammersley 锥采样近似 cmft 的逐 texel solid-angle 积分;采样端 roughness→LOD 改为 cmftStudio 的线性 `roughness * (mipCount - 1)`,并在 skybox/specular/IEM/source cube lookup 前加入 `fixCubeLookup` 压非主轴分量。仍不照搬 cmft 的全 texel bbox 扫描和 bake-side Warp 扭曲;GPU/offline UE GGX FIS 仍作为后续生产级 artifact/compute 对拍目标,不能再把当前运行时验证链描述为 UE 对数 LOD。

### 2.2 UE5:捕获管线、split-sum 与实时环境

| 机制 | 结论 | 出处 |
|------|------|------|
| 捕获流程 | 逐面渲染 → mip0 premultiply alpha → `CreateCubeMips` 降采样链 → `ComputeDiffuseIrradiance`(SH9)→ `FilterCubeMap`(GGX) → 拷入 `TextureCubeArray` | `ReflectionEnvironmentCapture.cpp:490` |
| 分辨率/格式 | 128 或 256,`PF_FloatRGBA`(FP16);mip 数 = `CeilLogTwo(size)+1` | `ReflectionEnvironmentCapture.cpp:274` |
| roughness↔mip | `LevelFrom1x1 = 1.0 - 1.2·log2(max(r, 0.001))`,`mip = maxMip - 1 - LevelFrom1x1`;逆映射 `r = exp2((1.0 - LevelFrom1x1)/1.2)`——mip↔roughness 与总 mip 数无关 | `ReflectionEnvironmentShared.ush:16–39` |
| GGX 重要性采样 | Hammersley 序列,`E.y *= 0.995` 防掠射;`CosTheta = √((1-E.y)/(1+(a²-1)E.y))`,`a² = r⁴`(α=r² 的平方);`L = 2(H·V)H - V` | `MonteCarlo.ush:347–363`,`ReflectionEnvironmentShaders.usf:612–648` |
| **filtered importance sampling** | 按样本 PDF 选源 mip:`PDF = D·NoH/4`,`Ω_s = 1/(N·PDF)`,`Ω_p = 4π/(6·size²)·2`,`srcMip = 0.5·log2(Ω_s/Ω_p)`;NoL 加权累加——这是 32/64 样本就能干净的关键 | `ReflectionEnvironmentShaders.usf:612–648` |
| 样本数 | 桌面 `r<0.1 ? 32 : 64`;参考路径 1024 | `ReflectionEnvironmentShaders.usf:561–580` |
| split-sum LUT | `PreIntegratedGF` 128×128,UV=(NoV, roughness),RG 双通道;`GF = F0·A + saturate(50·F0.g)·B`(F90 项) | `BRDF.ush:559–573` |
| 无 LUT 近似 | Lazarov 多项式 `EnvBRDFApprox`(常数 c0/c1 见出处),移动端用 | `BRDF.ush:583–604` |
| SH9 计算 | 实时路径:8×8=64 线程均匀球采样 mip2,树形归约,输出 8×float4(预乘 diffuse 卷积系数);离线路径:32³ 中间 cubemap 三 pass 归约 | `ReflectionEnvironmentShaders.usf:759–892`,`ReflectionEnvironmentDiffuseIrradiance.cpp:88–230` |
| SH9 重建 | `GetSkySHDiffuse`:9 点积无分支(L0+L1 一组 dot4、L2 双线性项、L2 m=±2 项),`max(0, …)` 截断 | `SkyLightingShared.ush:84–103` |
| 探针混合 | light grid 剔除后按覆盖半径大→小 under-operator 累加:`rgb += s.rgb·a_acc`,`a_acc *= 1-s.a`,`a<0.001` 早退;剩余 alpha 给 skylight | `ReflectionEnvironmentComposite.ush:12–188` |
| parallax | box:局部空间 AABB 最远交点,`DistanceAlpha = 1-smoothstep(0, 0.7·transition, boxDist)`;sphere:二次方程远交点 | `ReflectionEnvironmentShared.ush:136–201` |
| SSR/RT/Lumen 组合 | 高频源(SSR/Lumen/RT)先写,`Color.a = 1 - hit.a`,探针吃剩余 alpha,skylight 兜底,最后统一乘 `EnvBRDF` | `ReflectionEnvironmentPixelShader.usf:113–281` |
| 实时天空捕获 | 时间切片:每帧渲染 N 面(云默认 2 面/帧)→ `FDownsampleCubeFaceCS`(8×8)→ `FConvolveSpecularFaceCS` 分帧卷积 → SH 一次性 64 线程 | `ReflectionEnvironmentRealTimeCapture.cpp:340, 84–153` |

**取舍**:mip 映射常数(1.0/1.2)、GGX 采样式、FIS、样本数、LUT 尺寸/公式、SH9 归约与重建、SSR→探针→天空的 alpha under 组合契约全部原样采纳;探针混合沿用计划 11 已定稿的 top-2 截断(URP 量级)而非 UE 的 grid 全列表;light grid 剔除、Lumen 本体不在本计划范围。

### 2.3 2026-07-06 cubemap/PMREM 设计修订

- **source 与 PMREM 分离**:source cubemap mip0 是天空盒显示源;specular PMREM 是反射卷积结果。任何把 PMREM 粗 mip 用作天空背景的实现都会把反射模糊直接变成背景马赛克。
- **分辨率规则**:按 cmft 的 equirect height 规则,`polyhaven_lakes_1k.hdr`(1024x512)的 source face 是 256,`polyhaven_lakes_2k.hdr`(2048x1024)的 source face 是 512。文档和测试不得再写"1K 默认 512 face"。
- **PMREM mip 生成**:2026-07-07 起当前运行时验证链采用 cmft/cmftStudio radiance filter 口径:cmft face axes/lat-long/source sampling,`pow(dot, specularPower)` lobe,Blinn BRDF power 修正、末级六面平均与 cmftStudio 线性 roughness/gloss LOD。UE GGX FIS 仍保留为后续 GPU/offline bake 的生产级对拍目标,但不得再把当前 CPU bridge 的可视截图按 UE 对数 roughness mip 解释。
- **mip 模糊纪律**:source mip chain 只是 FIS 的输入 pyramid,不能当 PMREM 结果。`roughness >= 0.99` 的高粗糙度尾部按 UE `FilterPS` 的 cosine hemisphere branch 从 source cubemap 金字塔卷积采样,不能从上一层 PMREM 普通降采样;末级 1x1 六面平均;验收要覆盖高频方差下降、cube seam 下降和截图级 roughness 单调。
- **cmft/cmftStudio 保留价值**:face order、lat-long、exact solid angle、邻面 edge 处理、final mip averaging 与 IEM/PMREM 双贴图消费分工继续作为实现纪律;Warp fixup 仅作旧 GL/调试参考,wgpu 原生 cube seamless filtering 下不作为默认算法。

### 2.4 UE TextureCompressor:普通 cubemap mip 与 PMREM 的边界

| 机制 | 结论 | 出处 |
|------|------|------|
| long-lat base cube | 源图先线性化;每个 face texel 用 texel center 得到 world direction,再转 longitude/latitude 采样源 HDRI。该阶段只生成 source cubemap mip0,不做反射卷积。 | `TextureCompressorModule.cpp:2254–2315` |
| 普通纹理 mip | `GenerateMipChain` 用可分离 Gaussian/sharpen kernel 与可选 2x2 average intermediate chain;这是普通 2D 纹理 mip,不是 cubemap angular mip。 | `TextureCompressorModule.cpp:1799–2057` |
| cubemap angular mip | `GenerateAngularFilteredMips` 先构建简单平均 mip 作为输入加速,再按每个输出 mip 的 cone angle 选择输入 mip,跨六面在角锥内积分并用平滑核归一化。该算法解决 cubemap 缩小/低 mip 的角域模糊与接缝一致性。 | `TextureCompressorModule.cpp:2473–2651` |
| 角色边界 | angular cubemap mip 是 source cubemap 的 mip pyramid/texture minification/FIS source LOD,不是 specular BRDF PMREM。标准 PBR 反射仍使用 §4.2 的 UE ReflectionEnvironment GGX FIS。 | `ReflectionEnvironmentShaders.usf:612–648` + `TextureCompressorModule.cpp:2473–2651` |

设计取舍:
- `source_cubemap_mipmap` 必须成为独立 owner:输入 source mip0,输出 source mip pyramid;它可服务 skybox 缩小、FIS 源 LOD、诊断 atlas 与资产 `.zcube` source mip,但不写 PMREM。
- 当前 CPU bridge 的 face-aware bilinear/box pyramid 只允许作为过渡实现;产品/离线标准切到 UE angular filtered cubemap mip:先建平均链,按输出 mip 的角面积与 quality bias 选输入 mip,跨六面 cone 积分,末级 1x1 六面平均。
- `pmrem.rs` / `ibl_prefilter.wgsl` 继续只拥有 GGX/cosine radiance convolution;不得把 ordinary/angular source mip downsample 当作 PMREM 高粗糙度结果。
- `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 继续作为 rejected baseline;下一张有效截图必须同时给 source skybox、PMREM mip diagnostic 和 8x8 roughness/metallic matrix 的定量指标。

## 3. 目标数据链(总览)

```
.hdr/.exr equirect(≥1K,建议 2K)
   │ 导入:计划 13 TX-M3 .zcube(RGBA16F,face-major,含全 mip)——就绪前由 EC-M1 的运行期路径顶替
   ▼
[env.equirect_to_cube]   compute/EC-M1 CPU bridge:双线性重投影 → 源 cubemap mip0(面尺寸 = min(pow2((H+1)/2), 1024))
   ▼
[env.source_cubemap_mipmap] UE angular filtered cubemap mip pyramid(过渡期可用 face-aware CPU pyramid)
   ▼                                + EC-M2a CPU GGX FIS PMREM bridge(正式 GPU/RGBA16F PMREM 前过渡)
GpuEnvironmentMap(计划 11 落点):
   ├─ [env.ibl_prefilter]      compute:128³×8mip GGX 重要性采样 + FIS(mip0 直拷,末级 6 面平均)
   ├─ [env.ibl_irradiance_sh]  compute:SH9 立体角加权树形归约(源取 32³ 档 mip)
   └─ [env.brdf_lut]           compute:128×128 RG16F split-sum LUT(设备生命周期一次,与环境内容无关)
   ▼
采样端 zr_environment.wgsl v2:
   skybox pass   → 源 cubemap(全分辨率 trilinear)          ← 消灭天空马赛克
   specular 项   → prefiltered 链 mip=对数映射 × EnvBRDF(LUT) ← 消灭假粗糙度
   diffuse 项    → SH9 求值 × albedo × (1-metallic)          ← 消灭 0.35 常数
```

关键分离:**天空盒显示采样源 cubemap,不采样 128³ 预滤波链**。当前"天空盒分辨率极低"的另一半原因是显示与 IBL 共用同一低分辨率数据;UE/Unity 的天空背景都走全分辨率源(或天空材质),预滤波链只服务反射。

烘焙产物(PMREM mip 链、SH9、可选 IEM)按 §4.7 三来源优先序取用:资产伴生离线产物 → 运行期回写缓存 → 现场 compute;命中离线产物时运行期环境 compute dispatch 为 0。

## 4. 算法定稿

### 4.1 equirect → cubemap(`equirect_to_cube.wgsl`)

- 每 texel 一线程(8×8 workgroup,z=face)。texel 中心寻址:`uu = (2(x+0.5)/size) - 1`(修正 cmft 的非中心寻址)。
- 方向公式与六面轴表照抄 cmft(`cubemaputils.h:38–70, 149–161`);方向→UV 用 `u=(π+atan2(x,z))/2π, v=acos(clamp(y,-1,1))/π`,源图双线性(equirect 源以 `texture_2d` + linear sampler 上传,直接硬件双线性)。
- 面尺寸:`clamp(next_pow2((H+1)/2), 64, 1024)`。该节点只承诺 source cubemap mip0 的正确重投影;source mip pyramid 归 `env.source_cubemap_mipmap`/`source_cubemap_mipmap.rs` 后续节点所有,避免 equirect 投影、普通 mip 与 PMREM 混在同一 owner。

### 4.1a source cubemap angular mip(`source_cubemap_mipmap.rs` / `env.source_cubemap_mipmap`)

- 输入:source cubemap mip0(RGBA16F face-major,+X,-X,+Y,-Y,+Z,-Z)。输出:source cubemap 全 mip pyramid,供 skybox trilinear minification、GGX FIS `src_mip` 采样、离线 `.zcube` source mip 与诊断 atlas 使用。
- 算法标准:参照 UE `GenerateAngularFilteredMips`。先从 mip0 构建简单平均链作为积分输入;对每个输出 mip/face/texel,以该 texel direction 为 cone axis,按 mip texel angular extent 得到 cone angle;依据球面覆盖面积和 quality bias 选择输入 mip;遍历六面中落入 cone 的 texel,用 smoothstep 类核和 cubemap solid angle 加权,最后归一化写出。
- 末级纪律:source 最终 1x1 mip 做六面共同平均,与 cmft final mip averaging 一致;这样 skybox 低 mip 与 FIS 源 LOD 不保留 face-local 方向噪声。
- 过渡实现:当前 CPU bridge 可继续使用 face-aware bilinear/box source pyramid 解锁 PMREM/FIS 验证,但状态表必须标记为 bridge;产品验收和离线 artifact 不得把该过渡链声明为 Unreal angular mip 完成。
- 测试:高频棋盘/条纹 cubemap 的 source mip luma variance 必须随 mip 单调下降;六面边界平均差随 mip 下降;`runtime_shader_pbr_real_hdri_lakes_*_mip_diagnostic_*.png` 中 source mip 区域不允许出现 nearest-neighbor 块状放大。

### 4.2 GGX 预滤波(`ibl_prefilter.wgsl`)

固定 128³×8 mip(计划 11 定稿),每 mip 一 dispatch,特化常量 = 该 mip 的 roughness 与样本数:

```
// 每 mip roughness(采样端映射的逆,常数 1.0/1.2,mip 数无关)
level_from_1x1 = (num_mips - 1) - mip
roughness(mip) = exp2((1.0 - level_from_1x1) / 1.2)
// mip0:roughness≈0,直拷源 cubemap(重采样到 128)

// 每 texel(法线 N = texel 方向,V=N 假设,切空间 Z=N):
alpha = roughness²;a2 = alpha²                    // UE Pow4 约定
for i in 0..N_samples:
    E = hammersley(i, N);E.y *= 0.995
    cos_theta = sqrt((1 - E.y) / (1 + (a2 - 1) * E.y))
    H = spherical(2π·E.x, cos_theta)               // 切空间半向量
    L = 2·H.z·H - vec3(0,0,1)                      // 反射方向
    NoL = L.z
    if NoL > 0:
        pdf = D_GGX(a2, H.z) * 0.25
        omega_s = 1.0 / (f32(N) * pdf)
        omega_p = 4π / (6 · src_size²) * 2.0
        src_mip = clamp(0.5 * log2(omega_s / omega_p), 0.0, src_max_mip)   // FIS
        color += textureSampleLevel(src_cube, world(L), src_mip).rgb * NoL
        weight += NoL
out = color / max(weight, 1e-4)
// 末级 mip(1³ 或 2³)dispatch 后追加六面平均 pass(cmft 纪律)
```

样本数档位沿用计划 11 表(Fast 32 / Normal 64 / High 128,`roughness<0.1` 减半),**FIS 是硬性前提**——没有按 PDF 选源 mip 时这些样本数不足以消噪,这正是当前直觉"需要几千样本"与 UE 实际 32/64 的差距来源。累加在 f32 域进行,写出 RGBA16F。

### 4.3 SH9 投影(`ibl_irradiance_sh.wgsl`)与重建

- 源取源 cubemap 的 32³ 档 mip(UE `GDiffuseIrradianceCubemapSize=32` 同量级)。
- 每面 8×8 线程读全部 texel,权重用 cmft 精确 `texelSolidAngle`(非均匀球近似);workgroup 共享内存树形归约(64→1),6 面结果二段归约,`norm = 4π/Σω`。
- 输出即计划 11 的 `array<vec4<f32>, 9>` 布局(L00,L1-1,L10,L11,L2-2,L2-1,L20,L21,L22),**烘焙端预乘** cosine lobe `A_l ∈ {1, 2/3, 1/4}` 与基函数常数(Stupid SH Tricks);采样端 `zr_env_sh9_eval(n)` 为 9 项多项式无分支求值 + `max(vec3(0), …)` 截断(数学等价于 UE `GetSkySHDiffuse` 的 dot4 重排)。无 windowing(3 阶 + 低频信号,ringing 不构成问题;若未来夹具出现负瓣再补 Hanning)。
- CPU 参考实现(测试用)与 compute 用同一权重公式,逐系数对拍。

### 4.4 环境 BRDF LUT(`brdf_lut.wgsl`)

- 128×128 `rg16float`,UV=(NoV, roughness),1024 样本 GGX 重要性采样积分 Karis split-sum 的 A/B 两项;设备生命周期生成一次(不进 `IblBakeKey`)。
- 采样端 `GF = F0 · A + saturate(50·F0.g) · B`(F90 白色项,UE 式)。
- 同时在 `zr_environment.wgsl` 提供 `zr_env_brdf_approx(F0, roughness, NoV)` Lazarov 多项式(常数照抄 `BRDF.ush:583–604`)作为低端档 fallback,由引擎 define 选择,默认走 LUT。

### 4.5 采样端契约(`zr_environment.wgsl` v2)

绑定沿计划 11 group1 binding 16..23 表(sky specular cube、sampler、SH9 buffer),新增 BRDF LUT 一槽(占用计划 11 表的 23 预留位之前的空位由实施期在计划 11 登记确切编号)。删除 `scene.environment_samples` 后接口:

```wgsl
fn zr_env_mip_from_roughness(roughness: f32, max_mip: f32) -> f32;   // §2.2 对数式
fn zr_env_sh9_eval(n: vec3<f32>) -> vec3<f32>;                        // 预乘 SH9,9 点积
fn zr_environment_sky_color(dir: vec3<f32>) -> vec3<f32>;             // skybox:源 cube trilinear
fn zr_environment_pbr_indirect(n, v, roughness, metallic, diffuse_color, base_color, occlusion, …) -> vec3<f32>;
// 内部:
//   F0 = mix(vec3(0.04), base_color, metallic);F90 = saturate(50·F0.g)
//   specular = prefiltered(R, mip(roughness)) · (F0·A + F90·B)
//   diffuse  = zr_env_sh9_eval(n) · diffuse_color · (1 - metallic)
//   return (diffuse + specular) · occlusion · intensity      // intensity/rotation 采样期,不进 bake key
```

roughness 定义统一:材质 smoothness → `perceptual_roughness = 1 - smoothness`;预滤波/映射/LUT 全部消费 perceptual_roughness,GGX 内部 `α = r²`。经验常数路径(0.35/0.20/0.65 与两方向 lerp)整体删除。

### 4.6 探针、实时捕获与光追(契约对齐,实施归计划 11)

- **探针捕获**:6 面渲染 → 降采样 → 复用 §4.2/§4.3 同一 compute 节点(UE `FilterReflectionEnvironment` 结构);混合用计划 11 top-2 + skybox fallback,box/sphere parallax 公式计划 11 已定稿(与 UE 同式)。本计划只保证 prefilter/SH 节点以"源 cube 任意尺寸输入"为参数化前提,探针 128³ 与天空源共用。
- **实时环境捕获**(登记为 EC-M4,对齐 UE 时间切片):每帧预算 = 渲染 N 面(默认 2)+ 降采样该面 + 分帧 GGX 卷积(每帧 1 mip)+ 末帧一次性 SH;`IblBakeKey` 增加 `realtime_slice_state` 不参与——实时模式下 bake key 判定退化为"每 6+8 帧一轮"。V1 只做 procedural sky 的实时重烘(参数拖动即触发),场景几何入捕获留 EL-M2 之后。
- **光追/SSR 组合契约**:预留 alpha under 组合序 = 高频源(SSR/RT)→ 探针 → 天空,`indirect_specular.a` 表示未覆盖率,统一末端乘 EnvBRDF(UE `ReflectionEnvironmentPixelShader.usf` 结构)。V1 不实现 RT,只保证 `zr_environment_pbr_indirect` 拆出 `zr_env_specular_ibl(...)` 可被后续 pass 以剩余 alpha 调用。

### 4.7 离线烘焙镜面反射(PMREM)与三来源持久化

术语:PMREM = prefiltered mipmapped radiance environment map,即 §4.2 GGX 预滤波 mip 链的持久化形态(cmft CLI 的同名产物)。HDR 镜面反射的离线烘焙**不引入第二套算法**——离线与运行期跑同一份 compute WGSL(headless wgpu)或其 CPU 参考实现,产物一致性由 `render_env_prefilter_cpu_gpu_match_16` 保证。

`GpuEnvironmentMap` 装配时按三来源优先序取产物,以 `IblBakeKey` + 算法版本号判定命中:

| 优先 | 来源 | 产出时机 | 说明 |
|---|---|---|---|
| 1 | 资产伴生 derived artifact | 导入期 / staged build 期离线烘焙 | importer(计划 13 texture_importer 插件)或 `tools/zircon_build.py` staged 阶段 headless 跑 equirect→cube + prefilter + SH(+IEM),随资产持久化;镜像 shader prewarm 纪律 |
| 2 | `.zircon-cache` 回写缓存 | 首次运行期烘焙后异步 readback 回写(计划 11 既有机制,由"仅 SH"扩为 PMREM+SH9+IEM 全产物) | 编辑器内拖 procedural 参数 / 换 HDR 的迭代路径 |
| 3 | 现场 compute | prepare 期(计划 11 帧时序 §1) | 前两级全 miss 兜底;烘完即回写为来源 2 |

- 持久化格式:face-major RGBA16F 全 mip(cmft DDS 布局纪律,`image.cpp:1416` 同序)+ SH9 144B + 可选 IEM 32³;头部携带 `IblBakeKey` 与**算法版本号**——mip 映射常数(1.0/1.2)、FIS、样本档位、SH 权重公式全部入版本,算法升级旧产物自动失效重烘,不会静默混用两代产物。
- 当前 payload 合同:`IblBakeArtifactPayload` 固定按 PMREM RGBA16F 全 mip → SH9(9×vec4 f32 little-endian,144B)→ 可选 IEM 32×32×6 RGBA16F(alpha=1.0) 写入;`expected_payload_size_bytes()` 与三段 byte-range helper 是 importer/cache/file container 的共同 offset 来源。
- 当前 blob 合同:`IblBakeArtifactBlob` 固定 header + payload 完整字节容器,`decode_current_for_request(...)` 同时验证 header 魔数/格式、payload 长度和 request/key/layout/content/algorithm-version 当前性。
- 当前 runtime cache 合同:`IblBakeArtifactCacheStore` 在 `.zircon-cache/render/ibl/v{IBL_BAKE_ALGORITHM_VERSION}/{request_hash}/face_####_mips_##.zribl` 读写 raw blob;missing/rejected 非 fatal,只有 current blob 会转成 `IblBakeArtifactCandidate::runtime_cache(...)`。
- 当前 runtime readback writeback 合同:`IblBakeArtifactReadbackSections` 以 descriptor 为真理接收 PMREM RGBA16F、SH9 与 optional IEM RGBA16F 分段字节,拒绝缺失/多余/长度错误分段;`write_ibl_bake_artifact_runtime_readback(...)` 对 stale descriptor 不写文件,current descriptor 则组装 payload/blob 并写入 `IblBakeArtifactCacheStore`。WGPU section acquisition 由 `IblBakeArtifactWgpuReadbackResources` 与 `read_ibl_bake_artifact_wgpu_sections(...)` 装配已有 PMREM texture、SH9 buffer 与 optional IEM texture,实际 render graph/async scheduling 仍由后续 runtime/GPU 切片拥有。
- 当前 WGPU RGBA16F readback helper:`read_texture_rgba16float_region(...)` 可按 mip/origin/extent 读取 `Rgba16Float` texture region 并剥离 row padding;`read_texture_rgba16float_cube_mip_chain(...)` 输出 face-major、face 内 mip 顺序的 cubemap bytes,与 PMREM artifact section layout 对齐;`SceneEnvironmentCubemap` 的 source/specular/IEM cube texture 已具备 `COPY_SRC` usage。SH9 buffer readback helper 与 IBL artifact section acquisition helper 已存在;IBL compute 生产和调度仍待后续切片。
- 当前 resolved payload 合同:`resolve_ibl_bake_artifact_payload(...)` 消费来源 1/2 的 blob-backed candidates,复用 asset-derived > runtime-cache > runtime-compute 优先级,命中时返回可直接应用的 payload 且 dispatch=0,miss 时 `payload()==None` 并保留 required contents dispatch 计数。文件 IO 仍归 asset artifact cache/importer/staged build owner。
- 当前 runtime dispatch/cache 集成合同:`resolve_ibl_bake_artifact_runtime_dispatch(...)` 在 asset artifact 边界读取 runtime cache,合并 asset-derived blob 候选,并返回 resolved payload 与 dispatch count;`write_ibl_bake_artifact_runtime_dispatch_readback(...)` 只在该 dispatch report 需要 runtime compute 时写入 readback sections,首次 miss -> writeback -> 下一次同 request runtime cache hit 且 dispatch=0 已有合同测试,asset-derived/cache hit 时 stray readback 被跳过。该合同仍不生产 GPU bake 资源,也不拥有 render graph async scheduling。
- 当前 source-cubemap 应用合同:`source_cubemap_mip_chain_with_bake_artifact(...)` 保留 source/display mips,仅把 decoded PMREM/SH9 payload 应用到 specular PMREM 链与 diffuse SH9;`source_cubemap_environment_with_bake_artifact(...)` 进一步保留环境 source revision/hash、intensity、rotation,并在 payload 含 IEM 时附加 `SourceCubemapIrradianceCube`;这保证天空盒仍采全分辨率源 cubemap,反射/漫反射可来自 derived/runtime cache artifact。
- 当前 WGPU upload identity 合同:`SourceCubemapEnvironment::ibl_bake_key()` 继续只描述 source 输入,artifact 应用不会改变 bake key;`texture_upload_key()` 额外包含 artifact payload hash,让 `SceneEnvironmentCubemap::ensure_uploaded(...)` 在同一 source 命中不同 PMREM/SH9/IEM artifact 时重传 source/specular/IEM cube。
- 命中来源 1/2 时运行期环境 compute dispatch 数为 0(验收断言),与计划 08 MS-M4"二次启动 miss=0"同构。
- **外部预滤波 cubemap 的处理**:cmft/cmftStudio 产出的含 mip DDS/KTX 导入时,其 mip 链默认只作为源 cubemap 的降采样链消费;PMREM 仍由引擎按当前算法版本重新生成。2026-07-07 后当前运行时验证链已对齐 cmftStudio 的 Blinn/cosine-power radiance + 线性 LOD,但外部容器仍缺 Zircon 的 `IblBakeKey`、算法版本、SH9/IEM payload 与 artifact header,因此不能静默声明为可直接消费的 PMREM artifact;`.zcube` 暂不设 `prefiltered` 声明位。
- 开发者使用说明:导入 `.hdr` → importer 自动产 `.zcube` + 伴生 PMREM/SH(来源 1);编辑器改 procedural 参数 → 运行期切片重烘 + 回写(来源 2/EC-M4);发布走 staged build 全量预烘,产品运行期零环境烘焙成本。

### 4.8 IEM(irradiance cubemap)替代 SH 的 diffuse 消费选项

cmftStudio 的 diffuse 消费端是 32³ irradiance cubemap 直采(`fs_mesh.shdr:210` 的 `textureCube(u_texIem, n)`),不是 SH 求值。引擎提供两种 diffuse 消费路径,由**引擎 define** `ZR_ENV_DIFFUSE_IEM`(全局维度,走计划 08 defines,不是材质 option,不增材质变体)切换:

| 路径 | 求值 | 优势 | 默认 |
|---|---|---|---|
| SH9 | `zr_env_sh9_eval(n)` 9 项多项式 | 零纹理带宽;与 ambient/probe grid 契约同一表示(计划 11) | 是 |
| IEM | `textureSample(zr_env_irradiance, zr_env_sampler, n)` 1 fetch | 无 3 阶截断 ringing;强方向性环境下更准;带宽换 ALU | 质量档选项 |

- 生成:32³ RGBA16F cubemap,余弦加权重要性采样卷积(复用 §4.2 compute 骨架,kernel 换 cosine lobe)。**不采用** cmft 的"SH 重建回 cubemap"式(`imageIrradianceFilterSh`)作为主路径——直接卷积无阶数截断,精度更高;SH 重建式仅作 Fast 档快捷路径。随 §4.7 三来源链持久化,同一 `IblBakeKey`。
- SH9 无条件仍产出:它是 ambient 三模式(计划 11 EL-M4)与 probe grid(EL-M3)契约的权威表示,IEM 只是 standard PBR diffuse 项的可选消费端,不替代契约层。
- 绑定:当前 scene group0 环境段 binding5 为 `zr_environment_irradiance_cube`(`texture_cube<f32>`),主渲染路径无 IEM 时绑定 1x1 黑色 fallback,有 IEM 时上传 32x32x6 RGBA16F;`ZR_ENV_DIFFUSE_IEM` 默认 false,后续由 engine quality/specialization 决定启用。
- 一致性纪律:IEM 与 SH9 求值在低频上必须一致(球面 64 方向对拍,均值/低频差异 < 容差);高频差异是 IEM 的合法优势,不设上限。

## 5. 里程碑与切片

依赖:计划 13 TX-M3(`.zcube` 资产)未就绪前,EC-M1 以"运行期从已加载 HDR 像素生成源 cubemap"打通(数据已在 `docs/tests/runtime/shader/assets/polyhaven_lakes_1k.hdr` 链路上);TX-M3 落地时导入期直接产出含 mip 的 `.zcube`,运行期 equirect_to_cube 节点仅保留给"源是 equirect 资产"的形态,**不留双轨**(同一 `GpuEnvironmentMap` 入口,来源判别切换)。

**EC-M1 源 cubemap + skybox 全分辨率(先消灭天空马赛克)**
- 触碰:`gpu_texture`(Cube 维度 + 逐面逐 mip 上传/视图)、`equirect_to_cube.wgsl`、`gpu_environment_map.rs`(计划 11 落点)、skybox 采样改走 cube 纹理;删除 `SAMPLED_EQUIRECT_ENVIRONMENT_*`、`SkyboxMode::SampledEquirectangular` 的 sample table 形态、scene uniform `environment_samples` 字段与 `zr_environment_sampled_equirect_*` 函数(硬切换)。
- 完成判据:`cargo check -p zircon_runtime --lib --locked` 过;真实 HDRI 天空盒截图无 16×8 块状(与 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 对比);仓库内 `environment_samples`/`SAMPLED_EQUIRECT` 零命中。

**EC-M2 GGX 预滤波 + SH9 + BRDF LUT + 采样端 v2(消灭假粗糙度与经验常数)**
- 切片 1(算法与采样端):`ibl_prefilter.wgsl`/`ibl_irradiance_sh.wgsl`/`brdf_lut.wgsl` 与对应 Rust 构建(计划 11 落点 `ibl_prefilter.rs` + 新增 `env_brdf_lut.rs`)、`zr_environment.wgsl` v2、binding 接线、`IblBakeKey` 消费(procedural 参数/HDR 内容哈希触发重烘)。
- 切片 2(离线持久化,§4.7):derived artifact 读取 + readback 回写(PMREM+SH9)+ 三来源优先序 + 算法版本失效;importer/staged build 预烘入口。
- 切片 3(IEM 选项,§4.8):余弦卷积 compute、`ZR_ENV_DIFFUSE_IEM` define 接线、`zr_env_irradiance` 槽绑定、与 SH9 低频对拍。
- 完成判据:`render_env_mip_from_roughness_roundtrip`、SH 常色→band0、CPU/GPU prefilter 16³ 小尺寸对拍、LUT 角点特值(NoV→1,r→0 时 A→1,B→0)全绿;粗糙度阶梯抓帧模糊单调;bake key 命中来源 1/2 时二次启动环境 compute dispatch 数 = 0;IEM/SH9 低频对拍绿。

**EC-M3 8×8 材质球矩阵验收闭环(直到正确)**
- 触碰:升级 ignored export 用例为 IBL 路径 + 新增非 ignored 定量断言测试(离屏 readback,§6 判据);截图归档 `docs/tests/runtime/shader/`。
- 完成判据:§6 六条判据全部量化通过,截图与 Unity/UE 校准图目视一致。

**EC-M4 探针捕获接线 + 实时天空捕获时间切片(镜像计划 11 EL-M2)**
- 触碰:归计划 11 EL-M2 实施,本计划交付参数化 prefilter/SH 节点与时间切片调度表;procedural sky 参数拖动实时重烘走切片路径。
- 完成判据:计划 11 EL-M2 判据 + 拖动 procedural 参数时帧时间无尖峰(切片生效)。

执行顺序 EC-M1 → EC-M2 → EC-M3 → EC-M4;EC-M1/M2 各自切片内硬切换删除被取代路径。

## 6. "8×8 材质球矩阵正确"的量化判据(全局验收)

矩阵布局沿用现有 export 用例:列 = metallic 0→1,行 = smoothness 0→1,真实 HDRI 天空。六条判据(离屏 readback 自动断言 + 截图人工复核):

1. **镜面正确性**:smoothness=1、metallic=1 球面中心区域与"按反射向量直接采样源 cubemap"的参考渲染 SSIM ≥ 0.95(天空镜像清晰可辨,湖面/地平线不糊)。
2. **粗糙度单调性**:任一 metallic 列沿 smoothness 递减方向,球面高频能量(Laplacian 方差)严格单调下降,且相邻档差异 > 噪声阈值(证明 mip 链在被使用且逐级模糊)。
3. **电介质行正确**:metallic=0 行球面主体颜色与 `SH9(n)·albedo` 参考着色的平均色差 ΔE < 阈值,且仍带 4% F0 的弱边缘高光(Fresnel 掠射变亮可见)。
4. **金属能量合理**:metallic=1、smoothness=0 球的平均亮度 ∈ [SH 均值×F0 分量下限, 源图均值×1.05]——不为黑(当前实现的典型失败),也无能量爆炸。
5. **无块状伪影**:天空背景与球面反射的块边界检测(16×8 网格对齐的梯度峰)命中数 = 0。
6. **无 cube 接缝**:预滤波链每 mip 六面共 12 条边的跨边亮度差 < 阈值(替代 cmft Warp 的防回归断言)。

判据 1–4 同时构成"调节金属度/光滑度示意图"与 Unity 校准图表 / UE 材质示例的对照基础;截图命名 `runtime_shader_pbr_ibl_metallic_smoothness_matrix_<date>.png` 归档。

## 7. 测试清单

| 测试 | 断言 | 位置 |
|---|---|---|
| `runtime_environment_cubemap_projection_contract_matches_cmft_face_axes` / `runtime_environment_equirect_uv_contract_matches_cmft_latlong_axes` | ±X/±Y/±Z 面中心与四角 texel 的方向→UV 与 cmft 公式闭式对拍 | `zircon_runtime/tests/runtime_environment_cubemap_projection_contract.rs` + `core/framework/render/environment/cubemap_projection.rs` |
| `runtime_texture_cube_resource_contract` | `dimension = "cube"` 默认 6 面、`cubemap` 别名拒绝非 6 倍数层数、完整 RGBA8 六面 payload upload-ready | `zircon_runtime/tests/runtime_texture_cube_resource_contract.rs` + `asset/assets/texture/*` + `graphics/scene/resources/gpu_texture/*` |
| `render_env_prefilter_cpu_gpu_match_16` | 16³ 源、全 mip CPU 参考(同 FIS/同序列)与 compute 输出逐 texel 容差对拍 | `ibl_prefilter.rs` |
| `render_env_prefilter_constant_env_is_identity` | 常色环境预滤波后所有 mip 仍为常色(权重归一正确性) | `ibl_prefilter.rs` |
| `render_env_sh9_matches_cpu_reference` | compute SH9 与 CPU 立体角参考逐系数容差对拍;常色→仅 band0 | `ibl_prefilter.rs` |
| `render_env_brdf_lut_corner_values` | (NoV→1, r→0) A→1,B→0;(任意, r→1) A+B < 1(能量守恒上界) | `env_brdf_lut.rs` |
| `render_env_mip_from_roughness_roundtrip` | 已在计划 11 清单,常数 1.0/1.2 双端一致 | 计划 11 |
| `render_env_seam_luminance_below_threshold` | §6 判据 6 | `render_product_environment.rs` |
| `render_product_environment_pbr_matrix_quantitative` | §6 判据 1–5 | `render_product_environment.rs` |
| `render_env_derived_cache_second_launch_zero_dispatch` | bake key 命中来源 1/2 时环境 compute dispatch 计数 = 0;算法版本号变更后失效重烘 | `gpu_environment_map.rs` |
| `render_env_pmrem_artifact_roundtrip` | PMREM+SH9(+IEM) 持久化 → 重载逐字节等值;`IblBakeKey`/版本号不匹配拒绝命中 | `gpu_environment_map.rs` |
| `render_env_external_prefiltered_container_is_source_only` | 含 mip DDS/KTX 导入后 PMREM 仍由引擎生成(源 mip 只作降采样链) | 计划 13 importer 用例 |
| `render_env_iem_matches_sh9_low_frequency` | 球面 64 方向 IEM 采样与 SH9 求值低频差异 < 容差 | `ibl_prefilter.rs` |

命令沿计划 11:`cargo test -p zircon_runtime environment --locked`、`cargo test -p zircon_runtime render_product_environment --locked`。

## 8. 跨计划登记

- **计划 11**(实施权威):本计划 §4 为其 EL-M1 切片 2 的算法定稿来源;新增差异需在其状态表登记——skybox 显示采样源 cubemap(非 128³ 链)、BRDF LUT 节点与 binding、实时捕获时间切片(EC-M4)、16×8 sample table 删除清单、readback 回写由"仅 SH"扩为 PMREM+SH9+IEM 全产物(§4.7)、IEM 新增 `zr_env_irradiance` 一槽(§4.8)。
- **计划 13**(资产权威):`.zcube` 需满足 RGBA16F、face-major、全 mip、equirect/六面/十字三形态导入(TX-M3 原有范围),源面尺寸上限 1024;SH 与预滤波产物不进 `.zcube` 本体;derived artifact(PMREM/SH9/IEM)按 §4.7 作为资产伴生产物由 importer/staged build 产出;外部含 mip 容器(cmft DDS/KTX)只作源消费,`.zcube` 无 `prefiltered` 声明位。
- **计划 08**:无变体键增维;`zr_environment.wgsl` 仍经既有 include 注册表分发,BRDF LUT/低端近似由引擎 define(全局维度)切换,不新增材质局部 option。

## 9. 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-07-08 | EC-M3ah interactive PBR HDRI RenderDoc capture hook | shader_plan06_interactive_pbr_hdri_renderdoc_vulkan_capture_passed_dx12_shader_unroll_open | 已验证:补上交互式 PBR/HDRI viewer 的一次性 RenderDoc 捕获入口,不引入 RenderDoc DLL 依赖。`SceneRenderer` 公开 wgpu graphics-debugger capture 包装;viewer 新增 `--renderdoc-capture-once --exit-after-capture`,在第一帧真实 HDRI/PBR render 前后调用 start/stop,便于 `renderdoccmd capture -w` 自动收口。 | 更新 `zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_render_capture.rs` 与 `zircon_app/src/bin/zircon_shader_pbr_viewer/{args,app,scene}.rs`;新增 `docs/zircon_runtime/graphics/scene/scene_renderer/renderdoc-capture.md`;更新 viewer 测试文档。RenderDoc `.rdc` 写入 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_renderdoc_vulkan_20260708_capture.rdc`,不写 target;画面截图继续使用 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_startup_lifetime_20260708.png`。 | `rustfmt --edition 2021` 覆盖本轮 runtime/viewer 文件通过;E 盘构建前剩余 `97766920192` bytes;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过,耗时约 17m16s,仅既有 warnings;`--help` 显示 RenderDoc capture flags;`$env:WGPU_BACKEND='vulkan'; D:\Tools\renderdoc\renderdoccmd.exe capture -w -c ...zircon_shader_pbr_viewer_renderdoc_vulkan_20260708 ... --face-size 64 --renderdoc-capture-once --exit-after-capture` exit 0,viewer 日志显示 `starting graphics debugger capture on wgpu(vulkan)`/`graphics debugger capture completed`,生成 `.rdc` 6,557,527 bytes;target/cargo-target 同名 `.rdc`/thumb scan 为 0。 | Vulkan RenderDoc capture 路径关闭;DX12 capture 仍 open:首次 `WGPU_BACKEND=dx12` 暴露 deferred lighting FXC `X3511` loop unroll failure,需后续收敛 shader loop/gradient 结构或 DXC 路径。继续真实 `.hdr/.exr` importer/build command、DDS/KTX source cubemap 转 `.zcube`、product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3ag `.zcube` source cubemap + asset-derived `.zribl` staged bundle | shader_plan06_zcube_staged_ibl_bundle_passed | 已验证:补上 importer/staged build 可调用的 source cubemap 资产链路 owner,同一 `IblBakeArtifactRequest` 同时写入 source `.zcube` 和 asset-derived `.zribl`,但两者目录/扩展名/解码合同保持分离。`.zcube` 存 `SourceCubemapMipChain::source_texels()` 源 mip 金字塔,`.zribl` 继续由 asset-derived store 持有 PMREM/SH9/IEM payload;运行时 dispatch resolve 以 asset-derived 来源命中时 `environment_compute_dispatch_count=0`。 | 新增 `zircon_runtime/src/asset/artifact/ibl_source_cubemap_staging.rs`,更新 `artifact/mod.rs` facade;新增 `zircon_runtime/tests/runtime_texture_zcube_staged_ibl_bundle_contract.rs`;新增/更新 `docs/zircon_runtime/asset/ibl-source-cubemap-staging.md`、`.zcube` 与 asset-derived artifact 文档。本切片不生成新截图。 | `rustfmt --edition 2021 --check zircon_runtime\src\asset\artifact\ibl_source_cubemap_staging.rs zircon_runtime\src\asset\artifact\mod.rs zircon_runtime\tests\runtime_texture_zcube_staged_ibl_bundle_contract.rs` 通过;E 盘验证前剩余 `80762052608` bytes;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-zcube-staged-bundle-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_texture_zcube_staged_ibl_bundle_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1,测试本体 0.07s,总耗时约 7m17s,仅既有 warnings。 | 关闭 staged source `.zcube` + derived `.zribl` asset-layer handoff;后续仍需真实 `.hdr/.exr` importer/build command 调用该 store、外部 DDS/KTX source cubemap 转 `.zcube`、product/RenderDoc capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3af interactive PBR HDRI viewer startup fix | shader_plan06_interactive_pbr_hdri_viewer_startup_fix_passed | 已验证:修复用户反馈的生成 exe 双击/直接运行看似无法启动问题。根因是旧入口在创建窗口前同步构建 Poly Haven HDRI source cubemap、PMREM 与临时项目,进程长时间存在但无顶层窗口。现在 `main.rs` 只解析配置并启动 winit app,`app.rs` 先创建窗口并呈现启动帧,随后加载 `PbrMirrorScene`;`args.rs` 新增 `--face-size 64|128|256|512`,默认 256,用于交互启动质量/性能折中;`hdri.rs`/`scene.rs` 将 face-size 传入 source cubemap 构建。 | 更新 `zircon_app/src/bin/zircon_shader_pbr_viewer/{main,app,args,hdri,scene}.rs` 和 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`;新增启动验证截图 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_startup_fixed_20260708.png` 与 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_startup_lifetime_20260708.png`,均为 1296x999,显示 HDRI skybox 与镜面球反射。本切片截图只写入 `docs/tests/runtime/shader`,未写 target。 | `rustfmt --edition 2021` 覆盖本轮 5 个查看器模块通过;E 盘编译前剩余 `84550942720` bytes;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过,耗时约 7m25s,仅既有 warnings;`zircon_shader_pbr_viewer.exe --help` 通过并显示默认 face size 256 与鼠标控制;从生成目录 `Start-Process ... --face-size 256` 启动 pid `26996`,80 秒寿命监控显示 3s 已有顶层窗口,40s 恢复 `Responding=True`,80s 仍以标题 `Zircon PBR HDRI Mirror Viewer` 存活并响应,stdout/stderr 日志为空。 | exe 启动失败问题关闭;手动目视验证继续使用该窗口。后续仍需 importer/staged `.zcube` 接线、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3ae interactive PBR HDRI viewer owner split rebuild | shader_plan06_interactive_pbr_hdri_viewer_owner_split_rebuild_passed | 已验证:按 `engine-code-structure-convention.md` 和 2026-06 review findings 继续收敛手动验证程序,删除临时单文件查看器形态,将 `zircon_shader_pbr_viewer` 拆为 `main/app/args/camera/hdri/presenter/project_assets/scene` owner modules。行为保持为同一 `EnvironmentExtract::source_cubemap(...)` + standard PBR 路径,默认 Poly Haven Lakes 2K HDRI,左键拖拽 orbit camera,滚轮 zoom,用于手动检查真实 HDRI skybox、镜面球反射和上下左右视角。 | 更新 `zircon_app/Cargo.toml`;新增 `zircon_app/src/bin/zircon_shader_pbr_viewer/{main,app,args,camera,hdri,presenter,project_assets,scene}.rs`;更新 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`。新入口 `main.rs` 23 行,最大 child `project_assets.rs` 275 行;当前查看器源码扫描无 `allow(...)`、`unwrap(...)`、`expect(...)`、`panic!`。本切片不生成新 PNG,也不写 target 图片。 | `rustfmt --edition 2021 --check` 覆盖 8 个查看器模块通过;E 盘编译前剩余 `58433208320` bytes,高于 50GB 清理阈值;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过,耗时约 7m05s/426.3s,仅既有 warnings;`E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe --help` 通过并确认默认 HDRI 与鼠标控制;可执行文件 69,835,264 bytes,`2026-07-08 09:44:59`;`Start-Process ... --hdri ...polyhaven_lakes_2k.hdr` 首次启动 pid `42132`,重新打开当前可操作窗口 pid `35272`,两次 `Get-Process` 均显示 `Responding=True`。 | 手动目视验证可继续通过该窗口进行;后续仍需 importer/staged `.zcube` 接线、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3ad external DDS/KTX cubemap source-only guard | shader_plan06_external_dds_ktx_cubemap_source_only_guard_passed | 已验证:补上外部 DDS/KTX source cubemap asset-layer 分类合同,让完整 mip 的六面 DDS/KTX1/KTX2 cubemap 只能作为 IBL bake/source 输入,不能走 generic compressed texture upload 或被误认成 PMREM/runtime material texture。分类器接受当前 importer 形态的 D2+6 layers 和未来 Cube+6 layers metadata,要求 square power-of-two face、完整 source mip chain、payload/render descriptor 与 header 一致,并拒绝 cubemap array。 | 新增 `zircon_runtime/src/asset/assets/texture/external_source_cubemap.rs` 与公开 facade re-export;`upload_support.rs` 在 DDS/KTX upload planning 前先拦截 source cubemap;新增 `zircon_runtime/tests/runtime_texture_external_cubemap_source_only_contract.rs`;新增 `docs/zircon_runtime/asset/assets/texture/external-source-cubemap.md`,并更新 `docs/zircon_runtime/asset/assets/texture/zcube.md`。本切片不生成新 PNG,也不写 target 图片。 | `rustfmt --edition 2021` 覆盖本轮 Rust 文件通过;E 盘验证前剩余 `70521638912` bytes,高于 50GB 清理阈值;首轮新增测试暴露 `zircon_runtime::asset` facade 漏导出 `external_source_cubemap_container_info`,补导出后 `CARGO_TARGET_DIR=E:\cargo-targets\zircon-external-cubemap-source-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_texture_external_cubemap_source_only_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 5/5;同 target `cargo test -p zircon_runtime --test runtime_texture_zcube_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2;`git diff --check` 对触及 tracked 文件 warning-only(LF/CRLF),新增 doc/test trailing-whitespace 扫描为空。 | 关闭外部 DDS/KTX cubemap source-only guard;仍需 importer/staged command 将真实 DDS/KTX source cubemap 解码或转换为 `.zcube`,再生产/引用 derived `.zribl`,并继续 RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3ac interactive PBR HDRI viewer rebuild handoff | shader_plan06_interactive_pbr_hdri_viewer_rebuild_handoff_passed | 已验证:按用户要求重新编译并启动可用鼠标手动控制镜头的 PBR/HDRI 查看器,用于人工检查真实 HDRI skybox、镜面球反射、上下左右视角和掠射表现。程序仍使用同一 `EnvironmentExtract::source_cubemap(...)` 与 standard PBR 路径,默认 HDRI 为 `docs/tests/runtime/shader/assets/polyhaven_lakes_2k.hdr`。 | 可执行文件 `E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe`,69,813,760 bytes;启动参数 `--hdri E:\Git\ZirconEngine\docs\tests\runtime\shader\assets\polyhaven_lakes_2k.hdr`;左键拖拽 orbit camera,滚轮 zoom。本切片只交付可交互程序,不生成新 PNG。 | E 盘编译前剩余 `73948434432` bytes;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --features default-platform --locked --jobs 1 --color never` 通过,耗时 17m07s,仅既有 warnings;`zircon_shader_pbr_viewer.exe --help` 通过并确认鼠标说明和默认 HDRI;`Start-Process ... --hdri ...` 启动成功,pid `50400`。 | 手动目视验证可继续通过该窗口进行;后续仍需 importer/staged `.zcube` 接线、product capture、RenderDoc capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3ab `.zcube` source cubemap container + RGBA16F shared source-only guard | shader_plan06_zcube_source_container_source_only_guard_passed | 已验证:补上 source cubemap 资产容器边界,新增共享 `rgba16f.rs` 编解码 helper,`.zribl` artifact payload 改复用同一 RGBA16F 字节路径;新增 `.zcube` source-only texture container,只保存 `SourceCubemapMipChain::source_texels()` 的全 mip 源 cubemap,descriptor 明确为 linear `rgba16float` cube,但 payload format 使用自有 source marker,防止被当作 PMREM 或普通材质贴图直接上传。 | 新增 `zircon_runtime/src/core/framework/render/environment/rgba16f.rs`,`zircon_runtime/src/asset/assets/texture/zcube.rs`,`zircon_runtime/tests/runtime_texture_zcube_source_cubemap_contract.rs`;更新 texture exports、upload readiness、`.zribl` artifact 文档和 source-cubemap 文档;新增 `docs/zircon_runtime/asset/assets/texture/zcube.md`,`docs/zircon_runtime/core/framework/render/environment/rgba16f.md`。本切片不生成新 PNG,也没有把验证图写入 target。 | `rustfmt --edition 2021 --check` 覆盖本轮 Rust 文件通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-zcube-source-0708 cargo test -p zircon_runtime --test runtime_texture_zcube_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2;同 target `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 21/21;既有 warning flood 未新增本切片阻断。 | 关闭 source-only `.zcube` container 与 `.zribl` payload 误用防线;仍需 importer/staged command 接线、外部 DDS/KTX 含 mip source-only 导入合同、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3aa asset-derived IBL artifact store + source-1 dispatch/seam guard | shader_plan06_asset_derived_ibl_store_dispatch_seam_passed | 已验证:补上 §4.7 来源 1 的 asset-derived companion artifact store,让 importer/staged build 后续可把 PMREM/SH9(+IEM) `.zribl` 写入 `library/render/ibl-derived/...`,运行期解析时来源 1 明确压过 runtime cache 且环境 compute `dispatch=0`。新增 `ibl_bake_artifact_asset_derived.rs`,与 runtime cache 共享 `IblBakeKey/face/mip` request hash,但路径和来源 2 分离;提供 `write_source_cubemap_asset_derived_artifact(...)` staged prebake helper、`read_asset_derived_artifact(...)` 的 Hit/Missing/Rejected 非 fatal 读取和 asset-derived blob candidate。新增独立 integration test `runtime_environment_ibl_bake_asset_derived_contract.rs`,避免继续扩张 991 行 artifact 合同测试;测试覆盖 source 1 写读、source 1 胜过同 request runtime cache、stale algorithm 拒绝、PMREM seam 指标写盘读盘后保持。 | 更新 `docs/zircon_runtime/asset/ibl-bake-artifact-asset-derived.md`,`docs/zircon_runtime/asset/artifact.md`,`docs/zircon_runtime/asset/ibl-bake-artifact-runtime-writeback.md`,`docs/zircon_runtime/core/framework/render/environment/ibl-bake-artifact.md`;本切片不生成新 PNG。 | `rustfmt --edition 2021 --check zircon_runtime\src\asset\artifact\ibl_bake_artifact_cache.rs zircon_runtime\src\asset\artifact\ibl_bake_artifact_asset_derived.rs zircon_runtime\src\asset\artifact\mod.rs zircon_runtime\tests\runtime_environment_ibl_bake_asset_derived_contract.rs` 通过;E 盘剩余空间 `55741014016` bytes,高于 50GB 阈值,但因前一轮 Cargo wrapper 在冷编译中生成 0-byte object 且 `$LASTEXITCODE=-1`,清理本轮 target 中 `zircon_runtime` 产物(`cargo clean -p zircon_runtime --target-dir E:\cargo-targets\zircon-ibl-asset-derived-0708`,removed 35 files/1.1GiB);`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-asset-derived-0708 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` 通过;同 target `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_asset_derived_contract --no-run --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` 通过并生成 `runtime_environment_ibl_bake_asset_derived_contract-10ff0cfb48251da9.exe`;直接运行该二进制 `--nocapture --test-threads=1` 通过 3/3,1.52s,日志 `docs/tests/runtime/render/plan11_ibl_asset_derived_contract_direct_20260708.raw.log`/`.exit.txt` exit 0。前两次 Cargo wrapper 测试在冷编译中无 Rust error 但 exit 1/`-1`,未进入测试,不计通过。 | 关闭 asset-layer 来源 1 companion artifact store 与 source1>source2/dispatch=0/seam gate;仍需 TX-M3 `.zcube` importer/staged build command 接线、外部含 mip DDS/KTX importer source-only 守卫、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3z live IEM graph readback directional guard | shader_plan06_live_iem_graph_readback_directional_passed | 已验证:补上 live compute-produced IEM graph output 边界的方向响应守卫,防止真实 WGPU irradiance cube pass 生成的 `environment.ibl.irradiance_cube` 经 graph readback/runtime cache 写回后出现 face/order/row 布局错乱或退化成黑/常量漫反射。`ibl_bake_runtime_writeback.rs` 新增 `runtime_graph_writeback_reads_iem_graph_output_and_preserves_directional_irradiance`:生成低频方向性 source cubemap,构建 CPU `SourceCubemapIrradianceCube` 参考,按 render graph 执行 IEM WGPU pass,通过 `write_ibl_bake_runtime_cache_from_graph_resources(...)` 写入 runtime cache,二次 resolve 要求 `dispatch=0`,再解码 IEM payload 并用归一化 luma RMS/相关性对拍 CPU 余弦卷积参考。 | 更新 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-runtime-writeback.md`,`docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-readback.md`,`docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`,`docs/zircon_runtime/asset/ibl-bake-artifact-runtime-writeback.md`,`docs/zircon_runtime/core/framework/render/environment/ibl-bake-artifact.md`;本切片不生成新 PNG,也不改交互查看器。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_runtime_writeback.rs` 通过;`rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_runtime_writeback.rs` 通过;E 盘剩余空间 `78243672064` bytes,高于 50GB 阈值,未清理 `E:\cargo-targets\zircon-ibl-live-iem-readback-0708`;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-live-iem-readback-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib runtime_graph_writeback_reads_iem_graph_output_and_preserves_directional_irradiance --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 在冷编译阶段超过工具窗口 1204s,不计通过;等待同 target rustc 退出后直接运行 `E:\cargo-targets\zircon-ibl-live-iem-readback-0708\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe graphics::scene::scene_renderer::environment::ibl_bake_runtime_writeback::tests::runtime_graph_writeback_reads_iem_graph_output_and_preserves_directional_irradiance --exact --nocapture --test-threads=1` 通过 1/1,测试本体 8.14s,7312 filtered。 | 关闭 live IEM graph-output readback/runtime-cache directional gate;仍需 scheduler/product 注入与二次启动 dispatch=0、importer/staged derived artifact seam 对拍、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3y live PMREM graph readback seam guard | shader_plan06_live_pmrem_graph_readback_seam_passed | 已验证:补上 live compute-produced PMREM graph output 边界的 seam 守卫,防止真实 WGPU PMREM pass 生成的 `environment.ibl.pmrem` 经 graph readback/runtime cache 写回后出现 face/mip/row 布局错乱或粗 mip 接缝退化。`ibl_bake_runtime_writeback.rs` 新增 `runtime_graph_writeback_reads_pmrem_graph_output_and_preserves_readback_seams`:生成 seam-stress source cubemap,绑定全 mip source cube,按 render graph 顺序执行所有 PMREM WGPU pass,通过 `write_ibl_bake_runtime_cache_from_graph_resources(...)` 写入 runtime cache,二次 resolve 要求 `dispatch=0`,再解码 PMREM payload 并量化 base/mid/rough cube-edge luma seam。 | 更新 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-runtime-writeback.md`,`docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-readback.md`,`docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`,`docs/zircon_runtime/asset/ibl-bake-artifact-runtime-writeback.md`,`docs/zircon_runtime/core/framework/render/environment/ibl-bake-artifact.md`;本切片不生成新 PNG。按用户要求重新编译既有交互查看器 `E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe` 并确认 `--help` 中默认 HDRI、左键拖拽 orbit、滚轮缩放说明。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_runtime_writeback.rs` 通过;`rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_runtime_writeback.rs` 通过;E 盘剩余空间 `29803991040` bytes,低于 50GB 阈值,按 target policy 清理 `E:\cargo-targets\zircon-ibl-live-pmrem-readback-seam-0708`(`cargo clean` removed 1 file/354B);`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-live-pmrem-readback-seam-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib runtime_graph_writeback_reads_pmrem_graph_output_and_preserves_readback_seams --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 在冷编译阶段超过工具窗口 1204s,不计通过;等待同 target rustc 退出后直接运行 `E:\cargo-targets\zircon-ibl-live-pmrem-readback-seam-0708\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe graphics::scene::scene_renderer::environment::ibl_bake_runtime_writeback::tests::runtime_graph_writeback_reads_pmrem_graph_output_and_preserves_readback_seams --exact --nocapture --test-threads=1` 通过 1/1,测试本体 3.28s,7310 filtered;`git diff --check -- zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_runtime_writeback.rs` 通过;交互查看器 target 清理后 `CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过(17m21s,仅既有 warnings),`zircon_shader_pbr_viewer.exe --help` 通过,二进制 69,803,008 bytes。 | 关闭 live PMREM graph-output readback/runtime-cache seam gate;仍需 live IEM graph-output parity、scheduler/product 注入与二次启动 dispatch=0、importer/staged derived artifact seam 对拍、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3x WGPU PMREM readback seam guard | shader_plan06_wgpu_pmrem_readback_seam_passed | 已验证:补上 GPU texture readback artifact 边界的 PMREM seam 守卫,防止 `Rgba16Float` cube texture 读回时 face/mip/row 布局打乱导致粗 mip 接缝或 artifact payload 失真。`read_ibl_bake_artifact_sections.rs` 新增 `readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip`:构造 seam-stress PMREM/SH9 payload,写入真实 WGPU cube texture 与 SH9 buffer,再经 `read_ibl_bake_artifact_wgpu_sections(...)` 读回为 sections、组装 payload、应用回 source cubemap 并量化 mid/rough cube-edge luma seam。 | 新增 backend lib-test `graphics::backend::render_backend::read_ibl_bake_artifact_sections::tests::readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip`;更新 `docs/zircon_runtime/graphics/backend-ibl-artifact-readback.md`,`docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-readback.md`,`docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`。本切片不生成新 PNG。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\backend\render_backend\read_ibl_bake_artifact_sections.rs` 通过;E 盘剩余空间 `46904709120` bytes,低于 50GB 阈值,按 target policy 重建并清理 `E:\cargo-targets\zircon-ibl-wgpu-readback-seam-0708`(`cargo clean` removed 1 file/354B);`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-readback-seam-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip --no-default-features --features core-min --locked --jobs 1 --color never -- --nocapture --test-threads=1` 两次在编译/链接阶段超过工具窗口(1204s/604s),不计通过;等待同 target cargo/rustc 全部退出后,最终生成的 `E:\cargo-targets\zircon-ibl-wgpu-readback-seam-0708\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe graphics::backend::render_backend::read_ibl_bake_artifact_sections::tests::readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip --exact --nocapture --test-threads=1` 通过 1/1,测试本体 1.81s,7308 filtered。 | 关闭 backend texture-copy/readback layout 层面的 seam-preservation gate;仍需 live compute-produced PMREM/IEM artifact readback seam 对拍、importer/staged derived artifact seam 对拍、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3w artifact PMREM seam roundtrip guard | shader_plan06_artifact_pmrem_seam_roundtrip_passed | 已验证:补上 derived/runtime artifact payload 边界的 PMREM seam 守卫,防止 PMREM 经 RGBA16F payload 编码、blob 解码和 `source_cubemap_mip_chain_with_bake_artifact(...)` 应用后破坏粗 mip 接缝统计。新增独立测试文件 `runtime_environment_ibl_bake_artifact_seam_contract.rs`,避免继续扩展已接近 1000 行的 `runtime_environment_ibl_bake_artifact_contract.rs`;测试构造高频 seam-stress 环境,对 artifact roundtrip 前后的 mid/rough mip cube-edge luma mean/max 做容差对拍,并重新断言 artifact-applied rough mips 继续降低 mean 和 worst seam delta。 | 新增非 ignored 合同测试 `runtime_environment_ibl_bake_artifact_pmrem_roundtrip_preserves_seam_metrics`;更新 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md` 记录 artifact seam roundtrip。该切片不生成新 PNG。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_environment_ibl_bake_artifact_seam_contract.rs` 通过;因 E 盘剩余空间低于 50GB 阈值,先清理并重建专用外部 target `E:\cargo-targets\zircon-ibl-artifact-seam-0708`;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-artifact-seam-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_seam_contract --no-default-features --features core-min --locked --jobs 1 --color never -- --nocapture --test-threads=1` 通过 1/1,测试本体 1.83s,冷构建 18m57s,stderr 只有当前 workspace 既有 warnings。 | 关闭 artifact payload roundtrip 层面的 seam-preservation gate;仍需 GPU-produced texture readback artifact 与 importer/staged derived artifact 的 PMREM seam 对拍、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3v cardinal 120-degree mirror validation + interactive PBR HDRI viewer | shader_plan06_cardinal_120deg_interactive_viewer_passed | 已验证:按用户要求新增上下左右分别旋转 120 度的真实 HDRI 镜面球 contact sheet,并编译一个可用鼠标手动控制镜头的 PBR HDRI 查看器。`scene_fixtures.rs` 新增 `perspective_orbit_degrees(...)`,以镜面球中心为目标、固定半径生成 up/down/left/right 四个 120 度视角;`runtime_shader_pbr_hdri_export.rs` 新增 cardinal 120-degree 导出与 saved-PNG environment/source-reference 两道断言;`zircon_app/src/bin/shader_pbr_viewer.rs` 新增 `zircon_shader_pbr_viewer`,默认加载 `docs/tests/runtime/shader/assets/polyhaven_lakes_2k.hdr`,左键拖拽绕球旋转、滚轮缩放,走同一 `EnvironmentExtract::source_cubemap(...)` 和 standard PBR 渲染路径。 | 新增截图 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_cardinal_120deg_reflection_20260708.png`,1600x1200,2,099,569 bytes,SHA256 `0E57C496E49B6044F9AA5495BBFE855D5EB575A8D5AFAA8CB55B3DFF1D18AF15`;查看器可执行文件 `E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe`,69,941,248 bytes;更新 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md` 记录四向 120 度验证、查看器用法与最新证据。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\scene_fixtures.rs zircon_app\src\bin\shader_pbr_viewer.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo check -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过;同 target `cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过;`zircon_shader_pbr_viewer.exe --help` 通过并确认默认 HDRI 与鼠标控制说明;旧 target 的 Cargo 包装导出先遇到 dep-info 写入错误,新外部 target 的 Cargo 包装命令在 20m 超时但生成测试二进制,随后直接运行 `E:\cargo-targets\zircon-shader-120deg-0708\debug\deps\runtime_shader_pbr_hdri_export-810bef67c8d69fb9.exe export_runtime_shader_pbr_real_hdri_mirror_cardinal_120deg_png --ignored --exact --nocapture --test-threads=1` 通过 1/1,161.57s;同二进制 `runtime_shader_pbr_real_hdri_mirror_cardinal_120deg_png_matches_environment_metrics` 通过 1/1,0.75s,`runtime_shader_pbr_real_hdri_mirror_cardinal_120deg_png_matches_source_reference_metrics` 通过 1/1,125.40s;同名 PNG 在 repo `target`、`E:\cargo-targets\zircon-shader-120deg-0708` 与 `E:\cargo-targets\zircon-cmft-skybox-0707` 扫描 0。 | 关闭用户要求的 up/down/left/right 120-degree saved-PNG gate 与手动查看器编译交付;EC-M3 仍保留 GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-08 | EC-M3u mirror multi-view source-reference camera-basis gate | shader_plan06_mirror_multi_view_source_reference_passed | 已验证:针对多视角验证继续收紧,2x2 镜面球 contact sheet 不再只做方向/掠射启发式检查。`frame_assertions.rs` 新增 `assert_mirror_sphere_matches_source_reference_with_camera_view(...)`,按 `SinglePbrSphereCameraView` 的 eye/target/projection/ortho_size 重建镜面球反射参考;相机 basis 与 `Transform::looking_at(...)` 一致,即 forward=`target-eye`,right=`forward x world_up`,up=`right x forward`。旧单张正交/透视截图仍保留 legacy-front source-reference 路径,避免把新多视角相机假设错误套到已归档旧基线。 | 新增非 ignored 测试 `runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_source_reference_metrics`,逐格读取 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_multi_view_reflection_20260707.png` 并对正交正面、透视正面、透视左偏航、透视右偏航全部执行 source HDRI/PMREM mip0 参考相关性、SSIM 与左右掠射参考平衡检查;更新 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`。本切片不生成新 PNG,复用既有 1600x1200 contact sheet,1,857,183 bytes,SHA256 `8E39454F46B8A30530A648FFD5813FEF8307BA1F0CC3C81991E4CF47F9536E2B`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\frame_assertions.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_mirror_reflection_png_matches_source_reference_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 122.82s;直接运行 `E:\cargo-targets\zircon-cmft-skybox-0707\debug\deps\runtime_shader_pbr_hdri_export-ddba9a07f2a7f0d8.exe runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_source_reference_metrics --exact --nocapture --test-threads=1` 通过 1/1,117.13s;同二进制 `runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_orientation_metrics --exact --nocapture --test-threads=1` 通过 1/1,0.78s;repo `target`、`E:\cargo-targets\zircon-cmft-skybox-0707` 与 `E:\cargo-targets\zircon-cmft-skybox-0708-multiview` 同名 PNG 扫描 0。一次干净外部 target 的 `CARGO_INCREMENTAL=0 cargo test ... --locked` 被当前工作区无关 UI 编译错误阻断:`parse_hex_color` 未导出、`ensure_font_asset_record` 不可见、`ScreenSpaceUiVertex` 私有导入;不计为通过证据。 | 关闭 EC-M3 的非正面相机 strict source-reference saved-PNG gate;EC-M3 仍保留 GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3t mirror sphere multi-view HDRI validation | shader_plan06_mirror_multi_view_reflection_passed | 已验证:针对"请你也进行一下多视角验证",镜面球验证不再只看正交/透视正面两张图。`runtime_shader_pbr_hdri_export.rs` 新增 2x2 contact-sheet 导出,四格分别为正交正面、透视正面、透视左偏航、透视右偏航;`scene_fixtures.rs` 新增 `SinglePbrSphereCameraView` 和 `Transform::looking_at` 相机写场景路径,并删除旧的未使用投影-only 测试辅助。保存图读回测试会逐格检查真实 HDRI 天空上下差异、镜面上/下方向和左右掠射平衡。 | 新增截图 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_multi_view_reflection_20260707.png`,1600x1200,1,857,183 bytes,SHA256 `8E39454F46B8A30530A648FFD5813FEF8307BA1F0CC3C81991E4CF47F9536E2B`;新增非 ignored 测试 `runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_orientation_metrics`;新增测试文档 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\scene_fixtures.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export export_runtime_shader_pbr_real_hdri_mirror_multi_view_png --no-default-features --features core-min --locked --jobs 1 --color never -- --ignored --exact --nocapture --test-threads=1` 通过 1/1,测试本体 170.43s;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_orientation_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 0.70s;repo `target` 与 `E:\cargo-targets\zircon-cmft-skybox-0707` 同名 PNG 扫描 0。 | 多视角 saved-PNG gate 已关闭;EC-M3 仍保留 strict source-reference SSIM 扩展到非正面相机、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3s BRDF LUT grazing energy clamp + mirror source-reference compare | shader_plan06_brdf_grazing_energy_clamp_mirror_ref_passed | 已验证:针对"掠射特征不像真实、左右掠射不一致、右侧过分掠射"继续收敛。根因复核显示镜面球大结构与直接 HDRI 几何参考一致,但 split-sum BRDF LUT 在 `roughness=0`、低 `NoV` 时 `A+B` 可超过 1,会让 `F0=1` 完美金属镜面在掠射边缘非物理增亮;右侧若反射到 HDRI 亮内容会被放大。`environment_brdf_lut.rs` 现对 `scale+bias>1` 的 texel 做比例归一,`zr_environment.wgsl` 对采样 BRDF 系数夹到 `[0,1]`。 | 新增/更新 `runtime_environment_brdf_lut_contract.rs` 的完美镜面低 `NoV` 能量守卫,并新增模块文档 `docs/zircon_runtime/core/framework/render/environment/environment-brdf-lut.md`。刷新镜面截图:`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` 1280x960,909,652 bytes,SHA256 `35F0711D0990EC1825DC6C5D734ED6F3BC5ADD51C953280A3928DA5D0E1CCE61`;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` 1280x960,1,016,502 bytes,SHA256 `A610CDA7C5F095E3EDDA5D5F97F5A9FD296B888AFF6027A96B6063827F925FF1`。新增 CPU 参考对拍图:`runtime_shader_pbr_real_hdri_lakes_persp_cpu_ref_compare_20260707.png` 与 `runtime_shader_pbr_real_hdri_lakes_ortho_cpu_ref_compare_20260707.png`,结构 SSIM 分别为 `0.9596`/`0.9573`;刷新后外环透视 left/right luma `138.68/130.11`,mean ratio `0.938`,stddev ratio `1.012`;正交 left/right luma `152.44/149.33`,mean ratio `0.980`,stddev ratio `1.010`。 | 直接运行 `E:\cargo-targets\zircon-cmft-skybox-0707\debug\deps\zircon_runtime-1ff53e05a9088131.exe environment_brdf_lut_conserves_smooth_perfect_mirror_grazing_energy --nocapture --test-threads=1` 通过 1/1;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export export_runtime_shader_pbr_real_hdri_mirror_reflection_png --no-default-features --features core-min --locked --jobs 1 --color never -- --ignored --exact --nocapture --test-threads=1` 通过 1/1,测试本体 304.72s,总 13m41s;stderr 仅当前 workspace 既有 warnings。 | 本轮关闭 BRDF LUT 掠射能量放大回归并保留直接 HDRI 参考对拍图;EC-M3 仍保留把 CPU reference compare 固化为 Rust saved-PNG 测试、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3r saved PMREM mip diagnostic blur gate | shader_plan06_saved_pmrem_mip_diagnostic_blur_metrics_passed | 已验证:针对用户最初指出的 cubemap/PMREM mipmap 马赛克与"mipmap 应当进行模糊算法处理"问题,既有 1K angular source/PMREM 诊断图现在具备 saved-PNG 自动断言。测试读取诊断图上半 source mip tiles 与下半 PMREM mip tiles,比较每个 mip 六个 face 的高频能量,锁定 PMREM rough mips 必须比同级 source mips 更模糊。 | 新增非 ignored 测试 `runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png_matches_blur_metrics`;`hdri_metrics.rs` 新增 `assert_saved_pmrem_mip_diagnostic_blur_response(...)` 与 tile 高频能量 helper。被验证图为 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_angular_source_pmrem_mip_diagnostic_20260706.png`(864x1152),本切片不生成新 PNG。当前诊断能量: mip0 source/PMREM 相等约 `9.073/9.073`;mip1 source/PMREM 约 `7.782/1.950` ratio `0.251`;mip1..7 平均 ratio 约 `0.666`;断言要求每个非平 rough mip 的 PMREM 能量 <= source×0.92+0.05,平均 ratio <=0.75,且 mip1 <= source×0.55。 | `CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png_matches_blur_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 0.29s,总 9m35s;随后直接运行最新 `runtime_shader_pbr_hdri_export-ddba9a07f2a7f0d8.exe` 中 `runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics` 通过 1/1,0.46s,`runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png_matches_blur_metrics` 通过 1/1,0.35s。 | 本轮关闭 saved PMREM mip diagnostic blur gate;EC-M3 仍保留 strict source-cubemap/reference SSIM、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3q saved 2K PBR matrix high-frequency roughness gate | shader_plan06_saved_png_high_frequency_roughness_metrics_passed | 已验证:EC-M3 的截图级 roughness 单调验收新增定量守卫,不再只看整表亮度/颜色响应。`runtime_shader_pbr_hdri_export/hdri_metrics.rs` 现在对已保存的 2K 10x10 HDRI PBR 矩阵图采样高金属列中心内圈高频能量,避开球体轮廓边缘,要求 mid/smooth rows 的局部反射细节能量高于 rough rows,防止 PMREM roughness mip 未被使用或粗糙度响应被压平。 | 新增 `assert_high_metal_smoothness_increases_reflection_detail(...)`、`average_high_frequency_energy_for_cells(...)` 与 `pbr_matrix_cell_high_frequency_energy(...)`;被验证截图仍为既有 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_10x10_cosine_pmrem_reflection_20260706.png`,未生成新 PNG。当前采样指标:高金属列 rough rows 高频能量约 `15.52`,mid rows 约 `17.96`,smooth rows 约 `18.85`;断言要求 mid > rough×1.05 且 smooth > rough×1.10。 | `CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 0.46s,总 2m26s;stderr 只有当前 workspace 既有 warnings。 | 本轮关闭 saved 2K PBR matrix 的高频 roughness 截图守卫;EC-M3 仍保留 strict source-cubemap/reference SSIM、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3p frame render-region projection aspect + mirror grazing recapture | shader_plan06_frame_projection_aspect_grazing_exports_passed | 已验证:用户指出的"左右掠射不一致、右侧过分掠射"不是 cmft face 轴或 PMREM 滤波继续漂移,而是 scene uniform 在截图/离屏路径中用旧 `RenderFrameExtract.view.effective_render_size()` 计算投影矩阵;当 extract 未被 viewport size 回填时会退回 1x1,而实际渲染目标是 1280x960,导致镜面球投影比例和边缘反射几何失真。`SceneUniform::from_frame(...)` 现改用 `frame.render_region().local_size()` 作为 `ViewProjectionMatrixPair::from_camera(...)` 的尺寸来源,使正交/透视矩阵与实际 render pass 局部尺寸一致。 | 新增 `scene_uniform_uses_frame_render_region_size_for_projection_aspect` 回归断言:在旧 extract 仍为 1x1 的情况下,1280x960 正交帧的 uniform projection x/y scale 固定为 4:3 正确比例 `0.75/1.0`;刷新镜面截图:`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` 1280x960,909,796 bytes,SHA256 `D22CF31B3BF9742B4077E4A52608FC68EE9F47DD492241E26FB8D4847D4E62A8`;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` 1280x960,1,016,494 bytes,SHA256 `C5D273AFD58880BD03295BF2077975ED1844A0B78C0F4D81A8B5159358107572`。外环采样:透视 left/right luma `138.24/133.67`,mean ratio `0.967`,stddev ratio `0.985`;正交 left/right luma `137.40/122.42`,mean ratio `0.891`,stddev ratio `0.936`,右侧不再异常高于左侧。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --color never` 在 `E:\cargo-targets\zircon-cmft-skybox-0707` 通过(既有 warning 415);直接运行 `E:\cargo-targets\zircon-cmft-skybox-0707\debug\deps\runtime_shader_pbr_hdri_export-ddba9a07f2a7f0d8.exe export_runtime_shader_pbr_real_hdri_mirror_reflection_png --ignored --exact --nocapture --test-threads=1` 通过 1/1,225.23s;直接运行 `runtime_shader_pbr_real_hdri_mirror_reflection_png_matches_orientation_and_grazing_metrics --exact --nocapture --test-threads=1` 通过 1/1,0.64s;同名 PNG 在 repo `target` 与 `E:\cargo-targets` 扫描均为 0。`cargo test -p zircon_runtime scene_uniform_uses_frame_render_region_size_for_projection_aspect --lib ...` 在当前工作区先遇到一次 rustc 非诊断退出,复跑 364s 超时,因此不作为通过证据;该断言已由 `cargo check` 编译覆盖,后续 full lib-test 仍需 CI/长时窗口补跑。 | 本轮关闭镜面球掠射几何尺寸错配回归;EC-M3 仍保留 strict SSIM/source-cubemap reference、cube seam 自动量化、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3o standard material normal-map feature guard + mirror grazing balance | shader_plan06_standard_material_normal_guard_grazing_exports_passed | 已验证:用户指出的左右掠射不一致、右侧过度掠射问题收敛;根因不是 cubemap 方向轴,而是 StandardPBR 模板在材质没有 normal map 时仍采样 renderer-owned neutral normal 贴图,该占位贴图的量化值会轻微扰动镜面球法线,在完全镜面和掠射边缘被放大成单侧异常高光/噪声 | 新增 `ShaderFeatureBits::HAS_NORMAL_TEXTURE`,由 `PipelineKey.has_normal_texture` 与 `StandardMaterialDescriptor.normal_texture` 派生到 shader variant/template define;`material_surface.rs` 的 `standard_material_sampled_normal(...)` 在 `!ZR_FEATURE_HAS_NORMAL_TEXTURE` 时直接返回几何法线,只有真实 normal map 材质才构建 tangent frame 并采样 normal 贴图;模板/mesh pipeline source 测试覆盖 true/false define;`frame_assertions.rs` 新增 `MirrorSphereGrazingBalanceStats`,对正交/透视镜面球左右外环 luma/stddev 做平衡断言。刷新镜面图:`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` 1280x960,1,033,454 bytes,SHA256 `9B294F637F66CAB25DBD1B0C711C557C55749ABFB967837D0D41CC7EDEF13825`;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` 1280x960,897,232 bytes,SHA256 `072DE7EDD7A3D9522B8350FFF4F9401F1DD3F7A14285C22923302F5DAAFE3727`。 | 直接运行 `E:\cargo-targets\zircon-cmft-skybox-0707\debug\deps\runtime_shader_pbr_hdri_export-ddba9a07f2a7f0d8.exe export_runtime_shader_pbr_real_hdri_mirror_reflection_png --ignored --nocapture --test-threads=1` 通过 1/1,281.59s;截图指标:透视外环 left/right luma `152.59/162.11` ratio `1.062`,stddev ratio `1.535`;正交外环 luma `141.15/129.07` ratio `0.914`,stddev ratio `0.972`;同名 PNG 在 repo `target` 与 `E:\cargo-targets` 扫描均为 0,验证图只在 `docs/tests/runtime/shader`。`cargo test -p zircon_runtime normal_texture --lib --no-default-features --features core-min --locked` 曾被当前工作区既有 `NativeBitmapAtlasPrepareReport.approximate_raster_image_count` UI text 测试编译错误阻断,不计为本切片失败或通过。 | 本轮关闭无 normal map 镜面材质的法线占位采样回归和左右掠射平衡回归;EC-M3 仍保留 strict SSIM/reference compare、cube seam 自动量化、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3n cmft/cmftStudio PMREM + skybox ray/fixCubeLookup alignment | shader_plan06_cmft_pmrem_skybox_fixcube_exports_passed | 已验证:按用户反馈重新精读 cmft/cmftStudio shader 与滤波实现后,当前运行时验证链对齐 cmftStudio 采样行为;source cubemap skybox 不再用固定屏幕 `+/-Z` 猜方向,而是由 `inverse_view_proj` 与相机位置/正交标记重建世界方向;PMREM 从 UE 对数 roughness/FIS bridge 改为 cmft Blinn/cosine-power radiance lobe,roughness→LOD 改为线性,source/specular/IEM/skybox cube lookup 加 `fixCubeLookup` | `source_cubemap/pmrem.rs` 用 `specularPowerFor` 等价公式、`BlinnBrdf(power/4+1)`、`CMFT_FILTER_THRESHOLD=0.00001` 与 Hammersley cone 采样生成模糊 mip;`source_cubemap.rs` 的 roughness/mip 双向映射改为 `roughness * (mipCount-1)` 与 `mip/maxMip`;`zr_environment.wgsl`/`skybox_procedural.wgsl` 增加 cmftStudio `fixCubeLookup` 采样端修正;`skybox_procedural.wgsl` 通过 inverse view-projection 重建透视/正交 skybox ray;单镜面球细分提升到 96x192,降低几何 faceting。刷新镜面图并覆盖 EC-M3m 哈希:`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` 1280x960,1,011,195 bytes,SHA256 `B3D9EC6B22671BF6E663BDBD130CDFE72BAB8F3AA059B3C25BC44E258721819E`;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` 1280x960,827,376 bytes,SHA256 `8515068DAF9716AB8AA2DDD2B6A391C9A3F3035C4CC83663663141C4D8A9A142`。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime skybox_shader_is_valid_wgsl --lib --no-default-features --features core-min --locked --jobs 1 --color never` 通过;直接运行同 target 的 lib-test 二进制:`source_cubemap_cmft_pmrem` 2/2 通过,`skybox_shader_is_valid_wgsl` 1/1 通过,`skybox_shader_reconstructs_camera_ray_before_source_cubemap_sampling` 1/1 通过,`deferred_lighting_shader_applies_environment_reflections_to_standard_pbr` 1/1 通过;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --no-run --no-default-features --features core-min --locked --jobs 1 --color never` 通过;直接运行 `runtime_shader_pbr_hdri_export` mirror export 二进制 exit 0,1/1 passed,288.44s;同名镜面 PNG 在 repo `target` 与 `E:\cargo-targets` 扫描均为 0,验证图只在 `docs/tests/runtime/shader`。 | 本轮关闭 cmft/cmftStudio shader 采样口径、镜面球白/倒置和 skybox 方向的当前回归;EC-M3 仍保留 strict SSIM/reference compare、cube seam 自动量化、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3m mirror sphere winding + skybox -Z orientation | shader_plan06_mirror_fixture_winding_skybox_orientation_exports_passed | 已验证:用户指出的镜面反射上下/前后倒置已关闭;根因是验证导出用 UV sphere 的三角形绕序与径向法线相反,单面渲染会露出背壳,同时 skybox 默认屏幕方向仍按 `+Z` 采样,与 Zircon 默认相机 `-Z` 视线不一致 | `runtime_shader_pbr_hdri_export/scene_fixtures.rs` 改为外向三角形绕序并保留注释;`skybox_procedural.wgsl` 默认 cubemap sky direction 改为 `vec3(ndc.x, ndc.y, -1.0)`;`frame_assertions.rs` 改为方向稳定断言:上部内侧反射必须更接近上方天空,且 blue-excess 高于底部路面/岸边条带。刷新镜面图: `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` 1280x960,1,010,236 bytes,SHA256 `E6E6E09DEC7EA4014D0A1302641DFD558111B665D8255D0282A78C3289C58992`,upper/lower blue-excess `69.46/9.65`,stats `mean_luma=164.73,luma_std=45.35,mean_sat=0.2234,clip=0.1151`;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` 1280x960,1,055,118 bytes,SHA256 `4DE32672A09851B2A304461FA1E5938ECDA5531D20B9901B04CCDD6BE424BADB`,upper/lower blue-excess `79.14/-6.98`,stats `mean_luma=192.52,luma_std=41.85,mean_sat=0.2419,clip=0.2790`。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-reflection-orientation-0707 cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --color never` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-reflection-orientation-0707-m3m cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --no-run --no-default-features --features core-min --locked --jobs 1 --color never` 通过;直接运行 `E:\cargo-targets\zircon-reflection-orientation-0707-m3m\debug\deps\runtime_shader_pbr_hdri_export-810bef67c8d69fb9.exe export_runtime_shader_pbr_real_hdri_mirror_reflection_png --ignored --nocapture --test-threads=1` exit 0,1/1 passed,227.66s;repo `target`、旧外部 target 与本轮外部 target 同名镜面 PNG 扫描均为 0。 | 本轮关闭 mirror 反射倒置回归;EC-M3 仍保留 strict SSIM/reference compare、cube seam 自动量化、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3l mirror roughness floor + mip0 reflection validation | shader_plan06_mirror_roughness_floor_reflection_exports_passed | 已验证:用户指出的透视/正交镜面球发白问题关闭;根因是 standard material roughness 最低被夹到 `0.04`,按 UE 对数映射会采约 mip2.4,完全镜面永远取不到 mip0;标准材质/模板/GBuffer/deferred/fallback/SSR 的最小 roughness 已统一到 `0.001`,与 `ComputeReflectionCaptureMipFromRoughness` 口径一致 | `STANDARD_MATERIAL_MIN_ROUGHNESS = 0.001` 从 `standard_material.rs` 通过 render facade 暴露,uniform packing、procedural smoothness、WGSL template、GBuffer、deferred lighting、fallback mesh、SSR 和测试夹具全部改用同一口径;`frame_assertions.rs` 新增 mirror stats guard,要求镜面球 luma stddev/saturation/clip ratio 合格,可拦住旧白/灰球。重新导出同名镜面图: `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` 1280x960,1,105,989 bytes,SHA256 `8109D779CCCED49354C2B5FAACF4D6561F12287C7537E7ED621CF80D5C61AB4B`,stats `mean_luma=143.45,luma_std=49.43,mean_sat=0.1031,clip=0.1114`;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` 1280x960,1,202,996 bytes,SHA256 `49414938370F7ECD1AD7B182E3815CE442F7141CE8DE1515BE025DC01FA1651C`,stats `mean_luma=130.58,luma_std=39.95,mean_sat=0.0624,clip=0.0820`。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-reflection-orientation-0707 cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --color never` 通过(既有 warning);同 target 下 `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --no-run --no-default-features --features core-min --locked --jobs 1` 通过;直接运行已构建 `runtime_shader_pbr_hdri_export-3eb8718632dd95b6.exe export_runtime_shader_pbr_real_hdri_mirror_reflection_png --ignored --nocapture --test-threads=1` exit 0,1/1 passed,254.40s;repo `target` 与 `E:\cargo-targets\zircon-reflection-orientation-0707` 同名镜面 PNG 扫描为 0。 | 本轮关闭 mirror 白球/非 mip0 反射回归;EC-M3 仍保留 strict SSIM/reference compare、cube seam 自动量化、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M3k mirror view-vector orientation + ambientCG Metal008/025/029 validation | shader_plan06_mirror_view_direction_and_ambientcg_exports_passed | 已验证:标准 PBR/fallback/deferred 环境反射不再使用固定 `+Z` 视线,而是由 scene uniform 提供相机世界位置与正交/透视视向;完全镜面材质分别用正交和透视相机复拍,并用 ambientCG Metal008、Metal025、Metal029 真实 Color/NormalGL/Roughness/Metalness 贴图复拍真实 HDRI 反射 | `SceneUniform` 新增 `camera_world_position` 与 `camera_view_direction`;`zr_scene_runtime.wgsl`/standard PBR/deferred/fallback shader helper 按透视 fragment->camera 或正交固定 view dir 计算反射向量;`runtime_shader_pbr_hdri_export.rs` 新增 mirror orthographic/perspective 两图与 ambientCG 008/025/029 batch export,fixture 自动把 roughness+metalness 打包为 glTF 风格 metallic-roughness。输出位于 `docs/tests/runtime/shader/`: `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `9667EEBA655665AFF78DD80B1895C11651771DEF87ACC31F1FC9915E6D71460F`, `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `1FA18935B01B6C8199BBFFB18F482899BC6578D124C2EBA4ABC1270204724278`, `runtime_shader_pbr_real_hdri_lakes_ambientcg_metal008_texture_maps_20260707.png` SHA256 `FE62236AE3A3401C1C77893CA97086D4437C58F22B2B7F4C2AE7AE19DA72530A`, `runtime_shader_pbr_real_hdri_lakes_ambientcg_metal025_texture_maps_20260707.png` SHA256 `2C518C372C42D586157A5654833B69B2CE4CA91C0527C58B916468A16536E01A`, `runtime_shader_pbr_real_hdri_lakes_ambientcg_metal029_texture_maps_20260707.png` SHA256 `14DC53B73A92D3D4404F8906769C8084A1B22DC43FA9B341193A6F62E76C1E74`。直接运行已构建 `runtime_shader_pbr_hdri_export` 测试二进制:镜面 export 1/1 通过(264.81s),ambientCG batch 1/1 通过(401.45s);`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1` 通过(既有 warning)。Cargo 包装的 mirror no-run/export 在工具窗口超时,但测试二进制已更新并由直接二进制复跑给出 exit 0。 | 本轮关闭用户指出的材质球反射上下/视向错误与三组真实金属贴图可视验证;后续仍保留 RenderDoc/product capture、strict SSIM/reference compare、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EC-M2an IBL runtime cache Wgpu product capture nonblack | shader_plan06_ibl_product_wgpu_capture_nonblack_dispatch_zero_passed | 已验证:镜像 Plan 11 EL-M1/EC-M2an,前一轮 project-render framework capture 黑帧来自正交 PBR matrix 场景默认选择 Core2D 产品管线,不是 `capture_frame` 读取层;改用透视 Core3D 产品快照后,WgpuRenderFramework 的 `capture_frame` 能输出非黑 PBR matrix/source-cubemap IBL 画面,并继续证明第二帧 runtime cache 命中后 PMREM/SH9/IEM bake executor=0 | `export_runtime_render_ibl_cache_second_launch_dispatch_zero_png` 现在断言两次提交均为 `RenderPipelineHandle(1)`;Wgpu capture PNG `docs/tests/runtime/render/plan11_ibl_product_wgpu_capture_second_launch_dispatch_zero_20260707.png` 已目检,报告 `docs/tests/runtime/render/plan11_ibl_product_wgpu_capture_second_launch_dispatch_zero_20260707.txt` 记录 `first_ibl_executor_count=9`,`second_ibl_executor_count=0`,`post_first_environment_dispatch_count=0`,`first_compute_dispatch_count=12`,`second_compute_dispatch_count=3`,capture report 为 `FrameworkOffscreen` 1280x960。直接运行已构建 lib-test 二进制通过 1/1,日志 `docs/tests/runtime/render/plan11_ibl_product_wgpu_capture_second_launch_dispatch_zero_test_20260707.raw.log`/`.exit.txt` exit 0;本轮还补回 `GlyphAtlasSet` 测试导入以解锁 lib-test 编译。 | Wgpu product capture 非黑帧 gate 关闭;后续继续 RenderDoc product trace、asset-derived artifact 入口、strict screenshot/SSIM/seam gate、4K/16K offline bake 与 full CI。 |
| 2026-07-07 | EC-M2am IBL runtime cache product second-launch dispatch=0 | shader_plan06_ibl_product_second_launch_dispatch_zero_framework_stats_scene_png_passed | 已验证:镜像 Plan 11 EL-M1/EC-M2am,source cubemap runtime cache miss 首帧会在 WgpuRenderFramework 产品提交路径执行 IBL bake graph 并落盘 `.zribl`;同项目同 source cubemap 第二帧命中 cache 后不再调度 PMREM/SH9/IEM bake executor,环境 dispatch 解析为 0 | 新增 ignored 产品测试 `export_runtime_render_ibl_cache_second_launch_dispatch_zero_png`:首帧 `first_ibl_executor_count=9`,`first_compute_dispatch_count=10`,cache hit 解析 `post_first_environment_dispatch_count=0`;第二帧 `second_ibl_executor_count=0`,`second_compute_dispatch_count=1`。可视证据写入 `docs/tests/runtime/render/plan11_ibl_product_second_launch_dispatch_zero_20260707.png`,计数报告写入同名 `.txt`;最终 focused Cargo 日志 `plan11_ibl_product_second_launch_dispatch_zero_mixed_proof_test_20260707.raw.log`/`.exit.txt` exit 0。Wgpu `capture_frame` 对该 project-render path 仍黑帧,PNG 使用同项目同环境 SceneRenderer 产品截图路径,不把黑帧 capture 计为通过。 | 关闭 product second-launch dispatch=0 验收;后续继续 Wgpu capture 非黑帧/RenderDoc product capture、asset-derived artifact 入口、strict screenshot/SSIM/seam gate、4K/16K offline bake 与 full CI。 |
| 2026-07-06 | EC-M3j 10x10 HDRI PBR matrix + single sphere + texture-map validation | shader_plan06_hdri_pbr_10x10_single_textured_exports_passed | 已验证:8x8 验收升级为 10x10 metallic 0..1 / smoothness 0..1 矩阵,并新增单金属球与真实 PBR 贴图材质球,三者都在 Poly Haven lakes HDRI skybox 中采样 source cubemap/PMREM 环境反射 | `runtime_shader_pbr_hdri_export.rs` 拆出 `scene_fixtures.rs`、`fixture_assets.rs`、`frame_assertions.rs`,矩阵常量升为 `PBR_MATRIX_DIMENSION = 10`,真实贴图使用 ambientCG Metal009 1K Color/NormalGL/Roughness/Metalness 并在临时验证项目中打包 metallic-roughness。输出写入 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_10x10_cosine_pmrem_reflection_20260706.png`、`runtime_shader_pbr_real_hdri_lakes_single_metal_sphere_reflection_20260706.png`、`runtime_shader_pbr_real_hdri_lakes_ambientcg_metal009_texture_maps_20260706.png`;direct test binary 分别通过 1/1,非 ignored saved-PNG metrics 通过 1/1,target 与 `E:\cargo-targets` 同名 PNG 扫描为 0。 | 10x10/single/textured 截图验收关闭本轮用户可视检查;RenderDoc/product capture、SSIM against reference、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M3i Unreal angular source-cubemap mip implementation + diagnostic | shader_plan06_source_cubemap_angular_mips_contract_and_diagnostic_passed | 已验证:source cubemap mip owner 从设计锁定进入代码实现,天空盒 source mip pyramid 不再是低分辨率 sampled-equirect/nearest-like 块状链,PMREM 继续独立 GGX/cosine 卷积 | 新增 `source_cubemap/mipmap.rs`,由 mip0 构建 average input chain,再按输出 texel angular footprint 做 4x4 sub-sample、exact cubemap solid-angle 加权和 cross-face linear sampling,final 1x1 六面平均;`source_cubemap.rs` 调用该 owner。新增/更新 `runtime_environment_source_cubemap_contract` 高频 blur 与 roughest face-average 断言,直接运行生成二进制通过 12/12。诊断图写入 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_angular_source_pmrem_mip_diagnostic_20260706.png`,SHA256 `FD448CF295A6FCEE139994F2516C0C729086C279615D47C7B114C820FEF8C692`,target 与 `E:\cargo-targets` 同名 PNG 扫描为 0。 | 该切片关闭当前 CPU bridge source mip 马赛克问题;GPU/offline derived `.zcube` source mip bake、asset importer 持久化、RenderDoc/product capture 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2al IBL runtime graph scheduler + cache writeback | shader_plan06_ibl_runtime_scheduler_cache_writeback_core_min_cargo_passed_test_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2al,source cubemap runtime cache miss 会进入最终 compile options,使 PMREM/SH9/IEM bake graph 在产品渲染路径中自动追加;默认 SceneRenderer 注册 IBL compute executors,并在 transient graph resource 释放前把 runtime graph 输出写回 cache | 新增 build-frame submission 的 `environment_ibl_compile_options.rs` miss/hit 判定,`RenderPipelineCompileOptions`/`CompiledRenderPipeline` 承载 `IblBakeArtifactRequest`,`pass_authoring.rs` 将 bake graph pass 挂到 AmbientOcclusion stage,`submit_compiled_scene_frame.rs` 在 queue submit 后执行 runtime cache writeback closeout。该切片不改 PMREM/SH9/IEM WGSL 算法;验证图写入 `docs/tests/runtime/render/plan11_ibl_runtime_scheduler_validation_20260706.png`。 | `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-runtime-scheduler-0706 --message-format short --color never` exit 0;focused `environment_ibl` lib-test 在 600s 工具窗口内停在测试 harness 构建/链接阶段,已记录 `docs/tests/runtime/render/plan11_ibl_runtime_scheduler_tests_20260706.*`,不计通过。 | 该切片关闭 runtime scheduler/product graph injection/default executor registration/cache writeback 接线;focused tests green、产品级 second-launch dispatch=0、RenderDoc/product capture、asset-derived artifact 入口、strict screenshot/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ak IBL source cubemap graph binding | shader_plan06_ibl_source_cubemap_graph_binding_cargo_check_passed_test_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ak,当前帧 source cubemap 的 uploaded WGPU view 现在能在 compiled scene render 资源绑定阶段导入 `environment.ibl.source_cubemap`,IBL bake graph 不再只停留在测试手工导入 source input | 新增 `bind_environment_ibl_graph_resources.rs`,按 compiled graph resource lifetime 判断是否存在 required external texture `environment.ibl.source_cubemap`,存在时导入 `SceneEnvironmentCubemap::source_view()` borrowed view;无 frame source cubemap view 时保持 missing required external,让后续 materialization report 暴露缺失。`render.rs` 在 scene uniform/source cubemap upload 后传递该 view;同步修正 review-guard `SOURCE_METADATA_GUARD_GUARD` 编译阻塞。本切片不改 PMREM/SH9/IEM WGSL 算法,截图验证写入 `docs/tests/runtime/render/plan11_ibl_source_binding_validation_20260706.png`,并核对 Lumen Radiosity compute shader 的 8x8x1 dispatch 输入/输出模式。 | `rustfmt --edition 2021 --check` 覆盖本切片文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-runtime-writeback-0706 --message-format short --color never` exit 0;focused `environment_ibl_source_binder` Cargo test 超过 600s 工具窗口,不计通过,timeout 与空 stdout/stderr 保留在 `docs/tests/runtime/render/plan11_ibl_source_binding_tests_20260706.*`。 | 该切片关闭 source cubemap external input 的生产绑定入口;focused test 绿色、production scheduler/product graph injection、async readback/cache writeback、optimized SH9 reduction、BRDF/IEM 产品接线、dispatch=0 二次启动、strict screenshot/SSIM/seam gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M3h Unreal angular source-cubemap mip design lock | shader_plan06_unreal_angular_source_mipmap_design_locked_no_screenshot | 设计完成:确认 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 是 16x8 sampled-equirect rejected baseline;source skybox、source mip pyramid、specular PMREM 三者职责已在 §2.4/§4.1a 分离 | 精读 `dev/UnrealEngine/Engine/Source/Developer/TextureCompressor/Private/TextureCompressorModule.cpp` 的 long-lat cubemap、普通 texture mip 与 `GenerateAngularFilteredMips`;补充 `env.source_cubemap_mipmap` owner 设计,明确 UE angular cubemap mip 服务 source pyramid/FIS,不替代 §4.2 GGX PMREM;同步 `related_code`。本切片不改生产代码、不生成新截图。 | 仅文档设计锁定,未跑 Cargo;下一步实现 `source_cubemap_mipmap.rs`/WGSL owner 与高频 variance、seam、mip diagnostic 截图,再重新导出 docs/tests/runtime/shader 下的真实 HDRI PBR 验证图。 |
| 2026-07-06 | EC-M2aj IBL bake runtime graph-output cache writeback bridge | shader_plan06_ibl_bake_runtime_graph_writeback_direct_binary_passed_screenshot_reused | 已验证:renderer-local IBL bake 收尾桥可在 transient graph outputs 释放前,按显式 `IblBakeArtifactRequest` 读取已调度 GPU graph 输出并写入 runtime cache;asset/cache hit 时不会触碰 graph resources;SH9 runtime compute miss -> graph readback -> runtime cache -> 第二次 resolve dispatch=0 已有 focused 证据 | 新增 `ibl_bake_runtime_writeback.rs`,从 request 重建 artifact descriptor, runtime compute required 时调用 `read_ibl_bake_artifact_wgpu_sections_from_graph_resources(...)` 后进入 `write_ibl_bake_artifact_runtime_dispatch_readback(...)`,不 required 时走 asset-layer skip;新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-runtime-writeback.md`,同步 readback/compute-executor/asset/core artifact 文档。本切片不生成新截图,复用 2026-07-06 2K cosine PMREM 截图并确认无新 target PNG。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-runtime-writeback-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` 首轮暴露 IBL artifact 只在 `crate::asset::artifact` 下导出的导入路径,修正后复跑 exit 0;Cargo wrapper focused test 604s 超时于后台编译窗口,编译完成后直接运行 `E:\cargo-targets\zircon-ibl-runtime-writeback-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_runtime_writeback --nocapture --test-threads=1` exit 0,2/2 passed、7053 filtered;repo target 与该 cargo-target shader/HDRI PNG 扫描均为 0。 | 该切片关闭已调度 graph 输出到 runtime cache 的 renderer-local closeout bridge;production scheduler 队列/产品图注入、项目 cache-root 选择、PMREM/IEM 产品 artifact writeback、optimized SH9 reduction、BRDF/IEM 产品接线、产品级 dispatch=0 二次启动、strict screenshot/SSIM/seam gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ai IBL bake explicit frame-source request owner | shader_plan06_ibl_bake_explicit_frame_source_request_owner_cargo_passed_screenshot_reused | 已验证:WGPU IBL bake executor 不再从 PMREM 输出资源反推 source cubemap 的 face size/mip count;当前帧 source cubemap environment 明确提供 bake key、face size 与 mip count,graph metadata 只负责组合 PMREM/SH9/IEM required contents,SH9-only graph 不再因为缺 PMREM metadata 失败 | `SourceCubemapEnvironment::ibl_bake_artifact_request(...)` 与 `EnvironmentExtract::source_cubemap_ibl_bake_request(...)` 成为 request shape owner;`ibl_bake_compute_executor.rs` 改为从 graph resources 读取 required contents,再从 frame source cubemap 读取 request shape;旧的 SH9-only failure test 改为 success test 并显式断言 graph 中没有 `environment.ibl.pmrem`;同步更新 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md` 与 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-compute-executor.md`。本切片不生成新截图,复用并验证 2026-07-06 2K cosine PMREM 截图。 | `cargo fmt --package zircon_runtime` 通过;`cargo test -p zircon_runtime --lib ibl_bake_wgpu_request --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-request-owner-coremin-0706 --color never -- --nocapture --test-threads=1` 通过 2/2;`cargo test -p zircon_runtime --lib ibl_bake_request --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-request-owner-coremin-0706 --color never -- --nocapture --test-threads=1` 通过 2/2;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-request-owner-coremin-0706 --color never -- --nocapture --test-threads=1` 通过 1/1;`docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_cosine_pmrem_reflection_20260706.png` 存在且 target/cargo-target 同名 PNG 扫描为 0。 | 该切片关闭 request shape owner 与 SH9-only graph 的 PMREM 反依赖;production scheduler 队列/生命周期、async readback drain before transient release、runtime cache writeback、GPU artifact cache writeback、optimized SH9 reduction、BRDF/IEM 产品接线、dispatch=0 二次启动、strict screenshot/SSIM/seam gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ah IBL bake WGPU readback graph-resource bridge | shader_plan06_ibl_bake_wgpu_readback_resources_direct_binary_passed_cache_writeback_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ah,renderer-local IBL bake 输出在 transient graph resources 生命周期内可以按 artifact descriptor 解析成 backend 读回资源包,为后续 PMREM/SH9/IEM GPU artifact sections 和 runtime cache writeback 提供单一入口 | 新增 `ibl_bake_wgpu_readback.rs`:按 `IblBakeArtifactDescriptor.contents()` 要求 `environment.ibl.pmrem` owned transient texture、`environment.ibl.irradiance_sh9` buffer、`environment.ibl.irradiance_cube` owned transient texture,并提供 `read_ibl_bake_artifact_wgpu_sections_from_graph_resources(...)` 薄入口;SH9-only descriptor 不再被 readback bridge 强制依赖 PMREM shape,缺资源错误包含稳定 graph resource 名;新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-readback.md`,并更新 dispatch/compute-executor 文档。本切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;首次 `CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-readback-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` 在新增测试 `expect_err` 处暴露 backend readback resource packet 无 `Debug` 的测试写法问题,改为显式 `match` 后同命令复跑 exit 0;`cargo test -p zircon_runtime --lib ibl_bake_wgpu_readback ...` Cargo wrapper 904s 超时于测试编译窗口,后台编译结束后直接运行 `E:\cargo-targets\zircon-ibl-wgpu-readback-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_readback --nocapture --test-threads=1` exit 0,4/4 passed、7022 filtered、测试本体 1.90s;`git diff --check` 对触及文件通过且仅报告既有 LF/CRLF warning;repo target 与该 cargo-target 的 shader/HDRI PNG 扫描 `COUNT=0`。 | 该切片只关闭 WGPU bake graph resource acquisition/readback packet bridge;production scheduler request owner、async readback drain before transient release、runtime cache writeback、GPU artifact cache writeback、optimized SH9 reduction、BRDF/IEM 产品接线、dispatch=0 二次启动、strict screenshot/SSIM/seam gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ag WGPU PMREM final 1x1 six-face average | shader_plan06_ibl_pmrem_final_mip_average_wgpu_passed_screenshot_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ag,参照 cmft `cubemapfilter.cpp` 1x1 face average 规则,renderer-local WGPU PMREM 最末级 mip 不再让六个 face 各自保留方向噪声,而是在 final mip dispatch 内写入共同的六面平均 radiance | `ibl_prefilter.wgsl` 新增 `final_pmrem_face_average(...)`,对六个 face axis 各做 cosine prefilter 后平均,仅在 `params.mip_level + 1u >= params.mip_count && mip_size == 1u` 时启用;`ibl_bake_shader_plan.rs` 将 shader content hash 更新到 `0x1b1_ba6e_0003` 并锁定 WGSL 合同 marker;`ibl_bake_wgpu_dispatch.rs` 新增 `final_pmrem_mip_writes_common_six_face_average`,构造非对称 16x16x6 source cubemap、dispatch mip4、读回 `Rgba16Float` 1x1x6,断言六面颜色一致且非零。为解锁本轮 lib-test 编译,同步修复 Runtime 15 child-owner budget 路由的 private glob import 暴露问题。该切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-final-mip-average-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_shader_plan --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,5/5 passed;Windows Cargo wrapper 复跑 `ibl_bake_wgpu_dispatch` 在 lib-test 编译后超过工具窗口,随后直接运行已生成 `E:\cargo-targets\zircon-ibl-final-mip-average-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_dispatch --nocapture --test-threads=1` exit 0,7/7 passed、6995 filtered、测试本体 6.73s,新增 final mip 读回断言通过。 | 该切片只关闭 WGPU PMREM 末级 1x1 六面平均算法/读回证据;production scheduler request injection、async readback/cache writeback、GPU artifact cache writeback、optimized SH9 reduction、BRDF/IEM 产品接线、dispatch=0 二次启动、strict screenshot/SSIM/seam gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2af IBL bake WGPU pipeline cache ownership/reuse | shader_plan06_ibl_bake_wgpu_pipeline_cache_cargo_passed_scheduler_readback_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2af,IBL bake graph-context WGPU dispatch 不再在每个 pass 临时创建 shader module/pipeline layout/compute pipeline,而是要求 renderer 生命周期的 `IblBakeWgpuPipelineCache` 并按 `ComputePipelineCacheKey` + output layout kind 复用 | 新增 `ibl_bake_wgpu_pipeline_cache.rs`,持有 storage-texture/storage-buffer 两套 bind group layout、shader module cache、pipeline layout cache 与 compute pipeline cache;`SceneRendererCore` 构造并跨 stage 传入 `RenderPassGpuExecutionContext`;`record_ibl_bake_wgpu_command(...)` graph-context 路径通过 cache 获取 pipeline,缺少 cache 时明确报错。focused test 锁定 PMREM mip0/mip1 只产生 1 个 shader module + 1 个 pipeline layout + 1 个 compute pipeline,SH9 再产生 storage-buffer 侧第二套缓存;PMREM/SH9/IEM graph-context dispatch 测试均挂载 cache 并提交 queue。本切片不改 WGSL 算法,不生成截图。同步新增 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-pipeline-cache.md`,并更新 binding/command-plan/dispatch 文档。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-dispatch-coverage-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_wgpu_pipeline_cache --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,1/1 passed、6992 filtered,测试本体 0.84s;同 target `cargo test -p zircon_runtime --lib ibl_bake_wgpu_dispatch --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,6/6 passed、6987 filtered,测试本体 3.40s;stderr 只有既有 warnings。 | 该切片关闭 renderer-lifetime IBL WGPU pipeline cache reuse 门;production scheduler request injection、async scheduling/readback/cache writeback、GPU final 1x1 face average、optimized SH9 reduction、product dispatch=0、strict screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ae IBL bake SH9/IEM graph-context WGPU coverage | shader_plan06_ibl_bake_wgpu_dispatch_sh9_iem_graph_context_cargo_passed_readback_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ae,`record_ibl_bake_wgpu_pass_for_request(...)` 的 graph-context 覆盖从 PMREM mip0 扩展到 SH9 storage-buffer 与 IEM storage-texture,三种 IBL bake 输出路径都能从 materialized RenderGraph resources 解析并记录 dispatch audit | `ibl_bake_wgpu_dispatch.rs` 将 PMREM graph-context 测试抽成 `record_graph_context_dispatch(...)`,新增 `graph_context_records_sh9_wgpu_dispatch_from_materialized_resources` 与 `graph_context_records_iem_wgpu_dispatch_from_materialized_resources`;SH9 验证 storage buffer 输出 resource, IEM 验证 `environment.ibl.irradiance_cube` D2Array storage texture 输出 resource,两者都提交 offscreen WGPU queue 并 poll 完成。同步更新 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-dispatch.md`。本切片不改 WGSL 算法,不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-dispatch-coverage-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_wgpu_dispatch --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,6/6 passed、0 failed、6986 filtered,测试本体 3.93s,stderr 只有既有 warnings。 | 该切片关闭 PMREM/SH9/IEM focused graph-context dispatch/audit 覆盖;production scheduler integration、pipeline cache reuse、async scheduling/readback/cache writeback、GPU final 1x1 face average、optimized SH9 reduction、product dispatch=0、strict screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ad IBL bake opt-in executor WGPU request inference | shader_plan06_ibl_bake_compute_executor_wgpu_request_inference_cargo_passed_scheduler_request_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ad,显式 opt-in IBL bake executor 在 `RenderPassExecutionContext` 带 GPU context 时不再只写 metadata dispatch record,而是从当前帧 environment bake key 与 PMREM graph texture metadata 恢复 `IblBakeArtifactRequest`,再进入 `record_ibl_bake_wgpu_pass_for_request(...)` 的真实 WGPU dispatch helper | `ibl_bake_compute_executor.rs` 新增 GPU context 分支:`ibl_bake_request_for_wgpu_context(...)` 要求当前帧存在 IBL bake key,`ibl_bake_request_from_graph_metadata(...)` 要求 graph 中有 6-face Cube PMREM transient texture 来提供 face size/mip count,`ibl_bake_required_contents_from_graph_metadata(...)` 从 PMREM/SH9/IEM graph resources 组合 required contents;无 GPU context 的旧 dispatch-record 派生函数保持原语义。新增 focused tests 锁定 full PMREM+SH9+IEM graph 可从 SH9 pass 恢复 face size/mip count/contents,并锁定 SH9-only graph 因缺 PMREM shape source 明确失败。本切片不改 WGSL 算法,不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-compute-executor-wgpu-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0,stderr 只有既有 warnings;`cargo test -p zircon_runtime --lib ibl_bake_compute_executor --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,6/6 passed、0 failed、6984 filtered,测试本体 0.01s。 | 该切片关闭 opt-in executor 到 WGPU helper 的 request inference 门;production scheduler 仍需拥有显式 request 注入以支持 SH9-only/IEM-only graph、pipeline cache reuse、async scheduling/readback/cache writeback、GPU final 1x1 face average、optimized SH9 reduction、product dispatch=0、strict screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI。 |
| 2026-07-06 | EC-M2ac IBL bake WGPU compute pipeline dispatch bridge | shader_plan06_ibl_bake_wgpu_dispatch_cargo_passed_readback_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ac,PMREM/IEM/SH9 WGSL source 现在能通过 renderer-local command plan 创建 WGPU compute pipeline,PMREM/SH9 直接输出路径可提交 queue,PMREM mip0 graph-context 路径也能解析 materialized graph resources 并记录 dispatch audit | `ibl_bake_wgpu_command_plan.rs` 在每条 command 中携带 `wgsl_source`;新增 `ibl_bake_wgpu_dispatch.rs`,从 command plan 创建 shader module/pipeline layout/compute pipeline,entry point 固定为 `IBL_BAKE_COMPUTE_ENTRY_POINT`,并按 command dispatch groups 编码 compute pass。focused WGPU test 用 PMREM mip0 `texture_storage_2d_array<rgba16float, write>` 与 SH9 storage buffer 各创建 pipeline/bind group 并提交同一 command buffer;graph-context test 从 `RenderPassExecutionContext` 匹配 PMREM mip0 pass,解析 source/output 资源并写入 dispatch record;zero dispatch groups 与缺失 `.mipN` pass name 在进入 GPU lookup 前报错。本切片不改 WGSL 算法,不生成截图。 | `rustfmt --edition 2021 --check` 覆盖 dispatch/binding/command-plan/environment mod 通过;`cargo test -p zircon_runtime --lib ibl_bake_wgpu_dispatch --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-dispatch-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0,4/4 passed、0 failed、6984 filtered,测试本体 5.28s;直接复跑已生成 lib-test binary exit 0,4/4 passed,日志 `docs/tests/runtime/render/plan11_ibl_wgpu_dispatch_direct_tests_20260706.out.log`。 | 该切片关闭 live pipeline creation、command encoding primitive 与 focused PMREM graph-context dispatch/audit;production scheduler integration、pipeline cache reuse、SH9/IEM graph-context coverage、async scheduling/readback/cache writeback、GPU final 1x1 face average、optimized SH9 reduction、product dispatch=0、strict screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2ab IBL bake WGPU storage bind-group resource bridge | shader_plan06_ibl_bake_wgpu_bind_group_direct_tests_passed_pipeline_encoding_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2ab,PMREM/IEM 的 D2Array storage texture 输出与 SH9 storage-buffer 输出现在可落到真实 WGPU bind group,不再只停留在 layout entry 或 output descriptor | `ibl_bake_wgpu_binding.rs` 创建 storage-texture/storage-buffer 两套 bind group layout,并用 `IblBakeWgpuOutputBindingResource` 显式区分 `TextureView` 与 `Buffer` 输出;`create_ibl_bake_wgpu_bind_group(...)` 复用 command plan 的 params/source/sampler/output slot,在调用 WGPU 前拒绝 PMREM/IEM command 绑定 SH9 buffer 等 output-kind mismatch。新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-binding.md`,并同步 command-plan/shader-plan 文档。本切片不改 WGSL,不生成截图。 | `rustfmt --edition 2021 --check` 覆盖 binding/command-plan/environment mod 通过;首轮 `cargo test -p zircon_runtime --lib ibl_bake_wgpu_binding --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-binding-0706 --message-format short --color never -- --nocapture --test-threads=1` 超过 1204s 工具窗口,不计通过;直接运行 `E:\cargo-targets\zircon-ibl-wgpu-binding-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_binding --nocapture --test-threads=1` exit 0,2/2 passed、0 failed、6974 filtered,测试本体 1.77s,日志 `docs/tests/runtime/render/plan11_ibl_wgpu_binding_direct_tests_20260706.out.log`。 | 该切片只关闭 live storage-output bind-group helper;executor resource lookup、pipeline cache、command encoding/submission、async readback/cache writeback、GPU 末级 1x1 六面平均、optimized SH9 reduction、product dispatch=0、strict screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2aa IBL bake WGPU uniform params + sampler ABI | shader_plan06_ibl_bake_wgpu_uniform_params_sampler_check_passed_unit_runtime_blocked | 已验证:renderer-local IBL WGPU command plan 不再只记录参数 map,而是为 PMREM/SH9/IEM 生成 WGSL uniform buffer 的 32-bit word 顺序与 little-endian bytes;同时修复 source cubemap sampler 的 WGPU 29 `mipmap_filter` 类型,保证 FIS/cosine PMREM 真实采样 source mip 时使用线性 mip 过滤 | `ibl_bake_wgpu_command_plan.rs` 新增 `IblBakeWgpuParamsPlan`,PMREM 布局为 `face_size/mip_face_size/mip_level/mip_count/sample_count/pad/roughness_bits/pad`,SH9/IEM 为 16B 布局;`ibl_bake_wgpu_binding.rs` 通过该计划创建 params uniform buffer,并将 `mipmap_filter` 改为 `wgpu::MipmapFilterMode::Linear`;新增 `command_plan_serializes_wgsl_uniform_params_in_layout_order` 静态断言。本切片未生成新截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-params-check-0706-final CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0,测试配置编译检查通过,stderr 只有既有 warnings;同 target 的 focused `cargo test -p zircon_runtime --lib command_plan_serializes_wgsl_uniform_params_in_layout_order ...` 当前未计绿色:完整 lib-test 树在进入该断言前被并行/无关 `runtime_absorption/structure_convention` 生成测试诊断或进程 exit `-1` 阻断。 | 该切片只关闭 uniform byte layout 与 sampler ABI 编译门;真实 executor resource lookup、bind group 接入、pipeline cache、command encoding/submission、async readback/cache writeback、GPU 末级 1x1 六面平均、optimized SH9 reduction、product dispatch=0、strict screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2z IBL bake owned Cube storage-view resource bridge | shader_plan06_ibl_bake_owned_cube_storage_view_cargo_passed_command_encoding_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2z,PMREM/IEM 的 WGPU 输出资源不再只停在 descriptor plan,RenderGraph execution resources 已能从 owned Cube transient texture 创建 shader 需要的 D2Array storage texture view | `RenderGraphExecutionResources::owned_texture_view_with_descriptor(...)` 接收 `ibl_bake_wgpu_command_plan.rs` 可产生的 `wgpu::TextureViewDescriptor`,在创建 view 前校验 format、usage、dimension、mip range 与 array-layer range;`materialization/tests.rs::materialization_exposes_owned_cube_storage_texture_array_views` 用 offscreen WGPU 后端 materialize `Rgba16Float` Cube,再创建 mip2/6-layer D2Array `STORAGE_BINDING` view,并覆盖非法 mip、array 和 usage 错误。本切片更新 graph-execution 模块文档,不改 WGSL。 | `rustfmt --edition 2021 --check` 覆盖 graph-execution 资源/物化相关文件通过;最终 `cargo test -p zircon_runtime --lib materialization_exposes_owned_cube_storage_texture_array_views --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-command-plan-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0,1/1 passed、0 failed、6965 filtered,测试本体 0.97s,stderr 只有既有 warnings。 | 该切片只关闭 PMREM/IEM output storage view 资源出口;真实 source cubemap/params/sampler/output bind group、pipeline cache、compute command encoding/submission、async readback/cache writeback、product dispatch=0、optimized SH9 reduction、strict roughness/SSIM/seam screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2y IBL bake WGPU command/readback plan | shader_plan06_ibl_bake_wgpu_command_readback_plan_focused_tests_passed_wgpu_execution_deferred | 已验证:镜像 Plan 11 EL-M1/EC-M2y,PMREM/SH9/IEM compute kernel plan 已下沉到 WGPU bind-group ABI、per-mip storage-view 与 artifact readback-copy 计划,不再只停留在 WGSL 字符串/dispatch 描述 | 新增 renderer-local `ibl_bake_wgpu_command_plan.rs`;绑定 ABI 与三份 WGSL 一致:uniform params、source cubemap、filtering sampler、storage output;PMREM/IEM storage texture 输出使用 WGPU 可创建的 `Rgba16Float` D2Array view,单 mip、6 layers;PMREM readback 不做 naive whole-mip D2Array copy,而按 face/mip artifact offset 生成 texture copy,SH9 固定 144B buffer copy,IEM 固定 32x32x6 face-major copy。新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-command-plan.md`,并补充 shader-plan 文档边界。 | `rustfmt --edition 2021 --check` 覆盖命令计划与 environment mod 通过;首个 Cargo wrapper 超出工具窗口且没有 exit file,不计通过;直接运行已生成 lib-test 程序 `E:\cargo-targets\zircon-ibl-wgpu-command-plan-0706\debug\deps\zircon_runtime-1ff53e05a9088131.exe ibl_bake_wgpu_command_plan --nocapture --test-threads=1` exit 0,4/4 passed、0 failed、6934 filtered,日志 `docs/tests/runtime/render/plan11_ibl_wgpu_command_plan_direct_tests_20260706.out.log`。 | 该切片只关闭 WGPU command/readback 计划合同;真实 GPU bind group/resource allocation、compute pipeline cache、command encoding/submission、async readback/cache writeback、product dispatch=0、optimized SH9 reduction、strict roughness/SSIM/seam screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2x GPU PMREM WGSL cosine/FIS parity | shader_plan06_pmrem_wgsl_cosine_fis_parity_cargo_passed_command_encoding_deferred | 已验证:renderer-local `ibl_prefilter.wgsl` 的 PMREM 采样合同已与 EC-M2u CPU bridge / Plan 06 §4.2 对齐,不再只走普通 GGX 分支 | `ibl_prefilter.wgsl` 新增 `roughness >= 0.99` cosine hemisphere convolution 分支,从 source mip pyramid 通过 PDF/FIS 选源 mip;GGX 路径同步 Unreal `E.y *= 0.995` 掠射保护、Hammersley `(index+0.5)/N` 居中采样与 texel solid-angle `* 2.0`;`ibl_bake_shader_plan.rs` 将 high-roughness sample count 提到 128,更新 shader content hash,并新增静态合同断言。新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-shader-plan.md`,同步 `source-cubemap.md`。本切片不生成新截图。 | `cargo fmt --package zircon_runtime` 通过;E 盘测试前剩余 83.54GB,未清理 target;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-shader-plan-check-0706 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib ibl_bake_shader_plan --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,5/5 passed、0 failed、0 ignored、6933 filtered、测试本体 0.03s、总耗时 13m52s,stderr 只有仓库既有 warnings。 | 该切片只关闭 GPU PMREM WGSL/dispatch-plan 算法口径;真实 WGPU bind group/storage view 创建、command encoding、async scheduling/readback/cache writeback、GPU 末级 1x1 六面平均、optimized SH9 reduction、product dispatch=0、strict screenshot Laplacian/SSIM、RenderDoc/product capture、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EC-M2w public source-cubemap PMREM contract convergence | shader_plan06_pmrem_public_contract_cosine_convolution_cargo_passed_gpu_kernel_deferred | 已验证:公开 source-cubemap integration contract 不再固定旧 EC-M2r “roughness=1 等于上一层 PMREM downsample”行为,已与 EC-M2u full-roughness cosine convolution 合同一致 | `zircon_runtime/tests/runtime_environment_source_cubemap_contract.rs` 将 `runtime_environment_source_cubemap_saturated_roughness_mip_downsamples_previous_pmrem_mip` 改为 `runtime_environment_source_cubemap_saturated_roughness_mip_uses_cosine_convolution`;测试构造高频 HDRI,计算 roughness=1 选中 PMREM mip,要求它与 ordinary previous-mip trilinear downsample 有可测 luma 差异,并要求饱和 mip 方差低于上一 mip。该切片只修正公开验收合同,不生成新截图。 | `cargo fmt --package zircon_runtime` 通过;首次 `CARGO_TARGET_DIR=E:\cargo-targets\zircon-source-cubemap-pmrem-contract-0706 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 在 Windows 冷编译阶段超过 1204s 工具窗口,后台 cargo/rustc 自然结束后复跑同命令 exit 0,10/10 passed、0 failed、0 ignored、测试本体 6.63s、总耗时 7m23s,stderr 只有仓库既有 warnings。 | 公开 CPU PMREM 合同已与 EC-M2u 对齐;GPU/offline PMREM/SH9/BRDF/IEM WGSL compute、strict screenshot Laplacian/SSIM、RenderDoc/product capture、derived importer/staged build 与 full CI 仍按后续切片执行。 |
| 2026-07-06 | EC-M2v IBL bake shader plan + per-mip PMREM WGPU contract | render_plan11_ibl_bake_shader_plan_per_mip_pmrem_core_min_and_focused_tests_passed_wgpu_command_deferred | 已验证:镜像 Plan 11 EC-M2v,PMREM GPU 合同不再假设单 pass 写全 mip 或 `texture_storage_cube`;每个 mip 独立 pass/dispatch,WGSL 通过 D2Array storage view 写 6 个 cube face layer | 新增 renderer-local `ibl_bake_shader_plan.rs` 与三份 WGSL:`ibl_prefilter.wgsl`、`ibl_irradiance_sh.wgsl`、`ibl_irradiance_cube.wgsl`;shader plan 绑定 source `texture_cube<f32>`、source sampler、PMREM/IEM `texture_storage_2d_array<rgba16float, write>`、SH9 storage buffer;PMREM plan 记录 mip_face_size、mip_level、roughness、sample_count 与 per-mip dispatch groups。 | `rustfmt --edition 2021 --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-shader-plan-check-0706 --message-format short --color never` 通过;focused lib tests 通过:`ibl_bake_graph_plan` 4/4、`ibl_bake_compute_executor` 4/4、`ibl_bake_shader_plan` 4/4,其中 shader plan 测试用 Naga 解析三份 WGSL。 | EC-M2 仍未全关闭:真实 WGPU bind group/storage view 创建、pipeline cache、command encoding、async readback/cache writeback、product second-launch dispatch=0、optimized SH9 reduction、strict roughness/SSIM/seam screenshot gate、RenderDoc/product capture、4K/16K offline bake 与 full CI 继续执行。 |
| 2026-07-06 | EC-M2u full-roughness cosine PMREM tail + PMREM owner split | shader_plan06_pmrem_full_roughness_cosine_convolution_cargo_and_screenshot_passed_gpu_kernel_deferred | 已验证:用户指出的 1K HDRI cubemap/反射“马赛克”问题在 CPU bridge 的 PMREM 生成口径中修正:高粗糙度尾部不再从上一层 PMREM 普通降采样,而是按 UE roughness>0.99 cosine hemisphere branch 从 source mip 金字塔卷积采样;同时把 source-cubemap 的 PMREM 算法拆入子 owner | 新增 `zircon_runtime/src/core/framework/render/environment/source_cubemap/pmrem.rs`,父 `source_cubemap.rs` 降为 799 行、PMREM 子模块 246 行;`build_source_cubemap_from_equirect(...)` 改调用 `pmrem::prefilter_pmrem_mips_from_source(...)`;模块 guard 改为 `source_cubemap_saturated_roughness_mip_uses_cosine_convolution`,断言 roughness=1 mip 不等于上一层普通降采样且方差下降;补 `IblBakeArtifactContents` 的 `BitOr/BitOrAssign` 以支持 `SH9 | IEM` 内容表达,并修正 IBL compute executor 测试对已删除 pass 常量的引用;刷新真实 HDRI 输出文件名为 `runtime_shader_pbr_real_hdri_lakes_cosine_pmrem_reflection_20260706.png`、`runtime_shader_pbr_real_hdri_lakes_2k_cosine_pmrem_reflection_20260706.png` 与 `runtime_shader_pbr_real_hdri_lakes_1k_cosine_pmrem_mip_diagnostic_20260706.png`。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-source-cubemap-pmrem-0706 cargo test -p zircon_runtime --lib source_cubemap --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,15/15 passed、0 failed、6897 filtered;ignored HDRI exports 分别通过 1/1:1K PMREM 诊断测试本体 21.58s、1K PBR 反射矩阵 30.91s、2K PBR 反射矩阵 82.20s。PNG 复核:1K 诊断 864x1152,SHA256 `6F7B37E061A04C3701CD5384F13F5A21159992527C65F21FA3C2D4210B1DAAB1`;1K 矩阵 1280x960,SHA256 `B7BEBB66E806667B35A1B5792D5698607C683931DA7EDB78339DEEED86CD15C5`;2K 矩阵 1280x960,SHA256 `4C974C6D1A43309032B0B1718762F9B9566B19C5D02DF3B954D45E5988831DCD`;`target` 下未发现 `*cosine_pmrem*.png`;外部导出 target 在剩余空间跌破 50GB 后已清理。 | 本轮关闭 CPU bridge 的 full-roughness PMREM 低 mip 普通降采样块状回归与 source-cubemap 文件膨胀风险;真实 GPU/offline PMREM/SH9/BRDF/IEM WGSL compute、async readback queue、derived importer/staged build、product second-launch dispatch=0、strict Laplacian/SSIM/seam screenshot gate、RenderDoc/product capture、4K/16K 离线 bake 与 full CI 继续执行。 |
| 2026-07-06 | EC-M2t IBL bake compute executor plan record | render_plan11_ibl_bake_compute_executor_plan_record_core_min_passed_gpu_kernel_deferred | 已验证:IBL bake 的 PMREM/SH9/IEM executor ids 已有显式 opt-in 注册与 dispatch/audit 记录合同,默认产品 registry 不启用 | 新增 `graphics/scene/scene_renderer/environment/ibl_bake_compute_executor.rs`;executor 校验 source cubemap read 与目标 output write,从 RenderGraph metadata 推导 dispatch groups 并记录 compute dispatch;`RenderPassExecutorRegistry::with_environment_ibl_bake_compute_executors()` 作为显式扩展覆盖三个 IBL executor id,默认 builtin registry 继续排除它们。该切片不编码真实 WGPU compute 命令,不生成 PMREM/SH9/IEM bytes。 | `rustfmt --edition 2021 --check` 对 IBL graph/executor、registry 与 GPU report 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-graph-plan-check-0706 --message-format short --color never` 通过;`cargo test -p zircon_runtime --lib ibl_bake_compute_executor --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-graph-plan-check-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3、6902 filtered。 | EC-M2 仍未全关闭:真实 PMREM/SH9/IEM WGSL compute、bind group/pipeline cache、WGPU 命令编码、async readback queue、runtime cache writeback from GPU outputs、derived artifact importer/staged build、product second-launch dispatch=0、strict roughness/SSIM/seam screenshot gate、RenderDoc/product capture 与 full CI 继续执行。 |
| 2026-07-06 | EC-M2s IBL bake render-graph plan | render_plan11_ibl_bake_graph_plan_core_min_passed_executor_deferred | 已验证:Plan 11 renderer environment 已能声明 IBL bake 所需 RenderGraph async-compute pass、storage outputs、readback roots 与 fixed workload metadata | 新增 `graphics/scene/scene_renderer/environment/ibl_bake_graph_plan.rs`;`append_ibl_bake_artifact_graph_plan(...)` 按 request contents 创建 PMREM `Rgba16Float` cube、SH9 storage buffer、optional IEM `Rgba16Float` cube,并声明 source cubemap required external read、storage write、readback usage、executor ids 与 pipeline labels。该切片只落 RenderGraph 合同,不包含 PMREM/SH9/IEM WGSL compute 内核或 executor 注册。 | `rustfmt --edition 2021 --check` 对 graph-plan 文件与 environment mod 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-graph-plan-check-0706 --message-format short --color never` 通过;focused lib-test wrapper 被当前 UI `UiStateFlags.focused/hovered` 字段漂移挡在测试执行前,不计绿色。 | EC-M2 仍未全关闭:GPU/offline PMREM/SH9/BRDF/IEM bake production、真实 executor/async scheduling/readback queue、derived artifact importer/staged build、product second-launch dispatch=0、strict roughness/SSIM/seam screenshot gate、RenderDoc/product capture 与 full CI 继续执行。 |
| 2026-07-06 | EC-M2r saturated roughness PMREM tail downsample + real HDRI refresh | shader_plan06_pmrem_saturated_tail_downsample_cargo_and_screenshot_passed_strict_laplacian_deferred | 已验证:roughness=1.0 首个被 shader 选中的饱和 PMREM mip 也从上一层 PMREM 降采样,避免第一层饱和粗糙度仍用低分辨率 GGX 重采样源链造成块状反射;真实 HDRI matrix 改为 IBL 主导验证 | `source_cubemap.rs` 将 `filter.roughness >= 1.0` 的全部饱和尾部统一走 `downsample_pmrem_mip_from_previous(...)`;新增模块内 guard `source_cubemap_saturated_roughness_mip_downsamples_previous_pmrem_mip` 与公开 integration contract `runtime_environment_source_cubemap_saturated_roughness_mip_downsamples_previous_pmrem_mip`,共同锁定 roughness=1.0 选中 mip 与上一层 PMREM trilinear 采样一致;`runtime_shader_pbr_hdri_export.rs` 的真实 HDRI 矩阵直射光强改为 `0.0`;`hdri_metrics.rs` 将 high-metal rough/smooth 分组判据从场景相关的 `>30.0` 收敛为 `>10.0` 可测变化;刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_pmrem_mip_diagnostic_20260706.png`(864x1152,954,870 bytes,SHA256 `12BF0D51060CCB0A53C1BCC4A97D6118D0B55004FB8619D46C27B67869DF26A2`)与 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png`(1280x960,1,009,114 bytes,SHA256 `7878B8A2CFF3DE85E1528D88DC86909F7AFC431206BB12FD7EFA3854F2E90606`)。 | `rustfmt --edition 2021` 对 `source_cubemap.rs` 与 source-cubemap integration contract 通过;E 盘剩余空间低于 50GB 前置执行 `cargo clean --target-dir E:\cargo-targets\zircon-pmrem-saturated-tail-0706`,removed 0 files;早前模块内 filtered `--lib source_cubemap_saturated_roughness_mip_downsamples_previous_pmrem_mip` 通过 1/1,最终复跑该 `--lib` 目标被当前工作区无关 UI 单元测试 `UiStateFlags::{focused,hovered}` 字段漂移阻断,未进入本断言;同一合同已镜像到公开 integration 后,`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-pmrem-saturated-tail-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 10/10;非 ignored `runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics` 通过 1/1;两张 PNG 元数据复核与 SHA256 匹配;扫描 `target` 与 `E:\cargo-targets\zircon-pmrem-saturated-tail-0706` 未发现 runtime shader/PBR/HDRI PNG。stderr 只有仓库既有 warnings。 | 本轮关闭“首个饱和 roughness mip 仍可能块状”的 CPU PMREM 回归,并刷新用户可检查的真实 HDRI/PMREM 诊断截图;Plan §6 的任意 metallic 列 Laplacian/SSIM 严格单调、GPU/offline PMREM/SH9/BRDF/IEM bake、render graph async scheduling、derived importer/staged build、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2q IBL runtime dispatch/cache integration | render_plan11_ibl_runtime_dispatch_cache_integration_cargo_passed_gpu_scheduler_deferred | 已验证:§4.7 来源 1/2/3 的 asset-layer dispatch 合同能把 runtime cache read、resolved payload priority、runtime readback writeback 串成可复用闭环 | 新增 `asset/artifact/ibl_bake_artifact_runtime_dispatch.rs`;`resolve_ibl_bake_artifact_runtime_dispatch(...)` 读取 `.zircon-cache` 并合并 asset-derived blob 候选,返回 selected source、payload 与 dispatch count;`write_ibl_bake_artifact_runtime_dispatch_readback(...)` 只在 dispatch report 需要 runtime compute 时写入 readback sections,asset-derived/cache hit 会跳过 stray readback,避免污染 runtime cache。 | `cargo fmt --package zircon_runtime` 通过;focused `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract runtime_environment_ibl_bake_artifact_runtime_dispatch --locked` exit 0,2/2 passed、19 filtered;完整 `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --locked` exit 0,21/21 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;stderr 只有仓库既有 warnings;本切片不生成截图。 | 关闭 runtime readback-to-cache dispatch integration 的 asset-layer 合同;GPU/offline PMREM/SH9/BRDF/IEM bake production、render graph async scheduling、derived artifact importer/staged build、product second-launch dispatch=0、RenderDoc/product capture、4K/16K 离线 bake、strict roughness screenshot/SSIM 与 full CI 仍待后续。 |
| 2026-07-06 | EC-M2p WGPU IBL artifact section acquisition | render_plan11_ibl_wgpu_section_acquisition_cargo_passed_scheduler_deferred | 已验证:backend 可按 artifact descriptor 的 contents 显式判断 PMREM texture、SH9 buffer 与 optional IEM texture 是否必需,并将已存在的 WGPU 资源读回成 `IblBakeArtifactReadbackSections` | `read_ibl_bake_artifact_sections.rs` 增加 `requires_pmrem_texture()`、`requires_irradiance_sh9_buffer()`、`requires_irradiance_cube_texture()` 合同,实际 `read_ibl_bake_artifact_wgpu_sections(...)` 复用该合同后分别调用 RGBA16F cubemap mip-chain readback 与 SH9 buffer readback helper;缺失资源仍返回包含资源名的 `GraphicsError::BufferMap`。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\backend\render_backend\read_ibl_bake_artifact_sections.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib read_ibl_bake_artifact_sections --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-sections-readback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0,3/3 passed、0 failed、6874 filtered,测试本体 0.00s;exit 记录 elapsed 00:09:11.9856058;stderr 只有仓库既有 warnings;本切片不生成截图。 | 关闭 WGPU artifact section acquisition helper;GPU/offline PMREM/SH9/BRDF/IEM bake production、render graph async scheduling、runtime readback writeback dispatch integration、derived artifact importer/staged build、product dispatch=0、RenderDoc/product capture、4K/16K 离线 bake、strict roughness screenshot/SSIM 与 full CI 仍待后续。 |
| 2026-07-06 | EC-M2o WGPU SH9 buffer readback helper | render_plan11_ibl_wgpu_sh9_buffer_readback_helper_cargo_passed_gpu_bake_deferred | 已验证:§4.7 runtime readback acquisition 具备 SH9 compute buffer 的 144B byte layout helper | `read_buffer_bytes.rs` 增加 backend-only buffer readback owner:`read_buffer_bytes(...)` 复制任意 aligned byte range 到 MAP_READ staging buffer;`read_buffer_f32x4_array_bytes(...)` 表达 WGSL `array<vec4<f32>>` layout;`read_buffer_sh9_f32x4_bytes(...)` 固定九个 SH9 coefficient vec4,与 artifact SH9 section 的 little-endian 144B 布局一致。该 helper 只提供 WGPU acquisition primitive,不改变 SH9 算法、shader binding、artifact section validation 或 cache writeback。 | `rustfmt --edition 2021` 对 buffer readback helper 与 backend facade 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib read_buffer_bytes --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-buffer-readback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_wgpu_buffer_readback_helper_cargo_20260706.out.log` 报告 2/2 passed、0 failed、6870 filtered,测试本体 0.00s;exit 记录 elapsed 00:08:55.6792597;stderr 只有仓库既有 warnings。 | 关闭 SH9 buffer byte readback helper;GPU/offline SH9 compute production、PMREM/BRDF/IEM bake、异步调度、runtime writeback dispatch integration、derived artifact/staged build、product dispatch=0、RenderDoc/product capture、4K/16K 离线 bake 与 full CI 仍待后续。 |
| 2026-07-06 | EC-M3g 1K source-vs-PMREM mip diagnostic | runtime_shader_hdri_pmrem_mip_diagnostic_current_source_cargo_passed_target_scan_passed | 已验证:针对用户指出的 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 马赛克问题,新增当前 source-cubemap 链的 mip 诊断导出,把普通 source mip 与 GGX PMREM mip 分开可视化,避免把旧 16x8 sampled-equirect 失败图误判为当前 cubemap/PMREM 结果 | `runtime_shader_pbr_hdri_export.rs` 新增 ignored `export_runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png`,加载 `polyhaven_lakes_1k.hdr` 后输出 6 face × 全 mip 诊断图:上半部为 `source_texels()` 规则 source mip,下半部为 `texels()` UE-style GGX FIS PMREM mip;PNG 写入 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_pmrem_mip_diagnostic_20260706.png`,尺寸 864x1152,942,567 bytes,SHA256 `82F759AC85E72C3CBE75CE5502F4D350AE5C245174081A8A31A8A32F581857B6`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-text-default-fix-0706 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png --nocapture --test-threads=1` 通过 1/1、0 failed、3 filtered,测试本体 22.32s;同 target 复跑 `runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics` 通过 1/1、0 failed、3 filtered,测试本体 0.19s;`runtime_environment_source_cubemap_contract` 在 core-min 下通过 9/9、0 failed、测试本体 4.62s,覆盖 PMREM 高/低频 blur、rough mip seam-energy 与 final 1x1 六面平均;路径扫描确认该诊断 PNG 未出现在 `target` 或 `E:\cargo-targets`。 | 该图是 raw mip/source-vs-PMREM 调试图,不是最终产品截图或 GPU/offline bake 验收。GPU/offline PMREM/SH9/BRDF/IEM bake、derived cache、runtime readback 调度、strict roughness screenshot/SSIM、RenderDoc/product capture、4K/16K 离线 bake 与 full CI 仍按 EC-M2/EC-M3 后续关闭。 |
| 2026-07-06 | EC-M2n WGPU RGBA16F cubemap readback helper | render_plan11_ibl_wgpu_rgba16float_region_readback_helper_cargo_passed_gpu_bake_deferred | 已验证:backend 可读取指定 mip/array-layer 的 RGBA16F texture region,并能按 artifact PMREM 所需 face-major 顺序读取 cubemap mip-chain bytes | 新增 `read_texture_rgba16float_region.rs`;`read_texture_rgba16float_region(...)` 封装 copy_texture_to_buffer、map-read、row padding strip;`read_texture_rgba16float_cube_mip_chain(...)` 按 face→mip 迭代 cubemap layers,返回与 `SourceCubemapMipChain`/`IblBakeArtifactPayload` PMREM section 一致的字节顺序;`SceneEnvironmentCubemap` source/specular/IEM texture usage 增加 `COPY_SRC`;backend facade 导出 helper 供后续 runtime readback acquisition 接入。 | `rustfmt --edition 2021` 对 backend readback helper、backend facade 与 environment cubemap 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib read_texture_rgba16float_region --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-readback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_wgpu_rgba16float_region_readback_helper_cargo_20260706.out.log` 报告 2/2 passed、0 failed、6851 filtered,测试本体 0.00s;exit 记录 elapsed 00:17:19.2055212;stderr 只有仓库既有 warnings。 | 关闭 RGBA16F cubemap readback helper 与 environment cube COPY_SRC 前提;GPU/offline PMREM/SH9/BRDF/IEM bake、SH9 buffer readback、异步调度、runtime readback-to-cache 集成、importer/staged artifact、product second-launch dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2m IBL runtime readback sections writeback | render_plan11_ibl_runtime_readback_writeback_cargo_passed_gpu_acquisition_deferred | 已验证:§4.7 来源 2 的运行期读回字节可组装为 artifact blob 并写入 `.zircon-cache` | 新增 `ibl_bake_artifact_readback.rs` 与 `asset/artifact/ibl_bake_artifact_runtime_writeback.rs`;`IblBakeArtifactReadbackSections` 分别承载 PMREM RGBA16F、SH9 与 optional IEM RGBA16F readback sections,按 descriptor expected size 精确校验并复用 payload decode;`write_ibl_bake_artifact_runtime_readback(...)` stale descriptor 先跳过、不校验 sections、不写文件,current descriptor 则写入 `.zribl` runtime cache 并返回 `IblBakeArtifactRuntimeWritebackReport`;文件 IO 不进入 render core,实际 GPU readback acquisition 不在本切片。 | `rustfmt --edition 2021` 对新增 readback/writeback、facade 与合同测试通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-writeback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_runtime_readback_writeback_current_source_cargo_20260706.out.log` 报告 19/19 passed、0 failed、0 ignored、0 filtered,测试本体 0.49s;exit 记录 elapsed 00:17:45.8058804;stderr 只有仓库既有 warnings。 | 关闭 runtime readback bytes 到 cache artifact 的 writeback 边界;GPU/offline PMREM/SH9/BRDF/IEM bake、实际 WGPU readback acquisition/scheduling、importer/staged derived artifact、product second-launch dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2j runtime IEM carrier + scene binding ABI | 已验证:SourceCubemapEnvironment 可携带 optional IEM,scene group0 第 5 号 irradiance cube 槽位与 shader 开关接入 | `SourceCubemapEnvironment` 新增 `irradiance_cube`、`with_irradiance_cube(...)` 与 getter;`environment_cubemap.rs` 新增 irradiance `Rgba16Float` cube texture/view,无 IEM 时上传 1x1 黑色 fallback,有 IEM 时上传 32x32x6 face-major 数据;`scene_bind_group_layout_entries()` 从 5 槽扩为 6 槽,`shadow_map_renderer.rs` 同步 fallback bind group;`zr_environment.wgsl` 新增 `override ZR_ENV_DIFFUSE_IEM: bool = false`、binding5 `zr_environment_irradiance_cube` 和 `zr_environment_irradiance_cube_color(...)`;真实 HDRI project/export helper 构建并附加 CPU IEM;artifact bridge 新增 `source_cubemap_environment_with_bake_artifact(...)`,可把 PMREM/SH9/IEM payload 应用到环境对象。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;E 盘测试前剩余约 99.7GB;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 12/12、0 failed、0 ignored、0 filtered,测试本体 0.07s;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` 通过 1/1、2 filtered,测试本体 0.21s。两次 exact lib 测试试图运行新增 layout test,但在目标断言前被当前工作区既有 `zircon_runtime/src/graphics/text/cache/tests.rs` 的 `MeasureKey: Default` 编译错误阻断;本切片未生成新截图。 | 关闭 runtime IEM 数据承载与 binding ABI;`ZR_ENV_DIFFUSE_IEM` 默认仍为 false,由后续 engine quality/specialization 切片决定启用策略。GPU/offline IEM bake、runtime readback 写入、derived artifact/staged build、product dispatch=0、严格截图高频/roughness/SSIM、RenderDoc/product capture 与 full CI 仍待后续。 |
| 2026-07-06 | EC-M2l IBL artifact upload key for WGPU cubemap refresh | render_plan11_ibl_artifact_upload_key_wgpu_refresh_cargo_passed_product_dispatch_deferred | 已验证:artifact payload 应用后不改变 bake key,但 WGPU environment cube upload 会感知 PMREM/SH9/IEM payload 内容变化 | `SourceCubemapEnvironment` 新增 `SourceCubemapUploadKey`、`bake_artifact_hash` 与 `texture_upload_key()`;`source_cubemap_environment_with_bake_artifact(...)` 根据 descriptor+bake key+payload bytes 生成 artifact hash;`SceneEnvironmentCubemap::ensure_uploaded(...)` 不再只用 source_revision/source_hash 判断跳过上传,而是用完整 upload key,避免同源不同 artifact payload 时 specular PMREM/IEM GPU 纹理不刷新。 | `rustfmt --edition 2021 --check zircon_runtime/src/core/framework/render/environment/skybox.rs zircon_runtime/src/core/framework/render/environment/source_cubemap_artifact.rs zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_core/environment_cubemap.rs zircon_runtime/src/core/framework/render/environment/mod.rs zircon_runtime/src/core/framework/render/mod.rs zircon_runtime/tests/runtime_environment_ibl_bake_artifact_contract.rs` 通过;首次同命令异常退出且无日志,不计通过;复跑 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-resolve-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_artifact_upload_key_current_source_cargo_retry_20260706.out.log` 报告 16/16 passed、0 failed、0 ignored、0 filtered,测试本体 0.06s;exit 记录 elapsed 00:12:55.2297214;stderr 只有仓库既有 warnings。 | 关闭 artifact 内容变化触发 WGPU source/specular/IEM cube 重传的 upload identity 合同;GPU/offline PMREM/SH9/BRDF/IEM bake 生产、runtime readback 调度写入 cache、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2k IBL artifact resolved payload priority | render_plan11_ibl_artifact_resolved_payload_priority_cargo_passed_gpu_readback_deferred | 已验证:§4.7 来源 1/2 blob-backed artifact 可解析为可直接应用 payload,miss 时保留 runtime compute fallback | 新增 `ibl_bake_artifact_resolution.rs`;`IblBakeArtifactBlobCandidate` 承载 asset-derived/runtime-cache blob 候选,`resolve_ibl_bake_artifact_payload(...)` 复用三来源选择合同并返回 `IblBakeArtifactResolvedPayload`;current asset-derived blob 优先于 current runtime-cache blob,stale derived 会被拒绝并落到 current cache,全 miss 时 `blob()/payload()` 为 `None` 且 `environment_compute_dispatch_count()` 按 required contents 计算;文件系统路径和 raw cache IO 仍归 asset artifact cache,source-cubemap 应用仍归 artifact bridge。 | `rustfmt --edition 2021 --check zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact_resolution.rs zircon_runtime/src/core/framework/render/environment/mod.rs zircon_runtime/src/core/framework/render/mod.rs zircon_runtime/tests/runtime_environment_ibl_bake_artifact_contract.rs` 通过;首轮同 target Cargo 在 20 分钟工具窗口内冷编译超时且未产生日志/二进制,不计通过;复跑 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-resolve-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_artifact_resolved_payload_current_source_cargo_retry_20260706.out.log` 报告 15/15 passed、0 failed、0 ignored、0 filtered,测试本体 0.08s;exit 记录 elapsed 00:14:30.4501358;stderr 只有仓库既有 warnings。 | 关闭 blob-backed resolved payload 优先级与 fallback 合同;GPU/offline PMREM/SH9/BRDF/IEM bake 生产、runtime readback 调度写入 cache、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2i IBL artifact payload application to source cubemap | render_plan11_ibl_artifact_payload_apply_to_source_cubemap_cargo_passed_gpu_readback_deferred | 已验证:decoded PMREM/SH9 payload 可接回现有 source cubemap 上传模型,且不替换 skybox/source mips | 新增 `zircon_runtime/src/core/framework/render/environment/source_cubemap_artifact.rs`,作为 `source_cubemap.rs` 的 child owner;`source_cubemap_mip_chain_with_bake_artifact(...)` 校验 face/mip layout,要求 PMREM+SH9,保留 `source_texels()` 给天空盒和 source lookup,替换 `texels()` specular PMREM 与 `irradiance_sh9()`;`source_cubemap_environment_with_bake_artifact(...)` 继续保留 `SourceCubemapEnvironment` 的 source revision/hash、intensity、rotation,并在 payload 含 IEM 时 attach `SourceCubemapIrradianceCube`;root render/environment facade 导出两个函数与 `SourceCubemapBakeArtifactError`;`source_cubemap.rs` 仅开放 `pub(super)` 构造口,未继续堆 artifact 逻辑进大文件。 | `rustfmt --edition 2021 --check zircon_runtime/src/core/framework/render/environment/source_cubemap_artifact.rs zircon_runtime/src/core/framework/render/environment/mod.rs zircon_runtime/src/core/framework/render/mod.rs zircon_runtime/tests/runtime_environment_ibl_bake_artifact_contract.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-apply-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_artifact_payload_apply_current_source_cargo_20260706.out.log` 报告 12/12 passed、0 failed、0 ignored、0 filtered,测试本体 0.05s;exit 记录 elapsed 00:07:33.5548958;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 关闭 decoded artifact payload 到 SourceCubemapMipChain/SourceCubemapEnvironment 与 optional IEM attach 的 CPU 合同;GPU/offline PMREM/SH9/BRDF bake 生产、runtime readback 调度写入 cache、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2h IBL bake artifact runtime cache store | render_plan11_ibl_bake_artifact_runtime_cache_store_cargo_passed_readback_deferred | 已验证:§4.7 来源 2 的 `.zircon-cache` 文件入口、raw blob 读写、missing/stale/truncated 拒绝路径完成 | 新增 `zircon_runtime/src/asset/artifact/ibl_bake_artifact_cache.rs`,由 asset artifact cache 边界持有 `.zircon-cache/render/ibl/v{IBL_BAKE_ALGORITHM_VERSION}/{request_hash}/face_####_mips_##.zribl` 路径与 IO;`IblBakeArtifactCacheStore` 写入 `IblBakeArtifactBlob::encode()` 原始字节,读取时用 `decode_current_for_request(...)` 判定 current,missing 与 rejected 均不阻断 fallback;`IblBakeArtifactCacheRead::candidate()` 只为 hit 产出 runtime-cache candidate;未把文件系统逻辑塞入 backend-free 的 render core artifact 合同。 | `rustfmt --edition 2021 zircon_runtime/src/asset/artifact/ibl_bake_artifact_cache.rs zircon_runtime/src/asset/artifact/mod.rs zircon_runtime/tests/runtime_environment_ibl_bake_artifact_contract.rs` 通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-payload-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_payload_roundtrip_current_source_cargo_20260706.out.log` 报告 9/9 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;exit 记录 elapsed 00:05:41.7209590;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 关闭 runtime cache file store/read/write 合同;GPU/offline PMREM/SH9/BRDF bake 生产、runtime readback 调度写入该 store、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2g IBL bake artifact blob byte container | render_plan11_ibl_bake_artifact_blob_roundtrip_cargo_passed_filesystem_io_deferred | 已验证:§4.7 的完整 header+payload artifact blob 字节容器完成 | 新增 `ibl_bake_artifact_blob.rs`,让 `IblBakeArtifactBlob` 组合 `IblBakeArtifactHeader` 与 `IblBakeArtifactPayload`,编码顺序固定为 108B header + descriptor-sized payload;`decode(...)` 拒绝截断 header 和 payload 长度不匹配,`decode_current_for_request(...)` 在读取完整 blob 后继续套用 request/key/layout/content/algorithm-version 当前性检查;environment/root render facade 导出 `IblBakeArtifactBlob` 与 `IblBakeArtifactBlobError`;模块文档同步记录 blob/container owner,避免继续扩大 710 行 `ibl_bake_artifact.rs`。 | `rustfmt --edition 2021 zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact_blob.rs zircon_runtime/src/core/framework/render/environment/mod.rs zircon_runtime/src/core/framework/render/mod.rs zircon_runtime/tests/runtime_environment_ibl_bake_artifact_contract.rs` 通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-payload-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_payload_roundtrip_current_source_cargo_20260706.out.log` 报告 9/9 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;exit 记录 elapsed 00:05:41.7209590;stderr 只有仓库既有 warnings;本切片不生成截图。 | 关闭 artifact blob byte container;filesystem placement、runtime `.zircon-cache` read/write、GPU/offline PMREM/SH9/BRDF bake、staged importer/build-tool 生产、product second-launch dispatch=0、RenderDoc/product capture、probe 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2f IBL bake artifact payload layout + roundtrip | render_plan11_ibl_bake_artifact_payload_roundtrip_cargo_passed_file_io_deferred | 已验证:§4.7 的 PMREM/SH9/IEM payload 字节布局、range helper 与 round-trip 合同完成 | `IblBakeArtifactPayload` 从 `SourceCubemapMipChain` 与可选 `SourceCubemapIrradianceCube` 生成 payload,顺序为 PMREM RGBA16F face-major 全 mip、SH9 9×vec4 f32 little-endian、可选 IEM 32x32x6 RGBA16F(alpha=1.0);`IblBakeArtifactDescriptor::expected_payload_size_bytes()`、`pmrem_rgba16f_byte_range()`、`irradiance_sh9_byte_range()`、`irradiance_cube_rgba16f_byte_range()` 与 decode helper 公开验证同一布局;environment/root render facade 同步导出 payload API 与 `IBL_BAKE_ARTIFACT_RGBA16F_TEXEL_SIZE_BYTES`/`IBL_BAKE_ARTIFACT_SH9_SIZE_BYTES`。 | `rustfmt --edition 2021` 对 `ibl_bake_artifact.rs`、facade 与 `runtime_environment_ibl_bake_artifact_contract.rs` 通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-payload-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_payload_roundtrip_current_source_cargo_20260706.out.log` 报告 9/9 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;exit 记录 elapsed 00:05:41.7209590;stderr 只有仓库既有 warnings;本切片不生成截图,未向 `target` 写入图片。 | 关闭 in-memory payload layout/roundtrip;实际磁盘 container IO、GPU/offline PMREM/SH9/BRDF bake、runtime readback writeback、staged importer/build-tool 生产、product second-launch dispatch=0、RenderDoc/product capture、probe 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2e IBL bake artifact header + source-selection contract | render_plan11_ibl_bake_artifact_contract_cargo_passed_gpu_cache_deferred | 已验证:§4.7 三来源持久化的 header/算法版本/选择合同完成 | 新增 `ibl_bake_artifact.rs` 与公开合同测试:`IblBakeArtifactHeader` 固定记录 `IblBakeKey`、`IBL_BAKE_ALGORITHM_VERSION`、face/mip layout 与 PMREM/SH9/IEM content bits;`IblBakeArtifactDescriptor::is_current_for(...)` 拒绝 key/layout/content/version 不匹配产物;`select_ibl_bake_artifact(...)` 固定 asset derived artifact > runtime cache > runtime compute,并保证来源 1/2 命中时环境 compute dispatch 计数为 0。同步导出 environment/root render facade,新增 `docs/zircon_runtime/core/framework/render/environment/ibl-bake-artifact.md`。 | `rustfmt --edition 2021` 对新模块、facade 与测试通过;首次 focused Cargo 因冷编译/外层工具超时无可计日志;复跑 `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;stdout `docs/tests/runtime/render/plan11_ibl_bake_artifact_contract_current_source_cargo_20260706.out.log` 报告 3/3 passed、0 failed、0 ignored、0 filtered,测试本体 0.00s,总 elapsed 00:10:56.4688476;stderr 只有仓库既有 warnings。 | 只关闭 §4.7 artifact 决策合同;PMREM+SH9+IEM payload roundtrip、GPU/offline bake、readback writeback、importer/staged build 生产、product second-launch dispatch=0、RenderDoc/product capture 与 full CI 仍未关闭。 |
| 2026-07-06 | EC-M2d CPU source IEM bridge | 已验证:optional diffuse IEM 的 CPU reference artifact 与公开合同完成 | 新增 `source_irradiance_cubemap.rs`,从 source cubemap 的 diffuse 32 face mip 做 direct cosine convolution,输出固定 32x32x6 face-major `SourceCubemapIrradianceCube`;采样端支持 cross-face bilinear tap;`environment/mod.rs` 与 root `core::framework::render` facade 导出 `build_source_cubemap_irradiance_cube`、`source_cubemap_sample_irradiance_cube`、`SOURCE_CUBEMAP_IRRADIANCE_CUBE_FACE_SIZE`;新增 `runtime_environment_source_irradiance_cubemap_contract.rs`,覆盖常量 diffuse 保真与低频环境对 SH9 的一致性。 | `rustfmt --edition 2021` 对新模块、facade 与测试通过;首轮 focused Cargo run 暴露 root render facade 未导出 IEM 符号,补 re-export 后同命令 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_irradiance_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2(测试本体 14.93s,总 7m57s,stderr 只有既有 workspace warnings);后续 Cargo wrapper recheck 在工具边界 904s 超时,不计通过;直接运行已构建测试二进制通过 2/2(15.85s);本切片不生成截图。 | 当前 IEM 仍是 CPU reference/contract,未进入 runtime WGSL diffuse binding;后续继续 GPU/offline IEM bake、derived artifact/cache、runtime upload/binding、IEM/SH9 选项、截图级 diffuse 对拍、RenderDoc/product capture 与 full CI。 |
| 2026-07-06 | EC-M3f 20260704 HDRI screenshot rejection + Unreal/cmft PMREM design lock | 设计完成:旧 sampled-equirect 图明确拒收,PMREM 设计收敛 | 按用户截图复核结论,将 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 标记为 rejected baseline;更新 `codex-cubemap.md` 与 source-cubemap 模块文档,锁定 source sky cube mip0、specular PMREM 独立 cube、1K→256 face/2K→512 face、UE GGX FIS、cmft solid-angle/edge/final-mip 纪律和 cmftStudio IEM/PMREM 分工。 | 文档设计切片,不改 production code,不生成新截图;`git diff --check -- docs/plans/zircon_runtime/shader/codex-cubemap.md docs/plans/zircon_runtime/shader/06-environment-ibl-and-pbr-correctness.md docs/plans/zircon_runtime/shader/index.md docs/plans/zircon_runtime/render/11-environment-lighting.md docs/zircon_runtime/core/framework/render/environment/source-cubemap.md` 只有既有 LF/CRLF warning,无 whitespace error。 | 下一步按该设计补截图级严格 high-frequency/roughness 单调、SSIM 源参考、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM 与 RenderDoc/product capture。 |
| 2026-07-06 | EC-M3e 2K HDRI Plan06 metrics current-source Cargo refresh | render_plan11_hdri_2k_plan06_metrics_current_source_cargo_passed_renderdoc_deferred | 已验证:已保存 2K HDRI PMREM PBR matrix PNG 在当前源码 Cargo wrapper 下通过非 ignored Plan06/EC-M3e 指标守卫;纯验证切片,不修改 shader/render production owner。 | `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-png-metrics-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_hdri_2k_plan06_metrics_current_source_cargo_20260706.out.log` 报告 running 1 test,结果 1 passed、0 failed、0 ignored、2 filtered,测试本体 0.38s;exit 记录 elapsed 00:11:40.9052706,PNG bytes 1,009,731,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`;stderr 只有仓库既有 warning。 | 只关闭已保存 2K PNG 的指标回放 current-source Cargo refresh;EC-M3 仍未全关闭:RenderDoc/product capture、严格高频/roughness 单调与 SSIM 源参考、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、4K/16K 离线 bake 和 full test-target/CI 继续执行。 |
| 2026-07-06 | EC-M3e 2K HDRI PNG metrics non-ignored regression + test owner split | 已验证:保存后的 2K HDRI PBR matrix PNG 现在由非 ignored 回归读回并执行 Plan 06 矩阵/旧 16x8 网格量化 | `runtime_shader_pbr_hdri_export.rs` 引入 `runtime_shader_pbr_hdri_export/hdri_metrics.rs`,把 sky directional variation、64-cell metallic/smoothness matrix response、legacy 16x8 grid seam probes 从导出用例拆到独立测试 owner;新增 `runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics`,不重新渲染,直接读取 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png`,检查 1280x960 尺寸并复跑同一组量化断言;导出测试文件降到 525 行,度量文件 382 行。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\hdri_metrics.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-png-metrics-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` 通过 1/1,0 ignored,2 filtered,测试本体 0.23s,总构建 8m43s,stderr 只有仓库既有 warnings;PNG 为 1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`;同名 PNG 在 `target` 与 `E:\cargo-targets` 扫描 0;`git diff --check -- zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\hdri_metrics.rs` 仅报告当前工作区 LF/CRLF warning,无 whitespace error。 | 关闭"2K 截图只靠 ignored/manual export 验收"的回归缺口;EC-M3 仍未全关闭:严格高频/roughness 单调与 SSIM 源参考、RenderDoc/product capture、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、4K/16K 离线 bake 与 full test-target/CI 继续执行。 |
| 2026-07-05 | EC-M3d cmft/UE cubemap mip edge sampling + 2K HDRI PMREM export | 已验证:修复 CPU source/PMREM mip 跨面采样与饱和粗糙度末级模糊,2K 真实 HDRI 截图验收通过 | `source_cubemap.rs` 将规则 source mip 的 2x2 downsample 与 GGX PMREM 输入采样从 per-face clamp 改为跨 face 投影采样,越界 tap 通过 `cubemap_direction_from_scaled_uv`/`cubemap_face_scaled_uv_from_direction` 重投影到相邻面,对齐 cmft 的跨面邻域纪律;GGX 采样保持 UE roughness/mip/pdf 公式,高粗糙度样本数升到 128,当相邻高 mip 的 roughness 都饱和为 1.0 时改为从上一层 PMREM 做逐级降采样,避免末级重新采样源 mip 造成马赛克/面块回流;`runtime_shader_pbr_hdri_export.rs` 参数化 1K/2K HDRI 资产与输出名,新增 ignored 2K 导出。 | `rustfmt --edition 2021` 对本轮 Rust 文件通过;`cargo test -p zircon_runtime --lib source_cubemap_linear_sampling_bleeds_across_face_edges --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --nocapture` 通过 1/1,锁定跨 face bilinear tap 不再 clamp;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 9/9(测试本体 5.28s);`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_2k_reflection_png --nocapture --test-threads=1` 通过 1/1(测试本体 135.57s);PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 为 1280x960,1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`,80,333 unique colors;源 HDR `docs/tests/runtime/shader/assets/polyhaven_lakes_2k.hdr` 为 5,918,432 bytes,SHA256 `B2506E0EE912C4C599FF013566FBD3ECAAC2F4B176319D450CCE0DE5758FED98`;同名 PNG 未出现在 `target` 或 `E:\cargo-targets`。 | 本轮关闭用户指出的 1K 低源分辨率/天空盒马赛克与 CPU PMREM mip 马赛克回归到 2K 截图级验收;仍未关闭 GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、SSIM/RenderDoc/product capture、4K/16K 离线 bake 与 full test-target/CI。 |
| 2026-07-06 | EC-M3b-EC-M3c source cubemap contract current-source Cargo refresh | render_plan11_source_cubemap_contract_current_source_cargo_passed_renderdoc_deferred | 已验证:source cubemap PMREM/SH9/HDR contract 在当前源码 Cargo wrapper 下复跑通过;纯验证切片,不修改 shader/render production owner。 | `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_source_cubemap_contract_current_source_cargo_rerun_20260706.out.log` 报告 running 9 tests,覆盖 high-frequency PMREM blur、progressive blur、constant env、rough mip seam energy、roughest mip all-face average、roughness mapping shader contract、HDR float upload preservation、SH9 constant diffuse 与 SH9 vertical gradient;结果 9 passed、0 failed、0 ignored、0 filtered,测试本体 6.83s;exit 记录 elapsed 00:11:14.0717002;stderr 只有仓库既有 warning。前一轮 `source_cubemap_linear_sampling_bleeds_across_face_edges --lib --exact` 0-test 与 source-cubemap contract 1204s timeout 均保留为不计通过的历史尝试。 | strict source-cubemap contract fresh pass 已关闭;EC-M3 仍未全关闭:RenderDoc/product capture、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、SSIM/seam 对拍和 full test-target/CI 继续执行。 |
| 2026-07-06 | EC-M3a 2K HDRI PMREM export current-source Cargo refresh | render_plan11_hdri_2k_pmrem_export_current_source_cargo_passed_renderdoc_deferred | 已验证:纯验证切片,不修改 shader/render production owner;复验 Poly Haven lakes 2K HDRI 导出路径继续走 source cubemap sky、specular PMREM cube、BRDF LUT 与 SH9 diffuse bridge,并输出高分辨率 PBR metallic/smoothness matrix PNG。 | `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_2k_reflection_png --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_hdri_2k_pmrem_export_current_source_cargo_20260706.out.log` 报告 1/1 passed、1 filtered、测试本体 158.36s,总 elapsed 00:15:58.6938140;PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 为 1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`;stderr 只有仓库既有 warning。`source_cubemap_linear_sampling_bleeds_across_face_edges --lib --exact` 本轮只匹配 0 tests,source-cubemap contract refresh 1204s 超时且无 stdout/stderr,均不计通过。 | 只关闭 2K HDRI PMREM export current-source Cargo refresh;EC-M3 仍未全关闭:RenderDoc/product capture、strict source-cubemap contract fresh pass、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、SSIM/seam 对拍和 full test-target/CI 继续执行。 |
| 2026-07-05 | EC-M3c source cubemap PMREM seam quantization guard | 已验证:source PMREM 粗 mip 新增 cube 面边界亮度差量化守卫 | `runtime_environment_source_cubemap_contract.rs` 使用公开 `cubemap_direction_from_scaled_uv`/`cubemap_face_scaled_uv_from_direction` 反推相邻 cube 面,在 mip0、中间粗 mip、rough mip 上采样四条边的 luma delta;新增 `runtime_environment_source_cubemap_pmrem_rough_mips_reduce_cube_seam_energy` 断言中间 mip 降低 mean seam energy、rough mip 继续降低 mean seam energy,并降低 worst seam delta,防止 PMREM 低 mip 在 cube 面交界重新出现块状断裂。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_environment_source_cubemap_contract.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 9/9(测试本体 4.74s,总 7m18s,既有 warnings);本切片不生成新截图。 | source CPU PMREM 的 seam 合同已覆盖;EC-M3 仍未全关闭:GPU/offline PMREM 产物 seam 对拍、SSIM 源 cubemap 参考、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM 与截图级高频/粗糙度验收继续执行。 |
| 2026-07-05 | EC-M3b source cubemap PMREM mip blur contract | 已验证:PMREM mip 链新增逐级高频方差下降与末级六面平均公开合同 | `runtime_environment_source_cubemap_contract.rs` 新增 64-face 高频棋盘+亮斑环境,采样所有 PMREM mip 的全局 luma variance,断言 mip1 已低于 mip0、后续 roughness 更高的 mip 方差持续下降、粗 mip 相对 base 重度模糊;另新增公开合同锁定 cmft 纪律的 final 1x1 mip 六面平均,防止低 mip 方向噪声或普通 box mip 回流。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_environment_source_cubemap_contract.rs` 通过;首轮 `cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 在 1204s 工具超时但后台 cargo/rustc 仍在冷编译,等待进程自然结束后复跑同命令通过 8/8(测试本体 3.56s,总 7m53s,既有 warnings);本切片不生成新截图,只验证 PMREM 算法合同。 | EC-M3 仍未全关闭:预滤波 cube seam 量化、SSIM 源 cubemap 参考、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM 与截图级高频/粗糙度验收继续执行。 |
| 2026-07-05 | EC-M3a 8x8 HDRI PBR matrix quantitative guard | 已验证:真实 HDRI 8x8 metallic/smoothness 矩阵导出新增整表量化断言,旧 16x8 马赛克边界回归可检测 | `runtime_shader_pbr_hdri_export.rs` 在保存 `runtime_shader_pbr_real_hdri_lakes_hdr_pmrem_reflection_20260705.png` 后采样 64 个球体格,断言亮度范围、smooth metallic 与 dielectric 端点差异、metallic 分组差异、high-metal rough/smooth 分组差异、row/column 响应覆盖率,并在天空上半区检测旧 16×8 sample-table 对齐边界;同步修复 `scene_bind_group_layout_entries()` 定义可见性到 `scene_renderer` 子树范围,避免 5-binding scene layout re-export 在导出测试编译期失效。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\src\graphics\scene\scene_renderer\environment\scene_bind_group_layout.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过 1/1(测试本体 28.76s);PNG 1280x960,845,069 bytes,SHA256 `64D5873A09DDC348C15A2444221DF07961A47A34D5FB281B73F7122FEAB0782E`,74,903 unique colors;量化采样:luma range `124.501..233.256`,smooth endpoint delta `85.106`,metal sweep group delta `32.130`,high-metal rough/smooth group delta `59.064`,responsive rows `8/8`,responsive columns `6/8`,legacy grid vertical/horizontal means `0.02491`/`0.02711`;同名图未出现在 `target` 或 `E:\cargo-targets`;`git diff --check` 仅报告当前工作区 LF/CRLF warning。 | 这是 EC-M3 的首个截图矩阵量化守卫,不是 §6 全关闭;剩余 SSIM 源 cubemap 参考、严格高频/粗糙度单调、预滤波 cube seam 量化、GPU/offline PMREM/SH9/BRDF bake、derived cache 与 IEM 继续执行。 |
| 2026-07-05 | EC-M2c HDR source/PMREM float bridge + BRDF LUT binding | 已验证:source sky cube 与 specular PMREM cube 分离,运行时上传改 RGBA16F,standard PBR 使用 RG16F BRDF LUT split-sum | `SourceCubemapMipChain` 新增 `source_texels()` 常规 source mip 链,`texels()` 保持 GGX PMREM 链;`environment_cubemap.rs` 改为 source/specular 两个 `Rgba16Float` cube texture,group0 binding1/4 分离,binding3 上传 BRDF LUT;`zr_environment.wgsl` reflection 改采 binding4 PMREM 并用 binding3 LUT;真实 HDRI export 保留 exposed linear HDR 而非 Reinhard tone-map;同步 `shadow_map_renderer.rs` 的 scene bind group fallback 到 5 binding ABI;新增公开 `runtime_environment_brdf_lut_contract.rs`;模块文档更新 source/PMREM/BRDF LUT 数据流。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(既有 warning 386);`runtime_environment_source_cubemap_contract` 通过 6/6,覆盖 HDR>1 保留与 PMREM 比同级 source mip 更模糊;`runtime_environment_brdf_lut_contract` 通过 2/2;ignored `runtime_shader_pbr_hdri_export` 首跑暴露 shadow scene bind group 3/5 ABI drift,同步修复后在 `E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705` 复跑通过 1/1;PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_hdr_pmrem_reflection_20260705.png` 1280x960,845,069 bytes,SHA256 `64D5873A09DDC348C15A2444221DF07961A47A34D5FB281B73F7122FEAB0782E`,74,903 unique colors,upper/lower avg `228.226,245.209,244.246` / `131.642,132.692,124.451`;同名图未出现在 `target` 或 `E:\cargo-targets`;`git diff --check` exit 0(仅当前工作区既有 LF/CRLF warnings)。 | 当前仍是 CPU reference/runtime bridge;生产级 GPU/offline PMREM/SH9/BRDF bake、derived artifact/cache、IEM、seam quant 与 EC-M3 8x8 定量验收继续执行。 |
| 2026-07-05 | EC-M2b CPU SH9 diffuse bridge | 已验证:source cubemap diffuse 从粗糙低 mip 近似切到 SH9 求值 | `SourceCubemapMipChain` 新增 `SourceCubemapIrradianceSh9` 与 `irradiance_sh9()`;`build_source_cubemap_from_equirect(...)` 在 GGX PMREM 覆写高 mip 前从规则 source mip 金字塔投影 32x32 face 档 SH9,权重使用 cubemap exact solid angle 并预乘 cosine lobe band scale;`SourceCubemapEnvironment` 与 `SceneUniform.environment_sh9` 携带 9×vec4;`zr_environment.wgsl` 新增 `zr_environment_sh9_eval(...)`,source cubemap diffuse 改为 SH9×intensity;fallback/deferred/template/静态 skybox ABI 同步;同时用最小改动修复当前工作区 `atlas_renderer/renderer.rs` 的 `report` 可变性编译断点以解锁验证。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(既有 warning 385);`cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 5/5;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-sh9-export-it-0705 --message-format short --color never` 配 `CARGO_INCREMENTAL=0` 通过 1/1(测试本体 27.00s);PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png` 1280x960,862,851 bytes,SHA256 `D3A8077CAD3A7F0CBD83634F33EBBFA62422E3C75E2B65B34AA85A66FEDF0029`,55,383 unique colors,upper/lower avg `203.406,229.504,237.745` / `116.015,117.394,112.453`;同名图未出现在 `target` 或 `E:\cargo-targets`;`cargo test -p zircon_runtime --lib deferred_lighting_shader_applies_environment_reflections_to_standard_pbr ...` 仍被当前脏工作区既有 `runtime_absorption` private re-export 与 text atlas test visibility errors 阻断,未进入 shader 字符串断言。 | 当前 SH9 是 CPU reference/runtime bridge;生产级 GPU/offline SH9 compute/readback、RGBA16F PMREM artifact、BRDF LUT/IEM、derived artifact/cache、seam 与 8x8 PBR 定量验收仍按 EC-M2/EC-M3 后续执行。 |
| 2026-07-05 | EC-M1e sampled-equirect bridge hard delete | 已验证:旧 sampled/equirect 过渡桥从 runtime 源码与测试中硬删 | 删除 `environment/equirect_samples.rs` 与 `docs/zircon_runtime/core/framework/render/environment/equirect-sampled-mip-chain.md`;移除 `SampledEquirectangularEnvironment`、`SharedSampledEquirectangularSamples`、`SkyboxMode::SampledEquirectangular`、`SkyboxSettings::sampled_equirectangular(...)`、`EnvironmentExtract::sampled_equirectangular(...)`、sampled/equirect re-export 和 scene uniform sampled 分支;`IblBakeKey.sampled_environment_hash` 收敛为 `source_hash`;`source-cubemap.md` 标记 sampled-equirect compatibility path retired。 | `rustfmt --edition 2021` 对硬删涉及 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(约 1m37s,既有 warning 385);`cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过 1/1(测试本体 26.92s);`git grep` 确认 `zircon_runtime/src` 与 `zircon_runtime/tests` 中 `SampledEquirectangular`/`sampled_equirectangular`/`SAMPLED_EQUIRECT`/`environment_samples`/旧 roughness helper 零命中;截图 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png` 为 1280x960,863,859 bytes,SHA256 `8F361638E61343B48C92DA12F59978729118CE4DB8DABEA80FEE6AC1FA712C5A`,同名图未出现在 `target` 或 `E:\cargo-targets`。 | sampled-equirect 兼容桥删除完成;剩余 EC-M2 是生产级 RGBA16F/GPU/offline PMREM、SH9 diffuse、BRDF LUT/IEM、derived artifact/cache,EC-M3 继续 seam/亮度/8x8 PBR 定量验收。 |
| 2026-07-05 | EC-M2a CPU GGX FIS PMREM bridge | 已验证:source cubemap 高 mip 已从 cmftStudio Blinn 过渡滤波切到 UE 风格 GGX filtered importance sampling CPU bridge | `source_cubemap.rs` 新增 `SOURCE_CUBEMAP_ROUGHEST_MIP = 1.0`、`SOURCE_CUBEMAP_ROUGHNESS_MIP_SCALE = 1.2`、`source_cubemap_pmrem_mip_from_roughness(...)`、`source_cubemap_roughness_from_pmrem_mip(...)`;`build_source_cubemap_from_equirect(...)` 保持 mip0 为源 cubemap,另建 2x2 source mip 金字塔供 FIS 选源 mip,输出 mip1..N 改为 Hammersley + GGX importance sample + `D_GGX * 0.25` pdf + solid-angle-selected source mip + `NoL` 加权累积,末级 1x1 继续六面平均;新增公开集成测试 `runtime_environment_source_cubemap_contract.rs`;真实 HDRI export 改写 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png`;同步更新 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(约 1m23s,既有 warning 412);`cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3;`cargo test -p zircon_runtime --lib source_cubemap ...` 在当前脏工作区被既有 `runtime_absorption/plan_status` private re-export errors 阻断,未进入本切片断言;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过 1/1(测试本体 24.99s);PNG 1280x960,863,859 bytes,SHA256 `8F361638E61343B48C92DA12F59978729118CE4DB8DABEA80FEE6AC1FA712C5A`,55,055 unique colors,upper sky avg `152.278,201.774,221.422`,lower sky avg `115.737,114.429,111.059`,grid column/row ratio `1.2401`;确认同名图只在 `docs/tests/runtime/shader/`,不在 `target` 或 `E:\cargo-targets`。 | 当前仍是 CPU + RGBA8 tone-mapped bridge,不是最终 RGBA16F/GPU compute PMREM;sampled-equirect 兼容桥已由 EC-M1e 硬删;EC-M2 继续 SH9、BRDF LUT/IEM、derived artifact/cache,EC-M3 继续 seam/亮度/8x8 PBR 定量验收。 |
| 2026-07-05 | EC-M1d source cubemap + cmftStudio radiance mip bridge | 已验证:真实 HDRI 截图路径切到 `texture_cube` source cubemap,旧 16x8 马赛克截图问题关闭到当前验收图 | 新增/接入 `SourceCubemapMipChain` face-major 全 mip 链,`build_source_cubemap_from_equirect(...)` 先按 cmft lat-long 公式生成 mip0,再用 cmftStudio 默认 gloss scale/bias + Blinn BRDF power 的 radiance lobe 生成 mip1..N,末级 1x1 六面平均;`SourceCubemapEnvironment` 进入 `EnvironmentExtract`;scene group0 binding1/2 改为 cube texture + filtering sampler;skybox 与 `zr_environment.wgsl` 采样 `texture_cube<f32>`;真实 HDRI export 写入 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_source_cubemap_reflection_20260705.png`;新增文档 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(既有 warning 412);`cargo test -p zircon_runtime --lib source_cubemap --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 7/7;`runtime_environment_cubemap_projection_contract` 通过 4/4;`runtime_texture_cube_resource_contract` 通过 3/3;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 冷构建后通过 1/1(测试本体 13.97s,构建 22m04s,既有 warning 411);PNG 1280x960,874,867 bytes,SHA256 `9AFA5DDEC8AE50A2A7CC4342B50F3B16442ED0BB29891D0B42C9BD8938ED98BF`,58,598 unique colors,旧 16x8 网格探针 column ratio `1.0052`/row ratio `0.9672`;目视确认天空与反射无明显块状马赛克。 | EC-M1 过渡链后续已由 EC-M1e 关闭 sampled-equirect 兼容桥;源 cubemap 仍为 CPU 构建 + RGBA8 tone-mapped 上传,EC-M2 继续 RGBA16F GGX FIS PMREM、SH9、BRDF LUT、IEM 与定量 8x8 PBR 判据。 |
| 2026-07-05 | EC-M1c GPU `texture_cube` 资源合同基础 | 已验证:描述符/上传判定/GPU view 资源层完成 | 新增 `RenderImageDimension::Cube`;`TextureAssetDescriptor` 支持 `dimension = "cube"` 与 `cubemap` alias,默认 6 面并拒绝非 `6N` 层数;RGBA8 与 compressed container upload readiness 接受 square `6N` cube shape;WGPU 资源层将 cube 创建为 D2 texture 并映射为 `TextureViewDimension::Cube`/`CubeArray`,同时保持普通材质 D2 贴图 view 只绑定 layer0;新增公开集成测试 `runtime_texture_cube_resource_contract.rs`;新增文档 `docs/zircon_runtime/core/framework/render/cube-texture-resource-contract.md`,并同步更新 `render/image.md`。 | `rustfmt --edition 2021 ...` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(最终复跑 1m29s,既有 warning 412);`cargo test -p zircon_runtime --test runtime_texture_cube_resource_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3(测试本体 0.00s,增量 test profile 2m20s);同 target 下 `runtime_environment_cubemap_projection_contract` 已通过 4/4。本切片未生成新截图。为运行集成测试,同步修正当前工作区 UI text native bitmap atlas cache 可见性编译断点并补文档;`cargo check --tests` 仍被既有 `m1_runtime_editor_boundary_contract.rs` 缺 `environment` 字段阻断,不计本切片通过。 | 已由 EC-M1d/EC-M1e 接续完成 source-cubemap HDRI 截图路径和 sampled-equirect bridge 硬删除;剩余为 EC-M2 的 RGBA16F GGX FIS/SH9/BRDF LUT/IEM 生产级 IBL。 |
| 2026-07-05 | EC-M1b cubemap 投影黄金参考 | 已验证:公开 API 投影合同完成 | 新增 `core/framework/render/environment/cubemap_projection.rs`,采用 cmft/UE face order `+X,-X,+Y,-Y,+Z,-Z`、texel center `[-1,1]`、cmft lat-long UV 公式与 Rory Driscoll exact solid-angle 公式;`core/framework/render/mod.rs` 与 environment facade 公开 re-export;新增 `runtime_environment_cubemap_projection_contract.rs` 以公开 API 锁定六面方向、lat-long 轴向、solid angle 总和 `4*pi` 与 equirect height→cube face size 规则;新增模块文档 `docs/zircon_runtime/core/framework/render/environment/cubemap-projection.md`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(格式化后最终复跑 1m06s,既有 warning 410 量级);`cargo test -p zircon_runtime --test runtime_environment_cubemap_projection_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 4/4(测试本体 0.01s,增量 test profile 编译 3m30s)。本切片不生成截图,因此没有新增 target/cargo-target 图片产物。 | 后续 EC-M1d/EC-M1e/EC-M2a 已接续完成 source cubemap 采样、旧桥硬删和 GGX FIS PMREM CPU bridge;剩余为最终 RGBA16F/GPU PMREM、SH9、BRDF LUT/IEM 与粗糙度 mip 单调截图。 |
| 2026-07-05 | EC-M1a sampled HDRI 高分辨率 mip 链 | 中间实现,截图验证通过 | `equirect_samples.rs` 将 Poly Haven 1K HDRI sampled bridge 从 16x8 table 提升为 512x256 base + 10 mip storage buffer(174,763 vec4);mip 生成从 2x2 box downsample 改为参照 cmft radiance filter 的方向空间卷积:solid angle × `dot^power`,power 采用 cmftStudio 默认 gloss scale/bias 与 Blinn-BRDF lighting model;scene group0 binding1 上传环境样本,scene uniform 删除内嵌 `environment_samples`;`zr_environment.wgsl` 与 skybox shader 维持 bilinear + lod 采样,roughness→mip 采用 UE `ROUGHEST_MIP=1.0`/`ROUGHNESS_MIP_SCALE=1.2`,specular 使用 Lazarov EnvBRDF 近似;真实 HDRI export 写入 `runtime_shader_pbr_real_hdri_lakes_pmrem_reflection_20260705.png`,不覆盖 2026-07-04 失败图。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-core-check-0705 --message-format short --color never` 通过(2026-07-05,1m55s,409/410 个既有 warning 量级);首次 ignored export 因 512x256 大数组按值复制导致 32MB 测试线程栈溢出,导出/相关单测改为 128MB 栈后 `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过(1/1,8.75s);PNG 1280x960,862,941 bytes,SHA256 `2FD20BB1109EC754F7DAED70AA6F8CABF2D8651FBEB1B32F4149AF89FFCB57F7`,49,960 unique colors,位于 `docs/tests/runtime/shader/`,未放入 target。 | 这仍是 storage-buffer/equirect 过渡路径,存在大数组栈压力;下一切片按本计划 EC-M1 硬切到 GPU `texture_cube` 源 skybox,删除 sampled equirect bridge,再进入 EC-M2 GGX FIS/SH9/BRDF LUT。默认 `cargo test -p zircon_runtime --lib` 仍被 `runtime_absorption/structure_convention/.../typed_error_child_owners/source_inventory` 可见性/重导出债务阻断,不计本切片通过。 |
| 2026-07-05 | 计划建立 | 已完成源码研究与算法定稿 | cmft/cmftStudio 与 UE5 精读结论(§2)、数据链与算法定稿(§3/§4,含离线烘焙三来源持久化 §4.7 与 IEM 替代 SH 选项 §4.8)、量化验收(§6) | 精读出处见 §2 各表;现状缺陷定位:16×8 sample table(`skybox.rs`)、最近邻采样与两方向 lerp 伪粗糙度(`zr_environment.wgsl`)、无 Cube 纹理维度 | EC-M1 起实施 |
