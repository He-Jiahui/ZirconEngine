# Shader index 当前执行状态迁出记录

> 来源文件：`docs/plans/zircon_runtime/shader/index.md`
> 迁移说明：Shader 总索引中的当前执行状态明细已迁入 Shader 06 产出目录。

## 4.1 当前执行状态

| 日期 | 子计划 | 状态 | 已完成 | 未完成/待验证 |
|---|---|---|---|---|
| 2026-07-06 | SH06 EC-M2j runtime IEM carrier + scene binding ABI | 已验证:optional IEM runtime carrier 与 6-binding scene ABI 接入 | `SourceCubemapEnvironment` 可携带 `SourceCubemapIrradianceCube`;scene group0 binding5 固定为 irradiance cube fallback/真实 IEM;`zr_environment.wgsl` 新增 `ZR_ENV_DIFFUSE_IEM` override、binding5 与采样函数;真实 HDRI helpers 构建 IEM;artifact environment bridge 可应用 PMREM/SH9/IEM payload。 | `rustfmt` 通过;`runtime_environment_ibl_bake_artifact_contract` 12/12 通过;`runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics` 1/1 通过。exact lib layout test 被既有 `graphics/text/cache/tests.rs` 的 `MeasureKey: Default` 编译错误阻断;本切片不生成截图。GPU/offline IEM bake、engine quality/specialization 启用 `ZR_ENV_DIFFUSE_IEM`、derived/staged、dispatch=0、RenderDoc/product capture 与 full CI 仍待后续。 |
| 2026-07-06 | SH06 EC-M2i IBL artifact payload application to source cubemap | 已验证:decoded PMREM/SH9 payload 可保留 source mips 并替换 specular PMREM/SH9 | 新增 `core/framework/render/environment/source_cubemap_artifact.rs`;`source_cubemap_mip_chain_with_bake_artifact(...)` 校验 face/mip layout 与 PMREM+SH9 content,保留 skybox/source 使用的 `source_texels()`,仅替换 PBR 反射使用的 PMREM `texels()` 与 diffuse SH9;`source_cubemap.rs` 只开放 `pub(super)` 构造口,避免继续扩大 942 行文件。 | `rustfmt` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 11/11;本切片不生成截图、不写 target 图像。GPU/offline bake、runtime readback 写入、derived artifact/staged build、product dispatch=0、RenderDoc/product capture 与 full CI 仍待后续。 |
| 2026-07-06 | SH06 EC-M2h IBL bake artifact runtime cache store | 已验证:runtime `.zircon-cache` raw blob 文件入口完成 | 新增 `asset/artifact/ibl_bake_artifact_cache.rs`;`IblBakeArtifactCacheStore` 在 `.zircon-cache/render/ibl/v{IBL_BAKE_ALGORITHM_VERSION}/{request_hash}/face_####_mips_##.zribl` 写入/读取 `IblBakeArtifactBlob` raw bytes,missing/stale/truncated 均按 fallback 友好方式报告,hit 才转为 runtime-cache candidate。 | `rustfmt` 通过;E 盘测试前剩余约 73GB;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 9/9;本切片不生成截图、不写 target 图像。GPU/offline bake、runtime readback 写入、derived artifact/staged build、product dispatch=0、RenderDoc/product capture 与 full CI 仍待后续。 |
| 2026-07-06 | SH06 EC-M2g IBL bake artifact blob byte container | 已验证:header+payload artifact blob 字节容器完成 | 新增 `ibl_bake_artifact_blob.rs`,`IblBakeArtifactBlob` 以 108B header + descriptor-sized payload 固定编码完整 reusable artifact;`decode_current_for_request(...)` 验证魔数/格式、payload 长度和 request/key/layout/content/algorithm-version 当前性;environment/root render facade 导出 blob API。 | `rustfmt` 通过;E 盘测试前剩余空间约 87.5GB;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 7/7;本切片不生成截图、不写 target 图像。filesystem/cache read/write、GPU/offline bake、staged importer/build-tool、product dispatch=0、RenderDoc/product capture 与 full CI 仍待后续。 |
| 2026-07-06 | SH06 EC-M2f IBL bake artifact payload layout + roundtrip | 已验证:PMREM/SH9/IEM payload byte layout 与 round-trip 合同完成 | `IblBakeArtifactPayload` 固定 PMREM RGBA16F 全 mip、SH9 144B f32 little-endian、可选 IEM 32x32x6 RGBA16F(alpha=1.0) 顺序;descriptor expected size/range helpers 与 decode helper 已公开;payload API 与常量已从 environment/root render facade 导出。 | `rustfmt` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 5/5;本切片不生成截图、不写 target 图像。磁盘 container IO、GPU/offline bake、readback writeback、staged importer/build-tool、product dispatch=0、RenderDoc/product capture 与 full CI 仍待后续。 |
| 2026-07-06 | SH06 EC-M2d CPU source IEM bridge | 已验证:optional diffuse IEM 的 CPU reference artifact 与公开合同完成 | 新增 `source_irradiance_cubemap.rs` 和 `runtime_environment_source_irradiance_cubemap_contract.rs`;IEM 输出固定 32x32x6 face-major cubemap,直接 cosine convolution source cubemap diffuse mip,采样端支持 cross-face bilinear tap;environment/root render facade 已导出构建与采样 API。 | `rustfmt` 通过;首轮 focused Cargo run 暴露 root facade re-export 缺口,补齐后 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_irradiance_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2;后续 Cargo wrapper recheck 在工具边界 904s 超时,不计通过;直接运行已构建测试二进制通过 2/2(15.85s);本切片不生成截图。GPU/offline IEM bake、derived cache、runtime upload/binding、shader 选项、截图对拍、RenderDoc/product capture 和 full CI 仍待后续。 |
| 2026-07-06 | SH06 EC-M3f 20260704 HDRI screenshot rejection + Unreal/cmft PMREM design lock | 设计完成:旧 sampled-equirect 图明确拒收,PMREM 设计收敛 | 按用户复核意见,`runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 只保留为 rejected baseline;`codex-cubemap.md`、计划 06 与 source-cubemap 模块文档明确 source cube mip0 负责天空盒、独立 specular PMREM 负责反射、1K HDRI 生成 256 face/2K 生成 512 face、PMREM 采用 UE GGX FIS 与 roughness/mip 对数映射,cmft/cmftStudio 只作为 face/solid-angle/edge/final-mip 与 IEM/PMREM 分工参考。 | 本切片不改 production code,不生成新截图;`git diff --check` 对本轮文档只有既有 LF/CRLF warning,无 whitespace error。下一步仍待严格 high-frequency/roughness 截图单调、SSIM、GPU/offline bake、derived cache、IEM、probe、RenderDoc/product capture、4K/16K 和 full CI。 |
| 2026-07-06 | SH06 EC-M3e 2K HDRI PNG metrics non-ignored regression | 已验证:保存后的 2K HDRI PBR matrix PNG 可由非 ignored 测试读回并执行 Plan 06 量化 | `runtime_shader_pbr_hdri_export.rs` 新增 `runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics`;截图量化逻辑拆到 `runtime_shader_pbr_hdri_export/hdri_metrics.rs`,覆盖 sky directional variation、64-cell matrix response、legacy 16x8 grid seam probes;导出测试文件降到 525 行,度量 owner 382 行。 | `rustfmt` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-png-metrics-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` 通过 1/1,0 ignored,2 filtered;PNG `runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`,同名 target/cargo-target 扫描 0。严格高频/roughness 单调、SSIM、GPU/offline bake、derived cache、IEM、probe、RenderDoc/product capture、4K/16K 和 full CI 仍待后续。 |
| 2026-07-05 | SH06 EC-M3d cmft/UE cubemap mip edge sampling + 2K HDRI screenshot | 已验证:CPU source/PMREM mip 跨面采样与高粗糙度末级模糊修复,2K 真实 HDRI PBR matrix 截图通过 | `source_cubemap.rs` 将规则 source mip 与 GGX PMREM 输入采样从 per-face clamp 改为跨 face 投影 tap;高粗糙度 GGX PMREM 使用 128 样本,roughness 饱和后的后续 mip 从上一层 PMREM 降采样;`runtime_shader_pbr_hdri_export.rs` 新增 Poly Haven lakes 2K HDRI ignored 导出。 | `rustfmt` 通过;`source_cubemap_linear_sampling_bleeds_across_face_edges` 通过 1/1;`runtime_environment_source_cubemap_contract` 通过 9/9;`export_runtime_shader_pbr_real_hdri_2k_reflection_png` 通过 1/1;PNG `runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 1280x960,1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`,80,333 unique colors,未进入 target/cargo-target。GPU/offline bake、derived cache、IEM、probe、SSIM/RenderDoc/product capture、4K/16K 离线资产与 full CI 仍待后续。 |
| 2026-07-05 | SH06 EC-M3c source cubemap PMREM seam quantization guard | 已验证:source PMREM 粗 mip 新增 cube 面边界亮度差量化守卫 | `runtime_environment_source_cubemap_contract.rs` 通过公开 cubemap 投影 API 反推相邻面,对 mip0、中间粗 mip、rough mip 的四边 luma delta 做 mean/max 量化,断言中间/rough PMREM mip 持续降低 seam energy,避免低 mip 反射在 cube 面边界重新块状断裂。 | `rustfmt` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 9/9(测试本体 4.74s,总 7m18s,既有 warnings)。本切片不生成截图;GPU/offline PMREM seam 对拍、SSIM、GPU/offline bake/cache/IEM 与截图级高频验收仍待后续。 |
| 2026-07-05 | SH06 EC-M3b source cubemap PMREM mip blur contract | 已验证:PMREM mip 链新增逐级高频方差下降与末级六面平均公开合同 | `runtime_environment_source_cubemap_contract.rs` 新增 64-face 高频棋盘+亮斑环境,对全部 PMREM mip 计算 luma variance,锁定 mip1 低于 mip0、后续 roughness 更高 mip 方差持续下降、粗 mip 相对 base 重度模糊,并公开断言 final 1x1 mip 六面平均。 | `rustfmt` 通过;首轮 focused Cargo 测试在冷编译期间工具超时,等待后台 cargo/rustc 结束后同命令复跑通过 8/8(测试本体 3.56s,总 7m53s,既有 warnings)。本切片不生成截图;cube seam、SSIM、GPU/offline bake/cache/IEM 与截图级高频验收仍待后续。 |
| 2026-07-05 | SH06 EC-M3a 8x8 HDRI PBR matrix quantitative guard | 已验证:真实 HDRI metallic/smoothness 矩阵导出已增加整表量化断言 | `runtime_shader_pbr_hdri_export.rs` 采样 64 个 PBR 球体格,锁定亮度范围、smooth metal/dielectric 端点差异、metallic 分组差异、high-metal rough/smooth 分组差异、row/column 响应覆盖率与旧 16×8 sample-table 边界检测;`scene_bind_group_layout_entries()` 可见性同步到 `scene_renderer` 子树,恢复 5-binding scene layout re-export 编译。 | `rustfmt` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过 1/1;PNG 1280x960,845,069 bytes,SHA256 `64D5873A09DDC348C15A2444221DF07961A47A34D5FB281B73F7122FEAB0782E`,74,903 unique colors,同名图不在 target/cargo-target;量化值:luma `124.501..233.256`,endpoint/group deltas `85.106`/`32.130`/`59.064`,responsive rows/columns `8/8`/`6/8`,legacy grid means `0.02491`/`0.02711`。SSIM 参考、高频严格单调、cube seam、GPU/offline bake/cache/IEM 仍待后续。 |
| 2026-07-05 | SH06 EC-M2c HDR source/PMREM float bridge + BRDF LUT binding | 已验证:source cube/specular PMREM 分离,RGBA16F 上传与 BRDF LUT binding 通过真实 HDRI 截图 | `SourceCubemapMipChain::source_texels()` 保留 source mip 链,`texels()` 保留 GGX PMREM;scene group0 binding1 source cube、binding3 RG16F BRDF LUT、binding4 specular PMREM cube;`zr_environment.wgsl` reflection 使用 PMREM cube + BRDF LUT;真实 HDRI helper 保留 exposed linear HDR;shadow-map scene bind group fallback 同步 5 binding ABI;新增 `runtime_environment_brdf_lut_contract.rs`;截图 `runtime_shader_pbr_real_hdri_lakes_hdr_pmrem_reflection_20260705.png`。 | `rustfmt` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`runtime_environment_source_cubemap_contract` 6/6、`runtime_environment_brdf_lut_contract` 2/2、ignored HDRI export 1/1 通过;PNG 1280x960,845,069 bytes,SHA256 `64D5873A09DDC348C15A2444221DF07961A47A34D5FB281B73F7122FEAB0782E`,74,903 unique colors,同名图不在 target/cargo-target。GPU/offline bake、derived cache、IEM、seam quant 与 EC-M3 8x8 定量验收仍待后续。 |
| 2026-07-05 | SH06 EC-M2b CPU SH9 diffuse bridge | 已验证:source cubemap diffuse 改用 SH9 irradiance | `SourceCubemapMipChain` 新增 SH9 系数;构建时从 32x32 source mip 做 exact-solid-angle 投影并预乘 cosine lobe;`SceneUniform.environment_sh9` 与 `zr_environment_sh9_eval(...)` 接入 source cubemap diffuse;真实 HDRI 导出刷新 `runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png`;顺手修复当前工作区 atlas renderer `report` 可变性编译断点以解锁验证。 | `rustfmt` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`runtime_environment_source_cubemap_contract` 通过 5/5;ignored HDRI export 通过 1/1;PNG 1280x960,862,851 bytes,SHA256 `D3A8077CAD3A7F0CBD83634F33EBBFA62422E3C75E2B65B34AA85A66FEDF0029`,同名图不在 target/cargo-target。lib shader-string test 仍被既有 runtime_absorption/text atlas test 编译债阻断。完整 GPU/offline SH9、RGBA16F PMREM、BRDF LUT/IEM、derived cache 与 EC-M3 定量验收仍待后续。 |
| 2026-07-05 | SH06 EC-M1e sampled-equirect bridge hard delete | 已验证:旧 sampled/equirect 过渡桥从 runtime 源码与测试中硬删 | 删除 `environment/equirect_samples.rs`;移除 `SampledEquirectangularEnvironment`、`SharedSampledEquirectangularSamples`、`SkyboxMode::SampledEquirectangular`、sampled `EnvironmentExtract`/`SkyboxSettings` 构造、re-export 和 scene uniform sampled 分支;`IblBakeKey` 收敛为 source hash;删除 sampled mip-chain 模块文档。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(既有 warning 385);`runtime_environment_source_cubemap_contract` 通过 3/3;ignored `runtime_shader_pbr_hdri_export` 通过 1/1;`git grep` 确认 `zircon_runtime/src` 与 `zircon_runtime/tests` 中 sampled/equirect 旧符号零命中;截图仍只在 `docs/tests/runtime/shader/`。生产级 RGBA16F/GPU PMREM、SH9、BRDF LUT/IEM、derived artifact/cache 与 EC-M3 定量验收仍待后续。 |
| 2026-07-05 | SH06 EC-M2a CPU GGX FIS PMREM bridge | 已验证:source cubemap 高 mip 切到 UE 风格 GGX filtered importance sampling CPU bridge | `source_cubemap.rs` 新增 roughness/mip 常数 `1.0/1.2` 与映射 helper;高 mip 从 cmftStudio Blinn 过渡滤波改为 Hammersley + GGX importance sample + solid-angle-selected source mip + `NoL` 加权;新增 `runtime_environment_source_cubemap_contract.rs`;真实 HDRI 导出 `runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`runtime_environment_source_cubemap_contract` 通过 3/3;ignored `runtime_shader_pbr_hdri_export` 通过 1/1;PNG 1280x960,863,859 bytes,SHA256 `8F361638E61343B48C92DA12F59978729118CE4DB8DABEA80FEE6AC1FA712C5A`,同名图不在 target/cargo-target。当前仍是 CPU + RGBA8 bridge,完整 RGBA16F/GPU/offline PMREM、SH9、BRDF LUT/IEM 与 EC-M3 定量验收仍待后续。 |
| 2026-07-05 | SH06 EC-M1c GPU `texture_cube` 资源合同基础 | 已验证:描述符/上传判定/资源 view 合同完成 | 新增 `RenderImageDimension::Cube`;texture descriptor 支持 `cube`/`cubemap` 维度并校验 `6N` layer count;RGBA8/compressed upload readiness 接受 square cube shape;GPU texture resource 将 cube 映射为 D2 texture + Cube/CubeArray view;新增 `runtime_texture_cube_resource_contract.rs` 与 `cube-texture-resource-contract.md`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`cargo test -p zircon_runtime --test runtime_texture_cube_resource_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3。本切片未生成截图;source cubemap 采样、sampled bridge 硬删与 GGX FIS CPU bridge 已由后续行推进;SH9/BRDF LUT/IEM 与最终 RGBA16F/GPU IBL 仍待后续。 |
| 2026-07-05 | SH06 EC-M1b cubemap 投影黄金参考 | 已验证:公开 API 投影合同完成 | 新增 `core/framework/render/environment/cubemap_projection.rs` 与 `runtime_environment_cubemap_projection_contract.rs`;按 cmft/UE face order `+X,-X,+Y,-Y,+Z,-Z`、texel center `[-1,1]`、lat-long UV 与 exact solid angle 建立 CPU 黄金参考;`core/framework/render/mod.rs` 顶层 re-export。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`cargo test -p zircon_runtime --test runtime_environment_cubemap_projection_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 4/4。本切片不生成截图;完整 GPU `texture_cube`、`equirect_to_cube.wgsl`、GGX FIS/SH9/BRDF LUT、粗糙度 mip 截图仍待 SH06 EC-M1/EC-M2 后续。 |
| 2026-07-05 | SH06 EC-M1a sampled HDRI mip 链纠偏 | 中间实现,截图验证通过 | 16x8 sampled equirect table 改为 512x256 base + 10 mip storage buffer;多级 mip 生成参照 cmft radiance filter,用 solid angle × `dot^power` 方向卷积替代 2x2 box downsample;scene uniform 删除内嵌 `environment_samples`;skybox/PBR helper 使用 bilinear/lod 采样;roughness→mip 对齐 UE reflection capture 公式;真实 HDRI 导出 `runtime_shader_pbr_real_hdri_lakes_pmrem_reflection_20260705.png`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-core-check-0705 --message-format short --color never` 通过;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过并生成 1280x960 PNG(862,941 bytes,SHA256 `2FD20BB1109EC754F7DAED70AA6F8CABF2D8651FBEB1B32F4149AF89FFCB57F7`,49,960 unique colors)到 `docs/tests/runtime/shader/`;完整 GPU `texture_cube`、GGX FIS prefilter、SH9、BRDF LUT、probe capture 与 Plan 13 `.zcube` 仍按 SH06 EC-M1/EC-M2 后续推进。默认 lib test 仍被 structure convention typed-error child owner 可见性债务阻断。 |
| 2026-07-05 | SH06 环境 IBL 资产链与 PBR 正确性验收 | 计划建立:源码研究与算法定稿完成,未实施 | cmft/cmftStudio 与 UE5 精读结论、equirect→cube/GGX prefilter(filtered importance sampling)/SH9(立体角权重+预乘 A_l)/BRDF LUT(128×128 RG16F) 算法定稿、`zr_environment.wgsl` v2 契约、离线烘焙三来源持久化(资产伴生→缓存回写→现场 compute)与 IEM 替代 SH 消费选项、8×8 材质球矩阵六条量化判据;定位当前马赛克根源:16×8 sampled equirect table + 最近邻采样 + 两方向 lerp 伪粗糙度 + 无 Cube 纹理维度。 | EC-M1..M4 全部待实施;依赖 render 计划 13 TX-M3 `.zcube` 与计划 11 EL-M1/M2 落点;镜像条目已登记进计划 11/13 状态表。 |
| 2026-07-04 | Plan 11 / SH04 PBR environment reflection matrix | procedural skybox + PBR environment matrix 已聚焦验证 | `EnvironmentExtract` 已进入 render frame/viewport packet;formal procedural skybox 由 `EnvironmentExtract.skybox` 驱动;`zr_environment.wgsl` 进入 fallback mesh、standard PBR 与 deferred lighting,standard PBR 可按 metallic/smoothness 采样 procedural sky environment;ignored export 生成 8x8 metallic 0..1 / smoothness 0..1 材质球矩阵。 | `cargo test -p zircon_runtime --lib graphics::tests::project_render::project_scenes::export_runtime_shader_pbr_metallic_smoothness_matrix_png --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m4-pbr-matrix-0704 --message-format short --color never -- --ignored --exact --nocapture --test-threads=1` 通过 1/1；截图 `docs/tests/runtime/shader/runtime_shader_pbr_metallic_smoothness_matrix_skybox_20260704.png`,1280x960,109556 bytes,SHA256 `E883A3BDF657025EAD16A7F39B1F8BE5D7FFCDA1FDEF0243A8636A05C217030D`;同名 target/cargo-target 扫描 0。cubemap prefilter、reflection probes、lightmaps/probes、fog 与 RenderDoc/product capture 仍待 Plan 11 后续里程碑。 |
| 2026-07-04 | Plan 11 / SH04 real HDRI sampled environment reflection | 真实 HDRI 采样天空与 PBR 反射矩阵已验证 | Poly Haven `lakes` 1K HDRI 已保存到 `docs/tests/runtime/shader/assets/polyhaven_lakes_1k.hdr`;`SampledEquirectangularEnvironment`/`SkyboxMode::SampledEquirectangular` 通过 scene uniform 上传 16x8 sample table,skybox 与 standard PBR/fallback/deferred 环境 helper 共用真实 HDR-derived 采样；ignored export 生成真实 HDRI 8x8 PBR 反射矩阵。 | `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-real-hdri-reflection-0704 --message-format short --color never` 通过；direct binary `E:\cargo-targets\zircon-real-hdri-reflection-0704-server\debug\deps\zircon_runtime-0a7825d39d44b0c4.exe graphics::tests::project_render::project_scenes::export_runtime_shader_pbr_real_hdri_reflection_png --ignored --exact --nocapture --test-threads=1` 通过 1/1；截图 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png`,1280x960,132232 bytes,SHA256 `958E3B200EC56BCA16BF9596B1F05D872179F51CEB9A64925E10FC2D41792DEE`;同名 target/cargo-target 扫描 0。完整 cubemap asset import、GPU `texture_cube`、GGX prefilter、probe blending、SH bake 与 RenderDoc/product capture 仍待 Plan 11/13 后续里程碑。 |
| 2026-07-03 | SH01 shader 资产类型与 zshader v2 | 已完成 runtime 硬切换 | `.zshader` v2 kind 分型、surface-only prewarm 过滤、存量 `.zshader` 迁移、v1/pipeline_layout/user shader_defs 拒绝诊断。 | `zircon_plugins` workspace check 仍待最终集成阶段单独跑。 |
| 2026-07-03 | SH02 属性代码生成与 vec4 打包 | 主路径完成，截图验收通过；focused 布局/模板/Naga 验证已补跑通过 | `MaterialPropertyLayout`/`MaterialOptionTable`、generated material WGSL、layout_hash、ResourceStreamer material uniform 上传、模板 generated module 注入、option define 注入；renderer material group2 ABI 已统一为 binding0 uniform + binding1..10 标准贴图/采样器。 | `cargo check` 与 vampire 离屏截图导出通过；本轮 `cargo test -p zircon_runtime --lib property_layout` 4/4 通过，`cargo test -p zircon_runtime --lib render_shader_template` 首跑暴露 status mirror 缺口、修复后 18/18 通过；更宽的 `shader`/`material` filter、产品级 RenderDoc/capture 与 product miss=0 仍待补跑。 |
| 2026-07-03 | SH03 WGSL 模块 import 与跨引用 | M1 完成，M2 redirect、asset-root include 失效传播、插件 manifest 注册、source-only 项目扫描依赖传播、redirect 材质 readiness 与产品侧 ResourceStreamer 诊断已通过聚焦验证 | `include_registry` 硬切换为 `module_registry`；内建/资产模块统一解析、传递依赖、去重、环诊断、WGSL include 扫描/剥离、include asset ResourceStreamer 供给、importer 一致性与词法诊断；focused `shader_module` tests 6/6 通过；`ShaderImportReadiness.source_diagnostic` 现在为 redirect import 行记录 import path -> redirect locator 来源说明；`zircon_shader_prewarm` asset-root 扫描通过 `manifest/module_dependencies.rs` 把同 root 下 include 模块依赖 hash 混入引用者 request 与 material revision，旁观者 shader revision 保持不变；插件 manifest 新增 `[[shader_permutation.shader_modules]]`，工具链会导出 registry `shader_modules`，prewarm overlay 会把外部插件模块 hash 作为 fallback 纳入项目 shader revision；`scan_and_import/shader_import_dependencies.rs` 现在把 source-only 项目 import_path 解析为 include shader 的 `ResourceRecord.dependency_ids`；`MaterialAsset::readiness_report_with_shader_contract(...)` 现在把 shader redirect import 依赖解析失败投射为材质 `UnresolvedShaderReference` 与 shader fallback usage；产品侧 `ResourceStreamer` 已覆盖 resolved redirect include prepare/module source 供给与 missing redirect fallback 诊断。 | redirect 来源诊断 1/1；asset-root include revision exact 1/1，`shader_prewarm_asset_root_manifest` 10/10；插件注册工具链 Python 回归 52/52、py_compile、Rust fmt、prewarm bin check 通过，直接运行生成 test binary 的两个新增 exact tests 各 1/1；source-only import 依赖传播 `cargo check` 与 `shader_import_dependency_contract` 1/1 通过；redirect 材质 readiness 补口 `cargo check` 与 `material_shader_redirect_dependency_contract` 2/2 通过，覆盖内存合成依赖与 `ProjectManager::scan_and_import` imported artifact 链路；产品侧 ResourceStreamer redirect 子测试 direct generated runtime binary 通过 2/2。focused product material-pass 二次启动 miss=0 已由 SH04 用例关闭；产品级 RenderDoc/product capture、更广 product/perf sweep 与 editor project-sync refresh exact Cargo result 仍待完成。 |
| 2026-07-03 | SH04 材质绑定与渲染器契约 | M1/L1/L2、M2 renderer/prewarm 主路径与 M3 compute/fullscreen 契约样例完成；material-pass 二次启动 miss=0 聚焦验证已补跑 | `.zmaterial` v2、parent/options/queue、强校验、material option bits、layout_hash variant key、shading_model/queue/disabled_passes renderer 接线、所有 material pass 模板源码经 ResourceStreamer 注入 generated material/modules/options；标准/备用/内置 WGSL、材质 bind group layout 与 bind group 创建已同步新版 group2 ABI；`MaterialPropertyOverrideBlock` 已接入 scene extract、per-draw material uniform override payload、静态批/命令缓存排除和覆盖 payload 窄测；`zircon_shader_prewarm` 已从 `.zshader` 生成 material layout hash/option table，并按 `.zmaterial` 实际 option 值稀疏生成 variant key；`ComputeDispatchBuilder`/`FullscreenPassBuilder`、graph compute workload bridge、RenderFeaturePassDescriptor 消费入口、clustered lighting compute descriptor 与 motion-vector fullscreen descriptor 样例已接线。 | SH03 focused `shader_module` tests 与 SH05 IDE/stub 聚焦验证已关闭；截图证据 `docs/tests/runtime/shader/runtime_shader_material_vampire_offscreen_20260703.png` 已归档；prewarm manifest 21/21、compute filter 36/36、fullscreen filter 3/3 聚焦测试通过；direct generated lib-test binary `E:\cargo-targets\zircon-runtime-03-gates-0704\debug\deps\zircon_runtime-33095b46939b64fc.exe` 精确运行 `render_product_project_plugin_registry_material_passes_second_launch_use_staged_prewarm_without_compile_miss` 通过 1/1、6400 filtered、10.59s，覆盖 product material-pass staged cache 二次启动 `compile_miss_count == 0`。HZB/particles/更多 postprocess executor 全量迁移、真实 compute pipeline disk cache、更广 product/perf sweep 与 RenderDoc/product capture 仍需产品级验收。 |
| 2026-07-04 | Plan 08 / SH04 shader prewarm 组合 WGPU 校验 | 组合 module+pipeline WGPU 路径与 owner budget 已聚焦关闭 | `zircon_shader_prewarm` 同时启用 `--validate-wgpu-modules` 与 `--validate-wgpu-pipelines` 时改走组合入口，复用一个 offscreen backend/device，先执行 shader module validation 再执行 render pipeline validation；module validation 失败时 pipeline validation 记 skipped，report 同时保持 `wgpu_module_validation.enabled = true` 与 `wgpu_pipeline_validation.enabled = true`。结构上，`variant_cache/prewarm.rs` 的组合行为测试下沉到 `variant_cache/prewarm/tests/combined_validation_tests.rs`，`manifest/tests.rs` 的 raw source revision 测试下沉到 `manifest/tests/raw_revision.rs`，相关结构守卫同步读取 `manifest/material_sources.rs` 与 raw revision child。 | `cargo test -p zircon_runtime --lib --no-run --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-shader-combined-validation-0704 --message-format short --color never` 通过；直接运行同 target runtime lib-test binary 的 `shader` filter 通过 366/366、1 ignored；public wrapper live WGPU prewarm 回归通过，report 为 requested/written/failed 18/18/0，module validation 18/18，pipeline validation 18/18；prewarm bin rebuild 通过。截图证据仍在 `docs/tests/runtime/shader`，同名 target/cargo-target 扫描无命中。RenderDoc/product capture 与更广 product/perf sweep 仍待后续补跑。 |
| 2026-07-03 | SH05 编写 DX 与 IDE | M1 截图验收、M2 共享生成器/CLI/默认与显式非默认 preview/增量 diff/Naga parse/validate gate/editor refresh hook typecheck/批量 preview matrix、M3 诊断回映完成 | import_path 项目命名空间推导、同名目录折叠、显式冗余 warning、`self::`/`zr_` 保留段校验、扫描级重复 import_path 诊断、`self::material` 锚点字节等价测试；`graphics/shader/ide_env_generation.rs` 成为 CLI/editor 共享生成 owner,`zircon_shader_ide_env/run.rs` 收缩为参数解析/项目扫描/report 薄壳,生成 `module_map.json`、`modules/` stub 树、per-shader `generated/*.material.wgsl`、`preview/<shader>.<variant>.wgsl` 和 `preview/<shader>.<variant>.segments.json`,并改为 content-aware 写入:内容未变不重写、受管目录内过期产物可清理、module 一字节变化只改对应 stub 与 map；写入前所有 stub 通过 Naga WGSL parse,默认与显式非默认 preview 通过完整 Naga validate,report 输出 `naga_parsed_stub_count`/`naga_validated_preview_count`,并新增 validation-only builtin/feature/context/material option defines 让跨 stub 依赖和 `ZR_OPT_*` 可解析但不污染写出文件；`--variants` 保留默认 preview 快捷入口,`--variant <pass[:options=bits]>`/`ShaderIdePreviewVariant` 可生成 `gbuffer_options_0x00000001` 等非默认组合并拒绝同名 preview variant；默认 editor refresh 仍只请求 static mesh + Forward pass + option bits 0,所有 preview 都把 defines/include/generated/user/pass-template 段表导出给 IDE/人工回映；`DefaultEditorAssetManager::sync_from_project(...)` 在导入后扫描到 Ready shader 时刷新同一 `.zircon-cache/shader_ide/v1` 产物,editor lib typecheck 已通过；`zr_surface_types.wgsl` 现在统一拥有 `ZrSurfaceInput` alias、`zr_surface_default(...)`、`ZrDeferredGBufferOutput` 与 deferred material flag encode helper,GBuffer template 不再重复声明共享输出结构；`MaterialShaderTemplateAssembly` 记录 source segment 表,Naga parse/validation 诊断可回映到 module id + 局部行号,运行时 Forward/GBuffer/TAA material pass 会把 surface shader 的 `import_path` 作为用户源 module id；材质球 surface shader 通过 compound zshader 导入并离屏渲染到 `docs/tests/runtime/shader/runtime_shader_material_sphere_offscreen_20260703.png`；本轮补入并执行通过 `shader_ide_env_batches_preview_matrix_for_all_surface_shaders` 批量矩阵回归,覆盖两个 surface shader × default/GBuffer options=1/DepthPrepass/Shadow/Velocity/TAA preview 路径与 stale 清理,并修复多 shader generated `self::material` scope 与 TAA material-mask 新 ABI 预览缺口。 | 已复跑通过 `cargo check -p zircon_editor --lib --no-default-features`、`cargo test -p zircon_runtime --lib shader_ide_env` 6/6、`cargo test -p zircon_runtime --lib shader_template_` 20/20、`cargo test -p zircon_runtime --lib shader_module` 6/6,以及本轮非默认 preview 聚焦 tests/CLI parse tests；截图证据 `docs/tests/runtime/shader/runtime_shader_material_sphere_offscreen_20260703.png` 与 `runtime_shader_material_vampire_offscreen_20260703.png` 已复核在 docs 目录且非空。新增 preview matrix 先通过 `cargo check -p zircon_runtime --tests` 编译门,随后 `cargo test -p zircon_runtime --lib shader_ide_env_batches_preview_matrix_for_all_surface_shaders` 在 `F:\cargo-targets\zircon-runtime-shader-sh03-readiness-0704b` 通过 1/1；`cargo test -p zircon_runtime --lib taa_reactive_mask` 同 target 通过 13/13；product material-pass 二次启动 miss=0 已由 SH04 聚焦用例补跑通过；RenderDoc/product capture 与更广 product/perf sweep 仍需后续补跑；M3 结构预算已把 template surface module tests、mesh ensure pipeline tests 和 IDE generator tests 下沉到 child owner。 |

2026-07-04 final recheck note: after this status sync, direct runtime `shader` filter, touched-file rustfmt, screenshot hash check, and same-name target scan were re-run successfully. The wrapper live-WGPU unittest re-run was skipped because no `zircon_shader_prewarm.exe` was present and `ZR_TEST_SHADER_PREWARM_EXE` was unset; a follow-up bin rebuild attempt timed out after 244s while other cargo/rustc lanes were active, so those two commands are not counted as additional pass evidence beyond the earlier counted wrapper/bin runs in the table.
