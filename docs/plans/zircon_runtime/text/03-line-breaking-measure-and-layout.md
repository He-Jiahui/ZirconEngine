---
related_code:
  - zircon_runtime/src/ui/text/layout_engine.rs
  - zircon_runtime/src/ui/text/layout_engine/candidate_line.rs
  - zircon_runtime/src/ui/text/layout_engine/direction.rs
  - zircon_runtime/src/ui/text/layout_engine/ellipsis.rs
  - zircon_runtime/src/ui/text/layout_engine/line_box.rs
  - zircon_runtime/src/ui/text/layout_engine/overflow_style.rs
  - zircon_runtime/src/ui/text/layout_engine/range_mapping.rs
  - zircon_runtime/src/ui/text/layout_engine/visual_order.rs
  - zircon_runtime/src/ui/text/layout_engine/wrapping.rs
  - zircon_runtime/src/ui/text/measure_cache.rs
  - zircon_runtime/src/ui/surface/mod.rs
  - zircon_runtime/src/ui/surface/text_geometry.rs
  - zircon_runtime/src/ui/surface/text_shape.rs
  - zircon_runtime/src/ui/text/hit_test.rs
  - zircon_runtime/src/ui/text/geometry.rs
  - zircon_runtime/src/ui/text/resolved_layout.rs
  - zircon_runtime/src/graphics/text/mod.rs
  - zircon_runtime/src/graphics/text/layout/mod.rs
  - zircon_runtime/src/graphics/text/layout/line_break/mod.rs
  - zircon_runtime/src/graphics/text/layout/line_break/tests.rs
  - zircon_runtime/src/graphics/text/layout/line_break/glue.rs
  - zircon_runtime/src/graphics/text/layout/line_break/glyph_fallback.rs
  - zircon_runtime/src/graphics/text/layout/line_break/smart.rs
  - zircon_runtime/src/graphics/text/layout/line_break/soft_hyphen.rs
  - zircon_runtime/src/graphics/text/layout/line_break/wrap_space.rs
  - zircon_runtime/src/graphics/text/layout/kinsoku.rs
  - zircon_runtime/src/graphics/text/layout/kinsoku/tests.rs
  - zircon_runtime/src/graphics/text/layout/align.rs
  - zircon_runtime/src/graphics/text/layout/overflow.rs
  - zircon_runtime/src/graphics/text/layout/tab.rs
  - zircon_runtime/src/graphics/text/layout/measure.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/text.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/text_pixel_snap.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_advances.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_char_run.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout/tests.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/placement.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs/tests.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs/row.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs/row/tests.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_text/raster.rs
  - zircon_runtime/src/ui/surface/render/resolve.rs
  - zircon_runtime/src/ui/surface/render/text_measure.rs
  - zircon_runtime/src/ui/surface/input/editable_text/ime_context.rs
  - zircon_runtime/src/ui/tests/widget_text_input_ime_context.rs
  - zircon_runtime_interface/src/ui/surface/render/resolved_style.rs
  - zircon_runtime_interface/src/ui/surface/render/command.rs
  - zircon_runtime_interface/src/ui/surface/render/text_geometry.rs
  - zircon_runtime_interface/src/ui/surface/render/text_layout.rs
  - zircon_runtime_interface/src/ui/surface/render/text_shape.rs
  - zircon_runtime_interface/src/tests/render_contracts.rs
  - zircon_runtime/src/ui/tests/text_layout/mod.rs
  - zircon_runtime/src/ui/tests/text_layout/alignment.rs
  - zircon_runtime/src/ui/tests/text_layout/wrapping.rs
  - zircon_runtime/src/ui/tests/text_layout/overflow.rs
  - zircon_runtime/src/ui/tests/text_layout/direction.rs
  - zircon_runtime/src/ui/tests/text_layout/edit_state.rs
design_references:
  - dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Fonts/FontCache.h
  - dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Private/Fonts/FontMeasure.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Fonts/FontMeasure.h
  - dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Text/TextLayout.h
  - dev/godot/servers/text/text_server.h
  - dev/godot/modules/text_server_adv/text_server_adv.cpp
  - dev/slint/internal/core/textlayout/linebreaker.rs
  - dev/slint/internal/core/textlayout/linebreak_unicode.rs
  - dev/Fyrox/fyrox-ui/src/formatted_text/textwrapper.rs
plan_sources:
  - docs/plans/zircon_runtime/text/index.md
  - docs/plans/zircon_runtime/text/02-shaping-unicode-and-bidi.md
  - docs/plans/zircon_editor/editor_layout/17-text-rendering-and-typography.md
status: in_progress
---

# 03 换行规则 / 文本长度计算 / 布局 / 对齐

> 本计划在 `02` 的 `ShapedGlyphRun` 之上做**行切分、度量、对齐、竖排布局**。它是 `editor_layout/17 G1`(测量=绘制)与 G3(默认多行换行)的运行时实现,根治"等宽近似 → 错位/溢出/`Sce` 截断"。

## 1. 目标

1. **文本长度计算(UE 对齐)**:基于真实字形 advance/kerning 的度量,支持**子范围度量**(对齐 `FShapedGlyphSequence::GetMeasuredWidth(StartIndex, EndIndex)`)、行高/上下行距、tab stop、首行缩进;BIDI 与竖排下度量正确。
2. **换行规则**:UAX#14 行断机会 + word/glyph 模式 + **CJK 行首尾禁则**(避头尾)+ 连字符(soft hyphen + 字典可选)+ 长词逐字回退 + 不可断空白处理。
3. **对齐与两端对齐**:left/center/right/start/end(随 BIDI 基方向)+ justify(词间 + CJK 字间 + 阿拉伯 kashida 可选,对齐 godot `JustificationFlag`)。
4. **溢出**:clip / ellipsis(首/中/尾省略)/ shrink-to-fit / clamp 字号(接 `editor_layout/17 §规则4`)。
5. **竖排布局**:列切分(主轴 y)、列间距、竖排禁则、行(列)对齐。

## 2. 现状与差距

- `layout_engine.rs`:`layout_text`/`wrap_source_runs`/`append_word_wrapped_segment`/`ellipsize_line`/`aligned_x` 全部建立在 `text_advance(font_size)=font_size*0.5` 等宽近似上 → `editor_layout/17 G1` 根因;`baseline: font_size*0.8` 硬编码 → 垂直错位。
- 换行:有 `UiTextWrap::{None,Word,Glyph}`,但 word 仅按空格(不识 CJK 无空格断点、不走 UAX#14)、无禁则、无连字符。
- `measure_cache.rs`:宽度桶缓存在,但喂启发式宽度;`graphics/text/layout/measure.rs` 已有 owner-local source byte 子范围度量首段并喂回 `measured_grapheme_widths(...)`;2026-07-03 已通过 `ui::surface::measure_text_source_range_width(...)` 暴露 include-kerning=true 的 public source-range measure 入口;2026-07-04 `ui/text/geometry.rs` 的 IME/caret 简单 LTR source-isomorphic 路径已直接消费 source-range shaped width,并通过 `ui/surface/text_geometry.rs` 暴露 public cursor/range geometry surface;但 cache direct consumer、复杂 BIDI/竖排 source-range 几何与 `include_kerning=false` 的真实去 kerning 语义仍等待后端 unkerned advance 与 cluster reverse lookup。
- `hit_test.rs`:已优先消费 `UiResolvedTextLine.glyph_advances` 做视觉 grapheme midpoint 反查,tab/justify/kashida 等 layout-stage advance 结果不再被默认样式重测量覆盖；仍需完整 backend `ShapedGlyph.source_range` cluster 反查、UAX#9 level/isolate 视觉序与竖排命中测试。
- Justify、shrink-to-fit、clamp font size、tab stop 已有首段；kashida 已有 advance-based 首段但尚未插入真实 tatweel glyph；2026-07-03 已补 SDF char-run 对 resolved grapheme advances 的投影与 mixed overlay whole-grapheme fallback 首段,并补 `sdf_char_run.rs` 对 ZWJ/zero-width/Bidi format/variation selector 等 invisible format controls 的零槽位/零 advance 规则,避免 `glyph_advances` 契约在组合字符/emoji cluster 或 format-control scalar 上被字符数校验/错误 fallback advance 打断；同日 `text_pixel_snap.rs` 把 native glyphon `TextArea` 与 SDF draw planning 的文本 frame 原点贴像素规则合并到单一 owner,避免布局 frame 小数原点在 native/SDF 上屏阶段重新制造左右落点漂移；后续又把 horizontal/vertical SDF glyph bitmap frame 的 x/y 原点经 `text_glyph_device_frame(...)` 同步吸附,保留 layout advances 与 bitmap extent 但消除小字号 glyph bitmap 左右小数落点；editor retained-host 已把 runtime/host advance 与 shaped-origin 接受路径改成 fail-closed,并在 2026-07-04 将每 grapheme/shaped-origin advance 可接受窗固定为 `0.0625px`,让 `editor base.zui` 这类小字号 label 的 0.125px 局部借位直接回退 host natural spacing；同日又新增 retained 1/8px phase-bin 接受门,即使 `folder-open.svg` 这类 label 只有 +0.05px shaped-origin delta,只要会把 glyph 推到另一个最终 raster phase 也会回退 host natural spacing；仍缺复杂 overflow/shrink/clamp/tab 交互、完整 native/SDF paragraph parity、真实 shaping cluster backend 数据与竖排布局。

- `zircon_editor` retained-host fallback raster:2026-07-04 继续处理同一用户截图中“字体已是等线但字符左右间距/落点仍怪”的观感问题。布局桥已保证 host/runtime advances 与 shaped positions fail-closed 后,本轮确认更低层 fontdue fallback alpha mask 仍未消费 pen-origin subpixel phase,会让小字号 tab/file labels 在整数像素采样上显得忽左忽右；`draw/glyphs.rs`/`raster.rs`/`draw/glyphs/row.rs` 现在把 retained placement phase 以 8x/8-bin 传到 fallback downsampling,把最大 retained-host origin 量化误差收窄到 0.0625px。该修复不改变 `layout_text` 算法、ZUI 控件字体族、root painter 或 runtime atlas owner,仍需 live editor window typography QA 与 focused Cargo 绿跑。

- `zircon_editor` retained-host framebuffer ink spacing guard:2026-07-04 在同一截图问题上新增 framebuffer 级回归,不只看命令录制或示意图。`paint_text_tests.rs::retained_text_editor_crop_labels_keep_stable_ink_spacing` 直接绘制 `editor base.zui` 与窄 `folder-open.svg`,扫描真实 `HostRgbaFrame` 的 ink left edge、ink center、painted pixel count 与 internal empty columns,并比较 8.875px/8.925px 近起点,防止布局桥和 retained raster phase 修复后仍出现可见左右跳。该守卫不替代 live editor window typography QA,focused Cargo 仍需空闲 lane 绿跑。

## 3. 参考代码

| 文件 | 应重点阅读 |
|------|-----------|
| `dev/UnrealEngine/.../Fonts/FontCache.h` | `FShapedGlyphSequence::GetMeasuredWidth`/`GetMeasuredWidth(Start,End,bIncludeKerning)`/`GetGlyphAtOffset`(像素偏移→字形,带边界):**本计划度量与命中测试主样板**;`FShapedGlyphSequence` 的 `TextBaseline`/`MaxTextHeight`/`SourceTextRange` |
| `dev/UnrealEngine/.../Fonts/FontMeasure.cpp/.h` | `FSlateFontMeasure::MeasureStringInternal`/`Measure`/`FindLastWholeCharacterIndexBeforeOffset`:子串度量与 offset 反查的实现细节 |
| `dev/UnrealEngine/.../Framework/Text/TextLayout.h` | `FTextLayout`:`ETextJustify`、`ETextWrappingPolicy`、`FTextLayout::FlowDirection`、line view/run/block 的布局模型、`CreateWrappingCache`、`MarginAndJustification` |
| `dev/godot/servers/text/text_server.h` | `AutowrapMode::{OFF,ARBITRARY,WORD,WORD_SMART}`、`LineBreakFlag`、`OverrunBehavior::{TRIM_CHAR,TRIM_WORD,TRIM_ELLIPSIS,...}`、`JustificationFlag::{KASHIDA,WORD_BOUND,TRIM_EDGE_SPACES,...}`——换行/溢出/两端对齐枚举权威 |
| `dev/godot/modules/text_server_adv/text_server_adv.cpp` | `shaped_text_get_line_breaks`(ICU `ubrk_*` 行断)、`shaped_text_fit_to_width`(kashida + 字间 justify)、CJK 禁则——本计划换行与 justify 算法对照 |
| `dev/slint/.../textlayout/{linebreaker,linebreak_unicode}.rs` | Rust UAX#14 行断器 + 简单回退:断点机会编码、贪心断行循环——落地首选参照 |
| `dev/Fyrox/fyrox-ui/.../formatted_text/textwrapper.rs` | 极简 `TextWrapMode::{NoWrap,AtWidth,ByWords}` + 空白修剪 Rust 实现 |

**Rust/wgpu 落地**:`unicode-linebreak`(UAX#14 break opportunities,cosmic-text 内置)、cosmic-text `Buffer::set_size` + `layout_runs`(已做断行+对齐,可直接消费其行结果)。CJK 禁则与 justify 在 cosmic-text 结果之上后处理。(2026-07-02 评审收口:按 D1 改判——`layout_runs` 仅作对拍参考,不进入生产链路,见 §4/§7。)

## 4. 目标架构

```
ShapedGlyphRun(02, 无宽度约束 + 断点机会) + LayoutConstraints { wrap_width, wrap_mode, align, justify, overflow, tab, orientation }
  └─ line_break(UAX#14 机会 + CJK 禁则 + 连字符) → 贪心/逐字断行 →
     measure(真实 advance/kerning,子范围可查) → align/justify(行内分布) →
     overflow(ellipsis/shrink/clamp) → LaidOutText { lines[LaidOutLine], size, baseline 表 }
```

度量与布局分两层:**measure-only**(taffy 测量闭包用,只算尺寸,走 shaped+measure 缓存,不产顶点)与 **full layout**(产 `LaidOutText`,含每字形定位)。两者共享 shaping 与断行,measure 短路在 size 计算后(接 `editor_layout/17` 两阶段 + `render/14` "measure 必须走 shaped cache")。

**换行归属裁决(2026-07-02 评审收口,D1)**:02 只交付"无宽度约束整形+断点机会标注",不产出行;行切分/重排归 03 自研贪心断行;cosmic-text `Buffer::set_size`+`layout_runs` 仅作对拍参考。shaped cache 键不含 wrap。

**BIDI 行级视觉重排裁决(2026-07-02 评审收口,D2)**:02 输出逻辑序 glyph + 每 glyph `bidi_level: u8`;UAX#9 L1/L2 行级视觉重排由 03 在断行后调用 02 `bidi.rs` 的 per-line reorder API 完成。

## 5. 里程碑

### LB-M1 真实度量(度量=绘制)

实施切片:
1. `graphics/text/layout/measure.rs`:基于 `ShapedGlyphRun` 的 `measure_text_size`、子范围度量 `measured_width(run, byte_start, byte_end, include_kerning)`(UE `GetMeasuredWidth` 对齐);ascent/descent/line_height 取真实 face metrics。
2. 替换 `ui/text` 启发式 measure:`text_measure.rs`、`measure_cache.rs` 改喂真实度量(`render/14` 硬切换 #7);baseline 取真实 ascent。

测试:`text_measure_width_matches_shaped_advance_sum`、`text_measure_subrange_matches_ue_semantics`、`text_measure_cjk_fullwidth_advance`。

### LB-M2 换行与禁则

实施切片:
1. `layout/line_break/mod.rs`:UAX#14 机会(cosmic-text/unicode-linebreak)+ 贪心断行;`wrap_mode = None|Word|Glyph|WordSmart`;长词逐字回退。`layout/line_break/glue.rs` 只承接不可断 glue 与 variation selector 分类,`layout/line_break/glyph_fallback.rs` 只承接普通过宽 chunk 是否退回 glyph wrapping 的宽度/字素数 predicate,`layout/line_break/soft_hyphen.rs` 只承接 U+00AD visual chunk 拆分与 break suffix metadata,`layout/line_break/wrap_space.rs` 只承接普通 ASCII wrap-space edge trimming,`layout/line_break/smart.rs` 承接 WordSmart 首段收尾标点 glue、Unicode ellipsis/leader、standalone interrobang 与 double/interrobang 尾标点和连续收尾标点 cluster re-split,避免主断行 owner 继续混入 Unicode glue 表、glyph fallback 宽度策略、soft-hyphen source/visual 特例、smart-wrap 标点特例或 UI-local 空格策略。
2. CJK 行首尾禁则(避头尾):行首禁止标点(`、。」』）` 等)、行尾禁止开括号(`「『（` 等);kinsoku 表 + 挤压/移出策略(对照 godot)。
3. 连字符:soft hyphen(U+00AD)断点 + 末尾连字符字形;字典连字符为可选 feature。
4. (2026-07-02 评审收口)泰/老/高棉字典断行 **V1 豁免**:这三类无空格 SEA 脚本的词典断行(需要 ICU 词典数据)V1 不做,豁免期按 glyph 断;换行黄金集中的泰文用例期望值按该豁免标定并显式标注 `sea_dictionary_break_exempt`。后续以 `icu_segmenter` 可选 feature 单独立项补齐。

测试:`text_wrap_word_breaks_at_uax14_opportunities`、`text_wrap_cjk_kinsoku_no_leading_punctuation`、`text_wrap_long_word_falls_back_to_glyph`、`text_wrap_soft_hyphen_inserts_hyphen`。

### LB-M3 对齐 / 两端对齐 / 溢出

实施切片:
1. align(left/center/right/start/end 随 BIDI);justify(词间均分 + CJK 字间 + kashida 可选)。
2. overflow:ellipsis(首/中/尾,对齐 godot `OverrunBehavior`)、shrink-to-fit、clamp 字号(`editor_layout/17` 规则4);tab stop。
3. (2026-07-02 评审收口)`max_lines`(超行截断+末可见行尾省略)与 `first_line_indent`(首行缩进)在本里程碑落地,类型定义见 §6 `LayoutConstraints`。

测试:`text_align_end_follows_rtl_base_direction`、`text_justify_distributes_word_and_cjk_gaps`、`text_overflow_ellipsis_middle_keeps_head_tail`、`text_shrink_to_fit_scales_within_bounds`。

### LB-M4 竖排布局

实施切片:
1. 竖排列切分(主轴 y、列宽=字号+列距)、列对齐、竖排禁则;`LaidOutLine` 在竖排语义下为"列"。
2. 2026-07-01 首个实现切片已把 `UiTextWritingMode::{HorizontalTb,VerticalRl}` 加入 public contract,并贯穿 `UiResolvedStyle`、`UiResolvedTextLayout`、`UiShapedText` 与 `UiTextPaint`;surface parser 接受 `writing_mode = "vertical-rl"` / `font.writing_mode = "vertical-rl"`。UI resolved-layout 首段由 `zircon_runtime/src/ui/text/layout_engine/vertical.rs` 承接:以 frame height 为主轴断列,列从右向左排布,复用现有 CJK kinsoku chunk metadata,shaped DTO 的竖排 glyph frame 沿 y 轴推进并给 ASCII glyph 标记 `Cw90`。
3. 2026-07-01 vertical_rl 几何消费首段已把 `ui/text/hit_test.rs` 与 `ui/text/geometry.rs` 接到竖排语义:命中测试按 x 选择右到左列、按 y/glyph advance midpoint 反查 source byte offset;caret/range/IME cursor rect 在竖排下投影为横向 1px bar,避免继续使用横排竖线几何。
4. 2026-07-01 render-contract 编辑装饰消费首段已把 `zircon_runtime_interface/src/ui/surface/render/text_geometry.rs` 接到 `UiResolvedTextLayout.writing_mode`:selection 在竖排下使用整列宽度与 y 轴 range span,composition underline 改为列右侧竖向 side rule,caret 改为横向 bar,避免 neutral paint DTO 在平台绘制前仍输出横排装饰几何。
5. 2026-07-02 SDF render 消费首段已把 `ScreenSpaceUiTextBatch.writing_mode` 从 render extract 传到 `sdf_render.rs`;`VerticalRl` 下 SDF glyph quad 沿 y 轴推进并按列中心投影,避免布局 DTO 已是竖排但 SDF 上屏仍按横排 cursor_x 排列。该切片只处理 vertex projection,字体竖排替换与 Latin sideways rotation 仍属于 shaping/font-orientation 后续。

测试:`vertical_rl_wraps_columns_on_frame_height`、`text_vertical_kinsoku_applies_to_column_break`、`render_extract_parses_vertical_rl_writing_mode_layout`、`ui_text_writing_mode_vertical_rl_serializes_as_contract_value`、`ui_shaped_text_contract_derives_vertical_rl_glyph_bounds`、`text_hit_test_vertical_rl_uses_column_x_and_vertical_advances`、`source_geometry_uses_vertical_writing_mode_advances`、`text_input_ime_cursor_rect_uses_vertical_rl_geometry`、`ui_text_decorations_use_vertical_rl_geometry`、`sdf_draw_plan_vertical_rl_advances_glyphs_on_y_axis`。

(2026-07-02 评审收口)LB-M4 收束条件补充:`ui/text/layout_engine/vertical.rs` 首段实现需在本里程碑收尾时语义迁移到 `graphics/text/layout/vertical_layout.rs`,UI 层仅保留投影消费;迁移条目已列入 §6 硬切换表。

## 6. 工程落地细化(实施权威)

### 模块与文件落点

实现层 `zircon_runtime/src/graphics/text/layout/`:

| 文件 | 内容 |
|------|------|
| `mod.rs` | 布局入口装配(薄):`layout(run, constraints) -> LaidOutText`、`measure(...) -> Vec2` |
| `measure.rs` | 真实度量:总宽/子范围宽/行高/ascent/descent;UE `GetMeasuredWidth` 对齐 |
| `line_break/mod.rs` | UAX#14 机会 + 贪心断行 + 长词逐字 + 连字符 |
| `line_break/glue.rs` | NBSP/ZWJ/NBHY/NNBSP/WJ/ZWNBSP/variation selector 等不可断 glue 分类,输出给 `LineBreakChunk.allow_glyph_fallback` |
| `line_break/glyph_fallback.rs` | 普通过宽 chunk 是否退回 glyph wrapping 的 shared measured-width + grapheme-count predicate |
| `line_break/soft_hyphen.rs` | U+00AD soft hyphen visual chunk 剥离、断点 `-` suffix metadata 与 source range 归属 |
| `line_break/wrap_space.rs` | ASCII wrap-space 行首/行尾 trimming 策略,输出 source offset 与 byte count 给 UI run/range mutation |
| `line_break/smart.rs` | WordSmart 首段 smart-wrap 策略:ASCII 收尾标点 `,.:;!?`、Unicode ellipsis/leader `…`/`‥`、standalone interrobang `‽`、Unicode double/interrobang punctuation `‼`/`⁇`/`⁈`/`⁉` 与 Arabic/RTL 常见收尾标点 `،`/`؛`/`؟` 绑定前一词,支持标点后 ASCII/Unicode right closing quote 链 `"`/`'`/`’`/`”`,并覆盖全角/CJK 收尾标点 `、。，．・：；！？`、其后的 CJK/fullwidth 闭合符号链 `）］｝｠】〕〉》」』〗〙〛〟〞＂＇`、以及 `go?!` / `go！？` / `go，」！` / `go‽!` / `go⁉!` / `go؟!` 这类连续收尾标点 cluster,必要时在 chunk 内循环切分并关闭 protected 段 glyph fallback,但不吸收后续词 |
| `ui/text/layout_engine/wrapping.rs` | UI Word/Glyph wrapping orchestration、newline segmentation、leading grapheme continuation 与 line width fit helper |
| `ui/text/layout_engine/candidate_line.rs` | UI candidate line text/run/source/visual range mutation、pending break suffix 追加与 trailing wrap-space run 修剪 |
| `ui/text/layout_engine/direction.rs` | UI paragraph/base direction first-strong 解析、strong char helpers 与 RTL direction predicate |
| `ui/text/layout_engine/ellipsis.rs` | UI ellipsis projection: clipped-line merge、shared overflow segment 到 rich run/source/visual range remap、ellipsis marker run insertion |
| `ui/text/layout_engine/line_box.rs` | UI line-box measured/tab-aligned advances、Justify gating、line width clamp、logical Start/End x alignment 与 fallback advance minimum |
| `ui/text/layout_engine/range_mapping.rs` | UI layout 内部 source/visual byte subrange mapping,供 ellipsis projection 与 visual-order 重排共同消费 |
| `kinsoku.rs` / `kinsoku/tests.rs` | CJK 避头尾禁则表 + 挤压/移出; module-local owner regressions 独立承接 halfwidth kana、JLREQ pair、white bracket 等边界 |
| `align.rs` | align + justify(词间/字间/kashida) |
| `overflow.rs` | ellipsis(首/中/尾/word-trim) |
| `tab.rs` | tab stop / `tab_size` advance expansion |
| `vertical_layout.rs` | 竖排列切分与对齐 |
| `ui/text/layout_engine/vertical.rs` | 当前 UI `VerticalRl` resolved-layout 首段 owner:高度主轴断列、右到左列 placement、height-based overflow extent、复用 kinsoku chunks |

### 核心类型与接口

```rust
pub struct LayoutConstraints {
    pub wrap_width: Option<f32>,     // 竖排时为 wrap_height
    pub wrap_mode: TextWrapMode,     // None | Word | Glyph | WordSmart
    pub align: TextAlign,            // Left|Center|Right|Start|End|Justify
    pub justify: JustifyFlags,       // WordBound | CjkInter | Kashida | TrimEdgeSpaces
    pub overflow: TextOverflow,      // Clip | Ellipsis(EllipsisPos) | ShrinkToFit | Clamp
    pub line_height: LineHeight,     // Normal | Scale(f32) | Absolute(f32)
    pub tab_stops: TabStops,
    pub orientation: TextOrientation,// 接 02
    // (2026-07-02 评审收口)新增:
    pub max_lines: Option<u32>,      // 超出行数截断;末可见行行尾按 overflow 的 ellipsis 语义收尾
    pub first_line_indent: f32,      // 首行缩进(逻辑像素);实现归 LB-M3
}

// (2026-07-02 评审收口)TabStops 完整定义:
pub struct TabStops {
    pub default_tab_size: u32,           // 默认制表宽 = tab_size × space_advance(主 face 空格 advance)
    pub stops: Vec<TabStop>,             // 显式 stop,按 x 升序;越过全部显式 stop 后回落默认宽
}
pub struct TabStop { pub x: f32, pub align: TabAlign }
pub enum TabAlign { Left, Center, Right, Decimal } // Decimal 按小数点字符对齐
// (2026-07-02 评审收口)EllipsisPos 完整定义(对齐 godot OverrunBehavior 的 TRIM_ELLIPSIS 族):
pub enum EllipsisPos { Start, Middle, End }

// (2026-07-02 评审收口,D1/07 内联对象)LaidOutText 改多 run 模型:
pub struct LaidOutText {
    pub runs: Vec<Arc<ShapedGlyphRun>>, // 复用 02 字形;富文本/回退分段时 >1
    pub items: Vec<LayoutItem>,         // 布局项序列(逻辑序)
    pub lines: Vec<LaidOutLine>,
    pub size: Vec2,
}
pub enum LayoutItem {
    Text { run_index: u32, glyph_range: (u32, u32) },
    Inline { object_id: InlineObjectId, size: Vec2, baseline: InlineBaseline }, // 预留:07 §4 内联对象(image/widget)回填
}
pub struct LaidOutLine {
    pub spans: Vec<(u32 /* run_index */, (u32, u32) /* glyph_range */)>, // 行内按逻辑序引用各 run 片段
    pub visual_order: Vec<u32>,      // (2026-07-02 评审收口,D2)行级视觉序:spans 索引的视觉排列,由 02 bidi.rs per-line reorder API 产出
    pub origin: Vec2, pub baseline: f32, pub width: f32,
    pub ascent: f32, pub descent: f32, pub trailing_whitespace: f32,
}

// (2026-07-02 评审收口)caret 亲和性:软换行行尾/BIDI 边界同一 offset 有两个视觉位置
pub enum CaretAffinity { Upstream, Downstream }
// - hit_test 返回 (source_offset, CaretAffinity);
// - caret 几何计算入参含 affinity:软换行点 offset 处,Upstream → 上一行行尾 caret,Downstream → 下一行行首 caret;
// - edit_state 光标携带 affinity(点击/End 键置 Upstream,Home/常规输入置 Downstream);
// - 08 IME 光标矩形(SetCursorArea 入参)引用同一 (offset, affinity) 模型,不另建几何。

// 度量(UE FShapedGlyphSequence::GetMeasuredWidth 对齐)
pub fn measured_width(run: &ShapedGlyphRun, byte_start: u32, byte_end: u32, include_kerning: bool) -> f32;
pub fn measure_text_size(run: &ShapedGlyphRun, c: &LayoutConstraints) -> Vec2;
```

(2026-07-02 评审收口)`LaidOutText` 多 run 化后,单 run 场景 `runs.len()==1` 且 `items` 只有一个 `Text` 项,现有单 run 消费方按 `spans[0]` 语义平移;`LayoutItem::Inline` 的解析来源与 `InlineBaseline` 定义见 07 §4(本处仅预留槽位,07 落地时回填)。

### 度量算法(对齐 UE)

- 总宽 = Σ glyph.advance(行内,trailing whitespace 不计入 content width,但 layout width 含)。
- **子范围度量**:给字节区间 `[s, e)`,累加 `source_range` 落入 `[s,e)` 的 glyph advance;`include_kerning=false` 时减去簇间 GPOS 调整(UE `bIncludeKerning` 语义)——供光标/选区精确定位。
- 行高:`Normal` = ascent+descent+line_gap(face hhea/OS2);`Scale(k)` = font_size×k;baseline = line_top + ascent。
- BIDI:度量按逻辑序累加(顺序无关于视觉序);命中测试用视觉序 + `source_range` 反查(`hit_test.rs` 改造,`render/14` 硬切换 #3)。
- **visual_order 按行应用(2026-07-02 评审收口,D2)**:视觉重排是**行级**操作——断行完成后,对每一 `LaidOutLine` 的 spans 调用 02 `bidi.rs` 的 per-line reorder API(UAX#9 L1:行尾空白/分隔符复位为段落 level;L2:按 level 由高到低逐段反转),结果写入 `LaidOutLine.visual_order`;glyph 存储保持逻辑序不动,渲染/命中测试按 `visual_order` 遍历。禁止在整形阶段或段落级做一次性重排。
- 竖排:advance 在 y,width→height 语义对调。

#### 混 face 行度量(2026-07-02 评审收口,D7)

- 一行内含多个 face(fallback/富文本混排)时:行 `ascent`/`descent` = 行内各 run face 度量的 **max**;`line_gap` 取**主 face**(段落解析出的首选 face),不参与 max。
- 单 face 度量优先级:OS/2 `fsSelection` 的 `USE_TYPO_METRICS` 位置位时取 `sTypoAscender/sTypoDescender/sTypoLineGap`,否则取 `hhea` 的 ascent/descent/line_gap;`usWinAscent/usWinDescent` 仅作裁剪参考,不进行高计算。
- baseline 统一 alphabetic:所有 run 对齐到同一 alphabetic baseline,行 baseline = line_top + max_ascent。
- 测试:`text_line_height_mixed_face_uses_max_metrics`(拉丁主 face + CJK fallback face 混行,断言行高 = max(ascent)+max(descent)+主 face line_gap,baseline 取 max_ascent)。

### CJK 禁则(`kinsoku.rs`,对照 godot)

- 行首禁则集(不能出现在行首):`、。，．・：；！？）］｝｠】〕〉》」』〗〙〛’”〟〞`、小书写假名 `ぁぃぅぇぉっゃゅょゎゕゖァィゥェォッャュョヮヵヶㇰㇱㇲㇳㇴㇵㇶㇷㇸㇹㇺㇻㇼㇽㇾㇿ`、日文非行首字符 `ー々〻ゝゞヽヾ`、JLREQ hyphens `‐〜゠–`、全角 spacing voicing marks `゛゜` 与半角 `｡｣､･ｧｨｩｪｫｯｬｭｮｰﾞﾟ` …
- JLREQ cl-08 分离禁止成对字符:`——`、`……`、`‥‥`、`〳〵`、`〴〵` 之间不可断;跨 chunk 合并并对单 chunk 内 pair 关闭 glyph fallback,避免过窄 frame 把第二个符号拆到下一行。
- 行尾禁则集(不能出现在行尾):`（｛｟［【〔〈《「『〖〘〚‘“〝` 与半角 `｢`。
- 策略:断点落在禁则字符时,优先**前移**断点(把行首禁则字符挤到上一行末——"追い込み"),次选**移出**(把行尾禁则字符移到下一行——"追い出し");可配 squeeze 标点半角。

### 与既有路径硬切换(`render/14` 清单 #2/#3/#7)

| 现有 | 切换 |
|------|------|
| `layout_engine.rs` 全体(等宽 wrap/align/ellipsize/baseline) | 删除;语义迁 `graphics/text/layout/*`(真实度量重写) |
| `layout_engine/tests.rs` 期望值 | 按真实字形度量重标定 |
| `hit_test.rs::hit_test_text_layout` | 改基于 `ShapedGlyph.source_range` 反查;签名/返回类型不变 |
| `text_measure.rs::measure_text_size` | 改调 `graphics/text/layout::measure`(taffy measure 闭包,走 shaped cache) |
| `ui/text/layout_engine/vertical.rs`(竖排首段) | (2026-07-02 评审收口)语义迁 `graphics/text/layout/vertical_layout.rs`;UI 层退化为投影(仅消费 graphics 层竖排结果,不再自持断列/列排布逻辑) |

### 测试与验收清单

| 测试 | 断言 |
|------|------|
| `text_measure_width_matches_shaped_advance_sum` | 度量宽 = Σadvance(含 kerning),与绘制端一致(度量=绘制) |
| `text_measure_subrange_matches_ue_semantics` | 子范围宽对 UE `GetMeasuredWidth` 期望表;include_kerning 两路径 |
| `text_measure_cjk_fullwidth_advance` | CJK 全角 advance = 字号(对照 face) |
| `text_wrap_word_breaks_at_uax14_opportunities` | 断点集 = UAX#14 机会(对照标准用例) |
| `text_wrap_cjk_kinsoku_no_leading_punctuation` | 行首无禁则标点;追い込み/追い出し正确 |
| `text_wrap_cjk_kinsoku_no_leading_halfwidth_small_kana` | 半角小假名不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_small_katakana_ka` | 小写 ka/ke 假名 `ゕゖヵヶ` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_katakana_phonetic_extension_small_kana` | 片假名音标扩展小假名 `ㇰ..ㇿ` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_cjk_white_close_punctuation` | CJK 白闭括号/引号 `〗〙〛〟` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_cjk_double_prime_closing_quote` | CJK 双素引号闭合变体 `〞` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_fullwidth_white_close_parenthesis` | 全角白右圆括号 `｠` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_spacing_voicing_mark` | 全角 spacing dakuten/handakuten `゛゜` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_jlreq_hyphen` | JLREQ hyphens `‐〜゠–` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_keeps_jlreq_inseparable_ellipsis_pair_together` | JLREQ cl-08 `……` 在过窄 frame 下不可在两个 `…` 之间断行;允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_prolonged_sound_mark` | 日文全角长音符不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_iteration_mark` | 日文迭代符号不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_leading_vertical_iteration_mark` | U+303B vertical ideographic iteration mark `〻` 不可成为行首;过窄 frame 下允许 protected overhang |
| `text_wrap_cjk_kinsoku_no_trailing_halfwidth_open_punctuation` | 半角开引号不可留在行尾;与后续 glyph 同 chunk |
| `text_wrap_cjk_kinsoku_no_trailing_cjk_white_open_punctuation` | CJK 白开括号/引号 `〖〘〚〝` 不可留在行尾;与后续 glyph 同 chunk |
| `text_wrap_cjk_kinsoku_no_trailing_fullwidth_white_open_parenthesis` | 全角白左圆括号 `｟` 不可留在行尾;与后续 glyph 同 chunk |
| `text_wrap_long_word_falls_back_to_glyph` | 超宽单词 Word 模式逐字断 |
| `text_wrap_soft_hyphen_inserts_hyphen` | U+00AD 断点末尾出连字符字形 |
| `word_wrap_keeps_variation_selector_sequence_together` | variation selector 与 base glyph 保持 glue;过窄 frame 下允许 overhang |
| `word_wrap_keeps_additional_glue_sequences_together` | NBHY/NNBSP/WJ/ZWNBSP 不被 glyph fallback 拆开 |
| `word_smart_keeps_ascii_trailing_punctuation_with_previous_word` | WordSmart 下 ASCII 收尾标点不可成为 wrapped line leader;`go,` 可 protected overhang |
| `word_smart_keeps_ascii_closing_quote_after_trailing_punctuation_with_previous_word` | WordSmart 下 ASCII 收尾标点后的闭合引号与前一词保持同一 protected chunk;`go,"` 可 protected overhang |
| `word_smart_keeps_unicode_closing_quote_after_trailing_punctuation_with_previous_word` | WordSmart 下 ASCII 收尾标点后的 Unicode right closing quote `’`/`”` 与前一词保持同一 protected chunk;`go,”` 可 protected overhang |
| `word_smart_keeps_fullwidth_trailing_punctuation_with_previous_word` | WordSmart 下全角/CJK 收尾标点 `，`/`。`/`？` 与前一词保持同一 protected chunk;`go，` 可 protected overhang |
| `word_smart_keeps_cjk_closing_delimiter_after_fullwidth_punctuation_with_previous_word` | WordSmart 下全角/CJK 收尾标点后的 CJK/fullwidth 闭合引号/括号与前一词保持同一 protected chunk;`go，」` 可 protected overhang |
| `word_smart_keeps_trailing_punctuation_cluster_without_absorbing_next_word` | WordSmart 下连续收尾标点 cluster `go?!` 保持前词 protected chunk,后续 `a` 仍独立换行候选 |
| `word_smart_keeps_ellipsis_trailing_punctuation_with_previous_word` | WordSmart 下 Unicode ellipsis/leader `…`/`‥` 与前一词保持 protected chunk,后续词仍独立换行候选 |
| `word_smart_keeps_unicode_interrobang_punctuation_with_previous_word` | WordSmart 下 Unicode standalone interrobang `‽` 与前一词保持 protected chunk,`go‽` 可 protected overhang 且后续词仍独立换行候选 |
| `word_smart_keeps_unicode_double_punctuation_with_previous_word` | WordSmart 下 Unicode double/interrobang punctuation `‼`/`⁇`/`⁈`/`⁉` 与前一词保持 protected chunk,`go⁉` 可 protected overhang 且后续词仍独立换行候选 |
| `word_smart_keeps_arabic_trailing_punctuation_with_previous_word` | WordSmart 下 Arabic/RTL 常见收尾标点 `،`/`؛`/`؟` 与前一词保持 protected chunk,`go؟` 可 protected overhang 且后续词仍独立换行候选 |
| `text_align_end_follows_rtl_base_direction` | End 对齐在 RTL 段落靠左 |
| `text_justify_distributes_word_and_cjk_gaps` | 两端对齐词间 + CJK 字间均分,末行不拉伸 |
| `text_overflow_ellipsis_middle_keeps_head_tail` | 中部省略保头尾,`…` 宽度计入 |
| `text_shrink_to_fit_scales_within_bounds` | 缩放后宽≤bounds,字号≥min clamp |
| `text_hit_test_maps_pixel_to_source_offset` | 像素点→源字节 offset,affinity 正确(对照 cluster) |
| `text_line_height_mixed_face_uses_max_metrics` | (2026-07-02 评审收口,D7)混 face 行:行高 = max(ascent)+max(descent)+主 face line_gap,baseline = max_ascent |
| `text_caret_affinity_soft_wrap_boundary` | (2026-07-02 评审收口)软换行点同一 offset:Upstream 返回上一行行尾 caret 矩形,Downstream 返回下一行行首 caret 矩形 |
| `vertical_rl_wraps_columns_on_frame_height` | 竖排按高度断列,列序右到左,`UiResolvedTextLayout.writing_mode` 保持 `VerticalRl` |
| `text_vertical_kinsoku_applies_to_column_break` | 竖排列断点复用 CJK 行尾/行首禁则,开标点不留在上一列末 |
| `render_extract_parses_vertical_rl_writing_mode_layout` | surface render extract 可解析 `writing_mode = "vertical-rl"` 并产出竖排 resolved layout |
| `ui_text_writing_mode_vertical_rl_serializes_as_contract_value` | public interface writing-mode contract 序列化为 `vertical_rl` |
| `ui_shaped_text_contract_derives_vertical_rl_glyph_bounds` | shaped DTO 继承竖排 writing mode,ASCII glyph 竖排 frame/rotation contract 有界 |
| `text_hit_test_vertical_rl_uses_column_x_and_vertical_advances` | 竖排命中测试先按 x 选择右到左列,再按 y midpoint/advance 返回 source byte offset |
| `source_geometry_uses_vertical_writing_mode_advances` | `caret_frame_for_text_layout`/`text_range_frames_for_text_layout` 在 `VerticalRl` 下把 advance 投影到 y,输出横向 caret/range bar |
| `text_input_ime_cursor_rect_uses_vertical_rl_geometry` | TextInput 的 IME cursor rect 复用竖排 resolved-layout caret frame,拒绝横排竖线 fallback |
| `ui_text_decorations_use_vertical_rl_geometry` | render-facing `UiTextPaintDecoration` 在 `VerticalRl` 下输出列宽 selection、右侧 composition side rule 和横向 caret bar |

里程碑命令:`cargo test -p zircon_runtime text_measure --locked`、`text_wrap --locked`、`text_align --locked`、`text_overflow --locked`。

## 7. 风险与回退

- cosmic-text `layout_runs` 已做断行/对齐,优先消费其结果再后处理 CJK 禁则/justify;若其断行不可控,改用 `unicode-linebreak` 机会 + 自研贪心。(2026-07-02 评审收口:本条已按 D1 改判——自研贪心断行是主链,cosmic-text `layout_runs` 定位为**对拍参考**(黄金集比对/回归定位工具),不作为生产回退路径。)
- Knuth-Plass 最优断行(段落级最小化破碎度)列为 V2,V1 用贪心(对齐多数引擎)。

## 8. 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证 | 后续 |
|------|-------------|------|------|------|------|
| 2026-07-04 | LB-M1/LB-M3 editor retained-host ink spacing guard | runtime_text_editor_retained_ink_spacing_guard_rustfmt_static_visual_cargo_deferred | 针对最新用户 editor crop 中“等线已生效但字符左右间距/渲染位置仍偏左或偏右”的剩余观感问题,`paint_text_tests.rs` 新增 `retained_text_editor_crop_labels_keep_stable_ink_spacing`,直接通过 `draw_text_with_size_and_style(...)` 渲染 `editor base.zui` 和 42px `folder-open.svg` 到 `HostRgbaFrame`,扫描像素轮廓并断言 full label 没有异常大内部空列、ellipsized narrow label 仍保持紧凑可读；同测例比较 8.875px 与 8.925px 两个近亚像素起点,要求 ink center 与 left edge 不产生超过 1px 的水平跳动。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text_tests.rs` 通过;验证图 `docs/tests/runtime/text/runtime_text_editor_retained_ink_spacing_guard_validation_20260704.png` SHA256 `EF2585989C59E78D25BA0F836A2554FDA509A1800486A3D978955ABA8643308D`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_ink_spacing_guard_validation_20260704.log` SHA256 `BD2583F1B4C7111BC45055E934303F839DD66C156B6BF7180A6A5F9B908B0AD6`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets`、`F:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | retained-host label 像素轮廓、内部空列和近起点水平稳定性已有 framebuffer 回归;真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy 与生产 bitmap atlas renderer 仍 open。 |
| 2026-07-04 | LB-M1/LB-M3 editor retained-host subpixel bin precision | runtime_text_editor_retained_subpixel_bin_precision_rustfmt_static_visual_cargo_deferred | 针对同一 editor tab/file label crop 中“等线已生效但字符左右间距/落点仍怪”的剩余问题,`paint_text/draw/glyphs.rs` 将 `TEXT_RASTER_SUPERSAMPLE` 提升到 8.0,`paint_text/draw/placement.rs` 将 retained pen-origin phase 收敛为 8 个 bin；`draw/glyphs/tests.rs` 锁定 20.90px 仍留在 20px cell 的 .875 phase,只有 20.95px 才进入下一像素,避免小字号 label 过早左右跳。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs/tests.rs` 通过;验证图 `docs/tests/runtime/text/runtime_text_editor_retained_subpixel_bin_precision_preview_20260704.png` SHA256 `5A4268CDF1774AFA81B9C9EF7A141F23BE6ED61623DE3F50E7C71C73F35E9CDD`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_subpixel_bin_precision_validation_20260704.log` SHA256 `6FD74EE60E988F92B13BABE18E5B7F764E4D5ECC58971364F1867E165EA2CCF0`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | retained-host pen-origin 最大量化误差从 0.125px 收窄到 0.0625px;真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy 与生产 bitmap atlas renderer 仍 open。 |
| 2026-07-04 | LB-M1/editor retained-host phase stability | runtime_text_editor_retained_phase_stability_rustfmt_static_visual_cargo_deferred | 针对用户 editor crop 中“等线已生效但字符左右间距/落点仍偏左或偏右”的剩余问题,新增 `paint_text/draw/placement.rs` 作为 retained 8-bin glyph phase owner,`draw/glyphs.rs` 复用同一 owner,`paint_text/draw/layout.rs` 在接受 runtime/shaped positions 前新增 retained raster-bin 保持检查；`draw/layout/tests.rs` 新增 `runtime_positioned_glyphs_rejects_shaped_positions_that_shift_raster_phase`,证明 `folder-open.svg` 在 3.93px 起点时 +0.05px shaped-origin delta 虽小但跨到下一 1/8px raster bin,因此回退 host natural spacing。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs/tests.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout/tests.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/placement.rs` 通过;验证图 `docs/tests/runtime/text/runtime_text_editor_retained_phase_stability_validation_20260704.png` SHA256 `3B2B0DCCC1570D8DA577493911C544641684412AD59ED21733D4E80D76FD9729`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_phase_stability_validation_20260704.log` SHA256 `D7ACD3AB64CCDCF3559931F6DA33D8F807CCDC4A70850F64071215486C662D43`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | retained-host shaped/runtime sub-tolerance phase crossing 已回退 host natural spacing;真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy、生产 bitmap atlas renderer 仍 open。 |
| 2026-07-04 | LB-M1/LB-M3 editor retained-host framebuffer crop regression | runtime_text_editor_retained_framebuffer_crop_regression_rustfmt_static_visual_cargo_deferred | 在 retained-host 公开文本测试面补更接近截图的像素回归:`paint_text_tests.rs` 新增 `retained_text_draws_editor_crop_labels_with_runtime_ellipsis_pixels`,直接把 `editor base.zui` 和 `folder-open.svg` 通过 `draw_text_with_size_and_style(...)` 画入 `HostRgbaFrame`,断言真实 frame 中有可见非背景像素；同一测试用 recording-only frame 记录 42px 窄 `folder-open.svg`,断言 recorded text 已是 runtime ellipsis 文本。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text_tests.rs` 通过;验证图 `docs/tests/runtime/text/runtime_text_editor_retained_framebuffer_crop_validation_20260704.png` SHA256 `96A4254E602DC5CCA5CAEA51190BFB9EE6B65DBA35B2026B1FF050CF4D34597D`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_framebuffer_crop_validation_20260704.log` SHA256 `A4305CCCD441C0BBEEABFD94826CA2316A329B1F9ED468C4B1CCBDB6C0972820`;该图为 framebuffer crop validation,不是 live window capture;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | retained-host label 像素输出和窄 label runtime ellipsis 录制已锁定;真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy、生产 bitmap atlas renderer 仍 open。 |
| 2026-07-04 | LB-M1/LB-M3 editor retained-host runtime ellipsis cutover | runtime_text_editor_retained_runtime_ellipsis_cutover_rustfmt_static_visual_cargo_deferred | 补齐 retained-host 单行窄标签省略号 hard-cutover 回归:`paint_text/draw/layout/tests.rs` 新增 `retained_text_run_uses_runtime_ellipsis_for_narrow_editor_labels`,用 `folder-open.svg`、runtime shaped ellipsis 宽度和窄 `FrameRect` 证明 editor retained `layout_text_run(...)` 消费 `layout_text(... UiTextOverflow::Ellipsis)` 的 line text,而不是旧 editor-local ellipsis helper。静态扫描确认旧 `paint_text/draw/ellipsis.rs` 不存在,`ellipsize_single_line`/`mod ellipsis`/`draw/ellipsis` 在 retained paint_text 下 0 命中。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout/tests.rs` 通过;scoped `git diff --check` exit 0(仅 LF-to-CRLF working-tree warning);验证图 `docs/tests/runtime/text/runtime_text_editor_retained_runtime_ellipsis_cutover_preview_20260704.png` SHA256 `CAF36D913152319BD119E45BF99318B791D86D317383442F81ABBB0CD8E43C12`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_runtime_ellipsis_cutover_validation_20260704.log` SHA256 `AE6E46C92C64E24DB3A71A03AD58704B03C207DB74C0E081F43904CC212E4C6E`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | retained-host 窄 label 的省略号显示所有权已锁到 runtime layout;真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy、生产 bitmap atlas renderer 仍 open。 |
| 2026-07-04 | LB-M1/editor retained-host advance tolerance precision | runtime_text_editor_retained_advance_tolerance_precision_rustfmt_static_visual_cargo_deferred | 针对用户 editor crop 中“等线已生效但字符左右间距/落点仍偏左或偏右”的剩余问题,`paint_text/draw/layout.rs` 将 retained-host 接受 runtime/shaped per-grapheme advance 的局部容差从旧的比例/上下限窗口改为固定 `RETAINED_GLYPH_ADVANCE_TOLERANCE_PX = 0.0625`;总宽容差不变,但 `editor base.zui` 的 0.125px 局部借位会 fail-closed 到 host natural spacing。`draw/layout/tests.rs` 新增 `runtime_positioned_glyphs_rejects_one_eighth_pixel_tab_label_jitter`。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout/tests.rs` 通过;验证图 `docs/tests/runtime/text/runtime_text_editor_retained_advance_tolerance_precision_preview_20260704.png` SHA256 `B9D0783FA13003567E2C10A2176342010F8D7306F061B229E9FA926A2F2BC995`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_advance_tolerance_precision_validation_20260704.log` SHA256 `461FC7C5FC2416240AF33CF2A278AF9EB0BA09BC17E3FDB0D326CB3B4C6D2AA2`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | retained-host 小字号 label 的 1/8px 局部字距抖动已回退 host natural spacing;真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy、生产 bitmap atlas renderer 仍 open。 |
| 2026-07-04 | LB-M1/AT-M2 retained fallback phase guard | runtime_text_editor_retained_fallback_subpixel_phase_rustfmt_static_visual_cargo_deferred | 继续收束用户 editor tab/file label 截图中的小字号字距/落点问题:在 retained-host layout/shaped-position 已 fail-closed 后,`draw/glyphs.rs` 以 pen-origin 选择 4 档 subpixel phase,`raster.rs` 为 fontdue fallback 记录 `sample_offset_x`,`draw/glyphs/row.rs` 用该 offset 修正 alpha-mask fallback downsampling；swash native/SubpixelMask 路径仍保持 phase-baked 语义。 | `rustfmt --edition 2021 --check` 覆盖 touched retained-host glyph/raster/test files 通过;验证图 `docs/tests/runtime/text/runtime_text_editor_retained_fallback_subpixel_phase_preview_20260704.png` SHA256 `DB111D54EEF562F0277D59C5ECABAECED92283ED9FEDCC2FC01919EB3728AFB4`;验证日志 `docs/tests/runtime/text/runtime_text_editor_retained_fallback_subpixel_phase_validation_20260704.log` SHA256 `2B39D23D80FF4310F4D6272903951D53D90B814DDBAC2C673D2C343AD4CD37EF`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred。 | 关闭 retained-host fallback phase loss 首段;完整 layout/cache consumer、backend cluster reverse lookup、RTL/vertical geometry、live editor typography QA 与 focused Cargo 绿跑仍 pending。 |
| 2026-07-04 | LB-M1 / public cursor-range geometry surface | runtime_text_public_cursor_geometry_surface_rustfmt_static_visual_cargo_deferred | 新增 `ui/surface/text_geometry.rs`,公开 `text_caret_frame_for_layout(...)` 与 `text_range_frames_for_layout(...)`,把已有 `ui/text/geometry.rs` source-range-aware caret/range geometry 挂到 public surface；`ui/surface/mod.rs` 仅新增 module/export；focused 回归锁定 simple LTR `Wi` 用 shaped source-range width 而非 stale advances,并锁定 tab text 继续走 resolved `glyph_advances` fallback | `rustfmt --edition 2021 --check zircon_runtime/src/ui/surface/mod.rs zircon_runtime/src/ui/surface/text_geometry.rs` 通过;scoped `git diff --check` 仅 LF/CRLF 提示;尾随空白扫描通过;验证图 `docs/tests/runtime/text/runtime_text_public_cursor_geometry_surface_preview_20260704.png` SHA256 `120E15B98B1A8705A9E6B6F193E9C26D8EAFB11C72DFE2946750EC0FDBC0FD1F`;验证日志 `docs/tests/runtime/text/runtime_text_public_cursor_geometry_surface_validation_20260704.log` SHA256 `EB81F8C62A1BAEF683E54F84C85D5DA33F7DB772D0FFBE0D5AE516EF851CDA21`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred | public cursor/range geometry surface 首段关闭;cache direct consumer、backend cluster reverse lookup、RTL isolate affinity、复杂 BiDi/竖排 source-range geometry、true unkerned measure 与 live editor typography QA 仍 pending |
| 2026-07-04 | LB-M1 / IM-M1 source-range caret geometry consumer | runtime_text_ime_source_range_geometry_rustfmt_static_visual_cargo_deferred | `ui/text/geometry.rs` 增加 `caret_frame_for_text_layout_with_source_metrics(...)` 与 `text_range_frames_for_text_layout_with_source_metrics(...)`,在简单 LTR、非 tab、非 justify、非 ellipsis、source-isomorphic run 下用 shaped source byte range 宽度计算 caret/selection/composition rect;`editable_text/ime_context.rs` 保留 `UiResolvedStyle` 并把 `state.text`/style 传入该几何入口;tab/justify/rich/non-isomorphic 映射仍回退 resolved `glyph_advances` | `rustfmt --edition 2021 --check` 覆盖 `ui/text/geometry.rs`、`ui/text/mod.rs`、`ui/surface/input/editable_text/ime_context.rs` 通过;scoped `git diff --check` 仅 LF/CRLF 提示;尾随空白扫描通过;验证图 `docs/tests/runtime/text/runtime_text_ime_source_range_geometry_preview_20260704.png` SHA256 `138A6385C2A0B4A6AC20DCEB649ACEF4F855D09BDED310ACB060C36F0C04A32A`;验证日志 `docs/tests/runtime/text/runtime_text_ime_source_range_geometry_validation_20260704.log` SHA256 `DDF9395D4B8F670E486E4297B0DC287DD9B05102FC22BCBAE54F7AC290226569`;repo `target`、`E:\cargo-targets`、`D:\cargo-targets` 同名扫描为 0;focused Cargo 因既有 cargo/rustc lanes 活跃而 deferred | 关闭 IME/caret 简单 LTR source-range consumer 首段;`measure_cache` direct consumer、public cursor rect API、backend cluster reverse lookup、RTL isolate affinity、复杂 BiDi/竖排 source-range geometry 与 true unkerned measure 仍 pending |
| 2026-07-03 | LB-M1 public source-range measure surface | runtime_text_lb_m1_public_source_range_measure_rustfmt_visual_cargo_deferred | `graphics/text/layout/measure.rs` 新增 `measure_text_source_range_width(...)` owner wrapper,经 `ui/text/layout_engine.rs`、`ui/text/shaper.rs`、`ui/text/mod.rs` 接到 `ui/surface/mod.rs::measure_text_source_range_width(text, style, UiTextRange)`。该 public surface API 明确只测量 shaping 后、kerning included 的 source byte 区间宽度；`ui/surface/text_shape.rs` 新增回归锁定 full width、prefix+range、`W+i` 与 out-of-range/reversed range 为 0.0。没有把 `include_kerning=false` 或 unkerned/GPOS delta 假装完成,也没有新增 root facade/compat shim。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/measure.rs zircon_runtime/src/graphics/text/layout/mod.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/shaper.rs zircon_runtime/src/ui/text/mod.rs zircon_runtime/src/ui/surface/mod.rs zircon_runtime/src/ui/surface/text_shape.rs` 通过；验证图 `docs/tests/runtime/text/runtime_text_public_source_range_measure_preview_20260703.png`,SHA256 `E7930642A9DD4CF02936ED97BF50BB5963E0EF00C91A6C0BA7C70D18DD6DAA08`；验证日志 `docs/tests/runtime/text/runtime_text_public_source_range_measure_validation_20260703.log`,SHA256 `071B768AD9FDDEF1E9CB62228A9D82519B8F8705654F6D78B6D3162C06EF74E8`；repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描 0。外部 cargo/rustc lanes 活跃,本切片不启动 focused Cargo,不声明 Cargo green。 | public source-range measure 首段关闭；`include_kerning=false` 真 unkerned/GPOS delta、cache/IME/cursor rect 直接消费、selection cluster 反查、BIDI/竖排 subrange measure、focused Cargo 绿跑与真实 editor window typography QA 仍 pending。 |
| 2026-07-03 | LB-M1/editor retained-host shaped-origin jitter guard | runtime_text_editor_shaped_origin_jitter_guard_rustfmt_visual_cargo_deferred | 针对用户最新 editor tab crop 中“等线已生效但字符左右间距/落点仍怪”的问题,`paint_text/draw/layout.rs` 在接受 runtime shaped positions 前新增 shaped-origin local-advance 守卫:即使 glyph id、visual range 与总宽匹配,也必须让 `shaped.x + shaped.offset_x` 推导出的相邻 origin delta 落在当前 host face 的自然 glyph advance 容差内。局部 0.25px 借位会回退到 host natural spacing,但 0.05px 这类合法微小 shaped offset 仍被保留。该切片不改字体族、ZUI 资产、root painter 或 runtime atlas owner。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout/tests.rs` 通过；`git diff --check --` scoped 通过(仅 LF-to-CRLF working-tree warning)。验证图 `docs/tests/runtime/text/runtime_text_editor_shaped_origin_jitter_guard_preview_20260703.png`,SHA256 `1F7A41698236F15F9BEA6AA6D6B18707A398C9F50221BBFFC2FA59C532D835D8`；验证日志 `docs/tests/runtime/text/runtime_text_editor_shaped_origin_jitter_guard_validation_20260703.log`,SHA256 `D0B2BCFCFDDB8555844966E3B6BBE41E3C4E9B04E2DBEF4A4B07620BF372A502`；repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描 0。外部 cargo/rustc lanes 活跃,本切片不启动 focused Cargo,不声明 Cargo green。 | shaped-position 低层局部字距抖动首段关闭；真实 editor window typography QA、focused Cargo 绿跑、GPU/native text parity、LCD/gamma/background policy、alpha bitmap atlas/glyphon `TextAtlas` cutover 仍 pending。 |
| 2026-07-03 | LB-M2 line_break root tests owner split | runtime_text_line_break_tests_owner_split_rustfmt_visual_cargo_deferred | `zircon_runtime/src/graphics/text/layout/line_break/mod.rs` 只保留 UAX#14 soft-break chunk、soft-hyphen/kinsoku/WordSmart handoff、`LineBreakChunk` API 与 `#[cfg(test)] mod tests;`,当前为 107 行且 0 个 inline tests；新增 `zircon_runtime/src/graphics/text/layout/line_break/tests.rs` 承接 14 个 module-local owner regressions。该切片只移动测试 owner,不改 break chunk 生成、Unicode glue、WordSmart、kinsoku handoff、UI layout consumption、SDF/native 渲染或 editor retained-host 路径。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/line_break/tests.rs` 通过；static scan: `line_break/mod.rs=107 lines/0 tests/1 test hook`,`line_break/tests.rs=192 lines/14 tests`。验证图 `docs/tests/runtime/text/runtime_text_line_break_tests_owner_split_preview_20260703.png`,SHA256 `2842E994673D06D293DA9B4623FBF06C1BC42EEF6671C425737591902445A436`；日志 `docs/tests/runtime/text/runtime_text_line_break_tests_owner_split_validation_20260703.log`,SHA256 `E64A98BCDAF96BDEA89927748FC98F02A775A21693B98C20D88F1CA6C86294FE`；repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描为 0。Cargo 因外部 cargo/rustc lane 活跃延后。 | 本切片关闭 shared line_break root production/test mixed owner 结构风险；完整 JIS/UAX kinsoku 表审计、squeeze/overhang generalization、native/SDF paragraph parity、真实窗口 typography QA 与 focused Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M2 kinsoku tests owner split | runtime_text_lb_m2_kinsoku_tests_owner_split_rustfmt_visual_cargo_deferred | `zircon_runtime/src/graphics/text/layout/kinsoku.rs` 只保留 CJK 禁则表、JLREQ inseparable pair、chunk 合并与 glyph fallback policy,从 546 行收缩为 240 行且 0 个内联测试；新增 folder-backed `zircon_runtime/src/graphics/text/layout/kinsoku/tests.rs` 承接 18 个 module-local owner regressions。该切片只移动测试 owner,不改禁则表、line-break merge、UI layout consumption、SDF/native 渲染或 editor retained-host 路径。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/graphics/text/layout/kinsoku/tests.rs` 通过；static scan: `kinsoku.rs=240 lines/0 tests`,`kinsoku/tests.rs=305 lines/18 tests`。验证图 `docs/tests/runtime/text/runtime_text_kinsoku_tests_owner_split_preview_20260703.png`,SHA256 `CADCB65D9FD7B9E68CBBDCA5F8B93444E596D6F593495CD8A867DCD8E9257CC0`；日志 `docs/tests/runtime/text/runtime_text_kinsoku_tests_owner_split_validation_20260703.log`,SHA256 `AA79185CB505C65D277139AECEE457FB1300D6CB35FFC28E77C2F5EDDC87BF70`；repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描为 0。Cargo 因外部 cargo/rustc lane 活跃延后。 | 本切片关闭 shared kinsoku production/test mixed owner 结构风险；完整 JIS/UAX kinsoku 表审计、squeeze/overhang generalization、native/SDF paragraph parity、真实窗口 typography QA 与 focused Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M1/LB-M2/LB-M3/LB-M4 layout_engine private tests owner split | runtime_text_layout_engine_tests_owner_split_rustfmt_visual_cargo_deferred | `zircon_runtime/src/ui/text/layout_engine/tests.rs` 不再承接 wrapping、overflow、Bidi、justify、sizing、tab、vertical 等混合私有回归；父文件收缩为 33 行,只挂载 child owners 与共享 `test_style(...)`/`ellipsis_width_for_test(...)` helper。新增/补齐 `layout_engine/tests/{alignment,bidi,grapheme,justify,measure,overflow,sizing,soft_hyphen,tab,vertical,wrapping}.rs`,并继续使用既有 `glue.rs`、`kinsoku.rs`、`word_smart.rs`、`wrap_space.rs`；总计 15 个 child owners/65 个 tests。该切片只移动测试 owner,不改 `layout_text(...)`、度量、换行、溢出、对齐或渲染路径。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/*.rs` 通过；static scan: parent `tests.rs=33 lines/0 tests`,child owners 65 tests,最大 child `kinsoku.rs=314 lines/18 tests`；trailing-whitespace 与 `allow(dead_code)`/panic token scan 无输出。验证图 `docs/tests/runtime/text/runtime_text_layout_engine_tests_owner_split_preview_20260703.png`,SHA256 `30B7801223DECD6797C6262138ABAEBDB5A9576EA97C866577B498992B1EE223`；日志 `docs/tests/runtime/text/runtime_text_layout_engine_tests_owner_split_validation_20260703.log`,SHA256 `F29A7E76D57A473EE1F9563A2D878FE2583709675CDA2F15912D57BB697C8D3D`；repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描为 0。Cargo 因外部 cargo/rustc lane 活跃延后。 | 本切片关闭 layout_engine private tests mixed owner 结构风险；完整 native/SDF paragraph parity、真实窗口 typography QA、focused Cargo 绿跑与里程碑测试阶段仍 pending。 |
| 2026-07-03 | LB-M2 WordSmart tests owner split | runtime_text_word_smart_tests_owner_split_rustfmt_visual_cargo_deferred | `zircon_runtime/src/graphics/text/layout/line_break/smart.rs` 只保留 WordSmart trailing-punctuation policy owner,从 506 行降到 176 行；新增 folder-backed `zircon_runtime/src/graphics/text/layout/line_break/smart/tests.rs` 承接 28 个 module-local regressions,共 329 行。该切片只移动测试 owner,不改 `apply_word_smart_rules(...)`、标点/delimiter 表、chunk splitting 或 UI layout consumption。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/smart.rs zircon_runtime/src/graphics/text/layout/line_break/smart/tests.rs` 通过；static scan: `smart.rs=176 lines/0 tests`,`smart/tests.rs=329 lines/28 tests`；trailing-whitespace 与 `allow(dead_code)`/panic token scan 无输出。验证图 `docs/tests/runtime/text/runtime_text_word_smart_tests_owner_split_preview_20260703.png`,SHA256 `F722DC88AEC7944CA22B7177A15F4520D98B760A7737B27EF50966CB17DE8AAA`；日志 `docs/tests/runtime/text/runtime_text_word_smart_tests_owner_split_validation_20260703.log`,SHA256 `4AE4A5E7DC565DC7E193C2FAADE2C09F2FCBF791560DF95BC2A5196A11C9EA79`；repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描为 0。Cargo 因外部 cargo/rustc lane 活跃延后。 | 本切片关闭 WordSmart policy/test mixed owner 结构风险；完整 Unicode/Godot-style WordSmart word classes、paired/full quote inference、CJK/多脚本 smart-wrap、squeeze/overhang 泛化、vertical layout、native/SDF paragraph parity、真实窗口 typography QA 与 focused Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M1/LB-M3/LB-M4 UI text layout tests owner split | runtime_text_ui_text_layout_tests_owner_split_rustfmt_visual_cargo_deferred | `zircon_runtime/src/ui/tests/text_layout.rs` 不再作为 788 行混合 render-extract regression owner。新增 folder-backed `text_layout/mod.rs` 承接 shared fixture/helper wiring,并拆出 `alignment.rs`、`wrapping.rs`、`overflow.rs`、`direction.rs`;既有 `edit_state.rs` 保持 child owner。该切片只移动 UI text layout tests,不改生产 layout engine、surface extract 或 editor retained-host。 | `rustfmt --edition 2021 --check` 覆盖 6 个 text_layout child owner 通过；static scan: `mod.rs=47 lines/0 tests/3 helpers`,`alignment.rs=89/2`,`wrapping.rs=130/3`,`overflow.rs=264/6`,`direction.rs=266/5`,`edit_state.rs=250/6`。验证图 `docs/tests/runtime/text/runtime_text_ui_text_layout_tests_owner_split_preview_20260703.png`,SHA256 `6F14FA7CC616EC33C333FEC63C99152D05BB426BDD93454450F3DD4206F5D771`；日志 `docs/tests/runtime/text/runtime_text_ui_text_layout_tests_owner_split_validation_20260703.log`,SHA256 `BD19E62B5CA6766570B38D823DEDE0B6D6EED0878AB3FBB3B082115D1B6D09EC`；target scans 0。Cargo 因外部 cargo/rustc lane 活跃延后。 | 该切片只关闭 UI text layout render-extract tests owner 结构风险；完整 native/SDF paragraph parity、真实窗口 typography QA、focused Cargo 绿跑与里程碑测试阶段仍 pending。 |
| 2026-07-03 | LB-M1/SM-M5 editor SDF glyph bitmap-origin snap | runtime_text_editor_sdf_glyph_origin_snap_rustfmt_visual_cargo_deferred | 针对用户 editor crop 中 SDF 小字号 label 字符左右落点不舒服的问题,`text_pixel_snap.rs` 新增 `text_glyph_device_frame(...)`,由同一 owner 负责 SDF glyph bitmap frame 原点贴设备像素；`sdf_render.rs` 的 horizontal/vertical glyph frame 在不改 layout advances 的前提下吸附 bitmap x/y,避免只吸附文本 frame origin 仍留下逐 glyph 小数落点。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/scene/scene_renderer/ui/text_pixel_snap.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render/tests.rs` 通过；新增/更新 `text_glyph_device_frame_snaps_bitmap_origin_only`、`sdf_draw_plan_snaps_text_and_glyph_origin_before_horizontal_quad_placement`、`sdf_glyph_frames_snap_fractional_bitmap_origins_without_changing_size`。验证图 `docs/tests/runtime/text/runtime_text_editor_sdf_glyph_origin_snap_preview_20260703.png`,SHA256 `9576EFE406DE518BBFD889DA9D63DF28DE8C1C32BC8657E392D62D415B9A541E`；日志 `docs/tests/runtime/text/runtime_text_editor_sdf_glyph_origin_snap_validation_20260703.log`,SHA256 `6C33A5D3DF05663E5753824734E2D5F559C5806E971055AC026F8A22B76EC21A`；target 同名扫描 0。外部 cargo/rustc lane 活跃,未启动 focused Cargo,不声明 Cargo green。 | SDF glyph bitmap-origin jitter 首段关闭；完整 kerning/GPOS subrange measurement、GPU/native-SDF paragraph parity、真实窗口 typography QA 与空闲 Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M1/editor retained-host subpixel spacing fix | runtime_text_editor_retained_subpixel_spacing_fix_rustfmt_visual_cargo_deferred | 针对用户最新 editor tab crop 中“等线已生效但字符左右间距仍怪、glyph 落点偏左/偏右”的问题,`paint_text/draw/layout.rs` 将 runtime advance projection 改为 fail-closed:host run width 或 host per-grapheme natural advances 不可得时不再投影,并把 per-grapheme 接受窗收紧为 `clamp(host*2%, 0.08px, 0.18px)`；`paint_text/draw/glyphs.rs` 将 retained 1/3 subpixel bin 从 floor 改为 nearest bin。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/atlas/render_gpu_plan.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/glyphs.rs` 通过；`git diff --check --` 覆盖 editor touched files 通过。新增/更新回归 `runtime_positioned_glyphs_rejects_quarter_pixel_tab_label_jitter`、`retained_glyph_placement_uses_nearest_subpixel_bin_without_left_bias`。验证图 `docs/tests/runtime/text/runtime_text_editor_retained_subpixel_spacing_fix_preview_20260703.png`,SHA256 `ED53B97AE58A6930F41054B323BA3D475E8A80D44BCC7AB223E121485DA50F4B`；日志 `docs/tests/runtime/text/runtime_text_editor_retained_subpixel_spacing_fix_validation_20260703.log`,SHA256 `CFF4653F26BC37A0C0C7350903A5AACA3359D4E2FD8F9FB7EB2D68A0419DEF95`；target 同名扫描 0。外部 cargo/rustc lane 活跃,未启动 focused Cargo,不声明 Cargo green。 | retained-host 小字号局部 advance/placement jitter 首段关闭；完整 kerning/GPOS subrange measurement、GPU/native-SDF paragraph parity、真实窗口 typography QA 与空闲 Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M1/editor retained-host subpixel spacing threshold | runtime_text_editor_subpixel_spacing_threshold_rustfmt_visual_cargo_deferred | `paint_text/draw/layout.rs` 将 retained-host runtime advance projection 的 per-grapheme 接受窗收紧为 `clamp(host*6%, 0.35px, 0.60px)`。这让 `editor base.zui` 这类小字号 tab label 在总 run width 接近、但相邻 grapheme 发生 +0.75px/-0.75px 局部借位时回退 host face natural spacing,不再把局部 jitter 投影到绘制位置。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs` 通过；新增 `runtime_positioned_glyphs_rejects_subpixel_tab_label_jitter`。外部 cargo/rustc lane 活跃,未启动 focused Cargo,不声明 Cargo green。验证图 `docs/tests/runtime/text/runtime_text_editor_subpixel_spacing_threshold_preview_20260703.png`,SHA256 `BBBED73034CD2C41D8FC18E38EBE6630412E691C03E7728137B6060F9AF54693`；日志 `docs/tests/runtime/text/runtime_text_editor_subpixel_spacing_threshold_validation_20260703.log`,SHA256 `2F0CBC3675CBF4452963A9828DA22130143CF955D16AF5B6974B95D41900AD36`；target 同名扫描 0。 | retained-host 小字号局部 advance threshold 首段关闭；完整 kerning/GPOS subrange measurement、GPU/native-SDF paragraph parity、真实窗口 typography QA 与空闲 Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M1/SM-M5 SDF format-control zero advance | runtime_text_sdf_format_control_filter_rustfmt_visual_cargo_deferred | 当前 SDF path 仍按 Unicode scalar run 过渡,如果 ZWJ、variation selector 或 Bidi format controls 被当作普通 glyph measurement,会在没有 resolved layout advance 时产生 tofu/fallback 宽度并破坏后续 glyph 的 x/y 位置。本切片新增 `scene_renderer/ui/sdf_char_run.rs` 作为槽位判断 owner,让 invisible format controls 保留 char-run index 但不进 atlas slot,并让 SDF fallback measurement 返回 0 advance。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_char_run.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/mod.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_atlas.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_atlas/tests.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render/tests.rs` 通过；新增/更新 `filters_invisible_format_controls_from_sdf_slots`、`sdf_atlas_plan_keeps_format_controls_out_of_slots`、`sdf_draw_plan_zeroes_format_control_advances_without_slots`。外部 cargo/rustc lane 活跃,未启动新的 focused Cargo,不声明 Cargo green。验证图 `docs/tests/runtime/text/runtime_text_sdf_format_control_filter_preview_20260703.png`,SHA256 `44E8F6F622C2C1648EA8AF71D2FD8F51359A2F09EB44CC99DB44FA517E0D924E`;日志 `docs/tests/runtime/text/runtime_text_sdf_format_control_filter_validation_20260703.log`,SHA256 `6DBED416FFA104E1EA21F3F72D5EA86D29E79297E782F9294C530BD791056748`;repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描 0。 | SDF scalar-run format-control advance 风险首段关闭；完整 kerning/GPOS subrange measurement、GPU/native-SDF paragraph parity、真实窗口 typography QA 与空闲 Cargo 绿跑仍 pending。 |
| 2026-07-03 | LB-M1/SM-M5 SDF grapheme advance mapping | runtime_text_sdf_grapheme_advance_mapping_rustfmt_visual_cargo_deferred | `UiResolvedTextLine.glyph_advances` 的契约是每 grapheme cluster 一项,但当前 SDF atlas/run 仍按 Unicode scalar char 烘焙与绘制。本切片新增 `scene_renderer/ui/sdf_advances.rs` 作为唯一投影 owner,把 resolved grapheme advances 映射到 SDF char-run advances:簇内前置 scalar advance 为 0,簇尾 advance 等于整簇宽度,避免组合字符/emoji cluster 在 SDF 顶点规划或 mixed native overlay 上被拆散。`text/sdf_fallback.rs` 同步按 grapheme 边界扩展 failed char span,局部 overlay 不再切半个 cluster。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_advances.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/mod.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render/tests.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/text/sdf_fallback.rs` 通过；新增/更新 `maps_grapheme_advances_to_sdf_character_advances`、`sdf_draw_plan_maps_resolved_grapheme_advances_to_sdf_char_run`、`sdf_atlas_fallback_overlays_whole_grapheme_from_resolved_advances`。当前外部 cargo/rustc lane 活跃,未启动新的 focused Cargo,不声明 Cargo green。验证图 `docs/tests/runtime/text/runtime_text_sdf_grapheme_advance_mapping_preview_20260703.png`,SHA256 `095FE45AFE9BCBAA673902C42938D838B73848D92D52986C320BFB9DC1BEE315`;日志 `docs/tests/runtime/text/runtime_text_sdf_grapheme_advance_mapping_validation_20260703.log`,SHA256 `12C4B8F36CBDA5B4BC4D550D6D30D8AD813BBBF9EFBC10BDFA3D3465C204CC76`;repo `target`、`E:\cargo-targets` 与 `D:\cargo-targets` 同名扫描 0。 | 组合字符/emoji cluster 的 SDF char-run advance 数据面首段关闭；完整 SM-M5 仍 pending:RTL/竖排 paragraph parity、glyphon full paragraph buffer 对拍、真实 SDF/Native pixel 容差抓帧、复杂 shaping cluster 后端数据与空闲 Cargo 绿跑。 |
| 2026-07-03 | LB-M1/editor retained-host per-grapheme spacing guard | runtime_text_editor_per_grapheme_spacing_guard_rustfmt_visual_cargo_timeout | 编辑器截图显示字体族已回到 DengXian/等线,但局部字符 advance 仍左右漂移。本切片在 `paint_text/draw/layout.rs` 中把 runtime grapheme advances 投影到 host glyph 前,同时检查总 run width 与逐 grapheme host natural advances；只要总宽看似接近但局部 advance 抖动,就回退到 host face 自然 spacing,避免 `editor base.zui` 一类 tab label 被局部拉伸。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs` 通过；focused `cargo test -p zircon_editor runtime_positioned_glyphs --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-text-spacing-0703 --message-format short --color never -- --nocapture --test-threads=1` 904s 超时且无 Rust diagnostics,不计通过；日志 `docs/tests/runtime/text/runtime_text_editor_per_grapheme_spacing_guard_validation_20260703.log`,SHA256 `48AFB99C98A9037F1B51E156257A7A43AD25F09D9E065EAA5E5A3D7FA10AB5C8`；验证图 `docs/tests/runtime/text/runtime_text_editor_per_grapheme_spacing_guard_preview_20260703.png`,SHA256 `7991429B66A536B05D538C05947EBAD6FB7D6049EF894F1D6EE4F8E32406A5F8`,target 同名扫描 0。 | retained-host 单行 layout 的局部 advance jitter 防线已补；完整 kerning/GPOS subrange measurement、GPU/native-SDF paragraph parity、真实窗口 typography QA 仍 pending。 |
| 2026-07-02 | LB-M4/SM-M1 vertical_rl SDF vertex projection first slice | runtime_text_lb_m4_sm_m1_vertical_rl_sdf_projection_check_passed | `ScreenSpaceUiTextBatch` 新增 writing mode,render extract 从 paint/layout/style 三条来源传播;`sdf_render.rs` 在 `VerticalRl` 下改为列中心 x + y 轴 advance 投影 SDF glyph quad,横排路径不变。 | scoped rustfmt 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0702-sdf-vertical --message-format short --color never` 通过(仅既有 warnings)；focused `sdf_draw_plan_vertical_rl_advances_glyphs_on_y_axis` 通过 1/1；验证图 `docs/tests/runtime/text/runtime_text_vertical_rl_sdf_vertex_projection_preview_20260702.png` 已检查,SHA256 `86C7CEA59DBAB17A63B6FB61C2FF72C591B4E7216439FE0AABF97C793E5BC1C1`,target 同名扫描为 0。 | SDF 竖排上屏投影首段关闭；完整 vertical shaping/font orientation、native/SDF paragraph parity、backend cluster reverse lookup、screenPxRange/MSDF 与窗口级视觉 QA 仍 pending。 |
| 2026-07-01 | LB-M4 vertical_rl editable decoration render-contract slice | runtime_text_lb_m4_vertical_rl_edit_decoration_contract_passed | `zircon_runtime_interface/src/ui/surface/render/text_geometry.rs` 在生成 `UiTextPaintDecoration` 时消费 `layout.writing_mode`:竖排 selection 使用 column width 与 y-span,composition underline 改为列右侧 side rule,caret 改为横向 bar;新增 `ui_text_decorations_use_vertical_rl_geometry` 锁定 neutral paint DTO,避免平台绘制前仍沿用横排装饰。 | `rustfmt --edition 2021 --check zircon_runtime_interface/src/ui/surface/render/text_geometry.rs zircon_runtime_interface/src/tests/render_contracts.rs` 通过；`cargo test -p zircon_runtime_interface ui_text_decorations_use_vertical_rl_geometry --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-vertical-decorations-interface --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1。验证图 `docs/tests/runtime/text/runtime_text_vertical_rl_edit_decoration_geometry_preview_20260701.png` 已检查可读,SHA256 `A856E741CC5722DE9BD06A924E887657F2DBDB9301073F4B0172C020CE50A044`,repo `target`、`D:\cargo-targets`、`E:\cargo-targets` 同名扫描为 0。 | 竖排 render-facing 编辑装饰几何首段关闭；完整 backend cluster reverse lookup、vertical shaping/font orientation parity、native/SDF paragraph parity、平台实机 IME 候选窗锚定与更广 editor 字体一致性 QA 仍 pending。 |
| 2026-07-01 | LB-M1/editor retained-host utility-tab mono consumer | runtime_text_editor_retained_utility_tab_mono_consumer_recorded | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:Asset Browser/Assets Activity utility tab labels 现在显式走 `UiTextRunPaintStyle::code`,measurement/projection 与 raster font family 都使用 code/mono lane;普通 UI/strong button 仍走 DengXian/等线比例字体。 | `cargo fmt -p zircon_editor --check` 通过；focused `asset_browser_utility_tab_label_uses_mono_text_style` 1/1；direct `button_label` 4/4；direct `asset_browser_utility_tabs_use_compact_slate_tab_strip_geometry` 1/1；direct `dock_tab_button_measures_label_with_declared_font_size` 1/1；ignored M3 screenshot harness 1/1；ignored component atlas screenshot harness 1/1；editor-host build passed in `D:\cargo-targets\zircon-editor-text-mono-tabs-0701`。截图只记录刷新证据和人工检查输入,不直接判定文本渲染有效:mono crop `2026-07-01 23:24:25 +08:00`,SHA256 `E49FAEF998E7A10F5C4FB673713C2DBA21D816AD074D71C924525ECB3E4D9D9F`;3x crop `2026-07-01 23:24:25 +08:00`,SHA256 `EDCBFEE05E31F092BD0721BBF7DF67DAF7ADDBEB47AA23216C5CE3126CE4EF8B`;runtime-text evidence copies use the same hashes;target scan `NO_MATCHING_SCREENSHOTS_IN_TARGETS`。 | Utility-tab mono consumer 首段关闭；完整 LB-M1 runtime subrange/kerning semantics、FontDatabase/FontFace DTO、GPU draw-list family/style、DPI glyph reraster/subpixel bins、native/SDF paragraph parity、baseline 与 vertical layout 仍 pending。 |
| 2026-07-01 | LB-M4 vertical_rl hit-test/caret/IME geometry first slice | runtime_text_lb_m4_vertical_rl_geometry_direct_binary_passed | `ui/text/hit_test.rs` 现在在 `VerticalRl` 下按 x 选列、按 y/glyph advances 反查 grapheme midpoint/source offset;`ui/text/geometry.rs` 的 caret/range frames 在竖排下输出横向 bar;TextInput IME context 测试夹具读取实际 resolved layout 并断言 `SetCursorArea` 使用竖排 caret frame。 | `rustfmt --edition 2021 zircon_runtime\src\ui\text\hit_test.rs zircon_runtime\src\ui\text\geometry.rs zircon_runtime\src\ui\tests\text_hit_testing.rs zircon_runtime\src\ui\tests\widget_text_input_ime_context.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-vertical-geometry-check --message-format short --color never` 通过(仅既有 warnings)；focused `cargo test ... --no-run` 在 `E:\cargo-targets\zircon-runtime-text-0701-vertical-geometry-check` 904s 工具窗口超时后继续链接且已停止,不计通过；复用 no-default-features lib-test binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe` 直接运行 `text_hit_test_vertical_rl`、`source_geometry_uses_vertical`、`cursor_rect_uses_vertical_rl` 各通过 1/1。验证图 `docs/tests/runtime/text/runtime_text_vertical_rl_hit_test_geometry_preview_20260701.png` 已检查可读,SHA256 `6B137ECAA345CF7EEA898032AD93B5E2292B16563AC40B43C136684740BA1E6B`,repo `target`、`D:\cargo-targets`、`E:\cargo-targets` 同名扫描为 0。 | 竖排 hit-test/caret/IME 几何首段关闭；完整 backend cluster reverse lookup、vertical shaping/font orientation parity、native/SDF paragraph parity 与平台实机 IME 候选窗锚定仍 pending。 |
| 2026-07-01 | LB-M4 vertical_rl writing-mode/column layout first slice | runtime_text_lb_m4_vertical_rl_layout_check_passed | `UiTextWritingMode::{HorizontalTb,VerticalRl}` 进入 interface,`UiResolvedStyle`/`UiResolvedTextLayout`/`UiShapedText`/`UiTextPaint` 携带 writing mode;surface parser 接受 `writing_mode = "vertical-rl"`;`ui/text/layout_engine/vertical.rs` 拥有首个 vertical-rl 列布局,按 frame height 断列、列从右到左放置、复用 CJK kinsoku,shaped DTO 竖排 glyph frame 沿 y 轴推进并给 ASCII 标记 `Cw90`。 | `cargo test -p zircon_runtime_interface vertical --locked --no-default-features --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2；Cargo wrapper 的 runtime focused `vertical_rl` 在 Windows 编译/链接阶段超时但产出 test binary,直接运行 `E:\cargo-targets\zircon-runtime-text-0701-vertical-writing-mode\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe vertical_rl --nocapture --test-threads=1` 通过 2/2,`text_vertical_kinsoku --nocapture --test-threads=1` 通过 1/1；scoped `rustfmt --check` 通过；验证图 `docs/tests/runtime/text/runtime_text_vertical_rl_layout_preview_20260701.png` 已检查可读,SHA256 `7E2797C4D3D71DA40233B944521468167E995CEAD722E1CB3DA75140E2F1031F`,repo `target`、`D:\cargo-targets`、`E:\cargo-targets` 同名扫描为 0。 | 首个 `vertical_rl` contract/layout/shaped DTO 切片关闭；完整 vertical shaping/font orientation parity、native/SDF paragraph parity、vertical hit-test/caret/IME geometry、完整 kinsoku 表审计、squeeze/overhang 策略仍 pending。 |
| 2026-07-01 | LB-M1/editor retained-host DengXian UI priority evidence refresh 22:09 | runtime_text_editor_retained_dengxian_priority_evidence_refreshed_2209 | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:当前代码恢复 ordinary UI `Deng.ttf/Dengl.ttf` 优先、strong UI `Dengb.ttf/Deng.ttf` 优先,Segoe 只作 fallback,code style 仍优先 Cascadia/Consolas 等宽。`paint_text_tests.rs` 锁定 Windows 有 Deng 字体时 retained runtime family 为 `DengXian`;`template_buttons/content/metrics.rs` 保持 button/tab label line-height 的整数像素槽。 | `cargo fmt -p zircon_editor --check` 通过；direct `retained_ui_runtime_family_prefers_dengxian_when_available_on_windows` 1/1；direct `paint_text` 20/20；direct `button_label` 4/4；direct `dock_tab_button_measures_label_with_declared_font_size` 1/1；direct `button_label_text_slot_snaps_utility_tab_line_height_to_pixels` 1/1；ignored component atlas screenshot harness 1/1；ignored M3 screenshot harness 1/1；editor-host build passed in `D:\cargo-targets\zircon-editor-text-dengxian-0701`。截图只记录刷新证据和人工检查输入,不直接判定文本渲染有效:Asset Browser `2026-07-01 22:08:15 +08:00`,SHA256 `DFA19780B09D6C2F323EC4B80407BF992B0202EAC033849892E942D1EAC84FDE`;utility tabs crop `2026-07-01 22:09:39 +08:00`,SHA256 `71EFF8ADD1DA1DB908389A9776B3C48DB04DC98971271C4347FA90E613435B26`;3x crop SHA256 `D7C7D86ABCB0D7B65425AC31137D4F5163F8DD9A1FE23AC039D2465CB82CDC82`;repo `target`、`zircon_editor\target` 与外部 target 同名截图扫描为 0。 | Editor retained-host UI family priority 已恢复为 DengXian/等线比例字体。完整 runtime FontDatabase/FontFace DTO、GPU draw-list family/style propagation、shaped glyph paint DTO、subrange kerning semantics、DPI glyph reraster/subpixel bins、native/SDF paragraph parity、baseline 与 vertical layout 仍 pending。 |
| 2026-07-01 | LB-M2 vertical ideographic iteration kinsoku slice | runtime_text_lb_m2_vertical_iteration_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将 `〻` 加入 `FORBIDDEN_LINE_START_JAPANESE_NON_STARTERS`,并新增 shared owner test `merges_vertical_ideographic_iteration_mark_with_previous_chunk`；`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_vertical_iteration_mark`,锁定窄 frame 下 `時〻` 作为 protected overhang 保持同一行。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\text\layout\kinsoku.rs zircon_runtime\src\ui\text\layout_engine\tests\kinsoku.rs` 通过；Cargo wrapper 产出测试二进制但未形成可采信 `test result` 结束行,不计通过；direct lib-test binary `vertical_iteration` 通过 1/1,`vertical_ideographic_iteration` 通过 1/1。验证图 `docs/tests/runtime/text/runtime_text_vertical_iteration_kinsoku_preview_20260701.png` 已检查可读,SHA256 `1E5FA7EC10E5302DAB82FB50EF1F68A4486AF0397E88CFA6D2A7DB505C95A1F0`;repo `target`、`D:\cargo-targets`、`E:\cargo-targets` 同名扫描为 0。 | `〻` targeted 禁则已关闭；完整 JIS/UAX 表审计、squeeze/overhang generalization、broader glue、full WordSmart、vertical layout 与 native/SDF parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart Unicode standalone interrobang trailing punctuation slice | runtime_text_lb_m2_word_smart_unicode_interrobang_punctuation_check_passed | `graphics/text/layout/line_break/smart.rs` 在 shared smart owner 内把 Unicode standalone interrobang `‽` 纳入 trailing punctuation 集合:`go‽next` 会切成 protected `go‽` 与独立后续词,`go‽!next` 也保持 interrobang+ASCII cluster 不吸收后续词。`line_break/mod.rs` 新增 shared chunk 回归,`ui/text/layout_engine/tests/word_smart.rs` 新增 narrow-frame UI consumption 回归,UI 仍只消费 `word_smart_line_break_chunks(...)`。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\text\layout\line_break\smart.rs zircon_runtime\src\graphics\text\layout\line_break\mod.rs zircon_runtime\src\ui\text\layout_engine\tests\word_smart.rs` 通过,触达 Rust debt scan `TODO|FIXME|unwrap\(|panic!|allow\(dead_code\)|Result<.*String` 无输出。`cargo test -p zircon_runtime unicode_interrobang --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-arabic --message-format short --color never -- --nocapture --test-threads=1` 通过 4/4(仅既有 warnings,完整命令约 9m03s,测试执行 0.64s);direct lib-test binary `unicode_interrobang` 复跑通过 4/4(0.68s)。验证图 `docs/tests/runtime/text/runtime_text_word_smart_unicode_interrobang_punctuation_preview_20260701.png` 已检查可读,1120x720,SHA256 `FC93057F17430A50524F8B3D1DE684F9EFEF494E4F30623E589DFE3688FEB8D1`;repo `target` 与 `E:\cargo-targets` 同名扫描为 0。 | Unicode standalone interrobang trailing punctuation 首段已由 focused Cargo、direct binary 与截图证据关闭；完整 Unicode/Godot-style WordSmart word classes、paired/full quote inference、broader punctuation classes、CJK/多脚本 smart-wrap、squeeze/overhang、vertical layout 与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart Unicode double/interrobang trailing punctuation slice | runtime_text_lb_m2_word_smart_unicode_double_punctuation_check_passed | `graphics/text/layout/line_break/smart.rs` 在 shared smart owner 内把 Unicode double exclamation `‼`、double question `⁇`、question exclamation `⁈` 与 exclamation question `⁉` 纳入 trailing punctuation 集合:`go‼next`、`go⁇next`、`go⁈next`、`go⁉next` 会切成 protected `go‼`/`go⁇`/`go⁈`/`go⁉` 与独立后续词,`go⁉!next` 也保持 Unicode+ASCII cluster 不吸收后续词。`line_break/mod.rs` 新增 shared chunk 回归,`ui/text/layout_engine/tests/word_smart.rs` 新增 narrow-frame UI consumption 回归,UI 仍只消费 `word_smart_line_break_chunks(...)`。 | 初次 `cargo test -p zircon_runtime unicode_double_punctuation --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-arabic --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test 编译阶段 304s 超时且未形成 RED 证据,残留同 target-dir 编译进程已停止；实现后冷 target-dir `E:\cargo-targets\zircon-runtime-text-0701-word-smart-unicode-punct` 1204s 超时且未产出二进制,残留进程已停止；复用 warm target-dir 后同 `unicode_double_punctuation` focused test 通过 3/3(仅既有 warnings,完整命令约 9m05s,测试执行 0.61s)。scoped `rustfmt --edition 2021 --check` 通过,触达 Rust debt scan `TODO|FIXME|unwrap\(|panic!|allow\(dead_code\)|Result<.*String` 无输出。验证图 `docs/tests/runtime/text/runtime_text_word_smart_unicode_double_punctuation_preview_20260701.png` 已检查可读,1120x620,SHA256 `B42D4B564BD3277DCFEB423560D32734BEE4C5E937ACE013BCBB2D7A2355E722`;repo `target` 与 `E:\cargo-targets` 同名扫描为 0。 | Unicode double/interrobang trailing punctuation 首段已由 focused Cargo 与截图证据关闭；完整 Unicode/Godot-style WordSmart word classes、paired/full quote inference、broader punctuation classes、CJK/多脚本 smart-wrap、squeeze/overhang、vertical layout 与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart Arabic/RTL trailing punctuation slice | runtime_text_lb_m2_word_smart_arabic_punctuation_check_passed | `graphics/text/layout/line_break/smart.rs` 在 shared smart owner 内把 Arabic comma `،`、Arabic semicolon `؛` 与 Arabic question mark `؟` 纳入 trailing punctuation 集合:`go،next`、`go؛next`、`go؟next` 会切成 protected `go،`/`go؛`/`go؟` 与独立后续词,`go؟!next` 也保持 Arabic+ASCII cluster 不吸收后续词。`line_break/mod.rs` 新增 shared chunk 回归,`ui/text/layout_engine/tests/word_smart.rs` 新增 narrow-frame UI consumption 回归,UI 仍只消费 `word_smart_line_break_chunks(...)`。 | 初次 `cargo test -p zircon_runtime arabic_trailing --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-arabic --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test 编译阶段 904s 超时且未形成 RED 证据；实现落地后 scoped `rustfmt --edition 2021 --check` 通过,触达 Rust debt scan `TODO|FIXME|unwrap\(|panic!|allow\(dead_code\)|Result<.*String` 无输出,同 `arabic_trailing` focused test 通过 3/3(仅既有 warnings,完整命令 14m14s,测试执行 0.76s)。`arabic_ascii` Cargo filter 304s 超时且未形成通过证据,随后直接运行已产出的 lib-test binary 通过 1/1。验证图 `docs/tests/runtime/text/runtime_text_word_smart_arabic_punctuation_preview_20260701.png` 已检查可读,1120x620,SHA256 `540149B2C66D0110F5A705D99B3631803FD300341EDAF5233B30FCE3A06766E4`;repo `target` 与 `E:\cargo-targets` 同名扫描为 0。 | Arabic/RTL 常见 trailing punctuation 首段已由 focused Cargo、direct binary 与截图证据关闭；完整 Unicode/Godot-style WordSmart word classes、paired/full quote inference、broader punctuation classes、CJK/多脚本 smart-wrap、squeeze/overhang、vertical layout 与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart ellipsis/leader trailing punctuation slice | runtime_text_lb_m2_word_smart_ellipsis_punctuation_check_passed | `graphics/text/layout/line_break/smart.rs` 在 shared smart owner 内把 Unicode horizontal ellipsis `…` 与 two-dot leader `‥` 纳入 trailing punctuation 集合:`go…next`、`go‥next` 会切成 protected `go…`/`go‥` 与独立后续词。`line_break/mod.rs` 新增 shared chunk 回归,`ui/text/layout_engine/tests/word_smart.rs` 新增 narrow-frame UI consumption 回归,UI 仍只消费 `word_smart_line_break_chunks(...)`。验证前还修复了 `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/fixture.rs` 的旧 `MaterialAsset::lighting_model` 字段访问,改为写入当前 `property_values["lighting_model"]`,避免无关渲染测试夹具阻断 runtime lib-test 编译。 | 初次 `cargo test -p zircon_runtime ellipsis_trailing --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-fullwidth --message-format short --color never -- --nocapture --test-threads=1` 被旧材质字段编译错误阻断;支撑修复后同命令首次长编译超出工具窗口,实现落地后复跑通过 4/4(仅既有 warnings)。`rustfmt --edition 2021 --check` 覆盖 touched text files 与渲染 fixture 通过,触达 Rust debt scan `TODO|FIXME|unwrap\(|panic!|allow\(dead_code\)|Result<.*String` 无输出。`cargo test -p zircon_runtime word_smart --lib ...` 全组复跑未形成通过证据:复用旧 target-dir 命中 Cargo dep-info 写入路径缺失,新 target-dir 依赖编译早期异常退出且未进入测试。验证图 `docs/tests/runtime/text/runtime_text_word_smart_ellipsis_punctuation_preview_20260701.png` 已检查可读,1120x620,SHA256 `C7639D78EA81567970BE95BD52F2F6F7D1DBBC37EB149E81EF838CC33C617693`;repo `target` 与 `E:\cargo-targets` 同名扫描为 0。 | Unicode ellipsis/leader trailing punctuation 首段已由 focused Cargo 与截图证据关闭；完整 Unicode/Godot-style WordSmart word classes、paired/full quote inference、broader punctuation classes、CJK/多脚本 smart-wrap、squeeze/overhang、vertical layout 与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart punctuation-cluster glue slice | runtime_text_lb_m2_word_smart_punctuation_cluster_check_passed | `graphics/text/layout/line_break/smart.rs` 在 shared smart owner 内修复连续收尾标点 cluster 的 chunk 内切分:旧实现遇到 `go?!next`、`go！？next`、`go，」！next` 时只切一次内部标点,再把后续 leading punctuation suffix 连同下一词误并入 protected chunk。本轮改为每次 split 后继续重跑 leading/internal trailing-punctuation 规则,得到 protected `go?!` / `go！？` / `go，」！` 与独立后续词。`line_break/mod.rs` 新增 shared chunk 回归,`ui/text/layout_engine/tests/word_smart.rs` 新增 narrow-frame UI consumption 回归,UI 仍只调用 `word_smart_line_break_chunks(...)`。 | RED `cargo test -p zircon_runtime punctuation_cluster --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-fullwidth --message-format short --color never -- --nocapture --test-threads=1` 先失败 3 个新增 owner cases;修复后 `rustfmt --edition 2021 --check zircon_runtime\src\graphics\text\layout\line_break\smart.rs zircon_runtime\src\graphics\text\layout\line_break\mod.rs zircon_runtime\src\ui\text\layout_engine\tests\word_smart.rs` 通过,触达 Rust debt scan `TODO|FIXME|unwrap\(|expect\(|panic!|allow\(dead_code\)|Result<.*String` 无输出,同 `punctuation_cluster` focused test 通过 5/5,`cargo test -p zircon_runtime word_smart --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-fullwidth --message-format short --color never -- --nocapture --test-threads=1` 通过 34/34(均仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_word_smart_punctuation_cluster_preview_20260701.png` 已检查可读,1120x620,SHA256 `1FFA95EB87CD8A0A3156E67F85C86F126F37BCAE061481740BAFF7719BC48001`;repo `target` 与 `E:\cargo-targets` 同名扫描为 0。 | 连续收尾标点 cluster 已由 focused Cargo 与截图证据关闭；完整 Unicode/Godot-style WordSmart word classes、paired/full quote inference、broader punctuation classes、CJK/多脚本 smart-wrap、squeeze/overhang、vertical layout 与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M1/editor retained-host DengXian UI priority verification refresh | runtime_text_editor_retained_dengxian_priority_restore_verification_refreshed | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:当前代码恢复 ordinary UI `Deng.ttf/Dengl.ttf` 优先、strong UI `Dengb.ttf/Deng.ttf` 优先,Segoe 只作 fallback,code style 仍优先 Cascadia/Consolas 等宽。`paint_text_tests.rs` 锁定 Windows 有 Deng 字体时 retained runtime family 为 `DengXian`;`template_buttons/content/metrics.rs` 保持 button/tab label line-height 的整数像素槽。 | `cargo fmt -p zircon_editor --check` 通过；direct `retained_ui_runtime_family_prefers_dengxian_when_available_on_windows` 1/1；direct `paint_text` 20/20；direct `button_label` 4/4；direct `dock_tab_button_measures_label_with_declared_font_size` 1/1；direct `button_label_text_slot_snaps_utility_tab_line_height_to_pixels` 1/1；ignored component atlas screenshot harness 1/1；ignored M3 screenshot harness 1/1；editor-host build passed in `D:\cargo-targets\zircon-editor-text-dengxian-0701`。截图只记录刷新证据和人工检查输入,不直接判定文本渲染有效:Asset Browser `2026-07-01 21:55:51 +08:00`,SHA256 `DFA19780B09D6C2F323EC4B80407BF992B0202EAC033849892E942D1EAC84FDE`;utility tabs crop `2026-07-01 21:57:07 +08:00`,SHA256 `869168A7A04768D1D9596B0C39E228FA984461CE19A03FB4432F6B4F6415F052`;3x crop SHA256 `B9F323C0407EA9452385C0A94AECCD49A924BCBAD7BBC40B02B3EDF579B1AD29`;repo `target`、`zircon_editor\target` 与外部 target 同名截图扫描为 0。 | Editor retained-host UI family priority 已恢复为 DengXian/等线比例字体。完整 runtime FontDatabase/FontFace DTO、GPU draw-list family/style propagation、shaped glyph paint DTO、subrange kerning semantics、DPI glyph reraster/subpixel bins、native/SDF paragraph parity、baseline 与 vertical layout 仍 pending。 |
| 2026-07-01 | LB-M1/editor retained-host DengXian UI priority restoration consumer | runtime_text_editor_retained_dengxian_priority_restore_check_build_screenshot_metadata_recorded | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:用户最新 utility-tab crop 证明 Segoe-priority experiment 与当前等线要求冲突。本轮恢复 ordinary UI `Deng.ttf/Dengl.ttf` 优先、strong UI `Dengb.ttf/Deng.ttf` 优先,Segoe 只作 fallback,code style 仍优先 Cascadia/Consolas 等宽。`paint_text_tests.rs` 锁定 Windows 有 Deng 字体时 retained runtime family 为 `DengXian`;`template_buttons/content/metrics.rs` 同步把 button/tab label line-height snap 到整数像素,让 12px label 使用 14px slot。 | `cargo fmt -p zircon_editor --check` 通过；direct `retained_ui_runtime_family_prefers_dengxian_when_available_on_windows` 1/1；direct `paint_text` 20/20；direct `button_label` 4/4；direct `dock_tab_button_measures_label_with_declared_font_size` 1/1；ignored component atlas screenshot harness 1/1；ignored M3 screenshot harness 1/1；editor-host build passed in `D:\cargo-targets\zircon-editor-text-tabs-0701`。截图只记录刷新证据和人工检查输入,不直接判定文本渲染有效:Asset Browser `2026-07-01 20:47:01 +08:00`,SHA256 `DFA19780B09D6C2F323EC4B80407BF992B0202EAC033849892E942D1EAC84FDE`;utility tabs crop `2026-07-01 20:48:35 +08:00`,SHA256 `C0EA3A82F121551601FCB71D3AF538C55A9C3A5A22937AF5BB637ED53745D261`;3x crop SHA256 `279D6E3BBF66581CDD2C7417DD375E724137932BD7C4A9AAF605E7027C38266F`;repo `target`、`zircon_editor\target` 与外部 target 同名截图扫描为 0。 | Editor retained-host UI family priority 已恢复为 DengXian/等线。完整 runtime FontDatabase/FontFace DTO、GPU draw-list family/style propagation、shaped glyph paint DTO、subrange kerning semantics、DPI glyph reraster/subpixel bins、native/SDF paragraph parity、baseline 与 vertical layout 仍 pending。 |
| 2026-07-01 | LB-M1/editor retained-host runtime glyph-advance draw projection consumer | runtime_text_editor_retained_glyph_advance_projection_check_build_screenshot_metadata_recorded | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:前序切片已让 display text、ellipsis、font family projection 来自 runtime `layout_text(...)`,本轮把实际 host glyph x 坐标也改为消费 runtime `UiResolvedTextLine.glyph_advances`。`paint_text/draw/layout.rs` 新增 runtime-projected glyph DTO,按 grapheme 把 runtime advance 投影到 fontdue glyph indices/bounds 上;`draw/glyphs.rs` 绘制这些 projected positions,避免 display/measure 是 runtime 而 draw advance 仍是 fontdue 的双度量路径。 | `cargo fmt -p zircon_editor` 通过;`cargo fmt --check -p zircon_editor` 通过;`cargo check -p zircon_editor --no-default-features --locked --target-dir D:\cargo-targets\zircon-editor-text-spacing-0701-check --message-format short --color never` 通过(仅既有 warnings);`cargo test -q -p zircon_editor runtime_positioned_glyphs_use_runtime_grapheme_advances --no-default-features --locked --target-dir D:\cargo-targets\zircon-editor-text-spacing-0701-check` 通过 1/1;`cargo test -q -p zircon_editor retained_text_run_carries_runtime_projected_spacing --no-default-features --locked --target-dir D:\cargo-targets\zircon-editor-text-spacing-0701-check` 通过 1/1;`cargo test -q -p zircon_editor capture_m3_gui_acceptance_visual_artifacts --no-default-features --locked --target-dir D:\cargo-targets\zircon-editor-text-spacing-0701-check -- --ignored --nocapture` 通过 1/1。截图只记录刷新证据和人工检查输入,不直接判定文本渲染有效:Asset Browser `2026-07-01 12:34:01 +08:00`,SHA256 `40B3C1C5BD7D9E1CEE5DE282CBEF649D6AA21A1CCA5CC091D5BF6B71C1EE28F3`;utility tabs crop `2026-07-01 12:35:19 +08:00`,SHA256 `AE63ADADCB27E34B4E9BDFCAC88FF0B22AFB2768BA924170A38744C34876A5F9`;3x zoom crop `2026-07-01 12:35:19 +08:00`,SHA256 `8367763422CA9F8D4C4F4978F3C59ADC2C5B12B37781810942EC547001111C7F`;repo `target`、`zircon_editor\target` 与外部 target 同名截图扫描为 0。 | Editor retained-host actual draw advance mismatch 已关闭。完整 runtime FontDatabase/FontFace DTO、GPU draw-list family/style propagation、shaped glyph paint DTO、subrange kerning semantics、DPI glyph reraster/subpixel bins、native/SDF paragraph parity、baseline 与 vertical layout 仍 pending。 |
| 2026-07-01 | LB-M1/editor retained runtime font-family measurement/projection consumer | runtime_text_editor_retained_font_family_measurement_check_build_screenshot_metadata_recorded | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:fontdue raster face 已按 UI/Mono 分流后,本轮把 runtime measurement/projection 的 `UiResolvedStyle.font_family` 也显式对齐。`paint_text/font.rs` 新增 retained face 到 runtime family 的映射与 style-aware measurement helper,普通 UI text 使用 DengXian/等线 family,code text 使用 Cascadia/Consolas 等宽 family;`draw/layout.rs` 在 runtime `layout_text(...)` single-line ellipsis projection 中使用同一 family。 | scoped rustfmt 通过;`cargo test -p zircon_editor --lib paint_text --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-text-surface-0630 -- --nocapture` 通过 13/13;`cargo build -p zircon_app --bin zircon_editor --no-default-features --features target-editor-host --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-text-surface-0630 --message-format short --color never` 通过。截图只记录“刷新到了 `docs/tests/editor` 并可供人工查看”,不直接判定文本渲染有效:Asset Browser 修改时间 `2026-07-01 11:37:11 +08:00`,69660 bytes,SHA256 `9A47921836E870BD4414AD40C02534D2E7086355BA78A3129A1238A1A8AFF7F3`;utility tabs crop 修改时间 `2026-07-01 11:43:39 +08:00`,2414 bytes,SHA256 `72B23815D657450782CA45EF052D6CE974193FDDDBCB9BDB544394ABFCF4E253`;3x zoom crop SHA256 `F8F0344D354864E8A84E74E490B1E2C1BC32C191ECAA1F0290A0D134FB517F3D`;component atlas 修改时间 `2026-07-01 11:43:12 +08:00`,84041 bytes,SHA256 `12D98FCCF5718E2913670AEA3F07A6CE70C540F2C4A9DDB45325D353F507900F`;repo `target` 与 `D:\cargo-targets` 同名截图扫描为 0。 | Editor retained measurement/projection family mismatch 已关闭。完整 runtime line-breaking/measurement parity 仍需 runtime FontDatabase/FontFace、GPU draw-list family/style propagation、shaped glyph paint DTO、subrange kerning semantics、native/SDF paragraph parity、DPI baseline 与 vertical layout 后续切片。 |
| 2026-07-01 | LB-M1/editor retained-host UI/monospace font-face consumer | runtime_text_editor_retained_font_face_routing_check_build_screenshot_metadata_recorded | `zircon_editor` retained-host draw path 继续作为 runtime text surface consumer:single-line displayed text 仍来自 `zircon_runtime::ui::surface::layout_text`,但 fontdue raster face 不再固定 embedded fallback。`paint_text/font.rs` 将普通 UI text 映射到 UI face(Windows DengXian/等线候选优先),将 `UiTextRunPaintStyle::code` 映射到 monospace face(Cascadia/Consolas 候选优先);`draw/layout.rs`、`draw/glyphs.rs`、`raster.rs` 贯穿该 face 并把它纳入 glyph cache key。 | scoped rustfmt 通过;`cargo test -p zircon_editor --lib paint_text --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-text-surface-0630 -- --nocapture` 通过 12/12;`cargo build -p zircon_app --bin zircon_editor --no-default-features --features target-editor-host --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-text-surface-0630 --message-format short --color never` 通过。截图只记录“刷新到了 `docs/tests/editor` 并可供人工查看”,不直接判定文本渲染有效:component atlas 修改时间 `2026-07-01 10:08:56`,84041 bytes,SHA256 `210B5BA25C6C5C7E0FF6B85A79974447EAFFED0A9CB01A551799A0585329FA84`;M3 workbench 修改时间 `2026-07-01 10:09:36`,69666 bytes,SHA256 `4CA1F17CF1043F5E75B75B8A50974ABD7D225D056EE59DB58EBC504111E0CE87`;M3 asset-browser list 修改时间 `2026-07-01 10:10:00`,66190 bytes,SHA256 `8D9BF6A6595DE116BC7B26C2549673DB0A7325CA797336BDD548AC6F29681073`;repo `target` 与 `D:\cargo-targets` 同名截图扫描为 0。 | Editor retained fixed-font fallback 与 cache identity 已关闭。完整 runtime line-breaking/measurement parity 仍需 runtime FontDatabase/FontFace、shaped glyph paint DTO、subrange kerning semantics、native/SDF paragraph parity、DPI baseline 与 vertical layout 后续切片。 |
| 2026-07-01 | LB-M1/editor render-command alignment surface measurement consumer | runtime_text_editor_render_command_alignment_surface_measurement_check_build_screenshot_timestamp_recorded | `zircon_editor` retained-host render-command conversion 的 `render_command_conversion/style/text.rs::aligned_text_x(...)` 现在调用 `zircon_runtime::ui::surface::measure_text_size(...)` 计算 Center/Right/Start/End 对齐宽度,移除 command conversion 内的 `chars().count() * font_size * 0.5` 本地估算;`Justify` 在该 bridge 保持 line-start,不在 editor render-command 层伪造 paragraph justification。 | `rustfmt --edition 2021 --check zircon_editor\src\ui\retained_host\host_contract\paint_template_nodes\render_command_conversion\style\text.rs zircon_editor\src\ui\retained_host\host_contract\paint_template_nodes\template_list_rows\surface.rs` 通过;`cargo test -p zircon_editor --lib aligned_text_x_uses_runtime_surface_measurement --locked --jobs 1 -- --nocapture` 通过 1/1;`cargo build -p zircon_app --bin zircon_editor --no-default-features --features target-editor-host --locked --jobs 1 --message-format short --color never` 通过(仅既有 warnings)。截图只记录“刷新到了 `docs/tests/editor` 并可供人工查看”,不直接判定文本渲染有效:component atlas 修改时间 `2026-07-01 04:28:35`,85148 bytes,SHA256 `3CCB93AC8A30D89CC7288AA399041488A1401912AAFFE40EBC3C879D8A77C04F`;M3 workbench 修改时间 `2026-07-01 04:31:01`,71590 bytes,SHA256 `0A64A3A20EBBD3CC9399EA79B8054861F26F9F0F7D56E9401B8C6B0D64A811EE`;M3 asset-browser list 修改时间 `2026-07-01 04:31:18`,66812 bytes,SHA256 `F4DA73552425B2C7E4889B3FB9C6EA7CED1296523A707A345E1B88C4A33BB2CA`;repo `target` 与 `D:\cargo-targets` 同名截图扫描为 0。 | Editor render-command 对齐测量已接 runtime surface boundary;截图只证明文件刷新与位置正确。完整 `UiTextPaint` shaped DTO、subrange kerning semantics、native/SDF paragraph parity、DPI baseline、vertical layout 与文本视觉质量人工评审仍 pending。 |
| 2026-07-01 | LB-M2 JLREQ hyphen kinsoku table slice | runtime_text_lb_m2_jlreq_hyphen_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将 JLREQ cl-03 hyphens `‐〜゠–` 纳入 shared 行首禁则表，owner unit test `merges_jlreq_hyphens_with_previous_chunk` 锁定 `文‐`、`文〜`、`文゠`、`文–` 都与前一 chunk 合并并关闭 glyph fallback。`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_jlreq_hyphen`，验证过窄 frame 下 `文‐` 保持同一行 protected overhang，不让 `‐` 成为新行行首。UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-hyphen-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings；首次 300s 窗口超时后同 target-dir 后台编译自然完成，增量复跑通过)。`cargo test -p zircon_runtime jlreq_hyphen --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-hyphen-kinsoku --message-format short --color never --no-run` 最终通过并生成 lib-test binary(仅既有 warnings；前两次同命令在依赖编译阶段无诊断返回失败码，第三次 tail-captured rerun 15m30s 完成)。direct lib-test binary `E:\cargo-targets\zircon-runtime-text-0701-jlreq-hyphen-kinsoku\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe jlreq_hyphen --nocapture --test-threads=1` 通过 2/2。验证图 `docs/tests/runtime/text/runtime_text_jlreq_hyphen_kinsoku_preview_20260701.png` 已检查可读，SHA256 `4D45E10D708A8A13DDA9FEC3C06F5681D122306DEE8521A31DAB874434788ABE`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | JLREQ hyphen 行首禁则首段已关闭；完整 JIS/UAX kinsoku 泛化、squeeze/overhang 策略、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart ASCII quote-after-punctuation glue slice | runtime_text_lb_m2_word_smart_ascii_quote_punctuation_glue_check_passed | `graphics/text/layout/line_break/smart.rs` 将 WordSmart 首段从 ASCII 收尾标点扩展到标点后的 ASCII 闭合引号链 `"`/`'`: shaped single chunk 或 leading punctuation chunk `go,"a` 都会切为 protected `go,"` 加后续 `a`;结尾 `go,"` 也关闭 glyph fallback。`line_break/mod.rs` 新增 shared owner regression，`ui/text/layout_engine/tests/word_smart.rs` 新增 `word_smart_keeps_ascii_closing_quote_after_trailing_punctuation_with_previous_word`，验证过窄 frame 下 `go,"` 保持第一行 protected overhang。UI 仍只选择 `word_smart_line_break_chunks(...)`，不复制标点表。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/smart.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/ui/text/layout_engine/tests/word_smart.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)。冷 target `E:\cargo-targets\zircon-runtime-text-0701-word-smart-quote` 首次验证在 Windows lib-test compile 阶段超时且未产出本轮 binary，不计通过；增量复用 `E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku` 后 `cargo test -p zircon_runtime word_smart --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never -- --nocapture --test-threads=1` 通过 13/13(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_word_smart_quote_punctuation_preview_20260701.png` 已检查可读，SHA256 `D2BFF7961FEE5E129A9C273E880B00E309F03410AA17FBCBCE1AB18268226BAD`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | ASCII quote-after-punctuation glue 首段已关闭；完整 Unicode/Godot-style WordSmart word classes、成对引号判定、CJK/multi-script smart-wrap、squeeze/overhang、竖排与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart Unicode closing quote-after-punctuation glue slice | runtime_text_lb_m2_word_smart_unicode_quote_punctuation_glue_check_passed | `graphics/text/layout/line_break/smart.rs` 将同一个 protected run 从 ASCII closing quote 扩展到 Unicode right closing quotes U+2019/U+201D；shaped single chunk 或 leading punctuation chunk `go,”a` 都会切为 protected `go,”` 加后续 `a`，结尾 `go,’`/`go,”` 也关闭 glyph fallback。`line_break/mod.rs` 新增 shared chunk regression，`ui/text/layout_engine/tests/word_smart.rs` 新增 `word_smart_keeps_unicode_closing_quote_after_trailing_punctuation_with_previous_word`，验证过窄 frame 下 `go,”` 保持第一行 protected overhang。UI 仍只选择 `word_smart_line_break_chunks(...)`，不复制 Unicode quote policy。 | RED focused `cargo test -p zircon_runtime word_smart --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never -- --nocapture --test-threads=1` 先失败 5 项，证明旧实现把 `go,”a` 切成 `go,` 与 quote/suffix；修复后 `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/smart.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/ui/text/layout_engine/tests/word_smart.rs` 通过，`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)，同 focused `word_smart` 通过 18/18。验证图 `docs/tests/runtime/text/runtime_text_word_smart_unicode_quote_preview_20260701.png` 已检查可读，SHA256 `B4DE63AFBA9AAD94001A43FA008479AF2351BDCD02AE23EAB3A9E1733F022B7F`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Unicode right quote-after-punctuation glue 首段已关闭；完整 Unicode/Godot-style WordSmart word classes、成对引号方向判定、broader punctuation、CJK/multi-script smart-wrap、squeeze/overhang、竖排与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart fullwidth/CJK trailing punctuation glue slice | runtime_text_lb_m2_word_smart_fullwidth_punctuation_glue_check_passed | `graphics/text/layout/line_break/smart.rs` 将同一个 shared WordSmart trailing-punctuation owner 扩展到计划与 kinsoku 表已承认的全角/CJK 收尾标点 `、`/`。`/`，`/`．`/`・`/`：`/`；`/`！`/`？`。shaped single chunk 或 leading punctuation chunk `go，a` 现在切为 protected `go，` 与后续 `a`，结尾 `go，`/`go？`/`go。` 也关闭 glyph fallback。`line_break/mod.rs` 新增 shared chunk regression，`ui/text/layout_engine/tests/word_smart.rs` 新增 `word_smart_keeps_fullwidth_trailing_punctuation_with_previous_word`，UI 仍只选择 `word_smart_line_break_chunks(...)`，不复制 fullwidth/CJK 标点表。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/smart.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/ui/text/layout_engine/tests/word_smart.rs` 通过；触达 Rust 文件尾随空白扫描无输出；focused `cargo test -p zircon_runtime fullwidth_trailing --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-fullwidth --message-format short --color never -- --nocapture --test-threads=1` 通过 4/4（仅既有 warnings；完整命令 14m26s，测试执行 0.66s）。验证图 `docs/tests/runtime/text/runtime_text_word_smart_fullwidth_punctuation_preview_20260701.png` 已检查可读，尺寸 1120x620，SHA256 `F0B4F4AA2F80D3722F1F39B81F89A46DC4F15EBA8EFC03D157DAF834B4A7ADC0`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Fullwidth/CJK trailing punctuation glue 首段已由 focused Cargo 4/4 绿跑验证通过。完整 Unicode/Godot-style WordSmart word classes、成对引号方向判定、paired/full quote inference、CJK/multi-script smart-wrap、squeeze/overhang、竖排与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart CJK/fullwidth closing delimiter glue slice | runtime_text_lb_m2_word_smart_cjk_closing_delimiter_glue_check_passed | `graphics/text/layout/line_break/smart.rs` 继续作为 shared smart-wrap owner，将 protected punctuation run 扩展到全角/CJK 收尾标点后的 CJK/fullwidth 闭合符号 `）］｝｠】〕〉》」』〗〙〛〟〞＂＇`。shaped single chunk 或 leading punctuation chunk `go，」a` 现在切为 protected `go，」` 与后续 `a`，`go,）a` 与 `go，＂a` 同样不让闭合符号成为新行首；`line_break/mod.rs` 与 `ui/text/layout_engine/tests/word_smart.rs` 分别覆盖 shared chunk 与 UI wrapped-line consumption。UI layout 仍只调用 `word_smart_line_break_chunks(...)`，没有新增 UI-local CJK/fullwidth delimiter 表。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/smart.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/ui/text/layout_engine/tests/word_smart.rs` 通过；触达 Rust 文件尾随空白扫描无输出。focused `cargo test -p zircon_runtime cjk_closing_delimiter --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-word-smart-fullwidth --message-format short --color never -- --nocapture --test-threads=1` 通过 5/5（仅既有 warnings；完整命令约 7m45s，测试执行 0.78s）。验证图 `docs/tests/runtime/text/runtime_text_word_smart_cjk_closing_delimiter_preview_20260701.png` 已检查可读，尺寸 1120x620，SHA256 `73AAC7DF5328374E5F1CE9B0FECDC418E984407F4D5805381663C23C14C2E92E`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | CJK/fullwidth closing delimiter after punctuation 首段已由 focused Cargo 5/5 绿跑验证通过；完整 Unicode/Godot-style WordSmart word classes、成对/full quote inference、broader punctuation、CJK/多脚本 smart-wrap、squeeze/overhang 泛化、vertical layout 与 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 JLREQ cl-08 inseparable pair kinsoku slice | runtime_text_lb_m2_jlreq_inseparable_pair_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 新增 JLREQ cl-08 分离禁止 pair 表，覆盖 `——`、`……`、`‥‥`、`〳〵`、`〴〵`:跨 chunk 时合并到前一 chunk，单 chunk 内含 pair 时关闭 glyph fallback，避免 Word-wrap glyph fallback 把 pair 拆开。owner unit tests `merges_jlreq_inseparable_pairs`、`keeps_different_jlreq_inseparable_classes_separate`、`marks_single_chunk_jlreq_inseparable_pair_unbreakable` 分别锁定合并、负向不误合并和单 chunk 不可拆；`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_keeps_jlreq_inseparable_ellipsis_pair_together`，验证过窄 frame 下 `……` 保持同一行 protected overhang。验证阻塞中同步修复既有结构预算测试 `shader_prewarm_plugin_shading_model_descriptor.rs` 的 `&String` 数组 `.join("")` 编译错误为显式 `format!`，不改变 runtime text 行为。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_plugin_shading_model_descriptor.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)。首次 `cargo test -p zircon_runtime jlreq_inseparable --lib ... --no-run` 被上述无关结构预算测试编译错误阻断；修复后 `cargo test -p zircon_runtime jlreq_inseparable --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never -- --nocapture --test-threads=1` 通过 4/4。验证图 `docs/tests/runtime/text/runtime_text_jlreq_inseparable_kinsoku_preview_20260701.png` 已检查可读，尺寸 1120x560，SHA256 `5826FB74039E4903DD8A8FD869B0F45664564D236392DD1ABE678CC67DA76EA2`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | JLREQ cl-08 横排 pair 不可断首段已关闭；完整 JIS/UAX kinsoku 泛化、squeeze/overhang 策略、竖排 cl-08/kana repeat 细化、完整 greedy line breaker、native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 spacing voicing mark kinsoku table slice | runtime_text_lb_m2_spacing_voicing_mark_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将全角 spacing dakuten/handakuten `゛`/`゜` 纳入 shared 行首禁则表，owner unit test `merges_spacing_voicing_marks_with_previous_chunk` 锁定 `カ゛` 与 `ハ゜` 都和前一 chunk 合并并关闭 glyph fallback。`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_spacing_voicing_mark`，验证过窄 frame 下 `カ゛` 保持同一行 protected overhang，不让 `゛` 成为新行行首。UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-spacing-voicing-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings；首次 300s 窗口超时后同 target-dir 后台编译自然完成，增量复跑通过)。`cargo test -p zircon_runtime kinsoku --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-spacing-voicing-kinsoku --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test compile/link 阶段 904s 超时且无 Rust diagnostics；等待产物完成后 direct lib-test binary `E:\cargo-targets\zircon-runtime-text-0701-spacing-voicing-kinsoku\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe kinsoku --nocapture --test-threads=1` 通过 27/27。验证图 `docs/tests/runtime/text/runtime_text_spacing_voicing_mark_kinsoku_preview_20260701.png` 已检查可读，SHA256 `82C63DFE03B83C03F87E9B1420D1404DE5E348051273561A439EA64EC3237599`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Spacing voicing mark 行首禁则首段已关闭；完整 JIS/UAX kinsoku 泛化、squeeze/overhang 策略、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 fullwidth white parenthesis kinsoku table slice | runtime_text_lb_m2_fullwidth_white_parenthesis_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将 fullwidth white parenthesis variants 纳入 shared CJK 禁则表:行首禁止 `｠`,行尾禁止 `｟`。owner unit tests 锁定 `文｠` 与前一 chunk 合并、`｟文` 与后一 chunk 合并并关闭 glyph fallback；`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_fullwidth_white_close_parenthesis` 与 `text_wrap_cjk_kinsoku_no_trailing_fullwidth_white_open_parenthesis`，验证过窄 frame 下 `文｠` 保持 protected overhang，`文｟字` 不让 `｟` 留在上一行行尾。UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-fullwidth-white-parenthesis-kinsoku --message-format short --color never --quiet` 在本轮 300s 工具窗口内通过(仅既有 warnings)。direct lib-test binary `E:\cargo-targets\zircon-runtime-text-0701-fullwidth-white-parenthesis-kinsoku\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe kinsoku --nocapture --test-threads=1` 通过 25/25。验证图 `docs/tests/runtime/text/runtime_text_fullwidth_white_parenthesis_kinsoku_preview_20260701.png` 已检查可读，SHA256 `4946765AE3C692066A4C04E38671F98D78B42C281AB6904C2A4A6E464E1AB52D`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Fullwidth white parenthesis 禁则首段已关闭；完整 JIS/UAX kinsoku 泛化、squeeze/overhang 策略、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 CJK double-prime closing quote kinsoku table slice | runtime_text_lb_m2_cjk_double_prime_closing_quote_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将 CJK double-prime closing quote `〞` 纳入 shared 行首禁则表。owner unit test 锁定 `文〞` 与前一 chunk 合并并关闭 glyph fallback；`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_cjk_double_prime_closing_quote`，验证过窄 frame 下 `文〞` 保持 protected overhang，不让 `〞` 成为新行行首。UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；触达 Rust 文件尾随空白扫描无输出；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-cjk-double-prime-kinsoku --message-format short --color never --quiet` 最终通过(仅既有 warnings；首次同命令遇到非复现 `asset::pack` import 状态漂移，未改 asset pack 代码，重跑通过)。验证图 `docs/tests/runtime/text/runtime_text_cjk_double_prime_closing_quote_kinsoku_preview_20260701.png` 已检查可读，SHA256 `EC77D5A02F5D0FE4E9A8D8E782ED90C29F081691B8BC6AD3EA9A06A3FC39C032`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | CJK double-prime closing quote 行首禁则首段已关闭；完整 JIS/UAX 禁则表审计、squeeze/overhang policy generalization、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 与 focused Cargo test green run 仍 pending。 |
| 2026-07-01 | LB-M2 Katakana phonetic extension small-kana kinsoku table slice | runtime_text_lb_m2_katakana_phonetic_extension_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将片假名音标扩展小假名 `ㇰㇱㇲㇳㇴㇵㇶㇷㇸㇹㇺㇻㇼㇽㇾㇿ` 纳入 shared 行首禁则表。owner unit test 覆盖全部 16 个字符与前一 chunk 合并并关闭 glyph fallback；`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_katakana_phonetic_extension_small_kana`，验证过窄 frame 下 `一ㇰ` 保持 protected overhang，不让 `ㇰ` 成为新行行首。UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；触达 Rust 文件尾随空白扫描无输出；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-katakana-phonetic-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_katakana_phonetic_extension_kinsoku_preview_20260701.png` 已检查可读，SHA256 `44AD05A1739BBC75B7FA9D9B2FA356EB6F394DA90453B76ED1DDC11BCFB6A6DA`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 片假名音标扩展小假名行首禁则首段已关闭；完整 JIS/UAX 禁则表审计、squeeze/overhang policy generalization、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 与 focused Cargo test green run 仍 pending。 |
| 2026-06-30 | LB-M1/editor consumer retained measurement surface bridge | runtime_text_editor_retained_measure_surface_bridge_check_build_screenshot_passed | `zircon_runtime::ui::surface::measure_text_size(...)` 已作为 retained/editor-facing public surface wrapper 暴露,内部仍委托 `ui::text::measure_text_size(...)`。`zircon_editor` 的 retained-host `measure_runtime_text_width(...)` 用单行 `UiResolvedStyle` 调该入口,替换 editor-local fontdue character summation;button labels、status chip values、table commands/right-aligned cells 都消费同一 runtime surface measurement。 | scoped rustfmt --check 通过;direct exact `retained_text_measure_uses_runtime_surface_measurement` 1/1;direct `template_buttons` 41/41、`template_status_controls` 20/20、`template_table_rows` 20/20;`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-text-surface-0630 --message-format short --color never` 通过(仅既有 warnings);`zircon_app --bin zircon_editor --no-default-features --features target-editor-host` build passed。截图刷新到 `docs/tests/editor`:component atlas SHA256 `EA370390245E24AE5A8D024BF91C644B8B02F0D14BB5A10659E34738B8A0809E`,M3 workbench SHA256 `0A64A3A20EBBD3CC9399EA79B8054861F26F9F0F7D56E9401B8C6B0D64A811EE`,M3 asset-browser list SHA256 `F4DA73552425B2C7E4889B3FB9C6EA7CED1296523A707A345E1B88C4A33BB2CA`;repo `target` 同名匹配为 0。 | Editor retained measurement 已接 surface boundary;完整 shaped backend parity、subrange kerning semantics、native/SDF paragraph parity、vertical layout 与更广 focused runtime text matrix 仍 pending。 |
| 2026-07-01 | LB-M2 CJK white bracket kinsoku table slice | runtime_text_lb_m2_cjk_white_bracket_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将 CJK white bracket/quote variants 纳入 shared 禁则表:行首禁止 `〗〙〛〟`,行尾禁止 `〖〘〚〝`。owner unit tests 覆盖四个闭合变体与前一 chunk 合并、四个开启变体与后一 chunk 合并并关闭 glyph fallback；`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_cjk_white_close_punctuation` 与 `text_wrap_cjk_kinsoku_no_trailing_cjk_white_open_punctuation`，验证过窄 frame 下 `文〗` 保持 protected overhang，`文〖字` 不让 `〖` 留在上一行行尾。UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；触达 Rust 文件尾随空白扫描无输出；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-cjk-white-bracket-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_cjk_white_bracket_kinsoku_preview_20260701.png` 已检查可读，SHA256 `9CB6BBB21F385F1D889B73B5EE900360C2AD5E9058D48D7F73AA43618983FFAF`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | CJK white bracket/quote 禁则首段已关闭；完整 JIS/UAX 禁则表审计、squeeze/overhang policy generalization、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 与 focused Cargo test green run 仍 pending。 |
| 2026-07-01 | LB-M2 small ka/ke kinsoku + panic-free handoff slice | runtime_text_lb_m2_small_ka_ke_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将小写平假名/片假名 ka/ke `ゕゖヵヶ` 纳入 shared 行首禁则表，owner unit test 覆盖四个字符都与前一 chunk 合并并关闭 glyph fallback；同轮把禁则合并中两个 `peek().expect(...)` 改为显式 `if let Some(next)` 分支，消除该生产路径的强断言。`ui/text/layout_engine/tests/kinsoku.rs` 新增 `text_wrap_cjk_kinsoku_no_leading_small_katakana_ka`，验证过窄 frame 下 `一ヵ` 保持同一行 protected overhang，不把 `ヵ` 放到新行行首；UI 仍只消费 shared kinsoku metadata。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；触达 Rust 文件尾随空白扫描无输出；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-small-ka-ke-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_small_ka_ke_kinsoku_preview_20260701.png` 已检查可读，SHA256 `C13AA26D914D75A370FED02C8FD87AD784080281F348C33D0BD5CB653D1FD381`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 小写 ka/ke 禁则首段和本文件生产 `expect` 清理已关闭；完整 JIS/UAX 禁则表审计、squeeze/overhang policy generalization、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 与 focused Cargo test green run 仍 pending。 |
| 2026-06-30 | LB-M2 Japanese non-starter kinsoku table slice | runtime_text_lb_m2_japanese_nonstarter_kinsoku_check_passed | `graphics/text/layout/kinsoku.rs` 将全角长音符 `ー`、迭代符号 `々`、平假名/片假名迭代符号 `ゝゞヽヾ` 纳入行首禁则命名表；owner unit tests 直接锁定 `カー` 与 `時々` 在 split 点处合并为不可 glyph fallback 的 protected chunk。`ui/text/layout_engine/tests/kinsoku.rs` 新增长音符和迭代符号布局回归，验证过窄 frame 下允许 protected overhang，但不会让 `ー` 或 `々` 成为新行行首；UI 仍只消费 shared kinsoku chunk metadata，不复制禁则表。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-japanese-nonstarter-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_japanese_nonstarter_kinsoku_preview_20260630.png` 已检查可读，SHA256 `C4D561C28049B2E9BE86CE6C94778BD6F83A39FA0A5BA1F6011C21DA05E3BE2D`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 日文非行首字符禁则首段已关闭；完整 JIS/UAX 禁则表审计、squeeze/overhang policy generalization、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 greedy wrap decision owner split | runtime_text_lb_m2_greedy_wrap_decision_owner_check_passed_focused_test_timeout_no_result | 新增 `graphics/text/layout/line_break/greedy.rs`，把当前行加下一 chunk 是否仍能放入 frame 的宽度判断和 `line_text_fits(...)` 从 `ui/text/layout_engine/wrapping.rs` 下沉到 shared line-break 子 owner；`wrapping.rs` 的 Word/WordSmart 与 Glyph 路径只消费 `should_wrap_before_chunk(...)`，不再本地维护 append-fit helper；`line_break/mod.rs` 和 `layout/mod.rs` 只做 crate-private 薄导出。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/greedy.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/mod.rs zircon_runtime/src/ui/text/layout_engine/wrapping.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-greedy-wrap-decision --message-format short --color never --quiet` 通过(仅既有 warnings)。focused `cargo test -p zircon_runtime should_wrap_before_chunk --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-greedy-wrap-decision-test --message-format short --color never -- --nocapture --test-threads=1` 约 1154s 后仍处于 Windows lib-test compile/link 阶段，无 Rust diagnostics，matching target-dir cargo/rustc 已停止，未计通过。验证图 `docs/tests/runtime/text/runtime_text_greedy_wrap_decision_owner_preview_20260630.png` 已检查可读，SHA256 `C8CA68364812DC5C93D47ED2861131ABF0FAD4A258ED45E021D7644C2EF78278`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 本切片只关闭 append-width fit decision owner；完整 greedy paragraph line breaker、完整 Godot-style WordSmart policy、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused test 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 WordSmart wrap contract entry | runtime_text_lb_m2_word_smart_contract_entry_check_passed | `UiTextWrap::WordSmart` 已进入 public typography contract，并由 surface parser 支持 `word_smart`/`word-smart`/`smart_word`/`smart-word`；`ui/text/layout_engine/wrapping.rs` 将 `WordSmart` 路由到 shared Word wrapping path，保留 long-word glyph fallback；`graphics/scene/scene_renderer/ui/text.rs` 将 native glyphon 暂映射到 `Wrap::Word`，避免复制一套未完成的 smart-wrap policy。新增 `ui_text_wrap_word_smart_serializes_as_contract_value`、`render_extract_parses_word_smart_wrap_layout` 与 alias regression。 | RED `cargo check -p zircon_runtime_interface --lib --tests --locked --target-dir E:\cargo-targets\zircon-runtime-text-0630-word-smart-red --message-format short --color never` 先失败于缺少 enum variant；GREEN `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件通过；`cargo check -p zircon_runtime_interface --lib --tests --locked --target-dir E:\cargo-targets\zircon-runtime-text-0630-word-smart-interface --message-format short --color never --quiet` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-word-smart-runtime --message-format short --color never --quiet` 通过(仅既有 warnings)。后续 `cargo test -p zircon_runtime word_smart --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-word-smart-runtime --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test compile/link 阶段 904s 超时、无 Rust 诊断，残留 matching cargo/rustc 已停止，未计通过。验证图 `docs/tests/runtime/text/runtime_text_word_smart_wrap_preview_20260630.png` 已检查可读，SHA256 `494880855721F5E0F6B48FA4DB8B8F34EEE1AB0DF0386C7F5B35646E9AB23AFF`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 只关闭 `wrap_mode = WordSmart` 合同入口和当前接线；完整 smart word policy、完整 greedy line breaker、CJK/Unicode break 策略审计、vertical layout 和 native/SDF paragraph parity 仍 pending。 |
| 2026-07-01 | LB-M2 WordSmart ASCII trailing punctuation glue slice | runtime_text_lb_m2_word_smart_ascii_punctuation_glue_check_passed | 新增 `graphics/text/layout/line_break/smart.rs` 作为 WordSmart 首段 smart-wrap owner,把 ASCII 收尾标点 `,.:;!?` 与前一词绑定;当 shaping/UAX chunk 把 `go,a` 放在同一 chunk 时,smart owner 会在标点后切分为 protected `go,` 与后续 `a`,并关闭 protected 段 glyph fallback。`ui/text/layout_engine/wrapping.rs` 对 `UiTextWrap::WordSmart` 调用 shared `word_smart_line_break_chunks(...)`,不在 UI 层复制 smart-wrap 表。新增 owner tests 覆盖跨 chunk 合并、单 chunk suffix 保护、leading punctuation 负向场景,并新增 UI 回归 `word_smart_keeps_ascii_trailing_punctuation_with_previous_word`。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/smart.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/mod.rs zircon_runtime/src/ui/text/layout_engine/wrapping.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/word_smart.rs` 通过;`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never --quiet` 通过(仅既有 warnings;冷 target-dir `E:\cargo-targets\zircon-runtime-text-0701-word-smart-punctuation` 曾在依赖编译阶段无诊断异常退出,改用已热 text target-dir 增量复核)。首次 focused `cargo test -p zircon_runtime word_smart --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0701-jlreq-inseparable-kinsoku --message-format short --color never -- --nocapture --test-threads=1` 通过 5/7,暴露真实缺口:actual shaping 把 `go,next` 作为单 chunk,仅检查 chunk 起始标点不足;修复 chunk 内标点后切分后同命令通过 7/7。验证图 `docs/tests/runtime/text/runtime_text_word_smart_punctuation_preview_20260701.png` 已检查可读,SHA256 `781220B1414FA2B62E4433A2540AC5DFB1E6810694C52C1CEFD165985E2822C6`,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | WordSmart ASCII 收尾标点首段已关闭;完整 Godot-style smart-wrap 仍需 Unicode 词类/标点类扩展、CJK/多脚本策略、squeeze/overhang 泛化、完整 greedy paragraph line breaker、vertical layout 与 native/SDF paragraph parity。 |
| 2026-06-30 | UI layout line-box + direction owner split | runtime_text_ui_layout_line_box_direction_owner_split_check_passed | 新增 `ui/text/layout_engine/direction.rs`，把 Auto/Mixed first-strong paragraph direction、strong LTR/RTL char helpers 与 `is_rtl_direction(...)` 从 root 拆出；新增 `ui/text/layout_engine/line_box.rs`，把 measured/tab-aligned grapheme advances、Justify gate、line width clamp、logical Start/End x alignment、`MIN_TEXT_FONT_SIZE` 与 `text_advance(...)` 从 root 拆出。`visual_order.rs` 直接消费 direction child helpers；`layout_engine.rs` 只保留 `layout_text(...)` orchestration 与 overflow-style delegation，未新增 public facade、compat shim、旧路径 re-export 或第二套 layout policy。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/direction.rs zircon_runtime/src/ui/text/layout_engine/line_box.rs zircon_runtime/src/ui/text/layout_engine/wrapping.rs zircon_runtime/src/ui/text/layout_engine/ellipsis.rs zircon_runtime/src/ui/text/layout_engine/candidate_line.rs zircon_runtime/src/ui/text/layout_engine/range_mapping.rs zircon_runtime/src/ui/text/layout_engine/visual_order.rs zircon_runtime/src/ui/text/layout_engine/overflow_style.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-line-box-direction-owner --message-format short --color never --quiet` 通过(仅既有 warnings)；`layout_engine.rs` 138 行、`direction.rs` 36 行、`line_box.rs` 71 行。验证图 `docs/tests/runtime/text/runtime_text_line_box_direction_owner_preview_20260630.png` 已检查可读，SHA256 `438A776894C0C1BF46E3CC16B90BC3F7168D27F126F4B8B9C5BD0F504BD0DC8D`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Direction 与 line-box owner 已拆出；root 仍有 overflow-style handoff 与 layout orchestration。完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、完整 kinsoku、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | UI layout wrapping owner split | runtime_text_ui_layout_wrapping_owner_split_check_passed | 新增 `ui/text/layout_engine/wrapping.rs`，把 `wrap_source_runs(...)`、`split_preserving_newline(...)`、Word chunk consumption、Glyph fallback append path、leading grapheme continuation、append fit helper 与 `line_text_fits(...)` 从 `layout_engine.rs` 拆出；`ellipsis.rs` 改从 wrapping owner 消费 `line_text_fits(...)`。`layout_engine.rs` 只保留 layout orchestration、overflow-style resolution、line width/justify、alignment 与 direction resolution；未新增 public facade、compat shim、旧路径 re-export 或 UI-local Unicode/line-break policy。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/wrapping.rs zircon_runtime/src/ui/text/layout_engine/ellipsis.rs zircon_runtime/src/ui/text/layout_engine/candidate_line.rs zircon_runtime/src/ui/text/layout_engine/range_mapping.rs zircon_runtime/src/ui/text/layout_engine/visual_order.rs zircon_runtime/src/ui/text/layout_engine/overflow_style.rs` 通过；`layout_engine.rs` 236 行、`layout_engine/wrapping.rs` 230 行。`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-wrapping-owner --message-format short --color never` 通过(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_wrapping_owner_preview_20260630.png` 已检查可读，SHA256 `884D8361E0C44E971C2426D6FB8E625C760B692A40DE285E136D7006AFC4A0EA`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Wrapping owner 已拆出；root 仍承担 line width/justify、alignment、direction resolution。完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、完整 kinsoku、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | UI layout ellipsis projection owner split | runtime_text_ui_layout_ellipsis_projection_owner_split_check_passed | 新增 `ui/text/layout_engine/ellipsis.rs`，把 clipped-line merge、ellipsis run insertion、`EllipsisSegment` 到 `UiResolvedTextRun` 的 rich-run/source/visual range projection 和 horizontally overwide line predicate 从 `layout_engine.rs` 拆出；新增 `ui/text/layout_engine/range_mapping.rs` 作为 `source_subrange(...)` owner，供 ellipsis projection 与 `visual_order.rs` 共用。`graphics/text/layout/overflow.rs` 继续负责 shared ellipsis segment selection，UI 子 owner 只负责投影到 resolved runs；未新增 public facade、compat shim、旧路径 re-export 或第二套 overflow policy。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/ellipsis.rs zircon_runtime/src/ui/text/layout_engine/range_mapping.rs zircon_runtime/src/ui/text/layout_engine/visual_order.rs zircon_runtime/src/ui/text/layout_engine/candidate_line.rs zircon_runtime/src/ui/text/layout_engine/overflow_style.rs` 通过；触达 Rust 文件尾随空白扫描无输出；`layout_engine.rs` 454 行、`layout_engine/ellipsis.rs` 177 行、`layout_engine/range_mapping.rs` 19 行、`layout_engine/visual_order.rs` 348 行。`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-ellipsis-owner --message-format short --color never` 通过(仅既有 warnings)。验证图 `docs/tests/runtime/text/runtime_text_ellipsis_projection_owner_preview_20260630.png` 已检查可读，SHA256 `C234DB8ECC288C9C73173F5D8C336AC9D3D10E8AA51814023B8705C9BE5D12B5`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | Ellipsis projection 与 range mapping owner 已拆出；root 仍承担 word/glyph wrapping、direction、line width/justify。完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused test 绿跑仍 pending。 |
| 2026-06-30 | UI layout candidate-line owner split | runtime_text_ui_layout_candidate_line_owner_split_static_passed_cargo_timeout_no_result | 新增 `ui/text/layout_engine/candidate_line.rs`，把 `CandidateLine`/`PendingBreakSuffix` 状态、segment append、line push、pending suffix 追加和 trailing wrap-space run/source/visual range mutation 从 `layout_engine.rs` 拆出。`layout_engine.rs` 继续负责 `wrap_source_runs(...)` orchestration、word/glyph wrap algorithm choice、overflow/ellipsis、justify 和 direction resolution；未新增 public facade、compat shim、旧路径 re-export 或 UI-local line-break policy。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/candidate_line.rs` 通过；scoped `git diff --check` 仅 LF/CRLF 提示；触达 Rust 文件尾随空白扫描无输出；`layout_engine.rs` 633 行、`layout_engine/candidate_line.rs` 106 行。验证图 `docs/tests/runtime/text/runtime_text_candidate_line_owner_preview_20260630.png` 已检查可读，SHA256 `E1592C81E64310DDF589054E763D09FA3326C329460979121439F0FB9CC934D9`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-candidate-line-owner --message-format short --color never` 124s 超时且无 Rust diagnostics，不计通过；事后未发现匹配该 target-dir 的残留进程，其他活动 cargo 属于 editor build 且未停止。 | CandidateLine mutation owner 已拆出；root 仍承担 word/glyph wrapping、ellipsis construction、direction、line width/justify。完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 glyph fallback predicate owner split | runtime_text_lb_m2_glyph_fallback_predicate_owner_split_static_passed_cargo_deferred | 新增 `graphics/text/layout/line_break/glyph_fallback.rs`，把普通过宽 chunk 是否退回 glyph wrapping 的 measured-width + Unicode grapheme-count predicate 从 `line_break/mod.rs` 拆到 line-break 子 owner；主 `line_break/mod.rs` 只保留 `LineBreakChunk::should_fallback_to_glyph_wrap(...)` method handoff、shaped soft-break chunk production 与 kinsoku handoff。新增 child owner unit tests 覆盖 plain overwide、glue overwide、single grapheme；未新增 UI-local Unicode 表、public facade、compat shim 或旧路径 re-export。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/line_break/glyph_fallback.rs zircon_runtime/src/graphics/text/layout/line_break/glue.rs zircon_runtime/src/graphics/text/layout/line_break/soft_hyphen.rs zircon_runtime/src/graphics/text/layout/line_break/wrap_space.rs zircon_runtime/src/graphics/text/layout/mod.rs` 通过；scoped `git diff --check` 仅 LF/CRLF 提示；触达 Rust 文件尾随空白扫描无输出；`line_break/mod.rs` 152 行、`line_break/glyph_fallback.rs` 59 行、`line_break/glue.rs` 50 行、`line_break/soft_hyphen.rs` 151 行、`line_break/wrap_space.rs` 39 行、`layout/mod.rs` 18 行。验证图 `docs/tests/runtime/text/runtime_text_glyph_fallback_predicate_owner_preview_20260630.png` 已检查可读，SHA256 `D4796855759A42D7FE652B6AEF95B1639C42AB6E609CC210B32CCA59876A033F`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。Cargo 按里程碑测试阶段补跑，本结构切片不计 Cargo 通过。 | glyph fallback predicate 已从主断行模块拆出；完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 soft-hyphen policy owner split | runtime_text_lb_m2_soft_hyphen_owner_split_static_passed_cargo_deferred | 新增 `graphics/text/layout/line_break/soft_hyphen.rs`，把 U+00AD 视觉 chunk 剥离、wrap-time `-` break suffix 与 source range metadata 从 `line_break/mod.rs` 拆到 line-break 子 owner；当轮主 `line_break/mod.rs` 只保留 shaped soft-break chunk production、glyph fallback decision 与 kinsoku handoff，后续 glyph fallback predicate 已继续拆到 `line_break/glyph_fallback.rs`。新增 owner unit tests 覆盖 plain chunk、单个 U+00AD visual stripping/source suffix、多 U+00AD prefix suffix。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/line_break/glue.rs zircon_runtime/src/graphics/text/layout/line_break/soft_hyphen.rs zircon_runtime/src/graphics/text/layout/line_break/wrap_space.rs zircon_runtime/src/graphics/text/layout/mod.rs` 通过；scoped `git diff --check` 仅 `layout/mod.rs` LF/CRLF 提示；触达 Rust 文件尾随空白扫描无输出；`line_break/mod.rs` 157 行、`line_break/soft_hyphen.rs` 134 行、`line_break/glue.rs` 44 行、`line_break/wrap_space.rs` 31 行、`layout/mod.rs` 16 行。验证图 `docs/tests/runtime/text/runtime_text_soft_hyphen_owner_split_preview_20260630.png` 已检查可读，SHA256 `F885F5E4450DF9464427A6A760DC156B1153C2BCB1936C0C7CA3FBAFBBE1A485`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。Cargo 按里程碑测试阶段补跑，本结构切片不计 Cargo 通过。 | soft-hyphen visual/source policy 已从主断行模块拆出；完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 wrap-space policy owner slice | runtime_text_lb_m2_wrap_space_owner_static_passed_cargo_deferred | 新增 `graphics/text/layout/line_break/wrap_space.rs`，把 Word-wrap 行首/行尾普通空格修剪策略从 `ui/text/layout_engine.rs` 下沉到 line-break 子 owner；UI 只保留它必须拥有的 `CandidateLine` run/source/visual range mutation。helper 保持现有行为，只处理 ASCII `' '`，不扩展到 NBSP 或 Unicode whitespace。新增 owner unit tests 与 `ui/text/layout_engine/tests/wrap_space.rs` 覆盖 ASCII edge trimming、source range 推进、NBSP 不被修剪。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/mod.rs zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/line_break/glue.rs zircon_runtime/src/graphics/text/layout/line_break/wrap_space.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/glue.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests/wrap_space.rs` 通过；scoped `git diff --check` 仅 LF/CRLF 提示；触达 Rust 文件尾随空白扫描无输出；`line_break/mod.rs` 270 行、`line_break/wrap_space.rs` 39 行、`ui/text/layout_engine.rs` 737 行、父 `layout_engine/tests.rs` 755 行。验证图 `docs/tests/runtime/text/runtime_text_wrap_space_owner_preview_20260630.png` 已检查可读，SHA256 `B2D8AB6798F2974C7D78826E3CF74E0682819D140AC2A58E99A8B80002A3B362`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。Cargo 按里程碑测试阶段补跑，本结构切片不计 Cargo 通过。 | wrap-space trimming 首段已从 UI 下沉；完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 glyph fallback decision owner slice | runtime_text_lb_m2_glyph_fallback_decision_owner_static_passed_cargo_deferred | `graphics/text/layout/line_break/mod.rs` 新增 `LineBreakChunk::should_fallback_to_glyph_wrap(...)`，把“chunk 允许 glyph fallback + 当前宽度放不下 + 多于一个 grapheme”这一 Word-wrap 决策收回 shared line-break owner；`ui/text/layout_engine.rs` 只调用该 helper，再执行既有 glyph wrap 拼接。新增 owner unit tests 覆盖普通过宽 chunk 会请求 glyph fallback、glue chunk 不请求、单 grapheme 不请求；未新增 UI-local Unicode 表、public facade、compat shim 或旧路径 re-export。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/line_break/glue.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/glue.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；scoped `git diff --check` 仅 LF/CRLF 提示；触达 Rust 文件尾随空白扫描无输出；`line_break/mod.rs` 233 行、`ui/text/layout_engine.rs` 689 行、父 `layout_engine/tests.rs` 648 行。验证图 `docs/tests/runtime/text/runtime_text_glyph_fallback_owner_preview_20260630.png` 已检查可读，SHA256 `E04C23E1B7CBB7106C687283D393529FF29D4EB2E7376A6D766714CE1D16FC82`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。Cargo 按里程碑测试阶段补跑，本结构切片不计 Cargo 通过。 | glyph fallback 决策首段已从 UI 下沉；完整 greedy line breaker、WordSmart、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 line-break glue owner split | runtime_text_lb_m2_line_break_glue_owner_split_static_passed_cargo_deferred | `graphics/text/layout/line_break.rs` 已硬切为目录模块：当轮 `line_break/mod.rs` 当时仍保留 shaped soft-break chunk、soft-hyphen suffix 与 kinsoku 入口；本次之后 soft-hyphen policy 已继续拆到 `line_break/soft_hyphen.rs`。`line_break/glue.rs` 独立拥有 NBSP/ZWJ/NBHY/NNBSP/WJ/ZWNBSP 与 variation selector glue 分类。`layout/mod.rs` 仍只挂载 `mod line_break;` 并导出 `line_break_chunks(...)`，未保留旧文件 shim、旧路径 re-export 或 UI-local Unicode glue 表。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/line_break/mod.rs zircon_runtime/src/graphics/text/layout/line_break/glue.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/glue.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs zircon_runtime/src/graphics/text/layout/kinsoku.rs` 通过；scoped `git diff --check` 仅既有 LF/CRLF 提示；触达 Rust 文件尾随空白扫描无输出；`line_break/mod.rs` 194 行、`line_break/glue.rs` 44 行、父 `layout_engine/tests.rs` 648 行。结构验证图 `docs/tests/runtime/text/runtime_text_line_break_glue_owner_preview_20260630.png` 已检查可读，SHA256 `DBAD6B0CF280F9EFD5E04C85A42C44F83B8A48219A42C20766B84A349BCCC32E`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。Cargo 仍按里程碑测试阶段补跑，不把相邻 Windows lib-test 904s 超时队列计为通过。 | glue 分类 owner 已从主断行模块拆出；完整 greedy line breaker、完整 Unicode glue/kind 分类、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 additional glue characters slice | runtime_text_lb_m2_additional_glue_static_passed_cargo_deferred | `graphics/text/layout/line_break.rs` 将 U+2011、U+202F、U+2060、U+FEFF 纳入 `is_glue_character(...)`，共享 chunk metadata 统一关闭 glyph fallback；`ui/text/layout_engine/tests/glue.rs` 新增额外 glue 字符布局回归，父测试文件保持 754 行。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/line_break.rs zircon_runtime/src/ui/text/layout_engine/tests/glue.rs zircon_runtime/src/ui/text/layout_engine/tests.rs` 通过；触达 Rust 文件尾随空白扫描无输出。验证图 `docs/tests/runtime/text/runtime_text_additional_glue_preview_20260630.png` 已检查可读，SHA256 `E9BC81D86E3CCF12B15C7485BD6A1404479F32AEFC19229A4F471F68273C742C`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。前两条相邻 focused Cargo 已连续 904s 编译超时，本切片不再抢第三条 Cargo 队列，按里程碑测试阶段补跑，不计通过。 | 额外 glue 字符首段已关闭；完整 Unicode line-break glue 分类、完整 greedy line breaker、JIS/UAX kinsoku 表审计、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 variation selector glue slice | runtime_text_lb_m2_variation_selector_glue_static_passed_focused_test_timeout | `graphics/text/layout/line_break.rs` 将 VS15/VS16 与 supplemental variation selectors 纳入 `allows_glyph_fallback(...)`，含 selector 的 chunk 不允许 Word-wrap glyph fallback 拆开。新增 `ui/text/layout_engine/tests/glue.rs` 子模块，承接既有 NBSP/ZWJ 回归并新增 variation selector 布局回归；父 `layout_engine/tests.rs` 降到 754 行。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/line_break.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/glue.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs zircon_runtime/src/graphics/text/layout/kinsoku.rs` 通过；scoped `git diff --check` 仅 LF/CRLF 提示，触达文件尾随空白扫描无输出。focused `cargo test -p zircon_runtime variation_selector --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-variation-selector-glue --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test compile 阶段 904s 超时，无 Rust diagnostics，owned target-dir cargo/rustc 已停止，不计通过。验证图 `docs/tests/runtime/text/runtime_text_variation_selector_glue_preview_20260630.png` 已检查可读，SHA256 `52BE2D7079B2BC75B7AE2F4DE48F32D46837AFB0166EF6FA8D98F98FC79EB76A`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | VS glue 首段已关闭；更完整 emoji variation/ZWJ 分类、完整 greedy line breaker、完整 JIS/UAX kinsoku、squeeze/overhang、vertical layout、native/SDF parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 halfwidth kana kinsoku table slice | runtime_text_lb_m2_halfwidth_kana_kinsoku_static_passed_focused_test_timeout | `graphics/text/layout/kinsoku.rs` 补齐半角 CJK 禁则首段：半角句读/括号、小假名、长音、半角浊音/半浊音标记不允许成为行首，半角开引号 `｢` 不允许成为行尾；同轮把禁则字符从长 `matches!` 拆成命名表，后续完整 JIS/UAX 审计只需扩表。新增 owner unit tests 直接验证半角小假名、浊音标记和半角开引号 chunk 合并并关闭 glyph fallback。布局回归移入 `ui/text/layout_engine/tests/kinsoku.rs` 子模块，父测试文件仅保留 `mod kinsoku;`，避免继续扩大 `layout_engine/tests.rs`。 | `rustfmt --check zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs` 通过；`git diff --check -- zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/text/layout_engine/tests/kinsoku.rs docs/tests/runtime/text/runtime_text_halfwidth_kana_kinsoku_preview_20260630.png` 仅 LF/CRLF 提示，触达文件尾随空白扫描无输出。focused `cargo test -p zircon_runtime halfwidth --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-halfwidth-kinsoku --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test compile 阶段 904s 超时，无 Rust diagnostics，owned target-dir cargo/rustc 已停止，不计通过。验证图 `docs/tests/runtime/text/runtime_text_halfwidth_kana_kinsoku_preview_20260630.png` 已检查可读，SHA256 `D3B590DC3D420F20B26E0A0500E3B3A992AC9B742A41D3686D27942B6072D0F8`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 半角禁则表首段已关闭；完整 JIS/UAX 禁则表审计、squeeze/overhang policy generalization、完整 greedy line breaker、竖排 kinsoku、native/SDF paragraph parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M2 ZWJ emoji glue first slice | runtime_text_lb_m2_zwj_emoji_glue_static_passed_focused_test_timeout | `graphics/text/layout/line_break.rs` 将 chunk 的 `allow_glyph_fallback` 判定集中到 `allows_glyph_fallback(...)`，含 U+00A0 或 U+200D 的 glue chunk 都不允许 Word-wrap 退回逐字素拆分；`layout_engine.rs` 继续只消费 chunk metadata，不复制 NBSP/ZWJ/emoji 规则。新增 chunk-level ZWJ guard 与 `word_wrap_keeps_zwj_emoji_sequence_together`，锁定 `👩‍💻` 这类 ZWJ emoji 序列在窄 frame 下允许 overhang 但不能拆成多行。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\text\layout\line_break.rs zircon_runtime\src\ui\text\layout_engine\tests.rs` 通过；`git diff --check -- zircon_runtime\src\graphics\text\layout\line_break.rs zircon_runtime\src\ui\text\layout_engine\tests.rs` 仅 LF/CRLF 提示，触达文件尾随空白扫描无输出。focused `cargo test -p zircon_runtime word_wrap_keeps_zwj_emoji_sequence_together --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-zwj-glue --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test compile 阶段 908s 超时，无 Rust diagnostics、无 test binary，owned target-dir cargo/rustc 已停止，不计通过。验证图 `docs/tests/runtime/text/runtime_text_zwj_emoji_glue_preview_20260630.png` 已检查可读，SHA256 `35C06A4EDF04B40B1D3F791FB3156257592BB550C42FACA55881404BB3D643B5`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | NBSP/ZWJ glue 的首段 metadata 已关闭；完整 greedy line breaker、完整 JIS/UAX 行首尾禁则、squeeze/overhang 策略泛化、emoji variation selector/更广 ZWJ 序列、竖排布局、native/SDF paragraph parity 与 focused Cargo 绿跑仍 pending。 |
| 2026-06-30 | LB-M1/IM-M1 soft-wrapped composition geometry consumer slice | runtime_text_im_m1_m2_composition_source_range_soft_wrap_focused_tests_passed | `editable_text/ime_context.rs` 在 IME state 已通过 `SetComposition` 写入当前 preedit 文本后，直接用当前 `state.text` 构建 `UiTextLayoutRequest` 并解析 `UiResolvedTextLayout`，不再叠加第二层 `UiPreeditSpan`。软换行回归用 `text_range_frames_for_text_layout(...)` 对 composition range `1..6` 做预期值，允许 glyph-wrap 按 run 产生多个 rect fragment，同时断言后续 rect 的 y 坐标跨到下一行。 | `cargo test -p zircon_runtime text_input_ime_preedit_rects_follow_soft_wrapped_composition_range --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-ime-composition-range --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1；验证图 `docs/tests/runtime/text/runtime_text_ime_composition_range_soft_wrap_preview_20260630.png` 已检查，SHA256 `DAA61025233DD0F684884573459D85B86AA86BE673B1EEBAD7AC68F8A5AE61AA`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | soft-wrapped composition rect 消费首段已关闭；完整 backend cluster reverse lookup、RTL isolate/level caret affinity、竖排 caret/selection/IME 几何与 native/SDF paragraph parity 仍 pending。 |
| 2026-06-30 | LB-M3 editor retained-host consumer bridge | editor_retained_host_consumes_runtime_layout_text_build_screenshot_passed | Editor retained-host text paint now consumes the runtime text layout surface for single-line overflow projection: `zircon_editor/src/ui/retained_host/host_contract/paint_text/draw/layout.rs` calls `zircon_runtime::ui::surface::layout_text` and uses the returned displayed line text before fontdue rasterization; `draw.rs` records the same projected display text. The old editor-private `draw/ellipsis.rs` was deleted, so retained paint no longer carries a second line-overflow implementation. This is a consumer hard-cut to the runtime interface, not a new runtime layout-engine slice. | scoped rustfmt passed; direct `D:\cargo-targets\zircon-editor-components-0630-runtime-text-bridge\debug\deps\zircon_editor-ae3f11e68a49a9b4.exe paint_text --nocapture --test-threads=1` passed 7/7, including `retained_text_records_runtime_single_line_ellipsis_projection`; component atlas screenshot refreshed `docs/tests/editor/editor-components-workbench-slate-atlas-900x620.png`; full editor-host build passed in external `D:\cargo-targets\zircon-editor-components-0630-runtime-text-bridge`; repo `target` received no new screenshot. | Runtime layout remains owner for overflow/grapheme projection; future text slices still need backend cluster reverse lookup, RTL isolate/level caret affinity, vertical geometry, and broader native/SDF paragraph parity. |
| 2026-06-30 | LB-M1 shaped glyph non-isomorphic source range slice | runtime_text_lb_m1_shaped_glyph_non_isomorphic_source_range_focused_test_passed | `zircon_runtime_interface/src/ui/surface/render/text_shape.rs` 不再用 `run.source_range.start + local_visual_offset` 截断 source/visual 非一一映射 run。`source_range_for_run_visual_span(...)` 在 run source byte length 与 visual text length 不一致时返回完整 `run.source_range`；既有 soft-hyphen render contract 现在也断言 visual `-` 对应的 `UiShapedGlyph.source_range` 保留 `3..5`。未新增 public DTO field、compat shim、旧路径 re-export 或 UI-local geometry policy。 | `rustfmt --edition 2021 --check zircon_runtime_interface/src/ui/surface/render/text_shape.rs zircon_runtime_interface/src/tests/render_contracts.rs` 通过；`cargo test -p zircon_runtime_interface ui_text_decorations_use_run_visual_ranges_for_non_isomorphic_source_ranges --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-interface-source-range --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1。验证图 `docs/tests/runtime/text/runtime_text_shaped_glyph_source_range_soft_hyphen_preview_20260630.png` 已检查可读，SHA256 `A98D1EB788ADC9B759665D4592E9CC557C20846BDD3511F710CAEDE7C7993F6E`，repo `target` 与 `E:\cargo-targets` 同名匹配均为 0。 | render DTO source range truncation 首段已关闭；backend `ShapedGlyph.source_range` full cluster reverse lookup、RTL isolate/level caret affinity、竖排 caret/selection/IME 几何仍 pending。 |
| 2026-06-30 | LB-M1/IM-M1 public IME cursor rect resolved geometry slice | runtime_text_lb_m1_ime_cursor_rect_resolved_geometry_check_passed_focused_test_blocked | `zircon_runtime/src/ui/text/geometry.rs` 新增为 runtime resolved source/visual geometry owner，集中 caret frame 与 text range frames 的 `UiResolvedTextRun.source_range`→`visual_range` 映射、grapheme snapping 与 `glyph_advances` x 坐标计算；`editable_text/ime_context.rs` 在 `UpdateCursor` 时优先按当前 editable text state 重新生成 `UiResolvedTextLayout`，cursor rect 与 composition rects 先走该 geometry owner，只有 layout 不可用时才回退旧 fixed-column estimate。`surface/render/resolve.rs::resolve_style` 只扩到 `crate::ui::surface` 内部复用，未新增 public facade/compat shim/第二套 style parser。新增 `text_input_ime_cursor_rect_uses_resolved_tab_advances` 锁定 `a\tb` caret byte offset 2 的 IME rect 使用 resolved tab advance 而不是 `font_size*0.6` 两列估算。 | `rustfmt --edition 2021 zircon_runtime/src/ui/text/geometry.rs zircon_runtime/src/ui/text/mod.rs zircon_runtime/src/ui/surface/render/mod.rs zircon_runtime/src/ui/surface/render/resolve.rs zircon_runtime/src/ui/surface/input/editable_text/ime_context.rs zircon_runtime/src/ui/tests/widget_text_input_ime_context.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-ime-geometry-check --message-format short --color never` 通过(既有 warnings)。focused `cargo test -p zircon_runtime text_input_ime_cursor_rect_uses_resolved_tab_advances --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-ime-geometry-test --message-format short --color never -- --nocapture --test-threads=1` 首跑在 lib-test 编译阶段被无关当前 `graphics/scene/scene_renderer/mesh/build_mesh_draws/build/morph_payload_upload.rs:274` 的 `borrow of moved value: first` 阻断；最终复跑在 Windows lib-test 编译阶段 304s 超时且无 Rust diagnostics，均未进入本切片断言，不计测试通过。验证图 `docs/tests/runtime/text/runtime_text_ime_cursor_tab_preview_20260630.png` 已检查可读，SHA256 `486AF719C9CF9F447AA8A75166C546F24EB84C9337E5BBE5329EEA57DD9BA1B9`，repo `target` 与 `E:\cargo-targets` 同名匹配均为 0。 | public IME cursor/composition rect 的 resolved layout handoff 首段已关闭；仍需 backend `ShapedGlyph.source_range` cluster 级反查、RTL isolate/level caret affinity、竖排 caret/selection/IME 几何，以及平台候选窗实机锚定验收。 |
| 2026-06-30 | LB-M1 editable source/visual geometry slice | runtime_text_lb_m1_editable_source_visual_geometry_focused_tests_passed | `zircon_runtime_interface/src/ui/surface/render/text_geometry.rs` 新增为 editable text decoration 几何 owner，`command.rs` 只委托 `editable_text_decorations(...)`。selection、composition underline 与 caret frame 现在按 `UiResolvedTextRun.source_range` 到 `visual_range` 映射，再消费 `UiResolvedTextLine.glyph_advances` 定位；非一一映射 run（如 soft hyphen source `3..5` 可视为单个 `-`）不再用源字节差错误扩展到两个可视字符。 | `rustfmt zircon_runtime_interface/src/ui/surface/render/command.rs zircon_runtime_interface/src/ui/surface/render/mod.rs zircon_runtime_interface/src/ui/surface/render/text_geometry.rs zircon_runtime_interface/src/tests/render_contracts.rs` 通过；`cargo check -p zircon_runtime_interface --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-text-geometry-interface --message-format short --color never` 通过；focused `cargo test -p zircon_runtime_interface ui_text_decorations --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-text-geometry-interface --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2，覆盖既有 grapheme cluster edge 与新增 `ui_text_decorations_use_run_visual_ranges_for_non_isomorphic_source_ranges`。验证图 `docs/tests/runtime/text/runtime_text_editable_geometry_soft_hyphen_preview_20260630.png` 已检查，SHA256 `F4781A30F80E90336E53A5F1C0A843EFB37C2D30CF09577E455DA1D061027AA8`，repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | editable decoration 的 source/visual range 首段已关闭；public IME cursor rect 已由同日 runtime IME slice 接上；仍需把完整 backend `ShapedGlyph.source_range` cluster 反查、RTL isolate/level caret affinity 与竖排 caret/selection 几何接到同一映射 owner。 |
| 2026-06-30 | LB-M1 source subrange measure slice | runtime_text_lb_m1_source_subrange_measure_check_passed_focused_test_timeout | `graphics/text/layout/measure.rs` 新增基于 `ShapedGlyphRun.source_range`/`ShapedGlyph.source_range` 的 owner-local `measured_width(run, byte_start, byte_end, include_kerning)`：请求区间先 clamp 到 run source range,按 glyph source overlap 累加 advance,partial cluster 用 source grapheme count 比例分摊；`measured_grapheme_widths(...)` 改为复用同一入口,让 tab/justify/hit-test fallback advance 走 source-range measurement。`layout/mod.rs` 只保留实际被消费的薄导出,未新增未消费 root façade/compat shim。 | `rustfmt --edition 2021 zircon_runtime/src/graphics/text/layout/measure.rs zircon_runtime/src/graphics/text/layout/mod.rs` 通过；`git diff --check -- zircon_runtime/src/graphics/text/layout/measure.rs zircon_runtime/src/graphics/text/layout/mod.rs` 仅报告 LF/CRLF 提示；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-measured-width-check --message-format short --color never` 通过(既有 warnings)。focused `cargo test -p zircon_runtime measured_width_ --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-measured-width-check --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test 编译阶段 904s 超时、无 Rust diagnostics、无 test binary,本轮 target-dir 匹配进程已停止,不计通过。验证图 `docs/tests/runtime/text/runtime_text_source_subrange_measure_preview_20260630.png` 已检查可读,SHA256 `3231DDE698E6C9F64D632F1A8EA72BA4C65EE01B58052A5BC2F28A40D7D28EC0`,repo `target` 与 `E:\cargo-targets` 同名匹配均为 0。 | 这只关闭 source byte 子范围宽度首段和 grapheme advance 消费接线；`include_kerning=false` 的 unkerned/GPOS delta 语义、public layout measure API、selection cluster 反查、完整 backend cluster hit-test、BIDI/竖排 subrange measure 仍 pending。 |
| 2026-06-30 | LB-M3 hit-test resolved advances slice | runtime_text_lb_m3_hit_test_resolved_advances_check_passed_focused_test_timeout | `ui/text/hit_test.rs` 现在优先使用 `UiResolvedTextLine.glyph_advances` 推算视觉 grapheme midpoint,只有 resolved advances 缺失/全 0 时才回退 shared measurement；`text_hit_test_uses_resolved_tab_advances` 锁定 `a\tb` 的 tab-expanded advance 区域,midpoint 前映射 source offset 1、midpoint 后映射 source offset 2,避免 hit-test 用默认样式重测量绕过 layout 阶段 tab/justify advance。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/hit_test.rs zircon_runtime/src/ui/tests/text_hit_testing.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-hit-test-check --message-format short --color never` 通过(既有 warnings)。focused `cargo test -p zircon_runtime text_hit_test_uses_resolved_tab_advances --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-hit-test-check --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test 编译阶段 904s 超时、无 Rust diagnostics、无 test binary,本轮 target-dir 匹配进程已停止,不计通过。验证图 `docs/tests/runtime/text/runtime_text_hit_test_tab_caret_preview_20260630.png` 已检查可读,SHA256 `F11D43A7EC078146D747F1B8FD057367B1110238DB03DE3587CF8FD166AB16EE`,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | hit-test 已接入 resolved visual advances 首段；完整 `ShapedGlyph.source_range` cluster 级反查、RTL isolate/level 命中、竖排命中、native/SDF paragraph parity 仍 pending。 |
| 2026-06-30 | LB-M3 kashida justify advance first slice | runtime_text_lb_m3_kashida_justify_check_passed_focused_test_blocked | `graphics/text/layout/align.rs` 的 shared justify owner 新增 Arabic joining-pair opportunity,在 `JUSTIFICATION_KASHIDA` 对齐口径下先把 extra advance 分配到可连接阿拉伯字母对左侧 advance；`ui/text/layout_engine.rs` 与 `graphics/scene/scene_renderer/ui/sdf_render.rs` 继续消费同一 `justify_line_advances(...)`,没有把 kashida policy 复制到 UI/root renderer。新增 `text_justify_distributes_arabic_kashida_advances` 与 `sdf_draw_plan_expands_arabic_kashida_advances_for_justify` 锁定 UI resolved `glyph_advances` 与 SDF draw-plan advance resolver。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/text/layout/align.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/graphics/scene/scene_renderer/ui/sdf_render/tests.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-kashida-check --message-format short --color never` 通过(既有 warnings)。focused `cargo test -p zircon_runtime kashida --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-kashida-check --message-format short --color never -- --nocapture --test-threads=1` 在进入本切片测试前被无关当前 lib-test 编译错误阻断:`extend_pending_draws_for_mesh_instance.rs` 的 `PendingSkinnedGpuSource`/`SkinnedMeshJointPaletteUniform` 参数不匹配与参数数量错误；不计 focused test 通过。验证图 `docs/tests/runtime/text/runtime_text_kashida_justify_preview_20260630.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | kashida advance 首段已关闭到 shared owner；真正插入/移除 U+0640 tatweel glyph、与完整 HarfBuzz shaping/ligature 的重排、JustifyFlags 外露控制、native/SDF paragraph parity、full UAX#9 与竖排 layout 仍 pending。 |
| 2026-06-30 | LB-M3 horizontal overflow ellipsis interaction slice | runtime_text_lb_m3_horizontal_overflow_ellipsis_check_passed_focused_test_timeout | `ui/text/layout_engine.rs` 在高度裁剪省略之外新增横向可见行 overflow pass:当 `wrap=None` 或 protected line 的自然宽度超过 frame 且 `UiTextOverflow` 为 Ellipsis* 时,继续调用 `graphics/text/layout/overflow.rs::ellipsize_text(...)` 这个 shared overflow owner。`should_justify_line(...)` 改以 `line.ellipsized` 作为拒绝条件,避免 start/middle/end 省略后的非末行再被 Justify 拉回整行宽。新增 `horizontal_ellipsis_applies_to_nowrap_overwide_line` 与 `horizontal_start_ellipsis_non_last_line_is_not_justified` 锁定横向超宽省略与 Justify 交互。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-horizontal-overflow-check --message-format short --color never` 通过(既有 warnings)。focused `cargo test -p zircon_runtime horizontal_ --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-horizontal-overflow --message-format short --color never -- --nocapture --test-threads=1` 在 Windows lib-test compile/link 阶段 604s 超时,无 Rust diagnostics、未产出本轮 test binary,匹配验证进程已停止,不计通过。验证图 `docs/tests/runtime/text/runtime_text_horizontal_ellipsis_preview_20260630.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | 横向 overwide Ellipsis* 与 ellipsized non-last Justify re-stretch 交互已关闭到 UI layout + shared overflow owner；仍需 kashida justify、tab/overflow/shrink/clamp/ellipsis 更广交互矩阵、native/SDF paragraph parity、full UAX#9 与竖排 layout。 |
| 2026-06-30 | LB-M3 justify edge-space trim slice | runtime_text_lb_m3_justify_edge_space_trim_focused_tests_passed | `graphics/text/layout/align.rs` 在选择 justify opportunities 前先裁掉行首/行尾 word/ideographic spaces 的内容边界,只把内容区间内的 word spaces 与 CJK inter-character pairs 作为伸展点；`ui/text/layout_engine.rs` 与 SDF draw planning 继续复用同一 shared helper,不在 UI/root renderer 中复制 spacing policy。新增 `text_justify_trims_edge_spaces_before_distributing_gaps` 和 `sdf_draw_plan_trims_edge_spaces_for_justify`,分别锁定 resolved `glyph_advances` 与 SDF 首个可见 glyph 位置。 | scoped `rustfmt --check` 覆盖 `align.rs`、layout tests 与 SDF tests 通过；`cargo test -p zircon_runtime edge_spaces --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-justify-edge --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2(既有 warnings)。首次复跑曾被并行会话正在编辑的 `project_plugin_registry_staged_cache.rs` helper 符号阻断,确认该支撑文件随后已补齐后复跑通过。验证图 `docs/tests/runtime/text/runtime_text_justify_edge_space_preview_20260630.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | edge-space trim 关闭到 shared justify owner、UI layout 与 SDF draw-plan contract；仍需 kashida justify、tab 与 overflow/shrink/clamp/ellipsis 交互、native/SDF paragraph parity、full UAX#9 与竖排 layout。 |
| 2026-06-30 | LB-M3 tab stop first slice | runtime_text_lb_m3_tab_stop_focused_contract_passed | `graphics/text/layout/tab.rs` 成为 tab stop advance expansion child owner,按 `space_width * tab_size` 的 stop interval 将 `\t` 展开到下一个 stop；`UiResolvedStyle::tab_size` 默认 4.0, surface parser 接受 `text_tab_size`/`font.tab_size`/`tab_size`,并过滤非有限/非正值。`graphics/text/layout/measure.rs`、`ui/text/layout_engine.rs` 和 `UiTextStyleKey` 现在共同消费 tab-aware advances,cache key 记录 `tab_size_bits`,root layout file 只委托 shared layout owner,未新增 compat shim、旧路径 re-export、public graphics facade 或 target 内截图。 | scoped `rustfmt --check` 覆盖 touched Rust 文件通过；`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0630-tab-interface --message-format short --color never` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-tab-check-2 --message-format short --color never` 通过(既有 warnings)。直接运行 lib-test binary 的 exact tests 通过 3/3:`resolve_style_parses_text_tab_size_alias`、`text_tab_stop_advances_to_next_interval`、`style_key_encodes_tab_size_bits`。验证图 `docs/tests/runtime/text/runtime_text_tab_stops_preview_20260630.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。宽泛 `tab` filter 会匹配大量无关既有 tests,不作为本切片通过证据。 | tab stop 首段关闭到 parser/API/layout/cache/measurement contract；仍需 kashida justify、tab 与 overflow/shrink/clamp/ellipsis 交互、native/SDF paragraph parity、full UAX#9 与竖排 layout。 |
| 2026-06-30 | LB-M3/PF-M1 overflow cache key interface guard | runtime_text_overflow_cache_key_interface_check_passed_focused_test_blocked | `UiTextOverflow::ClampFontSize { min_px,max_px }` 已加入 public typography contract,但 `f32` 不能直接作为 `Eq` 字段进入 `UiTextStyleKey`。`ui/text/resolved_layout.rs` 新增私有 `UiTextOverflowKey`,把 `Clip`/ellipsis/shrink/clamp variants 投影为 cache-key-safe 表示,其中 clamp min/max 用 `to_bits()` 保存;这样 layout/cache key 可继续稳定比较,同时不把 public interface enum 退回整数或字符串。新增 focused unit test 锁定 clamp overflow 的 float-bit key。 | scoped rustfmt 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-components-0629-thumb-semantic-icon --message-format short --color never` 通过；`zircon_app --bin zircon_editor --no-default-features --features target-editor-host` build 通过。focused runtime unit test 在 default features 下被既有 test-only include path/E0282 阻断,no-default fresh target run 20min timeout,未计通过。 | 本切片只关闭 overflow public contract 与 cache-key `Eq` 的接口风险;仍需完整 overflow/shrink/clamp 交互测试、PF-M1 三级缓存 contract 和 runtime test harness 阻断修复。 |
| 2026-06-30 | LB-M3 clamp font-size first slice | runtime_text_lb_m3_clamp_font_size_focused_contract_passed | `UiTextOverflow::ClampFontSize { min_px,max_px }` 加入 neutral typography contract 与 surface parser aliases(`clamp_font_size`/`font-size-clamp`/`clamp`);parser 可从 `font.min_size`/`font.max_size` 或 flat `min_font_size`/`max_font_size` 读取边界。`ui/text/layout_engine/overflow_style.rs` 成为 adaptive overflow effective-style child owner,集中 shrink-to-fit 与 clamp font-size 的 bounded measurement search；`layout_engine.rs` 只委托该 owner,避免继续扩张 root layout file,并保留 shared measurement owner 作为唯一宽度来源。新增 layout、render-extract parser 与 public contract regressions；验证支撑还修复既有 `zircon_host_reflection_docs` bin module path,不再让 moved child files 被根 `mod args/error/run;` 阻塞 integration test 编译。 | scoped `rustfmt --check` 覆盖 touched Rust 文件通过；`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0630-clamp-interface --message-format short --color never` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-clamp-check --message-format short --color never` 通过(既有 warnings)。focused lib-test 初次在 Windows 编译中超时且不计通过；随后以 `--no-run` 产出 lib-test binary,直接运行 `E:\cargo-targets\zircon-runtime-text-0630-clamp-contract\debug\deps\zircon_runtime-370b12c13577a30d.exe clamp_font_size --nocapture --test-threads=1` 通过 2/2。复跑前曾暴露 `render_extract_parses_clamp_font_size_overflow_and_bounds` 的夹具错误:硬编码 frame width 低于 authored min font size 可放入的宽度；已改为用 shared measurement 计算最小可容纳宽度。integration contract 初跑暴露既有 bin module path 错误,路径修复后 `cargo test -p zircon_runtime --test runtime_text_clamp_font_size_contract --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0630-clamp-contract --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1。验证图 `docs/tests/runtime/text/runtime_text_clamp_font_size_preview_20260630.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。 | clamp 字号首段关闭到 layout/parser/API contract 代码层；仍需 tab stop、Justify/overflow/shrink/clamp 交互、native/SDF paragraph parity、full UAX#9 与竖排 layout。 |
| 2026-06-29 | LB-M3 shrink-to-fit first slice | runtime_text_lb_m3_shrink_to_fit_focused_tests_passed | `UiTextOverflow::ShrinkToFit` 加入 neutral typography contract 与 surface parser aliases(`shrink_to_fit`/`shrink-to-fit`/`shrink`/`fit`/`scale_down`);`layout_engine.rs` 在 wrap/layout 前按 shared measurement owner 计算 effective `UiResolvedStyle`,以 bounded search 降低 `font_size`/`line_height` 直到自然宽度落入 frame,并用 `MIN_TEXT_FONT_SIZE`、`SHRINK_TO_FIT_SEARCH_STEPS`、`SHRINK_TO_FIT_WIDTH_EPSILON` 命名常量避免裸魔数;新增 layout 与 render-extract parser regressions。 | `rustfmt --check zircon_runtime_interface/src/ui/surface/render/typography.rs zircon_runtime/src/ui/surface/render/resolve.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/tests/text_layout.rs zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/code_review_findings.rs zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/code_review_findings/typed_error_child_owners.rs` 通过;`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0629-shrink-interface --message-format short --color never` 通过;`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-word-check2 --message-format short --color never` 通过(既有 warnings);focused `cargo test -p zircon_runtime shrink_to_fit --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-word-check2 --message-format short --color never -- --nocapture` 通过 2/2；最终复核时同 command 在工具窗口内超时但产出 test binary，直接运行 `E:\cargo-targets\zircon-runtime-text-0629-overflow-word-check2\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe shrink_to_fit --nocapture --test-threads=1` 通过 2/2。验证图 `docs/tests/runtime/text/runtime_text_shrink_to_fit_preview_20260629.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0。验证过程中补齐优先结构守卫 `code_review_findings/typed_error_child_owners.rs`,使 focused lib-test 编译可穿过 Runtime 15 typed-error structure guard。 | shrink-to-fit 首段关闭;仍需 tab stop、Justify/overflow/shrink/clamp 交互、native/SDF paragraph parity、full UAX#9 与竖排 layout。 |
| 2026-06-29 | LB-M3 overflow focused validation + run remap coalescing | runtime_text_lb_m3_overflow_ellipsis_focused_tests_passed | `ui/text/layout_engine.rs::push_ellipsis_fragment` 现在在 owner 选出的 ellipsis ranges 回填后合并相邻的同 kind、同 direction、source/visual 连续 fragments，避免 Glyph-wrap 的逐字素 run 颗粒度泄漏到最终 rich-run 输出；`graphics/text/layout/overflow.rs` 仍是 end/start/middle/word-trim segment selection owner，未扩大 public DTO。 | 首次 focused `cargo test -p zircon_runtime ellipsis --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-word-check2 --message-format short --color never -- --nocapture` 通过 10/11，并暴露 `word_ellipsis_trims_partial_word_before_marker` run fragmentation；`rustfmt --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs` 通过；同一 `ellipsis` focused command 复跑通过 11/11，仅有既有 warnings；既有 middle/start/word ellipsis 验证图仍在 `docs/tests/runtime/text`，target 同名匹配为 0。 | overflow focused 绿跑风险已关闭；继续 clamp 字号、tab stop、Justify/overflow/shrink 交互、native/SDF paragraph parity、full UAX#9 与竖排 layout。 |
| 2026-06-29 | LB-M3 overflow owner + word-trim ellipsis slice | runtime_text_lb_m3_word_ellipsis_check_passed_focused_test_timeout | 在既有 `graphics/text/layout/overflow.rs` owner 上补齐 Godot `OVERRUN_TRIM_WORD_ELLIPSIS` 对齐的 word-trim 尾省略首段,新增 `EllipsisPlacement::EndWord`、`UiTextOverflow::EllipsisWord` neutral contract 与 surface parser aliases(`ellipsis_word`/`word_ellipsis`/`trim_word_ellipsis`/`truncate_word`);word-trim 只保留完整词边界,半词候选回退到前一个空白边界,首个词不能完整放下时只输出 ellipsis;`layout_engine.rs` 仍只消费 shared overflow segments 并回填 rich-run source ranges;新增 word partial trim、partial first word drop 与 render-extract parser regressions | `rustfmt --check zircon_runtime_interface/src/ui/surface/render/typography.rs zircon_runtime/src/graphics/text/layout/overflow.rs zircon_runtime/src/ui/surface/render/resolve.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/tests/text_layout.rs` 通过;`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-word-interface --message-format short --color never` 通过;`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-word-check2 --message-format short --color never` 通过(既有 warnings);静态覆盖扫描确认 `EllipsisWord` 处理点集中在 parser/layout/overflow/tests;focused `cargo test -p zircon_runtime word_ellipsis --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-word-test --message-format short --color never -- --nocapture` 604s 编译超时无 Rust diagnostics,匹配验证进程已停止,不计 test 通过;验证图 `docs/tests/runtime/text/runtime_text_word_ellipsis_preview_20260629.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0 | 本切片关闭 word-trim ellipsis 首段与 parser/owner 选择;仍需空闲编译通道补跑 overflow focused lib-tests,并继续 clamp 字号、tab stop、Justify 与 ellipsis/shrink 交互、full native/SDF paragraph overflow 对拍、竖排 layout |
| 2026-06-29 | LB-M3 overflow owner + start/end ellipsis slice | runtime_text_lb_m3_start_end_ellipsis_format_interface_check_passed_runtime_compile_deferred | 在既有 `graphics/text/layout/overflow.rs` owner 上补齐 `EllipsisPlacement::Start`,保留 `UiTextOverflow::Ellipsis` 作为尾省略并新增 `UiTextOverflow::EllipsisStart` neutral contract 与 surface parser aliases(`ellipsis_start`/`start_ellipsis`/`truncate_start`);`layout_engine.rs` 对 start/middle 这类需保留尾部文本的高度裁剪路径统一先合并被裁掉行,再由 shared overflow owner 选择 grapheme segments 并回填 rich-run source ranges;新增 end owner regression、start tail preservation regression 与 render-extract parser regression | `rustfmt --check zircon_runtime_interface/src/ui/surface/render/typography.rs zircon_runtime/src/graphics/text/layout/overflow.rs zircon_runtime/src/ui/surface/render/resolve.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/ui/tests/text_layout.rs` 通过;`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-start-interface --message-format short --color never` 通过;静态枚举覆盖扫描确认新增 `EllipsisStart` 处理点集中在 parser/layout/overflow/tests;runtime Cargo check/tests 因同时存在 Plan08 runtime lib-test 与 editor lib-test rustc/link 占用未抢跑,暂不声明 runtime Cargo 通过;验证图 `docs/tests/runtime/text/runtime_text_start_ellipsis_preview_20260629.png` 已检查可读,repo `target` 与 `E:\cargo-targets` 同名匹配为 0 | 本切片关闭首/尾省略的 owner 选择与 parser 首段;仍需空闲编译通道补跑 overflow focused lib-tests,并继续 clamp 字号、tab stop、Justify 与 ellipsis/shrink 交互、full native/SDF paragraph overflow 对拍、竖排 layout |
| 2026-06-29 | LB-M3 editor visual validation support gate | runtime_text_lb_m3_editor_visual_support_gate_rustfmt_passed | editor Asset Browser 视觉截图验证过程中暴露两个文本支撑层编译门问题: `ui/text/layout_engine.rs` 仍调用共享 grapheme iterator 但缺 `grapheme_indices` import; `graphics/text/layout/overflow.rs::EllipsisSegment` 只保存 byte ranges 却带无用 lifetime。修复为恢复共享 import、移除 index-only overflow segment lifetime,不改变 overflow 选择算法、不扩大 public DTO、不声明新的 LB-M3 功能完成。 | `rustfmt --edition 2021 --check zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/graphics/text/layout/overflow.rs` 通过;editor focused `asset_thumbnail_visual_uses_centered_semantic_preview_icon_and_corner_badge` 1/1 与 `asset_placeholder_visual` 8/8 通过,证明该支撑门不再阻塞 editor focused 编译。 | 这只是验证支撑修复;runtime middle ellipsis focused Cargo 绿跑、end/head ellipsis、shrink/clamp、tab stop、Justify 与 overflow 交互、full native/SDF paragraph overflow 对拍、竖排 layout 仍 pending。 |
| 2026-06-29 | LB-M3 overflow owner + middle ellipsis first slice | runtime_text_lb_m3_middle_ellipsis_format_interface_check_passed_runtime_compile_deferred | 新增 `graphics/text/layout/overflow.rs` 作为 LB-M3 overflow leaf owner,把 end/middle ellipsis 的 grapheme 选择算法从 `ui/text/layout_engine.rs` 抽出为纯段选择;`UiTextOverflow::EllipsisMiddle` 进入 interface enum 与 surface parser(`ellipsis_middle`/`middle_ellipsis`/`truncate_middle`);`layout_engine.rs` 在高度裁剪时对 middle ellipsis 先合并被裁掉行的尾部,再保留 head/tail 并输出独立 ellipsis run,`UiResolvedTextLine.ellipsized` 改由布局状态标记而不是只看文本末尾 | `rustfmt --check` 覆盖 interface typography、layout overflow/mod、surface resolve、layout engine/tests 与 render-extract test 通过;`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-interface --message-format short --color never` 通过;`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-check --message-format short --color never` 与 `cargo test -p zircon_runtime middle_ellipsis_keeps_head_and_tail_graphemes --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-overflow-test --message-format short --color never -- --nocapture` 在其他 active runtime/editor 编译占用期间超时且无 Rust diagnostics,未生成本轮 test binary;验证图 `docs/tests/runtime/text/runtime_text_middle_ellipsis_preview_20260629.png` 已检查可读 | 本切片只关闭 middle ellipsis 首段与 `overflow.rs` owner 落点;仍需 runtime focused Cargo 绿跑、end/start/middle/word focused regressions、shrink-to-fit、clamp 字号、tab stop、Justify 与 ellipsis/shrink 交互、full native/SDF paragraph overflow 对拍、竖排 layout |
| 2026-06-29 | LB-M3 justify word/CJK first slice | runtime_text_lb_m3_justify_word_cjk_check_passed | 新增 `graphics/text/layout/align.rs` 作为 LB-M3 justify 分配 leaf,对非末行把额外宽度分配到 word spaces 与 CJK inter-character gaps;`UiTextAlign::Justify` 进入 interface enum 与 surface parser,`layout_engine.rs` 将 justified non-final line 的 `glyph_advances`、`measured_width`、line frame width 展开到目标 frame;native glyphon 映射到 `Align::Justified`,SDF draw path 复用同一 `justify_line_advances(...)` 展开 word-gap 顶点 advance;render extract 增加 `text_align = "justify"` 入口覆盖 | `rustfmt --check` 覆盖 interface typography、surface resolve、layout align/mod/engine/tests、render native/SDF/tests 与 render-extract test 通过;`cargo check -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0629-justify-check --message-format short --color never` 通过(仅既有 warnings);`cargo test -p zircon_runtime text_justify_distributes_word_and_cjk_gaps --lib --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-text-0629-justify-layout-test --message-format short --color never -- --nocapture` 通过 1/1;`sdf_draw_plan_justifies_word_gaps_inside_frame` 通过 1/1;`render_extract_parses_justify_text_align_and_expands_non_final_line` 通过 1/1;验证图 `docs/tests/runtime/text/runtime_text_justify_distribution_preview_20260629.png` 已检查,repo `target` 与 `E:\cargo-targets` 同名匹配为 0 | 本切片只关闭 word-space + CJK 字间 justify 首段和 SDF 单行 word-gap 顶点展开;完整 kashida、edge-space trim、last-line/paragraph policy 细化、tab stop、ellipsis 首/中/尾、shrink/clamp、full native paragraph justification 对拍、竖排 layout 仍 pending |
| 2026-06-28 | LB-M3 Auto/Mixed first-strong base direction slice | runtime_text_lb_m3_first_strong_base_direction_check_passed | `ui/text/layout_engine.rs` 现在按 UAX#9 P2/P3 首强字符规则把 `Auto` 与暂存的 `Mixed` request 解析成具体 `LeftToRight` / `RightToLeft` paragraph base direction，再执行 logical Start/End 对齐；surface parser 与 interface enum 仍保留 `Mixed` 作为外部请求值，避免新增兼容 shim；`ui/tests/text_layout.rs` 的 mixed-direction extraction 断言同步为 resolved concrete base direction，同时保留 run-level visual order/source range 覆盖 | `rustfmt` 覆盖 touched layout/extraction files 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-first-strong-check --message-format short --color never` 通过(仅既有 warnings)；Cargo test harness 编译先超过工具窗口，但产出 test binary 后直接运行 `first_strong` 通过 4/4，`start_end` 通过 6/6，`mixed_direction`、`neutral_separator`、`rich_directional_ellipsis` 各通过 1/1；截图 `docs/tests/runtime/text/runtime_text_first_strong_direction_preview_20260628.png` 已视觉检查，repo `target` 下无对应文本验证图 | 这只关闭 paragraph base direction 的 first-strong 首段；完整 UAX#9 level run/isolate/mirror、script segmentation、justify、tab stop、shrink/clamp、ellipsis variants、vertical layout 与完整 LB-M3/LB-M4 仍 pending |
| 2026-06-28 | LB-M3 logical Start/End alignment first slice | runtime_text_lb_m3_rtl_start_end_align_check_passed | `UiTextAlign` 保留 `Start` / `End` 逻辑值，`ui/surface/render/resolve.rs` 不再把 `"start"` / `"end"` 提前折叠成 left/right；`ui/text/layout_engine.rs` 按 resolved line direction 解析 Start/End；`ScreenSpaceUiTextBatch` 携带 `text_direction`，native glyphon 与 SDF draw-plan 对齐 helper 都按方向映射 Start/End，避免 render backend 丢失 RTL 语义 | `rustfmt --check` 覆盖 interface typography、surface resolve、layout engine、render batch、native/SDF text files 与相关 tests 通过；`cargo check -p zircon_runtime_interface --lib --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-align-interface` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-align` 通过(仅既有 warnings)；`cargo test -p zircon_runtime start_end --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-align` 通过 3/3，覆盖 layout/native/SDF；`render_extract_preserves_logical_start_text_align` 通过 1/1；验证中先遇到结构守卫 `mesh_pipeline_variant_cache_owner.rs` 对 `gbuffer_record` moved-value 后续借用的编译阻塞，已改为借用拼接并用 `runtime_15_non_base_mesh_variant_cache_owner_is_wired` 通过 1/1 证明支撑层恢复；截图 `docs/tests/runtime/text/runtime_text_rtl_start_end_alignment_preview_20260628.png`，确认 repo `target` 下无对应文本验证图 | 这只关闭 Start/End 随显式 RTL base direction 的首段；`Mixed` 的真实 UAX#9 段落方向解析、justify、tab stop、shrink/clamp、ellipsis variants、vertical layout 与完整 LB-M3/LB-M4 仍 pending |
| 2026-06-28 | LB-M2 CJK open punctuation line-end first slice | runtime_text_lb_m2_cjk_open_punctuation_line_end_check_passed | `graphics/text/layout/kinsoku.rs` 增加行尾禁止开标点首段：当 UAX#14 chunk 以 `（` 等开标点结尾或开头时，把开标点与后续文本组成不可 glyph fallback 的 protected chunk；`line_break_chunks(...)` 继续只产 shared metadata，`ui/text/layout_engine.rs` 不持有开标点表 | RED 先证明 `"中（文"` 在单 glyph 宽 Word wrap 下会拆成 3 行；`rustfmt --check` 通过；`line_break_chunks_keep_cjk_open_punctuation_with_following_text` 通过，锁定 shared chunks 为 `"中"` / `"（文"` 且第二 chunk 不允许 glyph fallback；`text_wrap_cjk_kinsoku_no_trailing_open_punctuation` 通过，锁定 visual lines 为 `"中"` / `"（文"` 且无行以 `（` 结尾；既有 `text_wrap_cjk_kinsoku_no_leading_punctuation` 与 `word_wrap_uses_uax14_cjk_break_opportunities` 回归各通过 1/1；runtime lib check 通过(仅既有 warnings)；截图 `docs/tests/runtime/text/runtime_text_cjk_open_punctuation_preview_20260628.png`，确认 repo `target` 下无对应文本验证图 | 完整 LB-M2 仍需完整 greedy line breaker、完整 JIS/UAX line-head/line-tail 禁则表、squeeze/overhang policy generalization、tab/justify/shrink/ellipsis variants、竖排 layout 与更完整 glue 策略 |
| 2026-06-28 | LB-M2 long-word glyph fallback + NBSP glue first slice | runtime_text_lb_m2_long_word_nbsp_check_passed | `graphics/text/layout/line_break.rs` 继续作为 LB-M2 chunk owner，普通过宽 chunk 保持 `allow_glyph_fallback = true`，Word wrap 可退回 grapheme wrapping；含 U+00A0 的 chunk 标记 `allow_glyph_fallback = false`，让 NBSP glue group 在窄 frame 下保持同一不可断 run 并允许 overhang；UI 仍只消费 chunk metadata，不复制 NBSP 规则；验证还修复了下层 review guard `plugin_importer_dx/d13_importer_sdk.rs` 的 stale plan-status include 路径，避免无关支撑文件阻断 runtime lib-test 编译 | `rustfmt --check` 通过；`text_wrap_long_word_falls_back_to_glyph` 通过，锁定 `"abcd"` 在单 glyph 宽 Word wrap 下逐字分成 4 行；`word_wrap_keeps_non_breaking_space_group_together` 通过，锁定 `"a\u{00a0}b"` 在同宽度下保持 1 行且 measured width 超出 frame；runtime lib check 通过(仅既有 warnings)；截图 `docs/tests/runtime/text/runtime_text_long_word_nbsp_preview_20260628.png`，确认 repo `target` 下无对应文本验证图 | 完整 LB-M2 仍需完整 greedy line breaker、行尾禁则/open punctuation push-out、squeeze/overhang generalization、tab/justify/shrink/ellipsis variants、竖排 layout 与更完整 NBSP/ZWJ/emoji glue 策略 |
| 2026-06-28 | LB-M2 soft hyphen break suffix first slice | runtime_text_lb_m2_soft_hyphen_break_suffix_check_passed | `graphics/text/layout/line_break.rs` 为 `LineBreakChunk` 增加显式 `source_range` 与 `break_suffix` metadata，遇到 U+00AD 时把 soft hyphen 从 visual chunk text 移除，并把断行后显示的普通 `-` 绑定到源 soft-hyphen range；`ui/text/layout_engine.rs` 只在真实宽度换行时消费 pending break suffix，不在自然段落结束时显示 `-`；`layout_engine::source_subrange(...)` 与 `ui/text/hit_test.rs` 对 source/visual byte length 不一致的 run 采用保守映射，避免 `pre-` 的可见 hyphen 把 caret source offset 错算成普通 ASCII byte | `rustfmt --check` 通过；`text_wrap_soft_hyphen_inserts_hyphen` 通过，锁定 `"pre\u{00ad}fix"` 在窄 Word wrap 下输出 `"pre-"` / `"fix"` 且 visual text 不含 U+00AD；`text_hit_test_soft_hyphen_break_suffix_maps_to_source_hyphen` 通过，锁定行末命中映射到源 U+00AD 后方；runtime lib check 通过(仅既有 warnings)；soft-hyphen 改动后 `text_wrap_cjk_kinsoku_no_leading_punctuation` 与 `word_wrap_uses_uax14_cjk_break_opportunities` 回归各通过 1/1；截图 `docs/tests/runtime/text/runtime_text_soft_hyphen_preview_20260628.png`，确认 repo `target` 下无对应文本验证图 | 完整 LB-M2 仍需完整贪心断行 owner、行尾禁则/open punctuation push-out、不可断空白、long-word fallback 合同、squeeze/overhang generalization，以及后续 align/overflow/vertical owner |
| 2026-06-28 | LB-M2 CJK kinsoku line-start punctuation first slice | runtime_text_lb_m2_cjk_kinsoku_line_start_check_passed | 新增 `graphics/text/layout/kinsoku.rs`，在共享 layout 层处理行首禁则首段；`LineBreakChunk.allow_glyph_fallback` 让 UI Word wrap 遵循 runtime chunk metadata，而不是在 UI 层复制 CJK 标点表；`line_break_chunks(...)` 对关闭标点 suffix/leading chunk 关闭 glyph fallback，允许 `文。` 作为过宽 kinsoku chunk 保持在同一行；新增 focused test 锁定 `"中文。"` 窄宽度下分成 `"中"` / `"文。"`，无 line 以 `。` 开头 | RED 先失败为 3 行；`rustfmt --check zircon_runtime/src/graphics/text/layout/mod.rs zircon_runtime/src/graphics/text/layout/line_break.rs zircon_runtime/src/graphics/text/layout/kinsoku.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs zircon_runtime/src/tests/runtime_absorption/code_review_findings/plugin_importer_dx.rs` 通过；`cargo test -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-check --message-format short --color never text_wrap_cjk_kinsoku_no_leading_punctuation` 通过 1/1；`word_wrap_uses_uax14_cjk_break_opportunities` 通过 1/1；`text_shape_` 通过 6/6；runtime lib check 通过(仅既有 warnings)；截图 `docs/tests/runtime/text/runtime_text_cjk_kinsoku_preview_20260628.png` | 完整 `kinsoku.rs` 仍需行尾禁则、open punctuation push-out、squeeze/overhang policy generalization、软连字符、不可断空白、long-word fallback 合同和后续 align/overflow/vertical owner |
| 2026-06-28 | LB-M2 消费首段: UAX#14 chunks drive Word wrap for CJK | runtime_text_lb_m2_uax14_word_wrap_cjk_check_passed | 新增 `graphics/text/layout/line_break.rs`，以 `ShapedGlyphRun` cluster soft-break flags 为唯一断点来源生成 `line_break_chunks(...)`；`ui/text/layout_engine.rs` 的 Word wrap 消费该共享 chunk 流而不是只按 ASCII space 分块；新增 CJK no-space regression，证明 `"中文"` 在窄 frame 下按 UAX#14 机会断成两行 | `rustfmt --check zircon_runtime/src/graphics/text/layout/mod.rs zircon_runtime/src/graphics/text/layout/line_break.rs zircon_runtime/src/ui/text/layout_engine.rs zircon_runtime/src/ui/text/layout_engine/tests.rs` 通过；`cargo test -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-check --message-format short --color never word_wrap_uses_uax14_cjk_break_opportunities` 通过 1/1；`cargo check -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-check --message-format short --color never` 通过(仅既有 warnings)；截图证据仍在 `docs/tests/runtime/text/runtime_text_shared_metrics_preview_20260628.png`，target 路径检查无文本验证图 | 完整 LB-M2 仍需把 chunk 流升级为贪心断行 owner、CJK 行首尾禁则、soft hyphen 插入、long-word glyph fallback 与不可断空白策略；LB-M3/LB-M4 的 justify/overflow/vertical layout 仍 pending |
| 2026-06-28 | LB-M2 数据面首段: UAX#14 break opportunity flags from shaped run | runtime_text_sh_m1_uax14_break_flags_focused_tests_passed | `graphics/text/shaping/line_break.rs` 先把 UAX#14 断点机会投影到 `ShapedGlyphClusterFlags`，为后续 `graphics/text/layout/line_break.rs` 的贪心断行、WordSmart 与 CJK kinsoku 提供真实 cluster 数据；当前 UI 仍未切到完整 LB-M2 line breaker | `rustfmt --check` 通过；`cargo check -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-check --message-format short --color never` 通过(仅既有 warnings)；`cargo test -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-check --message-format short --color never text_shape_` 通过 6/6，其中 word-space 与 CJK soft-break focused tests 覆盖 LB-M2 输入数据 | 完整 LB-M2 仍需 `layout/line_break.rs` 贪心断行、CJK 行首尾禁则、long-word glyph fallback 与 soft hyphen 插入；LB-M3/LB-M4 justify/overflow/vertical layout 仍 pending |
| 2026-06-28 | SH-M1 owner slice: cosmic-backed `ShapedGlyphRun` contract and isolated shaping owner | runtime_text_sh_m1_shaping_owner_core_check_passed_focused_libtest_blocked | Added neutral `core/framework/render/text/{shaped_run.rs,shaping_service.rs}` contracts and `graphics/text/shaping/{mod.rs,cosmic.rs}` owner; `cosmic.rs` is now the isolated glyphon/cosmic-text Buffer/LayoutGlyph projection point and emits glyph id, source_range, visual_range, advance, baseline, direction, cluster flags, and rotation contract data; `graphics/text/layout/measure.rs` now derives line width/line metrics/per-grapheme advances from `ShapedGlyphRun` instead of importing backend text types directly | scoped rustfmt passed; `cargo check -q -p zircon_runtime --lib --no-default-features --locked --target-dir E:\cargo-targets\zircon-runtime-text-0628-check` passed with existing warnings only; focused `cargo test -q -p zircon_runtime text_shape_ --lib --no-default-features --locked` compiles past old importer include guards but is blocked by unrelated camera-loop lib-test closure signature errors; screenshot evidence remains `docs/tests/runtime/text/runtime_text_shared_metrics_preview_20260628.png` and target image checks are false | Actual fallback-selected `FontFaceId` remains pending; full UAX#9 isolate/mirroring, script segmentation, UAX#14 break data, CJK kinsoku, vertical metrics/rotation policy beyond contract fields, and UI hard cutover from `visual_order.rs` remain future SH/LB slices |
| 2026-06-28 | LB-M1 DTO 首段: measured grapheme advances exported to render layout | runtime_text_sh_lb_m1_shaped_glyph_advances_interface_check_passed_runtime_check_timeout | `ui/text/layout_engine.rs` 将 shared backend `measured_grapheme_widths(...)` 写入 `UiResolvedTextLine.glyph_advances`；`UiShapedText::from_resolved_layout(...)`、rich text paint runs 与 editable decoration geometry 消费该数组生成非等宽 glyph frames/paint frames；新增 focused tests 覆盖 `"Wi"` 非均匀 advance 与 shaped glyph frame projection | scoped rustfmt 通过；`zircon_runtime_interface --lib` 与 `zircon_runtime_interface --tests` Cargo check 通过；runtime no-default check 244s 编译超时无 Rust diagnostics，匹配验证进程已停止；截图路径检查沿用 `docs/tests/runtime/text/runtime_text_shared_metrics_preview_20260628.png` | 完整 LB-M1 仍需 `ShapedGlyphRun` 上的 UE-style subrange measure/kerning semantics；LB-M2/LB-M3/LB-M4 的 UAX#14、CJK 禁则、justify、shrink、tab、竖排列布局仍 pending |
| 2026-06-27 | 计划建立 | planned | 真实度量(UE 对齐)+ UAX#14 换行 + CJK 禁则 + justify + 竖排布局路线 | 文档 | LB-M1 度量=绘制(editor_layout/17 G1);喂 render/14 顶点生成 |
| 2026-06-28 | LB-M1 首段:真实 glyph metrics 驱动 UI 度量/换行/缓存/命中 | runtime_text_shared_metrics_owner_core_check_passed_focused_test_timeout_visual_evidence | 新增 `graphics/text/layout/measure.rs` 的 line width、line metrics、visual prefix width、grapheme widths、width bucket;`ui/text/layout_engine.rs` 用共享宽度做 wrap/align/ellipsis/baseline,`measure_cache.rs` 用共享 width bucket,`hit_test.rs` 用 measured grapheme midpoint;触达路径移除 fixed half-em 等宽公式 | `rustfmt` 通过;静态扫描确认触达文本路径无旧等宽公式;focused tests 更新 `WWW > iii`、combining-mark ellipsis、measured cache bucket、measured hit-test points;`cargo check -p zircon_runtime --lib --no-default-features --locked --jobs 1` 通过(仅既有 warning);截图 `docs/tests/runtime/text/runtime_text_shared_metrics_preview_20260628.png`;focused Cargo test 超时无 diagnostics | 完整 LB-M1 仍需基于 `ShapedGlyphRun` 的子范围度量/kerning 语义;LB-M2/LB-M3/LB-M4 的 UAX#14、CJK 禁则、justify、shrink、竖排布局仍 pending |

