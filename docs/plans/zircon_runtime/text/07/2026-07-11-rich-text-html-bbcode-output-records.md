---
related_code:
  - zircon_runtime/src/graphics/mod.rs
  - zircon_runtime/src/text/model/rich.rs
  - zircon_runtime/src/text/layout/rich.rs
  - zircon_runtime/src/text/layout/rich_vertical.rs
  - zircon_runtime/src/ui/text/layout_engine/rich_inline.rs
  - zircon_runtime/src/ui/text/layout_engine/rich_inline_vertical.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/render/rich_text.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/image.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/shaders/screen_space_ui_image.wgsl
  - zircon_runtime/src/graphics/scene/resources/ui_texture.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_ensure_scene_resources.rs
  - zircon_runtime_interface/src/ui/surface/render/typography.rs
  - zircon_runtime/src/text/rich/mod.rs
  - zircon_runtime/src/text/rich/parser.rs
  - zircon_runtime/src/text/rich/bbcode.rs
  - zircon_runtime/src/text/rich/bbcode_table.rs
  - zircon_runtime/src/text/rich/html_subset.rs
  - zircon_runtime/src/text/rich/decorator.rs
  - zircon_runtime/src/text/rich/emoji_shortcode.rs
  - zircon_runtime/src/text/rich/inline_decorators.rs
  - zircon_runtime/src/text/rich/tests.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/render.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/render/tests/rich_inline.rs
  - zircon_runtime/src/ui/text/rich_text.rs
  - zircon_runtime/src/ui/text/layout_engine/rich_table.rs
  - zircon_runtime/src/ui/text/rich_text/link_hit.rs
  - zircon_runtime/src/ui/surface/input/rich_link.rs
  - zircon_runtime/src/ui/surface/input/effect/link.rs
  - zircon_runtime/src/ui/surface/input/effect/host_request.rs
  - zircon_runtime_interface/src/ui/dispatch/input/effect.rs
  - zircon_runtime_interface/src/ui/dispatch/input/result.rs
  - zircon_runtime/src/ui/text/layout_engine/tests/grapheme.rs
  - zircon_runtime/src/ui/text/layout_engine/tests/rich_inline.rs
  - zircon_runtime/src/ui/text/layout_engine/tests/bidi.rs
  - zircon_runtime/src/ui/text/layout_engine/tests/overflow.rs
  - zircon_runtime/tests/runtime_text_multilingual_product_framebuffer.rs
plan_sources:
  - docs/plans/zircon_runtime/text/07-rich-text-html-bbcode.md
  - docs/plans/zircon_runtime/text/02-shaping-unicode-and-bidi.md
  - docs/plans/zircon_runtime/text/03-line-breaking-measure-and-layout.md
  - docs/plans/engine-code-structure-convention.md
  - docs/plans/engine-code-review-findings-2026-06.md
tests:
  - cargo test -p zircon_runtime --lib text_rich --no-default-features --features target-client --locked --offline --jobs 1 -- --test-threads=1
  - direct current-source zircon_runtime test binary ui::text::layout_engine::tests:: --test-threads=1
  - cargo check -p zircon_runtime --lib --no-default-features --features ui --locked --offline --jobs 1 --target-dir F:\cargo-targets\zircon-ui-state-priority-0711
  - cargo test -p zircon_runtime --test runtime_text_multilingual_product_framebuffer --no-default-features --features target-client --offline --jobs 1 --target-dir F:\cargo-targets\zircon-ui-state-priority-0711 export_runtime_multilingual_text_product_framebuffer_png -- --ignored --exact --nocapture
doc_type: plan-output-records
status: in_progress
---

# 07 富文本（BBCode + HTML 子集）产出记录归档

> 来源：[`07-rich-text-html-bbcode.md`](../07-rich-text-html-bbcode.md) 的 `## 8. 状态与产出记录`。

## 8. 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证 | 后续 |
|------|-------------|------|------|------|------|
| 2026-07-11 | RT-M1 解析框架 + BBCode 核心标签 | runtime_text_rt_m1_bbcode_markdown_grapheme_boundary_passed | 新增中立 `RichTextFormat`、`StyleOverride`、`StyledRun`、段落/内联/link DTO；`graphics/text/rich/{parser,bbcode,decorator}.rs` 承接统一解析与装饰器注册。BBCode 支持嵌套 b/i/u/s/color/bgcolor/size/font/code 并扁平化；既有 Markdown 三标记硬切到同一 owner。解析后以 Unicode grapheme 对齐 run，簇内标记按簇首样式裁决；UI adapter、布局、预热消费剥标记文本，不再对 raw markup 计量。三条旧布局断言同步改为整簇 run，不保留半簇兼容路径。 | 当前源码完整重建后 `text_rich` 首轮 5/5；加入 RT-M2 后 9/9，加入 RT-M3 解析契约与安全负例后总计 13/13。当前源码同一测试二进制的 layout engine 67/67，覆盖组合标记、RTL mark 与省略号整簇 run。scoped rustfmt/diff-check 通过。 | 段落 override/BBCode center/right 与自定义装饰器公开注册仍 open。 |
| 2026-07-11 | RT-M2 HTML 受控子集 | runtime_text_rt_m2_html_whitelist_entities_break_style_passed | 新增 folder-backed `graphics/text/rich/html_subset.rs`，实现引号感知的轻量 tag/attribute tokenizer、白名单 b/i/u/s/span/font/br、受控 span style 属性 color/font-size/font-weight/font-style/text-decoration、font face/size/color、命名与十六/十进制 numeric entity 解码。未知标签/属性只丢标签保留文本；解码后的 `<...>` 不二次解析；`background:url(...)` 等非白名单 CSS 被忽略；`<br>`/`<br/>` 投影为强制换行。没有引入完整 HTML/CSS 引擎、网络请求或脚本执行面。 | TDD 红态为 HTML 4/4 失败（旧实现按 plain 原样返回）；实现后当前源码 Cargo exact 4/4；最终聚合在 12/12 中继续通过。HTML 重建用时 17m58s，测试 0.02s；现有单体测试警告基线 538。 | HTML 表格、任意 CSS 和网络加载不进入 V1。 |
| 2026-07-11 | RT-M3 inline image + hyperlink 解析契约 | runtime_text_rt_m3_inline_link_parse_contract_passed_layout_metric_open | HTML `<img>` 与 BBCode `[img=...]` 产 U+FFFC 占位及 `InlineObjectRef::Image`；HTML `<a href>` 与 BBCode `[url=...]` 产 `LinkRef`、剥标记命中 byte range、默认下划线和链接色。资源只接受 `res://`、`lib://`、`package://`、`builtin://` 或安全相对路径，`http(s)`/`mem`/越界路径拒绝；链接 metadata 跨嵌套 style 保留。 | TDD 首轮编译红确认 `inline` metric owner 不存在；实现解析契约后，当前源码 target-client 聚焦 `text_rich` 13/13（含 network/root-escape 负例），UI-feature production check 退出 0（2m28s），layout engine 67/67。 | inline parser 合同关闭；03 行度量与渲染接线继续由下一行记录。 |
| 2026-07-11 | RT-M3 inline 03 行度量与 placement 首段 | runtime_text_rt_m3_inline_layout_metrics_15_of_15_passed_render_hit_open | 中立层新增 `LayoutItem::{Text,Inline}`、`LaidOutLine` 与 `LaidOutText`；`graphics/text/layout/rich.rs` 作为 03 行度量 owner，统一计算 image/widget/icon 的 advance、尺寸、Baseline/Center/Top/Bottom 等效 ascent/descent 与最终 origin。文本 run 随扩大后的行 baseline 下移；line `item_range` 取实际产出 item 数而非 parser run 数，非法 range 不会制造越界索引。 | 首轮 baseline 用例准确以 descent 期望不一致失败 0/1；按共享字体实际 baseline/line-height 修正门禁并补齐文本垂直定位后，当前源码 target-client exact 1/1（构建总计 684.4s、测试 23.30s）。同一 11:27 binary 的 `text_rich` 15/15、layout engine 67/67；scoped rustfmt/diff-check 通过。 | 当前 owner 尚未接入 `UiResolvedTextLayout`/`UiTextPaint` 与 scene renderer image batch，链接 hit-test 也尚未消费 `LinkRef`；RT-M3 与 Text07 保持 `in_progress`，不把 DTO/metric 测试冒充产品渲染闭环。 |
| 2026-07-11 | RT-M3 显式格式 + UI/renderer 接线实施切片 | runtime_text_rt_m3_explicit_format_ui_renderer_wired_production_check_passed_test_build_external_blocked | `UiResolvedStyle` 从 Markdown 专用 `rich_text: bool` 硬切为 `UiRichTextFormat::{Plain,Markdown,BbCode,Html}`，surface metadata 使用 `rich_text_format`；UI adapter 保留 style/inline/link/paragraph/rich DTO。无换行、水平、nowrap 的 inline 文本通过 `ui/text/layout_engine/rich_inline.rs` 投影到 `UiResolvedTextLayout`，glyph advances 中 U+FFFC 的 advance 为对象宽，caret/hit-test 继续消费同一 advance。03 owner 同时应用 run font size/weight/family。scene renderer leaf 识别 inline run、跳过 U+FFFC glyph batch，按共享 `LaidOutText` origin 提交 inline quad，并消费颜色、背景、字体、字号、weight、链接下划线。 | `cargo check -p zircon_runtime_interface --locked --offline --jobs 1` 通过（71.7s），interface lib-test no-run 通过（205.1s），随后同一 current binary 完整 203/203（0.72s）；当前 `cargo check -p zircon_runtime --lib --no-default-features --features ui --locked --offline --jobs 1` 在补齐并发 BaseScene integrated-volumetric 新参数后通过（87.5s，439 个既有 warning）。target-client lib-test 已不再报告 Text07/UiRichTextFormat/inline 接线编译错误，但被并发 Scene test 缺 `SceneError` 导入及 Environment IBL 测试仍调用已移除 `pmrem_*` 方法的外部 E0433/E0599 阻断；未越权修改这些 owners，也不声明新测试 green。 | 新增的 resolved-layout、renderer inline、style override 测试需在外部 owner 收敛并重新生成 test binary 后执行。当前 renderer 先提交可见 inline 占位 quad；真实资源纹理采样、LinkRef 激活 host request、多行/RTL/vertical/ellipsis/wrap 仍 open，因此 RT-M3/Text07 保持 `in_progress`。 |
| 2026-07-11 | RT-M3 BBCode 段落对齐共享契约 | runtime_text_rt_m3_bbcode_paragraph_alignment_production_and_test_compile_passed_external_integration_blocked | `[center]`/`[right]` 不再只剥标签：parser 以剥标记 byte range 产 `ParagraphOverride`，闭合/未闭合标签均收束区间；UI layout 按每行 source start 选择段落 align，center/right 覆盖多行 line frame，inline 单行入口也消费同一段落 owner。嵌套块级对齐采用最外层有效覆盖，不制造重叠 override。 | 新增 parser `text_rich_bbcode_block_alignment_emits_paragraph_overrides` 与 UI `bbcode_paragraph_alignment_reaches_resolved_line_frames`。scoped rustfmt/diff-check 通过；`cargo check -p zircon_runtime --lib --no-default-features --features ui` 通过（43.14s，439 个既有 warning）。Environment IBL 的旧 `pmrem_*` 已由其 owner 修复；`cargo check -p zircon_runtime --lib --tests --no-default-features --features target-client` 在 233.1s 内编译通过 Runtime library（423 个既有 warning），随后仅在无关 integration test `runtime_plugin_world_extensions_contract` 缺 `World::run_native_scene_systems_for_stage` 时 E0599，未出现 Text07 诊断。target-client lib-test 链接约 20 分钟无新诊断/新 binary 后停止；新增测试仍不声明 green。 | 在可用的 current-source lib-test binary 产出后执行新增两条 exact tests；真实纹理、link 激活、inline 多行/wrap/RTL/vertical/ellipsis 与公开自定义 decorator 仍 open。 |
| 2026-07-11 | RT-M3 LinkRef 命中与 host request 激活边界 | runtime_text_rt_m3_link_hit_host_request_wired_production_checks_passed_runtime_tests_deferred | 新增 `ui/text/rich_text/link_hit.rs`，用 resolved layout caret/affinity 解析 LinkRef，避免 run 末尾 trailing half 被误判为链接外；`ui/surface/input/rich_link.rs` 只在 primary release、release-inside-pressed、click-target 一致时生成 `RequestLinkActivation`。effect owner 重新验证输入 owner 与 `res/lib/package/builtin` scheme，最终只产 `UiDispatchHostRequestKind::ActivateLink`，Runtime 不执行浏览器/网络动作。接口 DTO 与 serde round-trip 同步接线；无 UI-local 第二解析器或网络兼容通道。 | `cargo check -p zircon_runtime_interface` 通过（134.7s）；`cargo check -p zircon_runtime --lib --no-default-features --features ui` 通过（126.7s，440 个既有 warning）；完全限定 interface exact `tests::dispatch_reply_contracts::dispatch_reply_rich_link_activation_roundtrips_with_host_request_kind` 通过 1/1。Runtime 新增 link-hit / pointer default-action / scheme rejection 测试已写入，按里程碑策略等待 current-source Runtime lib-test binary；未把 production check 冒充测试 green。 | 下一个 RT-M3 源码切片处理真实 inline image texture sampling；随后进入 milestone testing stage 执行 Text07 聚合测试与产品 framebuffer。 |
| 2026-07-11 | RT-M3 真实 inline image 资源采样 + 产品 framebuffer | runtime_text_rt_m3_inline_texture_sampling_product_framebuffer_1_of_1_passed | 新增 `resources/ui_texture.rs` 作为 locator ID→导入 UUID ID 与 D2 单层纹理裁决 owner；`ensure_scene_resources` 继续由唯一 `ResourceStreamer` 准备/修订 GPU 纹理。`scene_renderer/ui/image.rs` 与专用 WGSL 只建立 UI binding、UV/tint 顶点和采样 draw；富文本 renderer 不再为 image 写实色 placeholder quad。普通 `UiVisualAssetRef::Image` 与 rich image 共用该采样 owner。产品测试建立临时项目并导入红/绿/蓝/黄四象限 PNG，fallback 为纯白，三主色像素门禁因此能区分真实采样与降级路径。 | 稳定 UI-feature `cargo check` 236.1s 通过（442 个既有 warning）。首次独立目标因 F 盘空间耗尽准确失败并删除本会话 11.96GB 缓存；复用既有稳定缓存后产品构建 21m00s，ignored exact WGPU 用例首次执行 121.72s、1/1 通过，最终直接二进制复验 108.07s、1/1 再通过。PNG 1080×620、100,378 bytes、SHA256 `0220C44E0F41F0B6B8BB52CB6EA8A800E298C4A28D6B671374ED47C63B51CB27`，两次输出哈希一致；视觉检查显示顶行四象限贴图及 multilingual/RTL/emoji/SDF/VerticalRl 均可见；repo target 与 `F:\cargo-targets` 同名计数均 0。target-client lib-test 编译穿过 Text07，期间同步修复 `precise_scroll` fixture 与 effect variant matrix，最终为优先产品门禁停止超大链接，不声明该二进制行为测试 green。 | image 真实采样门槛关闭；Text07 保持 `in_progress`，后续处理 inline wrap/multiline/RTL/vertical/ellipsis、icon/widget/emoji shortcode、BBCode V1 余项和公开 decorator。 |
| 2026-07-11 | RT-M3 inline 强制换行多行布局 | runtime_text_rt_m3_inline_forced_newline_multiline_2_of_2_passed | `graphics/text/layout/rich.rs` 新增中立多行 owner：按 mandatory newline 切出 source range，逐行计算富文本/inline ascent、descent、advance、item range 与累计 y，并把局部 run 索引映回原 parser run。UI 投影不再因 `\n` 退回把 U+FFFC 当普通字形的旧路径；每行保留自身 source/visual range、run、glyph advance、baseline 和段落对齐。renderer 的 inline frame 按对象 range 选择对应 resolved line，第二行图片不再错误锚定第一行。 | 当前源码稳定 `--features ui` production check 469.7s、退出 0（442 个既有 warning）。`ui`-only lib-test 暴露 34 个既有 feature-gate 错误，改用计划规定的 target-client 后完整 lib-test no-run 24m49s、退出 0并产 current binary。随后 exact `html_inline_image_after_forced_newline_keeps_rich_line_metrics` 1/1（32.79s）与中立 `text_rich_forced_lines_preserve_inline_metrics_and_original_run_indices` 1/1（32.57s）均通过；scoped rustfmt/diff-check 通过。 | forced-newline 多行门槛关闭；软 wrap、RTL/Mixed、VerticalRl 与 ellipsis 的 inline 融合仍 open，Text07 保持 `in_progress`。 |
| 2026-07-11 | RT-M3 inline glyph 软换行 | runtime_text_rt_m3_inline_glyph_soft_wrap_3_of_3_passed | 中立 rich layout 按 grapheme 计算 glyph wrap 范围；普通文字使用 run override 后的共享测量，U+FFFC 使用 inline box advance，因此超宽图片作为不可拆 grapheme 独占一行而不会被测成替代字形。UI glyph-wrap 分支消费同一 source ranges 与多行度量；renderer placement 改从 resolved line 的 glyph advances 计算对象 x，避免 wrapped line 使用整段未换行 origin。Word/WordSmart 仍显式不进入该路径。 | 更新后稳定 `--features ui` production check 208s、退出 0（442 个既有 warning）。target-client lib-test 首次链接因 F 盘仅余约 1MB 失败；确认目标后删除本会话无关且陈旧的 `F:\cargo-targets\zircon-frameworks03-domain-matrix-0711` 7.17GB，重试 no-run 17m39s、退出 0并产 current binary。exact glyph-wrap 1/1（28.19s）、forced-newline 回归 1/1（31.80s）、中立多行回归 1/1（33.13s）均通过。 | Glyph wrap 门槛关闭；Word/WordSmart、RTL/Mixed、VerticalRl 与 ellipsis 仍 open，Text07 保持 `in_progress`。 |
| 2026-07-11 | RT-M3 inline Word / WordSmart 软换行 + 当前源码产品证据 | runtime_text_rt_m3_inline_word_wordsmart_product_framebuffer_1_of_1_passed_unit_relink_external_blocked | `graphics/text/layout/rich.rs` 把 UAX#14 word chunks 与 WordSmart 超宽 chunk 的 grapheme fallback 收口到中立 rich owner；run 样式、inline U+FFFC advance、行首空格裁剪和 parser source ranges 使用同一测量通道。UI Word/WordSmart 不再退回 plain layout；renderer 继续从 resolved line advances 放置对象。产品场景同时绘制 nowrap、Word、WordSmart 三行真实导入四象限 PNG，并扩大 viewport 保留 multilingual/RTL/emoji/SDF/VerticalRl 既有行。 | 当前源码 target-client 产品构建 16m13s；ignored exact WGPU 用例执行 119.42s，1/1 通过。三行各自的红/绿/蓝主色像素扫描通过，证明对象经真实纹理 sampling 且参与对应 wrap；PNG 1080×850、122,562 bytes、SHA256 `2DE7CF6D892E73AEFA519B984CE9491ACF266ECCAA80343DB23EDFCE8FCB51F5`，人工复核可见 Word 行对象换到下一行、WordSmart 行对象后保留 suffix。新增 unit exact 已写入，但当前重新链接首先被 Runtime 01 已登记的 `wgpu-hal`/`gpu-allocator` Windows 类型版本分裂阻断，因此不计 green。scoped rustfmt/diff-check 待本记录更新后统一执行。 | Word/WordSmart 产品门槛关闭；unit exact 待 Runtime 01 依赖图修复后补跑。下一切片进入 inline RTL/Mixed；VerticalRl、ellipsis、icon/widget/emoji shortcode、BBCode V1 余项和公开 decorator 仍 open。 |
| 2026-07-11 | RT-M3 inline RTL / Mixed 共享 UAX#9 实施切片 | runtime_text_rt_m3_inline_bidi_shared_visual_order_implemented_static_passed_cargo_external_blocked | rich-inline 入口不再拒绝 `RightToLeft/Mixed`；每条中立 rich line 构造逻辑 source runs 后复用 Text02 `visual_order` owner。共享 owner 同步按同一 UAX#9 visual-index→logical-index 映射重排 glyph advances，避免视觉文本与 hit/placement advance 分离；inline renderer 用 resolved run 的 source→visual range 定位 U+FFFC，而非把逻辑 byte offset 当成视觉前缀。新增 shared advance、Mixed、RTL 与 renderer visual-offset 四个回归，不创建 rich-only bidi 分支。 | scoped rustfmt 与 diff-check 通过。当前 locked target-client production check 在进入 Zircon 源码前复现 Runtime 01 open handoff：`wgpu-hal 29.0.4`/`gpu-allocator 0.28` 的 `windows 0.54/0.62` COM 类型分裂，E0308×1/E0277×9；因此本切片标记 implemented/static-passed，不能宣称 compile 或 tests green。 | Runtime 01 依赖图收敛后按 support-first 顺序执行 shared advance exact、Mixed/RTL exact、renderer exact，随后追加真实 RTL inline image WGPU 行与截图证据。VerticalRl/ellipsis 仍 open。 |
| 2026-07-11 | RT-M3 inline RTL / Mixed 当前依赖图复验 | runtime_text_rt_m3_inline_bidi_focused_7_of_7_passed_wgpu_sampling_regression_open | 当前依赖图可编译；共享 UAX#9 advance 重排、Mixed/RTL rich-inline 与 renderer visual-range placement 进入 current-source test binary。追加 RTL 产品行及其四象限纹理扫描；renderer 同时补充当 `UiTextPaint` 未暴露 U+FFFC run 时按 parsed inline + resolved layout 恢复 image batch，避免纯 inline/合并 paint run 丢对象。 | `cargo check -p zircon_runtime --lib ... --features target-client --locked --offline` 16m17s 通过。direct current-source binary exact：shared advance、Mixed、RTL、renderer visual-offset、Word、WordSmart、Glyph 各 1/1，共 7/7。新增 renderer recovery lib-test 的重建被并发 asset registry `AssetRegistryError` 缺 `PartialEq` 的 9 个外部 E0369 阻断。WGPU 产品二进制两次均在首行 checker scan 稳定失败 `[0,0,0]`，先于新增 RTL 断言；未写出/覆盖 docs 截图。 | 继续由共享 UI image resource/renderer owner 定位当前纹理 sampling 回归，恢复既有 nowrap/Word/WordSmart 产品门槛后再接受 RTL WGPU 行与新截图；VerticalRl/ellipsis 仍 open。 |
| 2026-07-11 | RT-M3 inline RTL 产品 framebuffer 收口 | runtime_text_rt_m3_inline_bidi_wgpu_texture_1_of_1_passed | 根因在共享视觉排序 owner：相邻 visual fragment 只按 kind/direction/source 连续性合并，导致 HTML image 的 U+FFFC 与前后 plain styled run 合并，renderer 无法按 parser run range 解析 inline。`visual_order.rs` 现在为 token/fragment 保留原始 run owner，仅允许同 owner 碎片合并；inline 占位、样式与 link 边界均继续留在 resolved runs。删除临时的 renderer paint-run 恢复通道，不保留第二套定位策略。 | 当前源码 target-client 产品测试重建 17m19s、退出 0；ignored exact `export_runtime_multilingual_text_product_framebuffer_png` 117.55s、1/1。nowrap、Word、WordSmart、RTL 四组导入四象限纹理门禁全部通过。PNG 1080×850、127,136 bytes、SHA256 `6DB26B605256EE3A5B5B8C910DB3940C86CFDE53EC205CE5C16F1A725F10E526`；人工检查可见底部 `RTL rich` 行的四象限纹理与 Hebrew 视觉序，`D:\cargo-targets\zircon-text07-product-0711` 同名 PNG 计数 0；scoped rustfmt/diff-check 通过。 | RTL/Mixed 产品门槛关闭；Text07 仍为 `in_progress`，下一切片处理 VerticalRl/ellipsis，再处理 icon/widget/emoji shortcode、BBCode V1 余项和公开 decorator。 |
| 2026-07-12 | RT-M3 rich inline horizontal ellipsis + 产品 framebuffer 收口 | runtime_text_rt_m3_inline_ellipsis_wgpu_texture_1_of_1_passed | `layout_engine/ellipsis.rs` 按真实 grapheme/object advance 裁决 End/Start/Middle/Word，`rich_inline.rs` 从最终可见 fragments 重建 line/item ranges 与 measured width/height；styled-run owner 边界继续保留，zero-length synthetic ellipsis 不会被 renderer 识别为 image。rich renderer 规划抽到 `ui/render/rich_text.rs`（197 行），root 收敛为 794 行。产品场景增加 End ellipsis 行，门禁同时要求被保留 U+FFFC 对应的导入四象限纹理通过 RGB dominance。验证期间先定位到共享 render graph 两层问题：camera region clear 把首个 `scene-color` Clear 改为 Load 后，图编译仍按图内 producer 误报；并发 owner 收敛后进一步暴露 preview-sky 把只读 depth 以 Write 解析。最终仅修正 `record_preview_sky_to_resources` 的 depth access 为 Read，没有测试绕过或第二条渲染路径。 | current-source target-client 产品 no-run 构建 13m21s、退出 0；修复 depth access 后增量重建 4m25s、退出 0。ignored exact `export_runtime_multilingual_text_product_framebuffer_png` 116.11s、1/1；nowrap/Word/WordSmart/RTL/End-ellipsis 五组真实纹理门禁通过。PNG 1080×920、131,867 bytes、SHA256 `17685053A570A334E4DEC76D23B4F041D07E8D309ECF853FED7E2BDC7B080D2A`；人工检查可见 `Ellipsis A [checker] trailin …`，checker 为真实红绿蓝黄纹理；`D:\cargo-targets` 同名 PNG 计数 0。scoped rustfmt/diff-check 通过；聚焦 lib-test 编译尝试被并发 asset migration 的 `directory_snapshot` 可见性 E0425×2 与 `CommitFault` 非穷尽 E0004 阻断，未把未执行的 unit exact 计为 green。 | horizontal ellipsis 产品门槛关闭；Text07 保持 `in_progress`。Start/Middle/Word 聚焦 exact 待外部 lib-test 编译收敛后补跑；下一切片处理 VerticalRl inline，再处理 icon/widget/emoji shortcode、BBCode V1 余项和公开 decorator。 |
| 2026-07-12 | RT-M3 VerticalRl rich inline + 产品 framebuffer 收口 | runtime_text_rt_m3_vertical_rl_inline_wgpu_texture_1_of_1_passed | 新增 `graphics/text/layout/rich_vertical.rs`，通过共享 vertical shape provider 计算 Glyph/Word/WordSmart 列范围：对象 height 是 y 主轴 advance，width 是列 cross extent。`layout_engine/rich_inline_vertical.rs` 复用 Text03 右到左列 capacity/placement 和 advance-aware ellipsis；renderer 从 resolved visual prefix advance 计算 image y，并按列宽处理 cross-axis baseline。产品场景追加两列 `竖<img>排富文本`，使用已验收的 SDF VerticalRl consumer；Native/SDF 竖排 parity 继续由 Text05 跟踪。renderer rich child 236 行、renderer root 794；renderer test root/child 779/216，产品测试 root/command child 771/151。 | VerticalRl layout exact filter 2/2；renderer placement exact 1/1（34.77s）；UI-feature production check 2m41s；current-source 产品 no-run 10m50s，最终 ignored exact WGPU 120.00s、1/1。nowrap/Word/WordSmart/RTL/End-ellipsis/VerticalRl 六组导入纹理 RGB 门禁通过。PNG 1080×920、137,133 bytes、SHA256 `49DB1034E895A6CC6DF063EFD1E83A08EC7AED79A683345406A4FD19C08AC33E`；放大复核右列为“竖→checker→排”、左列为“富→文→本”；repo target 与 `D:\cargo-targets\zircon-text07-product-0711` 同名计数均 0。scoped rustfmt/diff-check 通过。 | VerticalRl inline 门槛关闭；Text07 保持 `in_progress`。继续 icon/widget/emoji shortcode、BBCode V1 余项和公开 decorator；Native/SDF VerticalRl parity 不在本切片冒充完成。 |
| 2026-07-12 | RT-M3 自定义装饰器公开注册契约 | runtime_text_rt_m3_custom_decorator_contract_implemented_compile_external_blocked | 公开 `RichTextParser`、`RichTextDecorator`、`RichTextDecoration` 与注册错误；装饰器只修改继承的中立 style/link 或发出一个 `InlineObjectRef`，parser 不按自定义标签名增加分支。注册统一规范化 ASCII tag，并拒绝非法、重复、内置及全部 parser-owned tag 覆盖；单次 decorate 返回 false 时剥标签但保留内文。HTML 继续维持固定白名单，不开放自定义标签。实现参考 UE `ITextDecorator::Supports/Create`、`TextDecorators.h` 的 run 装饰器边界与 Godot `RichTextLabel` custom effect 注册模型，但保持 Zircon 的中立 DTO 和无 UI/GPU 权限边界。 | 新增样式、inline object、内置覆盖、parser-owned 覆盖、非法标签和单次拒绝 6 条行为测试。后续 inline/literal owner 接入后最终 `decorator.rs` 149 行、`parser.rs` 479 行、parser tests 489 行，公开 facade 仍仅 `graphics/mod.rs`；scoped rustfmt/diff-check 通过。locked/offline target-client production check 已编译穿过 Text07，随后被 Shader06 Realtime IBL 的 E0106/E0277 与两个 prepared `Option::then` E0599 阻断，故新增测试不冒充 green。 | Shader06 owner 收敛后执行 6 条 exact 和 `text_rich` 聚合；BBCode V1 余项继续 open，Text07 保持 `in_progress`。 |
| 2026-07-12 | RT-M3 icon/widget/emoji shortcode 中立内联契约 | runtime_text_rt_m3_icon_widget_emoji_contracts_implemented_compile_external_blocked | 新增独立 `inline_decorators.rs` 与 `emoji_shortcode.rs` owner。`[icon=glyph\|family]` 产 `InlineObjectRef::Icon`，renderer 把 U+FFFC 替换为指定 family 的真实 glyph batch，不再画 fallback 色块；`[widget=id\|widthxheight]` 产有限尺寸/ID 的 `InlineObjectRef::Widget` 并参与共享横排/纵排 metric，V1 按计划保持占位而不创建任意 UI；BBCode 文本片段展开固定内置 emoji shortcodes，未知 shortcode 原样保留，调用方可在单个 `RichTextParser` 注册一个 grapheme replacement，重复/非法注册失败。HTML 白名单与网络/资源边界不变。 | TDD 新增 icon、widget、known/unknown emoji、parser-local 注册、内置覆盖 6 条 parser 行为测试，以及 renderer icon glyph batch 1 条测试；与 decorator 前一切片合计 13 条待执行测试。最终 `decorator.rs` 149 行、`emoji_shortcode.rs` 121 行、`inline_decorators.rs` 69 行、`parser.rs` 479 行、parser tests 489 行、renderer rich child 276 行、renderer inline tests 268 行，均低于结构预算；scoped rustfmt/diff-check 通过。target-client production check 再次编译穿过 Text07，只被 Shader06 已登记的 Realtime IBL E0106/E0277 与两个 prepared `Option::then` E0599 阻断。 | Shader06 owner 收敛后执行 parser/renderer exact 与 `text_rich` 聚合；widget V1 继续仅 metric placeholder，完整交互 widget 随编辑器富文本控件；BBCode V1 其余标签仍 open，Text07 保持 `in_progress`。 |
| 2026-07-12 | RT-M3 Godot 对齐 literal/control tags + paragraph align | runtime_text_rt_m3_bbcode_literal_control_alignment_implemented_compile_external_blocked | BBCode owner 将 `[br]/[lb]/[rb]`、LRM/RLM/ALM、LRE/RLE/LRO/RLO/PDF、LRI/RLI/FSI/PDI、ZWJ/ZWNJ/WJ/SHY 映射为明确 Unicode 文本；这些标签不建立样式栈或第二套 BiDi/line-break 策略，后续继续由 Text02/03 处理。段落对齐补齐 Godot `[left]` 与 `[fill]`，分别映射共享 `UiTextAlign::Left/Justify`。所有 literal/paragraph 名称纳入统一 parser-owned shadow rejection。 | TDD 新增 left/fill paragraph override 与 literal/BiDi/control Unicode 输出 2 条测试；parser-owned shadow 测试追加 `[br]` 拒绝断言。最终 `bbcode.rs` 163 行、`decorator.rs` 149 行、`parser.rs` 479 行、parser tests 489 行；scoped rustfmt/diff-check 通过。该纯源码切片复用同一 current-source compile 结论：Text07 无诊断，Shader06 Realtime IBL E0106/E0277 与两个 prepared `Option::then` E0599 阻断 test binary。 | Shader06 owner 收敛后执行新增 exact；列表/缩进/段落容器与 V2 table/cell 继续 open，Text07 保持 `in_progress`。 |
| 2026-07-12 | RT-M3 BBCode V1 列表/缩进/段落容器共享布局 | runtime_text_rt_m3_bbcode_blocks_integration_4_of_4_wgpu_1_of_1_passed | 新增 `bbcode_blocks.rs` 作为 `[p]`、`[indent]`、`[ul]`、`[ol]`、`[li]` 的唯一块语义 owner；`[p align=... indent=...]` 产段落覆盖，嵌套 indent/list 产有上限的逻辑层级，ordered list 支持 decimal、alpha、Roman，unordered list 支持受控 bullet。列表 marker 是解析后的真实可整形文本，不用空格伪造；`ParagraphOverride` 记录 prefix range 与层级。UI 新增 `paragraph_layout.rs`，用共享 space glyph × `tab_size` 计算层级宽度，首行和续行用不同可用宽度实现 hanging indent，并按文本方向收缩 logical-start content frame；plain 与 rich-inline 路径消费同一中立段落 metadata。parser 对段落元数据稳定排序；重叠 block 范围的 align/list-prefix 由最具体的内层范围拥有，层级与首行 indent 则明确累加。恶意深层缩进会保留至少一个逻辑像素的内容 extent，避免零行或越界 frame。`[table]/[cell]` 继续按计划保持 V2，未知标签丢弃而保留内文。 | TDD 新增 parser 6 条、UI layout 7 条。Windows 原生 production check 完整通过；独立 production-surface integration 首轮准确暴露深缩进零行和 marker-width 场景未触发续行，修正共享内容 extent 并加长门禁样本后重跑 4/4（28.91s）。真实产品测试在受管 Windows GPU lane 构建 31m38s 后运行 122.78s、ignored exact 1/1；新图 1080×1130、187,114 bytes、SHA256 `14A4ABD16406E08B5251E3410D3C440A6CD3EEDA210E691B03CDCBCE8F1BC39F`。人工复核可见 `BBCode V1` 的真实星形 icon、rocket shortcode、嵌套 A/B ordered markers、菱形列表及续行 hanging indent，以及右侧 paragraph container 的逻辑缩进换行；既有多语言、RTL、emoji、SDF、ellipsis、VerticalRl checker 同时保留。仓库 `target` 不存在，验证 target 同名 PNG 为 0。新增 owner 约 258/244 行，parser 585、UI layout root 296、wrapping 309，均低于结构预算。 | V1 横排 block 产品门槛关闭；Text07 保持 `in_progress`，因为 Text07 V2 table/cell 仍 open。Text03 完整 VerticalRl 首行缩进/列对齐与 Text05 Native/SDF VerticalRl parity 继续由各自 owner 跟踪。 |
| 2026-07-12 | RT-M4 V2 table/cell 中立解析、块级嵌套布局与产品帧验收 | runtime_text_rt_m4_table_cell_windows_and_wgpu_product_framebuffer_accepted | `RichParseResult` 新增中立 `RichTable`/`RichTableColumn`/`RichTableCell`，只记录剥标记文本范围、行优先顺序和列扩展/收缩约束，不把 BBCode 标签泄露到 UI/renderer。`bbcode_table.rs` 独立承载 `[table=N]`、`[cell]`、`expand`/`shrink`、64 列与 8 层嵌套上限；table 作为块在前后文本间建立真实换行边界。`layout_engine/rich_table.rs` 将现有 `UiParsedText` 按 block/cell range 切片，单元格递归复用既有 rich run、paragraph、inline、BiDi 与 wrapping owner；列按 cell preferred width 测量后在可用宽度内 shrink/expand，行高取该行最高 cell。参考 Godot `rich_text_label.{h,cpp}` 的 ItemTable/ItemFrame、min/max column 与 row-height 流程，以及 Slint/Fyrox 的 measure-then-arrange grid；Zircon 首段不复制完整 GUI cell style，也没有 shared layout 标签名特判。产品 exporter 新增真实两列表格，包含共享列、换行长 cell、最高行高、样式和 inline icon；结构断言在创建 WGPU renderer 前即验证同列 x、同行 y、逐行推进、cell wrap 与 U+FFFC。 | Windows 协调器 production check 退出 0；current-source `text_rich` 56/56（含 parser 5/5、UI table 4/4）通过；最终 fresh ignored exact WGPU 产品帧 1/1（99.00s）通过，协调器 job `3376acdf8f404a26a04b9509e0d3e47a`。PNG `runtime_text_multilingual_rich_table_product_framebuffer_20260712.png` 为 1080×1450、234,456 bytes、SHA256 `0B69036E831C376B6C7235CF5CE05D62331F48BE18D7D93F59D97C6527A1A0AA`；人工检查确认表头、两列共享对齐、右列换行、左列长 cell 与真实 WGPU inline icon 均可见。repo `target` 不存在，D/E/F 外部 target 同名 PNG 计数为 0；scoped rustfmt/diff-check 通过。 | RT-M4 首段验收关闭；colspan/rowspan、完整 Godot cell border/background/padding options、VerticalRl table 及表格专用 cell 交互仍明确保留 open，总 Text07 状态继续 `in_progress`。 |
| 2026-07-12 | RT-M5 colspan/rowspan 设计与实施计划 | runtime_text_rt_m5_span_design_plan_completed | 用户选择确定性 row-major 自动排布：跳过 `rowspan` 占位、在首个空位把 `colspan` 收缩到连续空闲列、无空位进下一行、非法值按 1、hostile rowspan 上限 64。设计采用 parser 一次性解析并固化中立 row/column/span DTO，UI 仅做 bounds-checked grid projection 与跨轨尺寸求解；拒绝 UI 重复 placement 和持久 dense occupancy matrix。模块按结构规范拆为 parser `attributes`/`placement` 与 UI `grid`/`sizing` 叶子 owner。 | 设计 `docs/superpowers/specs/2026-07-12-runtime-rich-table-spans-design.md`；计划 `docs/superpowers/plans/2026-07-12-runtime-rich-table-spans.md`；已比较 Godot rich cell/frame、Slint span constraints、Fyrox measure/arrange；计划 placeholder/type consistency 自审通过；协调器租约覆盖全部 planned source/test/doc paths，无 Text07 Failure。 | S0 完成；S1 DTO/parser、S2 UI track sizing、S3 WGPU 产品 source、RT-M5-T 与 S4 docs/status 按计划执行，尚未冒充 compile/test green。 |
| 2026-07-12 | RT-M5 S1 中立 span DTO 与 parser 自动占位 | runtime_text_rt_m5_span_parser_implemented_testing_stage_pending | `RichTableCell` 新增 serde-safe `row_index`/`column_index`/`column_span`/`row_span`，默认跨度固定为 1；`bbcode_table/attributes.rs` 唯一负责 `colspan`/`rowspan`、expand/shrink、非法值降级与 64-row hostile cap；`bbcode_table/placement.rs` 用每列 `occupied_until_row` 游标执行 row-major skip、首个空位连续区间 clamp 和无空位换行，不分配 dense grid。root `bbcode_table.rs` 只保留 table/cell 生命周期编排，旧 `cells.len() % columns.len()` 路径已删除；span cell 的 expand/shrink 覆盖所有实际列。 | 先写 4 个 parser 行为测试：`text_rich_bbcode_table_spans_skip_occupied_slots_and_clamp_to_the_row`、`text_rich_bbcode_table_colspan_stops_before_an_occupied_slot`、`text_rich_bbcode_table_span_values_are_bounded_and_invalid_values_default_to_one`、`text_rich_bbcode_spanning_cell_configures_every_covered_column`；既有 row-major 测试追加默认坐标/跨度断言。生产文件 183/135/86/82 行，test 141 行；编译与执行按 milestone-first 规则留到 RT-M5-T，当前不冒充 green。 | S1 实现完成；进入 S2 UI bounds-checked grid、跨列 preferred width 与跨行 height constraint。 |
| 2026-07-12 | RT-M5 S2 span-aware UI grid 与跨轨尺寸求解 | runtime_text_rt_m5_span_ui_layout_implemented_testing_stage_pending | 新增 `layout_engine/rich_table/grid.rs`，把 parser-resolved cell 投影为 allocation-safe `PlacedTableCell`，对 manually constructed/corrupt DTO 的 row/column/span 做列数、cell count×共享 64-row bound 限制；新增 `sizing.rs`，先应用单轨 preferred constraints，再按 expand ratio 或均分补足 colspan deficit，最后复用 shrink/expand policy 适配 frame；纵向先解普通 row height，再均分 rowspan deficit。root `rich_table.rs` 只做 block/nested layout orchestration：最终 span width 下布局一次 cell、解 row tracks、平移 line frame 并按真实 clip 过滤。旧 `cells.chunks(column_count)`、`index % preferred.len()` 与 inline solver 已删除，不保留 no-span 旁路。 | 先写 4 个 UI 行为测试：`text_rich_bbcode_table_colspan_uses_the_combined_track_width`、`text_rich_bbcode_table_rowspan_reserves_its_column_on_following_rows`、`text_rich_bbcode_table_colspan_reduces_before_a_rowspan_collision`、`text_rich_bbcode_table_rowspan_height_keeps_the_next_row_below_its_content`；既有 4 个普通 table tests 保留兼容门禁。root/grid/sizing/test 分别 485/62/182/209 行，均低于结构预算；编译与执行按计划留到 RT-M5-T，当前不冒充 green。 | S2 实现完成；进入 S3 真实 merged heading + rowspan + colspan + wrap + icon WGPU 产品场景和 pre-render 结构断言。 |
| 2026-07-12 | RT-M5 S3 span 产品 framebuffer 场景与 pre-render 门禁 | runtime_text_rt_m5_span_product_source_implemented_wgpu_pending | 产品表格改为真实三列 span 场景：首行 `colspan=3` merged heading；第一详情列 `rowspan=2`；第一详情行其余两列 `colspan=2`；下一行保留 ordinary wrapped detail 与 `[icon]` 真实 glyph。`proof_assertions.rs` 在创建 WGPU renderer 前断言 merged heading 跨过第三列 origin、rowspan owner 与 colspan 同行、下一行不占 rowspan slot、colspan 跨过第三列 origin、ordinary cell wrap 以及 U+FFFC icon placeholder。proof canonical path 改为 `docs/tests/runtime/text/runtime_text_multilingual_rich_table_spans_product_framebuffer_20260712.png`。 | 修改 `zircon_runtime/tests/runtime_text_multilingual_product_framebuffer.rs` 与 `.../proof_assertions.rs`；场景仍通过既有 `layout_text`、UI render extract、WGPU submit/capture 路径，不新增 renderer span 分支。此切片只完成测试 source；RT-M5-T 尚未构建/执行，PNG 当前未生成，因此不冒充 pixel acceptance。 | S3 source 完成；进入 RT-M5-T formatting、Windows production/focused tests、exact ignored WGPU、原图检查、hash 与 target-copy scan。 |
| 2026-07-12 | RT-M5-T colspan/rowspan Windows + WGPU 验收 | runtime_text_rt_m5_span_windows_focused_and_wgpu_accepted | deterministic parser placement、neutral resolved grid DTO、bounds-checked UI grid projection、colspan preferred constraint、rowspan height constraint 与真实 renderer reuse 全部进入 current-source acceptance。产品断言初跑先暴露 rowspan label 被 proportional shrink 拆行，改用 `shrink=false` 固定清晰 owner column；第二次暴露第三列英文短语按预期换行，断言改以真实 U+FFFC inline icon 首行为几何锚点。没有为测试增加 renderer bypass。 | `cargo fmt --all -- --check` 通过；scoped `git diff --check` 通过（仅 tracked 文件 LF/CRLF 提示）；managed `cargo build -p zircon_runtime --locked` 通过。current-source lib-test binary `text_rich` 64/64（新增 parser 4 + UI 4 全通过，35.44s）。package-wide `cargo test -p zircon_runtime --locked` 尝试在外部 `runtime_plugin_world_extensions_contract.rs` 的 removed `World::run_native_scene_systems_for_stage` E0599 阻断。最终 exact target-client WGPU exporter 1/1（116.88s，job `15dbf21801754a618cfc0e2354c0ebd5`）。PNG 1080×1450、242,497 bytes、SHA256 `47A5C96CBD833ABD46C563D98F52CF1C7F1B5A3E0537CC26943730128D621F43`；原图复核 merged heading、rowspan owner、colspan detail、ordinary wrap、真实 star icon；repo target 不存在，D/E/F 九个允许 target roots 同名计数 0。 | RT-M5 验收关闭 colspan/rowspan。Text07 保持 `in_progress`：完整 cell border/background/padding、VerticalRl table、table interaction、Text03 vertical block gaps 与 Text05 Native/SDF parity 仍 open。 |
| 2026-07-12 | RT-M6 S0 cell 盒模型测试契约 | runtime_text_rt_m6_cell_box_contract_tests_authored_testing_stage_pending | 在生产类型与行为落地前先写 2 条 parser、2 条 UI layout、1 条 runtime-interface paint projection 与 1 条 screen-space render planning 回归。契约固定 Godot 对齐的 `border`、单/双色 `bg`、四边 `padding`，并要求 invalid tuple 原子回退、负值归零、单边 4096 logical-pixel 上限；布局门禁要求 authored padding 改变内容 origin/measure，colspan+rowspan cell 只产一个最终跨轨 box；paint/render 门禁要求 background 在 text 前、1px border 在 text 后。 | 按 milestone-first 策略，本切片只写测试源，不提前运行 Cargo；当前预期因 `RichTableCellBoxStyle`、`UiResolvedTextBox` 与 table decoration kinds 尚不存在而处于编译红态，不冒充行为 green。规格 `docs/superpowers/specs/2026-07-12-runtime-rich-table-cell-box-design.md` 与计划 `docs/superpowers/plans/2026-07-12-runtime-rich-table-cell-box.md` 已完成 placeholder/type/scope 自审；精确 source/test/doc 路径已重新领取 coordinator 租约并归属当前哈希。 | S0 完成；进入 S1 中立 cell style DTO 与 BBCode 属性解码，Cargo 统一留到 RT-M6-T。 |
| 2026-07-12 | RT-M6 S1 中立 cell style DTO 与 BBCode 解码 | runtime_text_rt_m6_cell_box_parser_implemented_testing_stage_pending | `RichTableCell` 新增 serde-safe `RichTableCellBoxStyle` 与显式 `RichTableCellPadding`；missing padding 与 authored zero 保持不同语义。`bbcode_table/attributes.rs` 作为唯一属性 owner 解码 Godot 对齐的 `border`、`bg` 与四分量 `padding`，复用既有 hex color grammar；单色背景复制到 odd/even，两色按 resolved start-row parity 保存。padding 必须恰好四个 finite 数，负值归零、单边封顶 4096；invalid tuple/颜色不部分写入。`ActiveCell` 在 open 时固化 style，close 时随 placement 一次性写入中立 DTO。 | 未增加命名颜色、别名、border width、renderer 类型或 parser-side geometry；`RichTableCell` 的默认 span 与旧 serde payload 继续安全回退。按 milestone-first 规则，Cargo 留到 RT-M6-T，当前仅声明 source 实现完成，不冒充测试 green。 | S1 完成；进入 S2 `UiResolvedTextBox` transport 与 paint decoration projection。 |
| 2026-07-12 | RT-M6 S2 resolved box transport 与 paint projection | runtime_text_rt_m6_resolved_box_transport_implemented_testing_stage_pending | Runtime Interface 新增 `UiResolvedTextBox`，以 source range、最终 `UiFrame`、可选 RGBA background/border 与 border width 承载 renderer-neutral box；`UiResolvedTextLayout.boxes` 使用 serde default/empty skip，普通横排/纵排 layout 显式为空。`UiRenderCommand::text_paint()` 将 box 两遍投影：background 先进入 decoration 列表、editable selection/caret/composition 保持原 owner、border 最后进入；颜色由共享 `UiRgbaColor::to_u8()` 确定性输出 `#RRGGBBAA`。`UiTextPaintDecorationKind` 仅新增 `TableCellBackground/Border`，thickness 对旧 payload 默认 1。 | 新 interface contract 预先断言 exact frame/range/color/order/thickness；现有 full struct literals 已补空 boxes，使用 `..Default` 的调用点继续无侵入兼容。没有把 BBCode、track solver 或 table DTO 引入 interface/command。按 milestone-first 留待 RT-M6-T 编译与行为执行。 | S2 完成；进入 S3 padding-aware measure/arrange 与 span box frame。 |
| 2026-07-12 | RT-M6 S3 padding-aware measure/arrange 与最终 span box | runtime_text_rt_m6_cell_box_layout_implemented_testing_stage_pending | rich-table layout 以 per-cell `ResolvedCellPadding` 取代全局-only inset：authored logical pixels 覆盖旧 fallback，missing 继续使用 0.35em inline / 0.2em block；preferred width 加 left/right，height constraint 加 top/bottom，每个 prepared cell 保存自己的 provisional y/top inset。列宽与行高最终求解后，styled cell 只生成一个 `UiResolvedTextBox`，width 覆盖 colspan 的全部轨道与内部 gap，height 覆盖 rowspan 全部行；背景按 resolved start-row parity 选择，border 固定 1px。顶层 block/table 与 cell 内嵌 table 的 boxes 随 line 一起平移、source-range shift、clip visibility 过滤并合并。 | 普通横排/VerticalRl constructor 显式空 boxes；未改变 span placement、column/row solver 或 renderer，未从 line bounds 反推 geometry。两条预写 layout 契约将于 RT-M6-T 验证 authored origin/measure 和最终 span box。root `rich_table.rs` 仍低于结构审查硬阈值，box helper 未进入 sizing owner。 | S3 完成；进入 S4 screen-space border geometry 与真实产品场景。 |
| 2026-07-12 | RT-M6 S4 screen-space cell box 与真实产品场景 | runtime_text_rt_m6_cell_box_renderer_product_source_implemented_wgpu_pending | screen-space renderer 将 `Selection/TableCellBackground` 分类为 pre-text decorations，将 `TableCellBorder` 保持在 post-text lane；border 唯一复用现有 `push_border`，其余 decoration 仍走 `push_rect`。产品表格升级为 5 个显式 styled cells：cyan/amber borders、odd/even 深色背景、不对称 padding，并保留 merged heading、rowspan owner、colspan detail、普通 wrapped cell、多语言与 inline star glyph。pre-render proof 要求 5 个 resolved boxes、5 background + 5 border paint decorations、非透明颜色、1px border 以及 heading 16/10 padding origin；最终 framebuffer 像素/readback 门禁未弱化。 | canonical 输出改为 `docs/tests/runtime/text/runtime_text_multilingual_rich_table_cell_box_product_framebuffer_20260712.png`。本切片只完成 renderer/product source；按 milestone-first 尚未构建、运行 WGPU 或生成 PNG，因此状态明确为 pending，不冒充视觉验收。 | S4 完成；进入 RT-M6-T formatting、Windows compile/focused tests、ignored exact WGPU、原图检查与 docs/status。 |
| 2026-07-12 | RT-M6-T cell border/background/padding Windows + WGPU 验收 | runtime_text_rt_m6_cell_box_windows_focused_and_wgpu_accepted | 中立 cell style、原子属性解析、padding-aware measure/arrange、span-aware resolved box、interface paint ordering 与 screen-space border geometry 全部进入 current-source acceptance。首轮 focused 调试发现单 cell table 的 owner range 被当成自身 nested table 递归，最低共享层改为只保留更深 table；首轮产品 proof 又准确暴露 authored top padding 使不同 cell 文本基线相差 1px，门禁改为比较最终 cell box frame，而没有弱化 framebuffer/readback 或颜色断言。 | exact leased Rust 文件 rustfmt 与 scoped diff-check 通过；Runtime Interface production build 通过；结构拆分后的新 paint projection exact 1/1（239 filtered）。Runtime current-source locked lib check 通过；`text_rich` 68/68（7,695 filtered），renderer exact 1/1（7,762 filtered）。package-wide test 仍被外部 `MaterialAsset`/`SourceCubemap` 旧接口与 removed `World::run_native_scene_systems_for_stage` 阻断。最终 exact target-client WGPU exporter 1/1（125.10s，job `bdb1cd9c78c44822af5c98f21cdcb2ad`）。PNG 1080×1450、242,151 bytes、SHA256 `CAE02C87B1AD0196C426E880B925B63B22C89F907785E7BFDF72D1A433233B18`；原图确认 5 个背景/边框、交替深色、青/琥珀边框、非对称 padding、span/wrap/star；受检 target roots 同名计数 0。 | RT-M6 验收关闭完整横排 cell border/background/padding。Text07 保持 `in_progress`：VerticalRl table、table interaction、Text03 vertical block gaps 与 Text05 Native/SDF parity 仍 open。 |
| 2026-07-12 | RT-M7 S0 VerticalRl table structure-first test contracts | runtime_text_rt_m7_vertical_table_contract_tests_authored_testing_stage_pending | 现有 RT-M4–M6 十条横排 table 回归原样迁入 folder-backed `tests/rich_table/horizontal.rs`，共享 `mod.rs` 仅保留 import、VerticalRl fixture 与 `find_line`；新增四条纵排契约，分别固定 column index 物理向下、row index 从右向左、colspan→物理高度、rowspan→物理宽度、physical top/right padding 与 before/table/after block 顺序。 | `horizontal.rs`/`vertical_rl.rs`/`mod.rs` 为 240/88/29 行；exact-file rustfmt 与 scoped diff-check 通过。按 milestone-first 未运行 Cargo，当前生产 `VerticalRl => None` guard 会在 RT-M7-T 使新契约红，不冒充 green。 | S0 完成；进入 S1 606 行 root hard-split 与共享 logical-axis owner。 |
| 2026-07-12 | RT-M7 S1 rich-table hard split 与 logical axes | runtime_text_rt_m7_rich_table_hard_split_axes_implemented_testing_stage_pending | 原 606 行 `rich_table.rs` 硬拆为 8 行导航 root 与 `axes/layout/cell_layout/grid/sizing/source_slice` 六个单责 owner；source slicing、nested depth、range shifting 原样归属 source owner，solver 的 width/height 专用名称被 axis-neutral extent 名称彻底替换。唯一 `TableAxes::physical_frame` 固定 HorizontalTb `(x+inline,y+block,inline_extent,block_extent)` 与 VerticalRl `(right-block-block_extent,y+inline,block_extent,inline_extent)` 映射，没有 compat module 或双 solver。 | exact-file rustfmt 与 scoped diff-check 通过；Cargo 按 milestone-first 延后到 RT-M7-T，不将静态完成冒充编译通过。 | S1 完成；进入 S2 共享 measure/arrange、span、padding 与 surrounding block flow。 |
| 2026-07-12 | RT-M7 S2 VerticalRl table shared measure/arrange | runtime_text_rt_m7_vertical_table_shared_layout_implemented_testing_stage_pending | 删除 `VerticalRl => None`；preferred column/row constraint、track solver、cell arrange 与 resolved box 全部经同一 logical-axis pipeline。VerticalRl column→物理 y/height，row→从右向左 x/width；colspan→物理高度，rowspan→物理宽度；padding 始终保持 physical left/top/right/bottom。prepared layout 用 Horizontal top-left / Vertical top-right anchor 同步平移 line 与 nested box，并与最终 content/global clip 相交。before/table/after 现在按 writing-mode block axis 消耗。 | exact-file rustfmt 与 scoped diff-check 通过；保留 parser bounds、nested depth、row parity 与 renderer-neutral resolved box contract。Cargo 按计划留给 RT-M7-T。 | S2 完成；进入 S3 真实产品 VerticalRl rich table 与 pre-render proof gates。 |
| 2026-07-12 | RT-M7 S3 VerticalRl table 真实产品源与 pre-render gates | runtime_text_rt_m7_vertical_table_product_source_implemented_wgpu_pending | RT-M6 横排 markup 从 794 行产品 root 抽到 39 行 `proof_commands/rich_table.rs`；root 当前 791 行。新增右侧 `690,1120,348,300` 真实 VerticalRl BBCode table，使用 Microsoft YaHei UI、SDF、Glyph wrap、CJK/Latin、colspan/rowspan、非对称 physical padding、交替深色背景及 cyan/amber border。pre-render gate 固定 5 boxes + 5 background + 5 border、column 向下、row 向左、span 物理尺寸、top/right padding 与 Latin shaped run。 | canonical proof 改为 `docs/tests/runtime/text/runtime_text_multilingual_rich_table_vertical_rl_product_framebuffer_20260712.png`；既有 per-command changed pixels、background-only compare、vertical glyph bounds 与真实 WGPU readback 均未弱化。本切片尚未运行 WGPU，明确 pending。 | S3 完成；进入 RT-M7-T Windows compile/focused/WGPU/原图验收与 docs/status。 |
| 2026-07-12 | RT-M7-T VerticalRl table Windows + WGPU 验收 | runtime_text_rt_m7_vertical_table_windows_product_wgpu_accepted_focused_lib_rebuild_externally_blocked | Windows current-source `cargo check -p zircon_runtime --lib --locked --offline --jobs 1` 通过；产品 integration no-run 通过。旧 lib-test artifact 执行 `text_rich` 72 条时 71 pass/1 fail，准确暴露 VerticalRl padding cell 使用 top-right anchor 后横排仍错误复用 right anchor；共享 arrange 改为 Horizontal top-left、Vertical top-right 后 production 再次通过。current-source lib-test rebuild 被外部 `advanced_lighting/subsurface.rs` ambiguous `u16::MAX.into()` E0283×2 阻断，因此不冒充 72/72。 | exact ignored product exporter 1/1（197.86s）。PNG 1080×1450、257,649 bytes、SHA256 `82EC5035EDB80AC4F6D894C9A1A000279F23B75B95D4FE1881B0AC70655813DE`；原图确认右侧纵表的背景/边框、row 向左、column 向下、colspan height、rowspan width、非对称 padding 与 CJK/Latin SDF。repo target、D/E/F target roots 同名计数均 0。 | RT-M7 VerticalRl table layout/rendering 验收关闭；Text07 仍 `in_progress`，下一底层切片为 table-specific interaction，并保留 Text03/Text05 open 项。 |
| 2026-07-13 | RT-M8 S0 table-link interaction contracts | runtime_text_rt_m8_table_link_contracts_red_then_scoped | 新增 HorizontalTb 第二 cell link、VerticalRl 第二 inline slot link、padding/background 负向命中与 surface primary-release host request 四个契约。旧单轴 candidate 选择下两条正向 table-link 测试 0/2，padding 负向 1/1，准确复现同 y/x 的前一 cell line 抢占命中。 | TDD red 来自实际 current-source lib-test artifact；既有普通 hyperlink affinity exact 仍 1/1，证明不是 parser link range 回退。 | S0 完成；进入共享 resolved-line full-frame candidate 修复。 |
| 2026-07-13 | RT-M8 S1 shared full-frame text hit candidate | runtime_text_rt_m8_full_frame_line_hit_implemented_focused_green | `ui/text/hit_test.rs` 新增私有 `containing_line_index`，先以完整 physical `UiResolvedTextLine.frame` 选中真实 cell line；无 line 包含 point 时才沿用 Horizontal y / VerticalRl x 最近行列回退。未读取 table box、BBCode、RichTable DTO，affinity/grapheme/visual-source mapping 与 surface effect 均不变。 | current-source Windows binary: Horizontal/Vertical table links 2/2、padding negative 1/1、surface table host request 1/1；既有 ordinary link affinity、primary release、default-action dispatch 各 1/1。 | S1 interaction 完成；RT-M8 focused acceptance 通过。 |
| 2026-07-13 | RT-M8-T focused validation 与下层 Vertical preferred measure correction | runtime_text_rt_m8_interaction_focused_accepted_vertical_padding_regression_correction_pending_external_rebuild | interaction focused gates 全绿。全量 `text_rich` 首轮 74/75，唯一失败为 RT-M7 large-padding VerticalRl cell 短 CJK content 被裁掉；support-first 诊断确认 vertical preferred measure 使用 `f32::MAX/4` 造成短列浮点精度塌缩，现改为 source length × line advance 的有界 measure frame，并让 row block extent 至少容纳 `font_size`。后续复核又确认 VerticalRl arrange 不能把 provisional container right 当成 glyph anchor，现改为取 prepared lines/nested boxes 的实际最右边界对齐最终 content right。 | latest correction exact rustfmt/scoped diff-check 通过；最终 current-source Cargo retry 被 concurrent Environment API drift（PMREM constants、ProceduralSkyParams 等 72–100 errors）阻断，故不冒充 75/75。RT-M8 interaction 自身 focused evidence 不受该外部漂移影响。 | RT-M8 interaction 关闭；Vertical padding correction 保持 testing pending，Goal 继续 Text03/Text05。 |
| 2026-06-27 | 计划建立 | planned | BBCode 全集 + HTML 受控子集 + 装饰器 + 内联对象路线 | 文档 | RT-M1 解析框架 + BBCode；产 StyledRun 喂 02/03。 |

### RT-M3 里程碑验收

| 里程碑 | 测试阶段 | 状态 | 验收证据 |
|--------|----------|------|----------|
| M3 | RT-M3-T testing | 通过 | Windows 原生 production check 通过；独立 production-surface integration 4/4；真实 WGPU framebuffer ignored exact 1/1。验收图为 1080×1130、187,114 bytes，SHA256 `14A4ABD16406E08B5251E3410D3C440A6CD3EEDA210E691B03CDCBCE8F1BC39F`，且未写入任何 `target` 目录。 |

### RT-M4 首段里程碑验收

| 里程碑 | 测试阶段 | 状态 | 验收证据 |
|--------|----------|------|----------|
| M4 首段 | RT-M4-T testing | 通过 | Windows 原生 production check 通过；current-source `text_rich` 56/56；fresh 真实 WGPU framebuffer ignored exact 1/1（99.00s，job `3376acdf8f404a26a04b9509e0d3e47a`）。验收图为 1080×1450、234,456 bytes，SHA256 `0B69036E831C376B6C7235CF5CE05D62331F48BE18D7D93F59D97C6527A1A0AA`，且未写入任何 `target` 目录。 |

### RT-M5 colspan/rowspan 里程碑验收

| 里程碑 | 测试阶段 | 状态 | 验收证据 |
|--------|----------|------|----------|
| M5 | RT-M5-T testing | 通过 | Windows managed production build 通过；current-source `text_rich` 64/64；真实 WGPU framebuffer ignored exact 1/1（116.88s，job `15dbf21801754a618cfc0e2354c0ebd5`）。验收图 1080×1450、242,497 bytes，SHA256 `47A5C96CBD833ABD46C563D98F52CF1C7F1B5A3E0537CC26943730128D621F43`；repo target 不存在，九个允许 target roots 同名 PNG 为 0。package-wide test 的外部 E0599 已如实记录，不作为 Text07 focused green。 |

### RT-M6 cell 盒模型里程碑验收

| 里程碑 | 测试阶段 | 状态 | 验收证据 |
|--------|----------|------|----------|
| M6 | RT-M6-T testing | 通过 | Windows managed Runtime Interface production/test 与 Runtime current-source check 通过；`text_rich` 68/68；renderer exact 1/1；真实 WGPU framebuffer ignored exact 1/1（125.10s，job `bdb1cd9c78c44822af5c98f21cdcb2ad`）。验收图 1080×1450、242,151 bytes，SHA256 `CAE02C87B1AD0196C426E880B925B63B22C89F907785E7BFDF72D1A433233B18`；受检 target roots 同名 PNG 为 0。package-wide 外部旧 API 编译失败已如实记录，不作为 focused green。 |
