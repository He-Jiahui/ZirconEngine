---
related_code:
  - zircon_runtime/src/core/framework/render/environment/source_cubemap.rs
  - zircon_runtime/src/core/framework/render/environment/source_cubemap/pmrem.rs
  - zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_compute_executor.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export/frame_assertions.rs
  - zircon_runtime/tests/runtime_shader_pbr_hdri_export/scene_fixtures.rs
  - zircon_runtime/src/core/framework/render/material/standard_material.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs
  - zircon_runtime/src/graphics/shader/template/material_surface.rs
  - zircon_runtime/src/graphics/shader/wgsl/zr_gbuffer_encode_standard_pbr.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/shaders/deferred_lighting.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/shaders/fallback_mesh.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/post_process/shaders/post_process_screen_space_reflection.wgsl
  - zircon_runtime/src/core/framework/render/light/snapshots.rs
  - zircon_runtime/src/core/framework/render/environment/cubemap_projection.rs
  - zircon_runtime/tests/runtime_environment_cubemap_projection_contract.rs
  - zircon_runtime/src/core/framework/render/image/dimension.rs
  - zircon_runtime/src/asset/assets/texture/descriptor.rs
  - zircon_runtime/src/asset/assets/texture/upload_support.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_texture/gpu_texture_resource_from_asset.rs
  - zircon_runtime/tests/runtime_texture_cube_resource_contract.rs
  - zircon_runtime/src/graphics/feature/builtin_render_feature_descriptor/feature_descriptors/mesh.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_texture/mod.rs
  - zircon_plugins/rendering/plugin.toml
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironment.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentCapture.cpp
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Private/ReflectionEnvironmentDiffuseIrradiance.cpp
  - dev/UnrealEngine/Engine/Source/Developer/TextureCompressor/Private/TextureCompressorModule.cpp
  - dev/Graphics/Packages/com.unity.render-pipelines.universal/Runtime/Passes/DrawSkyboxPass.cs
  - dev/Graphics/Packages/com.unity.render-pipelines.core/Runtime/Lighting/ProbeVolume/ProbeReferenceVolume.cs
  - dev/bevy/crates/bevy_core_pipeline/src/skybox/mod.rs
  - dev/bevy/crates/bevy_light/src/probe.rs
  - dev/bevy/crates/bevy_pbr/src/light_probe/mod.rs
  - dev/bevy/crates/bevy_pbr/src/light_probe/environment_map.rs
  - dev/bevy/crates/bevy_pbr/src/light_probe/generate.rs
  - dev/bevy/crates/bevy_pbr/src/light_probe/irradiance_volume.rs
  - dev/bevy/crates/bevy_pbr/src/lightmap/mod.rs
  - dev/bevy/crates/bevy_pbr/src/fog.rs
  - dev/Fyrox/fyrox-impl/src/scene/probe.rs
plan_sources:
  - .codex/plans/Rendering 插件选项补齐计划.md
  - .codex/plans/Hybrid GI Lumen-Style V1 三阶段计划.md
---

# 计划 11:环境光照(skybox / 反射探针 / 烘焙 / 雾)

## 目标

补齐场景级环境光照资产链,与 HGI 动态 GI 形成"烘焙基线 + 动态增强"的两档结构:

1. skybox/cubemap:天空盒材质域(程序化渐变 + cubemap 两种)、preview-sky 收编、IBL 预滤波(specular mip 链 + irradiance SH)。
2. 反射探针:box/sphere 影响域、烘焙捕获、box projection 校正、探针混合与 fallback 到 skybox;接入既有 rendering.reflection_probes 插件 feature。
3. 光照烘焙:lightmap(2U 通道、烘焙图集)与 light probe(SH L2)采样路径定稿;烘焙器本体走离线工具/插件,runtime 只定消费契约;接入 rendering.baked_lighting feature。
4. 解析雾:线性/指数/指数平方 + 高度雾,场景级设置(可被 Volume 覆写,计划 07 协同);与后处理屏幕空间雾区分:解析雾在前向着色/deferred 合成中计算。
5. ambient:平坦色 / 三段渐变 / SH 环境光三模式。

## 现状与差距

- 有 preview-sky executor(预览天空),但无正式 skybox 材质域、无 IBL 预滤波链;cubemap 资产格式未定。
- reflection_probes/baked_lighting 插件 feature 是描述符与合成占位,无捕获/烘焙数据链与采样 ABI。
- 无解析雾;ambient 只有单色。

## 参考代码

| 文件 | 应重点阅读 |
|------|-----------|
| `dev/Graphics/.../Runtime/Passes/DrawSkyboxPass.cs` | skybox 作为 opaque 末尾 pass 的插入位置(early-z 受益)与深度处理 |
| `dev/UnrealEngine/.../Renderer/Private/ReflectionEnvironmentCapture.cpp` | 探针捕获:cubemap 渲染 → mip 预滤波(GGX 重要性采样)→ diffuse irradiance 提取 |
| `dev/UnrealEngine/.../Developer/TextureCompressor/Private/TextureCompressorModule.cpp` | equirect/long-lat → source cubemap mip0、普通 texture mip、angular filtered cubemap mip 的边界;source mip pyramid 不等同 PMREM |
| `dev/UnrealEngine/.../Renderer/Private/ReflectionEnvironment.cpp` | 探针混合与按距离/影响域排序合成;skybox fallback |
| `dev/Graphics/.../core/Runtime/Lighting/ProbeVolume/ProbeReferenceVolume.cs` | probe volume 的索引与插值组织(远期 probe GI 参考,本计划只取 SH 采样 ABI) |

次参考:`dev/bevy/crates/bevy_pbr`(environment map light 的 wgsl 采样与 binding 组织)。

**Rust/wgpu 落地参照(防凭空实现)**:

| 文件 | 对应本计划机制 | 应重点阅读 |
|------|---------------|-----------|
| `dev/bevy/crates/bevy_core_pipeline/src/skybox/mod.rs` | skybox pass(Cubemap 模式) | `SkyboxUniforms`(:89)/`SkyboxPipeline` 与 `prepare_skybox_pipelines`/`prepare_skybox_bind_groups`:cubemap 天空的 pipeline/深度策略组织(配同目录 `skybox.wgsl`) |
| `dev/bevy/crates/bevy_light/src/probe.rs` | `ReflectionProbeData` 契约面 | `LightProbe`(:75)影响域组件 + `EnvironmentMapLight`(diffuse/specular 双贴图、intensity、旋转)与 `GeneratedEnvironmentMapLight`(:260)字段清单 |
| `dev/bevy/crates/bevy_pbr/src/light_probe/mod.rs` | 探针选择与 per-view 上传 | `LightProbesUniform`(:122)/`RenderViewLightProbes`(:255):view 内探针收集、裁剪进 uniform 的 Rust 全链(对应"extract 端按 layer/距离裁到 64") |
| `dev/bevy/crates/bevy_pbr/src/light_probe/environment_map.rs` | `GpuEnvironmentMap` 绑定 | `EnvironmentMapUniform` 与 binding 组织(rotation 采样期生效,与 `IblBakeKey` 排除项同思路;配 `environment_map.wgsl` 采样) |
| `dev/bevy/crates/bevy_pbr/src/light_probe/generate.rs` | IBL 预滤波 compute(`ibl_prefilter.rs` 直接同类) | 运行时 GGX specular mip 链卷积 + irradiance 生成的 compute 编排、radiance/irradiance 双 bind layout(:73)(配 `environment_filter.wgsl`/`copy.wgsl`) |
| `dev/bevy/crates/bevy_pbr/src/light_probe/irradiance_volume.rs` | `LightProbeGridData`(V1 均匀网格) | 体素化 probe 网格的 GPU 资源与 per-view bind group(`RenderViewIrradianceVolumeBindGroupEntries` :165) |
| `dev/bevy/crates/bevy_pbr/src/lightmap/mod.rs` | lightmap 消费契约(EL-M3) | lightmap slot/uv_rect 实例数据组织与采样接线(配 `lightmap.wgsl`)—— `LightmapInstanceSlot` 的 Rust 同型 |
| `dev/bevy/crates/bevy_pbr/src/fog.rs` | `FogSettings` 距离雾三模式 | `DistanceFog`(:52)/`FogFalloff`(Linear/Exponential/ExponentialSquared,:17–19)参数契约与文档内衰减公式 |
| `dev/Fyrox/fyrox-impl/src/scene/probe.rs` | 探针捕获(EL-M2)与 ambient 来源 | `ReflectionProbe` 场景节点:捕获 cube 纹理 + `EnvironmentLightingSource`(:176)把探针作为环境光来源的组件契约 |

高度雾(height fog)无 Rust 同类参照(bevy 仅距离/大气雾),实现时以 UE 高度雾为唯一样板,按 index §8 第 8 条配对拍测试先行。

## 目标架构

归属:契约(skybox 设置、探针/lightmap 采样 ABI、雾参数)进 `core/framework/render/`;捕获/预滤波 pass 在 `scene_renderer/` 新增 `environment/`;烘焙器与高级合成留在 rendering 插件。

核心设计:

- `SkyboxSettings`:模式(Procedural | Cubemap)、材质引用、强度/旋转;skybox pass 插在 opaque 之后 transparent 之前(深度 = far,early-z 剔除);preview-sky executor 收编为 Procedural 模式实现,删除独立路径。
- IBL 预滤波:cubemap 导入或捕获后离线/加载期生成 specular mip 链(GGX)与 irradiance SH9;`GpuEnvironmentMap` 资源含两者。
- `ReflectionProbeData`:影响域(box/sphere)、box projection 参数、烘焙 cubemap 引用、优先级;着色端按"对象位置选 ≤2 探针加权 + skybox fallback";探针数据进 GpuScene 风格 buffer(计划 03 模式)。
- lightmap 消费:静态对象 UV2 + 图集页索引(`RendererCommon.is_static` 前提);light probe SH 按 tetrahedral/网格插值留接口,V1 均匀网格。
- `FogSettings`:模式/颜色/密度/起止距离 + 高度雾(基准高度/衰减);WGSL include 同时服务 forward 与 deferred 合成;Volume 可覆写(计划 07 的 volume 组件之一)。

## 里程碑

### EL-M1 skybox 与 IBL

实施切片:
1. `SkyboxSettings` 契约 + skybox pass(两模式);preview-sky 收编。
2. cubemap 资产导入与预滤波(mip 链 + SH);PBR 环境项接 `GpuEnvironmentMap`(替代当前隐式 ambient)。

测试阶段:
- `cargo check -p zircon_runtime --lib --locked`;`cargo test -p zircon_runtime environment --locked` + `render_product` 回归
- 验收证据:金属球在 cubemap 天空下反射正确(抓帧);粗糙度扫描条带对应 mip 链。

### EL-M2 反射探针

实施切片:
1. `ReflectionProbeData` 契约与探针 buffer;着色端双探针混合 + box projection。
2. 捕获 pass(编辑器触发,渲染 6 面 + 预滤波)走 rendering 插件;探针资产持久化。

测试阶段:
- `cargo test -p zircon_runtime environment --locked` 与插件 workspace 测试
- 验收证据:室内/室外两探针交界处反射平滑过渡;feature 关闭时回落 skybox。

### EL-M3 lightmap 与 light probe 消费

实施切片:
1. lightmap 采样 ABI(UV2/图集/编码格式定稿)与 shader 变体 flag(计划 08);probe SH 网格插值。
2. 烘焙器输入输出契约定稿(烘焙实现归 baked_lighting 插件后续计划)。

测试阶段:
- `cargo test -p zircon_runtime environment --locked`(外部烘焙夹具数据渲染对拍)
- 验收证据:夹具 lightmap 场景静态光影正确;动态物从 probe 取间接光。

### EL-M4 解析雾与 ambient 模式

实施切片:
1. `FogSettings` 三模式 + 高度雾;forward/deferred 双路径 include;Volume 覆写组件。
2. ambient 三模式(色/渐变/SH)。

测试阶段:
- `cargo test -p zircon_runtime environment --locked`(雾衰减曲线 readback 断言)
- 验收证据:距离/高度雾抓帧;deferred 与 forward 雾产物一致。

## 工程落地细化

本章节为计划 11 的实施权威(index.md §8.7)。bind group 槽位、storage buffer std430 基线、WGSL `zr_` include 约定、RenderQueueValue 数值段、测试命名直接引用 index.md §8,不再重述。契约类型全部落在 `zircon_runtime::core::framework::render`(facade 固定,无 wgpu);所有 pass 经 RenderGraph 节点 + executor id 接入,无旁路提交;只消费 `RenderFrameExtract`;烘焙器本体经 rendering 插件;一律硬切换,不留兼容路径。

跨计划契约名原样消费:01 `RgTextureHandle`/`TransientResourcePool`;05 `GpuLightData` 与 `zr_light_grid.wgsl`(probe 数据不进 light grid,走本计划独立 buffer,cluster 化留后续与 05 协调);07 `VolumeComponentDescriptor`/`VolumeEvaluator`(`FogSettings` 注册为 Volume 可覆写组件);13 `TextureMetadata`/`CubemapAsset`(本计划只消费,不定义 cubemap 资产格式);16 `ComputePassDescriptor`(IBL 预滤波与 SH 投影 compute 经它声明)。

### 模块与文件落点

新增文件:

| 路径 | 内容 |
|------|------|
| `zircon_runtime/src/core/framework/render/environment/mod.rs` | 模块声明与 curated re-export(薄) |
| `zircon_runtime/src/core/framework/render/environment/cubemap_projection.rs` | equirect→cube/GGX/SH 测试共用的 CPU 黄金投影:face order、texel center UV、lat-long UV、solid angle |
| `zircon_runtime/src/core/framework/render/environment/skybox.rs` | `SkyboxSettings` / `SkyboxMode` / `ProceduralSkyParams` / `CubemapSkyParams` / `IblBakeKey` |
| `zircon_runtime/src/core/framework/render/environment/reflection_probe.rs` | `ReflectionProbeData` / `ProbeInfluenceShape` / `ProbeBakeTiming` |
| `zircon_runtime/src/core/framework/render/environment/fog.rs` | `FogSettings` / `FogMode` / `HeightFogParams` |
| `zircon_runtime/src/core/framework/render/environment/ambient.rs` | `AmbientMode` / `ShL2Rgb`(SH9 系数容器) |
| `zircon_runtime/src/core/framework/render/environment/lightmap.rs` | `LightmapConsumeContract` / `LightmapInstanceSlot` / `LightProbeGridData` / `LightmapBakeRequest` / `LightmapBakeOutput`(DTO,序列化跨插件边界) |
| `zircon_runtime/src/core/framework/render/environment/extract.rs` | `EnvironmentExtract`(进 `RenderFrameExtract` 的环境快照) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/mod.rs` | 实现侧模块声明(薄) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/skybox_executor.rs` | skybox pass executor(Procedural / Cubemap 两 pipeline) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/gpu_environment_map.rs` | `GpuEnvironmentMap`(specular mip 链 cube 纹理 + SH9 buffer + bake key 缓存) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_prefilter.rs` | 预滤波/SH 投影 compute pass 构建(经 `ComputePassDescriptor`) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/probe_buffer.rs` | `GpuReflectionProbe` 上传、cube array 槽位分配器 |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/lightmap_binding.rs` | lightmap atlas / probe grid GPU 资源绑定 |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/shaders/skybox_procedural.wgsl` | 程序化天空(吸收 `overlay/shaders/sky.wgsl` 渐变逻辑,深度 = far) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/shaders/skybox_cubemap.wgsl` | cubemap 天空(rotation/intensity/tint 采样期生效) |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/shaders/ibl_prefilter.wgsl` | GGX 重要性采样 specular mip 烘焙 compute |
| `zircon_runtime/src/graphics/scene/scene_renderer/environment/shaders/ibl_irradiance_sh.wgsl` | SH9 投影 compute(workgroup 归约) |
| `zircon_runtime/src/graphics/shader/includes/zr_fog.wgsl` | 解析雾函数 include(若计划 08 已定 include 目录则随其落点,内容不变) |
| `zircon_runtime/src/graphics/shader/includes/zr_environment.wgsl` | probe 混合 + box projection + SH 求值 + env specular 采样 include |
| `zircon_runtime/src/graphics/tests/render_product_environment.rs` | 环境光照产物对拍测试 |

修改文件:

| 路径 | 改动 |
|------|------|
| `zircon_runtime/src/core/framework/render/mod.rs` | 声明 `environment` 模块,re-export 契约类型 |
| `zircon_runtime/src/core/framework/render/frame_extract.rs` | `RenderFrameExtract` 增加 `environment: EnvironmentExtract` 字段 |
| `zircon_runtime/src/core/framework/render/scene_extract.rs` | `PreviewEnvironmentExtract` 的 `skybox_enabled`/`fallback_skybox` 字段删除,改引 `EnvironmentExtract` |
| `zircon_runtime/src/core/framework/render/camera.rs` | 删除 `FallbackSkyboxKind`;`ViewportRenderSettings::preview_skybox` 改为映射 `SkyboxSettings` 启停 |
| `zircon_runtime/src/core/framework/render/light/snapshots.rs` | `RenderReflectionProbeSnapshot` 删除(被 `ReflectionProbeData` 取代);`RenderBakedLightingExtract` 扩展为 lightmap/probe grid 引用 |
| `zircon_runtime/src/scene/world/render.rs` | extract 端填充 `EnvironmentExtract`;删除 `FallbackSkyboxKind::ProceduralGradient` 映射(L911–915) |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry.rs` | 注册 `skybox`/`ibl_prefilter`/`ibl_irradiance_sh` executor;删除 `preview_sky_scene_color_executor`/`preview_sky_final_color_executor` 注册 |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu.rs` | 删除 `record_preview_sky_to_resources`/`with_preview_sky_renderer`;新增 environment 资源访问 |
| `zircon_runtime/src/graphics/feature/builtin_render_feature_descriptor/feature_descriptors/mesh.rs`、`deferred_geometry.rs` | preview-sky pass 声明改为 skybox pass 节点(opaque 后、transparent 前) |
| `zircon_runtime/src/graphics/scene/scene_renderer/primitives/scene_uniform/from_frame.rs` | `authored_ambient_color` 改读 resolved ambient SH band0;删除 preview lighting 的硬编码 ambient 常量分支 |
| `zircon_runtime/src/graphics/scene/scene_renderer/mesh/shaders/fallback_mesh.wgsl` | shading 末尾接 `zr_apply_fog`;环境项接 `zr_environment.wgsl` |
| `zircon_runtime/src/graphics/scene/scene_renderer/deferred/shaders/deferred_lighting.wgsl` | 合成末尾同上(与 forward 同一 include,产物一致) |
| `zircon_runtime/src/graphics/scene/resources/gpu_texture/mod.rs` | cube / cube array 维度支持(消费计划 13 `CubemapAsset` 上传产物) |
| `zircon_plugins/rendering/plugin.toml` 及 `rendering.reflection_probes.*`、`rendering.baked_lighting.*` crate | 探针捕获调度(渲染 6 面 + 调 runtime 预滤波)、烘焙器接口实现 |

preview-sky 硬切换删除清单(EL-M1 切片 1 同一变更内完成,详见"帧时序与集成点"):`graph_execution/preview_sky_executor.rs`、`overlay/passes/preview_sky_pass.rs`、`overlay/viewport_overlay_renderer/record/scene_content/record_preview_sky.rs`、`overlay/shaders/sky.wgsl`、`overlay/passes/pass_order.rs` 的 `"PreviewSkyPass"` 项、`overlay/viewport_overlay_renderer/construct/new.rs` 的 `preview_sky` 字段。

### 核心类型与接口

契约层(`core/framework/render/environment/`,纯数据可序列化,无 wgpu):

```rust
// skybox.rs
pub enum SkyboxMode {
    Procedural(ProceduralSkyParams),
    Cubemap(CubemapSkyParams),
}

pub struct ProceduralSkyParams {
    pub zenith_color: Vec3,
    pub horizon_color: Vec3,
    pub ground_color: Vec3,
    pub horizon_falloff: Real,     // 渐变指数,默认 1.0
    pub sun_disk_enabled: bool,
    pub sun_disk_size_degrees: Real,
    pub sun_disk_intensity: Real,
}

pub struct CubemapSkyParams {
    pub cubemap: AssetId,          // 计划 13 CubemapAsset 引用
    pub tint: Vec3,
}

pub struct SkyboxSettings {
    pub mode: SkyboxMode,
    pub intensity: Real,           // 采样期乘子,不进 bake key
    pub rotation_y_degrees: Real,  // 采样期旋转方向向量,不进 bake key
    pub ibl_enabled: bool,         // 关闭时环境项退回 ambient 模式
}

/// 重烘判定键:mode 判别值 + Procedural 参数 hash 或 cubemap 内容版本 + 质量档。
/// intensity / rotation 显式排除(采样期生效)。
pub struct IblBakeKey(pub u64);

// reflection_probe.rs
pub enum ProbeInfluenceShape {
    Box { half_extents: Vec3, blend_distance: Real },
    Sphere { radius: Real, blend_distance: Real },
}

pub enum ProbeBakeTiming { EditorManual, RuntimeManual }  // V1 无自动重烘

pub struct ReflectionProbeData {
    pub probe_id: u64,                       // 稳定 id(对齐 05 light id 机制)
    pub position: Vec3,
    pub rotation: Quat,
    pub shape: ProbeInfluenceShape,
    pub box_projection: bool,
    pub projection_half_extents: Vec3,       // 投影盒可与影响域不同
    pub baked_cubemap: Option<AssetId>,      // 预滤波后的 CubemapAsset;None = 未烘焙,着色端跳过
    pub intensity: Real,
    pub priority: i32,                       // 同权重并列时高优先
    pub layer_mask: RenderLayerSet,          // 计划 09 同一 mask
    pub bake_timing: ProbeBakeTiming,
}

// fog.rs
pub enum FogMode {
    None,
    Linear { start: Real, end: Real },
    Exponential { density: Real },
    ExponentialSquared { density: Real },
}

pub struct HeightFogParams {
    pub enabled: bool,
    pub base_height: Real,    // 世界 Y,低于此高度雾全强
    pub falloff: Real,        // 高度衰减系数(1/m)
    pub max_opacity: Real,    // [0,1],雾不透明度上限
}

pub struct FogSettings {
    pub mode: FogMode,
    pub color: Vec3,
    pub height: HeightFogParams,
    pub affects_skybox: bool,
}

// ambient.rs
pub struct ShL2Rgb(pub [Vec3; 9]);   // 9×RGB f32,band 顺序 L00,L1-1,L10,L11,L2-2,L2-1,L20,L21,L22

pub enum AmbientMode {
    Flat { color: Vec3, intensity: Real },                       // 收编 RenderAmbientLightSnapshot
    Gradient { sky: Vec3, equator: Vec3, ground: Vec3, intensity: Real },
    SkyboxSh,                                                    // 消费 GpuEnvironmentMap 的 SH9
}

// lightmap.rs
pub struct LightmapInstanceSlot {
    pub atlas_page: u32,           // Texture2DArrayAsset 切片索引(计划 13)
    pub uv_rect: Vec4,             // scale.xy + offset.xy,UV2 → atlas 页变换
}

pub struct LightmapConsumeContract {
    pub atlas: AssetId,            // RGBA16F Texture2DArrayAsset
    pub slots: Vec<(u64, LightmapInstanceSlot)>,   // renderer 稳定 id → slot
}

pub struct LightProbeGridData {    // V1 均匀网格,接口不锁死插值结构
    pub bounds_min: Vec3,
    pub cell_size: Vec3,
    pub dims: [u32; 3],
    pub sh: Vec<ShL2Rgb>,          // dims.x*y*z 个
}

// extract.rs —— 进 RenderFrameExtract,extract 仅由 runtime 生成
pub struct EnvironmentExtract {
    pub skybox: Option<SkyboxSettings>,
    pub fog: FogSettings,                       // 已经 VolumeEvaluator 解析后的最终值(计划 07)
    pub ambient: AmbientMode,
    pub probes: Vec<ReflectionProbeData>,
    pub baked_lighting: Option<LightmapConsumeContract>,
    pub probe_grid: Option<LightProbeGridData>,
}
```

归属裁决:`FogSettings` 同时注册为计划 07 的 Volume 组件(`VolumeComponentDescriptor` schema 见下表);Volume 求值发生在 extract 之前,`EnvironmentExtract.fog` 已是解析后终值,渲染侧不再做 volume 插值。烘焙器(lightmap/probe 捕获)实现归 `rendering.baked_lighting` / `rendering.reflection_probes` 插件,跨边界只走 `LightmapBakeRequest`/`LightmapBakeOutput` 序列化 DTO(`zircon_runtime_interface` ABI 规则:不传 trait 对象/wgpu 对象)。

`FogSettings` Volume 可覆写字段表(mode 取最高权重 volume 的判别值,不插值;其余 lerp):

| 字段 | 插值 | 默认 |
|------|------|------|
| `color` | lerp | (0.5, 0.6, 0.7) |
| `Linear.start` / `Linear.end` | lerp | 30 / 300 |
| `Exponential.density` / `ExponentialSquared.density` | lerp | 0.01 |
| `height.base_height` / `height.falloff` / `height.max_opacity` | lerp | 0 / 0.1 / 1.0 |
| `mode` / `affects_skybox` / `height.enabled` | 权重最高者 | None / true / false |

### GPU 数据布局与 WGSL 约定

binding 编号:环境段固定占用 `group1`(pass 级)的 `@binding(16..23)` 区段,与计划 05 light grid/shadow 的低位段错开;material(`group2`)、instance(`group3`)不变。

| group/binding | 资源 | 类型 |
|---|---|---|
| 1/16 | `zr_env_probes` | `var<storage, read> array<GpuReflectionProbe>` |
| 1/17 | `zr_env_probe_header` | uniform(`probe_count: u32` + pad) |
| 1/18 | `zr_env_probe_cubemaps` | `texture_cube_array<f32>`(预滤波 mip 链) |
| 1/19 | `zr_env_sky_specular` | `texture_cube<f32>`(skybox 预滤波 mip 链,fallback 源) |
| 1/20 | `zr_env_sampler` | `sampler`(linear,clamp) |
| 1/21 | `zr_env_sh` | `var<storage, read> array<vec4<f32>, 9>`(active ambient SH9) |
| 1/22 | `zr_fog_uniform` | uniform `ZrFogParams` |
| 1/23 | 预留(probe SH grid buffer,EL-M3) | storage |

`GpuReflectionProbe`(std430,96 bytes,offset 注释):

```wgsl
struct GpuReflectionProbe {
    position_blend: vec4<f32>,  // 0:  xyz=position, w=blend_distance
    box_min: vec4<f32>,         // 16: xyz=influence min(世界轴对齐前的本地),w=f32(priority)
    box_max: vec4<f32>,         // 32: xyz=influence max, w=shape(0=box, 1=sphere)
    proj_params: vec4<f32>,     // 48: xyz=projection_half_extents, w=box_projection(0/1)
    rotation: vec4<f32>,        // 64: 单位四元数(世界→probe 本地)
    misc: vec4<f32>,            // 80: x=intensity, y=mip_count, z=array_slice, w=bitcast(layer_mask)
};                              // total 96
```

SH9 存储:9×`vec4<f32>`(RGB + pad)= 144 bytes,storage buffer;CPU 侧 `ShL2Rgb` 与之一一对应,投影 compute 输出后异步 readback 回写资产持久化(探针/天空共用同一布局)。

`zr_fog.wgsl`(URP `unity_FogParams` 打包思路,见精读笔记):

```wgsl
struct ZrFogParams {
    color: vec4<f32>,          // rgb=fog color, a=affects_skybox(0/1)
    params: vec4<f32>,         // mode 打包:Linear: z=-1/(end-start), w=end/(end-start)
                               //           Exp/Exp2: x=density/ln(2)(exp2 域)
    height_params: vec4<f32>,  // x=base_height, y=falloff, z=max_opacity, w=enabled
    mode: vec4<u32>,           // x=FogMode 判别值(0/1/2/3)
};

// 返回"剩余透过率"(1=无雾,0=全雾),与 URP fogIntensity 同语义
fn zr_fog_factor(view_distance: f32, world_height: f32, fog: ZrFogParams) -> f32;
fn zr_apply_fog(color: vec3<f32>, view_distance: f32, world_pos: vec3<f32>,
                fog: ZrFogParams) -> vec3<f32>;   // mix(fog.color.rgb, color, factor)
```

公式定稿:Linear `f = saturate(d * params.z + params.w)`;Exp `f = saturate(exp2(-density_ln2 * d))`;Exp2 `f = saturate(exp2(-(density_ln2 * d)^2))`;height fog 乘项 `h = mix(1.0, saturate(exp(-(world_height - base_height) * falloff)), height.enabled)`,合成 `f_final = max(1 - (1 - f * (1 - h * (1 - f))), 1 - max_opacity)` 简化实现为:`opacity = (1 - f) * h_weight`,`opacity = min(opacity, max_opacity)`,`return 1 - opacity`(单测对拍闭式)。

box projection(`zr_environment.wgsl`,UE `GetLookupVectorForBoxCapture` 的 AABB 求交式):

```wgsl
fn zr_box_project(reflect_dir: vec3<f32>, world_pos: vec3<f32>,
                  probe: GpuReflectionProbe) -> vec3<f32> {
    let p = zr_quat_rotate_inv(probe.rotation, world_pos - probe.position_blend.xyz);
    let d = zr_quat_rotate_inv(probe.rotation, reflect_dir);
    let inv_d = 1.0 / d;
    let t1 = (-probe.proj_params.xyz - p) * inv_d;   // 三个 min 面交点
    let t2 = ( probe.proj_params.xyz - p) * inv_d;   // 三个 max 面交点
    let t_far = max(t1, t2);
    let t = min(t_far.x, min(t_far.y, t_far.z));     // 最近的"最远面"
    let hit = world_pos + t * reflect_dir;
    return hit - probe.position_blend.xyz;           // 修正后的采样向量
}
```

≤2 探针混合权重(URP `CalculateProbeWeight` 式边缘权重 + 排序截断):每像素全量扫 probe buffer(V1 上限 64,extract 端按 layer/距离裁到该数),单 probe 权重 `w = saturate(min_axis((p_edge_dist) / blend_distance))`(box 取三轴边距最小值,sphere 取 `(radius - dist) / blend_distance`);取权重最大两个(权重并列比 priority),`w1 = min(w1, 1 - w0)`,skybox fallback 权重 `w_sky = 1 - w0 - w1`;三项各按自身校正向量采 `mip = zr_env_mip_from_roughness(roughness)` 后线性加权。roughness→mip 采用 UE 对数映射(常数 `ZR_IBL_ROUGHEST_MIP = 1.0`、`ZR_IBL_MIP_SCALE = 1.2`):`mip = max_mip - 1 - (ZR_IBL_ROUGHEST_MIP - ZR_IBL_MIP_SCALE * log2(max(roughness, 0.001)))`;烘焙端按逆映射给每 mip 的 roughness,保证 mip 数无关的粗糙度一致性。

IBL 预滤波样本数档位表(`ibl_prefilter.wgsl` 特化常量):

| 档位 | mip0 | 中段 mip | 末两级 mip | 用途 |
|------|------|---------|-----------|------|
| Fast | 直拷 | 32 | 64 | 编辑器拖参数实时迭代 |
| Normal | 直拷 | 64 | 128 | 默认导入/捕获 |
| High | 直拷 | 128 | 256 | 显式高质量重烘 |

cubemap 基准 128×128×6,mip 数 = `log2(128)+1 = 8`(UE `GetNumMips = CeilLogTwo(CaptureSize)+1` 同式);SH9 投影 compute 按面分 workgroup 归约(立体角加权),输出 9×vec4 storage。

### 帧时序与集成点

帧内顺序(全部经 graph 节点声明,executor id 注册在 `render_pass_executor_registry.rs`):

1. **prepare 阶段**:`gpu_environment_map.rs` 比对 `IblBakeKey`,脏则经 `ComputePassDescriptor` 入队 `env.ibl_prefilter`(逐 mip 一 dispatch)+ `env.ibl_irradiance_sh` 两个 compute 节点;Procedural 模式先跑 `env.sky_capture`(渐变烘成 cubemap mip0)再预滤波。重烘当帧完成(128³ 成本低);probe 捕获(6 面渲染)只由编辑器/运行期手动触发,经 `rendering.reflection_probes` 插件 feature 的 capture 调度复用同一预滤波节点。
2. **skybox pass**:节点声明 `SCENE_COLOR` write + `SCENE_DEPTH` read(depth test `LessEqual`,depth write off,全屏三角形顶点深度置 far)——插在 opaque 之后、transparent 之前(URP `DrawSkyboxPass` 同位,享受 early-z);Procedural/Cubemap 各一 pipeline,按 `SkyboxMode` 选。`EnvironmentExtract.skybox == None` 时 compiled graph 不含该节点(约束 4)。
3. **shading 中的环境项**:forward(`fallback_mesh.wgsl` 与计划 08 模板)与 deferred lighting 合成共用 `zr_environment.wgsl`:specular = probe 混合 + skybox fallback;diffuse ambient = `zr_env_sh` 求值(三模式统一为 SH9:Flat 只写 band0 = color×intensity;Gradient 用解析投影闭式写 L0+L1;SkyboxSh 直接用预滤波产物 SH)。shader 内无 ambient 模式分支。
4. **雾**:`zr_apply_fog` 在 forward shading 输出前、deferred lighting 合成输出前各调用一次(同一 include 保证产物一致);skybox pass 按 `affects_skybox` 在自身输出端调用(view_distance 取 z_far)。**与计划 07 边界**:本计划的解析雾/高度雾发生在 shading/lighting pass 内,不新增 post pass;07 的屏幕空间雾(散射/体积感,SSR 槽位附近)是独立 post 节点,消费同一 `FogSettings.color` 但有独立强度参数,两者共存时由 07 的 volume stack 管理开关,本计划不做屏幕空间合成。

preview-sky 硬切换(EL-M1 切片 1,同一变更):删除"模块与文件落点"清单所列 6 个文件/条目,并同步删除 `render_pass_execution_context/gpu.rs` 的 `record_preview_sky_to_resources`/`with_preview_sky_renderer`、`camera.rs` 的 `FallbackSkyboxKind`、`scene/world/render.rs` 的映射分支与 `scene_extract.rs` 的 `skybox_enabled`/`fallback_skybox` 字段;`ViewportRenderSettings::preview_skybox = true` 改为 extract 出默认 `SkyboxSettings { mode: Procedural(默认渐变), .. }`,编辑器预览行为不变。受影响测试(`graphics/tests/pipeline_compile.rs`、`scene_overlay.rs`、`scene/tests/render_extract.rs` 等)同变更内改断言,不留旧符号。

GPUScene 衔接(计划 03):`LightmapInstanceSlot`(uv_rect + atlas_page)进 instance 数据扩展位;`RendererCommon.is_static` 为 lightmap 采样前提,shader 变体 flag(`LIGHTMAP_ON` 等价物)走计划 08 permutation。

### 实施切片细化

**EL-M1 / 切片 1 —— SkyboxSettings 契约 + skybox pass + preview-sky 收编**
- 触碰:新增 `environment/skybox.rs`、`environment/extract.rs`、`skybox_executor.rs`、两个 skybox wgsl;修改 `mod.rs`、`frame_extract.rs`、`scene_extract.rs`、`camera.rs`、`scene/world/render.rs`、executor registry、`feature_descriptors/{mesh,deferred_geometry}.rs`;删除 preview-sky 清单全部文件。
- 要点:pass 位置与深度策略按"帧时序"§2;Procedural 渐变公式从 `sky.wgsl` 平移并参数化(zenith/horizon/ground)。
- 完成判据:`cargo check -p zircon_runtime --lib --locked` 过;仓库内 `preview_sky` 零命中(`grep -r preview_sky zircon_runtime/src` 为空);编辑器视口天空渲染行为与改前一致。

**EL-M1 / 切片 2 —— IBL 预滤波 + GpuEnvironmentMap 接 PBR 环境项**
- 触碰:新增 `gpu_environment_map.rs`、`ibl_prefilter.rs`、`ibl_prefilter.wgsl`、`ibl_irradiance_sh.wgsl`、`zr_environment.wgsl`;修改 `gpu_texture/mod.rs`(cube 维度)、`fallback_mesh.wgsl`、`deferred_lighting.wgsl`、`scene_uniform/from_frame.rs`。
- 要点:bake key 判定与档位表;roughness→mip 常数定稿;环境 specular/diffuse 替代现有隐式 ambient 常量。依赖计划 13 TX-M3 的 `CubemapAsset` 导入(联调用例共享)。
- 完成判据:粗糙度 0→1 扫描条带对应 mip 链(抓帧);`render_env_*` M1 测试组绿。

**EL-M2 / 切片 1 —— ReflectionProbeData 契约 + probe buffer + 着色混合**
- 触碰:新增 `environment/reflection_probe.rs`、`probe_buffer.rs`;修改 `light/snapshots.rs`(删 `RenderReflectionProbeSnapshot`)、`extract.rs`、`zr_environment.wgsl`(混合 + box projection)、binding 16–18 接线。
- 要点:96-byte 布局与 offset 断言;≤2 混合 + fallback 权重法;cube array 槽位分配器(2 的幂槽,LRU 驱逐)。
- 完成判据:CPU 参考实现与 WGSL 权重/投影对拍;`render_probe_*` M2 单测绿。

**EL-M2 / 切片 2 —— 捕获 pass 经 rendering 插件 + 探针资产持久化**
- 触碰:`zircon_plugins/rendering` 的 reflection_probes runtime/editor crate、`plugin.toml`;runtime 侧暴露捕获请求入口(序列化 DTO)与预滤波节点复用。
- 要点:编辑器触发渲染 6 面(临时 RT 经 `TransientResourcePool`)→ 预滤波 → 写 `CubemapAsset`;feature 关闭时 graph 无捕获节点且着色回落 skybox。
- 完成判据:插件 workspace 测试绿;两探针交界过渡平滑(产物对拍);关 feature 后 graph dump 无 probe pass。

**EL-M3 / 切片 1 —— lightmap 采样 ABI + probe grid 插值**
- 触碰:新增 `environment/lightmap.rs`、`lightmap_binding.rs`;GPUScene instance 扩展(协调计划 03);shader 变体 flag(协调计划 08);binding 23 probe grid buffer。
- 要点:UV2 = 顶点第二 UV 通道,`uv_rect` 变换定稿;atlas 用 RGBA16F `Texture2DArrayAsset`(encoding 结论见下);probe grid GPU 端 trilinear(对象用 instance 世界位置采样,避免逐实例 CPU 上传)。
- 完成判据:外部烘焙夹具数据渲染对拍;`render_env_lightmap_*` 测试绿。
- encoding 结论:**RGBA16F(half)**。理由:wgpu 各后端对 `rgba16float` 过滤采样全覆盖、无 RGBM 解码乘加与低亮度 banding、HDR 余量足;RGBM8 仅作未来移动档可选项,不进 V1;BC6H 压缩留给 importer 插件后续转码,消费 ABI 不变。

**EL-M3 / 切片 2 —— 烘焙器输入输出契约定稿**
- 触碰:`environment/lightmap.rs` 的 `LightmapBakeRequest { scene_snapshot, atlas_budget, texel_density }` / `LightmapBakeOutput { atlas_pages, slots, probe_grid }` DTO;`rendering.baked_lighting` 插件 manifest 声明烘焙 capability。
- 要点:runtime 不含烘焙实现;DTO 序列化 round-trip 测试;atlas 分配(skyline/shelf 二选一,插件内实现,runtime 只认 `uv_rect`)。
- 完成判据:DTO round-trip + 契约静态断言绿;夹具 `LightmapBakeOutput` 喂给 runtime 渲染正确。

**EL-M4 / 切片 1 —— FogSettings + zr_fog.wgsl + Volume 覆写**
- 触碰:新增 `environment/fog.rs`、`zr_fog.wgsl`;修改两条 shading 路径 wgsl、skybox wgsl、binding 22;向计划 07 注册 fog 的 `VolumeComponentDescriptor` schema(覆写字段表见上)。
- 要点:三模式 + 高度雾公式定稿(见 GPU 约定节);extract 携带解析后终值。
- 完成判据:`render_fog_*` 闭式对拍绿;forward/deferred 雾产物一致(对拍);相机进雾 volume 时参数平滑过渡(07 的求值测试覆盖,本计划只消费)。

**EL-M4 / 切片 2 —— ambient 三模式**
- 触碰:`environment/ambient.rs`、`scene_uniform/from_frame.rs`、`zr_environment.wgsl` SH 求值、extract 端 `RenderAmbientLightSnapshot` → `AmbientMode::Flat` 收编。
- 要点:三模式统一 SH9 表示(Flat=band0、Gradient=解析 L0+L1 投影、SkyboxSh=预滤波产物);shader 单路径。
- 完成判据:三模式切换产物差异符合预期(对拍);`render_env_ambient_*` 绿。

### 测试与验收清单

单元测试(随实现文件就近放 `#[cfg(test)]`,模块过滤词 `environment`):

| 测试函数 | 断言 | 位置 |
|---|---|---|
| `render_env_bake_key_ignores_intensity_and_rotation` | 仅 intensity/rotation 变化时 `IblBakeKey` 不变;渐变参数/cubemap 版本变化时改变 | `environment/skybox.rs` |
| `render_env_mip_from_roughness_roundtrip` | 烘焙端逆映射与采样端映射互逆(8 mip 全档,误差 < 1e-4) | `ibl_prefilter.rs` |
| `render_env_sh9_constant_color_projects_to_band0_only` | 常色 cubemap 投影后 L1/L2 系数 ≈ 0,band0 = 颜色×归一化常数 | `ibl_prefilter.rs`(CPU 参考)|
| `render_env_ambient_gradient_sh_matches_analytic` | Gradient 闭式投影与数值积分对拍 | `environment/ambient.rs` |
| `render_env_extract_skybox_none_removes_graph_node` | `skybox: None` 时 compiled graph 无 skybox 节点 | `graphics/tests/pipeline_compile.rs` |
| `render_probe_gpu_layout_is_96_bytes_with_documented_offsets` | `size_of::<GpuReflectionProbe>() == 96` + 各字段 offset 静态断言 | `probe_buffer.rs` |
| `render_probe_weight_box_edge_matches_blend_distance` | 边缘内 blend_distance 处权重线性 0→1,中心饱和 1 | `probe_buffer.rs`(CPU 参考)|
| `render_probe_two_probe_blend_weights_sum_to_one_with_sky_fallback` | w0+w1+w_sky == 1;无探针时 w_sky == 1 | `probe_buffer.rs` |
| `render_probe_box_projection_axis_ray_hits_face_center` | 盒心沿 +X 反射,校正向量命中 +X 面中心(CPU 复刻 WGSL 公式) | `probe_buffer.rs` |
| `render_probe_slot_allocator_evicts_lru_on_pressure` | cube array 槽位满时按 LRU 驱逐且老句柄失效 | `probe_buffer.rs` |
| `render_env_lightmap_uv_rect_transform_roundtrip` | UV2 经 `uv_rect` 变换落在页内,逆变换还原 | `environment/lightmap.rs` |
| `render_env_lightmap_bake_dto_serde_roundtrip` | `LightmapBakeRequest`/`Output` 序列化 round-trip 等值 | `environment/lightmap.rs` |
| `render_env_probe_grid_trilinear_center_equals_cell_average` | 网格中心插值 = 8 邻 cell 均值 | `environment/lightmap.rs` |
| `render_fog_linear_factor_matches_closed_form` | 与 `(end-d)/(end-start)` 闭式逐点对拍(start/end/边界外) | `environment/fog.rs` |
| `render_fog_exp2_monotonic_and_clamped` | 距离单调递减、[0,1] 夹紧 | `environment/fog.rs` |
| `render_fog_height_max_opacity_clamps` | 深谷远距下不透明度 ≤ max_opacity | `environment/fog.rs` |
| `render_fog_volume_schema_fields_match_contract` | 注册到 07 的 schema 字段集与覆写字段表一致 | `environment/fog.rs` |

产物对拍(`zircon_runtime/src/graphics/tests/render_product_environment.rs`,配合 `ZR_RENDERDOC_CAPTURE_NEXT=1` 人工抓帧):

| 场景 | 断言 |
|---|---|
| `render_product_environment_skybox_procedural_after_opaque` | skybox 像素只出现在深度 == far 区域(early-z 生效);与旧 preview-sky 基线图容差内一致 |
| `render_product_environment_cubemap_roughness_ladder` | 金属球粗糙度阶梯反射模糊单调,相邻档位 SSIM 阈值 |
| `render_product_probe_blend_boundary_smooth` | 两探针交界采样带无突变(逐行梯度上限断言) |
| `render_product_probe_feature_off_falls_back_to_skybox` | 关 `rendering.reflection_probes` feature 后产物 == 纯 skybox 基线 |
| `render_product_fog_forward_deferred_consistent` | 同场景两管线雾产物逐像素容差内一致 |
| `render_product_environment_lightmap_fixture` | 烘焙夹具场景与参考图对拍;动态物 ambient 来自 probe grid |

里程碑命令:切片期 `cargo check -p zircon_runtime --lib --locked`;测试阶段 `cargo test -p zircon_runtime environment --locked`、`cargo test -p zircon_runtime render_product_environment --locked`;插件接缝 `cargo test --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_rendering_reflection_probes_runtime --locked`(crate 名见 `zircon_plugins/rendering/plugin.toml`)。

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-07-08 | EL-M1/EC-M3ah interactive PBR HDRI RenderDoc capture hook | render_plan11_interactive_pbr_hdri_renderdoc_vulkan_capture_passed_dx12_shader_unroll_open | 已验证:镜像 Shader 06 EC-M3ah,交互式 PBR/HDRI viewer 已可通过 `D:\Tools\renderdoc\renderdoccmd.exe` 做一次性 RenderDoc 捕获。capture 入口走 wgpu graphics-debugger API,不是 RenderDoc DLL 硬依赖;默认手动 viewer 行为不变。 | `SceneRenderer` 公开 start/stop graphics debugger capture;viewer 新增 `--renderdoc-capture-once --exit-after-capture`;新增 `docs/zircon_runtime/graphics/scene/scene_renderer/renderdoc-capture.md`;更新 viewer 测试文档。Vulkan `.rdc` 为 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_renderdoc_vulkan_20260708_capture.rdc`,6,557,527 bytes;画面 PNG 证据继续使用 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_startup_lifetime_20260708.png`。 | scoped `rustfmt` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过,耗时约 17m16s,仅既有 warnings;`--help` 显示 RenderDoc flags;Vulkan `renderdoccmd capture -w -c ... --face-size 64 --renderdoc-capture-once --exit-after-capture` exit 0,viewer 输出 capture start/complete,生成 `.rdc`;target/cargo-target 同名 `.rdc`/thumb scan 为 0。 | Vulkan RenderDoc capture 路径关闭;DX12 capture 仍 open:现有 deferred-lighting shader 在 RenderDoc/DX12 下触发 FXC `X3511` loop unroll failure。继续 product capture、DX12 shader portability、真实 importer/build staged IBL、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3ag `.zcube` source cubemap + asset-derived `.zribl` staged bundle | render_plan11_zcube_staged_ibl_bundle_passed | 已验证:镜像 Shader 06 EC-M3ag,环境光照资产链新增 staged source cubemap handoff。一个 `IblBakeArtifactRequest` 写 source `.zcube` 到 `render/ibl-source/...`,同时写 asset-derived `.zribl` 到 `render/ibl-derived/...`;source 输入与最终 PMREM/SH9/IEM 反射 artifact 保持不同格式和不同目录,防止 source cubemap 被误作 runtime reflection payload。 | 新增 `ibl_source_cubemap_staging.rs` owner、facade exports 和 `runtime_texture_zcube_staged_ibl_bundle_contract.rs`;模块文档记录 `.zcube`/`.zribl` 双产物、source-only 约束、asset-derived source1 resolve 与 `dispatch=0` 语义。本切片不生成新截图。 | `rustfmt --edition 2021 --check` 覆盖 staging owner/facade/test 通过;E 盘验证前剩余 `80762052608` bytes;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-zcube-staged-bundle-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_texture_zcube_staged_ibl_bundle_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1,测试本体 0.07s,总耗时约 7m17s,仅既有 warnings。 | 关闭 asset-layer staged source/derived handoff;继续真实 `.hdr/.exr` importer/build command、DDS/KTX source cubemap 转换、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3af interactive PBR HDRI viewer startup fix | render_plan11_interactive_pbr_hdri_viewer_startup_fix_passed | 已验证:镜像 Shader 06 EC-M3af,修复交互式 PBR/HDRI 查看器生成 exe 看似无法启动的问题。旧入口在窗口创建前同步加载 HDRI、构建 source cubemap/PMREM 并生成临时项目,导致进程存在但无窗口;现在先创建 `winit` 窗口并呈现启动帧,再加载真实 HDRI 场景。`--face-size 64|128|256|512` 新增为交互启动控制,默认 256。 | 更新 `zircon_app/src/bin/zircon_shader_pbr_viewer/{main,app,args,hdri,scene}.rs` 与查看器测试文档;新增 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_startup_fixed_20260708.png` 与 `docs/tests/runtime/shader/zircon_shader_pbr_viewer_startup_lifetime_20260708.png`,均为 1296x999,显示 HDRI skybox 与镜面球反射。本切片不写 target 截图。 | `rustfmt --edition 2021` 覆盖本轮查看器模块通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过,耗时约 7m25s,仅既有 warnings;`--help` 通过并显示 face-size/鼠标控制;从生成目录启动 `zircon_shader_pbr_viewer.exe --face-size 256` 得到 pid `26996`,80 秒寿命监控显示 3s 已有顶层窗口,40s 恢复 `Responding=True`,80s 仍以标题 `Zircon PBR HDRI Mirror Viewer` 存活并响应,stdout/stderr 日志为空。 | exe 启动失败问题关闭;继续 importer/staged `.zcube` 生成、derived `.zribl` 生产/解析、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3ae interactive PBR HDRI viewer owner split rebuild | render_plan11_interactive_pbr_hdri_viewer_owner_split_rebuild_passed | 已验证:镜像 Shader 06 EC-M3ae,交互式 PBR/HDRI 手动查看器已从临时单文件实现收敛为 owner modules,继续使用 source cubemap + standard PBR 环境反射路径,供人工检查真实 HDRI skybox、镜面球反射、掠射和上下左右视角。 | 更新 `zircon_app/Cargo.toml`;新增 `zircon_app/src/bin/zircon_shader_pbr_viewer/{main,app,args,camera,hdri,presenter,project_assets,scene}.rs`;更新查看器验证文档。新入口 `main.rs` 23 行,最大 child `project_assets.rs` 275 行;源码扫描无 `allow(...)`、`unwrap(...)`、`expect(...)`、`panic!`。本切片不生成新 PNG,也不写 target 截图。 | `rustfmt --edition 2021 --check` 覆盖 8 个查看器模块通过;E 盘编译前剩余 `58433208320` bytes;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过,耗时约 7m05s/426.3s,仅既有 warnings;`--help` 通过;可执行文件 `E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe`,69,835,264 bytes;`Start-Process` 以 Poly Haven Lakes 2K HDRI 首次启动 pid `42132`,重新打开当前可操作窗口 pid `35272`,两次进程检查均为 `Responding=True`。 | 手动目视验证可继续通过该窗口进行;后续仍需 importer/staged `.zcube` 接线、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3ad external DDS/KTX cubemap source-only guard | render_plan11_external_dds_ktx_cubemap_source_only_guard_passed | 已验证:镜像 Shader 06 EC-M3ad,环境光照资产链新增外部 DDS/KTX source cubemap 分类守卫,完整 mip 的六面 DDS/KTX1/KTX2 cubemap 只作为 IBL bake/source 输入,不会被 generic compressed texture upload 当作材质贴图或 final reflection payload。 | 新增 `external_source_cubemap.rs`,在 `TextureAsset::upload_readiness(...)` 的 container path 进入 DDS/KTX upload planner 前拦截 source cubemap;公开 `external_source_cubemap_container_info(...)`、typed kind/error/info 和 source-only reason;新增 `runtime_texture_external_cubemap_source_only_contract.rs` 覆盖 DDS/KTX1/KTX2、普通 2D DDS 不误伤和 incomplete source mip chain;新增/更新 texture source container 文档。本切片不生成新 PNG。 | `rustfmt --edition 2021` 覆盖本轮 Rust 文件通过;E 盘验证前剩余 `70521638912` bytes;首轮新增测试因顶层 asset facade 漏导出 helper 失败,补导出后 `CARGO_TARGET_DIR=E:\cargo-targets\zircon-external-cubemap-source-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_texture_external_cubemap_source_only_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 5/5;同 target `cargo test -p zircon_runtime --test runtime_texture_zcube_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2;`git diff --check` 触及 tracked 文件 warning-only(LF/CRLF),新增 doc/test trailing-whitespace 扫描为空。 | 关闭外部 DDS/KTX source cubemap 误上传防线;继续 importer/staged `.zcube` 生成、derived `.zribl` 生产/解析、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3ac interactive PBR HDRI viewer rebuild handoff | render_plan11_interactive_pbr_hdri_viewer_rebuild_handoff_passed | 已验证:镜像 Shader 06 EC-M3ac,重新编译并启动交互式 PBR/HDRI 查看器,供人工验证真实 HDRI skybox、PBR 镜面反射和相机上下左右旋转后的视角一致性。 | 可执行文件 `E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe`,69,813,760 bytes;默认/当前 HDRI `E:\Git\ZirconEngine\docs\tests\runtime\shader\assets\polyhaven_lakes_2k.hdr`;左键拖拽 orbit camera,滚轮 zoom。本切片不生成新 PNG,也不写 target 截图。 | E 盘编译前剩余 `73948434432` bytes;`cargo build -p zircon_app --bin zircon_shader_pbr_viewer --features default-platform --locked --jobs 1` 通过,耗时 17m07s,仅既有 warnings;`--help` 通过;`Start-Process` 启动成功,pid `50400`。 | 手动目视验证可继续通过该窗口进行;后续仍需 importer/staged `.zcube` 接线、product/RenderDoc capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3ab `.zcube` source cubemap container + RGBA16F shared source-only guard | render_plan11_zcube_source_container_source_only_guard_passed | 已验证:镜像 Shader 06 EC-M3ab,环境光照资产链新增 `.zcube` source-only cubemap 容器,用于 staged/importer 后续写入源 cubemap 全 mip,与 `.zribl` 的 PMREM/SH9/IEM derived artifact 明确分离。新增共享 `rgba16f.rs`,`.zribl` payload 与 `.zcube` source payload 复用 RGBA16F 编解码,减少半浮点布局漂移。 | `.zcube` 解码要求 magic/version/6 faces/完整 mip 链/精确 payload 长度;generic texture upload readiness 对 `.zcube` fail closed,说明其是 IBL bake source container,不是 PMREM 或 direct material upload。新增 texture container 文档、RGBA16F helper 文档,并同步 artifact/source-cubemap 文档。本切片不生成新 PNG。 | `rustfmt --edition 2021 --check` 覆盖本轮 Rust 文件通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-zcube-source-0708 cargo test -p zircon_runtime --test runtime_texture_zcube_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` 通过 21/21;本轮未新增截图,也未向 target 写图片。 | 关闭 source `.zcube` container 与 direct-upload/`.zribl` 混用防线;仍需 importer/staged command 接线、外部 DDS/KTX 含 mip source-only importer tests、RenderDoc/product capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3aa asset-derived IBL artifact store + source-1 dispatch/seam guard | render_plan11_asset_derived_ibl_store_dispatch_seam_passed | 已验证:镜像 Shader 06 EC-M3aa,环境光照来源 1 asset-derived companion artifact store 已落地,importer/staged build 后续可把 PMREM/SH9(+IEM) `.zribl` 写入 `library/render/ibl-derived/...`;runtime dispatch 合并 source1/source2 时 source1 胜出且 environment compute dispatch=0。新增 `ibl_bake_artifact_asset_derived.rs`,共享 request hash 但与 runtime cache 分目录;独立测试覆盖写读、source1>runtime cache、stale algorithm rejection、PMREM seam preserved。 | 更新 `docs/zircon_runtime/asset/ibl-bake-artifact-asset-derived.md`,`docs/zircon_runtime/asset/artifact.md`,`docs/zircon_runtime/asset/ibl-bake-artifact-runtime-writeback.md`,`docs/zircon_runtime/core/framework/render/environment/ibl-bake-artifact.md`;本切片不生成新 PNG。 | `rustfmt --check` 通过;E 盘剩余空间 `55741014016` bytes;早先 Cargo 包装冷编译因 0-byte object 退出 `-1`,按策略执行 `cargo clean -p zircon_runtime --target-dir E:\cargo-targets\zircon-ibl-asset-derived-0708` removed 35 files/1.1GiB;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1` 通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_asset_derived_contract --no-run --no-default-features --features core-min --locked --jobs 1` 通过;直接运行 `E:\cargo-targets\zircon-ibl-asset-derived-0708\debug\deps\runtime_environment_ibl_bake_asset_derived_contract-10ff0cfb48251da9.exe --nocapture --test-threads=1` 通过 3/3,1.52s,日志 `docs/tests/runtime/render/plan11_ibl_asset_derived_contract_direct_20260708.raw.log` 与 `.exit.txt` exit 0;first wrapper attempts did not enter tests and are not passing evidence。 | 关闭 source1 companion store + source1>source2 dispatch=0/seam gate;仍需 TX-M3 `.zcube` importer/staged command 接线、外部含 mip DDS/KTX importer source-only 守卫、RenderDoc/product capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3z live IEM graph readback directional guard | render_plan11_live_iem_graph_readback_directional_passed | 已验证:镜像 Shader 06 EC-M3z,live WGPU IEM graph output 可经 graph readback 写入 `.zircon-cache` runtime artifact,二次 resolve 为 runtime-cache 命中且 `dispatch=0`,并保持方向性 irradiance 响应。 | `ibl_bake_runtime_writeback.rs` 新增 `runtime_graph_writeback_reads_iem_graph_output_and_preserves_directional_irradiance`,覆盖低频方向性 source cubemap、CPU IEM 参考、IEM graph pass 编码、runtime cache 写回、payload decode 与归一化 luma RMS/相关性检查;更新 runtime writeback/WGPU readback/source-cubemap/artifact 文档。本切片不生成新 PNG,也不改交互查看器。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_runtime_writeback.rs` 与 `rustfmt --check` 通过;E 盘空间 `78243672064` bytes,高于 50GB 阈值,未清理 `E:\cargo-targets\zircon-ibl-live-iem-readback-0708`;Cargo 包装测试在冷编译 1204s 超时不计通过,随后直接运行 `E:\cargo-targets\zircon-ibl-live-iem-readback-0708\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe graphics::scene::scene_renderer::environment::ibl_bake_runtime_writeback::tests::runtime_graph_writeback_reads_iem_graph_output_and_preserves_directional_irradiance --exact --nocapture --test-threads=1` 通过 1/1,8.14s,7312 filtered。 | 关闭 live IEM readback/cache directional gate;仍需 product scheduler/root cache 接入、二次启动 dispatch=0、importer/staged artifact 对拍、RenderDoc/product capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3y live PMREM graph readback seam guard | render_plan11_live_pmrem_graph_readback_seam_passed | 已验证:镜像 Shader 06 EC-M3y,live WGPU PMREM graph output 可经 graph readback 写入 `.zircon-cache` runtime artifact,二次 resolve 为 runtime-cache 命中且 `dispatch=0`,并保持 mid/rough PMREM seam 量化结果。 | `ibl_bake_runtime_writeback.rs` 新增 `runtime_graph_writeback_reads_pmrem_graph_output_and_preserves_readback_seams`,覆盖 seam-stress source cubemap、全 mip source cube 绑定、PMREM graph pass 编码、runtime cache 写回、payload decode 与 cube-edge luma seam 检查;更新 runtime writeback/WGPU readback/source-cubemap/artifact 文档。本切片不生成新 PNG;同时重新编译既有 `zircon_shader_pbr_viewer.exe` 供用户手动拖拽验证。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_runtime_writeback.rs` 与 `rustfmt --check` 通过;E 盘空间 `29803991040` bytes,按策略清理 `E:\cargo-targets\zircon-ibl-live-pmrem-readback-seam-0708`;Cargo 包装测试在冷编译 1204s 超时不计通过,随后直接运行 `E:\cargo-targets\zircon-ibl-live-pmrem-readback-seam-0708\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe graphics::scene::scene_renderer::environment::ibl_bake_runtime_writeback::tests::runtime_graph_writeback_reads_pmrem_graph_output_and_preserves_readback_seams --exact --nocapture --test-threads=1` 通过 1/1,3.28s,7310 filtered;`git diff --check -- zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_runtime_writeback.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过(17m21s),`--help` 通过,二进制 69,803,008 bytes。 | 关闭 live PMREM readback/cache seam gate;仍需 live IEM graph-output parity、product scheduler/root cache 接入、二次启动 dispatch=0、importer/staged artifact 对拍、RenderDoc/product capture、probe capture/blending、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3x WGPU PMREM readback seam guard | render_plan11_wgpu_pmrem_readback_seam_passed | 已验证:镜像 Shader 06 EC-M3x,环境光照 artifact 的 backend WGPU texture readback 边界现在有 PMREM seam 量化守卫。新增测试把 seam-stress PMREM artifact bytes 写入真实 `Rgba16Float` cube texture,把 SH9 bytes 写入 WGPU buffer,再通过 `read_ibl_bake_artifact_wgpu_sections(...)` 读回并组装 payload,确认 artifact bytes 不变且应用回 source cubemap 后 mid/rough cube-edge luma seam 统计保持稳定。 | 新增 backend lib-test `readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip`;更新 `docs/zircon_runtime/graphics/backend-ibl-artifact-readback.md`,`docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-readback.md`,`docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`;本切片不生成新 PNG。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\backend\render_backend\read_ibl_bake_artifact_sections.rs` 通过;E 盘剩余空间 `46904709120` bytes,低于 50GB 阈值,清理并重建 `E:\cargo-targets\zircon-ibl-wgpu-readback-seam-0708`(`cargo clean` removed 1 file/354B);`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-readback-seam-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip --no-default-features --features core-min --locked --jobs 1 --color never -- --nocapture --test-threads=1` 两次在编译/链接阶段超出工具窗口(1204s/604s),不计通过;等待同 target cargo/rustc 退出后,最终生成的 `E:\cargo-targets\zircon-ibl-wgpu-readback-seam-0708\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe graphics::backend::render_backend::read_ibl_bake_artifact_sections::tests::readback_sections_preserve_pmrem_seams_after_wgpu_texture_roundtrip --exact --nocapture --test-threads=1` 通过 1/1,测试本体 1.81s,7308 filtered。 | 关闭 backend texture-copy/readback layout 层面的 seam-preservation gate;继续 live compute-produced PMREM/IEM artifact readback seam 对拍、importer/staged derived artifact seam 对拍、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3w artifact PMREM seam roundtrip guard | render_plan11_artifact_pmrem_seam_roundtrip_passed | 已验证:镜像 Shader 06 EC-M3w,环境光照 artifact payload 边界现在有 PMREM seam 量化守卫。新增 `runtime_environment_ibl_bake_artifact_seam_contract.rs`,将 seam-stress source cubemap 的 PMREM/SH9 编码为 RGBA16F artifact blob,按当前 request 解码并应用回 source cubemap,再比较 artifact roundtrip 前后 mid/rough mip 的 cube-edge luma mean/max。 | 新增非 ignored 合同测试 `runtime_environment_ibl_bake_artifact_pmrem_roundtrip_preserves_seam_metrics`;更新 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`;本切片不生成新 PNG。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_environment_ibl_bake_artifact_seam_contract.rs` 通过;E 盘剩余空间 `45107818496` bytes,按 target policy 清理并重建 `E:\cargo-targets\zircon-ibl-artifact-seam-0708`;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-artifact-seam-0708 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_seam_contract --no-default-features --features core-min --locked --jobs 1 --color never -- --nocapture --test-threads=1` 通过 1/1,测试本体 1.83s,冷构建 18m57s,stderr 只有当前 workspace 既有 warnings。 | 关闭 artifact payload roundtrip 层面的 seam-preservation gate;继续 GPU-produced texture readback artifact 与 importer/staged derived artifact 的 PMREM seam 对拍、RenderDoc/product capture、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3v cardinal 120-degree mirror validation + interactive PBR HDRI viewer | render_plan11_cardinal_120deg_interactive_viewer_passed | 已验证:镜像 Shader 06 EC-M3v,环境光照验收现在包含上下左右各 120 度旋转的真实 HDRI 镜面球验证图,并交付可鼠标手动操作视角的 PBR HDRI 查看器。四向截图使用同一 source cubemap skybox、SH diffuse 与 standard PBR 镜面反射路径;查看器默认加载 2K Poly Haven Lakes HDRI,左键拖拽绕镜面球旋转、滚轮缩放,用于手动检查 skybox 与镜面反射是否同步。 | 新增 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_cardinal_120deg_reflection_20260708.png`,1600x1200,2,099,569 bytes,SHA256 `0E57C496E49B6044F9AA5495BBFE855D5EB575A8D5AFAA8CB55B3DFF1D18AF15`;新增应用目标 `zircon_shader_pbr_viewer`,可执行文件 `E:\cargo-targets\zircon-shader-viewer-0708\debug\zircon_shader_pbr_viewer.exe`,69,941,248 bytes;更新 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\scene_fixtures.rs zircon_app\src\bin\shader_pbr_viewer.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-shader-viewer-0708 CARGO_INCREMENTAL=0 cargo check -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过;同 target `cargo build -p zircon_app --bin zircon_shader_pbr_viewer --no-default-features --features target-client --locked --jobs 1 --color never` 通过;`zircon_shader_pbr_viewer.exe --help` 通过;直接运行 `E:\cargo-targets\zircon-shader-120deg-0708\debug\deps\runtime_shader_pbr_hdri_export-810bef67c8d69fb9.exe export_runtime_shader_pbr_real_hdri_mirror_cardinal_120deg_png --ignored --exact --nocapture --test-threads=1` 通过 1/1,161.57s;同二进制 `runtime_shader_pbr_real_hdri_mirror_cardinal_120deg_png_matches_environment_metrics` 通过 1/1,0.75s,`runtime_shader_pbr_real_hdri_mirror_cardinal_120deg_png_matches_source_reference_metrics` 通过 1/1,125.40s;同名 PNG 在 repo `target`、`E:\cargo-targets\zircon-shader-120deg-0708` 与 `E:\cargo-targets\zircon-cmft-skybox-0707` 扫描 0。旧 target 的 Cargo 包装导出曾遇到 dep-info 写入错误,新 target 的 Cargo 包装导出曾超时但测试二进制随后直跑通过,不作为失败证据。 | 关闭用户要求的四向 120-degree environment reflection saved-PNG gate 与手动查看器交付;继续 GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-08 | EL-M1/EC-M3u mirror multi-view source-reference camera-basis gate | render_plan11_mirror_multi_view_source_reference_passed | 已验证:镜像 Shader 06 EC-M3u,环境光照多视角验收现在不仅检查 skybox 上下、镜面方向和掠射平衡,还会把每个视角的镜面球像素与源 HDRI/PMREM mip0 参考场对拍。`frame_assertions.rs` 新增 camera-view-aware source-reference 路径,按 `SinglePbrSphereCameraView` 重建正交正面、透视正面、透视左偏航、透视右偏航的光线/球面反射方向;旧单图 source-reference 继续走 legacy-front 基线。 | 新增 `runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_source_reference_metrics`,验证既有 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_multi_view_reflection_20260707.png`,1600x1200,1,857,183 bytes,SHA256 `8E39454F46B8A30530A648FFD5813FEF8307BA1F0CC3C81991E4CF47F9536E2B`;更新 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`。本切片未生成新图,验证图仍只在 `docs/tests/runtime/shader`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\frame_assertions.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_mirror_reflection_png_matches_source_reference_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,122.82s;直接运行已构建二进制 `runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_source_reference_metrics` 通过 1/1,117.13s;`runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_orientation_metrics` 通过 1/1,0.78s;repo `target`、`E:\cargo-targets\zircon-cmft-skybox-0707` 与 `E:\cargo-targets\zircon-cmft-skybox-0708-multiview` 同名 PNG 扫描为 0。干净外部 target 的 `CARGO_INCREMENTAL=0 cargo test ... --locked` 被当前工作区无关 UI 编译错误阻断(`parse_hex_color`、`ensure_font_asset_record`、`ScreenSpaceUiVertex`),不作为通过证据。 | 关闭非正面相机 source-reference saved-PNG gate;继续 GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3t mirror sphere multi-view HDRI validation | render_plan11_mirror_multi_view_reflection_passed | 已验证:镜像 Shader 06 EC-M3t,环境光照验收现在包含 2x2 多视角镜面球图,不再只依赖正交/透视正面两张截图。四格分别为正交正面、透视正面、透视左偏航、透视右偏航;相机由 `SinglePbrSphereCameraView` 写入临时场景并看向同一镜面球中心,验证 skybox 源 cubemap 与 PBR 镜面反射在不同观察方向下保持一致。 | 新增 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_multi_view_reflection_20260707.png`,1600x1200,1,857,183 bytes,SHA256 `8E39454F46B8A30530A648FFD5813FEF8307BA1F0CC3C81991E4CF47F9536E2B`;新增 `runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_orientation_metrics`,逐格读回检查 HDRI 天空上下差异、镜面方向和左右掠射平衡;新增测试文档 `docs/zircon_runtime/tests/runtime_shader_pbr_hdri_export.md`。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\scene_fixtures.rs` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export export_runtime_shader_pbr_real_hdri_mirror_multi_view_png --no-default-features --features core-min --locked --jobs 1 --color never -- --ignored --exact --nocapture --test-threads=1` 通过 1/1,测试本体 170.43s;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_mirror_multi_view_png_matches_orientation_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 0.70s;repo `target` 与 `E:\cargo-targets\zircon-cmft-skybox-0707` 同名 PNG 扫描 0。 | 关闭多视角 PBR environment reflection saved-PNG gate;继续 strict source-reference SSIM 扩展到非正面相机、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3s BRDF LUT grazing energy clamp + mirror source-reference compare | render_plan11_brdf_grazing_energy_clamp_mirror_ref_passed | 已验证:镜像 Shader 06 EC-M3s,完全镜面球的剩余掠射不真实问题来自 split-sum BRDF LUT 低 `NoV` 端 `A+B` 超过 1 的能量放大,不是 cubemap 方向或 PMREM 模糊。`environment_brdf_lut.rs` 归一 `scale+bias>1` 的 texel,`zr_environment.wgsl` 采样端把 BRDF 系数夹到 `[0,1]`,防止完美金属镜面在掠射边缘超过源环境能量。 | 新增 `docs/zircon_runtime/core/framework/render/environment/environment-brdf-lut.md`;刷新镜面图:`docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `35F0711D0990EC1825DC6C5D734ED6F3BC5ADD51C953280A3928DA5D0E1CCE61`,909,652 bytes;`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `A610CDA7C5F095E3EDDA5D5F97F5A9FD296B888AFF6027A96B6063827F925FF1`,1,016,502 bytes。新增 CPU direct-HDRI reference 对拍图 `runtime_shader_pbr_real_hdri_lakes_persp_cpu_ref_compare_20260707.png` 与 `runtime_shader_pbr_real_hdri_lakes_ortho_cpu_ref_compare_20260707.png`,SSIM `0.9596/0.9573`;透视外环 left/right luma `138.68/130.11`,正交 `152.44/149.33`,右侧不再量化高于左侧。 | 直接运行 lib-test 二进制 `environment_brdf_lut_conserves_smooth_perfect_mirror_grazing_energy` 通过 1/1;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export export_runtime_shader_pbr_real_hdri_mirror_reflection_png --no-default-features --features core-min --locked --jobs 1 --color never -- --ignored --exact --nocapture --test-threads=1` 通过 1/1,测试本体 304.72s,总 13m41s;stderr 只有当前 workspace 既有 warnings。 | 关闭 BRDF LUT 掠射能量放大回归;继续把 CPU reference compare 固化为 Rust saved-PNG 测试、strict seam/SSIM gates、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3r saved PMREM mip diagnostic blur gate | render_plan11_saved_pmrem_mip_diagnostic_blur_metrics_passed | 已验证:镜像 Shader 06 EC-M3r,已归档的 PMREM mip diagnostic 图现在具备自动 blur gate,专门防止 source cubemap mip 与 PMREM rough mip 混用或 PMREM mipmap 没有实际模糊的问题。 | 新增非 ignored 测试 `runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png_matches_blur_metrics`;`hdri_metrics.rs` 读取 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_angular_source_pmrem_mip_diagnostic_20260706.png` 上半 source tiles 和下半 PMREM tiles,按 mip/face 计算高频能量。当前 mip0 source/PMREM `9.073/9.073`,mip1 `7.782/1.950`,mip1..7 平均 PMREM/source ratio 约 `0.666`;断言要求非平 rough mip PMREM <= source×0.92+0.05、平均 ratio <=0.75、mip1 <= source×0.55。本切片不生成新图。 | `CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_1k_pmrem_mip_diagnostic_png_matches_blur_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 0.29s,总 9m35s;随后直接运行最新 `runtime_shader_pbr_hdri_export-ddba9a07f2a7f0d8.exe` 中 2K matrix metrics 通过 1/1,0.46s,PMREM diagnostic metrics 通过 1/1,0.35s。 | 关闭 saved PMREM mip diagnostic blur gate;继续 strict source-cubemap/reference SSIM、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3q saved 2K PBR matrix high-frequency roughness gate | render_plan11_saved_png_high_frequency_roughness_metrics_passed | 已验证:镜像 Shader 06 EC-M3q,已保存的 2K 10x10 HDRI PBR matrix 截图现在具备截图级 high-frequency roughness 断言;这补上先前 metrics 只看亮度范围、metal/smooth 分组差异和旧 sample-grid seam 的缺口。 | `runtime_shader_pbr_hdri_export/hdri_metrics.rs` 新增高金属列中心内圈高频能量检测,跳过球体轮廓边缘,比较 rough rows、mid rows、smooth rows 的局部反射细节;使用既有 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_10x10_cosine_pmrem_reflection_20260706.png`,不生成新图。当前样本 rough/mid/smooth 高频能量约 `15.52/17.96/18.85`,断言门为 mid > rough×1.05、smooth > rough×1.10。 | `CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics --no-default-features --features core-min --locked --jobs 1 --color never -- --exact --nocapture --test-threads=1` 通过 1/1,测试本体 0.46s,总 2m26s;stderr 只有当前 workspace 既有 warnings。 | 关闭 saved 2K PBR matrix high-frequency roughness gate;继续 strict source-cubemap/reference SSIM、GPU/offline PMREM seam 对拍、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3p frame render-region projection aspect + mirror grazing recapture | render_plan11_frame_projection_aspect_grazing_exports_passed | 已验证:镜像 Shader 06 EC-M3p,完全镜面球右侧过分掠射的最后一层问题来自场景 uniform 投影矩阵尺寸错配,不是 source cubemap face 轴或 PMREM 滤波。截图/离屏路径的 `RenderFrameExtract.view.effective_render_size()` 可在未回填 viewport 时退回 1x1,但 render pass 实际写 1280x960,导致正交/透视镜面球边缘几何条件失真。 | `SceneUniform::from_frame(...)` 改用 `frame.render_region().local_size()` 构建 `ViewProjectionMatrixPair`,使 GPU 投影矩阵与当前帧局部渲染区域一致;新增 `scene_uniform_uses_frame_render_region_size_for_projection_aspect` 锁定旧 extract=1x1 时 1280x960 正交 projection scale `0.75/1.0`。刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `D22CF31B3BF9742B4077E4A52608FC68EE9F47DD492241E26FB8D4847D4E62A8`、909,796 bytes;刷新 `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `C5D273AFD58880BD03295BF2077975ED1844A0B78C0F4D81A8B5159358107572`、1,016,494 bytes;两图均为 1280x960 且未写入 target。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --color never` 通过(既有 warning 415);直接运行已构建 `runtime_shader_pbr_hdri_export` 二进制:mirror export 1/1 通过,225.23s;saved-PNG orientation/grazing metrics 1/1 通过,0.64s。新外环指标:透视 left/right luma `138.24/133.67`,mean ratio `0.967`,stddev ratio `0.985`;正交 left/right luma `137.40/122.42`,mean ratio `0.891`,stddev ratio `0.936`;repo `target` 与 `E:\cargo-targets` 同名镜面 PNG 扫描为 0。聚焦 lib-test Cargo 包装命令在当前工作区一次 rustc 非诊断退出、一次 364s 超时,不计为通过证据。 | 关闭 frame/render-region 尺寸错配导致的镜面掠射几何回归;继续 strict SSIM/reference compare、seam gate、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3o standard material normal-map feature guard + mirror grazing balance | render_plan11_standard_material_normal_guard_grazing_exports_passed | 已验证:镜像 Shader 06 EC-M3o,完全镜面球左右掠射不一致和右侧过度掠射已收敛;无 normal map 的标准材质不再采样 renderer-owned neutral normal 占位贴图,避免量化占位法线在镜面边缘被反射项放大 | 新增 `HAS_NORMAL_TEXTURE` shader feature bit 并由 `PipelineKey`/标准材质 descriptor 派生到 WGSL define;`standard_material_sampled_normal(...)` 在 `!ZR_FEATURE_HAS_NORMAL_TEXTURE` 时直接使用几何法线,只有真实 normal map 材质才采样 normal 贴图;镜面导出断言新增左右外环 luma/stddev 平衡门。刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `9B294F637F66CAB25DBD1B0C711C557C55749ABFB967837D0D41CC7EDEF13825`、1,033,454 bytes;刷新 `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `072DE7EDD7A3D9522B8350FFF4F9401F1DD3F7A14285C22923302F5DAAFE3727`、897,232 bytes;两图均为 1280x960 且未写入 target。 | 直接运行 `E:\cargo-targets\zircon-cmft-skybox-0707\debug\deps\runtime_shader_pbr_hdri_export-ddba9a07f2a7f0d8.exe export_runtime_shader_pbr_real_hdri_mirror_reflection_png --ignored --nocapture --test-threads=1` 通过 1/1,281.59s;透视外环 left/right luma `152.59/162.11` ratio `1.062`,stddev ratio `1.535`;正交外环 luma `141.15/129.07` ratio `0.914`,stddev ratio `0.972`;repo `target` 与 `E:\cargo-targets` 同名镜面 PNG 扫描为 0。`cargo test -p zircon_runtime normal_texture --lib --no-default-features --features core-min --locked` 被既有 UI text `NativeBitmapAtlasPrepareReport.approximate_raster_image_count` 编译错误阻断,不计通过。 | 关闭当前 standard material 镜面掠射回归;继续 strict SSIM/reference compare、seam gate、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3n cmftStudio PMREM + skybox ray/fixCubeLookup alignment | render_plan11_cmft_pmrem_skybox_fixcube_exports_passed | 已验证:镜像 Shader 06 EC-M3n,当前 source cubemap/PMREM 运行时验证链按 cmft/cmftStudio shader 行为收口;skybox 由 inverse view-projection 重建透视/正交世界方向,PMREM 低 mip 用 cmft Blinn/cosine-power radiance lobe 生成,roughness→LOD 线性,skybox/specular/source/IEM cube lookup 应用 `fixCubeLookup` | `source_cubemap/pmrem.rs` 改为 cmft `specularPowerFor`/Blinn BRDF power 修正的 Hammersley cone 近似;`source_cubemap.rs` 与 `zr_environment.wgsl` 改为线性 roughness/mip 合同;`skybox_procedural.wgsl` 同步 cmftStudio `fixCubeLookup` 与相机 ray 重建;`create_sky_pipeline.rs` 增加 skybox WGSL parse 与 ray-reconstruction 静态守卫;单镜面球导出细分升到 96x192。刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `B3D9EC6B22671BF6E663BDBD130CDFE72BAB8F3AA059B3C25BC44E258721819E`、1,011,195 bytes;刷新 `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `8515068DAF9716AB8AA2DDD2B6A391C9A3F3035C4CC83663663141C4D8A9A142`、827,376 bytes;两图均为 1280x960 且未写入 target。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-cmft-skybox-0707 cargo test -p zircon_runtime skybox_shader_is_valid_wgsl --lib --no-default-features --features core-min --locked --jobs 1 --color never` 通过;直接运行同 target lib-test 二进制:`source_cubemap_cmft_pmrem` 2/2、`skybox_shader_is_valid_wgsl` 1/1、`skybox_shader_reconstructs_camera_ray_before_source_cubemap_sampling` 1/1、`deferred_lighting_shader_applies_environment_reflections_to_standard_pbr` 1/1 通过;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --no-run --no-default-features --features core-min --locked --jobs 1 --color never` 通过;直接运行 mirror export 二进制 1/1 passed,288.44s;repo `target` 与 `E:\cargo-targets` 同名镜面 PNG 扫描为 0。 | 关闭本轮 cmft/cmftStudio 采样/PMREM/skybox 方向回归;继续 strict SSIM/reference compare、seam gate、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3m mirror sphere winding + skybox -Z orientation | render_plan11_mirror_fixture_winding_skybox_orientation_exports_passed | 已验证:镜像 Shader 06 EC-M3m,完全镜面球截图中上下/前后反射倒置已关闭;导出测试球体夹具的三角形绕序已与法线方向一致,skybox 默认采样方向也回到默认相机 `-Z` | `scene_fixtures.rs` 的 UV sphere index 顺序改成外向绕序,避免单面渲染看到背壳;`skybox_procedural.wgsl` 的 source cubemap 采样方向改为 `-Z`;`frame_assertions.rs` 用上方天空接近度与 upper/lower blue-excess 替代脆弱亮度规则。刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `E6E6E09DEC7EA4014D0A1302641DFD558111B665D8255D0282A78C3289C58992` 与 `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `4DE32672A09851B2A304461FA1E5938ECDA5531D20B9901B04CCDD6BE424BADB`;两图均为 1280x960,上部蓝天反射在球体上方,底部为道路/岸边/湖面反射。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-reflection-orientation-0707 --color never` 通过;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --no-run --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-reflection-orientation-0707-m3m --color never` 通过;直接运行本轮 mirror export 二进制 exit 0,1/1 passed,227.66s;repo `target` 与两个外部 target 同名镜面 PNG 扫描均为 0。 | 关闭 mirror 反射方向回归;继续 strict SSIM/reference compare、seam gate、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3l mirror roughness floor + mip0 reflection validation | render_plan11_mirror_roughness_floor_reflection_exports_passed | 已验证:镜像 Shader 06 EC-M3l,透视/正交完全镜面球不再是白色/灰色糊面;standard material authored roughness=0/smoothness=1 现在能走 PMREM mip0 镜面反射 | 标准材质最小 roughness 从旧 `0.04` 收敛为 `STANDARD_MATERIAL_MIN_ROUGHNESS = 0.001`,覆盖 CPU uniform packing、procedural smoothness 映射、shader template、GBuffer encode、deferred lighting、fallback mesh 与 SSR roughness 读取;mirror export 的 `frame_assertions.rs` 增加亮度方差、平均饱和度和 clipping 比例断言。刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png` SHA256 `8109D779CCCED49354C2B5FAACF4D6561F12287C7537E7ED621CF80D5C61AB4B` 与 `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png` SHA256 `49414938370F7ECD1AD7B182E3815CE442F7141CE8DE1515BE025DC01FA1651C`,均为 1280x960 且可见 lakes HDRI 路面/湖面/天空反射。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-reflection-orientation-0707 --color never` 通过;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --no-run --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-reflection-orientation-0707 --color never` 通过;直接运行已构建 mirror export 二进制 exit 0,1/1 passed,254.40s;repo `target` 与该外部 target 同名镜面 PNG 扫描为 0。 | 关闭完全镜面 roughness 下限导致的白球回归;继续 strict SSIM/reference compare、seam gate、RenderDoc/product capture、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M3k mirror view-vector orientation + ambientCG Metal008/025/029 validation | render_plan11_mirror_view_direction_and_ambientcg_exports_passed | 已验证:镜像 Shader 06 EC-M3k,source-cubemap/PMREM 环境反射的视向改为相机驱动,完全镜面材质在正交和透视相机下复拍,另用 ambientCG Metal008/025/029 真实贴图材质球复拍真实 lakes HDRI 环境反射 | `SceneUniform` 增加相机世界位置与正交/透视 view-direction 标记;standard PBR、fallback mesh、deferred lighting 的环境反射 helper 使用透视 fragment->camera 或正交固定视向,不再固定 `+Z`。`runtime_shader_pbr_hdri_export.rs` 输出 `runtime_shader_pbr_real_hdri_lakes_mirror_sphere_orthographic_reflection_20260707.png`、`runtime_shader_pbr_real_hdri_lakes_mirror_sphere_perspective_reflection_20260707.png`、`runtime_shader_pbr_real_hdri_lakes_ambientcg_metal008_texture_maps_20260707.png`、`runtime_shader_pbr_real_hdri_lakes_ambientcg_metal025_texture_maps_20260707.png`、`runtime_shader_pbr_real_hdri_lakes_ambientcg_metal029_texture_maps_20260707.png`,均位于 `docs/tests/runtime/shader`。直接运行已构建 `runtime_shader_pbr_hdri_export` 二进制:mirror export 1/1 通过,ambientCG batch 1/1 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1` 通过,只有既有 warning。 | 本轮关闭 mirror 反射方向验收和三组真实金属贴图可视证据;后续继续 RenderDoc/product capture、strict SSIM/reference compare、derived/offline artifact、4K/16K bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M2an IBL runtime cache Wgpu product capture nonblack | render_plan11_ibl_product_wgpu_capture_nonblack_dispatch_zero_passed | 已验证:前一轮 framework capture 黑帧根因不是 `capture_frame` 存取层,而是 PBR 矩阵正交相机在产品默认路由中选择 Core2D 管线,不会产生 3D mesh/skybox 输出;证明用例改为透视 Core3D 产品快照后,WgpuRenderFramework 自身 `capture_frame` 可输出非黑 PBR matrix PNG,同时保留首帧 cache miss 执行 IBL bake、第二帧 cache hit bake executor=0 的产品统计 | 更新 ignored 产品测试 `export_runtime_render_ibl_cache_second_launch_dispatch_zero_png`:首帧/第二帧均断言 `last_pipeline=Some(RenderPipelineHandle(1))`;报告 `docs/tests/runtime/render/plan11_ibl_product_wgpu_capture_second_launch_dispatch_zero_20260707.txt` 记录 `first_ibl_executor_count=9`,`second_ibl_executor_count=0`,`post_first_environment_dispatch_count=0`,`first_compute_dispatch_count=12`,`second_compute_dispatch_count=3`,capture report 为 `FrameworkOffscreen` 1280x960 且 `graph_dump_present=true`。Wgpu PNG `docs/tests/runtime/render/plan11_ibl_product_wgpu_capture_second_launch_dispatch_zero_20260707.png` 已目检非黑;直接运行已构建 lib-test 二进制通过 1/1,日志 `docs/tests/runtime/render/plan11_ibl_product_wgpu_capture_second_launch_dispatch_zero_test_20260707.raw.log`/`.exit.txt` exit 0。为解锁本轮 lib-test 编译,同步补回 `GlyphAtlasSet` 测试导入漂移。 | 关闭 Wgpu product capture 非黑帧 + second-launch dispatch=0 联合证据;后续继续 RenderDoc product trace、asset-derived artifact 入口、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI。 |
| 2026-07-07 | EL-M1/EC-M2am IBL runtime cache product second-launch dispatch=0 | render_plan11_ibl_product_second_launch_dispatch_zero_framework_stats_scene_png_passed | 已验证:WgpuRenderFramework 产品提交路径首帧 cache miss 会执行 IBL bake graph 并写入 `.zribl`,同项目同 source cubemap 第二次提交命中 runtime cache 后 IBL bake executor 数为 0;同一项目/source cubemap 生成可视 PBR matrix PNG | 新增 ignored 产品测试 `export_runtime_render_ibl_cache_second_launch_dispatch_zero_png`:首帧 `first_ibl_executor_count=9`,`first_compute_dispatch_count=10`,cache hit 后 `post_first_environment_dispatch_count=0`;第二帧 `second_ibl_executor_count=0`,`second_compute_dispatch_count=1`;PNG `docs/tests/runtime/render/plan11_ibl_product_second_launch_dispatch_zero_20260707.png`,计数报告 `docs/tests/runtime/render/plan11_ibl_product_second_launch_dispatch_zero_20260707.txt`,最终日志 `docs/tests/runtime/render/plan11_ibl_product_second_launch_dispatch_zero_mixed_proof_test_20260707.raw.log`/`.exit.txt` exit 0。`--tests` 全量编译 600s timeout 与两次错误入口失败日志保留为过程证据,不计通过。Wgpu `capture_frame` 当前对该 project-render path 仍为黑帧,本 PNG 使用同项目同环境的 SceneRenderer 产品截图路径。 | 关闭 product second-launch dispatch=0 证据;后续继续 Wgpu capture 非黑帧/RenderDoc product capture、asset-derived artifact 入口、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI。 |
| 2026-07-06 | EL-M1/EC-M3j 10x10 HDRI PBR matrix + single sphere + texture-map validation | render_plan11_hdri_pbr_10x10_single_textured_exports_passed | 已验证:镜像 Shader 06 EC-M3j,10x10 metallic/smoothness 矩阵、单金属球和真实 PBR 贴图材质球都在真实 lakes HDRI skybox 中采样 environment cubemap/PMREM 反射 | `runtime_shader_pbr_hdri_export.rs` 拆出 scene/material/texture fixture 与 frame assertions,矩阵升级为 10x10;ambientCG Metal009 1K Color/NormalGL/Roughness/Metalness 进入临时项目材质,roughness/metalness 打包到 metallic-roughness 贴图。输出写入 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_10x10_cosine_pmrem_reflection_20260706.png`、`runtime_shader_pbr_real_hdri_lakes_single_metal_sphere_reflection_20260706.png`、`runtime_shader_pbr_real_hdri_lakes_ambientcg_metal009_texture_maps_20260706.png`;direct test binary 三个 ignored export 各 1/1 通过,non-ignored saved-PNG metrics 1/1 通过,target/cargo-target 同名 PNG 扫描为 0。 | 本轮产品可视验收关闭;RenderDoc/product capture、strict SSIM/reference compare、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M3i Unreal angular source-cubemap mip implementation + diagnostic | render_plan11_source_cubemap_angular_mips_contract_and_diagnostic_passed | 已验证:镜像 Shader 06 EC-M3i,source cubemap mip pyramid 现在由独立 angular source-mipmap owner 生成,skybox/source minification 与 PMREM 反射卷积不再混用低分辨率块状链 | 新增 `core/framework/render/environment/source_cubemap/mipmap.rs`,以 average input chain + angular-footprint 4x4 solid-angle weighted cross-face sampling 生成 source mips,final 1x1 六面平均;`runtime_environment_source_cubemap_contract` 直接二进制通过 12/12。诊断图写入 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_angular_source_pmrem_mip_diagnostic_20260706.png`,target 与 `E:\cargo-targets` 同名 PNG 扫描为 0。 | 该切片关闭当前 CPU source-mip 马赛克问题;asset/importer derived bake、GPU/offline source mip 生产、RenderDoc/product capture 和 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2al IBL runtime graph scheduler + cache writeback | render_plan11_ibl_runtime_scheduler_cache_writeback_core_min_cargo_passed_test_deferred | 已验证:source cubemap runtime cache miss 现在会在最终 pipeline compile options 中携带 `IblBakeArtifactRequest`,自动把 PMREM/SH9/IEM bake graph pass 追加到 compiled RenderGraph,默认 SceneRenderer 注册 IBL compute executors,并在 queue submit 后、transient graph resource 释放前触发 runtime cache writeback | 新增 `environment_ibl_compile_options.rs` 在 build-frame submission closeout 中根据 source cubemap + project runtime cache store 判定 miss/hit;`render_pipeline_compile_options`/`compiled_render_pipeline` 承载 request;`pass_authoring.rs` 把 IBL bake graph 追加到 AmbientOcclusion stage 以保证 Lighting 前可调度;`new_with_icon_source.rs` 默认注册 IBL compute executors;`submit_compiled_scene_frame.rs` 把 request 传入 cache writeback closeout。可视化验证图写入 `docs/tests/runtime/render/plan11_ibl_runtime_scheduler_validation_20260706.png`。 | `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-runtime-scheduler-0706 --message-format short --color never` exit 0,日志 `docs/tests/runtime/render/plan11_ibl_runtime_scheduler_cargo_check_20260706.raw.log` 与 `.exit.txt`;focused `cargo test -p zircon_runtime --lib environment_ibl ...` 在 lib-test 构建/链接窗口超过 600s,已确认并停止同 target-dir 进程,timeout 记录保留在 `docs/tests/runtime/render/plan11_ibl_runtime_scheduler_tests_20260706.*`,不计通过。 | 该切片关闭 production runtime scheduler + graph injection + default executor registration + closeout writeback 接线;focused test 绿色、产品级 second-launch dispatch=0、真实 RenderDoc/product capture、asset-derived artifact 入口、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2ak IBL source cubemap graph binding | render_plan11_ibl_source_cubemap_graph_binding_cargo_check_passed_test_deferred | 已验证:compiled scene render path 现在会把当前帧已上传的 source cubemap view 导入到 IBL bake graph 的 `environment.ibl.source_cubemap` required external texture,让 opt-in IBL WGPU executor 在正常渲染管线资源绑定阶段拿到输入 cubemap | 新增 `bind_environment_ibl_graph_resources.rs`,只在 compiled graph 声明 `environment.ibl.source_cubemap` 且资源表尚未绑定时导入 borrowed `wgpu::TextureView`;无 source view 时保持 required external missing 状态,不伪造完成。`render.rs` 在 `write_scene_uniform(device, queue, frame)` 后从 `SceneEnvironmentCubemap::source_view()` 传入当前帧 source cubemap view;同时修正 review-guard `SOURCE_METADATA_GUARD_GUARD` 编译阻塞。Lumen 参考核对 `dev/LumenInUE5.5.4WithComputeShader/Res/Shader/LumenSceneLighting/Radiosity/*.hlsl` 的 `[numthreads(8,8,1)]` + SRV/StructuredBuffer 输入 + RWTexture 输出模式,确认本切片方向仍是 graph resource -> WGPU compute dispatch。本切片截图验证写入 `docs/tests/runtime/render/plan11_ibl_source_binding_validation_20260706.png`。 | `rustfmt --edition 2021 --check` 覆盖本切片 render/source-binding 与 guard 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-runtime-writeback-0706 --message-format short --color never` exit 0,日志 `docs/tests/runtime/render/plan11_ibl_source_binding_cargo_check_20260706.{out,err}.log` 与 `.exit.txt`;focused `cargo test -p zircon_runtime --lib environment_ibl_source_binder ...` 在 lib-test 构建窗口超过 600s,不计通过,日志与 timeout 记录保留在 `docs/tests/runtime/render/plan11_ibl_source_binding_tests_20260706.*`。 | 该切片只关闭 frame source cubemap view 到 compiled graph required external resource 的生产绑定入口;focused test 绿色、production scheduler 队列/产品图注入、runtime readback/cache writeback、PMREM/IEM 产品 artifact writeback、optimized SH9 reduction、产品级 dispatch=0、RenderDoc/product capture、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M3h Unreal angular source-cubemap mip design lock | render_plan11_unreal_angular_source_mipmap_design_locked_no_screenshot | 设计完成:镜像 Shader 06 EC-M3h,确认 20260704 lakes HDRI 图只证明旧 sampled-equirect 失败;source skybox、source mip pyramid、specular PMREM 不能共用一条 mip/downsample 逻辑 | 在参考代码与精读笔记中加入 UE `TextureCompressorModule.cpp`;计划 11 将 `env.source_cubemap_mipmap` 作为 renderer-local/asset-bake source mip owner,只服务 skybox minification、FIS source LOD 和 `.zcube` source mip,不写 specular PMREM;PMREM 仍归 `ibl_prefilter.wgsl`/GGX FIS。该切片为文档设计锁定,不改生产代码、不生成截图。 | 未跑 Cargo;下一步按 Shader 06 §4.1a 实现 source angular mip owner,补 source mip variance/seam 测试与 docs/tests/runtime/shader 诊断截图,再进入产品级 8x8 PBR 复拍。 |
| 2026-07-06 | EL-M1/EC-M2aj IBL bake runtime graph-output cache writeback bridge | render_plan11_ibl_bake_runtime_graph_writeback_direct_binary_passed_screenshot_reused | 已验证:镜像 Shader 06 EC-M2aj,IBL bake graph 已调度输出现在能在 transient resources 释放前被 renderer-local closeout bridge 读回并写入 runtime cache;asset/cache hit 路径不会读取 graph resources;SH9 graph output 写入 runtime cache 后第二次 resolve 环境 compute dispatch=0 | 新增 `graphics/scene/scene_renderer/environment/ibl_bake_runtime_writeback.rs`,用显式 frame-source request 重建 artifact descriptor, runtime compute required 时从 `RenderGraphExecutionResources` 提取 readback sections 并调用 asset runtime writeback,skip/hit 时交给 asset 层返回 `SkippedRuntimeComputeNotRequired`;同步新增 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-runtime-writeback.md` 并更新 IBL readback、compute executor 与 asset/core artifact 文档。本切片不生成新截图,复用 2026-07-06 已保存 2K cosine PMREM PBR matrix。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-runtime-writeback-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` 修正导入路径后 exit 0;Cargo wrapper focused test 604s timeout 后直接运行 `E:\cargo-targets\zircon-ibl-runtime-writeback-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_runtime_writeback --nocapture --test-threads=1` exit 0,2/2 passed、7053 filtered;repo target 与该 cargo-target shader/HDRI PNG 扫描均为 0。 | 该切片关闭 graph-output -> runtime cache 的 renderer-local 收尾桥;production scheduler 队列/产品图注入、项目 cache-root、PMREM/IEM 产品 artifact writeback、optimized SH9 reduction、BRDF/IEM 产品接线、产品级 dispatch=0 二次启动、RenderDoc/product capture、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2ai IBL bake explicit frame-source request owner | render_plan11_ibl_bake_explicit_frame_source_request_owner_cargo_passed_screenshot_reused | 已验证:镜像 Shader 06 EC-M2ai,renderer-local IBL bake executor 的 source shape 现在由当前帧 source cubemap environment 显式提供,不再由 PMREM output resource metadata 反推;SH9-only graph 可以在没有 PMREM resource 的情况下重建 `IblBakeArtifactRequest` | `core/framework/render/environment/skybox.rs` 新增 `SourceCubemapEnvironment::ibl_bake_artifact_request(...)`;`extract.rs` 新增 `EnvironmentExtract::source_cubemap_ibl_bake_request(...)`;`ibl_bake_compute_executor.rs` 从 compiled graph metadata 只读取 required contents,再向 frame source cubemap 请求 bake key/face size/mip count。同步更新 source-cubemap 与 IBL compute executor 模块文档。本切片不生成新截图,复用 2026-07-06 已保存 2K cosine PMREM PBR matrix。 | `cargo fmt --package zircon_runtime` 通过;`cargo test -p zircon_runtime --lib ibl_bake_wgpu_request --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-request-owner-coremin-0706 --color never -- --nocapture --test-threads=1` 通过 2/2;`cargo test -p zircon_runtime --lib ibl_bake_request ...` 通过 2/2;`cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics ...` 通过 1/1;`docs/tests/runtime/shader` 下 20260706 cosine PMREM PNG 存在,target 与 `E:\cargo-targets` 同名 HDRI PNG 扫描为 0。 | 该切片关闭 executor request shape ownership 与 SH9-only graph 反依赖;production scheduler 队列/生命周期、async readback drain/cache writeback、product dispatch=0、RenderDoc/product capture、optimized SH9 reduction、BRDF/IEM 产品接线、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2ah IBL bake WGPU readback graph-resource bridge | render_plan11_ibl_bake_wgpu_readback_resources_direct_binary_passed_cache_writeback_deferred | 已验证:镜像 Shader 06 EC-M2ah,IBL bake PMREM/SH9/IEM transient graph outputs 现在能在释放 transient backings 前被整理成 backend artifact readback resources,为 production scheduler 的 GPU artifact cache writeback 提供 renderer-local 接入口 | 新增 `graphics/scene/scene_renderer/environment/ibl_bake_wgpu_readback.rs`,由 `IblBakeArtifactDescriptor.contents()` 驱动所需 graph resources:PMREM texture、SH9 buffer、IEM texture;新增 readback sections 薄入口并保持 backend 只依赖 raw WGPU texture/buffer;测试覆盖 full PMREM+SH9+IEM、SH9-only、缺 PMREM 命名错误和 IEM single-mip shape。同步新增 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-readback.md`,更新 dispatch/compute-executor 文档。本切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-readback-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` 首跑暴露测试-only `expect_err`/`Debug` bound,修正后复跑 exit 0;Cargo wrapper focused test 904s timeout,直接运行生成二进制 `E:\cargo-targets\zircon-ibl-wgpu-readback-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_readback --nocapture --test-threads=1` exit 0,4/4 passed、7022 filtered;`git diff --check` 触及文件 warning-only;target/cargo-target shader/HDRI PNG 扫描 `COUNT=0`。 | 该切片关闭 readback resource acquisition;production scheduler request owner、async readback drain/cache writeback、product dispatch=0、RenderDoc/product capture、optimized SH9 reduction、BRDF/IEM 产品接线、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2ag IBL PMREM WGPU final 1x1 six-face average | render_plan11_ibl_pmrem_final_mip_average_wgpu_passed_screenshot_deferred | 已验证:镜像 Shader 06 EC-M2ag,renderer-local PMREM WGPU 内核已按 cmft 末级 1x1 六面平均规则收敛,避免 final mip 六个 face 各自保留方向噪声造成高粗糙反射块状/方向不一致 | `shaders/ibl_prefilter.wgsl` 在 final mip dispatch 内对六个 face axis 做 cosine prefilter 并写入共同平均值;`ibl_bake_shader_plan.rs` bump shader content hash 到 `0x1b1_ba6e_0003` 并锁定 WGSL marker;`ibl_bake_wgpu_dispatch.rs` 新增 offscreen WGPU 读回测试,用非对称 source cubemap 验证 mip4 的 `Rgba16Float` 1x1x6 输出六面一致且非零。同步修复 Runtime 15 child-owner budget route import 可见性编译阻塞。本切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-final-mip-average-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_shader_plan --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,5/5 passed;直接运行 `E:\cargo-targets\zircon-ibl-final-mip-average-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_dispatch --nocapture --test-threads=1` exit 0,7/7 passed、6995 filtered、测试本体 6.73s。 | 该切片只关闭 WGPU PMREM final mip 平均;生产 scheduler/readback/cache writeback、BRDF/IEM 产品接线、optimized SH9 reduction、dispatch=0、RenderDoc/product capture、strict roughness/SSIM/seam 截图门、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2af IBL bake WGPU pipeline cache ownership/reuse | render_plan11_ibl_bake_wgpu_pipeline_cache_cargo_passed_scheduler_readback_deferred | 已验证:renderer-local IBL bake WGPU dispatch 的 shader module、pipeline layout 与 compute pipeline 已由 `SceneRendererCore` 生命周期 cache 持有,PMREM 多 mip dispatch 复用同一 compute pipeline,graph-context dispatch 不再每 pass 临时创建 pipeline | 新增 `ibl_bake_wgpu_pipeline_cache.rs`;`IblBakeWgpuPipelineCache` 持有 IBL bind group layouts、shader module cache、pipeline layout cache 与 compute pipeline cache,cache key 为 `ComputePipelineCacheKey` + output binding kind;`RenderPassGpuExecutionContext` 新增 IBL cache context,`execute_graph_stage(...)` 从 renderer core 向各 stage 传入;`record_ibl_bake_wgpu_command(...)` graph-context 路径要求 cache 并从 cache 获取 pipeline。同步新增 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-pipeline-cache.md`,更新 dispatch/binding/command-plan 文档。本切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-dispatch-coverage-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_wgpu_pipeline_cache --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,1/1 passed、6992 filtered,测试本体 0.84s;同 target `cargo test -p zircon_runtime --lib ibl_bake_wgpu_dispatch --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,6/6 passed、6987 filtered,测试本体 3.40s;stderr 只有既有 warnings。 | 该切片关闭 renderer-lifetime IBL WGPU pipeline cache reuse;生产 scheduler request injection、readback buffer acquisition/drain、runtime cache writeback from GPU outputs、product dispatch=0、RenderDoc/product capture、GPU final 1x1 face average、optimized SH9 reduction、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2ae IBL bake SH9/IEM graph-context WGPU coverage | render_plan11_ibl_bake_wgpu_dispatch_sh9_iem_graph_context_cargo_passed_readback_deferred | 已验证:renderer-local WGPU dispatch bridge 的 materialized graph-context 覆盖已从 PMREM mip0 扩展到 SH9 storage-buffer 与 IEM storage-texture,三类 IBL bake 输出都能解析 source/output resources、编码 dispatch 并记录 audit | `ibl_bake_wgpu_dispatch.rs` 复用 `record_graph_context_dispatch(...)` 搭建 offscreen WGPU + materialized RenderGraph resources;新增 SH9 pass 覆盖 `environment.ibl.irradiance_sh9` storage buffer 输出,新增 IEM pass 覆盖 `environment.ibl.irradiance_cube` D2Array storage texture 输出。同步更新 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-dispatch.md`。本切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-dispatch-coverage-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_wgpu_dispatch --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,6/6 passed、6986 filtered,测试本体 3.93s,stderr 只有既有 warnings。 | 该切片关闭 PMREM/SH9/IEM focused graph-context dispatch/audit 覆盖;生产 scheduler、pipeline cache ownership/reuse、readback buffer acquisition/drain、runtime cache writeback from GPU outputs、product dispatch=0、RenderDoc/product capture、GPU final 1x1 face average、optimized SH9 reduction、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2ad IBL bake opt-in executor WGPU request inference | render_plan11_ibl_bake_compute_executor_wgpu_request_inference_cargo_passed_scheduler_request_deferred | 已验证:显式 opt-in IBL bake executor 在真实 GPU execution context 下可从 frame environment bake key 与 compiled graph PMREM texture metadata 恢复 `IblBakeArtifactRequest`,再进入 live WGPU dispatch helper;metadata-only dispatch 记录路径保持给结构审计使用 | `ibl_bake_compute_executor.rs` 的注册 executor 现在检测 `context.gpu().is_some()` 后走 WGPU request inference:PMREM transient Cube texture 提供 face size/mip count,graph 中 PMREM/SH9/IEM 资源提供 required contents,当前帧 environment 提供 bake key,随后调用 `record_ibl_bake_wgpu_pass_for_request(...)`。新增 focused tests 覆盖 full PMREM+SH9+IEM graph 从 SH9 pass 恢复 request,以及 SH9-only graph 因缺少 PMREM shape source 失败关闭。本切片更新 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-compute-executor.md`,不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-compute-executor-wgpu-0706 CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0;同 target `cargo test -p zircon_runtime --lib ibl_bake_compute_executor --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,6/6 passed、6984 filtered,stderr 只有既有 warnings。 | 该切片关闭 opt-in executor 到 live WGPU helper 的 request inference;production scheduler 仍需显式 request owner 支持 SH9-only/IEM-only graph、pipeline cache ownership/reuse、readback buffer acquisition/drain、runtime cache writeback from GPU outputs、product dispatch=0、RenderDoc/product capture、GPU final 1x1 face average、optimized SH9 reduction、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI。 |
| 2026-07-06 | EL-M1/EC-M2ac IBL bake WGPU compute pipeline dispatch bridge | render_plan11_ibl_bake_wgpu_dispatch_cargo_passed_readback_deferred | 已验证:renderer-local IBL bake command plan + live bind group 现在能创建真实 WGPU compute pipeline,并把 PMREM storage texture 与 SH9 storage buffer dispatch 编码提交到 offscreen WGPU queue;PMREM mip0 graph-context 路径也能从 materialized RenderGraph resources 编码并记录 dispatch audit | 新增 `ibl_bake_wgpu_dispatch.rs`,用 `IblBakeWgpuCommandPlan.wgsl_source` 创建 shader module,按 command 的 output bind group layout 创建 compute pipeline,entry point 固定为 `IBL_BAKE_COMPUTE_ENTRY_POINT`;`encode_ibl_bake_wgpu_compute_dispatch(...)` 在打开 WGPU pass 前拒绝 zero dispatch groups,随后设置 pipeline、bind group0 并按 command dispatch groups 编码。`record_ibl_bake_wgpu_pass_for_request(...)` 从 `RenderPassExecutionContext` 匹配 pass/executor,验证 source/output access,解析 source cubemap/output 资源,编码 dispatch 并推送 `RenderGraphComputeDispatchRecord`。`ibl_bake_wgpu_command_plan.rs` 将 shader source 纳入 command plan;新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-dispatch.md`,并同步 command-plan/binding/shader-plan 文档。本切片不生成截图。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_wgpu_dispatch.rs zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_wgpu_binding.rs zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_wgpu_command_plan.rs zircon_runtime/src/graphics/scene/scene_renderer/environment/mod.rs` 通过;`cargo test -p zircon_runtime --lib ibl_bake_wgpu_dispatch --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-dispatch-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0,4/4 passed、0 failed、6984 filtered,测试本体 5.28s;直接复跑 `E:\cargo-targets\zircon-ibl-wgpu-dispatch-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_dispatch --nocapture --test-threads=1` exit 0,4/4 passed、6984 filtered,日志 `docs/tests/runtime/render/plan11_ibl_wgpu_dispatch_direct_tests_20260706.out.log`。 | 该切片关闭 live compute pipeline creation、command encoder dispatch primitive 与 focused PMREM graph-context resource dispatch/audit;生产 executor 仍需接入完整 scheduler、pipeline cache ownership/reuse、SH9/IEM graph-context coverage、readback buffer acquisition/drain、runtime cache writeback from GPU outputs、product dispatch=0、RenderDoc/product capture、GPU final 1x1 face average、optimized SH9 reduction、strict roughness/SSIM/seam gate、4K/16K offline bake 与 full CI。 |
| 2026-07-06 | EL-M1/EC-M2ab IBL bake WGPU storage bind-group resource bridge | render_plan11_ibl_bake_wgpu_bind_group_direct_tests_passed_pipeline_encoding_deferred | 已验证:renderer-local IBL bake command plan 现在可创建真实 WGPU storage texture/storage buffer bind group layout 与 bind group,关闭 command/readback plan 到 compute pass 之间的输出绑定资源缺口 | `ibl_bake_wgpu_binding.rs` 新增 `IblBakeWgpuBindGroupLayouts` 与 `IblBakeWgpuOutputBindingResource`,分别覆盖 PMREM/IEM 的 `texture_storage_2d_array<rgba16float, write>` 输出路径和 SH9 storage-buffer 输出路径;`create_ibl_bake_wgpu_bind_group(...)` 复用 command plan 的 binding slot、params buffer 与 source sampler,并在调用 WGPU 前校验 command output kind 与 caller 提供的 storage texture/buffer 是否匹配。`ibl_bake_wgpu_command_plan.rs` 的 binding slot 常量提升为 renderer-local 可复用常量;新增模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-binding.md`,并同步 command-plan/shader-plan 文档。本切片不生成截图。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_wgpu_binding.rs zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_wgpu_command_plan.rs zircon_runtime/src/graphics/scene/scene_renderer/environment/mod.rs` 通过;`cargo test -p zircon_runtime --lib ibl_bake_wgpu_binding --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-binding-0706 --message-format short --color never -- --nocapture --test-threads=1` 首轮在 Windows lib-test 编译后超过 1204s 工具窗口,不计通过;直接执行已生成 lib-test 程序 `E:\cargo-targets\zircon-ibl-wgpu-binding-0706\debug\deps\zircon_runtime-9b53a08da5f6e0e7.exe ibl_bake_wgpu_binding --nocapture --test-threads=1` exit 0,2/2 passed、0 failed、6974 filtered,测试本体 1.77s,日志 `docs/tests/runtime/render/plan11_ibl_wgpu_binding_direct_tests_20260706.out.log`。 | 该切片只关闭 live storage-output bind-group helper;生产 executor 仍需接入 graph source/output resource lookup、compute pipeline cache、command encoding/submission、readback buffer ownership、async readback/cache writeback、product dispatch=0、RenderDoc/product capture、optimized SH9 reduction、strict roughness/SSIM/seam gate 与 full CI。 |
| 2026-07-06 | EL-M1/EC-M2aa IBL bake WGPU uniform params + sampler ABI | render_plan11_ibl_bake_wgpu_uniform_params_sampler_check_passed_unit_runtime_blocked | 已验证:镜像 Shader 06 EC-M2aa,Plan 11 renderer-local IBL bake 命令现在能把 shader plan 参数投影为 WGPU uniform buffer 字节布局,并修正 source-cubemap filtering sampler 的 mipmap 类型 | `ibl_bake_wgpu_command_plan.rs` 新增 `IblBakeWgpuParamsPlan`,为 PMREM/SH9/IEM 输出 32-bit word 顺序与 little-endian bytes;`ibl_bake_wgpu_binding.rs` 用该计划创建 params uniform buffer,source sampler 的 `mipmap_filter` 改为 `wgpu::MipmapFilterMode::Linear`;新增 uniform-layout 单测覆盖 PMREM mip0/mip7、SH9、IEM 与 endian bytes。本切片不生成截图。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-wgpu-params-check-0706-final CARGO_INCREMENTAL=0 cargo check -p zircon_runtime --lib --tests --no-default-features --features core-min --locked --jobs 1 --message-format short --color never` exit 0,测试配置编译检查通过;focused `cargo test -p zircon_runtime --lib command_plan_serializes_wgsl_uniform_params_in_layout_order ...` 当前没有计为通过,因为完整 lib-test 树在进入该断言前被无关 `runtime_absorption/structure_convention` 生成测试诊断或 exit `-1` 阻断。 | 只关闭 WGPU params/sampler ABI 编译门;真实 executor 资源查找、bind group 接入、pipeline cache、command encoding/submission、async readback/cache writeback、product dispatch=0、RenderDoc/product capture、optimized SH9 reduction、strict roughness/SSIM/seam gate 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2z IBL bake owned Cube storage-view resource bridge | render_plan11_ibl_bake_owned_cube_storage_view_cargo_passed_command_encoding_deferred | 已验证:RenderGraph execution resources 现在能把 owned transient Cube/D2 backing 按自定义 `wgpu::TextureViewDescriptor` 暴露为 PMREM/IEM 需要的 D2Array storage texture view,填上 WGPU command plan 到真实 executor 之间的输出资源出口 | `RenderGraphExecutionResources::owned_texture_view_with_descriptor(...)` 新增 descriptor 校验后创建 view:校验 format、view usage、view dimension、mip range 与 array-layer range;`materialization.rs` 的 RHI->WGPU texture format/usage 映射开放给同层资源表复用;新增 `materialization_exposes_owned_cube_storage_texture_array_views` 通过 offscreen WGPU materialization 创建 `Rgba16Float` Cube、再创建 mip2 `D2Array` + 6 layers + `STORAGE_BINDING` view,并锁定越界 mip/array 与非法 usage 失败路径。更新 `docs/zircon_runtime/graphics/scene/scene_renderer/graph_execution/render_graph_execution_resources.md`。 | `rustfmt --edition 2021 --check` 覆盖 `materialization.rs`、`render_graph_execution_resources.rs`、`materialization/tests.rs` 通过;最终复用 `E:\cargo-targets\zircon-ibl-wgpu-command-plan-0706` 执行 `cargo test -p zircon_runtime --lib materialization_exposes_owned_cube_storage_texture_array_views --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-command-plan-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0,1/1 passed、0 failed、6965 filtered,测试本体 0.97s;stderr 只有仓库既有 warnings。 | 该切片只关闭 owned output storage texture view 创建;source cubemap external binding、params/readback buffer ownership、bind group creation、compute pipeline cache、command encoding/submission、async readback/cache writeback、product dispatch=0、RenderDoc/product capture、optimized SH9 reduction、strict roughness/SSIM/seam gate 与 full CI 仍继续执行。 |
| 2026-07-06 | EL-M1/EC-M2y IBL bake WGPU command/readback plan | render_plan11_ibl_bake_wgpu_command_readback_plan_focused_tests_passed_wgpu_execution_deferred | 已验证:renderer-local IBL bake 已有 WGPU bind-group ABI、per-mip D2Array storage texture view 与 artifact-aware readback copy 计划,但仍不实际创建 GPU 资源或提交 compute 命令 | 新增 `zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_wgpu_command_plan.rs`;命令计划复用 shader/kernel plan 派生 pipeline label/key 与 dispatch groups,绑定 0 uniform params、1 source `texture_cube<f32>`、2 filtering sampler、3 output storage texture/storage buffer;PMREM/IEM 输出视图固定为 `Rgba16Float` + `D2Array` + 单 mip + 6 layers + `STORAGE_BINDING`;PMREM 读回按 face-major artifact offset 拆成每 face/mip texture copy,SH9 读回为 144B storage-buffer copy,IEM 读回为 32x32x6 face-major copies。新增 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-wgpu-command-plan.md`;本切片不生成截图。 | `rustfmt --edition 2021 --check` 覆盖 `ibl_bake_wgpu_command_plan.rs` 与 `environment/mod.rs` 通过;首次 Cargo wrapper 在 Windows lib-test 编译后超过工具窗口且未写 exit file,不计通过;随后直接执行已生成 lib-test 程序 `E:\cargo-targets\zircon-ibl-wgpu-command-plan-0706\debug\deps\zircon_runtime-1ff53e05a9088131.exe ibl_bake_wgpu_command_plan --nocapture --test-threads=1` exit 0,4/4 passed、0 failed、6934 filtered,日志 `docs/tests/runtime/render/plan11_ibl_wgpu_command_plan_direct_tests_20260706.out.log`。 | 只关闭 WGPU command/readback plan 契约;真实 WGPU bind group/texture-view/resource allocation、compute pipeline cache、command encoder dispatch、queue submission、async readback dequeue、runtime cache writeback from GPU outputs、product second-launch dispatch=0、RenderDoc/product capture、optimized SH9 reduction、strict roughness/SSIM/seam gate 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2x GPU PMREM WGSL cosine/FIS parity | render_plan11_pmrem_wgsl_cosine_fis_parity_cargo_passed_command_encoding_deferred | 已验证:镜像 Shader 06 EC-M2x,renderer-local PMREM WGSL 内核的采样口径已与 CPU PMREM bridge 对齐 | `shaders/ibl_prefilter.wgsl` 新增 full-roughness cosine hemisphere convolution 分支,并同步 Plan 06/UE filtered importance sampling 细节:Hammersley 居中采样、`E.y *= 0.995`、source mip PDF 选择、texel solid-angle `* 2.0`;`ibl_bake_shader_plan.rs` 更新 high roughness 128 sample dispatch 参数与 shader content hash,新增 WGSL 合同测试;新增 `docs/zircon_runtime/graphics/scene/scene_renderer/environment/ibl-bake-shader-plan.md` 并同步 source-cubemap 文档。本切片不生成新截图。 | `cargo fmt --package zircon_runtime` 通过;E 盘测试前剩余 83.54GB,未触发 target 清理;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-ibl-shader-plan-check-0706 CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib ibl_bake_shader_plan --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,5/5 passed、0 failed、0 ignored、6933 filtered、测试本体 0.03s、总耗时 13m52s,stderr 只有既有 warnings。 | 只关闭 PMREM GPU shader-plan 算法合同;真实 WGPU bind group/storage view、command encoding、async scheduling/readback/cache writeback、GPU final 1x1 face average、optimized SH9 reduction、product second-launch dispatch=0、RenderDoc/product capture、strict roughness/SSIM/seam gate 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2w public source-cubemap PMREM contract convergence | render_plan11_pmrem_public_contract_cosine_convolution_cargo_passed_gpu_kernel_deferred | 已验证:镜像 Shader 06 EC-M2w,公开 source-cubemap integration contract 已从旧 saturated-tail downsample 口径改为 full-roughness cosine convolution 口径 | `zircon_runtime/tests/runtime_environment_source_cubemap_contract.rs` 的 saturated roughness guard 现在要求 roughness=1 选中 PMREM mip 与 ordinary previous-mip trilinear downsample 存在可测 luma 差异,并继续降低高频方差;该公开合同与 `source_cubemap/pmrem.rs` 的 UE-style full-roughness 分支一致。本切片不改 shader 输出图,不生成新截图。 | `cargo fmt --package zircon_runtime` 通过;首次 focused integration command 在 Windows 冷编译阶段超过 1204s 工具窗口,后台 cargo/rustc 完成后复跑 `CARGO_TARGET_DIR=E:\cargo-targets\zircon-source-cubemap-pmrem-contract-0706 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,10/10 passed、0 failed、0 ignored、测试本体 6.63s、总耗时 7m23s,stderr 只有既有 warnings。 | 公开 CPU PMREM 合同已与当前 Plan 06/11 算法一致;真实 GPU/offline PMREM/SH9/BRDF/IEM kernels、WGPU command encoding、strict roughness/SSIM/seam screenshot gate、RenderDoc/product capture 与 full CI 继续执行。 |
| 2026-07-06 | EL-M1/EC-M2v IBL bake shader plan + per-mip PMREM WGPU contract | render_plan11_ibl_bake_shader_plan_per_mip_pmrem_core_min_and_focused_tests_passed_wgpu_command_deferred | 已验证:IBL bake graph/executor/shader 合同已按 WGPU 实际限制修正为每个 PMREM mip 一个 async-compute pass,pass 名 `env.ibl_prefilter.mipN`;shader plan 和 WGSL 通过 `texture_storage_2d_array<rgba16float, write>` 写 cubemap face array view,不使用 WGPU 不支持的 `texture_storage_cube` | 新增 `zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_shader_plan.rs` 与 `shaders/ibl_prefilter.wgsl`、`ibl_irradiance_sh.wgsl`、`ibl_irradiance_cube.wgsl`;`ibl_bake_graph_plan.rs` 将 PMREM 拆为 per-mip pass 并串接 mip 依赖;`ibl_bake_compute_executor.rs` 从 `.mipN` pass name 推导 dispatch groups,缺少 suffix 时 fail closed。WGSL/Naga 解析覆盖三份 shader;SH9 当前为 deterministic single-invocation starter,后续再替换为两阶段 reduction。 | `rustfmt --edition 2021 --check` 覆盖 graph/executor/shader-plan/environment mod 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-shader-plan-check-0706 --message-format short --color never` 通过(既有 warnings);同 target 下 focused lib tests 通过:`ibl_bake_graph_plan` 4/4、`ibl_bake_compute_executor` 4/4、`ibl_bake_shader_plan` 4/4。 | 只关闭 WGPU 可实现的 shader/dispatch/graph 合同;真实 WGPU bind group view 创建、per-mip storage texture view、compute pipeline cache、命令编码、async readback queue、runtime cache writeback from GPU outputs、product second-launch dispatch=0、RenderDoc/product capture、optimized SH9 reduction、strict roughness/SSIM/seam gate 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2u full-roughness cosine PMREM tail + PMREM owner split | render_plan11_pmrem_full_roughness_cosine_convolution_cargo_and_screenshot_passed_gpu_kernel_deferred | 已验证:镜像 Shader 06 EC-M2u,source-cubemap CPU PMREM 的 roughness>=0.99 尾部改为 UE-style cosine hemisphere convolution,不再从上一层 PMREM 普通降采样;同时拆出 PMREM 子 owner 控制父模块规模 | 新增 `zircon_runtime/src/core/framework/render/environment/source_cubemap/pmrem.rs`;`source_cubemap.rs` 父 owner 保留 source cubemap/SH9/布局合同,PMREM 采样、GGX FIS 与 full-roughness cosine 分支进入子模块;模块 guard 改为断言 full-roughness PMREM 与普通 previous-mip downsample 有差异且方差下降;`IblBakeArtifactContents` 增加 bitmask `BitOr/BitOrAssign`;IBL bake compute executor 测试改用 executor id 作为缺失 `.mipN` 的负例 pass name;刷新 1K/2K cosine PMREM PBR 矩阵与 1K PMREM 诊断图,均写入 `docs/tests/runtime/shader`。 | `cargo fmt --package zircon_runtime` 通过;`CARGO_TARGET_DIR=E:\cargo-targets\zircon-source-cubemap-pmrem-0706 cargo test -p zircon_runtime --lib source_cubemap --no-default-features --features core-min --locked --jobs 1 --message-format short --color never -- --nocapture --test-threads=1` exit 0,15/15 passed、0 failed、6897 filtered;ignored `runtime_shader_pbr_hdri_export` 三个导出分别通过 1/1。PNG 复核:1K 诊断 864x1152,SHA256 `6F7B37E061A04C3701CD5384F13F5A21159992527C65F21FA3C2D4210B1DAAB1`;1K reflection 1280x960,SHA256 `B7BEBB66E806667B35A1B5792D5698607C683931DA7EDB78339DEEED86CD15C5`;2K reflection 1280x960,SHA256 `4C974C6D1A43309032B0B1718762F9B9566B19C5D02DF3B954D45E5988831DCD`;`target` 下 `*cosine_pmrem*.png` 扫描为 0,导出用外部 target 已按磁盘策略清理。 | CPU bridge 的高粗糙度 PMREM 块状回归已关闭;真实 GPU/offline PMREM/SH9/BRDF/IEM kernels、render graph async scheduling、runtime readback/cache 写回、derived importer/staged build-tool、product dispatch=0、RenderDoc/product capture、strict roughness/SSIM/seam gate、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2t IBL bake compute executor plan record | render_plan11_ibl_bake_compute_executor_plan_record_core_min_passed_gpu_kernel_deferred | 已验证:renderer environment 内部已有显式 opt-in 的 IBL bake compute executor 注册与 dispatch/audit 记录入口,但默认产品 registry 仍不注册这些 executor | 新增 `zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_compute_executor.rs`;`ibl_bake_compute_executor_registrations()` 返回 PMREM、SH9、IEM 三个 executor 注册项;executor 从 RenderGraph resource metadata 推导 fixed dispatch groups,校验 source cubemap read 与目标 output write,并通过 `RenderPassGpuExecutionContext::push_compute_dispatch_record(...)` 写入 execution record。`RenderPassExecutorRegistry::with_environment_ibl_bake_compute_executors()` 只作为显式扩展,默认 builtin registry 继续排除 IBL bake ids。 | `rustfmt --edition 2021 --check` 对 IBL graph/executor、registry 与 GPU report 相关文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-graph-plan-check-0706 --message-format short --color never` 通过(既有 warnings);`cargo test -p zircon_runtime --lib ibl_bake_compute_executor --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-graph-plan-check-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3、6902 filtered、测试本体 0.00s、总耗时约 11m15s。 | 只关闭显式 executor 注册与计划 dispatch/audit 记录;真实 WGPU PMREM/SH9/IEM WGSL 内核、bind group/pipeline cache、命令编码、async readback queue、runtime cache writeback from GPU outputs、product second-launch dispatch=0、RenderDoc/product capture、strict roughness/SSIM/seam gate 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2s IBL bake render-graph plan | render_plan11_ibl_bake_graph_plan_core_min_passed_executor_deferred | 已验证:renderer environment 内部已能按 `IblBakeArtifactRequest` 声明 PMREM、SH9 与 optional IEM 的 RenderGraph async-compute pass、transient output 与 readback root,但尚未注册默认 executor | 新增 `zircon_runtime/src/graphics/scene/scene_renderer/environment/ibl_bake_graph_plan.rs`;`append_ibl_bake_artifact_graph_plan(...)` 导入 required source cubemap external resource,按 PMREM -> SH9 -> IEM 顺序创建 `Rgba16Float` cube/storage texture、SH9 storage buffer、readback usage 与 fixed compute workload,并将 pass executor id 固定为 `environment.ibl_prefilter`、`environment.ibl_irradiance_sh`、`environment.ibl_irradiance_cube`。`environment/mod.rs` 只公开 scene_renderer 内部模块,不把该计划接入默认帧图或旧 preview path。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\environment\ibl_bake_graph_plan.rs zircon_runtime\src\graphics\scene\scene_renderer\environment\mod.rs` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-graph-plan-check-0706 --message-format short --color never` 通过(既有 warnings);`cargo test -p zircon_runtime --lib ibl_bake_graph_plan --no-default-features --features core-min --locked ...` 两次均在进入目标断言前被当前 UI lib-test 编译漂移阻断:`UiStateFlags` 已无 `focused`/`hovered` 字段,相关 UI 测试仍初始化旧字段,不计本切片失败。 | 只关闭 IBL bake 资源/compute workload 的 RenderGraph 声明合同;真实 WGPU executor、PMREM/SH9/IEM WGSL compute、async scheduling/readback queue、runtime dispatch report 接入、derived artifact importer/staged build-tool 生产、product second-launch dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2r saturated roughness PMREM tail downsample + real HDRI refresh | render_plan11_pmrem_saturated_tail_downsample_cargo_and_screenshot_passed_strict_laplacian_deferred | 已验证:镜像 shader 06 EC-M2r,source-cubemap CPU PMREM 的 roughness=1.0 首个饱和 mip 不再独立低分辨率重采样源链,而是从上一层 PMREM 连续降采样;真实 HDRI PBR matrix 以 IBL 为主导 | `source_cubemap.rs` 对 `filter.roughness >= 1.0` 的饱和 PMREM 尾部统一调用 `downsample_pmrem_mip_from_previous(...)`;新增模块内 guard 与公开 integration contract 锁定 roughness=1.0 选中 mip 与上一层 PMREM 的 trilinear 结果一致;真实 HDRI export 场景将 directional light intensity 设为 `0.0`,并将 saved-PNG metrics 的 high-metal rough/smooth 变化阈值收敛到当前真实 HDRI 曝光可稳定覆盖的 `>10.0`;刷新 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_1k_pmrem_mip_diagnostic_20260706.png`(864x1152,954,870 bytes,SHA256 `12BF0D51060CCB0A53C1BCC4A97D6118D0B55004FB8619D46C27B67869DF26A2`)与 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png`(1280x960,1,009,114 bytes,SHA256 `7878B8A2CFF3DE85E1528D88DC86909F7AFC431206BB12FD7EFA3854F2E90606`)。 | `rustfmt --edition 2021` 对 `source_cubemap.rs` 与 source-cubemap integration contract 通过;测试前按磁盘策略执行 `cargo clean --target-dir E:\cargo-targets\zircon-pmrem-saturated-tail-0706`,removed 0 files;早前模块内 filtered `--lib source_cubemap_saturated_roughness_mip_downsamples_previous_pmrem_mip` 通过 1/1,最终复跑该 `--lib` 目标被当前工作区无关 UI 单元测试 `UiStateFlags::{focused,hovered}` 字段漂移阻断,未进入本断言;同一合同已镜像到公开 integration 后,`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-pmrem-saturated-tail-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 10/10;非 ignored 2K saved-PNG metrics 通过 1/1;两张 PNG 元数据复核与 SHA256 匹配;扫描 repo `target` 与外部 cargo target 未发现 runtime shader/PBR/HDRI PNG。stderr 只有仓库既有 warnings。 | 只关闭 CPU bridge 中首个 saturated roughness mip 的 PMREM 连续模糊回归和本轮截图刷新;RenderDoc/product capture、GPU/offline PMREM/SH9/BRDF/IEM bake、derived cache/importer、render graph async scheduling、probe capture/blending、strict Laplacian/SSIM screenshot gate、4K/16K 离线资产与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2q IBL runtime dispatch/cache integration | render_plan11_ibl_runtime_dispatch_cache_integration_cargo_passed_gpu_scheduler_deferred | 已验证:asset artifact 层具备 runtime compute miss -> readback sections -> `.zircon-cache` writeback -> 下一次同 request runtime-cache hit 且 dispatch=0 的调度合同 | 新增 `zircon_runtime/src/asset/artifact/ibl_bake_artifact_runtime_dispatch.rs`;`resolve_ibl_bake_artifact_runtime_dispatch(...)` 读取 runtime cache、合并 asset-derived blobs、复用 resolved payload priority,返回 source/payload/dispatch_count;`write_ibl_bake_artifact_runtime_dispatch_readback(...)` 只在 dispatch report 要求 runtime compute 时调用 runtime readback writeback,asset-derived/cache hit 会跳过 readback 写入,避免来源 1/2 命中后污染 cache。 | `cargo fmt --package zircon_runtime` 通过;focused `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract runtime_environment_ibl_bake_artifact_runtime_dispatch --locked` exit 0,2/2 passed、19 filtered;完整 `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --locked` exit 0,21/21 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;stderr 只有仓库既有 warnings;本切片不生成截图。 | 只关闭 runtime readback-to-cache dispatch integration 的 asset-layer 合同;GPU/offline PMREM/SH9/BRDF/IEM bake 生产、render graph/async WGPU scheduling、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2p WGPU IBL artifact section acquisition | render_plan11_ibl_wgpu_section_acquisition_cargo_passed_scheduler_deferred | 已验证:给定 WGPU PMREM texture、SH9 buffer 与 optional IEM texture 后,backend 可按 artifact descriptor 显式判断必需资源并取得 `IblBakeArtifactReadbackSections` | `zircon_runtime/src/graphics/backend/render_backend/read_ibl_bake_artifact_sections.rs` 的 `IblBakeArtifactWgpuReadbackResources` 持有 descriptor 与 PMREM/SH9/IEM 三个可选 WGPU resource slot,并新增 `requires_pmrem_texture()`、`requires_irradiance_sh9_buffer()`、`requires_irradiance_cube_texture()` 合同;`read_ibl_bake_artifact_wgpu_sections(...)` 复用该合同读取 PMREM RGBA16F cubemap mip-chain、SH9 144B `array<vec4<f32>, 9>` buffer 与 optional IEM 32x32x6 RGBA16F cubemap,section 长度校验仍归 `ibl_bake_artifact_readback.rs`,cache 写入仍归 asset artifact writeback。本切片不生产 compute 输出,不调度异步读回,不触发 cache 写回。 | `rustfmt --edition 2021 zircon_runtime\src\graphics\backend\render_backend\read_ibl_bake_artifact_sections.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib read_ibl_bake_artifact_sections --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-sections-readback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0,3/3 passed、0 failed、6874 filtered,测试本体 0.00s;exit 记录 elapsed 00:09:11.9856058;stderr 只有仓库既有 warnings;本切片不生成截图。 | 只关闭“已有 WGPU bake 资源 → descriptor-driven readback sections”的同步 acquisition helper;GPU/offline PMREM/SH9/BRDF/IEM bake、异步 readback scheduling、runtime readback-to-cache dispatch 集成、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2o WGPU SH9 buffer readback helper | render_plan11_ibl_wgpu_sh9_buffer_readback_helper_cargo_passed_gpu_bake_deferred | 已验证:WGPU backend 具备通用 buffer byte-range readback 与 IBL SH9 `array<vec4<f32>, 9>` 144B 字节读回 helper | 新增 `zircon_runtime/src/graphics/backend/render_backend/read_buffer_bytes.rs`;`read_buffer_bytes(...)` 封装 `copy_buffer_to_buffer`、map-read 与 shared RenderDoc readback marker,并拒绝 0 byte 或不满足 WGPU copy alignment 的长度;`read_buffer_f32x4_array_bytes(...)` 固定 `vec4<f32>` 数组布局,`read_buffer_sh9_f32x4_bytes(...)` 将 SH9 读回尺寸固定为 9×16B,与 `IBL_BAKE_ARTIFACT_SH9_SIZE_BYTES` 对齐;backend facade 导出 helper 供后续 runtime readback acquisition 接入。本切片不调度 IBL compute,不组装 artifact sections,不写 cache artifact。 | E 盘测试前剩余约 60.8GB;`rustfmt --edition 2021` 对新 buffer readback helper 与 backend facade 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib read_buffer_bytes --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-buffer-readback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_wgpu_buffer_readback_helper_cargo_20260706.out.log` 报告 2/2 passed、0 failed、6870 filtered,测试本体 0.00s;exit 记录 elapsed 00:08:55.6792597;stderr 只有仓库既有 warnings;本切片不生成截图。 | 只关闭 WGPU buffer byte-range/SH9 bytes readback helper;GPU/offline PMREM/SH9/BRDF/IEM bake、异步 readback scheduling、runtime readback-to-cache 集成、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2n WGPU RGBA16F cubemap readback helper | render_plan11_ibl_wgpu_rgba16float_region_readback_helper_cargo_passed_gpu_bake_deferred | 已验证:WGPU backend 具备按 mip/array-layer 读回 RGBA16F 区域与 face-major cubemap mip-chain 字节的基础 helper,环境 cube 纹理允许 COPY_SRC | 新增 `zircon_runtime/src/graphics/backend/render_backend/read_texture_rgba16float_region.rs`;`read_texture_rgba16float_region(...)` 支持指定 mip、origin、extent 的 `Rgba16Float` texture copy-to-buffer、map-read 与 row padding strip;`read_texture_rgba16float_cube_mip_chain(...)` 按 face-major、face 内 mip 顺序读取 cubemap bytes,与 `SourceCubemapMipChain`/`IblBakeArtifactPayload` PMREM layout 对齐;`SceneEnvironmentCubemap` 创建 source/specular/IEM cube texture 时增加 `COPY_SRC`,为后续 PMREM/IEM runtime readback acquisition 提供资源前提;本切片不调度 IBL compute,不读取 SH9 buffer,不写 cache artifact。 | `rustfmt --edition 2021` 对 backend helper、backend facade 与 environment cubemap 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --lib read_texture_rgba16float_region --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-wgpu-readback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_wgpu_rgba16float_region_readback_helper_cargo_20260706.out.log` 报告 2/2 passed、0 failed、6851 filtered,测试本体 0.00s;exit 记录 elapsed 00:17:19.2055212;stderr 只有仓库既有 warnings;本切片不生成截图。 | 只关闭 RGBA16F texture region/cube mip-chain readback helper 与 environment cube COPY_SRC 前提;GPU/offline PMREM/SH9/BRDF/IEM bake、SH9 buffer readback、异步 readback scheduling、runtime readback-to-cache 集成、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2m IBL runtime readback sections writeback | render_plan11_ibl_runtime_readback_writeback_cargo_passed_gpu_acquisition_deferred | 已验证:镜像 shader 06 §4.7,运行期 PMREM/SH9/IEM 读回分段可组装为 `.zribl` blob 并写入 `.zircon-cache` runtime cache | 新增 `zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact_readback.rs` 与 `zircon_runtime/src/asset/artifact/ibl_bake_artifact_runtime_writeback.rs`;`IblBakeArtifactReadbackSections` 以 descriptor 为真理校验 PMREM RGBA16F、SH9 与 optional IEM RGBA16F 分段长度,拒绝缺失/多余/错误长度分段后复用 `IblBakeArtifactPayload::decode(...)`;`write_ibl_bake_artifact_runtime_readback(...)` 对 stale descriptor 先跳过且不写文件,current descriptor 则生成 `IblBakeArtifactBlob` 并通过 `IblBakeArtifactCacheStore` 写入 runtime cache,报告 status/path/encoded_len/payload_len;render core 保持 backend-free,文件 IO 仍在 asset artifact 边界。 | `rustfmt --edition 2021` 对新增 readback/writeback、artifact/facade 与合同测试通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-writeback-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_runtime_readback_writeback_current_source_cargo_20260706.out.log` 报告 19/19 passed、0 failed、0 ignored、0 filtered,测试本体 0.49s;exit 记录 elapsed 00:17:45.8058804;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 只关闭“已取得读回字节 → artifact payload/blob → runtime cache”边界;GPU/offline PMREM/SH9/BRDF/IEM bake 生产、实际 WGPU readback acquisition/scheduling、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2j runtime IEM carrier + scene binding ABI | 已验证:镜像 shader 06 EC-M2j,optional IEM runtime carrier 与 6-binding scene ABI 接入 | `SourceCubemapEnvironment` 可携带 optional `SourceCubemapIrradianceCube`;scene group0 binding5 固定为 irradiance cube,主渲染路径在无 IEM 时提供 1x1 黑色 fallback、有 IEM 时上传 32x32x6 RGBA16F face-major 数据;shadow map fallback bind group 同步第 5 号槽;`zr_environment.wgsl` 暴露 `ZR_ENV_DIFFUSE_IEM` override、binding5 与 IEM 采样函数;真实 HDRI project/export helper 构建并附加 CPU IEM;artifact environment bridge 可应用 PMREM/SH9/IEM payload。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 12/12、0 failed、0 ignored、0 filtered,测试本体 0.07s;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` 通过 1/1、2 filtered,测试本体 0.21s;两次 exact lib layout test 试跑被既有 `graphics/text/cache/tests.rs` 的 `MeasureKey: Default` 编译错误阻断,未进入目标断言。本切片不生成新截图。 | 只关闭 runtime IEM 数据承载与 scene bind group ABI;`ZR_ENV_DIFFUSE_IEM` 默认仍 false,启用策略归后续 engine quality/specialization。GPU/offline IEM bake、runtime readback、derived artifact/staged build、product dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2l IBL artifact upload key for WGPU cubemap refresh | render_plan11_ibl_artifact_upload_key_wgpu_refresh_cargo_passed_product_dispatch_deferred | 已验证:artifact payload 应用后保留 IBL bake key,但 WGPU 环境 cube 上传缓存会感知 PMREM/SH9/IEM artifact 内容变化 | `SourceCubemapEnvironment` 新增 `SourceCubemapUploadKey` 与 `bake_artifact_hash`;`source_cubemap_environment_with_bake_artifact(...)` 对 descriptor+bake key+payload bytes 生成 artifact hash,只影响 runtime texture upload identity,不改变 `ibl_bake_key()`;`SceneEnvironmentCubemap::ensure_uploaded(...)` 由 source_revision/source_hash 二元判断改为完整 upload key,避免同一源命中不同 artifact payload 时跳过 specular PMREM/IEM GPU 重传;root render/environment facade 导出 upload key。 | `rustfmt --edition 2021 --check` 对 skybox、source-cubemap artifact、WGPU environment cubemap、facade 与合同测试通过;首次同命令异常退出 `1073807364` 且 stdout/stderr 为空,写入 `docs/tests/runtime/render/plan11_ibl_artifact_upload_key_current_source_cargo_20260706.exit.txt` 后不计通过;复跑 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-resolve-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_artifact_upload_key_current_source_cargo_retry_20260706.out.log` 报告 16/16 passed、0 failed、0 ignored、0 filtered,测试本体 0.06s;exit 记录 elapsed 00:12:55.2297214;stderr 只有仓库既有 warnings;本切片不生成截图。 | 只关闭 artifact 内容变化触发 WGPU source/specular/IEM cube 重传的 upload identity 合同;GPU/offline PMREM/SH9/BRDF/IEM bake、runtime readback 调度写入、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2k IBL artifact resolved payload priority | render_plan11_ibl_artifact_resolved_payload_priority_cargo_passed_gpu_readback_deferred | 已验证:镜像 shader 06 §4.7,来源 1/2 的 blob-backed artifact 可解析为可直接应用 payload,miss 时保留 runtime compute fallback | 新增 `zircon_runtime/src/core/framework/render/environment/ibl_bake_artifact_resolution.rs`;`IblBakeArtifactBlobCandidate` 承载 asset-derived/runtime-cache blob 候选,`resolve_ibl_bake_artifact_payload(...)` 复用三来源选择合同并返回 `IblBakeArtifactResolvedPayload`;asset derived current blob 优先于 runtime cache,stale derived 自动落到 current cache,全 miss 时 `blob()/payload()` 为 `None` 且按 required contents 产生 dispatch 计数;render/environment facade 导出新 API,文件系统职责仍在 `asset/artifact/ibl_bake_artifact_cache.rs`,payload 应用仍在 `source_cubemap_artifact.rs`。 | `rustfmt --edition 2021 --check` 对 resolved-payload child owner、facade 与合同测试通过;首轮同 target Cargo 在 20 分钟工具窗口内冷编译超时且未产生日志/二进制,不计通过;复跑 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-resolve-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_artifact_resolved_payload_current_source_cargo_retry_20260706.out.log` 报告 15/15 passed、0 failed、0 ignored、0 filtered,测试本体 0.08s;exit 记录 elapsed 00:14:30.4501358;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 只关闭 blob-backed resolved payload 优先级与 fallback 合同;GPU/offline PMREM/SH9/BRDF/IEM bake、runtime readback 调度写入、derived artifact importer/staged build-tool 生产、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2i IBL artifact payload application to source cubemap | render_plan11_ibl_artifact_payload_apply_to_source_cubemap_cargo_passed_gpu_readback_deferred | 已验证:镜像 shader 06 EC-M2i,decoded artifact payload 可接回现有 source cubemap 上传模型 | 新增 `zircon_runtime/src/core/framework/render/environment/source_cubemap_artifact.rs`;`source_cubemap_mip_chain_with_bake_artifact(...)` 保留 source/display mip 链给 skybox/source lookup,只替换 specular PMREM texels 与 diffuse SH9,并拒绝 face/mip layout 不匹配或缺 PMREM/SH9 的 payload;`source_cubemap_environment_with_bake_artifact(...)` 保留 `SourceCubemapEnvironment` 的 source revision/hash、intensity、rotation,并在 payload 含 IEM 时附加 `SourceCubemapIrradianceCube`;root render/environment facade 导出 API;`source_cubemap.rs` 只开放 `pub(super)` 构造口,新增职责未堆入接近 1000 行的大文件。 | `rustfmt --edition 2021 --check` 对 source-cubemap artifact child owner、facade 与合同测试通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-apply-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_artifact_payload_apply_current_source_cargo_20260706.out.log` 报告 12/12 passed、0 failed、0 ignored、0 filtered,测试本体 0.05s;exit 记录 elapsed 00:07:33.5548958;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 只关闭 artifact payload 应用到 SourceCubemapMipChain/SourceCubemapEnvironment 与 optional IEM attach 的 CPU 合同;GPU/offline PMREM/SH9/BRDF bake、runtime readback 调度写入、derived artifact/staged build-tool、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2h IBL bake artifact runtime cache store | render_plan11_ibl_bake_artifact_runtime_cache_store_cargo_passed_readback_deferred | 已验证:镜像 shader 06 EC-M2h,来源 2 `.zircon-cache` runtime cache raw blob store 完成 | 新增 `zircon_runtime/src/asset/artifact/ibl_bake_artifact_cache.rs`;`IblBakeArtifactCacheStore` 以 `.zircon-cache/render/ibl/v{IBL_BAKE_ALGORITHM_VERSION}/{request_hash}/face_####_mips_##.zribl` 作为 runtime bake artifact 文件入口,写入 `IblBakeArtifactBlob` 原始字节,读取时区分 hit/missing/rejected,只有 current hit 变成 runtime-cache candidate;render core artifact DTO 继续保持 backend-free,文件系统职责留在 asset artifact cache 边界。 | `rustfmt --edition 2021` 对 cache store、artifact mod 与合同测试通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-payload-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_payload_roundtrip_current_source_cargo_20260706.out.log` 报告 9/9 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;exit 记录 elapsed 00:05:41.7209590;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 只关闭 runtime cache file store/read/write 合同;GPU/offline PMREM/SH9/BRDF bake、runtime readback 调度写入、derived artifact/staged build-tool、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2g IBL bake artifact blob byte container | render_plan11_ibl_bake_artifact_blob_roundtrip_cargo_passed_filesystem_io_deferred | 已验证:镜像 shader 06 EC-M2g,完整 header+payload artifact blob 字节容器完成 | 新增 `ibl_bake_artifact_blob.rs`,让 reusable artifact 从 header/payload 两个合同推进为完整 byte blob:108B header + descriptor-sized payload;`decode(...)` 拒绝截断 header 与 payload 长度不匹配,`decode_current_for_request(...)` 同时验证 request/key/layout/content/algorithm-version 当前性;environment/root render facade 导出 blob API。 | `rustfmt --edition 2021` 对 blob、facade 与合同测试通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-payload-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_payload_roundtrip_current_source_cargo_20260706.out.log` 报告 9/9 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;exit 记录 elapsed 00:05:41.7209590;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 只关闭 byte blob container;filesystem placement、runtime `.zircon-cache` read/write、GPU/offline PMREM/SH9/BRDF bake、runtime readback 回写、staged importer/build-tool、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2f IBL bake artifact payload layout + roundtrip | render_plan11_ibl_bake_artifact_payload_roundtrip_cargo_passed_file_io_deferred | 已验证:镜像 shader 06 EC-M2f,PMREM/SH9/IEM artifact payload 字节布局与 round-trip 合同完成 | `IblBakeArtifactPayload` 在 `core/framework/render/environment` 内承载 reusable artifact 的 in-memory payload:PMREM RGBA16F face-major 全 mip、SH9 9×vec4 f32 little-endian、可选 IEM 32x32x6 RGBA16F(alpha=1.0);descriptor expected size、三段 byte-range helper 与 decode helper 公开同一布局;root render facade 与 environment facade 导出 payload API 与字节常量。 | `rustfmt --edition 2021` 对 `ibl_bake_artifact.rs`、facade 与合同测试通过;`cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-ibl-artifact-payload-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_payload_roundtrip_current_source_cargo_20260706.out.log` 报告 9/9 passed、0 failed、0 ignored、0 filtered,测试本体 0.03s;exit 记录 elapsed 00:05:41.7209590;stderr 只有仓库既有 warnings;本切片不生成截图、不写 target 图像。 | 只关闭 in-memory payload layout/roundtrip;磁盘 container IO、GPU/offline PMREM/SH9/BRDF bake、runtime readback 回写、staged importer/build-tool、product 二次启动 dispatch=0、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2e IBL bake artifact header + source-selection contract | render_plan11_ibl_bake_artifact_contract_cargo_passed_gpu_cache_deferred | 已验证:镜像 shader 06 EC-M2e,§4.7 的持久化 artifact header、算法版本失效、三来源优先序与 runtime compute dispatch 计数合同已完成 | 新增 `ibl_bake_artifact.rs`,在 `core/framework/render/environment` 内独立承载 `IblBakeArtifactRequest`/`Descriptor`/`Header`/`Candidate`/`Selection`;header 记录 `IblBakeKey`、`IBL_BAKE_ALGORITHM_VERSION`、face/mip layout 与 PMREM/SH9/IEM content bits;选择逻辑固定 asset derived artifact > `.zircon-cache` runtime cache > runtime compute,命中 derived/cache 时 `environment_compute_dispatch_count() == 0`;root render facade 与 environment facade 导出公开 API;新增 `runtime_environment_ibl_bake_artifact_contract.rs`;新增模块文档 `docs/zircon_runtime/core/framework/render/environment/ibl-bake-artifact.md`。 | `rustfmt --edition 2021` 对新模块、facade 与测试通过;首轮 focused Cargo 因冷编译/外层工具超时无可计日志,不计通过;复跑 `cargo test -p zircon_runtime --test runtime_environment_ibl_bake_artifact_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_ibl_bake_artifact_contract_current_source_cargo_20260706.out.log` 报告 3/3 passed、0 failed、0 ignored、0 filtered,测试本体 0.00s,总 elapsed 00:10:56.4688476;stderr 只有仓库既有 warnings。 | 只关闭 §4.7 的 header/选择合同;实际 PMREM+SH9+IEM payload 序列化、GPU/offline bake、readback 回写、staged importer/build-tool 产物、product 二次启动 dispatch=0 统计、RenderDoc/product capture、probe capture/blending、4K/16K 离线 bake 与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M2d CPU source IEM bridge | 已验证:镜像 shader 06 EC-M2d,optional diffuse IEM CPU reference artifact 与公开合同完成 | 新增 `source_irradiance_cubemap.rs`,在 `core/framework/render/environment` 内独立承载 32x32x6 face-major IEM,避免继续扩大 `source_cubemap.rs`;IEM 通过 direct cosine convolution source cubemap diffuse mip 构建,采样端支持 cross-face bilinear tap;root render facade 与 environment facade 已导出 IEM 构建/采样 API;新增 focused contract test。 | `rustfmt` 对新模块、facade 与测试通过;首轮 focused Cargo run 暴露 root facade 未导出新 API,补 re-export 后 `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_irradiance_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-iem-contract-0706 --message-format short --color never -- --nocapture --test-threads=1` 通过 2/2;后续 Cargo wrapper recheck 在工具边界 904s 超时,不计通过;直接运行已构建测试二进制通过 2/2(15.85s);本切片不生成截图、不写 target 图像。 | IEM 当前仍未绑定到 runtime shader;继续执行 GPU/offline IEM bake、derived artifact/cache、runtime cube upload/binding、SH9/IEM sampling option、diffuse screenshot 对拍、RenderDoc/product capture、probe capture/blending 与 full CI。 |
| 2026-07-06 | EL-M1/EC-M3f old HDRI sampled screenshot rejection + PMREM design lock | 设计完成:镜像 shader 06 EC-M3f,旧 20260704 sampled-equirect 图明确拒收 | 用户复核确认 `runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png` 的天空盒和反射 mip 都是低分辨率马赛克;本行撤销它作为 cubemap/PMREM 验收图的语义,只保留为 rejected baseline。设计收敛到 source cube mip0 skybox、specular PMREM 独立 cube、1K→256 face/2K→512 face、UE GGX FIS roughness/mip 对数映射、cmft solid-angle/edge/final-mip 纪律与 cmftStudio IEM/PMREM 消费分工。 | 文档设计切片,不修改 renderer/source production owner,不生成新截图;`git diff --check` 对本轮文档只有既有 LF/CRLF warning,无 whitespace error。 | 按该设计继续 EL-M1/EC-M3 的严格 high-frequency/roughness 截图单调、SSIM/RenderDoc/product capture、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、4K/16K 离线 bake 与 full CI。 |
| 2026-07-06 | EL-M1/EC-M3e 2K HDRI Plan06 metrics current-source Cargo refresh | render_plan11_hdri_2k_plan06_metrics_current_source_cargo_passed_renderdoc_deferred | 已验证:已保存 2K HDRI PMREM PBR matrix PNG 在当前源码 Cargo wrapper 下通过非 ignored Plan06/Shader06 指标守卫;纯验证切片,不修改 renderer/source owner 代码。 | `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-png-metrics-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_hdri_2k_plan06_metrics_current_source_cargo_20260706.out.log` 报告 running 1 test,结果 1 passed、0 failed、0 ignored、2 filtered,测试本体 0.38s;exit 记录 elapsed 00:11:40.9052706,PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 为 1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`;stderr 只有仓库既有 warning。 | 只关闭已保存 2K PNG 的 Plan06/EC-M3e 指标回放 current-source Cargo refresh;RenderDoc/product capture、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、SSIM/seam 对拍、4K/16K 离线 bake 与 full test-target/CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M3e 2K HDRI PNG metrics non-ignored regression + test owner split | 已验证:镜像 shader 06 EC-M3e,保存后的 2K HDRI PBR matrix PNG 现在由非 ignored 回归读回并执行环境矩阵量化 | `runtime_shader_pbr_hdri_export.rs` 将 real-HDRI screenshot response metrics 拆到 `runtime_shader_pbr_hdri_export/hdri_metrics.rs`;新增 `runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics`,直接读取 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png`,检查尺寸、sky directional variation、metallic/smoothness matrix response 与旧 16x8 sample-table grid seams;导出测试文件降到 525 行,度量 owner 为 382 行。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_shader_pbr_hdri_export.rs zircon_runtime\tests\runtime_shader_pbr_hdri_export\hdri_metrics.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-png-metrics-0706 --message-format short --color never runtime_shader_pbr_real_hdri_2k_reflection_png_matches_plan06_metrics -- --exact --nocapture --test-threads=1` 通过 1/1,0 ignored,2 filtered,测试本体 0.23s,总构建 8m43s,stderr 只有仓库既有 warnings;PNG 1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`;同名 PNG 在 `target` 与 `E:\cargo-targets` 扫描 0;`git diff --check` 对本轮测试文件无 whitespace error(仅 LF/CRLF warning)。 | 关闭保存后截图的非 ignored 验收缺口;EL-M1/EC-M3 仍未全关闭:RenderDoc/product capture、严格高频/roughness 单调/SSIM、GPU/offline bake、derived cache、IEM、probe capture/blending、4K/16K 离线 bake 与 full test-target/CI 继续执行。 |
| 2026-07-05 | EL-M1/EC-M3d cmft/UE cubemap mip edge sampling + 2K HDRI PMREM export | 已验证:镜像 shader 06 EC-M3d,CPU source/PMREM mip 跨面采样与高粗糙度末级模糊修复,2K 真实 HDRI PBR matrix 截图通过 | `source_cubemap.rs` 的规则 source mip 与 GGX PMREM 输入采样改为跨 face 投影 tap,不再对 CPU mip 生成做 per-face clamp;高粗糙度 GGX PMREM 使用 128 样本,roughness 饱和后的后续 mip 从上一层 PMREM 降采样,避免末级重新采源造成块状马赛克;`runtime_shader_pbr_hdri_export.rs` 新增 Poly Haven lakes 2K HDRI 导出路径,天空盒采 source cube mip0,PBR 反射采 specular PMREM cube + BRDF LUT。 | `rustfmt --edition 2021` 对本轮 Rust 文件通过;`cargo test -p zircon_runtime --lib source_cubemap_linear_sampling_bleeds_across_face_edges --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --nocapture` 通过 1/1;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 9/9;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_2k_reflection_png --nocapture --test-threads=1` 通过 1/1;PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 为 1280x960,1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`,80,333 unique colors;`polyhaven_lakes_2k.hdr` 为 5,918,432 bytes,SHA256 `B2506E0EE912C4C599FF013566FBD3ECAAC2F4B176319D450CCE0DE5758FED98`;同名 PNG 未出现在 `target` 或 `E:\cargo-targets`。 | 只关闭当前 source-cubemap CPU bridge 的 2K 截图与 mip 合同;RenderDoc/product capture、GPU/offline bake、derived cache、IEM、probe capture/blending、SSIM/seam 对拍、4K/16K 离线资产与 full CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M3b-EC-M3c source cubemap contract current-source Cargo refresh | render_plan11_source_cubemap_contract_current_source_cargo_passed_renderdoc_deferred | 已验证:source cubemap PMREM/SH9/HDR contract 在当前源码 Cargo wrapper 下复跑通过;纯验证切片,不修改 renderer/source owner 代码。 | `CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_source_cubemap_contract_current_source_cargo_rerun_20260706.out.log` 报告 running 9 tests,覆盖 high-frequency PMREM blur、progressive blur、constant env、rough mip seam energy、roughest mip all-face average、roughness mapping shader contract、HDR float upload preservation、SH9 constant diffuse 与 SH9 vertical gradient;结果 9 passed、0 failed、0 ignored、0 filtered,测试本体 6.83s;exit 记录 elapsed 00:11:14.0717002;stderr 只有仓库既有 warning。前一轮 `source_cubemap_linear_sampling_bleeds_across_face_edges --lib --exact` 0-test 与 source-cubemap contract 1204s timeout 均保留为不计通过的历史尝试。 | strict source-cubemap contract fresh pass 已关闭;RenderDoc/product capture、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、SSIM/seam 对拍、full test-target/CI 仍未关闭。 |
| 2026-07-06 | EL-M1/EC-M3a 2K HDRI PMREM export current-source Cargo refresh | render_plan11_hdri_2k_pmrem_export_current_source_cargo_passed_renderdoc_deferred | 已验证:镜像 shader 06 EC-M3a,纯验证切片,不修改 renderer/source owner 代码;复验 Poly Haven lakes 2K HDRI 路径可通过当前 source/specular PMREM、BRDF LUT 与 SH9/source cubemap bridge 导出高分辨率 8x8 PBR matrix PNG。 | `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_2k_reflection_png --nocapture --test-threads=1` exit 0;日志 `docs/tests/runtime/render/plan11_hdri_2k_pmrem_export_current_source_cargo_20260706.out.log` 报告 1/1 passed、1 filtered、测试本体 158.36s,总 elapsed 00:15:58.6938140;PNG `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_2k_hdr_pmrem_reflection_20260705.png` 为 1,009,731 bytes,SHA256 `920A028DC6B0BB64A45F1798E89BF5E0FBE2BABF3A90BED22FFBA842DD1714F0`;stderr 只有仓库既有 warning。另一次 `source_cubemap_linear_sampling_bleeds_across_face_edges --lib --exact` 只跑到 0 tests,source-cubemap contract refresh 1204s 工具超时且无 stdout/stderr,均不计通过。 | 只关闭 2K HDRI PMREM export current-source Cargo refresh;RenderDoc/product capture、strict source-cubemap contract fresh pass、GPU/offline PMREM/SH9/BRDF bake、derived cache、IEM、probe capture/blending、SSIM/seam 对拍、full test-target/CI 仍未关闭。 |
| 2026-07-05 | EL-M1/EC-M3c source cubemap PMREM seam quantization guard | 已验证:镜像 shader 06 EC-M3c,source PMREM 粗 mip 新增 cube seam 亮度差守卫 | `runtime_environment_source_cubemap_contract.rs` 通过公开 cubemap 投影 API 反推相邻面,对 mip0、中间粗 mip、rough mip 的四边 luma delta 做 mean/max 量化,断言中间/rough PMREM mip 持续降低 seam energy,避免低 mip 反射在 cube 面边界重新块状断裂。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_environment_source_cubemap_contract.rs` 通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 9/9(测试本体 4.74s,总 7m18s,既有 warnings);本切片不生成新截图。 | source CPU PMREM seam 合同已覆盖;GPU/offline PMREM 产物 seam 对拍、SSIM、GPU/offline bake、derived cache、IEM、probe capture/blending 与截图级高频/粗糙度验收继续按 shader 06/plan 11 后续执行。 |
| 2026-07-05 | EL-M1/EC-M3b source cubemap PMREM mip blur contract | 已验证:镜像 shader 06 EC-M3b,PMREM mip 链新增逐级高频模糊公开合同 | `runtime_environment_source_cubemap_contract.rs` 新增高频棋盘+亮斑环境,对 PMREM 全 mip 计算 luma variance,断言 mip1 低于 mip0、后续 roughness 更高的 mip 持续下降、粗 mip 相对 base 重度模糊;另公开锁定 final 1x1 mip 六面平均。 | `rustfmt --edition 2021 zircon_runtime\tests\runtime_environment_source_cubemap_contract.rs` 通过;同一 `cargo test -p zircon_runtime --test runtime_environment_source_cubemap_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 首轮在冷编译期间工具超时,等待后台 cargo/rustc 结束后复跑通过 8/8(测试本体 3.56s,总 7m53s,既有 warnings);本切片不生成新截图。 | 仍不是完整 EL-M2/EC-M3 验收:预滤波 cube seam、SSIM 源参考、GPU/offline bake、derived cache、IEM、probe capture/blending 与截图级高频/粗糙度验收继续按 shader 06/plan 11 后续执行。 |
| 2026-07-05 | EL-M1/EC-M3a 8x8 HDRI PBR matrix quantitative guard | 已验证:镜像 shader 06 EC-M3a,真实 HDRI PBR 矩阵导出已增加整表量化与旧 16x8 马赛克边界回归检测 | `runtime_shader_pbr_hdri_export.rs` 对 64 个 metallic/smoothness 球体格采样,断言亮度范围、smooth metal/dielectric 端点差异、metallic 与 smoothness 分组差异、row/column 响应覆盖率;天空上半区检测旧 sampled-equirect 16×8 对齐边界;`scene_bind_group_layout_entries()` 定义可见性同步到 `scene_renderer` 子树,保持 scene layout 5-binding ABI 可供 renderer/prewarm/test layout 复用。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`CARGO_INCREMENTAL=0 cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-hdr-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过 1/1;PNG `runtime_shader_pbr_real_hdri_lakes_hdr_pmrem_reflection_20260705.png` 1280x960,845,069 bytes,SHA256 `64D5873A09DDC348C15A2444221DF07961A47A34D5FB281B73F7122FEAB0782E`,74,903 unique colors;量化采样:luma range `124.501..233.256`,smooth endpoint delta `85.106`,metal sweep group delta `32.130`,high-metal rough/smooth group delta `59.064`,responsive rows/columns `8/8`/`6/8`,legacy grid vertical/horizontal means `0.02491`/`0.02711`;同名图未出现在 `target` 或 `E:\cargo-targets`;`git diff --check` 仅 LF/CRLF warning。 | 仍不是完整 EL-M2/EC-M3 验收:SSIM 源 cubemap 参考、严格高频/粗糙度单调、预滤波 cube seam、GPU/offline bake、derived cache、IEM 与 probe capture/blending 继续按 shader 06/plan 11 后续执行。 |
| 2026-07-05 | EL-M1/EC-M2c HDR source/PMREM float bridge + BRDF LUT binding | 已验证:镜像 shader 06 EC-M2c,source cubemap sky 与 specular PMREM 运行时分离并改 RGBA16F | `SourceCubemapMipChain::source_texels()` 保留 source mip 链,`texels()` 保留 GGX PMREM;scene group0 binding1 source cube、binding3 RG16F BRDF LUT、binding4 specular PMREM cube;`zr_environment.wgsl` 使用 PMREM cube + BRDF LUT 做 standard PBR 间接高光;真实 HDRI helper 保留 exposed linear HDR;shadow-map scene bind group fallback 同步 5 binding ABI。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`runtime_environment_source_cubemap_contract` 通过 6/6;`runtime_environment_brdf_lut_contract` 通过 2/2;ignored `runtime_shader_pbr_hdri_export` 在修复 shadow bind group 3/5 ABI drift 后通过 1/1;PNG `runtime_shader_pbr_real_hdri_lakes_hdr_pmrem_reflection_20260705.png` 1280x960,845,069 bytes,SHA256 `64D5873A09DDC348C15A2444221DF07961A47A34D5FB281B73F7122FEAB0782E`,74,903 unique colors,未出现在 `target` 或 `E:\cargo-targets`;`git diff --check` exit 0(仅 LF/CRLF warnings)。 | 仍不是完整 EL-M2 IBL:GPU/offline bake、derived cache、IEM、seam quant、probe capture/blending 与 EC-M3 8x8 定量验收继续按 shader 06/plan 11 后续执行。 |
| 2026-07-05 | EL-M1/EC-M2b source cubemap SH9 diffuse bridge | 已验证:镜像 shader 06 EC-M2b,source cubemap diffuse 改用 SH9 irradiance | `SourceCubemapMipChain` 持有 9×vec4 SH9;构建时从规则 source mip 金字塔的 32x32 face 档做 exact-solid-angle 投影并预乘 cosine lobe;`SourceCubemapEnvironment`、`SceneUniform.environment_sh9` 与 `zr_environment_sh9_eval(...)` 接线;source cubemap diffuse 不再采样 rough low mip;静态 WGSL SceneUniform ABI 同步。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`runtime_environment_source_cubemap_contract` 通过 5/5;ignored `runtime_shader_pbr_hdri_export` 在 `E:\cargo-targets\zircon-hdri-sh9-export-it-0705` 且 `CARGO_INCREMENTAL=0` 下通过 1/1;PNG `runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png` 1280x960,862,851 bytes,SHA256 `D3A8077CAD3A7F0CBD83634F33EBBFA62422E3C75E2B65B34AA85A66FEDF0029`,55,383 unique colors,未出现在 `target` 或 `E:\cargo-targets`;lib shader-string test 仍被当前脏工作区既有 runtime_absorption/text atlas test 编译债阻断。 | 仍不是完整 EL-M2 IBL:GPU/offline SH9 compute、RGBA16F PMREM artifact、BRDF LUT/IEM、derived cache、probe capture/blending 与 EC-M3 定量截图验收继续按 shader 06/plan 11 后续执行。 |
| 2026-07-05 | EL-M1 sampled-equirect bridge hard delete | 已验证:镜像 shader 06 EC-M1e,旧 sampled/equirect 过渡桥从 runtime 源码与测试中硬删 | 删除 `environment/equirect_samples.rs` 与 sampled mip-chain 模块文档;移除 `SampledEquirectangularEnvironment`、`SharedSampledEquirectangularSamples`、`SkyboxMode::SampledEquirectangular`、sampled `EnvironmentExtract`/`SkyboxSettings` 构造、sampled re-export 和 scene uniform sampled 分支;`IblBakeKey` 只保留 source cubemap hash。 | `rustfmt --edition 2021` 对硬删涉及 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过(既有 warning 385);`runtime_environment_source_cubemap_contract` 通过 3/3;ignored `runtime_shader_pbr_hdri_export` 通过 1/1(测试本体 26.92s);`git grep` 确认 `zircon_runtime/src` 与 `zircon_runtime/tests` 中 sampled/equirect 旧 API 与 `environment_samples` 零命中;PNG `runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png` 仍在 `docs/tests/runtime/shader/`,未出现在 `target` 或 `E:\cargo-targets`。 | sampled-equirect 兼容桥删除完成;生产级 RGBA16F/GPU/offline PMREM、SH9 diffuse、BRDF LUT/IEM、derived artifact/cache、seam 与 8x8 PBR 定量验收继续按 shader 06 执行。 |
| 2026-07-05 | EL-M1/EC-M2a source cubemap GGX PMREM bridge | 已验证:镜像 shader 06 EC-M2a,真实 HDRI source-cube 反射 mip 已从 Blinn 过渡滤波切到 UE 风格 GGX FIS | `SourceCubemapMipChain` 保持 mip0 为天空盒源 cubemap,高 mip 改为 CPU GGX filtered importance sampling PMREM bridge;roughness/mip 常数为 `1.0/1.2`,FIS 通过 sample solid angle 选择 source mip 并用 `NoL` 加权;新增 `runtime_environment_source_cubemap_contract.rs`;真实 HDRI export 输出 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_ggx_pmrem_reflection_20260705.png`;同步更新 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`。 | `rustfmt --edition 2021` 对本切片 Rust 文件通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`runtime_environment_source_cubemap_contract` 通过 3/3;ignored `runtime_shader_pbr_hdri_export` 通过 1/1(测试本体 24.99s);PNG 1280x960,863,859 bytes,SHA256 `8F361638E61343B48C92DA12F59978729118CE4DB8DABEA80FEE6AC1FA712C5A`,55,055 unique colors,upper sky avg `152.278,201.774,221.422`,lower sky avg `115.737,114.429,111.059`;确认同名图只在 `docs/tests/runtime/shader/`,不在 `target` 或 `E:\cargo-targets`。广义 `cargo test -p zircon_runtime --lib source_cubemap ...` 当前被既有 `runtime_absorption/plan_status` private re-export errors 阻断。 | 仍不是完整 EL-M1/EL-M2 IBL:source cubemap 仍为 CPU + RGBA8 tone-mapped bridge;sampled-equirect bridge 已由后续 hard delete 行关闭;生产级 RGBA16F/GPU/offline PMREM、SH9 diffuse、BRDF LUT/IEM、derived artifact/cache、seam 与 8x8 PBR 定量验收继续按 shader 06 执行。 |
| 2026-07-05 | EL-M1 / sampled HDRI shared sample storage | 历史过渡:栈压力收敛,后续已被 hard delete 行关闭 | `SampledEquirectangularEnvironment` 改为持有 `SharedSampledEquirectangularSamples` (`Arc<SampledEquirectangularSamples>`),`empty_sampled_equirectangular_samples()` 通过 `OnceLock` 复用空表;`SkyboxSettings`/scene uniform 测试路径不再按值复制 174,763-sample mip 表。同步导出 `SharedSampledEquirectangularSamples` 和空样本 helper,并保持 SourceCubemap 走独立 cube texture/fallback-none 路径。 | Plan 08 回归中复验该底层修复:`plan08_material_phase_ordering_after_shared_equirect_samples_export_present_rerun_20260705.out.log` 通过 1/1;`plan08_material_phase_ordering_neighbors_after_shared_equirect_samples_20260705.out.log` 通过 3/3。 | sampled-equirect runtime 兼容桥已由 EL-M1 hard delete 行移除;历史证据仅保留说明过渡期栈压力修复。 |
| 2026-07-05 | EL-M1 / source cubemap HDRI skybox + radiance mip bridge | 已验证:镜像 shader 06 EC-M1d,真实 HDRI 截图路径已从 sampled equirect 切到 `texture_cube` source cubemap | `SourceCubemapMipChain` 以 face-major 全 mip 链承载 HDRI 重投影;cmftStudio gloss scale/bias + Blinn BRDF power 的 radiance lobe 生成反射 mip,末级 mip 六面平均;scene bind group 环境段绑定 cube texture view + filtering sampler;skybox 与 PBR 环境 include 直接采样 `texture_cube<f32>`;导出 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_source_cubemap_reflection_20260705.png`;新增 `docs/zircon_runtime/core/framework/render/environment/source-cubemap.md`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`cargo test -p zircon_runtime --lib source_cubemap --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 7/7;`runtime_environment_cubemap_projection_contract` 通过 4/4;`runtime_texture_cube_resource_contract` 通过 3/3;ignored HDRI export 通过 1/1(构建 22m04s,测试本体 13.97s);PNG 1280x960,874,867 bytes,SHA256 `9AFA5DDEC8AE50A2A7CC4342B50F3B16442ED0BB29891D0B42C9BD8938ED98BF`,58,598 unique colors,旧 16x8 网格探针 column ratio `1.0052`/row ratio `0.9672`,目视无明显块状马赛克。 | EL-M1 sampled-equirect bridge 硬切删除已由 hard delete 行关闭;EC-M2 继续生产级 RGBA16F GGX FIS PMREM、SH9 diffuse、BRDF LUT/IEM、derived artifact/cache 与完整 8x8 PBR 定量验收。 |
| 2026-07-05 | EL-M1 / `texture_cube` 资源合同基础 | 已验证:资源层合同完成 | 镜像 shader 06 EC-M1c:新增 `RenderImageDimension::Cube`;texture descriptor 支持 `cube`/`cubemap` 维度、默认 6 面、拒绝非 `6N` 层数;RGBA8/compressed upload readiness 接受 square cube shape;WGPU resource creation 将 cube 映射为 D2 texture + Cube/CubeArray view,普通 D2 材质贴图仍保持 layer0 D2 view;新增 `runtime_texture_cube_resource_contract.rs` 与 `docs/zircon_runtime/core/framework/render/cube-texture-resource-contract.md`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`cargo test -p zircon_runtime --test runtime_texture_cube_resource_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 3/3。本切片未生成截图;为恢复集成测试编译,同步修正当前工作区 UI text native bitmap atlas cache 可见性。`cargo check --tests` 仍被既有 `m1_runtime_editor_boundary_contract.rs` 缺 `environment` 字段阻断。 | 已由 EL-M1/source cubemap HDRI 行和 hard delete 行接续完成真实 HDRI source-cube 截图路径与 sampled-equirect bridge 硬删除;剩余为 EL-M2 的 PMREM/SH9/BRDF LUT/IEM 生产级 IBL。 |
| 2026-07-05 | EL-M1 / cubemap 投影黄金参考 | 已验证:公开 API 投影合同完成 | 镜像 shader 06 EC-M1b:新增 `core/framework/render/environment/cubemap_projection.rs`,按 cmft/UE face order `+X,-X,+Y,-Y,+Z,-Z`、texel center `[-1,1]`、lat-long UV 与 exact solid angle 建立 CPU 黄金参考;`core/framework/render/mod.rs` 顶层 re-export;新增 `runtime_environment_cubemap_projection_contract.rs` 验证公开 API。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never` 通过;`cargo test -p zircon_runtime --test runtime_environment_cubemap_projection_contract --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-cubemap-projection-check-0705 --message-format short --color never -- --nocapture --test-threads=1` 通过 4/4。本切片只验证 cubemap 投影合同,不生成截图或 target 图片。 | 下一步继续 EL-M1 的 GPU `texture_cube` 资源、`equirect_to_cube.wgsl` 与 skybox 源 cubemap 采样硬切;EL-M2 接 GGX FIS/SH9/BRDF LUT 与粗糙度 mip 截图验收。 |
| 2026-07-05 | EL-M1 / sampled HDRI mip 链纠偏 | 中间实现,截图验证通过 | 镜像 shader 06 EC-M1a:scene group0 binding1 新增 read-only environment sample storage buffer;`SkyboxMode::SampledEquirectangular` 的 16x8 table 替换为 512x256 base + 10 mip 链(174,763 vec4);mip 生成参照 cmft radiance filter,以 solid angle × `dot^power` 方向卷积替代 2x2 box downsample,并使用 cmftStudio 默认 gloss scale/bias + Blinn-BRDF lighting model;scene uniform 删除内嵌 `environment_samples`;skybox 与 PBR helper 使用 bilinear/lod 采样,roughness→mip 对齐 UE reflection capture 公式,真实 HDRI export 改写为 `runtime_shader_pbr_real_hdri_lakes_pmrem_reflection_20260705.png`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-core-check-0705 --message-format short --color never` 通过(2026-07-05,1m55s);首次 ignored export 因 512x256 大数组按值复制导致 32MB 测试线程栈溢出,改为 128MB 栈后 `cargo test -p zircon_runtime --test runtime_shader_pbr_hdri_export --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hdri-pmrem-export-it-0705 --message-format short --color never -- --ignored --exact export_runtime_shader_pbr_real_hdri_reflection_png --nocapture --test-threads=1` 通过(1/1,8.75s);PNG 1280x960,862,941 bytes,SHA256 `2FD20BB1109EC754F7DAED70AA6F8CABF2D8651FBEB1B32F4149AF89FFCB57F7`,49,960 unique colors,位于 `docs/tests/runtime/shader/`,未放入 target。 | 历史行保留该中间 storage-buffer/equirect 过渡路径证据;后续 `SharedSampledEquirectangularSamples` 行已收敛大数组按值复制栈压力。继续按 shader 06 EC-M1 硬切 GPU `texture_cube` 源 skybox,EC-M2 接 GGX FIS/SH9/BRDF LUT。 |
| 2026-07-05 | EL-M1/M2 IBL 算法定稿镜像登记 | 登记(文档,无代码) | shader 计划集新增子计划 06(`docs/plans/zircon_runtime/shader/06-environment-ibl-and-pbr-correctness.md`):定稿 equirect→cube、GGX 预滤波(filtered importance sampling 为硬性前提,样本档位表语义随之更新)、SH9(立体角权重、烘焙端预乘 A_l 与基函数常数)、BRDF LUT(128×128 RG16F,新增一个 group1 环境段 binding,实施期在本计划登记确切编号)与 `zr_environment.wgsl` v2 契约。差异定稿:skybox 显示采样"源 cubemap"全分辨率(非 128³ 预滤波链);实时天空捕获时间切片列为 shader 06 EC-M4;16×8 sampled equirect table、scene uniform `environment_samples` 字段与两方向 lerp 伪粗糙度路径列入硬切换删除清单;离线侧定稿:PMREM(GGX mip 链)/SH9/可选 IEM(32³ irradiance cube,新增 `zr_env_irradiance` 一槽)按 `IblBakeKey`+算法版本以三来源优先序持久化(资产伴生 → 缓存回写 → 现场 compute),二次启动环境 compute dispatch=0 为验收(shader 06 §4.7/§4.8)。 | 文档登记;算法出处见 shader 06 §2 精读表(cmft/UE5 文件:行号)。 | EL-M1 切片 2 与 EL-M2 实施时以本计划工程落地细化 + shader 06 §4 算法定稿为共同权威。 |
| 2026-07-04 | EL-M1 / 环境契约与抽取接线 | 已验证: 契约与抽取完成,skybox/PBR 后续行已接续关闭 | 新增 `core/framework/render/environment` 契约类型并导出 `EnvironmentExtract`、`SkyboxSettings`、`ProceduralSkyParams`、`IblBakeKey`;`RenderFrameExtract` 与 `SceneViewportRenderPacket` 持有 `EnvironmentExtract`;视口 preview sky 映射为 procedural 默认环境并保留旧 preview 输出。 | `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m1-contract-0704 --message-format short --color never` 通过；`cargo test -p zircon_runtime --lib core::framework::render::environment --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m1-contract-0704 --message-format short --color never -- --nocapture --test-threads=1` 通过,6 passed。 | 后续仍不包含 cubemap 资产导入、reflection probe 捕获、lightmap/probe grid 或 analytic fog。 |
| 2026-07-04 | EL-M1 / 正式 procedural skybox 路径 | 已验证: 可见 skybox 改由 `EnvironmentExtract` 驱动 | 新增 `graphics/scene/scene_renderer/environment` route、`procedural_environment.rs` 与 `skybox_procedural.wgsl`;scene uniform 增加 sky horizon/zenith/ground 与 environment params;preview sky pass 只按 `SkyboxMode::ProceduralGradient` 绘制,不再自持 fallback-sky policy。 | `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m2-skybox-0704 --message-format short --color never` 通过。 | 旧 preview executor 的完全删除、外部 cubemap skybox 与 RenderDoc/product capture 仍属后续切片。 |
| 2026-07-04 | EL-M1 / PBR 环境反射采样 | 已验证: standard/fallback/deferred PBR 消费同一环境 helper | 新增 `graphics/shader/wgsl/zr_environment.wgsl`,并由 shader template support includes、fallback mesh shader、standard PBR WGSL 与 deferred lighting WGSL 统一调用 `zr_environment_pbr_indirect(...)`;roughness 由 smoothness 单调映射,环境反射受 metallic/smoothness 影响。 | `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::deferred::lighting_pipeline --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m3-pbr-0704 --message-format short --color never -- --nocapture --test-threads=1` 通过 13/13；`cargo test -p zircon_runtime --lib scene_renderer::mesh::mesh_pipeline::fallback_mesh_shader_source --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m3-pbr-0704 --message-format short --color never -- --nocapture --test-threads=1` 通过 14/14；`cargo test -p zircon_runtime --lib graphics::shader::template --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m3-pbr-0704 --message-format short --color never -- --nocapture --test-threads=1` 通过 21/21。 | 该行是 procedural 环境采样,不是 cubemap prefilter、reflection probe blending 或 SH diffuse bake 完成。 |
| 2026-07-04 | EL-M1 / 8x8 PBR metallic-smoothness matrix | 已验证: 截图产物写入 docs/tests/runtime/shader | 新增 ignored export `graphics::tests::project_render::project_scenes::export_runtime_shader_pbr_metallic_smoothness_matrix_png`:临时项目生成 64 个 standard PBR 材质球,列为 metallic 0..1,行为 smoothness 0..1,场景包含 procedural skybox,导出 `docs/tests/runtime/shader/runtime_shader_pbr_metallic_smoothness_matrix_skybox_20260704.png`。 | `cargo test -p zircon_runtime --lib graphics::tests::project_render::project_scenes::export_runtime_shader_pbr_metallic_smoothness_matrix_png --locked --jobs 1 --target-dir E:\cargo-targets\zircon-env-m4-pbr-matrix-0704 --message-format short --color never -- --ignored --exact --nocapture --test-threads=1` 通过 1/1；PNG 1280x960,109556 bytes,SHA256 `E883A3BDF657025EAD16A7F39B1F8BE5D7FFCDA1FDEF0243A8636A05C217030D`;同名 PNG 在 repo `target`、`E:\cargo-targets`、`F:\cargo-targets`、`D:\cargo-targets` 扫描 0。 | 需要后续补 RenderDoc/product capture、更宽 product/perf sweep 与真正 cubemap IBL 资产链。 |
| 2026-07-04 | EL-M1 / 真实 HDRI sampled environment reflection | 已验证: Poly Haven `lakes` HDRI 驱动天空与 PBR 反射矩阵 | 下载 Poly Haven `lakes` 1K HDR 到 `docs/tests/runtime/shader/assets/polyhaven_lakes_1k.hdr`;`SkyboxMode::SampledEquirectangular` 与 `SampledEquirectangularEnvironment` 通过 `EnvironmentExtract`/`SceneUniform` 上传 16x8 equirectangular sample table,skybox 与 `zr_environment_pbr_indirect(...)` 同步采样真实 HDR-derived 环境；导出 `docs/tests/runtime/shader/runtime_shader_pbr_real_hdri_lakes_reflection_20260704.png`。 | HDR API 记录 `size=1464859`,MD5 `b615491d315a3d4e23bb09c2c96c9e03`;本地 HDR SHA256 `FAF3ECE79216E568A29F0D8FC176A795C66EB9C312C3CF3EE18D9AC04A71DECB`;`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-real-hdri-reflection-0704 --message-format short --color never` 通过；direct binary `export_runtime_shader_pbr_real_hdri_reflection_png` 通过 1/1；PNG 1280x960,132232 bytes,SHA256 `958E3B200EC56BCA16BF9596B1F05D872179F51CEB9A64925E10FC2D41792DEE`;同名 PNG 在 repo `target` 与 E/F/D cargo-target 根扫描 0。 | 该行只关闭真实 HDRI sampled environment 验证；仍不是 Plan 13 cubemap 资产导入、GPU `texture_cube` 绑定、GGX prefilter mip 链、reflection probe capture/blending 或 SH irradiance bake 完成。 |
| 2026-06-23 | Render index 当前状态总览拆分 | EL-M1~M3 部分完成,EL-M4 未启动 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 11 Environment Lighting 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：analytic fog/ambient modes、正式 skybox/IBL、probe capture/bake；验收缺口：需要 cubemap/IBL 资产链、probe blending/parallax、fog/ambient product tests |
| 2026-06-15 | EL-M1 skybox and IBL | 部分完成: preview sky 存在,正式 skybox/IBL 未落地 | 当前有 preview-sky executor 和环境色基础,但无 skybox 材质域、IBL cubemap 预滤波链与正式资产格式。 | 本文件 `现状与差距` 明确只有 preview-sky executor;计划 13 纹理 cubemap 资产也尚未完成。 | 实施 skybox material、cubemap import、diffuse/specular IBL prefilter 和 shader binding。 |
| 2026-06-15 | EL-M2 reflection probes | 部分完成: descriptor/占位链路存在,捕获/烘焙未完成 | reflection probe feature/合成入口已有占位;计划 06 已把反射探针投屏 helper 改走 unjittered matrix pair,但探针捕获、烘焙数据和采样 ABI 仍缺。 | 计划 06 TP-M2-S2 状态表记录 reflection probe projection helper 审计完成。 | 建立 probe capture camera、cubemap storage、parallax correction 与 blending。 |
| 2026-06-15 | EL-M3 lightmap and light probe consumption | 部分完成: 单一 SH grid 设计面存在,完整 lightmap/probe 消费未完成 | 计划 18 现状记录当前只有全局单个均匀 SH 网格概念,无局部/可变换/逐像素 irradiance volume;lightmap asset 与 shader sampling 尚未闭环。 | 计划 18 `现状与差距` 将 irradiance volumes 标为后续扩展;本文件未记录完成实现。 | 落地 lightmap UV/channel、LightProbeGridData 上传、instance 采样和编辑器 bake 输入。 |
| 2026-06-15 | EL-M4 analytic fog and ambient modes | 未启动: 与 post fog/advanced fog 尚未贯通 | ambient 仍是基础单色/占位,解析雾与计划 07 screen-space fog、计划 18 froxel fog 尚未形成分层实现。 | 本文件 `现状与差距` 明确无解析雾、ambient 只有单色。 | 先实现 distance/height fog 与 ambient modes,再由计划 18 扩展到 froxel volumetric。 |

### 参考实现精读笔记

**UE `ReflectionEnvironmentShared.ush` — `GetLookupVectorForBoxCapture`**:把反射射线变换到盒本地空间(`RelativeWorldToBox`,盒归一到 ±1),`InvRayDir` 倒数后分别求与三对 min/max 面的交点 `FirstPlaneIntersections`/`SecondPlaneIntersections`,`max` 取每轴远交点、`min` 取三轴最近者得 `Intersection`,交点减捕获位置即校正向量;另用 `ComputeDistanceFromBoxToPoint` 对收缩过渡带(`BoxScales.w`)的盒求距,`DistanceAlpha = 1 - smoothstep(0, 0.7*transition, dist)` 做影响淡出。Zircon 对应:`zr_box_project` 直接采用该 AABB 求交式(本地空间用四元数逆旋转代替矩阵,省 64B/probe);淡出权重不用 smoothstep,改用 URP 的边距/blend_distance 线性式(更便宜,且与 sphere 形状权重公式统一)。

**UE `ReflectionEnvironmentShared.ush` — `ComputeReflectionCaptureMipFromRoughness`**:`LevelFrom1x1 = ROUGHEST_MIP - MIP_SCALE * log2(max(Roughness, 0.001))`,`mip = CubemapMaxMip - 1 - LevelFrom1x1`;注释明确该映射使"某一 mip 永远对应同一 roughness,与 mip 总数无关"。Zircon 对应:常数取 1.0/1.2 原样采用,作为采样与烘焙的共同真理;取舍:不用 bevy 风格的线性 `mip = r * (N-1)`,因为换分辨率重烘后旧材质观感会漂移。

**UE `ReflectionEnvironmentComposite.ush` — `CompositeReflectionCapturesAndSkylightTWS`**:按排序后的捕获列表累加权重直到饱和,盒/球分别走 `GetLookupVectorForBoxCapture`/`GetLookupVectorForSphereCapture`,剩余 alpha 给天空光。Zircon 对应:同思路但截断在 2 探针(URP 量级),省去 UE 的逐 tile 捕获列表;`misc.w` 层掩码过滤对齐其 capture 裁剪职责。

**UE `ReflectionEnvironmentCapture.cpp`**:`FCubeDownsamplePS` 逐 mip 降采样 → `FCubeFilterPS`(GGX 滤波)逐 mip 执行;`FilterReflectionEnvironment` 末尾调 `ComputeDiffuseIrradiance` 输出 `FSHVectorRGB3`(SH9);mip 数 `FMath::CeilLogTwo(CaptureSize) + 1`。Zircon 对应:同"先降采样后滤波"的两段式,但用 compute(经 `ComputePassDescriptor`)替代像素 pass,SH 投影同样在预滤波后一次性产出;取舍:不做 UE 的实时捕获(`ReflectionEnvironmentRealTimeCapture.cpp`)路径,V1 只有手动烘焙。

**UE `TextureCompressorModule.cpp` — `GenerateBaseCubeMipFromLongitudeLatitude2D` / `GenerateAngularFilteredMips`**:long-lat HDRI 先按 face texel center 方向重投影为 source cubemap mip0;普通 2D mip 由 `GenerateMipChain` 的 separable filter 负责;source cubemap 低 mip 由 `GenerateAngularFilteredMips` 先建平均链、再按输出 mip cone angle 跨六面积分。Zircon 对应:新增 `env.source_cubemap_mipmap` owner 设计,用它生成 source mip pyramid 和 FIS source LOD;不把 angular source mip 当成 specular PMREM,PMREM 仍由 `ibl_prefilter.wgsl` 的 GGX FIS/cosine branch 生成。

**URP `ReflectionProbeManager.cs` — `UpdateGpuData`**:CPU 侧维护 `m_BoxMin`/`m_BoxMax` 两个 `Vector4[]`,`boxMax.w = probe.blendDistance`、`boxMin.w = probe.importance`,经 `SetGlobalVectorArray` 上传;探针 cubemap 打进一张 2D atlas(空间不足时增长并告警)。Zircon 对应:字段打包思路一致(`position_blend.w`/`box_min.w`);存储取舍:用 `texture_cube_array` + 槽位分配器替代 2D atlas——wgpu 有原生 cube array 采样,免去 atlas 的 octahedral/面排布与接缝处理,代价是所有探针同分辨率(V1 接受,128³ 固定)。

**URP `GlobalIllumination.hlsl` — `CalculateProbeWeight` / `CalculateIrradianceFromReflectionProbes`**:权重 = `saturate(min_axis(min(p - boxMin, boxMax - p) / blendDistance))`;混合循环在 `totalWeight < 0.99` 内累加并 `weight = min(weight, 1 - totalWeight)`,盒投影走 `BoxProjectedCubemapDirection`。Zircon 对应:权重公式与 `1-total` 截断原样采用,循环截断改为显式 top-2(确定性强、便于 CPU 对拍),skybox fallback 吃剩余权重。

**URP `ShaderVariablesFunctions.hlsl` — `ComputeFogFactorZ0ToFar` / `ComputeFogIntensity` / `MixFogColor`** 与 `UnityInput.hlsl` 的 `unity_FogParams`:linear 把 `(end-z)/(end-start)` 预打包为 `z*params.z + params.w`;exp/exp2 在顶点端只算 `density*z`,像素端 `saturate(exp2(-x))` / `saturate(exp2(-x*x))`(exp2 域避免 `exp`);`MixFogColor = lerp(fogColor, fragColor, fogFactor)`。Zircon 对应:`ZrFogParams.params` 打包与 exp2 域计算原样采用;取舍:不拆顶点/像素两段(`zr_fog_factor` 全在像素端,现代 GPU 上差异可忽略且 deferred 合成本就无顶点插值),height fog 为 URP 所无、按 UE 高度雾思路以解析指数衰减乘项补充。

**URP `DrawSkyboxPass.cs`**:skybox 作为独立 `ScriptableRenderPass` 在 opaque 之后以 `RendererList` 提交(`Render` 接收 colorTarget + depthTarget 做深度测试)。Zircon 对应:skybox executor 同位插入(opaque 后 transparent 前),深度策略(far + LessEqual + no write)在 pipeline 状态固化,graph 声明 `SCENE_DEPTH` 只读。

## 风险与回退

- 与 HGI 的职责边界:本计划是烘焙/静态基线,HGI 开启时其 GI 输出叠加/替换烘焙间接光的策略由 HGI 计划 M2 定,这里只保证采样 ABI 兼容两来源。
- 预滤波成本:导入期/捕获期执行,运行时零成本;编辑器内迭代时降采样快速档。
- tetrahedral probe 插值复杂:V1 均匀网格,接口不锁死插值结构。
