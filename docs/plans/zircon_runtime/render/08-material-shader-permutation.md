---
related_code:
  - zircon_runtime/src/graphics/shader/mod.rs
  - zircon_runtime/src/graphics/shader/template/mod.rs
  - zircon_runtime/src/graphics/shader/template/assemble.rs
  - zircon_runtime/src/graphics/shader/template/module_registry.rs
  - zircon_runtime/src/graphics/shader/template/material_surface.rs
  - zircon_runtime/src/graphics/shader/template/pass_specialization.rs
  - zircon_runtime/src/graphics/shader/template/taa_reactive_mask.rs
  - zircon_runtime/src/graphics/shader/template/validation.rs
  - zircon_runtime/src/graphics/shader/template/tests.rs
  - zircon_runtime/src/graphics/shader/wgsl/zr_surface_types.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_scene_runtime.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_geometry_static.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_geometry_skinned.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_geometry_morphed.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_geometry_skinned_morphed.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_forward.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_gbuffer.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_depth.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_depth_alpha.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_shadow.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_shadow_alpha.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_velocity.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_velocity_alpha.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_template_taa_reactive_mask.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_shading_standard_pbr.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_gbuffer_encode_standard_pbr.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_shade_deferred_standard_pbr.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_shade_deferred_blinn_phong.wgsl
  - zircon_runtime/src/graphics/shader/wgsl/zr_shade_deferred_unlit.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/shaders/zr_gpu_scene.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/lighting_pipeline/shader_source.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/lighting_pipeline/tests.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/shaders/deferred_lighting.wgsl
  - zircon_runtime/src/graphics/tests/pipeline_compile/dynamic_resolution.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_mesh/gpu_mesh_vertex.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_mesh/gpu_mesh_vertex_layout.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/prewarm_pipeline_validation.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_depth_prepass_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_gbuffer_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_shadow_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_taa_reactive_mask_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_velocity_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline/create_taa_reactive_mask_mesh_pipeline.rs
  - zircon_runtime/src/core/framework/render/shader/geometry_source.rs
  - zircon_runtime/src/core/framework/render/shader/variant_prewarm.rs
  - zircon_runtime/src/graphics/shader/variant_cache/prewarm.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_shader_geometry_source_descriptor.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_shader_template_assembly.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/args.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/manifest.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/manifest/paths.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/manifest/permutation_registry.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/manifest/resource_registry.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/manifest/revision.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/manifest/tests.rs
  - zircon_runtime/src/bin/zircon_shader_prewarm/run.rs
  - zircon_runtime/src/plugin/package_manifest/plugin_package_manifest.rs
  - zircon_runtime/src/plugin/package_manifest/constructors.rs
  - zircon_runtime/src/plugin/package_manifest/plugin_shader_permutation_manifest.rs
  - zircon_runtime/src/plugin/extension_registry/runtime_extension_registry.rs
  - zircon_runtime/src/plugin/extension_registry/ownership.rs
  - zircon_runtime/src/plugin/extension_registry/register/metadata.rs
  - zircon_runtime/src/plugin/extension_registry/access/metadata.rs
  - zircon_runtime/src/plugin/extension_registry_error.rs
  - zircon_runtime/src/plugin/runtime_plugin/registration_report/package_contributions/manifest_metadata.rs
  - zircon_runtime/src/plugin/runtime_plugin/runtime_plugin_catalog/contributions/extension.rs
  - zircon_runtime/src/graphics/material/shading_models/registry.rs
  - zircon_runtime/src/graphics/material/shading_models/builtins.rs
  - zircon_runtime/src/builtin/runtime_modules/assembly/extension_inputs.rs
  - zircon_runtime/src/builtin/runtime_modules/assembly/registration_inputs.rs
  - zircon_runtime/src/builtin/runtime_modules/assembly/target_modules.rs
  - zircon_runtime/src/builtin/runtime_modules/core_modules.rs
  - zircon_runtime/src/graphics/runtime_builtin_graphics/mod.rs
  - zircon_runtime/src/graphics/runtime_builtin_graphics/host/module_host/module_registration/module_descriptor.rs
  - zircon_runtime/src/graphics/runtime_builtin_graphics/host/module_host/create/create_render_framework.rs
  - zircon_runtime/src/graphics/runtime/render_framework/wgpu_render_framework_construction/construct.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_construct/construct.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_construct/new_with_icon_source.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_construction.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_shading_models.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_accessors/material_capture.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source/tests/runtime_shading_model_sources.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_gbuffer_pipeline.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/deferred_scene_resources/construct.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/deferred_scene_resources/record_gbuffer_geometry.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/deferred/lighting_pipeline/create.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu/deferred.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/material_custom_shading_model_runtime.rs
  - zircon_plugins/virtual_geometry/runtime/src/plugin.rs
  - zircon_plugins/virtual_geometry/plugin.toml
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_manifest.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_registry_revision.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_registry_auto_export.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_permutation_registry.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_permutation_registry_auto_export.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_plugin_shading_model_descriptor.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_plugin_geometry_source_descriptor.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_plugin_permutation_registry_auto_export.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_cache_artifact_contract.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_resource_registry_report_correlation.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_project_plugin_registry_runtime_staged_cache_hit.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/mesh_pipeline_variant_cache_owner.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/shader_prewarm_report_dimension_contract.rs
  - tools/zircon_build_shader_prewarm.py
  - tools/zircon_build_shader_prewarm_cache_artifacts.py
  - tools/zircon_build.py
  - tools/tests/test_zircon_build_shader_prewarm.py
  - tools/tests/test_zircon_build_shader_prewarm_dimension_contract.py
  - tools/tests/test_zircon_build_shader_prewarm_cache_contract.py
  - tools/tests/test_zircon_build_plugin_carriers.py
  - zircon_runtime/src/core/framework/render/shader/definition_value.rs
  - zircon_runtime/src/core/framework/render/shader/pipeline_layout.rs
  - zircon_runtime/src/core/framework/render/material/standard_material.rs
  - zircon_runtime/src/core/framework/render/material/management.rs
  - zircon_runtime/src/core/framework/render/material/management/tests.rs
  - zircon_runtime/src/core/framework/render/material/management/tests/record_views.rs
  - zircon_runtime/src/core/framework/render/material/management/tests/query_execution.rs
  - zircon_runtime/src/graphics/scene/render_product_material_property_tests.rs
  - zircon_runtime/src/graphics/scene/render_product_material_property_tests/uniform_debug_counts.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_material_product_debug_counts_tests.rs
  - zircon_runtime/src/core/framework/render/backend_types.rs
  - zircon_runtime/src/core/runtime/diagnostics/render_stats_store/product/mesh_queue.rs
  - zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/update_stats/base_stats.rs
  - zircon_runtime/src/graphics/tests/project_render.rs
  - zircon_runtime/src/graphics/tests/project_render/project_scenes.rs
  - zircon_runtime/src/graphics/tests/project_render/render_quality.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_project_scene_products_tests.rs
  - zircon_runtime/src/graphics/scene/render_product_streamer_tests/material_runtime.rs
  - zircon_runtime/src/graphics/scene/render_product_streamer_tests/material_runtime/pbr_projection.rs
  - zircon_runtime/src/graphics/scene/resources/runtime/material_runtime.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_ensure_material.rs
  - zircon_runtime/src/graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/material_runtime_pbr_projection_tests.rs
  - zircon_runtime/src/graphics/material/mod.rs
  - zircon_runtime/src/graphics/material/shading_models/mod.rs
  - zircon_runtime/src/graphics/material/shading_models/include_sources.rs
  - zircon_runtime/src/graphics/tests/render_product_mesh_cache.rs
  - zircon_runtime/src/graphics/tests/render_product_mesh_cache/morph.rs
  - zircon_runtime/src/graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/mod.rs
  - zircon_runtime/src/graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/fixture.rs
  - zircon_runtime/src/graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/product_png.rs
  - zircon_runtime/src/graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/custom_second_launch.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/mesh_pipeline_cache.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/skinning.rs
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_ensure_shader_source.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_material_management_tests.rs
  - dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/VertexFactory.h
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Public/MaterialShader.h
  - dev/UnrealEngine/Engine/Source/Runtime/Renderer/Public/MeshMaterialShader.h
  - dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/GlobalShader.h
  - dev/Graphics/Packages/com.unity.shadergraph
  - dev/bevy/crates/bevy_pbr/src/render/mesh.rs
  - dev/bevy/crates/bevy_mesh/src/lib.rs
  - dev/bevy/crates/bevy_material/src/specialize.rs
  - dev/bevy/crates/bevy_shader/src/shader.rs
  - dev/bevy/crates/bevy_shader/src/shader_cache.rs
  - dev/bevy/crates/bevy_pbr/src/render/skin.rs
  - dev/bevy/crates/bevy_pbr/src/render/morph.rs
  - dev/Fyrox/fyrox-material/src/shader/mod.rs
plan_sources:
  - .codex/plans/Rendering 插件选项补齐计划.md
  - .codex/plans/ZirconEngine 资产、Texture、模型、ZShaderZMaterialZMesh 缺口补齐计划.md
---

# 计划 08:材质、光照模型与 shader permutation 管理

## 目标

1. 建立 VertexFactory 等价物 `GeometrySource`:static / skinned / morph / instanced / VG 等几何来源与材质着色正交组合,GPU skinning/morph 对任意材质生效(不再只有 fallback shader)。
2. 建立光照模型(shading model)注册体系:`Unlit` / `BlinnPhong` / `StandardPbr` / 自定义模型,材质声明模型,deferred G-buffer 编码与 forward 着色按模型分发。
3. permutation 管理:(材质 shader, GeometrySource, pass 类型, feature 开关) → 变体键 → 编译缓存(内存 + 磁盘),离线预热工具,缺失变体诊断。
4. `mesh_pipeline_cache` 重构在统一变体键之上,与 shader graph 插件编译路径收敛。

## 现状与差距

- skinning 由 `fallback_mesh_shader_source` 拼接进 fallback WGSL,自定义材质(zshader/shader graph)无法获得 GPU 形变;morph 仅 CPU。
- shader 变体键(`definition_value.rs`)存在但维度不全(无几何源、pass 维度不显式);无磁盘缓存与预热,首次遇到变体即时编译造成卡顿。
- 光照模型单一:standard material 走固定 PBR 路径,无 unlit/blinn-phong/自定义模型位;deferred G-buffer 编码未按模型抽象。
- SRP bind group ABI(group 0 model / 1 material / 2 view)中 skinning 绑定硬编码在 mesh shader,几何源差异没有抽象层。

## 参考代码

| 文件 | 应重点阅读 |
|------|-----------|
| `dev/UnrealEngine/.../RenderCore/Public/VertexFactory.h` | 顶点工厂抽象:几何源声明顶点流/位置获取函数,与材质 shader 正交;permutation 维度如何进 shader map 键 |
| `dev/UnrealEngine/.../Renderer/Public/MaterialShader.h` + `MeshMaterialShader.h` | 材质 shader map:(material, vertex factory, shader type) 三维组合、permutation domain、lazy compile + DDC 缓存的层次 |
| `dev/UnrealEngine/.../RenderCore/Public/GlobalShader.h` | 非材质全局 shader(后处理/compute)的独立管理 —— 对应本引擎 post/compute WGSL 不进材质变体空间 |
| `dev/Graphics/Packages/com.unity.shadergraph` | shader graph → 模板代码生成:graph 输出函数 + 管线模板拼接(本引擎 zshader/shader_graph 插件的生成式参考);Unity 的 multi_compile/shader_feature 即变体开关先例 |

次参考:`dev/bevy/crates/bevy_pbr`(`MeshPipelineKey` 位打包变体键的 Rust 表达;`StandardMaterial` 的 shader defs 注入)。

**Rust/wgpu 落地参照(防凭空实现)**:

| 文件 | 对应本计划机制 | 应重点阅读 |
|------|---------------|-----------|
| `dev/bevy/crates/bevy_pbr/src/render/mesh.rs` | `ShaderVariantKey` 位打包与 pipeline specialization(重点) | `MeshPipelineKey: u64` bitflags 全集与 `MeshPipeline::specialize`(键 → shader defs → pipeline descriptor 的完整链);`ViewKeyCache` 的 per-view 键缓存 |
| `dev/bevy/crates/bevy_mesh/src/lib.rs` | 变体键维度分段 | `BaseMeshPipelineKey`:几何属性位段独立定义、高位保留给上层键合成(与 `packed_dims` 分段思路同构) |
| `dev/bevy/crates/bevy_material/src/specialize.rs` | 材质维度进变体键 | `ErasedMaterialPipelineKey` + specializer 函数指针表:材质类型擦除后统一走 `SpecializedMeshPipelines::specialize` |
| `dev/bevy/crates/bevy_shader/src/shader.rs` | defines header 注入 | `Shader.shader_defs`/`ShaderDefVal`(Bool/Int/UInt 值型 define,等价本计划 `RenderShaderDefinitionValue`) |
| `dev/bevy/crates/bevy_shader/src/shader_cache.rs` | 模板拼接器 + 变体缓存 | `naga_oil::compose::Composer` 的 include 组合与去重、`global_shader_defs`(平台 capability → define,即 `platform_token` 语义)、编译产物缓存键 |
| `dev/bevy/crates/bevy_pbr/src/render/skin.rs` | `SkinnedMesh` 几何源(storage palette) | `SkinUniforms::current_buffer/prev_buffer`:palette 升 storage buffer 与 prev palette 双缓冲(配 `skinning.wgsl` 的 uniform/storage 双形态) |
| `dev/bevy/crates/bevy_pbr/src/render/morph.rs` | `MorphedMesh` 几何源 | morph 权重双帧映射(`prev` 表)与 GPU 权重 buffer 布局(配 `morph.wgsl` 的 `prev_weight_at`) |
| `dev/Fyrox/fyrox-material/src/shader/mod.rs` | zshader 资产形态与 pass 集合 | `ShaderDefinition`:RON 数据驱动 shader(passes/properties/draw_parameters/disabled_passes),封闭 pass 枚举先例 |

`GeometrySource` × 材质 surface 函数的模板拼接(改名注入 `zr_material_surface`、pass 特化裁剪)无完整 Rust 同类参照(bevy 用 naga_oil import 组合而非 surface 函数模板),实现时以 UE `VertexFactory.h`/`MeshMaterialShader.h` 为唯一样板,按 index §8 第 8 条配对拍测试先行;磁盘变体缓存 + 离线预热同样无 Rust 同类参照(bevy 仅内存缓存),以 UE DDC 思路为样板。

## 目标架构

归属:`GeometrySource` 与变体键契约进 `core/framework/render/shader/`;WGSL 组装与缓存在 `graphics/shader/` 与 `graphics/scene/scene_renderer/mesh/`;shading model 注册在 `graphics/material/`。

核心设计:

- `GeometrySourceDescriptor`:声明顶点输入布局、形变阶段代码片段(WGSL include:`fetch_position/fetch_normal/...`)、所需绑定(skinning palette storage、morph 权重、instance index)。内建实现:`StaticMesh`、`SkinnedMesh`(palette 升 storage buffer,解除 256 uniform 限制)、`MorphedMesh`(GPU 权重混合)、组合型 `SkinnedMorphed`;VG 几何源由 virtual_geometry 插件注册。
- 材质 shader 模板:材质(手写 zshader 或 shader graph 产物)只提供 surface 函数(输入插值器,输出 `SurfaceOutput`);管线模板按 (GeometrySource, pass, shading model) 拼接最终 WGSL —— 自定义材质自动获得 skinning/morph/instancing。
- `ShadingModelDescriptor`:注册 forward 着色函数与 deferred G-buffer 编/解码函数;G-buffer 写 shading model id;内建 `Unlit`/`BlinnPhong`/`StandardPbr`,插件可注册自定义模型(描述符含所需 G-buffer 通道,超出当前布局时报诊断而非静默)。
- `ShaderVariantKey` 定稿:位打包 (material id+revision, geometry source, pass type, shading model, feature flags, quality);`mesh_pipeline_cache` 改为以此为键。
- `ShaderVariantCache`:内存 LRU + 磁盘缓存(键哈希 → 编译产物/naga 验证结果);`zircon_build.py` 增加预热步骤(枚举资产引用的变体离线编译);运行时 miss 记录进诊断(缺失变体报告)。

## 里程碑

### MS-M1 GeometrySource 与 WGSL 组装管线

实施切片:
1. `GeometrySourceDescriptor` 契约与四个内建实现;模板拼接器(surface 函数 + 几何源 include + pass 模板)。
2. 内建 standard material 切到模板路径;删除 fallback 拼接中被取代的 skinning 段。

测试阶段:
- `cargo check -p zircon_runtime --lib --locked`;`cargo test -p zircon_runtime shader --locked`(组装产物 naga 验证)+ `render_product` 回归
- 验收证据:同一材质在 static/skinned 两种几何源下产出不同变体且渲染正确。

### MS-M2 GPU skinning/morph 全材质化

实施切片:
1. palette 升 storage buffer;prev palette(供计划 06)同步;morph 权重 GPU 混合。
2. 自定义材质(zshader)走模板获得形变;CPU skinning 收缩为能力回落档。

测试阶段:
- `cargo test -p zircon_runtime skinning --locked` 与动画场景 `render_product`
- 验收证据:自定义材质角色 GPU 蒙皮正确(与 CPU 路径产物对拍);palette 数量超 256 骨骼用例通过。

### MS-M3 shading model 注册体系

实施切片:
1. `ShadingModelDescriptor` 与内建三模型;G-buffer 编码加 model id;deferred lighting 按 id 分发。
2. 材质资产增加 shading model 字段;blinn-phong/unlit 内建材质模板。

测试阶段:
- `cargo test -p zircon_runtime material --locked`(三模型 forward/deferred 产物对拍一致性)
- 验收证据:同场景三模型混用,deferred 与 forward+ 产物一致。

### MS-M4 变体缓存与预热

实施切片:
1. `ShaderVariantKey` 定稿与 `mesh_pipeline_cache` 重构;磁盘缓存。
2. `tools/zircon_build.py` 预热步骤;缺失变体诊断报告。

测试阶段:
- `cargo test -p zircon_runtime shader --locked`(键稳定性:同输入跨进程同键;缓存命中)
- 验收证据:二次启动同场景零运行时编译(诊断计数);预热产物被运行时命中。

## 工程落地细化

本章是本计划的实施权威(见 index.md §8 第 7 条)。bind group 槽位、GPU 数据布局、`zr_` include、测试命名等全局约定直接引用 index.md §8,不在此重定义;facade 固定 `zircon_runtime::core::framework::render`,契约层不出现 `wgpu` 类型,全部硬切换,渲染侧只消费 `RenderFrameExtract`。

### 模块与文件落点

新增(契约归 `core/framework/render`,实现归 `graphics`,不新增 crate):

| 落点 | 内容 |
|------|------|
| `zircon_runtime/src/core/framework/render/shader/geometry_source.rs` | `GeometrySourceId`、`GeometrySourceDescriptor` 契约(无 wgpu) |
| `zircon_runtime/src/core/framework/render/material/shading_model.rs` | `ShadingModelId`、`ShadingModelDescriptor`、`GBufferChannelMask` 契约 |
| `zircon_runtime/src/graphics/shader/geometry_sources/{mod,static_mesh,skinned_mesh,morphed_mesh,skinned_morphed}.rs` | 四个内建几何源描述符构造 + 注册表(插件注册入口,VG 经 virtual_geometry 插件走此口) |
| `zircon_runtime/src/graphics/shader/template/{mod,assemble,module_registry,pass_specialization}.rs` | 模板拼接器:include 注入去重、surface 函数改名、pass 特化裁剪、`ZrVertexInput` 生成 |
| `zircon_runtime/src/graphics/shader/variant_cache/{mod,resolve,disk,prewarm}.rs` | `ShaderVariantCache`:内存 interning + LRU、磁盘缓存、预热清单、缺失变体诊断 |
| `zircon_runtime/src/graphics/material/shading_models/{mod,registry,unlit,blinn_phong,standard_pbr}.rs` | shading model 注册表与三内建模型(含 G-buffer model id 编码) |
| `zircon_runtime/src/graphics/shader/wgsl/zr_geometry_{static,skinned,morphed,skinned_morphed}.wgsl` | 几何源 include(`fetch_*` 族),经 `include_str!` 内嵌 |
| `zircon_runtime/src/graphics/shader/wgsl/{zr_surface_types,zr_gbuffer_encode}.wgsl` | `ZrSurfaceOutput`/`ZrShadingContext` struct 与 G-buffer 编码骨架 |
| `zircon_runtime/src/graphics/shader/wgsl/zr_shading_{unlit,blinn_phong,standard_pbr}.wgsl` | shading include(`shade_forward`/`encode_gbuffer`/`shade_deferred`) |
| `zircon_runtime/src/graphics/shader/wgsl/zr_template_{forward,gbuffer,depth,shadow,velocity}.wgsl` | entry point 模板保留 `zr_vs_main`/`zr_fs_main` 调试入口,并通过 `zr_vs_main_impl`/`zr_fs_main_impl` 暴露 runtime `vs_main`/`fs_main` 别名,含拼接占位符 |

修改:

| 落点 | 改动 |
|------|------|
| `zircon_runtime/src/graphics/shader/mod.rs` | 删除旧 `ShaderVariantKey { shader_id, domain, keywords }`;改为模块声明 + 再导出(保持瘦) |
| `zircon_runtime/src/core/framework/render/shader/{mod.rs,variant_key.rs}` | `ShaderVariantKey` 定稿(下节);既有 `RenderShaderVariantKey { entry_point, stage, defines }` 降级为单模块编译请求载体,由新键展开生成 |
| `zircon_runtime/src/core/framework/render/material/{mod.rs,lighting_model.rs,standard_material.rs}` | `RenderMaterialLightingModel::as_token()` → `ShadingModelId` 解析;`StandardMaterialDescriptor.lighting_model` 接入变体维度 |
| `zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/{mesh_pipeline_cache,ensure_pipeline,ensure_motion_vector_pipeline,new}.rs` | 以 `pipeline_variant_id` 为键重构;删除 `HashMap<String, ShaderModule>` 与 `PipelineKey` 双轨;motion vector 管线并入 `ShaderPassType::Velocity` 变体 |
| `zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/skinning.rs` | CPU `skin_model_primitive` 默认路径删除,收缩为能力回落档;palette 直传 storage 上传路径 |
| `zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_ensure_shader_source.rs` | zshader 校验从"整段 WGSL"改为"surface 函数 + 声明清单";fallback 材质走同一模板路径 |
| `tools/zircon_build.py` | 新增 `--prewarm-shaders` 步骤(见下) |
| `zircon_plugins`(shader_graph 生成器、virtual_geometry 注册点) | MS-M1 同步改产 surface 函数;VG 几何源注册(切片内硬切换) |

### 核心类型与接口

契约层(`core/framework/render`,可序列化、无 wgpu):

```rust
// shader/geometry_source.rs
#[derive(Clone, Copy, PartialEq, Eq, Hash)]
pub struct GeometrySourceId(pub u8); // 0=StaticMesh 1=SkinnedMesh 2=MorphedMesh 3=SkinnedMorphed; 4..=15 插件注册

pub struct GeometrySourceDescriptor {
    pub id: GeometrySourceId,
    pub token: String,                      // 稳定名,进磁盘键;如 "static_mesh"
    pub wgsl_include: String,               // 提供 fetch_* 函数族的 include 全文
    pub vertex_attributes: Vec<RenderShaderVertexAttribute>, // 生成 ZrVertexInput
    pub object_bindings: Vec<RenderShaderBindingDescriptor>, // 仅允许 group3 槽内(binding 1..=4)
    pub supports_prev_position: bool,       // false 时 velocity pass 回退 instance prev transform
    pub defines: Vec<RenderShaderDefinitionValue>, // 注入拼接 header,等价 UE ModifyCompilationEnvironment
}

// material/shading_model.rs
#[derive(Clone, Copy, PartialEq, Eq, Hash)]
pub struct ShadingModelId(pub u8);          // G-buffer 8bit 编码,上限 256;0=Unlit 1=BlinnPhong 2=StandardPbr;插件自 16 起
pub struct ShadingModelDescriptor {
    pub id: ShadingModelId,
    pub token: String,                      // 对齐 RenderMaterialLightingModel::as_token()
    pub forward_include: String,            // 提供 shade_forward
    pub gbuffer_encode_include: String,     // 提供 encode_gbuffer
    pub deferred_include: String,           // 提供 shade_deferred(解码后着色)
    pub required_channels: GBufferChannelMask, // 超出当前 G-buffer 布局 → 注册时诊断拒绝,非静默
}

// shader/variant_key.rs(定稿)
pub enum ShaderPassType { Forward, GBuffer, DepthPrepass, Shadow, Velocity } // 4bit,余位保留
pub struct ShaderFeatureBits(pub u32);
// bit0 ALPHA_TEST | bit1 RECEIVE_SHADOWS | bit2 DOUBLE_SIDED | bit3 LOD_DITHER_CROSSFADE(计划 10 占用)
// bit4 INSTANCED_PREV_TRANSFORM(计划 06) | bit5..=23 保留 | bit24..=31 插件段
pub struct ShaderVariantKey {
    pub material_shader: ResourceId,        // zshader/graph 产物资源
    pub material_revision: u64,             // resource_revision,热改材质即新键
    pub geometry_source: GeometrySourceId,
    pub shading_model: ShadingModelId,
    pub pass_type: ShaderPassType,
    pub features: ShaderFeatureBits,
    pub quality: ShaderQualityTier,         // Low/Medium/High/Ultra(2bit)
    pub platform_token: String,             // backend + downlevel caps 摘要,只进磁盘哈希
}
impl ShaderVariantKey {
    pub fn packed_dims(&self) -> u64;       // geometry 0..3 | shading 4..11 | pass 12..15 | features 16..47 | quality 48..49;材质维度不打包
    pub fn canonical_string(&self) -> String; // 全字段 + 模板修订号,磁盘哈希输入
}
```

实现层(`graphics/shader/variant_cache`,持 wgpu 对象):

```rust
pub struct ResolvedVariant {
    pub pipeline_variant_id: u32,           // cache interning 分配的稠密索引,写入 MeshDrawCommand(计划 02)
    pub module: wgpu::ShaderModule,
    pub layout: RenderShaderPipelineLayoutDescriptor, // 语义槽声明,重排后 replayer 映射收敛恒等(计划 02)
}
impl ShaderVariantCache {
    pub fn resolve(&mut self, key: &ShaderVariantKey) -> Result<ResolvedVariant, GraphicsError>;
    pub fn miss_report(&self) -> &VariantMissReport; // 帧诊断:本次会话即时编译清单
}
```

归属:`ShaderVariantKey`/两 descriptor 在 framework 契约层;拼接器、cache、注册表实例在 graphics 层并挂在 scene_renderer resources 上,生命周期与 `MeshPipelineCache` 现位置一致。

### GPU 数据布局与 WGSL 约定

bind group 槽位直接引用 index.md §8 第 1 条(group2=material、group3=object/instance),并落到具体 binding:

| group | binding | 资源 | 来源 |
|-------|---------|------|------|
| 0 | 0 | `SceneUniform` | MS-M1 `zr_scene_runtime.wgsl` 引用当前 runtime view/clip uniform |
| 1 | — | light grid / shadow map+sampler / HZB | 计划 05 的 `zr_light_grid.wgsl`/`zr_shadow.wgsl` 槽位;shadow 采样按计划 03 GS-M2 重排落 group1 |
| 2 | 0 | material uniform | 材质 property uniform(既有 `property_uniform.rs` 路径) |
| 2 | 1..N | 材质纹理/采样器对 | 按材质声明顺序 |
| 3 | 0 | `zr_primitive_data`(storage) | 当前 runtime `zr_gpu_scene.wgsl` primitive SOA/AoS 数据 |
| 3 | 1 | `zr_instance_data`(storage) | 当前 runtime `zr_gpu_scene.wgsl` instance transform 数据,供 `zr_world_from_local(instance_index)` |
| 3 | 2 | `zr_light_data`(storage) | 当前 runtime light 数据 |
| 3 | 3 | `zr_skinned_joint_palette`(uniform) | MS-M1 template 复用当前 runtime skinned palette helper;storage palette 升级归 MS-M2 |
| 3 | 4 | `zr_previous_skinned_joint_palette`(uniform) | 供 velocity/previous pose;storage palette 升级归 MS-M2 |
| 3 | 5 | `zr_visible_instance_remap`(storage) | 当前 runtime indirect/visible-instance remap |
| 3 | 6 | `zr_visible_instance_remap_params`(uniform) | 当前 runtime remap/light-count 参数 |
| 3 | 7 | `zr_morph_deltas`(storage, template reserved) | Morphed 源 target 增量;真实 runtime layout/上传归 MS-M2 |
| 3 | 8 | `zr_morph_weights`(storage, template reserved) | Morphed 源权重;真实 runtime layout/上传归 MS-M2 |
| 3 | 11 | `zr_morph_payloads`(storage) | MS-M2 payload header slot;`GpuInstanceData.morph_payload_slot` 指向每 draw 的 delta/weight base、vertex count 和 target count |

几何源 include 契约 —— 每个 include 必须以固定签名提供下列函数(`ZrVertexInput` 由拼接器按 `vertex_attributes` 生成;沿用本计划"目标架构"既定的 `fetch_*` 命名,计划 15 等下游按此消费):

```wgsl
fn fetch_position(v: ZrVertexInput, instance_index: u32) -> vec3<f32>;      // object space,含形变
fn fetch_prev_position(v: ZrVertexInput, instance_index: u32) -> vec3<f32>; // 上一帧形变位置(计划 06 velocity)
fn fetch_normal(v: ZrVertexInput, instance_index: u32) -> vec3<f32>;
fn fetch_tangent(v: ZrVertexInput, instance_index: u32) -> vec4<f32>;
fn fetch_uv0(v: ZrVertexInput) -> vec2<f32>;
fn fetch_uv1(v: ZrVertexInput) -> vec2<f32>;
fn fetch_color(v: ZrVertexInput, instance_index: u32) -> vec4<f32>;
```

- Static:直读顶点属性;`fetch_prev_position` 返回当前位置(prev 差异由 instance prev transform 承担)。
- Skinned:MS-M1 先复用当前 runtime `zr_skinned_joint_matrix(...)` / `zr_previous_skinned_joint_matrix(...)` uniform palette helper,避免与 group3 binding1/2 的 GPUScene instance/light 数据冲突;palette 升 storage buffer 和 per-instance palette offset 归 MS-M2。
- 精确锚点：当前 skinned 模板路径保留 `zr_skinned_joint_matrix(v.joints.x)`，Base mesh source/cache handoff 的消费 owner 为 `graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_pipeline.rs`。
- Morphed:`fetch_position` 内做 GPU 权重混合;SkinnedMorphed 先 morph 后 skin,与 CPU 路径 `to_morphed_model_primitive` → `skin_model_primitive` 顺序一致以便对拍。MS-M2 已落地 bindings 7/8 的 delta/weight storage、binding 11 的 payload header 和 `GpuInstanceData.morph_payload_slot`；production draw selection now routes payload-backed unmorphed sources to `GEOMETRY_SOURCE_ID_MORPHED_MESH` / `GEOMETRY_SOURCE_ID_SKINNED_MORPHED_MESH`, while CPU-baked morph fallbacks remain Static/Skinned to avoid double morph. 2026-06-30 product source-selection observability proves direct active morph weights reach the WGPU GPU-morphed source path through `RenderStats`/DiagnosticStore counters, and the follow-up product readbacks compare direct GPU Morphed and skinned GPU SkinnedMorphed output against CPU-baked reference frames. Previous morph weights for velocity are now code-wired through GPUScene successful-submit rolling plus a previous-weight block in the shared morph weight buffer; focused direct and skinned 0.0 -> 1.0 WGPU product readbacks prove nonzero `scene-velocity` output. RenderDoc capture, broader miss=0 acceptance, and full CI still gate final closure.
- 变体位:几何源本身即 `geometry_source` 维度,不再额外占 feature bit。

shading include 接口(`zr_surface_types.wgsl` 定义 struct,材质 surface 函数产出 `ZrSurfaceOutput`):

```wgsl
struct ZrSurfaceOutput {
    base_color: vec4<f32>, normal_ws: vec3<f32>,
    metallic: f32, roughness: f32, occlusion: f32,
    emissive: vec3<f32>, custom0: vec4<f32>,   // 自定义模型扩展通道
}
struct ZrShadingContext { position_ws: vec3<f32>, view_dir_ws: vec3<f32>, ndc: vec4<f32>, instance_index: u32 }
fn shade_forward(surface: ZrSurfaceOutput, ctx: ZrShadingContext) -> vec3<f32>;   // 消费 zr_light_grid/zr_shadow 查询函数(计划 05)
fn encode_gbuffer(surface: ZrSurfaceOutput, ctx: ZrShadingContext) -> ZrGBufferEncoded; // 写 8bit shading model id
fn shade_deferred(surface: ZrSurfaceOutput, ctx: ZrShadingContext) -> vec3<f32>;
```

拼接顺序(固定):defines header → `zr_scene_runtime.wgsl` → `zr_gpu_scene.wgsl` → `zr_surface_types.wgsl` → 几何源 include → pass 专属 include(Forward:`zr_light_grid`+`zr_shadow`;GBuffer:`zr_gbuffer_encode`)→ shading include → 材质 surface 函数 → entry point 模板。include 按 id 注入一次去重;材质 surface 函数拼接时改名为 `zr_material_surface`,材质源内全局符号若撞 `zr_`/`fetch_`/`shade_` 前缀,naga 解析后诊断拒绝并回落 fallback 材质(报材质 uri)。pass 特化:DepthPrepass/Shadow 只保留 `fetch_position`(ALPHA_TEST 位再保留 `fetch_uv0`+base_color 采样),Velocity 保留 `fetch_position`+`fetch_prev_position` 双投影,均不拼 shading include。

### 帧时序与集成点

入口时序不变(Extract → Prepare → Queue/Sort → Execute):

1. Prepare 早段:`resource_streamer.ensure_shader_source` 产出 surface 函数源 + revision(沿用现 fallback 报告通道)。
2. Prepare 中段(计划 02 的 command build 处,即现 `build_mesh_draws`):每 draw 组装 `ShaderVariantKey`(几何源由 mesh 形变能力判定,shading model 由 `StandardMaterialDescriptor.lighting_model` 解析,pass 由所属 mesh pass 决定,features 由材质/组件旗标合成)→ `ShaderVariantCache::resolve` → `pipeline_variant_id` 写入 `MeshDrawCommand`。命中内存 interning 为 O(1);磁盘命中只做 naga 反序列化校验;全 miss 即时编译并计入 `VariantMissReport`。
3. Queue/Sort:不触碰 —— sort_key 位段归计划 09。
4. Execute:replayer 按语义槽映射绑定;本计划完成 WGSL 槽位重排后映射表收敛为恒等(计划 02 约定)。

硬切换删除项(随对应切片同变更删除,不留双路径):

- `fallback_mesh_shader_source` 中 skinning 拼接段(GS-M2 重排后的 fallback_mesh.wgsl 以模板路径产出)。
- `SkinnedMeshJointPaletteUniform` uniform palette 路径与 256 骨骼上限。
- `build/skinning.rs` 的 CPU 顶点蒙皮默认路径(保留为 storage buffer 能力缺失时的显式回落档,经能力检测进入)。
- `MeshPipelineCache` 的 `shader_modules: HashMap<String, _>` 与 `PipelineKey` 旧键、`motion_vector_mesh_pipelines` 独立表。
- `graphics/shader/mod.rs` 旧 `ShaderVariantKey { keywords }`。
- shader_graph 插件旧"整段 shader 产物"生成路径。

磁盘缓存:根目录默认 `<project>/.zircon-cache/shader_variants/`(`ZR_SHADER_CACHE_DIR` 覆写);布局 `<root>/v<schema_version>/<hash[0..2]>/<hash>.wgsl.zst` + 同名 `.meta`(键 canonical_string、模板修订号、naga/wgpu 版本、创建时间)。`hash = blake3(canonical_string + 全部参与 include 的内容哈希)`,宁可多失效不可错命中(与"风险与回退"口径一致)。并发写:临时文件 + 原子 rename,先到为准;读损坏视为 miss 并删除条目。

prewarm 钩子:`tools/zircon_build.py --prewarm-shaders` 在 stage 完成后,扫描 staged 资产清单中材质引用,生成变体枚举清单(材质 × 适用几何源 × 启用 pass × 默认 quality),调用 runtime 的 headless 入口 `zircon_runtime::dynamic_api::prewarm_shader_variants(manifest, cache_dir)` 离线编译,产物放入 staged payload 的 `ZirconEngine/cache/shader_variants/`,运行时缓存查找链为"运行期目录 → staged 预热目录"。

### 实施切片细化

MS-M1(GeometrySource 与拼接管线;切片期只 `cargo check -p zircon_runtime --lib --locked`):

- 切片 1:新增 `geometry_source.rs` 契约、`graphics/shader/geometry_sources/*` 四内建、`template/*` 拼接器、`zr_geometry_*.wgsl`/`zr_surface_types.wgsl`/entry 模板;`ShaderVariantKey` 结构与内存 interning(供计划 02 的 `pipeline_variant_id`)。完成判据:check 过;拼接产物 static/skinned 双变体均通过 naga 验证(模块内单测)。
- 切片 2:standard material 与 fallback 材质切到模板路径;改 `ensure_pipeline.rs` 消费 `ResolvedVariant`;删除 fallback skinning 拼接段与旧 `ShaderVariantKey`;shader_graph 插件生成器改产 surface 函数。完成判据:check 过;`render_product` 现有场景产物不回归(里程碑测试阶段验证)。

MS-M2(GPU 形变全材质化):

- 切片 1:group3 binding1/2 palette 升 storage(含 prev palette 槽对接 GpuScene)、morph deltas/weights/payload header 上 GPU;`build/skinning.rs` 与 mesh-build morph payload 上传路径改写。完成判据:check 过;>256 骨骼用例与 morph payload slot/indexing 单测就位。
- 切片 2:zshader 自定义材质经模板获得形变;CPU skinning 改能力回落档并加进入日志。完成判据:`render_product` 蒙皮对拍(GPU vs CPU 参考)通过 —— 本切片是"GPU skinning 全材质可用"验收主线。

MS-M3(shading model 注册体系):

- 切片 1:`shading_model.rs` 契约、`shading_models/*` 三内建与注册表、`zr_shading_*.wgsl`、`zr_gbuffer_encode.wgsl` 写 8bit model id、deferred lighting 按 id 分发。完成判据:check 过;id 编解码 roundtrip 单测。
- 切片 2:`lighting_model.rs` token → `ShadingModelId` 解析接入变体键;blinn-phong/unlit 内建材质模板;自定义模型通道溢出诊断。完成判据:三模型 forward/deferred 对拍一致(里程碑测试)。

MS-M4(变体缓存与预热):

- 切片 1:`variant_cache/disk.rs` 磁盘缓存(布局/哈希/并发如上);`mesh_pipeline_cache` 以 `pipeline_variant_id` 重构,motion vector 管线并入 Velocity 变体;`VariantMissReport` 进帧诊断。完成判据:check 过;键稳定性单测(跨进程同键)。
- 切片 2:`variant_cache/prewarm.rs` + `dynamic_api::prewarm_shader_variants` + `zircon_build.py --prewarm-shaders`。完成判据:预热后二次启动同场景 miss 计数为 0。

### 测试与验收清单

单测放各 owner 模块 `#[cfg(test)]`(命名遵循 index.md §8 第 6 条),产物对拍走既有 `render_product_*` 设施:

| 测试函数 | 断言要点 | 位置 |
|---------|---------|------|
| `render_shader_variant_key_packs_dimensions_stably` | `packed_dims`/`canonical_string` 对 golden 值稳定;字段变更必须显式改 golden | `framework/render/shader/variant_key.rs` |
| `render_shader_template_assembles_static_and_skinned_variants` | 同一 surface 函数 × 两几何源产物不同且均过 naga | `graphics/shader/template/assemble.rs` |
| `render_shader_template_dedupes_includes_and_renames_surface` | include 注入恰一次;`zr_material_surface` 改名生效;前缀冲突材质被诊断拒绝 | 同上 |
| `render_shader_geometry_include_provides_fetch_contract` | 四内建源均解析出全部 `fetch_*` 签名(含 `fetch_prev_position`) | `graphics/shader/geometry_sources/mod.rs` |
| `render_shader_variant_cache_hits_disk_after_restart` | 写盘→新建 cache 实例→resolve 零编译;损坏条目按 miss 处理 | `graphics/shader/variant_cache/disk.rs` |
| `render_shader_variant_miss_report_counts_runtime_compiles` | miss 计数与即时编译次数一致 | `graphics/shader/variant_cache/resolve.rs` |
| `render_skinning_storage_palette_exceeds_256_bones` | 300 骨骼 palette 上传与寻址正确 | `scene_renderer/mesh/skinning` 测试模块 |
| `render_material_shading_model_id_roundtrips_gbuffer_encoding` | 0/1/2/自定义 id 编解码 roundtrip;255 上限 | `graphics/material/shading_models/registry.rs` |
| `render_material_custom_model_channel_overflow_reports_diagnostic` | `required_channels` 超布局 → 注册返回诊断而非静默 | 同上 |
| `render_material_lighting_model_token_resolves_shading_id` | `as_token()` 全枚举映射 + 未注册 custom 回落 Pbr 并诊断 | `framework/render/material/lighting_model.rs` |
| `render_product_skinned_custom_material_matches_cpu_reference` | zshader 材质 GPU 蒙皮 vs CPU 参考对拍(验收主线) | render_product 套件 |
| `render_product_three_shading_models_forward_deferred_parity` | 同场景三模型混用,deferred 与 forward+ 产物一致 | render_product 套件 |
| `render_product_morph_gpu_blend_matches_cpu_reference` | GPU morph 混合 vs CPU `to_morphed_model_primitive` 对拍 | render_product 套件 |

里程碑过滤词:`cargo test -p zircon_runtime shader --locked` / `material` / `skinning`;插件接缝按 index.md §7 跑 `zircon_plugins` 受影响包。

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-07-04 | Plan 08 project/plugin registry material-pass product readback PNG | render_plan08_project_plugin_registry_material_passes_product_readback_png_passed_renderdoc_deferred | Added folder-backed ignored export `project_plugin_registry_material_passes_staged_cache/product_png.rs::export_project_plugin_registry_material_passes_product_png` and a test-only fixture entry `submit_registry_material_passes_with_staged_cache_capture(...)`. The export prewarms a single project/plugin registry material-pass manifest, keeps the staged cache read-only at runtime, asserts first-frame and Velocity-frame staged disk hits with compile miss=0, then writes the captured first/Velocity product frames side-by-side to `docs/tests/runtime/render/runtime_render_plan08_project_plugin_registry_material_passes_20260703.png`. The filename keeps the 2026-07-03 slice name, but this row records the 2026-07-04 rerun evidence. | The initial Cargo-wrapper command on `E:\cargo-targets\zircon-plan08-project-plugin-png-0703` exceeded the 904.5s tool window while still compiling/linking and is not counted. The same background build later produced `E:\cargo-targets\zircon-plan08-project-plugin-png-0703\debug\deps\zircon_runtime-0a7825d39d44b0c4.exe`; direct generated-binary execution of `graphics::tests::render_product_mesh_cache::project_plugin_registry_material_passes_staged_cache::product_png::export_project_plugin_registry_material_passes_product_png --ignored --exact --nocapture --test-threads=1` passed 1/1 with 6290 filtered, finished in 6.58s, and generated a 641x240 PNG, 3871 bytes, SHA256 `2FF919F50FDFFBAEB1544CAD9C14B7748FA8234C784175195AF3E550FB6151BB`, with 4794 non-black pixels and 240 magenta separator pixels. RenderDoc MCP still had no running instance for an `.rdc` capture. | Closes the focused product readback PNG evidence for the project/plugin registry material-pass staged-prewarm path. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export beyond the focused fixture, and broader product miss=0 outside the existing filters. |
| 2026-07-03 | Plan 08 morph velocity scene-velocity readback PNG | render_plan08_morph_weight_velocity_product_png_passed_renderdoc_deferred | Added a test-only RG16Float scene-velocity readback byte outlet and ignored exports `export_direct_morph_weight_velocity_product_png` / `export_skinned_morph_weight_velocity_product_png`. The normal direct/skinned morph velocity product tests still assert Morphed/SkinnedMorphed source selection, shader skinning where applicable, previous velocity transform, zero missing velocity transforms, and nonzero `scene-velocity`; the ignored exports write `docs/tests/runtime/render/runtime_render_plan08_direct_morph_weight_velocity_20260703.png` and `docs/tests/runtime/render/runtime_render_plan08_skinned_morph_weight_velocity_20260703.png` through the new `morph/velocity_png.rs` child owner. The lower-level readback report now ignores signed-zero RG16Float pixels, and the PNG helper visualizes sign-masked half-float payload bits so sparse velocity evidence is not collapsed to a black export. | The first Cargo-wrapper attempt on `E:\cargo-targets\zircon-plan08-morph-velocity-png-0703` exceeded the 1213.4s tool window and is not counted. The generated binary `zircon_runtime-0a7825d39d44b0c4.exe` passed the regular direct/skinned product tests 1/1 each with 6207 filtered (3.84s direct, 3.60s skinned), passed `scene_velocity_readback_ignores_signed_zero_half_float_pixels` 1/1 with 6207 filtered, passed the ignored exports 1/1 each with 6207 filtered (3.36s direct, 3.42s skinned), and passed the fully qualified `runtime_15_render_product_mesh_cache_morph_tests_are_child_owners` 1/1 with 6207 filtered and 0.25s. Both PNGs are 128x128, 723 bytes, SHA256 `7B40FA8BA6EA60F7F24F3C5465F3C802B4119E26A8965DACECA08187FF665DE7`, with 508 non-black pixels. | Closes focused product scene-velocity PNG evidence for the existing direct and skinned morph 0.0 -> 1.0 velocity product paths. Still open: RenderDoc/product capture, workspace/full CI, and broader product miss=0/second-launch acceptance outside focused morph velocity filters. |
| 2026-07-03 | Plan 08 VirtualGeometry page/cluster product readback PNG | render_plan08_virtual_geometry_page_cluster_product_readback_png_passed_renderdoc_deferred | Added ignored product export `export_virtual_geometry_page_cluster_product_png` beside `render_product_virtual_geometry_page_cluster_bindings_drive_visible_frame`. It reuses the automatic cooked `ModelAsset` VirtualGeometry path, visible Unlit product capture, `custom:virtual_geometry` page/cluster binding requirements, and public WGPU execution stat assertions, then writes `docs/tests/runtime/render/runtime_render_plan08_virtual_geometry_page_cluster_product_20260703.png`. The normal page/cluster product test remains an in-memory WGPU readback/assertion path and writes no files unless the ignored export is requested. | The first Cargo-wrapper attempt on `E:\cargo-targets\zircon-plan08-vg-png-0703` exceeded the 1209.4s tool window and is not counted. It later produced `E:\cargo-targets\zircon-plan08-vg-png-0703\debug\deps\zircon_runtime-06153fefd6ded19a.exe`; direct generated-binary execution of `graphics::tests::render_product_mesh_cache::virtual_geometry::export_virtual_geometry_page_cluster_product_png --ignored --exact --nocapture --test-threads=1` passed 1/1 with 6204 filtered and 7.97s, writing a 320x240 PNG, 2965 bytes, SHA256 `0322783567544681379085E0C944EF40DD2E6453EE4AE0CB5897F12EBBEBDDE6`. | Closes the focused VirtualGeometry page/cluster product readback PNG evidence while preserving the earlier no-default and default-feature WGPU page/cluster product execution rows. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside focused filters. |
| 2026-07-03 | Plan 08 three shading-model forward/deferred product readback PNG | render_plan08_three_shading_models_forward_deferred_product_readback_png_passed_renderdoc_deferred | Added an ignored product export path `export_three_shading_models_forward_deferred_product_png` beside `render_product_three_shading_models_forward_deferred_parity`. It reuses the existing PBR/Blinn-Phong/Unlit Forward + Deferred WGPU capture path, verifies the same executor and RGBA parity assertions, then writes a side-by-side product readback PNG to `docs/tests/runtime/render/runtime_render_plan08_three_shading_models_forward_deferred_product_20260703.png`. The automatic parity test still stays focused on WGPU readback assertions and does not write files unless the ignored export is requested. | The first Cargo-wrapper export attempt on `E:\cargo-targets\zircon-plan08-parity-png-0703` compiled to MSVC link and failed with `link.exe` exit `0xc0000142`; no test binary or PNG was produced and that attempt is not counted. The warmed rerun with `CARGO_PROFILE_DEV_DEBUG=0` produced `E:\cargo-targets\zircon-plan08-parity-png-0703\debug\deps\zircon_runtime-33095b46939b64fc.exe`; direct generated-binary execution of `graphics::tests::render_product_mesh_cache::shading_model_parity::export_three_shading_models_forward_deferred_product_png --ignored --exact --nocapture --test-threads=1` passed 1/1 with 6212 filtered, finished in 14.04s, and wrote a 641x240 PNG, 3998 bytes, SHA256 `D493C941CBCF418A2C66F84F663881E9DA6B0984B023BAACEC254F094AB483B6`. | Closes the focused non-custom product readback PNG evidence for Plan 08 three shading-model forward/deferred product parity while preserving the earlier default-feature WGPU backfill. Still open: RenderDoc/product capture, workspace/full CI, and broader product coverage outside the focused filters. |
| 2026-07-03 | Project/plugin registry material-pass second-launch default-feature WGPU backfill | render_plan08_project_plugin_registry_material_passes_second_launch_default_features_wgpu_passed_renderdoc_deferred | Re-ran the project/plugin registry material-pass second-launch product contract through default features and fixed the real GBuffer pipeline blocker in the product fixture: standard registry shader assets now use `registry_material_pass_runtime_surface_source`, which is the standard PBR material surface source renamed to the runtime `fn zr_material_surface(` entry, instead of registering a fully assembled Forward pass template as a `Surface` shader. This keeps staged prewarm on the six requested pass templates while runtime launches assemble Surface assets through the normal Forward/GBuffer/DepthPrepass/Shadow/Velocity/TAA reactive paths. | Initial default-feature Cargo-wrapper failed 0/1 with `deferred GBuffer command must resolve a mesh pipeline`. After the fixture fix, `cargo test -p zircon_runtime --lib render_product_project_plugin_registry_material_passes_second_launch_use_staged_prewarm_without_compile_miss --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-product-default-0703 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 6177 filtered, test body 14.96s after an 8m35s build. The no-default regression rerun of the same filter on the same target dir passed 1/1 with 6171 filtered, test body 12.75s after a 13m59s build, and `registry_material_pass_runtime_surface_source_uses_surface_entry_contract` passed 1/1 with 6171 filtered. Output carried repository-existing warnings only. | Closes the default-feature Cargo-wrapper rerun gap for the focused project/plugin registry material-pass second-launch product gate. Still open: RenderDoc/product capture, workspace/full CI, and broader product cases outside this focused filter. The custom shading-model product-group follow-up is closed by `render_plan08_custom_shading_model_product_group_default_features_wgpu_refresh_passed_renderdoc_deferred`. |
| 2026-07-03 | Custom shading-model product group default-feature WGPU refresh | render_plan08_custom_shading_model_product_group_default_features_wgpu_refresh_passed_renderdoc_deferred | Re-ran the current default-feature `custom:toon` product group after the standard surface-source fix and closed the new 6/12 prewarm-validation regression. The lowest shared fix updates `mesh_pipeline_cache/prewarm_pipeline_validation.rs` so the validation material bind-group layout matches the runtime material ABI: group2 binding0 is the material uniform and bindings 1-10 are the standard texture/sampler pairs. The custom product fixture now uses `registry_material_pass_runtime_surface_source()` for runtime `Surface` shader assets and records the `ShaderAsset.shading_model` token from the plugin `ShadingModelDescriptor`, so custom runtime requests stay on `custom:toon` instead of falling back to `standard_pbr`. | `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::prewarm_pipeline_validation::tests::mesh_prewarm_pipeline_validation_creates_all_builtin_pass_pipelines --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-product-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6186 filtered, test body 4.15s. Focused default-feature product reruns on the same target dir passed: `render_product_custom_shading_model_registry_material_passes_use_staged_prewarm_without_compile_miss` 1/1 with 6187 filtered and 5.69s, `render_product_custom_shading_model_deferred_lighting_readback_uses_project_include` 1/1 with 6191 filtered and 4.14s, and `render_product_custom_shading_model_second_launch_uses_staged_prewarm_without_compile_miss` 1/1 with 6195 filtered and 9.65s. Output carried repository-existing warnings only. | Supersedes the previous separate-gate 6/12 note for the current default-feature custom product path. Still open: RenderDoc/product capture, workspace/full CI, and product cases outside the focused custom shading-model filter. |
| 2026-07-03 | VirtualGeometry page/cluster product default-feature WGPU backfill | render_plan08_virtual_geometry_page_cluster_product_default_features_wgpu_passed_renderdoc_deferred | Re-ran the exact `render_product_virtual_geometry_page_cluster_bindings_drive_visible_frame` product path through the default-feature runtime after the no-default page/cluster product execution slice. No production renderer, provider, descriptor, WGSL ABI, or fixture behavior changed in this backfill; the test still exercises automatic cooked `ModelAsset` VirtualGeometry, the registered `custom:virtual_geometry` geometry source descriptor, page/cluster binding requirements, visible Unlit product capture, and public WGPU execution stats. | `cargo test -p zircon_runtime --lib graphics::tests::render_product_mesh_cache::virtual_geometry::render_product_virtual_geometry_page_cluster_bindings_drive_visible_frame --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-product-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6152 filtered; test body 3.55s after a 25m36s cold default-feature build. Output carried repository-existing warnings only. `runtime_15_virtual_geometry_product_draw_source_is_wired` now tracks this default-feature status with the Plan 08, render index, shader doc, review findings, structure convention, and session anchors; its default-feature Cargo-wrapper guard pass then completed 1/1 with 6166 filtered after a 9m34s build on the same target dir. | Closes the default-feature rerun gap for the focused VirtualGeometry page/cluster product execution test. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside this focused filter. |
| 2026-07-03 | VirtualGeometry page/cluster product execution | render_plan08_virtual_geometry_page_cluster_product_execution_wgpu_passed_renderdoc_deferred | VirtualGeometry product draw-source now reaches a focused no-default product WGPU frame that exercises the page/cluster binding path. The product fixture registers `custom:virtual_geometry` through `GeometrySourceDescriptor`, declares `GeometrySourceBindingKind::VirtualGeometryPages` and `GeometrySourceBindingKind::VirtualGeometryClusters`, emits `ZR_GEOMETRY_SOURCE_VIRTUAL_GEOMETRY`, and feeds the descriptor through `new_with_plugin_render_extensions_and_shading_models` / `new_with_plugin_render_extensions_and_solari_and_shading_models`. `render_product_virtual_geometry_page_cluster_bindings_drive_visible_frame` is anchored by `PAGE_CLUSTER_PRODUCT_STATUS` and reuses the cooked automatic `ModelAsset` path, visible Unlit material, and product capture helper while asserting public WGPU execution signals: input pages, visible clusters, resident+pending execution segments, indirect args, VisBuffer64 entries, and hardware rasterization records. The same slice fixes a real product pipeline blocker by forcing `builtin://shader/pbr.wgsl` fallback keys through the standard material template even when the resource streamer also records that URI as a Surface shader, avoiding the reserved `zr_gpu_scene_light_count` material-symbol path. | `cargo test -p zircon_runtime --lib graphics::tests::render_product_mesh_cache::virtual_geometry::render_product_virtual_geometry_page_cluster_bindings_drive_visible_frame --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-readback-0702 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6133 filtered; test body 4.79s after an 8m08s build. Regression `render_product_virtual_geometry_model_asset_uses_automatic_draw_source` passed 1/1 with 6133 filtered; test body 3.56s after a 7m39s build. Shader-source regression `builtin_fallback_shader_loaded_as_surface_still_uses_standard_material_template` passed 1/1 with 6133 filtered; test body 1.35s after a 4m09s build. Output carried repository-existing warnings only. `runtime_15_virtual_geometry_product_draw_source_is_wired` tracks the product fixture, fallback regression, docs/status/session anchors, and owner budgets for this row. | Closes the focused no-default product WGPU proof that automatic VirtualGeometry `ModelAsset` execution consumes page/cluster bindings and produces a visible frame; the default-feature rerun is closed by `render_plan08_virtual_geometry_page_cluster_product_default_features_wgpu_passed_renderdoc_deferred`. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside the focused filter. |
| 2026-07-03 | VirtualGeometry page/cluster shader bindings default-feature Cargo-wrapper WGPU-layout backfill | render_plan08_virtual_geometry_page_cluster_shader_bindings_default_features_cargo_wrapper_wgpu_layout_passed_renderdoc_deferred | Re-ran the existing page/cluster shader-source assembly, GPUScene layout, and structure guard contracts through the default-feature Cargo wrapper without changing production renderer code, descriptor ownership, WGSL ABI, or GPUScene slot ownership. The covered seams remain `mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings`, `render_gpu_scene_bind_group_layout_reserves_storage_and_palette_bindings`, and `runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired`. | `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::shader_source::tests::mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6106 filtered after a 12m55s build. `cargo test -p zircon_runtime --lib graphics::scene::gpu_scene::binding::tests::render_gpu_scene_bind_group_layout_reserves_storage_and_palette_bindings --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6108 filtered after a 13m56s build. `cargo test -p zircon_runtime --lib runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-default-0703 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 6111 filtered after a 9m51s build. Output carried repository-existing warnings only; RenderDoc MCP listed zero running instances. | Closes the default-feature Cargo-wrapper rerun gap for page/cluster shader-source assembly, WGPU-facing GPUScene layout, and the current structure guard. Still open: real render-pipeline/product execution for this binding path, RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-03 | VirtualGeometry resident/cluster upload default-feature Cargo-wrapper backfill | render_plan08_virtual_geometry_resident_cluster_upload_default_features_cargo_wrapper_passed_renderdoc_deferred | Re-ran the same resident page/cluster upload contracts through the default-feature Cargo wrapper without changing production renderer code, DTO ownership, GPUScene ABI, or the mesh-build resident upload owner. The covered seams remain `render_gpu_scene_uploads_virtual_geometry_resident_buffers` for offscreen WGPU resident page/cluster storage upload, plus `virtual_geometry_page_rows_follow_submission_slots` and `virtual_geometry_cluster_words_follow_resident_page_payloads` for snapshot Resident+submission-slot page rows and resident payload vertex -> position/normal/tangent/pad cluster words. | Initial default-feature `render_gpu_scene_uploads_virtual_geometry_resident_buffers` Cargo-wrapper run on `E:\cargo-targets\zircon-plan08-vg-upload-default-0703` exceeded the 1204s tool window while compiling/linking and is not counted as passing, then the matching processes finished naturally. Warmed default-feature rerun `cargo test -p zircon_runtime --lib graphics::scene::gpu_scene::virtual_geometry::tests::render_gpu_scene_uploads_virtual_geometry_resident_buffers --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6094 filtered, test body 1.78s, after a 9m08s build. The same target dir then passed `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::build_mesh_draws::build::virtual_geometry_resident_upload::tests::virtual_geometry_page_rows_follow_submission_slots --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` 1/1 with 6097 filtered after an 8m02s build, and `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::build_mesh_draws::build::virtual_geometry_resident_upload::tests::virtual_geometry_cluster_words_follow_resident_page_payloads --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-default-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` 1/1 with 6097 filtered after a 4m55s build. Output carried repository-existing warnings only; RenderDoc MCP listed zero running instances. | Closes the default-feature rerun gap for resident buffer upload and resident payload -> cluster word projection. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-03 | VirtualGeometry resident/cluster upload Cargo-wrapper backfill | render_plan08_virtual_geometry_resident_cluster_upload_cargo_wrapper_passed_renderdoc_deferred | Re-ran the existing resident page/cluster upload contracts through the Cargo wrapper without changing production renderer code, DTO ownership, GPUScene ABI, or the mesh-build resident upload owner. The covered seams are `render_gpu_scene_uploads_virtual_geometry_resident_buffers` for offscreen WGPU resident page/cluster storage upload, plus `virtual_geometry_page_rows_follow_submission_slots` and `virtual_geometry_cluster_words_follow_resident_page_payloads` for snapshot Resident+submission-slot page rows and resident payload vertex -> position/normal/tangent/pad cluster words. | `cargo test -p zircon_runtime --lib graphics::scene::gpu_scene::virtual_geometry::tests::render_gpu_scene_uploads_virtual_geometry_resident_buffers --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6037 filtered, test body 2.35s, after a 13m59s build. After stale same-target/fingerprint issues were superseded by fresh target dir `E:\cargo-targets\zircon-plan08-vg-upload-cargo-0703`, `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::build_mesh_draws::build::virtual_geometry_resident_upload::tests::virtual_geometry_page_rows_follow_submission_slots --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-cargo-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6060 filtered after an 8m50s build, and `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::build_mesh_draws::build::virtual_geometry_resident_upload::tests::virtual_geometry_cluster_words_follow_resident_page_payloads --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-upload-cargo-0703 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 6063 filtered after a 13m19s build. Output carried repository-existing warnings only. | Closes the no-default Cargo-wrapper rerun gap for resident buffer upload and resident payload -> cluster word projection; the default-feature rerun is closed by `render_plan08_virtual_geometry_resident_cluster_upload_default_features_cargo_wrapper_passed_renderdoc_deferred`. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-03 | VirtualGeometry page/cluster shader bindings structure-guard compile drift repair | render_plan08_virtual_geometry_page_cluster_shader_bindings_structure_guard_compile_drift_cargo_passed_renderdoc_deferred | Repaired the current-source compile drift that blocked `runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired` after the Cargo-wrapper WGPU-layout backfill. The repair keeps the Plan 08 owner boundaries intact: direct `MaterialAsset` fixtures now initialize the new `parent`/`options`/`queue` fields with neutral defaults, `MaterialCaptureSeed` preserves `MaterialDisabledPasses`, pending command-cache test helpers and `CachedMeshDrawKey` initializers carry default disabled-pass state, and the shader-template structure guard follows the hard-cut `module_registry.rs` owner instead of the retired `include_registry` name. No product renderer branch, compatibility shim, old-path re-export, or RenderDoc/product evidence is introduced by this closeout. | `rustfmt --edition 2021 --check` passed for the touched material/runtime, command-cache, shader-template, and structure-guard files. Stale source scan for `include_registry` under `zircon_runtime/src` returned no matches. Fresh no-default Cargo-wrapper guard `cargo test -p zircon_runtime --lib runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 6028 filtered after a 16m49s build, with repository-existing warnings only. The default-feature guard rerun is closed by `render_plan08_virtual_geometry_page_cluster_shader_bindings_default_features_cargo_wrapper_wgpu_layout_passed_renderdoc_deferred`. | Closes the current-source Cargo structure-guard compile drift for the page/cluster shader-binding status. Still open: real render-pipeline/product execution for this binding path, RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-02 | VirtualGeometry product draw-source default-feature WGPU backfill | render_plan08_virtual_geometry_product_draw_source_default_features_wgpu_passed_renderdoc_deferred | Re-ran the existing automatic VG product readback contract through the default-feature runtime path without changing production renderer code, submit-context/provider ownership, or the product fixture layout. `graphics/tests/render_product_mesh_cache/virtual_geometry.rs::render_product_virtual_geometry_model_asset_uses_automatic_draw_source` still registers the cooked VG `ModelAsset` plus visible Unlit material, submits ordinary `GeometryExtract::from_meshes(...)`, captures the product frame, and proves `RenderVirtualGeometryPayloadSource::AutomaticFallback`, indirect execution stats, and non-black pixels under the default feature set. | Initial default-feature Cargo-wrapper `cargo test -p zircon_runtime --lib graphics::tests::render_product_mesh_cache::virtual_geometry::render_product_virtual_geometry_model_asset_uses_automatic_draw_source --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-default-0702 --message-format short --color never -- --exact --nocapture --test-threads=1` timed out after 1204s while compiling/linking, produced `E:\cargo-targets\zircon-plan08-vg-default-0702\debug\deps\zircon_runtime-770562bad16f99eb.exe`, and left no target-dir residual cargo/rustc/link processes. Direct generated-binary rerun of the same full path passed 1/1 with 5928 filtered in 4.19s. Warmed Cargo-wrapper rerun then passed 1/1 with 5933 filtered; test body 2.72s after the target build finished in 7m19s, with repository-existing warnings only. | Closes the default-feature WGPU/product readback gap for the focused automatic VG `ModelAsset` draw-source filter. Still open: RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside this focused filter. |
| 2026-07-02 | VirtualGeometry product draw-source readback fixture | render_plan08_virtual_geometry_product_draw_source_readback_passed_targeted_cargo | Extended `graphics/tests/render_product_mesh_cache/virtual_geometry.rs::render_product_virtual_geometry_model_asset_uses_automatic_draw_source` so the automatic `ModelAsset` VG product path now registers an explicit Unlit material, keeps a visible Perspective/Core3d camera with black clear and Shaded display mode, captures the product frame, and asserts non-black visible pixels through `assert_virtual_geometry_capture_visible(...)` after proving `RenderVirtualGeometryPayloadSource::AutomaticFallback` and indirect execution stats. This keeps the readback fixture in the existing product child owner instead of adding production render branches or a second provider path. | Targeted no-default Cargo/WGPU passed: `cargo test -p zircon_runtime --lib graphics::tests::render_product_mesh_cache::virtual_geometry::render_product_virtual_geometry_model_asset_uses_automatic_draw_source --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-readback-0702 --message-format short --color never -- --exact --nocapture --test-threads=1` returned 1/1 passed with 5902 filtered; repository-existing warnings only. Scoped rustfmt, anchor scans, conflict/trailing-whitespace scans, line budgets, and diff-check also pass for this slice. | Closes the focused no-default Cargo/WGPU readback evidence for the automatic VG product draw-source fixture. Still open: RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-02 | VirtualGeometry ProjectAssetManager fixture Cargo-wrapper rerun | render_plan08_virtual_geometry_project_asset_manager_fixture_cargo_wrapper_passed_renderdoc_deferred | Rebuilt and reran the fixed ProjectAssetManager `.model.toml` VG fixture after the stale-binary audit. The source still normalizes `expected_model.primitives[0]` through `assign_virtual_geometry_vertex_ordinals()` before comparing `load_model_asset(...)`, keeping root `ModelAsset` import expectations aligned with `ModelPrimitiveAsset`, `MeshAsset` conversion, importer backfill, and `zr_geometry_virtual_geometry.wgsl` ordinal unpacking. | Latest generated no-default lib-test binary `E:\cargo-targets\zircon-plan08-vg-readback-0702\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe` first passed `asset::tests::pipeline::manager::model_import::asset_manager_imports_model_toml_with_virtual_geometry_payload --exact` 1/1 with 5906 filtered. Fresh Cargo-wrapper `cargo test -p zircon_runtime --lib asset::tests::pipeline::manager::model_import::asset_manager_imports_model_toml_with_virtual_geometry_payload --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-readback-0702 --message-format short --color never -- --exact --nocapture --test-threads=1` then passed 1/1 with 5911 filtered and repository-existing warnings only. | Closes the ProjectAssetManager fixture Cargo-wrapper rerun gap from the stale-binary audit. Still open: RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-02 | VirtualGeometry ProjectAssetManager fixture source guard and stale-binary audit | render_plan08_virtual_geometry_project_asset_manager_fixture_source_guarded_cargo_rerun_deferred | Strengthened `runtime_15_virtual_geometry_meshlet_vertex_ordinal_is_wired` so the guard now locks `asset/tests/pipeline/manager/model_import.rs::asset_manager_imports_model_toml_with_virtual_geometry_payload` consuming `expected_model.primitives[0].assign_virtual_geometry_vertex_ordinals()` before comparing against `load_model_asset(...)`. This keeps the ProjectAssetManager root model fixture on the same VG ordinal helper as `ModelPrimitiveAsset`, `MeshAsset` conversion, OBJ/GLTF/model import, and `zr_geometry_virtual_geometry.wgsl`. | A direct rerun against `E:\cargo-targets\zircon-plan08-three-shading-default-0702\debug\deps\zircon_runtime-770562bad16f99eb.exe asset::tests::pipeline::manager::model_import::asset_manager_imports_model_toml_with_virtual_geometry_payload --exact --test-threads=1 --nocapture` failed 0/1 with the same stale all-zero expected joint payload; that binary timestamp is `2026-07-02 05:27:25 +08:00`, before the source fixture fix, so it is not counted as a post-fix regression or pass. A scan found no newer reusable `zircon_runtime` test binary after `2026-07-02 07:00 +08:00`, and active Cargo/Rust lanes were present, so no new Cargo-wrapper rerun is claimed. | Closes only the false-positive/stale-binary documentation and source-guard gap. Still open: fresh Cargo-wrapper rerun for the patched ProjectAssetManager fixture, RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-02 | VirtualGeometry meshlet vertex ordinal direct-binary asset/shader backfill | render_plan08_virtual_geometry_meshlet_vertex_ordinal_direct_binary_asset_shader_passed_renderdoc_deferred | Reused the current no-default `zircon_runtime` lib-test binary to backfill focused evidence for the existing VG source vertex ordinal -> WGSL payload fetch seam. The covered contracts are `ModelPrimitiveAsset::assign_virtual_geometry_vertex_ordinals()` packing 16+16 bit source vertex ordinals into `joint_indices[0..=1]`, non-VG primitive preservation, `MeshAsset::from_model_primitive(...)` / `to_model_primitive(...)` carrying the same ordinal stream, OBJ/GLTF/`.model.toml` import assertions, and `zr_geometry_virtual_geometry.wgsl` unpacking `v.joints.x | (v.joints.y << 16)` before resident cluster-word fetch. The ProjectAssetManager `.model.toml` fixture expectation was corrected to normalize the expected VG primitive through the same helper; the old generated binary still contains the stale expected zero-joint assertion for that broader asset-manager test, so that failed direct-binary attempt is documented but not counted as passing. `runtime_15_virtual_geometry_meshlet_vertex_ordinal_is_wired` now tracks this stronger status anchor while the original static status remains in historical rows for older generated guard binaries. | Direct generated-binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe asset::tests::assets::model::virtual_geometry_vertex_ordinals --test-threads=1 --nocapture` passed 2/2 with 5880 filtered in 0.00s. Direct generated-binary exact runs passed `asset::tests::assets::mesh::conversion_import::model_primitive_converts_to_mesh_asset_with_builtin_attributes` 1/1, `asset::tests::assets::importer::shader_model::importer_decodes_obj_into_model_asset` 1/1, `asset::tests::assets::importer::shader_model::importer_backfills_virtual_geometry_for_model_toml_without_dropping_base_mesh` 1/1, `asset::tests::assets::gltf_importer::basic_import::importer_decodes_triangle_gltf_into_model_asset` 1/1, `asset::tests::assets::gltf_importer::basic_import::default_importer_decodes_gltf_without_first_wave_plugin_fixture` 1/1, and `graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::shader_source::tests::mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings` 1/1, each with 5881 filtered except the shader source test with 5881 filtered in 0.00s. The broader direct-binary `asset::tests::pipeline::manager::model_import::asset_manager_imports_model_toml_with_virtual_geometry_payload --exact` failed because the old generated test expected all-zero joints while the current production path returns normalized VG ordinals; source expectation was fixed, but no new Cargo-wrapper run is claimed while other Cargo lanes are active. | Closes focused direct-binary evidence for the asset/importer/MeshAsset/WGSL ordinal seam. Still open: Cargo-wrapper rerun for the patched ProjectAssetManager fixture, RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-03 | VirtualGeometry page/cluster shader bindings Cargo-wrapper WGPU-layout backfill | render_plan08_virtual_geometry_page_cluster_shader_bindings_cargo_wrapper_wgpu_layout_passed_renderdoc_deferred | Re-ran the two existing page/cluster shader-source and GPUScene layout contracts through fresh no-default Cargo-wrapper commands without changing production renderer code, descriptor ownership, WGSL ABI, or GPUScene slot ownership. The first same-command attempt failed before the target tests because the concurrently dirty text raster source imported `swash` before the current Cargo dependency snapshot included it; that compile failure is documented but not counted as a Plan 08 pass or regression. After the dependency snapshot was current, the focused Cargo-wrapper reruns used full test paths so the earlier short-name `--exact` 0-tests issue could not recur. `runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired` now tracks this stronger status anchor while continuing to lock descriptor, plugin manifest, GPUScene slots, WGSL fetch, prewarm fixtures, docs/status/session anchors, and file budgets. | `cargo test -p zircon_runtime --lib graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::shader_source::tests::mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 5984 filtered after a 17m45s build. `cargo test -p zircon_runtime --lib graphics::scene::gpu_scene::binding::tests::render_gpu_scene_bind_group_layout_reserves_storage_and_palette_bindings --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 5992 filtered after a 14m59s build. Output carried repository-existing warnings only. | Closes the focused no-default Cargo-wrapper rerun gap for the page/cluster shader-source assembly and WGPU-facing GPUScene layout contracts; the default-feature rerun is closed by `render_plan08_virtual_geometry_page_cluster_shader_bindings_default_features_cargo_wrapper_wgpu_layout_passed_renderdoc_deferred`. Still open: real render-pipeline/product execution for this binding path, RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-02 | VirtualGeometry page/cluster shader bindings direct-binary WGPU-layout backfill | render_plan08_virtual_geometry_page_cluster_shader_bindings_direct_binary_wgpu_layout_passed_renderdoc_deferred | Reused the current no-default `zircon_runtime` lib-test binary to backfill the two focused page/cluster shader-binding tests that previously timed out during Cargo compilation. `mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings` assembles the VirtualGeometry standard-material shader source and proves the generated WGSL declares group3 page/cluster storage bindings plus guarded page/cluster fetch helpers. `render_gpu_scene_bind_group_layout_reserves_storage_and_palette_bindings` proves the GPUScene bind-group layout reserves the non-morph storage slots for VirtualGeometry pages/clusters while preserving palette/remap bindings. `runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired` now tracks this stronger status anchor while continuing to lock descriptor, plugin manifest, GPUScene slots, WGSL fetch, prewarm fixtures, docs/status/session anchors, and file budgets. | Direct generated-binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::shader_source::tests::mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings --exact --test-threads=1 --nocapture` passed 1/1 with 5881 filtered in 0.00s. Direct generated-binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe graphics::scene::gpu_scene::binding::tests::render_gpu_scene_bind_group_layout_reserves_storage_and_palette_bindings --exact --test-threads=1 --nocapture` passed 1/1 with 5881 filtered in 0.00s. Active runtime text Cargo lanes were present, so no new Cargo-wrapper run is claimed for this slice. | Closes the focused direct-binary shader-source and WGPU-layout evidence gap for page/cluster shader bindings. Still open: Cargo-wrapper rerun if required by milestone validation, real render-pipeline/product execution for this binding path, RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0. |
| 2026-07-02 | VirtualGeometry cluster payload upload direct-binary WGPU backfill | render_plan08_virtual_geometry_cluster_payload_upload_direct_binary_wgpu_passed_renderdoc_deferred | Reused the current no-default `zircon_runtime` lib-test binary to backfill focused runtime evidence for the existing resident page payload -> cluster word projection slice without changing production renderer code. The covered contract remains `graphics/scene/scene_renderer/mesh/build_mesh_draws/build/virtual_geometry_resident_upload.rs::virtual_geometry_cluster_words_follow_resident_page_payloads`, which proves `RenderVirtualGeometryDebugSnapshot.resident_page_payloads` payload vertices become position/normal/tangent/pad `GpuVirtualGeometryClusterWord` rows, repeated resident submissions for the same page share the same cluster word storage, and page rows carry the expected nonzero vertex counts. `runtime_15_virtual_geometry_cluster_payload_upload_is_wired` now tracks this stronger status anchor while continuing to lock the debug snapshot DTO/re-export, production sidecar handoff, GPU ABI constant, resident upload projection, docs/status/session anchors, and file budgets. | Direct generated-binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe graphics::scene::scene_renderer::mesh::build_mesh_draws::build::virtual_geometry_resident_upload::tests::virtual_geometry_cluster_words_follow_resident_page_payloads --exact --test-threads=1 --nocapture` passed 1/1 with 5881 filtered in 0.00s. Active runtime text Cargo lanes were present, so no new Cargo-wrapper run is claimed for this slice. | Closes the focused direct-binary WGPU evidence gap for resident payload -> cluster word projection. Still open: Cargo-wrapper rerun if required by milestone validation, RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside this focused owner. |
| 2026-07-02 | VirtualGeometry resident buffers upload direct-binary WGPU backfill | render_plan08_virtual_geometry_resident_buffers_upload_direct_binary_wgpu_passed_renderdoc_deferred | Reused the current no-default `zircon_runtime` lib-test binary to backfill focused runtime evidence for the existing resident buffer upload slice without touching production renderer code. The covered contracts remain `graphics/scene/gpu_scene/virtual_geometry.rs::render_gpu_scene_uploads_virtual_geometry_resident_buffers`, which creates an offscreen backend and verifies typed resident page/cluster storage upload, CPU shadows, uploaded byte accounting, and bind-group rebuild/no-rebuild behavior, plus `graphics/scene/scene_renderer/mesh/build_mesh_draws/build/virtual_geometry_resident_upload.rs::virtual_geometry_page_rows_follow_submission_slots`, which proves only resident segments with submission slots produce page rows and that slot gaps stay default. `runtime_15_virtual_geometry_resident_buffers_upload_is_wired` now tracks this stronger status anchor while continuing to lock GPUScene ABI/upload owner, mesh-build handoff, payload-slot assignment, docs/status/session anchors, and file budgets. | Direct generated-binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe graphics::scene::gpu_scene::virtual_geometry::tests::render_gpu_scene_uploads_virtual_geometry_resident_buffers --exact --test-threads=1 --nocapture` passed 1/1 with 5881 filtered in 1.24s. Direct generated-binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe graphics::scene::scene_renderer::mesh::build_mesh_draws::build::virtual_geometry_resident_upload::tests::virtual_geometry_page_rows_follow_submission_slots --exact --test-threads=1 --nocapture` passed 1/1 with 5881 filtered in 0.00s. Active runtime text Cargo lanes were present, so no new Cargo-wrapper run is claimed for this slice. | Closes the focused direct-binary WGPU evidence gap for resident page/cluster upload and resident page-row projection. Still open: Cargo-wrapper rerun if required by milestone validation, RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside this focused owner pair. |
| 2026-07-02 | VirtualGeometry product draw-source Cargo-wrapper WGPU backfill | render_plan08_virtual_geometry_product_draw_source_cargo_wrapper_wgpu_passed_renderdoc_deferred | Re-ran the existing product draw-source contract through the no-default-features Cargo wrapper without changing production renderer code or the product fixture owner layout. `graphics/tests/render_product_mesh_cache/virtual_geometry.rs::render_product_virtual_geometry_model_asset_uses_automatic_draw_source` still registers a cooked VG `ModelAsset`, submits ordinary `GeometryExtract::from_meshes(...)` snapshots, and proves the frame used `RenderVirtualGeometryPayloadSource::AutomaticFallback` with visible automatic provider instance/page/cluster and indirect execution stats. `runtime_15_virtual_geometry_product_draw_source_is_wired` now tracks this stronger status anchor while continuing to lock the submit-context/provider/product-test/docs seam and file budgets. | First short-name `--exact` direct-binary attempt ran 0 tests and is not counted. The full-path direct generated-binary rerun `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe graphics::tests::render_product_mesh_cache::virtual_geometry::render_product_virtual_geometry_model_asset_uses_automatic_draw_source --exact --test-threads=1 --nocapture` passed 1/1 with 5873 filtered in 3.64s. Fresh Cargo-wrapper `cargo test -p zircon_runtime --lib graphics::tests::render_product_mesh_cache::virtual_geometry::render_product_virtual_geometry_model_asset_uses_automatic_draw_source --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --exact --nocapture --test-threads=1` passed 1/1 with 5881 filtered; the test body finished in 5.25s after the warmed target build finished in 10m13s, and output only carried repository-existing warnings. | Closes the focused no-default Cargo/WGPU product draw-source evidence for the automatic VG `ModelAsset` path. Still open: RenderDoc/product capture, default features, workspace/full CI, full live project/plugin registry export, and broader product miss=0 outside this focused filter. |
| 2026-07-01 | Plan 08 three shading-model forward/deferred product parity | render_plan08_three_shading_models_forward_deferred_parity_wgpu_passed_light_grid_fallback_renderdoc_deferred | Added folder-backed product test `graphics/tests/render_product_mesh_cache/shading_model_parity.rs::render_product_three_shading_models_forward_deferred_parity` and Runtime 15 structure guard `runtime_15_render_plan08_three_shading_models_forward_deferred_parity_is_wired`. The test renders the same PBR/Blinn-Phong/Unlit material swatches through Forward + Deferred default product pipelines, disables optional quality features that would mask material differences, checks `mesh.opaque` vs `deferred.gbuffer` + `lighting.deferred` executor evidence, and compares primary-surface RGBA captures with `assert_rgba_frames_nearly_equal(...)`. The first WGPU run exposed the shared disabled-clustered-lighting gap, so `bind_execution_owned_graph_resources.rs` now binds `LightGridParams::disabled()`, empty z-bin/tile-mask buffers, and `:light-grid-execution-fallback` aliases whenever mesh/deferred passes read light-grid resources without the clustered writer. | Scoped rustfmt passed for the product child, parent mount, guard, guard parent, and execution-resource binder. `light_grid_external_fallback_buffers_satisfy_materialization_report` passed 1/1. Focused no-default Cargo/WGPU `render_product_three_shading_models_forward_deferred_parity` passed 1/1 with `5818` filtered out. After docs sync, `runtime_15_render_plan08_three_shading_models_forward_deferred_parity_is_wired` passed 1/1, and direct-binary reruns of the fallback and product filters both passed 1/1. A related existing probe, `deferred_pipeline_uses_gbuffer_material_path_instead_of_forward_shader_path`, no longer fails on unbound `light-grid-params` after the fallback; the follow-up `render_plan08_deferred_project_shader_gbuffer_probe_wgpu_passed_renderdoc_deferred` row closes its fixture/threshold gate. | RenderDoc/product capture, default features, and workspace/full CI remain open. |
| 2026-07-02 | Plan 08 three shading-model forward/deferred product parity default-feature WGPU backfill | render_plan08_three_shading_models_forward_deferred_parity_default_features_wgpu_passed_renderdoc_deferred | Re-ran the existing `graphics/tests/render_product_mesh_cache/shading_model_parity.rs::render_product_three_shading_models_forward_deferred_parity` product contract through the default-feature Cargo harness instead of the earlier no-default harness. The product owner, renderer production code, and test layout did not change; the only code unblock was a test-scope import in `graphics/scene/scene_renderer/ui/text.rs` so the default-feature lib-test build can name `SdfAtlasRect` from the sibling `sdf_atlas` module without using the wrong `super::sdf_atlas` path. | Initial default-feature compile failed before the render test because the UI text test module referenced `super::sdf_atlas::SdfAtlasRect`; after the test-only path fix, `cargo test -p zircon_runtime --lib render_product_three_shading_models_forward_deferred_parity --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-default-0702 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5876 filtered out, finished in 11.81s, and emitted repository-existing warnings only. The first Cargo wrapper run for `runtime_15_render_plan08_three_shading_models_forward_deferred_parity_is_wired` timed out after 904s during compile/link but produced the no-default lib-test binary; direct generated-binary rerun passed 1/1 with 5873 filtered in 0.41s. RenderDoc MCP still reported 0 running instances. | Closes the default-feature Cargo/WGPU rerun gap for this focused PBR/Blinn-Phong/Unlit Forward + Deferred product parity filter and refreshes the status/docs guard. Still open: RenderDoc/product capture, workspace/full CI, and broader product cases outside this focused filter. |
| 2026-07-01 | Deferred project-shader vs GBuffer probe fixture repair | render_plan08_deferred_project_shader_gbuffer_probe_wgpu_passed_renderdoc_deferred | Repaired the existing `graphics/tests/project_render/render_quality.rs::deferred_pipeline_uses_gbuffer_material_path_instead_of_forward_shader_path` probe after support-first diagnosis showed the prior fixture mixed two signals: `write_flat_green_wgsl(...)` multiplies project shader color by material tint, while the test uses a red base color so Deferred can prove GBuffer material decode. The test now writes a pure green project shader through `write_flat_color_wgsl(..., [0.0, 1.0, 0.0])` and samples the covered center region through shared `average_channel_in_region(...)`; Forward now proves authored project WGSL output and Deferred proves standard GBuffer/base-color shading. The Runtime 15 guard now locks `average_channel_in_region`, the pure green fixture, docs anchors, and line budgets alongside the three-model parity owner. | Initial 120s Cargo wrapper attempt timed out while building the lib-test binary; the matching cargo/rustc process was allowed to finish and an earlier direct generated-binary rerun passed 1/1 with 5822 filtered. After removing the introduced unused import, final Cargo wrapper `cargo test -p zircon_runtime --lib deferred_pipeline_uses_gbuffer_material_path_instead_of_forward_shader_path --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1, 0 failed, 5825 filtered out, finished in 4.42s; output still has repository-existing warnings only. Direct generated-binary `runtime_15_render_plan08_three_shading_models_forward_deferred_parity_is_wired` passed 1/1 with 5825 filtered after docs sync. | Closes the existing Deferred color-threshold gate for the no-default focused WGPU binary. Still open: RenderDoc/product capture, default features, workspace/full CI, and broader product coverage outside focused filters. |
| 2026-07-01 | Project/plugin registry material-pass second-launch direct-binary WGPU backfill | render_plan08_project_plugin_registry_material_passes_second_launch_direct_binary_wgpu_passed_renderdoc_deferred | Backfilled the previously open Plan 08 MS-M4 project/plugin registry material-pass second-launch WGPU result without touching production renderer code. The existing `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/second_launch.rs` contract still writes the staged manifest once, creates two fresh `WgpuRenderFramework` launches per project/plugin registry shader against the same staged fallback root, covers first-frame DepthPrepass/GBuffer/Shadow/TAA reactive mask plus second-frame Velocity, requires compile miss=0, and uses `assert_runtime_shader_cache_root_empty(...)` so the runtime cache root stays empty instead of letting the second product launch read a first-launch runtime write. | Reused `E:\cargo-targets\zircon-plan08-custom-shading-second-launch-guard-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe` and ran `render_product_project_plugin_registry_material_passes_second_launch_use_staged_prewarm_without_compile_miss --test-threads=1 --nocapture`; result: 1 passed / 0 failed / 5806 filtered out, finished in 14.95s. After docs sync, the same binary also passed `runtime_15_shader_prewarm_project_plugin_registry_material_passes_staged_cache_is_wired` 1/1 with 5806 filtered in 0.44s. | Closes the direct-binary WGPU result and existing material-pass structure guard for the project/plugin registry material-pass second-launch product gate. Still open: Cargo-wrapper rerun if required by milestone validation, RenderDoc/product capture, default features, and workspace/full CI. |
| 2026-07-02 | Project/plugin registry material-pass second-launch Cargo-wrapper WGPU backfill | render_plan08_project_plugin_registry_material_passes_second_launch_cargo_wrapper_wgpu_passed_renderdoc_deferred | Re-ran the existing project/plugin registry material-pass second-launch product contract through a fresh no-default-features Cargo-wrapper command, replacing the earlier direct-binary-only evidence for this focused gate. No production renderer code or owner layout changed; the existing `second_launch.rs` test still proves two fresh product launches per registry shader, staged fallback hits for DepthPrepass/GBuffer/Shadow/TAA reactive mask plus Velocity, compile miss=0, second product launch behavior, and runtime cache root stays empty. | `cargo test -p zircon_runtime --lib render_product_project_plugin_registry_material_passes_second_launch_use_staged_prewarm_without_compile_miss --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5842 filtered out, finished in 12.86s, and emitted repository-existing warnings only. After docs sync, `cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_project_plugin_registry_material_passes_staged_cache_is_wired --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5848 filtered. | Closes the Cargo-wrapper rerun gap for the focused project/plugin registry material-pass second-launch product gate and its status/docs structure guard. Still open: RenderDoc/product capture, default features, workspace/full CI, and broader product cases outside this focused filter. |
| 2026-07-01 | Plan 08 selected-plugin/source-registry deferred guard direct-binary backfill | render_plan08_selected_plugin_shading_model_registration_inputs_direct_binary_passed_cargo_wrapper_deferred / render_plan08_edited_shader_revision_export_direct_binary_passed_cargo_wrapper_deferred / render_plan08_shading_model_include_source_set_direct_binary_passed_cargo_wrapper_deferred | Backfilled three previously deferred low-level Plan 08 guards without touching production renderer code. The backfill covers selected plugin `ShadingModelDescriptor` registration input handoff, edited project shader Ready-record revision export after `ProjectAssetManager::import_asset(...)`, and project/plugin Ready shader source-set export into Forward/GBuffer template requests. Existing static status anchors remain in place for structure guards; this row records the fresh direct lib-test binary evidence. | Reused `E:\cargo-targets\zircon-plan08-custom-shading-product-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe`. `plugin_registration_inputs_collect_shading_model_descriptors` passed 1/1 (`5809` filtered); `shader_reimport_exports_updated_revision_for_prewarm_registry` passed 1/1 (`5809` filtered); `exported_include_source_set_feeds_forward_and_gbuffer_template_requests` passed 1/1 (`5809` filtered). A later fresh Cargo wrapper run closed the staged-prewarm product-sweep structure guard with 1 passed / 0 failed / 5825 filtered out. | Closes the direct-binary validation gap for these three focused guards. Still open: Cargo-wrapper reruns if required by milestone testing, RenderDoc/product capture, default features, and workspace/full CI. |
| 2026-07-02 | Selected plugin/source-registry guard Cargo-wrapper backfill | render_plan08_selected_plugin_source_registry_guards_cargo_wrapper_passed_renderdoc_deferred | Re-ran the same three low-level Plan 08 selected-plugin/source-registry guards through fresh no-default-features Cargo wrapper commands, replacing the earlier direct-binary-only evidence for this group. The covered seams are selected plugin `ShadingModelDescriptor` report handoff into `RuntimeModuleRegistrationInputs`, edited project shader Ready-record revision export after `ProjectAssetManager::import_asset(...)`, and Ready project/plugin shader source-set export into Forward/GBuffer template requests. | `cargo test -p zircon_runtime --lib plugin_registration_inputs_collect_shading_model_descriptors --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5839 filtered. The same Cargo-wrapper shape passed `shader_reimport_exports_updated_revision_for_prewarm_registry` 1/1 with 5839 filtered and `exported_include_source_set_feeds_forward_and_gbuffer_template_requests` 1/1 with 5839 filtered. All three runs emitted repository-existing warnings only. After docs sync, `cargo test -p zircon_runtime --lib runtime_15_material_custom_shading_model_runtime_registry_is_wired --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5842 filtered. | Closes the Cargo-wrapper rerun gap for these three focused selected-plugin/source-registry guards and their status/docs structure guard. Still open: RenderDoc/product capture, default features, workspace/full CI, and broader product cases outside these focused guards. |
| 2026-07-03 | Plan 08 staged-prewarm product sweep default-feature current WGPU refresh | render_plan08_staged_prewarm_product_sweep_default_features_current_wgpu_refresh_passed_renderdoc_deferred | Re-ran the current default-feature `staged_prewarm_without_compile_miss` sweep after the standard and custom surface-source fixes. The first current sweep exposed a real 10/11 regression in the raw project/plugin registry product path: its runtime `ShaderAssetKind::Surface` still used assembled Forward-pass WGSL and raw `mesh-template-v1` cache identity. The lowest test-support fix moves `registry_staged_cache_runtime_surface_source()` to the mesh-cache product test parent, lets raw registry and material-pass registry use the same runtime `fn zr_material_surface(` source, and keeps the raw registry prewarm manifest on the standard material template include hashes and `zr-material-template-v1` revision. | Current default-feature `--list` on `E:\cargo-targets\zircon-plan08-vg-product-default-0703` enumerated the same 11 tests after a 12m 45s build. First current sweep failed 10/11 at `render_product_project_plugin_registry_materials_use_staged_prewarm_without_compile_miss` with `base mesh command must resolve a cache-backed pipeline variant`. After the fixture/key fix, the exact raw registry product test passed 1/1 with 6197 filtered, test body 5.65s after a 10m 04s build. Final Cargo-wrapper sweep `cargo test -p zircon_runtime --lib staged_prewarm_without_compile_miss --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-vg-product-default-0703 --message-format short --color never -- --nocapture --test-threads=1` passed 11/11 with 6187 filtered, test body 48.90s after a 7m 36s build. Output carried repository-existing warnings only. | Supersedes the stale 2026-07-02 default-feature sweep evidence for current source. Still open: RenderDoc/product capture, workspace/full CI, and product cases outside this filter. `runtime_15_render_plan08_staged_prewarm_product_sweep_is_wired` now locks this current refresh. |
| 2026-07-01 | Plan 08 staged-prewarm product sweep | render_plan08_staged_prewarm_product_sweep_wgpu_passed_renderdoc_deferred | Plan 08 broader staged-prewarm product/runtime sweep now has a single filtered WGPU evidence run across the existing staged cache gates. The sweep covers runtime Base mesh staged cache, custom geometry Base and non-Base runtime consumers, project/plugin registry shader-key runtime handoff, raw project/plugin product materials, project/plugin material-pass product, project/plugin material-pass second launch, custom shading-model material-pass product, custom shading-model second launch, material-mesh deferred product second launch, and Base mesh product second launch. Added `runtime_15_render_plan08_staged_prewarm_product_sweep_is_wired` to lock the test set, status/docs anchors, and module docs without adding production behavior. | Reused the current no-default-features `zircon_runtime` lib-test binary and ran `E:\cargo-targets\zircon-plan08-custom-shading-product-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe staged_prewarm_without_compile_miss --test-threads=1 --nocapture`; result: 11 passed / 0 failed / 5799 filtered out, finished in 62.14s. RenderDoc MCP reported no running RenderDoc instances. Fresh Cargo wrapper `cargo test -p zircon_runtime --lib runtime_15_render_plan08_staged_prewarm_product_sweep_is_wired --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5825 filtered out. | Closes the current broader staged-prewarm miss=0 product/runtime sweep and the staged-prewarm product-sweep structure guard. Still open: RenderDoc/product capture, default features, workspace/full CI, and any future product cases outside the `staged_prewarm_without_compile_miss` filter. |
| 2026-07-02 | Plan 08 staged-prewarm product sweep default-feature direct-binary WGPU backfill | render_plan08_staged_prewarm_product_sweep_default_features_direct_binary_wgpu_passed_renderdoc_deferred | Reused the current default-feature `zircon_runtime` lib-test binary to run the existing `staged_prewarm_without_compile_miss` sweep without changing production renderer code, product fixtures, or owner layout. `--list` enumerated the same 11 runtime/product staged-cache tests: Base mesh runtime/product second launch, custom geometry runtime Base/non-Base, project/plugin registry shader-key/runtime and product raw/material-pass/second-launch, custom shading-model product/second-launch, and material-mesh deferred second launch. | Direct generated-binary `E:\cargo-targets\zircon-plan08-custom-default-0702\debug\deps\zircon_runtime-770562bad16f99eb.exe staged_prewarm_without_compile_miss --nocapture --test-threads=1` passed 11/11, 0 failed, 5924 filtered out, finished in 56.74s. RenderDoc MCP reported 0 running instances. No target-dir cargo/rustc/link processes were active before the run. | Closes the focused default-feature direct-binary WGPU sweep for the existing staged-prewarm miss=0 filter. Still open: Cargo-wrapper rerun if required by milestone testing, RenderDoc/product capture, workspace/full CI, and product cases outside this filter. |
| 2026-07-02 | Plan 08 staged-prewarm product sweep default-feature Cargo-wrapper WGPU backfill | render_plan08_staged_prewarm_product_sweep_default_features_cargo_wrapper_wgpu_passed_renderdoc_deferred | Re-ran the same existing `staged_prewarm_without_compile_miss` sweep through the default-feature Cargo wrapper, replacing the preceding default-feature direct-binary-only evidence for this focused filter. No production renderer code, product fixture, or owner layout changed. | `cargo test -p zircon_runtime --lib staged_prewarm_without_compile_miss --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-custom-default-0702 --message-format short --color never -- --nocapture --test-threads=1` passed 11/11, 0 failed, 5925 filtered out, finished in 56.99s after the target build finished in 10m 04s. Output had repository-existing warnings only. | Closes the focused default-feature Cargo-wrapper WGPU sweep for the existing staged-prewarm miss=0 filter. Still open: RenderDoc/product capture, workspace/full CI, and product cases outside this filter. |
| 2026-07-01 | Custom shading-model deferred-lighting product readback | render_plan08_custom_shading_model_deferred_lighting_product_readback_wgpu_passed_renderdoc_deferred | Added `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/custom_shading_model.rs::render_product_custom_shading_model_deferred_lighting_readback_uses_project_include` to prove the product deferred-lighting pass consumes the authored project/plugin `custom:toon` deferred include, not only the staged-cache keys. The product child now registers Ready Forward/GBuffer/deferred include WGSL, prewarms the six-pass manifest with WGPU pipeline validation, submits visible product deferred frames through `submit_registry_material_passes_with_plugin_shading_model_capture(...)`, captures both first and Velocity frames, and checks that `max(0.65, ...)` in the custom deferred include produces dominant green pixels. The shared fixture now uses `select_visible_registry_material_pass_camera(...)` and `registry_material_pass_viewport_size()` so readback tests see the mesh instead of a black offscreen frame. | Focused `cargo test -p zircon_runtime --lib render_product_custom_shading_model_deferred_lighting_readback_uses_project_include --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5831 filtered out and repository warnings only. The earlier red/green cycle first proved the old include produced zero dominant-green pixels, then isolated the black-frame fixture root cause before the visible-camera fix. | Closes the focused product readback evidence for custom deferred lighting include execution. Still open: RenderDoc/product capture, default features, workspace/full CI, and broader product cases outside the focused filter. |
| 2026-07-02 | Custom shading-model product group direct-binary sweep | render_plan08_custom_shading_model_product_group_direct_binary_wgpu_passed_renderdoc_deferred | Reused the freshly built no-default `zircon_runtime` lib-test binary to run the whole `render_product_custom_shading_model` product group without starting a new Cargo compile while text/editor lanes were active. The filter covers `render_product_custom_shading_model_registry_material_passes_use_staged_prewarm_without_compile_miss`, `render_product_custom_shading_model_second_launch_uses_staged_prewarm_without_compile_miss`, and `render_product_custom_shading_model_deferred_lighting_readback_uses_project_include`, so custom `custom:toon` staged-cache, second-launch read-only cache behavior, and deferred-lighting pixel readback are checked together in one WGPU product pass. | Direct binary `E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701\debug\deps\zircon_runtime-fe15dbfd02d9864e.exe render_product_custom_shading_model --test-threads=1 --nocapture` passed 3/3, 0 failed, 5830 filtered out, finished in 15.76s. `--list` for the same filter enumerated exactly those 3 tests before execution. After status/doc sync, `cargo test -p zircon_runtime --lib runtime_15_material_custom_shading_model_runtime_registry_is_wired --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with 5838 filtered out. | Closes the current custom shading-model product group direct-binary rerun gap and its status/docs structure guard. Still open: RenderDoc/product capture, default features, workspace/full CI, and product cases outside the custom shading-model filter. |
| 2026-07-02 | Custom shading-model product group Cargo-wrapper WGPU backfill | render_plan08_custom_shading_model_product_group_cargo_wrapper_wgpu_passed_renderdoc_deferred | Re-ran the whole `render_product_custom_shading_model` product group through a fresh no-default-features Cargo-wrapper command, replacing the earlier direct-binary-only evidence for this focused custom gate. No production renderer code, test owner, or owner layout changed; the same filter covers custom staged-cache miss=0, custom second-launch read-only staged-cache behavior, and custom deferred-lighting pixel readback for `custom:toon`. | `cargo test -p zircon_runtime --lib render_product_custom_shading_model --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 3/3, 0 failed, 5850 filtered out, finished in 19.04s, and emitted repository-existing warnings only. After docs sync, `cargo test -p zircon_runtime --lib runtime_15_material_custom_shading_model_runtime_registry_is_wired --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-three-shading-model-parity-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1. | Closes the Cargo-wrapper rerun gap for the focused custom shading-model product group and its status/docs structure guard. Still open: RenderDoc/product capture, default features, workspace/full CI, and product cases outside the custom shading-model filter. |
| 2026-07-02 | Custom shading-model product group default-feature WGPU backfill | render_plan08_custom_shading_model_product_group_default_features_wgpu_passed_renderdoc_deferred | Re-ran the same `render_product_custom_shading_model` product group through the default-feature runtime path without changing production renderer code, test ownership, or owner layout. The filter still covers custom `custom:toon` staged-cache miss=0, second-launch read-only staged-cache behavior, and deferred-lighting product readback in one WGPU product group. | Initial default-feature Cargo-wrapper on `E:\cargo-targets\zircon-plan08-custom-default-0702` timed out after 1204.4s while compiling/linking and is not counted as a pass; it produced `zircon_runtime-770562bad16f99eb.exe` and left no target-dir residual cargo/rustc/link processes. Direct generated-binary rerun passed 3/3 with 5932 filtered in 20.20s. Warmed Cargo-wrapper `cargo test -p zircon_runtime --lib render_product_custom_shading_model --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-custom-default-0702 --message-format short --color never -- --nocapture --test-threads=1` passed 3/3 with 5932 filtered; test body 21.45s after a 4m00s build, repository-existing warnings only. RenderDoc MCP reported 0 running instances. | Closes the focused default-feature WGPU rerun gate for the custom shading-model product group. Still open: RenderDoc/product capture, workspace/full CI, and product cases outside the custom shading-model filter. |
| 2026-07-01 | Custom shading-model second-launch staged-cache WGPU validation | render_plan08_custom_shading_model_second_launch_staged_cache_wgpu_passed_renderdoc_deferred | Plan 08 custom shading-model product staged-cache proof now covers two fresh `WgpuRenderFramework` launches. Added folder-backed `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/custom_second_launch.rs`, reusing the `custom:toon` descriptor/source helper, the plugin-shading six-pass manifest builder, and product submit helper. The test prewarms with `prewarm_shader_variants_with_wgpu_pipeline_validation(...)`, then runs first and second product launches against the same staged fallback root while keeping the runtime cache root read-only. Both launches require first-frame and Velocity-frame custom shading-model dimensions, staged disk hits, compile miss=0, disk write/error=0, previous velocity transforms, and `assert_runtime_shader_cache_root_empty(...)`. | `rustfmt --edition 2021 --check` passed on the touched product-test and structure-guard files. Focused `cargo test -p zircon_runtime --lib render_product_custom_shading_model_second_launch_uses_staged_prewarm_without_compile_miss --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-custom-shading-product-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with repository warnings only. | Closes the focused custom shading-model second-launch product staged-cache miss=0 rerun. Still open: RenderDoc/product capture, broader product sweep, default features, and full CI. |
| 2026-07-01 | Deferred lighting custom include WGPU pipeline validation | render_plan08_deferred_lighting_custom_include_wgpu_pipeline_passed_product_renderdoc_deferred | Plan 08 deferred lighting custom include now has focused WGPU render-pipeline evidence. Added folder-backed child test `graphics/scene/scene_renderer/deferred/lighting_pipeline/tests/runtime_pipeline.rs` and mounted it from `lighting_pipeline/tests.rs` with `mod runtime_pipeline;`. The test registers Ready plugin shader assets for `zr_shading_toon.wgsl`, `zr_gbuffer_encode_toon.wgsl`, and `zr_shade_deferred_toon.wgsl`, builds a plugin-range `custom:toon` descriptor, creates a real offscreen `RenderBackend`, product `create_lighting_bind_group_layout(...)`, product `GpuScene::new(...).scene_bind_group_layout()`, and production `create_lighting_pipeline(...)`, then verifies the created WGPU pipeline under a validation error scope. | `cargo test -p zircon_runtime --lib custom_shading_model_deferred_lighting_pipeline_creates_with_project_include_source --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-deferred-lighting-custom-pipeline-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with repository warnings only after dependency/test-binary build, then passed again after the status guard sweep. Scoped `rustfmt --edition 2021 --check` passed for the touched Rust owners, and `runtime_15_material_custom_shading_model_runtime_registry_is_wired` passed 1/1 after correcting the pre-existing `plan_status/index_tables` child include paths to `../status_output_tables/...`. | Closes deferred lighting custom include runtime source assembly -> WGPU render-pipeline validation. Still open: full product scene/readback evidence, RenderDoc/product capture, broader miss=0/product sweeps, default-feature reruns where useful, and full CI. |
| 2026-07-01 | Custom shading-model runtime WGPU module validation | render_plan08_custom_shading_model_runtime_wgpu_module_passed_product_renderdoc_deferred | Plan 08 runtime source handoff now has focused authored-plugin WGPU module evidence. Added folder-backed child test `graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source/tests/runtime_shading_model_sources.rs` and mounted it from `shader_source.rs` without growing the main owner. The test registers a plugin-range `custom:toon` `ShadingModelDescriptor`, exports Ready shader assets for `zr_shading_toon.wgsl`, `zr_gbuffer_encode_toon.wgsl`, and `zr_shade_deferred_toon.wgsl`, constructs `ResourceStreamer::new_for_test_with_plugin_shading_models(...)`, drives a plugin `PipelineKey` through `mesh_pipeline_shader_source_for_geometry_descriptor(...)` and `mesh_pipeline_deferred_gbuffer_template_source_for_geometry_descriptor_with_streamer(...)`, then validates the assembled Forward and GBuffer WGSL with `device.create_shader_module(...)` under a WGPU validation error scope. | Scoped `rustfmt --edition 2021 --check` passed for `shader_source.rs`, the new runtime source child test, and the updated structure guard. Static conflict/trailing-whitespace scan passed; line counts are `shader_source.rs` 832 and child test 198. Focused default-feature cargo test attempts timed out during Windows lib-test compile/link with no result and residual target processes were stopped; the narrowed command `cargo test -p zircon_runtime --lib runtime_custom_shading_model_sources_compile_as_wgpu_modules --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-custom-shading-wgpu-nodefault-0701 --message-format short --color never -- --nocapture --test-threads=1` completed after dependency/test-binary build and passed 1/1 with repository warnings only. | Closes authored plugin Forward/GBuffer runtime source assembly -> WGPU shader-module validation for custom shading models. Still open: deferred lighting custom include WGPU render-pipeline/product evidence, RenderDoc/product capture, broader miss=0/product sweeps, default-feature rerun where Windows lib-test compile/link timed out, and full CI. |
| 2026-07-01 | Runtime shading-model include source handoff | render_plan08_shading_model_include_source_runtime_handoff_static_passed_cargo_deferred | Plan 08 project/plugin shading-model source set now reaches runtime SceneRenderer pipeline creation. `ResourceStreamer` exposes `shading_model_descriptor_for_pipeline_key(...)`, `shading_model_descriptors()`, and `shading_model_include_source_set()` from its live `ShadingModelRegistry` plus `ProjectAssetManager`. Forward/Base mesh template creation now routes plugin-range shading-model pipeline keys through `with_runtime_shading_model_sources(...)` and `MaterialShaderTemplateRequest::with_shading_model_forward_include_sources(...)`; deferred GBuffer cache creation takes the same streamer and feeds `DeferredGBufferShaderTemplateRequest::with_shading_model_gbuffer_include_sources(...)`. Deferred graph execution now carries the `ResourceStreamer` into `record_gbuffer_geometry(...)`, and deferred lighting pipeline creation assembles source through `ShadingModelIncludeSourceSet::from_project_asset_manager(...)`, plugin descriptors, and `DeferredLightingShaderSourceRequest::with_shading_model_deferred_include_sources(...)` instead of compiling the static lighting source only. Built-in shading models remain on the compiled built-in include path; the runtime source-set handoff is applied only for plugin-range descriptors. | Scoped `rustfmt --edition 2021 --check` passed on touched runtime, shader-cache, deferred, construction, registry, and structure-guard files. Focused `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-shading-runtime-handoff-0701 --message-format short --color never` passed after one earlier 304s timeout attempt; output had repository warnings only. Source/docs/session anchor scans, line-count scan, conflict/trailing-whitespace scan, and scoped diff-check were run for this slice after docs sync; diff-check only reports LF/CRLF line-ending warnings when present. | Closes the static/runtime handoff from plugin shading-model descriptors plus project/plugin Ready shader WGSL source set into Forward/Base, deferred GBuffer, and deferred lighting pipeline source assembly. Follow-up now narrows to deferred lighting custom include WGPU render-pipeline/product evidence, product/RenderDoc capture, broader miss=0/product sweep, focused WGPU second-launch reruns where earlier commands timed out, and full CI. |
| 2026-07-01 | Custom shading-model product material-pass staged cache WGPU validation | render_plan08_custom_shading_model_product_material_pass_staged_cache_wgpu_passed_renderdoc_deferred | Plan 08 custom shading-model 现在从 project/plugin descriptor/source set 推进到产品材质多 pass staged-cache WGPU 路径。`graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/custom_shading_model.rs` 注册 `custom:toon` `ShadingModelDescriptor` 与 Ready Forward/GBuffer/deferred include WGSL，`dynamic_api::builtin_standard_material_shader_prewarm_manifest_for_geometry_with_plugin_shading_models(...)` 生成带插件 include source/hash 的六 pass manifest，`prewarm_shader_variants_with_wgpu_pipeline_validation(...)` 验证并写入 staged fallback cache，随后 `submit_registry_material_passes_with_plugin_shading_model(...)` 通过 product `WgpuRenderFramework`、`lighting_model = "custom:toon"`、deferred GBuffer/lighting/post-process/TAA/Velocity 产品链消费该 staged cache。运行时 first-frame 和 Velocity-frame miss report 均要求 custom shading-model 维度、disk hit、compile miss=0、disk write=0。为修复产品链中的真实缺口，Lighting stage 现在通过 `with_deferred_lighting_renderer(...)` 只携带 deferred resources + mesh draw lists，不再强制要求 GBuffer 专用 streamer；GBuffer 的 `with_deferred_renderer(deferred, streamer, mesh_draw_lists)` streamer 合同保持不变。 | 验证已通过：`cargo test -p zircon_runtime --lib render_product_custom_shading_model_registry_material_passes_use_staged_prewarm_without_compile_miss --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-custom-shading-product-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1；标准回归 `cargo test -p zircon_runtime --lib render_product_project_plugin_registry_material_passes_use_staged_prewarm_without_compile_miss --no-default-features --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-custom-shading-product-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1；`rustfmt --edition 2021 --check` 已覆盖本切片触达 Rust 文件。两条 Cargo run 均仅有仓库既有 warnings。 | 关闭 custom shading-model descriptor/source -> prewarm WGPU pipeline validation -> product material-pass staged-cache miss=0 的 focused 产品路径。仍需 RenderDoc/product capture、custom shading-model second-launch 产品复跑、broader product sweep、默认 features/full CI。 |
| 2026-07-01 | Project/plugin shading-model include source set | render_plan08_shading_model_include_source_set_static_passed_cargo_deferred | Plan 08 项目/插件 custom shading-model WGSL source export 现在有 source-set 桥接合同。`ShadingModelIncludeSourceSet::from_project_asset_manager(...)` 从 `ProjectAssetManager` 的 Ready `ResourceKind::Shader` records 解析 plugin `ShadingModelDescriptor` 的 `forward_include`、`gbuffer_encode_include` 与 `deferred_include`；built-in descriptor 被跳过，include token 支持带 `.wgsl` 与不带扩展名的 locator 后缀匹配，缺失、重复、无 runtime WGSL 与加载失败分别返回 `MissingInclude`、`DuplicateIncludeToken`、`MissingRuntimeSource` 与 `LoadShader`。`MaterialShaderTemplateRequest::with_shading_model_forward_include_sources(...)`、`DeferredGBufferShaderTemplateRequest::with_shading_model_gbuffer_include_sources(...)` 与 `DeferredLightingShaderSourceRequest::with_shading_model_deferred_include_sources(...)` 现在可消费同一 source set。 | 新增 TDD source tests 覆盖 ready shader export、Forward/GBuffer 模板请求喂入、无扩展名 token 匹配、built-in descriptor 跳过、missing include 与 duplicate include 诊断。静态验证已通过：`rustfmt --edition 2021 --check` 覆盖 `include_sources.rs`、`material/mod.rs`、`shading_models/mod.rs`、`assemble.rs`、`deferred_gbuffer.rs`、`lighting_pipeline/shader_source.rs` 与结构守卫；source/docs/session/per-doc guard anchors、行数、conflict/trailing-whitespace 与 scoped diff-check 均通过，diff-check 仅报告 LF/CRLF 提示。外部 Cargo lanes 仍 active，本切片未启动 focused Cargo/Naga/WGPU；后续 direct-binary backfill row 记录 `exported_include_source_set_feeds_forward_and_gbuffer_template_requests` 1/1 通过。 | 关闭 project/plugin ready shader WGSL -> Forward/GBuffer/deferred request source set 的静态桥接，并由 direct-binary backfill 补齐其中 focused guard 证据。仍需把 SceneRenderer/deferred runtime pipeline creation 接到 descriptor/source 请求、prewarm/project asset scan handoff、真实 custom shading-model Naga/WGPU compile、Cargo-wrapper validation、RenderDoc/product capture、broader product sweep 与 full CI。 |
| 2026-07-01 | Deferred lighting include source dispatch | render_plan08_deferred_lighting_include_source_dispatch_static_passed_cargo_deferred | Plan 08 `ShadingModelDescriptor.deferred_include` 现在有 deferred lighting request-level source assembly 合同。`graphics/scene/scene_renderer/deferred/lighting_pipeline/shader_source.rs` 新增 `DeferredLightingShaderSourceRequest::with_shading_model_descriptor(...)` 与 `with_shading_model_deferred_include_source(...)`，`assemble_deferred_lighting_shader_source(...)` 先拼接 GPUScene/light-grid/shadow 与内建 deferred lighting include，再按 descriptor `deferred_include` 注入请求携带的插件 WGSL 源，未知 token 返回 `UnknownDeferredInclude`。`deferred_lighting.wgsl` 保留 lighting 主体和内建 dispatch，但 StandardPBR、BlinnPhong、Unlit 的 leaf 函数迁入 `zr_shade_deferred_{standard_pbr,blinn_phong,unlit}.wgsl`，并留下 `zr-deferred-lighting-custom-shading-model-dispatch` 插入点。新增 `CUSTOM_TOON_DEFERRED_INCLUDE`、`deferred_lighting_shader_rejects_unknown_shading_model_deferred_include` 与 `deferred_lighting_shader_uses_custom_shading_model_deferred_include_source`，锁定 custom `zr_shade_deferred_toon.wgsl` 注入、unknown token 失败和 Naga validation 调用。 | 按 milestone cadence 先补测试源码再实现；静态验证已通过：`rustfmt --edition 2021 --check` 覆盖 `lighting_pipeline/shader_source.rs`、`lighting_pipeline/tests.rs`、`pipeline_compile/dynamic_resolution.rs` 与 `render_shader_template_assembly` guard child；source-anchor、docs/session anchor、line-budget、conflict/trailing-whitespace 与 scoped diff-check 均通过，diff-check 仅报告 LF/CRLF 提示。外部 cargo/rustc lanes 仍 active，本切片不启动新的 Cargo/WGPU；新增 Rust tests、Naga/WGPU 与 focused Cargo 结果不声明通过。 | 关闭“请求级插件 WGSL 文本 -> deferred lighting `deferred_include`”静态合同。仍需把自动 project/plugin WGSL source export 接到该请求源、把运行时 deferred lighting pipeline 创建接入 descriptor/source 请求、真实 custom shading-model Naga/WGPU compile、focused Cargo、project/plugin material-pass second-launch WGPU rerun、RenderDoc/product capture、broader product sweep 与 full CI。 |
| 2026-07-01 | Deferred GBuffer shading include source dispatch | render_plan08_deferred_gbuffer_shading_include_source_static_passed_cargo_deferred | Plan 08 `ShadingModelDescriptor.gbuffer_encode_include` 现在进入 deferred GBuffer template assembly 的 include 选择合同。`DeferredGBufferShaderTemplateRequest` 新增可选 `with_shading_model_descriptor(...)` 与请求级 `with_shading_model_gbuffer_include_source(...)`；`graphics/shader/template/include_registry.rs::shading_model_gbuffer_include_for(...)` 先解析内建 `zr_gbuffer_encode_standard_pbr.wgsl`，再按 descriptor token 查找请求携带的插件 WGSL 源。`zr_template_deferred_gbuffer.wgsl` 保留三目标输出、alpha clip 与 material flag helper，但 fragment entry 现在调用 `encode_gbuffer(surface, zr_build_shading_context(input))`；标准 PBR albedo/normal/material 打包迁到 `zr_gbuffer_encode_standard_pbr.wgsl`。新增 `CUSTOM_TOON_GBUFFER_INCLUDE`、`render_deferred_gbuffer_template_rejects_unknown_shading_model_gbuffer_include` 与 `render_deferred_gbuffer_template_uses_custom_shading_model_gbuffer_include_source`，锁定 unknown token 报错和 custom GBuffer include 注入。 | 按 milestone cadence 先补测试源码再实现；静态验证已通过：`rustfmt --edition 2021 --check` 覆盖 `deferred_gbuffer.rs`、`include_registry.rs`、`tests.rs`、`shader_source.rs` 及 `render_shader_template_assembly` 三个 guard child；source-anchor scan 9 files、docs/session anchor scan 8 files、line-budget scan（118/199/782/729/706/149/215 lines）、conflict/trailing-whitespace scan 17 files 与 scoped `git diff --check` 均通过，diff-check 仅报告 LF/CRLF 提示。外部 cargo/rustc lanes 仍 active（6 个 cargo/rustc 进程），本切片不启动新的 Cargo/WGPU；新增 Rust test、Naga/WGPU 与 focused Cargo 结果不声明通过。 | 关闭“请求级插件 WGSL 文本 -> deferred GBuffer encode include”静态合同。仍需把自动 project/plugin WGSL source export 接到该请求源、deferred lighting `deferred_include` dispatch、真实 custom shading-model Naga/WGPU compile、focused Cargo、project/plugin material-pass second-launch WGPU rerun、RenderDoc/product capture、broader product sweep 与 full CI。 |
| 2026-07-01 | Custom forward shading include source dispatch | render_plan08_custom_forward_shading_include_source_static_passed_cargo_deferred | Plan 08 `ShadingModelDescriptor.forward_include` 现在可以解析到请求携带的插件 WGSL 文本，而不只限于内建 `zr_shading_standard_pbr.wgsl`。`ShaderTemplateInclude` 改为持有动态 `String` token/source，`ShaderTemplateIncludeRegistry` 用 `HashSet<String>` 去重并继续输出 include content hashes；`MaterialShaderTemplateRequest::with_shading_model_forward_include_source(...)` 让调用方把插件导出的 forward include 源交给模板 assembly；`shading_model_forward_include_for(..., source_includes)` 先解析内建 include，再按 descriptor token 查找请求源。新增 `CUSTOM_TOON_FORWARD_INCLUDE` 与 `render_shader_template_uses_custom_shading_model_forward_include_source`，验证 `zr_shading_toon.wgsl` 被注入、标准 PBR include 不被注入，并在测试体中保留 Naga validation 调用。 | 按 milestone cadence 先补测试源码再实现；`rustfmt --edition 2021 --check` 已覆盖 `assemble.rs`、`include_registry.rs`、`tests.rs`、`assembly_assertions.rs` 与 `docs_anchors.rs`；source-anchor scan 5 files、docs-anchor scan 8 docs x 4 anchors、line-budget scan（226/142/724/682/142 lines）、conflict-marker scan、trailing-whitespace scan 与 scoped `git diff --check` 均通过，diff-check 仅报告 LF/CRLF 提示。外部 cargo/rustc lanes 仍 active（editor/runtime text/WGPU 相关 build/test），本切片不启动新的 Cargo/WGPU；新增 Rust test、Naga/WGPU 与 focused Cargo 结果不声明通过。 | 关闭“请求级插件 WGSL 文本 -> Forward shading include”静态合同。仍需把自动 project/plugin WGSL source export 接到该请求源、GBuffer/deferred descriptor include dispatch、真实 custom shading-model Naga/WGPU compile、focused Cargo、project/plugin material-pass second-launch WGPU rerun、RenderDoc/product capture、broader product sweep 与 full CI。 |
| 2026-07-01 | Descriptor-driven forward shading include dispatch | render_plan08_descriptor_driven_forward_shading_include_dispatch_static_passed_cargo_deferred | Plan 08 `ShadingModelDescriptor.forward_include` 现在进入 Forward template assembly 的 include 选择合同。`MaterialShaderTemplateRequest` 新增可选 `with_shading_model_descriptor(...)`，`graphics/shader/template/include_registry.rs::shading_model_forward_include_for(...)` 将 descriptor token 解析到已编译进 runtime 的 `zr_shading_standard_pbr.wgsl`，`assemble_material_shader_template(...)` 在 Forward pass 需要 shading include 时走该 descriptor-driven resolver。未知插件 include token 现在返回 `ShaderTemplateAssemblyError::UnknownShadingInclude`，不再有静默 StandardPBR 回退。新增 `render_shader_template_uses_shading_model_descriptor_forward_include` 与 `render_shader_template_rejects_unknown_shading_model_forward_include`，`runtime_15_render_shader_template_assembly_is_folder_backed` 同步锁定 request 字段、include resolver、错误分支、测试名、文档状态和会话锚点。 | 按 zircon milestone cadence 先补测试源码，再实现 owner 内最小合同；外部 editor `paint_text` cargo/rustc lane 仍在编译，本切片不启动新的 Cargo/WGPU。`rustfmt --edition 2021 --check` 已覆盖 `assemble.rs`、`include_registry.rs`、`tests.rs`、`assembly_assertions.rs` 与 `docs_anchors.rs`；source-anchor scan 5 files、docs-anchor scan 7 docs x 7 anchors、line-budget scan、conflict-marker scan、trailing-whitespace scan 与 scoped `git diff --check` 均通过，diff-check 仅报告 LF/CRLF 提示。尚未声明新增 Rust tests 的 Cargo/Naga/WGPU 结果。 | 关闭 descriptor -> Forward shading include resolver 的静态模板合同。仍需把插件 WGSL include 文本/export 接入 registry、GBuffer/deferred descriptor include dispatch、custom shading model Naga/WGPU compile、focused Cargo、project/plugin material-pass second-launch WGPU rerun、RenderDoc/product capture、broader product sweep 与 full CI。 |
| 2026-07-01 | Plugin shading-model descriptor registry export | render_plan08_plugin_shading_model_descriptor_registry_export_static_passed_cargo_deferred | Plan 08 `ShadingModelDescriptor` 现在从 selected plugin discovery 进入 generated shader permutation registry 与 prewarm overlay。`tools/zircon_build.py::PluginPackage.shader_shading_model_descriptors` 保存插件 `[[shading_models]]` descriptor，`tools/zircon_build_shader_prewarm.py::generated_shader_permutation_registry_document(...)` 在 `shading_model_ids` 旁写出 `shading_model_descriptors`，`zircon_shader_prewarm/manifest/permutation_registry.rs::ShaderPrewarmPermutationRegistryOverlay` 读取、规范化并合并 `ShadingModelDescriptor`，同 id 不兼容 descriptor 通过 `IncompatibleShadingModelDescriptor` 报错。新增 `test_generated_shader_permutation_registry_document_exports_selected_plugin_shading_model_descriptors` 与 `shader_prewarm_permutation_registry_merges_custom_shading_model_descriptors`，`runtime_15_shader_prewarm_plugin_shading_model_descriptor_registration_is_wired` 同步锁定 build helper、generated registry、overlay、run/error owners、文档状态和会话锚点。 | TDD 红跑 `test_generated_shader_permutation_registry_document_exports_selected_plugin_shading_model_descriptors` 先失败于 generated registry 缺 `shading_model_descriptors`；实现后该 focused test 与 4 个相关 prewarm registry tests 通过。组合 Python `tools.tests.test_zircon_build_plugin_carriers tools.tests.test_zircon_build_shader_prewarm` 通过 41 tests；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_plugin_carriers.py` 通过；scoped rustfmt 已覆盖 `error.rs`、`permutation_registry.rs` 与 `run.rs`。Cargo/WGPU 暂缓，因为外部 cargo/rustc lane 仍在编译 runtime-text/WGPU target；不声明 `shader_prewarm_permutation_registry_merges_custom_shading_model_descriptors` 的 Cargo 结果，也不声明真实 Naga/WGPU/custom shading include 编译通过。 | 关闭 selected plugin `shading_model_descriptors` -> generated registry -> prewarm overlay 的静态/行为合同。仍需 descriptor-driven shader template/include dispatch、custom shading model Naga/WGPU compile、focused Cargo、project/plugin material-pass second-launch WGPU rerun、RenderDoc/product capture、broader product sweep 与 full CI。 |
| 2026-07-01 | Selected plugin shading-model registration inputs | render_plan08_selected_plugin_shading_model_registration_inputs_static_guard_cargo_deferred | Plan 08 F11 selected-plugin shading-model descriptor registry now has a registration-input behavior guard instead of relying only on downstream ResourceStreamer injection tests. New `zircon_runtime/src/builtin/runtime_modules/assembly/registration_inputs.rs::tests::plugin_registration_inputs_collect_shading_model_descriptors` builds a selected `RuntimePluginRegistrationReport`, registers a plugin `ShadingModelDescriptor` through `RuntimeExtensionRegistry::register_shading_model(...)`, calls `registration_inputs_for_plugin_reports(...)`, and asserts both `linked_plugin_ids()` and `RuntimeModuleRegistrationInputs::shading_models()` preserve the selected plugin descriptor. `runtime_15_material_custom_shading_model_runtime_registry_is_wired` now anchors that test beside `render_product_streamer_projects_plugin_custom_shading_model_into_pipeline_key`, so the Material custom shading-model runtime registry source/doc chain covers selected plugin registration inputs, runtime module assembly, GraphicsModule, WGPU renderer construction, and ResourceStreamer ownership. | Scoped static verification passed: `rustfmt --edition 2021 --check` on `registration_inputs.rs` and `material_custom_shading_model_runtime.rs`; source/docs anchor scans found the new test, status, selected-plugin registration handoff, and six required docs anchors; conflict-marker and trailing-whitespace scans were clean; scoped `git diff --check` returned only LF/CRLF warnings; line budgets are `registration_inputs.rs` 201 lines and guard 134 lines. Focused Cargo was deferred because unrelated active cargo/rustc lanes were compiling runtime-text targets;后续 direct-binary backfill row 记录 `plugin_registration_inputs_collect_shading_model_descriptors` 1/1 通过。 | Closes the source-level selected plugin descriptor -> `RuntimeModuleRegistrationInputs::shading_models()` handoff and focused direct-binary evidence. Still open: Cargo-wrapper validation, broader WGPU second-launch acceptance, true Naga/WGPU/product/RenderDoc gates, and full CI. |
| 2026-07-01 | Edited shader ResourceRecord revision export | render_plan08_edited_shader_revision_export_static_guard_cargo_deferred | Plan 08 MS-M4 now has an asset-pipeline guard for the remaining edited-shader revision export seam. `zircon_runtime/src/asset/tests/pipeline/manager/resource_revisions.rs::shader_reimport_exports_updated_revision_for_prewarm_registry` opens a project, records the initial Ready Shader revision for `res://shaders/pbr.wgsl`, edits the WGSL source, reimports that shader through `ProjectAssetManager::import_asset(...)`, and asserts that the shader stays `ResourceState::Ready`, receives a higher revision, and is exported through `manager.resource_manager().ready_records_for_kind(ResourceKind::Shader)` with the same `ResourceId` and updated revision. This links the project asset edit/reimport path to the live `ResourceRecord.revision` stream consumed by the prewarm resource registry overlay. | Scoped verification passed: `rustfmt --edition 2021 --check zircon_runtime/src/asset/tests/pipeline/manager/resource_revisions.rs`; source/docs anchor scan found the new test, status anchor, `ready_records_for_kind(ResourceKind::Shader)`, `ProjectAssetManager::import_asset`, and `res://shaders/pbr.wgsl`; conflict-marker and trailing-whitespace scans were clean; scoped `git diff --check` returned only LF/CRLF warnings; line budget is 167 lines. Focused Cargo was deferred because unrelated active cargo/rustc lanes were already compiling;后续 direct-binary backfill row 记录 `shader_reimport_exports_updated_revision_for_prewarm_registry` 1/1 通过。 | Closes the source-level project shader edit/reimport -> Ready Shader record export contract and focused direct-binary evidence. Still open: Cargo-wrapper validation, broader WGPU second-launch acceptance, RenderDoc/product capture, and full CI. |
| 2026-07-01 | Project/plugin registry material-pass second-launch miss=0 | render_plan08_project_plugin_registry_material_passes_second_launch_static_passed_wgpu_timeout_no_result | Plan 08 MS-M4 now has an explicit second-launch product acceptance owner for the project/plugin registry material-pass staged-cache path. New `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/second_launch.rs` keeps the broader launch-cycle proof out of the folder root and reuses the existing registry cases, six-pass manifest, product fixture, and miss-report assertions. The test writes the staged manifest once, then for each project/plugin registry shader creates two fresh `WgpuRenderFramework` launch cycles against the same runtime cache root and staged fallback root. Each launch must cover first-frame DepthPrepass/GBuffer/Shadow/TAA reactive requests and second-frame Velocity requests with compile miss=0, and `assert_runtime_shader_cache_root_empty(...)` requires the runtime cache root stays empty so the second product launch still comes from staged fallback rather than a first-launch write. | Static/scoped verification passed: `rustfmt --edition 2021 --check` on `mod.rs`, `second_launch.rs`, and `shader_prewarm_project_plugin_registry_material_passes_staged_cache.rs`; source-anchor scan found the new test, `mod second_launch`, status anchor, and runtime-cache-empty assertion; line budgets are `second_launch.rs` 137 lines and guard 166 lines. Focused WGPU product command `cargo test -p zircon_runtime render_product_project_plugin_registry_material_passes_second_launch_use_staged_prewarm_without_compile_miss --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-registry-second-launch-0701 --message-format short --color never -- --nocapture --test-threads=1` timed out after about 1204s without a test result or reusable `zircon_runtime` test binary; target-dir cargo/rustc processes were stopped, so no WGPU pass is claimed. A short rerun reusing `E:\cargo-targets\zircon-plan08-support-repair-0630` was also stopped at 2026-07-01 04:56 +08:00 while still compiling dependencies, with no stdout test result. A second bounded rerun on `E:\cargo-targets\zircon-plan08-registry-second-launch-0701` was stopped at 2026-07-01 05:03 +08:00 while still compiling/linking `zircon_runtime`; stderr only had existing warning output and stdout had no test result. A final bounded rerun launched from `target\codex-plan08-rerun-0701-final` against the same target dir was stopped at 2026-07-01 05:15 +08:00 while still compiling/linking `zircon_runtime`; no `zircon_runtime` test binary was produced, stdout stayed empty, and stderr only had existing warning output. Later direct-binary backfill closed the focused WGPU result under `render_plan08_project_plugin_registry_material_passes_second_launch_direct_binary_wgpu_passed_renderdoc_deferred`. | Advances broader second-launch acceptance by adding the product contract and structure guard. Direct-binary WGPU is now covered by the later backfill row; still open: Cargo-wrapper rerun if required, RenderDoc/product capture, broader product gates outside the current filters, and full CI. |
| 2026-07-01 | Skinned morph weight velocity product readback | render_plan08_skinned_morph_weight_velocity_product_wgpu_passed_renderdoc_deferred | Plan 08 MS-M2 skinned GPU morph velocity now has focused WGPU product readback evidence for the same zero-start morph change. The new `graphics/tests/render_product_mesh_cache/morph/skinned_velocity.rs` child keeps skinned velocity-only coverage out of the already large morph product owner. The test registers an unlit material, a skinned mesh with one morph target, a skeleton asset, and a pose sideband, then submits two `WgpuRenderFramework` frames with the same stable instance key: frame 1 stages `[0.0]`, frame 2 stages `[1.0]`. The second frame must select the GPU SkinnedMorphed source, keep shader skinning active, emit a previous velocity transform, report no missing velocity transform, and write nonzero `scene-velocity` pixels through the forward-plus/Core3d WGPU product path. | Scoped verification passed: `rustfmt --edition 2021 --check zircon_runtime\src\graphics\tests\render_product_mesh_cache\morph.rs zircon_runtime\src\graphics\tests\render_product_mesh_cache\morph\skinned_velocity.rs`; focused WGPU product test `cargo test -p zircon_runtime render_product_skinned_mesh_morph_weight_change_writes_scene_velocity_pixels --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-velocity-product-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 (`5705` filtered out) with repository warnings only. | Closes skinned morph velocity readback parity for the focused 0 -> 1 morph-weight product case. Still open: RenderDoc/product capture, broader product miss=0/second-launch acceptance, and full CI. |
| 2026-07-01 | Direct morph weight velocity product readback | render_plan08_direct_morph_weight_velocity_product_wgpu_passed_renderdoc_deferred | Plan 08 MS-M2 direct mesh GPU morph velocity now has focused WGPU product readback evidence for a zero-start weight change. `PendingMeshDraw.source_morph_weights` carries source morph weights independently from the active GPU morph payload, so a direct mesh with morph targets can stage explicit nonempty all-zero weights on frame 1. `GpuScene::stage_current_morph_weights(...)` now keeps those explicit zero rows instead of treating them as missing, and `extend_pending_draws_for_mesh_instance.rs` stages direct mesh source weights for both dynamic and prepared direct mesh paths while model/skinned-model paths continue to avoid unrelated staging. The next frame's 0.0 -> 1.0 weight change therefore reads previous `[0.0]`, selects the GPU Morphed source, emits a previous velocity transform, and writes nonzero `scene-velocity` pixels through the real forward-plus/Core3d WGPU product path. | Scoped verification passed: `rustfmt --edition 2021 --check` on the touched Rust files; `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-velocity-product-0701 --message-format short --color never` passed with repository warnings only; focused WGPU product test `cargo test -p zircon_runtime render_product_direct_mesh_morph_weight_change_writes_scene_velocity_pixels --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-velocity-product-0701 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 (`5701` filtered out). `git diff --check` on tracked touched files returned only LF/CRLF warnings. | Closes the focused direct non-skinned morph 0 -> 1 scene-velocity product readback gate. The skinned counterpart is closed by `render_plan08_skinned_morph_weight_velocity_product_wgpu_passed_renderdoc_deferred`. Still open: RenderDoc/product capture, broader product miss=0/second-launch acceptance, and full CI. |
| 2026-07-01 | Morph previous weights velocity | render_plan08_morph_previous_weights_velocity_check_passed_product_deferred | Plan 08 MS-M2 now has a code path for GPU-morphed velocity previous weights. `gpu_scene/prev_morph_weights.rs` owns current/previous morph-weight maps keyed by stable mesh instance key; draw sync stages current source weights; both compiled-scene and legacy successful-submit paths roll the current map into previous state after `queue.submit(...)`. `PendingMorphPayload` now carries current source weights plus previous weight rows without changing `GpuMorphPayload` ABI: the current block remains at `weight_base`, and the previous block starts at `weight_base + target_count`. `morph_payload_upload.rs` keeps previous-only active targets so current-zero/previous-nonzero transitions still emit a velocity-capable payload, and direct mesh previous-only draws route through the GPU Morphed source rather than a CPU-baked fallback. `zr_geometry_morphed.wgsl` and `zr_geometry_skinned_morphed.wgsl` now use `zr_morph_previous_weight(...)` in `fetch_prev_position`, with skinned-morphed previous position applying previous skinning to the previous morphed position. | Scoped verification passed: `rustfmt --edition 2021 --check` on the touched Rust files; `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-prev-0701 --message-format short --color never` passed with the repository warning set; source-anchor scan found `previous_morph_weights`, `stage_current_morph_weights`, `roll_prev_morph_weights_after_success`, `previous_weights`, and `payload.y + payload.w`; scoped `git diff --check` returned only LF/CRLF warnings. Focused `cargo test -p zircon_runtime --lib morph_payload ...` is not counted: one attempt was blocked by unrelated dirty-state status-doc test files, and the rerun timed out after about 606s during Windows lib-test compile/link with no Rust diagnostics; matching target-dir cargo/rustc processes were stopped and no reusable test binary was produced. | Closes the code-level previous morph-weight data path for velocity shader consumption only. Still open: RenderDoc/product velocity parity capture, broader product miss=0/second-launch acceptance, and full CI. |
| 2026-06-30 | Skinned morph GPU-vs-CPU product parity | render_plan08_skinned_morph_gpu_cpu_product_parity_wgpu_passed_renderdoc_deferred | Plan 08 MS-M2 skinned morph output now has product readback parity. `graphics/tests/render_product_mesh_cache/morph.rs::render_product_skinned_mesh_gpu_morph_matches_cpu_baked_reference_pixels` renders two independent `WgpuRenderFramework` offscreen frames with the same unlit colored triangle, skeleton asset, and posed skeletal sideband. The GPU path uses active morph weights on a skinned mesh and must report `last_mesh_gpu_skinned_morphed_source_draw_count == 1` plus `last_mesh_skinned_gpu_skinning_draw_count == 1`; the CPU reference pre-bakes the same morph deltas into the skinned mesh positions, submits no active morph weights, still exercises shader skinning, and must report zero skinned-morphed source draws. The captured primary-surface RGBA frames are compared with the same tolerance used by direct morph parity. | Scoped verification passed: `rustfmt --edition 2021 --check --config skip_children=true zircon_runtime\src\graphics\tests\render_product_mesh_cache\morph.rs`; focused WGPU product test `cargo test -p zircon_runtime render_product_skinned_mesh_gpu_morph_matches_cpu_baked_reference_pixels --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-parity-0630 --message-format short --color never -- --nocapture --test-threads=1` passed 1/1 with repository warnings only. The existing direct morph product regressions were rerun from the generated `zircon_runtime` test binary with filter `render_product_direct_mesh_` and passed 2/2 after a redundant Cargo relink attempt was stopped. | Closes skinned GPU-vs-CPU morph pixel/readback parity and keeps direct source/parity regressions green. Still open: previous morph weights for velocity if product acceptance requires morph velocity parity, RenderDoc/product capture, broader product miss=0/second-launch acceptance, and full CI. |
| 2026-06-30 | Morph GPU-vs-CPU product parity | render_plan08_morph_gpu_cpu_product_parity_wgpu_passed_renderdoc_deferred | Plan 08 MS-M2 direct non-skinned morph output now has product readback parity. `graphics/tests/render_product_mesh_cache/morph.rs::render_product_direct_mesh_gpu_morph_matches_cpu_baked_reference_pixels` renders the same unlit colored triangle through two independent `WgpuRenderFramework` offscreen frames: the GPU path registers a `MeshAsset` morph target with active weights and must report `last_mesh_gpu_morphed_source_draw_count == 1`, while the CPU reference registers positions pre-baked with the same deltas and no active weights and must report zero GPU-morphed source draws. The captured primary-surface RGBA frames are compared with tight per-channel tolerance and a small edge-pixel allowance. | Scoped verification passed: `rustfmt --edition 2021 --check --config skip_children=true zircon_runtime\src\graphics\tests\render_product_mesh_cache\morph.rs`; focused WGPU product filter `cargo test -p zircon_runtime render_product_direct_mesh_ --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-parity-0630 --message-format short --color never -- --nocapture --test-threads=1` passed 2/2, covering both `render_product_direct_mesh_active_morph_weights_use_gpu_morphed_source` and `render_product_direct_mesh_gpu_morph_matches_cpu_baked_reference_pixels`, with repository warnings only. | Closes direct non-skinned GPU-vs-CPU morph pixel/readback parity and keeps the previous source-selection guard covered. The skinned counterpart is closed by `render_plan08_skinned_morph_gpu_cpu_product_parity_wgpu_passed_renderdoc_deferred`. Still open: previous morph weights for velocity, RenderDoc/product capture, broader product miss=0/second-launch acceptance, and full CI. |
| 2026-06-30 | Morph GPU-source product guard | render_plan08_morph_gpu_source_product_guard_wgpu_passed_renderdoc_deferred | Plan 08 MS-M2 direct mesh active morph weights now have public/product observability. `PreparedMeshQueueStats` records `gpu_morphed_source_draw_count` and `gpu_skinned_morphed_source_draw_count`, `RenderStats` exposes `last_mesh_gpu_morphed_source_draw_count` and `last_mesh_gpu_skinned_morphed_source_draw_count`, and DiagnosticStore mirrors them as `render.mesh.queue.gpu_morphed_source_draw_count` and `render.mesh.queue.gpu_skinned_morphed_source_draw_count`. The new product child `graphics/tests/render_product_mesh_cache/morph.rs` registers a direct `MeshAsset` with active morph weights, submits through `WgpuRenderFramework`, and proves the draw selects the GPU Morphed source instead of a CPU-baked fallback. | Scoped verification passed: `rustfmt --edition 2021 --check --config skip_children=true` on the touched Rust files; `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-check-0630 --message-format short --color never`; focused `prepared_queue_stats_count_gpu_morphed_sources_as_dynamic_geometry` 1/1; focused `render_product_diagnostics_record_skinned_mesh_queue_count` 1/1; focused `render_product_direct_mesh_active_morph_weights_use_gpu_morphed_source` 1/1. Scoped `git diff --check` returned only LF/CRLF warnings. | Closes product source-selection observability for the direct non-skinned morph path only. Direct and skinned pixel parity are closed by later status rows. Still open: previous morph weights for velocity, RenderDoc/product capture, broader product miss=0/second-launch acceptance, and full CI. |
| 2026-06-30 | Project/plugin registry product/material-pass staged-cache WGPU closeout | render_plan08_project_plugin_registry_product_material_passes_staged_cache_wgpu_passed_renderdoc_deferred | Plan 08 MS-M4 now has focused product WGPU evidence for both the raw project/plugin registry material path and the full material-pass staged-cache path. The offscreen device-limit synthesis now requests the full HZB occlusion capacity when the adapter exposes it: 9 GPUScene compute-visible storage buffers plus 6 HZB pass storage buffers, while still falling back to the 12-slot mesh-forward requirement on lower-capacity adapters. Temporal descriptor filtering now declares `TaaSceneColor` history only when the effective post-process stack contains TAA resolve, and camera descriptor selection preserves an explicit `AntiAliasSettings::taa()` override instead of rewriting it back to Auto/FXAA. Together these fixes keep the material-pass product frame on `temporal.velocity-object`, allow the Velocity runtime cache dimension to hit staged cache, and keep TAA history resources absent when TAA is disabled. | Scoped verification passed: `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-support-repair-0630 --message-format short --color never`; scoped `rustfmt --edition 2021 --check --config skip_children=true` over the touched render/core/text support files; focused `offscreen_device_limits` 3/3; `hzb_occlusion_limit_gate` 2/2; `taa_resolve_` 10/10; `render_gpu_scene_bind_group_layout_reserves_storage_and_palette_bindings` 1/1; `render_view_select_camera_descriptor_preserves_explicit_anti_alias` 1/1; `render_product_project_plugin_registry_materials_use_staged_prewarm_without_compile_miss` 1/1; and `render_product_project_plugin_registry_material_passes_use_staged_prewarm_without_compile_miss` 1/1. A later duplicate material-pass rerun was inconclusive while Cargo rebuilt/locked the target and is not counted as a regression; the earlier focused WGPU product pass is the counted evidence. | Closes the focused project/plugin registry raw product and material-pass staged-cache WGPU miss=0 gates. Still open: RenderDoc/product capture, broader product sweep/second-launch acceptance, and full workspace CI. |
| 2026-06-30 | Project/plugin registry production wrapper no-proxy WGPU run | render_plan08_project_plugin_registry_production_wrapper_no_proxy_wgpu_passed_product_renderdoc_deferred | Plan 08 MS-M4 now has a real public `zircon_build.py` no-proxy staged run for project + selected-plugin shader registry export. `BuildConfig.runtime_preview_feature_arg` keeps the runtime preview executable on `target-client` while the runtime library and `zircon_shader_prewarm` use the requested `target-server` runtime feature set, so `--runtime-features target-server` no longer breaks `zircon_app --bin zircon_runtime`. The public command stages runtime artifacts, engine assets, project shader `.zmeta`, and `native_dynamic_fixture` shader assets, exports `shader_resource_records.json`, then consumes those Ready records in the same WGPU module-validation prewarm run. | Regression `test_runtime_server_wrapper_uses_client_features_for_preview_binary` first failed on the old preview `target-server` command and now passes; `python -m py_compile tools\zircon_build.py tools\tests\test_zircon_build_shader_prewarm_wrapper_orchestration.py` passed. Real no-proxy command `python -u tools\zircon_build.py --targets runtime --plugins native_dynamic_fixture --out target\codex-plan08-wrapper-no-proxy-0630 --mode debug --runtime-features target-server --jobs 1 --prewarm-shaders --validate-wgpu-shaders --shader-asset-root target\codex-plan08-wrapper-no-proxy-0630\project_assets --shader-quality-tier medium --shader-geometry-source static` completed with runtime lib `target-server`, preview binary `target-client`, prewarm cargo run `target-server`, `requested=18/written=18/failed=0`, WGPU module validation `requested=18/validated=18/failed=0`, and Ready records for `res://project/shaders/project_shader` and `package://native_dynamic_fixture/shaders/shader`; hot no-proxy rerun exited 0 with the same 18/18 report. Structure guard `runtime_15_shader_prewarm_project_plugin_registry_wrapper_no_proxy_is_wired` passed via focused `cargo test -p zircon_runtime --lib --no-default-features --features target-server ... runtime_15_shader_prewarm_project_plugin_registry_wrapper_no_proxy_is_wired` (1/1). | Closes the public no-proxy staged build/prewarm/export wrapper gate. Still open: RenderDoc/product capture and broader product miss=0/second-launch product acceptance. This run uses WGPU module validation; the stricter render-pipeline validation flag remains covered by the separate 2026-06-30 pipeline-validation slice. |
| 2026-06-30 | Prewarm opt-in WGPU render-pipeline validation | render_plan08_prewarm_wgpu_render_pipeline_validation_gate_focused_tests_passed_product_deferred | Plan 08 MS-M4 now has an opt-in strict prewarm path that creates real WGPU render pipelines before writing staged shader cache entries. `zircon_shader_prewarm --validate-wgpu-pipelines` dispatches `dynamic_api::prewarm_shader_variants_with_wgpu_pipeline_validation(...)`; the dynamic API creates an offscreen backend and a mesh validation pipeline layout, while `mesh_pipeline_cache/prewarm_pipeline_validation.rs` creates Forward, GBuffer, DepthPrepass, Shadow, Velocity, and TAA reactive-mask pipelines from the prewarm WGSL. `ShaderVariantPrewarmReport.wgpu_pipeline_validation` records requested/validated/failed/skipped counts, and `tools/zircon_build.py --validate-wgpu-pipelines` forwards that stricter gate through staged prewarm acceptance. The focused WGPU run exposed that full mesh forward validation needs 12 storage-buffer slots per shader stage; `MESH_FORWARD_PIPELINE_REQUIRED_STORAGE_BUFFERS_PER_SHADER_STAGE` and the offscreen device limit request now cover that renderer layout requirement. | Python/tooling evidence passed for command forwarding, report summary, report contract, and acceptance wiring: `python -m py_compile ...` passed; `python -m unittest tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` passed 8 tests; focused pipeline summary/command/acceptance tests passed 3 tests. Runtime WGPU evidence passed by running the generated `zircon_runtime` lib-test binary filters directly: `mesh_prewarm_pipeline_validation` passed 2/2 real render-pipeline creation tests, and `wgpu_pipeline_validation` covers the two prewarm behavior tests plus structure guard. Cargo wrapper reruns for the same filters repeatedly timed out while rebuilding/relinking the large lib-test binary, so the wrapper timeout is not counted as a test failure. Structure guard: `runtime_15_shader_prewarm_wgpu_render_pipeline_validation_is_wired`; focused test anchor: `test_build_command_forwards_wgpu_shader_pipeline_validation`. | This closes the code, build-tool handoff, and focused runtime WGPU render-pipeline validation gate, not the broader Plan 08 product gates. Still open: RenderDoc/product capture and second-launch product miss=0. |
| 2026-06-30 | Morph geometry-source selection | render_plan08_morph_geometry_source_selection_static_passed_wgpu_deferred | Plan 08 MS-M2 now selects the production Morphed/SkinnedMorphed geometry sources when a draw has a morph payload slot and the source mesh has not already been CPU-morphed. `MeshDrawGeometrySource` gained `DynamicGpuMorphedSource` and `DynamicGpuSkinnedMorphedSource`, mapping to `GEOMETRY_SOURCE_ID_MORPHED_MESH` and `GEOMETRY_SOURCE_ID_SKINNED_MORPHED_MESH`. `build_mesh_draws/build/geometry_source_selection.rs` is the shared owner for final draw assembly, prepared-queue stats, and pending command-cache profile classification. Direct non-skinned morphs use `PendingMeshGeometry::GpuMorphed` with the original prepared mesh when payload upload succeeds; skinned direct morphs keep `PendingSkinnedGpuSource::Prepared` when a morph payload is available, so WGSL morphs first and GPU skinning follows. CPU-baked fallbacks still use `DynamicCpuMorphedSource` / `DynamicCpuMorphedGpuSkinningSource` and Static/Skinned shader IDs to avoid double morph. | Scoped verification passed: `rustfmt --edition 2021 --check` on touched Rust/guard files; `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-selection-0630-server --message-format short --color never`; focused `runtime_15_morph_geometry_source_selection_is_wired` 1/1; `queue_profile_maps_gpu_` 2/2; `gpu_morphed_geometry_uses_morphed_shader_source` 1/1; `prepared_skinned_gpu_source_with_morph_slot_uses_skinned_morphed_shader_source` 1/1; `mesh_pass_build_context_resolves_gpu_skinned_morphed_as_skinned_morphed_variant` 1/1; and `cpu_morphed` fallback filter 11/11. All Cargo runs used target-server and completed with the repository warning set. | Closes production geometry-source selection for payload-backed direct morph paths only. Remaining MS-M2 gates: GPU-vs-CPU morph/skinned product parity, previous morph weights for velocity if required, real WGPU/Product/RenderDoc capture, and broader shader-miss/product acceptance. |
| 2026-06-30 | Morph storage buffers upload | render_plan08_morph_storage_buffers_upload_check_passed_wgpu_deferred | Plan 08 MS-M2 now gives morph weights/target storage bindings 7/8 a real GPUScene owner without changing CPU-baked draw-source shader selection. `graphics/scene/gpu_scene/layout.rs` defines `GpuMorphDelta` and `GpuMorphWeight` ABI rows; `binding.rs` reserves group3 `GPU_SCENE_MORPH_DELTAS_BINDING = 7` and `GPU_SCENE_MORPH_WEIGHTS_BINDING = 8`; `gpu_scene.rs` carries fallback buffers and CPU shadows; `gpu_scene/morph.rs` owns `upload_morph_buffers(...)`, storage-buffer recreation, queue writes, and bind-group rebuild. `zr_gpu_scene.wgsl` now declares `zr_morph_deltas` / `zr_morph_weights` and guarded helper functions, while `zr_geometry_morphed.wgsl` and `zr_geometry_skinned_morphed.wgsl` consume those helpers instead of redeclaring bindings. | Scoped verification passed: rustfmt check covered the touched GPUScene Rust files and `runtime_15_morph_storage_buffers_upload_is_wired`; source/docs anchor scan covered ABI, bindings, upload owner, WGSL helper use, docs/status/session anchors, line budgets, conflict markers, and trailing whitespace; scoped diff-check returned only LF/CRLF warnings. `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-storage-0630 --message-format short --color never` passed with the repository warning set. WGPU/Product/RenderDoc remains deferred. | Closes the storage owner for MS-M2 morph bindings 7/8 only. Runtime selection for pure `GEOMETRY_SOURCE_ID_MORPHED_MESH` / `GEOMETRY_SOURCE_ID_SKINNED_MORPHED_MESH`, asset-to-GPU morph payload projection, product morph parity, RenderDoc/product capture, and broader miss=0 remain open. |
| 2026-06-30 | Morph payload projection | render_plan08_morph_payload_projection_check_passed_wgpu_deferred | Plan 08 MS-M2 now has the first direct mesh asset-to-GPUScene payload projection without switching production shader variants. `build_mesh_draws/build/morph_payload_upload.rs` reads active direct mesh morph weights, projects `MESH_ATTRIBUTE_POSITION` `Float32x3` target deltas into `GpuMorphDelta::position_xyz(...)` rows, writes row-aligned `GpuMorphWeight::new(...)` weights, deduplicates shared pending payloads by `Arc::as_ptr`, and calls `GpuScene::upload_morph_buffers(...)`. `PendingMeshDraw` now carries an optional shared `PendingMorphPayload`; direct CPU-morphed and CPU-morphed GPU-skinning paths attach that payload, while model/static prepared draws leave it `None`. Build aggregation adds `morph_upload_report.uploaded_bytes` to the existing GPUScene upload report. | Scoped verification passed: `rustfmt --edition 2021 --check` on touched mesh-build and guard files; source/docs anchor scan for projection, upload handoff, no-production-selection guard, docs/status/session anchors, line budgets, conflict markers, and trailing whitespace; scoped `git diff --check` with only LF/CRLF warnings; `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-payload-0630 --message-format short --color never` passed with the repository warning set. `runtime_15_morph_payload_projection_is_wired` locks this source/docs/status seam. Focused `cargo test -p zircon_runtime --lib morph_payload ...` timed out after 20 minutes with no test result; matching cargo/rustc processes for that target dir were stopped, so no focused lib-test pass is claimed. | Closes only the position-delta payload handoff from direct mesh morph assets into GPUScene morph storage. Normal/tangent/color morph attributes, per-instance payload slot/indexing ABI, and payload-backed Morphed/SkinnedMorphed selection are closed by later 2026-06-30 status anchors; GPU-vs-CPU product morph parity, RenderDoc/product capture, and broader miss=0 remain open. |
| 2026-06-30 | Morph payload slot indexing | render_plan08_morph_payload_slot_indexing_check_passed_wgpu_deferred | Plan 08 MS-M2 now connects morph payload metadata to draw slots and shader indexing without switching production shader selection. `GpuMorphPayload` defines the binding 11 header rows; `GpuInstanceData.morph_payload_slot` carries a per-instance header index separate from VirtualGeometry `payload_slot`; `morph_payload_upload.rs` projects active position/normal/tangent/color morph rows, emits payload headers, deduplicates shared pending payloads, and writes `PendingMeshDraw.morph_payload_slot`; GPUScene sync copies that slot into instance data. `zr_surface_types.wgsl` carries `@builtin(vertex_index)`, velocity templates preserve it, and morphed/skinned-morphed WGSL uses `instance.morph_payload_slot`, `vertex_index`, target count, and row constants to fetch position/normal/tangent/color deltas through `zr_morph_payloads`/`zr_morph_deltas`/`zr_morph_weights`. | Scoped verification passed: `rustfmt --edition 2021 --check` on the touched slice files; `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-slot-0630-server --message-format short --color never` passed with repository warnings; focused `cargo test -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-morph-slot-0630-server render_shader_template_validates_morphed_geometry_sources_with_payload_slots -- --nocapture --test-threads=1` passed 1/1. Default-feature `cargo check -p zircon_runtime --lib --locked ...` is blocked by unrelated UI dirty-state errors around private `resolve_style` re-export/import, so no default-feature check pass is claimed. `runtime_15_morph_payload_slot_indexing_is_wired` locks the ABI/binding/WGSL/docs/session seam. | Closes normal/tangent/color morph row projection and per-instance payload slot/indexing ABI for the morphed shader templates. Production `GEOMETRY_SOURCE_ID_MORPHED_MESH` / `GEOMETRY_SOURCE_ID_SKINNED_MORPHED_MESH` selection, GPU-vs-CPU product morph parity, real WGPU/Product/RenderDoc capture, and broader product miss=0 remain open. |
| 2026-06-30 | Direct CPU-morphed draw-source metadata | render_plan08_direct_cpu_morphed_draw_source_metadata_check_passed_wgpu_deferred | Plan 08 MS-M2 now keeps direct CPU-baked morph draws explicit instead of collapsing them into generic dynamic geometry. Direct mesh build records active morph weights as `PendingMeshGeometry::CpuMorphed`; `pending_mesh_geometry_source(...)`, pending command cache extract/plan, queue profile, and prepared queue stats all preserve `DynamicCpuMorphedSource`; `uses_cpu_morphed_source()` exposes the shared CPU-morphed classification. Shader selection remains `GEOMETRY_SOURCE_ID_STATIC_MESH` because morph deltas have already been baked into the dynamic vertex stream before the mesh reaches WGPU. | Scoped verification passed: rustfmt check covered the touched mesh draw, direct mesh build, pending draw, command cache plan/extract, prepared queue, prepared queue tests, and Runtime 15 guards; source/docs anchor scan covered `PendingMeshGeometry::CpuMorphed`, `DynamicCpuMorphedSource`, `uses_cpu_morphed_source`, and `GEOMETRY_SOURCE_ID_STATIC_MESH`; line budgets stayed under the existing owner limits; conflict/trailing-whitespace scans passed; scoped diff-check returned only LF/CRLF warnings. `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plan08-direct-cpu-morph-0630 --message-format short --color never` passed with existing warnings only. | Keeps direct CPU morph metadata distinct for the later GPU morph upload cutover without selecting morphed WGSL prematurely. True GPU morph weights/target storage upload on bindings 7/8, morphed/skinned-morphed shader selection, WGPU/Product/RenderDoc capture, product morph parity, and broader miss=0 remain open. |
| 2026-06-30 | CPU-morphed GPU-skinning draw-source metadata | render_plan08_cpu_morphed_gpu_skinning_draw_source_metadata_static_passed_cargo_deferred_active_lanes | Plan 08 MS-M1/MS-M2 now preserves CPU-morphed + GPU-skinned draw metadata without pretending GPU morph storage is ready. `MeshDrawGeometrySource` has explicit `DynamicCpuMorphedGpuSkinningSource`; `PendingSkinnedGpuSource::CpuMorphed` maps to that draw source, prepared queue stats keep it in dynamic geometry accounting, and `uses_cpu_morphed_gpu_skinning_source()` exposes the classification for guards and later runtime migration. Shader selection remains conservative: the runtime shader geometry id is still `GEOMETRY_SOURCE_ID_SKINNED_MESH` because current CPU-morphed sources have already baked morph deltas before GPU skinning. | Scoped static verification passed: rustfmt check covered the touched mesh draw, queue profile, GPU-scene sync, prepared queue, mesh-pass processor, prepared-queue test, and Runtime 15 guard files; source/docs anchor scan covered `DynamicCpuMorphedGpuSkinningSource`, `uses_cpu_morphed_gpu_skinning_source`, `PendingSkinnedGpuSource::CpuMorphed`, and `GEOMETRY_SOURCE_ID_SKINNED_MESH`; line budgets stayed under the prepared-queue guard limits; conflict/trailing-whitespace scans passed; scoped diff-check returned only LF/CRLF warnings. Cargo/WGPU remains deferred while active cargo/rustc lanes are running, so this row records source/static contract evidence only. | Keeps runtime draw-source metadata rich enough for the later MS-M2 GPU morph upload cutover while avoiding unsafe skinned-morphed shader include selection. Pure GPU morphed runtime selection, morph weights/target storage bindings 7/8 upload, product morph parity, RenderDoc/product capture, and broader miss=0 remain open. |
| 2026-06-30 | Product material mesh staged prewarm owner split + Velocity runtime contract | render_plan08_product_material_mesh_staged_prewarm_owner_split_velocity_static_passed_cargo_deferred_active_lanes | The former `graphics/tests/render_product_mesh_cache/staged_prewarm.rs` owner is hard-cut into `staged_prewarm/mod.rs` for Base mesh staged-cache product coverage and `staged_prewarm/material_passes.rs` for the material mesh multi-pass product chain. The material-pass child now also requires a previous velocity transform, zero missing velocity transforms, `temporal.velocity-object`, runtime `velocity pass` dimension accounting, request count >= 5, compile miss=0, and zero runtime write/error on the velocity frame. | Scoped static verification passed: `rustfmt --edition 2021 --check` for both new staged-prewarm owners and `runtime_15_product_base_mesh_staged_prewarm_is_wired`; source/docs anchor scan passed for Base/current owner paths, the new status, `temporal.velocity-object`, `velocity pass`, and previous velocity transform; line budgets are parent 673, `staged_prewarm/mod.rs` 354, `staged_prewarm/material_passes.rs` 652, guard 174, and old `staged_prewarm.rs` is absent. Conflict/trailing-whitespace scans passed; scoped `git diff --check` returned only LF/CRLF warnings. Cargo/WGPU remains deferred while active cargo/rustc lanes are running, so this row does not replace the earlier focused WGPU pass for `render_plan08_product_material_mesh_second_launch_staged_prewarm_passed_renderdoc_deferred`. | Keeps the already-passing product material-mesh staged-cache gate structurally under budget and locks the Velocity runtime contract. Real RenderDoc/product capture and broader product miss=0 remain open; the no-proxy wrapper gate is closed by the later 2026-06-30 no-proxy status. |
| 2026-06-30 | Project/plugin registry material-pass Velocity runtime contract | render_plan08_project_plugin_registry_material_passes_velocity_runtime_contract_static_passed_cargo_deferred_active_lanes | Plan 08 MS-M4 material-pass product staged-cache proof now locks the Velocity runtime path instead of only listing Velocity in the prewarm manifest. `assert_registry_material_pass_velocity_frame_shader_cache_hit` requires a previous velocity transform, zero missing velocity transforms, `temporal.velocity-object`, a runtime `velocity pass` dimension, request count >= 5, compile miss=0, and zero runtime write/error while still covering DepthPrepass/GBuffer/Shadow/TAA reactive mask through the folder-backed owner. | Scoped source edit is confined to `project_plugin_registry_material_passes_staged_cache/assertions.rs` plus `runtime_15_shader_prewarm_project_plugin_registry_material_passes_staged_cache_is_wired`; Cargo/WGPU remains deferred because active cargo/rustc lanes are still running, so this row records static contract evidence only. | Real focused product Cargo/WGPU pass, RenderDoc capture, and broader miss=0 remain open; the public no-proxy wrapper gate is closed by the later 2026-06-30 no-proxy status. |
| 2026-06-30 | Project/plugin registry material-pass owner split | render_plan08_project_plugin_registry_material_passes_owner_split_static_passed_cargo_deferred_active_lanes | The near-budget material-pass product owner is now folder-backed under `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/`. `mod.rs` owns orchestration, `case.rs` owns registry locators/revisions, `manifest.rs` owns staged request rewriting, `fixture.rs` owns product framework/material setup, `pipeline.rs` owns the product pipeline descriptors, and `assertions.rs` owns prewarm/runtime miss-report checks. The old single-file owner removed path is guarded by `runtime_15_shader_prewarm_project_plugin_registry_material_passes_staged_cache_is_wired`, so future material-pass coverage cannot return to one near-800-line file unnoticed. | Scoped `rustfmt --edition 2021 --check` passed for all six material-pass child owners and the Runtime 15 guard. Line budgets after the split: `mod.rs` 92, `case.rs` 47, `manifest.rs` 52, `fixture.rs` 219, `pipeline.rs` 119, `assertions.rs` 301, guard 138. Cargo/WGPU deferred due active editor validation lanes, so this row records structure/static evidence only. | Closes the structure-convention risk introduced by the prior 788-line material-pass test owner. Runtime behavior remains governed by `render_plan08_project_plugin_registry_material_passes_staged_cache_static_passed_cargo_timeout_no_result`; real focused product Cargo/WGPU pass, RenderDoc capture, and broader miss=0 remain open. |
| 2026-06-30 | Project/plugin registry material-pass staged-cache miss=0 | render_plan08_project_plugin_registry_material_passes_staged_cache_static_passed_cargo_timeout_no_result | Plan 08 MS-M4 now extends project/plugin registry staged-cache product coverage from the forward/raw `mesh.opaque` path to the full material-pass product chain in the folder-backed owner `graphics/tests/render_product_mesh_cache/project_plugin_registry_material_passes_staged_cache/`. The focused child owner rewrites built-in standard-material prewarm requests for `res://project/shaders/project_shader` and `package://native_dynamic_fixture/shaders/shader` while preserving pass-template content hashes, includes fallback variants, registers TAA-reactive registry materials, and runs a product `WgpuRenderFramework` pipeline covering DepthPrepass/GBuffer/Shadow/TAA reactive mask plus lighting/post. `runtime_15_shader_prewarm_project_plugin_registry_material_passes_staged_cache_is_wired` locks the child owner, parent mount, docs/session anchors, and line budgets. | Scoped `rustfmt --edition 2021 --check` passed for the parent product test, both project/plugin registry product children, both Runtime 15 guards, and `test_file_budget/mod.rs`. Focused Cargo/WGPU execution timed out after about 904 seconds with no test result for `render_product_project_plugin_registry_material_passes_use_staged_prewarm_without_compile_miss`; only matching cargo/rustc processes for `E:\cargo-targets\zircon-plan08-material-pass-registry-0630` were stopped. | Closes source/static product material-pass wiring for project/plugin registry staged-cache consumption and requires DepthPrepass/GBuffer/Shadow/TAA reactive mask coverage, compile miss=0, and zero runtime write/error in the test contract. Real focused product Cargo/WGPU pass, RenderDoc capture, and broader miss=0 remain open. |
| 2026-06-30 | Project/plugin registry product staged-cache miss=0 | render_plan08_project_plugin_registry_product_staged_cache_static_passed_cargo_timeout_no_result | Plan 08 MS-M4 now has a product-path registry material staged-cache proof in `graphics/tests/render_product_mesh_cache/project_plugin_registry_staged_cache.rs`. The new product child owner keeps the already-large staged prewarm owner from growing, registers project and selected-plugin shader assets for `res://project/shaders/project_shader` and `package://native_dynamic_fixture/shaders/shader`, then overwrites the exported `ResourceRecord` revisions through `register_record(exported_record)` so runtime material shader ids/revisions match the staged manifest. The manifest rewrites `ShaderVariantKey.material_shader`, `material_revision`, and `include_content_hashes` to the runtime raw-WGSL hash, while the product `WgpuRenderFramework` consumes the staged root through `ShaderVariantCacheDisk::with_fallback_roots(...)`. `runtime_15_shader_prewarm_project_plugin_registry_product_staged_cache_is_wired` locks the child owner, parent module mount, docs/session anchors, and line budgets. | Scoped `rustfmt --edition 2021` passed for `render_product_mesh_cache.rs`, `render_product_mesh_cache/project_plugin_registry_staged_cache.rs`, the new Runtime 15 guard, and `test_file_budget/mod.rs`. Line budgets after formatting: parent product test 672, product child 420, guard 84, guard mod 143. Cargo/WGPU execution timed out after about 904 seconds with no test result for `render_product_project_plugin_registry_materials_use_staged_prewarm_without_compile_miss`; only the matching cargo/rustc processes for `E:\cargo-targets\zircon-plan08-product-registry-staged-cache-0630` were stopped. RenderDoc MCP reported no running instances, so no capture evidence exists. | Closes source/static product wiring for project/plugin registry material staged-cache consumption and requires product `mesh.opaque` stats, disk hit, compile miss=0, zero runtime write/error in the test contract. A real product Cargo/WGPU result, RenderDoc/product capture, and broader product miss=0 remain open; the public no-proxy wrapper run is closed by the later 2026-06-30 no-proxy status. |
| 2026-06-30 | Project/plugin registry runtime staged-cache hit | render_plan08_project_plugin_registry_runtime_staged_cache_hit_static_passed_cargo_deferred | Plan 08 MS-M4 now has a runtime staged-cache key handoff guard for project/plugin registry shader records. `runtime_project_plugin_registry_shader_keys_use_staged_prewarm_without_compile_miss` builds staged prewarm requests for `res://project/shaders/project_shader` and `package://native_dynamic_fixture/shaders/shader`, explicitly rewrites `ShaderVariantKey.material_shader` and `material_revision` from the registry locator/revision, and then resolves matching `PipelineKey` values through `MeshPipelineCache` with `ShaderVariantCacheDisk::with_fallback_roots(...)`. `runtime_15_shader_prewarm_project_plugin_registry_runtime_staged_cache_hit_is_wired` locks the runtime test, docs/session anchors, and file budgets. | Scoped Rust formatting has been applied; line budgets are `ensure_pipeline.rs` 724, new guard 66, `test_file_budget/mod.rs` 139. Cargo/WGPU execution deferred while external Cargo/rustc lanes are active, so this slice records source-level/static guard evidence only and does not claim the new offscreen WGPU test passed. | Closes the source-level runtime fallback-root key handoff that proves registry material id/revision can be consumed by the runtime cache path with compile miss=0 in the test contract. Product Cargo/WGPU execution, RenderDoc/product capture, and broader product miss=0 remain open; the no-proxy public Cargo wrapper run is closed by the later 2026-06-30 no-proxy status. |
| 2026-06-30 | Project/plugin registry production wrapper orchestration | render_plan08_project_plugin_registry_production_wrapper_orchestration_passed_cargo_proxy_product_renderdoc_deferred | Plan 08 MS-M4 now has a public `zircon_build.py` runtime-wrapper orchestration proof for project/plugin shader registry export without starting a real Cargo lane while other builds are active. `tools/tests/test_zircon_build_shader_prewarm_wrapper_orchestration.py::test_public_runtime_wrapper_exports_project_plugin_registry_with_live_wgpu` calls `zircon_build.main(...)` with `--targets runtime --plugins native_dynamic_fixture --prewarm-shaders --validate-wgpu-shaders`, a temporary project shader `.wgsl/.zmeta` root, and a test-local cargo proxy. The proxy creates the runtime lib/preview artifacts expected by staging, then forwards the public prewarm `cargo run ... -- zircon_shader_prewarm` payload to the already-built `E:\cargo-targets\zircon-runtime-text-0630-clamp-contract\debug\zircon_shader_prewarm.exe`; `runtime_15_shader_prewarm_project_plugin_registry_wrapper_orchestration_is_wired` locks the wrapper test, build orchestration path, command helper, acceptance helper, docs/session anchors, and file budgets. | Focused Python `python -m unittest tools.tests.test_zircon_build_shader_prewarm_wrapper_orchestration` passed 1/1. The public wrapper path staged runtime artifacts, staged engine assets, emitted one real prewarm run through the proxy, and then executed the existing `validate_staged_shader_prewarm_acceptance_contract(...)`. Report summary under `target\codex-plan08-wrapper-orchestration-test\ZirconEngine\cache`: requested/written/failed = 18/18/0; WGPU module validation requested/validated/failed/skipped = 18/18/0/0; source provenance source_count/variant_count = 8/18; pass dimensions covered `forward`, `gbuffer`, `depth_prepass`, `shadow`, `velocity`, and `taa_reactive_mask` at 3/3 each; exported registry contains Ready Shader records for `res://project/shaders/project_shader` and `package://native_dynamic_fixture/shaders/shader`. The fake cargo log records two `build` artifact calls and one `prewarm_run`, and the prewarm args include `--validate-wgpu-modules`, both project/plugin `--asset-root` values, and automatic `--export-resource-registry`. | Closes public wrapper orchestration through a cargo proxy plus real WGPU prewarm and staged acceptance. The real no-proxy Cargo-wrapper build/run is closed by the later 2026-06-30 no-proxy status; RenderDoc/product capture and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry production direct WGPU export | render_plan08_project_plugin_registry_production_direct_wgpu_export_passed_product_renderdoc_deferred | Plan 08 MS-M4 now has a live direct `zircon_shader_prewarm` project/plugin registry export with WGPU module validation. Because other Cargo lanes were active, this slice reused the already-built `E:\cargo-targets\zircon-runtime-text-0630-clamp-contract\debug\zircon_shader_prewarm.exe` instead of starting Cargo, created a temporary project shader `.wgsl/.zmeta` root under `target\codex-plan08-production-registry-live-0630\project_assets`, and paired it with the real selected-plugin root `zircon_plugins\native_dynamic_fixture\assets`. The command used `--builtin-fallback --pretty --validate-wgpu-modules`, both `--asset-root` values, and automatic `--export-resource-registry target\codex-plan08-production-registry-live-0630\ZirconEngine\cache\shader_resource_records.json`; `runtime_15_shader_prewarm_project_plugin_registry_production_live_wgpu_is_wired` locks the run-path, acceptance, registry helper, plugin sidecar, docs/session anchors, and file budgets. | Direct prewarm exit code was 0 with empty stderr and wrote `shader_variants_report.json`, `shader_resource_records.json`, and stdout JSON under `target\codex-plan08-production-registry-live-0630`. Report summary: requested/written/failed = 18/18/0; WGPU module validation requested/validated/failed/skipped = 18/18/0/0; source provenance source_count/variant_count = 8/18; pass dimensions covered `forward`, `gbuffer`, `depth_prepass`, `shadow`, `velocity`, and `taa_reactive_mask` at 3/3 each; runtime cache wrote 18 `.wgsl.zst` artifacts plus 18 `.meta` files. Exported registry summary: 2 Ready Shader records, `res://project/shaders/project_shader` revision 126198881308539824 and `package://native_dynamic_fixture/shaders/shader` revision 14843875089575827114. The existing Python `validate_staged_shader_prewarm_acceptance_contract(...)` then passed against the same staged `engine_root`, requiring WGPU validation, cache artifacts, usable Ready Shader records, and report-visible registry-backed sources. Scoped `rustfmt --edition 2021 --check` passed for the new Runtime 15 guard and `test_file_budget/mod.rs`; code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: new Runtime 15 live WGPU guard 103, acceptance helper 269, registry helper 464, `test_file_budget/mod.rs` 140. Cargo wrapper/Product/RenderDoc were not run for this direct-exe slice. | Closes the live project/plugin registry export + staged WGPU module-validation acceptance for a temporary project shader root plus the real selected-plugin shader sidecar. Public Cargo wrapper execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry production CLI dry-run handoff | render_plan08_project_plugin_registry_production_cli_dry_run_python_passed_cargo_deferred | Plan 08 MS-M4 now proves the public `zircon_build.py` dry-run path prints the real selected-plugin prewarm command without starting Cargo. `test_cli_dry_run_prints_native_dynamic_fixture_prewarm_command` calls `zircon_build.main(...)` with `--targets runtime --plugins native_dynamic_fixture --prewarm-shaders --dry-run`, captures stdout, verifies exit code 0, and confirms the printed dry-run command includes `zircon_shader_prewarm`, the real `native_dynamic_fixture/assets` root, and automatic `--export-resource-registry` while not using explicit `--resource-registry`. `runtime_15_shader_prewarm_project_plugin_registry_production_cli_dry_run_is_wired` locks the dry-run main path, helper command path, plugin sidecar, docs/session anchors, and file budgets. | Focused Python `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_cli_dry_run_prints_native_dynamic_fixture_prewarm_command` passed 1/1; full `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` passed 26/26; `python -m py_compile tools/tests/test_zircon_build_shader_prewarm.py` passed; scoped `rustfmt --edition 2021 --check` passed for the new Runtime 15 guard, the production CLI-selection guard, and `test_file_budget/mod.rs`. Code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: Python prewarm tests 729, new Runtime 15 dry-run guard 94, CLI-selection guard 105, command guard 105, fixture guard 100, plugin shader sidecar 11. Cargo/WGPU/Product/RenderDoc are deferred for this dry-run handoff slice. | Closes only the public dry-run proof that the real selected-plugin shader sidecar root is visible in the generated automatic registry-export prewarm command. Full staged WGPU/product execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry production CLI selection handoff | render_plan08_project_plugin_registry_production_cli_selection_python_passed_cargo_deferred | Plan 08 MS-M4 now proves the real selected-plugin production fixture survives the public `zircon_build.py` CLI selection path before the prewarm command is built. `test_cli_selects_native_dynamic_fixture_assets_for_prewarm_command` parses `--targets runtime --plugins native_dynamic_fixture --prewarm-shaders`, resolves config from the repository plugin catalog, confirms the selected package is `native_dynamic_fixture`, verifies its production `assets` root contains `shader.wgsl.zmeta`, and then confirms `build_shader_prewarm_command(...)` emits that real root as `--asset-root` while using automatic `--export-resource-registry` rather than explicit `--resource-registry`. `runtime_15_shader_prewarm_project_plugin_registry_production_cli_selection_is_wired` locks the CLI/config/helper/plugin/docs/session anchors and file budgets. | Focused Python `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_cli_selects_native_dynamic_fixture_assets_for_prewarm_command` passed 1/1; full `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` passed 25/25; `python -m py_compile tools/tests/test_zircon_build_shader_prewarm.py` passed; scoped `rustfmt --edition 2021 --check` passed for the new Runtime 15 guard, the production command guard, and `test_file_budget/mod.rs`. Code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: Python prewarm tests 697, new Runtime 15 guard 105. Cargo/WGPU/Product/RenderDoc are deferred for this CLI-selection slice. | Closes only the public CLI selection proof that the real selected-plugin shader sidecar root reaches the automatic registry-export prewarm command. Full staged WGPU/product execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry production command handoff | render_plan08_project_plugin_registry_production_command_python_passed_cargo_deferred | Plan 08 MS-M4 now gives the real selected-plugin production fixture a build-helper command regression. `test_build_command_auto_export_registry_uses_native_dynamic_fixture_assets` discovers the repository `native_dynamic_fixture` package through `zircon_build.discover_plugins(...)`, verifies its real `assets` root contains `shader.wgsl.zmeta`, then builds the shader prewarm command and confirms that root is passed as `--asset-root` together with automatic `--export-resource-registry`. `runtime_15_shader_prewarm_project_plugin_registry_production_command_is_wired` locks the Python test, shared prewarm helper seams, plugin sidecar, docs/session anchors, and file budgets. | Focused Python `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_build_command_auto_export_registry_uses_native_dynamic_fixture_assets` passed 1/1; full `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` passed 24/24; `python -m py_compile tools/tests/test_zircon_build_shader_prewarm.py` passed; scoped `rustfmt --edition 2021 --check` passed for the new Runtime 15 guard and `test_file_budget/mod.rs`. Code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: Python prewarm tests 654, new Runtime 15 guard 105. Cargo/WGPU/Product/RenderDoc are deferred for this command-handoff slice. | Closes only the build-helper command proof that the real selected-plugin shader sidecar root reaches automatic registry export. Full staged WGPU/product execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry production fixture prewarm | render_plan08_project_plugin_registry_production_fixture_static_passed_cargo_timeout_no_result | Plan 08 MS-M4 now moves project/plugin registry export evidence from temporary roots into a real selected-plugin fixture. `zircon_plugins/native_dynamic_fixture/assets/shader.wgsl.zmeta` declares `package://native_dynamic_fixture/shaders/shader` as a Ready Shader sidecar for the existing plugin asset root, while `collect_plugin_asset_roots(...)` and `shader_asset_root_paths_for_prewarm(...)` keep selected plugin `asset_roots` feeding the automatic `shader_resource_records.json` export path. `runtime_15_shader_prewarm_project_plugin_registry_production_fixture_is_wired` locks the real plugin manifest/assets, build-helper handoff, docs/session anchors, and file budgets. | Scoped `rustfmt --edition 2021 --check` passed for the new Runtime 15 guard and `test_file_budget/mod.rs`. Code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: plugin shader sidecar 11, new Runtime 15 guard 100. A real project+plugin `zircon_shader_prewarm --validate-wgpu-modules --export-resource-registry` attempt against staged project assets plus `zircon_plugins/native_dynamic_fixture/assets` exited nonzero after about 546 seconds with no `shader_variants_report.json` or `shader_resource_records.json`; a follow-up `--help` run on the same target timed out after about 304 seconds in compile. Matching `zircon_shader_prewarm` cargo processes for this render target were stopped, unrelated lanes were left untouched, and this is not counted as Cargo/WGPU/Product validation. | Closes only the static production fixture wiring for a real selected-plugin shader sidecar and plugin asset-root auto-export path. Full staged WGPU/product execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry export file handoff | render_plan08_project_plugin_registry_export_file_static_passed_cargo_deferred | Plan 08 MS-M4 now locks the actual `zircon_shader_prewarm` run-path file handoff for project+plugin registry export. `shader_prewarm_project_and_plugin_asset_roots_export_wrapped_resource_registry_file` creates separate project and plugin `.zmeta` shader roots, calls `export_shader_resource_registry_for_asset_roots(...)` with a staged `ZirconEngine/cache/shader_resource_records.json` path, verifies the written JSON uses the wrapped `{ "resources": [...] }` shape, and confirms returned records match the exported file records for `res://project/shaders/project` and `package://virtual_geometry/shaders/plugin`. It also constructs `ShaderPrewarmResourceRegistryOverlay::from_records(...)` from the exported file records and proves locator lookup returns the exported revisions. `runtime_15_shader_prewarm_project_plugin_registry_export_file_is_wired` locks the run/test/docs/session anchors and file budget. | Scoped `rustfmt --edition 2021 --check` passed for `run.rs`, the new Runtime 15 guard, and `test_file_budget/mod.rs`. Code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: `run.rs` 315, new Runtime 15 guard 52. Cargo/WGPU/Product/RenderDoc validation remains deferred for this implementation slice under milestone-first cadence. | Closes only the run-path export-file handoff from project+plugin `.zmeta` roots to wrapped `shader_resource_records.json` plus returned overlay records. Full staged WGPU/product execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry live asset-root export | render_plan08_project_plugin_registry_live_asset_roots_static_passed_cargo_deferred | Plan 08 MS-M4 now adds a real project+plugin asset-root fixture for full project/plugin registry export. `shader_resource_records_from_project_and_plugin_asset_roots_export_distinct_shader_sources` creates project and plugin `.zmeta` shader roots and proves `shader_resource_records_from_asset_roots(...)` exports two distinct Ready Shader `ResourceRecord`s for `res://project/shaders/project` and `package://virtual_geometry/shaders/plugin`. `shader_prewarm_project_and_plugin_asset_roots_use_exported_registry_revisions` then builds manifests for both roots through `asset_root_manifest_with_resource_registry_revisions(...)`, merges them, and proves each registry-backed source label emits six prewarm requests whose `material_revision` comes from the exported record. `runtime_15_shader_prewarm_project_plugin_registry_live_asset_roots_are_wired` locks the run/manifest/export/test/docs/session anchors and file budgets. | Scoped `rustfmt --edition 2021 --check` passed after applying mechanical formatting. Focused Cargo `cargo test -q -p zircon_runtime --bin zircon_shader_prewarm --target-dir target\codex-plan08-registry-live-roots-0630 --locked --jobs 1 project_and_plugin -- --nocapture --test-threads=1` timed out after 605 seconds with no test result and is not counted as passed; matching target cargo/rustc residuals were stopped. Code/docs anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed; diff-check only reported LF/CRLF warnings. Line budgets: manifest/tests/resource_registry 276, manifest/resource_registry/tests 247, new Runtime 15 guard 104. | Closes only the static live asset-root fixture for project+plugin `.zmeta` shader registry export and manifest revision overlay. Full staged WGPU/product execution, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry report-source acceptance | render_plan08_project_plugin_registry_report_source_python_passed_cargo_deferred | Plan 08 MS-M4 now tightens staged project/plugin shader registry auto-export from "nonempty registry" to report-visible source evidence. `validate_shader_resource_registry_export_contract(...)` accepts `require_report_registry_backed_sources`; when `validate_staged_shader_prewarm_acceptance_contract(...)` is using automatic `shader_resource_records.json` export because project `shader_asset_roots` or selected plugin `asset_roots` are configured, it now requires at least one registry-backed report source label in addition to a usable Ready Shader `ResourceRecord`. This prevents a builtin-only successful staged report from passing project/plugin registry export acceptance. `tools/tests/test_zircon_build_shader_prewarm_project_plugin_registry_acceptance.py::test_acceptance_contract_requires_registry_source_for_project_plugin_auto_export` owns the focused negative regression, its positive companion covers the accepted report source, and `runtime_15_shader_prewarm_project_plugin_registry_report_source_is_wired` locks helper/test/docs/session anchors and file budgets. | Focused Python `tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_project_plugin_registry_acceptance tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` passed 44/44; wider build-helper Python combo passed 72/72; `py_compile`, scoped `rustfmt --edition 2021 --check`, anchor scans, conflict-marker scan, trailing-whitespace scan, and scoped `git diff --check` passed, with diff-check only reporting LF/CRLF warnings. Line budgets: acceptance helper 269, registry helper 464, main acceptance tests 768, project/plugin test 182, registry tests 598, new Rust guard 115. Cargo/WGPU/Product/RenderDoc are not counted for this Python/static slice. | Closes only build-helper report-source evidence for automatic project/plugin registry export. Full live project/plugin registry export with real staged WGPU/product assets, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-30 | Project/plugin registry auto-export nonempty acceptance | render_plan08_project_plugin_registry_auto_export_nonempty_python_passed_cargo_deferred | Plan 08 MS-M4 now tightens staged project/plugin shader registry auto-export acceptance. `validate_shader_resource_registry_export_contract(...)` accepts `require_usable_shader_records`, and `validate_staged_shader_prewarm_acceptance_contract(...)` enables it only for automatic registry export when explicit project `shader_asset_roots` or selected plugin `asset_roots` are configured. This prevents a successful staged report that only exposes builtin/raw source labels from accepting an empty `shader_resource_records.json` while project/plugin roots were supposed to be scanned. Focused tests `test_acceptance_contract_requires_usable_records_for_project_plugin_auto_export`, `test_validate_registry_export_contract_requires_usable_shader_records_when_requested`, and `test_validate_registry_export_contract_accepts_usable_shader_records_when_requested` cover the contract; new guard `runtime_15_shader_prewarm_project_plugin_registry_auto_export_is_wired` locks helper handoff, registry validation, docs/status/session anchors, and file budgets. | Focused acceptance/resource-registry Python 39/39 passed; wider build-helper Python combo 67/67 passed; `py_compile` for touched helpers/tests passed; scoped `rustfmt --edition 2021 --check` for the new Runtime 15 guard and `test_file_budget/mod.rs` passed; anchor scan, conflict-marker scan, trailing-whitespace scan, line-budget scan, and scoped `git diff --check` passed, with diff-check only reporting LF/CRLF warnings. Cargo/WGPU/Product/RenderDoc are not counted for this Python/static slice. | Closes only the build-helper nonempty auto-export acceptance for project/plugin shader asset roots. Full live project/plugin registry export with real staged WGPU/product assets, RenderDoc/product capture, and broader product miss=0 remain open. |
| 2026-06-29 | VirtualGeometry product draw-source | render_plan08_virtual_geometry_product_draw_source_static_passed_cargo_deferred | Plan 08 MS-M4 now locks the product draw-source path for VirtualGeometry after the page/cluster binding, resident upload, payload decode, and meshlet ordinal slices. `submit_frame_extract/build_frame_submission_context` only calls `build_automatic_virtual_geometry_extract(...)` when VirtualGeometry is enabled and no authored `RenderVirtualGeometryExtract` is present; that path uses the runtime provider `build_extract_from_meshes(...)` callback and `ProjectAssetManager::load_model_asset(...)` to derive the effective extract from submitted `RenderMeshSnapshot` model handles. The product fixture provider now has a folder-backed `virtual_geometry_provider.rs` child owner that can build a VG extract from registered `ModelAsset` primitives, and `render_product_virtual_geometry_model_asset_uses_automatic_draw_source` registers a cooked VG `ModelAsset`, submits a normal mesh extract, and asserts `RenderVirtualGeometryPayloadSource::AutomaticFallback` plus indirect execution stats. New guard `runtime_15_virtual_geometry_product_draw_source_is_wired` locks the submit-context/provider/product-test/docs seam and file budgets. | 聚焦验证：scoped rustfmt、静态 code/docs anchor scan、line-budget scan、conflict/trailing whitespace scan、scoped diff-check 通过，diff-check 仅报告 LF/CRLF warnings。Focused `cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1 render_product_virtual_geometry_model_asset_uses_automatic_draw_source -- --exact --nocapture --test-threads=1` 超过 11 分钟仍在编译 `zircon_runtime` lib-test，无 test result；本轮该 target 对应 cargo/rustc 残留已停止，不计 Cargo/WGPU/Product/RenderDoc 通过。 | 关闭的是产品测试层从真实 `ModelAsset`/mesh snapshot 到 automatic VG provider/execution stats 的 draw-source 证明；RenderDoc/product capture、full live project/plugin registry export 与 broader product miss=0 仍未关闭。 |
| 2026-06-29 | VirtualGeometry meshlet vertex ordinal | render_plan08_virtual_geometry_meshlet_vertex_ordinal_static_passed_cargo_deferred | Plan 08 MS-M4 在 asset payload decode 之后补齐 shader fetch 所需的独立 vertex ordinal 资产合同。`ModelPrimitiveAsset` 新增 `assign_virtual_geometry_vertex_ordinals()` 以及 `encode/decode_virtual_geometry_vertex_ordinal(...)`，把 VG primitive 的源顶点序号按 16+16 bit 写入 `MeshVertex.joint_indices[0..2]`；普通非 VG primitive 不改写 joint channels。OBJ/GLTF indexed mesh 导入、`.model.toml` backfill、`MeshAsset::from_model_primitive(...)` 和 `MeshAsset::to_model_primitive(...)` 都复用该标准化入口，保证 root `ModelPrimitiveAsset` 与 labeled `MeshAsset` subasset 的 shader input ordinal 一致。`zr_geometry_virtual_geometry.wgsl` 不再只读 `v.joints.x`，而是解包 `v.joints.x | (v.joints.y << 16)`；`mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings` 同步锁定该 WGSL 合同。新增/扩展 `virtual_geometry_vertex_ordinals_pack_into_joint_index_slots`、`virtual_geometry_vertex_ordinals_do_not_rewrite_non_vg_primitives`、mesh conversion/importer 断言与结构守卫 `runtime_15_virtual_geometry_meshlet_vertex_ordinal_is_wired`。 | 聚焦验证：scoped rustfmt、静态 code-anchor scan、line-budget scan、conflict/trailing whitespace scan、scoped diff-check 通过。`cargo check -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1` 约 303 秒超时，无 check result，不计 Cargo/WGPU/Product/RenderDoc 通过。 | 关闭的是 VG source vertex ordinal 到 WGSL page/cluster payload fetch ordinal 的资产/shader 合同；产品 VG draw-source 正式化、RenderDoc/product capture、full live registry export 与 broader product miss=0 仍未关闭。 |
| 2026-06-29 | VirtualGeometry asset payload decode | render_plan08_virtual_geometry_asset_payload_decode_static_passed_cargo_deferred | Plan 08 MS-M4 在渲染侧 payload upload 合同之后，补上 first-party `virtual_geometry` runtime plugin 的 cooked asset page payload 解码入口。`zircon_plugins/virtual_geometry/runtime/src/virtual_geometry/nanite/page_payload.rs` 独立解析 `ZVG0` cooked page payload，将 payload item 的 `triangle_start/triangle_count` 映射回 `ModelPrimitiveAsset` 的 source indices/vertices，并生成 `RenderVirtualGeometryPagePayloadVertex { position, normal, tangent }`；`automatic_extract.rs` 通过 `VirtualGeometryAutomaticExtractInstance::from_model_primitive(...)` 保存 primitive 顶点/索引，在 local->global page remap 后产出 `resident_page_payloads` sidecar。`VirtualGeometryRuntimeExtractOutput`、`FrameSubmissionContext` 与生产 `build_virtual_geometry_debug_snapshot(...)` 现在把该 sidecar 传入 `RenderVirtualGeometryDebugSnapshot.resident_page_payloads`，resident upload owner 继续唯一消费并写入 cluster words。新增 `render_page_payloads_decode_cooked_triangle_vertices_with_global_page_ids`、扩展 imported cooked model extract 断言，并新增结构守卫 `runtime_15_virtual_geometry_asset_payload_decode_is_wired` 锁定 decode 模块、sidecar 链路、snapshot 消费、docs/status/session anchors 和行数预算。 | 聚焦验证：scoped `rustfmt` 已覆盖 runtime sidecar/context/snapshot、virtual_geometry plugin nanite page payload/automatic extract/provider/imported extract test 与 Runtime 15 守卫；静态 code-anchor scan 已确认 decode/sidecar/snapshot 链路。`cargo check -q -p zircon_plugin_virtual_geometry_runtime --lib --target-dir target\codex-plan08-vg-asset-payload-decode-0629 --locked --jobs 1` 约 304 秒超时，无 check result；本轮 target 对应残留 cargo/rustc 已停止，不计 Cargo/WGPU/Product/RenderDoc 通过。 | 关闭的是 cooked asset page payload 解码到 render debug snapshot sidecar，以及 upload owner 可消费真实 position/normal/tangent cluster words 的输入合同；meshlet vertex ordinal 独立资产合同已由后续 `render_plan08_virtual_geometry_meshlet_vertex_ordinal_static_passed_cargo_deferred` 关闭。仍未关闭产品 VG draw-source 正式化、RenderDoc/product capture、full live registry export 与 broader product miss=0。 |
| 2026-06-29 | VirtualGeometry cluster payload upload | render_plan08_virtual_geometry_cluster_payload_upload_static_passed_cargo_deferred | Plan 08 MS-M4 在 resident buffer upload 基础上新增显式 page payload -> cluster words 合同。`core/framework/render/virtual_geometry_debug_snapshot/page_payload.rs` 定义 `RenderVirtualGeometryPagePayload` / `RenderVirtualGeometryPagePayloadVertex`，`RenderVirtualGeometryDebugSnapshot.resident_page_payloads` 成为 mesh build 可消费的 resident payload 输入；生产快照构建器现从 `FrameSubmissionContext::virtual_geometry_resident_page_payloads()` 接收该输入。`graphics/scene/gpu_scene/layout.rs` 新增 `GPU_VIRTUAL_GEOMETRY_CLUSTER_WORDS_PER_VERTEX = 4`，`build_mesh_draws/build/virtual_geometry_resident_upload.rs` 现在按 resident `submission_slot` 生成 page row，并在存在 payload 时把每个 vertex 写成 position/normal/tangent/pad 四个 `GpuVirtualGeometryClusterWord`，重复 page submission 共享同一 cluster word 区间。新增 `virtual_geometry_cluster_words_follow_resident_page_payloads` 与结构守卫 `runtime_15_virtual_geometry_cluster_payload_upload_is_wired` 锁定 DTO、re-export、GPU ABI 常量、payload 投影、docs/status/session anchors 和行数预算。 | 聚焦验证：`rustfmt --edition 2021 --check` 覆盖 debug snapshot payload DTO/re-export、生产快照构建器、GPUScene ABI、resident upload owner 和两个 VG 结构守卫，通过。`cargo check -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1` 约 304 秒超时，无 check result；本轮 target 对应残留 cargo/rustc 已停止，不计 Cargo 通过。 | 关闭的是渲染侧 payload DTO 到 cluster word upload 投影，以及“缺 payload 时 vertex_count=0 继续 fallback”的安全语义；asset payload decode 与 meshlet vertex ordinal 已分别由后续 `render_plan08_virtual_geometry_asset_payload_decode_static_passed_cargo_deferred`、`render_plan08_virtual_geometry_meshlet_vertex_ordinal_static_passed_cargo_deferred` 接入。产品 VG draw、RenderDoc/product capture、full live registry export 与 broader product miss=0 仍未关闭。 |
| 2026-06-29 | VirtualGeometry resident buffers upload | render_plan08_virtual_geometry_resident_buffers_upload_static_passed_cargo_deferred | Plan 08 MS-M4 在前一段 page/cluster shader binding 合同之后，把 VirtualGeometry resident 数据上传 owner 接入 GPUScene 与 mesh draw 同步链。`graphics/scene/gpu_scene/layout.rs` 新增 16-byte 对齐的 `GpuVirtualGeometryPage` 与 `GpuVirtualGeometryClusterWord`，并锁定 page row 的 cluster-base、vertex-count、page-id、flags 偏移；`graphics/scene/gpu_scene/virtual_geometry.rs` 新增 `upload_virtual_geometry_resident_buffers(...)`，负责 pages/clusters CPU shadow、storage buffer 重建、`queue.write_buffer` 上传和 bind group rebuild，同时将上传字节汇入既有 `GpuSceneUploadReport`。`build_mesh_draws` 新增 `virtual_geometry_resident_upload.rs`，在 VirtualGeometry execution snapshot 中只为 `Resident` 且有 `submission_slot` 的 segment 生成 page row，并在 GPUScene draw sync 前上传；`gpu_scene_sync.rs` 把 `PendingMeshDraw.indirect_draw_ref.segment_key.submission_slot` 写入 primitive/instance `payload_slot`，`VirtualGeometrySubmissionDetail::payload_slot()` 同步成为 execution projection 的语义 helper。新增 `render_gpu_scene_uploads_virtual_geometry_resident_buffers`、`virtual_geometry_page_rows_follow_submission_slots` 与结构守卫 `runtime_15_virtual_geometry_resident_buffers_upload_is_wired` 锁定 GPUScene typed ABI、resident upload owner、mesh build 接线、payload slot、docs/status/session anchors 和行数预算。 | 聚焦验证：`rustfmt --edition 2021 --check` 覆盖 GPUScene layout/runtime/mod/upload owner、mesh build 接线、gpu_scene_sync、VirtualGeometry submission/execution projection 和新结构守卫，通过。`cargo check -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1` 在 warmed target 通过，仅既有 warnings。Focused `cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1 render_gpu_scene_uploads_virtual_geometry_resident_buffers -- --exact --nocapture --test-threads=1` 约 304 秒超时，仍在编译 lib-test，无 test result；本轮该 target 对应 cargo/rustc 残留已停止，不计 Cargo test 通过。 | 关闭的是 GPUScene resident page/cluster buffer ownership、submission-slot -> shader payload-slot 接线和 resident page-row 上传入口；后续 cluster payload upload、asset payload decode 与 meshlet vertex ordinal 已补齐真实 cluster word 输入和 shader ordinal 合同。产品 VG draw、RenderDoc/product capture、full live registry export 与 broader product miss=0 仍未完成。 |
| 2026-06-29 | VirtualGeometry page/cluster shader bindings | render_plan08_virtual_geometry_page_cluster_shader_bindings_static_passed_cargo_deferred | Plan 08 MS-M4 把 `custom:virtual_geometry=4` 从 WGPU-valid passthrough include 推进到真实 page/cluster shader binding 合同。`GeometrySourceBindingKind` 新增 `VirtualGeometryClusters`，`zircon_plugins/virtual_geometry` runtime descriptor 与 `plugin.toml` 现在同时声明 `virtual_geometry.pages` 和 `virtual_geometry.clusters`；GPUScene group3 预留非 morph 槽 `@binding(9)` pages 与 `@binding(10)` clusters，并在默认、palette、visible-remap bind group 重建路径统一挂入 fallback storage buffers。`zr_gpu_scene.wgsl` 暴露 `zr_virtual_geometry_pages` / `zr_virtual_geometry_clusters`，`zr_geometry_virtual_geometry.wgsl` 不再只透传 vertex input，而是按 primitive `payload_slot`、page table 的 cluster base / vertex count 和 cluster word storage 读取 position / normal / tangent，越界或无 payload 时回退到原始 vertex input。新增 `mesh_pipeline_virtual_geometry_template_source_declares_page_cluster_fetch_bindings` 与结构守卫 `runtime_15_virtual_geometry_page_cluster_shader_bindings_are_wired` 锁定 descriptor、manifest、GPUScene slots、WGSL fetch、prewarm fixtures、docs/status/session anchors 和行数预算。 | 聚焦静态验证通过：`rustfmt --edition 2021 --check` 覆盖 geometry descriptor、virtual_geometry plugin、GPUScene binding/runtime、mesh pipeline shader source/ensure tests、prewarm manifest fixtures、package manifest fixture 与新结构守卫；`python -m py_compile tools/tests/test_zircon_build_shader_prewarm.py` 通过；PowerShell 语法修正后 focused Python `python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_generated_shader_permutation_registry_document_exports_selected_plugin_descriptors` 1/1 通过；conflict-marker scan 与 scoped `git diff --check` 通过，diff-check 仅报告 LF/CRLF warnings。行数预算：`gpu_scene/binding.rs` 268、`gpu_scene/gpu_scene.rs` 611、`mesh_pipeline_cache/shader_source.rs` 661、`ensure_pipeline.rs` 553、新守卫 142。Cargo check 和两个 focused Cargo tests 分别 300 秒左右超时仍在编译 `zircon_runtime`，无 test result，相关 target 残留 cargo/rustc 已停止，不计 Cargo 通过。 | 关闭的是 descriptor -> GPUScene group3 storage slot -> WGSL guarded fetch 的 shader 合同；runtime resident uploader、payload upload、asset decode 与 meshlet ordinal 已由后续状态锚继续关闭。产品 VG draw、RenderDoc/product capture、full live registry export 或 broader product miss=0 仍未完成。 |
| 2026-06-29 | Project shader asset roots auto-export | render_plan08_project_shader_asset_roots_auto_export_python_static_passed_cargo_deferred | Plan 08 MS-M4 继续补齐 full project/plugin registry export 的 project 侧入口：`tools/zircon_build.py` 新增 `BuildConfig.shader_asset_roots` 与 `--shader-asset-root`，用于把项目 shader asset roots 显式加入 staged prewarm；`tools/zircon_build_shader_prewarm.py::shader_asset_root_paths_for_prewarm(config)` 现在按 staged `ZirconEngine/assets`、项目 roots、selected plugin roots 的顺序生成同一个 `--asset-root` 集合，因此自动 `--export-resource-registry ZirconEngine/cache/shader_resource_records.json` 能扫描项目 `.zmeta` shader 资产和插件资产。新增 `test_zircon_build_resolves_project_shader_asset_roots_for_prewarm`，扩展 `test_build_command_auto_export_registry_scans_all_asset_roots` / `test_prewarm_plan_lists_asset_roots_for_registry_export`，并新增结构守卫 `runtime_15_shader_prewarm_project_asset_roots_auto_export_is_wired` 锁定 CLI、命令、测试、docs/status/session anchors。 | 聚焦验证已通过：`PYTHONDONTWRITEBYTECODE=1 python -m unittest ...test_build_command_auto_export_registry_scans_all_asset_roots ...test_prewarm_plan_lists_asset_roots_for_registry_export ...test_zircon_build_resolves_project_shader_asset_roots_for_prewarm` 3/3；build-helper 聚合 76/76 通过；`python -m py_compile tools/zircon_build.py tools/zircon_build_shader_prewarm.py tools/tests/test_zircon_build_shader_prewarm.py` 通过；`rustfmt --edition 2021 --check` 覆盖 `shader_prewarm_project_asset_roots_auto_export.rs` 与 `test_file_budget/mod.rs` 通过。`cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1 runtime_15_shader_prewarm_project_asset_roots_auto_export_is_wired -- --exact --nocapture --test-threads=1` 300 秒超时，仍在编译 `zircon_runtime` test library 且无 test result，本轮该 target 相关 cargo/rustc 残留已停止，不计 Cargo 通过。`tools/zircon_build.py` 已超过 1000 行，本切片只贴近既有 build-config/CLI orchestration 入口，结构补记记录后续最小拆分边界，不把该根文件计入 800 行 owner 预算。 | 关闭的是项目 asset roots 进入 staged prewarm/resource-registry auto-export 输入集合；仍未声明 full live project/plugin registry export、真实 `zircon_shader_prewarm --validate-wgpu-modules` 产品覆盖、RenderDoc/product capture、真实 VG page/cluster fetch/bindings 或 broader product miss=0 完成。 |
| 2026-06-29 | Build-tool explicit registry exact revision acceptance | render_plan08_build_tool_explicit_registry_exact_revision_acceptance_python_passed_cargo_deferred | Plan 08 MS-M4 把 full live registry exact-revision 门禁中的 build-helper acceptance 部分从“显式 `--shader-resource-registry` 不验证自动导出产物”推进到“显式 registry 仍必须按成功 report 做 ResourceRecord 关联验收”。`tools/zircon_build_shader_prewarm_acceptance.py::validate_staged_shader_prewarm_acceptance_contract(...)` 现在对 `config.shader_resource_registry` 或自动 `shader_prewarm_resource_registry_path` 二选一调用 `validate_shader_resource_registry_export_contract(..., report_path=config.shader_prewarm_report_path)`，因此 caller-supplied live/project registry 不能再绕过 report-visible source label、`kind=Shader`、`state=Ready` 和 positive `revision` 合同。新增 `test_acceptance_contract_validates_explicit_registry_against_report` 与 `test_acceptance_contract_rejects_explicit_registry_without_ready_revision`，结构守卫 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper/test/docs/session/status anchors。 | RED focused acceptance tests 先失败：显式 registry positive handoff 没有调用 registry validator，zero-revision explicit registry 没有抛出 `usable shader ResourceRecord revisions`；实现后 focused 2/2 通过，acceptance owner 17/17 通过。收尾验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest ...` 104/104 通过，isolated `py_compile` 通过，targeted `rustfmt --edition 2021 --check` 通过，旧显式跳过语义扫描、conflict scan、line-budget scan 与 scoped `git diff --check` 通过，diff-check 仅报告 LF/CRLF warnings。Cargo 结构守卫 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 用独立 target 运行 300 秒超时，仍在编译 `zircon_runtime` 测试库且无 test result，残留本轮 cargo/rustc 已清理，不计 Cargo 通过。 | 关闭的是 build-helper explicit/live registry exact-revision acceptance；不声明 full production project/plugin registry export 已完成。RenderDoc/product capture、真实 VG page/cluster fetch/bindings、broader product coverage 与 product miss=0 仍为后续门禁。 |
| 2026-06-29 | Product material mesh staged prewarm and pure-depth GBuffer contract | render_plan08_product_material_mesh_second_launch_staged_prewarm_passed_renderdoc_deferred | Plan 08 MS-M4 把 Base 产品二次启动验收推进到材质网格完整产品 pass 链：`graphics/tests/render_product_mesh_cache/staged_prewarm/material_passes.rs::render_product_material_mesh_passes_second_launch_use_staged_prewarm_without_compile_miss` 通过 fresh staged fallback root 连续构造两个 `WgpuRenderFramework`，产品 pipeline 覆盖 `DepthPrepass`、`Shadow`、`Deferred/GBuffer`、`Lighting`、`PostProcess` 与 terminal AA fallback。为匹配真实 deferred 资源契约，同步把 `DepthPrepass` 收敛为 pure-depth pass，`deferred.gbuffer` 独立拥有 `GBUFFER_NORMAL` 写目标，`zr_template_deferred_gbuffer.wgsl` 输出 albedo/normal/material 三个 color target，`create_gbuffer_mesh_pipeline(...)` 与产品 graph descriptor 保持一致。测试断言首帧 staged disk hit、同 framework 二次帧 memory hit/no compile miss、runtime write/error 为零，并要求 shadow atlas、light grid、deferred lighting 与 post executor 可见。 | `cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1 render_product_material_mesh_passes_second_launch_use_staged_prewarm_without_compile_miss -- --nocapture --test-threads=1` 通过，`1 passed; 0 failed; 5480 filtered out`。同一目标目录复跑 `graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::ensure_pipeline::tests::runtime_custom_geometry_descriptor_non_base_pipelines_use_staged_prewarm_without_compile_miss` 通过，`1 passed; 0 failed; 5480 filtered out`。`cargo check -q -p zircon_runtime --lib --target-dir target\codex-plan08-product-material-pass-cache-0629 --locked --jobs 1` 通过，仅既有 warnings。 | 关闭 focused 产品材质网格 staged-cache second-launch compile-miss=0 和 runtime pure-depth DepthPrepass/GBuffer normal-target 产品 graph 契约；RenderDoc/product capture、完整 live project/plugin registry export、真实 VG page/cluster fetch/bindings、broader product coverage 仍是后续门禁。 |
| 2026-06-29 | Runtime custom geometry descriptor non-Base staged cache hit WGPU pipelines | render_plan08_runtime_custom_geometry_descriptor_non_base_staged_cache_hit_wgpu_pipelines_passed_product_deferred | Plan 08 MS-M4 把 `custom:virtual_geometry=4` 的 staged cache runtime 消费从 Base 扩到 GBuffer、DepthPrepass、Shadow、Velocity 与 TAA reactive pass。`mesh_pipeline_standard_material_template_source_for_shader_pass_and_descriptor(...)` 现在把 GBuffer 路由到 deferred-gbuffer template，把 TAA reactive 路由到 TAA reactive mask template，其余 pass 继续走 material-pass template，保证 built-in standard-material prewarm manifest 与运行时 pass-specific ensure path 使用同一源码 owner 和 source hash。新增 focused WGPU 回归在 `MeshPipelineCache` 注册 plugin-range geometry descriptor 后，通过 staged fallback root 创建五个非 Base WGPU pipelines。 | 诊断阶段第一次 focused WGPU run 失败为 `disk_hit_count` 4/5，临时 cache 显示只有 GBuffer 写入 runtime cache，证明 prewarm/runtime 的 GBuffer source hash 漂移；修复后 `cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-runtime-custom-geometry-cache-0629 --locked --jobs 1 graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::ensure_pipeline::tests::runtime_custom_geometry_descriptor_non_base_pipelines_use_staged_prewarm_without_compile_miss -- --exact --nocapture --test-threads=1` 零退出，`1 passed; 0 failed; 5467 filtered out`，并断言总 report `request=5/disk_hit=5/compile_miss=0/disk_write=0/disk_error=0`、geometry id `4` 维度同样 `disk_hit=5/compile_miss=0`。`cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-runtime-custom-geometry-cache-0629 --locked --jobs 1 dynamic_api::shader_prewarm::tests::builtin_ -- --nocapture --test-threads=1` 通过 `4 passed; 0 failed; 5464 filtered out`；`rustfmt --edition 2021 --check` 覆盖 `shader_source.rs` 与 `ensure_pipeline.rs`。 | 这关闭的是 descriptor-backed runtime cache lookup 到非 Base WGPU pipeline hit 的门禁。Product VG draw-source/page-cluster fetch、full live registry export、RenderDoc/product capture、product fallback-root hit 与 second-launch miss=0 仍未关闭。 |
| 2026-06-29 | Runtime custom geometry descriptor staged cache hit WGPU pipeline | render_plan08_runtime_custom_geometry_descriptor_staged_cache_hit_wgpu_pipeline_passed_product_deferred | Plan 08 MS-M4 把 selected-plugin geometry descriptor 从“只被离线 prewarm 生成和 WGPU module validate”推进到运行时 mesh pipeline cache 消费链。`MeshPipelineCache` 现在持有 `geometry_source_descriptors` registry，启动时注册内建 descriptors，运行时可注册 plugin descriptor；Base/GBuffer/DepthPrepass/Shadow/Velocity/TAA reactive shader source 组装先按 `ShaderVariantKey.geometry_source` 解析 descriptor，再进入 staged cache lookup 和 WGPU pipeline 创建。插件 `[[geometry_sources]]` 也从 `RuntimeExtensionRegistry` 经过 `RuntimeModuleExtensionInputs`、`RuntimeModuleRegistrationInputs`、`GraphicsModule`、graphics module host、`WgpuRenderFramework`、`SceneRenderer` 到 `SceneRendererCore`，最终注册进 `MeshPipelineCache`，不再停留在 build/prewarm 辅助路径。 | Fresh `cargo check -q -p zircon_runtime --lib --target-dir target\codex-plan08-runtime-custom-geometry-cache-0629 --locked --jobs 1` 零退出，仅既有 warnings。Fresh focused default-feature WGPU lib-test `cargo test -q -p zircon_runtime --lib --target-dir target\codex-plan08-runtime-custom-geometry-cache-0629 --locked --jobs 1 graphics::scene::scene_renderer::mesh::mesh_pipeline_cache::ensure_pipeline::tests::runtime_custom_geometry_descriptor_pipeline_uses_staged_prewarm_without_compile_miss -- --exact --nocapture --test-threads=1` 零退出，`1 passed; 0 failed; 5463 filtered out`。该用例预热 `custom:virtual_geometry=4` descriptor 到 staged fallback root，在 `MeshPipelineCache` 注册同一 descriptor 后创建 Base mesh WGPU pipeline，并断言 runtime cache report `request=1/disk_hit=1/compile_miss=0/disk_write=0/disk_error=0`，geometry id `4` 维度同样为 `disk_hit=1/compile_miss=0`。针对本切片 Rust 文件的 `rustfmt --edition 2021 --check` 通过。 | 这关闭的是 runtime mesh pipeline 对 plugin geometry descriptor staged cache 的 Base WGPU pipeline hit 门禁。仍未声明 product virtual-geometry draw-source 路径、真实 page/cluster fetch/bindings、full live project/plugin shader resource export、RenderDoc/product capture 或 product second-launch miss=0 完成。 |
| 2026-06-29 | Plugin geometry descriptor runtime WGPU prewarm | render_plan08_plugin_geometry_descriptor_runtime_wgpu_prewarm_passed_product_deferred | Plan 08 MS-M4 把 selected plugin `[[geometry_sources]]` 从“只生成 custom id”推进到“生成完整 `geometry_source_descriptors` 并进入真实 WGPU shader-module validation”。`tools/zircon_build.py::resolve_config(...)` 现在允许 `--targets runtime --plugins virtual_geometry` 选择插件 shader/geometry 贡献而不触发插件打包，`tools/zircon_build_shader_prewarm.py::generated_shader_permutation_registry_document(...)` 输出 `geometry_source_descriptors`，`zircon_shader_prewarm` overlay 合并 descriptor 后通过 `dynamic_api::builtin_standard_material_shader_prewarm_manifest_for_geometry_descriptor(...)` 组装 descriptor WGSL include。新增 `zr_geometry_virtual_geometry.wgsl` 作为 `custom:virtual_geometry=4` 的 WGPU-valid 几何 include，并由 include registry 接入模板。 | `python -m unittest tools.tests.test_zircon_build_plugin_carriers.ZirconBuildPluginCarrierTests.test_zircon_build_selects_plugin_contributions_for_runtime_prewarm` 通过；干跑确认 runtime-only 命令包含 generated `shader_permutation_registry.json` 且不进入插件打包。真实命令 `python -u tools\zircon_build.py --targets runtime --plugins virtual_geometry --out target\codex-plan08-live-wgpu-prewarm-0629 --mode debug --prewarm-shaders --validate-wgpu-shaders` 零退出；报告 `requested_count=12`、`written_count=12`、`failed_count=0`，`wgpu_module_validation.enabled=true`、`requested=12`、`validated=12`、`failed=0`、`skipped=0`，`geometry_source_ids` 同时覆盖 `0` 与 `4` 各 6 个 pass；生成注册表包含 `custom:virtual_geometry=4` 与 `wgsl_include="zr_geometry_virtual_geometry.wgsl"` descriptor。 | 这关闭的是 selected-plugin geometry descriptor 到 runtime-only staged WGPU prewarm 的真实门禁。仍需 RenderDoc/product capture、full live project/plugin shader resource export、真实 VirtualGeometry page/cluster fetch 绑定实现、runtime/product fallback-root hit 与 second-launch miss=0；`zr_geometry_virtual_geometry.wgsl` 当前是 descriptor/WGPU 验证用 include，不宣称完整 Nanite-like VG 数据读取。 |
| 2026-06-29 | Build-tool source-label trim contract | render_plan08_build_tool_source_label_trim_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm source evidence 继续把 source provenance 与 written variant source identity 从“trim 后非空”收紧为“原始 wire 字符串必须已经 trim”。`tools/zircon_build_shader_prewarm_report_contract.py::_is_nonblank_string(...)` 现在要求值等于 `value.strip()`；`tools/zircon_build_shader_prewarm_written_variants.py` 新增 `_is_trimmed_nonblank_string(...)` 并拒绝 untrimmed `written_variants[].source_label`；`tools/zircon_build_shader_prewarm_acceptance.py` 在 success precheck 中同步拒绝 untrimmed source label，避免 `" res://... "` 与 `"res://... "` 这类值绕过 registry-backed locator 关联或在直接 cache helper 调用中伪装成 provenance match。新增 `test_validate_report_contract_rejects_untrimmed_source_provenance_strings`、`test_acceptance_contract_rejects_untrimmed_written_variant_source_label` 与 `test_validate_cache_artifact_contract_rejects_untrimmed_written_variant_source_label`；source provenance、acceptance、cache artifact Runtime 15 guards 同步锁定 helper/test/docs/status anchors。 | RED focused source-provenance test 先 3 个 subcase 失败为 `RuntimeError not raised`，RED focused acceptance 与 cache tests 先分别失败为 `RuntimeError not raised`；实现后三个 focused tests 1/1 通过，source+acceptance+cache 46/46 通过，build-helper 聚合 102/102 通过；py_compile、rustfmt、anchor/conflict/trailing-whitespace/line-budget scans 与 scoped `git diff --check` 通过，diff-check 仅报告 LF/CRLF warnings。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续仍需受控运行真实 `zircon_shader_prewarm --validate-wgpu-modules`、full live project/plugin shader resource export、RenderDoc/product capture、runtime/product fallback-root hit 与 second-launch miss=0；本切片只关闭 build-helper source-label/source metadata trim 规范合同。 |
| 2026-06-29 | Build-tool source-label nonblank contract | render_plan08_build_tool_source_label_nonblank_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm report/source evidence 继续把 source label 与 provenance metadata 从“非空字符串”收紧为“非空白字符串”。`tools/zircon_build_shader_prewarm_report_contract.py::_validate_source_provenance_contract(...)` 现在要求 `source_label`、`source_hash`、`template_revision` 都通过 `_is_nonblank_string(...)`；`tools/zircon_build_shader_prewarm_written_variants.py` 与 `tools/zircon_build_shader_prewarm_acceptance.py` 同步拒绝空白 `written_variants[].source_label`，避免空白 label 通过 provenance 关联并绕过 registry-backed locator 关联。新增 `test_validate_report_contract_rejects_blank_source_provenance_strings` 与 `test_acceptance_contract_rejects_blank_written_variant_source_label`；`runtime_15_shader_prewarm_source_provenance_report_contract_is_wired` 和 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused source-provenance test 先 3 个 subcase 失败为 `RuntimeError not raised`，RED focused acceptance test 先失败为 `RuntimeError not raised`；实现后两个 focused tests 1/1 通过，`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 43/43 通过。收尾 build-helper 聚合 99/99、py_compile、rustfmt、source/docs anchor scan、conflict/trailing-whitespace scan、line-budget scan 与 scoped `git diff --check` 均通过；diff-check 仅报告 LF/CRLF warnings。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续仍需受控运行真实 `zircon_shader_prewarm --validate-wgpu-modules`、full live project/plugin shader resource export、RenderDoc/product capture、runtime/product fallback-root hit 与 second-launch miss=0；本切片只关闭 build-helper source-label/source metadata 非空白合同。 |
| 2026-06-28 | Build-tool staged prewarm written cache-hash shape acceptance | render_plan08_build_tool_staged_prewarm_written_cache_hash_shape_python_passed_cargo_deferred | Plan 08 MS-M4 staged acceptance precheck 继续把 `written_variants[].cache_hash` 从“非空字符串”收紧为“64 字符小写十六进制 BLAKE3 形状”。`tools/zircon_build_shader_prewarm_acceptance.py::_validate_staged_shader_prewarm_written_variant_identity(...)` 复用共享 `validate_cache_hash_shape(...)`，在 report/cache/resource registry validators 之前拒绝 malformed cache identity。新增 `test_acceptance_contract_rejects_invalid_written_variant_cache_hash_shape`；`runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused acceptance test 先失败为 `RuntimeError not raised`，证明旧 precheck 可单独放过 `cache_hash="not-a-cache-key"`；实现后 focused test 1/1 通过，`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 38/38 通过。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续仍需受控运行真实 `zircon_shader_prewarm --validate-wgpu-modules`、full live project/plugin shader resource export、RenderDoc/product capture、runtime/product fallback-root hit 与 second-launch miss=0；本切片只关闭 staged acceptance written cache-hash shape precheck。 |
| 2026-06-28 | Build-tool cache metadata field type contract | render_plan08_build_tool_cache_metadata_field_type_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged cache artifact contract 继续把 `.meta` JSON 从“字段存在且值非空”收紧为“wire type 也必须可由 Rust/runtime cache owner 消费”。`tools/zircon_build_shader_prewarm_cache_artifacts.py::_validate_cache_metadata(...)` 现在要求 `schema_version` 与 `created_unix_seconds` 是非 bool integer，`canonical_string`、`template_revision`、`naga_version`、`wgpu_version` 是 string，避免 `schema_version=True` 被 Python 等同于 1、或错误类型字段延迟到 variant mismatch 才暴露。新增 `test_validate_cache_artifact_contract_rejects_invalid_metadata_field_types`；`runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused cache metadata field-type test 先失败：字符串字段只触发 `reported cache variant mismatch`，`created_unix_seconds` 错误类型未抛错，追加 `schema_version=True` 后也未抛错。实现后 focused field-type test 1/1 通过，`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 37/37 通过。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续仍需受控运行真实 `zircon_shader_prewarm --validate-wgpu-modules`、full live project/plugin shader resource export、RenderDoc/product capture、runtime/product fallback-root hit 与 second-launch miss=0；本切片只关闭 build-helper cache metadata field type contract。 |
| 2026-06-28 | Build-tool written variant uniqueness contract | render_plan08_build_tool_written_variant_uniqueness_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged cache/report acceptance 继续把 `written_variants` 从“字段完整且数量匹配”收紧为“写入 identity 集合本身唯一”。新增 `tools/zircon_build_shader_prewarm_written_variants.py`，把 `ReportedWrittenVariant`、BLAKE3 cache hash 形状、report `written_variants` 解析、source-label provenance 关联和 `validate_unique_written_variant_identity(...)` 从 cache artifact helper 抽成共享 owner；`tools/zircon_build_shader_prewarm_cache_artifacts.py` 与 `tools/zircon_build_shader_prewarm_acceptance.py` 现在都会拒绝重复 `cache_hash` 或重复 `canonical_string` 的 success report。新增 `test_validate_cache_artifact_contract_rejects_duplicate_written_variant_identity` 与 `test_acceptance_contract_rejects_duplicate_written_variant_identity`；`runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 和 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused cache/acceptance tests 先分别失败为 `RuntimeError not raised`，证明旧 build-helper 会接受重复 written variant identity；实现与 helper 抽取后 focused duplicate tests 各 1/1 通过。收尾验证：`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 36/36 通过；build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 95/95 通过；`py_compile`、`rustfmt --edition 2021 --check`、source/docs anchor scan、conflict/trailing-whitespace scan、line-budget scan 与 scoped `git diff --check` 均通过，diff-check 仅报告 LF/CRLF warning。行数：written variants helper 177、cache helper 572、acceptance helper 220、cache Python owner 612、acceptance Python owner 469、acceptance guard 217、cache guard 266。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续仍需受控运行真实 `zircon_shader_prewarm --validate-wgpu-modules`、full live project/plugin shader resource export、RenderDoc/product capture、runtime/product fallback-root hit 与 second-launch miss=0；本切片只关闭 build-helper written variant identity 唯一性合同。 |
| 2026-06-28 | Resource registry ready shader revision contract | render_plan08_resource_registry_ready_shader_revision_contract_python_static_passed_cargo_deferred | Plan 08 MS-M4 staged registry/report correlation 与 Rust prewarm overlay 继续从“Shader + revision 非零”收紧为“Shader + Ready + revision 非零”。`tools/zircon_build_shader_resource_registry.py::_is_usable_shader_record(...)` 现在要求 matching `ResourceRecord.state == "Ready"` 后才把 report source 视为可由 exported registry 覆盖；`bin/zircon_shader_prewarm/manifest/resource_registry.rs::ShaderPrewarmResourceRegistryOverlay::from_records(...)` 同步拒绝非 `ResourceState::Ready` 的 shader records，保持与 `ResourceManager::ready_records_for_kind(ResourceKind::Shader)` 一致。新增 `test_validate_registry_export_contract_rejects_non_ready_report_source_record` 与 `shader_prewarm_resource_registry_overlay_uses_ready_shader_revisions_only`；`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 和 `runtime_15_shader_prewarm_resource_registry_revision_overlay_is_wired` 同步锁定 helper、Rust owner、tests、docs/status anchors 与 800 行预算。 | Focused Python `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 19/19 通过；`python -m py_compile tools\zircon_build_shader_resource_registry.py tools\tests\test_zircon_build_shader_prewarm_resource_registry_contract.py` 通过。首轮 `rustfmt --edition 2021 --check zircon_runtime\src\bin\zircon_shader_prewarm\manifest\resource_registry.rs zircon_runtime\src\bin\zircon_shader_prewarm\manifest\tests.rs` 发现新增 Rust 回归不应继续加厚 800+ 行 `manifest/tests.rs`，已迁入 `manifest/resource_registry/tests.rs` 子 owner；最终 rustfmt、source/docs anchor、conflict/trailing-whitespace、line-budget 与 scoped `git diff --check` 均通过。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续仍需受控运行真实 `zircon_shader_prewarm --validate-wgpu-modules`、full live project/plugin shader resource export、RenderDoc/product capture、runtime/product fallback-root hit 与 second-launch miss=0；本切片只关闭 Ready-state registry revision 可用性合同。 |
| 2026-06-28 | Build-tool shader prewarm report dimension totals match contract | render_plan08_build_tool_report_dimension_totals_match_python_passed_cargo_deferred | Plan 08 MS-M4 staged report contract 继续把 dimension summary 证据链与 top-level report totals 对齐。`tools/zircon_build_shader_prewarm_report_contract.py` 新增 `_validate_dimension_summary_totals_match_report(...)` 与 `_dimension_group_totals(...)`，在 top-level `requested_count > 0` 时要求每个存在的 dimension group 的 requested/written/failed 汇总等于 report-level `requested_count`、`written_count`、`failed_count`；这样 `pass_types.forward` 自身 complete 但汇总为 `requested=7/written=7/failed=0`，而 top-level 为 `requested=6/written=6/failed=0` 的报告会被拒绝。新增 `test_validate_report_contract_rejects_dimension_count_total_mismatch`；`runtime_15_shader_prewarm_report_dimension_contract_is_wired` 同步锁定 helper、测试、docs/status anchors。 | RED focused dimension owner 先失败为 `RuntimeError not raised`，证明旧 report contract 接受 dimension summary 与 top-level totals 不一致；实现后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_dimension_contract` 8/8 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 92/92 通过；隔离 `PYTHONPYCACHEPREFIX` 后 `python -m py_compile tools\zircon_build_shader_prewarm_report_contract.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_report_dimension_contract.rs` 通过。行数：report helper 576、dimension test owner 226、Rust guard 186。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续真实 staged prewarm run 仍需证明所有 dimension groups 与 top-level report totals 一致，cache artifacts 全量命中 runtime fallback root，并继续关闭 RenderDoc/product capture、full live registry export 与 second-launch miss=0。 |

Runtime 15 report dimension guard anchor supplement: `test_validate_report_contract_requires_requested_pass_types` and `test_validate_report_contract_requires_requested_quality_tiers` remain the original requested-dimension coverage names tied to `runtime_15_shader_prewarm_report_dimension_contract_is_wired`.
| 2026-06-28 | Build-tool shader prewarm report dimension complete-count contract | render_plan08_build_tool_report_dimension_complete_counts_python_passed_cargo_deferred | Plan 08 MS-M4 staged report contract 继续从“requested dimension 存在”收紧到“requested dimension 全部写出且失败数为 0”。`tools/zircon_build_shader_prewarm_report_contract.py` 新增 `_incomplete_dimension_counts(...)`，`_validate_expected_pass_types(...)`、`_validate_expected_quality_tiers(...)`、`_validate_expected_geometry_sources(...)` 与 `_validate_expected_shader_dimension_ids(...)` 现在先保留 missing-requested 错误边界，再拒绝 `requested_count > 0` 但 `written_count != requested_count` 或 `failed_count != 0` 的 dimension summary。新增 `test_validate_report_contract_rejects_incomplete_requested_dimension_counts`，并把 positive fixture 补齐 written/failed counts；`runtime_15_shader_prewarm_report_dimension_contract_is_wired` 同步锁定 helper、测试、docs/status anchors 与当前 product material pass handoff。 | RED focused dimension owner 先失败为 `RuntimeError not raised`，证明旧 report contract 接受 `forward requested=6 written=5 failed=1`；首个实现把 incomplete 误归类为 missing，随后修正为 missing/requested 与 incomplete-count 分离；`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_dimension_contract` 7/7 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 91/91 通过；隔离 `PYTHONPYCACHEPREFIX` 后 `python -m py_compile tools\zircon_build_shader_prewarm_report_contract.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_report_dimension_contract.rs` 通过；source/docs anchors、conflict/trailing-whitespace、line-count 与 scoped `git diff --check` 通过，diff-check 仅报告 LF/CRLF warning。行数：report helper 595、dimension test owner 222、Rust guard 182。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 后续真实 staged prewarm run 仍需证明每个 requested pass/quality/geometry/custom id 的 dimension summary 都全量写出、cache artifacts 全量命中 runtime fallback root，并继续关闭 RenderDoc/product capture、full live registry export 与 second-launch miss=0。 |
| 2026-06-28 | Build-tool product material mesh pass acceptance contract | render_plan08_build_tool_product_material_mesh_pass_acceptance_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged acceptance 继续从较早 Base-only handoff 扩到完整产品 material mesh pass tuple。`tools/zircon_build_shader_prewarm_acceptance.py` 现在用 `_PRODUCT_MATERIAL_MESH_PASS_TYPES = ("forward", "gbuffer", "depth_prepass", "shadow", "velocity", "taa_reactive_mask")` 同时传入 report 与 cache validators，使 staged prewarm 成功不能只写 `forward` 就被当作完整产品 material cache。新增 `test_acceptance_contract_rejects_forward_only_staged_pass_report`，并扩展 `test_acceptance_contract_validates_report_cache_and_exported_registry` handoff 断言；`runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper、测试、docs/status anchors 与 TAA reactive mask pass token。 | RED focused acceptance owner 先失败，因为旧 helper 仍把 `("forward",)` 传给 validators；实现后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 12/12 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 90/90 通过；隔离 `PYTHONPYCACHEPREFIX` 后 `python -m py_compile tools\zircon_build_shader_prewarm_acceptance.py tools\tests\test_zircon_build_shader_prewarm_acceptance_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_acceptance_contract.rs` 通过；source/docs anchors、conflict/trailing-whitespace、line-count 与 scoped `git diff --check` 通过，diff-check 仅报告 LF/CRLF warning。行数：acceptance helper 209、acceptance test owner 486、Rust guard 189。Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓，且本切片不计真实 WGPU/Product 通过。 | 仍需在受控 WGPU 窗口运行真实 `zircon_shader_prewarm --validate-wgpu-modules`，证明六类 material mesh pass cache artifacts 被 runtime fallback root 命中，并继续关闭 RenderDoc/product capture、full live registry export 与 second-launch miss=0。 |
| 2026-06-28 | Build-tool resource registry-backed locator correlation | render_plan08_build_tool_resource_registry_backed_locator_correlation_python_passed_cargo_deferred | Plan 08 MS-M4 staged registry/report correlation 继续从 `res://` 扩到其他 registry-backed `ResourceLocator` schemes。`tools/zircon_build_shader_resource_registry.py` 新增 `_RESOURCE_REGISTRY_BACKED_LOCATOR_SCHEMES`，`_append_report_resource_source_label(...)` 现在复用 Rust-shaped locator parser，只把 `res://`、`lib://`、`package://` 与 `mem://` report source labels 纳入 `ResourceRecord.primary_locator` / `artifact_locator` 关联；`builtin://` 仍作为内建 shader source 跳过项目 registry export 要求。新增 `test_validate_registry_export_contract_accepts_registry_backed_source_locators` 与 `test_validate_registry_export_contract_rejects_missing_registry_backed_source_locator`，覆盖 `lib://plugin/shaders/example`、`package://demo/shaders/example` 与 `mem://generated/shader`；`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused registry owner 先失败为 `RuntimeError not raised`，证明旧 correlation 只收集 `res://` 并漏过 `lib://` / `package://` 缺失记录；实现后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 18/18 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 89/89 通过；隔离 `PYTHONPYCACHEPREFIX` 后 `python -m py_compile tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_resource_registry.py tools\tests\test_zircon_build_shader_prewarm_resource_registry_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_resource_registry_report_correlation.rs` 通过；anchor/conflict/trailing-whitespace/line-budget scans 通过；scoped `git diff --check` 仅报告 LF/CRLF warning。行数：prewarm helper 495、registry helper 427、registry test owner 475、Rust guard 235，均低于 800 行预算。Cargo/WGPU/RenderDoc/product gates 仍因外部 cargo/rustc 通道 active 暂缓。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper registry-backed locator correlation contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool resource registry locator wire-shape contract | render_plan08_build_tool_resource_registry_locator_wire_shape_python_passed_cargo_deferred | 在 ResourceRecord wire-shape、enum 与 numeric width 合同基础上继续对齐 Rust `ResourceLocator::parse(...)`。`tools/zircon_build_shader_resource_registry.py::_is_resource_locator_string(...)` 现在只接受 `res/lib/package/builtin/mem` scheme，拒绝空路径、根路径、Windows drive prefix、越界 `..`、空 label 和缺少 package 内路径；`package://` 额外要求单段 package id 加相对 package path。新增 `test_validate_registry_export_contract_rejects_invalid_locator_wire_shape`、`test_validate_registry_export_contract_accepts_locator_wire_shape_variants` 与 `test_validate_registry_export_contract_rejects_invalid_artifact_locator`，覆盖 primary/artifact locator 负例与 `res/lib/package/builtin/mem` 正例。`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步锁定 locator helper、测试、docs/status anchors 与 800 行预算。 | RED focused registry owner 先失败为 `RuntimeError not raised` 和正例拒绝，证明旧 helper 只检查 `://` 且未按 Rust locator 规则处理；修复后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 16/16 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 87/87 通过；隔离 `PYTHONPYCACHEPREFIX` 后 `python -m py_compile tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_resource_registry.py tools\tests\test_zircon_build_shader_prewarm_resource_registry_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_resource_registry_report_correlation.rs` 通过；anchor/conflict/trailing-whitespace scans 通过；scoped `git diff --check` 仅报告 LF/CRLF warning。行数：prewarm helper 495、registry helper 416、registry test owner 419、Rust guard 223，均低于 800 行预算。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper ResourceLocator wire-shape contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool resource registry numeric width contract | render_plan08_build_tool_resource_registry_numeric_width_python_passed_cargo_deferred | 在 ResourceRecord wire-shape 合同基础上继续对齐 Rust 数值宽度。Rust `ResourceRecord.revision` 是 `u64`，`importer_version` 是 `u32`；Python 之前只检查非负整数，会接受 `2**64` 或 `2**32` 这类 JSON number，随后 Rust serde 无法反序列化。`tools/zircon_build_shader_resource_registry.py` 新增 `_U64_MAX/_U32_MAX` 与 `_is_unsigned_int_within(...)`，`revision` 必须落在 `0..=u64::MAX`，`importer_version` 必须落在 `0..=u32::MAX`；新增 `test_validate_registry_export_contract_rejects_u64_revision_overflow` 与 `test_validate_registry_export_contract_rejects_u32_importer_version_overflow`。`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused registry owner 先失败为两个 `RuntimeError not raised`，证明旧 helper 放行了超出 Rust 宽度的数值；修复后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 13/13 通过，build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 84/84 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_resource_registry.py tools\tests\test_zircon_build_shader_prewarm_resource_registry_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_resource_registry_report_correlation.rs` 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper numeric width contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool resource registry enum wire-shape contract | render_plan08_build_tool_resource_registry_enum_wire_shape_python_passed_cargo_deferred | 在 ResourceRecord wire-shape 合同基础上继续收紧 `kind/state` 的 serde 形状。Rust `zircon_runtime_interface::resource::{ResourceKind, ResourceState}` 是普通 serde unit enum，JSON 应为 `"Shader"` / `"Ready"` 字符串，而不是 `{ "type": "Shader" }` 或 `{ "state": "Ready" }` 这类 tagged object。`tools/zircon_build_shader_resource_registry.py::_resource_record_kind_is_known(...)` 与 `_resource_record_state_is_known(...)` 现在只接受字符串枚举，`_resource_record_kind_is_shader(...)` 也只接受 `"Shader"`；新增 `test_validate_registry_export_contract_rejects_tagged_enum_resource_record`，构造完整 ResourceRecord 但把 kind/state 写成 object，确保 build-helper 不再接受 Rust 不能反序列化的 tagged enum wire shape。`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步锁定 helper、测试、docs/status anchors 与 800 行预算。 | RED focused registry owner 先失败为 dict enum 引起的 `TypeError: cannot use 'dict' as a set element`，证明旧 helper 未按 Rust enum wire shape 正常拒绝；修复后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 11/11 通过，build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 82/82 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_resource_registry.py tools\tests\test_zircon_build_shader_prewarm_resource_registry_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_resource_registry_report_correlation.rs` 通过；conflict/trailing-whitespace scans 与 scoped `git diff --check` 通过，diff-check 仅报告 LF/CRLF warning。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper enum wire-shape contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool resource registry ResourceRecord wire-shape contract | render_plan08_build_tool_resource_registry_record_shape_python_passed_cargo_deferred | Plan 08 MS-M4 staged registry/export validation 继续从“JSON object”收紧为“可按 Rust `zircon_runtime_interface::resource::ResourceRecord` 反序列化的 wire shape”。新增 `tools/zircon_build_shader_resource_registry.py` 作为 registry export contract owner，`tools/zircon_build_shader_prewarm.py` 只保留 `validate_shader_resource_registry_export_contract(...)` 入口导入，避免把 ResourceRecord 结构校验继续堆入 prewarm 编排文件。该 helper 现在要求每个 registry entry 含 `id/kind/primary_locator/artifact_locator/revision/state/dependency_ids/diagnostics/source_hash/importer_id/importer_version/config_hash`，其中 `id` 和 dependency ids 必须是 UUID 字符串，`kind/state` 必须是已知枚举，`artifact_locator` 必须出现且可为 null，diagnostics 必须是 severity/message 记录，`importer_version` 与 `revision` 必须是非负整数。新增 `test_validate_registry_export_contract_rejects_incomplete_resource_record`，并把正例 fixture 改为完整 ResourceRecord wire record。结构守卫 `runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 现在读取新 helper owner 并锁定字段、错误、测试与 800 行预算；当前行数：prewarm helper 495、registry helper 370、registry test owner 272、Rust guard 168。 | 红跑 focused registry owner 先失败为 `AssertionError: RuntimeError not raised`，证明旧 helper 接受缺字段 registry record；实现和拆分后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 10/10 通过，build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 81/81 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_resource_registry.py tools\tests\test_zircon_build_shader_prewarm_resource_registry_contract.py` 通过；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_resource_registry_report_correlation.rs` 通过；anchor/conflict/trailing-whitespace scans 通过；scoped `git diff --check` 仅报告 LF/CRLF warning。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper ResourceRecord wire-shape contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool resource registry contract test owner split | render_plan08_build_tool_resource_registry_contract_tests_owner_split_python_passed_cargo_deferred | Plan 08 MS-M4 staged registry/report correlation 的 Python regression owner 已从通用 `tools/tests/test_zircon_build_shader_prewarm.py` 拆到 `tools/tests/test_zircon_build_shader_prewarm_resource_registry_contract.py`，承接 ResourceRecord container、report provenance locator、written variant source label、usable Shader revision 和 builtin-source skip 等 9 个 registry contract 用例。通用 build-helper owner 不再承载 `test_validate_registry_export_contract_*`，行数从 759 降到 540；新 registry owner 234 行。`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步读取新 owner，锁定正向/负向锚点和 800 行预算，避免后续继续把 registry/report 测试塞回通用 owner。 | `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract` 通过 30/30；拆分后 build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_resource_registry_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 80/80 通过。 | 本切片只是测试 ownership/结构预算收口，不计真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 或 second-launch miss=0 通过。 |
| 2026-06-28 | Build-tool resource registry usable shader revision contract | render_plan08_build_tool_resource_registry_usable_shader_revision_python_passed_cargo_deferred | Plan 08 MS-M4 staged registry/report correlation 继续从“locator 存在”收紧为“locator 对应可用于 shader revision overlay 的 `ResourceRecord`”。`tools/zircon_build_shader_resource_registry.py::_validate_registry_export_matches_report_sources(...)` 先用 `_resource_record_locators(...)` 保持原 missing-locator 错误边界，再用 `_usable_shader_resource_record_locators(...)` 要求匹配记录通过 `_is_usable_shader_record(...)`：`kind` 必须表示 Shader，`revision` 必须是正整数；这样与 Rust `ShaderPrewarmResourceRegistryOverlay::from_records(...)`/`ResourceManager::ready_records_for_kind(ResourceKind::Shader)` 的消费条件一致。新增 `test_validate_registry_export_contract_rejects_non_shader_report_source_record` 与 `test_validate_registry_export_contract_rejects_zero_revision_report_source_record`，并把正例 fixture 改为 `kind=Shader/revision=1`。`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步锁定 helper、test、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` 先失败为两个 `RuntimeError not raised`，证明旧 helper 接受了同 locator 但非 Shader 或 revision=0 的 registry 记录；实现后同一 owner 30/30 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper usable shader ResourceRecord revision contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool resource registry written source-label correlation | render_plan08_build_tool_resource_registry_written_source_label_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm registry/report correlation 继续从 provenance 扩到实际写入结果。`tools/zircon_build_shader_resource_registry.py::_report_resource_source_labels(...)` 现在同时读取 `source_provenance.sources[*].source_label` 与 `written_variants[].source_label`，经 `_append_report_resource_source_label(...)` 去重后，只要求 `res://` shader source 出现在导出的 `ResourceRecord.primary_locator` 或 `artifact_locator` 中；`builtin://...` 与 raw/path-like source 仍不进入 `.zmeta` registry export 要求。新增 `test_validate_registry_export_contract_rejects_missing_written_variant_locator`，构造只有 `written_variants[].source_label = res://shaders/example`、没有 registry locator 的成功报告，确保 auto-exported registry 不能漏掉实际写入 cache 的 shader 资源。`runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired` 同步锁定 helper、test、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` 先失败为 `RuntimeError not raised`，证明旧 registry/report correlation 只看 source provenance 而没有覆盖 written variants；实现后同一 owner 28/28 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 78/78 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile ...` 覆盖 prewarm/report/cache/acceptance helpers 与相关 Python owners 通过；`rustfmt --edition 2021 --check` 覆盖 `shader_prewarm_resource_registry_report_correlation.rs` 通过；anchor/conflict/trailing-whitespace scans 通过；scoped `git diff --check` 仅报告文档/工具 LF/CRLF warning。行数：prewarm helper 634、prewarm Python owner 693、Rust guard 104，均低于 800 行 owner 预算。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper resource-registry written source-label correlation contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool staged prewarm written source-label identity acceptance | render_plan08_build_tool_staged_prewarm_written_source_label_identity_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm acceptance 继续把 zero-exit success report 的 written cache identity 预检补齐到 source label。`tools/zircon_build_shader_prewarm_acceptance.py::_validate_staged_shader_prewarm_written_variant_identity(...)` 现在把 `source_label` 纳入 required fields，要求每个 `written_variants[]` entry 在进入 report/source/cache/registry 下层 validators 前已经携带 cache hash、canonical string、source label、template revision、Naga/WGPU version。新增 `test_acceptance_contract_requires_written_variant_source_label_identity`，构造成功报告缺 `source_label` 的 written variant，确保错误停在 staged acceptance identity gate，而不是后续 source-provenance validator。`runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper field、test、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 先失败为旧的 `shader prewarm report did not confirm shader source provenance`，证明 acceptance identity 预检没有覆盖 `source_label`；实现后同一 owner 11/11 通过。补充验证：focused cache+acceptance `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 33/33 通过；build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 77/77 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm_acceptance.py tools\zircon_build_shader_prewarm_cache_artifacts.py tools\tests\test_zircon_build_shader_prewarm_acceptance_contract.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py` 通过；scoped `rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_acceptance_contract.rs` 通过；anchor/conflict/trailing-whitespace scans 通过；scoped `git diff --check` 仅报告文档 LF/CRLF warning。行数：acceptance helper 202、acceptance Python owner 432、cache helper 774、cache Python owner 640、acceptance Rust guard 175、cache Rust guard 242，均低于 800 行 owner 预算。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper staged acceptance source-label identity contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool cache source-label provenance correlation contract | render_plan08_build_tool_cache_source_label_provenance_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm cache artifact contract 继续把 written cache identity 与 source provenance 对齐。`tools/zircon_build_shader_prewarm_cache_artifacts.py` 现在解析 `written_variants[].source_label`，在报告含 `source_provenance.sources` 时调用 `_validate_written_variant_source_labels(...)`，要求每个 written variant 的 source label 非空且存在于 `source_provenance.sources[*].source_label` 集合；缺失或指向未记录 source 的 cache variant 会在 cache artifact contract 阶段失败。新增 `test_validate_cache_artifact_contract_requires_written_variant_source_labels_in_provenance`，构造 `.meta`/cache identity 全匹配但 `written_variants[].source_label` 指向 `res://materials/missing-source.wgsl`、provenance 只声明 `res://materials/source-a.wgsl` 的报告。`runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 同步锁定 helper、error、test、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract` 先失败为 `RuntimeError not raised`，证明旧 cache contract 只验证 `.meta` identity 而没有把 written source label 与 provenance source table 关联；实现后同一 owner 22/22 通过。补充验证：focused cache+acceptance `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 32/32 通过；build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 76/76 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm_cache_artifacts.py tools\zircon_build_shader_prewarm_acceptance.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py tools\tests\test_zircon_build_shader_prewarm_acceptance_contract.py` 通过；scoped `rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_cache_artifact_contract.rs` 通过；anchor/conflict/trailing-whitespace scans 通过；scoped `git diff --check` 仅报告文档 LF/CRLF warning。行数：cache helper 774、acceptance helper 201、cache Python owner 640、acceptance Python owner 408、Rust guard 242，均低于 800 行 owner 预算，但 cache helper 已接近拆分阈值。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper cache source-label/provenance correlation contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool cache custom id combination contract | render_plan08_build_tool_cache_custom_id_combination_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm cache artifact contract 继续把 selected plugin/CLI custom id 从“单独出现过”收紧为“与请求的 pass/quality 同时出现在同一个 cache identity”。`tools/zircon_build_shader_prewarm_cache_artifacts.py` 新增 `_validate_expected_written_custom_id_combinations(...)`、`_custom_id_combination_matches(...)` 与 `_custom_id_combination_label(...)`，在 geometry/shading 单维度 custom id 检查之后，要求 requested custom geometry id 与 custom shading id 的组合出现在 `written_variants[].canonical_string` 中；当 `expected_pass_types` / `expected_quality_tiers` 存在时，同一个写出变体也必须同时覆盖这些维度。新增 `test_validate_cache_artifact_contract_requires_requested_custom_id_combinations`，构造 `forward/high/geometry=0/shading=0` 与 `shadow/medium/geometry=4/shading=16` 两个写出变体，验证 staged acceptance 期望 `forward x high x geometry=4 x shading=16` 时必须失败。`runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 同步锁定 helper、error、test、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract` 先失败为 `RuntimeError not raised`，证明旧 cache contract 只独立检查 custom geometry/shading id 而接受了缺少 exact custom-id product 的 cache；实现后同一 owner 21/21 通过。补充验证：focused cache+acceptance `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 31/31 通过；build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 75/75 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm_cache_artifacts.py tools\zircon_build_shader_prewarm_acceptance.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py tools\tests\test_zircon_build_shader_prewarm_acceptance_contract.py` 通过；scoped `rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_cache_artifact_contract.rs` 通过。行数：cache helper 718、acceptance helper 201、cache Python owner 573、acceptance Python owner 408、Rust guard 232。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper cache custom id combination contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool cache dimension combination contract | render_plan08_build_tool_cache_dimension_combination_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm cache artifact contract 继续把单维度 identity 检查收紧为组合检查。`tools/zircon_build_shader_prewarm_cache_artifacts.py` 新增 `_validate_expected_written_variant_combinations(...)` 与 `_canonical_dimension_values(...)`，在 pass/quality/geometry 单维度检查之后，要求 `written_variants[].canonical_string` 覆盖 requested `pass x quality x built-in geometry` 组合，而不是只分别出现过这些维度。新增 `test_validate_cache_artifact_contract_requires_requested_dimension_combinations`，构造 `pass=forward|quality=high|geometry=0` 与 `pass=shadow|quality=medium|geometry=1` 两个写出变体，验证当 staged acceptance 期望 `forward x high x skinned` 时必须失败。`runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 同步锁定 helper、test、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract` 先失败为 `RuntimeError not raised`，证明旧 cache contract 只检查单维度存在而接受了缺少 exact combination 的 cache；实现后同一 owner 20/20 通过。补充验证：focused cache+acceptance `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 30/30 通过；build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 74/74 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm_cache_artifacts.py tools\zircon_build_shader_prewarm_acceptance.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py tools\tests\test_zircon_build_shader_prewarm_acceptance_contract.py` 通过；scoped `rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_cache_artifact_contract.rs` 通过。行数：cache helper 559、acceptance helper 181、cache Python owner 484、acceptance Python owner 361、Rust guard 217。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper cache combination contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool cache quality/geometry identity contract | render_plan08_build_tool_cache_quality_geometry_identity_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm cache artifact contract 继续把 report-level requested dimensions 压到实际 written cache identity。`tools/zircon_build_shader_prewarm_acceptance.py` 现在把 `config.shader_quality_tiers` 与 `config.shader_geometry_sources` 传给 `validate_shader_prewarm_cache_artifact_contract(...)`；`tools/zircon_build_shader_prewarm_cache_artifacts.py` 新增 `expected_quality_tiers`、`expected_geometry_sources`、`_validate_expected_written_quality_tiers(...)`、`_validate_expected_written_geometry_sources(...)` 与 `_geometry_source_dimension_id(...)`，要求 `written_variants[].canonical_string` 至少覆盖本次 build 请求的 `quality=<tier>` 和内建 `geometry=<id>`。这样 report 虽然在 `dimension_summary` 声称请求了 `high/skinned`，但实际写出的 cache key 只有 `medium/static` 时，会在 cache artifact contract 阶段失败。扩展 `tools/tests/test_zircon_build_shader_prewarm_cache_contract.py` 与 `tools/tests/test_zircon_build_shader_prewarm_acceptance_contract.py`，并扩展 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 锁定 helper、handoff、tests、docs/status anchors 与 line budgets。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 先失败为 `unexpected keyword argument 'expected_quality_tiers'`、`unexpected keyword argument 'expected_geometry_sources'` 和 acceptance mock 缺少 handoff 参数，证明旧路径没有把 requested quality/builtin geometry 压到 cache identity；实现后同一组合 29/29 通过，`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm_cache_artifacts.py tools\zircon_build_shader_prewarm_acceptance.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py tools\tests\test_zircon_build_shader_prewarm_acceptance_contract.py` 通过。补充验证：build-helper 聚合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 73/73 通过；完整 py_compile 覆盖 build/prewarm/report/cache/acceptance helpers 与 8 个 Python test owners；scoped `rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_cache_artifact_contract.rs` 通过；anchor/conflict/trailing-whitespace scans 通过；scoped `git diff --check` 仅报告文档 LF/CRLF warning；行数：cache helper 503、acceptance helper 181、cache Python owner 427、acceptance Python owner 361、Rust guard 208。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper cache identity contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool product Base pass acceptance contract | render_plan08_build_tool_product_base_pass_acceptance_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm acceptance 继续把 build 产物与产品 Base/Opaque 消费路径对齐：`tools/zircon_build_shader_prewarm_acceptance.py` 新增 `_PRODUCT_BASE_MESH_PASS_TYPES = ("forward",)`，成功验收时把 `expected_pass_types` 传给 report contract 与 cache artifact contract。`tools/zircon_build_shader_prewarm_report_contract.py::validate_shader_prewarm_report_contract(...)` 现在会在要求 pass 类型时检查 `dimension_summary.pass_types.forward.requested_count > 0`；`tools/zircon_build_shader_prewarm_cache_artifacts.py::validate_shader_prewarm_cache_artifact_contract(...)` 现在会检查 `written_variants[].canonical_string` 中至少有 `pass=forward` 的写出变体。这样一个只写出 Shadow/TAA 等非 Base/Opaque pass 的成功 report/cache，不能再满足产品 Base mesh staged prewarm 的 build-helper 验收。扩展 `tools/tests/test_zircon_build_shader_prewarm_dimension_contract.py`、`tools/tests/test_zircon_build_shader_prewarm_cache_contract.py`、`tools/tests/test_zircon_build_shader_prewarm_acceptance_contract.py` 与三个 Runtime 15 structure guards，锁定 pass handoff、line budgets 与 docs/status anchors。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 先失败为 `unexpected keyword argument 'expected_pass_types'` / mock 缺少 `expected_pass_types`，证明旧路径不会检查产品 pass 维度；实现后同一组合 32/32 通过。扩展组合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 70/70 通过；`python -m py_compile` 覆盖 report/cache/acceptance helpers 通过；`rustfmt --edition 2021 --check` 覆盖 report dimension/cache artifact/acceptance structure guards 通过。行数：report helper 551、cache helper 473、acceptance helper 199、dimension/cache/acceptance Python owners 167/390/404、三个 Rust guards 168/193/168。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、完整产品 fallback-root hit 与 second-launch miss=0；本切片只是 build-helper product pass handoff contract，不计真实 WGPU/Product 通过。 |
| 2026-06-28 | Build-tool WGPU validation totals match contract | render_plan08_build_tool_wgpu_validation_totals_match_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm report contract 继续收紧 WGPU module validation 证据链：`tools/zircon_build_shader_prewarm_report_contract.py::_validate_wgpu_module_validation_contract(report)` 现在在确认 `enabled=true`、`validated_count == requested_count`、`failed_count == 0`、`skipped_count == 0` 后，还会在 top-level `requested_count > 0` 时要求 `wgpu_module_validation.requested_count == report.requested_count`、`validated_count == report.written_count`、`failed_count == report.failed_count`。这样 top-level 声称请求/写出 6 个 shader variants，但 WGPU validation 只验证 5 个 module 的报告会被拒绝。新增专门 test owner `tools/tests/test_zircon_build_shader_prewarm_wgpu_report_contract.py`，迁出既有 WGPU report-contract tests，并新增 `test_validate_report_contract_rejects_wgpu_validation_total_mismatch`；`runtime_15_shader_prewarm_wgpu_report_contract_is_wired` 现在读取该 owner，并断言 WGPU report-contract regressions 不回流到通用 prewarm test owner。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract` 先失败为 `RuntimeError not raised`，证明旧 report contract 接受 report totals 与 WGPU validation totals 不一致；实现后该 owner 3/3 通过，`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_wgpu_report_contract tools.tests.test_zircon_build_shader_prewarm` 30/30 通过，`PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build_shader_prewarm_report_contract.py tools\tests\test_zircon_build_shader_prewarm_wgpu_report_contract.py tools\tests\test_zircon_build_shader_prewarm.py` 通过，`rustfmt --edition 2021 --check` 覆盖 WGPU report guard 与 `test_file_budget/mod.rs` 通过。拆分后通用 owner 653 行，新 WGPU report owner 87 行，report contract helper 513 行。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是 build-helper WGPU report contract，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool source provenance report test owner split | render_plan08_build_tool_source_provenance_report_tests_owner_split_python_passed_cargo_deferred | Plan 08 MS-M4 source provenance report-contract tests 已从接近 800 行的通用 `tools/tests/test_zircon_build_shader_prewarm.py` 拆到专门 owner `tools/tests/test_zircon_build_shader_prewarm_source_provenance_contract.py`。新文件承接 `test_validate_report_contract_requires_source_provenance_when_requested`、`test_validate_report_contract_accepts_source_provenance_counts`、`test_validate_report_contract_rejects_source_provenance_count_mismatch`；`runtime_15_shader_prewarm_source_provenance_report_contract_is_wired` 现在读取新 test owner，并断言这些 source-provenance regressions 不回流到通用 prewarm test owner。该切片不改变 report contract 行为，只关闭后续 Plan 08 验收继续堆进 781 行 owner 的结构风险。 | 拆分后 `tools/tests/test_zircon_build_shader_prewarm.py` 从 781 行降到 694 行，新 source provenance test owner 101 行，source provenance guard 114 行；`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm` 32/32 通过；组合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_source_provenance_contract tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 66/66 通过；`rustfmt --edition 2021 --check` 覆盖 source provenance guard 与 `test_file_budget/mod.rs` 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是测试 owner split，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool source provenance totals match contract | render_plan08_build_tool_source_provenance_totals_match_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm report contract 继续收紧 source provenance 证据链：`tools/zircon_build_shader_prewarm_report_contract.py::_validate_source_provenance_contract(report)` 现在汇总 `source_provenance.sources[*].requested/written/failed`，并要求它们与 top-level `requested_count`、`written_count`、`failed_count` 一致。这样 top-level 声称 `failed=0`、`written=requested`，但 source provenance 内部记录某个 shader source `failed_count=1` 的报告会被拒绝。新增 `test_validate_report_contract_rejects_source_provenance_count_mismatch`，扩展 `runtime_15_shader_prewarm_source_provenance_report_contract_is_wired` 锁定 helper error、Python test、docs/status anchors 与 800 行预算。 | 红跑 focused Python 先失败为 `RuntimeError not raised`，证明旧 source provenance contract 接受了 top-level 成功但 provenance 内部失败的报告；实现后 focused mismatch/positive tests 2/2 通过，组合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 66/66 通过；`rustfmt --edition 2021 --check` 覆盖 source provenance guard 与 `test_file_budget/mod.rs` 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是 build-helper report contract，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool staged prewarm complete written count acceptance | render_plan08_build_tool_staged_prewarm_complete_written_count_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm acceptance 继续把 zero-exit 成功定义从“报告列出写入变体 identity”收紧到“所有 requested variants 必须全部写出”。`tools/zircon_build_shader_prewarm_acceptance.py::validate_staged_shader_prewarm_nonempty_success_report(config)` 现在在 `failed_count == 0` 且进入 written-variant identity 校验前，要求 `written_count == requested_count`；这样 `requested=2`、`written=1`、`failed=0` 且 `written_variants` 只覆盖一个 cache key 的报告不会被后续 source/cache 校验当作完整 staged success。新增 `test_acceptance_contract_rejects_partial_written_success_report`，扩展 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 锁定 helper error、Python test、docs/status anchors、`runtime fallback root` 语义与 800 行预算。 | 红跑 focused Python 先失败为旧 source-provenance 错误，证明 acceptance 入口没有优先拒绝部分写入成功报告；实现后 focused test 通过，`PYTHONDONTWRITEBYTECODE=1 python -m py_compile ...` 覆盖 build/prewarm/report/cache/acceptance helpers 与 6 个 Python test owners 通过；组合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 65/65 通过；`rustfmt --edition 2021 --check` 覆盖 acceptance guard 与 `test_file_budget/mod.rs` 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是 build-helper acceptance gate，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool staged prewarm written variant identity acceptance | render_plan08_build_tool_staged_prewarm_written_variant_identity_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm acceptance 继续把 zero-exit 成功定义从“非空写出”收紧到“报告必须列出每个写入变体的 exact cache identity”。`tools/zircon_build_shader_prewarm_acceptance.py::validate_staged_shader_prewarm_nonempty_success_report(config)` 现在同时要求 `written_variants` 是列表、长度等于 `written_count`，且每个条目包含非空字符串 `cache_hash`、`canonical_string`、`template_revision`、`naga_version`、`wgpu_version`；具体 `.meta` 对齐仍由既有 `validate_shader_prewarm_cache_artifact_contract(...)` 负责。新增 `test_acceptance_contract_requires_written_variant_identity` 与 `test_acceptance_contract_rejects_incomplete_written_variant_identity`，扩展 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 锁定 helper、tests、docs/status anchors 与 800 行预算。 | 红跑 focused Python 先失败为旧 source provenance 错误，证明 acceptance 入口未优先要求 written cache variants；实现后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 9/9 通过；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile ...` 覆盖 build/prewarm/report/cache/acceptance helpers 与 6 个 Python test owners 通过；组合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 64/64 通过；`rustfmt --edition 2021 --check` 覆盖 acceptance guard 与 `test_file_budget/mod.rs` 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是 build-helper acceptance gate，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool staged prewarm nonempty success report acceptance | render_plan08_build_tool_staged_prewarm_nonempty_success_report_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm acceptance 现在在 runtime fallback root layout gate 之后、report/cache/registry 细项校验之前，先拒绝“zero-exit 但没有真实写出 shader 变体”的成功报告。`tools/zircon_build_shader_prewarm_acceptance.py::validate_staged_shader_prewarm_nonempty_success_report(config)` 读取 `shader_variants_report.json`，要求 `requested_count > 0`、`written_count > 0` 且 `failed_count == 0`，避免下层 cache helper 对 `written_count <= 0` 的兼容返回让空成功报告进入后续验收。新增 `test_acceptance_contract_rejects_empty_success_report` 与 `test_acceptance_contract_rejects_failed_success_report`，扩展 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 锁定 helper、tests、docs/status anchors、runtime fallback root 语义与 800 行预算。 | 红跑 focused Python 先失败为旧的 source provenance 错误，证明 acceptance 入口没有优先表达“必须写出变体”的成功报告契约；实现后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 7/7 通过，`PYTHONDONTWRITEBYTECODE=1 python -m py_compile ...` 覆盖 build/prewarm/report/cache/acceptance helpers 与 6 个 Python test owners 通过；组合 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 62/62 通过；`rustfmt --edition 2021 --check` 覆盖 acceptance guard 与 `test_file_budget/mod.rs` 通过。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是 build-helper acceptance gate，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool shader asset-root plan visibility fallback handoff extension | render_plan08_build_tool_shader_asset_root_plan_visibility_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm dry-run/plan 输出继续从 asset-root 可见性扩展到 runtime fallback handoff path 可见性。`tools/zircon_build_shader_prewarm.py::print_shader_prewarm_plan(config)` 现在同时打印 `shader prewarm cache root`、`shader prewarm report` 与 `shader runtime fallback root`，让受控 WGPU 窗口前能直接审查 staged build 将写到 `ZirconEngine/cache/shader_variants`，report 将写到 `ZirconEngine/cache/shader_variants_report.json`，并与 runtime fallback root 同源。新增 `test_prewarm_plan_lists_runtime_fallback_handoff_paths`，扩展 `runtime_15_shader_prewarm_asset_root_plan_visibility_is_wired` 锁定输出、测试、docs/status anchors 与 800 行预算。 | 红跑 focused Python 先失败，输出仅有 asset roots 与 resource-registry export；实现后 focused test 通过，focused `py_compile` 通过，`rustfmt --edition 2021 --check` 覆盖 asset-root plan visibility guard 与 `test_file_budget/mod.rs` 通过。收尾验证：`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 60/60 通过；py_compile、rustfmt、per-doc anchors、conflict/trailing-whitespace、line-budget 与 scoped `git diff --check` 通过，diff-check 只报告 LF/CRLF warning。 | 后续仍需真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；本切片只是 plan visibility，不计真实 WGPU 通过。 |
| 2026-06-28 | Build-tool staged prewarm runtime fallback layout verification closeout | render_plan08_build_tool_staged_prewarm_runtime_fallback_layout_python_passed_cargo_deferred | 收尾验证补齐 runtime fallback root layout gate 后的组合边界。`tools/zircon_build_shader_prewarm_acceptance.py` 现在在 acceptance bundle 开头先拒绝 staged cache/report/auto registry path drift；`tools/tests/test_zircon_build_shader_prewarm_acceptance_contract.py` 的 layout drift/acceptance tests 纳入原 build-helper 组合套件；`runtime_15_shader_prewarm_acceptance_contract_is_wired` 同步锁定 helper、tests、docs/status anchors 与 line budgets。 | 通过：`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 5/5；`PYTHONDONTWRITEBYTECODE=1 python -m py_compile ...` 覆盖 build/prewarm/report/cache/acceptance helpers 与 6 个 Python test owners；`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 59/59；`rustfmt --edition 2021 --check` 覆盖 acceptance guard、report dimension guard 和 `test_file_budget/mod.rs`；helper/per-doc source/docs anchor scan、conflict/trailing-whitespace scan、line-budget scan 与 scoped `git diff --check` 通过，diff-check 只报告 LF/CRLF warning。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0；当前另有 editor Cargo/rustc lanes 活跃，本切片不抢占。 |
| 2026-06-28 | Build-tool staged prewarm runtime fallback layout contract | render_plan08_build_tool_staged_prewarm_runtime_fallback_layout_python_passed_cargo_deferred | Plan 08 MS-M4 staged prewarm acceptance 现在先校验 staged output path 与 runtime fallback root 默认约定同源，避免真实 WGPU run 成功写入却被产品路径当作 miss。`tools/zircon_build_shader_prewarm_acceptance.py::validate_staged_shader_prewarm_runtime_fallback_layout(config)` 要求 `shader_prewarm_cache_root == engine_root/cache/shader_variants`，要求 report 位于 `engine_root/cache/shader_variants_report.json`，并在自动 export resource registry 时要求 `engine_root/cache/shader_resource_records.json`。该检查在 report/cache/registry 内容校验前执行。新增 `test_acceptance_contract_rejects_runtime_fallback_layout_drift` 与 `test_acceptance_contract_accepts_runtime_fallback_layout`，并扩展 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 锁定 runtime fallback root layout、tests、docs/status anchors 与 800 行预算。 | 红跑 focused acceptance drift test 先失败为缺失 report 错误，证明路径漂移未被优先拒绝；实现后 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` 5/5 通过，focused `py_compile` 通过。Cargo/WGPU/RenderDoc/product miss=0 因 active compile lanes 未运行也不计通过。 | 后续真实 WGPU 窗口需要运行完整 staged prewarm 命令，确认该默认 `ZirconEngine/cache/shader_variants` 能被 runtime/product `ShaderVariantCacheDisk::with_fallback_roots(...)` 消费，并继续关闭 RenderDoc/product capture 与 second-launch miss=0。 |
| 2026-06-28 | Build-tool staged prewarm acceptance contract verification closeout | render_plan08_build_tool_staged_prewarm_acceptance_contract_python_passed_cargo_deferred | 收尾验证补齐 acceptance helper 后的组合边界。`tools/tests/test_zircon_build_shader_permutation_registry_contract.py` 中仍 patch 旧的 `zircon_build.validate_shader_prewarm_report_contract`，组合套件先暴露 2 个 AttributeError；本轮按新架构改为验证 `validate_staged_shader_prewarm_acceptance_contract` entry point，并让 `runtime_15_shader_prewarm_acceptance_contract_is_wired` 锁住 permutation registry 测试不能再 patch 底层 report validator。 | 通过：`PYTHONDONTWRITEBYTECODE=1 python -m py_compile ...` 覆盖 build/prewarm/report/cache/acceptance helpers 与 6 个 Python test owners；`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 57/57；`rustfmt --edition 2021 --check` 覆盖 acceptance guard 和 `test_file_budget/mod.rs`；source/docs anchor scan、conflict/trailing-whitespace scan、line-budget scan 与 scoped `git diff --check` 通过，diff-check 只报告 LF/CRLF warning。 | 仍未运行或计通过：真实 `zircon_shader_prewarm --validate-wgpu-modules`、focused Cargo guard、RenderDoc/product capture、full live registry export、runtime/product fallback-root hit 与 second-launch miss=0。 |
| 2026-06-28 | Build-tool staged prewarm acceptance contract | render_plan08_build_tool_staged_prewarm_acceptance_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged build success path now has one acceptance bundle instead of scattered report/cache/registry checks in `tools/zircon_build.py`. New `tools/zircon_build_shader_prewarm_acceptance.py::validate_staged_shader_prewarm_acceptance_contract(config)` collects `shader_geometry_source_id_specs(config)` and `shader_shading_model_id_specs(config)`, then requires the successful prewarm report to prove WGPU module validation when enabled, source provenance, requested quality/geometry dimensions, and requested custom geometry/shading ids; requires staged cache artifacts through `validate_shader_prewarm_cache_artifact_contract(...)`; and requires automatic resource-registry export correlation unless an explicit `--shader-resource-registry` was supplied. `tools/zircon_build.py::prewarm_shaders(...)` now calls only this acceptance helper after a zero prewarm exit. Added `tools/tests/test_zircon_build_shader_prewarm_acceptance_contract.py::test_acceptance_contract_validates_report_cache_and_exported_registry` for report/cache/registry bundle behavior, explicit-registry skip, and build success-path handoff; updated existing prewarm tests to patch the acceptance entry point; added `runtime_15_shader_prewarm_acceptance_contract_is_wired` to lock helper, build handoff, tests, docs/status anchors, and 800-line budgets. | RED focused Python first failed with `ModuleNotFoundError: No module named 'tools.zircon_build_shader_prewarm_acceptance'`; after implementation `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_acceptance_contract` passed 3/3 and `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` passed 30/30. Additional verification for this slice is recorded below after full py_compile, combined Python suite, rustfmt, source/docs anchors, conflict/whitespace scans, line-budget scan, and scoped diff-check. Current active Cargo/rustc lanes remain outside this slice, so real `zircon_shader_prewarm --validate-wgpu-modules`, focused Cargo guard, RenderDoc/product capture, full live registry export, and second-launch miss=0 are not run or counted. | Next controlled WGPU window should run the full staged prewarm command and use this acceptance bundle as the post-run gate, then prove runtime fallback-root hits and product miss=0 with RenderDoc/product evidence. |
| 2026-06-28 | Build-tool staged WGPU handoff command contract | render_plan08_build_tool_staged_wgpu_handoff_command_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged build 现在把分散的 WGPU validation、generated permutation registry、engine/plugin asset roots、resource registry export、cache/report path handoff 收敛成一个命令合同。`tools/zircon_build_shader_prewarm.py::build_shader_prewarm_command(...)` 在返回前调用 `validate_shader_prewarm_command_contract(config, command)`，要求 `--validate-wgpu-modules` 随 `--validate-wgpu-shaders` 出现，要求 `--asset-root` 与 `shader_asset_root_paths_for_prewarm(config)` 同源，要求 selected-plugin/custom id 触发 generated `--shader-permutation-registry ZirconEngine/cache/shader_permutation_registry.json`，并锁定 `--export-resource-registry`、`--cache-dir`、`--report`、quality/geometry/custom id 参数。新增 `tools/tests/test_zircon_build_shader_prewarm_command_contract.py` 覆盖 full staged WGPU handoff 和缺失 WGPU validation flag 拒绝；新增 `runtime_15_shader_prewarm_staged_wgpu_handoff_command_contract_is_wired` 锁定 helper、测试、docs/status anchors 与 800 行预算。 | 红跑 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_command_contract` 先以 `cannot import name 'validate_shader_prewarm_command_contract'` 失败；实现后新合同测试 2/2 通过，现有 `PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm` 30/30 通过。补充验证 `PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_prewarm_report_contract.py tools\zircon_build_shader_prewarm_cache_artifacts.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm_command_contract.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py tools\tests\test_zircon_build_shader_permutation_registry_contract.py` 通过；`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_command_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 54/54 通过；`rustfmt --edition 2021 --check` 覆盖新结构守卫与 `test_file_budget/mod.rs` 通过；source/docs anchor scan、conflict/trailing-whitespace scan 和 scoped `git diff --check` 通过，diff-check 仅报告既有 LF/CRLF warning。按 milestone-first cadence，本切片不抢 active Cargo/WGPU lanes，真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 暂未运行也不计通过。 | 下一步仍需要在受控 Cargo/WGPU 验证窗口运行完整 staged prewarm 命令，证明该 handoff 在真实 `zircon_shader_prewarm --validate-wgpu-modules` 中生成 custom-id cache artifacts 并被 runtime fallback root 命中，继续关闭 RenderDoc/product capture 与 miss=0 acceptance。 |
| 2026-06-28 | Runtime custom id staged fallback lookup contract | render_plan08_runtime_custom_id_staged_fallback_lookup_static_passed_cargo_deferred | Plan 08 MS-M4 staged cache runtime seam 继续把 custom geometry-source / shading-model id 从“同 root 可 lookup”推进到“runtime writable root miss 后能从 staged fallback root 命中”。`graphics/shader/variant_cache/prewarm.rs` 新增 `render_shader_variant_prewarm_custom_ids_hit_staged_fallback_root`，把 `GeometrySourceId::new(4)` / `ShadingModelId::new(16)` 的 request 写入 `staged/cache/shader_variants`，先断言 `ShaderVariantCacheDisk::new(&runtime_root).lookup(&disk_key)` 为 `Miss`，再断言 `ShaderVariantCacheDisk::with_fallback_roots(&runtime_root, [&staged_root]).lookup(&disk_key)` 命中同一 canonical string，并确认 fallback lookup 不创建或写入 runtime root。`runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 同步锁定新测试、fallback-root call、docs/status anchors 与 800 行预算。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\shader\variant_cache\prewarm.rs zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_cache_artifact_contract.rs zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\mod.rs` 通过；source/docs anchor scan、conflict marker scan、trailing-whitespace scan 与 scoped `git diff --check` 通过，diff-check 只报告既有 LF/CRLF warning。文件预算：`prewarm.rs` 543 行、cache artifact guard 175 行、`variant_cache.md` 80 行。按 milestone-first cadence，本切片不抢 active Cargo/WGPU lanes，focused Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 暂未运行也不计通过。 | 后续仍需要受控 Cargo/WGPU 验证和真实产品路径，把 custom-id fallback-root hit 扩到完整 staged prewarm process、product launch 与 miss=0 acceptance。 |
| 2026-06-28 | Runtime prewarm custom id cache lookup contract | render_plan08_runtime_prewarm_custom_id_cache_lookup_static_passed_cargo_deferred | Plan 08 MS-M4 staged cache runtime seam 现在补上 custom geometry-source / shading-model id 的 Rust cache-key lookup 合同。`graphics/shader/variant_cache/prewarm.rs` 新增 `render_shader_variant_prewarm_custom_ids_survive_disk_lookup`，直接构造 `GeometrySourceId::new(4)` 与 `ShadingModelId::new(16)` 的 `ShaderVariantPrewarmRequest`，经 `prewarm_shader_variants_to_disk(...)` 写入 staged cache 后，用同一个 `ShaderVariantCacheDiskKey::from_variant_key(...)` 通过 `ShaderVariantCacheDisk::new(&root).lookup(&disk_key)` 命中，并断言 report `written_variants[].canonical_string`、metadata canonical string 与 dimension summary 都保留 `geometry=4` / `shading=16`。该切片没有新增生产路径，只把上一条 build-helper custom-id cache artifact contract 背后的运行时 key 语义固定在现有 prewarm/cache owner 内；扩展 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 锁定新测试、helper、docs/status anchors 与 800 行预算。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\shader\variant_cache\prewarm.rs zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\shader_prewarm_cache_artifact_contract.rs zircon_runtime\src\tests\runtime_absorption\structure_convention\test_file_budget\mod.rs` 通过；source/docs anchor scan 覆盖 runtime test、helper、lookup call、`|geometry=4|`、`|shading=16|` 与状态锚；conflict marker scan、trailing-whitespace scan 和 scoped `git diff --check` 通过，diff-check 只报告既有 LF/CRLF warning。文件预算：`prewarm.rs` 496 行、cache artifact guard 169 行、cache helper 391 行、cache tests 302 行、build prewarm tests 718 行、`zircon_build.py` 1351 行。按 milestone-first cadence，本切片不抢 active Cargo lanes，focused Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 均未运行也不计通过。 | 后续仍需要在受控验证阶段运行 focused Rust/Cargo guard，并用真实 staged prewarm run 证明 custom-id cache keys 在完整 WGPU pipeline/product 路径中 hit fallback root。 |
| 2026-06-28 | Prewarm cache custom id correlation contract | render_plan08_prewarm_cache_custom_id_correlation_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged cache build gate 继续从 report custom-id dimension summary 收紧到 actual written cache key identity。`tools/zircon_build.py::prewarm_shaders(...)` 在 successful prewarm 后调用 `validate_shader_prewarm_cache_artifact_contract(...)` 时传入 `expected_geometry_source_ids=shader_geometry_source_id_specs(config)` 与 `expected_shading_model_ids=shader_shading_model_id_specs(config)`；`tools/zircon_build_shader_prewarm_cache_artifacts.py` 复用 `parse_shader_id_record(...)` 解析 selected plugin / explicit CLI `custom:name=ID`，并要求 `written_variants[].canonical_string` 中至少有一个写入变体包含 `|geometry=<id>`，至少有一个写入变体包含 `|shading=<id>`，从而证明 staged `.wgsl.zst` / `.meta` 产物的 `ShaderVariantKey::canonical_string()` 没有丢失 custom geometry-source 或 shading-model id。新增 `test_validate_cache_artifact_contract_requires_requested_custom_ids`、`test_validate_cache_artifact_contract_requires_requested_shading_ids`、`test_validate_cache_artifact_contract_accepts_requested_custom_ids`，扩展 `test_prewarm_shaders_validates_wgpu_report_after_success` 与 `test_prewarm_shaders_uses_same_acceptance_entry_for_explicit_registry` 验证 build handoff 参数，并扩展 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 锁定 helper、build handoff、tests、docs/status anchors 与 800 行预算。 | 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract.ZirconBuildShaderPrewarmCacheContractTests.test_validate_cache_artifact_contract_requires_requested_custom_ids` 先以 `unexpected keyword argument 'expected_geometry_source_ids'` 失败；实现后 focused custom-id cache tests 3/3 通过。随后 `PYTHONDONTWRITEBYTECODE=1 python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_prewarm_report_contract.py tools\zircon_build_shader_prewarm_cache_artifacts.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py tools\tests\test_zircon_build_shader_permutation_registry_contract.py` 通过；`PYTHONDONTWRITEBYTECODE=1 python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract` 52/52 通过。补充验证 `rustfmt --edition 2021 --check` 覆盖 cache artifact guard 与 `test_file_budget/mod.rs` 通过；source/docs anchor scan、conflict/trailing-whitespace scan 和 scoped `git diff --check` 通过，diff-check 仅报告既有 LF/CRLF warning。文件预算：cache helper 391 行、cache test owner 302 行、prewarm test owner 718 行、Rust guard 160 行。Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 本切片仍未运行也不计通过。 | 后续真实 staged prewarm run 仍需要证明这些 custom-id cache keys 能被 `ShaderVariantCacheDisk::lookup(...)` fallback root 命中，并继续关闭 real WGPU execution、full project/plugin shader resource discovery、RenderDoc/product capture 与产品级 compile miss=0。 |
| 2026-06-28 | Build-tool shader permutation id report dimension contract | render_plan08_build_tool_permutation_id_report_dimension_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged build 的 report contract 现在继续从内置 quality/geometry 维度收紧到 selected plugin / explicit CLI custom id 维度。`tools/zircon_build.py::prewarm_shaders(...)` 在 successful prewarm 后把 `shader_geometry_source_id_specs(config)` 与 `shader_shading_model_id_specs(config)` 传入 `validate_shader_prewarm_report_contract(...)`；`tools/zircon_build_shader_prewarm_report_contract.py` 作为新 report-contract owner 承接 `expected_geometry_source_ids` / `expected_shading_model_ids`、summary 格式化和 `custom:name=ID` 解析，要求 `shader_variants_report.json` 的 `dimension_summary.geometry_source_ids` / `dimension_summary.shading_model_ids` 对应 numeric id 有 `requested_count > 0`，`tools/zircon_build_shader_prewarm.py` 只 re-export 公开 report helper 并保留 command/registry orchestration。新增/扩展 `test_validate_report_contract_requires_requested_geometry_source_ids`、`test_validate_report_contract_requires_requested_shading_model_ids`、`test_validate_report_contract_accepts_requested_dimensions`、`test_prewarm_shaders_passes_selected_custom_ids_to_report_contract` 和 `test_prewarm_shaders_validates_wgpu_report_after_success`，并扩展 `runtime_15_shader_prewarm_report_dimension_contract_is_wired` 锁定 helper、build handoff、docs/status anchors 与 800 行预算。 | Focused `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py tools\tests\test_zircon_build_shader_permutation_registry_contract.py` 通过；focused `python -m unittest tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_permutation_registry_contract` 8/8 通过。后续验证补跑 full build-helper Python、rustfmt guard、source/docs anchors、conflict/whitespace scan 与 scoped `git diff --check`；Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 本切片仍未运行也不计通过。 | 后续真实 staged prewarm run 需要证明 selected plugin / custom geometry-source 与 shading-model ids 真的进入 report/request/cache artifacts，并继续关闭 full project/plugin shader resource discovery、RenderDoc/product capture、real WGPU execution 与产品级 compile miss=0。 |
| 2026-06-28 | Plugin shader permutation registry export contract | render_plan08_plugin_shader_permutation_registry_export_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged build 现在会在运行真实 `zircon_shader_prewarm` 前校验自动生成的 `ZirconEngine/cache/shader_permutation_registry.json`。`tools/zircon_build_shader_prewarm.py` 新增 `validate_shader_permutation_registry_export_contract(...)`，按当前 build config 合并出的 explicit CLI id 与 selected plugin descriptor/permutation id，要求生成 registry 的 `geometry_source_ids` / `shading_model_ids` 均包含对应 token/id；`tools/zircon_build.py::prewarm_shaders(...)` 在 `write_generated_shader_permutation_registry(...)` 返回路径后立即调用该合同。新增 `tools/tests/test_zircon_build_shader_permutation_registry_contract.py` 承接缺失 selected plugin shading-model id 拒绝和 prewarm 运行前验证 handoff，用小 owner 避免继续扩张既有 688 行 prewarm 测试文件。扩展 `runtime_15_shader_prewarm_plugin_permutation_registry_auto_export_is_wired` 锁定 helper、build handoff、新测试、docs/status anchors 和 800 行预算。 | TDD 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm` 先以 `ImportError: cannot import name 'validate_shader_permutation_registry_export_contract'` 失败；实现后 `python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_permutation_registry_contract` 32/32 通过。后续验证补跑 `py_compile`、相关 build-helper Python、rustfmt guard、source/docs anchors、conflict/whitespace scan 与 scoped `git diff --check`；Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 本切片仍未运行也不计通过。 | 后续仍需要真实 staged prewarm run 证明该 generated registry 能驱动 selected plugin geometry/shading ids 进入实际 Naga/WGPU 编译与 cache artifacts，并继续关闭 full project/plugin shader resource discovery、RenderDoc/product capture 与产品级 compile miss=0。 |
| 2026-06-28 | Prewarm cache hash shape contract | render_plan08_prewarm_cache_hash_shape_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged cache build gate 继续从运行时目录布局收紧到运行时 key 形状。`tools/zircon_build_shader_prewarm_cache_artifacts.py` 新增 `_BLAKE3_HEX_LENGTH = 64` 与 `_validate_cache_hash_shape(...)`，要求 staged `.wgsl.zst` artifact 文件名 hash 和 report `written_variants.cache_hash` 均为 64 字符小写十六进制 BLAKE3 形状，匹配 `ShaderVariantCacheDiskKey::from_variant_key(...)` 的输出；短 hash、非 hex 或大小写漂移不再能被 build helper 误判为 runtime-addressable cache entry。扩展结构守卫 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 锁定 helper hash shape、`test_validate_cache_artifact_contract_rejects_non_blake3_hex_cache_hash`、docs/status anchors 和 800 行预算。 | 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract` 先失败 1 项，证明旧 helper 接受短 hash；实现后 focused cache contract tests 11/11 通过。随后 `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_prewarm_cache_artifacts.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py` 通过；full build-helper Python `python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 44/44 通过；`rustfmt --edition 2021 --check` 覆盖 cache artifact guard 与 `test_file_budget/mod.rs` 通过。Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 本切片仍未运行也不计通过。 | 后续真实 staged prewarm run 仍需要证明该 64-hex key 形状与实际 `ShaderVariantCacheDisk::lookup(...)` fallback root 命中一致，并继续关闭 WGPU validation、full registry export、RenderDoc/product capture 与产品级 compile miss=0。 |
| 2026-06-28 | Prewarm cache runtime layout contract | render_plan08_prewarm_cache_runtime_layout_contract_python_passed_cargo_deferred | Plan 08 MS-M4 staged cache build gate 现在不只证明 report identity 与 `.meta` 一致，还要求 artifact 位于运行时 `ShaderVariantCacheDisk` 实际查找的目录布局。`tools/zircon_build_shader_prewarm_cache_artifacts.py` 新增 `_SHADER_VARIANT_CACHE_SCHEMA_VERSION = 1`、`_validate_runtime_cache_layout(...)` 与 `_runtime_cache_wgsl_artifact_path(...)`，要求每个 `.wgsl.zst` 路径精确为 `<cache_root>/v1/<hash[0..2]>/<hash>.wgsl.zst`，并要求 `.meta.schema_version == 1`，否则分别报 `runtime cache layout` 或 `cache metadata schema mismatch`。扩展结构守卫 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 锁定 helper layout/schema 检查、`test_validate_cache_artifact_contract_rejects_non_runtime_cache_layout`、`test_validate_cache_artifact_contract_rejects_schema_version_mismatch`、docs/status anchors 和 800 行预算。 | 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract` 先失败 2 项，证明旧 helper 会接受错误 shard 目录和 schema mismatch；实现后 focused cache contract tests 10/10 通过。随后 `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_prewarm_cache_artifacts.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py` 通过；full build-helper Python `python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 43/43 通过；`rustfmt --edition 2021 --check` 覆盖 cache artifact guard 与 `test_file_budget/mod.rs` 通过。Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 本切片仍未运行也不计通过。 | 后续真实 staged prewarm run 仍需要用该 runtime layout contract 证明 build 产物能被 `ShaderVariantCacheDisk::lookup(...)` fallback root 直接命中，并继续关闭 WGPU validation、full registry export、RenderDoc/product capture 与产品级 compile miss=0。 |
| 2026-06-28 | Prewarm report cache identity contract | render_plan08_prewarm_report_cache_identity_contract_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged prewarm report 现在不只给 `written_count`，还记录每个成功写入 cache entry 的 exact identity。`ShaderVariantPrewarmReport.written_variants` 新增 `ShaderVariantPrewarmWrittenVariant` 列表，`graphics/shader/variant_cache/prewarm.rs` 在 `ShaderVariantCacheDisk::write(...)` 成功后把同一个 `ShaderVariantCacheDiskKey` 的 `cache_hash`、`canonical_string`、source label、template revision、Naga/WGPU version 写入 report；`tools/zircon_build_shader_prewarm_cache_artifacts.py` 在新报告存在 `written_variants` 时要求列表长度匹配 `written_count`，并逐个验证 staged `.wgsl.zst`/`.meta` artifact 的 `hash`、`canonical_string`、template/Naga/WGPU metadata 与 report 完全一致。旧报告没有 `written_variants` 时仍按上一条 cache artifact presence contract 的数量/metadata 闸兼容处理。扩展结构守卫 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired` 锁定 Rust DTO、写盘路径、helper、`test_validate_cache_artifact_contract_requires_written_cache_pairs`、`test_validate_cache_artifact_contract_requires_report_written_variants`、`test_validate_cache_artifact_contract_rejects_partial_written_variant_report`、`test_validate_cache_artifact_contract_rejects_wrong_canonical_variant`、docs/status anchors 和 800 行预算。 | TDD 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm_cache_contract` 先暴露当前 helper 会放过 wrong canonical metadata，并且缺少 report cache hash 时只按数量报旧错误；实现后 focused cache contract tests 8/8 通过。随后 `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\zircon_build_shader_prewarm_cache_artifacts.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm_dimension_contract.py tools\tests\test_zircon_build_shader_prewarm_cache_contract.py` 通过；full build-helper Python `python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 41/41 通过；`rustfmt --edition 2021 --check` 覆盖 `variant_prewarm.rs`、shader facade、prewarm write path、cache artifact guard 与 `test_file_budget/mod.rs` 通过。Focused Cargo guard `cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_cache_artifact_contract_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-cache-identity-contract-0628 --message-format short --color never -- --nocapture` 300 秒超时无 test result，且进程检查未发现该 target-dir 残留 cargo/rustc/link/cl，不计通过；真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 本切片仍未运行也不计通过。 | 后续真实 staged prewarm run 需要用该 `written_variants` identity 证明实际 report 写出的 exact cache keys 能被运行时 `ShaderVariantCacheDisk` fallback root 命中，并继续关闭 WGPU validation、full registry export、RenderDoc/product capture 与产品级 compile miss=0。 |
| 2026-06-28 | Build-tool shader prewarm cache artifact contract | render_plan08_build_tool_cache_artifact_contract_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build 成功路径现在会把 `shader_variants_report.json` 的 `written_count` 与 staged cache 目录里的实际磁盘产物关联，而不只确认 report、WGPU validation、source provenance、registry/report 和 requested dimensions。`tools/zircon_build.py::prewarm_shaders(...)` 在 report contract 通过后调用 `validate_shader_prewarm_cache_artifact_contract(config.shader_prewarm_cache_root, report_path=config.shader_prewarm_report_path)`；新 helper owner `tools/zircon_build_shader_prewarm_cache_artifacts.py` 读取 report 的 `written_count`，扫描 cache root 下 `*.wgsl.zst` 与同名 `.meta` 产物对，拒绝缺失 metadata、无效 metadata、metadata hash 与文件名不一致或 cache pair 数少于 written variants。新增 `tools/tests/test_zircon_build_shader_prewarm_cache_contract.py` 覆盖无 artifact、孤立 WGSL artifact、无效 metadata、hash mismatch 和完整 pair 通过，扩展 `test_prewarm_shaders_validates_wgpu_report_after_success` / explicit registry 路径断言 build handoff 始终校验 cache artifacts；新增结构守卫 `runtime_15_shader_prewarm_cache_artifact_contract_is_wired`。 | TDD 红跑 focused 5 tests 先以 `ModuleNotFoundError: tools.zircon_build_shader_prewarm_cache_artifacts` / `AttributeError: validate_shader_prewarm_cache_artifact_contract` 失败；metadata 扩展红测先以 invalid metadata/hash mismatch 未报错失败；实现后 focused cache tests 5/5 通过。`python -m py_compile ...` 通过；full `python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_shader_prewarm_dimension_contract tools.tests.test_zircon_build_shader_prewarm_cache_contract` 38/38 通过；`rustfmt --edition 2021 --check`、source/docs anchors、whitespace、line-budget 和 scoped `git diff --check` 通过，diff-check 仅报告既有 LF/CRLF 转换提示。focused Cargo guard `cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_cache_artifact_contract_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-cache-artifact-contract-0628 --message-format short --color never -- --nocapture` 120 秒超时无 test result，且超时后无残留 cargo/rustc/link/cl 进程，不计通过；真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 仍未运行也不计通过。 | 后续真实 staged prewarm run 仍需要证明 cache artifacts 可由运行时按 `ShaderVariantCacheDisk` key 命中，且 WGPU validation 全量通过、source provenance/registry/report 关联完整、RenderDoc/product capture 与产品级二次启动 compile miss=0 关闭。 |
| 2026-06-28 | Build-tool shader prewarm report dimension contract | render_plan08_build_tool_report_dimension_contract_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build 成功路径现在会把本次 build 请求的 shader quality tier 与 geometry source 维度传入 `shader_variants_report.json` 契约校验，而不只检查 report 存在、WGPU validation、source provenance 和 registry/report 关联。`tools/zircon_build.py::prewarm_shaders(...)` 在预热进程 `returncode == 0` 后调用 `validate_shader_prewarm_report_contract(...)` 时传入 `expected_quality_tiers=config.shader_quality_tiers` 与 `expected_geometry_sources=config.shader_geometry_sources`；`tools/zircon_build_shader_prewarm.py` 新增 `_validate_expected_dimension_contract(...)`、`_validate_expected_quality_tiers(...)`、`_validate_expected_geometry_sources(...)` 和 `_geometry_source_dimension_id(...)`，要求 report 的 `dimension_summary.quality_tiers` 与 `dimension_summary.geometry_source_ids` 对所有 requested build dimensions 都有 `requested_count > 0`。新增小测试文件 `tools/tests/test_zircon_build_shader_prewarm_dimension_contract.py` 覆盖缺少 requested quality tier、缺少 requested geometry source 和完整维度通过，扩展 `test_prewarm_shaders_validates_wgpu_report_after_success` 断言 build handoff 带上 expected dimensions；新增结构守卫 `runtime_15_shader_prewarm_report_dimension_contract_is_wired`。 | TDD 红跑 focused 3 tests 先以 `unexpected keyword argument 'expected_quality_tiers'` / mock 缺少 keyword 参数失败；实现后 `python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_prewarm_shaders_validates_wgpu_report_after_success tools.tests.test_zircon_build_shader_prewarm_dimension_contract` 4/4 通过。后续已补 `py_compile`、full build-helper Python、rustfmt、source/docs anchors、whitespace 和 scoped diff-check 验证记录；Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 仍未运行也不计通过。 | 后续真实 staged prewarm run 需要同时证明 requested quality/geometry 维度在 report 中有请求计数、WGPU validation 全量通过、source provenance 覆盖 requested variants、所有 `res://` report source 可在自动导出的 `ResourceRecord` locator 中找到，并继续推进 lockfile/Cargo guard、full project/plugin shader registry export、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | Build-tool shader resource registry report correlation | render_plan08_build_tool_resource_registry_report_correlation_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build 成功路径现在会把自动导出的 `shader_resource_records.json` 与 `shader_variants_report.json` 的 source provenance 关联，而不只验证 registry JSON 形状。`tools/zircon_build_shader_prewarm.py::validate_shader_resource_registry_export_contract(..., report_path=...)` 在读取 `ResourceRecord` 容器后解析 prewarm report，只对 `source_label` 以 `res://` 开头的 shader source 强制要求 registry 中存在匹配的 `primary_locator` 或 `artifact_locator`；`builtin://...` 与裸 WGSL/path-like source 不要求 registry 记录，避免把内建 fallback 和无 `.zmeta` 的 source 错判为导出失败。`tools/zircon_build.py::prewarm_shaders(...)` 在自动 export 成功路径把 `report_path=config.shader_prewarm_report_path` 传给该校验器，显式 `--shader-resource-registry` 输入仍跳过 export artifact contract。新增 `test_validate_registry_export_contract_rejects_missing_report_source_locator`、`test_validate_registry_export_contract_accepts_report_source_locator`、`test_validate_registry_export_contract_ignores_builtin_report_sources`，并扩展 `test_prewarm_shaders_validates_wgpu_report_after_success` 断言 registry 校验收到 report path；新增结构守卫 `runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired`。 | TDD 红跑 focused 4 tests 先以缺少 `report_path` 关键字支持失败；实现后同一 focused 4 tests 通过。`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；full `python -m unittest tools.tests.test_zircon_build_shader_prewarm` 30/30 通过。`rustfmt --edition 2021 --check` 覆盖 `shader_prewarm_resource_registry_report_correlation.rs` 与 `test_file_budget/mod.rs` 通过；文件预算检查确认 build helper 646 行、Python tests 755 行、新守卫 96 行；source/docs anchor scan、scoped whitespace scan 与 scoped `git diff --check` 通过，diff-check 仅报告既有 LF/CRLF 转换提示。focused Cargo guard `cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_resource_registry_report_correlation_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-registry-report-correlation-0628 --message-format short --color never -- --nocapture` 120 秒超时无 test result，且超时后无残留 cargo/rustc 进程，不计通过；真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 仍未运行也不计通过。 | 后续真实 staged prewarm run 必须同时留下 WGPU report contract、source provenance contract、parseable registry JSON，并保证所有 `res://` report source 均能在导出的 `ResourceRecord` locator 中找到；继续推进 lockfile 状态修复后的 Cargo guard、full project/plugin shader registry export、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | Build-tool shader resource registry export contract | render_plan08_build_tool_resource_registry_export_contract_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build 成功路径现在会校验自动 `--export-resource-registry` 产物，而不只是把导出路径传给 `zircon_shader_prewarm`。`tools/zircon_build_shader_prewarm.py::validate_shader_resource_registry_export_contract(...)` 读取 `shader_resource_records.json`，接受原始 `ResourceRecord` 数组或 `{ resources: [...] }` / `{ records: [...] }` 包装对象，拒绝缺失、非 JSON、非数组容器或非 object 记录；空数组被允许，以兼容没有 shader `.zmeta` 的项目。显式 registry 输入不计入自动导出证据，后续 staged acceptance 使用同一 report-aware registry validator 验收其可用 revision。新增 `test_validate_registry_export_contract_requires_resource_records`、`test_validate_registry_export_contract_accepts_wrapped_resources`、`test_validate_registry_export_contract_accepts_raw_array`、`test_prewarm_shaders_uses_same_acceptance_entry_for_explicit_registry`，并扩展 `test_prewarm_shaders_validates_wgpu_report_after_success` 断言成功路径会校验自动导出的 registry；新增结构守卫 `runtime_15_shader_prewarm_resource_registry_export_contract_is_wired`。 | TDD 红跑 focused 4 tests 先以 `ImportError: cannot import name 'validate_shader_resource_registry_export_contract'` 失败；实现后 focused 5 tests 通过。`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；full `python -m unittest tools.tests.test_zircon_build_shader_prewarm` 27/27 通过；`rustfmt --edition 2021 --check` 覆盖 `shader_prewarm_resource_registry_export_contract.rs` 与 `test_file_budget/mod.rs` 通过；文件预算检查确认 build helper 562 行、Python tests 670 行、新守卫 96 行。focused Cargo guard `cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_resource_registry_export_contract_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-registry-export-contract-0628 --message-format short --color never -- --nocapture` 在编译前因 `Cargo.lock` 需要更新且 `--locked` 禁止改写而失败，未产生 Rust diagnostics，不计通过；真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full live registry export 与二次启动 miss=0 仍未运行也不计通过。 | 后续真实 staged prewarm run 必须同时留下 report contract、source provenance contract 与 parseable `shader_resource_records.json`；继续推进 lockfile 状态修复后的 Cargo guard、full project/plugin shader registry export、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | Build-tool source provenance report contract | render_plan08_build_tool_source_provenance_report_contract_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build 成功路径现在会校验 prewarm report 确认 shader source provenance，而不只是把 provenance 打印出来。`tools/zircon_build_shader_prewarm.py::validate_shader_prewarm_report_contract(...)` 新增 `require_source_provenance` 参数和 `_validate_source_provenance_contract(...)`，要求 `source_provenance.sources` 非空、`source_count` 与 sources map 一致、`variant_count` 覆盖 `requested_count`、每个 source entry 有 `source_label/source_hash/template_revision`，且 entry 的 written+failed 与 requested 匹配；`tools/zircon_build.py::prewarm_shaders(...)` 在预热进程 `returncode == 0` 后始终传入 `require_source_provenance=True`，并继续按 `validate_wgpu_shaders` 条件要求 WGPU module validation。新增 `test_validate_report_contract_requires_source_provenance_when_requested`、`test_validate_report_contract_accepts_source_provenance_counts`，并扩展 `test_prewarm_shaders_validates_wgpu_report_after_success` 断言 build 成功路径同时要求 WGPU contract 与 source provenance contract；新增结构守卫 `runtime_15_shader_prewarm_source_provenance_report_contract_is_wired`。 | TDD 红跑 focused 3 tests 先以 `unexpected keyword argument 'require_source_provenance'` / missing keyword-only arg 失败；实现后同一 3 tests 通过。`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；full `python -m unittest tools.tests.test_zircon_build_shader_prewarm` 23/23 通过；`rustfmt --edition 2021 --check` 覆盖 `shader_prewarm_source_provenance_report_contract.rs` 与 `test_file_budget/mod.rs` 通过；文件预算检查确认 build helper 519 行、Python tests 578 行、新守卫 92 行。当前外部 Cargo/rustc lanes 活跃，本切片不运行也不声明 Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full registry export 或二次启动 miss=0 通过。 | 后续真实 staged prewarm run 必须同时通过 `source_provenance` contract 与 WGPU validation contract；继续推进 full project/plugin shader registry export、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | Shader prewarm source provenance summary | render_plan08_shader_prewarm_source_provenance_summary_python_passed_cargo_deferred | Plan 08 MS-M4 的 shader prewarm report 现在能说明每个写入/失败变体来自哪个 shader source/template payload，而不是只给出维度计数。`ShaderVariantPrewarmRequest` 新增向后兼容的 `source_label` 字段，asset-root 扫描填入 `.zmeta`/路径稳定 label，内置 fallback 填入 `builtin://shader/pbr.wgsl`；`ShaderVariantPrewarmReport.source_provenance` 按 source label、WGSL source hash、include hash、template revision、Naga/WGPU version 聚合 requested/written/failed counts；写盘路径和 WGPU setup failure 路径改用 request 级 `record_written_request(...)` / `record_failure_request(...)`，因此 WGSL 失败、WGPU 失败和写盘失败都保留 provenance。`tools/zircon_build_shader_prewarm.py` 新增 `source provenance:` 摘要行，报告只有 provenance 而没有 dimension summary 时也会打印。新增/更新 `test_dimension_summary_lines_format_source_provenance`、`render_shader_variant_prewarm_report_groups_written_and_failed_dimensions`、manifest source-label 断言和结构守卫 `runtime_15_shader_prewarm_source_provenance_summary_is_wired`。 | TDD 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_dimension_summary_lines_format_source_provenance` 先失败，证明 provenance-only report 仍返回空摘要；实现后该 focused test 通过。`python -m py_compile tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm` 21/21 通过；`rustfmt --edition 2021 --check` 覆盖 report DTO、facade re-export、prewarm write path、manifest/tests、dynamic API、结构守卫和 `test_file_budget/mod.rs` 通过。当前外部 Cargo/rustc lanes 活跃，本切片不运行也不声明 Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full registry export 或二次启动 miss=0 通过。 | 继续推进真实 staged prewarm runtime run、full project/plugin shader registry export evidence、RenderDoc/product capture 与产品级 miss=0；后续真实 report 应同时检查 `source_provenance.source_count > 0` 与 WGPU validation contract。 |
| 2026-06-28 | Build-tool WGPU validation report contract | render_plan08_build_tool_wgpu_report_contract_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build 现在在 `--validate-wgpu-shaders` 成功路径上校验 `shader_variants_report.json` 的 `wgpu_module_validation` 契约，而不是只检查外层命令是否传了 `--validate-wgpu-modules`。`tools/zircon_build_shader_prewarm.py::validate_shader_prewarm_report_contract(...)` 在需要 WGPU module validation 时读取报告，要求 `enabled=true`、`requested_count > 0`、`validated_count == requested_count`，且 `failed_count/skipped_count` 都为 0；缺失、非 JSON、未启用或计数不完整都会抛出明确 `RuntimeError`。`tools/zircon_build.py::prewarm_shaders(...)` 只在预热进程 `returncode == 0` 后调用该契约检查，非零退出仍先打印 report 摘要再传播原 `CalledProcessError`。新增 Python 回归 `test_prewarm_shaders_validates_wgpu_report_after_success`、`test_validate_report_contract_requires_wgpu_validation_when_requested`、`test_validate_report_contract_accepts_wgpu_validation_counts`，并扩展非零退出回归确保 `nonzero prewarm should not validate`；新增结构守卫 `runtime_15_shader_prewarm_wgpu_report_contract_is_wired` 锁定 build helper、build orchestration、Python tests、docs/status anchors 和 800 行预算。 | TDD 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_validate_report_contract_requires_wgpu_validation_when_requested` 先以 `ImportError: cannot import name 'validate_shader_prewarm_report_contract'` 失败；实现后 focused Python tests 4/4 通过。`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm` 20/20 通过；`rustfmt --edition 2021 --check` 覆盖 `shader_prewarm_wgpu_report_contract.rs` 与 `test_file_budget/mod.rs` 通过。当前外部 editor/runtime Cargo lanes 活跃，本切片不运行也不声明 Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture、full registry export 或二次启动 miss=0 通过。 | 后续真实 staged prewarm run 必须检查 report contract：`wgpu_module_validation.validated_count == requested_count` 且 failure/skipped 为 0；继续推进 full project/plugin shader registry export、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | Prewarm WGPU validation report summary | render_plan08_prewarm_wgpu_validation_report_summary_python_passed_cargo_deferred | Plan 08 MS-M4 的 shader prewarm report 现在能直接说明 WGPU shader-module validation 是否启用以及实际验证结果，而不是只能从命令行参数推断。`ShaderVariantPrewarmReport` 新增 `wgpu_module_validation` 摘要，包含 `enabled`、`requested_count`、`validated_count`、`failed_count` 与 `skipped_count`；`prewarm_shader_variants_to_disk_inner(...)` 在 opt-in validation 路径记录通过、失败和因 WGSL validation 先失败而跳过的数量；`dynamic_api/shader_prewarm.rs` 的 offscreen backend setup failure 也会把所有请求记为 WGPU validation failure。`tools/zircon_build_shader_prewarm.py` 的 report 摘要新增 `WGPU module validation: enabled requested=... validated=... failed=... skipped=...` 行，并兼容 Rust report 中真实的 `requested_count`/`written_count`/`failed_count` 字段。新增 `test_dimension_summary_lines_accept_rust_count_field_names`、`test_dimension_summary_lines_format_wgpu_module_validation_counts`、`render_shader_variant_prewarm_records_wgpu_module_validation_success` 与结构守卫 `runtime_15_shader_prewarm_wgpu_validation_report_summary_is_wired`。 | `python -m py_compile tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；focused Python tests `test_dimension_summary_lines_accept_rust_count_field_names` 与 `test_dimension_summary_lines_format_wgpu_module_validation_counts` 2/2 通过；full `python -m unittest tools.tests.test_zircon_build_shader_prewarm` 17/17 通过；`rustfmt --edition 2021 --check` 覆盖 report DTO、prewarm write path、dynamic setup failure path、新结构守卫和 `test_file_budget/mod.rs` 通过；source-anchor scan、docs-anchor scan、untracked guard whitespace scan 与 scoped `git diff --check` 通过，diff-check 仅报告既有 CRLF 转换提示。当前外部 editor/runtime Cargo lanes 活跃，本切片不运行也不声明 Cargo guard、真实 `zircon_shader_prewarm --validate-wgpu-modules`、RenderDoc/product capture 或二次启动 miss=0 通过。 | 继续推进真实 staged prewarm runtime run、full project/plugin shader registry export evidence、RenderDoc/product capture 与产品级 miss=0；后续实跑时必须检查 `wgpu_module_validation.validated_count` 而不是只检查命令参数。 |
| 2026-06-28 | Build-tool shader asset-root plan visibility | render_plan08_build_tool_shader_asset_root_plan_visibility_python_passed_cargo_deferred | Plan 08 MS-M4 的 staged build dry-run/plan 输出现在会显式列出参与 shader prewarm 与 staged resource registry export 的资源根。`tools/zircon_build_shader_prewarm.py::print_shader_prewarm_plan(...)` 通过 `shader_asset_root_paths_for_prewarm(config)` 打印 `shader asset roots: ...`，与最终 `build_shader_prewarm_command(...)` 的 `--asset-root` 序列保持同源；自动 `--export-resource-registry ZirconEngine/cache/shader_resource_records.json` 仍随同一命令输出。新增 `test_prewarm_plan_lists_asset_roots_for_registry_export` 覆盖 engine root + 多 plugin roots 的 plan 可见性，新增 `test_build_command_auto_export_registry_scans_all_asset_roots` 覆盖 auto-export 命令会扫描所有 roots；新增结构守卫 `runtime_15_shader_prewarm_asset_root_plan_visibility_is_wired` 锁定代码、Python tests、docs/status anchors 与 800 行预算。 | TDD 红跑先证明 `print_shader_prewarm_plan(...)` 未输出 `shader asset roots:`；实现后 focused Python tests 2/2 通过。`python -m py_compile tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py`、`python -m unittest tools.tests.test_zircon_build_shader_prewarm`（15 tests）、`rustfmt --edition 2021 --check` 新守卫与 `test_file_budget/mod.rs`、source-anchor scan、docs-anchor scan 与 scoped `git diff --check` 于 2026-06-28 通过；`git diff --check` 仅报告既有 CRLF 转换提示。Cargo guard 未运行，不计通过。本切片不声明真实 staged WGPU runtime execution、RenderDoc capture、full live registry export 或二次启动 miss=0 通过。 | 继续推进完整 project/plugin shader registry export 的真实运行证据、`zircon_shader_prewarm --validate-wgpu-modules` runtime run、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | Staged shader resource registry multi-root dedupe | render_plan08_shader_resource_registry_multi_root_dedupe_static_passed_cargo_deferred | Plan 08 MS-M4 的 staged shader resource registry 导出现在由 `manifest/resource_registry.rs` 统一处理多 `--asset-root` 输入。新增 `shader_resource_records_from_asset_roots(...)` 聚合 engine/plugin 资产根后调用 `deduplicate_shader_resource_records(...)`；同一 `ResourceId` 指向不同 locator 或同一 locator 指向不同 id 会返回显式错误，完全重复的 shader metadata 只导出一条 `ResourceRecord`，再按 `primary_locator`/id 稳定排序。`run.rs::export_shader_resource_registry_for_asset_roots(...)` 删除手工拼接逻辑，改为调用 registry leaf owner。新增 `shader_resource_records_from_asset_roots_deduplicates_duplicate_shader_records` 覆盖 `engine_assets` 与 `plugin_assets` 重复 shader `.zmeta` 只导出 1 条记录；`shader_resource_records_from_asset_roots_rejects_id_locator_conflicts` 与 `shader_resource_records_from_asset_roots_rejects_locator_id_conflicts` 覆盖两类冲突错误；新增结构守卫 `runtime_15_shader_prewarm_resource_registry_multi_root_dedupe_is_wired`，并扩展 auto-export 守卫对 registry owner 与子测试的锚点。 | `rustfmt --edition 2021 --check`、source-anchor scan、docs-anchor scan、stale manual-concat scan 与 `git diff --check` 于 2026-06-28 通过；`git diff --check` 仅报告既有 CRLF 转换提示。Cargo 因无关 editor/runtime 构建通道活跃暂缓，不计通过。本切片没有声明真实 staged WGPU runtime execution、RenderDoc capture 或二次启动 miss=0 通过。 | 继续推进完整 project/plugin shader registry export 的真实运行证据、`zircon_shader_prewarm --validate-wgpu-modules` runtime run、RenderDoc/product capture 与产品级 miss=0。 |
| 2026-06-28 | TAA reactive shader pass identity | render_plan08_taa_reactive_shader_pass_identity_static_passed_cargo_deferred | Plan 08 MS-M4 的 TAA reactive mesh pass 不再借用 Forward shader pass 维度。`ShaderPassType` 新增 `TaaReactiveMask`/`taa_reactive_mask` token；`MeshPipelineVariantRegistry` 将 `MeshPassPipelineKind::TaaReactiveMask` 与 `TaaReactiveMaterialMask` 映射到专属 pass；TAA reactive shader-module key 现在包含 `|pass=taa_reactive_mask|`；`BUILTIN_STANDARD_MATERIAL_PREWARM_PASSES` 与 asset-root full-material pass 枚举从 5 扩到 6，使 staged cache/report 可出现 TAA reactive 独立维度；`pass_specialization.rs` 可通过通用 material template 组装 `zr_template_taa_reactive_mask.wgsl`。新增 `mesh_pipeline_variant_registry_maps_taa_reactive_to_taa_reactive_pass_type`、`render_shader_pass_type_names_taa_reactive_mask_separately_from_forward`、更新内置/asset prewarm manifest tests，并新增结构守卫 `runtime_15_taa_reactive_shader_pass_identity_is_wired` 锁定代码、docs/status anchors 和 800 行预算。 | `rustfmt --edition 2021 --check`、source-anchor scan、docs-anchor scan、stale pass-pattern scan 与 `git diff --check` 于 2026-06-28 通过；Cargo 因无关 runtime text/editor layout 构建通道仍活跃而暂缓，不计通过。This row does not count as runtime WGPU execution, RenderDoc capture, or miss=0 product evidence. | 继续推进 full project/plugin shader registry export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 与二次启动产品级 miss=0；后续若引入更多 mesh/post pass，应继续使用独立 `ShaderPassType` 维度而不是复用 Forward。 |
| 2026-06-28 | Staged prewarm runtime execution retry | render_plan08_staged_prewarm_runtime_execution_retry_build_timeout_no_result | 为关闭 Plan 08 MS-M4 的真实 staged prewarm 运行证据，本轮在已通过 Python、rustfmt、`zircon_runtime --lib` cargo check 与 `zircon_shader_prewarm` bin cargo check 后，尝试单独构建 `zircon_shader_prewarm` 二进制以便运行 `--validate-wgpu-modules` 与 `--export-resource-registry` 路径。该尝试不改生产代码，只复核真实运行验收边界。 | `cargo build -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-staged-prewarm-run-build-0628 --message-format short --color never` 运行 1205 秒超时，日志尾部仅有既有 warning，无 Rust diagnostics；`target\codex-plan08-staged-prewarm-run-build-0628` 下未生成 `zircon_shader_prewarm*.exe`；残留 cargo/rustc 已停止。因此该项不计 staged runtime execution 通过，也不计 WGPU module runtime validation 通过。 | 继续保留真实 staged prewarm `cargo run`、RenderDoc/product capture、完整 project/plugin shader registry export 与二次启动产品级 miss=0 为后续项；下一步应避免重复同一 debug-build 死等路径，改用更小二进制验收、外部共享 target 或 milestone testing stage 统一处理。 |
| 2026-06-27 | Prewarm opt-in WGPU shader-module validation | render_plan08_prewarm_wgpu_module_validation_gate_python_cargo_check_passed_runtime_run_timeout_deferred | Plan 08 MS-M4 的 staged shader prewarm 现在有显式 WGPU shader module 校验闸门，而默认预热写盘路径保持不变。`graphics/shader/variant_cache/prewarm.rs` 新增 `prewarm_shader_variants_to_disk_with_module_validation(...)`，在 Naga WGSL validation 通过后、磁盘写入前调用注入式 module validator；失败记录为 `WGPU shader module validation failed` 并跳过写盘。`dynamic_api::prewarm_shader_variants_with_wgpu_module_validation(...)` 创建 offscreen `RenderBackend`，通过 `device.create_shader_module(...)` 与 validation error scope 校验每个 `ShaderVariantPrewarmRequest`。`zircon_shader_prewarm --validate-wgpu-modules` 只在显式开关下切到该路径；`tools/zircon_build.py --validate-wgpu-shaders` 将 staged build 开关转发为内部 `--validate-wgpu-modules`，并在 plan 输出中标记 WGPU module validation。新增 `render_shader_variant_prewarm_rejects_wgpu_module_validation_failure_before_disk_write`、`shader_prewarm_args_parse_wgpu_module_validation_flag`、`test_build_command_forwards_wgpu_shader_module_validation` 与 `runtime_15_shader_prewarm_wgpu_module_validation_is_wired`，锁定写盘前失败、CLI 解析、build-tool 转发、docs/status anchors 和 800 行预算。 | TDD 红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_build_command_forwards_wgpu_shader_module_validation` 先失败，证明构建命令未转发 `--validate-wgpu-modules`；实现后该 focused test 通过。`rustfmt --edition 2021` 覆盖 touched Rust files 通过；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm` 通过，13 tests。`cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-wgpu-module-prewarm-check --message-format short --color never` 通过，仅既有 warning；`cargo check -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-wgpu-module-prewarm-check --message-format short --color never` 通过，仅既有 warning。实际 `cargo run ... --validate-wgpu-modules` 尝试在 Windows 编译阶段 604 秒超时，未生成 report，残留 cargo/rustc 已停止，不计 runtime execution 通过。 | 该切片关闭 opt-in WGPU shader-module validation gate 的代码路径、CLI/build-tool 转发和编译证据；真实 staged prewarm `cargo run`、RenderDoc/product capture、完整 project/plugin shader registry export 与二次启动产品级 miss=0 仍按后续切片推进。 |
| 2026-06-27 | Runtime pure-depth DepthPrepass product migration | render_plan08_runtime_depth_prepass_pure_depth_product_migration_static_passed_cargo_check_renderdoc_deferred | Plan 08 MS-M1/MS-M4 的 runtime/product DepthPrepass 现在从临时 normal-target GBuffer 合同切到 pure-depth 合同。`MeshPassPipelineKind::DepthPrepass` 映射为 `ShaderPassType::DepthPrepass`，`mesh_pipeline_depth_prepass_template_source_for_geometry(...)` 不再借用 `ShaderPassType::GBuffer`，而是选择 `zr_template_depth.wgsl` / `zr_template_depth_alpha.wgsl`；`depth_prepass_mesh_shader_key(...)` 继续把 `ShaderVariantKey::canonical_string()` 与 source hash 纳入 shader module key。`create_depth_prepass_mesh_pipeline(...)` 删除 `NORMAL_FORMAT` color target：opaque depth prepass 没有 fragment stage，alpha-test depth prepass 保留 `fs_main` 只用于 alpha discard 且 `targets: &[]`，因此 WGPU pipeline 只写 `DEPTH_FORMAT`。新增/更新 `mesh_pipeline_depth_prepass_template_source_uses_depth_only_template`、`mesh_pipeline_variant_registry_maps_depth_prepass_to_depth_prepass_pass_type`、`depth_prepass_mesh_pipeline_declares_depth_only_template_entries_and_static_layout`、`depth_prepass_mesh_shader_key_includes_shader_variant_identity_and_source_hash` 与 `runtime_15_depth_prepass_pure_depth_product_migration_is_wired`，锁定 source、variant identity、WGPU descriptor、docs/status anchors 和 800 行预算。 | 先红跑 scoped source contract 失败，提示 DepthPrepass variant 仍未映射到 `ShaderPassType::DepthPrepass`；实现后同一 contract 通过。`rustfmt --edition 2021` 覆盖 depth prepass source/cache/pipeline/guard 文件通过；`cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depth-prepass-pure-depth-check --message-format short --color never` 通过，仅既有 warning。`cargo test -p zircon_runtime --lib depth_prepass_mesh_pipeline_creates_on_wgpu_device_with_template_shader ...` 在 Windows lib-test 链接阶段 15 分钟超时无 test result，残留 cargo/rustc 已停止，不计通过。RenderDoc/product capture 未运行。 | 该切片关闭 runtime pure-depth DepthPrepass product migration 这一层；真实 staged prewarm Naga/WGPU compile、完整 live project/plugin shader resource registry 自动导出、RenderDoc/product capture 和更宽产品验收仍按后续切片推进。 |
| 2026-06-27 | Plugin shader asset roots auto-export | render_plan08_plugin_shader_asset_roots_auto_export_focused_tests_passed_cargo_deferred_renderdoc_deferred | Plan 08 MS-M4 staged build 现在会把选中插件的 package asset roots 也作为 `zircon_shader_prewarm --asset-root` 输入。`tools/zircon_build.py::PluginPackage` 新增 `asset_roots`，`discover_plugins(...)` 从 `asset_roots`、默认存在的 `assets` 目录以及 legacy `[distribution] assets = ["assets/**"]` 推导包内资源根；不存在的根不会进入 prewarm，字段存在但不是 list 会按 manifest shape 错误退出。`tools/zircon_build_shader_prewarm.py::shader_asset_root_paths_for_prewarm(...)` 保留 staged `ZirconEngine/assets` 作为第一根，并追加 selected plugin roots，因此同一次 `--export-resource-registry ZirconEngine/cache/shader_resource_records.json` 会覆盖 engine staged assets 与插件资产根。新增 `test_build_command_includes_selected_plugin_asset_roots`、`test_zircon_build_discovers_plugin_asset_roots_for_shader_prewarm`、`test_zircon_build_discovers_distribution_assets_as_plugin_asset_roots`、`test_zircon_build_uses_existing_default_plugin_assets_root`、`test_zircon_build_rejects_invalid_plugin_asset_roots_shape` 和 `runtime_15_shader_prewarm_plugin_asset_roots_auto_export_is_wired` 结构守卫。 | 先红跑 `python -m unittest tools.tests.test_zircon_build_shader_prewarm.ZirconBuildShaderPrewarmTests.test_build_command_includes_selected_plugin_asset_roots` 失败，证明命令只有一个 `--asset-root`；实现后 `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_plugin_carriers.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_plugin_carriers` 通过，20 tests；`python tools\zircon_build.py --targets runtime,plugins --plugins native_dynamic_fixture --out target\codex-plan08-plugin-asset-roots-dry-run --mode debug --prewarm-shaders --dry-run` 通过，最终 prewarm 命令同时包含 staged `ZirconEngine\assets` 与 `zircon_plugins\native_dynamic_fixture\assets`。Rust 结构守卫已加入并完成 rustfmt，Cargo 守卫按当前 Windows lib-test 链接成本 deferred，不计通过。RenderDoc/product capture 未运行。 | 该切片关闭“selected plugin package asset roots 进入 staged prewarm/resource-registry auto-export 输入”这一层；仍未关闭完整 live project/plugin shader resource registry 自动导出、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration。 |
| 2026-06-27 | Plugin geometry-source descriptor registration | render_plan08_plugin_geometry_source_descriptor_registration_typecheck_python_cargo_check_passed_renderdoc_deferred | Plan 08 MS-M1/MS-M4 的 custom GeometrySource 现在有真实插件描述符 owner，而不只停留在 `shader_permutation.geometry_source_ids` id 表。`PluginPackageManifest.geometry_sources` 承载 `GeometrySourceDescriptor`，`RuntimeExtensionRegistry` 新增 owner-tracked `geometry_sources` extension point，package manifest contribution 与 runtime plugin catalog merge 负责注册、撤销和合并；注册前校验 `custom:<name>` token、plugin-range id、非空 `wgsl_include`、至少一个 vertex attribute、binding slot/define name trim。`virtual_geometry` runtime descriptor 和静态 `plugin.toml` 现在声明 `custom:virtual_geometry = 4` 的 `[[geometry_sources]]` descriptor，同时保留旧 `shader_permutation.geometry_source_ids` 作为兼容输入。`tools/zircon_build.py::discover_plugins(...)` 从 selected plugin `[[geometry_sources]]` rows 派生 staged prewarm `shader_geometry_source_ids`，并与旧 id rows 去重合并。新增 `plugin_package_manifest_declares_custom_geometry_source_descriptors`、extension registry geometry-source owner/revoke 断言、static manifest `KNOWN_GEOMETRY_SOURCE_FIELDS` schema 断言、`test_zircon_build_discovers_plugin_geometry_source_descriptors_as_shader_ids` 和 `runtime_15_shader_prewarm_plugin_geometry_source_descriptor_registration_is_wired` 结构守卫。 | `rustfmt --edition 2021` touched Rust files 通过；`python -m py_compile tools\zircon_build.py tools\tests\test_zircon_build_plugin_carriers.py` 通过；`python -m unittest tools.tests.test_zircon_build_plugin_carriers` 通过，4 tests；`cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plan08-geometry-source-descriptor-check --message-format short --color never` 通过，仅既有 warning；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_virtual_geometry_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plan08-geometry-source-descriptor-plugin-check --message-format short --color never` 通过，仅既有 warning。`cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_plugin_geometry_source_descriptor_registration_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plan08-geometry-source-descriptor-check --message-format short --color never -- --nocapture` 1204 秒超时停在 lib-test 编译/链接阶段，未生成可直接运行的 lib-test 二进制，残留本 target cargo/rustc 已停止，不计测试通过。RenderDoc/product capture 与真实 Naga/WGPU prewarm compile 未计通过。 | 该切片关闭“自定义 geometry-source 插件描述符已有真实 project/plugin descriptor owner、可注册入 RuntimeExtensionRegistry、且 selected-plugin build discovery 可由 descriptor 派生预热 id”这一层；完整 project shader resource discovery、自动 project/plugin shader resource registry export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Material custom shading-model runtime registry | render_plan08_material_custom_shading_model_runtime_registry_material_test_static_guard_passed_cargo_guard_timeout_renderdoc_deferred | Plan 08 MS-M3/MS-M4 的材质运行时路径现在消费 selected plugin shading-model descriptor registry，而不是在 `.zmaterial` custom lighting model 到达上传时重新创建 built-in-only registry。`RuntimeModuleExtensionInputs` 从 `RuntimeExtensionRegistry::shading_models()` 收集 `ShadingModelDescriptor`，`RuntimeModuleRegistrationInputs`、`core_modules.rs`、`GraphicsModule` 与 graphics module host factory 把 `plugin_shading_models` 传入 `WgpuRenderFramework`；`SceneRenderer::new_with_plugin_render_extensions_and_shading_models(...)` 继续下传到 `ResourceStreamer::new_with_plugin_shading_models(...)`。`ResourceStreamer` 持有同一份 `ShadingModelRegistry`，`ensure_material(...)` 与 `material_capture_seed(...)` 都通过 `self.shading_model_registry.resolve_lighting_model(...)` 解析 `RenderMaterialLightingModel::Custom`，因此 `PipelineKey.shading_model_id`、`MaterialRuntime` capture seed 和 readiness validation 使用同一个插件 descriptor owner。新增 `shading_model_registry_with_plugin_descriptors(...)` 复用 built-in registry 后注册 plugin descriptor 并保留 plugin id range / duplicate / channel validation；新增 `render_product_streamer_projects_plugin_custom_shading_model_into_pipeline_key` 和 `runtime_15_material_custom_shading_model_runtime_registry_is_wired` 锁定 runtime projection、assembly handoff、WGPU/SceneRenderer 构造链、ResourceStreamer registry owner、docs/status anchors 与 800 行预算。 | `rustfmt --edition 2021 --check` scoped touched Rust files 通过；`cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plan08-material-custom-shading-runtime-check` 通过，仅既有 warning；`cargo test -p zircon_runtime --lib render_product_streamer_projects_plugin_custom_shading_model_into_pipeline_key ...` 1/1 通过。结构守卫 `runtime_15_material_custom_shading_model_runtime_registry_is_wired` 首轮暴露过窄源码锚点，已修正为稳定锚点；等价静态守卫脚本覆盖 assembly、GraphicsModule、WGPU/SceneRenderer 构造链、ResourceStreamer registry、focused test 与 6 个文档锚点全 PASS。cargo 守卫入口复跑在 Windows lib-test 链接阶段无 Rust diagnostics 返回 `-1`，fresh target 20 分钟仍停在 rustc link，已停止残留进程，不计 cargo 守卫通过。RenderDoc/product capture 与真实 Naga/WGPU prewarm compile 未计通过。 | 该切片关闭“custom lighting-model 材质运行时解析接到 selected plugin descriptor registry”这一层；完整 project shader resource discovery、自动 project/plugin shader/shading/geometry registry export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Plugin shading-model descriptor registration | render_plan08_plugin_shading_model_descriptor_registration_typecheck_python_passed_libtest_blocked_by_ui_input_error | Plan 08 MS-M3/MS-M4 的 custom shading-model 不再停留在 F11 指出的半成品 registry surface：`PluginPackageManifest.shading_models` 现在直接承载 `ShadingModelDescriptor`，`RuntimeExtensionRegistry` 新增 owner-tracked `shading_models` extension point，package manifest contribution 与 runtime plugin catalog merge 会把 descriptor 注册到统一 registry；注册前校验 `custom:<name>` token、plugin-range id、trimmed include token 与非空 G-buffer channel mask。`graphics/material/shading_models/registry.rs` 新增 `register_plugin_descriptor(...)`，明确拒绝 built-in id range，避免插件模型覆盖 Unlit/BlinnPhong/StandardPbr。`tools/zircon_build.py::discover_plugins(...)` 同时从 selected plugin `[[shading_models]]` rows 派生 `shader_shading_model_ids`，所以 staged prewarm registry 可以从同一个 descriptor owner 获得 custom model id，而不要求 manifest 再写一份重复 `shader_permutation.shading_model_ids`。新增 `plugin_package_manifest_declares_custom_shading_model_descriptors`、`runtime_plugin_registration_collects_package_manifest_declared_runtime_contributions` 的 shading-model 断言、owner revoke 断言、static manifest `KNOWN_SHADING_MODEL_FIELDS` schema 断言、`test_zircon_build_discovers_plugin_shading_model_descriptors_as_shader_ids` 和 `runtime_15_shader_prewarm_plugin_shading_model_descriptor_registration_is_wired` 结构守卫。 | `rustfmt --edition 2021 --check` touched Rust files 通过；`python -m py_compile tools\zircon_build.py tools\tests\test_zircon_build_plugin_carriers.py` 通过；`python -m unittest tools.tests.test_zircon_build_plugin_carriers` 通过，3 tests；`cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plan08-shading-model-descriptor-check --color never` 通过，仅既有 warning；`cargo test -q -p zircon_runtime --lib shading_model --no-default-features --features target-server --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plan08-shading-model-descriptor-check --color never -- --nocapture` 在 lib-test 编译期被既有 UI 输入法错误阻断：`UiInputMethodSurroundingTextError` 不满足 `StdError` / `AsDynError`，未运行到本切片测试，不计通过。RenderDoc/product capture 未计通过。 | 该切片关闭“自定义 shading-model 插件描述符已有真实 project/plugin descriptor owner、可注册入 RuntimeExtensionRegistry、且 selected-plugin build discovery 可由 descriptor 派生预热 id”这一层；仍需把材质资产 custom lighting-model 解析接到运行时 selected plugin descriptor registry、修复/绕过既有 UI lib-test 阻断后跑 focused Rust tests、对预热 WGSL 做真实 Naga/WGPU compile、补 RenderDoc/product capture、完整 project shader resource discovery 与 runtime pure-depth DepthPrepass product migration。 |
| 2026-06-27 | Plugin shader permutation registry auto-export | render_plan08_plugin_shader_permutation_registry_auto_export_focused_tests_passed_renderdoc_deferred | Plan 08 MS-M4 staged build 现在能从选中的插件 `plugin.toml` 读取 shader permutation id 并生成同一份 staged registry：`PluginPackageManifest.shader_permutation` 新增 `geometry_source_ids` / `shading_model_ids` schema，`virtual_geometry` 的 runtime descriptor 与静态 `plugin.toml` 声明 `custom:virtual_geometry = 4`，`tools/zircon_build.py::discover_plugins(...)` 只从本次 `--plugins` 选择集中收集这些 id，`tools/zircon_build_shader_prewarm.py` 把 selected plugin ids 与 CLI `--shader-geometry-source-id` / `--shader-shading-model-id` 合并后写入 `ZirconEngine/cache/shader_permutation_registry.json`。显式 `--shader-permutation-registry` 仍作为 override，不会被自动文件覆盖。新增 `test_zircon_build_discovers_plugin_shader_permutation_records`、`test_generated_shader_permutation_registry_document_merges_selected_plugin_ids`、`test_build_command_uses_generated_shader_permutation_registry_for_selected_plugin_ids`、`virtual_geometry_registration_contributes_render_feature_descriptor` 断言以及 `runtime_15_shader_prewarm_plugin_permutation_registry_auto_export_is_wired` 结构守卫，锁定 manifest schema、插件静态/动态一致性、selected-only discovery、build command handoff、文档状态锚和 800 行预算。 | `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py tools\tests\test_zircon_build_plugin_carriers.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm tools.tests.test_zircon_build_plugin_carriers` 通过，13 tests；`python tools\zircon_build.py --targets runtime,plugins --plugins virtual_geometry --out target\codex-plan08-plugin-permutation-registry-dry-run --mode debug --prewarm-shaders --dry-run` 通过，计划输出包含 `shader geometry source ids: custom:virtual_geometry=4` 且最终 prewarm 命令使用 generated `--shader-permutation-registry`；`cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-plugin-permutation-registry-check --color never` 通过，仅既有 warning；`cargo test -q -p zircon_plugin_virtual_geometry_runtime --manifest-path zircon_plugins\Cargo.toml virtual_geometry_registration_contributes_render_feature_descriptor --locked --jobs 1 --target-dir target\codex-plan08-plugin-permutation-registry-plugin --color never -- --nocapture` 通过，1 test。 | 该切片只关闭“选中插件 manifest 的 shader permutation id 可自动导出到 staged registry”这一层；完整 project shader resource discovery、自定义 shading-model 插件描述符实际注册、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Shader permutation registry auto-export | render_plan08_build_tool_shader_permutation_registry_auto_export_focused_tests_passed_renderdoc_deferred | Plan 08 MS-M4 staged build 现在能在没有显式 `--shader-permutation-registry` 且存在 `--shader-geometry-source-id` / `--shader-shading-model-id` 时自动写出 `ZirconEngine/cache/shader_permutation_registry.json`：`BuildConfig.shader_prewarm_permutation_registry_path` 定义 staged 输出路径，`tools/zircon_build.py::prewarm_shaders(...)` 在非 dry-run 运行前调用 `write_generated_shader_permutation_registry(...)`，`tools/zircon_build_shader_prewarm.py::shader_permutation_registry_paths_for_prewarm(...)` 让最终命令统一消费显式 registry 或自动生成 registry。自动 JSON 使用既有 overlay schema 的 `geometry_source_ids` / `shading_model_ids`，会把 `name=ID` 规范化为 `custom:name`，显式 `--shader-permutation-registry` 仍作为 override，不生成 staged 文件。新增 `test_build_command_uses_generated_shader_permutation_registry_for_custom_ids`、`test_build_command_prefers_explicit_shader_permutation_registry`、`test_generated_shader_permutation_registry_document_groups_custom_ids`、`test_write_generated_shader_permutation_registry_writes_json` 与 `runtime_15_shader_prewarm_permutation_registry_auto_export_is_wired` 锁定命令、JSON 写出、显式覆盖、文档状态锚和 800 行预算。 | `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm` 通过，9 tests；`python tools\zircon_build.py --targets runtime --out target\codex-plan08-permutation-registry-auto-export-dry-run --mode debug --prewarm-shaders --shader-geometry-source-id custom:gpu-driven=4 --shader-shading-model-id custom:toon=16 --dry-run` 通过，计划输出包含 `shader permutation registry export`，最终 prewarm 命令包含 `--shader-permutation-registry ...\cache\shader_permutation_registry.json`；`cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_permutation_registry_auto_export_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-permutation-registry-auto-export-0627 --color never -- --nocapture` 通过，仅既有 warning。 | 该切片只关闭“构建工具可把已知 CLI custom id 输入生成 staged permutation registry”这一 build-tool seam；完整 automatic project/plugin shader、shading-model、geometry-source registry discovery/export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Shader permutation registry overlay | render_plan08_shader_permutation_registry_overlay_focused_tests_passed_renderdoc_deferred | Plan 08 MS-M4 staged-cache 现在能消费项目/插件导出的 shader permutation registry overlay：`zircon_shader_prewarm --shader-permutation-registry <registry.json>` 与每个 `--asset-root` 下自动发现的 `shader_permutation_registry.json` 会在 manifest 展开前合并，`manifest/permutation_registry.rs::shader_permutation_registry_paths(...)` 负责显式路径和 asset-root sidecar 去重/排序；registry 中 `geometry_source_ids` / `shading_model_ids` 记录被规范化为 `custom:*` token，校验 plugin id range 后写入 `GeometrySourceId` / `ShadingModelId` map，从而让 custom geometry-source 与 custom shading-model id 进入 `ShaderVariantKey` 维度。`tools/zircon_build.py --shader-permutation-registry <path>` 通过 `tools/zircon_build_shader_prewarm.py` 转发显式 registry，既有 staged `--asset-root ZirconEngine/assets` 同时启用自动发现。新增 `shader_prewarm_permutation_registry_merges_custom_geometry_and_shading_ids`、`shader_prewarm_permutation_registry_discovers_asset_root_registry`、`test_build_command_forwards_shader_permutation_registries` 和 `runtime_15_shader_prewarm_permutation_registry_overlay_is_wired` 锁定 CLI、run、registry owner、build helper、文档状态锚和 800 行预算。 | `rustfmt --edition 2021` 覆盖触及 Rust 文件通过；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm` 通过，5 tests；`python tools\zircon_build.py --targets runtime --out target\codex-plan08-permutation-registry-dry-run --mode debug --prewarm-shaders --shader-permutation-registry Project\shader_permutation_registry.json --dry-run` 通过；`cargo test -p zircon_runtime --bin zircon_shader_prewarm shader_prewarm_permutation_registry --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-permutation-registry-0627 --color never -- --nocapture` 通过，2 tests，仅既有 warning；`cargo test -p zircon_runtime --lib runtime_15_shader_prewarm_permutation_registry_overlay_is_wired --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-permutation-registry-0627 --color never -- --nocapture` 通过，1 test，仅既有 warning。 | 该切片只关闭“prewarm 消费项目/插件导出的 permutation registry 文件”这一输入层；完整 automatic project/plugin shader resource registry export、自动生成 shader/shading/geometry registry、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Live ResourceManager shader registry export | render_plan08_live_resource_manager_shader_registry_export_focused_tests_passed_renderdoc_deferred | Plan 08 MS-M4 live revision export 现在有运行时侧 seam：`ResourceManager::ready_records_for_kind(ResourceKind::Shader)` 从权威 registry 导出 deterministic ready/non-zero-revision `ResourceRecord`；`shader_resource_records_from_manager(&manager)` 把这些记录喂给 `ShaderPrewarmResourceRegistryOverlay`，asset-root `.zmeta` shader 扫描由此使用 live `ResourceRecord.revision` 写入 `ShaderVariantKey.material_revision`。新增结构守卫 `runtime_15_shader_prewarm_live_resource_manager_registry_export_is_wired` 锁定 `core/resource/manager/registry_export.rs`、prewarm registry helper、focused tests、docs/status anchors 和 800 行预算。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过；`cargo test -p zircon_runtime --lib resource_manager_exports_ready_records_for_kind_with_live_revisions --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-live-resource-registry-0627 --message-format short --color never -- --test-threads=1 --nocapture` 包装层 1204 秒超时无 test result，但已生成 lib-test 二进制；直接运行 `target\codex-plan08-live-resource-registry-0627\debug\deps\zircon_runtime-09d65f3d4d31577f.exe resource_manager_exports_ready_records_for_kind_with_live_revisions --test-threads=1 --nocapture` 1/1 通过；`cargo test -p zircon_runtime --bin zircon_shader_prewarm shader_prewarm_resource_registry_overlay_uses_live_resource_manager_shader_revisions --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-live-resource-registry-0627 --message-format short --color never -- --test-threads=1 --nocapture` 1/1 通过，仅既有 warning；直接运行同一 lib-test 二进制的 `runtime_15_shader_prewarm_live_resource_manager_registry_export_is_wired --test-threads=1 --nocapture` 1/1 通过。 | 该切片只关闭 live `ResourceManager` shader revision export seam；automatic project/plugin shader/shading/geometry registry export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Runtime shader variant dimension correlation | render_plan08_runtime_shader_variant_dimension_correlation_product_passed_renderdoc_deferred | Plan 08 MS-M4 staged-cache 运行时报表现在和 prewarm report 使用同一组维度诊断：`ShaderVariantMissReport` 新增 serde-defaulted `dimension_summary`，按 pass type、geometry source id、shading model id 与 quality tier 汇总 request/memory-hit/disk-hit/compile-miss/disk-write/disk-error。`MeshPipelineVariantRegistry` 的 request、memory hit 与 cache disk hit/write/error/compile-miss 记录均传入 `ShaderVariantKey`，产品 staged-prewarm 测试把 prewarm 写入的 Forward/skinned/StandardPBR/Medium 维度与两次 runtime disk-hit 维度相关起来。共享层同时修复 prepared GPU-skinning source 的 shader 维度：`MeshDrawQueueProfile::shader_geometry_source_id()` 让统计仍计 prepared geometry，但 variant key 走 `GEOMETRY_SOURCE_ID_SKINNED_MESH`。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件通过；`cargo test -p zircon_runtime --lib gpu_skinning --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-runtime-dimension-correlation-check-0627 --message-format short --color never -- --nocapture` 通过，4 tests；直接运行 `target\codex-plan08-runtime-dimension-correlation-check-0627\debug\deps\zircon_runtime-09d65f3d4d31577f.exe shader_variant_miss_report_groups_runtime_outcomes_by_variant_dimensions --test-threads=1 --nocapture` 通过；同二进制直接运行 `render_product_base_mesh_second_launch_uses_staged_prewarm_without_compile_miss --test-threads=1 --nocapture` 通过。验证期间顺手把已 hard-cut 的 construct 路径测试守卫从 `new.rs` include 更新到 `construct.rs`，不回退任何 deleted owner。 | 该切片关闭 runtime/product 层面对 staged prewarm 维度相关性的诊断缺口；RenderDoc capture、automatic project/plugin registry export、live Runtime `ResourceManager` revision export、真实 Naga/WGPU prewarm compile 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-27 | Build-tool prewarm dimension summary | render_plan08_build_tool_prewarm_dimension_summary_python_tests_passed_cargo_deferred | Plan 08 MS-M4 staged-cache report 的 `dimension_summary` 现在被 `tools/zircon_build.py --prewarm-shaders` 消费：`zircon_shader_prewarm` 进程返回后，构建工具读取 `shader_variants_report.json` 并输出 `shader prewarm dimension summary`，按 pass type、geometry source id、shading model id 与 quality tier 打印 requested/written/failed 汇总，再传播原非零退出码。解析、格式化与旧报告兼容逻辑集中在 `tools/zircon_build_shader_prewarm.py`，`tools/zircon_build.py` 只保留 staged build orchestration 和一个打印调用。新增 `tools/tests/test_zircon_build_shader_prewarm.py` 覆盖完整维度格式、旧报告缺省兼容、malformed entry 跳过以及非零退出码传播前的摘要打印顺序。 | `python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py tools\tests\test_zircon_build_shader_prewarm.py` 通过；`python -m unittest tools.tests.test_zircon_build_shader_prewarm` 通过，4 tests；`python tools\zircon_build.py --targets runtime --out target\codex-plan08-build-tool-prewarm-summary-dry-run --mode debug --prewarm-shaders --dry-run` 通过并确认 prewarm 命令仍含 `--report`、`--geometry-source static` 与默认 `--export-resource-registry`。Cargo/WGPU/RenderDoc 本轮未新增验证，不计通过。 | 该切片只关闭 build log 层面对 report dimension summary 的消费；runtime/product pipeline-hit correlation、automatic project/plugin registry export、live Runtime `ResourceManager` revision export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass migration 仍按后续切片推进。 |
| 2026-06-27 | Prewarm dimension diagnostics report | render_plan08_prewarm_dimension_diagnostics_typecheck_passed_test_timeout_no_result | Plan 08 MS-M4 staged-cache report 现在按 variant key 维度输出请求/写入/失败汇总：`ShaderVariantPrewarmReport` 新增 serde-defaulted `dimension_summary`，包含 `pass_types`、`geometry_source_ids`、`shading_model_ids` 与 `quality_tiers` 四组 `requested_count` / `written_count` / `failed_count` 计数。`graphics/shader/variant_cache/prewarm.rs` 在成功写盘、WGSL 验证失败和写盘失败时通过 variant-aware report 方法记录维度，schema-level 失败仍保留为无维度 key 的顶层 failure。新增 `render_shader_variant_prewarm_report_groups_written_and_failed_dimensions` 覆盖 forward/static/medium 写入与 shadow/skinned/high 失败的分组计数，并保持旧报告 JSON 反序列化兼容。 | `rustfmt --edition 2021 --check zircon_runtime\src\core\framework\render\mod.rs zircon_runtime\src\core\framework\render\shader\variant_prewarm.rs zircon_runtime\src\core\framework\render\shader\mod.rs zircon_runtime\src\graphics\shader\variant_cache\prewarm.rs` 通过；`cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-prewarm-diagnostics-check-0627 --message-format short --color never` 通过，仅既有 warning。聚焦 `cargo test -p zircon_runtime --lib render_shader_variant_prewarm_report_groups_written_and_failed_dimensions ...` 首轮 fresh target 1204 秒超时无结果；warmed target 604 秒再次超时，无 test result 且未生成可直接运行的 lib-test 二进制；两轮残留 target cargo/rustc 已停止，不计测试通过。 | 该切片只关闭 prewarm report 的维度级诊断可见性；不声明 automatic project/plugin registry export、live Runtime `ResourceManager` revision export、真实 Naga/WGPU prewarm compile、RenderDoc/product capture、pipeline-hit UI 汇总或 runtime pure-depth DepthPrepass migration 已完成。 |
| 2026-06-27 | Asset-root custom geometry-source id prewarm | render_plan08_asset_root_custom_geometry_source_id_prewarm_typecheck_passed_test_timeout_no_result | Plan 08 MS-M4 staged-cache 现在有显式 custom geometry-source plugin id 入口：`zircon_shader_prewarm --geometry-source-id custom:gpu-driven=4` 会规范化 token、拒绝 `< GEOMETRY_SOURCE_PLUGIN_ID_START` 的内建段 id，并把 plugin-range `GeometrySourceId` 追加进 asset-root manifest 的 geometry dimension；`tools/zircon_build.py --shader-geometry-source-id custom:gpu-driven=4` 通过 `tools/zircon_build_shader_prewarm.py` 原样转发到 prewarm 命令。`bin/zircon_shader_prewarm/args.rs` 新增 `geometry_source_ids` 显式 map、`parse_geometry_source_id(...)`、重复 token/id 校验和 focused parser tests；`bin/zircon_shader_prewarm/manifest/tests.rs::shader_prewarm_asset_root_manifest_expands_custom_geometry_source_plugin_ids` 覆盖 raw asset-root shader 按 plugin-range geometry source 展开 Forward/GBuffer/DepthPrepass/Shadow/Velocity 五个请求；`runtime_15_shader_prewarm_custom_geometry_source_id_is_wired` 锁定 CLI、run、manifest、build helper、docs/status anchors 和 800 行预算。 | `rustfmt --edition 2021` 覆盖触及 Rust 文件通过；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py` 通过；`python tools\zircon_build.py --targets runtime --out E:\zircon-custom-geometry-id-dry-run --mode debug --prewarm-shaders --shader-geometry-source-id custom:gpu-driven=4 --dry-run` 通过，计划输出 `shader geometry source ids: custom:gpu-driven=4` 且最终 prewarm 命令含 `--geometry-source-id custom:gpu-driven=4` 与默认 `--geometry-source static`；line-budget scan：args.rs 516 行、manifest/tests.rs 701 行、geometry-source 守卫 201 行、父守卫 229 行、build helper 119 行；`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-asset-revision-prewarm-0625 --color never` 通过，仅既有 warning。`cargo test -q -p zircon_runtime --bin zircon_shader_prewarm custom_geometry_source ...` 首轮到达目标测试并暴露 explicit geometry-source id 输出顺序断言，修正为按 `GeometrySourceId` 数值排序后，旧 target 复跑命中 Cargo fingerprint 输出文件异常，fresh target `target\codex-plan08-custom-geometry-test-0627` 904 秒超时且未生成可直接运行的测试二进制，残留本 target cargo/rustc 已停止，因此不计 focused test 通过。 | 该切片只关闭显式 custom geometry-source id 输入、构建转发和 asset-root manifest 展开；它不声明 automatic project/plugin geometry-source registry export、live Runtime `ResourceManager` revision export、完整 shader/shading/geometry registry 自动导出、真实 Naga/WGPU compile、RenderDoc/product capture 或 runtime pure-depth DepthPrepass migration 已完成。 |
| 2026-06-27 | Staged shader resource registry auto-export | render_plan08_shader_resource_registry_auto_export_focused_tests_passed_renderdoc_deferred | Plan 08 MS-M4 staged-cache 现在能在没有显式 `--shader-resource-registry` 时自动导出 staged shader registry：`tools/zircon_build.py --prewarm-shaders` 默认把 `--export-resource-registry ZirconEngine/cache/shader_resource_records.json` 转发给 `zircon_shader_prewarm`，显式 `--shader-resource-registry` 仍优先走 `--resource-registry`。`bin/zircon_shader_prewarm/manifest/resource_registry.rs::shader_resource_records_from_asset_root(...)` 扫描 staged asset roots 中的 `.zmeta`，只导出 `ResourceKind::Shader` 的 `ResourceRecord`，以 `.zmeta.source_hash` 派生 staged revision，并把 `ResourceState::Ready`、primary locator、artifact locator、source hash、importer id 与 config hash 写入 JSON；`run.rs` 立即把导出的记录转成 `ShaderPrewarmResourceRegistryOverlay::from_records(...)`，让同一次 asset-root manifest 扫描可消费这些 revision。首轮 focused test 暴露 sidecar `.zmeta` 接管的单文件 shader 还会被裸 `.wgsl` fallback 重复加入清单，导致 registry revision 与 content-hash revision 混在同一 manifest；`manifest/paths.rs::has_sidecar_zmeta(...)` 现在跳过这些裸源。新增 `shader_prewarm_asset_root_exports_shader_resource_records` 与 `runtime_15_shader_prewarm_registry_auto_export_is_wired`，锁定 CLI、run、manifest scan、path helper、resource owner、build helper、文档状态锚和 800 行预算。 | `rustfmt --edition 2021` 覆盖触及 Rust 文件通过；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py` 通过；默认 build dry-run 确认 prewarm 命令含 `--export-resource-registry ...\cache\shader_resource_records.json`，显式 registry dry-run 确认命令改用 `--resource-registry E:\tmp\resources.json`；line-budget scan 通过：manifest.rs 758 行、manifest/paths.rs 80 行、manifest/resource_registry.rs 186 行、manifest/tests.rs 667 行、run.rs 132 行、auto-export 守卫 147 行；`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-asset-revision-prewarm-0625 --color never` 通过，仅既有 warning。首次 `cargo test ... shader_prewarm_asset_root_exports_shader_resource_records` 失败于 all-revision 断言；修复 sidecar 去重后，`cargo test ... shader_prewarm_asset_root` 通过 8/8，`cargo test ... runtime_15_shader_prewarm_registry_auto_export_is_wired` 通过 1/1，均仅既有 warning。 | 该切片关闭“staged build 可自动生成并消费 shader ResourceRecord JSON”的缺口，但它仍是 asset-root `.zmeta` staged scan，不声明 live Runtime `ResourceManager` 递增 revision 导出；custom geometry-source plugin ids、自动导出项目/插件 shader/shading/geometry registry、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass migration 仍按后续切片推进。 |
| 2026-06-25 | Asset-root shader edit revision export | render_plan08_asset_root_shader_edit_revision_export_passed_cargo_renderdoc_deferred | Plan 08 MS-M4 staged-cache 的 asset-root 扫描不再把 `.zmeta` 与裸 `.wgsl`/`.zshader` 的 `ShaderVariantKey.material_revision` 固定为 1。新增 `bin/zircon_shader_prewarm/manifest/revision.rs`，把 `.zmeta.source_hash` 投影为稳定非零 revision；没有 `.zmeta` 的 raw source 则把 include/source content hash 列表投影为稳定非零 revision。`manifest.rs` 在 `shader_source_from_zmeta(...)` 与 fallback `shader_prewarm_source(...)` 中消费该 revision，确保 shader 源编辑会生成新的 prewarm key 并让 staged disk-cache entry 自然失效。新增 `shader_prewarm_asset_root_manifest_uses_zmeta_source_hash_revision` 与 `shader_prewarm_asset_root_manifest_uses_raw_source_hash_revision` 覆盖 source_hash/raw content 修改后 revision 变化，`runtime_15_shader_prewarm_asset_revision_export_is_wired` 锁定 revision child owner、测试、line budgets 和 docs/status anchors。 | 首次 cold-target focused cargo test 15 分钟停在编译阶段且无 test result，已停止残留 cargo/rustc，不计通过；随后 `cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-asset-revision-prewarm-0625 --color never` 通过，仅既有 warning；`cargo test -q -p zircon_runtime --bin zircon_shader_prewarm shader_prewarm_asset_root_manifest_uses --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-asset-revision-prewarm-0625 --color never -- --nocapture` 通过：2 passed / 0 failed / 9 filtered out，仅既有 warning。 | 该切片关闭 asset-root staged prewarm 的编辑后 key 变化/revision 导出缺口；它不伪造运行中 `ResourceManager` 的递增计数器。RenderDoc capture、automatic plugin/project shader id export、custom geometry-source plugin id、live project registry exact revision overlay 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-25 | Asset-root resource registry revision overlay | render_plan08_asset_root_resource_registry_revision_overlay_typecheck_passed_test_timeout_no_result | Plan 08 MS-M4 staged-cache 现在有显式 live registry revision overlay 入口：`zircon_shader_prewarm --resource-registry <records.json>` 与 `tools/zircon_build.py --shader-resource-registry <records.json>` 接收 `ResourceRecord` 数组或 `{ resources/records: [...] }` 包装对象。新增 `bin/zircon_shader_prewarm/manifest/resource_registry.rs` 解析并过滤 `ResourceKind::Shader` 且 revision 非零的记录，以 `ResourceId`、primary locator 和 artifact locator 建索引；`asset_root_manifest_with_resource_registry_revisions(...)` 在 `.zmeta` shader source 上优先使用匹配记录的 `ResourceRecord.revision`，未匹配时继续回退 `.zmeta.source_hash` revision，raw `.wgsl`/`.zshader` 继续走 content-hash revision。`manifest/paths.rs` 同时承接原 manifest 末尾路径扫描 helper，使 `manifest.rs` 从 791 行降到 705 行。新增 `shader_prewarm_args_parse_resource_registry_path`、`shader_prewarm_asset_root_manifest_uses_resource_registry_revision_overlay` 与 `runtime_15_shader_prewarm_resource_registry_revision_overlay_is_wired`，锁定 CLI、run、manifest、build helper、docs/status anchors 和 800 行预算。 | `rustfmt --edition 2021` 已格式化触及 Rust 文件；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py` 通过；文件预算当前为 manifest.rs 705 行、manifest/paths.rs 75 行、manifest/resource_registry.rs 75 行、manifest/tests.rs 563 行、registry 结构守卫 126 行。红跑 focused bin test 120 秒超时；实现后两个 focused bin cargo 测试并行跑因目标目录竞争 185 秒超时，后续顺序 `cargo test ... shader_prewarm` 与 `cargo check --bin zircon_shader_prewarm` 各 604 秒仍停在编译阶段。2026-06-27 复跑 `cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-asset-revision-prewarm-0625 --color never` 通过，仅既有 warning；同 target focused `cargo test ... shader_prewarm_asset_root_manifest_uses_resource_registry_revision_overlay` 904 秒超时且无 test result，已停止残留 cargo/rustc，不计测试通过。 | 该切片关闭“预热工具可消费已导出的 live `ResourceRecord.revision`”入口，但不等同于 automatic plugin/project registry export；custom geometry-source plugin ids、自动导出项目/插件 shader/shading/geometry registry、真实 Naga/WGPU prewarm compile、RenderDoc/product capture 和 runtime pure-depth DepthPrepass migration 仍按后续切片推进。 |
| 2026-06-25 | Product Base mesh second-launch staged prewarm | render_plan08_product_base_mesh_second_launch_staged_prewarm_passed_renderdoc_deferred | Plan 08 MS-M4 的 staged-cache 验收从 runtime cache consumer 推进到完整产品提交路径：`graphics/tests/render_product_mesh_cache/staged_prewarm/mod.rs::render_product_base_mesh_second_launch_uses_staged_prewarm_without_compile_miss` 先用 `prewarm_shader_variants(...)` 把 builtin fallback + skinned StandardPBR Base mesh manifest 写入 staged root，再连续构造两个新的 `WgpuRenderFramework`，通过测试专用 `replace_shader_variant_disk_cache_for_tests(...)` 注入 `ShaderVariantCacheDisk::with_fallback_roots(runtime_root, [staged_root])`。产品 pipeline 显式注册 `mesh.opaque` Base/Opaque pass 并设为 side-effect cull root，fixture 使用 skinned direct replay 与 `DisplayMode::Shaded`，避免 WireOnly overlay 跳过 BaseScenePass。两次产品提交均要求 `last_shader_variant_miss_report.request_count >= 1`、`disk_hit_count >= 1`、`compile_miss_count == 0`、`disk_write_count == 0`、`disk_error_count == 0`，并要求 mesh replay state change、skinned draw 和 `mesh.opaque` executor 可见。父测试文件只挂载 `mod staged_prewarm;`，当前 Base 验收放在 `staged_prewarm/mod.rs` folder-backed owner 中，`runtime_15_product_base_mesh_staged_prewarm_is_wired` 锁定 child owner、test seam、产品 stats 和文档锚点。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/tests/render_product_mesh_cache/staged_prewarm/mod.rs` 由 2026-06-30 folder-backed follow-up 覆盖；`cargo test -p zircon_runtime --lib render_product_base_mesh_second_launch_uses_staged_prewarm_without_compile_miss --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-staged-prewarm-wgpu-module-0625 --message-format short --color never -- --test-threads=1 --nocapture` 通过：1 passed / 0 failed / 5226 filtered out，用时 8.18s，仅既有 warning。为恢复同一 `zircon_runtime --lib` 构建，顺手修正 `status_output_expected_slices/maps/top_level_maps.rs` 的 include 相对路径。 | 该切片关闭 Base/Opaque 产品级 staged prewarm second-launch miss=0 验收；RenderDoc capture、automatic plugin/project shader id export、custom geometry-source plugin id、automatic resource-registry export 和 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-25 | Runtime Base mesh staged prewarm cache hit | render_plan08_runtime_base_mesh_staged_prewarm_cache_hit_wgpu_pipeline_passed_renderdoc_deferred | Plan 08 MS-M4 的 staged-cache 证据从“写盘后可读回 WGSL 并创建 shader module”推进到运行时 Base mesh pipeline consumer：`mesh_pipeline_cache/ensure_pipeline.rs::runtime_base_mesh_pipeline_uses_staged_prewarm_without_compile_miss` 先把 `dynamic_api::builtin_fallback_shader_prewarm_manifest()` 写入临时 staged root，再构造真实 offscreen `MeshPipelineCache`，把 `ShaderVariantCacheDisk::with_fallback_roots(runtime_root, [staged_root])` 注入运行时 cache，随后通过 `ensure_pipeline_for_variant(...)` 创建 Base mesh WGPU render pipeline。测试断言 staged fallback root 命中后 `ShaderVariantMissReport.disk_hit_count == 1`、`compile_miss_count == 0`、`disk_write_count == 0`、`disk_error_count == 0`，并用 WGPU validation error scope 确认 read-back source 能创建 runtime Base pipeline。 | Fresh target `cargo test -p zircon_runtime --lib runtime_base_mesh_pipeline_uses_staged_prewarm_without_compile_miss --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-runtime-staged-cache-hit-0625 --message-format short --color never -- --test-threads=1 --nocapture` 15 分钟超时后仍在 lib-test link，继续等待 240 秒后仍无二进制，已停止仅该 target 的 cargo/rustc，不计通过。复用 warmed target `target\codex-plan08-staged-prewarm-wgpu-module-0625` 运行同一 cargo 命令通过：1 passed / 0 failed / 5226 filtered out，用时 2.46s，只有既有 warning。 | 该切片关闭 runtime Base mesh consumer 对 staged prewarm fallback root 的 WGPU pipeline hit 证据；完整 Base/Opaque 产品二次启动 miss=0 已由 `render_plan08_product_base_mesh_second_launch_staged_prewarm_passed_renderdoc_deferred` 关闭。RenderDoc/product capture、automatic plugin/project shader id export 与 runtime pure-depth DepthPrepass product migration 仍需后续切片。 |
| 2026-06-25 | Builtin material staged prewarm cache-hit WGPU module validation | render_plan08_builtin_material_staged_prewarm_cache_hit_wgpu_module_passed_renderdoc_deferred | Plan 08 MS-M4 的内建 standard material staged-cache 预热现在有写盘后重启命中与 WGPU shader-module 创建证据。`dynamic_api::builtin_standard_material_prewarm_writes_restart_hits_and_wgpu_modules` 构造 static receive-shadows 与 skinned alpha-test 两组 builtin standard material manifests，调用 `prewarm_shader_variants(...)` 写入临时 staged cache；随后用新的 `ShaderVariantCacheDisk` 模拟重启后的只读命中路径，经 `ShaderVariantCacheDiskKey::from_variant_key(...)` 验证每个请求均为 `ShaderVariantCacheDiskLookup::Hit`，并确认 read-back WGSL 与 template revision 与请求一致。测试再把 cache entry 的 WGSL 交给默认 offscreen WGPU device `create_shader_module(...)`，并用 `push_error_scope(wgpu::ErrorFilter::Validation)` 收集 shader-module validation 错误。 | 直接运行已生成的 `target\codex-plan08-staged-prewarm-wgpu-module-0625\debug\deps\zircon_runtime-09d65f3d4d31577f.exe builtin_standard_material_prewarm_writes_restart_hits_and_wgpu_modules --test-threads=1 --nocapture` 通过：1 passed / 0 failed / 5225 filtered out，用时 2.37s。先行 cargo wrapper `cargo test -p zircon_runtime --lib builtin_standard_material_prewarm_writes_restart_hits_and_wgpu_modules --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-staged-prewarm-wgpu-module-0625 --message-format short --color never -- --test-threads=1 --nocapture` 在 lib-test 编译/link 阶段 15 分钟超时，无 Rust diagnostics/test result；后台进程自然结束后生成了上述测试二进制，因此只计直接二进制结果。RenderDoc MCP 仍无运行实例。 | 该切片只关闭 builtin standard material staged prewarm 的写盘、重启 cache hit 与 WGPU shader module validation；完整产品二次启动 miss=0、运行时 pipeline 直接命中统计、RenderDoc/product acceptance、automatic plugin/project shader id export，以及 runtime pure-depth DepthPrepass product migration 仍按后续切片推进。 |
| 2026-06-25 | Builtin material multi-pass depth-only prewarm | render_plan08_builtin_material_multi_pass_depth_only_prewarm_tests_passed_renderdoc_deferred | Plan 08 MS-M4 的内建 standard material staged-cache 清单从 Forward-only 扩展为 Forward、GBuffer、DepthPrepass、Shadow、Velocity 五个 pass 维度：`dynamic_api::builtin_standard_material_shader_prewarm_manifest(...)` / `dynamic_api::builtin_standard_material_shader_prewarm_manifest_for_geometry(...)` 对每个 quality tier 与 requested geometry source 生成 pass-specific `ShaderVariantKey`、WGSL source、include/source hashes 和 `zr-material-template-v1` revision。`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs` 新增 `mesh_pipeline_standard_material_template_source_for_shader_pass(...)` 作为 pass-aware source owner；预热用 `ShaderPassType::DepthPrepass` 选择纯 depth-only `zr_template_depth.wgsl`，alpha-test 选择 `zr_template_depth_alpha.wgsl` 并只保留 alpha clip fragment。当前 runtime/product 的 `mesh_pipeline_depth_prepass_template_source_for_geometry(...)` 仍按 compiled graph 资源合同使用 normal-target GBuffer 模板，不在本切片切换。 | `rustfmt --edition 2021` 已格式化本切片 Rust 文件；`cargo check -p zircon_runtime --lib --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depthonly-prewarm-check-0625 --message-format short --color never` 通过，仅既有 warning。首轮 focused lib cargo test 15 分钟超时且无 test result，已停止本轮 runtime cargo/rustc，不计通过；首轮 bin focused test 暴露过宽 `surface.normal_ws` 断言，修正为 normal-target encode token 后通过。随后 `shader_prewarm_asset_root_manifest_templates_builtin_standard_material_source` cargo wrapper 1/1 通过；直接运行 `zircon_shader_prewarm-a1a1054e65cd4f3b.exe shader_prewarm --test-threads=1 --nocapture` 9/9 通过；`mesh_pipeline_standard_material_shader_pass_source_keeps_depth_only_contract` 1/1 通过；`builtin_standard_material_shader_prewarm_manifest` 直接 lib-test 2/2 通过；`render_shader_template_specializes_depth_and_velocity_passes` cargo wrapper 1/1 通过。RenderDoc MCP 查询仍无运行实例。 | 该切片关闭内建 standard material 预热的五 pass 展开与 pure depth-only DepthPrepass source/hash 合同；后续仍需真实 staged prewarm Naga/WGPU compile、二次启动 miss=0、RenderDoc/product acceptance，以及在 normal producer 分离后再迁移 runtime DepthPrepass product 到纯 depth-only 资源合同。 |
| 2026-06-25 | Mesh pipeline default WGPU device validation rerun | render_plan08_mesh_pipeline_default_wgpu_device_validation_passed_renderdoc_deferred | 复用当前已生成的 `zircon_runtime` lib-test 二进制，在无 `WGPU_BACKEND` 环境变量的默认路径下重新执行 Plan 08 mesh pipeline device-validation 过滤器，确认 backend 默认从 `all()` 收束到 `PRIMARY` 后，Shadow、Velocity、TAA reactive、Deferred GBuffer 与 DepthPrepass 的真实 WGPU shader module + render pipeline descriptor 均可通过 validation error scope。该行闭合 `render_plan08_shadow_wgpu_device_pipeline_validation_implemented_validation_not_closed`、`render_plan08_velocity_taa_wgpu_device_pipeline_validation_implemented_validation_not_closed` 和 `render_plan08_offscreen_backend_primary_default_implemented_recompile_not_closed` 的默认路径直接执行证据，但不重写旧超时/WSL GL 段错误历史。 | 先尝试 `cargo test -p zircon_runtime --lib creates_on_wgpu_device_with_template_shader --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-wgpu-pipeline-default-0625 --message-format short --color never -- --nocapture`，15 分钟后仍停在 `zircon_runtime` lib-test 编译/link 阶段，无 Rust diagnostics、无 `Finished`/test result，已停止本轮 runtime cargo/rustc，不计通过。随后直接运行 `target\codex-runtime-tests-check-after-vg-layer\debug\deps\zircon_runtime-09d65f3d4d31577f.exe creates_on_wgpu_device_with_template_shader --test-threads=1 --nocapture`，5/5 通过，5219 filtered out，用时 5.37s；`render_backend_config_honors_renderdoc_wgpu_env_selection` 直接二进制 1/1 通过，5223 filtered out，确认 default PRIMARY 与显式 GL 解析守卫仍在。RenderDoc MCP 查询仍无运行实例。 | RenderDoc/product acceptance 仍未关闭；真实 staged prewarm Naga/WGPU 编译、二次启动 miss=0、runtime pure-depth DepthPrepass product migration 和更宽 render-product 回归仍按后续切片推进。 |
| 2026-06-25 | Asset-root custom shading-model id prewarm | render_plan08_asset_root_custom_shading_model_id_prewarm_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M4 的 asset-root staged-cache 预热现在可以显式接收 custom shading-model 插件 id，而不再只能把 `.zmaterial` 的 `lighting_model = "custom:..."` 静默降级成 StandardPBR key。`zircon_shader_prewarm` 新增可重复 `--shading-model-id <custom:name>=<16-255>`，CLI 侧规范化 token、拒绝内建 id 段和重复 id；`manifest.rs` 新增 `asset_root_manifest_for_quality_tiers_geometry_sources_and_shading_model_ids(...)`，在扫描 `.zmaterial` 时先解析内建 PBR/BlinnPhong/Unlit，再用显式 id map 解析 custom token。`tools/zircon_build.py --prewarm-shaders` 新增 `--shader-shading-model-id`，命令组装抽到 `tools/zircon_build_shader_prewarm.py` 避免继续堆大文件。新增 `shader_prewarm_asset_root_manifest_maps_custom_shading_model_plugin_ids` 与 `runtime_15_shader_prewarm_custom_shading_model_id_is_wired` 锁定 CLI、manifest、build-tool 与 docs/status anchors。 | `rustfmt --edition 2021 --check` 通过；`python -m py_compile tools\zircon_build.py tools\zircon_build_shader_prewarm.py` 通过；scoped status-anchor/static scans 通过；`git diff --check` 通过，仅提示既有 LF/CRLF 换行转换；`python tools\zircon_build.py --targets runtime --out D:\zircon-shader-custom-id-dry-run --mode debug --prewarm-shaders --shader-shading-model-id custom:subsurface=16 --dry-run` 通过，最终预热命令含 `--shading-model-id custom:subsurface=16`；`cargo check -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-custom-shading-model-prewarm-check --message-format short --color never` 通过，只有既有 warning。`cargo check -p zircon_runtime --tests ...` 被既有 `virtual_geometry_debug_snapshot_contract.rs` 的 `RenderLayerSet`/`u32` 类型不匹配阻断；focused `cargo test -p zircon_runtime --bin zircon_shader_prewarm shader_prewarm_asset_root_manifest_maps_custom_shading_model_plugin_ids ...` 两次超时，后续重试在 warning-only 日志后 exit 1/`-1`，未产生 test result，不计通过；未运行 WGPU/RenderDoc。 | 这只关闭手动/构建脚本显式 custom shading-model id 映射，不等同于完整项目 registry export；asset-root 编辑后 revision 导出已由 `render_plan08_asset_root_shader_edit_revision_export_passed_cargo_renderdoc_deferred` 关闭。后续仍需从项目/插件 registry 自动导出 custom shading-model ids、custom geometry-source plugin ids、automatic resource-registry export、真实 Naga/WGPU prewarm compile 与 RenderDoc/product 验收。 |
| 2026-06-25 | Builtin fallback multi-geometry prewarm | render_plan08_builtin_fallback_multi_geometry_prewarm_static_passed_cargo_no_result | Plan 08 MS-M4 的纯 `--builtin-fallback` staged-cache 清单现在和 asset-root builtin standard material 一样尊重显式 geometry source 维度：`zircon_shader_prewarm::manifest::builtin_fallback_manifest_for_quality_tiers_and_geometry_sources(...)` 对请求的 `GeometrySourceId` 逐个调用 `dynamic_api::builtin_standard_material_shader_prewarm_manifest_for_geometry(...)`，默认 wrapper 仍只生成 static，`run.rs` 将 `--geometry-source` 传给 `--builtin-fallback` 分支。新增 `shader_prewarm_builtin_fallback_manifest_expands_requested_geometry_sources` 锁定 static/skinned × Medium/High 展开、Forward pass、StandardPbr shading id、ReceiveShadows feature 与 skinned include。 | `rustfmt --edition 2021` 已格式化 `zircon_shader_prewarm` manifest/run/tests。Windows `cargo check -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-builtin-fallback-multigeometry-prewarm-check --message-format short --color never` 两次在依赖编译阶段 exit 1，日志只有编译进度、无 Rust error/`could not compile`/`Finished`；复用 warmed `target\codex-plan08-builtin-template-multigeometry-prewarm-check` 的同类 bin check 304 秒超时后无残留进程、未生成可运行二进制。上述 Cargo 结果均不计通过；未运行 WGPU/RenderDoc。 | 后续在 Cargo lane 稳定后重跑 bin check 与 focused no-run test；继续补 custom geometry-source/plugin shading-model registry export、真实 Naga/WGPU prewarm compile、二次启动 miss=0 与 RenderDoc/product 验收。 |
| 2026-06-25 | Offscreen WGPU backend PRIMARY default | render_plan08_offscreen_backend_primary_default_implemented_recompile_not_closed | `graphics/backend/render_backend/config.rs` 的无环境变量默认后端从 `wgpu::Backends::all()` 收束为 `wgpu::Backends::PRIMARY`，保留 `WGPU_BACKEND=gl`/`dx12`/`vulkan` 等显式覆盖。`graphics/tests/render_debugger_and_history.rs::render_backend_config_honors_renderdoc_wgpu_env_selection` 增加 default=`Backends::PRIMARY` 与 explicit GL=`Backends::GL` 断言，避免 Plan 08 offscreen device validation 在无显示句柄路径默认落到二线 GL backend。 | 已生成的 WSL lib-test 二进制 `/tmp/zircon-plan08-shadow-wgpu-final/debug/deps/zircon_runtime-28cf7c97640ed2da` 证明根因：默认路径与 `WGPU_BACKEND=gl` 下 Velocity/TAA/Shadow 设备过滤测试退出 139；同一二进制在 `WGPU_BACKEND=vulkan` 下 Velocity、TAA、Shadow、GBuffer、DepthPrepass 设备过滤测试均 1 passed / 0 failed。`rustfmt --edition 2021 --check` 已通过 backend config、render debugger test、mesh pipeline 与结构守卫文件。重新编译新默认策略的 WSL no-run 超时 604 秒后仍在 link，已停止本轮 cargo/rustc；后续 `cargo check` 也 604 秒无结果并已停止残留 cargo，因此新默认策略的实跑验收不计通过。 | 继续在可稳定产出测试二进制的 target 上重跑无 `WGPU_BACKEND` 默认路径的 Shadow/Velocity/TAA 设备测试；通过后可把 Shadow/Velocity/TAA 的真实 WGPU validation 从“代码落地/显式 Vulkan 通过”推进到默认路径接受。 |
| 2026-06-25 | Velocity/TAA WGPU device pipeline validation code | render_plan08_velocity_taa_wgpu_device_pipeline_validation_implemented_validation_not_closed | Plan 08 MS-M1/MS-M4 的 Velocity 与 TAA reactive mask template/source-cache cutover 已补设备级 pipeline creation 测试代码。`graphics/scene/scene_renderer/mesh/mesh_pipeline/test_support.rs` 新增测试专用 `create_standard_mesh_pipeline_layout(...)`，集中构造 scene uniform、空 shadow receiver、standard material texture/sampler/uniform 与真实 `GpuScene::scene_bind_group_layout()` 组成的标准 mesh pipeline layout；`create_velocity_mesh_pipeline.rs` 新增 `velocity_mesh_pipeline_creates_on_wgpu_device_with_template_shader`，用 `mesh_pipeline_velocity_template_source_for_geometry(...)` 创建 Velocity template shader module，并验证 `Rg16Float` target + previous-position vertex slot 的 pipeline descriptor；`create_taa_reactive_mask_mesh_pipeline.rs` 新增 `taa_reactive_mask_mesh_pipeline_creates_on_wgpu_device_with_template_shader`，用 `mesh_pipeline_taa_reactive_mask_template_source_for_geometry(...)` 同时创建 reactive mask 与 material mask 两个 `R8Unorm` pipeline。两个测试均使用 WGPU 29 `push_error_scope(wgpu::ErrorFilter::Validation)` / `ErrorScopeGuard::pop()` 捕获 validation error。 | `rustfmt --edition 2021` 已格式化本切片触碰的 mesh pipeline 与结构守卫文件。Windows `cargo test -p zircon_runtime --lib velocity_mesh_pipeline_creates_on_wgpu_device_with_template_shader --no-run --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-velocity-taa-wgpu-norun --message-format short --color never` 首轮在依赖编译阶段 exit 1 且无 Rust error，第二轮 183 秒超时，不计通过。WSL 复用 `/tmp/zircon-plan08-shadow-wgpu-final` 的 no-run 编译 304 秒超时；`rustc` 结束后 cargo 父进程长时间不退出，已停止本轮遗留 cargo。直接运行生成的 `/tmp/zircon-plan08-shadow-wgpu-final/debug/deps/zircon_runtime-28cf7c97640ed2da` 证明两个新过滤测试已进入二进制，但 `velocity_mesh_pipeline_creates_on_wgpu_device_with_template_shader` 与 `taa_reactive_mask_mesh_pipeline_creates_on_wgpu_device_with_template_shader` 均在 offscreen WGPU 路径 `SIGSEGV`/exit 139；同一二进制中的静态 `velocity_mesh_pipeline_declares_template_entries_and_previous_position_vertex_slot` 与 `taa_reactive_mask_pipeline_declares_template_entries_and_static_vertex_layout` 均 1 passed / 0 failed。 | Velocity/TAA 的设备级测试代码已落地，但真实 WGPU device validation 尚未关闭；后续需先修复共享 offscreen WGPU 测试夹具或 Linux 退出清理段错误，再补 Windows focused execution、RenderDoc/product acceptance，并继续把结果写回状态表。 |
| 2026-06-25 | Shadow WGPU device pipeline validation | render_plan08_shadow_wgpu_device_pipeline_validation_implemented_validation_not_closed | Plan 08 MS-M1/MS-M4 的 Shadow template/source-cache cutover 已补设备级 pipeline creation 测试代码。`graphics/scene/scene_renderer/mesh/mesh_pipeline/create_shadow_mesh_pipeline.rs` 新增 `shadow_mesh_pipeline_creates_on_wgpu_device_with_template_shader`，分别用 `mesh_pipeline_shadow_template_source_for_geometry(...)` 生成 ShadowDepth 与 ShadowDepthAlphaMask 模板 WGSL，创建 shader module，并用 scene uniform、空 shadow receiver、standard material texture/sampler/uniform、真实 `GpuScene::scene_bind_group_layout()` 组成 pipeline layout 后调用 `create_shadow_mesh_pipeline(...)`。测试使用 WGPU 29 `push_error_scope(wgpu::ErrorFilter::Validation)` / `ErrorScopeGuard::pop()` 捕获 validation error，覆盖 depth-only shadow 与 alpha-mask shadow 两个 pass shape。 | `rustfmt --edition 2021 --check` 已通过本切片触碰的 Shadow pipeline、结构守卫和 typed-error convergence 路径修正文件。Windows `cargo test -p zircon_runtime --lib shadow_mesh_pipeline_creates_on_wgpu_device_with_template_shader --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-shadow-wgpu-final-fresh --message-format short --color never -- --nocapture` 多轮在 `zircon_runtime` lib-test 编译阶段以 exit `-1` 结束，日志只有既有 warning、无 Rust error、无 `Finished`/test result，且未生成 Windows 测试二进制；期间修正了本测试 import 上跳路径和现有 `typed_error_convergence` docs `include_str!` 多跳一层的编译阻断。WSL 续跑最终生成 `/tmp/zircon-plan08-shadow-wgpu-final/debug/deps/zircon_runtime-28cf7c97640ed2da`，但直接运行该过滤测试在 `running 1 test` 后 `SIGSEGV`/exit 139，不能计作 WGPU validation 通过。 | Shadow 的设备级测试代码已落地，但真实 WGPU device validation 尚未关闭；后续需在 Windows 生成 lib-test 二进制并通过该过滤测试，或先定位 WSL offscreen WGPU 初始化段错误，再补 RenderDoc/product acceptance。 |
| 2026-06-25 | DepthPrepass WGPU device pipeline validation | render_plan08_depth_prepass_wgpu_device_pipeline_validation_passed_renderdoc_deferred | Plan 08 MS-M1/MS-M4 的当前 normal-target DepthPrepass template/source-cache cutover 已补真实 WGPU 设备级 pipeline creation 验证。`graphics/scene/scene_renderer/mesh/mesh_pipeline/create_depth_prepass_mesh_pipeline.rs` 新增 `depth_prepass_mesh_pipeline_creates_on_wgpu_device_with_template_shader`，用 `mesh_pipeline_depth_prepass_template_source_for_geometry(...)` 生成当前 GBuffer-style normal-target DepthPrepass 模板 WGSL，创建 shader module，并用 scene uniform、空 shadow receiver、standard material texture/sampler/uniform、真实 `GpuScene::scene_bind_group_layout()` 组成 pipeline layout 后调用 `create_depth_prepass_mesh_pipeline(...)`。测试使用 WGPU 29 `push_error_scope(wgpu::ErrorFilter::Validation)` / `ErrorScopeGuard::pop()` 捕获 validation error，锁定 `NORMAL_FORMAT` color target、`DEPTH_FORMAT` depth write、template `vs_main`/`fs_main`、static mesh vertex layout 与当前 group layout ABI 能在离屏 device 上真实创建 pipeline。 | 本切片按既有 normal-target 合同验证，不提前切到纯 depth-only DepthPrepass。`cargo test -p zircon_runtime --lib depth_prepass_mesh_pipeline_creates_on_wgpu_device_with_template_shader --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depth-prepass-template-cache-check --message-format short --color never -- --nocapture` 通过：1 passed / 0 failed / 5203 filtered out，仅既有 warning。`runtime_15_depth_prepass_pipeline_template_cache_is_mesh_cache_owned` 的 cargo wrapper 两轮在 warning-only 编译阶段 exit `-1` 且无 Rust error/测试结果；随后直接运行已生成 lib-test 二进制 `target\codex-plan08-depth-prepass-template-cache-check\debug\deps\zircon_runtime-09d65f3d4d31577f.exe runtime_15_depth_prepass_pipeline_template_cache_is_mesh_cache_owned --nocapture` 通过：1 passed / 0 failed / 5209 filtered out。 | DepthPrepass 的设备级 pipeline validation 目标由本切片关闭；后续仍需 RenderDoc/product acceptance 和 runtime pure-depth DepthPrepass product migration。 |
| 2026-06-25 | DepthPrepass WGPU final fresh-target verification | render_plan08_depth_prepass_wgpu_device_pipeline_validation_passed_renderdoc_deferred | 旧 warmed target 后续出现测试二进制无法启动的 Windows 文件损坏错误，因此最终补跑使用新 target `target\codex-plan08-depthprepass-final-fresh` 重新生成 lib-test 二进制。 | 新 target 的 cargo wrapper 超时后后台编译自然结束但日志仍无 `Finished`/test result，不能计作 cargo 包装层通过；直接运行新 target 生成的 `zircon_runtime-09d65f3d4d31577f.exe` 过滤 `depth_prepass_mesh_pipeline_creates_on_wgpu_device_with_template_shader` 与 `runtime_15_depth_prepass_pipeline_template_cache_is_mesh_cache_owned` 均通过：各 1 passed / 0 failed / 5216 filtered out。 | 该行只补最终本地验证证据；RenderDoc/product acceptance、其它 Plan 08 pass 的同级 WGPU 覆盖和 runtime pure-depth DepthPrepass product migration 仍未关闭。 |
| 2026-06-25 | Deferred GBuffer WGPU device pipeline validation | render_plan08_deferred_gbuffer_wgpu_device_pipeline_validation_passed_renderdoc_deferred | Plan 08 MS-M1/MS-M4 的 Deferred GBuffer template/source-cache cutover 已补真实 WGPU 设备级 pipeline creation 验证。`graphics/scene/scene_renderer/mesh/mesh_pipeline/create_gbuffer_mesh_pipeline.rs` 新增 `gbuffer_mesh_pipeline_creates_on_wgpu_device_with_template_shader`，用 `mesh_pipeline_deferred_gbuffer_template_source_for_geometry(...)` 生成真实 Deferred GBuffer 模板 WGSL，创建 shader module，并用 scene uniform、空 shadow receiver、standard material texture/sampler/uniform、真实 `GpuScene::scene_bind_group_layout()` 组成 pipeline layout 后调用 `create_gbuffer_mesh_pipeline(...)`。测试使用 WGPU 29 `push_error_scope(wgpu::ErrorFilter::Validation)` / `ErrorScopeGuard::pop()` 捕获 validation error，锁定 `GBUFFER_ALBEDO`、`GBUFFER_MATERIAL`、depth-read、static mesh vertex layout 与当前 group layout ABI 能在离屏 device 上真实创建 pipeline。 | 首轮 focused cargo test 暴露旧 WGPU API 假设，修正为 WGPU 29 `ErrorScopeGuard::pop()`；随后 lib-test 编译被既有 `SceneError` 字符串断言阻塞，最小改为 typed `SceneError::{ZeroLengthQuaternion, NonFinitePropertyValue, NonEditableDynamicComponentProperty, UndeclaredDynamicComponentProperty}` 断言。最终 `cargo test -p zircon_runtime --lib gbuffer_mesh_pipeline_creates_on_wgpu_device_with_template_shader --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depth-prepass-template-cache-check --message-format short --color never -- --nocapture` 通过，1 passed / 0 failed / 5195 filtered out，仅既有 warning。`cargo check -p zircon_runtime --lib --tests --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depth-prepass-template-cache-check --message-format short --color never` 仍命中既有无关 integration blocker：`zircon_runtime/tests/virtual_geometry_debug_snapshot_contract.rs` 期望 `RenderLayerSet` 但收到 `u32`。WSL clean-target 复跑在依赖编译期超时，无目标测试结果，不计通过。 | Deferred GBuffer 的设备级 pipeline validation 缺口已关闭；后续仍需 RenderDoc/product acceptance 和 runtime pure-depth DepthPrepass product migration。 |
| 2026-06-25 | Deferred GBuffer template source cache cutover | render_plan08_deferred_gbuffer_template_source_cache_static_passed_cargo_check_wgpu_deferred | Plan 08 MS-M1/MS-M4 的 Deferred geometry pass 已从 renderer-local `deferred/geometry_pipeline` 与 `deferred_geometry.wgsl` 硬切到 mesh pipeline template/source-cache 路径。新增 `graphics/shader/template/deferred_gbuffer.rs` 与 `graphics/shader/wgsl/zr_template_deferred_gbuffer.wgsl`，从标准材质 surface 写 `GBUFFER_ALBEDO` 与 `GBUFFER_MATERIAL`；`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs` 新增 `mesh_pipeline_deferred_gbuffer_template_source_for_geometry(...)`；`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_gbuffer_pipeline.rs` 新增 `ensure_gbuffer_pipeline_for_variant(...)`，复用 registry-owned `ShaderVariantKey.geometry_source`、`mesh_pipeline_shader_source_with_cache(...)` 和 source-hash-aware shader module key；`graphics/scene/scene_renderer/mesh/mesh_pipeline/create_gbuffer_mesh_pipeline.rs` 统一创建双 color target + depth-read 的 WGPU pipeline。Deferred graph stage 现在接收 mesh pipeline context，`record_gbuffer_geometry` 在 replay 时把 Base command variant 映射到 GBuffer variant 并绑定 GPUScene、standard material 与 geometry。旧 renderer-local GBuffer pipeline/source owner 已删除。 | 新增/更新 `mesh_pipeline_deferred_gbuffer_template_source_writes_albedo_and_material_targets`、`gbuffer_mesh_shader_key_includes_shader_variant_identity_and_source_hash`、`gbuffer_mesh_pipeline_declares_albedo_material_targets_and_static_layout`、`mesh_pipeline_variant_registry_maps_deferred_gbuffer_to_gbuffer_pass_type` 和 `runtime_15_deferred_gbuffer_pipeline_template_cache_is_mesh_cache_owned` 结构守卫，锁定 source-cache、entry/layout、双 G-buffer target、graph-stage mesh cache context 以及无旧 `deferred_geometry.wgsl`/`create_geometry_pipeline` 合同。新 clean target 两轮 cargo check 在依赖编译阶段返回 exit `-1` 且无 Rust error，不计通过；随后复用 warmed Plan 08 target 运行 `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depth-prepass-template-cache-check --message-format short --color never` 通过，仅输出既有 warning。Windows/WSL focused lib-test no-run 多次被工具层超时或 cargo 父进程挂起打断；WSL no-run 已生成 `/tmp/zircon-plan08-gbuffer-libtest-nodebug/debug/deps/zircon_runtime-09705b887cd1ef69`，直接运行其中的 `runtime_15_deferred_gbuffer_pipeline_template_cache_is_mesh_cache_owned` 结构守卫通过。 | 真实 WGPU device pipeline validation 已由 `render_plan08_deferred_gbuffer_wgpu_device_pipeline_validation_passed_renderdoc_deferred` 关闭；后续仍需 RenderDoc/product acceptance 和 runtime pure-depth DepthPrepass product migration。 |
| 2026-06-25 | DepthPrepass normal-target template source cache cutover | render_plan08_depth_prepass_template_source_cache_static_passed_cargo_check_wgpu_deferred | Plan 08 MS-M1/MS-M4 的当前运行时 DepthPrepass 仍写 `GBUFFER_NORMAL` + `SCENE_DEPTH`，本切片按现有 graph 资源合同把旧 renderer-local `NormalPrepassPipeline` 硬切到 mesh pipeline template/source-cache 路径，而不是提前改成纯 depth-only pass。`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs` 新增 `mesh_pipeline_depth_prepass_template_source_for_geometry(...)`，当前用 `ShaderPassType::GBuffer` 生成 normal-target 模板 source；`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_depth_prepass_pipeline.rs` 新增 `ensure_depth_prepass_pipeline_for_variant(...)`，复用 `mesh_pipeline_shader_source_with_cache(...)`、registry-owned `ShaderVariantKey.geometry_source` 和 source-hash-aware shader module key，并把 WGPU render pipeline 存入 `depth_prepass_mesh_pipelines`；`graphics/scene/scene_renderer/mesh/mesh_pipeline/create_depth_prepass_mesh_pipeline.rs` 统一创建带 `NORMAL_FORMAT` color target 与 `DEPTH_FORMAT` depth write 的 WGPU pipeline；DepthPrepass processor 不再写固定 `MeshPipelineVariantId::new(0)`，graph execution 直接用 mesh command replay 绑定 GPUScene、standard material 和 geometry。旧 `NormalPrepassPipeline` / normal prepass shader owner 已删除。 | 新增/更新 `mesh_pipeline_depth_prepass_template_source_writes_normal_target`、`depth_prepass_mesh_shader_key_includes_shader_variant_identity_and_source_hash`、`depth_prepass_mesh_pipeline_declares_normal_target_template_entries_and_static_layout`、`mesh_pipeline_variant_registry_maps_depth_prepass_to_normal_gbuffer_template`、DepthPrepass processor variant-id 断言和 `runtime_15_depth_prepass_pipeline_template_cache_is_mesh_cache_owned` 结构守卫。首轮 scoped cargo check 超时无结果且不计通过；后续 `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-depth-prepass-template-cache-check --message-format short --color never` 通过，仅输出既有 warning。 | 真实 WGPU device pipeline validation 已由 `render_plan08_depth_prepass_wgpu_device_pipeline_validation_passed_renderdoc_deferred` 关闭；focused structure guard 已通过直接运行生成的 lib-test 二进制补证，cargo wrapper 仍有无诊断 exit `-1` 工具层不确定结果；后续仍需 RenderDoc/product acceptance，并继续把纯 depth-only prewarm/DepthPrepass 资源合同收敛到最终 pass 语义。 |
| 2026-06-24 | Shadow pipeline template source cache cutover | render_plan08_shadow_pipeline_template_source_cache_static_passed_cargo_check_test_compile_wgpu_deferred | Plan 08 MS-M1/MS-M4 的 ShadowDepth/ShadowDepthAlphaMask pass 已从 `ShadowMapRenderer` 内置固定 shader/pipeline 切到 mesh pipeline template/source-cache 路径：删除旧 renderer-local shadow source owner 与 inline shadow WGSL body；`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs` 新增 `mesh_pipeline_shadow_template_source_for_geometry(...)`，按 `ShaderPassType::Shadow` 选择 `graphics/shader/wgsl/zr_template_shadow.wgsl` 或 `graphics/shader/wgsl/zr_template_shadow_alpha.wgsl`，alpha-test shadow 只拉入 material surface 做 discard，不注入 Forward light-grid/shadow include；`graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/ensure_shadow_pipeline.rs` 通过 `pipeline_and_shader_key_for_variant(...)` 消费 registry 的 `ShaderVariantKey.geometry_source`、复用 `mesh_pipeline_shader_source_with_cache(...)` 和 source-hash-aware shader key；`graphics/scene/scene_renderer/mesh/mesh_pipeline/create_shadow_mesh_pipeline.rs` 统一创建 depth-only/alpha-mask WGPU render pipeline，保留 shadow depth bias 和静态 mesh vertex layout；Shadow pass processor、pre-MeshDraw non-material rebuild、compiled graph Shadow stage 与 `ShadowMapRenderer` replay 都改为使用真实 `MeshPipelineVariantId` 和 mesh pipeline context。 | `rustfmt --edition 2021` touched Rust files 已通过；新增/更新 `mesh_pipeline_shadow_template_source_uses_shadow_pass_surface_only_when_alpha_masked`、`shadow_mesh_shader_key_includes_shader_variant_identity_and_source_hash`、`shadow_mesh_pipeline_declares_template_entries_static_layout_and_depth_bias`、shadow processor/rebuild variant-id 断言和 `runtime_15_render_shader_template_assembly_is_folder_backed` 结构守卫，静态扫描确认运行时代码无旧 renderer-local shadow source owner/inline shadow WGSL、旧 inline shader 常量、legacy alpha-mask fragment entry、shadow alpha-test variant pipeline 或固定 shadow variant 残留。首轮 scoped cargo check 600 秒超时无结果且不计通过；后续 `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-shadow-template-cache-check-2 --message-format short --color never` 通过，仅输出既有 warning；focused `cargo test -p zircon_runtime --lib runtime_15_render_shader_template_assembly_is_folder_backed --no-run --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-shadow-template-cache-check-2 --message-format short --color never` 在补齐 folder-backed guard `#[path]` 并修正 ECS 结构测试旧 include 路径后通过，仅输出既有 warning。 | 设备级测试代码已由 `render_plan08_shadow_wgpu_device_pipeline_validation_implemented_validation_not_closed` 补上，但 Windows lib-test 编译未生成测试二进制，WSL 直接运行该过滤测试 `SIGSEGV`；RenderDoc/product acceptance 仍 pending。 |
| 2026-06-24 | TAA reactive mask template source cache cutover | render_plan08_taa_reactive_mask_template_source_cache_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 的 TAA reactive mask pass 已从直接消费旧 `FALLBACK_MESH_SHADER` 切到 template/source-owner 路径：新增 `graphics/shader/template/taa_reactive_mask.rs` 专用辅助 assembler 与 `zr_template_taa_reactive_mask.wgsl`，复用 runtime scene/GPUScene/surface types/geometry include 和标准材质 surface，但不注入 Forward light-grid/shadow/shading include；`fs_taa_reactive_mask` 继续输出 `max(surface.base_color.a, surface.custom0.x)`，`fs_taa_reactive_material_mask` 继续只读取 `standard_material_properties.data8.x`。`shader_source.rs` 新增 `mesh_pipeline_taa_reactive_mask_template_source_for_geometry(...)`，`ensure_taa_reactive_mask_pipeline.rs` 通过 `shader_variant_key.geometry_source` 组装模板、复用 `mesh_pipeline_shader_source_with_cache(...)` 写入 disk/source-hash cache，并把 final source hash 拼入 TAA mask shader module key。`create_taa_reactive_mask_mesh_pipeline.rs` 仍使用普通 `GpuMeshVertex::layout()`、`vs_main` 与两个 mask fragment entry，不引入 Velocity previous-position vertex slot。 | `rustfmt --edition 2021 --check` touched Rust files 已通过；新增/更新 `mesh_pipeline_taa_reactive_mask_template_source_uses_material_surface_without_lighting`、`taa_reactive_mask_shader_key_includes_shader_variant_identity_and_source_hash`、`taa_reactive_mask_pipeline_declares_template_entries_and_static_vertex_layout` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 source helper、source hash、entry names、无旧 fallback import 和 docs/status anchor。首轮 scoped `cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --target-dir target\codex-plan08-taa-reactive-template-cache-check` 244 秒超时无结果且不计通过；后续 `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-taa-reactive-template-cache-check-2 --message-format short --color never` 通过，仅输出既有 warning；focused `cargo test -p zircon_runtime --lib runtime_15_render_shader_template_assembly_is_folder_backed --no-run --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-plan08-taa-reactive-template-cache-check-2 --message-format short --color never` 424 秒超时无测试编译结果且无残留本 target 进程，不计通过。 | 后续仍需补 focused no-run tests、Naga/WGPU device pipeline validation、RenderDoc/product acceptance，并继续把 Deferred/GBuffer、Depth/Shadow pass cache owners 和 staged prewarm manifest 收敛到同一 source/hash 合同。 |
| 2026-06-24 | Velocity pipeline template source cache cutover | render_plan08_velocity_pipeline_template_source_cache_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 的 Velocity object pass 已从直接消费旧 `FALLBACK_MESH_SHADER` 推进到 template/source-owner 路径：`zr_template_velocity.wgsl` 现在声明专用 `ZrVelocityVertexInput`，只在 Velocity pass 使用 `@location(8) previous_position`，避免污染共享 Base `ZrVertexInput`；模板按当前/历史 object position 分别走 `fetch_position(...)`/`fetch_prev_position(...)`、`zr_world_from_local(...)`/`zr_previous_world_from_local(...)` 和 unjittered current/previous view-proj，fragment 输出 motion vector。新增 `zr_template_velocity_alpha.wgsl`，alpha-test Velocity 变体通过 `zr_material_surface(...)` + `zr_surface_fails_alpha_clip(...)` 做 discard。`pass_specialization.rs` 根据 `ShaderFeatureBits::ALPHA_TEST` 选择普通/alpha Velocity 模板；`mesh_pipeline_cache/shader_source.rs` 新增 `mesh_pipeline_velocity_template_source_for_geometry(...)`；`ensure_velocity_pipeline.rs` 现在用该 source owner、共享 `mesh_pipeline_shader_source_with_cache(...)` 与 source-hash-aware shader module key 写入/读取同一 shader variant disk-cache 合同；`create_velocity_mesh_pipeline.rs` 切到模板标准 `vs_main`/`fs_main` entry，同时保留 previous-position vertex slot。结构守卫 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定普通/alpha Velocity 模板、source owner、no fallback shader、entry name、`velocity_mesh_shader_key_includes_shader_variant_identity_and_source_hash` 和 docs/status anchors。 | `rustfmt --edition 2021 --check` touched Rust/guard files 通过；`cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --target-dir target\codex-plan08-velocity-template-cache-check` 通过，仅输出既有 warning。此前首次 cargo check 120 秒超时无结果，残留本 target cargo/rustc 已停止，不计通过；`cargo test -q -p zircon_runtime --lib runtime_15_render_shader_template_assembly_is_folder_backed --no-run --no-default-features --features target-server --locked --target-dir target\codex-plan08-velocity-template-cache-check` 244 秒超时无测试编译结果，残留本 target cargo/rustc 已停止，不计通过。尚未完成真实 WGPU device pipeline creation、RenderDoc capture 或 focused lib-test 运行。 | TAA reactive/mask 已由后续记录切到同一 source/hash contract；后续仍需把 Deferred/GBuffer、Depth/Shadow pass cache owner 迁到同一路径，补 WGPU pipeline validation、产品对拍与二次启动 miss=0/RenderDoc 验收。 |
| 2026-06-24 | Velocity/TAA variant-id pipeline cache owner | render_plan08_velocity_taa_variant_id_pipeline_cache_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 非 Base pass runtime cache 先把 WGPU pipeline map 主键从裸 `PipelineKey` 收束到 `MeshPipelineVariantId`：`MeshPipelineCache.velocity_mesh_pipelines`、`taa_reactive_mask_mesh_pipelines` 和 `taa_reactive_material_mask_mesh_pipelines` 现在都以 registry 分配的 variant id 为键；旧 `pipeline_key_for_variant(...)` 删除，Velocity/TAA `_for_variant` 路径统一读取 `pipeline_and_shader_key_for_variant(...)`，同时拿到 `PipelineKey` 与 registry-owned `ShaderVariantKey`。`ensure_velocity_pipeline.rs` 与 `ensure_taa_reactive_mask_pipeline.rs` 的 shader module key 分别通过 `velocity_mesh_shader_key(...)` / `taa_reactive_mask_mesh_shader_key(...)` 拼入 `ShaderVariantKey::canonical_string()`，新增 `velocity_mesh_shader_key_includes_shader_variant_identity` 与 `taa_reactive_mask_shader_key_includes_shader_variant_identity`；`runtime_15_render_shader_template_assembly_is_folder_backed` 锁定非 Base pass map key、variant query、测试名和 docs/status anchors。 | `rustfmt --edition 2021 --check` touched Rust/guard files 通过；`cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --target-dir target\codex-plan08-velocity-taa-variant-cache-check` 通过，仅输出既有 warning；`cargo test -q -p zircon_runtime --lib shader_key_includes_shader_variant_identity --no-default-features --features target-server --locked --target-dir target\codex-plan08-velocity-taa-variant-cache-check -- --test-threads=1 --nocapture` 608 秒超时无结果，残留本 target cargo/rustc 进程已停止，不计通过。WGPU/RenderDoc 运行证据仍未计通过。 | Velocity 与 TAA reactive source/cache 后续已分别由 source-cache cutover 记录推进；后续仍需收束 deferred/depth/shadow pass cache owner，并补真实 WGPU device pipeline validation、产品对拍与二次启动 miss=0/RenderDoc 验收。 |
| 2026-06-24 | Shader prewarm WGSL validation gate | render_plan08_shader_prewarm_wgsl_validation_check_passed_test_compile_timeout_no_result | Plan 08 MS-M4 staged cache 写盘前新增 WGSL 解析/验证门：`graphics/shader/template/validation.rs` 暴露 crate-visible `validate_shader_variant_prewarm_wgsl(...)` wrapper，复用现有 Naga `parse_str` + validator owner；`graphics/shader/variant_cache/prewarm.rs::prewarm_shader_variants_to_disk(...)` 现在先验证每个 `ShaderVariantPrewarmRequest.wgsl_source`，验证失败时记录 `ShaderVariantPrewarmReport.failures` 并 `continue`，不会把无效 WGSL 写入 `ShaderVariantCacheDisk`。新增 `render_shader_variant_prewarm_rejects_invalid_wgsl_before_disk_write` 覆盖 invalid WGSL 产生 failed report 且 disk lookup 保持 miss；`runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 template validation owner、prewarm owner、测试名与 docs/status anchors。 | `rustfmt --edition 2021 --check` touched Rust/guard files 通过；`cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --target-dir target\codex-plan08-prewarm-wgsl-validation-check` 通过，仅输出既有 warning；`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --target-dir target\codex-plan08-prewarm-wgsl-validation-check` 通过，仅输出既有 warning；临时 invalid manifest 工具实跑 `cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --target-dir target\codex-plan08-prewarm-wgsl-validation-check -- --manifest <invalid> --cache-dir <temp>/cache --report <temp>/report.json --pretty` 返回 exit 2，report 为 requested=1/written=0/failed=1，cache_files=0；`cargo test -q -p zircon_runtime --lib render_shader_variant_prewarm_rejects_invalid_wgsl_before_disk_write --no-run --no-default-features --features target-server --locked --target-dir target\codex-plan08-prewarm-wgsl-validation-check` 604 秒超时无结果，残留本 target cargo/rustc 进程已停止，不计通过。WGPU/RenderDoc 运行证据仍未计通过。 | 后续仍需把 Velocity/TAA/deferred/depth/shadow pass cache owner 迁到同一 `ShaderVariantKey`/source-hash contract，补 WGPU device pipeline validation、真实 staged asset prewarm 执行、custom geometry-source/plugin shading-model registry export、产品对拍与二次启动 miss=0/RenderDoc 验收。 |
| 2026-06-24 | Asset-root builtin standard material multi-geometry prewarm | render_plan08_asset_root_builtin_standard_material_multi_geometry_prewarm_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M4 staged cache 补齐上一片 runtime geometry-source source owner 后的 asset-root 缺口：`dynamic_api::builtin_standard_material_shader_prewarm_manifest(...)` 保持 static wrapper 语义，新增 `dynamic_api::builtin_standard_material_shader_prewarm_manifest_for_geometry(...)`，从 material-derived `ShaderFeatureBits`、`ShadingModelId`、alpha cutoff、显式 `GeometrySourceId` 与 quality tiers 生成 Forward standard material template request，并通过 `mesh_pipeline_standard_material_template_source_for_geometry(...)` 写入对应 `ShaderVariantKey.geometry_source` 与 skinned/static template include。`zircon_shader_prewarm` 的 `.zmaterial` builtin 分支现在对请求的 geometry source 列表逐个调用该 builder，不再用 `geometry_sources.contains(&GEOMETRY_SOURCE_ID_STATIC_MESH)` 只生成 static；custom `.zshader`/standalone `.wgsl` 仍保持 raw scanned source 路径。新增 `builtin_standard_material_shader_prewarm_manifest_projects_geometry_source`，并把 `shader_prewarm_asset_root_manifest_templates_builtin_standard_material_source` 从 1 个 static request 更新为 static+skinned 两个 request；`runtime_15_shader_prewarm_builtin_standard_material_template_source_is_wired` 同步锁定新 builder、manifest 展开、skinned include 与 docs/status anchors。 | `cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --target-dir target\codex-plan08-builtin-template-multigeometry-prewarm-check` 通过，仅输出既有 warning；`cargo test -q -p zircon_runtime --bin zircon_shader_prewarm shader_prewarm_asset_root_manifest_templates_builtin_standard_material_source --no-run --no-default-features --features target-server --locked --target-dir target\codex-plan08-builtin-template-multigeometry-prewarm-check` 通过，仅输出既有 warning；`rustfmt --edition 2021 --check` touched Rust/guard files 通过；source/docs-anchor scans、line-count scan、touched-file whitespace scan、conflict-marker scan 通过；tracked-path scoped `git diff --check` 通过，仅报告既有 LF→CRLF 提示，未跟踪 touched paths 由 whitespace/conflict scans 覆盖。Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把 Velocity/TAA/deferred/depth/shadow pass cache owner 迁到同一 `ShaderVariantKey`/source-hash contract，补 custom geometry-source/plugin shading-model id registry export、真实 Naga/WGPU prewarm compile、产品对拍与二次启动 miss=0/RenderDoc 验收；runtime morphed/skinned-morphed 选择仍需更细 draw metadata。 |
| 2026-06-24 | Runtime mesh variant geometry-source key wiring | render_plan08_runtime_mesh_variant_geometry_source_key_wiring_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 runtime geometry-source 维度不再在 Base mesh 路径中退回 static-only：`MeshDrawGeometrySource::shader_geometry_source_id()` 将 Prepared/Dynamic 映射为 `GEOMETRY_SOURCE_ID_STATIC_MESH`、将 `DynamicGpuSkinningSource` 映射为 `GEOMETRY_SOURCE_ID_SKINNED_MESH`；`MeshPassBuildContext::pipeline_variant_id(...)` 现在直接消费 `MeshBatchRef`，从 batch queue profile 派生 geometry source 后调用 `MeshPipelineVariantResolver::resolve_variant_for_geometry(...)`。`MeshPipelineVariantRegistry`/`MeshPipelineCache` 新增显式 geometry-source 解析入口，并通过 `PipelineKey::shader_variant_key_for_geometry(...)` 写入 `ShaderVariantKey.geometry_source`，所以同一 pass/material/quality 的 static 与 skinned 会产生不同 `MeshPipelineVariantId`。Base `ensure_pipeline_for_variant(...)` 将 `shader_variant_key.geometry_source` 传给 `mesh_pipeline_shader_source(...)`，而 `shader_source.rs` 新增 `mesh_pipeline_standard_material_template_source_for_geometry(...)`，根据请求的 built-in `GeometrySourceId` 选择对应 template include，保留 `mesh_pipeline_standard_material_template_source(...)` static wrapper 供现有 prewarm API 使用。新增 `mesh_pipeline_standard_material_template_source_uses_requested_geometry_source`、`mesh_pipeline_variant_registry_separates_geometry_sources`、`render_mesh_draw_processor_uses_batch_geometry_source_for_pipeline_variant_key`，并扩展 `runtime_15_render_shader_template_assembly_is_folder_backed` 与 `runtime_15_mesh_pass_processors_are_folder_backed` 锁定 runtime geometry-source 透传、source owner 与 line budgets。 | `rustfmt --edition 2021 --check` touched Rust/guard files 通过；`cargo check -q -p zircon_runtime --lib --no-default-features --features target-server --locked --target-dir target\codex-plan08-runtime-geometry-key-check` 通过，仅输出既有 warning；`cargo test -q -p zircon_runtime --lib mesh_pipeline_standard_material_template_source_uses_requested_geometry_source --no-run --no-default-features --features target-server --locked --target-dir target\codex-plan08-runtime-geometry-key-test-compile` 364 秒超时无结果，残留本 target cargo/rustc 进程已停止，不计通过；source/docs-anchor scans、line-count scan、touched-file whitespace scan、conflict-marker scan 通过；tracked-path scoped `git diff --check` 通过，仅报告既有 LF→CRLF 提示，未跟踪 touched paths 由 whitespace/conflict scans 覆盖。Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把 asset-root builtin standard material prewarm 从 static-only 扩展为按请求 geometry source 生成 template source，并继续迁移 Velocity/TAA/deferred/depth/shadow pass cache owner 到同一 `ShaderVariantKey`/source-hash contract；真实 Naga/WGPU pipeline creation、产品对拍与 RenderDoc 验收仍未完成。 |
| 2026-06-24 | Asset-root builtin standard material template prewarm | render_plan08_asset_root_builtin_standard_material_template_prewarm_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M4 asset-root prewarm 现在只在 `.zmaterial` 显式引用 `builtin://shader/pbr.wgsl` 且 static geometry source 被请求时走标准材质 Forward template source；custom `.zshader`/standalone `.wgsl` 仍按扫描出的 raw WGSL 生成预热请求，不被误包装。新增 `dynamic_api::builtin_standard_material_shader_prewarm_manifest(...)` 作为库侧 builder，从 `ShaderFeatureBits`、`ShadingModelId`、alpha cutoff 和 quality tiers 投影到 `PipelineKey`，复用 `mesh_pipeline_standard_material_template_source(...)`，避免在 `zircon_shader_prewarm` bin 工具复制 renderer-private assembly。`manifest.rs` 识别 `BUILTIN_STANDARD_MATERIAL_SHADER_URI`、记录 `material_alpha_cutoff` 和 `uses_builtin_standard_shader`，并把 `.zmaterial` 的 `receive_shadows()` 纳入 `ShaderFeatureBits::RECEIVE_SHADOWS`，对齐 runtime `PipelineKey.receive_shadows`。新增 `builtin_standard_material_shader_prewarm_manifest_projects_material_features`、`shader_prewarm_asset_root_manifest_templates_builtin_standard_material_source` 和 `runtime_15_shader_prewarm_builtin_standard_material_template_source_is_wired`，锁定动态 API、manifest 分支、static-only template request、docs/status anchors 和 line budgets。 | `rustfmt --edition 2021 --check` touched Rust files 通过；source/docs-anchor scans、line-count scan、touched-file whitespace scan、conflict-marker scan 通过；`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --locked --target-dir target\codex-plan08-builtin-template-prewarm-check` 通过，仅输出既有 warning；tracked-path scoped `git diff --check` 通过，仅报告既有 LF→CRLF 提示，未跟踪 touched paths 由 whitespace/conflict scans 覆盖。Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把非 static geometry 的 builtin template source、Velocity/TAA/deferred/template pass cache owner、custom shading-model plugin id registry export、真实 Naga/WGPU prewarm compile、产品对拍与二次启动 miss=0/RenderDoc 验收收口。 |
| 2026-06-24 | Mesh pipeline shader source owner split | render_plan08_mesh_pipeline_shader_source_owner_split_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 runtime source cutover 的结构收束片：新增 `graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/shader_source.rs` 作为 Base mesh shader source owner，集中持有 `MeshPipelineShaderSource`、raw WGSL hash 包装、fallback/missing shader 的 `mesh_pipeline_standard_material_template_source(...)`、static `GeometrySourceDescriptor` 选择、`standard_material_surface_source_for_features(...)` 和 `assemble_material_shader_template(...)`。`ensure_pipeline.rs` 现在只调用 `mesh_pipeline_shader_source(...)`，保留 WGPU shader module/render pipeline 创建、disk cache lookup/write 和 module cache key 逻辑，不再直接导入 template assembly、geometry descriptor 或 standard material source helper。`mesh_pipeline_cache/mod.rs` 继续只做窄 re-export 给 dynamic prewarm。新增 `mesh_pipeline_template_source_hashes_include_template_revision`，并扩展 `runtime_15_render_shader_template_assembly_is_folder_backed` 与 `runtime_15_builtin_fallback_prewarm_uses_template_source` 锁定 `shader_source.rs` owner、no assembly imports in `ensure_pipeline.rs`、line budgets 和 docs/status anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 已通过；tracked-path scoped `git diff --check` 已通过且仅报告既有 LF→CRLF 提示，未跟踪 touched paths 由空白/冲突扫描覆盖。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需让 asset-root `.zshader`/`.zmaterial` prewarm 在适用时走 template assembly source owner，并继续迁移 Velocity/TAA/deferred pass cache owner 到同一 source/hash contract；真实 Naga/WGPU pipeline creation、产品对拍与 RenderDoc 验收仍未完成。 |
| 2026-06-24 | Builtin fallback prewarm template source alignment | render_plan08_builtin_fallback_prewarm_template_source_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M4 staged cache 对齐上一片 runtime source cutover：`dynamic_api/shader_prewarm.rs::builtin_fallback_shader_prewarm_manifest()` 不再写旧 `FALLBACK_MESH_SHADER`，而是通过 `graphics::scene` facade 调用 `mesh_pipeline_standard_material_template_source(...)`，把 `MeshPipelineShaderSource.wgsl_source`、`cache_content_hashes` 和 `template_revision` 原样写入 `ShaderVariantPrewarmRequest`。为避免复制 assembly 逻辑，`MeshPipelineShaderSource` 与 builder 从 `mesh_pipeline_cache/ensure_pipeline.rs` 沿 `mesh_pipeline_cache/mod.rs`、`mesh/mod.rs`、`scene_renderer/mod.rs`、`scene/mod.rs` 做 crate-visible 窄导出；assembly 失败时内置预热清单返回空 manifest，不回退旧 monolithic fallback source。新增 `builtin_fallback_shader_prewarm_manifest_uses_mesh_template_source` 与 `runtime_15_builtin_fallback_prewarm_uses_template_source`，锁定 source 中 `zr_material_surface`、Forward `vs_main`/`fs_main`、light-grid/shadow include、template revision 和 no `FALLBACK_MESH_SHADER` 预热。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 与 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需让 asset-root `.zshader`/`.zmaterial` prewarm 也可走 template assembly，而非只对内置 fallback 生效；同时补 Velocity/TAA/deferred pass cache owner、真实 Naga/WGPU pipeline creation、产品对拍与 RenderDoc 验收。 |
| 2026-06-24 | Mesh pipeline standard material template source cutover | render_plan08_mesh_pipeline_standard_material_template_source_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 runtime cutover 首片把 Base mesh fallback/missing shader source 从整段 `FALLBACK_MESH_SHADER` 推进到标准材质 Forward 模板 assembly 输出。`PipelineKey::shader_feature_bits()` 现在 crate-visible，`mesh_pipeline_cache/ensure_pipeline.rs` 通过 `mesh_pipeline_standard_material_template_source(...)` 使用 `GEOMETRY_SOURCE_ID_STATIC_MESH`、`standard_material_surface_source_for_features(...)` 和 `assemble_material_shader_template(...)` 生成 `MeshPipelineShaderSource`；ready custom WGSL 仍保留 raw source 路径。磁盘 cache key 现在使用 template include content hashes 加最终 WGSL source hash，shader module cache key 也带 `source_hash`，避免 cutoff/source/template 变化复用旧 module。新增 `mesh_pipeline_standard_material_template_source_assembles_forward_base_source` 与 `mesh_pipeline_template_source_hashes_feed_disk_and_module_keys`，`runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 `ensure_pipeline.rs`、`shader_feature_bits`、source hash 和 docs/status anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 与 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把 dynamic prewarm manifest 改为同一 template assembly source，补 Velocity/TAA/deferred/template pass cache owner 迁移，并跑 Naga/WGPU pipeline creation、产品对拍与 RenderDoc 验收。 |
| 2026-06-24 | Forward template built-in shading model dispatch parity foundation | render_plan08_forward_template_builtin_shading_model_dispatch_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M3 runtime cutover 前置补齐模板 Forward 与当前 fallback mesh shader 的内建 shading-model 分发面：`zr_surface_types.wgsl` 的 `ZrSurfaceOutput` 新增 `unlit` 与 `shading_model_id`，`material_surface.rs` 通过 `standard_material_shading_model_id()` 从 `standard_material_properties.data8.y` 解码模型 id，并在 `data0.w >= 0.5` 时收敛到 Unlit；`zr_shading_standard_pbr.wgsl` 增加 `ZR_SHADING_MODEL_UNLIT_ID`、`ZR_SHADING_MODEL_BLINN_PHONG_ID`、`ZR_SHADING_MODEL_STANDARD_PBR_ID`，Forward `shade_forward(...)` 对 Unlit 直接返回 base+emissive，对 BlinnPhong 走 `zr_standard_pbr_shade_blinn_phong_light_vector(...)`，否则走 StandardPBR。`render_shader_template_assembles_standard_material_surface_source` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 `standard_material_shading_model_id` 和 `surface.shading_model_id == ZR_SHADING_MODEL_BLINN_PHONG_ID` anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 与 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把内建 shading model registry include 拆成正式 `zr_shading_{unlit,blinn_phong,standard_pbr}.wgsl` 体系、补 GBuffer/deferred parity，再切换 mesh pipeline cache standard/fallback source 并做 Naga/WGPU/产品/RenderDoc 验收。 |
| 2026-06-24 | Forward template runtime light-grid shadow shading context foundation | render_plan08_forward_template_light_grid_shadow_context_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 runtime template cutover 前置把 Forward template 的 shading 输入面接近当前 WGPU fallback mesh shader：`include_registry.rs` 新增 `zr_light_grid.wgsl` 与 `zr_shadow.wgsl` include，`pass_specialization.rs` 通过 `support_includes` 只给 Forward 注入 light-grid/shadow 支撑 include，`zr_surface_types.wgsl` 扩展 `ZrVertexOutput` 的 runtime `tint`、`shadow_params`、`motion_params` 与 `ZrShadingContext`/`zr_build_shading_context(input)`，`zr_shading_standard_pbr.wgsl` 改为消费 `ZrShadingContext`、`zr_light_grid_params`、`zr_gpu_light(...)` 和 `zr_gpu_light_shadow_visibility(...)`，并用 `ZR_FEATURE_RECEIVE_SHADOWS && ctx.shadow_params.z > 0.5` 控制 shadow 采样。标准材质 source 同步把 base color 乘以 `input.tint * input.color`，`render_shader_template_assembles_standard_material_surface_source` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 `zr_build_shading_context(input)`、light-grid/shadow include、receive-shadows 条件和 docs/status anchors。 | scoped rustfmt、static source scan、docs-anchor scan、line-count scan、touched-file whitespace/conflict-marker scan 与 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需实际把更多 pass cache owner 切到 template/source-hash 输出，补 Naga/WGPU pipeline creation、shading-model dispatch parity 与产品/RenderDoc 验收；Velocity motion-vector source/cache 已由 `render_plan08_velocity_pipeline_template_source_cache_static_passed_cargo_deferred_implementation_cadence` 关闭。 |
| 2026-06-24 | Standard material runtime feature surface source foundation | render_plan08_standard_material_runtime_feature_surface_source_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1/MS-M4 template runtime cutover 前置把标准材质 surface source 从“只能消费 `StandardMaterialDescriptor`”拆成可由 runtime feature key 重建的 helper：`graphics/shader/template/material_surface.rs` 新增 `standard_material_surface_source_for_features(features, alpha_cutoff)`，现有 `standard_material_surface_source(descriptor)` 继续调用它；helper 保留 `ShaderFeatureBits::ALPHA_TEST`/`RECEIVE_SHADOWS`/`DOUBLE_SIDED` 传入结果，并集中 clamp/NaN 处理 `ZR_STANDARD_MATERIAL_ALPHA_CUTOFF`。新增 `standard_material_surface_source_can_be_built_from_runtime_features`，`runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 helper、测试名和 docs/status anchors。 | scoped rustfmt、static source scan、docs-anchor scan、line-count scan、touched-file whitespace/conflict-marker scan 与 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续 mesh pipeline cache 可从 `PipelineKey.shader_feature_bits()` + cutoff 直接生成标准材质 template source；仍需实际切换 standard/fallback shader source、补 WGPU pipeline creation 与产品/RenderDoc 验收。 |
| 2026-06-24 | PipelineKey receive-shadows shader feature foundation | render_plan08_pipeline_key_receive_shadows_shader_feature_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M4/运行时模板接入前置把 `PipelineKey` 补齐 `receive_shadows` shader feature 维度：`resource_streamer_ensure_material.rs` 从 `StandardMaterialDescriptor.receive_shadows` 写入 `PipelineKey.receive_shadows`，`pipeline_key.rs` 在派生 `ShaderVariantKey` 时把该字段映射到 `ShaderFeatureBits::RECEIVE_SHADOWS`，`default_pipeline_key()` 与标准材质 PBR projection tests 同步锁定默认 true、覆盖 false 和 `material.pipeline_key.receive_shadows` 标准材质 runtime key。该切片让后续 `standard_material_surface_source(...)` 的 `RECEIVE_SHADOWS` feature 能由 runtime cache key 重建，而不是只能存在于 template-only descriptor projection。 | scoped rustfmt、source anchor scan、docs-anchor scan、line-count scan、touched-file whitespace/conflict-marker scan 与 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。Cargo/Naga/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把 mesh pipeline cache 的 standard/fallback shader source 切到 template assembly 输出，并让 prewarm asset-root `.zmaterial` feature 枚举也纳入 receive-shadows 维度；真实 WGPU pipeline creation 与产品/RenderDoc 验收仍未完成。 |
| 2026-06-24 | Shader template runtime entry alias foundation | render_plan08_shader_template_runtime_entry_alias_static_passed_cargo_timeout_no_result | Plan 08 MS-M1 Shader template runtime entry alias foundation 把 template pass WGSL 从仅导出 `zr_vs_main`/`zr_fs_main` 推进到同时导出当前 WGPU mesh pipeline 使用的 `vs_main`/`fs_main`。Forward/GBuffer/Depth/DepthAlpha/Shadow/ShadowAlpha/Velocity 模板现在先把主体落在非入口 `zr_vs_main_impl(...)`，带 fragment 的模板再把主体落在 `zr_fs_main_impl(...)`；`zr_vs_main`/`zr_fs_main` 保留为模板调试入口，`vs_main`/`fs_main` 作为 runtime entry alias 调同一 impl，避免入口函数互调。`render_shader_template_assembles_static_and_skinned_geometry_sources`、`render_shader_template_validates_standard_material_wgsl_with_naga` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 `zr_vs_main_impl`、`zr_fs_main_impl`、`vs_main`、`fs_main` 和 docs/status anchors。 | scoped rustfmt、entry-alias source anchor scan、docs-anchor scan、line-count scan、touched-file whitespace/conflict-marker scan 和 scoped `git diff --check` 已通过；`git diff --check` 仅报告既有 LF→CRLF 提示。聚焦 `cargo test -p zircon_runtime --lib render_shader_template_validates_standard_material_wgsl_with_naga --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-render-shader-template-entry -- --test-threads=1` 300 秒超时且残留子进程已清理，无 Cargo/Naga 结果，不计通过；WGPU 与 RenderDoc 未运行。 | 后续仍需把更多 mesh pipeline cache pass owner 切到 template assembly/source-hash 输出；Velocity motion-vector entry 与 previous-position vertex slot 语义已由 `render_plan08_velocity_pipeline_template_source_cache_static_passed_cargo_deferred_implementation_cadence` 关闭；bind group layout、WGPU pipeline creation、prewarm cache 与产品/RenderDoc 验收仍未完成。 |
| 2026-06-24 | Shader template runtime scene transform foundation | render_plan08_shader_template_runtime_scene_transform_static_passed_cargo_timeout_no_result | Plan 08 MS-M1 Shader template runtime scene transform foundation 把 template WGSL 从 object-space 直出推进到当前 runtime scene/GPUScene ABI：新增 `graphics/shader/wgsl/zr_scene_runtime.wgsl` 声明 group0 `SceneUniform`，`include_registry.rs` 把 `zr_scene_runtime.wgsl` 与 runtime `scene_renderer/mesh/shaders/zr_gpu_scene.wgsl` 纳入 assembly content hashes，`zr_build_vertex_output(...)` 现在接收 `instance_index`，用 `zr_world_from_local(instance_index)` 与 `scene.view_proj * position_ws` 生成 `clip_position`，并把 normal/tangent 转到 world-space。为避免 template skinned source 与 runtime group3 binding1/2 的 `zr_instance_data`/`zr_light_data` 冲突，`zr_geometry_skinned.wgsl` 和 `zr_geometry_skinned_morphed.wgsl` 先复用现有 `zr_skinned_joint_matrix(...)`/`zr_previous_skinned_joint_matrix(...)` uniform palette helper；morph deltas/weights 预留到 group3 binding7/8，storage palette 升级仍归 MS-M2。`render_shader_template_assembles_static_and_skinned_geometry_sources` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 scene include、GPUScene transform、skinned palette ABI 和 static/skinned Naga validation call site。 | scoped rustfmt/static source scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 和 scoped `git diff --check` 已通过；聚焦 `cargo test -p zircon_runtime --lib render_shader_template_assembles_static_and_skinned_geometry_sources --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-render-shader-template-scene -- --test-threads=1` 300 秒超时且残留子进程已清理，无 Cargo/Naga 结果，不计通过；WGPU/RenderDoc 未运行。 | 后续仍需在空闲 Cargo lane 复跑该 focused test 与完整 `render_shader_template` 过滤，再把 runtime standard/fallback shader entry point、bind group layout 和 WGPU pipeline cache 消费切到 template 输出后做产品/RenderDoc 验收。 |
| 2026-06-24 | Shader template vertex input ABI alignment | render_plan08_shader_template_vertex_input_abi_alignment_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Shader template vertex input ABI alignment 修正 `graphics/shader/wgsl/zr_surface_types.wgsl` 的 `ZrVertexInput` location 顺序，使其匹配当前 `GpuMeshVertex::layout()`：position 0、normal 1、uv0 2、joints 3、weights 4、tangent 5、color 6、uv1 7。`render_shader_template_assembles_standard_material_surface_source` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 现在锁定该输入 ABI，避免后续 runtime template cutover 时把 uv/tangent/joint 数据读错。 | scoped rustfmt/static source scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/Naga test execution/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需运行 Naga/Cargo 验证、补 bind group layout/WGPU pipeline 验证，并把 runtime standard/fallback shader 切到模板路径后做真实 vertex-input product/RenderDoc 验收。 |
| 2026-06-24 | Standard material uniform alpha cutoff foundation | render_plan08_standard_material_uniform_alpha_cutoff_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Standard material uniform alpha cutoff foundation 把 alpha-mask cutoff 从仅有 pipeline key / test capture 提升为生产 `MaterialRuntime.alpha_cutoff` 与 standard material uniform 输入。`resource_streamer_ensure_material.rs` 现在把 `RenderMaterialAlphaMode::Mask { cutoff }` 写入运行时材质，`gpu_material_uniform_resource.rs` 将经过有限值检查和 0..1 clamp 的 cutoff 写入 `data8.z`，并新增 `standard_material_uniform_packs_alpha_cutoff_for_template_clip` 覆盖 None、负值、有效值、超范围和 NaN。`graphics/shader/template/material_surface.rs` 的 `standard_material_alpha_cutoff()` 优先读取 `standard_material_properties.data8.z`，再回退到 descriptor 生成的 `ZR_STANDARD_MATERIAL_ALPHA_CUTOFF`，使 template alpha clip 与后续 WGPU runtime cutover 使用同一 uniform ABI。 | scoped rustfmt/static source scan、docs-anchor scan、touched-file whitespace/conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/Naga test execution/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需运行 Naga/Cargo 验证、补 bind group layout/WGPU pipeline 验证，并把 runtime standard/fallback shader 切到模板路径后做真实 alpha-mask product/RenderDoc 验收。 |
| 2026-06-24 | Standard material alpha clip pass foundation | render_plan08_standard_material_alpha_clip_pass_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Standard material alpha clip pass foundation 在 `graphics/shader/wgsl/zr_surface_types.wgsl` 新增 `alpha_cutoff` 与纯判断 helper `zr_surface_fails_alpha_clip(...)`，标准材质 source 从 `StandardMaterialDescriptor::alpha_mode` 生成 `ZR_STANDARD_MATERIAL_ALPHA_CUTOFF` 并写入 `surface.alpha_cutoff`。Forward/GBuffer 模板各自声明 fragment-only `zr_apply_alpha_clip(...)` 并调用该 helper；Depth/Shadow 在 `ShaderFeatureBits::ALPHA_TEST` 时改选 `zr_template_depth_alpha.wgsl` / `zr_template_shadow_alpha.wgsl`，fragment 只取 `zr_material_surface(input)` 并执行 discard，不把 alpha-only discard 逻辑塞进无 fragment 的 depth/shadow 模板。新增/更新 `render_shader_template_clips_alpha_for_masked_standard_material_passes`、`render_shader_template_assembles_standard_material_surface_source` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 cutoff 常量、alpha 模板 token、discard helper 和 docs/status anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/Naga test execution/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需运行 Naga/Cargo 验证、补 bind group layout/WGPU pipeline 验证，并把 runtime standard/fallback shader 切到模板路径。 |
| 2026-06-24 | Shader template root GeometrySource export restoration | render_plan08_shader_template_root_geometry_exports_m6_cargo_passed | Plan 08 shader template assembly 的 graphics consumer 继续经 `core::framework::render` facade 使用 GeometrySource descriptor 合同；本切片只恢复 `render/mod.rs` 对 `GeometrySourceDescriptor` 与四个内建 WGSL include token 的薄转发，避免 `graphics/shader/template/{assemble,include_registry}.rs` 在默认 editor/plugin 测试构建中找不到该合同。未新增 root 行为、未添加兼容 shim、未切换 runtime pipeline。 | `rustfmt --edition 2021 zircon_runtime\src\core\framework\render\mod.rs` 通过；M6 editor/plugin 复跑中 `cargo test --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --lib --no-run --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-descriptor-rerun-0624 --message-format short --color never` 通过；同包 `cargo test --lib` 4/4 通过；`cargo test -p zircon_editor --lib export_wizard_panel_ --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-descriptor-rerun-0624 --message-format short --color never -- --test-threads=1 --nocapture` 18/18 通过。该证据证明默认 editor/plugin 测试图可重新编过 shader template root import；专门的 `render_shader_template --core-min` focused lane 仍待单独复跑。 | 后续仍需复跑 `cargo test -p zircon_runtime --lib render_shader_template --no-default-features --features core-min`，再推进 Naga/WGPU shader-template validation 与 runtime standard/fallback material cutover。 |
| 2026-06-24 | GeometrySource descriptor contract foundation | render_plan08_geometry_source_descriptor_contract_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 GeometrySource descriptor contract foundation 已把 `core/framework/render/shader/geometry_source.rs` 从仅有 id reserved segment 扩展为 backend-neutral `GeometrySourceDescriptor`、`GeometrySourceVertexAttribute`、`GeometrySourceBindingKind` 与 `GeometrySourceBindingRequirement` 合同。四个内建 GeometrySource descriptor helper 现在覆盖 static/skinned/morphed/skinned-morphed token、WGSL include token、GPUScene instance binding、skinning palette storage、morph weights/target storage 和 `ZR_GEOMETRY_SOURCE_ID`/primary define 输出；`shader/mod.rs` 重新导出该合同。新增 `runtime_15_render_shader_geometry_source_descriptor_contract_is_complete` 锁定无 `wgpu::`、四内建 descriptor、shape requirements、module exports、298 行 owner 预算和 Plan 08/render index/shader doc/structure/review/session docs anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 已由 Shader template assembly foundation 接到 graphics 层拼接合同；后续继续把 standard material/fallback shader 切到模板路径，并推进 custom geometry/shading-model registry export。 |
| 2026-06-24 | Shader template assembly foundation | render_plan08_shader_template_assembly_foundation_static_passed_cargo_timeout_no_result | Plan 08 MS-M1 Shader template assembly foundation 新增 `graphics/shader/template/{mod,assemble,include_registry,pass_specialization,tests}.rs` 和 `graphics/shader/wgsl/zr_{surface_types,geometry_*,template_*,shading_standard_pbr}.wgsl`。`assemble_material_shader_template(...)` 现在消费 `GeometrySourceDescriptor`、`ShaderPassType`、`ShaderFeatureBits` 和材质 surface 函数，按 defines header → surface types → GeometrySource include → optional shading include → renamed `zr_material_surface` → pass template 的顺序生成稳定 WGSL；include registry 会去重并输出 content hashes，pass specialization 覆盖 Forward/GBuffer/DepthPrepass/Shadow/Velocity，Velocity 显式消费 `fetch_prev_position`，Depth/Shadow 只在 `ALPHA_TEST` 时保留 surface。新增 `render_shader_template_assembles_static_and_skinned_geometry_sources`、`render_shader_template_specializes_depth_and_velocity_passes`、`render_shader_template_rejects_reserved_material_symbols` 与 `runtime_15_render_shader_template_assembly_is_folder_backed`。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；line-count 当前为 shader mod 15、template mod 20、assemble 211、include_registry 98、material_surface 148、pass_specialization 64、validation 37、tests 239、guard 237。聚焦 `cargo test -p zircon_runtime --lib render_shader_template --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-render-shader-template -- --test-threads=1` 300 秒超时，无测试结果且不计 Cargo 通过；后台残留为无关 editor 编译通道。 | 下一步把 standard material/fallback 材质接到该 template assembly 输出，随后再做真实 Naga/WGPU 编译验证、非静态 geometry runtime cache 消费与二次启动 miss=0 验收。 |
| 2026-06-24 | Standard material template surface foundation | render_plan08_standard_material_template_surface_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Standard material template surface foundation 在 `graphics/shader/template/material_surface.rs` 新增标准材质 surface 源 owner：`standard_material_surface_source(...)` 从 `StandardMaterialDescriptor` 投影 `ShaderFeatureBits::ALPHA_TEST`、`RECEIVE_SHADOWS` 与 `DOUBLE_SIDED`，并暴露 `standard_material_surface` WGSL、group 2 material texture/sampler/uniform 绑定、PBR base-color/metallic/roughness/occlusion/emissive 采样路径。`assemble.rs` 的 generated header 现在写出 `ZR_FEATURE_ALPHA_TEST`、`ZR_FEATURE_RECEIVE_SHADOWS` 和 `ZR_FEATURE_DOUBLE_SIDED` 常量；`template/mod.rs` 只挂载并重导出该 leaf owner。新增 `render_shader_template_assembles_standard_material_surface_source` 与更新后的 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 descriptor projection、入口改名为 `zr_material_surface`、binding 10 uniform、material source owner、docs/status anchors 和行数预算。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把 runtime standard/fallback material shader 真实切到 template assembly 输出，补 Naga/WGPU validation、alpha discard 深度/阴影语义、uv1/tangent/normal-map parity、bind group layout 验证与 mesh pipeline/prewarm cache 消费。 |
| 2026-06-24 | Shader template Naga validation foundation | render_plan08_shader_template_naga_validation_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Shader template Naga validation foundation 在 `graphics/shader/template/validation.rs` 新增 `validate_material_shader_template_wgsl(...)`、`MaterialShaderTemplateValidation` 与 `ShaderTemplateValidationError`，集中调用 `naga::front::wgsl::parse_str` 和 `naga::valid::Validator`，并返回组装 WGSL 的 entry point 摘要。新增 `render_shader_template_validates_standard_material_wgsl_with_naga` 作为标准材质模板的 Naga 合同测试；`runtime_15_render_shader_template_assembly_is_folder_backed` 同步锁定 validation leaf owner、module export、docs/status anchors 和行数预算。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/Naga test execution/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续需在 Cargo lane 空闲后运行 `render_shader_template_validates_standard_material_wgsl_with_naga`，再把 runtime standard/fallback shader 输出接入该验证路径与 WGPU pipeline cache。 |
| 2026-06-24 | Shader template uv1 tangent interpolation foundation | render_plan08_shader_template_uv1_tangent_interpolation_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Shader template uv1 tangent interpolation foundation 扩展 `graphics/shader/wgsl/zr_surface_types.wgsl` 的 `ZrVertexOutput`，新增 `uv1`、`tangent_ws` 与 `tangent_handedness` 插值字段，并把 `zr_build_vertex_output(...)` 改为接收 `fetch_tangent(v, instance_index)` 和 `fetch_uv1(v)`。Forward/GBuffer/Depth/Shadow/Velocity pass templates 全部传入 tangent/uv1；`standard_material_surface_source(...)` 现在用 `input.uv1` 作为 UV channel 1 输入，不再把 uv1 退化为 uv0。`render_shader_template_assembles_standard_material_surface_source` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 surface interpolation、pass template fetch 调用、标准材质 source 和 docs/status anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/Naga test execution/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需把 normal map sampling/tangent frame 接入 `standard_material_surface`，补 alpha discard 深度/阴影语义，并把 runtime standard/fallback shader 切到模板与 WGPU pipeline cache。 |
| 2026-06-24 | Standard material normal map sampling foundation | render_plan08_standard_material_normal_map_sampling_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M1 Standard material normal map sampling foundation 在 `graphics/shader/template/material_surface.rs` 新增 `standard_material_sampled_normal(...)` 与 `standard_material_normalize_or_fallback(...)`，使用 `standard_material_normal_tex`/sampler、`input.tangent_ws`、`input.tangent_handedness` 和 `input.normal_ws` 构造 tangent frame，并把 `surface.normal_ws` 改为 normal-map 采样结果。`render_shader_template_assembles_standard_material_surface_source` 与 `runtime_15_render_shader_template_assembly_is_folder_backed` 锁定 normal texture、tangent frame helper、标准材质 source 和 docs/status anchors。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；Cargo/Naga test execution/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需运行 Naga/Cargo 验证，补 alpha discard 深度/阴影语义、bind group layout/WGPU pipeline 验证，并把 runtime standard/fallback shader 切到模板路径。 |
| 2026-06-24 | Shader prewarm geometry-source enumeration | render_plan08_shader_prewarm_geometry_source_enumeration_static_passed_cargo_deferred_implementation_cadence | Plan 08 MS-M4 Shader prewarm geometry-source enumeration 已把 asset-root 自动预热从硬编码 static mesh 扩展为显式 geometry source 维度：`zircon_shader_prewarm --geometry-source static|skinned|morphed|skinned-morphed|all` 默认仍为 static，`all` 通过 `builtin_geometry_source_descriptors()` 展开四个内建 source；`asset_root_manifest_for_quality_tiers_and_geometry_sources(...)` 在 pass × quality × geometry source 上生成 `ShaderVariantKey`，并保持旧 `asset_root_manifest_for_quality_tiers(...)` 兼容 static 默认。`tools/zircon_build.py --shader-geometry-source ...` 按同样维度转发 staged cache 生成。新增 `shader_prewarm_args_default_to_static_geometry_source`、`shader_prewarm_args_expand_all_builtin_geometry_sources`、`shader_prewarm_asset_root_manifest_expands_requested_geometry_sources` 与 `runtime_15_shader_prewarm_geometry_source_enumeration_is_wired`。 | scoped rustfmt、static source scan、line-count scan、docs-anchor scan、touched-file whitespace scan、conflict-marker scan、py_compile 和 scoped `git diff --check` 已通过；line-count 当前为 args 206、run 79、manifest 707、manifest tests 183、guard 181。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | 后续仍需 custom geometry/shading-model plugin id registry export、真实 naga/wgpu prewarm compile、Velocity/TAA/deferred/template cache owner 迁移与二次启动 miss=0 产品验收。 |
| 2026-06-24 | Material runtime PBR projection tests owner split | render_plan08_material_runtime_pbr_projection_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | Material runtime PBR projection tests owner split 已把 `graphics/scene/render_product_streamer_tests/material_runtime.rs` 中标准 PBR runtime key 投影长测试与 `RenderMaterialTextureTransform` helper 迁入 `graphics/scene/render_product_streamer_tests/material_runtime/pbr_projection.rs`；父测试 owner 从 768 行降到 572 行，只保留 BlinnPhong、sort offset、shadow override、authored texture key fallback、standard texture alias 与 shader property/slot runtime mapping coverage 以及 `mod pbr_projection;` 挂载。新增 `runtime_15_material_runtime_pbr_projection_tests_are_child_owner` 锁定 moved test/helper ownership、父/子 800 行预算和 Plan 08/structure/review/render architecture/session docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan 和 scoped `git diff --check` 已通过；line-count 当前为 parent 572、child 200、guard 101。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | Cargo lane 空闲后补跑 `material_runtime` focused render product streamer tests，并继续收敛 Plan 08 GeometrySource/template、custom shader GPU skinning 和 variant-cache owner 缺口。 |
| 2026-06-24 | Project render scene product tests owner split | render_project_scene_products_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | Project render scene product tests owner split 已把 `graphics/tests/project_render.rs` 中的 directory project render、vampire example visible pixels、manual screenshot export、project material shader color output 与 wire-only coverage 迁入 `graphics/tests/project_render/project_scenes.rs`；父测试 owner 从 774 行继续降到 467 行，只保留 project asset/WGSL/mesh/material/scene writers、snapshot submit helpers 与 `mod project_scenes; mod render_quality;` 挂载。新增 `runtime_15_project_render_scene_products_tests_are_child_owner` 锁定 moved test ownership、父/子/render_quality 800 行预算和 Plan 08/render index/structure/review/render submit/session docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan、conflict-marker scan 和 scoped `git diff --check` 已通过；line-count 当前为 parent 467、project_scenes 240、render_quality 594。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | Cargo lane 空闲后补跑 `project_render` scene-products focused tests，并继续收敛 Plan 08 GeometrySource/template 与 variant-cache owner 缺口。 |
| 2026-06-24 | Project render quality/deferred tests owner split | render_project_render_quality_deferred_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | Project render quality/deferred tests owner split 已把 `graphics/tests/project_render.rs` 中的 temporal material history、SSAO shared-HZB、clustered lighting quality profile 与 Deferred G-buffer product tests 迁入 `graphics/tests/project_render/render_quality.rs`；父测试 owner 从 1387 行降到 774 行，只保留基础 project render/export/material/wire tests、shared asset fixtures 与 `mod render_quality;`。新增 `runtime_15_project_render_quality_tests_are_child_owner` 锁定 moved test/helper ownership、父子 800 行预算和 Plan 04/05/08/render index/structure/review/render submit docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan 和 scoped `git diff --check` 已通过；line-count 当前为 parent 774、child 631、guard 98。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | Cargo lane 空闲后补跑 `project_render` quality/deferred focused product group，并继续收敛 Plan 08 GeometrySource/template 与 variant-cache owner 缺口。 |
| 2026-06-24 | RendererData asset-aware compile tests owner split | render_plan08_renderer_data_asset_compile_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | RendererData asset-aware compile tests owner split 已把 `graphics/tests/renderer_data_asset.rs` 中的五个 shader/material asset-aware compile diagnostic tests 迁入 `graphics/tests/renderer_data_asset/asset_aware_compile.rs`；父测试 owner 从 888 行降到 715 行，只保留 document roundtrip/projection/authoring-name/stage-name/unknown-source tests、shared fixtures 与 `mod asset_aware_compile;` 挂载。新增 `runtime_15_renderer_data_asset_compile_tests_are_child_owner` 锁定 moved test 不回流、父/子 800 行预算和 Plan 08/render index/structure/review/SRP RendererData/render submit docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan 和 scoped `git diff --check` 已通过；line-count 当前为 parent 715、child 277、guard 96。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓，不计通过。 | Cargo lane 空闲后补跑 `renderer_data_asset` / asset-aware compile focused lane，并继续收敛 Plan 08 GeometrySource/template 与 variant-cache owner 缺口。 |
| 2026-06-24 | Render material product debug-counts test owner split | render_plan08_material_product_debug_counts_test_owner_split_static_passed_cargo_deferred_active_compile_lane | Render material product debug-counts test owner split 已把 `graphics/scene/render_product_material_property_tests.rs` 中的长篇 material uniform debug/counts、management record/query/selection 与 prepared-state aggregate 覆盖迁入 `graphics/scene/render_product_material_property_tests/uniform_debug_counts.rs`；父测试 owner 从 921 行降到 424 行，只保留 compact product tests、shared fixtures 与 `mod uniform_debug_counts;` 挂载。新增 `runtime_15_render_material_product_debug_counts_tests_are_child_owner` 锁定 moved test 不回流、父/子 800 行预算和 Plan 08/render index/structure/review/render-assets/zmeta/material docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped `git diff --check` 已通过；line-count 当前为 parent 424、child 500、guard 90。Cargo/WGPU/RenderDoc 因 active compile lane 暂缓，不计通过。 | Cargo lane 空闲后补跑 `render_product_material_property_tests` focused lib-test lane，并继续收敛 Plan 08 GeometrySource/template 与 variant-cache owner 缺口。 |
| 2026-06-24 | Render material readiness report tests owner split | render_plan08_material_readiness_report_tests_owner_split_static_passed_cargo_deferred_active_compile_lane | `core/framework/render/material/readiness_report.rs` 从 769 行退为 266 行 material readiness DTO/summary/issue/prepared state 与 management snapshot/record production owner,只保留 `#[cfg(test)] mod tests;` 挂载;新增 502 行 `core/framework/render/material/readiness_report/tests.rs` 承接 readiness diagnostic dedup/status severity/summary-prepared-state/management record-set coverage。结构守卫 `runtime_15_render_material_readiness_report_tests_are_child_owner` 锁定 moved-test ownership、父/子 800 行预算和 Plan 08/render index/structure/review/material docs status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active compile lanes 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `material_readiness_report` focused lib-test lane,并继续收敛 Plan 08 剩余 GeometrySource/template 与 variant cache owner 缺口。 |
| 2026-06-24 | Render material management tests owner split | render_plan08_material_management_tests_owner_split_static_passed_cargo_deferred_active_compile_lane | `core/framework/render/material/management/tests.rs` 退为 44 行共享 fixture/root module,只保留 `record(...)`、`record_with_issue_counts(...)` 与子 owner 挂载;新增 `core/framework/render/material/management/tests/record_views.rs` 承接记录排序、summary、issue-index 与 issue-view 覆盖,新增 `core/framework/render/material/management/tests/query_execution.rs` 承接 issue-kind filtering、query page selection、sort/page execution 与 selected-record ordering 覆盖。结构守卫 `runtime_15_render_material_management_tests_are_child_owners` 锁定 moved-test ownership、三文件 800 行预算和 Plan 08/render index/structure/review/material docs status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active compile lanes 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `material_management` focused lib-test lane,并继续收敛 Plan 08 剩余 GeometrySource/template 与 variant cache owner 缺口。 |
| 2026-06-23 | Render index 当前状态总览拆分 | MS-M1/M2 部分完成,MS-M3 注册底座、deferred id 通路、asset/runtime 诊断、稳定 key 维度与内建 BlinnPhong forward/deferred 分支部分完成,MS-M4 key 契约、`PipelineKey`/mesh variant 派生桥、base mesh 磁盘缓存/miss 诊断、预热 manifest/CLI、staged cache handoff、asset-root shader 扫描、entry-point 多 pass 预热、`.zmaterial` alpha-test/double-sided feature、内置 shading-model 预热、初始 runtime revision 对齐、材质实例 alpha-blend Forward-only 预热、运行时 shader quality key 接线、quality-tier prewarm 枚举与 Base mesh quality-aware cache owner 已落地 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 08 Material/Permutation 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：GeometrySource/template owner、三模型完整 WGSL 模板/parity、非静态 geometry/custom shading-model plugin id 的完整 prewarm manifest、Velocity/TAA/deferred/template 运行时统一 cache、编辑后 `ResourceRecord.revision` registry 导出、剩余 pass 的 mesh pipeline/disk cache 以 `ShaderVariantKey` 做最终命中主键；验收缺口：需要 Unlit/BlinnPhong/StandardPbr forward-deferred 产品对拍、custom shading model 插件注册入口、variant cache hit/miss 宽验证、prewarm 二次启动 miss=0 |
| 2026-06-15 | MS-M1 GeometrySource and WGSL assembly | 部分完成: GPUScene include 与 group2/3 ABI 已硬切,通用 GeometrySource 管线未完成 | 计划 03 已让内建 forward/normal/shadow/deferred/PBR shader 拼接 `zr_gpu_scene.wgsl`,自定义测试 WGSL 也按 group2 material + group3 GPUScene 读取;但计划 08 的 GeometrySource registry、模板编译 owner 与磁盘缓存未落地。 | 计划 03 GS-M2 状态表记录 shader ABI hard cut、旧 model uniform/cache/replay 删除和 scoped `cargo check` 通过。 | 建立 shader template graph、GeometrySource key 和 shader source provenance dump。 |
| 2026-06-15 | MS-M2 GPU skinning/morph for all materials | 部分完成: GPUScene skinned palette 与 CPU-morphed previous source 已支撑内建路径 | 计划 03/06 已把 current/previous skinned palette、CPU-morphed morph-shape signature、changing-shape previous source velocity 接回 GPUScene/velocity path;但所有材质域统一 skin/morph 仍未完成。 | 计划 06 TP-M1/S23 状态表记录 previous source velocity tests 通过;计划 03 GS-M2 状态表记录 current/previous skinned palette group3 binding。 | 把 GPU skinning/morph 入口从 fallback shader 扩展到所有 shader permutation。 |
| 2026-06-16 | MS-M3-S1 shading model registry contract and deferred id path | 部分完成: 三个内置模型注册底座、运行时 `ShadingModelId` 与 deferred G-buffer id 通路已接入;完整三模型 parity 仍未完成 | 新增 `core/framework/render/material/shading_model.rs` 定义 `ShadingModelId`、`ShadingModelDescriptor`、`GBufferChannelMask` 和注册错误;新增 `graphics/material/shading_models/{mod,builtins,registry}.rs` 注册 `Unlit=0`、`BlinnPhong=1`、`StandardPbr=2`;`MaterialRuntime`/`PipelineKey` 携带 `shading_model_id`;标准材质 uniform 复用 `data8.y` 存 8-bit id 归一化值;deferred geometry 写 `gbuffer_material.a`,deferred lighting 解码并按 Unlit/BlinnPhong/StandardPbr 分发,其中 BlinnPhong 暂走 lit fallback。 | `rustfmt --edition 2021` touched Rust files 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shading-model-check-0616` 通过(仅既有 warning);新增源码/单元测试覆盖 id roundtrip、注册表重复/通道溢出、uniform packing、deferred encode/decode 源码合同。`cargo test -q -p zircon_runtime --lib shading_model --target-dir D:\cargo-targets\zircon-runtime-shading-model-check-0616` 在 lib-test 编译阶段被既有 `RenderBloomSettings { intensity: 0.6 }` 缺 `radius`/`threshold` 字段阻塞,未进入本切片测试。 | 下一步把 `lighting_model`/自定义模型诊断接入材质资产与 variant key,补 `zr_shading_*.wgsl`/`zr_gbuffer_encode.wgsl` 模板,实现 BlinnPhong 真正 deferred/forward parity 和三模型产品对拍。 |
| 2026-06-16 | MS-M3-S2 lighting-model asset/runtime diagnostics and stable variant dimension | 部分完成: 内置 lighting model 已解析为稳定 `ShadingModelId` 变体维度;未注册 custom 模型不再静默当 PBR ready | `PipelineKey` 移除重复的 `RenderMaterialLightingModel` 字段,改以 `shading_model_id` 作为当前材质模型变体维度;`MaterialRuntime`/capture seed 保留原始 lighting model 供调试和资产视图;`ResourceStreamer::ensure_material` 对 `custom:<name>` 但未注册的模型写入 `RenderMaterialValidationError::UnregisteredShadingModel` 与 Validation fallback,同时管线 key 使用 StandardPBR fallback id;新增/更新 render-product streamer 断言覆盖 PBR id=2、BlinnPhong id=1、自定义模型显式诊断。 | `rustfmt --edition 2021` touched Rust files 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shading-model-check-0616` 通过(仅既有 warning)。测试代码已补,但整包 lib-test 仍受上一行记录的既有 `RenderBloomSettings` 夹具缺字段阻塞,按用户指示测试延后。 | 下一步仍需 `zr_shading_*.wgsl`/`zr_gbuffer_encode.wgsl` 模板化、插件注册自定义 shading model 的公开入口,以及三模型产品/RenderDoc 对拍。 |
| 2026-06-16 | MS-M3-S3 builtin BlinnPhong forward/deferred shader branch | 部分完成: 内建 BlinnPhong 已从 StandardPBR fallback 拆成当前 forward fallback 与 deferred lighting 的真实分支;完整模板化与三模型产品 parity 仍未完成 | `fallback_mesh.wgsl` 从标准材质 uniform `data8.y` 解出 8-bit `shading_model_id`,unlit flag 继续强制走 `Unlit=0`,forward fallback 对 Unlit 直接输出 albedo+emissive,对 BlinnPhong 使用独立 diffuse/specular 函数,其他模型保留 StandardPBR 路径;`deferred_lighting.wgsl` 从 G-buffer material alpha 解码模型 id,把 id 传入 light-grid/shadow 光照函数,并用 `shade_deferred_lit` 分别调度 StandardPBR 与 BlinnPhong 的 diffuse/specular 规则;新增源码合同断言覆盖 forward/deferred 模型分发关键字符串。 | `rustfmt --edition 2021 zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline/fallback_mesh_shader_source.rs zircon_runtime/src/graphics/scene/scene_renderer/deferred/lighting_pipeline/tests.rs` 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shading-model-check-0616` 通过(仅既有 warning)。按用户"测试可以暂时延后"指令未运行新 WGSL 源码合同测试;整包 lib-test 仍需先修复既有 `RenderBloomSettings { intensity: 0.6 }` 缺 `radius`/`threshold` 夹具问题。 | 后续仍需把当前内建分支收敛进 `zr_shading_*.wgsl`/`zr_gbuffer_encode.wgsl` 模板体系,开放插件自定义 shading model 注册入口,补 forward/deferred 三模型产品对拍与 RenderDoc 验收。 |
| 2026-06-22 | F11 shading-model registry dead API removal | render_shading_model_registry_dead_api_removed_coremin_passed | `graphics/material/shading_models/registry.rs` 删除无项目/插件描述符 owner 的 `register_plugin()`、`supported_channels()`、`len()` 与全部 `#[allow(dead_code)]`;`resolve_token(...)` 改为私有 live helper,`resolve_lighting_model(...)` 统一通过 `RenderMaterialLightingModel::as_token()` 查询 token map;`ShadingModelRegistrationError::PluginIdBelowReservedRange` 随半接线插件入口删除。当前 runtime 仍只拥有 built-in registry;custom lighting model 继续显式产生 `UnregisteredShadingModel` readiness 诊断并回退 StandardPBR。 | scoped rustfmt 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime-f16-0622-coremin --message-format short --color never` 通过(既有 warnings);新增 `review_f11_shading_model_registry_has_no_dead_plugin_registration_surface` 结构守卫,focused lib-test 1/1 通过。 | custom shading-model plugin registration remains a future Plan 08 surface:需要先定义项目/插件 shading-model descriptor owner、registry export/import 生命周期和 shader-template include 接线,再开放插件注册入口。 |
| 2026-06-16 | MS-M4-S0 shader variant key contract foundation | 部分完成: Plan 08 目标 `ShaderVariantKey` 的 framework 契约已落地;mesh pipeline cache、磁盘缓存与预热仍未接入 | 新增 `core/framework/render/shader/geometry_source.rs` 定义内建 `GeometrySourceId` 段(`StaticMesh=0`,`SkinnedMesh=1`,`MorphedMesh=2`,`SkinnedMorphed=3`,插件自 4 起);扩展 `shader/variant_key.rs` 保留旧 `RenderShaderVariantKey` 的单模块编译请求职责,新增 `ShaderPassType`、`ShaderFeatureBits`、`ShaderQualityTier` 与完整 `ShaderVariantKey`;`packed_dims()` 固定位段为 geometry 0..3、shading 4..11、pass 12..15、features 16..47、quality 48..49,`canonical_string()` 产出含 material id/revision/platform 的磁盘缓存哈希输入;通过 `render::shader` 与 top-level render facade 再导出。 | `rustfmt --edition 2021 zircon_runtime/src/core/framework/render/shader/{geometry_source,variant_key,mod}.rs zircon_runtime/src/core/framework/render/mod.rs` 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shading-model-check-0616` 通过(仅既有 warning)。新增 key/geometry source 单测代码,但按用户"测试可以暂时延后"未运行 lib-test。 | 下一步把现有 `PipelineKey`/`mesh_pipeline_cache` 接到该 key,补磁盘缓存 `variant_cache/disk.rs`、miss report 和 `zircon_build.py --prewarm-shaders`。 |
| 2026-06-17 | MS-M4-S1a PipelineKey to ShaderVariantKey runtime bridge | 部分完成: 现有 WGPU mesh pipeline variant 已持有派生 `ShaderVariantKey`;磁盘缓存、预热与管线 map 主键切换仍未完成 | `PipelineKey::shader_variant_key(...)`/`shader_variant_key_for_geometry(...)` 现在从材质 shader id/revision、`shading_model_id`、pass type、geometry source 和 platform token 派生 neutral `ShaderVariantKey`;alpha-mask 与 double-sided 映射到 `ShaderFeatureBits`;`MeshPipelineVariantKey` 保留完整 `PipelineKey` 作为当前 WGPU render-pipeline state key,同时缓存派生 `ShaderVariantKey`,并把 Base/TAA reactive 映射 Forward、DepthPrepass 映射 DepthPrepass、ShadowDepth/ShadowDepthAlphaMask 映射 Shadow、Velocity 映射 Velocity。 | `rustfmt --edition 2021 zircon_runtime/src/graphics/scene/resources/pipeline/pipeline_key.rs zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/mesh_pipeline_variant_registry.rs` 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shading-model-check-0616` 通过(仅既有 warning)。新增派生 key 单测代码,但按用户"测试可以暂时延后"未运行 lib-test。 | 后续仍需把 `ShaderVariantKey` 升级为 mesh pipeline cache/disk cache/prewarm 的命中主键,补 variant miss report、shader provenance dump 和二次启动 miss=0 验收。 |
| 2026-06-17 | MS-M4-S1b shader variant disk cache and miss diagnostics | 部分完成: base mesh shader module 创建路径已接入 `ShaderVariantKey` 磁盘缓存与帧诊断;prewarm、Velocity/TAA 全路径和最终 pipeline map 主键切换仍未完成 | 新增 `graphics/shader/variant_cache/disk.rs` 以 `blake3(canonical_string + include/source hash)` 生成 shard key,按 `<root>/v1/<hash[0..2]>/<hash>.wgsl.zst` + `.meta` 写入 zstd WGSL 和 JSON metadata,支持 `ZR_SHADER_CACHE_DIR` 覆写、临时文件 + rename、损坏条目删除后 miss;新增 neutral `ShaderVariantMissReport` 并挂到 `RenderStats.last_shader_variant_miss_report`;runtime diagnostics 输出 `render.shader_variant.request_count/memory_hit_count/disk_hit_count/compile_miss_count/disk_write_count/disk_error_count`;`MeshPipelineCache::ensure_pipeline` 在 base mesh shader module 首次创建时读写磁盘缓存,同时保留完整 `PipelineKey` 作为当前 WGPU render-pipeline key。 | `rustfmt --edition 2021` touched shader/cache/render-stats/mesh-cache files 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shader-variant-cache-check-0617` 通过(仅既有 warning)。新增 disk hit/corrupt cleanup 与 miss-report 计数测试代码,但按用户"测试可以暂时延后"未运行 lib-test。 | 后续仍需把 Velocity/TAA reactive/deferred/template 变体都接入同一 cache,补 shader provenance dump、prewarm manifest/headless API、`zircon_build.py --prewarm-shaders`,以及二次启动同场景 compile miss=0 验收。 |
| 2026-06-17 | MS-M4-S1c shader prewarm manifest, CLI, and staged cache handoff | 部分完成: 离线预热入口、构建脚本参数和 packaged cache 查找链已落地;资产枚举、真实设备编译与二次启动 miss=0 验收仍未完成 | 新增 `ShaderVariantPrewarmManifest/Request/Report` 中立 DTO;新增 `graphics/shader/variant_cache/prewarm.rs` 通过同一磁盘缓存布局写入预热条目;新增 `dynamic_api::prewarm_shader_variants(...)` 与 `builtin_fallback_shader_prewarm_manifest()`;新增 `zircon_shader_prewarm` 命令行工具,支持 `--manifest` 与 `--builtin-fallback`,并输出 report JSON;`tools/zircon_build.py --prewarm-shaders` 在 runtime/editor assets staging 后运行该工具,把产物写入 `ZirconEngine/cache/shader_variants`;运行时 `ShaderVariantCacheDisk` 现在按"运行期写入目录 → staged 预热目录"查找。同步修复 ECS read-only query `single()` 的 `D: 'world` 生命周期约束以恢复当前 runtime 检查。 | `rustfmt --edition 2021` touched Rust files 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shader-prewarm-check-0617` 通过(仅既有 warning);`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-prewarm-bin-check-0617` 通过(仅既有 warning);`python -m py_compile tools\zircon_build.py` 通过;`python tools\zircon_build.py --targets runtime --out D:\zircon-shader-prewarm-dry-run --mode debug --prewarm-shaders --dry-run` 通过。新增 prewarm disk-write 测试代码,但按用户"测试可以暂时延后"未运行 lib-test。 | 后续仍需扫描 staged 资产/材质生成完整 variant manifest,把 Velocity/TAA reactive/deferred/template 变体接入同一 prewarm/cache,对预热 WGSL 做真实 naga/wgpu 编译,补 shader provenance dump,并完成二次启动同场景 compile miss=0 产品验收。 |
| 2026-06-17 | MS-M4-S1d asset-scanned shader prewarm manifest | 部分完成: `zircon_shader_prewarm` 已能从资产根扫描 `.zmeta` compound shader、`.zshader` 与 standalone `.wgsl` 并写入磁盘 cache;完整多 pass/多几何源 manifest 与真实运行时 revision 仍未完成 | `zircon_shader_prewarm --asset-root <dir>` 现在会扫描资产根,对 compound `.zmeta` 读取 `uuid/url/source_hash` 并定位同名 shader 包目录;对 `.zshader` 按 `wgsl_files` 顺序合并 WGSL,无 `wgsl_files` 时按包内 `.wgsl` 排序;对独立 `.wgsl` 直接生成预热项;生成 static-mesh Forward/StandardPBR/Mid quality `ShaderVariantKey` 并复用现有 `variant_cache/prewarm.rs` 写盘。`tools/zircon_build.py --prewarm-shaders` 现在把 staged `ZirconEngine/assets` 作为 `--asset-root` 传给预热工具,同时保留 `--builtin-fallback` 兜底。 | `rustfmt --edition 2021 zircon_runtime/src/bin/zircon_shader_prewarm/{args,manifest,run}.rs` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617` 通过(仅既有 warning);`python -m py_compile tools\zircon_build.py` 通过;`python tools\zircon_build.py --targets runtime --out D:\zircon-shader-asset-prewarm-dry-run --mode debug --prewarm-shaders --dry-run` 确认命令含 `--asset-root ZirconEngine/assets`;`cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617 -- --project-root <temp> --cache-dir <temp>/cache --report <temp>/report.json --asset-root examples/vampire/assets/shaders --pretty` 写入 4/4 变体。 | 后续仍需把材质资产实例组合成 alpha/double-sided/geometry/pass 完整 manifest,把 Runtime `ResourceManager` revision 导出到 staged manifest,对预热 WGSL 做真实 naga/wgpu 编译,补 Velocity/TAA/deferred/template 统一 cache,并完成二次启动 miss=0 产品验收。 |
| 2026-06-17 | MS-M4-S1e asset-scanned multi-pass shader prewarm | 部分完成: asset-root 预热已从单 Forward 扩到按 `.zshader` entry point 推导 pass 集合;材质实例 feature、非静态几何源和真实 runtime revision 仍未完成 | `zircon_shader_prewarm --asset-root <dir>` 现在给 vertex+fragment `.zshader` 生成 Forward/GBuffer/DepthPrepass/Shadow/Velocity 五个 static-mesh StandardPBR 变体;vertex-only 生成 DepthPrepass/Shadow/Velocity;fragment-only 生成 Forward/GBuffer;compute-only 不进入材质变体预热;standalone `.wgsl` 因无 stage 元数据继续生成完整材质 pass 集合。更新 asset-root 单测预期覆盖 compound `.zshader` 五 pass 展开。 | `rustfmt --edition 2021 zircon_runtime/src/bin/zircon_shader_prewarm/manifest.rs` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617` 通过(仅既有 warning);`cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617 -- --project-root <temp> --cache-dir <temp>/cache --report <temp>/report.json --asset-root examples/vampire/assets/shaders --pretty` 写入 20/20 变体。 | 后续仍需从材质资产实例组合 alpha-test/double-sided/quality/非静态 geometry source/shading model 维度,把 Runtime `ResourceManager` revision 导出到 staged manifest,对预热 WGSL 做真实 naga/wgpu 编译,并完成二次启动 miss=0 产品验收。 |
| 2026-06-17 | MS-M4-S1f asset-scanned material feature prewarm manifest | 部分完成: asset-root 预热已能把 `.zmaterial` 实例的 alpha-test/double-sided feature 组合进同一 `ShaderVariantKey`;quality、非静态几何源、shading model 与真实 runtime revision 仍未完成 | `zircon_shader_prewarm --asset-root <dir>` 现在额外扫描 `.zmaterial`,通过 `MaterialAsset` 读取 shader 引用、`AlphaMode` 和 `double_sided`,再按 shader URL 或 resource id 关联到已扫描 shader source;`AlphaMode::Mask` 映射 `ShaderFeatureBits::ALPHA_TEST`,`double_sided = true` 映射 `ShaderFeatureBits::DOUBLE_SIDED`,并按 `ShaderVariantKey::canonical_string()` 去重,避免多个共享材质重复膨胀预热清单。更新 asset-root 单测夹具覆盖 mask + double-sided 材质生成 feature 变体。 | `rustfmt --edition 2021 --check zircon_runtime/src/bin/zircon_shader_prewarm/manifest.rs` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617` 通过(仅既有 warning);`cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617 -- --project-root <temp> --cache-dir <temp>/cache --report <temp>/report.json --asset-root examples/vampire/assets --pretty` 写入 40/40 变体;同命令扫 `examples/vampire/assets/shaders` 仍写入 20/20 变体。 | 后续仍需把 quality tier、非静态 geometry source、shading model、alpha-blend pass 过滤和 Runtime `ResourceManager` revision 导出接入 staged manifest,对预热 WGSL 做真实 naga/wgpu 编译,并完成二次启动 miss=0 产品验收。 |
| 2026-06-17 | MS-M4-S1g asset-scanned built-in shading-model prewarm dimension | 部分完成: `.zmaterial` 内置 lighting model 已进入 asset-root 预热 key;custom shading model 插件 id、quality、非静态几何源和真实 runtime revision 仍未完成 | material feature 预热路径现在调用 `MaterialAsset::lighting_model()` 与 `ShadingModelId::from_lighting_model()`,把 PBR/BlinnPhong/Unlit 分别写入 `ShaderVariantKey.shading_model`;custom 模型因缺 project registry 导出仍回退 StandardPBR,避免预热工具猜测插件 id。更新 asset-root 单测夹具让 mask + double-sided 材质声明 `lighting_model = "blinn_phong"` 并断言后半组变体使用 `SHADING_MODEL_ID_BLINN_PHONG`。 | `rustfmt --edition 2021 --check zircon_runtime/src/bin/zircon_shader_prewarm/manifest.rs` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617` 通过(仅既有 warning);`cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617 -- --project-root <temp> --cache-dir <temp>/cache --report <temp>/report.json --asset-root examples/vampire/assets --pretty` 写入 40/40 变体;扫 `examples/vampire/assets/shaders` 写入 20/20 变体。 | 后续仍需导出 custom shading-model registry/plugin ids、quality tier、非静态 geometry source、alpha-blend pass 过滤和 Runtime `ResourceManager` revision,再做真实 naga/wgpu 编译与二次启动 miss=0 产品验收。 |
| 2026-06-17 | MS-M4-S1h asset-scanned initial runtime revision alignment | 部分完成: asset-root 预热 key 的 `material_revision` 已对齐运行时初次 ready 资源 revision;编辑后递增 revision 的 registry 导出仍未完成 | 确认运行时 `PipelineKey.shader_revision` 来自 `ResourceStreamer::resource_revision()` 读取 `ResourceRecord.revision`,而 `ResourceManager::register_ready` 初次 ready 资源 revision 为 `1`;`zircon_shader_prewarm --asset-root` 现在将扫描出的 shader 预热请求 revision 固定为 `ASSET_SCAN_INITIAL_RESOURCE_REVISION = 1`,source/include content hash 继续参与磁盘 cache key payload,避免初始 staged cache 因 `.zmeta source_hash` 派生 revision 与运行时 key 错位。单测断言 asset-root 预热请求 revision 为初始运行时 revision。 | `rustfmt --edition 2021 --check zircon_runtime/src/bin/zircon_shader_prewarm/manifest.rs` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617` 通过(仅既有 warning);`cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617 -- --project-root <temp> --cache-dir <temp>/cache --report <temp>/report.json --asset-root examples/vampire/assets --pretty` 写入 40/40 变体。 | 后续仍需导出项目 registry 中编辑后递增的 `ResourceRecord.revision`,再补 quality tier、非静态 geometry source、custom shading model plugin id、alpha-blend pass 过滤、真实 naga/wgpu 编译与二次启动 miss=0 产品验收。 |
| 2026-06-17 | MS-M4-S1i asset-scanned alpha-blend pass filtering | 部分完成: `.zmaterial` 透明材质实例已按当前透明队列收敛为 Forward-only 预热;基础 shader 源仍保守生成完整 pass 集合 | `MaterialPrewarmSource` 现在携带 material pass filter,`AlphaMode::Blend` 经 `MaterialAsset` 解析后只保留 `ShaderPassType::Forward`,避免为透明材质实例写入当前运行时不会消费的 GBuffer/DepthPrepass/Shadow/Velocity 变体;asset-root 单测夹具新增透明 unlit + double-sided 材质,断言总清单从 base 五 pass + mask 五 pass 增加为仅一个 transparent Forward 变体。 | `rustfmt --edition 2021 --check zircon_runtime/src/bin/zircon_shader_prewarm/manifest.rs` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617` 通过(仅既有 warning);`cargo run -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-asset-prewarm-check-0617 -- --project-root <temp> --cache-dir <temp>/cache --report <temp>/report.json --asset-root examples/vampire/assets --pretty` 写入 40/40 变体。 | 后续仍需导出项目 registry 中编辑后递增的 `ResourceRecord.revision`,再补 quality tier、非静态 geometry source、custom shading model plugin id、真实 naga/wgpu 编译与二次启动 miss=0 产品验收;透明材质的半分辨率/独立透明路径优化留给计划 19。 |
| 2026-06-17 | MS-M4-S1j runtime shader-quality key wiring | 部分完成: 运行时 viewport shader quality 已进入 mesh variant `ShaderVariantKey`;预热枚举与最终 cache 主键仍未完全收口 | `RenderQualityProfile` 新增 `shader_quality` 与 `with_shader_quality(...)`;quality 从 `ViewportRecordState`、`FrameSubmissionContext`、`ViewportRenderFrame` 传入 `MeshPassBuildContext`,再由 `MeshPipelineVariantRegistry` 写入 `ShaderVariantKey.quality`;不同 quality tier 现在会得到不同 mesh variant id 与 sort-key material bits。`zircon_shader_prewarm` 仍只生成 Medium 静态请求,当前 WGPU render-pipeline map/disk-cache 最终 owner 仍未完全切到 quality-aware `ShaderVariantKey`。 | `rustfmt --edition 2021` touched runtime shader-quality files 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shader-quality-check-0617` 通过(仅既有 warning)。新增 `mesh_pipeline_variant_registry_separates_shader_quality_tiers` 测试代码,但按用户"测试可以暂时延后"未运行 lib-test。 | 后续仍需让 asset-root/build profile 枚举 quality tier,把最终 WGPU pipeline/disk-cache owner 全面切到 `ShaderVariantKey`,并与非静态 geometry/custom shading-model plugin id、真实 naga/wgpu 编译和二次启动 miss=0 验收一起收口。 |
| 2026-06-17 | MS-M4-S1k quality-tier prewarm enumeration | 部分完成: asset-root 与内置 fallback 自动预热已能按显式 quality tier 展开;真实运行时写盘探针超时,未完成 miss=0 验收 | `zircon_shader_prewarm` 新增可重复 `--quality-tier low|medium|high|ultra|all`,默认仍为 Medium;自动生成的 built-in fallback 与 asset-root manifest 会按所选 quality 展开,而外部 `--manifest` 保留文件内原始 key;`tools/zircon_build.py --prewarm-shaders` 新增 `--shader-quality-tier` 并转发到预热工具,支持高/超高等目标质量档的 staged cache 生成。 | `rustfmt --edition 2021 --check zircon_runtime/src/bin/zircon_shader_prewarm/{args,manifest,run}.rs` 通过;`python -m py_compile tools\zircon_build.py` 通过;`cargo check -q -p zircon_runtime --bin zircon_shader_prewarm --no-default-features --features target-server --target-dir D:\cargo-targets\zircon-runtime-shader-quality-prewarm-check-0617` 通过(仅既有 warning);`python tools\zircon_build.py --targets runtime --out D:\zircon-shader-quality-prewarm-dry-run --mode debug --prewarm-shaders --shader-quality-tier high --shader-quality-tier ultra --dry-run` 确认命令含 `--quality-tier high --quality-tier ultra`。实际 `cargo run ... --asset-root examples/vampire/assets --quality-tier high --quality-tier ultra` 在 build/run 阶段超时,未作为通过证据。 | 后续仍需补轻量 manifest-count 单测或完成真实工具运行,再把非静态 geometry source、custom shading-model plugin id、编辑后 revision 导出、最终 WGPU pipeline/disk-cache owner 与二次启动 miss=0 验收收口。 |
| 2026-06-17 | MS-M4-S1l base mesh quality-aware cache owner | 部分完成: Base mesh render-command 路径已用 registry `ShaderVariantKey` 做 WGPU shader module、render pipeline 与磁盘 cache 命中主键;Velocity/TAA/deferred/template 尚未迁移 | `MeshPipelineCache` 新增 `mesh_variant_pipelines: HashMap<MeshPipelineVariantId, RenderPipeline>` 并移除旧 Base `PipelineKey` map/direct `ensure_pipeline`;`ensure_pipeline_for_variant` 现在从 `MeshPipelineVariantRegistry` 取 `(kind, PipelineKey, ShaderVariantKey)`,只处理 Base pass,用 `MeshPipelineVariantId` 缓存 render pipeline,用 `material_shader@revision#canonical_shader_variant_key` 缓存 shader module,并把同一个 quality-aware `ShaderVariantKey` 传入 `ShaderVariantCacheDiskKey`。这修正了 S1j 后 registry 已区分 quality、但实际 disk-cache lookup 仍会回退默认 Medium 的断点。 | `rustfmt --edition 2021 zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pipeline_cache/{mesh_pipeline_cache,new,ensure_pipeline}.rs` 通过;`cargo check -q -p zircon_runtime --lib --target-dir D:\cargo-targets\zircon-runtime-shader-quality-cache-check-0617` 通过(仅既有 warning)。按用户"测试可以暂时延后"未运行 lib-test/产品对拍。 | 后续仍需把 Velocity/TAA reactive/deferred/template pass 的 shader/pipeline cache owner 迁到同一 `ShaderVariantKey` 路径,再补非静态 geometry source、custom shading-model plugin id、编辑后 revision 导出、真实工具运行与二次启动 miss=0 验收。 |
| 2026-06-28 | MS-M4-S1m Non-Base mesh variant-aware cache owner | 部分完成: GBuffer/DepthPrepass/Shadow/Velocity/TAA reactive render-pipeline owner 已静态锁定为同一 `ShaderVariantKey`/`MeshPipelineVariantId` 路径;真实二次启动 miss=0 与产品对拍仍未完成 | `runtime_15_non_base_mesh_variant_cache_owner_is_wired` 锁定 `MeshPipelineCache` 的非 Base pipeline maps 均为 `HashMap<MeshPipelineVariantId, RenderPipeline>`,各 pass 的 `_for_variant` 入口均先通过 `pipeline_and_shader_key_for_variant(...)` 取 `(MeshPassPipelineKind, PipelineKey, ShaderVariantKey)`,shader module key 都包含 `variant_key.canonical_string()` 与模板 `source_hash`;GBuffer、DepthPrepass、Shadow、Velocity 与 TAA reactive 外部执行路径只调用 `_for_variant` 入口,不回流旧 direct ensure。 | 状态锚: `render_plan08_non_base_mesh_variant_cache_owner_static_passed_cargo_deferred`;scoped rustfmt/static source scan/docs anchor scan/line-count/conflict/trailing-whitespace/scoped diff-check 作为本切片验证;Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓,不计通过。 | Base 与非 Base runtime WGPU pipeline owner 已统一到 variant-aware cache seam;后续仍需真实 staged WGPU run、full live registry export、编辑后 revision 导出、RenderDoc/product capture 和 second-launch miss=0 验收。 |
| 2026-07-03 | Runtime 15 M4 non-Base mesh variant render-call guard sync | 已完成: 当前 Shadow/Velocity multiline render-call 形态已同步回 Runtime 15 静态守卫;真实二次启动 miss=0 与产品对拍仍未完成 | `runtime_15_non_base_mesh_variant_cache_owner_is_wired` 继续锁定 GBuffer/DepthPrepass/Shadow/Velocity/TAA reactive 的 `_for_variant` owner，本轮只把 Shadow/Velocity render pass 调用锚从旧单行片段改为 `.ensure_shadow_pipeline_for_variant(` 与 `.ensure_velocity_pipeline_for_variant(`，匹配已加入 `ResourceStreamer` 参数的 multiline 调用。 | 状态锚: `runtime_15_non_base_mesh_variant_render_call_guard_sync_static_passed_cargo_deferred`; scoped rustfmt passed；standalone `structure_convention.rs` 重新编译后 exact guard passed 1/1；`status_output` passed 143/143；full `plan_status` passed 42/42；full structure sweep 当前为 605/622，剩余红项仍在 shader-prewarm/TAA reactive；Cargo/WGPU/RenderDoc/product gates 因外部 cargo/rustc 通道 active 暂缓,不计通过。 | 该同步不改变 Plan 08 variant cache 行为、ShaderVariantKey、pipeline maps 或产品路径;后续仍需真实 staged WGPU run、full live registry export、编辑后 revision 导出、RenderDoc/product capture 和 second-launch miss=0 验收。 |
| 2026-07-03 | Runtime 15 M4 shader prewarm manifest path helper owner split | 已完成: shader prewarm manifest path/hash helpers 已从 manifest 父文件拆入 paths child;真实二次启动 miss=0 与产品对拍仍未完成 | `bin/zircon_shader_prewarm/manifest.rs` 继续负责 manifest assembly、source/material expansion 与 resource overlay；`bin/zircon_shader_prewarm/manifest/paths.rs` 承接 `wgsl_files_for_document(...)`、`primary_zshader_path(...)`、`content_hash(...)` 和 `ShaderSourceOutsidePackageDir`。`runtime_15_shader_prewarm_manifest_tests_are_folder_backed` 现在锁定 paths child、10 个 manifest tests、no-oversized production budget 与 path-boundary helper 不回流。 | 状态锚: `runtime_15_shader_prewarm_manifest_path_helpers_owner_split_static_passed_cargo_deferred`; scoped rustfmt passed；standalone `structure_convention.rs` 与 `plan_status.rs` 重新编译后 exact global production budget、manifest folder-backed、asset revision 与 builtin template-source guards 均 passed 1/1；`status_output` passed 143/143，full `plan_status` passed 42/42；full structure sweep 614/622，剩余 8 个 shader-prewarm plugin/registry/product staged-cache 红项在本切片之外；当前 `manifest.rs` 734 行、`paths.rs` 113 行。Cargo/WGPU/RenderDoc/product gates 暂缓,不计通过。 | 该拆分不改变 shader prewarm manifest 输出、ShaderVariantKey、cache key、registry overlay 或 WGPU validation 行为；后续仍需真实 staged WGPU run、full live registry export、编辑后 revision 导出、RenderDoc/product capture 和 second-launch miss=0 验收。 |
| 2026-07-03 | Runtime 15 M4 shader prewarm owner guard sync | 已完成: build-tool/plugin descriptor/runtime staged-cache/product staged-cache 守卫已同步到当前 owner;真实二次启动 miss=0 与产品对拍仍未完成 | `tools/zircon_build.py` 只锁定 orchestrator import/call/pass-through；`tools/zircon_build_plugin_assets.py` 承接 `distribution.assets` asset-root helpers；`tools/zircon_build_plugin_shader_descriptors.py` 承接 `geometry_sources` / `shading_models` descriptor parsing；`mesh_pipeline_cache/ensure_pipeline/tests.rs` 承接 runtime staged-cache tests；product staged-cache 守卫锁定 `include_content_hashes.contains(request_source_hash)`。 | 状态锚: `runtime_15_shader_prewarm_owner_guard_sync_static_passed_cargo_deferred`; scoped rustfmt passed；standalone `structure_convention.rs` compiled cleanly；8 个相关 Runtime 15 exact structure guards each passed 1/1；full standalone structure sweep 622/622 passed。Cargo/WGPU/RenderDoc/product gates 暂缓,不计通过。 | 该同步不改变 shader prewarm command、plugin registry export、runtime cache key、产品 submit 或 WGPU 行为；后续仍需真实 staged WGPU run、full live registry export、编辑后 revision 导出、RenderDoc/product capture 和 second-launch miss=0 验收。 |
| 2026-07-03 | Runtime 15 M4 deferred GBuffer template output guard sync | 已完成: Deferred GBuffer output layout 守卫已同步到共享 surface type owner;真实产品对拍仍未完成 | `graphics/shader/wgsl/zr_surface_types.wgsl` 现在是 `ZrDeferredGBufferOutput` 与 albedo/normal/material location layout 的守卫锚点；`graphics/shader/wgsl/zr_template_deferred_gbuffer.wgsl` 只锁定 alpha clip 和 selected `encode_gbuffer(...)` 委托。 | 状态锚: `runtime_15_deferred_gbuffer_template_output_guard_sync_static_passed_cargo_deferred`; scoped rustfmt passed；standalone `structure_convention.rs` compiled cleanly；exact `runtime_15_deferred_gbuffer_pipeline_template_cache_is_mesh_cache_owned` passed 1/1；full standalone structure sweep 622/622 passed。Cargo/WGPU/RenderDoc/product gates 暂缓,不计通过。 | 该同步不改变 Deferred GBuffer WGSL ABI、mesh cache ownership、pipeline render targets 或 WGPU validation 语义；后续仍需真实 product capture/perf gates。 |
| 2026-06-15 | MS-M4 shader variant cache and warmup | 未启动: 仍缺磁盘缓存与预热 | `definition_value.rs` 等变体基础存在,但无几何源/pass 维度完整 key、无磁盘缓存、无预热队列。 | 本文件 `现状与差距` 明确首次遇到变体即时编译造成卡顿。 | 与计划 17 PF-M4 联动建立 compile cache、warmup manifest 与回归指标。 |
| 2026-07-03 | SH02-SH04 shader material module import + option/layout keying + compute/fullscreen contract | renderer 截图、asset-root sparse prewarm、M3 compute/fullscreen 契约样例验收通过;executor 全量迁移与产品级 miss=0 未闭合 | `graphics/shader/template/module_registry.rs` 替代旧 `include_registry` owner,解析内建与资产模块 include,按拓扑注入并去重,unknown/cycle 进入模板错误;`core/framework/render/shader/module_import.rs` 负责 WGSL `#include` 扫描/剥离;`MaterialPropertyLayout`/`MaterialOptionTable` 生成 material WGSL 与 option defines;`.zmaterial` v2 支持 parent/options/queue 并强校验;`ShaderVariantKey`/`PipelineKey` 带 layout_hash,material option bits 进入变体组装;Depth/Shadow/Velocity/TAA/GBuffer/Forward material pass source 从 `ResourceStreamer` 接收 generated material、module sources 和 option defines;renderer material bind group、fallback/standard/builtin WGSL 与测试布局统一为 group2 binding0 uniform + binding1..10 标准贴图/采样器;`MaterialPropertyOverrideBlock` 经 `MeshRenderer`/`GeometryExtract`/`ResourceStreamer` 进入 per-draw binding0 uniform,覆盖 draw 排除静态批与静态命令缓存;`zircon_shader_prewarm` asset-root manifest 复用 zshader 生成 material layout hash/option table,按 zmaterial 实际 option 值写入 `material_option_bits`,不枚举全 option 笛卡尔积。新增 `ComputeDispatchBuilder`/`FullscreenPassBuilder` 中立契约、graph compute workload bridge、RenderFeaturePassDescriptor 消费入口、clustered lighting compute plan 样例和 motion-vector-tile-max fullscreen plan 样例；streamed builtin fallback shader 仍固定使用 standard material template；async-compute bridge stats 期望改从 compiled graph workload 派生。 | `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-shader-sh02-m2 --message-format short --color never` 通过;`cargo check -p zircon_runtime --bin zircon_shader_prewarm --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-shader-sh02-m2 --message-format short --color never` 通过;direct lib-test binary 通过 L2 material override/static-batch 3/3、`compute` filter 36/36、`fullscreen` filter 3/3、fallback streamed-surface regression 1/1、non-Base staged prewarm fallback regression 1/1;direct bin-test binary `E:\cargo-targets\zircon-runtime-shader-sh02-m2\debug\deps\zircon_shader_prewarm-1eec6d2f27a12a1f.exe` 通过新增 sparse option 用例和 V2 fixture 邻近用例 3/3,并通过 `manifest::tests` 21/21;Cargo-wrapper bridge regression `headless_wgpu_server_falls_back_async_compute_passes_to_graphics` 通过 1/1;ignored vampire screenshot test 通过 1/1,验证图 `docs/tests/runtime/shader/runtime_shader_material_vampire_offscreen_20260703.png` 已写入 docs 而非 target,尺寸 1280x720、54403 bytes、SHA256 `1526BE245965025596FA6098C495D85DCCBDA90E295C1B80489F4649740B5CE0`;`render_shader_template_expands_declared_surface_modules_and_strips_directives` 与 `renderer_material_layout_diagnostics` focused tests 在 Windows lib-test harness 编译阶段超时,不计通过。状态锚:`render_shader_material_compute_fullscreen_contract_screenshot_sparse_prewarm_passed_tests_timeout_followups_open`。 | 继续补 `shader_module`/`property_layout`/`material`/模板 focused tests,并补双 option/disabled pass/render queue 命令生成断言、product miss=0、L2 render_product 对拍和 perf 计数；HZB/particles/更多 postprocess executor 仍需按 owner 计划迁移到 compute/fullscreen contract，compute pipeline 目前只有中立 key 而非真实 GPU pipeline disk cache。 |

### 2026-07-03 Deferred Project-Shader GBuffer Default-Feature WGPU Refresh

Plan 08 three shading-model forward/deferred product parity now has a default-feature refresh for the non-custom Deferred/GBuffer project-shader probe. Status: `render_plan08_deferred_project_shader_gbuffer_probe_default_features_wgpu_refresh_passed_renderdoc_deferred`; it extends the existing `DEFERRED_PROBE_STATUS` row `render_plan08_deferred_project_shader_gbuffer_probe_wgpu_passed_renderdoc_deferred` without replacing the broader `STATUS` / `DEFAULT_FEATURES_STATUS` anchors for `render_plan08_three_shading_models_forward_deferred_parity_wgpu_passed_light_grid_fallback_renderdoc_deferred` and `render_plan08_three_shading_models_forward_deferred_parity_default_features_wgpu_passed_renderdoc_deferred`. The mirrored guard constants are `STATUS`, `DEFAULT_FEATURES_STATUS`, `DEFERRED_PROBE_STATUS`, and `DEFERRED_PROBE_DEFAULT_STATUS`.

The slice keeps `render_product_three_shading_models_forward_deferred_parity`, `runtime_15_render_plan08_three_shading_models_forward_deferred_parity_is_wired`, PBR/Blinn-Phong/Unlit, Forward + Deferred, `light_grid_external_fallback_buffers_satisfy_materialization_report`, `deferred_pipeline_uses_gbuffer_material_path_instead_of_forward_shader_path`, and `average_channel_in_region` as the product evidence chain. The first real default-feature run failed in the Base pass because a standalone full-pass project `.wgsl` had `ShaderAssetKind::Surface` metadata but contained `vs_main` / `fs_main` instead of `fn zr_material_surface`; `ResourceStreamer::shader_uses_material_surface_source` now requires material-surface source text before the mesh shader-source owner wraps it in the runtime material template. `runtime_surface_shader_with_full_pass_entry_points_uses_raw_wgsl_source` locks the raw-WGSL path for imported full-pass Surface shaders.

Evidence: the initial short-name `--exact` run executed 0 tests and is not counted; `--list` found the fully qualified test. After the support-layer fix, `runtime_surface_shader_with_full_pass_entry_points_uses_raw_wgsl_source` passed 1/1 with 6202 filtered, and the default-feature product rerun `deferred_pipeline_uses_gbuffer_material_path_instead_of_forward_shader_path` passed 1/1 with 6202 filtered and 3.56s on `E:\cargo-targets\zircon-plan08-deferred-probe-default-0703`. The earlier default-feature parity backfill remains recorded with 5876 filtered and 11.81s. Output carried repository-existing warnings only. Remaining gates: RenderDoc/product capture, workspace/full CI, and broader product coverage outside the focused filters.

### 2026-07-04 Project/Plugin Registry Material-Pass Product Readback PNG

Plan 08 project/plugin registry material-pass staged prewarm now has focused product readback PNG evidence under `render_plan08_project_plugin_registry_material_passes_product_readback_png_passed_renderdoc_deferred`. The ignored export `export_project_plugin_registry_material_passes_product_png` lives in the folder-backed `project_plugin_registry_material_passes_staged_cache/product_png.rs` child owner, reuses the same manifest, staged-cache hit assertions, first-frame/Velocity-frame product capture path, and read-only runtime cache checks as the existing material-pass product tests, then writes `runtime_render_plan08_project_plugin_registry_material_passes_20260703.png` as a first/Velocity side-by-side frame.

Evidence: the Cargo-wrapper command on `E:\cargo-targets\zircon-plan08-project-plugin-png-0703` exceeded the 904.5s tool window while still compiling/linking and is not counted; the same background build later produced `zircon_runtime-0a7825d39d44b0c4.exe`. Direct generated-binary execution passed `export_project_plugin_registry_material_passes_product_png` 1/1 with 6290 filtered and 6.58s. The PNG is 641x240, 3871 bytes, SHA256 `2FF919F50FDFFBAEB1544CAD9C14B7748FA8234C784175195AF3E550FB6151BB`, with 4794 non-black pixels and 240 magenta separator pixels. This closes the focused product readback PNG gate only; RenderDoc/product capture, workspace/full CI, full live project/plugin registry export beyond the focused fixture, and broader product miss=0 remain open.

### 2026-07-03 Morph Velocity Scene-Velocity Readback PNG

Plan 08 morph velocity now has focused scene-velocity PNG evidence under `render_plan08_morph_weight_velocity_product_png_passed_renderdoc_deferred`, with child statuses `render_plan08_direct_morph_weight_velocity_product_png_passed_renderdoc_deferred` and `render_plan08_skinned_morph_weight_velocity_product_png_passed_renderdoc_deferred`. The exports reuse the existing 0.0 -> 1.0 direct/skinned morph velocity product paths, keep Morphed/SkinnedMorphed source and previous velocity assertions, then use the test-only `last_scene_velocity_readback_rg16_float_bytes_for_tests` outlet to encode `scene-velocity` RG16Float bytes as sign-masked payload PNGs in `morph/velocity_png.rs`. The support-layer `RenderSceneVelocityReadbackReport` now treats RG16Float signed zero as zero so readback stats do not report false nonzero velocity.

Evidence: the first Cargo-wrapper attempt on `E:\cargo-targets\zircon-plan08-morph-velocity-png-0703` exceeded the 1213.4s tool window and is not counted. The generated binary `zircon_runtime-0a7825d39d44b0c4.exe` passed the regular direct/skinned product tests 1/1 each with 6207 filtered (3.84s direct, 3.60s skinned), `scene_velocity_readback_ignores_signed_zero_half_float_pixels` 1/1 with 6207 filtered, `export_direct_morph_weight_velocity_product_png` 1/1 with 6207 filtered and 3.36s, `export_skinned_morph_weight_velocity_product_png` 1/1 with 6207 filtered and 3.42s, and the fully qualified `runtime_15_render_product_mesh_cache_morph_tests_are_child_owners` 1/1 with 6207 filtered and 0.25s. Both `runtime_render_plan08_direct_morph_weight_velocity_20260703.png` and `runtime_render_plan08_skinned_morph_weight_velocity_20260703.png` are 128x128, 723 bytes, SHA256 `7B40FA8BA6EA60F7F24F3C5465F3C802B4119E26A8965DACECA08187FF665DE7`, with 508 non-black pixels. This closes focused scene-velocity PNG evidence only; RenderDoc/product capture, workspace/full CI, and broader product miss=0/second-launch acceptance remain open.

### 2026-07-03 Three Shading-Model Product Readback PNG

Plan 08 three shading-model forward/deferred product parity now has focused product readback PNG evidence under `render_plan08_three_shading_models_forward_deferred_product_readback_png_passed_renderdoc_deferred`. The ignored export `export_three_shading_models_forward_deferred_product_png` reuses `render_product_three_shading_models_forward_deferred_parity`, PBR/Blinn-Phong/Unlit, Forward + Deferred, `light_grid_external_fallback_buffers_satisfy_materialization_report`, `deferred_pipeline_uses_gbuffer_material_path_instead_of_forward_shader_path`, and `average_channel_in_region` as the surrounding product chain, then writes `runtime_render_plan08_three_shading_models_forward_deferred_product_20260703.png`.

Evidence: the first Cargo-wrapper attempt reached MSVC link and failed with `link.exe` exit `0xc0000142`, so it is not counted. A warmed `CARGO_PROFILE_DEV_DEBUG=0` rerun produced `zircon_runtime-33095b46939b64fc.exe`; direct generated-binary execution passed 1/1 with 6212 filtered and 14.04s, writing a 641x240 product readback PNG, 3998 bytes, SHA256 `D493C941CBCF418A2C66F84F663881E9DA6B0984B023BAACEC254F094AB483B6`. The same generated binary then passed `runtime_15_render_plan08_three_shading_models_forward_deferred_parity_is_wired` 1/1 with 6212 filtered and 0.86s, confirming the status/docs/structure anchors. This closes the product readback PNG gate only; RenderDoc/product capture, workspace/full CI, and broader product coverage remain open.

### 2026-07-03 VirtualGeometry Page/Cluster Product Readback PNG

VirtualGeometry page/cluster product execution now has focused product readback PNG evidence under `render_plan08_virtual_geometry_page_cluster_product_readback_png_passed_renderdoc_deferred`. The ignored export `export_virtual_geometry_page_cluster_product_png` reuses `render_product_virtual_geometry_page_cluster_bindings_drive_visible_frame`, the automatic cooked `ModelAsset` path, visible Unlit product capture, `custom:virtual_geometry` page/cluster binding requirements, and `runtime_15_virtual_geometry_product_draw_source_is_wired`, then writes `runtime_render_plan08_virtual_geometry_page_cluster_product_20260703.png`.

Evidence: the first Cargo-wrapper attempt on `E:\cargo-targets\zircon-plan08-vg-png-0703` exceeded the 1209.4s tool window and is not counted. The generated binary `zircon_runtime-06153fefd6ded19a.exe` passed the ignored export 1/1 with 6204 filtered and 7.97s, writing a 320x240 product readback PNG, 2965 bytes, SHA256 `0322783567544681379085E0C944EF40DD2E6453EE4AE0CB5897F12EBBEBDDE6`. The same binary passed `runtime_15_virtual_geometry_product_draw_source_is_wired` 1/1 with 6204 filtered and 0.26s, confirming the status/docs/structure anchors. This closes the VG page/cluster product readback PNG gate only; RenderDoc/product capture, workspace/full CI, full live project/plugin registry export, and broader product miss=0 remain open.

### 参考实现精读笔记

仅记录本次实际读到的符号:

- `dev/UnrealEngine/.../RenderCore/Public/VertexFactory.h:313` `FVertexFactoryType`:以 `Name + ShaderFilename + EVertexFactoryFlags` 加一组函数指针(`ShouldCacheType`、`ModifyCompilationEnvironmentType`、`GetPSOPrecacheVertexFetchElementsType`、`ConstructParametersType`)构成注册项,经静态 `GetTypeList()` 链表全局注册。Zircon 对应:`GeometrySourceDescriptor` 把 `ShaderFilename` 数据化为 `wgsl_include` 字符串、把 `ModifyCompilationEnvironment` 收敛为静态 `defines` 列表、把 `ShouldCompilePermutation` 收敛为预热清单枚举时的适用性过滤。取舍:不复刻 UE 的 per-frequency `FVertexFactoryShaderParameters` 动态绑定对象 —— Zircon 绑定固定走 group3 槽表,描述符只声明 binding,不携带绑定逻辑。
- `VertexFactory.h:144` `EVertexFactoryFlags::SupportsPrimitiveIdStream`:UE 按 VF 开关 primitive id 流;Zircon 不需要该开关 —— 计划 03 已定全部 draw 经 `first_instance` ABI + `get_instance_data` 取 GPUScene 数据,几何源一律可拿 `instance_index`。
- `dev/UnrealEngine/.../Renderer/Public/MeshMaterialShader.h:32` `FMeshMaterialShaderPermutationParameters`(`Platform + MaterialParameters + VertexFactoryType + PermutationId + Flags`)与 `:43` `FVertexFactoryShaderPermutationParameters`(额外含 `ShaderType`):UE 变体空间是 (material, vertex factory, shader type, permutation id, platform) 五维。Zircon `ShaderVariantKey` 一一对应为 (material_shader+revision, geometry_source, pass_type, features, platform_token);取舍:UE 开放的 `FShaderType` 集合收敛为封闭 `ShaderPassType` 枚举 —— 本引擎 pass 形态由模板特化定义,不开放任意 shader type 注册,自定义着色经 `ShadingModelDescriptor` 而非新 shader type。
- `MeshMaterialShader.h:67` `FMeshMaterialShader : FMaterialShader`:UE 用类继承分层(global/material/mesh-material)隔离变体空间;Zircon 对应做法是 post/compute WGSL 不进 `ShaderVariantKey` 空间(沿既有 pipeline/post 路径),与计划正文"GlobalShader 独立管理"的口径一致。

## 风险与回退

- 模板拼接与现有 zshader 编译路径冲突:shader_graph 插件的生成器在 MS-M1 同步改为产出 surface 函数,硬切换,不保留旧整段 shader 产物路径。
- shading model 进 G-buffer 占用通道:V1 用 8bit id + 共享通道布局,自定义模型受布局约束并由诊断兜底,不做动态 G-buffer 布局。
- 磁盘缓存跨版本失效:键中含 shader 模板修订号与 wgpu/naga 版本,宁可多失效不可错命中。

## 2026-07-01 Runtime 15 Structure Mirror

Plugin shader permutation registry auto-export remains mirrored for the current Runtime 15 structure guard: `Plugin shader permutation registry auto-export`, `render_plan08_plugin_shader_permutation_registry_auto_export_focused_tests_passed_renderdoc_deferred`, `Plugin shader permutation registry export contract`, `render_plan08_plugin_shader_permutation_registry_export_contract_python_passed_cargo_deferred`, `test_zircon_build_discovers_plugin_shader_permutation_records`, `test_validate_generated_registry_requires_selected_plugin_ids`, and `runtime_15_shader_prewarm_plugin_permutation_registry_auto_export_is_wired`.

Build-tool staged WGPU handoff command contract remains mirrored here with `render_plan08_build_tool_staged_wgpu_handoff_command_contract_python_passed_cargo_deferred`, `test_full_staged_wgpu_handoff_keeps_generated_registries_and_roots`, and `runtime_15_shader_prewarm_staged_wgpu_handoff_command_contract_is_wired`. Plugin shading-model descriptor registration/export mirrors include `Plugin shading-model descriptor registration`, `render_plan08_plugin_shading_model_descriptor_registration_typecheck_python_passed_libtest_blocked_by_ui_input_error`, `test_zircon_build_discovers_plugin_shading_model_descriptors_as_shader_ids`, `Plugin shading-model descriptor registry export`, `render_plan08_plugin_shading_model_descriptor_registry_export_static_passed_cargo_deferred`, `shading_model_descriptors`, `test_generated_shader_permutation_registry_document_exports_selected_plugin_shading_model_descriptors`, `shader_prewarm_permutation_registry_merges_custom_shading_model_descriptors`, and `runtime_15_shader_prewarm_plugin_shading_model_descriptor_registration_is_wired`.

## 2026-07-02 Shader Prewarm Acceptance Identity Mirror

Runtime 15 structure follow-up mirrors the staged prewarm acceptance identity guard for Plan 08: `Build-tool staged prewarm acceptance contract`, `duplicate written cache variant identity`, `test_prewarm_shaders_validates_staged_acceptance_after_success`, and `runtime_15_shader_prewarm_acceptance_contract_is_wired`. This status only records the acceptance/report identity contract in the plan; broader WGPU product capture, RenderDoc capture, and full CI remain open unless their own rows record passing evidence.

### 2026-07-02 Shader Prewarm Structure Guard Full Mirror

Guard anchors synchronized for shader prewarm geometry/custom ids: `Shader prewarm geometry-source enumeration`, `render_plan08_shader_prewarm_geometry_source_enumeration_static_passed_cargo_deferred_implementation_cadence`, `Asset-root custom geometry-source id prewarm`, `render_plan08_asset_root_custom_geometry_source_id_prewarm_typecheck_passed_test_timeout_no_result`, `Asset-root custom shading-model id prewarm`, `render_plan08_asset_root_custom_shading_model_id_prewarm_static_passed_cargo_deferred_implementation_cadence`, `bin/zircon_shader_prewarm/args.rs`, `bin/zircon_shader_prewarm/manifest.rs`, `tools/zircon_build.py`, `tools/zircon_build_shader_prewarm.py`, `shader_prewarm_asset_root_manifest_expands_requested_geometry_sources`, `shader_prewarm_asset_root_manifest_expands_custom_geometry_source_plugin_ids`, `shader_prewarm_asset_root_manifest_maps_custom_shading_model_plugin_ids`, `runtime_15_shader_prewarm_geometry_source_enumeration_is_wired`, `runtime_15_shader_prewarm_custom_geometry_source_id_is_wired`, and `runtime_15_shader_prewarm_custom_shading_model_id_is_wired`.

Acceptance/cache/report anchors synchronized for the same guard set: `Build-tool staged prewarm nonempty success report acceptance`, `render_plan08_build_tool_staged_prewarm_nonempty_success_report_python_passed_cargo_deferred`, `Build-tool staged prewarm written variant identity acceptance`, `render_plan08_build_tool_staged_prewarm_written_variant_identity_python_passed_cargo_deferred`, `Build-tool staged prewarm written source-label identity acceptance`, `render_plan08_build_tool_staged_prewarm_written_source_label_identity_python_passed_cargo_deferred`, `Build-tool staged prewarm complete written count acceptance`, `render_plan08_build_tool_staged_prewarm_complete_written_count_python_passed_cargo_deferred`, `Build-tool written variant uniqueness contract`, `render_plan08_build_tool_written_variant_uniqueness_contract_python_passed_cargo_deferred`, `Build-tool staged prewarm written cache-hash shape acceptance`, `render_plan08_build_tool_staged_prewarm_written_cache_hash_shape_python_passed_cargo_deferred`, `Build-tool source-label nonblank contract`, `render_plan08_build_tool_source_label_nonblank_contract_python_passed_cargo_deferred`, `Build-tool source-label trim contract`, `render_plan08_build_tool_source_label_trim_contract_python_passed_cargo_deferred`, `Build-tool explicit registry exact revision acceptance`, `render_plan08_build_tool_explicit_registry_exact_revision_acceptance_python_passed_cargo_deferred`, `expected_pass_types`, `taa_reactive_mask`, `test_acceptance_contract_rejects_duplicate_written_variant_identity`, `test_validate_cache_artifact_contract_requires_requested_pass_types`, `test_validate_cache_artifact_contract_accepts_requested_pass_types`, `test_validate_cache_artifact_contract_requires_requested_quality_tiers`, `test_validate_cache_artifact_contract_requires_requested_geometry_sources`, `test_validate_cache_artifact_contract_accepts_requested_quality_and_geometry`, `tools/zircon_build_shader_prewarm_written_variants.py`, `duplicate written cache variant identity`, `runtime fallback root`, and `usable shader ResourceRecord revisions`.

Cache/resource/WGPU anchors synchronized for focused static/Python contracts: `Build-tool shader prewarm cache artifact contract`, `Prewarm report cache identity contract`, `Prewarm cache runtime layout contract`, `Prewarm cache hash shape contract`, `Prewarm cache custom id correlation contract`, `Runtime prewarm custom id cache lookup contract`, `Runtime custom id staged fallback lookup contract`, `render_plan08_runtime_custom_id_staged_fallback_lookup_static_passed_cargo_deferred`, `Build-tool cache quality/geometry identity contract`, `render_plan08_build_tool_cache_quality_geometry_identity_contract_python_passed_cargo_deferred`, `Build-tool cache dimension combination contract`, `render_plan08_build_tool_cache_dimension_combination_contract_python_passed_cargo_deferred`, `Build-tool cache custom id combination contract`, `render_plan08_build_tool_cache_custom_id_combination_contract_python_passed_cargo_deferred`, `Build-tool cache source-label provenance correlation contract`, `render_plan08_build_tool_cache_source_label_provenance_contract_python_passed_cargo_deferred`, `Build-tool cache metadata field type contract`, `render_plan08_build_tool_cache_metadata_field_type_contract_python_passed_cargo_deferred`, `test_validate_report_contract_rejects_untrimmed_source_provenance_strings`, `runtime_15_shader_prewarm_cache_artifact_contract_is_wired`, `Asset-root resource registry revision overlay`, `render_plan08_asset_root_resource_registry_revision_overlay_typecheck_passed_test_timeout_no_result`, `render_plan08_resource_registry_ready_shader_revision_contract_python_static_passed_cargo_deferred`, `bin/zircon_shader_prewarm/manifest/resource_registry.rs`, `shader_prewarm_asset_root_manifest_uses_resource_registry_revision_overlay`, `shader_prewarm_resource_registry_overlay_uses_ready_shader_revisions_only`, `runtime_15_shader_prewarm_resource_registry_revision_overlay_is_wired`, `Prewarm WGPU validation report summary`, `render_plan08_prewarm_wgpu_validation_report_summary_python_passed_cargo_deferred`, `Build-tool WGPU validation report contract`, `render_plan08_build_tool_wgpu_report_contract_python_passed_cargo_deferred`, `Build-tool WGPU validation totals match contract`, `render_plan08_build_tool_wgpu_validation_totals_match_python_passed_cargo_deferred`, `test_zircon_build_shader_prewarm_wgpu_report_contract.py`, `test_dimension_summary_lines_format_wgpu_module_validation_counts`, `test_validate_report_contract_requires_wgpu_validation_when_requested`, `test_validate_report_contract_rejects_wgpu_validation_total_mismatch`, `runtime_15_shader_prewarm_wgpu_validation_report_summary_is_wired`, and `runtime_15_shader_prewarm_wgpu_report_contract_is_wired`.

### 2026-07-03 Shader Prewarm Manifest Path Helper Owner Split

Runtime 15 mirrors a Plan 08 shader-prewarm structure slice under `Runtime 15 M4 shader prewarm manifest path helper owner split` / `runtime_15_shader_prewarm_manifest_path_helpers_owner_split_static_passed_cargo_deferred`. The manifest parent remains the source/material expansion and registry overlay owner, while `bin/zircon_shader_prewarm/manifest/paths.rs` owns package-local WGSL path discovery, primary `.zshader` lookup, content hashing, and `ShaderSourceOutsidePackageDir` diagnostics. This closes the current Runtime 15 no-oversized-production guard for `bin/zircon_shader_prewarm/manifest.rs` without changing prewarm manifest semantics or product acceptance gates.

Evidence: scoped rustfmt passed; standalone `structure_convention.rs` and `plan_status.rs` compiled cleanly; exact global production budget, manifest folder-backed, asset revision, and builtin template-source guards each passed 1/1; `status_output` passed 143/143 and full plan-status passed 42/42. Full structure sweep is 614/622 with the remaining 8 failures in shader-prewarm plugin/registry/product staged-cache anchors outside this path-helper slice. Cargo/WGPU/RenderDoc/product miss=0 validation remains a separate Plan 08 acceptance path.
