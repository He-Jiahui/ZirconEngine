---
related_code:
  - zircon_runtime/src/render_graph/builder.rs
  - zircon_runtime/src/render_graph/builder/compile.rs
  - zircon_runtime/src/render_graph/graph.rs
  - zircon_runtime/src/render_graph/types.rs
  - zircon_runtime/src/graphics/pipeline/compiled_graph_cache.rs
  - zircon_runtime/src/graphics/pipeline/compiled_graph_cache/tests.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/compiled_graph_cache_tests.rs
  - zircon_runtime/src/graphics/pipeline/declarations/compiled_render_pipeline.rs
  - zircon_runtime/src/graphics/pipeline/declarations/renderer_feature_asset.rs
  - zircon_runtime/src/graphics/feature/builtin_render_feature_descriptor/dispatch/descriptor_for.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry/tests.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry/tests/registry_contracts.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry/tests/postprocess_context_guards.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry/tests/renderer_context_guards.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_graph_execution_record.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_graph_execution_record/compute_workload.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_graph_execution_record/tests.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/materialization.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/materialization/tests.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_pass_executor_registry_tests.rs
  - zircon_runtime/src/graphics/pipeline/render_pipeline_asset/compile_tests.rs
  - zircon_runtime/src/graphics/pipeline/render_pipeline_asset/compile_tests/core_contracts.rs
  - zircon_runtime/src/graphics/pipeline/render_pipeline_asset/compile_tests/postprocess_routes.rs
  - zircon_runtime/src/graphics/pipeline/render_pipeline_asset/compile_tests/external_compute_guards.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_pipeline_asset_compile_tests.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/default_pipelines.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/dynamic_resolution.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/plugin_features.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/temporal_and_ops.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/compile_options.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/feature_descriptors.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/validation_core.rs
  - zircon_runtime/src/graphics/tests/pipeline_compile/validation_descriptors.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_pipeline_compile_monolith_tests.rs
  - zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/submit/submit.rs
  - zircon_runtime/src/graphics/backend/render_backend/graphics_debugger_capture.rs
  - dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphBuilder.h
  - dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphPass.h
  - dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h
  - dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphUtils.h
  - dev/bevy/crates/bevy_render/src/texture/texture_cache.rs
  - dev/bevy/crates/bevy_render/src/texture/texture_attachment.rs
  - dev/bevy/crates/bevy_render/src/render_resource/pipeline_cache.rs
  - dev/bevy/crates/bevy_render/src/renderer/mod.rs
  - dev/bevy/crates/bevy_render/src/renderer/render_context.rs
  - dev/Fyrox/fyrox-impl/src/renderer/cache/mod.rs
  - dev/godot/servers/rendering/rendering_device_graph.h
plan_sources:
  - .codex/plans/Zircon SRP_RHI Rendering Architecture Roadmap.md
  - .codex/plans/Runtime 渲染风险清单与 RenderDoc 调试支持计划.md
---

# 计划 01:RenderGraph 向 RDG 对齐

## 目标

把 `zircon_runtime/src/render_graph/` 从"统计与依赖记录层"升级为真正承担资源生命周期、瞬态复用、pass 裁剪与编译缓存职责的 RDG 等价物。完成后:

1. 所有 pass 资源(scene color、depth、G-buffer、shadow atlas、history)由 graph 分配或注册,生命周期由 graph 推导。
2. 同一帧内不重叠生命周期的瞬态纹理/缓冲复用同一块物理资源(池化别名)。
3. 对最终输出无贡献的 pass 在编译期被裁剪。
4. feature descriptor → compiled graph 的解析编译结果按签名缓存,稳态帧零重编译。

## 现状与差距

- `render_graph/builder.rs`、`graph.rs` 已有 pass/资源声明与依赖推导,但风险清单(P1)明确指出它"更像统计层":真实 wgpu 资源仍由 `FrameSubmissionContext` 与各 executor 自行持有,生命周期固定。
- `graphics/pipeline/declarations/compiled_render_pipeline.rs` 的编译在帧时执行(pipeline asset → feature descriptors → pass contracts),没有跨帧缓存,feature 集不变时也重复付出解析成本。
- 部分 pass 缺显式 clear/load 语义(风险清单 P0 的一部分),attachment ops 虽存在但未被 graph 统一校验。
- 无 pass culling:禁用下游效果时,上游孤立 pass 仍然执行。

## 参考代码

主参考(UE RDG):

| 文件 | 应重点阅读 |
|------|-----------|
| `dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphBuilder.h` | `FRDGBuilder` 的 CreateTexture/CreateBuffer/RegisterExternal*、AddPass、Execute 生命周期;`QueueTextureExtraction` 跨帧导出 |
| `dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphPass.h` | pass 参数结构体如何声明资源访问(读/写/UAV/RT),执行 lambda 与参数分离 |
| `dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h` | `FRDGTexture`/`FRDGBuffer` 句柄、transient 标记、生命周期区间(FirstPass/LastPass) |
| `dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphUtils.h` | Clear/Copy/Resolve 等标准工具 pass 的形态 |

次参考:`dev/bevy/crates/bevy_render`(render graph 的 Rust 表达,节点/slot 风格,主要看 Rust 侧 API 人体工学,不照搬其无生命周期推导的设计)。

**Rust/wgpu 落地参照(防凭空实现)**:

| 文件 | 对应本计划机制 | 应重点阅读 |
|------|---------------|-----------|
| `dev/bevy/crates/bevy_render/src/texture/texture_cache.rs` | `TransientResourcePool` 池化复用 | `TextureCache::get` 以完整 `TextureDescriptor` 做桶键、`frames_since_last_use < 3` 帧龄驱逐;与 Zircon 的描述符桶 + KEEP_FRAMES 驱逐同构,但 bevy 无生命周期区间着色,同帧别名需自实现 |
| `dev/bevy/crates/bevy_render/src/texture/texture_attachment.rs` | 首写 attachment ops 裁决 | `ColorAttachment`/`DepthAttachment` 以 `is_first_call: AtomicBool` 实现"首写 Clear、后续 Load";Zircon 把同一裁决前移到 `compile()` 期静态校验,不要照搬运行期原子标记 |
| `dev/bevy/crates/bevy_render/src/render_resource/pipeline_cache.rs` | 编译结果缓存 | `PipelineCache` 的 key 化缓存、异步编译与失效流程;注意它只缓存 PSO 粒度,graph 粒度的 `CompiledGraphCache` 键组成仍以 Unity RenderGraphCompilationCache 为准 |
| `dev/bevy/crates/bevy_render/src/renderer/mod.rs` | graph 执行驱动 | 新版 bevy 已删除 node 式 render graph,改为 `RenderGraph` schedule(Begin/Render/Submit/Finish)+ `render_system` 驱动;印证 Rust 侧不需要 trait-object 节点图,Zircon 的 compiled pass 序列 + executor registry 形态可行 |
| `dev/bevy/crates/bevy_render/src/renderer/render_context.rs` | pass 录制与提交分段 | `RenderContext`/`PendingCommandBuffers` 的 encoder 生命周期管理与分段 submit,wgpu 下命令录制组织的参考形态 |
| `dev/Fyrox/fyrox-impl/src/renderer/cache/mod.rs` | 池条目驱逐策略 | `TemporaryCache`/`TimeToLive` 按帧龄回收 GPU 资源,Fyrox 全部 renderer 缓存(texture/geometry/shader)共用该形态;印证瞬态池可独立于 graph 先落地 |
| `dev/godot/servers/rendering/rendering_device_graph.h` | 资源使用跟踪(C++) | `RenderingDeviceGraph` 按资源 usage 推导命令依赖并重排/插 barrier;wgpu 自动 barrier 下只借鉴其 usage 合并思路,barrier 部分不移植 |

`pass culling(反向可达性裁剪)` 与 `graph 粒度编译缓存` 无 Rust 同类参照,实现时以 UE/Unity 为唯一样板,按 index §8 第 8 条配对拍测试先行。

wgpu 适配要点:wgpu 没有显式 barrier 与 placed resource,RDG 的"自动 barrier"在本引擎对应为 (a) 资源池复用时机的正确性(同 encoder 内 usage 冲突由 wgpu 校验),(b) pass 间纹理 usage 推导(声明读写 → TextureUsages 合并),(c) load/store/clear ops 的统一裁决。瞬态别名退化为"池化复用",不做底层内存 alias。

## 目标架构

归属:全部在 `zircon_runtime/src/render_graph/`(基础设施层)与 `graphics/scene/scene_renderer/graph_execution/`(执行层),facade 不变。

核心类型(新增/改造):

- `RenderGraphBuilder`:帧内唯一声明入口。`create_texture(desc) -> RgTextureHandle`、`register_external_texture(...)`、`add_pass(decl, executor_id)`。executor 不再直接拿 `wgpu::TextureView`,改为在执行期通过 `RgResourceResolver` 以句柄解析。
- `TransientResourcePool`:按 (size, format, usage, sample_count) 键控的纹理/缓冲池;graph 编译出每个资源的 [first_pass, last_pass] 区间,区间不重叠的资源映射到同一池条目;帧末归还,LRU 收缩。
- `CompiledGraphCache`:键 = (pipeline asset 修订, 启用 feature 集, quality profile, viewport 尺寸/格式, capability 摘要) 的哈希;值 = 编译后的 pass 序列 + 资源计划。命中则跳过 descriptor 解析与拓扑排序。
- pass culling:从声明为 output/extraction 的资源反向遍历,未到达的 pass 标记 culled,不录制;统计中保留 culled 计数以便测试断言。
- attachment ops 裁决:每个纹理首次写入 pass 必须给出 Clear 或显式 Load(external);graph 编译期校验,违例返回编译诊断而不是默默通过。

执行层改造:`render_pass_executor_registry.rs` 的 executor 签名增加资源解析上下文;`render_graph_execution_record.rs` 改为按 compiled graph 的 pass 顺序驱动录制,删除 executor 自行查找全局纹理的旁路。

## 里程碑

### RG-M1 资源句柄与生命周期模型

实施切片:
1. `RgTextureHandle`/`RgBufferHandle` 与资源声明表;builder 收口所有内建 pass 的资源声明(mesh、deferred、shadow、post 系列全部经 builder 声明 IO)。
2. 生命周期区间推导与 attachment ops 校验;违例诊断类型。
3. executor 上下文改为句柄解析;删除直接持有 view 的旁路字段。

测试阶段:
- 编译:`cargo check -p zircon_runtime --lib --locked`
- 测试:`cargo test -p zircon_runtime render_graph --locked` 与 `cargo test -p zircon_runtime render_product --locked`
- 验收证据:全部内建 pass 的 IO 在 graph dump 中可见;缺 clear 的 pass 列表清零或逐个给出显式 Load 理由。
- 文档:更新 `docs/zircon_runtime` 下 render_graph 镜像文档。

### RG-M2 瞬态资源池

实施切片:
1. `TransientResourcePool` 与区间着色分配(线性扫描即可,资源数 < 百级)。
2. history/extraction 资源标记为持久,绕过池;与计划 06 的 history 管理对齐。
3. 池统计(峰值条目、复用率)进 RenderStats。

测试阶段:
- `cargo test -p zircon_runtime render_graph --locked`(新增:同尺寸两瞬态资源生命周期不重叠时物理资源数为 1 的断言)
- 验收证据:典型 deferred + post 场景的瞬态纹理物理数明显低于逻辑数;RenderDoc 抓帧确认画面不变。

### RG-M3 pass culling 与编译缓存

实施切片:
1. 反向可达性裁剪 + culled 统计。
2. `CompiledGraphCache` 键控缓存;feature 开关/分辨率变化触发重编译,稳态命中。
3. 帧时 descriptor 解析移出热路径(仅 miss 时执行)。

测试阶段:
- `cargo test -p zircon_runtime render_graph --locked` 与 `cargo test -p zircon_runtime compiled_render_pipeline --locked`
- 验收证据:关闭 bloom 后其上游独立 downsample pass 被裁剪(统计断言);连续两帧第二帧编译耗时为 0(缓存命中计数断言)。

### RG-M4 诊断与 RenderDoc 对接

实施切片:
1. graph dump(pass 顺序、资源区间、culled 列表)走 debug 接口输出。
2. 每个 pass 录制时打 debug marker(对接 `graphics_debugger_capture.rs` 与既有 RenderDoc 计划)。

测试阶段:
- `cargo test -p zircon_runtime --lib --locked` 范围回归
- 验收证据:开启 RenderDoc 捕获后,抓帧中 pass 名与 graph dump 一致。

## 工程落地细化

本章是计划 01 的实施权威(见 index.md §8 第 7 条)。bind group 槽位、GPU 数据布局、WGSL include、RenderQueueValue、sort_key、测试命名等全局约定直接引用 index.md §8,本章不重复定义。

### 模块与文件落点

现状基础:`render_graph/` 已有 `RgTextureHandle`/`RgBufferHandle`、`create_texture`/`create_buffer`/`import_external_resource`、读写声明、依赖推导、`cull_passes`、`resource_lifetimes`(first_pass/last_pass)与逻辑层 `transient_allocation_plan()`;执行层已有 `RenderGraphExecutionResources`(每帧新建 wgpu 资源,无池化)与 `RgResourceResolver`(pass-scoped graph declaration/access resolver)。本计划的增量是:首写 ops 决策表校验、root 驱动的反向裁剪、物理瞬态池、编译缓存、句柄级物理解析收口。

新增文件:

| 路径 | 职责(一行) |
|------|------------|
| `zircon_runtime/src/render_graph/dump.rs` | `RenderGraphDump` 纯数据构建与文本序列化(pass 顺序、资源区间、culled 列表),无 wgpu |
| `zircon_runtime/src/render_graph/tests/attachment_ops.rs` | 首写 attachment ops 决策表与 usage flags 的编译期校验测试 |
| `zircon_runtime/src/render_graph/tests/culling.rs` | root 驱动反向可达裁剪的单测(从既有 `tests/ordering.rs` 拆出裁剪主题) |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/transient_resource_pool.rs` | `TransientResourcePool`:描述符桶 + 区间着色的 wgpu 物理资源池,含 `#[cfg(test)] mod tests` |
| `zircon_runtime/src/graphics/pipeline/compiled_graph_cache.rs` | `CompiledGraphCache`/`CompiledGraphCacheKey`/统计与 LRU 驱逐,通过 `#[cfg(test)] mod tests;` 挂载 child tests |
| `zircon_runtime/src/graphics/pipeline/compiled_graph_cache/tests.rs` | compiled graph cache hit/miss/status/fingerprint/target-format/LRU 源码合同测试 |

修改文件:

| 路径 | 改动点 |
|------|--------|
| `zircon_runtime/src/render_graph/types.rs` | 新增 `RenderGraphResourceUsageFlags`;`RenderGraphResourceDeclaration`/`RenderGraphResourceLifetime` 增 `usage` 字段 |
| `zircon_runtime/src/render_graph/builder.rs` | `mark_persistent`/`mark_readback`/`import_external_resource_with_usage`;`compile()` 内首写 ops 校验;`cull_passes` 改为 usage-root 驱动并删除 `has_no_writes`/`writes_external` 兜底存活 |
| `zircon_runtime/src/render_graph/error.rs` | 新增 `FirstWriteMissingAttachmentOps`、`MissingCullRoot` 变体 |
| `zircon_runtime/src/render_graph/graph.rs` | `allocate_transient_lifetimes` 槽位分桶键从 kind 细化为完整描述符哈希;`CompiledRenderGraphTransientAllocation` 增 `bucket_key_hash: u64`;新增 `CompiledRenderGraph::dump()` |
| `zircon_runtime/src/render_graph/mod.rs` | 仅 wiring:声明 `dump` 模块、导出新类型 |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/resource_resolver.rs` | `RgResourceResolver` 增加物理解析(借用 `RenderGraphExecutionResources`),元数据 API 全量保留 |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context.rs` | `resource_resolver()` 改为按需组合 graph 元数据 + `gpu.resources` 物理表返回 `RgResourceResolver`;导出名同步 |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_graph_execution_resources.rs` | 删除 `materialize_transient_resources`(职责移入池);新增 `bind_pooled_texture`/`bind_pooled_buffer`;`require_texture_view`/`require_buffer` 可见性收紧为 `pub(in crate::graphics::scene::scene_renderer)`,执行器只许经 `RgResourceResolver` |
| `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/mod.rs` | 仅 wiring:`transient_resource_pool` 模块声明与受控导出 |
| `zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_core/scene_renderer_core.rs` | `SceneRendererCore` 增 `transient_pool: TransientResourcePool` 字段 |
| `zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_core_render_compiled_scene/render/render.rs` | `RenderGraphExecutionResources::new()` 之后接 `transient_pool.allocate_frame_resources(...)`;帧末 `end_frame()` |
| `zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_core_render_compiled_scene/render/execute_graph_stage.rs` | 删除其内 `materialize_transient_resources` 调用;pass 循环改经 `RgResourceResolver` 注入 |
| `zircon_runtime/src/graphics/pipeline/declarations/render_pipeline_asset.rs` | `RenderPipelineAsset` 增 `revision: u64` |
| `zircon_runtime/src/graphics/pipeline/declarations/mod.rs`、`pipeline/mod.rs` | 仅 wiring:`compiled_graph_cache` 导出 |
| `zircon_runtime/src/graphics/runtime/render_framework/render_framework_state/render_framework_state.rs` | `RenderFrameworkState` 增 `compiled_graph_cache: CompiledGraphCache` 字段 |
| `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/build_frame_submission_context/compile_pipeline.rs` | `compile_submission_pipeline` 改为 `CompiledGraphCache::get_or_compile`,原编译函数降级为 miss 路径闭包 |
| `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/frame_submission_context.rs` | `compiled_pipeline` 持有类型改 `Arc<CompiledRenderPipeline>` |
| `zircon_runtime/src/graphics/runtime/render_framework/register_pipeline_asset/register_pipeline_asset.rs`、`set_pipeline_asset/set_pipeline_asset.rs`、`reload_pipeline/reload_pipeline.rs` | 资产注册/替换/重载时 bump `revision` 并调 `compiled_graph_cache.invalidate_pipeline(handle)` |
| `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/update_stats/`(update_stats 实现文件) | 写入池统计与缓存命中统计 |
| `zircon_runtime/src/graphics/pipeline/render_pipeline_asset/compile.rs` | 内建资源声明补 usage 标记(backbuffer→present、history→persistent、readback 输出→readback);违反首写决策表的声明就地修正 |
| `zircon_runtime/src/core/framework/render/backend_types.rs` | `RenderStats` 增池/缓存统计字段(framework 契约层,纯 POD,无 wgpu) |
| `zircon_runtime/src/graphics/runtime/render_framework/graphics_debugger_capture.rs`(及 `graphics_debugger_capture/` 状态) | capture 帧附带 graph dump 文本,经既有 capture 查询路径返回 |

### 核心类型与接口

层归属约定:`render_graph/` 是无 wgpu 的规划层;`graphics/**` 是实现层,wgpu 类型只能出现在这里;`core::framework::render` 契约层只接收纯数据统计(`RenderStats` 字段)。`RgResourceResolver`、`TransientResourcePool` 携带 wgpu 类型,因此固定在 graphics 层,不出 graphics(不进 framework 契约、不进 `zircon_runtime_interface`)。

```rust
// ---- render_graph/types.rs(规划层,无 wgpu)----
#[derive(Clone, Copy, Debug, Default, PartialEq, Eq, Hash)]
pub struct RenderGraphResourceUsageFlags {
    /// 写入它的 pass 链是裁剪 root(最终呈现面)
    pub present: bool,
    /// CPU 回读目标,裁剪 root(对接计划 16 GpuReadbackQueue)
    pub readback: bool,
    /// 跨帧持久(history 等),裁剪 root 且绕过瞬态池(对接计划 06)
    pub persistent: bool,
}
// RenderGraphResourceDeclaration / RenderGraphResourceLifetime 各增字段:
//     pub usage: RenderGraphResourceUsageFlags,

// ---- render_graph/builder.rs(规划层)----
impl RenderGraphBuilder {
    // 既有 API 原样保留(签名不变):
    // create_texture(desc: TextureDesc) -> RgTextureHandle
    // create_buffer(desc: BufferDesc) -> RgBufferHandle
    // import_external_resource(name) -> ExternalResource
    // add_pass_with_executor(name, queue, executor_id) -> RenderPassId
    // read_texture / write_texture / write_texture_with_ops / write_storage_texture
    // read_buffer / write_buffer / read_external / write_external(_with_ops)
    // compile(self) -> Result<CompiledRenderGraph, RenderGraphError>

    /// 新增:带 usage 的外部导入;既有 import_external_resource 等价于全 false。
    pub fn import_external_resource_with_usage(
        &mut self,
        name: impl Into<String>,
        usage: RenderGraphResourceUsageFlags,
    ) -> ExternalResource;
    /// 新增:标记瞬态资源跨帧持久(history)。persistent 资源允许首写 Load,绕过池化。
    pub fn mark_persistent(&mut self, texture: RgTextureHandle) -> Result<(), RenderGraphError>;
    /// 新增:标记资源为回读 root。
    pub fn mark_readback(&mut self, resource: RenderGraphResource) -> Result<(), RenderGraphError>;
}

// ---- render_graph/error.rs 新增变体 ----
// FirstWriteMissingAttachmentOps { resource: String, pass: String }
// MissingCullRoot { graph_name: String }   // 全图无 present/readback/persistent/side-effect root

// ---- render_graph/dump.rs(规划层)----
#[derive(Clone, Debug, PartialEq, Eq)]
pub struct RenderGraphDump {
    pub graph_name: String,
    pub pass_rows: Vec<RenderGraphDumpPassRow>,      // 执行序、queue、executor_id、culled、资源 IO
    pub resource_rows: Vec<RenderGraphDumpResourceRow>, // name、kind、usage、[first_pass, last_pass]、桶哈希
}
impl CompiledRenderGraph {
    pub fn dump(&self) -> RenderGraphDump;
}
impl RenderGraphDump {
    pub fn to_text(&self) -> String; // 行式文本,RenderDoc 抓帧对拍用
}

// ---- graphics/.../graph_execution/transient_resource_pool.rs(graphics 实现层)----
pub(in crate::graphics::scene::scene_renderer) struct TransientResourcePool {
    texture_buckets: HashMap<TransientTextureKey, Vec<PooledTextureEntry>>,
    buffer_buckets: HashMap<TransientBufferKey, Vec<PooledBufferEntry>>,
    frame_index: u64,
    last_frame_stats: TransientPoolFrameStats,
}
/// 描述符桶键:同键资源物理可互换。label 不参与哈希。
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
struct TransientTextureKey {
    width: u32, height: u32, depth: u32,
    mip_levels: u32, sample_count: u32,
    dimension: TextureDimension, format: TextureFormat, usage: TextureUsage,
}
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
struct TransientBufferKey { size_bytes: u64, usage: BufferUsage }
struct PooledTextureEntry {
    texture: wgpu::Texture,
    default_view: wgpu::TextureView,
    last_used_frame: u64,
}
#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
pub struct TransientPoolFrameStats {
    pub logical_texture_count: usize,
    pub physical_texture_count: usize,
    pub reused_texture_count: usize,
    pub created_texture_count: usize,
    pub logical_buffer_count: usize,
    pub physical_buffer_count: usize,
    pub pool_entry_count: usize,
    pub evicted_entry_count: usize,
}
impl TransientResourcePool {
    pub fn new() -> Self;
    /// 帧首调用:对 graph 的瞬态生命周期做桶内区间着色,把逻辑资源绑定进
    /// RenderGraphExecutionResources(name → 池条目 view/buffer)。
    /// 跳过 imported、usage.persistent、is_sparse_reserved_texture() 的资源。
    pub fn allocate_frame_resources(
        &mut self,
        device: &wgpu::Device,
        graph: &CompiledRenderGraph,
        resources: &mut RenderGraphExecutionResources,
    ) -> Result<TransientPoolFrameStats, String>;
    /// 帧末调用:frame_index += 1;驱逐 last_used_frame 距今超过 KEEP_FRAMES(=8)的条目。
    pub fn end_frame(&mut self);
    pub fn last_frame_stats(&self) -> TransientPoolFrameStats;
}
```

`allocate_frame_resources` 分配算法(物理别名 = 池化复用,不做底层内存 alias):

1. 取 `graph.resource_lifetimes()`,过滤掉 `imported`、`usage.persistent`、`is_sparse_reserved_texture()`(稀疏纹理保留计划 13 的既有旁路)。
2. 按 `TransientTextureKey` / `TransientBufferKey` 分桶(描述符哈希做桶)。
3. 桶内按 `(first_pass, last_pass, name)` 排序后线性扫描区间着色:维护 `slot_last_passes: Vec<usize>`,若存在 slot 满足 `slot_last_pass < lifetime.first_pass`(区间不相交)则复用该 slot,否则新开 slot。算法与 `graph.rs::allocate_transient_lifetimes` 既有实现一致,唯一差异是桶键从 kind 细化为完整描述符(资源数 < 百级,线性扫描足够,与里程碑 RG-M2 原文一致)。
4. 桶内 slot i 映射到该桶池条目 i:条目已存在则复用(`reused_texture_count += 1`,刷新 `last_used_frame`);不足则 `device.create_texture`/`create_buffer` 新建(`created_texture_count += 1`)。物理创建复用 `render_graph_execution_resources.rs` 中既有的 `create_wgpu_texture`/`create_wgpu_buffer` 与 usage 推导函数(随删除 `materialize_transient_resources` 一并移入本文件)。
5. 对每个逻辑资源调 `resources.bind_pooled_texture(name, entry.default_view.clone())` / `bind_pooled_buffer(name, entry.buffer.clone())`;SSR coarse mip 别名沿用既有 `ssr_pyramid_mip_alias` 路径(父纹理来自池条目)。

```rust
// ---- graphics/pipeline/compiled_graph_cache.rs(graphics 实现层,无 wgpu)----
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
pub struct CompiledGraphCacheKey(u64); // 各成分 FNV-1a 折叠
impl CompiledGraphCacheKey {
    pub fn compose(
        pipeline_handle: RenderPipelineHandle,
        pipeline_revision: u64,                       // 资产修订,注册/重载时 bump
        options: &RenderPipelineCompileOptions,        // feature 集 + 能力开关 + msaa + post stack
        quality_profile: Option<&str>,                 // profile
        viewport_size: UVec2,                          // viewport class(尺寸;格式经 options.graph_msaa 与内建约定)
        extract_fingerprint: u64,                      // 编译期读取的 extract 字段指纹
        capabilities: &RenderCapabilitySummary,        // device caps 摘要
    ) -> Self;
}
/// 编译期读取面指纹:core pipeline 种类、effect stack 各效果 enable 位、
/// camera target 拓扑相关字段。逐帧变化的场景数据(变换/灯光参数)不进指纹。
pub(crate) fn extract_compile_fingerprint(extract: &RenderFrameExtract) -> u64;

pub struct CompiledGraphCache {
    entries: Vec<CompiledGraphCacheEntry>, // capacity 默认 16,LRU(对齐 Unity k_CachedGraphCount 量级)
    capacity: usize,
    stats: CompiledGraphCacheStats,
}
struct CompiledGraphCacheEntry {
    key: CompiledGraphCacheKey,
    pipeline_handle: RenderPipelineHandle, // invalidate_pipeline 用
    last_used_frame: u64,
    pipeline: Arc<CompiledRenderPipeline>,
}
#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
pub struct CompiledGraphCacheStats { pub hits: u64, pub misses: u64, pub evictions: u64 }
impl CompiledGraphCache {
    pub fn with_capacity(capacity: usize) -> Self;
    /// 命中:刷新 last_used_frame 并克隆 Arc;miss:执行 compile 闭包,
    /// 满容量时按 last_used_frame 驱逐最旧条目(evictions += 1)。
    pub fn get_or_compile(
        &mut self,
        key: CompiledGraphCacheKey,
        pipeline_handle: RenderPipelineHandle,
        frame_index: u64,
        compile: impl FnOnce() -> Result<CompiledRenderPipeline, RenderFrameworkError>,
    ) -> Result<Arc<CompiledRenderPipeline>, RenderFrameworkError>;
    pub fn invalidate_pipeline(&mut self, pipeline: RenderPipelineHandle);
    pub fn clear(&mut self);
    pub fn stats(&self) -> CompiledGraphCacheStats;
}

// ---- graphics/.../render_pass_execution_context/resource_resolver.rs(graphics 实现层)----
/// 跨计划契约名:RgResourceResolver。
/// 元数据 API 全量保留,新增物理解析。不出 graphics 层。
pub struct RgResourceResolver<'a> {
    graph: &'a CompiledRenderGraph,
    pass_id: RenderPassId,
    physical: Option<&'a RenderGraphExecutionResources>, // 纯元数据上下文(无 GPU 测试)为 None
}
impl<'a> RgResourceResolver<'a> {
    pub fn new(graph: &'a CompiledRenderGraph, pass_id: RenderPassId) -> Self; // 元数据模式
    pub(in crate::graphics::scene::scene_renderer) fn with_physical(
        graph: &'a CompiledRenderGraph,
        pass_id: RenderPassId,
        physical: &'a RenderGraphExecutionResources,
    ) -> Self;
    // 物理解析:句柄 → declaration → 校验本 pass 声明了对应访问 → 物理表取值。
    // 未声明访问返回 Err(消息含 pass 名与资源名),这是"删除旁路"后的唯一访问面。
    pub fn texture_view(&self, handle: RgTextureHandle) -> Result<&'a wgpu::TextureView, String>;
    pub fn external_texture_view(&self, handle: ExternalResource) -> Result<&'a wgpu::TextureView, String>;
    pub fn buffer(&self, handle: RgBufferHandle) -> Result<&'a wgpu::Buffer, String>;
    // 内建 executor 以 PostProcessGraphResourceNames 常量寻址,按名解析同样过声明校验:
    pub fn texture_view_by_name(&self, name: &str, access: RenderGraphResourceAccessKind) -> Result<&'a wgpu::TextureView, String>;
    pub fn buffer_by_name(&self, name: &str, access: RenderGraphResourceAccessKind) -> Result<&'a wgpu::Buffer, String>;
    // 元数据 API(签名保持稳定):
    // resource_declaration / resource_declaration_by_name / resource_lifetime /
    // resource_lifetime_by_name / pass_declares_resource / pass_declares_resource_access /
    // pass_resource_access / pass_resource_access_by_name / pass_resource_declaration_by_name / pass_resources
}
```

首写 attachment ops 决策表(`compile()` 期对每个非 culled 瞬态纹理找到执行序中第一个写它的 pass 后裁决;违例返回 `RenderGraphError`,不默默通过):

| 首写场景 | ops | 裁决 |
|---------|-----|------|
| transient texture,attachment 写(`write_texture`/`write_texture_with_ops`) | `Clear/Store` 或 `Clear/Discard` | 合法 |
| 同上 | `Load/*` | `LoadBeforeProducer`(既有,保留) |
| 同上 | `None`(漏声明 ops) | `FirstWriteMissingAttachmentOps`(新增) |
| transient texture,storage 写(`write_storage_texture`,compute 整面覆写) | `None` | 合法(DontCare 语义,覆写责任在 executor) |
| `mark_persistent` 标记的瞬态纹理(history) | `Load/*` | 合法(跨帧内容);未标记则按上两行裁决 |
| external(backbuffer / 相机 RT) | `Load` 或 `Clear` | 合法(外部内容由导入方负责) |

root 驱动反向裁剪(替换 `cull_passes` 既有判据):live root = 写入 `usage.present || usage.readback || usage.persistent` 资源的 pass、`flags.has_side_effects`、`!flags.allow_culling`;然后沿"root 的读集合 + 显式 dependencies"逆执行序传播(算法骨架沿用既有 `cull_passes` 的逆序扫描)。同一变更内删除"写 external 一律存活"与"无写 pass 一律存活"两条兜底;全图无 root 时编译报 `MissingCullRoot`。`compile.rs` 在同一变更内给内建资源补标记:viewport 输出 external → `present`,history 槽 → `persistent`,picking/capture 输出 → `readback`。

### GPU 数据布局与 WGSL 约定(适用时)

本计划不新增任何 GPU buffer、bind group 或 WGSL include:瞬态池与编译缓存是 CPU 侧资源管理,`RgResourceResolver` 只是句柄到既有 wgpu 资源的查找面。唯一与 GPU 布局相关的规则是 usage 推导:graph 编译期把每个纹理的所有声明访问合并为 `TextureUsage`(被读 → `SAMPLED`,storage 写 → `STORAGE`,attachment 写 → `RENDER_ATTACHMENT`,history 拷贝 → `COPY_SRC|COPY_DST`),该合并结果进入 `TransientTextureKey`,保证池条目可被所有使用场景复用。新 pass 的 bind group 槽位遵守 index.md §8 第 1 条,本计划不涉及新增槽位。

### 帧时序与集成点

稳态一帧的精确时序(函数均为现存实现,括号内为本计划的插入/替换动作):

1. `submit_frame_extract`(`graphics/runtime/render_framework/submit_frame_extract/submit/submit.rs`)→ `build_frame_submission_context`。
2. `compile_submission_pipeline`(`build_frame_submission_context/compile_pipeline.rs`):**替换** —— 由每帧无条件 `pipeline_asset().compile_with_options(extract, options)` 改为 `state.compiled_graph_cache.get_or_compile(key, ...)`,key 按上节 `compose` 组成;原编译调用降级为 miss 闭包,`validate_compiled_pipeline_capabilities` 只在 miss 路径执行(命中条目编译时已验过)。`FrameSubmissionContext::compiled_pipeline()` 改持 `Arc<CompiledRenderPipeline>`。缓存实例挂在 `RenderFrameworkState`(`render_framework_state.rs`),与 `pipelines`/`viewports` 同级。
3. 失效路径:`register_pipeline_asset`、`set_pipeline_asset`、`reload_pipeline`、`set_quality_profile` 在变更资产/选项时 bump `RenderPipelineAsset::revision` 并调 `invalidate_pipeline(handle)`;viewport 尺寸与能力摘要变化天然改变 key,无需显式失效。
4. `SceneRenderer::render_frame_with_pipeline` → `render_frame_with_pipeline_to_target` → `SceneRendererCore::render_compiled_scene`(`render/render.rs`):在 `let mut graph_resources = RenderGraphExecutionResources::new();` 之后先由 `bind_frame_graph_resources(...)` 绑定 live renderer-owned frame resources,再**插入** `self.transient_pool.allocate_frame_resources(device, &pipeline.graph, &mut graph_resources)?`;**删除** `render.rs` 与 `execute_graph_stage.rs` 中对 `materialize_transient_resources` 的两处调用及该函数本体(硬切换)。history 纹理仍由 `prepare_history_textures`(`scene_renderer_history`)持久管理并以 import 方式进 `graph_resources`,池不接管(与计划 06 对齐的接缝就是 `mark_persistent` + import)。
5. pass 循环(`execute_graph_stage.rs`):构造 `RenderPassExecutionContext` 时 `with_resource_resolver(&pipeline.graph, pass.id)` 语义升级为物理模式(`RgResourceResolver::with_physical`,physical 取 `gpu.resources`);各内建 executor(`builtin_scene_executors.rs`、`builtin_postprocess_executors.rs`、`preview_sky_executor.rs`)中所有 `resources.require_texture_view(name)` 直取改为 `context.resolver()?.texture_view_by_name(name, access)`;`RenderGraphExecutionResources::require_*` 可见性收紧,使绕过声明校验的直取在编译期不可达(删除旁路)。debug marker 仍由 `marker_for_render_graph_pass`(`graphics/debug_markers.rs`)在录制时打点,culled pass 不录制、不打 marker。
6. 帧末:`render_compiled_scene` 返回前调 `self.transient_pool.end_frame()`;`update_stats` 把 `transient_pool.last_frame_stats()` 与 `compiled_graph_cache.stats()` 写入 `RenderStats` 新字段(`backend_types.rs`):`last_graph_transient_pool_logical_texture_count`、`last_graph_transient_pool_physical_texture_count`、`last_graph_transient_pool_reused_texture_count`、`last_graph_transient_pool_created_texture_count`、`last_graph_transient_pool_entry_count`、`last_graph_compiled_cache_hits`、`last_graph_compiled_cache_misses`、`last_graph_compiled_cache_evictions`。
7. 诊断:`begin_graphics_debugger_capture`/`finish_active_capture_and_relock`(`graphics_debugger_capture.rs`)的 capture 帧附带 `pipeline.graph.dump().to_text()`,经既有 capture 查询路径返回给调用方与 RenderDoc 对拍。

### 实施切片细化

里程碑结构沿用正文 RG-M1..M4;每个切片是一次可评审的变更,切片期只跑 `cargo check -p zircon_runtime --lib --locked`,测试集中在各里程碑测试阶段。

**RG-M1 资源句柄与生命周期模型**

- 切片 1.1 usage flags 与声明扩展
  1. `render_graph/types.rs`:加 `RenderGraphResourceUsageFlags`;`RenderGraphResourceDeclaration`/`RenderGraphResourceLifetime` 加 `usage` 字段 → 判据:cargo check 通过,既有构造点以 `Default::default()` 补齐。
  2. `render_graph/builder.rs` + `mod.rs`:`import_external_resource_with_usage`/`mark_persistent`/`mark_readback`,`ResourceNode` 存 usage 并传入 declarations/lifetimes → 判据:`render_graph/tests` 既有用例不改语义即编译通过。
  3. `graphics/pipeline/render_pipeline_asset/compile.rs`:viewport 输出标 `present`、history 槽标 `persistent`、readback 输出标 `readback` → 判据:cargo check;graph dump(切片 4.1 前先用 `Debug` 输出人工确认)中标记可见。
- 切片 1.2 首写 attachment ops 校验
  1. `render_graph/error.rs`:加 `FirstWriteMissingAttachmentOps` → 判据:cargo check。
  2. `render_graph/builder.rs::compile()`:按决策表实现首写裁决(在 `infer_resource_dependencies` 之后、`cull_passes` 之前,作用于执行序首写);persistent 资源放行 Load → 判据:cargo check;以临时单测核对一例违例报错文案。
  3. `compile.rs` 与各 feature descriptor 声明修正:对新校验报出的缺 ops 写入逐个补 `write_texture_with_ops` 显式声明或给出 Load 理由(persistent)→ 判据:内建 forward+/deferred/core2d 三条默认管线 `compile()` 全部 Ok。
- 切片 1.3 RgResourceResolver 收口(本里程碑最大切片,波及全部内建 executor)
  1. `resource_resolver.rs`:`RgResourceResolver` 加 `with_physical` 与句柄/按名物理解析 → 判据:cargo check(旧 resolver 名引用全部清零)。
  2. `render_pass_execution_context.rs`:`resource_resolver()` 返回物理模式 resolver(gpu 存在时);删除旧导出名 → 判据:cargo check。
  3. `render_graph_execution_resources.rs`:`require_texture_view`/`require_buffer`/`texture_view`/`buffer` 收紧可见性;`builtin_*_executors.rs`、`preview_sky_executor.rs`、`post_process` 执行路径全部改经 resolver;插件 executor(`zircon_plugins` 的 rendering/hybrid_gi/virtual_geometry/particles)同一变更内适配 → 判据:root workspace + `zircon_plugins` workspace cargo check 全绿,`grep require_texture_view` 在 executor 文件中无直调残留。
- 测试阶段(RG-M1):新增 `render_graph/tests/attachment_ops.rs`;跑 `cargo test -p zircon_runtime render_graph --locked`、`cargo test -p zircon_runtime render_product --locked`;验收证据按正文(graph dump 可见全部内建 pass IO、缺 clear 清零);更新 `docs/zircon_runtime` render_graph 镜像文档。

**RG-M2 瞬态资源池**

- 切片 2.1 池本体与桶化分配计划
  1. `graph.rs`:`allocate_transient_lifetimes` 桶键细化为描述符哈希,`CompiledRenderGraphTransientAllocation` 加 `bucket_key_hash` → 判据:cargo check,既有 `transient_allocation_plan` 统计字段语义保持(slot 字节=桶内描述符尺寸)。
  2. 新增 `transient_resource_pool.rs`:类型、分配算法、`end_frame` 驱逐;把 `create_wgpu_texture`/`create_wgpu_buffer`/usage 推导从 `render_graph_execution_resources.rs` 移入 → 判据:cargo check。
- 切片 2.2 帧路径接池(硬切换)
  1. `scene_renderer_core.rs` 加字段;`render.rs` 插入 `allocate_frame_resources` + 帧末 `end_frame`;删除 `materialize_transient_resources` 本体与 `execute_graph_stage.rs` 调用;`RenderGraphExecutionResources` 加 `bind_pooled_*` → 判据:cargo check;编辑器 viewport 手动冒烟画面正常。
  2. persistent/sparse 旁路核对:history 经 import、sparse 经计划 13 既有路径,池逻辑断言不接管 → 判据:cargo check + 旁路单测留到测试阶段。
- 切片 2.3 统计入口
  1. `backend_types.rs` 加池字段;`update_stats` 写入 → 判据:cargo check。
- 测试阶段(RG-M2):`cargo test -p zircon_runtime render_graph --locked`(含正文要求的"同尺寸两瞬态区间不重叠 → 物理资源数 1"断言);RenderDoc 抓帧确认画面不变;验收证据:deferred + post 典型场景 `physical_texture_count` 明显低于 `logical_texture_count`。

**RG-M3 pass culling 与编译缓存**

- 切片 3.1 root 驱动裁剪
  1. `builder.rs::cull_passes` 改 root 判据,删两条兜底;`error.rs` 加 `MissingCullRoot`;`compile.rs` 标记缺口补齐 → 判据:三条默认管线编译通过且 culled 集合不增(全启用时);关闭 bloom 后 `post.bloom-extract` 上游链 culled(临时断言)。
- 切片 3.2 revision 与 CompiledGraphCache
  1. `render_pipeline_asset.rs` 加 `revision`;注册/替换/重载路径 bump + invalidate → 判据:cargo check。
  2. 新增 `compiled_graph_cache.rs`;`render_framework_state.rs` 挂字段;`compile_pipeline.rs` 接 `get_or_compile`;`frame_submission_context.rs` Arc 化(调用方 `render_frame_with_pipeline` 形参 `&CompiledRenderPipeline` 不变,传 `&*arc`)→ 判据:cargo check;手动连续两帧日志确认第二帧走 hit。
- 切片 3.3 指纹固化与热路径清理
  1. `extract_compile_fingerprint` 清点 `compile_with_options` 的 extract 读取面并实现;debug_assert:命中帧重算指纹等于 key 成分 → 判据:cargo check + 编辑器冒烟无 assert。
  2. `update_stats` 写缓存统计 → 判据:cargo check。
- 测试阶段(RG-M3):`cargo test -p zircon_runtime render_graph --locked` 与 `cargo test -p zircon_runtime compiled_render_pipeline --locked`;验收证据按正文(关 bloom 裁剪统计断言、第二帧零编译 / 命中计数断言)。

**RG-M4 诊断与 RenderDoc 对接**

- 切片 4.1 graph dump
  1. 新增 `render_graph/dump.rs` + `CompiledRenderGraph::dump()`;`mod.rs` wiring → 判据:cargo check。
  2. `graphics_debugger_capture.rs`:capture 帧生成 dump 文本并随 capture 报告暴露 → 判据:`ZR_RENDERDOC_CAPTURE_NEXT=1` 手动抓帧能取到 dump。
- 切片 4.2 marker 对拍
  1. 审计 `execute_graph_stage.rs` 的 `marker_for_render_graph_pass` 打点覆盖全部非 culled pass(含 async 回落 pass);culled pass 零 marker → 判据:`executed_debug_markers()` 与 dump pass 行一一对应。
- 测试阶段(RG-M4):`cargo test -p zircon_runtime --lib --locked` 范围回归;验收证据:RenderDoc 抓帧 pass 名与 graph dump 一致。

### 测试与验收清单

命名遵守 index.md §8 第 6 条(`render_<topic>_*` 单测、`render_product_*` 产物对拍)。

RG-M1(文件:`render_graph/tests/attachment_ops.rs`;resolver 用例在 `resource_resolver.rs` 的 `#[cfg(test)] mod tests`):

| 测试函数 | 断言 |
|---------|------|
| `render_graph_first_write_without_ops_fails_for_attachment_texture` | attachment 用途瞬态纹理首写 ops=None → `Err(FirstWriteMissingAttachmentOps)`,错误文案含资源与 pass 名 |
| `render_graph_first_write_load_without_producer_fails` | 首写 `load_store` → `Err(LoadBeforeProducer)`(既有语义回归) |
| `render_graph_persistent_marked_texture_allows_load_first_write` | `mark_persistent` 后首写 Load → `compile()` Ok |
| `render_graph_storage_first_write_without_ops_is_valid` | `write_storage_texture` 首写 → Ok |
| `render_graph_resolver_rejects_undeclared_resource_access` | pass 未声明的句柄经 `texture_view`/`buffer` 解析 → Err |
| `render_graph_resolver_returns_view_for_declared_access` | 声明过写访问的句柄解析出与物理表同一 view(`RenderBackend::new_offscreen()` 驱动) |

RG-M2(文件:`transient_resource_pool.rs` 内联 tests,需 offscreen backend):

| 测试函数 | 断言 |
|---------|------|
| `render_graph_transient_pool_aliases_disjoint_lifetimes_to_one_texture` | 同描述符两资源区间不相交 → `physical_texture_count == 1 && logical_texture_count == 2` |
| `render_graph_transient_pool_separates_overlapping_lifetimes` | 区间相交 → 物理数 2 |
| `render_graph_transient_pool_buckets_by_descriptor_not_name` | 同名义不同 format → 不同桶、不复用 |
| `render_graph_transient_pool_reuses_entries_across_frames` | 第二帧 `created_texture_count == 0 && reused_texture_count > 0` |
| `render_graph_transient_pool_evicts_stale_entries_after_keep_frames` | 资源停用后第 9 帧 `evicted_entry_count > 0`、`pool_entry_count` 下降 |
| `render_graph_transient_pool_skips_persistent_and_sparse_resources` | persistent/sparse 生命周期不产生池条目(history/SVT 旁路) |

RG-M3(文件:`compiled_graph_cache/tests.rs` child tests;裁剪在 `render_graph/tests/culling.rs`;集成断言在 render_framework 既有测试目录):

| 测试函数 | 断言 |
|---------|------|
| `render_graph_culls_passes_unreachable_from_present_root` | 写孤立纹理的 pass culled=true,统计 `culled_pass_count` 增加 |
| `render_graph_readback_marked_buffer_keeps_producer_alive` | `mark_readback` 的 buffer 生产 pass 不被裁剪 |
| `render_graph_side_effect_pass_survives_culling` | `has_side_effects` pass 始终存活 |
| `render_graph_missing_cull_root_is_compile_error` | 全图无 root → `Err(MissingCullRoot)` |
| `compiled_render_pipeline_cache_hits_on_identical_key` | 同 key 第二次 `get_or_compile` 不执行闭包,`hits == 1` |
| `compiled_render_pipeline_cache_misses_on_feature_set_change` | options 中 feature 集变化 → key 不同 → miss |
| `compiled_render_pipeline_cache_misses_on_viewport_resize` | 尺寸变化 → miss |
| `compiled_render_pipeline_cache_invalidates_on_pipeline_revision_bump` | bump revision 后旧 key 条目失效 |
| `compiled_render_pipeline_cache_evicts_least_recently_used_entry` | 超容量插入驱逐 `last_used_frame` 最小者,`evictions == 1` |
| `render_graph_disabled_bloom_culls_bloom_extract_chain` | 关 bloom 编译选项 → `post.bloom-extract` 及其独立上游 culled(对应正文验收) |
| `render_graph_steady_state_second_frame_skips_recompile` | 连续两帧提交,`RenderStats::last_graph_compiled_cache_hits` 第二帧递增、misses 不变(对应"第二帧编译耗时为 0") |

RG-M4(文件:`render_graph/tests` 与 graph_execution 既有测试):

| 测试函数 | 断言 |
|---------|------|
| `render_graph_dump_lists_pass_order_resources_and_culled` | dump 行数 = pass 数,culled 标记与 `CompiledRenderPass::culled` 一致,资源行含 `[first_pass, last_pass]` |
| `render_graph_executed_markers_match_dump_pass_rows` | `executed_debug_markers()` 与 dump 非 culled pass 行一一对应(前缀 `zircon::RenderGraphPass::`) |

render_product 对拍场景(`cargo test -p zircon_runtime render_product --locked` + `ZR_RENDERDOC_CAPTURE_NEXT=1` 人工抓帧):

- `render_product_deferred_post_chain_transient_pool`:deferred + SSAO + bloom + tonemap 场景,池化后帧产物与既有 render_product 基线逐像素一致;同帧 `physical_texture_count < logical_texture_count` 写入断言。
- `render_product_forward_plus_cache_steady_state`:forward+ 场景连续三帧,第 2/3 帧缓存命中且产物与第 1 帧一致(排除缓存引入的拓扑漂移)。

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-06-24 | CompiledGraphCache tests owner split | render_plan01_compiled_graph_cache_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | `graphics/pipeline/compiled_graph_cache.rs` 从 776 行 near-threshold production+inline-test owner 收束为 309 行 production cache parent，只保留 cache key/fingerprint/capability/status/cache API、LRU eviction 和 `#[cfg(test)] mod tests;` mount。新增 460 行 `graphics/pipeline/compiled_graph_cache/tests.rs` 承接 cache hit/miss/status、compile fingerprint、camera target/format class、revision invalidation 与 LRU eviction source-contract tests。新增 `runtime_15_compiled_graph_cache_tests_are_child_owner` 锁定 moved tests/helper trait 不回流、父/子 800 行预算和 Plan 01/render index/structure/review/builder/session docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan、conflict-marker scan 和 scoped diff-check 已通过；diff-check 仅有 Git LF->CRLF 提示。当前仍有 cargo/rustc 编译通道，Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_compiled_graph_cache_tests_are_child_owner`、compiled graph cache focused tests、second-frame cache-hit integration 与 RenderDoc marker/resource 对拍；后续 cache tests 继续进入 child owner,不得回流父文件。 |
| 2026-06-24 | RenderGraphBuilder compile owner split | render_graph_builder_compile_owner_split_static_passed_cargo_deferred_implementation_cadence | `zircon_runtime/src/render_graph/builder.rs` 从 890 行生产 graph authoring + compile-analysis 混合 owner 收束为 412 行 authoring parent，只保留 pass/resource declaration API、resource access mutation、usage marking 和 `mod compile;` mount。新增 486 行 `zircon_runtime/src/render_graph/builder/compile.rs` 承接 `RenderGraphBuilder::compile(...)`、unique-resource validation、manual reachability、write/read dependency validation、dependency inference、topological order、root-driven culling 与 resource lifetime assembly。新增 `runtime_15_render_graph_builder_compile_is_child_owner` 锁定 compile helpers 不回流、父/子 800 行预算和 Plan 01/render index/structure/review/builder/session docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan、conflict-marker scan 和 scoped diff-check 已通过；diff-check 仅有 Git LF->CRLF 提示。当前仍有 cargo/rustc 编译通道，Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `render_graph` focused lane、graph compile/cache broader validation 与 RenderDoc marker/resource 对拍；继续收敛剩余 near-threshold render graph/product owners。 |
| 2026-06-24 | RenderGraph resources transient aliasing tests owner split | render_graph_resources_transient_aliasing_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | `zircon_runtime/src/render_graph/tests/resources.rs` 从 884 行混合 graph resource/lifetime/ops/aliasing test owner 收束为 633 行基础 resource contract parent,只保留 resource lifetime、attachment ops、storage write、sparse reservation、compute workload、validation、dump 与 culling tests 并挂载 `mod transient_aliasing;`。新增 192 行 `zircon_runtime/src/render_graph/tests/resources/transient_aliasing.rs` 承接 `graph_builds_transient_aliasing_plan_for_non_overlapping_lifetimes` 与 `graph_transient_allocation_plan_reports_slot_reserved_bytes`，继续覆盖 descriptor-bucket aliasing、bucket-local slot bytes、dense/sparse byte accounting。新增 `runtime_15_render_graph_resources_transient_aliasing_tests_are_child_owner` 锁定 moved-test ownership、父/子 800 行预算和 Plan 01/render index/structure/review/builder/architecture/session docs anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan、conflict-marker scan 和 scoped diff-check 通过；Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `render_graph::tests::resources` focused lane、transient allocation focused test、graph dump text test 与 materialization broader validation；继续收敛剩余 near-threshold render graph tests。 |
| 2026-06-24 | RenderGraph materialization test owner split | render_plan01_materialization_tests_owner_split_static_passed_cargo_deferred_active_compile_lane | `graphics/scene/scene_renderer/graph_execution/materialization.rs` 从 699 行混合 WGPU materialization + inline tests 收束为 166 行 production owner,只保留 `materialize_transient_resources(...)`、WGPU texture/buffer descriptor conversion、usage mapping 与 `#[cfg(test)] mod tests;` mount。新增 `graphics/scene/scene_renderer/graph_execution/materialization/tests.rs` 承接 storage-usage、dense/sparse transient materialization、texture/buffer aliasing、terminal AA transient override、mip-view 与 SSR coarse-pyramid alias tests。新增 `runtime_15_render_graph_materialization_tests_are_child_owner_split` 锁定 moved-test ownership、父/子 800 行预算和 Plan 01/render index/structure/review/graph-execution docs anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active cargo/rustc compile lanes 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `materialization_` focused lane、graph execution broader validation 与 RenderDoc resource/marker 对拍;继续收敛剩余 near-threshold graph execution/material files。 |
| 2026-06-24 | Plan 01 GPU context mesh command lists owner split | render_plan01_gpu_context_mesh_command_lists_owner_split_static_passed_cargo_deferred_active_compile_lane | `graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu.rs` 从 819 行降到 709 行,继续保留 `RenderPassGpuExecutionContext`、WGPU pass context builder、depth/shadow/mesh/TAA reactive record methods 和 clear helper;新增 116 行 `graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu/mesh_command_lists.rs` 承接 `RenderPassMeshCommandLists`、phase stream helper、HZB occlusion candidate indirect execution 与 count helpers。新增 `runtime_15_render_pass_gpu_context_mesh_command_lists_are_child_owner` 锁定 moved owner 不回流、父/子 800 行预算和 docs/status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active cargo/rustc compile lanes 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `render_pass_execution_context`/`render_pass_executor_registry` focused lane 与 graph execution broader validation;继续收敛剩余 near-threshold submit/material files。 |
| 2026-06-24 | RenderPipelineAsset compile tests owner split | render_pipeline_asset_compile_tests_owner_split_static_passed_cargo_deferred_active_compile_lane | `graphics/pipeline/render_pipeline_asset/compile_tests.rs` 退为 104 行 helper/root test module,只保留 shared imports、child mounts、`test_extract(...)` 与 pass read/write/lifetime helpers。新增 `compile_tests/core_contracts.rs` 承接 renderer stage、compute workload、selected layer particle culling、HZB/SSR transient contract tests;`compile_tests/postprocess_routes.rs` 承接 LUT/bloom/blur/exposure/light-list/output-transfer/HZB mip route tests;`compile_tests/external_compute_guards.rs` 承接 required external texture/buffer conflict 与 compute queue validation tests。新增 `runtime_15_render_pipeline_compile_tests_are_child_owners` 锁定 moved-test ownership、四文件 800 行预算和 docs/status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active cargo/rustc/link compile lanes 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `compile_` / `render_pipeline_asset` focused test lane 与 graph compile broader validation;继续收敛剩余超预算 render product tests。 |
| 2026-06-24 | Pipeline compile monolith tests owner split | render_pipeline_compile_monolith_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | `graphics/tests/pipeline_compile.rs` 从 3086 行混合 pipeline compile test owner 收束为 339 行 shared fixture/root module,只保留 descriptor helpers、extract helpers 和 child mounts。新增 `graphics/tests/pipeline_compile/default_pipelines.rs`、`dynamic_resolution.rs`、`plugin_features.rs`、`temporal_and_ops.rs`、`compile_options.rs`、`feature_descriptors.rs`、`validation_core.rs` 与 `validation_descriptors.rs` 分别承接默认 Forward+/Deferred、动态分辨率/Core2D、plugin feature route、TAA/attachment ops、compile options、feature descriptor graph/resource、核心 validation 与 descriptor validation tests。新增 `runtime_15_pipeline_compile_monolith_tests_are_child_owners` 锁定 moved-test ownership、九文件 800 行预算和 docs/status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `pipeline_compile` focused lane、default pipeline exact filters、graph compile broader validation 与 RenderDoc/product 对拍;继续收敛剩余超预算 render framework bridge tests。 |
| 2026-06-24 | Plan 01 render pass executor registry test owner split | render_plan01_executor_registry_test_owner_split_static_passed_cargo_deferred_active_compile_lane | `graphics/scene/scene_renderer/graph_execution/render_pass_executor_registry/tests.rs` 退为 38 行测试根模块,只保留 support/plugin policy 挂载和三组 child owner:`tests/registry_contracts.rs` 承接注册表契约与 compiled-pipeline 校验测试,`tests/postprocess_context_guards.rs` 承接 temporal/postprocess/SSR 缺上下文 guard 与 effect-stack fixtures,`tests/renderer_context_guards.rs` 承接 UI/overlay/sprite/mesh/prepass/shadow/deferred WGPU context guard 和 shadow-atlas fixture。新增 `runtime_15_render_pass_executor_registry_tests_are_child_owners` 锁定 moved-test ownership、四文件 800 行预算和 docs/status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active cargo/rustc compile lanes 暂缓且不计通过。 | 后续在编译通道空闲后补跑 `render_pass_executor_registry` focused test lane 与 graph execution broader validation;继续收敛剩余超预算 render tests/product guards。 |
| 2026-06-24 | RG-M1 RenderGraphExecutionRecord observation suppression cleanup / F12 | render_plan01_execution_record_observation_suppression_cleanup_static_passed_cargo_deferred_active_lanes | `RenderGraphExecutionRecord` 不再用 dead-code suppression 维持 test-only observation/build helpers。legacy/simple pass push helpers、测试读回 setter、raw staged/resource/dependency/compute inspection accessors 已收进 `#[cfg(test)]`;`executed_post_process_nodes(...)` 与 `executed_stage_count(...)` 保持无 suppression 的生产观测面,因为 stats/product graph 路径实际消费。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\graph_execution\render_graph_execution_record.rs` 通过；`render_graph_execution_record.rs` suppression 扫描零命中；cfg(test) helper 调用面扫描限定在 tests/record owner 或 `#[cfg(test)]` 读回 attach helpers；graph_execution scoped F12 扫描当前无 matching suppression；Cargo/WGPU/RenderDoc 因 active cargo/rustc lanes 暂缓且不计通过。 | 后续补跑 execution-record/compute workload focused tests、broader graph_execution validation 与 RenderDoc marker/profile/resource 对拍。 |
| 2026-06-24 | RG-M1 RenderGraphExecutionResources owned texture descriptor suppression cleanup / F12 | render_plan01_execution_resources_owned_texture_desc_suppression_static_passed_cargo_deferred_active_lanes | `RenderGraphExecutionResources::owned_texture_desc(...)` 不再用 `cfg_attr(not(test), allow(dead_code))` 维持。该 descriptor lookup 已由 `copy_history_textures(...)` 的 GI history copy 路径和 `submit_compiled_scene_frame.rs` 的 graph-owned texture readback helpers 生产消费，用于确认 execution-owned texture 的格式、尺寸与 copy layout；资源 registry owner 不再把这个 live helper 标成未接线脚手架。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\graph_execution\render_graph_execution_resources.rs` 通过；`render_graph_execution_resources.rs` 中 `allow(dead_code)` / `allow(unused_imports)` / `cfg_attr(not(test), allow(dead_code))` / `#![allow]` 扫描零命中；`owned_texture_desc(...)` 使用扫描确认 live 生产调用；Cargo/WGPU/RenderDoc 因 active cargo/rustc lanes 暂缓且不计通过。 | 后续继续处理 `render_graph_execution_record.rs` 的剩余 execution-record F12 pockets；补跑 graph resource/materialization focused tests 与 RenderDoc resource/marker 对拍。 |
| 2026-06-24 | RG-M1 RenderPassExecutionContext suppression cleanup / F12 | render_plan01_execution_context_suppression_cleanup_static_passed_cargo_deferred_active_lanes | `RenderPassExecutionContext` 不再用 `cfg_attr(not(test), allow(dead_code))` 维持构造和队列 fallback helper。公开构造面保持可用，继续服务 graph execution focused tests 与插件测试上下文；`require_buffer_by_name(...)` 已是 postprocess/deferred/SSR 等生产 WGPU buffer lookup 路径的 live helper，因此直接移除 suppression。`render_pass_execution_context.rs` 与 `resource_resolver.rs` 当前均无 dead-code/unused-import suppression。 | `rustfmt --edition 2021 --check` 覆盖 `render_pass_execution_context.rs` 与 `resource_resolver.rs` 通过；两个文件的 `allow(dead_code)` / `allow(unused_imports)` / `cfg_attr(not(test), allow(dead_code))` / `#![allow]` 扫描零命中；graph_execution broader scan 仍显示 `render_graph_execution_record.rs` / `render_graph_execution_resources.rs` 存量 F12 debt，本切片不冒充关闭；Cargo/WGPU/RenderDoc 因 active cargo/rustc lanes 暂缓且不计通过。 | 后续继续处理 execution record/resource registry suppression pockets；补跑 context/resolver focused tests、plugin locked check 与 RenderDoc resource/marker 对拍。 |
| 2026-06-24 | RG-M1 RgResourceResolver physical test-surface suppression cleanup / F12 | render_plan01_rg_resource_resolver_physical_test_surface_static_passed_cargo_deferred_active_lanes | `RgResourceResolver` 的生产面现在只保留 compiled pass declaration/access gate；`with_physical(...)`、resolver 直取 texture/buffer 的 helper、`physical_resources(...)` 与 handle 级 require helper 收进 `#[cfg(test)]`，避免用 `cfg_attr(not(test), allow(dead_code))` 维持未被生产执行路径调用的物理直查 surface。生产 WGPU 物理资源访问仍由 `RenderPassExecutionContext` / `RenderPassGpuExecutionContext` 和 `gpu/resource_lookup.rs` 在 resolver 声明校验之后进入 `RenderGraphExecutionResources`。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\graph_execution\render_pass_execution_context\resource_resolver.rs` 通过；`resource_resolver.rs` 中 `allow(dead_code)` / `allow(unused_imports)` / `cfg_attr(not(test), allow(dead_code))` / `#![allow]` 扫描零命中；进程审计仍有 active cargo/rustc lanes，Cargo/WGPU/RenderDoc 暂缓且不计通过。 | 后续补跑 `rg_resource_resolver_requires_pass_declared_access_before_physical_texture_lookup` 与 broader RG-M1 resolver/materialization focused tests；继续 External actual binding 泛化、plugin locked check 解阻和 RenderDoc resource/marker 对拍。 |
| 2026-06-24 | RG-M1 advanced plugin resource/readback suppression cleanup / F12 | render_plan01_advanced_plugin_resource_readback_suppression_cleanup_static_passed_cargo_deferred_active_lanes | `SceneRendererAdvancedPluginReadbacks::is_empty(...)` 和 `SceneRendererAdvancedPluginResources::register_runtime_prepare_collector(...)` 只服务 runtime-prepare/readback focused tests,已收进 `#[cfg(test)]`;`hybrid_gi_enabled(...)` 与未被生产消费的 HGI capability 字段同样改为 test-only,保留 HGI descriptor capability source-contract,不再用 `#[allow(dead_code)]` 维持未接线生产 surface。 | `rustfmt --edition 2021 --check` 覆盖 touched advanced plugin resource/readback 文件通过;`advanced_plugin_readbacks/` 与 `advanced_plugin_resources/` 目录 `allow(dead_code)`/`allow(unused_imports)`/`cfg_attr(not(test), allow(dead_code))` 扫描零命中;Cargo/WGPU/RenderDoc 因当前 editor/plugin cargo/rustc lanes 活跃而暂缓,不计通过。 | 后续补跑 `runtime_prepare_collectors_*` 和 `advanced_plugin_readbacks_*` focused tests;继续 plugin package locked check 解阻、External 泛化校验与 RenderDoc resource/marker 对拍。 |
| 2026-06-23 | Plan 01 render graph execution record owner split | render_plan01_execution_record_owner_split_static_passed | `graphics/scene/scene_renderer/graph_execution/render_graph_execution_record.rs` 已从 1510 行拆为 550 行执行记录聚合 owner；新增 `graphics/scene/scene_renderer/graph_execution/render_graph_execution_record/compute_workload.rs` 承接 compute dispatch record、workload dispatch context、dispatch group sizing、compute workload audit record/status 与 compute audit tests；新增 `graphics/scene/scene_renderer/graph_execution/render_graph_execution_record/tests.rs` 承接非 compute 的 alias/resource/profile/stage/debug-marker 记录测试。新增 `runtime_15_render_graph_execution_record_is_folder_backed` 锁定 child module mounts、moved compute/test owner 不回流、docs/status 锚点与父子行数预算。 | scoped rustfmt/static/line-count/docs-anchor/diff-check 通过；locked core-min Cargo check 被当前 `Cargo.lock` 更新需求在编译前阻断，不计 Cargo 通过；不声明新的 WGPU/RenderDoc 通过。 | 继续补 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked`、focused structure guard rerun；之后回到 RG-M4 RenderDoc marker/profile 对拍与 RG-M3 focused cache tests。 |
| 2026-06-23 | RG-M3 compiled graph cache focused validation | render_plan01_compiled_cache_lookup_status_test_timeout_no_result: 源码合同测试已定位但本轮未形成测试结果 | 复核 `graphics/pipeline/compiled_graph_cache.rs` 中既有 `compiled_render_pipeline_cache_reports_lookup_status` 与 `render_graph_compile_frame_fingerprint_tracks_compile_extract_inputs` 等 RG-M3 源码合同测试;本切片未改生产代码,只尝试补跑计划 01 后续项中的 compiled graph cache focused lane。 | `cargo test -p zircon_runtime --lib compiled_render_pipeline_cache_reports_lookup_status --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-rg-cache-focused-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 300s 编译窗口内超时,无测试结果输出;事后进程审计未发现该 target-dir 相关 cargo/rustc/rustdoc/link 残留,`target\\codex-rg-cache-focused-0623\\debug\\deps` 未生成可直接执行的 `zircon_runtime*.exe`。 | 给足共享 lib-test 编译窗口后重跑 `compiled_render_pipeline_cache_reports_lookup_status`、`render_graph_compile_frame_fingerprint_tracks_compile_extract_inputs` 与二帧 cache-hit integration;本轮不把 RG-M3 focused tests 记为通过。 |
| 2026-06-23 | Render index 当前状态总览拆分 | 四个里程碑均部分完成;RG-M2 transient pool 预算/eviction、RG-M3 compiled graph cache key/invalidation、compile fingerprint/hit audit、root-driven pass culling、RG-M4 graph dump/capture artifact、alias map/profile timings、typed materialization validation、required/report-only External materialization coverage split、stale materialization binding lifetime validation、HZB executor-owned external buffer typed binding、required External binding contract、required External texture descriptor/validation 合同、`SHADOW_ATLAS` 首个生产 required External texture 绑定、optional External typed report-only descriptor/compile/materialization metadata、built-in history External actual binding、renderer-owned frame resource graph-lifetime-aware binding、first-party plugin buffer External fallback binding、runtime-prepare plugin buffer External real-binding handoff、Virtual Geometry prepared feedback producer、particles neutral GPU-frame buffer producer、particles real `ParticleGpuBackend` runtime-prepare owner、particles runtime-prepare ping-pong graph buffer aliases、particles runtime-prepare multi-system aggregation、particles transparent GPU draw shared-owner consumption、particles transparent offscreen visual readback、particles GPU readback count parity support、particles scene neutral GPU-frame auto-collection、`RgResourceResolver` pass-scoped physical lookup/postprocess validation 起步、GPU context resolver propagation、deferred-lighting/depth-prepass/deferred-G-buffer lookup 迁移与 `gpu/deferred.rs` bridge 拆分、mesh-stage/TAA reactive-mask mesh lookup 迁移、sprite/preview-sky/UI/overlay surface bridge lookup 迁移与 `gpu/surface.rs` 拆分、particle transparent/velocity bridge lookup 迁移、SSR-specific bridge lookup 迁移与 `light-list` descriptor read 声明、root postprocess bridge lookup 迁移与 `post_process/{effects,computed_resources,temporal,terminal}.rs` 拆分、HZB/shadow/velocity lookup cutover 与 raw lookup visibility 收紧,以及 `graph_resources.rs`/`materialization_validation.rs`/`resource_descriptors.rs`/`descriptor_filtering.rs`/`pass_authoring.rs`/`compile_tests.rs`/`transient_materialization.rs` 边界拆分、graph transient allocation descriptor bucket plan 与 execution bucket-local materialization 已接入 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 01 RenderGraph/RDG 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：进一步 raw lookup 结构隔离、particles GPU transparent draw 的产品/RenderDoc 验收；验收缺口：需要 transient pool/dump/culling/compiled_graph_cache/fingerprint/alias-profile/materialization/HZB external/history external/frame external/plugin external/required External/required texture/typed optional External/RgResourceResolver/descriptor filtering/pass-authoring/transient-materialization/transient-allocation-bucket focused tests、shadow atlas compiled graph focused tests、plugin package locked check 解阻、second-frame hit integration 补跑、External typed ownership 泛化校验、test-crate root-surface/import drift 解阻、RenderDoc marker/profile/resource 对拍;stale-binding texture/buffer focused filters 已在 2026-06-18 通过;particles runtime-prepare/CPU extract/shared-manager/GPU owner/ping-pong graph binding/multi-system aggregation/transparent shared-owner draw/offscreen visual readback/count parity/scene neutral GPU-frame auto-collection focused filters 已在 2026-06-18 通过 |
| 2026-06-15 | RG-M1 resource handles and lifetime model | 部分完成: graph contract 已服务后续渲染切片,统一生命周期 owner 未完全收束 | 计划 04/06/07 已依赖 graph resource names、external imports、execution-owned resources 与 WGPU executor contract 承载 HZB、TAA、exposure、output-transfer 等资源;但真实 wgpu resource 仍有不少 executor 自持路径。 | 当前计划 04/06/07 的 `## 状态与产出记录` 均记录了 graph resource/executor 接入;`render_graph_execution_resources` 相关 alias/materialization 测试在会话记录中已有通过证据。 | 继续把 executor 私有资源迁入 graph materialization,并为 attachment clear/load 与 resource lifetime 建统一校验。 |
| 2026-06-15 | RG-M2 transient resource pool | 部分完成: transient texture/buffer pool 已被 TAA/HZB/postprocess 使用 | TAA history、HZB chain、postprocess HDR/tonemapped/output-transfer、reactive mask、exposure 等切片已通过 graph resource 名称和 transient materialization 复用资源。 | 计划 06/07 状态表记录 `R8Unorm`/`R16Float`/`Rg16Float`/`Rg11b10Ufloat` transient usage 修正和 core-min `cargo check` 通过;会话记录包含 transient pool reuse/alias tests。 | 补齐池容量预算、跨帧 eviction 诊断和 graph dump 中的 transient alias 可视化。 |
| 2026-06-15 | RG-M3 pass culling and compile cache | 部分完成: pass culling 已在多处生效,跨帧编译缓存仍未完成 | Postprocess optional pass filtering、TAA disabled stack culling、HZB occlusion capability gate、contact shadow feature disable gate 已落地;编译缓存仍以当前 pipeline asset 编译路径为主。 | 计划 04、06、07、05 状态表分别记录 HZB gate、TAA reactive mask culling、postprocess optional pass filtering、contact-shadow disabled graph absent。 | 建立 `CompiledRenderPipeline` cache key、增量 invalidation 和 graph dump 对拍测试。 |
| 2026-06-15 | RG-M4 diagnostics and RenderDoc bridge | 部分完成: debug marker/diagnostics 可用,自动化捕获验收仍待后续 | Graph pass marker、runtime diagnostics 与 RenderDoc capture env hook 已被后续计划使用;但缺少统一 graph dump artifact 和自动附带 frame profile 的 capture 包。 | 计划 04/06/07 状态表均记录 RenderDoc 仍无运行实例或待后续验收;当前只完成源码/测试级证据。 | 为每帧输出 graph dump、resource alias map、pass timings 和 RenderDoc marker 对拍。 |
| 2026-06-17 | RG-M4 graph dump and capture artifact | 部分完成: 统一 graph dump 规划层产物与 capture 附带文本已接入,RenderDoc marker/profile 对拍仍待后续 | 新增 `render_graph/dump.rs` 与 `CompiledRenderGraph::dump()`,输出 pass 顺序、queue/fallback、culled 标记、executor、依赖、资源 IO、compute workload、资源声明/生命周期、transient slot/byte reservation 和文本序列化;`RenderPassId::index()` 作为稳定文本 id;`CapturedFrame` 新增 `graph_dump: Option<String>`,提交 capture 时从当前 `CompiledRenderPipeline.graph.dump().to_text()` 写入,查询 captured frame 即可拿到对应帧 RDG dump。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`git diff --check -- <graph dump scoped files>` 通过(仅 Git LF→CRLF 提示);`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-graph-dump-0617` 通过(既有 warning set);新增 `render_graph_dump_lists_pass_order_resources_and_culled` 源码合同测试,但过滤 `cargo test` 在 604s 共享 lib-test 编译窗口内超时,残留本 target-dir cargo 进程已清理。 | 补跑 dump focused test 与 graphics debugger capture 查询测试;继续补 alias map/profile timings、RenderDoc marker 对拍和 RG-M3 compile cache key/invalidation。 |
| 2026-06-17 | RG-M3 compiled graph cache key/invalidation | 部分完成: 编译结果缓存已接入 frame submission 热路径,稳态同 key 复用 `Arc<CompiledRenderPipeline>`;pass culling root 驱动仍待后续 | 新增 `graphics/pipeline/compiled_graph_cache.rs`;`RenderPipelineAsset` 增加 revision;`RenderFrameworkState.compiled_graph_cache` 接入提交路径;`compile_submission_pipeline` 改为 `get_or_compile`;register/reload pipeline 执行缓存失效;`RenderStats`/`DiagnosticStore` 暴露 `render.graph.compiled_cache.*`。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`git diff --check -- <RG-M3 scoped files>` 通过(仅 Git LF->CRLF 提示);`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-graph-cache-0617` 通过(既有 warning set);新增 compiled_graph_cache 源码合同测试,按用户要求未运行。 | 补跑 compiled_graph_cache focused tests 和 `render_graph_steady_state_second_frame_skips_recompile`;继续 RG-M3 root-driven culling 与热路径 descriptor 解析收束。 |
| 2026-06-17 | RG-M3 root-driven pass culling | 部分完成: root 驱动反向裁剪数据模型与判据已接入 RenderGraph,热路径 descriptor 解析和 focused test 执行仍待后续 | 新增 `RenderGraphResourceUsageFlags { present, readback, persistent }`;`RenderGraphResourceDeclaration`/`RenderGraphResourceLifetime` 保留 usage;`import_external_resource_with_usage` 支持非 present external;`mark_readback`/`mark_persistent` 支持 readback/history root;`cull_passes` 改为 present/readback/persistent/side-effect root 反向可达,删除 `writes_external`/`has_no_writes` 兜底存活;无 root 图返回 `MissingCullRoot`;graph dump 资源行输出 usage。新增 `render_graph/tests/culling.rs` 合同测试覆盖 present root、非 root external 裁剪、readback、persistent、side-effect 与 missing root。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`git diff --check -- <root culling scoped files>` 通过(仅 Git LF->CRLF 提示);冷 target-dir `cargo check` 曾在 604s 超时且无编译诊断,随后 `cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-graph-cache-0617` 通过(既有 warning set);focused culling tests 按用户实现优先要求未运行。 | 补跑 `render_graph_culls_passes_unreachable_from_present_root`、`render_graph_readback_marked_buffer_keeps_producer_alive`、`render_graph_side_effect_pass_survives_culling`、`render_graph_missing_cull_root_is_compile_error`;继续 RG-M3 热路径 descriptor 解析收束与二帧 cache hit 集成验证。 |
| 2026-06-17 | RG-M3 compile fingerprint/hit audit | 部分完成: 热路径编译输入指纹和 cache hit 审计已接入,focused tests 与二帧集成验收仍待后续 | `extract_compile_fingerprint(...)` 显式记录 `compile_with_options(...)` 读取的 frame 输入面(core pipeline、view/render size、HDR、MSAA、particle sprite pass presence);`CompiledGraphCacheLookupStatus` 与 `get_or_compile_with_status(...)` 暴露单次 lookup hit/miss;提交路径 cache hit 会在 debug build 重新计算 live fingerprint 并与 key frame fingerprint 对拍,避免未来新增 descriptor 输入时静默复用陈旧 graph。新增源码合同测试覆盖 lookup status 与 fingerprint 输入面。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-compile-fingerprint-0617` 通过(既有 warning set;第一次检查暴露的新 dead_code wrapper/unused ctor 已删除并复跑通过);focused tests 按用户实现优先要求未运行。 | 补跑 `compiled_render_pipeline_cache_reports_lookup_status`、`render_graph_compile_frame_fingerprint_tracks_compile_extract_inputs` 与 `render_graph_steady_state_second_frame_skips_recompile`;继续 RG-M4 alias map/profile timings 与 transient pool 预算/eviction。 |
| 2026-06-17 | RG-M2 transient pool budget/eviction | 部分完成: 执行侧 WGPU transient pool 已有显式 byte budget 与预算淘汰诊断,focused tests/RenderDoc 验收仍待后续 | `TransientResourcePool` 增加默认 texture/buffer byte budget,归还条目记录 `TextureDesc::checked_storage_size_bytes()` / `BufferDesc.size_bytes`,帧末先按 `TRANSIENT_RESOURCE_POOL_KEEP_FRAMES` 清 stale,再按 LRU 淘汰至预算内;`RenderGraphTransientPoolReport` 增 retained bytes、budget bytes、budget eviction counts;`DiagnosticStore` 新增 `render.graph.execution.transient_pool.{texture,buffer}_pool_retained_bytes`、`{texture,buffer}_pool_budget_bytes` 与 budget eviction count 行;源码合同测试新增 `transient_resource_pool_evicts_oldest_entries_to_budget`。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-transient-pool-budget-0617` 首次通过(573.2s,既有 warning set;顺手移除 `graphics::pipeline::mod.rs` 的 unused re-export 后复跑通过 44.3s);`git diff --check -- <RG-M2 scoped files>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中。按用户“测试可以暂时延后”要求,新增 focused test 尚未运行。 | 补跑 transient pool focused tests、runtime diagnostics focused test、产品场景 transient retention 对拍和 RenderDoc memory/marker 检查;继续 graph materialization 与 RG-M4 alias map/profile timings。 |
| 2026-06-17 | RG-M4 alias map/profile timings | 部分完成: 运行时资源 alias map 与 CPU pass profile 已接入 stats/diagnostics,focused tests/RenderDoc 对拍仍待后续 | 新增 `RenderGraphExecutionAliasRecord`/`RenderGraphExecutionAliasReport`、`RenderGraphPassProfileRecord`/`RenderGraphExecutionProfileReport`;`RenderGraphExecutionResources::resource_alias_report()` 在 transient release 前快照 owned-backed logical->physical backing 关系;`execute_graph_stage(...)` 围绕 executor 调用记录 CPU microsecond span;`RenderStats` 暴露完整 alias/profile rows;`DiagnosticStore` 新增 `render.graph.execution.alias.*` 与 `render.graph.execution.profile.*` 低基数行;runtime diagnostics fixture/断言已覆盖新行。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-alias-profile-0617` 首次冷 target-dir 124s 超时,暖 target-dir 复跑 301.3s 通过(既有 warning set);`git diff --check -- <RG-M4 alias/profile scoped files>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中。按用户“测试可以暂时延后”要求,focused tests 尚未运行。 | 补跑 runtime diagnostics focused test、alias/profile execution-record focused tests、RenderDoc marker/profile 对拍;继续 graph materialization 与 resource lifetime 校验收束。 |
| 2026-06-17 | RG-M1 typed materialization validation | 部分完成: live typed texture/buffer lifetimes 已在 WGPU pass 执行前硬校验,External lifetimes 先进入 report/diagnostics | 新增 `RenderGraphMaterializationReport`;`RenderGraphExecutionResources::validate_materialized_graph_resources(...)` 遍历 `CompiledRenderGraph::resource_lifetimes()` 并要求 live dense texture 具备 texture view、live buffer 具备 buffer,sparse texture reservation 只计数不要求 dense backing;`render_compiled_scene(...)` 在 executor 运行前调用校验并将 typed 缺失映射为 `GraphicsError::Asset`;`RenderGraphExecutionRecord`、`RenderStats.last_graph_materialization_report` 与 `DiagnosticStore` 新增 `render.graph.materialization.*` 计数;源码合同测试覆盖 unbound typed lifetimes 与 report-only external lifetimes。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-materialization-0617` 首次冷 target-dir 244s 超时,暖 target-dir 复跑 188s 通过(既有 warning set);`git diff --check -- <materialization scoped files>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中。按用户“测试可以暂时延后”要求,focused tests 尚未运行。 | 补跑 materialization/runtime-diagnostics focused tests;继续把 HZB occlusion 等 executor-owned external buffer 迁入 typed graph ownership,再收紧 External 缺失为硬错误并做 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 HZB executor-owned external typed binding | 部分完成: HZB occlusion executor-owned external buffer 已在 materialization validation 前绑定到执行资源表,External 泛化硬错误仍待后续 | 新增 `bind_execution_owned_graph_resources.rs`;`render_compiled_scene(...)` 在 transient materialization 后调用它,把 live HZB external 名称绑定到 phase-local indirect args、compaction metadata、compacted args、visible-instance remap、draw-count 与 stats buffers;零候选/无 culler 场景创建最小 bindable fallback buffers;`RenderGraphExecutionResources::bind_execution_owned_buffer(...)` 记录 logical->backing buffer alias;HZB culler 暴露 stats buffer 与 external resource 常量。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;`cargo check -q -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-external-materialization-0617` 首次冷 target-dir 364s 超时且无 Rust 诊断,暖 target-dir 复跑 73.5s 通过(既有 warning set);scoped `git diff --check` 通过(仅 Git LF->CRLF 提示),冲突标记扫描无命中;新增 `hzb_external_fallback_buffers_satisfy_materialization_report` 源码合同测试,按用户“测试可以暂时延后”要求未运行。 | 补跑 HZB external/materialization focused test;为多 phase graph 增加 typed external resource model;继续非 HZB External ownership 泛化、External 缺失硬错误策略、resolver 硬切换和 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 required External binding contract | 部分完成: External descriptor 已携带类型/必需性,HZB external buffers 已进入 required-buffer 硬错误路径,非 HZB 泛化仍待后续 | 新增 `RenderGraphExternalResourceBinding`、`RenderGraphExternalResourceType`、`RenderGraphExternalResourceRequirement`;`RenderGraphBuilder::import_external_resource_with_binding(...)` 与 feature descriptor 的 `read_required_external_buffer(...)`/`write_required_external_buffer(...)` 将 required buffer 语义传到 compiled graph lifetime;pipeline compile 汇聚 external binding 并拒绝同名 external 的类型冲突;HZB descriptor 将 source/compaction/replay/draw-count/stats external 资源声明为 required buffer;materialization validation 对缺失 required external buffer 直接报错,report-only frame target/history external 继续只计数。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-external-binding-contract-0617 --message-format short --color never` 通过(既有 warning set);新增/更新源码合同测试 `materialization_validation_fails_unbound_required_external_buffer`、`compile_describes_hzb_as_half_power_of_two_mip_chain` 的 required binding 断言、`hzb_occlusion_cull_declares_execution_owned_external_buffers` 的 required binding 断言,按用户“测试可以暂时延后”要求未运行。 | 补跑 required External/HZB descriptor focused tests;泛化非 HZB executor-owned/optional External typed ownership 和 required texture import;把 `compile.rs` external resource plan 与 `render_graph_execution_resources.rs` materialization validation 从超大文件拆出;继续 resolver 硬切换、External resource/marker RenderDoc 对拍。 |
| 2026-06-17 | RG-M1 required External texture and materialization modularization | 部分完成: required External texture 已有 descriptor/compile/materialization 合同,Plan 01 点名的两个超大文件边界已拆出首批 owner;实际 required texture 生产绑定与非 HZB optional External 泛化仍待后续 | `RenderFeaturePassDescriptor` 新增 required external texture 读、attachment 写与 storage 写 helper;`render_pipeline_asset/graph_resources.rs` 接管 SRP descriptor resource plan、external binding merge 与同名 texture/buffer 冲突拒绝;`graph_execution/materialization_validation.rs` 接管 materialization completeness audit,`RenderGraphExecutionResources` 保留原调用面但不再承载 validation 状态机;新增/更新文档 `docs/zircon_runtime/graphics/pipeline/render_pipeline_asset/graph_resources.md`、render_graph builder 与 graph execution resources 文档。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-required-external-texture-0617 --message-format short --color never` 通过(既有 warning set);新增源码合同测试 `materialization_validation_fails_unbound_required_external_texture`、`compile_preserves_required_external_texture_binding`、`compile_rejects_conflicting_required_external_texture_and_buffer_binding`,按实现优先策略未运行 focused tests。 | 补跑 required texture/materialization/pipeline compile focused tests;继续非 HZB executor-owned/optional External typed ownership、实际 required texture 生产绑定、resolver 硬切换、剩余 `compile.rs` 路由/descriptor sizing 与 `render_graph_execution_resources.rs` transient materialization 边界拆分,以及 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `SHADOW_ATLAS` required External texture production binding | 部分完成: 首个生产 required External texture 已接入 shadow atlas,非 HZB/非 shadow atlas External 泛化仍待后续 | `shadows.rs` 将 `shadow-atlas` pass 从 report-only external write 升级为 `write_required_external_texture_with_ops(SHADOW_ATLAS, clear_store)`;forward mesh、deferred transparent mesh、deferred lighting 均改为 `read_required_external_texture(SHADOW_ATLAS)`;`shadow_atlas_required_external_tests.rs` 从超大 `compile.rs` 拆出默认 Forward+/Deferred compiled graph 合同,断言 `SHADOW_ATLAS` lifetime 是 `RenderGraphExternalResourceBinding::required_texture()` 且 writer/readers 保留 external IO;相关 render graph、pipeline graph resources、graph execution resources、shadow 文档同步到生产绑定事实。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;`git diff --check -- <SHADOW_ATLAS required texture scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-required-external-texture-0617 --message-format short --color never` 通过(74 个既有 warning)。新增/更新 descriptor 与 compiled graph focused tests 已写入,本实现切片未运行 lib-test。 | 补跑 `compile_forward_plus_preserves_shadow_atlas_required_external_texture_binding`、`compile_deferred_preserves_shadow_atlas_required_external_texture_binding` 与 descriptor focused tests;继续非 HZB/非 shadow atlas executor-owned 与 optional External typed ownership、resolver 硬切换、剩余 `compile.rs` 路由/descriptor sizing 与 transient materialization 边界拆分,以及 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 optional External typed report-only ownership metadata | 部分完成: optional External 已有 texture/buffer descriptor、compiled lifetime 与 materialization report 类型元数据;非 HZB/非 shadow atlas executor-owned 实际绑定泛化仍待后续 | `RenderGraphExternalResourceBinding` 新增 `report_only_texture()` / `report_only_buffer()`;`RenderFeaturePassDescriptor` 新增 typed optional external read/write/storage/attachment-ops helpers;内建 frame target、history、postprocess、SSAO、bloom、TAA、UI、AA、debug-overlay、deferred-geometry/deferred-lighting descriptor 迁到 typed optional helper;particles、Hybrid GI、Virtual Geometry first-party plugin descriptor 迁到 typed optional buffer/texture helper;新增 `typed_optional_external_tests.rs`,并扩展 materialization validation 覆盖 missing typed optional external 只报告不失败。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;生产 descriptor 扫描确认 `graphics/feature`、`zircon_plugins`、runtime feature fixture 无遗留 untyped optional external helper;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-typed-optional-external-0617 --message-format short --color never` 通过(既有 warning set,约 6m18s);package-scoped plugin check `cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_particles_runtime -p zircon_plugin_hybrid_gi_runtime -p zircon_plugin_virtual_geometry_runtime --locked ...` 在编译前被 `zircon_plugins/Cargo.lock` 需更新阻塞,未改 lockfile。 | 补跑 typed optional External focused tests 与 plugin package check(先处理 plugin lockfile);继续非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resolver 硬切换、剩余 `compile.rs` 路由/descriptor sizing 与 `render_graph_execution_resources.rs` transient materialization 边界拆分,以及 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` pass-scoped physical lookup cutover | 部分完成: resolver name/API 与 postprocess validation/input selection 已经使用 compiled pass access;广域 executor 直接资源 lookup 迁移仍待后续 | `RgResourceResolver` 增加 pass 声明访问检查与 physical texture/buffer lookup helper;`RenderPassExecutionContext::require_texture_view_by_name(...)` 在访问 WGPU resource table 前校验当前 pass 是否声明了对应 resource/access;product postprocess required resource validation、output-transfer/bloom/uber 输入选择从 stale `context.resources` 扫描迁到 resolver-backed name access。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 74 warning set);新增源码合同测试 `rg_resource_resolver_requires_pass_declared_access_before_physical_texture_lookup` 与 `resolver_backed_name_access_ignores_stale_context_resource_rows`,按实现优先策略未运行 lib-test。 | 继续 broad GPU executor lookup migration、非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、剩余 `compile.rs` 路由/descriptor sizing 与 `render_graph_execution_resources.rs` transient materialization 边界拆分,以及 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` GPU context propagation | 部分完成: `RenderPassGpuExecutionContext` 已携带 pass resolver, deferred lighting texture/buffer lookup 已迁到 compiled pass access gate;剩余 GPU executor lookup 仍待迁移 | `RenderPassExecutionContext::with_gpu(...)` 把 pass-scoped `RgResourceResolver` 传播进 GPU context;`RenderPassGpuExecutionContext` 的 resolver-backed texture/buffer helper 拆到 `gpu/resource_lookup.rs`;`record_deferred_lighting_to_resources(...)` 的 G-buffer、scene-depth、background、scene-color 与 light-grid params/zbins/tile-mask lookup 全部先校验当前 pass 的 Read/Write 声明再访问 WGPU resource table。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`rustfmt --edition 2021 --check` 覆盖 touched Rust files;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 74 warning set);`git diff --check -- <resolver GPU propagation scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记与旧 resolver 名扫描无命中。 | 继续把 depth/shadow/mesh/sprite/overlay/postprocess/SSR 等剩余 GPU direct lookup 迁到 resolver-backed path,再收紧 `RenderGraphExecutionResources` 直接 lookup 可见性;补跑 focused resolver tests 与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` deferred scene bridge lookup migration | 部分完成: depth-prepass 与 deferred G-buffer/lighting 主要场景 bridge lookup 已迁到 compiled pass access gate;其他 GPU executor lookup 仍待迁移 | 新增 `render_pass_execution_context/gpu/deferred.rs` 承载 deferred G-buffer/lighting bridge;depth-prepass 的 `gbuffer-normal`/`scene-depth` 写 lookup、deferred G-buffer 的 `gbuffer-albedo`/`gbuffer-material` 写与 `scene-depth` 读 lookup、deferred lighting lookup 均经 `gpu/resource_lookup.rs` resolver-backed helpers;`gpu.rs` 为 810 行,保持在 modularization 阈值内。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 74 warning set);focused lib-test 按实现优先策略未运行。 | 继续 mesh/sprite/particle/UI/overlay/postprocess/SSR 等剩余 GPU direct lookup 迁移,再收紧 `RenderGraphExecutionResources` 直接 lookup 可见性;补跑 focused resolver tests 与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` mesh bridge lookup migration | 部分完成: mesh-stage 与 TAA reactive-mask mesh bridge lookup 已迁到 compiled pass access gate;其他 sprite/particle/UI/overlay/postprocess/SSR lookup 仍待迁移 | `record_mesh_stage_to_resources(...)` 的 `scene-color` 写 attachment、`scene-depth` 读 attachment 和 Forward mesh light-grid buffer 读经 resolver-backed helpers;新增 `optional_buffer_by_name(...)` 支持 Forward mesh 声明必需 light-grid buffer 而 Deferred transparent mesh 不声明 light-grid 的共享 executor 形态;`record_taa_reactive_mask_mesh_to_resource(...)` 的 reactive-mask 写与 scene-depth 读也迁到 resolver-backed lookup。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 142 warning set);focused lib-test 按实现优先策略未运行。 | 继续 sprite/particle/UI/overlay/postprocess/SSR direct lookup 迁移,再收紧 `RenderGraphExecutionResources` 直接 lookup 可见性;补跑 focused resolver tests 与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` scene surface bridge lookup migration | 部分完成: sprite/preview-sky/UI/overlay surface bridge lookup 已迁到 compiled pass access gate;particle/postprocess/SSR lookup 仍待迁移 | 新增 `render_pass_execution_context/gpu/surface.rs` 承载 sprite、screen-space UI、preview sky、overlay bridge;surface bridge 的 sprite `scene-color`/`scene-depth` 写 lookup、preview sky color/depth 写 lookup、UI `viewport-output` 写 lookup、overlay `viewport-output` 写与 `scene-depth` 读 lookup 均经 resolver-backed helpers;`gpu.rs` 为 730 行,`gpu/surface.rs` 为 158 行,`gpu/resource_lookup.rs` 为 70 行。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;direct lookup 扫描确认 root `gpu.rs` 无 surface direct lookup 残留,剩余 direct lookup 集中在 `gpu/particle.rs`、`gpu/post_process.rs` 与 `gpu/post_process/screen_space_reflection.rs`;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 当前被无关 dirty root-surface 状态阻塞: `zircon_runtime/src/lib.rs` 缺少 crate-root `RuntimePluginId`/`RuntimeTargetMode` re-export,而 plugin/platform 模块仍从 `crate::` 导入这些类型;未报告本切片 render 代码诊断。 | 解阻 root re-export/import 状态后补跑 core-min check;继续 particle/postprocess/SSR direct lookup 迁移,再收紧 `RenderGraphExecutionResources` 直接 lookup 可见性;补跑 focused resolver tests 与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` particle bridge lookup migration | 部分完成: particle transparent/velocity bridge lookup 已迁到 compiled pass access gate;postprocess/SSR lookup 仍待迁移 | `record_particle_billboards_to_resources(...)` 的 `scene-color` 写与 `scene-depth` 读 lookup、`record_particle_velocity_to_resource(...)` 的 `scene-velocity` 写与 `scene-depth` 读 lookup 均经 resolver-backed helpers;移除 `zircon_plugins/particles/runtime/src/render/executors.rs` 中 particle transparent executor 对 `gpu.resources.require_texture_view(...)` 的重复直取预检;当前行数为 `gpu.rs` 685、`gpu/particle.rs` 76、`gpu/resource_lookup.rs` 65、particles executor 292。 | `rustfmt --edition 2021 --check` 覆盖 particle runtime/plugin 文件;direct lookup 扫描确认 particle runtime/plugin 路径只剩 resolver helper 调用,广域扫描显示剩余 direct lookup 集中在 `gpu/post_process.rs` 与 `gpu/post_process/screen_space_reflection.rs`;冲突标记/旧 resolver 名/尾随空白扫描无命中;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 142 warning set);`cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugins-rg-resolver-particle-0617 --message-format short --color never` 在编译前被 `zircon_plugins/Cargo.lock` 需更新阻塞,未改 lockfile。 | 继续 postprocess/SSR direct lookup 迁移,再收紧 `RenderGraphExecutionResources` 直接 lookup 可见性;补跑 focused resolver tests、plugin package check(先处理 plugin lockfile)与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` SSR bridge lookup migration | 部分完成: SSR-specific GPU bridge lookup 已迁到 compiled pass access gate;root postprocess bridge lookup 仍待迁移 | `gpu/post_process/screen_space_reflection.rs` 的 SSR resolve/reflection-pyramid/coarse-pyramid/specular-occlusion graph inputs/outputs 与 mip alias target lookup 均经 resolver-backed helpers;新增 optional texture/mip helper 让 shared postprocess fallback bind slots 在 resolver 存在时先检查 pass 声明;`PostProcessGraphResourceNames::LIGHT_LIST` 作为 SSR external buffer read 写入 stack initial resources 与四个 SSR descriptor pass;当前行数为 `screen_space_reflection.rs` 678、`gpu/resource_lookup.rs` 116、post_process descriptor 284、post_process stack 1581。 | `rustfmt --edition 2021 --check` 覆盖 SSR/resource/descriptor/stack 文件;direct lookup 扫描确认 `gpu/post_process/screen_space_reflection.rs` 无 `resources.require_*` / `owned_texture_mip_view` 直取残留,广域扫描剩余 direct lookup 集中在 `gpu/post_process.rs` 与 helper fallback/internal calls;冲突标记/旧 resolver 名/尾随空白扫描无命中;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 142 warning set)。 | 继续 `gpu/post_process.rs` direct lookup 迁移,再收紧 `RenderGraphExecutionResources` 直接 lookup 可见性;补跑 focused resolver/SSR descriptor tests、plugin package check(先处理 plugin lockfile)与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` root postprocess bridge lookup migration | 部分完成: root postprocess GPU bridge lookup 已迁到 compiled pass access gate;直接 lookup 只剩 shared helper fallback/internal 与 HZB occlusion legacy path | `gpu/post_process.rs` 的 stack/color-LUT/root effect selection、exposure/SSAO/clustered/HZB/bloom/DoF/motion-vector/terminal AA/upscale/output-transfer lookup 均经 `gpu/resource_lookup.rs` helpers;新增 `post_process/effects.rs`、`computed_resources.rs`、`temporal.rs`、`terminal.rs`,root `post_process.rs` 降到 490 行;SSR mip-count metadata 也改经 resolver-gated helper。 | `rustfmt --edition 2021 --check` 覆盖 postprocess/resource helper 文件;postprocess subtree direct lookup 扫描无 `resources.require_*`/`buffer`/`owned_texture`/`texture_view`/`owned_texture_mip_level_count` 直取命中;广域 GPU direct lookup 扫描只剩 `gpu/resource_lookup.rs` helper fallback/internal calls 与既有 `gpu/hzb_occlusion.rs` legacy path;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617 --message-format short --color never` 通过(既有 142 warning set)。 | 继续收紧 `RenderGraphExecutionResources` 直接 lookup 可见性、处理 `gpu/hzb_occlusion.rs` legacy lookup、非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、focused resolver tests、plugin package check(先处理 plugin lockfile)与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `RgResourceResolver` HZB/shadow/velocity lookup and resource-table visibility cutoff | 部分完成: 剩余 GPU executor direct lookup 已清到 shared helper fallback/internal calls;`RenderGraphExecutionResources` raw texture/buffer lookup 从 public 收到 graph-execution 范围,非 HZB/非 shadow atlas External 泛化仍待后续 | `gpu/hzb_occlusion.rs` 的 previous HZB history read 改用 declared-optional resolver helper,保留 first-frame fallback texture 但要求 compiled pass 声明 Read;`record_shadow_atlas_to_resources(...)` 的 `SHADOW_ATLAS` write 与 `temporal/velocity/execute_velocity_object.rs` 的 `scene-velocity` write / `scene-depth` read 均经 resolver-backed helper;`RenderGraphExecutionResources::{texture_view,buffer,require_texture_view,require_texture_view_for_declaration,require_buffer,require_buffer_for_declaration}` 收窄到 `graph_execution`;`RenderPassGpuExecutionContext::resource_resolver()` 暴露窄 accessor 支持 scene_renderer 外侧 velocity impl。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;direct lookup 扫描确认 `render_pass_execution_context/gpu.rs`、`gpu/**` 与 `temporal/velocity` 的旧直取只剩 `gpu/resource_lookup.rs` helper fallback/internal calls;public raw lookup 扫描确认 `texture_view`/`buffer`/`require_*` 不再是 public method;首次共享 target-dir `cargo check` 因 fingerprint output 写入/解析失败停止且无 Rust 诊断,独立 target-dir 冷编译先 124s 超时,暖 rerun `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resolver-cutover-0617-hzb-shadow-velocity --message-format short --color never` 通过(既有 142 warning set)。 | 补跑 resolver/HZB/velocity focused tests;继续非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、剩余 `render_graph_execution_resources.rs` transient materialization 结构拆分、plugin package check(先处理 plugin lockfile)与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `compile.rs` transient descriptor sizing boundary split | 部分完成: `compile.rs` 的 texture/buffer descriptor sizing 已移入 sibling owner,但 descriptor routing/pass-authoring 仍在超大 compile 文件内 | 新增 `render_pipeline_asset/resource_descriptors.rs`,接管 `texture_desc_for(...)`、`buffer_desc_for(...)`、post-process format/size/mip-level 选择、Color LUT 3D descriptor、HZB/SSR half-resolution mip-chain sizing、light-grid/exposure buffer sizing与 scene-color HDR fallback;`compile.rs` 只通过 `resource_descriptors::{texture_desc_for, buffer_desc_for}` 调用 descriptor policy,行数从 1797 降到 1606;新增 `docs/zircon_runtime/graphics/pipeline/render_pipeline_asset/resource_descriptors.md`,并同步 graph resources/render graph 文档 related-code header。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`git diff --check -- <resource descriptor scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-resource-descriptors-0617 --message-format short --color never` 通过(冷 target-dir 约 5m59s,既有 142 warning set)。现有 compile source-contract tests 覆盖 HZB/SSR mip-chain 与 Color LUT 3D descriptor 策略,按实现优先策略未运行 lib-test。 | 补跑 HZB/SSR/Color LUT focused compile tests;继续拆分 `compile.rs` descriptor filtering/routing 与 pass-authoring loop、`render_graph_execution_resources.rs` transient materialization 结构,并继续非 HZB/非 shadow atlas executor-owned External 泛化、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `compile.rs` descriptor filtering and compile-test boundary split | 部分完成: `compile.rs` 已降到 modularization 阈值以下,descriptor filtering/routing 与 compile source-contract tests 已拆到 sibling owner;pass-authoring loop 仍待后续 | 新增 `render_pipeline_asset/descriptor_filtering.rs` 接管 feature descriptor 过滤、post-process stack optional pass/resource 裁剪、bloom/scene-composite/blur/uber/output-transfer/terminal AA 输入路由;新增 `render_pipeline_asset/compile_tests.rs` 承载原 inline compile contract tests;`compile.rs` 保留 compile orchestration、frame-extract-dependent core scene particle descriptor insertion、feature descriptor validation与 RenderGraph pass authoring。行数为 `compile.rs` 568、`descriptor_filtering.rs` 507、`compile_tests.rs` 534。新增 `docs/zircon_runtime/graphics/pipeline/render_pipeline_asset/descriptor_filtering.md`,并同步 graph resources/resource descriptors 文档。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;`git diff --check -- <descriptor filtering scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-descriptor-filtering-0617 --message-format short --color never` 通过(9m27s,既有 142 warning set)。focused compile lib-tests 迁移到 `compile_tests.rs` 但仍按实现优先策略未运行。 | 补跑 `compile_routes_bloom_extract_after_split_scene_color_passes`、`compile_routes_output_transfer_through_fxaa_terminal_input`、`compile_routes_output_transfer_through_smaa_terminal_input`、HZB/SSR/Color LUT compile focused tests;继续拆分 `compile.rs` pass-authoring loop、`render_graph_execution_resources.rs` transient materialization 结构,并继续非 HZB/非 shadow atlas executor-owned External 泛化、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍。 |
| 2026-06-17 | RG-M1 `compile.rs` pass-authoring boundary split | 部分完成: `compile.rs` pass-authoring loop 已移入 sibling owner,剩余 RG-M1 工作转回 External 泛化与执行侧 materialization 结构 | 新增 `render_pipeline_asset/pass_authoring.rs`,接管 `RenderGraphBuilder` 创建、resource plan handle allocation、stage/pass authoring、resolved/declared queue 写入、pass flags、compute workload、texture/buffer/external read-write declaration mapping、serial pass dependency preservation与 `CompiledRenderPipelinePassStage` 输出;`compile.rs` 保留 compile orchestration、frame-extract-dependent core particle descriptor insertion、feature descriptor validation、extract/capability/history metadata collection。行数为 `compile.rs` 362、`pass_authoring.rs` 276、`descriptor_filtering.rs` 507、`resource_descriptors.rs` 197、`compile_tests.rs` 534。新增 `docs/zircon_runtime/graphics/pipeline/render_pipeline_asset/pass_authoring.md`,并同步 graph resources/resource descriptor/filtering docs。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;`git diff --check -- <pass-authoring scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-pass-authoring-0617 --message-format short --color never` 通过(6m58s,既有 142 warning set)。focused compile lib-tests 仍按实现优先策略未运行。 | 补跑 compile focused tests(stage preservation、compute workload、postprocess routing、HZB/SSR/Color LUT);继续非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、`render_graph_execution_resources.rs` transient materialization 结构拆分与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 execution transient materialization boundary split | 部分完成: 执行侧 transient slot grouping/allocation 已移入 sibling owner,剩余 RG-M1 工作回到 External 泛化、lifetime 闭环与进一步 lookup 隔离 | 新增 `graph_execution/transient_materialization.rs`,接管 transient texture/buffer slot grouping、WGPU backing compatibility 判断、pool/fresh acquire 选择、logical-to-backing bind、preimported terminal AA transient override 与 SSR mip alias policy;`materialization.rs` 保留 materialization entry、SSR alias attachment、WGPU descriptor conversion/create helpers和既有 source-contract tests;`RenderGraphExecutionResources` 继续只承载 logical/physical resource table、reports 与 accessors。行数为 `materialization.rs` 648、`transient_materialization.rs` 274、`render_graph_execution_resources.rs` 513。新增 `docs/zircon_runtime/graphics/scene/scene_renderer/graph_execution/transient_materialization.md`,并同步 graph execution resource docs。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;`git diff --check -- <transient materialization scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中;D: 剩余空间 140710256640 bytes;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-transient-materialization-0618 --message-format short --color never` 通过(6m47s,既有 142 warning set)。focused materialization lib-tests 仍按实现优先策略未运行。 | 补跑 materialization focused tests;继续非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、进一步 raw lookup 结构隔离与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 transient allocation descriptor bucket plan | 部分完成: graph transient allocation plan 已按描述符桶分配 bucket-local slot,执行侧 incompatible fallback 不再是常规合同 | `CompiledRenderGraphTransientAllocation` 与 `CompiledRenderGraphTransientSlotReservation` 增 `bucket_key_hash`;`transient_allocation_plan()` 先按 texture descriptor(width/height/depth/mip/sample/format/dimension/residency/usage) 与 buffer descriptor(size/usage) 分桶,再对每个桶做生命周期区间着色;`texture_slot_count`/`buffer_slot_count` 统计 bucket-local slot reservations;新增 `slot_bytes_for_bucket(...)`,保留 `slot_bytes(...)` 作为跨 bucket 聚合兼容面;graph dump transient slot 行输出 bucket hash,resource row 使用 bucket-local reserved bytes;`graph_transient_allocation_plan_reports_slot_reserved_bytes` 断言不同尺寸 texture/buffer 进入不同 bucket,并同步 materialization incompatible-texture 合同测试名。 | `rustfmt --edition 2021` 覆盖 graph/dump/materialization scoped Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-transient-allocation-buckets-0618 --message-format short --color never` 冷 target-dir 7m58s 通过,修正 slot count 后暖 rerun 34.02s 通过(既有 142 warning set)。最终 `rustfmt --edition 2021 --check`、scoped `git diff --check`、冲突标记扫描和最终 cargo check 结果记录在会话 note。focused render_graph/materialization lib-tests 仍按实现优先策略未运行。 | 补跑 `graph_transient_allocation_plan_reports_slot_reserved_bytes`、graph dump text test 与 materialization focused tests;继续非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、进一步 raw lookup 结构隔离与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 execution bucket-local transient materialization | 部分完成: 执行侧 WGPU materialization 已按 descriptor bucket + bucket-local slot 建物理 backing,剩余 RG-M1 工作回到 External 泛化与 lifetime 闭环 | `transient_materialization.rs` 的 texture/buffer grouping 从单独 `slot` 改为 `(bucket_key_hash, slot)`;物理 backing label 改为 `rg-transient-{texture|buffer}-bucket-<hash>-slot-<slot>`,避免两个 descriptor bucket 的 slot `0` 在 WGPU resource table / alias report 中被误合并;materialization source-contract tests 断言同 bucket 兼容 texture/buffer 共享 bucketed label、不同 texture descriptor bucket 保持不同 backing label;runtime diagnostics fixture 改用 bucketed alias rows;同步 transient materialization、graph execution resources 与 render graph builder 文档。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件;旧 `rg-transient-{texture|buffer}-slot-*` label 扫描无命中;`git diff --check -- <bucket materialization scoped files/docs>` 通过(仅 Git LF->CRLF 提示);冲突标记与尾随空白扫描无命中;D: 剩余空间 141339967488 bytes;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-transient-bucket-materialization-0618 --message-format short --color never` 通过(7m38s,既有 142 warning set)。focused materialization lib-tests 仍按实现优先策略未运行。 | 补跑 materialization focused tests;继续非 HZB/非 shadow atlas executor-owned External 实际绑定泛化、resource lifetime 验证闭环、进一步 raw lookup 结构隔离与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 built-in history External actual binding | 部分完成: TAA/SSR/HZB/exposure history actual binding 已从 render path inline 逻辑收敛到 graph-lifetime-aware binder,plugin-owned External 泛化仍待后续 | 新增 `render/bind_history_graph_resources.rs`;`render_compiled_scene(...)` 用 `bind_history_graph_resources(...)` 取代内联 history import block;binder 接收 compiled graph、`SceneFrameHistoryTextures` 和 `HistoryGraphResourceBindingFlags`,只在对应 `resource_lifetime_by_name(...)` 存在时绑定 `TAA_HISTORY_PREVIOUS`/`TAA_HISTORY_CURRENT`、`HISTORY_PREVIOUS_SCREEN_SPACE_REFLECTION`、`HISTORY_PREVIOUS_HZB_FURTHEST`、`EXPOSURE_PREVIOUS`/`EXPOSURE_CURRENT`;新增 source-contract tests 覆盖启用且 live 的 history external 通过 materialization validation,以及启用但不在 live graph 的 history external 不被预绑定。新增 `docs/zircon_runtime/graphics/scene/scene_renderer/render_path/history_graph_resources.md`,并同步 graph execution/render graph docs 与 render index。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-history-binding-0618 --message-format short --color never` 通过(6m48s,既有 142 warning set)。最终 `rustfmt --edition 2021 --check` 通过;scoped `git diff --check` 通过(仅 Git LF->CRLF 提示);冲突标记/尾随空白/旧 transient slot label 扫描无命中。focused history/materialization lib-tests 仍按实现优先策略未运行。 | 补跑 history external/materialization focused tests;继续非 HZB/非 shadow atlas/非 built-in history executor-owned External 实际绑定泛化、resource lifetime 验证闭环、plugin package locked check 解阻与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 renderer-owned frame External actual binding | 部分完成: 固定 frame target/final alias/light-list/shadow-atlas actual binding 已从 unconditional import 收敛到 compiled-graph-lifetime-aware binder,plugin-owned/non-frame External 泛化仍待后续 | 新增 `render/bind_frame_graph_resources.rs`;`render_compiled_scene(...)` 在 transient materialization 前调用 `bind_frame_graph_resources(...)`,只为 live compiled lifetimes 绑定 `SCENE_COLOR`、`SCENE_DEPTH`、final aliases、G-buffer/lighting frame views、`LIGHT_LIST` 和 `SHADOW_ATLAS`;移除 `execute_graph_stage.rs` 内旧 frame-target/final-alias import helpers;新增 source-contract tests 覆盖 live-only frame binding、direct imported final target alias rebinding,以及 advanced postprocess transients 在 materialization 前不被 frame binder 预绑定。新增 `docs/zircon_runtime/graphics/scene/scene_renderer/render_path/frame_graph_resources.md`,并同步 graph execution/render graph docs 与 render index。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;首次 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-frame-binding-0618 --message-format short --color never` 暴露新 binder alias loop 的 `&&str` 类型错误与一个 stale import;修正后暖 rerun 通过(1m36s,既有 141 warning set)。focused frame/materialization lib-tests 仍按实现优先策略未运行。 | 补跑 frame binder/materialization focused tests;继续 plugin-owned/non-frame External 实际绑定模型、resource lifetime 验证闭环、plugin package locked check 解阻与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 first-party plugin buffer External fallback binding | 部分完成: particles / Virtual Geometry 当前 typed plugin buffer External 已进入 graph resource table 与 alias report;真实 plugin WGPU resource handoff 仍待后续 | 新增 `render/bind_plugin_graph_resources.rs`;`render_compiled_scene(...)` 在 transient/HZB binding 后、materialization validation 前调用它;该 binder 只绑定 live typed external buffer lifetimes,覆盖 `particles.gpu.particles-a`、`particles.gpu.emitter-params`、`particles.gpu.particles-b`、`particles.gpu.counters`、`particles.gpu.alive-indices`、`particles.gpu.indirect-draw-args`、`particles.gpu.debug-readback` 与 `virtual-geometry-feedback`,并用 `:plugin-external-fallback` backing 记录 alias。`bind_history_graph_resources.rs` 同步接管 Hybrid GI `history-global-illumination` history texture alias,避免把 history texture 混入 plugin buffer owner。新增 `docs/zircon_runtime/graphics/scene/scene_renderer/render_path/plugin_graph_resources.md`,并同步 history/frame/graph-execution/render_graph docs 与 render index。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-plugin-external-binding-0618 --message-format short --color never` 通过(6m09s,既有 141 warning set)。focused plugin/history materialization lib-tests 仍按实现优先策略未运行。 | 补跑 `plugin_external_fallback_buffers_satisfy_materialization_report`、`plugin_external_binder_skips_unknown_and_untyped_externals`、history Hybrid GI alias focused tests;继续真实 plugin WGPU buffer handoff、External 缺失硬错误策略、resource lifetime 验证闭环、plugin package locked check 解阻与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 runtime-prepare plugin buffer External real-binding handoff | 部分完成: scene renderer 侧真实 WGPU buffer handoff 通道已接入,first-party plugin backend 生产者注册仍待后续 | `RuntimePrepareCollectorContext` 新增 `register_external_buffer_binding(...)` 与 explicit backing 变体,只在 `graphics` 层携带 WGPU buffer clone;`SceneRendererAdvancedPluginResources::execute_runtime_prepare_passes(...)` 汇总所有 collector 注册的 per-frame buffer bindings;`SceneRendererAdvancedPluginReadbacks` 同时保存 neutral `RenderPluginRendererOutputs` 与 graphics-internal external buffer sideband;`bind_plugin_graph_resources(...)` 先绑定 runtime-prepare 注册的 live typed external buffers,再只对未满足的 current particles/VG first-party 名称创建 `:plugin-external-fallback` buffer。新增 source-contract tests 覆盖 collector context 注册、runtime prepare owner 汇总、readback owner 保存、registered buffer 覆盖 fallback、registered non-fallback typed external 名称绑定。同步 plugin graph resources、graph execution resources、render graph builder 文档。 | `rustfmt --edition 2021` 覆盖本切片 Rust 文件;D: 剩余空间 139077767168 bytes;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-plugin-real-buffer-handoff-0618 --message-format short --color never` 通过(8m23s,既有 141 warning set)。随后 attempted `cargo test -p zircon_runtime --lib runtime_prepare_collectors_can_register_external_buffer_bindings --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-plugin-real-buffer-handoff-0618 --message-format short --color never`;首次暴露本切片 test import 缺失并已修正,复跑仍在 test-crate 编译前被无关 root-surface/test drift 阻塞: 多个 graphics tests/fixtures 从 `crate::BuiltinRenderFeature`/`RenderPassStage` 等 root 导入缺失,mesh pass tests 缺 `RenderPhaseSortComponents`,以及 `compile_tests.rs` 访问 private `scene_renderer` module。 | 补跑新 plugin real-binding focused tests(先修 test-crate root-surface/import drift);把 particles GPU backend buffers 与 Virtual Geometry feedback producer 接到 `RuntimePrepareCollectorContext::register_external_buffer_binding(...)`;继续 External 缺失硬错误策略、plugin package locked check 解阻、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 Virtual Geometry runtime-prepare feedback producer registration | 部分完成: VG prepared feedback 已通过 runtime-prepare collector 注册真实 WGPU buffer,particles backend 仍需 renderer-owned producer 接入 | `virtual_geometry_plugin_renderer_outputs.rs` 的 runtime-prepare collector 在 prepared NodeAndClusterCull `page_request_ids` 非空时创建 `virtual-geometry-feedback:runtime-prepare-page-requests` storage buffer,并通过 `RuntimePrepareCollectorContext::register_external_buffer_binding_with_backing(...)` 注册到 plugin graph binder;空反馈不注册,继续由 binder fallback 兜底。`ParticleGpuBackend::active_buffers()` 补齐 `emitter_params` 与 `debug_readback` 只读句柄,为后续注册完整 `particles.gpu.*` 外部集做准备,但当前 particles plugin 未注册 runtime-prepare collector,scene renderer 也未拥有/执行 `ParticleGpuBackend`,因此未伪造 particles producer。同步 plugin graph resources、graph execution resources、render_graph builder 与 particles runtime 文档。 | `rustfmt --edition 2021 --check zircon_plugins/virtual_geometry/runtime/src/virtual_geometry/renderer/root_output_sources/virtual_geometry_plugin_renderer_outputs.rs zircon_plugins/particles/runtime/src/render/gpu/backend.rs` 通过;D: 剩余空间 138383216640 bytes;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-vg-feedback-producer-0618 --message-format short --color never` 通过(6m26s,既有 141 warning set)。`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_virtual_geometry_runtime -p zircon_plugin_particles_runtime --locked --message-format short --color never` 在编译前被既有 `zircon_plugins/Cargo.lock` 需更新阻塞,本切片未改 lockfile。 | 补跑 VG runtime-prepare/plugin binder focused tests;把 `ParticleGpuBackend` 纳入 scene renderer runtime-prepare producer 链后注册 particles buffers;继续 External 缺失硬错误策略、plugin package locked check 解阻、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 Particles runtime-prepare neutral buffer producer registration | 部分完成: particles 插件现在注册 `particles.runtime-prepare` 并在 neutral GPU frame 存在时注册 per-frame WGPU buffer backings;完整 `ParticleGpuBackend` 执行/透明 GPU draw 仍待后续 | 新增 `zircon_plugins/particles/runtime/src/render/runtime_prepare.rs`;插件注册路径贡献 runtime-prepare collector;collector 对 `ParticleExtract.gpu_frame` 创建 `particles.gpu.*` storage/indirect/readback buffers,写入 counters/alive indices/indirect args,通过 `RuntimePrepareCollectorContext::register_external_buffer_binding_with_backing(...)` 交给 plugin graph binder,同时返回 neutral `RenderPluginRendererOutputs.particles`;同步 CPU particle extract DTO(`previous_sprites`,stable sprite keys,aspect/billboard/depth fields)以满足当前 render DTO 合同。同步 particles runtime、plugin graph resources、graph execution resources、render_graph builder 与 architecture docs。 | `rustfmt --edition 2021` 覆盖本切片 particles Rust 文件;首次 `cargo check -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-runtime-prepare-0618 --message-format short --color never` 到达 particles crate 后暴露 DTO drift(`ParticleExtract.previous_sprites` 与 `RenderParticleSpriteSnapshot` 新字段)并已修正,暖 rerun 通过(既有 zircon_runtime 141 warning set)。`cargo test -p zircon_plugin_particles_runtime particle_runtime_prepare --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-runtime-prepare-0618 --message-format short --color never -- --nocapture` 通过(3 passed,21 filtered);`cargo test -p zircon_plugin_particles_runtime cpu_extract_preserves_material_texture_rotation_bounds_and_sort_metadata --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-runtime-prepare-0618 --message-format short --color never -- --nocapture` 通过(1 passed,23 filtered)。 | 将真实 `ParticleGpuBackend` lifecycle/execution 接入 runtime-prepare producer;补跑 wider particles runtime/plugin registration lane;解阻 `zircon_plugins/Cargo.lock` package check;继续 External 缺失硬错误策略、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 Particles real `ParticleGpuBackend` runtime-prepare owner | 部分完成: particles runtime-prepare 现在可通过插件共享 manager 执行真实 `ParticleGpuBackend` 并注册后端 WGPU buffers;透明 GPU draw 仍待后续 | 新增 `ParticleGpuRuntimeOwner`,按 `ParticleEmitterHandle` 缓存 planner/backend,从共享 `ParticlesManager::gpu_runtime_instances()` 读取 concrete `ParticleSystemComponent`/asset/transform/age,执行 spawn-update、compact-alive、indirect-args compute,并暴露 particles/emitter-params/alive-indices/indirect/counters/debug buffers 给 runtime-prepare external binding。`ParticlesRuntimePlugin` 现在持有共享 manager,模块 descriptor 与 runtime-prepare collector 使用同一 manager。同步修复 `find_emitter` WGSL return path 与 compact pass bind group 同 buffer read/write usage 冲突。 | `rustfmt --edition 2021` 覆盖本切片 particles Rust 文件;`cargo check -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never` 通过(既有 zircon_runtime warning set)。`cargo test -p zircon_plugin_particles_runtime particles_runtime_plugin_module_and_runtime_prepare_share_manager --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 通过(1 passed);`cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner_executes_backend_and_exposes_active_buffers --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 初次暴露 WGSL validation 与 WGPU storage usage conflict,修复后通过(1 passed)。 | GPU transparent draw 对 active buffer/indirect args 的消费、wider particles/plugin package locked check、External 泛化校验与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 Particles runtime-prepare ping-pong graph buffer aliases | 部分完成: real particles runtime-prepare 现在把 `particles-a`/`particles-b` 绑定为上一帧输入/当前输出两侧的 distinct WGPU buffers;透明 GPU draw 消费仍待后续 | `ParticleGpuBuffers` 与 `ParticleGpuRuntimeBufferBindings` 从单一 `particles` handle 改为 `particles_a`/`particles_b` graph-facing aliases。`ParticleGpuBackend::active_buffers()` 在 `execute_frame(...)` 翻转 active buffer 后返回 `particles_a = previous/input`、`particles_b = current/output`,runtime-prepare collector 分别注册到 `particles.gpu.particles-a` 与 `particles.gpu.particles-b`,避免两个 graph resource name 指向同一个 active backend buffer。focused GPU owner test 断言两个 alias size 相同且 `std::ptr::eq(...) == false`。同步 particles runtime、plugin graph resources、render index 与会话状态。 | `rustfmt --edition 2021 zircon_plugins/particles/runtime/src/render/gpu/backend.rs zircon_plugins/particles/runtime/src/render/gpu/runtime_owner.rs zircon_plugins/particles/runtime/src/render/runtime_prepare.rs zircon_plugins/particles/runtime/src/tests/gpu.rs` 通过;`cargo check -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never` 通过(既有 zircon_runtime warning set);`cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner_executes_backend_and_exposes_active_buffers --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 通过(1 passed,25 filtered;既有 warning set)。 | GPU transparent draw 对 `particles-b`/alive indices/indirect args 的消费、wider particles/plugin package locked check、External 泛化校验与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 Particles runtime-prepare multi-system aggregation | 部分完成: runtime-prepare 真实 particles producer 现在把所有 playing GPU instances 聚合进一个 backend-owned `particles.gpu.*` 绑定集;透明 GPU draw 消费仍待后续 | `ParticleGpuRuntimeOwner` 不再按最大 handle 选择单个 authoritative system,而是保留每个 `ParticleEmitterHandle` 的 planner/time state,从所有 playing GPU runtime instances 合成一个 aggregate GPU asset/backend,并把各实例 planner 产出的 emitter params remap 到 aggregate layout 的 base slots/capacities。`ParticleGpuFrameParams::encode_emitters(...)` 保留每个 emitter 自己的 dt/age,避免聚合时把不同实例时钟压成一个 frame-wide 时间。新增 focused WGPU test `particle_gpu_runtime_owner_aggregates_playing_gpu_instances`,断言两个 GPU systems 的 burst 输出合成 `spawned_total=6`、`per_emitter_spawned=[2,4]`、`indirect_draw_args=[6,6,0,0]`,并继续暴露 distinct ping-pong graph buffers。同步 particles runtime、plugin graph resources、render index 与会话状态。 | `rustfmt --edition 2021 zircon_plugins/particles/runtime/src/render/gpu/planner.rs zircon_plugins/particles/runtime/src/render/gpu/runtime_owner.rs zircon_plugins/particles/runtime/src/tests/gpu.rs` 通过;首次 `cargo check -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never` 暴露 owner refactor 的 missing import/ambiguous float type 并已修正,复跑通过(既有 zircon_runtime warning set);`cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 通过(2 passed,25 filtered;既有 warning set)。 | 补齐 GPU transparent draw 对 `particles-b`/alive indices/indirect args 的消费、wider particles/plugin package locked check、External 泛化校验与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 External materialization coverage report split | 部分完成: required External 与 report-only External 覆盖已在 report/diagnostics 中分离,resource lifetime 验证闭环继续推进 | `RenderGraphMaterializationReport` 将 external 覆盖拆成 `required_external_count` / `bound_required_external_count` / `missing_required_external_count` 与 `report_only_external_count` / `bound_report_only_external_count` / `missing_report_only_external_count`;保留 `bound_external_count()` / `missing_external_count()` aggregate helpers 供诊断总量使用;`materialization_validation.rs` 只把 `required_buffer()` / `required_texture()` lifetimes 计入 required bucket,report-only unknown/texture/buffer lifetimes 进入 report-only bucket,缺失 required external 仍然硬错误,缺失 report-only external 仍然只影响 `is_complete()`。同步 binder source-contract tests、runtime diagnostics fixture/rows、graph execution resources、render_graph builder 与 submit-boundary docs。 | 新增 `materialization_validation_reports_required_and_report_only_external_coverage_separately`;更新 report-only external、typed optional external、HZB required external、history/plugin binder 与 runtime diagnostics source-contract assertions。`rustfmt --edition 2021` 覆盖本切片 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-external-coverage-report-0618 --message-format short --color never` 通过(6m25s,既有 141 warning set);后续 focused `cargo test -p zircon_runtime --lib materialization_validation_reports_required_and_report_only_external_coverage_separately --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-external-coverage-report-0618 --message-format short --color never` 通过(1 passed, 4459 filtered out,既有 52 warning set),其间先清理了 root-surface import drift 与 `PhaseOrderedMeshSnapshot.snapshot.node_id` 测试投影漂移;`cargo test -p zircon_runtime --lib runtime_diagnostics_combines_core_render_contract_and_missing_externalized_plugins --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-external-coverage-report-0618 --message-format short --color never` 通过(1 passed, 4459 filtered out),确认 diagnostics 行投影。scoped `git diff --check` 通过(仅 Git LF->CRLF 提示),冲突标记扫描无命中。 | 补跑 history/plugin/HZB focused tests;继续 particles renderer-owned producer 接入、plugin package locked check 解阻、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 stale materialization binding lifetime validation | 完成: materialization validation 已拒绝执行资源表中不属于 live compiled lifetimes 的预绑定 texture/buffer,focused stale-binding texture/buffer source-contract tests 已通过 | `RenderGraphMaterializationReport` 新增 `stale_texture_binding_count` / `stale_buffer_binding_count` 与 `stale_binding_count()`;`materialization_validation.rs` 以 `CompiledRenderGraph::resource_lifetimes()` 作为 live set,扫描 `RenderGraphExecutionResources` 中已绑定的 logical texture view / buffer 名称,若名称不在 live set 中则在 executor dispatch 前报 stale binding 错误;`materialized_resources_complete()` 与 `is_complete()` 均要求 stale count 为 0;runtime diagnostics 新增 `render.graph.materialization.stale_binding_count`、`stale_texture_binding_count`、`stale_buffer_binding_count`;新增 source-contract tests 覆盖 stale texture/buffer 预绑定被拒绝。同步 graph execution resources、render_graph builder、render-product submit 与 diagnostics docs。 | `rustfmt --edition 2021` 覆盖 stale-binding 相关 Rust 文件;scoped `git diff --check` 通过(仅 Git LF->CRLF 提示);冲突标记扫描无命中;D: 剩余空间 133489541120 bytes;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-stale-lifetime-validation-0618 --message-format short --color never` 通过(421.59s,既有 141 warning set)。首次 `cargo test -p zircon_runtime --lib materialization_validation_rejects_stale --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-stale-lifetime-validation-0618 --message-format short --color never` 在 904s 超时且未返回测试结果,后续 `cargo test ... --no-run` 同 target-dir 完成 lib-test 编译(8m06s,既有 52-warning set)并生成 `zircon_runtime-d071a300da0585cb.exe`;直接执行该二进制的 full-path exact filters 通过 `materialization_validation_rejects_stale_texture_binding_outside_live_lifetimes`(1 passed,4474 filtered,1.41s) 与 `materialization_validation_rejects_stale_buffer_binding_outside_live_lifetimes`(1 passed,4474 filtered,1.43s)。结束后 stale-lifetime target-dir 无 Cargo/rustc/rustdoc 残留进程。 | 继续 history/plugin/HZB focused tests、particles renderer-owned producer 接入、plugin package locked check 解阻与 RenderDoc resource/marker 对拍。 |
| 2026-06-18 | RG-M1 split postprocess exposure dependency trim | 部分完成: split DoF/motion-blur/blur pass 声明已按真实 shader 输入收敛,避免误把它们排到 exposure resolve 之后 | `post.depth-of-field`、`post.motion-blur` 与 `post.blur` 不再声明 `EXPOSURE_CURRENT` read,因为对应 WGSL entry points 不采样 resolved exposure;`post.scene-composite`、`post.color-lut-bake` 与 `post.uber` 继续读取 `EXPOSURE_CURRENT`,所以真实 exposure consumer 仍由 `post.exposure.resolve` 排序保护。新增 compile source-contract test 断言 split DoF/motion-blur 不读 exposure,而 exposure resolve 与 uber 仍保留真实读依赖。同步 post-process docs、render_graph builder docs 与会话 note。 | `rustfmt --edition 2021` 覆盖 `post_process.rs` 与 `compile_tests.rs`;`cargo test -p zircon_runtime --lib compile_keeps_split_postprocess_passes_before_exposure_when_they_do_not_sample_exposure --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-rg-required-external-0618` 通过;同一 target-dir 补跑 `compile_forward_plus_preserves_shadow_atlas_required_external_texture_binding`、`compile_deferred_preserves_shadow_atlas_required_external_texture_binding` 与 `required_external` focused lane,结果为 8 passed,0 failed。 | 继续 particles renderer-owned producer 接入、plugin package locked check 解阻、resource lifetime 验证闭环与 RenderDoc resource/marker 对拍;必要时补跑更宽 post-process graph/order focused lane。 |
| 2026-06-18 | RG-M1 Particles transparent GPU draw shared-owner consumption | 部分完成: `particle.transparent` 现在优先消费 runtime-prepare 刚执行过的 plugin-owned `ParticleGpuRuntimeOwner` backend;CPU billboard fallback 与 graph contract 保持,产品/RenderDoc 验收仍待后续 | `ParticlesRuntimePlugin::register(...)` 创建一个共享 `ParticleGpuRuntimeOwnerHandle`,同时传给 `particles.runtime-prepare` collector 与 object-backed `particle.transparent` executor。runtime prepare 执行 concrete `ParticleGpuBackend` 后仍注册 `particles.gpu.particles-a/b`、alive indices、indirect args 等 external buffers;透明 executor 通过 `RenderPassGpuExecutionContext::record_particle_gpu_transparent_to_resources(...)` 获取 resolver 校验后的 `scene-color`/`scene-depth` views、scene bind group/layout、target/depth format、queue/encoder 与 camera billboard basis,再调用 owner lazy-enable transparent renderer 并从 active backend 记录 indirect transparent draw。无 executed backend 时返回 false 并走 CPU billboard fallback。`RenderPassGpuExecutionContext` 与 `SceneRendererCore` 现在携带 scene bind group layout、target format、depth format;direct render path与 scene-pass wrapper 都转发同一 context metadata。同步 particles runtime docs、plugin graph resources、render index 与会话状态。 | `rustfmt --edition 2021` 覆盖本切片 runtime/particles Rust 文件;首次 `cargo check -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never` 暴露中间 re-export、scene-pass wrapper 参数与 borrow split 问题并已修正,复跑通过(既有 warning set);`cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 通过(4 passed,0 failed),覆盖 executed-backend transparent draw 与 empty-backend skip;首次 `cargo test -p zircon_runtime render_pass_executor_registry --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-render-context-0618 --message-format short --color never -- --nocapture` 在 364s 超时,暖 target-dir 复跑先暴露 4 个 test fixture/guard-order drift(无 cull root、自身未绑定 light-grid buffers),按 support-first 修正 fixture 后最终通过(41 passed,0 failed)。 | 补跑 product scene/RenderDoc resource+marker 对拍、wider particles/plugin package locked check(需先处理 plugin lockfile drift)、scene auto-collection 与 GPU/CPU parity 验收。 |
| 2026-06-18 | RG-M1 Particles transparent GPU draw offscreen visual readback | 完成(focused): transparent owner test 现在在 offscreen WGPU 目标上确认 indirect transparent draw 写入非透明 RGBA pixels;完整 product scene 与 RenderDoc 对拍仍待后续 | `particle_gpu_runtime_owner_records_transparent_draw_from_executed_backend` 保留 shared-owner backend 执行与 `record_transparent_render(...)` 断言,并新增 test-local `copy_texture_to_buffer` RGBA8 readback helper。测试在 32x32 `Rgba8Unorm` color target 清透明后提交 transparent pass,随后读取 texture 并断言存在 alpha>0 且 RGB 非零的像素,覆盖 draw 被记录但没有实际出图的回归。该变更只加强 `zircon_plugins/particles/runtime/src/tests/gpu.rs` 的 WGPU evidence,不新增 public runtime/plugin API。 | `rustfmt --edition 2021 zircon_plugins/particles/runtime/src/tests/gpu.rs` 通过;`cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 通过(4 passed,0 failed,25 filtered;既有 zircon_runtime warning set),其中 executed-backend transparent test 包含 RGBA8 visual readback 断言。 | 继续 product scene/RenderDoc resource+marker 对拍、wider particles/plugin package locked check(需先处理 plugin lockfile drift)、scene auto-collection 与 GPU/CPU parity 验收。 |
| 2026-06-18 | RG-M1 Particles GPU readback count parity support | 完成(focused): concrete backend counter/readback path 现在支持 CPU/GPU 小场景 alive/spawn parity 与 indirect args word1 验证 | 新增 `render_particle_cpu_gpu_parity_small_scene_matches_counts_and_indirect_args`,用同一 burst-only asset 分别走 `ParticlesManager` CPU fallback extract 与 concrete `ParticleGpuBackend` WGPU path,再通过 `ParticleGpuCpuParityReport` 比较 live/spawn counts,并验证 readback 的 indirect args 为 `[6,cpu_live,0,0]`。该测试先暴露两个 lower support bug: `read_buffer_u32s_at(...)` 对 byte offset 20 的 map range 未按 wgpu 8-byte 对齐,以及 spawn shader 的 per-emitter counter 记录 claim count 而非 successful spawn count。修复后 readback helper 映射 aligned range 并在 mapped bytes 中切回 requested window;`particle_build_indirect_args` 以 `min(claimed, emitter.spawn_count)` 规范化每个 per-emitter spawned counter 并重写 spawned total。 | `rustfmt --edition 2021` 覆盖 `render/gpu/backend.rs`、`render/gpu/shaders.rs`、`tests/gpu.rs`;首次 focused parity test 因 wgpu map offset alignment panic 失败,修复 readback helper 后进入断言并暴露 per-emitter spawned `[16] != [5]`,修复 shader counter 语义后 `cargo test -p zircon_plugin_particles_runtime render_particle_cpu_gpu_parity_small_scene_matches_counts_and_indirect_args --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 通过(1 passed,29 filtered;既有 warning set);随后 `cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner --locked --jobs 1 --target-dir D:\cargo-targets\zircon-particles-gpu-owner-0618 --message-format short --color never -- --nocapture` 继续通过(4 passed,26 filtered)。 | 继续 product scene/RenderDoc resource+marker 对拍、wider particles/plugin package locked check、concrete scene-to-manager `ParticleSystemComponent` integration、per-view sorting 与更宽 CPU/GPU image parity。 |
| 2026-06-18 | RG-M1 Particles scene neutral GPU-frame auto-collection | 完成(focused): scene dynamic particle payloads can now project neutral `ParticleExtract.gpu_frame` without importing plugin or WGPU ownership into `World` | `World::collect_render_particles(...)` now parses visible `render.particle_sprites` / `gameplay.particle_sprites` dynamic payloads for a `gpu_frame` object, aggregates `alive_count`、`spawned_total`、`per_emitter_spawned`, synthesizes non-indexed indirect args `[6,alive_count,0,0]`, and folds optional GPU bounds into emitter bounds/visibility. Entities with only a visible GPU-frame payload are included as particle emitters even when they contribute no CPU sprites. Runtime prepare can then create neutral `particles.gpu.*` buffers from the scene-produced `ParticleExtract.gpu_frame` when no concrete shared-manager backend executed for the frame. | `rustfmt --edition 2021 zircon_runtime/src/scene/world/render_particles.rs zircon_runtime/src/scene/tests/render_extract.rs` 通过;`cargo check -q -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-particle-gpu-frame-0618` 通过(既有 warning set);`cargo test -p zircon_runtime --lib render_frame_extract_collects_dynamic_particle_gpu_frames_by_camera_layers --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-particle-gpu-frame-0618 --message-format short --color never -- --nocapture` 通过(1 focused scene test,4487 filtered)。 | 继续 product scene/RenderDoc resource+marker 对拍、wider particles/plugin package locked check、concrete scene-to-manager `ParticleSystemComponent` integration、per-view sorting 与更宽 CPU/GPU image parity。 |

### 参考实现精读笔记

`dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphBuilder.h` — `FRDGBuilder`:

- `CreateTexture(const FRDGTextureDesc&, const TCHAR* Name, ERDGTextureFlags)` / `CreateBuffer(...)` / `RegisterExternalTexture(const TRefCountPtr<IPooledRenderTarget>&, ...)`:解决"声明与分配分离"——句柄先行,RHI 资源延迟到执行期。Zircon 对应 `RenderGraphBuilder::create_texture/create_buffer/import_external_resource_with_usage`(已有,本计划补 usage)。取舍:UE 的 `CreateBuffer(NumElementsCallback)` 延迟定容不移植,wgpu 无 placed resource,定容收益小。
- `AddPass(FRDGEventName&&, const ParameterStructType*, ERDGPassFlags, ExecuteLambdaType&&)`:参数结构体(\_RDG\_ 宏)即资源 IO 声明,lambda 与参数分离。Zircon 用显式 `read_*/write_*` 声明 + executor id 注册表替代 lambda(动态插件边界不能传闭包),等价性由 `validate_compiled_pipeline` 的 executor 存在性校验保证。
- `QueueTextureExtraction(FRDGTextureRef, TRefCountPtr<IPooledRenderTarget>*)` 与 `FRDGViewableResource::IsCullRoot() { return bExternal || bExtracted; }`(RenderGraphResources.h):extraction/external 即裁剪 root。Zircon 把它显式化为 `RenderGraphResourceUsageFlags{present, readback, persistent}`,比 UE 更细(UE 的 external 一律 root,Zircon 的 external 仅在标记后成 root,避免导入贴图把上游全保活)。
- `Compile()` / `SetupPassDependencies` / `MarkResourcesAsProduced`:编译期推导依赖与裁剪。Zircon 对应 `RenderGraphBuilder::compile()` 内的 `infer_resource_dependencies` + `cull_passes`(已有),本计划只改 root 判据。
- `IRHITransientResourceAllocator* TransientResourceAllocator` + `bSupportsTransientTextures` + `IsTransientInternal` + `FCollectResourceOp::Allocate/Deallocate`:UE 把生命周期区间编译成 Allocate/Deallocate 操作流,由 RHI transient allocator 做真实内存别名。wgpu 没有该能力,Zircon 退化为 `TransientResourcePool` 池化复用(正文既定取舍),Allocate/Deallocate 操作流简化为帧首一次性区间着色。
- `TickPoolElements()`:每帧池维护钩子 → Zircon `TransientResourcePool::end_frame` 的 KEEP_FRAMES 驱逐。

`dev/UnrealEngine/Engine/Source/Runtime/RenderCore/Public/RenderGraphResources.h`:

- `FRDGViewableResource` 的位标记组:`bExternal / bExtracted / bProduced / bTransient / bForceNonTransient / bSkipLastTransition`,以及 `FirstPass / LastPasses`、`AcquirePass / DiscardPass`、`ReferenceCount`、`AliasingOverlaps(FRHITransientAliasingOverlap)`:资源自带生命周期端点与别名重叠表。Zircon 的 `RenderGraphResourceLifetime{first_pass, last_pass}` 已对应端点;`AliasingOverlaps`(别名内存的 acquire/discard 屏障)不移植——池化复用下 wgpu 自行管理底层内存,串台风险由首写 Clear 决策表兜底。
- `FRDGSubresourceState`(`FirstPass/LastPass` per pipeline、`IsTransitionRequired` / `IsMergeAllowed`):子资源粒度 barrier 合并。wgpu 自动 barrier,Zircon 不需要;mip 粒度的需求(SSR pyramid)用既有 `ssr_pyramid_mip_alias` 的 mip view 别名即可。
- `FRDGTexture::TransientTexture(FRHITransientTexture*)`:transient 分配的句柄回填位 → Zircon 对应 `RenderGraphExecutionResources::bind_pooled_texture` 的 name→view 绑定。

`dev/Graphics/Packages/com.unity.render-pipelines.core/Runtime/RenderGraph/RenderGraph.cs`:

- `Execute()` 中 `int graphHash = m_EnableCompilationCaching ? ComputeGraphHash() : 0; CompileNativeRenderGraph(graphHash);`:编译缓存以"图内容哈希"为 key,在录制后、编译前求值。`ComputeGraphHash()` 用 `HashFNV1A32` 对 `m_RenderPasses[i].ComputeHash(ref hash128, m_Resources)` 逐 pass 折叠。Zircon 的差异:Zircon 的图由 pipeline asset + options 决定、不每帧重录,所以 `CompiledGraphCacheKey` 直接由输入端(revision/options/profile/viewport/caps/extract 指纹)组成,省掉每帧全图哈希,这是比 Unity 更便宜的稳态路径;代价是必须用 `extract_compile_fingerprint` 的 debug_assert 守住"编译读取面都进 key"的不变量。
- pass 合并(`CompileNativeRenderGraph` 转入 NativeRenderPassCompiler 的 merge)不在本计划移植:wgpu 的 render pass 合并收益体现在 tile GPU,留待后续独立立项,不影响缓存接口。

`dev/Graphics/Packages/com.unity.render-pipelines.core/Runtime/RenderGraph/RenderGraphCompilationCache.cs`:

- `HashEntry<T>{hash, lastFrameUsed, compiledGraph}`、`k_CachedGraphCount = 20`、`GetCompilationCache(hash, frameIndex, out CompilerContextData)`:命中刷新 `lastFrameUsed`;miss 时优先从 `m_NativeCompiledGraphPool` 取空条目,池空则按 `lastFrameUsed` QuickSort 复用最旧条目。Zircon `CompiledGraphCache` 采用同构 LRU(capacity 16,`last_used_frame` 驱逐),但条目直接持 `Arc<CompiledRenderPipeline>` 而非预分配对象池——Rust 下 Arc 复用即免 GC 压力,无需 Unity 的对象池形态;另补 `invalidate_pipeline`(Unity 靠图哈希天然失效,Zircon 输入端 key 需要资产修订显式失效)。

`dev/Graphics/Packages/com.unity.render-pipelines.core/Runtime/RenderGraph/RenderGraphResourcePool.cs`(TransientResourcePool 的桶结构参照):

- `m_ResourcePool: Dictionary<int, SortedList<ulong, (Type resource, int frameIndex)>>` + `TryGetResource(hashCode)` / `ReleaseResource(hash, resource, currentFrameIndex)` + `kStaleResourceLifetime = 10` + `PurgeUnusedResources`:描述符哈希做桶、桶内按 `GetSortIndex` 排序保证跨帧取用稳定、按帧龄清陈旧条目。Zircon `TransientResourcePool` 对应:`HashMap<TransientTextureKey, Vec<PooledTextureEntry>>` 桶、桶内 slot 索引序(区间着色后 slot i ↔ 条目 i,天然稳定)、`KEEP_FRAMES = 8` 驱逐。取舍:Unity 是"借出/归还"动态池(pass 边界归还),Zircon 是"帧首整批绑定、帧末整批归还"——因为生命周期区间在编译期已知,整批绑定可保证同帧别名决策确定性,也避免执行期池锁。

## 风险与回退

- executor 签名改动波及全部内建 executor 与插件 executor:按硬切换原则一次迁完,以 `cargo check` 驱动清单;插件侧(rendering/hybrid_gi/virtual_geometry)在同一里程碑内适配。
- 池化复用引入串台风险(写后读到旧内容):靠 attachment ops 校验(首写必 Clear)兜底;出现画面异常时用 graph dump + RenderDoc 定位区间分配错误。
- 不做底层内存 alias,wgpu 池化即止;若未来需要更激进的显存控制,在本计划之上单独立项,不回改接口。
