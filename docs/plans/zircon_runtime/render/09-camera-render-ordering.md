---
related_code:
  - zircon_runtime/src/core/framework/render/camera.rs
  - zircon_runtime/src/core/framework/render/frame_extract.rs
  - zircon_runtime/src/core/framework/render/frame_extract/geometry.rs
  - zircon_runtime/src/core/framework/render/scene_extract.rs
  - zircon_runtime/src/core/framework/render/sprite/sprite.rs
  - zircon_runtime/src/core/framework/render/light/snapshots.rs
  - zircon_runtime/src/core/framework/render/core_pipeline/phase_queue.rs
  - zircon_runtime/src/core/framework/render/core_pipeline/phase_sort.rs
  - zircon_runtime/src/core/framework/render/core_pipeline/pipeline_kind.rs
  - zircon_runtime/src/scene/world/render.rs
  - zircon_runtime/src/scene/world/render_post_process.rs
  - zircon_runtime/src/scene/world/render_particles.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/lighting/light_buffer.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/shadow/plan.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/shadow/view_projection.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/transparent/mixed_submission.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/overlay/passes/base_scene_pass.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/core/scene_renderer_core_render_compiled_scene/scene_passes/render_scene_passes.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/sprite/build_sprite_vertices.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/sprite/sprite_renderer.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/particle/build_particle_vertices/build_particle_vertices.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/particle/build_particle_velocity_vertices/build_particle_velocity_vertices.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/mesh/mesh_pass/replay.rs
  - zircon_runtime/src/graphics/pipeline/render_pipeline_asset/default_forward_plus.rs
  - zircon_runtime/src/graphics/pipeline/render_pipeline_asset/default_deferred.rs
  - zircon_runtime/src/graphics/pipeline/compiled_graph_cache.rs
  - zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/build_frame_submission_context/build.rs
  - zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/build_frame_submission_context/compile_pipeline.rs
  - zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/build_frame_submission_context/target_resolution.rs
  - zircon_runtime/src/graphics/runtime/render_framework/viewport_record/camera_history_key.rs
  - zircon_runtime/src/graphics/types/viewport_render_frame.rs
  - zircon_runtime/src/graphics/types/viewport_render_output_target.rs
  - zircon_runtime/src/graphics/types/viewport_render_region.rs
  - zircon_runtime/src/graphics/runtime/render_framework/create_viewport/mod.rs
  - zircon_runtime/src/graphics/tests/m4_behavior_layers.rs
  - zircon_runtime/src/graphics/tests/m4_behavior_layers/particles.rs
  - zircon_runtime/src/graphics/tests/m4_behavior_layers/postprocess.rs
  - zircon_runtime/src/graphics/tests/m4_behavior_layers/queue_override.rs
  - zircon_runtime/src/graphics/tests/m4_behavior_layers/transparent3d.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/m4_behavior_postprocess_tests.rs
  - zircon_runtime/src/graphics/tests/render_product_sprite.rs
  - zircon_runtime/src/graphics/tests/render_product_submit.rs
  - zircon_runtime/src/graphics/tests/render_product_submit/profiles.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_product_submit_profiles_tests.rs
  - zircon_runtime/src/scene/tests/world_basics.rs
  - zircon_runtime/src/scene/tests/render_post_process_extract.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/assertions.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/camera.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/custom_target.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/custom_target/composite.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/custom_target/material_sampling.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/custom_target/ordering.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/custom_target/viewport.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/fixture.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/mesh.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/primary_surface.rs
  - zircon_runtime/src/graphics/tests/render_product_camera_targets/texture_target.rs
  - zircon_runtime/src/graphics/tests/pipeline_overlay_order.rs
  - zircon_runtime/src/graphics/tests/plugin_render_feature_fixtures.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/production_file_budget/render_frame_extract_geometry.rs
  - dev/Graphics/Packages/com.unity.render-pipelines.universal/Runtime/UniversalRenderPipeline.cs
  - dev/Graphics/Packages/com.unity.render-pipelines.universal/Runtime/UniversalAdditionalCameraData.cs
  - dev/Graphics/Packages/com.unity.render-pipelines.universal/Runtime/ScriptableRenderer.cs
  - dev/Graphics/Packages/com.unity.render-pipelines.universal/Runtime/UniversalRenderPipelineCore.cs
  - dev/bevy/crates/bevy_camera/src/camera.rs
  - dev/bevy/crates/bevy_camera/src/clear_color.rs
  - dev/bevy/crates/bevy_camera/src/visibility/render_layers.rs
  - dev/bevy/crates/bevy_render/src/camera.rs
  - dev/bevy/crates/bevy_render/src/render_phase/mod.rs
  - dev/bevy/crates/bevy_render/src/render_phase/rangefinder.rs
  - dev/bevy/crates/bevy_core_pipeline/src/core_3d/mod.rs
  - dev/bevy/crates/bevy_core_pipeline/src/core_2d/mod.rs
plan_sources:
  - .codex/plans/ZirconEngine Bevy-Level Rendering Completion Plan.md
  - .codex/plans/Runtime 吸收层与 Editor_Scene 边界收束计划.md
---

# 计划 09:相机管理与渲染顺序体系(Unity 对齐)

## 目标

建立 Unity 语义的相机与排序体系,作为所有渲染器(3D/2D/UI/特效)共享的顺序权威:

1. 多相机:相机栈(Base + Overlay)、per-camera 渲染目标(屏幕 / RT)、viewport rect、depth(相机间先后)、clear 策略。
2. RenderLayer:相机 culling mask × 渲染器 layer 的可见性矩阵;灯光/阴影/volume 也按 layer 过滤(计划 05/07 消费)。
3. RenderQueue 数值体系(Unity 对齐):Background(1000) / Geometry(2000) / AlphaTest(2450) / Transparent(3000) / Overlay(4000);材质可覆写 queue 值;queue 段映射到 RenderPhase。
4. 2D sorting layer + order in layer、UI z-index、3D 深度排序统一收敛进计划 02 的 sort_key 打包规则,各渲染域共享一个排序定义而不是各自为政。

## 现状与差距

- `camera.rs` 有 `ViewportCameraSnapshot`/`RenderCameraTarget`/`RenderLayer` 基础;2026-06-18 已新增 `CameraRenderDescriptor` 与 `RenderViewExtract.cameras`,让计划 04 custom-target visibility、submit preflight、runtime-frame accessors 和 temporal history validation 改读 descriptor target/layer/viewport/order/clear;`ViewportCameraSnapshot` 上 `target`/`order`/`viewport`/`clear_color`/`render_layers` 五字段已删除,offscreen submit 已有 Base/Overlay descriptor loop scaffold,但 editor 面板字段、WGPU target 合成/output ownership 与 Base/Overlay attachment/load-op 仍未完成。
- `phase_queue.rs`/`phase_sort.rs` 的 phase 划分(Opaque3d/AlphaMask3d/Transparent3d/Sprite2d)是硬编码枚举,材质无 queue 数值可覆写;sprite 与 UI 的排序规则与 3D 不在同一定义里。
- UI z-index 在 UI 模块内部自洽(已闭环),但与场景内 3D/2D 内容混排(world-space UI、sprite 在 3D 中)无统一权威。

## 参考代码

| 文件 | 应重点阅读 |
|------|-----------|
| `dev/Graphics/.../Runtime/UniversalRenderPipeline.cs` | 相机栈排序与逐相机渲染循环(`RenderCameraStack`):Base/Overlay 的目标合成、相机 depth 排序 |
| `dev/Graphics/.../Runtime/UniversalAdditionalCameraData.cs` | 相机扩展数据面:render type、culling mask、volume layer mask、renderer 选择 —— 本计划相机契约字段的清单样板 |
| `dev/Graphics/.../Runtime/ScriptableRenderer.cs` | renderer 与相机解耦:相机选择 renderer 实例(对应本引擎 pipeline asset 选择) |
| `dev/Graphics/.../Runtime/UniversalRenderPipelineCore.cs` | `CameraData`/`SortingCriteria` 的组织:排序准则位组合(深度、queue、材质 id) |

次参考:`dev/bevy/crates/bevy_render/src/camera/`(`RenderTarget`/`Viewport`/`ClearColorConfig` 的 Rust 契约表达)。

**Rust/wgpu 落地参照(防凭空实现)**:

| 文件 | 对应本计划机制 | 应重点阅读 |
|------|---------------|-----------|
| `dev/bevy/crates/bevy_camera/src/camera.rs` | `CameraRenderDescriptor`(order/target/viewport/clear) | `Camera` 组件的 `order: isize`(:355)/`viewport: Option<Viewport>`/`clear_color` 字段与 `RenderTarget`(窗口 / Image / TextureView)枚举 —— 相机契约字段的 Rust 形态 |
| `dev/bevy/crates/bevy_camera/src/clear_color.rs` | `RenderCameraClear` | `ClearColorConfig`(Default/Custom/None)三态枚举的契约表达 |
| `dev/bevy/crates/bevy_camera/src/visibility/render_layers.rs` | `RenderLayerSet` 四方共用 mask | `RenderLayers` 以 SmallVec 位集支持 ≥32 layer、`intersects` 过滤 —— 与既有 `RenderLayerSet` 同构,定稿时对照其 API 面 |
| `dev/bevy/crates/bevy_render/src/camera.rs` | `resolve_camera_sequence` + camera_loop | `sort_cameras`(:674):按 order→target 排序、同 order 同 target 发 ambiguity 警告;`SortedCameras` 驱动逐相机执行的 Rust 同型 |
| `dev/bevy/crates/bevy_render/src/render_phase/mod.rs` | `phase_queue.rs`/`phase_sort.rs` 改造 | `PhaseItem::sort_key()`(:2091)+ `sort_phase_system`(:2173);Sorted/Binned 双形态 —— 域 A"聚簇优先"即 bin key 思路的单键化 |
| `dev/bevy/crates/bevy_render/src/render_phase/rangefinder.rs` | 域 A/B 的 effective_depth 来源 | `ViewRangefinder3d::distance`:view 矩阵 row2 点积取 view-space z 作距离键(深度量化的输入定义) |
| `dev/bevy/crates/bevy_core_pipeline/src/core_3d/mod.rs` | 域 A 不透明聚簇 / 域 B 透明深度 | `Opaque3dBatchSetKey`/`Opaque3dBinKey`(:186/:221,pipeline+material 聚簇键)与 `Transparent3d` 的 `FloatOrd` 距离键(:422)对照 |
| `dev/bevy/crates/bevy_core_pipeline/src/core_2d/mod.rs` | 域 C 2D 排序 | `Transparent2d.sort_key: FloatOrd`(:312)与 2D phase 划分 —— 单 f32 键与本计划三级字典序(sorting_layer/order/y)的差距即扩展点 |

`RenderQueueValue` 材质 queue 数值段/覆写与相机栈 Base/Overlay 显式契约(bevy 仅有 order + `ClearColorConfig::None` 的隐式叠加拼法,无栈结构)无 Rust 同类参照,实现时以 UE/URP 为唯一样板,按 index §8 第 8 条配对拍测试先行。

## 目标架构

归属:契约全部在 `core/framework/render/camera.rs` 与 `core_pipeline/`;执行在 render_framework 的 viewport/相机循环;不新增 crate。

核心设计:

- `CameraRenderDescriptor` 扩展:`render_order: i32`(相机间顺序,Unity camera depth)、`render_type: Base | Overlay`、`stack: Vec<CameraId>`(Base 持有 Overlay 列表)、`target: Screen | Texture(handle)`、`viewport_rect`、`clear: Color | Depth | Skybox | None`、`culling_mask: RenderLayerMask`、`volume_mask`。
- 帧循环改造:按 render_order 排序的相机序列 → 每相机一次 graph 执行(共享 compiled graph 缓存,按相机配置键控);Overlay 相机复用 Base 的 color/depth attachment(load 而非 clear),计划 01 的资源声明天然表达。
- `RenderQueueValue(u16)`:材质资产新增字段,默认按材质域映射(opaque→2000、alpha-test→2450、transparent→3000);queue 段→phase 映射表固化在 `core_pipeline`;段内值参与 sort_key。
- 统一排序定义(写入计划 02 的 sort_key 打包):
  `[相机 render_order] > [queue 段/phase] > [2D: sorting_layer → order_in_layer → y/深度] / [3D opaque: pipeline 聚簇 → 深度前向] / [3D transparent: 深度后向 → queue 内序]`;
  world-space UI 作为 transparent queue 的普通成员参与 3D 排序,screen-space UI 维持既有闭环链路在 graph 末端。
- RenderLayer 定稿为 32 位 mask;相机/灯光/volume/渲染器四方共用同一类型(已有 `RenderLayer` 扩展,不新造)。

## 里程碑

### CO-M1 相机契约与多相机循环

实施切片:
1. 相机契约扩展(order/type/stack/target/viewport/clear/masks);extract 与编辑器相机面板字段对接。
2. 帧循环按 render_order 多相机执行;RT 相机(离屏)路径与既有 offline/viewport 渲染收敛。

测试阶段:
- `cargo check -p zircon_runtime --lib --locked`;`cargo test -p zircon_runtime camera --locked` + `render_product` 回归
- 验收证据:双相机(主 + RT)各自产物正确;render_order 交换后合成顺序改变(对拍断言)。

### CO-M2 相机栈与 Overlay 合成

实施切片:
1. Base/Overlay 栈:Overlay 不 clear color、深度策略可选;栈内顺序生效。
2. viewport rect 与多相机分屏。

测试阶段:
- `cargo test -p zircon_runtime camera --locked`(栈合成:overlay 内容叠于 base 之上的像素断言)
- 验收证据:武器手/HUD 相机叠加用例;分屏用例。

### CO-M3 RenderQueue 与统一排序键

实施切片:
1. 材质 queue 字段与段→phase 映射;计划 02 sort_key 打包按本计划定义重排。
2. sprite/world-space UI 并入统一排序;sorting layer/order in layer 契约进 `core_pipeline`(2D 细则由计划 14 消费)。

测试阶段:
- `cargo test -p zircon_runtime phase --locked`(queue 覆写改变绘制顺序的断言;透明排序正确性)
- 验收证据:自定义 queue=2900 的 opaque 材质绘于 transparent 之前最后;sprite 与透明 mesh 混排顺序正确。

### CO-M4 layer 过滤全线贯通

实施切片:
1. culling mask 进计划 04 可见性(per-view 过滤);灯光 layer(计划 05)、volume layer(计划 07)对接同一 mask 类型。

测试阶段:
- `cargo test -p zircon_runtime visibility --locked` 与 light/volume 范围回归
- 验收证据:同场景两相机不同 mask 看到不同对象集合;灯光 mask 只照亮指定 layer。

## 工程落地细化

本章是本计划的实施权威(index.md §8 第 7 条)。`RenderQueueValue` 数值段与 `sort_key: u64` 位段的全局权威即本章(index.md §8 第 4/5 条);计划 02(`MeshDrawCommand` 命令排序)、计划 10(合批切分)、计划 14(2D sorting layer/y-sort)只消费本章布局,不得另造位段。术语:**域(domain)** = sort_key 中段的按 phase 族多态解释;**相机序列(camera sequence)** = 帧内按 `render_order` 解析出的 Base 相机列表及其各自的 Overlay 子列表。

### 模块与文件落点

新增文件:

| 文件 | 内容 |
|---|---|
| `zircon_runtime/src/core/framework/render/core_pipeline/render_queue.rs` | `RenderQueueValue(u16)` 常量段、材质覆写 clamp(±100)、段→`RenderPhase` 映射,含 `#[cfg(test)] mod tests` |
| `zircon_runtime/src/core/framework/render/core_pipeline/packed_sort_key.rs` | 位段常量(shift/width)、`packed_sort_key_u64` 唯一打包入口、各域编码 helper、与 breakdown 共用的解码函数,含 `#[cfg(test)] mod tests` |
| `zircon_runtime/src/core/framework/render/camera_stack.rs` | `CameraRenderType` / `RenderCameraClear` / `CameraRenderDescriptor` / `resolve_camera_sequence`(栈校验与 Base/Overlay 合成规则),含 `#[cfg(test)] mod tests` |
| `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop.rs` | 帧内多相机循环:相机序列 → 逐 Base 相机 graph 执行 → 逐 Overlay 相机 graph 执行(复用 Base attachment) |

修改文件:

| 文件 | 改动 |
|---|---|
| `core/framework/render/camera.rs` | `ViewportCameraSnapshot` 收缩为投影/曝光/MSAA/动态分辨率载荷:`order`/`target`/`viewport`/`clear_color`/`render_layers` 五字段迁出至 `CameraRenderDescriptor`(硬切换,同变更迁移调用方);删除对应 serde default 函数 |
| `core/framework/render/camera_ordering.rs` | `RenderCameraOrderInput` 改持 `CameraRenderDescriptor`;`SortedRenderCamera` 增 `render_type`;Overlay 相机不再独立成序(只随 Base 出现) |
| `core/framework/render/core_pipeline/mod.rs` | 声明 `render_queue`/`packed_sort_key` 模块与 re-export(仅 wiring) |
| `core/framework/render/core_pipeline/phase_sort.rs` | `RenderPhaseSortKey` 改为 `u64` newtype 薄层(`raw() -> u64`);删除 i128 打包、`signed_15/23/35`、`render_queue_sort_key` 等全部 i128 helper;`RenderPhaseSortComponents` 字段按本章定稿(见下) |
| `core_pipeline/phase_sort_key_breakdown.rs`、`phase_sort_decision.rs`、`phase_sort_decision_field.rs` | breakdown/decision 字段按新位段(camera_order/queue/domain/tie)重写,解码函数从 `packed_sort_key.rs` 复用 |
| `core_pipeline/phase_queue.rs` | `MeshPhaseInput`/`SpritePhaseInput` 的 `render_queue: i32`+`material_queue: i32` 合并为 `queue: RenderQueueValue`;增 `camera_order`/`sorting_layer`/`y_sort` 输入;`into_phase_item` 改调 `packed_sort_key_u64` |
| `core_pipeline/phase_queue_ordering_key.rs` | `raw_sort_key()` 返回 `u64`;entity 全量比较保留为最终决胜 |
| `core_pipeline/render_phase.rs` | `RenderPhase::mesh_phase(pipeline, alpha_mask, transparent)` 布尔签名删除,改 `RenderQueueValue::phase(pipeline)` 驱动 |
| `core/framework/render/frame_extract.rs` | `RenderViewExtract` 增 `cameras: Vec<CameraRenderDescriptor>`;`from_camera` 内部构造单元素序列(editor viewport 单相机路径不变) |
| `core/framework/render/material/`(材质快照模块) | 材质快照增 `render_queue: Option<RenderQueueValue>` 覆写字段 + 导入期 queue×混合态矛盾校验(对应风险条目) |
| `core/framework/render/scene_extract.rs` | `RendererCommon.render_queue_override: Option<RenderQueueValue>`(计划 10 契约对齐本类型);灯光/volume/渲染器 mask 字段统一 `RenderLayerSet` |
| `graphics/types/viewport_render_frame.rs` | `camera()` 访问器改返回主相机 `CameraRenderDescriptor`;`previous_motion_vector_camera` 同步换型 |
| `graphics/runtime/render_framework/create_viewport/create.rs` | viewport 创建按 descriptor 的 `target`/`viewport_rect` |
| `graphics/runtime/render_framework/submit_frame_extract/submit/submit.rs` | 单相机提交改为调 `camera_loop.rs` 序列循环 |
| `graphics/pipeline/compiled_graph_cache.rs`(计划 01 所有) | `CompiledGraphCacheKey` 增 camera target 成分(见"帧时序与集成点") |

### 核心类型与接口

契约归属:类型全部在 `core/framework/render`(camera.rs / camera_stack.rs / core_pipeline),无 `wgpu`;执行归属:render_framework 的 camera_loop 与 viewport 路径。

```rust
// core/framework/render/camera_stack.rs —— 相机契约(对齐 URP UniversalAdditionalCameraData)
pub enum CameraRenderType { Base, Overlay }

pub enum RenderCameraClear {
    Skybox,            // 清 depth,天空盒覆盖 color(MSAA>1 时额外清 color,见精读笔记)
    Color(Vec4),       // 清 color + depth
    DepthOnly,         // 仅清 depth
    None,              // 都不清(load 上一相机/上一帧内容)
}

pub struct CameraRenderDescriptor {
    pub entity: Option<EntityId>,           // 场景相机实体;editor 合成相机为 None
    pub render_order: i32,                  // 相机间先后(Unity camera depth 语义),升序执行
    pub render_type: CameraRenderType,
    pub stack: Vec<EntityId>,               // 仅 Base 有效;Overlay 必须为空(resolve 校验)
    pub target: RenderCameraTarget,         // 复用既有 PrimarySurface | Texture(handle) | Headless
    pub viewport_rect: Option<RenderViewportRect>, // 复用既有类型;None = 全目标
    pub clear: RenderCameraClear,           // Base 相机消费;Overlay 忽略 color 部分
    pub clear_depth: bool,                  // 仅 Overlay 消费(URP clearDepth 语义)
    pub culling_mask: RenderLayerSet,       // 计划 04 per-view 过滤消费
    pub volume_mask: RenderLayerSet,        // 计划 07 VolumeEvaluator 消费
    pub camera: ViewportCameraSnapshot,     // 投影/曝光/HDR/MSAA/动态分辨率载荷(收缩后)
}

/// 校验并合成相机序列:Base 按 render_order 升序;每个 Base 携带其 stack 解析出的
/// Overlay 列表(保持 stack 内顺序)。违规(Overlay 带 stack、stack 引用非 Overlay、
/// stack 成员 target 与 Base 不一致)记入 report 并剔除该成员,不 panic。
pub fn resolve_camera_sequence(
    cameras: &[CameraRenderDescriptor],
) -> CameraSequenceReport; // { sequence: Vec<CameraSequenceEntry>, violations: Vec<...> }

pub struct CameraSequenceEntry {
    pub base: CameraRenderDescriptor,
    pub overlays: Vec<CameraRenderDescriptor>,
}
```

```rust
// core/framework/render/core_pipeline/render_queue.rs —— queue 数值权威(index.md §8 第 4 条)
#[derive(Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize)]
pub struct RenderQueueValue(pub u16);

impl RenderQueueValue {
    pub const BACKGROUND: Self = Self(1000);
    pub const GEOMETRY: Self = Self(2000);
    pub const ALPHA_TEST: Self = Self(2450);
    pub const GEOMETRY_LAST: Self = Self(2500);   // 段边界:≤2500 走不透明族
    pub const TRANSPARENT: Self = Self(3000);
    pub const OVERLAY: Self = Self(4000);
    pub const MAX: Self = Self(5000);

    /// 材质默认映射:Opaque→GEOMETRY、Mask→ALPHA_TEST、Blend→TRANSPARENT。
    pub fn from_alpha_mode(mode: &RenderMaterialAlphaMode) -> Self;
    /// 材质覆写偏移,clamp 到 ±100(index.md §8 第 4 条)。
    pub fn with_material_offset(self, offset: i16) -> Self;
    /// 段→phase 映射(唯一权威):
    ///   0..=2449  → Opaque{2d,3d};2450..=2500 → AlphaMask{2d,3d};
    ///   2501..=3999 → Transparent{2d,3d};4000.. → Overlay。
    pub fn phase(self, pipeline: CorePipelineKind) -> RenderPhase;
}
```

`RenderLayer` 定稿:`RenderLayer = u32` 保持**layer 索引**语义(0 起);掩码类型统一为既有 `RenderLayerSet`(支持 ≥32 layer)。四处同一语义与 extract 传递:

| 消费方 | 字段 | 传递路径 |
|---|---|---|
| 相机 | `CameraRenderDescriptor.culling_mask` | extract view → 计划 04 per-view 可见性过滤 |
| 灯光 | 灯光 extract 快照 `layer_mask: RenderLayerSet` | `frame_extract.lighting` → 计划 05 light/shadow 过滤 |
| volume | volume extract `volume_mask: RenderLayerSet` × 相机 `volume_mask` 相交 | 计划 07 `VolumeEvaluator` |
| 渲染器 | `RendererCommon.layer_mask: RenderLayerSet` | 计划 10 extract 基座 → 可见性矩阵行 |

GPU 侧或 ABI 边界需要 u32 位掩码时,经既有 `RenderLayerSet::to_legacy_mask_lossy()` 降级;layer ≥32 仅参与 CPU 侧过滤。注:计划 07 细化章节的 `volume_mask`/`camera_volume_mask` 已统一为 `RenderLayerSet`,与本定稿一致。

### 排序键位段定稿(本计划核心交付)

`sort_key: u64` 全局位段权威表,从高到低:

| 位区间 | 宽度 | 字段 | 编码 |
|---|---|---|---|
| 63–56 | 8 | `camera_order` | `(render_order.clamp(-128, 127) + 128) as u64`。URP camera depth 实践范围 [-100,100];同一相机的命令列表内本段恒定(不影响相机内排序),保留它使跨相机合并诊断与全局重放有全序;同 order 歧义由 `camera_ordering.rs` ambiguity 报告兜底 |
| 55–43 | 13 | `queue` | `RenderQueueValue.0.min(8191) as u64`。覆盖 0..5000 实用域;queue 段同时决定 phase(`RenderQueueValue::phase`),段内数值直接参与排序 → "queue=2900 的材质绘于 transparent 段内最前"自然成立 |
| 42–10 | 33 | `domain`(按 phase 族多态) | 见下三张子表 |
| 9–0 | 10 | `tie_breaker` | `entity_tie_breaker & 0x3ff`。配合 Rust `sort_by_key` 稳定排序 + extract 顺序确定性;`RenderPhaseQueueOrderingKey` 保留 entity 全量比较作诊断路径最终决胜 |

**域 A:3D 不透明族**(`Opaque3d`/`AlphaMask3d`/`Prepass`/`Shadow`/`Deferred`)——pipeline/material 聚类优先,深度前到后:

| 位区间 | 宽度 | 字段 | 编码 |
|---|---|---|---|
| 42–33 | 10 | `pipeline_cluster` | `pipeline_variant & 0x3ff`(mesh_pipeline_cache 变体索引低 10 位) |
| 32–25 | 8 | `material_cluster` | `((material_discriminant >> 8) ^ material_discriminant) & 0xff`(u16 折叠 8 位) |
| 24–10 | 15 | `depth_f2b` | `clamp(round(effective_depth * 8.0), 0, 32767)`,1/8 m 步长、4096 m 量程,前到后升序 |

**域 B:3D 透明族**(`Transparent3d`,world-space UI 作为 transparent queue 普通成员走此域)——深度后到前主导:

| 位区间 | 宽度 | 字段 | 编码 |
|---|---|---|---|
| 42–20 | 23 | `depth_b2f` | `8_388_607 - clamp(round(effective_depth * 1000.0), 0, 8_388_607)`,mm 步长、8388 m 量程,后到前 |
| 19–10 | 10 | `pipeline_cluster` | 同域 A;仅在等深桶内聚簇,不破坏混合正确性 |

**域 C:2D 族**(`Opaque2d`/`AlphaMask2d`/`Transparent2d`)——sorting_layer → order_in_layer → y-sort(计划 14 消费):

| 位区间 | 宽度 | 字段 | 编码 |
|---|---|---|---|
| 42–35 | 8 | `sorting_layer` | `(sorting_layer.clamp(-128, 127) + 128) as u64` |
| 34–20 | 15 | `order_in_layer` | `(order_in_layer.clamp(-16_384, 16_383) + 16_384) as u64` |
| 19–10 | 10 | `y_sort` | 启用时 `(round(y * Y_SORT_UNITS).clamp(-512, 511) + 512) as u64`(y 大者键大→后绘,Godot 语义;`Y_SORT_UNITS = 16.0` 常量由计划 14 调整);未启用填中值 512 |

**域 D:UI/Overlay 族**(`Ui`/`Overlay` phase;screen-space UI 既有闭环不经此键,本域服务 queue≥4000 的材质与 overlay 内容)——ui z-index 映射进 Overlay 域的规则:queue 段固定为 `OVERLAY`(4000)基段(材质可 ±100 覆写),z-index 全量进 domain:

| 位区间 | 宽度 | 字段 | 编码 |
|---|---|---|---|
| 42–20 | 23 | `z_index` | `(ui_z_index.clamp(-4_194_304, 4_194_303) + 4_194_304) as u64`(与旧 `signed_23` 同宽,无损迁移) |
| 19–10 | 10 | 保留 | 填 0 |

域选择函数:`Transparent3d → B`;`*2d → C`;`Ui | Overlay → D`;其余(含 `PostProcess`/`Debug` 的少量命令)→ A。

**编码函数(签名与计划 02 已锁定的一致,不改)**:

```rust
// core/framework/render/core_pipeline/packed_sort_key.rs —— 唯一 u64 打包入口
pub const SORT_KEY_CAMERA_ORDER_SHIFT: u32 = 56;
pub const SORT_KEY_QUEUE_SHIFT: u32 = 43;
pub const SORT_KEY_DOMAIN_SHIFT: u32 = 10;

pub fn packed_sort_key_u64(
    phase: RenderPhase,
    components: RenderPhaseSortComponents,
    pipeline_variant: u32,      // 域 A/B 聚簇;域 C/D 忽略
    material_discriminant: u16, // 域 A 聚簇;其余域忽略
) -> u64;
```

`RenderPhaseSortComponents` 定稿(CO-M3 与唯一生产方 `phase_queue.rs` 同变更切换):

```rust
pub struct RenderPhaseSortComponents {
    pub camera_order: i32,        // 新增:CameraRenderDescriptor.render_order
    pub queue: RenderQueueValue,  // 合并替换 render_queue/material_queue 两个 i32
    pub sorting_layer: i32,       // 新增:域 C
    pub order_in_layer: i32,
    pub y_sort: Option<f32>,      // 新增:域 C
    pub depth: f32,
    pub depth_bias: f32,          // effective_depth = depth + depth_bias(保留)
    pub ui_z_index: i32,
    pub entity_tie_breaker: u64,
}
```

**从 i128 旧键的保序量化迁移方案**:

1. **queue 合并**:旧键 `render_queue`(15 bit,bit 112 起)高于 `material_queue`(15 bit)。新键单一 `queue` = 材质基础段 `from_alpha_mode` + `with_material_offset`。现有唯一生产方 `phase_queue.rs` 以"段值 + 段内偏移"填充两字段,合并是单调映射 → 保序。
2. **透明深度 23 bit**:沿用旧 `depth_sort_key` 的 `round(depth * 1000)` 量化(mm),仅把旧 signed_35 量程裁到 8388 m;默认 `z_far = 200 m` 场景内逐项保序,无回归。
3. **不透明深度降为 15 bit 粗量化(1/8 m)**:**有意非保序**。依据:opaque 深度序只是 early-z/overdraw 性能提示,正确性由 depth buffer 保证(URP `canSkipFrontToBackSorting` 在 HSR GPU 上整个放弃距离排序,见精读笔记);把高位让给 pipeline/material 聚簇,状态切换收益 > 精确深度序。等深桶内由 tie + 稳定排序定序。
4. **聚类位宽取舍**:`pipeline_cluster` 10 bit = 1024 变体/相机/phase,对照 mesh_pipeline_cache 现存变体数(双位数量级)余量 >10×;`material_cluster` 8 bit 分桶。两者折叠冲突只降低聚簇率(多一次状态切换),不影响绘制正确性 —— 这是 33 bit 域预算下牺牲冲突率换深度位的依据。
5. **为什么是绝对量化而非按 z_near/z_far 归一**:归一需要 per-view 参数进函数签名,破坏与计划 02 锁定的签名;且计划 02 MD-M3 缓存的静态 opaque 命令带陈旧深度位,绝对量化下陈旧度有界、归一量化在相机 z 范围变化时整体失真。
6. **`order_in_layer` 23→15 bit**:clamp ±16383(Unity order in layer 实践为 i16 域);extract 侧越界 `debug_assert` + clamp。`ui_z_index` 23→23 无损。`tie` 16→10 bit,由稳定排序兜底。
7. **对拍迁移测试**:`render_sort_key_matches_legacy_i128_order_on_representative_set` 用覆盖四域的样本分别按旧 i128 与新 u64 排序,断言除第 1/3 条声明的有意差异外顺序逐项一致;该测试随 i128 路径删除而转为固定快照断言。

若计划 02 的过渡实现(i128 分段压缩 u64)已先落地,CO-M3 只替换 `packed_sort_key_u64` 函数内部为本位段,消费方零改动;若本计划先行,则直接以本位段实现,02 落地时直接消费。

### 帧时序与集成点

宿主链路:`WgpuRenderFramework::submit_frame_extract`(`submit/submit.rs`)→ 新 `camera_loop.rs` → 逐相机 `SceneRendererCore` graph 执行。帧内顺序:

1. **extract**:scene 侧投影全部活动相机为 `RenderViewExtract.cameras: Vec<CameraRenderDescriptor>`;editor viewport 路径经 `from_camera` 退化为单 Base 相机,行为不变。
2. **序列解析**:`resolve_camera_sequence` 产出 Base 序列(render_order 升序;同 order 沿用 `camera_ordering.rs` 的 target 维度次序 + ambiguity 报告)。RT 相机(`target = Texture | Headless`)是普通 Base 序列项;其输出 texture 由消费方 graph import——若 RT 相机 render_order 晚于消费者,消费者读到上一帧内容(与 Unity 同 depth 行为一致,文档约定 RT 相机应取更小 render_order)。
3. **每 Base 相机一次 graph 执行**:`CompiledGraphCache::get_or_compile`(计划 01),**缓存键扩展点**:键成分增加 camera target 描述 = `RenderCameraTargetOrderKey`(kind + 尺寸/格式)+ `viewport_rect` 有无 + `render_type`。`render_order`/`culling_mask`/`volume_mask`/clear 颜色值是运行时参数,不进键(对应风险条目"缓存膨胀")。clear 策略翻译为首个 color/depth pass 的 load op:`Skybox→depth clear + sky pass`、`Color→clear(color)+clear(depth)`、`DepthOnly→load(color)+clear(depth)`、`None→load+load`。
4. **Overlay 子循环**:每个 Overlay 相机独立一次 graph 执行,color/depth attachment 以 import 方式复用 Base 的目标资源;color 恒 `load`(Overlay 永不清 color),depth 按 `clear_depth` 选 `clear|load`(URP `GetCameraClearFlag` Overlay 分支语义)。Overlay 继承 Base 的 target 与 viewport_rect(URP 以 baseCamera 构造 overlayCameraData 的对应物),自身仅贡献 view 矩阵、culling/volume mask 与深度策略。
5. **帧末**:screen-space UI 既有闭环 graph 末端节点只挂在"最后一个 PrimarySurface Base 相机"的执行上(URP `isLastBaseCamera` 语义);present/writeback 路径不动。

**硬切换删除项**(各切片落地同变更执行):

- `ViewportCameraSnapshot` 的 `order`/`target`/`viewport`/`clear_color`/`render_layers` 字段与其 serde default 函数(场景资产经 importer 一次性升级映射,不留双读路径)。
- `RenderPhase::mesh_phase(pipeline, alpha_mask, transparent)` 布尔签名。
- `phase_sort.rs` 的 i128 打包路径、`signed_15/23/35`、`RenderPhaseSortKey::new(raw: i64)`/`raw() -> i128`。
- `camera_ordering.rs` 中以 `ViewportCameraSnapshot` 为输入的 `RenderCameraOrderInput` 旧形态。
- `submit/submit.rs` 内单相机直通提交分支(由 camera_loop 单元素序列覆盖)。

### 实施切片细化

**CO-M1 相机契约与多相机循环**

| 切片 | 触碰文件 | 改动要点 | 完成判据 |
|---|---|---|---|
| M1-S1 契约迁移 | `camera_stack.rs`(新)、`camera.rs`、`camera_ordering.rs`、`frame_extract.rs`、scene 相机投影与编辑器相机面板字段 | `CameraRenderDescriptor` 三类型落地;snapshot 五字段迁出并删除;extract 增 `cameras` | `cargo check -p zircon_runtime --lib --locked` 通过;grep 无 `ViewportCameraSnapshot` 上的 `target`/`order` 残留访问 |
| M1-S2 多相机循环 | `camera_loop.rs`(新)、`submit/submit.rs`、`create_viewport/create.rs`、`compiled_graph_cache.rs` | 序列循环替换单相机提交;缓存键加 target 成分;RT 相机走普通序列项 | 双相机(主 + RT)帧各自产物正确;单相机路径回归不变 |

测试阶段:`cargo test -p zircon_runtime camera --locked` + `render_product` 回归;render_order 交换对拍断言。

**CO-M2 相机栈与 Overlay 合成**

| 切片 | 触碰文件 | 改动要点 | 完成判据 |
|---|---|---|---|
| M2-S1 栈合成 | `camera_stack.rs`、`camera_loop.rs` | `resolve_camera_sequence` 校验三规则;Overlay attachment import + load 语义 + `clear_depth` 分支 | overlay 内容叠于 base 之上像素断言;违规栈进 report 不 panic |
| M2-S2 分屏 | `camera_loop.rs`、`create_viewport/create.rs` | `viewport_rect.clamped_to_size` 应用到 attachment viewport/scissor | 双 Base 相机各占半屏像素断言 |

测试阶段:`cargo test -p zircon_runtime camera --locked`。

**CO-M3 RenderQueue 与统一排序键**

| 切片 | 触碰文件 | 改动要点 | 完成判据 |
|---|---|---|---|
| M3-S1 queue 契约 | `render_queue.rs`(新)、材质快照模块、`render_phase.rs`、`phase_queue.rs` | `RenderQueueValue` + 材质覆写字段 + 导入期矛盾校验;`mesh_phase` 改 queue 驱动并删旧签名 | check 通过;queue=2900 材质落 Transparent 段最前 |
| M3-S2 u64 位段定稿 | `packed_sort_key.rs`(新)、`phase_sort.rs`、`phase_sort_key_breakdown.rs`、`phase_sort_decision*.rs`、`phase_queue_ordering_key.rs` | 本章位段落地;i128 路径全删;breakdown 按新段重写;(计划 02 已落地时仅改函数内部) | check 通过;对拍迁移测试通过;repo 内 `i128` 在 core_pipeline 下无残留 |

测试阶段:`cargo test -p zircon_runtime phase --locked` + `cargo test -p zircon_runtime render_sort_key --locked`。

**CO-M4 layer 过滤全线贯通**

| 切片 | 触碰文件 | 改动要点 | 完成判据 |
|---|---|---|---|
| M4-S1 mask 贯通 | `scene_extract.rs`、计划 04 可见性入口、计划 05 灯光 extract、计划 07 volume extract | 四处字段统一 `RenderLayerSet`;per-view 过滤用 `culling_mask.intersects` | 两相机不同 mask 对象集合断言;灯光 mask 过滤断言 |

测试阶段:`cargo test -p zircon_runtime visibility --locked` + light/volume 范围回归。

### 测试与验收清单

单测(`render_<topic>_*` 命名,index.md §8 第 6 条):

| 测试函数 | 断言 | 位置 |
|---|---|---|
| `render_sort_key_camera_order_dominates_queue` | 不同 camera_order 的项排序先于一切 queue/深度差异 | `core_pipeline/packed_sort_key.rs` |
| `render_sort_key_queue_segment_maps_to_phase` | 1000/2000/2450/2500/2501/3000/4000 各值经 `phase()` 落入预期 `RenderPhase` | `core_pipeline/render_queue.rs` |
| `render_queue_material_offset_clamped_to_100` | offset=±150 被 clamp 到 ±100 | `core_pipeline/render_queue.rs` |
| `render_sort_key_opaque_clusters_pipeline_before_depth` | 两 pipeline 三深度样本先按 cluster 聚簇、簇内深度升序 | `core_pipeline/packed_sort_key.rs` |
| `render_sort_key_transparent_depth_back_to_front_ignores_cluster` | 透明排序深度降序主导,cluster 仅等深决胜 | 同上 |
| `render_sort_key_2d_sorting_layer_then_order_then_y` | 域 C 三级字典序成立;y 大者后绘 | 同上 |
| `render_sort_key_ui_z_index_maps_into_overlay_segment` | queue=OVERLAY 时 z_index 全序保持且不越出 domain 位 | 同上 |
| `render_sort_key_matches_legacy_i128_order_on_representative_set` | 迁移保序(声明差异除外),见迁移方案第 7 条 | 同上 |
| `render_sort_key_breakdown_roundtrip` | 打包→breakdown 解码逐字段还原 | `core_pipeline/phase_sort_key_breakdown.rs` |
| `render_camera_sequence_sorts_by_render_order` | 序列按 render_order 升序;同 order 报 ambiguity | `core/framework/render/camera_stack.rs` |
| `render_camera_stack_overlay_follows_base` | Overlay 紧随其 Base,保持 stack 内顺序 | 同上 |
| `render_camera_stack_rejects_invalid_members` | Overlay 带 stack / stack 引用 Base / target 不一致 → 进 violations 且被剔除 | 同上 |
| `render_camera_clear_overlay_never_clears_color` | Overlay 翻译出的 load op:color 恒 load;depth 按 clear_depth | `camera_loop.rs` |
| `render_camera_viewport_rect_clamped` | 越界 viewport_rect 被 clamp 进目标尺寸 | `camera.rs`(既有 `clamped_to_size` 扩展) |

产物对拍(`render_product_*` + `ZR_RENDERDOC_CAPTURE_NEXT=1` 人工比对):

| 场景 | 验收 |
|---|---|
| `render_product_dual_camera_rt_then_main` | RT 相机(order=-1)产物被主相机材质采样,内容正确 |
| `render_product_camera_render_order_swap_changes_composite` | 交换两 Base 相机 render_order 后合成层次反转 |
| `render_product_overlay_stack_composites_over_base` | 武器手/HUD overlay 叠加于 base,depth 按 clear_depth 两种策略各一例 |
| `render_product_split_screen_viewports` | 双相机各占半屏,边界无串扰 |
| `render_product_queue_override_reorders_draws` | queue=2900 不透明材质绘于 Geometry 之后、Transparent 之前(抓帧事件序) |

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-06-24 | Render product submit profile tests owner split | render_plan09_product_submit_profile_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | `graphics/tests/render_product_submit.rs` 从 765 行 near-threshold submit/product profile mixed owner 收敛为 506 行 base submit/material diagnostics parent，只保留 direct extract authority、unknown viewport error、core pipeline selection、quality override mismatch、PBR material/light stats、material diagnostics 与 shared snapshot/material fixtures，并通过 `mod profiles;` 挂载 child owner。新增 263 行 `graphics/tests/render_product_submit/profiles.rs` 承接 Default/Headless/Advanced/Solari profile acceptance tests 以及 default Core3D/Core2D/UI acceptance extract helpers。新增 `runtime_15_render_product_submit_profile_tests_are_child_owner` 锁定 moved tests/helper 不回流、父/子 800 行预算和 Plan 09/render index/structure/review/render-product-submit/session docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan、conflict-marker scan 和 scoped diff-check 已通过；diff-check 仅有 Git LF->CRLF 提示。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_render_product_submit_profile_tests_are_child_owner` 与 `render_product_submit` profile focused product tests；后续 profile acceptance 覆盖继续进入 `render_product_submit/profiles.rs` 或继续拆 child owner，不得回流父文件。 |
| 2026-06-24 | M4 behavior postprocess tests owner split | render_plan09_m4_behavior_postprocess_tests_owner_split_static_passed_cargo_deferred_implementation_cadence | `graphics/tests/m4_behavior_layers.rs` 从 796 行 near-threshold M4 behavior mixed test owner 收敛为 678 行 fixture/offline-bake parent，只保留 shared WGPU fixture/helpers、offline bake 产品守卫和 `mod particles/postprocess/queue_override/transparent3d` 挂载。新增 127 行 `graphics/tests/m4_behavior_layers/postprocess.rs` 承接 bloom quality profile 与 color-grading extract 两条 post-process 产品测试。新增 `runtime_15_m4_behavior_postprocess_tests_are_child_owner` 锁定 moved tests 不回流、M4 behavior 五个 owner 800 行预算和 Plan 09/render index/structure/review/session docs anchors。 | scoped rustfmt、static owner scan、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan、conflict-marker scan 和 scoped diff-check 已通过；diff-check 仅有 Git LF->CRLF 提示。Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_m4_behavior_postprocess_tests_are_child_owner`、`m4_behavior_layers` focused WGPU tests 与 Plan 09 postprocess/render-order product sweep；后续 M4 行为测试继续进入 child owners,不得回流父文件。 |
| 2026-06-24 | Surface targets texture-target tests owner split | render_plan09_surface_targets_texture_target_test_owner_split_static_passed_cargo_deferred_implementation_cadence | `graphics/tests/surface_targets.rs` 从 874 行降到 441 行,继续承载 surface/no-op/headless/present-source tests、shared fixture/helper 与 `mod texture_target;` 挂载。新增 452 行 `graphics/tests/surface_targets/texture_target.rs` 承接 texture-target missing/sampled-only/non-renderable asset errors、metadata-driven capture sizing、sRGB direct graph import、Base+Overlay composite、independent texture target writeback 和 texture-target present fallback tests。结构守卫 `runtime_15_surface_targets_texture_target_tests_are_child_owner` 锁定 moved-test ownership、父/子 800 行预算和 Plan 09/render index/structure/review/render-product docs status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、stale-path scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 按 milestone implementation cadence 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_surface_targets_texture_target_tests_are_child_owner`、`surface_targets` focused tests 与 camera-target WGPU guard group;后续 texture-target tests 继续进入 child owner,不得回流父文件。 |
| 2026-06-24 | Plan 09 frame submission context tests owner split | render_plan09_frame_submission_context_tests_owner_split_static_passed_cargo_deferred_active_compile_lane | `graphics/runtime/render_framework/submit_frame_extract/frame_submission_context.rs` 从 763 行降到 455 行,继续承载 `FrameSubmissionContext`、`UiSubmissionStats`、shared `source_extract`、view visibility、advanced-provider reports、TAA jitter helper 与 `#[cfg(test)] mod tests;` 挂载。新增 307 行 `graphics/runtime/render_framework/submit_frame_extract/frame_submission_context/tests.rs` 承接 advanced runtime plan gating、VG/HGI payload source sanitizing、view visibility lookup 和 TAA jitter inactive/exact coverage。结构守卫 `runtime_15_render_frame_submission_context_tests_are_child_owner` 锁定 moved-test ownership、父/子 800 行预算和 Plan 09/render index/structure/review/advanced/anti-alias docs status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active compile lanes 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_render_frame_submission_context_tests_are_child_owner`、`frame_submission_context` focused tests 与 F3 submit-context focused lane;后续 context 新测试继续进入 child owner,不得回流父文件。 |
| 2026-06-24 | Plan 09 update-base-stats post-process diagnostics owner split | render_plan09_update_base_stats_post_process_diagnostics_owner_split_static_passed_cargo_deferred_active_compile_lane | `graphics/runtime/render_framework/submit_frame_extract/update_stats/base_stats.rs` 从 773 行降到 665 行,继续承载 `update_base_stats(...)`、graph/visibility/HZB/light/UI/material/mesh queue stats 写入和测试挂载;新增 117 行 `graphics/runtime/render_framework/submit_frame_extract/update_stats/base_stats/post_process_diagnostics.rs` 承接 effect-stack resource status、SSR/motion-vector graph resource detection、particle velocity missing/anonymous-stream diagnostics helper。结构守卫 `runtime_15_render_update_base_stats_post_process_diagnostics_is_child_owner` 锁定 moved-helper ownership、父/子 800 行预算和 Plan 09/render index/structure/review/render-product docs status anchors。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active compile lanes 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_render_update_base_stats_post_process_diagnostics_is_child_owner`、base-stats focused tests 与 render product diagnostics lane;后续 post-process/particle stats helper 新逻辑继续进入 child owner,不得回流父文件。 |
| 2026-06-24 | Plan 09 frame extract geometry owner split | render_plan09_frame_extract_geometry_owner_split_static_passed_cargo_deferred_active_compile_lane | `core/framework/render/frame_extract.rs` 从 894 行降到 682 行,保留 frame/view/sprite/post-process/particle/visibility DTO 与 snapshot adapter;新增 223 行 `core/framework/render/frame_extract/geometry.rs` 承接 `GeometryPhaseInput`、`GeometryExtract`、`StaticMeshBatchExtract`、static mesh batch key 与 `build_static_mesh_batches(...)`。根模块通过 `pub use geometry::{...}` 保持既有 `frame_extract::{GeometryExtract,...}` API。结构守卫 `runtime_15_frame_extract_geometry_is_child_owner` 锁定 moved geometry 不回流、父/子 800 行预算和 docs/status 锚点。 | scoped rustfmt/static owner scans、line-count scan、docs-anchor scan、touched-file whitespace scan 和 scoped diff-check 通过;Cargo/WGPU/RenderDoc 因 active compile lanes 暂缓且不计通过。 | Cargo lane 空闲后补跑 `runtime_15_frame_extract_geometry_is_child_owner` 与 frame-extract/visibility focused guards;后续 frame extract 新 DTO 继续优先进入 child owner,不得回流到根文件。 |
| 2026-06-24 | Plan 09 update-base-stats test owner split | render_plan09_update_base_stats_test_owner_split_static_passed_cargo_deferred_active_editor_lane: 已把 submit stats helper inline tests 移入 child owner,关闭 `base_stats.rs` 超 1000 行回流风险 | `graphics/runtime/render_framework/submit_frame_extract/update_stats/base_stats.rs` 从 1141 source lines 收敛到 753 source lines,继续只承载 `update_base_stats(...)`、visibility/static-index/HZB/post-process/particle velocity stats helper 与 `#[cfg(test)] mod tests;` 挂载。新增 `graphics/runtime/render_framework/submit_frame_extract/update_stats/base_stats/tests.rs` 承接 graph execution coverage、visibility/static-index/HZB occlusion、effect-stack resource status 和 particle velocity diagnostics tests。新增结构守卫 `runtime_15_render_update_base_stats_tests_are_child_owner`,锁定父/子 owner、moved test 不回流、800 行预算和 docs/status 锚点。 | scoped `rustfmt --edition 2021 --check`、父/子行数扫描、moved test owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过;当前外部 editor/plugin cargo/rustc 通道仍活跃,本切片不启动 Cargo/WGPU/RenderDoc,不计新的运行通过证据。 | 等 active Cargo lanes 清空后重跑 `runtime_15_render_update_base_stats_tests_are_child_owner` 与 base-stats focused tests;后续 submit stats helper 新测试继续进入 `base_stats/tests.rs`,不得回流父文件。 |
| 2026-06-24 | Plan 09 camera-loop test helper owner split | render_plan09_camera_loop_test_helper_owner_split_static_passed_cargo_deferred_active_editor_lane: 已把 selected-camera loop 剩余测试辅助函数继续下沉到 test child owner | `graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop.rs` 不再承载 `camera_loop_extracts(...)`、`camera_loop_frame_submissions(...)`、runtime-frame projection helpers、`camera_sequence_descriptors(...)` 或 `CameraLoopFrameSubmission`,父文件仅保留 production submit loop/streaming helpers 与 `#[cfg(test)] mod tests;`。`graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop/tests.rs` 保留 Base/Overlay 序列、extract projection、UI/output-owner 覆盖和共享 fixtures;新增 `graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop/tests/frame.rs` 承接 direct runtime-frame child projection tests 与 frame-only helpers。结构守卫 `runtime_15_render_submit_camera_loop_tests_are_child_owner` 同步锁定三文件 owner、moved helper 不回流、父/子 800 行预算和 docs/status 锚点。 | scoped `rustfmt --edition 2021 --check`、父/test root/frame child 行数扫描、moved helper owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过;当前外部 editor/plugin cargo/rustc 通道仍活跃,本切片继续不启动 Cargo/WGPU/RenderDoc,不计新的运行通过证据。 | 等 active Cargo lanes 清空后重跑 `runtime_15_render_submit_camera_loop_tests_are_child_owner` 与 `camera_loop` focused guard;后续 direct runtime-frame 新覆盖继续进入 `camera_loop/tests/frame.rs`,序列/extract 覆盖继续进入 `camera_loop/tests.rs`,不得回流父文件。 |
| 2026-06-24 | Plan 09 camera-loop test owner split | render_plan09_camera_loop_test_owner_split_static_passed_cargo_deferred_active_editor_lane: 已把 selected-camera loop 的内联测试移入 child owner,父文件回到生产编排职责 | `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop.rs` 不再承载 600+ 行内联测试,仅保留 selected-camera submit loop、direct runtime-frame loop、streaming child submission helpers 与 `#[cfg(test)] mod tests;` 挂载。新增 `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop/tests.rs`,承接 Base/Overlay flattening、selected descriptor projection、terminal UI routing、stack/viewport output owner、viewport-terminal target 与 direct runtime-frame child projection覆盖。新增结构守卫 `runtime_15_render_submit_camera_loop_tests_are_child_owner`,锁定父/子 owner 行数预算、moved test/helper 不回流、Plan 09/render index/结构规范/审查记录/模块文档状态锚点。 | scoped `rustfmt --edition 2021 --check`、父子行数扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过;当前外部 plugin/editor cargo/rustc 通道活跃,本切片不启动 Cargo/WGPU/RenderDoc,不计新的运行通过证据。 | 等 active Cargo lanes 清空后重跑 `runtime_15_render_submit_camera_loop_tests_are_child_owner` 与 `camera_loop` focused guard;后续 camera-loop 新行为必须继续落在 production owner,新增测试留在 child owner 或继续拆分,不得回流父文件。 |
| 2026-06-23 | Plan 05/09 shadow view-projection owner split | render_plan05_09_shadow_view_projection_owner_split_static_passed: 已把 `shadow/plan.rs` 中 directional/point/spot shadow view-projection 与 sanitizing helper 抽入 child owner | 新增 `zircon_runtime/src/graphics/scene/scene_renderer/shadow/view_projection.rs`,承接 `directional_cascade_view_projection(...)`、`spot_light_view_projection(...)`、`point_light_face_view_projection(...)`、`point_light_face_axes(...)`、方向 fallback、stable up-vector、finite vector 与 far-plane sanitizing;`shadow/plan.rs` 只保留 atlas allocation、slot pass、globals、light slot assignment 与调用点。新增结构守卫 `runtime_15_shadow_plan_view_projection_is_child_owner`,锁定 `mod view_projection;`、moved helpers 不回流、docs/status 锚点和 900 行预算。 | scoped rustfmt/static/line-count/docs-anchor checks 通过。`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan05-shadow-view-projection-0623 --message-format short --color never` 在 304s 工具窗口超时且无输出,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 后续 Plan 09/05 layer 与 shadow planning 新职责继续优先拆 child owner,不得回流到近阈值 `shadow/plan.rs`。 |
| 2026-06-23 | Plan 09 CO-M4 world visibility input owner split | render_plan09_world_visibility_input_owner_split_static_passed_cargo_timeout_no_result: 已把 scene world visibility input DTO 构造从近阈值 `scene/world/render.rs` 抽成 child owner; focused Cargo 在工具窗口超时且无测试结果 | 新增 `zircon_runtime/src/scene/world/render_visibility.rs`,承接 `build_visibility_input(...)`、`particle_emitter_render_layer_masks(...)` 和 `empty_visibility_input(...)`。`scene/world/render.rs` 只保留 `use super::render_visibility::{build_visibility_input, empty_visibility_input};` 与调用点,不再持有 visibility DTO 拼装、particle emitter layer 聚合或 `BTreeMap` owner。`scene/world/mod.rs` 挂载 `mod render_visibility;`。新增结构守卫 `runtime_15_scene_world_render_visibility_input_is_child_owner`,锁定 moved functions 不回流 `render.rs`、child owner 持有 `VisibilityRenderableInput` 与 `RenderLayerSet::union(...)`、相关 docs/status 锚点和 owner 行数预算。 | `rustfmt --edition 2021 --check` 覆盖 `scene/world/render.rs`、`scene/world/render_visibility.rs`、`scene/world/mod.rs` 与 `tests/runtime_absorption/structure_convention/production_file_budget.rs`;line-count: `scene/world/render.rs` 878、`scene/world/render_visibility.rs` 87、`scene/world/mod.rs` 34、`production_file_budget.rs` 441。静态扫描确认 `render.rs` 不再含 `build_visibility_input` / `particle_emitter_render_layer_masks` / `empty_visibility_input` 定义或 `BTreeMap<crate::scene::EntityId, RenderLayerSet>`; focused locked Cargo `cargo test -p zircon_runtime --lib runtime_15_scene_world_render_visibility_input_is_child_owner --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-world-visibility-owner-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 124s 工具窗口超时且无输出/无 `zircon_runtime*.exe` test binary; no residual matching target cargo/rustc after timeout, only unrelated plugin-runtime check remained active。不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 给足共享 lib-test 编译窗口后重跑 `runtime_15_scene_world_render_visibility_input_is_child_owner` 与 visibility input focused guard;后续 world render extract 新职责继续优先拆 `render_particles` / light / camera owner,不得回流到 900+ root。 |
| 2026-06-23 | CO-M4 VisibilityRenderableInput typed layer set | render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result: 已把 visibility input renderable DTO 从 legacy `u32` layer mask 收束为 `RenderLayerSet`; focused Cargo 单测超时无结果, locked check 被当前 `Cargo.lock` 漂移阻断 | `VisibilityRenderableInput.render_layer_mask` 现在直接携带 `RenderLayerSet` 且不再 `Copy`。`RenderFrameExtract::from_snapshot(...)`、visibility fallback `visibility_entries(...)` 与 `scene/world/render.rs::build_visibility_input(...)` 均 clone typed mesh/sprite layer set,构造 visibility input 时不再走 legacy mask conversion。particle emitter layer 聚合改为 `BTreeMap<EntityId, RenderLayerSet>` 并使用 `RenderLayerSet::union(...)`,无 sprite 的 emitter fallback 从现有 `default_render_layer_mask()` 包装 typed set。为避免继续堆叠 1000 行附近的 `frame_extract.rs`,原内联 frame-extract tests 已迁到 `core/framework/render/frame_extract/tests.rs`,新增 `render_frame_extract_visibility_input_preserves_layers_above_legacy_mask_width` 用 layer 40 锁定 snapshot adapter 不再丢失高位 layer。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 `frame_extract.rs`、`frame_extract/tests.rs`、`visibility_entries.rs`、`scene/world/render.rs` 通过;stale scans 确认 `VisibilityRenderableInput` 构造点不再出现 mesh/sprite `to_legacy_mask_lossy()`、`BTreeMap<..., u32>` 或 bitwise OR 聚合;line-count: `frame_extract.rs` 894、`frame_extract/tests.rs` 133、`visibility_entries.rs` 18、`scene/world/render.rs` 958。focused locked Cargo `cargo test -p zircon_runtime --lib render_frame_extract_visibility_input_preserves_layers_above_legacy_mask_width --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-visibility-input-typed-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 124s 工具窗口超时且无输出/无 `zircon_runtime*.exe` test binary;locked `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-visibility-input-typed-layer-0623-check --message-format short --color never` 被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,未进入编译,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 visibility input focused guard、batch-key/relevance layer 40 guards 与 broader visibility/custom-target layer filters;后续继续补真实 WGPU/RenderDoc 产品证据,并保留 scene authoring legacy entity mask 与 WGPU light-buffer 32-bit ABI 的明确边界。 |
| 2026-06-23 | CO-M4 VisibilityBatchKey typed layer set | render_plan09_visibility_batch_key_layer_set_static_passed_cargo_lock_blocked: 已把 visibility batch key 与 FrameVisibility layer 列表从 legacy `u32` 收束为 `RenderLayerSet`;focused Cargo 被当前 `Cargo.lock` 漂移在编译前阻断 | `VisibilityBatchKey.render_layer_mask` 现在直接持有 `RenderLayerSet`,并同步派生 `Clone`/`Debug`/`Eq`/`Ord`/`Hash` 而不再 `Copy`。`collect_batching_result.rs` 从 typed `RenderMeshSnapshot.render_layer_mask` clone 出 batch key、BVH instance 和 history entry;`FrameVisibility.render_layer_masks` 改为 `Vec<RenderLayerSet>`,custom-target `build_views.rs` 直接把 typed layer set 传给 `PrimitiveRelevance::view_visible_for_layers(...)`。`visible_batches_for_entities(...)` 与 draw-command planning 显式 clone batch key,避免通过 old Copy 语义隐藏 layer set ownership。新增 `visibility_batch_key_preserves_layers_above_legacy_mask_width`,用 layer 40 断言 `VisibilityBatchKey` 与 `FrameVisibility.render_layer_masks` 都保留 typed layer 且 `to_legacy_mask_lossy()` 为 0。该切片完成时 visibility input DTO 尚待 typed 收束,后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 已关闭该 DTO 边界。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 `camera.rs`、visibility batch key/context/batching/view/planning/static-index、graphics visibility tests 与 mesh build fixtures 通过;旧 batch-key 残留扫描确认无 `render_layer_mask: u32`、`render_layer_masks: Vec<u32>`、`from_legacy_mask(*render_layer_mask)`、`render_layer_mask: entry.render_layer_mask` 或 old batch-key 注释残留;`VisibilityBatchKey` 构造/clone 扫描确认无 move 回退;line-count: `construct.rs` 775、`view_context/mod.rs` 189、`view_context/build_views.rs` 366、`visibility_batch_key.rs` 10、`visibility_context.rs` 90、`build_draw_commands.rs` 21。scoped `git diff --check` 通过,仅 LF->CRLF 提示。focused locked Cargo `cargo test -p zircon_runtime --lib visibility_batch_key_preserves_layers_above_legacy_mask_width --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-visibility-batch-typed-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 与 locked `cargo check` 均被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,未进入编译,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 visibility batch-key focused guard、custom-target visibility layer filters 和 broader visibility tests;该层下一步转向 WGPU/RenderDoc 产品证据与 GPU ABI 收束。 |
| 2026-06-23 | CO-M4 PrimitiveRelevance typed layer filter | render_plan09_primitive_relevance_typed_layer_filter_static_passed_cargo_lock_blocked_timeout_no_result: 已把 shared relevance layer 判断从 legacy `u32` 输入改为 `RenderLayerSet` 引用;focused Cargo 未取得通过证据 | `PrimitiveRelevance::for_mesh_view(...)` 与 `view_visible_for_layers(...)` 现在接收 `&RenderLayerSet` 并用 `camera_layers.intersects(render_layers)` 判定主视图 layer 可见性。`collect_batching_result.rs` 从 typed `RenderMeshSnapshot.render_layer_mask` 直接生成 main-view relevance,保留 layer 32+ 身份。该 relevance 切片完成时 custom-target visibility 仍需从 legacy batch key 转回 typed set;后续 `render_plan09_visibility_batch_key_layer_set_static_passed_cargo_lock_blocked` 已把 `VisibilityBatchKey` 与 `FrameVisibility.render_layer_masks` 升级为 typed layer set,`VisibilityRenderableInput` / `build_visibility_input(...)` 已由后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 收束为 typed layer set。mesh pass processor/build tests 已同步 typed relevance fixture,新增 `primitive_relevance_preserves_layers_above_legacy_mask_width` 用 layer 40 锁定 relevance 本身不再经 lossy mask。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 `relevance.rs`、visibility batching/view context、mesh draw/build processor fixtures 通过;scoped `git diff --check` 通过,仅 LF->CRLF 提示;stale scan 确认 `intersects_legacy_mask(render_layer_mask)` 只剩 `scene/world/render.rs` authoring-side entity legacy boundary;line-count: `relevance.rs` 213、`view_context/build_views.rs` 368、`collect_batching_result.rs` 112。focused locked Cargo `cargo test -p zircon_runtime --lib primitive_relevance --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-relevance-typed-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 124s 工具窗口超时且未返回通过证据;后续 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-relevance-typed-layer-0623-check --message-format short --color never` 被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 `primitive_relevance` focused guard 与 visibility custom-target layer filters;继续补 WGPU/RenderDoc 产品证据,并视后续 GPU ABI 收束需求处理 shader-facing legacy mask。 |
| 2026-06-23 | CO-M4 mesh render-layer snapshot typed mask | render_plan09_mesh_render_layer_set_snapshot_static_passed_cargo_lock_blocked: 已把 `RenderMeshSnapshot.render_layer_mask` 从 legacy `u32` 收束为 `RenderLayerSet`;focused Cargo 被当前 `Cargo.lock` 漂移在编译前阻断 | `RenderMeshSnapshot` 现在直接携带 typed `RenderLayerSet`。`scene/world/render.rs` 继续从 scene entity legacy mask 读取 authoring 输入,但只在 mesh DTO 边界用 `RenderLayerSet::from_legacy_mask(...)` 包装;`build_mesh_draws/build/phase_ordering.rs` 在 raw mesh-vector fallback 和 `RenderPhaseQueue` consumption 两条路径改用 `selected_camera_layers().intersects(&mesh.render_layer_mask)` 做 CPU 侧过滤,支持 layer 32+ 不经 lossy mask。`StaticMeshBatchExtract.render_layer_mask` 与 frame-history mesh validation key 同步 typed;该 mesh 切片完成时 `RenderFrameExtract::from_snapshot(...)`、`build_visibility_input(...)` 和 visibility fallback 还保留显式 lossy visibility DTO 边界;后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 已改为 typed `VisibilityRenderableInput`。mesh product/submit/visibility fixture 构造点已同步 typed mask。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 mesh DTO、frame extract/static batch、world mesh extraction、mesh phase ordering、history key、visibility fallback 和 mesh product fixtures 通过;全仓库 mesh stale `intersects_legacy_mask(mesh.render_layer_mask)` / direct bitwise/equality 扫描无生产残留,只剩显式 `to_legacy_mask_lossy()` 在 visibility/authoring ABI 边界;diff 级 `TODO|FIXME|#[allow|extern "C"` 扫描无新增;line-count: `scene_extract.rs` 547、`frame_extract.rs` 979、`scene/world/render.rs` 956、`phase_ordering.rs` 401、`validation_key.rs` 101、`visibility_entries.rs` 18、`m4_behavior_layers.rs` 796、`project_render.rs` 1387、`render_product_anti_alias.rs` 1083、`render_product_shadow_captures.rs` 1125、`render_product_shadows.rs` 912。scoped `git diff --check` 对 tracked files 通过,仅 LF->CRLF 提示。focused locked Cargo `cargo test -p zircon_runtime --lib phase_ordered_meshes_filter_meshes_by_selected_camera_layers --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-mesh-typed-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,未进入编译,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 mesh typed-layer focused guard、static batch/history/visibility focused guards 与 broader mesh/product filters;后续继续补 RenderDoc/product 证据,并视后续 GPU ABI 收束需求处理 shader-facing legacy mask。 |
| 2026-06-23 | CO-M4 particle render-layer snapshot typed mask | render_plan09_particle_render_layer_set_snapshot_static_passed_cargo_lock_blocked: 已把 `RenderParticleSpriteSnapshot.render_layer_mask` 从 legacy `u32` 收束为 `RenderLayerSet`;focused Cargo 被当前 `Cargo.lock` 漂移在编译前阻断 | `RenderParticleSpriteSnapshot` 现在直接携带 typed `RenderLayerSet`。`scene/world/render_particles.rs` 继续从 scene entity legacy mask 读取 authoring 输入,但只在 particle DTO 边界用 `RenderLayerSet::from_legacy_mask(...)` 包装;`build_particle_vertices(...)`、`build_particle_velocity_vertices(...)` 与 `RenderPipelineAsset::compile(...)` 在 WGPU 顶点扩展或 `particle-render` auto-insert 前改用 `selected_camera_layers().intersects(&sprite.render_layer_mask)` 做 CPU 侧过滤,支持 layer 32+ 不经 lossy mask。该 particle 切片完成时 `build_visibility_input(...)` 还通过 lossy 聚合 emitter mask;后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 已改为 typed `RenderLayerSet::union(...)` visibility input。particle product/submit fixtures 和 previous-state tests 已同步 typed 构造。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 particle DTO、pipeline compile、particle vertex/velocity builders、world particle extraction、world visibility input、particle product fixtures 和 `scene/tests/render_extract.rs` 通过;全仓库 `RenderParticleSpriteSnapshot` 构造点扫描确认字段均为 `RenderLayerSet` 或 typed clone;`intersects_legacy_mask(sprite.render_layer_mask)` 与 direct particle bitwise/equality stale 扫描无生产残留,只剩 `to_legacy_mask_lossy()` 在显式 visibility ABI 边界;diff 级 `TODO|FIXME|#[allow|extern "C"` 扫描无新增;line-count: `scene_extract.rs` 492、`compile.rs` 370、`build_particle_vertices.rs` 177、`build_particle_velocity_vertices.rs` 425、`render_particles.rs` 571、`scene/world/render.rs` 904、`compiled_graph_cache.rs` 701、`compile_tests.rs` 770、`render_product_anti_alias.rs` 1005、`render_product_particle_velocity.rs` 544、`render_product_post_process.rs` 856、`render_product_sprite.rs` 233、`m4_behavior_layers/particles.rs` 83、`scene/tests/render_extract.rs` 1356。scoped `git diff --check` 对 tracked files 通过,仅 LF->CRLF 提示。focused locked Cargo `cargo test -p zircon_runtime --lib particle_vertices_filter_sprites_by_selected_camera_layers --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-particle-typed-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,未进入编译,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 particle typed-layer focused guard、velocity guard、compile auto-insert guard 与 broader particle/product filters;后续继续补 RenderDoc/product 证据,并视后续 GPU ABI 收束需求处理 shader-facing legacy mask。 |
| 2026-06-23 | CO-M4 sprite render-layer snapshot typed mask | render_plan09_sprite_render_layer_set_snapshot_static_passed_cargo_lock_blocked: 已把 `RenderSpriteSnapshot.render_layer_mask` 从 legacy `u32` 收束为 `RenderLayerSet`;focused Cargo 被当前 `Cargo.lock` 漂移在编译前阻断 | `RenderSpriteSnapshot` 现在直接携带 typed `RenderLayerSet`,并保留 serde default。`World::render_sprite_snapshot_for_camera(...)` 继续从 scene entity legacy mask 读取 authoring 输入,但只在 extract 边界用 `RenderLayerSet::from_legacy_mask(...)` 包装;`build_sprite_vertices(...)` 在 WGPU 顶点扩展前改用 `selected_camera_layers().intersects(&sprite.render_layer_mask)` 做 CPU 侧过滤,支持 layer 32+ 不经 lossy mask。visibility 输入与 particle/mesh legacy interop 仍是旧 `u32` 边界,通过 `to_legacy_mask_lossy()` 显式降级;particle 与 mesh DTO typed mask 已由后续状态行收束。新增/更新守卫使用 layer 40 sprite 与 selected camera layer 40 验证 non-lossy sprite 顶点过滤,并把 sprite product fixtures 改为 typed mask 构造。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 sprite DTO、world render extract、sprite vertex builder、sprite product fixtures 和 `world_basics.rs` 通过;全仓库 `RenderSpriteSnapshot` 构造点扫描确认字段均为 `RenderLayerSet`;sprite 直接位运算扫描无残留,仅旧 ABI 出口保留显式 lossy 转换;diff 级 `TODO|FIXME|#[allow|extern "C"` 扫描无新增;line-count: `sprite.rs` 26、`build_sprite_vertices.rs` 646、`scene/world/render.rs` 903、`world_basics.rs` 661、`render_product_sprite.rs` 233、`render_product_submit.rs` 710、`transparent3d.rs` 225。scoped `git diff --check` 对 tracked files 通过,仅 LF->CRLF 提示;untracked `transparent3d.rs` 尾随空白扫描无输出。focused locked Cargo `cargo test -p zircon_runtime --lib build_sprite_vertices_filters_sprites_by_selected_camera_layers --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-sprite-typed-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,未进入编译,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 sprite typed-layer focused guard 与 broader sprite/product filters;后续继续补 RenderDoc/product 证据,并视后续 GPU ABI 收束需求处理 shader-facing legacy mask。 |
| 2026-06-23 | CO-M4 selected-camera history layer key | render_plan09_history_key_layer_masks_static_passed_cargo_lock_blocked: 已把 per-camera history slot key 接入 selected descriptor 的 culling/volume layer masks;focused Cargo 被当前 `Cargo.lock` 漂移在编译前阻断 | `ViewportCameraHistoryKey::from_camera(...)` 现在把 `CameraRenderDescriptor.culling_mask` 与 `volume_mask` 归一成私有 `ViewportCameraHistoryLayerKey`,以 `RenderLayerSet::iter()` 保留 32+ layer 身份,避免在 history/previous-state/per-camera product report 槽位选择时退回 `to_legacy_mask_lossy()`。新增 `camera_history_key_distinguishes_culling_layers_without_legacy_loss` 与 `camera_history_key_distinguishes_volume_layers_without_legacy_loss`,分别用 layer 40/41 对比 empty mask,锁定同实体/同 target/order/viewport 但不同 layer 选择的相机不会共用 history key。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 `viewport_record/camera_history_key.rs` 通过;diff 级 `TODO|FIXME|allow(|extern "C"` 扫描无新增;line-count: `camera_history_key.rs` 139;scoped `git diff --check` 通过,仅 LF->CRLF 提示。focused locked Cargo `cargo test -p zircon_runtime --lib camera_history_key_distinguishes_culling_layers_without_legacy_loss --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-history-layer-key-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被当前 `Cargo.lock` 需更新且 `--locked` 禁止更新阻断,未进入编译,不计 Cargo/WGPU 通过;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移后重跑两个 history key layer guards 与 broader `camera_history` filter;继续补 RenderDoc/product 证据。 |
| 2026-06-23 | CO-M4 volume mask separated from culling mask | render_plan09_volume_mask_separate_from_culling_static_passed_cargo_lock_blocked_timeout_no_result: 已把 post-process Volume 评估从 camera culling mask 分离到 selected camera `volume_mask`;focused Cargo 因共享编译超时/lock 漂移无结论 | `RenderViewExtract::selected_camera_volume_layers()` 成为 Volume 评估唯一读口,保留 `selected_camera_layers()` 只服务 visibility/culling。`World::collect_post_process_volumes_for_view(...)` 在 post-process owner 内按 selected/stack camera `volume_mask` 做候选 union,不再复用 culling layer set;`build_frame_submission_context(...)` 调 `PostProcessExtract::resolved_settings_for_camera(...)` 时传入 selected camera volume layers。新增 `explicit_request_camera_uses_volume_mask_for_post_process_volumes`,覆盖显式 request camera `culling_mask=0b0010`、`volume_mask=0b0100` 时 Volume DTO 与 resolved bloom 只按 volume mask 命中。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 touched Rust files 通过;`resolved_settings_for_camera(...)` 调用点扫描确认仅传 `selected_camera_volume_layers()`;direct misuse 扫描 `resolved_settings_for_camera(...selected_camera_layers` 无输出;diff 级 `TODO|FIXME|#[allow(dead_code)]|extern "C"` 扫描无新增;line-count: `frame_extract.rs` 979、`build_frame_submission_context/build.rs` 534、`scene/world/render.rs` 953、`scene/world/render_post_process.rs` 140、`scene/tests/render_post_process_extract.rs` 624、`scene/tests/render_extract.rs` 1446;scoped `git diff --check` 通过,仅 LF->CRLF 提示。focused locked Cargo `explicit_request_camera_uses_volume_mask_for_post_process_volumes` 约 245s 超时且无输出/无 test binary;`cargo check ... --locked` 被当前 `Cargo.lock` 漂移在编译前阻断;残留 cargo/rustc 已停止;RenderDoc 未跑。 | 解阻 `Cargo.lock`/manifest 漂移并给足共享 lib-test 编译窗口后重跑 Volume focused guard 与 broader volume/render-product gates;继续补 post/history layer edge 与 RenderDoc product proof。`scene/world/render.rs` 当前 953 行、`frame_extract.rs` 当前 979 行,下一次新增 world/frame extract 职责前需先拆 owner;`scene/tests/render_extract.rs` 已 1446 行,后续守卫不得继续堆入该文件。 |
| 2026-06-23 | CO-M4 light layer typed mask convergence | render_plan09_light_layer_set_snapshot_static_passed_cargo_timeout_no_result: 已把四类 render light snapshot 的 `layer_mask` 从旧 `u32` 收束为 `RenderLayerSet`;focused Cargo 超时无结论 | `RenderDirectionalLightSnapshot`、`RenderPointLightSnapshot`、`RenderSpotLightSnapshot`、`RenderRectLightSnapshot` 现在直接持有 `RenderLayerSet`。`World` 的 light extraction 仍从 scene entity legacy `RenderLayerMask(u32)` 读取 authoring mask,但只在 extract 边界用 `RenderLayerSet::from_legacy_mask(...)` 包装。`pack_lighting_extract(...)` / `light_buffer.rs` 在 WGPU `GpuLightData.shadow_slot_layer[1]` 写入前通过 `to_legacy_mask_lossy()` 回落到旧 32-bit GPU ABI,因此本切片关闭 CPU 快照 typed mask 漂移,不扩大 shader-facing buffer layout。相关 light-buffer、shadow-plan、visibility-context 和 graphics product 测试构造点已同步改为 typed mask 构造。 | `rustfmt --edition 2021` 与 `rustfmt --edition 2021 --check` 覆盖 15 个 touched Rust files 通过;全仓库旧 direct light `layer_mask: DEFAULT_RENDER_LAYER_MASK/default_render_layer_mask()/1/0b/0x` 扫描无输出;diff 级 `TODO|FIXME|#[allow(dead_code)]|extern "C"` 扫描无新增;line-count: light snapshots 103、light readiness 131、light buffer 264、shadow plan 903、scene/world/render.rs 903;scoped `git diff --check` 通过,仅 LF->CRLF 提示。focused locked Cargo `cargo test -p zircon_runtime --lib pack_light_slices_encodes_directional_shadow_and_layer_contract --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-light-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 188s 超时且无输出/无 `zircon_runtime*.exe`;残留 cargo/rustc 进程已停止;RenderDoc MCP 无实例,未跑 `.rdc` 对拍。 | 解阻共享 lib-test 编译窗口后重跑 light-buffer focused guard,并继续把 volume/post/history layer 边界收敛到同一 `RenderLayerSet`。`scene/world/render.rs` 与 shadow plan 均已到 903 行,后续新增 world render extract 或 shadow planning 职责前必须先拆 owner。 |
| 2026-06-23 | CO-M4 particle selected-camera layer filter | render_plan09_particle_selected_camera_layer_filter_static_passed_cargo_timeout_no_result: 已补 particle sprite/velocity WGPU 顶点构建与 pipeline auto-insert 的 selected-camera layer 过滤;focused Cargo 超时无结论 | `RenderParticleSpriteSnapshot` 新增 `render_layer_mask`,默认 `u32::MAX` 保持旧直接构造可见性;`scene/world/render_particles.rs` 将实体 layer mask 传入 authored particle sprites 与 world HUD bar 粒子四边形。`build_particle_vertices(...)` 与 `build_particle_velocity_vertices(...)` 在 CPU 顶点生成前读取 selected camera layers 并过滤不相交 sprite;`RenderPipelineAsset::compile(...)` 只在至少一个 particle sprite 与 selected camera mask 相交时 auto-insert `particle-render`;`build_visibility_input(...)` 从 particle sprite snapshot OR 聚合 emitter layer mask,无 sprite 的 GPU/direct emitter fallback 到默认 layer。新增 `particle_vertices_filter_sprites_by_selected_camera_layers`、`particle_velocity_vertices_filter_current_sprites_by_selected_camera_layers`、`compile_skips_core_particle_pass_when_particle_sprites_miss_selected_camera_layers`,并补 world particle 单元断言确认 authored/HUD sprite 保留 mask。该切片完成时 `scene/world/render.rs` 为 899 行,后续 light typed slice 后当前为 903 行;后续 world render extract 新职责应拆 visibility/particle/light owner。 | `rustfmt --edition 2021 --check` 覆盖 touched Rust files 通过;source anchor scan、真正 `RenderParticleSpriteSnapshot` 字面量字段扫描、静态债务扫描、line-count 与 `git diff --check` 通过(`diff --check` 仅报告既有 LF->CRLF 提示)。focused locked Cargo `cargo test -p zircon_runtime --lib particle_vertices_filter_sprites_by_selected_camera_layers --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-particle-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 184s 工具窗口超时、无输出、无 `zircon_runtime*.exe` test binary;事后无 cargo/rustc/rustdoc/link 残留进程,不计 Cargo/WGPU 通过。RenderDoc MCP 无实例,未跑 `.rdc` 对拍。 | 解阻共享 lib-test 编译窗口与 lock/manifest 漂移后重跑 particle vertices/velocity/compile focused guards,并继续补 overlay/post/history/light layer 边界与 RenderDoc/product 证据。 |
| 2026-06-23 | CO-M4 mesh selected-camera layer filter / phase-ordering owner split | render_plan09_mesh_selected_camera_layer_filter_static_passed_cargo_timeout_no_result: 已补 mesh draw phase-ordering selected-camera layer 过滤;focused Cargo 超时无结论 | 新增 `zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/phase_ordering.rs`,把原 1036 行 `build.rs` 中的 mesh phase queue/material sort owner 抽出,`build.rs` 降到 721 行、`phase_ordering.rs` 为 401 行。`phase_ordering.rs` 在 raw mesh-vector fallback 与 `RenderPhaseQueue` consumption 两条路径都读取 `RenderViewExtract::selected_camera_layers()`；该初始切片先在旧 mesh DTO 上完成 selected-camera 过滤,后续 `render_plan09_mesh_render_layer_set_snapshot_static_passed_cargo_lock_blocked` 已把同一路径升级为 typed `RenderLayerSet` / `intersects(&mesh.render_layer_mask)`。新增 `phase_ordered_meshes_filter_meshes_by_selected_camera_layers`,同一组 layer 1/2 mesh 分别覆盖无 phase queue 与 phase queue 路径,断言 `RenderLayerSet::layer(2)` 只保留 layer 2 mesh。 | `rustfmt --edition 2021` 覆盖 `build.rs`、`phase_ordering.rs`、`mod.rs` 通过;line count 确认 `build.rs` 从 1036 降为 721,新 owner 401。focused locked Cargo `cargo test -p zircon_runtime --lib phase_ordered_meshes_filter_meshes_by_selected_camera_layers --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-mesh-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在 124s 工具窗口超时、无输出、无 `zircon_runtime*.exe` test binary;事后进程审计只剩 unrelated `D:\cargo-targets\zircon-plugin-m5-runtime-plugin-id-check` cargo/rustc 进程,不计 Cargo/WGPU 通过。RenderDoc MCP 无实例,未跑 `.rdc` 对拍。 | 解阻共享 lib-test 编译窗口与 lock/manifest 漂移后重跑 focused guard,并继续补粒子/overlay/post/history/light layer 边界与 RenderDoc/product 证据。 |
| 2026-06-23 | CO-M4 sprite selected-camera layer filter | render_plan09_sprite_selected_camera_layer_filter_static_passed_cargo_lock_blocked: 已补 sprite WGPU 顶点构建的 selected-camera layer 过滤;Cargo 被 lock 漂移/共享编译窗口阻断 | `build_sprite_vertices(...)` 初始接入 selected camera layer 过滤,让 sprite 顶点扩展先按当前相机 culling mask 剔除不可见 sprite。新增 `build_sprite_vertices_filters_sprites_by_selected_camera_layers`,构造 layer 1/2 两个 sprite 与 `RenderLayerSet::layer(2)` 相机,断言只有匹配相机 mask 的 sprite index 被展开为 6 个顶点。该切片修改生产 sprite draw prep,但不改变 WGPU product/RenderDoc 证据状态；后续 `render_plan09_sprite_render_layer_set_snapshot_static_passed_cargo_lock_blocked` 已把 sprite DTO 与过滤实现升级为 typed `RenderLayerSet` / `intersects(&sprite.render_layer_mask)`。 | `rustfmt --edition 2021`、`rustfmt --edition 2021 --check`、source anchor scan 与静态债务扫描覆盖 `zircon_runtime/src/graphics/scene/scene_renderer/sprite/build_sprite_vertices.rs` 通过;行数 701。focused locked Cargo `cargo test -p zircon_runtime --lib build_sprite_vertices_filters_sprites_by_selected_camera_layers --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-plan09-sprite-layer-0623 --message-format short --color never -- --test-threads=1 --nocapture` 先被 `Cargo.lock` 需更新且 `--locked` 禁止更新拒绝;最终复跑在 124s 工具窗口超时、无输出、无 `zircon_runtime*.exe` test binary、无本 target-dir cargo/rustc 残留,不计 Cargo/WGPU 通过。RenderDoc MCP 无实例,未跑 `.rdc` 对拍。 | 解阻 `Cargo.lock`/manifest 漂移并给足共享 lib-test 编译窗口后重跑 focused guard,并继续补 sprite/transparent mixed/product 与 RenderDoc 证据。 |
| 2026-06-23 | CO-M3 queue override product source guard | render_plan09_queue_override_product_source_guard_static_passed: 已补计划表规范名源码守卫;尚未形成新的 Cargo/WGPU/RenderDoc 运行结果 | 新增 `m4_behavior_layers/queue_override.rs::render_product_queue_override_reorders_draws`,构造 Geometry red、queue=2900 opaque green 与 Transparent blue 三个重叠 mesh,先断言 `GeometryPhaseInput` 投影后的 phase queue 顺序为 Opaque3d red -> Transparent3d green(2900) -> Transparent3d blue(3000),再用最终捕获中心像素要求 green 高于 red 且 blue 仍参与混合。`m4_behavior_layers.rs` 只新增 `mod queue_override;`,产品守卫不回流父文件;`runtime_15_render_camera_target_products_are_folder_backed` 同步锁定新 owner、规范测试名和文档状态锚。该切片只改测试源码/结构守卫/文档,未改生产渲染路径。 | scoped rustfmt 覆盖 `m4_behavior_layers.rs`、new queue override child 与 render-products 结构守卫通过;行数:m4 parent 732、queue_override 167、particles 80、transparent3d 225、render_products guard 258。文档锚点扫描覆盖 Plan 09、render index、review、structure 与 render-product-submit;focused locked Cargo `render_product_queue_override_reorders_draws` 两次在 184s 与 304s 工具窗口超时无测试结果,未产出可直接复跑的 `zircon_runtime` test binary,第二次残留 cargo/rustc 进程已停止。RenderDoc MCP 无实例,不声明 `.rdc` 对拍。 | 解阻 lock/manifest 漂移并给足共享 lib-test 编译窗口后重跑 `render_product_queue_override_reorders_draws`、`runtime_15_render_camera_target_products_are_folder_backed` 与透明混排/queue focused guard;RenderDoc 事件序对拍仍需后续环境支持。 |
| 2026-06-23 | CO-M2 custom-target composite source guard | render_plan09_custom_target_composite_source_guard_static_passed: 已补更宽 custom-target viewport-stack 合成源码守卫;尚未形成新的 WGPU/Cargo/RenderDoc 运行结果 | 新增 `render_product_camera_targets/custom_target/composite.rs::custom_target_two_viewport_stacks_preserve_independent_composites_before_primary_sample`,在同一 sRGB custom target 上提交左右两个 `RenderViewportRect` Base+Overlay stack,再由后续 PrimarySurface fullscreen material 采样 prepared target;断言左侧 green overlay + red base、右侧 red overlay + blue base 互不串扰。`custom_target.rs` 继续只挂载子 owner,`runtime_15_render_camera_target_products_are_folder_backed` 同步锁定新 child、moved guard 不回流 root 和文档路径。该切片只改测试源码/结构守卫/文档,未改生产渲染路径。 | scoped rustfmt 覆盖 custom-target root、new composite child 与 render-products 结构守卫通过;行数:camera-target root 7、custom_target root 25、composite 195、material_sampling 354、viewport 250、ordering 105、render_products guard 232、test_file_budget parent 450。当前仍有 active cargo/rustc 进程且 `Cargo.lock` 已修改,未启动新的 focused Cargo/WGPU;RenderDoc MCP 无实例,不声明 `.rdc` 对拍。 | 解阻 `Cargo.lock`/manifest 漂移、清空 active Cargo lanes 并给足共享 lib-test 编译窗口后重跑 `runtime_15_render_camera_target_products_are_folder_backed` 与完整 `render_product_camera_targets` guard group;RenderDoc/product 对拍仍需后续环境支持。 |
| 2026-06-23 | CO-M2 camera-target custom-target sub-owner split | render_plan09_custom_target_subowners_static_passed: 已完成结构拆分;六条 custom-target/RT 产品守卫已从 726 行 `custom_target.rs` 继续拆入 folder-backed 子 owner;尚未形成新的 WGPU/Cargo 运行结果 | `custom_target.rs` 现在只保留共享 imports 与 `mod material_sampling; mod ordering; mod viewport;`。`material_sampling.rs` 承载 `render_product_dual_camera_rt_then_main`、multi-RT stack 与 chained RT sampling 三条 material-sampling 产品守卫;`viewport.rs` 承载 split viewport 与 overlay viewport inheritance 两条 viewport 产品守卫;`ordering.rs` 承载 previous-frame late-producer ordering 守卫。该切片只改测试结构与源码归属,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 custom-target root、三个新子 owner 与 render-products 结构守卫通过;文档锚点扫描确认 Plan 09、render index、review、structure、render-product-submit 与 module-convention 均记录三条新路径和 `render_plan09_custom_target_subowners_static_passed`;旧 `custom_target.rs::...` 当前路径扫描无残留;`git diff --check` 仅报告既有 LF->CRLF 警告。行数:custom_target root 22、material_sampling 332、viewport 235、ordering 97、render_products guard 208。`runtime_15_render_camera_target_products_are_folder_backed` 已更新为锁定 custom-target root 只挂载子 owner、六条产品守卫不回流 root、文档记录三条新路径。本轮发现 active cargo/rustc 进程和已修改 `Cargo.lock`,未启动新的 focused Cargo/WGPU。 | 解阻 `Cargo.lock`/manifest 漂移、清空 active Cargo lanes 并给足共享 lib-test 编译窗口后重跑结构 guard 与完整 `render_product_camera_targets` guard group;后续新增 custom-target 守卫必须落在对应 sub-owner 或继续拆分,不得回流到 `custom_target.rs`。 |
| 2026-06-23 | CO-M2 camera-target custom-target owner split | render_plan09_camera_target_custom_owner_split_static_passed_guard_timeout_no_result: 已完成结构拆分;六条 RT/custom-target -> PrimarySurface 产品守卫已从 `render_product_camera_targets.rs` root 迁入 folder-backed `render_product_camera_targets/custom_target.rs`;root 已收敛为 7 行模块编排;尚未形成新的 WGPU/Cargo 运行结果 | 新增 `render_product_camera_targets/custom_target.rs`,并通过 root `mod custom_target;` 接入。`render_product_dual_camera_rt_then_main`、`custom_target_stacks_feed_later_primary_surface_materials_independently`、`custom_target_viewport_regions_feed_later_primary_surface_sample`、`custom_target_overlay_inherits_base_viewport_region_before_primary_sample`、`custom_target_chain_feeds_later_texture_and_primary_surface_samples` 与 `custom_target_late_producer_feeds_previous_frame_not_future_sample` 迁入 custom-target owner。新增 `runtime_15_render_camera_target_products_are_folder_backed` 结构守卫锁定 root/child owner 与 800 行预算。该切片只改测试结构与源码归属,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 camera-target root、`custom_target.rs`、test-budget parent 与 `test_file_budget/render_products.rs` 通过;行数:root 7、custom_target 726、texture_target 307、primary_surface 247、fixture 456、mesh 172、assertions 166、camera 94、test-budget parent 778、render_products guard 154。名称扫描确认六条 custom-target/RT 产品守卫位于 `custom_target.rs`,root 不再承载测试函数或 imports。focused locked `cargo test -p zircon_runtime --lib runtime_15_render_camera_target_products_are_folder_backed --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-camera-target-owner-guard-0623 --message-format short --color never -- --test-threads=1 --nocapture` 180s 超时无测试结果;事后进程审计未发现该 target-dir 残留 cargo/rustc,不计 Cargo/WGPU 通过。 | 解阻 `Cargo.lock`/manifest 漂移并给足共享 lib-test 编译窗口后重跑结构 guard 与完整 `render_product_camera_targets` guard group;后续新增 custom-target 守卫必须落在 `custom_target.rs` 或继续拆 owner,不得回流到 root。 |
| 2026-06-23 | CO-M3 particle owner split / R1.4 m4 budget closure | 已完成结构拆分: 两条 particle M4 守卫、particle executor 与 warm-pixel helper 已从 `m4_behavior_layers.rs` 迁入 folder-backed `m4_behavior_layers/particles.rs`;`m4_behavior_layers.rs` 已降到 795 行,低于 R1.4 测试文件 800 行预算;尚未形成新的 WGPU/Cargo 运行结果 | 新增 `m4_behavior_layers/particles.rs`,并通过父模块 `mod particles;` 接入。`particle_rendering_draws_billboard_sprites_in_transparent_stage`、`particle_shader_preserves_sprite_alpha_for_transparent_blending`、`particle_transparent_billboard_executor(...)` 与 `warm_pixels(...)` 迁入粒子 owner;父模块移除 particle-only imports,继续承载 post-process/offline/shared fixture/helper。该切片只改测试结构与源码归属,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `m4_behavior_layers.rs`、`m4_behavior_layers/particles.rs`、`m4_behavior_layers/transparent3d.rs` 与 camera-target touched files 通过;父模块残留扫描确认 particle/transparent moved tests、particle executor/helper 与 particle-only imports 不在父模块;子模块名称扫描确认粒子与透明守卫已迁入;静态债务扫描只命中粒子 owner 内既有 executor 签名 `particle_transparent_billboard_executor(...) -> Result<(), String>`,本切片未新增 dead-code/FFI/plugin builder 债;行数:m4 795、particles 87、transparent3d 234、camera-target root 731、texture_target 307。focused locked `cargo test -p zircon_runtime --lib particle_shader_preserves_sprite_alpha_for_transparent_blending --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-m4-particle-owner-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被 Cargo 在编译前拒绝:`cannot update the lock file ... because --locked was passed`,不计 WGPU pass。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 particle 与 transparent3d focused guards;后续 m4 只允许按 post-process/offline family 继续拆,不得回流到 800+ 单文件。 |
| 2026-06-23 | CO-M3 transparent3d product owner split / R1.4 m4 threshold | 已完成结构拆分: 两条 transparent3d 产品守卫已从 `m4_behavior_layers.rs` 迁入 folder-backed `m4_behavior_layers/transparent3d.rs`;`m4_behavior_layers.rs` 已降到 R1.4 测试文件阈值内;尚未形成新的 WGPU/Cargo 运行结果 | 新增 `m4_behavior_layers/transparent3d.rs`,并通过父模块 `mod transparent3d;` 接入。`transparent3d_product_interleaves_mesh_and_sprite_pixels_by_phase_sort_key` 与 `transparent3d_product_treats_world_space_ui_sprite_as_transparent_member` 迁入子模块;子模块显式导入 transparent/sprite/material phase 类型,父模块只保留共享 fixture/helper 与其他 M4 行为测试。该切片只改测试结构与源码归属,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `m4_behavior_layers.rs`、`m4_behavior_layers/transparent3d.rs` 与 camera-target touched files 通过;父模块残留扫描确认两个 moved test definition、sprite phase construction 与 transparent-only imports 不在父模块;新子模块名称扫描确认两个产品守卫已迁入;静态债务扫描只剩既有 `particle_transparent_billboard_executor(...) -> Result<(), String>` helper,本切片未新增债;冲突标记扫描无输出;行数:m4 880、transparent3d 234、camera-target root 731、texture_target 307、fixture 456、primary_surface 247。focused locked `cargo test -p zircon_runtime --lib transparent3d_product_interleaves_mesh_and_sprite_pixels_by_phase_sort_key --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-m4-transparent-owner-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被 Cargo 在编译前拒绝:`cannot update the lock file ... because --locked was passed`,不计 WGPU pass。 | 解阻 `Cargo.lock`/manifest 漂移后重跑两个 transparent3d 产品守卫;后续如继续收束 m4,按 remaining particle/post-process/offline behavior family 拆分,不得回流到 800+ 单文件。 |
| 2026-06-23 | CO-M2 camera-target texture-target owner split | 已完成结构拆分: 三条 texture-target camera 产品守卫已从 `m4_behavior_layers.rs` 迁入 folder-backed `render_product_camera_targets/texture_target.rs`;尚未形成新的 WGPU/Cargo 运行结果 | 新增 `render_product_camera_targets/texture_target.rs`,并通过 root `mod texture_target;` 接入。`texture_target_overlay_camera_draws_layered_mesh_over_base_clear`、`texture_target_overlay_camera_converts_linear_final_product_after_composite`、`texture_target_stack_preserves_composite_when_primary_surface_renders_later` 迁入该子模块;`fixture.rs` 新增 `insert_linear_render_target_texture(...)`;`m4_behavior_layers.rs` 删除旧测试与 texture/primary descriptor、render-target texture 插入和 descriptor helper。该切片只改测试结构与源码归属,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `m4_behavior_layers.rs`、camera-target root 与子模块通过;残留扫描确认三条 moved test 与旧 helper 不在 `m4_behavior_layers.rs`;新子模块名称扫描确认三条产品守卫与私有 `texture_overlay_quad(...)` 已迁入;静态债务扫描只剩既有 `particle_transparent_billboard_executor(...) -> Result<(), String>` helper,本切片未新增 `TODO`/`FIXME`/dead-code suppression/FFI/plugin builder/`Result<_, String>` 债;冲突标记和 scoped diff 检查通过;行数:m4 1105、root 731、texture_target 307、primary_surface 247、fixture 456。focused locked `cargo test -p zircon_runtime --lib texture_target_overlay_camera_draws_layered_mesh_over_base_clear --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-texture-target-owner-0623 --message-format short --color never -- --test-threads=1 --nocapture` 最新 124s 超时且未产出 `zircon_runtime` test binary;同 target-dir 事后进程审计无 cargo/rustc 残留,不计 WGPU pass。 | 继续按 product family 拆 `m4_behavior_layers.rs` 剩余 transparent/particle/product 测试族,先把 1105 行降到 R1.4 阈值内;解阻 `Cargo.lock`/manifest 漂移并给足编译窗口后重跑 texture-target 三条守卫与完整 `render_product_camera_targets` guard group。 |
| 2026-06-23 | CO-M2 camera-target PrimarySurface owner split | 已完成结构拆分: 三个纯 PrimarySurface 产品守卫已从 `render_product_camera_targets.rs` root 移入 folder-backed `primary_surface.rs`;尚未形成新的 WGPU/Cargo 运行结果 | 新增 `render_product_camera_targets/primary_surface.rs`,并通过 root `mod primary_surface;` 接入。`render_product_camera_render_order_swap_changes_composite`、`render_product_overlay_stack_composites_over_base`、`render_product_split_screen_viewports` 及其 private capture helper 迁入该子模块;root owner 继续只承载 RT/custom-target -> PrimarySurface material sampling 与跨 target 产品编排;共享 assertion/camera/fixture/mesh helper 保持在既有子模块。该切片只改测试结构与源码归属,未改生产渲染路径。 | `rustfmt --edition 2021` 覆盖 root 与 `primary_surface.rs` 通过;root 残留扫描确认 `average_channel_in_region`、`primary_surface_stack_camera_descriptor`、`colored_mesh_on_layer`、两个 `capture_primary_surface_*` helper 和三个 moved test definition 均已不在 root;`primary_surface.rs` 名称扫描确认三个规范 test 与两个 capture helper 已迁入;测试树静态扫描无新增 `TODO`/`FIXME`/`#[allow(dead_code)]`/FFI/plugin builder/`Result<_, String>` 债;冲突标记扫描无输出;行数:root 730、primary_surface 247、surface_targets 963、m4 1595、assertions 166、camera 94、fixture 437、mesh 172。focused locked `cargo test -p zircon_runtime --lib render_product_split_screen_viewports --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-camera-targets-primary-surface-split-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被 Cargo 在编译前拒绝:`cannot update the lock file ... because --locked was passed`,不计 WGPU pass。 | 解阻 `Cargo.lock`/manifest 漂移后重跑 `render_product_split_screen_viewports` 与完整 `render_product_camera_targets` guard group;后续新增 custom-target 守卫前继续按 product family 拆 root owner。 |
| 2026-06-23 | CO-M2 split-screen viewport canonical product guard migration | 已完成源码归属迁移: 计划表中的 `render_product_split_screen_viewports` 现在是当前可运行的 folder-backed camera-target 产品守卫;旧 `surface_targets.rs` split-screen 测试与专用 helper 已删除;尚未形成新的 WGPU/Cargo 运行结果 | `render_product_camera_targets/primary_surface.rs::render_product_split_screen_viewports` 构造两个 PrimarySurface Base camera,左半 viewport clear red、右半 viewport clear green,并在左右 inset 区域断言 dominant pixels 不串扰。该守卫复用 `RenderFixture::configured_framework(...)`、`primary_surface_camera_descriptor(...).with_viewport(...)` 与 camera-target shared dominant-region assertions,替代旧 `graphics_primary_surface_split_screen_base_cameras_clear_only_their_viewport_regions` 及其 `surface_targets.rs`-local `primary_base_camera(...)`、region dominance helpers、`pixel_at(...)`。该切片只改测试归属与产品验收命名,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `render_product_camera_targets.rs`、`surface_targets.rs` 和 camera-target 子模块通过;冲突标记扫描无输出;源码扫描确认旧 split-screen 测试名及旧专用 helper 已从 `surface_targets.rs` 移除,规范名已迁入 `render_product_camera_targets/primary_surface.rs`;测试树静态扫描无新增 `TODO`/`FIXME`/`#[allow(dead_code)]`/FFI/plugin builder/`Result<_, String>` 债;`git diff --check` 通过,仅 LF->CRLF 提示;行数:camera-target root 964、surface_targets 963、m4 1595、assertions 166、camera 94、fixture 437、mesh 172。focused locked `cargo test -p zircon_runtime --lib render_product_split_screen_viewports --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-camera-targets-split-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被 Cargo 在编译前拒绝:`cannot update the lock file ... because --locked was passed`,不计 WGPU pass。 | 解阻 `Cargo.lock`/manifest 漂移后,与其他 `render_product_camera_targets` 守卫一起重跑 focused locked WGPU guard group;2026-06-23 结构拆分已把 root owner 降到 730 行,后续新增守卫前继续按 primary-surface/custom-target product family 拆 root owner;继续 custom-target product/RenderDoc、UI/scene overlay 宽场景和 editor authoring 面板。 |
| 2026-06-23 | CO-M2 PrimarySurface Overlay stack canonical product guard migration | 已完成源码归属迁移: 计划表中的 `render_product_overlay_stack_composites_over_base` 现在是当前可运行的 folder-backed camera-target 产品守卫;旧 `m4_behavior_layers.rs` Overlay clear-depth 测试与专用 helper 已删除;尚未形成新的 WGPU/Cargo 运行结果 | `render_product_camera_targets/primary_surface.rs::render_product_overlay_stack_composites_over_base` 构造 PrimarySurface Base red near mesh + Overlay green farther mesh,分别以 `clear_depth=false`/`true` 提交同一 Base stack,断言 false 时 Base depth 挡住 Overlay、true 时 Overlay 清 depth 后替换中心像素。该守卫复用 `RenderFixture::configured_framework(...)`、`primary_surface_stack_camera_descriptor(...)`、`colored_mesh_on_layer(...)` 与 `average_channel_in_region(...)`,替代旧 `primary_surface_overlay_clear_depth_controls_depth_reuse` 和 `render_primary_overlay_depth_scene(...)`。该切片只改测试归属与产品验收命名,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 root owner、camera-target 子模块和 `m4_behavior_layers.rs` 通过;冲突标记扫描无输出;源码扫描确认旧 render-order/RT/Overlay 测试名及旧 helper 均已从 `src/graphics/tests/*.rs` 移除,三个规范名随后已集中到 `render_product_camera_targets/primary_surface.rs` 和 root custom-target owner;测试树静态扫描无新增 `TODO`/`FIXME`/`#[allow(dead_code)]`/FFI/plugin builder/`Result<_, String>` 债,仅 `m4_behavior_layers.rs` 保留既有 `submit_extract(...) -> Result<(), String>` 辅助函数;`git diff --check` 通过,仅 LF->CRLF 提示;行数:camera-target root 825、assertions 148、camera 87、fixture 400、mesh 163、m4 1492。focused locked `cargo test -p zircon_runtime --lib render_product_overlay_stack_composites_over_base --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-camera-targets-canonical-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被 Cargo 在编译前拒绝:`cannot update the lock file ... because --locked was passed`,不计 WGPU pass。 | 解阻 `Cargo.lock`/manifest 漂移后,与其他 `render_product_camera_targets` 守卫一起重跑 focused locked WGPU guard group;继续 custom-target product/RenderDoc、UI/scene overlay 宽场景和 editor authoring 面板。 |
| 2026-06-23 | CO-M2 PrimarySurface render_order canonical product guard migration | 已完成源码归属迁移: 计划表中的 `render_product_camera_render_order_swap_changes_composite` 现在是当前可运行的 folder-backed camera-target 产品守卫;旧 `m4_behavior_layers.rs` render-order 测试与专用 helper 已删除;尚未形成新的 WGPU/Cargo 运行结果 | `render_product_camera_targets/primary_surface.rs::render_product_camera_render_order_swap_changes_composite` 提交两个 full-viewport PrimarySurface Base camera,交换 red/green 两个 camera 的 `render_order`,断言最终捕获颜色随较晚 Base 在 green/red 之间反转。该守卫复用 `RenderFixture::configured_framework(...)` 和 primary-surface descriptor helper,替代旧 `primary_surface_base_camera_render_order_swap_changes_composite` 与 `render_primary_base_order_scene(...)`。该切片只改测试归属与产品验收命名,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `render_product_camera_targets.rs` 与 `m4_behavior_layers.rs` 通过;冲突标记扫描无输出;源码扫描确认旧 render-order 测试名、旧 helper 和旧 RT 采样测试名均已从 `src/graphics/tests/*.rs` 移除,两个规范名随后已集中到 `render_product_camera_targets/primary_surface.rs` 和 root custom-target owner;测试树静态扫描无新增 `TODO`/`FIXME`/`#[allow(dead_code)]`/FFI/plugin builder/`Result<_, String>` 债,仅 `m4_behavior_layers.rs` 保留既有 `submit_extract(...) -> Result<(), String>` 辅助函数;`git diff --check` 通过,仅 LF->CRLF 提示;行数:camera-target root 734、assertions 117、camera 68、fixture 400、mesh 139、m4 1622。当前仍有 cargo/rustc 进程,本切片不启动 focused Cargo/WGPU,不计 WGPU pass。 | 与其他 `render_product_camera_targets` 守卫一起在 Cargo/RenderDoc 环境稳定后重跑 focused locked WGPU guard group;继续 overlay stack canonical migration、custom-target product/RenderDoc、UI/scene overlay 宽场景和 editor authoring 面板。 |
| 2026-06-23 | CO-M2 dual-camera RT -> PrimarySurface canonical product guard migration | 已完成源码归属迁移: 计划表中的 `render_product_dual_camera_rt_then_main` 现在是当前可运行的 folder-backed camera-target 产品守卫;旧 `m4_behavior_layers.rs` 重复守卫已删除;尚未形成新的 WGPU/Cargo 运行结果 | `render_product_camera_targets/custom_target/material_sampling.rs::render_product_dual_camera_rt_then_main` 先用 order=-1 texture camera 把 sRGB `RenderTarget + Sampled` RT clear 为 red,再让 order=1 PrimarySurface camera 的 fullscreen material 采样该 prepared RT;该守卫复用 `RenderFixture::configured_framework(...)`、camera descriptor helpers 与 layer-scoped fullscreen mesh,并把旧 `texture_target_render_order_feeds_later_primary_surface_material_sample` 从 oversized `m4_behavior_layers.rs` 迁出。该切片只改测试归属与产品验收命名,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `render_product_camera_targets.rs` 与 `m4_behavior_layers.rs` 通过;冲突标记扫描无输出;源码扫描确认旧测试名已从 `src/graphics/tests/*.rs` 移除且规范名后续迁入 `render_product_camera_targets/custom_target/material_sampling.rs`;测试树静态扫描无新增 `TODO`/`FIXME`/`#[allow(dead_code)]`/FFI/plugin builder/`Result<_, String>` 债,仅 `m4_behavior_layers.rs` 保留既有 `submit_extract(...) -> Result<(), String>` 辅助函数;`git diff --check` 通过,仅 LF->CRLF 提示;行数:camera-target root 682、assertions 117、camera 68、fixture 400、mesh 139、m4 1675。当前仍有 cargo/rustc 进程,本切片不启动 focused Cargo/WGPU,不计 WGPU pass。 | 与其他 `render_product_camera_targets` 守卫一起在 Cargo/RenderDoc 环境稳定后重跑 focused locked WGPU guard group;继续 custom-target product/RenderDoc、UI/scene overlay 宽场景和 editor authoring 面板。 |
| 2026-06-23 | CO-M1/M2 output-target prepared format drift guard | 部分完成: renderer 内部 graph-import/writeback planner 现在会在 prepared WGPU resource descriptor format 与 selected camera preflight format label 不一致时阻断 direct import/copy/conversion;WGPU/Cargo 产品验证仍未取得新通过结果 | `ViewportTextureWritebackStatus` 与 `ViewportTextureGraphImportStatus` 新增内部 `BlockedPreparedFormatMismatch`;`ViewportRenderOutputTarget::writeback_plan(...)` / `graph_import_plan(...)` 同时保存 actual prepared format 与 expected preflight format,并先检查 prepared descriptor format 是否匹配 selected camera output-target format label;resource streamer 将该内部阻断投影到既有 neutral `blocked_format_mismatch` report,避免扩张公开 stats DTO。 | 新增 source guards:`output_target_writeback_plan_blocks_prepared_format_drift`、`output_target_graph_import_plan_blocks_prepared_format_drift`;`rustfmt --edition 2021 --check` 覆盖 3 个 touched Rust files 通过;冲突标记扫描无输出;`TODO|FIXME|#[allow(dead_code)]|RuntimePluginDescriptor::new|extern "C"` 扫描无输出;scoped `git diff --check` 通过,仅仓库 LF→CRLF 提示;行数:output-target 681、ensure-scene-resources 447、execute-output-target-writeback 363。当前仍有多个 Cargo/Rust 编译进程占用环境,本切片未运行新的 Cargo/WGPU 验证,不计 Cargo 通过。 | 等 active Cargo lanes 与 lock/manifest 漂移收束后重跑 `viewport_render_output_target`、`compiled_graph_cache` 与 `render_product_camera_targets` focused locked tests;继续 custom-target product/RenderDoc 与 UI/scene overlay 宽场景验收。 |
| 2026-06-23 | CO-M1/M2 texture-target output format label propagation | 部分完成: selected camera texture target 的解析结果现在同时驱动 compiled graph cache key 与 renderer-bound output target 格式标签;WGPU 产品测试仍未取得新通过结果 | `ResolvedCameraTargetDescriptor` 新增 `texture_format`,从 `RenderGraphCompileTextureTargetFormat::as_format_label()` 取得 `rgba8unorm_srgb` / `rgba8unorm`;`ViewportRenderOutputTarget::Texture` 新增 `format` 字段与 `texture_format()` accessor;`build_frame_submission_context` 以同一次 target preflight 构造 output target,让 cache-key format、frame output target identity、writeback/graph-import planning 使用同一解析事实;输出目标、resource streamer 和 final-target 测试夹具同步补齐格式字段,新增 `output_target_from_camera_target_retains_resolved_texture_format` source guard。 | `rustfmt --edition 2021 --check` 覆盖 `viewport_render_output_target.rs`、`graphics/types/mod.rs`、`compiled_graph_cache.rs`、build-context、build_runtime_frame、resource_streamer output-target 文件与 `final_target_output.rs` 通过;旧 `Texture { handle, size }` 构造/旧 two-arg `from_camera_target`/新增触碰文件 `TODO|FIXME|#[allow(dead_code)]|RuntimePluginDescriptor::new|extern "C"` 扫描均无输出;scoped `git diff --check` 通过,仅仓库 LF→CRLF 提示;focused locked `cargo test -p zircon_runtime --lib viewport_render_output_target --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-output-target-format-0623 --message-format short --color never -- --test-threads=1 --nocapture` 进入编译但 124s 超时,未产出测试结果,事后进程审计未发现该 target-dir 残留 cargo/rustc,不计 Cargo 通过。 | 待当前 active Cargo lanes 与根 `Cargo.lock` / manifest 漂移收束后重跑 `viewport_render_output_target`、`compiled_graph_cache` 与 `render_product_camera_targets` focused locked tests;继续推进更宽 custom-target WGPU product/RenderDoc 与 UI/scene overlay 宽场景对拍。 |
| 2026-06-23 | Render index 当前状态总览拆分 | CO-M1/3/4 部分完成,CO-M2 sequence/output policy 与 per-camera state owner 部分完成;CO-M1 `CameraRenderDescriptor`/Base+Overlay sequence resolver、extract-side `RenderViewExtract.cameras`、descriptor-driven scene layer union/custom-target visibility consumer、selected descriptor target/layer submit preflight、descriptor-owned snapshot hard cutover、offscreen/present/direct runtime-frame camera loop、terminal UI routing、target-sensitive graph cache key、camera clear plan + region-scoped scene clear draw、fixed `scene-color`/`scene-depth` physical target binding、stack-terminal final-target owner gate with suppressed diagnostics、generated offscreen/surface-present/direct runtime-frame viewport-terminal shared record/present owner gate、explicit shared viewport product/debug owner helper、shared light-grid product report boundary、selected-camera light-grid report/VG debug snapshot product-report slots、selected-camera temporal history/static-index/motion-vector/particle previous-state/HGI runtime/VG runtime slots、selected-camera `ViewportRenderRegion` viewport/scissor graph-raster enforcement、fullscreen post-process origin policy、terminal output-transfer/FXAA/SMAA local/physical region split、graph-owned Hybrid GI history copy、texture-target overlay clear-preservation 与 layered mesh product guards、linear texture conversion final-product guard、split-screen Base viewport product guard、independent multi-texture target guard source/readback helper、screen-space UI graph-tail ordering、UI/scene overlay focused pixel product guard、dynamic-resolution terminal AA target sizing、render-target texture material sampling guard、Bloom-before-Uber pass-authoring fix、FXAA product executor output-access split、CO-M3 `RenderQueueValue` queue authority/material snapshot validation、final `u64` sort-key hard cutover、transparent Sprite/Mesh mixed submission path 与 world-space UI-like transparent member product path 已接入 core framework/scene/visibility/submit/graphics;三条 texture-target overlay/linear focused runs、split-screen focused product run、independent multi-texture focused product run、render-target texture material sampling focused product run、output-policy/camera-loop owner helper focused runs、screen-space UI graph-tail focused runs、UI/scene overlay pixel focused run、dynamic-resolution terminal AA descriptor test、light-grid stats/product diagnostics filter、`render_sort_key` 7 tests、phase sort key 3 tests、geometry/sprite/material-adjusted phase queue focused tests、transparent submission/replayer focused tests 和 transparent3d product tests 已通过;per-camera history owner、per-camera previous-state owner 与 shared light-grid product-report `core-min` checks 已通过;dynamic-scene value helper visibility blocker 已修复 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 09 Camera/Ordering 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：editor authoring 面板、更宽像素/product custom-target composite、透明混排 RenderDoc、UI/scene overlay RenderDoc/宽场景对拍；验收缺口：需要更宽 Base/Overlay/custom-target WGPU 合成产品规则、history/previous-state 产品规则、remaining focused lib-test wrapper 清理、像素/Product/RenderDoc 验收 |
| 2026-06-15 | CO-M1 camera contract and multi-camera loop | 部分完成: camera snapshot/layer 基础存在,2026-06-18 visibility payload bridge、neutral descriptor/sequence contract、extract-side `RenderViewExtract.cameras`、descriptor-driven visibility consumer、selected descriptor submit preflight、descriptor-owned snapshot hard cutover 与 offscreen submit loop scaffold 已接入;完整 Base/Overlay 合成仍未完成 | `ViewportCameraSnapshot`、`RenderCameraTarget`、`RenderLayer`、temporal jitter 与 view/projection matrix pair 已被计划 06 使用;`CameraRenderDescriptor`/`resolve_camera_sequence` 已提供 Base/Overlay 序列契约;scene-backed `RenderViewExtract` 现在携带 active scene camera descriptor 列表,scene layer candidate union、custom-target visibility、submit target resolution/output-target/present preflight 已从 selected descriptor 读取 target/layer;offscreen submit 已通过 `submit/camera_loop.rs` 将 Base/Overlay descriptors 投影为 selected-camera child extracts。 | 计划 06 TP-M2 状态表记录 `temporal_jitter`、Halton、matrix pair 与 scene uniform ABI;计划 04 状态表记录 per-view visibility 和 2026-06-18 custom-target visibility payload bridge;core-min `cargo check`、lib-test `--no-run` 与 direct binary camera/extract/visibility exact tests 通过;本表新增 2026-06-18 descriptor/sequence、extract descriptor、descriptor visibility consumer、selected descriptor submit preflight、descriptor-owned hard cutover 与 M1-S2 loop scaffold focused evidence。 | 完成 Base/Overlay attachment reuse/load-op、custom target WGPU composite/output ownership、per-camera post/history/light ownership、surface present/direct runtime-frame 多相机路径和 editor authoring 面板。 |
| 2026-06-18 | CO-M1 M1-S1 camera descriptor/sequence contract | 部分完成: neutral `CameraRenderDescriptor` 与 Base/Overlay sequence resolver 落地;hard cutover 和 submit loop 未开始 | 新增 `core/framework/render/camera_stack.rs`,定义 `CameraRenderType`、`RenderCameraClear`、`CameraRenderDescriptor`、`CameraSequenceReport`、`CameraSequenceEntry` 与 violation reason;`resolve_camera_sequence(...)` 只让 Base 相机成序,按 Base stack 顺序挂接 Overlay,继承 Base target/viewport,并把缺失、非 Overlay、target 不匹配、Overlay 带 stack 记入 report;`camera_ordering.rs` 的 `RenderCameraOrderInput` 改为 descriptor-backed,保留 `ViewportCameraSnapshot` transitional constructor;`mod.rs` 导出新契约。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-stack-contract-0618 --message-format short --color never` 通过(既有 warning set);filtered `cargo test -p zircon_runtime --lib render_camera_ ...` 在 lib-test binary 编译完成后因工具 timeout/BrokenPipe 未计为通过;直接运行 hot binary exact tests 通过: `render_camera_sequence_sorts_by_render_order`、`render_camera_stack_overlay_follows_base_and_inherits_target_viewport`、`render_camera_stack_rejects_invalid_members`、`render_camera_ordering_sorts_by_order_then_target_and_tracks_target_hdr_index`。 | 继续 M1-S1 hard cutover: `RenderViewExtract.cameras`、scene/editor projection fields、删除 snapshot 上 target/order 直接所有权;随后协调 dirty submit_frame_extract worktree 后接 `camera_loop.rs`、per-camera post/history/light ownership 和 Base/Overlay WGPU 输出。 |
| 2026-06-18 | CO-M1 M1-S1 extract-side camera descriptor cutover | 部分完成: `RenderViewExtract.cameras` 已落地并由 scene extract 填充 active scene camera descriptors;single-camera submit 仍读 `view.camera` | `RenderViewExtract` 新增 `cameras: Vec<CameraRenderDescriptor>`, synthetic/explicit camera 路径自动生成单元素 descriptor;scene-backed extract 以 deterministic camera schedule order 写入 active scene camera descriptors,并把 selected scene camera descriptor 对齐到 request override 后的 effective `view.camera`;`apply_target_size(...)` 只同步 selected/synthetic descriptor,避免污染其他 Texture/Headless camera descriptors;`SortedRenderCamera` 增 `render_type`。 | `rustfmt --edition 2021` 已跑过 touched Rust files;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-extract-descriptors-0618 --message-format short --color never` 通过(既有 warning set);三次 `cargo test --exact` 因未给完整路径只匹配 0 tests,不计为通过;随后直接运行 lib-test binary exact tests 通过: `scene::tests::render_extract::render_frame_extract_carries_scene_camera_order_report_for_scene_camera`、`scene::tests::render_extract::explicit_camera_render_frame_extract_has_no_scene_camera_order_report`、`core::framework::render::camera_ordering::tests::render_camera_order_report_carries_descriptor_render_type`。 | 继续 M1-S1: 把 scene/editor 相机字段投影迁到 descriptor 所有权,再删除 `ViewportCameraSnapshot` 上 `target`/`order`/`viewport`/`clear_color`/`render_layers` 残留直接所有权;之后协调 dirty submit path 接 `camera_loop.rs`。 |
| 2026-06-18 | CO-M1 M1-S1 descriptor-driven visibility consumer cutover | 部分完成: scene layer union 与 custom-target visibility 已消费 `RenderViewExtract.cameras`;single-camera submit 仍读 `view.camera` | `RenderViewExtract::selected_camera_descriptor()` 提供 selected scene/synthetic descriptor read path;`World::render_extract_layers_for_view(...)` 改用 selected descriptor layers 加 Texture/Headless descriptor layers 构造 mesh/sprite candidate layer union;`FrameVisibility::from_frame_views(...)` 改接收 `&[CameraRenderDescriptor]`, custom-target views 从非 PrimarySurface descriptors 构建并以 descriptor `culling_mask` 过滤 primitive relevance;`RenderCameraOrderReport` 保留 ordering diagnostics,不再作为 visibility custom-target payload source。 | `rustfmt --edition 2021` 已跑过 touched Rust files;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-descriptor-visibility-0618 --message-format short --color never` 通过(既有 warning set);首次并行 `cargo test` 因同 target dir Cargo lock contention 超时且未产出 binary,不计为通过;`cargo test -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-descriptor-visibility-0618 --no-run --message-format short --color never` 通过;直接运行 lib-test binary exact tests 通过: `graphics::visibility::context::from_extract_with_history::construct::tests::visibility_context_builds_custom_target_view_from_camera_descriptors`、`scene::tests::render_extract::render_frame_extract_keeps_custom_target_layer_geometry_for_visibility_views`、`scene::tests::render_extract::render_frame_extract_carries_scene_camera_order_report_for_scene_camera`。 | 继续 M1-S1: 把剩余 scene/editor 相机字段读写迁到 descriptor 所有权,删除 `ViewportCameraSnapshot` 上 `target`/`order`/`viewport`/`clear_color`/`render_layers` 直接所有权;随后协调 dirty submit path 接 `camera_loop.rs`。 |
| 2026-06-18 | CO-M1 M1-S1 selected descriptor projection and submit preflight cutover | 已被后续 hard cutover 取代: selected camera target/layer/viewport sizing consumers started reading descriptor;`camera_loop.rs` 未开始 | `CameraRenderDescriptor::as_effective_camera()` 与 `RenderViewExtract` selected descriptor helpers 让 submit target resolution/output-target/target diagnostics、surface present preflight、post-process volume layer resolution 与 main-view visibility relevance 改读 selected descriptor;当时仍保留 snapshot 过渡投影,随后同日 hard cutover 删除了 snapshot 五字段。 | `rustfmt --edition 2021` 已跑过 touched Rust files;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-selected-descriptor-0618 --message-format short --color never` 通过(既有 warning set);新增 `render_view_apply_target_size_preserves_descriptor_target_and_layers` 覆盖 descriptor target/layer/viewport sizing projection;focused `cargo test -p zircon_runtime --lib render_view_apply_target_size_preserves_descriptor_target_and_layers --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-selected-descriptor-0618 --message-format short --color never -- --nocapture` 在 shared lib-test 编译阶段 904s timeout,无测试结果,已清理该 target-dir Cargo/rustc 进程,不计为通过。 | 已由后续 descriptor-owned snapshot field hard cutover 收束 snapshot 字段删除;继续 `camera_loop.rs`、per-camera history/light/post ownership 与 custom target WGPU 输出。 |
| 2026-06-18 | CO-M1 M1-S1 runtime-frame camera descriptor accessor cutover | 部分完成: renderer-bound `ViewportRenderFrame::camera()` 已返回 selected `CameraRenderDescriptor`;snapshot payload 仍是 transitional projection,previous motion-vector camera 仍为 snapshot,`camera_loop.rs` 未开始 | `ViewportRenderFrame::camera()` 改为 descriptor-backed accessor,新增 `effective_camera()` 作为矩阵/阴影/粒子/post-process/temporal history 等 legacy math consumers 的 selected descriptor projection;`RenderViewExtract::sync_selected_descriptor_camera_payload()` 在 frame 构造与 submit jitter 投影时同步 transform/projection/dynamic-resolution/MSAA/temporal jitter payload,同时保留 target/order/viewport/clear/layer 的 descriptor 所有权;scene uniform、shadow cascades、selection/handle/gizmo billboard axes、particle quads、DoF prepare、velocity-camera pass、temporal/particle history 改读 `effective_camera()`。 | `rustfmt --edition 2021` 已跑过 touched Rust files;stale scan 确认 `frame.camera()` snapshot-style consumers 已迁移,仅保留 `frame.camera().camera.temporal_jitter` descriptor payload regression assertion;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-runtime-frame-descriptor-0618 --message-format short --color never` 通过(既有 141 warning set,6m32s);focused unit/product tests 未在本实现切片执行,留到 milestone testing stage。 | 继续 M1-S1: 迁移剩余 scene/editor 相机字段写入并删除 `ViewportCameraSnapshot` 上 `target`/`order`/`viewport`/`clear_color`/`render_layers` 直接所有权;随后接 `camera_loop.rs`、per-camera history/light/post ownership 与 custom target WGPU 输出。 |
| 2026-06-18 | CO-M1 M1-S1 descriptor-owned snapshot field hard cutover | 部分完成: `ViewportCameraSnapshot` 五个 descriptor-owned 字段已删除;single-effective-camera submit 仍存在,`camera_loop.rs` 未开始 | `ViewportCameraSnapshot` 现在只保留 transform/projection/aspect/active/HDR/exposure/MSAA/dynamic-resolution/temporal-jitter payload;`CameraRenderDescriptor` 独占 target、viewport_rect、render_order、clear、culling_mask、volume_mask;`SceneViewportExtractRequest::camera` 改为 `Option<CameraRenderDescriptor>`;scene extract、ordering、visibility、submit preflight、runtime-frame accessors、pipeline/test fixtures 和 `FrameHistoryValidationKey` 均改读 selected descriptor,避免 history/target/layer state 因 snapshot 收缩丢失。 | `cargo fmt --package zircon_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-descriptor-hard-cutover-0618 --message-format short --color never` 通过(既有 warning set);`cargo test -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-descriptor-hard-cutover-0618 --no-run --message-format short --color never` 通过(既有 warning set);stale field scans、conflict marker scan、scoped `git diff --check` 通过,仅有仓库 LF/CRLF 提示。 | 下一步转 M1-S2 前先协调 editor 面板/authoring 字段与 dirty submit worktree;随后实现 `camera_loop.rs`、per-camera post/history/light ownership、Base/Overlay load-op 翻译和 custom-target WGPU 输出。 |
| 2026-06-18 | CO-M1 M1-S2 submit camera loop scaffold | 部分完成: generated offscreen submit 已按 resolved Base/Overlay descriptors 循环执行 selected-camera child extracts;Base/Overlay load-op、target reuse/composite、present/direct runtime-frame 与 per-camera ownership 未完成 | 新增 `graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop.rs`;`submit_frame_extract_with_ui(...)` 在一个 operation lock 与 `submit_frame_extract` profile scope 内调用 `submit_camera_loop(...)`;loop 使用 `resolve_camera_sequence(...)` 扁平化 Base→Overlay 顺序,并用 `RenderFrameExtract::with_selected_camera_descriptor(...)` 生成只含当前 selected descriptor 的 child extract 后复用原 single-camera submit body。 | `cargo fmt --package zircon_runtime` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-loop-0618 --message-format short --color never` 通过(既有 warning set);`cargo test -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-loop-0618 --no-run --message-format short --color never` 通过;focused tests 通过: `camera_loop` 2 tests 与 `render_frame_extract_selected_camera_descriptor_replaces_active_selection_only` 1 test。 | 继续 M1-S2/M2: Base/Overlay attachment load/store 与 clear/clear_depth 翻译,自定义 target 输出 ownership/composite,per-camera post/history/light state,screen-space UI final composite ownership,surface present/direct runtime-frame 多相机路径,以及双相机 RT/product 像素证据。 |
| 2026-06-18 | CO-M1 M1-S2 screen-space UI terminal routing | 部分完成: `camera_loop` 已把共享 UI extract 路由到 terminal Base stack child;full Base/Overlay composite/attachment ownership 未完成 | `camera_loop_submissions(...)` 现在为每个 selected-camera child 标记 `receives_terminal_ui`;`terminal_screen_space_ui_camera_position(...)` 选择最后一个 `PrimarySurface` Base stack,没有 primary 时回退到最后一个 Base stack 以保留 texture/headless-only 单目标 UI 行为;terminal child 是该 stack 的最后一个 Overlay,没有 Overlay 时为 Base;非 terminal child 传入 `None` UI。 | `cargo fmt --package zircon_runtime` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-ui-terminal-0618 --message-format short --color never` 通过(既有 warning set);`cargo test -p zircon_runtime --lib camera_loop --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-ui-terminal-0618 --message-format short --color never -- --nocapture` 通过 4 tests。 | 继续 M1-S2/M2: Base/Overlay attachment load/store 与 clear/clear_depth 翻译,final target composite ownership,per-camera post/history/light state,surface present/direct runtime-frame 多相机路径,以及双相机 RT/product 像素证据。 |
| 2026-06-18 | CO-M1 M1-S2 camera target compile fingerprint | 部分完成: compiled graph cache key 已包含 selected camera target/render type/viewport-rect presence;texture target format 当时尚未进入 key(已由 2026-06-23 格式类切片补齐),完整 composite ownership 未完成 | `CompiledGraphCacheKey` 的 `RenderGraphCompileFrameFingerprint` 新增 `RenderGraphCompileCameraTargetFingerprint`、`camera_render_type` 与 `viewport_rect_present`;target 指纹区分 PrimarySurface、Texture `ResourceId` 与 Headless size;`extract_compile_fingerprint(...)` 改读 selected `CameraRenderDescriptor` 的 camera payload,避免 descriptor hard cutover 后与 transitional `view.camera` 不一致;`CameraRenderType` 补 `Hash`。 | `cargo fmt --package zircon_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-target-fingerprint-0618 --message-format short --color never` 通过(既有 warning set);首次 `cargo test -p zircon_runtime --lib compiled_graph_cache ...` 暴露 test-only 导入/move 与旧测试直接改 `view.camera` 的契约漂移,修正后同命令通过 8 tests。 | 2026-06-23 已把 prepared texture descriptor 的格式类/尺寸接入 compile key;继续 CO-M2 physical attachment reuse、final output owner、per-camera history/post/light 与像素证据。 |
| 2026-06-18 | CO-M1/M2 camera clear-load attachment policy | 部分完成: selected-camera child frame 已把 Base/Overlay clear 语义转成 scene color/depth 首写 load ops;物理 attachment reuse/composite 未完成 | 新增 `graphics/types/viewport_camera_stack_attachment_policy.rs`;`ViewportRenderFrame` 构造时从 selected `CameraRenderDescriptor` 派生策略;`RenderPassGpuExecutionContext` 暴露 frame policy;`RenderPassExecutionContext::attachment_ops_for_write(...)` 在 GPU frame 存在时只改 `scene-color`/`scene-depth` graph-declared 首写 `Clear`,保留 store op 与后续 `Load` 写入。Base `Skybox`/`Color` 清 color+depth,`DepthOnly` load color/clear depth,`None` load+load,`None`+MSAA>1 清 color/load depth;Overlay color 恒 load,depth 按 `clear_depth`。 | `cargo fmt --package zircon_runtime` 已运行;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-attachment-policy-0618 --message-format short --color never` 通过(既有 warning set);新增 4 个策略源码合同测试,但 `cargo test -p zircon_runtime --lib viewport_camera_stack_attachment_policy ...` 在 shared lib-test 编译阶段 904s timeout,无测试通过结果,残留 Cargo/rustc 进程已清理。 | 继续 CO-M2: Overlay 复用 Base 物理 color/depth attachment/import,final Base-stack composite owner,per-camera post/history/light state,custom target 输出、split-screen product evidence 和像素/RenderDoc 对拍。 |
| 2026-06-18 | CO-M1/M2 camera stack output ownership policy | 部分完成: camera loop 已标记 stack/viewport terminal child,非 stack-terminal child 不再写 texture output target;generated offscreen 非 viewport-terminal child 不再消费 pending capture、分配 shared history、写 viewport record/history/stats;物理 composite 与独立 per-camera state 未完成 | 新增 `graphics/types/viewport_camera_stack_output_policy.rs`;`camera_loop` 为每个 child 计算 `stack_terminal` 与 `viewport_terminal`,并把策略经 `build_runtime_frame(...)` 写入 `ViewportRenderFrame`。`direct_imported_final_target(...)` 对非 stack-terminal child 返回 `None`,`render_frame_with_pipeline_to_target(...)` 对这些 child 调用 `suppress_output_target_writeback(...)`,`output_target_capture_resource(...)` 不再从 prepared texture target 捕获,避免中间 Base/Overlay child 过早改写最终 texture 输出。`submit_selected_camera_frame(...)` 现在用 `owns_viewport_submission()` gate graphics-debugger capture、`resolve_history_handle(...)`、`record_submission(...)`、temporal/particle previous-state、history release、virtual-geometry debug snapshot 和 `update_stats(...)`;非 owner 使用 inactive history 并只 drain renderer feedback。Present/direct runtime-frame 路径保持默认 terminal 策略。 | `cargo fmt --package zircon_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-output-policy-0618 --message-format short --color never` 通过(既有 warning set);首次 focused tests 因并行同 target-dir 与 cold lib-test compile timeout,不计为通过;热 rerun 通过 `cargo test -p zircon_runtime --lib camera_loop ...` 5 tests 和 `cargo test -p zircon_runtime --lib build_runtime_frame ...` 1 test。viewport-owner follow-up `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-record-owner-0618 --message-format short --color never` 通过(既有 warning set),hot rerun 通过 `cargo test -p zircon_runtime --lib camera_loop ...` 5 tests 与 `cargo test -p zircon_runtime --lib resolve_history_handle ...` 1 test。 | 继续 CO-M2: Overlay 复用 Base 物理 color/depth attachment/import,独立 per-camera post/history/light state,custom target composite 输出、split-screen product evidence 和像素/RenderDoc 对拍。 |
| 2026-06-18 | CO-M2 fixed scene target physical reuse contract | 部分完成: Base/Overlay child graph submits 共享 fixed `OffscreenTarget` scene color/depth backing 的底层契约已受测;final composite 与像素/product 验收未完成 | `bind_frame_graph_resources(...)` 对 live `SCENE_COLOR`/`SCENE_DEPTH` 绑定 renderer-owned `OffscreenTarget` views,不让它们进入 graph-owned transient materialization;`ViewportCameraStackAttachmentPolicy` 决定 Base/Overlay 首写 clear/load,因此 Overlay load 可以保留 Base child 的物理 color/depth 内容。新增 `frame_binder_reuses_fixed_scene_color_and_depth_targets` 断言 scene targets 是 external texture views 且没有 owned transient backing。 | `cargo fmt --package zircon_runtime -- --check` 通过;首次 fresh target-dir focused test 超时且无产物,不计为通过;热 target-dir `cargo test -p zircon_runtime --lib frame_binder_reuses_fixed_scene_color_and_depth_targets --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-record-owner-0618 --message-format short --color never -- --nocapture` 通过 1 test(既有 warning set)。 | 继续 CO-M2: final custom-target composite owner、独立 per-camera post/history/light/HGI/VG/particle state、surface present/direct runtime-frame 多相机路径和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 selected-camera viewport/scissor render region | 部分完成: graph-raster camera passes now apply selected `CameraRenderDescriptor.viewport_rect` as clamped WGPU viewport+scissor;split-screen raster isolation contract exists,但 final composite/product evidence 仍未完成 | 新增 `ViewportRenderRegion` 并挂到 `ViewportRenderFrame`;frame constructors derive it from selected camera plus target size;normal prepass、base mesh、deferred G-buffer/lighting、sprite、CPU particle billboard、particle velocity、TAA reactive-mask mesh、preview sky、grid/wireframe/selection/gizmo/handle overlays 与 particles plugin GPU transparent draw 都应用该 region;plugin direct tests use `ViewportRenderRegion::full_target(...)` for old full-target behavior。 | `cargo fmt --package zircon_runtime -- --check` 通过;`cargo fmt -p zircon_plugin_particles_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-viewport-region-0619 --message-format short --color never` 通过(既有 warning set);`cargo test -p zircon_runtime --lib viewport_render_region --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-viewport-region-0619 --message-format short --color never -- --nocapture` 通过 3 tests;`cargo check -p zircon_plugin_particles_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-viewport-region-0619-particles --message-format short --color never` 通过;`cargo test -p zircon_plugin_particles_runtime particle_gpu_runtime_owner_ --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-viewport-region-0619-particles --message-format short --color never -- --nocapture` 通过 4 tests。 | 继续 CO-M2: final custom-target composite owner、独立 per-camera post/history/light/HGI/VG/particle state、surface present/direct runtime-frame 多相机路径和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 post-process viewport origin and terminal writeback | 部分完成: fullscreen post-process now distinguishes local graph-owned viewport textures from fixed full-frame source/history textures and writes terminal output only into selected-camera region;final composite/product evidence 仍未完成 | `PostProcessParams` packs local viewport size, physical viewport origin, cluster dimensions, and scene source origin separately; `post_process.wgsl` / SSR WGSL read graph intermediates locally and fixed frame/history/G-buffer/AO inputs through physical origin; split bloom/DoF/motion-blur/scene-composite/blur/SSR executors receive source origins; `BLOOM` and `GLOBAL_ILLUMINATION` are graph-owned post-process textures; Hybrid GI history copies graph-owned GI into the selected camera physical history region; output-transfer/FXAA/final SMAA resolve apply `ViewportRenderRegion` viewport/scissor and subtract a terminal-origin uniform while SMAA edge/blend remain local. | `cargo fmt --package zircon_runtime` 通过;`cargo test -p zircon_runtime --lib post_process_params_pack_viewport_and_scene_source_origins_separately --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-postprocess-region-tests-0619 --message-format short --color never -- --nocapture` 通过 1 test;`cargo test -p zircon_runtime --lib viewport_region_maps_local_postprocess_coords_to_physical_target_coords --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-postprocess-region-tests-0619 --message-format short --color never -- --nocapture` 通过 1 test;`cargo test -p zircon_runtime --lib history_region_copy --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-postprocess-region-tests-0619 --message-format short --color never -- --nocapture` 通过 2 tests;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-postprocess-region-tests-0619 --message-format short --color never` 通过(既有 warning set)。 | 继续 CO-M2: final custom-target composite owner、UI/scene overlay 顺序、per-camera light/VG/particle state、TAA/SSR/history product rules、surface present/direct runtime-frame 多相机路径和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 region-scoped scene clear draw | 部分完成: selected-camera Base/Overlay clear intent no longer depends on whole-target WGPU attachment load clear; color/depth clears are drawn through the selected `ViewportRenderRegion`, but final composite/product evidence remains open | `ViewportCameraStackAttachmentPolicy` now stores `ViewportSceneClearPlan` (`Skybox` preview-color+depth, `Color` color+depth, `DepthOnly` depth, `None`+MSAA transparent color, Overlay optional depth) and rewrites scene first-clear graph attachments to `Load`; new `scene_clear::SceneRegionClearResources` owns color/depth clear pipelines and records color-only/depth-only/color+depth fullscreen-triangle draws with viewport/scissor before the first graph stage in `SceneRendererCore::render_compiled_scene`. | `cargo fmt --package zircon_runtime` 通过;initial focused lib-test timed out during compile and then exposed unrelated dirty `NodeKind::Light` compile break, fixed to `NodeKind::PointLight`;`cargo test -p zircon_runtime --lib viewport_camera_stack_attachment_policy --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-region-clear-0619 --message-format short --color never -- --nocapture` 通过 4 tests;`cargo test -p zircon_runtime --lib scene_region_clear_resources --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-region-clear-0619 --message-format short --color never -- --nocapture` 通过 1 test;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-region-clear-0619 --message-format short --color never` 通过(既有 warning set)。 | 继续 CO-M2: final custom-target composite owner、UI/scene overlay 顺序、per-camera light/VG/particle state、TAA/SSR/history product rules、surface present/direct runtime-frame 多相机路径和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 surface-present camera loop ownership | 部分完成: `present_frame_extract` now uses the same Base/Overlay selected-camera loop as generated offscreen submit; only the viewport-terminal PrimarySurface child presents and owns shared viewport state, but direct runtime-frame multi-camera and pixel/RenderDoc evidence remain open | `camera_loop::viewport_terminal_camera_target(...)` derives the terminal target for preflight; `present_frame_extract_with_ui(...)` validates only that terminal target for surface present and preflights the bound surface once; per-child `present_selected_camera_frame(...)` builds context/runtime frame with `ViewportCameraStackOutputPolicy`, renders non-owner children through `render_frame_with_pipeline(...)` with inactive history, and presents/records only the viewport-terminal child through `present_frame_with_pipeline(...)`; non-owner feedback drains without mutating `ViewportRecord`/history/stats/capture. | `cargo fmt --package zircon_runtime` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-present-camera-loop-0619 --message-format short --color never` 通过(既有 warning set);focused `cargo test -p zircon_runtime --lib camera_loop ...` 在 shared lib-test compile/link 阶段超时,无测试结果,其 orphan process tree 已停止;`cargo check -p zircon_runtime --lib --tests ...` 因 unrelated `tests/virtual_geometry_debug_snapshot_contract.rs` stale `RenderMeshSnapshot` fixture fields 失败。 | 继续 CO-M2: direct runtime-frame 多相机路径、final custom-target composite/product owner、UI/scene overlay 顺序、per-camera light/VG/particle/history state 和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 direct runtime-frame camera loop ownership | 部分完成: direct `submit_runtime_frame(...)` now uses the same Base/Overlay selected-camera loop and viewport-terminal owner policy as generated offscreen and surface-present submit; final custom-target composite, independent per-camera state, and pixel/RenderDoc evidence remain open | `camera_loop_frame_submissions(...)` projects an existing `ViewportRenderFrame` into selected-camera child frames, routes UI only to the viewport-terminal child, preserves frame-local shader quality/output target/previous motion-vector camera/virtual-geometry debug snapshot/runtime-prepare collector scene snapshot, and carries stack/viewport output policy. `submit_runtime_frame(...)` loops those child frames under one operation lock, renders non-owner children with inactive shared history, and records shared viewport/history/stats/previous-state/debug-capture only for the viewport-terminal child. `refresh_camera_policy_to_runtime_frame(...)` recomputes selected-camera payload, clear policy, and `ViewportRenderRegion` after target-size preflight. | `cargo fmt --package zircon_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-direct-camera-loop-0619 --message-format short --color never` 通过(既有 warning set);focused `cargo test -p zircon_runtime --lib camera_loop_frame_submissions ... --no-run` 首次 late failed with visible warning-only output and no test binary,log-backed rerun was interrupted during dependency compilation,不计为通过。 | 继续 CO-M2: final custom-target composite/product owner、UI/scene overlay 顺序、per-camera light/VG/particle/history state 和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 final-target output owner diagnostics | 部分完成: final-target texture output owner selection is now explicit and non-stack-terminal texture children report camera-stack suppression;pixel/product custom-target composite evidence remains open | `ViewportCameraStackOutputPolicy::owns_final_target_output()` replaces the old write helper; new `render/final_target_output.rs` owns direct final-target import selection and records `RenderCameraTargetGraphImportReport::suppressed_by_camera_stack(...)` for texture non-owners; `ResourceStreamer::suppress_output_target_writeback(...)` records matching `RenderCameraTargetWritebackReport::suppressed_by_camera_stack(...)`; product diagnostics added one-hot `render.camera.target.graph_import.suppressed_by_camera_stack` and `render.camera.target.writeback.suppressed_by_camera_stack`. | `rustfmt --edition 2021 --check` passed on touched Rust files;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-final-target-output-0619 --message-format short --color never` passed with existing warnings;direct lib-test binary passed filters `final_target_output` (2 tests), `suppressed_output_target_writeback_report_is_texture_only` (1 test), and `render_product_diagnostics_record_camera_stack_suppressed_target_output` (1 test);package-wide `cargo fmt --package zircon_runtime -- --check` is blocked by unrelated dirty `zircon_runtime/src/scene/dynamic_scene/session/io.rs` formatting drift. | 继续 CO-M2: pixel/product custom-target composite、UI/scene overlay 顺序、完整 per-camera light/VG/particle/history state 和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 texture-target overlay product guard, Bloom producer ordering, and FXAA output access | 部分完成: texture-target Base+Overlay direct-import product guard 已通过 focused WGPU product run;默认 PostProcess authored graph 中 `post.bloom-extract` 已规范到 `post.uber` 前;产品后处理 executor 也修复了把 produced output 当作 Read 校验的 FXAA 访问缺口 | 新增 `graphics_camera_target_texture_overlay_stack_preserves_base_composite`,构造同一 sRGB texture target 的 Base red clear + Overlay clear none stack,断言 stack-terminal capture 走 DirectImported/SkippedDirectImport 且 Base red pixels 保留;`pass_authoring.rs` 通过 `ordered_stage_pass_descriptors(...)` 在 PostProcess stage 将 `post.bloom-extract` 规范到 `post.uber` 前;`builtin_postprocess_executors.rs` 将 product node `required_inputs` 按 Read 校验、`produced_outputs` 按 Write 校验,保持 FXAA 读 `FINAL_COMPOSITED`/写 `FINAL_COLOR` 的图契约;默认 Forward+/Deferred pipeline expected pass order 已补齐完整默认后处理链。 | `rustfmt --edition 2021 --check` 覆盖 touched render Rust files 通过;`cargo test -p zircon_runtime --lib graphics_camera_target_texture_overlay_stack_preserves_base_composite --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never -- --test-threads=1 --nocapture` 通过 1 test(既有 warning set);同一新 lib-test binary exact rerun 通过 `default_forward_plus_pipeline_compiles_expected_stage_order_and_passes`、`default_deferred_pipeline_compiles_expected_stage_order_and_passes`、`compile_routes_output_transfer_through_fxaa_terminal_input` 和 `graphics_camera_target_texture_overlay_stack_preserves_base_composite`;path-scoped `git diff --check` 仅 LF/CRLF warnings,冲突标记扫描无命中,最终无 Cargo/rustc 遗留进程。 | 继续 CO-M2: pixel/product custom-target composite、UI/scene overlay 顺序、完整 per-camera light/VG/particle/history state 和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 layered texture-target overlay mesh product guard | 部分完成: texture-target Base+Overlay stack 现在有真实 overlay mesh 覆盖 base clear 的 WGPU product 证据;最终 custom-target composite、独立 per-camera state 和 RenderDoc 仍未完成 | 新增 `texture_target_overlay_camera_draws_layered_mesh_over_base_clear`,在 `m4_behavior_layers` 的资产 fixture 中注册 sRGB render-target texture,用内置 WGPU pipeline 提交同一 texture target 的 Base red clear camera 与 Overlay clear-none/`clear_depth=false` camera;Base 只看 layer 1,Overlay 只看 layer 2 绿色 quad,并断言 capture 为 `TextureDirectGraphImport`、graph import `DirectImported`、writeback `SkippedDirectImport`,且红底和绿叠加像素都保留。测试单独走 `RenderFixture::builtin_server()` 以避开旧 pluginized m4 post-process fixture,不改变既有 plugin behavior-layer 测试。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\tests\m4_behavior_layers.rs` 通过;`cargo test -p zircon_runtime --lib texture_target_overlay_camera_draws_layered_mesh_over_base_clear --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never -- --test-threads=1 --nocapture` 通过 1 test(既有 53-warning set);早期失败定位为旧 pluginized m4 renderer 的 stale `post.uber` descriptor 和 quad 投影面积过小,最终 guard 使用内置 pipeline + 0.75 scale quad 通过。 | 继续 CO-M2: final custom-target composite、UI/scene overlay 顺序、完整 per-camera light/VG/particle/history state 和 product/RenderDoc 对拍。 |
| 2026-06-19 | CO-M2 split-screen Base viewport guard and terminal coordinate split | 部分完成: PrimarySurface split-screen Base camera product guard and terminal local/physical coordinate split now have focused WGPU product evidence;2026-06-23 已把当前可运行守卫迁移为规范名 `render_product_split_screen_viewports`;final custom-target composite、independent per-camera state and RenderDoc remain open | 历史守卫 `graphics_primary_surface_split_screen_base_cameras_clear_only_their_viewport_regions` 构造两个 PrimarySurface Base cameras:左半红色 clear、右半绿色 clear,并按左右 inset 区域统计 dominant pixels;当前源码归属已迁入 `render_product_camera_targets.rs::render_product_split_screen_viewports`。`ViewportRenderRegion` 新增 local vs physical viewport/scissor helpers,graph-owned child targets use local origin zero,固定 renderer-owned final/scene targets keep physical origin;terminal output-transfer writes `FINAL_COMPOSITED` with local region when terminal AA is active,while FXAA and final SMAA resolve write `FINAL_COLOR` with physical region/origin。 | `rustfmt --edition 2021 --check` 覆盖 touched render/test Rust files 通过;`cargo test -p zircon_runtime --lib viewport_render_region --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never -- --test-threads=1 --nocapture` 通过 5 tests;split-screen guard initially exposed WGPU scissor validation when local child targets received physical scissor;Runtime 05 retention facade compile blocker cleared后,`cargo test -p zircon_runtime --lib graphics_primary_surface_split_screen_base_cameras_clear_only_their_viewport_regions --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never -- --test-threads=1 --nocapture` 通过 1 test(既有 53-warning set)。迁移后的规范名守卫尚未取得新的 WGPU/Cargo pass。 | 继续 CO-M2: final custom-target composite、UI/scene overlay 顺序、完整 per-camera light/VG/particle/history state 和 RenderDoc/product 对拍。 |
| 2026-06-20 | CO-M2 focused validation closeout | 部分完成: independent multi-texture product guard、output-policy/camera-loop owner helpers 与 light-grid stats/product diagnostics focused evidence 已补齐;更宽 custom-target composite、UI/scene overlay 顺序、独立 per-camera light/product-debug 和 RenderDoc/product 验收仍未完成 | 复用 `D:\cargo-targets\zircon-runtime-texture-linear-product-0620\debug\deps\zircon_runtime-c339c28ec98a5de7.exe` 直接执行已产出的 lib-test binary,补齐此前 Cargo wrapper 超时或 unrelated dynamic-scene blocker 后留下的 focused lane。 | direct exact `graphics::tests::surface_targets::texture_target::graphics_camera_target_texture_base_stacks_write_independent_texture_targets` 通过 1 test;direct exact `graphics::types::viewport_camera_stack_output_policy::tests::final_target_output_owner_is_stack_terminal_not_viewport_terminal` 通过 1 test;direct exact `graphics::runtime::render_framework::submit_frame_extract::submit::camera_loop::tests::camera_loop_marks_stack_and_viewport_output_owners` 通过 1 test;direct filter `light_grid_stats --test-threads=1 --nocapture` 通过 3 tests。Docs updated: Plan 09 supplement, render index, lighting docs, output-policy docs, camera-loop docs, render-product docs, and session note. | 继续 CO-M2: 更宽 pixel/product custom-target composite、UI/scene overlay ordering、统一 sort key 和 RenderDoc/product 对拍。 |
| 2026-06-20 | CO-M2 custom-target/PrimarySurface coexistence product guard | 部分完成: texture-target Base+Overlay stack 与后续 PrimarySurface Base 同帧共存已有 WGPU 产品像素证据;RT 产物被后续主相机材质采样由后续 `render-target texture sampled by later PrimarySurface material` 行补齐;RenderDoc/宽场景对拍仍未完成 | 新增 `texture_target_stack_preserves_composite_when_primary_surface_renders_later`,同一帧先提交 sRGB texture target Base red clear + Overlay clear-none green mesh stack,随后提交 render_order 更晚的 PrimarySurface Base blue clear。测试读取 prepared texture target 并捕获 viewport,断言 texture target 保留 red Base + green Overlay,同时 viewport capture 归后绘 PrimarySurface blue clear,防止后续主表面 clear 泄入 custom target。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\tests\m4_behavior_layers.rs` 通过;`cargo test -p zircon_runtime --lib texture_target_stack_preserves_composite_when_primary_surface_renders_later --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-runtime-camera-products-0620 --message-format short --color never -- --test-threads=1 --nocapture` 在 Windows lib-test compile/link 阶段超时但后台编译完成并产出更新后的 `target\\codex-runtime-camera-products-0620\\debug\\deps\\zircon_runtime-c339c28ec98a5de7.exe`;随后 direct binary `texture_target_stack_preserves_composite_when_primary_surface_renders_later --test-threads=1 --nocapture` 通过 1 test(4640 filtered)。 | 继续 CO-M2: UI/scene overlay RenderDoc/宽场景对拍、更宽 custom-target composite/product 和 product/RenderDoc 验收。 |
| 2026-06-20 | CO-M2 per-camera history slot owner foundation | 部分完成: temporal color history 与 visibility static index 已从 viewport-wide 单槽收束为 selected-camera keyed slots;remaining light/VG/particle/motion previous-state still viewport-wide | 新增 `ViewportCameraHistoryKey` 和 `ViewportRecord.camera_histories` map;key 由 selected `CameraRenderDescriptor` 的 entity、render_order、Base/Overlay type、target order key 与 viewport rect 组成;`resolve_viewport_record_state(...)` 和 `resolve_history_handle(...)` 按 key 读取 previous visibility/static-index/history;`ViewportFrameHistory` now stores `VisibilityStaticIndex`;generated offscreen、surface-present 和 direct runtime-frame non-viewport-terminal children now call `record_camera_history_after_success(...)` to rotate/release only their camera history slot while leaving shared capture/stats/previous motion/particle/VG debug state to the viewport-terminal child;`destroy_viewport(...)` releases all stored history handles. | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-history-owner-0619 --message-format short --color never` 通过(既有 warning set);filtered Cargo wrapper `cargo test -p zircon_runtime --lib camera_history ... --no-run` timed out during shared lib-test compile after exposing/fixing test-only helper drift, then direct binary execution passed `zircon_runtime-d071a300da0585cb.exe camera_history --test-threads=1 --nocapture` (4 tests) and exact `viewport_record_keeps_histories_per_camera_key` (1 test);docs updated: `docs/zircon_runtime/graphics/runtime/render_framework/viewport_record/history.md`, `camera_loop.md`, `viewport_camera_stack_output_policy.md`, `render-product-submit.md`, and render plan/index. | 继续 CO-M2: per-camera motion-vector camera、particle previous sprites、light/VG/HGI runtime state,final custom-target composite/product owner、UI/scene overlay 顺序和 RenderDoc/product 对拍。 |
| 2026-06-20 | CO-M2 per-camera previous-state owner expansion | 部分完成: motion-vector camera、particle previous sprites、Hybrid GI runtime state 与 Virtual Geometry runtime state 已从 viewport-wide 单槽收束为 selected-camera keyed slots;light/product debug ownership、final custom-target composite、UI/scene overlay 顺序和 RenderDoc/product 验收仍未完成 | `ViewportRecord` 新增 `motion_vector_cameras`、`particle_previous_sprites`、`hybrid_gi_runtimes`、`virtual_geometry_runtimes` keyed maps;`resolve_viewport_record_state(...)` 按 `ViewportCameraHistoryKey` 读取 previous motion-vector camera 与 particle previous sprites;`prepare_runtime_submission(...)` 按 key ensure HGI/VG runtimes;`record_submission(...)`/`record_present_submission(...)` 按 key apply runtime feedback;generated offscreen、surface-present 和 direct runtime-frame non-viewport-terminal children now call `record_non_viewport_camera_state_after_success(...)` to rotate their history slot, apply HGI/VG feedback, and persist their own motion/particle previous state while leaving shared capture/stats/virtual-geometry debug snapshot to the viewport-terminal child. | `rustfmt --edition 2021` 覆盖 touched Rust files 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-prev-state-0620 --message-format short --color never` 通过(既有 warning set);focused `cargo test -p zircon_runtime --lib viewport_record_keeps ...` 首次 exposed/fixed moved-value test issue in `motion_vector_camera.rs`, rerun exceeded tool window and left no runnable lib-test binary,不计为通过;`cargo check -p zircon_runtime --lib --tests ...` 仍被 unrelated `tests/virtual_geometry_debug_snapshot_contract.rs` stale `RenderMeshSnapshot` fixture fields 阻塞。Docs updated: `docs/zircon_runtime/graphics/runtime/render_framework/viewport_record/history.md`, `docs/zircon_runtime/graphics/runtime/render_framework/submit_frame_extract/submit/camera_loop.md`, render plan/index, and session note. | 继续 CO-M2: light/product debug ownership、final custom-target composite/product owner、UI/scene overlay 顺序、统一 sort key 和 RenderDoc/product 对拍。 |
| 2026-06-20 | CO-M2 independent custom texture target product guard | 部分完成: 同一提交中多个独立 sRGB texture Base stacks 的像素守卫源码已落地,并新增 crate-local prepared output-target readback helper;focused product execution 被 unrelated lib-test 构建错误阻塞,尚不能算通过验收 | `SceneRenderer::read_output_target_texture_rgba_for_tests(...)` 直接从 `ResourceStreamer` 中按 `ResourceId` 读取 prepared output-target texture 像素;`WgpuRenderFramework::read_output_target_texture_rgba_for_tests(...)` 只在 `#[cfg(test)]` 暴露 crate 内转发,不扩展公共 `RenderFramework` facade;新增 `graphics_camera_target_texture_base_stacks_write_independent_texture_targets`,构造红/绿两个 sRGB render-target textures,同帧提交两个 Base cameras,分别读取两个 prepared texture targets 并断言首个非 viewport-terminal Base stack 的红色目标不被最后绿色目标覆盖,同时确认 viewport capture 仍归最后 Base stack。 | `rustfmt --edition 2021 --check` 覆盖 `scene_renderer_render_with_pipeline.rs`、`wgpu_render_framework.rs`、`surface_targets.rs` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-prev-state-0620 --message-format short --color never` 通过(既有 warning set);focused `cargo test -p zircon_runtime --lib graphics_camera_target_texture_base_stacks_write_independent_texture_targets --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-prev-state-0620 --message-format short --color never -- --nocapture` 未运行到目标测试,因 unrelated `zircon_runtime/src/scene/dynamic_scene/session/query` private re-export errors 先失败。Docs updated: camera-loop docs, render-product docs, render plan/index, and session note. | 继续 CO-M2: 解阻 unrelated lib-test 后补跑该 focused product test;实现 light/product debug ownership、final custom-target composite/product owner、UI/scene overlay 顺序、统一 sort key 和 RenderDoc/product 对拍。 |
| 2026-06-20 | CO-M2 shared viewport product/debug owner gate | 部分完成: viewport-terminal shared record/present owner 与 shared stats/capture/debug product owner 已显式分名;light/product debug ownership、final custom-target composite、UI/scene overlay 顺序和 RenderDoc/product 验收仍未完成 | `ViewportCameraStackOutputPolicy::owns_shared_viewport_products()` 作为 `owns_viewport_submission()` 的命名同义入口落地,用于表达共享 `RenderStats`、graphics-debugger capture 和 last virtual-geometry debug snapshot 的唯一写入者;generated offscreen、surface-present、direct runtime-frame 三条 selected-camera submit path 改用该 helper gate capture/snapshot/stats,继续用 `owns_viewport_submission()` gate shared viewport record/present path;`camera_loop_marks_stack_and_viewport_output_owners` 与 output-policy helper test 现在同时断言 final-target、viewport-submission 和 shared-product owner 映射。 | `rustfmt --edition 2021 --check` 覆盖 touched submit/output-policy Rust files 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-shared-product-owner-0620 --message-format short --color never` 通过(既有 warning set);focused `cargo test -p zircon_runtime --lib viewport_camera_stack_output_policy --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-shared-product-owner-0620 --message-format short --color never -- --nocapture` 906s timeout,未产出 lib-test binary,无 focused test pass 可声明;进程审计无 cargo/rustc/test 残留。Docs updated: output-policy docs, camera-loop docs, render-product docs, render plan/index, and session note. | 继续 CO-M2: focused closeout 已补齐 output-policy/camera-loop direct tests;继续实现 per-camera light/product debug ownership、final custom-target composite/product owner、UI/scene overlay 顺序、统一 sort key 和 RenderDoc/product 对拍,并按需清理 remaining long-timeout Cargo wrappers。 |
| 2026-06-20 | CO-M2 shared light-grid product report boundary | 部分完成: light-grid `RenderStats` 报告现在走 shared viewport product owner gate;final custom-target composite、UI/scene overlay 顺序和 RenderDoc/product 验收仍未完成 | 新增 `SharedViewportProductReports`,在 generated offscreen、surface-present、direct runtime-frame 三条 selected-camera submit path 的 `owns_shared_viewport_products()` 分支内从 `RenderFrameworkState` snapshot `SceneRenderer::last_light_grid_report()`;`update_stats(...)` 与 `update_base_stats(...)` 改为显式接收该 shared-product report,`base_stats.rs` 不再直接读 renderer 的 light-grid report;`update_light_grid_stats(...)` 和对应 tests 抽到 `update_stats/light_grid_stats.rs`,让 oversized `base_stats.rs` 少一个独立统计责任并把 light-grid stats 归属写清楚。 | `rustfmt --edition 2021 --check` 覆盖 touched submit/update_stats Rust files 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-light-grid-shared-products-0620 --message-format short --color never` 通过(既有 warning set);本切片没有运行 focused lib-test,因为当前 lib-test lane 仍有 unrelated build blockers/长时超时历史。Docs updated: lighting docs, output-policy docs, camera-loop docs, render-product docs, render plan/index, and session note. | 继续 CO-M2: final custom-target composite/product owner、UI/scene overlay 顺序、独立 per-camera light/product-debug 产品规则、统一 sort key、RenderDoc/product 对拍;focused closeout 已补齐 `light_grid_stats` 和 output-policy/camera-loop direct tests,后续只需按需清理 remaining long-timeout Cargo wrappers。 |
| 2026-06-20 | CO-M2 linear texture target final-product guard | 部分完成: 线性 `rgba8unorm` texture target 的 Base+Overlay 终端合成与转换写回产品守卫已通过;更宽 custom-target composite/product、UI/scene overlay 顺序和 RenderDoc/product 验收仍未完成 | `m4_behavior_layers.rs` 新增 `texture_target_overlay_camera_converts_linear_final_product_after_composite`;`RenderFixture` 扩展线性/通用 render-target texture 插入器;测试构造 Base red clear + layer-filtered green Overlay mesh stack,要求 `TextureWritebackConversion`、`RequiresConversionWriteback`、`Converted`、`converted_count=1`、`copied_count=0`,并同时断言 captured frame 与 prepared output target 都保留 red Base composite 且包含 green Overlay draw。为解阻 lib-test 编译,将 dynamic-scene `value/{json,remap}.rs` 两个 helper 可见性提升为 `pub(in crate::scene::dynamic_scene)`,让 `value/mod.rs` 能向 sibling `scene/spawn.rs` 重导出而不扩大到 crate public API。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/tests/m4_behavior_layers.rs` 通过;首次 Cargo wrapper focused test 20min 超时且无 binary,清理残留进程;修复 dynamic-scene helper 可见性后,`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-light-grid-shared-products-0620 --message-format short --color never` 通过(既有 warning set);第二次 Cargo wrapper 产出 `D:\cargo-targets\zircon-runtime-texture-linear-product-0620\debug\deps\zircon_runtime-c339c28ec98a5de7.exe` 后超时,直接执行 exact `graphics::tests::m4_behavior_layers::texture_target_overlay_camera_converts_linear_final_product_after_composite` 通过 1 test;direct exact `scene::tests::dynamic_scene::dynamic_scene_roundtrips_reflected_components_with_entity_remap` 通过 1 test。Docs updated: camera-loop docs, render-product docs, dynamic-scene docs, render plan/index, and session note. | 继续 CO-M2: 继续实现更宽 custom-target composite/product 规则、UI/scene overlay 顺序、独立 per-camera light/product-debug 产品规则、统一 sort key 与 RenderDoc/product 对拍;independent multi-texture focused product guard 已由 focused closeout direct exact run 补齐。 |
| 2026-06-20 | CO-M2 screen-space UI graph-tail ordering | 部分完成: 默认 Forward+/Deferred 3D 管线已让 scene overlay/debug 在 screen-space UI 前执行,`runtime-ui` 成为 graph 末端 pass;UI/scene overlay 像素产品与 RenderDoc 对拍仍未完成 | `default_forward_plus.rs` 与 `default_deferred.rs` 将 `RenderPhase::Ui` / `RenderPassStage::Ui` 移到 `Overlay`、`Debug` 之后,并让默认 feature 列表中 `DebugOverlay` 先于 `Ui`;`pipeline_compile.rs` 的默认与 pluginized Forward+/Deferred pass-order 快照同步为 `overlay-gizmo` → `runtime-ui`;新增 `pipeline_overlay_order.rs` 专门断言 Overlay/Debug stage 在 Ui 前、`overlay-gizmo` 在 `runtime-ui` 前且 `runtime-ui` 是默认 3D graph 末端 pass;同时把测试用 plugin post-process fixture 的 `FINAL_COMPOSITED` 从 external 改为 graph texture,避免内置 FXAA 读取 terminal AA input 时出现资源归属冲突。 | `rustfmt --edition 2021` 覆盖 touched pipeline/test files 通过;`cargo test -p zircon_runtime --lib pipeline_overlay_order --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-texture-linear-product-0620 --message-format short --color never -- --test-threads=1 --nocapture` 通过 2 tests;direct exact `default_forward_plus_pipeline_compiles_expected_stage_order_and_passes` 与 `default_deferred_pipeline_compiles_expected_stage_order_and_passes` 各通过 1 test;修正 plugin fixture 后 `cargo test -p zircon_runtime --lib rendering_plugin_default_features_restore_legacy --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-texture-linear-product-0620 --message-format short --color never -- --test-threads=1 --nocapture` 通过 2 tests;最终 direct filter `graphics::tests::pipeline_overlay_order` 再次通过 2 tests。 | 继续 CO-M2: 更宽 custom-target composite/product、UI/scene overlay pixel/product 对拍、统一 sort key 与 RenderDoc/product 对拍。 |
| 2026-06-20 | CO-M2 UI/scene overlay pixel product and dynamic-resolution terminal target | 部分完成: 默认 3D `overlay-gizmo` → `runtime-ui` 的提交级统计与 WGPU 像素产品守卫已通过,动态分辨率 + terminal AA 的 `final-composited` 目标尺寸已修正;RenderDoc 对拍、更宽 custom-target composite 与统一 sort key 仍未完成 | `render.rs` 不再用固定 `[Ui, Overlay, Debug]` late-stage 顺序,而是按 compiled pipeline stage order 过滤 late graph stages,因此 Core2D 保持 `postprocess-ui-overlay`,默认 Forward+/Deferred 3D 保持 `postprocess-overlay-ui`;`update_stats/ui_stats.rs` 抽出 `runtime_ui_graph_pass_order(...)`,让 `base_stats.rs` 同时识别两种合法顺序;`render_product_ui.rs` 补充 Core2D/3D compile-order、提交统计、动态分辨率 primary surface、动态分辨率 direct texture target 和 dense green overlay + cyan UI 中心像素产品守卫;`resource_descriptors.rs` 让 `PostProcessGraphResourceNames::FINAL_COMPOSITED` 与 `UPSCALED` 一样按 `effective_view_size()` 建 transient texture,避免 terminal AA 在动态分辨率下把 320x240 scissor 写进 160x120 input target。 | `rustfmt --edition 2021` 覆盖 touched Rust files 通过;`cargo test -p zircon_runtime --lib render_product_ui --no-default-features --features core-min --locked --jobs 4 --target-dir target\codex-runtime-plan-status-current-0620 -- --test-threads=1 --nocapture` 通过 5 tests;`cargo test -p zircon_runtime --lib dynamic_resolution_keeps_terminal_anti_alias_input_at_viewport_size --no-default-features --features core-min --locked --jobs 4 --target-dir target\codex-runtime-plan-status-current-0620 -- --nocapture` 通过 1 test;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 4 --target-dir target\codex-runtime-plan-status-current-0620 --message-format short --color never` 通过(既有 warning set)。Docs updated: render-product docs, camera-loop docs, pass/resource descriptor docs, render plan/index, runtime UI docs, and session note. | 继续 CO-M2: 更宽 custom-target composite/product、统一 sort key、UI/scene overlay RenderDoc/宽场景对拍与 remaining focused wrapper 清理。 |
| 2026-06-20 | CO-M2 render-target texture sampled by later PrimarySurface material | 已完成历史产品像素验证: 同帧先写入的 sRGB render-target texture 可被后续 PrimarySurface mesh material 通过标准 `base_color_texture` 采样;2026-06-23 已把当前可运行守卫迁移为规范名 `render_product_dual_camera_rt_then_main`;RenderDoc 对拍仍未完成 | `OutputTargetTextureResource` 现在保留 sampler;`MaterialTextureBinding`/`MaterialTextureSet` 允许材质纹理槽绑定普通 `GpuTextureResource` 或 prepared `OutputTargetTextureResource`;mesh draw batch key 用绑定身份区分普通纹理与输出目标;`ResourceStreamer::ensure_material_texture(...)` 对 `RenderTarget + Sampled` 纹理准备输出目标资源;`extend_pending_draws_for_mesh_instance.rs` 优先把已准备且声明 `Sampled` 的 output target 作为材质纹理绑定。历史守卫 `texture_target_render_order_feeds_later_primary_surface_material_sample` 先用 texture camera red clear 写入 sRGB RT,再让后续 PrimarySurface blue clear + mesh material 采样该 RT,断言 prepared texture 为 red 且 final viewport 中有 red sampled mesh over blue clear;当前源码归属已迁入 `render_product_camera_targets/custom_target/material_sampling.rs::render_product_dual_camera_rt_then_main`。 | Red run: 生产修改前 `cargo test -p zircon_runtime --lib texture_target_render_order_feeds_later_primary_surface_material_sample --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-runtime-camera-products-0620 --message-format short --color never -- --test-threads=1 --nocapture` 失败,`red=0, blue=17436, total=19200`,证明测试命中旧普通纹理绑定路径。Green run: 同一 Cargo wrapper 通过 1 test(既有 warning set);`rustfmt --edition 2021`/`--check` 覆盖 touched Rust files 通过;直接运行更新后的 `target\\codex-runtime-camera-products-0620\\debug\\deps\\zircon_runtime-c339c28ec98a5de7.exe` 复验 `primary_surface_base_camera_render_order_swap_changes_composite`、`texture_target_stack_preserves_composite_when_primary_surface_renders_later`、`texture_target_render_order_feeds_later_primary_surface_material_sample` 均通过 1 test。迁移后的规范名守卫尚未取得新的 WGPU/Cargo pass。 | 继续 CO-M2: UI/scene overlay RenderDoc/宽场景对拍、更宽 custom-target composite/product 规则和 product/RenderDoc 验收;editor authoring 面板仍未完成。 |
| 2026-06-23 | CO-M2 wider multi custom-target stack sampled by later PrimarySurface guard | 部分完成: 更宽 multi custom-target stack → later PrimarySurface material sampling 产品守卫源码已落地;带锁 Cargo 被当前 `Cargo.lock` 漂移阻断,尚不能算 WGPU 产品通过 | 新增 `render_product_camera_targets.rs` 独立测试模块而非继续扩张 `m4_behavior_layers.rs`;`custom_target_stacks_feed_later_primary_surface_materials_independently` 同帧构造两个 sRGB texture target Base+Overlay stack,分别保留 red+green 与 blue+red 合成,随后 PrimarySurface 两个 mesh material 分别采样两个 prepared RT,同时断言两个 prepared targets 不串色且最终主表面左右区域采样各自目标。该切片仅扩展 CO-M2 产品验收面,未改生产渲染路径,未新增 `#[allow(dead_code)]`、String error、FFI 或 builder/API 债。 | `rustfmt --edition 2021 --check zircon_runtime\\src\\graphics\\tests\\mod.rs zircon_runtime\\src\\graphics\\tests\\render_product_camera_targets.rs` 通过;`render_product_camera_targets.rs` 773 行,低于 folder-backed 测试 owner 阈值;focused `cargo test -p zircon_runtime --lib custom_target_stacks_feed_later_primary_surface_materials_independently --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-camera-targets-0623 --message-format short --color never -- --test-threads=1 --nocapture` 被 Cargo 拒绝:`cannot update the lock file ... because --locked was passed`,不计通过。 | 继续 CO-M2: 解阻 `Cargo.lock` / manifest 漂移后补跑该 WGPU 产品守卫;继续 UI/scene overlay RenderDoc/宽场景对拍、更宽 custom-target product/RenderDoc 验收与 editor authoring 面板。 |
| 2026-06-23 | CO-M2 custom-target viewport region sampled by later PrimarySurface guard | 部分完成: 同一 custom target 的 split viewport-region 写入与后续 PrimarySurface 采样产品守卫源码已落地;focused Cargo 仍未返回 WGPU 测试结果 | `render_product_camera_targets/custom_target/viewport.rs::custom_target_viewport_regions_feed_later_primary_surface_sample` 在同一 sRGB texture target 内用两个 Base camera descriptor 分别按 left/right `RenderViewportRect` 写 red/green 区域,随后 PrimarySurface 用 fullscreen sampled material mesh 读取该 prepared RT;断言目标纹理左右中心像素按区域保留,并断言最终主表面左右区域采样到对应颜色。该切片不改生产 submit/streamer/material/scene-renderer 路径;后续结构切片已将 custom-target 产品守卫拆入 folder-backed `custom_target/{material_sampling.rs,viewport.rs,ordering.rs}`。 | `rustfmt --edition 2021 --check zircon_runtime\\src\\graphics\\tests\\render_product_camera_targets.rs zircon_runtime\\src\\graphics\\tests\\mod.rs` 通过;新增测试文件扫描 `#[allow(dead_code)]` / `Result<.*String>` / `extern "C"` / `RuntimePluginDescriptor::new` 无命中;冲突标记扫描通过;`git diff --check` 仅有既有 LF→CRLF 提示。focused `cargo test -p zircon_runtime --lib custom_target_viewport_regions_feed_later_primary_surface_sample --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-plan09-camera-targets-viewport-0623 --message-format short --color never -- --test-threads=1 --nocapture` 两次在共享 lib-test 编译阶段超时(约 120s 与 600s),无测试二进制/无测试结果;已停止本 target-dir 关联 cargo/rustc 进程。 | 继续 CO-M2: 在共享 lib-test/lock 状态稳定后补跑 custom-target WGPU 守卫;后续不要继续扩大 root,新增 custom-target 守卫必须进入对应 `custom_target/*` sub-owner 或继续拆 owner;RenderDoc、UI/scene overlay 宽场景、editor authoring 面板仍未完成。 |
| 2026-06-23 | CO-M2 camera-target product owner folder-backed split | 已完成结构拆分: camera-target 产品守卫由 `render_product_camera_targets.rs` 编排,临时项目/资产写入、camera descriptor、mesh/snapshot 与像素断言下沉到 folder-backed 子模块;未改变生产渲染路径或测试语义 | `render_product_camera_targets.rs` 现在只保留模块声明、产品测试和少量导入;新增 `render_product_camera_targets/{assertions.rs,camera.rs,fixture.rs,mesh.rs}` 分别拥有像素区域断言、Base/Overlay descriptor/profile helpers、临时 Project/Asset/WGPU fixture 与 mesh/snapshot 构造。拆分后避免继续扩大原 923 行测试 owner。 | `rustfmt --edition 2021 --check` 覆盖 root owner 与 4 个子模块通过;新测试树扫描 `#[allow(dead_code)]` / `Result<.*String>` / `extern "C"` / `RuntimePluginDescriptor::new` 无命中;冲突/尾随空白扫描通过;`git diff --check` 仅换行符提示。RenderDoc MCP 无运行实例;本轮未重跑 Cargo,因为同机仍有 unrelated Cargo/rustc 编译进程且前两次 focused Cargo 已在共享 lib-test 编译窗口超时/lock 漂移下无结果。 | 继续 CO-M2: 在 Cargo/RenderDoc 环境稳定后补跑 `render_product_camera_targets` WGPU 守卫;后续新增 camera-target 产品守卫应继续落在该 folder-backed owner 内,不要回退成单文件堆叠。 |
| 2026-06-23 | CO-M2 chained custom-target material sampling guard | 部分完成: 同帧 A texture target -> B texture target -> PrimarySurface 的链式材质采样产品守卫源码已落地;尚未形成 WGPU 产品运行结果 | `custom_target_chain_feeds_later_texture_and_primary_surface_samples` 先用 source texture camera 写红色 sRGB RT,再让 intermediate texture camera 的 fullscreen mesh material 采样 source RT,最后让更晚的 PrimarySurface fullscreen mesh material 采样 intermediate RT;新增 `sampled_fullscreen_mesh_on_layer(...)` 让链路中 B 与主表面 mesh 分别受独立 camera layer 过滤,并用 `legacy_render_layer_mask(...)` 明确 `RenderMeshSnapshot::render_layer_mask` 只能编码 0..31 的 legacy mask。断言 source/intermediate prepared targets 的中心像素为红,并断言最终主表面中心区域从 intermediate RT 继续采样红色而不是保留 intermediate 的蓝色 clear。该切片仅扩展 CO-M2 产品验收面,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `render_product_camera_targets.rs` 与 `assertions.rs`/`camera.rs`/`fixture.rs`/`mesh.rs` 通过;测试树扫描 `#[allow(dead_code)]` / `Result<.*String>` / `extern "C"` / `RuntimePluginDescriptor::new` 无命中;冲突标记与尾随空白扫描通过;当前 root owner 434 行、最大子模块 424 行。RenderDoc MCP 无运行实例;本轮不启动 focused Cargo,因为同机仍有 unrelated Cargo/rustc 编译进程且前两次 focused Cargo 已在共享 lib-test 编译窗口超时/lock 漂移下无结果。 | 继续 CO-M2: 待 Cargo/RenderDoc 环境稳定后补跑已有 `render_product_camera_targets` WGPU 守卫;仍需 RenderDoc、UI/scene overlay 宽场景与 editor authoring 面板。 |
| 2026-06-23 | CO-M2 late custom-target producer previous-frame sampling guard | 部分完成: RT producer render_order 晚于 PrimarySurface consumer 时只能采样上一帧 prepared target 的产品守卫源码已落地;尚未形成 WGPU 产品运行结果 | 新增 `custom_target_late_producer_feeds_previous_frame_not_future_sample`。测试第一帧用 texture camera 把 sRGB render target 写成 green,第二帧让更早的 PrimarySurface fullscreen material 采样该 target,随后更晚的 texture camera 把同一 target 写 red;断言 warmup target 为 green、帧末 target 已变 red,但第二帧 PrimarySurface 中心区域仍为 green 且未读到同帧未来 red producer。该切片覆盖本计划"RT 相机 render_order 晚于消费者时消费者读上一帧内容"语义,只扩展 CO-M2 产品验收面,未改生产渲染路径。 | `rustfmt --edition 2021 --check` 覆盖 `render_product_camera_targets.rs` 与 `assertions.rs`/`camera.rs`/`fixture.rs`/`mesh.rs` 通过;测试树扫描 `#[allow(dead_code)]` / `Result<.*String>` / `extern "C"` / `RuntimePluginDescriptor::new` / `TODO` / `FIXME` 无命中;当前 root owner 547 行、最大子模块 424 行。当前同机仍有 unrelated Cargo/rustc 编译进程,本轮不启动 focused Cargo;RenderDoc MCP 仍无运行实例,不声明 WGPU/RenderDoc 通过。 | 继续 CO-M2: 待 Cargo/RenderDoc 环境稳定后补跑 `render_product_camera_targets` WGPU 守卫组;仍需 RenderDoc、UI/scene overlay 宽场景与 editor authoring 面板。 |
| 2026-06-23 | CO-M2 camera-target configured framework fixture owner | 已完成结构清理: camera-target product tests 的 WGPU framework/viewport/pipeline/profile setup 已归入 fixture owner;不改变产品语义或生产渲染路径 | `RenderFixture::configured_framework(...)` 统一创建 `WgpuRenderFramework`、viewport、pipeline asset 与 quality profile,当时五个 `render_product_camera_targets` 守卫改为只接收 `(framework, viewport)` 并提交场景;root owner 不再导入/重复 `RenderPipelineHandle`、`RenderViewportDescriptor`、`set_pipeline_asset(...)`、`set_quality_profile(...)`,旧 `fixture.server(...)` helper 已移除。后续 `render_product_dual_camera_rt_then_main`、`render_product_camera_render_order_swap_changes_composite` 与 `render_product_overlay_stack_composites_over_base` 迁入后,当前该 owner 覆盖八个 camera-target 守卫。 | `rustfmt --edition 2021 --check` 覆盖 root owner 与 `assertions.rs`/`camera.rs`/`fixture.rs`/`mesh.rs` 通过;测试树扫描 `#[allow(dead_code)]` / `Result<.*String>` / `extern "C"` / `RuntimePluginDescriptor::new` / `TODO` / `FIXME` 无命中;重复初始化扫描确认 `set_pipeline_asset(...)`、`set_quality_profile(...)`、`RenderViewportDescriptor::new`、`RenderPipelineHandle::new` 只剩 `fixture.rs`,且 `fixture.server(` 无命中。当前同机仍有 unrelated Cargo/rustc 编译进程,本轮不启动 focused Cargo;RenderDoc MCP 仍无运行实例,不声明 WGPU/RenderDoc 通过。 | 继续 CO-M2: 待 Cargo/RenderDoc 环境稳定后补跑八个 `render_product_camera_targets` WGPU 守卫;后续 camera-target 产品守卫继续复用 fixture owner,避免回退到重复初始化或单文件堆叠。 |
| 2026-06-23 | CO-M2 custom-target overlay viewport inheritance sampling guard | 部分完成: custom target Base viewport_rect + Overlay 继承 viewport 的产品守卫源码已落地;尚未形成 WGPU 产品运行结果 | 新增 `custom_target_overlay_inherits_base_viewport_region_before_primary_sample`。测试先用同一 sRGB custom target 的右半 Base camera 写蓝色,随后左半 Base camera 写红色并挂接 green Overlay;Overlay descriptor 自身不设置 viewport,必须继承左半 Base viewport。断言 prepared target 左半有 green overlay 与 red base edge、右半保持 blue 且 green 不泄漏;再让后续 PrimarySurface fullscreen material 采样该 prepared target,断言最终帧左右半区同样保留该受限合成。`assertions.rs` 新增 raw RGBA region dominant-pixel helpers,用于 prepared texture 与 `CapturedFrame` 共用相同区域判定。该切片仅扩展 CO-M2 产品验收面,未改生产渲染路径。 | `rustfmt --edition 2021` 已格式化 root owner 与 `assertions.rs`;`rustfmt --edition 2021 --check` 覆盖 root owner 与 `assertions.rs`/`camera.rs`/`fixture.rs`/`mesh.rs` 通过;测试树扫描 `#[allow(dead_code)]` / `Result<.*String>` / `extern "C"` / `RuntimePluginDescriptor::new` / `TODO` / `FIXME` 无命中;冲突/尾随空白扫描通过;当时 root owner 652 行、最大子模块 `fixture.rs` 437 行。当前同机仍有 unrelated Cargo/rustc 编译进程,本轮不启动 focused Cargo;RenderDoc MCP 仍无运行实例,不声明 WGPU/RenderDoc 通过。 | 继续 CO-M2: 待 Cargo/RenderDoc 环境稳定后补跑八个 `render_product_camera_targets` WGPU 守卫;仍需 RenderDoc、UI/scene overlay 宽场景与 editor authoring 面板。 |
| 2026-06-23 | CO-M1/M2 texture-target compile-key format class | 部分完成: production compile key 已携带 texture target 的 `ResourceId`、解析尺寸与 `rgba8unorm_srgb`/`rgba8unorm` 格式类;locked Cargo 仍被当前 `Cargo.lock` 漂移阻断 | `target_resolution.rs` 的 `resolve_camera_target_descriptor(...)` 取代只返回尺寸的旧入口,在读取并验证 texture target `RenderImageDescriptor` 后返回 `ResolvedCameraTargetDescriptor`。`CompiledGraphCacheKey::from_inputs(...)` 改为显式接收 `RenderGraphCompileCameraTargetFingerprint`,Texture 变体包含 id、width、height、`RenderGraphCompileTextureTargetFormat`;`compile_submission_pipeline(...)` 与 `compile_submission_pipeline_with_options(...)` 在早期 feature-discovery compile 和最终 effective-options compile 中复用同一已验证 target fingerprint。新增 `compiled_render_pipeline_cache_key_tracks_texture_target_format_class`,锁定同一 texture id/尺寸但 sRGB 与 linear 目标格式必须产生不同 key。 | `rustfmt --edition 2021 --check` 覆盖 `compiled_graph_cache.rs`、`pipeline/mod.rs`、`build.rs`、`compile_pipeline.rs`、`target_resolution.rs` 通过;静态扫描 touched pipeline/build-context 源码确认无新增 `#[allow(dead_code)]`、FFI、plugin builder 或旧 `resolve_camera_target_size`,冲突标记扫描通过。focused `cargo test -p zircon_runtime --lib compiled_graph_cache --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-compiled-graph-format-0623 --message-format short --color never -- --test-threads=1 --nocapture` 在编译前被 Cargo 拒绝:`cannot update the lock file ... because --locked was passed`,不计 Cargo 通过。 | 解阻 `Cargo.lock`/manifest 漂移后补跑 `compiled_graph_cache` focused tests 与 CO-M2 `render_product_camera_targets` WGPU 守卫;继续 final custom-target product/RenderDoc、UI/scene overlay 宽场景与 editor authoring 面板。 |
| 2026-06-20 | CO-M2 PrimarySurface Overlay clear-depth product guard | 已完成历史产品像素验证: PrimarySurface Base+Overlay stack 已覆盖 Overlay `clear_depth=false` 复用 Base depth 与 `clear_depth=true` 清空 depth 的两种 WGPU 像素结果;2026-06-23 已把当前可运行守卫迁移为规范名 `render_product_overlay_stack_composites_over_base`;RenderDoc/宽场景对拍仍未完成 | 历史守卫 `primary_surface_overlay_clear_depth_controls_depth_reuse` 构造 PrimarySurface Base red near mesh + Overlay green farther mesh。Base camera 写 red color/depth,Overlay camera 加入 Base stack 并分别以 `clear_depth=false`/`true` 执行;测试中心像素断言 false 时 red 保留且 green 被 Base depth 挡住,true 时 green 替换中心像素。该守卫补齐计划原始 `render_product_overlay_stack_composites_over_base` 中 clearDepth 双策略的 focused 产品证据;当前源码归属已迁入 `render_product_camera_targets.rs::render_product_overlay_stack_composites_over_base`。 | `rustfmt --edition 2021` 格式化 `zircon_runtime\src\graphics\tests\m4_behavior_layers.rs`;`cargo test -p zircon_runtime --lib primary_surface_overlay_clear_depth_controls_depth_reuse --no-default-features --features core-min --locked --jobs 1 --target-dir target\\codex-runtime-camera-products-0620 --message-format short --color never -- --test-threads=1 --nocapture` 通过 1 test(既有 54-warning set),完成 4650 filtered 的 focused WGPU 产品验证。迁移后的规范名守卫尚未取得新的 WGPU/Cargo pass。 | 继续 CO-M2: UI/scene overlay RenderDoc/宽场景对拍、更宽 custom-target composite/product 规则和 product/RenderDoc 验收;editor authoring 面板仍未完成。 |
| 2026-06-15 | CO-M2 camera stack and overlay composition | 部分完成: neutral Base/Overlay sequence contract、scene color/depth 首写 load + region-scoped clear draw 策略、fixed scene target physical reuse 契约、texture-output stack-terminal gate 与 suppressed diagnostics、generated offscreen/surface-present/direct runtime-frame viewport-terminal shared record/present owner gate、explicit shared viewport product/debug owner gate、shared light-grid product report boundary、selected-camera light-grid report/VG debug snapshot product-report slots、selected-camera temporal history/static-index/motion-vector/particle previous-state/HGI runtime/VG runtime slots、selected-camera viewport/scissor graph-raster region、fullscreen post-process viewport-origin/terminal writeback policy、terminal local/physical coordinate split、split-screen product guard、PrimarySurface 多 Base render_order composite guard、texture-target overlay product guards、linear texture conversion final-product guard、screen-space UI graph-tail ordering、UI/scene overlay focused pixel product guard、dynamic-resolution terminal AA target sizing、Bloom-before-Uber post-process authoring rule 与 FXAA produced-output access split 已存在,更宽 custom-target composite/product 与 UI/scene overlay RenderDoc/宽场景验收未完成 | `resolve_camera_sequence(...)` 已表达 Overlay 不独立成序、Base stack 顺序、target/viewport 继承和违规 report;`ViewportCameraStackAttachmentPolicy` 已保存 Base clear/Overlay `clear_depth` 的 scene clear plan并把 graph-declared first scene clear 写入改为 load;`SceneRegionClearResources` 已在 graph stage 前通过 selected `ViewportRenderRegion` 对 fixed scene color/depth 执行 region-scoped draw clear;`bind_frame_graph_resources(...)` 已把 live `SCENE_COLOR`/`SCENE_DEPTH` 导向 fixed `OffscreenTarget` views;`ViewportCameraStackOutputPolicy` 已阻止非 stack-terminal child 直接 import/writeback/capture texture output target,并用 graph-import/writeback suppressed rows 报告 texture non-owner;surface-present 与 direct runtime-frame submit 已复用 camera loop,所有 child 解析并记录 selected-camera history/previous/runtime slots,只有 viewport-terminal child 记录 shared viewport record/present path,`owns_shared_viewport_products()` 明确 gate `RenderStats`、graphics-debugger capture 和 last VG debug snapshot;`SharedViewportProductReports` 在同一 shared-product gate 内 snapshot light-grid execution report before `update_stats(...)`;`ViewportRecord` now stores `ViewportFrameHistory` per `ViewportCameraHistoryKey`,and history includes per-camera `VisibilityStaticIndex`;`ViewportRenderRegion` 已把 selected `viewport_rect` 应用为 graph-raster 与 terminal post-process WGPU viewport/scissor;post-process graph-owned intermediates localize selected-camera execution,固定 frame/history inputs 通过 origin 采样;terminal output-transfer writes graph-owned `FINAL_COMPOSITED` in local child-target coordinates while FXAA/SMAA final resolves write physical `FINAL_COLOR` regions;texture-target Base+Overlay product guards 现已验证 Base color 在 Overlay clear-none child 后保留,layer-filtered green Overlay mesh 会叠到同一 texture target 的 red Base clear 上,并验证 linear `rgba8unorm` texture target 通过 terminal conversion writeback 把 red Base composite 与 green Overlay draw 写入 captured frame 和 prepared output target;split-screen PrimarySurface guard validates independent left/right Base viewport clears;PrimarySurface full-viewport Base render_order swap guard validates later Base camera owns the final composite;默认 3D late graph execution 现在按 compiled stage order 执行 Overlay/Debug/Ui,并由 UI/scene overlay focused product guard 证明 dense scene overlay 下 cyan screen-space UI 位于最终像素顶层。当前仍没有更宽自定义 target composite 规则或 UI/scene overlay RenderDoc/宽场景验收。 | 2026-06-18 descriptor/sequence focused tests 覆盖排序、继承与违规剔除;camera attachment/output policy `core-min` check 通过;output policy focused tests 覆盖 stack/viewport terminal flags 和 frame policy carry;fixed scene target binding focused test 通过;2026-06-19 viewport region tests、particles transparent plugin tests、post-process origin/history focused tests、scene-clear policy tests 与 offscreen WGPU scene-clear submit test 通过;final-target owner diagnostics direct binary focused tests 通过;surface-present camera-loop slice `core-min` check 通过,但 focused `camera_loop` test 超时且 `--tests` 被 unrelated VG snapshot fixture 阻塞;direct runtime-frame camera-loop slice `core-min` check 通过,但 focused lib-test `--no-run` 未产出可用测试结果;texture-target overlay clear-preservation guard、layered mesh guard、linear conversion final-product guard、Forward+/Deferred default pass order、FXAA terminal routing focused tests、split-screen PrimarySurface Base viewport product guard、PrimarySurface Base render_order swap product guard 通过;dynamic-scene value helper visibility blocker 已修复且 core-min check/dynamic-scene remap focused test 通过;per-camera history owner `core-min` check 通过,direct binary `camera_history` filter 4 tests 和 exact record map test 1 test 通过;shared viewport product/debug owner slice 与 shared light-grid product-report boundary `core-min` checks 通过;per-camera product-report focused test/check 通过,focused lib-test wrapper 超时无 binary;默认 Forward+/Deferred screen-space UI graph-tail ordering focused tests 通过;2026-06-20 `render_product_ui` 5 tests、dynamic-resolution terminal AA descriptor test 1 test 与 `zircon_runtime --features core-min` check 通过;`primary_surface_base_camera_render_order_swap_changes_composite` direct test binary 通过 1 test,Cargo wrapper 在 test binary 生成后因 Windows target fingerprint 写入路径缺失失败,不计为 Cargo wrapper 通过。 | 继续实现更宽 pixel/product custom-target composite、UI/scene overlay RenderDoc/宽场景对拍和 product/RenderDoc 验收。 |
| 2026-06-15 | CO-M3 RenderQueue and unified sort key | 部分完成: phase sort 已有,统一 queue override 未完成 | `phase_queue.rs`/`phase_sort.rs` 有 Opaque/AlphaMask/Transparent/Sprite2d 等 phase 排序;计划 03/04/05 已依赖 source entity、view visibility 和 shadow sorting,但材质 queue 数值覆写、sprite/UI/3D 统一 sort key 尚未落地。 | 本文件 `现状与差距` 记录 phase 硬编码;计划 05/06 状态表记录 shadow/TAA pass order 依赖现有 phase。 | 实施统一 sort key 位段、材质 queue override 和 world-space UI/sprite 混排规则。 |
| 2026-06-20 | CO-M3 M3-S1 RenderQueueValue phase authority and material queue snapshot | 部分完成: M3-S1 queue 数值权威、mesh/sprite phase 映射、材质快照 typed projection 与导入期 queue×alpha 矛盾校验已完成;最终 u64 sort key 仍未完成 | 新增 `core_pipeline/render_queue.rs`,定义 Unity queue 常量、alpha 默认队列、authored Unity-range queue 覆写、legacy 小 offset clamp 和段到 `RenderPhase` 的唯一映射;`phase_queue.rs` 先解析 `RenderQueueValue` 再分配 phase,并把 resolved queue 写入现有 sort component;`RenderPhase::mesh_phase(pipeline, alpha_mask, transparent)` 旧布尔签名已删除, sprite fallback 改读 `RenderQueueValue`。`StandardMaterialDescriptor.render_queue_value` 与 `resolved_render_queue_value()` 保存/回填 typed queue snapshot;`MaterialAsset::render_queue_value()` 只为有效显式 `render_queue` 生成 `RenderQueueValue`;`validate_render_queue_alpha_mode(...)` 对 blend+opaque/alpha-test queue 段报告 `RenderQueueAlphaModeConflict`,同时允许 opaque queue=2900 进入后段;`MaterialRuntime.render_queue_value` 把 snapshot 送到 runtime streamer 边界。为解除当前 Cargo 验证阻塞,同步修正 `component_storage` sibling re-export 的内部可见性。 | `rustfmt --edition 2021` 覆盖 touched Rust files 通过;`rustfmt --edition 2021 --check` 覆盖本切片 touched Rust files 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 4 --target-dir target\\codex-runtime-camera-products-0620 --message-format short --color never` 通过(既有 warning set)。Cargo wrapper `render_queue` 两次因 Windows lib-test 链接超时,随后 direct binary `target\\codex-runtime-camera-products-0620\\debug\\deps\\zircon_runtime-c339c28ec98a5de7.exe render_queue --test-threads=1 --nocapture` 通过 5 tests;direct binary `mesh_renderer_sort_fields_feed_geometry_phase_queue geometry_phase_inputs_feed_unified_sort_components_into_queue --test-threads=1 --nocapture` 通过 2 tests。材质快照 slice 的 Cargo test wrapper 同样在 Windows lib-test 链接阶段超时,但同一 direct binary 通过 `material_owned_render_queue` 2 tests、`material_owned_sort_fields` 2 tests、`render_product_pbr_streamer_projects_standard_material_into_runtime_key` 1 test 和 `render_product_pbr_streamer_projects_material_sort_offsets_without_pipeline_variant` 1 test。 | 继续 M3-S2: 执行最终 `u64` sort key 位段硬切换,随后推进 sprite/world-space UI 混排和 product/RenderDoc 对拍。 |
| 2026-06-20 | CO-M3 M3-S2 final u64 sort key hard cutover | 已完成: Plan 09 `sort_key: u64` 位段权威落地,core_pipeline 下旧 `i128` 打包路径/旧 helper 已删除;breakdown/decision 改为 camera_order、queue、domain、tie-breaker 诊断 | 新增 `core_pipeline/packed_sort_key.rs` 作为唯一 `packed_sort_key_u64(...)` 入口,按 `[camera_order:8][queue:13][domain:33][tie:10]` 打包;`RenderPhaseSortKey` 改为 `u64` newtype,`RenderPhaseSortComponents` 合并为 typed `RenderQueueValue` 并新增 camera/sorting-layer/y-sort 输入;`phase_sort_key_breakdown.rs`、`phase_sort_decision*.rs`、`phase_queue_ordering_key.rs` 全部重写到新位段;`phase_queue.rs` 的 `MeshPhaseInput`/`SpritePhaseInput` 不再接收 raw `render_queue`/`material_queue`,source-level raw authoring fields 只在 `frame_extract.rs` 和 mesh draw construction 边界折叠为 `RenderQueueValue`;mesh draw command sort input 与 material-adjusted phase queue 同步消费 typed queue。 | `rustfmt --edition 2021` 和 `rustfmt --edition 2021 --check` 覆盖 touched Rust files 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 4 --target-dir target\\codex-runtime-camera-products-0620 --message-format short --color never` 通过(既有 warning set)。`cargo test -p zircon_runtime --lib render_sort_key --no-default-features --features core-min --locked --jobs 4 --target-dir target\\codex-runtime-camera-products-0620 -- --test-threads=1 --nocapture` 通过 7 tests;direct binary `render_phase_sort_key` 3 tests、`geometry_phase_inputs_feed_unified_sort_components_into_queue` 1 test、`render_product_sprite_phase_queue` 2 tests、`phase_ordered_meshes_apply_material_sort_offsets_to_extract_phase_queue` 1 test 均通过。core_pipeline 残留扫描确认 `i128|signed_15|signed_23|signed_35|render_queue_sort_key|material_queue_sort_key|ordered_depth_sort_key` 无命中。 | 继续 CO-M3: sprite/world-space UI 与透明 mesh 混排产品规则和 RenderDoc/product 对拍;CO-M2 仍剩更宽 custom-target composite/product 边界。 |
| 2026-06-20 | CO-M3 M3-S3 transparent Sprite/Mesh mixed submission path | 已完成产品像素验证: 透明 3D stage 已能在同一 WGPU render pass 内按统一 `u64` sort key 混排 Sprite、world-space UI-like Sprite 与 Mesh;RenderDoc 对拍仍未完成 | 新增 `scene_renderer/transparent/mixed_submission.rs`,从 `MeshDrawCommand` 与 sprite `RenderPhaseQueue` 构建透明提交序列,只消费既有 `sort_key`/entity/source,不重新定义排序规则;`SpriteRenderer` 新增当前 render pass 内绘制顶点的内部入口,`build_sprite_vertices(...)` 支持 `RenderPassStage::Transparent3d`;`BaseScenePass.record_transparent_mixed_with_attachment_ops(...)` 预构建透明 Sprite vertex buffer/texture,在 `TransparentMixedScenePass` 中按提交序列交替 replay Mesh 与 Sprite;`MeshDrawCommandReplayer.invalidate_state_after_external_pipeline()` 防止 Sprite pipeline 覆盖后 Mesh 复用陈旧 pipeline/bind-group/geometry 状态;`RenderPassGpuExecutionContext.record_mesh_stage_to_resources(...)` 仅在 `Transparent3d` 且存在 3D Sprite phase item 时切换到混排路径;Forward+ 与 Deferred 的透明 mesh graph stage 现在把 `SpriteRenderer` 传入执行上下文,且该 3D 混排不再依赖 2D Sprite graph feature 是否启用。world-space UI 当前以透明 3D Sprite 数据表达:即使 phase input 携带高 `ui_z_index`,queue 仍落在 `Transparent3d`,并按 3D 透明深度和同一 sort key 混排,screen-space UI 继续留在 graph 末端闭环。 | `rustfmt --edition 2021` 覆盖 touched Rust files 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 2 --target-dir target\\codex-runtime-camera-products-0620 --message-format short --color never` 通过(既有 warning set)。Cargo wrapper 首次 `transparent_submission` 过滤在 Windows lib-test 链接阶段超时,随后 direct binary `target\\codex-runtime-camera-products-0620\\debug\\deps\\zircon_runtime-c339c28ec98a5de7.exe` 通过 `transparent_submission` 2 tests、`transparent_sprite_submission` 1 test、`mesh_draw_command_replayer_rebinds_after_external_pipeline` 1 test、`render_sort_key` 7 tests;Cargo wrapper `build_sprite_vertices_routes_transparent3d_to_transparent3d_phase` 通过 1 test。产品像素补测 `transparent3d_product_interleaves_mesh_and_sprite_pixels_by_phase_sort_key` 通过 1 test,证明默认 Forward+ 产品输出中绿色透明 Sprite 与红色透明 Mesh 按同一 `mesh.transparent` pass 混合;新增产品补测 `transparent3d_product_treats_world_space_ui_sprite_as_transparent_member` 通过 1 test,证明高 `ui_z_index` 的 world-space UI-like 透明面板仍作为透明 3D 成员按深度排在近处透明 Mesh 之前并在最终 WGPU 输出中混合。当前环境没有运行中的 RenderDoc MCP Bridge 实例,`start_graphics_debugger_capture()` 在无外部调试器附着时仅转发/no-op,真实 RenderDoc 捕获未完成。 | 继续 CO-M3: 透明 Sprite/Mesh/world-space UI RenderDoc 捕获;CO-M2 仍剩更宽 custom-target composite/product 与 editor authoring 边界。 |
| 2026-06-15 | CO-M4 layer filtering across the stack | 部分完成: visibility/shadow layer 使用已接入,custom-target visibility payload 已接入,多相机全线贯通未完成 | 计划 04 的 FrameVisibility 与计划 05 shadow view 已按 view key/layer mask 消费可见集;2026-06-18 起 scene extract 会为 Texture/Headless scene cameras 合并 mesh/sprite 候选层,custom target visibility 使用各自 camera layer mask;但 UI/2D/overlay layer、post/history/light ownership 和 WGPU target loop 仍未统一。 | 计划 04 VC-M1 状态表记录 directional cascade/point face/spot shadow view 与 custom-target visibility payload bridge;计划 05 LS-M3 状态表记录 shadow atlas view key 消费。 | 等 CO-M1 多相机 loop/descriptor 完成后贯通 render queue、UI、2D、shadow、post history 和 target 输出。 |

### 2026-06-20 CO-M2 补充验证记录

- `D:\cargo-targets\zircon-runtime-texture-linear-product-0620\debug\deps\zircon_runtime-c339c28ec98a5de7.exe graphics::tests::surface_targets::texture_target::graphics_camera_target_texture_base_stacks_write_independent_texture_targets --exact --test-threads=1 --nocapture` 通过 1 test。此前 independent multi-texture target guard 的 Cargo wrapper 被 unrelated dynamic-scene private re-export errors 阻塞;该阻塞已清理,本次 direct exact run 证明两个同帧 sRGB texture Base stacks 的 prepared output targets 独立且 viewport capture 仍归最后 Base stack。
- 同一 lib-test binary direct exact `graphics::types::viewport_camera_stack_output_policy::tests::final_target_output_owner_is_stack_terminal_not_viewport_terminal` 通过 1 test,并且 direct exact `graphics::runtime::render_framework::submit_frame_extract::submit::camera_loop::tests::camera_loop_marks_stack_and_viewport_output_owners` 通过 1 test。此前 shared-product owner slice 的 Cargo wrapper 超时未产出 binary;本次补齐 stack-terminal、viewport-terminal、final-target、viewport-submission 和 shared-product owner 映射证据。
- 同一 lib-test binary direct filter `light_grid_stats --test-threads=1 --nocapture` 通过 3 tests:`render_product_diagnostics_record_light_grid_stats`、`update_light_grid_stats_records_latest_grid_report`、`update_light_grid_stats_resets_when_no_report`。此前 shared light-grid product-report boundary 只有 core-min check;本次补齐 stats updater 与 product diagnostic focused evidence。
- 默认 Forward+/Deferred screen-space UI graph-tail ordering 已补齐:focused `pipeline_overlay_order` cargo run 通过 2 tests;direct exact 默认 Forward+/Deferred pass-order tests 各通过 1 test;pluginized legacy Forward+/Deferred pass-order focused cargo run 通过 2 tests。该切片将 `overlay-gizmo` 放到 `runtime-ui` 前,并断言 `runtime-ui` 是默认 3D graph 末端 pass。
- UI/scene overlay focused WGPU product 与动态分辨率 terminal AA 目标尺寸已补齐:`render_product_ui` focused cargo run 通过 5 tests,覆盖 Core2D `postprocess-ui-overlay`、默认 3D `postprocess-overlay-ui`、dynamic-resolution primary surface、dynamic-resolution direct texture target 和 dense green overlay + cyan UI 像素顶层;`dynamic_resolution_keeps_terminal_anti_alias_input_at_viewport_size` 通过 1 test,证明 `final-composited` terminal AA input 使用 viewport/presentation 尺寸而不是缩放后的 render size;同一 target dir 的 `zircon_runtime --features core-min` check 通过。
- per-camera product-report ownership 已补齐:`ViewportRecord` 现在按 `ViewportCameraHistoryKey` 保存 light-grid report 与 virtual-geometry debug snapshot,generated offscreen、surface-present 和 direct runtime-frame submit 都在成功 child feedback 后写入该相机槽位;focused `viewport_record_keeps_product_reports_per_camera_key` cargo run 通过 1 test,同一 target dir 的 `zircon_runtime --features core-min` check 通过。
- PrimarySurface 多 Base render_order 合成产品证据已补齐:历史守卫 `primary_surface_base_camera_render_order_swap_changes_composite` 在同一主表面上提交两个全屏 Base clear 相机,交换 `render_order` 后最终捕获颜色随最后执行的 Base 相机在红/绿之间反转。Cargo wrapper 在生成 lib-test binary 后因 `target\\codex-runtime-camera-products-0620\\debug\\.fingerprint` dep-info 写入路径缺失失败,不计为 wrapper 通过;直接运行生成的 `target\\codex-runtime-camera-products-0620\\debug\\deps\\zircon_runtime-c339c28ec98a5de7.exe primary_surface_base_camera_render_order_swap_changes_composite --test-threads=1 --nocapture` 通过 1 test。2026-06-23 当前源码守卫已迁移并改名为 `render_product_camera_targets.rs::render_product_camera_render_order_swap_changes_composite`,旧 `m4_behavior_layers.rs` 测试与 `render_primary_base_order_scene(...)` 删除;迁移后尚未形成新的 WGPU/Cargo pass。
- PrimarySurface Overlay clear-depth 产品证据已补齐:历史守卫 `primary_surface_overlay_clear_depth_controls_depth_reuse` 在同一 PrimarySurface Base stack 中先写 red near mesh depth,再用 Overlay green farther mesh 对比 `clear_depth=false` 与 `clear_depth=true`;focused Cargo wrapper 通过 1 test,证明 false 时保留 Base depth 挡住 Overlay,true 时清空 depth 让 Overlay 替换中心像素。2026-06-23 当前源码守卫已迁移并改名为 `render_product_camera_targets.rs::render_product_overlay_stack_composites_over_base`,旧 `m4_behavior_layers.rs` 测试、`render_primary_overlay_depth_scene(...)` 与 `primary_surface_camera_stack_descriptor(...)` 删除;迁移后尚未形成新的 WGPU/Cargo pass。
- custom-target 与 PrimarySurface 同帧共存产品证据已补齐:`texture_target_stack_preserves_composite_when_primary_surface_renders_later` 在同一帧先提交 sRGB texture target 的 Base red clear + Overlay green mesh stack,再提交 render_order 更晚的 PrimarySurface Base blue clear;直接运行更新后的 lib-test binary 通过 1 test,证明 prepared texture target 保留 red+green 合成且 viewport capture 归后续 PrimarySurface blue clear。Cargo wrapper 首次精确运行在 Windows lib-test compile/link 阶段超时,但后台编译完成并产出更新后的 test binary。
- RT texture 被后续主相机材质采样的产品证据已补齐:历史守卫 `texture_target_render_order_feeds_later_primary_surface_material_sample` 在同一帧先用 texture camera 把 sRGB render-target texture clear 为 red,再用后续 PrimarySurface blue clear + mesh material 通过标准 `base_color_texture` 采样该 RT;生产修改前 red run 失败为 `red=0`,修改后 Cargo wrapper 通过 1 test,并且直接二进制复验 `primary_surface_base_camera_render_order_swap_changes_composite`、`texture_target_stack_preserves_composite_when_primary_surface_renders_later`、`texture_target_render_order_feeds_later_primary_surface_material_sample` 均通过。2026-06-23 当前源码守卫已迁移并改名为 `render_product_camera_targets/custom_target/material_sampling.rs::render_product_dual_camera_rt_then_main`,旧 `m4_behavior_layers.rs` 重复守卫删除;迁移后尚未形成新的 WGPU/Cargo pass。
- 2026-06-23 更宽 multi custom-target stack sampling 产品守卫源码已补:`custom_target_stacks_feed_later_primary_surface_materials_independently` 独立在 `render_product_camera_targets.rs`,同帧写两个 sRGB Base+Overlay texture target 并让后续 PrimarySurface 左右两个 mesh material 分别采样各自 prepared RT。scoped rustfmt/check 已通过,但 focused Cargo `--locked` 被当前 `Cargo.lock` 漂移阻断,尚不声明 WGPU 产品通过。
- 2026-06-23 custom-target viewport-region sampling 产品守卫源码已补:`custom_target_viewport_regions_feed_later_primary_surface_sample` 使用同一个 sRGB texture target,两个 Base camera descriptor 分别通过 `RenderViewportRect` 写 left/right red/green 区域,再让后续 PrimarySurface fullscreen material 采样该 prepared RT。scoped rustfmt/check、静态债务扫描和冲突标记扫描通过;focused Cargo `--locked` 两次停在共享 lib-test 编译阶段并超时,无测试结果。随后已把原 923 行 `render_product_camera_targets.rs` 拆为 root owner + `assertions.rs`/`camera.rs`/`fixture.rs`/`mesh.rs`,root 约 301 行、最大子模块约 387 行,后续 camera-target 产品守卫应继续沿该 folder-backed owner 扩展。
- 2026-06-23 chained custom-target material sampling 产品守卫源码已补:`custom_target_chain_feeds_later_texture_and_primary_surface_samples` 在同一帧先写 source sRGB RT,再让 intermediate texture target 采样 source,最后让 PrimarySurface 采样 intermediate;root owner 现为 434 行、最大子模块 424 行。scoped rustfmt/check、静态债务扫描、冲突标记与尾随空白扫描通过;因 unrelated Cargo/rustc 仍在运行且 RenderDoc MCP 无实例,本轮不声明 WGPU/RenderDoc 通过。
- 2026-06-23 late custom-target producer previous-frame sampling 产品守卫源码已补:`custom_target_late_producer_feeds_previous_frame_not_future_sample` 第一帧把 sRGB RT 写成 green,第二帧让更早 PrimarySurface material 采样该 RT,再让更晚 texture camera 把同一 RT 写 red,用于锁定"producer render_order 晚于 consumer 时读上一帧而非同帧未来输出"语义;root owner 现为 547 行、最大子模块 424 行。scoped rustfmt/check 与静态债务扫描通过;因 unrelated Cargo/rustc 仍在运行且 RenderDoc MCP 无实例,本轮不声明 WGPU/RenderDoc 通过。
- 2026-06-23 camera-target configured framework fixture owner 结构清理已补:`RenderFixture::configured_framework(...)` 统一持有 WGPU framework、viewport、pipeline asset 与 quality profile 初始化,五个 camera-target 产品守卫不再各自重复 `RenderPipelineHandle` / `RenderViewportDescriptor` / `set_pipeline_asset(...)` / `set_quality_profile(...)` 设置;root owner 现为 652 行、最大子模块 `fixture.rs` 437 行。scoped rustfmt/check、静态债务扫描和重复初始化扫描通过;因 unrelated Cargo/rustc 仍在运行且 RenderDoc MCP 无实例,本轮不声明 WGPU/RenderDoc 通过。
- 2026-06-23 custom-target Overlay viewport 继承产品守卫源码已补:`custom_target_overlay_inherits_base_viewport_region_before_primary_sample` 先让同一 sRGB custom target 右半 Base 写 blue,再让左半 Base 写 red 并挂 green Overlay,Overlay 自身不设置 viewport 而必须继承左半 Base viewport;prepared target 与后续 PrimarySurface sample 都断言左半出现 overlay 合成、右半保持 blue 且 green 不泄漏。scoped rustfmt/check、静态债务扫描、冲突标记与尾随空白扫描通过;因 unrelated Cargo/rustc 仍在运行且 RenderDoc MCP 无实例,本轮不声明 WGPU/RenderDoc 通过。
- 2026-06-23 custom-target 双 viewport stack 合成源码守卫已补:`custom_target_two_viewport_stacks_preserve_independent_composites_before_primary_sample` 在同一个 sRGB custom target 上让左半 Base red + Overlay green 与右半 Base blue + Overlay red 分别合成,再由后续 PrimarySurface fullscreen material 采样该 prepared target;prepared target 与 PrimarySurface sample 均断言左右 base/overlay 颜色存在且互不泄漏。scoped rustfmt 已通过,结构/文档/债务扫描见本轮状态行;因 unrelated Cargo/rustc 仍在运行且 RenderDoc MCP 无实例,本轮不声明 WGPU/RenderDoc 通过。
- 这些补充验证进一步收窄但不完全关闭 CO-M2 剩余边界:UI/scene overlay RenderDoc/宽场景对拍、custom-target product/RenderDoc 对拍与 editor authoring 边界仍未完成。
- CO-M3 M3-S1 已新增 `RenderQueueValue` queue 数值权威并接入 mesh/sprite phase queue,同时完成材质快照 typed projection、runtime streamer snapshot 传递和导入期 queue×alpha 矛盾校验。当前证据为 scoped rustfmt、`zircon_runtime --features core-min` check、direct binary `render_queue` 5 tests、旧排序回归 2 tests、`material_owned_render_queue` 2 tests、`material_owned_sort_fields` 2 tests 和 material runtime streamer focused tests 2 tests 通过。该 checkpoint 的剩余 M3-S2 final `u64` sort key 已由下一条记录关闭。
- CO-M3 M3-S2 已完成 final `u64` sort key hard cutover:`packed_sort_key_u64(...)` 现在按本计划 `[camera_order:8][queue:13][domain:33][tie:10]` 位段打包,`RenderPhaseSortKey` raw 类型为 `u64`,breakdown/decision 改为 camera_order/queue/domain/tie 诊断,core_pipeline 旧 `i128` helper 残留扫描为空。当前证据为 scoped rustfmt/check、`zircon_runtime --features core-min` check、`render_sort_key` 7 tests、direct binary `render_phase_sort_key` 3 tests、geometry phase queue 1 test、sprite phase queue 2 tests 与 material-adjusted mesh phase queue 1 test 通过。剩余 CO-M3 工作转入 sprite/world-space UI 混排和产品/RenderDoc 对拍。
- CO-M3 M3-S3 已完成透明 3D Sprite/Mesh 混排提交路径、world-space UI-like 透明成员产品场景和产品像素补测:透明 Sprite phase item 与透明 Mesh command 现在在 `mesh.transparent` graph stage 内通过同一 `u64` sort key 合并,并在 `TransparentMixedScenePass` 同一 WGPU render pass 中交替绘制;默认 Forward+/Deferred 透明 mesh stage 会提供 `SpriteRenderer`,混排仍由透明 3D Sprite phase item 触发;带高 `ui_z_index` 的 world-space UI-like 透明 Sprite 仍作为 transparent queue 普通成员按 3D 透明深度排序。当前证据为 scoped rustfmt、`zircon_runtime --features core-min` check、direct binary `transparent_submission` 2 tests、`transparent_sprite_submission` 1 test、`mesh_draw_command_replayer_rebinds_after_external_pipeline` 1 test、`render_sort_key` 7 tests,Cargo wrapper `build_sprite_vertices_routes_transparent3d_to_transparent3d_phase` 1 test,以及 product tests `transparent3d_product_interleaves_mesh_and_sprite_pixels_by_phase_sort_key` 1 test、`transparent3d_product_treats_world_space_ui_sprite_as_transparent_member` 1 test 通过。剩余 CO-M3 工作为透明混排 RenderDoc 捕获;当前环境没有运行中的 RenderDoc MCP Bridge 实例,因此未形成真实 `.rdc` 对拍。
- 2026-06-23 CO-M4 sprite selected-camera layer filter 初始把 `zircon_runtime/src/graphics/scene/scene_renderer/sprite/build_sprite_vertices.rs` 的生产顶点扩展接到 selected camera layer mask:每个 sprite 在生成 WGPU 顶点前先按当前相机剔除。新增 `build_sprite_vertices_filters_sprites_by_selected_camera_layers` 静态/单元守卫;状态锚为 `render_plan09_sprite_selected_camera_layer_filter_static_passed_cargo_lock_blocked`,当前 focused Cargo 先被 `Cargo.lock` 漂移和 `--locked` 阻断,最终复跑在 124s 工具窗口超时且无 test binary,尚无 WGPU 产品通过或 RenderDoc 对拍。后续 `render_plan09_sprite_render_layer_set_snapshot_static_passed_cargo_lock_blocked` 已把该路径升级为 typed `RenderLayerSet` 过滤。
- 2026-06-23 CO-M4 mesh selected-camera layer filter 已把 `zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/phase_ordering.rs` 作为 mesh phase ordering owner:raw mesh-vector fallback 与 material-adjusted phase queue consumption 都会先用 selected camera layer set 过滤 `RenderMeshSnapshot.render_layer_mask`。新增 `phase_ordered_meshes_filter_meshes_by_selected_camera_layers` 覆盖 fallback 与 phase queue 两条路径;状态锚为 `render_plan09_mesh_selected_camera_layer_filter_static_passed_cargo_timeout_no_result`,当前只声明 rustfmt/static/line-count 证据,focused Cargo 124s 超时且无 test binary,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 mesh render-layer snapshot typed mask 已把 `RenderMeshSnapshot.render_layer_mask` 从 legacy `u32` 改为 `RenderLayerSet`。scene authoring 的实体 mask 仍是旧 `u32`,但只在 `scene/world/render.rs` 的 mesh DTO 边界包装为 typed set;`build_mesh_draws/build/phase_ordering.rs` 直接用 `selected_camera_layers().intersects(&mesh.render_layer_mask)` 过滤,避免未来 layer 32+ mesh 在 CPU 侧被 legacy mask 截断。`StaticMeshBatchExtract` 与 frame-history mesh validation key 同步 typed;该切片完成时 `RenderFrameExtract::from_snapshot(...)`、`build_visibility_input(...)` 与 visibility fallback 仍投影到旧 `VisibilityRenderableInput` / visibility ABI,后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 已关闭该 DTO 边界。状态锚为 `render_plan09_mesh_render_layer_set_snapshot_static_passed_cargo_lock_blocked`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 被 `Cargo.lock` 漂移在编译前阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 PrimitiveRelevance typed layer filter 已把 shared relevance 判断从 legacy `u32` 改为 `RenderLayerSet` 引用:`PrimitiveRelevance::for_mesh_view(...)` 直接消费 typed mesh snapshot layer set,`view_visible_for_layers(...)` 也只接收 typed set。该切片关闭 relevance 内部 lossy 判断;后续 `render_plan09_visibility_batch_key_layer_set_static_passed_cargo_lock_blocked` 已把 `VisibilityBatchKey` / `FrameVisibility.render_layer_masks` 迁到 typed layer set,再由 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 关闭 visibility input DTO 旧边界。状态锚为 `render_plan09_primitive_relevance_typed_layer_filter_static_passed_cargo_lock_blocked_timeout_no_result`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 超时且 locked check 被 `Cargo.lock` 漂移阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 VisibilityBatchKey typed layer set 已把 `VisibilityBatchKey.render_layer_mask` 与 `FrameVisibility.render_layer_masks` 从 legacy `u32` 收束为 `RenderLayerSet`。visibility batching 从 typed mesh snapshot layer set clone 出 batch key、BVH instance 与 history entry;custom-target `build_views.rs` 不再 `from_legacy_mask(*render_layer_mask)`,而是直接传 typed set 给 relevance。新增 `visibility_batch_key_preserves_layers_above_legacy_mask_width` 用 layer 40 锁定 batch key 和 `FrameVisibility` 不再经 32-bit lossy mask;状态锚为 `render_plan09_visibility_batch_key_layer_set_static_passed_cargo_lock_blocked`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 与 locked check 均被 `Cargo.lock` 漂移在编译前阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 VisibilityRenderableInput typed layer set 已把 `VisibilityRenderableInput.render_layer_mask`、`RenderFrameExtract::from_snapshot(...)`、visibility fallback 和 `scene/world/render.rs::build_visibility_input(...)` 收束为 typed `RenderLayerSet`。particle emitter layer 聚合改用 `RenderLayerSet::union(...)`;`frame_extract.rs` 内联 tests 迁到 `frame_extract/tests.rs`,主文件降到 894 行,新增 `render_frame_extract_visibility_input_preserves_layers_above_legacy_mask_width` 锁定 layer 40 不再被 visibility input 截断。状态锚为 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 与 locked check 均被 `Cargo.lock` 漂移在编译前阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 Plan 09 CO-M4 world visibility input owner split 已把 `scene/world/render.rs` 内的 visibility input DTO 构造抽入 `scene/world/render_visibility.rs`。新 child owner 承接 `build_visibility_input(...)`、`particle_emitter_render_layer_masks(...)`、`empty_visibility_input(...)`、typed `VisibilityRenderableInput` rows 与 `RenderLayerSet::union(...)` particle emitter layer 聚合;`scene/world/render.rs` 只保留 import 与调用点。新增 `runtime_15_scene_world_render_visibility_input_is_child_owner` 锁定 owner 挂载、moved functions 不回流、docs/status 锚点和行数预算。状态锚为 `render_plan09_world_visibility_input_owner_split_static_passed_cargo_timeout_no_result`;当前只声明 rustfmt/static/line-count/diff-check 证据,focused locked Cargo 124s 超时无结果,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 particle selected-camera layer filter 初始把 `RenderParticleSpriteSnapshot.render_layer_mask` 接入 particle sprite 可见性,并由 `scene/world/render_particles.rs` 把实体 layer 传入 authored particles 与 world HUD bars。`build_particle_vertices(...)`、`build_particle_velocity_vertices(...)` 和 `RenderPipelineAsset::compile(...)` 都读取 selected camera layer set,分别阻断不可见 particle 顶点生成、velocity current sprite 配对和 `particle-render` auto-insert。新增三条本地守卫和 world-particle mask 保留断言;状态锚为 `render_plan09_particle_selected_camera_layer_filter_static_passed_cargo_timeout_no_result`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 184s 超时且无 test binary,尚无 WGPU 产品通过或 RenderDoc 对拍。后续 `render_plan09_particle_render_layer_set_snapshot_static_passed_cargo_lock_blocked` 已把该 DTO 从 legacy `u32` 升级为 typed `RenderLayerSet`。
- 2026-06-23 CO-M4 light layer typed mask convergence 已把 directional/point/spot/rect light snapshot 的 `layer_mask` 统一为 `RenderLayerSet`。scene extraction 继续从 legacy entity mask 输入,在 extract 边界包装为 typed set;`light_buffer.rs` 只在写入 `GpuLightData.shadow_slot_layer[1]` 时用 `to_legacy_mask_lossy()` 回落到旧 32-bit GPU ABI。状态锚为 `render_plan09_light_layer_set_snapshot_static_passed_cargo_timeout_no_result`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 188s 超时且无 test binary,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 volume-mask separation 已把 post-process Volume 过滤从 selected camera culling mask 分离出来:`RenderViewExtract::selected_camera_volume_layers()` 读取 selected descriptor `volume_mask`,scene extraction 在 `render_post_process.rs` 内按 selected/stack camera volume mask union 收集 Volume,submit-time `resolved_settings_for_camera(...)` 也传 volume mask。新增 `explicit_request_camera_uses_volume_mask_for_post_process_volumes`;状态锚为 `render_plan09_volume_mask_separate_from_culling_static_passed_cargo_lock_blocked_timeout_no_result`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 超时且 locked check 被 `Cargo.lock` 漂移阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 selected-camera history layer key 已把 `ViewportCameraHistoryKey` 的槽位选择扩展到 selected descriptor 的 `culling_mask` 与 `volume_mask`。私有 `ViewportCameraHistoryLayerKey` 通过 `RenderLayerSet::iter()` 记录 typed layer 列表,避免用 legacy 32-bit mask 丢失 layer 40+。新增 `camera_history_key_distinguishes_culling_layers_without_legacy_loss` 与 `camera_history_key_distinguishes_volume_layers_without_legacy_loss`;状态锚为 `render_plan09_history_key_layer_masks_static_passed_cargo_lock_blocked`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 被 `Cargo.lock` 漂移在编译前阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 sprite render-layer snapshot typed mask 已把 `RenderSpriteSnapshot.render_layer_mask` 从 legacy `u32` 改为 `RenderLayerSet`。scene authoring 的实体 mask 仍是旧 `u32`,但只在 `World::render_sprite_snapshot_for_camera(...)` 边界包装为 typed set;`build_sprite_vertices(...)` 直接用 `selected_camera_layers().intersects(&sprite.render_layer_mask)` 过滤,layer 40+ sprite 不再被 `to_legacy_mask_lossy()` 截断。该切片完成时 visibility input 仍通过显式 `to_legacy_mask_lossy()` 留在旧 ABI 边界;后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 已关闭该 DTO 边界。状态锚为 `render_plan09_sprite_render_layer_set_snapshot_static_passed_cargo_lock_blocked`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 被 `Cargo.lock` 漂移在编译前阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。
- 2026-06-23 CO-M4 particle render-layer snapshot typed mask 已把 `RenderParticleSpriteSnapshot.render_layer_mask` 从 legacy `u32` 改为 `RenderLayerSet`。scene authoring 的实体 mask 仍是旧 `u32`,但只在 `scene/world/render_particles.rs` 的 particle DTO 边界包装为 typed set;`build_particle_vertices(...)`、`build_particle_velocity_vertices(...)` 与 `RenderPipelineAsset::compile(...)` 直接用 `selected_camera_layers().intersects(&sprite.render_layer_mask)` 过滤,避免未来 layer 32+ particle sprite 在 CPU 侧被 legacy mask 截断。该切片完成时 `build_visibility_input(...)` 仍用 `to_legacy_mask_lossy()` OR 聚合 particle emitter mask;后续 `render_plan09_visibility_renderable_input_layer_set_static_passed_cargo_lock_blocked_timeout_no_result` 已改为 typed `RenderLayerSet::union(...)`。状态锚为 `render_plan09_particle_render_layer_set_snapshot_static_passed_cargo_lock_blocked`,当前只声明 rustfmt/static/line-count/diff-check 证据,focused Cargo 被 `Cargo.lock` 漂移在编译前阻断,尚无 WGPU 产品通过或 RenderDoc 对拍。

### 参考实现精读笔记

| 真实符号 | 要点 | Zircon 对应物与取舍 |
|---|---|---|
| `UniversalRenderPipelineCore.cs` `cameraComparison = (camera1, camera2) => (int)camera1.depth - (int)camera2.depth`、`SortCameras` | 相机间只按 float depth 截断 int 比较,无次级键,同 depth 顺序未定义 | `render_order: i32` + `camera_ordering.rs` 既有 ambiguity 报告:同 order 同 target 显式诊断而非未定义——比 URP 更严,代价是多一份 report 结构,接受 |
| `UniversalRenderPipeline.cs` `RenderCameraStack`(L1038):`renderType == CameraRenderType.Overlay` 即 return;`lastActiveOverlayCameraIndex`;overlay 循环内 `CreateCameraData(overlayFrameData, baseCamera, baseCameraAdditionalData)` 后 `InitializeAdditionalCameraData(overlayCamera, ...)` 覆写 | Overlay 不独立成帧;overlay 的 CameraData 先以 **base** 相机构造再逐相机覆写 → target/viewport 继承 Base 是构造性事实;"最后活跃相机"决定 resolve-to-screen 时机 | `resolve_camera_sequence` 把 Overlay 收进 `CameraSequenceEntry.overlays`;继承规则写死为 target/viewport_rect 来自 Base。Zircon 用 graph import 表达 resolve 时机,不需要 lastActive 标志位,简化接受 |
| `UniversalAdditionalCameraData.cs`:`CameraRenderType { Base, Overlay }`、`cameraStack`(非 Base 访问报 warning 返 null)、`clearDepth`、`volumeLayerMask`、`renderPostProcessing` | 相机扩展字段清单样板;stack 仅 Base 暴露的访问器级防御 | `CameraRenderDescriptor` 字段一一对应(`renderPostProcessing` 归计划 07,不进本契约);防御移到 `resolve_camera_sequence` 的数据校验,契约结构保持 plain data |
| `ScriptableRenderer.cs` `GetCameraClearFlag`(L1031):Overlay → `clearDepth ? ClearFlag.DepthStencil : ClearFlag.None`;Skybox/Nothing 且 `msaaSamples > 1` → 额外清 color(防 alpha-to-coverage 混上一帧) | clear 决策集中一处、按 render_type 分叉;MSAA 边角案例显式处理 | `RenderCameraClear` → load op 翻译表集中在 `camera_loop.rs` 单函数;MSAA>1 时 Skybox/None 同样强制清 color,采纳该边角 |
| `UniversalRenderPipeline.cs` L1667:`commonOpaqueFlags = SortingCriteria.CommonOpaque`、`noFrontToBackOpaqueFlags = SortingCriteria.SortingLayer \| RenderQueue \| OptimizeStateChanges \| CanvasOrder`、`canSkipFrontToBackSorting`(HSR GPU 或 `OpaqueSortMode.NoDistanceSort`) | opaque 的距离排序是**可整体放弃**的优化项,SortingLayer/RenderQueue/状态切换优化才是常驻准则 | 直接支撑位段决策:域 A 深度只占 15 bit 粗量化、聚簇位在深度之上;Zircon 不做运行时开关(位段静态),以聚簇优先的单一布局覆盖两种模式,取舍接受 |

## 风险与回退

- 多相机 × compiled graph 缓存膨胀:缓存键只含影响图结构的相机字段(目标格式/尺寸/feature),order/mask 类字段走运行时参数,避免每相机一份图。
- queue 自由覆写破坏 phase 假设(如 queue<2500 却半透明混合):材质验证器在资产导入期诊断 queue 与混合态矛盾。
- screen-space UI 已闭环路径不动:本计划只接管 world-space UI 的排序归属,越界改动按硬切换原则需先在 UI 计划侧立项。
