# 14-2d-stack 产出记录归档

> 来源：[`14-2d-stack.md`](../14-2d-stack.md) 的 `## 状态与产出记录`。

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-07-03 | Runtime 15 M4 SDF atlas/render tests folder-backed guard sync | runtime_15_sdf_atlas_render_tests_folder_backed_guard_sync_static_passed_cargo_deferred | Runtime 15 structure guards now read the current folder-backed SDF test owners instead of stale single-file paths: `graphics/scene/scene_renderer/ui/sdf_atlas/tests/mod.rs`, `graphics/scene/scene_renderer/ui/sdf_atlas/tests/plan.rs`, `graphics/scene/scene_renderer/ui/sdf_atlas/tests/allocation.rs`, `graphics/scene/scene_renderer/ui/sdf_atlas/tests/cache_report.rs`, `graphics/scene/scene_renderer/ui/sdf_atlas/tests/owner.rs`, `graphics/scene/scene_renderer/ui/sdf_render/vertices.rs`, `graphics/scene/scene_renderer/ui/sdf_render/tests/mod.rs`, `graphics/scene/scene_renderer/ui/sdf_render/tests/draw_plan.rs`, `graphics/scene/scene_renderer/ui/sdf_render/tests/shader_contract.rs`, `graphics/scene/scene_renderer/ui/sdf_render/tests/layout_placement.rs`, and `graphics/scene/scene_renderer/ui/sdf_render/tests/prepare_report.rs`. Historical `Screen-space UI SDF atlas test owner split` / `render_plan14_sdf_atlas_test_owner_split_static_passed_cargo_deferred_active_compile_lane` and `Screen-space UI SDF render test owner split` / `render_plan14_sdf_render_test_owner_split_static_passed_cargo_deferred_active_compile_lane` anchors remain intact while `runtime_15_screen_space_ui_sdf_atlas_tests_are_child_owner_split` validates the atlas directory owner and `runtime_15_screen_space_ui_sdf_render_tests_are_child_owner_split` validates the render vertex child plus test directory owners. | Scoped standalone structure/status validation for this sync; Cargo/WGPU/RenderDoc remain deferred because active build lanes are outside this structure-only slice. | Real UI/text product sweep, RenderDoc overlay/UI capture, Plan 14 shared text service, and scene text renderer remain separate follow-up work. |
| 2026-06-24 | TD-M2/F12 Sprite build vertices test owner split | render_plan14_sprite_build_vertices_test_owner_split_static_passed_cargo_deferred_active_compile_lane | Sprite build vertices test owner split 已把 `graphics/scene/scene_renderer/sprite/build_sprite_vertices.rs` 底部 inline sprite phase/filter/scale/tile/slice tests 移入 `graphics/scene/scene_renderer/sprite/build_sprite_vertices/tests.rs`；父 owner 只保留 `build_sprite_vertices(...)`、image mode CPU slicing、nine-slice/tile helpers 和 `#[cfg(test)] mod tests;` 挂载，行数从 701 降到 473，测试 owner 为 227 行。新增 `runtime_15_sprite_build_vertices_tests_are_child_owner_split` 锁定 moved tests 不回流、父/子 800 行预算与 docs/status 锚点。 | `rustfmt --edition 2021 --check` 覆盖 `build_sprite_vertices.rs`、`build_sprite_vertices/tests.rs` 与 guard；静态 source scans、父子行数预算、docs-anchor、whitespace 和 scoped diff-check 通过。当前有其他 Cargo/Rust 编译通道活跃，本切片未叠加 Cargo/WGPU/RenderDoc，且不计通过。 | TD-M2 仍需共享化 nine-slice 函数、ImageRenderer fill/radial 模式、真实 sprite product sweep 与 RenderDoc 对拍。 |
| 2026-06-24 | TD-M1/F12 Screen-space UI SDF atlas test owner split | render_plan14_sdf_atlas_test_owner_split_static_passed_cargo_deferred_active_compile_lane | Screen-space UI SDF atlas test owner split 已把 `graphics/scene/scene_renderer/ui/sdf_atlas.rs` 底部 inline atlas plan/cache tests 移入 `graphics/scene/scene_renderer/ui/sdf_atlas/tests.rs`；父 owner 只保留 SDF atlas slot planning、glyph-key collection、cache retention/eviction 与 `#[cfg(test)] mod tests;` 挂载，行数从 701 降到 384，测试 owner 为 313 行。新增 `runtime_15_screen_space_ui_sdf_atlas_tests_are_child_owner_split` 锁定 moved tests 不回流、父/子 800 行预算与 docs/status 锚点。 | `rustfmt --edition 2021 --check` 覆盖 `sdf_atlas.rs`、`sdf_atlas/tests.rs` 与 guard；静态 source scans、父子行数预算、docs-anchor、whitespace 和 scoped diff-check 通过。当前有其他 Cargo/Rust 编译通道活跃，本切片未叠加 Cargo/WGPU/RenderDoc，且不计通过。 | 真实 UI/text product sweep、RenderDoc overlay/UI capture、Plan 14 shared text service 与 scene text renderer 仍是后续项。 |
| 2026-06-24 | TD-M1/F12 Screen-space UI SDF render test owner split | render_plan14_sdf_render_test_owner_split_static_passed_cargo_deferred_active_compile_lane | Screen-space UI SDF render test owner split 已把 `graphics/scene/scene_renderer/ui/sdf_render.rs` 底部 inline SDF draw-plan tests 移入 `graphics/scene/scene_renderer/ui/sdf_render/tests.rs`；父 owner 只保留 SDF WGPU pipeline/atlas/vertex generation production path 和 `#[cfg(test)] mod tests;` 挂载，行数从 820 降到 540。新增 `runtime_15_screen_space_ui_sdf_render_tests_are_child_owner_split` 锁定 moved tests 不回流、父/子 800 行预算与 docs/status 锚点。 | `rustfmt --edition 2021 --check` 覆盖 `sdf_render.rs`、`sdf_render/tests.rs` 与 guard；静态 source scans、父子行数预算、docs-anchor、whitespace 和 scoped diff-check 通过。当前有其他 Cargo/Rust 编译通道活跃，本切片未叠加 Cargo/WGPU/RenderDoc，且不计通过。 | 真实 UI/text product sweep、RenderDoc overlay/UI capture、Plan 14 shared text service 与 scene text renderer 仍是后续项。 |
| 2026-06-24 | TD-M1/F12 Screen-space UI render test owner split | render_plan14_screen_space_ui_render_test_owner_split_static_passed_cargo_deferred_active_compile_lane | Screen-space UI render test owner split 已把 `graphics/scene/scene_renderer/ui/render.rs` 底部 inline plan tests 移入 `graphics/scene/scene_renderer/ui/render/tests.rs`；父 owner 只保留 WGPU UI record/prepare/plan production path 和 `#[cfg(test)] mod tests;` 挂载，行数从 960 降到 639。新增 `runtime_15_screen_space_ui_render_tests_are_child_owner_split` 锁定 moved tests 不回流、父/子 800 行预算与 docs/status 锚点。 | `rustfmt --edition 2021 --check` 覆盖 `render.rs`、`render/tests.rs` 与 guard；静态 source scans、父子行数预算、docs-anchor、whitespace 和 scoped diff-check 通过。当前有其他 Cargo/Rust 编译通道活跃，本切片未叠加 Cargo/WGPU/RenderDoc，且不计通过。 | 真实 UI/text product sweep、RenderDoc overlay/UI capture、Plan 14 shared text service 与 scene text renderer 仍是后续项。 |
| 2026-06-24 | TD-M1/M7/F12 UI font and attachment test-surface cleanup | render_plan14_ui_font_attachment_test_surface_suppression_static_passed_cargo_deferred_active_lanes | UI renderer 剩余生产 suppression 已清零。无 asset-manager 的 `load_ui_font_manifest(...)` 改为 test-only，生产字体路径继续使用 `load_ui_font_manifest_with_asset_manager(...)` 解析项目/默认 font manifest；`ScreenSpaceUiRenderer::last_attachment_ops()` 也改为 test-only，只服务 graph executor attachment-op contract。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\ui\font_asset.rs zircon_runtime\src\graphics\scene\scene_renderer\ui\render.rs` 通过；UI owner suppression scan 零命中；`zircon_runtime/src/graphics` 全目录 suppression scan 零命中。当前有其他 `zircon_runtime` Cargo/Rust test lanes 活跃，本切片未叠加 Cargo/WGPU/RenderDoc，且不计通过。 | 真实 UI/text product sweep、RenderDoc overlay/UI capture、Plan 14 shared text service 与 scene text renderer 仍是后续项。 |
| 2026-06-24 | TD-M2/F12 sprite queue stats suppression cleanup | render_plan14_sprite_queue_stats_suppression_cleanup_static_passed_cargo_deferred_active_lanes | `PreparedSpriteQueueStats` 不再用 `cfg_attr(not(test), allow(dead_code))` 维持生产结构。该 DTO 已经由 `prepare_sprite_queue_stats(...)`、`SceneRendererCompiledSceneOutputs`、`SceneRenderer::last_prepared_sprite_queue_stats()` 与 `update_stats/base_stats.rs` 写入 `RenderStats.last_sprite_*` 和 runtime diagnostics，属于已接线的 sprite queue diagnostics。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\sprite\prepared_batches.rs` 通过；sprite owner suppression scan 零命中；graphics suppression scan 不再列出 sprite prepared batches。当前有其他 `zircon_runtime` Cargo/Rust test lanes 活跃，本切片未叠加 Cargo/WGPU/RenderDoc，且不计通过。 | TD-M2 的 nine-slice/image renderer、完整 2D batching 升级和 UI font/render suppression pockets 仍是后续项。 |
| 2026-06-23 | Render index 当前状态总览拆分 | TD-M2/TD-M4 部分完成,TD-M1/TD-M3 未启动 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 14 2D Stack 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：text service、scene text renderer、tilemap data plane/renderer；验收缺口：需要 shaping/atlas service、nine-slice/image renderer、tilemap chunk/dirty upload tests |
| 2026-06-15 | TD-M1 text service and scene text renderer | 未启动: 文本仍主要在 UI 内部 | UI 文本/排版能力存在,但场景 2D 无共享 shaping/atlas 服务和 scene text renderer。 | 本文件 `现状与差距` 明确文本仅 UI 内部、场景 2D 不能直接复用。 | 下沉 font/shaping/atlas 服务,实现 scene text draw command 和 batching。 |
| 2026-06-15 | TD-M2 nine-slice and image renderer | 部分完成: sprite/atlas 基础存在,九宫和 image renderer 未完成 | sprite renderer 与 atlas management 可用,prepared batches 有批组织;九宫、image renderer、sprite material override 仍待计划实现。 | 本文件 `现状与差距` 记录 sprite 渲染器和图集管理可用但无九宫。 | 实现 nine-slice mesh generation、image renderer asset contract 和 batching stats。 |
| 2026-06-15 | TD-M3 tilemap data plane and renderer | 未启动: tilemap 数据面缺失 | 无 tilemap chunk、palette、collision/render separation 或 streaming 机制。 | 本文件 `现状与差距` 明确无 tilemap。 | 建立 tilemap asset/chunk renderer、dirty region upload 和 material atlas binding。 |
| 2026-06-15 | TD-M4 sorting and brush contract | 部分完成: UI 内部排序自洽,场景 2D 混排未统一 | UI z-index 与 painter 内部已有规则,但 sprite/world-space UI/3D 统一排序仍依赖计划 09。 | 计划 09 状态表记录 unified sort key 尚未完成;本文件记录无 y-sort。 | 与计划 09 CO-M3 对齐 sort key、y-sort、layer 和 brush priority。 |

### 参考实现精读笔记

1. **godot `scene/2d/tile_map_layer.cpp/.h`**(quadrant 组织与脏重建)
   - `TileMapLayer::_coords_to_quadrant_coords`(L58):负坐标向下取整的整除(`(x - (size-1)) / size`),Zircon `chunk_coords_for_cell` 直接采用该语义。
   - 脏跟踪:`dirty.flags[DIRTY_FLAGS_MAX]` 布尔位 + `dirty.cell_list`(侵入式 `SelfList<CellData>` 链,tile_map_layer.h L419-421);`quadrant_shape_changed`(L251-253)在 y-sort 开关、tile_set 更换、quadrant size 改变时全清重建,否则仅 `_rendering_quadrants_update_cell` 把受影响 quadrant 挂入 `dirty_rendering_quadrant_list` 增量重绘。Zircon 对应:cell 级编辑 → chunk 脏集;grid/tile_set 级变更 → 全 chunk 脏。
   - y-sort:启用时 quadrant 退化为"行桶"——`canvas_items_position = (0, map_to_local(coords).y + tile_y_sort_origin + y_sort_origin)`、`quadrant_coords = canvas_items_position * 100`(L552-553),即按世界 y(放大 100 取整)聚簇;`rendering_quadrant_size` 默认 16。Zircon 取舍:保留固定 32×32 chunk 做剔除与缓冲管理,仅在 y-sort 开启时 chunk 内按行拆提交子段 —— 不学 canvas item 重聚簇,因为我们直接产顶点缓冲、排序交给计划 09 sort_key。
2. **godot `scene/resources/2d/tile_set.cpp`**(坐标换算权威)
   - `TileSet::map_to_local`(L1556-1627):half-offset square / hexagon / isometric 三形共用同一半偏移公式族,差异仅 `overlapping_ratio`(hex=0.75、iso=0.5,L1610-1624);返回 cell 中心 `(ret + (0.5,0.5)) * tile_size`。
   - `TileSet::local_to_map`(L1629-1755):先除 overlapping_ratio,再"smart floor" + `in_top_left_triangle`/`in_top_right_triangle` 叉积测试(L1666-1667)修正顶角三角形归属。Zircon 取舍:对外用 axial hex / diamond iso 坐标(公式更可逆、便于 cube round),单测以 godot stacked / DIAMOND_DOWN 布局数值做对照表,保证渲染语义一致而不复刻其六种 `tile_layout` 全集。
3. **bevy `crates/bevy_text/src/pipeline.rs`**(shaping→图集→quad 链路)
   - `TextPipeline::update_text_layout_info`(L301):遍历 `layout.lines()` 的 `PositionedLayoutItem::GlyphRun`,组 `FontAtlasKey { id, index, font_size_bits, variations_hash, hinting, font_smoothing }`,逐 glyph `get_glyph_atlas_info` miss 时 `add_glyph_to_atlas`,产 `PositionedGlyph { position, atlas_info, section_index, line_index }`;`RunGeometry` 记 underline/strikethrough 几何。注意:本仓 vendored bevy 已切 parley 后端(`Layout<TextBrush>`/`FontCx`),而非 cosmic-text —— 链路结构同构,Zircon 按既定选型用 cosmic-text,`ShapedGlyphRun` 隔离层保证后端可替换正是为此。
   - 借鉴:`font_size_bits = f32::to_bits` 进缓存键(Zircon `ShapedTextCacheKey.font_size_bits` 同法);quad position = 图集尺寸/2 + glyph 位置 + `atlas_info.offset`(bearing),Zircon `glyph_quads.rs` 公式同源。
4. **bevy `crates/bevy_text/src/font_atlas.rs`**(字形图集)
   - `FontAtlas { dynamic_texture_atlas_builder, glyph_to_atlas_index: HashMap<GlyphCacheKey, GlyphAtlasLocation>, texture_atlas, texture }`(L29);`add_glyph_to_atlas`(L130)先 `get_outlined_glyph_texture` 栅格化再逐页尝试,页满建新页;padding=2(`DynamicTextureAtlasBuilder::new(size, 2)`,L62)。Zircon 取舍:用 shelf 行分配器替代其动态 builder(字形高度聚类下碎片更低、实现更可控);`R8Unorm` 单通道页替代其 `Rgba8UnormSrgb`(SDF 与 alpha 都只需单通道,显存 1/4);增加页级 LRU 逐出(bevy 无逐出,长会话字形累积)。
5. **UE `SlateCore/Private/Fonts/FontCache.cpp` + `Slate/Framework/Text/ShapedTextCache.h`**(shaped run 缓存键)
   - `FShapedGlyphSequence`(FontCache.cpp L200):GlyphsToRender + TextBaseline + MaxTextHeight + SourceTextRange;`FShapedGlyphEntry { GlyphIndex, SourceIndex, XAdvance, XOffset, Kerning, NumCharactersInGlyph, NumGraphemeClustersInGlyph, TextDirection, bIsVisible }`(FontCache.h L149-184)—— Zircon `ShapedGlyph.source_range` 即 `SourceIndex + NumCharactersInGlyph` 的区间化,cluster 命中测试 `GetGlyphAtOffset`(L349)对应 `hit_test` 新实现。
   - 缓存键 `FCachedShapedTextKey { TextRange, Scale, TextContext, FontInfo }`(ShapedTextCache.h L13-50):UE 持文本区间 + 完整 FontInfo 等值比较;Zircon 取舍:改 `text_hash + font_id + font_size_bits + features_hash`,避免缓存键持有文本/字体对象引用(ABI 与生命周期更干净),代价是理论 hash 碰撞,以 64 位 hash + 字体 id 复合键压到可忽略。
   - shaping 入口对 `FSlateFontCache::ShapeBidirectionalText / ShapeUnidirectionalText`(L1222/L1232)的双向拆分:cosmic-text 内置 bidi,Zircon 不需要该双入口,`TextShapeRequest.base_direction` 单入口即可。

## 风险与回退

- shaping 库选型(cosmic-text vs 自研 harfbuzz 绑定):优先 cosmic-text(纯 Rust,bevy 同源);若 CJK 排版细节不足再评估,接口以 `ShapedGlyphRun` 隔离选型。
- UI 文本硬切换风险大:TD-M1 以 UI 全量文本测试为闸门,失败即修服务而不是回退双路径。
- tilemap 动画 tile/自动地形(terrain tile)规则:V1 只留属性槽与规则钩子,完整规则系统随编辑器画笔计划推进。
