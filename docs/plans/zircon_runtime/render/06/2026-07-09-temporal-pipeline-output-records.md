# 06-temporal-pipeline 产出记录归档

> 来源：[`06-temporal-pipeline.md`](../06-temporal-pipeline.md) 的 `## 状态与产出记录`。

## 状态与产出记录

| 日期 | 计划切片 | 状态 | 产出 | 验证/证据 | 剩余说明 |
|---|---|---|---|---|---|
| 2026-08-30 | TAA external-texture exact lease | 源码完成，动态验证待解锁 | TAA previous/current history 发布 View-sized `Rgba16Float` typed schema 与 full-texture access intent；previous 为 fragment sampled read，current 为 color-attachment write。history owner 暴露 borrowed texture/view/descriptor，compiled-scene binder 以稳定 identity 发布完整 physical binding，执行物化按 access ID 创建精确 WGPU view。 | builder/descriptor/compiled packet/binder 四层源码回归已补齐；精确 rustfmt、locked Cargo metadata、source-contract 与 scoped diff 检查通过。受管 validator 在 Cargo 前被 `cargo_reuse_target_mismatch` 阻塞，未产生 rustc/WGPU 结果。 | 真实连续帧 TAA PNG、RenderDoc RDC、300 帧 history valid/reset、GPU timestamp、VRAM/功耗与协调器验收仍待完成；不得标记 TP-M3/TP-M4 accepted。 |
| 2026-08-15 | ViewFamily resolution / temporal reconstruction foundation | 源码完成，受管动态验证待运行 | 新增 `RenderViewFamilyPipeline`，复用唯一的 camera `RenderViewportRect`，定义 primary/secondary logical viewport、8 像素对齐 allocation extent、包含 viewport/allocation 的 temporal history key，以及所有阶段消费的 `RenderViewFamilyTarget`；真实 frame submission 从选中相机的 `viewport_rect` 和动态分辨率解析 Temporal 或 Spatial pipeline，并在最终管线编译前校验 post-process graph phase。 | `rustfmt --edition 2021 --config skip_children=true --check` 覆盖 ViewFamily、post-process graph、render exports、frame submission build；`git diff --check` 覆盖 tracked touched files；新增奇数设备尺寸、非对齐分屏 origin、分屏 history identity、Temporal/Spatial 必需重建阶段、阶段逻辑 rect 与 padded allocation 分离、depth range 保持的静态回归；独立二次审查确认重复 ViewRect 类型和 depth-range 回归均已关闭。未运行 Cargo。 | 仍需将 resolved plan 接入 frame-context resource allocation 与 temporal-history owner，然后在 UI12 释放后的受管 Rust/product gate 及 RenderDoc/截图中验证；静态证据不能宣称 temporal super-resolution 画面验收。 |
| 2026-06-24 | TP-M3-S1d/F12 TemporalHistoryTexture lifetime owner cleanup | render_plan06_temporal_history_texture_lifetime_owner_suppression_coremin_passed | `TemporalHistoryTexture` 不再用 `#[allow(dead_code)]` 保留底层 `wgpu::Texture`。字段改名为 `_texture`，明确该 texture 是 paired `TextureView` 的 lifetime owner；`previous_view()` / `current_view()` 仍是 TAA scene-color history 的唯一运行时视图访问面。 | `rustfmt --edition 2021 --check zircon_runtime\src\graphics\scene\scene_renderer\temporal\taa\temporal_history_store.rs` 通过；owner suppression scan 零命中；graphics suppression scan 不再列出 temporal history store；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-render-temporal-history-owner-f12-0624 --message-format short --color never` 通过。 | RenderDoc TAA history capture、产品级 TAA sweep 与剩余 sprite/UI suppression pockets 仍是后续项。 |
| 2026-06-23 | Render index 当前状态总览拆分 | 计划内切片记录齐全,主体实现已大量落地 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 06 Temporal 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：宽回归与若干 shared lib-test 超时项仍需复跑；验收缺口：需要完整 temporal/product sweep、RenderDoc 和 CI 级验证 |
| 2026-06-14 | TP-M1-S1 GpuScene prev 数据面 | 已完成 | `gpu_scene/prev_transform.rs` 新增 previous transform 滚动与查询;compiled-scene/legacy submit 成功后滚动;mesh draw 同步可 fallback 到 GPUScene previous。 | scoped `rustfmt --edition 2021 --check`;8 文件 source-contract scan;tracked `git diff --check` 与未跟踪/忽略 trailing scan 通过;锁定 `cargo check` 曾被根 `Cargo.lock` 刷新要求阻塞。 | CPU object history 与 post motion-vector pass 当时仍待后续切片处理。 |
| 2026-06-14 | TP-M1-S2 velocity 双 pass 与旧 post pass 删除 | 已完成(核心迁移面) | 新增 `BuiltinRenderFeature::Temporal` 与 `feature_descriptors/temporal.rs`;默认 forward+/deferred 管线插入 `velocity-object`/`velocity-camera`;删除 `post.motion-vector-clear/camera/object` 声明与注册;相机 velocity 参数/执行迁入 `scene_renderer/temporal/velocity/*`;`SCENE_VELOCITY = "scene-velocity"` 落地并使用 `Rg16Float`;tile/max 链改读 `SCENE_VELOCITY`。 | `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-velocity-0614 --message-format short --color never` 通过(67 个既有 warning);`cargo test -p zircon_runtime pipeline_compile --lib ...` 中 40/42 通过,剩余 deferred WGSL include 既有缺 `zr_skinned_joint_count`;`compile_options_fallback_async_compute_passes_to_graphics_queue` 单测修复后通过;旧 post velocity pass/id/resource 扫描清零。 | CPU object-history 与 mesh velocity 内部命名已由 TP-M1-S3/S5 处理;TAA/history 语义切换仍按后续切片执行。 |
| 2026-06-14 | TP-M1-S3 CPU object-history 硬切 | 已完成(核心迁移面) | 删除 `ViewportMotionVectorObjectHistory`、viewport record object-history 字段、`ViewportRenderFrame.previous_motion_vector_object_history` 与 `update_motion_vector_history_after_success`;提交成功路径改为 `update_temporal_camera_history_after_success`;mesh pending draw 不再携带 CPU previous,unskinned previous transform 只来自 GPUScene rolled previous;object-history RenderStats/diagnostic rows 删除,保留 mesh queue previous/missing transform draw 计数作为对象 velocity readiness 面。 | `rustfmt --edition 2021 --check` 通过本切片 Rust 文件;源码扫描 `ViewportMotionVectorObjectHistory|previous_motion_vector_object_history|motion_vector_object_history|update_motion_vector_history|render.post_process.motion_vector.object.*_history_count` 清零。首轮 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-cpu-history-cut-0614 --message-format short --color never` 曾通过(66 个既有 warning);最终复跑在渲染代码前被无关 UI 编译错误阻塞:`zircon_runtime/src/ui/surface/surface/default_interactions.rs:748` 未覆盖 `UiComponentKeyboardAction::BeginEdit`。 | previous skinned palette、velocity 内部命名、CPU-morphed 诊断、stable shape 覆盖与 changing-shape previous-source velocity 已由 TP-M1-S4..S23 处理。 |
| 2026-06-14 | TP-M1-S4 GPUScene previous skinned palette roll | 已完成(核心迁移面) | 新增 `gpu_scene/prev_skinned_palette.rs`,GPUScene 持有 `current_skinned_joint_palettes`/`previous_skinned_joint_palettes`;successful submit 后 compiled-scene 与 legacy path 同步滚动 previous palette;mesh pending draw 携带 `skinned_palette_signature`,在 signature/joint count 匹配时接入 previous palette;CPU-morphed stable morph-shape gate 由 TP-M1-S7 追加;新增 draw 接入侧 guard 单测。 | `rustfmt --edition 2021` 与 `--check` 通过本切片 Rust 文件;静态扫描覆盖 `previous_skinned_joint_palette|stage_current_skinned_joint_palette|roll_prev_skinned_palettes|skinned_palette_signature` 以及 CPU object-history 旧符号;`git diff --check` 仅报告既有 LF/CRLF 提示。首轮 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-skinned-prev-palette-0614 --message-format short --color never` 暴露 `E0364/E0603` 可见性错误,已通过移除私有常量重导出并从 `scene_renderer` 层重导出 `SkinnedMeshJointPaletteUniform` 修复;复跑同命令通过(65 个既有 warning)。 | CPU-morphed stable shape 已由 S7 覆盖;changing-shape previous-source velocity 已由 S23 覆盖;TAA/history、jitter 与粒子 velocity 后续处理。 |
| 2026-06-14 | TP-M1-S5 temporal velocity 命名/文件所有权硬切 | 已完成(核心迁移面) | `mesh_motion_vector.rs` 删除并迁入 `temporal/velocity/execute_velocity_object.rs`;`create_motion_vector_mesh_pipeline.rs`/`ensure_motion_vector_pipeline.rs` 硬切为 `create_velocity_mesh_pipeline.rs`/`ensure_velocity_pipeline.rs`;`MeshPassPipelineKind::MotionVector` 改为 `Velocity`;mesh draw readiness、queue stats、RenderStats 与 diagnostics 改为 previous/missing/skinned GPU velocity 命名;相机 shader 迁入 `temporal/velocity/shaders/velocity_camera.wgsl`,camera pipeline/layout 字段改为 `velocity_camera_*`。 | `rustfmt --edition 2021 --check` 通过本切片 Rust 文件;全目录目标符号扫描 `mesh_motion_vector|create_motion_vector_mesh_pipeline|ensure_motion_vector_pipeline|has_previous_motion_vector_transform|previous_motion_vector_transform|missing_motion_vector_transform|skinned_gpu_motion_vector|motion_vector_history_eligible|motion_vector_camera_shader_source|motion_vector_camera_bind_group_layout|motion_vector_camera_pipeline|vs_motion_vector|fs_motion_vector` 清零;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-skinned-prev-palette-0614 --message-format short --color never` 通过(65 个既有 warning)。 | motion blur tile-max/neighbor-max 仍保留 motion-vector 命名;CPU-morphed previous-shape policy 已由 TP-M1-S6 拆出诊断,实际 previous-shape buffer、粒子 velocity、TAA/history 与 jitter 后续处理。 |
| 2026-06-14 | TP-M1-S6 CPU-morphed previous-shape velocity policy | 已完成(诊断策略面) | `prepared_queue.rs` 新增 `skinned_gpu_cpu_morphed_previous_shape_velocity_missing_count`;`RenderStats`、submit stats 映射、product diagnostics 与 runtime diagnostics fixtures 新增同名 public count/path。CPU-morphed GPU-skinning source 缺 previous shape 时不再计入普通 `missing_velocity_transform_draw_count`;S7 之后签名匹配者可进入 object velocity,签名变化仍走专用缺口。 | `rustfmt --edition 2021 --check` 通过 S6 触碰 Rust 文件;source scan 覆盖 `skinned_gpu_cpu_morphed_previous_shape_velocity_missing_count|last_mesh_skinned_gpu_cpu_morphed_previous_shape_velocity_missing_count`;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-skinned-prev-palette-0614 --message-format short --color never` 通过(65 个既有 warning)。 | Changing morph-weight previous-source velocity 已由 S23 覆盖;粒子 velocity、TAA/history 与 jitter 后续处理。 |
| 2026-06-14 | TP-M1-S7 CPU-morphed stable morph-shape velocity 覆盖 | 已完成(稳定 shape 覆盖面) | `PendingSkinnedGpuSource::CpuMorphed` 携带 `morph_shape_signature`;`GpuSceneSkinnedJointPaletteState` 记录可选 morph-shape signature;previous palette 选择逻辑在 CPU-morphed source 上要求 current/previous signature 一致。稳定 morph weights 的 CPU-morphed GPU-skinning draw 可进入 object velocity,权重变化仍保留 S6 专用缺口。 | `rustfmt --edition 2021 --check` 通过 S7/S6 触碰 Rust 文件;source scan 覆盖 `morph_shape_signature|PendingSkinnedGpuSource::CpuMorphed`;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-skinned-prev-palette-0614 --message-format short --color never` 通过(65 个既有 warning)。`cargo test -p zircon_runtime --lib morph_shape_signature ...` 244s 超时且无测试输出,未标记通过。 | CPU-morphed morph-weight 变化时仍缺 previous-shape buffer/velocity writer;粒子 velocity、TAA/history 与 jitter 后续处理。 |
| 2026-06-14 | TP-M1-S8 粒子 velocity 缺口诊断 | 已完成(诊断策略面) | `FrameSubmissionContext` 携带 particle sprite count;submit stats 在 motion blur/SSR reconstructed velocity 请求且 `particle.transparent` 执行时写入 `last_particle_velocity_missing_sprite_count`;runtime diagnostics 新增 `render.particle.velocity.missing_sprite_count`。 | `rustfmt --edition 2021 --check` 通过 S8 触碰 Rust 文件;source scan 覆盖 `last_particle_velocity_missing_sprite_count|particle_velocity_missing_sprite_count|particle_sprite_count`;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-skinned-prev-palette-0614 --message-format short --color never` 通过(65 个既有 warning)。过滤单测 `particle_velocity_gap_counts_sprites_only_when_reconstructed_velocity_is_requested` 304s 超时且无可用结果,残留 cargo/rustc 已终止。 | 真实粒子 velocity writer 仍缺 stable sprite id、previous position/shape 与 previous billboard basis 数据契约;TAA/history 与 jitter 后续处理。 |
| 2026-06-14 | TP-M2-S1a 契约与矩阵对数据面 | 已完成(契约数据面;S1b 已补后续 ABI/注入) | 新增 `TemporalJitterSample`、`TemporalJitterSequence`、`halton(...)` 与 `ViewProjectionMatrixPair`;`ViewportCameraSnapshot` 增加 `temporal_jitter`;`SceneUniform::from_frame` 改经矩阵对生成当前 `view_proj`,并由 frame viewport size 推导 aspect。 | `rustfmt --edition 2021 --check` 通过 S1a 触碰 Rust 文件;source scan 覆盖 `TemporalJitterSample|TemporalJitterSequence|ViewProjectionMatrixPair|temporal_jitter|SceneUniform::from_frame`。复跑 core-min `cargo check` 当时被无关 UI tree-view helper 缺失阻塞:`decode_tree_reorder_drag`/`encode_tree_reorder_drag`/`tree_reorderable` 等未定义;后续 S1b core-min check 已通过。 | S1a 的 ABI/注入后续已由下一行 S1b 补齐;剩余工作收敛到投影消费审计、TAA-off product 对拍、TAA resolve/history。 |
| 2026-06-14 | TP-M2-S1b jitter 注入与 scene uniform ABI 扩展 | 已完成(核心 ABI/注入面) | `ViewportRecord` 增加 temporal frame index,`FrameSubmissionContext` 按有效 TAA 与 temporal frame index 生成 jitter;build/direct/present 提交成功路径保存去 jitter previous camera 并推进 frame index;`SceneUniform` 与 fallback/deferred/HZB/builtin PBR shader layout 增加 current unjittered、previous unjittered、jitter params;velocity object/camera 使用 unjittered 矩阵。 | `cargo fmt --package zircon_runtime` 与 `cargo fmt --package zircon_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s1b-0614 --message-format short --color never` 通过(65 个既有 warning)。source scan 确认旧 `scene.previous_view_proj` shader 访问只剩“不得包含旧字段”的测试断言。`cargo test --lib --no-run` 被无关 UI table 测试/交互代码阻塞。 | 仍需完整 SSR/SSAO/HZB 等投影消费审计清单、关闭 TAA product 对拍、TAA resolve/history 语义切换;filtered test/`--no-run` 需待 UI table compile drift 修复后补证。 |
| 2026-06-14 | TP-M2-S2 上游 pass 审计 | 已完成(矩阵消费审计面) | `SceneUniform::inverse_view_proj` 改为 current unjittered inverse;新增 scene uniform inverse 回归测试;SSR/post-process 参数新增 jitter 不影响 `effect_projection` 与 view-basis 的回归测试;反射探针投屏 helper 改走 `ViewProjectionMatrixPair::clip_from_world_unjittered` 并删除本地重复投影 helper。审计结论:光栅 pass 保留 jittered `scene.view_proj`,反投影/重投影 pass 使用 unjittered 矩阵或无 jitter post-process 参数。 | `cargo fmt --package zircon_runtime -- --check` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s2-0614 --message-format short --color never` 通过(65 个既有 warning);`view_proj|inverse_view_proj|previous_view_proj|clip_from_world|world_from_clip` source scan 覆盖 scene renderer WGSL/Rust 消费点。 | TP-M2 还缺 TAA Off product 逐像素对拍;TP-M3/T4 的 TAA resolve/history 语义切换仍未开始。 |
| 2026-06-14 | TP-M2-S3 TAA Off product 对拍基线 | 已完成(当前仓库可执行基线面) | `render_product_anti_alias.rs` 新增 `render_product_temporal_off_matches_anti_alias_feature_disabled_product`;同一 `World::new()` 产品 extract 在 `AntiAliasSettings::off()` 下分别提交 AA feature-enabled 与 feature-disabled 两个 viewport,断言 requested/effective mode 均为 Off、AA/FXAA pass 未执行,并逐像素比较 captured RGBA。 | `cargo fmt --package zircon_runtime` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s3-0614 --message-format short --color never` 通过(65 个既有 warning)。filtered `cargo test --lib ... --no-run` 被无关 UI table 输入路由测试字段漂移阻塞:`capture_started`/`capture_released` 已不存在;无残留 cargo/rustc 进程。 | 历史 pre-jitter hash 基线与全量 `render_product` 对拍仍待补证;TP-M3/T4 的 TAA resolve/history 语义切换仍未开始。 |
| 2026-06-14 | TP-M3-S1a TAA resolve graph/resource contract | 已完成(合同面;shader/store 未实现) | 新增 `PostProcessEffectKind::TaaResolve`、`TAA_HISTORY_PREVIOUS`/`TAA_HISTORY_CURRENT`/`TAA_OUTPUT`、`temporal.taa-resolve` pass/executor id 与 `post-process` 可选 `TAA_OUTPUT` 输入;TAA 生效时 legacy `HistoryResolve` stack node 禁用并让 final composite 读 `TAA_OUTPUT`;TAA 关闭时 `taa-resolve` 与 TAA resources 不进入 live graph;raw `SCENE_VELOCITY` producer 与 tile/neighbor motion-vector 链的编译/执行条件拆分;history fallback 移除 TAA-only velocity 并保留 motion blur/SSR velocity。 | `cargo fmt --package zircon_runtime` 通过;`git diff --check` 覆盖本切片文件仅有既有 LF/CRLF 提示;source scan 覆盖 `temporal.taa-resolve`/`TAA_OUTPUT` 且本切片相关路径无 `SCENE_MOTION_VECTOR` 残留。`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 被无关 scene asset compile drift 阻塞:`SceneSpotLightAsset` 缺失、scene asset `serde`/`AssetReference`/Clone-Debug-Serialize 派生链未收敛。 | 仍需实现 `taa_resolve.wgsl`、WGPU fullscreen resolve pipeline、quality/blend 参数、persistent double-buffer `TemporalHistoryStore`、history 槽位硬切与旧 `history_resolve` 删除;聚焦测试需待无关 asset/UI 编译漂移恢复后补跑。 |
| 2026-06-14 | TP-M3-S1b TAA resolve WGPU executor/history flip | 已完成(最小真实执行面) | 新增 `temporal/taa/shaders/taa_resolve.wgsl`、`TaaResolveParams`、TAA bind group layout/pipeline/buffer bundle 与 `ScenePostProcessResources::execute_taa_resolve(...)`;`temporal.taa-resolve` 改为专用 executor,写 `TAA_OUTPUT` 和 `TAA_HISTORY_CURRENT` 双 render target;`SceneFrameHistoryTextures` 新增 scene-color write slot,compiled graph 导入 previous/current TAA history,帧末成功后 swap 而不是从最终 target copy。 | `rustfmt --edition 2021` 通过本切片触碰 Rust 文件;raw 与 viewport-depth-fallback `taa_resolve.wgsl` 通过 Naga parse/validate;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning)。`cargo fmt --package zircon_runtime` 被无关 UI `default_interactions/table/virtualization.rs` 缺失阻塞;`cargo test -p zircon_runtime taa_resolve --lib ...` 被无关 UI table diagnostics `scroll_defaulted` 字段漂移阻塞。 | quality profile、YCoCg/AABB clip、深度差 disocclusion、responsive suppression 与完整 TAA history ownership 已由 S1c/S1d/S1e 继续覆盖;仍需旧 `history_resolve` hard-cut 与产品级 TAA 收敛/遮挡对拍。 |
| 2026-06-14 | TP-M3-S1c TAA 质量档与 resolve 质量内核 | 已完成(基础质量面;产品启用仍后续) | 新增 `TaaQualityPreset::{Low, Medium, High}` 并接入 `AntiAliasSettings`、`AntiAliasFallbackReport` 与 `RenderQualityProfile::with_taa_quality(...)`;`build_frame_submission_context` 将 quality profile TAA 档位投到有效 view extract;`TaaResolveParams` 通过 uniform 编码 history blend、motion rejection、variance clip gamma 与 depth disocclusion threshold;`taa_resolve.wgsl` 改为 YCoCg 3x3 variance AABB clip、closest-depth velocity dilation、深度差 disocclusion 与 history confidence。 | `rustfmt --edition 2021 --check` 通过 S1c 触碰 Rust 文件;scoped `git diff --check` 仅有既有 LF/CRLF 提示;raw 与 viewport-depth-fallback `taa_resolve.wgsl` 通过 Naga parse/validate;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning);`cargo test -p zircon_runtime taa_quality --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 在进入 TAA/quality 过滤测试前被无关 UI tree-view 漂移阻塞:`tree_view_reparent`/`tree_view_virtualization` 模块缺失、`toml_number` 未定义、`apply_default_tree_view_virtual_scroll` 方法缺失。 | WGPU offscreen TAA 产品 gate 已由 S1f 打开;responsive/resource-derived suppression 与完整 `TemporalHistoryStore` 失效语义已由 S1e/S1d 覆盖;旧 `history_resolve` hard-cut、产品级 TAA 对拍和过滤单测仍待后续/待无关 UI 测试漂移清理后补证。 |
| 2026-06-14 | TP-M3-S1d TemporalHistoryStore 基础硬切 | 已完成(基础 history ownership;产品启用仍后续) | 新增 `temporal/taa/temporal_history_store.rs`,TAA scene-color history 改为独立 `Rgba16Float` read/write store,带 `TemporalHistoryKey`、valid/reset 状态与 success-only flip;`SceneFrameHistoryTextures` 只通过 TAA history accessor 暴露 previous/current view;`prepare_history_textures(...)` 在 resize/format/previous-history 不可用时 invalidate TAA store;`build_frame_submission_context` 在后端支持 TAA 且 history store 可用时允许无效 previous 的 seed 帧进入 TAA resolve;`TaaResolveParams` 接收 history-valid flag,无效首帧 history weight=0 并 seed 下一帧。 | `rustfmt --edition 2021 --check` 通过 S1d 触碰 Rust 文件;scoped `git diff --check` 仅有既有 LF/CRLF 提示;source scan 确认旧 `scene_color_write_view`/`swap_scene_color_history` 不再作为公开 history 字段/API。`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 复跑通过(65 个既有 warning);`cargo test -p zircon_runtime temporal_history_state --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 184s 超时仍在编译 shared lib-test target,残留本目标 cargo/rustc 已停止。 | WGPU offscreen TAA 产品 gate 已由 S1f 打开;旧 `history_resolve` hard-cut、产品级 TAA 收敛/遮挡对拍和过滤单测仍待后续/待无关 UI 测试漂移清理后补证。 |
| 2026-06-14 | TP-M3-S1e responsive/reactive history suppression | 已完成(资源内推导 V1;authored mask 后续) | `TaaResolveParams` 扩展 `responsive_and_reactive` uniform(luma threshold、velocity scale、full-responsive history multiplier、confidence cap);`taa_resolve.wgsl` 用当前/裁剪后历史亮度差与速度推导 responsive rejection,再同时压低 history weight 与下一帧 confidence,避免高对比快速变化像素继续高权重累积;shader-source 断言覆盖 `responsive_and_reactive` 与 `responsive_rejection`。 | `rustfmt --edition 2021 --check` 通过 S1e 触碰 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning);`cargo test -p zircon_runtime taa_resolve_shader_parses_and_declares_history_outputs --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 364s 超时仍在编译 shared lib-test target,残留本目标 cargo/rustc 已停止。 | WGPU offscreen TAA 产品 gate 已由 S1f 打开;材质/透明物 authored reactive mask、旧 `history_resolve` hard-cut、产品级 TAA 收敛/遮挡对拍和过滤单测仍待后续/待测试目标编译窗口恢复后补证。 |
| 2026-06-14 | TP-M3-S1f WGPU TAA 产品 gate 启用 | 已完成(产品入口启用;对拍仍后续) | `capability_summary(...)` 将 `supports_taa` 绑定到 `caps.supports_offscreen`;新增 capability summary 单测源码;AA 统计把 `temporal.taa-resolve` 纳入 `last_anti_alias_graph_executed_pass_count`;`render_product_anti_alias.rs` 新增 `render_product_taa_uses_temporal_resolve_seed_frame_when_requested`,断言请求 TAA 时 product seed 帧走 `temporal.taa-resolve` 而非 FXAA。 | `rustfmt --edition 2021 --check` 通过 S1f 触碰 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning);`cargo test -p zircon_runtime render_product_taa_uses_temporal_resolve_seed_frame_when_requested --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 364s 超时仍在编译 shared lib-test target,残留本目标 cargo/rustc 已停止。 | 旧 `history_resolve` hard-cut、材质/透明物 authored reactive mask、产品级 TAA 收敛/遮挡对拍、RenderDoc 截图与过滤单测结果仍待后续/待测试目标编译窗口恢复后补证。 |
| 2026-06-14 | TP-M4-S1 legacy history resolve 硬切 | 已完成(旧路径删除;产品验收仍后续) | 删除 `BuiltinRenderFeature::HistoryResolve` 与 explicit opt-in 分支、`PostProcessEffectKind::HistoryResolve`、`feature_descriptors/history_resolve.rs`、legacy history resolve executor/noop id、旧 `HISTORY_PREVIOUS/CURRENT/OUTPUT_SCENE_COLOR` 资源名、legacy scene-color history texture/copy/import/alias 路径;quality profile 与测试夹具从 `history_resolve` 命名切到 `temporal_history`;公共 `FrameHistorySlot::SceneColor` 改为 `FrameHistorySlot::TaaSceneColor`。TAA scene-color history 只由 `TemporalHistoryStore`/`temporal.taa-resolve` 管理,SSR 继续使用自己的 temporal history。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;source scan `HistoryResolve|history_resolve|HISTORY_PREVIOUS_SCENE_COLOR|HISTORY_CURRENT_SCENE_COLOR|HISTORY_OUTPUT_SCENE_COLOR|history.scene-color|post.history-resolve|FrameHistorySlot::SceneColor` 在 `zircon_runtime/src` 清零;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning)。 | 材质/透明物 authored reactive mask、历史 pre-jitter hash 基线、产品级 TAA 收敛/遮挡对拍、RenderDoc 截图与过滤单测结果仍待后续/待测试目标编译窗口恢复后补证。 |
| 2026-06-14 | TP-M4-S2 TAA reactive mask 图输入/default clear | 已完成(接口与默认零值;材质 writer 仍后续) | 新增 `TAA_REACTIVE_MASK = "taa.reactive-mask"`、`temporal.taa-reactive-mask-clear` pass/executor、`R8Unorm` graph texture 格式、WGPU clear-only render pass 与 TAA resolve binding 5;`taa_resolve.wgsl` 读取 `taa_reactive_mask_tex`,把 authored mask 与 luma/velocity responsive rejection 取最大值后压低 history weight/confidence;TAA 关闭时 mask pass/resource 随 stack 过滤消失。同步清理 `zircon_runtime/tests` 里残留的 `with_history_resolve(false)` 夹具命名。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;source scan `HistoryResolve|history_resolve|HISTORY_PREVIOUS_SCENE_COLOR|HISTORY_CURRENT_SCENE_COLOR|HISTORY_OUTPUT_SCENE_COLOR|history.scene-color|post.history-resolve|FrameHistorySlot::SceneColor|with_history_resolve` 在 `zircon_runtime/src` 与 `zircon_runtime/tests` 清零;source scan 覆盖 `temporal.taa-reactive-mask-clear|taa.reactive-mask|TAA_REACTIVE_MASK|taa_reactive_mask_tex|load_authored_reactive_mask`;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning)。`cargo check -p zircon_runtime --lib --tests ...` 已越过 `with_history_resolve` 残留后,继续被无关 `virtual_geometry_debug_snapshot_contract.rs` 的 `RenderMeshSnapshot` 缺字段漂移阻塞;全包 `cargo fmt --manifest-path zircon_runtime/Cargo.toml --check --all` 被无关 `asset/pack`/`zircon_export_pack` 格式漂移阻塞。 | 透明 alpha writer 已由 TP-M4-S3 覆盖,显式材质 authored strength 已由 TP-M4-S3b 覆盖;opaque writer、历史 pre-jitter hash 基线、产品级 TAA 收敛/遮挡对拍与 RenderDoc 截图验收仍待后续。 |
| 2026-06-14 | TP-M4-S3 透明物 reactive mask writer V1 | 已完成(透明材质 alpha writer;材质强度由 S3b 补齐) | 新增 `temporal.taa-reactive-mask-mesh` pass/executor,位于 clear 与 `taa-resolve` 之间,读 `SCENE_DEPTH`、写 `TAA_REACTIVE_MASK` load/store;新增 `MeshPassPipelineKind::TaaReactiveMask`、`TaaReactiveMaskPassProcessor`、`taa_reactive_mask` command/indirect/stats 链路;新增 `R8Unorm` mesh pipeline/cache 与 `fs_taa_reactive_mask`,按材质/贴图/vertex tint 采样 base alpha 写 mask,完全透明 texel discard。`PostProcessStackDescriptor` 将 `TAA_REACTIVE_MASK` 视为 TAA initial resource,避免 resolve stack validation 误判缺输入。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;source scan 覆盖 `temporal.taa-reactive-mask-mesh|taa-reactive-mask-mesh|TaaReactiveMask|fs_taa_reactive_mask|ensure_taa_reactive_mask_pipeline`;旧 history scan `with_history_resolve|history_resolve|HistoryResolve|FrameHistorySlot::SceneColor|history.scene-color|post.history-resolve|HISTORY_PREVIOUS_SCENE_COLOR` 在 `zircon_runtime/src` 与 `zircon_runtime/tests` 清零;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning);`cargo test -p zircon_runtime taa_reactive_mask --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 4 个过滤测试;`cargo test -p zircon_runtime taa_resolve_compiles_temporal_history_pass_when_taa_stack_is_effective --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 1 个过滤测试。 | Opaque material flag writer、历史 pre-jitter hash 基线、产品级 TAA 收敛/遮挡对拍与 RenderDoc 截图验收仍待后续。 |
| 2026-06-14 | TP-M4-S3b 显式材质 reactive strength | 已完成(材质 authored strength;opaque writer 仍后续) | `StandardMaterialDescriptor`、material control/asset、`MaterialRuntime`/capture seed 与 resource streamer 接入 `taa_reactive_mask_strength`;字段默认 0,校验 `0..=1`,作为材质拥有属性不进入 shader custom property overrides。标准材质 uniform 扩展到 144 bytes,`data8.x` 写入该强度;`fs_taa_reactive_mask` 写 `max(sampled_base_alpha, data8.x)`。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;source scan 覆盖 `taa_reactive_mask_strength|material_properties.data8|fs_taa_reactive_mask|GPU_MATERIAL_UNIFORM_MIN_SIZE: usize = 144`;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning);`cargo test -p zircon_runtime material_owned_taa_reactive_mask_strength --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 2 个过滤测试;`cargo test -p zircon_runtime taa_reactive_mask --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 6 个过滤测试。 | Opaque material flag writer、历史 pre-jitter hash 基线、产品级 TAA 收敛/遮挡对拍与 RenderDoc 截图验收仍待后续。 |
| 2026-06-14 | TP-M4-S3c opaque material reactive flag writer | 已完成(非透明材质 authored mask writer;产品验收仍后续) | `PendingMeshDraw`、`MeshDraw` 与 `MeshBatchRef` 携带 clamped `taa_reactive_mask_strength`;`TaaReactiveMaskPassProcessor` 只为 visible opaque/alpha-mask 且强度非零的 batch 生成 `MeshPassPipelineKind::TaaReactiveMaterialMask`;新增 `fs_taa_reactive_material_mask` 与对应 `R8Unorm` pipeline/cache map,透明 batch 继续走 `TaaReactiveMask` alpha-sampling path。static cached batch 仍会为 reactive-material mask 追加 uncached postprocess command。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过(65 个既有 warning);`cargo test -p zircon_runtime taa_reactive_mask --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 6 个过滤测试;`cargo test -p zircon_runtime mesh_pass_command_buffers --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 4 个过滤测试。 | 历史 pre-jitter hash 基线、产品级 TAA 收敛/遮挡对拍与 RenderDoc 截图验收仍待后续。 |
| 2026-06-14 | TP-M4-S4a TAA product 静态 history 基线 | 已完成(静态空场景多帧基线;动态遮挡仍后续) | `render_product_anti_alias.rs` 新增 `render_product_taa_static_empty_scene_history_stays_stable_after_seed_frame`;同一 viewport 连续提交 seed/history/history 三帧 `AntiAliasSettings::taa()` 空场景 product extract,共享 `assert_taa_resolve_product_stats(...)` 断言三帧都保持 TAA requested/effective、执行 `temporal.taa-resolve` executor 且不回退 FXAA,并逐像素比较后两帧 captured RGBA 稳定。产品验证同时暴露并修复 WGPU 资源边界:`POST_PROCESS_REQUIRED_SAMPLED_TEXTURES_PER_SHADER_STAGE = 21` 对齐 `post_process` bind group layout,`R8Unorm` reactive mask、`R16Float` 与 `Rg16Float` transient textures 不再请求非法 `STORAGE_BINDING`。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;`cargo test -p zircon_runtime offscreen_device_limits --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 2 个过滤测试;`cargo test -p zircon_runtime post_process_layout_sampled_texture_count_matches_device_request_limit --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614 --message-format short --color never` 通过 1 个过滤测试;`cargo test -p zircon_runtime texture_formats --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 2 个过滤测试;`cargo test -p zircon_runtime render_product_taa_uses_temporal_resolve_seed_frame_when_requested --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 1 个过滤测试;`cargo test -p zircon_runtime render_product_taa_static_empty_scene_history_stays_stable_after_seed_frame --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 1 个过滤测试;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过(65 个既有 warning)。 | 历史 pre-jitter hash 基线、动态场景 TAA 收敛/遮挡对拍、RenderDoc 截图验收仍待后续。 |
| 2026-06-14 | TP-M4-S4b TAA product 动态遮挡收敛基线 | 已完成(动态遮挡产品基线;RenderDoc 仍后续) | `render_product_anti_alias.rs` 新增 `render_product_taa_dynamic_occlusion_change_converges_after_history_seed`;同一 viewport 先提交可见目标帧,再连续提交三帧遮挡墙+目标帧,逐帧断言 TAA requested/effective、执行 `temporal.taa-resolve` 且不回退 FXAA,并要求 visible->occluded 过渡帧差大于 0 且重复遮挡帧差收敛低于过渡帧差。为打通真实 mesh+velocity+TAA 产品路径,mesh pass 在缺少图内 light-grid writer 时改用已有 disabled light-grid fallback,`LightGridParams` CPU uniform 布局显式对齐 WGSL 128 bytes,DepthPrepass 阶段向 `velocity-object` 注入 mesh pipeline/cache 与 streamer。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;`git diff --check --` 通过本切片 Rust/文档/会话文件(仅 LF/CRLF 提示);`cargo test -p zircon_runtime light_grid_params_cpu_layout_matches_wgsl_uniform_size --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 1 个过滤测试;`cargo test -p zircon_runtime render_product_taa_dynamic_occlusion_change_converges_after_history_seed --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 1 个过滤测试;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过(65 个既有 warning)。 | 历史 pre-jitter hash artifact 仍未找到;RenderDoc 截图验收与更复杂透明/粒子动态场景仍待后续。 |
| 2026-06-14 | TP-M4-S4c TAA product authored reactive mask writer 基线 | 已完成(材质 authored writer 产品统计面;RenderDoc 仍后续) | `RenderStats.last_mesh_taa_reactive_mask_command_count` 暴露已有 `PreparedMeshQueueStats.taa_reactive_mask_command_count`;submit stats 写回该计数,product diagnostics 新增 `render.mesh.queue.taa_reactive_mask_command_count`;`render_product_anti_alias.rs` 新增 `render_product_taa_authored_reactive_mask_records_material_writer_path`,注册两个真实 `MaterialAsset`:`taa_reactive_mask_strength = 0.0` 的 opaque 材质提交后 command count 为 0,`taa_reactive_mask_strength = 1.0` 的 opaque 材质提交后 command count 为 1,并断言 clear/mesh/resolve executor 都执行且材质 ready/fallback 统计正常。为解除 lib-test 编译阻塞,同步给无关 UI 测试 `pointer_popup_routes.rs` 补了 `UiWidgetBehavior` 导入。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;`cargo test -p zircon_runtime render_product_diagnostics_record_taa_reactive_mask_queue_count --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 1 个过滤测试;`cargo test -p zircon_runtime render_product_taa_authored_reactive_mask_records_material_writer_path --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过 1 个过滤测试;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-m3-s1-0614b --message-format short --color never` 通过(65 个既有 warning)。 | 历史 pre-jitter hash artifact 仍未找到;RenderDoc 截图验收与更复杂透明/粒子动态场景仍待后续。 |
| 2026-06-14 | TP-M4-S4d TAA product transparent reactive mask writer 基线 | 已完成(透明 alpha writer 产品统计面;RenderDoc 仍后续) | `render_product_anti_alias.rs` 新增 `render_product_taa_transparent_reactive_mask_records_alpha_writer_path`;测试注册真实 Blend `MaterialAsset`,将产品 extract 的 `GeometryPhaseInput` 显式设为 `RenderMaterialAlphaMode::Blend`,提交后断言 TAA clear/mesh/resolve executor 执行、材质 ready/fallback 正常、opaque/alpha-mask draw count 为 0、transparent draw count 为 1,且透明 alpha writer 生成 1 条 `last_mesh_taa_reactive_mask_command_count`。同时补齐 `project_io.rs` 中拆分到 `references.rs` 后漏导入的 `reference_for_handle`,解除 lib-test 编译阻塞。 | `cargo test -p zircon_runtime render_product_taa_transparent_reactive_mask_records_alpha_writer_path --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 1 个过滤测试(44 个既有 warning);此前同测试先暴露 Blend 材质缺 phase input 对齐导致 command count 为 0,已通过显式 Blend `GeometryPhaseInput` 修正;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);`rustfmt --edition 2021 --check`、scoped `git diff --check` 与文档/会话尾随空白扫描通过。 | 历史 pre-jitter hash artifact 仍未找到;RenderDoc 截图验收与更复杂粒子动态场景仍待后续。 |
| 2026-06-14 | TP-M4-S4e TAA product particle transparent pass 基线 | 已完成(粒子透明 pass 顺序/贡献产品基线;RenderDoc 仍后续) | `render_product_anti_alias.rs` 新增 `render_product_taa_particle_transparent_pass_contributes_before_resolve`;测试用 `particle_render_feature_descriptor()` 注册 `particle.transparent` executor,分别提交空 TAA 帧与含 1 个 `RenderParticleSpriteSnapshot` 的 TAA 帧,断言两帧都执行 TAA resolve、`particle.transparent` 位于 `temporal.taa-resolve` 前、TAA-only 不增加 `last_particle_velocity_missing_sprite_count`,且粒子帧相对空帧产生非零 RGBA delta。 | 首轮同测试暴露空场景在启用粒子 feature/profile 时也会调度 `particle.transparent`,已把期望从“空场景 pass count 为 0”修正为“空/粒子帧均执行粒子 pass,贡献由 RGBA delta 证明”;`cargo test -p zircon_runtime render_product_taa_particle_transparent_pass_contributes_before_resolve --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 1 个过滤测试(44 个既有 warning);`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);`rustfmt --edition 2021 --check`、scoped `git diff --check` 与文档/会话尾随空白扫描通过;RenderDoc MCP 当前无运行实例。 | 历史 pre-jitter hash artifact 仍未找到;RenderDoc 截图验收与粒子 previous-state velocity writer/更复杂动态场景仍待后续。 |
| 2026-06-15 | TP-M1-S9 粒子 previous-state velocity 诊断收敛 | 已完成(诊断收敛;writer 仍后续) | 新增 `RenderParticlePreviousSpriteSnapshot` 与 `ParticleExtract.previous_sprites`;`ParticleExtract::previous_state_sprite_count()`/`missing_previous_state_sprite_count()` 按 entity 匹配 current sprite 与 previous-state rows,并逐个消费 previous-state row,避免同一 entity 多 sprite 时一个 previous row 被重复计数;`FrameSubmissionContext` 携带 current particle sprite count 与 matched previous-state count;`update_base_stats(...)` 在 motion blur/SSR 请求 reconstructed velocity 且 `particle.transparent` 执行时,把 `last_particle_velocity_missing_sprite_count` 收敛为 current minus matched previous-state。`render_particles.rs` 的当前 scene JSON 路径暂以空 `previous_sprites` 保持“不伪造 previous state”。产品测试新增 motion-blur/TAA 粒子帧:无 previous-state 时 missing=1,带 `RenderParticlePreviousSpriteSnapshot::from_current(...)` 时 missing=0,且 particle pass 仍在 TAA resolve 前执行。 | `cargo test -p zircon_runtime previous_state --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 4 个过滤测试:`particle_extract_counts_previous_state_by_entity`、`particle_extract_consumes_duplicate_entity_previous_state_once_per_row`、`particle_velocity_gap_excludes_sprites_with_previous_state`、`render_product_particle_previous_state_suppresses_velocity_gap_stats`;`cargo test -p zircon_runtime render_product_particle_previous_state_suppresses_velocity_gap_stats --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 前台复跑通过 1 个产品测试;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);`rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;scoped `git diff --check` 通过(仅 LF/CRLF 提示);文档/会话尾随空白扫描通过;RenderDoc MCP 当前无运行实例。 | 仍未实现粒子 `scene-velocity` writer、稳定多 sprite identity、previous billboard basis 和 RenderDoc 截图验收;历史 pre-jitter hash artifact 仍未找到。 |
| 2026-06-15 | TP-M1-S10 粒子 `scene-velocity` writer V1 | 已完成(V1 writer;像素/RenderDoc 仍后续) | `ParticleRenderer` 新增第二条 WGPU `Rg16Float` velocity pipeline;`particle_velocity.wgsl` 使用 current/previous unjittered scene matrices 写 xy screen velocity;`build_particle_velocity_vertices(...)` 按 entity 为 current sprite 消费一条 previous row,展开 current/previous billboard corner 对;`RenderPassGpuExecutionContext::record_particle_velocity_to_resource(...)` 拆入 `gpu/particle.rs`;`particle.velocity` 注册为 built-in executor 并在 pluginized descriptor 中写 `SCENE_VELOCITY`;新增 `render_product_particle_velocity.rs` 验证 `particle.velocity` 位于 `particle.transparent` 与 `temporal.taa-resolve` 之前,且缺 previous-state 时 writer 安全 no-op。 | `cargo test -p zircon_runtime particle_velocity_vertices --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 3 个过滤测试;`cargo test -p zircon_runtime render_product_particle_velocity --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 2 个产品过滤测试;移动粒子 GPU 入口到子模块后同产品过滤测试复跑通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);`gpu.rs` 拆分后降至 856 行;RenderDoc MCP 当前无运行实例。 | V1 仍用 entity-only previous row 匹配;scene JSON 路径仍不生产 previous state;多 sprite 稳定 identity、previous billboard basis/expanded-corner ownership、velocity surface 像素读回、RenderDoc 截图验收与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S11 粒子 stable sprite identity 匹配 | 已完成(合约实现;过滤测试/生产 check 被无关 UI 编译错误阻塞) | `RenderParticleSpriteIdentity { entity, stable_sprite_key }` 新增为 current/previous 粒子 snapshot 的共享匹配键;`RenderParticleSpriteSnapshot` 与 `RenderParticlePreviousSpriteSnapshot` 均携带 `stable_sprite_key`,且 `from_current(...)` 复制该键;`ParticleExtract::previous_state_sprite_count()` 与 `build_particle_velocity_vertices(...)` 改按 identity 消费 previous rows,避免 same-entity 多 sprite 使用非零 key 时交叉匹配;`World::collect_render_particles(...)` 对当前 JSON sprite 读取 `stable_sprite_key`/`sprite_key`,并在排序 tie-break 中使用 key;产品/构建器 fixture 改为显式 key。 | `rustfmt --edition 2021 --check` 通过本切片触碰 Rust 文件;构造点扫描确认 `RenderParticleSpriteSnapshot`/`RenderParticlePreviousSpriteSnapshot` 显式构造均带 `stable_sprite_key`;scoped `git diff --check` 与文档尾随空白扫描通过(仅 LF/CRLF 提示);`cargo test -p zircon_runtime stable_sprite_key --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 编译 shared lib-test 时被无关 UI 错误阻塞:`zircon_runtime\src\ui\surface\render\command_palette.rs:311` `E0282` type annotations needed;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 同样被该无关 UI 错误阻塞;本轮超时残留的 target-dir cargo/rustc 进程已终止。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;scene JSON/current extractor 仍不生成 `previous_sprites`;renderer-owned previous-state population、previous billboard basis/expanded-corner ownership、velocity surface 像素读回、RenderDoc 截图验收与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S12 renderer-owned 粒子 previous-state roll | 已完成(代码实现;过滤测试/生产 check 被无关 UI 编译错误阻塞) | `ViewportRecord` 新增 `particle_previous_sprites` owner 与访问器;`submit_frame_extract`/`present_frame_extract`/`submit_runtime_frame` 成功后通过 `update_particle_previous_state_after_success(...)` 把 current sprites 滚动为 previous rows;`resolve_viewport_record_state(...)`/`FrameSubmissionContext` 携带 viewport-owned rows,`build_frame_submission_context(...)` 仅在输入 extract 未显式提供 previous rows 时注入;`build_runtime_frame(...)` 与 direct runtime-frame path 将 effective previous rows 写入最终 `ViewportRenderFrame`;`render_product_particle_velocity_writer_uses_renderer_owned_previous_state_on_second_frame` 覆盖第二帧无 authored previous-state 时 missing 归零并执行 velocity-before-TAA 顺序。 | `rustfmt --edition 2021 --check` 通过 S12 触碰 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 被无关 UI 错误阻塞:`zircon_runtime\src\ui\surface\render\command_palette.rs:311` `E0282` type annotations needed;`cargo test -p zircon_runtime successful_submit_records_particle_previous_state_for_next_frame --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 同样被该无关 UI 错误阻塞,未进入过滤测试执行;无残留本目标目录 cargo/rustc 进程。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;previous billboard basis/expanded-corner ownership、velocity surface 像素读回、RenderDoc 截图验收与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S13 粒子 previous billboard basis ownership | 已完成(代码/验证通过;像素/RenderDoc 仍后续) | 新增 `RenderParticleBillboardBasisSnapshot` 并把 `RenderParticlePreviousSpriteSnapshot.billboard_basis` 作为可选 previous-row 数据面公开;renderer-owned `update_particle_previous_state_after_success(...)` 在 submit/present/direct runtime-frame 成功后把提交帧相机 `right/up` 写入 previous rows;`build_particle_velocity_vertices(...)` 生成 previous corners 时优先使用 previous row 存储的 basis,显式 legacy previous rows 无 basis 时仍 fallback 到 current camera basis;`RenderParticlePreviousSpriteSnapshot::from_current(...)` 保持显式行兼容,新增 `from_current_with_billboard_basis(...)` 供 renderer-owned roll 使用。 | `rustfmt --edition 2021 --check` 通过 S13 触碰 Rust 文件;构造点扫描确认所有显式 `RenderParticlePreviousSpriteSnapshot` 构造均带 `billboard_basis`;`cargo test -p zircon_runtime previous_billboard_basis --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 1 个过滤测试;直接运行 `D:\cargo-targets\zircon-runtime-temporal-s4d-0614\debug\deps\zircon_runtime-5d2828c2001649f6.exe graphics::runtime::render_framework::submit_frame_extract::submit::update_particle_previous_state::tests::successful_submit_records_particle_previous_state_for_next_frame --exact --nocapture` 通过 1 个过滤测试;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning)。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;velocity surface 像素读回、RenderDoc 截图验收、复杂动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S14 粒子 velocity surface readback evidence | 已完成(测试读回/产品验证通过;RenderDoc 仍后续) | 新增 `RenderSceneVelocityReadbackReport`,并把 graph-owned `scene-velocity` `Rg16Float` target 的 test-build raw readback 接入 `RenderGraphExecutionRecord`、`SceneRenderer` accessor 与 `RenderStats.last_scene_velocity_readback_report`;compiled-scene 在 graph command submit 后、transient backings 释放前读回 velocity surface,产品测试新增 `render_product_particle_velocity_writer_writes_nonzero_scene_velocity_pixels`,断言 matched 粒子帧 available、尺寸/字节数匹配并产生非零 raw velocity pixels。 | `rustfmt --edition 2021 --check` 通过 S14 触碰 Rust 文件;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);`cargo test -p zircon_runtime execution_record_preserves_scene_velocity_readback_report --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 1 个过滤测试;直接运行 `D:\cargo-targets\zircon-runtime-temporal-s4d-0614\debug\deps\zircon_runtime-5d2828c2001649f6.exe graphics::tests::render_product_particle_velocity::render_product_particle_velocity_writer_writes_nonzero_scene_velocity_pixels --exact --nocapture` 通过 1 个产品测试;直接运行 `D:\cargo-targets\zircon-runtime-temporal-s4d-0614\debug\deps\zircon_runtime-5d2828c2001649f6.exe graphics::tests::render_product_particle_velocity --nocapture` 通过 4 个产品测试;`cargo check -p zircon_runtime --tests ...` 被无关 `zircon_runtime/tests/virtual_geometry_debug_snapshot_contract.rs` 的 `RenderMeshSnapshot` 字段漂移阻塞;RenderDoc MCP 当前无运行实例。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收、复杂动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S15 renderer-owned 粒子 velocity readback baseline | 已完成(二帧产品读回通过;RenderDoc 仍后续) | `render_product_particle_velocity_writer_uses_renderer_owned_previous_state_on_second_frame` 复用 S14 的 `RenderSceneVelocityReadbackReport` 断言:第一帧成功提交后由 `ViewportRecord` 滚动 previous particle rows,第二个移动粒子帧不提供 authored previous-state 也能让 `particle.velocity` 使用 renderer-owned previous rows 写入 graph `scene-velocity`;测试同时锁住 missing sprite count 归零、读回 available、尺寸/字节数正确与非零 raw velocity pixels。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/tests/render_product_particle_velocity.rs` 通过;`cargo test -p zircon_runtime render_product_particle_velocity --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 4 个过滤产品测试(44 个既有 warning),覆盖 writer 顺序、缺 previous-state no-op、显式 previous-state 非零读回与 renderer-owned 二帧非零读回。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收、更复杂动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S16 same-entity keyed 粒子 multi-sprite readback baseline | 已完成(多 sprite 产品读回通过;RenderDoc 仍后续) | `render_product_particle_velocity_writer_matches_same_entity_renderer_owned_sprites_by_key_on_second_frame` 新增同一 entity 下两个非零 `stable_sprite_key` 粒子的二帧产品路径;首帧无 authored previous rows 时 missing=2,成功提交后 `ViewportRecord` 滚动两个 previous rows,第二个移动帧通过 stable identity 匹配到 renderer-owned previous rows,missing=0,并继续通过 `RenderSceneVelocityReadbackReport` 断言 raw `scene-velocity` 非零。 | `cargo test -p zircon_runtime render_product_particle_velocity --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 5 个过滤产品测试(44 个既有 warning),新增覆盖 same-entity nonzero-key multi-sprite renderer-owned 二帧读回;`rustfmt --edition 2021` 已格式化测试文件。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收、更大规模/未显式 key 动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S17 key=0 anonymous stream ambiguity diagnostics | 已完成(诊断/产品验证通过;RenderDoc 仍后续) | 新增 `ParticleExtract::anonymous_stream_ambiguity_sprite_count()`、`FrameSubmissionContext::particle_anonymous_stream_ambiguity_sprite_count()`、`RenderStats.last_particle_velocity_anonymous_stream_ambiguity_count` 与 runtime diagnostic `render.particle.velocity.anonymous_stream_ambiguity_count`;统计仅在 reconstructed velocity 请求且 `particle.transparent` 执行时发布,保留 key=0 FIFO 兼容语义,但 same-entity 多个 key=0 sprite 不再静默伪装成稳定 identity。 | 首轮 `cargo test -p zircon_runtime anonymous_stream --lib ...` 发现 diagnostics tag 顺序断言与 store 排序不一致,修正后通过 3 个过滤测试;`cargo test -p zircon_runtime render_product_particle_velocity --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 6 个过滤产品测试(44 个既有 warning),新增覆盖 key=0 same-entity ambiguity=2;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);rustfmt check 通过。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收、更大规模动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S18 keyed multi-sprite three-frame dynamic baseline | 已完成(产品验证通过;RenderDoc 仍后续) | 新增 `render_product_particle_velocity_writer_rolls_keyed_multi_sprite_motion_across_three_frames`:同一 viewport 连续提交四个 same-entity nonzero-key 粒子;首帧缺 previous rows 时 missing=4 且 ambiguity=0,随后两个移动帧均使用 renderer-owned previous rows 保持 missing=0/ambiguity=0,并通过 `RenderSceneVelocityReadbackReport` 断言 raw `scene-velocity` 非零,覆盖 previous-state 连续滚动而非只覆盖第二帧。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/tests/render_product_particle_velocity.rs` 通过;`cargo test -p zircon_runtime render_product_particle_velocity --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 7 个过滤产品测试(44 个既有 warning),新增三帧动态基线;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);RenderDoc MCP 当前无运行实例。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收、压力规模动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S19 previous-palette gate module split | 已完成(边界收束/过滤测试通过;RenderDoc 仍后续) | 新增 `build_mesh_draws/build/previous_skinned_palette.rs`,把 previous skinned-palette 选择、`skinned_joint_palette_state_for_pending_draw(...)` 与 CPU-morphed morph-shape compatibility 单测从 `build.rs` 抽出;`build.rs` 从 1045 行降到 902 行,仅保留 draw orchestration 调用点;S23 changing-shape previous-source policy 已落在该模块而不是继续堆进 orchestration 文件。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/build.rs zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/mod.rs zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/previous_skinned_palette.rs` 通过;`cargo test -p zircon_runtime previous_skinned_joint_palette --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 2 个过滤测试(44 个既有 warning);`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning)。 | S23 已关闭 CPU-morphed changing-shape previous-source velocity;S22 已关闭 key=0 hard enforcement/HUD producer migration;RenderDoc 截图验收、压力规模动态粒子场景与历史 pre-jitter hash artifact 仍待后续。 |
| 2026-06-15 | TP-M1-S20 keyed stress-field particle velocity product baseline | 已完成(源码/生产 check/产品过滤测试通过;RenderDoc 仍后续) | 新增 `render_product_particle_velocity_writer_rolls_keyed_stress_field_motion`:同一 viewport 首帧提交 32 个 same-entity nonzero-key 粒子,第二帧整体位移;首帧锁住 missing=32/ambiguity=0,第二帧锁住 renderer-owned previous rows 后 missing=0/ambiguity=0 并要求 `RenderSceneVelocityReadbackReport` 非零。当前工作区 `ime_context.rs` 已覆盖 `UiInputEvent::ToastTimer(_)` 非刷新输入法上下文路径,解除共享 lib-test 编译阻塞。 | `rustfmt --edition 2021 --check zircon_runtime/src/graphics/tests/render_product_particle_velocity.rs zircon_runtime/src/ui/surface/input/editable_text/ime_context.rs` 通过;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning);`cargo test -p zircon_runtime render_product_particle_velocity --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过 8 个过滤产品测试(44 个既有 warning),新增覆盖 32-sprite keyed stress-field motion。 | S22 已关闭 key=0 hard enforcement/HUD producer migration;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收仍待后续。 |
| 2026-06-15 | TP-M2/S21 historical pre-jitter artifact recovery audit | 已完成(仓库内不可恢复;当前 Off 对拍为验收基线) | 审计原设计中的 `render_product_temporal_off_matches_pre_jitter_baseline` 与 TP-M2-S1 前 hash 产物。当前 checkout 的 docs/`.codex`/examples/graphics tests 只保留计划描述;限定 git history 显示 `0559abbc` 仅在 Plan 06 文本中提出该测试名和 hash 要求,未提交测试实现、golden file 或 hash artifact。 | 文本搜索覆盖 `docs`、`.codex/plans`、`.codex/sessions`、`examples` 与 `zircon_runtime/src/graphics/tests`;`git grep`/`git log -S` 限定渲染计划、render product submit 文档、camera 文档、TAA 文档和 anti-alias 产品测试确认没有可恢复产物。 | 当前仓库完成判据改为 `render_product_temporal_off_matches_anti_alias_feature_disabled_product` 的 Off-path product parity;若外部 artifact 后续出现可作为额外补证,但不再阻塞当前 Plan 06 方案。 |
| 2026-06-15 | TP-M1/S22 key=0 ambiguous stream hard enforcement and HUD producer migration | 已完成(策略拆分/源码/生产 check/过滤测试通过;RenderDoc 仍后续) | `ParticleExtract` previous-state/ambiguity 策略从 929 行 `frame_extract.rs` 拆入 `frame_extract/particle_extract_policy.rs`;same-entity 多个 key=0 current sprites 被视为不可追踪匿名流,不再匹配 previous rows,missing count 维持为歧义 sprite 数。`build_particle_velocity_vertices(...)` 跳过歧义匿名 current/previous rows,`update_particle_previous_state_after_success(...)` 不再缓存这些 rows;`World::collect_render_particles(...)` 的 world-HUD bar 背景/填充 producer 改为按 bar index 派生非零 stable keys。产品测试新增 `render_product_particle_velocity_rejects_anonymous_key_multi_sprite_previous_state_on_second_frame`,锁住 renderer-owned second-frame 不会把 key=0 多 sprite 恢复为有效 velocity。 | `rustfmt --edition 2021 --check` 通过 S22 触碰 Rust 文件;首次 `cargo test -p zircon_runtime particle_extract --lib ...` 工具超时但后台编译自然结束,同命令重跑通过 4 个过滤测试;`cargo test -p zircon_runtime particle_velocity_vertices --lib ...` 通过 6 个过滤测试;`cargo test -p zircon_runtime successful_submit --lib ...` 通过 3 个过滤测试;宽泛 `world_hud_bar` 过滤命中既有 vampire 示例资产缓存读取失败,精确 `cargo test -p zircon_runtime scene::world::render_particles::tests::world_hud_bar --lib ...` 通过 2 个过滤测试;`cargo test -p zircon_runtime render_product_particle_velocity --lib ...` 通过 9 个过滤产品测试;`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(70 个既有 warning)。 | key=0 多 sprite hard enforcement 已完成;S23 已关闭 CPU-morphed changing-shape previous-source velocity;RenderDoc 截图验收仍待后续。 |
| 2026-06-15 | TP-M1/S23 CPU-morphed changing-shape previous source velocity | 已完成(源码/生产 check/过滤测试通过;RenderDoc 仍后续) | `GpuScene` 新增 current/previous skinned GPU source map 与成功提交后 roll;mesh draw sync 为 `PendingSkinnedGpuSource::CpuMorphed` 缓存当前 morphed source,在下一帧 morph-shape signature 变化时选择 previous source + previous palette。velocity pipeline 增加第二 vertex buffer slot,`GpuMeshVertex::previous_position_layout()` 把上一帧 source position 绑定到 `@location(8) previous_position`;draw command/replay 的 geometry bind key 纳入 previous geometry,避免同 current mesh 不同 previous mesh 时跳过绑定。 | `rustfmt --edition 2021` 通过本切片 Rust 文件;`git diff --check` 对触碰源码/WGSL 通过(仅 LF/CRLF 提示);`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-temporal-s4d-0614 --message-format short --color never` 通过(既有 warning);`cargo test -p zircon_runtime previous_skinned --lib ...` 通过 6 个过滤测试;`velocity_mesh_pipeline_declares_previous_position_vertex_slot`、`gpu_mesh_previous_position_layout`、`mesh_batch_ref_attaches_previous_geometry_only_to_velocity_commands`、`velocity_geometry_bind_key_includes_previous_geometry_slot` 各通过 1 个过滤测试。 | CPU-morphed changing-shape previous source velocity 已关闭源码/单测缺口;RenderDoc 截图验收仍待后续。 |
| 2026-06-15 | TP-M4-S1 legacy history-resolve consumer residue sweep | 已完成(消费者残留清零;重型 Cargo 待后续) | `zircon_editor/src/tests/editing/state.rs` 的 editor viewport capture fixture 从删除后的 `RenderQualityProfile::with_history_resolve(false)` 迁移到 `with_temporal_history(false)`,保留测试关闭 history 链路的意图,并避免恢复 legacy `HistoryResolve` / `history_resolve` 兼容入口。同步刷新 TAA/submit 文档中仍描述旧 HistoryResolve opt-in 的段落。 | `rustfmt --edition 2021 --check zircon_editor\src\tests\editing\state.rs` 通过;源码扫描 `with_history_resolve|HistoryResolve|history_resolve` 覆盖 `zircon_runtime/src` 与 `zircon_editor/src` 后无命中;相关文档扫描剩余命中均为历史迁移记录或当前 `taa.history.*` 资源名说明;路径限定 `git diff --check` 仅报告既有 LF/CRLF 提示。 | 重新跑 editor `component_showcase`/export wizard 等曾被该 API drift 阻塞的重型 lib-test 需要等活跃 UI/plugin lanes 稳定;本轮检查到仍有 cargo/rustc 进程运行,未额外启动重型 Cargo;Plan 06 RenderDoc 捕获验收仍待后续。 |

## 风险与回退

- 透明物 velocity 缺失导致 TAA 拖影:V1 已用资源内推导 responsive/reactive suppression + 透明材质 alpha reactive mask writer + 显式材质 authored strength + opaque/alpha-mask material flag writer 降低高对比材质拖影;静态空场景 product history、动态遮挡 product 收敛、authored material/transparent alpha reactive-mask writer 产品统计、粒子透明 pass 产品顺序/贡献基线、previous-state 粒子 velocity 诊断收敛、V1 粒子 `scene-velocity` writer、stable sprite identity、renderer-owned previous-state roll、renderer-owned previous billboard basis、velocity surface 测试读回、renderer-owned 二帧动态读回基线、same-entity nonzero-key multi-sprite 读回基线、keyed multi-sprite 三帧动态基线、32-sprite keyed stress field 产品基线、key=0 anonymous ambiguity 诊断、key=0 hard enforcement、world-HUD producer nonzero-key 迁移、CPU-morphed changing-shape previous source velocity 与 WGPU resource-limit/texture-usage/light-grid ABI gate 已补,RenderDoc 证据仍后续。
- jitter 波及面大(一切投影矩阵消费方):TP-M2 用全量 render_product 对拍守护,"关闭 TAA 时逐像素一致"是硬验收线。
- 上采样类 TAA(TSR/STP 风格)不在本计划:接口预留 resolve 输入输出分辨率不等的可能,实施另立计划。
