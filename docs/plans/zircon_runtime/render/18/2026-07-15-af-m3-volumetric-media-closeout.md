# Render 18 AF-M3 volumetric media closeout

Plan: docs/plans/zircon_runtime/render/18-advanced-lighting-features.md
Milestone: M3
Status: testing_in_progress
Date: 2026-07-15

## Scope delivered

- Added scene/project authoring for global volumetric-fog settings and per-light volumetric participation, including serde-default compatibility and project-asset round trips.
- Extracted local fog media from `PostProcessVolumeComponent + ColliderComponent`, preserved render-layer masks, and filtered local media against the active camera mask before GPU upload.
- Separated post-process `volume_mask` from volumetric-media `culling_mask`: local fog follows the union of the selected base camera and referenced overlay camera render layers, while ordinary post-process overrides continue to follow volume layers. Each camera executor still applies its own final layer filter. Local volumetric profiles no longer enter the global VolumeEvaluator, and degenerate/non-finite fog bounds are rejected before upload.
- Extended `AdvancedLightingExtract` with deterministic volumetric-light IDs and packed participation into `GpuLightData.cookie_misc.z`; light-scatter WGSL rejects lights that do not participate.
- Added the `VOLUMETRIC_FOG` shader feature bit, pipeline-key propagation, and pass-specialized enabled/disabled sources for forward, deferred, and sky pipelines.
- Corrected the shared shadow scene bind group to bind default SH9 data at binding 6, matching the seven-entry environment scene layout used by real WGPU product execution.
- Reconciled the volumetric product fixture with `ProjectAssetManagerAccess` and corrected the Shader IDE dependency expectation now that volumetric code is pass-specialized.
- Replaced absolute-fragment-coordinate transmission sampling with viewport-local UV reconstructed from `scene.view_proj * world_position`; added Naga validation for forward and sky feature-enabled/disabled variants.

## Fresh managed validation

- Managed Runtime focused job passed `advanced_lighting`: 78 passed, 0 failed, 8 ignored.
- Managed Runtime exact Shader IDE dependency test passed: 1 passed, 0 failed.
- Managed Runtime shadow scene-bind-group WGPU regression job `4034b57165444f9298cfafe5065a7cc0` passed: 1 passed, 0 failed, 8120 filtered out. The test creates the seven-binding scene group inside a WGPU validation error scope.
- The previous post-fixture plugin run passed 8 non-ignored tests with 1 ignored product exporter.
- A fresh plugin rerun job `9a58dfbaee3c4d61a7dd7f80f9b61c11` was blocked before plugin compilation by 14 foreign Text03 `Ui*` to `Text*` type-cutover errors. Render18 does not own or absorb those paths; the failure was synchronized to the coordinator for the active Frameworks05/Text03 session.
- A broad `zircon_runtime` integration build remains bounded by the clean-HEAD Shader03 `MaterialAsset::from_toml_str` and `AssetReference` contract drift. AF-M3 does not claim a broad workspace pass.
- Independent source review reported 0 Critical, 4 Important, and 2 Minor findings. The first post-fix review reduced this to one Important camera-stack layer-union gap; after the camera-stack source/test remediation, the final independent re-review closed at 0 Critical, 0 Important, and 0 Minor.
- Current managed Runtime rerun job `f9e4addefebd4b9f9ef6915d9e51cff8` exited 101 before Render18 tests because foreign `UiBindingExpression::ControlPropRef` was added without the corresponding exhaustive arm in `ui/template/asset/binding/validation.rs:358`. The coordinator routed this exact gate to `plugins05-control-prop-binding-ref-20260715`; Render18 did not edit either owner path.
- Managed graphics-only rerun job `b34eeebc14034295b0bd2ece23c0624f` compiled the `zircon_runtime` production library, then exited 101 before Render18 unit execution on foreign Text test drift (`Result<Vec<_>>` call sites, missing `layout_fallbacks`) and an integration test that assumes the disabled `ui` feature. This job is attached to the existing Frameworks05/Text03 Render18 retest gate and is not recorded as a test pass.
- After the routed Shader03/Plugins05/Text03 dependencies landed, managed Windows job `87b0728d17194711a2de95b4179455be` completed with exit code 0 on the current source. The exact `cargo test -p zircon_runtime render_volumetric_light_scatter --locked` filter passed 8 tests, failed 0, and filtered 8,169; this includes the finite ambient-radiance sum, unchanged 288-byte uniform ABI, participation marker, shadowed-froxel ambient floor, LightGrid/ShadowAtlas reuse, and integration consumer contracts.
- Managed Windows reservation/job/command-bound run `f1c822b416184a778bb07b064e246f5a` (run `325eb7c3be174d5c8835fa13215742c9`) completed with exit code 0 on the current source. The broader `cargo test -p zircon_runtime render_volumetric --locked` filter passed 28 tests, failed 0, ignored 0, and filtered 8,149 in the Runtime unit binary; the remaining integration binaries had no matching tests and exited green. The coordinator then released the job with no live process IDs.
- Managed GPU reservation `9124e8fb75964c1f92f7278fa2a62da8` bound the exact DX12 product command to job `fa38ad298d534d16b9b6c17678108464`, target `E:\\cargo-targets\\zircon-engine\\render18-af-m3-plugin`, and compatibility key `fd9ba63b...`. The run reached the managed supervisor but exited 101 before compilation because the foreign `zircon_plugins/Cargo.lock` needs its current workspace dependency update while `--locked` correctly forbids mutation. Render18 did not edit or absorb that lockfile; a fresh exact GPU job remains required after the lockfile owner lands the canonical update.
- Managed DX12 product job `c49eaa41d7324593b92ff24e92ddfa9a` ran the exact ignored exporter and failed the spatial shaft gate: 1 test ran, 0 passed, 1 failed. It proved three volumetric dispatches, 44,400 groups, 624 uploaded bytes, one LightGrid light, 524,160 non-empty clusters, one shadow pass, one atlas write, and four caster draws, but the expected shaft region had 0 brighter samples and `-2.347` average-luma delta. The rewritten PNG/report are diagnostic failure outputs, not accepted evidence.
- Follow-up managed DX12 job `aefb18e771394b42adce1ae9fa0cbc7c` tested a shadow-compare hypothesis and also failed 0/1 after 123.76 seconds. The experiment made the shadowed and unshadowed volumetric metrics identical and conflicted with the existing forward-depth comparison contract, so the compare-function experiment was fully reverted; `shadow/atlas/resources.rs` has no AF-M3 diff. Current debugging is focused on the real light-buffer participation, shadow-atlas, and froxel-scattering resources through RenderDoc rather than weakening the product gate.
- The foreign plugin lockfile closure is now canonical and clean: managed commit `5bf0c015202d96bc513528669bcbb944b07a7652` synchronized `zircon_plugins/Cargo.lock`, managed commit `6321a4df617e2f9473edfd2c44365a277d536c7c` committed the Navigation runtime manifest, and managed metadata job `c57885f1522f42188ce7a15d4e371975` passed `cargo metadata --manifest-path zircon_plugins/Cargo.toml --locked --offline --format-version 1` with exit code 0. Render18 did not absorb either dependency path.
- Fresh reservation-bound DX12 product job `0a467106c5024e1599b98bc96e304264` (run `6e2867ed9c534ec0bdab11da2f19a33b`) rebuilt the current source, passed the RenderDoc API-layout test 1/1, then failed the ignored exporter 0/1 after 149.25 seconds and released with no live process IDs. The shadowed frame changed all 24,576 pixels, but only 612 were brighter; the expected shaft region had 0/4,398 brighter pixels and `-2.3466604` average-luma delta, while the shadow control region was effectively identical at `-2.3466446`. The diagnostic PNG/report are intentionally retained as failed evidence; the script correctly did not run RenderDoc after the exporter failure.
- Capture-only DX12 job `1b8d8f94361a4155b78fbd5b15f887b5` (run `8a445fd2088a41e0b3a4dc8c1b9c91bc`) passed the RenderDoc API-layout test 1/1, then failed the ignored capture test 0/1 after 75.40 seconds. `EndFrameCapture` reported success, but the helper read `GetNumCaptures` before the capture list exposed a new entry and returned `RenderDoc ended the frame without registering a capture`. The coordinator reconciled the initially orphaned projection to `released`, exit code 101, with no live process IDs and the raw logs retained. The 03:11 RDC was not overwritten and remains diagnostic-only. Managed TDD RED job `039767dd1c5e475fb0490405ba6b6f35` (run `1963a07e036e4d10bdc008781c649ebd`) built in 20m07s and ran the exact delayed-registration test 0/1, failing at `assert!(registered)` with 12 filtered tests. The helper now polls `GetNumCaptures` for at most 10 seconds at 20ms intervals. Managed GREEN job `d23da12a3b1146b4851b1ef843a4bcfd` (run `0ac46047763849319be3bf257ecc86ee`) reused the exact plugin compatibility/target, built in 6m10s, passed the same exact test 1/1 with 12 filtered, and released with exit code 0 and no live process IDs. That single test proves both delayed visibility (`polls=3`, `retries=2`) and bounded expiry (`polls=1`, `retries=1`, unavailable).
- Read-only D3D12 replay of the existing 2026-07-16 diagnostic capture isolated a cross-plan Render05 forward-depth contract inversion. The shadow atlas is non-empty (`D32`, 4,096 x 4,096, observed min depth `0.53812`, max `1.0`), all four cascades execute four caster draws, and shadow draw event 5913 uses `LessEqual`, depth writes, and clear depth `1.0`. Volumetric scatter event 6353 binds that same atlas and slots but uses a `GreaterEqual` comparison sampler. With WGSL receiver depth `light_ndc.z - bias`, this rejects the clear window aperture and can illuminate occluded texels. The exact lower-layer failure was routed to a Render05 owner; AF-M3 will not edit or commit shadow-atlas paths. The replay is diagnostic-only and must be repeated against a fresh post-fix capture.
- Runtime01 commit `958190da` hard-cut the missing historical session fixture to tracked archive evidence. Its fixed artifact `docs/plans/zircon_runtime/render/05/fixed-2026-07-16-runtime-absorption-missing-session-fixture.md` records managed retry `3b954179608e4144b0aa183c8a4497f6`: fresh Runtime lib-test compilation completed and the focused Plan05 test actually ran 0/1 with 8,177 filtered, failing `GreaterEqual` versus `LessEqual` as intended. The Render05 owner then changed only the production comparison constant to `LessEqual`. Managed focused GREEN reservation `67f74b9b...`, job `790777092d7b4024b5e8e5f78b9388f8`, and run `df2aca4672ba437689aad0185db305f8` executed `render_shadow_atlas_compare_function_matches_forward_depth_contract`: 1 passed, 0 failed, 8,177 filtered. The job finished and released with exit code 0 and no live process IDs. The subsequent broad shadow job `cea7be4a2ab0484eb57cca59c11d8c66` (run `3ac5c593d3d349feaf0445b93264201c`) compiled successfully but finished 111 passed, 5 failed, 1 ignored, and 8,061 filtered. Two forward/deferred parity tests fail because the deferred-lighting executor lacks mesh-pipeline fixture resources; the canonical child-record-only Render01 handoff is `docs/plans/zircon_runtime/render/01/failure-2026-07-16-deferred-graph-mesh-pipeline-fixture-resources.md`. Three product captures miss their existing visual thresholds (`multi-spot frame_delta=14,798`, `mixed rgb_delta=22,295`, and `spot PCF low_profile=22 pixels/182.51 luma`). Thresholds remain unchanged. AF-M3 stays shadow-read-only while Render01 repairs the shared fixture and Render05 returns valid product metrics, a broad result, review, fixed handoff, and managed commit SHA.
- The Render01 owner completed its canonical-rustc-1.94.1 focused resource-propagation gate in job `c753b96194c54b32b4bd8d3ae15b0f6` / run `b4f9d6cdc8ff41f9854f839d1f2ec7ef`: `deferred_mesh_pipeline_context_is_required_independently_from_streamer` passed 1/1 with 8,178 filtered. Product parity job `28cb7f882ce941fabde99689ca44cad7` / run `d02771a7a3464eb1872dfdd1e1f1c145` then ran `render_product_many_point_lights_forward_deferred_capture_parity` and passed 1/1 with 8,178 filtered. Corrected lib-only job `d57cb5f09ee24b9685c36e63fd445457` ran `render_product_directional_shadow_atlas_forward_deferred_darkening_parity` and also passed 1/1 with 8,178 filtered after an 8.11-second product test. It released exit 0 with no live process IDs, and root `Cargo.lock` SHA-256 remained `FED7DA1BF408C9FD58D37768ECC4F92CF72571B51E957C40ADE880CC460822A5` from launch through terminal. Both fixtures now reach and pass image comparison instead of failing during deferred submission. Managed broad job `1eca3d1ab532400e95428876edaa8328` / run `136024091c254c5b85c3fb6ba9afff37` then completed 113 passed, 3 failed, 1 ignored, and 8,062 filtered. The former deferred-resource failures are GREEN in the broad run; only the three Render05 visual gates remain RED: multi-spot `600` darkened pixels / `4,847.62` darkening sum / `14,798` frame delta, mixed-light delta `22,295`, and spot-PCF low profile `21` pixels / `173.22578` luma. Thresholds remain unchanged. The Sound lock-closure commit, Render01 fixed return/review/commit, and the subsequent canonical-rustc-1.94.1 Render05 metric repair remain pending; AF-M3 therefore remains blocked and does not request another GPU product run yet.
- A later coordinator job `ad9738792c97485a86c30dac361a536c` / run `67e4cfed63af4059bf088e5358be7dd2` is explicitly rejected as Render01 acceptance evidence. Its payload omitted `--lib` and chained the directional filter with a broad `shadow` command on a different target. It released with exit 101 before executing any test because the dirty Plugins02 Sound manifest requires a canonical root `Cargo.lock` update while `--locked` correctly forbids mutation. Render18 and Render01 will not absorb or bypass that foreign dependency closure; the Plugins02 Sound owner must return a managed manifest/lockfile commit before the corrected directional parity can run.
- Foreign cross-system integration commit `ad2c6f989cfff927ff5679467ca0cc71e2e20c0e` absorbed 26 of this session's 27 paths into `origin/main` while AF-M3 was still explicitly blocked. It included the implementation, helper, closeout, and diagnostic PNG/RDC/JSON evidence; the unchanged sibling text report was the only scoped path absent from that commit. This batch is a provisional source snapshot, not AF-M3 acceptance: the captured product evidence still fails, Render05 and Render01 remain open, and no immutable Render18 milestone manifest authorized the 1,812-path integration. Shared main will not be rewritten. AF-M3 will close forward with fresh passing evidence and a narrowly scoped corrective closeout commit.

## Remaining acceptance gates

- Revalidate the current-source Render05 `LessEqual` shadow comparison through the ignored exporter with WGPU debug and validation enabled; inspect and hash only a passing PNG and report.
- Capture and replay the current product execution with RenderDoc; record the capture size and SHA-256.
- Update the module document with final artifact hashes, complete independent review, and submit a corrective immutable manifest to coordinator validation and milestone commit; explicitly reconcile the provisional `ad2c6f98` absorption instead of claiming a first-time implementation commit.

## 2026-08-13 structure checkpoint

The froxel integration test tree was forward-split to keep the shared test owner below the
repository file-budget gate without changing any WGPU test, shader contract, product assertion,
or exporter name. `froxel/integrate/tests.rs` now owns the 3D-radiance WGSL ABI contract and
mounts `fixture.rs`, `product.rs`, `temporal_product.rs`, and `support.rs`. The fixture owner
contains only fixed grid, light-grid, shadow-atlas, buffer, and texture setup; the two product
owners separately retain the light-scatter and temporal-reprojection workflows and their existing
PNG exporters. Current line counts are 75/233/544/446/85 respectively. The volumetric plugin
product owner was also split into a 555-line execution owner and a 421-line evidence owner while
preserving its exporter names, pixel gates, report fields, and `docs/tests/runtime/render` output
path. No touched test owner exceeds the 800-line rule. Formatting and patch-whitespace checks
passed. This is a source-structure repair only: M3 remains `testing_in_progress` until the existing
product PNG and RenderDoc gates pass on current source.

## Status and completed items

| Milestone | Item | Status | Evidence |
|---|---|---|---|
| M3 | Scene authoring and local-media extraction | implementation_completed | Fog assets, project IO, base/overlay camera-stack render-layer union, per-camera final filtering, local/global override separation, and bounds rejection are wired; current managed rerun is waiting on foreign compile gates. |
| M3 | Per-light volumetric participation | completed | Extract IDs and `cookie_misc.z` GPU ABI are wired; non-participating lights produce zero scatter. |
| M3 | Feature-off shader variants | implementation_completed | Forward, deferred, and sky assembly use explicit enabled/disabled volumetric sources; forward and sky now have Naga validation assertions for both variants. |
| M3 | Shadow scene bind-group ABI repair | completed | Managed real-WGPU validation test passed 1/1. |
| M3 | Runtime testing stage | completed | Managed job `87b0728d17194711a2de95b4179455be` passed the exact light-scatter filter 8/8, then reservation-bound job `f1c822b416184a778bb07b064e246f5a` passed the broader Runtime filter 28/28; both completed with exit code 0 on Windows. |
| M3 | Plugin and product testing stage | dynamic_validation_pending | Current source uses the shared `LessEqual` shadow-atlas comparison, retains bounded RenderDoc capture registration, and keeps the product executor/evidence owners below the file-budget gate. The coordinator-managed ignored exporter and fresh RenderDoc replay remain pending; queued or running validation does not block independent source work. |
| M3 | Product evidence | source_fix_present_validation_pending | The forward-depth comparison mismatch has a current-source forward repair. Existing PNG/report/RDC artifacts remain diagnostic only until the coordinator produces a passing current-source exporter result and fresh RenderDoc replay. |
| M3 | Independent review and milestone commit | provisional_absorption_reconciliation_pending | Independent review is 0 Critical/0 Important/0 Minor, but foreign integration commit `ad2c6f98` absorbed 26/27 paths before product acceptance and outside the Render18 immutable manifest. Final closure requires passing product evidence, a fresh review, and a narrowly scoped corrective manifest/commit that records this reconciliation. |
