# 18-advanced-lighting-features 产出记录归档

> 来源：[`18-advanced-lighting-features.md`](../18-advanced-lighting-features.md) 的 `## 状态与产出记录`。

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-07-14 | AF-M1/S2 transmission multi-step command routing | 实现、聚焦测试、DX12 产品 PNG、RenderDoc、最终 73 文件受管验证与独立复审均通过；等待协调器提交 | 状态锚点 `render_plan18_af_m1_transmission_multistep_product_renderdoc_final_validation_review_accepted_commit_pending`。新增可序列化 `ScreenSpaceTransmissionSettings`，steps 限制为 `0..=4`，并为新增 extract 字段补 `serde(default)` 以兼容旧帧载荷；0-step 保留一次 environment-only transmission draw，正数档把深度排序 transmission command list 切成无重叠连续区间。pipeline 统一持有 copy/draw executor 合同并按 step 生成 `scene_copy -> transmission-mesh` 交替节点；命令缓冲新增独立 transmission 段，普通透明、sprite 与 OIT 不再随 transmission 重放。三球产品模块构造 clearcoat、anisotropy、玻璃及玻璃后方双色参照物，对比 baseline/advanced 两帧，锁定 `advanced-pbr-opaque -> transmission.scene_copy -> transmission-mesh.0` 顺序与三个材质分区像素差异。初审指出零步 fallback 采样黑色导致材质变暗、父材质缺失时子材质高级特性被丢弃；现以 alpha=0 sentinel 选择 environment lighting，并把父级 resolve/load 失败收敛为停止继承而保留已加载子链。独立复审又发现产品质量档关闭 clustered lighting，使原 PNG 的主要差异来自禁灯回退而非高级材质；现已启用 clustered lighting 重跑产品，baseline/advanced 两侧均有直接光，clearcoat 与 anisotropy 的轮廓/高光差异以及玻璃透射差异可独立观察。另增 advanced-only ignored 捕获入口，避免通用 capture-next 抓到 baseline；最终 `.rdc` 与修正后的 advanced 场景来自同一测试源。 | Windows 受管 production build exit 0；当前源码 lib-test 已在受管 Cargo 作业中编译。当前测试程序聚焦回归 25/25；修复后的三球非 ignored DX12 用例 1/1、ignored PNG export 1/1，advanced-only RenderDoc 用例 1/1。产品报告 clearcoat/anisotropy/glass 分区 changed pixels 为 `6403/7028/1876`，mean RGB error 为 `2.7136/1.9503/0.1489`。PNG `plan18_advanced_pbr_clearcoat_anisotropy_glass_three_spheres_wgpu_20260714.png` 为 1286x360、66,906 bytes，SHA-256 `606BEB60CD15394D356210B3EF68E27060B1EBE83EDF03A9815DA8AA60592C56`。RenderDoc 1.44 在 DX12、WGPU debug/validation、API validation 下捕获修正后的 advanced-only 帧；13,158,422-byte `.rdc` SHA-256 为 `1ECA3ED304AA042131B4674DB7040D1CEB92416399238F32E2B4F953988D24F2`，`renderdoccmd replay --loops 1` exit 0。首个 immutable manifest 的受管 validation `779e661cd6574eee931e1e0bf0378e3b` exit 0；review `a1009a7f4b464df8a7202d58c65b943a` 为 0 Critical/2 Important。修复后 scoped source assertions、rustfmt 与 diff check 通过；managed compile 曾被外部 active owner 的 binary serialization visibility 红态阻断，待该 owner 修复后当前源码 capture-only test target 编译 exit 0，未虚报 broad suite 全绿。 | 最终 73 文件 immutable manifest validation `b1fc385ea1de4809bb514a863cdf225e` exit 0；独立 review `f413e8e51d754be5b5a136e79146e26e` 为 0 Critical/0 Important。完成最终重绑定后执行 closeout checker 与 `milestone commit`；Render18 后续 M2-M6 和总 Goal 保持 active。 |
| 2026-07-14 | AF-M1/S2 transmission scene-copy physical resource contract | 实现完成；聚焦测试部分通过，fresh 标准门禁等待 Text01 外部导入漂移修复，不关闭 AF-M1 | 状态锚点 `render_plan18_af_m1_transmission_scene_copy_physical_owner_implemented_testing_stage_open`。compiled graph cache fingerprint 现区分 transmission scene-copy 与 late-forward opaque 图形态；材质使用统计只消费选中相机 `RenderLayerSet` 可见网格。固定 `SCENE_COLOR` 在 execution resources 中同时绑定 view、可复制 WGPU texture owner 与物理 `TextureDesc`，但继续保持非 transient owner；`transmission.scene_copy` 声明只读 source、写单采样 `Rgba16Float` transient，执行器按资源局部渲染区域复制，不再尝试把 external scene-color 当 graph-owned texture 或走无效 MSAA resolve。 | 受管 Windows `cargo build -p zircon_runtime --locked` 在格式修正前完成，证明 owner/copy API、缓存与 layer-filter 类型闭合。fresh 646 MB lib-test binary 实际执行：cache fingerprint、无 transmission 节点关断、late-forward 时序、固定 scene-color owner、相机层过滤共 5/5 通过；scene-copy 格式测试先红，精确暴露 `Rg11b10Ufloat != Rgba16Float`，随后已把 descriptor 强制修为 `Rgba16Float`。格式修正后的 fresh build 只在 Text01 活跃 owner `graphics/scene/scene_renderer/ui/text/prepare_report.rs` 的 `ResolvedScreenSpaceUiTextBatches` E0432 停止，未出现 Render18 错误，因此不把最终测试记绿。 | 等 Text01 当前 owner 收敛后重跑受管 build 与 `advanced_lighting`/render product。AF-M1 仍需完成多层 transmission steps 质量档、clearcoat/aniso/玻璃 WGPU PNG、像素断言与 RenderDoc 抓帧；这些完成前不提交 AF-M1 里程碑。 |
| 2026-07-14 | AF-M1/S1 advanced PBR material feature contract | 实现切片完成；里程碑 Cargo/WGPU 测试阶段待后续切片一起执行 | 状态锚点 `render_plan18_af_m1_material_feature_contract_implemented_testing_stage_pending`。按 Bevy `StandardMaterial` 与 transmission 参考新增纯数据 `StandardPbrMaterialFeatures`，覆盖 clearcoat/clearcoat roughness/normal texture、anisotropy strength/rotation、specular/diffuse transmission、thickness、IOR 与 attenuation；默认值不请求 forward-only 变体或 scene-color copy。`normalized()` 统一处理非有限值、单位区间、负 thickness 与 IOR 下界；无吸收使用可序列化的有限 `f32::MAX`。 | 先写 3 个 `render_advanced_material_features_*` 行为测试，再补实现；scoped `rustfmt --check` 与 `git diff --check` 通过。模块文档 `docs/zircon_runtime/core/framework/render/advanced_lighting/material-features.md` 已记录 ownership、默认值和后续集成边界。共享兼容 Cargo 池当前由 `plugins-08-zrvm-m1-20260714` 持有，因此本行不虚报 Cargo/WGPU 通过。 | AF-M1/S1 下一切片把合同投影进 `MaterialAsset`/`StandardMaterialDescriptor`、`PipelineKey` 与 `ShaderFeatureBits::{PBR_CLEARCOAT,PBR_ANISOTROPY,PBR_TRANSMISSION}`，并锁定默认材质 variant key 不变；随后实现 `zr_pbr_extras.wgsl` 和 transmission scene-copy 产品链。 |
| 2026-07-14 | Hybrid GI Editor actual/fallback product evidence and scene-velocity producer | 已完成 Editor actual/fallback 产品子门；broad/full 与 HGI 后续里程碑开放 | 状态锚点 `render_plan18_hybrid_gi_editor_actual_fallback_product_png_passed_broad_full_open`。组合图 `hybrid-gi.resolve` 现在接受 external/transient 两种只读 `scene-velocity` 来源；当 HGI capability 生效且 profile 关闭 temporal history 时，compile options 仍保留内部 Temporal velocity producer，TAA 继续由 post-process stack 过滤。Runtime Diagnostics 优先展示解析后 profile/mode/quality、预算、回退和 Render 状态；下一产品构建会把 Render 帧行放入紧凑 pane 前五项。 | 精确执行器 Rust 回归 1/1；HGI/Editor/35 篇文章静态合同 17/17。当前源码 r10 `target-editor-host` 产品构建 47m12s 通过，二进制 SHA-256 `B828F854EB342D661637E16A6944D59437937A74AD29F5B25C743899C911251F`。`plan18_hybrid_gi_editor_runtime_diagnostics_actual_20260714.png` 与 `...fallback_20260714.png` 均为 1688x980、非空 WGPU 场景、stderr 为空；actual 显示 `custom/dynamic-only/medium`、`32/64/16`、`none`，SHA-256 `35A8FF93D8C67E3EEBC6A59F9C251EE9FAB279BC00406753C0D6FD600511844E`；fallback 显示 `indoor-static/dynamic-only/high`、`64/256/64`、`baked-lighting-unavailable`，SHA-256 `9619D6E3B8B011CC5EB53D71505D5F19606B02C5FCDFEDDF5ECACEBB4F6BB7CF`。该产品二进制的紧凑 pane 把 Render 帧行放在可见折叠下方，因此不虚构文字帧计数；非空 viewport 与空 stderr 是本次实际产品证据。 | Editor actual/fallback 子门关闭。执行 display-order 新单测的托管验证、broad/full runtime/workspace 验证、HGI-M5-RC、Mesh/Global SDF 与 M6 仍开放；不据此关闭 HGI-M4 或总目标。 |
| 2026-07-14 | Hybrid GI real Editor product path and composed-graph velocity contract | 代码修复与静态门完成；真实产品已进入 HGI resolve，修复后 Rust/product 重建等待共享 Reflection/Text/Asset 硬切稳定，不关闭 Editor PNG/M4 总门 | 状态锚点 `render_plan18_hybrid_gi_editor_product_path_reaches_resolve_scene_velocity_contract_fixed_rebuild_open`。Navigation 05 回传后，修复 `zircon_app` Editor 入口未注入第一方 runtime plugin registrations；修复异步 WGPU framework 初始化时 pending `render_dirty` 被 presentation recompute 清除；submission error 写入 process diagnostics。产品 r8 日志最终精确报告 `hybrid-gi.resolve` 期望 transient `scene-velocity`、组合插件图实际提供 external。执行器现复用只读跨图纹理种类合同，允许 external/transient 两种来源而不改变 feature 对基础 Temporal 生产者的依赖声明；新增双来源 Rust 回归用例与 Python 静态合同。使用指南新增 `ZIRCON_EDITOR_HYBRID_GI_PROFILE` 五档切换及实际值/降级判读。 | r6/r7/r8 三次实际 `target-editor-host` 产品构建与窗口运行把状态从 HGI unavailable/`0 viewport` 推进到 `1 viewport, 0 frames`，typed 日志保存在 `.codex/tmp/plan18_r8_logs/2026-07-14-04-10-50/editor.log`。HGI M4 + Editor Profile 静态合同 15/15；scoped rustfmt、Python compile、diff check 通过。Rust 精确测试两次重跑均在进入 HGI crate 前被并行底层硬切阻断：先是 `zircon_reflect_derive` moved-value，修复后推进到当前 Runtime 31 errors，归属 Plugins08 Reflection visibility/lifetime、Text05 variable shaping visibility 和 Runtime04 `AssetUri::parse(&str)` 迁移。Render18 未覆盖这些 owner，也未把失败写成 HGI test green。 | 共享底层 owner 稳定后重跑 `hybrid_gi_resolve_accepts_external_or_transient_scene_velocity`，重建 r9 Editor 产品；Custom actual 应显示 `profile=custom, mode=dynamic-only, quality=medium` 与 `32/64/16`，IndoorStatic 无 baked 应显示 `profile=indoor-static, mode=dynamic-only, quality=high`、`64/256/64`、`baked-lighting-unavailable`，两图均要求 `1 viewport` 且 frames 大于零，再复制到 `docs/tests/runtime/render`。broad/full 仍开放。 |
| 2026-07-13 | Hybrid GI real Editor product build and Navigation startup boundary | 当前 `target-editor-host` 产品构建通过；真实窗口启动收敛到 Navigation Driver->Manager 层级错误并交接 Navigation 05 | 状态锚点 `render_plan18_hybrid_gi_editor_product_build_passed_navigation_module_startup_handoff_open`。当前源码受管 E 盘共享池重新构建实际 `zircon_editor.exe`，Text05 SDF uniform 与 Frameworks05 runtime-profile/bridge 编译面均已通过。隔离 Runtime Diagnostics 启动不再复现旧 stack overflow，而在窗口创建前由 Core 模块注册拒绝 `navigation.runtime.Driver.SceneNavigationRuntime -> Manager.DefaultNavigationManager`；已登记 [`navigation-runtime-driver-manager-layering`](../../../zircon_plugins/05/failure-2026-07-13-navigation-runtime-driver-manager-layering.md)。 | `cargo build -p zircon_app --bin zircon_editor --no-default-features --features target-editor-host --locked --jobs 1` 约 13 分 15 秒、exit 0。隔离启动约 8 秒 exit 1，报告 `valid_main_window_observed=false`、`timed_out=false`、`forced_stop=false`、`stack_overflow_observed=false`，只观察到一个可见 `0x0` 辅助窗口。Runtime 内置 Navigation 的合法 descriptor 已证明具体实现应是无依赖 Driver，Scene Driver 与公开 Manager 再依赖该 Driver。 | 产品编译子门关闭；等待 Navigation 05 回传后复跑产品构建与实际 Runtime Diagnostics actual/fallback 窗口，生成可见文字 PNG 到 `docs/tests/runtime/render`。在真实 UI 和 broad/full 通过前不关闭 M4、不进入 M5。 |
| 2026-07-13 | Hybrid GI standalone/crate behavior and Editor10 fixture return | standalone production 与 124 项 crate behavior 通过；Editor10 fixture handoff 已回传；M4 总门仍待真实 Editor UI 和 broad/full | 状态锚点 `render_plan18_hybrid_gi_standalone_crate_behavior_passed_editor10_fixture_returned`。fixture 已使用 manifest-owned asset roots 与公开 `to_project_toml_string(...)`，通过 `project_documents.rs` 持久化 builtin/project references；provider 测试缺失导入、prepared-frame 新字段与 direct dev-dependency 已同步。原 972 行 fixture 拆为 683 行编排与 301 行项目文档模块，无固定根、shim 或手写 TOML。coordinator 已将 [`hybrid-gi-project-fixture-api-drift`](fixed-2026-07-13-hybrid-gi-project-fixture-api-drift.md) 原子回传本子目录。 | 静态合同 13/13；official standalone `-SkipTest` production build exit 0；完整 crate test phase 为 library 119、invalidation/profile 2、Forward+/Deferred WGPU 3，共 124，0 failed / 0 ignored。组合 validator 的 build phase 曾观察并发 Text05 `material.rs` 中间态并使包装器 job exit 1，故不把该组合调用记为全绿；独立生产构建与随后完整 test compile/execute 分别构成有效门禁证据。 | crate/fixture 子门关闭。19:54 最新 Editor 产品重建已越过 Text05，随后在 Frameworks05 活跃迁移的 `runtime_profile/{feature_presets,defaults,descriptor}` 观察到 `RuntimeProfileId` owner/import 中间态 E0432/E0603；Render18 不修改该 owner。待共享边界稳定后继续真实 Runtime Diagnostics actual/fallback 窗口和截图，再执行 broad/full runtime/workspace 验证。 |
| 2026-07-13 | Hybrid GI standalone build after Text05 source progress | official validator 已越过 HybridGI graphics 导入和 Text05 类型解析点；当前被 Frameworks05 插件桥接硬切中间态阻断，不关闭 production/M4 总门 | 状态锚点 `render_plan18_hybrid_gi_standalone_build_text05_passed_frameworks05_bridge_cutover_observed`。受管 Windows F 盘池执行 `validate-matrix.ps1 -Package zircon_plugin_hybrid_gi_runtime -SkipTest`，`zircon_runtime` 完成编译后，唯一 error 为 `zircon_plugins/plugin_sdk/src/lib.rs:47` 从 `core::framework::bridge` 导入 `WeakBridge` 的 E0432。coordinator 核验该 SDK 文件、`core/framework/bridge` 和 `plugin/bridge` 均由 Frameworks05 活跃会话租约保护，并正在处理 [`core-contract-reverse-dependencies`](../../frameworks/05/failure-2026-07-13-core-contract-reverse-dependencies.md)；Render18 没有覆盖其源码。 | 本轮实际编译约 8 分钟，原 26 个 HGI E0432/E0433 与 Text05 E0425 均未再出现。构建结束后工作树已出现 Frameworks05 新拆出的 `core/framework/bridge/` owner，说明失败是并发硬切过程中旧编译快照观察到的边界不一致；在所有者完成前不添加 alias、shim 或 SDK 调用点特例。 | 等 Frameworks05 完成/释放桥接 owner 后重跑同一 official validator，再执行 HybridGI crate behavior tests。Text05 未正式回传，Editor10 fixture handoff、Runtime02 真实 Editor 窗口及 broad/full 验证继续开放。 |
| 2026-07-13 | Hybrid GI Editor10 project fixture explicit-root cutover | 消费方迁移实现和静态门已完成；Rust compile/behavior gate 尚未通过，Editor10 handoff 不回传 | 状态锚点 `render_plan18_hybrid_gi_editor10_project_fixture_explicit_asset_root_migrated_static_passed_compile_text05_blocked`。`hybrid_gi_scene_prepare_material_fixtures.rs` 的四个临时项目 fixture 现在先构造 `ProjectManifest`，调用 `ensure_layout(&manifest.asset_roots)`，再取 `manifest.primary_asset_root()` 并传给 `asset_root(&RelPath)`；39 个固定 `.assets_root()` 调用和 4 个无参数 `.ensure_layout()` 已硬切移除，也未增加额外 `zircon_runtime_interface` dev-dependency、旧 API 或 test-only shim。当前 `MaterialAsset/ModelAsset/SceneAsset::to_toml_string` 已公开，因此历史 handoff 中该项不再是剩余根因。 | TDD static RED 1/1 精确证明旧调用仍在；GREEN 后 HybridGI M4 + Editor profile 合同 13/13。旧 API 文本扫描为 0，rustfmt check 与 scoped diff check 通过；fixture 863 行，未跨越 900 行设计预警。首次编译停在 Text05；最新 official build 已越过该点并推进到 Frameworks05 桥接硬切中间态。 | 等共享桥接 owner 稳定后复跑 HybridGI standalone production validator，再编译并执行 crate 内 scene/material/provider 行为测试。实际通过后规范化并返回 `hybrid-gi-project-fixture-api-drift` handoff；之前不申领 Editor10 fixture gate 完成。 |
| 2026-07-13 | Hybrid GI runtime standalone graphics feature contract | 清单契约已修复、静态门通过；official standalone production build 已越过 HybridGI 自身错误，但被 Text05 的共享 Runtime 类型解析 handoff 阻断，不关闭 production/M4 总门 | 状态锚点 `render_plan18_hybrid_gi_runtime_manifest_graphics_contract_fixed_standalone_build_reaches_text05_blocker`。`zircon_plugins/hybrid_gi/runtime/Cargo.toml` 现对 `zircon_runtime` 显式声明 `features = ["graphics"]`，与 OIT、volumetric fog、subsurface scattering 等图形插件的独立编译合同一致；新增 `test_runtime_manifest_enables_the_graphics_contract_it_imports` 锁定该边界。 | TDD RED 精确因旧清单缺少 graphics 失败 1/1；修复后 HybridGI M4 + Editor profile 静态合同 12/12。Windows official validator 修复前报告 HybridGI 26 个 E0432/E0433；修复后这些错误消失并进入共享 `zircon_runtime`，只剩 `ui/render.rs:74` 缺少 `UiTextDistanceFieldEffects` 的 E0425。Cargo fingerprint 保存了准确文件/符号证据；该源码由 Text05/Editor Layout 会话拥有，已登记 [`ui-text-distance-field-effects-type-resolution`](../../text/05/failure-2026-07-13-ui-text-distance-field-effects-type-resolution.md)，本切片未越权修改。 | 等 Text05 类型解析 handoff 回传后复跑 `validate-matrix.ps1 -Package zircon_plugin_hybrid_gi_runtime -SkipTest`。Editor10 fixture-blocked crate suite、Runtime02 真实 Editor 窗口及 broad/full runtime/workspace 验证仍开放。 |
| 2026-07-13 | Hybrid GI M4 resolved settings provider-to-Editor diagnostics | 代码、Runtime 行为、Editor unit 与实际 `target-editor-host` product build 已完成；真实窗口产品门被 Runtime02 open handoff 阻断，不关闭 M4 总门 | 状态锚点 `render_plan18_hybrid_gi_m4_resolved_settings_runtime_editor_unit_product_build_passed_real_window_runtime02_blocked`。新增公开 quality/mode/profile/fallback label；生产 provider 将 resolved mode/profile/quality、trace/card/voxel budgets 与 fallback 写入 `HybridGiRuntimeStats`，经 submission snapshot 投影到 `RenderStats.last_hybrid_gi_resolved_settings`；关闭/移除 HGI 时同帧清空。Runtime Diagnostics pane 直接显示实际值，不在 Editor 重新推算。聚焦 Runtime fallback 用例请求 IndoorStatic 且把 fixture 非零 override 清零，验证缺 baked 时为 High、`64/256/64`、DynamicOnly、BakedLightingUnavailable。 | Windows current-source Runtime production build 通过；Runtime fallback 1/1，provider-missing 与 stale-state current binary 2/2；Editor exact unit 1/1；静态合同 11/11；scoped rustfmt/diff check 通过。`cargo build -p zircon_app --bin zircon_editor --no-default-features --features target-editor-host --locked --jobs 1` 一致源码重跑通过；首轮 E0599 是并发 Runtime/Editor 源码快照漂移。产品启动使用独立 `ZIRCON_CONFIG_PATH/LOCALAPPDATA/APPDATA`：默认布局首帧退出与 `--builtin-view editor.runtime_diagnostics` 分别约 96/59 秒后 main-thread stack overflow，均只出现 0x0 辅助窗口；已把复现追加到 Runtime02 [`service-corehandle-retention-cycle`](../../runtime/02/failure-2026-07-13-service-corehandle-retention-cycle.md)。 | 代码/编译/unit 子门关闭；真实 UI 截图继续开放，等待 Runtime02 CoreWeak 生命周期 handoff 修复并回传后复跑。Editor 10 fixture-blocked crate suite、plugin lock owner 收敛和 broad/full runtime/workspace 验证仍开放；Plan 18 总体未完成。 |
| 2026-07-13 | Hybrid GI M4 Forward+/Deferred RenderDoc DX12 capture/replay | 已完成：四 Profile 双管线当前源码产品测试生成 8 份可重放 `.rdc`，HGI handoff/resolve/completion/history 事件和 API validation 已审计；只关闭 M4 RenderDoc 产品门，不关闭 M4/Plan 18 总门 | 状态锚点 `render_plan18_hybrid_gi_m4_forward_deferred_renderdoc_dx12_eight_capture_replay_passed`。使用用户指定 `D:\Tools\renderdoc\renderdoccmd.exe` 1.44，在 `WGPU_BACKEND=dx12`、WGPU debug/validation、RenderDoc API validation 开启时运行当前源码 `hybrid_gi_m4_profiles_render_forward_deferred_wgpu_product_matrix`。8 个 panel/framework frame 捕获全部写入 `docs/tests/runtime/render/plan18_hybrid_gi_m4_profile_forward_deferred_renderdoc_20260713_capture*.rdc`；qrenderdoc 成功重放 capture 1/8，最大 EID `4554/4615`。capture 1 精确检索到 `HybridGiSceneDepthHandoffPass`、`HybridGiTraceScheduleHandoffPass`、`HybridGiResolveTraceDepthSourcePass`、`HybridGiCompletionPass`；capture 8 可见 completion、`zircon::RenderGraphPass::hybrid-gi-history` 与 `zircon::DeferredLighting`。 | 8 个 `.rdc` SHA-256：`D886C3BBABAE4B2FA0C6C40CA0D3F62C839314F1757AD7F77D7C19B8A69F281A`、`730F750950B0D040E1864280DBF8E1A351EB3ADEE2FC845AB9239931B610AED3`、`D5AD781FD97E37362C687653A21DD0102490177E343E109BD3FD8CC7C22DF902`、`0292366748A9501DFD21DFC9719ABDC91026B5EE395F3B8B68CAE879E763BDB6`、`CD5A40B093E4178102EA861618D526D649E2D013F236E4C6E0D7E3E7B5C4CCD9`、`DA30920CA4AF306AFCEDDA1989008C1AE75778A0E9131B1B3903B6FA9C61F319`、`A7D060CFC5862DB0BC087830A5CF726613619A62727E8C20763B12A879C8DAF1`、`6A8FA20FBA1F6E7591FBA1A890ECAEFBE91712E99ED7417A6C08556D11BE08F9`。四张已目检 qrenderdoc PNG 为 `plan18_hybrid_gi_m4_renderdoc_ui_20260713.png`、`plan18_hybrid_gi_m4_renderdoc_hybrid_passes_20260713.png`、`plan18_hybrid_gi_m4_renderdoc_handoff_passes_20260713.png`、`plan18_hybrid_gi_m4_renderdoc_api_validation_20260713.png`，SHA-256 分别为 `7BE5ACBB833E4760CDED04ABE5C6C3FFC55CF92DD31BD1EC3CA1B7B773156A1F`、`F2A27A1A4FDEEAEBEF8EEB26340A37F1B9AB987EC955093D747C0242A8AA19B8`、`FA930E0D4803542828B4118DD1F2A8375364CC66BDE085E94085D8B2F2B120F3`、`552CD2ACE2D4B8A110C5E2DE107C854248E86C24DADE6BFE8A865065DA696E41`。capture 1/8 的 71/80 条 API 提示均为 Source API、Medium、State Creation、ID 926 的 wgpu alias-heap CBV GPU VA 重叠诊断；如实保留，不宣称零告警，也未观察到 HGI shader/barrier/dispatch 错误。 | RenderDoc 产品门关闭。继续 Editor resolved/fallback 诊断编译与真实 UI、Editor 10 fixture handoff 后的 crate 行为套件，以及 broad/full runtime/workspace 验证；M3 的 Editor 默认路径画面仍开放，Plan 18 总体仍未完成。 |
| 2026-07-13 | Hybrid GI M4 mobility and moving-emissive WGPU round trips | 已完成：Forward+/Deferred 的 Static→Dynamic→Static lightmap ownership 与 moving Emissive 往返无残影产品门通过；M4 仍等待 Editor/RenderDoc/broad gates | 状态锚点 `render_plan18_hybrid_gi_m4_mobility_emissive_roundtrip_wgpu_no_ghost_passed_editor_renderdoc_open`。新增同一公开插件 WGPU integration 的两个跨帧测试。首轮 mobility RED 为 Forward+ static/dynamic MAE `0.000000`，定位 `gpu_scene_sync::instance_data_for_pending_draw` 无条件按 stable key 写 static atlas slot；修复后只允许 `Mobility::Static` 绑定 slot，Dynamic 使用 probe/HGI，恢复 Static 重绑。Moving Emissive 使用同 stable dynamic entity、transform revision 与 warmup/history 路径执行 origin→moved→origin。 | 最终当前二进制三项产品测试串行 3/3；Runtime production check 通过，联合静态合同 19/19。mobility Forward/Deferred release MAE `4.433819/4.429861`，restore MAE `0/0`；Emissive moved MAE `15.646076/15.649913`，restore `0/0`。两张 484x242 PNG 已目检。四个 artifact SHA-256：`DFE0CAAC2D14F375DCA3DBBE7B0FD00BF583F44189446429DD12921315A94722`、`5C7DE90C8ED20C757AB37EE5E4E63E228D9440FAE6ED6641192BDEA77B1E05F8`、`93B9E028BC3DA9D5DB0B90BF119E34153B37E4B7B852CF1E7D170BAE537464EE`、`6B2056EBDCBEE19B154703C0C4A34677B22AD55406F51BC9BD21928481633612`。共享 plugin lock 当前被其他插件清单改动置为待更新，未重写。 | 实现 Editor resolved/fallback UI，补 RenderDoc `.rdc`；等待 Editor 10 fixture handoff 与 lock owner 收敛后跑 crate 内完整行为套件，再做 broad/full runtime/workspace 验证。 |
| 2026-07-13 | Hybrid GI M4 four-profile Forward+/Deferred WGPU matrix | 已完成：四 Profile 固定场景的真实双管线 WGPU 产品矩阵、能量差和 parity 通过；不关闭跨帧残影、Editor 与 RenderDoc 总门 | 状态锚点 `render_plan18_hybrid_gi_m4_four_profile_forward_deferred_wgpu_matrix_passed_temporal_editor_renderdoc_open`。新增 `zircon_plugins/hybrid_gi/runtime/tests/hybrid_gi_m4_profile_matrix_wgpu.rs`，不依赖失效的 project fixture；通过公开插件 descriptor/executor/collector/provider 注册生产 WGPU framework，复用 Plan 11 DTO 语义构造 RGBA16F atlas + SH9 probe grid，场景含静态 UV2 面、动态对象与 movable directional light。FullyDynamic 不提交 baked 环境，IndoorStatic/OpenWorld/Cinematic 提交同一版本化 baseline；Forward+/Deferred 各三帧 warmup。 | 测试 1/1。每格非空、4 HGI passes、2 screen probes、8 个有限 clipmaps；终态无待调度 trace tile。四档 Forward+/Deferred MAE `0.012812/0.020000/0.020000/0.019740`。三个 baked profile 相对 FullyDynamic 的 Forward/Deferred 全帧 MAE `8.868472/8.875660`、`8.868472/8.875660`、`9.078333/9.085260`。646x242 PNG 已目检；PNG SHA-256 `A5C93292E04467D24E6D0CC46EF301CB88BD53B6F2FE6A30C8B38C3AD49E5883`，report SHA-256 `E7699770AC16D1AAC4131978DAF38B78961FF2D2F282FC80170DEAC78D8463D9`。 | 完成移动动态对象/移动 Emissive 与 mobility 往返跨帧像素无残影；实现 Editor resolved/fallback UI；补 RenderDoc `.rdc`，恢复受 Editor 10 fixture 漂移阻断的 crate 内行为套件并执行 broad/full 验证。 |
| 2026-07-13 | Hybrid GI M4-S4/S5 provider invalidation and profile acceptance | 已完成：公开 provider 的 invalidation/resolved-profile 行为通过可执行集成门；高预算 clipmap 非有限值缺陷已修复。只关闭 S4/S5 provider 合同，不关闭 M4 产品总门 | 状态锚点 `render_plan18_hybrid_gi_m4_s4_s5_provider_invalidation_profiles_finite_clipmap_passed_product_visual_open`。新增 `zircon_plugins/hybrid_gi/runtime/tests/hybrid_gi_m4_invalidation_profiles.rs`，通过生产 `PluginHybridGiRuntimeProvider` 验证 stable epoch、transform/material/mobility/light/bake-generation/streaming invalidation，source mask mobility 往返，以及四 profile resolved sideband/fallback。测试 RED 发现 profile 的 96/128/192 `voxel_budget` 被直接解释为 clipmap level count，`half_extent * 2^level` 溢出为非有限值；`build_clipmap_descriptors` 现限制 V1 最多 8 层，voxel bounds/cell-range 对 center、extent、radius 做有限值 gate。 | 首轮 integration 1/2，失败栈明确落在 `mesh_axis_range` 的 `f32::clamp(min=-inf,max=NaN)`；修复后 2/2。四 profile 预算和 effective mode/fallback 从 provider neutral sideband 实际读取；所有输出 clipmap descriptor 有限且数量 `<=8`。Windows target 位于 `D:\cargo-targets\zircon-engine\lanes\hybrid-gi-m4-provider-20260713`。 | 补 Forward+/Deferred 四 profile 固定场景能量与 PNG、动态物体/移动 Emissive、mobility 往返跨帧像素无残影、Editor resolved/fallback UI 和 RenderDoc `.rdc`。现有公开 integration 不替代完整场景与被 Editor 10 fixture 漂移阻断的 crate 内测试。 |
| 2026-07-13 | Hybrid GI M4-S3 production post-process source-ledger WGPU product slice | 已完成：生产 WGSL 的来源账本与 no-double-count GPU gate 通过真实 WGPU readback 和 PNG；只关闭 S3 独立 shader 切片，不关闭 M4 总门 | 状态锚点 `render_plan18_hybrid_gi_m4_source_ledger_production_post_process_wgpu_png_passed_product_matrix_open`。新增 `zircon_runtime/tests/hybrid_gi_m4_source_ledger_wgpu.rs`，拼接实际 `post_process.wgsl` 与 SSR 模块，显式建立与生产一致的 29-binding layout，并渲染 full dynamic、baked baseline、baked+dynamic delta、非法 baked+full dynamic 四组 64x64 面板。全局及 per-probe source mask/dynamic weight 由真实 uniform/storage buffer 驱动；非法 baked+full-dynamic probe 必须被 WGSL 拒绝。 | Windows WGPU 测试 1/1。`docs/tests/runtime/render/plan18_hybrid_gi_m4_source_ledger_wgpu_20260713.png` 已目检为非空 262x64 四面板；中心 RGBA `[121,50,34,255] / [70,70,70,255] / [166,95,79,255] / [70,70,70,255]`，证明 dynamic delta 增能且非法组合与 baked baseline 完全相同。PNG SHA-256 `9C717938FD1BCCAD9E6C5DD38041B8D4DF4CF43DC3A09EE051BF5A72F4B77C53`；报告 SHA-256 `F123BCD89F178CADCD5005C1B5EE329C76E6F30984916E049C48C281B6D45B10`。 | 完成插件 participation/invalidation 行为测试和 Forward+/Deferred 四 profile 场景矩阵；补动态物体、移动 Emissive、mobility 往返残影、编辑器 resolved/fallback 诊断与 RenderDoc `.rdc`。当前合成产品图不能替代双管线完整场景门。 |
| 2026-07-13 | Hybrid GI M4 S1-S5 core implementation candidate | 核心代码候选完成、产品总门开放：只读 baked dependency、participation/source ledger/dynamic delta、scene invalidation 和四 profile 已进入生产路径；不申领 M4 完成 | 状态锚点 `render_plan18_hybrid_gi_m4_s1_s5_core_candidate_source_ledger_invalidation_profiles_compiled_shader_validated_product_gate_open`。light snapshots 新增默认 Dynamic mobility；baked 模式只向 HGI seed 提交 movable directional/point/spot。stable primitive key 贯穿 card、screen probe、prepare DTO、neutral sideband 和 GPU probe；source mask/dynamic weight 使用既有 padding lane。post-process 全局/per-probe 双 gate 禁止 baked baseline 与 full-dynamic 同 lobe 双 owner。transform/geometry/material/resource/tint/light/mobility/shadow/light-set generation/streaming set 变化推进 participation epoch，epoch/generation/source 进入 temporal signature。四 profile 解析不同预算，缺 baked 合同返回 `BakedLightingUnavailable` 并实际降级 `DynamicOnly`；resolved settings 进入 neutral prepared-frame。 | Windows Runtime/插件生产检查通过。`tools.tests.test_hybrid_gi_m4_contract` 8/8，联合静态合同 17/17。独立 `zircon_runtime --test hybrid_gi_m4_source_ledger` 3/3，包含 source-policy、四 profile/fallback 与生产相同拼接的完整 post-process WGSL Naga parse+validation。Runtime `--lib` 测试已确认本切片 41 个 light fixture mobility 迁移完成，随后被无关 `RuntimeModuleLoadReport` 旧字段 25 errors 阻断；HybridGI plugin test 被 Editor 10 project fixture API 漂移阻断，handoff 为 `docs/plans/zircon_editor/editor/10/failure-2026-07-13-hybrid-gi-project-fixture-api-drift.md`。本切片没有新 WGPU PNG/`.rdc`。 | 修复 handoff 后执行插件 participation/invalidation 行为测试；完成 Deferred/Forward+ 四 profile 固定能量矩阵、mobility 往返无残影、真实 WGPU PNG/readback、编辑器实际值诊断和 RenderDoc `.rdc`。现有 Plan 11 PNG 只证明 baked baseline，不替代 M4 产品证据。 |
| 2026-07-13 | Hybrid GI M4 prerequisite / Plan 11 EL-M3 WGPU product dependency | 依赖已完成、M4 实现待开始：唯一 baked baseline、GPUScene slot、Forward+/Deferred 消费与真实 fixture 已闭合；本条不申领 participation/source-ledger/delta/epoch/preset 完成 | 状态锚点 `render_plan18_hybrid_gi_m4_plan11_el_m3_baked_baseline_dependency_wgpu_product_ready`。Plan 11 的 `EnvironmentExtract` 是唯一 baked owner；binding 23/24/28 分别为 SH9 probe storage、RGBA16F D2-array atlas 与 filtering sampler；静态实例按 stable slot 采样 atlas，动态/无 slot 实例采样 probe grid。单页 atlas 仍公开 `D2Array`，legacy 通用 bind group 只持有独立 page-zero `D2` view。Forward+ 直接加入 baked diffuse，Deferred geometry 经 HDR emissive MRT 搬运同一逐实例结果。 | Windows 最新 Runtime test binary：lightmap 20/20（1 ignored exporter）、baked-indirect 2/2、GBuffer WGPU 1/1、外部 fixture 1/1、产品对拍 1/1、ignored export 1/1。静态 baseline delta MAE Forward/Deferred `9.5296/9.5507`，动态 probe `3.6566/3.6784`；管线 parity MAE `0.0214`、max error `1`。PNG 已目检，SHA-256 `386909A40E13EB4C0B8E27B354D05AC0DAEE2113FA2EC9A564F787B9B30FAB22`。 | 进入 HGI-M4-S1..S5：generation/participation epoch、显式参与、source ledger/dynamic delta、失效和四 profile。完成前 `BakedStaticDynamic` 继续不可用；M3 Editor WGPU、fallback 产品、RenderDoc 与完整 CI 仍开放。 |
| 2026-07-13 | Hybrid GI M4 prerequisite / unique baked owner and dual-pipeline ABI | 部分完成：Plan 11 baked 合同已收束为单一 owner，旧全屏伪 ambient 被中性化，Forward/Deferred 同时绑定 lightmap/probe ABI；实际 UV2/SH 着色和 source-ledger composite 仍开放 | 状态锚点 `render_plan18_hybrid_gi_m4_unique_baked_owner_dual_pipeline_resource_abi_implemented_sampling_pending`。`LightingExtract` 不再承载第二份 lightmap/probe 合同，`EnvironmentExtract` 是唯一 generation owner；offline bake 不再生成方向光平均色占位，post-process baked 参数保持 0，消除在真实 lightmap 接入前沿旧路径与 HGI 直接双加的入口。Forward/Deferred 同时安装 23 probe storage、24 RGBA16F array、25 sampler，资源 ABI 不再单边。 | owner 合同 6/6、GPU ABI 3/3，和 profile/HGI 静态门合计 18/18；此前当前源码 `zircon_runtime` Windows build 通过唯一 owner 和合同。fresh check 找到并修复 queue/lifetime 两个自有编译错误；后续受管 build 因共享 target 的并发长时 rustc 占用两次超过 15 分钟，无新诊断，均按 exit 124 释放，不计 GREEN；没有产品 PNG/RenderDoc。 | 完成 imported atlas/probe 上传、GPUScene UV2/slot 和 WGSL 后，先做 EL-M3 静态面/动态物 probe 产品图；再开始 HGI M4 participation、dynamic delta ledger 和 generation invalidation。 |
| 2026-07-13 | Hybrid GI M4 prerequisite / Plan 11 EL-M3 strict contract and GPU ABI audit | 部分完成：可导入 bake DTO、过期结果拒绝和 bindings 23-25 GPU ABI 已落地静态门；实际双管线消费、唯一数据 owner 与产品证据仍开放，HGI M4 不得启动合成接线 | 状态锚点 `render_plan18_hybrid_gi_m4_baked_baseline_dependency_contract_gpu_abi_static_passed_product_consumption_pending`。严格审计将原始 ID-only DTO 修正为版本化 scene snapshot + atlas budget + texel density 输入，以及 RGBA16F page payload + slots + probe grid 输出；只有资源导入后才构造 `LightmapConsumeContract`。输出按 request id/scene revision/light-set generation/budget/instance set 验证，probe 索引采用 checked `usize`。GPU 侧保留 binding 23 probe storage、24 RGBA16F array、25 sampler，并提供 neutral fallback；尚未接入 Forward+/Deferred 或 HGI source ledger。 | EL-M3 CPU 静态门 6/6、GPU ABI 静态门 2/2；两轮 RED 分别记录初始缺失 1 failure + 1 error、严格合同补充 3 failures，GPU ABI RED 为 1 failure + 1 error。Cargo lane 仍被共享 job 占用，Rust compile/WGPU/product PNG/RenderDoc 均未执行；当前源码还存在独立 PMREM API 半迁移风险。 | 先完成 EL-M3 唯一 owner、资源导入、GPUScene UV2/slot、Forward+/Deferred WGSL 与真实 bake fixture。只有静态 baked baseline 与动态对象 probe 产品门通过后，才实现 HGI participation、dynamic-delta energy ledger、generation invalidation 和产品 presets。 |
| 2026-07-13 | Hybrid GI M3 editor-default provider catalog and operator usage | 部分完成：标准 `target-editor-host` 与 canonical Editor/Dev preset 已编译第一方 advanced-render provider catalog，编辑器默认 HGI 请求不再因 provider 未编译而必然降级；新增可执行使用指南；本条不关闭 M3 | 状态锚点 `render_plan18_hybrid_gi_m3_editor_target_provider_catalog_linked_usage_documented_static_gates_passed_wgpu_editor_visual_pending`。`zircon_app` editor target 通过聚合 feature 链接 advanced catalog，运行时仍按 render profile 只选择 HGI，不隐式激活 VG/Solari；`docs/zircon_plugins/hybrid_gi/usage.md` 记录 editor/runtime 启用、32/64/16 viewport 默认预算、Low/Medium/High 真实 4/8/16 ray direction、四段 graph、debug view、能力降级和 DynamicOnly 当前边界。只读 M4 审计确认当前 post-process 在 baked/HGI 同开时存在两次独立 GI 加法风险；随后 Plan 11 已落地版本化 CPU lightmap/probe/bake DTO、UV2 往返和 probe SH 三线性采样，但 GPU binding/fixture 仍开放，因此 `BakedStaticDynamic` 仍禁止申领完成。 | TDD RED：新 manifest/preset 测试初次 `2 failed`；EL-M3 结构合同初次 `1 failure + 1 error`。GREEN：HybridGI/editor preset 9/9，加 EL-M3 静态合同共 13/13。Windows validator 提交后被 coordinator 以 `cargo_reuse_pool_busy` 拒绝，Cargo 未启动；本切片未生成新 WGPU 图，也没有 RenderDoc `.rdc`，不得以静态门支持编辑器产品完成声明。 | 进入 Windows testing stage：编译 `target-editor-host` 和 `zircon_runtime` environment 聚焦合同；增加真实编辑器默认路径 WGPU 产品图与 report。EL-M3 继续 GPU lightmap/probe binding、双管线采样和真实 fixture，再做 HGI-M4 source ledger/composite/epoch；在此之前不得把单色 `RenderBakedLightingExtract` 当 lightmap。 |
| 2026-07-13 | AF-M4 OIT + planar reflection + SSS current-source combined acceptance | 已完成：AF-M4 三个重型可选项的启用产品行为、registered-but-inactive 精确基线、真实 WGPU Pass 与 PNG 均在严格 SSS 修复后的同一当前源码上复核通过；仅关闭 AF-M4，不代表 Plan 18 全计划完成 | 状态锚点 `render_plan18_af_m4_completed_current_source_oit_planar_sss_wgpu_product_png_passed`。OIT 继续以 fragment-store/resolve 替换透明排序；planar 继续由镜像 capture/main-camera 两次 submission、oblique clip/filter 与环境采样完成；SSS 使用 Deferred optional MRT、tile indirect、RGB normalized Burley 与 retained recombine。三者未激活时分别保持基线图/帧逐字节一致，不以 Pass 名或静态 shader 文本替代产品行为。planar 跨计划 failure 已通过 coordinator 原子移动为 `frameworks/03/fixed-2026-07-13-planar-filter-test-surface-export.md`。 | Windows 当前 test binary：OIT `render_product_advanced_lighting` 汇总 `2 passed / 0 failed / 1 ignored`；planar `render_product_planar_reflection` 汇总 `2 passed / 0 failed / 1 ignored`；SSS `subsurface` 汇总 `15 passed / 0 failed / 1 ignored`，插件 `5 passed / 0 failed`，SSS ignored exporter `1 passed / 0 failed`。三张已目检 PNG SHA-256：OIT `CC150ED7F2D3064623B4CB1355AE606FFEAEEC39123148606D816F4A6C4EE225`；planar `22FA4B7810ED9C85C286C4D7D3196BAC84964719D6680ADD93DFFAB4D1922AEC`；SSS `11A791EE707BBC7580914135B20409B2060E121204E31CB7716949178AABC344`。一次误用宽泛 `render_product_` filter 触发两个无关 HZB/camera-target 失败后已中止并改为三个 owner 精确 filter；它们不属于 AF-M4 acceptance。 | AF-M4 关闭。继续 Plan 18 仍开放的 AF-M1/AF-M2、HybridGI/DSRT、RenderDoc/product trace 与 broader runtime/workspace 门；不得据此标记整个 WGPU 渲染目标完成。 |
| 2026-07-13 | AF-M4 Slice 3 SSS strict review repair and current-source WGPU acceptance | 已完成：严格复审列出的 profile 自动生产、稀疏槽 ABI、Deferred diffuse/retained MRT、MSAA gate、逆投影世界尺度、真实 GPU indirect 审计和 feature-off 产品门均已修复；旧 SSS 完成记录仅保留为历史，本条为当前有效 acceptance | 状态锚点 `render_plan18_af_m4_sss_strict_review_repair_completed_current_source_energy_preserving_burley_wgpu_png_passed`。正常 frame submission 从可见 `MaterialAsset` 自动抽取内嵌 profile 与视图使用集合；profile id 直接映射 0..15 GPU 槽并由 active mask 阻止稀疏空槽；SSS 激活时 `deferred-lighting` 由 feature-owned MRT 输出可散射 diffuse 与 retained specular/emissive/volumetric 分量；setup 只分类 tile，scatter 对 RGB 分别做 64 样本 Burley、逆 VP 世界位置和 normal/depth/profile 边界拒绝，并用 `mix(center_diffuse, scattered, falloff)` 保持未散射能量；非 SSS/MSAA/无 profile 图不创建三段资源。修复 WGPU 发现的非 MRT render pass 仍携带两个 `None` color slot 导致 pipeline arity 不匹配，现仅在完整 diffuse+retained 对存在时追加 MRT attachment。 | Windows 当前源码 runtime `--no-run` 通过；`subsurface --nocapture --test-threads=1` 汇总 `15 passed / 0 failed / 1 ignored`，覆盖 registered-without-profile 精确基线、稀疏 profile、MRT、MSAA、GPU indirect workload 和真实 WGPU 皮肤球。插件 `zircon_plugin_rendering_subsurface_scattering_runtime` locked/offline 汇总 `5 passed / 0 failed`，`cargo metadata --manifest-path zircon_plugins/Cargo.toml --locked --offline --no-deps` 通过。ignored exporter `1 passed / 0 failed`；报告 `changed=27821`、brightened/darkened `581/657`、red-dominant gain `2047`、MAE `0.0534`、max `6`，Pass 含 `sss.setup,sss.scatter,sss.recombine`。PNG 原尺寸目检通过；PNG SHA-256 `11A791EE707BBC7580914135B20409B2060E121204E31CB7716949178AABC344`，报告 SHA-256 `A07A8805D03FF900B6086A995D65A12D44077E515E584C3737C85FB45D19C4ED`。 | SSS 切片关闭；planar failure 已原子回传 Frameworks 03。继续 AF-M4 OIT+planar+SSS 三项合并验收、feature-off 总图基线与 RenderDoc/product trace，再决定 AF-M4 总状态。 |
| 2026-07-12 | AF-M4 Slice 3 SSS strict review reopen | 重新打开：后续独立审查否决上一条 SSS testing-stage 完成结论；现有 WGPU 单采样图能执行，但正常提交缺少 profile 自动生产者、稀疏 profile id 与 GPU 槽不一致、Deferred MSAA 会进入非法绑定，且 80/20 scene-color 拆分不满足 Burley diffuse 语义，因此 AF-M4/SSS 均不得标记完成 | 状态锚点 `render_plan18_af_m4_sss_strict_review_reopened_profile_producer_sparse_abi_deferred_components_msaa_pending`。确认需从最低共享层修复：材质/场景提交自动抽取使用中的 SSS profile 与 view-level 使用集合；固定 0..15 profile id 到 GPU 槽并显式 active mask/诊断；Deferred Lighting 在 SSS 激活时输出独立 diffuse 与 retained 分量，禁止把 emissive/volumetric 固定按 80/20 散射；scatter 使用逆视投影、世界尺度和 RGB 独立 Burley 半径；feature-off 产品门不得注册 id16，正向门需验证真实生产抽取与执行审计。 | 独立只读审查共报告 P1 5 组/P2 3 组；本条 supersede 前一条“SSS Windows testing stage 已完成”，但保留其历史测试/PNG 作为发现问题前的执行证据，不再作为里程碑 acceptance。Planar 当前源码行为门 2/2、ignored exporter 1/1 与镜面地板 PNG 仍有效，不受本次 SSS 重开影响。 | 先完成 profile producer/index ABI/view early-out，再完成 Deferred 可选 MRT 与投影正确的 RGB Burley scatter；随后补 MSAA 合法性、feature-off/稀疏 profile/产品质量门、插件链和 WGPU 重新验收。 |
| 2026-07-12 | AF-M4 Slice 3 SSS Windows testing stage and WGPU product acceptance | 已完成：当前源码 compile、CPU/graph/材质 ABI 聚焦门、插件门、feature-off 逐字节基线、真实 Deferred WGPU 皮肤球差异门和 PNG 导出全部通过；仅关闭 SSS 切片，AF-M4 仍等待 planar failure 回传后整体收口 | 状态锚点 `render_plan18_af_m4_sss_completed_current_source_deferred_burley_skin_sphere_wgpu_png_passed`。测试阶段先发现并修正四个自有问题：Burley 积分测试的 f32 累积误差、recombine 源码断言名字漂移、`sss.scatter` 缺 `IndirectArgs` workload 声明、测试图注册时 descriptor 校验失败。修复后 workload 使用 `RenderGraphComputeDispatchExtent::IndirectArgs`，真实执行仍由 WGPU `dispatch_workgroups_indirect` 消费 `sss.indirect-args`。`zircon_plugins/Cargo.lock` 仅新增 runtime/editor 两个 SSS workspace package 条目，无依赖版本升级。 | Windows coordinator target `E:\cargo-targets\zircon-engine\pool\4bc372476874645c8e3379a95ac3760a4d783f6191c609ebe172a6b0431c1422`：runtime `cargo test -p zircon_runtime --lib --locked --offline --no-run` 通过；`subsurface` 聚焦 10 passed / 0 failed / 1 ignored；ignored 精确 exporter 1/1 通过。插件 target `F:\cargo-targets\zircon-engine\pool\5324af0d5c268f307e12b3cb5e5155999dd9e95baf3ff5fd2efae2677f5b137d`：runtime plugin 4/4、doc test 0/0 通过；锁文件回收后 `cargo metadata --manifest-path zircon_plugins/Cargo.toml --locked --offline --no-deps` 通过。产品报告：`changed_pixel_count=48188`、`mean_absolute_rgb_error=3.1372`、`max_rgb_error=25`、baseline/SSS submit CPU `281604/393227 us`，Pass 明确包含 `sss.setup,sss.scatter,sss.recombine`。PNG 已原分辨率目检；PNG SHA-256 `54D4C76E590142605A582CE2BE57BAFD8F081A8EB804F52A2311127E0002BEEE`，报告 SHA-256 `2DBC1646B99CF21774287AE20805448E8DA7131916EAA16F4E9B6E0F60E4E348`。 | 等 Frameworks 并行会话完成 planar camera-loop submission count 与 failure 生命周期回传；随后复跑当前源码 planar 产品门/ignored exporter，执行 AF-M4 OIT+planar+SSS 总验收并更新总状态。 |
| 2026-07-12 | AF-M4 Slice 3 SSS Deferred Burley implementation | 实施切片完成、里程碑测试阶段待执行：profile/材质 ABI、id 16 shading model、tile indirect 三节点、默认关闭插件、Forward 回退和皮肤球产品测试已落地；本条不声称 Cargo/WGPU GREEN，AF-M4 仍未完成 | 状态锚点 `render_plan18_af_m4_sss_deferred_burley_tile_indirect_implementation_completed_testing_pending`。新增 `SubsurfaceProfileData`、归一化 Burley CPU 核与 16 profile 表诊断；标准材质把 `subsurface_profile` 打包到 `data8.w`，G-buffer 以 material alpha=id16、normal alpha=profile index 传递；`sss.setup` 8x8 tile compact、`sss.scatter` 64 样本间接派发、`sss.recombine` 保留高光重组均由真实 WGPU executor/pipeline 实现。`rendering.subsurface_scattering` runtime/editor optional feature 默认关闭，只在 DeferredGeometry+DeferredLighting+非空 profile 时进图；Forward 明确复用 StandardPBR 并返回诊断。产品测试以内存 UV 球和 skin 材质覆盖无 profile 逐字节基线、三 Pass 执行、像素差异与 ignored PNG 导出。 | 本切片 touched Rust `rustfmt` 通过，`git diff --check` 无空白错误；模块文档 `docs/zircon_runtime/graphics/scene/scene_renderer/advanced_lighting/subsurface.md` 已同步。按 milestone-first 未在切片内运行 Cargo；当前共享源码的统一测试仍已知可能被并行 Navigation `[f32; 64]` serde 错误截断，需在 AF-M4 testing stage 通过 coordinator Windows lane 复核。 | 进入 AF-M4 Windows testing stage：先编译 runtime/plugin 受影响 crate，再跑 Burley/profile/include/plugin/graph 聚焦测试和两个 SSS 产品门，执行 ignored exporter并目检/哈希 PNG；随后合并 Frameworks 会话返回的 planar camera-loop 统计，完成 OIT/planar/SSS 三项总验收。 |
| 2026-07-12 | AF-M4 Slice 1 OIT current-source WGPU acceptance | 已完成：per-camera OIT 双节点替换、能力降级、mesh/sprite fragment store、bounded resolve、feature-off 精确基线和三层交叉透明产品图均闭环；仅关闭 OIT 切片，AF-M4 仍有 planar reflection/SSS | 状态锚点 `render_plan18_af_m4_oit_completed_current_source_wgpu_crossing_planes_png_passed`。`OitSettings`、`OitBufferPlan`、`oit.fragment_store`/`oit.resolve`、`zr_oit.wgsl`、mesh `fs_oit` specialization、Transparent3d sprite store、`rendering.oit` runtime/editor feature 与执行器注册均已接线。关闭或相机无设置时保留计划 09 `transparent-mesh`，开启后双节点替换排序路径；普通 Forward shader 不携带 OIT binding。 | Windows 当前源码 runtime lib check 通过；runtime lib-test `--no-run` 通过。CPU OIT 契约 3/3 通过；真实 WGPU feature registered-but-off 逐字节基线 1/1、三交叉片 OIT 差异门 1/1、ignored PNG exporter 1/1 通过。报告记录 `changed_pixel_count=12954`、`mean_absolute_rgb_error=2.2195`、`max_rgb_error=88`，OIT 图 Pass 为 `oit.fragment_store -> oit.resolve` 且无 `transparent-mesh`，预估双 buffer `7,372,800 + 921,600` bytes。PNG 已目检为右侧逐像素交叉混合、左侧对象排序丢失交叉关系；PNG SHA-256 `CC150ED7F2D3064623B4CB1355AE606FFEAEEC39123148606D816F4A6C4EE225`，报告 SHA-256 `45B5F1A85980A283D35B3413940345115AB7AE58FFE651BA00A8D29471CD35CD`。 | 继续 AF-M4 Slice 2 planar reflection 的 `planar.filter`、探针混合、插件与镜面地板产品图，再实施 Slice 3 SSS；AF-M4 三项全关图基线与 RenderDoc 留到里程碑总验收。 |
| 2026-07-12 | AF-M4 Slice 2 planar closed-form and derived-camera contract | 部分完成：镜像矩阵、WGPU 0..1 斜近平面、质量/更新档与普通 RT 相机派生合同已落地；filter、探针混合、插件和产品图仍未实现，AF-M4 未完成 | 状态锚点 `render_plan18_af_m4_planar_closed_form_camera_derivation_implemented_validation_external_ibl_blocked`。`PlanarUpdateMode` 默认硬切为 `OnDemand`；新增 256/512/1024 质量档、任意世界平面仿射反射矩阵、oblique near-clip 投影、`derive_planar_reflection_camera` 与 GPU 无关的 `PlanarReflectionUpdateState`。派生相机在主相机前执行、投递纹理、继承 probe layer mask、关闭 jitter，并通过 `ViewportCameraSnapshot::projection_override` 让 view/projection 与 LightGrid 重建消费同一斜裁剪矩阵；状态机固定首次/标脏/EveryFrame 捕获及成功后才清脏。planar 根模块已按结构约束拆成声明、矩阵、裁剪、相机和调度独立文件。就近测试覆盖 `R*R=I`、镜像点、反射面映射到 clip `z=0`、裁剪正反例、完整相机描述符和 OnDemand 状态转换。 | scoped rustfmt 与 `git diff --check` 已通过；模块说明新增 `docs/zircon_runtime/graphics/scene/scene_renderer/advanced_lighting/planar.md`。本条不声称 Cargo GREEN：runtime lib-test 当前仍被计划 11 owner 正在修复的 realtime IBL 测试导入/借用错误阻断；首次当前源码编译已把本计划缺口缩小为一个 LightGrid 测试夹具缺字段并已修复，新 planar 测试尚未执行。 | 等共享编译恢复后先运行 `render_planar` 聚焦测试并修正自有类型/数学误差；随后实现 `rendering.planar_reflections`、RT/OnDemand owner、`planar.filter` mip 链、`zr_environment.wgsl` 最高优先采样与镜面地板 WGPU 产品截图。 |
| 2026-07-12 | AF-M4 Slice 1 OIT current-source testing stage | 外部阻塞：OIT 源码与产品模块编译未报错，但 runtime lib/lib-test 被并行 realtime IBL 重构错误截断，真实 WGPU 测试与 PNG 尚未执行，OIT 不计完成 | 状态锚点 `render_plan18_af_m4_oit_current_source_compile_reached_product_module_external_realtime_ibl_blocked`。Windows test lane `7691667d6c6b4bbbb90672991e34abf9` 完成首次全依赖构建并进入 runtime lib-test；错误共 10 个，均位于 `environment/realtime_ibl_graph_plan{,/tests}.rs`、`realtime_ibl_wgpu_recorder.rs` 与当时并行修改的 rich-text parser 接口，日志没有 `render_product_advanced_lighting`/OIT 错误。随后生产 check lane `a5a6febdaf0949de833f4cd6579e068f` 复核为 4 个 realtime IBL owner 错误：`select_slot` 缺显式 lifetime、两处 `Option::then`、prefilter `Option` 上使用 `?`；rich-text 漂移已由其并行 owner 收敛。 | 两次命令均使用 `E:\cargo-targets\zircon-render18-afm4-oit-20260712`，日志为 `afm4-oit-product-compile.err.log` 与 `afm4-oit-check.err.log`；退出码均 101。scoped rustfmt 与 diff check 仍通过。本会话未修改计划 11 realtime IBL 文件，且 coordinator 拒绝业务 Session 写编号计划定义，不制造半套 handoff 链接。 | 继续可独立的 AF-M4 planar/SSS 切片；realtime IBL owner 稳定后复用已填充 target 快速重跑 `render_product_advanced_lighting` 与 ignored exporter。只有当前源码 Pass 断言、像素差异、PNG 目检与报告哈希通过后，才追加 OIT 完成记录。 |
| 2026-07-12 | AF-M4 Slice 1 OIT transparent-store product integration | 部分完成：生产接线与计划指定产品测试已实现，当前源码真实 WGPU 测试/截图仍在执行，AF-M4 未完成 | 状态锚点 `render_plan18_af_m4_oit_mesh_sprite_fragment_store_product_test_implemented_wgpu_acceptance_running`。`OitSettings`/能力降级/按视口双 buffer、`oit.fragment_store`/`oit.resolve` 图替换、mesh 专用 `fs_oit` shader specialization、Transparent3d sprite fragment-store 与 feature-off Forward 源隔离已接线。新增计划指定的 `graphics/tests/render_product_advanced_lighting.rs`，以同一组红/绿/蓝三张交叉透明平面对比排序透明和 OIT，并包含 feature registered-but-off 逐字节基线、真实 Pass 名、有效 feature、像素差异、PNG 与逐 Pass CPU 耗时/内存报告。静态审查同时确认 Zircon 使用 `LessEqual` 正向 Z，已将 CPU/WGSL resolve 从参考引擎反向 Z 假设修正为近到远排序。 | scoped `rustfmt` 与 `git diff --check` 通过；普通 Forward template/source 明确不含 `zr_oit.wgsl`/`fs_oit`，OIT pipeline 仅由专用 source specialization 生成。Windows 定向 `cargo test -p zircon_runtime --lib render_product_advanced_lighting --locked --offline --jobs 1 --no-run` 正在 `E:\cargo-targets\zircon-render18-afm4-oit-20260712` 执行，本条不声称 Cargo/WGPU GREEN，也尚无 current-source PNG。 | 等定向编译结束后运行非 ignored 产品门和 ignored PNG exporter，目检 `docs/tests/runtime/render/plan18_oit_three_crossing_transparent_planes_sorted_vs_oit_wgpu_20260712.png`，记录哈希/差异/Pass 耗时并追加测试阶段完成或阻塞记录；随后才能关闭 OIT 切片并继续 planar reflection。 |
| 2026-07-12 | AF-M3 slices 1-3 current-source compiled-scene acceptance | 已完成：froxel media/scatter/integrate/apply/temporal、图顺序、质量档、feature-off、性能计数和空间光柱产品门全部闭环；Plan 18 仍继续 AF-M4 | 状态锚点 `render_plan18_af_m3_completed_current_source_compiled_scene_window_light_shaft_wgpu_passed`。修复 ShadowAtlas 多 cascade 录制时共用 scene uniform 的提交期覆盖：每个 atlas slot 改为独立 immutable initialized uniform + scene bind group，确保每个 render pass 保留自己的 view-projection。HG 使用 light-travel 与 camera-outgoing 夹角。compiled-scene 产品保留 unshadowed 诊断、shadowed 最终帧、归一化梯形光柱走廊和左右对照带，不接受均匀全屏雾色。 | 当前源码 `cargo build -p zircon_runtime --lib --locked --offline --jobs 1` 通过；volumetric 插件当前测试二进制 8/8 non-ignored 通过、1 ignored；ignored compiled-scene WGPU exporter 1/1 通过。报告：3 dispatches、44,400 groups、624 uploaded bytes、1 LightGrid light、524,160 non-empty clusters、1 shadow atlas write、4 caster draws；全帧 8,475 brighter pixels，光柱区 4,241/4,398 brighter，光柱平均 luma `+76.264`、对照带 `+2.758`、空间对比 `73.506`。PNG `docs/tests/runtime/render/plan18_volumetric_compiled_scene_window_light_shaft_perf_wgpu_20260711.png` 已目检为窗口投射的集中亮柱，SHA-256 `2AF87DABF39EA5485F579D6300615E6A5DA380A166C5AFF28DF84A03EBACDB0D`；报告 SHA-256 `9AC7B7B29BA80A6F4311E71FE1732479B46B571075E42D009622D546EFAA969C`。运行时 lib-test 另一次链接因共享 E 盘空间不足报 `LNK1180`，不计 broad green。 | AF-M3 关闭；继续 AF-M4 OIT、planar reflection、SSS，再补 RenderDoc 与 broad runtime/full-workspace acceptance。前一条“外部 project-document 阻塞”状态由本条 supersede。 |
| 2026-07-12 | AF-M3 compiled-scene window light-shaft spatial product gate | 部分完成：空间验收合同与合成帧正反例已实现；当前源码 WGPU 产品运行仍被并行 project-document 可见性错误阻塞，AF-M3 未完成 | 状态锚点 `render_plan18_af_m3_window_light_shaft_spatial_gate_implemented_external_project_document_visibility_blocked`。产品帧比较新增归一化梯形窗口光柱走廊和左右遮挡对照带，报告记录两区采样数、光柱亮化像素、平均 luma 增量及光柱/遮挡对比；门槛除既有全帧 changed/chromaticity/RGB delta 外，还要求光柱区超过 20% 像素亮化、平均增量大于 `1.5`，且比遮挡区至少高 `1.5`。新增 64x32 合成帧正例证明集中光柱可通过，反例证明全屏均匀雾色即使满足全帧差异也必须失败。同步更新 volumetric 模块说明，删除“产品 harness/perf counter 未实现”的过期描述。 | `rustfmt --edition 2021 zircon_plugins/rendering/features/volumetric_fog/runtime/src/wgpu_product_tests.rs` 通过；源码静态检查确认 gate、正反例和报告字段已接线。当前未声称测试 GREEN：共享源码仍在 `project_document/codec.rs` 的 `encode_document`/`decode_document` `pub(super)` 与 parent 更宽 re-export 之间产生 2 个 `E0364`，该路径不属 Render 18 owner；现存 compiled-scene PNG/report 来自 hard-cut 前构建，不作为本切片 current-source 证据。 | 等外部 owner 收敛 codec 可见性后，先跑两个合成帧 gate 测试和插件图顺序测试，再运行 ignored compiled-scene WGPU export；只有空间对比门通过、PNG 目检和新报告哈希完成后才接受 AF-M3 产品门。 |
| 2026-07-12 | AF-M3 Slice 2 compiled-scene sky/volumetric ordering hard cut | 部分完成：生产路径与合同已按计划收束；当前 Cargo 绿灯和 shadowed 产品图仍被并行资产文档重构阻塞，AF-M3 未完成 | 状态锚点 `render_plan18_af_m3_sky_volumetric_ordering_hard_cut_implemented_external_project_document_visibility_blocked_product_pending`。`preview-sky` 从 `DepthPrepass` 移到 `Transparent3d` 且位于 `transparent-mesh` 前，读取 `SCENE_DEPTH`/`VOLUMETRIC_INTEGRATED`、load/store 写 `SCENE_COLOR`；sky triangle 改 far depth `1.0` + `LessEqual` + no depth write。Forward opaque 与 deferred lighting 作为图内首写声明 clear/store，由 camera-stack policy 在帧首 `scene_clear` 后转为 load/store；deferred 无几何像素 `discard`，删除 `FINAL_COLOR` 背景采样和 `sky.preview-final-color` executor。产品场景材质同时修正为 room receiver-only、window frame caster。 | 旧二进制 TDD RED 精确复现 `expected volumetric.integrate before preview-sky`；最新可用旧插件二进制仍复现 pre-fix `opaque-mesh loads transient attachment scene-color before any producer writes it`，证明它不能作为当前源码验收。首次当前源码测试完整编译并进入测试，暴露并定位该 owned graph RED；已修正首写 clear/store 合同并加入 forward/deferred attachment 断言。后续重建曾因共享 E 盘耗尽报 `os error 112`。容量恢复后，资产迁移 singular helper 导出漂移已由外部 owner 收敛；后续受控重建依次在并发编辑中的 `asset/migration/transaction/recovery.rs` 截断文本、`project_document` material/model 可见性和新增 `project_document/codec.rs` 可见性失败。最新失败缩小为 2 个 `E0364`：`decode_document`/`encode_document` 仅 `pub(super)`，parent 却向更宽 assets 层级 re-export。上述路径均非 Render 18 owner，不越权修改；当前源码测试尚未执行，不计 GREEN。sky/deferred WGSL 独立 Naga parse+full validation 2/2、scoped rustfmt 与 `git diff --check` 通过。 | 等资产文档 owner 收敛可见性边界后重跑 plugin graph/order、default pipeline、attachment/deferred WGPU tests；随后运行 shadowed compiled-scene window light-shaft ignored WGPU export，检查差异指标并刷新 PNG/report。 |
| 2026-07-11 | AF-M3 Slice 3 graph/quality/temporal history | 部分完成：三节点图接线、质量档、High 档持久 D3 history/jitter/reprojection 与真实 WGPU temporal 对比证据已闭环；AF-M3/Plan 18 整体仍未完成 | 状态锚点 `render_plan18_af_m3_volumetric_graph_quality_temporal_wgpu_png_passed_scene_perf_plugin_tests_pending`。`ShaderQualityTier` 进入编译选项与 cache key，Low/Medium/High/Ultra 驱动 48/64/96/96 slices；图 workload 新增 `FroxelGrid`/`FroxelGridXy`，执行期从 D3 resource desc 派生 dispatch。`SceneFrameHistoryTextures` 新增仅 High+temporal 分配的 RGBA16F D3 scattering history，按质量/尺寸变化失效，graph 前绑定 external、graph 后校验并 copy。LightScatter 复用 06 camera jitter，Z 使用 base-5 Halton，按 previous clip/camera/depth 重投影并以 extinction discontinuity 拒绝历史。`rendering.volumetric_fog` 清单、runtime/editor 模块、三 pass descriptor/executor 与 feature-off contract 已接线；插件 runtime manifest 显式启用 `zircon_runtime/graphics`。 | 独立 shader acceptance 4/4：forward/deferred/sky/LightScatter Naga parse+validation。生产 runtime lib `cargo check` 在本轮改动后通过。真实 WGPU temporal contract 1/1：16x8x8、dispatch 4,2,2，1024/1024 froxels 累积；history RGB distance `1.375000 -> 0.137634`，extinction rejection max error `0.000000`。生成并目检 770x128 `docs/tests/runtime/render/plan18_volumetric_temporal_reprojection_wgpu_20260711.png`，PNG SHA-256 `0666A56364FD645A768A5E37A8CBED790F4E645C23A5B5795DB8409F811852AA`，报告 SHA-256 `1FB2C52EC7E01EC49ACAED85893012A6F3679372C91F60B95B1FB7EC1ED47540`。插件结构审计与 locked/offline metadata 通过。标准 Cargo integration 被并行 UI/asset 编译错误挡住；插件缺失 graphics feature 的 RED 已修复，但 post-fix 聚焦编译/测试在重复资源上限内未完成，不计 GREEN。 | 继续完整 compiled-scene 窗口光柱抓帧、`render_perf_*` froxel upload bytes/dispatch counters、post-fix 插件 5 项图契约测试和 broad runtime/full workspace。当前 slice 不外推 AF-M3/Plan18 完成。 |
| 2026-07-11 | AF-M3 Slice 2 LightGrid/ShadowAtlas volumetric scatter and integrate | 部分完成：HG 体积散射、共享 LightGrid/ShadowAtlas 消费、闭式前向 3D 积分与 shading apply 已取得真实 WGPU 光柱证据；AF-M3/Plan 18 整体仍未完成 | 状态锚点 `render_plan18_af_m3_volumetric_light_scatter_integrate_shadow_wgpu_png_passed_graph_temporal_pending`。框架层新增 normalized Henyey-Greenstein 与 homogeneous step 闭式契约。`FroxelLightScatterPipeline` 组合既有 `zr_light_grid.wgsl`/`zr_shadow.wgsl`，直接消费 `GpuLightData`、z-bin/tile mask、Depth32Float ShadowAtlas、comparison sampler、slot/globals，只遍历 grid 选中的 directional/point/spot/rect lights；输出 RGB 为 media scattering coefficient * incident radiance * HG phase，A 保留 extinction。`FroxelIntegratePipeline` 每个 XY invocation 沿 Z 前向执行 Beer-Lambert 闭式积分，每个 RGBA16F 3D slice 写累计 radiance/transmittance；不读取场景色。`zr_volumetric_apply(color, uv, view_depth)` 固定消费 group1 bindings 26/27 并执行 `color*T+radiance`，遵守无 post 节点约束。三个 pipeline 暂保持 crate-private，等待 graph executor owner，不制造临时 public API。 | 首轮编译 RED 关闭测试导入边界；设备 RED 依次关闭 WGSL u32/i32 坐标组合和 reversed-Z 阴影夹具语义；后续严格计划审计将 2D integrate+compose 硬切为 3D integrated product + shading include。最终 `render_volumetric` 10/10（7482 filtered），含 LightScatter/Integrate/apply Naga parse+validation、真实 WGPU media/scatter/integrate/apply/ShadowAtlas 链；ignored exporter 1/1。生成并目检 `docs/tests/runtime/render/plan18_volumetric_light_scatter_integrate_shadow_wgpu_20260711.png`：256x128，media/scatter dispatch `4,2,2`，integrate/apply `2,1`；左 shadowed RGB `0.036987,0.043182,0.061676`，右 unshadowed RGB `1.393555,0.844238,0.232788`。PNG SHA-256 `D836AB5300F066E2BAFA9CB03E61375CC2AC62107BB8FADB8599B968AA59D681`，报告 SHA-256 `E2298952B0561FAB9E4AB58EB30520FFAFE9D41EE03AF7731AC2DCBFF6489A37`。 | 本切片 compute/math owner 关闭；继续三节点 graph resources/executors、scene extract/layer mask、temporal/history、插件 `rendering.volumetric_fog`、feature-off graph absence、perf counters 与完整窗口光柱场景。当前 PNG 是真实 WGPU compute 产品但非 compiled-scene 产品，不外推 AF-M3/Plan18 完成。 |
| 2026-07-11 | AF-M3 Slice 1 volumetric contracts and froxel media injection | 部分完成：框架契约、共享 Volume 组件和真实 WGPU media inject 已闭环；AF-M3 与 Plan 18 整体仍未完成 | 状态锚点 `render_plan18_af_m3_volumetric_media_inject_wgpu_png_passed_scatter_integrate_pending`。新增 `VolumetricFogSettings`、`FogVolumeData`、`FroxelGridParams` 与 Low/Medium/High `160x90x48/64/96` 质量表；`lighting.volumetric-fog` 复用既有 `VolumeComponentRegistry`/`VolumeEvaluator`。scene renderer 新增 `Rgba16Float` D3 froxel media compute owner，`4x4x4` workgroup 将全局指数高度雾与局部 AABB 介质合并，RGB 写 scattering、A 写 extinction；Low 档明确关闭局部体积。同步修复共享 Volume descriptor 中 motion-blur samples 与 SSR max-steps/temporal 默认值漂移，并将重复注册测试从数组位置假设改为显式组件 ID。 | WGSL Naga parse/validation 通过；初始 volumetric 聚焦测试 5/5，最终 `render_volume` 范围回归 25/25（7459 filtered），ignored WGPU exporter 1/1。生成并目检 `docs/tests/runtime/render/plan18_volumetric_media_inject_wgpu_20260711.png`：256x128、GPU froxel `16x8x8`、dispatch `4,2,2`；左侧 global-only 平均 extinction `0.032955`，右侧 global+local 为 `0.152908`。PNG SHA-256 `D40B09EFA5423E2C0C458D4E821139D459BA29B38C04B3DC191C8F30332629FA`，报告 SHA-256 `2C609B75B6474AE82E7F381FF1080BCD2B0CB949794145BA657261EF9B4EFF0D`。 | 本切片关闭；继续 AF-M3 `volumetric.light_scatter`、ShadowAtlas/light-grid 消费、HG 相位、`volumetric.integrate`、合成、temporal、graph executor 与插件 feature 接线。Frameworks 会话仍持有 manifest，当前不越权写 `rendering.volumetric_fog`；不外推 AF-M3/Plan 18 完成。 |
| 2026-07-11 | Cross-plan HGI depth/normal/support spatial denoise | 完成：当前帧 trace radiance 已在 temporal 前执行 edge-aware probe-grid 过滤并取得真实 WGPU raw/filtered 对照图；Hybrid GI V1/Plan 18 整体仍未完成 | 状态锚点 `render_plan18_hybrid_gi_depth_normal_support_spatial_denoise_wgpu_png_passed`。resolve 使用 3x3 `1-2-1` kernel，只接纳 valid、同 source、同 10-bit support、depth delta <= 0.02、normal dot >= 0.75 的邻居；越界跳过，只更新中心 radiance，中心 depth/source/support/normal/confidence 元数据不变。该实现借鉴 Lumen `SpatialFilterProbe.hlsl` 的 edge-aware 邻域意图，但明确限于 Zircon trace-tile packet，不宣称完整 world-space plane filter。 | Naga parse/validation 通过；空间合同 1/1、WGPU noise/edge 1/1、resolve/temporal 8/8、插件非 ignored 98/98 且 16 ignored、主场景产品 1/1。ignored exporter 1/1，生成并目检 `docs/tests/runtime/render/plan18_hybrid_gi_depth_normal_support_spatial_denoise_wgpu_20260711.png`：257x128，左右噪声方差从 `0.124567` 降到 `0.013245`，red/blue support 主导色保持分离。PNG SHA-256 `1FF7851F148B66840B66D34EFADFD746419E23EAC03D007345EE4BB039C080D1`，报告 SHA-256 `80BECCDB34562B5D1FAE6B50AA1B556030FFEBF5CA12EC820C352E06B939DA3D`。 | 本切片关闭；继续 editor-host manifest/Editor WGPU、真实 RenderDoc、full runtime/full CI。Plan 18 AF-M1..AF-M4 仍按各自地基推进，不外推整体完成。 |
| 2026-07-11 | Cross-plan HGI normal-aware temporal rejection | 完成：GBuffer normal 已进入 scene/trace/temporal validity 链并取得真实 WGPU 对照图；Hybrid GI V1/Plan 18 整体仍未完成 | 状态锚点 `render_plan18_hybrid_gi_normal_aware_temporal_rejection_wgpu_png_passed`。scene prepare 的 single/MSAA Shader 将同一 depth sample 的 normal 编为 6-bit octahedral code；scene tile flags bits 8-13 承载该编码。trace tile 新增独立 normal word，固定 packet 从 512 words/2048 bytes 升为 576 words/2304 bytes。temporal metadata Y 精确保存 `source * 64 + normal_code`，resolve 新增 decoded-normal dot >= 0.75，且不牺牲既有 10-bit local support signature。 | scene-normal single/MSAA WGPU 2/2、trace handoff 3/3、temporal resolve 5/5（含 opposite-normal disocclusion）、executor contract 4/4、public graph/frame 1/1；插件非 ignored 96/96、15 ignored。ignored exporter 1/1，生成并目检 `docs/tests/runtime/render/plan18_hybrid_gi_normal_aware_temporal_rejection_wgpu_20260711.png`：257x128，3x3 interior 中 opposite normal 5 rejected、matching normal 4 retained，另有 7 个 reprojection-border pixels。PNG SHA-256 `F90381BA2C9B675DE93564A82E14E01DF4B1E3A82F6976DEEDEB8A6A097075C5`，报告 SHA-256 `6199172970DE762709F688CC1B3470951D9C6A45E53C96B23C8799A6258CEF15`。 | 本切片关闭；继续 spatial denoise、editor-host manifest/Editor WGPU、真实 RenderDoc、full runtime/full CI。Plan 18 AF-M1..AF-M4 仍按各自地基推进，不外推整体完成。 |
| 2026-07-11 | Cross-plan HGI true multi-direction trace quality | 完成：Low/Medium/High 的 4/8/16 rays 已成为真实独立方向追踪并取得 WGPU compute matrix 视觉证据；Hybrid GI V1/Plan 18 整体仍未完成 | 状态锚点 `render_plan18_hybrid_gi_multi_direction_trace_quality_wgpu_matrix_passed`。`trace_probe_tiles.wgsl` 保留方向 0-7 ABI，新增 8-15 的确定性中间方向；每条 ray 独立执行 direction-aware HZB march、Surface Cache depth/radiance sampling，再由 tile 等权聚合。Low 至少 march 一个 texel。测试产品 owner 位于 `dispatch_probe_trace_tiles/tests/multi_ray_quality.rs`，不扩大生产模块可见性。 | 初始 RED 固定 Low/High 同为 `0x0f0f0f`；实现后额外方向 WGPU 1/1、精确 Shader 回归 9/9、插件非 ignored 95/95 且 14 ignored。ignored exporter 1/1，生成并目检 `docs/tests/runtime/render/plan18_hybrid_gi_multi_direction_trace_quality_wgpu_20260711.png`：257x128，左 Low 4 hits、右 High 16 hits、16 cells 均不同；6x 统一显示曝光不参与断言，报告保留原 packed RGB。PNG SHA-256 `06D3156183BC7B75C8F1876CC108162D1D020AD5BFB2FD41000049344EA1B4BF`，报告 SHA-256 `5CFBDA9F48222F1FCC043FDE0351C5F307CE0749B420C0F24506F57077A1BBC3`。 | 本切片关闭；继续 editor-host manifest/Editor WGPU、真实 RenderDoc、spatial/temporal denoise、full runtime/full CI。Plan 18 AF-M1..AF-M4 仍按各自地基推进，不外推整体完成。 |
| 2026-07-11 | Cross-plan HGI quality-scaled trace rays and HDR history copy | 完成：质量档位驱动真实 tile rays，并取得 HDR history 正常复制后的 WGPU 产品图；Hybrid GI V1/Plan 18 整体仍未完成 | 状态锚点 `render_plan18_hybrid_gi_quality_scaled_trace_rays_hdr_history_wgpu_png_passed`。Low/Medium/High quality tracing budget 映射为 Surface Cache/Voxel tile 的 4/8/16 rays，无预算维持 8；预算沿 `execute_prepare -> create_buffers -> scene_prepare_resources -> scene_prepare_trace_tiles` 单一 owner 链传递。首轮 WGPU RED 暴露 HGI graph lighting `Rgba8UnormSrgb` 向 `Rgba16Float` history copy 非法；`HYBRID_GI_LIGHTING` 现固定为 HDR `Rgba16Float`，copy guard 只接受同格式单采样源。 | quality mapping 1/1；插件非 ignored 94/94、13 ignored；ignored WGPU export 1/1。新 PNG `docs/tests/runtime/render/plan18_hybrid_gi_quality_scaled_trace_rays_wgpu_20260711.png` 已目检非黑：192x128、1813 visible pixels、max luma 140.43、4 HGI passes、2 trace tiles、dispatch `[1,1,2]`、High=16 rays、2 screen probes、2 radiance cache、1 resident page、1 voxel clipmap；SHA-256 `DA9454E744E6691C98A71AF8E51C0BFBC6FB22A16328175DBF1EAFEAB529E53C`。Runtime unit-test binary 被外部 reflection-probe `stats` E0425/E0282 阻断，不计 green。 | 继续 editor-host provider manifest/Editor WGPU、真实 RenderDoc 捕获、broader screen-probe quality/denoise、full runtime/full CI；不外推为 Hybrid GI V1 或 Plan 18 整体完成。 |
| 2026-07-11 | Cross-plan HGI editor default, runtime opt-in, and capability fallback | 部分完成：行为与聚焦测试已闭环；editor-host 高级插件清单及编辑器 WGPU 产品验收待完成 | 状态锚点 `render_plan18_hybrid_gi_editor_default_runtime_opt_in_capability_fallback_behavior_passed_manifest_pending`。Editor 默认 render bundle 和 retained viewport quality profile 请求 HGI、不隐式请求 VG；提交 settings-only extract 时启用 HGI并只补齐零预算。Runtime 默认 bundle 不启用 HGI，继续要求 project/profile 显式选择。VG/HGI 允许 capability/provider 不足时经 runtime plan 干净禁用，AA/Solari 和 compiled-pipeline 要求保持严格。 | capability validation 6/6、compile options 3/3、runtime profile bundle 3/3、feature resolve 3/3、profile revalidation 1/1、advanced capability/provider reason 1/1、Editor controller 1/1、App editor/runtime default 1/1、显式 advanced catalog 下 Editor HGI-only registration 1/1；scoped rustfmt 通过。 | `target-editor-host` 尚未组合 `first-party-advanced-render-runtime-plugins`；该 manifest 由活动 Frameworks 03 会话持有，本切片未越权修改。完成清单接线后补真实 Editor WGPU 截图并升级最终状态；不宣称 Hybrid GI V1 或 Plan 18 整体完成。 |
| 2026-07-11 | Cross-plan HGI/Lumen-style authored probe/trace hard cut | 完成: authored DTO/fixture 生产链硬切并取得 scene-representation-only WGPU 产品证据；Hybrid GI V1 与 Plan 18 整体仍未完成 | 状态锚点 `render_plan18_hybrid_gi_authored_probe_trace_hard_cut_scene_representation_only_wgpu_png_passed`。公共 extract 只保留六个设置字段；删除 authored probe/trace DTO、generic visibility planner、runtime/plugin extract adapters、GPU fallback、plugin post-process snapshots 和未接线旧 fixtures。GPU prepare 与 runtime post-process 统一消费 scene representation、`HybridGiResolveRuntime` 和 `RenderHybridGiPreparedFrame`；统计 source 改为 `SceneRepresentation`。 | 插件 target-client production check、`cargo check --tests` 均通过；六项 prepared/scene-truth 可执行契约测试 6/6 通过。ignored WGPU export `export_hybrid_gi_scene_representation_only_forward_deferred_wgpu_png` 1/1 通过并已目检：`docs/tests/runtime/render/plan18_hybrid_gi_scene_representation_only_forward_deferred_wgpu_20260710.png` 为 771x257，8 格各 1540 visible pixels、4 HGI passes、1 Surface Cache page、2 trace tiles、1 screen probe、1 voxel clipmap；Forward+/Deferred directional/point/spot/emissive 中心 RGB 对应一致，SHA-256 `1F4CC3565B9E3B7C3F8B46D7B6B792E12EAABDF49D11F0A16AFD8D537F3970F6`。root runtime no-run 的 4 个本切片断言已修正，外部 `ViewProjectionMatrixPair` E0433 未计 green。 | 继续 editor default/runtime opt-in、capability fallback、RenderDoc、broader quality 与 full runtime/full CI；不宣称 Hybrid GI V1 或 Plan 18 整体完成。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style dynamic light matrix and authoritative radiance ownership | 部分完成: HGI V1 方向光/点光/聚光/自发光 Forward+/Deferred 产品矩阵完成；Plan 18 七项高级光照及完整产品化仍未完成 | 状态锚点 `render_plan18_hybrid_gi_dynamic_light_matrix_forward_deferred_authoritative_radiance_wgpu_png_passed`。Card/voxel capture 负责空间 direct/emissive radiance；trace/completion 对 authoritative Surface Cache/Voxel 与显式 RT radiance 直接消费，scene seed 仅保留给 synthetic legacy fallback。`update_completion_scene_radiance.wgsl` 从 1168 行 completion owner 拆出，主 WGSL 降至约 913 行。产品 RED 暴露 shared Deferred 三目标 GBuffer 丢失 emissive；新增 graph resource 与固定 `Rgba16Float` `gbuffer-emissive`，GBuffer location 3 写入，deferred lighting binding 5 读取并在所有 built-in/plugin shading-model 后统一相加。 | 已接线 HGI 回归 6/6；target-client cargo check 通过；no-debug plugin test build offline 通过。真实 WGPU 2x4 export 1/1，八面板均执行 4 个 HGI pass并具备 page/trace/screen-probe/voxel 资源。Forward+/Deferred 四列中心 RGB 完全一致：directional `51.48,42.90,41.33`、point `41.19,50.87,42.75`、spot `41.41,42.95,51.13`、emissive `48.66,25.07,15.71`。矩阵 PNG 已目检，771x257，SHA-256 `1F4CC3565B9E3B7C3F8B46D7B6B792E12EAABDF49D11F0A16AFD8D537F3970F6`。替代旧 seed 语义的 spatial-radiance export 也通过 1/1，385x128、warm/cool center delta 5.42/5.50、SHA-256 `EB9C8500CF2ED4F8FFEC8340C4C92FF7A6C04DFE5791BC50D62AFF78BDF29700`。首次 root runtime lib-test 独立构建在 15 分钟超时前未执行；预热 no-debug 构建随后完成当前 7439-test 二进制，本切片 Deferred/emissive 定向回归 8/8 通过。默认 Deferred 整图顺序用例仍因 Bloom/Exposure 旧预期失败，与本切片资源 ABI 无关；不宣称 full runtime/full workspace green。 | 继续 authored probe/trace fixture 清退、editor default/runtime opt-in、capability fallback、RenderDoc、broader tracing quality、full runtime/full CI；Plan 18 AF-M1..AF-M4 仍开放。SDF/GDF 不是 Hybrid GI V1 完成门槛。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style localized Surface Cache/Voxel temporal validity | 部分完成: M3 局部 support history validity 完成；Hybrid GI V1 与 Plan 18 整体仍未完成 | 状态锚点 `render_plan18_hybrid_gi_local_surface_voxel_support_history_wgpu_png_passed_forwardplus_productization`。trace tile ABI 从 6 words 扩为 7 words，64 tiles 的 trace packet 从 448 words/1792 bytes 扩为 512 words/2048 bytes；新增 local support signature word。Surface Cache hit 对 page id/owner card/atlas slot/radiance/bounds/radius 做签名，Voxel fallback 对 clipmap + exact cell id/radiance/occupancy 做签名，无驻留 radiance 的 screen hit 对局部 tile/depth range 做签名；resolve 使用签名低 10 bits 进行逐像素 history 判定。history allocation 与 content validity 分离：`FrameInputsChanged` 保持通用 previous unavailable 和 TAA invalidation，但不再重分配尺寸/管线/绑定兼容的 texture set；HGI pair 以独立 valid bit 只在 lighting + metadata 完整复制后可用。 | production trace WGPU 3/3，精确覆盖 page radiance、page bounds、voxel occupancy 改变后 resolve 实际消费的低 10-bit signature 变化；temporal resolve WGPU 5/5，其中 4x4 localized case 接受 3 个 unchanged interior pixels，仅拒绝 adjacent changed-support pixel。HGI executor 19/19；localized product export 1/1、既有 temporal export 1/1、main-scene export 1/1。`plan18_hybrid_gi_localized_support_history_wgpu_20260710.png` 已目检：385x128、两半各 1918 visible pixels、stable max luma 141.93、changed max luma 155.91、unchanged-left average RGB delta 3.051、changed-right 9.550；stable/changed history 均为 handle 1，changed public status=`FrameInputsChanged`，HGI pair copied=true，graph pass=4；SHA-256 `4C90EDB80CF871A33D169972ECAE7F2771E37D5E431BE2434E31E832E7B68CA3`。并行 Runtime feature-profile hard cut 期间插件构建以命令行 `--features zircon_runtime/target-client` 通过；root lib-test 被外部根模块迁移错误阻断，不计 green。 | 继续 Deferred/Forward+ parity、directional/point/spot/emissive 完整矩阵、authored probe/trace fixture 清退、editor default/runtime opt-in、capability fallback、RenderDoc、full runtime/full CI。Hybrid GI V1 Assumptions 明确 software voxel clipmap 是首版 miss fallback，SDF/GDF 不是 V1 前置或完成门槛。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style temporal reprojection and scene/support history rejection | 部分完成: motion/history validity 与基础 temporal stability 切片完成；Plan 18 七项高级光照及完整 Lumen-style 路径仍未完成 | 状态锚点 `render_plan18_hybrid_gi_temporal_motion_scene_signature_rejection_wgpu_png_passed_sdf_forwardplus`。新增单采样 `Rgba16Float` Hybrid GI temporal metadata graph/persistent history product，RGBA 分别保存 hit depth、trace source、scene signature、confidence；scene packet 以确定性签名覆盖光照/材质/场景输入，trace schedule 传递签名，resolve 读取 scene velocity 和历史 lighting/metadata 执行 UV 重投影。历史接受条件覆盖可用性、屏幕边界、motion、depth、trace source、scene signature 与 luma；接受后执行 current-neighborhood clamp 和 confidence-weighted accumulation，拒绝后使用当前 GI 并重置 confidence。历史 lighting/metadata 作为一对复制，history pass 标记 side effect；首帧仍绑定清零历史资源，由 `history_available` 决定是否采样。post-process stack 在 HGI 激活时保留 scene velocity。 | TDD RED 先确认 temporal graph/WGSL/scene signature 合同缺失；GREEN 后三项真实 WGPU readback 覆盖 static accumulation、motion rejection、scene signature/source rejection。当前 HGI executor filter 18/18、注册 1/1、public stats 1/1、时间产品导出 1/1、既有 main-scene 产品导出 1/1。`plan18_hybrid_gi_temporal_history_rejection_wgpu_20260710.png` 已目检：385x128，左右各 1813 visible pixels；静态 warm 帧 previous available 且 pair copied，变更 cool light 帧 previous unavailable / `FrameInputsChanged` 且 pair copied，二者 HGI graph pass 均为 4；stable center R/B=18.30/13.10，rejected center R/B=13.78/17.80，无可见 warm ghost；SHA-256 `06A9481FF5E03790172ECE61291C531C5EF5B29BA6EBA34CF3387B0E8415986D`。启用 temporal profile 后主场景产品图刷新为 max luma 141.14、SHA-256 `848B0E9B1B291489D3D69D30E13200B3F3B9359FD982C5BE93DA0D943846EDF3`，本行取代旧主场景记录中的旧图像指标。root runtime 定向测试在执行前被无关 `ui/sdf_render/tests/layout_placement.rs` 私有 `SdfUvRect` 字段 E0451 阻断，不计通过且不宣称 full workspace green。 | 继续 localized material/light/topology per-pixel invalidation、SDF/GDF、multi-ray screen probes、Deferred/Forward+ parity、RenderDoc、full runtime/full CI；Plan 18 AF-M1..AF-M4 与整体渲染方案仍开放。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style command-ordered main-scene HZB march and world-space radiance lookup | 部分完成: graph-owned HZB 已完成 per-mip command ordering、single/MSAA depth variants、tile-footprint coarse-to-fine screen march、Surface Cache hit 与 Voxel clipmap fallback；Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_main_scene_hzb_coarse_to_fine_surface_cache_voxel_wgpu_png_passed_motion_sdf_deferred`。runtime 将每个 mip 的 `HzbParams` 固化到 immutable upload buffer，并在各 dispatch 前编码 copy，消除多 mip 共用 uniform 时最后一次 queue write 覆盖前序参数的风险；新增 `hzb_build_msaa.wgsl` 在 mip0 先归约全部 depth samples。HGI 使用 jittered current-frame inverse VP，从 tile 实际像素 footprint 选择 base mip，按方向对齐与整段 ray-depth disjoint 执行安全 coarse skip，overlap 后逐级细化到 mip0；世界空间命中先查询最多 16 个 Surface Cache page，再查询最多 4 个 Voxel clipmap 与 64 个 cell。neutral DTO 与固定 scene 710 words / trace 448 words ABI 已接通；scene packet 写入已归还声明 write access 的 `scene-prepare` owner。`provider.rs` 测试拆入 `provider/tests.rs`，生产 owner 保持 775 行。 | Runtime WGPU `hzb_build_preserves_per_mip_params_and_resolves_msaa_depth` 1/1 通过，覆盖 single 与 4x MSAA 两条 RGBA16Float mip chain。插件 trace shader 3/3 通过，精确验证 Surface Cache radiance 与 Voxel fallback radiance/source flags；neutral DTO、scene packet、两帧 provider GPU feedback、jittered camera packet 各 1/1。六个 WGSL 文件 Naga validation 通过，source contracts 8/8。ignored 产品导出 `export_hybrid_gi_main_scene_hzb_surface_cache_trace_wgpu_png` 1/1 通过，生成并目检 `docs/tests/runtime/render/plan18_hybrid_gi_main_scene_hzb_surface_cache_trace_wgpu_20260710.png` / `.txt`：192x128、1813 visible pixels、max luma 155.05、Surface Cache page 1、Voxel clipmap 1、HGI graph pass 4、SHA-256 `DADC5BE60FEC3E905409D84967E90ECD461A39A341B9283337BF364A372D5BB7`。focused runtime/plugin 构建有既有 warning，但无错误；不宣称 full workspace green。 | 继续 motion/history rejection、temporal stability、multi-ray screen-probe quality、SDF/GDF、Deferred/Forward+ parity、RenderDoc、full runtime/full CI；Plan 18 AF-M1..AF-M4 与整体渲染方案仍开放。 |
| 2026-07-10 | Surface-cache HZB negative-direction stride correctness | 完成: coarse block skip 现已按实际 atlas step 坐标和 ray 方向分量对齐，负方向不会跳过当前 mip block 之外的 mip0 texel | 状态锚点 `render_plan18_hybrid_gi_surface_cache_hzb_direction_safe_stride_wgpu_passed`。`trace_probe_tiles.wgsl` 新增统一八方向向量和按正/负方向分别检查 block 首/尾坐标的 mip selector；只有 stride 覆盖的所有后续距离都位于当前 HZB block 时才允许跳步。新增 `-X` 反例 WGPU 测试，固定 base x=10、distance 2/mip1 与 distance 3/x=7 near-depth hit。 | 审查先证明旧 `ray_distance % stride` 合同在负方向会漏检；新 WGPU 回归 `trace_probe_tiles_shader_keeps_negative_direction_hzb_skips_inside_the_tested_block` 通过 1/1，精确输出 RGB `[51,77,21]`。正向既有用例与负向新用例合并复跑通过 2/2；surface-cache HZB 产品导出复跑 1/1，主场景 HZB 产品导出复跑 1/1。Cargo 当前插件 lib-test 已成功构建，locked/offline metadata 通过；新的插件 Cargo 复跑和 runtime root minimum-size 测试分别在 5/15 分钟编译阶段超时，未执行且不计 green，不宣称 full workspace green。 | 继续主场景 per-ray coarse-to-fine screen HZB march、surface-cache radiance hit、motion/history validity、SDF/GDF、Deferred/Forward+ parity、RenderDoc、temporal/full CI；Plan 18 总体仍未完成。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style graph-owned main-scene HZB trace packet | 部分完成: runtime graph HZB 已同时保存 furthest/closest/span，HGI 已消费完整 mip view、相机逆 VP 与 8x8 depth-range packet；Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_main_scene_hzb_trace_wgpu_png_passed_screen_ray_march_sdf_deferred`。`hzb_build.wgsl` 在既有 Rgba16Float 全 mip 链中保持 R/x furthest 兼容，并新增 G/y closest、B/z span；resolver 新增经过 pass declaration 校验的 full-mip texture view 入口。HGI `scene-prepare` 读取 `hzb-furthest`，写入 8x8 tile scene/HZB range 和 runtime canonical unjittered inverse-view-projection/camera/viewport packet；`trace-schedule` 按 WebGPU 0..1 clip depth 重建世界位置并执行 thickness-aware depth-range overlap，输出 8x8 trace records；resolve 按 screen UV 可视化 closest/furthest/span。render feature 新增通用 `write_buffer_with_minimum_size` graph contract，确保 1x1 viewport 仍分配 scene 1176 bytes、trace 1280 bytes，避免 packet 越界；资源名收束到 `PostProcessGraphResourceNames`。产品导出位于 child owner `hybrid_gi_render_framework_stats/main_scene_hzb_trace.rs`。 | TDD RED 源契约先确认缺少 `HzbDepthRange`、`closest_depth`、scene tile、world reconstruction 与 trace tile 五项；实现后 7 项 source contract GREEN。后续相机/深度复核再以 canonical `ViewProjectionMatrixPair` 与 WebGPU 0..1 clip depth 合同先红后绿。插件 lib-test 编译完成；直接定向执行 descriptor、camera packet、MSAA scene/HZB handoff、trace packet 与 shader source tests 均通过 1/1。ignored WGPU export `export_hybrid_gi_main_scene_hzb_trace_wgpu_png` 修正后复跑通过 1/1，生成并目检 `docs/tests/runtime/render/plan18_hybrid_gi_main_scene_hzb_trace_wgpu_20260710.png` / `.txt`：192x128、`visible_pixels=1813`、`max_luma=155.05`、HGI graph pass count 4。scoped rustfmt、locked/offline plugin metadata 与 diff check 通过。root runtime buffer-min test编译被并行 text owner 的 `GlyphAtlasBitmapRendererPrepareReport` 私有字段错误阻断，未计通过；该错误不在本切片路径。 | 这不是完整 Lumen screen trace：当前已建立主场景 HZB/camera/tile ABI 与 range-overlap packet，但尚未把每条 screen ray 做 coarse-to-fine HZB march，也未接入 motion/history validity、surface-cache radiance 命中、SDF/global-distance-field tracing、Deferred/Forward+ parity、RenderDoc product capture、temporal stability、full runtime/full CI；Plan 18 七项机制仍开放。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style surface-cache HZB trace | 部分完成: surface-cache depth atlas 已有 GPU min/max HZB mip chain，trace tile 已执行 aligned block skip 与 coarse-to-fine refinement；Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_surface_cache_hzb_trace_wgpu_png_passed_scene_hzb_sdf_deferred`。新增 `build_surface_cache_depth_hierarchy.wgsl` 与独立 owner `create_buffers/surface_cache_depth_hierarchy.rs`：WGPU 逐 mip 2x2 归约 RGBA8 深度，R/G 分别保存有效样本 min/max，mip0..6 把每张对齐 64x64 page 收敛到 1 texel 且不跨 card。`trace_probe_tiles.wgsl` 用 `textureNumLevels` 读取层级，按 ray distance 选择最大对齐块；深度区间不相交时按 mip stride 跳过，相交时逐级细化到 mip0 并按厚度确认 hit，之后才进入既有 voxel cone fallback。HZB WGPU 用例及测试纹理拆到 `dispatch_probe_trace_tiles/tests/surface_cache_hzb.rs`，ignored 产品导出拆到 `hybrid_gi_render_framework_stats/surface_cache_hzb_trace.rs`。 | TDD RED: 旧线性路径输出 packed `990795`（RGB `[75,30,15]`），不等于远端 near-depth hit 期望 `1260604`（RGB `[60,60,19]`）；GREEN: focused WGPU test `trace_probe_tiles_shader_uses_surface_cache_hzb_to_skip_depth_disjoint_blocks` 通过 1/1。ignored WGPU export `export_hybrid_gi_surface_cache_hzb_trace_wgpu_png` 通过 1/1，生成 `docs/tests/runtime/render/plan18_hybrid_gi_surface_cache_hzb_trace_wgpu_20260710.png` / `.txt`；PNG 已目检非黑，192x128、`visible_pixels=1813`、`max_luma=155.05`，报告记录 `gpu_surface_cache_depth_hierarchy=build_surface_cache_depth_hierarchy_compute+rgba8_min_max_depth_range_mips` 与 `gpu_probe_trace_tile_surface_cache_hzb=trace_probe_tiles_compute+aligned_block_skip+coarse_to_fine_refinement`。结构收束后 `dispatch_probe_trace_tiles/tests.rs=865`、HZB child test=180、`hybrid_gi_render_framework_stats.rs=916`、export child=84，均低于约 1000 行 owner 预算；拆分后 scoped rustfmt、source-path、冲突标记、line-count 与 diff check 通过。外部 owner 已修复先前 animation E0502 与临时 asset import re-export 错误；后续 Cargo 成功编译 `zircon_runtime` 并刷新 fingerprint，但在 process-output/fingerprint 异常后未重新链接插件测试，故不宣称 post-split Cargo green。 | 这不是完整 AF-M1..AF-M4 或完整 Lumen screen trace：当前层级属于 surface-cache atlas，尚未把 render graph 的全分辨率主场景 closest/furthest HZB、投影矩阵/屏幕 UV、motion/history validity 直接交给 HGI；SDF/global-distance-field tracing、Deferred/Forward+ parity、RenderDoc product capture、temporal stability、full runtime/full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-10 | Cross-plan HGI/Lumen-style surface-cache ray direction distribution | 部分完成: trace tile dispatch 已按 sample id 在 surface-cache atlas/depth 空间分布 8 个确定性 ray step 方向，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_surface_cache_ray_direction_distribution_wgpu_png_passed_hzb_sdf_deferred`。`trace_probe_tiles.wgsl` 将 `surface_cache_ray_step_coord(...)` 从 `tile_sample_id & 3u` 的四轴方向扩展为 `tile_sample_id & 7u` 的八方向分布，保留 sample 0..3 的 `+x/+y/-x/-y` 兼容路径，并新增 sample 4..7 的四个对角方向；既有 surface-cache atlas/depth 多步 march、depth jump 拒绝和 voxel cone fallback 保持不变。新增 ignored WGPU 导出子模块 `hybrid_gi_render_framework_stats/surface_cache_ray_direction_distribution.rs`，生成 SurfaceCache 产品截图和报告。 | 首次 focused Cargo `cargo test --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --lib trace_probe_tiles_shader_distributes_surface_cache_ray_steps_by_sample_id --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-product-plugin-0708 --message-format short --color never -- --test-threads=1 --nocapture` 通过 1/1，耗时 37m54s，只有既有 warning；随后直接运行测试二进制 `trace_probe_tiles_shader_ --test-threads=1 --nocapture` 通过 7/7；ignored WGPU export `export_hybrid_gi_surface_cache_ray_direction_distribution_wgpu_png --ignored --test-threads=1 --nocapture` 通过 1/1，并生成 `docs/tests/runtime/render/plan18_hybrid_gi_surface_cache_ray_direction_distribution_wgpu_20260710.png` / `.txt`，截图已目检非黑；报告记录 `gpu_probe_trace_tile_surface_cache_ray_direction_distribution=trace_probe_tiles_compute+sample_id_octant_axis_diagonal_distribution`、`validated_surface_cache_ray_direction_distribution_shader=trace_probe_tiles_shader_distributes_surface_cache_ray_steps_by_sample_id_diagonal_near_depth_texel`、`visible_pixels=1813`、`max_luma=155.05`。scoped `rustfmt --edition 2021 --check` 通过。 | 这不是完整 AF-M1..AF-M4 完成项；当前仍是 surface-cache atlas/depth 空间的 deterministic ray-step direction distribution，不是完整 HZB/screen trace，也不是 Lumen SDF/global-distance-field tracing；Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full runtime test/full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style surface-cache ray march | 部分完成: trace tile dispatch 已在 voxel fallback 前执行 surface-cache atlas/depth 多步 march，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_surface_cache_ray_march_wgpu_png_passed_hzb_sdf_deferred`。`trace_probe_tiles.wgsl` 将 surface-cache sample 从单 texel lookup 扩展为 atlas-space step march：按 `tile_sample_id` 派生 2D 步进方向，最多 4 步读取 surface-cache atlas/depth texel，近深度 texel 按距离加权混合，alpha/depth 无效和 depth jump 样本被拒绝；surface-cache 无效后才进入已有 voxel descriptor/cone fallback。新增 WGPU readback test `trace_probe_tiles_shader_marches_surface_cache_depth_before_voxel_fallback`，并把截图导出拆到 `hybrid_gi_render_framework_stats/surface_cache_ray_march.rs`。 | 新测试先红后绿：实现前输出旧单 texel `[150,60,15]`，期望 weighted near-depth `[111,84,21]`；实现后该用例通过。WGPU shader focused `cargo test --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --lib trace_probe_tiles_shader_ --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-product-plugin-0708 --message-format short --color never -- --test-threads=1 --nocapture` 通过 6/6；ignored WGPU export `export_hybrid_gi_surface_cache_ray_march_wgpu_png` 通过 1/1 并生成 `docs/tests/runtime/render/plan18_hybrid_gi_surface_cache_ray_march_wgpu_20260708.png` / `.txt`，截图已目检非黑；报告记录 `gpu_probe_trace_tile_surface_cache_ray_march=trace_probe_tiles_compute+atlas_depth_multi_step_march`、`validated_surface_cache_ray_march_shader=trace_probe_tiles_shader_marches_surface_cache_depth_before_voxel_fallback_weighted_near_depth_texels`、`visible_pixels=1813`、`max_luma=155.05`。scoped `rustfmt --check` 通过。 | 这不是完整 AF-M1..AF-M4 完成项；当前是 surface-cache atlas/depth 空间的多步 march，不是完整 HZB/screen trace，也不是 Lumen SDF/global-distance-field tracing；Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full runtime test/full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style voxel cone-trace fallback | 部分完成: surface-cache miss 后的 voxel fallback 已由单 cell descriptor 提升为按 cell 半径/occupancy 加权的多体素 cone-style 聚合，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_voxel_cone_trace_wgpu_png_passed_full_ray_marching_deferred`。`trace_probe_tiles.wgsl` 新增 `voxel_cell_cone_weight(...)`，按 clipmap、cell distance、descriptor half extent 与 occupancy 聚合 in-range voxel-cell radiance；exact cell 保留额外权重以维持旧单 cell fallback 行为；新增 WGPU readback test 覆盖 exact+neighbor 聚合，并排除远 cell 与异 clipmap；新增 ignored WGPU 导出子模块 `hybrid_gi_render_framework_stats/voxel_cone_trace.rs` 生成 VoxelClipmap 产品截图。 | scoped `rustfmt --edition 2021 --check` 通过；WGPU shader focused `cargo test --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --lib trace_probe_tiles_shader_ --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-product-plugin-0708 --message-format short --color never -- --test-threads=1 --nocapture` 通过 5/5；ignored WGPU export `export_hybrid_gi_voxel_cone_trace_wgpu_png` 通过 1/1 并生成 `docs/tests/runtime/render/plan18_hybrid_gi_voxel_cone_trace_wgpu_20260708.png` / `.txt`，截图已目检非黑；报告记录 `gpu_probe_trace_tile_voxel_cone_trace=trace_probe_tiles_compute+weighted_voxel_cell_cone_aggregation`、`validated_voxel_cone_trace_shader=trace_probe_tiles_shader_cone_traces_multiple_voxel_cells_when_surface_cache_misses_exact_neighbor_weighted_rgb`、`visible_pixels=1813`、`max_luma=155.05`。fresh target-dir 首次 focused Cargo 在 604s 编译超时后已停止，不计 green；一次 exact-filter export 匹配 0 tests，不计截图验证。 | 这不是完整 AF-M1..AF-M4 完成项；当前仍是 scene-prepare voxel descriptor 空间的 cone-style approximation，不是完整 screen-space ray marching / Lumen SDF march；Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full runtime test/full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style current-frame post.uber MSAA product route | 部分完成: MSAA graph 下 `hybrid-gi-lighting` 已作为单采样 graph product 进入 `post.uber`，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_current_frame_post_uber_msaa_single_sample_product_wgpu_png_passed`。`resource_descriptors.rs` 将 `PostProcessGraphResourceNames::HYBRID_GI_LIGHTING` 固定为单采样 graph product；`build_frame_submission_context` 在 HGI 有效 extract 存在时总是激活 current-frame `hybrid-gi-lighting` 输入，不再按 graph MSAA sample count 禁用；新增 compile route 测试要求 4x graph MSAA 中 `scene-color.sample_count == 4` 且 `hybrid-gi-lighting.sample_count == 1`，并把新的 MSAA WGPU 导出拆到 `hybrid_gi_render_framework_stats/current_frame_post_uber_msaa.rs`，避免继续扩张 1000 行测试源。 | scoped `rustfmt --edition 2021 --check` 通过；ignored WGPU export `export_hybrid_gi_current_frame_post_uber_msaa_wgpu_png` 通过 1/1 并生成/刷新 `docs/tests/runtime/render/plan18_hybrid_gi_current_frame_post_uber_msaa_wgpu_20260708.png` / `.txt`，截图已目检非黑；报告记录 `graph_msaa_sample_count=4`、`hybrid_gi_lighting_sample_count=1`、`current_frame_post_uber_input=hybrid-gi-lighting_single_sample_graph_product`、`render_graph_route=hybrid-gi-resolve_write_texture_hybrid-gi-lighting_to_post.uber_read_texture`、`warm_minus_cool_red=5.53`、`cool_minus_warm_blue=5.53`。runtime 定向测试 `compile_keeps_hybrid_gi_lighting_single_sample_when_graph_msaa_is_enabled` 在 root lib-test 编译阶段 10 分钟超时，未执行不计通过。 | 这不是完整 AF-M1..AF-M4 完成项；真实 ray marching/cone trace、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full runtime test/full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style current-frame post.uber composite | 部分完成: 单采样 `hybrid-gi-lighting` 已作为当前帧 GI 输入进入 `post.uber`，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_current_frame_post_uber_wgpu_png_passed_msaa_resolve_pending`。新增 `PostProcessGraphResourceNames::HYBRID_GI_LIGHTING` 和 stack 激活入口，`post.uber` 只在 Hybrid GI 单采样路径声明读取 `hybrid-gi-lighting`；descriptor filtering 防止无 stack 输入时把可选 HGI texture 强行编进普通 post graph；runtime graph execution 只把单采样、非 depth 的 `hybrid-gi-lighting` view 传给 post-process，`post_process.wgsl` 复用 GI history 采样槽，在 `params.hybrid_gi_counts.w` 标记下把当前帧 HGI lighting 混入 indirect light，缺失或 MSAA 时保留 history fallback。 | `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-current-composite-plugin-0708 --message-format short --color never` 通过；ignored WGPU export `export_hybrid_gi_product_composite_scene_seed_wgpu_png` 通过 1/1 并生成/刷新 `docs/tests/runtime/render/plan18_hybrid_gi_current_frame_post_uber_wgpu_20260708.png` / `.txt`，截图已目检非黑；报告记录 `current_frame_post_uber_input=hybrid-gi-lighting_graph_resource`、`render_graph_route=hybrid-gi-resolve_write_texture_hybrid-gi-lighting_to_post.uber_read_texture`、`warm_minus_cool_red=5.53`、`cool_minus_warm_blue=5.53`；scoped `rustfmt --edition 2021 --check` 通过。runtime 定向测试 `hybrid_gi_current_lighting_flag_is_encoded_separately_from_history` 在 root lib-test 编译阶段约 10 分钟仍未执行，已停止，不计为通过。 | 这不是完整 AF-M1..AF-M4 完成项；当前帧路径只覆盖单采样 `hybrid-gi-lighting`，MSAA 仍需 explicit single-sample resolve 或直接 composite；真实 ray marching/cone trace、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full runtime test/full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style graph lighting history handoff | 部分完成: HGI `hybrid-gi-lighting` 已成为 GI history copy 的优先 graph 源，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_graph_lighting_history_copy_check_passed_product_composite_pending`。`hybrid-gi-history` pass 声明从 `scene-color` 收束为读取 `hybrid-gi-lighting` 并写入 `history-global-illumination`，让 graph resolve 输出不再是无下游读者的 proof attachment；runtime 每帧 GI history copy 现在优先尝试单采样、非 depth 的 `hybrid-gi-lighting` transient texture，再退回 post-process `global-illumination` 或固定 target。 | `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --tests --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；插件 descriptor test `hybrid_gi_registration_contributes_render_feature_descriptor` 通过 1/1；executor contract test `hybrid_gi_executors_accept_declared_feature_pass_contexts` 通过 1/1；scoped `rustfmt --edition 2021 --check` 通过；现有视觉证据继续使用 `docs/tests/runtime/render/plan18_hybrid_gi_scene_depth_source_sampling_wgpu_20260707.png` / `.txt`，报告追加 graph lighting history copy 标记。runtime 定向测试 `global_illumination_history_source` 在 root lib-test 编译阶段 300s 超时，未计为通过。 | 这不是完整 AF-M1..AF-M4 完成项；`hybrid-gi-lighting` 只进入 GI history copy 候选，当前帧最终 post-process/product composite 仍未直接采样该 attachment；MSAA lighting source 按防护回退，仍需 resolve-to-single-sample 或产品 composite 路径；完整 WGPU history/product visual、真实 ray marching/cone trace、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style graph resolve trace depth-source attachment | 部分完成: HGI `hybrid-gi-resolve` 已把 `hybrid-gi-trace` packet 解析为 `hybrid-gi-lighting` fullscreen attachment 写入，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_graph_resolve_trace_depth_source_attachment_check_passed_scene_depth_png_refreshed`。`hybrid-gi-resolve` executor 在有 WGPU context 时消费 `hybrid-gi-trace` buffer，并以 graphics fullscreen pass 写入 pass-declared `hybrid-gi-lighting` attachment；新增 `render_pass_executors/resolve_trace_handoff.rs` 和 `resolve_trace_depth_source.wgsl`，shader 校验 trace magic/packet count/valid flag 后输出 packed RGBA depth proof color；管线沿用目标 texture desc 的 color format 与 sample_count，保持 attachment 写入模式而不是改造成 storage texture。 | `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --tests --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；scoped `rustfmt --edition 2021 --check` 通过；`trace_schedule_handoff.wgsl` 与 `resolve_trace_depth_source.wgsl` 均通过 Naga WGSL parse/validation；ignored Wgpu PNG export `export_hybrid_gi_scene_depth_source_sampling_wgpu_png` 复跑通过 1/1 并刷新 `docs/tests/runtime/render/plan18_hybrid_gi_scene_depth_source_sampling_wgpu_20260707.png` / `.txt`，截图已目检非空。 | 这不是完整 AF-M1..AF-M4 完成项；`hybrid-gi-lighting` attachment 已被 graph resolve 写入 proof color，但最终 post-process/product composite 尚未消费该 attachment 作为产品 GI 输入；完整 WGPU resolve unit、真实 ray marching/cone trace、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style graph trace-schedule depth-source packet | 部分完成: HGI `hybrid-gi-trace-schedule` 已把 graph scene-depth handoff buffer 提升为 trace-stage surface-cache/card depth-source packet，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_graph_trace_schedule_depth_source_packet_check_passed_visual_refresh_pending`。`hybrid-gi-trace-schedule` executor 在有 WGPU context 时消费 `hybrid-gi-scene` buffer 并写入 `hybrid-gi-trace` buffer；新增 `render_pass_executors/trace_schedule_handoff.rs` 和 `trace_schedule_handoff.wgsl`，把 scene-depth handoff 的 magic/尺寸/q24 depth/sample_count 转成 trace magic、packet count、dimensions、q24 depth、sample count、packed RGBA depth、valid flag、atlas/card slot 0 与 source magic，作为后续 resolve/product surface-cache 输入边界。 | `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --tests --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；scoped `rustfmt --edition 2021 --check` 通过；`trace_schedule_handoff.wgsl` 通过 Naga WGSL parse/validation。目标 WGPU 单测 `trace_schedule_shader_promotes_scene_depth_handoff_to_surface_cache_depth_packet` 两次在 test harness compile/link/run 阶段 600s 超时，未计为通过；本切片最初未刷新 PNG，后续 resolve 切片已复跑 ignored Wgpu PNG export 并刷新同一 `docs/tests/runtime/render/plan18_hybrid_gi_scene_depth_source_sampling_wgpu_20260707.png` / `.txt`。 | 这不是完整 AF-M1..AF-M4 完成项；后续 resolve 切片已消费 `hybrid-gi-trace` packet 并写入 `hybrid-gi-lighting` proof attachment；最终 post-process/product composite 消费、完整 WGPU 单测执行、完整 ray marching/cone trace、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-08 | Cross-plan HGI/Lumen-style graph scene-depth MSAA handoff | 部分完成: HGI graph scene-depth handoff 已支持单采样与 4x MSAA depth texture 分支，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_graph_scene_depth_handoff_msaa_wgpu_test_passed_product_surface_cache_pending`。`render_pass_executors.rs` 将 WGPU handoff body 拆到 `render_pass_executors/scene_depth_handoff.rs`；executor 通过 `RenderPassGpuExecutionContext::require_texture_desc(...)` 读取 pass-declared `scene-depth` 的 `TextureDesc.sample_count`，按 sample count 选择 `scene_depth_handoff.wgsl` 或 `scene_depth_handoff_msaa.wgsl`；MSAA shader 用 `texture_depth_multisampled_2d` 保守取各 sample 最小 depth，并把 magic/尺寸/depth/sample_count 写入 `hybrid-gi-scene` proof buffer；runtime graph descriptor lookup 现在优先使用 compiled graph 的 typed texture declaration，因此 frame-imported `scene-depth` 也能参与 sample-count 路由。 | `cargo test --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime scene_depth_handoff_msaa_shader_resolves_depth_sample_count --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1；`cargo test --manifest-path zircon_runtime\Cargo.toml public_gpu_buffer_lookup_requires_compiled_pass_declaration_access --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never -- --nocapture --test-threads=1` 通过 1/1；ignored Wgpu PNG export `export_hybrid_gi_scene_depth_source_sampling_wgpu_png` 复跑通过 1/1 并刷新 `docs/tests/runtime/render/plan18_hybrid_gi_scene_depth_source_sampling_wgpu_20260707.png` / `.txt`；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-msaa-depth-handoff-0708 --message-format short --color never` 通过；scoped `rustfmt --edition 2021 --check` 通过。 | 这不是完整 AF-M1..AF-M4 完成项；proof words 仍需升级为产品 surface-cache/card 输入；完整 ray marching/cone trace 质量、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style graph scene-depth handoff | 部分完成: HGI `hybrid-gi-scene-prepare` 已作为 graph-stage AsyncCompute pass 直接读取 render graph `scene-depth` texture view 并写入 `hybrid-gi-scene` buffer，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_graph_scene_depth_handoff_wgpu_png_passed_msaa_pending`。`hybrid-gi-scene-prepare` descriptor 现在声明 fixed compute workload、`scene-depth` texture read、`hybrid-gi-scene` buffer write 和 AsyncCompute lane；executor 通过 `RenderPassGpuExecutionContext` 解析 pass-declared `scene-depth`/`hybrid-gi-scene`，调度 `scene_depth_handoff.wgsl` 写入深度 handoff proof words；runtime graph resource lookup 增加 resolver-backed `require_buffer(...)` 公共 helper。 | ignored Wgpu PNG export `export_hybrid_gi_scene_depth_source_sampling_wgpu_png` 1/1 通过；插件 descriptor test `hybrid_gi_registration_contributes_render_feature_descriptor` 1/1 通过；executor contract test `hybrid_gi_plugin_registrations_execute_contract_bound_passes` 1/1 通过；`cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --message-format short --color never` 通过；scoped `rustfmt --edition 2021` 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_scene_depth_source_sampling_wgpu_20260707.png`，报告记录 `direct_dsrt_scene_depth_texture=hybrid_gi_scene_prepare_graph_executor_texture_depth_load_to_hybrid_gi_scene_buffer`、near/far HGI graph executed pass count 均为 4。 | 这不是完整 AF-M1..AF-M4 完成项；当前 handoff 覆盖单采样 `texture_depth_2d` proof，MSAA depth resolve/texture_multisampled 路径、把 proof words 升级为产品 surface-cache/card 输入、完整 ray marching/cone trace 质量、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。runtime crate focused `public_gpu_buffer_lookup_requires_compiled_pass_declaration_access` 受当前根 `Cargo.lock` 脏状态和并行 runtime 验证占用阻断，未计为 green。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style scene depth source sampling | 部分完成: scene-owned surface-cache depth source sample 已经进入 Wgpu depth texture 上传/readback 与 trace depth consumer，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_scene_depth_source_sampling_wgpu_png_passed_dsrt_pending`。新增 `HybridGiPrepareSurfaceCacheDepthSourceSample` 与 execution input 字段；`collect_inputs(...)` 从 scene prepare card/page ownership 生成 source depth samples；`scene_prepare_depth_samples.rs` 优先使用 source samples，旧 bounds fallback 只在缺失时保留；texture layout 支持 source-only atlas slot。 | `surface_cache_depth_samples` 2/2 通过；`collect_inputs_preserves_scene_prepare_contract_for_renderer_consumption` 1/1 通过；ignored Wgpu PNG export `export_hybrid_gi_scene_depth_source_sampling_wgpu_png` 1/1 通过；render-framework stats regression 1/1 通过；Wgpu shader consumer test `trace_probe_tiles_shader_samples_surface_cache_atlas_and_depth_textures` 1/1 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_scene_depth_source_sampling_wgpu_20260707.png`，报告记录 `scene_depth_source_precedence=surface_cache_depth_source_samples_preferred_over_bounds_fallback`、`wgpu_depth_upload=scene_prepare_surface_cache_depth_texture_upload_and_readback`、`direct_dsrt_scene_depth_texture=deferred_graph_pass_pending`、near/far first-frame depth sample count 均为 1。 | 这不是完整 AF-M1..AF-M4 完成项；直接 render graph `SCENE_DEPTH`/DSRT texture 采样仍 pending；完整 ray marching/cone trace 质量、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style product composite scene-seed proof | 部分完成: scene direct-light seed 已经通过 GPU trace tile、neutral sideband 与 post-process Hybrid GI composite 形成公开 Wgpu product-frame 差异，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_product_composite_scene_seed_wgpu_png_passed`。`trace_probe_tiles.wgsl` 新增 scene-light seed 参数并对有效 surface-cache sample 做乘法调制；runtime post-process `encode_hybrid_gi_probes` 在 authored probes 为空时从 prepared runtime sideband 追加 screen probes、irradiance 与 `probe_rt_lighting_rgb`；公开 `RenderFramework` export 用 warm/cool key light 且关闭 preview direct lighting，验证 scene prepare -> trace/resolve -> GlobalIllumination composite 路径。 | Wgpu shader seed test 1/1 通过；ignored Wgpu product PNG export `export_hybrid_gi_product_composite_scene_seed_wgpu_png` 1/1 通过；runtime encoder test `hybrid_gi_probe_encoder_projects_prepared_runtime_screen_probe_sideband` 1/1 通过；render-framework stats regression 1/1 通过；previous product export `export_hybrid_gi_runtime_trace_lighting_product_resolve_wgpu_png` 1/1 通过；touched-file `rustfmt --check` 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_product_composite_scene_seed_wgpu_20260707.png`，报告记录 `warm_minus_cool_red=5.53`、`cool_minus_warm_blue=5.53`、`product_preview_direct_lighting=disabled_for_product_gi_seed_isolation`、`product_composite_source=scene_prepare_direct_light_seed_to_surface_cache_trace_to_global_illumination`。 | 这不是完整 AF-M1..AF-M4 完成项；完整 runtime crate fmt 仍受既有无关格式差异阻塞；真实 scene depth/DSRT source sampling、完整 ray marching/cone trace 质量、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity、full CI 与 Plan 18 七项机制仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style runtime trace-lighting sideband product proof | 部分完成: trace lighting history 已经通过中立 prepared sideband 从 provider 回到 runtime prepare collector，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_runtime_trace_lighting_neutral_sideband_product_wgpu_png_passed`。`RenderHybridGiPreparedFrame` 新增 `probe_rt_lighting_rgb`；provider 从 `HybridGiResolveRuntime::probe_rt_lighting_rgb` 投影历史 trace lighting；collector 从 `RenderHybridGiPreparedProbeRtLighting` 重建 `HybridGiResolveRuntime`；公开 `RenderFramework` 路径用三帧 Wgpu 提交验证 provider completion -> neutral sideband -> collector rebuild。 | Provider sideband test 1/1 通过；collector rebuild test 1/1 通过；Wgpu shader tests `trace_probe_tiles_shader_` 3/3 通过；render-framework stats test 1/1 通过；ignored Wgpu product PNG export `export_hybrid_gi_runtime_trace_lighting_product_resolve_wgpu_png` 1/1 通过；`cargo fmt --manifest-path zircon_plugins/hybrid_gi/runtime/Cargo.toml --check` 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_runtime_trace_lighting_product_wgpu_20260707.png`，报告记录 `product_debug_view=none`、`runtime_trace_lighting_neutral_sideband=provider_resolve_runtime_probe_rt_lighting_rgb_to_render_hybrid_gi_prepared_frame`、`runtime_trace_lighting_collector_rebuild=render_hybrid_gi_prepared_probe_rt_lighting_to_hybrid_gi_resolve_runtime`、`visible_pixels=1813`、`max_luma=149.40`。 | 这不是完整 AF-M1..AF-M4 完成项；runtime crate 默认特性单测超时、core-min 复跑未产出可计 green；真实 scene depth/DSRT source sampling、完整 ray marching/cone trace、最终 GI resolve/composite 质量、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity 和 full CI 仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style voxel miss fallback | 部分完成: trace tile dispatch 已在 surface-cache sample 无效时消费 voxel-cell descriptor，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_voxel_miss_fallback_wgpu_png_passed_ray_dispatch_deferred`。`trace_probe_tiles.wgsl` 新增 scene-prepare descriptor storage binding；dispatch params 携带 descriptor count；GPU shader 先采 surface-cache atlas/depth，再按 tile probe id/sample id 查找 `SCENE_PREPARE_DESCRIPTOR_KIND_VOXEL_CELL` descriptor 并解包 RGB，最后才回到旧 hash fallback。 | Wgpu shader tests `trace_probe_tiles_shader_` 3/3 通过；ignored Wgpu PNG export `export_hybrid_gi_voxel_miss_fallback_wgpu_png` 1/1 通过；`cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-surface-cache-sampling-0707` 通过；`cargo fmt --manifest-path zircon_plugins/hybrid_gi/runtime/Cargo.toml --check` 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_voxel_miss_fallback_wgpu_20260707.png`，报告记录 `gpu_probe_trace_tile_voxel_miss_fallback=trace_probe_tiles_compute+scene_prepare_voxel_cell_descriptor_radiance`、`validated_voxel_miss_fallback_shader=trace_probe_tiles_shader_uses_voxel_cell_descriptor_when_surface_cache_sample_is_invalid_exact_rgb`、`visible_pixels=1813`、`max_luma=149.40`。 | 这不是完整 AF-M1..AF-M4 完成项；真实 scene depth/DSRT source sampling、完整 ray marching/cone trace、product GI resolve/composite、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity 和 full CI 仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style surface-cache texture sampling | 部分完成: trace tile dispatch 已消费 surface-cache atlas/depth texture，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_surface_cache_texture_sampling_wgpu_png_passed_ray_dispatch_deferred`。`trace_probe_tiles.wgsl` 新增 surface-cache atlas/depth texture bindings；Wgpu dispatch params 传入 atlas 尺寸、tile extent、atlas columns 与 texture availability；surface-cache trace tile record 第三 word 改为 atlas slot；缺失或无效 texture 时保留原确定性 fallback。 | Wgpu shader tests `trace_probe_tiles_shader_` 2/2 通过；ignored Wgpu PNG export `export_hybrid_gi_surface_cache_texture_sampling_wgpu_png` 1/1 通过；`cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-surface-cache-sampling-0707` 通过；`cargo fmt --manifest-path zircon_plugins/hybrid_gi/runtime/Cargo.toml --check` 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_surface_cache_texture_sampling_wgpu_20260707.png`，报告记录 `gpu_probe_trace_tile_surface_cache_sampling=trace_probe_tiles_compute+surface_cache_atlas_depth_texture_load`、`validated_surface_cache_depth_sample_count=1`、`visible_pixels=1813`、`max_luma=149.40`。 | 这不是完整 AF-M1..AF-M4 完成项；真实 scene depth/DSRT source sampling、完整 ray marching/cone trace、voxel miss fallback、product GI resolve/composite、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity 和 full CI 仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style runtime prepare collector GPU owner/readback | 部分完成: runtime prepare collector 已能直接持有 Hybrid GI GPU resources 并收集 pending readback，Plan 18 七项高级光照仍未完成 | 状态锚点 `render_plan18_hybrid_gi_runtime_prepare_collector_gpu_owner_readback_wgpu_png_passed_scene_prepare_reconstruction_deferred`。新增 stateful runtime prepare collector owner，从 neutral prepared runtime sideband 重建 `HybridGiPrepareFrame`/`HybridGiResolveRuntime`，提交 `execute_prepare(...)`，并把上一帧 pending GPU readback 回写为 renderer-neutral plugin outputs；运行时状态层允许 scene-representation screen probe 接受 GPU cache/irradiance/trace-lighting feedback。 | focused screen-probe GPU feedback test 红绿验证；`runtime_prepare_collector` 2/2 通过、1 ignored；ignored Wgpu PNG export `export_hybrid_gi_runtime_prepare_collector_gpu_owner_readback_wgpu_png` 1/1 通过；`cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_hybrid_gi_runtime --locked --jobs 1 --target-dir E:\cargo-targets\zircon-hgi-runtime-collector-gpu-owner-0707` 通过；scoped rustfmt 通过；视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_runtime_prepare_collector_gpu_owner_readback_wgpu_20260707.png`，报告记录 `runtime_prepare_collector_execution=stateful_gpu_prepare_pending_readback_collected`、`last_hybrid_gi_cache_entry_count=2`、`validated_runtime_prepare_transient_screen_probe_count=2`。 | 这不是完整 AF-M1..AF-M4 完成项；真实 scene depth/DSRT source sampling、真实 ray marching/surface-cache sampling、voxel miss fallback、product GI resolve/composite、Deferred/Forward+ parity、RenderDoc/product trace、temporal/product parity 和 full CI 仍开放。 |
| 2026-07-07 | Cross-plan HGI/Lumen-style post-process seed visual | 部分完成: Wgpu 可视化证据通过,Plan 18 七项高级光照仍未完成 | `encode_hybrid_gi_probes` 与 `encode_hybrid_gi_trace_regions` 不再输出空 buffer,而是把 `RenderHybridGiExtract` 中启用的 probe/trace region 投影为 post-process shader 可消费的 GPU 数据;新增 `render_framework_bridge/hybrid_gi_visual_export.rs` 导出 baseline/warm/cool 三联图。 | 直接运行已构建 `zircon_runtime` lib-test 二进制: `hybrid_gi_probe_encoder` 2/2 通过,`hybrid_gi_trace_region_encoder` 2/2 通过,ignored 精确用例 `graphics::tests::render_framework_bridge::hybrid_gi_visual_export::export_hybrid_gi_lumen_style_seed_visual_png` 1/1 通过;视觉证据 `docs/tests/runtime/render/plan18_hybrid_gi_lumen_style_seed_visual_20260707.png`,报告 `docs/tests/runtime/render/plan18_hybrid_gi_lumen_style_seed_visual_20260707.txt` 记录 warm/cool 中心通道差异。 | 这不是完整 AF-M1..AF-M4 完成项;froxel fog、cookies、clearcoat/anisotropy/transmission、OIT、irradiance volumes、planar reflection、SSS、RenderDoc/product trace、真实 surface-card/asset-derived HGI capture、temporal/product parity 和 full CI 仍开放。 |
| 2026-06-23 | Render index 当前状态总览拆分 | 全部未启动 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 18 Advanced Lighting 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：clearcoat/anisotropy/transmission/SSS、cookies/irradiance volumes、froxel fog/OIT/planar reflection；验收缺口：需要等 05/07/08/09/11/13 地基完成后逐 feature 做 parity 和 RenderDoc 验收 |
| 2026-06-15 | AF-M1 material feature family | 未启动: 依赖计划 08 shading model registry | clearcoat、anisotropy、transmission 和 SSS 所需材质参数、G-buffer 位与 shading model 仍未落地。 | 本文件 `现状与差距` 明确 StandardPBR 不含这些参数,计划 08 MS-M3 状态表也标为未启动。 | 先完成计划 08 的 shading model registry、G-buffer packing 与材质资产验证。 |
| 2026-06-15 | AF-M2 light cookies and irradiance volumes | 未启动: 依赖 05/11/13 | `GpuLightData` 尚无 cookie 槽位,计划 11 的 probe/lightmap 仍未完整,texture array/cubemap 资产也未完成;irradiance volumes 仍是后续设计。 | 本文件 `现状与差距` 记录 light cookies 和 irradiance volumes 缺口;计划 05/11/13 状态表分别记录相关基础仍有后续项。 | 扩展 light data ABI、cookie texture binding、local irradiance volume asset 和 sampling path。 |
| 2026-06-15 | AF-M3 froxel volumetric fog and lighting | 未启动: 依赖 light grid/history/post/compute 地基 | 体积介质、froxel volume、volumetric shadowing 和 temporal reprojection 均未实现。 | 本文件 `现状与差距` 明确计划 11 只有解析雾、计划 07 只有 screen-space fog 槽位,不与 light grid/shadow 交互。 | 等计划 05/06/07/16 稳定后实施 froxel grid、lighting injection、history filter 和 composite pass。 |
| 2026-06-15 | AF-M4 OIT, planar reflection and SSS | 未启动: 重型可选项待核心排序/G-buffer 完成 | OIT、planar reflection、Burley SSS 都无实现;当前透明仍依赖排序,反射探针为 cubemap 思路,无实时平面反射。 | 本文件 `现状与差距` 分别记录 OIT、planar reflection、SSS 空缺;计划 09 CO-M3 和计划 08 MS-M3 尚未完成。 | 等 camera/render queue、advanced material G-buffer 和 texture/cubemap 地基完成后分切片实施。 |

### 参考实现精读笔记

(只记录本计划撰写时实际读到的符号;实现前仍须按 index §8.8 通读对应文件。)

**bevy `bevy_light/src/volumetric.rs` + `bevy_pbr/src/volumetric_fog/render.rs`**:契约面是三个组件——相机挂 `VolumetricFog`(:25)、雾体实体挂 `FogVolume`(:78)、灯光挂 `VolumetricLight`(:16)空标记;GPU 面 `extract_volumetric_fog`(:256)收集雾体、`prepare_volumetric_fog_uniforms`(:685)按 view×volume 笛卡尔积填 `VolumetricFogUniform`(:146),`prepare_view_depth_textures_for_volumetric_fog`(:771)说明其依赖深度可采样。Zircon 取舍:组件三分法(settings/雾体/灯光参与位)原样采用;但 bevy 是逐雾体全屏 raymarch,Zircon 走 UE froxel(光照在 3D 网格内一次求值,与 light grid 天然契合),raymarch 仅作 Low 档备选不实现。

**UE `VolumetricFog.cpp`**:`FVolumetricFogMaterialSetupCS`(:249,entry `MaterialSetupCS`)把全局+局部介质写入 VBuffer(`VolumetricFogGridInjectionGroupSize = 4` 的 4×4×4 dispatch,:241);scatter 阶段以 `r.VolumetricFog.LightScatteringSampleJitterMultiplier`(:154)做时域抖动;`FVolumetricFogIntegrationParameterData`(:217)打包网格维度/切片分布进各 shader。Zircon 对应:三段节点同构(inject/scatter/integrate),workgroup 同取 4×4×4;jitter 接 06 的序列而非自带 cvar。

**Fyrox `light_volume.rs`**:`LightVolumeRenderer`(:46)是独立小渲染器(per-light 屏幕空间光柱),证明"体积光可以独立于雾框架做轻量版"——Zircon 不采纳该形态(光柱由 froxel scatter 自然产生),仅在风险节备案为降级路径。

**bevy `oit/mod.rs` + `oit/resolve/`**:`OrderIndependentTransparencySettings`(:42)三参数(默认 8/4.0/0.0)即全部用户面;`OitBuffers`(:152)+`prepare_oit_buffers`(:222)按视口重分配;`OIT_REQUIRED_STORAGE_BUFFERS = 3` 与 `is_oit_supported`(resolve/mod.rs :69)是能力检测样板(检查 fragment 阶段 storage buffer 数);resolve 入口 `oit_resolve`(node.rs :15)。Zircon 取舍:参数名与默认值原样采用;bevy 把 OIT 做成相机组件全局替换透明路径,Zircon 同样按相机粒度,但要求 feature 关闭时连 buffer 都不分配。

**bevy `pbr_material.rs` + `transmission/`**:特性族字段(clearcoat :498、anisotropy_strength :583、specular_transmission :260、thickness :290、ior :338、attenuation :351–363)与 KHR 扩展一一对应,导入器可直通;`main_transmissive_pass_3d`(node.rs :19)按 `ScreenSpaceTransmission.steps`(mod.rs :67)循环"拷 scene → 画一层",`ViewTransmissionTexture`(texture.rs :24)即拷贝目标,`Transmissive3d`(phase.rs :24)是独立 phase。Zircon 取舍:字段名直通 bevy;不新设 phase 而用 09 queue 2900 段(我们的 phase 由 queue 段映射生成,新 phase 即新段,语义等价且不动 02 命令管线)。

**bevy `probe.rs` + `light_probe/irradiance_volume.rs`**:`IrradianceVolume`(:329)契约仅三字段(voxels/intensity/affects_lightmapped_meshes),体素为 ambient cube 编码 3D 纹理;`RenderViewIrradianceVolumeBindGroupEntries`(:165)对"支持 binding array 与否"给 single/multiple 双实现,`IRRADIANCE_VOLUMES_ARE_USABLE`(:161)按平台关断。Zircon 取舍:契约字段照搬 + 加 `transform`/`priority`/`layer_mask`(bevy 靠 ECS transform,我们进 extract);V1 只做 single 形态,binding array 留远期。

**URP `LightCookieManager.cs` + `LightCookie.hlsl`**:主灯 cookie 独立纹理槽、附加灯进 2D atlas,`lightBufferIndex`(:76)注释明示 cookie 数据与灯光 buffer 按索引对齐——与 Zircon "`GpuLightData` 内嵌 uv rect" 同构但我们省去间接表;三投影函数(:16/:38/:53)中 point 灯先变换到灯本地再做方向→2D 映射。Zircon 取舍:point 灯采用导入期 octahedral 预投影,运行时只做方向→oct UV,免 cube atlas。

**HDRP `PlanarReflectionProbe.cs` + `PlanarReflectionFiltering.compute`**:`PlanarReflectionProbe : HDProbe`(:14)把平面探针建模为"捕获设置随主相机镜像派生的探针",`localReferencePosition`(:21)是镜像锚点;滤波 compute 对 RT 做逐 mip roughness 模糊使粗糙材质可采样。Zircon 取舍:同样归入探针体系(进 `zr_environment.wgsl` 混合)而非独立合成 pass;镜像相机经 09 相机循环,捕获即一次普通场景渲染。

**UE `PostProcessSubsurface.cpp`**:文件头注释(:7–8)即三段结构(setup 记录 tile → indirect dispatch Burley → recombine);`FSubsurfaceTiles::ETileType`(:785)区分 AFIS/Separable tile,`ShadingModelMaskInView`(:309)在 view 级早退,`SUBSURFACE_GROUP_SIZE`(:619)= tile 尺寸。Zircon 取舍:三段与 tile indirect 原样采用,只实现 Burley 一种核(Separable 不做);view 级早退对应"帧内无 Subsurface 材质时三节点不进 graph"。

## 风险与回退

- froxel 网格注入无 Rust 同类参照:闭式单测 + 逐切片抓帧先行(index §8.8);若 3D storage 写在个别后端受限,回退为逐切片 2D dispatch(语义不变)。
- OIT 内存:1080p × avg4 ≈ 66 MB,超预算时按计划 17 降级阶梯先降 avg 再整体降回排序透明;OIT 与 09 排序路径长期共存,排序永远是默认。
- planar 成本为整次场景渲染:默认 OnDemand;EveryFrame 仅编辑器预览推荐;镜像相机的阴影/体积雾等重 feature 在镜像 view 内默认裁掉(`RenderLayerSet` + feature 白名单)。
- SSS deferred-only 是 V1 边界:forward 路径回落 StandardPbr 并诊断,不做 forward 近似。
- 与 HGI/VG 插件:froxel scatter 只读 light grid/ShadowAtlas,不感知 GI;HGI 开启时体积光是否消费其辐照由 HGI 计划后续裁决,本计划留采样函数注入点。
- **远期不承诺**(只列名,不展开、不预留接口):体积云、distance field AO、SDF 软阴影、毛发 strand 渲染、水体焦散、光追反射/RTGI(归 ray_tracing policy 与 solari 计划域)。
