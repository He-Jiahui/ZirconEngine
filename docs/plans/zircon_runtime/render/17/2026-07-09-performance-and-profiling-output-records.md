# 17-performance-and-profiling 产出记录归档

> 来源：[`17-performance-and-profiling.md`](../17-performance-and-profiling.md) 的 `## 状态与产出记录`。

## 状态与产出记录

| 日期 | 里程碑/切片 | 状态 | 产出 | 验证与证据 | 后续 |
|------|-------------|------|------|------------|------|
| 2026-08-30 | Runtime90 standalone UI SUI-0至SUI-3 | 源码实现与静态合同通过，动态WGPU采集待完成 | standalone/offscreen共用initial device profile factory；所有native UI context必有typed `Arc<WgpuRenderDevice>` owner；standalone raw submit删除并发布真实ticket；local frame在acquire前唯一poll，旧readback只执行after-poll collect；UI image pin随native packet按ticket退休，fault/poll error同批终结submission、diagnostic与surface。 | 基础owner/profile合同6/6和5/5、SUI-3合同从0/8转为8/8；精确rustfmt、poll/acquire/submit/commit/present顺序、产品UI raw submit/direct poll/extra callback负向扫描、owner行数`763/58/53/475/798/51`与`715/206/129/477/749/113`、scoped diff与locked metadata通过。受管Cargo仍受已知target复用门阻断。 | 真实窗口、device-loss/resize/timeout、PNG/RDC与60帧预热+至少300帧profile/功耗验收。状态`render17_standalone_ui_sui_0_through_sui_3_source_implemented_static_checks_passed_dynamic_validation_pending`。 |
| 2026-08-30 | Runtime90 PFO-4d4b product queue authority | 源码实现与静态合同通过，raw data-plane与动态采集待完成 | scene resource/material frame preparation删除无行为的raw queue透传；product GPU timer改读唯一`WgpuRenderDevice`启动时固化的timestamp period。Legacy test fixture与standalone UI timer保持具名例外。 | failing-first 0/5转为扩展合同7/7；精确rustfmt、raw queue调用面分类、owner行数`798/615/726/945/459/392`、scoped diff与locked metadata通过。没有改变upload batch、native submit或query delivery。 | 继续迁移真实raw Device/Queue consumer；随后执行WGPU、PNG/RDC与60帧预热+至少300帧profile/功耗验收。状态`render17_pfo_4d4b_source_implemented_static_checks_passed_dynamic_validation_pending`。 |
| 2026-08-30 | Runtime09A texture mip preservation typed boundary | 源码实现与静态合同通过，动态WGPU采集待完成 | resident-mip replacement的pre-copy ticket发布`TextureMipPreservationBeforeUpload`；success/failure receipt均保留reason、resource id和ticket，copy/post tickets保持无reason。producer/reason错误配对在transaction变更前fail closed，提交顺序未改变。 | 精确rustfmt、唯一production赋值点、pre-copy/reason/upload顺序、错误配对拒绝与双回执传播通过；boundary/record/receipt root+tests/transaction root+tests/failure/backend/streamer为12/90/334+203/174+228/312/380/248行，结构守卫root/child为351/122行，locked metadata通过。受管Cargo仍受既知target复用门阻断。 | 采集0/1/N replacement场景，再决定是否建立frame-level pre-copy/upload批处理；数据前不做算法优化。状态`render17_texture_mip_preservation_typed_boundary_source_implemented_static_checks_passed_dynamic_validation_pending`。 |
| 2026-08-30 | PF-M1 / Runtime09A frame submission interval metrics | 源码实现与静态合同通过，动态WGPU采集待完成 | 新增backend-neutral `RenderFrameSubmissionMetrics`，frame receipt区分logical packet、flush ticket、physical backend submit及upload batch/write/bytes；compiled/legacy terminal owner以同一generation单调快照封口，owner变化或计数回退不发布样本。 | 精确rustfmt、双owner采样顺序、backend无flush/poll、逻辑/物理计数分离、203行receipt test owner与locked metadata通过；receipt production root在11.8落地时为370行，11.9抽出producer record后current source为334行。受管Cargo仍受既知target复用门阻断；没有WGPU/RenderDoc/PNG/RDC/profile数据。 | 对普通单viewport和显式边界路径各采集至少300帧计数分布并与RenderDoc提交对拍；数据前不实施submit算法优化，不声称一次submit或性能改善。状态`render17_frame_submission_interval_metrics_source_implemented_static_checks_passed_dynamic_validation_pending`。 |
| 2026-06-23 | Render index 当前状态总览拆分 | PF-M1/PF-M3 部分完成,PF-M2/PF-M4 未启动 | 从 docs/plans/zircon_runtime/render/index.md 的第 9 节迁入本计划；本行保留 17 Performance/Profiling 的当前事实，render 总索引不再维护计划级明细。 | 文档重组；本次未改生产代码，render/index.md 只保留状态路由说明。 | 仍未完成：CPU parallelization、compile stutter/perf baseline、预算降级阶梯；验收缺口：需要 GPU timestamp/profile hierarchy、perf fixtures、threshold policy、shader warmup reporting |
| 2026-06-15 | PF-M1 observation base | 部分完成: CPU profiling/markers/diagnostics 存在,GPU timestamp 未接 | `RenderStats`、runtime diagnostics、`profile_scope!`、debug markers 和 RenderDoc capture env hook 已存在并被各计划复用;但无 GPU pass timing、frame profile hierarchy 或 graph dump bundle。 | 本文件 `现状与差距` 实读代码列出 CPU profiling、debug marker、RenderDoc hook 已有,且 `timestamp_writes: None` 全面存在。 | 请求 timestamp feature、实现 `GpuPassTimer`、`RenderFrameProfile` 和 graph dump/profile 关联。 |
| 2026-06-15 | PF-M2 CPU parallelization | 未启动: 渲染提交仍串行 | submit/extract/prepare/present 仍在锁内串行,graph stage pass 逐个录制。 | 本文件 `现状与差距` 记录 `submit_frame_extract` 双锁串行和 `execute_graph_stage` for 循环。 | 设计 pipeline extract/prepare、rayon prepare jobs 和安全的并行 command recording。 |
| 2026-06-15 | PF-M3 memory and bandwidth budgets | 部分完成: 统计字段存在,预算/降级缺失 | 多计划已扩展 `RenderStats` 和 diagnostics,能看到 transient bytes、GPUScene upload、reactive mask、HZB readback 等;但无预算、超限降级或 OOM 阶梯。 | 本文件 `现状与差距` 明确统计扁平且无预算防回归;计划 03/04/06/07 状态表记录多项统计面已接入。 | 建立 budget policy、per-pass memory/bandwidth buckets 和 degrade ladder。 |
| 2026-06-15 | PF-M4 compile stutter and performance regression baseline | 未启动: 无性能 CI gate | CI 只有 build/test,无性能基线、无 shader warmup 统计、无 compile stutter 监控。 | 本文件 `现状与差距` 明确无预算与防回归机制;计划 08 MS-M4 也记录 shader variant cache/warmup 未启动。 | 建立 perf fixture、baseline artifacts、threshold policy 和 shader warmup reporting。 |
| 2026-08-01 | PF-M1..PF-M4 current implementation convergence | PF-M1..PF-M4 核心实现与第二轮源级审查完成；current-source Cargo、产品性能矩阵和 GPU 证据待完成 | 已落 shared WGPU pass timer/profile、bounded pipelined submission、deterministic TaskPool mesh prepare、预算/带宽/lint/degrade ladder、global mip bias、bounded async compiler、exact live PSO prewarm、adapter-keyed persistent pipeline cache及 deterministic two-frame `render_perf` baseline。PF-M2 现由 `FrameCommandEncoderSet` 持有全帧有序 command-buffer 流，`ParallelEncoderSet` 在 TaskPool worker 上录制拓扑分桶并返回 pass-owned profile/report/plugin/replay 结果，提交侧仍保持单次 `queue.submit`；`parallel_record`/`min_passes_per_bucket` 已从 `RenderSubmissionConfig` 原子传播到 compiled-scene 产品路径，关闭、单 worker、单桶、可变 renderer/cache owner 和未审计 executor 均退化为串行。 | 二次审查前向修复共享 replay stats 为原子累计、timestamp scope 独立 reservation/clone、UI/overlay/cache 假可变屏障、config scheduler/renderer 两阶段竞态及 queue-write 并行风险；新增默认 `Serial`、显式 `ParallelSafe` executor 契约，no-op 不创建负收益空并行 buffer。scoped rustfmt/diff-check、单 submit/source contract 和真实 offscreen WGPU 多 encoder 测试已落代码；当前受管请求均在 session.register 或 milestone prepare 前置阶段结束，Cargo 未启动，不能作为通过证据。 | 保持所有相关 failure 为 open；继续 current-source managed tests、开关矩阵与 cold/warm 复盘，实际生成并检查 `docs/tests/runtime/render` 下 2026-08-01 PNG/RDC 后再进入 accepted closeout。 |
| 2026-08-01 | PF-M1 explicit GPU timing resource gate | 非验收实现和第二轮源级复核完成，current-source managed validation 待完成 | `RenderSubmissionConfig.allow_gpu_timing` 默认关闭并独立于 pipelining/async/parallel；WGPU framework 在开关变化时 lazy 创建或释放 scene timer，动态 profile 会传播任意非默认 submission config。retained UI 的 `UiSurfaceDescriptor` 同样默认关闭 timer，仅现有 `ZIRCON_PROFILE_CAPTURE` 采集路径显式开启；关闭时 UI 设备也不请求 timestamp features。Render17 PNG exporter 显式请求 timing，正常产品路径不再为观测常驻 timestamp timer。第二轮复核前向修复 config scheduler/renderer 两阶段更新竞态：先在 scheduler 锁内排空 worker，再以 operation 锁保护 scheduler commit 与 renderer 开关同步。 | 相关 Rust owner全部 rustfmt/diff-check 通过；source contract probe 确认 scene/UI 默认关闭、所有 `GpuPassTimer::try_new` 创建点均受显式选项控制、UI feature negotiation 受同一选项控制、dynamic bridge 不再只识别 pipelining、exporter 显式 `with_gpu_timing()`；锁序复核确认不会持 operation 锁等待 active worker。未把 source probe 冒充 Cargo 或 GPU 运行证据。 | 保持 timer facade failure 为 open；不轮询已排队 validator。由 coordinator wakeup 后完成 current-source Cargo、真实 PNG/RDC 和开关矩阵验收，再进入 accepted closeout。 |
| 2026-08-01 | PF-M1 / Render16 shared readback handoff forward repair | 源码实现与二次审查完成，accepted closeout 待验证 | 唯一 WGPU `GpuReadbackQueue` 下沉 `rhi_wgpu`，计划路径保留 graphics-backend facade；SceneRenderer/UI device 各自唯一 owner。timer、realtime IBL、HZB/mesh、Hybrid GI、particles 已改为共享 N-frame callback，VG 未接入生产的 prepare 契约也禁止私有 map。二次审查前向修复容量失败 callback、粒子陈旧 future、生产 `expect`/mapped slice 越界风险、提交后错误清理顺序及三槽低利用率实际延迟到约720帧的偏差；现80次N+3复用即按240个全局帧触发一次减半。另删除 Hybrid GI/VG 共 9 个中间 buffer 与 9 次冗余 copy，复审 C0/I0。 | scoped rustfmt、diff-check、panic/dead-code/source scan 通过；最新当前源码受管编译 request `4dca61081c8a4e2b88cc857eb66dd89e` 仅为 accepted `session.register` post-timeout，validator 未启动 Cargo。`docs/tests/runtime/render` 尚无 2026-08-01 当前源码 PNG/RDC。 | 不轮询验证队列；完成证据入口审计后 coordinator wakeup。待真实 PNG/RDC 与 current-source tests 返回，再关闭 Render16 failure 与 PF-M1 accepted closeout。 |
| 2026-08-01 | PF-M3 UI command/style compaction and WGPU solid instancing | 非验收实现与第二轮审查完成，accepted closeout 待验证 | runtime RHI新增generation-owned style handle/table并由editor F4 owned chrome stream消费；WGPU普通quad/非圆角border改为32-byte `SolidInstance`与显式instance range，复杂圆角/裁剪保留vertex tessellation。1/100/1000/10000 guides/rows保持单draw、N instances、零驻留solid vertex payload；F4 profile新增submitted/compiled instance与visible compact style计数。`ui_surface.rs`拆出295行compact-style owner与375行test owner，父文件为583行。 | 完整模块树rustfmt、diff-check和第二轮source review通过；拆分测试owner的模块路径已由rustfmt完整解析，WGPU surface setup产品路径无panic。最新编译入口request `4dca61081c8a4e2b88cc857eb66dd89e`只在`session.register` post-response accepted timeout，validator未启动Cargo；真实当前源码PNG/RDC尚未生成。 | 不轮询协调器；继续current-source evidence入口、模块文档与F4 cold/stable counter对拍，Cargo/PNG/RDC齐全后再关闭UI fanout failure。 |
| 2026-08-01 | PF-M3 UI overlap planner allocation convergence | 生产实现完成，动态性能验收待完成 | interval index硬切为连续node/start/end pools，删除每节点Box与双Vec；layer materialization硬切为单一排序index buffer，solid/image/instance payload按精确投影容量预留。10k稀疏结构回归约束每个item只进入一个node及每个crossing pool一次；scale/cache测试拆分为597/335行owners。 | 完整模块树rustfmt、diff-check和静态算法复核通过；最新managed request `4dca61081c8a4e2b88cc857eb66dd89e`未启动Cargo，allocator/p50/p95与产品GPU证据不能用静态结论替代。 | coordinator wakeup后运行当前源码规模矩阵与产品profile，记录allocator和CPU p50/p95，再用PNG/RDC核对draw/pass/resource parity。 |

| 2026-08-01 | PF-M2 Core2D parallel-recording product regression | 源码实现与静态复核完成，current-source managed validation 待完成 | `render_perf_baseline` 新增 Core2D/正交帧夹具，以显式 `ParallelSafe` producer/consumer transient-buffer 依赖对拍串行/并行 capture 像素与 graph 拓扑；并行配置要求至少一次指定 TaskPool worker 录制，同时允许 Rayon 调度器保留部分调用线程工作。 | 文件已由 graphics tests 模块纳入，scoped rustfmt 通过；该结论不替代 Cargo、GPU 运行或 2026-08-01 PNG/RDC。 | 不轮询当前受管请求；coordinator wakeup 后执行产品测试、导出当前源码图像与 RenderDoc capture，再更新 failure 状态。 |

| 2026-08-01 | PF-M2 Render02 mesh prepare precondition | 源码实现与静态契约复核完成，current-source managed mesh/runtime gate 待完成 | `build_mesh_pass_command_buffers_from_batches_cached_parallel` 先按 `source_draw_index` 在 owner 线程完成 variant ID、cache lookup 与统计事务，worker 只消费不可变 `PreparedBatchPlan` 生成 `PreparedBatchChunk`；indexed collection 与 owner 单点 merge 保持原 command/cache 顺序，重复 cache key 或低并行度退回既有串行路径。 | builder 为 630 行，未创建额外 ThreadPool 或在 variant/cache owner 外包 mutex；交接自带 serial/TaskPool generation 1 miss/rebuild 与 generation 2 hit 的逐元素命令/cache parity 覆盖。尚无受管 Cargo 或 PF-M2 runtime receipt。 | 保持 Render02 failure open；coordinator wakeup 后执行 focused mesh tests 与 Render17 runtime gate，确认后由 Render02 负责 return/fixed 状态。 |

### 参考实现精读笔记

- **UE `FRealtimeGPUProfiler`**(`RealtimeGPUProfiler.h`):单例 `Get()`,`BeginFrame/EndFrame(FRHICommandListImmediate&)` 圈帧;`PushEvent(GPUMask, Name, Stat, Description)` 返回 `FRealtimeGPUProfilerQuery`,查询对象延迟 `Submit(RHICmdList, bBegin)`,且专门提供 `Discard(bBegin)` 处理"RDG 建了 profiler event 但 pass 被 culling 从未提交"的情况 —— Zircon 对应:`GpuPassTimer` 的 query 槽按实际录制挂载而非按 graph 声明预分配,01 的 pass culling 天然不产生悬空查询。`ActiveFrame` + `TQueue<TUniquePtr<FRealtimeGPUProfilerFrame>> PendingFrames` + `FRenderQueryPoolRHIRef` 即多帧 in-flight 池,与本计划 3 槽延迟环同构;`FRealtimeGPUProfilerHistoryItem`(HistoryCount=64,`AccumulatedTime`)与 `FetchPerfByDescription` 的 Avg/Min/Max 说明 UI 展示要做滑动窗口平滑 —— 我们放诊断层(`render_stats_store`)做,契约层只传单帧。
- **UE `FParallelCommandListSet`**(`SceneRendering.h` line 475):`NewParallelCommandList/AddParallelCommandList` 聚 `QueuedCommandLists`,派生类析构调 `Dispatch` 顺序合并,`MinDrawsPerCommandList` 控制切分粒度;整个类已 `UE_DEPRECATED(5.5, "Use GraphBuilder.AddDispatchPass instead")`。结论直接吸收:不做 pass 内 draw 级并行拆分(wgpu 也没有 secondary command buffer),并行粒度 = graph pass 桶,阈值参数对齐 `min_passes_per_bucket`。
- **Unity `ProfilingSampler`/`ProfilingScope`**(`ProfilingScope.cs`):一个采样名同时驱动三条时间线 —— `cmd?.BeginSample(m_Marker)` 进 CommandBuffer(渲染线程执行时间 + 自动 `MarkerFlags.SampleGPU` 的 GPU 时间)与 `Inl_<name>` inline marker(调用线程 CPU);recorder 懒分配保证不开观测零常驻成本。Zircon 对应:pass_name 同名贯通 `profile_scope!`(CPU)、debug marker(RenderDoc)、`GpuPassTimer`(GPU)三面;`allow_gpu_timing=false` 时 `GpuPassTimer` 不创建任何 wgpu 资源。
- **bevy `pipelined_rendering.rs`**:`RenderAppChannels` 用两条 `async_channel::bounded(1)` 交换 `SubApp` 所有权,render 线程 `loop { recv → render_app.update() → send back }`;`renderer_extract` 在主线程收回 render app 后才跑 extract(extract 是唯一双世界同步点,对应 `ExtractSchedule`);`Drop for RenderAppChannels` 里 `recv_blocking` 等 render world 归还,保证 non-send 数据在正确线程析构。Zircon 简化:我们传的不是 world 而是 `RenderFrameExtract` 值快照(契约已禁渲染侧访问 ECS),无需 world 往返,单向 frame 通道 + 反向 feedback 通道即可;线程退出排空语义照抄。

## 风险与回退

| 风险 | 缓解 / 回退 |
|------|------------|
| TIMESTAMP_QUERY 在部分后端(GL/老驱动)不可用或精度差 | 能力 gate 已是 None 语义;profile 其余计数维度不受影响;验收明确"无能力平台零 panic" |
| timestamp 读取引入气泡或与 present 抢提交 | resolve/copy 合入帧末既有 encoder,读取走 3 槽非阻塞环;`allow_gpu_timing=false` 一键全关(不创建资源) |
| pipelined 模式暴露隐藏的线程亲和假设(surface/窗口句柄、Drop 顺序) | 回退开关默认关,逐平台开;Drop 排空语义照 bevy;feedback 滞后语义有契约测试,消费方(拾取/回读)按 N-1 帧语义改造在同一里程碑内完成 |
| 并行录制收益不足(pass 数少、单 pass 过重) | `min_passes_per_bucket` 阈值退化单 encoder;收益依赖 02 命令化后录制成本下降,故 PF-M2 排在 01/02 之后 |
| rayon 并行引入排序不确定性导致产物抖动 | 确定性归并(按 sort_key 稳定序,位段归 09)先于并行开启;产物对拍测试在开关矩阵上跑 |
| 降级阶梯与用户显式画质设置打架 | 阶梯只作用于"预算超标"运行态,且 `degrade_step_active` 进 profile 可解释;用户设置为底,阶梯只降不改底 |
| 异步编译占位造成首帧画面缺 draw 被误判为 bug | 占位策略仅两档且进 stats(`last_pipeline_async_pending_count`);`allow_async_compile=false` 回退同步编译 |
| 基线上限定得过松(永远绿)或过紧(噪声红) | 只断言确定性计数不断言时间;每个基线测试配一个"注入劣化必须红"的反向用例防失效 |
| `RenderStats` 双形态(扁平 + 层级)长期并存漂移 | `render_perf_frame_profile_matches_flat_stats` 自洽断言常驻;扁平字段新增项必须同步进层级聚合,否则该测试红 |

回退总开关:`allow_gpu_timing` / `allow_pipelined_render` / `allow_parallel_record` / `allow_async_compile` 四开关相互独立,任意组合下 `render_product_*` 产物一致是 PF-M2/M4 的硬验收;全关即与本计划落地前行为等价(观测字段为空,不影响渲染正确性)。
