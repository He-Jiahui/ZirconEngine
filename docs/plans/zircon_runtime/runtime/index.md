---
related_code:
  - zircon_runtime/src/lib.rs
  - zircon_runtime/src/builtin/runtime_modules.rs
  - zircon_runtime/src/core/mod.rs
  - zircon_runtime/src/scene/ecs/mod.rs
  - zircon_runtime/src/plugin/mod.rs
  - zircon_runtime/src/asset/pipeline/worker_pool.rs
  - zircon_app/Cargo.toml
  - zircon_runtime/Cargo.toml
  - dev/bevy/crates/bevy_app/src/main_schedule.rs
  - dev/bevy/crates/bevy_asset/src
  - dev/Fyrox/fyrox-impl/src/plugin/mod.rs
  - dev/UnrealEngine/Engine/Source/Runtime
plan_sources:
  - .codex/plans/Zircon Runtime 架构渐进式 Review 与优化计划.md
  - .codex/plans/Runtime 吸收层与 Editor_Scene 边界收束计划.md
  - .codex/plans/全系统重构方案.md
  - docs/plans/zircon_runtime/render/index.md
---

# Zircon Runtime 架构完善与优化总体计划

本目录是 `zircon_runtime` 在渲染骨架计划（`docs/plans/zircon_runtime/render/01-08`）之外的 runtime 侧架构完善总计划：技术选型治理、core spine 与 root surface 收束、调度/帧循环、资产管线、scene/editor 边界收尾、插件公开面与生命周期、runtime 侧性能热路径。它承接 `.codex/plans/Zircon Runtime 架构渐进式 Review 与优化计划.md`（M0–M7），把其中 runtime 侧尚未完成的项落成可执行子计划，并以 2026-06-12 的实仓审计修正其现状假设。

## 1. 技术选型评审结论（2026-06-12 实仓核对）

总体结论:选型与 Bevy 同代生态一致（winit + wgpu/naga + glam + crossbeam + notify + taffy），自研 ECS/UI/资产管线的目录形状与 `bevy_ecs`/`bevy_asset` 可逐项对照，方向合理。但"声称技术栈"与实仓有 5 处失实，另有 4 个能力缺口需要决策。

### 1.1 声称栈核对表

| 声称 | 实仓状态 | 证据 | 评估 |
|------|---------|------|------|
| winit 窗口输入 | ✓ `0.31.0-beta.2`，optional + feature 门控 | 根 `Cargo.toml`、`zircon_runtime/Cargo.toml` | 合理；**beta 版本风险**，见子计划 01 |
| wgpu / naga 渲染 | ✓ 29.0.1 / 29.0.1 配套一致 | 根 `Cargo.toml` | 合理 |
| taffy UI 布局 | ✓ 0.10，自研 UI 经 `ui/layout/taffy_bridge.rs` 桥接 | `zircon_runtime/Cargo.toml` | 合理 |
| glam 数学 | ✓ 0.32.1 (serde) | 根 `Cargo.toml` | 合理 |
| fontdue / cosmic-text / glyphon | **部分失实**:fontdue 0.9.3 仅在 editor;cosmic-text 不存在;runtime 实际为 glyphon 0.11 + fontsdf 0.5.3 + 自研 text shaper | `zircon_editor/Cargo.toml`、`zircon_runtime/Cargo.toml:77-78` | 三库职责口径需定稿，见子计划 01 |
| 自研 ECS | ✓ `scene/ecs/`（archetype、query、schedule、parallel executor、conflict graph、observer、change detection） | `zircon_runtime/src/scene/ecs/` | 形状对齐 `bevy_ecs`，合理 |
| kira 音频 | **失实**:不存在;实际为 cpal 0.15（optional）+ sound 插件自研 DSP/HRTF/occlusion/mixer | `zircon_plugins/sound/runtime/Cargo.toml` | 既有自研混音栈下 cpal 底座更合理，**不建议**再引 kira;矫正文档即可 |
| image / serde / crossbeam 异步加载 | ✓ 全部存在;worker pool + watcher 在 `asset/pipeline`、`asset/watch` | `zircon_runtime/src/asset/` | 合理 |
| zip / tar 打包 | **失实**:均不存在;仅 zstd 0.13.3;`ExportPackagingStrategy` 已有契约但无归档实现 | `zircon_runtime/Cargo.toml:100`、`plugin/export_profile.rs` | **导出打包能力缺口**，见子计划 01 |
| gilrs 手柄 | ✓ 0.11.0，optional，`gamepad-gilrs` feature | `zircon_app/Cargo.toml:84` | 合理 |
| tracing / tracing-subscriber | ✓ tracing 常驻;subscriber 0.3.20 仅在 `profiling-tracy` 后 | `zircon_runtime/Cargo.toml:24,93` | 合理;profiling 构建超时问题见子计划 07 |
| rfd / arboard 编辑器辅助 | **失实**:均不存在 | 全仓 Cargo.toml grep 无命中 | editor 侧文件对话框/剪贴板缺口，归 `zircon_editor` 决策 |

### 1.2 声称栈未列、但实际承重的依赖

rayon（ECS/资产并行）、tokio + hyper + reqwest + tokio-tungstenite（net 插件网络栈）、bincode/ron/toml/serde_json（多格式序列化）、gltf/tobj/dxf/ply/stl（模型导入）、notify 9.0.0-rc.3（资产监视）、libloading（cdylib runtime 与 native 插件）、zstd、accesskit（optional）、Recast C++ 经 cc 绑定（navigation 插件）、tauri 2.11（zircon_hub，**Slint 已不在任何 Cargo.toml**）、zr_vm_rust_binding（**指向仓库外 `../../zr_vm` 的路径依赖**）。

### 1.3 版本与依赖治理风险

1. `winit 0.31.0-beta.2`:beta 跟踪策略未定稿（锁定/升级 gate）。
2. `notify 9.0.0-rc.3`:RC 版本。
3. `zr_vm_rust_binding` 路径依赖逃逸出仓库根，影响 clone 即建的可复现性（optional 缓解，仍需文档化或 vendor 决策）。
4. `jolt = []` 空 feature + physics backend:已由 Runtime 01 M3 裁决为 plugin-owned builtin V1 可执行后端 + future Jolt unavailable slot；Rapier 不进主路径，剩余实现工作归物理插件后续 native-backend 计划。

## 2. 架构评审结论

### 2.1 已收敛项（旧计划假设需修正处）

旧《渐进式 Review 计划》的部分目标已经落地，本计划不再重复:

- `zircon_runtime/src/lib.rs` 仅 74 行，root surface 已薄化;`root_surface_guard` 固定当前 20 个 public module declarations、3 个 public `pub use` sites、禁止子系统 root flattening，并把 graphics alias block 标为 crate-private M3 debt；M3.2 type alias debt 当前状态为 `pre_m3_type_alias_guard_static_passed_pending_render_owner`，实际删除仍等待 render owner；`builtin/runtime_modules.rs` 已是 28 行 folder-backed 装配入口（assembly/manifest/core_modules/plugin_modules/load_report 分离）。旧计划 M1/M2 对这两处的"待拆"描述已过时。
- `zircon_app` 插件扇出已收束为单一 `zircon_first_party_runtime_catalog` optional 依赖（`zircon_app/Cargo.toml:88`），feature 全部经 catalog 转发。
- 服务注册热路径已是强类型键:`HashMap<RegistryName, ServiceEntry>`，`RegistryName` 带缓存解析偏移（`core/runtime/descriptors/registry_name.rs`），非 per-frame 字符串查找。
- World 序列化不含 selection/viewport authoring 状态，且已有 serialization_guard 守卫;测试树全部 folder-backed，无巨型 `tests.rs`。
- 渲染分层（framework 契约 / graphics 实现 / rhi / rhi_wgpu）与 UE 的 Engine/Renderer/RenderCore/RHI 方向一致，骨架差距已由 render 子计划 01–08 接管。

### 2.2 问题清单（本计划的工作对象）

| # | 问题 | 证据 | 子计划 |
|---|------|------|--------|
| P1 | `lib.rs:39-72` 的 70+ 类型 `pub(crate) use` 巨型别名块:内部调用方经 crate 根访问 graphics 内部类型，掩盖 owner 路径;且混入 HybridGi/VirtualGeometry/Solari 等插件 provider 语义 | `zircon_runtime/src/lib.rs:39-72` | 02 |
| P2 | ~~`core/` 根下 13+ 散件游离于五件套 spine 口径之外，归属未定~~（2026-06-13 复核：Runtime 02 M2 已硬切换完成，`core/` 根当前仅 `framework/manager/math/resource/runtime/mod.rs`；`root_entries` 守卫覆盖 retired fragments 与 spine 形态；新增 `root_surface_guard` / `docs/zircon_runtime/core/root_surface.md` 固定 20 个 root namespace module、3 个 public `pub use` sites、无子系统 root flattening，并把 graphics alias block 继续限制为 crate-private debt；M3.2 type alias debt 已进入 `pre_m3_type_alias_guard_static_passed_pending_render_owner`，禁止转成 public root export，实际删除仍待 render owner 稳定；`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation` 锁定 core/root/generated/export_build_plan/app/editor/plugin 验证；Cargo default/lib-test 仍待 active lanes 清空后重跑） | `zircon_runtime/src/core/mod.rs`、`zircon_runtime/src/core/{framework,manager,math,resource,runtime}`、`zircon_runtime/src/tests/runtime_absorption/{root_entries,root_surface}.rs`、`docs/zircon_runtime/core/root_surface.md`、`docs/engine-architecture/core-runtime-service-registry.md` | 02 |
| P3 | ~~帧循环阶段无单一权威定义~~（2026-06-12 M0/M1/M2 复核矫正:`SystemStage` 九阶段权威表、`FIXED_LOOP`、Real/Virtual/Fixed 三时钟均已落地）。`WorldDriver` 已改为消费上游 `RuntimeTimeAdvance`，二次 `advance_time_by(...)` 与 driver 局部 cap=4 已删除；`FixedStepPlan::overstep_fraction()` 已提供；UI extract 已定稿为 runtime 03 合法 dynamic-session side path；阶段内顺序已有注册顺序负例守卫。M2 固定步 owner 测试已拆入 `scene/tests/ecs_schedule/fixed_update.rs`，覆盖 drained steps、零 lag、cap；M3.1 已补 `ScheduleParallelExecutionReport`、关闭并行开关与 `schedule.parallel_batches` / `schedule.serial_fallbacks` 诊断；M3.2 已补代表性多批次收益与串并行终态一致性守卫。`schedule_frame_loop_boundary` 已把 source files 18/18、guard/test files 8/8、`SystemStage` count and variants 9/9、fixed-loop stages 3/3、dynamic-session `.tick_time(...)` calls 1/1、Runtime 03 guard anchors 14/14、no `WorldDriver` second `advance_time_by(...)` references、no dynamic-session raw-delta level tick references 与 `risks = []` 纳入结构审计；`runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 锁定 Runtime 03 计划、总索引、M0 review 与 runtime-interface convergence 的镜像数字。剩余缺口:`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation` 锁定 `ecs_schedule/time/session/schedule_parallel` Cargo 回归待运行 | `docs/zircon_runtime/core/frame_schedule.md`、`docs/zircon_runtime/scene/ecs/schedule_parallel_executor.md`、`scene/ecs/system_stage.rs:4-45`、`core/runtime/time.rs`、`core/framework/time/fixed_step_plan.rs`、`scene/module/world_driver.rs:11-73`、`dynamic_api/session.rs`、`dynamic_api/session/extract.rs`、`scene/ecs/schedule_parallel_executor.rs`、`scene/tests/ecs_schedule.rs`、`scene/tests/ecs_schedule/fixed_update.rs`、`dynamic_api/tests/session_profiles.rs`、`schedule_frame_loop_boundary` | 03 |
| P4 | `plugin/mod.rs` 把 native plugin loader 与 native bridge-method 相关类型/常量全量 `pub`，与"native loader 退出公共主路径"决策冲突；细化重核：V1/V2 真实使用者仅 `native_dynamic_fixture` 夹具 1 文件，export 宿主文件不引用版本符号，淘汰面比预估小。当前 `native_plugin_public_surface.m4_gate_status=migration-debt-present` 且 `root_reexport_count = 68`；`runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation` 锁定 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` 验证线，在真实后端、plugin/native plugin、app 与插件 workspace Cargo 通过前不得关账 | `zircon_runtime/src/plugin/mod.rs`、`docs/engine-architecture/native-plugin-boundary.md`、`docs/engine-architecture/runtime-interface-convergence.md`、`tests/runtime_absorption/plan_status/cargo_gates/early.rs` | 06 |
| P5 | 实测性能 ~10fps（230 draws / 231 pre-draw buffer copies / 31 passes）;权威 FPS 取证被 ZrVM native assertion（`zr_vm_core.dll function.c:1394`，空参数空指针）阻塞;2026-06-12 binding 层已修复空参数 marshalling 并通过 binding 回归，runtime 真实后端验证仍待完成;profiling 构建两次超时；`performance_hotpath_boundary` 已把 source 10/10、guard/test 5/5、frame span anchors 9/9、QueryState/change-detection/extract/asset-worker 计数 anchors 全满、hotspot guard anchors 16/16、pending gate anchors 5/5、Runtime 07 owner-budgeted optimization gate 与 `large_file_ownership_gate` 摘要纳入结构审计；当前大文件门为 `migration-debt-present`（1000 行阈值、hotspots 41、debt groups 5、owner classes 5、unclassified 0），M2 优化不得在大文件仍超 owner budget 时继续堆行为；`risks = []` 仅代表 Runtime 07 镜像同步；剩余缺口：`runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation` 锁定 extract/ecs_query/performance profiling/FPS gates 的 Cargo/profiling/FPS 待运行状态 | `.codex/sessions/20260611-0416-rendering-10fps-analysis.md`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/performance_hotpath_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/large_file_ownership.py` | 07（render 侧归 render/01-08） |
| P6 | ~~`scene/editor_projection/` 空目录残留~~（2026-06-12 矫正:目录已删，残留文本只保留在禁止复活守卫中）;runtime 内 "editor" 白名单裁决与守卫机器化已落 `runtime_naming_boundary` / `runtime_absorption::naming_boundary`（2026-06-13 当前工作区 editor 1363 locations / 237 files / 0 unclassified；序列化纯净 token 守卫已存在:`scene/tests/authoring_boundary.rs` 双 token 表 19+25 词） | `scene/tests/authoring_boundary.rs`、`scene/tests/inspection.rs`、`runtime_absorption/naming_boundary.rs` | 05 |
| P7 | 资产管线对照差距已由 Runtime 04 M1-M3 静态/聚焦回归收束：句柄语义裁决为保留 `Copy` typed ID，悬挂句柄查询返回 `NotLoaded`；Runtime 04 reference gap table 已定稿 handle/loader/processor 裁决，不再保留 unresolved 对照占位；asset facade 查询面已锁为 `ProjectAssetManager` / `Assets<TAsset>` / `AssetManager` service trait 的 manager/facade/service 分层，并由 `runtime_04_asset_facade_query_surface_stays_manager_owned_and_server_free` 禁止回退到 asset server 词汇；`core::resource` 状态机收紧为失败后必须经 `Reloading`；worker pool 已落 `AssetWorkerPoolOptions`、bounded 背压、in-flight 去重、诊断计数与 Runtime 11 IO 预算记账，并由 `asset_worker_pool_matches_runtime_04_and_11_decisions` 固定当前状态不回退到原始无背压/无去重基线；watcher 已落 debounce/options、watch error 订阅与 typed hot-reload event 回归；Scene `.zasset` cache wire type 修复后 `artifact_store_roundtrips_scene_assets_with` 4/4 与 `watcher` 7/7 已通过。`asset_pipeline_boundary` 已把 source 19/19、guard 10/10、worker diagnostics 5/5、artifact roundtrip 4/4、guard anchors 21/21、旧 worker-count 构造 0、旧 `WATCH_DEBOUNCE` 回退 0 纳入结构审计且 `risks = []`。剩余：`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation` 锁定 M1/M2 broader `asset::` / `worker_pool` Cargo filters 仍待干净编译窗口重跑。 | `docs/plans/zircon_runtime/runtime/04-asset-pipeline-alignment.md`、`docs/zircon_runtime/asset/{facade,worker_pool,watcher,artifact}.md`、`tests/runtime_absorption/{asset_surface,asset_worker_policy}.rs`、`asset/pipeline/worker_pool.rs`、`asset/tests/pipeline/worker_pool.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/asset_pipeline_boundary.py` | 04 |
| P8 | generated 口径矫正:真实文件头 `// @generated ...` 标记为 0，"generated" 词根命中 42 文件（混杂法线生成等领域词）;标记规范与 leaf DTO/table 守卫已固化。M4.2 已把生成入口中的 direct `EntryRunner`/`NativePluginLoader` 行为迁入 `zircon_app::entry::export_bootstrap`，并把 linked plugin registration 从即时调用改成 app provider 表；provider 行已裁决为允许的 generated table adapter，结构审计从 13 行为点/5 标签/5 债务降到 6 行为点/3 标签/0 债务，`m1_gate_status=classified-and-clear`；`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation` 锁定 generated/export_build_plan/app 回归；Cargo 测试阶段仍受 render/graphics 编译漂移与 Windows 测试目标超时影响 | `docs/engine-architecture/generated-code-boundary.md`、`zircon_app/src/entry/export_bootstrap.rs`、`plugin/export_build_plan/*template*.rs` | 02 |
| P9 | "legacy" 命名已机器化裁决（2026-06-13 当前工作区 runtime-only 审计 518 locations / 101 files / 0 unclassified）；剩余 10 个 debt bucket 按 runtime UI input/render、graphics、DDS、UI template/layout、input、asset、dynamic API、scene schema owner 分派 | `runtime_naming_boundary` 结构审计 | 05 |
| P10 | 物理 / 导出归档 / editor 辅助三类完备性缺口已由 Runtime 01 M3 裁决并有守卫锚：physics 插件不是空壳，V1 唯一可执行后端为 builtin，Jolt 保留为未来 native 方向但保持 unavailable，Rapier 不进主路径；`physics_backend_option_decision_keeps_jolt_unavailable_and_plugin_owned` 锁定两处 `jolt = []` 空 feature、`JOLT_BACKEND_AVAILABLE = false`、Jolt 不降级 builtin、Rapier/Avian 不进 manifest；导出归档当前目录优先，未来桌面/editor 容器裁决为 ZIP，但 `zip`/`tar` 不进 manifest 直到 archive materialization 落地；`fontdue`/`rfd`/`arboard` 均为 editor-host 候选或临时债务，不属于 runtime/interface 技术栈。`tech_stack_boundary` 已把 manifest 5/5、corrected non-dependencies 6、guard anchors 11/11、editor-only candidates 3、Jolt slots 2、removed/editor-only manifest deps 0、Rapier/Avian deps 0 纳入结构审计且 `risks = []`。`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation` 锁定 Runtime 01 在 `tech_stack/text_shaper/plugin physics Cargo gates` 通过前保持待验证状态 | `docs/zircon_plugins/physics-plugin-options.md`、`docs/engine-architecture/runtime-tech-stack.md`、`docs/editor-and-tooling/runtime-editor-only-dependency-backlog.md`、`tests/extensions/tech_stack_dependency_guard.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_boundary.py` | 01 |
| P11 | ECS 数据面与 `bevy_ecs` 对照已启动（2026-06-12 盘点修正）:`StorageType::{Table,SparseSet}` 与 `ComponentStorage` 双 backing store 已确认；实体生命周期测试矩阵、观察者三类入口触发时机测试、命令队列错误报告面、events/messages 双通道分工测试、change tick 回绕与 stale 窗口截断测试已落地待 Cargo；`ecs_kernel_data_boundary` 已把 `expected_source_file_count = 20`、`expected_test_file_count = 7`、`storage_anchors = 9/9`、`entity_lifecycle_anchors = 10/10`、`observer_anchors = 8/8`、`deferred_command_anchors = 11/11`、`event_message_anchors = 11/11`、`change_tick_anchors = 6/6`、`runtime_08_guard_anchors = 17/17`、`doc_anchors = 7/7`、`pending_cargo_gate_anchors = 6/6`、`mirror_docs_guard_present = true`、`risks = []` 纳入结构审计；`runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；`runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation` 锁定这些行在 Cargo 过滤测试通过前不能提升完成 | `docs/zircon_runtime/scene/ecs.md` Runtime 08 数据面对照、`scene/ecs/` 40 条目盘点、`observer.rs:54-112`、`commands/command_queue.rs`、`events.rs`、`messages.rs`、`change_detection/{change_tick.rs,change_tick_window.rs}`、`tests/runtime_absorption/ecs_kernel_data.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ecs_kernel_data_boundary.py` | 08 |
| P12 | UI 子系统 Runtime 09 M0 已静态落地：`docs/zircon_runtime/ui/architecture.md` 覆盖 17 个 `ui/` 顶层条目与当前 20 个 `surface/` 条目，裁决 v2 为 replacement mainline；`.zui` 是生产 component suffix，`.v2.ui.toml` 保留为 v2 view/style/runtime fixture/editor chrome profile，old recursive template 路径为 migration/test-only 直到 M3；UI legacy 基线为 `ui_legacy_hits=167` / `ui_legacy_production_hits=102` / `ui_legacy_production_files=12`，taffy production 基线为 `ui_taffy_production_hits=161` / `ui_taffy_production_files=7`；`runtime_absorption::ui_architecture` 已新增三条结构守卫锁模块边界、基线扫描与 v2 runtime/interface 模块形状（Cargo 待 active lanes 清空）；`ui_architecture_boundary` 已接入总审计，报告 source/doc 11/11、`ui/` entries 17/17、`surface/` entries 20/20、runtime `ui::v2` 10/10、interface `ui::v2` 9/9、guard anchors 4/4、pending gate 7/7、doc anchors 10/10、`risks = []`；`runtime_09_m0_ui_architecture_static_passed` 仍是 static-only，`runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation` 锁定 `ui/input/naming_boundary/layout/template` owner/Cargo gate；M1 输入路由单点、M2 taffy owner 收敛、M3 template generated/failure-path 仍待 editor UI owner 与 Cargo lane 空窗 | `docs/zircon_runtime/ui/architecture.md`、`docs/plans/zircon_runtime/runtime/09-ui-subsystem-architecture.md`、`zircon_runtime/src/tests/runtime_absorption/ui_architecture.rs`、`tests/runtime_absorption/plan_status/cargo_gates/middle.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ui_architecture_boundary.py`、`ui/` 17 条目、`surface/` 20 条目、`surface/input/navigation.rs:22-54` | 09 |
| P13 | cdylib 函数表版本不同步（`ZrRuntimeApiV1`/`ZrHostApiV1` vs plugin 宿主 `ZrHostApiV3` + 子 API V1×5，含 `ZrHostBridgeApiV1`）已由 runtime 10 定稿保守 bump 规则;interface `ui/` 22 条目镜像契约仍是 M2 owner/Cargo gate：`runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff` 锁定 UI 镜像契约 M2 owner/Cargo gate，在 Runtime 09/editor UI owner 交接与 interface/ui/editor Cargo 验证前不得误标完成；session 失败路径中的坏句柄/缺失非零句柄/销毁注册表 removal 守卫已 scoped Cargo 通过，`minimal`/`headless` profile 已改为跳过 `RuntimeRenderBridge` 并补 live double-destroy / destroyed-handle 夹具；`runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces` 进一步锁定 optional render bridge、capture 空帧 fallback 与 bind/unbind/present no-op，新增守卫局部 rustfmt/锚点/差异检查通过；FFI panic 边界首批已静态落地，`dynamic_api/exports.rs` 的 `zircon_runtime_get_api_v1` 与 11 个函数表入口经 `catch_unwind` wrapper 收口，panic 转 `ZrStatusCode::Panic` 或 null 表指针，`session.rs` owner 函数保持 Rust ABI，并由 `runtime_api_table_entries_are_panic_wrapped_at_ffi_boundary` 与 `runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge` 锁定，M1.3 rustfmt/锚点/差异检查通过；`dynamic_runtime_api_boundary` 已把 Runtime 10 的 `expected_source_file_count = 14`、`function_table_structs = 10/10`、`field_count_mismatches = 0`、`missing_repr_c_tables = 0`、`runtime_session_ffi_wrappers = 11/11`、`direct_session_table_entry_bypasses = 0`、`session_owner_extern_c_present = false`、`headless_lifecycle_anchors = 12/12`、`ffi_panic_anchors = 9/9`、`loader_failure_anchors = 10/10`、`ui_pending_gate_anchors = 8/8`、`pending_cargo_gate_anchors = 5/5`、`doc_anchors = 7/7`、`mirror_docs_guard_present = true`、`risks = []` 纳入结构审计；`runtime_10_dynamic_runtime_api_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；Runtime 10 的 headless lifecycle、panic-boundary、完整 app loader 与 UI 镜像契约 Cargo 验证仍待活动编译通道空闲后重跑 | `runtime_api/api_table.rs:43,63`、`plugin_api.rs:41-249`、`dynamic_api/exports.rs:25`、`dynamic_api/session.rs`、`dynamic_api/tests/{api_table,session_lifecycle,session_entry_points,session_profiles}.rs`、`tests/runtime_absorption/dynamic_api_session.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/dynamic_runtime_api_boundary.py` | 10 |
| P14 | JobSystem M0/M1 已静态扩展：`TaskPoolOptions` 为线程预算 owner，`JobHandle`（wait/combine）、`JobScheduler::schedule`/`schedule_after` 与阻塞式 `parallel_for` 已在 `core/runtime/tasks` 落地并有 tasks 测试锚；`schedule_after` 用完成回调启动依赖任务，不占 worker 等待。M2.3 ECS 批次依赖化已静态落地：`ScheduleParallelExecutor` 现在用 `JobHandle::completed()` 起点 + `schedule_after` 链提交每批，等待尾批并按批次顺序回放错误；`ScheduleParallelTaskRegistry` 内部改为 `Arc` 任务；M2.2 非 graphics 收编也已静态落地，批次内部固定/泛化并行路径改走 `JobScheduler::join(...)` 与 `run_parallel_tasks(...)`，`schedule_parallel_executor_does_not_call_rayon_directly` 与 `runtime_absorption::rayon_boundary` 固定生产源码直连 rayon 边界。M2.1 当前为 `pre_m2_1_rayon_render_exception_guard_static_passed_pending_render_owner`：`parallel_frustum` 是唯一 `render-owner-pending-runtime-11-m2-1-cutover` direct-rayon 例外，actual graphics cutover not executed，真实迁移等待 render owner。M2.4 asset worker 线程来源已按显式记账路线静态落地：`ProjectAssetManager::default()` 与 asset module factory 从 `TaskPoolOptions` IO 配额推导 worker 数，显式构造保持 override，并通过 `asset.worker.budgeted_threads` 诊断记账；`asset_worker_pool_matches_runtime_04_and_11_decisions` 同步锁定该跨计划判词。M3.1 调度诊断已补 `JobSchedulerReport`、`tasks.scheduled` / `tasks.completed` / `tasks.dependency_wait_ms` / `tasks.main_thread_wait_ms` 与 `DiagnosticStore` 发布路径；M3.2 压测锚已补 64 层依赖链与 128 宽扇出 combine。`job_system_boundary` 已报告 `expected_module_count = 9`、`direct_rayon_paths = 3`、`schedule_parallel_executor_direct_rayon = []`、`diagnostic_anchor_count = 4`、`oversized_modules = []`、`mirror_docs_guard_present = true` 与 `risks = []`；`runtime_11_job_system_mirror_docs_match_structure_audit_counts` 锁定 JobSystem 模块文档、Runtime 11、总索引、M0 review 与 runtime-interface convergence 的镜像数字。`runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass` 锁定 Runtime 11 在验证前保持待完成。剩余 M2 缺口：graphics `parallel_frustum` 仍需在 render owner 窗口剔除 rayon 旁路；tasks/ecs_schedule/worker_pool/rayon Cargo 待 active lanes 清空 | `docs/zircon_runtime/core/job_system.md`、`docs/zircon_runtime/asset/worker_pool.md`、`docs/zircon_runtime/scene/ecs/schedule_parallel_executor.md`、`core/runtime/tasks/{diagnostics,job_handle,job_scheduler,parallel_for,report}.rs`、`scene/ecs/schedule_parallel_executor.rs`、`scene/tests/ecs_schedule/parallel_executor.rs`、`scene/tests/ecs_schedule_parallel_executor_structure.rs`、`tests/runtime_absorption/{job_system,rayon_boundary,asset_worker_policy}.rs`、`tests/tasks.rs`、`graphics/visibility/culling/parallel_frustum.rs`、`asset/pipeline/worker_pool.rs`、`asset/pipeline/manager/project_asset_manager/{project_asset_manager,construction,debug}.rs`、`asset/tests/pipeline/worker_pool.rs` | 11 |
| P15 | 输入栈最小动作映射层已由 12-M1 静态落地（`InputAction`/`InputBinding`/`InputActionMap`/`InputActionState` + `InputActionEvaluator`），Cargo 待跑；帧输入语义（just_*/清帧时机）已由 12-M0.1 文档与测试锚静态落地但 Cargo 待跑；gamepad 已确认 app 侧 gilrs → runtime ABI → `InputEvent::Gamepad*` 路径并由 12-M2.1 新增断开清理/source guard 锚，Cargo/app 回归待跑；UI 与玩法的输入仲裁已由 12-M0.2 裁决为 UI surface/capture/popup/focus 优先、玩法/action mapping 只消费 UI 未处理或无 UI/headless 输入流；`runtime_absorption::input_stack` 已锁帧输入导出/文档、UI-filtered action evaluator、gamepad ABI bridge 与 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 镜像文档计数；`input_stack_boundary` 结构审计已接入总审计，报告 `expected_runtime_module_count = 10`、`expected_framework_module_count = 17`、`expected_test_module_count = 5`、`public_surface_anchors = 10/10`、`runtime_12_guard_anchors = 5/5`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`，但 `runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation` 仍锁定 input/action_map/gamepad/app Cargo gate | `input/mod.rs:6-22`、`input/runtime/{default_input_manager,input_driver,input_state,action_evaluator}.rs`、`core/framework/input/input_action*.rs`、`core/framework/input/input_binding.rs`、`zircon_app/src/entry/runtime_entry_app/gamepad/{events,polling}.rs`、`tests/runtime_absorption/input_stack.rs`、`docs/plans/zircon_runtime/runtime/12-input-stack-and-action-mapping.md`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/input_stack_boundary.py` | 12 |
| P16 | 脚本绑定面已由 Runtime 13 静态收束：`function_ledger.md` 固定 builtin/gameplay host ledger 为 6 模块 / 50 函数 / 2 类型描述符，`zr.zircon.bridge` 只锁动态模块形状；`ZirconScriptType`/VM host marshalling 已裁决为 value descriptors / host handles / serialized payloads 三类；脚本-ECS 默认路径为 `zr.zircon.gameplay` / `ScriptRuntimeCallContext` gameplay facade，`ZrHostEcsApiV1` 保持 native/plugin ABI 层；`host_function_registry_matches_documented_ledger`、capability 代表函数 descriptor 守卫、缺失授权拒绝守卫、脚本持有实体 despawn 后失效语义与 native ECS ABI 旁路 source guard 已静态落地；`script_binding_boundary` 结构审计已接入总审计，报告 source files 10/10、guard/test files 2/2、callback counts 11/37/2、host capability anchors 11/11、guard anchors 7/7、native ECS ABI references 0、`risks = []`；`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass` 锁定 Runtime 13 在 script filters 通过前保持 Cargo 待 active lane 清空状态 | `docs/zircon_runtime/script/vm/host/function_ledger.md`、`tests/runtime_absorption/script_host_ledger.rs`、`script/vm/gameplay_host/tests.rs`、`script/vm/capability_set.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/script_binding_boundary.py` | 13 |
| P17 | crate 根四个"小族"已由 Runtime 14 M0/M1 静态收束：animation 27 个 `.rs` 文件并补 bevy/Fyrox/Godot 差距表，root motion backlog、GPU skinning/render owner 与 editor tooling 非目标已由 `runtime_animation_backlog_boundary_requires_doc_update` 守卫；navigation 3 个 `.rs` 文件但 `runtime.rs` 约 32KB，已裁决为 built-in fallback runtime 且由 `runtime_navigation_boundary_file_set_requires_doc_update` 守卫文件集；diagnostic_log vs core::diagnostics 双诊断面分工已文档化，`diagnostic_log_snapshot_bridge_stays_single_owner` 固定唯一 snapshot-to-log 桥；engine_module 已裁决为 core::runtime 之上的 declared layering，不合并、不删除，并由 `engine_module_declared_layer_does_not_own_runtime_lifecycle` 守卫；`runtime_14_module_family_root_seats_match_documented_judgements` 进一步锁定四族仍是 crate-root module family 且不在 `lib.rs` 扁平化 re-export；Runtime 14 仍非 completed，Cargo/rustc 重跑待 active lane 清空 | `docs/zircon_runtime/animation/runtime.md`、`docs/zircon_runtime/navigation/runtime.md`、`docs/zircon_runtime/diagnostic_log/mod.md`、`docs/zircon_runtime/engine_module/relationship.md`、`tests/runtime_absorption/root_entries.rs`、`engine_module/tests.rs`、`diagnostic_log/diagnostics.rs` | 14 |

### 2.3 参考引擎证据锚点（2026-06-13 扩充：全路径实测存在；实现型切片动工前必读对应行，见 §7.9）

| 维度 | Bevy | Fyrox | Unreal | Godot / 其他 |
|------|------|-------|--------|--------------|
| 帧循环权威 | `dev/bevy/crates/bevy_app/src/main_schedule.rs`（MainScheduleOrder） | `dev/Fyrox/fyrox-impl/src/engine/executor.rs`（fixed-step 累积） | Tick group（PrePhysics→PostUpdateWork） | `dev/godot/core/os/main_loop.{h,cpp}`、`dev/godot/main/main.cpp` |
| 固定步与时钟 | `dev/bevy/crates/bevy_time/src/{time.rs,fixed.rs,virt.rs,real.rs,timer.rs}` | 同上 executor.rs 的 lag 循环 | — | `dev/godot/main/main_timer_sync.{h,cpp}`（固定步+插值同步的工程实现） |
| ECS 存储 | `dev/bevy/crates/bevy_ecs/src/storage/{table/,sparse_set.rs,blob_array.rs}` | `dev/Fyrox`（图式 scene graph，对照非 ECS 取舍） | — | — |
| 实体分配/观察者/事件 | `dev/bevy/crates/bevy_ecs/src/entity/mod.rs`、`observer/{mod.rs,runner.rs,centralized_storage.rs}`、`event/{mod.rs,trigger.rs}` | — | — | — |
| 调度执行器 | `dev/bevy/crates/bevy_ecs/src/schedule/{schedule.rs,executor/,graph/,auto_insert_apply_deferred.rs}` | — | — | — |
| 任务/JobSystem | `dev/bevy/crates/bevy_tasks/src/{task_pool.rs,usages.rs,slice.rs,iter/}` | — | `dev/UnrealEngine/Engine/Source/Runtime/Core/Public/Tasks/{Task.h,Pipe.h,TaskConcurrencyLimiter.h}`、`Async/{ParallelFor.h,Async.h,LocalWorkQueue.h}` | `dev/godot/core/object/worker_thread_pool.{h,cpp}`（组任务/优先级/yield）；Unity 仅语义锚（源码不在 dev/） |
| 渲染世界分离 | `dev/bevy/crates/bevy_render/src/extract_plugin.rs` | `fyrox-impl/src/renderer/` | RHI/RenderCore/Renderer 三层 | — |
| 资产 | `dev/bevy/crates/bevy_asset/src/{loader.rs,handle.rs,server/,processor/,meta.rs}` | `dev/Fyrox/fyrox-resource/src/{manager.rs,loader.rs,state.rs,event.rs}` | AssetRegistry | `dev/godot/core/io/{resource_loader,resource_importer,resource_uid}.{h,cpp}`（线程化加载/去重/稳定 ID）；`dev/Piccolo/engine/source/runtime/resource` |
| UI | `dev/bevy/crates/bevy_ui/src/layout/{mod.rs,ui_surface.rs}`（taffy 桥接） | `dev/Fyrox/fyrox-ui/src/{control.rs,canvas.rs,button.rs,...}`（retained 控件树+消息路由） | Slate/UMG（概念对照） | `dev/godot/scene/gui/`（Control/Container 布局族） |
| 插件/热重载 | Plugin/PluginGroup（静态） | `fyrox-impl/src/plugin/{mod.rs,dylib.rs}`（DynamicPlugin + 状态序列化重载） | 模块系统 | `dev/godot/core/extension/{gdextension.{h,cpp},gdextension_function_loader.{h,cpp}}`（C ABI 函数表装载/协商） |
| 动态扩展 ABI | — | 同上 dylib.rs | — | `dev/godot/core/extension/extension_api_dump.cpp`（API 面 dump/版本化） |
| 诊断/Profiling | `dev/bevy/crates/bevy_diagnostic/src/{diagnostic.rs,frame_time_diagnostics_plugin.rs,frame_count.rs}` | — | — | `dev/godot/main/performance.{h,cpp}`（性能监视器单点）；`dev/tracy`（profiler 本体源码） |
| 物理 | 核心无物理（生态 rapier/avian） | `dev/Fyrox/fyrox-impl/Cargo.toml:30-31`（rapier 外挂） | Chaos 内置 | `dev/godot/modules/{godot_physics_3d,jolt_physics}`（自研+Jolt 双后端） |
| 模块尺度 | crate-per-subsystem（约 40+ crate） | crate-per-layer（core/resource/graph/ui/impl/editor） | `Engine/Source/Runtime` 约 189 模块 | `dev/Piccolo/engine/source/runtime/{core,function,platform,resource}`（小型引擎单 runtime 分层全参照） |

`zircon_runtime` 取"单 crate + 内部 spine"形状，介于 Fyrox（多 crate 分层）与 Unreal（巨型模块树）之间;在 cdylib 热重载约束下单 crate 是合理选择，但要求内部模块边界承担 Bevy 中 crate 边界的职责——这正是 P1/P2/P4 要修的内容。

## 3. 子计划地图与执行顺序

| 计划 | 文档 | 依赖 | 状态 |
|------|------|------|------|
| 01 技术选型与依赖治理 | `01-tech-stack-and-dependency-governance.md` | 无 | in_progress（M1 runtime-tech-stack 文档、架构索引挂接与 tech_stack 依赖守卫已落地，standalone rustc 守卫 10/10 通过；M2.1/M2.2 text.md 三层文本栈矩阵、tech-stack 交叉引用、NativeGlyphon 状态口径与 complex text 候选裁决守卫完成，cosmic-text/Parley/Swash/HarfBuzz 只能经替换 `UiTextShaper` 接入；M2.3 fontdue 裁决为 editor retained-host 临时文本 fallback；M3.1 物理后端选型文档完成并补 `physics_backend_option_decision_keeps_jolt_unavailable_and_plugin_owned` 守卫，builtin 为 V1 唯一可执行后端、Jolt 为未来 native 方向但保持 unavailable；M3.2 导出归档决策完成，未来桌面/editor 容器选 ZIP 但当前不引入 zip/tar 依赖；M3.3 editor-only 依赖 backlog 完成，rfd/arboard 不进 runtime/interface；`tech_stack_boundary` 结构审计报告 manifest 5/5、non-dependencies 6、guard anchors 11/11、Jolt slots 2、removed/editor-only manifest deps 0、Rapier/Avian deps 0、`risks = []`；`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation` 已补为 Cargo 待验证守卫；Cargo `tech_stack` 重跑未执行到测试，lib-test 编译先被活跃 plugin bridge 切片的 `extension_registry_bridge.rs` unresolved `BridgeInterfaceSnapshot` / `BridgeInterfaceStatus` 导入阻断，当前 `tech_stack/text_shaper/plugin physics Cargo gates` Cargo 待 active lanes 清空） |
| 02 core spine 与 root surface 收束 | `02-core-spine-and-root-surface.md` | 无 | in_progress（M1/M2.1/M2.2 物理 cutover 完成；root/naming 独立守卫通过；新增 `root_surface_guard` 固定 `lib.rs` public namespace surface 与 crate-private graphics alias debt，防止 M3 前 root flattening 回退；M3.2 type alias debt 状态为 `pre_m3_type_alias_guard_static_passed_pending_render_owner`，禁止图形类型别名升级成 public root API，实际 alias 删除等待 render owner；M4.1 generated 规范与裁决完成，M4.2 generated 结构守卫与 app export-bootstrap owner 已落地，export 模板 direct native-loader/EntryRunner/registration-call 行为已迁出，provider 表裁决为允许的 generated table adapter，结构审计 0 迁移债且 `classified-and-clear`；新增 `runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation` 锁定 core/root/generated/export_build_plan/app/editor/plugin pending gate；M2/M4 全量 Cargo 回归当前受 render/graphics 活动编译与测试目标超时影响，M3 lib.rs graphics alias 清理需等待 render owner 稳定） |
| 03 调度与帧循环对齐 | `03-schedule-and-frame-loop-alignment.md` | 02（core 归属定稿） | in_progress（M0/M1/M2/M3 实现与源守卫已落地；M2 固定步三条 `level_tick_*` owner 测试已拆入 `scene/tests/ecs_schedule/fixed_update.rs`；`schedule_frame_loop_boundary` 结构审计 owner 已报告 source files 18/18、guard/test files 8/8、`SystemStage` count and variants 9/9、fixed-loop stages 3/3、dynamic-session `.tick_time(...)` calls 1/1、Runtime 03 guard anchors 14/14、no `WorldDriver` second `advance_time_by(...)` references、no dynamic-session raw-delta level tick references、`risks = []`；`runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 锁定 Runtime 03 计划、总索引、M0 review 与 runtime-interface convergence 的镜像数字；`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation` 已补为 Cargo 待验证守卫；`ecs_schedule/time/session/schedule_parallel Cargo gates` 待 active lanes 清空后重跑，上一轮 Cargo 回归被无关 `ui/tests/asset_dependency_index.rs` 导入错误阻断） |
| 04 资产管线对齐 | `04-asset-pipeline-alignment.md` | 可与 03 并行 | in_progress（M0 asset facade/reference gap table 完成并回写 `.zmeta` processor/meta owner 口径，计划表已同步 handle/loader/processor 最终裁决并由 `runtime_04_asset_facade_query_surface_stays_manager_owned_and_server_free` 防止 unresolved 占位回流；M0.2 facade 查询面盘点已静态锁定为 `ProjectAssetManager` / `Assets<TAsset>` / `AssetManager` service trait 的 manager/facade/service 分层，`runtime_04_asset_facade_query_surface_stays_manager_owned_and_server_free` 禁止回退到 asset server 词汇；M1.1 句柄语义裁决为保留 Zircon `Copy` typed ID，悬挂 handle 查询返回 `NotLoaded` 的命名回归测试已新增；M1.2 资源状态机收紧已落地，`Error -> Ready` 必须经 `Reloading`，失败原因通过 `ResourceRecord` diagnostics 投影到 facade；M2.1 worker pool 已切到 `AssetWorkerPoolOptions`，显式无界模式与 bounded queue-full 错误已落地；M2.2 pool-local in-flight request coalescing 已落地，`request_sender()` 暂留为低层旁路；M2.3 worker diagnostics 计数与 `DiagnosticStore` 发布已落地，in-flight 注册顺序已收紧以避免快完成悬挂计数；`asset_worker_pool_matches_runtime_04_and_11_decisions` 已静态落地，防止 worker pool 状态回退到旧基线；M3 watcher debounce/options、watch error 订阅面与 typed hot-reload event 回归已落地；Scene `.zasset` cache wire type 已补 scene mesh/camera/collider/joint 场景缓存修复并拆入 `asset/artifact/cache_payload/scene.rs`，`cargo test -p zircon_runtime --lib artifact_store_roundtrips_scene_assets_with ...` 已通过 4/4，watcher acceptance 已通过 7/7；`asset_pipeline_boundary` 结构审计报告 source 19/19、guard 10/10、worker diagnostics 5/5、artifact roundtrip 4/4、guard anchors 21/21、`risks = []`；`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation` 已补为 Cargo 待验证守卫，剩余 broader `asset::` / `worker_pool` Cargo filters 待空闲窗口重跑） |
| 05 scene/editor 边界收尾 | `05-scene-editor-boundary-closeout.md` | 无（最小） | in_progress（本计划 frontmatter 从 `completed` 修正为 `in_progress`，因为计划 DoD 要求 `cargo test -p zircon_runtime --lib scene:: --locked` 全族无回归后才能最终关账；当前只有 `naming_boundary`、`authoring`、`inspection` scoped Cargo 通过记录。新增 `runtime_plan_last_refined_covers_latest_recorded_date`、`runtime_plan_status_does_not_claim_completed_while_validation_is_pending`、`runtime_plan_frontmatter_status_uses_known_lifecycle_values`、`runtime_index_status_map_matches_subplan_frontmatter`、`runtime_index_subplan_map_covers_existing_plan_files_without_stale_rows`、`runtime_index_problem_rows_reference_existing_subplans`、`runtime_index_execution_dependencies_reference_existing_subplans`、`runtime_index_in_progress_rows_record_remaining_gate`、`runtime_known_backlog_gaps_keep_owner_and_trigger_columns`、`runtime_subplans_keep_status_and_evidence_tables`、`runtime_subplan_status_records_keep_non_empty_evidence`、`runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs`、`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation`、`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation`、`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation`、`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation`、`runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation`、`runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation`、`runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`、`runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation`、`runtime_10_m1_3_cargo_pending_gate_stays_explicit_until_dynamic_api_validation`、`runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass`、`runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation`、`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass`、`runtime_14_module_family_cargo_gate_stays_visible_until_filters_pass`、`runtime_architecture_review_documents_all_absorption_guards` 与 `runtime_05_closeout_status_waits_for_full_scene_cargo_gate`，防止待验证计划误标 completed，并锁定 last_refined 覆盖最新状态日期、总表状态必须跟随子计划 frontmatter、总索引子计划地图必须精确覆盖 01-14 子计划文件、问题清单与执行依赖列必须指向现有子计划、所有 in_progress 总表行必须说明剩余 gate 或 blocker、总索引已知 backlog 缺口必须保留现状依据与 owner/触发条件、所有子计划必须保留状态/证据表且已启动记录必须写入具体证据、Runtime 01-14 近期静态/待验证守卫必须同步到计划/镜像文档/评审/索引（Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` gate 必须保持可见，Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` gate 必须保持可见，Runtime 09 `ui/input/naming_boundary/layout/template` owner/Cargo gate 必须保持可见，Runtime 07 覆盖 `runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` 与 `runtime_frame_schedule_stage.<SystemStage>`，Runtime 10 同时覆盖 `runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces` 与 `runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge`，且 M1.3 在 `dynamic_api` Cargo 通过前保持 `code_static_passed_cargo_pending`，Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 必须保持可见，Runtime 12 在 input/action_map/gamepad/app Cargo 通过前保持 pending gate 可见，Runtime 13 在 script filters Cargo 通过前保持 `code_static_pending_cargo`，Runtime 14 模块族守卫必须覆盖四族镜像文档，Runtime 05 closeout 必须保持 `pending_full_scene_cargo`）、Runtime 01 `tech_stack/text_shaper/plugin physics` Cargo gate、Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` gate、Runtime 03 `ecs_schedule/time/session/schedule_parallel` Cargo gate、Runtime 04 broader `asset::` / `worker_pool` Cargo gate、Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gate、Runtime 07 `extract/ecs_query/performance profiling/FPS` Cargo gate、Runtime 08 ECS 数据面 Cargo gate、Runtime 09 `ui/input/naming_boundary/layout/template` owner/Cargo gate、Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 与 Runtime 14 模块族 Cargo/rustc gate 必须保持可见，以及 M0 评审必须覆盖 `runtime_absorption/mod.rs` 和所有已挂载 guard；最终 `completed` 仍等待 `cargo test -p zircon_runtime --lib scene:: --locked` 全族闭环） |
| 06 插件公开面与生命周期 | `06-plugin-surface-and-lifecycle.md` | 02 | in_progress（M1 binding 空参数修复通过；runtime 真实后端验证待重跑；新增 `runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation` 锁定 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` validation gate；`plugin_surface_lifecycle_boundary` 已接入总审计，报告 source 9/9、doc 5/5、native root re-export 68/68、M4 gate `migration-debt-present`、debt groups 5/5、unclassified symbols 0/0、native loader V1/V2 files 6/6、fixture-only V1/V2 usage 1/1、export_build_plan V1/V2 usage 0、app NativePlugin current call-site files: 7；M2/M3 native 收口、测试迁移、V1/V2 处置和回滚失败注入仍待开始） |
| 07 runtime 侧性能热路径 | `07-runtime-performance-hotpath.md` | 03;ZrVM assertion 解除;render 计划阶段 A | in_progress（M0.3 frame breakdown span 已静态落地：`runtime_frame_time_update` / `runtime_frame_update` / `runtime_frame_extract` / `runtime_frame_submit` / `runtime_frame_schedule_stage.<SystemStage>`；schedule runner stage-level span 已由 `runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` 跨源码/计划/文档/索引锁定，trace/profiling 验收待 render HZB blocker 与 active lanes 清空；M1.1 scoped counter points：ECS QueryState cache telemetry 已落第一段，含 hits/misses/rebuilds/candidate/matched/revision 快照、`DiagnosticStore` 投影路径、行为断言与结构守卫；change detection scan telemetry 已落第一段，含 scanned/added/changed 计数、`ecs.change_detection.*` 投影路径和 snapshot 断言；extract telemetry 已接入 `dynamic_api/session/extract.rs` 与 `extract_stats.rs`，`current_extract()` 自动写入 `extract.rebuild_clones` / `extract.output_bytes`，并新增 `headless_session_capture_records_frame_extract_diagnostics` 读回断言；M1.2 三个命名断言 `query_state_reuses_archetype_matches_across_unchanged_frames`、`change_detection_scan_skips_unmarked_archetypes`、`frame_extract_rebuild_skips_unchanged_entities` 已落地并通过源码/rustfmt 静态检查；M1.3 新增 `docs/zircon_runtime/performance/hotspot_inventory.md` 与 `runtime_07_hotspot_inventory_requires_counted_evidence_before_m2`，把热点清单推进为证据门槛 scaffold：extract 是当前唯一可计数 Runtime 07 M2 候选，QueryState/change detection/asset worker 均需权威 runtime 数值后再提升，230 draws / 231 copies / 31 passes 分流至 render 计划 02/04；`performance_hotpath_boundary` 当前报告 source 10/10、guard/test 5/5、frame span anchors 9/9、QueryState telemetry 13/13、change-detection telemetry 9/9、extract telemetry 10/10、asset-worker candidate telemetry 5/5、hotspot guard 16/16、counter assertion 12/12、doc anchors 16/16、pending gate anchors 5/5、stale top3 placeholder false，并复用 `large_file_ownership_gate` 作为 Runtime 07 owner-budgeted optimization gate：large-file gate `migration-debt-present`、hotspots 41、debt groups 5、owner classes 5、unclassified 0；新增 `runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit` 锁定 owner-budget mirror；新增 `runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation` 锁定 extract/ecs_query/performance profiling/FPS gates，Cargo/profiling/FPS 待 render HZB blocker 与 active lanes 清空；已通过 QueryState/change detection 的 core-min `cargo check` 与两条聚焦测试，extract/span/M1.2 touched 文件的 rustfmt/git diff/conflict 静态检查通过；extract 与 M1.2/M1.3 Cargo rerun 当前被 render owner 的 HZB 编译错误和 active lanes 阻塞，待 render 会话清理后复跑） |
| 08 ECS 内核数据面对齐 | `08-ecs-kernel-data-alignment.md` | 与 03/07 同文件区错峰；08↔10 实体 ID ABI 互检 | in_progress（M0 数据面对照裁决完成；M1 生命周期测试矩阵四条测试已落地；M2.1 观察者同步触发/目标实体/dispatch 移除三条测试已落地；M2.2 命令队列错误报告面与两条测试已落地；M3.1 events/messages 双通道四条测试已落地；M3.2 change tick 回绕与 stale 窗口截断两条测试已落地；`ecs_kernel_data_boundary` 结构审计当前报告 `expected_source_file_count = 20`、`expected_test_file_count = 7`、`storage_anchors = 9/9`、`entity_lifecycle_anchors = 10/10`、`observer_anchors = 8/8`、`deferred_command_anchors = 11/11`、`event_message_anchors = 11/11`、`change_tick_anchors = 6/6`、`runtime_08_guard_anchors = 17/17`、`doc_anchors = 7/7`、`pending_cargo_gate_anchors = 6/6`、`mirror_docs_guard_present = true`、`risks = []`；新增 `runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；新增 Runtime 08 ECS 数据面守卫 `runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`，Cargo 待活动 lanes 清空后运行 entity/observer/command/messages/change_tick/ecs filters） |
| 09 UI 子系统架构收束 | `09-ui-subsystem-architecture.md` | 05 debt 移交清单；editor UI 会话稳定；文本栈归 01-M2；12-M0.2 全局输入仲裁判词 | in_progress（2026-06-14 M0.1/M0.2 static passed：`docs/zircon_runtime/ui/architecture.md` 完成模块边界图与 `v2-replacement-mainline` 裁决，`runtime_absorption::ui_architecture` 三条结构守卫已落地但 Cargo 待跑；`ui_architecture_boundary` 已补为结构审计 owner，报告 source/doc 11/11、`ui/` entries 17/17、`surface/` entries 20/20、UI legacy 167 full-tree / 102 production hits / 12 production files、UI taffy 161 production hits / 7 production files、runtime `ui::v2` anchors 10/10、interface `ui::v2` anchors 9/9、guard anchors 4/4、pending UI owner/Cargo gate anchors 7/7、doc anchors 10/10、`risks = []`；新增 `runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation` 锁定 `ui/input/naming_boundary/layout/template` owner/Cargo gate；无 UI 生产代码改动；M1-M3 等待 editor UI owner/Cargo lanes 空窗） |
| 10 dynamic_api 与 interface 收敛 | `10-dynamic-api-and-interface-convergence.md` | 06（native ABI 同口径互引）；09-M0（镜像重复清单移交） | in_progress（M0 ABI 清册/版本策略完成；M1.1 ABI-safe/清册/版本策略守卫已扩展并通过完整 `zircon_runtime_interface` 包测试 165/165；M1.2 headless/minimal 跳过 render bridge、live destroy 夹具与 `runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces` 静态守卫已落地，守卫覆盖 optional render bridge、capture 空帧 fallback 与 bind/unbind/present no-op，新增守卫局部 rustfmt/锚点/差异检查通过；`destroy_session` Cargo 验证在活动编译通道下 904s 超时未声明通过；M1.3 FFI panic 边界已静态落地，`zircon_runtime_get_api_v1` 与 11 个 runtime 函数表入口经 `_ffi` wrapper 收口，panic 转 `ZrStatusCode::Panic` 或 null 表指针，`session.rs` owner 函数保持 Rust ABI，并有 `runtime_api_table_entries_are_panic_wrapped_at_ffi_boundary` 与 `runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge` 守卫，M1.3 rustfmt/锚点/差异检查通过；`dynamic_runtime_api_boundary` 当前报告 `expected_source_file_count = 14`、`function_table_structs = 10/10`、`field_count_mismatches = 0`、`missing_repr_c_tables = 0`、`runtime_session_ffi_wrappers = 11/11`、`direct_session_table_entry_bypasses = 0`、`session_owner_extern_c_present = false`、`headless_lifecycle_anchors = 12/12`、`ffi_panic_anchors = 9/9`、`loader_failure_anchors = 10/10`、`ui_pending_gate_anchors = 8/8`、`pending_cargo_gate_anchors = 5/5`、`doc_anchors = 7/7`、`mirror_docs_guard_present = true`、`risks = []`，并由 `runtime_10_dynamic_runtime_api_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；M2 UI 镜像契约 owner/Cargo gate 已由 `runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff` 锁定，等待 Runtime 09/editor UI owner handoff 与 interface/ui/editor 验证，未改 UI/interface 生产类型；M3.1 loader 失败路径含真实缺符号 fixture 的 scoped app Cargo 已通过，完整 app 包待测；dynamic_api/full app/UI contract Cargo 待空闲通道，2026-06-14） |
| 11 多线程 JobSystem 任务模型 | `11-job-system-task-model.md` | 02-M2 已完成（tasks 落位）；M2.4 与 04-M2 同窗口；物理客户随 01-M3 | in_progress（M0 模型选型与线程预算文档完成；M1 `JobHandle` / `JobScheduler::schedule_after` / `parallel_for` API、测试锚和 docs 已静态落地；M2.1 预守卫 `pre_m2_1_rayon_render_exception_guard_static_passed_pending_render_owner` 已落在 `runtime_absorption::rayon_boundary`，把 `parallel_frustum` 固定为唯一 `render-owner-pending-runtime-11-m2-1-cutover` 例外，actual graphics cutover not executed；M2.2 非 graphics direct-rayon 收编已静态落地，`ScheduleParallelExecutor` 固定/泛化并行路径改走 `JobScheduler::join(...)` 与 `run_parallel_tasks(...)`，`runtime_absorption::rayon_boundary` 只允许 task primitive owner 与 render-owned `parallel_frustum` 例外；M2.3 ECS 批次依赖化已静态落地，`ScheduleParallelExecutor` 通过 `schedule_after` 链接批次并新增行为/结构锚；M2.4 asset worker 线程来源已按自建线程显式记账路线静态落地，`ProjectAssetManager::default()` / asset module factory 从 `TaskPoolOptions` IO 配额推导 worker 数，`asset.worker.budgeted_threads` 进入诊断；M3.1 调度诊断与 M3.2 依赖链/宽扇出测试锚已静态落地；`job_system_boundary` 已报告 `expected_module_count = 9`、`direct_rayon_paths = 3`、`schedule_parallel_executor_direct_rayon = []`、`diagnostic_anchor_count = 4`、`oversized_modules = []`、`mirror_docs_guard_present = true` 与 `risks = []`；`runtime_11_job_system_mirror_docs_match_structure_audit_counts` 锁定 JobSystem 模块文档、Runtime 11、总索引、M0 review 与 runtime-interface convergence 的镜像数字；新增 `runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass` 锁定 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate；本轮静态证据已覆盖 Runtime 11 计划/JobSystem+asset worker 文档/task+ECS+asset worker 代码/tests 的 `rustfmt --edition 2021 --check`、锚点扫描、冲突标记扫描、尾随空白扫描与 `git diff --check`；tasks/ecs_schedule/worker_pool/rayon Cargo 重跑待 active lanes 清空；M2.1 `parallel_frustum` 真实迁移需等待 render/graphics 区稳定，2026-06-14） |
| 12 输入栈与动作映射 | `12-input-stack-and-action-mapping.md` | 清帧时机与 03 帧序图互引；09 owns UI internal route；gamepad 波及 zircon_app | in_progress（M0.1 输入链路与帧语义已静态落地：`docs/zircon_runtime/input/input_state.md` 增 "Frame Input Contract"，`zircon_runtime/src/input/tests/input_manager.rs` 增 `input_snapshot_just_pressed_is_true_for_exactly_one_frame` 与 `frame_input_clears_after_level_tick_not_before`；M0.2 仲裁判词已文档落地并与 09 互引：UI surface/capture/popup/focus 优先，玩法/action mapping 只消费 UI 未处理或无 UI/headless 输入；M1 最小动作映射已静态落地：`InputAction`/`InputBinding`/`InputActionMap`/`InputActionState` + `InputActionEvaluator`，测试锚 `action_map_resolves_chords_and_reports_just_activated`、`rebinding_action_does_not_require_recompilation`；M2.1 gamepad bridge 锚已静态落地：`gamepad_disconnect_clears_held_state_without_panic` 与 `gamepad_host_bridge_uses_runtime_gamepad_abi_constructors`；`runtime_absorption::input_stack` 已新增 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts`，锁定 `input_stack_boundary` 镜像事实 `expected_runtime_module_count = 10`、`expected_framework_module_count = 17`、`expected_test_module_count = 5`、`public_surface_anchors = 10/10`、`runtime_12_guard_anchors = 5/5`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；新增 `runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation` 防止 input/action/gamepad/app 验证前误标完成；本轮静态证据已覆盖 Runtime 12 输入契约/runtime/tests 的 `rustfmt --edition 2021 --check`、锚点扫描、冲突标记扫描、尾随空白扫描与 `git diff --check`，Cargo 待 active lane 清空后运行 input/action_map/gamepad/app filters） |
| 13 脚本绑定面与反射收束 | `13-script-binding-and-reflection.md` | 与 06 共享 script/vm 错峰；marshalling 引用 10-M0 清册；句柄语义与 08 对齐 | in_progress（M0.1/M0.2 静态完成：新增 `docs/zircon_runtime/script/vm/host/function_ledger.md`，固定 host ledger 记录 6 模块 / 50 函数 / 2 类型描述符，bridge 动态模块只锁形状；marshalling 裁决为 value descriptors / host handles / serialized payloads 三类；ECS 访问路径当前裁决为 `zr.zircon.gameplay` gameplay facade，native `ZrHostEcsApiV1` 保持 ABI 层；M1.1 清册守卫 `host_function_registry_matches_documented_ledger` 已落在 `runtime_absorption/script_host_ledger.rs`，含缺失函数负例自检；M1.2 capability 代表函数正向 descriptor 守卫与缺失授权拒绝测试已落地；M2.1 脚本持有实体 id despawn 后失效语义测试已落地；M2.2 脚本 ECS 访问路径 source guard 已落地；`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass` 已补为 Cargo 待验证守卫；本轮静态证据已覆盖 Runtime 13 计划/清册/测试挂载/ledger/gameplay_host tests 的 `rustfmt --edition 2021 --check`、锚点扫描、冲突标记扫描、尾随空白扫描与 `git diff --check`，Cargo 待 active lane 清空后运行 script filters，2026-06-13） |
| 14 运行时模块族完备性收尾 | `14-runtime-module-family-closeout.md` | engine_module 裁决与 02 root 守卫对齐；diagnostic_log 与 07 计数通道互引 | in_progress（M0 四族判词静态完成：animation/navigation/diagnostic_log/engine_module 文档已落；M1 navigation 文件集、engine_module declared-layer、diagnostic_log 单桥接、animation backlog/非目标、crate 根四族席位总守卫五类守卫已静态落地；`module_family_boundary` 当前报告 `expected_family_count = 4`、`animation = 27`、`navigation = 3`、`diagnostic_log = 7`、`engine_module = 8`、`root_seat_guard_present = true`、`risks = []`；`runtime_14_module_family_mirror_docs_match_structure_audit_counts` 锁定 Runtime 14 计划、总索引、M0 review 与 runtime-interface convergence 不得漂移；本轮静态证据已覆盖 Runtime 14 计划/四族文档/root_entries/engine_module/diagnostic_log 守卫文件的 `rustfmt --edition 2021 --check`、锚点扫描、冲突标记扫描、尾随空白扫描与 `git diff --check`，独立 `root_entries.rs` rustc 守卫曾通过 10/10，当前 Cargo/重跑 rustc 待 active lane 清空） |

Runtime 07 owner-budget gate 镜像：`runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit` 已把 Runtime 07 的大文件优化前置门固定到当前 `large_file_ownership_gate` 审计事实：threshold 1000、hotspots 41、debt groups 5、owner classes 5、unclassified 0，并拒绝 stale 33-hotspot 与已删除 Hub `app/` 路径锚点。该镜像只说明计划/文档/结构审计同步，不代表大文件已拆分，也不解除 `extract/ecs_query/performance profiling/FPS` Cargo/profiling/FPS 待验证门。

Runtime 05 plan-status 镜像：`runtime_plan_status_boundary` 已接入总审计，报告 plan-status support files 6/6、runtime subplans 14/14、runtime index subplan rows 14/14、problem rows 17/17、known backlog rows 7/7、status counts `in_progress=14`、core guard anchors 13/13、pending Cargo gate anchors 15/15、doc anchors 8/8、Runtime 05 closeout status `in_progress`、`risks = []`；这只确认计划/索引/评审文档同步，最终 `completed` 仍等待 `pending_full_scene_cargo` 与 `cargo test -p zircon_runtime --lib scene:: --locked`。

Runtime 05 non-network server 命名门禁复核：`non_network_server_naming` 已把 Hub UNC path fixture（`\\?\UNC\server\share\Game` / `\\server\share\Game`）列为允许上下文；当前审计 count 59、classification groups 2、allowed contexts 94、observer false positives 87、unclassified 0，剩余风险只是不应由 Runtime 05 跨会话处理的 graphics render-framework 与 editor workbench authority-label 已分类命名债。

Runtime 05 hard-cutover migration-smell 门禁复核：`hard_cutover_migration_smells` 当前审计 source_file_count 5839、legacy_reference_count 212、compat/shim 0、allowed business bridge 300、classification groups 7、unclassified 0；新增/确认的 owner groups 为 runtime UI input、hybrid GI render、runtime graphics、Hub archived text、DDS、Net hyper client API、editor UI fixture。该复核只同步审计归类和计划证据，剩余清理仍由 UI/render/plugin/Hub/Net/editor owner 切片处理。

Runtime 02 core/root/generated 镜像：`core_spine_root_generated_boundary` 已接入总审计，聚合 core spine、root surface 与 generated/export 事实，报告 core root entries 6/6、core public modules 5/5、retired core root entries 0、runtime root public modules 20/20、public `pub use` sites 3/3、crate-visible graphics alias debt 80/80、root-surface M1 gate `migration-debt-present`、generated export templates 9/9、generated behavior 6/6、generated allowed adapters 6/6、generated migration debt 0/0、generated-code M1 gate `classified-and-clear`、root_entries guard tests 12（baseline >=8）、root_surface guard tests 6/6、generated-code guard tests 7/7、`risks = []`；`root_surface_interface_convergence_mirror_uses_current_audit_counts` 额外锁定 interface convergence 的 20/80 root-surface 镜像并拒绝旧 17/75 文本。这不关闭 Runtime 02，`core/root/generated/export_build_plan/app/editor/plugin` Cargo gates 与 M3 render-owner alias cutover 仍待验证窗口。

Runtime 06 plugin surface/lifecycle 镜像：`plugin_surface_lifecycle_boundary` 已接入总审计，绑定 Runtime 06 子计划、总索引、native boundary、interface convergence 与 M0 review。当前报告 Runtime 06 source 9/9、doc 5/5、frontmatter `in_progress`、`last_refined = 2026-06-14`、native root re-export 68/68、M4 gate `migration-debt-present`、native public-surface debt groups 5/5、unclassified native symbols 0/0、public native re-export locations 1/1、app NativePlugin current call-site files: 7、native loader V1/V2 implementation files 6/6、`zircon_plugins` V1/V2 usage files 1/1（仅 native dynamic fixture）、export_build_plan V1/V2 usage 0/0、source/doc/validation anchors 全满；这不关闭 Runtime 06，`script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gate 与 M2/M3 hard-cutover 仍待执行。

### 3.1 已知但暂不立项的缺口（防遗忘清单，立项前先补证据）

| 缺口 | 现状依据 | 归属/触发条件 |
|---|---|---|
| 网络复制 runtime 侧 | `docs/engine-architecture/runtime-network-extension.md` 既有；net 栈在 zircon_plugins | net 插件 owner；runtime 侧出现复制语义需求时立项 |
| 音频 runtime 服务面 | sound 插件自研 DSP/HRTF + cpal（01 §1.1 已矫正） | sound 插件 owner；runtime 侧仅保持服务注册中性 |
| FFI panic 安全（extern "C" 边界 catch_unwind 审计） | dynamic_api/exports 与函数表分发面 | 归 10：M1.3 首批导出表 panic containment 已静态落地，Cargo 动态 API 回归待活动编译通道空闲；后续新增 ABI entry 必须同步 `_ffi` wrapper 与守卫 |
| 输入录制/回放 | 无 | 12 backlog；确定性回放需求出现时立项（依赖 03 固定步定稿） |
| 脚本调试器/断点面 | 无 | 13 backlog；ZrVM 上游调试协议确定后立项 |
| 存档/会话持久化语义 | World 序列化纯净守卫已有（05），玩法存档格式未定 | 项目侧需求触发；先经 04 资产/序列化口径 |
| 本地化/i18n | 无 | UI 文本管线（01-M2/09-M3）稳定后评估 |

阶段划分:

- 阶段 A（低风险、可立即）:05 + 01 的决策记录切片。纯删除/文档/守卫，不与活动会话冲突。
- 阶段 B（结构收束）:02 → 06；10 的 M0/M1（清册 + ABI 守卫，纯文档/守卫件）可与 06 并行，M2/M3 在 06-M2 收口后执行。
- 阶段 C（子系统对齐）:03 与 04 并行；08 的 M0/M1 可与 03 并行（同文件区切片错峰），M2/M3 在 03-M2 后执行；09 在 editor UI 会话稳定 + 05 收尾闭环后启动（M0 纯文档可提前）。
- 阶段 D（性能）:07。前置:ZrVM 空指针修复、render 计划 01/02 落地;08 的"债证明"锚定测试可作为 07-M1 计数的语义基线。
- 横切（并行底座）:11 的 M0（模型选型/线程预算，纯文档）、M1（JobHandle/parallel_for）与 M3 调度诊断/压测锚已静态落地；M2.1 已推进到 `pre_m2_1_rayon_render_exception_guard_static_passed_pending_render_owner`，仍以 `parallel_frustum` 作为唯一 `render-owner-pending-runtime-11-m2-1-cutover` 例外且 actual graphics cutover not executed；M2.2 非 graphics rayon 守卫已落；M2.1 真实 graphics 剔除旁路收编需等 render/graphics 区稳定；M2.3 已在 03-M3 后完成同文件错峰；M2.4 已与 04-M2 同窗口完成自建 asset worker 显式预算记账；`runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass` 保持 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 可见。
- 补全批（12/13/14）:12-M1 动作映射与 M2.1 gamepad bridge 锚已静态落地，`input_stack_boundary` 已把 runtime/framework/test owner、UI-filtered evaluator、gamepad ABI bridge、`runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 与 pending Cargo gate 纳入结构审计，当前镜像事实为 `expected_runtime_module_count = 10`、`expected_framework_module_count = 17`、`expected_test_module_count = 5`、`public_surface_anchors = 10/10`、`runtime_12_guard_anchors = 5/5`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`，Cargo/app 回归待跑；13-M0/M1/M2 已完成宿主清册、capability 代表守卫、脚本持有实体失效语义与 native ECS ABI 旁路 source guard，`script_binding_boundary` 已把 host ledger/capability/gameplay facade/native ECS ABI/Cargo gate 纳入结构审计，Cargo 待跑且后续新增 bridge/VM 行为仍需与 plugin 会话错峰；14-M0/M1 四族判词、navigation/engine_module/diagnostic_log/animation 边界守卫与 crate 根四族席位总守卫已静态落地，Cargo/rustc 重跑待编译通道空闲。

## 4. 全局边界约束（各子计划必须遵守）

继承 `Runtime 吸收层与 Editor_Scene 边界收束计划.md` 与 render 总计划 §5:

1. 不新增 crate;公共架构保持 `zircon_app`/`zircon_runtime`/`zircon_editor` 三件套 + `zircon_runtime_interface` ABI 层 + 内部 `core/{runtime,framework,manager,math,resource}` spine。
2. 硬切换:新 owner 路径落地的同一变更内迁移调用方并删除旧路径，不留 re-export、alias、shim。
3. 非网络语义的 `server` 命名是 blocker（`target-server` 为真实 headless 服务宿主语义，合法）。
4. 动态边界（dynamic_api、native 插件、VM 插件）只传 ABI-safe 值与序列化负载。
5. generated 产物只许 leaf binding/DTO/table，不许持有业务规则、调度或状态突变。
6. 渲染骨架（RDG、MeshDrawCommand、GPUScene、可见性、光照、时域、后处理、permutation）一律归 `docs/plans/zircon_runtime/render/01-08`，本目录子计划不得重复或冲突。

## 5. 全局验收与测试基线

按 milestone-first 政策:实现切片期间只做轻量检查，每个里程碑末进入测试阶段。

- 切片期:`cargo check -p zircon_runtime --lib --locked`（必要时 `-p zircon_app` / `-p zircon_editor`）。
- 里程碑测试阶段:`cargo test -p zircon_runtime --lib --locked`（按子计划模块过滤词收窄）;涉及装配时加 `cargo test -p zircon_app --locked`。
- 插件接缝:`cargo check --manifest-path zircon_plugins/Cargo.toml --workspace --all-targets --locked`。
- 结构守卫:各子计划列出的源断言/结构测试（如 app 不得直依插件实现 crate、interface 不得出现 wgpu）。
- 文档:每个里程碑完成后按源码镜像路径更新 `docs/zircon_runtime/**`，并刷新本目录子计划状态标记。

## 6. 协调与活动会话避让

- `20260604-1232-runtime-architecture-review`（活跃）:正在执行旧渐进式计划的切片（root surface 债 4、大文件债 5、hotspots 37 的审计口径以该会话为准）。本目录 02/05 子计划的执行必须先对齐该会话的最新 touched_modules，避免双写。
- `20260611-0416-rendering-10fps-analysis`（活跃）:graphics 性能修复进行中，明示"不回退 worktree 改动、只做聚焦编辑"。07 子计划执行前必须复读该笔记。
- `20260603-2304-plugin-ecosystem-continuation`（活跃）:06 子计划执行前同上。

## 7. 工程化落地公约（2026-06-12 细化定稿，约束全部子计划执行）

各子计划已细化到切片级；执行任何切片时遵守以下公约，违反视为切片未完成：

1. **切片五要素**:每个切片的"目标文件 / 改动形态 / 调用方迁移 / 验收 / DoD"五项缺一即不开工;签名草案在动手前定稿，定稿差异回写计划文件。
2. **执行前检查清单是闸门**:各子计划"执行前检查清单"逐项过完才能动第一刀;行号与计数以重核结果为准，漂移时先回写"现状与证据"节再继续。
3. **状态节实时性**:每完成一个切片，立刻更新该计划"状态与产出记录"表（状态/日期/证据三列），禁止批量补记;基线数值在开工首日填写。
4. **milestone-first 测试节奏**:切片期只跑该计划列出的 `cargo check`;测试统一压到里程碑末的"测试阶段"命令清单，逐条可复制执行并留存输出摘要。
5. **硬切换提交粒度**:`git mv` / 删导出 / 改签名类切片，旧路径删除与调用方迁移必须同一提交闭合，禁止中间态（双签名、临时 re-export）跨提交存在。
6. **会话避让**:触及 `20260604-1232`（架构审查）、`20260611-0416`（10fps，禁止回退其改动）、`20260603-2304`（插件生态）三个活跃会话工作区前，先重读其笔记并按计划"风险与协调"节对齐;每次执行开新会话笔记（cross-session-coordination 规范）。
7. **证据纪律**:计划中标注"执行时核验:<命令>"的条目，核验输出粘入状态节;新增守卫测试一律带负例自检（参照 `authoring_boundary_guard_fails_on_representative_tokens` 模式）。
8. **共享基建复用**:结构扫描守卫（02-M4 generated、05-1.4 命名、01-1.4 manifest）共享同一套源码遍历 helper，后落地者复用先落地者，禁止三套扫描实现。
9. **参考锚点纪律（防凭空实现）**:实现型切片（新增类型/算法/调度/状态机语义）动工前，必须先读该计划"参考锚点"列出的 dev/ 源码与本 index §2.3 速查表对应行，并在状态节证据列注明"已读锚点: <路径>"；锚点与设计有出入时，差异判词写入计划再动手。计划未列锚点的领域：先查 §2.3 速查表，速查表也无对应行的，在计划中显式标注"无参考，自研判词: <理由>"后方可实现——禁止默默凭记忆复刻其他引擎行为。
