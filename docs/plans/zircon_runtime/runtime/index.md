---
related_code:
  - zircon_runtime/src/lib.rs
  - zircon_runtime/src/builtin/runtime_modules.rs
  - zircon_runtime/src/core/mod.rs
  - zircon_runtime/src/scene/ecs/mod.rs
  - zircon_runtime/src/plugin/mod.rs
  - zircon_runtime/src/plugin/runtime_plugin/descriptor/builder/runtime_plugin_descriptor_builder.rs
  - zircon_runtime/src/asset/pipeline/worker_pool.rs
  - zircon_runtime/src/asset/assets/texture/descriptor.rs
  - zircon_runtime/src/asset/assets/texture/texture_asset.rs
  - zircon_runtime/src/asset/importer/ingest/import_texture.rs
  - zircon_plugins/texture_importer/runtime/src/importers.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings.rs
  - zircon_app/Cargo.toml
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/entry_static_dependencies.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/entry_static_dependencies_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/legacy_standalone_references.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/legacy_standalone_references_markdown.py
  - zircon_runtime/Cargo.toml
  - zircon_runtime/src/dynamic_api/session/events.rs
  - zircon_runtime/src/dynamic_api/session/host_requests.rs
  - zircon_runtime/src/dynamic_api/tests/host_requests.rs
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/dynamic_api_test_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/dynamic_api_test_markdown.py
  - zircon_runtime_interface/src/runtime_api/host_requests.rs
  - zircon_runtime_interface/src/tests/contracts.rs
  - zircon_app/src/entry/runtime_entry_app/host_requests/routing.rs
  - zircon_app/src/entry/runtime_entry_app/host_requests/cursor/request.rs
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_api_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_api_markdown.py
  - zircon_runtime/src/tests/runtime_absorption/input_stack.rs
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/input_stack_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/input_stack_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/job_system_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/job_system_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/module_inventory.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/module_inventory_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/native_plugin_public_surface.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/native_plugin_public_surface_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/plugin_runtime_gaps.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/plugin_runtime_gaps_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/large_file_ownership.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/large_file_ownership_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_root_surface.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_root_surface_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ecs_query_state_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ecs_query_state_markdown.py
  - dev/bevy/crates/bevy_app/src/main_schedule.rs
  - dev/bevy/crates/bevy_asset/src
  - dev/Fyrox/fyrox-impl/src/plugin/mod.rs
  - dev/UnrealEngine/Engine/Source/Runtime
plan_sources:
  - .codex/plans/Zircon Runtime 架构渐进式 Review 与优化计划.md
  - .codex/plans/Runtime 吸收层与 Editor_Scene 边界收束计划.md
  - .codex/plans/全系统重构方案.md
  - docs/plans/zircon_runtime/render/index.md
---

# Zircon Runtime 架构完善与优化总体计划

本目录是 `zircon_runtime` 在渲染骨架计划（`docs/plans/zircon_runtime/render/01-08`）之外的 runtime 侧架构完善总计划：技术选型治理、core spine 与 root surface 收束、调度/帧循环、资产管线、scene/editor 边界收尾、插件公开面与生命周期、runtime 侧性能热路径。它承接 `.codex/plans/Zircon Runtime 架构渐进式 Review 与优化计划.md`（M0–M7），把其中 runtime 侧尚未完成的项落成可执行子计划，并以 2026-06-12 的实仓审计修正其现状假设。

## 1. 技术选型评审结论（2026-06-12 实仓核对）

总体结论:选型与 Bevy 同代生态一致（winit + wgpu/naga + glam + crossbeam + notify + taffy），自研 ECS/UI/资产管线的目录形状与 `bevy_ecs`/`bevy_asset` 可逐项对照，方向合理。但"声称技术栈"与实仓有 5 处失实，另有 4 个能力缺口需要决策。

### 1.1 声称栈核对表

| 声称 | 实仓状态 | 证据 | 评估 |
|------|---------|------|------|
| winit 窗口输入 | ✓ `0.31.0-beta.2`，optional + feature 门控 | 根 `Cargo.toml`、`zircon_runtime/Cargo.toml` | 合理；**beta 版本风险**，见子计划 01 |
| wgpu / naga 渲染 | ✓ 29.0.1 / 29.0.1 配套一致 | 根 `Cargo.toml` | 合理 |
| taffy UI 布局 | ✓ 0.10，自研 UI 经 `ui/layout/taffy_bridge/{mod,compute}.rs` 桥接 | `zircon_runtime/Cargo.toml` | 合理 |
| glam 数学 | ✓ 0.32.1 (serde) | 根 `Cargo.toml` | 合理 |
| fontdue / cosmic-text / glyphon | **部分失实**:fontdue 0.9.3 仅在 editor;cosmic-text 不存在;runtime 实际为 glyphon 0.11 + fontsdf 0.5.3 + 自研 text shaper | `zircon_editor/Cargo.toml`、`zircon_runtime/Cargo.toml:77-78` | 三库职责口径需定稿，见子计划 01 |
| 自研 ECS | ✓ `scene/ecs/`（archetype、query、schedule、parallel executor、conflict graph、observer、change detection） | `zircon_runtime/src/scene/ecs/` | 形状对齐 `bevy_ecs`，合理 |
| kira 音频 | **失实**:不存在;实际为 cpal 0.15（optional）+ sound 插件自研 DSP/HRTF/occlusion/mixer | `zircon_plugins/sound/runtime/Cargo.toml` | 既有自研混音栈下 cpal 底座更合理，**不建议**再引 kira;矫正文档即可 |
| image / serde / crossbeam 异步加载 | ✓ 全部存在;worker pool + watcher 在 `asset/pipeline`、`asset/watch` | `zircon_runtime/src/asset/` | 合理 |
| zip / tar 打包 | **失实**:均不存在;仅 zstd 0.13.3;`ExportPackagingStrategy` 已有契约但无归档实现 | `zircon_runtime/Cargo.toml:100`、`plugin/export_profile.rs` | **导出打包能力缺口**，见子计划 01 |
| gilrs 手柄 | ✓ 0.11.0，optional，`gamepad-gilrs` feature | `zircon_app/Cargo.toml:84` | 合理 |
| tracing / tracing-subscriber | ✓ tracing 常驻;subscriber 0.3.20 仅在 `profiling-tracy` 后 | `zircon_runtime/Cargo.toml:24,93` | 合理;profiling 构建超时问题见子计划 07 |
| rfd / arboard 编辑器辅助 | **失实**:均不存在 | 全仓 Cargo.toml grep 无命中 | editor 侧文件对话框/剪贴板缺口，归 `zircon_editor` 决策 |

### 1.2 声称栈未列、但实际承重的依赖

rayon（ECS/资产并行）、tokio + hyper + reqwest + tokio-tungstenite（net 插件网络栈）、bincode/ron/toml/serde_json（多格式序列化）、gltf/tobj/dxf/ply/stl（模型导入）、notify 9.0.0-rc.3（资产监视）、libloading（cdylib runtime 与 native 插件）、zstd、accesskit（optional）、Recast C++ 经 cc 绑定（navigation 插件）、tauri 2.11（zircon_hub，**Slint 已不在任何 Cargo.toml**）、zr_vm_rust_binding（**指向仓库外 `../../zr_vm` 的路径依赖**）。

### 1.3 版本与依赖治理风险

1. `winit 0.31.0-beta.2`:beta 跟踪策略未定稿（锁定/升级 gate）。
2. `notify 9.0.0-rc.3`:RC 版本。
3. `zr_vm_rust_binding` 路径依赖逃逸出仓库根，影响 clone 即建的可复现性（optional 缓解，仍需文档化或 vendor 决策）。
4. `jolt = []` 空 feature + physics backend:已由 Runtime 01 M3 裁决为 plugin-owned builtin V1 可执行后端 + future Jolt unavailable slot；Rapier 不进主路径，剩余实现工作归物理插件后续 native-backend 计划。

## 2. 架构评审结论

### 2.1 已收敛项（旧计划假设需修正处）

旧《渐进式 Review 计划》的部分目标已经落地，本计划不再重复:

- `zircon_runtime/src/lib.rs` 仅 29 行，root surface 已薄化;`root_surface_guard` 固定当前 19 个 public module declarations、2 个 public `pub use` sites、禁止子系统 root flattening，并已删除 graphics alias block、`#[allow(unused_imports)]`、`rhi_wgpu` public root backend exposure 和 builtin root facade；M3.5 当前状态为 `builtin_root_facade_removed_static_passed_cargo_pending`，`crate-visible graphics alias debt 0/0`，root-surface M1 gate `classified-and-clear`；`builtin/runtime_modules.rs` 已是 folder-backed 装配入口（assembly/manifest/core_modules/plugin_modules/load_report 分离）。旧计划 M1/M2 对这两处的"待拆"描述已过时。
- `zircon_app` 插件扇出已收束为单一 `zircon_first_party_runtime_catalog` optional 依赖（`zircon_app/Cargo.toml:88`），feature 全部经 catalog 转发。
- 服务注册热路径已是强类型键:`HashMap<RegistryName, ServiceEntry>`，`RegistryName` 带缓存解析偏移（`core/runtime/descriptors/registry_name.rs`），非 per-frame 字符串查找。
- World 序列化不含 selection/viewport authoring 状态，且已有 serialization_guard 守卫;测试树全部 folder-backed，无巨型 `tests.rs`。
- 渲染分层（framework 契约 / graphics 实现 / rhi / rhi_wgpu）与 UE 的 Engine/Renderer/RenderCore/RHI 方向一致，骨架差距已由 render 子计划 01–08 接管。

### 2.2 问题清单（本计划的工作对象）

| # | 问题 | 证据 | 子计划 |
|---|------|------|--------|
| P1 | `lib.rs:39-72` 的 70+ 类型 `pub(crate) use` 巨型别名块:内部调用方经 crate 根访问 graphics 内部类型，掩盖 owner 路径;且混入 HybridGi/VirtualGeometry/Solari 等插件 provider 语义 | `zircon_runtime/src/lib.rs:39-72` | 02 |
| P2 | ~~`core/` 根下 13+ 散件游离于五件套 spine 口径之外，归属未定~~（2026-06-17 复核：Runtime 02 M2 已硬切换完成，`core/` 根当前仅 `framework/manager/math/resource/runtime/mod.rs`；`root_entries` 守卫覆盖 retired fragments 与 spine 形态；`root_surface_guard` / `docs/zircon_runtime/core/root_surface.md` 固定 19 个 root namespace module、2 个 public `pub use` sites、无子系统 root flattening；M3.3 已删除 graphics alias block 和 root type alias debt，阶段锚点 `graphics_alias_block_removed_static_passed_cargo_pending` 仍保持可见，M3.4 已将 `rhi_wgpu` 改为 crate-private backend owner，M3.5 已删除 builtin root facade，当前状态 `builtin_root_facade_removed_static_passed_cargo_pending`、`crate-visible graphics alias debt 0/0`、`root_surface_migration_debt_count = 0`、root-surface M1 gate `classified-and-clear`；`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation` 锁定 core/root/generated/export_build_plan/app/editor/plugin 验证；Cargo default/lib-test 仍待 active lanes 清空后重跑） | `zircon_runtime/src/core/mod.rs`、`zircon_runtime/src/core/{framework,manager,math,resource,runtime}`、`zircon_runtime/src/tests/runtime_absorption/{root_entries,root_surface}.rs`、`docs/zircon_runtime/core/root_surface.md`、`docs/engine-architecture/core-runtime-service-registry.md` | 02 |
| P3 | ~~帧循环阶段无单一权威定义~~（2026-06-12 M0/M1/M2 复核矫正:`SystemStage` 九阶段权威表、`FIXED_LOOP`、Real/Virtual/Fixed 三时钟均已落地）。`WorldDriver` 已改为消费上游 `RuntimeTimeAdvance`，二次 `advance_time_by(...)` 与 driver 局部 cap=4 已删除；`FixedStepPlan::overstep_fraction()` 已提供；UI extract 已定稿为 runtime 03 合法 dynamic-session side path；阶段内顺序已有注册顺序负例守卫。M2 固定步 owner 测试已拆入 `scene/tests/ecs_schedule/fixed_update.rs`，覆盖 drained steps、零 lag、cap；M3.1 已补 `ScheduleParallelExecutionReport`、关闭并行开关与 `schedule.parallel_batches` / `schedule.serial_fallbacks` 诊断；M3.2 已补代表性多批次收益与串并行终态一致性守卫。`schedule_frame_loop_source_inventory.py`/`schedule_frame_loop_anchor_inventory.py` 已拆出 source/guard 与 anchor 清单，`schedule_frame_loop_markdown.py` 已拆出 Markdown 渲染，`schedule_frame_loop_boundary` 保留审计读取/缺失锚点/风险判定并已把 source files 18/18、guard/test files 8/8、`SystemStage` count and variants 9/9、fixed-loop stages 3/3、dynamic-session `.tick_time(...)` calls 1/1、Runtime 03 guard anchors 14/14、`behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、`doc_anchors = 10/10`、`mirror_docs_guard_present = true`、frame schedule module-doc anchors 3/3、no `WorldDriver` second `advance_time_by(...)` references、no dynamic-session raw-delta level tick references 与 `risks = []` 纳入结构审计；当前 `schedule_frame_loop_boundary.py` 368 行、`schedule_frame_loop_markdown.py` 146 行；`runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 锁定 Runtime 03 计划、`frame_schedule.md`、总索引、M0 review 与 runtime-interface convergence 的镜像数字。剩余缺口:`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation` 锁定 `ecs_schedule/time/session/schedule_parallel` Cargo 回归待运行 | `docs/zircon_runtime/core/frame_schedule.md`、`docs/zircon_runtime/scene/ecs/schedule_parallel_executor.md`、`scene/ecs/system_stage.rs:4-45`、`core/runtime/time.rs`、`core/framework/time/fixed_step_plan.rs`、`scene/module/world_driver.rs:11-73`、`dynamic_api/session.rs`、`dynamic_api/session/extract.rs`、`scene/ecs/schedule_parallel_executor.rs`、`scene/tests/ecs_schedule.rs`、`scene/tests/ecs_schedule/fixed_update.rs`、`dynamic_api/tests/session_profiles.rs`、`schedule_frame_loop_source_inventory.py`、`schedule_frame_loop_anchor_inventory.py`、`schedule_frame_loop_markdown.py`、`schedule_frame_loop_boundary` | 03 |
| P4 | `plugin/mod.rs` 的 native plugin loader / native bridge-method 相关类型已从 `zircon_runtime::plugin` 根硬切到 `zircon_runtime::plugin::native`，旧根 re-export 不保留；M1.2 已新增 fallback lifecycle failure tests 4/4，M3.1 已删除 native plugin loader V1/V2 descriptor、entry fallback、host table、fixture exports，保留 V3-only 当前 ABI 与 unknown ABI rejection；M3.2 已新增 hot reload failure injection 两条回滚守卫；2026-06-22 已按 F1/E7 补齐 native host FFI panic guard，`host_api_adapter.rs` 9 个 `ZrHostApiV3` callbacks 与 `host_callbacks.rs` 4 个 private native host callbacks 均在 `extern "C"` 边界内把 panic 映射为状态码，状态为 `native_host_ffi_panic_guard_static_passed_cargo_timeout`。2026-06-22 已完成 F8 RuntimePluginDescriptor public-field convergence：`RuntimePluginDescriptor` 15 个字段私有化，`descriptor/access.rs` 暴露 15 个只读访问器，状态为 `runtime_plugin_descriptor_public_field_convergence_coremin_check_passed`，并由 `review_f8_runtime_plugin_descriptor_fields_are_private_with_accessors` 锁定 RuntimePluginDescriptor private fields 15/15 与 RuntimePluginDescriptor public-field convergence complete。当前 `native_plugin_public_surface.m4_gate_status=classified-and-clear`、`root_reexport_count = 0`、`native_namespace_reexport_count = 60`；`plugin_surface_lifecycle_boundary.py` 保留 450 行审计/risk owner，`plugin_surface_lifecycle_markdown.py` 为 144 行 Markdown owner；`plugin_surface_lifecycle_boundary` 报告 `expected_source_file_count = 14`、`expected_doc_file_count = 5`、native root re-export 0/0、native namespace re-export 60/60、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1、app NativePlugin current call-site files: 7、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、fallback lifecycle failure tests 4/4、unknown ABI rejection、hot reload failure injection、`mirror_docs_guard_present = true`、`risks = []`，并由 `runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts`、`runtime_06_native_loader_tests_use_isolated_plugin_native_namespace` 与 `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed` 锁定镜像文档、native 测试命名空间和 fallback 生命周期失败路径；`runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation` 继续锁定 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` 验证线，在真实后端、plugin/native plugin、app 与插件 workspace Cargo 通过前不得关账 | `zircon_runtime/src/plugin/mod.rs`、`zircon_runtime/src/plugin/native.rs`、`zircon_runtime/src/plugin/runtime_plugin/descriptor.rs`、`zircon_runtime/src/plugin/runtime_plugin/descriptor/access.rs`、`zircon_runtime/src/plugin/native_plugin_loader/ffi_panic_guard.rs`、`zircon_runtime/src/plugin/native_plugin_loader/host_api_adapter.rs`、`zircon_runtime/src/plugin/native_plugin_loader/host_callbacks.rs`、`zircon_runtime/src/script/vm/tests/lifecycle_failures.rs`、`docs/engine-architecture/native-plugin-boundary.md`、`docs/engine-architecture/runtime-interface-convergence.md`、`tests/runtime_absorption/plan_status/cargo_gates/early.rs`、`tests/runtime_absorption/plan_status/cargo_gates/early/runtime_06.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/plugin_surface_lifecycle_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/plugin_surface_lifecycle_markdown.py`、`zircon_runtime/src/tests/runtime_absorption/plugin_surface_lifecycle.rs` | 06 |
| P5 | 实测性能 ~10fps（230 draws / 231 pre-draw buffer copies / 31 passes）;权威 FPS 取证被 ZrVM native assertion（`zr_vm_core.dll function.c:1394`，空参数空指针）阻塞;2026-06-12 binding 层已修复空参数 marshalling 并通过 binding 回归，runtime 真实后端验证仍待完成;profiling 构建两次超时；`performance_hotpath_boundary` 当前镜像已拆为 source/test 清单、anchor 清单、审计聚合和 Markdown 四个 owner（`performance_hotpath_source_inventory.py` 70 行、`performance_hotpath_anchor_inventory.py` 244 行、`performance_hotpath_boundary.py` 353 行、`performance_hotpath_markdown.py` 139 行），`expected_source_file_count = 46`、`animation_scene_anchor_count = 19`、`profile_counter_hotspot_anchor_count = 8`、`hotspot_guard_anchor_count = 32`、`doc_anchor_count = 35`、`large_file_hotspot_count = 30`、`large_file_m1_gate_status = migration-debt-present`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`risks = []`，当前大文件门为 1000 行阈值、hotspots 30、debt groups 5、owner classes 5、unclassified 0，owner classes 为 `editor-retained-host=3`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=13`、`support-hub=3`；`runtime_07_artifact_cache_payload_owner_split_keeps_wire_types_folder_backed` 已把 artifact cache JSON/Mesh/TOML wire owners 拆出 dispatcher；Runtime 14 navigation fallback runtime owner split 已把 navigation runtime 移出大文件热点；`AnimationSceneFrameDiagnostics` 已补 `animation.scene.scanned_entities` / `animation.scene.output_poses` 等 animation scene hook 取证入口；`counter_hotspots.json` / `CounterHotspotReport` 已把通用 profiling counters 接入证据导出；`runtime_07_render_product_diagnostics_owner_split_keeps_families_folder_backed` 已把 render product camera/mesh_queue/GPUScene 等诊断族拆出 dispatcher；`runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；`runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation` 继续锁定 extract/ecs_query/performance profiling/FPS gates 的 Cargo/profiling/FPS 待运行状态 | `.codex/sessions/20260611-0416-rendering-10fps-analysis.md`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/performance_hotpath_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/performance_hotpath_source_inventory.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/performance_hotpath_anchor_inventory.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/performance_hotpath_markdown.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/large_file_ownership.py`、`zircon_runtime/src/tests/runtime_absorption/performance_hotspots.rs` | 07（render 侧归 render/01-08） |
| P6 | ~~`scene/editor_projection/` 空目录残留~~（2026-06-12 矫正:目录已删，残留文本只保留在禁止复活守卫中）;runtime 内 "editor" 白名单裁决与守卫机器化已落 `runtime_naming_boundary` / `runtime_absorption::naming_boundary`（2026-06-21 当前工作区 editor 1556 locations / 281 files / 0 unclassified；`runtime_naming_markdown.py` 已独立承接 Markdown 输出；序列化纯净 token 守卫已存在:`scene/tests/authoring_boundary.rs` 双 token 表 19+25 词） | `scene/tests/authoring_boundary.rs`、`scene/tests/inspection.rs`、`runtime_absorption/naming_boundary.rs`、`runtime_naming_boundary.py`、`runtime_naming_markdown.py` | 05 |
| P7 | 资产管线对照差距已由 Runtime 04 M1-M3 静态/聚焦回归收束：句柄语义裁决为保留 `Copy` typed ID，悬挂句柄查询返回 `NotLoaded`；Runtime 04 reference gap table 已定稿 handle/loader/processor 裁决，不再保留 unresolved 对照占位；asset facade 查询面已锁为 `ProjectAssetManager` / `Assets<TAsset>` / `AssetManager` service trait 的 manager/facade/service 分层，并由 `runtime_04_asset_facade_query_surface_stays_manager_owned_and_server_free` 禁止回退到 asset server 词汇；`core::resource` 状态机收紧为失败后必须经 `Reloading`；worker pool 已落 `AssetWorkerPoolOptions`、bounded 背压、in-flight 去重、诊断计数与 Runtime 11 IO 预算记账，`AssetWorkerPool::request_sender(...)` 已收回，`AssetWorkerPool::request(...)` 是唯一公开请求入口，并由 `asset_worker_pool_matches_runtime_04_and_11_decisions` 固定当前状态不回退到原始无背压/无去重基线；watcher 已落 debounce/options、watch error 订阅与 typed hot-reload event 回归；Scene `.zasset` cache wire type 修复后 `artifact_store_roundtrips_scene_assets_with` 4/4 与 `watcher` 7/7 已通过。`asset_pipeline_source_inventory.py`/`asset_pipeline_anchor_inventory.py` 已拆出 source/guard 与 anchor 清单，`asset_pipeline_markdown.py` 已拆出 Markdown 渲染，`asset_pipeline_boundary.py` 保留审计读取/缺失锚点/风险聚合（328 行），`asset_pipeline_markdown.py` 为 117 行，并已把 `expected_source_file_count = 22`、`expected_guard_file_count = 11`、`worker_diagnostic_count = 7`、`expected_worker_diagnostic_count = 7`、`artifact_store_roundtrip_count = 4`、`expected_artifact_store_roundtrip_count = 4`、`watcher_acceptance_reference_count = 1`、`expected_watcher_acceptance_count = 7`、`artifact_acceptance_reference_count = 3`、`test_anchor_count = 24`、`behavior_test_anchor_count = 20`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`retired_worker_new_references = []`、`retired_worker_request_sender_references = []`、`old_watch_debounce_references = []`、`mirror_docs_guard_present = true` 与 `risks = []` 纳入结构审计；`runtime_04_asset_pipeline_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数。剩余：`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation` 锁定 M1/M2 broader `asset::` / `worker_pool` Cargo filters 仍待干净编译窗口重跑。 | `docs/plans/zircon_runtime/runtime/04-asset-pipeline-alignment.md`、`docs/zircon_runtime/asset/{facade,worker_pool,watcher,artifact}.md`、`docs/zircon_runtime/core/resource.md`、`tests/runtime_absorption/{asset_surface,asset_worker_policy,asset_pipeline}.rs`、`asset/pipeline/worker_pool.rs`、`asset/tests/pipeline/worker_pool.rs`、`tests/runtime_absorption/plan_status/cargo_gates/early/runtime_04.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/asset_pipeline_source_inventory.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/asset_pipeline_anchor_inventory.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/asset_pipeline_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/asset_pipeline_markdown.py` | 04 |
| P8 | generated 口径矫正:真实文件头 `// @generated ...` 标记为 0，"generated" 词根命中 42 文件（混杂法线生成等领域词）;标记规范与 leaf DTO/table 守卫已固化。M4.2 已把生成入口中的 direct `EntryRunner`/`NativePluginLoader` 行为迁入 `zircon_app::entry::export_bootstrap`，并把 linked plugin registration 从即时调用改成 app provider 表；provider 行已裁决为允许的 generated table adapter，结构审计从 13 行为点/5 标签/5 债务降到 6 行为点/3 标签/0 债务，`m1_gate_status=classified-and-clear`；`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation` 锁定 generated/export_build_plan/app 回归；Cargo 测试阶段仍受 render/graphics 编译漂移与 Windows 测试目标超时影响 | `docs/engine-architecture/generated-code-boundary.md`、`zircon_app/src/entry/export_bootstrap.rs`、`plugin/export_build_plan/*template*.rs` | 02 |
| P9 | "legacy" 命名已机器化裁决（2026-06-21 当前工作区 runtime-only 审计 491 locations / 101 files / 0 unclassified）；剩余 7 个 naming debt bucket 按 runtime graphics、DDS container policy、UI template schema、input event、asset schema、dynamic API migration、scene schema/render owner 分派；`runtime_naming_markdown.py` 已独立承接 Markdown 输出 | `runtime_naming_boundary.py`、`runtime_naming_markdown.py` 结构审计 | 05 |
| P10 | 物理 / 导出归档 / editor 辅助三类完备性缺口已由 Runtime 01 M3 裁决并有守卫锚：physics 插件不是空壳，V1 唯一可执行后端为 builtin，Jolt 保留为未来 native 方向但保持 unavailable，Rapier 不进主路径；`physics_backend_option_decision_keeps_jolt_unavailable_and_plugin_owned` 锁定两处 `jolt = []` 空 feature、`JOLT_BACKEND_AVAILABLE = false`、Jolt 不降级 builtin、Rapier/Avian 不进 manifest；导出归档保持 directory-first API，同时 ZIP archive materialization 已落地为显式 `materialize_zip_archive(...)` / `preview_zip_archive(...)`，只在 `zircon_runtime` archive materializer 引入 `zip = { version = "9.0.0-pre2", default-features = false, features = ["deflate-flate2"] }`，`tar` 仍不进 manifest；2026-06-20 export materializer 已追加 `export_materialize_owner_split_static_passed_cargo_deferred_tests_deferred`、`export_materialize_symlink_boundary_static_passed_cargo_deferred_tests_deferred`、`export_materialize_preview_static_passed_cargo_deferred_tests_deferred`、`export_materialize_fatal_gate_static_passed_cargo_deferred_tests_deferred`、`editor_native_aware_export_fatal_gate_static_passed_cargo_deferred_tests_deferred`、`editor_native_aware_export_discovery_reuse_static_passed_cargo_deferred_tests_deferred` 与 `export_archive_zip_materialization_static_passed_cargo_deferred_tests_deferred`：materializer 切到 `materialize/{mod,archive,generated,paths,native,package_lookup,copy,report}.rs`，生成文件和 ZIP entry 写入前拒绝 empty/absolute/`.`/`..`/trailing-separator/backslash 路径，NativeDynamic lookup/copy 不跟随 symlink，preview API 提供 no-write planned-path/archive 报告，fatal plan 在写文件/复制包/native staging/build/archive 前阻断，editor native-aware export 复用同一份 `NativePluginLoadReport` 避免重复扫描。`fontdue`/`rfd`/`arboard` 均为 editor-host 候选或临时债务，不属于 runtime/interface 技术栈。`tech_stack_source_inventory.py` / `tech_stack_anchor_inventory.py` 已拆出 manifest/dependency/version/count 清单与 doc/guard/behavior/decision/Cargo gate 锚点，`tech_stack_boundary.py` 保持 341 行审计/依赖扫描/风险 owner，`tech_stack_markdown.py` 为 103 行 Markdown owner，并已把 `expected_manifest_count = 5`、`expected_non_dependency_count = 5`、`zip_dependency_count = 1`、`expected_zip_dependency_count = 1`、`zip_dependency_violations = []`、`tech_stack_guard_count = 12`、`behavior_test_anchor_count = 4`、`missing_behavior_test_anchors = []`、`editor_only_candidate_count = 3`、`jolt_feature_slot_count = 2`、`declared_removed_dependencies = []`、`rapier_or_avian_dependencies = []`、`mirror_docs_guard_present = true` 与 `risks = []` 纳入结构审计；`runtime_01_tech_stack_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数。`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation` 锁定 Runtime 01 在 `tech_stack/text_shaper/plugin physics Cargo gates` 通过前保持待验证状态，`export_build_plan Cargo gate` 单列待验证 | `docs/zircon_plugins/physics-plugin-options.md`、`docs/engine-architecture/runtime-tech-stack.md`、`docs/zircon_runtime/plugin/export_build_plan.md`、`docs/editor-and-tooling/runtime-editor-only-dependency-backlog.md`、`tests/extensions/tech_stack_dependency_guard.rs`、`zircon_runtime/src/tests/runtime_absorption/tech_stack.rs`、`zircon_runtime/src/plugin/export_build_plan/materialize/{mod,archive,generated,paths,native,package_lookup,copy,report}.rs`、`zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/manager.rs`、`zircon_editor/src/ui/host/editor_manager_plugins_export/manifest_completion/native.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_source_inventory.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_anchor_inventory.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/tech_stack_markdown.py` | 01 |
| P11 | ECS 数据面与 `bevy_ecs` 对照已启动（2026-06-12 盘点修正）:`StorageType::{Table,SparseSet}` 与 `ComponentStorage` 双 backing store 已确认；实体生命周期测试矩阵、观察者三类入口触发时机测试、命令队列错误报告面、events/messages 双通道分工测试、First-stage 事件通道统一推进测试、change tick 回绕与 stale 窗口截断测试已落地待 Cargo；Runtime 08 archetype/component/entity/event/message/resource/resource-store/component-storage/observer/commands facade/change-detection owner 已硬切为 `scene/ecs/archetype/{mod,id,index,move_result,record,signature}.rs`、`scene/ecs/component/{mod,marker,id,registry}.rs`、`scene/ecs/entity/{mod,despawned,error,internal,location,registry,slot,stable_location}.rs`、`scene/ecs/events/{mod,cursor,id,metrics,queue,store,subscription}.rs`、`scene/ecs/messages/{mod,cursor,id,queue,store}.rs`、`scene/ecs/resource/{mod,marker,id,registry}.rs`、`scene/ecs/resource_store/{mod,stored_resource,store}.rs`、`scene/ecs/storage/component_storage/{mod,entry,location,sparse,store,table,utils}.rs`、`scene/ecs/observer/{mod,callbacks,entry,id,store,utils}.rs`、`scene/ecs/commands/commands/{mod,entity_commands,facade,param}.rs` 与 `scene/ecs/change_detection/{mod,change_tick,change_tick_window,component_ticks,stats,wrappers}.rs` folder-backed 子树，并由 root leaf 守卫锁定 `scene/ecs/{bundle,removal,storage_type}.rs` 显式根层叶子 owner；`ecs_kernel_data_boundary` 已把 `expected_source_file_count = 69`、`expected_test_file_count = 8`、`archetype_anchors = 15/15`、`storage_anchors = 9/9`、`component_storage_private_reexport_anchors = 9/9`、`unexpected_component_storage_private_reexports = []`、`component_identity_anchors = 18/18`、`entity_lifecycle_anchors = 10/10`、`observer_anchors = 8/8`、`deferred_command_anchors = 11/11`、`event_message_anchors = 12/12`、`resource_identity_anchors = 12/12`、`change_tick_anchors = 6/6`、`runtime_08_guard_anchors = 21/21`、`behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、`doc_anchors = 13/13`、`pending_cargo_gate_anchors = 6/6`、`mirror_docs_guard_present = true`、`risks = []` 纳入结构审计；`ecs_kernel_data_boundary.py` 当前为 344 行审计读取/缺失锚点/风险聚合 owner，`ecs_kernel_data_markdown.py` 为 154 行 Markdown 渲染 owner；`runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；`runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation` 锁定这些行在 Cargo 过滤测试通过前不能提升完成 | `docs/zircon_runtime/scene/ecs.md` Runtime 08 数据面对照、`scene/ecs/` 当前 30 个顶层条目盘点、`observer/{store,id,entry,callbacks,utils}.rs`、`commands/command_queue.rs`、`commands/commands/{mod,entity_commands,facade,param}.rs`、`archetype/{mod,id,index,move_result,record,signature}.rs`、`component/{mod,id,marker,registry}.rs`、`entity/{mod,despawned,error,internal,location,registry,slot,stable_location}.rs`、`events/{mod,cursor,id,metrics,queue,store,subscription}.rs`、`messages/{mod,cursor,id,queue,store}.rs`、`resource/{mod,id,marker,registry}.rs`、`resource_store/{mod,stored_resource,store}.rs`、`storage/component_storage/{mod,entry,location,sparse,store,table,utils}.rs`、`change_detection/{mod,change_tick,change_tick_window,component_ticks,stats,wrappers}.rs`、`scene/ecs/{bundle,removal,storage_type}.rs`、`tests/runtime_absorption/ecs_kernel_data.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ecs_kernel_data_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ecs_kernel_data_markdown.py` | 08 |
| P12 | UI 子系统 Runtime 09 M0/M1.1/M1.2/M2.1/M2.2/M3.1 已静态落地：`docs/zircon_runtime/ui/architecture.md` 覆盖 18 个 `ui/` 顶层条目与当前 20 个 `surface/` 条目，裁决 v2 为 replacement mainline，并由 `runtime_09_v2_verdict_matches_runtime_and_interface_modules` 锁定；`.zui` 是生产 component suffix，`.v2.ui.toml` 保留为 v2 view/style/runtime fixture/editor chrome profile，old recursive template 路径为 migration/test-only；M1.1 已新增 `route_authority=runtime_09_m1_1_ui_input_route_authority` 诊断，normalized `UiInputEvent` 经 `surface/input/dispatch.rs` 单点出口消费 `UI_INPUT_ROUTE_ORDER`，直接 pointer/navigation 入口带 owner 判词 `runtime_09_m1_1_direct_pointer_navigation_routes_are_leaf_owner_helpers`；M1.2 已将 navigation local reply 改为 `routed_reply`，将 pointer local route result 改为 `routed_result`，将 pointer capture fallback 判断改为 `has_pointer_capture_or_unindexed_fallback_for_owner`，将 table row-label fallback splitter 改为 `split_row_label_table_text`，将 template component-name fallback 改为 `component_name_interaction_fallback` / `component_name_interactive`，并将 property visibility flag 与 responsive MUI visibility flag 改为 `state_visible_flag`，将 accessibility open-state fallback property set 改为 `fallback_properties`，将 layout engine backend 公开名硬切为 `UiLayoutEngineBackend::Zircon`、`UiLayoutEngineCapability::zircon()` 与 `zircon_selected_count`，并将 surface default interaction fallback property set 改为 `fallback_properties`；M2.1 已把 Taffy 树构建与 `compute_layout` 收束到 `taffy_bridge/compute.rs::compute_taffy_child_frames(...)`，`layout/pass/pipeline.rs` 暴露 `UI_LAYOUT_PASS_ORDER` 并由 full/incremental layout 消费，`style_mapping.rs` 明确为 `runtime_09_m2_1_style_mapping_remains_taffy_dto_adapter`；M2.2 已把虚拟化滚动边界收束到 `layout/scroll.rs::UiScrollVirtualizationPlan` / `plan_scrollable_virtual_window(...)`，`layout/pass/arrange.rs` 与 `tree/node/scroll.rs` 消费同一 planner，`scroll_virtualization.rs` 记录三个行为锚；M3.1 已新增 `UiTemplateRuntimePipeline`、`UI_TEMPLATE_RUNTIME_PIPELINE_STAGES`、`UiTemplateRuntimePipelineError`，把 template 旧路径固定为 `load -> validate -> instance -> build`，并以 `runtime_09_m3_1_binary_leaf_dto_artifact_not_generated_source` 声明当前 compiled artifact 是 binary/TOML DTO payload 而不是 generated source；UI legacy 基线为 `ui_legacy_hits=54` / `ui_legacy_production_hits=0` / `ui_legacy_production_files=0`，taffy production 基线为 `ui_taffy_production_hits=175` / `ui_taffy_production_files=10`；`runtime_absorption::ui_architecture` 已新增十九条结构守卫锁模块边界、基线扫描、v2 runtime/interface 模块形状、M1.1 route authority、M1.2 navigation rename、M1.2 pointer rename、M1.2 pointer capture fallback rename、M1.2 table row-label fallback rename、M1.2 template component-name fallback rename、M1.2 property visibility flag rename、M1.2 responsive MUI visibility flag rename、M1.2 accessibility open-state fallback rename、M1.2 layout engine backend name cutover、M1.2 surface default interaction fallback rename、M2.1 bridge/pass-order、M2.2 scroll virtualization、M3.1 template pipeline 与镜像文档计数；`ui_architecture_boundary` 已接入总审计，报告 `expected_source_file_count = 52`、`expected_ui_entry_count = 18`、`expected_surface_entry_count = 20`、`legacy_full_hits = 54`、`expected_legacy_full_hits = 54`、`legacy_production_hits = 0`、`expected_legacy_production_hits = 0`、`legacy_production_file_count = 0`、`expected_legacy_production_file_count = 0`、`taffy_production_hits = 175`、`expected_taffy_production_hits = 175`、`taffy_production_file_count = 10`、`expected_taffy_production_file_count = 10`、`runtime_v2_anchor_count = 10`、`interface_v2_anchor_count = 9`、`guard_anchor_count = 19`、`cargo_gate_anchor_count = 7`、`doc_anchor_count = 61`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`；`runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation` 锁定 `ui/input/naming_boundary/layout/template` owner/Cargo gate；M1 production legacy 命名桶已清零，完整 UI behavior filters 仍待 editor UI owner 与 Cargo lane 空窗 | `docs/zircon_runtime/ui/architecture.md`、`docs/zircon_runtime/ui/surface/input.md`、`docs/zircon_runtime/ui/surface/default_interactions.md`、`docs/zircon_runtime/ui/template/pipeline.md`、`docs/ui-and-layout/shared-ui-template-runtime.md`、`docs/plans/zircon_runtime/runtime/09-ui-subsystem-architecture.md`、`zircon_runtime/src/ui/surface/input/{mod,dispatch,route_authority,navigation,pointer,pointer_reply}.rs`、`zircon_runtime/src/ui/surface/input/state/pointer_capture.rs`、`zircon_runtime/src/ui/surface/input/effect/focus_pointer.rs`、`zircon_runtime/src/ui/surface/render/collection_rows/table.rs`、`zircon_runtime/src/ui/surface/property_mutation.rs`、`zircon_runtime/src/ui/surface/surface/default_interactions.rs`、`zircon_runtime/src/ui/accessibility/extract.rs`、`zircon_runtime_interface/src/ui/layout/engine.rs`、`zircon_runtime/src/ui/layout/{mod,style_mapping,scroll,virtualization}.rs`、`zircon_runtime/src/ui/layout/pass/{pipeline,layout_tree,incremental,arrange,responsive_mui,taffy_arrange}.rs`、`zircon_runtime/src/ui/layout/taffy_bridge/{mod,compute}.rs`、`zircon_runtime/src/ui/tree/node/scroll.rs`、`zircon_runtime/src/ui/template/{mod,pipeline,loader,validate,instance}.rs`、`zircon_runtime/src/ui/template/build/{surface_builder,interaction}.rs`、`zircon_runtime/src/ui/template/asset/compiler/package/artifact.rs`、`zircon_runtime/src/ui/tests/{scroll_virtualization,template_pipeline}.rs`、`zircon_runtime/src/tests/runtime_absorption/ui_architecture.rs`、`tests/runtime_absorption/plan_status/cargo_gates/middle.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/ui_architecture_boundary.py`、`ui/` 18 条目、`surface/` 20 条目 | 09 |
| P13 | cdylib 函数表版本不同步（`ZrRuntimeApiV1`/`ZrHostApiV1` vs plugin 宿主 `ZrHostApiV3` + 子 API V1×5，含 `ZrHostBridgeApiV1`）已由 runtime 10 定稿保守 bump 规则；interface `ui/` 22 条目镜像契约进入 M2：`runtime_10_m2_1_ui_contract_duplicate_public_types_removed_static_passed_cargo_pending` 已删除 runtime-local `UiBindingCodec` 与 `UiAssetSchemaVersionPolicy` duplicate definitions，`ui_contract_duplicate_public_types = 0`；`runtime_10_m2_2_ui_v2_contract_sync_static_passed_cargo_pending` 已把 Runtime 09 `v2-replacement-mainline` 判词、interface `ui/v2` DTO owner、runtime `ui/v2` consumer 与 `UiComponentApiVersion` 共享版本契约同步到 Runtime 10，`ui_v2_contract_sync_anchors = 9/9`；`runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff` 继续锁定 UI 镜像契约 M2 owner/Cargo gate，interface package 已在 2026-06-17 通过 168/168，仍在 runtime UI/editor Cargo 验证前不得误标完成；session 失败路径中的坏句柄/缺失非零句柄/销毁注册表 removal 守卫已 scoped Cargo 通过，`minimal`/`headless` profile 已改为跳过 `RuntimeRenderBridge` 并补 live double-destroy / destroyed-handle 夹具；`runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces` 进一步锁定 optional render bridge、capture 空帧 fallback 与 bind/unbind/present no-op；FFI panic 边界首批已静态落地，`dynamic_api/exports.rs` 的 `zircon_runtime_get_api_v1` 与 11 个函数表入口经 `catch_unwind` wrapper 收口，panic 转 `ZrStatusCode::Panic` 或 null 表指针，`session.rs` owner 函数保持 Rust ABI，并由 `runtime_api_table_entries_are_panic_wrapped_at_ffi_boundary` 与 `runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge` 锁定；host-request payload ABI boundary 已把 `drain_host_requests` 返回链路纳入 Runtime 10：interface `ZrRuntimeHostRequestBatchV1` / `ZrRuntimeHostRequestV1::{Ime,GamepadRumble,Cursor}` DTO、runtime conversion、dynamic API host-request tests 与 app host routing 统一被 38 个 anchor 锁定；`runtime_api_markdown_split_static_passed_cargo_deferred_tests_deferred` 已把 interface ABI facade 审计的 Markdown 渲染移到 `runtime_api_markdown.py`，`runtime_api_boundary.py` 降到 143 行审计/风险入口并继续报告 folder-backed owner modules 6/6、facade 12/20 non-empty lines、`risks = []`；`dynamic_runtime_api_markdown_split_static_passed_cargo_deferred_tests_deferred` 已把 dynamic API boundary Markdown 渲染移到 `dynamic_runtime_api_markdown.py`，`dynamic_runtime_api_boundary.py` 降到 330 行审计/风险入口、Markdown owner 为 65 行；`dynamic_runtime_api_boundary` 已把 Runtime 10 的 `expected_source_file_count = 33`、`function_table_structs = 10/10`、`field_count_mismatches = 0`、`missing_repr_c_tables = 0`、`runtime_session_ffi_wrappers = 11/11`、`direct_session_table_entry_bypasses = 0`、`session_owner_extern_c_present = false`、`headless_lifecycle_anchors = 12/12`、`ffi_panic_anchors = 9/9`、`loader_failure_anchors = 10/10`、`behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、`runtime_diagnostics_anchors = 15/15`、`missing_runtime_diagnostics_anchors = []`、`scene_asset_reload_diagnostic_path_anchors = 21/21`、`host_request_payload_anchors = 38/38`、`missing_host_request_payload_anchors = []`、`ui_pending_gate_anchors = 8/8`、`ui_contract_single_source_anchors = 7/7`、`ui_contract_duplicate_public_types = 0`、`ui_v2_contract_sync_anchors = 9/9`、`pending_cargo_gate_anchors = 5/5`、`doc_anchors = 13/13`、`mirror_docs_guard_present = true`、`risks = []` 纳入结构审计；`runtime_10_dynamic_runtime_api_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；Runtime 10 的 headless lifecycle、panic-boundary、完整 app loader 与 runtime UI/editor Cargo 验证仍待活动编译通道空闲后重跑；interface package gate 已 168/168 通过 | `runtime_api/api_table.rs:43,63`、`runtime_api/host_requests.rs`、`plugin_api.rs:41-249`、`dynamic_api/exports.rs:25`、`dynamic_api/session.rs`、`dynamic_api/session/host_requests.rs`、`dynamic_api/tests/{api_table,host_requests,session_lifecycle,session_entry_points,session_profiles}.rs`、`runtime_entry_app/host_requests/{routing,cursor/request}.rs`、`tests/runtime_absorption/dynamic_api_session.rs`、`tests/runtime_absorption/dynamic_api_session/{shared,mirror_docs,ui_contract,v2_contract}.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late/runtime_10.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_api_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_api_markdown.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/dynamic_runtime_api_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/dynamic_runtime_api_markdown.py` | 10 |
| P14 | JobSystem M0/M1 已静态扩展：`TaskPoolOptions` 为线程预算 owner，`JobHandle`（wait/combine）、`JobScheduler::schedule`/`schedule_after`/`wait_all` 与阻塞式 `parallel_for` 已在 `core/runtime/tasks` 落地并有 tasks 测试锚；`schedule_after` 用完成回调启动依赖任务，不占 worker 等待，`wait_all` 作为 scheduler-owned 多句柄同步点记录 `tasks.main_thread_wait_ms`。`worker_wait_assist_static_passed_cargo_deferred` 进一步把 worker 内 `wait()` 自锁风险收束到 task-pool-owned `assist_current_thread_once(...)`，`worker_thread_wait_does_not_deadlock_scheduler` 锚锁单 worker 下 child handle 等待不死锁。M2.1 graphics 剔除旁路已完成静态收编：`WgpuRenderFramework` 持有 `compute_task_pool`，runtime graphics module construction 传入 `core.task_pools().compute().clone()`，`parallel_frustum.rs` 通过 `VisibilityContext::from_extract_with_history_static_index_and_task_pool(...)` 消费 `parallel_for(...)`，不再 direct rayon；当前锚点 `runtime_11_m2_1_graphics_frustum_rayon_cutover_static_passed_cargo_pending`，`direct_rayon_paths = 2`。M2.3 ECS 批次依赖化已静态落地：`ScheduleParallelExecutor` 现在用 `JobHandle::completed()` 起点 + `schedule_after` 链提交每批，等待尾批并按批次顺序回放错误；`ScheduleParallelTaskRegistry` 内部改为 `Arc` 任务；M2.2 非 graphics 收编也已静态落地，批次内部固定/泛化并行路径改走 `JobScheduler::join(...)` 与 `run_parallel_tasks(...)`，`schedule_parallel_executor_does_not_call_rayon_directly` 与 `runtime_absorption::rayon_boundary` 固定生产源码直连 rayon 边界。M2.4 asset worker 线程来源已按显式记账路线静态落地：`ProjectAssetManager::default()` 与 asset module factory 从 `TaskPoolOptions` IO 配额推导 worker 数，显式构造保持 override，并通过 `asset.worker.budgeted_threads` 诊断记账；`asset_worker_pool_matches_runtime_04_and_11_decisions` 同步锁定该跨计划判词。M3.1 调度诊断已补 `JobSchedulerReport`、`tasks.scheduled` / `tasks.completed` / `tasks.dependency_wait_ms` / `tasks.main_thread_wait_ms` 与 `DiagnosticStore` 发布路径；M3.2 压测锚已补 64 层依赖链、128 宽扇出 combine 与 scheduler wait_all 同步点。`job_system_boundary` 已报告 `expected_module_count = 9`、`direct_rayon_paths = 2`、`schedule_parallel_executor_direct_rayon = []`、`diagnostic_anchor_count = 4`、`behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true` 与 `risks = []`；2026-06-21 `job_system_inventory_split_static_passed_cargo_deferred_tests_deferred` 将 source/Rayon 清单拆到 `job_system_source_inventory.py`，将声明/API/测试/文档锚拆到 `job_system_anchor_inventory.py`，`job_system_boundary.py` 保持 193 行审计/风险入口，`job_system_markdown.py` 为 64 行 Markdown owner；`runtime_11_job_system_mirror_docs_match_structure_audit_counts` 锁定 JobSystem 模块文档、Runtime 11、总索引、M0 review 与 runtime-interface convergence 的镜像数字。`runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass` 锁定 Runtime 11 在剩余验证前保持待完成。core-min `ecs_schedule` 已由 `runtime_11_core_min_ecs_schedule_cargo_passed_remaining_gates_pending` 通过；剩余 M2/Cargo 缺口：完整 `tasks/ecs_schedule/worker_pool/rayon` gate anchor 保持可见，其中 `tasks`/`worker_pool`/`rayon` 与更宽配置 Cargo 待 active lanes 清空 | `docs/zircon_runtime/core/job_system.md`、`docs/zircon_runtime/asset/worker_pool.md`、`docs/zircon_runtime/scene/ecs/schedule_parallel_executor.md`、`core/runtime/tasks/{diagnostics,job_handle,job_scheduler,parallel_for,report}.rs`、`scene/ecs/schedule_parallel_executor.rs`、`scene/tests/ecs_schedule/parallel_executor.rs`、`scene/tests/ecs_schedule_parallel_executor_structure.rs`、`tests/runtime_absorption/{job_system,rayon_boundary,asset_worker_policy}.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late/runtime_11.rs`、`tests/tasks.rs`、`graphics/visibility/culling/parallel_frustum.rs`、`asset/pipeline/worker_pool.rs`、`asset/pipeline/manager/project_asset_manager/{project_asset_manager,construction,debug}.rs`、`asset/tests/pipeline/worker_pool.rs`、`runtime_structure_audits/job_system_{boundary,source_inventory,anchor_inventory,markdown}.py` | 11 |
| P15 | 输入栈最小动作映射层已由 12-M1 静态落地（`InputAction`/`InputActionContext`/`InputBinding`/`InputAxisBinding`/`InputAxisDirection`/`InputActionMap`/`InputActionState`/`InputConfig` + `InputActionEvaluator`），Cargo 待跑；帧输入语义（just_*/清帧时机）已由 12-M0.1 文档与测试锚静态落地但 Cargo 待跑；gamepad 已确认 app 侧 gilrs → runtime ABI → `session/events.rs` → `InputEvent::Gamepad*` 路径并由 12-M2.1 新增断开清理/source guard 锚，Cargo/app 回归待跑；UI 与玩法的输入仲裁已由 12-M0.2 裁决为 UI surface/capture/popup/focus 优先、玩法/action mapping 只消费 UI 未处理或无 UI/headless 输入流；`evaluate_with_active_contexts(...)` / `evaluate_with_active_contexts_and_consumed_buttons(...)` 已支持 gameplay/menu/global action context routing，`evaluate_with_consumed_input(...)` / `evaluate_with_active_contexts_and_consumed_input(...)` 已让 UI 消费的 `GamepadAxisInput` 在轴值和轴边沿求值前被过滤；`InputActionState::value(...)` 已支持 gamepad axis 连续 action value，按钮 action 继续输出 `1.0`；`GamepadAxisTransition` / `InputFrameSnapshot::gamepad_axis_transitions` / `binding_axis_transition(...)` 已支持 axis action 的死区跨越 `just_activated` / `just_deactivated` 边沿；`InputConfig::action_evaluator()` 已把序列化 action map 接入 evaluator 构造，`InputActionManager` / `DefaultInputActionManager` 与 `resolve_input_action_manager(...)` 已把 action map 接入 runtime manager 注册路径；`InputRecording` / `InputReplayCursor` 已提供基于 `InputEventRecord` 的确定性录制/回放 helper；`CursorHostRequest` / `InputEvent::CursorHostRequest` / `InputFrameSnapshot::cursor_host_requests` / `InputManager::drain_cursor_host_requests()` 已提供 frame-local cursor host handoff；`runtime_absorption::input_stack` 已锁帧输入导出/文档、UI-filtered action evaluator、gamepad ABI bridge、axis action value、consumed-axis arbitration、axis transition edges、config source、action manager registration、input recording/replay、cursor host requests 与 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 镜像文档计数；`input_stack_boundary.py` 结构审计已接入总审计并作为 337 行审计/风险 owner，`input_stack_markdown.py` 为 108 行 Markdown owner，报告 `expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 15`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`，但 `runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation` 仍锁定 input/action_map/gamepad/app Cargo gate | `input/mod.rs:6-22`、`input/module/config.rs`、`input/runtime/{default_input_manager,input_driver,input_state,action_evaluator,recording}.rs`、`core/framework/input/{cursor,input_action*,input_binding,input_action_state,input_frame_snapshot}.rs`、`zircon_app/src/entry/runtime_entry_app/gamepad/{events,polling}.rs`、`dynamic_api/session/events.rs`、`tests/runtime_absorption/input_stack.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late/runtime_12.rs`、`docs/plans/zircon_runtime/runtime/12-input-stack-and-action-mapping.md`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/input_stack_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/input_stack_markdown.py` | 12 |
| P16 | 脚本绑定面已由 Runtime 13 静态收束：`function_ledger.md` 固定 builtin/gameplay host ledger 为 6 模块 / 52 函数 / 2 类型描述符，`zr.zircon.bridge` 只锁动态模块形状；`ZirconScriptType`/VM host marshalling 已裁决为 value descriptors / host handles / serialized payloads 三类；脚本-ECS 默认路径为 `zr.zircon.gameplay` / `ScriptRuntimeCallContext` gameplay facade，`ZrHostEcsApiV1` 保持 native/plugin ABI 层；`host_function_registry_matches_documented_ledger`、capability 代表函数 descriptor 守卫、缺失授权拒绝守卫、脚本持有实体 despawn 后失效语义与 native ECS ABI 旁路 source guard 已静态落地；`script_binding_boundary.py` 保留 351 行审计/risk owner，`script_binding_markdown.py` 为 106 行 Markdown owner；`script_binding_boundary` 当前复核报告 `expected_source_file_count = 18`、`expected_test_file_count = 3`、`fixed_host_module_count = 6`、`fixed_host_function_count = 52`、`type_descriptor_count = 2`、`builtin_callback_count = 11`、`gameplay_callback_count = 39`、`macro_host_function_count = 2`、`host_capability_count = 11`、`missing_ledger_doc_anchors = []`、`missing_runtime_13_guards = []`、`native_ecs_abi_references = []`、`oversized_test_files = []`、`mirror_docs_guard_present = true`、`risks = []`；`runtime_13_script_binding_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass` 锁定 Runtime 13 在 broader script filters 通过前保持 Cargo 待 active lane 清空，`script::vm` Cargo 已 48/48 passed，2026-06-21 Markdown renderer split 轻量守卫通过但 broader script Cargo gate 仍 pending | `docs/zircon_runtime/script/vm/host/function_ledger.md`、`tests/runtime_absorption/{script_host_ledger,script_binding}.rs`、`script/vm/gameplay_host/tests.rs`、`script/vm/capability_set.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late/runtime_13.rs`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/script_binding_boundary.py`、`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/script_binding_markdown.py` | 13 |
| P17 | crate 根四个"小族"已由 Runtime 14 M0/M1 静态收束：animation 28 个 `.rs` 文件并补 bevy/Fyrox/Godot 差距表，root motion backlog、GPU skinning/render owner 与 editor tooling 非目标已由 `runtime_animation_backlog_boundary_requires_doc_update` 守卫；navigation 9 个 `.rs` 文件，已裁决为 built-in fallback runtime，并做 folder-backed runtime owner split，把 `runtime/{baked_mesh,world_scan,avoidance,state,math,tests}.rs` 纳入 `runtime_navigation_boundary_file_set_requires_doc_update` 文件集守卫；diagnostic_log vs core::diagnostics 双诊断面分工已文档化，`diagnostic_log_snapshot_bridge_stays_single_owner` 固定唯一 snapshot-to-log 桥；engine_module 已裁决为 core::runtime 之上的 declared layering，不合并、不删除，并由 `engine_module_declared_layer_does_not_own_runtime_lifecycle` 守卫；`runtime_14_module_family_root_seats_match_documented_judgements` 进一步锁定四族仍是 crate-root module family 且不在 `lib.rs` 扁平化 re-export；`module_family_boundary` 当前报告 `expected_family_count = 4`、`animation = 28`、`navigation = 9`、`diagnostic_log = 7`、`engine_module = 8`、`root_seat_guard_present = true`、`animation_status_json_guard_present = true`、`animation_status_json_anchor_count = 8`、`missing_animation_status_json_anchors = []`、`module_family_guard_anchor_count = 7`、`missing_module_family_guard_anchors = []`、`cargo_gate_anchor_count = 5`、`missing_cargo_gate_anchors = []`、`risks = []`；`module_family_boundary.py` 当前为 305 行审计数据/风险聚合 owner，`module_family_markdown.py` 为 61 行 Markdown 渲染 owner；Runtime 14 仍非 completed，Cargo/rustc 重跑待 active lane 清空 | `docs/zircon_runtime/animation/runtime.md`、`docs/zircon_runtime/navigation/runtime.md`、`docs/zircon_runtime/diagnostic_log/mod.md`、`docs/zircon_runtime/engine_module/relationship.md`、`tests/runtime_absorption/root_entries.rs`、`engine_module/tests.rs`、`diagnostic_log/diagnostics.rs`、`tests/runtime_absorption/plan_status/cargo_gates/late/runtime_14.rs`、`module_family_boundary.py`、`module_family_markdown.py` | 14 |

Runtime 09 P12 supplement (2026-06-21): `ui_architecture_markdown.py` now owns `render_ui_architecture_boundary_markdown(...)`; `ui_architecture_boundary.py` remains the 541-line audit/risk owner and the Markdown owner is 110 lines under `ui_architecture_markdown_split_static_passed_cargo_deferred_tests_deferred`.

Runtime 02 P2 supplement (2026-06-21): `runtime_root_surface_markdown.py` now owns `render_runtime_root_surface_markdown(...)`; `runtime_root_surface.py` remains the 268-line root public-surface audit/risk owner, reports 19 public modules, 2 public `pub use` sites, zero unclassified root surface entries, zero migration debt, and M1 gate `classified-and-clear` under `runtime_root_surface_markdown_split_static_passed_cargo_deferred_tests_deferred`.

### 2.3 参考引擎证据锚点（2026-06-13 扩充：全路径实测存在；实现型切片动工前必读对应行，见 §7.9）

| 维度 | Bevy | Fyrox | Unreal | Godot / 其他 |
|------|------|-------|--------|--------------|
| 帧循环权威 | `dev/bevy/crates/bevy_app/src/main_schedule.rs`（MainScheduleOrder） | `dev/Fyrox/fyrox-impl/src/engine/executor.rs`（fixed-step 累积） | Tick group（PrePhysics→PostUpdateWork） | `dev/godot/core/os/main_loop.{h,cpp}`、`dev/godot/main/main.cpp` |
| 固定步与时钟 | `dev/bevy/crates/bevy_time/src/{time.rs,fixed.rs,virt.rs,real.rs,timer.rs}` | 同上 executor.rs 的 lag 循环 | — | `dev/godot/main/main_timer_sync.{h,cpp}`（固定步+插值同步的工程实现） |
| ECS 存储 | `dev/bevy/crates/bevy_ecs/src/storage/{table/,sparse_set.rs,blob_array.rs}` | `dev/Fyrox`（图式 scene graph，对照非 ECS 取舍） | — | — |
| 实体分配/观察者/事件 | `dev/bevy/crates/bevy_ecs/src/entity/mod.rs`、`observer/{mod.rs,runner.rs,centralized_storage.rs}`、`event/{mod.rs,trigger.rs}` | — | — | — |
| 调度执行器 | `dev/bevy/crates/bevy_ecs/src/schedule/{schedule.rs,executor/,graph/,auto_insert_apply_deferred.rs}` | — | — | — |
| 任务/JobSystem | `dev/bevy/crates/bevy_tasks/src/{task_pool.rs,usages.rs,slice.rs,iter/}` | — | `dev/UnrealEngine/Engine/Source/Runtime/Core/Public/Tasks/{Task.h,Pipe.h,TaskConcurrencyLimiter.h}`、`Async/{ParallelFor.h,Async.h,LocalWorkQueue.h}` | `dev/godot/core/object/worker_thread_pool.{h,cpp}`（组任务/优先级/yield）；Unity 仅语义锚（源码不在 dev/） |
| 渲染世界分离 | `dev/bevy/crates/bevy_render/src/extract_plugin.rs` | `fyrox-impl/src/renderer/` | RHI/RenderCore/Renderer 三层 | — |
| 资产 | `dev/bevy/crates/bevy_asset/src/{loader.rs,handle.rs,server/,processor/,meta.rs}` | `dev/Fyrox/fyrox-resource/src/{manager.rs,loader.rs,state.rs,event.rs}` | AssetRegistry | `dev/godot/core/io/{resource_loader,resource_importer,resource_uid}.{h,cpp}`（线程化加载/去重/稳定 ID）；`dev/Piccolo/engine/source/runtime/resource` |
| UI | `dev/bevy/crates/bevy_ui/src/layout/{mod.rs,ui_surface.rs}`（taffy 桥接） | `dev/Fyrox/fyrox-ui/src/{control.rs,canvas.rs,button.rs,...}`（retained 控件树+消息路由） | Slate/UMG（概念对照） | `dev/godot/scene/gui/`（Control/Container 布局族） |
| 插件/热重载 | Plugin/PluginGroup（静态） | `fyrox-impl/src/plugin/{mod.rs,dylib.rs}`（DynamicPlugin + 状态序列化重载） | 模块系统 | `dev/godot/core/extension/{gdextension.{h,cpp},gdextension_function_loader.{h,cpp}}`（C ABI 函数表装载/协商） |
| 动态扩展 ABI | — | 同上 dylib.rs | — | `dev/godot/core/extension/extension_api_dump.cpp`（API 面 dump/版本化） |
| 诊断/Profiling | `dev/bevy/crates/bevy_diagnostic/src/{diagnostic.rs,frame_time_diagnostics_plugin.rs,frame_count.rs}` | — | — | `dev/godot/main/performance.{h,cpp}`（性能监视器单点）；`dev/tracy`（profiler 本体源码） |
| 物理 | 核心无物理（生态 rapier/avian） | `dev/Fyrox/fyrox-impl/Cargo.toml:30-31`（rapier 外挂） | Chaos 内置 | `dev/godot/modules/{godot_physics_3d,jolt_physics}`（自研+Jolt 双后端） |
| 模块尺度 | crate-per-subsystem（约 40+ crate） | crate-per-layer（core/resource/graph/ui/impl/editor） | `Engine/Source/Runtime` 约 189 模块 | `dev/Piccolo/engine/source/runtime/{core,function,platform,resource}`（小型引擎单 runtime 分层全参照） |

`zircon_runtime` 取"单 crate + 内部 spine"形状，介于 Fyrox（多 crate 分层）与 Unreal（巨型模块树）之间;在 cdylib 热重载约束下单 crate 是合理选择，但要求内部模块边界承担 Bevy 中 crate 边界的职责——这正是 P1/P2/P4 要修的内容。

## 3. 子计划地图与执行顺序

| 计划 | 文档 | 依赖 | 状态 |
|------|------|------|------|
| 01 技术选型与依赖治理 | `01-tech-stack-and-dependency-governance.md` | 无 | in_progress（M1 runtime-tech-stack 文档、架构索引挂接与 tech_stack 依赖守卫已落地，standalone rustc 守卫 10/10 通过；M2.1/M2.2 text.md 三层文本栈矩阵、tech-stack 交叉引用、NativeGlyphon 状态口径与 complex text 候选裁决守卫完成，cosmic-text/Parley/Swash/HarfBuzz 只能经替换 `UiTextShaper` 接入；M2.3 fontdue 裁决为 editor retained-host 临时文本 fallback；M3.1 物理后端选型文档完成并补 `physics_backend_option_decision_keeps_jolt_unavailable_and_plugin_owned` 守卫，builtin 为 V1 唯一可执行后端、Jolt 为未来 native 方向但保持 unavailable；M3.2 导出归档决策完成，ZIP 为当前桌面/editor archive materializer，`zip = { version = "9.0.0-pre2", default-features = false, features = ["deflate-flate2"] }` 仅由 runtime archive materializer 引入，`tar` 仍不引入；M3.2a `export_materialize_owner_split_static_passed_cargo_deferred_tests_deferred` 已把目录式 export materializer 切到 `materialize/{mod,generated,paths,native,package_lookup,copy,report}.rs` 并补生成文件路径防逃逸校验；M3.2b `export_materialize_symlink_boundary_static_passed_cargo_deferred_tests_deferred` 已补 NativeDynamic package lookup/copy 的非跟随 symlink 边界，公开 API 与 report shape 不变；M3.2c `export_materialize_preview_static_passed_cargo_deferred_tests_deferred` 已新增 materialization dry-run preview API，复用同一报告 shape 与诊断规则且不落盘；M3.2d `export_materialize_fatal_gate_static_passed_cargo_deferred_tests_deferred` 已让 fatal plan 在 generated-file write / NativeDynamic package copy 前返回 no-op materialization report；M3.2e `editor_native_aware_export_fatal_gate_static_passed_cargo_deferred_tests_deferred` 已让 editor native-aware export 在 staging/build 前 early-exit；M3.2f `editor_native_aware_export_discovery_reuse_static_passed_cargo_deferred_tests_deferred` 已让执行路径复用初始 `NativePluginLoadReport` 生成 plan 与准备 native packages，避免重复扫描 plugin directory；M3.2g `export_archive_zip_materialization_static_passed_cargo_deferred_tests_deferred` 已新增 `materialize_zip_archive(...)` / `preview_zip_archive(...)`、`ExportMaterializeReport.archive_file`、ZIP entry path 校验、稳定 entry 时间/权限、NativeDynamic runtime payload archive 写入与 fatal/preview no-write 边界；M3.3 editor-only 依赖 backlog 完成，rfd/arboard 不进 runtime/interface；`tech_stack_source_inventory.py` / `tech_stack_anchor_inventory.py` 已拆出 Runtime 01 manifest/dependency/version/count 与 doc/guard/behavior/decision/Cargo gate 清单，`tech_stack_boundary` 结构审计报告 `expected_manifest_count = 5`、`expected_non_dependency_count = 5`、`zip_dependency_count = 1`、`expected_zip_dependency_count = 1`、`zip_dependency_violations = []`、`tech_stack_guard_count = 12`、`editor_only_candidate_count = 3`、`jolt_feature_slot_count = 2`、`declared_removed_dependencies = []`、`rapier_or_avian_dependencies = []`、`mirror_docs_guard_present = true` 与 `risks = []`；`runtime_01_tech_stack_mirror_docs_match_structure_audit_counts` 已补为镜像文档守卫；`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation` 已补为 Cargo 待验证守卫；Cargo `tech_stack` 重跑未执行到测试，lib-test 编译先被活跃 plugin bridge 切片的 `extension_registry_bridge.rs` unresolved `BridgeInterfaceSnapshot` / `BridgeInterfaceStatus` 导入阻断，当前 `tech_stack/text_shaper/plugin physics Cargo gates` Cargo 待 active lanes 清空，`export_build_plan Cargo gate` 单列待验证） |
| 02 core spine 与 root surface 收束 | `02-core-spine-and-root-surface.md` | 无 | in_progress（M1/M2.1/M2.2 物理 cutover 完成；root/naming 独立守卫通过；`root_surface_guard` 固定 `lib.rs` public namespace surface 与禁止 flattened subsystem root exports；M3.3 root graphics alias block removal 已删除 `pub(crate) use graphics::...` 与 `#[allow(unused_imports)]`，静态扫描未发现 root alias 调用方，当前 `crate-visible graphics alias debt 0/0`；M4.1 generated 规范与裁决完成，M4.2 generated 结构守卫与 app export-bootstrap owner 已落地，export 模板 direct native-loader/EntryRunner/registration-call 行为已迁出，provider 表裁决为允许的 generated table adapter，结构审计 0 迁移债且 `classified-and-clear`；新增 `runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation` 锁定 core/root/generated/export_build_plan/app/editor/plugin pending gate；M2/M4 全量 Cargo 回归当前受 render/graphics 活动编译与测试目标超时影响，root alias cutover 已静态完成但包级 Cargo 待验证） |
| 03 调度与帧循环对齐 | `03-schedule-and-frame-loop-alignment.md` | 02（core 归属定稿） | in_progress（M0/M1/M2/M3 实现与源守卫已落地；M2 固定步三条 `level_tick_*` owner 测试已拆入 `scene/tests/ecs_schedule/fixed_update.rs`；`schedule_frame_loop_source_inventory.py`/`schedule_frame_loop_anchor_inventory.py` 已拆出 Runtime 03 source/guard 与 anchor 清单，`schedule_frame_loop_markdown.py` 已拆出 Markdown 渲染，`schedule_frame_loop_boundary` 结构审计 owner 保留审计读取/缺失锚点/风险判定并已报告 source files 18/18、guard/test files 8/8、`SystemStage` count and variants 9/9、fixed-loop stages 3/3、dynamic-session `.tick_time(...)` calls 1/1、Runtime 03 guard anchors 14/14、`behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、`doc_anchors = 10/10`、frame schedule module-doc anchors 3/3、no `WorldDriver` second `advance_time_by(...)` references、no dynamic-session raw-delta level tick references、`risks = []`；当前 `schedule_frame_loop_boundary.py` 368 行、`schedule_frame_loop_markdown.py` 146 行；`runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 锁定 Runtime 03 计划、总索引、M0 review 与 runtime-interface convergence 的镜像数字；`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation` 已补为 Cargo 待验证守卫；`ecs_schedule/time/session/schedule_parallel Cargo gates` Cargo 待 active lanes 清空后重跑，上一轮 Cargo 回归被无关 `ui/tests/asset_dependency_index.rs` 导入错误阻断） |
| 04 资产管线对齐 | `04-asset-pipeline-alignment.md` | 可与 03 并行 | in_progress（M0 asset facade/reference gap table 完成并回写 `.zmeta` processor/meta owner 口径，计划表已同步 handle/loader/processor 最终裁决并由 `runtime_04_asset_facade_query_surface_stays_manager_owned_and_server_free` 防止 unresolved 占位回流；M0.2 facade 查询面盘点已静态锁定为 `ProjectAssetManager` / `Assets<TAsset>` / `AssetManager` service trait 的 manager/facade/service 分层，`runtime_04_asset_facade_query_surface_stays_manager_owned_and_server_free` 禁止回退到 asset server 词汇；M1.1 句柄语义裁决为保留 Zircon `Copy` typed ID，悬挂 handle 查询返回 `NotLoaded` 的命名回归测试已新增；M1.2 资源状态机收紧已落地，`Error -> Ready` 必须经 `Reloading`，失败原因通过 `ResourceRecord` diagnostics 投影到 facade；M2.1 worker pool 已切到 `AssetWorkerPoolOptions`，显式无界模式与 bounded queue-full 错误已落地；M2.2 pool-local in-flight request coalescing 已落地，`request_sender()` 公开旁路已删除，`request(...)` 成为唯一公开请求入口；M2.3 worker diagnostics 计数与 `DiagnosticStore` 发布已落地，in-flight 注册顺序已收紧以避免快完成悬挂计数；`asset_worker_pool_matches_runtime_04_and_11_decisions` 已静态落地，防止 worker pool 状态回退到旧基线；M3 watcher debounce/options、watch error 订阅面与 typed hot-reload event 回归已落地；Scene `.zasset` cache wire type 已补 scene mesh/camera/collider/joint 场景缓存修复并拆入 `asset/artifact/cache_payload/scene.rs`，`cargo test -p zircon_runtime --lib artifact_store_roundtrips_scene_assets_with ...` 已通过 4/4，watcher acceptance 已通过 7/7；`asset_pipeline_source_inventory.py`/`asset_pipeline_anchor_inventory.py` 已拆出 Runtime 04 source/guard 与 anchor 清单，`asset_pipeline_markdown.py` 已拆出 Markdown 渲染，`asset_pipeline_boundary.py` 保留审计读取/缺失锚点/风险聚合（328 行），`asset_pipeline_markdown.py` 为 117 行，并报告 `expected_source_file_count = 22`、`expected_guard_file_count = 11`、`worker_diagnostic_count = 7`、`expected_worker_diagnostic_count = 7`、`artifact_store_roundtrip_count = 4`、`expected_artifact_store_roundtrip_count = 4`、`watcher_acceptance_reference_count = 1`、`expected_watcher_acceptance_count = 7`、`artifact_acceptance_reference_count = 3`、`test_anchor_count = 24`、`behavior_test_anchor_count = 20`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`retired_worker_new_references = []`、`retired_worker_request_sender_references = []`、`old_watch_debounce_references = []`、`mirror_docs_guard_present = true` 与 `risks = []`；`runtime_04_asset_pipeline_mirror_docs_match_structure_audit_counts` 已补为镜像文档守卫；`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation` 已补为 Cargo 待验证守卫，剩余 broader `asset::` / `worker_pool` Cargo filters 待空闲窗口重跑） |
| 05 scene/editor 边界收尾 | `05-scene-editor-boundary-closeout.md` | 无（最小） | in_progress（本计划 frontmatter 从 `completed` 修正为 `in_progress`，因为计划 DoD 要求 `cargo test -p zircon_runtime --lib scene:: --locked` 全族无回归后才能最终关账；当前只有 `naming_boundary`、`authoring`、`inspection` scoped Cargo 通过记录。新增 `runtime_plan_last_refined_covers_latest_recorded_date`、`runtime_plan_status_does_not_claim_completed_while_validation_is_pending`、`runtime_plan_frontmatter_status_uses_known_lifecycle_values`、`runtime_index_status_map_matches_subplan_frontmatter`、`runtime_index_subplan_map_covers_existing_plan_files_without_stale_rows`、`runtime_index_problem_rows_reference_existing_subplans`、`runtime_index_execution_dependencies_reference_existing_subplans`、`runtime_index_in_progress_rows_record_remaining_gate`、`runtime_known_backlog_gaps_keep_owner_and_trigger_columns`、`runtime_subplans_keep_status_and_evidence_tables`、`runtime_subplan_status_records_keep_non_empty_evidence`、`runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs`、`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation`、`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation`、`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation`、`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation`、`runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation`、`runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation`、`runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`、`runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation`、`runtime_10_m1_3_cargo_pending_gate_stays_explicit_until_dynamic_api_validation`、`runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass`、`runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation`、`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass`、`runtime_14_module_family_cargo_gate_stays_visible_until_filters_pass`、`runtime_architecture_review_documents_all_absorption_guards` 与 `runtime_05_closeout_status_waits_for_full_scene_cargo_gate`，防止待验证计划误标 completed，并锁定 last_refined 覆盖最新状态日期、总表状态必须跟随子计划 frontmatter、总索引子计划地图必须精确覆盖 01-14 子计划文件、问题清单与执行依赖列必须指向现有子计划、所有 in_progress 总表行必须说明剩余 gate 或 blocker、总索引已知 backlog 缺口必须保留现状依据与 owner/触发条件、所有子计划必须保留状态/证据表且已启动记录必须写入具体证据、Runtime 01-14 近期静态/待验证守卫必须同步到计划/镜像文档/评审/索引（Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` gate 必须保持可见，Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` gate 必须保持可见，Runtime 09 `ui/input/naming_boundary/layout/template` owner/Cargo gate 必须保持可见，Runtime 07 覆盖 `runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` 与 `runtime_frame_schedule_stage.<SystemStage>`，Runtime 10 同时覆盖 `runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces` 与 `runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge`，且 M1.3 在 `dynamic_api` Cargo 通过前保持 `code_static_passed_cargo_pending`，Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 必须保持可见，Runtime 12 在 input/action_map/gamepad/app Cargo 通过前保持 pending gate 可见，Runtime 13 在 script filters Cargo 通过前保持 `code_static_pending_cargo`，Runtime 14 模块族守卫必须覆盖四族镜像文档，Runtime 05 closeout 必须保持 `pending_full_scene_cargo`）、Runtime 01 `tech_stack/text_shaper/plugin physics` Cargo gate、Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` gate、Runtime 03 `ecs_schedule/time/session/schedule_parallel` Cargo gate、Runtime 04 broader `asset::` / `worker_pool` Cargo gate、Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gate、Runtime 07 `extract/ecs_query/performance profiling/FPS` Cargo gate、Runtime 08 ECS 数据面 Cargo gate、Runtime 09 `ui/input/naming_boundary/layout/template` owner/Cargo gate、Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 与 Runtime 14 模块族 Cargo/rustc gate 必须保持可见，以及 M0 评审必须覆盖 `runtime_absorption/mod.rs` 和所有已挂载 guard；最终 `completed` 仍等待 `cargo test -p zircon_runtime --lib scene:: --locked` 全族闭环） |
| 06 插件公开面与生命周期 | `06-plugin-surface-and-lifecycle.md` | 02 | in_progress（M1 binding 空参数修复通过；fallback lifecycle failure tests 4/4 已落地；shader artifact cache 已修复并通过 targeted 预验证；Release ZrVM focused `vampire_project_session_starts_paused_until_start_button_click` 与 `vampire_project_session_game_over_menu_retries_to_playing` 已通过，覆盖 Start/Game Over/Retry；完整 `vampire_project_session` 组、plugin/native plugin、app 与 `zircon_plugins` gates 仍待关闭；`runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation` 锁定 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` validation gate；M2.1 `plugin::native` hard-cutover、M2.2 native loader 测试迁移、M3.1 V3-only native plugin ABI、M3.2 hot reload failure injection、native host FFI panic guard、F8 `RuntimePluginDescriptorBuilder` scaffold、first-party runtime plugin descriptor production files 16/16 builder migration 与 plugin extension RuntimePluginDescriptor test fixtures 14/14 builder migration 已静态落地，`runtime_plugin_descriptor_test_fixture_builder_migration_coremin_check_passed` 记录测试夹具 `RuntimePluginDescriptor::builder(...).build()` 迁移；`plugin_surface_lifecycle_boundary` 已接入总审计，报告 source 14/14、doc 5/5、`expected_source_file_count = 14`、`expected_doc_file_count = 5`、`root_reexport_count = 0`、`native_namespace_reexport_count = 60`、native root re-export 0/0、native namespace re-export 60/60、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、native loader test files 3/3、native test namespace import files 2/2、native test root import leaks 0/0、unknown ABI rejection、hot reload failure injection、app NativePlugin current call-site files: 7、`mirror_docs_guard_present = true`、`risks = []`；`runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts`、`runtime_06_native_loader_tests_use_isolated_plugin_native_namespace` 与 `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed` 锁定镜像文档计数、测试命名空间隔离和 fallback 失败路径覆盖；RuntimePluginDescriptor public-field convergence remains pending） |
| 07 runtime 侧性能热路径 | `07-runtime-performance-hotpath.md` | 03;ZrVM assertion 解除;render 计划阶段 A | in_progress（M0.2 profiling 构建入口、M0.3 frame breakdown span、direct runtime-frame `profiling-chrome` trace/export 测试与静态守卫已登记但 cargo 聚焦验证超时无结果、M1.1 scoped counter points/ECS frame diagnostics、QueryState/ChangeDetection frame auto-collection、M1.2 命名断言、M1.3 hotspot inventory/owner-budget mirror、`counter_hotspots.json` profiling counter evidence export、M2.1 `RuntimeFrameExtractCache` extract rebuild cache 与 cache hit/miss diagnostics 已静态落地；`performance_hotpath_source_inventory.py` / `performance_hotpath_anchor_inventory.py` / `performance_hotpath_boundary.py` / `performance_hotpath_markdown.py` 已把 Runtime 07 source/test 清单、anchor 清单、审计聚合和 Markdown 输出分离；`performance_hotpath_boundary` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 6`、`frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 21`、`asset_worker_anchor_count = 13`、`animation_scene_anchor_count = 19`、`profile_counter_hotspot_anchor_count = 8`、`hotspot_guard_anchor_count = 32`、`test_anchor_count = 29`、`doc_anchor_count = 35`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = migration-debt-present`、`large_file_hotspot_count = 30`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=3`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=13`、`support-hub=3`、`missing_large_file_owner_classes = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`；Runtime 14 navigation fallback runtime owner split 已把 `navigation/runtime.rs` 移出大文件热点，其他已拆 owner 包括 animation/scene asset、project_io、artifact cache payload、render product diagnostics、virtual geometry debug snapshot 与 gameplay host；`runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation` 继续锁定 extract/ecs_query/performance profiling/FPS gates 的 Cargo/profiling/FPS 待验证） |
| 08 ECS 内核数据面对齐 | `08-ecs-kernel-data-alignment.md` | 与 03/07 同文件区错峰；08↔10 实体 ID ABI 互检 | in_progress（M0 数据面对照裁决完成；M1 生命周期测试矩阵四条测试已落地；M2.1 观察者同步触发/目标实体/dispatch 移除三条测试已落地；M2.2 命令队列错误报告面与两条测试已落地；M3.1 events/messages 双通道五条测试已落地，包含 `first_stage_updates_all_registered_event_channels`；M3.2 change tick 回绕与 stale 窗口截断两条测试已落地；M4 archetype owner 已切到 `archetype/{mod,id,index,move_result,record,signature}.rs`，component owner 已切到 `component/{mod,marker,id,registry}.rs`，entity owner 已切到 `entity/{mod,despawned,error,internal,location,registry,slot,stable_location}.rs`，event owner 已切到 `events/{mod,cursor,id,metrics,queue,store,subscription}.rs`，message owner 已切到 `messages/{mod,cursor,id,queue,store}.rs`，resource identity owner 已切到 `resource/{mod,marker,id,registry}.rs`，resource-store owner 已切到 `resource_store/{mod,stored_resource,store}.rs`，component-storage owner 已切到 `storage/component_storage/{mod,entry,location,sparse,store,table,utils}.rs`，observer owner 已切到 `observer/{mod,callbacks,entry,id,store,utils}.rs`，commands facade owner 已切到 `commands/commands/{mod,entity_commands,facade,param}.rs`，change-detection owner 已由守卫锁定为 `change_detection/{mod,change_tick,change_tick_window,component_ticks,stats,wrappers}.rs`，root leaf owner 已由守卫锁定为 `scene/ecs/{bundle,removal,storage_type}.rs`；`ecs_kernel_data_boundary` 结构审计当前报告 `expected_source_file_count = 69`、`expected_test_file_count = 8`、`archetype_anchors = 15/15`、`storage_anchors = 9/9`、`component_storage_private_reexport_anchors = 9/9`、`unexpected_component_storage_private_reexports = []`、`component_identity_anchors = 18/18`、`entity_lifecycle_anchors = 10/10`、`observer_anchors = 8/8`、`deferred_command_anchors = 11/11`、`event_message_anchors = 12/12`、`resource_identity_anchors = 12/12`、`change_tick_anchors = 6/6`、`runtime_08_guard_anchors = 21/21`、`behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、`doc_anchors = 13/13`、`pending_cargo_gate_anchors = 6/6`、`mirror_docs_guard_present = true`、`risks = []`；2026-06-21 `ecs_kernel_data_markdown.py` 已承接 Markdown 渲染，`ecs_kernel_data_boundary.py` 保留 344 行审计入口，Markdown owner 为 154 行；`ecs_query_state_markdown.py` 已承接 QueryState boundary Markdown 渲染，`ecs_query_state_boundary.py` 保留 141 行审计入口，Markdown owner 为 33 行；`ecs_query_state_boundary` 当前报告 `expected_module_count = 9`、`root_non_empty_lines = 84/180`、`unexpected_modules = []`、`risks = []`，`query_state/cache.rs` 为 cache behavior owner；新增 `runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；新增 Runtime 08 ECS 数据面守卫 `runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`，Cargo 待活动 lanes 清空后运行 entity/observer/command/messages/change_tick/ecs filters） |
| 09 UI 子系统架构收束 | `09-ui-subsystem-architecture.md` | 05 debt 移交清单；editor UI 会话稳定；文本栈归 01-M2；12-M0.2 全局输入仲裁判词 | in_progress（2026-06-17 M0/M1.1/M1.2/M2.1/M2.2/M3.1 static passed：route authority、navigation rename、pointer rename、pointer capture fallback rename、table row label fallback rename、template component-name fallback rename、property visibility flag rename、responsive MUI visibility flag rename、accessibility open-state fallback rename、layout engine backend name cutover、surface default interaction fallback rename、Taffy bridge/pass order、virtualization scroll planner 与 template `load -> validate -> instance -> build` pipeline 已落地；`ui_architecture_boundary` 当前报告 `expected_source_file_count = 52`、`legacy_full_hits = 54`、`legacy_production_hits = 0`、`legacy_production_file_count = 0`、`guard_anchor_count = 19`、`doc_anchor_count = 61`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`risks = []`；`runtime_09_m1_2_pointer_capture_fallback_renamed_static_passed_cargo_pending` 记录 pointer capture fallback `has_pointer_capture_or_unindexed_fallback_for_owner` 命名收束；`runtime_09_m1_2_table_row_label_fallback_renamed_static_passed_cargo_pending` 记录 table row-label fallback `split_row_label_table_text` 命名收束；`runtime_09_m1_2_template_component_name_fallback_renamed_static_passed_cargo_pending` 记录 template interaction `component_name_interaction_fallback` 命名收束；`runtime_09_m1_2_property_visibility_flag_renamed_static_passed_cargo_pending` 记录 property mutation `state_visible_flag` 命名收束；`runtime_09_m1_2_responsive_mui_visibility_flag_renamed_static_passed_cargo_pending` 记录 responsive MUI `state_visible_flag` 命名收束；`runtime_09_m1_2_accessibility_open_state_fallback_renamed_static_passed_cargo_pending` 记录 accessibility open-state fallback `fallback_properties` 命名收束；`runtime_09_m1_2_layout_engine_backend_name_cutover_static_passed_cargo_pending` 记录 layout engine backend `UiLayoutEngineBackend::Zircon` / `UiLayoutEngineCapability::zircon()` / `zircon_selected_count` 命名收束；`runtime_09_m1_2_surface_default_interaction_fallback_renamed_static_passed_cargo_pending` 记录 surface default interaction `default_open_boolean_value` / `fallback_properties` 命名收束；`runtime_09_m3_1_template_compile_instance_validate_boundary_static_passed_cargo_pending` 记录 `UiTemplateRuntimePipeline`、`UI_TEMPLATE_RUNTIME_PIPELINE_STAGES`、`UiTemplateRuntimePipelineError` 与 binary DTO generated policy；`runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation` 继续锁定 `ui/input/naming_boundary/layout/template` owner/Cargo gate；完整 layout/template/ui behavior filters 按本轮要求后续补跑） |
| 10 dynamic_api 与 interface 收敛 | `10-dynamic-api-and-interface-convergence.md` | 06（native ABI 同口径互引）；09-M0（镜像重复清单移交） | in_progress（M0 ABI 清册/版本策略完成；M1.1 ABI-safe/清册/版本策略守卫已扩展并通过完整 `zircon_runtime_interface` 包测试 165/165；M1.2 headless/minimal 跳过 render bridge、live destroy 夹具与 `runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces` 静态守卫已落地，守卫覆盖 optional render bridge、capture 空帧 fallback 与 bind/unbind/present no-op，`destroy_session` Cargo 验证在活动编译通道下 904s 超时未声明通过；M1.3 FFI panic 边界已静态落地，`zircon_runtime_get_api_v1` 与 11 个 runtime 函数表入口经 `_ffi` wrapper 收口，panic 转 `ZrStatusCode::Panic` 或 null 表指针，`session.rs` owner 函数保持 Rust ABI，并有 `runtime_api_table_entries_are_panic_wrapped_at_ffi_boundary` 与 `runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge` 守卫，M1.3 rustfmt/锚点/差异检查通过；2026-06-20 host-request payload ABI boundary 已把 `drain_host_requests` payload owner chain 纳入 Runtime 10 Dynamic API 边界；`dynamic_runtime_api_boundary` 当前报告 `expected_source_file_count = 33`、`function_table_structs = 10/10`、`field_count_mismatches = 0`、`missing_repr_c_tables = 0`、`runtime_session_ffi_wrappers = 11/11`、`direct_session_table_entry_bypasses = 0`、`session_owner_extern_c_present = false`、`headless_lifecycle_anchors = 12/12`、`ffi_panic_anchors = 9/9`、`loader_failure_anchors = 10/10`、`behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、`runtime_diagnostics_anchors = 15/15`、`missing_runtime_diagnostics_anchors = []`、`host_request_payload_anchors = 38/38`、`missing_host_request_payload_anchors = []`、`ui_pending_gate_anchors = 8/8`、`ui_contract_single_source_anchors = 7/7`、`ui_contract_duplicate_public_types = 0`、`ui_v2_contract_sync_anchors = 9/9`、`pending_cargo_gate_anchors = 5/5`、`doc_anchors = 13/13`、`mirror_docs_guard_present = true`、`risks = []`，并由 `runtime_10_dynamic_runtime_api_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数；M2 UI 镜像契约 owner/Cargo gate 已由 `runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff` 锁定，`runtime_10_m2_1_ui_contract_duplicate_public_types_removed_static_passed_cargo_pending` 已完成 2.1 静态重复定义消化，`UiBindingCodec` 与 `UiAssetSchemaVersionPolicy` 仅由 interface 拥有，`ui_contract_duplicate_public_types = 0`；`runtime_10_m2_2_ui_v2_contract_sync_static_passed_cargo_pending` 已完成 2.2 静态 v2 契约同步，`UiComponentApiVersion` mismatch/parse-error 命名守卫已落地；interface package gate 已 168/168 通过，runtime ui/editor Cargo gates 仍 pending；M3.1 loader 失败路径含真实缺符号 fixture 的 scoped app Cargo 已通过，完整 app 包待测；dynamic_api/full app/UI contract Cargo 待空闲通道，2026-06-20） |
| 11 多线程 JobSystem 任务模型 | `11-job-system-task-model.md` | 02-M2 已完成（tasks 落位）；M2.4 与 04-M2 同窗口；物理客户随 01-M3 | in_progress（M0/M1/M2/M3 已按计划静态落地；`job_system_boundary` 报告 `expected_module_count = 9`、`direct_rayon_paths = 2`、`schedule_parallel_executor_direct_rayon = []`、`diagnostic_anchor_count = 4`、`behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true` 与 `risks = []`；2026-06-21 已拆分 JobSystem source/Rayon/anchor/Markdown owners，并由 `runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass` 锁定 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate；core-min 与默认配置 `tasks/ecs_schedule/worker_pool/rayon` 已全部通过；收尾全量 `cargo test -p zircon_runtime --lib --locked` 在 `runtime_11_full_lib_cargo_timeout_with_broader_failures_observed` 中 1200s 超时且观察到 broader failures，无最终 test result，2026-06-21） |
| 12 输入栈与动作映射 | `12-input-stack-and-action-mapping.md` | 清帧时机与 03 帧序图互引；09 owns UI internal route；gamepad 波及 zircon_app | in_progress（M0.1 输入链路与帧语义已静态落地：`docs/zircon_runtime/input/input_state.md` 增 "Frame Input Contract"，`zircon_runtime/src/input/tests/input_manager.rs` 增 `input_snapshot_just_pressed_is_true_for_exactly_one_frame` 与 `frame_input_clears_after_level_tick_not_before`；M0.2 仲裁判词已文档落地并与 09 互引：UI surface/capture/popup/focus 优先，玩法/action mapping 只消费 UI 未处理或无 UI/headless 输入；M1 最小动作映射、gamepad axis 连续值/transition/consumed-axis、InputConfig action-map 数据源与 action manager registration 已静态落地；M2.1 gamepad bridge 锚已静态落地；M4 input recording/replay helper 已静态落地；2026-06-20 补齐 cursor host request 低层请求契约：`CursorGrabMode`/`CursorPosition`/`CursorHostRequest`、`InputEvent::CursorHostRequest`、`InputFrameSnapshot::cursor_host_requests` 与 `InputManager::drain_cursor_host_requests()`，行为锚 `cursor_host_requests_are_frame_local_and_drainable`；`runtime_absorption::input_stack` 已锁定 Runtime 12 输入契约/runtime/tests 与 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts`，当前 `input_stack_boundary` 镜像事实为 `expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 15`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；2026-06-20 `cargo test -p zircon_runtime --lib input --no-default-features --features core-min ...` 1200s timeout no result，轻量守卫 py_compile/direct audit/standalone input_stack 4/4/rustfmt 通过；`runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation` 防止 input/action/gamepad/app 验证前误标完成；Cargo 待 active lane 清空后运行 input/action_map/gamepad/app filters） |
| 13 脚本绑定面与反射收束 | `13-script-binding-and-reflection.md` | 与 06 共享 script/vm 错峰；marshalling 引用 10-M0 清册；句柄语义与 08 对齐 | in_progress（M0.1/M0.2 静态完成：新增 `docs/zircon_runtime/script/vm/host/function_ledger.md`，固定 host ledger 记录 6 模块 / 52 函数 / 2 类型描述符，bridge 动态模块只锁形状；marshalling 裁决为 value descriptors / host handles / serialized payloads 三类；ECS 访问路径当前裁决为 `zr.zircon.gameplay` gameplay facade，native `ZrHostEcsApiV1` 保持 ABI 层；M1.1 清册守卫 `host_function_registry_matches_documented_ledger` 已落在 `runtime_absorption/script_host_ledger.rs`，含缺失函数负例自检；M1.2 capability 代表函数正向 descriptor 守卫与缺失授权拒绝测试已落地；M2.1 脚本持有实体 id despawn 后失效语义测试已落地；M2.2 脚本 ECS 访问路径 source guard 已落地；`script_binding_boundary.py` 保留审计/risk owner 于 351 行，`script_binding_markdown.py` 承接 Markdown owner 于 106 行；`script_binding_boundary` 当前报告 `expected_source_file_count = 18`、`expected_test_file_count = 3`、`fixed_host_module_count = 6`、`fixed_host_function_count = 52`、`type_descriptor_count = 2`、`builtin_callback_count = 11`、`gameplay_callback_count = 39`、`macro_host_function_count = 2`、`host_capability_count = 11`、`guard_anchor_count = 9`、`native_ecs_abi_references = []`、`oversized_test_files = []`、`mirror_docs_guard_present = true`、`risks = []`；新增 `runtime_13_script_binding_mirror_docs_match_structure_audit_counts` 锁定 Runtime 13 计划、总索引、M0 review、runtime-interface convergence 与 function ledger 镜像；`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass` 继续锁定 Cargo 待 active lane 清空，`script::vm` Cargo 48/48 passed；broader script filters Cargo pending，当前失败在 scene/vampire/UI 非本切片测试，2026-06-14） |
| 15 代码结构与模块规范 | `15-code-structure-and-module-conventions.md` | 横切（与 02 root 守卫 / 07 large-file gate 对齐） | in_progress（执行 `engine-code-structure-convention`：façade/可见性、命名前缀词表、`mod.rs`/`module.rs` 判据、测试组织单一化、行数硬上限；已落地 `Runtime 15 graphics facade visibility note`、`Runtime 15 F9 runtime prelude required type coverage`、`Runtime 15 runtime UI dead-code support split`、`Runtime 15 F12 runtime-owned dead-code suppression cleanup`、`Runtime 15 F12 script host value descriptor dead-code cleanup`、`Runtime 15 F12 offscreen target texture owner cleanup`、`Runtime 15 F12 render backend state owner cleanup`、`Runtime 15 F12 gpu texture resource owner cleanup`、`Runtime 15 F12 gpu material uniform owner cleanup`、`Runtime 15 F12 gpu mesh order signature cleanup`、`Runtime 15 F12 gpu model identity cleanup`、`Runtime 15 F12 post-process LUT texture owner cleanup`、`Runtime 15 F12 output target texture owner cleanup`、`Runtime 15 F12 material runtime capture seed cleanup`、`Runtime 15 F12 resource streamer diagnostics accessor cleanup`、`Runtime 15 F12 resource streamer resolve texture id cleanup`、`Runtime 15 F12 particle GPU readback output accessor cleanup`、`Runtime 15 F12 advanced plugin output test accessor cleanup`、`Runtime 15 M3 graphics dead-code guard module split`、`Runtime 15 M3 provider boilerplate guard module split`、`Runtime 15 F14 diagnostics normalization`、`Runtime 15 F13 provider registration shared owner`、`Runtime 15 F13 provider update shared stats owner`、`Runtime 15 F13 provider feedback shared payload owner`、`Runtime 15 F13 provider prepare input shared frame owner` 与 `Runtime 15 F13 full provider boilerplate audit`；新增/复用 `tests/runtime_absorption/structure_convention.rs`、`structure_convention/provider_boilerplate.rs`、`structure_convention/graphics_dead_code/mod.rs`、`structure_convention/graphics_dead_code/module_layout.rs`、`structure_convention/graphics_dead_code/renderer_output_accessors.rs`、状态输出期望行和镜像 `docs/zircon_runtime/structure/module-convention.md`；完整 `module_convention_gate`、全量 dead-code sweep 与测试组织拆分仍 pending） |

Runtime 07 owner-budget gate 镜像：`runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit` 已把 Runtime 07 的大文件优化前置门固定到当前 `large_file_ownership_gate` 审计事实：threshold 1000、hotspots 30、debt groups 5、owner classes 5、unclassified 0，并拒绝 stale 33/36/37/38/39/40/41/42-hotspot 与已删除 Hub `app/` 路径锚点。当前 owner classes 为 `editor-retained-host=3`, `editor-ui=8`, `runtime-framework-render=3`, `runtime-other=13`, and `support-hub=3`。该镜像只说明计划/文档/结构审计同步；animation、scene 资产、scene project_io、dynamic session event routing、artifact cache payload wire types、render product diagnostics、virtual geometry debug snapshot、gameplay host 与 navigation fallback runtime 大文件已拆分，其他大文件仍需 owner-budget gate，不解除 `extract/ecs_query/performance profiling/FPS` Cargo/profiling/FPS 待验证门。

Runtime 05 plan-status 镜像：`runtime_plan_status_boundary` 已接入总审计，报告 plan-status support files 32/32、runtime subplans 14/14、runtime index subplan rows 14/14、problem rows 17/17、known backlog rows 7/7、status counts `in_progress=14`、core guard anchors 15/15、pending Cargo gate anchors 15/15、doc anchors 12/12、status-output table guard anchors 4/4、`status_output_table_guard_present = true`、Runtime 07 scene status anchors 11/11 in runtime index、10/10 in status-output guards、4/4 in mirror docs、`runtime_07_scene_status_guard_present = true`、Runtime 07 owner-budget status anchors 7/7 in runtime index、6/6 in status-output guards、3/3 in mirror docs、`runtime_07_owner_budget_status_guard_present = true`、Runtime 02 generated status anchors 6/6 in runtime index、5/5 in status-output guards、3/3 in mirror docs、`runtime_02_generated_status_guard_present = true`、Runtime 05 closeout status `in_progress`、`plan_status_boundary_line_count = 454`、`risks = []`；`plan_status.rs` 已缩为结构挂载，`plan_status/support.rs`、`plan_status/index_tables.rs`、`plan_status/subplan_status.rs`、`plan_status/closeout.rs`、`plan_status/architecture_review.rs`、`status_output_tables.rs`、`status_output_tables/expected_status_row_data.rs`、`status_output_tables/expected_slices.rs` 与 `status_output_tables/expected_status_rows.rs` 已纳入 Rust support 文件集合，`runtime_plan_status_support_inventory.py` owns `PLAN_STATUS_SUPPORT_FILES` / `EXPECTED_PLAN_STATUS_SUPPORT_FILE_COUNT`，`runtime_plan_status_anchor_inventory.py` owns `CORE_GUARD_ANCHORS` / `PENDING_GATE_ANCHORS` / `DOC_ANCHORS` / `BACKLOG_GAPS`，`runtime_plan_status_markdown.py` owns `render_runtime_plan_status_boundary_markdown`，`runtime_plan_status_sources.py` owns `read_text` / `runtime_subplans` / `status_rows`，`expected_slices.rs` owns `expected_status_for_slice` / `expected_date_for_slice`，`expected_status_row_data.rs` owns `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS`，`expected_status_row_data/{runtime_01_04,runtime_05,runtime_06_09,runtime_10_13,runtime_14}.rs` own grouped evidence rows，`expected_status_rows.rs` owns `expected_status_output_slices`，`runtime_plan_status_output_tables_cover_index_and_all_subplans` 与 `runtime_index_status_output_records_recent_cross_plan_slices` 锁定总索引和子计划的 `## 状态与产出记录` 写回，且保留 all runtime index status rows full coverage guard、Runtime 03 module-doc status-output row、Runtime 07 scene asset owner-split / split-drift / public-surface / boundary-anchor rows、Runtime 07 virtual geometry debug snapshot owner split 行（含 `Runtime 07 owner-budget 36-hotspot navigation split sync` 历史证据锚）、Runtime 02 generated template count 审计同步行、Runtime 05 status-output expected anchor split 行、Runtime 05 plan-status root module split 行、Runtime 05 plan-status support inventory split 行、Runtime 05 plan-status anchor inventory split 行、Runtime 05 plan-status markdown renderer split 行、Runtime 05 plan-status source helper split 行、Runtime 05 status-output expected row data split 行、Runtime 05 status-output row-data group split 行、Runtime 05 cargo-gates early Runtime 04/06 split 行、Runtime 05 cargo-gates late Runtime 11/12/13/14 split 行与 Runtime 12 gamepad event-owner 漂移同步行。`virtual geometry debug snapshot owner split row` 是当前状态表元数据锚点。这只确认计划/索引/评审文档同步，最终 `completed` 仍等待 `pending_full_scene_cargo` 与 `cargo test -p zircon_runtime --lib scene:: --locked`。

Runtime 05 plan-status owner-budget current mirror（2026-06-20）：Runtime 07 owner-budget status metadata 当前锚点为 Runtime 07 owner-budget 30-hotspot current audit sync、large_file_hotspot_count = 30、runtime-other=13 与 editor-retained-host=3；历史 36/37/38/39/42-hotspot 行只保留为漂移记录。

Runtime 10 Dynamic API 行为锚状态表同步：`runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 10 Dynamic API 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、standalone dynamic_api_session 9/9 与 standalone status-output 2/2 证据；`Runtime 10 runtime diagnostics profile-control snapshot` 行额外固定 `runtime_diagnostics_anchors = 15/15` 与 `ProfileControlCommand::RuntimeDiagnosticsSnapshot`；`dynamic_api`、完整 app loader 与 UI contract owner/Cargo gates 仍 pending。

Runtime 02 guard-test anchors 状态表同步：`runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 02 guard-test anchors 审计同步` 行，要求保留 `guard_test_anchor_count = 26`、`missing_guard_test_anchors = []` 与 standalone core_spine_root_generated 1/1 / standalone plan-status 32/32 证据；`core/root/generated/export_build_plan/app/editor/plugin` Cargo gates 仍 pending，M3 root alias cutover 已记录为 `graphics_alias_block_removed_static_passed_cargo_pending`。

Runtime 05 non-network server 命名门禁复核：`non_network_server_naming` 已把 Hub UNC path fixture（`\\?\UNC\server\share\Game` / `\\server\share\Game`）和 runtime UI Table/DataGrid `sortingMode = "server"` 外部 API 字面值列为允许上下文；当前审计 count 77、classification groups 2、allowed contexts 94、observer false positives 95、unclassified 0，`non_network_server_naming_markdown.py` 已接管 Markdown renderer，剩余风险只是不应由 Runtime 05 跨会话处理的 graphics render-framework 与 editor workbench authority-label 已分类命名债。

Runtime 05 hard-cutover migration-smell 门禁复核：`hard_cutover_migration_smells` 当前审计 source_file_count 6145、legacy_reference_count 148、compat/shim 0、allowed business bridge 312、classification groups 5、hard_cutover_migration_debt_count 5、unclassified 0；DDS caps policy wording 已在 texture importer owner 内清掉，当前剩余 owner groups 为 hybrid GI render、runtime graphics、Hub archived text、Net hyper client API、editor UI fixture。该复核只同步审计归类和计划证据，剩余清理仍由 render/graphics/Hub/Net/editor owner 切片处理。

Runtime 02 core/root/generated 镜像：`core_spine_root_generated_boundary` 已接入总审计，聚合 core spine、root surface 与 generated/export 事实，报告 core root entries 6/6、core public modules 5/5、retired core root entries 0、runtime root public modules 19/19、public `pub use` sites 2/2、crate-visible graphics alias debt 0/0、root-surface M1 gate `classified-and-clear`、generated export templates 10/10、generated behavior 6/6、generated allowed adapters 6/6、generated migration debt 0/0、generated-code M1 gate `classified-and-clear`、root_entries guard tests 13、root_surface guard tests 6/6、generated-code guard tests 7/7、`guard_test_anchor_count = 26`、`missing_guard_test_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`；`core_spine_root_generated_markdown.py` 已独立承接 Markdown 输出，`core_spine_root_generated_boundary.py` 保持 315 行审计/风险 owner；`runtime_02_core_spine_root_generated_mirror_docs_match_structure_audit_counts` 锁定镜像文档计数，`root_surface_interface_convergence_mirror_uses_current_audit_counts` 额外锁定 interface convergence 的 19/0 root-surface 镜像和 `rhi_wgpu` crate-private backend owner，并拒绝旧 17/20/75/80 文本。这不关闭 Runtime 02，`core/root/generated/export_build_plan/app/editor/plugin` Cargo gates 仍待验证窗口。

Runtime 06 plugin surface/lifecycle 镜像：`plugin_surface_lifecycle_boundary` 已接入总审计，绑定 Runtime 06 子计划、总索引、native boundary、interface convergence 与 M0 review；`plugin_surface_lifecycle_markdown.py` 已拆出 Markdown renderer。当前报告 Runtime 06 source 14/14、doc 5/5、frontmatter `in_progress`、`last_refined = 2026-06-21`、`expected_source_file_count = 14`、`expected_doc_file_count = 5`、`plugin_surface_lifecycle_boundary.py = 450`、`plugin_surface_lifecycle_markdown.py = 144`、`root_reexport_count = 0`、`native_namespace_reexport_count = 60`、native root re-export 0/0、native namespace re-export 60/60、M4 gate `classified-and-clear`、native public-surface debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1、app NativePlugin current call-site files: 7、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、native loader test files 3/3、native test namespace import files 2/2、native test root import leaks 0/0、fallback lifecycle failure tests 4/4、unknown ABI rejection、hot reload failure injection、`mirror_docs_guard_present = true`、`risks = []`、source/doc/validation anchors 全满；`runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts`、`runtime_06_native_loader_tests_use_isolated_plugin_native_namespace` 与 `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed` 锁定镜像文档计数、测试命名空间隔离和 fallback 失败路径覆盖。这不关闭 Runtime 06，`script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gate 仍待执行。

Runtime 06 native plugin public-surface renderer 镜像：`native_plugin_public_surface_markdown.py` 已承接 `render_native_plugin_public_surface_markdown(...)`，`native_plugin_public_surface.py` 继续作为 400 行 public-surface scan / symbol classification / M4 gate owner；renderer owner 为 63 行。当前 direct probe 报告 `root_reexport_count = 0`、`native_namespace_reexport_count = 60`、`symbol_decision_group_count = 5`、`native_plugin_public_surface_migration_debt_count = 0`、`unclassified_root_reexport_symbol_count = 0`、`unclassified_native_namespace_symbol_count = 0`、`root_public_reexport_location_count = 0`、`public_reexport_location_count = 1`、M4 gate `classified-and-clear`、`risk_count = 0`、rendered output 12 lines。此切片只收束审计/Markdown owner 边界；Runtime 06 Cargo/native gate 仍由 `plugin_surface_lifecycle_gate` 保持 pending。

## 状态与产出记录

| 里程碑 | 切片 | 状态 | 完成日期 | 证据 |
|---|---|---|---|---|
| M0 | Runtime 07 render direct runtime-frame trace export | render_direct_runtime_frame_trace_export_static_passed_profile_timeout_fps_pending | 2026-06-22 | 状态锚 `render_direct_runtime_frame_trace_export_static_passed_profile_timeout_fps_pending`；`render_profiling.rs::direct_runtime_frame_submit_exports_perfetto_trace_artifacts` 在 `profiling-chrome` 构建下提交 direct `ViewportRenderFrame`、导出 `export_report()`，并断言 `timeline.zrtrace.json`、`timeline.perfetto.json`、`hotspots.json` 与 `summary.md` 生成。native trace 与 Perfetto trace 均要求包含 `submit_runtime_frame`、`render_frame_with_pipeline`、`DepthPrepass` 与 `depth-prepass`；`rustfmt --check`、`runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` 和 status-output expected rows 已通过并锁定测试名、Perfetto 文件锚和状态锚。core-min cargo 当前被 Runtime 06/plugin 侧 private-field 编译错误挡住，profiling-chrome 聚焦 cargo 10 分钟超时无测试结果；该行只登记 direct runtime-frame trace/export 子切片与静态证据，不关闭权威 vampire FPS、profiling-tracy 构建耗时和 full Runtime 07 gates。 |
| M2 | Runtime 07 render VG debug overlay frame override | render_vg_debug_overlay_frame_override_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_vg_debug_overlay_frame_override_coremin_check_passed_partial`；`ViewportRenderFrame` 新增 `runtime_overlay_override: Option<RenderOverlayExtract>`，`ViewportRenderFrame::overlays()` 现在先取 frame-local override，再回退到 shared extract overlays；`build_runtime_frame(...)` 通过 `runtime_virtual_geometry_debug_overlays(...)` 构造轻量 overlay packet 并用 `with_runtime_overlays(runtime_overlays)` 覆盖 runtime frame overlays，删除 production `Arc::try_unwrap(extract).unwrap_or_else(...)` 整帧 fallback clone。验证：scoped rustfmt、standalone hotspot/status-output guards 与 core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-f3-shared-extract-coremin` 通过（既有 warnings）；FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render direct runtime-frame shared context extract | render_direct_runtime_frame_shared_context_extract_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_direct_runtime_frame_shared_context_extract_coremin_check_passed_partial`；`build_frame_submission_context_from_runtime_frame_extract(...)` 新增 direct runtime-frame builder entry，`FrameSubmissionExtractSource::RuntimeFrame` 对 `&mut Arc<RenderFrameExtract>` 使用 `Arc::make_mut(extract)` 原地投影 effective extract；`submit_selected_runtime_frame(...)` 传入 `&mut frame.extract`，删除旧 direct context clone `frame.extract.as_ref().clone()`，随后继续用 `frame.extract = context.source_extract()` 与 renderer frame 共享 context effective payload。验证：scoped rustfmt、standalone hotspot/status-output guards 与 core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-f3-shared-extract-coremin` 通过（既有 warnings）；VG debug overlay fallback clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render shared effective extract frame source | render_shared_effective_extract_frame_source_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_shared_effective_extract_frame_source_coremin_check_passed_partial`；`ViewportRenderFrame.extract` 现在是 `Arc<RenderFrameExtract>`，`ViewportRenderFrame::from_shared_extract(...)` 与 `FrameSubmissionContext::source_extract()` 让 generated submit/present 的 runtime frame 和 submit context 共用同一份 effective extract；`build_frame_submission_context(...)` 删除旧 `let mut sized_extract = extract.clone();`，在 owned `effective_extract` 上投影 viewport size、previous particles、有效 HGI/VG、post-process settings、AA fallback、temporal jitter 与 post-process stack/graph 后 `Arc::new(effective_extract)`。direct runtime-frame submit 在 context 构建后以 `frame.extract = context.source_extract()` 使用同一份 effective payload。验证：scoped rustfmt 与 core-min `cargo check` 通过；`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 shared-frame 锚点。direct runtime-frame context clone 已由 `render_direct_runtime_frame_shared_context_extract_coremin_check_passed_partial` 移除；VG debug overlay fallback clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit source-extract sharing | render_submit_source_extract_shared_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_submit_source_extract_shared_coremin_check_passed_partial`；`FrameSubmissionContext` 持有 `source_extract: Arc<RenderFrameExtract>`，mesh/light/previous-particle accessors 借用共享 source extract；`build_frame_submission_context` 不再克隆 `extract.geometry.meshes`、五类 light Vec 或 `extract.particles.previous_sprites` 作为 context 字段，并把 visibility/post-process effective clone helper 收敛为 `effective_extract` 原地修改。新增 `runtime_07_submit_context_shares_large_extract_payloads` 守卫。验证：scoped rustfmt/check、旧 payload/effective helper clone 静态扫描、core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --target-dir D:\cargo-targets\zircon-f3-residual-0622` 通过（既有 warnings）；聚焦 `cargo test ... runtime_07_submit_context_shares_large_extract_payloads` 1200s 超时且不计通过，残留已清理；初始 viewport-sized clone、borrowed runtime-frame projection clone 与 Runtime 07 FPS/profiling/full gates 仍 pending。 |
| M2 | Runtime 07 render camera-loop descriptor submissions | render_camera_loop_descriptor_submissions_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_camera_loop_descriptor_submissions_coremin_check_passed_partial`；`camera_loop_submissions()` 只返回 `CameraLoopSubmission { camera: CameraRenderDescriptor, ... }`，不再构造 selected `RenderFrameExtract` 列表；`viewport_terminal_camera_target()` 直接从 descriptor 读取 target；`submit_camera_loop()` 保留源 extract 并把最后一个 camera 作为 move，前置 camera 才 clone。`runtime_07_submit_context_shares_large_extract_payloads` 守卫扩展覆盖 descriptor-only camera-loop 枚举。验证：scoped rustfmt/check、camera-loop 静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 过滤守卫 1/1、core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked` 通过（既有 warnings）；聚焦 `cargo test -p zircon_runtime --lib camera_loop` 异常退出无测试结果/无测试二进制；`camera_loop_frame_submissions()` borrowed frame projection clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render camera-loop frame terminal move | render_camera_loop_frame_terminal_move_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_camera_loop_frame_terminal_move_coremin_check_passed_partial`；direct runtime-frame submit 现在调用 `camera_loop_frame_submissions(frame)` 并把 source `ViewportRenderFrame` 所有权交给相机循环；`camera_loop_frame_submissions(...)` 保留 `source_frame: Option<ViewportRenderFrame>`，终端 child 通过 `source_frame.take()` 进入 `project_owned_frame_to_selected_camera(...)`，移动原 frame/extract/scene/UI/sidebands，删除旧 `project_frame_to_selected_camera(&frame, ...)` borrowed projection helper。`runtime_07_submit_context_shares_large_extract_payloads` 和 status-output 守卫新增该锚。验证：scoped rustfmt、静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2、core-min `cargo check` 通过（既有 warnings）；Cargo wrapper 聚焦 test 超时但生成二进制，direct exact `camera_loop_frame_submissions_project_selected_children_and_terminal_ui` 1/1 通过。非终端 child borrowed projection clone、初始 viewport-sized clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit feedback sideband owned merge | render_submit_feedback_sidebands_owned_merge_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_submit_feedback_sidebands_owned_merge_coremin_check_passed_partial`；`collect_runtime_feedback(...)` 现在接收 `&mut PreparedRuntimeSubmission`，通过 `take_hybrid_gi_readback_outputs()` / `take_particle_readback_outputs()` / `take_virtual_geometry_readback_outputs()` 消费 prepared sideband 回读输出，保留 `RenderVirtualGeometryReadbackOutputs` owned-merge 证据锚；HGI/Particle/VG merge helper 改为按值合并并移动 Vec 内容，不再在 renderer 为空或混合合并路径从 borrowed sideband `clone()`。`prepared_runtime_sidebands()` 仍在 render 前为 `ViewportRenderFrame` 保留快照，记录阶段继续消费可逐出 probe/page id。`runtime_07_submit_context_shares_large_extract_payloads` 扩展锁定 owned-merge 锚并拒绝 borrowed sideband clone 回流。验证：scoped rustfmt、owned-merge 静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2 与 core-min `cargo check` 通过（既有 142 warnings）；聚焦 lib-test `merge_` 未产出可用结果，exact merge filter 300s 超时且无 lib-test 二进制，残留本目标目录进程已清理。初始 viewport-sized clone、非终端 borrowed frame projection clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render prepared sideband frame owner move | render_prepared_sideband_frame_owner_move_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_prepared_sideband_frame_owner_move_coremin_check_passed_partial`；`PreparedRuntimeSubmission::into_prepared_runtime_sidebands(self)` 把 plugin renderer outputs 与可逐出 probe/page id 所有权直接移入 `ViewportRenderFrame`，`ViewportRenderFrame::prepared_runtime_sidebands_mut()` 暴露 frame-owned mutable sideband 给 `collect_runtime_feedback(...)` drain；feedback 现在通过 `take_hybrid_gi_evictable_probe_ids()` / `take_virtual_geometry_evictable_page_ids()` 附加 evictable id，record/present/camera-history 路径不再接收 `PreparedRuntimeSubmission`。非终端 `project_borrowed_frame_to_selected_camera(...)` 也不再克隆会被后续 child prepare 覆盖的 `frame.prepared_runtime_sidebands`。`runtime_07_submit_context_shares_large_extract_payloads` 锁定 `into_prepared_runtime_sidebands`、`prepared_runtime_sidebands_mut` 与 evictable take 锚，并拒绝 `prepared.prepared_runtime_sidebands()`、`plugin_renderer_outputs.clone()`、evictable id Vec clone 与 `frame.prepared_runtime_sidebands.clone()` 回流。验证：scoped rustfmt、old-clone 静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2 与 core-min `cargo check` 通过（既有 142 warnings）。初始 viewport-sized clone、非终端 borrowed frame extract/scene projection clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render direct runtime-frame streaming camera loop | render_direct_runtime_frame_streaming_camera_loop_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_direct_runtime_frame_streaming_camera_loop_coremin_check_passed_partial`；direct runtime-frame submit 现在通过 `submit_camera_loop_frame(...)` 流式提交一个 mutable `ViewportRenderFrame`，不再在 production `submit_runtime_frame.rs` 中调用 `camera_loop_frame_submissions(frame)` 生成整帧 projection 列表。`CameraLoopFrameSourceState` 在每个 child 前恢复会影响上下文的源字段，`RenderFrameExtract::select_camera_descriptor(...)` / `RenderViewExtract::select_camera_descriptor(...)` 原地切换 selected camera，terminal UI 只通过 `terminal_ui.take()` 移动一次；`submit_selected_runtime_frame(...)` 接收 `&mut ViewportRenderFrame`，prepared sideband 原地挂载并由 feedback drain。旧 frame-submission/projection helper 仅保留为 test-only 行为覆盖。验证：scoped rustfmt、production streaming 静态扫描、focused `submit_camera_loop_frame_streams_selected_children_and_restores_source_fields` lib-test 1/1、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2 与 core-min `cargo check` 通过（既有 warnings）。初始 viewport-sized clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit effective extract projection | render_submit_effective_extract_projection_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_submit_effective_extract_projection_coremin_check_passed_partial`；`build_frame_submission_context(...)` 在已拥有的 viewport-sized `effective_extract` 上应用有效 HGI/VG 与 post-process settings，再以 `Arc::new(effective_extract)` 交给 `FrameSubmissionContext`，不再通过 `post_process_extract_with_effective_settings(...)` / `visibility_extract_with_effective_advanced_features(...)` 克隆整帧 extract。验证：scoped rustfmt 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --target-dir D:\cargo-targets\zircon-f3-residual-0622` 通过（既有 143 warnings）；初始 viewport-sized clone、FPS/profiling/full gates 仍 pending。 |
| 横切 | Runtime 07 scene/EventBus poison-safe locks | scene_level_poison_recovery_coremin_passed_eventbus_guard_timeout | 2026-06-22 | 状态锚 `scene_level_poison_recovery_coremin_passed_eventbus_guard_timeout`；F2 scene/core lock unwrap 修复：`LevelSystem`、`DefaultLevelManager` 和 `EventBus` 的生产锁入口已收敛到 poison-safe helpers。验证：scoped rustfmt/check 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --target-dir D:\cargo-targets\zircon-f2-lock-poison-0622` 通过（既有 warnings）；`level_system_accessors_recover_poisoned_state_locks` 与 `level_system_recovers_world_lock_after_writer_panic` 各通过 1 test；EventBus root 结构守卫超时且不计通过，残留编译进程已清理。Runtime 07 extract/ecs_query/performance profiling/FPS gates 仍 pending。 |
| 横切 | Runtime 07 Performance hotpath Markdown renderer split | performance_hotpath_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `performance_hotpath_markdown_split_static_passed_cargo_deferred_tests_deferred`；`performance_hotpath_markdown.py` owns `render_performance_hotpath_boundary_markdown(...)`; `performance_hotpath_boundary.py` remains the 643-line audit/risk owner, and the Markdown owner is 139 lines. Direct audit reports source files 46/46, guard/test files 6/6, frame span anchors 9/9, QueryState telemetry anchors 32/32, change-detection telemetry anchors 13/13, extract telemetry anchors 21/21, asset-worker candidate telemetry anchors 13/13, animation scene telemetry anchors 19/19, profile counter hotspot export anchors 8/8, hotspot guard anchors 32/32, Runtime 07 counter assertion anchors 29/29, doc anchors 35/35, pending Cargo/profiling/FPS gate anchors 5/5, mirror-doc guard present, stale hotspot placeholder false, large-file gate `migration-debt-present`, hotspots 30, debt groups 5, owner classes 5, unclassified hotspots 0, missing anchors empty, and `risks = []`. Validation: Python py_compile, direct `performance_hotpath_boundary_audit`, standalone `performance_hotspots.rs`, and standalone `plan_status.rs`; package-level `extract` / `ecs_query` / profiling / FPS gates remain deferred. |
| 横切 | Runtime 07 Performance hotpath inventory split | performance_hotpath_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `performance_hotpath_inventory_split_static_passed_cargo_deferred_tests_deferred`；`performance_hotpath_source_inventory.py` owns Runtime 07 source/test file lists and expected counts at 70 lines; `performance_hotpath_anchor_inventory.py` owns frame/query/change/extract/asset-worker/animation/profile-counter/test/doc/Cargo anchor tuples at 244 lines; `performance_hotpath_boundary.py` is now the 353-line audit reader/missing-anchor/risk owner and `performance_hotpath_markdown.py` remains the 139-line renderer owner. Direct audit reports source files 46/46, guard/test files 6/6, all anchor groups full, large-file gate `migration-debt-present`, hotspots 30, unclassified hotspots 0, rendered lines 18, and `risks = []`. Validation: Python py_compile, direct `performance_hotpath_boundary_audit`, and direct renderer probe; package-level `extract` / `ecs_query` / profiling / FPS gates remain deferred. |
| 横切 | Runtime 05 dynamic scene patch preview resource preflight details status guard | dynamic_scene_patch_preview_resource_preflight_details_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 resource preflight details slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `ScenePatchPreviewResource`、`ScenePatchPreviewReport.resources`、`type_path` / `already_present` / `can_create_on_apply` / `field_count` DTO 字段、`resources_requiring_creation()` iterator、`preview.resources[0]` 行为锚和 `dynamic_scene_patch_preview_resource_preflight_details_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview resource ensure creation status guard | dynamic_scene_patch_preview_resource_ensure_creation_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 resource ensure creation slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 ensure-backed preview 行为锚 `register_frame_counter_resource_with_ensure`、`frame_counter_adapter_with_ensure`、`frame_counter_ensure`、`preview_with_ensure.resources[0].can_create_on_apply`、`target_with_ensure.get_resource::<FrameCounter>().is_none()`、`resources_requiring_creation()` 和 `dynamic_scene_patch_preview_resource_ensure_creation_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview status guard | dynamic_scene_patch_preview_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 patch preview API status guard 已补齐到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `ScenePatchPreviewReport` public facade、`ScenePatch::preview_apply` -> `DynamicScene::preview_spawn_into` route、`scene/spawn.rs` read-only preview helper、`scene_patch_preview_reports_remaps_without_mutating_target_world` 行为锚和 `dynamic_scene_patch_preview_status_guard_static_passed_cargo_pending` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview API | dynamic_scene_patch_preview_api_static_passed_cargo_timeout_no_result_tests_deferred | 2026-06-20 | `ScenePatchPreviewReport`、`ScenePatch::preview_apply` 与 `DynamicScene::preview_spawn_into` 已进入 Runtime 05 dynamic-scene apply surface，使 runtime hosts 能在 `ScenePatch` mutate 目标 `World` 前读取 component type/entity/resource/target entity/preserved entity/remapped entity counts。预览路径保留在 `scene/spawn.rs`，复用 `ensure_supported`、descriptor compatibility、entity remap planning 与 parent remap validation，但不调用 descriptor install、node insert、component write 或 resource write helper。行为锚 `scene_patch_preview_reports_remaps_without_mutating_target_world` 覆盖 remap diagnostics 与 target-world immutability；`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 public facade、只读 helper 调用链和文档/状态锚点。验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 32/32 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings；轻量 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-patch-preview-0620 --message-format short --color never` 历史尝试 304s timeout no result。Focused behavior tests 仍按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview component type install details status guard | dynamic_scene_patch_preview_component_type_install_details_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 component type install details slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `ScenePatchPreviewComponentType`、`ScenePatchPreviewReport.component_types`、`type_id` / `plugin_id` / `display_name` / `already_registered` DTO 字段、`new_component_types()` iterator、`preview.component_types[0]` 行为锚和 `dynamic_scene_patch_preview_component_type_install_details_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview component type install counts status guard | dynamic_scene_patch_preview_component_type_install_counts_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 component type install counts slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `ScenePatchPreviewReport.existing_component_type_count`、`new_component_type_count`、`has_new_component_types()`、`world.component_type_descriptor(...)` 只读 descriptor 计数、`preview.new_component_type_count` 行为锚和 `dynamic_scene_patch_preview_component_type_install_counts_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview reflection preflight status guard | dynamic_scene_patch_preview_reflection_preflight_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 reflection preflight slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `validate_components_are_previewable(...)`、`validate_resources_are_previewable(...)`、fixed component/resource runtime registration checks、adapter availability checks、`ReflectError::MissingResource`、read-only field remap conversion和 `dynamic_scene_patch_preview_reflection_preflight_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview component workload status guard | dynamic_scene_patch_preview_component_workload_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 component workload slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `ScenePatchPreviewReport.component_instance_count`、只读 `DynamicScene.entities[*].components` 计数投影、`preview.component_instance_count` 行为锚和 `dynamic_scene_patch_preview_component_workload_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene patch preview remap status guard | dynamic_scene_patch_preview_remap_status_guard_static_passed_cargo_pending | 2026-06-20 | Runtime 05 remap-details slice 已提升到总索引状态表和 status-output expectations：`runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 public `ScenePatchPreviewEntityRemap`、`source_entity` / `target_entity` 字段、`ScenePatchPreviewReport.entity_remaps`、`has_entity_remaps()`、只读 remap projection、`scene_patch_preview_reports_remaps_without_mutating_target_world` 行为锚和 `dynamic_scene_patch_preview_remap_details_static_passed_cargo_deferred_tests_deferred` 状态锚点。该行只补结构/状态守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 02 core/root/generated 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `runtime_02_core_spine_root_generated_mirror_docs_match_structure_audit_counts` 锁定 `core_spine_root_generated_boundary` 的 core root entries 6/6、core public modules 5/5、retired core root entries 0、runtime root public modules 19/19、public `pub use` sites 2/2、crate-visible graphics alias debt 0/0、root-surface M1 gate `classified-and-clear`、generated export templates 10/10、generated behavior 6/6、generated allowed adapters 6/6、generated migration debt 0/0、generated-code M1 gate `classified-and-clear`、root_entries guard tests 13、root_surface guard tests 6/6、generated-code guard tests 7/7、`guard_test_anchor_count = 26`、`missing_guard_test_anchors = []`、`mirror_docs_guard_present = true` 与 `risks = []`；Runtime 02、root_surface doc、generated boundary、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `core_spine_root_generated_boundary_audit`、standalone core_spine_root_generated 1/1、standalone plan-status 32/32；core/root/generated/export_build_plan/app/editor/plugin Cargo gates pending。 |
| M3 | Runtime 01 export build-plan directory materialization boundary | export_materialize_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/plugin/export_build_plan/materialize.rs` 已硬切到 `materialize/{mod,generated,paths,native,package_lookup,copy,report}.rs`；公开 `ExportBuildPlan::write_generated_files` / `materialize` / `materialize_with_native_packages` 与 `ExportMaterializeReport` shape 保持不变。`materialize/paths.rs` 在目录式生成文件写入前拒绝 empty、absolute/root/prefix、`.`、`..`、trailing separator 与 backslash 路径，先补 path traversal guard；本切片不引入 `zip` / `tar`，不声明 archive container materialization 完成。验证：rustfmt check、旧 flat file absent、conflict-marker scan、stale old-path scan、trailing-whitespace scan、path-scoped `git diff --check`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| M3 | Runtime 01 NativeDynamic materialization symlink boundary | export_materialize_symlink_boundary_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `materialize/package_lookup.rs` 现在只遍历真实目录、只读取真实 `plugin.toml`，防止 symlinked package root / manifest 成为 NativeDynamic source；`materialize/copy.rs` 跳过 symlinked package top-level payload、resource children 与 native artifact entries，顶层跳过项进入 materialization diagnostics。公开 API 与 report shape 不变；仍为 directory-first materialization，不引入 `zip` / `tar`。验证：rustfmt check、non-following helper/source scan、conflict-marker scan、trailing-whitespace scan、path-scoped `git diff --check`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| M3 | Runtime 01 export materialization dry-run preview | export_materialize_preview_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 新增 `ExportBuildPlan::preview_materialize(...)` 与 `preview_materialize_with_native_packages(...)`，复用 `ExportMaterializeReport` 返回 planned generated file paths、planned copied package directories、diagnostics 与 fatal diagnostics；preview 复用 generated path resolver、NativeDynamic package lookup、duplicate output-directory、native artifact 与 symlink diagnostics，但不创建目录、不写文件、不复制 payload、不写 package report。验证：rustfmt check、preview helper scan、write/copy call-site scan、conflict-marker scan、trailing-whitespace scan、path-scoped `git diff --check`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| M3 | Runtime 01 export materialization fatal preflight gate | export_materialize_fatal_gate_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `materialize/mod.rs` 现在在 mutating materialization 前检查 `effective_fatal_diagnostics()`；fatal plan 返回空 `generated_files` / `copied_packages`、保留 fatal diagnostics、追加 materialization-blocked diagnostic，并跳过 NativeDynamic package copy。直接 `write_generated_files(...)` 在 fatal plan 下 no-op，preview 保持 no-write planned path 语义。验证：rustfmt check、fatal gate source scan、write/copy call-site scan、conflict-marker scan、trailing-whitespace scan、path-scoped `git diff --check`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| M3 | Runtime 01 editor native-aware fatal export early exit | editor_native_aware_export_fatal_gate_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/manager.rs` 在 native-aware plan 生成后、`prepare-native-packages` 前检查 `plan.has_fatal_diagnostics()`；fatal plan 复用 runtime no-op `materialize(...)` report，写出 diagnostics，并返回空 generated/copied/native-cargo/source-cargo 结果，避免 fatal export 仍触发 NativeDynamic staging/build。验证：rustfmt check、fatal early-exit source scan、conflict-marker scan、trailing-whitespace scan、path-scoped `git diff --check`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| M3 | Runtime 01 editor native-aware discovery reuse | editor_native_aware_export_discovery_reuse_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `manifest_completion/native.rs` 新增 host-internal `complete_project_plugin_manifest_with_native_report(...)`，`export_build/manager.rs` 在执行 native-aware export 时复用初始 `NativePluginLoadReport` 生成 plan 并传给 package preparation，避免同一执行路径重复扫描 plugin directory。公开 `generate_native_aware_export_plan(...)` 与 project/plugin projection 行为不变。验证：rustfmt check、helper/discovery call-site scan、conflict-marker scan、trailing-whitespace scan、path-scoped `git diff --check`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| M3 | Runtime 01 export ZIP archive materialization | export_archive_zip_materialization_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `materialize/archive.rs` 新增 `ExportBuildPlan::materialize_zip_archive(...)` / `preview_zip_archive(...)`，把 generated files、NativeDynamic runtime payload 与 `native_dynamic_package.toml` 写入单个 ZIP；`ExportMaterializeReport.archive_file` 记录 archive 路径，directory materialization 保持 `None`。`zircon_runtime/Cargo.toml` 只为 runtime archive materializer 引入 `zip = { version = "9.0.0-pre2", default-features = false, features = ["deflate-flate2"] }`，`tar` 仍缺席。entry 名复用 `validated_materialized_relative_path(...)`，generated entries 排序，ZIP 时间戳/权限稳定，fatal plan 不创建 archive，preview 不落盘。测试锚新增 `native_dynamic_zip_archive_materialization_writes_generated_files_and_runtime_payloads`、`native_dynamic_zip_archive_preview_reports_archive_without_writes`；Cargo 与 focused behavior tests 按“先实现功能”方向暂缓。 |
| 横切 | Runtime 02 generated template count 审计同步 | structure_audit_static_passed_cargo_pending | 2026-06-14 | `source_template_build_plan.rs` 进入 `generated_code_boundary` 模板扫描范围后，Runtime 02 镜像计数同步为 `template_file_count=10` 与 generated export templates 10/10；行为扫描仍为 6 个 allowed adapter、0 migration debt、3 个 decision labels、`m1_gate_status=classified-and-clear`。`core_spine_root_generated_boundary` 与 `runtime_absorption::core_spine_root_generated` 已同步。验证：rustfmt check、Python py_compile、direct `core_spine_root_generated_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 02 assertions、standalone rustc 1/1、stale 9/9 scan、conflict/trailing scans 与 scoped `git diff --check` 通过（仅 LF-to-CRLF warnings）；包级 generated/export/app/editor/plugin Cargo gates pending。 |
| 横切 | Runtime 02 generated-code Markdown renderer split | generated_code_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `generated_code_markdown_split_static_passed_cargo_deferred_tests_deferred`；`generated_code_markdown.py` now owns `render_generated_code_boundary_markdown`; `generated_code_boundary.py` remains the 287-line generated-code audit and risk owner, with a 53-line Markdown owner. Direct audit reports `template_file_count = 10`, `behavior_location_count = 6`, `allowed_adapter_location_count = 6`, `migration_debt_location_count = 0`, `behavior_decision_count = 3`, `unclassified_behavior_label_count = 0`, `generated_boundary_migration_debt_count = 0`, M1 gate `classified-and-clear`, and `risks = []`. Validation passed: Python py_compile, direct `generated_code_boundary_audit`, rustfmt touched status files, standalone generated_code_guard 7/7, standalone plan_status 33/33; generated/export_build_plan/app/editor/plugin Cargo gates pending. |
| 横切 | Runtime 02 guard-test anchors 审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `core_spine_root_generated_boundary` 与 `runtime_02_core_spine_root_generated_mirror_docs_match_structure_audit_counts` 现在统一锁定 root_entries、root_surface 与 generated_code_guard 三组 Runtime 02 守卫锚，当前 `guard_test_anchor_count = 26`、`missing_guard_test_anchors = []`。Runtime 02、root_surface doc、generated boundary、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `core_spine_root_generated_boundary_audit`、standalone core_spine_root_generated 1/1、standalone plan-status 32/32；core/root/generated/export_build_plan/app/editor/plugin Cargo gates pending。 |
| 横切 | Runtime 02 root_entries guard-count current resync | mirror_docs_static_passed_cargo_pending | 2026-06-16 | `core_spine_root_generated_boundary.py` 的 `EXPECTED_ROOT_ENTRIES_TEST_COUNT` 已从旧 M2 baseline 8 提升到当前 root_entries 守卫数 13，并把 Runtime 14 root_entries 守卫锚纳入 Runtime 02 总锚清单：`runtime_navigation_boundary_file_set_requires_doc_update`、`runtime_animation_backlog_boundary_requires_doc_update`、`runtime_animation_status_json_boundary_sanitizes_non_finite_values`、`runtime_14_module_family_root_seats_match_documented_judgements`、`runtime_14_module_family_mirror_docs_match_structure_audit_counts`。Rust 镜像守卫与文档锚同步为 `root_entries guard tests 13/13`、`guard_test_anchor_count = 26`、`missing_guard_test_anchors = []`。验证：rustfmt check、Python py_compile、stale 21/baseline scan clean、standalone core_spine_root_generated 1/1、standalone plan-status 32/32。 |
| 横切 | Runtime 02 core/root/generated current audit recheck | core_root_generated_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `core_root_generated_current_audit_static_passed_cargo_pending`；本轮仅复核 Runtime 02 当前结构事实，不改生产代码；`core_spine_root_generated_boundary_audit` 报告 core root entries 6/6、core public modules 5/5、retired core root entries 0、runtime root public modules 19/19、public `pub use` sites 2/2、crate-visible graphics alias debt 0/0、root-surface M1 gate `classified-and-clear`、generated export templates 10/10、generated behavior 6/6、generated allowed adapters 6/6、generated migration debt 0/0、generated-code M1 gate `classified-and-clear`、root_entries guard tests 13/13、root_surface guard tests 6/6、generated-code guard tests 7/7、`guard_test_anchor_count = 26`、`missing_guard_test_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`。验证通过：Python py_compile、direct `core_spine_root_generated_boundary_audit`、standalone `core_spine_root_generated.rs` 1/1、standalone `root_entries.rs` 13/13、standalone `root_surface.rs` 6/6、standalone `generated_code_guard.rs` 7/7；core/root/generated/export_build_plan/app/editor/plugin Cargo gates pending。 |
| 横切 | Runtime 02 core/root/generated Markdown renderer split | core_root_generated_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `core_root_generated_markdown_split_static_passed_cargo_deferred_tests_deferred`；`core_spine_root_generated_markdown.py` now owns `render_core_spine_root_generated_boundary_markdown`; `core_spine_root_generated_boundary.py` remains the 315-line core/root/generated audit and risk owner, with a 60-line Markdown owner. Direct audit reports core root entries 6/6, core public modules 5/5, runtime root public modules 19/19, public `pub use` sites 2/2, crate-visible graphics alias debt 0/0, generated export templates 10/10, generated behavior 6/6, generated allowed adapters 6/6, generated migration debt 0/0, root_entries/root_surface/generated guard tests 13/13, 6/6, 7/7, `guard_test_anchor_count = 26`, missing anchors [], and `risks = []`. Validation passed: Python py_compile, direct `core_spine_root_generated_boundary_audit`, rustfmt touched status files, standalone core_spine_root_generated 1/1, standalone plan_status 33/33; core/root/generated/export_build_plan/app/editor/plugin Cargo gates pending. |
| 横切 | Runtime 02 F6 core resource registry typed errors | core_resource_registry_typed_errors_coremin_check_passed | 2026-06-22 | 状态锚 `core_resource_registry_typed_errors_coremin_check_passed`；按 F6/E1/E2 将 `core::resource` registry rename 错误合同收束到 `CoreError`：新增 `CoreError::MissingResourceRecordForLocator` / `MissingResourceRecordForId` 与 `CoreResult` root facade，`ResourceRegistry::rename(...)`、`ResourceManager::rename(...)` 不再返回 `Result<_, String>` 或 `Err(format!())`，缺失源 locator/id 保持 typed error。新增 `registry_rename_reports_missing_locator_with_core_error` 和 `review_f6_core_resource_registry_rename_uses_core_error`，模块文档记录 registry typed-error 合同。验证：scoped rustfmt、F6 结构守卫、status-output 守卫、focused behavior 与 core-min `cargo check` 通过；Runtime 02 broader core/root/generated/export_build_plan/app/editor/plugin Cargo gates 仍 pending。 |
| M3 | Runtime 02 root graphics alias block removal | graphics_alias_block_removed_static_passed_cargo_pending | 2026-06-17 | `graphics_alias_block_removed_static_passed_cargo_pending`：`zircon_runtime/src/lib.rs` 已删除 `pub(crate) use graphics::...` alias block 与 `#[allow(unused_imports)]`；扫描未发现 `crate::GraphicsError`、`zircon_runtime::SceneRenderer` 或 root 花括号导入调用方，未改 active render/graphics 生产文件。`runtime_root_surface` / `core_spine_root_generated_boundary` 当前同步 `crate_visible_graphics_reexport_count = 0`、`crate-visible graphics alias debt 0/0`，`root_surface.md`、Runtime 02、M0 review 与 runtime-interface convergence 已同步。验证通过：rustfmt check、Python py_compile、direct `runtime_root_surface_audit` / `core_spine_root_generated_boundary_audit`、standalone `root_surface` 6/6、standalone `core_spine_root_generated` 1/1、standalone `plan_status` 32/32、scoped `git diff --check`（仅 LF-to-CRLF warnings）；包级 core/root/generated/export_build_plan/app/editor/plugin Cargo gates 仍 pending。 |
| M3 | Runtime 02 rhi_wgpu root backend private cutover | rhi_wgpu_root_backend_private_static_passed_cargo_pending | 2026-06-17 | `rhi_wgpu_root_backend_private_static_passed_cargo_pending`：`zircon_runtime/src/lib.rs` now declares `mod rhi_wgpu;` as an internal backend module; external scan found no `zircon_runtime::rhi_wgpu` users, with only internal `crate::rhi_wgpu::WgpuUiSurfacePresenter` still used from `rhi/mod.rs`. At that slice, Runtime 02 mirrors reported runtime root public modules 19/19, `public_module_count = 19`, `module_decision_count = 19`, `root_surface_migration_debt_count = 1`, backend public debt removed, `rhi_wgpu` crate-private backend owner, and `risks = []`; the remaining migration debt was the broad builtin root facade removed by the next M3.5 row. 验证：rustfmt check、Python py_compile、direct `runtime_root_surface_audit` / `core_spine_root_generated_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 02/plan-status summary、standalone `root_surface` 6/6、standalone `core_spine_root_generated` 1/1、standalone `plan_status` 32/32、stale root-surface anchor scan、scoped `git diff --check`（仅 LF-to-CRLF warnings）；core/root/generated/export_build_plan/app/editor/plugin Cargo gates pending。 |
| M3 | Runtime 02 builtin root facade cutover | builtin_root_facade_removed_static_passed_cargo_pending | 2026-06-17 | `builtin_root_facade_removed_static_passed_cargo_pending`：`zircon_runtime/src/lib.rs` no longer re-exports `builtin::{RuntimeModuleLoadReport, RuntimePluginId, RuntimeRequiredPluginMissing, RuntimeTargetMode}` at the crate root; runtime internals, app/editor call sites, retained host/export tests, `zircon_plugins` callers, generated export templates, and prelude tests now use `crate::builtin::{...}` or `zircon_runtime::builtin::{...}`. Direct scan found no remaining `zircon_runtime::Runtime*`, grouped `zircon_runtime::{Runtime*}`, `crate::Runtime*`, or grouped `crate::{Runtime*}` root-facade users; no builtin helper type names remain in `zircon_runtime/src/lib.rs` or `zircon_runtime/src/prelude.rs`. Runtime 02 mirrors now report public `pub use` sites 2/2, `root_surface_migration_debt_count = 0`, root-surface M1 gate `classified-and-clear`, `namespace-entry`: `builtin`, `plugin`, and `risks = []`。验证：rustfmt check、Python py_compile、direct `runtime_root_surface_audit` / `core_spine_root_generated_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 02/plan-status summary、standalone `root_surface` 6/6、standalone `core_spine_root_generated` 1/1、standalone `plan_status` 32/32、stale root-surface anchor scan、scoped `git diff --check`（仅 LF-to-CRLF warnings）；core/root/generated/export_build_plan/app/editor/plugin Cargo gates pending。 |
| 横切 | Runtime 03 Schedule/frame-loop 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 锁定 `schedule_frame_loop_boundary` 的 source files 18/18、guard/test files 8/8、`SystemStage` count and variants 9/9、fixed-loop stages 3/3、dynamic-session `.tick_time(...)` calls 1/1、Runtime 03 guard anchors 14/14、`behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、`doc_anchors = 10/10`、no `WorldDriver` second `advance_time_by(...)` references、no dynamic-session raw-delta level tick references 与 `risks = []`；Runtime 03、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `schedule_frame_loop_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 03 assertions、standalone rustc 1/1、conflict/trailing scans 通过；ecs_schedule/time/session/schedule_parallel Cargo gates pending。 |
| 横切 | Runtime 03 Schedule/frame-loop module-doc 镜像元数据 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `schedule_frame_loop_boundary` 新增 `mirror_docs_guard_present = true` 与 frame schedule module-doc anchors 3/3；`runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 现在同时锁定 `docs/zircon_runtime/core/frame_schedule.md` 的 guard/test files 8/8、Runtime 03 guard anchors 14/14 和镜像守卫名，防止 module doc 回退到旧 7/7、13/13 数字；ecs_schedule/time/session/schedule_parallel Cargo gates pending。 |
| 横切 | Runtime 03 Schedule/frame-loop 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `schedule_frame_loop_boundary` 与 `runtime_03_schedule_frame_loop_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 03 M1/M2/M3 的 13 个调度/帧循环行为测试锚，当前 `behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、Runtime 03 guard anchors 14/14 与 `doc_anchors = 10/10`；Runtime 03、`docs/zircon_runtime/core/frame_schedule.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `schedule_frame_loop_boundary_audit`、aggregate Runtime 03 + plan-status assertions、standalone schedule_frame_loop 1/1、standalone status-output 2/2；ecs_schedule/time/session/schedule_parallel Cargo gates pending。 |
| 横切 | Runtime 03 world bootstrap fixed-loop stage guard sync | guard_sync_static_passed_cargo_pending | 2026-06-15 | `world_bootstraps_with_renderable_defaults` 已同步到 `SystemStage::ORDER` 的 9 阶段期望，明确包含 `FixedFirst` 与 `FixedPostUpdate`；该行记录 full `scene::` closeout 的 world_basics stale guard 修复，生产调度代码未改。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` support 27/27 risks=[]、wrapped standalone plan-status 30/30、conflict/trailing scans 与 scoped diff check 通过（仅 LF/CRLF warnings）；Cargo 包级复验因 active editor/export lanes 暂缓。 |
| 横切 | Runtime 03 Schedule/frame-loop current audit recheck | schedule_frame_loop_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `schedule_frame_loop_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 03 当前调度/帧循环结构事实，生产代码未改。`schedule_frame_loop_boundary_audit` 报告 source files 18/18、guard/test files 8/8、`SystemStage` count and variants 9/9、fixed-loop stages 3/3、dynamic-session `.tick_time(...)` calls 1/1、Runtime 03 guard anchors 14/14、`behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、`doc_anchors = 10/10`、frame schedule module-doc anchors 3/3、`mirror_docs_guard_present = true`、no `WorldDriver` second `advance_time_by(...)` references、no dynamic-session raw-delta level tick references、`risks = []`。验证通过：Python py_compile、direct `schedule_frame_loop_boundary_audit` risks=[]、standalone `schedule_frame_loop.rs` 1/1、standalone `plan_status.rs` 32/32；ecs_schedule/time/session/schedule_parallel Cargo gates pending。 |
| 横切 | Runtime 03 Schedule/frame-loop inventory split | schedule_frame_loop_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `schedule_frame_loop_inventory_split_static_passed_cargo_deferred_tests_deferred`；`schedule_frame_loop_source_inventory.py` owns Runtime 03 source/guard file inventory plus stage/fixed-loop/tick-count scans; `schedule_frame_loop_anchor_inventory.py` owns SystemStage, RuntimeTimeAdvance, FixedStepPlan, UI extract, stage ordering, schedule runner, parallel executor, behavior-test, mirror-doc, and Cargo gate anchors; `schedule_frame_loop_boundary.py` remains the audit reader / missing-anchor / risk / Markdown layer at 475 lines. Direct audit reports source files 18/18, guard/test files 8/8, SystemStage count and variants 9/9, fixed-loop stages 3/3, dynamic-session `.tick_time(...)` calls 1/1, Runtime 03 guard anchors 14/14, `behavior_test_anchor_count = 13`, `missing_behavior_test_anchors = []`, `doc_anchors = 10/10`, frame schedule module-doc anchors 3/3, `mirror_docs_guard_present = true`, no duplicate world-driver advance, no raw-delta dynamic-session level tick, and `risks = []`. Validation: Python py_compile, direct `schedule_frame_loop_boundary_audit` risks=[], standalone `schedule_frame_loop.rs` 1/1, standalone `plan_status.rs` 33/33; `ecs_schedule/time/session/schedule_parallel` Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 03 Schedule/frame-loop markdown renderer split | schedule_frame_loop_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `schedule_frame_loop_markdown_split_static_passed_cargo_deferred_tests_deferred`；`schedule_frame_loop_markdown.py` now owns `render_schedule_frame_loop_boundary_markdown`; `schedule_frame_loop_boundary.py` now owns only audit read/missing-anchor/risk aggregation at 368 lines, while the Markdown owner is 146 lines. Direct audit reports source files 18/18, guard/test files 8/8, SystemStage count and variants 9/9, fixed-loop stages 3/3, dynamic-session `.tick_time(...)` calls 1/1, Runtime 03 guard anchors 14/14, `behavior_test_anchor_count = 13`, `missing_behavior_test_anchors = []`, `doc_anchors = 10/10`, frame schedule module-doc anchors 3/3, `mirror_docs_guard_present = true`, no duplicate world-driver advance, no raw-delta dynamic-session level tick, and `risks = []`. Validation: Python py_compile, direct `schedule_frame_loop_boundary_audit` risks=[], standalone `schedule_frame_loop.rs` 1/1, standalone `plan_status.rs` 33/33; `ecs_schedule/time/session/schedule_parallel` Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 05 status-output Runtime 03 module-doc row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 03 Schedule/frame-loop module-doc 镜像元数据` 行，要求保留 frame schedule module-doc anchors 3/3、guard/test files 8/8、Runtime 03 guard anchors 14/14 与 `ecs_schedule/time/session/schedule_parallel Cargo gates pending`；`runtime_plan_status_boundary` 同步 Runtime 03 module-doc status anchors 6/6 in runtime index、5/5 in status-output guards、3/3 in mirror docs。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、aggregate `audit_runtime_structure.py --json` plan-status assertions、standalone status-output 2/2；Runtime 03 ecs_schedule/time/session/schedule_parallel Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 03 behavior-test row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 03 Schedule/frame-loop 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、standalone schedule_frame_loop 1/1 与 standalone status-output 2/2 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；Runtime 03 ecs_schedule/time/session/schedule_parallel Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output all-index-row coverage guard | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在用同一份期望清单反向校验 all runtime index status rows，新增 full coverage guard，确保 runtime 总索引 `## 状态与产出记录` 未来新增行时必须同步加入状态表守卫；`runtime_plan_status_boundary` 同步 status-output table guard anchors 4/4 与 `status_output_table_guard_present = true`。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 12 Input stack 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 已锁定 `input_stack_boundary` 的 runtime/framework/test owner、public surface、guard anchor、gamepad ABI anchors、pending gate 与镜像文档计数；`docs/plans/zircon_runtime/runtime/12-input-stack-and-action-mapping.md`、`docs/zircon_runtime/input/input_state.md`、M0 review、runtime-interface convergence 与本总索引已同步，当前包含 `missing_gamepad_abi_anchors = []`。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、standalone rustc 4/4、stale count scan、scoped diff/conflict checks 通过；Cargo input/action_map/gamepad/app gates pending。 |
| 横切 | Runtime 12 gamepad event-owner 漂移同步 | input_boundary_static_passed_cargo_pending | 2026-06-15 | Dynamic Session Event Split 后，`session/events.rs` 成为 gamepad reducer owner；Runtime 12 的结构审计、Rust source guard、子计划、输入模块文档、M0 review、runtime-interface convergence 与本索引已同步到 app gilrs → runtime ABI → `session/events.rs` → `InputEvent::Gamepad*`，并记录 `missing_gamepad_abi_anchors = []`。验证：Python py_compile 通过；direct `input_stack_boundary_audit` 报告 `risks = []`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_cargo_gate_anchors = []`；aggregate audit 报告 Runtime 12 与 plan-status risks 均为空；rustfmt check 通过；standalone `input_stack.rs` rustc 4/4 passed；conflict/trailing/diff checks clean（仅 LF-to-CRLF warning）；Cargo input/action_map/gamepad/app gates pending。 |
| 横切 | Runtime 12 Input stack 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `input_stack_boundary` 与 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 12 M0/M1/M2 的 12 个输入行为测试锚，当前 `behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`；Runtime 12、`docs/zircon_runtime/input/input_state.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、aggregate Runtime 12 + plan-status assertions、standalone input_stack 4/4、standalone status-output 2/2；input/action_map/gamepad/app Cargo gates pending。 |
| 横切 | Runtime 12 action context routing | action_context_static_passed_cargo_pending | 2026-06-17 | 新增 `InputActionContext` 与 `InputActionMap.contexts`，`InputActionEvaluator::evaluate_with_active_contexts(...)` / `evaluate_with_active_contexts_and_consumed_buttons(...)` 可按 gameplay/menu active context 过滤 action，同时保留无 context global action 与 UI consumed button 仲裁。新增行为锚 `action_contexts_filter_gameplay_and_menu_maps_without_rebinding`；`input_stack_boundary` 当前报告 `expected_runtime_module_count = 10`、`expected_framework_module_count = 18`、`expected_test_module_count = 5`、`public_surface_anchors = 11/11`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 7`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；input/action_map/gamepad/app Cargo gates pending。 |
| 横切 | Runtime 12 gamepad bridge source guard event-owner sync | gamepad_bridge_source_guard_static_passed_cargo_timeout | 2026-06-17 | `zircon_runtime/src/input/tests/gamepad_bridge.rs` 的 `gamepad_host_bridge_uses_runtime_gamepad_abi_constructors` 现在同时读取 `session.rs` 与 `session/events.rs`，锁定拆分后的 gamepad reducer owner 仍归约为 `InputEvent::Gamepad*`，并保留 app gilrs → `ZrRuntimeEventV1::{gamepad_connection_with_ids,gamepad_button,gamepad_axis}` 的桥接锚。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit` (`expected_runtime_module_count = 10`, `expected_framework_module_count = 18`, `expected_test_module_count = 5`, `public_surface_anchor_count = 11`, `guard_anchor_count = 5`, `behavior_test_anchor_count = 7`, `missing_gamepad_abi_anchors = []`, `risks = []`)、standalone `input_stack.rs` 4/4 passed；focused Cargo `gamepad_host_bridge_uses_runtime_gamepad_abi_constructors` 605s timeout no result，残留 cargo/rustc/rustdoc 扫描为空；input/action_map/gamepad/app gates 仍 pending。 |
| 横切 | Runtime 12 action axis value bindings | action_axis_value_static_passed_cargo_deferred | 2026-06-17 | `InputAxisBinding` / `InputAxisDirection` 已纳入 `core/framework/input/input_binding.rs`，`InputBinding` 可保存 gamepad axis 或 button-gated axis；`InputActionState::value(...)` 与 `valued_actions(...)` 公开连续 action 值，`InputActionEvaluator` 通过 `binding_axis_value` / `dominant_action_value` 从 `InputFrameSnapshot::gamepad_axes` 读取 `Full`/`Positive`/`Negative` 值，并继续保留 context 和 UI-consumed button 过滤。新增行为锚 `gamepad_axis_binding_reports_continuous_action_value`；`input_stack_boundary` 当前锚点同步为 `public_surface_anchors = 13/13`、`behavior_test_anchor_count = 8`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32 与 direct `runtime_plan_status_boundary_audit` passed。Focused Cargo `cargo test -p zircon_runtime --lib gamepad_axis_binding_reports_continuous_action_value --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-axis-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | Runtime 12 action map config source | action_config_static_passed_cargo_deferred | 2026-06-17 | `InputConfig` 现在序列化 `enabled` 与 `InputActionMap`，保留旧默认 disabled 语义，并通过 `InputConfig::action_evaluator()` 从配置构造 `InputActionEvaluator`；新增行为锚 `input_config_builds_action_evaluator_from_serialized_action_map` 覆盖 JSON roundtrip、config-driven evaluator、重绑定与 active context 求值。`input_stack_boundary` 当前锚点同步为 `public_surface_anchors = 14/14`、`behavior_test_anchor_count = 9`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32 与 direct `runtime_plan_status_boundary_audit` passed。Focused Cargo `cargo test -p zircon_runtime --lib input_config_builds_action_evaluator_from_serialized_action_map --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-config-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | Runtime 12 action manager registration path | action_manager_registration_static_passed_cargo_deferred | 2026-06-17 | `InputActionManager` / `DefaultInputActionManager` 将 action mapping 接入 runtime manager/resolver 注册路径，`InputModule.Manager.InputActionManager` 由 `module_descriptor_with_config(InputConfig)` 注册，`InputActionManagerHandle` 与 `resolve_input_action_manager(...)` 提供 `CoreRuntime` 解析入口；新增行为锚 `input_action_manager_resolves_from_runtime_module_descriptor` 覆盖配置驱动求值和运行时重绑定。`input_stack_boundary` 当前锚点同步为 `expected_runtime_module_count = 11`、`expected_framework_module_count = 19`、`expected_test_module_count = 5`、`public_surface_anchors = 17/17`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 10`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32 与 direct `runtime_plan_status_boundary_audit` passed。Focused Cargo `cargo test -p zircon_runtime --lib input_action_manager_resolves_from_runtime_module_descriptor --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-action-manager-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | Runtime 12 gamepad axis transition edges | action_axis_transition_static_passed_cargo_deferred | 2026-06-17 | `GamepadAxisTransition` 已加入公共输入协议，`InputFrameSnapshot::gamepad_axis_transitions` 记录本帧处理后的轴变化；`DefaultInputManager` 在轴值跨过死区或手柄断开时写入 transition，并把同一帧多次轴事件折叠为“帧起始值 -> 帧最终值”。`InputActionEvaluator::binding_axis_transition` 让 axis-only action 在轴从 0 到非 0 时报告 `just_activated`，从非 0 到 0 或 disconnect 时报告 `just_deactivated`；button-gated axis 保留 UI-consumed button 与 active context 过滤。新增独立测试 owner `input/tests/action_axis_transitions.rs`，行为锚 `gamepad_axis_action_reports_deadzone_transition_edges` 与 `gamepad_disconnect_deactivates_axis_action`。`input_stack_boundary` 当前同步为 `expected_runtime_module_count = 11`、`expected_framework_module_count = 19`、`expected_test_module_count = 6`、`public_surface_anchors = 19/19`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32 与 direct `runtime_plan_status_boundary_audit` passed。Focused Cargo `cargo test -p zircon_runtime --lib gamepad_axis_action_reports_deadzone_transition_edges --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-axis-transition-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | Runtime 12 consumed gamepad axis arbitration | action_axis_consumption_static_passed_cargo_deferred | 2026-06-17 | `GamepadAxisInput` 已加入公共输入协议，表示 UI 09 已消费的物理 gamepad axis；`InputActionEvaluator::evaluate_with_consumed_input(...)` 与 `evaluate_with_active_contexts_and_consumed_input(...)` 现在通过 `binding_axis_consumed(...)` 在 `binding_axis_value(...)` / `binding_axis_transition(...)` 前过滤 consumed axis，防止 UI 菜单/焦点已消费的摇杆继续激活 gameplay action。`InputActionManager` 与 `DefaultInputActionManager` 同步暴露 consumed-input manager 方法；新增行为锚 `consumed_gamepad_axis_does_not_activate_gameplay_action`，并修正 axis-only action 在 M1.7 后应报告 `just_activated` 的测试断言。`input_stack_boundary` 当前同步为 `public_surface_anchors = 19/19`、`behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`risks = []`。Focused Cargo `cargo test -p zircon_runtime --lib consumed_gamepad_axis_does_not_activate_gameplay_action --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-axis-consumption-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | Runtime 12 input recording/replay | input_recording_replay_static_passed_cargo_deferred | 2026-06-17 | `InputRecording`、`InputRecordingFrame`、`InputReplayCursor`、`InputReplayFrameReport` 已加入 `zircon_runtime::input` 与 prelude，`recording.rs` 复用 `InputManager::drain_event_records()` 捕获每帧 `InputEventRecord`，回放侧通过 `begin_frame()` + `submit_event(...)` 恢复同帧 `InputFrameSnapshot`，不绕过 `DefaultInputManager` 状态机。新增测试 owner `input/tests/recording_replay.rs`，行为锚 `input_recording_captures_drainable_event_records_by_frame` 与 `input_replay_restores_frame_snapshots_in_recorded_order`。`input_stack_boundary` 当前同步为 `expected_runtime_module_count = 12`、`expected_framework_module_count = 19`、`expected_test_module_count = 7`、`public_surface_anchors = 23/23`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 14`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt、Python py_compile、direct `input_stack_boundary_audit` risks=[]、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32、aggregate `audit_runtime_structure.py --json` Runtime 12/plan-status risks=[]；`cargo check -p zircon_runtime --lib --no-default-features --features core-min ...` 180s timeout no result；进程扫描仅发现其他会话 `zircon_editor --lib` 编译通道；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | Runtime 08 ECS 数据面镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 已锁定 `ecs_kernel_data_boundary` 的 source/test file count、storage/entity/observer/command/event-message/change-tick anchors、pending Cargo gate 与镜像文档计数；`docs/plans/zircon_runtime/runtime/08-ecs-kernel-data-alignment.md`、`docs/zircon_runtime/scene/ecs.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `ecs_kernel_data_boundary_audit`、standalone rustc 1/1、stale count scan、scoped diff/conflict checks 通过；entity/observer/command/messages/change_tick/ecs Cargo gates pending。 |
| 横切 | Runtime 08 First-stage event update guard | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `ecs_kernel_data_boundary` 与 `runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 现在锁定 `first_stage_updates_all_registered_event_channels`，要求 Runtime 08 的 events/messages 双通道裁决继续保留 `EventStore::update_all()` 在 `SystemStage::First` 统一推进所有已注册 event channels；当前审计同步 `event_message_anchors = 12/12`、`runtime_08_guard_anchors = 21/21`、`missing_event_message_anchors = []`、`missing_test_anchors = []` 与 `risks = []`。验证：rustfmt check、Python py_compile、direct `ecs_kernel_data_boundary_audit`、standalone ecs_kernel_data 1/1、standalone status-output 2/2；entity/observer/command/messages/change_tick/ecs Cargo gates pending。 |
| 横切 | Runtime 08 ECS 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `ecs_kernel_data_boundary` 与 `runtime_08_ecs_kernel_data_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 08 M1/M2/M3 的 16 个 ECS 行为测试锚，当前 `behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、`runtime_08_guard_anchors = 21/21` 与 `doc_anchors = 13/13`；Runtime 08、`docs/zircon_runtime/scene/ecs.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `ecs_kernel_data_boundary_audit`、standalone ecs_kernel_data 1/1、standalone status-output 2/2；entity/observer/command/messages/change_tick/ecs Cargo gates pending。 |
| 横切 | Runtime 08 ECS event owner folder split | ecs_events_folder_split_static_passed_cargo_deferred | 2026-06-20 | `scene/ecs/events.rs` 已硬切为 `scene/ecs/events/{mod,cursor,id,metrics,queue,store,subscription}.rs`：`mod.rs` 保持 public re-export，子 owner 分别承接 event id、payload/capacity metrics、double-buffer queue、cursor/read iter、dormant subscription boundary 与 erased `EventStore` channel registry。`EventStore::send_by_id(...)` / `send_batch_by_id(...)` 继续按 registered-channel presence 接收 producer writes，不回退到 `is_active` / `reader_count` gating；`ecs_events_messages.rs`、`ecs_observers_messages.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。`ecs_kernel_data_boundary` 当前同步 source files 26/26；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS message owner folder split | ecs_messages_folder_split_static_passed_cargo_deferred | 2026-06-20 | `scene/ecs/messages.rs` 已硬切为 `scene/ecs/messages/{mod,cursor,id,queue,store}.rs`：`mod.rs` 保持 public re-export，`id.rs` 承接 `Message` / `MessageId<T>`，`queue.rs` 承接 retained `Messages<T>` 队列与 generation，`cursor.rs` 承接 `MessageCursor` / `MessageReadIter`，`store.rs` 承接 erased `MessageStore` TypeId registry。`MessageStore` 写入、批量写入、reader cursor 与 explicit `clear_messages` retention boundary 保持原行为；`ecs_observers_messages.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。`ecs_kernel_data_boundary` 当前同步 source files 30/30；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS resource store owner folder split | ecs_resource_store_folder_split_static_passed_cargo_deferred | 2026-06-20 | `ResourceStore` flat owner 已硬切为 `scene/ecs/resource_store/{mod,stored_resource,store}.rs`：`mod.rs` 保持 `ResourceStore` public re-export，`stored_resource.rs` 承接 erased boxed resource、type name 与 `ComponentTicks`，`store.rs` 承接 `TypeId` registry、typed resource API 与 source-guarded direct branches。`ResourceStore` typed insert/get/get_mut/remove/ticks/type_names 行为和 `scene::ecs` public export 保持原样；`ecs_schedule.rs`、`ecs_change_detection.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。`ecs_kernel_data_boundary` 当前同步 source files 33/33；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS resource identity owner folder split | ecs_resource_identity_folder_split_static_passed_cargo_deferred | 2026-06-20 | `Resource` / `ResourceId` / `ResourceRegistry` flat owners 已硬切为 `scene/ecs/resource/{mod,marker,id,registry}.rs`：`mod.rs` 保持 public re-export，`marker.rs` 承接 marker trait，`id.rs` 承接 typed `ResourceId`，`registry.rs` 承接 `ResourceDescriptor` 与 TypeId registry。`scene::ecs::{Resource, ResourceId, ResourceDescriptor, ResourceRegistry}` public export、id allocation、registered lookup 与 descriptor access 保持原样；`component_structure.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。`ecs_kernel_data_boundary` 当前同步 source files 37/37；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS component identity owner folder split | ecs_component_identity_folder_split_static_passed_cargo_deferred | 2026-06-20 | `Component` / `ComponentId` / `ComponentRegistry` flat owners 已硬切为 `scene/ecs/component/{mod,marker,id,registry}.rs`：`mod.rs` 保持 public re-export，`marker.rs` 承接 `Component` marker trait 与默认 `StorageType::Table`，`id.rs` 承接 typed `ComponentId`，`registry.rs` 承接 `ComponentDescriptor`、`ComponentDescriptorSource` 与 Rust/dynamic component TypeId registry。`scene::ecs::{Component, ComponentId, ComponentDescriptor, ComponentDescriptorSource, ComponentRegistry}` public export、Rust/dynamic id allocation、registered lookup、descriptor access 与 `rust_type_for_id(...)` 保持原样；`component_structure.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。`ecs_kernel_data_boundary` 当前同步 source files 41/41；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS entity identity owner folder split | ecs_entity_identity_folder_split_static_passed_cargo_deferred | 2026-06-20 | `EntityRegistry` / `EntityRegistryError` / `EntityLocation` / `InternalEntity` / `StableEntityLocation` / `DespawnedEntity` flat owners 已硬切为 `scene/ecs/entity/{mod,despawned,error,internal,location,registry,slot,stable_location}.rs`：`mod.rs` 保持 public re-export，`registry.rs` 承接 stable-to-internal registry 行为，`slot.rs` 承接 generation slot 与 wrap helper，`internal.rs` 承接 generational handle，`location.rs` 与 `stable_location.rs` 承接 location DTO，`despawned.rs` 承接 despawn report，`error.rs` 承接 registry error。`scene::ecs::{EntityRegistry, EntityRegistryError, EntityLocation, InternalEntity, StableEntityLocation, DespawnedEntity}` public export、spawn/despawn、stable lookup、generation wrap 与 stale-handle rejection 保持原样；`component_structure.rs`、`ecs_identity_storage.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。该切片同步 source files 45/45；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS archetype owner folder split | ecs_archetype_owner_split_static_passed_cargo_deferred | 2026-06-20 | `ArchetypeId` / `ArchetypeSignature` / `ArchetypeIndex` flat owners 已硬切为 `scene/ecs/archetype/{mod,id,index,move_result,record,signature}.rs`：`mod.rs` 保持 public re-export，`id.rs` 承接 typed id，`signature.rs` 承接 table/sparse component signature，`record.rs` 承接 entity rows，`move_result.rs` 承接 move report，`index.rs` 承接 signature map、component inverted index、direct matching projection 与 entity move/remove。`scene::ecs::{ArchetypeId, ArchetypeIndex, ArchetypeMove, ArchetypeRecord, ArchetypeSignature}` public export、signature lookup、entity move reporting、shortest-bucket query matching 与 no-required full-record scan 保持原样；`ecs_archetype_index_structure.rs`、`ecs_query_structure.rs`、`component_structure.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。该切片同步 source files 51/51、archetype anchors 15/15；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS component storage owner folder split | ecs_component_storage_owner_split_static_passed_cargo_deferred | 2026-06-20 | `ComponentStorage` flat owner 已硬切为 `scene/ecs/storage/component_storage/{mod,entry,location,sparse,store,table,utils}.rs`：`mod.rs` 保持 `ComponentStorage` / `ComponentStorageLocation` public re-export，`store.rs` 承接 public facade、typed dispatch、type/storage guards 与 table/sparse bucket partition helper，`table.rs` 承接 row-index table backend，`sparse.rs` 承接 sparse backend，`location.rs` / `entry.rs` / `utils.rs` 承接 DTO、erased entry/remove result 与 deterministic sort/downcast helpers。`scene::ecs::{ComponentStorage, ComponentStorageLocation}` public export、table/sparse insert/get/remove/tick/location semantics 与 component id partition 保持原样；`component_structure.rs`、`ecs_component_storage_structure.rs`、`ecs_typed_api.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。该切片同步 source files 57/57、storage anchors 9/9；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS component storage private re-export cleanup | ecs_component_storage_private_reexport_cargo_check_passed | 2026-06-20 | 状态锚 `ecs_component_storage_private_reexport_cargo_check_passed`；`scene/ecs/storage/component_storage/mod.rs` 现在只保留 `ComponentStorage` / `ComponentStorageLocation` public re-export，不再把 `RawRemoveResult`、`StoredComponent`、`SparseComponentStorage`、`TableComponentStorage`、`downcast_component` 与 `sort_component_ids_if_needed` 作为 parent private re-export hub；`sparse.rs` / `table.rs` / `utils.rs` / `store.rs` 均直接引用 sibling owner。该切片只收束 component storage folder-backed owner visibility，不改变 public ECS storage API 或行为；验证：Python py_compile、direct `ecs_kernel_data_boundary_audit` 报告 `component_storage_private_reexport_anchors = 9/9`、`unexpected_component_storage_private_reexports = []`，standalone `ecs_kernel_data.rs` 1/1 与 rustfmt check 通过；`cargo check -p zircon_runtime --lib --locked --no-default-features --features core-min --jobs 1 --target-dir D:\cargo-targets\zircon-runtime08-component-storage-private-0620 --message-format short --color never` passed with existing warnings only；不提升 Runtime 08 entity/observer/command/messages/change_tick/ecs behavior Cargo gates。 |
| 横切 | Runtime 08 ECS observer owner folder split | ecs_observer_owner_split_static_passed_cargo_deferred | 2026-06-20 | `ObserverStore` flat owner 已硬切为 `scene/ecs/observer/{mod,callbacks,entry,id,store,utils}.rs`：`mod.rs` 保持 `ObserverId` / `ObserverStore` public re-export，`id.rs` 承接 handle，`callbacks.rs` 承接 erased callback 类型，`entry.rs` 承接 lifecycle/global/entity observer records，`store.rs` 承接 observe/remove/callback clone-out API，`utils.rs` 承接 exact-capacity callback counter 与 first-match removal helper。`scene::ecs::{ObserverId, ObserverStore}` public export、同步触发、callback clone-out、dispatch 中移除只影响后续触发的语义保持原样；`ecs_observers_messages.rs`、`component_structure.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。该切片同步 source files 65/65、observer anchors 8/8；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS commands facade owner split | ecs_commands_facade_owner_split_static_passed_cargo_deferred | 2026-06-20 | `Commands` flat facade owner 已硬切为 `scene/ecs/commands/commands/{mod,entity_commands,facade,param}.rs`：`mod.rs` 保持 `Commands` / `EntityCommands` / `CommandsParam` public re-export，`facade.rs` 承接 `Commands` 队列、spawn/entity/resource facade 与 deferred error reporting，`entity_commands.rs` 承接 builder，`param.rs` 承接 SystemParam bridge。`scene::ecs::{Commands, EntityCommands, CommandsParam}` public export、deferred visibility、`DeferredCommandReport` 与 missing-target error reporting 保持原样；`ecs_commands.rs` 与 `ecs_kernel_data.rs` source guards 已改读 folder-backed owner set。该切片同步 source files 65/65、deferred command anchors 11/11；Cargo 行为 gate 按本轮“先实现功能”方向 deferred。 |
| 横切 | Runtime 08 ECS command Cargo 验证窗口探测 | cargo_recheck_timeout_no_result | 2026-06-20 | `cargo test -p zircon_runtime --lib command --locked --no-default-features --features core-min --jobs 1 --target-dir target\codex-runtime08-commands-owner-0620 --message-format short --color never -- --test-threads=1 --nocapture` ended as `904s timeout no result`; target-dir process scan reported no residual cargo/rustc for `codex-runtime08-commands-owner-0620`. This records only a no-result timeout and does not promote Runtime 08 commands facade owner split or entity/observer/command/messages/change_tick/ecs Cargo gates. |
| 横切 | Runtime 08 ECS entity Cargo 验证窗口探测 | cargo_recheck_timeout_no_result | 2026-06-20 | `cargo test -p zircon_runtime --lib entity --locked --no-default-features --features core-min --jobs 1 --target-dir D:\cargo-targets\zircon-runtime08-component-storage-private-0620 --message-format short --color never -- --test-threads=1 --nocapture` exceeded the 1200s tool window with no test result; residual cargo/rustc processes in that target dir were stopped. This records only a no-result timeout and does not promote Runtime 08 entity/observer/command/messages/change_tick/ecs behavior Cargo gates. |
| 横切 | Runtime 08 ECS data owner-tree guard | ecs_data_owner_tree_guard_static_passed_cargo_pending | 2026-06-20 | `runtime_08_ecs_data_owner_trees_stay_folder_backed_after_cutover` 锁定 `scene/ecs/{archetype,component,entity,events,messages,observer,resource,resource_store}` 八个 Runtime 08 data owner、`scene/ecs/storage/component_storage/` 与 `scene/ecs/commands/commands/{mod,entity_commands,facade,param}.rs` 继续保持 folder-backed，每个 owner `mod.rs` 继续作为 structural module/export owner，并要求 retired flat Runtime 08 ECS owner 文件 `component.rs`、`component_id.rs`、`component_registry.rs`、`despawned_entity.rs`、`entity_location.rs`、`entity_registry.rs`、`entity_registry_error.rs`、`internal_entity.rs`、`stable_entity_location.rs`、`events.rs`、`messages.rs`、`observer.rs`、`resource.rs`、`resource_id.rs`、`resource_registry.rs`、`resource_store.rs`、`archetype_id.rs`、`archetype_index.rs`、`archetype_signature.rs`、`storage/component_storage.rs` 与 `commands/commands.rs` 不复活。该守卫只封住 owner-tree hard cutover，不提升 entity/observer/command/messages/change_tick/ecs Cargo gates。 |
| 横切 | Runtime 08 ECS change detection owner-tree guard | ecs_change_detection_owner_tree_guard_static_passed_cargo_pending | 2026-06-20 | `runtime_08_ecs_change_detection_owner_tree_stays_folder_backed_after_cutover` 锁定 `scene/ecs/change_detection/{mod,change_tick,change_tick_window,component_ticks,stats,wrappers}.rs` 继续保持 folder-backed，`change_detection/mod.rs` 继续作为 structural module/export owner，并要求 retired flat `scene/ecs/change_detection.rs`、`scene/ecs/change_tick.rs`、`scene/ecs/change_tick_window.rs`、`scene/ecs/component_ticks.rs`、`scene/ecs/change_detection_stats.rs` 与 `scene/ecs/change_detection_wrappers.rs` 不复活。该守卫只封住 change detection owner-tree hard cutover，不提升 entity/observer/command/messages/change_tick/ecs Cargo gates。 |
| 横切 | Runtime 08 ECS root leaf owner guard | ecs_root_leaf_owner_guard_static_passed_cargo_pending | 2026-06-20 | `runtime_08_ecs_root_leaf_owners_stay_explicit_after_data_cutover` 锁定 `scene/ecs/{bundle,removal,storage_type}.rs` 继续作为显式根层叶子 owner，要求 `scene/ecs/mod.rs` 继续声明并导出 `Bundle`、`RemovedComponentEvent` / `RemovedComponentEvents` / `RemovedComponentReader` 与 `StorageType`，并把 `bundle.rs` 纳入 `ecs_kernel_data_boundary` 源文件清单。当前结构镜像为 source files 69/69、Runtime 08 guard anchors 21/21；该守卫不提升 entity/observer/command/messages/change_tick/ecs Cargo gates。 |
| 横切 | Runtime 08 ECS 数据面 current audit recheck | ecs_kernel_data_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `ecs_kernel_data_current_audit_static_passed_cargo_pending`；本轮复核 Runtime 08 当前 ECS 数据面结构事实并纳入 archetype/component/entity/event/message/resource/resource-store/component-storage/observer/commands facade/change-detection owner folder split：`ecs_kernel_data_boundary_audit` 报告 source files 69/69、guard/test files 8/8、archetype anchors 15/15、storage anchors 9/9、component-storage private re-export anchors 9/9、unexpected component-storage private re-exports 0、component identity anchors 18/18、entity lifecycle anchors 10/10、observer anchors 8/8、deferred command anchors 11/11、event/message anchors 12/12、resource identity anchors 12/12、change tick anchors 6/6、Runtime 08 guard anchors 21/21、behavior-test anchors 16/16、`doc_anchors = 13/13`、pending Cargo gate anchors 6/6、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `ecs_kernel_data_boundary_audit` risks=[]、standalone `ecs_kernel_data.rs` 1/1；entity/observer/command/messages/change_tick/ecs Cargo gates 仍 pending。 |
| 横切 | Runtime 08 ECS source/test inventory split | ecs_kernel_data_source_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `ecs_kernel_data_source_inventory_split_static_passed_cargo_deferred_tests_deferred`；`ecs_kernel_data_source_inventory.py` 接管 Runtime 08 source/test/mirror guard inventory：`EXPECTED_SOURCE_FILE_COUNT = 69`、`EXPECTED_TEST_FILE_COUNT = 8`、source files 69/69、guard/test files 8/8、`mirror_docs_guard_present = true`、`risks = []`；`ecs_kernel_data_boundary.py` 保持审计编排与 domain anchor 逻辑并降到 727 行。验证：Python py_compile、direct `ecs_kernel_data_boundary_audit`、standalone `ecs_kernel_data.rs` 1/1、standalone `plan_status.rs` 通过；外部 HZB/render Cargo lane 活跃，本切片不启动 package-level Cargo。 |
| 横切 | Runtime 08 ECS anchor inventory split | ecs_kernel_data_anchor_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `ecs_kernel_data_anchor_inventory_split_static_passed_cargo_deferred_tests_deferred`；`ecs_kernel_data_anchor_inventory.py` 接管 Runtime 08 domain/test/doc/Cargo gate anchor inventory，`ecs_kernel_data_boundary.py` 保留审计读取、缺失计算、风险聚合与 Markdown 渲染并降到 497 行。direct audit 仍报告 source files 69/69、guard/test files 8/8、`archetype_anchor_count = 15`、storage 9、component identity 18、entity lifecycle 10、observer 8、command 11、event/message 12、resource identity 12、change tick 6、`behavior_test_anchor_count = 16`、doc anchors 13/13、Cargo gate anchors 6/6、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `ecs_kernel_data_boundary_audit`、rustfmt touched plan-status guards、standalone `ecs_kernel_data.rs` 1/1、standalone `plan_status.rs` 33/33；外部 render/editor Cargo lanes 活跃，本切片不启动 package-level Cargo。 |
| 横切 | Runtime 08 ECS markdown renderer split | ecs_kernel_data_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `ecs_kernel_data_markdown_split_static_passed_cargo_deferred_tests_deferred`；`ecs_kernel_data_markdown.py` now owns `render_ecs_kernel_data_boundary_markdown`; `ecs_kernel_data_boundary.py` now owns audit read, missing-anchor calculation, and risk aggregation at 344 lines, while the Markdown owner is 154 lines. Direct audit reports source files 69/69, guard/test files 8/8, `archetype_anchor_count = 15`, storage 9, component-storage private re-export anchors 9, unexpected component-storage private re-exports 0, component identity 18, entity lifecycle 10, observer 8, command 11, event/message 12, resource identity 12, change tick 6, `behavior_test_anchor_count = 16`, doc anchors 13/13, Cargo gate anchors 6/6, `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile, direct `ecs_kernel_data_boundary_audit` risks=[], standalone `ecs_kernel_data.rs` 1/1, standalone `plan_status.rs` 33/33; entity/observer/command/messages/change_tick/ecs Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 08 QueryState Markdown renderer split | ecs_query_state_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `ecs_query_state_markdown_split_static_passed_cargo_deferred_tests_deferred`；`ecs_query_state_markdown.py` now owns `render_ecs_query_state_boundary_markdown`; `ecs_query_state_boundary.py` now owns QueryState owner-module audit, root budget checks, forbidden-root behavior scan, and risk aggregation at 141 lines, while the Markdown owner is 33 lines. Direct audit reports legacy `query_state.rs` absent, owner modules 9/9, `root_non_empty_lines = 84/180`, missing/unexpected/oversized owner modules all empty, and `risks = []`. Validation: Python py_compile, direct `ecs_query_state_boundary_audit` risks=[], standalone `ecs_query_structure.rs` 11/11, standalone `plan_status.rs` 33/33; entity/observer/command/messages/change_tick/ecs Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 08 F17 entity path lookup verb rename | runtime_08_entity_path_lookup_getter_rename_coremin_check_passed | 2026-06-22 | Status anchor `runtime_08_entity_path_lookup_getter_rename_coremin_check_passed`; F17 entity path Option lookup verb rename now exposes `World::get_entity_by_path(&EntityPath) -> Option<EntityId>` and leaves the old resolve-verb entity path method absent from production source. Runtime animation and first-party animation plugin consumers call `get_entity_by_path(...)`; no shim or alias keeps the old public method alive. Added `review_f17_entity_path_option_lookup_uses_get_verb`; scoped rustfmt, F17 structure guard, status-output 2/2, runtime core-min `cargo check`, and `zircon_plugin_animation_runtime` package `cargo check` passed with existing warnings; Runtime 08 entity/observer/command/messages/change_tick/ecs gates pending. |
| 横切 | Runtime 08 F5 world typed mutation errors | world_typed_mutation_errors_coremin_check_passed_partial | 2026-06-22 | 状态锚 `world_typed_mutation_errors_coremin_check_passed_partial`；F5 world typed mutation errors 已把 `scene/world/error.rs` 纳入 `SceneError` / `SceneResult` owner，`World::spawn`、`spawn_at`、`insert_bundle`、`insert`、`remove` 与 `Bundle::insert_into(...)` 改返 `SceneResult`；缺失实体路径返回 `SceneError::MissingEntity`，storage error 保留为 typed source，deferred command report 只在 `DeferredCommandError` 边界 stringify。本轮继续把 `scene/world/{component_access,hierarchy,query,records}.rs` 的固定 world mutation façade 改返 `SceneResult`，并在 property write、dynamic-scene spawn、fixed reflection adapters 与 script gameplay host 字符串边界显式转换。新增 `world_typed_mutation_errors_report_missing_entities_as_scene_errors` 行为测试与 `review_f5_world_spawn_bundle_surface_uses_scene_error` 结构守卫。验证：F5 结构守卫 1/1、F5 behavior 1/1 与 core-min `cargo check` 通过；`dynamic_components` String error 与 `DynamicSceneError::WorldMutation(String)` 桥已由下一行 `dynamic_component_typed_errors_coremin_check_passed` 关闭，Runtime 08 entity/observer/command/messages/change_tick/ecs 行为 gates 仍 pending。 |
| 横切 | Runtime 08 F5 dynamic component typed errors | dynamic_component_typed_errors_coremin_check_passed | 2026-06-22 | 状态锚 `dynamic_component_typed_errors_coremin_check_passed`；`SceneError` 已承接 dynamic component / component type registry / `SceneError::PluginComponentsActive` plugin unload / reflection source 变体；`ComponentTypeRegistry` 与 `World` dynamic component mutation family 改返 `SceneResult` 并移除内部字符串化错误；`DynamicSceneError::WorldMutation(SceneError)` 现在保留 source（代码形态 `WorldMutation(#[from] SceneError)`），spawn apply 路径直接 `?` 保留 source。属性写入、反射、插件 registry 与脚本宿主仅在外部字符串合同边界转换。新增 `dynamic_component_mutation_errors_report_scene_error_variants`、`dynamic_scene_world_mutation_preserves_scene_error_source` 与 `review_f5_dynamic_component_errors_preserve_scene_error_sources`。验证：scoped rustfmt、F5 dynamic 结构守卫、status-output 守卫、core-min `cargo check` 通过；聚焦 behavior `cargo test` 构建 15m 超时无测试结果、不计通过；Runtime 08 entity/observer/command/messages/change_tick/ecs 行为 gates 仍 pending。 |
| 横切 | Runtime 08 ecs_events_messages Cargo 验证窗口探测 | cargo_recheck_timeout_no_result | 2026-06-20 | `cargo test -p zircon_runtime --lib ecs_events_messages --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-08-ecs-events-messages-0620 --message-format short --color never -- --test-threads=1 --nocapture` ended as `1200s timeout no result`; residual target-dir processes stopped: cargo/rustc processes in the `zircon-runtime-08-ecs-events-messages-0620` target-dir were stopped. This records only a no-result timeout and does not promote Runtime 08 event/message/resource-store owner split or entity/observer/command/messages/change_tick/ecs Cargo gates. |
| M4 | Runtime 08 QueryState cache owner split | query_state_cache_owner_split_static_passed_cargo_pending | 2026-06-17 | `query_state/cache.rs` 新增为 cache rebuild、cache-slot lookup、cached entity/component-location accessor 与 cache metadata accessor owner；`query_state/mod.rs` 收缩为 `QueryState` 字段、构造与 access descriptor，当前 `root_non_empty_lines = 84/180`；`ArchetypeIndex::matching_archetypes(...)` 当前保持直接预分配投影：有 required component 时从 borrowed shortest bucket 推入匹配 id，无 required component 时按 full record count 预分配并直接扫描 records，不恢复 `all_archetype_ids(...)` helper 或 clone-then-retain。`ecs_query_state_boundary` 同步 `expected_module_count = 9`、`unexpected_modules = []`、`risks = []`；验证：rustfmt check、Python py_compile、direct `ecs_query_state_boundary_audit` / `ecs_kernel_data_boundary_audit` risks=[]、standalone `ecs_query_structure.rs` 11/11、standalone `ecs_kernel_data.rs` 1/1、standalone `performance_hotspots.rs` 8/8、standalone `plan_status.rs` 32/32；`docs/zircon_runtime/scene/ecs/query_state.md`、`docs/zircon_runtime/scene/ecs.md`、Runtime 08、M0 review 与 runtime-interface convergence 已同步；entity/observer/command/messages/change_tick/ecs Cargo gates pending。 |
| 横切 | Runtime 05 status-output Runtime 08 behavior-test row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 08 ECS 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、standalone ecs_kernel_data 1/1 与 standalone status-output 2/2 证据；验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；Runtime 08 entity/observer/command/messages/change_tick/ecs Cargo gates 仍 pending。 |
| 横切 | Runtime 10 Dynamic API 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_10_dynamic_runtime_api_mirror_docs_match_structure_audit_counts` 已锁定 `dynamic_runtime_api_boundary` 的 33 source owner、10 function table structs、11 FFI wrappers、private Rust ABI session owner、pending gates 与 6 份镜像文档计数；当前 `expected_source_file_count = 33`、`behavior_test_anchor_count = 16`、`runtime_diagnostics_anchors = 15/15`、`missing_runtime_diagnostics_anchors = []`、`doc_anchors = 13/13`。`docs/plans/zircon_runtime/runtime/10-dynamic-api-and-interface-convergence.md`、`docs/zircon_runtime/dynamic_api/session.md`、M0 review、runtime-interface convergence、cdylib loader 与本总索引已同步。验证：Python py_compile、direct `dynamic_runtime_api_boundary_audit` risks=[]、standalone `dynamic_api_session.rs` 9/9；dynamic_api/app/UI gates pending。 |
| 横切 | Runtime 10 host-request payload ABI boundary | host_request_payload_boundary_static_passed_cargo_pending | 2026-06-20 | `dynamic_runtime_api_boundary` now locks the host-request payload ABI owner chain from interface DTOs through runtime conversion, dynamic API host-request tests, and app host routing; current audit records `expected_source_file_count = 33`, `host_request_payload_anchors = 38/38`, `missing_host_request_payload_anchors = []`, `doc_anchors = 13/13`, `mirror_docs_guard_present = true`, `risks = []`. Validation: Python py_compile/direct audit, standalone `dynamic_api_session.rs`, standalone status-output, and rustfmt lightweight gates; dynamic_api/app/UI Cargo gates pending. |
| 横切 | Runtime 10 F18 asset manager resolution return shape | runtime_10_asset_manager_resolution_handle_shape_coremin_check_passed | 2026-06-22 | Status anchor `runtime_10_asset_manager_resolution_handle_shape_coremin_check_passed`; F18 manager resolution now matches the generic registered-handle shape: `resolve_asset_manager(core)` returns `Result<Arc<AssetManagerHandle>, CoreError>` through `CoreHandle::resolve_manager::<AssetManagerHandle>(ASSET_MANAGER_NAME)` instead of returning `Arc<dyn AssetManager>` directly. The dynamic project boundary calls `.shared()` before `open_project(...)`, keeping trait-object conversion explicit at the session edge. Added `review_f18_asset_manager_resolution_returns_registered_handle`; scoped rustfmt, F18 structure guard 1/1, status-output 2/2, and core-min `cargo check` passed with existing 141 warnings; dynamic_api/app/UI Cargo gates pending. |
| 横切 | Runtime 10 runtime diagnostics profile-control snapshot | runtime_diagnostics_profile_control_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_diagnostics_profile_control_static_passed_cargo_deferred_tests_deferred`；`ProfileControlCommand::RuntimeDiagnosticsSnapshot` 复用 existing `profile_control` JSON ABI，返回 `ProfileControlResponse.runtime_diagnostics` / `RuntimeDiagnosticsSnapshot` / scene-asset reload summary；no new `ZrRuntimeApiV1` function pointer。`dynamic_runtime_api_boundary` 当前同步 `expected_source_file_count = 33`、`runtime_diagnostics_anchors = 15/15`、`missing_runtime_diagnostics_anchors = []`；Cargo/focused tests 按实现优先策略 deferred。 |
| 横切 | Runtime 10 Dynamic API 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `dynamic_runtime_api_boundary` 与 `runtime_10_dynamic_runtime_api_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 10 的 16 个行为测试锚，覆盖 dynamic API session 失败路径、headless/minimal profile、FFI panic table wrapper、app loader 失败注入与 profile-control runtime diagnostics snapshot；当前 `behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`。验证：Python py_compile、direct `dynamic_runtime_api_boundary_audit` risks=[]、standalone `dynamic_api_session.rs` 9/9；dynamic_api/app/UI gates pending。 |
| 横切 | Runtime 10 dynamic_api_session 吸收守卫拆分 | focused_cargo_passed_broader_gates_pending | 2026-06-15 | `dynamic_api_session/{shared,headless_profiles,event_split,test_owner_split,ffi_panic_boundary,runtime_diagnostics,ui_contract,v2_contract,mirror_docs}.rs` now owns the split Runtime 10 absorption guard data/tests behind the `dynamic_api_session.rs` mount; `dynamic_runtime_api_boundary` reports `expected_source_file_count = 33`, `behavior_test_anchor_count = 16`, `missing_behavior_test_anchors = []`, `runtime_diagnostics_anchors = 15/15`, `host_request_payload_anchors = 38/38`, `ui_contract_duplicate_public_types = 0`, and `ui_v2_contract_sync_anchors = 9/9`. Validation: 2026-06-15 focused Cargo `cargo test -p zircon_runtime --lib dynamic_api_session --locked` 已 5 passed / 4231 filtered out；本轮 package Cargo 按实现优先策略后续补跑；broader dynamic_api/app/UI gates pending。 |
| M2 | Runtime 10 UI contract duplicate public types cleanup | runtime_10_m2_1_ui_contract_duplicate_public_types_removed_static_passed_cargo_pending | 2026-06-17 | `runtime_10_ui_contract_types_have_single_definition_across_interface_and_runtime` 锁定 interface/runtime UI public struct/enum duplicate count；已删除 runtime-local `UiBindingCodec` 与 `UiAssetSchemaVersionPolicy`，interface 继续拥有 `zircon_runtime_interface::ui::event_ui::UiBindingCodec` 与 `zircon_runtime_interface::ui::template::asset::schema::UiAssetSchemaVersionPolicy`；`dynamic_runtime_api_boundary` 当前记录 `expected_source_file_count = 33`、`host_request_payload_anchors = 38/38`、`ui_contract_single_source_anchors = 7/7`、`ui_contract_duplicate_public_types = 0`、`ui_v2_contract_sync_anchors = 9/9`。验证：历史 M2.1 静态/standalone 证据保留；runtime ui/editor Cargo gates pending。 |
| M2 | Runtime 10 UI v2 contract sync | runtime_10_m2_2_ui_v2_contract_sync_static_passed_cargo_pending | 2026-06-17 | `runtime_10_ui_v2_contract_sync_matches_runtime_09_verdict_and_interface_owner` 锁定 Runtime 09 `v2-replacement-mainline` 判词、interface `ui/v2` DTO owner、runtime `ui/v2` consumer、`UiComponentApiVersion` owner 与 runtime validation `actual.is_compatible_with(required)`；`ui_component_api_version_mismatch_is_rejected_with_parse_error` 覆盖 major mismatch 不兼容与非法 `2.0` parse error；`dynamic_runtime_api_boundary` 当前记录 `expected_source_file_count = 33`、`host_request_payload_anchors = 38/38`、`ui_v2_contract_sync_anchors = 9/9`。验证：历史 M2.2 静态/standalone 证据保留；2026-06-17 interface package Cargo 168/168 passed；runtime ui/editor Cargo gates pending。 |
| 横切 | Runtime 05 status-output Runtime 10 behavior-test row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 10 Dynamic API 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、standalone dynamic_api_session 9/9 与 standalone status-output 2/2 证据；Runtime 10 runtime diagnostics 行同步固定 `runtime_diagnostics_anchors = 15/15`。Runtime 10 `dynamic_api`、完整 app loader 与 UI contract owner/Cargo gates 仍 pending。 |
| 横切 | Runtime 05 plan-status output-anchor module split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_output_anchors.py` 现在 owns status-output 与 Cargo attempt anchor tuples；`runtime_plan_status_boundary.py` remains orchestration，拆分后为 761 lines，新锚点模块为 141 lines。验证：Python py_compile 通过；direct `runtime_plan_status_boundary_audit` risks=0 / `status_output_table_guard_present = true` / `cargo_attempt_status_guard_present = true` / `runtime_10_behavior_status_guard_present = true`；standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 plan-status output-anchor budget guard | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_boundary_audit` 现在显式报告 `plan_status_boundary_line_count = 855` / `max_plan_status_boundary_lines = 900`、`status_output_anchor_module_present = true`、`status_output_anchor_module_line_count = 143` / `max_status_output_anchor_module_lines = 300`，且 `plan_status_boundary_under_line_budget = true`、`status_output_anchor_module_under_line_budget = true`、`risks = []`。验证：Python py_compile 通过；direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 status-output expected anchor split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_status_rows.rs` now owns `EXPECTED_STATUS_OUTPUT_SLICES`; `status_output_tables.rs` is reduced to traversal/assertion logic and `status_output_tables/expected_slices.rs` keeps status/date mapping. `runtime_plan_status_boundary` now tracks plan-status support files 21/21 and `plan_status_boundary_line_count = 862`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 plan-status root module split | status_table_static_passed_cargo_pending | 2026-06-15 | `plan_status.rs` now only mounts child modules; `plan_status/support.rs` owns shared helper traversal, `plan_status/index_tables.rs` owns runtime index table guards, `plan_status/subplan_status.rs` owns subplan frontmatter/status evidence guards, and `plan_status/closeout.rs` plus `plan_status/architecture_review.rs` own closeout/review guards. `runtime_plan_status_boundary` now tracks plan-status support files 26/26 and `plan_status_boundary_line_count = 867`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone plan-status test list 30 tests；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 plan-status support inventory split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_support_inventory.py` now owns `PLAN_STATUS_SUPPORT_FILES` and `EXPECTED_PLAN_STATUS_SUPPORT_FILE_COUNT`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 842` while direct audit still reports support 26/26、missing support files []、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 plan-status anchor inventory split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_anchor_inventory.py` now owns `CORE_GUARD_ANCHORS` / `PENDING_GATE_ANCHORS` / `DOC_ANCHORS` / `BACKLOG_GAPS`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 789` while direct audit still reports support 26/26、missing support files []、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 plan-status markdown renderer split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_markdown.py` now owns `render_runtime_plan_status_boundary_markdown`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 559` while direct audit still reports support 26/26、missing support files []、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0 and rendered markdown lines > 0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 plan-status markdown direct import hard-cutover | status_table_static_passed_cargo_pending | 2026-06-21 | 状态锚 `status_table_static_passed_cargo_pending`；`audit_runtime_structure.py` now imports `render_runtime_plan_status_boundary_markdown` directly from `runtime_plan_status_markdown.py`, and `runtime_plan_status_boundary.py` no longer imports or re-exports the Markdown renderer. Direct audit reports support files 79/79, `plan_status_boundary_line_count = 451`, status-output anchor module 145/300 lines, status-output table guard present, Runtime 05 status `in_progress`, status table gaps [], missing support files [], `risks = []`, and rendered lines 26. Validation: Python py_compile, direct `runtime_plan_status_boundary_audit`, and standalone `plan_status.rs` 33/33; full Runtime 05 `scene::` Cargo closeout remains pending. |
| 横切 | Runtime 05 plan-status source helper split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_sources.py` now owns `read_text` / `runtime_subplans` / `status_rows`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 454` while direct audit still reports support 26/26、missing support files []、rendered markdown lines 26、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 status-output expected row data split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_status_row_data.rs` now owns `EXPECTED_STATUS_OUTPUT_SLICES`; `expected_status_rows.rs` is the thin const-alias entrypoint and `runtime_plan_status_boundary.py` now reports plan-status support files 27/27 with `plan_status_boundary_line_count = 454`。Validation: rustfmt、Python py_compile、direct `runtime_plan_status_boundary_audit` support 27/27 / risks=0、wrapped standalone plan-status 30/30；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| 横切 | Runtime 05 status-output row-data group split | status_table_static_passed_cargo_pending | 2026-06-15 | `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now groups status-output evidence rows by runtime owner in `expected_status_row_data/{runtime_01_04,runtime_05,runtime_06_09,runtime_10_13,runtime_14}.rs`; `expected_status_output_slices` flattens those groups for the unchanged all-index-row coverage guard. Current direct plan-status audit target is plan-status support files 32/32, with `expected_status_row_data/runtime_05.rs` as the largest data-owner slice at 445 lines. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` support 32/32 risks=[]、wrapped standalone plan-status 30/30、conflict/trailing/diff checks 通过（仅 LF/CRLF warning）；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 06-09 row-data group split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_06_09.rs` is now a structural entrypoint that delegates Runtime 06、07、08、09 evidence rows to `runtime_06_09/{runtime_06,runtime_07,runtime_08,runtime_09}.rs`; concrete support owners are `runtime_06_09/runtime_06.rs`, `runtime_06_09/runtime_07.rs`, `runtime_06_09/runtime_08.rs`, and `runtime_06_09/runtime_09.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 36/36. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 10-13 row-data group split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_10_13.rs` is now a structural entrypoint that delegates Runtime 10、11、12、13 evidence rows to `runtime_10_13/{runtime_10,runtime_11,runtime_12,runtime_13}.rs`; concrete support owners are `runtime_10_13/runtime_10.rs`, `runtime_10_13/runtime_11.rs`, `runtime_10_13/runtime_12.rs`, and `runtime_10_13/runtime_13.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 40/40. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 10 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_10_13/runtime_10.rs` is now a structural entrypoint that delegates Runtime 10 evidence rows to `runtime_10_13/runtime_10/{dynamic_api,session,ui_contract}.rs`; concrete support owners are `runtime_10/dynamic_api.rs`, `runtime_10/session.rs`, and `runtime_10/ui_contract.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those three Runtime 10 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 65/65. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 65/65, standalone plan-status 32/32; Runtime 10 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output Runtime 12 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_10_13/runtime_12.rs` is now a structural entrypoint that delegates Runtime 12 evidence rows to `runtime_10_13/runtime_12/{baseline,action_mapping,gamepad,host_recording}.rs`; concrete support owners are `runtime_12/baseline.rs`, `runtime_12/action_mapping.rs`, `runtime_12/gamepad.rs`, and `runtime_12/host_recording.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four Runtime 12 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 69/69. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 69/69, standalone plan-status 32/32; Runtime 12 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output support-structure owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/support_structure.rs` is now a structural entrypoint that delegates Runtime 05 support-structure evidence rows to `runtime_05/support_structure/{plan_status_modules,status_output_splits}.rs`; concrete owners separate plan-status module support rows from status-output owner-split rows. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes both Runtime 05 support-structure owner groups separately, `Runtime 05 support rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 71/71. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 71/71, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output scene-closeout owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/scene_closeout.rs` is now a structural entrypoint that delegates Runtime 05 scene closeout evidence rows to `runtime_05/scene_closeout/{dynamic_scene_rows,full_scene_gate_rows,source_guard_rows}.rs`; concrete owners separate dynamic-scene row data, full `scene::` gate/diagnostic rows, and serialization/editor-projection source guards. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes all three Runtime 05 scene-closeout owner groups separately, `Runtime 05 scene closeout rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 79/79. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 79/79, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output audit-metadata owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/audit_metadata.rs` is now a structural entrypoint that delegates Runtime 05 audit metadata evidence rows to `runtime_05/audit_metadata/{plan_coverage_rows,runtime_02_03_rows,runtime_07_rows}.rs`; concrete owners separate plan coverage/Cargo status, Runtime 02/03 status, and Runtime 07 status rows. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes all three Runtime 05 audit owner groups separately, `Runtime 05 audit rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 74/74. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 74/74, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output cargo-gates owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/cargo_gates.rs` is now a structural entrypoint that delegates Runtime 05 cargo gate evidence rows to `runtime_05/cargo_gates/{early_rows,late_rows}.rs`; concrete owners separate Runtime 01-04/06-08 early Cargo gate rows from Runtime 10-14 late Cargo gate rows. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes both Runtime 05 cargo gate owner groups separately, `Runtime 05 cargo gate rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 76/76. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 76/76, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 01-04 row-data group split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_01_04.rs` is now a structural entrypoint that delegates Runtime 01、02、03、04 evidence rows to `runtime_01_04/{runtime_01,runtime_02,runtime_03,runtime_04}.rs`; concrete support owners are `runtime_01_04/runtime_01.rs`, `runtime_01_04/runtime_02.rs`, `runtime_01_04/runtime_03.rs`, and `runtime_01_04/runtime_04.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 44/44. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output status/date owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `status_output_tables/expected_slices.rs` is now a structural entrypoint that delegates row status mapping to `expected_slices/status.rs` and row completion-date mapping to `expected_slices/date.rs`; `status_output_tables.rs` still calls `expected_status_for_slice` and `expected_date_for_slice` through the same module boundary. `runtime_plan_status_support_inventory.py` now tracks plan-status support files 46/46. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 05 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05.rs` is now a structural entrypoint that delegates Runtime 05 evidence rows to `runtime_05/{baseline,cross_runtime_rows,support_structure,scene_closeout,cargo_gates,audit_metadata}.rs`; concrete support owners include `runtime_05/scene_closeout.rs`, `runtime_05/cargo_gates.rs`, and `runtime_05/audit_metadata.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those six owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 52/52. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 14 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_14.rs` is now a structural entrypoint that delegates Runtime 14 evidence rows to `runtime_14/{audit_sync,cargo_gates,guard_anchors}.rs`; concrete support owners are `runtime_14/audit_sync.rs`, `runtime_14/cargo_gates.rs`, and `runtime_14/guard_anchors.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those three Runtime 14 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 55/55. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 55/55, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 07 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_06_09/runtime_07.rs` is now a structural entrypoint that delegates Runtime 07 evidence rows to `runtime_06_09/runtime_07/{performance,asset_render,scene_asset,owner_budget}.rs`; concrete support owners include `runtime_07/performance.rs`, `runtime_07/asset_render.rs`, `runtime_07/scene_asset.rs`, and `runtime_07/owner_budget.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four Runtime 07 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 59/59. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 59/59, standalone plan-status 32/32; Runtime 07 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output Runtime 09 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_06_09/runtime_09.rs` is now a structural entrypoint that delegates Runtime 09 evidence rows to `runtime_06_09/runtime_09/{baseline,layout_pipeline,legacy_renames}.rs`; concrete support owners include `runtime_09/baseline.rs`, `runtime_09/layout_pipeline.rs`, and `runtime_09/legacy_renames.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` keeps consuming the Runtime 09 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 62/62. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 62/62, standalone plan-status 32/32; Runtime 09 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 dynamic scene document serialization owner split | dynamic_scene_document_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/document.rs` 已硬切为 `document/{mod,legacy,read,write}.rs`：`document/read.rs` owns `DynamicScene::from_versioned_json` parse、legacy `ProjectDocument { world }` migration 与 `ensure_supported` validation，`document/write.rs` owns `DynamicScene::to_versioned_json_pretty`，`document/legacy.rs` owns legacy DTO。Public method names、one-way migration behavior、dynamic-scene JSON schema 与 scene/editor 边界语义不变；serialization source guard 已同步扫描拆分后的 `document/` owner 文件。验证：rustfmt check、旧 flat `document.rs` absent、新 `document/mod.rs` present、conflict/trailing/diff checks、standalone plan-status 32/32；Cargo 与 focused behavior tests 按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene scene-asset bridge owner split | dynamic_scene_scene_asset_bridge_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/scene_asset.rs` 已硬切为 `scene_asset/{mod,dynamic_scene,error,prepared_spawn}.rs`：`scene_asset/dynamic_scene.rs` owns imported `SceneAsset` / URI -> `DynamicScene` conversion through `SceneAssetSerializer`，`scene_asset/prepared_spawn.rs` owns prepared-payload constructors，`scene_asset/error.rs` owns `DynamicSceneError::SceneAsset` wrapping。`DynamicScene::from_scene_asset(_uri)`、`PreparedDynamicSceneSpawn::from_scene_asset(_uri)`、asset reload scheduling consumption、dynamic-scene validation/apply path 与 scene/editor 边界语义不变，未触碰 importer/artifact cache/watcher/render/editor/plugin 行为。验证：rustfmt check、旧 flat `scene_asset.rs` absent、新 `scene_asset/mod.rs` present、conflict/trailing/diff checks、standalone plan-status 32/32；Cargo 与 focused behavior tests 按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene entity declaration owner split | dynamic_scene_entity_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/entity.rs` 已硬切为 `entity/{mod,dynamic_component,dynamic_entity,dynamic_resource}.rs`：`entity/mod.rs` 保留 `DynamicComponent`、`DynamicEntity`、`DynamicResource` public exports，三个 declaration owners 分别持有 serde/reflection imports、字段定义与 constructor。`scene::dynamic_scene` / `scene` re-export surface、dynamic-scene JSON schema、capture/apply 行为与 scene/editor 边界语义不变；serialization source guard 已同步扫描拆分后的 `entity/`、`scene/`、`value/` owner 文件。验证：rustfmt check、旧 flat `entity.rs` absent、新 `entity/mod.rs` present、conflict/trailing/diff checks、standalone plan-status 32/32；Cargo 与 focused behavior tests 按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene value conversion owner split | dynamic_scene_value_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/value.rs` 已硬切为 `value/{mod,json,remap}.rs`：`value/mod.rs` 保留 `scene/spawn.rs` 消费的内部 helper surface，`value/json.rs` owns reflected fields -> JSON object conversion、finite float validation 与 unsupported value error，`value/remap.rs` owns `ReflectedValue` entity remap 与 JSON `{ "entity": ... }` remap。`DynamicScene::spawn_into` 行为、dynamic-scene JSON schema、`scene::dynamic_scene` / `scene` re-export surface 与 asset reload prepared-payload apply path 不变，未触碰 editor/plugin/render/session 行为。验证：rustfmt check、旧 flat `value.rs` absent、新 `value/mod.rs` present、conflict/trailing/diff checks、standalone plan-status 32/32；Cargo 与 focused behavior tests 按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene spawn task owner split | dynamic_scene_spawn_task_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/spawn_task.rs` 已硬切为 `spawn_task/{mod,loader,prepared,task}.rs`：`spawn_task/mod.rs` 保留 `PreparedDynamicSceneSpawn` 与 `DynamicSceneSpawnTask` public exports，`spawn_task/prepared.rs` owns validated payload counts 与 caller-thread `spawn_into`，`spawn_task/task.rs` owns descriptor/status/result collection，`spawn_task/loader.rs` owns `JobScheduler` scheduling、scene/JSON/path/scene-asset loader variants 与 task id allocation。`scene::dynamic_scene` / `scene` re-export surface、asset reload consumption 与 ECS world mutation 时机不变，未触碰 editor/plugin/render/session 行为。验证：rustfmt check、旧 flat `spawn_task.rs` absent、新 `spawn_task/mod.rs` present、conflict/trailing/diff checks、standalone plan-status 32/32；Cargo 与 focused behavior tests 按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene root scene owner split | dynamic_scene_scene_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/scene.rs` 已硬切为 `scene/{mod,capture,spawn,validation}.rs`：`scene/mod.rs` 保留 `DynamicScene`、`DYNAMIC_SCENE_FORMAT_VERSION`、`DynamicScene::empty`、`DynamicScene::from_world`、`DynamicScene::spawn_into` 与 `DynamicScene::ensure_supported` public facade，`scene/capture.rs` owns world snapshot capture，`scene/validation.rs` owns format/source/component descriptor validation，`scene/spawn.rs` owns entity remap、component/resource apply 与 plugin-owned component install。`scene::dynamic_scene` re-export surface 与 JSON schema 未变，未触碰 archive/schema/editor/plugin/render 行为。验证：rustfmt check、旧 flat `scene.rs` absent、新 `scene/mod.rs` present、conflict/trailing/diff checks、standalone plan-status 32/32；Cargo 与 focused behavior tests 按 implementation-first 方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene session owner-tree guard | dynamic_scene_session_owner_tree_guard_static_passed_cargo_pending | 2026-06-20 | `dynamic_scene_session_owner_tree_stays_folder_backed_after_runtime_05_cutover` now locks the Runtime 05 session archive owner tree after the hard cutover: `session/mod.rs remains structural`, `session/{metadata,manifest,query,reports,merge,retention,slot_copy,slot_export,slot_import,slot_mutation,capture_retention}` and sibling session owners must stay folder-backed, and `retired flat session owner files stay absent` including old `dynamic_scene/session.rs` plus retired flat child owners. Validation: rustfmt check, standalone component_structure 21/21, standalone plan-status 32/32, direct `runtime_plan_status_boundary_audit` risks=[] support 79/79 status_table_gaps=[], conflict-marker scan clean, scoped `git diff --check` only LF/CRLF warnings; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 dynamic scene root owner-tree guard | dynamic_scene_root_owner_tree_guard_static_passed_cargo_pending | 2026-06-20 | `dynamic_scene_root_owner_tree_stays_folder_backed_after_runtime_05_cutover` now locks the Runtime 05 dynamic-scene root owner tree after the hard cutover: `dynamic_scene/mod.rs` remains structural, `document/entity/scene/scene_asset/spawn_task/value` must stay folder-backed, `retired flat dynamic_scene owner files stay absent` covers old `dynamic_scene/{document,entity,scene,scene_asset,spawn_task,value}.rs` files, and `scene/mod.rs keeps DynamicScene facade` anchors (`DYNAMIC_SCENE_FORMAT_VERSION`, `empty`, `from_world`, `spawn_into`, `ensure_supported`). Validation: rustfmt check, standalone component_structure 23/23, standalone plan-status 32/32, direct `runtime_plan_status_boundary_audit` risks=[] support 79/79 missing_support_files=[] status_table_gaps=[], conflict-marker scan clean, scoped `git diff --check` only LF/CRLF warnings; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 full scene compile-pass graphics-scene blocker | runtime_05_full_scene_gate_compile_passed_graphics_scene_tests_failed_scene_gate_pending | 2026-06-20 | Fresh full gate `cargo test -p zircon_runtime --lib scene:: --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --test-threads=1 --nocapture` wrote `.codex/tmp/zircon_runtime_scene_closeout_20260620_040700.{out,err}.log`; lib-test compilation passed in 40m12s with existing warnings, then the filtered binary reported `running 1074 tests`. The current failure cluster is active render-owned `graphics::scene::*` tests, including render product material/readiness/zshader/runtime-feature and render-graph materialization cases, and the process ended with `STATUS_ACCESS_VIOLATION`, so no total pass/fail summary is available. Runtime 05 closeout remains `pending_full_scene_cargo`; the same full gate must be rerun green after the render lane clears the graphics-scene blocker. |
| 横切 | Runtime 05 full scene closeout failed evidence | cargo_recheck_failed_full_scene_gate | 2026-06-15 | Re-ran `cargo test -p zircon_runtime --lib scene:: --locked` after active cargo/rustc lanes cleared; the gate completed with 880 passed / 31 failed / 0 ignored / 3289 filtered out after a 6m07s compile and 32.70s test run. Failures span graphics scene material/readiness/runtime-feature/graph/HZB/post-process/scene-uniform guards plus scene asset/project IO/ECS query/schedule/event/property/render_extract/world_basics guards, while the workspace still has dirty graphics/render/editor owner changes; Runtime 05 therefore remains `pending_full_scene_cargo` and no completed status is promoted. |
| 横切 | Runtime 05 full scene closeout no-result recheck | cargo_recheck_no_result_external_editor_lane | 2026-06-15 | Re-attempted `cargo test -p zircon_runtime --lib scene:: --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-scene-closeout-0615 --message-format short --color never -- --test-threads=1 --nocapture`; the wrapper exited `SCENE_CLOSEOUT_EXIT=-1` after a compile-phase interruption and wrote only dependency compilation output to `.codex/tmp/zircon_runtime_scene_closeout_20260615_1806.log`, so there is `无测试结果`. Immediately afterward the editor `command_palette` lane owned `E:\cargo-targets\zircon-editor-ui-command-registry-0615`; Runtime 05 remains `pending_full_scene_cargo` and no completed status is promoted. |
| 横切 | Runtime 05 scene:: failure support-first triage | support_first_triage_static_passed_cargo_pending | 2026-06-15 | `runtime_05_full_scene_failure_clusters_keep_support_first_triage_visible` keeps the 880/31 closeout failure mapped to `graphics-scene-lower-layer-candidate`, `scene-asset-project-io-lower-layer-candidate`, and `ecs-scene-lower-layer-candidate`, with `support-first-scene-closeout-triage-before-owner-edits` as the required next step before touching render/editor/runtime owner files. This is static triage evidence only; full `scene::` Cargo closeout remains pending. |
| 横切 | Runtime 05 scene:: lower-layer diagnostic matrix | support_first_matrix_static_passed_cargo_pending | 2026-06-15 | `runtime_05_scene_failure_triage_records_minimum_lower_layer_diagnostics` keeps the `support-first-minimal-diagnostics-matrix` visible across Runtime 05, M0 review, runtime-interface convergence, and this index. `graphics-scene-diagnostics` covers `render_product_streamer_tests`, `render_product_material_property_tests`, `render_pass_executor_registry`, and `deferred_lighting_shader_matches_scene_uniform_layout`; `scene-asset-project-io-diagnostics` covers `scene_asset_toml_roundtrip_preserves_entities_and_bindings`, `scene_assets_roundtrip_primitive_mesh_material_bindings`, and `scene_project_serialization_sources_do_not_store_editor_authoring_state`; `ecs-scene-diagnostics` covers `ecs_query`, `first_stage_updates_all_registered_event_channels`, `render_extract_prepare_flushes_parent_reorder_and_active_changes`, `world_bootstraps_with_renderable_defaults`, and `world_resolves_entity_paths_and_mutates_component_properties`. Next Cargo window should start with `cargo test -p zircon_runtime --lib render_product_streamer_tests --locked`, `cargo test -p zircon_runtime --lib scene_asset --locked`, and `cargo test -p zircon_runtime --lib ecs_query --locked`. |
| 横切 | Runtime 05 scene:: diagnostic matrix source anchors | support_first_matrix_source_anchors_static_passed_cargo_pending | 2026-06-20 | `runtime_05_scene_failure_diagnostic_matrix_source_anchors_exist` now checks the support-first matrix against current source anchors before owner edits: graphics scene anchors cover `render_product_streamer_tests`, `render_product_material_property_tests`, `render_pass_executor_registry`, and `deferred_lighting_shader_matches_scene_uniform_layout`; scene asset/project IO anchors cover `scene_asset_toml_roundtrip_preserves_entities_and_bindings`, `scene_assets_roundtrip_primitive_mesh_material_bindings`, and `scene_project_serialization_sources_do_not_store_editor_authoring_state`; ECS anchors cover `ecs_query`, `first_stage_updates_all_registered_event_channels`, `render_extract_prepare_flushes_parent_reorder_and_active_changes`, `world_bootstraps_with_renderable_defaults`, and `world_resolves_entity_paths_and_mutates_component_properties`. This only tightens status/source verification; full `scene::` Cargo closeout remains pending. |
| 横切 | Runtime 05 render product streamer 2026-06-21 no-result diagnostic | runtime_05_render_product_streamer_20260621_no_result_residual_stopped | 2026-06-21 | 状态锚 `runtime_05_render_product_streamer_20260621_no_result_residual_stopped`；per the support-first lower-layer matrix, retried `cargo test -p zircon_runtime --lib render_product_streamer_tests --locked --jobs 1 --message-format short --color never -- --test-threads=1 --nocapture` with `CARGO_TARGET_DIR=target\codex-runtime05-render-product-streamer-0621`. First attempt exited non-zero after 143s with compile-only output and no diagnostic error line; second attempt timed out after 424s with no test result. Residual processes from this run were stopped: 6 cargo and 3 rustc. No source code was changed, the lowest broken shared layer is still not identified, and `scene_asset` / `ecs_query` lower-layer diagnostics remain pending before any owner-file repair. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| 横切 | Runtime 05 scene_asset 2026-06-21 no-result diagnostic | runtime_05_scene_asset_20260621_no_result_residual_stopped | 2026-06-21 | 状态锚 `runtime_05_scene_asset_20260621_no_result_residual_stopped`；per the support-first lower-layer matrix, ran `cargo test -p zircon_runtime --lib scene_asset --locked --jobs 1 --target-dir target\codex-runtime05-scene-asset-0621 --message-format short --color never -- --test-threads=1 --nocapture`. The command timed out after 604s with no test result. Residual processes from this run were stopped: 4 cargo and 2 rustc. No source code was changed, the lowest broken shared layer is still not identified, and `ecs_query` lower-layer diagnostics remained pending at this checkpoint. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| 横切 | Runtime 05 ecs_query 2026-06-21 no-result diagnostic | runtime_05_ecs_query_20260621_no_result_residual_stopped | 2026-06-21 | 状态锚 `runtime_05_ecs_query_20260621_no_result_residual_stopped`；per the support-first lower-layer matrix, ran `cargo test -p zircon_runtime --lib ecs_query --locked --jobs 1 --target-dir target\codex-runtime05-scene-asset-0621 --message-format short --color never -- --test-threads=1 --nocapture`. The command timed out after 604s with no test result. Residual processes from this run were stopped: 4 cargo and 2 rustc. No source code was changed. The graphics-scene, scene-asset/project-IO, and ECS-scene lower-layer probes have now all failed to produce actionable pass/fail evidence in this local window, so no owner-file repair is justified yet. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| 横切 | Runtime 05 serialization source folder-split guard sync | source_guard_static_passed_cargo_deferred_active_lanes | 2026-06-15 | `scene_project_serialization_sources_do_not_store_editor_authoring_state` now scans `src/asset/assets/scene/mod.rs`, `src/scene/world/project_io/{camera,physics,post_process,references,script,transform}.rs`, and the `project_io.rs` entry owner with `SOURCE_AUTHORING_TOKENS`; this fixes the full `scene::` closeout serialization source path drift without touching production serialization code. Validation: rustfmt check、standalone component_structure 20/20、Python py_compile、direct `scene_project_serialization_boundary_audit` file_count=13 forbidden_location_count=0 risks=[]、direct `runtime_plan_status_boundary_audit` support 27/27 risks=[]、wrapped standalone plan-status 30/30；Cargo package recheck is deferred while active editor/export lanes are compiling. |
| 横切 | Runtime 05 scene/project serialization Markdown renderer split | scene_project_serialization_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `scene_project_serialization_markdown_split_static_passed_cargo_deferred_tests_deferred`；`scene_project_serialization_boundary.py` now mirrors the current folder-backed Rust source guard by scanning 24 runtime scene/project serialization files, including `dynamic_scene/{document,entity,scene,value}/` owners, and keeps authoring-state detection/risk aggregation at 127 lines; `scene_project_serialization_markdown.py` now owns `render_scene_project_serialization_boundary_markdown` at 31 lines. Direct audit reports audited files 24, `forbidden_location_count = 0`, and `risks = []`. Validation: Python py_compile, direct `scene_project_serialization_boundary_audit` risks=[], standalone `component_structure.rs` 20/20, standalone `plan_status.rs` 33/33; full `scene::` Cargo gate remains deferred while external compile lanes are active. |
| 横切 | Runtime 05 scene/editor surface Markdown renderer split | runtime_scene_editor_surface_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `runtime_scene_editor_surface_markdown_split_static_passed_cargo_deferred_tests_deferred`；`runtime_scene_editor_surface.py` now owns only scene/editor neutral-boundary scanning at 92 lines, while `runtime_scene_editor_surface_markdown.py` owns `render_runtime_scene_editor_surface_markdown` at 25 lines. Direct audit reports `editor_named_paths = 0`, `public_editor_named_locations = 0`, `editor_named_locations_sample = 0`, `risks = []`, and `rendered_lines = 3`, preserving the neutral `scene::inspection` boundary without touching runtime/editor production code. Validation: Python py_compile, direct `runtime_scene_editor_surface_audit`, standalone `component_structure.rs` 25/25, standalone `plan_status.rs` 33/33; full `scene::` Cargo gate remains deferred and is not promoted by this audit-only slice. |
| 横切 | Runtime 05 editor_projection residual guard verdict | editor_projection_residual_guard_static_passed_cargo_pending | 2026-06-20 | `scene/tests/component_structure.rs` now documents that the retired editor-projection module name only as a resurrection guard; `scene_components_keep_only_runtime_world_domains_after_editor_boundary_cutover` and `runtime_scene_exposes_neutral_world_inspection_surface` keep the directory absent and prevent the neutral inspection public surface from reintroducing `SceneEditor` / `editor_projection`. Grep verdict: editor_projection text remains guard-owned. Validation: rustfmt check, standalone component_structure 20/20, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 10 Dynamic Session Event Split | folder_split_static_passed_cargo_deferred_active_lanes | 2026-06-14 | `session.rs` 保留私有 Rust-ABI event entry、session registry 与 frame/surface/profile/host-request 编排；`session/events.rs` 成为 pointer/mouse/touch/keyboard/IME/file-drag/window/gamepad/accessibility dispatch 与 camera/menu event helper owner；`runtime_10_dynamic_session_event_split_keeps_abi_owner_and_event_router` 与 `runtime_07_dynamic_session_event_split_keeps_abi_entry_and_event_owner` 锁定该 Dynamic Session Event Split，`dynamic_runtime_api_boundary` 同步 `expected_source_file_count = 21`，`performance_hotpath_boundary` 同步 `large_file_hotspot_count = 38` / `runtime-other=14` / `hotspot_guard_anchor_count = 23`。验证：rustfmt check、Python py_compile、direct dynamic/performance/status audits、aggregate audit JSON assertions、standalone `dynamic_api_session.rs` 4/4、standalone `performance_hotspots.rs` 6/6、standalone `status_output` 2/2；包级 `dynamic_api` / Runtime 07 Cargo gates 因 active compile lanes deferred。 |
| 横切 | Runtime 10 Dynamic Session Test Owner Split | folder_split_static_passed_cargo_deferred_active_lanes | 2026-06-14 | `session/tests.rs` 已删除并切到 folder-backed `session/tests/{mod,helpers,vampire_gameplay,vampire_menu,vampire_hud,frame_diagnostics,runtime_errors}.rs`；`runtime_10_dynamic_session_test_owner_split_keeps_focused_modules` 锁定旧单文件不可复活、`mod.rs` 仅声明、helpers/gameplay/menu/HUD/frame diagnostics/error owners 分离；Runtime 07 `performance_hotpath_boundary` 与 `performance_hotspots.rs` 的 extract/FPS 证据路径同步到 `session/tests/frame_diagnostics.rs`。验证：rustfmt check、Python py_compile、direct performance/status audits、standalone `dynamic_api_session.rs` 5/5、standalone `performance_hotspots.rs` 6/6、standalone `status_output` 2/2；包级 `dynamic_api` / Runtime 07 Cargo gates 因 active compile lanes deferred。 |
| 横切 | Runtime 10 Dynamic API test boundary Markdown renderer split | dynamic_api_test_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `dynamic_api_test_markdown_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_api_test_markdown.py` now owns `render_dynamic_api_test_boundary_markdown(...)`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `dynamic_api_test_boundary.py`; `dynamic_api_test_boundary.py` now owns test-tree audit and risk aggregation at 89 lines, while the Markdown owner is 35 lines. Direct `dynamic_api_test_boundary_audit` reports folder-backed owner modules 11/11, legacy `zircon_runtime/src/dynamic_api/tests.rs` absent, no missing modules/declarations, no oversized modules, and `risks = []`. Validation: Python py_compile and direct `dynamic_api_test_boundary_audit`; broader `dynamic_api`, app loader, and UI/editor Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 10 Dynamic API current audit recheck | dynamic_api_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `dynamic_api_current_audit_static_passed_cargo_pending`；本轮复核 Runtime 10 当前 Dynamic API / interface 边界结构事实：`dynamic_runtime_api_boundary_audit` 报告 source files 33/33、function tables 10/10、field-count mismatches 0、missing `#[repr(C)]` tables 0、runtime session FFI wrappers 11/11、direct session table-entry bypasses 0、session owner extern C false、headless lifecycle anchors 12/12、FFI panic anchors 9/9、loader failure anchors 10/10、behavior-test anchors 16/16、runtime diagnostics anchors 15/15、host-request payload anchors 38/38、UI pending-gate anchors 8/8、UI contract single-source anchors 7/7、duplicate public UI contract types 0、UI v2 contract sync anchors 9/9、pending Cargo gate anchors 5/5、doc anchors 13/13、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `dynamic_runtime_api_boundary_audit` risks=[]、standalone `dynamic_api_session.rs` 9/9；`dynamic_api`、完整 app loader 与 runtime UI/editor Cargo gates 仍 pending。 |
| 横切 | Runtime 10 diagnostics inventory split | runtime_10_dynamic_api_diagnostics_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_10_dynamic_api_diagnostics_inventory_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_diagnostics_inventory.py` now owns Runtime 10 diagnostics anchor inventory outside the 980-line `dynamic_runtime_api_boundary.py`; the audit keeps `runtime_diagnostics_anchors = 15/15` and now reports `scene_asset_reload_diagnostic_path_anchors = 21/21`, `missing_scene_asset_reload_diagnostic_path_anchors = []`, and `risks = []` for the dynamic session scene-asset reload DiagnosticStore projection. Mirror-doc guard synchronization is required before package-level Cargo is promoted; active external render Cargo lanes keep broader `dynamic_api` / app loader / UI gates pending. |
| 横切 | Runtime 10 host-request inventory split | runtime_10_host_request_payload_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_10_host_request_payload_inventory_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_host_request_inventory.py` now owns the 38 Runtime 10 host-request payload anchor inventory outside `dynamic_runtime_api_boundary.py`; the boundary audit stays at source files 33/33 and reports `host_request_payload_anchors = 38/38`, `missing_host_request_payload_anchors = []`, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit` source 33/33 / host-request payload 38/38 / diagnostics 15/15 / scene-asset reload 21/21 / risks=[]; package-level Cargo remains deferred while an external render lane is active. |
| 横切 | Runtime 10 UI contract inventory split | runtime_10_ui_contract_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_10_ui_contract_inventory_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_ui_contract_inventory.py` now owns the Runtime 10 UI pending-gate, UI contract single-source, and UI v2 contract sync anchor inventories outside `dynamic_runtime_api_boundary.py`; the boundary audit reports `ui_pending_gate_anchors = 8/8`, `ui_contract_single_source_anchors = 7/7`, `ui_v2_contract_sync_anchors = 9/9`, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit` source 33/33 / host-request payload 38/38 / UI pending 8/8 / single-source 7/7 / v2 9/9 / risks=[]; package-level Cargo remains deferred while an external render lane is active. |
| 横切 | Runtime 10 validation inventory split | runtime_10_dynamic_api_validation_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_10_dynamic_api_validation_inventory_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_validation_inventory.py` now owns the Runtime 10 behavior-test, pending Cargo gate, doc-anchor, and mirror-doc guard inventories outside `dynamic_runtime_api_boundary.py`; the boundary audit reports `behavior_test_anchor_count = 16`, `missing_behavior_test_anchors = []`, `pending_cargo_gate_anchors = 5/5`, `doc_anchors = 13/13`, `missing_doc_anchors = []`, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit` source 33/33 / behavior-test 16/16 / pending Cargo gates 5/5 / doc anchors 13/13 / risks=[]; rustfmt touched plan-status Rust files; standalone `dynamic_api_session.rs` 10/10 and `plan_status.rs` 33/33; package-level Cargo remains deferred while an external render lane is active. |
| 横切 | Runtime 10 session lifecycle inventory split | runtime_10_session_lifecycle_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_10_session_lifecycle_inventory_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_session_lifecycle_inventory.py` now owns the Runtime 10 headless/minimal lifecycle anchor inventory outside `dynamic_runtime_api_boundary.py`; the boundary audit reports `headless_lifecycle_anchors = 12/12`, `missing_headless_lifecycle_anchors = []`, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit` source 33/33 / headless lifecycle 12/12 / risks=[]; rustfmt touched plan-status Rust files; standalone `dynamic_api_session.rs` 10/10 and `plan_status.rs` 33/33; package-level Cargo remains deferred while an external render lane is active. |
| 横切 | Runtime 10 failure boundary inventory split | runtime_10_failure_boundary_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | 状态锚 `runtime_10_failure_boundary_inventory_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_failure_inventory.py` now owns the Runtime 10 FFI panic and loader failure anchor inventories outside `dynamic_runtime_api_boundary.py`; the boundary audit reports `ffi_panic_anchors = 9/9`, `missing_ffi_panic_anchors = []`, `loader_failure_anchors = 10/10`, `missing_loader_failure_anchors = []`, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit` source 33/33 / FFI panic 9/9 / loader failure 10/10 / risks=[]; rustfmt touched plan-status Rust files; standalone `dynamic_api_session.rs` 10/10 and `plan_status.rs` 33/33; package-level Cargo remains deferred while an external render lane is active. |
| 横切 | Runtime 10 ABI source inventory split | runtime_10_dynamic_api_abi_inventory_split_static_passed_cargo_timeout_no_result_tests_deferred | 2026-06-21 | 状态锚 `runtime_10_dynamic_api_abi_inventory_split_static_passed_cargo_timeout_no_result_tests_deferred`；`dynamic_runtime_api_abi_inventory.py` now owns the Runtime 10 source owner, function-table shape, and session operation inventories outside `dynamic_runtime_api_boundary.py`; the boundary audit reports `expected_source_file_count = 33`, function tables 10/10, `runtime_session_operation_count = 11`, no field-count mismatch, no direct table-entry bypass, `session_owner_extern_c_present = false`, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit` source 33/33 / function-table 10/10 / session operation 11/11 / risks=[]; rustfmt touched plan-status Rust files; standalone `dynamic_api_session.rs` 10/10 and `plan_status.rs` 33/33; `CARGO_TARGET_DIR=E:\cargo-targets\zircon-shared cargo test -p zircon_runtime --lib dynamic_api_session --locked --jobs 1 --message-format short --color never -- --test-threads=1 --nocapture` timed out after 904s with no result, and post-timeout process scan found no residual cargo/rustc/rustdoc; no `dynamic_api` / app loader / UI gate is promoted. |
| 横切 | Runtime 10 runtime API Markdown renderer split | runtime_api_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `runtime_api_markdown_split_static_passed_cargo_deferred_tests_deferred`；`runtime_api_markdown.py` now owns `render_runtime_api_boundary_markdown(...)`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `runtime_api_boundary.py`; `runtime_api_boundary.py` now owns interface ABI facade audit and risk aggregation at 143 lines, while the Markdown owner is 39 lines. Direct `runtime_api_boundary_audit` reports folder-backed owner modules 6/6, facade 12/20 non-empty lines, no missing modules/declarations/re-exports, no forbidden facade declarations, no oversized modules, and `risks = []`. Validation: Python py_compile and direct `runtime_api_boundary_audit`; broader `dynamic_api`, app loader, and UI/editor Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 10 dynamic runtime API Markdown renderer split | dynamic_runtime_api_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `dynamic_runtime_api_markdown_split_static_passed_cargo_deferred_tests_deferred`；`dynamic_runtime_api_markdown.py` now owns `render_dynamic_runtime_api_boundary_markdown(...)`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `dynamic_runtime_api_boundary.py`; `dynamic_runtime_api_boundary.py` remains the 330-line audit/risk owner, while the Markdown owner is 65 lines. Direct `dynamic_runtime_api_boundary_audit` reports source 33/33, function tables 10/10, field-count mismatches 0, missing `#[repr(C)]` tables 0, runtime session wrappers 11/11, direct table-entry bypasses 0, `session_owner_extern_c_present = false`, headless lifecycle 12/12, FFI panic 9/9, loader failure 10/10, behavior 16/16, runtime diagnostics 15/15, scene-asset reload diagnostics 21/21, host-request payload 38/38, UI pending 8/8, UI single-source 7/7, UI duplicate public types 0, UI v2 sync 9/9, pending Cargo gates 5/5, docs 13/13, mirror-doc guard present, and `risks = []`. Validation: Python py_compile and direct `dynamic_runtime_api_boundary_audit`; broader `dynamic_api`, app loader, and UI/editor Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 10 dynamic input mouse-wheel event owner guard | dynamic_input_mouse_wheel_event_owner_guard_focused_cargo_passed_broader_input_pending | 2026-06-21 | 状态锚 `dynamic_input_mouse_wheel_event_owner_guard_focused_cargo_passed_broader_input_pending`；`dynamic_api/tests/input_events.rs` now includes both `include_str!("../session.rs")` and `include_str!("../session/events.rs")` for `mouse_wheel_at_events_decode_delta_bits_for_dynamic_session`, matching the post-split `handle_mouse_wheel` owner. Focused default Cargo passed `1 passed; 0 failed; 4704 filtered out` in `target\codex-runtime12-logs\dynamic_input_mouse_wheel_event_owner_default_20260621.log`; the follow-up Runtime 12 `input` filter now reports `342 passed; 12 failed; 4353 filtered out` in `target\codex-runtime12-logs\input_default_after_mouse_wheel_guard_20260621.log`. This closes only the source-owner guard; `dynamic_api`, app loader, UI/editor, and Runtime 12 input/action_map/gamepad/app gates remain pending. |
| 横切 | Runtime 10 Vampire W input real-backend gate | dynamic_vampire_w_input_real_backend_gate_ignored_without_zr_vm_remaining_ui_input_pending | 2026-06-21 | 状态锚 `dynamic_vampire_w_input_real_backend_gate_ignored_without_zr_vm_remaining_ui_input_pending`；`vampire_project_session_w_key_moves_player_before_input_clear` now carries `requires zr-vm-real-backend and ZR_VM_RUST_BINDING_LIB_DIR` as a default-feature ignored gate instead of failing when the real VM backend is unavailable. Focused default Cargo recorded `0 passed; 0 failed; 1 ignored` in `target\codex-runtime12-logs\vampire_w_input_default_real_backend_gate_20260621.log`; Runtime 12 `input` now reports `342 passed; 11 failed; 1 ignored` in `target\codex-runtime12-logs\input_default_after_vampire_real_backend_gate_20260621.log`, with remaining failures in UI input/text owners. This does not promote real-backend Vampire behavior, `dynamic_api`, app loader, UI/editor, or Runtime 12 input/action_map/gamepad/app gates. |
| 横切 | Runtime 02 root-surface Markdown renderer split | runtime_root_surface_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `runtime_root_surface_markdown_split_static_passed_cargo_deferred_tests_deferred`；`runtime_root_surface_markdown.py` now owns `render_runtime_root_surface_markdown`; `runtime_root_surface.py` remains the 268-line audit/risk owner for the crate root public surface, with a 35-line Markdown owner. Direct audit reports public modules 19/19, public `pub use` sites 2/2, module decisions 19/19, public-use decisions 2/2, unclassified modules/public-use 0/0, root-surface migration debt 0, crate-visible graphics re-export 0, M1 gate `classified-and-clear`, and `risks = []`. Validation passed: Python py_compile, direct `runtime_root_surface_audit`, rustfmt touched status files, standalone root_surface 6/6, standalone core_spine_root_generated 1/1, standalone plan_status 33/33; core/root/generated/export_build_plan/app/editor/plugin Cargo gates remain pending. |
| 横切 | Runtime 10 dynamic_api_session Cargo 验证窗口探测 | cargo_recheck_timeout_static_guards_passed | 2026-06-20 | `cargo test -p zircon_runtime --lib dynamic_api_session --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-10-dynamic-current-0620 --message-format short --color never -- --test-threads=1 --nocapture` 在 604s 工具窗口超时，未生成 `zircon_runtime` 测试二进制或测试结果；进程检查未发现残留 cargo/rustc/rustdoc。当前轻量验证已通过：Python py_compile、direct `dynamic_runtime_api_boundary_audit` risks=[]、standalone `dynamic_api_session.rs` 9/9；`dynamic_api`、app loader、runtime UI/editor Cargo gates 仍 pending，不能由本窗口提升。 |
| 横切 | Runtime 11 JobSystem 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_11_job_system_mirror_docs_match_structure_audit_counts` 锁定 `job_system_boundary` 的 `expected_module_count = 9`、`direct_rayon_paths = 2`、`schedule_parallel_executor_direct_rayon = []`、`diagnostic_anchor_count = 4`、`behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true` 与 `risks = []`；`docs/zircon_runtime/core/job_system.md`、Runtime 11、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `job_system_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 11 assertions、standalone rustc 1/1、conflict/trailing scans 通过；tasks/ecs_schedule/worker_pool/rayon Cargo gates pending，`parallel_frustum.rs` cutover 已在 2026-06-16 静态完成。 |
| 横切 | Runtime 11 JobSystem 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-17 | `job_system_boundary` 与 `runtime_11_job_system_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 11 M1/M3 的 12 个 `tests/tasks.rs` 行为测试锚，新增 `scheduler_wait_all_waits_for_all_handles_and_records_sync_time`；当前 `behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`；Runtime 11、`docs/zircon_runtime/core/job_system.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `job_system_boundary_audit` (`behavior_test_anchor_count = 12`, `risks = []`)、standalone job_system 1/1、standalone plan_status 32/32 通过；tasks/ecs_schedule/worker_pool/rayon Cargo gates pending，`parallel_frustum.rs` cutover 已在 2026-06-16 静态完成。 |
| M1 | Runtime 11 scheduler wait_all 同步点 | wait_all_static_passed_cargo_pending | 2026-06-17 | `JobScheduler::wait_all(&[JobHandle])` 已作为 scheduler-owned 多句柄主线程同步 API 落地，复用 `JobHandle::combine_with_scheduler_diagnostics(...)` 并把等待耗时写入当前调度器的 `tasks.main_thread_wait_ms`；新增行为锚 `scheduler_wait_all_waits_for_all_handles_and_records_sync_time`，同步 JobSystem 模块文档、Runtime 11、M0 review、runtime-interface convergence、`job_system_boundary` 与状态输出表守卫；当前 `behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`。验证：rustfmt check、Python py_compile、direct `job_system_boundary_audit` (`behavior_test_anchor_count = 12`, `risks = []`)、standalone job_system 1/1、standalone plan_status 32/32 通过；tasks/ecs_schedule/worker_pool/rayon Cargo gates pending。 |
| M1 | Runtime 11 panic-safe handle completion | panic_safe_completion_static_passed_cargo_deferred | 2026-06-20 | 状态锚 `panic_safe_completion_static_passed_cargo_deferred`；`JobHandle` 增加 panic terminal state，`JobScheduler::schedule` / `schedule_after` 在 handle-backed task 周围使用 `catch_unwind`，任务 panic 后仍记录 `tasks.completed`、标记 handle 终止并唤醒等待方；`wait()` 在调用线程报告失败，dependent/combined handles 传播依赖 panic 且不运行 dependent task。新增行为锚 `job_handle_wait_reports_task_panic_without_leaking_completion` 与 `schedule_after_propagates_dependency_panic_without_running_dependent_task`；`job_system_boundary` 当前 `behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`。验证：rustfmt/static/standalone guard 见 Runtime 11 子计划；tasks/ecs_schedule/worker_pool/rayon Cargo gates 按“先实现功能”方向 deferred。 |
| 横切 | Runtime 11 graphics frustum rayon cutover | runtime_11_m2_1_graphics_frustum_rayon_cutover_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_11_m2_1_graphics_frustum_rayon_cutover_static_passed_cargo_pending`；`parallel_frustum.rs` 已从 direct rayon 切到 runtime `parallel_for(...)`，`WgpuRenderFramework.compute_task_pool` 由 runtime module construction 复用 `core.task_pools().compute().clone()`；`runtime_absorption::rayon_boundary` 与 `job_system_boundary` 当前锁定 `direct_rayon_paths = 2`，只允许 `core/runtime/tasks/{pool,parallel_for}.rs` 两个 task primitive owner。验证目标：rustfmt check、Python py_compile、standalone rayon_boundary/job_system/Runtime 11 cargo-gate/status-output guards；tasks/ecs_schedule/worker_pool/rayon Cargo gates pending。 |
| 横切 | Runtime 11 JobSystem 2026-06-20 验证窗口探测 | cargo_recheck_timeout_static_guards_passed | 2026-06-20 | `cargo test -p zircon_runtime --lib tasks --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-11-validation-0620 --message-format short --color never -- --test-threads=1 --nocapture` 在 1200s 工具窗口和追加 650s 等待后仍停留在 `zircon_runtime` lib-test 编译，未产出测试二进制或测试结果；本轮残留 Cargo/rustc 进程已停止，不声明 Cargo 通过。轻量验证通过：standalone `job_system.rs` 1/1、standalone `rayon_boundary.rs` 3/3、standalone `asset_worker_policy.rs` 1/1、`asset_worker_policy` rustfmt check；守卫修正为只在 `impl AssetWorkerPool` 内拒绝退休 `AssetWorkerPool::new(worker_count)`，避免误判合法 `AssetWorkerPoolOptions::new(worker_count)`。Runtime 11 仍保留 tasks/ecs_schedule/worker_pool/rayon Cargo gates pending。 |
| 横切 | Runtime 11 JobSystem core-min 验证窗口探测 | core_min_cargo_recheck_timeout_static_guards_passed | 2026-06-20 | 状态锚 `core_min_cargo_recheck_timeout_static_guards_passed`；`cargo test -p zircon_runtime --lib tasks --no-default-features --features core-min --locked --jobs 1 --target-dir E:\Git\ZirconEngine\target\codex-runtime11-coremin-0620 --message-format short --color never -- --test-threads=1 --nocapture` 在 1200s 后仍停留在 `zircon_runtime` lib-test 编译；`target\codex-runtime11-coremin-0620\debug\deps` 无 `zircon_runtime*.exe` test binary（no `zircon_runtime*.exe` test binary），匹配该 target-dir 的残留 Cargo/rustc 进程已停止，不声明 Cargo 通过。轻量验证通过：`job_system_boundary.py` py_compile、direct `job_system_boundary_audit` (`expected_module_count = 9`, `direct_rayon_paths = 2`, `behavior_test_anchor_count = 12`, `risks = []`)、standalone `job_system.rs` 1/1、standalone `rayon_boundary.rs` 3/3；Runtime 11 tasks/ecs_schedule/worker_pool/rayon Cargo gates pending。 |
| 横切 | Runtime 11 JobSystem current audit recheck | job_system_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `job_system_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 11 当前 JobSystem/TaskPool/调度消费边界事实，生产代码未改：`job_system_boundary_audit` 报告 task owner modules 9/9、direct Rayon paths 2/2、`schedule_parallel_executor_direct_rayon = []`、diagnostic anchors 4/4、behavior-test anchors 12/12、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `job_system_boundary_audit` risks=[]、standalone `job_system.rs` 1/1、standalone `rayon_boundary.rs` 3/3、standalone `plan_status.rs` 32/32；tasks/ecs_schedule/worker_pool/rayon Cargo gates 仍 pending。 |
| 横切 | Runtime 11 JobSystem inventory split | job_system_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `job_system_inventory_split_static_passed_cargo_deferred_tests_deferred`；`job_system_source_inventory.py` now owns the Runtime 11 task owner module list, line budget, and direct-Rayon whitelist scan; `job_system_anchor_inventory.py` now owns mod/public/API/schedule executor/behavior-test/mirror-doc anchors outside `job_system_boundary.py`; the boundary audit remains at task owner modules 9/9, direct Rayon paths 2/2, diagnostic anchors 4/4, behavior-test anchors 12/12, `oversized_modules = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile and direct `job_system_boundary_audit` risks=[]; standalone `job_system.rs` 1/1, standalone `rayon_boundary.rs` 3/3, standalone `plan_status.rs` 33/33; package-level tasks/ecs_schedule/worker_pool/rayon Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 11 JobSystem Markdown renderer split | job_system_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `job_system_markdown_split_static_passed_cargo_deferred_tests_deferred`；`job_system_markdown.py` now owns `render_job_system_boundary_markdown`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `job_system_boundary.py`; `job_system_boundary.py` now owns audit read, missing-anchor calculation, and risk aggregation at 193 lines, while the Markdown owner is 64 lines. Direct audit reports task owner modules 9/9, direct Rayon paths 2/2, `schedule_parallel_executor_direct_rayon = []`, diagnostic anchors 4/4, behavior-test anchors 12/12, `oversized_modules = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile and direct `job_system_boundary_audit`; standalone `job_system.rs` 1/1, standalone `rayon_boundary.rs` 3/3, and standalone `plan_status.rs` 33/33; broader tasks/ecs_schedule/worker_pool/rayon Cargo gates remain deferred while external compile lanes are active. |
| M1 | Runtime 11 worker wait-assist | worker_wait_assist_static_passed_cargo_deferred | 2026-06-21 | 状态锚 `worker_wait_assist_static_passed_cargo_deferred`；`JobHandle::wait()` 通过 task-pool-owned `assist_current_thread_once(...)` 在 worker 等待时让当前 Rayon worker 执行同池 pending task，`WORKER_WAIT_IDLE_PARK` 只作为空闲短暂 park；新增 `worker_thread_wait_does_not_deadlock_scheduler`，`job_system_boundary` 当前 `behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、`direct_rayon_paths = 2`、`risks = []`。Validation: rustfmt check、Python py_compile、direct `job_system_boundary_audit`、standalone `job_system.rs` 1/1、standalone `rayon_boundary.rs` 3/3、standalone `plan_status.rs` 33/33；tasks/ecs_schedule/worker_pool/rayon Cargo gates remain deferred. |
| 横切 | Runtime 11 worker wait-assist core-min 验证窗口探测 | worker_wait_assist_core_min_cargo_timeout_no_result_residual_stopped | 2026-06-21 | 状态锚 `worker_wait_assist_core_min_cargo_timeout_no_result_residual_stopped`；`cargo test -p zircon_runtime --lib worker_thread_wait_does_not_deadlock_scheduler --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime11-workerwait-0621 --message-format short --color never -- --test-threads=1 --nocapture` 在 1800s 工具窗口超时，未产出测试结果；`target\codex-runtime11-workerwait-0621\debug\deps` 无 `zircon_runtime*.exe` 测试二进制，仅有 `zircon_runtime-c339c28ec98a5de7.d` 依赖文件；匹配该 target-dir 的 2 个 cargo 与 1 个 rustc 残留进程已停止。并行 render-owned shadow validation 编译通道仍活动；Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gates remain deferred，未声明包级 Cargo 通过。 |
| 横切 | Runtime 11 worker wait-assist core-min test binary 验证 | worker_wait_assist_core_min_test_binary_passed_cargo_gate_pending | 2026-06-21 | 状态锚 `worker_wait_assist_core_min_test_binary_passed_cargo_gate_pending`；复用并复制 render-owned core-min test binary 为 `target\codex-runtime11-workerwait-0621\debug\deps\zircon_runtime-coremin-workerwait.exe` 后直跑 `worker_thread_wait_does_not_deadlock_scheduler --test-threads=1 --nocapture`，结果 `1 passed; 0 failed; 4687 filtered out`。该证据覆盖 worker wait-assist 行为在当前 core-min test binary 中可运行通过，但不是 `cargo test -p zircon_runtime --lib tasks/ecs_schedule/worker_pool/rayon` 完整 gate；Runtime 11 包级 Cargo gates 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 core-min test binary task/guard batch | runtime_11_core_min_test_binary_task_guard_batch_passed_cargo_gate_pending | 2026-06-21 | 状态锚 `runtime_11_core_min_test_binary_task_guard_batch_passed_cargo_gate_pending`；继续用 `target\codex-runtime11-workerwait-0621\debug\deps\zircon_runtime-coremin-workerwait.exe` 直跑 Runtime 11 相关过滤项：`tests::tasks::` 为 `18 passed; 0 failed; 4670 filtered out`，`worker_pool` 为 `10 passed; 0 failed; 4678 filtered out`，`rayon` 为 `4 passed; 0 failed; 4684 filtered out`，`runtime_absorption::job_system` 为 `1 passed; 0 failed; 4687 filtered out`，`runtime_absorption::rayon_boundary` 为 `3 passed; 0 failed; 4685 filtered out`，`runtime_absorption::asset_worker_policy` 为 `1 passed; 0 failed; 4687 filtered out`。该批次覆盖 core-min test binary 中的任务模型、worker pool、Rayon 边界、JobSystem 镜像和 Runtime 04/11 asset worker policy 守卫；完整 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 ecs_schedule source-guard lifetime anchor repair | runtime_11_ecs_schedule_lifetime_guard_anchor_static_passed_rebuild_pending | 2026-06-21 | 状态锚 `runtime_11_ecs_schedule_lifetime_guard_anchor_static_passed_rebuild_pending`；复用 core-min test binary 直跑 `ecs_schedule` 得到 `74 passed; 1 failed; 4613 filtered out`，唯一失败 `world_driver_reuses_tick_schedule_snapshots_for_stage_runs` 是源码守卫仍匹配旧锚 `native_steps: &[Self]`；当前实现 `scheduled_scene_step.rs` 已是 `native_steps: &'a [Self]` 且无 `native_steps: Vec<Self>`。已将守卫更新为 `native_steps: &'a [Self]` 并通过 rustfmt/source-anchor scan；由于 render-owned Cargo/rustc lane 活动，本修复仍需重建 test binary 后复跑 `ecs_schedule`，不提升 Runtime 11 Cargo gate。 |
| 横切 | Runtime 11 ecs_schedule core-min Cargo 复验 | runtime_11_core_min_ecs_schedule_cargo_passed_remaining_gates_pending | 2026-06-21 | 状态锚 `runtime_11_core_min_ecs_schedule_cargo_passed_remaining_gates_pending`；修正 Runtime 11 `ecs_schedule` stale source guards 后，运行 `cargo test -p zircon_runtime --lib ecs_schedule --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime-shadow-spot-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\ecs_schedule_coremin_20260621-142346.{out,err}.log` 记录 `75 passed; 0 failed; 4616 filtered out`（既存 54 warnings）。本轮同步锚包括 `native_steps: &'a [Self]`、显式 `internal_system_counts` / `native_step_counts`、runtime-aware taken-id/step-count/conflict-count 与 native registration variants + restore 计数；该证据关闭 core-min `ecs_schedule` 复验窗口，但 Runtime 11 `tasks`/`worker_pool`/`rayon` Cargo gates 与更宽配置仍 pending，未声明 completed。 |
| 横切 | Runtime 11 tasks core-min Cargo 复验 | runtime_11_core_min_tasks_cargo_passed_remaining_gates_pending | 2026-06-21 | 状态锚 `runtime_11_core_min_tasks_cargo_passed_remaining_gates_pending`；运行 `cargo test -p zircon_runtime --lib tasks --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime-shadow-spot-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\tasks_coremin_20260621-145417.{out,err}.log` 记录 `19 passed; 0 failed; 4673 filtered out`（panic-propagation 用例按预期打印 panic 后最终通过，既存 54 warnings）。该证据关闭 core-min `tasks` 复验窗口；完整守卫锚 `tasks/ecs_schedule/worker_pool/rayon` 继续可见，剩余 `worker_pool`/`rayon` Cargo gates 与更宽配置仍 pending，未声明 completed。 |
| 横切 | Runtime 11 worker_pool core-min Cargo 复验 | runtime_11_core_min_worker_pool_cargo_passed_remaining_gates_pending | 2026-06-21 | 状态锚 `runtime_11_core_min_worker_pool_cargo_passed_remaining_gates_pending`；运行 `cargo test -p zircon_runtime --lib worker_pool --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime-shadow-spot-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\worker_pool_coremin_20260621-153418.{out,err}.log` 记录 `10 passed; 0 failed; 4682 filtered out`（既存 54 warnings）。该证据关闭 core-min `worker_pool` 复验窗口；完整守卫锚 `tasks/ecs_schedule/worker_pool/rayon` 继续可见，剩余 `rayon` Cargo gate 与更宽配置仍 pending，未声明 completed。 |
| 横切 | Runtime 11 rayon core-min Cargo 复验 | runtime_11_core_min_rayon_cargo_passed_broader_gates_pending | 2026-06-21 | 状态锚 `runtime_11_core_min_rayon_cargo_passed_broader_gates_pending`；运行 `cargo test -p zircon_runtime --lib rayon --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime-shadow-spot-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\rayon_coremin_20260621-155517.{out,err}.log` 记录 `4 passed; 0 failed; 4688 filtered out`（既存 54 warnings）。至此 core-min `tasks/ecs_schedule/worker_pool/rayon` 四个过滤项均通过；默认/更宽配置 Cargo gate 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 tasks default Cargo 复验 | runtime_11_default_tasks_cargo_passed_remaining_default_gates_pending | 2026-06-21 | 状态锚 `runtime_11_default_tasks_cargo_passed_remaining_default_gates_pending`；运行 `cargo test -p zircon_runtime --lib tasks --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\tasks_default_20260621-161421.{out,err}.log` 记录默认配置 test binary 编译通过并运行 `19 passed; 0 failed; 4673 filtered out`（panic-propagation 用例按预期打印 panic 后最终通过，既存 53 warnings）。该证据关闭默认配置 `tasks` 复验窗口；默认/更宽配置 `worker_pool`/`rayon`/`ecs_schedule` gates 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 worker_pool default Cargo 复验 | runtime_11_default_worker_pool_cargo_passed_remaining_default_gates_pending | 2026-06-21 | 状态锚 `runtime_11_default_worker_pool_cargo_passed_remaining_default_gates_pending`；运行 `cargo test -p zircon_runtime --lib worker_pool --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\worker_pool_default_20260621-165855.{out,err}.log` 记录默认配置 test binary 编译通过并运行 `10 passed; 0 failed; 4683 filtered out`（既存 53 warnings）。该证据关闭默认配置 `worker_pool` 复验窗口；默认/更宽配置 `rayon`/`ecs_schedule` gates 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 rayon default Cargo 复验 | runtime_11_default_rayon_cargo_passed_full_lib_gate_pending | 2026-06-21 | 状态锚 `runtime_11_default_rayon_cargo_passed_full_lib_gate_pending`；运行 `cargo test -p zircon_runtime --lib rayon --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\rayon_default_20260621-171236.{out,err}.log` 记录默认配置 test binary 编译通过并运行 `4 passed; 0 failed; 4690 filtered out`（既存 53 warnings）。该证据关闭默认配置 `rayon` 复验窗口；默认配置 `ecs_schedule` 与收尾全量 `cargo test -p zircon_runtime --lib --locked` gate 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 ecs_schedule default Cargo 复验 | runtime_11_default_ecs_schedule_cargo_passed_full_lib_gate_pending | 2026-06-21 | 状态锚 `runtime_11_default_ecs_schedule_cargo_passed_full_lib_gate_pending`；运行 `cargo test -p zircon_runtime --lib ecs_schedule --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\ecs_schedule_default_20260621-172513.{out,err}.log` 记录默认配置 test binary 编译通过并运行 `75 passed; 0 failed; 4619 filtered out`（既存 53 warnings）。至此 core-min 与默认配置 `tasks/ecs_schedule/worker_pool/rayon` 过滤项均已通过；收尾全量 `cargo test -p zircon_runtime --lib --locked` gate 仍 pending，未声明 completed。 |
| 横切 | Runtime 11 full-lib default Cargo closeout attempt | runtime_11_full_lib_cargo_timeout_with_broader_failures_observed | 2026-06-21 | 状态锚 `runtime_11_full_lib_cargo_timeout_with_broader_failures_observed`；运行 `cargo test -p zircon_runtime --lib --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture`，1200s 工具窗口超时；日志 `target\codex-runtime11-logs\full_lib_default_20260621-173046.{out,err}.log` 未出现最终 `test result:`，最后一行停在 `test graphi`，超时前已观察到 58 条 `... FAILED`，跨 asset、core runtime structure、dynamic_api session、graphics/post-process/materialization/project-render 等 broader non-11 filters；匹配该 target-dir 的 cargo/rustc/test 残留进程为 0。该证据阻塞收尾全量 gate，Runtime 11 仍 in_progress，未声明 completed。 |
| 横切 | Runtime 11 core runtime full-lib triage recheck | runtime_11_core_runtime_tests_passed_full_lib_gate_broader_failures_pending | 2026-06-21 | 状态锚 `runtime_11_core_runtime_tests_passed_full_lib_gate_broader_failures_pending`；full-lib broader failure 的 core runtime 低层组已收束：activation empty startup/shutdown fast path、typed unload-order slice 传递、event bus prune/registration/registry-name/resolution 等 source guards 已更新。验证：`registration_source_preserves_hot_path_structure` 单测 `1 passed; 0 failed; 4694 filtered out`（日志 `target\codex-runtime11-logs\registration_structure_20260621-195458.log`）；`cargo test -p zircon_runtime --lib core::runtime::tests:: --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture` 通过 `82 passed; 0 failed; 4613 filtered out`（日志 `target\codex-runtime11-logs\core_runtime_all_20260621-200647.log`）。该证据只关闭 core runtime broader slice；asset/dynamic_api/graphics broader gates 仍 pending，Runtime 11 不提升 completed。 |
| 横切 | Runtime 11 asset broader failure triage core-min 复验 | runtime_11_asset_tests_passed_full_lib_gate_dynamic_graphics_pending | 2026-06-21 | 状态锚 `runtime_11_asset_tests_passed_full_lib_gate_dynamic_graphics_pending`；full-lib broader failure 的 asset 低层组已收束：scene mesh direct bincode、UI v1/v2 artifact cache、stacked array RGBA8 upload-readiness fixture 与 vampire shader material/light marker 均已修复/同步。验证：`importer_texture_fixture_reinterprets_stacked_array_layout` core-min Cargo 通过 `1 passed; 0 failed; 4696 filtered out`（日志 `target\codex-runtime11-logs\asset_texture_stacked_array_coremin_20260621.log`）；direct `vampire_example_manifest_scene_and_scripts_are_importable` 通过 `1 passed; 0 failed; 4696 filtered out`；direct `asset::tests::` 通过 `363 passed; 0 failed; 4334 filtered out`（日志 `target\codex-runtime11-logs\asset_namespace_coremin_direct_20260621.log`）。该证据只关闭当前 asset broader slice；dynamic_api/graphics 和默认 full-lib gate 仍 pending，Runtime 11 不提升 completed。 |
| 横切 | Runtime 11 full-lib default after asset triage recheck | runtime_11_full_lib_after_asset_recheck_blocked_graphics_compile_timeout | 2026-06-21 | 状态锚 `runtime_11_full_lib_after_asset_recheck_blocked_graphics_compile_timeout`；asset slice 关闭后再次运行默认全库 `cargo test -p zircon_runtime --lib --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture`，日志 `target\codex-runtime11-logs\full_lib_default_after_asset_20260621.log` 在编译阶段命中 graphics execution-record 漂移：`render_graph_execution_record.rs:871:22` 报 `RenderExposureReadbackReport` 未解析；随后窄过滤 `execution_record_preserves_exposure_readback_report`（日志 `target\codex-runtime11-logs\graphics_execution_record_exposure_default_20260621.log`）在 904s 工具窗口内无最终结果，匹配 target-dir/filter 的 2 cargo + 1 rustc 残留已停止。该证据只说明 default full-lib gate 仍由 graphics/default 编译验证链路阻塞；core runtime 与 asset 低层切片不回退，dynamic_api/graphics gate 仍 pending，Runtime 11 不提升 completed。 |
| 横切 | Runtime 11 full-lib default after graphics exposure retry | runtime_11_full_lib_after_graphics_exposure_retry_timeout_104_broader_failures | 2026-06-21 | 状态锚 `runtime_11_full_lib_after_graphics_exposure_retry_timeout_104_broader_failures`；默认配置窄过滤 `execution_record_preserves_exposure_readback_report` 复验通过 `1 passed; 0 failed; 4702 filtered out`（日志 `target\codex-runtime11-logs\graphics_execution_record_exposure_default_retry_20260621.log`），说明 graphics exposure-readback 编译阻断已收束。随后默认全库复验日志 `target\codex-runtime11-logs\full_lib_default_after_graphics_exposure_retry_20260621.log` 在 1800s 工具窗口超时，已进入 `running 4704 tests` 并观察到 104 条 `... FAILED`，失败簇跨 post-process/render graph、dynamic_api session、graphics product/render-framework、input boundary、native/plugin export、scene dynamic/session 与 extension/graphics-surface guards；本 target-dir 无 cargo/rustc 残留，外部 `target\codex-runtime-postprocess-0621` Cargo lane 未触碰。Runtime 11 过滤项、core runtime 与 asset 低层切片不回退；默认 full-lib gate 仍被跨计划 broader failures 阻塞，Runtime 11 不提升 completed。 |
| 横切 | Runtime 05 plan-status 输出表守卫 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::plan_status::status_output_tables::runtime_plan_status_output_tables_cover_index_and_all_subplans` 锁定 runtime 总索引与 01-14 子计划均保留 `## 状态与产出记录` 表；`runtime_index_status_output_records_recent_cross_plan_slices` 锁定总索引记录 Runtime 01/02/03/04/06/07/08/09/10/11/12/13/14 镜像文档守卫的 standalone rustc 证据与 pending Cargo gates。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit`（support 7/7、subplans 14/14、core 15/15、pending 15/15、doc 9/9、status_table_gaps []、risks []）、standalone rustc `status_output` 2/2 通过；full scene:: Cargo gate 仍 pending。 |
| 横切 | Runtime 05 plan-status 审计元数据守卫 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 维护 `status_output_table_guard_count = 4`、`missing_status_output_table_guard_anchors = []`、`status_output_table_guard_present = true`，把 `runtime_plan_status_output_tables_cover_index_and_all_subplans`、`runtime_index_status_output_records_recent_cross_plan_slices`、all runtime index status rows 与 full coverage guard 提升为显式审计字段；Runtime 05、M0 review、runtime-interface convergence 与本总索引已同步，full `scene::` Cargo gate 仍 pending。 |
| 横切 | Runtime 05 plan-status Cargo attempt 状态审计 | cargo_attempt_status_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 `cargo_attempt_status_anchor_count = 20`、`missing_cargo_attempt_status_index_anchors = []`、`missing_cargo_attempt_status_runtime_14_anchors = []`、`missing_cargo_attempt_status_guard_anchors = []`、`cargo_attempt_status_guard_present = true`，锁定 `cargo_deferred_active_lane` / `cargo_blocked_external_compile_drift` / `cargo_recheck_blocked_external_ui_compile_drift` / `cargo_recheck_timeout_no_result` 四类 Cargo attempt 状态必须同时保留在 runtime index、Runtime 14 子计划与 `status_output_tables` 守卫中；已记录 `Runtime 14 Cargo 验证窗口探测`、`Runtime 14 animation Cargo gate 尝试`、`Runtime 14 animation Cargo gate 修复与复验阻塞` 与 `Runtime 14 animation runtime-status focused recheck timeout`，但 full `scene::` Cargo gate 与 Runtime 14 remaining gates 仍 pending。 |
| 横切 | Runtime 05 plan-status Cargo timeout 状态审计 | cargo_attempt_timeout_status_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_boundary` 将 Runtime 14 focused recheck timeout 纳入 Cargo attempt 审计：`cargo_attempt_status_anchor_count = 20`、runtime index 20/20、Runtime 14 plan 20/20、status-output guards 8/8、`cargo_attempt_status_guard_present = true`，新增锁定 `cargo_recheck_timeout_no_result`、`Runtime 14 animation runtime-status focused recheck timeout`、`runtime_status_reports_player_rig_and_gpu_readiness`、`904s`、`无测试结果` 与 `residual cargo/rustc processes were stopped`；验证为 Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、aggregate `audit_runtime_structure.py --json` plan-status assertions 和 standalone `status_output_tables.rs` 2/2。该行只记录无测试结果的超时复验，Runtime 14 remaining gates 与 Runtime 05 full `scene::` Cargo gate 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 07 scene asset rows | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 07 scene asset owner split`、`Runtime 07 scene asset split-drift repair`、`Runtime 07 scene asset folder-split public-surface guard` 与 `Runtime 07 scene asset guard 纳入 performance_hotpath_boundary` 四条状态产出记录，要求保留 `hotspot_guard_anchor_count = 20`、standalone `...performance_hotspots.rs` 6/6、`scene_asset` / Runtime 07 Cargo gates pending 与三类 deferred status；Runtime 05 子计划已同步该状态表闭环，full `scene::` Cargo gate 与 Runtime 07 Cargo gates 仍 pending。 |
| 横切 | Runtime 05 Runtime 07 scene status 审计元数据 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 Runtime 07 scene status 显式审计字段：`runtime_07_scene_status_index_anchor_count = 11`、`runtime_07_scene_status_guard_anchor_count = 10`、`runtime_07_scene_status_doc_anchor_count = 4`、`missing_runtime_07_scene_status_index_anchors = []`、`missing_runtime_07_scene_status_guard_anchors = []`、`missing_runtime_07_scene_status_doc_anchors = []`、`runtime_07_scene_status_guard_present = true`；direct audit 报告 index 11/11、status-output guard 10/10、mirror docs 4/4，full `scene::` Cargo gate 与 Runtime 07 Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 02 generated template row | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 02 generated template count 审计同步` 行，要求保留 `template_file_count=10`、generated export templates 10/10、0 migration debt 与 stale 9/9 scan 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2、conflict/trailing scans 与 scoped `git diff --check` 通过（仅 LF-to-CRLF warnings）；Runtime 02 generated/export/app/editor/plugin Cargo gates 仍 pending。 |
| 横切 | Runtime 05 Runtime 02 generated status 审计元数据 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 Runtime 02 generated status 显式审计字段：`runtime_02_generated_status_index_anchor_count = 6`、`runtime_02_generated_status_guard_anchor_count = 5`、`runtime_02_generated_status_doc_anchor_count = 3`、`missing_runtime_02_generated_status_index_anchors = []`、`missing_runtime_02_generated_status_guard_anchors = []`、`missing_runtime_02_generated_status_doc_anchors = []`、`runtime_02_generated_status_guard_present = true`；direct audit 报告 index 6/6、status-output guard 5/5、mirror docs 3/3，full `scene::` Cargo gate 与 Runtime 02 generated/export/app/editor/plugin Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 07 owner-budget row | status_table_static_passed_cargo_pending | 2026-06-20 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 07 owner-budget 30-hotspot current audit sync` 行，要求保留 `large_file_hotspot_count = 30`、`runtime-other=13`、`editor-retained-host=3`、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=30 与 standalone `performance_hotspots.rs` 6/6 证据；Runtime 05 子计划已同步该状态表闭环。验证目标：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `plan_status.rs` 32/32；Runtime 07 extract/ecs_query/performance profiling/FPS Cargo gates 仍 pending。 |
| 横切 | Runtime 05 plan-status owner-budget current mirror fix | status_table_static_passed_cargo_pending | 2026-06-20 | Runtime 05 `计划状态镜像` 当前段已同步到当前 Runtime 07 owner-budget mirror：`large_file_hotspot_count = 30`、`runtime-framework-render=3`、`runtime-other=13` 与 `Runtime 07 owner-budget 30-hotspot current audit sync`。`runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs` now locks those anchors；验证目标：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=[]、standalone plan-status。This is a static mirror fix only; Runtime 05 full `scene::` and Runtime 07 extract/ecs_query/performance profiling/FPS Cargo gates remain pending. |
| 横切 | Runtime 05 Runtime 02 root_entries count 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-16 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 02 root_entries guard-count current resync` 行，要求保留 `EXPECTED_ROOT_ENTRIES_TEST_COUNT`、`root_entries guard tests 13/13`、`guard_test_anchor_count = 26` 与 standalone core_spine_root_generated 1/1 证据；该行只同步 Runtime 02/14 共享 root_entries 守卫期望，不提升 Runtime 02 Cargo gates 或 Runtime 05 full `scene::` closeout。验证：rustfmt check、Python py_compile、stale 21/baseline scan clean、standalone plan-status 32/32。 |
| 横切 | Runtime 05 status-output current anchor fix | status_table_static_passed_cargo_pending | 2026-06-16 | 将总索引状态表当前证据锚同步到 status-output 守卫期望：`runtime_02_generated_status_guard_anchor_count = 5`、`runtime_07_owner_budget_status_guard_anchor_count = 5`、`runtime_09_m1_2_navigation_legacy_reply_renamed_static_passed_cargo_pending`、Runtime 10 `standalone rustc 5/5`、`runtime_11_m2_1_graphics_frustum_rayon_cutover_static_passed_cargo_pending` 与 Runtime 12 `standalone rustc 4/4`；direct `runtime_plan_status_boundary_audit` 仍报告 `missing_runtime_02_generated_status_guard_anchors = []`、`missing_runtime_07_owner_budget_status_guard_anchors = []`、`risks = []`，wrapped standalone status-output 2/2 通过；该行只同步状态表元数据，不提升 Runtime 02/07/09/10/11/12 Cargo gates 或 Runtime 05 full `scene::` closeout。 |
| 横切 | Runtime 05 status-output Runtime 12 gamepad event-owner row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 12 gamepad event-owner 漂移同步` 行，要求保留 `session/events.rs`、`InputEvent::Gamepad*`、`missing_gamepad_abi_anchors = []` 与 standalone `input_stack.rs` rustc 4/4 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2、conflict/trailing/diff checks 通过（仅 LF-to-CRLF warnings）；Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 12 behavior-test row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 12 Input stack 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 6`、`missing_behavior_test_anchors = []`、standalone input_stack 4/4 与 standalone status-output 2/2 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。 |
| 横切 | Runtime 12 cursor host requests | cursor_host_request_static_passed_cargo_deferred | 2026-06-20 | `CursorGrabMode`、`CursorPosition` 与 `CursorHostRequest` 已加入 runtime 输入公共契约，`InputEvent::CursorHostRequest`、`InputFrameSnapshot::cursor_host_requests`、`InputManager::drain_cursor_host_requests()` 与 `DefaultInputManager` frame-local queue/drain 路径已同步；`RuntimeDynamicSession::drain_host_requests()` 将其编码为 `ZrRuntimeHostRequestV1::Cursor`，runtime preview host 的 `apply_runtime_cursor_host_request(...)` 通过 winit `set_cursor_visible`、`set_cursor_grab`、`set_cursor_hittest` 与 `set_cursor_position` 应用请求，desktop `platform.cursor_options` 已提升为 `supported:winit_window_options`。行为锚 `cursor_host_requests_are_frame_local_and_drainable` 纳入 Runtime 12 输入测试与结构守卫。`input_stack_boundary` 当前镜像为 `expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 15`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；input/action_map/gamepad/app Cargo gates 仍 pending。 |
| 横切 | Runtime 12 input validation window recheck | cargo_recheck_timeout_static_guards_passed | 2026-06-20 | `cargo test -p zircon_runtime --lib input --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-12-input-validation-0620 --message-format short --color never -- --test-threads=1 --nocapture` timed out after 1200s with no test result; residual matching cargo/rustc processes were stopped. Lightweight guard evidence passed: direct `input_stack_boundary_audit` risks=[] (12/20/7, public 26, behavior 15), standalone `input_stack.rs` 4/4, rustfmt check. input/action_map/gamepad/app Cargo gates pending. |
| 横切 | Runtime 12 Input stack current audit recheck | input_stack_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `input_stack_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 12 当前输入栈/动作映射/cursor host request 边界事实，生产代码未改：`input_stack_boundary_audit` 报告 runtime/framework/test owner 12/20/7、public surface anchors 26/26、Runtime 12 guard anchors 5/5、behavior-test anchors 15/15、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `input_stack_boundary_audit` risks=[]、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32；input/action_map/gamepad/app Cargo gates 仍 pending。 |
| 横切 | Runtime 12 Input stack inventory split | input_stack_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `input_stack_inventory_split_static_passed_cargo_deferred_tests_deferred`；`input_stack_source_inventory.py` 接管 Runtime 12 runtime/framework/test owner module 清单和 line-budget 常量，`input_stack_anchor_inventory.py` 接管 module declarations、public surface、action evaluator、gamepad ABI、cursor host-request、guard/doc/test/behavior/Cargo gate anchors；`input_stack_boundary.py` 保留审计读取、缺失计算、风险聚合并降到 337 行，`input_stack_markdown.py` 承接 Markdown 渲染并为 108 行。direct audit 仍报告 runtime/framework/test owner 12/20/7、public surface anchors 26/26、Runtime 12 guard anchors 5/5、behavior-test anchors 15/15、cursor host-request anchors 12/12、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `input_stack_boundary_audit`、rustfmt touched plan-status guards、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 33/33；外部 render/editor Cargo lanes 活跃，本切片不启动 package-level Cargo。 |
| 横切 | Runtime 12 Input stack Markdown renderer split | input_stack_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `input_stack_markdown_split_static_passed_cargo_deferred_tests_deferred`；`input_stack_markdown.py` now owns `render_input_stack_boundary_markdown`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `input_stack_boundary.py`; `input_stack_boundary.py` now owns audit read, missing-anchor calculation, and risk aggregation at 337 lines, while the Markdown owner is 108 lines. Direct audit reports runtime/framework/test owner 12/20/7, public surface anchors 26/26, Runtime 12 guard anchors 5/5, behavior-test anchors 15/15, cursor host-request anchors 12/12, missing doc/test/behavior/Cargo anchors [], oversized modules [], `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile and direct `input_stack_boundary_audit`; standalone `input_stack.rs` 4/4 and standalone `plan_status.rs` 33/33; broader input/action_map/gamepad/app Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 12 input boundary grouped manager import guard repair | input_boundary_grouped_manager_import_guard_passed_broader_input_failures_pending | 2026-06-21 | 状态锚 `input_boundary_grouped_manager_import_guard_passed_broader_input_failures_pending`；full-lib broader failure 中的 `input_protocol_types_live_in_runtime_input_surface` 是测试字符串锚漂移，当前 `core::manager::resolver` 已从 `crate::core::framework::input::{InputActionManager, InputManager}` grouped import 取得输入管理器契约；`input/tests/boundary.rs` 已接受 grouped import 并继续禁止 manager 重新导出 input protocol types。验证：rustfmt check 通过；focused default Cargo `input_protocol_types_live_in_runtime_input_surface` 通过 `1 passed; 0 failed; 4704 filtered out`（日志 `target\codex-runtime12-logs\input_boundary_grouped_manager_import_default_20260621.log`）。Runtime 10 dynamic input mouse-wheel owner 与 Vampire real-backend gate 修复后，较宽 `cargo test -p zircon_runtime --lib input --locked ...` 记录 `342 passed; 11 failed; 1 ignored; 4353 filtered out`（日志 `target\codex-runtime12-logs\input_default_after_vampire_real_backend_gate_20260621.log`），剩余为 UI input routing/ownership、MUI text/IME/pointer 上层消费路径；Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output Runtime 04 behavior-test row | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 04 Asset pipeline 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 20`、`missing_behavior_test_anchors = []`、standalone asset_pipeline 1/1 与 standalone status-output 2/2 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；Runtime 04 broader asset::/worker_pool Cargo gates 仍 pending。 |
| 横切 | Runtime 05 Runtime 07 owner-budget status 审计元数据 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 Runtime 07 owner-budget status 显式审计字段：`runtime_07_owner_budget_status_index_anchor_count = 7`、`runtime_07_owner_budget_status_guard_anchor_count = 6`、`runtime_07_owner_budget_status_doc_anchor_count = 3`、`missing_runtime_07_owner_budget_status_index_anchors = []`、`missing_runtime_07_owner_budget_status_guard_anchors = []`、`missing_runtime_07_owner_budget_status_doc_anchors = []`、`runtime_07_owner_budget_status_guard_present = true`；direct audit 报告 index 7/7、status-output guard 6/6、mirror docs 3/3，full `scene::` Cargo gate 与 Runtime 07 Cargo gates 仍 pending。 |
| 横切 | Runtime 05 recent-static Runtime 02/07 status metadata guard | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs` 现在锁定 Runtime 05 子计划、runtime 总索引与 M0 review 必须保留 `runtime_02_generated_status_guard_present = true` 和 `runtime_07_owner_budget_status_guard_present = true`，确保 Runtime 02 generated template 与 Runtime 07 owner-budget 状态审计元数据不会只存在于 Python 审计输出中。验证：rustfmt check、standalone recent_static 1/1、direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo gate 与 Runtime 02/07 Cargo gates 仍 pending。 |
| 横切 | Runtime 05 status-output recent-static metadata row | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 05 recent-static Runtime 02/07 status metadata guard` 行，要求保留 `runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs`、`runtime_02_generated_status_guard_present = true`、`runtime_07_owner_budget_status_guard_present = true` 与 standalone recent_static 1/1 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、standalone status-output 2/2、direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo gate 与 Runtime 02/07 Cargo gates 仍 pending。 |
| 横切 | Runtime 05 non-network server UI sortingMode allowlist | static_audit_passed_cargo_pending | 2026-06-14 | `non_network_server_naming` 现在把 runtime UI Table/DataGrid `sortingMode = "server"` 字面值列为外部 UI API allowed context，而不是 `unclassified-non-network-server` owner debt；当前审计 count 59、classification groups 2、allowed_context_count 99、observer_false_positive_count 87、unclassified_location_count 0，剩余风险只是不应由 Runtime 05 跨会话处理的 graphics render-framework 与 editor workbench authority-label 已分类命名债。验证：Python py_compile、aggregate `audit_runtime_structure.py --json` non-network assertions；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 status-output non-network server allowlist row | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定本总索引中的 `Runtime 05 non-network server UI sortingMode allowlist` 行，要求保留 `sortingMode = "server"`、`allowed_context_count 99`、`unclassified_location_count 0` 与 aggregate `audit_runtime_structure.py --json` non-network assertions 证据；Runtime 05 子计划已同步该状态表闭环。验证：rustfmt check、standalone status-output 2/2、direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 naming_boundary non-network server Rust guard | naming_guard_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::naming_boundary` 新增 `runtime_non_network_server_naming_is_classified_by_owner`，Rust 侧扫描 `zircon_runtime/src` 的非网络 `server` 命名，允许真实网络/target-server/dev-server/API 字面值，保留 graphics render-framework 已分类 owner debt，并把 `sortingMode = "server"` 视为外部 UI API literal；同切片把 runtime UI `state_reducer` 的 `field-editor` / `source-editor` 纳入 `runtime-ui-component-catalog-editor-controls` 桶。验证：rustfmt check、standalone naming_boundary 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 non-network server Markdown renderer split | non_network_server_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | Status anchor `non_network_server_markdown_split_static_passed_cargo_deferred_tests_deferred`; `non_network_server_naming_markdown.py` owns `render_non_network_server_naming_markdown(...)`, while `non_network_server_naming.py` remains the 323-line token scan/allowed-context/classification/risk owner. Direct audit reports count 77, classification_count 2, allowed_context_count 94, observer_false_positive_count 95, unclassified_location_count 0, non_network_server_migration_debt_count 2, Markdown owner 41 lines, rendered_lines 30. Validation: Python py_compile, direct `non_network_server_references`, and standalone `plan_status.rs` 33/33; full Runtime 05 `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 runtime naming Markdown renderer split | runtime_naming_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `runtime_naming_markdown_split_static_passed_cargo_deferred_tests_deferred`；`runtime_naming_markdown.py` now owns `render_runtime_naming_boundary_markdown` and per-term Markdown rendering, including explicit unclassified counts; `runtime_naming_boundary.py` remains the 394-line classification/audit owner with `state_reducer` editor references classified as `runtime-ui-component-catalog-editor-controls`, while the Markdown owner is 35 lines. Direct audit reports `gate_status = classified`, editor locations/files: 1556 / 281, `editor_unclassified = 0`, legacy locations/files: 491 / 101, `legacy_unclassified = 0`, and `legacy_debt_count = 7`. Validation passed: Python py_compile, direct `runtime_naming_boundary_audit`, rustfmt check for touched status files, standalone naming_boundary 2/2, standalone plan_status 33/33; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 texture importer DDS caps policy wording | hard_cutover_dds_caps_policy_static_passed_cargo_pending | 2026-06-17 | `zircon_plugins/texture_importer/runtime/src/container/dds.rs` 的 DX10 双 cubemap 诊断现在使用 `DDSCAPS2_CUBEMAP caps2 policy` 与 DX10 texturecube flag 当前术语；`hard_cutover_migration_smells` 当前 direct audit 报告 source_file_count 6145、legacy_reference_count = 148、classification_count = 5、hard_cutover_migration_debt_count = 5、unclassified_locations = []，DDS debt bucket absent。验证：rustfmt check、Python py_compile、direct hard-cutover audit、aggregate audit hard-cutover/status assertions、standalone plan-status 32/32、texture_importer runtime `legacy` scan clean；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 13 Script binding 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_13_script_binding_mirror_docs_match_structure_audit_counts` 锁定 `script_binding_boundary` 的 `expected_source_file_count = 18`、3 guard/test files、6/52/2 host ledger、11/39/2 callback counts、11 capability anchors、9 guard anchors、native ECS ABI references []、oversized test files []、mirror-doc guard present 与 risks []；`docs/plans/zircon_runtime/runtime/13-script-binding-and-reflection.md`、`docs/zircon_runtime/script/vm/host/function_ledger.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `script_binding_boundary_audit`、standalone rustc 2/2、stale old-count scan 通过；script Cargo filters pending。 |
| 横切 | Runtime 13 Gameplay Host Owner Split | folder_split_static_passed_script_vm_cargo_broader_gate_pending | 2026-06-14 | `gameplay_host.rs` 保留 `zr.zircon.gameplay` descriptor 与 39 个 `HostExportFunction::new` 注册锚，具体回调拆入 `gameplay_host/{combat,components,input,lifecycle,navigation,script_bindings,transform,values}.rs`；新增 `runtime_13_gameplay_host_owner_split_keeps_domain_files` 锁定 domain files、400 行 owner 预算、代表回调注册锚和 shared value helper owner；`script_binding_boundary` 同步为 `expected_source_file_count = 18`、`guard_anchor_count = 9`。验证：rustfmt check、Python py_compile、direct `script_binding_boundary_audit` risks=0/source 18/18/guards 9/9、aggregate `audit_runtime_structure.py --json` Runtime 13/plan-status assertions、standalone `script_binding.rs` 2/2、standalone status-output 2/2、`cargo test -p zircon_runtime --lib --locked script::vm -- --nocapture` 48/48 passed；broader `cargo test -p zircon_runtime --lib script --locked -- --nocapture` 仍为 script Cargo filters pending，当前失败在非本切片测试 `scene_assets_keep_script_only_entities_as_empty_nodes`、`vampire_example_manifest_scene_and_scripts_are_importable`、`material_virtualized_descriptors_expose_mui_web_aliases`。 |
| 横切 | Runtime 13 Gameplay host predicate functions for real ZR VM | focused_behavior_passed_broader_script_gate_pending | 2026-06-16 | `gameplay.entity_exists(entity)` 与 `gameplay.script_number_at_most(entity, property, threshold, fallback)` 已加入 `zr.zircon.gameplay` host facade、function ledger 与 Runtime 13 结构审计，保持 fixed host functions 52、gameplay callbacks 39；Vampire 脚本改用 host-side predicate 避免 real ZR VM 中 host-returned entity/numeric 直接比较不稳定。验证：Release ZR VM 二进制复用执行 `gameplay_host_script_property_match_and_heal_update_bindings` 1/1 与 `host_function_registry_matches_documented_ledger` 1/1 通过；broader `cargo test -p zircon_runtime --lib script --locked` 仍按 Runtime 13 gate pending。 |
| 横切 | Runtime 13 Script binding current audit recheck | script_binding_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `script_binding_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 13 当前脚本绑定/反射结构事实，生产代码未改：`script_binding_boundary_audit` 报告 source files 18/18、guard/test files 3/3、fixed host modules/functions/types 6/52/2、callback counts 11/39/2、host capabilities 11/11、Runtime 13 guard anchors 9/9、missing fixed modules/capabilities/ledger doc anchors/bridge anchors/gameplay facade anchors/Cargo gate anchors 均为空、native ECS ABI references []、oversized test files []、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `script_binding_boundary_audit` risks=[]、standalone `script_binding.rs` 2/2；broader `cargo test -p zircon_runtime --lib script --locked` 仍按 Runtime 13 gate pending。 |
| 横切 | Runtime 13 script binding Markdown renderer split | script_binding_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `script_binding_markdown_split_static_passed_cargo_deferred_tests_deferred`；新增 `script_binding_markdown.py` 承接 Runtime 13 Markdown 输出；`script_binding_boundary.py remains the 351-line audit/risk owner`，`Markdown owner is 106 lines`。直接审计报告 source files 18/18、guard/test files 3/3、fixed host modules/functions/types 6/52/2、callback counts 11/39/2、host capabilities 11/11、Runtime 13 guard anchors 9/9、missing fixed modules/capabilities/ledger doc anchors/bridge anchors/gameplay facade anchors/Cargo gate anchors 均为空、native ECS ABI references []、oversized test files []、`mirror_docs_guard_present = true`、`risks = []`；Python py_compile、direct audit、standalone script_binding 2/2、standalone plan-status 33/33 通过，broader script Cargo gate 仍 deferred。 |
| 横切 | Runtime 01 Tech-stack 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_01_tech_stack_mirror_docs_match_structure_audit_counts` 锁定 `tech_stack_boundary` 的 `expected_manifest_count = 5`、`expected_non_dependency_count = 5`、`zip_dependency_count = 1`、`expected_zip_dependency_count = 1`、`zip_dependency_violations = []`、`tech_stack_guard_count = 12`、`editor_only_candidate_count = 3`、`jolt_feature_slot_count = 2`、`declared_removed_dependencies = []`、`rapier_or_avian_dependencies = []`、`mirror_docs_guard_present = true` 与 `risks = []`；`docs/plans/zircon_runtime/runtime/01-tech-stack-and-dependency-governance.md`、`docs/engine-architecture/runtime-tech-stack.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `tech_stack_boundary_audit`、standalone rustc 1/1、stale old-count scan、scoped conflict/diff checks 通过（diff 仅 LF-to-CRLF warnings）；tech_stack/extensions/text_shaper/plugin physics Cargo gates pending。 |
| 横切 | Runtime 01 Tech-stack 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `tech_stack_boundary` 与 `runtime_01_tech_stack_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 01 M2/M3 的 4 个 text-shaper / physics Jolt-unavailable 现状验收锚，当前 `behavior_test_anchor_count = 4`、`missing_behavior_test_anchors = []`；Runtime 01、`runtime-tech-stack.md`、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `tech_stack_boundary_audit`、aggregate Runtime 01 + plan-status assertions、standalone tech_stack 1/1、standalone status-output 2/2；tech_stack/extensions/text_shaper/plugin physics Cargo gates pending。 |
| 横切 | Runtime 01 Tech-stack current audit recheck | tech_stack_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `tech_stack_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 01 当前技术栈/依赖治理边界事实，生产代码未改：`tech_stack_boundary_audit` 报告 manifest files 5/5、corrected non-dependencies 5、zip dependency 1/1、tech-stack guard anchors 12/12、behavior-test anchors 4/4、editor-only candidate count 3、Jolt feature slots 2、`declared_removed_dependencies = []`、`rapier_or_avian_dependencies = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `tech_stack_boundary_audit` risks=[]、standalone `tech_stack.rs` 1/1、standalone `tech_stack_dependency_guard.rs` 11/11、standalone `plan_status.rs` 32/32；tech_stack/extensions/text_shaper/plugin physics/export_build_plan Cargo gates 仍 pending。 |
| 横切 | Runtime 01 Tech-stack inventory split | tech_stack_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `tech_stack_inventory_split_static_passed_cargo_deferred_tests_deferred`；`tech_stack_source_inventory.py` owns Runtime 01 manifest inventory, dependency/version anchors, corrected non-dependency list, ZIP dependency policy line, and expected manifest/non-dependency/ZIP/editor-only candidate counts; `tech_stack_anchor_inventory.py` owns text-stack doc anchors, tech-stack doc anchors, Rust/static guard anchors, behavior-test anchors, physics decision anchors, editor backlog anchors, mirror-doc guard, and pending Cargo gate anchors; `tech_stack_boundary.py` now remains the audit reader / dependency scanner / risk layer at 341 lines, while `tech_stack_markdown.py` owns the Markdown layer at 103 lines. Direct audit reports manifest files 5/5, corrected non-dependencies 5, ZIP dependency declarations 1/1, tech-stack guard anchors 12/12, behavior-test anchors 4/4, editor-only candidate count 3, Jolt feature slots 2, `declared_removed_dependencies = []`, `rapier_or_avian_dependencies = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile, direct `tech_stack_boundary_audit` risks=[], standalone `tech_stack.rs` 1/1, standalone `tech_stack_dependency_guard.rs` 11/11, standalone `plan_status.rs` 33/33; broader `tech_stack` / `extensions` / `text_shaper` / plugin physics / export_build_plan Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 01 Tech-stack Markdown renderer split | tech_stack_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `tech_stack_markdown_split_static_passed_cargo_deferred_tests_deferred`；`tech_stack_markdown.py` now owns `render_tech_stack_boundary_markdown`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `tech_stack_boundary.py`; `tech_stack_boundary.py` now owns audit read, dependency scanning, missing-anchor calculation, and risk aggregation at 341 lines, while the Markdown owner is 103 lines. Direct audit reports manifest files 5/5, corrected non-dependencies 5, ZIP dependency declarations 1/1, tech-stack guard anchors 12/12, behavior-test anchors 4/4, editor-only candidate count 3, Jolt feature slots 2, `declared_removed_dependencies = []`, `rapier_or_avian_dependencies = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile and direct `tech_stack_boundary_audit`; standalone `tech_stack.rs` 1/1, standalone `tech_stack_dependency_guard.rs` 11/11, and standalone `plan_status.rs` 33/33; broader `tech_stack` / `extensions` / `text_shaper` / plugin physics / export_build_plan Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 04 Asset pipeline 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_04_asset_pipeline_mirror_docs_match_structure_audit_counts` 锁定 `asset_pipeline_boundary` 的 `expected_source_file_count = 22`、`expected_guard_file_count = 11`、`worker_diagnostic_count = 7`、`expected_worker_diagnostic_count = 7`、`artifact_store_roundtrip_count = 4`、`expected_artifact_store_roundtrip_count = 4`、`watcher_acceptance_reference_count = 1`、`expected_watcher_acceptance_count = 7`、`artifact_acceptance_reference_count = 3`、`test_anchor_count = 24`、`behavior_test_anchor_count = 20`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`retired_worker_new_references = []`、`retired_worker_request_sender_references = []`、`old_watch_debounce_references = []`、`mirror_docs_guard_present = true` 与 `risks = []`；Runtime 04、asset facade/worker/watcher/artifact/core-resource docs、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `asset_pipeline_boundary_audit`、standalone rustc 1/1、stale old-count scan 通过；broader asset::/worker_pool Cargo gates pending。 |
| 横切 | Runtime 04 Asset pipeline 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `asset_pipeline_boundary` 与 `runtime_04_asset_pipeline_mirror_docs_match_structure_audit_counts` 现在锁定 Runtime 04 M1/M2/M3 的 20 个行为测试锚，当前 `behavior_test_anchor_count = 20`、`missing_behavior_test_anchors = []`；Runtime 04、asset facade/worker/watcher/artifact/core-resource docs、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `asset_pipeline_boundary_audit`、aggregate Runtime 04 + plan-status assertions、standalone asset_pipeline 1/1、standalone status-output 2/2；broader asset::/worker_pool Cargo gates pending。 |
| M2 | Runtime 04 asset worker request entry hard-cutover | asset_worker_request_sender_hard_cutover_static_passed_cargo_deferred | 2026-06-20 | `AssetWorkerPool::request_sender(...)` 已删除，`AssetWorkerPool::request(...)` 成为唯一公开请求入口；`asset_pipeline_boundary` 新增 `retired_worker_request_sender_references = []` 负向审计字段，`asset_worker_pool_matches_runtime_04_and_11_decisions` 与镜像文档守卫同步防回流。轻量验证已补齐：Python py_compile 通过，direct `asset_pipeline_boundary_audit` 报告 source/guard 22/11、`worker_diagnostic_count = 7`、`behavior_test_anchor_count = 20`、`retired_worker_request_sender_references = []`、`missing_doc_anchors = []`、`risks = []`，standalone `asset_pipeline` 1/1 与 `asset_worker_policy` 1/1 通过；broader `asset::` / `worker_pool` Cargo filters 后续补跑。 |
| 横切 | Runtime 04 Asset pipeline current audit recheck | asset_pipeline_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `asset_pipeline_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 04 当前资产管线结构事实，生产代码未改：`asset_pipeline_boundary_audit` 报告 source files 22/22、guard/test files 11/11、worker diagnostics 7/7、artifact-store scene roundtrip guards 4/4、watcher acceptance evidence references 1（expected watcher tests 7）、artifact acceptance references 3、Runtime 04 guard anchors 24/24、behavior-test anchors 20/20、missing doc/Cargo gate anchors 均为空、`retired_worker_new_references = []`、`retired_worker_request_sender_references = []`、`old_watch_debounce_references = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `asset_pipeline_boundary_audit` risks=[]、standalone `asset_pipeline.rs` 1/1、standalone `asset_worker_policy.rs` 1/1；broader `asset::` / `worker_pool` Cargo filters 仍 pending。 |
| 横切 | Runtime 04 Asset pipeline inventory split | asset_pipeline_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `asset_pipeline_inventory_split_static_passed_cargo_deferred_tests_deferred`；`asset_pipeline_source_inventory.py` owns Runtime 04 source/guard file inventory plus expected source/guard, worker diagnostic, artifact roundtrip, and watcher acceptance counts; `asset_pipeline_anchor_inventory.py` owns handle/load-state, resource reload, worker-pool, diagnostics, watcher, artifact cache, guard, behavior, doc, and Cargo gate anchors; `asset_pipeline_boundary.py` now remains the audit reader / missing-anchor / risk layer at 328 lines, and `asset_pipeline_markdown.py` owns the Markdown layer at 117 lines. Direct audit reports source files 22/22, guard/test files 11/11, worker diagnostics 7/7, artifact-store scene roundtrip guards 4/4, watcher acceptance evidence references 1, artifact acceptance references 3, Runtime 04 guard anchors 24/24, behavior-test anchors 20/20, missing doc/Cargo anchors [], retired worker constructor/request-sender and old debounce references [], `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile, direct `asset_pipeline_boundary_audit` risks=[], standalone `asset_pipeline.rs` 1/1, standalone `asset_worker_policy.rs` 1/1, standalone `plan_status.rs` 33/33; broader `asset::` / `worker_pool` Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 04 Asset pipeline Markdown renderer split | asset_pipeline_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `asset_pipeline_markdown_split_static_passed_cargo_deferred_tests_deferred`；`asset_pipeline_markdown.py` now owns `render_asset_pipeline_boundary_markdown`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `asset_pipeline_boundary.py`; `asset_pipeline_boundary.py` owns audit read, missing-anchor calculation, and risk aggregation at 328 lines, while the Markdown owner is 117 lines. Direct audit reports source files 22/22, guard/test files 11/11, worker diagnostics 7/7, artifact-store scene roundtrip guards 4/4, behavior-test anchors 20/20, missing doc/Cargo anchors [], retired worker constructor/request-sender and old debounce references [], `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile and direct `asset_pipeline_boundary_audit`; standalone `asset_pipeline.rs` 1/1, standalone `asset_worker_policy.rs` 1/1, standalone `plan_status.rs` 33/33; broader `asset::` / `worker_pool` Cargo gates remain deferred while external compile lanes are active. |
| 横切 | Runtime 04 F7 asset artifact/importer typed errors | asset_artifact_importer_typed_errors_coremin_passed | 2026-06-22 | 状态锚 `asset_artifact_importer_typed_errors_coremin_passed`；`AssetImportError::Registry` 已改为 `#[from] AssetImporterRegistryError`，artifact cache conversion 改为 `AssetImportError` 并为 TOML serialize/deserialize、cached datetime、UI/UI v2 document、bincode serialize/deserialize 保留 source，`ArtifactStore` 不再把 cache conversion 包进 `Parse(String)`。新增 `asset_import_error_preserves_registry_error_source` 与 `review_f7_asset_artifact_errors_use_asset_import_error_sources`；验证：F7 结构守卫 1/1、status-output 守卫 1/1、core-min `cargo check` 通过；focused behavior Cargo 过滤项两次在测试二进制编译阶段超时无结果且 F7 残留已清理，broader `asset::` / `worker_pool` Cargo gate 仍 pending。 |
| 横切 | Runtime 04 F8 texture import settings apply API | texture_import_settings_apply_api_coremin_check_passed | 2026-06-22 | 状态锚 `texture_import_settings_apply_api_coremin_check_passed`；`TextureAssetDescriptor` 与 `TextureAsset` 的 texture import settings 可失败入口已硬切为 `apply_import_settings(...) -> Result<_, String>`，runtime 内置 texture ingest 与 first-party texture importer plugin 调用点同步迁移；新增 `review_f8_texture_import_settings_use_fallible_apply_not_with`，锁定 `apply_import_settings` 与 old fallible with-entry absent。验证：F8 结构守卫 1/1、status-output 守卫 2/2、runtime core-min `cargo check` 通过（既有 warnings）；`zircon_plugin_texture_importer_runtime` package `cargo check` 当前被既有 `graphics/runtime_provider/registration.rs` trait-object Clone drift 阻塞，未计通过。RuntimePluginDescriptor public-field convergence remains pending，F8 全量关闭仍待 Runtime 06/结构规范后续切片；broader Runtime 04 `asset::` / `worker_pool` Cargo gate 仍 pending。 |
| 横切 | Runtime 06 Plugin surface/lifecycle 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts` 锁定 `plugin_surface_lifecycle_boundary` 的 `expected_source_file_count = 14`、`expected_doc_file_count = 5`、native root re-export 0/0、native namespace re-export 60/60、M4 gate `classified-and-clear`、debt groups 0/0、unclassified native symbols 0/0、public native namespace re-export locations 1/1、app NativePlugin current call-site files: 7、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、fallback lifecycle failure tests 4/4、unknown ABI rejection、hot reload failure injection、`mirror_docs_guard_present = true` 与 `risks = []`；Runtime 06、native boundary、M0 review、runtime-interface convergence 与本总索引已同步；`runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed` 防止 fallback 生命周期失败路径测试回落到单文件堆叠。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 06 assertions、standalone plugin_surface_lifecycle 1/1、standalone plan-status 32/32；stale old-count scan、conflict marker scan 通过；`script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates pending。 |
| 横切 | Runtime 06 native root re-export current mirror fix | mirror_docs_static_passed_cargo_pending | 2026-06-16 | `runtime_absorption::plugin_surface_lifecycle::runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts` 的 `plugin_root_symbols.len()` 同步到当前 `native_plugin_public_surface` / `plugin_surface_lifecycle_boundary` 事实：native root re-export 0/0、`root_reexport_count = 0`、M4 gate `classified-and-clear`、debt groups 0/0、unclassified native symbols 0/0，并把 Runtime 06 `last_refined` 与 Python 审计期望同步为 `last_refined = 2026-06-16`。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` risks=[] / native root re-export 0/0、standalone plugin_surface_lifecycle 1/1、standalone plan-status 32/32。`script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates 仍 pending。 |
| 横切 | Runtime 06 plugin surface/lifecycle Markdown renderer split | plugin_surface_lifecycle_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `plugin_surface_lifecycle_markdown_split_static_passed_cargo_deferred_tests_deferred`；新增 `plugin_surface_lifecycle_markdown.py` 承接 Runtime 06 Markdown 输出；`plugin_surface_lifecycle_boundary.py remains the 450-line audit/risk owner`，`Markdown owner is 144 lines`。直接审计报告 source 14/14、doc 5/5、frontmatter `in_progress` / `last_refined = 2026-06-21`、native root re-export 0/0、native namespace re-export 60/60、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1、app NativePlugin current call-site files 7/7、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、native loader test files 3/3、native test namespace import files 2/2、native test root import leaks 0/0、fallback lifecycle failure tests 4/4、source anchors 19/19、doc anchors 23/23、validation command anchors 9/9、`mirror_docs_guard_present = true`、`risks = []`；Python py_compile、direct audit、standalone plugin_surface_lifecycle 3/3、standalone plan-status 33/33 通过，包级 Cargo/native gates 仍 deferred。 |
| 横切 | Runtime 06 native plugin public-surface Markdown renderer split | native_plugin_public_surface_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `native_plugin_public_surface_markdown_split_static_passed_cargo_deferred_tests_deferred`；新增 `native_plugin_public_surface_markdown.py` 承接 `render_native_plugin_public_surface_markdown(...)`，`native_plugin_public_surface.py` 保持 400 行 native public-surface scan / symbol classification / M4 gate owner，Markdown owner 为 63 行。Direct probe 报告 `root_reexport_count = 0`、`native_namespace_reexport_count = 60`、`symbol_decision_group_count = 5`、`native_plugin_public_surface_migration_debt_count = 0`、`unclassified_root_reexport_symbol_count = 0`、`unclassified_native_namespace_symbol_count = 0`、`root_public_reexport_location_count = 0`、`public_reexport_location_count = 1`、M4 gate `classified-and-clear`、`risk_count = 0`、rendered output 12 lines；Python py_compile、direct native public-surface/renderer probe、standalone `plan_status.rs` 33/33 通过，Runtime 06 script VM / native plugin / app / plugin workspace Cargo-native gates 仍 deferred。 |
| 横切 | Runtime 06 F8 RuntimePluginDescriptor builder scaffold | runtime_plugin_descriptor_builder_scaffold_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_plugin_descriptor_builder_scaffold_coremin_check_passed`；新增 `RuntimePluginDescriptorBuilder` 与 `RuntimePluginDescriptor::builder(...).build()`，并经 `runtime_plugin` / `plugin` facade re-export；builder 覆盖 category、enabled/required defaults、target modes、capabilities、system sets、system anchors、maturity、capability status、optional features 与 default packaging。新增 `runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 与 `review_f8_runtime_plugin_descriptor_exposes_builder_scaffold`，锁定 builder 输出与现有 fluent descriptor projection 等价、API/re-export/doc/status 锚点齐全。验证：scoped rustfmt --check、F8 builder structure guard 2/2、standalone status-output 2/2、builder behavior filter 1/1 与 runtime core-min `cargo check` 通过（既有 warnings）；RuntimePluginDescriptor public-field convergence remains pending，15 个 public 字段裸访问收束和字段私有化仍属后续 Runtime 06/结构规范切片。 |
| 横切 | Runtime 06 F8 first-party RuntimePluginDescriptor builder migration | runtime_plugin_descriptor_first_party_builder_migration_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_plugin_descriptor_first_party_builder_migration_coremin_check_passed`；16 个 first-party runtime plugin descriptor production files 已迁到 `RuntimePluginDescriptor::builder(...).build()`，旧 `RuntimePluginDescriptor::new(` 在 `zircon_plugins` 生产源码中 absent；`rendering` optional-feature loop 改为 builder 累加后 `build()`。新增 `review_f8_first_party_runtime_plugin_descriptors_use_builder`，锁定 first-party runtime plugin descriptor production files 16/16。验证：scoped rustfmt --check 通过；F8 structure guard 3/3、status-output 2/2 通过；runtime core-min `cargo check` 与 16-package `zircon_plugins` focused `cargo check` 均通过（既有 warnings）；RuntimePluginDescriptor public-field convergence remains pending，字段私有化仍归后续切片。 |
| 横切 | Runtime 06 F8 RuntimePluginDescriptor test fixture builder migration | runtime_plugin_descriptor_test_fixture_builder_migration_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_plugin_descriptor_test_fixture_builder_migration_coremin_check_passed`；`zircon_runtime/src/tests/plugin_extensions` 下 14 个 runtime/plugin extension test fixture files 已迁到 `RuntimePluginDescriptor::builder(...).build()`，旧 `RuntimePluginDescriptor::new(` 在这些测试文件中 absent；`runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 保留 `RuntimePluginDescriptorBuilder` 显式类型覆盖但不再依赖旧 constructor 对照。新增 `review_f8_runtime_plugin_descriptor_test_fixtures_use_builder`，锁定 plugin extension RuntimePluginDescriptor test fixtures 14/14 与 builder call count 64。验证：scoped rustfmt --check 通过；F8 structure guard 4/4、status-output 2/2、runtime core-min `cargo check` 与 focused descriptor behavior filter 1/1 通过（既有 warnings）；broad plugin_extensions cargo filter 在 1200s timeout 后无测试结果，残留 cargo/rustc 已清理，不计通过；RuntimePluginDescriptor public-field convergence remains pending，字段私有化仍归后续切片。 |
| 横切 | Runtime 06 F8 RuntimePluginDescriptor public-field convergence | runtime_plugin_descriptor_public_field_convergence_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_plugin_descriptor_public_field_convergence_coremin_check_passed`；`RuntimePluginDescriptor` 15 个字段已私有化，`descriptor/access.rs` 暴露 15 个只读访问器，production catalog augmentation、registration validation 与 plugin extension 测试调用点改走 accessor。新增 `review_f8_runtime_plugin_descriptor_fields_are_private_with_accessors`，锁定 RuntimePluginDescriptor private fields 15/15、访问器签名和 RuntimePluginDescriptor public-field convergence complete。验证：runtime core-min `cargo check` 通过（既有 warnings）；F8 structure guard 5/5 通过；lib-test 编译通过，`plugin_workspace_shape` 过滤器运行暴露两个既有 manifest 断言失败，不计为本切片失败。 |
| 横切 | Runtime 06 F8 RuntimePluginDescriptor public constructor retired | runtime_plugin_descriptor_public_constructor_retired_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_plugin_descriptor_public_constructor_retired_coremin_check_passed`；旧 `RuntimePluginDescriptor::new` / descriptor fluent owner 已退役，`descriptor/builder/construction.rs retired`、`descriptor/builder/fluent.rs retired`，`RuntimePluginDescriptorBuilder` 直接组装私有字段并保持唯一公开构造入口。builtin catalog 子树改为传递 `BuiltinCatalogDescriptorBuilder`，catalog root 统一 `build()`；`zircon_plugins/plugin_sdk` 的 `RuntimePluginDeclaration` 改为持有 `RuntimePluginDescriptorBuilder`。新增 `review_f8_runtime_plugin_descriptor_public_constructor_is_retired`，锁定 RuntimePluginDescriptor::new retired、retired owner 不复活与 builtin catalog child modules 不直接使用 descriptor。验证：runtime core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime06-f8-public-constructor-0622` 通过（既有 144 warnings）；plugin SDK focused `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_sdk --locked --jobs 1 --target-dir target\codex-runtime06-f8-public-constructor-0622` 通过（既有 runtime warnings）。 |
| 横切 | Runtime 06 plugin::native hard-cutover | code_static_passed_cargo_pending | 2026-06-16 | `plugin::native` 公开命名空间落地并替代旧根 native re-export：`root_reexport_count = 0`、`native_namespace_reexport_count = 60`、native root re-export 0/0、native namespace re-export 60/60、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1；Runtime 06、native boundary、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `native_plugin_public_surface_audit` / `plugin_surface_lifecycle_boundary_audit` risks=[]；Cargo `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` gates pending。 |
| 横切 | Runtime 06 native loader test namespace migration | code_static_passed_cargo_pending | 2026-06-16 | 新增 `runtime_06_native_loader_tests_use_isolated_plugin_native_namespace`，锁定 native loader test files 3/3、native test namespace import files 2/2、native test root import leaks 0/0；`plugin_extensions/native_plugin_loader.rs` 与 `plugin_extensions/export_build_plan_native_dynamic.rs` 直接 import `crate::plugin::native::*`，`plugin_extensions/export_build_plan.rs` 只保留 generated source 不直接含 `NativePluginLoader` 的负例断言。Runtime 06、native boundary、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` risks=[]、standalone plugin_surface_lifecycle 2/2、standalone plan-status 32/32；包级 `cargo test -p zircon_runtime --lib plugin --locked` 仍待既有 render compile drift 清理后补跑。 |
| 横切 | Runtime 06 fallback lifecycle failure tests | code_static_passed_real_backend_pending | 2026-06-16 | 新增 folder-backed `zircon_runtime/src/script/vm/tests/lifecycle_failures.rs` 并由 `script/vm/tests.rs` 挂载，`runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed` 锁定 fallback lifecycle failure tests 4/4：bad entry module、missing optional export、deactivate after unload idempotency 与 empty arguments；`vm_lifecycle_fallback_missing_optional_export_returns_none_not_error` 固定 optional export 缺失返回 `None` 而不是错误。结构证据同步到 `plugin_surface_lifecycle_boundary` source 14/14 / doc 5/5 / `expected_source_file_count = 14`；real-backend Cargo remains pending。 |
| 横切 | Runtime 06 fallback lifecycle Cargo 验证 | fallback_cargo_passed_real_backend_pending | 2026-06-16 | 状态锚点 `fallback_cargo_passed_real_backend_pending`；为解除 M1.2 fallback 验证阻断，补齐 `graphics/pipeline/render_pipeline_asset/compile.rs` 测试模块局部 import，未改变 render 生产行为；`cargo test -p zircon_runtime --lib vm_lifecycle_fallback --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-06-fallback-lifecycle-0616 -- --nocapture --test-threads=1` 通过 5/5，覆盖四个 `vm_lifecycle_fallback_*` 测试与 `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed`。Runtime 06 仍保持 `in_progress`：real-backend Cargo、plugin/native plugin、app 与 `zircon_plugins` workspace gates 仍 pending。 |
| 横切 | Runtime 06 shader artifact cache real-backend unblock | asset_cache_fixed_vampire_session_pending | 2026-06-16 | 状态锚点 `asset_cache_fixed_vampire_session_pending`；real-backend `vampire_project_session` 首轮复验已越过 ZR VM 环境配置，暴露 `lib://shaders/ae3ee5f2-ac09-3b2c-d00c-0fd96cccca44.zasset` 反序列化失败：`tag for enum is not valid, found 5`。最低层修复在 `zircon_runtime/src/asset/artifact/cache_payload.rs`：shader import redirects 与 texture slots 改用 `ArtifactCacheShaderImportRedirectAsset` / `ArtifactCacheShaderTextureSlotAsset`，避免 authoring `skip_serializing_if` 影响 bincode wire shape；同步刷新 `examples/vampire/assets/shaders/default_pbr.zmeta` 与对应 `.zasset`。验证：`artifact_store_roundtrips_shader_assets_with_cache_safe_toml_metadata` 1/1 与 `project_manager_imports_compound_zshader_package_with_subassets` 1/1 通过；完整 `vampire_project_session` filter 604s timeout，单测 `vampire_project_session_starts_paused_until_start_button_click` 184s timeout，real-backend session gate remains pending。 |
| 横切 | Runtime 06 Vampire real-backend menu/retry focused validation | vampire_real_backend_focused_passed_full_gate_pending | 2026-06-16 | 状态锚点 `vampire_real_backend_focused_passed_full_gate_pending`；Release ZrVM DLL 验证通过 focused session path：`vampire_project_session_starts_paused_until_start_button_click` 覆盖 Start Game 前暂停、点击后进入 `playing`；`vampire_project_session_game_over_menu_retries_to_playing` 覆盖致命接触、Game Over 菜单、Retry 后回到 `playing` 且玩家归零位满血。最低共享边界修复包括 `gameplay.entity_exists` 与 `gameplay.script_number_at_most`，避免当前 real ZrVM 对 host-returned entity/numeric 直接比较路径崩溃；Vampire 脚本通过 `vampire.spawn_grace` 阻断 Retry 同帧再伤害。完整 `vampire_project_session` 组、plugin/native plugin、app 与 `zircon_plugins` gates 仍 pending。 |
| 横切 | Runtime 06 Vampire HUD real-backend capture validation | vampire_hud_real_backend_focused_passed_full_gate_pending | 2026-06-16 | 状态锚点 `vampire_hud_real_backend_focused_passed_full_gate_pending`；core scene particle descriptor 在 `Transparent3d` 自动插入 `particle-render` / `particle.transparent`，只读 `scene-depth`、写 `scene-color`，避免同 pass scene-color sampled/resource 与 color attachment 冲突；`RenderParticleSpriteSnapshot.depth_test` 将普通粒子与 world-HUD overlay 分流，HUD overlay 使用 alpha blend 且不写 velocity；Vampire scene `exposure_ev100 = 9.2` 迁入 EV100 语义，修复旧 `0.2` 在 PP-M3 exposure resolve 下造成的全白抓帧。验证：Release ZrVM 测试二进制 `vampire_project_session_capture_frame_draws_world_hud_bars` 1/1 通过；`core_scene_particle_extract_adds_billboard_pass_without_plugin_feature_identity`、`particle_plugin_render_feature_adds_transparent_pass_to_default_pipeline`、`particle_vertices_split_depth_tested_and_overlay_sprites`、`particle_velocity_vertices_skip_overlay_sprites`、`world_hud_bar_sprites_use_overlay_depth_path`、`authored_particle_sprites_keep_depth_test_path` 通过；新测试守卫 `particle_pipeline_keeps_world_hud_billboards_transparent_and_depth_read_only` 重编译 184s/424s 超时无结果。完整 `vampire_project_session` 组、plugin/native plugin、app 与 `zircon_plugins` gates 仍 pending。 |
| 横切 | Runtime 06 V1/V2 ABI hard-cutover | code_static_passed_cargo_pending | 2026-06-16 | V3-only native plugin ABI 与 unknown ABI rejection 落地：`native_loader_v1_v2_file_count = 0`、`plugin_v1_v2_usage_files = 0`、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0；`abi_unknown_version` fixture 覆盖 `unsupported native plugin ABI version 99; expected 3`；Cargo/package gates 等 active lanes 清空后补跑。 |
| 横切 | Runtime 06 hot reload failure injection | code_static_passed_cargo_pending | 2026-06-16 | `native_plugin_live_host/tests/hot_reload_failures.rs` 落地 hot reload failure injection：`hot_reload_missing_symbol_after_reload_rolls_back_to_previous_instance` 与 `hot_reload_state_restore_failure_rolls_back_and_reports` 覆盖缺目标/缺符号与 restore-state 失败两条回滚路径。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` source 14/14 / risks=[]；默认特性 Cargo 被既有 render compile drift 阻断，core-min Cargo timeout no result，包级 `native_plugin` gate 保持 pending。 |
| M2 | Runtime 09 taffy bridge pass order | runtime_09_m2_1_taffy_bridge_pass_order_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_09_m2_1_taffy_bridge_pass_order_static_passed_cargo_pending`；`taffy_bridge/compute.rs::compute_taffy_child_frames(...)` 成为 Taffy tree build / `TaffyTree::new()` / `compute_layout` 唯一 owner；`layout/pass/taffy_arrange.rs` 只保留 eligibility/fallback/reporting；`layout/pass/pipeline.rs` 暴露 `UI_LAYOUT_PASS_ORDER`，full 与 incremental layout 均消费同一 pass 序；`runtime_09_m2_1_style_mapping_remains_taffy_dto_adapter` 记录 `style_mapping.rs` 只做 DTO adapter。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit`、standalone `ui_architecture.rs` 7/7、`cargo check -p zircon_runtime` 通过（现有 warning noise）；完整 layout/ui behavior filters 仍 pending。 |
| M2 | Runtime 09 virtualization scroll boundary | runtime_09_m2_2_virtualization_scroll_boundary_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_09_m2_2_virtualization_scroll_boundary_static_passed_cargo_pending`；`layout/scroll.rs::UiScrollVirtualizationPlan` 与 `plan_scrollable_virtual_window(...)` 成为 scroll offset clamping、viewport/content extent 与 visible-range invalidation 的 owner；`layout/virtualization.rs::compute_virtual_list_window(...)` 继续只做纯 window math；`layout/pass/arrange.rs` 与 `tree/node/scroll.rs` 消费 planner 并以 OR 方式保留既有 visible_range dirty。`scroll_virtualization.rs` 已补 `virtualized_list_only_materializes_visible_window`、`scroll_offset_invalidates_virtualization_window`、`non_virtualized_scroll_offset_keeps_full_window_dirty_domain`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit`、standalone `ui_architecture.rs` 8/8、`cargo check -p zircon_runtime --lib --locked` 通过；focused `cargo test -p zircon_runtime --lib scroll_virtualization --locked --jobs 1` 304s timeout no result，完整 layout/ui behavior filters 按当前实现优先要求后续补跑。 |
| M3 | Runtime 09 template pipeline boundary | runtime_09_m3_1_template_compile_instance_validate_boundary_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_09_m3_1_template_compile_instance_validate_boundary_static_passed_cargo_pending`；`template/pipeline.rs` 新增 `UiTemplateRuntimePipeline`、`UI_TEMPLATE_RUNTIME_PIPELINE_STAGES` 与 `UiTemplateRuntimePipelineError`，固定 old recursive template path 为 `load -> validate -> instance -> build`；`UiTemplateInstance::from_validated_document(...)` 让 pipeline 在 validation 后进入实例展开；`UiRuntimeCompiledAssetArtifact::generated_policy()` 返回 `runtime_09_m3_1_binary_leaf_dto_artifact_not_generated_source`，当前 compiled artifact 是 binary/TOML DTO payload 而不是 generated source，未来 source output 必须使用 `// @generated <generator> - do not edit by hand`。`template_pipeline.rs` 已补 `template_validate_rejects_unknown_component_contract`、`template_instance_failure_surfaces_loader_error`、`compiled_template_artifact_stays_binary_leaf_dto_not_generated_source`；完整 `template` / `ui` behavior filters 按当前实现优先要求后续补跑。 |
| 横切 | Runtime 07 Performance hotpath 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` 锁定 `performance_hotpath_boundary` 的 `expected_source_file_count = 26`、`expected_test_file_count = 6`、`frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 21`、`asset_worker_anchor_count = 13`、`hotspot_guard_anchor_count = 24`、`test_anchor_count = 26`、`doc_anchor_count = 27`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = migration-debt-present`、`large_file_hotspot_count = 41`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`missing_large_file_owner_classes = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true` 与 `risks = []`；Runtime 07、hotspot inventory、dynamic-session/ECS docs、M0 review、large-file owner gate 与 runtime-interface convergence 已同步到当前 41-hotspot / runtime-other=16 state，并纳入 `EcsFramePerformanceDiagnostics`、QueryState frame auto-collection、ChangeDetection frame auto-collection、extract cache diagnostics、asset worker manager sampler entry 与 artifact cache payload owner split。验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit`、standalone guard；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 ECS frame diagnostics aggregation | ecs_frame_diagnostics_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/scene/ecs/frame_performance_diagnostics.rs` 新增 `EcsFramePerformanceDiagnostics`，在 ECS owner 内聚合 `QueryStateCacheStats` 与 `ChangeDetectionScanStats` 并写入同一 frame index；`ecs_frame_performance_diagnostics_record_query_and_change_counts` 锁定同帧 query/change-detection 诊断读回，`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 24`、`query_counter_anchor_count = 32`、`test_anchor_count = 18`、`doc_anchor_count = 21`。本切片不触碰 dynamic-session extract owner；package Cargo 按当前实现优先要求随 Runtime 07 `ecs_query` gate 后续补跑。 |
| 横切 | Runtime 07 QueryState frame auto-collection | system_query_frame_telemetry_static_passed_cargo_deferred | 2026-06-17 | `QueryState::take_unreported_cache_stats()`、`SystemParam::record_performance_diagnostics(...)`、tuple/`ParamSet` forwarding、`SystemState::run(...)`、`World::record_ecs_query_cache_stats(...)` 与 `WorldDriver::tick_level(...)` 已接成生产链路：每次系统运行只把新增 QueryState cache stats 合入 world-local `EcsFramePerformanceDiagnostics`，每帧 tick 前重置。`system_state_records_query_cache_stats_into_world_frame_diagnostics` 锁定两个 query system 同帧 cache-hit 聚合和 reset 后增量语义。验证：rustfmt check 通过；Cargo/ecs_query 按用户要求暂缓到后续验证窗口。 |
| 横切 | Runtime 07 ChangeDetection frame auto-collection | system_change_detection_frame_telemetry_static_passed_cargo_deferred | 2026-06-17 | `matches_component_locations_with_stats(...)` 将 `Added<T>` / `Changed<T>` 的扫描量接入 cached QueryState 读取路径，`QueryIter` / `QueryManyCachedIter` 与 cached count/contains/get helpers 把本次扫描聚合回 `QueryState::record_change_detection_stats(...)`，再由 `take_unreported_change_detection_stats()` 通过 `SystemParam::record_performance_diagnostics(...)` 写入 `World::record_ecs_change_detection_stats(...)`。`system_state_records_change_detection_stats_into_world_frame_diagnostics` 锁定 `Changed<Health>` 系统运行后的 scanned/changed per-frame aggregation；验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` 通过；core-min cargo check 约 180s 超时无测试结果且无残留 cargo/rustc/rustdoc，Cargo/ecs_query 后续补跑。 |
| 横切 | Runtime 07 QueryState iterator lifetime guard | query_state_iterator_lifetime_static_passed_cargo_timeout_no_result | 2026-06-17 | `QueryIter` / `QueryManyCachedIter` 用 `NonNull<QueryState<D, F>>` 保存 cached stats sink，让 cached slices 继续承担真实借用约束，同时避免 read-only, non-cached iterators 的输出生命周期被 QueryState 借用污染。该修复覆盖 `QueryState::single` 只读路径 E0309 风险，并把 `state: Option<NonNull<QueryState<D, F>>>` 纳入 `performance_hotpath_boundary`；当前 `query_counter_anchor_count = 32`。验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit`、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32 与 scoped diff/conflict checks 通过；core-min `cargo check` 300s timeout no result 且无残留 cargo/rustc/rustdoc，随 Runtime 07 `ecs_query` gate 补跑。 |
| M0 | Runtime 07 profiling build tooling | profiling_build_tooling_static_passed_cargo_deferred_active_lanes | 2026-06-17 | `profiling_build_tooling_static_passed_cargo_deferred_active_lanes`：Runtime 07 M0.2 profiling 构建入口已工具化，`tools/zircon_build.py` 支持 `--mode profiling` 并映射 Cargo `--profile profiling`，`--runtime-features target-client,profiling,profiling-tracy` 可复现 runtime profiling feature 组合；Hub/Tauri 与独立 plugin workspace 在 profiling mode 下显式拒绝。`tools/dev-fast-build.ps1` 新增 `-CargoProfile profiling`，fast-check 命令为 `./tools/dev-fast-build.ps1 -Profile client -Action check -Package zircon_runtime -CargoProfile profiling -FeatureOverride "target-client profiling profiling-tracy"`；`runtime_absorption::performance_hotspots` 已锁定根 `[profile.profiling]`、`zircon_runtime` profiling features、Python dry-run 入口和 PowerShell fast-build 入口。当前外部 `zircon_export_pack` Cargo/rustc lane 仍在运行，真实 profiling 构建耗时/瓶颈记录待空闲窗口复跑，不能视为 M0.2 构建通过。 |
| M0 | Runtime 07 FPS gate support unblock | fps_gate_support_unblocked_timeout_no_result | 2026-06-17 | Runtime 07 M0.1 权威 FPS 命令首次被 `ZR_VM_RUST_BINDING_LIB_DIR` 未设置挡住；已定位 `E:\Git\zr_vm\build\codex-msvc-debug\lib\Debug\zr_vm_rust_binding.lib` 与 `E:\Git\zr_vm\build\codex-msvc-debug\bin\Debug\zr_vm_rust_binding.dll` 运行目录，带环境变量重跑越过 ZR VM build script。随后修复 lib-test 进入 FPS gate 前的三处支持层编译阻塞：asset 测试改用 `zircon_runtime_interface::ui::template::UI_ASSET_CURRENT_SOURCE_SCHEMA_VERSION` 公开 re-export，`compile_routes_bloom_extract_after_split_scene_color_passes` 的 `RenderBloomSettings` 初始化补 `..Default::default()`。`rustfmt --edition 2021 --check` 覆盖三处 touched Rust 文件通过；同命令重跑 904s timeout no result，未产生 `vampire_runtime_perf` 输出，残留的本次 cargo/rustc 进程已停止，M0.1 仍需在空闲窗口重跑同命令 ×2。 |
| 横切 | Runtime 07 owner-budget evidence drift resync | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `large_file_ownership_gate` 当前报告 38 hotspots、runtime-other=14、runtime-framework-render=2、unclassified 0；`zircon_runtime/src/asset/assets/animation.rs` 已拆为 `animation/{mod,binary,channel,clip,graph,reference,sequence,skeleton,state_machine}.rs`，`zircon_runtime/src/asset/assets/scene.rs` 已拆为 `scene/{mod,animation,asset,camera,defaults,entity,extensions,lighting,management,mesh,physics,post_process,transform}.rs`，animation 与 scene 资产 payload 不再是大文件热点；当前 38-hotspot 状态已由 `project_io` folder split 进一步固定，`zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/build.rs` 仍按 Python 审计为 1045 行热点。已同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、`hotspot_inventory.md`、dynamic-session/ECS docs、M0 review、runtime-interface convergence 与 `runtime_absorption::performance_hotspots` 守卫；未改 render 生产代码，extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 38-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 38`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`runtime-framework-render=2`、`runtime-other=14` 与 `risks = []`；Runtime 07、large-file owner gate、hotspot inventory、dynamic-session/ECS docs、M0 review、runtime-interface convergence 与 `performance_hotspots.rs` 守卫已同步。不改 render/UI/runtime 生产代码。验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38、standalone `performance_hotspots.rs` 6/6、旧 41 当前值镜像文档扫描、conflict/trailing scans 与 scoped `git diff --check` 通过（仅 LF-to-CRLF warnings）；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 37-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | Runtime 13 Gameplay Host Owner Split 后 `zircon_runtime/src/script/vm/gameplay_host.rs` 降到 371 行，`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 37`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`runtime-framework-render=2`、`runtime-other=13`、`hotspot_guard_anchor_count = 20` 与 `risks = []`；Runtime 07、large-file owner gate、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫已同步。不改 render/UI/runtime/plugin 生产代码。验证：direct `performance_hotpath_boundary_audit` risks=0 / hotspots=37、standalone `performance_hotspots.rs` 6/6、standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 38-hotspot 回漂同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | 当前活跃 editor/export retained-host 工作使 `zircon_editor/src/ui/retained_host/app/build_export_actions.rs` 回到 1000 行阈值以上，`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 38`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=2`、`runtime-other=13`、`support-hub=3`、`hotspot_guard_anchor_count = 23` 与 `risks = []`；Runtime 07、large-file owner gate、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫已同步。不改 active editor/UI/render/plugin 生产代码。验证：direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38、direct `runtime_plan_status_boundary_audit` risks=0、standalone `performance_hotspots.rs` 6/6、standalone `status_output_tables.rs` 2/2；focused Cargo `cargo test -p zircon_runtime --lib runtime_absorption::performance_hotspots --locked --jobs 1 ...` 300s 超时且无测试结果，残留 cargo/rustc 已停止；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 37-hotspot 再同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | 当前静态审计回到 37-hotspot owner-budget：`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 37`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=2`、`runtime-other=12`、`support-hub=3`、`hotspot_guard_anchor_count = 23` 与 `risks = []`；`zircon_editor/src/ui/retained_host/app/build_export_actions.rs` 不再处于 1000 行热点集。Runtime 07、large-file owner gate、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫已同步；不改 active editor/UI/render/plugin 生产代码。验证：rustfmt check 通过；Python py_compile 通过；direct `performance_hotpath_boundary_audit` risks=0 / hotspots=37；direct `runtime_plan_status_boundary_audit` risks=0 / Runtime 07 owner-budget anchors index 6/6、guard 5/5、docs 3/3；standalone `performance_hotspots.rs` 6/6；standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 39-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | 当前静态审计更新为 39-hotspot owner-budget：`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 39`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=12`、`support-hub=3`、`hotspot_guard_anchor_count = 23` 与 `risks = []`；当前漂移来自活跃 render/post-process owner 文件 `zircon_runtime/src/core/framework/render/post_process/stack.rs` 与 `zircon_runtime/src/core/framework/render/post_process/volume_component.rs` 进入 1000 行阈值以上。Runtime 07、large-file owner gate、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫已同步；不改 active editor/UI/render/plugin 生产代码。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=39、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 42-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-17 | 当前静态审计更新为 42-hotspot owner-budget：`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 42`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=15`、`support-hub=3`、`hotspot_guard_anchor_count = 23` 与 `risks = []`；当前漂移来自活跃 render/post-process/runtime owner 文件 `zircon_runtime/src/core/framework/render/post_process/stack.rs`、`zircon_runtime/src/core/framework/render/post_process/volume_component.rs`、`zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_graph_execution_resources.rs` 与 `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu/post_process.rs` 进入 1000 行阈值以上。Runtime 07、large-file owner gate、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables` 与 `performance_hotspots.rs` 守卫已同步；不改 active editor/UI/render/plugin 生产代码。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=42、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget current doc mirror fix | mirror_docs_static_passed_cargo_pending | 2026-06-17 | 修正当前 owner-budget 正文漂移：`hotspot_inventory.md`、M0 review 与 runtime-interface convergence 的 current large-file gate 均同步为 42 hotspots / 5 migration-debt owner groups / 0 unclassified hotspots；`runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit` 新增逐文档 42-hotspot 当前句子断言，防止历史 39-hotspot 漂移行被误用为当前 owner-budget 证据。验证目标：rustfmt check、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=42、standalone `performance_hotspots.rs` 6/6、standalone plan-status status-output guard；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 36-hotspot navigation split sync | mirror_docs_static_passed_cargo_pending | 2026-06-17 | Runtime 14 navigation fallback runtime owner split 后，`zircon_runtime/src/navigation/runtime.rs` 降到 299 行并移出大文件热点；`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 36`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=10`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=12`、`support-hub=3` 与 `risks = []`；`runtime_07_navigation_runtime_owner_split_reduces_owner_budget_hotspot_count`、Runtime 07、large-file owner gate、hotspot inventory、M0 review、runtime-interface convergence、plan-status output anchors 与 `performance_hotspots.rs` 守卫已同步。不改 render/UI/plugin 生产代码；navigation 生产代码仍由 Runtime 14 owner。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=36、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget 30-hotspot current audit sync | mirror_docs_static_passed_cargo_pending | 2026-06-20 | 当前静态审计更新为 30-hotspot owner-budget：`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 30`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=3`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=13`、`support-hub=3` 与 `risks = []`；Runtime 07、large-file owner gate、hotspot inventory、M0 review、runtime-interface convergence、plan-status output anchors 与 `performance_hotspots.rs` 守卫已同步。不改 active editor/UI/render/plugin 生产代码。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=30、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M2 | Runtime 07 extract rebuild cache | extract_rebuild_cache_static_passed_cargo_deferred | 2026-06-17 | `RuntimeFrameExtractCache` 接入 `RuntimeDynamicSession::current_extract(&mut self)`，以 `change_tick`、`query_cache_revision`、active camera、viewport size 作为缓存键；`extract.rebuild_clones` 未变更 headless capture 从 `[1,1]` 收紧为 `[1,0]`，第二帧明确记录 `extract.rebuild_clones = 0`，`frame_extract_rebuilds_after_scene_change` 锁定场景 transform 变更后 `[1,1]`。该切片首次将 `performance_hotpath_boundary` 同步为 `expected_source_file_count = 25`、`extract_counter_anchor_count = 17`、`test_anchor_count = 23`、`doc_anchor_count = 24`，并在 hit/miss 诊断后复验到当前静态镜像 `extract_counter_anchor_count = 21`、`test_anchor_count = 26`、`doc_anchor_count = 27`、missing anchors 空、`risks = []`；验证通过 rustfmt/Python/standalone `performance_hotspots.rs` 6/6 与 `plan_status.rs` 32/32，包级 `extract`、profiling、FPS Cargo gates 按用户要求后续补跑。 |
| M2 | Runtime 07 extract cache hit/miss diagnostics | extract_cache_hit_miss_static_passed_cargo_deferred | 2026-06-17 | `RuntimeFrameExtractStats` 新增 `EXTRACT_CACHE_HITS_DIAGNOSTIC` / `EXTRACT_CACHE_MISSES_DIAGNOSTIC` 与 `extract.cache_hits` / `extract.cache_misses`，首帧记录 hits/misses `[0,1]`，未变更后续 capture 记录 `[1,0]`，场景 transform 变更后 hits `[0,0]` / misses `[1,1]`；`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 26`、`extract_counter_anchor_count = 21`、`test_anchor_count = 26`、`doc_anchor_count = 27`、missing anchors 空、`risks = []`。验证通过 rustfmt/Python/standalone `performance_hotspots.rs` 6/6 与 `plan_status.rs` 32/32，包级 `extract`、profiling、FPS Cargo gates 按用户要求后续补跑。 |
| M1 | Runtime 07 asset worker frame sampler | asset_worker_frame_sampler_static_passed_cargo_deferred | 2026-06-17 | `AssetWorkerPoolFrameSampler` / `AssetWorkerPoolFrameDiagnostics` 已在 asset worker owner 内提供帧级采样游标，累计 worker 完成/失败计数被转换为 `asset.worker.frame_completed` / `asset.worker.frame_failed` 每帧 delta，同时保留 `asset.worker.in_flight` 与 `asset.worker.budgeted_threads` 当前值。新增 `worker_pool_frame_sampler_records_per_frame_completion_deltas`；`asset_pipeline_boundary` 当前同步为 `worker_diagnostic_count = 7`、`behavior_test_anchor_count = 20`，`performance_hotpath_boundary` 当前同步为 `asset_worker_anchor_count = 13`、`doc_anchor_count = 27`、missing anchors 空、`risks = []`。验证：rustfmt check、Python py_compile、direct asset/performance boundary audits、standalone `asset_pipeline.rs` 1/1、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32 通过；focused Cargo 240s timeout no result，残留 cargo/rustc/rustdoc scan 为空，包级 `worker_pool`、`extract`、profiling、FPS Cargo gates 后续补跑。 |
| M1 | Runtime 04 worker-pool manager frame sampler entry | asset_worker_manager_sampler_static_passed_cargo_deferred | 2026-06-17 | `ProjectAssetManager::spawn_worker_pool_with_frame_sampler(...)` 已提供 manager-owned worker pool + frame sampler 配对入口，`project_asset_manager_spawns_worker_pool_with_frame_sampler` 锁定首帧采样基线；`asset_pipeline_boundary` 当前同步为 `test_anchor_count = 24`、`behavior_test_anchor_count = 20`。broader `asset::` / `worker_pool` Cargo filters 后续补跑。 |
| M1 | Runtime 07 asset worker manager sampler entry | asset_worker_manager_sampler_static_passed_cargo_deferred | 2026-06-17 | `performance_hotpath_boundary` 已把 `zircon_runtime/src/asset/pipeline/manager/project_asset_manager/construction.rs` 纳入 Runtime 07 asset-worker telemetry 审计，锁定 `spawn_worker_pool_with_frame_sampler` 与 `AssetWorkerPoolFrameSampler::from_pool(&pool)`；当前 `expected_source_file_count = 26`、`asset_worker_anchor_count = 13`。包级 `worker_pool` / `extract` / FPS / profiling Cargo gates 后续补跑。 |
| 横切 | Runtime 07 artifact cache payload owner split | artifact_cache_payload_owner_split_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/asset/artifact/cache_payload.rs` 的 JSON canonical value、Mesh wire payload 与 TOML table/value conversion 已拆入 `cache_payload/{json_value,mesh,toml_value}.rs`，dispatcher 降到 641 行；`runtime_07_artifact_cache_payload_owner_split_keeps_wire_types_folder_backed` 防止这些 cache wire owner 回流根文件。`asset_pipeline_boundary` 当前同步为 `expected_source_file_count = 22`、`missing_artifact_cache_anchors = []`、`risks = []`；`performance_hotpath_boundary` 当前同步为 `hotspot_guard_anchor_count = 24`、`large_file_hotspot_count = 41`、`runtime-other = 16`、`risks = []`。包级 asset/runtime Cargo gates 按实现优先策略后续补跑。 |
| 横切 | Runtime 07 render product diagnostics owner split | render_product_diagnostics_owner_split_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/core/runtime/diagnostics/render_stats_store/product.rs` 降为 render product family dispatcher，camera/visibility/HZB/light-grid/effect-stack/material/light/mesh-queue/GPUScene/sprite/UI 投影拆入 `render_stats_store/product/{camera,visibility,hzb,light_grid,effect_stack,material,light,mesh_queue,gpu_scene,sprite,ui}.rs`，状态表短锚为 `render_stats_store/product/{camera,mesh_queue,gpu_scene}.rs`；`runtime_07_render_product_diagnostics_owner_split_keeps_families_folder_backed` 防止各产品族 `record_*` 行为回流根文件。`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 38`、`hotspot_guard_anchor_count = 25`、`doc_anchor_count = 29`、`large_file_hotspot_count = 39`、`runtime-other = 15`、`risks = []`。包级 Runtime 07 Cargo gates 按实现优先策略后续补跑。 |
| 横切 | Runtime 07 animation scene frame diagnostics | animation_scene_frame_diagnostics_static_passed_cargo_deferred | 2026-06-17 | `AnimationSceneFrameDiagnostics` 已在 `animation/scene_hook/diagnostics.rs` 记录 `animation.scene.scanned_entities`、sequence/clip/graph/state-machine sample counts、`animation.scene.output_poses`、`animation.scene.applied_transforms`、`animation.scene.published_events` 与 `animation.scene.state_transitions`，并由 `tick_animation_world(...)` 在空/禁用/缺 asset manager/正常采样路径写入同一帧 count rows。`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 44`、`animation_scene_anchor_count = 19`、`hotspot_guard_anchor_count = 29`、`doc_anchor_count = 33`、`large_file_hotspot_count = 39`、`risks = []`；该切片只补 animation scene hook 取证入口，轻量验证已通过 rustfmt/Python/direct audit/standalone guards；短时 core-min Cargo check 244s 超时无诊断输出，包级 `animation` / Runtime 07 Cargo gates 后续补跑。 |
| 横切 | Runtime 07 profile counter hotspot export | profiling_counter_hotspot_export_static_passed_cargo_deferred | 2026-06-20 | Runtime profiling export 现在额外生成 `counter_hotspots.json`：`CounterHotspotReport` / `CounterHotspotEntry` / `PROFILE_COUNTER_HOTSPOTS_FILE` / `ProfileControlResponse.counter_hotspot_report` 进入 `zircon_runtime_interface::profiling`，`analyze_counter_hotspots` 按 `stream/name/path` 聚合有限正值 counters 并输出 total/avg/p95/max/latest/count/frame_count；`export_report` 写出 artifact、summary 增加 `Counter Hotspots`，并把 counter candidates 接在 UI alerts 与 span hotspots 之后。`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 46`、`profile_counter_hotspot_anchor_count = 8`、`hotspot_guard_anchor_count = 32`、`test_anchor_count = 29`、`doc_anchor_count = 35`；该切片只补 profiling counter 证据入口，不声明 Runtime 07 M2 优化或权威 top list 完成。验证：rustfmt/Python/direct audit/standalone `performance_hotspots.rs` 9/9/standalone `plan_status.rs` 32/32 与 `cargo check -p zircon_runtime_interface --locked` 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked` 180s timeout no result 且残留 cargo/rustc 已停止，包级 Cargo/profiling/FPS gates 后续补跑。 |
| 横切 | Runtime 07 QueryState cache owner performance audit sync | query_state_cache_owner_perf_audit_sync_static_passed_cargo_pending | 2026-06-17 | `performance_hotpath_boundary` 已把 `query_state/cache.rs` 纳入 Runtime 07 QueryState telemetry 扫描，当前 direct audit 报告 `expected_source_file_count = 45`、`query_counter_anchor_count = 32`、`missing_query_counter_anchors = []`、`missing_doc_anchors = []`、`risks = []`；`runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` 与当前镜像正文已同步到 45-source 口径，包级 `ecs_query` / Runtime 07 Cargo gates 后续补跑。 |
| 横切 | Runtime 07 virtual geometry debug snapshot owner split | virtual_geometry_debug_snapshot_owner_split_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/core/framework/render/virtual_geometry_debug_snapshot.rs` 降为 structural facade，BVH/CPU reference/cull input/execution/node-cluster cull/top-level snapshot/provenance owners 拆入 `virtual_geometry_debug_snapshot/{bvh_visualization,cpu_reference,cull_input,execution,node_and_cluster_cull,snapshot,sources}.rs`，状态表短锚为 `virtual_geometry_debug_snapshot/{cull_input,node_and_cluster_cull,snapshot}.rs`；新增 `runtime_07_virtual_geometry_debug_snapshot_owner_split_keeps_contracts_folder_backed` 防止 DTO/pack/worklist 声明回流根文件。该行保留本切片当时的 owner-budget 证据 `Runtime 07 owner-budget 36-hotspot navigation split sync`，当前 owner-budget 已由 `Runtime 07 owner-budget 30-hotspot current audit sync` 行接管；extract/ecs_query/performance profiling/FPS Cargo gates 按实现优先策略后续补跑。 |
| 横切 | Runtime 07 scene asset owner split | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `zircon_runtime/src/asset/assets/scene.rs` 已切为 folder-backed `scene/{mod,animation,asset,camera,defaults,entity,extensions,lighting,management,mesh,physics,post_process,transform}.rs`，`mod.rs` 只保留结构声明/导出与 `SceneMobilityAsset`，当前最大 owner 文件 `management.rs` 432 行；Runtime 07、`large-file-ownership-m1.md`、`hotspot_inventory.md`、M0 review、runtime-interface convergence 与 scene module doc 已同步到 38-hotspot / runtime-other=14 state；验证为 rustfmt/结构审计/standalone guard 层，extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 scene asset split-drift repair | split_drift_static_passed_cargo_deferred_active_lanes | 2026-06-14 | 删除拆分后遗留在 `zircon_runtime/src/asset/assets/scene/physics.rs` 的重复 `SceneMobilityAsset` 定义，确认 `scene/mod.rs` 是唯一 owner，且 `SceneSpotLightAsset` 公开链仍覆盖 `lighting.rs` / `scene/mod.rs` / `asset/assets/mod.rs` / `asset/mod.rs`；`rustfmt --edition 2021 --check` 覆盖所有 scene asset owner 文件，重复 enum 与导出路径扫描通过。当前外部 `taa_resolve` / `core-min` Cargo/rustc 通道仍在运行，本行只声明静态修复通过，`scene_asset` 与 Runtime 07 Cargo gates 继续 pending。 |
| 横切 | Runtime 07 scene asset folder-split public-surface guard | folder_split_guard_static_passed_cargo_deferred_active_lanes | 2026-06-14 | 新增 `runtime_07_scene_asset_folder_split_keeps_public_surface_and_single_owner`，锁定 scene asset folder-backed 声明、`SceneMobilityAsset` 单一 owner、`SceneSpotLightAsset` 字段/导出链与 Runtime 07/scene docs split-drift 状态锚点；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\performance_hotspots.rs` 通过，standalone `rustc --edition 2021 --test ...performance_hotspots.rs` 通过 6/6。包级 `scene_asset` / Runtime 07 Cargo gates 仍待 active lanes 清空后补跑。 |
| 横切 | Runtime 07 scene asset guard 纳入 performance_hotpath_boundary | boundary_guard_anchor_static_passed_cargo_deferred_active_lanes | 2026-06-14 | `performance_hotpath_boundary.py` 已将 `runtime_07_scene_asset_folder_split_keeps_public_surface_and_single_owner` 纳入 `HOTSPOT_GUARD_ANCHORS`，Runtime 07 镜像计数同步为 `hotspot_guard_anchor_count = 20`、`missing_hotspot_guard_anchors = []`、`risks = []`；Runtime 07、hotspot inventory、dynamic-session/ECS docs、M0 review 与 runtime-interface convergence 均同步 20-anchor 口径。验证：Python py_compile 通过；direct `performance_hotpath_boundary_audit` risks=0 / hotspot_guard_anchor_count=20；standalone `performance_hotspots.rs` 6/6；包级 Runtime 07 Cargo gates pending。 |
| 横切 | Runtime 07 project_io folder split | folder_split_static_passed_cargo_deferred_active_lanes | 2026-06-14 | `zircon_runtime/src/scene/world/project_io.rs` 的资源引用、脚本绑定、Transform、Camera、PostProcess 与 Physics shape 转换 helper 已拆入 `project_io/{camera,physics,post_process,references,script,transform}.rs`，入口文件保留 `World` 项目 I/O 编排并降到 772 行；新增 `docs/zircon_runtime/scene/world/project_io.md` 与 `runtime_07_project_io_folder_split_keeps_entry_and_converter_owners`，`performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 38`、`runtime-other=14`、`hotspot_guard_anchor_count = 23`、`risks = []`。验证：project_io 入口与 6 个子模块 rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38；包级 Cargo check 尝试 240s 超时且外部 editor/render lanes 仍在编译，未声明通过。 |
| 横切 | Runtime 05 M0 absorption guard coverage sync | static_docs_passed_cargo_pending | 2026-06-14 | `runtime_architecture_review_documents_all_absorption_guards` 的 M0 review 覆盖已同步到当前 25 个 mounted `runtime_absorption` guard：补齐 `runtime_absorption/ecs_kernel_data.rs`、`runtime_absorption/script_binding.rs`、`runtime_absorption/tech_stack.rs` 的 frontmatter/body 覆盖，并移除重复 `core_spine_root_generated.rs` frontmatter 行。验证：full M0 absorption guard coverage simulation 25/25 通过、conflict/trailing scans 通过、scoped `git diff --check` 仅 LF-to-CRLF warning；Runtime 05 closeout 仍等待 full `scene::` Cargo gate。 |
| 横切 | Runtime M0 entry static dependencies Markdown renderer split | entry_static_dependencies_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `entry_static_dependencies_markdown_split_static_passed_cargo_deferred_tests_deferred`；`entry_static_dependencies_markdown.py` now owns `render_entry_static_dependencies_markdown(...)`, while `entry_static_dependencies.py` remains the 73-line Cargo path dependency, runtime plugin fan-out, builtin entry/runtime module, and risk scanner. Direct probe reports app path dependency count 4 (`zircon_editor`, `zircon_first_party_runtime_catalog`, `zircon_runtime`, `zircon_runtime_interface`), optional runtime plugin path dependency count 0, optional runtime plugin feature mention count 0, builtin entry/runtime module count 1, risk count 0, Markdown owner 29 lines, and rendered output 5 lines. Validation: Python py_compile, direct entry static dependency/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| 横切 | Runtime M0 legacy standalone references Markdown renderer split | legacy_standalone_references_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `legacy_standalone_references_markdown_split_static_passed_cargo_deferred_tests_deferred`；`legacy_standalone_references_markdown.py` now owns `render_legacy_standalone_references_markdown(...)`, while `legacy_standalone_references.py` remains the 83-line architecture-doc stale standalone crate scanner and sample collector. Direct probe reports 10 legacy standalone crate terms, `reference_count=0`, `referenced_term_count=0`, `sample_location_count=0`, rendered output 2 lines, and Markdown owner 14 lines. Validation: Python py_compile, direct legacy standalone reference/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| 横切 | Runtime M0 module inventory Markdown renderer split | module_inventory_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `module_inventory_markdown_split_static_passed_cargo_deferred_tests_deferred`；`module_inventory_markdown.py` now owns module descriptor distribution, stub descriptor usage, `EngineModule` owner coverage, module classification, and support-crate Markdown renderers, while `module_inventory.py` remains the 193-line workspace production inventory / classification / hotspot-source owner. Direct inventory evidence reports `zircon_crates=6`, `module_crates=3`, `support_crates=3`, `stub_descriptor_crates=0`, `plugin_gap_count=0`, large-file hotspot source count 30, and Markdown owner 62 lines. Validation: Python py_compile, direct module inventory/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| 横切 | Runtime M0 plugin runtime gaps Markdown renderer split | plugin_runtime_gaps_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `plugin_runtime_gaps_markdown_split_static_passed_cargo_deferred_tests_deferred`；`plugin_runtime_gaps_markdown.py` now owns `render_plugin_runtime_gaps_markdown(...)`, while `plugin_runtime_gaps.py` remains the 77-line runtime plugin registration/resolve/context gap scanner. Direct probe reports `zircon_crate_count=6`, production Rust file count 7583, `plugin_gap_count=0`, rendered output 2 lines, and Markdown owner 11 lines. Validation: Python py_compile, direct plugin runtime gap/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| 横切 | Runtime M0 large-file ownership Markdown renderer split | large_file_ownership_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `large_file_ownership_markdown_split_static_passed_cargo_deferred_tests_deferred`；`large_file_ownership_markdown.py` now owns large-file hotspot, ownership-class, and ownership-gate Markdown renderers, while `large_file_ownership.py` remains the 223-line owner classification, hotspot summary, migration-debt, and risk gate owner. Direct probe reports `zircon_crate_count=6`, production Rust file count 7583, `hotspot_count=30`, `classification_count=5`, `large_file_migration_debt_count=5`, `unclassified_hotspot_count=0`, `m1_gate_status=migration-debt-present`, rendered section line counts 11/6/39, and Markdown owner 73 lines. Validation: Python py_compile, direct large-file ownership/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| 横切 | Runtime 05 hard-cutover migration-smell Markdown renderer split | hard_cutover_migration_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | Status anchor `hard_cutover_migration_markdown_split_static_passed_cargo_deferred_tests_deferred`; `hard_cutover_migration_smells_markdown.py` owns `render_hard_cutover_migration_smells_markdown(...)`, while `hard_cutover_migration_smells.py` remains the 408-line production Rust scanner/classifier/risk owner and now classifies Runtime 05 dynamic scene document legacy refs under `legacy-runtime-scene-document-debt`. Direct audit reports source_file_count 8520, legacy_reference_count 152, compat/shim 0, allowed business bridge references 316, migration-context bridge references 0, classification_count 6, hard_cutover_migration_debt_count 6, unclassified_location_count 0, Markdown owner 60 lines, rendered_lines 60. Validation: Python py_compile, direct `hard_cutover_migration_smells_audit`, and standalone `plan_status.rs` 33/33; full Runtime 05 `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 09 UI architecture Markdown renderer split | ui_architecture_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | Status anchor `ui_architecture_markdown_split_static_passed_cargo_deferred_tests_deferred`; `ui_architecture_markdown.py` owns `render_ui_architecture_boundary_markdown(...)`; `ui_architecture_boundary.py` remains the 541-line audit/risk owner, and the Markdown owner is 110 lines. Direct audit reports source files 52/52, ui entries 18/18, surface entries 20/20, legacy full hits 54/54, production legacy hits 0/0 across 0 files, taffy production hits 173/173 across 9 files, runtime ui::v2 anchors 10/10, interface ui::v2 anchors 9/9, guard anchors 19/19, pending UI owner/Cargo gate anchors 7/7, doc anchors 61/61, `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile, direct `ui_architecture_boundary_audit`, standalone `ui_architecture.rs` 18/18, and standalone `plan_status.rs` 33/33; full UI behavior filters and package Cargo remain deferred under the existing `ui/input/naming_boundary/layout/template` owner gate. |
| 横切 | Runtime 09 UI architecture 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-17 | `runtime_09_ui_architecture_mirror_docs_match_structure_audit_counts` 锁定 `ui_architecture_boundary` 的 `expected_source_file_count = 52`、`expected_ui_entry_count = 18`、`expected_surface_entry_count = 20`、`legacy_full_hits = 54`、`expected_legacy_full_hits = 54`、`legacy_production_hits = 0`、`expected_legacy_production_hits = 0`、`legacy_production_file_count = 0`、`expected_legacy_production_file_count = 0`、`taffy_production_hits = 175`、`expected_taffy_production_hits = 175`、`taffy_production_file_count = 10`、`expected_taffy_production_file_count = 10`、`runtime_v2_anchor_count = 10`、`interface_v2_anchor_count = 9`、`guard_anchor_count = 19`、`cargo_gate_anchor_count = 7`、`doc_anchor_count = 61`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true` 与 `risks = []`；Runtime 09、UI architecture doc、surface input doc、layout pass doc、template pipeline doc、shared template runtime doc、UI v2 doc、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit`、standalone rustc 18/18；完整 layout/template/ui behavior filters pending；ui/input/naming_boundary/layout/template Cargo gates pending。 |
| M1 | Runtime 09 UI input route authority | runtime_09_m1_1_ui_input_route_authority_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_09_m1_1_ui_input_route_authority_static_passed_cargo_pending`；`zircon_runtime/src/ui/surface/input/route_authority.rs` 新增 `runtime_09_m1_1_ui_input_route_authority`，`surface/input/dispatch.rs` 的 normalized `UiInputEvent` 分支统一在返回前调用 `annotate_authoritative_input_dispatch`，向 diagnostics notes 写入 `route_authority=...;policy=...;stages=...`；阶段来自 `UI_INPUT_ROUTE_ORDER`，结构守卫 `runtime_09_ui_input_events_route_through_single_dispatch_authority` 锁定该单点出口与 docs/index 状态锚。旁路清单已给 owner 判词 `runtime_09_m1_1_direct_pointer_navigation_routes_are_leaf_owner_helpers`：`dispatch_pointer_event*` / `dispatch_navigation_event` 保留为 leaf helper，不是 normalized route authority；standalone rustc `ui_architecture.rs` 6/6 通过；Cargo/行为测试按当前要求延后。 |
| M1 | Runtime 09 navigation legacy reply rename | runtime_09_m1_2_navigation_legacy_reply_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/surface/input/navigation.rs` 本地路由回复变量 `legacy` 硬切为 `routed_reply`，行为路径不变；状态锚 `runtime_09_m1_2_navigation_legacy_reply_renamed_static_passed_cargo_pending` 与 `runtime_09_navigation_legacy_reply_rename_reduces_ui_input_debt` 锁定 navigation input 源码不再含 `legacy`；UI legacy 基线从原始 `ui_legacy_hits=167` / `ui_legacy_production_hits=102` / `ui_legacy_production_files=12` 降为当前 `ui_legacy_hits=153` / `ui_legacy_production_hits=88` / `ui_legacy_production_files=11`，`ui_architecture_boundary` 当轮同步为 guard 7 / doc 14。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 6/6（standalone rustc 6/6）、conflict/trailing scans 通过；Cargo gates 仍等待 active lanes 空窗。 |
| M1 | Runtime 09 pointer legacy reply rename | runtime_09_m1_2_pointer_legacy_reply_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/surface/input/pointer.rs` 与 `zircon_runtime/src/ui/surface/input/pointer_reply.rs` 本地路由结果变量 `legacy` 硬切为 `routed_result`，行为路径不变；状态锚 `runtime_09_m1_2_pointer_legacy_reply_renamed_static_passed_cargo_pending` 与 `runtime_09_pointer_legacy_reply_rename_reduces_ui_input_debt` 锁定两个 pointer input 源码文件不再含 `legacy`；UI legacy 当轮基线降为 `ui_legacy_hits=104` / `ui_legacy_production_hits=39` / `ui_legacy_production_files=9`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 10/10（standalone rustc 10/10）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 pointer capture fallback rename | runtime_09_m1_2_pointer_capture_fallback_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/surface/input/state/pointer_capture.rs` 与 `zircon_runtime/src/ui/surface/input/effect/focus_pointer.rs` 的 pointer capture fallback 判断从 `has_legacy_or_indexed_pointer_capture_for_owner` 硬切为 `has_pointer_capture_or_unindexed_fallback_for_owner`，行为路径不变；状态锚 `runtime_09_m1_2_pointer_capture_fallback_renamed_static_passed_cargo_pending` 与 `runtime_09_pointer_capture_fallback_rename_reduces_ui_input_debt` 锁定旧 API 名称不再回归；UI legacy 当前基线降为 `ui_legacy_hits=102` / `ui_legacy_production_hits=37` / `ui_legacy_production_files=7`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 11/11（standalone rustc 11/11）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 table row label fallback rename | runtime_09_m1_2_table_row_label_fallback_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/surface/render/collection_rows/table.rs` 的 row-label fallback splitter 从 `split_legacy_table_text` 硬切为 `split_row_label_table_text`，行为路径不变；状态锚 `runtime_09_m1_2_table_row_label_fallback_renamed_static_passed_cargo_pending` 与 `runtime_09_table_row_label_fallback_rename_reduces_ui_render_debt` 锁定旧 helper 名称不再回归；UI legacy 当前基线降为 `ui_legacy_hits=100` / `ui_legacy_production_hits=35` / `ui_legacy_production_files=6`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 12/12（standalone rustc 12/12）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 template component-name fallback rename | runtime_09_m1_2_template_component_name_fallback_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/template/build/interaction.rs` 的 template interaction fallback 从 `legacy_component_interaction_fallback` / `legacy_interactive` 硬切为 `component_name_interaction_fallback` / `component_name_interactive`，行为路径不变；状态锚 `runtime_09_m1_2_template_component_name_fallback_renamed_static_passed_cargo_pending` 与 `runtime_09_template_component_name_fallback_rename_reduces_ui_template_debt` 锁定旧 helper/local 名称不再回归；UI legacy 当前基线降为 `ui_legacy_hits=95` / `ui_legacy_production_hits=30` / `ui_legacy_production_files=5`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 13/13（standalone rustc 13/13）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 property visibility flag rename | runtime_09_m1_2_property_visibility_flag_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/surface/property_mutation.rs` 的 visibility transition helper 参数从 `legacy_visible` 硬切为 `state_visible_flag`，行为路径不变；状态锚 `runtime_09_m1_2_property_visibility_flag_renamed_static_passed_cargo_pending` 与 `runtime_09_property_visibility_flag_rename_reduces_ui_surface_debt` 锁定旧 local 名称不再回归；UI legacy 当前基线降为 `ui_legacy_hits=92` / `ui_legacy_production_hits=27` / `ui_legacy_production_files=4`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 14/14（standalone rustc 14/14）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 responsive MUI visibility flag rename | runtime_09_m1_2_responsive_mui_visibility_flag_renamed_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/ui/layout/pass/responsive_mui.rs` 的 responsive visibility DTO 从 `legacy_visible` 硬切为 `state_visible_flag`，行为路径不变；状态锚 `runtime_09_m1_2_responsive_mui_visibility_flag_renamed_static_passed_cargo_pending` 与 `runtime_09_responsive_mui_visibility_flag_rename_reduces_ui_layout_debt` 锁定旧 local 名称不再回归；UI legacy 当前基线降为 `ui_legacy_hits=84` / `ui_legacy_production_hits=19` / `ui_legacy_production_files=3`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 15/15（standalone rustc 15/15）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 accessibility open-state fallback rename | runtime_09_m1_2_accessibility_open_state_fallback_renamed_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_09_m1_2_accessibility_open_state_fallback_renamed_static_passed_cargo_pending`；`zircon_runtime/src/ui/accessibility/extract.rs` 的 open-state fallback property set 从 `legacy_properties` / `legacy_property` 硬切为 `fallback_properties` / `fallback_property`，行为路径不变；状态锚与 `runtime_09_accessibility_open_state_fallback_rename_reduces_ui_a11y_debt` 锁定旧 helper/local 名称不再回归；UI legacy 当轮基线降为 `ui_legacy_hits=76` / `ui_legacy_production_hits=11` / `ui_legacy_production_files=2`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 16/16（standalone rustc 16/16）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 layout engine backend name cutover | runtime_09_m1_2_layout_engine_backend_name_cutover_static_passed_cargo_pending | 2026-06-16 | 状态锚 `runtime_09_m1_2_layout_engine_backend_name_cutover_static_passed_cargo_pending`；`zircon_runtime_interface/src/ui/layout/engine.rs` 与 `zircon_runtime/src/ui/layout/pass/engine.rs` 将公开后端名从 `LegacyZircon` / `legacy_zircon` / `legacy_selected_count` 硬切为 `UiLayoutEngineBackend::Zircon`、`UiLayoutEngineCapability::zircon()` 与 `zircon_selected_count`，不保留兼容别名；`runtime_09_layout_engine_backend_name_cutover_reduces_ui_layout_debt` 锁定旧公开名称不再回流。UI legacy 当前基线降为 `ui_legacy_hits=63` / `ui_legacy_production_hits=9` / `ui_legacy_production_files=1`，剩余生产 legacy 文件为 `surface/surface/default_interactions.rs`。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 17/17（standalone rustc 17/17）；Cargo/行为测试按当前实现优先要求延后。 |
| M1 | Runtime 09 surface default interaction fallback rename | runtime_09_m1_2_surface_default_interaction_fallback_renamed_static_passed_cargo_pending | 2026-06-17 | 状态锚 `runtime_09_m1_2_surface_default_interaction_fallback_renamed_static_passed_cargo_pending`；`zircon_runtime/src/ui/surface/surface/default_interactions.rs` 的 `default_open_boolean_value` 将 open-state fallback property set 从 `legacy_properties` / `legacy_property` 硬切为 `fallback_properties` / `fallback_property`，行为路径不变；`runtime_09_surface_default_interaction_fallback_rename_reduces_ui_surface_debt` 锁定旧 local 名称不再回归。UI legacy 当前基线降为 `ui_legacy_hits=54` / `ui_legacy_production_hits=0` / `ui_legacy_production_files=0`，M1 production legacy 命名桶清零。验证：rustfmt check、Python py_compile、direct `ui_architecture_boundary_audit` risks=0、standalone rustc `ui_architecture.rs` 18/18（standalone rustc 18/18）；Cargo/行为测试按当前实现优先要求延后。 |
| 横切 | Runtime 14 Module family 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `runtime_14_module_family_mirror_docs_match_structure_audit_counts` 锁定 `module_family_boundary` 的 `expected_family_count = 4`、`animation = 28`、`navigation = 9`、`diagnostic_log = 7`、`engine_module = 8`、`root_seat_guard_present = true`、`animation_status_json_guard_present = true`、`animation_status_json_anchor_count = 8`、`missing_animation_status_json_anchors = []`、`module_family_guard_anchor_count = 7`、`missing_module_family_guard_anchors = []` 与 `risks = []`；Runtime 14、M0 review、runtime-interface convergence 与本总索引已同步。验证：rustfmt check、Python py_compile、direct `module_family_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 14 assertions、standalone rustc 13/13、conflict/trailing scans 通过；module-family Cargo/rustc gates pending。 |
| 横切 | Runtime 14 Module family Cargo gate 审计元数据 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `module_family_boundary` 新增 `cargo_gate_anchor_count = 5`、`missing_cargo_gate_anchors = []`，锁定 Runtime 14 计划中 `cargo test -p zircon_runtime --lib animation --locked`、`cargo test -p zircon_runtime --lib navigation --locked`、`cargo test -p zircon_runtime --lib diagnostic_log --locked`、`cargo test -p zircon_runtime --lib engine_module --locked` 与 `cargo test -p zircon_runtime --lib --locked` 五个待验证门槛；`runtime_14_module_family_mirror_docs_match_structure_audit_counts` 要求 Runtime 14、总索引、M0 review 与 runtime-interface convergence 同步这些字段。验证仍为静态审计/standalone rustc 层，module-family Cargo/rustc gates pending。 |
| 横切 | Runtime 14 Module family guard anchors 审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `module_family_boundary` 与 `runtime_14_module_family_mirror_docs_match_structure_audit_counts` 现在锁定 7 个四族守卫锚，要求保留 `module_family_guard_anchor_count = 7`、`missing_module_family_guard_anchors = []`、standalone root_entries 13/13 与 standalone status-output 2/2 evidence；module-family Cargo/rustc gates pending，具体为 `animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates pending。 |
| 横切 | Runtime 14 Cargo 验证窗口探测 | cargo_deferred_active_lane | 2026-06-14 | 10:26 后发现外部 editor UI core-min Cargo 任务正在运行：`cargo test -p zircon_runtime --lib --no-default-features --features core-min ... tree_view_pointer`，目标目录 `E:\cargo-targets\zircon-editor-ui-tree-drag-reorder-coremin-0614`；本轮未启动 Runtime 14 的五条真实 Cargo gate。补充验证 `runtime_absorption::plan_status::cargo_gates::runtime_14_module_family_cargo_gate_stays_visible_until_filters_pass` standalone 1/1 通过；`animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates pending。 |
| 横切 | Runtime 14 animation Cargo gate 尝试 | cargo_blocked_external_compile_drift | 2026-06-14 | 空闲窗口后运行 `cargo test -p zircon_runtime --lib animation --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-14-module-family-0614 --message-format short --color never -- --nocapture`；命令进入 `zircon_runtime` lib-test 编译，但未执行 Runtime 14 animation 测试。失败属于共享 lib-test 编译层的活跃 render/UI 漂移：`SKINNED_MESH_MAX_JOINT_MATRICES`、`RenderPostProcessEffectStackSettings`、`particle_velocity_missing_sprite_count`、table reducer `String`/`&str`、`AdvancedProfileRuntimePlan: Default`、`FrameSubmissionContext::new` 参数数、`ViewportCameraSnapshot.temporal_jitter`。Runtime 14 四族生产代码未改；`navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates pending。 |
| 横切 | Runtime 14 animation Cargo gate 修复与复验阻塞 | cargo_recheck_blocked_external_ui_compile_drift | 2026-06-14 | 重跑 `cargo test -p zircon_runtime --lib animation --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-14-module-family-0614 --message-format short --color never -- --nocapture` 已执行 animation 过滤测试，结果为 31 passed; 3 failed。已修最低失败层：`AnimationPlayerRuntimeStatus::sanitized_snapshot` 与 serde 有限 JSON 快照、animation/physics 吸收守卫的当前边界判词（animation 仍是 runtime-owned module family，plugin runtime wrapper；physics manager impl 位于 `manager/service.rs`）。复验过滤项被外部 UI 编译漂移阻塞：`UiInputDispatchDiagnostics.capture_started` / `capture_released` 缺失与 `default_interactions/table.rs:257` move 后借用；当前外部 `table_column_resize` core-min Cargo lane 正在运行。Runtime 14 remaining gates pending。 |
| 横切 | Runtime 14 animation runtime-status JSON 边界守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | 新增 `runtime_animation_status_json_boundary_sanitizes_non_finite_values`，锁定 `AnimationPlayerRuntimeStatus::sanitized_snapshot` / `AnimationRuntimeStatus::sanitized_snapshot` 与 serde finite JSON helpers；`module_family_boundary` 同步报告 `animation_status_json_guard_present = true`、`animation_status_json_anchor_count = 8`、`missing_animation_status_json_anchors = []`。该行记录已修复的 animation DTO JSON 边界，不替代 `cargo test -p zircon_runtime --lib animation --locked` 复验；Runtime 14 remaining gates pending。 |
| 横切 | Runtime 14 animation runtime-status focused recheck timeout | cargo_recheck_timeout_no_result | 2026-06-15 | 空闲窗口运行 `cargo test -p zircon_runtime --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-14-module-family-0615 --message-format short --color never runtime_status_reports_player_rig_and_gpu_readiness -- --nocapture`，904s 后仍未执行到目标测试且无测试结果；residual cargo/rustc processes were stopped。Runtime 14 animation runtime-status JSON 边界守卫仍只有静态/standalone evidence，不提升 `animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates。 |
| 横切 | Runtime 14 animation family 28-file audit sync | module_family_source_count_static_passed_cargo_pending | 2026-06-17 | 状态锚 `module_family_source_count_static_passed_cargo_pending`；Runtime 07 animation scene diagnostics owner split 后，`animation/scene_hook/diagnostics.rs` 成为 animation module family 的正式 Rust owner；`module_family_boundary` 与 `runtime_14_module_family_mirror_docs_match_structure_audit_counts` 当前同步为 `expected_family_count = 4`、`animation = 28`、`navigation = 9`、`diagnostic_log = 7`、`engine_module = 8`、`file_count_mismatches = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt check、Python py_compile、direct `module_family_boundary_audit` risks=[]、standalone `root_entries.rs` 13/13、standalone `plan_status.rs` 32/32、aggregate `audit_runtime_structure.py --json` 已清除 module-family 漂移；Runtime 14 Cargo gates pending。 |
| 横切 | Runtime 14 navigation fallback runtime owner split | navigation_runtime_owner_split_static_passed_cargo_pending | 2026-06-17 | 状态锚 `navigation_runtime_owner_split_static_passed_cargo_pending`；navigation fallback runtime 已做 folder-backed runtime owner split：`zircon_runtime/src/navigation/runtime.rs` 保留 manager/orchestration owner，`zircon_runtime/src/navigation/runtime/{baked_mesh,world_scan,avoidance,state,math,tests}.rs` 分别承接 baked polygon graph/A*、ECS agent/obstacle scan、local avoidance、loaded mesh/settings/stats、shared math 与 focused fallback runtime tests；`runtime_navigation_boundary_file_set_requires_doc_update` 同步锁定 `runtime/avoidance.rs`、`runtime/baked_mesh.rs`、`runtime/world_scan.rs` 等文件集；`module_family_boundary` 与 `runtime_14_module_family_mirror_docs_match_structure_audit_counts` 当前同步为 `expected_family_count = 4`、`animation = 28`、`navigation = 9`、`diagnostic_log = 7`、`engine_module = 8`、`file_count_mismatches = []`、`missing_doc_anchors = []`、`risks = []`。验证：rustfmt check、Python py_compile、direct `module_family_boundary_audit` risks=[]、standalone `root_entries.rs` 13/13、standalone `plan_status.rs` 32/32；Cargo gates pending。 |
| 横切 | Runtime 14 module family current audit recheck | module_family_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `module_family_current_audit_static_passed_cargo_pending`；当前工作区复核 `module_family_boundary` 仍为 `expected_family_count = 4`，四族席位均存在，`animation = 28`、`navigation = 9`、`diagnostic_log = 7`、`engine_module = 8`、`root_seat_guard_present = true`、`mirror_docs_guard_present = true`、`animation_status_json_guard_present = true`、`module_family_guard_anchor_count = 7`、`missing_module_family_guard_anchors = []`、`cargo_gate_anchor_count = 5`、`missing_cargo_gate_anchors = []`、`file_count_mismatches = []`、`missing_doc_anchors = []`、`risks = []`。验证：Python py_compile、direct `module_family_boundary_audit`、standalone `root_entries.rs` 13/13、`rustfmt --edition 2021 --check root_entries.rs`；`animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates pending。 |
| 横切 | Runtime 14 module family markdown renderer split | module_family_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `module_family_markdown_split_static_passed_cargo_deferred_tests_deferred`；`module_family_markdown.py` now owns `render_module_family_boundary_markdown`; `module_family_boundary.py` now owns only audit data/risk aggregation at 305 lines, while the Markdown owner is 61 lines. Direct audit reports `expected_family_count = 4`, `animation = 28`, `navigation = 9`, `diagnostic_log = 7`, `engine_module = 8`, `root_seat_guard_present = true`, `mirror_docs_guard_present = true`, `animation_status_json_guard_present = true`, `animation_status_json_anchor_count = 8`, `missing_animation_status_json_anchors = []`, `module_family_guard_anchor_count = 7`, `missing_module_family_guard_anchors = []`, `cargo_gate_anchor_count = 5`, `missing_cargo_gate_anchors = []`, `file_count_mismatches = []`, `missing_doc_anchors = []`, and `risks = []`. Validation: Python py_compile, direct `module_family_boundary_audit` risks=[], standalone `root_entries.rs` 13/13, standalone `plan_status.rs` 33/33; `animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates remain deferred while external compile lanes are active. |
| M1 | Runtime 15 graphics facade visibility note | runtime_15_graphics_facade_visibility_note_static_passed_cargo_blocked_graphics_drift | 2026-06-22 | 状态锚 `runtime_15_graphics_facade_visibility_note_static_passed_cargo_blocked_graphics_drift`；`graphics/mod.rs` now marks crate-private implementation owners, public module entries, public facade exports, crate-visible bridge exports, and test-only access before the mixed `pub` / `pub(crate)` surface. Public entries remain limited to `hybrid_gi_extract_sources`, `prelude`, and `runtime_builtin_graphics`; `backend` / `scene` / `types` remain implementation modules. Guard: `runtime_15_mixed_visibility_has_facade_note`; status-output expected row data includes `graphics/mod.rs`, `Public facade exports`, and the guard anchor. Validation: scoped rustfmt with `skip_children=true` passed; standalone `runtime_15_mixed_visibility_has_facade_note` 1/1 passed; status-anchor static check passed; Cargo focused guard/status-output attempts were blocked by current graphics compile drift (`FrameSubmissionContext::new` arity mismatch and missing `AdvancedProfileRuntimePlan: Default`), so core-min crate Cargo gates remain pending. |
| M5 | Runtime 15 F9 runtime prelude required type coverage | runtime_15_prelude_required_types_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_prelude_required_types_coremin_check_passed`；crate prelude now aggregates `asset::prelude`, `scene::prelude`, `ui::prelude`, and `graphics::prelude`, with subsystem prelude files exposing asset handles/descriptors, ECS world/query/resource types, UI surface/template/v2 types, and graphics module/render pipeline types. Guards: `runtime_prelude_exports_asset_scene_ui_and_graphics_contracts` and `runtime_15_prelude_covers_required_types`; implementation files include `asset/prelude.rs`, `scene/prelude.rs`, `ui/prelude.rs`, `graphics/prelude.rs`, `prelude.rs`, and status-output expected row data. Validation: scoped rustfmt --check passed; focused prelude behavior 1/1 passed; Runtime 15 structure guard 1/1 passed; status-output guards 2/2 passed; core-min `cargo check` passed with existing warnings. |
| M5 | Runtime 15 runtime UI dead-code support split | runtime_15_runtime_ui_dead_code_support_split_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_runtime_ui_dead_code_support_split_coremin_check_passed`；`PublicRuntimeFrame` is now owned by production `ui/public_runtime_frame.rs`, while `RuntimeUiManager`, `RuntimeUiFixture`, input-router, manager errors, and window-event test helpers are test-only under `ui/tests/runtime_ui_support`. `ui/mod.rs` mounts that support only through `#[cfg(test)]` / `#[path = "tests/runtime_ui_support/mod.rs"]` and no longer declares `#[allow(dead_code)] mod runtime_ui;`; the old `ui/runtime_ui/` production directory is removed with no compatibility shim. `graphics/types/viewport_render_frame_from_public_runtime.rs` still consumes `crate::ui::PublicRuntimeFrame` and wraps `frame.extract` in `Arc`. Runtime 09 UI architecture mirror follows the entry-map drift and now records `expected_ui_entry_count = 18`, `ui_taffy_production_hits=175`, and `ui_taffy_production_files=10`. Guard: `runtime_15_runtime_ui_dead_code_surface_is_test_support`; status-output expected row data includes `ui/public_runtime_frame.rs`, `ui/tests/runtime_ui_support`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、ui_architecture 3/3、status-output 2/2 通过；direct ui_architecture_boundary_audit risks=[]（ui entries 18/18、taffy hits/files 175/175 与 10/10）；core-min focused cargo test `runtime_15_runtime_ui_dead_code_surface_is_test_support` 1/1 通过；core-min `cargo check` 通过（既有 warnings）。 |
| M5 | Runtime 15 F12 runtime-owned dead-code suppression cleanup | runtime_15_runtime_owned_dead_code_suppression_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_runtime_owned_dead_code_suppression_cleanup_coremin_check_passed`；runtime-owned dead-code suppression 子切片已清理 `asset/pipeline/worker_pool.rs` 和 `core/runtime/state/module_entry.rs`，避开 active render/provider、plugin、editor 会话区域。Asset worker test-only `request_rx_guard` 由 `request_channel_guard_is_alive_for_test()` 明确读取并由 worker-pool 测试断言，替代 `#[allow(dead_code)]`；`ModuleEntry::descriptor()` 让 runtime devtools 消费 module descriptor 的 name/description/service counts，删除 descriptor 字段抑制。Guard: `runtime_15_runtime_owned_dead_code_suppression_cleanup`; status-output expected row data includes `asset/pipeline/worker_pool.rs`, `core/runtime/state/module_entry.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-f12-cargo-0622` 通过（既有 warnings）。该记录只关闭 runtime-owned 两处 suppression 子切片；graphics resources、script value descriptors 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 script host value descriptor dead-code cleanup | runtime_15_script_host_value_descriptors_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_script_host_value_descriptors_coremin_check_passed`；`script/vm/host/builtin_host_modules.rs` 的 `Vec3` 与 `ColorRgba` 反射值描述器删除 `#[allow(dead_code)]`，改用字段布局哨兵构造并读取 `Vec3 { x, y, z }` 与 `ColorRgba { r, g, b, a }`。该哨兵只保持 descriptor-only 类型的布局被编译器看到，不新增 VM host function；Runtime 13 ledger 仍是 6 host modules、52 fixed host functions、2 fixed script type descriptors。Guard: `runtime_15_script_host_value_descriptors_do_not_suppress_dead_code`; status-output expected row data includes `script/vm/host/builtin_host_modules.rs`, `docs/zircon_runtime/script/vm/host/function_ledger.md`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-f12-script-cargo-0622` 通过（既有 warnings）。该记录只关闭 script host value descriptor 子切片；graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 offscreen target texture owner cleanup | runtime_15_offscreen_target_texture_owner_cleanup_static_passed_cargo_timeout_no_result | 2026-06-22 | 状态锚 `runtime_15_offscreen_target_texture_owner_cleanup_static_passed_cargo_timeout_no_result`；`graphics/backend/render_backend/offscreen_target.rs` 不再用 `#[allow(dead_code)]` 掩盖固定帧 texture owner，而是通过 `OffscreenTarget::RETAINED_FRAME_TEXTURE_COUNT` / `retained_frame_texture_count()` 明确读取 9 个 WGPU texture owner；compiled-scene `bind_frame_graph_resources(...)` 在生产路径 debug-assert 该 owner 契约，确保 graph-imported `TextureView` 背后的资源生命周期由 OffscreenTarget 保留。Guard: `runtime_15_offscreen_target_texture_owner_cleanup`; status-output expected row data includes `graphics/backend/render_backend/offscreen_target.rs`, `docs/zircon_runtime/graphics/render-product-submit.md`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output all-subplans guard 1/1 通过；core-min Cargo check 在独立 target 目录 10 分钟超时无编译结果，残留本切片 cargo/rustc 进程已停止，不计通过。该记录只关闭 OffscreenTarget 固定帧 texture owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 render backend state owner cleanup | runtime_15_render_backend_state_owner_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_render_backend_state_owner_cleanup_coremin_check_passed`；`graphics/backend/render_backend/render_backend.rs` 不再用 `#[allow(dead_code)]` 掩盖 WGPU instance/adapter/backend config owner，而是通过 `RenderBackend::RETAINED_STATE_OWNER_COUNT` / `retained_state_owner_count()` 明确读取 3 个 backend state owner；`RenderBackend::caps()` 在生产路径 debug-assert 该 owner 契约，确保 backend capability projection 仍保留支撑 WGPU device/queue/caps 的状态 owner。Guard: `runtime_15_render_backend_state_owner_cleanup`; status-output expected row data includes `graphics/backend/render_backend/render_backend.rs`, `docs/zircon_runtime/graphics/render-product-submit.md`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-render-backend-state-owner-0622` 通过（既有 warnings）。该记录只关闭 RenderBackend state owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu texture resource owner cleanup | runtime_15_gpu_texture_resource_owner_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_gpu_texture_resource_owner_cleanup_coremin_check_passed`；`graphics/scene/resources/gpu_texture/gpu_texture_resource.rs` 不再用 `#[allow(dead_code)]` 掩盖 material texture identity 与 WGPU texture/view/sampler owner，而是通过 `GpuTextureResource::RETAINED_TEXTURE_BINDING_OWNER_COUNT` / `retained_texture_binding_owner_count()` 明确读取 4 个 binding owner；`GpuTextureResource::view()` 和 `sampler()` 在材质绑定路径 debug-assert 该 owner 契约，确保 material texture binding 背后的资源生命周期由 GpuTextureResource 保留。Guard: `runtime_15_gpu_texture_resource_owner_cleanup`; status-output expected row data includes `graphics/scene/resources/gpu_texture/gpu_texture_resource.rs`, `docs/zircon_runtime/graphics/render-product-submit.md`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-texture-owner-0622` 通过（既有 warnings）。该记录只关闭 GpuTextureResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu material uniform owner cleanup | runtime_15_gpu_material_uniform_owner_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_gpu_material_uniform_owner_cleanup_coremin_check_passed`；`graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs` 不再用 `#[allow(dead_code)]` 掩盖 material uniform buffer 和 payload/buffer byte-length diagnostics owner，而是通过 `GpuMaterialUniformResource::RETAINED_MATERIAL_UNIFORM_OWNER_COUNT` / `retained_material_uniform_owner_count()` 明确读取 3 个 binding/diagnostics owner；`binding_resource()` 在材质 uniform 绑定路径 debug-assert 该 owner 契约，`resource_streamer_accessors.rs` 通过 owner accessor 读取 byte-length diagnostics。Guard: `runtime_15_gpu_material_uniform_owner_cleanup`; status-output expected row data includes `graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs`, `docs/zircon_runtime/graphics/render-product-submit.md`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-material-uniform-owner-0622` 通过（既有 144 warnings）。该记录只关闭 GpuMaterialUniformResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu mesh order signature cleanup | runtime_15_gpu_mesh_order_signature_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_gpu_mesh_order_signature_cleanup_coremin_check_passed`；`graphics/scene/resources/gpu_mesh/gpu_mesh_resource.rs` 不再用 `#[allow(dead_code)]` 掩盖 `indirect_order_signature`，字段收窄为 `gpu_mesh` owner 内部字段并由 `GpuMeshResource::indirect_order_signature()` 暴露；`graphics/scene/resources/gpu_mesh/gpu_mesh_resource_from_asset.rs` 继续从完整 vertex/index payload 派生 order signature；`graphics/scene/scene_renderer/mesh/build_mesh_draws/build/extend_pending_draws_for_mesh_instance.rs` 的 prepared mesh draw tie-breaker 通过 `mesh_order_command_sort_tie_breaker(...)` 混入该签名。Guard: `runtime_15_gpu_mesh_order_signature_cleanup`; status-output expected row data includes `graphics/scene/resources/gpu_mesh/gpu_mesh_resource.rs`, `graphics/scene/scene_renderer/mesh/build_mesh_draws/build/extend_pending_draws_for_mesh_instance.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-mesh-order-0622` 通过（既有 warnings）。该记录只关闭 GpuMeshResource order-signature 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu model identity cleanup | runtime_15_gpu_model_identity_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_gpu_model_identity_cleanup_coremin_check_passed`；`graphics/scene/resources/gpu_model/gpu_model_resource.rs` 不再用 `#[allow(dead_code)]` 掩盖 GPU model resource identity，字段收窄为 `gpu_model` owner 内部字段并由 `GpuModelResource::id()` 暴露；`graphics/scene/resources/gpu_model/gpu_model_resource_from_asset.rs` 继续在 GPU model 构造时记录资源身份；`ResourceStreamer::model(...)` 在返回缓存资源前 debug-assert `prepared.resource.id()` 与 streamer key 一致。Guard: `runtime_15_gpu_model_identity_cleanup`; status-output expected row data includes `graphics/scene/resources/gpu_model/gpu_model_resource.rs`, `graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-model-identity-0622` 通过（既有 warnings）。该记录只关闭 GpuModelResource identity 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 post-process LUT texture owner cleanup | runtime_15_post_process_lut_texture_owner_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_post_process_lut_texture_owner_cleanup_coremin_check_passed`；`graphics/scene/resources/post_process_lut_texture/post_process_lut_texture_resource.rs` 不再用 `#[allow(dead_code)]` 掩盖 post-process LUT texture owner，新增 `PostProcessLutTextureResource::RETAINED_LUT_TEXTURE_OWNER_COUNT` 与 `retained_lut_texture_owner_count()` 显式读取 texture/view owner；`PostProcessLutTextureResource::view()` 在 3D LUT 绑定 accessor 路径 debug-assert 该 owner 契约；`ResourceStreamer::prepared_post_process_lut_3d_view(...)` 通过 accessor 暴露 binding view 并保留 `Texture3d` descriptor 匹配。Guard: `runtime_15_post_process_lut_texture_owner_cleanup`; status-output expected row data includes `graphics/scene/resources/post_process_lut_texture/post_process_lut_texture_resource.rs`, `graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-post-process-lut-owner-0622` 通过（既有 warnings）。该记录只关闭 PostProcessLutTextureResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 output target texture owner cleanup | runtime_15_output_target_texture_owner_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_output_target_texture_owner_cleanup_coremin_check_passed`；`graphics/scene/resources/output_target_texture/output_target_texture_resource.rs` 不再用 `#[allow(dead_code)]` 掩盖 output target descriptor、texture、view、sampler owner，新增 `OutputTargetTextureResource::RETAINED_OUTPUT_TARGET_TEXTURE_OWNER_COUNT` 与 `retained_output_target_texture_owner_count()` 显式读取输出目标 descriptor 和 WGPU owner；`OutputTargetTextureResource` 的 descriptor/size/texture/view/sampler accessor 在 writeback、graph import 和材质采样路径 debug-assert 该 owner 契约。`PreparedOutputTargetTexture` 不再抑制 `resource` 字段，新增 cache owner 计数和 `resource()` accessor，`ResourceStreamer` 通过 accessor clone/borrow prepared output target resource。Guard: `runtime_15_output_target_texture_owner_cleanup`; status-output expected row data includes `graphics/scene/resources/output_target_texture/output_target_texture_resource.rs`, `graphics/scene/resources/prepared/prepared_output_target_texture.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-output-target-owner-0622` 通过（既有 warnings）。该记录只关闭 OutputTargetTextureResource / PreparedOutputTargetTexture owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 material runtime capture seed cleanup | runtime_15_material_runtime_capture_seed_cleanup_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_material_runtime_capture_seed_cleanup_coremin_check_passed`；`graphics/scene/resources/runtime/material_runtime.rs` 不再用 `#[allow(dead_code)]` 掩盖 `MaterialCaptureSeed`、`MaterialRuntime` 和 `capture_seed()`，其中 `MaterialRuntime` 保持生产材质运行态 DTO，`MaterialCaptureSeed` / `capture_seed()` 只在 `#[cfg(test)]` 下供 render product streamer 回归读取。`graphics/scene/resources/runtime/mod.rs` 与 `graphics/scene/resources/mod.rs` 的 capture seed re-export 收进 test cfg；`resource_streamer_accessors.rs` 的 `material_capture_seed(...)`、`sample_texture_rgba(...)` 与采样 helper 同样 test-only。Guard: `runtime_15_material_runtime_capture_seed_cleanup`; status-output expected row data includes `graphics/scene/resources/runtime/material_runtime.rs`, `graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-material-capture-0622` 通过（既有 warnings）。该记录只关闭 MaterialRuntime capture seed/test texture sampling 子切片；resource_streamer 其他 diagnostics accessor suppression 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 resource streamer diagnostics accessor cleanup | runtime_15_resource_streamer_diagnostics_accessor_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_resource_streamer_diagnostics_accessor_cleanup_static_passed_cargo_lock_blocked`；`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs` 已删除剩余 diagnostics accessor 上的 `#[allow(dead_code)]`。只由 render product / asset flow 测试消费的资产管理、材质管理、uniform/property/texture-slot 查询 helper 收进 `#[cfg(test)]`；生产 readiness bridge 保留 `material_readiness_report(...)` / `material_readiness_summary(...)`，并由 `resource_streamer_ensure_scene_resources.rs` 继续读取材质 readiness summary。Guard: `runtime_15_resource_streamer_diagnostics_accessor_cleanup`; status-output expected row data includes `resource_streamer_accessors.rs`, `resource_streamer_ensure_scene_resources.rs`, and the guard anchor. 验证：scoped rustfmt --check 通过；静态扫描确认 `resource_streamer_accessors.rs` 无 `#[allow(dead_code)]`；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞（Cargo 需要补齐 `zircon_plugin_sdk` 相关锁文件条目），不计通过。该记录只关闭 ResourceStreamer diagnostics accessor 子切片；全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 resource streamer resolve texture id cleanup | runtime_15_resource_streamer_resolve_texture_id_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_resource_streamer_resolve_texture_id_cleanup_static_passed_cargo_lock_blocked`；`graphics/scene/resources/resource_streamer/resource_streamer_resolve_texture_id.rs` 删除未使用的 `ResourceStreamer::resolve_texture_id(...)` helper 和对应 `#[allow(dead_code)]`。生产材质贴图解析入口保留 `resolve_texture_reference(...)` / `resolve_texture_reference_with_support(...)` 与 `ResolvedTextureReference::id()`，继续保留 unresolved/not-upload-ready validation 和 texture-slot fallback。Guard: `runtime_15_resource_streamer_resolve_texture_id_cleanup`; status-output expected row data includes `resource_streamer_resolve_texture_id.rs`, `resolve_texture_reference_with_support`, and the guard anchor. 验证：scoped rustfmt --check 通过；静态扫描确认该文件无 `#[allow(dead_code)]` 且 `resolve_texture_id(` 只剩状态守卫字符串；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞（Cargo 需要补齐 `zircon_plugin_sdk` 相关锁文件条目），不计通过。该记录只关闭 ResourceStreamer resolve_texture_id 僵尸 helper 子切片；全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 particle GPU readback output accessor cleanup | runtime_15_particle_gpu_readback_output_accessor_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_particle_gpu_readback_output_accessor_cleanup_static_passed_cargo_lock_blocked`；`graphics/scene/scene_renderer/core/scene_renderer_runtime_outputs/take_last_particle_gpu_readback_outputs.rs` 删除 stale `#[allow(dead_code)]`。`SceneRenderer::take_last_particle_gpu_readback_outputs(...)` 已由 `graphics/runtime/render_framework/submit_frame_extract/submit/collect_runtime_feedback.rs::collect_particle_feedback(...)` 消费，和 prepared sideband particle readback outputs 合并后投递到 `ParticleGpuFeedback::new(...)`。Guard: `runtime_15_particle_gpu_readback_output_accessor_cleanup`; status-output expected row data includes `take_last_particle_gpu_readback_outputs.rs`, `renderer.take_last_particle_gpu_readback_outputs()`, and the guard anchor. 验证：scoped rustfmt --check 通过；静态扫描确认该文件无 `#[allow(dead_code)]` 且 feedback collector 仍调用该 accessor；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 particle GPU readback accessor 子切片；全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 advanced plugin output test accessor cleanup | runtime_15_advanced_plugin_output_test_accessor_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_advanced_plugin_output_test_accessor_cleanup_static_passed_cargo_lock_blocked`；`graphics/scene/scene_renderer/core/scene_renderer/advanced_plugin_outputs/output_access.rs` 删除 `has_virtual_geometry_gpu_readback(...)`、`plugin_renderer_outputs(...)` 与 `has_particle_gpu_readback(...)` 的 stale `#[allow(dead_code)]`。这些 helpers 只由 mailbox/readback inline tests 观察，因此改为 `#[cfg(test)]`；生产路径继续通过 `take_hybrid_gi_readback_outputs(...)`、`take_particle_gpu_readback_outputs(...)` 与 `take_virtual_geometry_readback_outputs(...)` drain renderer plugin outputs。Guard: `runtime_15_advanced_plugin_output_test_accessor_cleanup`; status-output expected row data includes `advanced_plugin_outputs/output_access.rs`, `has_particle_gpu_readback`, and the guard anchor. 验证：scoped rustfmt --check 通过；静态扫描确认 `output_access.rs` 无 `#[allow(dead_code)]`、三个 observation helper 均收进 test cfg，且生产 take/drain 方法仍存在；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 advanced plugin output test accessor 子切片；全量 F12 sweep 仍 pending。 |
| M3 | Runtime 15 M3 graphics dead-code guard module split | runtime_15_graphics_dead_code_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_graphics_dead_code_guard_module_split_static_passed_cargo_lock_blocked`；旧 graphics dead-code 单文件 guard 已硬切为 folder-backed `structure_convention/graphics_dead_code/mod.rs`，layout 守卫移入 `structure_convention/graphics_dead_code/module_layout.rs`，renderer output accessor 守卫移入 `structure_convention/graphics_dead_code/renderer_output_accessors.rs`。Guard: `runtime_15_graphics_dead_code_guard_is_folder_backed`; status-output expected row data includes `graphics_dead_code/module_layout.rs`, `graphics_dead_code/renderer_output_accessors.rs`, and the guard anchor. 验证：scoped rustfmt/static checks 通过；父模块行数从 903 下降到近大文件阈值以下；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 graphics dead-code guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files` 与全量 F12 sweep 仍 pending。 |
| M3 | Runtime 15 M3 provider boilerplate guard module split | runtime_15_provider_boilerplate_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_provider_boilerplate_guard_module_split_static_passed_cargo_lock_blocked`；provider registration/update/feedback 三个结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/provider_boilerplate.rs`，与 provider prepare-input 和 full boilerplate audit 守卫同 owner。Guard: `runtime_15_provider_boilerplate_guard_is_folder_backed`; status-output expected row data includes `structure_convention/provider_boilerplate.rs`, the layout guard, and `runtime_15_provider_registration_uses_shared_owner`. 验证：scoped rustfmt/static checks 通过；`structure_convention.rs` 现在 553 行、`provider_boilerplate.rs` 约 618 行；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 provider boilerplate guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F14 diagnostics normalization | runtime_15_diagnostics_frame_trait_wrapper_removed_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_diagnostics_frame_trait_wrapper_removed_coremin_check_passed`；`core/runtime/diagnostics/frame_diagnostics.rs` now owns `FrameDiagnostics` / `FrameDiagnosticsStatus`, and render、physics、animation diagnostics plus `EcsFramePerformanceDiagnostics` implement that shared frame status contract. `RuntimeDiagnosticsSnapshot::frame_diagnostics_statuses()` composes render/physics/animation subdomains without changing existing `DiagnosticStore` metric paths. `World` now stores `EcsFramePerformanceDiagnostics` directly and `scene/world/performance_diagnostics.rs` no longer defines `WorldEcsFramePerformanceDiagnostics` or `.0` forwarding. Guards/tests: `runtime_snapshot_frame_diagnostics_statuses_preserve_subdomains`, `ecs_frame_performance_diagnostics_uses_scene_ecs_frame_domain`, and `runtime_15_diagnostics_use_frame_trait_without_world_wrapper`; status-output expected row data includes `FrameDiagnosticsStatus`, `scene.ecs`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-f14-cargo-0622` 通过（既有 warnings）。focused behavior Cargo test `runtime_snapshot_frame_diagnostics_statuses_preserve_subdomains` 1200s 超时无测试结果且残留已清理，不计通过。 |
| M5 | Runtime 15 F13 provider registration shared owner | runtime_15_provider_registration_shared_owner_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_provider_registration_shared_owner_coremin_check_passed`；`graphics/runtime_provider/registration.rs` now owns `RuntimeProviderRegistration<P: ?Sized>` and `define_runtime_provider_registration!`, while `hybrid_gi_runtime_provider/provider_registration.rs`, `virtual_geometry_runtime_provider/provider_registration.rs`, and `solari_runtime_provider/provider_registration.rs` generate their public wrappers from that shared owner. Public provider-specific registration names and methods stay stable; duplicated provider ID / priority / provider object / debug boilerplate is removed from the feature-specific files. Guard: `runtime_15_provider_registration_uses_shared_owner`; status-output expected row data includes `graphics/runtime_provider/registration.rs`, `RuntimeProviderRegistration<P: ?Sized>`, and the guard anchor. 验证：standalone guard red->green；scoped rustfmt、structure guard、status-output 与 core-min `cargo check` 通过（既有 warnings）。本记录只关闭 F13 registration 样板；update/feedback 子切片已由后续 shared-owner 记录收束，provider prepare-input duplication 与 full `module_convention_gate` 仍 pending。 |
| M5 | Runtime 15 F13 provider update shared stats owner | runtime_15_provider_update_shared_stats_owner_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_provider_update_shared_stats_owner_coremin_check_passed`；`graphics/runtime_provider/update.rs` now owns `RuntimeProviderUpdate<S>` and `define_runtime_provider_update!`, while `hybrid_gi_runtime_provider/runtime_update.rs` and `virtual_geometry_runtime_provider/runtime_update.rs` generate their public wrappers from that shared owner. Public update type names and `stats()` return shapes stay stable; duplicated stats storage, constructor, and getter boilerplate are removed from the feature-specific files. Guard: `runtime_15_provider_update_uses_shared_stats_owner`; status-output expected row data includes `graphics/runtime_provider/update.rs`, `RuntimeProviderUpdate<S>`, and the guard anchor. 验证：缺失 `graphics/runtime_provider/update.rs` 的红态静态证据已记录；scoped rustfmt 与 core-min `cargo check` 通过（既有 warnings）；standalone structure guard/status-output test binary 启动被 Windows `ResourceUnavailable` / 用户取消阻断，focused Cargo test 904s 超时无结果，不计为通过。本记录只关闭 F13 update stats 样板；feedback 子切片已由后续 shared-owner 记录收束，provider prepare-input duplication 与 full `module_convention_gate` 仍 pending。 |
| M5 | Runtime 15 F13 provider feedback shared payload owner | runtime_15_provider_feedback_shared_payload_owner_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_provider_feedback_shared_payload_owner_coremin_check_passed`；`graphics/runtime_provider/feedback.rs` now owns `RuntimeProviderFeedback<G, V>` for the common provider feedback `gpu_completion` and `visibility_feedback` payload pair. `hybrid_gi_runtime_provider/runtime_feedback.rs` and `virtual_geometry_runtime_provider/runtime_feedback.rs` keep their public type names, constructors, and getter surface while delegating common payload storage to that shared owner; HGI evictable probe ids and Virtual Geometry page-request/generation fields remain provider-specific. Guard: `runtime_15_provider_feedback_uses_shared_payload_owner`; status-output expected row data includes `graphics/runtime_provider/feedback.rs`, `RuntimeProviderFeedback<G, V>`, and the guard anchor. 验证：缺失 `graphics/runtime_provider/feedback.rs` 的红态静态证据已记录；scoped rustfmt、standalone structure guard 1/1、standalone status-output 2/2 与 core-min `cargo check` 通过（既有 warnings）。本记录只关闭 F13 feedback 共同 payload 样板；provider prepare-input duplication 与 full `module_convention_gate` 仍 pending。 |
| M5 | Runtime 15 F13 provider prepare input shared frame owner | runtime_15_provider_prepare_input_shared_frame_owner_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_provider_prepare_input_shared_frame_owner_coremin_check_passed`；`graphics/runtime_provider/prepare_input.rs` now owns `RuntimeProviderPrepareInput<'a, E>` for the common provider prepare `extract` and `generation` frame context. `hybrid_gi_runtime_provider/prepare_input.rs` and `virtual_geometry_runtime_provider/prepare_input.rs` keep their public type names, constructors, and getter surface while delegating common frame context storage to that shared owner; HGI mesh/light/update-plan inputs and Virtual Geometry page-upload/cluster/draw-segment inputs remain provider-specific. Guard: `runtime_15_provider_prepare_input_uses_shared_extract_generation_owner`; status-output expected row data includes `graphics/runtime_provider/prepare_input.rs`, `RuntimeProviderPrepareInput<'a, E>`, and the guard anchor. 验证：scoped rustfmt、standalone structure guard 1/1、standalone status-output all-subplans guard 1/1 与 core-min `cargo check` 通过（既有 warnings）；broader `status_output` 批次仍有非本切片 Runtime 06 F8 row-drift 失败。full `runtime_15_no_duplicated_provider_boilerplate` / `module_convention_gate` 仍 pending。 |
| M5 | Runtime 15 F13 full provider boilerplate audit | runtime_15_provider_boilerplate_full_audit_coremin_check_passed | 2026-06-22 | 状态锚 `runtime_15_provider_boilerplate_full_audit_coremin_check_passed`；`structure_convention/provider_boilerplate.rs::runtime_15_no_duplicated_provider_boilerplate` now verifies the full provider boilerplate closure across registration/update/feedback/prepare-input. The guard requires all shared owners under `graphics/runtime_provider/*`, rejects duplicated provider id / priority / trait-object / debug boilerplate in HGI/VG/Solari registration files, rejects manual update constructors/getters outside `define_runtime_provider_update!`, rejects common GPU completion / visibility feedback fields outside `RuntimeProviderFeedback<G, V>`, rejects common extract / generation fields outside `RuntimeProviderPrepareInput<'a, E>`, and records particle feedback as a feature-specific single-payload exception. Status-output expected row data includes `structure_convention/provider_boilerplate.rs`, `RuntimeProviderRegistration<P: ?Sized>`, and the guard anchor. 验证：scoped rustfmt --check 通过；standalone full provider boilerplate guard 1/1 通过；standalone status-output all-subplans guard 1/1 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-provider-boilerplate-full-coremin-0622` 通过（既有 141 warnings）。完整 `module_convention_gate`、全量 dead-code sweep 与测试组织拆分仍 pending。 |

| M3 | Runtime 15 M3 facade surface guard module split | runtime_15_facade_surface_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_facade_surface_guard_module_split_static_passed_cargo_lock_blocked`；prelude coverage 与 graphics façade visibility 两个结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/facade_surface.rs`，和 crate/subsystem prelude coverage、graphics façade 可见性说明同 owner。Guard: `runtime_15_facade_surface_guard_is_folder_backed`; status-output expected row data includes `structure_convention/facade_surface.rs`, the layout guard, and `runtime_15_prelude_covers_required_types`. 验证：scoped rustfmt/static checks 通过；`structure_convention.rs` 现在低于 500 行；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 façade/prelude guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 runtime dead-code guard module split | runtime_15_runtime_dead_code_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_runtime_dead_code_guard_module_split_static_passed_cargo_lock_blocked`；runtime UI dead-code support、runtime-owned dead-code cleanup 与 script host value descriptor 三个非 graphics dead-code 结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/runtime_dead_code.rs`，和 F10/F12 runtime dead-code surface 同 owner。Guard: `runtime_15_runtime_dead_code_guard_is_folder_backed`; status-output expected row data includes `structure_convention/runtime_dead_code.rs`, the layout guard, and `runtime_15_runtime_ui_dead_code_surface_is_test_support`. 验证：scoped rustfmt/static checks 通过；`structure_convention.rs` 现在低于 180 行；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 runtime dead-code guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 diagnostics guard module split | runtime_15_diagnostics_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | 状态锚 `runtime_15_diagnostics_guard_module_split_static_passed_cargo_lock_blocked`；diagnostics frame trait / ECS wrapper cleanup 结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/diagnostics_surface.rs`，和 F14 diagnostics normalization 同 owner。Guard: `runtime_15_diagnostics_guard_is_folder_backed`; status-output expected row data includes `structure_convention/diagnostics_surface.rs`, the layout guard, and `runtime_15_diagnostics_use_frame_trait_without_world_wrapper`. 验证：scoped rustfmt/static checks 通过；`structure_convention.rs` 现在低于 80 行；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 diagnostics guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| 横切 | Runtime 05 status-output status/date helper split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_slices.rs` now owns `expected_status_for_slice` and `expected_date_for_slice`; `status_output_tables.rs` keeps the status-output coverage tests and expected slice anchors while reducing inline status/date branching from the status-output guard. `runtime_plan_status_boundary` now tracks plan-status support files 14/14 including the helper. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 08 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_08.rs` now owns `runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`; `cargo_gates/early.rs` is reduced to 795 lines and keeps the Runtime 01/02/03/04/06/07 early-gate owners plus the Runtime 08 include. `runtime_plan_status_boundary` now tracks plan-status support files 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 07 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_07.rs` now owns `runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation`; `cargo_gates/early.rs` is reduced to 660 lines and keeps the Runtime 01/02/03/04/06 early-gate owners plus Runtime 07/08 includes. `runtime_plan_status_boundary` now tracks plan-status support files 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 07 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 03 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_03.rs` now owns `runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation`; `cargo_gates/early.rs` is reduced to 246 lines and keeps the Runtime 04/06 early-gate owners plus Runtime 01/02/03/07/08 includes. `runtime_plan_status_boundary` now tracks plan-status support files 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 03 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 01 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_01.rs` now owns `runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation`; `cargo_gates/early.rs` is reduced to 246 lines and keeps the Runtime 04/06 early-gate owners plus Runtime 01/02/03/07/08 includes. `runtime_plan_status_boundary` now tracks plan-status support files 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 01 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 02 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_02.rs` now owns `runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation`; `cargo_gates/early.rs` is reduced to 246 lines and keeps the Runtime 04/06 early-gate owners plus Runtime 01/02/03/07/08 includes. `runtime_plan_status_boundary` now tracks plan-status support files 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 02 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 04 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_04.rs` now owns `runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation`; `cargo_gates/early.rs` now includes Runtime 01/02/03/04/06/07/08 children with no inline early-gate owner bodies. `runtime_plan_status_boundary` now tracks plan-status support files 20/20 and `plan_status_boundary_line_count = 861`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 04 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates early Runtime 06 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_06.rs` now owns `runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation`; `cargo_gates/early.rs` now includes Runtime 01/02/03/04/06/07/08 children with no inline early-gate owner bodies. `runtime_plan_status_boundary` now tracks plan-status support files 20/20 and `plan_status_boundary_line_count = 861`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 06 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates late Runtime 10 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_10.rs` now owns `runtime_10_m1_3_cargo_pending_gate_stays_explicit_until_dynamic_api_validation` and `runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff`; `cargo_gates/late.rs` is reduced to 388 lines and keeps the Runtime 11/12/13/14 late-gate owners plus the Runtime 10 include. `runtime_plan_status_boundary` now tracks plan-status support files 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 10 cargo-gates 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates late Runtime 11 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_11.rs` now owns `runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass`; `cargo_gates/late.rs` is reduced to 232 lines and keeps the Runtime 12/13/14 late-gate owners plus the Runtime 10/11 includes. `runtime_plan_status_boundary` now tracks plan-status support files 15/15 and `plan_status_boundary_line_count = 856`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 11 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates late Runtime 12 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_12.rs` now owns `runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation`; `cargo_gates/late.rs` is reduced to 148 lines and keeps the Runtime 13/14 late-gate owners plus the Runtime 10/11/12 includes. `runtime_plan_status_boundary` now tracks plan-status support files 16/16 and `plan_status_boundary_line_count = 857`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 12 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates late Runtime 13 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_13.rs` now owns `runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass`; `cargo_gates/late.rs` now includes Runtime 10/11/12/13/14 children with no inline late-gate owner bodies. `runtime_plan_status_boundary` now tracks final plan-status support files 18/18 and `plan_status_boundary_line_count = 859`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 13 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| 横切 | Runtime 05 cargo-gates late Runtime 14 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_14.rs` now owns `runtime_14_module_family_cargo_gate_stays_visible_until_filters_pass`; `cargo_gates/late.rs` now includes Runtime 10/11/12/13/14 children with no inline late-gate owner bodies. `runtime_plan_status_boundary` now tracks plan-status support files 18/18 and `plan_status_boundary_line_count = 859`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 14 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |

### 3.1 已知但暂不立项的缺口（防遗忘清单，立项前先补证据）

| 缺口 | 现状依据 | 归属/触发条件 |
|---|---|---|
| 网络复制 runtime 侧 | `docs/engine-architecture/runtime-network-extension.md` 既有；net 栈在 zircon_plugins | net 插件 owner；runtime 侧出现复制语义需求时立项 |
| 音频 runtime 服务面 | sound 插件自研 DSP/HRTF + cpal（01 §1.1 已矫正） | sound 插件 owner；runtime 侧仅保持服务注册中性 |
| FFI panic 安全（extern "C" 边界 catch_unwind 审计） | dynamic_api/exports 与函数表分发面 | 归 10：M1.3 首批导出表 panic containment 已静态落地，Cargo 动态 API 回归待活动编译通道空闲；后续新增 ABI entry 必须同步 `_ffi` wrapper 与守卫 |
| 输入录制/回放 | `InputRecording` / `InputReplayCursor` runtime helper 已由 12-M4 静态落地；无 editor 录制 UI / asset-backed recording 格式 | 后续产品化录制面、资产格式或跨进程输入流需求出现时再立项；核心 runtime 确定性回放 helper 已可用 |
| 脚本调试器/断点面 | 无 | 13 backlog；ZrVM 上游调试协议确定后立项 |
| 存档/会话持久化语义 | 运行时 `DynamicScene` 会话归档核心 API 已落地（05），支持 plugin component type descriptor embedding/spawn install、reflected resource ensure hook、asset-backed dynamic scene preparation、typed scene asset reload queue、typed scene asset reload latest-revision gate、typed scene asset reload superseded-pending pruning、typed scene asset reload tick report、typed scene asset reload caller-thread apply report、typed scene asset reload frame apply report、typed scene asset reload LevelSystem target helpers、typed scene asset reload pending snapshot、dynamic runtime session scene asset reload integration、dynamic runtime session scene asset reload diagnostics、async dynamic scene spawn preparation、slot 化 world/level capture、folder-backed session owner split（mod/facade/{mod,construction/{mod,archive,capture/{mod,world,level},serialization},capture/{mod,world/{mod,ordinary/{mod,commit,preview},retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}},level/{mod,ordinary/{mod,commit,preview},retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}}},store/{mod,push,upsert,remove},transfer/{mod,copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}},export/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}},import/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}},mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove},retention/{mod,merge/{mod,commit,preview},global/{mod,commit,preview},tag/{mod,commit,preview}},query/{mod,basic,manifest,selection/{mod,global,tag,typed},statistics},restore/{mod,apply/{mod,named,selected},restore/{mod,named,selected},diff/{mod,named,selected}},validation/{mod,support,invariants}}/archive/manifest/{mod,archive,order,summary}/error/io/{mod,load_save/{mod,load,preview,save},atomic,mutation,support}/path_api/{mod,load_save/{mod,load,preview,save,atomic},query/{mod,manifest,selection,statistics,status},export/{mod,loaded/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}},source_path/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}}},restore/{mod,restore/{mod,named,selected},apply/{mod,named,selected},diff/{mod,named,selected}},transfer/{mod,copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}},import/{mod,loaded/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}},source_path/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}}},merge/{mod,loaded/{mod,commit,preview},source_path/{mod,commit,preview}}}/path_capture/{mod,world/{mod,ordinary/{mod,commit,preview},selected/{mod,commit,preview},retention/{mod,global/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}},tag/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}}}},level/{mod,ordinary/{mod,commit,preview},selected/{mod,commit,preview},retention/{mod,global/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}},tag/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}}}}}/path_export/{mod,loaded/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}},source_path/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}}}/path_merge/{mod,loaded/{mod,commit,preview},source_path/{mod,commit,preview}}/path_mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove/{mod,commit,preview}}/path_query/{mod,manifest,selection,statistics,status}/path_retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}/path_restore/{mod,apply/{mod,named,selected},restore/{mod,named,selected},diff/{mod,named,selected}}/path_transfer/{mod,copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}},import/{mod,loaded/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}},source_path/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}}}/restore/{mod,apply/{mod,named,selected},restore/{mod,named,selected},diff/{mod,named,selected}}/archive_save/capture_retention/{mod,world/{mod,commit,preview},level/{mod,commit,preview},apply}/construction/{mod,archive,capture,serialization}/retention/{mod,policy,report,prune/{mod,global/{mod,commit,preview},tag/{mod,commit,preview},planning}}/metadata/{mod,model,builders,level,tags}/selected_capture/{mod,world/{mod,ordinary/{mod,commit,preview},retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}},level/{mod,ordinary/{mod,commit,preview},retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}}}/selected_mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove/{mod,commit,preview}}/selected_retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview},policy}/slot/{mod,capture,restore,diff,summary}/reports/{mod,archive,capture,diff,mutation,restore,transfer}/merge/{mod,policy,report,algorithm/{mod,commit,preview}}/path_status/query/{mod,basic,manifest,selection/{mod,global,tag,typed},statistics}/slot_id/slot_store/{mod,push,upsert}/slot_capture/{mod,world/{mod,commit,preview},level/{mod,commit,preview},preview}/slot_copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}/slot_export/{mod,commit,preview}/slot_import/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}/slot_mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove/{mod,commit,preview},report}/slot_selector/{mod,selector,report,resolve}/statistics/target_path/{mod,source,target}/validation/{mod,support,lookup,ordering,metadata}）、manifest 清单与 manifest-from-path 读取、archive path slot id/existence/summary query、archive path manifest tag/display-name filtering、archive path status inspection、statistics-from-path、load-or-empty archive path、atomic capture world/level slot to path、atomic selected capture world/level slot to path、metadata-preserving selected capture world/level slot、path-level metadata-preserving selected capture world/level slot、atomic capture world/level slot to path with retention、atomic selected capture world/level slot to path with retention、atomic metadata-preserving selected capture world/level slot to path with retention、capture dry-run preview、selected capture dry-run preview、metadata-preserving selected capture dry-run preview、capture-retention dry-run preview、selected capture-retention dry-run preview、metadata-preserving selected capture-retention dry-run preview、restore/apply slot directly from archive path、diff slot directly from archive path、path-level restore/apply/diff owner split、path-level restore/apply/diff subowner split、path-level restore/apply/diff named-selected variant subowner split、path-level single-slot export owner split、path-level single-slot export subowner split、path-level single-slot export source-path subowner split、path-level single-slot export loaded subowner split、path-level merge owner split、path-level merge subowner split、path-level merge commit-preview variant subowner split、path-level manifest/query/status owner split、path-level manifest/query/status subowner split、path-level path API facade owner split、path-level path API load-save facade variant subowner split、path-level path API query facade subowner split、path-level path API transfer facade owner split、path-level path API transfer copy facade subowner split、path-level path API transfer copy facade variant subowner split、path-level path API transfer copy facade commit-preview variant subowner split、path-level path API transfer import facade owner split、path-level path API transfer import facade subowner split、path-level path API transfer import facade variant subowner split、path-level path API transfer import facade commit-preview variant subowner split、path-level path API restore facade subowner split、path-level path API restore facade named-selected variant subowner split、path-level path API export facade subowner split、path-level path API export source-path facade subowner split、path-level path API export loaded facade subowner split、path-level path API export facade commit-preview variant subowner split、path-level path API merge facade subowner split、path-level path API merge facade commit-preview variant subowner split、path-level capture owner split、path-level capture world/level owner split、path-level capture ordinary/selected commit-preview variant subowner split、path-level capture retention owner split、path-level capture global retention subowner split、path-level capture tag retention subowner split、path-level world capture retention commit-preview variant subowner split、path-level level capture retention commit-preview variant subowner split、archive-level restore/apply/diff owner split、archive-level restore/apply/diff subowner split、archive-level restore/apply/diff named-selected variant subowner split、archive-level manifest/query/statistics owner split、archive-level manifest/query/statistics subowner split、archive-level query selection variant subowner split、archive-level manifest subowner split、archive-level reports subowner split、archive-level typed slot selector subowner split、archive-level slot mutation owner split、archive-level slot mutation subowner split、archive-level slot mutation commit-preview variant subowner split、archive-level copy/export/import owner split、archive-level slot copy subowner split、archive-level slot copy variant subowner split、archive-level slot copy commit-preview variant subowner split、archive-level slot export subowner split、archive-level slot import subowner split、archive-level slot import variant subowner split、archive-level slot import commit-preview variant subowner split、archive-level capture owner split、archive-level slot capture subowner split、archive-level slot capture commit-preview variant subowner split、archive-level capture-retention subowner split、archive-level capture-retention commit-preview variant subowner split、archive-level slot subowner split、selected capture owner split、selected capture world/level owner split、selected capture ordinary commit-preview variant subowner split、selected capture retention owner split、selected capture retention commit-preview variant subowner split、archive-level merge owner split、archive-level merge subowner split、archive-level merge algorithm commit-preview variant subowner split、archive-level construction/serialization owner split、archive-level construction/serialization subowner split、archive-level facade construction subowner split、archive-level facade construction capture variant subowner split、retention owner split、retention prune owner split、retention prune commit-preview variant subowner split、archive-level slot store owner split、archive-level slot store subowner split、archive-level validation owner split、archive-level validation subowner split、archive-level metadata subowner split、archive-level facade subowner split、archive-level facade store subowner split、archive-level facade validation subowner split、archive-level facade capture subowner split、archive-level facade capture ordinary commit-preview variant subowner split、archive-level facade capture retention variant subowner split、archive-level facade capture retention commit-preview variant subowner split、archive-level facade restore subowner split、archive-level facade restore variant subowner split、archive-level facade query subowner split、archive-level facade query selection variant subowner split、archive-level facade mutation subowner split、archive-level facade mutation commit-preview variant subowner split、archive-level facade retention subowner split、archive-level facade retention global/tag variant subowner split、archive-level facade retention merge commit-preview variant subowner split、archive-level facade transfer subowner split、archive-level facade transfer copy subowner split、archive-level facade transfer copy variant subowner split、archive-level facade transfer copy commit-preview variant subowner split、archive-level facade transfer export subowner split、archive-level facade transfer export commit-preview variant subowner split、archive-level facade transfer import subowner split、archive-level facade transfer import variant subowner split、archive-level facade transfer import commit-preview variant subowner split、target-path preflight subowner split、path-level slot mutation owner split、path-level slot mutation commit-preview variant subowner split、path-level slot rename/metadata update/touch/remove、path-level slot mutation dry-run preview、path-level typed selected slot mutation、path-level typed selected slot mutation dry-run preview、path-level retention prune/tag prune、path-level retention subowner split、path-level retention commit-preview variant subowner split、path-level typed selected retention protection、path-level typed selected retention dry-run preview、path-level retention dry-run preview、path-level latest/oldest slot id selection、path-level tagged latest/oldest slot id selection、typed exact/latest/oldest/tagged slot selector、path-level typed slot select/restore/apply/diff、typed selected slot capture、path-level typed selected slot capture、typed selected slot mutation、selected mutation subowner split、typed selected retention protection、selected retention subowner split、selected retention commit-preview variant subowner split、typed selected slot copy/save-as、path-level typed selected slot copy/save-as、path-level transfer owner split、path-level transfer copy subowner split、path-level transfer copy variant subowner split、path-level transfer copy commit-preview variant subowner split、path-level transfer import owner split、path-level transfer loaded import subowner split、path-level transfer loaded import variant subowner split、path-level transfer loaded import commit-preview variant subowner split、path-level transfer source-path import owner split、path-level transfer source-path import subowner split、path-level transfer source-path import commit-preview variant subowner split、archive file I/O subowner split、archive file I/O load-save variant subowner split、typed selected single-slot import、path-level typed selected single-slot import、typed single-slot archive export、path-level typed single-slot archive export、path-level slot copy/save-as、path-level copy/save-as dry-run preview、path-level single-slot archive export、single-slot archive export dry-run preview、full archive save target dry-run preview、save/export target parent-file preflight、source/target archive path distinct preflight、loaded-archive single-slot archive save-to-path、path-level single-slot archive import、path-to-path single-slot archive import、single-slot import dry-run preview、path-to-path archive merge、merge dry-run preview、path-level archive merge/import、tag/display-name 过滤、latest/oldest updated slot selection、tagged latest/oldest slot selection、archive statistics、single-slot archive export、single-slot archive import、archive merge/import、slot copy/save-as、copy/save-as dry-run preview、slot mutation dry-run preview、retention prune、tag bucket retention prune、retention dry-run preview、atomic session archive save with existing-target rollback、slot rename/metadata update、slot update timestamp touch、metadata tag 规范化、受控 mutation surface、只读 diff preview、World/LevelSystem apply、empty-world restore 与 LevelSystem restore；玩法存档格式仍未定 | 项目侧玩法存档需求触发；后续 gameplay schema 仍需先经 04 资产/序列化口径 |
| 本地化/i18n | 无 | UI 文本管线（01-M2/09-M3）稳定后评估 |

阶段划分:

- 阶段 A（低风险、可立即）:05 + 01 的决策记录切片。纯删除/文档/守卫，不与活动会话冲突。
- 阶段 B（结构收束）:02 → 06；10 的 M0/M1（清册 + ABI 守卫，纯文档/守卫件）可与 06 并行，M2/M3 在 06-M2 收口后执行。
- 阶段 C（子系统对齐）:03 与 04 并行；08 的 M0/M1 可与 03 并行（同文件区切片错峰），M2/M3 在 03-M2 后执行；09 在 editor UI 会话稳定 + 05 收尾闭环后启动（M0 纯文档可提前）。
- 阶段 D（性能）:07。前置:ZrVM 空指针修复、render 计划 01/02 落地;08 的"债证明"锚定测试可作为 07-M1 计数的语义基线。
- 横切（并行底座）:11 的 M0（模型选型/线程预算，纯文档）、M1（JobHandle/parallel_for）与 M3 调度诊断/压测锚已静态落地；M2.1 graphics 剔除旁路已在 2026-06-16 收编到 runtime compute `TaskPool` + `parallel_for(...)`，当前 `runtime_11_m2_1_graphics_frustum_rayon_cutover_static_passed_cargo_pending` 与 `direct_rayon_paths = 2`；M2.2 非 graphics rayon 守卫已落；M2.3 已在 03-M3 后完成同文件错峰；M2.4 已与 04-M2 同窗口完成自建 asset worker 显式预算记账；2026-06-21 targeted worker wait-assist core-min Cargo 探测 `worker_wait_assist_core_min_cargo_timeout_no_result_residual_stopped` 仍未产出测试二进制或测试结果，随后复用 core-min test binary 直跑 `worker_thread_wait_does_not_deadlock_scheduler` 得到 `worker_wait_assist_core_min_test_binary_passed_cargo_gate_pending`（`1 passed; 0 failed; 4687 filtered out`），并用同一 test binary 取得 `runtime_11_core_min_test_binary_task_guard_batch_passed_cargo_gate_pending`（tasks 18/18、worker_pool 10/10、rayon 4/4、job_system 1/1、rayon_boundary 3/3、asset_worker_policy 1/1）；同二进制的 `ecs_schedule` filter 暴露 stale source guard，已静态修复为 `runtime_11_ecs_schedule_lifetime_guard_anchor_static_passed_rebuild_pending`，并通过 `runtime_11_core_min_ecs_schedule_cargo_passed_remaining_gates_pending` 取得 core-min Cargo `ecs_schedule` 75/75 passed；随后 `runtime_11_core_min_tasks_cargo_passed_remaining_gates_pending` 取得 core-min Cargo `tasks` 19/19 passed，`runtime_11_core_min_worker_pool_cargo_passed_remaining_gates_pending` 取得 core-min Cargo `worker_pool` 10/10 passed，`runtime_11_core_min_rayon_cargo_passed_broader_gates_pending` 取得 core-min Cargo `rayon` 4/4 passed，`runtime_11_default_tasks_cargo_passed_remaining_default_gates_pending` 取得默认配置 Cargo `tasks` 19/19 passed，`runtime_11_default_worker_pool_cargo_passed_remaining_default_gates_pending` 取得默认配置 Cargo `worker_pool` 10/10 passed，`runtime_11_default_rayon_cargo_passed_full_lib_gate_pending` 取得默认配置 Cargo `rayon` 4/4 passed，`runtime_11_default_ecs_schedule_cargo_passed_full_lib_gate_pending` 取得默认配置 Cargo `ecs_schedule` 75/75 passed。core-min 与默认配置 `tasks/ecs_schedule/worker_pool/rayon` 过滤闸门已闭合；收尾全量 `cargo test -p zircon_runtime --lib --locked` 在 `runtime_11_full_lib_cargo_timeout_with_broader_failures_observed` 中 1200s 超时，无最终 `test result:`，且已观察到 58 条 broader non-11 failures。随后 support-first triage 已关闭 core runtime 低层窗口（`runtime_11_core_runtime_tests_passed_full_lib_gate_broader_failures_pending`，`core::runtime::tests::` 82/82 passed）与 asset 低层窗口（`runtime_11_asset_tests_passed_full_lib_gate_dynamic_graphics_pending`，core-min direct `asset::tests::` 363/363 passed）；asset 后 exposure-readback 窄过滤已在 `runtime_11_full_lib_after_graphics_exposure_retry_timeout_104_broader_failures` 中通过（1/1），但默认全库复验 1800s 超时并观察到 104 个跨计划 broader failures，dynamic_api/graphics/plugin/scene 等默认 full-lib gate 仍 pending，Runtime 11 不提升为 completed。
- 补全批（12/13/14）:12-M1 动作映射、gamepad axis action value、axis transition edges、consumed gamepad axis arbitration、action-map config source、action manager runtime registration、M2.1 gamepad bridge、M4 input recording/replay helper 与 cursor host requests 已静态落地，`input_stack_boundary.py` 已把 runtime/framework/test owner、UI-filtered evaluator、action context routing、axis value contract、axis transition contract、consumed-axis arbitration contract、InputConfig config-driven evaluator、`InputActionManager` / `DefaultInputActionManager` / `resolve_input_action_manager(...)` 注册路径、`InputRecording` / `InputReplayCursor` 确定性回放 helper、`CursorHostRequest` frame-local host handoff、`ZrRuntimeHostRequestV1::Cursor` dynamic ABI、app host `apply_runtime_cursor_host_request(...)`、desktop `platform.cursor_options=supported:winit_window_options`、app gilrs → runtime ABI → `session/events.rs` → `InputEvent::Gamepad*` bridge、`runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 与 pending Cargo gate 纳入结构审计，并拆出 108 行 `input_stack_markdown.py` 作为 Markdown owner，当前镜像事实为 `expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 15`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；2026-06-20 `cargo test -p zircon_runtime --lib input --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-12-input-validation-0620 --message-format short --color never -- --test-threads=1 --nocapture` 1200s timeout no result，匹配残留 cargo/rustc 已停止，轻量守卫 py_compile/direct `input_stack_boundary_audit`/standalone `input_stack.rs` 4/4/rustfmt 通过；input/action_map/gamepad/app Cargo gates 仍 pending；13-M0/M1/M2 已完成宿主清册、capability 代表守卫、脚本持有实体失效语义与 native ECS ABI 旁路 source guard，`script_binding_boundary` 已把 host ledger/capability/gameplay facade/native ECS ABI/Cargo gate 纳入结构审计，Cargo 待跑且后续新增 bridge/VM 行为仍需与 plugin 会话错峰；14-M0/M1 四族判词、navigation/engine_module/diagnostic_log/animation 边界守卫与 crate 根四族席位总守卫已静态落地，Cargo/rustc 重跑待编译通道空闲。

## 4. 全局边界约束（各子计划必须遵守）

继承 `Runtime 吸收层与 Editor_Scene 边界收束计划.md` 与 render 总计划 §5:

1. 不新增 crate;公共架构保持 `zircon_app`/`zircon_runtime`/`zircon_editor` 三件套 + `zircon_runtime_interface` ABI 层 + 内部 `core/{runtime,framework,manager,math,resource}` spine。
2. 硬切换:新 owner 路径落地的同一变更内迁移调用方并删除旧路径，不留 re-export、alias、shim。
3. 非网络语义的 `server` 命名是 blocker（`target-server` 为真实 headless 服务宿主语义，合法）。
4. 动态边界（dynamic_api、native 插件、VM 插件）只传 ABI-safe 值与序列化负载。
5. generated 产物只许 leaf binding/DTO/table，不许持有业务规则、调度或状态突变。
6. 渲染骨架（RDG、MeshDrawCommand、GPUScene、可见性、光照、时域、后处理、permutation）一律归 `docs/plans/zircon_runtime/render/01-08`，本目录子计划不得重复或冲突。

## 5. 全局验收与测试基线

按 milestone-first 政策:实现切片期间只做轻量检查，每个里程碑末进入测试阶段。

- 切片期:`cargo check -p zircon_runtime --lib --locked`（必要时 `-p zircon_app` / `-p zircon_editor`）。
- 里程碑测试阶段:`cargo test -p zircon_runtime --lib --locked`（按子计划模块过滤词收窄）;涉及装配时加 `cargo test -p zircon_app --locked`。
- 插件接缝:`cargo check --manifest-path zircon_plugins/Cargo.toml --workspace --all-targets --locked`。
- 结构守卫:各子计划列出的源断言/结构测试（如 app 不得直依插件实现 crate、interface 不得出现 wgpu）。
- 文档:每个里程碑完成后按源码镜像路径更新 `docs/zircon_runtime/**`，并刷新本目录子计划状态标记。

## 6. 协调与活动会话避让

- `20260604-1232-runtime-architecture-review`（活跃）:正在执行旧渐进式计划的切片（root surface 债 4、大文件债 5、hotspots 38 的审计口径以该会话为准）。本目录 02/05 子计划的执行必须先对齐该会话的最新 touched_modules，避免双写。
- `20260611-0416-rendering-10fps-analysis`（活跃）:graphics 性能修复进行中，明示"不回退 worktree 改动、只做聚焦编辑"。07 子计划执行前必须复读该笔记。
- `20260603-2304-plugin-ecosystem-continuation`（活跃）:06 子计划执行前同上。

## 7. 工程化落地公约（2026-06-12 细化定稿，约束全部子计划执行）

各子计划已细化到切片级；执行任何切片时遵守以下公约，违反视为切片未完成：

1. **切片五要素**:每个切片的"目标文件 / 改动形态 / 调用方迁移 / 验收 / DoD"五项缺一即不开工;签名草案在动手前定稿，定稿差异回写计划文件。
2. **执行前检查清单是闸门**:各子计划"执行前检查清单"逐项过完才能动第一刀;行号与计数以重核结果为准，漂移时先回写"现状与证据"节再继续。
3. **状态节实时性**:每完成一个切片，立刻更新该计划"状态与产出记录"表（状态/日期/证据三列），禁止批量补记;基线数值在开工首日填写。
4. **milestone-first 测试节奏**:切片期只跑该计划列出的 `cargo check`;测试统一压到里程碑末的"测试阶段"命令清单，逐条可复制执行并留存输出摘要。
5. **硬切换提交粒度**:`git mv` / 删导出 / 改签名类切片，旧路径删除与调用方迁移必须同一提交闭合，禁止中间态（双签名、临时 re-export）跨提交存在。
6. **会话避让**:触及 `20260604-1232`（架构审查）、`20260611-0416`（10fps，禁止回退其改动）、`20260603-2304`（插件生态）三个活跃会话工作区前，先重读其笔记并按计划"风险与协调"节对齐;每次执行开新会话笔记（cross-session-coordination 规范）。
7. **证据纪律**:计划中标注"执行时核验:<命令>"的条目，核验输出粘入状态节;新增守卫测试一律带负例自检（参照 `authoring_boundary_guard_fails_on_representative_tokens` 模式）。
8. **共享基建复用**:结构扫描守卫（02-M4 generated、05-1.4 命名、01-1.4 manifest）共享同一套源码遍历 helper，后落地者复用先落地者，禁止三套扫描实现。
9. **参考锚点纪律（防凭空实现）**:实现型切片（新增类型/算法/调度/状态机语义）动工前，必须先读该计划"参考锚点"列出的 dev/ 源码与本 index §2.3 速查表对应行，并在状态节证据列注明"已读锚点: <路径>"；锚点与设计有出入时，差异判词写入计划再动手。计划未列锚点的领域：先查 §2.3 速查表，速查表也无对应行的，在计划中显式标注"无参考，自研判词: <理由>"后方可实现——禁止默默凭记忆复刻其他引擎行为。
