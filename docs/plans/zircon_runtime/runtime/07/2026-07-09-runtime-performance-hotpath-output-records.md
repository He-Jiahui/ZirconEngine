# 07-runtime-performance-hotpath 产出记录归档

> 来源：[`07-runtime-performance-hotpath.md`](../07-runtime-performance-hotpath.md) 的 `## 状态与产出记录`。

## 状态与产出记录

| 里程碑 | 切片 | 状态 | 完成日期 | 证据（命令输出 / 文件 / 测试名） |
|---|---|---|---|---|
| 横切 | ZrVM backend FPS command hard cutover | `runtime_07_backend_zr_vm_fps_command_hard_cutover_static_passed` | 2026-07-10 | Frameworks 03 已将 runtime 真实 ZrVM 后端 feature 硬切为 `backend-zr-vm`；Runtime 07 权威 Vampire FPS 验收命令同步到新 feature。`performance_hotpath_boundary` 同步以编号 Runtime 07 归档作为具体历史/owner-budget 证据输入，并把 FPS Cargo anchor 更新到当前 feature；Python regression 1/1，direct audit 为 source 46/test owner 91、`missing_doc_anchors=[]`、`missing_cargo_gate_anchors=[]`、mirror true、`risks=[]`。重新编译 standalone plan-status harness 后 48/48。该切片只修正当前计划/审计命令与产出归属，不改写历史编译旁证，也不关闭 FPS/profiling/package Cargo gate。 |
| 横切 | Runtime 15 M3 Runtime 07 owner-budget line-budgets guard folder-backed split | runtime_15_runtime_07_owner_budget_line_budgets_guard_folder_backed_static_passed_cargo_deferred | 2026-07-06 | `tests/runtime_absorption/performance_hotspots/owner_budget/line_budgets.rs` 继续作为 Runtime 07 line-budget route owner，root、artifact/render diagnostics、hotspot-inventory、owner-budget、scene/project 与 submit-context 预算检查分别下沉到 `tests/runtime_absorption/performance_hotspots/owner_budget/line_budgets/{root,artifact_render_diagnostics,hotspot_inventory,owner_budget,scene_project,submit_context}.rs`。新增 `runtime_15_runtime_07_owner_budget_line_budgets_guard_folder_backed_split` 锁定 moved budget entries 不回流、source inventory、status row data 与 Runtime 15 M3 status/date maps；`performance_hotpath_boundary` 当前口径为 `expected_source_file_count = 46`、`expected_test_file_count = 88`、`missing_tests = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`。验证结果：scoped rustfmt --check passed，Python py_compile passed，standalone performance-hotspots harness 26/26 passed，direct audit reported missing lists empty and risks=[]，plan-status status_output_tables 2/2 passed，old-82/conflict/trailing-whitespace scans clean，scoped git diff --check warning-only；Package Cargo deferred because `D_FREE_GB=177.39 E_FREE_GB=64.02 CARGO_PIDS=19280,26172,34948,36760 RUSTC_PIDS=36412,36776`，no package Cargo pass claimed。 |
| 横切 | Runtime 15 M3 Runtime 07 owner-budget split-layout route guard folder-backed split | runtime_15_runtime_07_owner_budget_split_layout_route_guard_folder_backed_static_passed_cargo_deferred | 2026-07-06 | `tests/runtime_absorption/performance_hotspots/owner_budget/split_layout/route.rs` 继续作为 split-layout route owner，parent route、support routes 与 split route assertions 下沉到 `tests/runtime_absorption/performance_hotspots/owner_budget/split_layout/route/{parent_route,split_route,support_routes}.rs`。新增 `runtime_15_runtime_07_owner_budget_split_layout_route_guard_folder_backed_split` 锁定 moved assertion blocks 不回流、source inventory、status row data 与 Runtime 15 M3 status/date maps；`performance_hotpath_boundary` 当前目标口径为 `expected_source_file_count = 46`、`expected_test_file_count = 91`、`missing_tests = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`。验证结果：scoped rustfmt --check passed，Python py_compile passed，standalone performance-hotspots harness 27/27 passed，direct audit reported missing lists empty and risks=[]，plan-status status_output_tables 2/2 passed，old-88/conflict/trailing-whitespace scans clean，scoped git diff --check warning-only；Package Cargo deferred because `D_FREE_GB=177.38 E_FREE_GB=56.86 CARGO_PIDS=2328,32428 RUSTC_PIDS=32452`，no package Cargo pass claimed。 |
| 横切 | Runtime 15 M3 Runtime 07 owner-budget child-routes guard folder-backed split | runtime_15_runtime_07_owner_budget_child_routes_guard_folder_backed_static_passed_cargo_deferred | 2026-07-06 | `tests/runtime_absorption/performance_hotspots/owner_budget/child_routes.rs` 继续作为 Runtime 07 child-routes route owner，submit-context、hotspot-inventory、scene/project、artifact/render diagnostics 与 owner-budget mirror checks 分别下沉到 `tests/runtime_absorption/performance_hotspots/owner_budget/child_routes/{submit_context,hotspot_inventory,scene_project,artifact_render_diagnostics,owner_budget}.rs`。新增 `runtime_15_runtime_07_owner_budget_child_routes_guard_folder_backed_split` 锁定 moved assertion blocks 不回流、source inventory、status row data 与 Runtime 15 M3 status/date maps；`performance_hotpath_boundary` 当前口径为 `expected_source_file_count = 46`、`expected_test_file_count = 82`、`missing_tests = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`。该记录只关闭静态测试 owner 组织，不关闭 extract/ecs_query/profiling/FPS Cargo gate；Cargo gate deferred。 |
| 横切 | Runtime 15 M3 Runtime 07 owner-budget sources guard folder-backed split | runtime_15_runtime_07_owner_budget_sources_guard_folder_backed_static_passed_cargo_deferred | 2026-07-06 | `tests/runtime_absorption/performance_hotspots/owner_budget/sources.rs` 继续作为 Runtime 07 owner-budget source route/type owner，`include_str!` source loading 下沉到 `tests/runtime_absorption/performance_hotspots/owner_budget/sources/load.rs`。新增 `runtime_15_runtime_07_owner_budget_sources_guard_folder_backed_split` 锁定 `include_str!` 不回流、mirror-docs source views、source inventory、status row data 与 Runtime 15 M3 status/date maps；`performance_hotpath_boundary` 当前口径为 `expected_source_file_count = 46`、`expected_test_file_count = 77`、`missing_tests = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：scoped rustfmt passed、Python py_compile passed、standalone performance-hotspots harness 24/24 passed、direct audit missing/risk empty、filtered plan-status `status_output_tables` 2/2 passed、38-file conflict/trailing-whitespace scan clean、scoped `git diff --check` warning-only。该记录只关闭静态测试 owner 组织，不关闭 extract/ecs_query/profiling/FPS Cargo gate；Cargo gate 因 active cargo/rustc lanes deferred。 |
| 横切 | Runtime M0 large-file ownership Markdown renderer split | large_file_ownership_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `large_file_ownership_markdown_split_static_passed_cargo_deferred_tests_deferred`；`large_file_ownership_markdown.py` now owns large-file hotspot, ownership-class, and ownership-gate Markdown renderers, while `large_file_ownership.py` remains the 223-line owner classification, hotspot summary, migration-debt, and risk gate owner. Direct probe reports `zircon_crate_count=6`, production Rust file count 7583, `hotspot_count=30`, `classification_count=5`, `large_file_migration_debt_count=5`, `unclassified_hotspot_count=0`, `m1_gate_status=migration-debt-present`, rendered section line counts 11/6/39, and Markdown owner 73 lines. Validation: Python py_compile, direct large-file ownership/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| 横切 | Runtime 07 performance hotspot guard folder split | runtime_15_runtime_07_performance_hotspots_guard_folder_split_static_passed_cargo_timeout_no_result | 2026-06-23 | Runtime 15 M3 结构切片已把 `zircon_runtime/src/tests/runtime_absorption/performance_hotspots.rs` 拆成 folder-backed `performance_hotspots/{submit_context,hotspot_inventory,scene_project_splits,artifact_render_diagnostics_splits}.rs` 等子 owner，父文件从 1394 行降到 12 行；`performance_hotpath_source_inventory.py` 同步把 Runtime 07 `expected_test_file_count = 11` 和新增 test inventory 记录为当前审计口径。新增 `runtime_15_runtime_07_performance_hotspots_guard_is_folder_backed` 锁定父/子模块挂载、moved guard 不回流、子 owner 行数预算与状态输出期望行。本记录只关闭测试守卫组织，不关闭 extract/ecs_query/profiling/FPS 性能验证 gate；scoped rustfmt --check、Python py_compile、standalone exact guard 1/1 与 Runtime 07 test inventory scan 通过，带锁 focused guard 与 core-min cargo check 均 120s 超时无结果，不计通过。 |
| M0 | Runtime 07 render direct runtime-frame trace export | render_direct_runtime_frame_trace_export_static_passed_profile_timeout_fps_pending | 2026-06-22 | 状态锚 `render_direct_runtime_frame_trace_export_static_passed_profile_timeout_fps_pending`；`zircon_runtime/src/graphics/tests/render_profiling.rs` 新增 `direct_runtime_frame_submit_exports_perfetto_trace_artifacts`，在 `profiling-chrome` 构建下真实创建 headless `WgpuRenderFramework`、提交 direct `ViewportRenderFrame`、`stop_capture()` 后调用 `export_report()`，并断言 `timeline.zrtrace.json`、`timeline.perfetto.json`、`hotspots.json` 与 `summary.md` 生成。该测试要求 native trace 与 Perfetto trace 同时包含 `submit_runtime_frame`、`render_frame_with_pipeline`、`DepthPrepass` 与 `depth-prepass`，把 M0.3 direct runtime-frame CPU span 从内存 snapshot 推进到 artifact trace/export 验收。`rustfmt --check`、`runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` 与 status-output expected rows 已通过并锁定该测试名、`PROFILE_TIMELINE_PERFETTO_FILE`、`timeline.perfetto.json` 与状态锚；`cargo check -p zircon_runtime --lib --no-default-features --features core-min` 当前被 Runtime 06/plugin 侧 `RuntimePluginDescriptor` private-field 编译错误挡住，profiling-chrome 聚焦命令 10 分钟超时无测试结果。后续验证目标仍是：`cargo test -p zircon_runtime --lib direct_runtime_frame_submit_exports_perfetto_trace_artifacts --profile profiling --features "profiling profiling-chrome" --locked --jobs 1`；本记录不关闭权威 vampire FPS、profiling-tracy 构建耗时或 full Runtime 07 gate。 |
| M2 | Runtime 07 render VG debug overlay frame override | render_vg_debug_overlay_frame_override_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_vg_debug_overlay_frame_override_coremin_check_passed_partial`；`ViewportRenderFrame` 新增 `runtime_overlay_override: Option<RenderOverlayExtract>`，`ViewportRenderFrame::overlays()` 现在优先返回 frame-local overlay override，否则继续借用 shared `RenderFrameExtract.debug.overlays`。`build_runtime_frame(...)` 不再用 `Arc::try_unwrap(extract).unwrap_or_else(...)` 为 VG debug overlays 克隆整帧 payload，而是通过 `runtime_virtual_geometry_debug_overlays(source_overlays: &RenderOverlayExtract, ...)` 只复制 overlay packet 并用 `with_runtime_overlays(runtime_overlays)` 附加 BVH / visbuffer scene gizmos。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 `runtime_overlay_override`、`runtime_virtual_geometry_debug_overlays` 和旧 fallback clone 禁止锚。验证：scoped rustfmt、standalone hotspot guard 1/1、standalone status-output guards 2/2 与 core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-f3-shared-extract-coremin --message-format short --color never` 通过（既有 warnings）；FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render direct runtime-frame shared context extract | render_direct_runtime_frame_shared_context_extract_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_direct_runtime_frame_shared_context_extract_coremin_check_passed_partial`；`build_frame_submission_context_from_runtime_frame_extract(...)` 新增 direct runtime-frame context builder entry，`FrameSubmissionExtractSource::RuntimeFrame` 对 `&mut Arc<RenderFrameExtract>` 使用 `Arc::make_mut(extract)` 投影 viewport size、renderer-owned previous particles、有效 HGI/VG、post-process settings、AA fallback、temporal jitter 与 post-process stack/graph；`submit_selected_runtime_frame(...)` 改为传入 `&mut frame.extract`，删除旧 direct context clone `frame.extract.as_ref().clone()`，context 最后仍通过 `frame.extract = context.source_extract()` 与 renderer frame 共享 effective payload。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 `build_frame_submission_context_from_runtime_frame_extract`、`Arc::make_mut(extract)`、`&mut frame.extract`，并禁止旧 clone 回流。验证：scoped rustfmt check 通过；standalone guard/status-output 与 core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-f3-shared-extract-coremin --message-format short --color never` 通过（既有 141 warnings）；新空 target 300s 编译窗口未完成且不计通过。VG debug overlay fallback clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render shared effective extract frame source | render_shared_effective_extract_frame_source_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_shared_effective_extract_frame_source_coremin_check_passed_partial`；`ViewportRenderFrame.extract` 改为 `Arc<RenderFrameExtract>`，`ViewportRenderFrame::from_shared_extract(...)` 可直接消费 `FrameSubmissionContext::source_extract()` 返回的 shared effective extract；generated submit / present 路径把 selected-camera owned extract 传入 `build_frame_submission_context(...)`，builder 不再执行旧 `let mut sized_extract = extract.clone();`，而是在同一个 `effective_extract` 上投影 viewport size、renderer-owned previous particles、有效 HGI/VG、post-process settings、AA fallback、temporal jitter、post-process stack/graph 后 `Arc::new(effective_extract)`。`build_runtime_frame(...)` 从 context 取 shared extract 建 runtime frame，direct runtime-frame submit 在 context 构建后执行 `frame.extract = context.source_extract()`，使渲染使用同一份 effective payload。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 `pub extract: Arc<RenderFrameExtract>`、`source_extract()`、`ViewportRenderFrame::from_shared_extract`、owned submit/present handoff、`frame.extract = context.source_extract()` 与禁止旧 initial clone 的锚点。验证：scoped rustfmt 与 core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-f3-shared-extract-coremin --message-format short --color never` 通过（既有 warnings）。direct runtime-frame context clone 已由后续 `render_direct_runtime_frame_shared_context_extract_coremin_check_passed_partial` 子切片移除；VG debug overlay fallback clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit source-extract sharing | render_submit_source_extract_shared_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_submit_source_extract_shared_coremin_check_passed_partial`；F3 第一段 runtime-submit 切片：`FrameSubmissionContext` 现在持有单个 `source_extract: Arc<RenderFrameExtract>` 作为 viewport-sized source frame，mesh/light/previous-particle accessors 直接借用该共享 payload；`build_frame_submission_context` 不再把 `extract.geometry.meshes`、五类 light Vec 或 `extract.particles.previous_sprites` 克隆进 `FrameSubmissionContext::new`，并把旧 `post_process_extract_with_effective_settings` / `visibility_extract_with_effective_advanced_features` 全帧 clone helper 收敛为单个 `effective_extract` 上的 `apply_effective_post_process_settings(...)` / `apply_effective_advanced_features(...)` 原地修改。新增 `runtime_07_submit_context_shares_large_extract_payloads` 守卫绑定代码、Runtime 07/runtime index/review 状态锚。验证：scoped rustfmt/check 通过；submit_frame_extract 静态扫描确认旧 payload Vec 字段、cloned constructor args 与旧 effective clone helper 未回流；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-f3-residual-0622 --message-format short --color never` 通过（既有 143 warnings）；聚焦 `cargo test ... runtime_07_submit_context_shares_large_extract_payloads` 1200s 超时且不计通过，残留已清理。本行关闭 context payload copy 与 effective helper clone 子切片；camera-loop descriptor enumeration 另见下一行，初始 viewport-sized `extract.clone()`、borrowed `ViewportRenderFrame` projection clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render camera-loop descriptor submissions | render_camera_loop_descriptor_submissions_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_camera_loop_descriptor_submissions_coremin_check_passed_partial`；F3 第二段 camera-loop 子切片：`camera_loop_submissions()` 现在只枚举 `CameraLoopSubmission { camera: CameraRenderDescriptor, ... }`，不再 materialize `Vec<RenderFrameExtract>`；`viewport_terminal_camera_target()` 直接读取 descriptor target，纯查询路径不再克隆整帧 extract；`submit_camera_loop()` 以 `source_extract: Option<RenderFrameExtract>` 保留源帧，最后一个 camera 直接 move source extract，前置 camera 才 clone 选中相机 extract。新增 `runtime_07_submit_context_shares_large_extract_payloads` 守卫覆盖 `camera_loop_submissions` descriptor-only 结构。验证：scoped rustfmt/check 通过；camera-loop descriptor 静态扫描通过；standalone `performance_hotspots.rs` exact guard 1/1 通过；standalone `plan_status.rs` status-output 过滤守卫 1/1 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime07-f3-source-extract-0622 --message-format short --color never` 通过（既有 143 warnings）；聚焦 `cargo test -p zircon_runtime --lib camera_loop --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime07-f3-source-extract-0622 --message-format short --color never -- --test-threads=1 --nocapture` 在编译 warnings 后异常退出、无测试结果且未生成测试二进制。本行只关闭 descriptor enumeration/terminal-query 子切片；`camera_loop_frame_submissions()` 的 borrowed `ViewportRenderFrame` projection 仍需在后续 `ViewportRenderFrame`/`RenderFrameExtract` 共享模型切片里消除整帧 clone，FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render camera-loop borrowed sequence resolution | render_camera_loop_borrowed_sequence_resolution_static_passed_cargo_deferred | 2026-06-27 | 状态锚 `render_camera_loop_borrowed_sequence_resolution_static_passed_cargo_deferred`；F3 camera-loop 预拷贝子切片：`core/framework/render/camera_stack.rs` 新增 `resolve_camera_sequence_borrowed(...)`，生产 `camera_loop_submissions(...)` 改为 `resolve_camera_sequence_borrowed(&extract.view.cameras)`，不再在排序/栈展开前调用 `resolve_camera_sequence(extract.view.cameras.clone())` 复制整组 camera descriptors；owned `resolve_camera_sequence(...)` 保留给测试和按值调用者，并共用 `resolve_active_camera_sequence(...)`。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 borrowed resolver 必须存在、生产旧 `extract.view.cameras.clone()` 解析路径不得回流、Runtime 07/review/status 锚必须同步。验证目标：scoped rustfmt、borrowed resolver/stale clone static scan、standalone hotspot/status guards；Cargo/full Runtime 07/FPS/profiling gates deferred，不计通过。本行只关闭相机序列解析预拷贝，`CameraLoopExtractSourceState` / `CameraLoopFrameSourceState` 的 post-process/VG/HGI restore 备份 clone 与 full gate 仍 pending。 |
| M2 | Runtime 07 render camera-loop source view restore narrowing | render_camera_loop_source_view_restore_narrowed_static_passed_cargo_deferred | 2026-06-27 | 状态锚 `render_camera_loop_source_view_restore_narrowed_static_passed_cargo_deferred`；F3 generated camera-loop restore 子切片：`CameraLoopExtractSourceState` 不再保存/恢复整份 `RenderViewExtract`，而只记录 `view_target_size: Option<UVec2>` 并在每个 child 前写回 `extract.view.target_size`；`extract.select_camera_descriptor(submission.camera)` 随后会覆盖 selected camera、descriptor list、pipeline、AA 与 scene camera entity，所以不需要为下一 child 克隆整组 `view.cameras`。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增旧 `view: RenderViewExtract` / `extract.view = self.view.clone()` 禁止锚和新 target-size restore 锚。验证目标：scoped rustfmt、source view restore static scan、standalone hotspot guard；Cargo/full Runtime 07/FPS/profiling gates deferred，不计通过。本行只关闭 generated camera-loop view restore clone；post-process restore 已由下一行字段级快照继续收窄，VG/HGI restore 备份 clone 与 full gate 仍 pending。 |
| M2 | Runtime 07 render camera-loop post-process source restore narrowing | render_camera_loop_post_process_restore_narrowed_static_passed_cargo_deferred | 2026-06-27 | 状态锚 `render_camera_loop_post_process_restore_narrowed_static_passed_cargo_deferred`；F3 generated/direct camera-loop restore 子切片：`CameraLoopExtractSourceState` 与 `CameraLoopFrameSourceState` 不再保存/恢复整份 `PostProcessExtract`，而是共享 `CameraLoopPostProcessSourceState` 字段级快照，只记录 child submit 会改写的 `bloom`、`color_grading`、`effect_stack`、`volumes`、`stack`、`graph`；`preview` 与 `display_mode` 不再被备份，`restore_to(...)` 只写回 source post-process 派生字段。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 `post_process: CameraLoopPostProcessSourceState`、`CameraLoopPostProcessSourceState::capture(...)` 与 `restore_to(...)` 必须存在，并拒绝 `post_process: PostProcessExtract`、`extract.post_process = self.post_process.clone()` 等整段 restore 回流。验证目标：scoped rustfmt、post-process restore static scan、standalone hotspot/status guards；Cargo/full Runtime 07/FPS/profiling gates deferred，不计通过。本行只关闭 camera-loop post-process source restore clone；VG/HGI 每 child 无条件 restore clone 已由下一行条件恢复继续收窄，full gate 仍 pending。 |
| M2 | Runtime 07 render camera-loop VG/HGI conditional source restore | render_camera_loop_vg_hgi_conditional_restore_static_passed_cargo_deferred | 2026-06-28 | 状态锚 `render_camera_loop_vg_hgi_conditional_restore_static_passed_cargo_deferred`；F3 generated/direct camera-loop feature-payload restore 子切片：`CameraLoopExtractSourceState` 与 `CameraLoopFrameSourceState` 不再在每个 child 前无条件执行 `extract.geometry.virtual_geometry = self.virtual_geometry.clone()` 和 `extract.lighting.hybrid_global_illumination = self.hybrid_global_illumination.clone()`；该子切片先用 `restore_optional_payload_source(...)` 将恢复限定到缺失 payload 场景，避免重复复制 VG/HGI 内部 Vec。后续 `render_camera_loop_source_payload_slot_owned_static_passed_cargo_deferred` 已进一步把 multi-child source backup clone 收敛到 source payload slot ownership。`runtime_07_submit_context_shares_large_extract_payloads` 守卫保留旧无条件 clone 赋值禁止锚。验证目标：scoped rustfmt、VG/HGI restore static scan、standalone hotspot/status guards；Cargo/full Runtime 07/FPS/profiling gates deferred，不计通过。本行只记录 camera-loop VG/HGI per-child unconditional restore clone 的关闭；FPS/profiling/full gate 仍 pending。 |
| M2 | Runtime 07 render camera-loop single-child source-state capture skip | render_camera_loop_single_child_source_state_capture_skipped_static_passed_cargo_deferred | 2026-06-28 | 状态锚 `render_camera_loop_single_child_source_state_capture_skipped_static_passed_cargo_deferred`；F3 generated/direct camera-loop source-state 子切片：`stream_camera_loop_extract_submissions(...)` 与 `stream_camera_loop_frame_submissions(...)` 现在只在 `submissions.len() > 1` 时捕获 `CameraLoopExtractSourceState` / `CameraLoopFrameSourceState`，并通过 `for (submission_index, submission) in submissions.into_iter().enumerate()` 让 `restore_for_submission(...)` 仅从第二个 child 开始执行。单 child 路径不再为 view/post-process/VG/HGI 做 source-state 备份 clone，也不执行首 child无意义 restore；多 child source-state 的 VG/HGI payload backup clone 由下一行 source payload slot ownership 子切片关闭。`runtime_07_submit_context_shares_large_extract_payloads` 守卫新增 conditional capture 与 `submission_index > 0` 锚。验证目标：scoped rustfmt、single-child capture-skip static scan、standalone hotspot/status guard；Cargo/full Runtime 07/FPS/profiling gates deferred，不计通过。本行只关闭单 child source-state backup clone 成本；FPS/profiling/full gate 仍 pending。 |
| M2 | Runtime 07 render camera-loop source payload slot ownership | render_camera_loop_source_payload_slot_owned_static_passed_cargo_deferred | 2026-06-28 | 状态锚 `render_camera_loop_source_payload_slot_owned_static_passed_cargo_deferred`；F3 generated/direct camera-loop multi-child source-state 子切片：`CameraLoopExtractSourceState` / `CameraLoopFrameSourceState` capture 现在通过 `take()` 取得 `RenderVirtualGeometryExtract` / `RenderHybridGiExtract` source payload slot，`FrameSubmissionSourcePayloads` 以借用形式传给 `build_frame_submission_context_from_runtime_frame_extract(...)`。build-context、`VisibilityContext::from_extract_with_history_static_index_task_pool_and_feature_payloads(...)` 与 `FrameHistoryValidationKey::from_extract_with_hybrid_gi(...)` 使用 effective payload refs 生成 visibility/history/context，source-state 不再保存 VG/HGI backup clone，也不再通过 `restore_optional_payload_source(...)` 恢复 feature payload。`runtime_07_submit_context_shares_large_extract_payloads` 守卫要求 `take()` / `source_payloads` 锚并拒绝旧 capture clone 回流。验证：scoped rustfmt、source-payload static scan、standalone hotspot guard 1/1、standalone status-output guards 2/2、docs/session anchor scan、conflict-marker scan 与 scoped diff-check 均通过；`git diff --check` 仅 LF/CRLF 规范化提示。Cargo/full Runtime 07/FPS/profiling gates 因 active cargo/rustc lanes deferred，不计通过。本行关闭 multi-child source-state 的一次性 VG/HGI payload backup clone；FPS/profiling/full gate 仍 pending。 |
| 横切 | Runtime 07 F16 compiled-scene split status guard | compiled_scene_render_split_review_guard_static_passed_cargo_deferred | 2026-06-28 | 状态锚 `compiled_scene_render_split_review_guard_static_passed_cargo_deferred`；`review_f16_compiled_scene_render_path_uses_split_owners` 锁定 2026-06-22 已完成的 `render_compiled_scene()` 结构拆分：`bind_compiled_scene_graph_resources.rs` 承接 frame/history/transient/execution/plugin resource binding，`execute_compiled_scene_graph_stages.rs` 承接 graph stage 执行与 late-stage 顺序 owner，`submit_compiled_scene_frame.rs` 承接 `queue.submit([encoder.finish()])`、HZB/readback 统计与 transient-pool release；`render.rs` 当前约 444 行，只保留编排调用和输出组装，不得回流 `execute_graph_stage(...)`、`active_late_graph_stages(...)`、submit/readback/pool-release 行为。本切片只补 review/status/docs guard，不改生产渲染逻辑；Cargo/full Runtime 07/FPS/profiling gates deferred。 |
| M2 | Runtime 07 render camera-loop frame terminal move | render_camera_loop_frame_terminal_move_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_camera_loop_frame_terminal_move_coremin_check_passed_partial`；F3 第三段 direct runtime-frame 子切片：`submit_runtime_frame(...)` 现在调用 `camera_loop_frame_submissions(frame)` 并把 source `ViewportRenderFrame` 所有权交给相机循环；`camera_loop_frame_submissions(...)` 用 `source_frame: Option<ViewportRenderFrame>` 保留源 frame，终端 child 通过 `source_frame.take()` 进入 `project_owned_frame_to_selected_camera(...)`，移动原 frame/extract/scene/UI/sidebands，避免旧 `project_frame_to_selected_camera(&frame, ...)` 对终端 child 再做 borrowed projection clone。新增守卫要求 owned frame 签名、`source_frame.take()`、`project_owned_frame_to_selected_camera(...)` 与旧 borrowed helper 名称不回流。验证：scoped rustfmt、camera-loop frame 静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2、core-min `cargo check` 通过（既有 warnings）；Cargo wrapper 聚焦 test 1200s 超时但生成二进制，direct exact `camera_loop_frame_submissions_project_selected_children_and_terminal_ui` 1/1 通过。该记录只关闭 direct runtime-frame terminal child clone 子切片；非终端 borrowed frame projection clone、初始 viewport-sized `extract.clone()`、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit feedback sideband owned merge | render_submit_feedback_sidebands_owned_merge_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_submit_feedback_sidebands_owned_merge_coremin_check_passed_partial`；F3 feedback 子切片：`PreparedRuntimeSubmission` 提供 `take_hybrid_gi_readback_outputs()` / `take_particle_readback_outputs()` / `take_virtual_geometry_readback_outputs()`，`collect_runtime_feedback(...)` 从 `&mut PreparedRuntimeSubmission` 按值消费 prepared sideband 回读输出，保留 `RenderVirtualGeometryReadbackOutputs` owned-merge 证据锚；HGI、Particle、Virtual Geometry merge helper 改为接收 owned sideband 输出并移动其中 Vec 内容，避免 `collect_runtime_feedback.rs:81,175,183` 在 renderer 无对应输出时 clone 整个 prepared sideband 回读包。`prepared_runtime_sidebands()` 仍在 render 前把 sideband 快照挂入 `ViewportRenderFrame`，record 阶段继续从 `PreparedRuntimeSubmission` 取可逐出 probe/page id。`runtime_07_submit_context_shares_large_extract_payloads` 守卫扩展锁定 owned-merge 结构并拒绝 borrowed sideband clone 回流。验证：scoped rustfmt、owned-merge 静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2 与 core-min `cargo check` 通过（既有 142 warnings）；聚焦 lib-test `merge_` 未产出可用结果，exact merge filter 300s 超时且无 lib-test 二进制，残留本目标目录进程已清理。该记录只关闭 feedback sideband clone 子切片，初始 viewport-sized `extract.clone()`、非终端 direct runtime-frame borrowed projection clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render prepared sideband frame owner move | render_prepared_sideband_frame_owner_move_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_prepared_sideband_frame_owner_move_coremin_check_passed_partial`；F3 prepared sideband frame-owner 子切片：`PreparedRuntimeSubmission::into_prepared_runtime_sidebands(self)` 将 plugin renderer outputs、HGI evictable probe ids 与 VG evictable page ids 直接 move 到 `ViewportRenderFrame`；`ViewportRenderFrame::prepared_runtime_sidebands_mut()` 把 frame-owned `RenderPreparedRuntimeSidebands` 交给 `collect_runtime_feedback(...)` drain readback outputs，并通过 `with_evictable_probe_ids(sidebands.take_hybrid_gi_evictable_probe_ids())` / `with_evictable_page_ids(sidebands.take_virtual_geometry_evictable_page_ids())` 保留原 record 阶段的 evictable 更新。`record_submission(...)`、`record_present_submission(...)` 与 `record_non_viewport_camera_state_after_success(...)` 不再接收 `PreparedRuntimeSubmission`；非终端 `project_borrowed_frame_to_selected_camera(...)` 不再克隆会被后续 child prepare 覆盖的 `frame.prepared_runtime_sidebands`。守卫拒绝 `prepared.prepared_runtime_sidebands()`、`plugin_renderer_outputs.clone()`、`hybrid_gi_evictable_probe_ids.clone()`、`virtual_geometry_evictable_page_ids.clone()`、prepared-side evictable take 与 `frame.prepared_runtime_sidebands.clone()` 回流。验证：scoped rustfmt、old-clone 静态扫描、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2 与 core-min `cargo check` 通过（既有 142 warnings）。该记录只关闭 prepared sideband frame snapshot clone 子切片，初始 viewport-sized `extract.clone()`、非终端 direct runtime-frame borrowed extract/scene projection clone、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render direct runtime-frame streaming camera loop | render_direct_runtime_frame_streaming_camera_loop_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_direct_runtime_frame_streaming_camera_loop_coremin_check_passed_partial`；F3 direct runtime-frame streaming 子切片：`submit_runtime_frame(...)` 不再 materialize `camera_loop_frame_submissions(frame)`，而是调用 `submit_camera_loop_frame(...)`，把一个 mutable `ViewportRenderFrame` 按 camera descriptor 顺序流式提交给 `submit_selected_runtime_frame(...)`。`CameraLoopFrameSourceState` 在每个 child 前恢复 viewport size、view target size、output target、frame visibility、post-process、VG 与 HGI 源字段，`RenderFrameExtract::select_camera_descriptor(...)` / `RenderViewExtract::select_camera_descriptor(...)` 负责原地选中相机，terminal UI 通过 `terminal_ui.take()` 只移动到最终 child；`submit_selected_runtime_frame(...)` 改为接收 `&mut ViewportRenderFrame`，prepared sideband 原地挂载到 frame 后再由 feedback drain。旧 `camera_loop_frame_submissions(...)`、`CameraLoopFrameSubmission` 与 `project_borrowed_frame_to_selected_camera(...)` 收窄为 test-only 行为覆盖，production `submit_runtime_frame.rs` 守卫拒绝恢复整帧 projection 列表。验证：scoped rustfmt、production streaming 静态扫描、focused `submit_camera_loop_frame_streams_selected_children_and_restores_source_fields` lib-test 1/1、standalone `performance_hotspots.rs` exact guard 1/1、standalone `plan_status.rs` status-output 2/2 与 core-min `cargo check` 通过（既有 warnings）。该记录关闭 direct runtime-frame 非终端 borrowed extract/scene projection clone 子切片；初始 viewport-sized `extract.clone()`、更大的 `RenderFrameExtract`/`ViewportRenderFrame` 共享模型、FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit effective extract projection | render_submit_effective_extract_projection_coremin_check_passed_partial | 2026-06-22 | 状态锚 `render_submit_effective_extract_projection_coremin_check_passed_partial`；F3 第三段 residual clone 子切片：submit builder 不再通过 `post_process_extract_with_effective_settings(...)` 或 `visibility_extract_with_effective_advanced_features(...)` 克隆整帧 extract，而是在 shared extract owner 上用 `Arc::make_mut(extract_source)` 应用有效 HGI/VG 与 post-process settings，并由后续 shared builder 形态以 `Arc::clone(extract_source)` 交给 `FrameSubmissionContext`。`runtime_07_submit_context_shares_large_extract_payloads` 守卫同步拒绝旧 helper 回流并锁定 `apply_effective_advanced_features(...)` / `apply_effective_post_process_settings(...)`。验证：scoped rustfmt 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-f3-residual-0622 --message-format short --color never` 通过（既有 143 warnings）。本行只关闭 submit builder 内 effective projection clone；普通 generated camera-loop source clone 已由 `render_generated_camera_loop_shared_extract_static_passed_cargo_locked_blocked` 收敛，FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render generated camera-loop shared extract | render_generated_camera_loop_shared_extract_static_passed_cargo_locked_blocked | 2026-06-22 | 状态锚 `render_generated_camera_loop_shared_extract_static_passed_cargo_locked_blocked`；F3 generated submit/present camera-loop 子切片：`submit_camera_loop(...)` 不再为非终端 child 执行 `extract.clone().with_selected_camera_descriptor(...)`，而是把源 `RenderFrameExtract` 包成 `Arc`，通过 `stream_camera_loop_extract_submissions(...)` 和 `CameraLoopExtractSourceState` 在每个 child 前恢复 view/post-process/VG/HGI 源字段，再用 `Arc::make_mut(&mut source_extract)` 原地选中 camera。`submit_selected_camera_frame(...)` 与 `present_selected_camera_frame(...)` 改为接收 `&mut Arc<RenderFrameExtract>` 并复用 `build_frame_submission_context_from_runtime_frame_extract(...)`，builder 删除旧 owned `build_frame_submission_context(...)` 与 `FrameSubmissionExtractSource::{Owned,RuntimeFrame}` 分支，最终直接 `Arc::clone(extract_source)` 交给 `FrameSubmissionContext`。新增 `submit_camera_loop_streams_source_extract_and_restores_derived_state` 行为测试覆盖 child 间 target size 派生状态恢复；`runtime_07_submit_context_shares_large_extract_payloads` 守卫拒绝普通 camera-loop clone 和旧 owned terminal move 回流。验证：scoped rustfmt、standalone hotspot guard 1/1 与旧 owned builder/`FrameSubmissionExtractSource` 扫描通过；聚焦 Cargo test 与最终 core-min `cargo check --locked` 被当前工作树 `Cargo.lock` 漂移阻断（`zircon_plugin_sdk` 依赖行已从多个 plugin lock entries 移除，Cargo 要求更新 lock file），不计通过；本轮较早的 core-min check 在锁漂移前通过但不作为最终代码通过声明。该记录关闭普通 generated submit/present camera-loop source clone 子切片；FPS/profiling/full Runtime 07 gates 仍 pending。 |
| M2 | Runtime 07 render submit viewport/provider errors | render_submit_viewport_provider_errors_review_guard_static_passed_cargo_timeout_no_result_full_runtime07_pending | 2026-06-27 | 状态锚 `render_submit_viewport_provider_errors_review_guard_static_passed_cargo_timeout_no_result_full_runtime07_pending`；F4 production panic 子切片保持闭合：`viewport_generation_guard.rs` 提供 `validate_viewport_generation(...)` 与 `viewport_record_mut_after_generation_check(...)`，`submit/submit.rs`、`submit/submit_runtime_frame.rs` 与 `submit/present_frame_extract.rs` 写回 viewport record 前均走 checked helper，viewport 删除返回 `UnknownViewport`，generation 变化返回 `ViewportChanged`。`prepare_runtime_submission/prepare.rs` 将 HGI/VG runtime provider 缺席从 panic/隐式缺口改为 `RenderFrameworkError::UnsupportedCapability`，并在返回错误前清理 stale runtime state。新增 `review_f4_render_submit_capability_gaps_return_typed_errors` 锁定 `submit_frame_extract` production paths must return RenderFrameworkError、production submit/prepare 段无裸 `.unwrap(`/`.expect(`，并把状态输出期望同步到 Runtime 07。验证：scoped rustfmt 与静态扫描通过；standalone `rustc --test` exact F4 guard 1/1 通过；core-min 聚焦 Cargo 在 `E:\cargo-targets\zircon-runtime-f4-submit-capability-0627` 15 分钟 timeout no result，匹配本目标目录的 cargo/rustc 残留已停止；完整 Runtime 07 FPS/profiling/full gates 仍 pending。旧实现证据 `render_submit_viewport_provider_errors_coremin_passed` 的 focused guard 与 core-min `cargo check` 已于 2026-06-22 通过。 |
| 横切 | Runtime 07 scene/EventBus poison-safe locks | scene_level_poison_recovery_coremin_passed_eventbus_guard_timeout | 2026-06-22 | 状态锚 `scene_level_poison_recovery_coremin_passed_eventbus_guard_timeout`；F2 低层共享锁修复：`LevelSystem` 的 world/runtime_state/metadata/lifecycle/subsystems 锁入口改为 poison 恢复 helper，`DefaultLevelManager` 的 level map 锁入口改为 `lock_levels()`，`EventBus` 的 subscribers/delivery 锁入口改为 owner 内私有 `lock_subscribers()` / `lock_delivery()`；`level_system.rs` 模块内 poison 测试覆盖 world/runtime/metadata/lifecycle/subsystems 恢复路径。验证：scoped rustfmt/check 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-f2-lock-poison-0622 --message-format short --color never` 通过（既有 warnings）；`cargo test ... level_system_accessors_recover_poisoned_state_locks` 通过 1 test；`cargo test ... level_system_recovers_world_lock_after_writer_panic` 通过 1 test。EventBus root 结构守卫单独执行超时且不计通过，残留编译进程已清理；Runtime 07 extract/ecs_query/performance profiling/FPS gates 仍 pending。 |
| 横切 | Runtime 07 Performance hotpath Markdown renderer split | performance_hotpath_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `performance_hotpath_markdown_split_static_passed_cargo_deferred_tests_deferred`；`performance_hotpath_markdown.py` owns `render_performance_hotpath_boundary_markdown(...)`; `performance_hotpath_boundary.py` remains the 643-line audit/risk owner, and the Markdown owner is 139 lines. Direct audit currently reports source files 46/46, guard/test files 11/11, frame span anchors 9/9, QueryState telemetry anchors 32/32, change-detection telemetry anchors 13/13, extract telemetry anchors 21/21, asset-worker candidate telemetry anchors 13/13, animation scene telemetry anchors 19/19, profile counter hotspot export anchors 8/8, hotspot guard anchors 32/32, Runtime 07 counter assertion anchors 29/29, doc anchors 35/35, pending Cargo/profiling/FPS gate anchors 5/5, mirror docs guard present, stale hotspot placeholder false, large-file owner gate `classified-and-clear`, threshold 1000, hotspots 0, debt groups 0, owner classes 0, unclassified hotspots 0, missing anchors empty, and `risks = []`. Validation: Python py_compile and direct `performance_hotpath_boundary_audit`; standalone `performance_hotspots.rs` and `plan_status.rs` target this row while package-level `extract` / `ecs_query` / profiling / FPS gates remain deferred. |
| 横切 | Runtime 07 Performance hotpath inventory split | performance_hotpath_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `performance_hotpath_inventory_split_static_passed_cargo_deferred_tests_deferred`；`performance_hotpath_source_inventory.py` owns the Runtime 07 source/test file lists and expected count constants at 70 lines; `performance_hotpath_anchor_inventory.py` owns frame/query/change/extract/asset-worker/animation/profile-counter/test/doc/Cargo anchor tuples at 244 lines; `performance_hotpath_boundary.py` is now the 353-line audit reader, missing-anchor calculator, large-file gate consumer, and risk aggregator, while `performance_hotpath_markdown.py` remains the 139-line renderer owner. Direct audit currently reports source files 46/46, guard/test files 11/11, frame span anchors 9/9, QueryState telemetry anchors 32/32, change-detection telemetry anchors 13/13, extract telemetry anchors 21/21, asset-worker candidate telemetry anchors 13/13, animation scene telemetry anchors 19/19, profile counter hotspot export anchors 8/8, hotspot guard anchors 32/32, Runtime 07 counter assertion anchors 29/29, doc anchors 35/35, pending Cargo/profiling/FPS gate anchors 5/5, rendered lines 18, mirror docs guard present, large-file owner gate `classified-and-clear`, hotspots 0, debt groups 0, owner classes 0, unclassified hotspots 0, and `risks = []`. Validation: Python py_compile, direct `performance_hotpath_boundary_audit`, and direct renderer probe; package-level `extract` / `ecs_query` / profiling / FPS gates remain deferred. |
| M0 | 0.1 权威 FPS | fps_gate_support_unblocked_timeout_no_result | 2026-06-17 | 首次运行在 `zr_vm_rust_binding_sys` build script 因 `ZR_VM_RUST_BINDING_LIB_DIR` 未设置而停止；已定位本机 MSVC debug import library `E:\Git\zr_vm\build\codex-msvc-debug\lib\Debug\zr_vm_rust_binding.lib` 与 DLL 目录 `E:\Git\zr_vm\build\codex-msvc-debug\bin\Debug`，带环境变量重跑后越过 ZR VM build script。lib-test 编译随后暴露三处支持层阻塞并已最小修复：asset 测试改走 `zircon_runtime_interface::ui::template::UI_ASSET_CURRENT_SOURCE_SCHEMA_VERSION` 公开 re-export，`compile_routes_bloom_extract_after_split_scene_color_passes` 的 `RenderBloomSettings` 初始化补 `..Default::default()`。`rustfmt --edition 2021 --check` 覆盖三处修复通过；随后同命令重跑 904s 超时无测试结果、未产生 `vampire_runtime_perf` 输出，残留的本次 cargo/rustc 进程已停止。M0.1 仍需在空闲编译窗口重跑同命令 ×2。 |
| M0 | 0.2 profiling 构建 | profiling_build_tooling_static_passed_cargo_deferred_active_lanes | 2026-06-17 | `profiling_build_tooling_static_passed_cargo_deferred_active_lanes`：`tools/zircon_build.py` 现在支持 `--mode profiling` 并映射到 Cargo `--profile profiling`，同时新增 `--runtime-features target-client,profiling,profiling-tracy` 作为 Runtime 07 M0.2 的可复现 feature 组合；Hub/Tauri 与独立 plugin workspace 在 profiling mode 下显式拒绝，避免把非 runtime profiling payload 误记为通过。`tools/dev-fast-build.ps1` 新增 `-CargoProfile profiling`，计划命令可写为 `./tools/dev-fast-build.ps1 -Profile client -Action check -Package zircon_runtime -CargoProfile profiling -FeatureOverride "target-client profiling profiling-tracy"`；`docs/cli-and-tooling/zircon-build-tool.md` 与 `docs/zircon_runtime/core/diagnostics/profiling.md` 已同步记录 `python tools/zircon_build.py --targets runtime --out E:\builds\zircon-profile --mode profiling --runtime-features target-client,profiling,profiling-tracy`。新增 `runtime_absorption::performance_hotspots::runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` anchors 锁定根 `[profile.profiling]`、`zircon_runtime` profiling feature、Python 工具 dry-run 入口与 PowerShell fast-build 入口；`performance_hotpath_boundary` 当前镜像计数随本轮工具入口扩展为 `hotspot_guard_anchor_count = 23`、`doc_anchor_count = 21`。当前仍有外部 `zircon_export_pack` Cargo/rustc lane，真实 profiling 构建耗时/瓶颈记录待空闲编译窗口复跑，不能把 M0.2 作为构建通过。 |
| M0 | 0.3 帧分解 span | frame_spans_static_passed_trace_pending | 2026-06-22 | 已落 frame breakdown span：`runtime_frame_time_update` / `runtime_frame_update` 写在 `dynamic_api/session.rs::tick_frame()`，`runtime_frame_extract` 写在 `dynamic_api/session/extract.rs::current_extract()`，`runtime_frame_submit` 写在 `dynamic_api/runtime_loop.rs::{submit_extract_with_ui,present_extract_with_ui}`，`runtime_frame_schedule_stage.<SystemStage>` 写在 `scene/ecs/schedule_runner.rs::run_stage(...)`；2026-06-22 又把 direct runtime-frame submit 补齐到同等 render-framework 分段：`submit_runtime_frame.rs` 现在覆盖 `build_submission_context` / `prepare_runtime_submission` / `render_frame_with_pipeline` / `collect_runtime_feedback`，并由 `runtime_07_submit_context_shares_large_extract_payloads` 与 `direct_runtime_frame_submit_nests_render_graph_spans_under_pipeline_scope` 锁定；`SceneScheduleRunner` span 使用 `profile_dynamic_scope!("runtime", "frame", format!("runtime_frame_schedule_stage.{stage:?}"))`，只在 profiling features 下 materialize stage 名称，不改变 borrowed sorted-step iterator、deferred flush、hook 执行或 final consistency sweep 语义；`runtime_absorption::performance_hotspots::runtime_07_hotspot_inventory_requires_counted_evidence_before_m2` 已扩展锁定 schedule-runner span、Runtime 07 计划、hotspot inventory、dynamic session doc、ECS doc、M0 架构评审与总索引锚点，并通过 `runtime_absorption/mod.rs` 挂载；静态守卫本轮 red->green，trace/profiling 构建验收仍待 Cargo/profiling/FPS gate 清除后复跑。 |
| M1 | 1.1 计数点 | scoped_counter_points_extract_implemented_cargo_blocked | 2026-06-13 | 已完成并验证两组本地计数口：1. `QueryState` 记录 `cache_hits` / `cache_misses` / `cache_rebuilds` / candidate / matched / revision，并通过 `QueryStateCacheStats::record_diagnostics(...)` 写入 `ecs.query.archetype_cache_hits` / `ecs.query.archetype_cache_misses` / `ecs.query.archetype_cache_rebuilds` / `ecs.query.candidate_entities` / `ecs.query.matched_entities`；2. `ChangeDetectionScanStats` 写入 `ecs.change_detection.scanned_marks` / `ecs.change_detection.added_matches` / `ecs.change_detection.changed_matches`。行为与诊断读回测试已通过：`cargo test -p zircon_runtime --lib query_state_cache_stats_record_reuse_and_rebuild_counts --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-07-query-0613 --message-format short --color never -- --nocapture --test-threads=1` 1/1 通过；`cargo test -p zircon_runtime --lib change_detection_scan_stats_record_mark_checks_and_diagnostics --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-07-query-0613 --message-format short --color never -- --nocapture --test-threads=1` 1/1 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-07-query-0613 --message-format short --color never` 通过（既有 warning set）；`rustfmt --edition 2021 --check` 覆盖 QueryState/change_detection touched 文件并通过；`git diff --check` 与冲突标记扫描通过（仅 LF-to-CRLF warning）；`query_state/mod.rs` 非空 174 行，低于 180 行预算。extract 构建点已补 `dynamic_api/session/extract.rs` 与 `session/extract_stats.rs`：`current_extract()` 每次构建后自动写入 `extract.rebuild_clones` / `extract.output_bytes`，`headless_session_capture_records_frame_extract_diagnostics` 断言 headless capture 读回 rebuild=1 且 output bytes 非零；`rustfmt --edition 2021 --check`、`git diff --check`、冲突标记扫描已覆盖 extract touched 文件并通过。Cargo 级验证暂未通过到本切片：`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-07-query-0613 --message-format short --color never` 被活跃 render owner 的 HZB 代码阻塞，先报 `graphics/scene/scene_renderer/hzb/hzb_occlusion_culler.rs` 缺 `HzbOcclusionCullReport`，以及 `scene_renderer_core_render_compiled_scene/render/render.rs` 两处 `expected &ShadowMapRenderer, found &HzbOcclusionCuller`。未完成项：M1.1 的 Cargo rerun、vampire 场景非零/稳定输出、QueryState/change detection 的帧级自动采集仍待后续切片。 |
| M1 | 1.1 ECS 帧级诊断聚合 | ecs_frame_diagnostics_static_passed_cargo_deferred | 2026-06-17 | 新增 `zircon_runtime/src/scene/ecs/frame_performance_diagnostics.rs` 与公开 `EcsFramePerformanceDiagnostics`，在 ECS owner 内聚合多条 `QueryStateCacheStats` 与 `ChangeDetectionScanStats`，并将二者写入同一个 `DiagnosticStore` frame index；extract 诊断仍由 `dynamic_api/session/extract_stats.rs` 私有 owner 自动记录，不拉入 ECS 聚合器。新增源码锚点测试 `ecs_frame_performance_diagnostics_record_query_and_change_counts`，锁定 cache hit/miss/rebuild、candidate/matched entity、scanned/added/changed counters 的同帧合并读回；`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 24`、`query_counter_anchor_count = 32`、`test_anchor_count = 18`、`doc_anchor_count = 18`。Cargo 运行按当前实现优先要求延后，后续与 `ecs_query` / Runtime 07 gates 一起补跑。 |
| M1 | 1.1 QueryState 帧自动采集 | system_query_frame_telemetry_static_passed_cargo_deferred | 2026-06-17 | `QueryState::try_new(...)` 在初始 cache build 后建立 `last_reported_cache_stats` 基线，`QueryStateCacheStats::saturating_delta_since(...)` 与 `QueryState::take_unreported_cache_stats()` 只报告每次系统运行后的新增 query cache 活动；`SystemParam::record_performance_diagnostics(...)` 由 `SystemState::run(...)` 在恢复 active change tick 后调用，tuple params 与 `ParamSet` 递归转发到内部 `QueryState`，`World::record_ecs_query_cache_stats(...)` 写入当前 `EcsFramePerformanceDiagnostics`，`WorldDriver::tick_level(...)` 在每帧 stage 执行前重置 world-local ECS frame diagnostics。新增 `system_state_records_query_cache_stats_into_world_frame_diagnostics` 锁定两个 `SystemState<QueryState<...>>` 同帧聚合为 2 次 cache hit，并验证 frame reset 后只记录下一次系统运行增量。验证：rustfmt check 通过；Cargo/ecs_query 按用户要求暂缓到后续验证窗口。 |
| M1 | 1.1 ChangeDetection 帧自动采集 | system_change_detection_frame_telemetry_static_passed_cargo_deferred | 2026-06-17 | `QueryFilter::matches_component_locations_with_stats(...)` 让 cached QueryState 读取路径在执行 `Added<T>` / `Changed<T>` 过滤时累积 `ChangeDetectionScanStats`；`QueryIter` 与 `QueryManyCachedIter` 在本地扫描后通过 drop 合并回 `QueryState::record_change_detection_stats(...)`，`is_empty_cached_with_ticks(...)`、`count_cached_with_ticks(...)`、`contains_cached_with_ticks(...)` 与 `get_cached_after_update_with_ticks(...)` 直接汇总单次读取扫描量；`QueryState::take_unreported_change_detection_stats()` 经 `SystemParam::record_performance_diagnostics(...)` 写入 `World::record_ecs_change_detection_stats(...)`，与 query cache stats 同帧聚合。新增 `system_state_records_change_detection_stats_into_world_frame_diagnostics` 锁定 `Changed<Health>` 系统运行后的 scanned/changed frame aggregation。验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-07-change-detection-0617 --message-format short --color never` 约 180s 超时无测试结果，残留 cargo/rustc/rustdoc 检查无运行进程；Cargo/ecs_query 按用户要求暂缓到后续验证窗口。 |
| M1 | 1.1 QueryState iterator lifetime guard | query_state_iterator_lifetime_static_passed_cargo_timeout_no_result | 2026-06-17 | `QueryIter` 与 `QueryManyCachedIter` 的 cached stats sink 从直接持有 `&QueryState<D, F>` 改为 `NonNull<QueryState<D, F>>`，cached slice borrow 仍保持实际生命周期约束，但 stats 回写不再把 QueryState 借用扩散到 read-only, non-cached iterators 的 item 输出生命周期。该底层修复解除 `QueryState::single` 在只读查询路径触发的 E0309 风险，并把 `state: Option<NonNull<QueryState<D, F>>>` 纳入 `performance_hotpath_boundary`；当前同步为 `expected_source_file_count = 24`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`test_anchor_count = 18`、`doc_anchor_count = 18`。验证：query iterator touched files rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit`、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32 与 scoped diff/conflict checks 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-07-lifetime-0617 --message-format short --color never` 300s timeout no result，残留 cargo/rustc/rustdoc 检查为空，后续随 Runtime 07 `ecs_query` gate 补跑。 |
| M1 | 1.1 QueryState 结构审计同步 | structure_audit_static_passed_cargo_pending | 2026-06-13 | `ecs_query_state_boundary` 已把 `query_state/stats.rs` 收敛为 Runtime 07 cache telemetry owner：`expected_module_count = 8`、`unexpected_modules = []`、`risks = []`；`docs/zircon_runtime/scene/ecs/query_state.md` 同步记录 8 个 folder-backed owner modules 与 `stats.rs` telemetry sidecar。已完成 `python -m py_compile` 与定向 `ecs_query_state_boundary_audit(...)` 结构断言；完整聚合审计在共享仓库上超出本轮轻量窗口，Cargo 运行仍随 Runtime 07 M1.1/M1.2 gate 待 active lanes 清空后补跑。 |
| M1 | 1.2 计数断言 | named_assertions_static_passed_cargo_blocked | 2026-06-13 | 三个命名断言测试已落地：`query_state_reuses_archetype_matches_across_unchanged_frames`（`zircon_runtime/src/scene/tests/ecs_performance_acceptance.rs`）断言 unchanged world revision 下 QueryState cache hit 递增、miss/rebuild/revision/candidate/matched 稳定；`change_detection_scan_skips_unmarked_archetypes`（`zircon_runtime/src/scene/tests/ecs_change_detection.rs`）断言 stale/unmarked tick marks 扫描后 added/changed match 仍为 0 且诊断读回一致；`frame_extract_rebuild_skips_unchanged_entities`（`zircon_runtime/src/dynamic_api/session/tests/frame_diagnostics.rs`）按计划先锚定现状值，两个 unchanged headless capture 均记录 `extract.rebuild_clones = 1` 且 `extract.output_bytes` 非零稳定。`rustfmt --edition 2021 --check` 已覆盖三份测试文件并通过；源码扫描确认测试名和 helper 存在；行数检查：`session/tests/frame_diagnostics.rs` 113 行、`session/tests/helpers.rs` 501 行，`vampire_gameplay.rs` 179 行，`vampire_hud.rs` 172 行，其他 session test owners 更低、`ecs_performance_acceptance.rs` 246 行、`ecs_change_detection.rs` 256 行。Cargo 验证未声明通过，仍待 render-owned HZB blocker（缺 `HzbOcclusionCullReport`；两处 `expected &ShadowMapRenderer, found &HzbOcclusionCuller`）清除后补跑。 |
| M1 | 1.3 热点清单 | inventory_scaffold_static_passed_pending_authoritative_values | 2026-06-13 | 新增 `docs/zircon_runtime/performance/hotspot_inventory.md` 与 `runtime_absorption::performance_hotspots::runtime_07_hotspot_inventory_requires_counted_evidence_before_m2`，把热点清单从空占位推进为有证据门槛的 M1.3 scaffold：无权威 runtime 数值不得进入 M2；extract 全量重建候选只以 `frame_extract_rebuild_skips_unchanged_entities` 的 `extract.rebuild_clones = 1` / stable `extract.output_bytes` 作为当前可计数 baseline，不宣称 top 排名；QueryState 当前证据为 128 entities / 8 repeated runs / 8 hits / 1 miss / 1 rebuild，暂不作为优化目标；change detection 当前证据为 6 scanned marks / 0 added / 0 changed，暂不作为优化目标；asset worker 仅有结构诊断与 `asset.worker.budgeted_threads`，未有每帧成本证据；10fps RenderDoc 的 230 draws / 231 `vkCmdCopyBuffer` / 31 render passes 明确分流至 render 计划 02/04。新增守卫将文档、Runtime 07 计划、总索引、QueryState/change detection/extract 测试和 10fps 证据锚点绑定在一起；新增 `runtime_absorption::plan_status::cargo_gates::runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation` 把 extract/ecs_query/performance profiling/FPS gates 固定为提升完成前的剩余验证门；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\performance_hotspots.rs zircon_runtime\src\tests\runtime_absorption\mod.rs` 通过；冲突标记/尾随空白/锚点扫描与 scoped `git diff --check` 通过（仅 LF-to-CRLF warning）；本轮补验 standalone rustc `performance_hotspots.rs` 3/3 通过；Cargo/profiling/FPS 仍待 render/active lanes 清空后补跑。 |
| M1 | 1.3 性能热路径结构审计镜像 | structure_audit_static_passed_cargo_pending | 2026-06-14 | `performance_hotpath_boundary` 已接入 Python 结构审计并覆盖 Runtime 07 frame span、QueryState/change-detection/extract/asset-worker 计数、hotspot inventory、测试锚、pending Cargo/profiling/FPS gate 与 Runtime 07 owner-budgeted optimization gate：`expected_source_file_count = 24`、`expected_test_file_count = 6`、`frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 10`、`asset_worker_anchor_count = 11`、`hotspot_guard_anchor_count = 20`、`test_anchor_count = 18`、`doc_anchor_count = 18`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = migration-debt-present`、`large_file_hotspot_count = 39`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`missing_large_file_owner_classes = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`；同时消费 `large_file_ownership_gate`，固定 `large production files remain above the owner budget` 的优化前置裁决。`risks = []` 仅代表 Runtime 07 结构镜像同步；`extract` / `ecs_query` / profiling / vampire FPS Cargo gates 仍待 active lanes 清空后补跑。 |
| M1 | 1.3 owner-budget gate 守卫同步 | owner_budget_guard_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::performance_hotspots::runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit` 把 `large-file-ownership-m1.md`、本计划、runtime index、`hotspot_inventory.md`、M0 review 与 interface-convergence mirror 的 owner-budget 摘要固定到同一组结构审计事实：threshold 1000、hotspots 38、debt groups 5、owner classes 5、unclassified 0，且拒绝 stale 33/41/42-hotspot、旧 owner-count 与已删除的 Hub `app/` 路径锚点。该守卫只锁定镜像一致性，不启动 M2 优化；验证：rustfmt check、Python py_compile、direct/aggregate `performance_hotpath_boundary` audit、standalone rustc `performance_hotspots.rs` 3/3、旧计数锚点扫描、conflict/trailing scans 通过；Cargo/profiling/FPS gates pending。 |
| M1 | 1.3 性能热路径镜像文档守卫 | mirror_docs_guard_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::performance_hotspots::runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` 与 `performance_hotpath_boundary` 的 `mirror_docs_guard_present` 把本计划、runtime index、`docs/zircon_runtime/performance/hotspot_inventory.md`、`docs/zircon_runtime/dynamic_api/session.md`、`docs/zircon_runtime/scene/ecs.md`、M0 review 与 runtime-interface convergence 固定到同一组结构审计事实：`expected_source_file_count = 24`、`expected_test_file_count = 6`、`frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 10`、`asset_worker_anchor_count = 11`、`hotspot_guard_anchor_count = 20`、`test_anchor_count = 18`、`doc_anchor_count = 18`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = migration-debt-present`、`large_file_hotspot_count = 39`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`missing_large_file_owner_classes = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`、`risks = []`；owner-budget 摘要继续保留 `hotspots 39, debt groups 5, owner classes 5, unclassified hotspots 0`。该切片只锁定镜像文档一致性，不启动 M2 优化；验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit`、aggregate `audit_runtime_structure.py --json` Runtime 07 assertions、standalone rustc 6/6、旧计数锚点扫描、conflict/trailing scans 通过；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 owner-budget evidence drift resync | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `large_file_ownership_gate` 当前同步为 38 hotspots / runtime-other=14 / runtime-framework-render=2；`zircon_runtime/src/asset/assets/animation.rs` 已切到 `animation/{mod,binary,channel,clip,graph,reference,sequence,skeleton,state_machine}.rs`，`zircon_runtime/src/asset/assets/scene.rs` 已切到 `scene/{mod,animation,asset,camera,defaults,entity,extensions,lighting,management,mesh,physics,post_process,transform}.rs`，animation 与 scene 资产 payload 不再贡献大文件热点；当前 38-hotspot 状态已由后续 `project_io` folder split 进一步固定，`zircon_runtime/src/graphics/scene/scene_renderer/mesh/build_mesh_draws/build/build.rs` 仍为 1045 行热点。同步 `runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit`、`runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts`、本计划、runtime index、`large-file-ownership-m1.md`、`hotspot_inventory.md`、dynamic-session/ECS docs、M0 review 与 runtime-interface convergence 到 `large_file_hotspot_count = 38`，验证仍为静态结构证据；Cargo/profiling/FPS gates pending。 |
| M1 | 1.3 owner-budget 38-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 38`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`runtime-framework-render=2`、`runtime-other=14`、`risks = []`；本切片只同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、dynamic-session/ECS docs、M0 review、runtime-interface convergence 与 `performance_hotspots.rs` 守卫，不改 render/UI/runtime 生产代码。验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38、standalone `performance_hotspots.rs` 6/6、旧 41 当前值镜像文档扫描、conflict/trailing scans 与 scoped `git diff --check` 通过（仅 LF-to-CRLF warnings）；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 animation asset 大文件拆分 | folder_split_static_passed_cargo_pending | 2026-06-14 | `zircon_runtime/src/asset/assets/animation.rs` 拆为 folder-backed `animation/{mod,binary,channel,clip,graph,reference,sequence,skeleton,state_machine}.rs`：`mod.rs` 只保留声明/导出，binary envelope、channel value、clip/event、sequence binding、graph node、state-machine DTO 与 direct-reference owner 分离；`rustfmt --edition 2021 --check` 覆盖新文件通过。当前 owner-budget 数值已由后续 scene split 与 render 文件行数漂移推进到 38 hotspots / runtime-other=14 / unclassified 0。Cargo/profiling/FPS 仍待 active lanes 清空后补跑。 |
| M1 | 1.3 scene asset 大文件拆分 | folder_split_static_passed_cargo_pending | 2026-06-14 | `zircon_runtime/src/asset/assets/scene.rs` 拆为 folder-backed `scene/{mod,animation,asset,camera,defaults,entity,extensions,lighting,management,mesh,physics,post_process,transform}.rs`：`mod.rs` 48 行，只保留声明/导出与 `SceneMobilityAsset`；当前最大 owner 文件 `management.rs` 432 行；`rustfmt --edition 2021 --check` 覆盖新 scene owner 文件通过；结构扫描未发现 `compat` / `shim` / `legacy` / conflict markers；聚合审计当前报告 `large_file_hotspot_count = 38`、`runtime-framework-render = 2`、`runtime-other = 14`、`large_file_unclassified_hotspot_count = 0`、`performance_hotpath_boundary.risks = []`。聚焦 `cargo test -p zircon_runtime --lib scene_asset --locked --jobs 1` 使用独立 target dir 尝试后 120s 超时，未执行到通过结论；Cargo/profiling/FPS 仍待 active lanes 清空后补跑。 |
| M1 | 1.3 scene asset 拆分后漂移修复 | split_drift_static_passed_cargo_deferred_active_lanes | 2026-06-14 | 修复 scene asset folder split 后的最低层漂移：删除 `zircon_runtime/src/asset/assets/scene/physics.rs` 中遗留的私有 `SceneMobilityAsset` 副本，保持 `zircon_runtime/src/asset/assets/scene/mod.rs` 为唯一 enum owner，并保留旧 `scene.rs` 中无 `serde(rename_all)` 的公开序列化口径；静态扫描确认 `pub enum SceneMobilityAsset` 只剩 `scene/mod.rs`，`SceneSpotLightAsset` 仍经 `lighting.rs` -> `scene/mod.rs` -> `asset/assets/mod.rs` -> `asset/mod.rs` 公开；旧单文件 `scene.rs` 与当前 folder-backed scene 树的公开 type/field 差集为空，唯一新增实现为 `SceneSpotLightAsset` 的 `Default` impl；`rustfmt --edition 2021 --check` 覆盖所有 scene asset owner 文件通过。当前存在外部 `taa_resolve` / `core-min` 等 Cargo/rustc 通道，本切片不启动包级 Cargo，`scene_asset` / Runtime 07 Cargo gates 继续 pending。 |
| M1 | 1.3 scene asset 拆分公开面守卫 | folder_split_guard_static_passed_cargo_deferred_active_lanes | 2026-06-14 | 新增 `runtime_absorption::performance_hotspots::runtime_07_scene_asset_folder_split_keeps_public_surface_and_single_owner`，锁定 `scene/{mod,animation,asset,camera,defaults,entity,extensions,lighting,management,mesh,physics,post_process,transform}.rs` 的 folder-backed 声明、`SceneMobilityAsset` 只能由 `scene/mod.rs` 持有、`physics.rs` 不得重引入副本、`SceneSpotLightAsset` 的 `direction` / `outer_angle_radians` 字段与 `lighting.rs` -> `scene/mod.rs` -> `asset/assets/mod.rs` -> `asset/mod.rs` 导出链必须保留，并要求 Runtime 07、runtime index、hotspot inventory 与 scene module doc 保留 split-drift 状态锚点；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\performance_hotspots.rs` 通过，standalone `rustc --edition 2021 --test ...performance_hotspots.rs` 通过 6/6。当前仍不启动包级 Cargo，`scene_asset` / Runtime 07 Cargo gates 继续 pending。 |
| M1 | 1.3 scene asset 守卫纳入结构审计 | boundary_guard_anchor_static_passed_cargo_deferred_active_lanes | 2026-06-14 | `performance_hotpath_boundary.py` 的 `HOTSPOT_GUARD_ANCHORS` 已纳入 `runtime_07_scene_asset_folder_split_keeps_public_surface_and_single_owner`，Runtime 07 镜像事实同步为 `hotspot_guard_anchor_count = 20`、`missing_hotspot_guard_anchors = []`、`risks = []`；Runtime 07、runtime index、hotspot inventory、dynamic-session/ECS docs、M0 review 与 runtime-interface convergence 均同步 20-anchor 口径。验证：`python -m py_compile ...performance_hotpath_boundary.py` 通过；direct `performance_hotpath_boundary_audit` 返回 risks=0 / hotspot_guard_anchor_count=20；`rustfmt --edition 2021 --check zircon_runtime\src\tests\runtime_absorption\performance_hotspots.rs` 通过；standalone `rustc --edition 2021 --test ...performance_hotspots.rs` 通过 6/6。包级 Runtime 07 Cargo gates 仍待 active lanes 清空。 |
| M1 | 1.3 project_io 大文件拆分 | folder_split_static_passed_cargo_deferred_active_lanes | 2026-06-14 | `zircon_runtime/src/scene/world/project_io.rs` 的资源引用、脚本绑定、Transform、Camera、PostProcess 与 Physics shape 转换 helper 已拆入 `project_io/{camera,physics,post_process,references,script,transform}.rs`，入口文件保留 `World` 项目 I/O 编排并降到 772 行；新增 `docs/zircon_runtime/scene/world/project_io.md` 与 `runtime_07_project_io_folder_split_keeps_entry_and_converter_owners`，`performance_hotpath_boundary` 现报告 `large_file_hotspot_count = 38`、`runtime-other=14`、`hotspot_guard_anchor_count = 20`、`risks = []`。验证：`rustfmt --edition 2021 --check` 覆盖 project_io 入口与 6 个子模块、`python -m py_compile ...performance_hotpath_boundary.py`、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked` 尝试 240s 超时，期间外部 editor/render Cargo lanes 仍在编译，未声明包级通过。 |
| M1 | 1.3 owner-budget 37-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | Runtime 13 Gameplay Host Owner Split 后 `zircon_runtime/src/script/vm/gameplay_host.rs` 降到 371 行，`large_file_ownership_gate` 当前报告 `large_file_hotspot_count = 37`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`runtime-framework-render=2`、`runtime-other=13`、`missing_large_file_owner_classes = []` 与 `risks = []`；`performance_hotpath_boundary` 当前审计口径已扩展为 `expected_source_file_count = 24`、`expected_test_file_count = 6`、`frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 10`、`asset_worker_anchor_count = 11`、`hotspot_guard_anchor_count = 20`、`test_anchor_count = 18`、`doc_anchor_count = 18`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = migration-debt-present`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`。本切片同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫；验证：Python py_compile 通过；rustfmt check 通过；direct `performance_hotpath_boundary_audit` risks=0 / hotspots=37；direct `runtime_plan_status_boundary_audit` risks=0 / Runtime 07 owner-budget anchors index 6/6、guard 5/5、docs 3/3；standalone `performance_hotspots.rs` 6/6；standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates 仍 pending。 |
| M1 | 1.3 owner-budget 38-hotspot 回漂同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | 当前活跃 editor/export retained-host 工作使 `zircon_editor/src/ui/retained_host/app/build_export_actions.rs` 回到 1000 行阈值以上，`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 38`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=2`、`runtime-other=13`、`support-hub=3`、`missing_large_file_owner_classes = []` 与 `risks = []`；`performance_hotpath_boundary` 当前审计口径已扩展为 `expected_source_file_count = 24`、`expected_test_file_count = 6`、`frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 10`、`asset_worker_anchor_count = 11`、`hotspot_guard_anchor_count = 20`、`test_anchor_count = 18`、`doc_anchor_count = 18`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = migration-debt-present`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true`。本切片同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫；不改 active editor/UI/render/plugin 生产代码。验证：Python py_compile 通过；rustfmt check 通过；direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38；direct `runtime_plan_status_boundary_audit` risks=0 / Runtime 07 owner-budget anchors index 6/6、guard 5/5、docs 3/3；standalone `performance_hotspots.rs` 6/6；standalone `status_output_tables.rs` 2/2；focused Cargo `cargo test -p zircon_runtime --lib runtime_absorption::performance_hotspots --locked --jobs 1 ...` 300s 超时且无测试结果，残留 cargo/rustc 已停止；extract/ecs_query/performance profiling/FPS Cargo gates 仍 pending。 |
| M1 | 1.3 owner-budget 37-hotspot 再同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | 当前静态审计回到 37-hotspot owner-budget：`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 37`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=2`、`runtime-other=12`、`support-hub=3`、`missing_large_file_owner_classes = []` 与 `risks = []`；`zircon_editor/src/ui/retained_host/app/build_export_actions.rs` 不再处于 1000 行热点集，但 editor-retained-host 仍有 12 个 owner-budget 热点。同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫；不改 active editor/UI/render/plugin 生产代码。验证：rustfmt check 通过；Python py_compile 通过；direct `performance_hotpath_boundary_audit` risks=0 / hotspots=37；direct `runtime_plan_status_boundary_audit` risks=0 / Runtime 07 owner-budget anchors index 6/6、guard 5/5、docs 3/3；standalone `performance_hotspots.rs` 6/6；standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates 仍 pending。 |
| M1 | 1.3 owner-budget 39-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | 当前静态审计更新为 39-hotspot owner-budget：`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 39`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=12`、`support-hub=3`、`missing_large_file_owner_classes = []` 与 `risks = []`；当前漂移来自活跃 render/post-process owner 文件 `zircon_runtime/src/core/framework/render/post_process/stack.rs` 与 `zircon_runtime/src/core/framework/render/post_process/volume_component.rs` 进入 1000 行阈值以上。本切片只同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables.rs` 与 `performance_hotspots.rs` 守卫；不改 active editor/UI/render/plugin 生产代码。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=39、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 owner-budget 42-hotspot 漂移同步 | mirror_docs_static_passed_cargo_pending | 2026-06-17 | 当前静态审计更新为 42-hotspot owner-budget：`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 42`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=12`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=15`、`support-hub=3`、`missing_large_file_owner_classes = []` 与 `risks = []`；当前漂移来自活跃 render/post-process/runtime owner 文件 `zircon_runtime/src/core/framework/render/post_process/stack.rs`、`zircon_runtime/src/core/framework/render/post_process/volume_component.rs`、`zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_graph_execution_resources.rs` 与 `zircon_runtime/src/graphics/scene/scene_renderer/graph_execution/render_pass_execution_context/gpu/post_process.rs` 进入 1000 行阈值以上。本切片只同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、dynamic-session/ECS/project_io docs、M0 review、runtime-interface convergence、`runtime_plan_status_boundary.py`、`status_output_tables` 与 `performance_hotspots.rs` 守卫；不改 active editor/UI/render/plugin 生产代码。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=42、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `status_output_tables.rs` 2/2；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 owner-budget current doc mirror fix | mirror_docs_static_passed_cargo_pending | 2026-06-17 | 修正当前 owner-budget 正文漂移：`docs/zircon_runtime/performance/hotspot_inventory.md`、`docs/engine-architecture/runtime-architecture-review-m0.md` 与 `docs/engine-architecture/runtime-interface-convergence.md` 的 current large-file gate 均同步为 42 hotspots / 5 migration-debt owner groups / 0 unclassified hotspots；`runtime_07_large_file_owner_budget_gate_stays_in_sync_with_structure_audit` 新增逐文档 42-hotspot 当前句子断言，避免历史 39-hotspot 行误作当前证据。验证目标：rustfmt check、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=42、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` status-output guard；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 owner-budget 36-hotspot navigation split sync | mirror_docs_static_passed_cargo_pending | 2026-06-17 | Runtime 14 navigation fallback runtime owner split 后，`zircon_runtime/src/navigation/runtime.rs` 降到 299 行并从大文件热点集合移除；`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 36`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=10`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=12`、`support-hub=3` 与 `risks = []`。本切片只同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、M0 review、runtime-interface convergence、`runtime_plan_status_output_anchors.py`、status-output expected rows 与 `performance_hotspots` owner-budget 守卫；navigation 生产代码已由 Runtime 14 切片实现，Runtime 07 不接管 navigation owner。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=36、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 owner-budget 30-hotspot current audit sync | mirror_docs_static_passed_cargo_pending | 2026-06-20 | 当前静态审计更新为 30-hotspot owner-budget：`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_hotspot_count = 30`、`large_file_migration_debt_count = 5`、`large_file_owner_class_count = 5`、`large_file_unclassified_hotspot_count = 0`、`editor-retained-host=3`、`editor-ui=8`、`runtime-framework-render=3`、`runtime-other=13`、`support-hub=3` 与 `risks = []`；本切片只同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、M0 review、runtime-interface convergence、`runtime_plan_status_output_anchors.py`、status-output expected rows 与 `performance_hotspots` owner-budget 守卫，不改 active editor/UI/render/plugin 生产代码。验证目标：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=30、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| M1 | 1.3 owner-budget 0-hotspot current audit sync | mirror_docs_static_passed_cargo_deferred | 2026-07-01 | 当前静态审计更新为 0-hotspot owner-budget：`large_file_ownership_gate` 与 `performance_hotpath_boundary` 当前报告 `large_file_m1_gate_status = classified-and-clear`、`large_file_hotspot_count = 0`、`large_file_migration_debt_count = 0`、`large_file_owner_class_count = 0`、`large_file_unclassified_hotspot_count = 0`、`missing_large_file_owner_classes = []` 与 `risks = []`。本切片只同步 Runtime 07、runtime index、`large-file-ownership-m1.md`、hotspot inventory、M0 review、runtime-interface convergence 与 `performance_hotspots/owner_budget/large_file_gate.rs` 守卫；不改 runtime/render/editor/text 生产代码。验证目标：Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=0、standalone `performance_hotspots.rs` exact owner-budget guards、standalone `plan_status.rs` status-output guards；extract/ecs_query/performance profiling/FPS Cargo gates pending。 |
| 横切 | Runtime 07 owner-budget mirror-docs child-owner audit sync | runtime_15_runtime_07_owner_budget_mirror_docs_child_owner_split_static_passed_cargo_deferred | 2026-07-01 | Runtime 15 M3 结构切片已把 `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` 从 `tests/runtime_absorption/performance_hotspots/owner_budget.rs` 下沉到 `tests/runtime_absorption/performance_hotspots/owner_budget/mirror_docs.rs`。Runtime 07 当前 audit input 增加 `performance_hotspots/owner_budget/{large_file_gate,mirror_docs,virtual_geometry_debug_snapshot}.rs`，`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 14`；`performance_hotpath_boundary` 当前还报告 `frame_span_anchor_count = 9`、`query_counter_anchor_count = 32`、`change_counter_anchor_count = 13`、`extract_counter_anchor_count = 21`、`asset_worker_anchor_count = 13`、`animation_scene_anchor_count = 19`、`profile_counter_hotspot_anchor_count = 8`、`hotspot_guard_anchor_count = 32`、`test_anchor_count = 29`、`doc_anchor_count = 35`、`cargo_gate_anchor_count = 5`、`stale_hotspot_placeholder_present = false`、`large_file_m1_gate_status = classified-and-clear`、`large_file_hotspot_count = 0`、`large_file_migration_debt_count = 0`、`large_file_owner_class_count = 0`、`large_file_unclassified_hotspot_count = 0`、`missing_large_file_owner_classes = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`mirror_docs_guard_present = true` 与 `risks = []`。本记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| 横切 | Runtime 15 M3 Runtime 07 submit-context guard child-owner split | runtime_15_runtime_07_submit_context_guard_child_owner_split_static_passed_cargo_deferred | 2026-07-05 | Runtime 15 M3 结构切片已把 `tests/runtime_absorption/performance_hotspots/submit_context.rs` 收束为 route owner，并把 source 读取、source-extract payload、camera-loop、feedback sideband、status-doc 与 split-layout 守卫拆入 `performance_hotspots/submit_context/{sources,source_extract_payloads,camera_loop_sharing,feedback_sidebands,status_docs,split_layout}.rs`。`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 20`；新增 `runtime_15_runtime_07_submit_context_guard_child_owner_split` 锁定二级 child owner、moved assertion block 不回流、Runtime 15/status-output 状态锚和 Runtime 07 mirror docs。该记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| 横切 | Runtime 15 M3 Runtime 07 hotspot-inventory guard child-owner split | runtime_15_runtime_07_hotspot_inventory_guard_child_owner_split_static_passed_cargo_deferred | 2026-07-05 | Runtime 15 M3 结构切片已把 `tests/runtime_absorption/performance_hotspots/hotspot_inventory.rs` 收束为 route owner，并把 source 读取、evidence gate docs、ECS/extract/asset/animation counter、profiling/trace/render diversion 与 split-layout 守卫拆入 `performance_hotspots/hotspot_inventory/{sources,evidence_gate_docs,ecs_extract_counters,profiling_trace_render,split_layout}.rs`。`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 25`；新增 `runtime_15_runtime_07_hotspot_inventory_guard_child_owner_split` 锁定二级 child owner、moved assertion block 不回流、Runtime 15/status-output 状态锚和 Runtime 07 mirror docs。该记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| 横切 | Runtime 15 M3 Runtime 07 owner-budget guard folder-backed split | runtime_15_runtime_07_owner_budget_guard_folder_backed_static_passed_cargo_deferred | 2026-07-06 | Runtime 15 M3 结构切片已把 `tests/runtime_absorption/performance_hotspots/owner_budget.rs` 收束为 route/test-entry owner，并把 parent route、child route、source inventory、line budget、status-doc 与 split-layout 守卫拆入 `performance_hotspots/owner_budget/{sources,parent_routes,child_routes,source_inventory,line_budgets,status_docs,split_layout}.rs`，其中 parent-route 精确锚点为 `performance_hotspots/owner_budget/parent_routes.rs`。`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 32`；新增 `runtime_15_runtime_07_owner_budget_guard_folder_backed_split` 锁定二级 folder-backed owner、moved assertion block 不回流、Runtime 15/status-output 状态锚和 Runtime 07 mirror docs。该记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| 横切 | Runtime 15 M3 Runtime 07 artifact/render diagnostics guard child-owner split | runtime_15_runtime_07_artifact_render_diagnostics_guard_child_owner_split_static_passed_cargo_deferred | 2026-07-06 | Runtime 15 M3 结构切片已把 `tests/runtime_absorption/performance_hotspots/artifact_render_diagnostics_splits.rs` 收束为 route owner，并把 artifact cache payload、render product diagnostics 与 split-layout 守卫拆入 `performance_hotspots/artifact_render_diagnostics_splits/{artifact_cache_payload,render_product_diagnostics,split_layout}.rs`，其中精确子文件锚点为 `performance_hotspots/artifact_render_diagnostics_splits/artifact_cache_payload.rs`。`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 35`；新增 `runtime_15_runtime_07_artifact_render_diagnostics_guard_child_owner_split` 锁定二级 child owner、moved assertion block 不回流、Runtime 15/status-output 状态锚和 Runtime 07 mirror docs。该记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| 横切 | Runtime 15 M3 Runtime 07 scene/project guard child-owner split | runtime_15_runtime_07_scene_project_guard_child_owner_split_static_passed_cargo_deferred | 2026-07-06 | Runtime 15 M3 结构切片已把 `tests/runtime_absorption/performance_hotspots/scene_project_splits.rs` 收束为 route owner，并把 scene asset、project I/O、dynamic session event 与 split-layout 守卫拆入 `performance_hotspots/scene_project_splits/{scene_asset,project_io,dynamic_session_event,split_layout}.rs`，其中精确子文件锚点为 `performance_hotspots/scene_project_splits/scene_asset.rs`。`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 39`；新增 `runtime_15_runtime_07_scene_project_guard_child_owner_split` 锁定二级 child owner、moved assertion block 不回流、Runtime 15/status-output 状态锚和 Runtime 07 mirror docs。该记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| 横切 | Runtime 15 M3 Runtime 07 submit-context split-layout guard folder-backed split | runtime_15_runtime_07_submit_context_split_layout_guard_folder_backed_static_passed_cargo_deferred | 2026-07-06 | Runtime 15 M3 结构切片已把 `tests/runtime_absorption/performance_hotspots/submit_context/split_layout.rs` 收束为 route owner，并把 route/layout、source inventory、shared source loading 与 status-doc 守卫拆入 `performance_hotspots/submit_context/split_layout/{route,source_inventory,sources,status_docs}.rs`。`runtime_15_runtime_07_submit_context_guard_child_owner_split` 保留为历史 wrapper；新增 `runtime_15_runtime_07_submit_context_split_layout_guard_folder_backed_split` 锁定二级 child owner、moved assertion block 不回流、Runtime 15/status-output 状态锚和 Runtime 07 mirror docs。`performance_hotpath_source_inventory.py` 当前报告 `expected_source_file_count = 46`、`expected_test_file_count = 61`。该记录只同步测试守卫组织和镜像计数，不关闭 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| M1 | 1.3 artifact cache payload owner split | artifact_cache_payload_owner_split_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/asset/artifact/cache_payload.rs` 只保留 `ArtifactCacheAsset` 派发、贴图/材质/Shader/Prefab/Physics cache conversion entry，JSON canonical value、Mesh wire payload 与 TOML value/table conversion 已拆入 `cache_payload/{json_value,mesh,toml_value}.rs`；`cache_payload.rs` 当前 641 行，三个子 owner 分别为 95/141/75 行，避免把 bincode cache wire 细节继续堆入 dispatcher。新增 `runtime_07_artifact_cache_payload_owner_split_keeps_wire_types_folder_backed` 锁定根文件不重新拥有 `ArtifactCacheJsonValue` / `ArtifactCacheMeshAsset` / `ArtifactCacheTomlValue`，`asset_pipeline_boundary` 当前同步为 `expected_source_file_count = 22`、`missing_artifact_cache_anchors = []`、`risks = []`，`performance_hotpath_boundary` 当前同步为 `hotspot_guard_anchor_count = 24`、`large_file_hotspot_count = 41`、`runtime-other = 16`、`risks = []`。验证：rustfmt check、Python py_compile、direct asset/performance boundary audits 与 standalone guards；包级 `asset::` / `worker_pool` / Runtime 07 Cargo gates 仍按用户要求后续补跑。 |
| M1 | 1.3 render product diagnostics owner split | render_product_diagnostics_owner_split_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/core/runtime/diagnostics/render_stats_store/product.rs` 只保留 render product family 调度与现有诊断回归测试，camera/visibility/HZB/light-grid/effect-stack/material/light/mesh-queue/GPUScene/sprite/UI 投影拆入 `render_stats_store/product/{camera,visibility,hzb,light_grid,effect_stack,material,light,mesh_queue,gpu_scene,sprite,ui}.rs`，状态表短锚为 `render_stats_store/product/{camera,mesh_queue,gpu_scene}.rs`；根文件当前 786 行，子 owner 最高 415 行。新增 `runtime_07_render_product_diagnostics_owner_split_keeps_families_folder_backed` 锁定根文件不重新拥有各产品族 `record_*` 行为，`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 38`、`hotspot_guard_anchor_count = 25`、`doc_anchor_count = 29`、`large_file_hotspot_count = 39`、`runtime-other = 15`、`risks = []`，large-file gate 当前为 hotspots 39, debt groups 5, owner classes 5, unclassified hotspots 0。验证先走 rustfmt/Python/direct audit/standalone guard；包级 `extract` / `ecs_query` / profiling / FPS Cargo gates 仍按用户要求后续补跑。 |
| M2 | 2.1 extract rebuild cache | extract_rebuild_cache_static_passed_cargo_deferred | 2026-06-17 | `RuntimeDynamicSession` 新增 runtime-owned `RuntimeFrameExtractCache`，缓存键为 `change_tick` / `query_cache_revision` / `active_camera` / `viewport_size`；`current_extract(&mut self)` 在未变更 headless capture 上复用上一帧 `RenderFrameExtract`，`extract.rebuild_clones` 从首帧 `1` 收紧为第二帧 `0`，`resize_viewport(...)` 和场景变更路径会失效缓存。`frame_extract_rebuild_skips_unchanged_entities` 现在锁定 `[1,0]`，新增 `frame_extract_rebuilds_after_scene_change` 锁定场景 transform 变更后 `[1,1]`；该切片首次将 `performance_hotpath_boundary` 同步为 `expected_source_file_count = 25`、`extract_counter_anchor_count = 17`、`test_anchor_count = 22`、`doc_anchor_count = 24`，并在 hit/miss 诊断落地后复验到当前静态镜像 `expected_source_file_count = 25`、`extract_counter_anchor_count = 21`、`test_anchor_count = 26`、`doc_anchor_count = 27`、missing anchors 空、`risks = []`。验证通过：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit`、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32；包级 `extract` / FPS / profiling Cargo gate 仍按用户要求后续补跑。 |
| M2 | 2.1 extract cache hit/miss diagnostics | extract_cache_hit_miss_static_passed_cargo_deferred | 2026-06-17 | `RuntimeFrameExtractStats` 新增 `EXTRACT_CACHE_HITS_DIAGNOSTIC` / `EXTRACT_CACHE_MISSES_DIAGNOSTIC` 与 `extract.cache_hits` / `extract.cache_misses` 诊断：首次 headless capture 记录 hits/misses `[0,1]`，未变更后续 capture 记录 `[1,0]`，场景 transform 变更后记录 hits `[0,0]` / misses `[1,1]`；`headless_session_capture_records_frame_extract_diagnostics`、`frame_extract_rebuild_skips_unchanged_entities` 与 `frame_extract_rebuilds_after_scene_change` 已同步断言。`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 25`、`extract_counter_anchor_count = 21`、`test_anchor_count = 26`、`doc_anchor_count = 27`、missing anchors 空、`risks = []`；验证通过：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit`、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32；包级 `extract` / FPS / profiling Cargo gate 仍按用户要求后续补跑。 |
| M1 | 1.1 asset worker frame sampler | asset_worker_frame_sampler_static_passed_cargo_deferred | 2026-06-17 | `AssetWorkerPoolFrameSampler` / `AssetWorkerPoolFrameDiagnostics` 已在 asset worker owner 内提供帧级采样游标，从累计 `completed` / `failed` 计算 `completed_delta` / `failed_delta`，写入 `asset.worker.frame_completed` 与 `asset.worker.frame_failed`，并同帧保留 `in_flight` / `budgeted_threads` 读数。新增 `worker_pool_frame_sampler_records_per_frame_completion_deltas` 覆盖重复请求合并完成、失败完成与下一帧零增量；`asset_pipeline_boundary` 同步为 `worker_diagnostic_count = 7`、`expected_worker_diagnostic_count = 7`、`test_anchor_count = 24`、`behavior_test_anchor_count = 20`；`performance_hotpath_boundary` 同步为 `expected_source_file_count = 26`、`asset_worker_anchor_count = 13`、`doc_anchor_count = 27`、missing anchors 空、`risks = []`。验证：rustfmt check、Python py_compile、direct asset/performance boundary audits、standalone `asset_pipeline.rs` 1/1、standalone `performance_hotspots.rs` 6/6、standalone `plan_status.rs` 32/32 通过；focused Cargo 240s timeout no result，残留 cargo/rustc/rustdoc scan 为空。该切片只补足 asset worker per-frame cost 取证入口，不宣称 M2 优化已开始；包级 `worker_pool` / `extract` / FPS / profiling Cargo gate 后续补跑。 |
| M1 | 1.1 asset worker manager sampler entry | asset_worker_manager_sampler_static_passed_cargo_deferred | 2026-06-17 | Runtime 07 frame owner 现在可通过 `ProjectAssetManager::spawn_worker_pool_with_frame_sampler(...)` 获取与 worker pool 同源初始化的 `AssetWorkerPoolFrameSampler`，避免从外层直接推断采样基线。`performance_hotpath_boundary` 已把 `zircon_runtime/src/asset/pipeline/manager/project_asset_manager/construction.rs` 纳入审计，当前 `expected_source_file_count = 26`、`asset_worker_anchor_count = 13`、`missing_asset_worker_anchors = []`、`risks = []`；Runtime 04 行为锚同步到 `project_asset_manager_spawns_worker_pool_with_frame_sampler`。验证先按用户要求走静态/轻量 lane；`worker_pool` / `extract` / FPS / profiling Cargo gates 后续补跑。 |
| M1 | 1.1 animation scene frame diagnostics | animation_scene_frame_diagnostics_static_passed_cargo_deferred | 2026-06-17 | `AnimationSceneFrameDiagnostics` 已在 `animation/scene_hook/diagnostics.rs` 记录 `animation.scene.scanned_entities`、sequence/clip/graph/state-machine sample counts、`animation.scene.output_poses`、`animation.scene.applied_transforms`、`animation.scene.published_events` 与 `animation.scene.state_transitions`；`tick_animation_world(...)` 在无 animation manager、playback disabled、缺 asset manager 与正常采样路径均写入同一帧 count rows，`publish_events(...)` 与 `apply_pose_transforms_to_scene_nodes(...)` 返回实际事件/transform 应用数量。`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 44`、`animation_scene_anchor_count = 19`、`hotspot_guard_anchor_count = 29`、`doc_anchor_count = 33`、missing anchors 空、`risks = []`。验证已通过 rustfmt/Python/direct audit/standalone hotspot 与 plan-status guards；短时 core-min Cargo check 244s 超时且无诊断输出，包级 `animation` / Runtime 07 Cargo gates 仍按用户要求后续补跑。该切片只补足 animation scene hook per-frame cost 取证入口，不宣称 M2 优化已开始。 |
| M1 | 1.3 profile counter hotspot export | profiling_counter_hotspot_export_static_passed_cargo_deferred | 2026-06-20 | Runtime profiling export 现在额外生成 `counter_hotspots.json`：`zircon_runtime_interface::profiling` 新增 `PROFILE_COUNTER_HOTSPOTS_FILE`、`CounterHotspotReport`、`CounterHotspotEntry` 与 `ProfileControlResponse.counter_hotspot_report`，`zircon_runtime::core::runtime::diagnostics::profiling::counter_hotspot::analyze_counter_hotspots` 聚合有限正值 `ProfileCounterSnapshot`，按 `stream/name/path` 输出 total/avg/p95/max/latest/count/frame_count；`export_report` 同步写出 artifact、在 `summary.md` 增加 `Counter Hotspots`，并把 counter candidates 接在 UI alerts 与 span hotspots 之后。`performance_hotpath_boundary` 同步为 `expected_source_file_count = 46`、`profile_counter_hotspot_anchor_count = 8`、`hotspot_guard_anchor_count = 32`、`test_anchor_count = 29`、`doc_anchor_count = 35`；该切片只让 `extract.*`、`ecs.*`、`asset.worker.*`、`animation.scene.*`、`time.*`、`schedule.*`、`tasks.*` 等现有 counters 可进入 profiling 证据，不声明权威 top list 或 M2 优化完成。验证：rustfmt check 通过；Python py_compile 通过；direct `performance_hotpath_boundary_audit` risks=[] / missing anchors empty；standalone `performance_hotspots.rs` 9/9；standalone `plan_status.rs` 32/32；`cargo check -p zircon_runtime_interface --locked` 通过；scoped `git diff --check` 仅 LF/CRLF warnings。`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked` 180s timeout no result，残留 cargo/rustc 已停止；包级 Cargo/profiling/FPS gates 按用户要求后续补跑。 |
| M1 | 1.1 QueryState cache owner performance audit sync | query_state_cache_owner_perf_audit_sync_static_passed_cargo_pending | 2026-06-17 | `performance_hotpath_boundary` 已把 `zircon_runtime/src/scene/ecs/query/query_state/cache.rs` 纳入 Runtime 07 QueryState telemetry 扫描，修复 cache hit/miss/rebuild 增量锚点随 QueryState cache owner split 迁移后的审计漂移；当前 direct audit 报告 `expected_source_file_count = 45`、`query_counter_anchor_count = 32`、`missing_query_counter_anchors = []`、`missing_doc_anchors = []`、`risks = []`。`runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts`、Runtime 07 当前镜像正文、runtime index、hotspot inventory、dynamic-session/ECS docs、M0 review 与 runtime-interface convergence 已同步到 45-source 口径；包级 `ecs_query` / Runtime 07 Cargo gates 仍按用户实现优先策略后续补跑。 |
| M1 | 1.3 virtual geometry debug snapshot owner split | virtual_geometry_debug_snapshot_owner_split_static_passed_cargo_deferred | 2026-06-17 | `zircon_runtime/src/core/framework/render/virtual_geometry_debug_snapshot.rs` 降为 51 行 structural facade，BVH visualization、CPU reference、cull-input packing、execution DTO、node/cluster cull worklists、top-level snapshot 与 provenance enums 已拆入 `virtual_geometry_debug_snapshot/{bvh_visualization,cpu_reference,cull_input,execution,node_and_cluster_cull,snapshot,sources}.rs`，状态表短锚为 `virtual_geometry_debug_snapshot/{cull_input,node_and_cluster_cull,snapshot}.rs`，最大子 owner `node_and_cluster_cull.rs` 为 543 行；`runtime_absorption::performance_hotspots` 拆出 `performance_hotspots/owner_budget.rs` 并新增 `runtime_07_virtual_geometry_debug_snapshot_owner_split_keeps_contracts_folder_backed`，防止 DTO/pack/unpack/worklist 声明回流根文件。`performance_hotpath_boundary` 当前同步为 `expected_source_file_count = 45`、`expected_test_file_count = 6`、`hotspot_guard_anchor_count = 29`、`large_file_hotspot_count = 38`、`runtime-framework-render = 3`、`runtime-other = 14`、`risks = []`；包级 `extract` / `ecs_query` / profiling / FPS Cargo gates 按用户实现优先策略后续补跑。 |
| M2 | （后续按清单实例化） | 待证据 | — | QueryState、asset per-frame cost、UI 系统成本与更广义 animation runtime 成本仍需权威 runtime 数值后再开独立 M2 切片；animation scene hook 现已有 count rows，但没有 FPS/trace 排名。 |

基线数值（取证期填写）：

- 权威 FPS：__（命令 ×2，偏差 __%）；2026-06-17 已完成 `ZR_VM_RUST_BINDING_LIB_DIR` / DLL 路径探测并修复 lib-test 进入 FPS gate 前的三处支持层编译阻塞，但随后同命令 904s 超时无测试结果、未产生 `vampire_runtime_perf` 输出，残留的本次 cargo/rustc 进程已停止。
- 帧分解：update __% / extract __% / submit __%
- `zr_profile` span 使用点数：M0.3 已非 0；当前生产 anchors 覆盖 dynamic session update/extract、runtime bridge submit/present、SceneScheduleRunner stage、render framework submit/present/capture 与 direct runtime-frame submit 的 build/prepare/render/feedback 分段。2026-06-22 direct runtime-frame `profiling-chrome` trace/export 已新增 `direct_runtime_frame_submit_exports_perfetto_trace_artifacts` 与静态守卫；cargo 聚焦验证仍因当前工作树编译/超时窗口待解阻。权威 vampire FPS 数值和 profiling-tracy 构建耗时仍待 profiling/FPS gate。
- real-backend 编译超时旁证（2026-06-12，06 计划状态节）：`vampire_project_session_starts_paused_until_start_button_click --features zr-vm-real-backend` 300s 编译超时——切片 0.2 的破解结论同时解锁 06 的 real-backend 回归
- 热点清单 top3：待权威 runtime 数值；当前只保留有计数证据的候选清单，见 `docs/zircon_runtime/performance/hotspot_inventory.md`，无权威 runtime 数值不得进入 M2。
- Runtime 07 owner-budgeted optimization gate：`performance_hotpath_boundary` 复用 `large_file_ownership_gate`，当前 1000 行阈值下 large-file gate 为 `classified-and-clear`，hotspots 0, debt groups 0, owner classes 0, unclassified hotspots 0。Current mirror anchors: `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, and `risks = []`。M2 优化若触及未来重新出现的大文件，必须先按 owner class 拆分或取得对应活跃 owner handoff；不得把性能改动继续堆进同一个热点文件。
- ECS query cache telemetry baseline：`cache_hits` / `cache_misses` / `cache_rebuilds` / `candidate_entity_count` / `matched_entity_count` 已有本地 `QueryStateCacheStats`，并提供 `DiagnosticStore` 投影路径且聚焦测试通过；change detection baseline 已有 `ChangeDetectionScanStats.scanned_marks` / `added_matches` / `changed_matches` 投影路径且聚焦测试通过；`EcsFramePerformanceDiagnostics` 已补 ECS 帧级聚合口，可把多条 QueryState 与 change-detection 样本合并到同一个 frame index；extract baseline 已接入 `RuntimeFrameExtractCache` 与 `extract.rebuild_clones` / `extract.output_bytes` / `extract.cache_hits` / `extract.cache_misses` 自动诊断，未变更 headless capture 的 rebuild 从 `[1,1]` 收紧为 `[1,0]`，cache hits/misses 分别记录 `[0,1]` / `[1,0]`，场景 transform 变更仍记录 rebuild `[1,1]` 与 cache miss `[1,1]`；animation scene hook baseline 已接入 `AnimationSceneFrameDiagnostics` 与 `animation.scene.scanned_entities` / `animation.scene.output_poses` / `animation.scene.applied_transforms` / `animation.scene.published_events` / `animation.scene.state_transitions` 自动诊断；profiling export baseline 已接入 `CounterHotspotReport` / `counter_hotspots.json` / `ProfileControlResponse.counter_hotspot_report`，可把现有 runtime counters 排序为证据入口但不替代权威 top list；M1.2/M2 命名断言已源码/格式静态通过，Cargo rerun 与 vampire 场景非零/稳定输出待后续验证窗口补跑
- 10fps 会话已落修复锚（不得回退）：model uniform 缓存、`ensure_material` revision 复用、禁用 postprocess 跳过 executor——锚定测试见"现状与证据"节三个范本

- 迁入记录：[runtime/index.md 迁出记录](2026-07-09-runtime-index-output-records.md)
## 风险与协调

- **硬性前置**：执行任何切片前重读 `.codex/sessions/20260611-0416-rendering-10fps-analysis.md` 最新状态——该会话仍活跃，worktree 改动是 live state，只做聚焦编辑，禁止回退。
- M0 依赖子计划 06 M1（ZrVM 修复）：若上游修复周期长，先用 fallback 诊断工程取"非权威"基线并显式标注局限（无 ZrVM 脚本路径），fallback 命令：去掉 `--features zr-vm-real-backend` 的同名测试（若 fallback 后端支持）或现有 `runtime_diagnostics` 测试族。
- 与 render 计划边界：M1 计数若把热点指向 draw 提交侧（231 次 pre-draw buffer copy 属此类），移交 render 计划 02（MeshDrawCommand/上载合并），不在本计划处理。
- profiling 构建可能与共享 `CARGO_TARGET_DIR` 的并行构建冲突（CLAUDE.md 磁盘政策：禁止并行重型构建）；M0 切片 0.2 安排在无其他构建窗口执行。
- 计数点铺在 `scene/ecs/query/query_state/` 与 `change_detection/`——若 10fps 会话 worktree 已触及这些文件，先对齐再动。

## 2026-07-06 Runtime 15 M3 Runtime 07 hotspot-inventory ECS/extract counters child-owner split

`Runtime 15 M3 Runtime 07 hotspot-inventory ECS/extract counters child-owner split` / `runtime_15_runtime_07_hotspot_inventory_ecs_extract_counters_child_owner_split_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/hotspot_inventory/ecs_extract_counters.rs` as a route owner. Query/change evidence lives in `performance_hotspots/hotspot_inventory/ecs_extract_counters/query_change.rs`, extract/cache evidence lives in `performance_hotspots/hotspot_inventory/ecs_extract_counters/extract_cache.rs`, asset/animation evidence lives in `performance_hotspots/hotspot_inventory/ecs_extract_counters/asset_animation.rs`, ECS frame aggregation evidence lives in `performance_hotspots/hotspot_inventory/ecs_extract_counters/frame_diagnostics.rs`, and the split guard lives in `performance_hotspots/hotspot_inventory/ecs_extract_counters/split_layout.rs::runtime_15_runtime_07_hotspot_inventory_ecs_extract_counters_child_owner_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 44`, `missing_tests = []`, `missing_docs = []`, and `risks = []`. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget mirror-docs guard folder-backed split

`Runtime 15 M3 Runtime 07 owner-budget mirror-docs guard folder-backed split` / `runtime_15_runtime_07_owner_budget_mirror_docs_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/mirror_docs.rs` as the Runtime 07 mirror-docs route owner. Source loading, performance guard anchors, source inventory anchors, audit wiring anchors, mirror-doc assertions, and split-layout checks now live in `performance_hotspots/owner_budget/mirror_docs/{sources,performance_guard,source_inventory,audit_wiring,doc_mirrors,split_layout}.rs`, while the split guard is `runtime_15_runtime_07_owner_budget_mirror_docs_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 50`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending.

## 2026-07-06 Runtime 15 M3 Runtime 07 hotspot-inventory split-layout guard folder-backed split

`Runtime 15 M3 Runtime 07 hotspot-inventory split-layout guard folder-backed split` / `runtime_15_runtime_07_hotspot_inventory_split_layout_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/hotspot_inventory/split_layout.rs` as the split-layout route owner. Route/layout checks, source inventory checks, status-doc checks, and shared source loading now live in `performance_hotspots/hotspot_inventory/split_layout/{route,source_inventory,status_docs,sources}.rs`, while the split guard is `runtime_15_runtime_07_hotspot_inventory_split_layout_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 54`, `frame_span_anchor_count = 9`, `query_counter_anchor_count = 32`, `change_counter_anchor_count = 13`, `extract_counter_anchor_count = 21`, `asset_worker_anchor_count = 13`, `animation_scene_anchor_count = 19`, `profile_counter_hotspot_anchor_count = 8`, `hotspot_guard_anchor_count = 32`, `test_anchor_count = 29`, `doc_anchor_count = 35`, `cargo_gate_anchor_count = 5`, `stale_hotspot_placeholder_present = false`, `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget split-layout guard folder-backed split

`Runtime 15 M3 Runtime 07 owner-budget split-layout guard folder-backed split` / `runtime_15_runtime_07_owner_budget_split_layout_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/split_layout.rs` as the split-layout route owner. Route/layout checks now live in `performance_hotspots/owner_budget/split_layout/route.rs`, source inventory checks live in `performance_hotspots/owner_budget/split_layout/source_inventory.rs`, and status-doc checks live in `performance_hotspots/owner_budget/split_layout/status_docs.rs`, while the split guard is `runtime_15_runtime_07_owner_budget_split_layout_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 57`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending.

## 2026-07-06 Runtime 15 M3 Runtime 07 submit-context split-layout guard folder-backed split

`Runtime 15 M3 Runtime 07 submit-context split-layout guard folder-backed split` / `runtime_15_runtime_07_submit_context_split_layout_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/submit_context/split_layout.rs` as the split-layout route owner while preserving the historical `runtime_15_runtime_07_submit_context_guard_child_owner_split` wrapper. Route/layout checks now live in `performance_hotspots/submit_context/split_layout/route.rs`, source inventory checks live in `performance_hotspots/submit_context/split_layout/source_inventory.rs`, shared source loading lives in `performance_hotspots/submit_context/split_layout/sources.rs`, and status-doc checks live in `performance_hotspots/submit_context/split_layout/status_docs.rs`. The new split guard is `runtime_15_runtime_07_submit_context_split_layout_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 61`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending.

## 2026-07-06 Runtime 15 M3 Runtime 07 scene/project split-layout guard folder-backed split

`Runtime 15 M3 Runtime 07 scene/project split-layout guard folder-backed split` / `runtime_15_runtime_07_scene_project_split_layout_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/scene_project_splits/split_layout.rs` as the split-layout route owner while preserving the historical `runtime_15_runtime_07_scene_project_guard_child_owner_split` wrapper. Route/layout checks now live in `performance_hotspots/scene_project_splits/split_layout/route.rs`, source inventory checks live in `performance_hotspots/scene_project_splits/split_layout/source_inventory.rs`, shared source loading lives in `performance_hotspots/scene_project_splits/split_layout/sources.rs`, and status-doc checks live in `performance_hotspots/scene_project_splits/split_layout/status_docs.rs`. The new split guard is `runtime_15_runtime_07_scene_project_split_layout_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 65`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation passed: scoped rustfmt, Python py_compile, standalone performance-hotspots harness 20/20, direct `performance_hotpath_boundary_audit` 65/65, filtered plan-status `status_output_tables` 2/2, conflict/whitespace scan, and scoped `git diff --check` warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending and package Cargo is deferred because D_FREE_GB=2.49.

## 2026-07-06 Runtime 15 M3 Runtime 07 artifact/render diagnostics split-layout guard folder-backed split

`Runtime 15 M3 Runtime 07 artifact/render diagnostics split-layout guard folder-backed split` / `runtime_15_runtime_07_artifact_render_diagnostics_split_layout_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/artifact_render_diagnostics_splits/split_layout.rs` as the split-layout route owner while preserving the historical `runtime_15_runtime_07_artifact_render_diagnostics_guard_child_owner_split` wrapper. Route/layout checks now live in `performance_hotspots/artifact_render_diagnostics_splits/split_layout/route.rs`, source inventory checks live in `performance_hotspots/artifact_render_diagnostics_splits/split_layout/source_inventory.rs`, shared source loading lives in `performance_hotspots/artifact_render_diagnostics_splits/split_layout/sources.rs`, and status-doc checks live in `performance_hotspots/artifact_render_diagnostics_splits/split_layout/status_docs.rs`. The new split guard is `runtime_15_runtime_07_artifact_render_diagnostics_split_layout_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 69`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation passed: scoped rustfmt, Python py_compile, standalone performance-hotspots harness 21/21, direct `performance_hotpath_boundary_audit` 69/69, filtered plan-status `status_output_tables` 2/2, conflict/whitespace scan, and scoped `git diff --check` warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending and package Cargo is deferred because `CARGO_DEFERRAL_CHECK D_FREE_GB=2.49 E_FREE_GB=92.66 CARGO_PIDS=12928,36004,39276,47164 RUSTC_PIDS=21716,47844`.

## 2026-07-06 Runtime 15 M3 Runtime 07 hotspot-inventory ECS/extract counters split-layout guard folder-backed split

`Runtime 15 M3 Runtime 07 hotspot-inventory ECS/extract counters split-layout guard folder-backed split` / `runtime_15_runtime_07_hotspot_inventory_ecs_extract_counters_split_layout_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/hotspot_inventory/ecs_extract_counters/split_layout.rs` as the split-layout route owner while preserving the historical `runtime_15_runtime_07_hotspot_inventory_ecs_extract_counters_child_owner_split` wrapper. Route/layout checks now live in `performance_hotspots/hotspot_inventory/ecs_extract_counters/split_layout/route.rs`, source inventory checks live in `performance_hotspots/hotspot_inventory/ecs_extract_counters/split_layout/source_inventory.rs`, shared source loading lives in `performance_hotspots/hotspot_inventory/ecs_extract_counters/split_layout/sources.rs`, and status-doc checks live in `performance_hotspots/hotspot_inventory/ecs_extract_counters/split_layout/status_docs.rs`. The new split guard is `runtime_15_runtime_07_hotspot_inventory_ecs_extract_counters_split_layout_guard_folder_backed_split`. `performance_hotpath_source_inventory.py` now reports `expected_source_file_count = 46`, `expected_test_file_count = 73`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, and `risks = []`. Validation passed: scoped rustfmt, Python py_compile, standalone performance-hotspots harness 22/22, direct `performance_hotpath_boundary_audit` 73/73, filtered plan-status `status_output_tables` 2/2, conflict/whitespace scan, and scoped `git diff --check` warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending and package Cargo is deferred because `CARGO_DEFERRAL_CHECK D_FREE_GB=2.49`.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget mirror-docs sources guard folder-backed split

`Runtime 15 M3 Runtime 07 owner-budget mirror-docs sources guard folder-backed split` / `runtime_15_runtime_07_owner_budget_mirror_docs_sources_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/mirror_docs/sources.rs` as the source-loading route/type owner. Assertion helper code lives in `performance_hotspots/owner_budget/mirror_docs/sources/assertions.rs`, include-source loading lives in `performance_hotspots/owner_budget/mirror_docs/sources/load.rs`, and source collection views live in `performance_hotspots/owner_budget/mirror_docs/sources/views.rs`; the compact child anchor is `performance_hotspots/owner_budget/mirror_docs/sources/{assertions,load,views}.rs`. The new split guard is `runtime_15_runtime_07_owner_budget_mirror_docs_sources_guard_folder_backed_split`. `performance_hotpath_boundary` now reports `expected_source_file_count = 46`, `expected_test_file_count = 76`, `frame_span_anchor_count = 9`, `query_counter_anchor_count = 32`, `change_counter_anchor_count = 13`, `extract_counter_anchor_count = 21`, `asset_worker_anchor_count = 13`, `animation_scene_anchor_count = 19`, `profile_counter_hotspot_anchor_count = 8`, `hotspot_guard_anchor_count = 32`, `test_anchor_count = 29`, `doc_anchor_count = 35`, `cargo_gate_anchor_count = 5`, `stale_hotspot_placeholder_present = false`, `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, `risks = []`, and keeps `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` visible. Validation passed: scoped rustfmt, Python py_compile, standalone performance-hotspots harness 23/23, direct `performance_hotpath_boundary_audit` 76/76, filtered plan-status `status_output_tables` 2/2, conflict/whitespace scan, and scoped `git diff --check` warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending and package Cargo is deferred because `CARGO_DEFERRAL_CHECK D_FREE_GB=25.82 E_FREE_GB=93.45 CARGO_PIDS=23752,34612,53720,59308 RUSTC_PIDS=57408`.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget sources guard folder-backed split

`Runtime 15 M3 Runtime 07 owner-budget sources guard folder-backed split` / `runtime_15_runtime_07_owner_budget_sources_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/sources.rs` as the owner-budget source route/type owner while moving include-source loading into `performance_hotspots/owner_budget/sources/load.rs`. The focused split guard is `runtime_15_runtime_07_owner_budget_sources_guard_folder_backed_split`. `performance_hotpath_boundary` now reports `expected_source_file_count = 46`, `expected_test_file_count = 77`, `frame_span_anchor_count = 9`, `query_counter_anchor_count = 32`, `change_counter_anchor_count = 13`, `extract_counter_anchor_count = 21`, `asset_worker_anchor_count = 13`, `animation_scene_anchor_count = 19`, `profile_counter_hotspot_anchor_count = 8`, `hotspot_guard_anchor_count = 32`, `test_anchor_count = 29`, `doc_anchor_count = 35`, `cargo_gate_anchor_count = 5`, `stale_hotspot_placeholder_present = false`, `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, `risks = []`, and keeps `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` visible. Validation passed: scoped rustfmt, Python py_compile, standalone performance-hotspots harness 24/24, direct `performance_hotpath_boundary_audit` missing/risk empty, filtered plan-status `status_output_tables` 2/2, conflict/trailing-whitespace scan, and scoped `git diff --check` warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending and package Cargo is deferred because `CARGO_DEFERRAL_CHECK D_FREE_GB=178.03 E_FREE_GB=79.82 CARGO_PIDS=38672,52420,54260,56788 RUSTC_PIDS=36200,45220`.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget child-routes guard folder-backed split

Explicit child path anchor: `tests/runtime_absorption/performance_hotspots/owner_budget/child_routes/submit_context.rs`.

`Runtime 15 M3 Runtime 07 owner-budget child-routes guard folder-backed split` / `runtime_15_runtime_07_owner_budget_child_routes_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/child_routes.rs` as the child-routes route owner while moving submit-context, hotspot-inventory, scene/project, artifact/render diagnostics, and owner-budget mirror checks into `performance_hotspots/owner_budget/child_routes/{submit_context,hotspot_inventory,scene_project,artifact_render_diagnostics,owner_budget}.rs`. The focused split guard is `runtime_15_runtime_07_owner_budget_child_routes_guard_folder_backed_split`. `performance_hotpath_boundary` now reports `expected_source_file_count = 46`, `expected_test_file_count = 82`, `frame_span_anchor_count = 9`, `query_counter_anchor_count = 32`, `change_counter_anchor_count = 13`, `extract_counter_anchor_count = 21`, `asset_worker_anchor_count = 13`, `animation_scene_anchor_count = 19`, `profile_counter_hotspot_anchor_count = 8`, `hotspot_guard_anchor_count = 32`, `test_anchor_count = 29`, `doc_anchor_count = 35`, `cargo_gate_anchor_count = 5`, `stale_hotspot_placeholder_present = false`, `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, `risks = []`, and keeps `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` visible. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending and package Cargo is deferred.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget line-budgets guard folder-backed split

Explicit child path anchor: `tests/runtime_absorption/performance_hotspots/owner_budget/line_budgets/owner_budget.rs`.

`Runtime 15 M3 Runtime 07 owner-budget line-budgets guard folder-backed split` / `runtime_15_runtime_07_owner_budget_line_budgets_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/line_budgets.rs` as the line-budget route owner while moving root, artifact/render diagnostics, hotspot-inventory, owner-budget, scene/project, and submit-context budget checks into `performance_hotspots/owner_budget/line_budgets/{root,artifact_render_diagnostics,hotspot_inventory,owner_budget,scene_project,submit_context}.rs`. The focused split guard is `runtime_15_runtime_07_owner_budget_line_budgets_guard_folder_backed_split`. `performance_hotpath_boundary` now reports `expected_source_file_count = 46`, `expected_test_file_count = 88`, `frame_span_anchor_count = 9`, `query_counter_anchor_count = 32`, `change_counter_anchor_count = 13`, `extract_counter_anchor_count = 21`, `asset_worker_anchor_count = 13`, `animation_scene_anchor_count = 19`, `profile_counter_hotspot_anchor_count = 8`, `hotspot_guard_anchor_count = 32`, `test_anchor_count = 29`, `doc_anchor_count = 35`, `cargo_gate_anchor_count = 5`, `stale_hotspot_placeholder_present = false`, `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, `risks = []`, and keeps `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` visible. Validation result: scoped rustfmt --check passed, Python py_compile passed, standalone performance-hotspots harness passed 26/26, direct `performance_hotpath_boundary_audit` reported `expected_test_file_count=88` with missing source/test/doc/cargo-anchor lists empty and `risks=[]`, module-path-preserving standalone plan-status `status_output_tables` passed 2/2, old-82/conflict/trailing-whitespace scans were clean, and scoped git diff --check was warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending. Package Cargo remains deferred because `D_FREE_GB=177.39 E_FREE_GB=64.02 CARGO_PIDS=19280,26172,34948,36760 RUSTC_PIDS=36412,36776`; no package Cargo pass is claimed.

## 2026-07-06 Runtime 15 M3 Runtime 07 owner-budget split-layout route guard folder-backed split

Explicit child path anchor: `tests/runtime_absorption/performance_hotspots/owner_budget/split_layout/route/support_routes.rs`.

`Runtime 15 M3 Runtime 07 owner-budget split-layout route guard folder-backed split` / `runtime_15_runtime_07_owner_budget_split_layout_route_guard_folder_backed_static_passed_cargo_deferred` keeps `tests/runtime_absorption/performance_hotspots/owner_budget/split_layout/route.rs` as the split-layout route owner while moving parent route, support route, and split route assertions into `performance_hotspots/owner_budget/split_layout/route/{parent_route,split_route,support_routes}.rs`. The focused split guard is `runtime_15_runtime_07_owner_budget_split_layout_route_guard_folder_backed_split`. `performance_hotpath_boundary` now reports `expected_source_file_count = 46`, `expected_test_file_count = 91`, `frame_span_anchor_count = 9`, `query_counter_anchor_count = 32`, `change_counter_anchor_count = 13`, `extract_counter_anchor_count = 21`, `asset_worker_anchor_count = 13`, `animation_scene_anchor_count = 19`, `profile_counter_hotspot_anchor_count = 8`, `hotspot_guard_anchor_count = 32`, `test_anchor_count = 29`, `doc_anchor_count = 35`, `cargo_gate_anchor_count = 5`, `stale_hotspot_placeholder_present = false`, `large_file_m1_gate_status = classified-and-clear`, `large_file_hotspot_count = 0`, `large_file_migration_debt_count = 0`, `large_file_owner_class_count = 0`, `large_file_unclassified_hotspot_count = 0`, `missing_large_file_owner_classes = []`, `missing_tests = []`, `missing_docs = []`, `missing_doc_anchors = []`, `missing_cargo_gate_anchors = []`, `mirror_docs_guard_present = true`, `risks = []`, and keeps `runtime_07_performance_hotpath_mirror_docs_match_structure_audit_counts` visible. Validation result: scoped rustfmt --check passed, Python py_compile passed, standalone performance-hotspots harness passed 27/27, direct `performance_hotpath_boundary_audit` reported `expected_test_file_count=91` with missing source/test/doc/cargo-anchor lists empty and `risks=[]`, module-path-preserving standalone plan-status `status_output_tables` passed 2/2, old-88/conflict/trailing-whitespace scans were clean, and scoped git diff --check was warning-only. This only updates static test-owner inventory; extract/ecs_query/profiling/FPS Cargo gates remain pending. Package Cargo remains deferred because `D_FREE_GB=177.38 E_FREE_GB=56.86 CARGO_PIDS=2328,32428 RUSTC_PIDS=32452`; no package Cargo pass is claimed.
