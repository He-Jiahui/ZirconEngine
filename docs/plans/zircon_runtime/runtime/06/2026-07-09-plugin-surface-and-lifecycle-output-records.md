# 06-plugin-surface-and-lifecycle 产出记录归档

> 来源：[`06-plugin-surface-and-lifecycle.md`](../06-plugin-surface-and-lifecycle.md) 的 `## 状态与产出记录`。

## 状态与产出记录

| 里程碑 | 切片 | 状态 | 完成日期 | 证据（命令输出 / 文件 / 测试名） |
|---|---|---|---|---|
| 横切 | ZrVM backend feature plan-command hard cutover | `runtime_06_backend_zr_vm_plan_commands_hard_cutover_static_passed` | 2026-07-10 | Frameworks 03 已将 runtime 真实 ZrVM 后端 feature 硬切为 `backend-zr-vm`；Runtime 06 当前父计划的环境检查、M1 DoD、失败测试分层、超时复跑说明与真实后端验收命令全部同步到新 feature，不保留已退役 feature 指令。Runtime 06 审计同时将三条 structure guard/source owner 从 route parents 更新为 `plugin_surface_lifecycle/{lifecycle_fallback,mirror_docs,native_loader_namespace}.rs`、把 Cargo gate status anchor 读取定位到 `cargo_gates/early/runtime_06.rs`，并同步 `last_refined=2026-07-10`。Python regression 3/3；direct plugin lifecycle audit 的 missing source/doc/Cargo anchors 与 `risks` 均为空；重新编译 standalone plan-status harness 后 48/48。该切片不把历史归档证据改写为新命令，也不关闭真实后端/package Cargo gate。 |
| 横切 | Runtime M0 plugin runtime gaps Markdown renderer split | plugin_runtime_gaps_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `plugin_runtime_gaps_markdown_split_static_passed_cargo_deferred_tests_deferred`；`plugin_runtime_gaps_markdown.py` now owns `render_plugin_runtime_gaps_markdown(...)`, while `plugin_runtime_gaps.py` remains the 77-line runtime plugin registration/resolve/context gap scanner. Direct probe reports `zircon_crate_count=6`, production Rust file count 7583, `plugin_gap_count=0`, rendered output 2 lines, and Markdown owner 11 lines. Validation: Python py_compile, direct plugin runtime gap/renderer probe, and standalone `plan_status.rs` 33/33; broader runtime Cargo gates remain deferred. |
| M1 | 1.1 空参数修复 | 代码完成，runtime Cargo 待验证 | 2026-06-12 | 修复最低共享层 `../zr_vm/zr_vm_rust_binding/rust/zr_vm_rust_binding/src/lib.rs`：空参数导出调用传合法 sentinel pointer + len=0；`ZrVmPluginInstance` 保持 `call_entry_lifecycle_export(..., &[])` 正常路径；binding `call_module_export_accepts_empty_argument_slice` 通过 |
| M1 | 1.2 失败路径测试 | code_static_passed_real_backend_pending | 2026-06-16 | 新增 folder-backed fallback 层测试 owner `zircon_runtime/src/script/vm/tests/lifecycle_failures.rs`，并在 `script/vm/tests.rs` 挂接；`vm_lifecycle_fallback_activate_bad_entry_module_surfaces_vm_error`、`vm_lifecycle_fallback_missing_optional_export_returns_none_not_error`、`vm_lifecycle_fallback_deactivate_is_idempotent_after_unload`、`vm_lifecycle_fallback_empty_arguments_do_not_require_real_backend` 覆盖坏入口模块、坏符号 optional export、deactivate 幂等、空参数 fallback 调用四类路径。新增 `runtime_absorption::plugin_surface_lifecycle::runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed`，`plugin_surface_lifecycle_boundary` 同步报告 fallback lifecycle failure tests 4/4。binding 侧 `call_module_export_accepts_empty_argument_slice` 与既有 `project_session_preserves_module_state_between_export_calls` 仍为 lower-boundary evidence；`cargo test -p zircon_runtime --lib vm_lifecycle_fallback --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-06-fallback-lifecycle-0616 -- --nocapture --test-threads=1` 已通过 5/5（四个 fallback lifecycle 测试 + `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed`）；`vampire_project_session_starts_paused_until_start_button_click --features zr-vm-real-backend` 曾 300s 编译超时，runtime real-backend Cargo remains pending。 |
| M2 | 2.1 native 收口 | code_static_passed_cargo_pending | 2026-06-16 | 采用计划选项 (b) `plugin::native` 子命名空间硬切换：新增 `zircon_runtime/src/plugin/native.rs`，`zircon_runtime/src/plugin/mod.rs` 只保留 `pub mod native;`，删除旧 `pub use native_plugin_loader::{...}` 根导出；运行时 importer、extension registry、native loader 测试、native dynamic export 测试与 `zircon_app` bootstrap/profile 测试调用点改用 `crate::plugin::native::*` / `zircon_runtime::plugin::native::*`。M3.1 后当前 `native_plugin_public_surface` / `plugin_surface_lifecycle_boundary` 锚点：`root_reexport_count = 0`、`native_namespace_reexport_count = 64`、native root re-export 0/0、native namespace re-export 64/64、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1、app NativePlugin current call-site files: 7。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` risks=[]；Cargo `plugin/native_plugin/app/plugins` gates 仍待 active lanes 清空后补跑。 |
| M2 | 2.2 测试/文档迁移 | code_static_passed_cargo_pending | 2026-06-16 | `zircon_runtime/src/tests/plugin_extensions/{native_plugin_loader.rs,export_build_plan_native_dynamic.rs,export_build_plan.rs}` 的 native loader 相关测试已固定在 `plugin_extensions` 隔离测试树；直接 native symbol import 仅出现在 `crate::plugin::native::*` 路径，旧 `crate::plugin::NativePlugin*` / `crate::plugin::ZIRCON_NATIVE_PLUGIN*` 根路径泄漏为 0。新增 `runtime_absorption::plugin_surface_lifecycle::runtime_06_native_loader_tests_use_isolated_plugin_native_namespace` 常驻结构守卫；`plugin_surface_lifecycle_boundary` 同步报告 native loader test files 4/4、native test namespace import files 3/3、native test root import leaks 0/0。`docs/engine-architecture/native-plugin-boundary.md` 由目标态口径刷新为当前态；验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` risks=[]、standalone plugin_surface_lifecycle 2/2、standalone plan-status 32/32；`cargo test -p zircon_runtime --lib plugin --locked` 仍待既有 render compile drift 清理后补跑。 |
| M3 | 3.1 V1/V2 处置 | code_static_passed_cargo_pending | 2026-06-16 | V3-only native plugin ABI hard-cutover：删除 loader V1/V2 descriptor probing、V1/V2 entry fallback、`NativePluginAbiV1/V2`、`NativePluginEntryReportV1/V2`、`NativePluginHostFunctionTableV2`、V1/V2 version/descriptor constants 与 fixture V1/V2 exports；保留 V3 当前 ABI 与 V3 行为/bridge method 路径。`native_dynamic_fixture` 改为 `abi_unknown_version` feature，unknown ABI rejection 覆盖 `unsupported native plugin ABI version 99; expected 3`；`native_loader_rejects_unknown_abi_version_with_explicit_report` 替代旧 V2 fallback 测试。当前审计锚点：`native_loader_v1_v2_file_count = 0`、`plugin_v1_v2_usage_files = 0`、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、`native_namespace_reexport_count = 64`、native namespace re-export 64/64。Cargo/package gates 因外部 active lanes 仍待补跑。 |
| M3 | 3.2 回滚失败注入 | code_static_passed_cargo_pending | 2026-06-16 | 新增 folder-backed `native_plugin_live_host/tests/hot_reload_failures.rs`，覆盖 hot reload failure injection：`hot_reload_missing_symbol_after_reload_rolls_back_to_previous_instance` 验证重载后缺少目标 runtime native package / entry symbol 时保留旧实例，`hot_reload_state_restore_failure_rolls_back_and_reports` 验证新实例 restore-state 失败时卸载新实例、恢复旧 snapshot 并报告 rollback。验证：rustfmt check 通过；默认特性聚焦 Cargo 进入 `zircon_runtime` 编译后被既有 render compile drift 阻断（`PostProcessStackDescriptor`、`RenderPipelineCompileOptions` 未解析）；`--no-default-features --features core-min` 聚焦 Cargo 604s 超时未到测试结果，已停止本切片 Runtime 06 cargo 进程，保留 Cargo pending。 |
| 横切 | Cargo/plugin pending gate | code_static_pending_cargo | 2026-06-13 | 新增 `runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation`，保持 Runtime 06 在 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` 验证线通过前为 `in_progress`：M1 空参数修复只记录 binding 与 session 静态/局部证据，真实后端 `vampire_project_session`、runtime fallback、plugin/native plugin、app 与 `zircon_plugins` workspace Cargo 仍待 clean lane 重跑；M2.1/M2.2/M3.1/M3.2 已静态落地，包级 Cargo/native 验证仍待完成；M0 评审同步引用 `native_plugin_public_surface` 与 `root_reexport_count = 0` / `native_namespace_reexport_count = 64`，以固定当前 public-surface evidence。 |
| 横切 | plugin surface/lifecycle 结构镜像 | structure_audit_static_passed_cargo_pending | 2026-06-14 | 新增 `plugin_surface_lifecycle_boundary` 并接入总审计；M1.2/M2.2/M3.2 后镜像 Runtime 06 source 14/14、doc 5/5、frontmatter `in_progress`、当前 `last_refined = 2026-07-01`、`native_plugin_public_surface.m4_gate_status = classified-and-clear`、`root_reexport_count = 0`、`native_namespace_reexport_count = 64`、native root re-export 0/0、native namespace re-export 64/64、public native namespace re-export locations 1/1、debt groups 0/0、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、fallback lifecycle failure tests 4/4、native loader test files 4/4、native test namespace import files 3/3、native test root import leaks 0/0、unknown ABI rejection、hot reload failure injection、app NativePlugin current call-site files: 7；Cargo 仍等待 active lanes 清空后按本计划验证线重跑。 |
| 横切 | plugin surface/lifecycle 静态验证 | static_validation_passed_cargo_blocked | 2026-06-14 | `python -m py_compile` 覆盖 `native_plugin_public_surface.py`、`plugin_surface_lifecycle_boundary.py` 与总审计脚本通过；直接运行 `plugin_surface_lifecycle_boundary_audit` 报告 `risks = []`；M3.1 后聚合 Runtime 06 镜像断言锁定 V3-only native plugin ABI、unknown ABI rejection、root/namespace/debt/unclassified = 0/60/0/0、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0；Markdown 输出含 Runtime 06 边界与 7 个 app NativePlugin 文件；`rustfmt --edition 2021 --check` 覆盖 plan-status early/recent guards 通过；Cargo/rustc 通道仍需等待 clean lane，因此未声明 Runtime 06 Cargo 通过。 |
| 横切 | plugin surface/lifecycle 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | 新增 `runtime_absorption::plugin_surface_lifecycle::runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts` 并接入 `runtime_absorption/mod.rs` 与 recent static guard 清单；Runtime 06 mirror-doc guard anchors: `plugin_surface_lifecycle_boundary`; `expected_source_file_count = 14`; `expected_doc_file_count = 5`; fallback lifecycle failure tests 4/4; native root re-export 0/0; native namespace re-export 64/64; M4 gate `classified-and-clear`; debt groups 0/0; unclassified native root symbols 0/0; unclassified native namespace symbols 0/0; public native namespace re-export locations 1/1; app NativePlugin current call-site files: 7; native loader V1/V2 implementation files 0/0; `zircon_plugins` V1/V2 usage files 0/0; export_build_plan V1/V2 usage 0/0; native loader test files 4/4; native test namespace import files 3/3; native test root import leaks 0/0; unknown ABI rejection; hot reload failure injection; `mirror_docs_guard_present = true`; `risks = []`; `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed`; `runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts`。本切片只同步静态守卫、Python 审计锚点和镜像文档写回；`script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gate 仍 pending。 |
| 横切 | native root re-export current mirror fix | mirror_docs_static_passed_cargo_pending | 2026-06-16 | `runtime_absorption::plugin_surface_lifecycle::runtime_06_plugin_surface_lifecycle_mirror_docs_match_structure_audit_counts` 的 `plugin_root_symbols.len()` 从 stale 68 同步到当前 `native_plugin_public_surface` / `plugin_surface_lifecycle_boundary` 事实；M3.2 后当前事实为 native root re-export 0/0、native namespace re-export 64/64、`root_reexport_count = 0`、M4 gate `classified-and-clear`、debt groups 0/0、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、hot reload failure injection，并把 Runtime 06 `last_refined` 与 Python 审计期望同步为 `last_refined = 2026-06-16`。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` risks=[]；`script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates 仍 pending。 |
| 横切 | Runtime 06 plugin surface/lifecycle Markdown renderer split | plugin_surface_lifecycle_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `plugin_surface_lifecycle_markdown_split_static_passed_cargo_deferred_tests_deferred`；新增 `plugin_surface_lifecycle_markdown.py` 承接 `render_plugin_surface_lifecycle_boundary_markdown(...)`，`plugin_surface_lifecycle_boundary.py remains the 450-line audit/risk owner`，`Markdown owner is 144 lines`；总审计入口改从新 renderer owner 导入，直接审计仍报告 source 14/14、doc 5/5、frontmatter `in_progress` / `last_refined = 2026-07-01`、native root re-export 0/0、native namespace re-export 64/64、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1、app NativePlugin current call-site files 7/7、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0、native loader test files 4/4、native test namespace import files 3/3、native test root import leaks 0/0、fallback lifecycle failure tests 4/4、source anchors 19/19、doc anchors 23/23、validation command anchors 9/9、`mirror_docs_guard_present = true`、`risks = []`，renderer module 为 `runtime_structure_audits.plugin_surface_lifecycle_markdown`；验证：Python py_compile、direct audit、standalone plugin_surface_lifecycle 3/3、standalone plan-status 33/33 通过，包级 Cargo gate 仍 deferred。 |
| 横切 | Runtime 06 native plugin public-surface Markdown renderer split | native_plugin_public_surface_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `native_plugin_public_surface_markdown_split_static_passed_cargo_deferred_tests_deferred`；新增 `native_plugin_public_surface_markdown.py` 承接 `render_native_plugin_public_surface_markdown(...)`，`native_plugin_public_surface.py` 保持 400 行 native public-surface scan / symbol classification / M4 gate owner，Markdown owner 为 63 行；总审计入口改从新 renderer owner 导入。Direct probe 报告 `root_reexport_count = 0`、`native_namespace_reexport_count = 64`、`symbol_decision_group_count = 5`、`native_plugin_public_surface_migration_debt_count = 0`、`unclassified_root_reexport_symbol_count = 0`、`unclassified_native_namespace_symbol_count = 0`、`root_public_reexport_location_count = 0`、`public_reexport_location_count = 1`、M4 gate `classified-and-clear`、`risk_count = 0`、rendered output 12 lines；验证：Python py_compile、direct native plugin public-surface/renderer probe、standalone plan-status 33/33 通过，包级 script VM / native plugin / app / plugin workspace Cargo-native gates 仍 deferred。 |
| 横切 | Runtime 06 native hot-update/replay public-surface audit sync | runtime_06_native_hot_update_replay_public_surface_audit_sync_static_passed_cargo_deferred | 2026-07-01 | `NativePluginRuntimeDeltaHotUpdateReport`、`NativePluginRuntimeDeltaHotUpdateRequest`、`NativePluginRuntimeRegistrationReplayReport` 与 `NativePluginRuntimeRegistrationSystemReplay` 归入 existing native live-host runtime public-surface group；当前 direct audit 报告 `root_reexport_count = 0`、`native_namespace_reexport_count = 64`、symbol decisions 64/64、symbol groups 5/5、unclassified native namespace symbols 0/0、native namespace re-export 64/64、native loader test files 4/4、native test namespace import files 3/3、native test root import leaks 0/0、`last_refined = 2026-07-01`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct native/plugin lifecycle audits、standalone plugin_surface_lifecycle 3/3；包级 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates 仍 deferred。 |
| 横切 | Runtime 15 M3 plugin surface lifecycle guard folder-backed split | runtime_15_plugin_surface_lifecycle_guard_folder_backed_static_passed_cargo_deferred | 2026-07-05 | `plugin_surface_lifecycle.rs` 不再承载 Runtime 06 source/doc inventory、mirror-doc guard、fallback lifecycle guard、native loader namespace guard 与 shared file-scan helpers，已硬切为 route owner；children 为 `plugin_surface_lifecycle/inventory.rs`、`lifecycle_fallback.rs`、`mirror_docs.rs`、`native_loader_namespace.rs`、`support.rs` 与 `plugin_surface_lifecycle/split_layout.rs::runtime_15_plugin_surface_lifecycle_guard_is_folder_backed`。验证：scoped rustfmt/static scans passed，父/子行数 12/44/31/173/50/119/145；scoped `git diff --check` 通过，仅 LF/CRLF normalization warnings。本切片只整理 Runtime 06 absorption 测试守卫 owner，不改变 `plugin_surface_lifecycle_boundary` 审计事实、不更新 Runtime 06 `last_refined`；Cargo gate deferred，包级 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` native gates 仍 deferred。 |
| M2 | Runtime 06 plugin::native hard-cutover | code_static_passed_cargo_pending | 2026-06-16 | `plugin::native` 公开命名空间落地并替代旧根 native re-export；M3.1 后当前审计口径为 `root_reexport_count = 0`、`native_namespace_reexport_count = 64`、native root re-export 0/0、native namespace re-export 64/64、M4 gate `classified-and-clear`、debt groups 0/0、native namespace symbol groups 5/5、unclassified native root symbols 0/0、unclassified native namespace symbols 0/0、root public native re-export locations 0/0、public native namespace re-export locations 1/1。验证：rustfmt check、Python py_compile、direct `native_plugin_public_surface_audit` / `plugin_surface_lifecycle_boundary_audit` risks=[]；standalone Rust/Cargo gates 待本切片后续验证窗口补跑。 |
| M3 | Runtime 06 V1/V2 ABI hard-cutover | code_static_passed_cargo_pending | 2026-06-16 | V3-only native plugin ABI 与 unknown ABI rejection 静态落地：`native_loader_v1_v2_file_count = 0`、`plugin_v1_v2_usage_files = 0`、native loader V1/V2 implementation files 0/0、`zircon_plugins` V1/V2 usage files 0/0、export_build_plan V1/V2 usage 0/0；`abi_unknown_version` fixture 覆盖 `unsupported native plugin ABI version 99; expected 3`；Cargo/package gates 等 active lanes 清空后补跑。 |
| M3 | Runtime 06 hot reload failure injection | code_static_passed_cargo_pending | 2026-06-16 | `native_plugin_live_host/tests/hot_reload_failures.rs` 落地 hot reload failure injection：`hot_reload_missing_symbol_after_reload_rolls_back_to_previous_instance` 与 `hot_reload_state_restore_failure_rolls_back_and_reports` 覆盖缺目标/缺符号与 restore-state 失败两条回滚路径。验证：rustfmt check、Python py_compile、direct `plugin_surface_lifecycle_boundary_audit` source 14/14 / risks=[]；默认特性 Cargo 被既有 render compile drift 阻断，core-min Cargo timeout no result，包级 `native_plugin` gate 保持 pending。 |
| 横切 | Runtime 06 native host FFI panic guard | native_host_ffi_panic_guard_static_passed_cargo_timeout | 2026-06-22 | 处理 F1/E7：新增 `zircon_runtime/src/plugin/native_plugin_loader/ffi_panic_guard.rs`，`host_api_adapter.rs` 的 9 个 `ZrHostApiV3` callbacks 与 `host_callbacks.rs` 的 4 个 private native host callbacks 均通过 panic guard，panic 分别转 `ZrStatusCode::Panic` / `ZIRCON_NATIVE_PLUGIN_STATUS_PANIC`；新增 `native_host_bridge_call_catches_plugin_method_panic` 与 guard helper tests。验证：`rustfmt --edition 2021 --config skip_children=true --check` 覆盖 touched native-loader files 通过；静态扫描 `unsafe extern "C" fn native_host_*` 13/13 均路由 guard；`git diff --check` 仅 LF/CRLF warning。`cargo test -p zircon_runtime --lib native_host_bridge_call_catches_plugin_method_panic --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime06-native-ffi-0622 --message-format short --color never -- --test-threads=1 --nocapture` 1200s 编译超时无测试结果，残留 cargo/rustc 已停止，Cargo gate 保持 pending。 |
| 横切 | F8 RuntimePluginDescriptor builder scaffold | runtime_plugin_descriptor_builder_scaffold_coremin_check_passed | 2026-06-22 | 处理 F8/E3 大 descriptor builder 缺口：新增 `RuntimePluginDescriptorBuilder` owner 与 `RuntimePluginDescriptor::builder(...).build()` 入口，并通过 `runtime_plugin` / `plugin` facade re-export；builder 覆盖 category、enabled/required defaults、target modes、capabilities、system sets、system anchors、maturity、capability status、optional features 与 default packaging。新增 `runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 与 `review_f8_runtime_plugin_descriptor_exposes_builder_scaffold`，锁定 builder 输出与既有 fluent descriptor projection 等价、API/re-export/document/status 锚点齐全。验证：scoped rustfmt --check 通过；F8 structure guard 2/2、status-output 2/2、`runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 1/1 与 runtime core-min `cargo check` 通过（既有 warnings）。RuntimePluginDescriptor public-field convergence complete；RuntimePluginDescriptor::new retired；15 个 public 字段裸访问收束和字段私有化已由后续切片完成；Runtime 06 broader `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates 仍 pending。 |
| 横切 | F8 first-party RuntimePluginDescriptor builder migration | runtime_plugin_descriptor_first_party_builder_migration_coremin_check_passed | 2026-06-22 | 处理 F8/E3 生产调用点迁移：`zircon_plugins/{ai,animation,hybrid_gi,navigation,net,particles,physics,prefab_tools,rendering,solari,sound,terrain,texture,tilemap_2d,virtual_geometry,zr_vm_language}/runtime` 的 16 个 runtime plugin descriptor 入口已改用 `RuntimePluginDescriptor::builder(...).build()`；`rendering` 的 optional-feature loop 改为 `let mut builder = ...` 后累加 `.with_optional_feature(...)` 并最终 `builder.build()`。新增 `review_f8_first_party_runtime_plugin_descriptors_use_builder`，锁定 first-party runtime plugin descriptor production files 16/16、旧 `RuntimePluginDescriptor::new(` absent、builder build present。验证：scoped rustfmt --check 通过；F8 structure guard 3/3、status-output 2/2 通过；runtime core-min `cargo check` 与 16-package `zircon_plugins` focused `cargo check` 均通过（既有 warnings）。RuntimePluginDescriptor public-field convergence complete；RuntimePluginDescriptor::new retired；字段私有化和旧构造入口退役已完成；Runtime 06 broader `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates 仍 pending。 |
| 横切 | F8 RuntimePluginDescriptor test fixture builder migration | runtime_plugin_descriptor_test_fixture_builder_migration_coremin_check_passed | 2026-06-22 | 处理 F8/E3 测试夹具迁移：`zircon_runtime/src/tests/plugin_extensions` 下 14 个曾使用 `RuntimePluginDescriptor::new(...).with_*` 的 runtime/plugin extension 测试文件已迁到 `RuntimePluginDescriptor::builder(...).build()`；`runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 保留显式 `RuntimePluginDescriptorBuilder` 类型断言，但不再依赖旧 constructor 作为对照。新增 `review_f8_runtime_plugin_descriptor_test_fixtures_use_builder`，锁定 plugin extension RuntimePluginDescriptor test fixtures 14/14、builder call count 64、旧 `RuntimePluginDescriptor::new(` absent。验证：scoped rustfmt --check 通过；F8 structure guard 4/4、status-output 2/2、runtime core-min `cargo check` 与 focused descriptor behavior filter 1/1 通过（既有 warnings）；broad plugin_extensions cargo filter 在 1200s timeout 后无测试结果，残留 cargo/rustc 已清理，不计通过。RuntimePluginDescriptor public-field convergence complete；RuntimePluginDescriptor::new retired；15 个 public 字段裸访问收束和字段私有化已完成；Runtime 06 broader `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gates 仍 pending。 |
| 横切 | F8 RuntimePluginDescriptor public-field convergence | runtime_plugin_descriptor_public_field_convergence_coremin_check_passed | 2026-06-22 | 处理 F8/E3 字段裸访问风险：`RuntimePluginDescriptor` 15 个字段已私有化，新增 `plugin/runtime_plugin/descriptor/access.rs` 暴露 `package_id()`、`display_name()`、`category()`、`runtime_id()`、`crate_name()`、`enabled_by_default()`、`required_by_default()`、`target_modes()`、`capabilities()`、`system_sets()`、`system_anchors()`、`capability_statuses()`、`maturity()`、`optional_features()`、`default_packaging()` 15 个只读访问器；builtin catalog augmentation、registration validation、plugin extension 测试和 manifest contribution 测试改走访问器。新增 `review_f8_runtime_plugin_descriptor_fields_are_private_with_accessors`，锁定 RuntimePluginDescriptor private fields 15/15、accessor 签名和 RuntimePluginDescriptor public-field convergence complete 状态锚。验证：runtime core-min `cargo check` 通过（既有 warnings）；lib-test 编译通过，`plugin_workspace_shape` 过滤器执行时暴露两个既有 plugin manifest 断言失败（native fixture sdk_api_version、opus_importer builtin catalog missing），不计为本切片失败；broader Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` gates 仍 pending。 |
| 横切 | F8 RuntimePluginDescriptor public constructor retired | runtime_plugin_descriptor_public_constructor_retired_coremin_check_passed | 2026-06-22 | 处理 F8/E3 旧构造面退役：删除 `descriptor/builder/construction.rs` 与 `descriptor/builder/fluent.rs`，`descriptor/builder.rs` 不再挂载 retired owner，`RuntimePluginDescriptorBuilder` 直接组装私有字段并继续作为唯一公开构造路径；builtin catalog 子模块改为传递 `BuiltinCatalogDescriptorBuilder`，catalog root 统一 `build()`；`zircon_plugins/plugin_sdk/src/runtime.rs` 的 `RuntimePluginDeclaration` 改为持有 `RuntimePluginDescriptorBuilder`，不再调用 `RuntimePluginDescriptor::new(` 或 descriptor 自身 `with_*`。新增 `review_f8_runtime_plugin_descriptor_public_constructor_is_retired`，锁定 RuntimePluginDescriptor::new retired、descriptor/builder/construction.rs retired、descriptor/builder/fluent.rs retired 与 builtin catalog child modules 不直接使用 descriptor。验证：runtime core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime06-f8-public-constructor-0622` 通过（既有 144 warnings）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_sdk --locked --jobs 1 --target-dir target\codex-runtime06-f8-public-constructor-0622` 通过（既有 runtime warnings）；broader Runtime 06 gates 仍 pending。 |
| M1 | Runtime 06 fallback lifecycle Cargo 验证 | fallback_cargo_passed_real_backend_pending | 2026-06-16 | 状态锚点 `fallback_cargo_passed_real_backend_pending`；为解除 M1.2 fallback 验证阻断，补齐 `graphics/pipeline/render_pipeline_asset/compile.rs` 测试模块对 `PostProcessStackDescriptor` 与 `RenderPipelineCompileOptions` 的局部 import；不改 render 生产行为。`cargo test -p zircon_runtime --lib vm_lifecycle_fallback --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-06-fallback-lifecycle-0616 -- --nocapture --test-threads=1` 通过 5/5：四个 `vm_lifecycle_fallback_*` 测试与 `runtime_06_vm_lifecycle_fallback_failure_tests_are_folder_backed`。Runtime 06 仍未关闭：real-backend Cargo、plugin/native plugin、app 与 `zircon_plugins` workspace gates remains pending。 |
| M1 | Runtime 06 shader artifact cache real-backend unblock | asset_cache_fixed_vampire_session_pending | 2026-06-16 | 状态锚点 `asset_cache_fixed_vampire_session_pending`；real-backend `vampire_project_session` 首轮复验已越过 ZR VM 环境配置，暴露 `lib://shaders/ae3ee5f2-ac09-3b2c-d00c-0fd96cccca44.zasset` 反序列化失败：`tag for enum is not valid, found 5`。最低层修复在 `zircon_runtime/src/asset/artifact/cache_payload.rs`：shader import redirects 与 texture slots 改用 `ArtifactCacheShaderImportRedirectAsset` / `ArtifactCacheShaderTextureSlotAsset`，避免 authoring `skip_serializing_if` 影响 bincode wire shape；同步刷新 `examples/vampire/assets/shaders/default_pbr.zmeta` 与对应 `.zasset`。验证：`artifact_store_roundtrips_shader_assets_with_cache_safe_toml_metadata` 1/1 与 `project_manager_imports_compound_zshader_package_with_subassets` 1/1 通过；完整 `vampire_project_session` filter 604s timeout，单测 `vampire_project_session_starts_paused_until_start_button_click` 184s timeout，real-backend session gate remains pending。 |
| M1 | Runtime 06 Vampire real-backend menu/retry focused validation | vampire_real_backend_focused_passed_full_gate_pending | 2026-06-16 | 状态锚点 `vampire_real_backend_focused_passed_full_gate_pending`；Release ZrVM DLL 验证通过 focused session path：`vampire_project_session_starts_paused_until_start_button_click` 覆盖 Start Game 前暂停、点击后进入 `playing`；`vampire_project_session_game_over_menu_retries_to_playing` 覆盖致命接触、Game Over 菜单、Retry 后回到 `playing` 且玩家归零位满血。最低共享边界修复包括 `gameplay.entity_exists` 与 `gameplay.script_number_at_most`，避免当前 real ZrVM 对 host-returned entity/numeric 直接比较路径崩溃；Vampire 脚本通过 `vampire.spawn_grace` 阻断 Retry 同帧再伤害。完整 `vampire_project_session` 组、plugin/native plugin、app 与 `zircon_plugins` gates 仍 pending。 |
| M1 | Runtime 06 Vampire HUD real-backend capture validation | vampire_hud_real_backend_focused_passed_full_gate_pending | 2026-06-16 | 状态锚点 `vampire_hud_real_backend_focused_passed_full_gate_pending`；最低层补齐 core scene particle descriptor：当 extract 存在 particle sprites、pipeline 有 `Transparent3d` stage 且没有插件 descriptor 提供 `particle-render`/`particle.transparent` 时，`RenderPipelineAsset::compile(...)` 插入 descriptor-only `particle-render` pass；pass 只读 `scene-depth`、写 `scene-color`，避免 scene-color 同 pass sampled/resource 与 color attachment 冲突。`RenderParticleSpriteSnapshot.depth_test` 将普通粒子与 world-HUD overlay 分流，HUD bars 使用 overlay pipeline 和 alpha blend，不写 velocity。Vampire scene `exposure_ev100` 从旧倍率语义的 `0.2` 迁到 EV100 `9.2`，解除 PP-M3 exposure resolve 下的全白帧。验证：复用 Release ZR VM 测试二进制 `vampire_project_session_capture_frame_draws_world_hud_bars` 1/1 通过；`core_scene_particle_extract_adds_billboard_pass_without_plugin_feature_identity`、`particle_plugin_render_feature_adds_transparent_pass_to_default_pipeline`、`particle_vertices_split_depth_tested_and_overlay_sprites`、`particle_velocity_vertices_skip_overlay_sprites`、`world_hud_bar_sprites_use_overlay_depth_path`、`authored_particle_sprites_keep_depth_test_path` 通过。重新编译 `particle_pipeline_keeps_world_hud_billboards_transparent_and_depth_read_only` 过滤项两次分别 184s/424s 超时未到测试结果，完整 `vampire_project_session` 组、plugin/native plugin、app 与 `zircon_plugins` gates 仍 pending。 |

| 横切 | Runtime extension registry stable-slot finalization | runtime_extension_registry_stable_slot_finalize_coremin_check_passed_tests_compile_blocked | 2026-07-10 | `TypedExtensionPoint` 现以单调逻辑 `ExtensionSlot` + slot-to-dense-row 映射承接 owner unload：survivor slot 不重编号、removed slot 留 tombstone 且不复用、sort 只重排 dense rows；旧 public `ExtensionSlot::index()` dense 语义已退役。`RuntimeExtensionRegistry::finalize()` 将 20 个 typed points 的 staging 容器移动为 frozen 表，并标记 non-typed asset importer registry 的当前 registration epoch 已发布；keys/owners/descriptors/slot map 不复制成并存缓存，仅派生 sorted-key lookup index；typed write 与 asset importer register/revoke 统一失效 finalized state；`TypedExtensionPoint` 不再从 plugin 根 facade 导出。`WorldRuntimeExtensionSet` 默认即 finalized，install 改为 clone/merge/finalize/commit 事务，失败不污染既有读状态。验证：standalone typed-point harness 4/4、finalization structure coverage 4/4、scoped rustfmt、`git diff --check` 与 runtime core-min library `cargo check` 通过；latest focused lib-test build 在执行目标测试前被范围外活动 `tests/plugin_extensions/extension_registry_bridge/basics.rs:216` `E0282` 阻断（更早的 font fixture 阻断已自愈），不计测试通过，完整 Runtime 06 gate 未关闭。验收记录：`tests/acceptance/runtime-extension-registry-finalization.md`。 |
| M3 | native restore-state failure rollback disposition repair | `runtime_06_native_hot_reload_restore_rollback_state_machine_repaired_fresh_cargo_pending` | 2026-07-10 | 旧 default-feature 二进制的 `native_plugin` 过滤结果为 124 passed / 14 failed；其中 `hot_reload_state_restore_failure_rolls_back_and_reports` 暴露真实状态机缺陷：restore 失败后旧实例已经重新插回 live host，但诊断仍保留 `previous_unloaded`，错误地报告 rollback unavailable。`NativePluginHotReloadState` 现以 `PreviousPluginDisposition::{NotLoaded,HeldForRollback,Unloaded,Restored}` 取代单一布尔标记；旧实例重新插回后显式进入 `Restored`，诊断与实际 live-host 状态一致。新增 `native_live_host_rollback_plan_reports_when_previous_plugin_was_restored`；scoped rustfmt 与 diff check 通过。完整行为验证仍需最新 lib-test 二进制，故不宣称 Cargo green。 |
| 横切 | native-plugin typed-error review guard numbered-output cutover | `runtime_06_native_plugin_typed_error_review_guards_18_passed_fresh_filter_pending` | 2026-07-10 | 15 个 native-plugin typed-error review guard owner 的 72 个 priority-plan `include_str!` 引用已从 route-only 父计划硬切到 Runtime 15 四份编号输出记录；212 个 include path 全部存在。直接 `rustc --test` current-source harness 18/18 通过。结构状态源清单同步到真实 `status_docs` / `structure_assertions` 子树后 84/84 路径存在。旧二进制中的其余 13 个 review/structure 失败均已在 current source 收口，但仍需 fresh `native_plugin` 过滤器复验。验收记录：`tests/acceptance/runtime-native-plugin-current-result.md`。 |
| M4 | 2026-07-12 current descriptor projection acceptance | `runtime_06_descriptor_projection_static_passed_package_gates_pending` | 2026-07-12 | Fresh current-source regression `test_descriptor_projects_provided_interfaces_to_package_manifest` passed 1/1. The runtime plugin descriptor continues to project provided interfaces into the package manifest without compatibility aliases. Native-plugin, registry and plugin-workspace package filters still require the fresh shared binaries, so Runtime 06 remains `in_progress`. |
| M4 | 2026-07-12 current plugin package filters | `runtime_06_plugin_777_18_failed_2_ignored_native_plugin_135_4_failed` | 2026-07-12 | Fresh current-source Runtime binary executed the broad `plugin` filter: 777 passed, 18 failed and 2 ignored. The focused `native_plugin` filter executed 139 tests with 135 passed and four failed; all four failures are the same real native dynamic fixture build exit 101, so the native loader behavior gate is not accepted. The broader plugin failures additionally expose rendering optional-feature manifest/catalog drift and Navigation system-anchor registration drift. No ignore, fallback or compatibility path was added; Runtime 06 remains `in_progress` pending the lowest manifest/fixture owners and upward rerun. |
| M2/M3 | Runtime 06 V4 public-surface inventory forward repair | `runtime_06_v4_surface_inventory_python_green_rust_hardcut_pending` | 2026-07-31 | 初始上行审计暴露 namespace 74/expected68、groups 6/expected5、V4 registration policy/scope 未分类、App 8/expected7；第一次修复又被独立审查发现错误归类、ABI 二分失真、source 17/expected14 false-green 与 root scanner 语法绕过。最终 classifier 建立独立 host-API adapter owner；ABI 矩阵区分 V3 descriptor/entry + host-function-table、V4 behavior + runtime-interface host API；lifecycle audit 为 source 17/17、namespace 74/74、groups 6/6、unclassified 0/0、App 8/8、`risks = []`；root alternate re-export 负例常驻。验证：focused Python 3/3、standalone Runtime06 1/1、完整 runtime tech-stack 8/8、py_compile通过。Rust mirror、V3 host adapter/V2 DTO hard cut 与 managed Cargo/native gates仍 pending，failure保持 open。 |
| M3 | Runtime 06 V1/V2 descriptor-entry-DTO hard-cut guard baseline | `runtime_06_v2_descriptor_entry_dto_hardcut_guard_red_production_scope_pending` | 2026-08-03 | guard-first TDD 已把 V1/V2 descriptor、entry、behavior、host function table、byte DTO、function pointer、`abi_v2_only` fixture feature、V3-to-V2 alias 与退役 `NativeHostApiV3RegistrationScope` 纳入独立零容忍扫描。连续独立审查依次暴露 root alias/comment bypass、`plugin/native.rs` 扫描遗漏、unrelated nested-native 假阳性、多跳/grouped-self alias、嵌套/cfg 作用域候选污染、普通/raw string 词法误判、macro token-tree 语句吞并、`extern crate self` alias 缺口、诊断行号漂移、`extern "C" fn` 局部 alias 污染、raw/Unicode identifier tokenizer 漂移、Unicode NFC 判等、macro_rules 生成根面、raw 宏名漏检、descriptor/entry function-pointer 库存遗漏与预期 RED 断言遮蔽；全部以 mutation-first 前向修复。当前 scanner 先遮罩 Rust 非代码区，再按 delimiter stack 只解析根作用域 `use` 与已确认的 `extern crate`，以 raw/Unicode/NFC-aware tokenizer 展开多候选 binding path 并保留真实 token offset；任何 plugin facade 根宏调用都以 `root-macro-invocation-present` 令 M4 gate 非 clear，禁止不可静态审计的宏生成根面，同时只把无 `r#` 前缀的 `macro_rules!` 识别为定义。14 个 control tests 全绿，完整 inventory 为 17 tests：14 passed / 3 expected production-migration failures；当前 RED 计数为 Runtime plugin 10、plugins 7、V3 alias 2、retired adapter 4、fixture feature 2。三个修改 Python 文件 `ast.parse` 通过，scoped `git diff --check` 通过（仅既有 CRLF 提示）；Windows ACL 阻止 py_compile 写入 `__pycache__`，未计入通过证据，且未运行 Cargo。第七次 C0/I1/M1 与第八次 C0/I2/M1 独立复核均已全部前向修复，修复后最终 exact5 复核为 C0/I0/M0、guard-first 静态切片 Source Ready。ownership-transfer preview `f36eb5dd...` 在 epoch245 审计 exact22：10 eligible、12 被 Frameworks02/04 或 Plugins01 executable owner 持有；为保持 descriptor/entry/behavior/DTO/fixture 同变更闭合，未对可转移子集 partial apply。生产硬切范围尚未生效，因此本状态只声明 guard 与失败基线完成，不声明硬切、fixed return 或 Runtime06 完成。 |

基线数值（开工首日记录）：

- `plugin/mod.rs` native 导出基线：约 48 类型/常量（:27-50；重核命令见执行前检查清单）
- V1/V2 使用者基线：1 文件（native_dynamic_fixture）；export 宿主文件引用：0
- `zircon_app` NativePlugin 引用基线：6 文件
- `function.c:1394` 断言复现命令：`cargo test -p zircon_runtime --lib vampire_project_session --features zr-vm-real-backend --locked -- --nocapture --test-threads=1`
- `cargo test -p zircon_runtime --lib plugin --locked` 通过数基线：__

## 风险与协调

- `20260603-2304-plugin-ecosystem-continuation` 会话活跃于插件注册/导出校验区，M2/M3 执行前强制重读其笔记；`20260611-0416` 是 ZrVM 根因记录所在，M1 前复读，**禁止回退其 worktree 改动**。
- M2 改动触及 `zircon_app` 入口装配（6 文件含 `entry_runner/bootstrap.rs`），与 `zircon_app::entry` 的源断言测试（活动会话 touched）对齐；prelude/lib.rs 的再导出若被外部工具消费，收窄前确认无隐藏调用方。
- ZrVM 是仓外依赖（`../../zr_vm`）：M1 若需上游修复，版本配对要求记入子计划 01 的 zr_vm 治理条目（`runtime-tech-stack.md`），双计划交叉引用。
- M3 删除 V1/V2 是硬切换：夹具与 loader 协商分支必须同一变更内闭合，禁止"先删 loader 后改夹具"的中间态提交。
