---
related_code:
  - zircon_runtime/src/scene/inspection/mod.rs
  - zircon_runtime/src/scene/inspection/snapshot.rs
  - zircon_runtime/src/scene/tests/authoring_boundary.rs
  - zircon_runtime/src/scene/tests/inspection.rs
  - zircon_runtime/src/scene/tests/world_basics.rs
  - zircon_runtime/src/scene/tests/component_structure.rs
  - zircon_runtime/src/scene/tests/derived_state.rs
  - zircon_runtime/src/scene/tests/asset_scene.rs
  - zircon_runtime/src/scene/world/project_io.rs
  - zircon_runtime/src/scene/ecs/archetype/index.rs
  - zircon_runtime/src/scene/ecs/events.rs
  - zircon_runtime/src/scene/ecs/schedule_conflict_graph.rs
  - zircon_runtime/src/scene/module/world_driver.rs
  - zircon_runtime/src/scene/world/property_access/entries.rs
  - zircon_runtime/src/scene/world/dynamic_components.rs
  - zircon_runtime/src/scene/world/render.rs
  - zircon_runtime/src/scene/world/typed_api.rs
  - zircon_runtime/src/scene/ecs/system/native/scheduled_scene_step.rs
  - zircon_runtime/src/scene/tests/mod.rs
  - zircon_runtime/src/scene/tests/dynamic_scene.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs
  - zircon_runtime/src/scene/tests/ecs_archetype_index_structure.rs
  - zircon_runtime/src/scene/tests/ecs_dynamic_components_structure.rs
  - zircon_runtime/src/scene/tests/ecs_identity_storage.rs
  - zircon_runtime/src/scene/tests/ecs_observers_messages.rs
  - zircon_runtime/src/scene/tests/ecs_query_combinations.rs
  - zircon_runtime/src/scene/tests/ecs_query_state_structure.rs
  - zircon_runtime/src/scene/tests/ecs_schedule/conflict_graph.rs
  - zircon_runtime/src/scene/tests/ecs_schedule.rs
  - zircon_runtime/src/scene/tests/ecs_scheduled_native_systems.rs
  - zircon_runtime/src/scene/tests/ecs_typed_api.rs
  - zircon_runtime/src/scene/tests/property_paths.rs
  - zircon_runtime/src/scene/tests/render_extract.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_session/load.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_session/merge.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_session/mutation.rs
  - zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/architecture_review.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/closeout.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/index_tables.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/recent_static_guards.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_slices.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_status_rows.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/subplan_status.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/support.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_01.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_02.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_03.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_04.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_06.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_07.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/early/runtime_08.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/late.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/late/runtime_10.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/late/runtime_11.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/late/runtime_12.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/late/runtime_13.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/cargo_gates/late/runtime_14.rs
  - zircon_runtime/src/scene/dynamic_scene/session/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/archive.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/construction/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/construction/archive.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/construction/capture/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/construction/capture/world.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/construction/capture/level.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/construction/serialization.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/ordinary/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/ordinary/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/ordinary/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/global/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/global/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/global/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/tag/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/tag/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/world/retention/tag/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/ordinary/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/ordinary/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/ordinary/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/global/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/global/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/global/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/tag/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/tag/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/capture/level/retention/tag/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/store/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/store/push.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/store/upsert.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/store/remove.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/named/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/selected/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/named/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/named/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/selected/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/selected/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/named/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/selected/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/rename/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/rename/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/rename/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/touch/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/touch/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/touch/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/remove.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/merge/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/merge/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/merge/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/global/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/global/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/global/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/tag/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/tag/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/retention/tag/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/basic.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/manifest.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/selection/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/selection/global.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/selection/tag.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/selection/typed.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/query/statistics.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/apply/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/apply/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/apply/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/restore/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/restore/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/diff/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/diff/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/restore/diff/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/validation/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/validation/support.rs
  - zircon_runtime/src/scene/dynamic_scene/session/facade/validation/invariants.rs
  - zircon_runtime/src/scene/dynamic_scene/session/construction/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/construction/archive.rs
  - zircon_runtime/src/scene/dynamic_scene/session/construction/capture.rs
  - zircon_runtime/src/scene/dynamic_scene/session/construction/serialization.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/loaded/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/loaded/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/loaded/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/source_path/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/source_path/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_merge/source_path/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/world/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/world/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/world/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/level/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/level/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/level/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/capture_retention/apply.rs
  - zircon_runtime/src/scene/dynamic_scene/session/merge/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/merge/policy.rs
  - zircon_runtime/src/scene/dynamic_scene/session/merge/report.rs
  - zircon_runtime/src/scene/dynamic_scene/session/merge/algorithm/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/merge/algorithm/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/merge/algorithm/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/manifest/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/manifest/archive.rs
  - zircon_runtime/src/scene/dynamic_scene/session/manifest/order.rs
  - zircon_runtime/src/scene/dynamic_scene/session/manifest/summary.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot/capture.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot/restore.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot/diff.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot/summary.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/world/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/world/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/world/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/level/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/level/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/level/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_capture/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/named/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_copy/selected/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_export/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_export/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_export/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_selector/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_selector/selector.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_selector/report.rs
  - zircon_runtime/src/scene/dynamic_scene/session/slot_selector/resolve.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/apply/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/apply/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/apply/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/restore/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/restore/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/diff/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/diff/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/restore/diff/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/query/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/query/manifest.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/query/selection.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/query/statistics.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/query/status.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/named/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/named/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/selected/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded/selected/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/named/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/named/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/selected/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path/selected/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/apply/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/apply/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/apply/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/restore/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/restore/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/diff/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/diff/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/restore/diff/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/named/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/selected/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/named/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/basic/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/basic/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/basic/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/selected/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/loaded/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/loaded/named/{mod,commit,preview}.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/loaded/selected/{mod,commit,preview}.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/source_path/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/source_path/named/{mod,commit,preview}.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_export/source_path/selected/{mod,commit,preview}.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_query/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_query/manifest.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_query/selection.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_query/statistics.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_query/status.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/global/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/global/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/global/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/tag/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/tag/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_retention/tag/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/rename/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/rename/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/rename/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/metadata/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/metadata/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/metadata/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/touch/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/touch/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/touch/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/remove/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/remove/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_mutation/remove/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/global/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/global/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/global/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/tag/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/tag/commit.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/tag/preview.rs
  - zircon_runtime/src/scene/dynamic_scene/session/selected_retention/policy.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/apply/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/apply/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/apply/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/restore/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/restore/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/restore/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/diff/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/diff/named.rs
  - zircon_runtime/src/scene/dynamic_scene/session/path_restore/diff/selected.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/archive.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/capture.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/diff.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/mutation.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/restore.rs
  - zircon_runtime/src/scene/dynamic_scene/session/reports/transfer.rs
  - zircon_runtime/src/scene/dynamic_scene/session/target_path/mod.rs
  - zircon_runtime/src/scene/dynamic_scene/session/target_path/source.rs
  - zircon_runtime/src/scene/dynamic_scene/session/target_path/target.rs
  - zircon_runtime/src/dynamic_api/session.rs
  - zircon_runtime/src/plugin/core_profiles.rs
  - zircon_runtime/src/ui/surface/input/navigation.rs
  - zircon_editor/src/scene
  - zircon_hub/src/projects/metadata.rs
  - docs/engine-architecture/non-network-server-naming-m1.md
  - docs/engine-architecture/hard-cutover-migration-smells-m1.md
  - docs/engine-architecture/runtime-architecture-review-m0.md
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_naming_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_naming_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_scene_editor_surface.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_scene_editor_surface_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_plan_status_anchor_inventory.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_plan_status_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_plan_status_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_plan_status_sources.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_plan_status_output_anchors.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/runtime_plan_status_support_inventory.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/non_network_server_naming.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/non_network_server_naming_markdown.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/hard_cutover_migration_smells.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/hard_cutover_migration_smells_markdown.py
plan_sources:
  - docs/plans/zircon_runtime/runtime/index.md
  - .codex/plans/Runtime 吸收层与 Editor_Scene 边界收束计划.md
status: in_progress
last_refined: 2026-07-01
---

# 05 scene/editor 边界收尾

## 现状与证据（2026-06-12 重核）

旧文两项核心"残留"已被解决，本计划工作面相应缩小并转向白名单机器化：

- **空目录已删（矫正）**：`zircon_runtime/src/scene/editor_projection/` 已不存在（`ls` 报 No such file or directory）。全仓 `editor_projection` 残留引用仅 1 处文本：`scene/tests/component_structure.rs`（执行时核验语义：是"断言不存在"的守卫还是过期注释）。原 M1 切片 1 的"删目录"工作已完成。
- **序列化纯净守卫已枚举完备（矫正）**：`scene/tests/authoring_boundary.rs` 已有双 token 禁入清单——`SERIALIZED_AUTHORING_TOKENS`（19 词：selection/selection_anchors/scene_gizmos/gizmo/overlay/camera_override/preview_lighting/viewport_camera/SceneViewportSettings/pane 等）与 `SOURCE_AUTHORING_TOKENS`（25 词：含 SelectionHighlightExtract/GridOverlayExtract/SceneGizmoOverlayExtract 等 extract 类型名）；helper `assert_text_excludes_authoring_tokens` + 负例自检 `authoring_boundary_guard_fails_on_representative_tokens`。消费方覆盖 world project、dynamic scene、asset scene、inspection JSON 与 serialization source 守卫。
- **inspection 守卫已存在（矫正）**：`scene/tests/inspection.rs` 已有 `world_inspection_serialization_excludes_editor_authoring_tokens` 与 `world_inspection_filters_missing_focus_without_storing_authoring_state`。原 M2 切片 2 的"给 inspection 加守卫"已完成。
- **inspection 公共面**：`scene/inspection/mod.rs` 仅导出 `WorldInspectionField` / `WorldInspectionHierarchyRow` / `WorldInspection` 三类型（field/hierarchy/snapshot 三文件），形状中性。
- **剩余真实工作 1——"editor" 命名白名单**：runtime 内 "editor" 命中约 181 文件（执行时重核：Grep `-l editor`，path `zircon_runtime/src`），混杂三类：合法 editor-host 目标语义（`dynamic_api/session.rs` 的 editor 模式、`plugin/core_profiles.rs:10` `pub struct EditorCoreProfile`、native loader 的 editor_host 校验）、测试夹具、待裁决 authoring 残留。无白名单则无法机器化判定新增违规。
- **剩余真实工作 2——"legacy" 命名裁决**："legacy" 403 处/84 文件。实仓抽样（`ui/surface/input/navigation.rs:22-54`）：`legacy` 是 `dispatch_navigation_event` 旧路由回复的本地变量名，承载真实运行语义（route/focus/diagnostics 全从它取值）——属"领域词化的迁移痕迹"，需逐类裁决是改名、文档化还是列债。
- 命名审计文档锚点（2026-06-12 实测存在）：`docs/engine-architecture/non-network-server-naming-m1.md`、`hard-cutover-migration-smells-m1.md`、`runtime-root-surface-m1.md`、`runtime-architecture-review-m0.md`——白名单产出并入这些既有口径，不另起新文件。

## 目标

1. 物理与文本残留清零：`editor_projection` 的最后 1 处文本残留裁决处理。
2. "editor" 与 "legacy" 命名逐类裁决，产出白名单 + 机器化守卫，使违规新增可被测试拒绝。
3. 守卫从"已存在"升级为"覆盖矩阵可证"：三序列化出口 × 双 token 表 × inspection 的覆盖关系成表，token 清单有维护公约。

## 非目标

- 不迁移任何已在 `zircon_editor` 的投影/选中/gizmo 逻辑；不改 inspection 三类型的数据形状（除非裁决发现 authoring 泄漏）。
- 不在本计划处理 ui/surface/input 的输入路径重构（"legacy" 若裁决为迁移债，移交 UI 边界 owner 计划）。
- 渲染骨架内容归 render 计划 01-08。

### 全局硬约束（继承总计划 §4，违反即返工）

- 硬切换不留兼容层；不新增 crate；非网络语义 server 命名是 blocker（白名单审计顺带复核，口径并入 `non-network-server-naming-m1.md`）。

## 执行前检查清单

1. 活动会话对齐：serialization 守卫与 inspection 均在 `20260604-1232` 会话工作区延长线上——执行前确认该会话对应切片已完成或已交接，避免双写同一守卫。
2. worktree 脏文件检查：`git status --porcelain -- zircon_runtime/src/scene/ docs/engine-architecture/`。
3. 事实重核：
   - `ls zircon_runtime/src/scene/ | grep editor_projection`（应无输出）
   - `grep -rn "editor_projection" zircon_runtime/src --include=*.rs`（应仅 component_structure.rs 1 处）
   - `grep -rl "editor" zircon_runtime/src --include=*.rs | wc -l`（editor 命中文件数基线重核）
   - `grep -rn "legacy" zircon_runtime/src/ui/surface/input --include=*.rs | wc -l`
4. 基线记录：`cargo test -p zircon_runtime --lib scene:: --locked` 通过数记入状态节。

## 里程碑

### M1 残留收尾与命名白名单裁决

#### 切片 1.1 editor_projection 文本残留处理

- 目标文件：`zircon_runtime/src/scene/tests/component_structure.rs`（唯一残留处）。
- 改动形态：核验该处语义——若是"目录不得复活"的守卫断言则保留并加注释；若是过期文本则删除该词。无其他代码改动。
- 调用方迁移：无。
- 验收：Grep `editor_projection` 全仓结果与裁决一致（0 处或仅守卫 1 处）。
- DoD：重核命令输出与判词记入状态节。

#### 切片 1.2 "editor" 命中三分类白名单

- 目标文件：`docs/engine-architecture/runtime-root-surface-m1.md` 或 `runtime-architecture-review-m0.md`（并入既有审计口径，执行时与 `20260604-1232` 会话定稿落点，禁止另起新文件）；本计划状态节（清单副本）。
- 改动形态：纯文档 + 裁决。分类规则（已核实锚点）：
  - **白名单（合法 editor-host 目标语义）**：`dynamic_api/session.rs` editor 会话模式、`plugin/core_profiles.rs` `EditorCoreProfile`（:10）/`RuntimeCoreProfile`（:4）双 profile、native loader 的 editor_host 校验行、`builtin` 的 target mode 词汇。
  - **测试夹具**：测试文件内的 editor 字样按文件粒度白名单。
  - **违规清单（authoring 语义）**：生产代码中 selection/gizmo/inspector 词根（与 `SOURCE_AUTHORING_TOKENS` 25 词复用判据）命中者，逐项给迁移/删除条目。
  - 枚举命令：Grep `editor`，path `zircon_runtime/src`，glob `**/*.rs`（约 181 文件，按顶层目录分桶逐桶裁决）。
- 调用方迁移：无（裁决期；违规项的迁移在切片 1.4 或移交 owner）。
- 验收：白名单三分类表齐备，每桶有计数；违规清单每项有处置判词。
- DoD：清单落审计文档；违规清单为空或每条带 owner。

#### 切片 1.3 "legacy" 逐类裁决

- 目标文件：同 1.2 的审计文档落点；代表区域 `ui/surface/input/`（pointer_reply/pointer/navigation 集中区）。
- 改动形态：纯文档 + 裁决。三分类：
  - **领域词（有真实运行语义）**：如 `navigation.rs:22-54` 的 `legacy` 路由回复变量——route/focus/diagnostics 取值来源；裁决"保留 + 代码注释说明语义"或"改名为语义词（如 `routed_reply`）"，二选一判词。
  - **测试夹具**：asset 测试夹具中 legacy 样例按文件白名单。
  - **真实迁移债**：列入 UI 输入路径 owner 计划，本计划不展开。
  - 枚举命令：Grep `legacy`，path `zircon_runtime/src`，glob `**/*.rs`（403 处/84 文件基线，执行时重核）。
- 调用方迁移：无（裁决期）。
- 验收：84 文件分桶判词齐备；改名项（若有）列成独立切片清单。
- DoD：清单落审计文档，UI 债条目已移交（文档交叉引用）。

#### 切片 1.4 命名守卫机器化

- 目标文件：`zircon_runtime/src/scene/tests/authoring_boundary.rs`（扩展）或 `zircon_runtime/src/tests/`（新守卫文件，执行时按 1.2/1.3 白名单形态定稿落点）。
- 改动形态（已定稿）：

  ```rust
  #[test]
  fn runtime_editor_and_legacy_naming_is_classified_by_owner() {
      // 扫描 zircon_runtime/src，按文件 owner 分类 editor / legacy 命名；
      // 新增未分类命名会失败，已分类 debt 留给对应 owner 切片。
  }
  ```

- 调用方迁移：无。
- 验收：守卫对违规注入有负例（参照既有 `authoring_boundary_guard_fails_on_representative_tokens` :62 的自检模式）。
- DoD：`cargo test -p zircon_runtime --lib naming_boundary --locked` 全绿且白名单外新增命名会失败。

#### M1 测试阶段（milestone-first）

- 切片期：`cargo check -p zircon_runtime --lib --locked`
- 里程碑末：
  - `cargo test -p zircon_runtime --lib scene:: --locked`（无回归）
  - `cargo test -p zircon_runtime --lib authoring --locked -- --nocapture`（新守卫）
  - `cargo test -p zircon_runtime --lib naming_boundary --locked -- --nocapture`（命名白名单）
- 验收证据：白名单/违规清单写入既有命名审计文档（与 `20260604-1232` 口径合并）；守卫进常驻测试树。

### M2 守卫覆盖矩阵审计与维护公约

#### 切片 2.1 覆盖矩阵成表

- 目标文件：`docs/zircon_runtime/scene/inspection.md`（既有，刷新守卫说明；执行时核验：`ls docs/zircon_runtime/scene/`）。
- 改动形态：纯文档。成表：行 = 四个出口（world 序列化、dynamic scene、asset scene、inspection 快照），列 = 双 token 表（SERIALIZED 19 词 / SOURCE 25 词）与负例自检；逐格标注承载测试（已核实：`world_basics.rs` / `component_structure.rs` / `derived_state.rs` / `asset_scene.rs` / `inspection.rs:148,161`）。空格（某出口未覆盖某表）即补测试条目。
- 调用方迁移：无。
- 验收：矩阵无空格，或空格有补测试切片。
- DoD：矩阵落 `inspection.md` 且每格可点到测试名。

#### 切片 2.2 token 清单维护公约

- 目标文件：`scene/tests/authoring_boundary.rs`（注释公约）+ `docs/zircon_runtime/scene/inspection.md`（公约正文）。
- 改动形态：定稿公约——editor 侧新增 authoring 状态类型（如新 overlay/gizmo extract 类型）时，`SOURCE_AUTHORING_TOKENS` 必须同 PR 追加该类型名；公约写明判定规则（出现在 `zircon_editor/src/scene` 的 extract/投影类型名默认入表）。补一条结构测试（签名草案）：`authoring_token_tables_stay_sorted_and_deduplicated`（防清单腐化）。
- 调用方迁移：无。
- 验收：公约 + 结构测试。
- DoD：测试绿，公约可执行（有判定规则而非倡议）。

#### M2 测试阶段（milestone-first）

- `cargo test -p zircon_runtime --lib authoring --locked -- --nocapture`
- `cargo test -p zircon_runtime --lib inspection --locked -- --nocapture`
- 验收证据：覆盖矩阵 + 公约 + 守卫测试全绿；`docs/zircon_runtime/scene/inspection.md` 刷新。

### M3 收尾闭环（2026-06-12 二次细化新增；M1/M2 全切片已完成后的关账步骤）

#### 切片 3.1 legacy debt bucket 移交确认

- 目标文件：本计划状态节 + 各 owner 计划/审计文档交叉引用。
- 改动形态：核对 `runtime_naming_boundary` 审计输出中的 legacy naming debt bucket（2026-06-21 当前为 7 个：runtime graphics、DDS container policy、UI template schema、input event、asset schema、dynamic API migration、scene schema/render）——每个 bucket 必须可点到 owner 计划条目或审计文档判词；缺失者补交叉引用（input/UI template 债 → UI/Input 边界 owner 计划；DDS/graphics/scene render 债 → render/asset/scene 对应子计划）。
- 调用方迁移：无。
- 验收：10 bucket 全部有 owner 落点链接。
- DoD：移交表落状态节。

#### 切片 3.2 状态闭环

- 目标文件：本计划 frontmatter（status → completed）；`docs/plans/zircon_runtime/runtime/index.md` §3 状态行同步。
- 改动形态：收尾回归 `cargo test -p zircon_runtime --lib scene:: --locked`（全族无回归确认，此前验证用的是 naming_boundary/authoring/inspection 过滤词）；通过后翻转状态并在 §2.2 P6/P9 行补"已闭环"判词。
- 验收：scene:: 全族绿；index 与本计划状态一致。
- DoD：status: completed 落盘。

#### M3 测试阶段（milestone-first）

- `cargo test -p zircon_runtime --lib scene:: --locked`
- 验收证据：命令输出摘要 + 状态翻转 + index 同步。

## 状态与产出记录

| 里程碑 | 切片 | 状态 | 完成日期 | 证据（命令输出 / 文件 / 测试名） |
|---|---|---|---|---|
| 横切 | Runtime 05 runtime index status detail split | runtime_index_status_detail_split_static_passed_cargo_deferred_tests_deferred | 2026-06-23 | `docs/plans/zircon_runtime/runtime/index.md` now keeps `## 状态与产出记录` as a routing section with `| 范围 | 记录位置 |` and no `| 里程碑 | 切片 | 状态 |` detail table; slice evidence remains in each owner subplan status table. Added the six missing migrated status anchors to Runtime 02/05/06/07/14 subplans and updated `runtime_absorption::plan_status::status_output_tables` to assert subplan tables plus index routing. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables.rs` passed; standalone `plan_status` filtered `status_output_tables` passed 2/2; unfiltered standalone `plan_status` still fails existing non-status-output plan index/architecture drift, so full suite remains deferred. |
| 横切 | Runtime 05 scene/project serialization Markdown renderer split | scene_project_serialization_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `scene_project_serialization_markdown_split_static_passed_cargo_deferred_tests_deferred`；`scene_project_serialization_boundary.py` now mirrors the current folder-backed Rust source guard by scanning 24 runtime scene/project serialization files, including `dynamic_scene/{document,entity,scene,value}/` owners, and keeps authoring-state detection/risk aggregation at 127 lines; `scene_project_serialization_markdown.py` now owns `render_scene_project_serialization_boundary_markdown` at 31 lines. Direct audit reports audited files 24, `forbidden_location_count = 0`, and `risks = []`. Validation: Python py_compile, direct `scene_project_serialization_boundary_audit` risks=[], standalone `component_structure.rs` 20/20, standalone `plan_status.rs` 33/33; full `scene::` Cargo gate remains deferred while external compile lanes are active. |
| M3 | 3.1 runtime session archive path-management behavior static guard | runtime_session_archive_path_management_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已新增 `zircon_runtime/src/scene/tests/dynamic_scene_session/path_management.rs` source anchor，锁定既有 path mutation/copy/import/export/merge 行为记录：`runtime_session_archive_path_management_code_check_passed_tests_deferred`、`runtime_session_archive_slot_mutation_preview_code_check_passed_tests_deferred`、`runtime_session_archive_copy_preview_code_check_passed_tests_deferred`、`runtime_session_archive_single_slot_import_preview_code_check_passed_tests_deferred`、`runtime_session_archive_source_path_single_slot_import_code_check_passed_tests_deferred`、`runtime_session_archive_loaded_single_slot_save_code_check_passed_tests_deferred`、`runtime_session_archive_single_slot_export_code_check_passed_tests_deferred`、`runtime_session_archive_merge_preview_code_check_passed_tests_deferred` 与 `runtime_session_archive_source_path_merge_code_check_passed_tests_deferred`。该守卫覆盖 `runtime_session_archive_renames_slot_at_path_atomically`、`runtime_session_archive_previews_slot_mutations_without_mutating_archive`、`runtime_session_archive_copies_slot_at_path_atomically`、`runtime_session_archive_previews_slot_copy_from_path_without_mutating_archive`、`runtime_session_archive_imports_single_slot_from_path_at_path_atomically`、`runtime_session_archive_previews_single_slot_import_from_path_without_mutating_archives`、`runtime_session_archive_saves_single_slot_archive_from_path_atomically`、`runtime_session_archive_saves_single_slot_archive_from_memory_atomically`、`runtime_session_archive_merges_archive_from_path_at_path_atomically` 与 `runtime_session_archive_previews_merge_from_path_without_mutating_archives`，确保 path-level rename/metadata/touch/remove、copy/save-as、single-slot import/export、merge preview/commit、source/target payload immutability 与 temp/bak cleanup 均被静态锁定；public path method names、archive 格式、path load/save flow、path-management behavior owners 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；conflict-marker 和 trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive query behavior static guard | runtime_session_archive_query_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已新增 `zircon_runtime/src/scene/tests/dynamic_scene_session/queries.rs` source anchor，锁定既有 path query/status 行为记录 `runtime_session_archive_path_query_code_check_passed_tests_deferred`、`runtime_session_archive_path_slot_summary_code_check_passed_tests_deferred`、`runtime_session_archive_path_manifest_filter_code_static_passed_cargo_blocked_external_postprocess_drift_tests_deferred`、`runtime_session_archive_path_slot_selection_code_static_passed_cargo_blocked_external_postprocess_drift_tests_deferred` 与 `runtime_session_archive_path_retention_preview_code_static_passed_cargo_blocked_external_postprocess_drift_tests_deferred`。该守卫覆盖 `runtime_session_archive_loads_statistics_from_path`、`runtime_session_archive_reads_slot_summaries_directly_from_path`、`runtime_session_archive_diffs_slot_from_path_without_mutating_target`、`runtime_session_archive_previews_path_retention_without_saving`、`runtime_session_archive_selects_updated_slots_directly_from_path` 与 `runtime_session_archive_filters_manifest_summaries_directly_from_path`，确保 persisted archive statistics、slot ids/contains/summary、diff world/level no-mutation、retention preview no-write/no-temp、latest/oldest/tag selector 与 manifest filter payload immutability 均被静态锁定；public query method names、archive 格式、path load semantics、query/path-query owners 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；conflict-marker 和 trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive load behavior static guard | runtime_session_archive_load_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已新增 `zircon_runtime/src/scene/tests/dynamic_scene_session/load.rs` source anchor，锁定既有 `runtime_session_archive_load_from_path_code_check_passed_tests_deferred` coverage row 与四组 load 行为测试：`runtime_session_archive_restores_slot_from_path_to_empty_world`、`runtime_session_archive_restores_slot_from_path_into_level_and_applies_metadata`、`runtime_session_archive_applies_slot_from_path_to_live_world_and_level` 与 `runtime_session_archive_path_load_helpers_report_missing_slot`。该守卫覆盖 path-to-empty-world restore、path-to-level restore + metadata、path-to-live-world/level apply remap、existing target entity preservation 与 missing-slot error boundary；public load/apply/restore method names、archive 格式、path load semantics、world/level mutation semantics 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；conflict-marker 和 trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive merge behavior static guard | runtime_session_archive_merge_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已新增 `zircon_runtime/src/scene/tests/dynamic_scene_session/merge.rs` source anchor，锁定既有 `runtime_05_dynamic_scene_merge_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending` coverage row 与两组 merge 行为测试：`runtime_session_archive_merge_preview_and_keep_existing_commit_are_side_effect_free` 和 `runtime_session_archive_path_merge_preview_commit_and_same_path_guard_are_atomic`。该守卫覆盖 archive-level `preview_merge_archive` / `merge_archive` KeepExisting side-effect boundary、incoming archive immutability、path-level loaded-incoming preview no-write、source-path atomic merge with ReplaceExisting、same-source/target path rejection、preserved target payload、persisted reload 与 temp/bak cleanup；public merge method names、archive 格式、target-path preflight、merge/path-merge behavior owners 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；conflict-marker 和 trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive selected capture behavior static guard | runtime_session_archive_selected_capture_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已锁定既有 selected capture 行为覆盖行 `runtime_05_dynamic_scene_selected_capture_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending` 与 `zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs` 锚点。该守卫覆盖 `runtime_session_archive_selected_capture_targets_resolved_slot_and_preserves_metadata` 与 `runtime_session_archive_selected_capture_to_path_previews_and_prunes_atomically`：in-memory selected world capture preview/commit 对 latest tagged slot 的 selector resolution、metadata override normalize、metadata-preserving selected capture 保留 display/tag/timestamp，以及 path-level selected capture with tag retention preview no-write、tag-prune report、atomic commit、reload manifest 与 temp/bak cleanup 均被纳入静态状态约束；selected capture public method names、archive 格式、path load/save flow、capture-retention behavior owners 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；conflict-marker 和 trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive selection behavior static guard | runtime_session_archive_selection_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已新增 `zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` source anchor，锁定既有 selection 行为覆盖行 `runtime_05_dynamic_scene_slot_selector_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending`、`runtime_05_dynamic_scene_selected_transfer_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending` 与 `runtime_05_dynamic_scene_selected_restore_apply_diff_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending`。该守卫覆盖 `runtime_session_archive_selector_resolves_in_memory_slots`、`runtime_session_archive_selected_retention_protects_resolved_slot`、`runtime_session_archive_selected_path_query_and_remove_are_atomic`、`runtime_session_archive_selected_metadata_update_targets_resolved_slot`、`runtime_session_archive_selected_transfer_helpers_use_resolved_slots`、`runtime_session_archive_selected_single_slot_export_to_path_is_atomic`、`runtime_session_archive_selected_restore_apply_and_diff_use_resolved_slots` 与 `runtime_session_archive_selected_path_restore_apply_and_diff_use_resolved_slots`，确保 typed selector、selected retention、metadata update、selected transfer/export、in-memory/path restore/apply/diff、path no-temp cleanup 与文档状态保持同步；public selected method names、archive 格式、selector semantics、path load/save flow、world/level mutation semantics 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；conflict-marker 和 trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive capture-retention transaction behavior anchors | runtime_session_archive_capture_retention_transaction_behavior_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 761 行后续项 `capture-retention protects captured slot`、`preview does not mutate archive/path` 与 `tag retention prunes only tagged bucket`：`zircon_runtime/src/scene/tests/dynamic_scene_session/retention.rs::runtime_session_archive_preview_capture_retention_prunes_clone_without_mutating_archive` 通过 `preview_capture_world_slot_with_tag_retention(...)` 断言 preview 只在 cloned archive 上加入 `autosave-new`、裁剪 `autosave-old`、保留 manual bucket，原 archive slot order 与 `!archive.contains_slot("autosave-new")` 保持不变；`runtime_session_archive_capture_retention_protects_captured_slot_before_pruning` 通过 `capture_world_slot_with_retention(...)` + `RuntimeSessionArchiveRetentionPolicy::keep_latest(0)` 断言 captured `quicksave` slot 被隐式保护且旧 slot 被裁剪。`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定测试锚、状态锚和模块文档锚；验证：rustfmt/static guard 见本轮记录，focused Cargo behavior test 因外部 Cargo lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive selected retention behavior anchors | runtime_session_archive_selected_retention_behavior_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 `selected retention protects latest/tagged slot`、`tag selected protection outside tag bucket is harmless` 与 `path selected retention preview does not write archive` 后续项：`zircon_runtime/src/scene/tests/dynamic_scene_session/retention.rs::runtime_session_archive_selected_retention_protects_latest_tagged_slot` 通过 `preview_prune_slots_with_selected_protection(...)` 与 `prune_slots_with_selected_protection(...)` 断言 `RuntimeSessionSlotSelector::latest_updated_with_tag(" manual ")` 解析到的 manual slot 被加入 protected set，即使 global `keep_latest(1)` 本会保留更新的 autosave slot；`runtime_session_archive_tag_selected_retention_ignores_protection_outside_bucket` 通过 tag-scoped selected retention 断言 bucket 外的 `manual-protected` 不挤占 autosave bucket 的 `max_slots` 预算，只裁剪 `autosave-old`；`runtime_session_archive_path_selected_retention_preview_does_not_write_archive` 通过 `preview_prune_slots_with_selected_protection_from_path(...)` 断言 path-level selected retention preview 返回 prune report 但保持 archive payload、loaded slot order 与 temp/bak leftovers 不变。public selected retention method names、archive 格式、tag bucket semantics、path preview no-write boundary 均不变；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定测试锚、状态锚和模块文档锚；验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；anchor/conflict/trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive mutation behavior static guard | runtime_session_archive_mutation_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已新增 `zircon_runtime/src/scene/tests/dynamic_scene_session/mutation.rs` source anchor，锁定既有 `runtime_05_dynamic_scene_mutation_behavior_anchors_rustfmt_passed_focused_cargo_timeout_no_result_scene_gate_pending` coverage row 与三组 mutation 行为测试：`runtime_session_archive_named_mutation_commits_preserve_preview_boundaries`、`runtime_session_archive_selected_mutations_resolve_targets_before_committing`、`runtime_session_archive_selected_path_mutations_preview_and_commit_atomically`。该守卫覆盖 named preview/commit boundary、duplicate rename rollback、selected mutation selector resolution、selected remove preview no-mutation、selected path-level preview no-write behavior、atomic selected path commit、persisted reload 与 temp/bak cleanup 锚点；public mutation method names、archive 格式、selector semantics、path load/save flow、mutation behavior owners 均不变。验证：rustfmt touched Rust 通过；standalone `runtime_absorption/dynamic_scene.rs` 1/1 通过；standalone `plan_status.rs` 33/33 通过；anchor/conflict/trailing-whitespace scans clean；scoped `git diff --check` 仅 LF/CRLF warnings；focused Cargo behavior test 因外部 `texture_target_render_order_feeds_later_primary_surface_material_sample` Cargo/rustc lane active deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive capture focused Cargo attempt | runtime_session_archive_capture_focused_cargo_timeout_no_result_residual_stopped | 2026-06-20 | 在本轮 rustfmt、standalone `runtime_absorption/dynamic_scene.rs` 1/1、standalone `plan_status.rs` 33/33 通过后，尝试执行 `cargo test -p zircon_runtime --lib scene::tests::dynamic_scene_session::capture --locked --jobs 1 --message-format short --color never`，命令 604s 超时且没有测试结果；超时后检测到 2 个 cargo 与 1 个 rustc 残留进程并已停止，随后又出现两轮 2 个 cargo 与 1 个 rustc 残留进程并再次停止，最终等待 10s 复查 no cargo/rustc processes。该尝试覆盖本轮 capture.rs 中 world capture preview/commit parity、level capture preview/from_level semantics 与 capture-retention shared preview projection 的 focused Cargo filter，但未关闭 Runtime 05 full `scene::` gate；persistence.rs 新增 full-save/parent-file preview 行为锚本轮仍只有 rustfmt/static guard 证据。 |
| M3 | 3.1 runtime session archive target parent-file preview preflight | runtime_session_archive_target_parent_file_preview_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 776 行后续项 `parent-file target preview 行为锚点`，并覆盖 777 行 `target parent-file preflight parity`：`zircon_runtime/src/scene/tests/dynamic_scene_session/persistence.rs::runtime_session_archive_preview_save_to_path_rejects_parent_file_without_writes` 构造 parent path is a file 的 target，调用 `preview_save_to_path(&target_path)`，断言返回 `RuntimeSessionArchiveError::Io` / `AlreadyExists`，同时 parent file payload 保持不变、parent 仍是 file、child target 不存在。`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定该测试锚、状态锚和 `docs/zircon_runtime/scene/dynamic_scene.md` 文档锚；验证：rustfmt/static guard 见本轮记录，focused Cargo behavior test 仍因 active cargo/rustc lanes deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive full-save preview no-write target behavior | runtime_session_archive_full_save_preview_no_write_targets_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 778 行后续项 `missing-target、existing-file、non-file-target 与 no-write/no-temp 行为锚点`：`zircon_runtime/src/scene/tests/dynamic_scene_session/persistence.rs::runtime_session_archive_preview_save_to_path_reports_targets_without_writing_files` 覆盖 `preview_save_to_path(&missing_path)` 不创建 parent/target 且 `will_replace_target = false`，`preview_save_to_path(&existing_path)` 返回 `will_replace_target = true` 且不改 existing payload / 不留下 temp 文件，以及 `preview_save_to_path(&directory_target)` 对 non-file target 返回 `RuntimeSessionArchiveError::Io` 并保持目录目标不被写入。`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定该测试锚、状态锚和 `docs/zircon_runtime/scene/dynamic_scene.md` 文档锚；验证：rustfmt/static guard 见本轮记录，focused Cargo behavior test 仍因 active cargo/rustc lanes deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive capture-retention shared preview projection | runtime_session_archive_capture_retention_shared_preview_projection_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 719 行后续项 `capture-retention keeps shared preview report projection` 的行为锚：`zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs::runtime_session_archive_capture_retention_reuses_shared_preview_report_projection` 先调用普通 `preview_capture_world_slot(" manual-mid ", &captured_world, metadata.clone())`，再调用 `preview_capture_world_slot_with_tag_retention(...)`，并断言 `RuntimeSessionArchiveCaptureRetentionReport.capture` 与普通 capture preview report 完全一致，同时 tag-retention preview 只在 cloned archive 上报告 prune/manifest 结果，原 archive slot order 与 `manual-mid.updated_at_unix_millis = Some(20)` 保持不变。`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定该测试锚、状态锚和 `docs/zircon_runtime/scene/dynamic_scene.md` 文档锚；验证：rustfmt/static guard 见本轮记录，focused Cargo behavior test 仍因 active cargo/rustc lanes deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive level capture preview/from_level semantics | runtime_session_archive_level_capture_preview_from_level_semantics_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 719 行后续项 `level capture preview preserves RuntimeSessionSlot::from_level semantics` 的行为锚：`zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs::runtime_session_archive_level_capture_preview_preserves_from_level_semantics` 先用 `RuntimeSessionSlot::from_level(" level-preview ", &level)` 建立 canonical level slot，再调用 `preview_capture_level_slot(" level-preview ", &level)`，并断言 preview 的 normalized slot id、metadata、entity/resource counts 与 canonical slot projection 一致，同时原 archive 仍为空。`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定该测试锚、状态锚和 `docs/zircon_runtime/scene/dynamic_scene.md` 文档锚；验证：rustfmt/static guard 见本轮记录，focused Cargo behavior test 仍因 active cargo/rustc lanes deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 runtime session archive world capture preview/commit parity | runtime_session_archive_world_capture_preview_commit_parity_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | 补齐 718 行后续项 `world capture commit delegates to preview-generated slot` 的行为锚：`zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs::runtime_session_archive_world_capture_commit_matches_preview_generated_slot` 先调用 `preview_capture_world_slot(" manual ", &source, metadata.clone())`，再以同一 canonical slot id/metadata 调用 `capture_world_slot(" manual ", &source, metadata)`，并断言 commit 后 manifest summary 的 metadata/entity/resource counts 与 preview-generated slot projection 一致，同时原 archive 仍保持 `updated_at_unix_millis = Some(20)`。`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 已同步锁定该测试锚、状态锚和 `docs/zircon_runtime/scene/dynamic_scene.md` 文档锚；验证：rustfmt/static guard 见本轮记录，focused Cargo behavior test 仍因 active cargo/rustc lanes deferred，Runtime 05 full `scene::` closeout gate 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview resource preflight details status guard | dynamic_scene_patch_preview_resource_preflight_details_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview resource preflight details status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 resource preflight details 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 `ScenePatchPreviewResource`、`ScenePatchPreviewReport.resources`、`type_path` / `already_present` / `can_create_on_apply` / `field_count` DTO 字段、`resources_requiring_creation()` iterator、`preview.resources[0]` 行为锚和 `dynamic_scene_patch_preview_resource_preflight_details_static_passed_cargo_deferred_tests_deferred` 状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview resource ensure creation status guard | dynamic_scene_patch_preview_resource_ensure_creation_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview resource ensure creation status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 resource ensure creation 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 ensure-backed preview 行为锚 `register_frame_counter_resource_with_ensure`、`frame_counter_adapter_with_ensure`、`frame_counter_ensure`、`preview_with_ensure.resources[0].can_create_on_apply`、`target_with_ensure.get_resource::<FrameCounter>().is_none()`、`resources_requiring_creation()` 和 `dynamic_scene_patch_preview_resource_ensure_creation_static_passed_cargo_deferred_tests_deferred` 状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview resource ensure creation | dynamic_scene_patch_preview_resource_ensure_creation_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | Strengthened the reflected-resource preview anchor with an ensure-backed `FrameCounter` resource adapter. `ScenePatch::preview_apply(...)` now has behavior coverage for the boundary where the target `World` has a registered resource adapter with an `ensure` hook but no resource instance: preview reports `already_present = false`, `can_create_on_apply = true`, `field_count = 1`, and exposes the row through `resources_requiring_creation()` while leaving the target resource absent. The same patch then applies through `ScenePatch::apply(...)`, where the adapter `ensure` hook creates the missing resource before reflected fields are written. This confirms the preview report mirrors apply creation semantics without invoking the mutating hook. `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` locks the ensure-backed helper anchors and documentation/status rows. Validation passed: rustfmt touched Rust passed; standalone rustc guard `runtime_absorption/dynamic_scene.rs` 1/1 passed; conflict-marker scan clean; trailing-whitespace scan clean; scoped `git diff --check` only LF/CRLF warnings. Full Cargo and focused behavior tests remain deferred, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview resource preflight details | dynamic_scene_patch_preview_resource_preflight_details_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | Added `ScenePatchPreviewResource` and `ScenePatchPreviewReport.resources` so runtime hosts can inspect reflected resource preflight results before `ScenePatch::apply(...)` mutates the target `World`. Each row exposes `type_path`, `already_present`, `can_create_on_apply`, and `field_count`; `resources_requiring_creation()` filters resources that are absent now but can be created by the resource adapter's `ensure` hook during apply. `DynamicScene::preview_spawn_into(...)` now collects resource rows through the same read-only registry/adapter/field/remap validation used by resource apply preflight and still does not call `ensure`, write fields, create resources, install descriptors, or insert nodes. The existing reflected-resource behavior anchor now previews a `FrameCounter` patch, asserts the resource row, and verifies preview leaves the target resource value unchanged before apply. `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` locks the public DTO/export, report field, resource preflight source anchors, behavior anchors, and module docs. Validation passed: rustfmt touched Rust passed; standalone rustc guard `runtime_absorption/dynamic_scene.rs` 1/1 passed; conflict-marker scan clean; trailing-whitespace scan clean; scoped `git diff --check` only LF/CRLF warnings. Full Cargo and focused behavior tests remain deferred, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview component type install details | dynamic_scene_patch_preview_component_type_install_details_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | Added `ScenePatchPreviewComponentType` and `ScenePatchPreviewReport.component_types` so runtime hosts can show per-descriptor install diagnostics without cloning the full plugin property schema into the preview DTO. Each row exposes `type_id`, `plugin_id`, `display_name`, and `already_registered`; `ScenePatchPreviewReport::new_component_types()` filters rows that a later apply would install. The preview path builds these rows from `DynamicScene.component_types` and target `world.component_type_descriptor(...)` after descriptor compatibility validation and remains non-mutating. The focused behavior anchor now asserts the plugin-owned descriptor detail row and `new_component_types()` iterator while preserving target-world immutability, exact remap rows, install counts, and component workload counts. `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` locks the public DTO/export, report field, source anchors, behavior anchors, and module docs. Validation passed: rustfmt touched Rust passed; standalone rustc guard `runtime_absorption/dynamic_scene.rs` 1/1 passed; conflict-marker scan clean; trailing-whitespace scan clean; scoped `git diff --check` only LF/CRLF warnings. Full Cargo and focused behavior tests remain deferred, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview component type install details status guard | dynamic_scene_patch_preview_component_type_install_details_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview component type install details status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 component type install details 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 `ScenePatchPreviewComponentType`、`ScenePatchPreviewReport.component_types`、`type_id` / `plugin_id` / `display_name` / `already_registered` DTO 字段、`new_component_types()` iterator、`preview.component_types[0]` 行为锚和 `dynamic_scene_patch_preview_component_type_install_details_static_passed_cargo_deferred_tests_deferred` 状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview component type install counts | dynamic_scene_patch_preview_component_type_install_counts_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | Extended `ScenePatchPreviewReport` with `existing_component_type_count`, `new_component_type_count`, and `has_new_component_types()` so runtime hosts can preview which component type descriptors are already compatible in the target `World` and which descriptors a later `ScenePatch::apply(...)` would install. `DynamicScene::preview_spawn_into(...)` computes the counts from `world.component_type_descriptor(...)` after the existing read-only descriptor compatibility check and still does not install descriptors, insert nodes, write components/resources, or create resources. The focused behavior anchor now leaves the target without the plugin-owned descriptor and asserts `new_component_type_count = 1` while preserving target-world immutability, exact remap rows, and component workload counts. `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` locks the public fields, helper, source anchors, behavior anchor, and module documentation. Validation passed: rustfmt touched Rust passed; standalone rustc guard `runtime_absorption/dynamic_scene.rs` 1/1 passed; conflict-marker scan clean; scoped `git diff --check` only LF/CRLF warnings. Full Cargo and focused behavior tests remain deferred, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview component type install counts status guard | dynamic_scene_patch_preview_component_type_install_counts_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview component type install counts status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 component type install counts 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 `ScenePatchPreviewReport.existing_component_type_count`、`new_component_type_count`、`has_new_component_types()`、`world.component_type_descriptor(...)` 只读 descriptor 计数、`preview.new_component_type_count` 行为锚和 `dynamic_scene_patch_preview_component_type_install_counts_static_passed_cargo_deferred_tests_deferred` 状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview reflection preflight | dynamic_scene_patch_preview_reflection_preflight_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `DynamicScene::preview_spawn_into(...)` now runs read-only reflection schema preflight before returning a `ScenePatchPreviewReport`: fixed component payloads resolve target-world runtime registrations, component kind, component adapter presence, editable/serializable fields, and remappable field values; resource payloads resolve resource kind, resource adapter presence, field metadata, remappable values, and reject missing target resources when the adapter has no `ensure` hook; plugin-owned dynamic components still validate through JSON conversion without requiring a fixed adapter. The preview path remains non-mutating and still does not install descriptors, insert nodes, write components/resources, or create missing resources. `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` locks the preflight helpers and read-only anchors. Documentation updated in `docs/zircon_runtime/scene/dynamic_scene.md`. Validation passed: rustfmt touched Rust passed; standalone rustc guard `runtime_absorption/dynamic_scene.rs` 1/1 passed; conflict-marker scan clean; scoped `git diff --check` only LF/CRLF warnings. Full Cargo and focused behavior tests remain deferred, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview reflection preflight status guard | dynamic_scene_patch_preview_reflection_preflight_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview reflection preflight status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 reflection preflight 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 `validate_components_are_previewable(...)`、`validate_resources_are_previewable(...)`、fixed component/resource runtime registration checks、adapter availability checks、`ReflectError::MissingResource`、read-only field remap conversion和 `dynamic_scene_patch_preview_reflection_preflight_static_passed_cargo_deferred_tests_deferred` 状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview component workload | dynamic_scene_patch_preview_component_workload_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | Extended `ScenePatchPreviewReport` with `component_instance_count`, computed from the same read-only `DynamicScene.entities[*].components` payload that `spawn_into(...)` later applies. The focused behavior anchor now includes a plugin-owned dynamic component and asserts both `component_type_count = 1` and `component_instance_count = 1` while preserving target-world immutability and exact remap rows. `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` now locks the new field and the existing `ScenePatchPreviewEntityRemap` public export. Documentation updated in `docs/zircon_runtime/scene/dynamic_scene.md`. Validation passed: rustfmt touched Rust passed; standalone rustc guard `runtime_absorption/dynamic_scene.rs` 1/1 passed; conflict-marker scan clean; scoped `git diff --check` only LF/CRLF warnings. Full Cargo and focused behavior tests remain deferred, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview component workload status guard | dynamic_scene_patch_preview_component_workload_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview component workload status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 component workload 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 `ScenePatchPreviewReport.component_instance_count`、只读 `DynamicScene.entities[*].components` 计数投影、`preview.component_instance_count` 行为锚和 `dynamic_scene_patch_preview_component_workload_static_passed_cargo_deferred_tests_deferred` 状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview remap details | dynamic_scene_patch_preview_remap_details_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | Extended `ScenePatchPreviewReport` with public `ScenePatchPreviewEntityRemap` rows and exported the DTO through `zircon_runtime::scene` / `scene::dynamic_scene`, so `ScenePatch::preview_apply(...)` and `DynamicScene::preview_spawn_into(...)` expose the exact planned `source_entity -> target_entity` remap list in addition to preserved/remapped counts. The preview path still reuses the existing read-only remap planner and does not install descriptors, insert nodes, write components, or write resources. Behavior anchor `scene_patch_preview_reports_remaps_without_mutating_target_world` now asserts the exact remap rows for a collision case while keeping the target world unchanged. Documentation updated in `docs/zircon_runtime/scene/dynamic_scene.md`. Validation: rustfmt touched Rust passed; API/documentation anchor scan passed; conflict-marker scan clean; scoped `git diff --check` passed with LF/CRLF warnings only. Full Cargo and focused behavior tests remain deferred under the implementation-first direction, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene patch preview remap status guard | dynamic_scene_patch_preview_remap_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview remap status guard` 已提升到 runtime 总索引状态表与 status-output expectations，避免 remap-details 完成项只存在 Runtime 05 子计划；`runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 现在同时锁定 `ScenePatchPreviewEntityRemap`、`source_entity` / `target_entity`、`ScenePatchPreviewReport.entity_remaps`、`has_entity_remaps()`、read-only remap projection和 `scene_patch_preview_reports_remaps_without_mutating_target_world` 行为锚。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust/status files passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 33/33 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview status guard | dynamic_scene_patch_preview_status_guard_static_passed_cargo_pending | 2026-06-20 | `Runtime 05 dynamic scene patch preview status guard` / `Runtime 05 dynamic scene patch preview API` 已提升到 runtime 总索引状态表与 status-output expectations，避免完成项只存在 Runtime 05 子计划；新增 `runtime_absorption::dynamic_scene::runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 锁定 `ScenePatchPreviewReport` public facade、`ScenePatch::preview_apply` -> `DynamicScene::preview_spawn_into` route、`scene/spawn.rs` read-only preview helper、`scene_patch_preview_reports_remaps_without_mutating_target_world` 行为锚和动态场景文档/状态锚点。该切片只补状态/结构守卫，不改变 runtime behavior；验证：rustfmt touched Rust passed；standalone `runtime_absorption/dynamic_scene.rs` 1/1 passed；standalone `plan_status.rs` 32/32 passed；`runtime_plan_status_boundary.py` py_compile passed；direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]` / Runtime 05 `in_progress`；conflict-marker scan clean；scoped `git diff --check` 仅 LF/CRLF warnings。Runtime 05 full `scene::` Cargo closeout 仍 pending。 |
| M3 | 3.1 dynamic scene patch preview API | dynamic_scene_patch_preview_api_static_passed_cargo_timeout_no_result_tests_deferred | 2026-06-20 | Added `ScenePatchPreviewReport`, `ScenePatch::preview_apply`, and `DynamicScene::preview_spawn_into` so Runtime 05 hosts can inspect component type, entity, resource, target-entity, preserved-entity, and remapped-entity counts before applying a `ScenePatch` to a target `World`. The preview path lives under `scene/spawn.rs`, reuses support validation, component descriptor conflict checks, entity remap planning, and parent remap validation, and deliberately avoids descriptor installation, node insertion, component writes, and resource writes. Behavior anchor: `scene_patch_preview_reports_remaps_without_mutating_target_world` records remap diagnostics and verifies the target world is unchanged after preview. Validation: rustfmt touched Rust passed; API/documentation anchor scan passed; conflict-marker scan clean; scoped `git diff --check` reported only LF/CRLF normalization warnings. A lightweight `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-patch-preview-0620 --message-format short --color never` attempt timed out after 304s with no result, and focused behavior tests remain deferred under the implementation-first direction, so Runtime 05 full `scene::` gate remains pending. |
| M3 | 3.1 dynamic scene document serialization owner split | dynamic_scene_document_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/document.rs` 已硬切为 folder-backed `document/{mod,legacy,read,write}.rs`；`document/read.rs` owns `DynamicScene::from_versioned_json` parse、legacy `ProjectDocument { world }` migration 与 `ensure_supported` validation，`document/write.rs` owns `DynamicScene::to_versioned_json_pretty`，`document/legacy.rs` owns legacy DTO。Public method names、one-way migration behavior、dynamic-scene JSON schema 与 scene/editor 边界语义不变；`scene_project_serialization_sources_do_not_store_editor_authoring_state` 已同步扫描拆分后的 `document/` owner 文件。验证：rustfmt touched Rust 通过；旧 flat `document.rs` absent、新 `document/mod.rs` present；conflict-marker、trailing-whitespace 与 scoped diff checks 通过；standalone plan-status 32/32 通过。Cargo 与 focused behavior tests 按“首先实现功能，测试可以暂时延后”方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 dynamic scene scene-asset bridge owner split | dynamic_scene_scene_asset_bridge_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/scene_asset.rs` 已硬切为 folder-backed `scene_asset/{mod,dynamic_scene,error,prepared_spawn}.rs`；`scene_asset/dynamic_scene.rs` owns imported `SceneAsset` / URI -> `DynamicScene` conversion through `SceneAssetSerializer`，`scene_asset/prepared_spawn.rs` owns `PreparedDynamicSceneSpawn::from_scene_asset(_uri)`，`scene_asset/error.rs` owns shared `DynamicSceneError::SceneAsset` wrapping。`DynamicScene::from_scene_asset(_uri)`、prepared-payload constructors、asset reload scheduling consumption、dynamic-scene validation/apply path 与 scene/editor 边界语义不变；未触碰 importer/artifact cache/watcher/render/editor/plugin 行为。验证：rustfmt touched Rust 通过；旧 flat `scene_asset.rs` absent、新 `scene_asset/mod.rs` present；conflict-marker、trailing-whitespace 与 scoped diff checks 通过；standalone plan-status 32/32 通过。Cargo 与 focused behavior tests 按“首先实现功能，测试可以暂时延后”方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 dynamic scene entity declaration owner split | dynamic_scene_entity_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/entity.rs` 已硬切为 folder-backed `entity/{mod,dynamic_component,dynamic_entity,dynamic_resource}.rs`；`entity/mod.rs` 保留 `DynamicComponent`、`DynamicEntity`、`DynamicResource` public exports，三个 declaration owners 分别持有 serde/reflection imports、字段定义与 constructor。`scene::dynamic_scene` re-export surface、dynamic-scene JSON schema、capture/apply 行为与 scene/editor 边界语义不变；`scene_project_serialization_sources_do_not_store_editor_authoring_state` 已同步扫描拆分后的 `entity/`、`scene/`、`value/` owner 文件。验证：rustfmt touched Rust 通过；旧 flat `entity.rs` absent、新 `entity/mod.rs` present；conflict-marker、trailing-whitespace 与 scoped diff checks 通过；standalone plan-status 32/32 通过。Cargo 与 focused behavior tests 按“首先实现功能，测试可以暂时延后”方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 dynamic scene value conversion owner split | dynamic_scene_value_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/value.rs` 已硬切为 folder-backed `value/{mod,json,remap}.rs`；`value/mod.rs` 继续保留 `scene/spawn.rs` 消费的内部 helper surface，`value/json.rs` owns reflected fields -> JSON object conversion、finite float validation 与 unsupported value error，`value/remap.rs` owns `ReflectedValue` entity remap 与 JSON `{ "entity": ... }` remap。`DynamicScene::spawn_into` 行为、dynamic-scene JSON schema、`scene::dynamic_scene` re-export surface 与 asset reload prepared-payload apply path 不变；未触碰 editor/plugin/render/session 行为。验证：rustfmt touched Rust 通过；旧 flat `value.rs` absent、新 `value/mod.rs` present；conflict-marker、trailing-whitespace 与 scoped diff checks 通过；standalone plan-status 32/32 通过。Cargo 与 focused behavior tests 按“首先实现功能，测试可以暂时延后”方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 dynamic scene spawn task owner split | dynamic_scene_spawn_task_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/spawn_task.rs` 已硬切为 folder-backed `spawn_task/{mod,loader,prepared,task}.rs`；`spawn_task/mod.rs` 保留 `PreparedDynamicSceneSpawn` 与 `DynamicSceneSpawnTask` public exports，`prepared.rs` owns validated payload counts 与 caller-thread `spawn_into`，`task.rs` owns descriptor/status/result collection，`loader.rs` owns `JobScheduler` scheduling 以及 scene/JSON/path/scene-asset loader variants。`scene::dynamic_scene` re-export surface、asset reload prepared-payload consumption 与 ECS world mutation 时机不变；未触碰 editor/plugin/render/session 行为。验证：rustfmt touched Rust 通过；旧 flat `spawn_task.rs` absent、新 `spawn_task/mod.rs` present；conflict-marker、trailing-whitespace 与 scoped diff checks 通过；standalone plan-status 32/32 通过。Cargo 与 focused behavior tests 按“首先实现功能，测试可以暂时延后”方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 dynamic scene root scene owner split | dynamic_scene_scene_owner_split_static_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/scene/dynamic_scene/scene.rs` 已硬切为 folder-backed `scene/{mod,capture,spawn,validation}.rs`；`scene/mod.rs` 保留 `DynamicScene` 数据结构、`DYNAMIC_SCENE_FORMAT_VERSION`、`DynamicScene::empty`、`DynamicScene::from_world`、`DynamicScene::spawn_into` 和 `DynamicScene::ensure_supported` public facade，capture/validation/spawn owner 分别接管 world snapshot capture、format/source/component descriptor validation 与 entity remap/component/resource apply。`scene::dynamic_scene` re-export surface、archive embedding 和 JSON schema 不变；未触碰 editor/plugin/render 行为。验证：rustfmt touched Rust 通过；旧 flat `scene.rs` absent、新 `scene/mod.rs` present；conflict-marker、trailing-whitespace 与 scoped diff checks 通过；standalone plan-status 32/32 通过。Cargo 与 focused behavior tests 按“首先实现功能，测试可以暂时延后”方向 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| 横切 | Runtime 05 dynamic scene session owner-tree guard | dynamic_scene_session_owner_tree_guard_static_passed_cargo_pending | 2026-06-20 | `dynamic_scene_session_owner_tree_stays_folder_backed_after_runtime_05_cutover` now locks the Runtime 05 session archive owner tree after the hard cutover: `session/mod.rs remains structural`, `session/{metadata,manifest,query,reports,merge,retention,slot_copy,slot_export,slot_import,slot_mutation,capture_retention}` and sibling session owners must stay folder-backed, and `retired flat session owner files stay absent` including old `dynamic_scene/session.rs` plus retired flat child owners. Validation: rustfmt check, standalone component_structure 21/21, standalone plan-status 32/32, direct `runtime_plan_status_boundary_audit` risks=[] support 79/79 status_table_gaps=[], conflict-marker scan clean, scoped `git diff --check` only LF/CRLF warnings; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 dynamic scene root owner-tree guard | dynamic_scene_root_owner_tree_guard_static_passed_cargo_pending | 2026-06-20 | `dynamic_scene_root_owner_tree_stays_folder_backed_after_runtime_05_cutover` now locks the Runtime 05 dynamic-scene root owner tree after the hard cutover: `dynamic_scene/mod.rs` remains structural, `document/entity/scene/scene_asset/spawn_task/value` must stay folder-backed, `retired flat dynamic_scene owner files stay absent` covers old `dynamic_scene/{document,entity,scene,scene_asset,spawn_task,value}.rs` files, and `scene/mod.rs keeps DynamicScene facade` anchors (`DYNAMIC_SCENE_FORMAT_VERSION`, `empty`, `from_world`, `spawn_into`, `ensure_supported`). Validation: rustfmt check, standalone component_structure 23/23, standalone plan-status 32/32, direct `runtime_plan_status_boundary_audit` risks=[] support 79/79 missing_support_files=[] status_table_gaps=[], conflict-marker scan clean, scoped `git diff --check` only LF/CRLF warnings; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 10-13 row-data group split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_10_13.rs` is now a structural entrypoint that delegates Runtime 10、11、12、13 evidence rows to `runtime_10_13/{runtime_10,runtime_11,runtime_12,runtime_13}.rs`; concrete support owners are `runtime_10_13/runtime_10.rs`, `runtime_10_13/runtime_11.rs`, `runtime_10_13/runtime_12.rs`, and `runtime_10_13/runtime_13.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 40/40. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 10 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_10_13/runtime_10.rs` is now a structural entrypoint that delegates Runtime 10 evidence rows to `runtime_10_13/runtime_10/{dynamic_api,session,ui_contract}.rs`; concrete support owners are `runtime_10/dynamic_api.rs`, `runtime_10/session.rs`, and `runtime_10/ui_contract.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those three Runtime 10 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 65/65. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 65/65, standalone plan-status 32/32; Runtime 10 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output Runtime 12 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_10_13/runtime_12.rs` is now a structural entrypoint that delegates Runtime 12 evidence rows to `runtime_10_13/runtime_12/{baseline,action_mapping,gamepad,host_recording}.rs`; concrete support owners are `runtime_12/baseline.rs`, `runtime_12/action_mapping.rs`, `runtime_12/gamepad.rs`, and `runtime_12/host_recording.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four Runtime 12 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 69/69. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 69/69, standalone plan-status 32/32; Runtime 12 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output support-structure owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/support_structure.rs` is now a structural entrypoint that delegates Runtime 05 support-structure evidence rows to `runtime_05/support_structure/{plan_status_modules,status_output_splits}.rs`; concrete owners separate plan-status module support rows from status-output owner-split rows. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes both Runtime 05 support-structure owner groups separately, `Runtime 05 support rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 71/71. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 71/71, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output scene-closeout owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/scene_closeout.rs` is now a structural entrypoint that delegates Runtime 05 scene closeout evidence rows to `runtime_05/scene_closeout/{dynamic_scene_rows,full_scene_gate_rows,source_guard_rows}.rs`; concrete owners separate dynamic-scene row data, full `scene::` gate/diagnostic rows, and serialization/editor-projection source guards. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes all three Runtime 05 scene-closeout owner groups separately, `Runtime 05 scene closeout rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 79/79. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 79/79, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output audit-metadata owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/audit_metadata.rs` is now a structural entrypoint that delegates Runtime 05 audit metadata evidence rows to `runtime_05/audit_metadata/{plan_coverage_rows,runtime_02_03_rows,runtime_07_rows}.rs`; concrete owners separate plan coverage/Cargo status, Runtime 02/03 status, and Runtime 07 status rows. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes all three Runtime 05 audit owner groups separately, `Runtime 05 audit rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 74/74. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 74/74, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output cargo-gates owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05/cargo_gates.rs` is now a structural entrypoint that delegates Runtime 05 cargo gate evidence rows to `runtime_05/cargo_gates/{early_rows,late_rows}.rs`; concrete owners separate Runtime 01-04/06-08 early Cargo gate rows from Runtime 10-14 late Cargo gate rows. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes both Runtime 05 cargo gate owner groups separately, `Runtime 05 cargo gate rows owner groups separately` is the status-output evidence anchor, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 76/76. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 76/76, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 01-04 row-data group split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_01_04.rs` is now a structural entrypoint that delegates Runtime 01、02、03、04 evidence rows to `runtime_01_04/{runtime_01,runtime_02,runtime_03,runtime_04}.rs`; concrete support owners are `runtime_01_04/runtime_01.rs`, `runtime_01_04/runtime_02.rs`, `runtime_01_04/runtime_03.rs`, and `runtime_01_04/runtime_04.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 44/44. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output status/date owner split | status_table_static_passed_cargo_pending | 2026-06-20 | `status_output_tables/expected_slices.rs` is now a structural entrypoint that delegates row status mapping to `expected_slices/status.rs` and row completion-date mapping to `expected_slices/date.rs`; `status_output_tables.rs` still calls `expected_status_for_slice` and `expected_date_for_slice` through the same module boundary. `runtime_plan_status_support_inventory.py` now tracks plan-status support files 46/46. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 05 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_05.rs` is now a structural entrypoint that delegates Runtime 05 evidence rows to `runtime_05/{baseline,cross_runtime_rows,support_structure,scene_closeout,cargo_gates,audit_metadata}.rs`; concrete support owners include `runtime_05/scene_closeout.rs`, `runtime_05/cargo_gates.rs`, and `runtime_05/audit_metadata.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those six owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 52/52. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 14 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_14.rs` is now a structural entrypoint that delegates Runtime 14 evidence rows to `runtime_14/{audit_sync,cargo_gates,guard_anchors}.rs`; concrete support owners are `runtime_14/audit_sync.rs`, `runtime_14/cargo_gates.rs`, and `runtime_14/guard_anchors.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those three Runtime 14 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 55/55. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 55/55, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 05 status-output Runtime 07 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_06_09/runtime_07.rs` is now a structural entrypoint that delegates Runtime 07 evidence rows to `runtime_06_09/runtime_07/{performance,asset_render,scene_asset,owner_budget}.rs`; concrete support owners include `runtime_07/performance.rs`, `runtime_07/asset_render.rs`, `runtime_07/scene_asset.rs`, and `runtime_07/owner_budget.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four Runtime 07 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 59/59. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 59/59, standalone plan-status 32/32; Runtime 07 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output Runtime 09 row-data family split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_06_09/runtime_09.rs` is now a structural entrypoint that delegates Runtime 09 evidence rows to `runtime_06_09/runtime_09/{baseline,layout_pipeline,legacy_renames}.rs`; concrete support owners include `runtime_09/baseline.rs`, `runtime_09/layout_pipeline.rs`, and `runtime_09/legacy_renames.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` keeps consuming the Runtime 09 owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 62/62. Validation: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[] support 62/62, standalone plan-status 32/32; Runtime 09 Cargo gates and full `scene::` closeout remain pending. |
| 横切 | Runtime 05 status-output Runtime 06-09 row-data group split | status_table_static_passed_cargo_pending | 2026-06-20 | `expected_status_row_data/runtime_06_09.rs` is now a structural entrypoint that delegates Runtime 06、07、08、09 evidence rows to `runtime_06_09/{runtime_06,runtime_07,runtime_08,runtime_09}.rs`; concrete support owners are `runtime_06_09/runtime_06.rs`, `runtime_06_09/runtime_07.rs`, `runtime_06_09/runtime_08.rs`, and `runtime_06_09/runtime_09.rs`. `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS` now consumes those four owner groups separately, and `runtime_plan_status_support_inventory.py` tracks plan-status support files 36/36. Validation target: rustfmt check, Python py_compile, direct `runtime_plan_status_boundary_audit` risks=[], standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| 横切 | Runtime 01 export fatal status-output coverage sync | runtime_01_export_fatal_status_output_coverage_static_passed | 2026-06-20 | The all-index-row status-output guard exposed two additional 2026-06-20 Runtime 01 export fatal rows that were already present in `docs/plans/zircon_runtime/runtime/index.md`: `Runtime 01 export materialization fatal preflight gate` and `Runtime 01 editor native-aware fatal export early exit`. `status_output_tables/expected_slices.rs` now maps both rows to their expected status/date, and `expected_status_row_data/runtime_01_04.rs` locks their `effective_fatal_diagnostics()`, `materialization-blocked diagnostic`, `plan.has_fatal_diagnostics()`, no-op materialize report, and NativeDynamic staging/build early-exit evidence anchors. Runtime 01's subplan also records `Runtime 01 export fatal status-output coverage sync`, updates `last_refined` to `2026-06-20`, and restores the runtime index Runtime 01 row's `tech_stack/text_shaper/plugin physics Cargo gates` anchor while keeping `export_build_plan Cargo gate` separately pending. Validation: `rustfmt --edition 2021 --check` passed for the two touched status-output expectation files; standalone `plan_status` status-output filter first failed on the uncovered editor fatal row, then passed 2/2 after the missing row anchors were added; full standalone `plan_status` first exposed the Runtime 01 metadata drift and is rerun after this row. This is a status/output guard sync only; no Runtime 01 export Cargo gate is promoted and Runtime 05 remains `pending_full_scene_cargo`. |
| 横切 | Runtime 05 full scene gate status-output index sync | runtime_05_full_scene_gate_status_output_index_sync_static_passed | 2026-06-20 | Synchronized the fresh Runtime 05 full `scene::` gate result into the runtime index status table as `Runtime 05 full scene compile-pass graphics-scene blocker`, preserving the same 2026-06-20 evidence from `.codex/tmp/zircon_runtime_scene_closeout_20260620_040700.{out,err}.log`: lib-test compilation passed, the filtered test binary reported `running 1074 tests`, active render-owned `graphics::scene::*` failures remain, and the process ended with `STATUS_ACCESS_VIOLATION`. Updated `zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_slices.rs` and `expected_status_row_data/runtime_05.rs` so the all-index-row status-output guard covers the new row. While validating the guard, the same all-index-row check exposed three pre-existing 2026-06-20 Runtime 01 export materialization status rows in `docs/plans/zircon_runtime/runtime/index.md` that lacked status-output expectations; `expected_slices.rs` and `expected_status_row_data/runtime_01_04.rs` now cover those rows too. Validation: `rustfmt --edition 2021 --check` passed for the touched status-output expectation files; standalone `plan_status` status-output filter passed 2/2 with `CARGO_MANIFEST_DIR` set to `zircon_runtime`; conflict-marker scan passed; scoped `git diff --check` reported only LF-to-CRLF notices. Runtime 05 remains `pending_full_scene_cargo`; no completion status is promoted. |
| M3 | 3.1 Runtime 05 full scene gate compile-pass graphics-scene blocker | runtime_05_full_scene_gate_compile_passed_graphics_scene_tests_failed_scene_gate_pending | 2026-06-20 | Fresh full gate `cargo test -p zircon_runtime --lib scene:: --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --test-threads=1 --nocapture` wrote `.codex/tmp/zircon_runtime_scene_closeout_20260620_040700.{out,err}.log`. The gate passed the lib-test compile frontier after 40m12s with existing warnings, then ran `scene::`-filtered tests (`running 1074 tests`). It failed in the active render/graphics owner area, not in the Runtime 05 scene/ECS/dynamic-scene fixes: failing tests were `graphics::scene::render_product_material_property_tests::render_product_material_properties_prepare_uniform_payload`, `graphics::scene::render_product_streamer_tests::readiness_diagnostics::render_product_streamer_dependency_readiness_change_invalidates_material_cache`, `graphics::scene::render_product_zshader_import_tests::render_product_streamer_reports_imported_zshader_material_layout_abi_diagnostics`, `graphics::scene::scene_renderer::core::runtime_features::runtime_features_from_pipeline::tests::pluginized_rendering_feature_names_drive_runtime_post_process_flags`, `graphics::scene::scene_renderer::core::scene_renderer_core_render_compiled_scene::render::render::tests::active_sprite_graph_stages_follow_unculled_sprite_passes`, and four `graphics::scene::scene_renderer::graph_execution::materialization::tests::*` cases. The process ended with `STATUS_ACCESS_VIOLATION`, so no pass/fail total summary is available. This records that the Runtime 05 compile gate is clear on the current worktree, but full closeout remains `pending_full_scene_cargo` until the render lane clears the graphics-scene failure cluster and the same full gate is rerun green. |
| M3 | 3.1 dynamic scene restore/query session re-export boundary sync | runtime_05_dynamic_scene_restore_query_reexport_boundary_static_passed_scene_gate_pending | 2026-06-20 | Follow-up full `scene::` Cargo attempts after the motion-vector camera test compile repair exposed the next folder-backed `RuntimeSessionArchive` session split drift. `.codex/tmp/zircon_runtime_scene_closeout_20260620_032904.err.log` first failed before test execution because `session/restore/**` and `session/path_restore/**` leaf functions were already `pub(in crate::scene::dynamic_scene::session)`, but their intermediate `apply` / `diff` / `restore` module re-exports remained `pub(super)`, making facade and path API callers see private imports. The six intermediate modules under `zircon_runtime/src/scene/dynamic_scene/session/{restore,path_restore}/{apply,diff,restore}/mod.rs` now re-export at `pub(in crate::scene::dynamic_scene::session)` without changing restore/apply/diff behavior or public facade names. The next full attempt `.codex/tmp/zircon_runtime_scene_closeout_20260620_035145.err.log` cleared that restore/path_restore blocker and exposed the same intermediate-boundary drift in `session/query/selection/mod.rs`; its global/tag/typed selection re-exports now also use `pub(in crate::scene::dynamic_scene::session)`, while leaf query functions and facade method behavior remain unchanged. Validation: `rustfmt --edition 2021 --check` passed for the seven touched dynamic-scene module files; direct source checks confirmed those re-exports use session-level visibility and no longer use `pub(super)` in these intermediate owners. A fresh full `scene::` gate has not completed after this query-selection sync because another render-focused Cargo lane was active; Runtime 05 remains `pending_full_scene_cargo`. |
| M3 | 3.1 Runtime 05 full scene lib-test compile blocker repair | runtime_05_full_scene_libtest_motion_vector_camera_compile_blocker_static_passed_focused_no_result_scene_gate_pending | 2026-06-20 | Fresh full gate attempt `cargo test -p zircon_runtime --lib scene:: --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --test-threads=1 --nocapture` wrote `.codex/tmp/zircon_runtime_scene_closeout_20260620_025817.{out,err}.log` and failed before executing any tests: `zircon_runtime/src/graphics/runtime/render_framework/viewport_record/motion_vector_camera.rs` test code moved `left_camera` / `right_camera` into `record.replace_motion_vector_camera(...)` and then tried to read `left_camera.transform` / `right_camera.transform`, producing two E0382 lib-test compile errors. The repair is test-only and behavior-preserving: `viewport_record_keeps_motion_vector_camera_per_camera_key` now copies `left_transform` and `right_transform` before moving the snapshots into `ViewportRecord`, while production `motion_vector_camera(...)` and `replace_motion_vector_camera(...)` APIs remain unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/graphics/runtime/render_framework/viewport_record/motion_vector_camera.rs` passed; direct source inspection confirms the expected transform values are captured before the move. A focused retry `cargo test -p zircon_runtime --lib viewport_record_keeps --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-camera-prev-state-0620 --message-format short --color never -- --test-threads=1 --nocapture` was started after the fix but produced only warning output in `.codex/tmp/zircon_runtime_viewport_record_keeps_20260620_0314.log` before exceeding the controlled window; its residual cargo/rustc processes were stopped, so no focused pass/fail is claimed. Full Runtime 05 `scene::` gate remains `pending_full_scene_cargo` and must be rerun from a clean compile lane before any completed status is promoted. |
| M3 | 3.1 Runtime 05 lib compile frontier recheck | runtime_05_lib_compile_frontier_current_passed_scene_gate_pending | 2026-06-20 | After the dormant subscription and dynamic-scene selected import-path slices, Runtime 05 rechecked the current compile frontier before promoting any `scene::` gate status. Earlier background `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never` attempts exposed transient graphics/render submission drift in `zircon_runtime/src/graphics/runtime/render_framework/submit_frame_extract/**` (`ViewportRecord` camera-history key call sites, runtime clear method names, and record-submission re-export visibility) while another active render lane was updating those owner files. The final foreground recheck against the current worktree used the same package/target-dir/lockfile settings, wrote `.codex/tmp/zircon_runtime_lib_check_current_foreground_20260620_0234.log`, and passed in 2m37s: `Finished dev profile`, with 141 existing warnings. No Runtime 05 source or render owner code was changed by this diagnostic row. Full `cargo test -p zircon_runtime --lib scene:: --locked` was not rerun after the lib compile pass, so Runtime 05 remains `pending_full_scene_cargo` and must still use a fresh `scene::` gate before any completed status is promoted. |
| M3 | 3.1 dynamic scene selected slot named-owner import path fix | runtime_05_dynamic_scene_selected_slot_named_owner_import_path_static_passed_cargo_retry_timeout_no_result_scene_gate_pending | 2026-06-20 | Support-first compile-path repair exposed while running the exact ECS dormant subscription focused Cargo check: the folder-backed selected slot helper leaves under `zircon_runtime/src/scene/dynamic_scene/session/{slot_copy,slot_import}/selected/{basic,metadata}/{commit,preview}.rs` were importing sibling `named` owners as `super::super::named`, which is one level too shallow from the commit/preview leaf modules. The eight selected helper files now import the unchanged named copy/import helpers through `super::super::super::named`; selector resolution still happens in the selected helper before delegating to named behavior, and public `RuntimeSessionArchive` method names, archive format, metadata normalization, duplicate-slot rejection, and preview/commit semantics are unchanged. `docs/zircon_runtime/scene/dynamic_scene.md` records the same behavior-preserving compile fix. Validation: `rustfmt --edition 2021 --check` over the eight selected helper files and `zircon_runtime/src/scene/tests/ecs_events_messages.rs` passed; path scan confirmed `super::super::named` no longer appears in those selected leaves and `super::super::super::named` is present. The exact focused Cargo retry for `scene::tests::ecs_events_messages::event_subscription_source_keeps_dormant_reader_boundaries` timed out after 15 minutes with no test result; residual Runtime 05 cargo/rustc processes from that retry were stopped. No focused Cargo pass/fail is claimed, and Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 event subscription boundary source guard | runtime_05_event_subscription_boundary_source_guard_static_passed_cargo_timeout_no_result_scene_gate_pending | 2026-06-20 | Support-first source guard for the lower-layer dormant subscription path behind `scene::tests::ecs_schedule::dormant_subscription_connects_on_plugin_activate`: `zircon_runtime/src/scene/tests/ecs_events_messages.rs` now adds `event_subscription_source_keeps_dormant_reader_boundaries`, which reads `zircon_runtime/src/scene/ecs/events.rs` and pins `EventSubscription::new_dormant(...)` to registration-only setup through `store.register::<T>()`, rejects `register_reader` / `connect_reader` in dormant construction, requires `connect(...)` to use `store.connect_reader(self.event_type_id)` and clear the cursor to `store.events_by_id::<T>(self.event_type_id)`, requires `disconnect(...)` to use `store.disconnect_reader(self.event_type_id)`, clear the cursor with `None`, and restore `EventSubscriptionStatus::Dormant`, and requires disconnected reads to clear the cursor and return `EventReadIter::empty()`. This extends the event writer/First-stage coverage to the weak-dependency reader boundary without touching ECS production code, render, editor, or plugin owner files. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/ecs_events_messages.rs` passed; direct source checks for dormant registration, connect/disconnect reader-count transitions, and disconnected read behavior passed. The first focused Cargo attempt exposed current dynamic-scene session split files under `zircon_runtime/src/scene/dynamic_scene/session/{slot_copy,slot_import}/selected/{basic,metadata}/` importing `super::super::named`, which is not present at that level; those eight selected helper files were minimally corrected to import `super::super::super::named` without changing session archive behavior. `rustfmt --edition 2021 --check` over the ECS guard plus the eight selected helper files passed, and a path scan confirmed only `super::super::super::named` remains there. The exact focused Cargo retry `cargo test -p zircon_runtime --lib scene::tests::ecs_events_messages::event_subscription_source_keeps_dormant_reader_boundaries --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --exact --test-threads=1 --nocapture` timed out after 15 minutes with no test result; residual Runtime 05 cargo/rustc processes from that retry were stopped. No focused Cargo pass/fail is claimed for the ECS guard, and Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 first-stage event update-all source guard | runtime_05_first_stage_event_update_all_source_guard_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-20 | Support-first source guard for the lower-layer path behind `scene::tests::ecs_events_messages::first_stage_updates_all_registered_event_channels`: `zircon_runtime/src/scene/tests/ecs_events_messages.rs` now adds `first_stage_event_update_all_uses_builtin_source_path`, which reads `zircon_runtime/src/scene/ecs/scene_system_registry.rs`, `zircon_runtime/src/scene/world/derived_state.rs`, and `zircon_runtime/src/scene/world/events.rs` to pin the built-in `zircon.scene.events_update_all` descriptor in `SystemStage::First`, its `InternalSceneSystem::UpdateEvents` dispatch, `World::run_internal_scene_system(...)` forwarding to `self.update_all_events();`, and `World::update_all_events()` forwarding to `self.events.update_all();`. This extends the event writer repair coverage without touching production code, render, editor, or plugin owner files. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/ecs_events_messages.rs` passed; direct source checks for the registry, derived-state dispatch, and world event forwarding passed. Focused Cargo was not started because implementation-slice cadence and current active-session coordination keep full `scene::` Cargo validation for the testing stage; Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 scene asset Cargo diagnostic window | runtime_05_scene_asset_cargo_diagnostic_timeout_no_result_scene_gate_pending | 2026-06-20 | After static support guards were updated, a Runtime 05 minimal diagnostic window was attempted for the scene-asset/project-IO lower layer. `cargo test -p zircon_runtime --lib scene_asset --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --test-threads=1 --nocapture` timed out after 10 minutes with no pass/fail result. A narrower retry, `cargo test -p zircon_runtime --lib scene::tests::asset_scene::scene_asset_load_uses_asset_preserving_normalizer_source_guard --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --exact --test-threads=1 --nocapture`, timed out after 15 minutes with no pass/fail result; its residual cargo/rustc processes were stopped. No Cargo success or functional failure is claimed from these attempts, and Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 event-store registered-channel send source guard | runtime_05_event_store_registered_channel_send_source_guard_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-20 | Support-first source guard for the Runtime 05 event writer queue repair: `zircon_runtime/src/scene/tests/ecs_events_messages.rs` now adds `event_store_send_by_id_uses_registered_channel_guard_source`, which reads `zircon_runtime/src/scene/ecs/events.rs` and pins `EventStore::send_by_id(...)` / `send_batch_by_id(...)` to registered-channel existence checks (`self.channel(event_type_id).is_none()` with `false`/`0` for unknown ids), while rejecting `is_active` or `reader_count` in producer-write paths. This preserves writer-before-reader queueing and keeps dormant subscriptions as reader-activation state only; no production code, render, editor, or plugin owner files changed. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/ecs_events_messages.rs` passed; direct source checks for both send paths passed. Focused Cargo was not started because active Cargo/rustc lanes were still running; Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 scene asset normalizer source guard | runtime_05_scene_asset_normalizer_source_guard_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-20 | Support-first source guard for the scene asset/project I/O lower-layer candidate: `zircon_runtime/src/scene/tests/asset_scene.rs` now adds `scene_asset_load_uses_asset_preserving_normalizer_source_guard`, which pins `World::from_scene_asset(...)` to `world.normalize_scene_asset_after_load();`, rejects the stale `world.normalize_after_load();` path inside scene-asset import, requires `normalize_scene_asset_after_load` to call `normalize_loaded_state(false)`, requires project-document `normalize_after_load` to call `normalize_loaded_state(true)`, and keeps fallback camera/directional-light spawns gated by `ensure_default_nodes`. This records the existing `project_io.rs` asset-preserving behavior without touching runtime owner logic, editor, render, or plugin surfaces. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/asset_scene.rs` passed; direct source checks for the asset/project I/O normalizer contract passed; conflict-marker scan over touched docs/test passed. Focused Cargo was not started because active Cargo/rustc lanes were still running; Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 schedule executor-plan refresh guard sync | runtime_05_schedule_executor_plan_refresh_guard_sync_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-20 | Support-first source-guard sync for the remaining stale `scene::tests::ecs_scheduled_native_systems::world_driver_reuses_tick_schedule_snapshots_for_stage_runs` assertion shape: production `zircon_runtime/src/scene/ecs/schedule.rs` now owns executor-stage snapshots through `executor_plan: Arc<SceneScheduleStagePlan>`, `stage_plan()` returning `Arc::clone(&self.executor_plan)`, and a stricter `refresh_or_defer_executor_plan(&mut self) -> Result<(), ScheduleError>` path that refreshes with `self.refresh_executor_plan()?` when no systems are taken or marks `self.executor_plan_dirty = true` while a native/runtime system is temporarily borrowed. `zircon_runtime/src/scene/tests/ecs_scheduled_native_systems.rs` now guards that current Result/deferred refresh contract instead of the removed `self.refresh_executor_plan();` shape, while preserving stage-plan reuse and taken-system membership checks. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/ecs_scheduled_native_systems.rs` passed; direct source checks for the updated schedule refresh/defer guard passed; remaining old source checks for property entries, dynamic scalar JSON reflection, schedule owner stage plans, conflict graph direct lookups, and world-driver snapshots passed. Focused Cargo was not started because active Cargo/rustc lanes were still running; Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 ECS identity storage source-guard support repair | runtime_05_ecs_identity_source_guard_support_repair_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-20 | Support-first repair for the stale source-guard failure `scene::tests::ecs_identity_storage::stable_entity_registration_uses_append_row_without_entity_scan` from `.codex/tmp/zircon_runtime_scene_closeout_20260619_2037.log`: production `zircon_runtime/src/scene/world/identity.rs` already uses the append row (`self.entities.len()`) and direct `EntityRegistry::spawn(entity, EntityLocation::new(ArchetypeId::EMPTY, row))` result branches, with no entity-list scan; `zircon_runtime/src/scene/tests/ecs_identity_storage.rs` now checks that direct spawn shape through whitespace-insensitive source text while preserving the negative guards against `.iter()`, `.position(...)`, `unwrap_or(self.entities.len())`, and `.map_err(...)`. Validation: rustfmt and direct source checks cover `ecs_identity_storage.rs`; focused Cargo remains deferred while active Cargo/rustc lanes and unrelated graphics/render compile drift block a fresh Runtime 05 full scene gate. Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 Frameworks02 scene ECS source-guard current-owner sync | frameworks_02_m3_scene_ecs_source_guard_current_owner_sync_static_passed_cargo_blocked_ui_text_measure_cache | 2026-07-04 | Support-first follow-up from the Frameworks02 full-runtime red domain: `scene::tests::ecs` had 6 stale source guards after current owner and typed-error migrations. `ecs_events_messages.rs` now reads `events/subscription.rs` directly for dormant subscription boundaries and uses the current `fn validate_system_descriptor` end marker for `builtin_scene_systems()`. `ecs_hierarchy_structure.rs` checks `SceneError::{DynamicMobilityWithStaticChildren,StaticMobilityUnderDynamicParent}` instead of retired String diagnostics. `ecs_identity_storage.rs` checks append-row `EntityRegistry::spawn(...)?` typed propagation. `ecs_query_structure.rs` expects `many_item_array` instead of the deleted `helpers` module. `ecs_typed_api.rs` checks dynamic component presence `SceneResult` propagation instead of old `map_err`/String branches. Validation: touched-file rustfmt check and direct static source-anchor checks passed. Focused Cargo `cargo test -p zircon_runtime --lib scene::tests::ecs --no-default-features --features core-min ...` was blocked before executing tests by unrelated UI/text compile drift in `ui/surface/surface.rs` (`UiTextMeasureCache` lacks `==` support), so no Cargo pass is claimed; Runtime 05 full scene gate remains pending. |
| M3 | 3.1 scene asset import and session fixture support repair | runtime_05_scene_asset_session_fixture_support_repair_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-19 | Support-first repair for the scene asset/dynamic session cluster from `.codex/tmp/zircon_runtime_scene_closeout_20260619_2037.log`: `zircon_runtime/src/scene/world/project_io.rs` now splits post-load normalization so `World::from_scene_asset(...)` rebuilds runtime registries and derived state without injecting fallback camera/directional-light nodes, while project-document load still keeps default-node repair; `zircon_runtime/src/scene/world/render.rs` now synthesizes a non-persistent default-layer camera descriptor when an asset-preserving world has no scene camera, so sparse assets can still produce render extracts without mutating or serializing default nodes; `zircon_runtime/src/scene/tests/asset_scene.rs` extends the script-only asset roundtrip guard to call `World::to_render_extract()` and assert no mesh/light payload is introduced; `zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs` and `load.rs` switch one-entity archive fixture tests from default bootstrap levels to explicit `World::empty()` levels. Validation is scoped static/support evidence only: rustfmt and direct source checks cover the touched project I/O, render, asset-scene, and dynamic-session files; focused Cargo remains deferred while active Cargo/rustc lanes and unrelated graphics/render compile drift block a fresh Runtime 05 full scene gate. Runtime 05 full `cargo test -p zircon_runtime --lib scene:: --locked` remains `pending_full_scene_cargo`. |
| M3 | 3.1 ECS event writer registered-channel queue repair | runtime_05_ecs_event_writer_registered_channel_queue_repair_rustfmt_passed_focused_tests_passed_scene_gate_pending | 2026-06-19 | Support-first root cause confirmed in `EventStore::send_by_id(...)` / `send_batch_by_id(...)`: registered event channels were incorrectly gated by `is_active(reader_count > 0)`, so `EventWriterParam<T>` initialized before `EventReaderParam<T>` dropped single and batch writes before `World::update_events::<T>()`. `zircon_runtime/src/scene/ecs/events.rs` now gates by registered channel presence, preserving invalid id `false`/`0` behavior and the existing type-mismatch assertion path; `zircon_runtime/src/scene/tests/ecs_schedule.rs` updates the lower-layer registered-channel and dormant-subscription expectations, and `docs/zircon_runtime/scene/ecs.md` now separates channel registration from active reader state. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/ecs/events.rs zircon_runtime/src/scene/tests/ecs_schedule.rs` passed; the first cold-target focused Cargo attempt on `D:\cargo-targets\zircon-runtime-ecs-event-support-0619` timed out before producing a test result and is not counted as pass/fail evidence; prewarmed Runtime 05 target `D:\cargo-targets\zircon-runtime-scene-closeout-0619` focused tests passed with existing warnings: `scene::tests::ecs_systems::event_reader_and_writer_use_current_and_next_queues` 1/1, `scene::tests::ecs_systems::event_writer_batch_preserves_next_queue_order` 1/1, `scene::tests::ecs_schedule::event_store_registered_channel_accepts_writes_before_reader_registered` 1/1, and `scene::tests::ecs_schedule::dormant_subscription_connects_on_plugin_activate` 1/1. Full `cargo test -p zircon_runtime --lib scene:: --locked` was not rerun after this focused repair, so Runtime 05 remains `pending_full_scene_cargo` with the non-event failure clusters from `.codex/tmp/zircon_runtime_scene_closeout_20260619_2037.log` still open. |
| M3 | 3.1 ECS source-guard support repair | runtime_05_ecs_source_guard_support_repair_static_passed_cargo_blocked_graphics_compile_drift_scene_gate_pending | 2026-06-19 | Support-first closeout for the lower ECS source-guard cluster from `.codex/tmp/zircon_runtime_scene_closeout_20260619_2037.log`: `zircon_runtime/src/scene/ecs/archetype/index.rs` restores `matching_archetypes(...)` to direct pre-sized loops over the borrowed shortest bucket or full record scan; `ecs_archetype_index_structure.rs` now reads `entity_row(...)` against the current `impl Default` boundary after `all_archetype_ids(...)` removal; `ecs_query_combinations.rs` keeps direct cached index-scan guards while compacting whitespace for rustfmt-wrapped `F::matches_component_locations(...)`; `ecs_query_state_structure.rs` scopes cached-direct item-fetch guards to the two get-path function bodies instead of counting the whole file; `ecs_observers_messages.rs` keeps the message cursor saturating-sub guard layout-stable; `dynamic_scene.rs` updates mutation-surface include paths to folder-backed `mutation/{rename,metadata}/commit.rs` plus validation invariants. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/ecs/archetype/index.rs zircon_runtime/src/scene/tests/ecs_archetype_index_structure.rs zircon_runtime/src/scene/tests/ecs_query_combinations.rs zircon_runtime/src/scene/tests/ecs_observers_messages.rs zircon_runtime/src/scene/tests/ecs_query_state_structure.rs zircon_runtime/src/scene/tests/dynamic_scene.rs` passed; standalone `rustc --edition 2021 --test zircon_runtime/src/scene/tests/ecs_archetype_index_structure.rs` then `ecs_archetype_index_structure.exe --exact archetype_index_hot_paths_use_direct_branches_without_adapter_chains --nocapture` passed 1/1; standalone `rustc --edition 2021 --test zircon_runtime/src/scene/tests/ecs_query_state_structure.rs` then `ecs_query_state_structure.exe --exact query_state_item_fetch_errors_use_direct_branches --nocapture` passed 1/1; direct source checks reported `query combination direct index scan guard passed`, `event/message cursor direct lookup guard passed`, and `dynamic scene mutation guard include paths passed`; `git diff --check -- ...` emitted only tracked LF-to-CRLF notices. Focused Cargo attempt `cargo test -p zircon_runtime --lib source --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --test-threads=1 --nocapture` failed during `zircon_runtime` lib-test compilation before reaching source tests because unrelated graphics/render drift currently leaves `VisibilityStaticIndex` / `ViewportCameraHistoryKey` imports unresolved and viewport history/frame-submission call signatures out of sync; no Cargo source-test pass is claimed. Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 ECS source-guard follow-up sync | runtime_05_ecs_source_guard_followup_sync_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-19 | Support-first follow-up for current-source drift behind the old full-scene failure log: `World::register_stable_entity(...)` already satisfies the append-row/direct `EntityRegistry::spawn(...)` branch guard, so no production entity identity code changed. `zircon_runtime/src/scene/tests/ecs_schedule/conflict_graph.rs` now recognizes the current `ScheduleConflictEdges` direct iterator path through `Some(&self.graph.edges[edge_index])` and rejects the removed `&edges[*index]`/iterator-map shape; `ecs_scheduled_native_systems.rs` now pins `WorldDriver::run_stage(...)` snapshot reuse through `schedule.internal_systems_for_stage(stage)` and `schedule.native_steps_for_stage(stage)` while keeping outer-loop hook lookups on `*stage`; `ecs_typed_api.rs` keeps the typed insert/remove direct `Result` branch guard but no longer depends on a single-line `insert_at_tick(...)` call; `property_paths.rs` recognizes the current `push_entry!(...)` macro projection for `Hierarchy.parent` while still requiring pre-sized `Vec::with_capacity(...)` property entries. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/ecs_schedule/conflict_graph.rs zircon_runtime/src/scene/tests/ecs_scheduled_native_systems.rs zircon_runtime/src/scene/tests/ecs_typed_api.rs zircon_runtime/src/scene/tests/property_paths.rs` passed; direct source checks reported `next ECS/source guard direct checks passed`; conflict-marker scan passed; `git diff --check -- zircon_runtime/src/scene/tests/ecs_schedule/conflict_graph.rs zircon_runtime/src/scene/tests/ecs_scheduled_native_systems.rs zircon_runtime/src/scene/tests/ecs_typed_api.rs zircon_runtime/src/scene/tests/property_paths.rs` emitted only tracked LF-to-CRLF notices. Focused Cargo was not started because another active Cargo/rustc lane was still running, and the prior focused Cargo check remained blocked by unrelated graphics/render compile drift; Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 scene behavior support repair | runtime_05_scene_behavior_support_repair_static_passed_cargo_deferred_active_lane_scene_gate_pending | 2026-06-19 | Support-first repair for the next Runtime 05 scene behavior cluster from `.codex/tmp/zircon_runtime_scene_closeout_20260619_2037.log`: `zircon_runtime/src/scene/world/dynamic_components.rs` now projects finite dynamic scalar JSON numbers through the f32 shortest decimal string before constructing `serde_json::Number`, preserving dynamic reflection writes such as `0.9` instead of exposing f32 binary precision as `0.8999999761581421`; `zircon_runtime/src/scene/ecs/system/native/scheduled_scene_step.rs` adds the same-order non-hook-before-hook tie-break so built-in/native render-extract preparation runs before same-order plugin hooks while preserving id and barrier ordering among non-hook steps; `zircon_runtime/src/scene/world/render.rs` keeps the selected scene camera descriptor in `RenderViewExtract.cameras` even when the selected camera entity is inactive, while continuing to filter other inactive descriptors; `zircon_runtime/src/scene/tests/render_extract.rs` records the selected-inactive descriptor guard and uses a small f32 tolerance for HUD width/height aspect ratio. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/world/dynamic_components.rs zircon_runtime/src/scene/tests/ecs_dynamic_components_structure.rs zircon_runtime/src/scene/tests/ecs_reflect/dynamic_components.rs zircon_runtime/src/scene/ecs/system/native/scheduled_scene_step.rs zircon_runtime/src/scene/world/render.rs zircon_runtime/src/scene/tests/ecs_scheduled_native_systems.rs zircon_runtime/src/scene/tests/render_extract.rs` passed; direct source checks reported the f32 JSON projection guard, scheduled-step hook tie-break guard, selected-camera descriptor retention guard, and HUD f32 tolerance guard all present. Focused Cargo was not started because active Cargo/rustc lanes were still running, and the prior focused Cargo path remained blocked by unrelated graphics/render compile drift; Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 Runtime 05 full scene closeout gate recheck | runtime_05_full_scene_closeout_failed_33_failures_support_first_pending | 2026-06-19 | 复跑 `cargo test -p zircon_runtime --lib scene:: --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never -- --test-threads=1 --nocapture`。前两次尝试分别只留下依赖编译输出/`SCENE_CLOSEOUT_EXIT=-1` 与 transient `path_api/transfer/import/loaded::selected` module-not-found 诊断；当前文件状态下 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-scene-closeout-0619 --message-format short --color never` 已通过，仅既有 warning。最终 `scene::` gate 进入测试阶段并失败：1035 passed / 33 failed / 0 ignored / 3479 filtered out / 106.77s，日志为 `.codex/tmp/zircon_runtime_scene_closeout_20260619_2037.log`。失败聚类：graphics render material/readiness/zshader/runtime-feature/render-graph materialization/post-process/scene-uniform 14 项；scene asset/dynamic scene session capture-load 4 项；ECS archetype/entity/message/query/reflect/schedule/event/source-guard 13 项；property projection 1 项；HUD particle extract float equality 1 项。按 support-first 规则，后续修复必须先从最低共享层聚焦诊断，不得通过上层特例关闭 Runtime 05；Runtime 05 仍保持 `pending_full_scene_cargo`，不得标记 completed。 |
| M3 | 3.1 runtime session archive facade construction capture variant subowner split | runtime_session_archive_facade_construction_capture_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/construction/capture.rs` 已硬切换为 folder-backed public archive construction capture facade variant 子树：`capture/mod.rs` 仅保留结构入口，`capture/world.rs` 承接 `from_world` 与 `from_world_with_metadata` facade methods，`capture/level.rs` 承接 `from_level` facade method。所有 child 仍只委托既有 `session/construction/` behavior owner，所有 `RuntimeSessionArchive::from_world*` 与 `from_level` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/construction/mod.rs` 3 行、`facade/construction/archive.rs` 15 行、`facade/construction/serialization.rs` 10 行、`facade/construction/capture/mod.rs` 2 行、`facade/construction/capture/world.rs` 18 行、`facade/construction/capture/level.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world construction capture facade delegates only to construction owner、metadata world constructor preserves delegation、level construction facade stays level-only delegation 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/construction/{mod,archive,serialization,capture/{mod,world,level}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/construction/capture.rs` file absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade query selection variant subowner split | runtime_session_archive_facade_query_selection_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/query/selection.rs` 已硬切换为 folder-backed public archive query selection facade variant 子树：`selection/mod.rs` 仅保留结构入口，`selection/global.rs` 承接 global latest/oldest updated slot id facade methods，`selection/tag.rs` 承接 tag-scoped latest/oldest updated slot id facade methods，`selection/typed.rs` 承接 typed `select_slot` facade method。所有 child 仍只委托既有 `session/query/` behavior owner，所有 `RuntimeSessionArchive::latest_updated_slot_id*`、`oldest_updated_slot_id*` 与 `select_slot` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/query/selection/mod.rs` 3 行、`facade/query/selection/global.rs` 12 行、`facade/query/selection/tag.rs` 18 行、`facade/query/selection/typed.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global selection facade delegates only to query selection owner、tag selection facade preserves tag bucket delegation、typed select_slot facade stays selector delegation-only 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/query/{mod,basic,manifest,statistics,selection/{mod,global,tag,typed}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/query/selection.rs` file absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade capture ordinary commit-preview variant subowner split | runtime_session_archive_facade_capture_ordinary_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/capture/{world,level}/ordinary.rs` 已硬切换为 folder-backed public archive ordinary capture facade variant 子树：`world/ordinary/mod.rs` 与 `level/ordinary/mod.rs` 仅保留结构入口，`world/ordinary/commit.rs` 与 `level/ordinary/commit.rs` 承接 mutating capture facade methods，`world/ordinary/preview.rs` 与 `level/ordinary/preview.rs` 承接 dry-run preview facade methods。所有 child 仍只委托既有 `slot_capture` behavior owner，所有 `RuntimeSessionArchive::capture_*slot` 与 `preview_capture_*slot` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/capture/world/ordinary/mod.rs` 2 行、`facade/capture/world/ordinary/commit.rs` 12 行、`facade/capture/world/ordinary/preview.rs` 13 行、`facade/capture/level/ordinary/mod.rs` 2 行、`facade/capture/level/ordinary/commit.rs` 11 行、`facade/capture/level/ordinary/preview.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world ordinary capture commit delegates only to slot_capture、world ordinary capture preview remains read-only delegation、level ordinary capture commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/capture/{world,level}/ordinary/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/capture/{world,level}/ordinary.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade capture retention commit-preview variant subowner split | runtime_session_archive_facade_capture_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/capture/{world,level}/retention/{global,tag}.rs` 已硬切换为 folder-backed public archive capture-retention commit/preview variant 子树：四个 `{global,tag}/mod.rs` 仅保留结构入口，`commit.rs` 承接 mutating capture-retention facade methods，`preview.rs` 承接 dry-run capture-retention facade methods。所有 child 仍只委托既有 `capture_retention` behavior owner，所有 `RuntimeSessionArchive::capture_*retention*` 与 `preview_capture_*retention*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `world/retention/global/mod.rs` 2 行、`world/retention/global/commit.rs` 13 行、`world/retention/global/preview.rs` 13 行、`world/retention/tag/mod.rs` 2 行、`world/retention/tag/commit.rs` 16 行、`world/retention/tag/preview.rs` 16 行、`level/retention/global/mod.rs` 2 行、`level/retention/global/commit.rs` 12 行、`level/retention/global/preview.rs` 12 行、`level/retention/tag/mod.rs` 2 行、`level/retention/tag/commit.rs` 13 行、`level/retention/tag/preview.rs` 13 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world global retention commit facade delegates only to capture_retention、world tag retention preview remains read-only delegation、level global/tag retention commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/capture/{world,level}/retention/{global,tag}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/capture/{world,level}/retention/{global,tag}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade capture retention variant subowner split | runtime_session_archive_facade_capture_retention_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/capture/{world,level}/retention.rs` 已硬切换为 folder-backed public archive capture-retention facade variant 子树：`world/retention/mod.rs` 与 `level/retention/mod.rs` 仅保留结构入口，`world/retention/global/` 与 `level/retention/global/` 承接 global capture-retention commit/preview facade variant 子树，`world/retention/tag/` 与 `level/retention/tag/` 承接 tag-scoped capture-retention commit/preview facade variant 子树。所有 child 仍只委托既有 `capture_retention` behavior owner，所有 `RuntimeSessionArchive::capture_*slot_with*_retention*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/capture/world/retention/mod.rs` 2 行、`facade/capture/world/retention/global/mod.rs` 2 行、`facade/capture/world/retention/global/commit.rs` 13 行、`facade/capture/world/retention/global/preview.rs` 13 行、`facade/capture/world/retention/tag/mod.rs` 2 行、`facade/capture/world/retention/tag/commit.rs` 16 行、`facade/capture/world/retention/tag/preview.rs` 16 行、`facade/capture/level/retention/mod.rs` 2 行、`facade/capture/level/retention/global/mod.rs` 2 行、`facade/capture/level/retention/global/commit.rs` 12 行、`facade/capture/level/retention/global/preview.rs` 12 行、`facade/capture/level/retention/tag/mod.rs` 2 行、`facade/capture/level/retention/tag/commit.rs` 13 行、`facade/capture/level/retention/tag/preview.rs` 13 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world global retention facade delegates only to capture_retention、world tag retention facade keeps tag-scoped delegation、level global/tag retention facade signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/capture/{mod,world/{mod,retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}},level/{mod,retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/capture/{world,level}/retention.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade restore variant subowner split | runtime_session_archive_facade_restore_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/restore/{apply,restore,diff}.rs` 已硬切换为 folder-backed public archive restore facade variant 子树：`apply/mod.rs`、`restore/mod.rs` 与 `diff/mod.rs` 仅保留结构入口，`apply/named.rs` 承接 named world/level apply facade methods，`apply/selected.rs` 承接 selector-based world/level apply facade methods，`restore/named.rs` 承接 named empty-world 与 level restore facade methods，`restore/selected.rs` 承接 selector-based empty-world 与 level restore facade methods，`diff/named.rs` 承接 named world/level diff facade methods，`diff/selected.rs` 承接 selector-based world/level diff facade methods。所有 child 仍只委托既有 `session/restore/` behavior owner，所有 `RuntimeSessionArchive::apply_*`、`restore_*`、`diff_*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/restore/mod.rs` 3 行、`facade/restore/apply/mod.rs` 2 行、`facade/restore/apply/named.rs` 23 行、`facade/restore/apply/selected.rs` 25 行、`facade/restore/restore/mod.rs` 2 行、`facade/restore/restore/named.rs` 23 行、`facade/restore/restore/selected.rs` 25 行、`facade/restore/diff/mod.rs` 2 行、`facade/restore/diff/named.rs` 23 行、`facade/restore/diff/selected.rs` 25 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named apply facade delegates only to restore apply owner、selected restore facade keeps selector delegation in behavior owner、diff facade remains read-only delegation 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/restore/{mod,apply/{mod,named,selected},restore/{mod,named,selected},diff/{mod,named,selected}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；old `facade/restore/{apply,restore,diff}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer import facade commit-preview variant subowner split | runtime_session_archive_path_api_transfer_import_facade_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/import/{loaded,source_path}/{named,selected}/{basic,metadata}.rs` 已硬切换为 folder-backed public path import facade commit/preview variant 子树：八个 `{basic,metadata}/mod.rs` 仅保留结构入口，`commit.rs` 承接 atomic import facade methods，`preview.rs` 承接 no-write preview import facade methods。所有 child 仍只委托 `session/path_transfer/` behavior owner，不上移 distinct-path preflight 或 archive mutation logic；所有 `RuntimeSessionArchive::*import*at_path*` public method names 与 `scene::dynamic_scene` re-export surface 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/transfer/import/mod.rs` 2 行、`loaded/mod.rs` 2 行、`loaded/named/mod.rs` 2 行、`loaded/named/basic/mod.rs` 2 行、`loaded/named/basic/commit.rs` 21 行、`loaded/named/basic/preview.rs` 22 行、`loaded/named/metadata/mod.rs` 2 行、`loaded/named/metadata/commit.rs` 24 行、`loaded/named/metadata/preview.rs` 24 行、`loaded/selected/mod.rs` 2 行、`loaded/selected/basic/mod.rs` 2 行、`loaded/selected/basic/commit.rs` 22 行、`loaded/selected/basic/preview.rs` 22 行、`loaded/selected/metadata/mod.rs` 2 行、`loaded/selected/metadata/commit.rs` 24 行、`loaded/selected/metadata/preview.rs` 24 行、`source_path/mod.rs` 2 行、`source_path/named/mod.rs` 2 行、`source_path/named/basic/mod.rs` 2 行、`source_path/named/basic/commit.rs` 21 行、`source_path/named/basic/preview.rs` 22 行、`source_path/named/metadata/mod.rs` 2 行、`source_path/named/metadata/commit.rs` 24 行、`source_path/named/metadata/preview.rs` 24 行、`source_path/selected/mod.rs` 2 行、`source_path/selected/basic/mod.rs` 2 行、`source_path/selected/basic/commit.rs` 22 行、`source_path/selected/basic/preview.rs` 22 行、`source_path/selected/metadata/mod.rs` 2 行、`source_path/selected/metadata/commit.rs` 24 行、`source_path/selected/metadata/preview.rs` 24 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named basic path import commit delegates only to path_transfer、source-path metadata path import preview remains read-only delegation、selected metadata path import commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/transfer/import/{loaded,source_path}/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `path_api/transfer/import/{loaded,source_path}/{named,selected}/{basic,metadata}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer import facade variant subowner split | runtime_session_archive_path_api_transfer_import_facade_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/import/{loaded,source_path}/{named,selected}.rs` 已硬切换为 folder-backed public facade variant 子树：`loaded/named/mod.rs`、`loaded/selected/mod.rs`、`source_path/named/mod.rs` 与 `source_path/selected/mod.rs` 仅保留结构入口，四个 `basic/` 承接 plain import commit/preview public facade variant 子树，四个 `metadata/` 承接 metadata override import commit/preview public facade variant 子树。所有 child 仍只委托 `session/path_transfer/` behavior owner，不上移 distinct-path preflight 或 archive mutation logic；所有 `RuntimeSessionArchive::*import*at_path*` public method names 与 `scene::dynamic_scene` re-export surface 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/transfer/import/mod.rs` 2 行、`loaded/mod.rs` 2 行、`loaded/named/mod.rs` 2 行、`loaded/named/basic/mod.rs` 2 行、`loaded/named/basic/commit.rs` 21 行、`loaded/named/basic/preview.rs` 22 行、`loaded/named/metadata/mod.rs` 2 行、`loaded/named/metadata/commit.rs` 24 行、`loaded/named/metadata/preview.rs` 24 行、`loaded/selected/mod.rs` 2 行、`loaded/selected/basic/mod.rs` 2 行、`loaded/selected/basic/commit.rs` 22 行、`loaded/selected/basic/preview.rs` 22 行、`loaded/selected/metadata/mod.rs` 2 行、`loaded/selected/metadata/commit.rs` 24 行、`loaded/selected/metadata/preview.rs` 24 行、`source_path/mod.rs` 2 行、`source_path/named/mod.rs` 2 行、`source_path/named/basic/mod.rs` 2 行、`source_path/named/basic/commit.rs` 21 行、`source_path/named/basic/preview.rs` 22 行、`source_path/named/metadata/mod.rs` 2 行、`source_path/named/metadata/commit.rs` 24 行、`source_path/named/metadata/preview.rs` 24 行、`source_path/selected/mod.rs` 2 行、`source_path/selected/basic/mod.rs` 2 行、`source_path/selected/basic/commit.rs` 22 行、`source_path/selected/basic/preview.rs` 22 行、`source_path/selected/metadata/mod.rs` 2 行、`source_path/selected/metadata/commit.rs` 24 行、`source_path/selected/metadata/preview.rs` 24 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named basic facade delegates only to path_transfer loaded owner、source-path metadata facade preserves source-path behavior delegation、selected metadata facade remains selector delegation-only 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/path_api/transfer/import/{loaded,source_path}/{mod,named/{mod,basic,metadata},selected/{mod,basic,metadata}}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/path_api/transfer/import/{loaded,source_path}/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 无命中；旧四个 `path_api/transfer/import/{loaded,source_path}/{named,selected}.rs` 与旧八个 `path_api/transfer/import/{loaded,source_path}/{named,selected}/{basic,metadata}.rs` 已不存在；本切片不启动新的 Cargo lane，Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot import variant subowner split | runtime_session_archive_slot_import_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_import/named.rs` 与 `slot_import/selected.rs` 已硬切换为 folder-backed in-memory slot import variant 子树；当日 variant split 先把 named/selected import families 收束到各自目录，后续已在 2026-06-20 下一行细化为 `session/slot_import/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` commit/preview variant 子 owner。selected variants 仍先解析 `RuntimeSessionSlotSelector` 再委托 named import owner；destination id normalization、duplicate-slot rejection、metadata normalization、incoming slot entity/resource count projection 与 public `RuntimeSessionArchive::*import*_slot_from_archive*` method names 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic import uses incoming metadata before metadata delegate、named metadata preview preserves normalization/duplicate rejection、selected metadata import resolves selector then delegates once 三个锚点。验证：variant split 当时 `rustfmt --edition 2021 --check`、conflict marker 与 trailing whitespace scans 通过；旧 `slot_import/named.rs` 与 `slot_import/selected.rs` 已不存在；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 304s timeout 且无诊断，残留 cargo/rustc 进程已停止，Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot import commit-preview variant subowner split | runtime_session_archive_slot_import_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | slot import `{named,selected}/{basic,metadata}.rs` leaves 已硬切换为 folder-backed `session/slot_import/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 子树：四个 `{basic,metadata}/mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 in-memory loaded-archive import commit helpers，`preview.rs` 承接 no-write loaded-archive import preview reports。所有 helper names 与 `RuntimeSessionArchive::*import*_slot_from_archive*` / `preview_import*_slot_from_archive*` 公开方法名保持不变，incoming metadata reuse、metadata normalization、duplicate-slot rejection、selector resolution、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_import/mod.rs` 12 行、`named/mod.rs` 9 行、`named/basic/mod.rs` 5 行、`named/basic/commit.rs` 12 行、`named/basic/preview.rs` 20 行、`named/metadata/mod.rs` 5 行、`named/metadata/commit.rs` 24 行、`named/metadata/preview.rs` 38 行、`selected/mod.rs` 10 行、`selected/basic/mod.rs` 5 行、`selected/basic/commit.rs` 14 行、`selected/basic/preview.rs` 15 行、`selected/metadata/mod.rs` 5 行、`selected/metadata/commit.rs` 22 行、`selected/metadata/preview.rs` 22 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic import commit/preview metadata parity、metadata import preview remains read-only、selected metadata import keeps selector delegation in selected owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/slot_import/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；旧 `slot_import/{named,selected}/{basic,metadata}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer loaded import variant subowner split | runtime_session_archive_path_transfer_loaded_import_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/import/loaded/named.rs` 与 `path_transfer/import/loaded/selected.rs` 已硬切换为 folder-backed loaded import variant 子树；当日 variant split 先把 named/selected loaded import families 收束到各自目录，后续已在 2026-06-20 下一行细化为 `session/path_transfer/import/loaded/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` commit/preview variant 子 owner。所有 child 仍只通过 `io::mutate_archive_at_path_atomically` 或 `io::load_from_path` 调用 archive-owned import semantics；同名 session-scoped helper names 与 public path API facade names 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named basic import commit/preview parity、loaded metadata import preserves override delegation、loaded selected metadata preview remains no-write 三个锚点。验证：variant split 当时 `rustfmt --edition 2021 --check`、conflict marker 与 trailing whitespace scans 通过；旧 `loaded/named.rs` 与 `loaded/selected.rs` 已不存在；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 304s timeout 且无诊断，残留 cargo/rustc 进程已停止，Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer loaded import commit-preview variant subowner split | runtime_session_archive_path_transfer_loaded_import_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | loaded import `{named,selected}/{basic,metadata}.rs` leaves 已硬切换为 folder-backed `session/path_transfer/import/loaded/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 子树：四个 `{basic,metadata}/mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 atomic loaded-incoming import helpers，`preview.rs` 承接 no-write loaded-incoming import preview helpers。所有 helper names、`io::mutate_archive_at_path_atomically` / `io::load_from_path` 调用边界与 public path API facade names 保持不变，archive-owned import semantics、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_transfer/import/loaded/mod.rs` 15 行、`loaded/named/mod.rs` 10 行、`loaded/named/basic/mod.rs` 5 行、`loaded/named/basic/commit.rs` 17 行、`loaded/named/basic/preview.rs` 18 行、`loaded/named/metadata/mod.rs` 5 行、`loaded/named/metadata/commit.rs` 24 行、`loaded/named/metadata/preview.rs` 21 行、`loaded/selected/mod.rs` 11 行、`loaded/selected/basic/mod.rs` 5 行、`loaded/selected/basic/commit.rs` 18 行、`loaded/selected/basic/preview.rs` 19 行、`loaded/selected/metadata/mod.rs` 5 行、`loaded/selected/metadata/commit.rs` 24 行、`loaded/selected/metadata/preview.rs` 21 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named basic import commit uses mutate_archive_at_path_atomically only、loaded metadata preview loads target without writing、loaded selected metadata import preserves selector delegation 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_transfer/import/loaded/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；旧 `path_transfer/import/loaded/{named,selected}/{basic,metadata}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer source-path import subowner split | runtime_session_archive_path_transfer_source_path_import_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/import/source_path/named.rs` 与 `path_transfer/import/source_path/selected.rs` 已硬切换为 folder-backed source-path import 子树；当日 subowner split 先把 named/selected source-path import families 收束到各自目录，后续已在 2026-06-20 下一行细化为 `session/path_transfer/import/source_path/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` commit/preview variant 子 owner。所有 child 仍在加载 source archive 前执行 source/target distinct-path preflight，并继续委托既有 loaded-incoming import owner；所有 `RuntimeSessionArchive::*archive_path*` inherent method names 与 public re-export surface 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic source-path import rejects same target before load、metadata source-path import delegates after distinct preflight、selected metadata preview does not write source/target 三个锚点。验证：variant split 当时 `rustfmt --edition 2021 --check` 覆盖 source-path import parent tree；本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-source-path-import-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer source-path import commit-preview variant subowner split | runtime_session_archive_path_transfer_source_path_import_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | source-path import `{named,selected}/{basic,metadata}.rs` leaves 已硬切换为 folder-backed `session/path_transfer/import/source_path/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 子树：四个 `{basic,metadata}/mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 source archive path import commit helpers，`preview.rs` 承接 no-write source archive path import preview helpers。source/target distinct-path preflight 仍在 commit 与 preview helper 中先于 `io::load_from_path(source_path)` 执行，随后继续委托 loaded-incoming import owner；helper names、public path API facade names、archive-owned import semantics、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_transfer/import/source_path/mod.rs` 15 行、`source_path/named/mod.rs` 10 行、`source_path/named/basic/mod.rs` 5 行、`source_path/named/basic/commit.rs` 24 行、`source_path/named/basic/preview.rs` 24 行、`source_path/named/metadata/mod.rs` 5 行、`source_path/named/metadata/commit.rs` 31 行、`source_path/named/metadata/preview.rs` 31 行、`source_path/selected/mod.rs` 11 行、`source_path/selected/basic/mod.rs` 5 行、`source_path/selected/basic/commit.rs` 24 行、`source_path/selected/basic/preview.rs` 24 行、`source_path/selected/metadata/mod.rs` 5 行、`source_path/selected/metadata/commit.rs` 31 行、`source_path/selected/metadata/preview.rs` 31 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 source-path named basic commit rejects same target before loading source、source-path metadata preview remains read-only after distinct preflight、source-path selected metadata import preserves loaded owner delegation 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_transfer/import/source_path/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；旧 `path_transfer/import/source_path/{named,selected}/{basic,metadata}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture tag retention subowner split | runtime_session_archive_path_capture_tag_retention_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_capture/world/retention/tag.rs` 与 `path_capture/level/retention/tag.rs` 已硬切换为 folder-backed `tag/` path capture retention 子树：world 与 level 的 `tag/mod.rs` 仅保留结构入口，`named.rs` 承接 explicit slot-id tag-retention capture-to-path commit/preview helpers，`selected.rs` 承接 selector-based tag-retention capture-to-path commit/preview helpers，`preserving_metadata.rs` 承接 metadata-preserving selected tag-retention capture-to-path commit/preview helpers。所有 child 仍只委托既有 archive-owned tag-retention capture behavior 与 `io` path save/load owner，所有 `RuntimeSessionArchive::*with_tag_retention_to_path*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_capture/world/retention/tag/mod.rs` 3 行、`world/.../tag/named.rs` 38 行、`world/.../tag/selected.rs` 41 行、`world/.../tag/preserving_metadata.rs` 39 行、`path_capture/level/retention/tag/mod.rs` 3 行、`level/.../tag/named.rs` 35 行、`level/.../tag/selected.rs` 36 行、`level/.../tag/preserving_metadata.rs` 39 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world named tag retention path capture writes only target archive、selected tag retention path capture delegates tag bucket policy、metadata-preserving tag retention path capture keeps selected metadata 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_capture/{world,level}/retention/tag/{mod,named,selected,preserving_metadata}.rs` 并通过；本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture world retention commit-preview variant subowner split | runtime_session_archive_path_capture_world_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | world path capture retention `{global,tag}/{named,selected,preserving_metadata}.rs` leaves 已硬切换为 folder-backed `session/path_capture/world/retention/{global,tag}/{named,selected,preserving_metadata}/{mod,commit,preview}.rs` 子树：六个 leaf `mod.rs` 仅保留结构入口，`commit.rs` 承接 rollback-protected capture-retention-to-path helpers，`preview.rs` 承接 no-write persisted archive capture-retention preview helpers。所有 `RuntimeSessionArchive::capture_world*_with*_retention_to_path_atomically` 与 `preview_capture_world*_with*_retention_to_path` public method names、selector/metadata-preserving semantics、archive format、gameplay save schema、editor/plugin/render/UI 行为均不变；matching level path capture retention leaves are now recorded in the following row. 模块边界当前 `world/retention/global/mod.rs` 3 行、`global/named/{mod,commit,preview}.rs` 41 行、`global/selected/{mod,commit,preview}.rs` 44 行、`global/preserving_metadata/{mod,commit,preview}.rs` 43 行、`world/retention/tag/mod.rs` 3 行、`tag/named/{mod,commit,preview}.rs` 44 行、`tag/selected/{mod,commit,preview}.rs` 48 行、`tag/preserving_metadata/{mod,commit,preview}.rs` 45 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world retention path commit writes only target archive、world retention path preview remains read-only、world tag/global metadata-preserving delegates preserve selector metadata 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_capture/world/retention/{mod,global/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}},tag/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}}}.rs` 并通过；cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive path capture level retention commit-preview variant subowner split | runtime_session_archive_path_capture_level_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | level path capture retention `{global,tag}/{named,selected,preserving_metadata}.rs` leaves 已硬切换为 folder-backed `session/path_capture/level/retention/{global,tag}/{named,selected,preserving_metadata}/{mod,commit,preview}.rs` 子树：六个 leaf `mod.rs` 仅保留结构入口，`commit.rs` 承接 rollback-protected capture-retention-to-path helpers，`preview.rs` 承接 no-write persisted archive capture-retention preview helpers。所有 `RuntimeSessionArchive::capture_level*_with*_retention_to_path_atomically` 与 `preview_capture_level*_with*_retention_to_path` public method names、selector/metadata-preserving semantics、archive format、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `level/retention/mod.rs` 2 行、`level/retention/global/mod.rs` 3 行、`global/named/{mod,commit,preview}.rs` 39 行、`global/selected/{mod,commit,preview}.rs` 39 行、`global/preserving_metadata/{mod,commit,preview}.rs` 43 行、`level/retention/tag/mod.rs` 3 行、`tag/named/{mod,commit,preview}.rs` 41 行、`tag/selected/{mod,commit,preview}.rs` 42 行、`tag/preserving_metadata/{mod,commit,preview}.rs` 45 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 level retention path commit writes only target archive、level retention path preview remains read-only、level tag/global metadata-preserving delegates preserve selector metadata 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_capture/level/retention/{mod,global/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}},tag/{mod,named/{mod,commit,preview},selected/{mod,commit,preview},preserving_metadata/{mod,commit,preview}}}.rs` 并通过；旧 `path_capture/level/retention/{global,tag}/{named,selected,preserving_metadata}.rs` flat leaf files absent；cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive path export visibility repair | runtime_session_archive_path_export_visibility_repair_rustfmt_passed_cargo_deferred_active_lane | 2026-06-19 | `session/path_export/loaded/mod.rs` 与 `session/path_export/source_path/mod.rs` 的 child helper re-export 已从 `pub(super)` 收束为 `pub(in crate::scene::dynamic_scene::session)`，对齐相邻 `path_transfer/import/loaded` owner split 模式，使 `session/path_export/mod.rs` 可继续向 session 父层 re-export 同一批 save/preview/single-slot export helper，而不会触发 E0364/E0603 private re-export/import 漂移。该切片只修复 folder-backed owner split 后的可见性边界，不改 archive 格式、`RuntimeSessionArchive` public method names、path API facade 行为、editor/plugin/render/UI 行为。验证：`rustfmt --edition 2021 --check zircon_runtime/src/scene/dynamic_scene/session/path_export/mod.rs zircon_runtime/src/scene/dynamic_scene/session/path_export/loaded/mod.rs zircon_runtime/src/scene/dynamic_scene/session/path_export/source_path/mod.rs` 通过；Cargo recheck 因同轮已有外部 Cargo/rustc lanes 正在运行而 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot copy variant subowner split | runtime_session_archive_slot_copy_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_copy/{named,selected}.rs` 已硬切换为 folder-backed `session/slot_copy/{named,selected}/` in-memory copy/save-as variant 子树；当日 variant split 先把 named/selected copy families 收束到各自目录，后续已在 2026-06-20 下一行细化为 `session/slot_copy/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` commit/preview variant 子 owner。`named/basic` 仍复用 source slot metadata 后委托 metadata owner，`selected/{basic,metadata}` 仍先解析 `RuntimeSessionSlotSelector` 再委托 named owner；destination id normalization、duplicate-slot rejection、metadata normalization、source slot entity/resource projection 与 public `RuntimeSessionArchive::copy*_slot*` names 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic copy preserves source metadata before metadata delegate、metadata copy preview/commit parity、selected metadata copy resolves selector once 三个锚点。验证：variant split 当时 `rustfmt --edition 2021 --check`、conflict marker 与 trailing whitespace scans 通过；Cargo check 未启动，因为 `.codex/sessions/20260619-2015-runtime05-scene-closeout-gate.md` owns active Runtime 05 full `scene::` Cargo lane；单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive slot copy commit-preview variant subowner split | runtime_session_archive_slot_copy_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | slot copy `{named,selected}/{basic,metadata}.rs` leaves 已硬切换为 folder-backed `session/slot_copy/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 子树：四个 `{basic,metadata}/mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 in-memory copy/save-as commit helpers，`preview.rs` 承接 no-write copy preview reports。所有 helper names 与 `RuntimeSessionArchive::copy*_slot*` / `preview_copy*_slot*` 公开方法名保持不变，source metadata reuse、metadata normalization、duplicate-slot rejection、selector resolution、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_copy/mod.rs` 9 行、`named/mod.rs` 6 行、`named/basic/mod.rs` 4 行、`named/basic/commit.rs` 10 行、`named/basic/preview.rs` 12 行、`named/metadata/mod.rs` 4 行、`named/metadata/commit.rs` 16 行、`named/metadata/preview.rs` 32 行、`selected/mod.rs` 8 行、`selected/basic/mod.rs` 4 行、`selected/basic/commit.rs` 12 行、`selected/basic/preview.rs` 13 行、`selected/metadata/mod.rs` 4 行、`selected/metadata/commit.rs` 14 行、`selected/metadata/preview.rs` 14 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic copy commit/preview metadata parity、metadata copy preview remains read-only、selected metadata copy keeps selector delegation in selected owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/slot_copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；旧 `slot_copy/{named,selected}/{basic,metadata}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer copy commit-preview variant subowner split | runtime_session_archive_facade_transfer_copy_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/{named,selected}/{basic,metadata}.rs` 已硬切换为 folder-backed public archive copy facade commit/preview variant 子树：四个 `{basic,metadata}/mod.rs` 仅保留结构入口，`commit.rs` 承接 mutating copy/save-as facade methods，`preview.rs` 承接 dry-run preview copy/save-as facade methods。所有 child 仍只委托既有 `slot_copy/` behavior owner，所有 `RuntimeSessionArchive::copy_slot*`、`preview_copy_slot*`、`copy_selected_slot*` 与 `preview_copy_selected_slot*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `named/basic/mod.rs` 2 行、`named/basic/commit.rs` 12 行、`named/basic/preview.rs` 13 行、`named/metadata/mod.rs` 2 行、`named/metadata/commit.rs` 13 行、`named/metadata/preview.rs` 14 行、`selected/basic/mod.rs` 2 行、`selected/basic/commit.rs` 12 行、`selected/basic/preview.rs` 13 行、`selected/metadata/mod.rs` 2 行、`selected/metadata/commit.rs` 14 行、`selected/metadata/preview.rs` 14 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic copy commit facade delegates only to slot_copy、named metadata copy preview remains read-only delegation、selected metadata copy commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/copy/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/transfer/copy/{named,selected}/{basic,metadata}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer copy variant subowner split | runtime_session_archive_facade_transfer_copy_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy/{named,selected}.rs` 已硬切换为 folder-backed `session/facade/transfer/copy/{named,selected}/` public archive facade variant 子树：每个 `mod.rs` 仅保留结构入口，`basic/` 承接 plain copy/save-as commit/preview facade variant 子树，`metadata/` 承接 metadata override copy/save-as commit/preview facade variant 子树。所有 child 仍只委托既有 `slot_copy/` behavior owner，所有 `RuntimeSessionArchive::copy_slot*` / `preview_copy_slot*` / `copy_selected_slot*` / `preview_copy_selected_slot*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/copy/mod.rs` 2 行、`facade/transfer/copy/named/mod.rs` 2 行、`facade/transfer/copy/named/basic/mod.rs` 2 行、`facade/transfer/copy/named/basic/commit.rs` 12 行、`facade/transfer/copy/named/basic/preview.rs` 13 行、`facade/transfer/copy/named/metadata/mod.rs` 2 行、`facade/transfer/copy/named/metadata/commit.rs` 13 行、`facade/transfer/copy/named/metadata/preview.rs` 14 行、`facade/transfer/copy/selected/mod.rs` 2 行、`facade/transfer/copy/selected/basic/mod.rs` 2 行、`facade/transfer/copy/selected/basic/commit.rs` 12 行、`facade/transfer/copy/selected/basic/preview.rs` 13 行、`facade/transfer/copy/selected/metadata/mod.rs` 2 行、`facade/transfer/copy/selected/metadata/commit.rs` 14 行、`facade/transfer/copy/selected/metadata/preview.rs` 14 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic facade delegates only to slot_copy plain owner、named metadata facade delegates only to metadata owner、selected metadata facade keeps selector resolution in behavior owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；old `facade/transfer/copy/{named,selected}.rs` files absent。Cargo check 未启动，因为 `.codex/sessions/20260619-2015-runtime05-scene-closeout-gate.md` owns active Runtime 05 full `scene::` Cargo lane；单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer import commit-preview variant subowner split | runtime_session_archive_facade_transfer_import_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/{named,selected}/{basic,metadata}.rs` 已硬切换为 folder-backed public archive import facade commit/preview variant 子树：四个 `{basic,metadata}/mod.rs` 仅保留结构入口，`commit.rs` 承接 mutating loaded-archive import facade methods，`preview.rs` 承接 dry-run preview import facade methods。所有 child 仍只委托既有 `slot_import/` behavior owner，所有 `RuntimeSessionArchive::*import*from_archive*` 与 `preview_import*from_archive*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `named/basic/mod.rs` 2 行、`named/basic/commit.rs` 13 行、`named/basic/preview.rs` 14 行、`named/metadata/mod.rs` 2 行、`named/metadata/commit.rs` 20 行、`named/metadata/preview.rs` 21 行、`selected/basic/mod.rs` 2 行、`selected/basic/commit.rs` 13 行、`selected/basic/preview.rs` 19 行、`selected/metadata/mod.rs` 2 行、`selected/metadata/commit.rs` 21 行、`selected/metadata/preview.rs` 21 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic import commit facade delegates only to slot_import、named metadata import preview remains read-only delegation、selected metadata import commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/import/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/transfer/import/{named,selected}/{basic,metadata}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer import variant subowner split | runtime_session_archive_facade_transfer_import_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import/{named,selected}.rs` 已硬切换为 folder-backed `session/facade/transfer/import/{named,selected}/` public archive import facade variant 子树：每个 `mod.rs` 仅保留结构入口，`basic/` 承接 plain loaded-archive import commit/preview facade variant 子树，`metadata/` 承接 metadata override import commit/preview facade variant 子树。所有 child 仍只委托既有 `slot_import/` behavior owner，所有 `RuntimeSessionArchive::*import*from_archive*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/import/mod.rs` 2 行、`facade/transfer/import/named/mod.rs` 2 行、`facade/transfer/import/named/basic/mod.rs` 2 行、`facade/transfer/import/named/basic/commit.rs` 13 行、`facade/transfer/import/named/basic/preview.rs` 14 行、`facade/transfer/import/named/metadata/mod.rs` 2 行、`facade/transfer/import/named/metadata/commit.rs` 20 行、`facade/transfer/import/named/metadata/preview.rs` 21 行、`facade/transfer/import/selected/mod.rs` 2 行、`facade/transfer/import/selected/basic/mod.rs` 2 行、`facade/transfer/import/selected/basic/commit.rs` 13 行、`facade/transfer/import/selected/basic/preview.rs` 19 行、`facade/transfer/import/selected/metadata/mod.rs` 2 行、`facade/transfer/import/selected/metadata/commit.rs` 21 行、`facade/transfer/import/selected/metadata/preview.rs` 21 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic facade delegates only to slot_import plain owner、named metadata facade delegates only to metadata owner、selected metadata facade keeps selector delegation in behavior owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/import/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；old `facade/transfer/import/{named,selected}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer export commit-preview variant subowner split | runtime_session_archive_facade_transfer_export_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export/{named,selected}.rs` 已硬切换为 folder-backed public archive export facade variant 子树：`named/mod.rs` 与 `selected/mod.rs` 仅保留结构入口，`named/commit.rs` 承接 named single-slot archive export facade method，`named/preview.rs` 承接 named single-slot archive export preview facade method，`selected/commit.rs` 承接 selector-based single-slot archive export facade method，`selected/preview.rs` 承接 selector-based single-slot archive export preview facade method。所有 child 仍只委托既有 `session/slot_export/` behavior owner，所有 `RuntimeSessionArchive::*single_slot_archive*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/export/mod.rs` 2 行、`facade/transfer/export/named/mod.rs` 2 行、`facade/transfer/export/named/commit.rs` 8 行、`facade/transfer/export/named/preview.rs` 12 行、`facade/transfer/export/selected/mod.rs` 2 行、`facade/transfer/export/selected/commit.rs` 11 行、`facade/transfer/export/selected/preview.rs` 12 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named export commit facade delegates only to slot_export、named export preview remains read-only delegation、selected export commit/preview keeps selector delegation in behavior owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/export/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/transfer/export/{named,selected}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer export subowner split | runtime_session_archive_facade_transfer_export_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/export.rs` 已硬切换为 folder-backed `session/facade/transfer/export/` public archive export facade 子树：`mod.rs` 仅保留结构入口，`named/` 子树承接 named single-slot archive export/preview facade methods，`selected/` 子树承接 selector-based single-slot archive export/preview facade methods。所有 child 仍只委托既有 `slot_export/` behavior owner，所有 `RuntimeSessionArchive::*single_slot_archive*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/export/mod.rs` 2 行、`facade/transfer/export/named/mod.rs` 2 行、`facade/transfer/export/named/commit.rs` 8 行、`facade/transfer/export/named/preview.rs` 12 行、`facade/transfer/export/selected/mod.rs` 2 行、`facade/transfer/export/selected/commit.rs` 11 行、`facade/transfer/export/selected/preview.rs` 12 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named export facade delegates only to slot_export named owner、selected export facade delegates only to slot_export selected owner、export facade remains no direct archive mutation logic 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/facade/transfer/export/{mod,named,selected}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/facade/transfer/export/{mod,named/{mod,commit,preview},selected/{mod,commit,preview}}.rs` 并通过；Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer copy facade commit-preview variant subowner split | runtime_session_archive_path_api_transfer_copy_facade_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/{named,selected}/{basic,metadata}.rs` 已硬切换为 folder-backed public path copy facade commit/preview variant 子树：四个 `{basic,metadata}/mod.rs` 仅保留结构入口，`commit.rs` 承接 atomic copy/save-as facade methods，`preview.rs` 承接 no-write preview copy/save-as facade methods。所有 child 仍只委托既有 `path_transfer/` behavior owner，所有 `RuntimeSessionArchive::*copy*from_path*` 与 `*_at_path_atomically` copy inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `named/basic/mod.rs` 2 行、`named/basic/commit.rs` 13 行、`named/basic/preview.rs` 14 行、`named/metadata/mod.rs` 2 行、`named/metadata/commit.rs` 20 行、`named/metadata/preview.rs` 20 行、`selected/basic/mod.rs` 2 行、`selected/basic/commit.rs` 14 行、`selected/basic/preview.rs` 14 行、`selected/metadata/mod.rs` 2 行、`selected/metadata/commit.rs` 20 行、`selected/metadata/preview.rs` 20 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic path copy commit facade delegates only to path_transfer、named metadata path copy preview remains read-only delegation、selected metadata path copy commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/transfer/copy/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `path_api/transfer/copy/{named,selected}/{basic,metadata}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer copy facade variant subowner split | runtime_session_archive_path_api_transfer_copy_facade_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy/{named,selected}.rs` 已硬切换为 folder-backed `session/path_api/transfer/copy/{named,selected}/` public path facade variant 子树：每个 `mod.rs` 仅保留结构入口，`basic/` 承接 plain copy/save-as commit/preview facade variant 子树，`metadata/` 承接 metadata override copy/save-as commit/preview facade variant 子树。所有 child 仍只委托既有 `path_transfer/` behavior owner，所有 `RuntimeSessionArchive::*copy*from_path*` / `*_at_path_atomically` copy inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/transfer/copy/mod.rs` 2 行、`path_api/transfer/copy/named/mod.rs` 2 行、`path_api/transfer/copy/named/basic/mod.rs` 2 行、`path_api/transfer/copy/named/basic/commit.rs` 13 行、`path_api/transfer/copy/named/basic/preview.rs` 14 行、`path_api/transfer/copy/named/metadata/mod.rs` 2 行、`path_api/transfer/copy/named/metadata/commit.rs` 20 行、`path_api/transfer/copy/named/metadata/preview.rs` 20 行、`path_api/transfer/copy/selected/mod.rs` 2 行、`path_api/transfer/copy/selected/basic/mod.rs` 2 行、`path_api/transfer/copy/selected/basic/commit.rs` 14 行、`path_api/transfer/copy/selected/basic/preview.rs` 14 行、`path_api/transfer/copy/selected/metadata/mod.rs` 2 行、`path_api/transfer/copy/selected/metadata/commit.rs` 20 行、`path_api/transfer/copy/selected/metadata/preview.rs` 20 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic facade delegates only to path_transfer plain copy、named metadata facade delegates only to metadata copy path、selected metadata facade keeps selector delegation in behavior owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/transfer/copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；old `path_api/transfer/copy/{named,selected}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer copy variant subowner split | runtime_session_archive_path_transfer_copy_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy/{named,selected}.rs` 已硬切换为 folder-backed `session/path_transfer/copy/{named,selected}/` behavior variant 子树；当日 variant split 先把 named/selected path copy families 收束到各自目录，后续已在 2026-06-20 下一行细化为 `session/path_transfer/copy/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` commit/preview variant 子 owner。`session/path_transfer/copy/mod.rs` 继续 re-export 同名 helper，`session/path_api/transfer/copy/` facade 继续通过这些 helper 调用 behavior owner，所有 `RuntimeSessionArchive::*copy*from_path*` / `*_at_path_atomically` copy inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic path copy mutates target archive atomically、named metadata path copy forwards metadata override、selected metadata path copy resolves selector inside loaded archive 三个锚点。验证：variant split 当时 `rustfmt --edition 2021 --check`、conflict marker 与 trailing whitespace scans 通过；Cargo check 未启动，因为 `.codex/sessions/20260619-2015-runtime05-scene-closeout-gate.md` owns active Runtime 05 full `scene::` Cargo lane；单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer copy commit-preview variant subowner split | runtime_session_archive_path_transfer_copy_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path transfer copy `{named,selected}/{basic,metadata}.rs` leaves 已硬切换为 folder-backed `session/path_transfer/copy/{named,selected}/{basic,metadata}/{mod,commit,preview}.rs` 子树：四个 `{basic,metadata}/mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 rollback-protected copy/save-as at-path helpers，`preview.rs` 承接 no-write path copy preview helpers。所有 helper names 与 `RuntimeSessionArchive::*copy*from_path*` / `*_at_path_atomically` 公开方法名保持不变，metadata override、selector resolution、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_transfer/copy/mod.rs` 10 行、`named/mod.rs` 8 行、`named/basic/mod.rs` 4 行、`named/basic/commit.rs` 14 行、`named/basic/preview.rs` 11 行、`named/metadata/mod.rs` 4 行、`named/metadata/commit.rs` 15 行、`named/metadata/preview.rs` 12 行、`selected/mod.rs` 9 行、`selected/basic/mod.rs` 4 行、`selected/basic/commit.rs` 14 行、`selected/basic/preview.rs` 12 行、`selected/metadata/mod.rs` 4 行、`selected/metadata/commit.rs` 16 行、`selected/metadata/preview.rs` 17 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named basic path copy commit/preview parity、metadata path copy preview remains read-only、selected metadata path copy keeps selector delegation in archive owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_transfer/copy/{mod,named/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}},selected/{mod,basic/{mod,commit,preview},metadata/{mod,commit,preview}}}.rs` 并通过；旧 `path_transfer/copy/{named,selected}/{basic,metadata}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload/path export follow-up Cargo recheck | runtime_05_asset_reload_path_export_followup_cargo_check_passed_tests_deferred | 2026-06-19 | 在外部 Cargo/rustc lanes 清空后复跑 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-asset-reload-owner-split-0619 --message-format short --color never`，150.7s 完成并通过，仅保留既有 warning。该复验覆盖前置 `asset_reload/{mod,queue,reports,result,skip,task}.rs` owner split 与 `session/path_export/{loaded,source_path}/mod.rs` session-scoped visibility repair，确认前一轮 E0364/E0603 private re-export/import 阻塞已消除；未改 archive 格式、`RuntimeSessionArchive` public method names、path API facade 行为、editor/plugin/render/UI 行为。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture global retention subowner split | runtime_session_archive_path_capture_global_retention_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_capture/world/retention/global.rs` 与 `path_capture/level/retention/global.rs` 已硬切换为 folder-backed `global/` path capture retention 子树：world 与 level 的 `global/mod.rs` 仅保留结构入口，`named.rs` 承接 explicit slot-id global-retention capture-to-path commit/preview helpers，`selected.rs` 承接 selector-based global-retention capture-to-path commit/preview helpers，`preserving_metadata.rs` 承接 metadata-preserving selected global-retention capture-to-path commit/preview helpers。所有 child 仍只委托既有 archive-owned capture-retention behavior 与 `io` path save/load owner，所有 `RuntimeSessionArchive::*with_retention_to_path*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_capture/world/retention/global/mod.rs` 3 行、`world/.../global/named.rs` 35 行、`world/.../global/selected.rs` 37 行、`world/.../global/preserving_metadata.rs` 37 行、`path_capture/level/retention/global/mod.rs` 3 行、`level/.../global/named.rs` 33 行、`level/.../global/selected.rs` 33 行、`level/.../global/preserving_metadata.rs` 37 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world named global retention path capture writes only target archive、selected global retention path capture resolves selector through archive owner、metadata-preserving global retention path capture keeps selected metadata 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_capture/{world,level}/retention/global/{mod,named,selected,preserving_metadata}.rs` 并通过；本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API merge facade subowner split | runtime_session_archive_path_api_merge_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/merge.rs` 已硬切换为 folder-backed `session/path_api/merge/` public path facade 子树：`mod.rs` 仅保留结构入口，`loaded` 与 `source_path` initially landed as flat facade leaves；下一行 2026-06-20 follow-up 已把两者继续拆为 commit/preview child owner。所有 child 仍只委托既有 `path_merge/` behavior owner，所有 `RuntimeSessionArchive::*merge*at_path*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded merge facade delegates only to path_merge loaded owner、source-path merge facade delegates only to source-path behavior owner、path API merge facade contains no direct archive mutation logic 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/path_api/merge/{mod,loaded,source_path}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/path_api/merge/{mod,loaded/{mod,commit,preview},source_path/{mod,commit,preview}}.rs` 并通过；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API merge facade commit-preview variant subowner split | runtime_session_archive_path_api_merge_facade_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path API merge facade `{loaded,source_path}.rs` leaves 已硬切换为 folder-backed `session/path_api/merge/{loaded,source_path}/{mod,commit,preview}.rs` public facade 子树：`loaded/mod.rs` 与 `source_path/mod.rs` 仅保留结构入口，`loaded/commit.rs` 承接 loaded incoming archive merge at path commit facade，`loaded/preview.rs` 承接 loaded incoming merge no-write preview facade，`source_path/commit.rs` 承接 source archive path merge at path commit facade，`source_path/preview.rs` 承接 source archive path merge no-write preview facade。所有 public facade method names 与 `scene::dynamic_scene` re-export surface 保持不变，source/target same-path preflight 与 archive mutation logic 仍留在 `session/path_merge/` behavior owner；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/merge/mod.rs` 2 行、`path_api/merge/loaded/mod.rs` 2 行、`path_api/merge/loaded/commit.rs` 14 行、`path_api/merge/loaded/preview.rs` 14 行、`path_api/merge/source_path/mod.rs` 2 行、`path_api/merge/source_path/commit.rs` 14 行、`path_api/merge/source_path/preview.rs` 14 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded merge commit facade delegates only to path_merge loaded commit owner、source-path merge preview delegates only to path_merge source-path preview owner、path API merge facade contains no direct archive mutation logic 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/merge/{mod,loaded/{mod,commit,preview},source_path/{mod,commit,preview}}.rs` 并通过；旧 `path_api/merge/{loaded,source_path}.rs` flat files absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API export facade commit-preview variant subowner split | runtime_session_archive_path_api_export_facade_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/export/{loaded,source_path}/{named,selected}.rs` 已硬切换为 folder-backed public path export facade commit/preview variant 子树：四个 `{named,selected}/mod.rs` 仅保留结构入口，`commit.rs` 承接 archive-producing projection 与 atomic save facade methods，`preview.rs` 承接 no-write preview report facade methods。所有 child 仍只委托 `session/path_export/` behavior owner，不上移 source/target distinct-path preflight 或 archive mutation logic；所有 `RuntimeSessionArchive::*single_slot_archive*path*` public method names 与 `scene::dynamic_scene` re-export surface 保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/export/mod.rs` 2 行、`path_api/export/loaded/mod.rs` 2 行、`path_api/export/loaded/named/mod.rs` 2 行、`path_api/export/loaded/named/commit.rs` 15 行、`path_api/export/loaded/named/preview.rs` 16 行、`path_api/export/loaded/selected/mod.rs` 2 行、`path_api/export/loaded/selected/commit.rs` 20 行、`path_api/export/loaded/selected/preview.rs` 16 行、`path_api/export/source_path/mod.rs` 2 行、`path_api/export/source_path/named/mod.rs` 2 行、`path_api/export/source_path/named/commit.rs` 26 行、`path_api/export/source_path/named/preview.rs` 16 行、`path_api/export/source_path/selected/mod.rs` 2 行、`path_api/export/source_path/selected/commit.rs` 27 行、`path_api/export/source_path/selected/preview.rs` 20 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named export commit facade delegates only to path_export、source-path selected export commit keeps projection/save delegation in behavior owner、path export preview facades remain no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/export/{loaded,source_path}/{named,selected}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `path_api/export/{loaded,source_path}/{named,selected}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path API export loaded facade subowner split | runtime_session_archive_path_api_export_loaded_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/export/loaded.rs` 已硬切换为 folder-backed `session/path_api/export/loaded/` public path facade 子树：`mod.rs` 仅保留结构入口，`named/` 承接 explicit slot-id loaded target-path save/preview facade variant 子树，`selected/` 承接 selector-based loaded target-path save/preview facade variant 子树。所有 child 仍只委托既有 `path_export/loaded/` behavior owner，所有 `RuntimeSessionArchive::save_*single_slot_archive_to_path_atomically` / `preview_save_*single_slot_archive_to_path` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/export/loaded/mod.rs` 2 行、`path_api/export/loaded/named/mod.rs` 2 行、`path_api/export/loaded/named/commit.rs` 15 行、`path_api/export/loaded/named/preview.rs` 16 行、`path_api/export/loaded/selected/mod.rs` 2 行、`path_api/export/loaded/selected/commit.rs` 20 行、`path_api/export/loaded/selected/preview.rs` 16 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named loaded export facade delegates only to path_export loaded named owner、selected loaded export facade delegates only to selector loaded owner、loaded facade remains delegation-only 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/path_api/export/loaded/{mod,named,selected}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/path_api/export/loaded/{named,selected}/{mod,commit,preview}.rs` 并通过；旧 `path_api/export/loaded/{named,selected}.rs` files absent；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload owner split | dynamic_scene_asset_reload_owner_split_rustfmt_passed_cargo_blocked_external_path_export | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/asset_reload.rs` 已硬切换为 folder-backed `asset_reload/{mod,queue,reports,result,skip,task}.rs`：`mod.rs` 保持结构入口，`queue.rs` owns typed scene asset event drain/schedule/ready collection，`reports.rs` owns frame/drain/ready/apply report DTOs，`result.rs` owns prepared payload apply/stale/superseded result DTOs，`skip.rs` owns skip reason/event DTOs，`task.rs` owns pending task/task snapshot；`scene::dynamic_scene` public re-export surface 与所有 `DynamicSceneAssetReload*` 名称不变。验证：`rustfmt --edition 2021 --check` 覆盖新 asset_reload subtree 并通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-asset-reload-owner-split-0619 --message-format short --color never` 两次复跑均进入依赖/`zircon_runtime` 编译后失败，最终诊断为既有 `session/path_export/mod.rs` private re-export drift 与 `session/path_api/export/**` private function import errors（E0364/E0603），不在本切片触碰范围；未声明 Cargo pass。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path single-slot export loaded subowner split | runtime_session_archive_path_export_loaded_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_export/loaded.rs` 已硬切换为 folder-backed `session/path_export/loaded/` behavior 子树：`mod.rs` 仅保留 session-scoped helper re-export，`named.rs` 承接 explicit slot-id loaded archive target-path save/preview helpers，`selected.rs` 承接 selector-based loaded archive target-path save/preview helpers。`session/path_export/mod.rs` 继续 re-export 同名 helper，`session/path_api/export/` facade 公开方法名保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_export/loaded/mod.rs` 10 行、`path_export/loaded/named/{mod,commit,preview}.rs` 26 行、`path_export/loaded/selected/{mod,commit,preview}.rs` 28 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named loaded export saves single-slot archive atomically、selected loaded export resolves selector before export、loaded export preview remains no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_export/loaded/{mod,named,selected}.rs` 并通过；本切片后补跑 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never`：第一次在依赖编译阶段 exit 1 且未返回 Rust 诊断，第二次 304s 超时；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API export source-path facade subowner split | runtime_session_archive_path_api_export_source_path_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/export/source_path.rs` 已硬切换为 folder-backed `session/path_api/export/source_path/` public path facade 子树：`mod.rs` 仅保留结构入口，`named/` 承接 named source archive path projection/save/preview facade variant 子树，`selected/` 承接 selector-based source archive path projection/save/preview facade variant 子树。所有 child 仍只委托既有 `path_export/` behavior owner，所有 `RuntimeSessionArchive::*single_slot_archive_from_path*` / `preview_save_*from_path` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/export/source_path/mod.rs` 2 行、`path_api/export/source_path/named/mod.rs` 2 行、`path_api/export/source_path/named/commit.rs` 26 行、`path_api/export/source_path/named/preview.rs` 16 行、`path_api/export/source_path/selected/mod.rs` 2 行、`path_api/export/source_path/selected/commit.rs` 27 行、`path_api/export/source_path/selected/preview.rs` 20 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named source-path export facade delegates only to path_export named owner、selected source-path export facade delegates only to path_export selected owner、preview facade remains delegation-only 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/path_api/export/source_path/{mod,named,selected}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/path_api/export/source_path/{named,selected}/{mod,commit,preview}.rs` 并通过；旧 `path_api/export/source_path/{named,selected}.rs` files absent；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path single-slot export source-path subowner split | runtime_session_archive_path_export_source_path_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_export/source_path.rs` 已硬切换为 folder-backed `session/path_export/source_path/` behavior 子树：`mod.rs` 仅保留 session-scoped helper re-export，`named.rs` 承接 named source archive projection/save/preview helpers，`selected.rs` 承接 selector-based source archive projection/save/preview helpers。两个 child 都保留 source/target distinct-path preflight，再委托 loaded-archive target-path save/preview helpers；`session/path_export/mod.rs` 继续 re-export 同名 helper，`session/path_api/export/` facade 公开方法名保持不变。archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_export/source_path/mod.rs` 11 行、`path_export/source_path/named/{mod,commit,preview}.rs` 53 行、`path_export/source_path/selected/{mod,commit,preview}.rs` 55 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named source-path export rejects same target before load、selected source-path export resolves selector through loaded export path、source-path export preview remains no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_export/source_path/{mod,named,selected}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在前置切片反复超时，本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path single-slot export behavior commit-preview variant subowner split | runtime_session_archive_path_export_behavior_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path single-slot export behavior leaves 已硬切换为 folder-backed `session/path_export/{loaded,source_path}/{named,selected}/{mod,commit,preview}.rs` 子树：四个 `{named,selected}/mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 standalone archive construction / projection 与 rollback-protected atomic save helpers，`preview.rs` 承接 no-write export preflight report helpers。`session/path_export/mod.rs` 继续 re-export 同名 helper，`session/path_api/export/` facade 公开方法名保持不变；source/target distinct-path preflight 仍在 source-path behavior owner，selector resolution 仍在 selected behavior owner，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `loaded/named/mod.rs` 5 行、`loaded/named/commit.rs` 17 行、`loaded/named/preview.rs` 14 行、`loaded/selected/mod.rs` 5 行、`loaded/selected/commit.rs` 19 行、`loaded/selected/preview.rs` 15 行、`source_path/named/mod.rs` 7 行、`source_path/named/commit.rs` 34 行、`source_path/named/preview.rs` 25 行、`source_path/selected/mod.rs` 7 行、`source_path/selected/commit.rs` 34 行、`source_path/selected/preview.rs` 25 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named behavior commit calls preview first、source-path selected preview rejects same target without writing、selected loaded behavior resolves selector once 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_export/{loaded,source_path}/{named,selected}/{mod,commit,preview}.rs` 并通过；旧 `path_export/{loaded,source_path}/{named,selected}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer copy subowner split | runtime_session_archive_facade_transfer_copy_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/copy.rs` 已硬切换为 folder-backed `session/facade/transfer/copy/` public archive facade 子树：`mod.rs` 仅保留结构入口，`named.rs` 承接 named copy/save-as commit/preview facade methods，`selected.rs` 承接 selector-based copy/save-as commit/preview facade methods。所有 child 仍只委托既有 `slot_copy/` behavior owner，所有 `RuntimeSessionArchive::copy_slot*` / `preview_copy_slot*` / `copy_selected_slot*` / `preview_copy_selected_slot*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/copy/mod.rs` 2 行、`facade/transfer/copy/named.rs` 37 行、`facade/transfer/copy/selected.rs` 37 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named transfer copy facade delegates only to slot_copy named owner、selected transfer copy facade delegates only to slot_copy selected owner、preview transfer copy facade remains read-only 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/copy/{mod,named,selected}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在前置切片反复超时，本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer copy subowner split | runtime_session_archive_path_transfer_copy_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/copy.rs` 已硬切换为 folder-backed `session/path_transfer/copy/` behavior 子树：`mod.rs` 仅保留 session-scoped helper re-export，`named.rs` 承接 path-level named copy/save-as commit/preview helpers，`selected.rs` 承接 selector-based path copy/save-as commit/preview helpers。`session/path_transfer/mod.rs` 继续 re-export 同名 helper，`session/path_api/transfer/copy/` facade 继续通过这些 helper 调用 behavior owner，所有 `RuntimeSessionArchive::*copy*from_path*` / `*_at_path_atomically` copy inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_transfer/copy/mod.rs` 11 行、`path_transfer/copy/named.rs` 46 行、`path_transfer/copy/selected.rs` 50 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named path copy loads/mutates/saves atomically、selected path copy resolves selector through loaded archive copy path、preview copy helpers remain no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_transfer/copy/{mod,named,selected}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在前置 facade query 切片反复超时，本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer copy facade subowner split | runtime_session_archive_path_api_transfer_copy_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/copy.rs` 已硬切换为 folder-backed `session/path_api/transfer/copy/` public path facade 子树：`mod.rs` 仅保留结构入口，`named.rs` 承接 explicit source slot copy/save-as commit/preview facade methods，`selected.rs` 承接 selector-based copy/save-as commit/preview facade methods。所有 child 仍只委托既有 `path_transfer/` behavior owner，所有 `RuntimeSessionArchive::*copy*from_path*` / `*_at_path_atomically` copy inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/transfer/copy/mod.rs` 2 行、`path_api/transfer/copy/named.rs` 47 行、`path_api/transfer/copy/selected.rs` 48 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named copy facade delegates only to path_transfer named copy paths、selected copy facade delegates only to selector copy paths、preview copy facade remains no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/transfer/copy/{mod,named,selected}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在前置 facade query 切片反复超时，本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API query facade subowner split | runtime_session_archive_path_api_query_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/query.rs` 已硬切换为 folder-backed `session/path_api/query/` public path facade 子树：`mod.rs` 仅保留结构入口，`manifest.rs` 承接 manifest/slot summary/filter facade methods，`selection.rs` 承接 latest/oldest/tagged selector 与 typed select facade methods，`statistics.rs` 承接 archive statistics facade method，`status.rs` 承接 non-throwing path inspection facade method。所有 child 仍只委托既有 `path_query/` behavior owner，所有 `RuntimeSessionArchive::*_from_path` query inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/query/mod.rs` 4 行、`path_api/query/manifest.rs` 41 行、`path_api/query/selection.rs` 35 行、`path_api/query/statistics.rs` 11 行、`path_api/query/status.rs` 7 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path API manifest facade delegates only to path_query manifest owner、selection facade delegates only to path_query selection owner、status facade remains non-throwing 三个锚点。验证：首次 `rustfmt --check` 要求 `status.rs` import 折叠，已运行 rustfmt 修正；`rustfmt --edition 2021 --check` 覆盖 `session/path_api/query/{mod,manifest,selection,statistics,status}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在前置 facade query 切片反复超时，本切片后尝试 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-path-capture-retention-0619 --message-format short --color never` 244s timeout；Cargo check 级验证仍 incomplete/deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API export facade subowner split | runtime_session_archive_path_api_export_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/export.rs` 已硬切换为 folder-backed `session/path_api/export/` public path facade 子树：`mod.rs` 仅保留结构入口，`loaded/` 承接 already-loaded archive target-path save/preview facade variant 子树，`source_path/` 承接 source archive path projection/save/preview facade variant 子树。所有 child 仍只委托既有 `path_export/` behavior owner，所有 `RuntimeSessionArchive::*single_slot_archive*path*` export inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/export/mod.rs` 2 行、`path_api/export/loaded/mod.rs` 2 行、`path_api/export/loaded/named/mod.rs` 2 行、`path_api/export/loaded/named/commit.rs` 15 行、`path_api/export/loaded/named/preview.rs` 16 行、`path_api/export/loaded/selected/mod.rs` 2 行、`path_api/export/loaded/selected/commit.rs` 20 行、`path_api/export/loaded/selected/preview.rs` 16 行、`path_api/export/source_path/mod.rs` 2 行、`path_api/export/source_path/named/mod.rs` 2 行、`path_api/export/source_path/named/commit.rs` 26 行、`path_api/export/source_path/named/preview.rs` 16 行、`path_api/export/source_path/selected/mod.rs` 2 行、`path_api/export/source_path/selected/commit.rs` 27 行、`path_api/export/source_path/selected/preview.rs` 20 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded path API export facade delegates only to path_export loaded owner、source-path export facade delegates only to path_export source owner、selected export facade keeps selector resolution in behavior owner 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/path_api/export/{mod,loaded,source_path}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/path_api/export/{loaded,source_path}/{named,selected}/{mod,commit,preview}.rs` 并通过；旧 `path_api/export/{loaded,source_path}/{named,selected}.rs` files absent；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API restore facade subowner split | runtime_session_archive_path_api_restore_facade_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/restore.rs` 已硬切换为 folder-backed `session/path_api/restore/` public path facade 子树：`mod.rs` 仅保留结构入口，`restore.rs`、`apply.rs`、`diff.rs` initially 承接 named/selected restore/apply/diff facade method families；2026-06-20 follow-up row 已继续将这三个 flat leaves 拆为 `{mod,named,selected}.rs` 子树。所有 child 仍只委托既有 `path_restore/` behavior owner，所有 `RuntimeSessionArchive::*_from_path*` restore/apply/diff inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 named-selected variant row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path API restore facade delegates only to path_restore restore owner、path API apply facade delegates only to path_restore apply owner、path API diff facade stays read-only 三个锚点。验证：首次 `rustfmt --check` 要求 `apply.rs` import 排序，已运行 rustfmt 修正；原 `rustfmt --edition 2021 --check` 覆盖 `session/path_api/restore/{mod,restore,apply,diff}.rs` 并通过；后续 named-selected variant split 已追加 scoped static validation。Cargo check 级验证仍 deferred；单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API restore facade named-selected variant subowner split | runtime_session_archive_path_api_restore_facade_named_selected_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path API restore facade `{restore,apply,diff}.rs` leaves 已硬切换为 folder-backed `session/path_api/restore/{restore,apply,diff}/{mod,named,selected}.rs` public facade 子树：三个 `mod.rs` 仅保留结构入口，`restore/named.rs` 承接 explicit slot-id empty-world 与 level restore-from-path facade methods，`restore/selected.rs` 承接 selector-based restore-from-path facade methods，`apply/named.rs` 承接 explicit slot-id world/level apply-from-path facade methods，`apply/selected.rs` 承接 selector-based apply-from-path facade methods，`diff/named.rs` 承接 explicit slot-id world/level diff-from-path facade methods，`diff/selected.rs` 承接 selector-based diff-from-path facade methods。所有 child 仍只委托 `session/path_restore/` behavior owner；所有 public method names、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/restore/mod.rs` 3 行、`path_api/restore/restore/mod.rs` 2 行、`path_api/restore/restore/named.rs` 25 行、`path_api/restore/restore/selected.rs` 25 行、`path_api/restore/apply/mod.rs` 2 行、`path_api/restore/apply/named.rs` 24 行、`path_api/restore/apply/selected.rs` 26 行、`path_api/restore/diff/mod.rs` 2 行、`path_api/restore/diff/named.rs` 25 行、`path_api/restore/diff/selected.rs` 26 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named restore facade delegates only to path_restore restore owner、selected apply facade resolves through path_restore selected apply owner、diff selected facade remains read-only 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/restore/{mod,restore/{mod,named,selected},apply/{mod,named,selected},diff/{mod,named,selected}}.rs` 并通过；旧 `path_api/restore/{restore,apply,diff}.rs` flat leaf files absent；conflict marker、trailing whitespace 与 stale active path scans 通过。Cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive facade validation subowner split | runtime_session_archive_facade_validation_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/validation.rs` 已硬切换为 folder-backed `session/facade/validation/` facade 子树：`mod.rs` 仅保留结构入口，`support.rs` 承接 public `ensure_supported` facade method，`invariants.rs` 承接 session-scoped `require_slot`、`slot_mut`、`sort_slots`、`normalize_slot_metadata` invariant helper facade methods。所有 child 仍只委托既有 `validation` behavior owner，public `ensure_supported` method name 与 session-scoped helper visibility 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/validation/mod.rs` 2 行、`facade/validation/support.rs` 7 行、`facade/validation/invariants.rs` 24 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 ensure_supported facade delegates only to validation owner、private invariant helpers stay session-scoped、slot_mut comment remains with private mutable access 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/validation/{mod,support,invariants}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在 query 切片反复超时，validation 切片未重复等待完整 Cargo check，Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade construction subowner split | runtime_session_archive_facade_construction_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/construction.rs` 已硬切换为 folder-backed `session/facade/construction/` facade 子树：`mod.rs` 仅保留结构入口，`archive.rs` 承接 `empty`、`from_slots` 与 `Default` impl，`capture/` 子树承接 world/level construction facade methods，`serialization.rs` 承接 versioned JSON load/save facade methods。所有 child 仍只委托既有 `construction/` behavior owner，所有 `RuntimeSessionArchive` construction inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/construction/mod.rs` 3 行、`facade/construction/archive.rs` 15 行、`facade/construction/serialization.rs` 10 行、`facade/construction/capture/mod.rs` 2 行、`facade/construction/capture/world.rs` 18 行、`facade/construction/capture/level.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 archive construction facade delegates only to construction archive owner、capture construction facade delegates only to construction capture owner、serialization facade preserves versioned JSON behavior 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/facade/construction/{mod,archive,capture,serialization}.rs` 并通过；后续 capture variant split 已追加覆盖 `session/facade/construction/{mod,archive,serialization,capture/{mod,world,level}}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在 query 切片反复超时，construction 切片未重复等待完整 Cargo check，Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade store subowner split | runtime_session_archive_facade_store_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/store.rs` 已硬切换为 folder-backed `session/facade/store/` facade 子树：`mod.rs` 仅保留结构入口，`push.rs` 承接 `push_slot` facade method，`upsert.rs` 承接 `upsert_slot` facade method，`remove/{mod,commit,preview}.rs` 承接 `remove_slot` commit facade method。`push`/`upsert` child 仍只委托既有 `slot_store` owner，`remove` child 仍只委托既有 `slot_mutation` remove owner，所有 `RuntimeSessionArchive` store inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/store/mod.rs` 3 行、`facade/store/push.rs` 10 行、`facade/store/upsert.rs` 10 行、`facade/store/remove.rs` 7 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 push facade delegates only to slot_store、upsert facade delegates only to slot_store、remove commit facade stays separate from preview remove 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/store/{mod,push,upsert,remove}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在 query 切片反复超时，store 切片未重复等待完整 Cargo check，Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade retention global/tag variant subowner split | runtime_session_archive_facade_retention_global_tag_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/retention/{global,tag}.rs` 已硬切换为 folder-backed public archive retention facade variant 子树：`global/mod.rs` 与 `tag/mod.rs` 仅保留结构入口，`global/commit.rs` 承接 global prune commit facade method，`global/preview.rs` 承接 global prune preview facade method，`tag/commit.rs` 承接 tag-scoped prune commit facade method，`tag/preview.rs` 承接 tag-scoped prune preview facade method。所有 child 仍只委托既有 `session/retention/` behavior owner，所有 `RuntimeSessionArchive::prune_slots*` 与 `preview_prune_slots*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 merge 子树已由后续 row 记录为 `facade/retention/merge/{mod,commit,preview}.rs`，global/tag 边界为 `facade/retention/global/mod.rs` 2 行、`global/commit.rs` 10 行、`global/preview.rs` 10 行、`tag/mod.rs` 2 行、`tag/commit.rs` 11 行、`tag/preview.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global commit facade delegates only to retention prune owner、global preview facade remains read-only delegation、tag commit/preview facade preserves tag-scoped delegation 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/facade/retention/{mod,merge,global/{mod,commit,preview},tag/{mod,commit,preview}}.rs` 并通过；后续 merge commit-preview variant split 已追加覆盖 `session/facade/retention/{mod,merge/{mod,commit,preview},global/{mod,commit,preview},tag/{mod,commit,preview}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/retention/{global,tag}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade retention subowner split | runtime_session_archive_facade_retention_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/retention.rs` 已硬切换为 folder-backed `session/facade/retention/` facade 子树：`mod.rs` 仅保留结构入口，`merge` initially landed as flat merge/preview facade leaf，`global/` 子树承接 global prune/preview facade methods，`tag/` 子树承接 tag-scoped prune/preview facade methods；后续 row 已把 `merge` 继续拆为 commit/preview child owner。所有 child 仍只委托既有 `merge/` 与 `retention/` behavior owners，所有 `RuntimeSessionArchive` merge/retention inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split rows 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 merge facade delegates only to merge owner、global retention facade delegates only to retention prune owner、tag retention facade preserves tag-scoped delegation 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/facade/retention/{mod,merge,global,tag}.rs` 并通过；后续 global/tag 与 merge variant split 已追加覆盖 folder-backed child subtrees；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade retention merge commit-preview variant subowner split | runtime_session_archive_facade_retention_merge_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | facade retention `merge.rs` leaf 已硬切换为 folder-backed `session/facade/retention/merge/{mod,commit,preview}.rs` public archive merge facade 子树：`merge/mod.rs` 仅保留结构入口，`merge/commit.rs` 承接 `RuntimeSessionArchive::merge_archive` commit facade method，`merge/preview.rs` 承接 `RuntimeSessionArchive::preview_merge_archive` no-write preview facade method。两个 child 仍只委托既有 archive-level `session/merge/` behavior owner，所有 `RuntimeSessionArchive` merge inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/retention/mod.rs` 3 行、`facade/retention/merge/mod.rs` 2 行、`facade/retention/merge/commit.rs` 11 行、`facade/retention/merge/preview.rs` 11 行、`facade/retention/global/mod.rs` 2 行、`facade/retention/global/commit.rs` 10 行、`facade/retention/global/preview.rs` 10 行、`facade/retention/tag/mod.rs` 2 行、`facade/retention/tag/commit.rs` 11 行、`facade/retention/tag/preview.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 merge commit facade delegates only to merge behavior owner、merge preview facade remains read-only delegation、retention facade merge subtree contains no prune behavior 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/retention/{mod,merge/{mod,commit,preview}}.rs` 并通过；旧 `facade/retention/merge.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade mutation commit-preview variant subowner split | runtime_session_archive_facade_mutation_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/mutation/{rename,metadata,touch}.rs` 已硬切换为 folder-backed public archive mutation facade variant 子树：`rename/mod.rs`、`metadata/mod.rs` 与 `touch/mod.rs` 仅保留结构入口，三个 `commit.rs` 承接 mutating facade methods，三个 `preview.rs` 承接 read-only preview facade methods。所有 child 仍只委托既有 `session/slot_mutation/` behavior owner，`remove_slot` commit facade 仍归 `facade/store/`，`preview_remove_slot` 仍归 `facade/mutation/remove.rs`；所有 `RuntimeSessionArchive::rename_slot*`、`update_slot_metadata*`、`touch_slot*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/mutation/mod.rs` 4 行、`facade/mutation/remove.rs` 10 行、`facade/mutation/rename/mod.rs` 2 行、`facade/mutation/rename/commit.rs` 11 行、`facade/mutation/rename/preview.rs` 11 行、`facade/mutation/metadata/mod.rs` 2 行、`facade/mutation/metadata/commit.rs` 11 行、`facade/mutation/metadata/preview.rs` 11 行、`facade/mutation/touch/mod.rs` 2 行、`facade/mutation/touch/commit.rs` 11 行、`facade/mutation/touch/preview.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 rename commit facade delegates only to slot_mutation rename owner、metadata preview remains read-only delegation、touch commit/preview signatures remain unchanged 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/mutation/{mod,remove,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview}}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `facade/mutation/{rename,metadata,touch}.rs` files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive facade mutation subowner split | runtime_session_archive_facade_mutation_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/mutation.rs` 已硬切换为 folder-backed `session/facade/mutation/` facade 子树：`mod.rs` 仅保留结构入口，`rename/` 子树承接 rename/preview rename facade methods，`metadata/` 子树承接 metadata update/preview facade methods，`touch/` 子树承接 updated timestamp touch/preview facade methods，`remove/{mod,commit,preview}.rs` 承接 remove preview facade method。所有 child 仍只委托既有 `slot_mutation/` behavior owner，`remove_slot` commit facade 仍归 `facade/store.rs`，所有 `RuntimeSessionArchive` mutation inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/mutation/mod.rs` 4 行、`facade/mutation/remove.rs` 10 行、`facade/mutation/rename/mod.rs` 2 行、`facade/mutation/rename/commit.rs` 11 行、`facade/mutation/rename/preview.rs` 11 行、`facade/mutation/metadata/mod.rs` 2 行、`facade/mutation/metadata/commit.rs` 11 行、`facade/mutation/metadata/preview.rs` 11 行、`facade/mutation/touch/mod.rs` 2 行、`facade/mutation/touch/commit.rs` 11 行、`facade/mutation/touch/preview.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 rename facade delegates only to slot_mutation rename、metadata/touch facade signatures remain unchanged、remove preview stays read-only 三个锚点。验证：`rustfmt --edition 2021 --check` 原覆盖 `session/facade/mutation/{mod,rename,metadata,touch,remove}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/facade/mutation/{mod,remove,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview}}.rs` 并通过；同一轮 `cargo check -p zircon_runtime --lib` 已在 query 切片反复超时，mutation 切片未重复等待完整 Cargo check，Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade query subowner split | runtime_session_archive_facade_query_subowner_split_rustfmt_passed_cargo_check_timeout_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/query.rs` 已硬切换为 folder-backed `session/facade/query/` facade 子树：`mod.rs` 仅保留结构入口，`basic.rs` 承接 slot count、empty/contains/lookup、slot slice/id iteration facade methods，`manifest.rs` 承接 manifest facade method，`selection.rs` 承接 latest/oldest/tag selector id 与 typed `select_slot` facade methods，`statistics.rs` 承接 archive statistics facade method。所有 child 仍只委托既有 `session/query/` behavior owner，所有 `RuntimeSessionArchive` archive-level query inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/query/mod.rs` 4 行、`facade/query/basic.rs` 22 行、`facade/query/manifest.rs` 7 行、`facade/query/selection.rs` 28 行、`facade/query/statistics.rs` 9 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 basic query facade delegates only to query basic owner、manifest/statistics facade preserve old method signatures、selection facade resolves through existing query selection owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/query/{mod,basic,manifest,selection,statistics}.rs` 并通过；`cargo metadata --locked --no-deps --format-version 1` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1`、同命令带既有 target-dir、`-vv` 诊断尝试和 `--no-default-features` 变体均未返回 Rust 编译错误但在 30-304 秒窗口内超时，尚未完成 Cargo check 级验证。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade restore subowner split | runtime_session_archive_facade_restore_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/restore.rs` 已硬切换为 folder-backed `session/facade/restore/` facade 子树：`mod.rs` 仅保留结构入口，`apply.rs` 承接 world/level apply facade methods，`restore.rs` 承接 empty-world 与 level restore facade methods，`diff.rs` 承接 world/level diff facade methods。所有 child 仍只委托既有 `session/restore/` behavior owner，所有 `RuntimeSessionArchive::apply_*`、`restore_*`、`diff_*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/restore/mod.rs` 3 行、`facade/restore/apply.rs` 34 行、`facade/restore/restore.rs` 31 行、`facade/restore/diff.rs` 33 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 apply facade delegates only to restore apply owner、restore facade delegates only to restore behavior owner、diff facade delegates only to restore diff owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/restore/{mod,apply,restore,diff}.rs`；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade capture subowner split | runtime_session_archive_facade_capture_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/capture.rs` 已硬切换为 folder-backed `session/facade/capture/` facade 子树：`mod.rs` 仅保留结构入口，`world/mod.rs` 与 `level/mod.rs` 保持结构入口，`world/ordinary/` 承接 world capture commit/preview facade variant 子树，`world/retention/` 承接 world capture-retention facade variant 子树，`level/ordinary/` 承接 level capture commit/preview facade variant 子树，`level/retention/` 承接 level capture-retention facade variant 子树。所有 child 仍只委托既有 `slot_capture` 与 `capture_retention` behavior owners，所有 `RuntimeSessionArchive::capture_*slot*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/capture/mod.rs` 2 行、`facade/capture/world/mod.rs` 2 行、`facade/capture/world/ordinary/mod.rs` 2 行、`facade/capture/world/ordinary/commit.rs` 12 行、`facade/capture/world/ordinary/preview.rs` 13 行、`facade/capture/world/retention/mod.rs` 2 行、`facade/capture/world/retention/global/mod.rs` 2 行、`facade/capture/world/retention/global/commit.rs` 13 行、`facade/capture/world/retention/global/preview.rs` 13 行、`facade/capture/world/retention/tag/mod.rs` 2 行、`facade/capture/world/retention/tag/commit.rs` 16 行、`facade/capture/world/retention/tag/preview.rs` 16 行、`facade/capture/level/mod.rs` 2 行、`facade/capture/level/ordinary/mod.rs` 2 行、`facade/capture/level/ordinary/commit.rs` 11 行、`facade/capture/level/ordinary/preview.rs` 11 行、`facade/capture/level/retention/mod.rs` 2 行、`facade/capture/level/retention/global/mod.rs` 2 行、`facade/capture/level/retention/global/commit.rs` 12 行、`facade/capture/level/retention/global/preview.rs` 12 行、`facade/capture/level/retention/tag/mod.rs` 2 行、`facade/capture/level/retention/tag/commit.rs` 13 行、`facade/capture/level/retention/tag/preview.rs` 13 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world ordinary capture facade delegates only to slot_capture、world retention facade delegates only to capture_retention、level retention facade preserves tag/global split 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/capture/{mod,world/{mod,ordinary/{mod,commit,preview},retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}},level/{mod,ordinary/{mod,commit,preview},retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}}}.rs`；后续 ordinary/retention variant split 已追加 scoped rustfmt/hygiene 验证；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer import subowner split | runtime_session_archive_facade_transfer_import_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer/import.rs` 已硬切换为 folder-backed `session/facade/transfer/import/` facade 子树：`mod.rs` 仅保留结构入口，`named.rs` 承接 named loaded archive import facade methods，`selected.rs` 承接 selected loaded archive import facade methods。所有 child 仍只委托既有 `slot_import` behavior owner，所有 `RuntimeSessionArchive::*import*from_archive*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/import/mod.rs` 2 行、`facade/transfer/import/named.rs` 49 行、`facade/transfer/import/selected.rs` 54 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named import facade delegates only to slot_import named paths、selected import facade resolves only through slot_import selected paths、transfer import mod remains structural 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/import/{mod,named,selected}.rs`；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade transfer subowner split | runtime_session_archive_facade_transfer_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade/transfer.rs` 已硬切换为 folder-backed `session/facade/transfer/` facade 子树：`mod.rs` 仅保留结构入口，`copy.rs` 承接 named/selected copy/save-as facade methods，`export.rs` 承接 single-slot archive export facade methods，`import.rs` 承接 named/selected loaded archive import facade methods。所有 child 仍只委托既有 `slot_copy`、`slot_export`、`slot_import` behavior owners，所有 `RuntimeSessionArchive::*copy*`、`*single_slot_archive*`、`*import*from_archive*` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/transfer/mod.rs` 3 行、`facade/transfer/copy.rs` 63 行、`facade/transfer/export.rs` 24 行、`facade/transfer/import.rs` 100 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 transfer facade copy methods delegate only to slot_copy、single-slot export facade delegates only to slot_export、loaded archive import facade delegates only to slot_import 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/transfer/{mod,copy,export,import}.rs`；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive facade subowner split | runtime_session_archive_facade_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/facade.rs` 已硬切换为 folder-backed `session/facade/` facade 子树：`mod.rs` 仅保留结构入口，`construction.rs` 承接 constructor/default/serialization facade methods，`capture.rs` 承接 world/level capture 与 capture-retention facade methods，`store.rs` 承接 push/upsert/remove facade methods，`transfer.rs` 承接 copy/export/import facade methods，`mutation.rs` 承接 rename/metadata/touch/remove preview facade methods，`retention.rs` 承接 merge/prune facade methods，`query.rs` 承接 count/manifest/selector/statistics facade methods，`restore.rs` 承接 apply/restore/diff facade methods，`validation.rs` 承接 support validation 与 session-scoped private invariant helpers。所有 `RuntimeSessionArchive` inherent method names 与 public re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `facade/mod.rs` 9 行、`facade/construction.rs` 49 行、`facade/capture.rs` 124 行、`facade/store.rs` 21 行、`facade/transfer.rs` 201 行、`facade/mutation.rs` 58 行、`facade/retention.rs` 49 行、`facade/query.rs` 66 行、`facade/restore.rs` 100 行、`facade/validation.rs` 31 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 facade child method inventory parity、facade mod remains structural、private invariant helpers stay session-scoped 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/facade/{mod,construction,capture,store,transfer,mutation,retention,query,restore,validation}.rs`；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive root/data/facade owner split | runtime_session_archive_root_data_facade_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session.rs` 已硬切换为 folder-backed `session/mod.rs`，并新增 `session/archive.rs` 承接 `RuntimeSessionArchive` 数据结构与 `RUNTIME_SESSION_ARCHIVE_FORMAT_VERSION`。`session/mod.rs` 仅保留子模块挂载与 public re-export，`session/facade.rs` 承接 `RuntimeSessionArchive` facade inherent impl，所有公开 method names 与 `scene::dynamic_scene` / `scene` re-export surface 保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `session/mod.rs` 56 行、`session/archive.rs` 12 行、`session/facade.rs` 671 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 root module file is folder-backed、archive DTO serde shape parity、facade method surface unchanged 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\mod.rs zircon_runtime\src\scene\dynamic_scene\session\archive.rs zircon_runtime\src\scene\dynamic_scene\session\facade.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path merge subowner split | runtime_session_archive_path_merge_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_merge.rs` 已硬切换为 folder-backed `session/path_merge/` path-level merge 子树：`mod.rs` 仅保留 session-scoped helper re-export；`loaded/` 与 `source_path/` were initially flat subowners and the 2026-06-20 follow-up row now splits both into commit/preview children while source-path commit/preview keep source/target distinct path preflight before loading the source archive。`session/path_api/merge.rs` 与所有 `RuntimeSessionArchive::*merge*at_path*` 公开方法名保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded merge commit/preview parity、source-path merge same-path rejection parity、source-path preview no-write parity 三个锚点。验证：初次 `rustfmt --check` 要求 `path_merge/source_path.rs` import 排序，已运行 rustfmt 修正；`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_merge\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_merge\loaded.rs zircon_runtime\src\scene\dynamic_scene\session\path_merge\source_path.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path merge commit-preview variant subowner split | runtime_session_archive_path_merge_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path merge `{loaded,source_path}.rs` leaves 已硬切换为 folder-backed `session/path_merge/{loaded,source_path}/{mod,commit,preview}.rs` 子树：`loaded/mod.rs` 与 `source_path/mod.rs` 仅保留 session-scoped helper re-export，`loaded/commit.rs` 承接 loaded-incoming atomic merge helpers，`loaded/preview.rs` 承接 no-write loaded-incoming merge preview helpers，`source_path/commit.rs` 与 `source_path/preview.rs` 在加载 source archive 前继续执行 source/target distinct path preflight 后委托 loaded owner。所有 `path_merge::*merge*` helper names、`RuntimeSessionArchive::*merge*at_path*` public facade method names、archive format、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_merge/mod.rs` 8 行、`path_merge/loaded/mod.rs` 4 行、`loaded/commit.rs` 14 行、`loaded/preview.rs` 12 行、`path_merge/source_path/mod.rs` 4 行、`source_path/commit.rs` 21 行、`source_path/preview.rs` 21 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded merge commit/preview parity、source-path merge same-path rejection parity、source-path preview no-write parity 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_merge/{mod,loaded/{mod,commit,preview},source_path/{mod,commit,preview}}.rs` 并通过；旧 `path_merge/{loaded,source_path}.rs` flat leaf files absent；cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive path mutation owner split | runtime_session_archive_path_mutation_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_mutation.rs` 已硬切换为 folder-backed `session/path_mutation/` path-level mutation owner 子树；当日 owner split 先把 named/selected rename、metadata update、touch、remove path helpers 从 `session/io.rs`/`session/path_api.rs` 收束到 path mutation behavior owner，后续已在 2026-06-20 下一行细化为 `session/path_mutation/{rename,metadata,touch,remove}/{mod,commit,preview}.rs` commit/preview variant 子 owner。所有方法仍是 `RuntimeSessionArchive` inherent impl，公开 method names 保持不变；commit 路径继续走 validated load -> archive-owned mutation -> rollback-protected atomic save -> updated manifest，preview 路径继续只 load/validate 并返回 `RuntimeSessionSlotMutationPreviewReport`。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path rename writes only target archive、path metadata preview does not write target、selected path remove delegates through selector once 三个锚点。验证：owner split 当时 `rustfmt --edition 2021 --check`、conflict marker、trailing whitespace、scoped `git diff --check` 与 scoped `cargo check -p zircon_runtime --lib --locked` 通过（现有 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path mutation commit-preview variant subowner split | runtime_session_archive_path_mutation_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path mutation `{rename,metadata,touch,remove}.rs` leaves 已硬切换为 folder-backed `session/path_mutation/{rename,metadata,touch,remove}/{mod,commit,preview}.rs` 子树：四个 family 的 `mod.rs` 仅保留结构入口，`commit.rs` 承接 rollback-protected named/selected mutation-at-path helpers，`preview.rs` 承接 no-write persisted archive mutation report helpers。所有 `RuntimeSessionArchive::*_at_path_atomically` 与 `preview_*_from_path` 公开方法名保持不变，selected variants 仍先复用 archive-owned selected mutation 语义，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_mutation/mod.rs` 4 行、`rename/mod.rs` 2 行、`rename/commit.rs` 27 行、`rename/preview.rs` 21 行、`metadata/mod.rs` 2 行、`metadata/commit.rs` 27 行、`metadata/preview.rs` 21 行、`touch/mod.rs` 2 行、`touch/commit.rs` 27 行、`touch/preview.rs` 21 行、`remove/mod.rs` 2 行、`remove/commit.rs` 28 行、`remove/preview.rs` 19 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path rename commit/preview parity、path metadata/touch preview remains read-only、path remove selected delegates once 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove/{mod,commit,preview}}.rs` 并通过；旧 `path_mutation/{rename,metadata,touch,remove}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive slot mutation subowner split | runtime_session_archive_slot_mutation_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_mutation.rs` 已硬切换为 folder-backed `session/slot_mutation/` 子树；当日 subowner split 先把 `mod.rs` 结构入口、`rename.rs`、`metadata.rs`、`touch.rs`、`remove.rs` 与 `report.rs` 分离，后续已在 2026-06-20 下一行细化为 `session/slot_mutation/{rename,metadata,touch,remove}/{mod,commit,preview}.rs` commit/preview variant 子 owner。`session.rs` 的 `mod slot_mutation;` 与所有 `RuntimeSessionArchive::*slot*` mutation 公开方法名保持不变；子 helper 仍限制在 `crate::scene::dynamic_scene::session` 内，不暴露到 runtime public surface。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 rename preview/commit parity、metadata/touch normalization stays identical、remove preview report shares projection 三个锚点。验证：首次 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 暴露 child helper `pub(super)` 不能 re-export 给 `session.rs` 的 visibility 错误，已收敛为 `pub(in crate::scene::dynamic_scene::session)`；variant split 当时 `rustfmt --edition 2021 --check`、conflict marker 与 trailing whitespace scans 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 复跑通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot mutation commit-preview variant subowner split | runtime_session_archive_slot_mutation_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | slot mutation `{rename,metadata,touch,remove}.rs` leaves 已硬切换为 folder-backed `session/slot_mutation/{rename,metadata,touch,remove}/{mod,commit,preview}.rs` 子树：四个 child `mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 mutating archive-level slot mutation helpers，`preview.rs` 承接 no-write mutation preview helpers；`report.rs` 继续承接 shared mutation preview report projection。`rename_slot`、`update_slot_metadata` 与 `touch_slot` 仍先调用同 owner preview helper 再提交 mutation，`remove_slot` 保持原有 `Option<RuntimeSessionSlot>` 直接删除语义；helper names、`RuntimeSessionArchive::*slot*` public method names、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_mutation/mod.rs` 10 行、`rename/mod.rs` 5 行、`rename/commit.rs` 24 行、`rename/preview.rs` 31 行、`metadata/mod.rs` 5 行、`metadata/commit.rs` 19 行、`metadata/preview.rs` 26 行、`touch/mod.rs` 5 行、`touch/commit.rs` 17 行、`touch/preview.rs` 27 行、`remove/mod.rs` 5 行、`remove/commit.rs` 12 行、`remove/preview.rs` 19 行、`report.rs` 14 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 rename commit uses preview destination id and sort、metadata/touch preview remains read-only、remove keeps direct Option return semantics 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/slot_mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove/{mod,commit,preview},report}.rs` 并通过；旧 `slot_mutation/{rename,metadata,touch,remove}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer source-path import owner split | runtime_session_archive_path_transfer_source_path_import_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/import/source_path.rs` 已硬切换为 folder-backed `session/path_transfer/import/source_path/` 子树：`mod.rs` 仅保留 session-scoped import helper re-export，`named.rs` 承接 named source-path single-slot import commit/preview helpers，`selected.rs` 承接 selected source-path single-slot import commit/preview helpers；same-source/target path rejection 仍在 named/selected source-path owner 内加载 source archive 前执行。`session/path_transfer/import/mod.rs`、`session/path_api/transfer/import/source_path/` 和所有 `RuntimeSessionArchive::*archive_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_transfer/import/source_path/mod.rs` 15 行、`path_transfer/import/source_path/named.rs` 94 行、`path_transfer/import/source_path/selected.rs` 95 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named source-path import rejects same source target before load、selected source-path import resolves selector after source load、source-path import preview does not write target/source 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected capture retention commit-preview variant subowner split | runtime_session_archive_selected_capture_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | selected capture retention `{global,tag}.rs` leaves 已硬切换为 folder-backed `session/selected_capture/{world,level}/retention/{global,tag}/{mod,commit,preview}.rs` 子树：四个 `{global,tag}/mod.rs` 仅保留结构入口，`commit.rs` 承接 mutating selected capture-retention 与 metadata-preserving selected capture-retention delegates，`preview.rs` 承接 read-only selected capture-retention preview delegates。所有 `RuntimeSessionArchive::capture_*_selected_slot*retention*` public method names 与 `scene::dynamic_scene` re-export surface 保持不变，selector resolution、metadata-preserving semantics、LevelSystem snapshot path、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `world/retention/global/mod.rs` 2 行、`world/retention/global/commit.rs` 31 行、`world/retention/global/preview.rs` 36 行、`world/retention/tag/mod.rs` 2 行、`world/retention/tag/commit.rs` 40 行、`world/retention/tag/preview.rs` 40 行、`level/retention/global/mod.rs` 2 行、`level/retention/global/commit.rs` 31 行、`level/retention/global/preview.rs` 31 行、`level/retention/tag/mod.rs` 2 行、`level/retention/tag/commit.rs` 34 行、`level/retention/tag/preview.rs` 39 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected world global retention commit resolves selector once、selected world tag retention preview remains read-only、selected level preserving tag retention snapshots through world capture 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/selected_capture/{world,level}/retention/{global,tag}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 selected capture retention flat leaf files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive selected capture retention owner split | runtime_session_archive_selected_capture_retention_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/selected_capture/world/retention.rs` 与 `selected_capture/level/retention.rs` 已硬切换为 folder-backed retention 子树：`world/retention/mod.rs` 与 `level/retention/mod.rs` 仅保留结构入口，`world/retention/global/{mod,commit,preview}.rs` 与 `level/retention/global/{mod,commit,preview}.rs` 承接 global selected capture retention commit/preview delegates，`world/retention/tag/{mod,commit,preview}.rs` 与 `level/retention/tag/{mod,commit,preview}.rs` 承接 tag-scoped selected capture retention commit/preview delegates。`session/selected_capture/world/mod.rs`、`session/selected_capture/level/mod.rs` 和所有 `RuntimeSessionArchive::capture_*_selected_slot*retention*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `selected_capture/world/retention/mod.rs` 2 行、`world/retention/global/mod.rs` 2 行、`world/retention/global/commit.rs` 31 行、`world/retention/global/preview.rs` 36 行、`world/retention/tag/mod.rs` 2 行、`world/retention/tag/commit.rs` 40 行、`world/retention/tag/preview.rs` 40 行、`selected_capture/level/retention/mod.rs` 2 行、`level/retention/global/mod.rs` 2 行、`level/retention/global/commit.rs` 31 行、`level/retention/global/preview.rs` 31 行、`level/retention/tag/mod.rs` 2 行、`level/retention/tag/commit.rs` 34 行、`level/retention/tag/preview.rs` 39 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected world global retention resolves selector once、selected world tag retention delegates tag bucket policy、selected level metadata-preserving retention snapshots through world capture 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\preview.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected capture ordinary commit-preview variant subowner split | runtime_session_archive_selected_capture_ordinary_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-19 | selected capture ordinary delegate leaves 已硬切换为 folder-backed `session/selected_capture/{world,level}/ordinary/{mod,commit,preview}.rs` 子树：`world/ordinary/mod.rs` 与 `level/ordinary/mod.rs` 仅保留结构入口，`commit.rs` 承接 mutating selected capture 与 metadata-preserving selected capture delegates，`preview.rs` 承接 read-only selected capture preview delegates。所有 `RuntimeSessionArchive::capture_*_selected_slot*` public method names 与 `scene::dynamic_scene` re-export surface 保持不变，selector resolution、metadata-preserving semantics、LevelSystem snapshot path、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `selected_capture/world/ordinary/mod.rs` 2 行、`world/ordinary/commit.rs` 24 行、`world/ordinary/preview.rs` 24 行、`selected_capture/level/ordinary/mod.rs` 2 行、`level/ordinary/commit.rs` 23 行、`level/ordinary/preview.rs` 24 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected world ordinary commit resolves selector once、selected world ordinary preview remains read-only、selected level preserving metadata snapshots through world capture 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/selected_capture/{world,level}/ordinary/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 selected capture ordinary flat leaf files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive selected capture world/level owner split | runtime_session_archive_selected_capture_world_level_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/selected_capture/world.rs` 与 `selected_capture/level.rs` 已硬切换为 folder-backed 子树：`world/mod.rs` 与 `level/mod.rs` 仅保留结构入口，`world/ordinary/{mod,commit,preview}.rs` 与 `level/ordinary/{mod,commit,preview}.rs` 承接 selected capture commit/preview 与 metadata-preserving variants，`world/retention/` 与 `level/retention/` 承接 global/tag retention selected capture commit/preview variant 子 owner。`session/selected_capture/mod.rs` 和所有 `RuntimeSessionArchive::capture_*_selected_slot*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `selected_capture/world/mod.rs` 2 行、`world/ordinary/mod.rs` 2 行、`world/ordinary/commit.rs` 24 行、`world/ordinary/preview.rs` 24 行、`world/retention.rs` 135 行、`selected_capture/level/mod.rs` 2 行、`level/ordinary/mod.rs` 2 行、`level/ordinary/commit.rs` 23 行、`level/ordinary/preview.rs` 24 行、`level/retention.rs` 123 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected world capture resolves selector once、selected level preserving metadata snapshots level through world capture、selected capture retention delegates protect selected slot 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\selected_capture\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\ordinary\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\ordinary\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\ordinary\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\ordinary\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\ordinary\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\ordinary\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\preview.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture retention owner split | runtime_session_archive_path_capture_retention_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_capture/world/retention.rs` 与 `path_capture/level/retention.rs` 已硬切换为 folder-backed retention 子树：`world/retention/mod.rs` 与 `level/retention/mod.rs` 仅保留结构入口，`world/retention/global.rs` 与 `level/retention/global.rs` 承接 global retention capture-to-path commit/preview helpers，`world/retention/tag.rs` 与 `level/retention/tag.rs` 承接 tag-scoped retention capture-to-path commit/preview helpers。`session/path_capture/world/mod.rs`、`session/path_capture/level/mod.rs` 和所有 `RuntimeSessionArchive::*with_retention_to_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_capture/world/retention/mod.rs` 2 行、`world/retention/global.rs` 81 行、`world/retention/tag.rs` 90 行、`path_capture/level/retention/mod.rs` 2 行、`level/retention/global.rs` 75 行、`level/retention/tag.rs` 82 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world global retention path capture saves only target archive、world tag retention path capture delegates tag bucket policy、level retention path capture preserves LevelSystem capture semantics 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\global.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\tag.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\global.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\tag.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture world/level owner split | runtime_session_archive_path_capture_world_level_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_capture/world.rs` 与 `path_capture/level.rs` 已硬切换为 folder-backed 子树：`world/mod.rs` 与 `level/mod.rs` 仅保留结构入口，`world/ordinary.rs` 与 `level/ordinary.rs` 承接 named path capture commit/preview helpers，`world/selected.rs` 与 `level/selected.rs` 承接 selected metadata-preserving path capture commit/preview helpers，`world/retention/` 与 `level/retention/` 承接 global/tag retention capture commit/preview helper 子 owner。`session/path_capture/mod.rs` 和所有 `RuntimeSessionArchive::*_to_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_capture/world/mod.rs` 3 行、`world/ordinary.rs` 32 行、`world/selected.rs` 54 行、`world/retention.rs` 176 行、`path_capture/level/mod.rs` 3 行、`level/ordinary.rs` 30 行、`level/selected.rs` 51 行、`level/retention.rs` 163 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 ordinary path capture writes only target archive、selected preserving path capture keeps slot metadata、retention path capture delegates protect captured slot 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_capture\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\ordinary\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\selected\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\global.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\tag.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\ordinary\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\selected\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\global.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\tag.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer import facade subowner split | runtime_session_archive_path_api_transfer_import_facade_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/import/loaded.rs` 与 `path_api/transfer/import/source_path.rs` 已硬切换为 folder-backed facade 子树：`loaded/mod.rs` 与 `source_path/mod.rs` 仅保留结构入口，`loaded/named.rs` 与 `source_path/named.rs` 承接 named public import facade methods，`loaded/selected.rs` 与 `source_path/selected.rs` 承接 selected public import facade methods。所有 child 仍只委托 `session/path_transfer/import/` behavior owner，所有 `RuntimeSessionArchive::*import*at_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_api/transfer/import/loaded/mod.rs` 2 行、`loaded/named.rs` 63 行、`loaded/selected.rs` 64 行、`path_api/transfer/import/source_path/mod.rs` 2 行、`source_path/named.rs` 63 行、`source_path/selected.rs` 64 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named facade delegates only to path_transfer loaded owner、loaded selected facade delegates only to selected behavior owner、source-path selected facade preserves distinct-path delegation 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\selected.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer import facade owner split | runtime_session_archive_path_api_transfer_import_facade_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer/import.rs` 已硬切换为 folder-backed `session/path_api/transfer/import/` facade 子树：`mod.rs` 仅保留结构入口，`loaded/` 承接 loaded-incoming archive import public facade methods，`source_path/` 承接 source-path archive import public facade methods；两个 child 仍只委托 `session/path_transfer/import/` behavior owner，所有 `RuntimeSessionArchive::*import*at_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_api/transfer/import/mod.rs` 2 行、`path_api/transfer/import/loaded/mod.rs` 2 行、`loaded/named.rs` 63 行、`loaded/selected.rs` 64 行、`path_api/transfer/import/source_path/mod.rs` 2 行、`source_path/named.rs` 63 行、`source_path/selected.rs` 64 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded import facade delegates only to path_transfer loaded owner、source-path import facade delegates only to source-path behavior owner、path API import facade contains no direct archive mutation logic 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\selected.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer import owner split | runtime_session_archive_path_transfer_import_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/import.rs` 已硬切换为 folder-backed `session/path_transfer/import/` 子树：`mod.rs` 仅保留 session-scoped import helper re-export，`loaded/` 承接 loaded-incoming archive named/selected single-slot import commit 与 preview 子树，`source_path/` 承接 source-path named/selected single-slot import commit 与 preview helpers，并在加载 source archive 前继续执行 source/target archive distinct-path preflight。`session/path_transfer/mod.rs` 和 `session/path_api/transfer/` facade 继续通过同名 helper 调用该 owner，所有 `RuntimeSessionArchive::*import*at_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_transfer/import/mod.rs` 23 行、`path_transfer/import/loaded/mod.rs` 14 行、`path_transfer/import/loaded/named.rs` 59 行、`path_transfer/import/loaded/selected.rs` 59 行、`path_transfer/import/source_path/mod.rs` 15 行、`path_transfer/import/source_path/named.rs` 94 行、`path_transfer/import/source_path/selected.rs` 95 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded-import preview delegates to in-memory archive preview、source-path import rejects same source target before load/mutate、selected source-path import preview does not write source/target 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\selected.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer loaded import subowner split | runtime_session_archive_path_transfer_loaded_import_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer/import/loaded.rs` 已硬切换为 folder-backed `session/path_transfer/import/loaded/` behavior 子树：`mod.rs` 仅保留 session-scoped helper re-export，`named.rs` 承接 loaded-incoming named single-slot import commit/preview 与 metadata variants，`selected.rs` 承接 loaded-incoming selected single-slot import commit/preview 与 metadata variants。`session/path_transfer/import/source_path/` 继续委托这些 loaded helper，`session/path_api/transfer/import/` facade 公开方法名保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_transfer/import/loaded/mod.rs` 14 行、`path_transfer/import/loaded/named.rs` 59 行、`path_transfer/import/loaded/selected.rs` 59 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded named import preview/commit parity、loaded selected import resolves selector once、source-path import delegates only after distinct-path preflight 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive file I/O subowner split | runtime_session_archive_io_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/io.rs` 已硬切换为 folder-backed `session/io/` primitive owner：`mod.rs` 仅保留 session-scoped helper re-export，`load_save` initially landed as flat direct load/save/preview leaf，`atomic.rs` 承接 rollback-protected atomic save 与 backup restore，`mutation.rs` 承接 validated load -> mutate -> atomic save orchestration，`support.rs` 承接 parent-dir creation 与 temporary archive path generation；后续 row 已把 `load_save` 继续拆为 load/preview/save child owner。所有 `RuntimeSessionArchive` public load/save path methods 仍通过 `path_api/load_save.rs` 委托到 `io`，各 path-level behavior owner 的 `io::*` 调用名保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split row 记录实际 `load_save` 模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 direct save and atomic save share preview preflight、mutation owner writes only after successful report、temporary path support remains process/counter unique 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/io/{mod,load_save,atomic,mutation,support}.rs` 并通过；后续 load-save variant split 已追加覆盖 `session/io/{mod,load_save/{mod,load,preview,save},atomic,mutation,support}.rs` 并通过；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive file I/O load-save variant subowner split | runtime_session_archive_io_load_save_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | primitive I/O `load_save.rs` leaf 已硬切换为 folder-backed `session/io/load_save/{mod,load,preview,save}.rs` 子树：`load_save/mod.rs` 仅保留 session-scoped helper re-export，`load.rs` 承接 `load_from_path` 与 missing-file-tolerant `load_or_empty_from_path`，`preview.rs` 承接 direct save preflight report delegation，`save.rs` 承接 non-atomic save after preview、parent-dir preflight 与 canonical pretty JSON write。所有 `io::load_from_path` / `io::load_or_empty_from_path` / `io::preview_save_to_path` / `io::save_to_path` helper names、atomic save preflight、path API load/save facade、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `io/mod.rs` 11 行、`io/load_save/mod.rs` 6 行、`io/load_save/load.rs` 18 行、`io/load_save/preview.rs` 11 行、`io/load_save/save.rs` 15 行、`io/atomic.rs` 64 行、`io/mutation.rs` 24 行、`io/support.rs` 34 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 load-or-empty handles missing file only、direct save still runs preview before write、atomic save still shares same preview preflight 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/io/{mod,load_save/{mod,load,preview,save},atomic,mutation,support}.rs` 并通过；旧 `io/load_save.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API transfer facade owner split | runtime_session_archive_path_api_transfer_facade_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api/transfer.rs` 已硬切换为 folder-backed `session/path_api/transfer/` facade 子树：`mod.rs` 仅保留结构入口，`copy.rs` 承接 public path copy/save-as facade methods，`import/` 承接 public loaded/source archive import facade methods；两个 child 仍只委托 `session/path_transfer/` behavior owner，所有 `RuntimeSessionArchive::*copy*from_path*` / `*import*at_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_api/transfer/mod.rs` 2 行、`path_api/transfer/copy.rs` 97 行、`path_api/transfer/import/mod.rs` 2 行、`path_api/transfer/import/loaded/mod.rs` 2 行、`loaded/named.rs` 63 行、`loaded/selected.rs` 64 行、`path_api/transfer/import/source_path/mod.rs` 2 行、`source_path/named.rs` 63 行、`source_path/selected.rs` 64 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path transfer facade copy methods delegate only to behavior owner、path transfer facade import methods preserve source-path distinct preflight delegation、path API transfer module contains no direct archive mutation logic 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\copy.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\loaded\selected.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_api\transfer\import\source_path\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path transfer owner split | runtime_session_archive_path_transfer_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_transfer.rs` 已硬切换为 folder-backed `session/path_transfer/` 子树：`mod.rs` 仅保留 session-scoped helper re-export，`copy.rs` 承接 path-level named/selected copy/save-as commit 与 preview helpers，`import/` 承接 loaded-incoming 与 source-path named/selected single-slot import commit 与 preview helpers。`session/path_api/transfer/` facade 继续通过同名 helper 调用该 owner，所有 `RuntimeSessionArchive::*copy*from_path*` / `*import*at_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_transfer/mod.rs` 25 行、`path_transfer/copy.rs` 81 行、`path_transfer/import/mod.rs` 23 行、`path_transfer/import/loaded/mod.rs` 14 行、`path_transfer/import/loaded/named.rs` 59 行、`path_transfer/import/loaded/selected.rs` 59 行、`path_transfer/import/source_path/mod.rs` 15 行、`path_transfer/import/source_path/named.rs` 94 行、`path_transfer/import/source_path/selected.rs` 95 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path copy preview delegates to loaded archive preview、source-path import rejects same source target、selected source-path import preview does not write source/target 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\copy.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\loaded\selected.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\named.rs zircon_runtime\src\scene\dynamic_scene\session\path_transfer\import\source_path\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive retention prune owner split | runtime_session_archive_retention_prune_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/retention/prune.rs` 已硬切换为 folder-backed `session/retention/prune/` 子树：`mod.rs` 保留结构入口与 session-scoped prune helper re-export，`global` 与 `tag` 最初作为 flat leaf 承接 global/tag prune commit-preview entry points，`planning.rs` 承接 shared sorted slot selection、preview projection 与 apply-report helpers；后续 row 已把 `global` / `tag` 继续拆为 `{mod,commit,preview}` child owner。`session/retention/mod.rs`、`session.rs` 和所有 `RuntimeSessionArchive::prune_slots*` 公开方法名保持不变；prune helper 仍限制在 `crate::scene::dynamic_scene::session` 内，不暴露到 runtime public surface。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。该父级切片当前由后续 child split row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global prune preview/commit parity、tag prune keeps non-matching bucket isolation、apply prune report removes exactly previewed ids 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/retention/prune/{mod,global,tag,planning}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/retention/prune/{mod,global/{mod,commit,preview},tag/{mod,commit,preview},planning}.rs` 并通过；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive retention prune commit-preview variant subowner split | runtime_session_archive_retention_prune_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | retention prune `global.rs` / `tag.rs` leaves 已硬切换为 folder-backed `session/retention/prune/{global,tag}/{mod,commit,preview}.rs` 子树：`global/mod.rs` 与 `tag/mod.rs` 仅保留结构入口与 session-scoped helper re-export，`global/commit.rs` 承接 mutating global prune helper，`global/preview.rs` 承接 read-only global prune preview helper，`tag/commit.rs` 承接 mutating tag-scoped prune helper，`tag/preview.rs` 承接 read-only tag-scoped prune preview helper；shared scoped-slot planning、sorted-id projection 与 apply-report helper 仍归 `planning.rs`。所有 `retention::prune_slots*` helper names、`RuntimeSessionArchive::prune_slots*` public method names、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `retention/prune/mod.rs` 8 行、`retention/prune/global/mod.rs` 5 行、`retention/prune/global/commit.rs` 15 行、`retention/prune/global/preview.rs` 11 行、`retention/prune/tag/mod.rs` 5 行、`retention/prune/tag/commit.rs` 23 行、`retention/prune/tag/preview.rs` 19 行、`retention/prune/planning.rs` 125 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global commit still applies previewed report、global preview remains no-mutation、tag commit/preview preserve empty-tag no-op 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/retention/{mod,policy,report}.rs` 与 `session/retention/prune/{mod,planning,global/{mod,commit,preview},tag/{mod,commit,preview}}.rs` 并通过；旧 `retention/prune/{global,tag}.rs` flat files absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive retention owner split | runtime_session_archive_retention_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/retention.rs` 已硬切换为 folder-backed `session/retention/` 子树：`mod.rs` 保留 policy/report public re-export 与 session-scoped prune entry re-export，`policy.rs` 承接 `RuntimeSessionArchiveRetentionPolicy` 与 protected-slot normalization，`report.rs` 承接 `RuntimeSessionArchivePruneReport`，`prune/` 承接 global/tag prune commit 与 preview algorithms 的子 owner wiring；后续 row 已把 `prune/{global,tag}` 继续拆为 commit/preview child owners。`session.rs` 的 `pub use retention::{RuntimeSessionArchivePruneReport, RuntimeSessionArchiveRetentionPolicy}`、`prune_slots*` facade 与 selected/path/capture-retention 调用面保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `retention/mod.rs` 8 行、`retention/policy.rs` 33 行、`retention/report.rs` 13 行、`retention/prune/mod.rs` 8 行、`retention/prune/global/mod.rs` 5 行、`retention/prune/global/commit.rs` 15 行、`retention/prune/global/preview.rs` 11 行、`retention/prune/tag/mod.rs` 5 行、`retention/prune/tag/commit.rs` 23 行、`retention/prune/tag/preview.rs` 19 行、`retention/prune/planning.rs` 125 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 retention policy normalizes protected ids、prune preview keeps tag bucket isolation、commit prune applies preview removal set 三个锚点。验证：首次 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 暴露 `pub(super)` prune re-export visibility 错误，已改为 `pub(in crate::scene::dynamic_scene::session)` session-scoped prune entry；原 scoped rustfmt 与 cargo check 通过，后续 commit-preview variant split 已追加覆盖当前 prune child subtree 的 rustfmt check。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected capture owner split | runtime_session_archive_selected_capture_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/selected_capture.rs` 已硬切换为 folder-backed `session/selected_capture/` 子树：`mod.rs` 仅保留结构入口，`world/` 承接 world selected capture ordinary/retention delegate subtrees，`level/` 承接 level selected capture ordinary/retention delegate subtrees；`session.rs` 的 `mod selected_capture;` 与所有 `RuntimeSessionArchive::capture_*_selected_slot*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `selected_capture/mod.rs` 2 行、`selected_capture/world/mod.rs` 2 行、`selected_capture/world/ordinary/mod.rs` 2 行、`selected_capture/world/ordinary/commit.rs` 24 行、`selected_capture/world/ordinary/preview.rs` 24 行、`selected_capture/world/retention/mod.rs` 2 行、`world/retention/global/mod.rs` 2 行、`world/retention/global/commit.rs` 31 行、`world/retention/global/preview.rs` 36 行、`world/retention/tag/mod.rs` 2 行、`world/retention/tag/commit.rs` 40 行、`world/retention/tag/preview.rs` 40 行、`selected_capture/level/mod.rs` 2 行、`selected_capture/level/ordinary/mod.rs` 2 行、`selected_capture/level/ordinary/commit.rs` 23 行、`selected_capture/level/ordinary/preview.rs` 24 行、`selected_capture/level/retention/mod.rs` 2 行、`level/retention/global/mod.rs` 2 行、`level/retention/global/commit.rs` 31 行、`level/retention/global/preview.rs` 31 行、`level/retention/tag/mod.rs` 2 行、`level/retention/tag/commit.rs` 34 行、`level/retention/tag/preview.rs` 39 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected world capture resolves selector once、selected level preserving metadata snapshots level through world capture、selected capture retention delegates protect selected slot 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\ordinary\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\ordinary\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\ordinary\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\world\retention\tag\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\ordinary\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\ordinary\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\ordinary\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\selected_capture\level\retention\tag\preview.rs` 通过；method uniqueness scan 确认 world/level selected capture 入口只在对应子 owner；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API facade owner split | runtime_session_archive_path_api_facade_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_api.rs` 已硬切换为 folder-backed `session/path_api/` facade 子树：`mod.rs` 仅保留结构入口，`load_save` initially landed as flat load/save facade leaf，`query/` 承接 manifest/query/status facade，`export/` 承接 single-slot export facade，`restore/` 承接 restore/apply/diff facade，`transfer/` 承接 copy/import transfer facade，`merge/` 承接 archive merge facade；后续 row 已把 `load_save` 继续拆为 load/preview/save/atomic child owner。各子模块继续只委托既有 `io`、`path_query`、`path_export`、`path_restore`、`path_transfer`、`path_merge` behavior owners，所有 `RuntimeSessionArchive` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。该父级切片当前由后续 child split rows 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 facade method parity for path query/export/transfer、load-save facade keeps io-only delegation、path API child modules contain no direct mutation logic 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/path_api/{mod,load_save,query,export,restore,transfer,merge}.rs` 并通过；后续 child split 已追加覆盖 folder-backed child subtrees；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path API load-save facade variant subowner split | runtime_session_archive_path_api_load_save_facade_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path API load-save facade `load_save.rs` leaf 已硬切换为 folder-backed `session/path_api/load_save/{mod,load,preview,save,atomic}.rs` 子树：`load_save/mod.rs` 仅保留结构入口，`load.rs` 承接 public `RuntimeSessionArchive::load_from_path` 与 `load_or_empty_from_path` facade methods，`preview.rs` 承接 public `preview_save_to_path` facade method，`save.rs` 承接 public direct `save_to_path` facade method，`atomic.rs` 承接 public `save_to_path_atomically` facade method。所有 child 仍只委托既有 primitive `session/io/` owner，所有 public load/save path method names、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_api/load_save/mod.rs` 4 行、`path_api/load_save/load.rs` 15 行、`path_api/load_save/preview.rs` 14 行、`path_api/load_save/save.rs` 9 行、`path_api/load_save/atomic.rs` 12 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 load facade delegates only to io load owner、preview save facade remains no-write preflight、atomic save facade delegates only to rollback-protected io owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_api/{mod,load_save/{mod,load,preview,save,atomic}}.rs` 并通过；旧 `path_api/load_save.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture owner split | runtime_session_archive_path_capture_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_capture.rs` 已硬切换为 folder-backed `session/path_capture/` 子树：`mod.rs` 仅保留结构入口，`world/` 承接 world archive-path capture ordinary/selected/retention helper subtrees，`level/` 承接 level archive-path capture ordinary/selected/retention helper subtrees；`session.rs` 的 `mod path_capture;` 与所有 `RuntimeSessionArchive::*_to_path*` 公开方法名保持不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `path_capture/mod.rs` 2 行、`path_capture/world/mod.rs` 3 行、`path_capture/world/ordinary/{mod,commit,preview}.rs` 32 行、`path_capture/world/selected/{mod,commit,preview}.rs` 54 行、`path_capture/world/retention/mod.rs` 2 行、`world/retention/global.rs` 81 行、`world/retention/tag.rs` 90 行、`path_capture/level/mod.rs` 3 行、`path_capture/level/ordinary/{mod,commit,preview}.rs` 30 行、`path_capture/level/selected/{mod,commit,preview}.rs` 51 行、`path_capture/level/retention/mod.rs` 2 行、`level/retention/global.rs` 75 行、`level/retention/tag.rs` 82 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world path capture commit writes only target archive、level path capture preview does not write archive、path capture retention protects captured selected slot 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\ordinary\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\selected\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\global.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\world\retention\tag.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\ordinary\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\selected\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\global.rs zircon_runtime\src\scene\dynamic_scene\session\path_capture\level\retention\tag.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path capture ordinary/selected commit-preview variant subowner split | runtime_session_archive_path_capture_ordinary_selected_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path capture ordinary/selected leaves 已硬切换为 folder-backed `session/path_capture/{world,level}/{ordinary,selected}/{mod,commit,preview}.rs` 子树：四个 `{ordinary,selected}/mod.rs` 仅保留结构入口，`commit.rs` 承接 rollback-protected atomic capture-to-path helpers，`preview.rs` 承接 no-write capture preview helpers。所有 `RuntimeSessionArchive::capture_*_slot_to_path_atomically`、`preview_capture_*_slot_to_path`、selected preserving-metadata path capture public method names 与 `scene::dynamic_scene` re-export surface 保持不变，load-or-empty semantics、selector resolution、metadata-preserving semantics、LevelSystem snapshot path、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `world/ordinary/mod.rs` 2 行、`world/ordinary/commit.rs` 23 行、`world/ordinary/preview.rs` 19 行、`world/selected/mod.rs` 2 行、`world/selected/commit.rs` 35 行、`world/selected/preview.rs` 29 行、`level/ordinary/mod.rs` 2 行、`level/ordinary/commit.rs` 21 行、`level/ordinary/preview.rs` 17 行、`level/selected/mod.rs` 2 行、`level/selected/commit.rs` 34 行、`level/selected/preview.rs` 27 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world ordinary path capture commit writes only target archive、world selected path preview remains read-only、level selected preserving metadata delegates through selected capture owner 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_capture/{world,level}/{ordinary,selected}/{mod,commit,preview}.rs` 并通过；conflict marker 与 trailing whitespace scans 通过；旧 `path_capture/{world,level}/{ordinary,selected}.rs` flat leaf files absent。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level validation owner split | runtime_session_archive_validation_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/validation.rs` 作为 in-memory archive-level validation/invariant owner，承接 format version validation、canonical slot id validation、duplicate slot detection、slot scene support validation、required slot lookup、mutable slot lookup、canonical sorting 与 metadata normalization helpers；后续 2026-06-20 row 已把该 initial flat owner 拆为 support/lookup/ordering/metadata 子 owner。`session.rs` 保留原 public `ensure_supported` 与 private helper facade 名称，现有 construction/query/restore/retention/slot_* child owners 调用面不变。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。该父级切片当前由后续 child split row 记录实际 validation 模块边界；原始模块边界为 `session.rs` 640 行、`validation.rs` 56 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 duplicate slot validation reports duplicate id、unsupported format version remains rejected、metadata normalization stays construction-owned through validation helper 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\validation.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level validation subowner split | runtime_session_archive_validation_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | archive-level validation `zircon_runtime/src/scene/dynamic_scene/session/validation.rs` 已硬切换为 folder-backed `session/validation/{mod,support,lookup,ordering,metadata}.rs` 子树：`validation/mod.rs` 仅保留 session-scoped helper re-export，`support.rs` 承接 format-version/canonical slot id/duplicate slot/slot scene support validation，`lookup.rs` 承接 required slot lookup 与 private mutable slot lookup helpers，`ordering.rs` 承接 canonical slot sorting，`metadata.rs` 承接 archive slot metadata normalization helper。所有 `validation::*` helper names、`RuntimeSessionArchive::ensure_supported` public method、session-scoped invariant helper facade names 与调用面保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `validation/mod.rs` 9 行、`validation/support.rs` 29 行、`validation/lookup.rs` 22 行、`validation/ordering.rs` 7 行、`validation/metadata.rs` 9 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 support validation reports duplicate id、lookup helpers preserve missing-slot error, ordering/metadata helpers remain session-scoped 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/validation/{mod,support,lookup,ordering,metadata}.rs` 与 `session/facade/validation/{mod,support,invariants}.rs` 并通过；旧 `session/validation.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive metadata subowner split | runtime_session_archive_metadata_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | runtime session metadata `zircon_runtime/src/scene/dynamic_scene/session/metadata.rs` 已硬切换为 folder-backed `session/metadata/{mod,model,builders,level,tags}.rs` 子树：`metadata/mod.rs` 仅保留 public `RuntimeSessionMetadata` re-export，`model.rs` 承接 serde DTO declaration，`builders.rs` 承接 `with_display_name` / `with_updated_at_unix_millis` / `with_tag` / `normalized` / `normalize` builder 与 normalization methods，`level.rs` 承接 `LevelMetadata` conversion，`tags.rs` 承接 trim/drop-empty/sort/dedup tag cleanup helper。所有 public type 与 inherent method names 保持不变；archive 格式、metadata tag normalization 行为、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `metadata/mod.rs` 6 行、`metadata/model.rs` 17 行、`metadata/builders.rs` 29 行、`metadata/level.rs` 22 行、`metadata/tags.rs` 8 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 metadata builder methods preserve normalized tags、LevelMetadata conversion stays DTO-only、tags helper remains archive-local 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/metadata/{mod,model,builders,level,tags}.rs` 与 `session/mod.rs` 并通过；旧 `session/metadata.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level slot store owner split | runtime_session_archive_slot_store_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/slot_store.rs` 作为 in-memory archive-level slot store owner，承接 `RuntimeSessionArchive::push_slot` 与 `upsert_slot` 的 canonical slot id validation、metadata normalization、scene support validation、duplicate rejection、replacement 与 canonical sorting；后续 2026-06-20 row 已把该 initial flat owner 拆为 push/upsert 子 owner。`session.rs` 保留原公开方法名并改为 facade delegation，slot-specific mutation/copy/import/capture owners 继续通过这些受控 insertion/update entry 写入 archive。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。该父级切片当前由后续 child split row 记录实际 slot_store 模块边界；原始模块边界为 `session.rs` 663 行、`slot_store.rs` 37 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 push rejects duplicate destination、upsert replaces and keeps sorted order、slot store normalizes metadata tags 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\slot_store.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level slot store subowner split | runtime_session_archive_slot_store_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | archive-level slot store `zircon_runtime/src/scene/dynamic_scene/session/slot_store.rs` 已硬切换为 folder-backed `session/slot_store/{mod,push,upsert}.rs` 子树：`slot_store/mod.rs` 仅保留 session-scoped helper re-export，`push.rs` 承接 duplicate-reject `push_slot` 写入，`upsert.rs` 承接 replace-or-insert `upsert_slot` 写入。两个 child 继续执行 canonical slot id validation、metadata normalization、scene support validation 与 canonical sorting，所有 `slot_store::*` helper names、`RuntimeSessionArchive::push_slot/upsert_slot` public method names 与 facade store 调用面保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_store/mod.rs` 5 行、`slot_store/push.rs` 19 行、`slot_store/upsert.rs` 22 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 push duplicate rejection parity、upsert replace/insert parity、slot store sorting/metadata normalization parity 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/slot_store/{mod,push,upsert}.rs` 与 `session/facade/store/{mod,push,upsert,remove}.rs` 并通过；旧 `session/slot_store.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level construction/serialization owner split | runtime_session_archive_construction_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/construction.rs` 作为 in-memory archive-level construction/serialization owner，承接 `empty`、`from_slots`、`from_world`、`from_world_with_metadata`、`from_level`、`from_versioned_json`、`to_versioned_json_pretty` 的构造与 versioned JSON 入口逻辑；`session.rs` 保留原 `RuntimeSessionArchive` 公开构造/JSON 方法名并改为 facade delegation，archive validation 与 private slot ordering/metadata normalization helpers 继续由 `session.rs` 作为 core invariant owner 暴露给子模块。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 663 行、`construction.rs` 60 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 constructors preserve metadata normalization、from-json validates and sorts slot ids、pretty-json rejects unsupported archive version 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\construction.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive construction/serialization subowner split | runtime_session_archive_construction_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/construction.rs` 已硬切换为 folder-backed `session/construction/` archive construction 子树：`mod.rs` 仅保留 session-scoped helper re-export，`archive.rs` 承接 `empty` 与 `from_slots` archive creation/invariant normalization，`capture.rs` 承接 world/level constructor delegates，`serialization.rs` 承接 `from_versioned_json` 与 `to_versioned_json_pretty` validation/serialization。`session.rs` 的 `mod construction;` 与所有 `RuntimeSessionArchive::empty/from_slots/from_world/from_world_with_metadata/from_level/from_versioned_json/to_versioned_json_pretty` 公开方法名保持不变，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `construction/mod.rs` 10 行、`construction/archive.rs` 24 行、`construction/capture.rs` 30 行、`construction/serialization.rs` 19 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 archive constructor invariant parity、world/level constructor delegate parity、versioned JSON load/save validation parity 三个锚点。验证：初次 `rustfmt --check` 要求 `construction/capture.rs` import 排序，已运行 rustfmt 修正；`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\construction\mod.rs zircon_runtime\src\scene\dynamic_scene\session\construction\archive.rs zircon_runtime\src\scene\dynamic_scene\session\construction\capture.rs zircon_runtime\src\scene\dynamic_scene\session\construction\serialization.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level merge owner split | runtime_session_archive_merge_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/merge.rs` 从 policy/report 与 preview owner 扩展为 in-memory archive-level merge owner，承接 `RuntimeSessionArchive::merge_archive` commit logic，并继续持有 reject/keep/replace duplicate-slot policy 的 dry-run report rules；`session.rs` 保留原 `RuntimeSessionArchive::merge_archive` 与 `preview_merge_archive` 公开方法名并改为 facade delegation，path-level merge 继续由 `session/path_merge.rs` 持有。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 704 行、`merge.rs` 88 行、`path_merge.rs` 56 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 merge commit matches preview report、reject-conflicts remains atomic、replace-existing preserves sorted slot order 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\merge.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level merge subowner split | runtime_session_archive_merge_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/merge.rs` 已硬切换为 folder-backed `session/merge/` archive-level merge 子树：`mod.rs` 仅保留 public policy/report re-export 与 session-scoped algorithm re-export，`policy.rs` 承接 `RuntimeSessionArchiveMergePolicy`，`report.rs` 承接 `RuntimeSessionArchiveMergeReport` 与 `is_empty` helper，`algorithm` initially landed as flat preview-before-commit behavior leaf；后续 row 已把 `algorithm` 继续拆为 commit/preview child owner。`session.rs` 的 `mod merge;`、public `pub use merge::{RuntimeSessionArchiveMergePolicy, RuntimeSessionArchiveMergeReport};` 和所有 `RuntimeSessionArchive::merge_archive` / `preview_merge_archive` 公开方法名保持不变，path-level merge 继续由 `session/path_merge.rs` 持有；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split row 记录实际 algorithm 模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 merge policy default parity、merge report is_empty parity、algorithm preview/commit parity 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/merge/{mod,policy,report,algorithm}.rs` 并通过；后续 algorithm commit-preview variant split 已追加覆盖 `session/merge/{mod,policy,report,algorithm/{mod,commit,preview}}.rs` 并通过；Cargo check 级验证 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level merge algorithm commit-preview variant subowner split | runtime_session_archive_merge_algorithm_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | archive-level merge `algorithm.rs` leaf 已硬切换为 folder-backed `session/merge/algorithm/{mod,commit,preview}.rs` 子树：`algorithm/mod.rs` 仅保留 session-scoped helper re-export，`algorithm/preview.rs` 承接 archive support validation、duplicate rejection preflight 与 inserted/replaced/skipped dry-run report planning，`algorithm/commit.rs` 承接 preview-before-mutate commit application 并继续通过 `push_slot` / `upsert_slot` 保持 archive invariant enforcement。所有 `merge::merge_archive` / `merge::preview_merge_archive` session-scoped helper names、`RuntimeSessionArchive::merge_archive` / `preview_merge_archive` public facade names、path-level merge 行为、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `merge/mod.rs` 6 行、`merge/policy.rs` 11 行、`merge/report.rs` 13 行、`merge/algorithm/mod.rs` 5 行、`merge/algorithm/commit.rs` 27 行、`merge/algorithm/preview.rs` 41 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 preview merge report parity、commit invokes preview before mutation、reject-conflicts duplicate preflight remains atomic 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/merge/{mod,policy,report,algorithm/{mod,commit,preview}}.rs` 并通过；旧 `merge/algorithm.rs` flat file absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level capture owner split | runtime_session_archive_slot_capture_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_capture.rs` 从 preview-only owner 扩展为 in-memory archive-level world/level slot capture owner，承接 ordinary `capture_world_slot` 与 `capture_level_slot` commit logic，并继续持有 capture preview report rules；`session.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为 facade delegation，selected capture 继续由 `session/selected_capture.rs` 持有，path-level capture 继续由 `session/path_capture.rs` 持有。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 721 行、`slot_capture.rs` 60 行、`selected_capture.rs` 302 行、`path_capture.rs` 419 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world capture commit matches preview replacement flag、level capture metadata derives from level metadata、capture commit keeps upsert sorting 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot capture subowner split | runtime_session_archive_slot_capture_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_capture.rs` 已硬切换为 folder-backed `session/slot_capture/` archive-level capture 子树：`mod.rs` 仅保留 session-scoped helper re-export，`world/` 与 `level/` 承接 world/level capture variant owner，`preview.rs` 承接 shared `RuntimeSessionSlotCapturePreview` slot/report projection；后续 2026-06-20 follow-up 将 world/level leaves 继续收束为 commit/preview children。`session.rs` 的 `mod slot_capture;` 与所有 `RuntimeSessionArchive::capture_*slot` / `preview_capture_*slot` 公开方法名保持不变，`capture_retention` 继续复用同一个 preview owner；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split 行记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world capture preview/commit parity、level capture preview/commit parity、capture-retention uses shared preview projection 三个锚点。验证：原父级切片 `rustfmt --edition 2021 --check` 与 scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 已通过（现有 141 warnings only）；当前 commit-preview child split 的 scoped validation 见下一行。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot capture commit-preview variant subowner split | runtime_session_archive_slot_capture_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | `session/slot_capture/{world,level}` leaves 已硬切换为 folder-backed `session/slot_capture/{world,level}/{mod,commit,preview}.rs` 子树：`world/mod.rs` 与 `level/mod.rs` 仅保留 session-scoped helper re-export，`world/commit.rs` 与 `level/commit.rs` 承接 mutating capture-to-upsert helpers，`world/preview.rs` 与 `level/preview.rs` 承接 dry-run slot construction/preflight helpers，shared `slot_capture/preview.rs` 继续持有 `RuntimeSessionSlotCapturePreview` 与 slot/report projection。所有 `RuntimeSessionArchive::capture_*slot` / `preview_capture_*slot` public method names、`capture_retention` internal preview consumption、archive format、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_capture/mod.rs` 6 行、`slot_capture/world/mod.rs` 4 行、`slot_capture/world/commit.rs` 14 行、`slot_capture/world/preview.rs` 17 行、`slot_capture/level/mod.rs` 4 行、`slot_capture/level/commit.rs` 11 行、`slot_capture/level/preview.rs` 11 行、`slot_capture/preview.rs` 22 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world capture commit delegates to preview-generated slot、level capture preview preserves RuntimeSessionSlot::from_level semantics、capture-retention keeps shared preview report projection 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\slot_capture\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\world\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\world\commit.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\world\preview.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\level\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\level\commit.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\level\preview.rs zircon_runtime\src\scene\dynamic_scene\session\slot_capture\preview.rs` 通过；cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive slot import subowner split | runtime_session_archive_slot_import_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_import.rs` 已硬切换为 folder-backed `session/slot_import/` 子树：`mod.rs` 仅保留 session-scoped helper re-export，`named.rs` 承接 named loaded-incoming single-slot import commit/preview 与 metadata variants，`selected.rs` 承接 selected loaded-incoming single-slot import commit/preview 与 metadata variants。`session.rs` 的 `mod slot_import;` 和所有 `RuntimeSessionArchive::import*_slot_from_archive*` 公开方法名保持不变；子 helper 仍限制在 `crate::scene::dynamic_scene::session` 内，不暴露到 runtime public surface。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `slot_import/mod.rs` 11 行、`slot_import/named.rs` 77 行、`slot_import/selected.rs` 58 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named import preview/commit parity、selected import resolves selector once、metadata import normalization stays identical 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\slot_import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_import\named.rs zircon_runtime\src\scene\dynamic_scene\session\slot_import\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level copy/export/import owner split | runtime_session_archive_slot_transfer_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_copy.rs`、`session/slot_export.rs`、`session/slot_import/` 从 preview-only owner 扩展为 in-memory archive-level copy/export/import owners，承接 named 与 selected copy/save-as commit+preview、standalone single-slot archive export commit+preview、loaded-incoming single-slot import commit+preview；`session.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为 facade delegation，path-level copy/export/import 继续由 `session/path_transfer/` 与 `session/path_export/` 持有。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 723 行、`slot_copy.rs` 94 行、`slot_export.rs` 59 行、`slot_import/mod.rs` 11 行、`slot_import/named.rs` 77 行、`slot_import/selected.rs` 58 行、`path_transfer/` 已后续拆为 folder-backed，`path_export/` 当前 `mod.rs` 14 行、`loaded.rs` 43 行、`source_path.rs` 89 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 copy commit matches preview destination metadata、selected single-slot archive delegates to selected id、selected import preserves incoming selector semantics 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\slot_copy.rs zircon_runtime\src\scene\dynamic_scene\session\slot_export.rs zircon_runtime\src\scene\dynamic_scene\session\slot_import\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_import\named.rs zircon_runtime\src\scene\dynamic_scene\session\slot_import\selected.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot copy subowner split | runtime_session_archive_slot_copy_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_copy.rs` 已硬切换为 folder-backed `session/slot_copy/` in-memory copy/save-as 子树：`mod.rs` 仅保留 session-scoped helper re-export，`named.rs` 承接 named copy/save-as commit 与 preview rules，`selected.rs` 承接 selected copy/save-as selector resolution 并委托 named copy path。`session.rs` 的 `mod slot_copy;` 和所有 `RuntimeSessionArchive::copy*_slot*` 公开方法名保持不变，helper visibility 仍限制在 `crate::scene::dynamic_scene::session` 内；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_copy/mod.rs` 9 行、`slot_copy/named.rs` 60 行、`slot_copy/selected.rs` 41 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named copy preview/commit parity、selected copy resolves selector once、metadata copy normalization stays identical 三个锚点。验证：首次 `rustfmt --check` 要求 `selected.rs` import 排序，已修复；`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\slot_copy\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_copy\named.rs zircon_runtime\src\scene\dynamic_scene\session\slot_copy\selected.rs` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive slot export subowner split | runtime_session_archive_slot_export_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_export.rs` 已硬切换为 folder-backed `session/slot_export/` standalone export 子树：`mod.rs` 仅保留 session-scoped helper re-export，`commit.rs` 承接 named/selected standalone archive extraction delegates，`preview.rs` 承接 source slot lookup、normalized export preview report projection 与 target-path preflight。`session.rs` 的 `mod slot_export;` 与所有 `RuntimeSessionArchive::single_slot_archive*` / `preview_*single_slot_archive*` 公开方法名保持不变，path-level `session/path_export/` 继续委托该 preview owner；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_export/mod.rs` 9 行、`slot_export/commit.rs` 16 行、`slot_export/preview.rs` 45 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 export commit/preview source parity、selected export resolves selector once、target-path export preflight stays shared 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\slot_export\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_export\commit.rs zircon_runtime\src\scene\dynamic_scene\session\slot_export\preview.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level slot mutation owner split | runtime_session_archive_slot_mutation_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_mutation.rs` 从 preview-only owner 扩展后已继续硬切换为 folder-backed `session/slot_mutation/` in-memory archive-level slot mutation owner，承接 `remove_slot`、`rename_slot`、`update_slot_metadata`、`touch_slot` commit logic，并继续持有 rename/update/touch/remove preview report rules；`session.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为 facade delegation，path-level mutation 继续由 folder-backed `session/path_mutation/` 持有，selected mutation delegate 继续由 `session/selected_mutation.rs` 持有。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 743 行、`slot_mutation/mod.rs` 10 行、`slot_mutation/rename/{mod,commit,preview}.rs` 53 行、`slot_mutation/metadata/{mod,commit,preview}.rs` 41 行、`slot_mutation/touch/{mod,commit,preview}.rs` 42 行、`slot_mutation/remove/{mod,commit,preview}.rs` 31 行、`slot_mutation/report.rs` 14 行、`path_mutation/mod.rs` 4 行、`path_mutation/rename/{mod,commit,preview}.rs` 46 行、`path_mutation/metadata/{mod,commit,preview}.rs` 46 行、`path_mutation/touch/{mod,commit,preview}.rs` 46 行、`path_mutation/remove/{mod,commit,preview}.rs` 45 行、`selected_mutation.rs` 72 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 commit mutation matches preview report、rename keeps sorted slot order、touch/update metadata keep tag normalization 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\slot_mutation\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_mutation\rename\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\slot_mutation\metadata\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\slot_mutation\touch\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\slot_mutation\remove\{mod,commit,preview}.rs zircon_runtime\src\scene\dynamic_scene\session\slot_mutation\report.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level manifest/query/statistics owner split | runtime_session_archive_query_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/query.rs` 作为 in-memory archive-level manifest/query/statistics owner，承接 slot count、empty/contains/lookup、slot slice/id iteration、manifest projection、latest/oldest selector id queries、typed `select_slot` 与 aggregate `statistics` delegates；`session.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为 facade delegation，`session/manifest/` 继续持有 manifest DTO/API 与 summary ordering，`session/path_query/` 继续持有 path-level load/query/status owner。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 771 行、`query.rs` 110 行、`path_query/` 当前最大 child 55 行、`restore.rs` 93 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 manifest facade keeps sorted slot summaries、statistics facade matches aggregate counts、selector facade keeps manifest ordering 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\query.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level manifest/query/statistics subowner split | runtime_session_archive_query_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/query.rs` 已硬切换为 folder-backed `session/query/` read-only owner 子树：`mod.rs` 仅保留 session-scoped helper re-export，`basic.rs` 承接 slot count、empty/contains/lookup、slot slice/id iteration，`manifest.rs` 承接 sorted manifest projection，初始 flat `selection.rs` 承接 latest/oldest/tag selector id 与 typed `select_slot` delegates，后续 2026-06-20 row 已拆为 global/tag/typed 子 owner，`statistics.rs` 承接 aggregate archive statistics。所有 `RuntimeSessionArchive` archive-level query public methods 仍通过 `session.rs` 委托到 `query`，`session/path_query/` 仍是 path-level load/query/status owner；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片的 `selection` child 边界由后续 query selection variant row 记录；原始模块边界为 `query/mod.rs` 11 行、`query/basic.rs` 31 行、`query/manifest.rs` 19 行、`query/selection.rs` 41 行、`query/statistics.rs` 36 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 basic slot query parity、manifest projection remains sorted、selection/statistics delegates match old owner 三个锚点。验证：首次 `rustfmt --check` 要求 import 排序，已修复；`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\query\mod.rs zircon_runtime\src\scene\dynamic_scene\session\query\basic.rs zircon_runtime\src\scene\dynamic_scene\session\query\manifest.rs zircon_runtime\src\scene\dynamic_scene\session\query\selection.rs zircon_runtime\src\scene\dynamic_scene\session\query\statistics.rs` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level query selection variant subowner split | runtime_session_archive_query_selection_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | archive-level query selection `zircon_runtime/src/scene/dynamic_scene/session/query/selection.rs` 已硬切换为 folder-backed `session/query/selection/{mod,global,tag,typed}.rs` 子树：`selection/mod.rs` 仅保留 session-scoped helper re-export，`selection/global.rs` 承接 global latest/oldest updated slot id helpers，`selection/tag.rs` 承接 tag-scoped latest/oldest updated slot id helpers，`selection/typed.rs` 承接 typed `select_slot` helper。所有 helper names 与 `RuntimeSessionArchive::latest_updated_slot_id*`、`oldest_updated_slot_id*`、`select_slot` public method names 保持不变，manifest projection/timestamp ordering 与 selector resolution 行为不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `query/selection/mod.rs` 7 行、`query/selection/global.rs` 18 行、`query/selection/tag.rs` 20 行、`query/selection/typed.rs` 11 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global query selection keeps manifest latest/oldest parity、tag query selection preserves tag bucket filtering、typed query selection remains selector.resolve delegation 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime/src/scene/dynamic_scene/session/query/mod.rs zircon_runtime/src/scene/dynamic_scene/session/query/selection/mod.rs zircon_runtime/src/scene/dynamic_scene/session/query/selection/global.rs zircon_runtime/src/scene/dynamic_scene/session/query/selection/tag.rs zircon_runtime/src/scene/dynamic_scene/session/query/selection/typed.rs` 通过；旧 `session/query/selection.rs` flat file absent；cargo check 和 focused behavior tests 按本轮实现优先方向 deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive manifest subowner split | runtime_session_archive_manifest_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/manifest.rs` 已硬切换为 folder-backed `session/manifest/` DTO/API 子树：`mod.rs` 仅保留 public manifest/summary re-export，`archive.rs` 承接 `RuntimeSessionArchiveManifest` 及 slot count、lookup、tag/display-name filter、latest/oldest selector API，`summary.rs` 承接 `RuntimeSessionSlotSummary`，`order.rs` 承接 `updated_at_unix_millis` + slot id tie-break ordering。`session.rs` 的 `mod manifest;` 与 public `pub use manifest::{RuntimeSessionArchiveManifest, RuntimeSessionSlotSummary};` 保持不变，archive-level query projection 继续由 `session/query/manifest.rs` 生成 sorted summaries；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `manifest/mod.rs` 6 行、`manifest/archive.rs` 85 行、`manifest/order.rs` 14 行、`manifest/summary.rs` 13 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 manifest DTO serde parity、tag/display-name filter parity、latest/oldest ordering parity 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\manifest\mod.rs zircon_runtime\src\scene\dynamic_scene\session\manifest\archive.rs zircon_runtime\src\scene\dynamic_scene\session\manifest\order.rs zircon_runtime\src\scene\dynamic_scene\session\manifest\summary.rs` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive reports subowner split | runtime_session_archive_reports_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/reports.rs` 已硬切换为 folder-backed `session/reports/` public report DTO 子树：`mod.rs` 仅保留 public report re-export，`archive.rs` 承接 full-archive save preview 与 capture-retention transaction reports，`capture.rs` 承接 slot capture preview report，`diff.rs` 承接 slot diff report，`mutation.rs` 承接 slot mutation preview report，`restore.rs` 承接 level restore report，`transfer.rs` 承接 slot import/export preview reports。`session.rs` 的 `mod reports;` 与 public `pub use reports::{...};` report surface 保持不变，所有调用点继续通过相同类型名编译；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `reports/mod.rs` 15 行、`reports/archive.rs` 21 行、`reports/capture.rs` 10 行、`reports/diff.rs` 9 行、`reports/mutation.rs` 10 行、`reports/restore.rs` 8 行、`reports/transfer.rs` 22 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 report DTO serde/public-surface parity、capture-retention report field parity、slot transfer/mutation report construction parity 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\reports\mod.rs zircon_runtime\src\scene\dynamic_scene\session\reports\archive.rs zircon_runtime\src\scene\dynamic_scene\session\reports\capture.rs zircon_runtime\src\scene\dynamic_scene\session\reports\diff.rs zircon_runtime\src\scene\dynamic_scene\session\reports\mutation.rs zircon_runtime\src\scene\dynamic_scene\session\reports\restore.rs zircon_runtime\src\scene\dynamic_scene\session\reports\transfer.rs` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive typed slot selector subowner split | runtime_session_archive_slot_selector_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot_selector.rs` 已硬切换为 folder-backed `session/slot_selector/` typed selector 子树：`mod.rs` 仅保留 public selector/report re-export，`selector.rs` 承接 `RuntimeSessionSlotSelector` variants，`report.rs` 承接 `RuntimeSessionSlotSelectionReport`，`resolve.rs` 承接 constructor helpers、archive/manifest resolution 与 missing-slot label。`session.rs` 的 `mod slot_selector;` 与 `pub use slot_selector::{RuntimeSessionSlotSelectionReport, RuntimeSessionSlotSelector};` 保持不变，所有 selected capture/copy/import/mutation/retention/restore/apply/diff callers 继续使用相同类型名；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot_selector/mod.rs` 5 行、`slot_selector/selector.rs` 10 行、`slot_selector/report.rs` 9 行、`slot_selector/resolve.rs` 67 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selector submodule serde/public-surface parity、resolve_manifest keeps latest/oldest tie-break parity、missing selector labels stay stable 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\slot_selector\mod.rs zircon_runtime\src\scene\dynamic_scene\session\slot_selector\selector.rs zircon_runtime\src\scene\dynamic_scene\session\slot_selector\report.rs zircon_runtime\src\scene\dynamic_scene\session\slot_selector\resolve.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level restore/apply/diff owner split | runtime_session_archive_restore_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/restore.rs` 作为 in-memory archive-level restore/apply/diff owner，承接 named 与 selected `apply_slot*`、`restore_slot*`、`diff_slot*` 委托；`session.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为 facade delegation，slot-to-world/level 行为继续由 `session/slot/{restore,diff}.rs` 与 summary projection owner 持有，path-level load side 继续由 `session/path_restore.rs` 持有。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 821 行、`restore.rs` 93 行、`path_restore.rs` 88 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected restore/apply/diff delegates match named slot behavior、missing selected slot reports selector error、restore facade remains path-independent 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\restore.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive archive-level restore/apply/diff subowner split | runtime_session_archive_restore_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/restore.rs` 已硬切换为 folder-backed `session/restore/` archive-level restore 子树：`mod.rs` 仅保留 session-scoped helper re-export，`apply.rs`、`restore.rs`、`diff.rs` initially 承接 named/selected archive-level apply/restore/diff delegates；2026-06-20 follow-up row 已继续将这三个 flat leaves 拆为 `{mod,named,selected}.rs` 子树。`session.rs` 的 `mod restore;` 和所有 `RuntimeSessionArchive::apply*` / `restore*` / `diff*` 公开方法名保持不变，helper visibility 仍限制在 `crate::scene::dynamic_scene::session` 内；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 named-selected variant row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 apply delegates match previous owner, restore delegates preserve selector resolution, diff delegates stay read-only 三个锚点。验证：首次 `cargo check` 暴露 child helper visibility 不能经 `restore/mod.rs` re-export 给 `session.rs`，已收敛为 `pub(in crate::scene::dynamic_scene::session)`；原 `rustfmt --edition 2021 --check` 覆盖 `session/restore/{mod,apply,restore,diff}.rs` 并通过；后续 named-selected variant split 已追加 scoped static validation。Cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive archive-level restore/apply/diff named-selected variant subowner split | runtime_session_archive_restore_named_selected_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | archive-level restore behavior `{apply,restore,diff}.rs` leaves 已硬切换为 folder-backed `session/restore/{apply,restore,diff}/{mod,named,selected}.rs` 子树：三个 `mod.rs` 仅保留 session-scoped helper re-export，`apply/named.rs` 承接 explicit slot-id world/level apply delegates，`apply/selected.rs` 承接 selector-based world/level apply delegates，`restore/named.rs` 承接 explicit slot-id empty-world 与 level restore delegates，`restore/selected.rs` 承接 selector-based restore delegates，`diff/named.rs` 承接 explicit slot-id world/level diff delegates，`diff/selected.rs` 承接 selector-based diff delegates。Selected helpers 仍先 `select_slot` 再委托 named helper，保持 explicit slot-id 行为唯一 owner；public facade method names、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `restore/mod.rs` 15 行、`restore/apply/mod.rs` 5 行、`restore/apply/named.rs` 19 行、`restore/apply/selected.rs` 22 行、`restore/restore/mod.rs` 5 行、`restore/restore/named.rs` 20 行、`restore/restore/selected.rs` 24 行、`restore/diff/mod.rs` 5 行、`restore/diff/named.rs` 21 行、`restore/diff/selected.rs` 25 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected archive apply delegates through named apply、selected archive restore preserves selector resolution report path、selected archive diff remains no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/restore/{mod,apply/{mod,named,selected},restore/{mod,named,selected},diff/{mod,named,selected}}.rs` 并通过；旧 `restore/{apply,restore,diff}.rs` flat leaf files absent；conflict marker、trailing whitespace 与 stale active path scans 通过。Cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive slot subowner split | runtime_session_archive_slot_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/slot.rs` 已硬切换为 folder-backed `session/slot/` data/behavior 子树：`mod.rs` 保留 serialized `RuntimeSessionSlot` 数据形态，`capture.rs` 承接 `from_world`、`from_world_with_metadata` 与 `from_level` constructors，`restore.rs` 承接 `apply_to_world`、`restore_to_empty_world`、`restore_into_level` 与 `apply_to_level`，`diff.rs` 承接 `diff_world` / `diff_level`，`summary.rs` 承接 manifest summary projection。`session.rs` 的 `mod slot;` 与 public `pub use slot::RuntimeSessionSlot` 保持不变，所有 archive/path/selected owners 调用的 `RuntimeSessionSlot::*` 方法名保持不变；serde 字段、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `slot/mod.rs` 17 行、`slot/capture.rs` 37 行、`slot/restore.rs` 43 行、`slot/diff.rs` 29 行、`slot/summary.rs` 14 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 slot capture constructors preserve metadata normalization、slot restore/apply behavior parity、slot diff and summary projection parity 三个锚点。验证：首次 `rustfmt --check` 要求 import 排序且首次 `cargo check` 暴露 `slot/mod.rs` unused summary import，已修复；最终 `rustfmt --edition 2021 --check ...slot files` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path manifest/query/status owner split | runtime_session_archive_path_query_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/path_query.rs` 作为 path-level manifest/query/status owner，承接 `load_manifest_from_path`、slot summary/id/filter queries、latest/oldest selector id queries、`select_slot_from_path`、`statistics_from_path` 与 non-throwing `inspect_path`。`session/path_api.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为委托 `path_query`；`session/io.rs` 删除 manifest/query/status helpers 与 path status/statistics/selection/summary imports，当前回到 primitive load/save/atomic-write 与 path mutation primitive owner。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 926 行、`io.rs` 162 行、`path_api.rs` 675 行、`path_query/mod.rs` 16 行、`path_query/manifest.rs` 55 行、`path_query/selection.rs` 39 行、`path_query/statistics.rs` 11 行、`path_query/status.rs` 24 行、`path_merge.rs` 56 行、`path_export/mod.rs` 14 行、`path_export/loaded.rs` 43 行、`path_export/source_path.rs` 89 行、`path_restore.rs` 102 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path query manifest validation matches load path、inspect path separates missing/invalid/available、path selector query uses normalized manifest order 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\io.rs zircon_runtime\src\scene\dynamic_scene\session\path_api.rs zircon_runtime\src\scene\dynamic_scene\session\path_query.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path manifest/query/status subowner split | runtime_session_archive_path_query_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_query.rs` 已硬切换为 folder-backed `session/path_query/` read-only path-query 子树：`mod.rs` 仅保留 session-scoped helper re-export，`manifest.rs` 承接 `load_manifest_from_path`、slot summary/id/filter queries，`selection.rs` 承接 latest/oldest/tagged selector id queries 与 `select_slot_from_path`，`statistics.rs` 承接 `statistics_from_path`，`status.rs` 承接 non-throwing `inspect_path`。`session/path_api/query.rs` 的 public `RuntimeSessionArchive::*_from_path` / `inspect_path` facade method names 保持不变，所有 helper visibility 仍限制在 `crate::scene::dynamic_scene::session` 内；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_query/mod.rs` 16 行、`path_query/manifest.rs` 55 行、`path_query/selection.rs` 39 行、`path_query/statistics.rs` 11 行、`path_query/status.rs` 24 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path manifest helpers keep manifest projection parity、path selection helpers use same validated load、path status inspection keeps missing/invalid/available tri-state 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_query\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_query\manifest.rs zircon_runtime\src\scene\dynamic_scene\session\path_query\selection.rs zircon_runtime\src\scene\dynamic_scene\session\path_query\statistics.rs zircon_runtime\src\scene\dynamic_scene\session\path_query\status.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path merge owner split | runtime_session_archive_path_merge_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/path_merge.rs` 作为 path-level archive merge owner，承接 loaded-incoming 与 source-path merge commit/preview：`merge_archive_at_path_atomically`、`preview_merge_archive_at_path`、`merge_archive_from_path_at_path_atomically`、`preview_merge_archive_from_path_at_path`。`session/path_api.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为委托 `path_merge`；`session/io.rs` 删除 merge helper 与 `RuntimeSessionArchiveMergePolicy`/`RuntimeSessionArchiveMergeReport` imports，继续只负责 archive file load/save、manifest/query 与 path mutation primitives。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 925 行、`io.rs` 269 行、`path_api.rs` 675 行、`path_merge.rs` 56 行、`path_export/mod.rs` 14 行、`path_export/loaded.rs` 43 行、`path_export/source_path.rs` 89 行、`path_restore.rs` 102 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 source-path merge rejects same archive、merge preview does not write target、loaded-incoming merge uses identical policy report 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\io.rs zircon_runtime\src\scene\dynamic_scene\session\path_api.rs zircon_runtime\src\scene\dynamic_scene\session\path_merge.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path single-slot export owner split | runtime_session_archive_path_export_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 path-level single-slot archive export owner，并由当前 folder-backed `zircon_runtime/src/scene/dynamic_scene/session/path_export/` 承接 named/selected source-path export、loaded-archive save-to-path、preview variants 与 standalone source archive projection：`single_slot_archive_from_path`、`selected_single_slot_archive_from_path`、`save_*single_slot_archive*_to_path_atomically`、`save_*single_slot_archive*_from_path_atomically`、`preview_save_*single_slot_archive*_to_path`、`preview_save_*single_slot_archive*_from_path`。`session/path_api.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为委托 `path_export`；`session/io.rs` 删除单槽导出 helper 与 `slot_export`/export-preview report imports，继续只负责 archive file load/save、manifest/query、source/target merge preflight 与 path mutation primitives。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 924 行、`io.rs` 320 行、`path_api.rs` 675 行、`path_export/mod.rs` 14 行、`path_export/loaded.rs` 43 行、`path_export/source_path.rs` 89 行、`path_restore.rs` 102 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 source-path selected export rejects same target、loaded-archive export preview matches commit source slot、path export preview does not write archive 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_export\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_export\loaded.rs zircon_runtime\src\scene\dynamic_scene\session\path_export\source_path.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path single-slot export subowner split | runtime_session_archive_path_export_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_export.rs` 已硬切换为 folder-backed `session/path_export/` behavior owner：`mod.rs` 仅保留结构入口和 session-scoped re-export，`loaded.rs` 承接 loaded-archive target-path save/preview helpers，`source_path.rs` 承接 source archive path projection/save/preview helpers 并保留 source/target distinct-path preflight。`session/path_api/export.rs` 公开 facade 方法名不变，仍只委托 `path_export` behavior owner；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_export/mod.rs` 14 行、`path_export/loaded.rs` 43 行、`path_export/source_path.rs` 89 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 loaded/source-path export facade parity、source-path same-target preflight stays in source owner、path export submodules contain no direct facade impl 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_export\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_export\loaded.rs zircon_runtime\src\scene\dynamic_scene\session\path_export\source_path.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path restore/apply/diff owner split | runtime_session_archive_path_restore_owner_split_code_check_passed_tests_deferred | 2026-06-19 | 新增 `zircon_runtime/src/scene/dynamic_scene/session/path_restore.rs` 作为 path-level restore/apply/diff owner，承接 named 与 selected variants：`restore_*_from_path_*`、`apply_*_from_path_*`、`diff_*_from_path_*`。`session/path_api.rs` 保留原 `RuntimeSessionArchive` 公开方法名并改为委托 `path_restore`；`session/io.rs` 删除该组 helper 与 `World`/`LevelSystem`/`EntityRemap`/restore-diff report imports，继续只负责 archive file load/save、manifest/query、source/target preflight 与 path mutation primitives。该切片不改 archive 格式、不改 gameplay save schema、不引入 editor authoring state、不改 plugin/render/UI 行为，公开 API 保持不变。模块边界当前 `session.rs` 923 行、`io.rs` 427 行、`path_api.rs` 672 行、`path_restore.rs` 102 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path restore/apply/diff selected delegation keeps named semantics、path restore missing archive error surface、path diff preview does not mutate target 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session.rs zircon_runtime\src\scene\dynamic_scene\session\io.rs zircon_runtime\src\scene\dynamic_scene\session\path_api.rs zircon_runtime\src\scene\dynamic_scene\session\path_restore.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path restore/apply/diff subowner split | runtime_session_archive_path_restore_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_restore.rs` 已硬切换为 folder-backed `session/path_restore/` path-level restore 子树：`mod.rs` 仅保留 session-scoped helper re-export，`apply.rs`、`restore.rs`、`diff.rs` initially 承接 named/selected path-level apply/restore/diff delegates；2026-06-20 follow-up row 已继续将这三个 flat leaves 拆为 `{mod,named,selected}.rs` 子树。`session.rs` 的 `mod path_restore;`、`session/path_api/restore/` facade 和所有 `RuntimeSessionArchive::*_from_path*` 公开方法名保持不变，helper visibility 仍限制在 `crate::scene::dynamic_scene::session` 内；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 named-selected variant row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 path apply delegates match previous owner、path restore delegates preserve selector resolution、path diff delegates stay read-only 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/path_restore/{mod,apply,restore,diff}.rs` 并通过；后续 named-selected variant split 已追加 scoped static validation。Cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive path restore/apply/diff named-selected variant subowner split | runtime_session_archive_path_restore_named_selected_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path restore behavior `{apply,restore,diff}.rs` leaves 已硬切换为 folder-backed `session/path_restore/{apply,restore,diff}/{mod,named,selected}.rs` 子树：三个 `mod.rs` 仅保留 session-scoped helper re-export，`apply/named.rs` 承接 explicit slot-id world/level apply-from-path delegates，`apply/selected.rs` 承接 selector-based world/level apply-from-path delegates，`restore/named.rs` 承接 explicit slot-id empty-world 与 level restore-from-path delegates，`restore/selected.rs` 承接 selector-based restore-from-path delegates，`diff/named.rs` 承接 explicit slot-id world/level diff-from-path delegates，`diff/selected.rs` 承接 selector-based diff-from-path delegates。所有 helpers 仍先通过 `io::load_from_path` 读取并验证 archive，再委托 archive-level restore/apply/diff owner；public facade method names、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_restore/mod.rs` 16 行、`path_restore/apply/mod.rs` 7 行、`path_restore/apply/named.rs` 21 行、`path_restore/apply/selected.rs` 21 行、`path_restore/restore/mod.rs` 9 行、`path_restore/restore/named.rs` 20 行、`path_restore/restore/selected.rs` 22 行、`path_restore/diff/mod.rs` 7 行、`path_restore/diff/named.rs` 21 行、`path_restore/diff/selected.rs` 23 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 named path apply loads once before archive apply、selected path restore keeps selector resolution in archive owner、selected path diff remains no-write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_restore/{mod,apply/{mod,named,selected},restore/{mod,named,selected},diff/{mod,named,selected}}.rs` 并通过；旧 `path_restore/{apply,restore,diff}.rs` flat leaf files absent；conflict marker、trailing whitespace 与 stale active path scans 通过。Cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 runtime session archive metadata-preserving selected capture | runtime_session_archive_metadata_preserving_selected_capture_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 新增 metadata-preserving selected capture commit/preview helpers：`capture_world_selected_slot_preserving_metadata`、`preview_capture_world_selected_slot_preserving_metadata`、`capture_level_selected_slot_preserving_metadata`、`preview_capture_level_selected_slot_preserving_metadata`，以及 global/tag same-transaction retention variants。`session/selected_capture.rs` 先用 `RuntimeSessionSlotSelector` 解析 exact/latest/oldest/tagged existing slot，再复用 selected manifest summary metadata 替换 scene snapshot；该路径故意保留 `updated_at_unix_millis`，需要 newest-sort refresh 的调用方仍应走 explicit metadata capture 或后续 `touch_selected_slot`。`session/path_capture.rs` 同步新增 path-level metadata-preserving selected capture commit/preview 与 retention/tag-retention variants，继续经 load-or-empty/validated load -> archive-owned capture -> rollback-protected atomic save，preview 不写 archive 文件。该切片不新增 archive 格式、不引入 gameplay save schema、不改变 explicit-metadata selected capture 行为、不加入 editor authoring state。模块边界保持 folder-backed：当前 `session.rs` 922 行、`io.rs` 524 行、`path_api.rs` 672 行，`path_capture.rs` 457 行与 `selected_capture.rs` 327 行仍低于拆分阈值；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 preserving selected world capture keeps display/tags、preserving level capture ignores current level metadata、path preserving capture preview does not write archive 三个锚点。验证：rustfmt touched Rust 与 `rustfmt --edition 2021 --check` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected capture | runtime_session_archive_selected_capture_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 新增 selected capture commit/preview helpers：`capture_world_selected_slot`、`preview_capture_world_selected_slot`、`capture_level_selected_slot`、`preview_capture_level_selected_slot`，以及 same-transaction retention variants：`capture_world_selected_slot_with_retention`、`preview_capture_world_selected_slot_with_retention`、`capture_world_selected_slot_with_tag_retention`、`preview_capture_world_selected_slot_with_tag_retention`、`capture_level_selected_slot_with_retention`、`preview_capture_level_selected_slot_with_retention`、`capture_level_selected_slot_with_tag_retention`、`preview_capture_level_selected_slot_with_tag_retention`。`session/selected_capture.rs` 作为 selected capture delegate owner，先用 `RuntimeSessionSlotSelector` 解析 exact/latest/oldest/tagged existing slot，再复用 named capture/upsert/capture-retention 语义；selector 对空 archive 或 missing bucket 保持 missing-slot error，不创建 fallback slot。`session/path_capture.rs` 新增为 path-level capture owner，接管 named path capture commit/preview，并新增 selected path variants：`capture_world_selected_slot_to_path_atomically`、`preview_capture_world_selected_slot_to_path`、`capture_level_selected_slot_to_path_atomically`、`preview_capture_level_selected_slot_to_path` 及 selected retention path variants；commit 继续经 load-or-empty/validated load -> archive-owned capture -> rollback-protected atomic save -> manifest/report，preview 只 load/resolve/capture preview，不写 archive 文件。该切片不新增 archive 格式、不引入 gameplay save schema、不改变 named capture 行为、不加入 editor authoring state。模块边界同步收束：`session.rs` 822 行、`io.rs` 467 行、`path_api.rs` 602 行，新增 `path_capture.rs` 277 行与 `selected_capture.rs` 144 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected capture replaces latest/tagged slot、selected capture preview does not mutate archive/path、selected capture-retention protects captured selected slot 三个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected retention protection | runtime_session_archive_selected_retention_protection_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 新增 selected retention protection commit/preview helpers：`prune_slots_with_selected_protection`、`preview_prune_slots_with_selected_protection`、`prune_slots_with_tag_and_selected_protection`、`preview_prune_slots_with_tag_and_selected_protection`；`session/selected_retention.rs` 作为 selected retention delegate owner，先用 `RuntimeSessionSlotSelector` 解析 exact/latest/oldest/tagged slot，再把 concrete slot id 加入 normalized retention policy protected set，最后复用 existing global/tag prune semantics。`session/path_retention.rs` 新增为 path-level retention owner，接管 named path retention commit/preview，并新增 selected-protection path variants：`prune_slots_with_selected_protection_at_path_atomically`、`preview_prune_slots_with_selected_protection_from_path`、`prune_slots_with_tag_and_selected_protection_at_path_atomically`、`preview_prune_slots_with_tag_and_selected_protection_from_path`；commit 继续经 validated load -> archive-owned prune -> rollback-protected atomic save -> prune report，preview 只 load/validate 并返回 `RuntimeSessionArchivePruneReport`，不写 archive 文件。底层 `session/retention.rs` 同步收紧 tag-scoped protected set：bucket 外 protected slot 不再计入当前 tag bucket 的 `max_slots` 上限，因此 named protection 与 selected protection 都不会错误挤占其它 bucket 的 retention 预算。该切片不新增 archive 格式、不引入 gameplay save schema、不改变 named retention 行为、不加入 editor authoring state。模块边界同步收束：`session.rs` 920 行、`io.rs` 668 行、`path_api.rs` 797 行，新增 `path_retention.rs` 81 行与 `selected_retention.rs` 53 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected retention protects latest/tagged slot、tag selected protection outside tag bucket is harmless、path selected retention preview does not write archive 三个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected retention subowner split | runtime_session_archive_selected_retention_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/selected_retention.rs` 已硬切换为 folder-backed `session/selected_retention/` selected-retention 子树：`mod.rs` 仅保留结构入口，`policy.rs` 承接 selector -> protected-slot policy composition，`global` 与 `tag` 最初作为 flat leaf 承接 global/tag selected-protection commit-preview delegates；后续 row 已把 `global` / `tag` 继续拆为 `{mod,commit,preview}` child owner。所有 `RuntimeSessionArchive::prune_slots_with_selected_protection*` 与 `prune_slots_with_tag_and_selected_protection*` 公开方法名保持不变，selected slot 仍只解析一次后复用 existing global/tag prune semantics；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split row 记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected retention policy composition parity、global selected retention delegates once、tag selected retention delegates once 三个锚点。验证：原 `rustfmt --edition 2021 --check` 覆盖 `session/selected_retention/{mod,global,tag,policy}.rs` 并通过；后续 commit-preview variant split 已追加覆盖 `session/selected_retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview},policy}.rs` 并通过；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected retention commit-preview variant subowner split | runtime_session_archive_selected_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | selected retention `global.rs` / `tag.rs` leaves 已硬切换为 folder-backed `session/selected_retention/{global,tag}/{mod,commit,preview}.rs` 子树：`global/mod.rs` 与 `tag/mod.rs` 仅保留结构入口，`global/commit.rs` 承接 `prune_slots_with_selected_protection` mutating delegate，`global/preview.rs` 承接 `preview_prune_slots_with_selected_protection` no-write delegate，`tag/commit.rs` 承接 `prune_slots_with_tag_and_selected_protection` mutating delegate，`tag/preview.rs` 承接 `preview_prune_slots_with_tag_and_selected_protection` no-write delegate；`policy.rs` 仍承接 selector -> protected-slot policy composition。所有 public selected retention method names、selector resolution、global/tag prune delegation、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `selected_retention/mod.rs` 3 行、`selected_retention/global/mod.rs` 2 行、`selected_retention/global/commit.rs` 16 行、`selected_retention/global/preview.rs` 16 行、`selected_retention/tag/mod.rs` 2 行、`selected_retention/tag/commit.rs` 17 行、`selected_retention/tag/preview.rs` 17 行、`selected_retention/policy.rs` 13 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global selected retention commit resolves selector once、global selected retention preview remains no-mutation、tag selected retention delegates preserve tag scope 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/selected_retention/{mod,policy,global/{mod,commit,preview},tag/{mod,commit,preview}}.rs` 并通过；旧 `selected_retention/{global,tag}.rs` flat files absent；Cargo check 和 focused behavior tests deferred。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path retention subowner split | runtime_session_archive_path_retention_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/path_retention.rs` 已硬切换为 folder-backed `session/path_retention/` path-level retention 子树：`mod.rs` 仅保留结构入口，`global.rs` 承接 global prune/preview path helpers 与 selected-protection variants，`tag.rs` 承接 tag-scoped prune/preview path helpers 与 selected-protection variants。所有 `RuntimeSessionArchive::*prune*from_path*` / `*_at_path_atomically` 公开方法名保持不变，commit 仍走 validated load -> archive-owned prune -> rollback-protected atomic save，preview 仍只 load/validate 并返回 `RuntimeSessionArchivePruneReport`；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_retention/mod.rs` 2 行、`path_retention/global/mod.rs` 2 行、`path_retention/global/commit.rs` 24 行、`path_retention/global/preview.rs` 20 行、`path_retention/tag/mod.rs` 2 行、`path_retention/tag/commit.rs` 26 行、`path_retention/tag/preview.rs` 23 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global path retention facade parity、tag path retention facade parity、selected-protection path retention delegates once 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\path_retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_retention\global\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_retention\global\commit.rs zircon_runtime\src\scene\dynamic_scene\session\path_retention\global\preview.rs zircon_runtime\src\scene\dynamic_scene\session\path_retention\tag\mod.rs zircon_runtime\src\scene\dynamic_scene\session\path_retention\tag\commit.rs zircon_runtime\src\scene\dynamic_scene\session\path_retention\tag\preview.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive path retention commit-preview variant subowner split | runtime_session_archive_path_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | path retention `{global,tag}.rs` leaves 已硬切换为 folder-backed `session/path_retention/{global,tag}/{mod,commit,preview}.rs` 子树：`global/mod.rs` 与 `tag/mod.rs` 仅保留结构入口，`commit.rs` 承接 rollback-protected prune-at-path helpers，`preview.rs` 承接 no-write persisted archive retention report helpers。所有 `RuntimeSessionArchive::*prune*from_path*` / `*_at_path_atomically` 公开方法名保持不变，selected-protection variants 仍先复用 archive-owned selected retention 语义，archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `path_retention/mod.rs` 2 行、`global/mod.rs` 2 行、`global/commit.rs` 24 行、`global/preview.rs` 20 行、`tag/mod.rs` 2 行、`tag/commit.rs` 26 行、`tag/preview.rs` 23 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 global path retention commit/preview parity、tag path retention commit/preview parity、selected-protection path preview does not write 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/path_retention/{mod,global/{mod,commit,preview},tag/{mod,commit,preview}}.rs` 并通过；旧 `path_retention/{global,tag}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive selected slot mutation | runtime_session_archive_selected_slot_mutation_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 新增 selected in-memory mutation commit/preview helpers：`rename_selected_slot`、`preview_rename_selected_slot`、`update_selected_slot_metadata`、`preview_update_selected_slot_metadata`、`touch_selected_slot`、`preview_touch_selected_slot`、`remove_selected_slot`、`preview_remove_selected_slot`；`session/selected_mutation.rs` 作为 selected delegate owner，先用 `RuntimeSessionSlotSelector` 解析 exact/latest/oldest/tagged source slot，再复用 named slot mutation 的 destination id normalization、duplicate rejection、metadata normalization、timestamp projection、sorted ordering 与 missing-slot errors。`session/path_mutation/` 新增为 path-level mutation owner，接管 named path mutation commit/preview，并新增 selected path mutation commit/preview：`rename_selected_slot_at_path_atomically`、`preview_rename_selected_slot_from_path`、`update_selected_slot_metadata_at_path_atomically`、`preview_update_selected_slot_metadata_from_path`、`touch_selected_slot_at_path_atomically`、`preview_touch_selected_slot_from_path`、`remove_selected_slot_at_path_atomically`、`preview_remove_selected_slot_from_path`；commit 继续经 validated load -> archive-owned mutation -> rollback-protected atomic save -> updated manifest，preview 只 load/validate 并返回 `RuntimeSessionSlotMutationPreviewReport`，不写 archive 文件。该切片不新增 archive 格式、不引入 gameplay save schema、不改变 named-slot mutation 行为、不加入 editor authoring state。模块边界同步收束：`session.rs` 918 行、`io.rs` 701 行、`path_api.rs` 828 行，新增 `path_mutation/mod.rs` 4 行、`path_mutation/rename/{mod,commit,preview}.rs` 46 行、`path_mutation/metadata/{mod,commit,preview}.rs` 46 行、`path_mutation/touch/{mod,commit,preview}.rs` 46 行、`path_mutation/remove/{mod,commit,preview}.rs` 45 行与 `selected_mutation.rs` 80 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected mutation resolves latest/tagged slot、selected remove preview does not mutate archive、selected path mutation writes only target archive 三个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected mutation subowner split | runtime_session_archive_selected_mutation_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/selected_mutation.rs` 已硬切换为 folder-backed `session/selected_mutation/` selected mutation 子树；当日 owner split 先把 selected rename、metadata update、timestamp touch、remove delegates 从 flat owner 收束到 `session/selected_mutation/`，后续已在 2026-06-20 下一行细化为 `session/selected_mutation/{rename,metadata,touch,remove}/{mod,commit,preview}.rs` commit/preview variant 子 owner。所有 `RuntimeSessionArchive::*selected_slot*` mutation 公开方法名保持不变，各 delegate 继续先解析 `RuntimeSessionSlotSelector` 再复用 named mutation 语义；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected rename delegates once、selected metadata/touch delegates once、selected remove preview/commit parity 三个锚点。验证：owner split 当时 `rustfmt --edition 2021 --check`、conflict marker、trailing whitespace、scoped `git diff --check` 与 scoped `cargo check -p zircon_runtime --lib --locked` 通过（现有 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected mutation commit-preview variant subowner split | runtime_session_archive_selected_mutation_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | selected mutation `{rename,metadata,touch,remove}.rs` leaves 已硬切换为 folder-backed `session/selected_mutation/{rename,metadata,touch,remove}/{mod,commit,preview}.rs` 子树：四个 family 的 `mod.rs` 仅保留结构入口，`commit.rs` 承接 selector-resolving mutating delegates，`preview.rs` 承接 selector-resolving no-write mutation reports。所有 `RuntimeSessionArchive::{rename,update,touch,remove}_selected_slot*` 与 `preview_*_selected_slot*` 公开方法名保持不变，selector resolution、named mutation delegation、archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `selected_mutation/mod.rs` 4 行、`rename/mod.rs` 2 行、`rename/commit.rs` 13 行、`rename/preview.rs` 14 行、`metadata/mod.rs` 2 行、`metadata/commit.rs` 14 行、`metadata/preview.rs` 14 行、`touch/mod.rs` 2 行、`touch/commit.rs` 13 行、`touch/preview.rs` 14 行、`remove/mod.rs` 2 行、`remove/commit.rs` 17 行、`remove/preview.rs` 13 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected rename commit/preview resolve exactly once、selected metadata/touch preview remains read-only、selected remove returns removed slot parity 三个锚点。验证：`rustfmt --edition 2021 --check` 覆盖 `session/selected_mutation/{mod,rename/{mod,commit,preview},metadata/{mod,commit,preview},touch/{mod,commit,preview},remove/{mod,commit,preview}}.rs` 并通过；旧 `selected_mutation/{rename,metadata,touch,remove}.rs` flat leaf files absent；conflict marker 与 trailing whitespace scans 通过。Cargo check 未启动；当前 Runtime 05 full scene closeout gate 已失败并记录 33 个 support-first 待诊断失败，本切片只执行 scoped static validation。单元测试与 focused behavior tests 仍 deferred。 |
| M3 | 3.1 runtime session archive selected copy/import transfer | runtime_session_archive_selected_copy_import_transfer_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 新增 selected copy/save-as 与 selected single-slot import helpers：`copy_selected_slot`、`preview_copy_selected_slot`、`copy_selected_slot_with_metadata`、`preview_copy_selected_slot_with_metadata`、`import_selected_slot_from_archive`、`preview_import_selected_slot_from_archive`、`import_selected_slot_from_archive_with_metadata`、`preview_import_selected_slot_from_archive_with_metadata`。`session/path_api.rs` 同步新增 path-level selected copy 与 loaded/source-path selected import commit/preview 入口；`session/path_transfer.rs` 新增为 cross-archive/path transfer owner，接管原本堆在 `session/io.rs` 的 copy/import path helpers，并把 named 与 selected variants 统一委派到 archive-owned preview/commit 语义。实现先用 `RuntimeSessionSlotSelector` 在 source archive 解析 exact/latest/oldest/tagged selector，再复用 named slot copy/import 的 destination id normalization、duplicate rejection、metadata normalization、incoming/source slot count projection、source/target distinct-path preflight 与 rollback-protected atomic save；不新增 archive 格式、不引入 gameplay save schema、不改变 named-slot transfer 行为。模块边界同步收束：`session/io.rs` 从 954 行降到 777 行，新增 `path_transfer.rs` 367 行；`path_api.rs` 891 行、`session.rs` 916 行仍处 near-threshold，后续若继续扩 selected path facade，应优先拆 path facade 子域而非回填 I/O。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected copy resolves latest/tagged slot、selected source-path import rejects same target、selected import preview does not write target/source 三个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive selected single-slot export | runtime_session_archive_selected_single_slot_export_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 新增 `selected_single_slot_archive` / `preview_selected_single_slot_archive`；`session/io.rs` 与 `session/path_api.rs` 新增 loaded-archive 与 source-path selected standalone export helpers：`selected_single_slot_archive_from_path`、`save_selected_single_slot_archive_to_path_atomically`、`save_selected_single_slot_archive_from_path_atomically` 及对应 preview 入口。实现先用 `RuntimeSessionSlotSelector` 解析 exact/latest/oldest/tagged selector，再复用既有 `preview_single_slot_archive_to_path` / `single_slot_archive` / rollback-protected atomic save，因此 selected export 与 named-slot export 共享 source slot validation、metadata normalization、target replacement preflight、source/target distinct-path preflight 和 standalone archive manifest 语义。该切片不新增 archive 格式、不引入 gameplay save schema、不改变 named-slot export；`io.rs` 增至 954 行但新增逻辑仅为薄委派，后续若 path export 面继续增加，应继续拆 `session/path_export/` child owner 而非继续堆 I/O。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selected export resolves latest/tagged slot、preview selected export does not write target、source-path selected export rejects same target 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive typed slot selector | runtime_session_archive_typed_slot_selector_code_check_passed_tests_deferred | 2026-06-19 | `session/slot_selector.rs` 新增 `RuntimeSessionSlotSelector` 与 `RuntimeSessionSlotSelectionReport`，支持 exact slot id、latest/oldest updated、latest/oldest updated with tag 五类 selection rule，并通过 manifest 解析成 concrete `selected_slot_id` 与 summary。`RuntimeSessionArchive` 新增 `select_slot`、selected restore/apply/diff helpers；`session/io.rs` 与 `session/path_api.rs` 新增 path-level `select_slot_from_path`、selected restore/apply/diff helpers；`scene::dynamic_scene` 与 `scene` 根出口已公开 selector/report。该切片只把 save/load selector 规则数据化，不引入 gameplay save schema、不改变 named-slot restore/apply/diff 语义、不把选择逻辑塞进 editor。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 selector resolves latest/oldest deterministically、tagged selector rejects empty/no-match bucket、path selected restore/apply/diff delegates to chosen slot 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive capture-retention transaction | runtime_session_archive_capture_retention_transaction_code_check_passed_tests_deferred | 2026-06-19 | `session/capture_retention.rs` 新增 archive-owned capture+retention transaction owner；`RuntimeSessionArchiveCaptureRetentionReport` 已从 `scene::dynamic_scene` 与 `scene` 根出口公开，并把 capture preview、retention prune report 与 final manifest 合并为一个结果。新增 in-memory 与 path-level world/level 入口：`capture_*_slot_with_retention` / `preview_capture_*_slot_with_retention` 以及 `*_to_path_atomically` 变体；tag-scoped 版本只裁剪指定 normalized tag bucket，不自动给 metadata 加 tag。commit 语义为先 capture/upsert，再把本次 captured slot 隐式加入 protected slot set 后执行 global/tag retention，避免 autosave/quicksave 刚写入的 slot 被同一保留策略清掉；path-level commit 继续复用 rollback-protected `save_to_path_atomically`。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 capture-retention protects captured slot、preview does not mutate archive/path、tag retention prunes only tagged bucket 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；首次 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-capture-retention-0619 --message-format short --color never` 604s 超时且已停止本会话 cargo/rustc，随后复用热身目标目录执行 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive capture-retention subowner split | runtime_session_archive_capture_retention_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/capture_retention.rs` 已硬切换为 folder-backed `session/capture_retention/` transaction 子树：`mod.rs` 仅保留 session-scoped helper re-export，`world/` 与 `level/` 承接 world/level capture-retention variant owner，`apply.rs` 承接 shared capture preview application、captured-slot protection injection 与 global/tag prune orchestration；后续 2026-06-20 follow-up 将 world/level leaves 继续收束为 commit/preview children。所有 `RuntimeSessionArchive::capture_*_slot_with*_retention` public methods 仍通过 `session.rs` 委托到 `capture_retention`，path-level capture retention owner 与 selected capture retention delegate 调用名保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。该父级切片当前由后续 child split 行记录实际模块边界；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 world capture-retention preview/commit parity、level capture-retention preserves captured-slot protection、shared apply owner prunes only after successful upsert 三个锚点。验证：原父级切片 `rustfmt --edition 2021 --check` 与 scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 已通过（现有 141 warnings only）；当前 commit-preview child split 的 scoped validation 见下一行。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive capture-retention commit-preview variant subowner split | runtime_session_archive_capture_retention_commit_preview_variant_subowner_split_rustfmt_passed_cargo_check_deferred_tests_deferred | 2026-06-20 | `session/capture_retention/{world,level}` leaves 已硬切换为 folder-backed `session/capture_retention/{world,level}/{mod,commit,preview}.rs` 子树：`world/mod.rs` 与 `level/mod.rs` 仅保留 session-scoped helper re-export，`world/commit.rs` 与 `level/commit.rs` 承接 mutating capture-retention helpers，`world/preview.rs` 与 `level/preview.rs` 承接 archive-cloned dry-run capture-retention helpers，shared `capture_retention/apply.rs` 继续持有 capture preview application、captured-slot protection injection 与 prune orchestration。所有 `RuntimeSessionArchive::capture_*_slot_with*_retention` public method names、selected/path capture-retention delegate call names、archive format、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `capture_retention/mod.rs` 11 行、`capture_retention/world/mod.rs` 8 行、`capture_retention/world/commit.rs` 27 行、`capture_retention/world/preview.rs` 29 行、`capture_retention/level/mod.rs` 8 行、`capture_retention/level/commit.rs` 25 行、`capture_retention/level/preview.rs` 27 行、`capture_retention/apply.rs` 25 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 commit path mutates original archive only、preview path mutates clone only、tag/global retention keep shared apply semantics 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\capture_retention\mod.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\world\mod.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\world\commit.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\world\preview.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\level\mod.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\level\commit.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\level\preview.rs zircon_runtime\src\scene\dynamic_scene\session\capture_retention\apply.rs` 通过；cargo check 与 focused behavior tests 按当前 implementation-first 指令 deferred。 |
| M3 | 3.1 dynamic runtime session scene asset reload diagnostics | dynamic_runtime_session_scene_asset_reload_diagnostics_code_check_passed_tests_deferred | 2026-06-19 | `dynamic_api/session/scene_asset_reload_diagnostics.rs` 新增 session-owned reload frame diagnostics projector：`RuntimeDynamicSession::tick_scene_asset_reload` 在 `tick_into_level` 返回后，把 `DynamicSceneAssetReloadFrameApplyReport` 写入 `DiagnosticStore` 的 `scene.asset_reload.events_drained`、`scheduled`、`skipped`、skip reason counts、`superseded_pending`、`applied`、`failed`、`stale`、`pending` 与 `receiver_disconnected` 行。该切片只公开可观测计数，不改变 reload queue commit policy、不读取队列内部字段、不把 reload failure 升级成 frame error，也不引入 editor authoring state。`docs/zircon_runtime/dynamic_api/session.md`、`docs/zircon_runtime/core/diagnostics.md`、`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 scene asset reload diagnostics record zero frame、diagnostics count skipped reasons、diagnostics report receiver disconnected 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic runtime session scene asset reload integration | dynamic_runtime_session_scene_asset_reload_integration_code_check_passed_tests_deferred | 2026-06-19 | `dynamic_api/session.rs` 为 project-backed `RuntimeDynamicSession` 增加 optional `DynamicSceneAssetReloadQueue` 与 last frame report：session 创建时通过 `RuntimeProjectConfig::scene_asset_reload_queue` 解析具体 `ProjectAssetManager` / `PROJECT_ASSET_MANAGER_NAME` 并订阅 typed `SceneAsset` events，`tick_frame` 在 `tick_time(...)` 后、`LevelSystem::tick(...)` 前调用 `tick_into_level`，非空 drain/apply/stale/superseded/pending 诊断写入 runtime log 并缓存 report。失败保持为 frame diagnostics，不中断 level tick；队列仍不持有 editor selection/viewport/gizmo/inspector state，也不自行发现或选择其他 live level。`docs/zircon_runtime/scene/dynamic_scene.md`、`docs/zircon_runtime/dynamic_api/session.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 project session builds reload queue、tick_frame drives reload before level tick、reload apply failure does not abort frame 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload pending snapshot | dynamic_scene_asset_reload_pending_snapshot_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 新增 `DynamicSceneAssetReloadPendingTaskSnapshot`、`DynamicSceneAssetReloadPendingReport` 与 `DynamicSceneAssetReloadQueue::pending_report`：runtime host 可在不消费 ready payload、不访问队列内部字段的情况下读取 pending reload task 对应的 typed asset event、`AsyncTaskDescriptor` 和当前 `AsyncTaskStatus`，并按 `AsyncTaskState` / terminal collectable 计数构建进度 UI、日志或诊断。该 API 只做只读快照，不改变 `take_ready_report` 的 stale/latest-revision gate，也不决定 live world/level commit policy；`DynamicSceneAssetReloadPendingTaskSnapshot` / `PendingReport` 已从 `scene::dynamic_scene` 与 `scene` 根出口公开。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 pending_report exposes descriptor and status、pending_report does not consume ready result、pending_report counts states 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload LevelSystem target helpers | dynamic_scene_asset_reload_level_target_helpers_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 新增 `DynamicSceneAssetReloadResult::spawn_into_level`、`DynamicSceneAssetReloadReadyReport::spawn_ready_into_level` 与 `DynamicSceneAssetReloadQueue::tick_into_level`：runtime host 在已经选择目标 `LevelSystem` 后，可通过 `LevelSystem::with_world_mut` 显式应用 ready scene reload payload，并复用现有 `DynamicSceneAssetReloadApplyReport` / `DynamicSceneAssetReloadFrameApplyReport` 诊断。该 API 只减少 runtime loop 侧重复 world-lock boilerplate，不让 reload queue 自行 discover 或选择 live level，也不重置 level runtime state；完整替换语义仍由 session restore 路径的 `replace_world_and_reset_runtime_state` 负责。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 spawn_into_level uses explicit LevelSystem target、spawn_ready_into_level keeps apply diagnostics、tick_into_level reports drain and apply counts 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload frame apply report | dynamic_scene_asset_reload_frame_apply_report_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 新增 `DynamicSceneAssetReloadFrameApplyReport` 与 `DynamicSceneAssetReloadQueue::tick_into`：runtime host 可在单个 frame call 中 drain typed `AssetEvent<SceneAsset>`、收集 ready/stale prepared payload，并把 ready payload 显式应用到调用方传入的目标 `World`，同时保留 drain/apply 两段诊断（events drained、scheduled、skipped、superseded pending、applied、failed、stale、receiver disconnected、final pending count）。该 API 组合既有 `tick` 与 `DynamicSceneAssetReloadReadyReport::spawn_ready_into`，仍不让 reload queue 自行选择 live world/level 或 commit timing；`DynamicSceneAssetReloadFrameApplyReport` 已从 `scene::dynamic_scene` 与 `scene` 根出口公开。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 tick_into drains and applies ready payloads、tick_into preserves stale diagnostics、tick_into surfaces apply failures 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload caller-thread apply report | dynamic_scene_asset_reload_apply_report_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 新增 `DynamicSceneAssetReloadAppliedScene`、`DynamicSceneAssetReloadApplyFailure`、`DynamicSceneAssetReloadApplyReport`、`DynamicSceneAssetReloadResult::spawn_into` 与 `DynamicSceneAssetReloadReadyReport::spawn_ready_into`：runtime host 在已经选择目标 `World` 后，可在 caller thread 显式应用 ready reload payload，并保留 asset event、entity remap、component/entity/resource count、apply failure、stale completion 与 final pending count 诊断。该 API 只消费已完成的 `PreparedDynamicSceneSpawn`，不让 reload queue 自行选择 live world/level，也不改变 latest-revision gate、superseded-pending pruning 或 `tick` 的事件收集语义；`DynamicSceneAssetReloadAppliedScene` / `ApplyFailure` / `ApplyReport` 已从 `scene::dynamic_scene` 与 `scene` 根出口公开。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 apply report records remap counts、prepared reload error becomes apply failure、stale completions are retained when applying ready report 三个锚点。验证：rustfmt touched Rust 通过；第一次 scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 暴露 active graphics camera-stack 切片的 root import drift：`submit_runtime_frame.rs` 仍从 `crate::graphics` 根导入 crate-private `ViewportCameraStackAttachmentPolicy`，已改为 `graphics::types::{ViewportCameraStackAttachmentPolicy, ViewportRenderFrame}` 以匹配当前 root surface；复跑同一 `cargo check` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload tick report | dynamic_scene_asset_reload_tick_report_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 新增 `DynamicSceneAssetReloadTickReport` 与 `DynamicSceneAssetReloadQueue::tick`：runtime host 可在单个 frame call 中先 drain typed `AssetEvent<SceneAsset>`，再收集 ready/stale prepared payload，并从 report 读取 drained/scheduled/skipped/superseded/ready/stale/final pending 计数；该 API 只组合已有 `drain_events` 与 `take_ready_report`，不改变 latest-revision gate、superseded-pending pruning 或 live world commit policy，`PreparedDynamicSceneSpawn::spawn_into` 仍由 caller thread 决定。`DynamicSceneAssetReloadTickReport` 已从 `scene::dynamic_scene` 与 `scene` 根出口公开；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 tick drains then reports ready、tick final pending count、tick preserves stale/superseded diagnostics 三个锚点。验证：rustfmt touched Rust 通过；conflict/trailing whitespace scans 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload superseded-pending pruning | dynamic_scene_asset_reload_superseded_pending_pruning_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 在 latest-revision gate 基础上新增 `DynamicSceneAssetReloadSupersededTask` 与 `DynamicSceneAssetReloadDrainReport.superseded_pending`：每次 `drain_events` 记录新 `AssetEvent<SceneAsset>` revision 后，会从 pending reload queue 中移除同一 `AssetId` 且 revision 更旧的后台准备任务，并把被替代的事件与最新 revision 返回给 runtime host 诊断；`Removed` / `ReloadFailed` 事件同样会抬高 latest revision 并清理旧 pending，避免旧 payload 后续以 stale completion 形式占用队列。`take_ready_report` 仍保留完成时的 stale guard，覆盖已经越过 pending pruning 的并发完成窗口；`take_ready` 继续只返回当前可 apply payload。本切片不改 asset watcher/importer/artifact cache，也不决定 live world commit policy；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 newer modified supersedes older pending、removed event supersedes older pending、ready report keeps stale guard 三个锚点。验证：rustfmt touched Rust 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload latest-revision gate | dynamic_scene_asset_reload_latest_revision_gate_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 为 `DynamicSceneAssetReloadQueue` 增加 per-`AssetId` latest revision 记录：`drain_events` 接收 typed `AssetEvent<SceneAsset>` 时记录最新 revision，早于最新 revision 的 `Added` / `Modified` / `Renamed` 事件进入 `DynamicSceneAssetReloadSkipReason::StaleRevision` 而不再调度后台准备；`take_ready_report` 会在后台 `DynamicSceneSpawnTask` 完成后再次比较 task event revision 与最新 revision，把过期完成项写入 `DynamicSceneAssetReloadStaleResult`，只把当前 revision 的 `DynamicSceneAssetReloadResult` 暴露给 caller-thread apply；`take_ready` 保持兼容 surface，返回 `take_ready_report().into_ready()`。该切片不改 asset watcher/importer/artifact cache，也不决定 live world commit policy；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 older completed task becomes stale、newer completed task remains ready、removed event invalidates older pending task 三个锚点。验证：rustfmt touched Rust 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset reload queue | dynamic_scene_asset_reload_queue_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/asset_reload.rs` 新增 runtime-owned `DynamicSceneAssetReloadQueue`：可从 `ProjectAssetManager::subscribe_asset_events::<SceneAsset>()` 构造，drain typed `AssetEvent<SceneAsset>` 后把 `Added` / `Modified` / `Renamed` 事件调度为 `DynamicSceneSpawnTask::schedule_scene_asset_uri`，并用 `take_ready` 返回 `DynamicSceneAssetReloadResult`。`Removed`、`ReloadFailed`、缺少 locator 的事件不会调度 world mutation，而是进入 `DynamicSceneAssetReloadDrainReport.skipped` 并带 `DynamicSceneAssetReloadSkipReason`；队列只产出 prepared payload， live world/level 更新时机仍由 runtime host 在 caller thread 调用 `PreparedDynamicSceneSpawn::spawn_into` 决定。本切片不改 asset watcher/importer/artifact cache，只消费 facade typed event surface；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 modified scene event schedules preparation、removed/reload_failed skip reasons、take_ready preserves pending incomplete tasks 三个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene asset-backed preparation | dynamic_scene_asset_backed_preparation_code_check_passed_tests_deferred | 2026-06-19 | `scene/dynamic_scene/scene_asset.rs` 新增 runtime-owned `SceneAsset` bridge：`DynamicScene::from_scene_asset` / `from_scene_asset_uri` 复用 `SceneAssetSerializer` 将 imported `SceneAsset` 或项目 scene-asset URI 实例化为 `World`，再通过 `DynamicScene::from_world` 转为版本化动态场景；`PreparedDynamicSceneSpawn::from_scene_asset(_uri)` 与 `DynamicSceneSpawnTask::schedule_scene_asset(_uri)` 让 runtime host 可以把已导入场景资产或项目 URI 提交给 `JobScheduler` 后台准备，并在 ready 后继续由 caller thread 调用 `PreparedDynamicSceneSpawn::spawn_into` 执行 ECS world mutation。该切片不改 importer、artifact cache 或 watcher，后续热更新 commit loop 可消费此 runtime-owned prepared payload。`DynamicSceneError` 新增 `SceneAsset` conversion 错误；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 URI asset preparation uses serializer、loaded SceneAsset preparation validates dynamic scene、schedule_scene_asset_uri reports conversion errors 三个锚点。验证：rustfmt touched Rust 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（两次长编译超时后 target-dir cache warm-up，第三次 4.49s 返回，现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene async spawn preparation | dynamic_scene_async_spawn_preparation_code_check_passed_tests_deferred | 2026-06-19 | `PreparedDynamicSceneSpawn` 与 `DynamicSceneSpawnTask` 已加入 `scene::dynamic_scene` public surface；runtime host 可把已构造 scene、dynamic scene JSON 或 JSON path 提交给 `JobScheduler` 后台任务执行 parse / legacy project migration / `ensure_supported`，通过 `AsyncTaskDescriptor`、polled `AsyncTaskStatus`、`JobHandle`、`take_ready` / `wait_ready` 观察和收集结果；完成后由 `PreparedDynamicSceneSpawn::spawn_into` 在 caller thread 复用现有 `DynamicScene::spawn_into`，保持 ECS world mutation、entity remap、plugin component descriptor install、component/resource writes 都不跨线程发生。`DynamicSceneError` 新增 `Io` 与 unavailable task result 错误；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 schedule_json parses off-thread、schedule_json_from_path reports Io、prepared spawn applies on caller thread 三个锚点。验证：rustfmt touched Rust 通过；初次 dedicated target-dir check 在 120s 超时且未返回结果，随后 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-distinct-path-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene reflected resource ensure hook | dynamic_scene_reflected_resource_ensure_hook_code_check_passed_tests_deferred | 2026-06-19 | `ReflectResource` 新增 optional `ensure` hook，`DynamicScene::apply_resource` 在写入 captured reflected resource fields 前先检查 target world 是否已有资源；缺失时调用 adapter `ensure`，若 hook 创建/初始化后 `contains` 仍为 false，或 adapter 没有 hook，则继续返回 `ReflectError::MissingResource`。这样支持具备默认构造语义的 runtime resources 在 scene/session restore 时被显式创建，同时不让无法安全构造的资源静默生成。现有测试 fixture 的 `ReflectResource` adapters 已补 `ensure: None` 保持旧语义；`docs/zircon_runtime/scene/dynamic_scene.md`、`docs/zircon_runtime/scene/reflect.md` 与 runtime index backlog 已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 ensure hook creates missing resource 与 missing hook still errors 两个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-distinct-path-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 dynamic scene component type descriptor embedding | dynamic_scene_component_type_descriptor_embedding_code_check_passed_tests_deferred | 2026-06-19 | `DynamicScene` 新增 `component_types: Vec<ComponentTypeDescriptor>`，`from_world` 会收集 captured plugin-owned dynamic components 所需的组件 schema 并按 type id 排序写入 scene JSON（空列表跳过序列化）；`spawn_into` 现在先安装缺失的 plugin component descriptors，再执行 entity remap、record insert 和 reflected component/resource apply，若目标 world 已有同 type id 但 schema 不同则返回 `ComponentTypeDescriptorConflict`。`DynamicScene::ensure_supported` 同步拒绝 duplicate/invalid component type descriptors，防止 session archive slot 或直接 scene load 携带不可安装 schema。`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与旧 architecture 文档的 registry/scene 描述已同步；本切片按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 scene roundtrip carries descriptor、spawn installs missing descriptor、conflicting descriptor rejects 三个锚点。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-distinct-path-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive source/target distinct-path preflight | runtime_session_archive_source_target_distinct_preflight_code_check_passed_tests_deferred | 2026-06-19 | `session/target_path.rs` 新增 `reject_same_archive_paths` 共享 source/target archive path preflight，并在原始路径相等之外对两侧已存在路径做 canonicalize 比较；`session/io.rs` 现在让 source-path single-slot export、source-path single-slot import、metadata import、source-path merge 以及对应 preview 入口在读取/写入归档前拒绝 self-import / self-merge。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步 source/target archive path distinct preflight；本切片按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补同路径 source-path import/merge preview 与 commit 错误锚点。验证：rustfmt touched Rust 与 `rustfmt --edition 2021 --check zircon_runtime/src/scene/dynamic_scene/session/io.rs zircon_runtime/src/scene/dynamic_scene/session/target_path.rs` 通过；冲突标记扫描通过；空格/Tab 尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-distinct-path-0619 --message-format short --color never` 通过（现有 142 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive target-path preflight owner | runtime_session_archive_target_path_preflight_code_check_passed_tests_deferred | 2026-06-19 | `session/target_path.rs` 新增共享 save/export target preflight owner，`archive_save.rs` 与 `slot_export.rs` 现在共用 `target_file_will_replace`，full-archive save preview 与 single-slot export preview 都会在 dry-run 阶段拒绝非文件目标和“existing parent path is a file”目标路径；commit 侧 `save_to_path` / `save_to_path_atomically` 继续复用 preview，避免 dry-run 允许、commit `create_dir_all` 才失败的路径语义裂缝。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步 folder-backed `target_path` owner 和 save/export target parent-file preflight；本切片按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 parent-file target preview 行为锚点。验证：rustfmt touched Rust 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-target-path-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive target-path preflight subowner split | runtime_session_archive_target_path_subowner_split_code_check_passed_tests_deferred | 2026-06-19 | `zircon_runtime/src/scene/dynamic_scene/session/target_path.rs` 已硬切换为 folder-backed `session/target_path/` preflight 子树：`mod.rs` 仅保留 session-scoped helper re-export，`source.rs` 承接 `reject_same_archive_paths` 与 canonical source/target path matching，`target.rs` 承接 `target_file_will_replace`、non-file target rejection 与 parent-path validation。`archive_save.rs`、`slot_export/preview.rs`、`path_export/source_path.rs`、`path_transfer/import/source_path/*` 与 `path_merge.rs` 的调用名保持不变；archive 格式、gameplay save schema、editor/plugin/render/UI 行为均不变。模块边界当前 `target_path/mod.rs` 4 行、`target_path/source.rs` 32 行、`target_path/target.rs` 41 行；`docs/zircon_runtime/scene/dynamic_scene.md`、runtime index backlog 与会话记录已同步。按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 source same-path preflight parity、target parent-file preflight parity、canonical existing-path comparison stays unchanged 三个锚点。验证：`rustfmt --edition 2021 --check zircon_runtime\src\scene\dynamic_scene\session\target_path\mod.rs zircon_runtime\src\scene\dynamic_scene\session\target_path\source.rs zircon_runtime\src\scene\dynamic_scene\session\target_path\target.rs` 通过；conflict marker 与 trailing whitespace scans 通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-dynamic-scene-asset-0619 --message-format short --color never` 通过（现有 141 warnings only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive full-save preview helpers | runtime_session_archive_full_save_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchiveSavePreviewReport` 已加入 session public surface，并从 `scene::dynamic_scene` 与 `scene` 根出口公开；`RuntimeSessionArchive::preview_save_to_path` 已加入 full-archive save target dry-run API。新增 `session/archive_save.rs` 统一复用 archive validation、archive statistics projection、target-path capture、non-file target rejection 与 target replacement detection；`save_to_path` 与 `save_to_path_atomically` 现在先调用同一 preview 规则，再执行 direct write 或 rollback-protected atomic write，保证 dry-run 与 commit 目标检查语义一致。preview 不创建 parent directory、不写 target archive、不生成 `.tmp`/`.bak` 文件；missing target 报 `will_replace_target=false`，existing file 报 `true`。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步 folder-backed `archive_save` owner 和 full archive save target preview API；本切片按“首先实现功能，测试可以暂时延后”不新增行为测试，后续可补 missing-target、existing-file、non-file-target 与 no-write/no-temp 行为锚点。验证：rustfmt touched Rust 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive single-slot export preview helpers | runtime_session_archive_single_slot_export_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionSlotExportPreviewReport` 已加入 session public surface，并从 `scene::dynamic_scene` 与 `scene` 根出口公开；`RuntimeSessionArchive::preview_single_slot_archive`、`preview_save_single_slot_archive_to_path` 与 `preview_save_single_slot_archive_from_path` 已加入 single-slot export/share dry-run API。新增 `session/slot_export.rs` 统一复用 archive validation、source slot lookup、metadata normalization、entity/resource count projection、target-path capture、non-file target rejection 与 target replacement detection；`single_slot_archive`、`save_single_slot_archive_to_path_atomically` 与 `save_single_slot_archive_from_path_atomically` 现在先调用同一 preview 规则，再用 report 中的 canonical source id 执行 standalone archive extraction/write，保证 dry-run 与 commit 语义一致。path-level preview 只 load/validate source archive path 与 inspect target path，不调用 `save_to_path_atomically`，因此不会写 source/target archive 文件。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步 folder-backed `slot_export` owner 和 export preview API；本切片按“首先实现功能，测试可以暂时延后”不新增行为测试，后续若补测试需拆出 single-slot export/export-preview 子模块，避免 `path_management.rs` 继续膨胀。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍 deferred。 |
| M3 | 3.1 runtime session archive capture preview helpers | runtime_session_archive_capture_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionSlotCapturePreviewReport` 已加入 session public surface，并从 `scene::dynamic_scene` 与 `scene` 根出口公开；`RuntimeSessionArchive::preview_capture_world_slot`、`preview_capture_level_slot`、`preview_capture_world_slot_to_path` 与 `preview_capture_level_slot_to_path` 已加入 quicksave/autosave capture dry-run API。新增 `session/slot_capture.rs` 统一复用 archive validation、slot id normalization、metadata normalization、captured `DynamicScene` validation、replace-existing 判断与 entity/resource count projection；`capture_world_slot` 与 `capture_level_slot` 现在先调用同一 preview 规则，再用 preview 生成的 canonical `RuntimeSessionSlot` 提交 `upsert_slot` mutation，保证 dry-run 与 commit 语义一致。path-level preview 复用 `load_or_empty_from_path`，因此缺失 archive 按空 archive 预检、损坏 archive 仍报错，且不调用 `save_to_path_atomically`、不创建 archive 文件。`scene/tests/dynamic_scene_session/capture.rs` 新增 in-memory world capture preview 与 path capture preview 行为锚点，覆盖 canonical slot id、replace-existing true/false、normalized metadata、entity/resource counts、loaded archive unchanged、missing path preview 不创建文件与无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive slot mutation preview helpers | runtime_session_archive_slot_mutation_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionSlotMutationPreviewReport` 已加入 session public surface，并从 `scene::dynamic_scene` 与 `scene` 根出口公开；`RuntimeSessionArchive::preview_rename_slot`、`preview_update_slot_metadata`、`preview_touch_slot`、`preview_remove_slot`、`preview_rename_slot_from_path`、`preview_update_slot_metadata_from_path`、`preview_touch_slot_from_path` 与 `preview_remove_slot_from_path` 已加入 slot mutation dry-run API。新增 `session/slot_mutation.rs` 统一复用 archive validation、rename destination slot id normalization、duplicate-slot rejection、metadata normalization、touch timestamp projection、missing-slot checks 与 entity/resource count projection；`rename_slot`、`update_slot_metadata` 与 `touch_slot` 现在先调用同一 preview 规则，再用 report 中的 canonical destination id 或 normalized metadata 提交 mutation，path-level remove 也先走 preview 后再提交删除，保证 dry-run 与 commit 语义一致。path-level preview 只 load/validate archive path，不调用 `save_to_path_atomically`，因此不会写 archive 文件。`scene/tests/dynamic_scene_session/path_management.rs` 新增 in-memory slot mutation preview 与 path slot mutation preview 行为锚点，覆盖 normalized rename destination、normalized metadata、touch timestamp projection、remove summary、duplicate rename error、archive unchanged 与无 `.tmp`/`.bak` 残留；`path_management.rs` 当前 884 行，仍处于单一 path-level session archive behavior owner，后续若继续增加 path preview 族再拆 `copy/import/merge/mutation` 子模块。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive copy/save-as preview helpers | runtime_session_archive_copy_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::preview_copy_slot`、`preview_copy_slot_with_metadata`、`preview_copy_slot_from_path` 与 `preview_copy_slot_with_metadata_from_path` 已加入 copy/save-as 预检 API。新增 `session/slot_copy.rs` 统一复用 archive validation、destination slot id normalization、duplicate-slot rejection、metadata normalization 与 source slot entity/resource count projection；`copy_slot_with_metadata` 现在先调用同一 preview 规则，再用 report 中的 canonical destination id 与 normalized metadata 提交 `push_slot` mutation，保证 dry-run 与 commit 语义一致。path-level preview 只 load/validate archive path，不调用 `save_to_path_atomically`，因此不会写 archive 文件。`scene/tests/dynamic_scene_session/path_management.rs` 新增 in-memory copy/save-as preview 与 path copy/save-as preview 行为锚点，覆盖 normalized destination id、normalized metadata、entity/resource counts、duplicate destination error、archive unchanged 与无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path API owner split | runtime_session_archive_path_api_owner_split_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive` 的 public load/save/path mutation/path preview facade 已从 near-threshold `session.rs` 抽入 `zircon_runtime/src/scene/dynamic_scene/session/path_api.rs`，保留同名 inherent methods 和既有调用语义；`session.rs` 现在回落到 in-memory archive construction、archive-owned mutation、apply/restore、validation 与 core facade owner，行数从 994 降到 588，`path_api.rs` 为 421 行。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步 folder-backed session owner 列表（manifest/error/io/path_api/retention/metadata/slot/reports/merge/path_status/slot_id/slot_import/statistics）。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive single-slot import-preview helpers | runtime_session_archive_single_slot_import_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionSlotImportPreviewReport` 已加入 session public surface，并从 `scene::dynamic_scene` 与 `scene` 根出口公开；`RuntimeSessionArchive::preview_import_slot_from_archive`、`preview_import_slot_from_archive_with_metadata`、`preview_import_slot_from_archive_at_path`、`preview_import_slot_from_archive_with_metadata_at_path`、`preview_import_slot_from_archive_path_at_path` 与 `preview_import_slot_from_archive_path_with_metadata_at_path` 已加入 single-slot import 预检 API。新增 `session/slot_import/` 统一复用 target/incoming archive validation、destination slot id normalization、duplicate-slot rejection、metadata normalization 与 incoming slot entity/resource count projection；`import_slot_from_archive_with_metadata` 现在先调用同一 preview 规则，再用 report 中的 canonical destination id 与 normalized metadata 提交 `push_slot` mutation，保证 dry-run 与 commit 语义一致。path-level preview 只 load/validate target archive 与可选 source archive path，不调用 `save_to_path_atomically`，因此不会写 target/source archive 文件。`scene/tests/dynamic_scene_session/path_management.rs` 新增 in-memory single-slot import preview 与 path-to-path single-slot import preview 行为锚点，覆盖 normalized destination id、normalized metadata、entity/resource counts、duplicate destination error、target/source unchanged 与无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive merge-preview helpers | runtime_session_archive_merge_preview_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::preview_merge_archive`、`preview_merge_archive_at_path` 与 `preview_merge_archive_from_path_at_path` 已加入 session API，并由 `session/merge.rs` 统一计算 `RejectConflicts` / `KeepExisting` / `ReplaceExisting` duplicate-slot policy 下的 `RuntimeSessionArchiveMergeReport`；`merge_archive` 现在先复用同一 preview 规则再提交 mutation，保证 dry-run 与 commit report 语义一致。path-level preview 只 load/validate target archive 与可选 source archive path，不调用 `save_to_path_atomically`，因此不会写 target/source archive 文件。`scene/tests/dynamic_scene_session/path_management.rs` 新增 in-memory merge preview 与 path-to-path merge preview 行为锚点，覆盖 inserted/replaced/skipped report、target/source payload unchanged 与无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive source-path merge helpers | runtime_session_archive_source_path_merge_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::merge_archive_from_path_at_path_atomically` 已加入 session API，并由 `session/io.rs` 复用 validated source archive `load_from_path`、loaded incoming archive merge 语义与 target archive rollback-protected `save_to_path_atomically`；运行时 import flow 现在可从 source archive path 合并到 target archive path，保持 `RejectConflicts` / `KeepExisting` / `ReplaceExisting` duplicate-slot policy 与 `RuntimeSessionArchiveMergeReport` 语义一致，且 source archive file 不被修改。`scene/tests/dynamic_scene_session/path_management.rs` 新增 path-to-path archive merge 行为锚点，覆盖 inserted/replaced report、target reload persistence、source payload unchanged、source/target 两侧无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive source-path single-slot import helpers | runtime_session_archive_source_path_single_slot_import_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::import_slot_from_archive_path_at_path_atomically` 与 `import_slot_from_archive_path_with_metadata_at_path_atomically` 已加入 session API，并由 `session/io.rs` 复用 validated source archive `load_from_path`、loaded incoming archive 单槽导入语义与 target archive rollback-protected `save_to_path_atomically`；运行时 save/load flow 现在可从一个 source archive path 导入一个命名 slot 到 target archive path，支持 metadata override，返回 updated target manifest，且 source archive file 不被修改。`scene/tests/dynamic_scene_session/path_management.rs` 新增 path-to-path single-slot import 行为锚点，覆盖 source/target archive persistence、destination id normalization、metadata normalization、source payload unchanged 与 source/target 两侧无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive loaded single-slot save-to-path helpers | runtime_session_archive_loaded_single_slot_save_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::save_single_slot_archive_to_path_atomically` 已加入 session API，并复用 `single_slot_archive` 与 rollback-protected `save_to_path_atomically`；运行时 save/share flow 现在可从已加载 archive 中抽取一个命名 slot，原子保存为独立 `RuntimeSessionArchive` 文件，返回 standalone manifest，且不 mutate source archive。`scene/tests/dynamic_scene_session/path_management.rs` 新增 in-memory single-slot export 行为锚点，覆盖 standalone manifest、reload persistence、metadata preservation、source archive slot list unchanged 与无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；scoped `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive single-slot standalone export helpers | runtime_session_archive_single_slot_export_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::single_slot_archive`、`single_slot_archive_from_path` 与 `save_single_slot_archive_from_path_atomically` 已加入 session API，并由 `session/io.rs` 复用 validated source archive load、archive-owned one-slot extraction 与 rollback-protected `save_to_path_atomically`；运行时 save/share flow 现在可把一个命名 slot 抽出为独立 `RuntimeSessionArchive`，或从 source archive path 原子保存到 distinct target archive path，同时保持 source archive 不变。`scene/tests/dynamic_scene_session/path_management.rs` 新增行为锚点，覆盖 standalone manifest、reload persistence、metadata preservation、source payload unchanged 与 source/target 两侧无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 后 `cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path single-slot import helpers | runtime_session_archive_path_single_slot_import_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::import_slot_from_archive`、`import_slot_from_archive_with_metadata`、`import_slot_from_archive_at_path_atomically` 与 `import_slot_from_archive_with_metadata_at_path_atomically` 已加入 session API，并由 `session/io.rs` 复用 validated target archive load、archive-owned single-slot import 语义与 rollback-protected `save_to_path_atomically`；运行时 save/load selector 现在可从已加载 incoming archive 中导入一个 slot 到内存 archive 或持久化 archive path，支持 metadata override，且不会隐式读取第二个路径。`scene/tests/dynamic_scene_session/path_management.rs` 新增行为锚点，覆盖 path single-slot import 的 id normalization、metadata normalization、updated manifest、reload persistence 与无 `.tmp`/`.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path slot summary helpers | runtime_session_archive_path_slot_summary_code_check_passed_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::slot_ids_from_path`、`contains_slot_from_path` 与 `slot_summary_from_path` 已加入 session API，并由 `session/io.rs` 复用 validated `load_manifest_from_path` 与既有 manifest sorted slot/summary 规则；运行时 save/load selector 现在可直接从持久化 archive path 读取 sorted slot id list、slot existence 或 cloned `RuntimeSessionSlotSummary`，不需要调用方重复 archive load + manifest projection，也不会 mutate archive 文件或 restore world。`scene/tests/dynamic_scene_session/queries.rs` 新增行为锚点，覆盖 slot-id list、contains true/false、summary metadata、missing summary 与 payload 不变；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 通过（现有 warning only）。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path manifest filter helpers | runtime_session_archive_path_manifest_filter_code_static_passed_cargo_blocked_external_postprocess_drift_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::slots_with_tag_from_path` 与 `slots_matching_display_name_from_path` 已加入 session API，并由 `session/io.rs` 复用 validated `load_manifest_from_path` 与既有 manifest tag/display-name filtering 规则；运行时 save/load selector 现在可直接从持久化 archive path 读取 filtered `RuntimeSessionSlotSummary` 列表，不需要调用方重复 archive load + manifest projection，也不会 mutate archive 文件或 restore world。`scene/tests/dynamic_scene_session/queries.rs` 新增行为锚点，覆盖 trimmed tag query、display-name substring query、empty display-name no-match 与 payload 不变；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示。`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 仍被当前外部 render post-process 漂移阻塞：多个 `graphics/scene/scene_renderer/post_process/resources/execute_*` 模块缺 `UVec2`，且 `render_pass_execution_context/gpu/post_process.rs:175` 调用参数数量不匹配；本切片不触碰 render 会话文件，未声明 Cargo 通过。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path slot selection helpers | runtime_session_archive_path_slot_selection_code_static_passed_cargo_blocked_external_postprocess_drift_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::latest_updated_slot_id_from_path`、`oldest_updated_slot_id_from_path`、`latest_updated_slot_id_with_tag_from_path` 与 `oldest_updated_slot_id_with_tag_from_path` 已加入 session API，并由 `session/io.rs` 复用 validated `load_from_path` 与既有 manifest latest/oldest selection 规则；运行时 save/load selector 现在可直接从持久化 archive path 读取全局或 normalized tag bucket 的最新/最旧 slot id，不需要调用方重复 `load_manifest_from_path(...).latest...` 投影，也不会 mutate archive 文件或 restore world。`scene/tests/dynamic_scene_session/queries.rs` 新增行为锚点，覆盖 global latest/oldest、tagged latest/oldest、empty tag no-match 与 payload 不变；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示。`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 仍被当前外部 render post-process 漂移阻塞：多个 `graphics/scene/scene_renderer/post_process/resources/execute_*` 模块缺 `UVec2`；本切片不触碰 render 会话文件，未声明 Cargo 通过。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path retention preview helpers | runtime_session_archive_path_retention_preview_code_static_passed_cargo_blocked_external_postprocess_drift_tests_deferred | 2026-06-19 | `RuntimeSessionArchive::preview_prune_slots_from_path` 与 `preview_prune_slots_with_tag_from_path` 已加入 session API，并由 `session/io.rs` 复用 validated `load_from_path` 与既有 in-memory retention preview 语义；运行时 autosave/quicksave cleanup 现在可直接对持久化 archive path 读取 retained/removed dry-run report，且不写回 archive 文件、不产生 `.tmp` / `.bak`。`scene/tests/dynamic_scene_session/queries.rs` 新增行为锚点，覆盖 global path retention preview、tag bucket path retention preview、payload 不变、archive reload 后 slot ids 未变。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 文档提示。`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0619 --message-format short --color never` 与同命令 `--no-default-features` 均被当前外部 render post-process 漂移阻塞：`execute_*` post-process resources 缺 `UVec2`，且 `render_pass_execution_context/gpu/post_process.rs:175` 调用参数数量不匹配；本切片不触碰 render 会话文件，未声明 Cargo 通过。单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path query helpers | runtime_session_archive_path_query_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::statistics_from_path`、`diff_slot_from_path_with_world` 与 `diff_slot_from_path_with_level` 已加入 session API，并由 `session/io.rs` 复用 validated `load_from_path` 后委托到既有 in-memory statistics/diff 语义；运行时 diagnostics / overwrite preview 现在可直接用 archive path + slot id 查询 aggregate statistics 或 slot-vs-world/level diff，且不会 mutate target world/level 或 archive file。`scene/tests/dynamic_scene_session/queries.rs` 新增行为锚点，覆盖 path statistics 聚合、path diff mismatch 不改变 target world、path diff against LevelSystem match；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive test module split | runtime_session_archive_test_module_split_code_check_passed_tests_deferred | 2026-06-18 | `zircon_runtime/src/scene/tests/dynamic_scene_session.rs` 已拆成 folder-backed `scene/tests/dynamic_scene_session/` subtree：`mod.rs` 只保留共享 fixture 与子模块声明，行为锚点按 capture/load/path_management/persistence/retention 分文件，最大文件约 247 行，避免后续 session 归档功能继续堆进 near-threshold 单文件；`scene/tests/mod.rs` 的 `mod dynamic_scene_session;` 入口不变。`docs/zircon_runtime/scene/dynamic_scene.md` tests header 与验证记录已同步。验证：rustfmt split test modules 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path copy import helpers | runtime_session_archive_path_copy_import_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::copy_slot_at_path_atomically`、`copy_slot_with_metadata_at_path_atomically` 与 `merge_archive_at_path_atomically` 已加入 session API，并由 `session/io.rs` 复用 validated `load_from_path`、archive-owned copy/merge 语义与 rollback-protected `save_to_path_atomically`；运行时 save-as/import flow 现在可直接对持久化 archive path 做 slot copy、metadata override copy 与 incoming archive merge，并返回 updated manifest 或 `RuntimeSessionArchiveMergeReport`。merge helper 接收已加载 incoming archive，避免 helper 隐式管理第二路径。行为锚点覆盖 path copy metadata tag 规范化与持久化、path merge replace-existing report 与持久化；后续切片已把 near-threshold `dynamic_scene_session.rs` 拆到 folder-backed test subtree。`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path retention helpers | runtime_session_archive_path_retention_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::prune_slots_at_path_atomically` 与 `prune_slots_with_tag_at_path_atomically` 已加入 session API，并由 `session/io.rs` 复用 validated `load_from_path`、in-memory retention prune 算法与 rollback-protected `save_to_path_atomically`；运行时 autosave/quicksave cleanup 现在可直接对持久化 archive path 提交全局 retention 或 tag bucket retention，并返回同一 `RuntimeSessionArchivePruneReport` retained/removed slot 语义。`scene/tests/dynamic_scene_session.rs` 新增行为锚点，覆盖 global path prune 持久化、无 `.tmp`/`.bak` 残留，以及 tag bucket path prune 不删除其它 tag slot；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive declaration split | runtime_session_archive_declaration_split_code_check_passed_tests_deferred | 2026-06-18 | `zircon_runtime/src/scene/dynamic_scene/session.rs` 已从 near-1000-line risk zone 拆分：metadata normalization → `session/metadata.rs`，slot data 与 slot-to-world/level behavior → `session/slot/{mod,capture,restore,diff,summary}.rs`，merge policy/report → `session/merge.rs`，path status → `session/path_status.rs`，restore/diff reports → `session/reports.rs`，slot-id normalization/validation → `session/slot_id.rs`，archive statistics → `session/statistics.rs`；`session.rs` 现在保留 archive 本体、public facade、archive-owned mutation/apply/restore/validation，继续 re-export 现有 `scene::dynamic_scene` / `scene` 公共面，不新增兼容层或根导出。`docs/zircon_runtime/scene/dynamic_scene.md` 已同步 related_code / implementation_files 与 owner split 描述。验证：rustfmt touched Rust 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path management helpers | runtime_session_archive_path_management_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::rename_slot_at_path_atomically`、`update_slot_metadata_at_path_atomically`、`touch_slot_at_path_atomically` 与 `remove_slot_at_path_atomically` 已加入 session API，并由 `session/io.rs` 统一执行 validated `load_from_path` → archive-owned mutation → rollback-protected `save_to_path_atomically` → updated manifest 返回；运行时 save selector 现在可直接对持久化 archive path 做 slot rename、metadata normalization update、timestamp touch 与 slot removal，同时保留 missing archive / invalid archive / duplicate rename target / missing slot 的错误边界，且不公开 mutable slot access。`scene/tests/dynamic_scene_session.rs` 新增行为锚点，覆盖 path rename 持久化与无 `.tmp`/`.bak` 残留、path metadata update tag 规范化、path touch 后 latest selection、path remove 与 missing-slot error；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。参考锚点沿用当前 session archive path helpers 自研判词：Zircon 只扩展 runtime session persistence 的 path-level mutation，不引入 gameplay save schema、fallback slot selection 或 editor authoring state。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive load-from-path helpers | runtime_session_archive_load_from_path_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::restore_slot_from_path_to_empty_world`、`restore_slot_from_path_into_level`、`apply_slot_from_path_to_world` 与 `apply_slot_from_path_to_level` 已加入 session API，并由 `session/io.rs` 复用 validated `load_from_path` 后委托到既有 in-memory restore/apply 语义；运行时 load/instantiate flow 现在可直接用 archive path + slot id 恢复空 World、替换 LevelSystem、实例化到 live World 或 live LevelSystem，同时保留 missing archive / invalid archive / missing slot 的错误边界、Level metadata restore 与 entity remap。`scene/tests/dynamic_scene_session.rs` 新增行为锚点，覆盖 path-to-empty-world restore、path-to-level restore + metadata、path-to-live-world/level apply remap 与 missing-slot error；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。参考锚点已读：`dev/bevy/crates/bevy_scene/src/scene.rs`；本切片自研判词：Zircon 的路径级 session load helper 只包装当前 runtime archive persistence，不引入 Bevy scene dependency queue 或 fallback slot selection。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive capture-to-path helpers | runtime_session_archive_capture_to_path_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::load_or_empty_from_path`、`capture_world_slot_to_path_atomically` 与 `capture_level_slot_to_path_atomically` 已加入 session API，并由 `session/io.rs` 串接“缺失 path → empty archive、已有 path → validated load、capture/upsert slot、rollback-protected atomic save、返回 updated manifest”的 quicksave/autosave 底层流程；存在但损坏或不可读的 archive 仍返回错误，不会被 helper 静默覆盖。`scene/tests/dynamic_scene_session.rs` 新增行为锚点，覆盖缺失路径创建 world slot、已有 archive 的 level slot upsert 并保留其它 slot、损坏 archive 不覆盖；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。参考锚点已读：`dev/bevy/crates/bevy_scene/src/scene.rs`；本切片自研判词：Zircon 当前会话归档是 runtime session persistence，不引入 gameplay save schema 或 Bevy asset dependency queue，只补 path-level load/capture/save helper。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）；`cargo test -p zircon_runtime --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never --no-run dynamic_scene_session` 300s 超时且未声明测试通过；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M1 | 1.1 文本残留处理 | 完成 | 2026-06-12 | `zircon_runtime/src/scene/editor_projection` 不存在；`editor_projection` 仅保留在 `component_structure.rs` 的禁止复活结构守卫中 |
| M1 | 1.2 editor 白名单 | 完成 | 2026-06-12 | `runtime_naming_boundary` 分类 runtime `editor` 命名：1260 locations / 0 unclassified；白名单并入 `runtime-architecture-review-m0.md` |
| M1 | 1.3 legacy 裁决 | 完成 | 2026-06-12 | `runtime_naming_boundary` 分类 runtime `legacy` 命名：516 locations / 0 unclassified；10 个 debt owner bucket 记录在审计输出 |
| M1 | 1.4 守卫机器化 | 完成 | 2026-06-12 | 新增 `runtime_absorption::naming_boundary` 与 `runtime_structure_audits/runtime_naming_boundary.py`；`cargo test -p zircon_runtime --lib naming_boundary --locked` 1 passed |
| M2 | 2.1 覆盖矩阵 | 完成 | 2026-06-12 | `docs/zircon_runtime/scene/inspection.md` 记录 world project / dynamic scene / asset scene / inspection JSON 覆盖矩阵；`cargo test -p zircon_runtime --lib authoring --locked` 17 passed；`cargo test -p zircon_runtime --lib inspection --locked` 6 passed |
| M2 | 2.2 维护公约 | 完成 | 2026-06-12 | `authoring_token_tables_stay_sorted_and_deduplicated` 固化 token 表排序去重；token 表 smoke check: serialized 19 / source 25；`inspection.md` 记录 editor authoring 类型新增时的同步规则；authoring/inspection Cargo 过滤测试通过 |
| M3 | 3.1 legacy debt bucket 移交确认 | 状态复核完成 | 2026-06-21 | `runtime_naming_boundary` 当前 gate status 为 `classified`：editor 1556 locations / 281 files / 0 unclassified；legacy 491 locations / 101 files / 0 unclassified；legacy 仍为 7 个 naming debt owner bucket，并已在 runtime 总览 P9 以 runtime graphics、DDS container policy、UI template schema、input event、asset schema、dynamic API migration、scene schema/render owner 分派口径同步；未改行为代码。 |
| M3 | 3.1 runtime naming Markdown renderer split | runtime_naming_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | `runtime_naming_markdown.py` 已接管 `render_runtime_naming_boundary_markdown` 与 per-term Markdown 输出，并显式渲染 unclassified 计数；`runtime_naming_boundary.py` 保留分类/audit 数据 owner，`state_reducer` editor 命名继续归 `runtime-ui-component-catalog-editor-controls`。Direct audit：`gate_status = classified`，editor locations/files: 1556 / 281，`editor_unclassified = 0`，legacy locations/files: 491 / 101，`legacy_unclassified = 0`，`legacy_debt_count = 7`。验证通过：Python py_compile、direct `runtime_naming_boundary_audit`、rustfmt touched status files、standalone naming_boundary 2/2、standalone plan_status 33/33；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 non-network server gate fixture sync | 静态复核完成 | 2026-06-14 | `non_network_server_naming` 现在把 Hub UNC path fixture（`\\?\UNC\server\share\Game` / `\\server\share\Game`）列为允许上下文，而不是 runtime owner debt；当前审计：count 59、graphics-render-framework-debt 58、editor-workbench-authority-label-debt 1、allowed_context_count 99、observer_false_positive_count 87、unclassified 0、`risks` 仅剩已分类 owner migration debt；当前 allowed 计数包含后续 Table/DataGrid `sortingMode = "server"` 外部 API 字面值裁决，未改 Hub/Runtime 生产代码。 |
| M3 | 3.1 non-network server Markdown renderer split | non_network_server_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | `non_network_server_naming_markdown.py` 接管 `render_non_network_server_naming_markdown(...)`；`non_network_server_naming.py` 保留 token 扫描、allowed-context 判定、分类与风险聚合。Direct audit：boundary 323 lines、Markdown owner 41 lines、count 77、classification_count 2、allowed_context_count 94、observer_false_positive_count 95、unclassified_location_count 0、non_network_server_migration_debt_count 2、rendered_lines 30；当前债务仍为 graphics render-framework 与 editor workbench authority-label owner groups。验证：Python py_compile、direct non-network audit、standalone plan_status 33/33；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 hard-cutover migration-smell owner sync | 静态复核完成 | 2026-06-14 | `hard_cutover_migration_smells` 当前审计：source_file_count 5839、legacy_reference_count 212、compat/shim 0、allowed_business_bridge_reference_count 300、migration_bridge_smell_count 0、classification groups 7、unclassified 0；新增/确认 runtime UI input 63、hybrid GI render 56、runtime graphics 30、Hub archived text 58、DDS 1、Net hyper client API 1、editor UI fixture 3。该切片只更新审计归类和文档证据，未改 UI/Hub/Net/graphics 生产行为代码。 |
| M3 | 3.1 texture importer DDS caps policy wording | hard_cutover_dds_caps_policy_static_passed_cargo_pending | 2026-06-17 | `zircon_plugins/texture_importer/runtime/src/container/dds.rs` 的 DX10 双 cubemap 诊断改为 `DDSCAPS2_CUBEMAP caps2 policy` 与 DX10 texturecube flag 当前术语；`hard_cutover_migration_smells` 当前 direct audit：source_file_count 6145、legacy_reference_count = 148、compat/shim 0、allowed_business_bridge_reference_count 312、classification_count = 5、hard_cutover_migration_debt_count = 5、unclassified_locations = []，DDS debt bucket absent。验证：rustfmt check、Python py_compile、direct hard-cutover audit、aggregate audit hard-cutover/status assertions、standalone plan-status 32/32、texture_importer runtime `legacy` scan clean；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 hard-cutover migration-smell Markdown renderer split | hard_cutover_migration_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | `hard_cutover_migration_smells_markdown.py` 接管 `render_hard_cutover_migration_smells_markdown(...)`；`hard_cutover_migration_smells.py` 保留扫描/分类/风险聚合并补齐 Runtime 05 dynamic scene document `legacy` refs 到 `legacy-runtime-scene-document-debt` owner bucket。Direct audit：boundary 408 lines、Markdown owner 60 lines、source_file_count 8520、legacy_reference_count 152、compat/shim 0、allowed_business_bridge_reference_count 316、migration_bridge_smell_count 0、classification_count 6、hard_cutover_migration_debt_count 6、unclassified_location_count 0、rendered_lines 60。验证：Python py_compile、direct hard-cutover audit、standalone plan_status 33/33；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 plan-status 输出表守卫 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::plan_status::status_output_tables::runtime_plan_status_output_tables_cover_index_and_all_subplans` 与 `runtime_index_status_output_records_recent_cross_plan_slices` 锁定 runtime 总索引和 01-14 子计划必须保留 `## 状态与产出记录` 表，并要求总索引记录 Runtime 01/02/03/04/06/07/08/09/10/11/12/13/14 镜像文档守卫的 standalone rustc 证据与 pending Cargo gates；当前守卫还反向覆盖 all runtime index status rows full coverage guard，防止总索引新增状态行未登记到期望清单。M0 review 已列出 `runtime_absorption/plan_status/status_output_tables.rs`；验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit`（support 7/7、subplans 14/14、core 15/15、pending 15/15、doc 11/11、status-output table guard anchors 4/4、`status_output_table_guard_present = true`、status_table_gaps []、risks []）、standalone rustc `status_output` 2/2 通过；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 M0 absorption guard coverage sync | static_docs_passed_cargo_pending | 2026-06-14 | `docs/engine-architecture/runtime-architecture-review-m0.md` 已补齐 `runtime_architecture_review_documents_all_absorption_guards` 所需的 25 个 mounted `runtime_absorption` guard 覆盖：新增/补正文档化 `runtime_absorption/ecs_kernel_data.rs`、`runtime_absorption/script_binding.rs`、`runtime_absorption/tech_stack.rs`，并移除重复 `core_spine_root_generated.rs` frontmatter 行。验证：full M0 absorption guard coverage simulation 25/25 通过、conflict/trailing scans 通过、scoped `git diff --check` 仅 LF-to-CRLF warning；未改生产代码，full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 plan-status 审计元数据守卫 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 维护 `status_output_table_guard_count = 4`、`missing_status_output_table_guard_anchors = []`、`status_output_table_guard_present = true`，把 `runtime_plan_status_output_tables_cover_index_and_all_subplans`、`runtime_index_status_output_records_recent_cross_plan_slices`、all runtime index status rows 与 full coverage guard 从普通 support anchor 提升为显式审计字段；同步 Runtime 05、runtime index、M0 review 与 runtime-interface convergence，full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 plan-status Cargo attempt 状态审计 | cargo_attempt_status_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 `cargo_attempt_status_anchor_count = 20`、`missing_cargo_attempt_status_index_anchors = []`、`missing_cargo_attempt_status_runtime_14_anchors = []`、`missing_cargo_attempt_status_guard_anchors = []`、`cargo_attempt_status_guard_present = true`，把 `cargo_deferred_active_lane` / `cargo_blocked_external_compile_drift` / `cargo_recheck_blocked_external_ui_compile_drift` / `cargo_recheck_timeout_no_result` 四类 Cargo attempt 状态提升为显式审计字段；runtime index、Runtime 14 子计划与 `status_output_tables` 守卫均保留 `Runtime 14 Cargo 验证窗口探测`、`Runtime 14 animation Cargo gate 尝试`、`Runtime 14 animation Cargo gate 修复与复验阻塞` 与 `Runtime 14 animation runtime-status focused recheck timeout` 记录。该切片是静态结构证据，full `scene::` Cargo closeout gate 与 Runtime 14 remaining gates 仍 pending。 |
| M3 | 3.1 plan-status Cargo timeout 状态审计 | cargo_attempt_timeout_status_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_boundary` 的 Cargo attempt 审计扩展到 `cargo_attempt_status_anchor_count = 20`：新增 `cargo_recheck_timeout_no_result`、`Runtime 14 animation runtime-status focused recheck timeout`、`runtime_status_reports_player_rig_and_gpu_readiness`、`904s`、`无测试结果` 与 `residual cargo/rustc processes were stopped` 锚点，要求 runtime index、Runtime 14 子计划与 `status_output_tables` 同步保留该 focused recheck timeout 事实。验证：Python py_compile 通过；direct `runtime_plan_status_boundary_audit` 报告 Cargo attempt runtime index 20/20、Runtime 14 plan 20/20、status-output guards 8/8、`risks = []`；aggregate `audit_runtime_structure.py --json` 同步报告 `cargo_attempt_status_anchor_count = 20`、`cargo_attempt_status_guard_present = true`；standalone `status_output_tables.rs` 2/2 通过。这只记录无测试结果的复验尝试，不提升 Runtime 14 `animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates，也不关闭 Runtime 05 full `scene::` gate。 |
| M3 | 3.1 Runtime 07 scene asset 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-14 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引 `## 状态与产出记录` 继续保留 `Runtime 07 scene asset owner split`、`Runtime 07 scene asset split-drift repair`、`Runtime 07 scene asset folder-split public-surface guard` 与 `Runtime 07 scene asset guard 纳入 performance_hotpath_boundary` 四行，并锁定 `hotspot_guard_anchor_count = 20`、standalone `...performance_hotspots.rs` 6/6、`scene_asset` / Runtime 07 Cargo gates pending 与三类 deferred status。验证为 rustfmt/standalone status-output/direct `runtime_plan_status_boundary_audit`；full `scene::` Cargo closeout gate 与 Runtime 07 Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 07 scene asset 状态审计元数据 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 `runtime_07_scene_status_index_anchor_count = 11`、`runtime_07_scene_status_guard_anchor_count = 10`、`runtime_07_scene_status_doc_anchor_count = 4`、`missing_runtime_07_scene_status_index_anchors = []`、`missing_runtime_07_scene_status_guard_anchors = []`、`missing_runtime_07_scene_status_doc_anchors = []`、`runtime_07_scene_status_guard_present = true`，把 Runtime 07 scene asset 状态行从 Rust 守卫提升为结构审计元数据；direct audit 报告 index 11/11、status-output guard 10/10、mirror docs 4/4，full `scene::` Cargo closeout gate 与 Runtime 07 Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 02 generated template 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-14 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引 `## 状态与产出记录` 继续保留 `Runtime 02 generated template count 审计同步` 行，并锁定 `template_file_count=10`、generated export templates 10/10、0 migration debt 与 stale 9/9 scan 证据。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2、conflict/trailing scans 与 scoped `git diff --check` 通过（仅 LF-to-CRLF warnings）；这只固定状态表写回，不关闭 Runtime 02 的 generated/export/app/editor/plugin Cargo gates。 |
| M3 | 3.1 Runtime 02 generated template 状态审计元数据 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 `runtime_02_generated_status_index_anchor_count = 6`、`runtime_02_generated_status_guard_anchor_count = 5`、`runtime_02_generated_status_doc_anchor_count = 3`、`missing_runtime_02_generated_status_index_anchors = []`、`missing_runtime_02_generated_status_guard_anchors = []`、`missing_runtime_02_generated_status_doc_anchors = []`、`runtime_02_generated_status_guard_present = true`，把 Runtime 02 generated template count 状态行从 Rust 守卫提升为结构审计元数据；direct audit 报告 index 6/6、status-output guard 5/5、mirror docs 3/3，full `scene::` Cargo closeout gate 与 Runtime 02 generated/export/app/editor/plugin Cargo gates 仍 pending。 |
| M3 | 3.1 status-output current anchor fix | status_table_static_passed_cargo_pending | 2026-06-16 | 将 runtime 总索引状态表当前证据锚同步到 status-output 守卫期望：`runtime_02_generated_status_guard_anchor_count = 5`、`runtime_07_owner_budget_status_guard_anchor_count = 5`、`runtime_09_m1_2_navigation_legacy_reply_renamed_static_passed_cargo_pending`、Runtime 10 `standalone rustc 5/5`、`runtime_11_m2_1_graphics_frustum_rayon_cutover_static_passed_cargo_pending` 与 Runtime 12 `standalone rustc 4/4`；direct `runtime_plan_status_boundary_audit` 仍报告 `missing_runtime_02_generated_status_guard_anchors = []`、`missing_runtime_07_owner_budget_status_guard_anchors = []`、`risks = []`，wrapped standalone status-output 2/2 通过；这只修复状态表元数据漂移，不关闭 Runtime 02/07/09/10/11/12 Cargo gates 或 Runtime 05 full `scene::` closeout。 |
| M3 | 3.1 Runtime 07 owner-budget 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-14 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引 `## 状态与产出记录` 继续保留 Runtime 07 owner-budget 状态行。该行的当时锚点后来由历史漂移行接管；当前 owner-budget 状态已由 2026-07-01 `Runtime 07 owner-budget 0-hotspot current audit sync` 行接管。验证目标：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` owner-budget anchors、standalone status-output 2/2；这只固定状态表写回，不关闭 Runtime 07 extract/ecs_query/performance profiling/FPS Cargo gates。 |
| M3 | 3.1 Runtime 07 owner-budget 状态审计元数据 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_plan_status_boundary` 新增 Runtime 07 owner-budget 状态元数据；2026-07-01 当前锚点为 `runtime_07_owner_budget_status_index_anchor_count = 9`、`runtime_07_owner_budget_status_guard_anchor_count = 8`、`runtime_07_owner_budget_status_doc_anchor_count = 3`、`missing_runtime_07_owner_budget_status_index_anchors = []`、`missing_runtime_07_owner_budget_status_guard_anchors = []`、`missing_runtime_07_owner_budget_status_doc_anchors = []`、`runtime_07_owner_budget_status_guard_present = true`，把 Runtime 07 owner-budget 状态行从 Rust 守卫提升为结构审计元数据；direct audit 应报告 index 9/9、status-output guard 8/8、mirror docs 3/3，full `scene::` Cargo closeout gate 与 Runtime 07 Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 07 owner-budget 39-hotspot 状态表同步 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_boundary` 的 Runtime 07 owner-budget status 显式审计字段现在指向 `Runtime 07 owner-budget 39-hotspot 漂移同步`：总索引锚点保留 `large_file_hotspot_count = 39`、`runtime-framework-render=4`、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=39、standalone `performance_hotspots.rs` 6/6 与 `owner-budget 39-hotspot resync row`；审计计数仍为 index 6/6、status-output guard 5/5、mirror docs 3/3。该行只同步状态表与审计元数据，full `scene::` Cargo closeout gate 与 Runtime 07 extract/ecs_query/performance profiling/FPS gates 仍 pending。 |
| M3 | 3.1 Runtime 07 owner-budget 42-hotspot 状态表同步 | status_table_static_passed_cargo_pending | 2026-06-17 | `runtime_plan_status_boundary` 的 Runtime 07 owner-budget status 显式审计字段现在指向 `Runtime 07 owner-budget 42-hotspot 漂移同步`：总索引锚点保留 `large_file_hotspot_count = 42`、`runtime-framework-render=4`、`runtime-other=15`、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=42、standalone `performance_hotspots.rs` 6/6 与 `owner-budget 42-hotspot resync row`；审计计数仍为 index 6/6、status-output guard 5/5、mirror docs 3/3。该行只同步状态表与审计元数据，full `scene::` Cargo closeout gate 与 Runtime 07 extract/ecs_query/performance profiling/FPS gates 仍 pending。 |
| M3 | 3.1 Runtime 05 plan-status owner-budget current mirror fix | status_table_static_passed_cargo_pending | 2026-06-20 | 修正 Runtime 05 `计划状态镜像` 当前段中的 owner-budget 元数据；2026-07-01 再同步为当前 Runtime 07 0-hotspot owner-budget mirror：`large_file_m1_gate_status = classified-and-clear`、`large_file_hotspot_count = 0`、`large_file_migration_debt_count = 0`、`large_file_owner_class_count = 0`、`large_file_unclassified_hotspot_count = 0` 与 `Runtime 07 owner-budget 0-hotspot current audit sync`。`runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs` 要求 Runtime 05 子计划保留这些当前锚点；验证目标：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` support / risks=[]、standalone plan-status。该行只修镜像文本，不提升 Runtime 05/07 Cargo gates。 |
| M3 | 3.1 Runtime 02/07 状态审计 recent-static 守卫 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::plan_status::recent_static_guards::runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs` 现在锁定 Runtime 05 子计划、runtime 总索引与 M0 review 均保留 `runtime_02_generated_status_guard_present = true` 和 `runtime_07_owner_budget_status_guard_present = true`，防止 Runtime 02 generated template 与 Runtime 07 owner-budget 状态审计元数据只停留在 Python 审计输出。验证：rustfmt check、standalone recent_static 1/1、direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo closeout gate 与 Runtime 02/07 Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 05 recent-static 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 05 recent-static Runtime 02/07 status metadata guard` 行，要求保留 `runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs`、`runtime_02_generated_status_guard_present = true`、`runtime_07_owner_budget_status_guard_present = true` 与 standalone recent_static 1/1 证据。验证：rustfmt check、standalone status-output 2/2、direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo closeout gate 与 Runtime 02/07 Cargo gates 仍 pending。 |
| M3 | 3.1 non-network server UI sortingMode allowlist | static_audit_passed_cargo_pending | 2026-06-14 | `non_network_server_naming` 现在把 runtime UI Table/DataGrid `sortingMode = "server"` 字面值列为外部 UI API allowed context，而不是 `unclassified-non-network-server` owner debt；当前审计 count 59、classification groups 2、allowed_context_count 99、observer_false_positive_count 87、unclassified_location_count 0，剩余风险只是不应由 Runtime 05 跨会话处理的 graphics render-framework 与 editor workbench authority-label 已分类命名债。验证：Python py_compile、aggregate `audit_runtime_structure.py --json` non-network assertions；未改 UI 生产行为代码，full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 non-network server allowlist 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 05 non-network server UI sortingMode allowlist` 行，要求保留 `sortingMode = "server"`、`allowed_context_count 99`、`unclassified_location_count 0` 与 aggregate `audit_runtime_structure.py --json` non-network assertions 证据。验证：rustfmt check、standalone status-output 2/2、direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 naming_boundary non-network server Rust 守卫 | naming_guard_static_passed_cargo_pending | 2026-06-14 | `runtime_absorption::naming_boundary` 新增 `runtime_non_network_server_naming_is_classified_by_owner`，Rust 侧扫描 `zircon_runtime/src` 的非网络 `server` 命名，允许真实网络/target-server/dev-server/API 字面值，保留 graphics render-framework 已分类 owner debt，并把 `sortingMode = "server"` 视为外部 UI API literal；同切片把 runtime UI `state_reducer` 的 `field-editor` / `source-editor` 纳入 `runtime-ui-component-catalog-editor-controls` 桶。验证：rustfmt check、standalone naming_boundary 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 Runtime 07 project_io 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-14 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引保留 `Runtime 07 project_io folder split` 行，锁定 `project_io/{camera,physics,post_process,references,script,transform}.rs`、`runtime_07_project_io_folder_split_keeps_entry_and_converter_owners`、`large_file_hotspot_count = 38` 与 `runtime-other=14`。验证：rustfmt check、Python py_compile、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=38、status-output 守卫待 standalone 复验；full `scene::` Cargo closeout gate 与 Runtime 07 Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 12 gamepad event-owner 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引保留 `Runtime 12 gamepad event-owner 漂移同步` 行，锁定 `session/events.rs`、`InputEvent::Gamepad*`、`missing_gamepad_abi_anchors = []` 与 standalone `input_stack.rs` rustc 4/4 evidence；Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2、conflict/trailing/diff checks 通过（仅 LF-to-CRLF warnings）；full `scene::` Cargo closeout gate 与 Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 12 behavior-test 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引保留 `Runtime 12 Input stack 行为测试锚审计同步` 行，锁定 `behavior_test_anchor_count = 6`、`missing_behavior_test_anchors = []`、standalone input_stack 4/4 与 standalone status-output 2/2 evidence；Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 与 Runtime 12 input/action_map/gamepad/app Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 04 behavior-test 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引保留 `Runtime 04 Asset pipeline 行为测试锚审计同步` 行，锁定 `behavior_test_anchor_count = 20`、`missing_behavior_test_anchors = []`、standalone asset_pipeline 1/1 与 standalone status-output 2/2 evidence；Runtime 04 broader `asset::` / `worker_pool` Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 与 Runtime 04 broader `asset::` / `worker_pool` Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 08 behavior-test 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引保留 `Runtime 08 ECS 行为测试锚审计同步` 行，锁定 `behavior_test_anchor_count = 16`、`missing_behavior_test_anchors = []`、standalone ecs_kernel_data 1/1 与 standalone status-output 2/2 evidence；Runtime 08 entity/observer/command/messages/change_tick/ecs Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone ecs_kernel_data 1/1、standalone status-output 2/2；full `scene::` Cargo closeout gate 与 Runtime 08 ECS Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 07 owner-budget 37-hotspot 状态表再同步 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 07 owner-budget 37-hotspot 再同步` 行，要求保留 `large_file_hotspot_count = 37`、`runtime-other=12`、direct `performance_hotpath_boundary_audit` risks=0 / hotspots=37 与 standalone `performance_hotspots.rs` 6/6 证据；`runtime_plan_status_boundary` 同步 `owner-budget 39-hotspot resync row` doc anchor。验证：rustfmt check 通过；Python py_compile 通过；direct `runtime_plan_status_boundary_audit` risks=0 / Runtime 07 owner-budget anchors index 6/6、guard 5/5、docs 3/3；standalone `status_output_tables.rs` 2/2；full `scene::` Cargo closeout gate 与 Runtime 07 extract/ecs_query/performance profiling/FPS Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 03 module-doc 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 03 Schedule/frame-loop module-doc 镜像元数据` 行，要求保留 frame schedule module-doc anchors 3/3、guard/test files 8/8、Runtime 03 guard anchors 14/14 与 `ecs_schedule/time/session/schedule_parallel Cargo gates pending`；`runtime_plan_status_boundary` 同步 Runtime 03 module-doc status-output row，报告 Runtime 03 module-doc status anchors 6/6 in runtime index、5/5 in status-output guards、3/3 in mirror docs，`runtime_03_module_doc_status_guard_present = true`。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、aggregate `audit_runtime_structure.py --json` plan-status assertions、standalone status-output 2/2；full `scene::` Cargo closeout gate 与 Runtime 03 ecs_schedule/time/session/schedule_parallel Cargo gates 仍 pending。 |
| M3 | 3.1 Runtime 03 behavior-test 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 03 Schedule/frame-loop 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 13`、`missing_behavior_test_anchors = []`、standalone schedule_frame_loop 1/1 与 standalone status-output 2/2 evidence；Runtime 03 ecs_schedule/time/session/schedule_parallel Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 与 Runtime 03 ecs_schedule/time/session/schedule_parallel Cargo gates 仍 pending。 |
| M3 | 3.1 status-output all-index-row coverage guard | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在反向校验 all runtime index status rows，要求 runtime 总索引 `## 状态与产出记录` 未来新增行时必须同步加入 status-output guard expectations；`runtime_plan_status_boundary` 同步 status-output table guard anchors 4/4、`status_output_table_guard_count = 4`、`status_output_table_guard_present = true` 与 full coverage guard。验证：rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 Runtime 11 behavior-test 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-17 | 扩展 `runtime_absorption::plan_status::status_output_tables::runtime_index_status_output_records_recent_cross_plan_slices`，要求 runtime 总索引保留 `Runtime 11 JobSystem 行为测试锚审计同步`、`Runtime 11 scheduler wait_all 同步点` 与 `Runtime 11 panic-safe handle completion` 行，锁定 `behavior_test_anchor_count = 12`、`missing_behavior_test_anchors = []`、`scheduler_wait_all_waits_for_all_handles_and_records_sync_time`、`job_handle_wait_reports_task_panic_without_leaking_completion`、`schedule_after_propagates_dependency_panic_without_running_dependent_task`、standalone job_system 1/1 与 standalone status-output/plan-status 证据；Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gates 与 render-owned `parallel_frustum.rs` cutover 仍 pending。验证：rustfmt check、Python py_compile、direct `job_system_boundary_audit` / aggregate Runtime 11 + plan-status assertions、standalone job_system、standalone plan-status；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 Runtime 01 Tech-stack 行为锚状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 01 Tech-stack 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 4`、`missing_behavior_test_anchors = []`、standalone tech_stack 1/1 与 standalone status-output 2/2 证据；Runtime 01 `tech_stack/extensions/text_shaper/plugin physics` Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `tech_stack_boundary_audit`、aggregate Runtime 01 + plan-status assertions、standalone tech_stack 1/1、standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 Runtime 10 Dynamic API 行为锚状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 10 Dynamic API 行为测试锚审计同步` 行，要求保留 `behavior_test_anchor_count = 15`、`missing_behavior_test_anchors = []`、standalone dynamic_api_session 5/5 与 standalone status-output 2/2 证据；`runtime_plan_status_boundary` 同步 Runtime 10 behavior status anchors index 6/6、guard 4/4、docs 3/3 与 `runtime_10_behavior_status_guard_present = true`，并把它作为 Runtime 10 behavior status-output row 记录。Runtime 10 `dynamic_api`、完整 app loader 与 UI contract owner/Cargo gates 仍 pending。验证：rustfmt check 通过；Python py_compile 通过；direct `runtime_plan_status_boundary_audit` risks=0 / Runtime 10 behavior status anchors index 6/6、guard 4/4、docs 3/3；standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 plan-status output-anchor 模块拆分 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_output_anchors.py` 现在 owns status-output 与 Cargo attempt anchor tuples；`runtime_plan_status_boundary.py` remains orchestration，拆分后为 761 lines，新锚点模块为 141 lines。验证：Python py_compile 通过；direct `runtime_plan_status_boundary_audit` risks=0 / `status_output_table_guard_present = true` / `cargo_attempt_status_guard_present = true` / `runtime_10_behavior_status_guard_present = true`；standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 plan-status output-anchor 预算守卫 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_boundary_audit` 现在显式报告 `plan_status_boundary_line_count = 855` / `max_plan_status_boundary_lines = 900`、`status_output_anchor_module_present = true`、`status_output_anchor_module_line_count = 143` / `max_status_output_anchor_module_lines = 300`，且 `plan_status_boundary_under_line_budget = true`、`status_output_anchor_module_under_line_budget = true`、`risks = []`。验证：Python py_compile 通过；direct `runtime_plan_status_boundary_audit` risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 Runtime 02 guard-test anchors 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 02 guard-test anchors 审计同步` 行，要求保留 `guard_test_anchor_count = 26`、`missing_guard_test_anchors = []` 与 standalone core_spine_root_generated 1/1 证据；Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` Cargo gates 与 M3 render-owner alias cutover 仍 pending。验证：rustfmt check、Python py_compile、direct `core_spine_root_generated_boundary_audit`、standalone plan-status 32/32；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 Runtime 02 root_entries count 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-16 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 02 root_entries guard-count current resync` 行，要求保留 `EXPECTED_ROOT_ENTRIES_TEST_COUNT`、`root_entries guard tests 13/13`、`guard_test_anchor_count = 26` 与 standalone core_spine_root_generated 1/1 证据；该行只同步 Runtime 02/14 共享 root_entries 守卫期望，不提升 Runtime 02 Cargo gates 或 Runtime 05 full `scene::` closeout。验证：rustfmt check、Python py_compile、stale 21/baseline scan clean、standalone plan-status 32/32。 |
| M3 | 3.1 Runtime 14 guard anchors 状态表闭环 | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_index_status_output_records_recent_cross_plan_slices` 现在锁定 runtime 总索引中的 `Runtime 14 Module family guard anchors 审计同步` 行，要求保留 `module_family_guard_anchor_count = 7`、`missing_module_family_guard_anchors = []`、standalone root_entries 13/13 与 standalone status-output 2/2 证据；Runtime 14 `animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates 仍 pending。验证：rustfmt check、Python py_compile、direct `module_family_boundary_audit`、aggregate Runtime 14 + plan-status assertions、standalone root_entries 13/13、standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 status-output status/date helper split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_slices.rs` now owns `expected_status_for_slice` / `expected_date_for_slice`; `status_output_tables.rs` stays as coverage/assertion owner and the plan-status support inventory is now 13/13. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 status-output expected anchor split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_status_rows.rs` now owns `EXPECTED_STATUS_OUTPUT_SLICES`; `status_output_tables.rs` is reduced to traversal/assertion logic while `expected_slices.rs` keeps status/date mapping. Plan-status support inventory is now 21/21 and `plan_status_boundary_line_count = 862`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 plan-status root module split | status_table_static_passed_cargo_pending | 2026-06-15 | `plan_status.rs` now only mounts child modules; `plan_status/support.rs` owns shared helper traversal, `plan_status/index_tables.rs` owns runtime index table guards, `plan_status/subplan_status.rs` owns subplan frontmatter/status evidence guards, and `plan_status/closeout.rs` plus `plan_status/architecture_review.rs` own closeout/review guards. Plan-status support inventory is now 26/26 and `plan_status_boundary_line_count = 867`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone plan-status test list 30 tests；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 plan-status support inventory split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_support_inventory.py` now owns `PLAN_STATUS_SUPPORT_FILES` and `EXPECTED_PLAN_STATUS_SUPPORT_FILE_COUNT`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 842` while direct audit still reports support 26/26、missing support files []、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 plan-status anchor inventory split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_anchor_inventory.py` now owns `CORE_GUARD_ANCHORS` / `PENDING_GATE_ANCHORS` / `DOC_ANCHORS` / `BACKLOG_GAPS`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 789` while direct audit still reports support 26/26、missing support files []、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 plan-status markdown renderer split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_markdown.py` now owns `render_runtime_plan_status_boundary_markdown`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 559` while direct audit still reports support 26/26、missing support files []、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0 and rendered markdown lines > 0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 plan-status markdown direct import hard-cutover | status_table_static_passed_cargo_pending | 2026-06-21 | `audit_runtime_structure.py` now imports `render_runtime_plan_status_boundary_markdown` directly from `runtime_plan_status_markdown.py`; `runtime_plan_status_boundary.py` no longer imports/re-exports that Markdown renderer and remains the 451-line orchestration/audit owner. Direct audit reports support files 79/79、status-output anchor module 145/300 lines、status-output table guard present、Runtime 05 status `in_progress`、status table gaps []、missing support files []、`risks = []`、rendered lines 26. Validation: Python py_compile, direct `runtime_plan_status_boundary_audit`, and standalone `plan_status.rs` 33/33; full `scene::` Cargo closeout gate remains pending. |
| M3 | 3.1 plan-status source helper split | status_table_static_passed_cargo_pending | 2026-06-15 | `runtime_plan_status_sources.py` now owns `read_text` / `runtime_subplans` / `status_rows`; `runtime_plan_status_boundary.py` remains orchestration and now reports `plan_status_boundary_line_count = 454` while direct audit still reports support 26/26、missing support files []、rendered markdown lines 26、`risks = []`。Validation: Python py_compile、direct `runtime_plan_status_boundary_audit` support 26/26 / risks=0；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 status-output expected row data split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_status_row_data.rs` now owns `EXPECTED_STATUS_OUTPUT_SLICES`; `expected_status_rows.rs` is the thin const-alias entrypoint and `runtime_plan_status_boundary.py` now reports plan-status support files 27/27 with `plan_status_boundary_line_count = 454`。Validation: rustfmt、Python py_compile、direct `runtime_plan_status_boundary_audit` support 27/27 / risks=0、wrapped standalone plan-status 30/30；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 status-output row-data group split | status_table_static_passed_cargo_pending | 2026-06-15 | `status_output_tables/expected_status_row_data.rs` is now a grouping entrypoint with `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS`; evidence rows are split into `expected_status_row_data/{runtime_01_04,runtime_05,runtime_06_09,runtime_10_13,runtime_14}.rs`, with `expected_status_output_slices` flattening groups for the existing full coverage guard. `runtime_plan_status_boundary.py` now reports plan-status support files 32/32; the largest data-owner slice is `expected_status_row_data/runtime_05.rs` at 445 lines. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` support 32/32 risks=[]、wrapped standalone plan-status 30/30、conflict/trailing/diff checks 通过（仅 LF/CRLF warning）；full `scene::` Cargo closeout gate 仍 pending。未改 runtime/render/UI/plugin 生产代码。 |
| M3 | 3.1 full scene closeout failed evidence | cargo_recheck_failed_full_scene_gate | 2026-06-15 | Re-ran `cargo test -p zircon_runtime --lib scene:: --locked` after active cargo/rustc lanes cleared; the gate completed with 880 passed / 31 failed / 0 ignored / 3289 filtered out after a 6m07s compile and 32.70s test run. Failures span graphics scene material/readiness/runtime-feature/graph/HZB/post-process/scene-uniform guards plus scene asset/project IO/ECS query/schedule/event/property/render_extract/world_basics guards, while the workspace still has dirty graphics/render/editor owner changes; Runtime 05 therefore remains `pending_full_scene_cargo` and no completed status is promoted. |
| M3 | 3.1 full scene closeout no-result recheck | cargo_recheck_no_result_external_editor_lane | 2026-06-15 | Re-attempted `cargo test -p zircon_runtime --lib scene:: --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-scene-closeout-0615 --message-format short --color never -- --test-threads=1 --nocapture`; the wrapper exited `SCENE_CLOSEOUT_EXIT=-1` after a compile-phase interruption and wrote only dependency compilation output to `.codex/tmp/zircon_runtime_scene_closeout_20260615_1806.log`, so there is `无测试结果`. Immediately afterward the editor `command_palette` lane owned `E:\cargo-targets\zircon-editor-ui-command-registry-0615`; Runtime 05 remains `pending_full_scene_cargo` and no completed status is promoted. |
| M3 | 3.1 Runtime 05 scene:: failure support-first triage | support_first_triage_static_passed_cargo_pending | 2026-06-15 | `runtime_05_full_scene_failure_clusters_keep_support_first_triage_visible` now requires this plan, the runtime index, M0 review, and runtime-interface convergence to keep the 880/31 full-scene failure mapped before owner edits: `graphics-scene-lower-layer-candidate` covers material/readiness/runtime-feature/render-graph/HZB/post-process/scene-uniform shared graphics-scene candidates; `scene-asset-project-io-lower-layer-candidate` covers scene asset cache plus folder-backed project IO serialization/source guards; `ecs-scene-lower-layer-candidate` covers ECS query/cache, schedule/event advancement, property/reflection, render_extract, and world_basics bootstrap candidates. The next action is `support-first-scene-closeout-triage-before-owner-edits`; no runtime/render/editor/plugin production owner file changed, and full `scene::` Cargo closeout remains pending. |
| M3 | 3.1 Runtime 05 scene:: lower-layer diagnostic matrix | support_first_matrix_static_passed_cargo_pending | 2026-06-15 | `runtime_05_scene_failure_triage_records_minimum_lower_layer_diagnostics` records the `support-first-minimal-diagnostics-matrix` before owner edits. `graphics-scene-diagnostics` starts from `render_product_streamer_tests`, `render_product_material_property_tests`, `render_pass_executor_registry`, and `deferred_lighting_shader_matches_scene_uniform_layout`; `scene-asset-project-io-diagnostics` starts from `scene_asset_toml_roundtrip_preserves_entities_and_bindings`, `scene_assets_roundtrip_primitive_mesh_material_bindings`, and `scene_project_serialization_sources_do_not_store_editor_authoring_state`; `ecs-scene-diagnostics` starts from `ecs_query`, `first_stage_updates_all_registered_event_channels`, `render_extract_prepare_flushes_parent_reorder_and_active_changes`, `world_bootstraps_with_renderable_defaults`, and `world_resolves_entity_paths_and_mutates_component_properties`. Next Cargo window should run `cargo test -p zircon_runtime --lib render_product_streamer_tests --locked`, `cargo test -p zircon_runtime --lib scene_asset --locked`, and `cargo test -p zircon_runtime --lib ecs_query --locked` before changing render/editor/runtime owner files. |
| M3 | 3.1 Runtime 05 scene:: diagnostic matrix source anchors | support_first_matrix_source_anchors_static_passed_cargo_pending | 2026-06-20 | `runtime_05_scene_failure_diagnostic_matrix_source_anchors_exist` now verifies the support-first matrix against current source anchors, not only plan text: graphics diagnostics keep `render_product_streamer_tests`, `render_product_material_property_tests`, `render_pass_executor_registry`, and `deferred_lighting_shader_matches_scene_uniform_layout` reachable from graphics scene sources; scene-asset/project-IO diagnostics keep `scene_asset_toml_roundtrip_preserves_entities_and_bindings`, `scene_assets_roundtrip_primitive_mesh_material_bindings`, and `scene_project_serialization_sources_do_not_store_editor_authoring_state`; ECS diagnostics keep `ecs_query`, `first_stage_updates_all_registered_event_channels`, `render_extract_prepare_flushes_parent_reorder_and_active_changes`, `world_bootstraps_with_renderable_defaults`, and `world_resolves_entity_paths_and_mutates_component_properties`. This is a source-guard/status slice only; no render/editor/runtime production owner behavior changed, and full `scene::` Cargo closeout remains pending. Validation passed: rustfmt touched guard/status files; standalone `plan_status.rs` 33/33; direct `runtime_plan_status_boundary_audit` `risks=[]` / `status_table_gaps=[]`; conflict-marker and trailing-whitespace scans clean; scoped `git diff --check` only LF/CRLF warnings. |
| M3 | 3.1 serialization source folder-split guard sync | source_guard_static_passed_cargo_deferred_active_lanes | 2026-06-15 | `scene_project_serialization_sources_do_not_store_editor_authoring_state` now scans the current folder-backed serialization sources: `src/asset/assets/scene/mod.rs` and `src/scene/world/project_io/{camera,physics,post_process,references,script,transform}.rs` in addition to the `project_io.rs` entry owner, using `SOURCE_AUTHORING_TOKENS` to keep editor authoring state out of runtime scene/project serialization. Validation: rustfmt check、standalone component_structure 20/20、Python py_compile、direct `scene_project_serialization_boundary_audit` file_count=13 forbidden_location_count=0 risks=[]、direct `runtime_plan_status_boundary_audit` support 27/27 risks=[]、wrapped standalone plan-status 30/30；production serialization code was not changed, and Cargo package recheck is deferred while active editor/export lanes are compiling. |
| M1 | 1.1 runtime scene/editor surface Markdown renderer split | runtime_scene_editor_surface_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `runtime_scene_editor_surface_markdown_split_static_passed_cargo_deferred_tests_deferred`；`runtime_scene_editor_surface.py` now only owns editor-named path/location scanning and risk aggregation at 92 lines, while `runtime_scene_editor_surface_markdown.py` owns `render_runtime_scene_editor_surface_markdown` at 25 lines. Direct audit reports `editor_named_paths = 0`, `public_editor_named_locations = 0`, `editor_named_locations_sample = 0`, `risks = []`, and `rendered_lines = 3`, preserving the neutral `scene::inspection` boundary without changing runtime/editor production code. Validation: Python py_compile, direct `runtime_scene_editor_surface_audit`, standalone `component_structure.rs` 25/25, standalone `plan_status.rs` 33/33; full `scene::` Cargo gate remains deferred and is not promoted by this audit-only slice. |
| M1 | 1.1 editor_projection residual guard verdict | editor_projection_residual_guard_static_passed_cargo_pending | 2026-06-20 | `scene/tests/component_structure.rs` now documents that the retired editor-projection module name only as a resurrection guard; `scene_components_keep_only_runtime_world_domains_after_editor_boundary_cutover` and `runtime_scene_exposes_neutral_world_inspection_surface` keep the directory absent and prevent the neutral inspection public surface from reintroducing `SceneEditor` / `editor_projection`. Grep verdict: editor_projection text remains guard-owned. Validation: rustfmt check, standalone component_structure 20/20, standalone plan-status 32/32; full `scene::` Cargo closeout gate remains pending. |
| M3 | 3.1 cargo-gates early Runtime 08 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_08.rs` now owns `runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`; `cargo_gates/early.rs` is reduced to 795 lines and keeps the Runtime 01/02/03/04/06/07 early-gate owners plus the Runtime 08 include. Plan-status support inventory is now 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates early Runtime 07 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_07.rs` now owns `runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation`; `cargo_gates/early.rs` is reduced to 660 lines and keeps the Runtime 01/02/03/04/06 early-gate owners plus Runtime 07/08 includes. Plan-status support inventory is now 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 07 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates early Runtime 03 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_03.rs` now owns `runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation`; `cargo_gates/early.rs` is reduced to 246 lines and keeps the Runtime 04/06 early-gate owners plus Runtime 01/02/03/07/08 includes. Plan-status support inventory is now 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 03 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates early Runtime 01 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_01.rs` now owns `runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation`; `cargo_gates/early.rs` is reduced to 246 lines and keeps the Runtime 04/06 early-gate owners plus Runtime 01/02/03/07/08 includes. Plan-status support inventory is now 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 01 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates early Runtime 02 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_02.rs` now owns `runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation`; `cargo_gates/early.rs` is reduced to 246 lines and keeps the Runtime 04/06 early-gate owners plus Runtime 01/02/03/07/08 includes. Plan-status support inventory is now 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 02 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates early Runtime 04 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_04.rs` now owns `runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation`; `cargo_gates/early.rs` now includes Runtime 01/02/03/04/06/07/08 children with no inline early-gate owner bodies. Plan-status support inventory is now 20/20 and `plan_status_boundary_line_count = 861`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 04 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates early Runtime 06 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/early/runtime_06.rs` now owns `runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation`; `cargo_gates/early.rs` now includes Runtime 01/02/03/04/06/07/08 children with no inline early-gate owner bodies. Plan-status support inventory is now 20/20 and `plan_status_boundary_line_count = 861`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 06 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates late Runtime 10 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_10.rs` now owns `runtime_10_m1_3_cargo_pending_gate_stays_explicit_until_dynamic_api_validation` and `runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff`; `cargo_gates/late.rs` is reduced to 388 lines and keeps the Runtime 11/12/13/14 late-gate owners plus the Runtime 10 include. Plan-status support inventory is now 14/14. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 10 cargo-gates 2/2；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates late Runtime 11 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_11.rs` now owns `runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass`; `cargo_gates/late.rs` is reduced to 232 lines and keeps the Runtime 12/13/14 late-gate owners plus Runtime 10/11 includes. Plan-status support inventory is now 15/15 and `plan_status_boundary_line_count = 856`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 11 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates late Runtime 12 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_12.rs` now owns `runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation`; `cargo_gates/late.rs` is reduced to 148 lines and keeps the Runtime 13/14 late-gate owners plus Runtime 10/11/12 includes. Plan-status support inventory is now 16/16 and `plan_status_boundary_line_count = 857`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 12 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates late Runtime 13 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_13.rs` now owns `runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass`; `cargo_gates/late.rs` now includes Runtime 10/11/12/13/14 children with no inline late-gate owner bodies. Plan-status support inventory is now 18/18 and `plan_status_boundary_line_count = 859`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 13 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.1 cargo-gates late Runtime 14 split | status_table_static_passed_cargo_pending | 2026-06-15 | `cargo_gates/late/runtime_14.rs` now owns `runtime_14_module_family_cargo_gate_stays_visible_until_filters_pass`; `cargo_gates/late.rs` now includes Runtime 10/11/12/13/14 children with no inline late-gate owner bodies. Plan-status support inventory is now 18/18 and `plan_status_boundary_line_count = 859`. Validation: rustfmt check、Python py_compile、direct `runtime_plan_status_boundary_audit` risks=0、wrapped standalone status-output 2/2、wrapped standalone Runtime 14 cargo-gate 1/1；full `scene::` Cargo closeout gate 仍 pending。 |
| M3 | 3.2 状态闭环 | pending_full_scene_cargo | — | 本计划 frontmatter 从 `completed` 修正为 `in_progress`，因为计划 DoD 要求 `cargo test -p zircon_runtime --lib scene:: --locked` 全族无回归后才能最终关账；当前只有 `naming_boundary`、`authoring`、`inspection` scoped Cargo 通过记录。新增 `runtime_plan_last_refined_covers_latest_recorded_date`、`runtime_plan_status_does_not_claim_completed_while_validation_is_pending`、`runtime_plan_frontmatter_status_uses_known_lifecycle_values`、`runtime_index_status_map_matches_subplan_frontmatter`、`runtime_index_subplan_map_covers_existing_plan_files_without_stale_rows`、`runtime_index_problem_rows_reference_existing_subplans`、`runtime_index_execution_dependencies_reference_existing_subplans`、`runtime_index_in_progress_rows_record_remaining_gate`、`runtime_known_backlog_gaps_keep_owner_and_trigger_columns`、`runtime_subplans_keep_status_and_evidence_tables`、`runtime_subplan_status_records_keep_non_empty_evidence`、`runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs`、`runtime_01_tech_stack_cargo_gate_stays_visible_until_dependency_validation`、`runtime_02_core_spine_root_surface_cargo_gate_stays_visible_until_validation`、`runtime_03_schedule_frame_loop_cargo_gate_stays_visible_until_schedule_validation`、`runtime_04_asset_pipeline_cargo_gate_stays_visible_until_asset_validation`、`runtime_06_plugin_surface_lifecycle_gate_stays_visible_until_plugin_validation`、`runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation`、`runtime_08_ecs_kernel_cargo_pending_gate_stays_explicit_until_ecs_validation`、`runtime_09_ui_architecture_cargo_gate_stays_visible_until_ui_owner_validation`、`runtime_10_m1_3_cargo_pending_gate_stays_explicit_until_dynamic_api_validation`、`runtime_10_ui_contract_m2_gate_stays_pending_until_runtime_09_owner_handoff`、`runtime_11_job_system_cargo_gate_stays_visible_until_job_system_filters_pass`、`runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation`、`runtime_13_script_binding_cargo_gate_stays_visible_until_script_filters_pass`、`runtime_14_module_family_cargo_gate_stays_visible_until_filters_pass`、`runtime_architecture_review_documents_all_absorption_guards` 与 `runtime_05_closeout_status_waits_for_full_scene_cargo_gate`，防止待验证计划误标 completed，并锁定 last_refined 覆盖最新状态日期、总表状态必须跟随子计划 frontmatter、总索引子计划地图必须精确覆盖 01-14 子计划文件、问题清单与执行依赖列必须指向现有子计划、所有 in_progress 总表行必须说明剩余 gate 或 blocker、总索引已知 backlog 缺口必须保留现状依据与 owner/触发条件、所有子计划必须保留状态/证据表且已启动记录必须写入具体证据、Runtime 01-14 近期静态/待验证守卫必须同步到计划/镜像文档/评审/索引（Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` gate 必须保持可见，Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` gate 必须保持可见，Runtime 09 `ui/input/naming_boundary/layout/template` owner/Cargo gate 必须保持可见，Runtime 07 覆盖 `runtime_07_hotspot_inventory_requires_counted_evidence_before_m2`、`runtime_frame_schedule_stage.<SystemStage>` 与 `runtime_07_performance_hotpath_cargo_gate_stays_visible_until_performance_validation`，Runtime 10 同时覆盖 `runtime_10_headless_profiles_keep_render_bridge_optional_and_noop_surfaces`、`runtime_10_ffi_panic_boundary_keeps_exports_as_only_c_abi_edge` 与 Runtime 10 UI 镜像契约 M2 owner/Cargo gate，且 M1.3 在 `dynamic_api` Cargo 通过前保持 `code_static_passed_cargo_pending`，M2 在 Runtime 09/editor UI owner 交接与 interface/ui/editor Cargo 通过前保持 pending，Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 必须保持可见，Runtime 12 在 input/action_map/gamepad/app Cargo 通过前保持 pending gate 可见，Runtime 13 在 script filters Cargo 通过前保持 `code_static_pending_cargo`，Runtime 14 模块族守卫必须覆盖四族镜像文档，Runtime 05 closeout 必须保持 `pending_full_scene_cargo`）、Runtime 01 `tech_stack/text_shaper/plugin physics` Cargo gate、Runtime 02 `core/root/generated/export_build_plan/app/editor/plugin` gate、Runtime 03 `ecs_schedule/time/session/schedule_parallel` Cargo gate、Runtime 04 broader `asset::` / `worker_pool` Cargo gate、Runtime 06 `script::vm/vampire_project_session/plugin/native_plugin/app/plugins` Cargo/native gate、Runtime 07 `extract/ecs_query/performance profiling/FPS` Cargo gate、Runtime 08 ECS 数据面 Cargo gate、Runtime 09 `ui/input/naming_boundary/layout/template` owner/Cargo gate、Runtime 10 UI 镜像契约 M2 owner/Cargo gate、Runtime 05 `scene::` Cargo closeout gate、Runtime 11 `tasks/ecs_schedule/worker_pool/rayon` Cargo gate 与 Runtime 14 模块族 Cargo/rustc gate 必须保持可见，以及 M0 评审必须覆盖 `runtime_absorption/mod.rs` 和所有已挂载 guard。 |
| M3 | 3.1 runtime session archive | runtime_session_archive_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `zircon_runtime/src/scene/dynamic_scene/session.rs` 新增 `RuntimeSessionArchive` / `RuntimeSessionSlot` / `RuntimeSessionMetadata` / `RuntimeSessionArchiveError`，支持 world/level 捕获、slot upsert/remove、pretty JSON load/save、slot apply 与 empty-world restore；`scene::dynamic_scene` 和 `scene` 出口已公开类型，`scene/tests/dynamic_scene.rs` 写入 roundtrip/duplicate-slot 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive manifest | runtime_session_archive_manifest_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveManifest` / `RuntimeSessionSlotSummary` 已加入 `zircon_runtime/src/scene/dynamic_scene/session.rs` 并从 `scene::dynamic_scene`、`scene` 出口公开；archive 现在支持 `slot_count` / `is_empty` / `contains_slot` / `manifest`，manifest 提供稳定排序 slot id、metadata、scene format version、entity/resource counts，供运行时存档选择逻辑读取而不恢复世界；`scene/tests/dynamic_scene.rs` 写入 sorted manifest 与 upsert replacement 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive level restore | runtime_session_archive_level_restore_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionLevelRestoreReport`、`RuntimeSessionArchive::restore_slot_into_level` 与 `RuntimeSessionSlot::restore_into_level` 已加入 session API，`LevelSystem::replace_world_and_reset_runtime_state` 提供安全替换入口；slot restore 现在可直接替换运行时 level world、回写 `LevelMetadata`、清理旧 physics/animation/script runtime cache，并返回 slot/entity restore report；`scene/tests/dynamic_scene.rs` 写入 direct LevelSystem restore 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive level apply | runtime_session_archive_level_apply_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::apply_slot_to_level` 与 `RuntimeSessionSlot::apply_to_level` 已加入 session API，live `LevelSystem` 可直接接收 slot scene instancing，保持现有实体并返回 `EntityRemap` 处理 source id collision；`scene/tests/dynamic_scene.rs` 写入 live-level apply/remap 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive embedded scene validation | runtime_session_archive_scene_validation_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `DynamicScene::ensure_supported` 现在作为公开共享校验入口覆盖 format version 与 duplicate source entity，`spawn_into`、`from_versioned_json` 与 `RuntimeSessionArchive::ensure_supported` 均复用该入口；session archive load/save/manifest 现在会在归档层拒绝 unsupported embedded dynamic scene version，而不是等到 apply/restore 时才失败；`scene/tests/dynamic_scene.rs` 写入 unsupported embedded scene version 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive slot write validation | runtime_session_archive_slot_write_validation_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::push_slot` 与 `upsert_slot` 现在会在写入 archive 前调用 embedded `DynamicScene::ensure_supported`，直接拒绝 unsupported slot scene version / duplicate source entity 等坏 payload，避免 manual slot insert 先污染 archive、后续 manifest/save/apply 才失败；`scene/tests/dynamic_scene.rs` 写入 push/upsert unsupported scene version 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive canonical slot validation | runtime_session_archive_slot_id_validation_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveError::NonCanonicalSlotId` 已加入 session archive 错误面，`from_slots` / `push_slot` / `upsert_slot` / `from_versioned_json` 现在拒绝带首尾空白的 persisted/manual slot id；capture helpers 仍会先 normalize 用户输入，保证写入归档的 slot id 为 canonical；`scene/tests/dynamic_scene.rs` 写入 manual slot 与 JSON load 两条 non-canonical slot id 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive deterministic write order | runtime_session_archive_write_order_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::to_versioned_json_pretty` 现在 clone 并按 slot id 排序后再校验/序列化，保证手工构造的 archive 也输出 canonical slot order，且不改变调用方内存中的 `slots` Vec；`scene/tests/dynamic_scene.rs` 写入 manual unsorted slots pretty JSON 顺序行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive metadata tag normalization | runtime_session_archive_metadata_tags_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionMetadata::normalize` / `normalized` 已加入 session API，metadata tags 现在在 capture、from_slots、push/upsert、JSON load、manifest 与 pretty JSON 输出路径被 trim、drop empty、sort、dedup，保证手工构造和持久化 archive 的 manifest/JSON 稳定；`scene/tests/dynamic_scene.rs` 写入 manifest 与 JSON 双路径 tag normalization 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive slot management | runtime_session_archive_slot_management_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::rename_slot` 与 `update_slot_metadata` 已加入 session API，运行时 save selector 可在不恢复世界的情况下重命名 slot、更新 metadata，并继续复用 slot id normalize、duplicate rejection、排序与 tag normalization 规则；直接 mutable slot lookup 保持 private helper，不作为 public session API；`scene/tests/dynamic_scene.rs` 写入 rename/sorted ids/metadata update/duplicate rename 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive mutation surface guard | runtime_session_archive_mutation_surface_guard_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::slot_mut` 已收束为 private helper 并加不变量注释，避免外部绕过 slot id 排序、duplicate checks 或 metadata normalization；public mutation surface 只保留 `rename_slot` / `update_slot_metadata` / capture upsert 等 archive-owned 方法；`scene/tests/dynamic_scene.rs` 写入 `runtime_session_archive_keeps_slot_mutation_surface_guarded` 源码结构锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive diff preview | runtime_session_archive_diff_preview_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionSlotDiffReport`、`RuntimeSessionSlot::diff_world` / `diff_level` 与 `RuntimeSessionArchive::diff_slot_with_world` / `diff_slot_with_level` 已加入 session API，运行时 save/load 选择面可在覆盖或恢复前只读比较 slot snapshot 与当前 World/LevelSystem，并得到 match flag、slot/target entity/resource counts；`scene::dynamic_scene` 与 `scene` 出口公开新 report 类型；`scene/tests/dynamic_scene.rs` 写入 same-world、changed-world 与 missing-slot diff 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive slot copy | runtime_session_archive_slot_copy_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::copy_slot` 与 `copy_slot_with_metadata` 已加入 session API，运行时 save selector 可在不恢复世界的情况下复制 slot 或创建 save-as slot；复制路径复用 archive-owned `push_slot`，继续覆盖 destination slot id normalization、duplicate rejection、canonical ordering 与 metadata tag normalization，且 missing source slot 返回 `MissingSlot`；`scene/tests/dynamic_scene.rs` 写入 metadata-preserving copy、metadata override、duplicate destination 与 missing-source 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive merge import | runtime_session_archive_merge_import_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveMergePolicy`、`RuntimeSessionArchiveMergeReport` 与 `RuntimeSessionArchive::merge_archive` 已加入 session API，并从 `scene::dynamic_scene` / `scene` 出口公开；archive import 现在支持 `RejectConflicts`、`KeepExisting`、`ReplaceExisting` 三种 duplicate slot policy，reject 模式先扫描冲突避免 partial merge，keep/replace 模式返回 inserted/replaced/skipped slot id report；`scene/tests/dynamic_scene.rs` 写入 reject 原子性、keep existing、replace existing 与 canonical ordering 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive retention prune | runtime_session_archive_retention_prune_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveRetentionPolicy`、`RuntimeSessionArchivePruneReport` 与 `RuntimeSessionArchive::prune_slots` 已加入 session API，并从 `scene::dynamic_scene` / `scene` 出口公开；archive retention 现在可按 `max_slots` 保留最新 `updated_at_unix_millis` slot，同时保护指定 slot id 不被自动清理，缺少更新时间的 slot 作为最旧候选，report 返回 retained/removed slot ids；`scene/tests/dynamic_scene.rs` 写入 protected slot、newest timestamp retention、untimed-oldest 与 no-limit no-op 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive statistics | runtime_session_archive_statistics_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveStatistics` 与 `RuntimeSessionArchive::statistics` 已加入 session API，并从 `scene::dynamic_scene` / `scene` 出口公开；archive diagnostics 现在可在不 restore/apply world 的情况下读取 slot_count、total/max entity/resource counts、earliest/latest `updated_at_unix_millis` 与 untimed slot count，供 retention、telemetry 或 runtime diagnostics 使用；`scene/tests/dynamic_scene.rs` 写入 multi-slot totals、timestamp range、untimed count 与 empty archive statistics 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive manifest filters | runtime_session_archive_manifest_filters_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveManifest::slots_with_tag` 与 `slots_matching_display_name` 已加入 manifest API，运行时 save selector 可基于 normalized slot summary 按 tag 或 display-name substring 搜索存档槽，不需要 restore/apply world；查询保持 manifest 的 canonical slot ordering，tag 查询裁剪输入空白，display-name 空查询不匹配；`scene/tests/dynamic_scene.rs` 写入 tag/display-name filter 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive slot selection | runtime_session_archive_slot_selection_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveManifest::latest_updated_slot` / `oldest_updated_slot` 与 `RuntimeSessionArchive::latest_updated_slot_id` / `oldest_updated_slot_id` 已加入 session API，运行时 save/load selector 可在 archive validation 后按 `updated_at_unix_millis` 选择默认 slot，不需要 restore/apply world；缺少更新时间的 slot 作为最旧候选，更新时间相同按 slot id 稳定裁决；`scene/tests/dynamic_scene.rs` 写入 latest/oldest selection、tie-break 与 empty archive 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive slot touch | runtime_session_archive_slot_touch_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchive::touch_slot` 已加入 session API，运行时 quicksave/autosave 流可只刷新 `updated_at_unix_millis`，不需要替换 world snapshot、display name、tags 或其它 slot metadata；该方法继续通过 private `slot_mut` 受控 mutation surface，并对 missing slot 返回 `MissingSlot`；`scene/tests/dynamic_scene.rs` 写入 metadata preservation、latest selection update 与 missing-slot error 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive tagged slot selection | runtime_session_archive_tagged_slot_selection_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveManifest::latest_updated_slot_with_tag` / `oldest_updated_slot_with_tag` 与 `RuntimeSessionArchive::latest_updated_slot_id_with_tag` / `oldest_updated_slot_id_with_tag` 已加入 session API，运行时 save/load selector 可在 archive validation 后按 normalized tag bucket 选择最新或最旧 slot，不需要 restore/apply world；tag 查询裁剪输入空白，空 tag 不匹配，缺少更新时间的 tagged slot 仍作为最旧候选，更新时间相同按 slot id 稳定裁决；`scene/tests/dynamic_scene.rs` 写入 tagged latest/oldest selection、tie-break、trimmed tag、empty/missing tag 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive owner module split | runtime_session_archive_owner_module_split_code_rustfmt_passed_cargo_deferred | 2026-06-18 | `RuntimeSessionArchiveManifest` / `RuntimeSessionSlotSummary` 与 manifest query/selection helpers 已从 `session.rs` 拆入 `session/manifest.rs`，`RuntimeSessionArchiveError` 已拆入 `session/error.rs`；`session.rs` 继续 re-export 相同公开类型，并保持 archive persistence、slot mutation、apply/restore、validation、report owner；`scene::dynamic_scene` / `scene` public re-export surface 未变；`docs/zircon_runtime/scene/dynamic_scene.md` 已同步。验证：rustfmt touched Rust 已执行；Cargo/测试按本轮“首先实现功能，测试可以暂时延后”要求 deferred，Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 runtime session archive tagged retention prune | runtime_session_archive_tagged_retention_prune_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchiveRetentionPolicy` / `RuntimeSessionArchivePruneReport` 与 prune algorithms 已从 `session.rs` 拆入 `session/retention.rs`；`RuntimeSessionArchive::prune_slots_with_tag` 已加入 session API，运行时 autosave/quicksave cleanup 可只裁剪一个 normalized tag bucket，并保留 bucket 外 slot 与 policy protected slot；`scene/tests/dynamic_scene_session.rs` 新增 tag bucket prune 与 empty tag no-op 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；`cargo check -p zircon_runtime --lib --locked --jobs 1 --message-format short` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive retention preview | runtime_session_archive_retention_preview_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::preview_prune_slots` 与 `preview_prune_slots_with_tag` 已加入 session API，运行时 save selector 可在实际删除前读取 retained/removed slot report；实际 prune 复用同一 retention 计算并应用删除，保证 dry-run 与 commit 结果一致；`scene/tests/dynamic_scene_session.rs` 新增全局 retention preview no-mutation 与 tag bucket preview/commit 一致性行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；`cargo check -p zircon_runtime --lib --locked --jobs 1 --message-format short` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive atomic save | runtime_session_archive_atomic_save_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::save_to_path_atomically` 已加入 session API，archive file I/O 已从 `session.rs` 拆入 `session/io.rs`；atomic save 先写同目录 temporary JSON，目标已存在时在 temporary payload 写完后再替换目标，并在 rename 失败时清理 temporary file；`scene/tests/dynamic_scene_session.rs` 新增 nested-path atomic save reload 与 temporary cleanup 行为锚点；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 已执行；`cargo check -p zircon_runtime --lib --locked --jobs 1 --message-format short` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive atomic save existing-target rollback | runtime_session_archive_atomic_save_existing_target_rollback_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::save_to_path_atomically` 覆盖已有 archive 时现在先把旧目标移动到同目录 backup，再把 temporary archive rename 到目标路径；commit 成功后删除 backup，commit rename 失败时清理 temporary file 并尝试把 backup 恢复到目标路径，非文件目标在移动旧目标前拒绝；temporary/backup 文件名加入 process id、时间戳与计数器以降低同目录碰撞。`scene/tests/dynamic_scene_session.rs` 新增 existing-target replace 行为锚点，检查替换后 archive 可重载且无 `.tmp` / `.bak` 残留；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --message-format short` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive manifest path loading | runtime_session_archive_manifest_path_loading_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchive::load_manifest_from_path` 已加入 session API，并由 `session/io.rs` 复用现有 `load_from_path` 校验后返回 manifest；运行时 save/load selector 可直接从持久化 archive 文件读取 validated slot index、tag filter 与 latest/oldest selection surface，不需要调用方手写 `load_from_path(...).manifest()`。`scene/tests/dynamic_scene_session.rs` 新增 manifest-from-path 行为锚点，覆盖 atomically saved archive 的 tag filter 与 tagged latest selection；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --message-format short` 通过（现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 runtime session archive path status inspection | runtime_session_archive_path_status_inspection_code_check_passed_tests_deferred | 2026-06-18 | `RuntimeSessionArchivePathStatus` 与 `RuntimeSessionArchive::inspect_path` 已加入 session API，并从 `scene::dynamic_scene` / `scene` public surface 导出；运行时 startup/recent-session flow 可把缺失 archive 识别为 `Missing`，把可读且校验通过的 archive 识别为 `Available { manifest }`，把存在但不可读、JSON 损坏或 archive 校验失败的文件识别为 `Invalid { error }`，从而区分“没有存档”和“存档损坏”。`scene/tests/dynamic_scene_session.rs` 新增 path-status 行为锚点，覆盖 missing/available/invalid 三态；`docs/zircon_runtime/scene/dynamic_scene.md` 与 runtime index backlog 已同步。验证：rustfmt touched Rust 与 `rustfmt --check` 通过；冲突标记/尾随空白扫描通过；`git diff --check` 仅报告既有 LF->CRLF 文档/结构文件提示；`cargo check -p zircon_runtime --lib --locked --jobs 1 --message-format short` 通过（等待 build directory lock 后完成，现有 warning only）；单元测试与 Runtime 05 full `scene::` gate 仍按“首先实现功能，测试可以暂时延后”要求 deferred。 |
| M3 | 3.1 export build-plan contract stale root import cleanup | export_build_plan_contract_stale_root_import_contract_check_passed_full_tests_blocked_external_vg_snapshot | 2026-06-18 | Runtime 05 会话存档切片后补跑 `cargo check -p zircon_runtime --lib --tests --locked --jobs 1 --message-format short` 暴露 `zircon_runtime/tests/export_build_plan_contract.rs` 仍从 crate root 导入 `RuntimePluginId` / `RuntimeTargetMode`；根因定位显示类型当前 owner 为 `zircon_runtime::builtin`（经 `builtin/runtime_modules/ids` 导出），crate root 不应新增兼容 re-export。该测试已改为直接 `use zircon_runtime::builtin::{RuntimePluginId, RuntimeTargetMode};`，保持 `zircon_runtime/src/lib.rs` root surface 不扩张。验证：`rustfmt --edition 2021 --check zircon_runtime\tests\export_build_plan_contract.rs` 通过；冲突标记/尾随空白扫描通过；scoped `git diff --check` 仅报告既有 LF->CRLF 提示；旧根入口路径扫描报告 no stale RuntimePluginId/RuntimeTargetMode root paths；`cargo check -p zircon_runtime --test export_build_plan_contract --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 通过（现有 warning only）。复跑 `cargo check -p zircon_runtime --lib --tests --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-session-archive-tests-0618 --message-format short --color never` 已越过该旧导入错误，但被 `zircon_runtime/tests/virtual_geometry_debug_snapshot_contract.rs` 的 `RenderMeshSnapshot` 缺少 `mesh_lod` / `stable_instance_key` / `static_state` 等字段漂移阻塞；Runtime 05 full `scene::` gate 仍 pending。 |
| M3 | 3.1 dynamic scene slot selector behavior anchors | runtime_05_dynamic_scene_slot_selector_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending | 2026-06-20 | Added `zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` and mounted it from `dynamic_scene_session/mod.rs` to cover the typed `RuntimeSessionSlotSelector` save/load selector surface that was previously only structurally present. New behavior anchors cover in-memory selector resolution, normalized tagged selector reports, explicit missing-slot labels, selected retention protection, selected metadata update targeting, path-level `select_slot_from_path`, and atomic `remove_selected_slot_at_path_atomically` without temp/backup leftovers. This is a test/coverage slice only: public `RuntimeSessionArchive` method names, archive JSON format, selected copy/import/export/restore behavior, gameplay save schema, and editor/plugin/render/UI behavior remain unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` passed; focused Cargo was not started because another Cargo lane was active, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene selected transfer behavior anchors | runtime_05_dynamic_scene_selected_transfer_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending | 2026-06-20 | Extended `zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` with selected transfer/export behavior anchors. The new tests cover `copy_selected_slot`, `selected_single_slot_archive`, `import_selected_slot_from_archive_with_metadata`, and `save_selected_single_slot_archive_to_path_atomically`, proving selector resolution happens inside the facade, source archives remain unmutated where expected, metadata overrides normalize, and atomic selected export leaves no `.tmp` / `.bak` leftovers. This remains a test/coverage slice only; selected transfer public method names, archive format, target-path preflight, and copy/import/export behavior owners are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` passed; focused Cargo was deferred because another Cargo lane was active, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene selected restore/apply/diff behavior anchors | runtime_05_dynamic_scene_selected_restore_apply_diff_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending | 2026-06-20 | Extended `zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` with in-memory and path-level selected restore/apply/diff behavior anchors. The new tests cover `restore_selected_slot_to_empty_world`, `restore_selected_slot_into_level`, `apply_selected_slot`, `apply_selected_slot_to_level`, `diff_selected_slot_with_world`, `diff_selected_slot_with_level`, and the matching `*_from_path_*` selected APIs, proving selector resolution happens before world/level mutation, latest/oldest tag selection reaches the intended slot, level restore replaces stale world state and applies metadata, apply remaps source entity ids on live-target conflicts while preserving existing target entities, and diff reports the resolved slot id plus matching entity counts. This remains a test/coverage slice only; selected restore/apply/diff public method names, archive format, path load behavior, and world mutation semantics are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` passed; focused Cargo was deferred because another Cargo/rustc lane was active, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene selected capture behavior anchors | runtime_05_dynamic_scene_selected_capture_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending | 2026-06-20 | Extended `zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs` with selected capture behavior anchors. The new tests cover in-memory selected world capture preview/commit against the latest tagged slot, metadata-preserving selected capture against the oldest tagged slot, and path-level selected world capture with tag retention preview plus rollback-protected atomic commit. The anchors prove selector resolution happens inside the selected capture facade, preview leaves archives and files unchanged, metadata overrides normalize, metadata-preserving replacement keeps existing display/tag/timestamp values, tag retention protects the captured slot while pruning tag peers, and atomic path commit leaves no `.tmp` / `.bak` leftovers. This remains a test/coverage slice only; selected capture public method names, archive format, path load/save flow, and capture-retention behavior owners are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` passed; focused Cargo was deferred because another Cargo lane was active, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene merge behavior anchors | runtime_05_dynamic_scene_merge_behavior_anchors_rustfmt_passed_cargo_deferred_scene_gate_pending | 2026-06-20 | Added `zircon_runtime/src/scene/tests/dynamic_scene_session/merge.rs` and mounted it from `dynamic_scene_session/mod.rs` so session archive merge behavior has a dedicated owner instead of adding more cases to the near-threshold `path_management.rs`. New anchors cover archive-level merge preview and `KeepExisting` commit side-effect boundaries, loaded-incoming path merge preview without writing the target archive, source-path atomic merge with `ReplaceExisting`, same-source/target path preflight rejection for preview and commit, preserved source archive state, replaced metadata/entity counts, and no `.tmp` / `.bak` leftovers. This remains a test/coverage slice only; public merge method names, archive format, target-path preflight, and merge/path-merge behavior owners are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_session/merge.rs zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` passed; focused Cargo was deferred per implementation-slice cadence, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene mutation behavior anchors | runtime_05_dynamic_scene_mutation_behavior_anchors_rustfmt_passed_focused_cargo_timeout_no_result_scene_gate_pending | 2026-06-20 | Added `zircon_runtime/src/scene/tests/dynamic_scene_session/mutation.rs` and mounted it from `dynamic_scene_session/mod.rs` so runtime session archive mutation behavior has a dedicated owner without expanding the near-threshold `path_management.rs`. New anchors cover named in-memory rename/metadata/touch/remove preview and commit boundaries, duplicate rename rollback, selected rename/metadata/touch/remove selector resolution, selected path-level preview no-write behavior, atomic selected path commits, rejected duplicate path rename preservation, persisted archive reload, and no `.tmp` / `.bak` leftovers. This remains a test/coverage slice only; public mutation method names, archive format, selector semantics, path load/save flow, and mutation behavior owners are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_session/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_session/mutation.rs zircon_runtime/src/scene/tests/dynamic_scene_session/merge.rs zircon_runtime/src/scene/tests/dynamic_scene_session/capture.rs zircon_runtime/src/scene/tests/dynamic_scene_session/selection.rs` passed; focused `cargo test -p zircon_runtime --lib scene::tests::dynamic_scene_session::mutation --locked --jobs 1 --message-format short --color never` timed out after 304s with no test result, and residual Cargo/rustc processes were stopped. Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene asset reload behavior anchors | runtime_05_dynamic_scene_asset_reload_behavior_anchors_rustfmt_passed_cargo_deferred_active_cargo_lane_scene_gate_pending | 2026-06-20 | Added `zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs` and mounted it from `scene/tests/mod.rs` so scene-asset hot reload queue behavior has a dedicated test owner instead of expanding the large `dynamic_scene.rs`. New anchors use a real temporary `ProjectManager` fixture plus typed `ResourceManager` asset events to cover newer `Modified` scene revisions superseding older pending preparations, `DynamicSceneAssetReloadReadyReport` collecting the latest ready payload, caller-thread `spawn_ready_into` applying the prepared scene into an explicit `World`, and `ReloadFailed` / `Removed` events being skipped without scheduling or leaving pending work. This remains a test/coverage slice only; public asset reload method names, queue ownership, scene-asset artifact loading, asset watcher behavior, and live-world commit policy are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/mod.rs zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs` passed; focused Cargo was deferred because an active Cargo lane was already running, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene asset reload frame entrypoint anchors | runtime_05_dynamic_scene_asset_reload_frame_entrypoint_anchors_rustfmt_passed_cargo_deferred_active_cargo_lane_scene_gate_pending | 2026-06-20 | Extended `zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs` with frame-entry behavior anchors for `DynamicSceneAssetReloadQueue::tick_into` and `tick_into_level`. New anchors drain one typed scene event into a pending reload, wait for the scheduled preparation through `JobScheduler::wait_all`, then verify `tick_into` applies the ready payload to an explicit `World` and `tick_into_level` applies through `LevelSystem::with_world_mut` while preserving `DynamicSceneAssetReloadFrameApplyReport` drain/apply counts. This remains a test/coverage slice only; public frame entrypoint method names, queue ownership, prepared payload apply semantics, project asset loading, and runtime session commit policy are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs zircon_runtime/src/scene/tests/mod.rs` passed; focused Cargo was deferred because active Cargo lanes were still running, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene asset reload renamed event anchors | runtime_05_dynamic_scene_asset_reload_renamed_event_anchors_rustfmt_passed_cargo_deferred_active_cargo_lane_scene_gate_pending | 2026-06-20 | Extended `zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs` with a `Renamed` scene-asset event behavior anchor and a bounded `drain_until_events` helper so typed event forwarding does not race the test assertions. The new anchor copies the project scene to a second URI, rescans the temporary `ProjectManager`, renames the live `ResourceManager` scene record, verifies the reload queue schedules the `AssetEventKind::Renamed` event with both current and previous locators, then applies the prepared payload through `tick_into` into an explicit `World`. This remains a test/coverage slice only; public asset event method names, queue scheduling policy, project scan/import behavior, scene artifact loading, and runtime session commit policy are unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs zircon_runtime/src/scene/tests/mod.rs` passed; focused Cargo was deferred because active Cargo lanes were still running, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3 | 3.1 dynamic scene asset reload behavior static guard | runtime_05_dynamic_scene_asset_reload_behavior_static_guard_rustfmt_passed_cargo_deferred_tests_deferred | 2026-06-20 | `zircon_runtime/src/tests/runtime_absorption/dynamic_scene.rs` now imports `zircon_runtime/src/scene/tests/dynamic_scene_asset_reload.rs` as a read-only Runtime 05 absorption source and locks the existing `dynamic_scene_asset_reload_supersedes_older_pending_scene_revision`, `dynamic_scene_asset_reload_skips_removed_and_reload_failed_events`, `dynamic_scene_asset_reload_tick_into_applies_ready_payload_to_world`, `dynamic_scene_asset_reload_tick_into_level_applies_ready_payload_to_level_world`, and `dynamic_scene_asset_reload_renamed_scene_event_schedules_new_project_uri` anchors. The guard ties those tests back to the plan rows for asset reload behavior, frame entrypoint behavior, and renamed-event behavior so the hot-reload queue, pending/latest revision gate, caller-thread world/level apply path, typed `AssetEventKind::Renamed` forwarding, and skip reasons cannot drift out of the Runtime 05 status table or module documentation. Public asset reload APIs, queue ownership, scene-asset artifact loading, asset watcher behavior, live-world commit policy, and runtime session commit policy remain unchanged. Validation: `rustfmt --edition 2021 --check zircon_runtime/src/tests/runtime_absorption/dynamic_scene.rs` passed; standalone Runtime 05 dynamic-scene absorption guard and standalone plan-status guard passed; Cargo and focused behavior tests were not started for this static guard slice, so Runtime 05 full `scene::` gate remains `pending_full_scene_cargo`. |
| M3-test | Runtime 05 render product streamer lower-layer diagnostic | runtime_05_render_product_streamer_lower_layer_diagnostic_timeout_no_result_residual_stopped | 2026-06-20 | Per `support-first-minimal-diagnostics-matrix`, attempted the first lower-layer diagnostic for the full `scene::` closeout with `cargo test -p zircon_runtime --lib render_product_streamer_tests --locked --jobs 1 --target-dir target\codex-runtime05-render-product-streamer-0620 --message-format short --color never -- --test-threads=1 --nocapture`. The command timed out after 905s with no compiler/test output and no pass/fail result. The timeout left residual processes from this run (`4` cargo and `2` rustc), which were stopped, and a follow-up process scan returned no cargo/rustc processes. No source code was changed by this diagnostic, the lowest broken shared layer is still not identified, and the remaining lower-layer diagnostics `scene_asset` and `ecs_query` are still pending before any owner-file repair should be attempted. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| M3-test | Runtime 05 render product streamer 2026-06-21 no-result diagnostic | runtime_05_render_product_streamer_20260621_no_result_residual_stopped | 2026-06-21 | 状态锚 `runtime_05_render_product_streamer_20260621_no_result_residual_stopped`；per `support-first-minimal-diagnostics-matrix`, retried the graphics-scene lower-layer diagnostic for the full `scene::` closeout with `cargo test -p zircon_runtime --lib render_product_streamer_tests --locked --jobs 1 --message-format short --color never -- --test-threads=1 --nocapture` and `CARGO_TARGET_DIR=target\codex-runtime05-render-product-streamer-0621`. First attempt exited non-zero after 143s with compile-only output and no diagnostic error line; second attempt timed out after 424s with no test result. Residual processes from this run were stopped: 6 cargo and 3 rustc. No source code was changed by this diagnostic, the lowest broken shared layer is still not identified, and the remaining lower-layer diagnostics `scene_asset` and `ecs_query` are still pending before any owner-file repair should be attempted. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| M3-test | Runtime 05 scene_asset 2026-06-21 no-result diagnostic | runtime_05_scene_asset_20260621_no_result_residual_stopped | 2026-06-21 | 状态锚 `runtime_05_scene_asset_20260621_no_result_residual_stopped`；per `support-first-minimal-diagnostics-matrix`, ran the scene-asset/project-IO lower-layer diagnostic for the full `scene::` closeout with `cargo test -p zircon_runtime --lib scene_asset --locked --jobs 1 --target-dir target\codex-runtime05-scene-asset-0621 --message-format short --color never -- --test-threads=1 --nocapture`. The command timed out after 604s with no test result. Residual processes from this run were stopped: 4 cargo and 2 rustc. No source code was changed by this diagnostic, the lowest broken shared layer is still not identified, and `ecs_query` remained pending at this checkpoint. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| M3-test | Runtime 05 ecs_query 2026-06-21 no-result diagnostic | runtime_05_ecs_query_20260621_no_result_residual_stopped | 2026-06-21 | 状态锚 `runtime_05_ecs_query_20260621_no_result_residual_stopped`；per `support-first-minimal-diagnostics-matrix`, ran the ECS lower-layer diagnostic for the full `scene::` closeout with `cargo test -p zircon_runtime --lib ecs_query --locked --jobs 1 --target-dir target\codex-runtime05-scene-asset-0621 --message-format short --color never -- --test-threads=1 --nocapture`. The command timed out after 604s with no test result. Residual processes from this run were stopped: 4 cargo and 2 rustc. No source code was changed by this diagnostic. The graphics-scene, scene-asset/project-IO, and ECS-scene lower-layer probes have now all failed to produce actionable pass/fail evidence in this local window, so no owner-file repair is justified yet. Runtime 05 full `scene::` closeout remains `pending_full_scene_cargo`. |
| M3-test | Runtime 05 dynamic scene selected retention/restore correction | runtime_05_dynamic_scene_selected_retention_restore_focused_passed_session_broad_zrvm_open | 2026-07-04 | 修正：`RuntimeSessionSlotSelector::slot_id` now trims selector ids；tag-selected retention now applies normal tag-latest pruning plus the selected scoped slot only when the selected slot is inside the tag bucket；selected restore behavior now checks stale scene content by node name instead of relying on reused entity ids. 通过 focused 4-test command for `runtime_session_archive_selected_capture_to_path_previews_and_prunes_atomically`、`runtime_session_archive_tag_selected_retention_ignores_protection_outside_bucket`、`runtime_session_archive_selected_restore_apply_and_diff_use_resolved_slots`、`runtime_session_archive_selected_retention_protects_resolved_slot`: 4/4 passed. Broad `--lib session` after this correction is 161 passed / 9 failed / 1 ignored / 6234 filtered, with remaining failures only in `zr-vm-real-backend` vampire session setup. Runtime 05 selected/restore drift cleared; full scene:: closeout remains pending. |

计划状态镜像（2026-06-15）：`runtime_plan_status_boundary` 已接入总结构审计，覆盖 plan-status support files 32/32、runtime subplans 14/14、runtime index subplan rows 14/14、problem rows 17/17、known backlog rows 7/7、status counts `in_progress=14`、core guard anchors 15/15、pending Cargo gate anchors 15/15、doc anchors 11/11、status-output table guard anchors 4/4、`status_output_table_guard_present = true`、Runtime 03 module-doc status anchors 6/6 in runtime index、5/5 in status-output guards、3/3 in mirror docs、`runtime_03_module_doc_status_guard_present = true`、Runtime 07 scene status anchors 11/11 in runtime index、10/10 in status-output guards、4/4 in mirror docs、`runtime_07_scene_status_guard_present = true`、Runtime 07 owner-budget status anchors 7/7 in runtime index、6/6 in status-output guards、3/3 in mirror docs、`runtime_07_owner_budget_status_guard_present = true`、Runtime 02 generated status anchors 6/6 in runtime index、5/5 in status-output guards、3/3 in mirror docs、`runtime_02_generated_status_guard_present = true`、Cargo attempt status anchors 20/20 in runtime index、20/20 in Runtime 14 plan、8/8 in status-output guards、`cargo_attempt_status_anchor_count = 20`、`cargo_attempt_status_guard_present = true`、Runtime 05 closeout status `in_progress`、closeout anchors present、`plan_status_boundary_line_count = 454`、`risks = []`；其中 `plan_status.rs` 已缩为结构挂载，`plan_status/support.rs`、`plan_status/index_tables.rs`、`plan_status/subplan_status.rs`、`plan_status/closeout.rs`、`plan_status/architecture_review.rs`、`status_output_tables.rs`、`status_output_tables/expected_status_row_data.rs`、`status_output_tables/expected_slices.rs` 与 `status_output_tables/expected_status_rows.rs` 已纳入 Rust support 文件集合，`runtime_plan_status_support_inventory.py` owns `PLAN_STATUS_SUPPORT_FILES` / `EXPECTED_PLAN_STATUS_SUPPORT_FILE_COUNT`，`runtime_plan_status_anchor_inventory.py` owns `CORE_GUARD_ANCHORS` / `PENDING_GATE_ANCHORS` / `DOC_ANCHORS` / `BACKLOG_GAPS`，`runtime_plan_status_markdown.py` owns `render_runtime_plan_status_boundary_markdown`，`runtime_plan_status_sources.py` owns `read_text` / `runtime_subplans` / `status_rows`，`expected_slices.rs` owns `expected_status_for_slice` / `expected_date_for_slice`，`expected_status_row_data.rs` owns `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS`，`expected_status_row_data/{runtime_01_04,runtime_05,runtime_06_09,runtime_10_13,runtime_14}.rs` own grouped evidence rows，`expected_status_rows.rs` owns `expected_status_output_slices`，`runtime_plan_status_output_tables_cover_index_and_all_subplans` 与 `runtime_index_status_output_records_recent_cross_plan_slices` 锁定所有 runtime 镜像切片的状态产出表写回，并用 all runtime index status rows full coverage guard 反向要求总索引新增状态行必须进入守卫期望清单，也保留 Runtime 01 Tech-stack 行为测试锚审计同步行、Runtime 03 module-doc status-output row、Runtime 03 schedule/frame-loop behavior-test row、Runtime 07 scene asset owner-split / split-drift / public-surface / boundary-anchor rows、Runtime 07 virtual geometry debug snapshot owner split 行、Runtime 07 project_io folder split 行、Runtime 02 generated template count 审计同步行、Runtime 04 Asset pipeline 行为测试锚审计同步行、Runtime 08 ECS 行为测试锚审计同步行、Runtime 11 JobSystem 行为测试锚审计同步行、Runtime 11 scheduler wait_all 同步点行、Runtime 11 panic-safe handle completion 行、Runtime 12 Input stack 行为锚状态表行与 Runtime 12 event-owner 状态表行；`behavior_test_anchor_count = 12` 与 `missing_behavior_test_anchors = []` 是 Runtime 11 JobSystem 行为锚状态表锚点，`job_handle_wait_reports_task_panic_without_leaking_completion` / `schedule_after_propagates_dependency_panic_without_running_dependent_task` 是 Runtime 11 panic-safe handle completion 行为锚点，`behavior_test_anchor_count = 6` 与 `missing_behavior_test_anchors = []` 是 Runtime 12 Input stack 行为锚状态表锚点，`missing_gamepad_abi_anchors = []` 是 Runtime 12 event-owner 状态表锚点。Cargo attempt 状态审计锁定 `cargo_deferred_active_lane` / `cargo_blocked_external_compile_drift` / `cargo_recheck_blocked_external_ui_compile_drift` / `cargo_recheck_timeout_no_result` 不能从 runtime index、Runtime 14 子计划或 status-output 守卫中丢失，且 Runtime 14 focused recheck timeout 必须保留 `runtime_status_reports_player_rig_and_gpu_readiness`、`904s`、`无测试结果` 与 `residual cargo/rustc processes were stopped` 证据。这仍是静态结构证据，最终 closeout 继续等待 `pending_full_scene_cargo` 与 `cargo test -p zircon_runtime --lib scene:: --locked`。

计划状态镜像（2026-07-01 owner-budget current mirror）：Runtime 07 owner-budget 当前状态表元数据锚点已切到 `Runtime 07 owner-budget 0-hotspot current audit sync`、`large_file_m1_gate_status = classified-and-clear`、`large_file_hotspot_count = 0`、`large_file_migration_debt_count = 0`、`large_file_owner_class_count = 0` 与 `large_file_unclassified_hotspot_count = 0`；`runtime_recent_static_guard_anchors_stay_recorded_across_plan_docs` 要求 Runtime 05、runtime index 与 M0 review 都保留这些当前锚点。该镜像只同步 Runtime 07 owner-budget status metadata，不关闭 Runtime 05 full `scene::` gate，也不提升 Runtime 07 extract/ecs_query/performance profiling/FPS gates。

Runtime 10 Dynamic API 行为锚状态表已同步到同一守卫链：runtime 总索引新增 `Runtime 10 Dynamic API 行为测试锚审计同步` 行，并由 `runtime_index_status_output_records_recent_cross_plan_slices` 锁定 `behavior_test_anchor_count = 15`、`missing_behavior_test_anchors = []`、standalone dynamic_api_session 5/5 与 standalone status-output 2/2 证据；`runtime_plan_status_boundary` 现在显式报告 Runtime 10 behavior status anchors index 6/6、guard 4/4、docs 3/3 与 `runtime_10_behavior_status_guard_present = true`，其中 Runtime 10 behavior status-output row 是当前镜像文档锚点；`dynamic_api`、完整 app loader 与 UI contract owner/Cargo gates 仍 pending。

Runtime 05 scene:: failure support-first triage：`runtime_05_full_scene_failure_clusters_keep_support_first_triage_visible` 固定 `graphics-scene-lower-layer-candidate`、`scene-asset-project-io-lower-layer-candidate`、`ecs-scene-lower-layer-candidate` 与 `support-first-scene-closeout-triage-before-owner-edits` 四个锚点。该切片只把 880/31 full-scene 失败映射到共享支撑层候选，后续修复必须先按 graphics scene material/readiness/runtime-feature/render-graph/HZB/post-process/scene-uniform、scene asset/project IO serialization、ECS query/schedule/event/property/render_extract/world_basics 三组重跑最小下层诊断，再决定是否进入 render/editor/runtime owner 文件。

Runtime 05 scene:: lower-layer diagnostic matrix：`runtime_05_scene_failure_triage_records_minimum_lower_layer_diagnostics` 固定 `support-first-minimal-diagnostics-matrix`，要求 `graphics-scene-diagnostics` 至少覆盖 `render_product_streamer_tests`、`render_product_material_property_tests`、`render_pass_executor_registry` 与 `deferred_lighting_shader_matches_scene_uniform_layout`；`scene-asset-project-io-diagnostics` 至少覆盖 `scene_asset_toml_roundtrip_preserves_entities_and_bindings`、`scene_assets_roundtrip_primitive_mesh_material_bindings` 与 `scene_project_serialization_sources_do_not_store_editor_authoring_state`；`ecs-scene-diagnostics` 至少覆盖 `ecs_query`、`first_stage_updates_all_registered_event_channels`、`render_extract_prepare_flushes_parent_reorder_and_active_changes`、`world_bootstraps_with_renderable_defaults` 与 `world_resolves_entity_paths_and_mutates_component_properties`。下一次 Cargo 空闲窗口先跑 `cargo test -p zircon_runtime --lib render_product_streamer_tests --locked`、`cargo test -p zircon_runtime --lib scene_asset --locked`、`cargo test -p zircon_runtime --lib ecs_query --locked`，再判断是否进入 owner 文件。

Plan-status output anchor tuples 已拆入 `runtime_plan_status_output_anchors.py`，由该模块 owns status-output 与 Cargo attempt anchor tuples；plan-status Rust support inventory 已拆入 `runtime_plan_status_support_inventory.py`，由该模块 owns `PLAN_STATUS_SUPPORT_FILES` 与 `EXPECTED_PLAN_STATUS_SUPPORT_FILE_COUNT`；plan-status core/doc/backlog anchor inventory 已拆入 `runtime_plan_status_anchor_inventory.py`，由该模块 owns `CORE_GUARD_ANCHORS` / `PENDING_GATE_ANCHORS` / `DOC_ANCHORS` / `BACKLOG_GAPS`；plan-status markdown rendering 已拆入 `runtime_plan_status_markdown.py`，由该模块 owns `render_runtime_plan_status_boundary_markdown`；plan-status source/frontmatter/table helpers 已拆入 `runtime_plan_status_sources.py`，由该模块 owns `read_text` / `runtime_subplans` / `status_rows`；`runtime_plan_status_boundary.py` 保持 orchestration boundary。`runtime_plan_status_boundary_audit` 现在显式报告 `plan_status_boundary_line_count = 454` / `max_plan_status_boundary_lines = 900`、`status_output_anchor_module_present = true`、`status_output_anchor_module_line_count = 143` / `max_status_output_anchor_module_lines = 300`，并保持 support 32/32、missing support files []、`risks = []`。该脚本拆分只改变审计脚本结构；当前 Rust plan-status support files 32/32 另含 `plan_status/support.rs`、`plan_status/index_tables.rs`、`plan_status/subplan_status.rs`、`plan_status/closeout.rs`、`plan_status/architecture_review.rs`、`status_output_tables.rs`、`status_output_tables/expected_slices.rs` 的 status/date helper、`status_output_tables/expected_status_row_data.rs` 的 expected anchor data、`status_output_tables/expected_status_rows.rs` 的 thin const-alias owner、`cargo_gates/early/runtime_04.rs`、`cargo_gates/early/runtime_06.rs`、`cargo_gates/late/runtime_10.rs`、`cargo_gates/late/runtime_11.rs`、`cargo_gates/late/runtime_12.rs`、`cargo_gates/late/runtime_13.rs` 与 `cargo_gates/late/runtime_14.rs`，也不改 runtime/render/UI/plugin 生产代码。

Cargo-gates early Runtime 01/02/03/04/06/07/08 pending gates 已拆入 `runtime_absorption/plan_status/cargo_gates/early/runtime_01.rs`、`runtime_absorption/plan_status/cargo_gates/early/runtime_02.rs`、`runtime_absorption/plan_status/cargo_gates/early/runtime_03.rs`、`runtime_absorption/plan_status/cargo_gates/early/runtime_04.rs`、`runtime_absorption/plan_status/cargo_gates/early/runtime_06.rs`、`runtime_absorption/plan_status/cargo_gates/early/runtime_07.rs` 与 `runtime_absorption/plan_status/cargo_gates/early/runtime_08.rs`；`cargo_gates/early.rs` 现在只保留 Runtime 01/02/03/04/06/07/08 includes，后续 early gate 扩展应继续按 runtime owner 拆分而不是回填到单一大文件。

Cargo-gates late Runtime 10/11/12/13/14 pending gates 已拆入 `runtime_absorption/plan_status/cargo_gates/late/runtime_10.rs`、`runtime_absorption/plan_status/cargo_gates/late/runtime_11.rs`、`runtime_absorption/plan_status/cargo_gates/late/runtime_12.rs`、`runtime_absorption/plan_status/cargo_gates/late/runtime_13.rs` 与 `runtime_absorption/plan_status/cargo_gates/late/runtime_14.rs`；`cargo_gates/late.rs` 现在只保留 Runtime 10/11/12/13/14 includes，后续 late gate 扩展应继续按 runtime owner 拆分而不是回填到单一大文件。

Runtime 02 guard-test anchors 状态表已同步到同一守卫链：runtime 总索引新增 `Runtime 02 guard-test anchors 审计同步` 行，并由 `runtime_index_status_output_records_recent_cross_plan_slices` 锁定 `guard_test_anchor_count = 26`、`missing_guard_test_anchors = []` 与 standalone core_spine_root_generated 1/1 / standalone plan-status 32/32 证据；`core/root/generated/export_build_plan/app/editor/plugin` Cargo gates 与 M3 render-owner alias cutover 仍 pending。

Runtime 14 guard anchors 状态表已同步到同一守卫链：runtime 总索引新增 `Runtime 14 Module family guard anchors 审计同步` 行，并由 `runtime_index_status_output_records_recent_cross_plan_slices` 锁定 `module_family_guard_anchor_count = 7`、`missing_module_family_guard_anchors = []`、standalone root_entries 13/13 与 standalone status-output 2/2 证据；`animation` / `navigation` / `diagnostic_log` / `engine_module` / full lib Cargo gates 仍 pending。

基线数值（开工首日记录）：

- `editor_projection` 残留基线：目录 0、文本 1 处（component_structure.rs）
- "editor" 命中基线：224 文件（2026-06-12 PowerShell `Select-String` 重核）
- "legacy" 命中基线：500 处 / 98 文件（2026-06-12 PowerShell `Select-String` 重核）
- 命名守卫重核：editor 1260 locations / 0 unclassified；legacy 516 locations / 0 unclassified；legacy debt bucket 10（2026-06-12 `runtime_naming_boundary` JSON）
- 命名守卫复核：editor 1556 locations / 281 files / 0 unclassified；legacy 491 locations / 101 files / 0 unclassified；legacy debt bucket 7；`runtime_naming_markdown.py` 已接管 Markdown renderer（2026-06-21 `runtime_naming_boundary_audit` 当前工作区）
- 非网络 server 命名门禁复核：count 77 / classification groups 2 / allowed contexts 94 / observer false positives 95 / unclassified 0（2026-06-21 `non_network_server_references` 当前工作区；`non_network_server_naming_markdown.py` 已接管 Markdown renderer，Hub UNC fixture 与 runtime UI Table/DataGrid `sortingMode = "server"` 外部 API 字面值保持允许）
- 硬切换 migration-smell 门禁复核：source_file_count 8520 / legacy_reference_count 152 / allowed_business_bridge_reference_count 316 / classification groups 6 / hard_cutover_migration_debt_count 6 / unclassified 0（2026-06-21 `hard_cutover_migration_smells` 当前工作区；`hard_cutover_migration_smells_markdown.py` 已接管 Markdown renderer，新增 Runtime 05 dynamic scene document legacy refs 已归 `legacy-runtime-scene-document-debt`，DDS caps policy wording 仍为已清）
- token 表基线：SERIALIZED 19 词 / SOURCE 25 词（authoring_boundary.rs:1-49）
- 守卫测试基线：authoring_boundary 2 + inspection 2 + serialization 出口消费文件 4（world_basics / dynamic_scene / asset_scene / inspection）+ source guard 1（component_structure，当前覆盖 13 个 scene/project serialization source files）
- 验证记录：`cargo check -p zircon_runtime --lib --locked` 通过；`cargo test -p zircon_runtime --lib naming_boundary --locked` 1 passed；`cargo test -p zircon_runtime --lib authoring --locked` 17 passed；`cargo test -p zircon_runtime --lib inspection --locked` 6 passed（均使用 `E:\cargo-targets\zircon-runtime-naming-boundary-0612`）。`cargo test -p zircon_runtime --lib scene:: --locked` 全族闭环仍待干净 Cargo 通道。

## 风险与协调

- serialization 守卫与 inspection 均在 `20260604-1232` 会话 touched_modules 中——本计划两个里程碑都属其工作区延长线，执行前必须确认该会话对应切片已完成或已交接，避免双写同一守卫；白名单文档落点必须与其 m1 审计文档合并口径。
- 切片 1.4 的源扫描守卫与子计划 02 M4 的 generated 守卫同属"结构扫描测试"family：实现时共享扫描 helper（若 02 先落地则复用其遍历工具，反之亦然），避免两套文件遍历代码。
- "legacy" 改名类裁决（若选改名）触及 `ui/surface/input` 行为文件——该区域同时是 UI 输入路径计划地盘，改名切片必须移交或与其 owner 协调，本计划不单方面执行。
