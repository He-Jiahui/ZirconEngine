---
related_code:
  - zircon_runtime/src/lib.rs
  - zircon_runtime/src/prelude.rs
  - zircon_runtime/src/asset/mod.rs
  - zircon_runtime/src/asset/prelude.rs
  - zircon_runtime/src/asset/assets/texture/upload_support/dds.rs
  - zircon_runtime/src/asset/tests/assets/texture_upload_readiness.rs
  - zircon_runtime/src/asset/tests/assets/texture_upload_readiness/container_fixtures.rs
  - docs/zircon_runtime/asset/render-assets.md
  - zircon_runtime/src/scene/mod.rs
  - zircon_runtime/src/scene/prelude.rs
  - zircon_runtime/src/plugin/mod.rs
  - zircon_runtime/src/graphics/mod.rs
  - zircon_runtime/src/graphics/prelude.rs
  - zircon_runtime/src/ui/mod.rs
  - zircon_runtime/src/ui/prelude.rs
  - zircon_runtime/src/ui/platform_input/keyboard_map.rs
  - zircon_runtime/src/ui/platform_input/winit_translation.rs
  - docs/zircon_runtime/ui/platform_input.md
  - zircon_runtime/src/ui/template/asset/schema/migrator.rs
  - zircon_runtime/src/ui/tests/asset_schema_migration.rs
  - zircon_runtime_interface/src/ui/template/asset/schema/report.rs
  - docs/zircon_runtime/ui/template/pipeline.md
  - docs/zircon_runtime_interface/ui/mod.md
  - zircon_runtime/src/ui/text/mod.rs
  - zircon_runtime/src/ui/text/edit_state.rs
  - zircon_runtime/src/ui/public_runtime_frame.rs
  - zircon_runtime/src/ui/tests/runtime_ui_support
  - zircon_runtime/src/graphics/types/viewport_render_frame_from_public_runtime.rs
  - zircon_runtime/src/animation/module.rs
  - zircon_runtime/src/animation/manager.rs
  - zircon_runtime/src/animation/manager/mod.rs
  - zircon_runtime/src/animation/manager/pose.rs
  - zircon_runtime/src/animation/manager/sampling.rs
  - zircon_runtime/src/animation/sequence/apply.rs
  - zircon_runtime/src/animation/sequence/conversion.rs
  - zircon_plugins/animation/runtime/tests/runtime_physics_animation_tick_contract.rs
  - zircon_plugins/animation/runtime/tests/runtime_physics_animation_tick_contract/animation_assets.rs
  - zircon_plugins/animation/runtime/tests/runtime_physics_animation_tick_contract/runtime_helpers.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/plugin_importer_dx/d11_test_runtime_fixture.rs
  - zircon_runtime/src/core/framework/animation/error.rs
  - zircon_runtime/src/core/framework/animation/manager.rs
  - docs/zircon_runtime/animation/runtime.md
  - zircon_runtime/src/core/framework/input/mouse_wheel.rs
  - zircon_runtime/src/core/framework/input/mod.rs
  - zircon_runtime/src/input/mod.rs
  - zircon_runtime/src/input/runtime/default_input_manager.rs
  - zircon_runtime/src/input/runtime/default_input_action_manager.rs
  - zircon_runtime/src/dynamic_api/session/events.rs
  - zircon_runtime/src/dynamic_api/tests/input_events.rs
  - docs/zircon_runtime/input/input_state.md
  - zircon_runtime/src/graphics/hybrid_gi_runtime_provider/mod.rs
  - zircon_runtime/src/graphics/virtual_geometry_runtime_provider/mod.rs
  - zircon_runtime/src/dynamic_api/session.rs
  - zircon_runtime/src/dynamic_api/session/tests/mod.rs
  - zircon_runtime/src/dynamic_api/session/tests/vampire_runtime_support.rs
  - docs/zircon_runtime/dynamic_api/session.md
  - zircon_runtime/src/core/framework/camera_controller/mod.rs
  - zircon_runtime/src/core/framework/camera_controller/controller_output.rs
  - docs/zircon_runtime/core/framework/camera_controller.md
  - zircon_runtime/src/scene/tests/ecs_systems.rs
  - zircon_runtime/src/scene/tests/ecs_systems/many_single_queries.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/scene_ecs_systems.rs
  - zircon_runtime/src/dynamic_api/session/tests/lock_poison.rs
  - docs/zircon_runtime/dynamic_api/session.md
  - zircon_runtime/src/core/framework/tests.rs
  - zircon_runtime/src/ui/tests/v2_asset.rs
  - zircon_runtime/src/ui/tests/shared_core.rs
  - zircon_runtime/src/asset/pipeline/worker_pool.rs
  - zircon_runtime/src/asset/tests/pipeline/worker_pool.rs
  - zircon_runtime/src/core/runtime/state/mod.rs
  - zircon_runtime/src/core/runtime/state/core_runtime_state.rs
  - zircon_runtime/src/core/runtime/state/module_entry.rs
  - zircon_runtime/src/core/runtime/config_store.rs
  - zircon_runtime/src/core/resource/manager/resource_manager.rs
  - zircon_runtime/src/core/resource/manager/registry_ops.rs
  - zircon_runtime/src/core/resource/manager/payload_ops.rs
  - zircon_runtime/src/core/resource/manager/lease_ops.rs
  - zircon_runtime/src/core/resource/manager/events.rs
  - docs/zircon_runtime/core/resource.md
  - zircon_runtime/src/navigation/runtime.rs
  - zircon_runtime/src/navigation/runtime/tests.rs
  - docs/zircon_runtime/navigation/runtime.md
  - zircon_runtime/src/dynamic_api/session.rs
  - zircon_runtime/src/dynamic_api/session/tests/lock_poison.rs
  - docs/zircon_runtime/dynamic_api/session.md
  - zircon_runtime/src/core/runtime/handle/registration/register_module.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/mod.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/types.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/multi.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/specialized.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/shutdown.rs
  - zircon_runtime/src/core/runtime/handle/core_handle.rs
  - zircon_runtime/src/core/runtime/handle/activation.rs
  - zircon_runtime/src/core/runtime/diagnostics/devtools.rs
  - zircon_runtime/src/core/runtime/diagnostics/profiling/mod.rs
  - zircon_runtime/src/core/runtime/handle/resolution.rs
  - zircon_runtime/src/core/runtime/handle/runtime_extensions.rs
  - zircon_runtime/src/core/runtime/handle/diagnostics.rs
  - zircon_runtime/src/core/runtime/handle/time.rs
  - zircon_runtime/src/core/runtime/handle/states.rs
  - zircon_runtime/src/core/runtime/tasks/job_handle.rs
  - zircon_runtime/src/core/runtime/tasks/job_scheduler.rs
  - zircon_runtime/src/asset/watch/mod.rs
  - zircon_runtime/src/asset/watch/shutdown_on_drop.rs
  - zircon_runtime/src/asset/watch/asset_change_construction.rs
  - zircon_runtime/src/scene/ecs/observer/mod.rs
  - zircon_runtime/src/scene/ecs/observer/store.rs
  - zircon_runtime/src/scene/ecs/observer/callback_registry.rs
  - zircon_runtime/src/scene/ecs/query/query_state/mod.rs
  - zircon_runtime/src/scene/ecs/query/query_state/many_item_array.rs
  - zircon_runtime/src/scene/ecs/query/query_state/cached_direct.rs
  - zircon_runtime/src/scene/ecs/query/query_state/mutable.rs
  - zircon_runtime/src/scene/ecs/query/query_state/read_only.rs
  - zircon_runtime/src/scene/ecs/query/query_state/read_only_cached.rs
  - zircon_runtime/src/scene/ecs/storage/component_storage/mod.rs
  - zircon_runtime/src/scene/ecs/storage/component_storage/store.rs
  - zircon_runtime/src/scene/ecs/storage/component_storage/component_results.rs
  - zircon_runtime/src/scene/tests/ecs_query.rs
  - zircon_runtime/src/scene/tests/ecs_query/cached_queries.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/core_framework.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/core_scene.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/asset_dynamic.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/scene_tests.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/plugin_static_manifest.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/ui.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/graphics.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/input.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary/runtime_15_m2/editor_workbench.rs
  - zircon_runtime/src/ui/surface/surface/default_interactions/table/columns.rs
  - zircon_editor/src/ui/retained_host/callback_dispatch/template_bridge/workbench/extension_module_feedback/gameplay_state.rs
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/non_network_server_naming.py
  - zircon_runtime/src/graphics/runtime/render_framework
  - zircon_runtime/src/graphics/scene/resources/resource_streamer/resource_streamer_construction.rs
  - zircon_runtime/src/graphics/backend/render_backend/mod.rs
  - zircon_runtime/src/graphics/backend/render_backend/offscreen_target_construct/construct.rs
  - docs/zircon_runtime/core/diagnostics.md
  - docs/zircon_runtime/core/runtime/lifecycle.md
  - docs/zircon_runtime/core/state.md
  - docs/zircon_runtime/core/tasks.md
  - docs/zircon_runtime/scene/ecs.md
  - zircon_runtime/src/script/vm/host/builtin_host_modules.rs
  - docs/zircon_runtime/script/vm/host/function_ledger.md
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/provider_boilerplate.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/facade_surface.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/runtime_dead_code.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy/core_runtime.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy/runtime_services.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy/asset_render_input.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/diagnostics_surface.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/mod.rs
  - zircon_runtime/src/core/framework/tests.rs
  - zircon_runtime/src/core/framework/tests/framework_surfaces.rs
  - zircon_runtime/src/core/framework/tests/render_product_surface.rs
  - zircon_runtime/src/ui/tests/v2_asset.rs
  - zircon_runtime/src/ui/tests/v2_asset/asset_loading.rs
  - zircon_runtime/src/ui/tests/v2_asset/style_runtime.rs
  - zircon_runtime/src/ui/tests/v2_asset/default_controls.rs
  - zircon_runtime/src/ui/tests/v2_asset/range_controls.rs
  - zircon_runtime/src/ui/tests/v2_asset/demo_and_builder.rs
  - zircon_runtime/src/ui/tests/v2_asset/composite_components.rs
  - zircon_runtime/src/ui/tests/v2_asset/file_cache.rs
  - zircon_runtime/src/ui/tests/shared_core.rs
  - zircon_runtime/src/ui/tests/shared_core/layout_surface.rs
  - zircon_runtime/src/ui/tests/shared_core/box_flow.rs
  - zircon_runtime/src/ui/tests/shared_core/input_visibility.rs
  - zircon_runtime/src/ui/tests/shared_core/navigation.rs
  - zircon_runtime/src/ui/tests/shared_core/scroll_mutation.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/typed_error_convergence/mod.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/typed_error_convergence/asset_records.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/typed_error_convergence/diagnostics.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/typed_error_convergence/scene_world.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/f12_dead_code.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/f8_api_convergence.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/plugin_importer_dx.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/plugin_importer_dx/d1_capability_single_source.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/render_structure.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings/late_api_cleanup.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/code_review_findings.rs
  - tools/plugin_structure_audits/capability.py
  - zircon_plugins/first_party_runtime_catalog/src/lib.rs
  - zircon_plugins/plugin_sdk/src/manifest/feature_bundle_builder.rs
  - zircon_plugins/plugin_sdk/src/manifest/importer_runtime.rs
plan_sources:
  - docs/plans/engine-code-structure-convention.md
  - docs/engine-architecture/large-file-ownership-m1.md
  - docs/engine-architecture/runtime-interface-convergence.md
implementation_files:
  - docs/zircon_runtime/structure/module-convention.md
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/module_convention_gate.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/module_convention_gate_markdown.py
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/module_convention_gate.rs
  - zircon_runtime/src/graphics/mod.rs
  - zircon_runtime/src/prelude.rs
  - zircon_runtime/src/asset/prelude.rs
  - zircon_runtime/src/scene/prelude.rs
  - zircon_runtime/src/ui/prelude.rs
  - zircon_runtime/src/graphics/prelude.rs
  - zircon_runtime/src/ui/public_runtime_frame.rs
  - zircon_runtime/src/ui/tests/runtime_ui_support
  - zircon_runtime/src/graphics/types/viewport_render_frame_from_public_runtime.rs
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/module_convention_gate.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/module_convention_gate_markdown.py
  - zircon_runtime/src/tests/prelude.rs
  - zircon_runtime/src/asset/pipeline/worker_pool.rs
  - zircon_runtime/src/asset/tests/pipeline/worker_pool.rs
  - zircon_runtime/src/core/runtime/state/mod.rs
  - zircon_runtime/src/core/runtime/state/core_runtime_state.rs
  - zircon_runtime/src/core/runtime/state/module_entry.rs
  - zircon_runtime/src/core/runtime/config_store.rs
  - zircon_runtime/src/core/resource/manager/resource_manager.rs
  - zircon_runtime/src/core/resource/manager/registry_ops.rs
  - zircon_runtime/src/core/resource/manager/payload_ops.rs
  - zircon_runtime/src/core/resource/manager/lease_ops.rs
  - zircon_runtime/src/core/resource/manager/events.rs
  - docs/zircon_runtime/core/resource.md
  - zircon_runtime/src/animation/manager/mod.rs
  - zircon_runtime/src/animation/manager/pose.rs
  - zircon_runtime/src/animation/manager/sampling.rs
  - zircon_runtime/src/animation/sequence/apply.rs
  - zircon_runtime/src/animation/sequence/conversion.rs
  - zircon_runtime/src/core/framework/animation/error.rs
  - zircon_runtime/src/core/framework/animation/manager.rs
  - docs/zircon_runtime/animation/runtime.md
  - zircon_runtime/src/input/runtime/default_input_manager.rs
  - zircon_runtime/src/input/runtime/default_input_action_manager.rs
  - docs/zircon_runtime/input/input_state.md
  - zircon_runtime/src/navigation/runtime.rs
  - zircon_runtime/src/navigation/runtime/tests.rs
  - docs/zircon_runtime/navigation/runtime.md
  - zircon_runtime/src/core/runtime/handle/registration/register_module.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/mod.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/types.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/multi.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/specialized.rs
  - zircon_runtime/src/core/runtime/handle/registration/service_lists/shutdown.rs
  - zircon_runtime/src/core/runtime/handle/core_handle.rs
  - zircon_runtime/src/core/runtime/handle/activation.rs
  - zircon_runtime/src/core/runtime/diagnostics/devtools.rs
  - zircon_runtime/src/core/runtime/diagnostics/profiling/mod.rs
  - zircon_runtime/src/core/runtime/handle/resolution.rs
  - zircon_runtime/src/core/runtime/handle/runtime_extensions.rs
  - zircon_runtime/src/core/runtime/tests/registration/structure.rs
  - zircon_runtime/src/core/runtime/tests/registration/structure/behavior_layout.rs
  - zircon_runtime/src/asset/watch/mod.rs
  - zircon_runtime/src/asset/watch/shutdown_on_drop.rs
  - zircon_runtime/src/scene/ecs/observer/mod.rs
  - zircon_runtime/src/scene/ecs/observer/store.rs
  - zircon_runtime/src/scene/ecs/observer/callback_registry.rs
  - zircon_runtime/src/scene/ecs/storage/component_storage/mod.rs
  - zircon_runtime/src/scene/ecs/storage/component_storage/store.rs
  - zircon_runtime/src/scene/ecs/storage/component_storage/component_results.rs
  - zircon_runtime/src/tests/runtime_absorption/naming_boundary.rs
  - docs/zircon_runtime/core/runtime/lifecycle.md
  - docs/zircon_runtime/scene/ecs.md
  - zircon_runtime/src/script/vm/host/builtin_host_modules.rs
  - docs/zircon_runtime/script/vm/host/function_ledger.md
  - zircon_runtime/src/tests/runtime_absorption/structure_convention.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/provider_boilerplate.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/facade_surface.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/runtime_dead_code.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy/core_runtime.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy/runtime_services.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/lock_poison_policy/asset_render_input.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/diagnostics_surface.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/test_file_budget/mod.rs
  - zircon_runtime/src/core/framework/tests.rs
  - zircon_runtime/src/core/framework/tests/framework_surfaces.rs
  - zircon_runtime/src/core/framework/tests/render_product_surface.rs
  - zircon_runtime/src/ui/tests/v2_asset.rs
  - zircon_runtime/src/ui/tests/v2_asset/asset_loading.rs
  - zircon_runtime/src/ui/tests/v2_asset/style_runtime.rs
  - zircon_runtime/src/ui/tests/v2_asset/default_controls.rs
  - zircon_runtime/src/ui/tests/v2_asset/range_controls.rs
  - zircon_runtime/src/ui/tests/v2_asset/demo_and_builder.rs
  - zircon_runtime/src/ui/tests/v2_asset/composite_components.rs
  - zircon_runtime/src/ui/tests/v2_asset/file_cache.rs
  - zircon_runtime/src/ui/tests/shared_core.rs
  - zircon_runtime/src/ui/tests/shared_core/layout_surface.rs
  - zircon_runtime/src/ui/tests/shared_core/box_flow.rs
  - zircon_runtime/src/ui/tests/shared_core/input_visibility.rs
  - zircon_runtime/src/ui/tests/shared_core/navigation.rs
  - zircon_runtime/src/ui/tests/shared_core/scroll_mutation.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/graphics_dead_code/mod.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/graphics_dead_code/module_layout.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/graphics_dead_code/renderer_output_accessors.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/graphics_dead_code/backend_owners.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/graphics_dead_code/gpu_resource_owners.rs
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/graphics_dead_code/resource_streamer_cleanup.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status.rs
  - zircon_runtime/src/tests/runtime_absorption/plan_status/status_output_tables/expected_slices/date.rs
tests:
  - cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_mixed_visibility_has_facade_note --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib prelude --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib structure_convention --no-default-features --features core-min --locked
  - zircon_runtime/src/tests/runtime_absorption/structure_convention/module_convention_gate.rs::runtime_15_module_convention_gate_reports_non_render_debt_boundary
  - cargo test -p zircon_runtime --lib runtime_15_runtime_ui_dead_code_surface_is_test_support --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_runtime_owned_dead_code_suppression_cleanup --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_script_host_value_descriptors_do_not_suppress_dead_code --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_graphics_dead_code_guard_is_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_provider_boilerplate_guard_is_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_facade_surface_guard_is_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_runtime_dead_code_guard_is_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_runtime_dead_code_guard_forbidden_attribute_literal_is_constant_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_ui_host_surface_splits_production_frame_from_test_support --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_diagnostics_guard_is_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_core_framework_tests_are_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_ui_v2_asset_tests_are_folder_backed --no-default-features --features core-min --locked
  - cargo test -p zircon_runtime --lib runtime_15_ui_shared_core_tests_are_folder_backed --no-default-features --features core-min --locked
  - python .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_runtime_structure.py --json
  - cargo fmt --all --check
doc_type: structure-plan
status: in_progress
---

# 15 · Runtime 代码结构与模块规范收束计划

> 状态：in_progress · 范围：`zircon_runtime` 全模块（含 `graphics/**`，render 子计划引用）
> 上游权威：[`docs/plans/engine-code-structure-convention.md`](../../engine-code-structure-convention.md)（本计划只**执行**该规范，不重定义规则）
> 原则：硬切不留兼容层；按 ownership 拆不按行数切；root 接线薄；规则收敛由审计 gate + guard 测试机器化验收。

## 1. 目标

把 `zircon_runtime` 的模块布局、命名、公共 API、测试组织收敛到 `engine-code-structure-convention`，让用户对 runtime 做 code review / inspect 时有**一致、可预测、接口友好**的结构，并以 `module_convention_gate` 防止回归。

## 2. 现状缺口（按代码实查，带路径证据）

| # | 缺口 | 规范条目 | 证据路径 |
|---|------|---------|---------|
| S1 | `mod.rs` / `module.rs` 混用，无统一判据 | R1.2 | `animation/module.rs` vs `input/mod.rs`（直塞注册）vs `graphics/mod.rs`（注册分散无 `module.rs`） |
| S2 | 巨型扁平 re-export façade（100+ 符号一坨） | R3.1 | `asset/mod.rs`、`scene/mod.rs`、`plugin/mod.rs` |
| S3 | `pub mod` / `pub(crate) mod` 无规则混排 | R3.4 | `graphics/mod.rs:1-93` |
| S4 | `runtime_*` 前缀泛滥到失去语义 | R2.2 | `graphics/{hybrid_gi,virtual_geometry}_runtime_provider/runtime_{feedback,state,stats,update}.rs` |
| S5 | `manager.rs` 与 `manager/` 并存、规则不明 | R1.2 | `animation/manager.rs` + `animation/manager/{graph,parameters,pose,sampling,state_machine}.rs` |
| S6 | 巨型 `tests.rs` + 三套测试组织并行 | R1.4 / R4.1-R4.3 | `core/framework/tests.rs`(1848)、`ui/tests/v2_asset.rs`(3806)、`ui/tests/shared_core.rs`(3145) |
| S7 | 复数 / 单数失序 | R2.1 | `scene/components/`（复数）vs `animation/manager/`（单数）规则缺失 |
| S8 | 单文件多职责协调 | R1.3 | `dynamic_api/session.rs`(773，协调 17 子模块) |
| S9 | **prelude 完全遗漏 asset/scene/ecs/ui/graphics**，gameplay/authoring 用户须深路径 import | R3.3 | `prelude.rs`（`crate::{asset,scene,ui,graphics}` 零命中） |
| S10 | **200 处 `#[allow(dead_code)]`（55 文件）掩盖僵尸**。`runtime_ui` 生产子模块已在 Runtime 15 runtime UI dead-code support split 中拆为生产 frame DTO + test-only support；Runtime 15 F12 runtime-owned dead-code suppression cleanup 已清理 asset worker test-only receiver guard 与 core `ModuleEntry` descriptor 生产字段的抑制；Runtime 15 F12 script host value descriptor dead-code cleanup 已移除 `Vec3` / `ColorRgba` 的 suppression 并用字段布局哨兵保持反射描述器 live；Runtime 15 F12 script reflection macro fixture dead-code cleanup 已移除 `reflection_docs.rs` 中 TestVec3/TestEnum/Point 宏 fixture 的 suppression 并由测试断言读取字段/变体；Runtime 15 F12 offscreen target texture owner cleanup 已把固定帧 WGPU 纹理 owner 变成生产绑定入口会读取的保活契约；Runtime 15 F12 render backend state owner cleanup 已把 `RenderBackend` 的 instance/adapter/config owner 转为 `caps()` 路径可检查的保活契约；Runtime 15 F12 gpu texture resource owner cleanup 已把 `GpuTextureResource` 的 id/texture/view/sampler 转为材质绑定入口可检查的资源驻留契约；Runtime 15 F12 gpu material uniform owner cleanup 已把 `GpuMaterialUniformResource` 的 buffer/payload byte len/buffer byte len 转为 uniform 绑定入口和诊断 accessor 可检查的资源驻留契约；Runtime 15 F12 gpu mesh order signature cleanup 已把 `GpuMeshResource::indirect_order_signature()` 接入 prepared mesh draw 排序 tie-breaker；Runtime 15 F12 gpu model identity cleanup 已把 `GpuModelResource::id()` 接入 `ResourceStreamer::model(...)` 缓存身份校验；Runtime 15 F12 post-process LUT texture owner cleanup 已把 `PostProcessLutTextureResource::view()` 接入 3D LUT 绑定查询并显式保活 texture/view owner；Runtime 15 F12 output target texture owner cleanup 已把 `OutputTargetTextureResource` descriptor/texture/view/sampler 与 `PreparedOutputTargetTexture` cache owner 接入 graph import、writeback 和材质采样路径；Runtime 15 F12 material runtime capture seed cleanup 已把 `MaterialCaptureSeed`、`MaterialRuntime::capture_seed()`、`ResourceStreamer::material_capture_seed(...)` 与 `sample_texture_rgba(...)` 收进 test-only 配置并移除 material runtime suppression；Runtime 15 F12 resource streamer diagnostics accessor cleanup 已把测试诊断 accessor 收进 `#[cfg(test)]` 并保留生产 readiness bridge live；Runtime 15 F12 resource streamer resolve texture id cleanup 已删除未使用的 `resolve_texture_id(...)` helper 并保留生产 `resolve_texture_reference(...)` / `resolve_texture_reference_with_support(...)` 入口；Runtime 15 F12 particle GPU readback output accessor cleanup 已删除已接入 runtime feedback 的 particle readback 输出 accessor stale suppression；其他 suppression sweep 仍待 M5/T2 继续清理 | 规范 E6 | `ui/public_runtime_frame.rs`、`ui/tests/runtime_ui_support/`、`asset/pipeline/worker_pool.rs`、`core/runtime/state/module_entry.rs`、`core/runtime/diagnostics/devtools.rs`、`script/vm/host/builtin_host_modules.rs`、`script/vm/tests/reflection_docs.rs`、`graphics/backend/render_backend/offscreen_target.rs`、`graphics/backend/render_backend/render_backend.rs`、`graphics/scene/resources/gpu_texture/gpu_texture_resource.rs`、`graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs`、`graphics/scene/resources/gpu_mesh/gpu_mesh_resource.rs`、`graphics/scene/resources/gpu_model/gpu_model_resource.rs`、`graphics/scene/resources/gpu_model/gpu_model_resource_from_asset.rs`、`graphics/scene/resources/post_process_lut_texture/post_process_lut_texture_resource.rs`、`graphics/scene/resources/output_target_texture/output_target_texture_resource.rs`、`graphics/scene/resources/prepared/prepared_output_target_texture.rs`、`graphics/scene/resources/runtime/material_runtime.rs`、`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs`、`graphics/scene/resources/resource_streamer/resource_streamer_resolve_texture_id.rs`、`graphics/scene/scene_renderer/core/scene_renderer_runtime_outputs/take_last_particle_gpu_readback_outputs.rs`、`graphics/runtime/render_framework/submit_frame_extract/submit/collect_runtime_feedback.rs`、`graphics/scene/resources/resource_streamer/resource_streamer_ensure_scene_resources.rs`、`graphics/scene/resources/resource_streamer/resource_streamer_execute_output_target_writeback.rs`、`graphics/scene/scene_renderer/mesh/build_mesh_draws/build/extend_pending_draws_for_mesh_instance.rs`、`scene_renderer_core_render_compiled_scene/render/bind_frame_graph_resources.rs`、`runtime_15_runtime_ui_dead_code_surface_is_test_support`、`runtime_15_runtime_owned_dead_code_suppression_cleanup`、`runtime_15_script_host_value_descriptors_do_not_suppress_dead_code`、`runtime_15_script_reflection_macro_fixtures_do_not_suppress_dead_code`、`runtime_15_offscreen_target_texture_owner_cleanup`、`runtime_15_render_backend_state_owner_cleanup`、`runtime_15_gpu_texture_resource_owner_cleanup`、`runtime_15_gpu_material_uniform_owner_cleanup`、`runtime_15_gpu_mesh_order_signature_cleanup`、`runtime_15_gpu_model_identity_cleanup`、`runtime_15_post_process_lut_texture_owner_cleanup`、`runtime_15_output_target_texture_owner_cleanup`、`runtime_15_material_runtime_capture_seed_cleanup`、`runtime_15_resource_streamer_diagnostics_accessor_cleanup`、`runtime_15_resource_streamer_resolve_texture_id_cleanup`、`runtime_15_particle_gpu_readback_output_accessor_cleanup`；全 crate dead-code suppression sweep 仍待审计；2026-06-22 render F11 已删除 `graphics/material/shading_models/registry.rs` 的未接线 dead-code surface |
| S11 | **4 套 `*_runtime_provider` 近 99% 复制粘贴** + 7+ 个 `*Diagnostics` 命名/嵌套重复。F14 子切片已新增 `FrameDiagnostics` trait 并移除 `WorldEcsFramePerformanceDiagnostics` 纯包装；F13 registration 子切片已抽 `RuntimeProviderRegistration<P: ?Sized>` + `define_runtime_provider_registration!`；F13 update stats 子切片已抽 `RuntimeProviderUpdate<S>` + `define_runtime_provider_update!`；F13 feedback shared payload 子切片已抽 `RuntimeProviderFeedback<G, V>`；F13 prepare input 子切片已抽 `RuntimeProviderPrepareInput<'a, E>`；full F13 audit 已由 `runtime_15_no_duplicated_provider_boilerplate` 总守卫闭合 | 规范 E5 | `graphics/runtime_provider/{registration,update,feedback,prepare_input}.rs`、`graphics/{hybrid_gi,virtual_geometry,solari}_runtime_provider/provider_registration.rs`、`graphics/{hybrid_gi,virtual_geometry}_runtime_provider/{prepare_input,runtime_update,runtime_feedback}.rs`、`graphics/{particle,solari}_runtime_provider/{prepare_input,runtime_feedback}.rs`、`core/runtime/diagnostics/frame_diagnostics.rs`、`scene/world/performance_diagnostics.rs`、`scene/world/world.rs` |
| S12 | 渲染器 construction owner 使用 `*_new` 后缀，读如迁移残留 | R2.5 | 2026-06-22 render F19 已硬切为 `scene_renderer_core_construct` / `scene_renderer_construct`，新增 `review_f19_scene_renderer_construction_modules_use_construct_names`，状态 `render_scene_renderer_construct_modules_coremin_passed`；2026-06-25 Runtime 15 M2 已把 asset watcher 的 `asset_change_new.rs` 硬切为 `asset_change_construction.rs`，状态 `runtime_15_asset_change_construction_naming_hard_cutover_static_passed_cargo_deferred`，把 graphics resource streamer 的 `resource_streamer_new.rs` 硬切为 `resource_streamer_construction.rs`，状态 `runtime_15_resource_streamer_construction_naming_hard_cutover_static_passed_cargo_deferred`，并把 render backend 的 `offscreen_target_new/` 硬切为 `offscreen_target_construct/`，状态 `runtime_15_offscreen_target_construct_naming_hard_cutover_static_passed_cargo_timeout_no_result`；2026-06-27 Runtime 15 M2 已把 graphics construction 裸 `new.rs` / `new/` owner 硬切为 `construct.rs` / `construct/`，状态 `runtime_15_graphics_construction_new_owner_naming_hard_cutover_static_passed_cargo_deferred`；同日把 dynamic scene document 的 `legacy.rs` / `LegacyProjectDocument` 硬切为 `v1_project_document.rs` / `V1ProjectDocument`，状态 `runtime_15_scene_dynamic_document_v1_owner_naming_hard_cutover_static_passed_cargo_deferred` |

> S9-S12 来自 [`engine-code-review-findings-2026-06.md`](../../engine-code-review-findings-2026-06.md)（F1/F9/F10/F12/F13/F14/F19）。注：native host callback panic guard 的 P0/F1 已由 Runtime 15 F1 native host callback panic guard 固定当前闭合状态；渲染热路径的 String 错误、每帧 clone、`render_compiled_scene` 533 行等 P0/P1 项归 Runtime 07 / render，不在本计划但与 M4 large-file 联动；F19 的目录命名债已作为 R2.5 样例闭合。

## 3. 目标结构（收敛后形态）

- 每个子系统 `mod.rs` = 分组注释的精选 façade + `pub mod prelude;`；crate 级 `prelude` 收窄为聚合各子系统 prelude。
- `module.rs` 仅出现在注册子系统目录（有 `module_descriptor()`）；`input/` 注册下沉 `input/module.rs`，`graphics/` 注册收口到 `graphics/module.rs`。
- `*_runtime_provider/` 内部模块去 `runtime_` 前缀（`state.rs`/`feedback.rs`/`stats.rs`/`update.rs`）。
- `animation/manager.rs` 删除，`animation/manager/mod.rs` 作薄 façade 拥有子模块。
- 测试统一：小测内联、行为测试 folder-backed 镜像源树、无 > 800 行 `tests.rs`。

## 4. 里程碑（任务级执行蓝本）

切片期 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked`；里程碑末进测试。命令省略共用尾部。

| 里程碑 | 任务 | 改动文件（代表） | 依赖 | 验收命令 / 测试函数 |
|---|---|---|---|---|
| **M1 façade / 可见性** | T1 大 façade 分组 + 建子系统 prelude | `asset/mod.rs`、`scene/mod.rs`、`plugin/mod.rs`、新 `asset/prelude.rs` 等 | — | `cargo check -p zircon_runtime --lib`；`runtime_15_facade_reexport_within_budget`、`runtime_15_no_glob_reexport_at_subsystem_facade` |
| | T2 收 `pub` / `pub(crate)` 混排 | `graphics/mod.rs` | T1 | `runtime_15_mixed_visibility_has_facade_note` |
| | T3 crate 级 `prelude` 收窄为聚合 | `lib.rs`、`prelude.rs` | T1 | `cargo test -p zircon_runtime --lib prelude` |
| **M2 命名 / 前缀** | T1 去冗余 `runtime_` 前缀 | `graphics/{hybrid_gi,virtual_geometry}_runtime_provider/*` | M1 | grep 旧符号零命中；`runtime_15_module_prefix_in_vocabulary` |
| | T2 复数 / 单数对齐 | 相关目录 rename | M1 | `runtime_15_collection_dirs_plural_owner_dirs_singular` |
| | T3 去 `_inner` / `_helper` / `util` | 命名债文件；`core/runtime/state/runtime_inner.rs` 已硬切为 `core/runtime/state/core_runtime_state.rs`，`scene/ecs/observer/utils.rs` 已硬切为 `scene/ecs/observer/callback_registry.rs`，`scene/ecs/query/query_state/helpers.rs` 已硬切为 `scene/ecs/query/query_state/many_item_array.rs`，`scene/ecs/storage/component_storage/utils.rs` 已硬切为 `scene/ecs/storage/component_storage/component_results.rs`，`asset/watch/drop_impl.rs` 已硬切为 `asset/watch/shutdown_on_drop.rs`，`scene/tests/ecs_query/cache_helpers.rs` 已硬切为 `scene/tests/ecs_query/cached_queries.rs`，`dynamic_api/session/tests/helpers.rs` 已硬切为 `dynamic_api/session/tests/vampire_runtime_support.rs`，`core/framework/camera_controller/common.rs` 已硬切为 `core/framework/camera_controller/controller_output.rs`，`scene/tests/ecs_systems/query_helpers.rs` 已硬切为 `scene/tests/ecs_systems/many_single_queries.rs`，`asset/tests/assets/texture_upload_readiness/common.rs` 已硬切为 `asset/tests/assets/texture_upload_readiness/container_fixtures.rs`，`plugin_extensions/static_manifest_contracts/{feature_bundles,package_coordinates,package_identity,package_kind}/helpers.rs` 已硬切为 `feature_bundle_rows.rs`、`package_coordinate_resolution.rs`、`package_id_tokens.rs` 与 `package_kind_fields.rs`，`ui/component/catalog/editor_showcase/helpers.rs` 已硬切为 `ui/component/catalog/editor_showcase/descriptor_builders.rs`；2026-06-27 已把 `graphics/runtime/render_framework/render_framework_impl/trait_impl.rs` 与 `graphics/runtime/render_framework/wgpu_render_framework_new/new.rs` 硬切为 `render_framework_trait_binding/wgpu_framework.rs` 与 `wgpu_render_framework_construction/construct.rs`，并把 `scene/dynamic_scene/document/legacy.rs` / `LegacyProjectDocument` 硬切为 `scene/dynamic_scene/document/v1_project_document.rs` / `V1ProjectDocument`，对应父模块只挂载新 owner，旧路径不保留兼容模块 | M1 | `runtime_15_no_banned_name_modules`；`runtime_15_core_runtime_state_module_uses_owner_name`；`runtime_15_scene_ecs_observer_callback_registry_uses_owner_name`；`runtime_15_scene_ecs_query_state_many_item_array_uses_owner_name`；`runtime_15_scene_ecs_component_storage_component_results_uses_owner_name`；`runtime_15_asset_watcher_shutdown_on_drop_uses_owner_name`；`runtime_15_scene_ecs_query_cached_queries_uses_owner_name`；`runtime_15_dynamic_api_vampire_runtime_support_uses_owner_name`；`runtime_15_camera_controller_output_uses_owner_name`；`runtime_15_scene_ecs_systems_many_single_queries_uses_owner_name`；`runtime_15_asset_texture_upload_readiness_container_fixtures_uses_owner_name`；`runtime_15_plugin_static_manifest_contract_owners_use_domain_names`；`runtime_15_ui_editor_showcase_descriptor_builders_use_owner_name`；`runtime_15_render_framework_trait_construction_owner_naming_hard_cutover_static_passed_cargo_deferred`；`runtime_15_scene_dynamic_document_v1_owner_uses_versioned_name` |
| | T4 `manager.rs` + `manager/` 消解 | `animation/manager.rs` 已硬切删除，`animation/manager/mod.rs` 作薄 façade 并继续挂载 `graph` / `parameters` / `pose` / `sampling` / `state_machine` child owners | M1 | `runtime_15_module_rs_only_for_registration_subsystems`；`runtime_15_animation_manager_is_folder_backed` |
| **M3 测试组织** | T1 拆 > 800 行测试 | `core/framework/tests.rs`、`ui/tests/v2_asset.rs`、`ui/tests/shared_core.rs` → folder-backed；graphics dead-code guard 聚合已拆为 `structure_convention/graphics_dead_code/mod.rs` + `module_layout.rs` + `renderer_output_accessors.rs`，并继续细分为 `backend_owners.rs` / `gpu_resource_owners.rs` / `resource_streamer_cleanup.rs`；provider boilerplate 守卫已集中到 `structure_convention/provider_boilerplate.rs`；asset test-budget 守卫已继续细分为 `structure_convention/test_file_budget/asset_tests/{pack,facade,project,material}.rs` | — | `runtime_15_no_oversized_test_files`；`runtime_15_graphics_dead_code_guard_is_folder_backed`；`runtime_15_provider_boilerplate_guard_is_folder_backed` |
| | T2 三套测试 → 单一规则 + 消重复 | 内联 / `tests/` / `tests.rs` 收敛 | T1 | `runtime_15_test_layout_single_rule` |
| **M4 行数硬上限** | T1 与 `large_file_ownership_gate` 联动收残余 | 各 owner 拆分；`core/runtime/handle/registration/service_lists.rs` 已硬切为 folder-backed `core/runtime/handle/registration/service_lists/{mod,types,multi,specialized,shutdown}.rs` | M1-M3 | 复用 `large_file_ownership_gate`（`migration-debt → 0`）；`runtime_15_core_runtime_service_lists_are_folder_backed` |
| **M5 prelude 完整化 + 死代码清除** | T1 prelude 增补 asset/scene/ecs/ui 高频类型 + "prelude 必含"清单 | `prelude.rs`、各子系统 `prelude.rs` | M1 | `runtime_15_prelude_covers_required_types`（F9） |
| | T2 `#[allow(dead_code)]` sweep 删抑制暴露真僵尸（含 `runtime_ui`、shading registry） | `ui/mod.rs`、55 文件；`runtime_ui` 已拆为 `ui/public_runtime_frame.rs` + `ui/tests/runtime_ui_support`；asset worker/core module-entry runtime-owned 子切片已清理；script host `Vec3` / `ColorRgba` descriptor 子切片已清理；OffscreenTarget、RenderBackend、GpuTextureResource、GpuMaterialUniformResource、GpuMeshResource order signature、GpuModelResource identity、PostProcessLutTextureResource owner、OutputTargetTextureResource/PreparedOutputTargetTexture owner、MaterialRuntime capture seed/test texture sampling、ResourceStreamer diagnostics accessor、ResourceStreamer resolve texture id helper 与 particle GPU readback output accessor 子切片已清理；shading registry F11 子切片已由 render 收口 | — | `runtime_15_no_dead_code_suppression_in_production`（F10/F12）；`runtime_15_runtime_ui_dead_code_surface_is_test_support`；`runtime_15_runtime_owned_dead_code_suppression_cleanup`；`runtime_15_script_host_value_descriptors_do_not_suppress_dead_code`；`runtime_15_offscreen_target_texture_owner_cleanup`；`runtime_15_render_backend_state_owner_cleanup`；`runtime_15_gpu_texture_resource_owner_cleanup`；`runtime_15_gpu_material_uniform_owner_cleanup`；`runtime_15_gpu_mesh_order_signature_cleanup`；`runtime_15_gpu_model_identity_cleanup`；`runtime_15_post_process_lut_texture_owner_cleanup`；`runtime_15_output_target_texture_owner_cleanup`；`runtime_15_material_runtime_capture_seed_cleanup`；`runtime_15_resource_streamer_diagnostics_accessor_cleanup`；`runtime_15_resource_streamer_resolve_texture_id_cleanup`；`runtime_15_particle_gpu_readback_output_accessor_cleanup` |
| | T3 抽 `RuntimeProviderRegistration<P>` / `RuntimeProviderUpdate<S>` / `RuntimeProviderFeedback<G, V>` / `RuntimeProviderPrepareInput<'a, E>` 泛型 + 统一 `*Diagnostics` | `graphics/*_runtime_provider/*`、`graphics/runtime_provider/*`、`core/runtime/diagnostics/frame_diagnostics.rs`、`scene/world/performance_diagnostics.rs` | — | `runtime_15_no_duplicated_provider_boilerplate`（F13）；`runtime_15_provider_update_uses_shared_stats_owner`（F13）；`runtime_15_provider_feedback_uses_shared_payload_owner`（F13）；`runtime_15_provider_prepare_input_uses_shared_extract_generation_owner`（F13）；`runtime_15_diagnostics_use_frame_trait_without_world_wrapper`（F14） |

## 5. 审计与 guard 契约（本计划新增）

- **审计脚本**（进 `runtime_structure_audits/`，由 `audit_runtime_structure.py` 聚合）：`module_convention_gate.py` + `module_convention_gate_markdown.py`。字段：
  - `facade_reexport_line_budget`、`oversized_facade_files`
  - `mixed_visibility_mod_files`
  - `prefix_vocabulary_violations`、`plural_singular_violations`、`banned_name_modules`
  - `module_rs_without_descriptor`（R1.2 违例）
  - `oversized_test_files`、`duplicate_test_trees`
  - `module_convention_gate.m1_gate_status`、`classification_counts`、`migration_debt_count`、`exempt`
- **guard 测试**：`zircon_runtime/src/tests/runtime_absorption/structure_convention.rs`（沿用 runtime_absorption 范式），含 `runtime_15_module_convention_mirror_docs_match_structure_audit_counts`。
- **镜像文档**：`docs/zircon_runtime/structure/module-convention.md`，计数与审计一致。

## 6. 硬切 checklist（每个含删除义务的切片必过）

- [ ] 旧路径文件 / 模块已物理删除（不是注释 / feature 门）
- [ ] 全部调用方已迁移到新 owner / façade 路径（grep 旧符号零命中）
- [ ] 无兼容 re-export / facade 桥残留
- [ ] 同一职责无双路径并存
- [ ] 删除清单写进提交说明

## 7. 完成定义

`module_convention_gate.m1_gate_status = classified-and-clear`、各 violation 字段 = 0、`exempt` 仅含登记豁免项、`runtime_15_module_convention_mirror_docs_match_structure_audit_counts` 绿；`cargo test -p zircon_runtime --lib`（按过滤词收窄）+ `cargo fmt --all --check` + `python .../audit_runtime_structure.py --json` 无 risk。

## 状态与产出记录

| 里程碑 | 切片 | 状态 | 完成日期 | 证据 |
|---|---|---|---|---|
| M3 | Runtime 15 M3 status output Runtime 15 M2 row data split | runtime_15_status_output_runtime_15_m2_row_data_split_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 status-output row-data follow-up：`plan_status/status_output_tables/expected_status_row_data/runtime_15/foundation.rs` 已从 784 行减压到 394 行，新增 `plan_status/status_output_tables/expected_status_row_data/runtime_15/m2.rs`（403 行）专门承接 Runtime 15 M2 module naming / hard-cutover status rows，并把本切片锚 `Runtime 15 M3 status output Runtime 15 M2 row data split` 写入 status-output expectations。`plan_status/status_output_tables/expected_status_row_data/runtime_15.rs` 与顶层 row group list 现在显式挂载 `RUNTIME_15_M2_EXPECTED_STATUS_OUTPUT_SLICES`；`runtime_15_status_output_runtime_15_m2_row_data_is_child_owner` 锁定新 child owner、旧 foundation owner 不回流 M2 row literals、foundation/M2 owner 均低于 800 行预算，以及 Runtime 15/index/review findings/structure/module docs/status-output expectations 的状态锚同步。该切片只整理测试/状态 row-data owner，不改 runtime/render/plugin/editor 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 lock-poison status row-data child-owner split | runtime_15_lock_poison_status_row_data_child_owner_split_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 status-output row-data follow-up：`plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/foundation_guards.rs` 已从 663 行继续减压到 390 行，新增 `plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/lock_poison_status.rs`（291 行）专门承接 lock-poison policy guard、core/runtime handle recovery、plugin/native bridge recovery、dynamic/session/scene/asset/render/input/script VM lock-poison recovery rows。`plan_status/status_output_tables/expected_status_row_data/runtime_15/m3.rs`、`runtime_15.rs` 与顶层 row group list 现在显式挂载 `LOCK_POISON_STATUS_EXPECTED_STATUS_OUTPUT_SLICES` / `RUNTIME_15_M3_LOCK_POISON_STATUS_EXPECTED_STATUS_OUTPUT_SLICES`；`runtime_15_status_output_m3_row_data_child_owner_split` 扩展为锁定新 child owner、旧 foundation owner 不回流 lock-poison row literals、各 owner 低于 800 行预算，以及 Runtime 15/index/review findings/structure/module docs/status-output expectations 的状态锚同步。该切片只整理测试/状态 row-data owner，不改 runtime/plugin/render 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 module-convention status row-data child-owner split | runtime_15_module_convention_status_row_data_child_owner_split_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 status-output row-data follow-up：`plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/foundation_guards.rs` 已从 729 行继续减压到 663 行，新增 `plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/module_convention_status.rs`（84 行）专门承接 module convention gate output/non-render debt、render-scoped migration debt handoff、hard-cutover Hyper risk、audit-clear mirror 与 audit script family naming cleanup rows。`plan_status/status_output_tables/expected_status_row_data/runtime_15/m3.rs`、`runtime_15.rs` 与顶层 row group list 现在显式挂载 `MODULE_CONVENTION_STATUS_EXPECTED_STATUS_OUTPUT_SLICES` / `RUNTIME_15_M3_MODULE_CONVENTION_STATUS_EXPECTED_STATUS_OUTPUT_SLICES`；`runtime_15_status_output_m3_row_data_child_owner_split` 扩展为锁定新 child owner、旧 foundation owner 不回流 module-convention row literals、各 owner 低于 800 行预算，以及 Runtime 15/index/review findings/structure/module docs/status-output expectations 的状态锚同步。该切片只整理测试/状态 row-data owner，不改 runtime/plugin/render 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 review top-row status row-data child-owner split | runtime_15_review_top_row_status_row_data_child_owner_split_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 status-output row-data follow-up：`plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/foundation_guards.rs` 已从 883 行减压到 729 行，新增 `plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/review_status_sync.rs`（172 行）专门承接 D13、D-S8/D3、P0 F1/F2/F4、D7、F5/F6/F7、F8/F9/F10、F13/F14、F17/F18 与 F19 的 review top-row closed status sync rows。`expected_status_row_data/runtime_15/m3.rs`、`runtime_15.rs` 与顶层 row group list 现在显式挂载 `REVIEW_STATUS_SYNC_EXPECTED_STATUS_OUTPUT_SLICES` / `RUNTIME_15_M3_REVIEW_STATUS_SYNC_EXPECTED_STATUS_OUTPUT_SLICES`；`runtime_15_status_output_m3_row_data_child_owner_split` 扩展为同时锁定新 child owner、旧 foundation owner 不回流 review top-row row literals、各 owner 低于 800 行预算，以及 Runtime 15/index/review findings/structure/module docs/status-output expectations 的状态锚同步。该切片只整理测试/状态 row-data owner，不改 runtime/render/plugin 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 F13/F14 provider diagnostics top-row closed status sync | f13_f14_provider_diagnostics_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 F13/F14 顶表行正文已经列出 provider registration/update/feedback/prepare-input shared owners 和 `FrameDiagnostics` trait + direct ECS diagnostics owner 闭合锚，本轮把两行状态列分别同步为 `convention + Runtime 15 / review closed` 与 `Runtime 15 / review closed`。`structure_convention/provider_boilerplate/full_audit.rs::runtime_15_no_duplicated_provider_boilerplate` 与 `structure_convention/diagnostics_surface.rs::runtime_15_diagnostics_use_frame_trait_without_world_wrapper` 现在要求各自顶表行包含本状态锚并以对应 review closed 状态结尾，status-output expectations 同步锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/graphics/diagnostics 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 F17/F18 lookup/manager top-row closed status sync | f17_f18_lookup_manager_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 F17/F18 顶表行正文已经列出 entity-path Option lookup `get_entity_by_path(...)` 硬切和 asset manager resolution registered-handle shape 闭合锚，本轮把两行状态列分别同步为 `convention + Runtime 08 / review closed` 与 `Runtime 10 / review closed`。`tests/runtime_absorption/code_review_findings/late_api_cleanup.rs::review_f17_entity_path_option_lookup_uses_get_verb` 与 `review_f18_asset_manager_resolution_returns_registered_handle` 现在要求各自顶表行包含本状态锚并以对应 review closed 状态结尾，status-output expectations 同步锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/asset/scene 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 F19 scene renderer construction top-row closed status sync | f19_scene_renderer_construction_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 F19 顶表行正文已经列出 scene renderer construction owner 从 `_new` 后缀硬切为 `scene_renderer_core_construct` / `scene_renderer_construct` 的闭合锚，本轮把状态列同步为 `convention + render index / review closed`。`tests/runtime_absorption/code_review_findings/late_api_cleanup.rs::review_f19_scene_renderer_construction_modules_use_construct_names` 现在要求 F19 顶表行包含本状态锚并以 review closed 状态结尾，status-output expectations 同步锁定本状态。该切片只同步审查状态列和结构镜像，不改 render 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 D10 animation/physics bridge call migration | d10_animation_physics_bridge_call_static_passed_cargo_deferred | 2026-06-28 | Plugins 11/12 + Runtime 15 DX review follow-up：`zircon_runtime/src/core/framework/physics/query_interface.rs` 新增 runtime-owned `PhysicsQueryInterface` / `PHYSICS_QUERY_INTERFACE_ID = "physics.query.v1"`，`zircon_plugins/plugin_sdk/src/registration.rs` 新增 module-scoped `export_interface<T>`，`zircon_plugins/physics/runtime/src/plugin.rs` 在 descriptor 中声明并在注册期导出 `physics.query.v1`，`module_descriptor_with_manager(...)` 让 bridge provider 与 runtime physics manager 共享同一 `DefaultPhysicsManager` 实例。`zircon_plugins/animation/runtime/tests/runtime_physics_animation_tick_contract/runtime_helpers.rs` 现在从 `TestRuntime::extension_report().registry.frozen_bridge_table()` 解析 `WeakBridge<dyn PhysicsQueryInterface>`，主合约测试通过 bridge `.call(...)` 覆盖 ray cast、shape overlap 与 shape cast，不再解析 concrete `DefaultPhysicsManager`。新增 `tests/runtime_absorption/code_review_findings/plugin_importer_dx/d10_bridge_call.rs::review_d10_animation_physics_tests_use_sdk_bridge_call`，并由 `runtime_15_code_review_findings_tests_are_folder_backed` 锁定 child owner 和 code-review guard 总数 56。Cargo gate 按实施切片节奏 deferred，不计通过。 |
| M3 | Runtime 15 M3 D11 animation/physics TestRuntime fixture migration | d11_animation_physics_test_runtime_fixture_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up：`zircon_plugins/animation/runtime/tests/runtime_physics_animation_tick_contract.rs` 现在通过 `zircon_plugin_sdk::TestRuntime::builder()` 和 `.with_runtime_plugin(...)` 搭建 animation+physics runtime，测试侧只调用 `runtime.create_default_level()` 与 `runtime.tick_level_seconds(...)`，不再本地手写 CoreRuntime、Scene 模块、fixed timestep 或 world extension 安装。fixture 构造拆入 `runtime_physics_animation_tick_contract/animation_assets.rs`，runtime helper 拆入 `runtime_physics_animation_tick_contract/runtime_helpers.rs`，主测试 981 行、子 owner 288/33 行。新增 `tests/runtime_absorption/code_review_findings/plugin_importer_dx/d11_test_runtime_fixture.rs::review_d11_animation_physics_tests_use_sdk_test_runtime_fixture`，并由 `runtime_15_code_review_findings_tests_are_folder_backed` 锁定 child owner 和 code-review guard 总数 55。Cargo gate 按实施切片节奏 deferred，不计通过。 |
| M3 | Runtime 15 M3 D1 capability single-source review sync | d1_capability_single_source_review_synced_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up；状态短名：`D1 capability single-source review/status sync`。本轮复核 D1 capability single-source 与 SDK builder mirror 现状，只补状态镜像和静态守卫，不改插件生产行为。15 个 trait-backed first-party runtime roots 继续以 `runtime/src/capability.rs` 的 `RUNTIME_CAPABILITIES` 投影 descriptor/manifest/accessor；`tools/audit_plugin_structure.py --json` 锁定 `plugins_12_runtime_capability_single_source_guard_passed`、`plugins_12_capability_single_source_conformance`、`m4_runtime_capability_gate_status = runtime-capability-single-source-clean`、`capability_source_mismatches = 0`、`m4_t2_builder_mirror_gate_status = sdk-builder-mirror-clean`、`sdk_builder_mirror_violations = 0`。新增 `tests/runtime_absorption/code_review_findings/plugin_importer_dx/d1_capability_single_source.rs::review_d1_plugin_capabilities_use_single_source_and_sdk_builder_mirror`，并由 `runtime_15_code_review_findings_tests_are_folder_backed` 锁定 child owner、父文件只挂载和 code-review guard 总数 54。Cargo gate 按实施切片节奏 deferred，不计通过。 |
| M3 | Runtime 15 M3 D5 editor authoring macro consumer guard | d5_editor_authoring_macro_consumers_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up：`zircon_plugins/plugin_sdk/src/editor.rs` 的 `zircon_plugin_sdk::authoring_plugin!` 现在接受 `mirrors_runtime_manifest:`，并生成 editor plugin struct、`Default`/`new`、declaration access、package manifest projection、registration report helper 与 `zircon_editor::EditorPlugin` impl。`zircon_plugins/animation/editor/src/plugin.rs`、`zircon_plugins/physics/editor/src/plugin.rs`、`zircon_plugins/net/editor/src/plugin.rs` 均改用 SDK macro，只保留 plugin-specific `register_*_extensions` body；`review_d5_editor_authoring_plugins_use_sdk_macro` 与 status-output expectations 锁定该状态。Cargo gate 仍按本切片 validation 记录；该切片只关闭 animation/physics/net editor authoring macro consumers，不声明 broader plugin SDK rollout 全量完成。 |
| M3 | Runtime 15 M3 D9 editor/runtime mirror consumer guard | d9_editor_runtime_mirror_consumers_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up：`zircon_plugins/animation/editor/src/plugin.rs`、`zircon_plugins/physics/editor/src/plugin.rs`、`zircon_plugins/net/editor/src/plugin.rs` 现在通过 `zircon_plugin_sdk::authoring_plugin!` 的 `mirrors_runtime_manifest:` 进入 `EditorPluginDeclaration::mirrors_runtime_manifest` projection 并 mirror runtime manifest；对应 editor tests 断言 `mirrored_runtime_package_id()`，并验证 package manifest 同时包含 runtime/editor capability。`tools/audit_plugin_structure.py --json` 当前报告 `editor_runtime_mirror_violations = 0`、`d9_editor_runtime_mirror_gate_status = editor-runtime-mirror-clean`；`review_d9_editor_runtime_mirror_consumers_use_sdk_declaration` 与 status-output expectations 锁定该状态。Cargo gate 仍按本切片 validation 记录；该切片只关闭 animation/physics/net editor/runtime mirror consumers，不声明 broader editor capability rollout 全量完成。 |
| M3 | Runtime 15 M3 D8 runtime registration builder original evidence paths | d8_runtime_registration_builder_original_paths_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up：`RuntimePluginRegistrationBuilder` / `RuntimePluginModuleRegistration` 已覆盖 `animation`、`physics`、`net` 三个 D8 原始证据 root。`plugin_sdk::registration` 新增 module-scoped `event(...)`、`plugin_option(...)` 与 `plugin_event_catalog(...)` helper；三个 runtime plugin owner 均通过 SDK builder 注册 module，并通过 module handle 注册 scene systems，net typed event 也不再直接传 `PluginModuleId`。`tools/plugin_structure_audits/registration.py` 新增 `D8_RUNTIME_REGISTRATION_ROOTS` guard，`tools/audit_plugin_structure.py --json` 当前报告 `runtime_registration_builder_violation_count = 0`、`m3_t2_runtime_registration_builder_status = runtime-registration-builder-clean`；`review_d8_runtime_registration_builder_original_evidence_paths_use_sdk_builder` 与 status-output expectations 锁定该状态。Cargo gate 仍按本切片 validation 记录；该切片不声明 importer private registry mutation 或 module-only registration 全量清零。 |
| M3 | Runtime 15 M3 D6 RuntimePluginId open string-newtype review sync | d6_runtime_plugin_id_open_string_newtype_review_static_passed_cargo_deferred | 2026-06-28 | Plugins 12/13 M5 与 Runtime 15 DX review follow-up：`RuntimePluginId` 当前为开放 string-newtype，`zircon_runtime/src/builtin/runtime_modules/ids/plugin_id.rs` 保留 first-party associated constants，但 `parse_key(...)` 对合法第三方 key 走 `intern_runtime_plugin_key(...)` fallback，不再要求新增 core enum 分支。新增 `tests/runtime_absorption/code_review_findings/plugin_importer_dx/d6_runtime_plugin_id.rs::review_d6_runtime_plugin_id_accepts_external_string_keys`，锁定 `runtime_plugin_id_accepts_external_keys_without_core_variant`、`plugin_modules/loader.rs` 的 `_ => externalized_runtime_plugin_module(id.key(), warnings)` 外部化诊断路径、`docs/plans/engine-code-review-findings-2026-06.md` 的 D6 顶表行、runtime index、module-convention docs、status-output expectations 与 session note。该切片只同步审查/状态镜像，不改 runtime/plugin 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 D13 importer manifest parity guard | d13_importer_manifest_parity_guard_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up：D13 importer manifest builder 已增加 SDK 输出 parity 守卫。`tests/runtime_absorption/code_review_findings/plugin_importer_dx/d13_importer_sdk.rs::review_d13_importer_manifest_parity_guard_lives_in_sdk_builder` 要求 `zircon_plugins/plugin_sdk/src/manifest/tests.rs::importer_runtime_manifest_builder_keeps_targets_platforms_modules_and_distribution_in_parity` 覆盖 `ImporterRuntimeManifestBuilder` 的 shared targets/platforms、runtime module、dist module、NativeDynamic distribution、`NATIVE_ABI_VERSION_V3` 与 `NATIVE_DESCRIPTOR_SYMBOL_V3`；`zircon_plugins/plugin_sdk/src/lib.rs` 与 `prelude.rs` re-export 这些 ABI v3 constants，防止 importer crate 恢复本地常量。`runtime_15_code_review_findings_tests_are_folder_backed`、status-output expectations、review findings、Plugins 12、plugin SDK、asset importer skeleton、runtime index、module-convention docs 与 session note 已同步该状态；Cargo gate deferred。 |
| M3 | Runtime 15 M3 P0/DX priority D13 parity sync | review_priority_recommendation_d13_parity_sync_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review priority follow-up：`docs/plans/engine-code-review-findings-2026-06.md` §5 现在把 D13 importer manifest parity guard 纳入当前闭合链，不再只列 D13 export macro 与 manifest builder。`tests/runtime_absorption/code_review_findings/p0_robustness.rs::review_priority_recommendation_tracks_current_remaining_work` 要求优先级段落包含 `d13_importer_manifest_parity_guard_static_passed_cargo_deferred`、`review_d13_importer_manifest_parity_guard_lives_in_sdk_builder` 和 `importer_runtime_manifest_builder_keeps_targets_platforms_modules_and_distribution_in_parity`，并由 status-output expectations 锁定本状态。该切片只同步优先级/状态文本，不改 runtime/plugin 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 D13 importer top-row closed status sync | d13_importer_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 D13 顶表行正文已经列出 runtime export macro、manifest builder 与 manifest parity guard 三条闭合锚，本轮把该行状态列同步为 `M3 / closed`。`tests/runtime_absorption/code_review_findings/plugin_importer_dx/d13_importer_sdk.rs::review_d13_importer_manifest_parity_guard_lives_in_sdk_builder` 现在要求 D13 顶表行以 `| M3 / closed |` 结尾，并由 status-output expectations 锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/plugin 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 D-S8/D3 native fixture top-row closed status sync | ds8_d3_native_fixture_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 D-S8/D3 顶表行正文已经列出 native dynamic fixture SDK macro、plugin.toml 单源 manifest 与 importer self-description 闭合锚，本轮把两行状态列分别同步为 `Plugins 13 M2 + Plugins 12 / closed` 与 `Plugins 13 M1 + Plugins 12 / closed`。`tests/runtime_absorption/code_review_findings/p0_robustness.rs::review_ds8_d3_native_fixture_uses_sdk_macro_and_single_manifest` 现在要求两行以 closed 状态结尾，并由 status-output expectations 锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/plugin 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 P0 F1/F2/F4 top-row closed status sync | p0_f1_f2_f4_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` §5 已把 F1/F2/F4 归为“已有闭合守卫”，本轮把三条 P0 顶表行状态列同步为 `Runtime 15 + Runtime 06 + Plugins 11 / review closed`、`Runtime 15 + Runtime 07 / review closed` 与 `Runtime 07 + render index / review closed`。`tests/runtime_absorption/code_review_findings/p0_robustness.rs` 中的 F1/F2/F4 review guards 现在要求三行以 review closed 状态结尾，并由 status-output expectations 锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/render/plugin 生产代码；F4 完整 Runtime 07 FPS/profiling/full gate 仍 pending；Cargo gate deferred。 |
| M3 | Runtime 15 M3 D7 core workspace dependency top-row closed status sync | d7_core_workspace_dependency_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 D7 顶表行正文已经列出 core workspace dependency inheritance 审计、117 个核心依赖继承、0 violation、`zircon_plugins/Cargo.lock` 离线解析和插件间 path 依赖仍归后续切片；本轮把该行状态列同步为 `M2 / closed`。`tests/runtime_absorption/code_review_findings/p0_robustness.rs::review_priority_recommendation_tracks_current_remaining_work` 现在要求 D7 顶表行包含 `d7_core_workspace_dependency_top_row_closed_status_static_passed_cargo_deferred` 并以 `| M2 / closed |` 结尾，status-output expectations 同步锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/plugin 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 F5/F6/F7 typed-error top-row closed status sync | f5_f6_f7_typed_error_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 F5/F6/F7 顶表行正文已经列出 ECS mutation `SceneResult`、core/resource rename `CoreResult` 与 asset artifact/importer `AssetImportError` typed source 闭合锚，本轮把三行状态列分别同步为 `Runtime 08 + Runtime 15 / review closed`、`Runtime 02 / review closed` 与 `Runtime 04 / review closed`。`tests/runtime_absorption/code_review_findings/typed_error_convergence/scene_world.rs::review_f5_world_spawn_bundle_surface_uses_scene_error`、`animation_resource.rs::review_f6_core_resource_registry_rename_uses_core_error` 与 `asset_loaders.rs::review_f7_asset_artifact_errors_use_asset_import_error_sources` 现在要求各自顶表行包含本状态锚并以对应 review closed 状态结尾，status-output expectations 同步锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/asset/core/scene 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 F8/F9/F10 runtime surface top-row closed status sync | f8_f9_f10_runtime_surface_top_row_closed_status_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + engine review status follow-up：`docs/plans/engine-code-review-findings-2026-06.md` 的 F8/F9/F10 顶表行正文已经列出 RuntimePluginDescriptor builder/private-field/constructor cleanup、runtime prelude 高频类型覆盖与 runtime UI production/test-support split 的闭合锚，本轮把三行状态列分别同步为 `convention + Runtime 04 + Runtime 06 + Runtime 15 / review closed`、`Runtime 15 / review closed` 与 `Runtime 09 + Runtime 15 / review closed`。`tests/runtime_absorption/code_review_findings/f8_api_convergence.rs::review_f8_runtime_plugin_descriptor_status_mirrors_do_not_claim_public_field_pending`、`tests/runtime_absorption/structure_convention/facade_surface.rs::runtime_15_prelude_covers_required_types` 与 `tests/runtime_absorption/structure_convention/runtime_dead_code.rs::runtime_15_runtime_ui_dead_code_surface_is_test_support` 现在要求各自顶表行包含本状态锚并以对应 review closed 状态结尾，status-output expectations 同步锁定本状态。该切片只同步审查状态列和结构镜像，不改 runtime/plugin/asset/ui 生产代码；Cargo gate deferred。 |
| M3 | Runtime 15 M3 D7 core workspace dependency inheritance guard | d7_core_workspace_dependency_inheritance_guard_static_passed_cargo_deferred | 2026-06-28 | Plugins 12 / Runtime 15 DX review follow-up：`zircon_plugins/Cargo.toml` 继续作为核心 path dependency 单源，所有 plugin member 中 `zircon_runtime`、`zircon_editor`、`zircon_runtime_interface` 本地 `path = ...` 声明已改为 `workspace = true`，`zircon_plugins/Cargo.lock` 已随离线解析同步。`tools/plugin_structure_audits/skeleton.py` 新增全局 `core_workspace_dependency_status` guard，`tools/audit_plugin_structure.py` markdown/JSON 输出当前 `core_workspace_dependency_status = core-workspace-deps-clean`、`core_workspace_dependency_count = 117`、`core_workspace_dependency_violation_count = 0`；`zircon_first_party_runtime_catalog::tests::plugins_12_crate_skeleton_conformance` 与 status-output expectations 锁定该状态。验证：审计 JSON/markdown、py_compile、rustfmt、静态 path 扫描与 `cargo metadata --manifest-path zircon_plugins/Cargo.toml --locked --offline` 通过；catalog Cargo 测试仍因依赖图编译超出窗口而 deferred，不计通过。该切片只关闭核心引擎依赖继承，不声明插件间 path 依赖清零。 |
| M3 | Runtime 15 M3 module convention audit script family naming cleanup | runtime_15_module_convention_audit_script_family_naming_core_min_cargo_passed_full_sweep_pending | 2026-06-27 | R4.3/M3 follow-up：将 Runtime 15 相关代码清单、§5 审计脚本族和全局结构规范 §7 中的旧 `module_convention_boundary.py` / `module_convention_markdown.py` 描述硬切为当前实际 owner：`module_convention_gate.py` / `module_convention_gate_markdown.py`，并同步 runtime index、review findings、module-convention docs、status-output expectations 与 session note。新增 `structure_convention/module_convention_gate.rs::runtime_15_module_convention_audit_script_family_uses_gate_names`，锁定 `module_convention_boundary.py zero hits`、`module_convention_markdown.py zero hits` 和跨文档状态锚。验证：rustfmt/static stale-name scan/diff-check 通过；core-min 聚焦 Cargo 守卫测试通过，1 passed、0 failed；`status_output_tables` 过滤测试 2 passed、0 failed；全量 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 module convention gate audit-clear status mirror | runtime_15_module_convention_gate_audit_clear_status_mirror_core_min_cargo_passed_full_sweep_pending | 2026-06-27 | R4.3/M3 follow-up：在 hard-cutover allowed Hyper policy risk cleanup 后，当前 runtime structure audit 已达到 `module_convention_gate audit clear`：`m1_gate_status=classified-and-clear`、`migration_debt_count=0`、render-scoped migration debt 0、non-render migration debt 0、`risk_count=0`、`risks=[]`。本切片只收敛当前状态镜像，把 runtime index 与 module-convention 镜像文档中旧的 gate-pending 摘要改为 audit-clear + `全量 Cargo sweep 仍 pending`；新增 `structure_convention/module_convention_gate.rs::runtime_15_module_convention_gate_audit_clear_is_status_locked` 锁定审计字段、状态行、跨文档锚和 stale 摘要不回流。验证：direct audit summary/rustfmt/static anchor/conflict/diff-check 通过；随后 `cargo test -p zircon_runtime --lib runtime_15_module_convention_gate_audit_clear_is_status_locked --no-default-features --features core-min --locked --target-dir E:\cargo-targets\zircon-runtime-module-gate-audit-clear-0627 --message-format short --color never` 通过，1 passed、0 failed；同目标目录 `status_output_tables` 过滤测试 2 passed、0 failed；全量 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 editor retained-host pane data conversion projection owner guard | runtime_15_editor_retained_host_pane_data_conversion_owner_guard_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3 + F15：当前 editor retained-host `pane_data_conversion` 已处于 folder-backed owner 形态，本切片新增结构守卫把该闭合状态固定下来：`zircon_editor/src/ui/retained_host/ui/pane_data_conversion/mod.rs` 只保留 projection 子模块挂载与窄 re-export，`zircon_editor/src/ui/retained_host/ui/pane_data_conversion/template_node_projection.rs` 独占 `project_nodes<T, F>` / `project_node_vec<T, F>` 通用节点映射，`zircon_editor/src/ui/retained_host/ui/pane_data_conversion/animation_projection.rs` 独占 animation sequence/graph payload 投影，`zircon_editor/src/ui/retained_host/ui/apply_presentation/pane_conversion.rs` 独占 `to_host_contract_pane(...)` pane routing。新增 `structure_convention/test_file_budget/editor_pane_data_conversion.rs::runtime_15_editor_retained_host_pane_data_conversion_uses_child_projection_owners`，锁定旧 F15 超长 owner 不回流、四侧均低于 800 行预算，并同步 Runtime 15/status/structure/module/editor workbench docs 镜像锚。验证：scoped rustfmt/static scans、owner anchor 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。该记录关闭 F15 原始 `pane_data_conversion` 超长样板复制的结构守卫子切片；完整 `module_convention_gate` 仍因 graphics/hard-cutover 等剩余迁移债 pending。 |
| F5 | Runtime 15 F5 texture loader typed errors | runtime_15_texture_loader_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5：`asset/load/texture.rs` CPU texture loader 已从 `Result<CpuTexturePayload, String>` / `format!("open image ...")` 收敛到 `TextureLoadError` / `TextureLoadResult`，`TextureLoadError::OpenImage` 保留 `image::ImageError` source；`asset/pipeline/worker_pool.rs` 只在 `CpuAssetPayload::Failure { message }` 出口执行 `error.to_string()`，`asset/tests/load/texture.rs::missing_image_file_reports_typed_texture_load_error` 覆盖缺失图片 typed failure。新增 `review_f5_texture_loader_uses_typed_error` 并同步 status-output expectations。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active deferred，不计通过。 |
| F5 | Runtime 15 F5 mesh loader typed errors | runtime_15_mesh_loader_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5：`asset/load/mesh.rs` CPU mesh loader 已从 `Result<CpuMeshPayload, String>` / `Err(format!(...))` 收敛到 `MeshLoadError` / `MeshLoadResult`，`MeshLoadError::UnsupportedFormat` 表达 unsupported extension，`MeshLoadError::Obj` 保留 `ObjDecodeError` source；`asset/formats/obj/error.rs` 新增 `ObjDecodeError` / `ObjDecodeResult`，OBJ read/scalar/index/face/empty-mesh failures 均改为 typed variants。`asset/pipeline/worker_pool.rs` 只在 mesh `CpuAssetPayload::Failure { message }` 出口 stringify；`asset/tests/load/mesh.rs::unsupported_mesh_file_reports_typed_mesh_load_error` 与 `asset/tests/formats/obj.rs::{obj_decode_reports_typed_read_error_source,obj_decode_reports_typed_scalar_parse_error_source}` 覆盖 typed failures。新增 `review_f5_mesh_loader_and_obj_decoder_use_typed_errors` 并同步 status-output expectations。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active deferred，不计通过。 |
| M1 | Runtime 15 M1 animation manager folder-backed cutover | runtime_15_animation_manager_folder_backed_cutover_static_passed_cargo_deferred | 2026-06-24 | R1.2/M1：`animation/manager.rs` 已删除并硬切为 `animation/manager/mod.rs`；`animation/mod.rs` 继续通过 `mod manager;` 与 `pub use manager::DefaultAnimationManager;` 暴露同一 facade，`animation/manager/mod.rs` 只挂载 `graph`、`parameters`、`pose`、`sampling` 与 `state_machine` 子 owner 并拥有 `DefaultAnimationManager` / `AnimationManager` impl，未保留旧平铺模块、alias 或兼容 re-export。新增 `structure_convention/animation_manager.rs::runtime_15_animation_manager_is_folder_backed`，验证旧文件不存在、新 folder root/child owner 形状、行数预算，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、animation runtime docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与 facade/child-owner 扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 animation manager 的 `manager.rs` + `manager/` 共存债；完整 `module_convention_gate` 与 M1 full structure sweep 仍 pending。 |
| M2 | Runtime 15 M2 core runtime state module naming hard cutover | runtime_15_core_runtime_state_module_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-24 | R2.3/M2：`core/runtime/state/runtime_inner.rs` 已删除并硬切为 `core/runtime/state/core_runtime_state.rs`，`core/runtime/state/mod.rs` 只声明 `mod core_runtime_state;` 并重导出 `CoreRuntimeInner`，未保留旧模块、alias 或兼容 re-export。`core/runtime/tests/registration/structure/mod.rs` 的 source fixture 改为 `runtime_state` 并读取 `../../../state/core_runtime_state.rs`，`module_layout.rs` / `cleanup.rs` 继续锁定 registry table 与 legacy cleanup 边界。新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_core_runtime_state_module_uses_owner_name`，验证旧文件不存在、新 owner/入口/registration fixture 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、core state docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 core runtime state 文件名的 `_inner` 债；完整 `runtime_15_no_banned_name_modules`、ECS `utils.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 scene ECS observer callback registry module naming hard cutover | runtime_15_scene_ecs_observer_callback_registry_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-24 | R2.3/M2：`scene/ecs/observer/utils.rs` 已删除并硬切为 `scene/ecs/observer/callback_registry.rs`，`scene/ecs/observer/mod.rs` 只声明 `mod callback_registry;`，`scene/ecs/observer/store.rs` 只从该 owner 读取 callback count 与 observer removal helpers，未保留旧 `utils` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_scene_ecs_observer_callback_registry_uses_owner_name`，验证旧文件不存在、新 owner/入口/store import 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、scene ECS docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 scene ECS observer callback registry 的 `utils` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 ECS `utils.rs` / `helpers.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 scene ECS query-state many-item array module naming hard cutover | runtime_15_scene_ecs_query_state_many_item_array_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`scene/ecs/query/query_state/helpers.rs` 已删除并硬切为 `scene/ecs/query/query_state/many_item_array.rs`，`scene/ecs/query/query_state/mod.rs` 只声明 `mod many_item_array;`，`cached_direct.rs`、`mutable.rs`、`read_only.rs` 与 `read_only_cached.rs` 只从该 owner 消费 `collect_many_query_items`，未保留旧 `helpers` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_scene_ecs_query_state_many_item_array_uses_owner_name`，验证旧文件不存在、新 owner/入口/调用方 import 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、scene ECS docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 scene ECS query-state many-item array 的 `helpers` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 ECS `utils.rs` / `helpers.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 scene ECS component-storage component results module naming hard cutover | runtime_15_scene_ecs_component_storage_component_results_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`scene/ecs/storage/component_storage/utils.rs` 已删除并硬切为 `scene/ecs/storage/component_storage/component_results.rs`，`scene/ecs/storage/component_storage/mod.rs` 只声明 `mod component_results;`，`store.rs` 只从该 owner 消费 `downcast_component` 与 `sort_component_ids_if_needed`，未保留旧 `utils` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_scene_ecs_component_storage_component_results_uses_owner_name`，验证旧文件不存在、新 owner/入口/store import 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、scene ECS docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 scene ECS component-storage component results 的 `utils` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 ECS `utils.rs` / `helpers.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 asset watcher shutdown-on-drop module naming hard cutover | runtime_15_asset_watcher_shutdown_on_drop_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`asset/watch/drop_impl.rs` 已删除并硬切为 `asset/watch/shutdown_on_drop.rs`，`asset/watch/mod.rs` 只声明 `mod shutdown_on_drop;`，该 owner 继续承接 `impl Drop for AssetWatcher`、stop signal 发送与 watcher thread join，未保留旧 `drop_impl` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/asset_dynamic.rs::runtime_15_asset_watcher_shutdown_on_drop_uses_owner_name`，验证旧文件不存在、新 owner/入口形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、asset watcher docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 asset watcher shutdown-on-drop 的 `_impl` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 `utils.rs` / `helpers.rs` / `_impl.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 asset change construction module naming hard cutover | runtime_15_asset_change_construction_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.5/M2：`asset/watch/asset_change_new.rs` 已删除并硬切为 `asset/watch/asset_change_construction.rs`，`asset/watch/mod.rs` 只声明 `mod asset_change_construction;`，该 owner 继续承接 `impl AssetChange { pub fn new(...) }` 的构造逻辑，`fold_events.rs` 继续通过 `AssetChange::new(...)` 消费同一 API，未保留旧 `*_new` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/asset_dynamic.rs::runtime_15_asset_change_construction_uses_owner_name`，验证旧文件不存在、新 owner/入口/调用方形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、asset watcher docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 asset watcher `AssetChange` construction 的 `*_new` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 construction-owner `_new` 路径与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 resource streamer construction module naming hard cutover | runtime_15_resource_streamer_construction_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.5/M2：`graphics/scene/resources/resource_streamer/resource_streamer_new.rs` 已删除并硬切为 `graphics/scene/resources/resource_streamer/resource_streamer_construction.rs`，`resource_streamer/mod.rs` 只声明 `mod resource_streamer_construction;`，该 owner 继续承接 `ResourceStreamer::new(...)`、fallback texture/material uniform 初始化和 output-target writeback converter 构造逻辑，未保留旧 `*_new` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_resource_streamer_construction_uses_owner_name`，验证旧文件不存在、新 owner/入口形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、graphics render-product docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 ResourceStreamer construction 的 `*_new` 文件名债；完整 construction-owner `_new` sweep、graphics Cargo sweep 与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 offscreen target construct directory naming hard cutover | runtime_15_offscreen_target_construct_naming_hard_cutover_static_passed_cargo_timeout_no_result | 2026-06-25 | R2.5/M2：`graphics/backend/render_backend/offscreen_target_new/` 已删除并硬切为 `graphics/backend/render_backend/offscreen_target_construct/`，`graphics/backend/render_backend/mod.rs` 只声明 `mod offscreen_target_construct;`，该目录继续承接 `OffscreenTarget::new(...)`、固定 offscreen frame target texture bundle 构造、cluster buffer 构造与 texture bundle owner 拆分，未保留旧 `*_new` 目录、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_offscreen_target_construct_uses_owner_name`，验证旧目录不存在、新目录/父模块/construct owner 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、graphics render-product docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Focused `cargo check -p zircon_runtime --lib --no-default-features --features target-server --locked --jobs 1 --target-dir target\codex-runtime15-offscreen-construct-check --message-format short --color never` 首跑 exit=1 但保留输出无错误尾部；同目标目录复跑 304s 超时，日志无 `Finished` / `error:` 结论，不计 Cargo 通过。该记录只关闭 OffscreenTarget construction directory 的 `*_new` 目录名债；完整 construction-owner `_new` sweep、graphics Cargo sweep 与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 asset texture upload readiness container fixtures module naming hard cutover | runtime_15_asset_texture_upload_readiness_container_fixtures_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`asset/tests/assets/texture_upload_readiness/common.rs` 已删除并硬切为 `asset/tests/assets/texture_upload_readiness/container_fixtures.rs`，`asset/tests/assets/texture_upload_readiness.rs` 只声明 `mod container_fixtures;`，该 test owner 继续承接 DDS/KTX/ASTC fixture bytes、container header writers 与 upload-readiness constants，未保留旧 `common` 模块、alias 或兼容 re-export。`boundaries.rs`、`containers.rs`、`dds.rs` 与 `ktx.rs` 只从 `container_fixtures` 消费共享 fixtures。新增 `naming_boundary/runtime_15_m2/asset_dynamic.rs::runtime_15_asset_texture_upload_readiness_container_fixtures_uses_owner_name`，验证旧文件不存在、新 owner/入口/调用方 import 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、render-assets docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 asset texture upload readiness container fixture 的 `common` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 `helpers.rs` / `common.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 scene ECS query cached queries module naming hard cutover | runtime_15_scene_ecs_query_cached_queries_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`scene/tests/ecs_query/cache_helpers.rs` 已删除并硬切为 `scene/tests/ecs_query/cached_queries.rs`，`scene/tests/ecs_query.rs` 只声明 `mod cached_queries;`，该 test owner 继续承接 cache rebuild、count/empty/get/many/unique、cached-direct table/sparse location、archetype movement 和 optional archetype membership 用例，未保留旧 `cache_helpers` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/asset_dynamic.rs::runtime_15_scene_ecs_query_cached_queries_uses_owner_name`，验证旧文件不存在、新 owner/入口形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、scene ECS docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 scene ECS query cached queries 的 `helpers` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 `utils.rs` / `helpers.rs` / `_impl.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 dynamic API vampire runtime support module naming hard cutover | runtime_15_dynamic_api_vampire_runtime_support_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`dynamic_api/session/tests/helpers.rs` 已删除并硬切为 `dynamic_api/session/tests/vampire_runtime_support.rs`，`dynamic_api/session/tests/mod.rs` 只声明 `mod vampire_runtime_support;`，`frame_diagnostics.rs`、`vampire_gameplay.rs`、`vampire_hud.rs` 与 `vampire_menu.rs` 只从该 owner 消费 vampire project/session/HUD/diagnostics 测试支撑函数，未保留旧 `helpers` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/asset_dynamic.rs::runtime_15_dynamic_api_vampire_runtime_support_uses_owner_name`，验证旧文件不存在、新 owner/入口/调用方 import 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、dynamic API session docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 dynamic API vampire runtime support 的 `helpers` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 `helpers.rs` / `_impl.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 camera controller output module naming hard cutover | runtime_15_camera_controller_output_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`core/framework/camera_controller/common.rs` 已删除并硬切为 `core/framework/camera_controller/controller_output.rs`，`core/framework/camera_controller/mod.rs` 只声明 `mod controller_output;` 并从该 owner 重导出 `CameraControllerOutput`、`CursorGrabIntent` 与 `CursorGrabMode`，未保留旧 `common` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/core_framework.rs::runtime_15_camera_controller_output_uses_owner_name`，验证旧文件不存在、新 owner/入口形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、camera controller docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 camera controller output 的 `common` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 `helpers.rs` / `_impl.rs` / `common.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 scene ECS systems many/single queries module naming hard cutover | runtime_15_scene_ecs_systems_many_single_queries_naming_hard_cutover_static_passed_cargo_timeout_no_result | 2026-06-25 | R2.3/M2：`scene/tests/ecs_systems/query_helpers.rs` 已删除并硬切为 `scene/tests/ecs_systems/many_single_queries.rs`，`scene/tests/ecs_systems.rs` 只声明 `mod many_single_queries;`，该 test owner 继续承接 `get_many` / `iter_many` / `single` query behavior 覆盖，未保留旧 `query_helpers` 模块、alias 或兼容 re-export。同步更新既有 M3 scene ECS systems folder split 守卫和文档锚点，使早期拆分证据不再引用退役路径。新增 `naming_boundary/runtime_15_m2/scene_tests.rs::runtime_15_scene_ecs_systems_many_single_queries_uses_owner_name`，验证旧文件不存在、新 owner/入口形状、M3 test-budget guard 已改到新路径，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、scene ECS docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 复跑在 305 秒超时，临时日志只到编译 warning 且没有 test result，不计通过。该记录只关闭 scene ECS systems many/single query test owner 的 `query_helpers` 文件名债；完整 `runtime_15_no_banned_name_modules`、剩余 `helpers.rs` / `_impl.rs` / `common.rs` 类命名债与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 plugin static manifest contract owner naming hard cutover | runtime_15_plugin_static_manifest_contract_owner_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`plugin_extensions/static_manifest_contracts/feature_bundles/helpers.rs`、`package_coordinates/helpers.rs`、`package_identity/helpers.rs` 与 `package_kind/helpers.rs` 已删除并硬切为 `plugin_extensions/static_manifest_contracts/feature_bundles/feature_bundle_rows.rs`、`plugin_extensions/static_manifest_contracts/package_coordinates/package_coordinate_resolution.rs`、`plugin_extensions/static_manifest_contracts/package_identity/package_id_tokens.rs` 与 `plugin_extensions/static_manifest_contracts/package_kind/package_kind_fields.rs`；四个父模块只挂载新 owner，feature bundle rows、package coordinate resolution、package-id token 与 package-kind field callers 只从职责命名 owner import，未保留旧 `helpers` 模块、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/plugin_static_manifest.rs::runtime_15_plugin_static_manifest_contract_owners_use_domain_names`，验证旧文件不存在、新 owner/入口/调用方 import 形状，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、package manifest docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 static manifest contract 测试 owner 的 `helpers` 文件名债；完整 `runtime_15_no_banned_name_modules` 与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 UI editor showcase descriptor builders module naming hard cutover | runtime_15_ui_editor_showcase_descriptor_builders_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`ui/component/catalog/editor_showcase/helpers.rs` 已删除并硬切为 `ui/component/catalog/editor_showcase/descriptor_builders.rs`；`ui/component/catalog/editor_showcase.rs` 只挂载 `mod descriptor_builders;` 并从该 owner import descriptor construction、layout role/default template projection、palette metadata、fallback policy、option/slot/value prop schema builders 与 TOML layout helpers，未保留旧 `helpers` 模块、alias 或兼容 re-export。既有 M4 production-file budget 守卫同步改到新路径。新增 `naming_boundary/runtime_15_m2/ui.rs::runtime_15_ui_editor_showcase_descriptor_builders_use_owner_name`，验证旧文件不存在、新 owner/入口/调用方 import 形状、M4 guard 已改到新路径，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、UI architecture docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、旧路径不存在与新模块入口扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 UI editor showcase descriptor builders 的 `helpers` 文件名债；完整 `runtime_15_no_banned_name_modules` 与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 UI table sortingMode server literal allowed-context sync | runtime_15_ui_table_sorting_mode_server_literal_allowed_context_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：M4 table column helper split 后，第三方 UI API 字面量 `sortingMode = "server"` 的生产读取点位于 `ui/surface/surface/default_interactions/table/columns.rs`。非网络 `server` 命名审计与 Rust 命名守卫已同步新 owner 路径：`non_network_server_naming.py` 和 `runtime_absorption/naming_boundary.rs::runtime_non_network_server_naming_is_classified_by_owner` 将该字面量继续视为 DataGrid/Table API allowed context，而不是 runtime 网络/server owner 债。新增 `naming_boundary/runtime_15_m2/ui.rs::runtime_15_ui_table_sorting_mode_server_literal_stays_allowed_context`，验证 `columns.rs` 仍只在 `table_uses_client_sorting(...)` 中消费 `sortingMode`/`Some("server")`，Python audit 与 Rust guard 都登记新路径，以及 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、UI architecture docs 与 status-output expectations 同步该状态锚。验证：scoped rustfmt/static scans、aggregate `audit_runtime_structure.py --json` non-network server assertions、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录只关闭 UI table `sortingMode = "server"` moved-owner allowlist 同步面；graphics render-framework server naming debt 已由后续 M2 hard cutover 关闭，完整 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 graphics render-framework receiver naming hard cutover | runtime_15_graphics_render_framework_receiver_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`graphics/runtime/render_framework/**` 中非网络 receiver/context 变量已从 `server` 硬切为 `framework`，覆盖 viewport lifecycle、pipeline mutation、stats/debug snapshot 查询、capture、frame-submission context build、camera-loop submit、direct runtime-frame submit 与 preflight failure helpers；代表性入口使用 `framework: &WgpuRenderFramework`、`framework.lock_operation()` 与 `framework.lock_state()`，未保留 `server` alias、compat variable 或 allowlist 债。Rust `runtime_non_network_server_naming_is_classified_by_owner` 与 Python `non_network_server_naming.py` 均不再保留/要求 `graphics-render-framework-debt`，aggregate `non_network_server_references` 在该切片收敛为 0 unclassified、0 graphics render-framework debt；后续 M2 editor workbench authority-label hard cutover 已继续清掉当时剩余的 editor workbench classified debt。新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_render_framework_receiver_uses_framework_name`，递归验证 render-framework source 无 `server` token，并同步 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、graphics render-product docs 与 status-output expectations。验证：scoped rustfmt/static scans、render-framework server-token scan、old graphics debt string scan、aggregate `audit_runtime_structure.py --json` non-network server assertions、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 graphics render-framework 非网络 receiver 命名债；editor workbench authority-label debt 已由后续 M2 切片关闭，`runtime_15_no_banned_name_modules` 与 `module_convention_gate` 仍 pending。 |
| M2 | Runtime 15 M2 render framework trait/construction owner naming hard cutover | runtime_15_render_framework_trait_construction_owner_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/R2.5/M2：`graphics/runtime/render_framework/render_framework_impl/trait_impl.rs` 已硬切为 `graphics/runtime/render_framework/render_framework_trait_binding/wgpu_framework.rs`，`graphics/runtime/render_framework/wgpu_render_framework_new/new.rs` 已硬切为 `graphics/runtime/render_framework/wgpu_render_framework_construction/construct.rs`，并同步 `create_default_pipelines.rs` 所在目录到 `wgpu_render_framework_construction/`。`graphics/runtime/render_framework/mod.rs` 只挂载 `render_framework_trait_binding` 与 `wgpu_render_framework_construction`，两个 child `mod.rs` 只挂载 `wgpu_framework` / `construct`，未保留旧 `_impl`、`trait_impl`、`*_new`、`new` child、alias 或兼容 re-export。新增 `naming_boundary/runtime_15_m2/banned_names.rs::runtime_15_no_banned_name_modules`，递归扫描 `zircon_runtime/src` 的文件/目录模块名，禁止 `common`、`helper(s)`、`misc`、`new`、`util(s)`、`*_helper(s)`、`*_impl`、`*_inner` 与 `*_new` 回流，并同步 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、graphics render-product docs 与 status-output expectations。验证：scoped rustfmt/static scans、文件/目录 banned-name scan、旧路径不存在扫描、docs/status anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭当前 `runtime_15_no_banned_name_modules` 全局闸口；完整 `module_convention_gate`、large-file gate 与全量 Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 graphics construction new owner naming hard cutover | runtime_15_graphics_construction_new_owner_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.5/M2：`graphics/feature/render_feature_descriptor/new.rs`、`graphics/feature/render_feature_pass_descriptor/new.rs`、graphics history/viewport/scene renderer/deferred/mesh/overlay/particle/UI 等私有 construction `new.rs` owners 以及 `graphics/scene/scene_renderer/post_process/resources/new/` 已硬切为 `construct.rs` 或 `construct/` owner；代表性新路径为 `graphics/feature/render_feature_descriptor/construct.rs` 与 `graphics/scene/scene_renderer/post_process/resources/construct/construct/construct.rs`。所有父模块只挂载 `mod construct;`，post-process construct child visibility path 从 `resources::new` 收束为 `resources::construct`，未保留旧 `new` 模块、alias 或兼容 re-export。新增 `runtime_15_graphics_construction_new_owners_use_construct_names`，并扩展 `runtime_15_no_banned_name_modules` 禁止裸 `new` 模块名；同步 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、graphics render-product docs 与 status-output expectations。验证按本轮 scoped rustfmt/static scans、旧路径不存在扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 执行；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。 |
| M2 | Runtime 15 M2 scene dynamic document v1 owner naming hard cutover | runtime_15_scene_dynamic_document_v1_owner_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2：`scene/dynamic_scene/document/legacy.rs` 已删除并硬切为 `scene/dynamic_scene/document/v1_project_document.rs`，DTO 从 `LegacyProjectDocument` 改为 `V1ProjectDocument`；`document/mod.rs` 只挂载 `mod v1_project_document;`，`document/read.rs` 只从该 owner 读取 v1 project-world JSON 兼容 schema，未保留旧 `legacy` 模块、alias 或兼容 re-export。`scene_project_serialization_boundary.py` 的 source list 同步改为新 owner 路径，避免结构审计继续读取 retired 文件。新增 `naming_boundary/runtime_15_m2/banned_names.rs::runtime_15_scene_dynamic_document_v1_owner_uses_versioned_name`，验证旧路径/旧类型/旧审计路径不存在，新路径、`V1ProjectDocument`、docs/status/session anchors 同步。验证：scoped rustfmt/static scans、旧路径/旧类型扫描、audit JSON hard-cutover debt scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 dynamic scene document 的 `legacy-runtime-scene-document-debt` live hit；完整 `module_convention_gate` 与 full dynamic scene Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 platform input DOM keycode naming hard cutover | runtime_15_platform_input_dom_keycode_naming_hard_cutover_static_passed_cargo_timeout_no_result | 2026-06-27 | R2.3/M2：`ui/platform_input/keyboard_map.rs` 的私有 key-code helper 已从 legacy 命名硬切为 `dom_key_code(...)` / `dom_character_key_code(...)`，明确其职责是保留 DOM keyCode 兼容表；`ui/platform_input/winit_translation.rs` 只导入 `dom_key_code`，像素滚轮 fallback 常量从 legacy scalar 命名改为 `PIXEL_SCROLL_LINE_DELTA_SCALE`，测试名同步为 line-delta scale 语义。新增 `naming_boundary/runtime_15_m2/ui.rs::runtime_15_platform_input_uses_dom_keycode_names`，锁定旧 helper/constant/test 名不回流，并同步 Runtime 15 子计划、runtime index、review findings、结构规范、module-convention docs、platform input docs 与 status-output expectations。验证：scoped rustfmt/static scans、platform input 旧命名零命中扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；focused Cargo 守卫测试 304 秒超时无结果，残留 cargo/rustc 进程已终止，不计通过。该记录关闭 runtime naming boundary 中 platform input legacy 命名 live hit；完整 `module_convention_gate` 与 full platform-input Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 platform input runtime baseline test naming hard cutover | runtime_15_platform_input_runtime_baseline_test_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2：`ui/platform_input/winit_translation.rs` 的 runtime-owned winit translation tests 不再使用 `editor_baseline` 作为测试函数名后缀，已硬切为 `translate_winit_keyboard_matrix_matches_runtime_input_baseline` 与 `translate_winit_ime_preedit_commit_and_disable_match_runtime_input_baseline`。该改动只收敛测试命名，使 runtime 平台输入 owner 的基线语义指向 runtime input contract，不改变 keyboard/IME translation、synthetic flag、modifier、preedit cursor range 或 text commit 行为。新增 `naming_boundary/runtime_15_m2/ui.rs::runtime_15_platform_input_winit_tests_use_runtime_input_baseline_names`，锁定 `editor_baseline` 不回流，并同步 Runtime 15 子计划、runtime index、review findings、结构规范、module-convention docs、platform input docs 与 status-output expectations。验证按本轮 scoped rustfmt/static scans、platform input test-name 扫描、runtime naming audit、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 执行；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。该记录关闭 runtime naming boundary 中 `unclassified-runtime-naming-reference` 的 platform input test-name live hit；完整 `module_convention_gate` 与 full platform-input Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 UI template schema source fixture naming hard cutover | runtime_15_ui_template_schema_source_fixture_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2：UI template schema migration report 的 source-template fixture 入口已从 `LegacyTemplateFixture` / `LegacyTemplateConverted` 硬切为 `SourceTemplateFixture` / `SourceTemplateFixtureConverted`。`zircon_runtime_interface/src/ui/template/asset/schema/report.rs` 暴露的新枚举名描述真实 source fixture 输入，不再把迁移报告 API 命名为 legacy；`zircon_runtime/src/ui/template/asset/schema/migrator.rs` 只写入新枚举项，`ui/tests/asset_schema_migration.rs` 的 fixture 常量与 control id 同步改为 source fixture 词汇。新增 `naming_boundary/runtime_15_m2/ui.rs::runtime_15_ui_template_schema_uses_source_fixture_names`，锁定接口枚举、运行时迁移器、测试 fixture、Runtime 15/status/docs anchors 和 `SourceTemplateFixture` 不回退；未保留旧枚举 alias、compat wrapper 或 re-export。验证按本轮 scoped rustfmt/static scans、runtime naming audit、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 执行；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。该记录关闭 `legacy-runtime-ui-template-schema-debt` live hit；完整 `module_convention_gate` 与 full UI template Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 input mouse-wheel line-delta naming hard cutover | runtime_15_input_mouse_wheel_line_delta_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2：`core/framework/input/mouse_wheel.rs` 的滚轮像素到 scalar 兼容量命名已硬切为 line-delta 语义：`LEGACY_PIXEL_SCROLL_SCALE` 改为 `PIXEL_SCROLL_LINE_DELTA_SCALE`，`MouseWheelEvent::legacy_vertical_delta()` 改为 `vertical_line_delta()`。`core/framework/input/mod.rs`、`input/mod.rs`、`prelude.rs`、`input/runtime/default_input_manager.rs` 与 `dynamic_api/session/events.rs` 只使用新名，`input/tests/input_manager/frame_state.rs` 通过新常量锁定 Pixel→Line scalar 计算，`dynamic_api/tests/input_events.rs` 锁定 preview scroll 调用新 helper。新增 `naming_boundary/runtime_15_m2/input.rs::runtime_15_input_mouse_wheel_line_delta_uses_current_names`，验证旧常量/方法不在生产 input/dynamic/prelude 路径回流，并同步 Runtime 15、runtime index、review findings、结构规范、module-convention、input/dynamic/prelude docs 与 status-output expectations。验证按本轮 scoped rustfmt/static scans、旧名精确扫描、runtime naming audit、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 执行；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。该记录关闭 `legacy-runtime-input-event-debt`、`legacy-dynamic-api-migration-debt` 与 curated prelude 中这组 mouse-wheel legacy live hit；完整 `module_convention_gate` 与 full input/dynamic Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 DDS upload policy naming hard cutover | runtime_15_dds_upload_policy_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2：`asset/assets/texture/upload_support/dds.rs` 的 DDS container policy 命名已从 generic legacy 词汇硬切为 classic DDS FourCC 语义：`dds_legacy_upload_layout(...)` 改为 `dds_classic_fourcc_upload_layout(...)`，`legacy_faces` 改为 `classic_faces`，`legacy_cubemap` 改为 `classic_header_cubemap`。`asset/tests/assets/texture_upload_readiness/container_fixtures.rs` 的 DDS fixture helpers 同步改为 `dds_classic_fourcc_bytes(...)`、`dds_classic_mip_bytes(...)` 与 `dds_classic_cubemap_bytes(...)`，调用方测试只使用 classic fixture 名。该改动只澄清 pre-DX10 DDS header/FourCC policy，不改变 DXT/ATI/BC 直接上传、DX10 header 验证、mip/layer/cubemap 校验或 unsupported diagnostics。新增 `naming_boundary/runtime_15_m2/asset_dynamic.rs::runtime_15_dds_upload_policy_uses_classic_container_names`，锁定旧 helper/变量名不回流，并同步 Runtime 15、runtime index、review findings、结构规范、module-convention、render-assets docs 与 status-output expectations。验证按本轮 scoped rustfmt/static scans、旧名精确扫描、runtime naming audit、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 执行；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。该记录关闭 `legacy-runtime-dds-container-policy` live hit；完整 `module_convention_gate` 与 full texture upload Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 material asset schema-v1 defaults naming hard cutover | runtime_15_material_asset_schema_v1_defaults_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2：`asset/assets/material/material_asset.rs` 的 `.zmaterial` v1 默认同步 helper 已从 generic legacy wording 硬切为显式 schema-v1 命名：`property_overrides_with_legacy_defaults(...)` 改为 `property_overrides_with_schema_v1_defaults(...)`，`texture_slots_with_legacy_defaults(...)` 改为 `texture_slots_with_schema_v1_defaults(...)`，`legacy_texture_slots(...)` 改为 `schema_v1_pbr_texture_slots(...)`。该改动只澄清 v1 material schema 与固定 PBR runtime fields 的同步责任，不改变 `.zmaterial` 序列化形状、`MaterialAsset` public API、descriptor/readiness 语义或 renderer material bridge。新增 `naming_boundary/runtime_15_m2/asset_schema.rs::runtime_15_material_asset_schema_v1_defaults_use_versioned_names`，锁定旧 helper 名不回流，并同步 Runtime 15、runtime index、review findings、结构规范、module-convention、zmeta material docs、render-assets docs 与 status-output expectations。验证按本轮 scoped rustfmt/static scans、旧名精确扫描、runtime naming audit、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 执行；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。该记录关闭 `legacy-runtime-asset-schema-debt` live hit；完整 `module_convention_gate` 与 full asset/render material Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 editor workbench authority-label naming hard cutover | runtime_15_editor_workbench_authority_label_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-25 | R2.3/M2：`zircon_editor/src/ui/retained_host/callback_dispatch/template_bridge/workbench/extension_module_feedback/gameplay_state.rs` 中的 Workbench extension feedback fixture 文案已从 `Selected Condition_Night   server authority` 硬切为 `Selected Condition_Night   editor authority`，避免把非网络 editor/workbench 权限标签称为 server owner。Python `non_network_server_naming.py` 不再保留 `editor-workbench-authority-label-debt` 分类分支；aggregate `non_network_server_references` 审计收敛为 0 reference decisions、0 classified debt、0 migration debt、0 unclassified，M1 gate 状态进入 `classified-and-clear`。新增 `naming_boundary/runtime_15_m2/editor_workbench.rs::runtime_15_editor_workbench_authority_label_uses_editor_name`，验证 Workbench feedback source 包含新 `editor authority` 文案、不再包含 `server authority`，审计脚本无 retired bucket，并同步 Runtime 15 计划、runtime index、review findings、结构规范、module-convention docs、non-network server naming doc、editor host commands doc 与 status-output expectations。验证：scoped rustfmt/static scans、Python py_compile、aggregate `audit_runtime_structure.py --json` non-network server assertions、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭最后一个当前非网络 `server` 命名迁移债；完整 `module_convention_gate` 与全量 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 test file budget guard root mod cutover | runtime_15_test_file_budget_guard_root_mod_cutover_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：旧平铺 `structure_convention/test_file_budget.rs` 已硬切为 folder-backed `structure_convention/test_file_budget/mod.rs`，父级 `structure_convention.rs` 直接挂载 `#[path = "structure_convention/test_file_budget/mod.rs"] mod test_file_budget;`，避免 rustfmt/模块解析把 `test_file_budget/*` 子 owner 误解析到兄弟目录。`root_layout.rs` 新增旧平铺文件不存在守卫、docs/status 锚点和父/子行数预算；`mod.rs` 当前 441 行、`root_layout.rs` 当前 709 行，均低于 800 行预算。验证：scoped rustfmt/static checks、旧文件不存在扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；带锁 Cargo 被当前 `Cargo.lock`/`Cargo.toml` 不一致阻断，不计通过。该记录只关闭 test-file budget guard 根模块路径硬切；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M4 | Runtime 15 M4 core runtime service-list registration visibility unblock | runtime_15_core_runtime_service_lists_registration_visibility_coremin_check_passed | 2026-06-23 | 下游 `zircon_editor` 包检查在进入 editor diagnostics 前暴露 runtime 最低层阻塞：`service_lists` folder split 后，`types.rs` 的 `ModuleServiceLists`/字段和 `specialized.rs::single_service_module_lists(...)` 仍是子模块级 `pub(super)`，导致父模块 re-export 不能供 sibling `register_module.rs` 使用并触发 E0364/E0365/E0603/E0616。修复只把这两个 registration 边界需要的符号收窄提升到 `pub(in crate::core::runtime::handle::registration)`，其余 multi/specialized/shutdown helper 保持 `pub(super)`，不新增 `pub(crate)`、compat re-export 或旧平铺路径。验证：`rustfmt --edition 2021 --check` 覆盖 `service_lists/{types,specialized,mod}.rs`、`register_module.rs` 与本轮窄类型推断修复的 `scene/world/render.rs`；静态扫描确认仅 `ModuleServiceLists`、三个字段和 `single_service_module_lists(...)` 使用 registration 范围可见性，`service_lists/mod.rs` 仍只暴露窄 entry；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --offline --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-service-lists-0623 --message-format short --color never` 通过（既有 warning noise），`--locked --offline` 仍会在编译前被当前 lockfile drift 拒绝；每次 Cargo 后 `Cargo.lock` 均恢复到基准 hash。 |
| M4 | Runtime 15 M4 core runtime service-list owner split | runtime_15_core_runtime_service_lists_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R1.4/M4：`core/runtime/handle/registration/service_lists.rs` 已删除并硬切为 folder-backed `core/runtime/handle/registration/service_lists/mod.rs`、`types.rs`、`multi.rs`、`specialized.rs` 与 `shutdown.rs`。父模块只保留 `module_service_lists(...)` 分派与 `single_service_module_lists` 窄 re-export；`types.rs` 拥有 `ModuleServiceLists` 返回形状，`multi.rs` 拥有通用扫描路径，`specialized.rs` 拥有 1-5 服务特化路径，`shutdown.rs` 拥有反向 shutdown list 组装。`register_module.rs` 的调用入口不变，不新增兼容层或旧路径 re-export；最大 owner 为 `core/runtime/handle/registration/service_lists/specialized.rs` 488 行，全部低于生产文件 800 行软预算。新增 `structure_convention/production_file_budget.rs::runtime_15_core_runtime_service_lists_are_folder_backed`，验证旧平铺文件不存在、父子 owner 挂载、服务列表入口保持窄接口、owner 行数预算，以及 Runtime 15/status/docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、docs/status 锚点扫描通过；`cargo test -p zircon_runtime --lib runtime_15_core_runtime_service_lists_are_folder_backed --locked` 被当前 `Cargo.lock` 漂移在编译前阻断，不计 Cargo 通过。该记录关闭 core runtime service-list owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M4 | Runtime 15 M4 RHI WGPU command validation render-state owner split | runtime_15_rhi_wgpu_command_validation_render_state_split_static_passed_cargo_lock_blocked | 2026-06-23 | R1.4/M4：`rhi_wgpu/command_validation.rs` 从 878 行减压为 621 行，继续拥有 `validate_recorded_commands(...)`、`execute_recorded_commands(...)`、debug group、render pass、queue 与 copy command traversal；新增 folder-backed 子 owner `rhi_wgpu/command_validation/render_state.rs`（300 行）承接 `RecordedRenderState`、`CommandResourceLookup`、bind group slot validation、binding range、vertex/index/strided range helper 和 pipeline-layout lookup。父模块只通过 `mod render_state;` 与 `use self::render_state::{...}` 消费子 owner，不新增 public API、不改 `rhi_wgpu::device` 调用入口，也不恢复旧平铺 helper。新增 `structure_convention/production_file_budget.rs::runtime_15_rhi_wgpu_command_validation_state_is_child_owner`，锁定父/子 owner 布局、moved helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/RHI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；`cargo test -p zircon_runtime --lib runtime_15_rhi_wgpu_command_validation_state_is_child_owner --locked` 被当前 `Cargo.lock`/`Cargo.toml` 漂移在编译前阻断，不计 Cargo 通过。该记录关闭 RHI WGPU command validation render-state owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 RHI/graphics Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 RHI WGPU UI surface render/setup owner split | runtime_15_rhi_wgpu_ui_surface_render_setup_owner_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R1.4/M4：`rhi_wgpu/ui_surface.rs` 从 1054 行减压为 549 行，继续拥有 UI presenter/render target lifecycle、surface frame acquire/present、image cache pruning 与 public `WgpuUiSurfaceRenderer` 调用入口；新增 folder-backed 子 owner `rhi_wgpu/ui_surface/render_pass.rs`（168 行）承接 `WgpuUiDrawBuffers`、`TargetLoad`、draw-op buffer upload、render pass begin、viewport/scissor setup 与 solid/image/text draw recording；新增 `rhi_wgpu/ui_surface/surface_setup.rs`（164 行）承接 surface configuration、format/present/alpha mode selection、adapter/device request、instance descriptor 与 raw-window-handle surface creation；新增 `rhi_wgpu/ui_surface/tests.rs`（273 行）保留 headless presenter stats、surface format/alpha、damage mode、batch stats、atlas image batching 与 image-cache prune coverage。父模块只通过 `mod render_pass;`、`mod surface_setup;`、`#[cfg(test)] mod tests;` 与窄 `pub(super)` imports 消费子 owner，不改变 `rhi::ui_surface` trait、editor retained-host GPU presenter contract、shader/pipeline layout 或 image cache key 语义。新增 `structure_convention/production_file_budget.rs::runtime_15_rhi_wgpu_ui_surface_render_setup_are_child_owners`，锁定父/子 owner 布局、render/setup helper 不回流、四侧低于 800 行预算，以及 Runtime 15/status/RHI UI surface/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；`cargo test -p zircon_runtime --lib runtime_15_rhi_wgpu_ui_surface_render_setup_are_child_owners --locked` 120 秒超时无测试结果，且超时后未发现该目标目录的 cargo/rustc 残留进程，不计 Cargo 通过。该记录关闭 RHI WGPU UI surface render/setup owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 RHI/graphics Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 RHI WGPU UI surface geometry test owner split | runtime_15_rhi_wgpu_ui_surface_geometry_tests_owner_split_static_passed_cargo_timeout_no_result | 2026-06-24 | R1.4/M4：`rhi_wgpu/ui_surface/geometry.rs` 从 812 行减压为 559 行，继续拥有 draw-list ordering、solid/image/text draw item projection、rounded quad/border tessellation、image UV trimming、effective rect clipping 与 `text_bounds_from_rect(...)`；新增 folder-backed 子 owner `rhi_wgpu/ui_surface/geometry/tests.rs`（308 行）承接原父文件内的 geometry test suite 与 test-only `solid_items(...)` helper，覆盖 damage/clip trimming、stable z-order、rounded geometry、image UV clipping/atlas UV composition、invalid atlas UV skip 与 text bounds/disjoint damage。父模块只通过 `#[cfg(test)] mod tests;` 挂载测试 owner，不改变 `UiSurfaceDrawList` 几何语义、batching 输入顺序、shader/pipeline layout、atlas UV 计算或 `rhi::ui_surface` trait。新增 `structure_convention/production_file_budget/rhi_wgpu_ui_surface_geometry.rs::runtime_15_rhi_wgpu_ui_surface_geometry_tests_are_child_owner`，锁定父/子 owner 布局、test helper/测试函数不回流、两侧低于 800 行预算，以及 Runtime 15/status/RHI UI surface/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；focused `cargo test -p zircon_runtime --lib runtime_15_rhi_wgpu_ui_surface_geometry_tests_are_child_owner --locked --jobs 1` 10 分钟超时无测试结果，且超时后未发现 cargo/rustc 残留进程，不计 Cargo 通过。该记录关闭 WGPU UI surface geometry test owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 RHI/UI surface Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 RHI device handle owner split | runtime_15_rhi_device_handles_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`rhi/device.rs` 从 799 行减压为 702 行，继续拥有 typed `RhiError`、command/render-pass DTOs、`CommandList` 与 `RenderDevice` contract；新增 folder-backed 子 owner `rhi/device/handles.rs`（105 行）承接 `BufferHandle`、`TextureHandle`、`SamplerHandle`、`BindGroupLayoutHandle`、`BindGroupHandle`、`ShaderModuleHandle`、`PipelineLayoutHandle` 与 `PipelineHandle` 这组 neutral resource handle newtypes。父模块只通过 `mod handles;` 与 `pub use self::handles::{...}` 保留原 `rhi::device::*` 与 `rhi::*` public export paths，不改变 handle raw/new API、RHI error model、command-list recording semantics 或 backend device contract。新增 `structure_convention/production_file_budget/rhi_device_handles.rs::runtime_15_rhi_device_handles_are_child_owner`，锁定父/子 owner 布局、handle newtypes 不回流、两侧低于 800 行预算，以及 Runtime 15/status/RHI descriptors/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 RHI device handle owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 RHI Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 dynamic API session profile owner split | runtime_15_dynamic_api_session_profile_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`dynamic_api/session.rs` 从 773 行减压为 728 行，继续拥有 Rust-ABI session entry points、session registry、`RuntimeDynamicSession` lifecycle、frame tick/capture/present 与 host-request orchestration；新增 folder-backed 子 owner `dynamic_api/session/profile.rs`（47 行）承接 `RuntimeDynamicSessionProfile`、ABI profile byte parsing、fixed-step policy、diagnostic-log schedule selection 与 render-bridge enablement policy。父模块只通过 `mod profile;` 与 `use profile::RuntimeDynamicSessionProfile;` 消费 profile policy，不改变 `ZrRuntimeSessionConfigV1` profile 字节语义、default/runtime/editor/dev/minimal/headless profile 行为、session 创建流程或 frame tick semantics。新增 `structure_convention/production_file_budget/dynamic_api_session_profile.rs::runtime_15_dynamic_api_session_profile_is_child_owner`，锁定父/子 owner 布局、profile enum/constants/methods 不回流、两侧低于 800 行预算，以及 Runtime 15/status/dynamic API session/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 dynamic API session profile owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 dynamic API Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 dynamic API session registry owner split | runtime_15_dynamic_api_session_registry_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`dynamic_api/session.rs` 从 728 行继续减压为 682 行，继续拥有 Rust-ABI session entry points、`RuntimeDynamicSession` lifecycle、frame tick/capture/present 与 host-request orchestration；新增 folder-backed 子 owner `dynamic_api/session/registry.rs`（69 行）承接 `SESSION_REGISTRY`、`SessionRegistry` handle map、handle allocation、poison-safe `lock_registry`/`lock_session` 与 `with_session` lookup/dispatch。父模块只通过 `mod registry;`、`use registry::{insert_session, lock_registry, with_session};` 和测试专用 `use registry::lock_session;` 消费 registry owner，不改变 `ZrRuntimeSessionHandle` 分配、destroy lookup、invalid/not-found status、lock-poison recovery 或 session ABI entry semantics。新增 `structure_convention/production_file_budget/dynamic_api_session_registry.rs::runtime_15_dynamic_api_session_registry_is_child_owner`，锁定父/子 owner 布局、registry static/struct/helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/dynamic API session/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 dynamic API session registry owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 dynamic API Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 native host API adapter tests owner split | runtime_15_native_host_api_adapter_tests_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`plugin/native_plugin_loader/host_api_adapter.rs` 从 967 行减压为 506 行，继续拥有 `NativeHostApiV3RegistrationScope`、`NativeHostBridgeCallScope`、9 个 host ABI callback entry、registration/component bridge dispatch、context table 与 status helper；新增 folder-backed 子 owner `plugin/native_plugin_loader/host_api_adapter/tests.rs`（455 行）承接原内联 13 个 native host API / bridge method descriptor 测试。父模块只通过 `#[path = "host_api_adapter/tests.rs"] mod tests;` 挂载测试，不改变 `ZrHostApiV3` 表面、runtime plugin handle 语义、bridge method dispatch、manifest method descriptor projection、panic guard 或 dotted plugin id projection。新增 `structure_convention/production_file_budget/native_host_api_adapter.rs::runtime_15_native_host_api_adapter_tests_are_child_owner`，锁定父/子 owner 布局、代表测试不回流、13 个测试保留、两侧低于 800 行预算，以及 Runtime 15/status/plugin bridge/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数与测试数量扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 19096、53460、61976、63160；`rustc` PIDs 6488、31436）按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 native host API adapter 测试 owner 外迁的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 plugin bridge Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 material asset value/readiness helper owner split | runtime_15_material_asset_value_readiness_owner_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R1.4/M4：`asset/assets/material/material_asset.rs` 从 937 行减压为 750 行，继续拥有 `MaterialAsset` DTO、TOML document 转换入口、descriptor/readiness public API、management overview 与 shader-aware dependency/texture-slot entry；新增 folder-backed 子 owner `asset/assets/material/material_asset/value_sync.rs`（136 行）承接 TOML override 读取、texture slot hydration、legacy default 同步与 TOML 数组生成 helper；新增 `asset/assets/material/material_asset/readiness.rs`（70 行）承接 shader readiness diagnostic projection、WGSL capture/missing runtime source 映射与 material validation diagnostic rows。父模块只通过 `mod value_sync;`、`mod readiness;` 和窄 `pub(super)` imports 消费 helper，不改变 `.zmaterial` 序列化形状、`MaterialAsset` public API、render material descriptor 字段或 readiness report 语义。新增 `structure_convention/production_file_budget/material_asset.rs::runtime_15_material_asset_value_readiness_helpers_are_child_owners`，锁定父/子 owner 布局、value/readiness helper 不回流、三侧低于 800 行预算，以及 Runtime 15/status/material asset/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；focused locked Cargo 120 秒超时无测试结果，不计 Cargo 通过。该记录关闭 material asset value/readiness helper 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 asset/render material Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 material asset management record owner split | runtime_15_material_asset_management_record_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`asset/assets/material/material_asset.rs` 从 750 行继续减压为 651 行，保留 `MaterialAsset` DTO、`.zmaterial` document 转换入口、descriptor/readiness public API、`overview(...)`/`management_record(...)` entry 与 shader-aware dependency/texture-slot entry；新增 folder-backed 子 owner `asset/assets/material/material_asset/management.rs`（108 行）承接 `MaterialAssetOverview`、`MaterialAssetManagementRecord`、`MaterialAssetManagementRecordSetSummary` 与 `MaterialAssetManagementRecordSet` 聚合 DTO 和 record-set 汇总 impl。父模块只通过 `mod management;` 与 `pub use self::management::{...}` 保留原公开类型路径，不改变 `.zmaterial` 序列化形状、`MaterialAsset::overview(...)` / `management_record(...)` public API、record sort/summary semantics、render descriptor 字段或 readiness report 语义。新增 `structure_convention/production_file_budget/material_asset.rs::runtime_15_material_asset_management_records_are_child_owner`，锁定父/子 owner 布局、management DTO/impl 不回流、两侧低于 800 行预算，以及 Runtime 15/status/material asset/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 material asset management record owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 asset/render material Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 asset artifact cache UI document owner split | runtime_15_asset_artifact_cache_ui_documents_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`asset/artifact/cache_payload.rs` 从 791 行减压为 710 行，继续拥有 artifact cache wire enum、variant dispatch、cache-safe data/texture/material/shader/prefab/physics conversion 与 child owner 挂载；新增 `asset/artifact/cache_payload/ui.rs`（93 行）承接 UI v1/v2 document TOML normalization 和 typed restore paths，包括 `UiLayoutAsset`、`UiWidgetAsset`、`UiStyleAsset`、`UiV2ViewAsset`、`UiV2ComponentAsset` 与 `UiV2StyleAsset` 的 cache-safe roundtrip。父模块只通过 `mod ui;` 与 `use ui::{ArtifactCacheUiAssetDocument, ArtifactCacheUiV2AssetDocument};` 消费子 owner，不改变 `.zasset` enum variant、typed `AssetImportError` source、UI document parser entry 或 `UiThemeAsset`/`UiIconAsset` direct-cache path。新增 `structure_convention/production_file_budget/asset_cache_payload.rs::runtime_15_asset_artifact_cache_ui_documents_are_child_owner`，锁定父/子 owner 布局、UI document helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/artifact cache/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 asset artifact cache UI document owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 asset artifact Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 mesh asset management record owner split | runtime_15_mesh_asset_management_record_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`asset/assets/mesh/mesh_asset.rs` 从 734 行减压为 607 行，继续拥有 `MeshAsset` DTO、model primitive conversion、morph target application、validation、render descriptor projection 与 management entry；新增 folder-backed 子 owner `asset/assets/mesh/mesh_asset/management.rs`（140 行）承接 `MeshAssetOverview`、`MeshAssetManagementRecord`、`MeshAssetManagementRecordFailure`、`MeshAssetManagementRecordSetSummary` 与 `MeshAssetManagementRecordSet` 聚合 DTO 和 record-set 汇总 impl。父模块只通过 `mod management;` 与 `pub use self::management::{...}` 保留原公开类型路径，不改变 `.zmesh` 序列化形状、`MeshAsset::overview(...)` / `management_record(...)` public API、render descriptor 字段或 resource id record semantics。新增 `structure_convention/production_file_budget/mesh_asset.rs::runtime_15_mesh_asset_management_records_are_child_owner`，锁定父/子 owner 布局、management DTO/impl 不回流、两侧低于 800 行预算，以及 Runtime 15/status/render asset/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 mesh asset management record owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 asset mesh Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 asset project scan/import source collection owner split | runtime_15_asset_project_scan_import_sources_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`asset/project/manager/scan_and_import.rs` 从 705 行减压为 599 行，继续拥有 `scan_and_import(...)` 主导入循环、artifact restore、success/failure meta 写回、dependency resolution 与 entry identity 注册；新增 folder-backed 子 owner `asset/project/manager/scan_and_import/sources.rs`（181 行）承接 `AssetImportSource`、project/package root source enumeration、compound `.zmeta` source 发现、source URI 映射、compound source byte assembly 与 mtime 汇总。父模块只通过 `mod sources;` 与窄 `use self::sources::{AssetImportSource, source_bytes_for_import, source_mtime_unix_ms_for_import};` 消费子 owner，不改变 importer selection、artifact writeback、`.zmeta` schema、package locator semantics 或 failed-import recovery。新增 `structure_convention/production_file_budget/asset_project_scan_import.rs::runtime_15_asset_project_scan_import_sources_are_child_owner`，锁定父/子 owner 布局、source collection helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/importer/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 asset project scan/import source collection owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 asset project Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 glTF labeled material subasset owner split | runtime_15_gltf_labeled_material_subasset_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`asset/importer/ingest/gltf_labeled_subassets.rs` 从 664 行减压为 390 行，继续拥有 glTF texture/mesh/scene labeled subasset entry、scene dependency collection、shared material URI/reference resolution、root dependency insertion 与 label URI/reference helper；新增 folder-backed 子 owner `asset/importer/ingest/gltf_labeled_subassets/material.rs`（283 行）承接 `add_gltf_material_subassets(...)`、default material generation、PBR material projection、texture-slot metadata、KHR_texture_transform bridge、glTF alpha mode mapping 与 default PBR shader reference。父模块只通过 `mod material;` 与 `pub(crate) use self::material::add_gltf_material_subassets;` 保留原 importer 调用入口，不改变 Bevy-style glTF label names、`Material{n}`/`DefaultMaterial` output shape、texture dependency collection、texture transform metadata、default shader locator 或 scene/mesh material reference semantics。新增 `structure_convention/production_file_budget/asset_gltf_labeled_subassets.rs::runtime_15_gltf_labeled_material_subassets_are_child_owner`，锁定父/子 owner 布局、material/PBR/texture-slot helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/importer/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 glTF labeled material subasset owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 asset importer Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 texture descriptor settings parser owner split | runtime_15_texture_descriptor_settings_parser_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`asset/assets/texture/descriptor.rs` 当前 577 行，继续拥有 `TextureArrayLayout`、`TextureAssetDescriptor` DTO、`apply_import_settings(...)`、render descriptor projection、extent normalization 与默认 texture descriptor contract；folder-backed 子 owner `asset/assets/texture/descriptor/settings.rs` 当前 214 行，承接 TOML settings parser helpers、usage/asset_usage token parsing、sampler shorthand/table parsing、array layout/color-space/dimension parsing 与 Bevy-style token normalization。父模块只通过 `mod settings;` 与窄 `use self::settings::{...}` 消费解析 helper；fallible apply API 名称保持不变，并已由后续 F8 typed-error 切片改为 `TextureDescriptorResult<_>`，同时保留 Bevy alias diagnostics、RGBA8 linear-format normalization、2D/3D extent validation、serialized descriptor shape 与 `TextureAsset::render_image_descriptor()` 语义。新增 `structure_convention/production_file_budget/texture_descriptor_settings.rs::runtime_15_texture_descriptor_settings_parser_is_child_owner`，锁定父/子 owner 布局、settings parser helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/importer/render-assets/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 texture descriptor settings parser 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 texture importer/render assets Cargo sweep 仍 pending。 |
| F8 | Runtime 15 F8 texture descriptor typed errors | runtime_15_texture_descriptor_typed_errors_static_passed_cargo_deferred | 2026-06-25 | F8/E3/F5：`asset/assets/texture/descriptor.rs` 新增 `TextureDescriptorError` / `TextureDescriptorResult`，`TextureArrayLayout::from_import_settings(...)` 与 `TextureAssetDescriptor::apply_import_settings(...)` 不再返回 `Result<_, String>`；`asset/assets/texture/descriptor/settings.rs` 的 TOML setting type、u32 overflow、unsupported token 与 array-layout mode errors 均映射到 typed variants；`asset/assets/texture/texture_asset.rs` 的 array-layout RGBA8/2D/single-layer/divisibility/byte-length/extent overflow 校验同样回传 `TextureDescriptorError`。`asset/assets/{texture/mod.rs,mod.rs}` 公开错误类型，runtime 与 first-party texture importer plugin 仍只在 `AssetImportError::Parse` 边界格式化 Display 文案。`descriptor.rs` 内部测试新增 typed variant 断言，`review_f8_texture_import_settings_use_fallible_apply_not_with` 同步锁定 typed fallible apply API、settings 子 owner 无 `Result<_, String>` / `Err(format!(...))` 回退和 docs/status 锚点。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active deferred，不计通过。 |
| M4 | Runtime 15 M4 scene world render light collection owner split | runtime_15_scene_world_render_lights_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`scene/world/render.rs` 从 832 行减压为 725 行，继续拥有 scene render extract orchestration、camera descriptor/view extraction、mesh/sprite snapshot projection、post-process extract wiring、visibility input handoff 与 shared `entity_intersects_camera_layers(...)` 边界；新增 folder-backed 子 owner `scene/world/render/lights.rs`（169 行）承接 ambient/directional/point/rect/spot light snapshot collection、typed `RenderLayerSet` projection、默认 render layer fallback、light sort order 与 rect-light degraded reason。父模块只通过 `mod lights;` 和同一 `World` inherent method surface 消费子 owner，不改变 `RenderFrameExtract::lighting` 字段形状、scene camera layer filtering、particle/post-process 复用的 layer helper、shadow slot readiness 输入或 renderer light snapshot semantics。新增 `structure_convention/production_file_budget/scene_world_render_lights.rs::runtime_15_scene_world_render_light_collectors_are_child_owner`，锁定父/子 owner 布局、light collector 不回流、两侧低于 800 行预算，以及 Runtime 15/status/render extract/render submit/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描、尾随空白扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 scene world render light collection 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 scene/render Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 core runtime render-stats graph execution-resources owner split | runtime_15_render_stats_graph_execution_resources_owner_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R1.4/M4：`core/runtime/diagnostics/render_stats_store/graph.rs` 从 1024 行减压为 765 行，继续拥有 graph diagnostics dispatcher、frame graph aggregate rows、coverage/stage/materialization/alias/profile/post-process rows；新增 `core/runtime/diagnostics/render_stats_store/graph/execution_resources.rs`（260 行）承接 `last_graph_execution_resource_report` 投影、texture/buffer binding counts 与 transient-pool creation/reuse/retained/budget/eviction byte/count rows。父模块只通过 `mod execution_resources;` 与 `execution_resources::record(...)` 消费子 owner，不改变 `record_render_stats_diagnostics(...)` 入口或诊断 path 名称。新增 `structure_convention/production_file_budget.rs::runtime_15_render_stats_graph_execution_resources_are_child_owner`，锁定父/子 owner 布局、moved resource rows 不回流、两侧低于 800 行预算，以及 Runtime 15/status/core diagnostics/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；focused locked Cargo 120 秒超时无测试结果，不计 Cargo 通过。该记录关闭 render-stats graph execution-resources owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 runtime diagnostics Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 scene fixed light reflection write-field owner split | runtime_15_scene_fixed_light_reflection_write_fields_owner_split_static_passed_cargo_lock_blocked | 2026-06-23 | R1.4/M4：`scene/reflect/fixed/lights.rs` 从 906 行减压为 379 行，继续拥有 Ambient/Directional/Point/Rect/Spot light 的 schema registration、adapter construction、contains、read/read_fields 与 remove callbacks；新增 folder-backed 子 owner `scene/reflect/fixed/lights/write_fields.rs`（542 行）承接五类 light 的 editable field write callbacks、typed component presence checks、Vec2/Vec3/scalar/bool value validation 与 no-op mutation comparisons。父模块只通过 `mod write_fields;` 与 `use self::write_fields::{...}` 消费写入 callback，不改变 `ReflectComponent` 函数表、type path、字段名、错误类型或 World typed ECS mutation path。新增 `structure_convention/production_file_budget.rs::runtime_15_scene_fixed_light_reflection_write_fields_are_child_owner`，锁定父/子 owner 布局、write helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/scene reflect/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；`cargo test -p zircon_runtime --lib runtime_15_scene_fixed_light_reflection_write_fields_are_child_owner --locked` 被当前 `Cargo.lock`/`Cargo.toml` 漂移在编译前阻断，不计 Cargo 通过。该记录关闭 fixed light reflection write-field owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 scene reflection Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 scene world property-access physics write owner split | runtime_15_scene_world_property_access_physics_owner_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R1.4/M4：`scene/world/property_access/write.rs` 从 936 行减压为 593 行，继续拥有 `World::set_property(...)` dispatch、name/hierarchy/transform/camera/mesh/light/animation 与 dynamic component write routing；新增 folder-backed 子 owner `scene/world/property_access/write/physics.rs`（357 行）承接 rigid body、collider 与 joint property writes、physics value parsing、shape replacement、material override、axis lock 与 dynamic fallback。父模块只通过 `mod physics;` 和窄 `pub(super)` inherent methods 消费子 owner，不改变 `ComponentPropertyPath` 字段名、`ScenePropertyValue` 验证、string error contract 或 dynamic component fallback。新增 `structure_convention/production_file_budget/scene_world_property_access.rs::runtime_15_scene_world_property_access_physics_writes_are_child_owner`，锁定父/子 owner 布局、physics write helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/scene ECS/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；focused locked core Cargo 120 秒超时无测试结果，不计 Cargo 通过。该记录关闭 scene world property-access physics write owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 scene Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 scene world property-access physics entry owner split | runtime_15_scene_world_property_access_physics_entries_owner_split_static_passed_cargo_lock_blocked | 2026-06-23 | R1.4/M4：`scene/world/property_access/entries.rs` 从 843 行减压为 602 行，继续拥有 property entry traversal、name/hierarchy/transform/camera/mesh/light/animation 非 physics 投影、dynamic JSON 投影、literal matching 与 capacity routing；新增 folder-backed 子 owner `scene/world/property_access/entries/physics.rs`（281 行）承接 rigid body、collider 与 joint property entries、collider shape/material projection、constraint rows、joint limit rows 与 physics capacity hint。父模块只通过 `mod physics;` 和窄 `pub(super)` inherent methods 消费子 owner，不改变 property path、`ScenePropertyValue` 内容、animatable 标记或 entry 顺序。新增 `structure_convention/production_file_budget/scene_world_property_access.rs::runtime_15_scene_world_property_access_physics_entries_are_child_owner`，锁定父/子 owner 布局、physics entry helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/scene ECS/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-property-entries-0623 --message-format short --color never` 被当前 `Cargo.lock`/`Cargo.toml` 漂移在编译前阻断，不计 Cargo 通过。该记录关闭 scene world property-access physics entry owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 scene Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 scene world project I/O mesh owner split | runtime_15_scene_world_project_io_mesh_owner_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R1.4/M4：`scene/world/project_io.rs` 从 799 行减压为 667 行，继续拥有 scene asset load/save entry、entity loop、non-mesh component projection、project document normalization 与 default-node repair；新增 folder-backed 子 owner `scene/world/project_io/mesh.rs`（130 行）承接 `SceneMeshInstanceAsset` 与 `MeshRenderer` 的双向投影、LOD/primitive binding 映射、mesh/model/material handle/reference resolution。父模块只通过 `mod mesh;` 和 `mesh_from_asset(...)` / `mesh_to_asset(...)` 消费子 owner，不改变 `.zscene` 资产格式、builtin fallback、project manager lookup、mesh primitive/LOD 顺序或 scene save/load public API。新增 `structure_convention/production_file_budget/scene_world_project_io.rs::runtime_15_scene_world_project_io_mesh_is_child_owner`，锁定父/子 owner 布局、mesh projection helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/scene ECS/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；focused locked core Cargo 120 秒超时无测试结果，且未发现该 target-dir 的 cargo/rustc 残留进程，不计 Cargo 通过。该记录关闭 scene world project I/O mesh owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 scene Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI text layout engine visual-order owner split | runtime_15_ui_text_layout_engine_visual_order_owner_split_static_passed_cargo_deferred | 2026-06-23 | R1.4/M4：`ui/text/layout_engine.rs` 从 823 行减压为 530 行，继续拥有 text measurement、source-run wrapping、ellipsis fragment construction、direction resolution、alignment 和 `#[cfg(test)] mod tests` 挂载；新增 folder-backed 子 owner `ui/text/layout_engine/visual_order.rs`（301 行）承接 low-fidelity BiDi visual-order scaffold、visual token/cluster/fragment 类型、neutral-direction assignment 与 visual fragment coalescing。父模块只通过 `mod visual_order;` 与 `visual_order::apply_visual_order(...)` 消费子 owner，不改变 `layout_text(...)`、`measure_text_size(...)`、resolved line/run DTO 或文本测试入口。新增 `structure_convention/production_file_budget.rs::runtime_15_ui_text_layout_engine_visual_order_is_child_owner`，锁定父/子 owner 布局、visual-order helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI text layout engine visual-order owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI text Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI layout arrange grid/masonry owner split | runtime_15_ui_layout_arrange_grid_masonry_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/layout/pass/arrange.rs` 从 853 行减压为 690 行，继续拥有 layout arrange entry point、free/canvas/container/overlay dispatch、SizeBox/BlockBox/Linear/Scrollable/WrapBox fallback arrangement、scroll virtualization window planning、wrap content sizing 与 subtree hide helper；新增 folder-backed 子 owner `ui/layout/pass/arrange/grid_masonry.rs`（181 行）承接 GridBox placement/dimension/cell-frame helper、MasonryBox column selection/content-size computation、masonry child outer-height helper 与 grid/masonry recursive child arrangement。父模块通过 `mod grid_masonry;` 与窄 imports 消费子 owner，不改变 Taffy fallback order、GridBox slot placement/span behavior、MasonryBox sequential/shortest-column selection、visible subtree hiding 或 recursive arrange/clip propagation。新增 `structure_convention/production_file_budget/ui_layout_arrange.rs::runtime_15_ui_layout_arrange_grid_masonry_is_child_owner`，锁定父/子 owner 布局、grid/masonry helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI layout arrange grid/masonry owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI layout Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI template MUI X DataGrid class owner split | runtime_15_ui_template_mui_x_data_grid_class_owner_split_static_passed_cargo_deferred | 2026-06-23 | R1.4/M4：`ui/template/asset/compiler/style_apply/mui_x_classes.rs` 从 823 行减压为 575 行，继续拥有 MUI X component-family dispatch、MaterialTreeView、Date/Time Pickers、Charts、AgentChat/Chat component class routing 与共享 attribute helper；新增 folder-backed 子 owner `ui/template/asset/compiler/style_apply/mui_x_classes/data_grid.rs`（277 行）承接 DataGrid root utility classes、columnHeader/row/cell/toolbar/footer/loading/noRows slot utility classes，以及 DataGrid-specific sort/filter/pagination/virtualization state class projection。父模块只通过 `mod data_grid;`、`data_grid::append_component_classes(...)` 与 `data_grid::append_slot_classes(...)` 消费子 owner，不改变 template asset compiler style application order、selector matching、slot-props projection 或其他 MUI X component class behavior。新增 `structure_convention/production_file_budget/ui_template_style_apply.rs::runtime_15_ui_template_mui_x_data_grid_classes_are_child_owner`，锁定父/子 owner 布局、DataGrid helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 MUI X DataGrid class owner 的 M4 减压子切片；上层 `style_apply.rs`、完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI template Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI template document validation owner split | runtime_15_ui_template_document_validation_owner_split_static_passed_cargo_deferred | 2026-06-23 | R1.4/M4：`ui/template/asset/document.rs` 从 805 行减压为 653 行，继续拥有 `UiAssetDocumentRuntimeExt` runtime extension API、style rule/sheet mutation API、node iterator、parent lookup 与 tree mutation helpers；新增 folder-backed 子 owner `ui/template/asset/document/validation.rs`（100 行）承接 node id/tree authority validation、stylesheet id validation、style rule id validation 与 selector parse validation。父模块只通过 `mod validation;` 与 `validate_*` helper 消费子 owner，不改变 UI asset document public extension trait、style rule/sheet insertion semantics、node traversal order 或 tree mutation API。新增 `structure_convention/production_file_budget/ui_template_document.rs::runtime_15_ui_template_document_validation_is_child_owner`，锁定父/子 owner 布局、validation helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI asset document validation owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI template Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI template style slot-contract owner split | runtime_15_ui_template_style_slot_contract_owner_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R1.4/M4：`ui/template/asset/compiler/style_apply.rs` 从 831 行减压为 701 行，继续拥有 style plan construction、selector path matching、rule merge order、MUI `sx` merge、component root class dispatch、generic variant/color/size suppression 与共享 attribute helper；新增 folder-backed 子 owner `ui/template/asset/compiler/style_apply/slot_contract.rs`（207 行）承接 root slot props merge、child slot props/slot component/class projection、Skeleton child metadata routing，以及 owner slot utility class routing。父模块通过 `mod slot_contract;` 和 `pub(super) use slot_contract::{apply_mui_child_slot_props, apply_mui_root_slot_props_to_node, mui_slot_name}` 消费子 owner，并把 `mui_slot_name` 作为同级 MUI class owner 的窄 helper 出口；不改变 style application order、selector matching、MUI `sx` precedence、slot-props behavior 或 MUI component class routing。新增 `structure_convention/production_file_budget/ui_template_style_apply.rs::runtime_15_ui_template_style_slot_contract_is_child_owner`，锁定父/子 owner 布局、slot-contract helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-ui-template-slot-0623 --message-format short --color never` 120 秒超时无结果，不计 Cargo 通过。该记录关闭 UI template style slot-contract owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI template Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI v2 style runtime-state owner split | runtime_15_ui_v2_style_runtime_state_owner_split_static_passed_cargo_deferred | 2026-06-23 | R1.4/M4：`ui/v2/style.rs` 从 1142 行减压为 793 行，继续拥有 `UiV2StyleResolver`、`UiV2RuntimeStyleIndex` baseline/index API、rule collection、token/theme resolution、selector path DTO 与 selector matching；新增 folder-backed 子 owner `ui/v2/style/runtime_state.rs`（362 行）承接 static/runtime pseudo-state extraction、resolved painter state aliases、retained runtime-state attribute projection、runtime style dirty-delta classification 与 dirty flag merge。父模块通过 `mod runtime_state;` 与窄 helper imports 消费子 owner，不改变 UI v2 resolved style sheet shape、runtime style index public crate API、selector matching semantics、theme token resolution 或 retained component-state behavior。新增 `structure_convention/production_file_budget/ui_v2_style.rs::runtime_15_ui_v2_style_runtime_state_is_child_owner`，锁定父/子 owner 布局、runtime-state helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前存在外部 cargo/rustc 编译通道，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI v2 style runtime-state owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI v2 Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI accessibility extract state owner split | runtime_15_ui_accessibility_extract_state_owner_split_static_passed_cargo_deferred | 2026-06-23 | R1.4/M4：`ui/accessibility/extract.rs` 从约 993 行减压为 668 行，继续拥有 accessibility snapshot traversal、relation target collection/pruning、name/description resolution、child filtering、role inference、action defaults、bounds/visibility/reference parsing 与 diagnostic construction；新增 folder-backed 子 owner `ui/accessibility/extract/state.rs`（339 行）承接 expanded/open、disabled、selected、pressed、checked、value text、text selection、component-state/TOML value conversion 与 byte-offset clamping。父模块通过 `mod state;` 与窄 helper imports 消费子 owner，不改变 `accessibility_snapshot(...)` 输出形状、diagnostic code、hidden relation target retention、widget behavior mapping 或 accessibility action behavior。新增 `structure_convention/production_file_budget/ui_accessibility_extract.rs::runtime_15_ui_accessibility_extract_state_is_child_owner`，锁定父/子 owner 布局、state projection helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static checks、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI accessibility extract state projection owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI accessibility Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI component catalog editor-showcase helper owner split | runtime_15_ui_component_catalog_editor_showcase_helper_owner_split_static_passed_cargo_timeout_no_result | 2026-06-24 | R1.4/M4：`ui/component/catalog/editor_showcase.rs` 从 1029 行减压为 674 行，继续拥有 editor showcase registry、descriptor list、descriptor assembly entry point 与 representative component coverage；当前 folder-backed 子 owner 为 `ui/component/catalog/editor_showcase/descriptor_builders.rs`（原 M4 拆分 owner 已由后续 M2 命名硬切从 `helpers.rs` 收束到职责名），承接 base descriptor construction、layout role/default template projection、palette metadata、fallback policy、option/slot/value prop schema builders 与 TOML layout helpers。父模块通过 `mod descriptor_builders;` 与窄 imports 消费子 owner，不改变 editor showcase registry ids、component descriptors、palette metadata shape、fallback policy 或 component catalog public lookup behavior。新增 `structure_convention/production_file_budget/ui_component_catalog_editor_showcase.rs::runtime_15_ui_component_catalog_editor_showcase_helpers_are_child_owner`，锁定父/子 owner 布局、descriptor helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static checks、父子行数预算扫描、moved owner 扫描与 docs/status 锚点扫描通过；focused `cargo test -p zircon_runtime --lib runtime_15_ui_component_catalog_editor_showcase_helpers_are_child_owner --no-default-features --features core-min --locked --jobs 1` 305 秒超时无诊断结果，超时后另有 editor layout cargo/rustc 通道活跃，不计 Cargo 通过。该记录关闭 UI component catalog editor-showcase helper owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI component catalog Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI component state-reducer keyboard menu submenu owner split | runtime_15_ui_component_state_reducer_keyboard_menu_submenu_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/component/state_reducer/keyboard/menu.rs` 从 872 行减压为 609 行，继续拥有 menu focus-control detection、keyboard text typeahead、search query/filter state、search option flattening、recursive filtering 与 filtered option visibility；新增 folder-backed 子 owner `ui/component/state_reducer/keyboard/menu/submenu.rs`（271 行）承接 submenu focus loop、hover pending state、open/close state, active parent index, invalid submenu pruning 与 submenu target lookup。父模块通过 `mod submenu;` 与窄 re-export 保持 `keyboard.rs` 的 `menu::open_focused_submenu(...)` / `menu::close_active_submenu(...)` 调用形状，不改变 typeahead buffer、search filter payload、submenu focus scope 或 option target semantics。新增 `structure_convention/production_file_budget/ui_component_state_reducer_keyboard_menu.rs::runtime_15_ui_component_state_reducer_keyboard_menu_submenu_is_child_owner`，锁定父/子 owner 布局、submenu helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI component state-reducer keyboard menu submenu 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI component Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI component state-reducer tree view editing owner split | runtime_15_ui_component_state_reducer_tree_view_editing_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/component/state_reducer/tree_view.rs` 从 834 行减压为 508 行，继续拥有 tree-view identity、keyboard expand/collapse、toggle expanded、select option、multi/single selection、range selection、ordered/expanded/selected node id helpers 与 disabled-option checks；新增 folder-backed 子 owner `ui/component/state_reducer/tree_view/editing.rs`（312 行）承接 begin/cancel/commit rename、editing property selection、editing state clearing、rename payload writes、tree node label lookup 与 edit text validation。父模块通过 `mod editing;` 与窄 re-export 保持 state-reducer 上层的 `tree_view::apply_begin_edit(...)` / `tree_view::apply_cancel_editing(...)` / `tree_view::apply_commit(...)` 调用形状，不改变 selection focus、expanded items、rename payload property fallback 或 disabled-option validation。新增 `structure_convention/production_file_budget/ui_component_state_reducer_tree_view.rs::runtime_15_ui_component_state_reducer_tree_view_editing_is_child_owner`，锁定父/子 owner 布局、editing helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI component state-reducer tree view editing 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI component Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI surface event-routing owner split | runtime_15_ui_surface_event_routing_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/surface/surface.rs` 从 1161 行减压为 317 行，继续拥有 `UiSurface` state、runtime-state style entry points、surface frame/debug snapshots、property mutation、reflector/focus path entry points 与 focus-reconcile reason helper；新增 folder-backed 子 owner `ui/surface/surface/event_routing.rs`（578 行）承接 pointer capture/release、input/window dispatch adapters、pointer route construction、pointer dispatch side effects、navigation routing 与 activation phase helper；新增 `ui/surface/surface/pointer_component_events.rs`（356 行）承接 route-derived hovered/pressed/focused component-state dirtying、component event reports、focus event reports 与 damage frame requests。父模块只通过 `mod event_routing;` 和 `mod pointer_component_events;` 暴露同一 `UiSurface` inherent API，不改变 input dispatch result shape、pointer route semantics、default interaction order、focus visibility reason 或 component event payload。新增 `structure_convention/production_file_budget/ui_surface_event_routing.rs::runtime_15_ui_surface_event_routing_is_child_owner`，锁定父/子 owner 布局、event-routing helper 不回流、三侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI surface event-routing owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI surface Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI surface property mutation metadata dirty owner split | runtime_15_ui_surface_property_mutation_metadata_dirty_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/surface/property_mutation.rs` 从 839 行减压为 522 行，继续拥有 `UiPropertyMutationRequest`/`UiPropertyMutationReport`、`mutate_tree_property(...)` entry、visibility/input/state property mutation、binding report construction、template attribute sync、visibility/input value parsing 与 state dirty marking；新增 folder-backed 子 owner `ui/surface/property_mutation/metadata_dirty.rs`（322 行）承接 metadata attribute dirty-domain classification、render/virtualized dirty helpers、MUI overlay/feedback/transition/customization predicates、virtualized range predicates 与 layout metadata predicate。父模块通过 `mod metadata_dirty;` 与窄 imports 消费 `metadata_attribute_dirty` / `render_dirty`，不改变 property mutation accepted/unchanged/rejected report shape、binding update payload、dirty flag selection 或 component metadata update behavior。新增 `structure_convention/production_file_budget/ui_surface_property_mutation.rs::runtime_15_ui_surface_property_mutation_metadata_dirty_is_child_owner`，锁定父/子 owner 布局、metadata dirty helper 不回流、两侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI surface property mutation metadata dirty 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI surface Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI surface render feedback command/color owner split | runtime_15_ui_surface_render_feedback_command_color_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/surface/render/feedback.rs` 从 872 行减压为 590 行，继续拥有 feedback component kind detection、Alert/AlertTitle/Tooltip/Toast command layout、metadata text/icon/size extraction、border/radius parsing 与 public render entry points；新增 folder-backed 子 owner `ui/surface/render/feedback/colors.rs`（268 行）承接 AlertTone、feedback color constants、visual-state-aware alert/tooltip/toast color selection 与 style override fallback；新增 `ui/surface/render/feedback/commands.rs`（100 行）承接 quad/text/icon `UiRenderCommand` DTO construction。父模块通过 `mod colors;`、`mod commands;` 与窄 imports 消费子 owner，不改变 feedback render command ordering、z-index offsets、metadata key fallback、painter-state projection 或 owner text/image suppression behavior；既有 `feedback/state.rs`（70 行）继续拥有 painter family state resolution。新增 `structure_convention/production_file_budget/ui_surface_render_feedback.rs::runtime_15_ui_surface_render_feedback_commands_are_child_owners`，锁定父/子 owner 布局、color/command helper 不回流、四侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI surface render feedback command/color 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI surface render Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI surface default-interactions keyboard/timer owner split | runtime_15_ui_surface_default_interactions_keyboard_timer_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/surface/surface/default_interactions.rs` 从 973 行减压为 596 行，继续拥有 pointer default action routing、button/toggle/disclosure open-state helpers、shared binding report construction、widget behavior predicates 与 component event token matching；新增 folder-backed 子 owner `ui/surface/surface/default_interactions/keyboard.rs`（229 行）承接 keyboard-triggered default component actions、semantic keyboard actions/text、keyboard behavior eligibility 与 semantic action/event-kind mapping；新增 `ui/surface/surface/default_interactions/timers.rs`（172 行）承接 typeahead timeout、submenu hover delay、tooltip timer, menu-role detection, tooltip id extraction 与 timer-expired component event reports。父模块只通过 `mod keyboard;` 和 `mod timers;` 保持同一 `UiSurface` inherent API，不改变 default pointer/keyboard/timer behavior、binding report payload、menu role matching 或 timer property names。新增 `structure_convention/production_file_budget/ui_surface_default_interactions.rs::runtime_15_ui_surface_default_interactions_keyboard_timers_are_child_owners`，锁定父/子 owner 布局、keyboard/timer helper 不回流、三侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI surface default-interactions keyboard/timer owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI surface Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 UI surface table column helper owner split | runtime_15_ui_surface_table_column_helper_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`ui/surface/surface/default_interactions/table/mod.rs` 从 949 行减压为 677 行，继续拥有 table pointer flow、column resize/sort event flow、row selection/virtual scroll dispatch、table mutation helper 与 shared owner predicates；新增 folder-backed 子 owner `ui/surface/surface/default_interactions/table/columns.rs`（292 行）承接 column resize/sort metadata helper、column field/width/min-width lookup、sort direction/model helper、row sort comparison、column match predicates 与 resize drag token encode/decode。父模块通过 `mod columns;` 与显式 `columns::...` 调用消费子 owner，不改变 column resize payload、sort model writes、row client sorting、selection 或 virtual scroll behavior；既有 `selection.rs`（296 行）与 `virtualization.rs`（381 行）继续作为子 owner 保持低于 800 行预算。新增 `structure_convention/production_file_budget/ui_surface_table_columns.rs::runtime_15_ui_surface_table_column_helpers_are_child_owner`，锁定父/子 owner 布局、column helper 不回流、table 四侧低于 800 行预算，以及 Runtime 15/status/UI/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 cargo/rustc 通道仍活跃，Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 UI surface table column helper 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 UI surface Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output Runtime 15 row data split | runtime_15_status_output_runtime_15_row_data_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data.rs` 从 815 行减压为 55 行，只保留 status-output row group aggregation、child module mounts 与 `EXPECTED_STATUS_OUTPUT_SLICE_GROUPS`；新增 folder-backed 子 owner `tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15.rs`（低于 800 行）承接 Runtime 15 全部 expected status row literals。Runtime 15 结构守护的 status row source 读取点已转向该子 owner，避免把 row literal 复制回聚合入口。新增 `structure_convention/test_file_budget/status_output_row_data.rs::runtime_15_status_output_runtime_15_row_data_is_child_owner`，锁定父/子 owner 布局、Runtime 15 row literal 不回流、两侧低于 800 行预算，以及 Runtime 15/status/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved row-data 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 status-output Runtime 15 row-data 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output Runtime 15 foundation row data split | runtime_15_status_output_runtime_15_foundation_row_data_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15.rs` 从 791 行二级减压为 145 行，只保留 `foundation`、`m3`、`m4` 子 owner 挂载、F12 resource row literals 与窄常量转发；新增 folder-backed 子 owner `tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15/foundation.rs`（651 行）承接 Runtime 15 foundation/F5/F8/F9/F12/F13/F14 expected status row literals。顶层 `expected_status_row_data.rs` 继续通过 `runtime_15::RUNTIME_15_FOUNDATION_EXPECTED_STATUS_OUTPUT_SLICES` 挂载 foundation 数据，Runtime 15 父文件只通过 `foundation::FOUNDATION_EXPECTED_STATUS_OUTPUT_SLICES` 委派。新增 `structure_convention/test_file_budget/status_output_row_data/runtime_15_row_data.rs::runtime_15_status_output_runtime_15_foundation_row_data_is_child_owner`，锁定父/子 owner 布局、foundation row literal 不回流、两侧低于 800 行预算，以及 Runtime 15/status/structure/module docs 镜像锚。验证：scoped rustfmt/static checks、父子行数预算扫描、foundation row 回流扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；当前外部 Cargo/rustc 车道 active，Cargo 按支撑切片节奏 deferred，不计通过。该记录关闭 Runtime 15 foundation status row-data 的 M3 减压子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 support Hub project-actions tests child-owner split | runtime_15_support_hub_project_actions_tests_child_owner_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3：`zircon_hub/src/tauri_app/runtime_state/project_actions.rs` 从 1287 行减压为 674 行，只保留 Hub project create/import/pin/remove/delete action runtime owner、target resolution、lifecycle failure recording 和 display/picker helpers；新增 folder-backed 子 owner `zircon_hub/src/tauri_app/runtime_state/project_actions/tests.rs`（719 行）承接 create/import/delete 行为测试、session fixture 与 temp-dir fixture。父文件只通过 `#[path = "project_actions/tests.rs"] mod tests;` 挂载测试 owner，不改变 Hub project lifecycle action routing、recent-project metadata、Source Engine binding、localized task summary 或 Recycle Bin recovery behavior。新增 `structure_convention/test_file_budget/hub_project_actions.rs::runtime_15_support_hub_project_actions_tests_are_child_owner`，锁定父/子 owner 布局、测试 fixture 不回流、两侧低于 800 行预算，并同步 Runtime 15/status/structure/module/Hub lifecycle docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved test 回流扫描、docs/status 锚点扫描、runtime structure audit 和 scoped `git diff --check` 通过；runtime structure audit 报告 `project_actions_hotspot=0`、support-hub large-file hotspots=2、`module_gate_migration_debt_count=7`。当前外部 Cargo/rustc 车道 active，Cargo 按支撑切片节奏 deferred，不计通过。该记录关闭 support-hub project-actions 测试 owner 的 M3 减压子切片；剩余 support-hub large-file owners、`large_file_ownership_gate` 与完整 `module_convention_gate` 仍 pending。 |
| M3 | Runtime 15 M3 support Hub runtime-state tests child-owner split | runtime_15_support_hub_runtime_state_tests_child_owner_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3：`zircon_hub/src/tauri_app/runtime_state.rs` 从 1367 行减压为 927 行，继续拥有 Hub action router、snapshot projection input、navigation/search/filter/sort/view-mode state, settings save/persist, Source Engine selection/registration and project-scoped refresh helpers；新增 folder-backed 子 owner `zircon_hub/src/tauri_app/runtime_state/tests.rs`（435 行）承接 startup selection、load/repair/persist、save-settings/catalog refresh、payload-validation, persist-retry 和 localized project-view status tests。父文件只通过 `#[path = "runtime_state/tests.rs"] mod tests;` 挂载测试 owner，不改变 `HubRuntimeSession::apply_action()` routing、settings draft/save semantics、Source Engine registration、selected-project persistence 或 task-status localization。新增 `structure_convention/test_file_budget/hub_runtime_state.rs::runtime_15_support_hub_runtime_state_tests_are_child_owner`，锁定父/子 owner 布局、测试 fixture 不回流、父文件低于 1000 行 large-file hotspot 阈值、测试子文件低于 800 行预算，并同步 Runtime 15/status/structure/module/Hub actionable pages docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved test 回流扫描、docs/status 锚点扫描、runtime structure audit 和 scoped `git diff --check` 通过；runtime structure audit 报告 `runtime_state_hotspot=0`、support-hub large-file hotspots=1、`hotspot_count=2`、`module_gate_migration_debt_count=7`。当前外部 Cargo/rustc 车道 active，Cargo 按支撑切片节奏 deferred，不计通过。该记录关闭 support-hub runtime-state 测试 owner 的 M3 减压子切片；剩余 support-hub large-file owner、`large_file_ownership_gate` 与完整 `module_convention_gate` 仍 pending。 |
| M3 | Runtime 15 M3 support Hub view-model quick-actions/tests child-owner split | runtime_15_support_hub_view_model_quick_actions_tests_child_owner_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3：`zircon_hub/src/tauri_app/view_model.rs` 从 1390 行减压为 665 行，只保留 Hub view-model DTO 编排、project/recent/source-engine/task/action-history projection 入口与共享 DTO 结构；新增 folder-backed 子 owner `zircon_hub/src/tauri_app/view_model/quick_actions.rs`（342 行）承接 quick-action id/title/detail/enabled projection、selected/latest/stale project target 判定和 Source Engine binding 状态文案；新增 `zircon_hub/src/tauri_app/view_model/tests.rs`（388 行）承接 view-model filtering、quick-action、Source Engine row、relative-time 与 task-summary 行为覆盖。父文件只通过 `mod quick_actions;`、`use quick_actions::quick_actions;` 和 `#[path = "view_model/tests.rs"] mod tests;` 挂载子 owner，不改变 dashboard quick actions、selected/latest fallback、localized task summary 或 DTO shape。新增 `structure_convention/test_file_budget/hub_view_model.rs::runtime_15_support_hub_view_model_quick_actions_tests_are_child_owners`，锁定父/子 owner 布局、quick-action owner/test fixture 不回流、三侧低于 800 行预算，并同步 Runtime 15/status/structure/module/Hub actionable pages docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner/test 回流扫描、docs/status 锚点扫描、runtime structure audit 和 scoped `git diff --check` 通过；runtime structure audit 报告 support-hub large-file hotspots=0、`hotspot_count=1`、`module_gate_migration_debt_count=6`，剩余热点仅为 editor retained-host。当前外部 Cargo/rustc 车道 active，Cargo 按支撑切片节奏 deferred，不计通过。该记录关闭 support-hub view-model owner 的 M3 减压子切片；support-hub large-file 迁移债已清零，完整 `large_file_ownership_gate` 与 `module_convention_gate` 仍因 editor/graphics 等剩余迁移债 pending。 |
| M3 | Runtime 15 M3 editor retained-host workbench window projection tests child-owner split | runtime_15_editor_retained_host_workbench_window_projection_tests_child_owner_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3：`zircon_editor/src/ui/retained_host/ui/workbench_window_projection.rs` 从 1054 行减压为 958 行，继续拥有 editor retained-host workbench window DTO projection、visibility inheritance、style normalization、text/value projection、route binding helpers 与 host-property TOML conversion；新增 folder-backed 子 owner `zircon_editor/src/ui/retained_host/ui/workbench_window_projection/tests.rs`（96 行）承接 authored-label/value-text/segmented-value projection tests 与 `test_host_node` fixture。父文件只通过 `#[path = "workbench_window_projection/tests.rs"] mod tests;` 挂载测试 owner，不改变 Workbench projection behavior、host-contract DTO shape、style token resolution 或 retained-host rendering path。新增 `structure_convention/test_file_budget/editor_workbench_window_projection.rs::runtime_15_editor_retained_host_workbench_window_projection_tests_are_child_owner`，锁定父/子 owner 布局、测试 fixture 不回流、父文件低于 1000 行 large-file hotspot 阈值、测试子文件低于 800 行预算，并同步 Runtime 15/status/structure/module/editor workbench docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved test 回流扫描、docs/status 锚点扫描、runtime structure audit 和 scoped `git diff --check` 通过；runtime structure audit 报告 `hotspot_count=0`、`large_file_ownership_gate=classified-and-clear`、`large_file_migration_debt_count=0`、`module_gate_migration_debt_count=4`。当前外部 Cargo/rustc 车道 active，Cargo 按支撑切片节奏 deferred，不计通过。该记录关闭当前最后一个 large-file hotspot，完整 `large_file_ownership_gate` 已清零；完整 `module_convention_gate` 仍因 graphics/hard-cutover 等剩余迁移债 pending。 |
| M3 | Runtime 15 M3 production file budget core runtime guard split | runtime_15_production_file_budget_core_runtime_guard_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/structure_convention/production_file_budget.rs` 已从 779 行减压为低于 800 行的聚合/剩余 guard owner，并新增 folder-backed 子 owner `tests/runtime_absorption/structure_convention/production_file_budget/core_runtime_service_lists.rs` 承接 `runtime_15_core_runtime_service_lists_are_folder_backed` 与生产文件预算守护自检。父文件只保留 `mod core_runtime_service_lists;`、material/scene world 子 owner 挂载和剩余生产 owner guard；子文件验证 service-list production owner 布局、旧平铺文件不存在、父守护不回流、父/子两侧低于 800 行预算。新增 `runtime_15_production_file_budget_core_runtime_guard_is_child_owner`，同步 Runtime 15/status/structure/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved guard 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 production-file-budget core runtime guard 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`large_file_ownership_gate`、`module_convention_gate` 与全量 production-file guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 production file budget guard child-owner split | runtime_15_production_file_budget_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/structure_convention/production_file_budget.rs` 从 732 行进一步减压为 63 行，只保留 production-file-budget child module mounts 与共享 `read_runtime_src` / `read_repo` helper；原父文件剩余的 UI text layout、RHI WGPU command validation、RHI WGPU UI surface render/setup、render-stats graph、scene fixed lights、scene world render visibility 与 shadow view-projection 守卫分别迁入 `production_file_budget/ui_text_layout.rs`、`rhi_wgpu_command_validation.rs`、`rhi_wgpu_ui_surface_render_setup.rs`、`render_stats_graph.rs`、`scene_fixed_lights.rs`、`render_scene_world.rs` 与 `render_shadow.rs`。新增 `structure_convention/production_file_budget/module_layout.rs::runtime_15_production_file_budget_guard_child_owner_split`，锁定父模块只挂载子 owner、代表性 guard 不回流、父/关键子 owner 低于 300 行预算，以及 Runtime 15/status/structure/module docs 镜像锚。验证：scoped rustfmt/static checks、父子行数预算扫描、moved guard 扫描、docs/status 锚点扫描通过；Cargo 因当前外部 Cargo/rustc 车道 active 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 production-file-budget guard 聚合层的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`large_file_ownership_gate`、`module_convention_gate` 与全量 production-file guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output variable evidence anchors | runtime_15_status_output_variable_evidence_anchors_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_status_rows.rs` 的 `ExpectedStatusOutputSlice` 从固定 4 项证据数组收敛为 `&'static [&'static str]` 证据切片；`expected_status_row_data/**.rs` 的状态行证据统一写成 `&[...]`，保留 Runtime 15 M3/M4 中 5 项和 9 项证据锚点的真实记录，不通过删锚点来适配旧类型。新增 `structure_convention/test_file_budget/status_output_row_data.rs::runtime_15_expected_status_output_rows_accept_variable_evidence_anchors`，锁定类型别名不再使用 `[&'static str; 4]`，并抽查 M3/M4 多锚点行继续作为切片编译。验证：scoped rustfmt/static checks、可变锚点扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计 Cargo 通过。该记录关闭 status-output evidence arity 的 M3 支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output M3 row data child-owner split | runtime_15_status_output_m3_row_data_child_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15/m3.rs` 从 740 行二级减压为 31 行聚合父模块，只保留 `foundation_guards`、`ui_tests_first`、`asset_budget_tests`、`scene_script_tests`、`status_support`、`ui_tests_second` 与 `production_guard_support` 七个 child owner 挂载和窄常量转发；新增 `plan_status/status_output_tables/expected_status_row_data/runtime_15/m3/production_guard_support.rs` 等 M3 child row owners 承接原 78 条 M3 status row literals，最大 child `asset_budget_tests.rs` 为 157 行。顶层 `expected_status_row_data.rs` 改为逐组挂载 Runtime 15 M3 child constants，避免 M3 row literal 或单体 M3 row-data owner 回流。新增 `structure_convention/test_file_budget/status_output_row_data.rs::runtime_15_status_output_m3_row_data_child_owner_split`，锁定 7 个 child group 全部进入输出聚合、代表性 row literals 不回流、父/子 owner 低于 800 行预算，以及 Runtime 15/status/module docs 镜像锚。验证：scoped rustfmt/static checks、M3 child group aggregation scan、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 Cargo/rustc 车道 active 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 Runtime 15 M3 status row-data 二级 child-owner 减压；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output row-data guard child-owner split | runtime_15_status_output_row_data_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/structure_convention/test_file_budget/status_output_row_data.rs` 从 643 行减压为 15 行父模块，只保留 `module_layout`、`evidence_anchors`、`runtime_15_row_data`、`runtime_15_m4_row_data`、`runtime_15_m3_row_data` 与 `runtime_15_m3_child_groups` 六个 child owner 挂载；原 variable-evidence、Runtime 15 row-data、M4 row-data、M3 row-data 与 M3 child-group 守卫分别迁入 `structure_convention/test_file_budget/status_output_row_data/*.rs`，最大 child `runtime_15_m3_child_groups.rs` 为 180 行。新增 `structure_convention/test_file_budget/status_output_row_data/module_layout.rs::runtime_15_status_output_row_data_guard_child_owner_split`，锁定父模块只挂载 child owners、代表性 guard 不回流、父/子 owner 低于 800 行预算，以及 Runtime 15/status/module docs 镜像锚；`root_layout/status_scan.rs` 同步纳入新 child owner 行数预算。验证：scoped rustfmt/static checks、父子行数预算扫描、moved guard 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 Cargo/rustc 车道 active 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 status-output row-data guard 自身的 M3 child-owner 减压；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output Runtime 15 M4 row data split | runtime_15_status_output_runtime_15_m4_row_data_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15.rs` 已从 780 行减压为 723 行，并改成 foundation、M4、F12 resource、M3 四个显式 row group owner；新增 folder-backed 子 owner `tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15/m4.rs`（85 行）承接 Runtime 15 M4 expected status row literals。`expected_status_row_data.rs` 现在按四组挂载 Runtime 15 row data，避免 M4 row literal 回流到 Runtime 15 父文件。新增 `structure_convention/test_file_budget/status_output_row_data.rs::runtime_15_status_output_runtime_15_m4_row_data_is_child_owner`，锁定父聚合引用、Runtime 15 父/子 owner 布局、M4 row literal 不回流、三侧低于 800 行预算，以及 Runtime 15/status/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved M4 row-data 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 status-output Runtime 15 M4 row-data 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output expected-slice maps split | runtime_15_status_output_expected_slice_maps_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status.rs` 从 737 行减压为 572 行，`tests/runtime_absorption/plan_status/status_output_tables/expected_slices/date.rs` 从 616 行减压为 451 行；新增 folder-backed 子 owner `tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status/runtime_15.rs`（179 行）和 `tests/runtime_absorption/plan_status/status_output_tables/expected_slices/date/runtime_15.rs`（177 行）承接 Runtime 15 status/date literal 映射。父文件只保留 `runtime_15::expected_*_for_slice(slice)` 路由和非 Runtime 15 分支，避免 Runtime 15 expected-slice literal 回流。新增 `structure_convention/test_file_budget/status_output_expected_slices.rs::runtime_15_status_output_expected_slice_maps_are_child_owners`，并在 `test_file_budget/root_layout.rs` 纳入新守护文件行数预算；同步 Runtime 15/status/module docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、moved expected-slice 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 status-output expected-slice map 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output Runtime 15 expected-slice child-owner split | runtime_15_status_output_runtime_15_expected_slice_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status/runtime_15.rs` 从 750 行减压为约 15 行路由父文件，`date/runtime_15.rs` 从 601 行减压为约 15 行路由父文件；新增 `status/runtime_15/{foundation,naming_boundary,m4_surface_cleanup,m3_structure_support}.rs` 与 date 对应四个 child owner，承接 Runtime 15 foundation/F5/F8/F12/F13、M1/M2 命名边界、M4/渲染清理、M3 测试与状态支撑 literal。新增 `structure_convention/test_file_budget/status_output_expected_slices/maps.rs::runtime_15_status_output_runtime_15_expected_slice_maps_are_child_owners`，锁定父文件只分发、代表性 literal 不回流、所有 child owner 低于 400 行预算，并同步状态输出期望行（含 `plan_status/status_output_tables/expected_slices/status/runtime_15/m3_structure_support.rs`）。验证：scoped rustfmt/static scans、父子行数预算扫描、moved expected-slice literal 扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 Runtime 15 expected-slice map 的 M3 child-owner 减压；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output expected-slice guard maps child-owner split | runtime_15_status_output_expected_slice_guard_maps_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`tests/runtime_absorption/structure_convention/test_file_budget/status_output_expected_slices/maps.rs` 从 572 行守护 owner 减压为路由父文件，只挂载 `maps/top_level_maps.rs` 与 `maps/runtime_15_topics.rs`；新增 `structure_convention/test_file_budget/status_output_expected_slices/maps/runtime_15_topics.rs` 承接 Runtime 15 expected-slice child-owner 守护，`maps/top_level_maps.rs` 承接顶层/legacy map 守护，父子 owner 均低于 400 行 focused 预算。新增 `runtime_15_status_output_expected_slice_guard_maps_are_child_owners`，锁定父文件不再定义被移动的两个守护、子 owner 保留原守护、状态输出期望行与 Runtime 15/status/module/session docs 锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved guard 扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按支撑切片节奏 deferred，不计 Cargo 通过。完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output expected-slice top-level map support child-owner split | runtime_15_status_output_expected_slice_top_level_map_support_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`structure_convention/test_file_budget/status_output_expected_slices/maps/top_level_maps.rs` 从 390 行近预算守护体减压为 139 行父文件，只挂载 `structure_convention/test_file_budget/status_output_expected_slices/maps/top_level_maps/assertions.rs` 与 `structure_convention/test_file_budget/status_output_expected_slices/maps/top_level_maps/sources.rs` 两个 support child owner；`assertions.rs` 承接 expected-slice parent/topic/legacy map 断言组，`sources.rs` 承接批量 source reads，最大 child 为 349 行，父子 owner 均低于 400 行 focused 预算。新增 `runtime_15_status_output_expected_slice_top_level_map_support_child_owners_are_folder_backed`，锁定 support owner 挂载、父文件不再承接批量 reads/断言组、父级 guard maps 测试计数更新为 4，并同步 Runtime 15/status/module/session docs 锚。验证按支撑切片节奏使用 scoped rustfmt/static scans、父子行数预算扫描、moved support anchor 扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active deferred，不计 Cargo 通过。 |
| M3 | Runtime 15 M3 status output expected-slice legacy child-owner split | runtime_15_status_output_expected_slice_legacy_child_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status.rs` 与 `tests/runtime_absorption/plan_status/status_output_tables/expected_slices/date.rs` 继续减压为 11 行路由父文件，只挂载 `runtime_15` 与 `pre_runtime_15` 子 owner；新增 folder-backed 子 owner `tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status/pre_runtime_15.rs`（567 行）和 `tests/runtime_absorption/plan_status/status_output_tables/expected_slices/date/pre_runtime_15.rs`（446 行）承接 Runtime 01-14 status/date literal 映射。新增 `structure_convention/test_file_budget/status_output_expected_slices.rs::runtime_15_status_output_expected_slice_legacy_maps_are_child_owners`，锁定父入口不再保留 legacy literal、Runtime 15 子 map 记录本切片、父/子 owner 低于 800 行预算，并同步 Runtime 15/status/module docs 镜像锚。验证：scoped rustfmt/static checks、父子行数预算扫描、moved legacy expected-slice 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 Cargo/rustc 车道 active 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 expected-slice map legacy 分支的 M3 child-owner 减压；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output expected-slice legacy group child-owner split | runtime_15_status_output_expected_slice_legacy_group_child_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_slices/status/pre_runtime_15.rs` 与 `tests/runtime_absorption/plan_status/status_output_tables/expected_slices/date/pre_runtime_15.rs` 继续收束为 17 行左右的 legacy group 路由父文件，只挂载 `runtime_01_05`、`runtime_06_10` 与 `runtime_11_14` 三组 child owner。新增 `plan_status/status_output_tables/expected_slices/status/pre_runtime_15/runtime_01_05.rs`（228 行）、`plan_status/status_output_tables/expected_slices/status/pre_runtime_15/runtime_06_10.rs`（240 行）、`plan_status/status_output_tables/expected_slices/status/pre_runtime_15/runtime_11_14.rs`（111 行）以及 `plan_status/status_output_tables/expected_slices/date/pre_runtime_15/runtime_01_05.rs`（100 行）、`plan_status/status_output_tables/expected_slices/date/pre_runtime_15/runtime_06_10.rs`（225 行）、`plan_status/status_output_tables/expected_slices/date/pre_runtime_15/runtime_11_14.rs`（131 行）承接 Runtime 01-14 status/date legacy literal。新增 `structure_convention/test_file_budget/status_output_expected_slices.rs::runtime_15_status_output_expected_slice_legacy_group_maps_are_child_owners`，锁定 group 父文件不保留 `Runtime 05 plan-status Cargo attempt 状态审计`、`Runtime 08 F17 entity path lookup verb rename`、`Runtime 14 animation Cargo gate 尝试` 等代表性 literal，Runtime 15 status/date maps 与 status rows 记录本切片，并同步 Runtime 15/status/module docs 镜像锚。验证：scoped rustfmt --check、父子行数预算扫描、moved legacy group literal 扫描、docs/status 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 因当前外部 Cargo/rustc 车道 active 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 pre-Runtime-15 expected-slice legacy map 的 group child-owner 减压；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 status output Runtime 15 M3 row data split | runtime_15_status_output_runtime_15_m3_row_data_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15.rs` 从 727 行减压为 240 行，只保留 foundation、F12 resource row literals 与 M3/M4 子 owner 挂载；新增 folder-backed 子 owner `tests/runtime_absorption/plan_status/status_output_tables/expected_status_row_data/runtime_15/m3.rs`（501 行）承接 Runtime 15 M3 expected status row literals。顶层 `expected_status_row_data.rs` 仍按 Runtime 15 四组聚合，Runtime 15 父文件通过 `m3::EXPECTED_STATUS_OUTPUT_SLICES` 委派 M3 row data，避免 M3 row literal 随后续状态行回流。新增 `structure_convention/test_file_budget/status_output_row_data.rs::runtime_15_status_output_runtime_15_m3_row_data_is_child_owner`，锁定顶层聚合引用、Runtime 15 父/子 owner 布局、M3 row literal 不回流、父/子/守护文件低于 800 行预算，并要求 status/date expected-slice maps 与 Runtime 15/status/module docs 镜像锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved M3 row-data 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 status-output Runtime 15 M3 row-data 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 status-output guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 code review findings test folder split | runtime_15_code_review_findings_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/code_review_findings.rs` 已拆成 folder-backed `tests/runtime_absorption/code_review_findings/{typed_error_convergence,f8_api_convergence,late_api_cleanup,p0_robustness,plugin_importer_dx,render_structure,f12_dead_code}` 评审守卫 owner；父文件只保留子模块挂载。后续 typed-error child-owner split、animation asset/profile export/gameplay host/script scene hook/export CLI/host reflection docs CLI/dynamic API session typed-error guards、F1 native host callback panic guard、F2/F4 review guard、D8 runtime registration builder original evidence paths guard `review_d8_runtime_registration_builder_original_evidence_paths_use_sdk_builder`（`d8_runtime_registration_builder_original_paths_static_passed_cargo_deferred`）、D12 runtime helper export macro guard `review_d12_runtime_helper_exports_use_sdk_macro`（`d12_runtime_export_macro_review_synced_static_passed_cargo_deferred`）、D13 importer runtime export macro convergence guard `review_d13_importer_runtime_exports_use_sdk_macro`（`d13_importer_runtime_export_macro_convergence_static_passed_cargo_deferred`）、D13 importer runtime manifest builder convergence guard `review_d13_importer_runtime_manifests_use_sdk_builder`（`d13_importer_runtime_manifest_builder_convergence_static_passed_cargo_deferred`）、D13 importer manifest parity guard `review_d13_importer_manifest_parity_guard_lives_in_sdk_builder`（`d13_importer_manifest_parity_guard_static_passed_cargo_deferred`）、F16 compiled-scene split status guard 与 F12 dead-code review status sync 追加后，57 个评审守卫保留，typed-error 子目录新增 `diagnostics.rs`、`export_cli.rs`、`dynamic_api.rs` 与 `script_host.rs`，P0 child owner 新增 `p0_robustness.rs`，plugin importer DX child owner 新增 `plugin_importer_dx.rs` 并通过 `plugin_importer_dx/d12_runtime_exports.rs` 和 `plugin_importer_dx/d13_importer_sdk.rs` 锁定 `RuntimePluginRegistrationBuilder`、`RuntimePluginModuleRegistration::event`、`zircon_plugin_sdk::runtime_plugin_exports!`、`ImporterRuntimeManifestBuilder` 与 `importer_runtime_manifest_builder_keeps_targets_platforms_modules_and_distribution_in_parity`，F16 child owner 新增 `render_structure.rs`，F12 child owner 新增 `f12_dead_code.rs`，最大子文件 `f8_api_convergence.rs` 仍低于 800 行预算。新增 `structure_convention/test_file_budget/code_review_findings.rs::runtime_15_code_review_findings_tests_are_folder_backed`，验证父/子模块挂载、代表性 F1/F5/F8/F11/F12/F16/F19/D8/D12/D13 moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `runtime_absorption/code_review_findings.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 D12 runtime helper export macro review sync | d12_runtime_export_macro_review_synced_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + Plugins 12 M3/D12：本切片不改插件生产行为，只同步 D12 runtime helper export macro rollout 的评审总表、Runtime 15 状态输出和 code-review findings folder-backed 守卫。`zircon_plugin_sdk::runtime_plugin_exports!` 已在 15 个 first-party trait-backed runtime roots 中生成标准 `runtime_plugin()`、`package_manifest()`、`runtime_selection()` 与 `plugin_registration()` helper；原始证据路径 animation/physics/net 和后续 ai/hybrid_gi/navigation/net/particles/physics/prefab_tools/rendering/solari/terrain/texture/tilemap_2d/virtual_geometry/zr_vm_language 均不再保留本地手写 helper block。新增 `tests/runtime_absorption/code_review_findings/plugin_importer_dx/d12_runtime_exports.rs::review_d12_runtime_helper_exports_use_sdk_macro`，并把 `runtime_15_code_review_findings_tests_are_folder_backed` 更新为 53 个 review guards，锁定 `plugins_12_runtime_export_macro_rollout_check_passed`、`d12_runtime_export_macro_review_synced_static_passed_cargo_deferred`、SDK macro 定义和跨文档状态锚。Cargo gate 按本状态 deferred。 |
| M3 | Runtime 15 M3 D-S7 static plugin manifest generation/parity review sync | ds7_static_plugin_manifest_generation_parity_review_synced_static_passed_cargo_deferred | 2026-06-28 | R4.1/M3 + Plugins 12 M1：本切片不改 runtime/plugin 生产代码，只同步评审总表、Plugins 12 状态、结构规范、status-output expectations 与 `p0_robustness.rs::review_priority_recommendation_tracks_current_remaining_work`。D-S7 当前由 `plugins_12_static_plugin_manifest_is_generated`、`plugins_12_manifest_schema_uniform_audit_report_is_clean`、`plugins_12_feature_enabled_runtime_descriptor_manifest_parity` 和 `tools/audit_plugin_structure.py --json` 守住：`expected_manifest_count = 37`、`manifest_count = 37`、`generated_manifest_count = 36`、`hand_written_native_manifest_count = 1`、`manifest_schema_violations = 0`、`generated_manifest_header_violations = 0`、`m1_gate_status = classified-and-clear`。优先级段落不再把 D-S7 当作剩余高 ROI；Cargo gate deferred。 |
| M3 | Runtime 15 M3 typed-error convergence guard child-owner split | runtime_15_typed_error_convergence_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3 follow-up：`tests/runtime_absorption/code_review_findings/typed_error_convergence.rs` 已退役并拆成 folder-backed `typed_error_convergence/mod.rs`，当前子 owner 为 `animation_resource.rs`、`asset_loaders.rs`、`asset_records.rs`、`diagnostics.rs`、`script_host.rs` 与 `scene_world.rs`。新增 sound、animation asset、profile export、gameplay host 与 script scene hook typed-error guards 后，typed-error review surface 保留 19 个测试，整体 code-review findings 保留 29 个 review guards；最大 typed-error 子文件 `scene_world.rs` 为 478 行，`asset_records.rs` 为 425 行，全部低于 800 行测试 owner 预算。`runtime_15_code_review_findings_tests_are_folder_backed` 更新为锁定 typed-error 子目录挂载、`review_f5_sound_asset_uses_typed_error` / `review_f5_profile_export_uses_typed_error` / `review_f5_gameplay_host_uses_typed_errors_before_script_host_boundary` / `review_f5_script_scene_hook_uses_typed_errors_before_core_boundary` 不丢失、29 个 review guards、父/子 owner 预算和跨文档状态锚。验证：scoped rustfmt/static line-count/docs anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active deferred，不计通过。该记录关闭 typed-error convergence guard 的新增 oversized test owner 债；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 code-review guard Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI architecture test folder split | runtime_15_ui_architecture_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/ui_architecture.rs` 已拆成 folder-backed `tests/runtime_absorption/ui_architecture/architecture_boundaries.rs`、`tests/runtime_absorption/ui_architecture/legacy_renames.rs` 与 `tests/runtime_absorption/ui_architecture/mirror_docs.rs` 三个 Runtime 09 UI architecture absorption guard owner；父文件从 1251 行降到 104 行，只保留共享扫描 helper 与子模块挂载。18 个 Runtime 09 UI architecture 守卫全部保留，最大子文件 `legacy_renames.rs` 为 530 行、`architecture_boundaries.rs` 为 505 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/ui_architecture.rs::runtime_15_ui_architecture_tests_are_folder_backed`，验证父/子模块挂载、代表性 architecture/legacy/mirror moved guard 不回流、迁移测试数量、owner 行数预算，并要求 `ui_architecture_boundary.py` 读取新的 child guard sources。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、include_str 路径解析、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `runtime_absorption/ui_architecture.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files` 仍 pending 于后续 oversized test owner；`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 dynamic scene absorption guard folder split | runtime_15_dynamic_scene_absorption_guard_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/dynamic_scene.rs` 中单个 814 行 Runtime 05 dynamic-scene patch/session 吸收守卫已拆成 folder-backed `tests/runtime_absorption/dynamic_scene/{patch_preview_api,patch_preview_status_docs,patch_preview_behavior,session_capture_persistence,session_retention_mutation_merge,session_load_query_path,asset_reload_selection_status}.rs` 七个 guard owner；父文件降到 38 行，只保留共享 `include_str!` source 常量与子模块挂载。原 `runtime_05_dynamic_scene_patch_preview_api_stays_read_only` 保留在 `tests/runtime_absorption/dynamic_scene/patch_preview_api.rs`，最大子文件 `tests/runtime_absorption/dynamic_scene/asset_reload_selection_status.rs` 为 268 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/dynamic_scene_absorption.rs::runtime_15_dynamic_scene_absorption_guard_is_folder_backed`，验证父/子模块挂载、七个 guard fragment、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、include_str 路径解析、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `tests/runtime_absorption/dynamic_scene.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files` 仍需避开活跃 render/editor/plugin 会话继续处理非本轮 owner，`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M1 | Runtime 15 graphics facade visibility note | runtime_15_graphics_facade_visibility_note_static_passed_cargo_blocked_graphics_drift | 2026-06-22 | R3.4/S3：`graphics/mod.rs` 已把混排的 `pub mod` / `pub(crate) mod` / crate-visible bridge / test-only export 分成明确区块，源码注释固定公共 module entries、公共 façade exports、crate-private implementation owners、crate-visible bridge 与 test-only access；导出集合未扩大，`backend`/`scene`/`types` 等 implementation module 仍为 `pub(crate)`。新增 `runtime_15_mixed_visibility_has_facade_note` 结构守卫并同步状态输出锚。验证：scoped rustfmt with `skip_children=true` 通过；standalone `runtime_15_mixed_visibility_has_facade_note` 1/1 通过；状态锚静态检查通过；Cargo 聚焦测试/`status_output` 尝试被当前 graphics 编译漂移阻塞（`FrameSubmissionContext::new` 参数数不匹配、`AdvancedProfileRuntimePlan: Default` 缺失），本目标目录残留 Cargo/rustc 已停止；core-min crate 编译门不计通过。 |
| M5 | Runtime 15 F9 runtime prelude required type coverage | runtime_15_prelude_required_types_coremin_check_passed | 2026-06-22 | R3.3/S9/F9：新增 `asset/prelude.rs`、`scene/prelude.rs`、`ui/prelude.rs`、`graphics/prelude.rs`，crate 级 `prelude.rs` 改为聚合 `pub use crate::{asset,scene,ui,graphics}::prelude::*`，覆盖 asset/scene ECS/UI/graphics 高频类型并避免直接在 crate prelude 维护符号清单。新增 `runtime_prelude_exports_asset_scene_ui_and_graphics_contracts` 行为测试和 `runtime_15_prelude_covers_required_types` 结构守卫；状态输出锚同步到 `expected_status_row_data.rs`。验证：scoped rustfmt --check 通过；`runtime_prelude_exports_asset_scene_ui_and_graphics_contracts` 1/1 通过；`runtime_15_prelude_covers_required_types` 1/1 通过；`status_output` 2/2 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked` 通过（既有 warnings）。 |
| M5 | Runtime 15 runtime UI dead-code support split | runtime_15_runtime_ui_dead_code_support_split_coremin_check_passed | 2026-06-22 | E6/S10/F10/F12：`PublicRuntimeFrame` 从旧 `ui/runtime_ui` 迁到生产 owner `ui/public_runtime_frame.rs`，`RuntimeUiManager` / `RuntimeUiFixture` / input-router / window-event support 移入 `ui/tests/runtime_ui_support` 并只由 `ui/mod.rs` 通过 `#[cfg(test)]` + `#[path = "tests/runtime_ui_support/mod.rs"]` 挂载；`ui/mod.rs` 删除 `#[allow(dead_code)] mod runtime_ui;`，旧生产 `ui/runtime_ui/` 目录移除，无兼容 re-export 或 shim。`graphics/types/viewport_render_frame_from_public_runtime.rs` 继续从 `crate::ui::PublicRuntimeFrame` 构造 `ViewportRenderFrame`，并保持共享 `Arc<RenderFrameExtract>` 路径。新增 `runtime_15_runtime_ui_dead_code_surface_is_test_support` 结构守卫，状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、ui_architecture 3/3、status-output 2/2 通过；direct ui_architecture_boundary_audit risks=[]（ui entries 18/18、taffy hits/files 175/175 与 10/10）；core-min focused cargo test `runtime_15_runtime_ui_dead_code_surface_is_test_support` 1/1 通过；core-min `cargo check` 通过（既有 warnings）。 |
| M5 | Runtime 15 F12 runtime-owned dead-code suppression cleanup | runtime_15_runtime_owned_dead_code_suppression_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：清理 runtime-owned dead-code suppression 子切片，未触碰 active render/provider、plugin、editor 会话区域。`asset/pipeline/worker_pool.rs` 的 test-only `request_rx_guard` 不再用 `#[allow(dead_code)]`，改由 `request_channel_guard_is_alive_for_test()` 明确读取并由 worker-pool 行为测试断言，保留 bounded overflow 无 worker 场景的通道存活语义；`core/runtime/state/module_entry.rs` 删除 descriptor 字段抑制，新增 `ModuleEntry::descriptor()`，`core/runtime/diagnostics/devtools.rs` 通过 accessor 消费 module name/description/service 计数。新增 `runtime_15_runtime_owned_dead_code_suppression_cleanup` 结构守卫并同步状态输出期望行。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-f12-cargo-0622` 通过（既有 warnings）。该记录只关闭 runtime-owned 两处 suppression 子切片；graphics resources、script value descriptors 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 script host value descriptor dead-code cleanup | runtime_15_script_host_value_descriptors_coremin_check_passed | 2026-06-22 | E6/S10/F12：`script/vm/host/builtin_host_modules.rs` 的 `Vec3` 与 `ColorRgba` 反射值描述器不再依赖 `#[allow(dead_code)]`；新增字段布局哨兵构造并读取 `Vec3 { x, y, z }` 与 `ColorRgba { r, g, b, a }`，让 `zircon_host_module!` 收集的 descriptor-only 类型保持编译器可见，同时不新增 VM host call、不改变 Runtime 13 ledger 的 6 模块 / 52 固定函数 / 2 类型描述器基线。新增 `runtime_15_script_host_value_descriptors_do_not_suppress_dead_code` 结构守卫，同步状态输出期望行和 `docs/zircon_runtime/script/vm/host/function_ledger.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-f12-script-cargo-0622` 通过（既有 warnings）。该记录只关闭 script host value descriptor 子切片；graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 script reflection macro fixture dead-code cleanup | runtime_15_script_reflection_macro_fixture_dead_code_cleanup_static_passed_cargo_deferred | 2026-06-23 | E6/S10/F12：`script/vm/tests/reflection_docs.rs` 的 `TestVec3`、`TestEnum` 与 nested `Point` 反射宏 fixture 不再依赖 `#[allow(dead_code)]`；测试现在构造并读取 `TestVec3` 字段、用 `matches!(TestEnum::A, TestEnum::A)` 覆盖枚举变体、通过 `macro_math::point_fixture_x()` 读取 nested module fixture 字段。新增 `runtime_15_script_reflection_macro_fixtures_do_not_suppress_dead_code` 结构守卫，同步状态输出期望行、script VM tests 文档和 host reflection 文档。验证：scoped rustfmt/static checks、dead-code suppression 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录只关闭 reflection macro test fixture 子切片；graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 offscreen target texture owner cleanup | runtime_15_offscreen_target_texture_owner_cleanup_static_passed_cargo_timeout_no_result | 2026-06-22 | E6/S10/F12：`graphics/backend/render_backend/offscreen_target.rs` 删除固定帧 WGPU texture owner 字段上的 `#[allow(dead_code)]`，新增 `OffscreenTarget::RETAINED_FRAME_TEXTURE_COUNT` 与 `retained_frame_texture_count()` 显式读取 `final_color`、GI、scene color、bloom、G-buffer、normal、AO 与 depth 的 9 个 owner；`bind_frame_graph_resources(...)` 通过 debug assertion 消费该契约，说明这些字段是支撑 graph-imported `TextureView` 的生产保活 owner，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_offscreen_target_texture_owner_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output all-subplans guard 1/1 通过；core-min Cargo check 在独立 target 目录 10 分钟超时无编译结果，残留本切片 cargo/rustc 进程已停止，不计通过。该记录只关闭 OffscreenTarget 固定帧 texture owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 render backend state owner cleanup | runtime_15_render_backend_state_owner_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/backend/render_backend/render_backend.rs` 删除 `instance`、`adapter`、`config` 字段上的 `#[allow(dead_code)]`，新增 `RenderBackend::RETAINED_STATE_OWNER_COUNT` 与 `retained_state_owner_count()` 显式读取 backend state owner；`RenderBackend::caps()` 通过 debug assertion 消费该契约，说明这些字段负责保留 WGPU instance/adapter 和 backend config 生命周期，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_render_backend_state_owner_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-render-backend-state-owner-0622` 通过（既有 warnings）。该记录只关闭 RenderBackend state owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu texture resource owner cleanup | runtime_15_gpu_texture_resource_owner_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/gpu_texture/gpu_texture_resource.rs` 删除 `id`、`texture`、`view`、`sampler` 字段上的 `#[allow(dead_code)]`，新增 `GpuTextureResource::RETAINED_TEXTURE_BINDING_OWNER_COUNT` 与 `retained_texture_binding_owner_count()` 显式读取 texture identity 与 WGPU texture/view/sampler owner；`view()` / `sampler()` 通过 debug assertion 消费该契约，说明这些字段负责保留材质绑定背后的 GPU resource 生命周期，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_gpu_texture_resource_owner_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-texture-owner-0622` 通过（既有 warnings）。该记录只关闭 GpuTextureResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu material uniform owner cleanup | runtime_15_gpu_material_uniform_owner_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs` 删除 `buffer`、`payload_byte_len`、`buffer_byte_len` 字段上的 `#[allow(dead_code)]`，新增 `GpuMaterialUniformResource::RETAINED_MATERIAL_UNIFORM_OWNER_COUNT` 与 `retained_material_uniform_owner_count()` 显式读取 uniform buffer 与 byte-length diagnostics owner；`binding_resource()` 通过 debug assertion 消费该契约，`resource_streamer_accessors.rs` 通过 `payload_byte_len()` / `buffer_byte_len()` 读取诊断长度，说明这些字段负责保留材质 uniform bind group 背后的 GPU buffer 和调试计数，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_gpu_material_uniform_owner_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-material-uniform-owner-0622` 通过（既有 144 warnings）。该记录只关闭 GpuMaterialUniformResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu mesh order signature cleanup | runtime_15_gpu_mesh_order_signature_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/gpu_mesh/gpu_mesh_resource.rs` 删除 `indirect_order_signature` 字段上的 `#[allow(dead_code)]`，字段收窄为 `gpu_mesh` owner 内部字段并通过 `indirect_order_signature()` accessor 暴露；`graphics/scene/resources/gpu_mesh/gpu_mesh_resource_from_asset.rs` 继续从 position、normal、uv、joint indices/weights、tangent、color 与 index payload 派生完整 order signature；`graphics/scene/scene_renderer/mesh/build_mesh_draws/build/extend_pending_draws_for_mesh_instance.rs` 的 prepared mesh draw tie-breaker 通过 `mesh_order_command_sort_tie_breaker(...)` 混入该签名，说明该字段是 Virtual Geometry / indirect submission 顺序契约的 live 输入，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_gpu_mesh_order_signature_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-mesh-order-0622` 通过（既有 warnings）。该记录只关闭 GpuMeshResource order-signature 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 gpu model identity cleanup | runtime_15_gpu_model_identity_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/gpu_model/gpu_model_resource.rs` 删除 `id` 字段上的 `#[allow(dead_code)]`，字段收窄为 `gpu_model` owner 内部字段并通过 `id()` accessor 暴露；`graphics/scene/resources/gpu_model/gpu_model_resource_from_asset.rs` 继续在 GPU model 构造时记录资源身份；`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs::model(...)` 在返回缓存资源前通过 debug assertion 校验 `GpuModelResource::id()` 与 streamer key 一致，说明该字段是模型缓存身份契约的 live 输入，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_gpu_model_identity_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-gpu-model-identity-0622` 通过（既有 warnings）。该记录只关闭 GpuModelResource identity 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 post-process LUT texture owner cleanup | runtime_15_post_process_lut_texture_owner_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/post_process_lut_texture/post_process_lut_texture_resource.rs` 删除 `texture` 字段上的 `#[allow(dead_code)]`，新增 `PostProcessLutTextureResource::RETAINED_LUT_TEXTURE_OWNER_COUNT` 与 `retained_lut_texture_owner_count()` 显式读取 texture/view owner；`view()` 通过 debug assertion 消费该契约，`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs::prepared_post_process_lut_3d_view(...)` 通过 accessor 暴露 3D LUT binding view，说明 texture 字段负责保活 LUT view 背后的 WGPU resource，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_post_process_lut_texture_owner_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-post-process-lut-owner-0622` 通过（既有 warnings）。该记录只关闭 PostProcessLutTextureResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 output target texture owner cleanup | runtime_15_output_target_texture_owner_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/output_target_texture/output_target_texture_resource.rs` 删除 descriptor、texture、view、sampler 字段上的 `#[allow(dead_code)]`，新增 `OutputTargetTextureResource::RETAINED_OUTPUT_TARGET_TEXTURE_OWNER_COUNT` 与 `retained_output_target_texture_owner_count()` 显式读取输出目标 descriptor 和 WGPU texture/view/sampler owner；`descriptor()` / `size()` / `texture()` / `view()` / `sampler()` 通过 debug assertion 消费该契约。`graphics/scene/resources/prepared/prepared_output_target_texture.rs` 删除 `resource` 字段 suppression，新增 `PreparedOutputTargetTexture::RETAINED_OUTPUT_TARGET_CACHE_OWNER_COUNT` / `retained_output_target_cache_owner_count()` / `resource()`，`ResourceStreamer` 的 output-target graph import、writeback 与 material sampling path 通过 accessor clone/borrow 该 Arc，说明输出目标资源缓存负责保活写回、graph import 和材质采样背后的 GPU resource，不是未接线僵尸字段。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_output_target_texture_owner_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-output-target-owner-0622` 通过（既有 warnings）。该记录只关闭 OutputTargetTextureResource / PreparedOutputTargetTexture owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 material runtime capture seed cleanup | runtime_15_material_runtime_capture_seed_cleanup_coremin_check_passed | 2026-06-22 | E6/S10/F12：`graphics/scene/resources/runtime/material_runtime.rs` 删除 `MaterialCaptureSeed`、`MaterialRuntime` 和 `capture_seed()` 上的 `#[allow(dead_code)]`；`MaterialRuntime` 继续作为生产材质运行态 DTO 保留，`MaterialCaptureSeed` 与 `MaterialRuntime::capture_seed()` 改为 `#[cfg(test)]`，只服务 render product streamer 测试对材质捕获种子的回归断言。`graphics/scene/resources/{runtime/mod.rs,mod.rs}` 把 `MaterialCaptureSeed` re-export 收进 test cfg；`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs` 将 `material_capture_seed(...)`、`sample_texture_rgba(...)`、`shading_model_id_for_lighting_model(...)`、`sample_texture_asset_rgba(...)` 和 `wrap01(...)` 收进 test cfg，生产路径不再暴露历史 capture helper。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_material_runtime_capture_seed_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-material-capture-0622` 通过（既有 warnings）。该记录只关闭 MaterialRuntime capture seed/test texture sampling 子切片；resource_streamer 其他 diagnostics accessor suppression 与全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 resource streamer diagnostics accessor cleanup | runtime_15_resource_streamer_diagnostics_accessor_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | E6/S10/F12：`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs` 删除剩余 diagnostics accessor 上的 `#[allow(dead_code)]`。资产/材质管理快照、uniform/property/texture-slot/material-management 查询等只由 render product 与 asset flow 测试消费的 helper 统一收进 `#[cfg(test)]`；生产仍消费的 `material_readiness_report(...)` / `material_readiness_summary(...)` 保留在正常构建中，并继续由 `resource_streamer_ensure_scene_resources.rs` 读取材质 readiness summary 统计。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_resource_streamer_diagnostics_accessor_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；静态扫描确认 `resource_streamer_accessors.rs` 无 `#[allow(dead_code)]`；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞（Cargo 需要补齐 `zircon_plugin_sdk` 相关锁文件条目），不计通过。该记录只关闭 ResourceStreamer diagnostics accessor 子切片；全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 ResourceStreamer material capture child owner split | runtime_15_resource_streamer_material_capture_child_owner_static_passed_cargo_deferred_implementation_cadence | 2026-06-24 | E6/S10/F12/R1.4：`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs` 从 891 行收束为 753 行生产 accessor + diagnostics query owner，并通过 `#[cfg(test)] mod material_capture;` 挂载 child owner。`graphics/scene/resources/resource_streamer/resource_streamer_accessors/material_capture.rs` 承接 `material_capture_seed(...)`、`sample_texture_rgba(...)`、`shading_model_id_for_lighting_model(...)`、`sample_texture_asset_rgba(...)` 与 `wrap01(...)`。`structure_convention/graphics_dead_code/resource_streamer_cleanup.rs::runtime_15_resource_streamer_diagnostics_accessor_cleanup` 同步锁定 moved helper 不回流、父/子 800 行预算和状态锚。验证：scoped rustfmt/static/line-count/docs-anchor/whitespace/diff-check 通过；Cargo/WGPU/RenderDoc 按 milestone implementation cadence deferred，不计通过。 |
| M5 | Runtime 15 F12 resource streamer resolve texture id cleanup | runtime_15_resource_streamer_resolve_texture_id_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | E6/S10/F12：`graphics/scene/resources/resource_streamer/resource_streamer_resolve_texture_id.rs` 删除未使用的 `ResourceStreamer::resolve_texture_id(...)` helper 和对应 `#[allow(dead_code)]`。全仓库调用面只保留生产 live 的 `resolve_texture_reference(...)` / `resolve_texture_reference_with_support(...)` 以及 `ResolvedTextureReference::id()`，材质贴图解析继续报告 unresolved/not-upload-ready validation 与 slot fallback。新增 `structure_convention/graphics_dead_code/mod.rs::runtime_15_resource_streamer_resolve_texture_id_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；静态扫描确认该文件无 `#[allow(dead_code)]` 且 `resolve_texture_id(` 只剩状态守卫字符串；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞（Cargo 需要补齐 `zircon_plugin_sdk` 相关锁文件条目），不计通过。该记录只关闭 ResourceStreamer resolve_texture_id 僵尸 helper 子切片；全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 particle GPU readback output accessor cleanup | runtime_15_particle_gpu_readback_output_accessor_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | E6/S10/F12：`graphics/scene/scene_renderer/core/scene_renderer_runtime_outputs/take_last_particle_gpu_readback_outputs.rs` 删除 stale `#[allow(dead_code)]`。`SceneRenderer::take_last_particle_gpu_readback_outputs(...)` 已由 `graphics/runtime/render_framework/submit_frame_extract/submit/collect_runtime_feedback.rs::collect_particle_feedback(...)` 生产路径消费，和 sideband particle readback 合并后投递到 `ParticleGpuFeedback::new(...)`，说明该 accessor 是 renderer output drain 的 live feedback bridge，不是未接线僵尸 helper。新增 `structure_convention/graphics_dead_code/renderer_output_accessors.rs::runtime_15_particle_gpu_readback_output_accessor_cleanup`，同步状态输出期望行和 `docs/zircon_runtime/graphics/render-product-submit.md`。验证：scoped rustfmt --check 通过；静态扫描确认该文件无 `#[allow(dead_code)]` 且 runtime feedback 路径消费 `renderer.take_last_particle_gpu_readback_outputs()`；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 particle GPU readback accessor 子切片；全量 F12 sweep 仍 pending。 |
| M5 | Runtime 15 F12 advanced plugin output test accessor cleanup | runtime_15_advanced_plugin_output_test_accessor_cleanup_static_passed_cargo_lock_blocked | 2026-06-23 | E6/S10/F12：`graphics/scene/scene_renderer/core/scene_renderer/advanced_plugin_outputs/output_access.rs` 删除三个 stale `#[allow(dead_code)]`。`has_virtual_geometry_gpu_readback(...)`、`plugin_renderer_outputs(...)` 与 `has_particle_gpu_readback(...)` 只服务 inline mailbox/readback tests，现在均为 `#[cfg(test)]`；生产路径仍保留 `take_hybrid_gi_readback_outputs(...)`、`take_particle_gpu_readback_outputs(...)` 与 `take_virtual_geometry_readback_outputs(...)` 作为 renderer output drain。新增 `structure_convention/graphics_dead_code/renderer_output_accessors.rs::runtime_15_advanced_plugin_output_test_accessor_cleanup`，同步状态输出期望行、`docs/zircon_runtime/graphics/render-product-submit.md` 与 `docs/zircon_plugins/particles/runtime.md`。验证：scoped rustfmt --check 通过；静态扫描确认 `output_access.rs` 无 `#[allow(dead_code)]`、三个 observation helper 均收进 test cfg，且生产 take/drain 方法仍存在；带锁 standalone structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 advanced plugin output test accessor 子切片；全量 F12 sweep 仍 pending。 |
| M3 | Runtime 15 M3 graphics dead-code guard module split | runtime_15_graphics_dead_code_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：旧 graphics dead-code 单文件 guard 已硬切为 folder-backed `structure_convention/graphics_dead_code/mod.rs`；layout 守卫迁入 `structure_convention/graphics_dead_code/module_layout.rs`，renderer output accessor 守卫迁入 `structure_convention/graphics_dead_code/renderer_output_accessors.rs`，父模块保留共享 `read_repo` / `read_runtime_src` helper 和其他 F12 graphics dead-code 守卫。新增 `runtime_15_graphics_dead_code_guard_is_folder_backed`，验证旧单文件路径不存在、新 parent/child 模块存在、父模块挂载 `mod module_layout;` 与 `mod renderer_output_accessors;`、子模块包含 `runtime_15_particle_gpu_readback_output_accessor_cleanup` 与 `runtime_15_advanced_plugin_output_test_accessor_cleanup`、父模块行数低于近大文件阈值，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 graphics dead-code guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 graphics dead-code guard child-owner split | runtime_15_graphics_dead_code_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`structure_convention/graphics_dead_code/mod.rs` 继续从 797 行收缩为 20 行模块挂载与共享读取 helper；既有 F12 graphics dead-code 守卫按责任域迁入 `backend_owners.rs`、`gpu_resource_owners.rs` 与 `resource_streamer_cleanup.rs`，父模块不再直接持有 offscreen/backend/GPU resource/material/runtime-streamer 守卫。`runtime_15_graphics_dead_code_guard_is_folder_backed` 已更新为验证五个 child owner、代表性 moved guard 不回流、所有 owner 低于 800 行预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、父/子行数与测试数量扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 graphics dead-code guard 的二级 child-owner 减压子切片；全量 F12 sweep 仍 pending。 |
| M3 | Runtime 15 M3 provider boilerplate guard module split | runtime_15_provider_boilerplate_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：provider registration/update/feedback 三个结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/provider_boilerplate.rs`，与 `runtime_15_provider_prepare_input_uses_shared_extract_generation_owner` 和 `runtime_15_no_duplicated_provider_boilerplate` 同 owner 管理。新增 `runtime_15_provider_boilerplate_guard_is_folder_backed`，验证顶层聚合文件只挂载 provider 子模块、不再直接持有三段 provider guard，`structure_convention.rs` 保持 553 行，`provider_boilerplate.rs` 保持约 618 行，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 provider boilerplate guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M5 | Runtime 15 F14 diagnostics normalization | runtime_15_diagnostics_frame_trait_wrapper_removed_coremin_check_passed | 2026-06-22 | E5/S11/F14：新增 `core/runtime/diagnostics/frame_diagnostics.rs`，由 `FrameDiagnostics` / `FrameDiagnosticsStatus` 统一 render、physics、animation 与 ECS frame diagnostics 的 domain/available/error 状态；`RuntimeDiagnosticsSnapshot::frame_diagnostics_statuses()` 组合三个 runtime 子域；`EcsFramePerformanceDiagnostics` 实现同一 trait 并使用 `scene.ecs` domain。`World` 现在直接保存 `EcsFramePerformanceDiagnostics`，`scene/world/performance_diagnostics.rs` 删除 `WorldEcsFramePerformanceDiagnostics` 纯包装和 `.0` 转发。新增 `runtime_snapshot_frame_diagnostics_statuses_preserve_subdomains`、`ecs_frame_performance_diagnostics_uses_scene_ecs_frame_domain` 与 `runtime_15_diagnostics_use_frame_trait_without_world_wrapper`，状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output 2/2 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-f14-cargo-0622` 通过（既有 warnings）。focused behavior Cargo test `runtime_snapshot_frame_diagnostics_statuses_preserve_subdomains` 1200s 超时无测试结果且残留已清理，不计通过。 |
| M5 | Runtime 15 F5 scene property access typed errors | runtime_15_scene_property_access_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`scene/world/property_access/{read,write,value_conversion}.rs` 与 `write/physics.rs` 的 `World::property`、`World::set_property`、固定组件 writer 和 property value conversion helper 已从裸 `Result<_, String>` 收敛到 `SceneResult`；`SceneError` 新增 `PropertyUnavailable`、`PropertySegmentCount`、`UnknownProperty`、`MissingPropertyComponent`、`PropertyTypeMismatch`、`UnknownPropertyAxis`、`ZeroLengthQuaternion`、`NonFinitePropertyValue`、`InvalidPropertyResourceId`、`UnsupportedPropertyValue`、`ReadOnlyProperty` 与 `InvalidPropertyIndex`。新增 `review_f5_scene_property_access_uses_scene_error` 锁定 read/write/conversion/physics writer 不回退到 `Err(format!)`、`Result<_, String>` 或 lossy `error.to_string()`，`property_paths/read_paths.rs` 源码守卫同步改查 `SceneError::PropertyUnavailable`。验证：scoped rustfmt --check 通过；property-access 旧 String 错误静态扫描通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 scene world property access 公共入口 typed-error 子切片；完整 `module_convention_gate` 与 broader scene/ECS Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 animation manager typed errors | runtime_15_animation_manager_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`core/framework/animation/error.rs` 新增 `AnimationError` / `AnimationResult` typed-error owner，`core/framework/animation/{mod,manager}.rs`、`animation/manager/{mod,pose,sampling}.rs` 与 `animation/sequence/{apply,conversion}.rs` 的 clip pose 采样、skeleton bind 验证、channel sample 转换和 sequence apply 公开入口已从裸 `Result<_, String>` 收敛到 `AnimationResult`。错误分支覆盖 `NonFiniteSkeletonBind`、`ZeroLengthSkeletonBindRotation`、`SampleTypeMismatch`、`NonFiniteSample`、`ZeroLengthQuaternionSample`、`NonFiniteChannelSample` 与 `ZeroLengthQuaternionChannelSample`；新增 `review_f5_animation_manager_uses_animation_error` 锁定 manager/apply 签名和采样/转换实现不回退到 `Err(format!)` 或 String result。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 animation manager/sequence 公共入口 typed-error 子切片；完整 `module_convention_gate` 与 broader animation Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 animation asset binary typed errors | runtime_15_animation_asset_binary_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`asset/assets/animation/error.rs` 新增 `AnimationAssetError` / `AnimationAssetResult` typed-error owner，`asset/assets/animation/{binary,channel,clip,graph,reference,sequence,skeleton,state_machine}.rs` 的 `from_bytes(...)` / `to_bytes(...)`、binary document/stream/v1 payload fallback、reference parsing、channel value tag 与 graph node tag failures 已从裸 `Result<_, String>` / `Err(format!(...))` / `error.to_string()` 收敛到 typed variants。`AssetImportError::AnimationAsset` 保留动画资产解码 source，`import_animation_asset.rs` 不再把动画二进制失败直接降级到 `AssetImportError::Parse(String)`；新增 `review_f5_animation_asset_binary_uses_typed_errors` 锁定 animation asset owners、import boundary、typed source 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 animation asset binary helper 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset/animation Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 profile export typed errors | runtime_15_profile_export_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`core/runtime/diagnostics/profiling/export.rs` 与 `profiling/mod.rs` 的 export path 已从 `Result<ProfileExportReport, String>`、`Result<(), String>`、`.map_err(|error| error.to_string())` 收敛到 `ProfileExportError` / `ProfileExportResult`。`FeatureDisabled`、`CreateExportDirectory`、`JsonSerialize` 与 `WriteFile` 分别保留 feature gate、目录创建、JSON 序列化和文件写入 source；`ProfileControlResponse::error(error.to_string())` 只保留在 external string DTO 边界。新增 `export_snapshot_reports_typed_directory_error_source` 与 `review_f5_profile_export_uses_typed_error` 锁定 typed owner、diagnostics façade re-export 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 profiling export 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader diagnostics Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 gameplay host typed errors | runtime_15_gameplay_host_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`script/vm/gameplay_host/error.rs` 新增 `GameplayHostError` / `GameplayHostResult` typed-error owner，`combat.rs`、`lifecycle.rs`、`navigation.rs` 与 `transform.rs` 的 world mutation、navigation tick、JSON serialization 和 missing-entity branches 已从内部 `Result<_, String>`、`Err(format!(...))` 与 `.map_err(|error| error.to_string())` 收敛到 `SceneError`、`NavigationError`、`serde_json::Error` 或 `GameplayHostError::MissingEntity`。`ScriptHostError` 字符串诊断只保留在 VM host-call 边界。新增 `review_f5_gameplay_host_uses_typed_errors_before_script_host_boundary` 锁定 error owner、domain owners、status/docs anchors 和无 String-error 回流。验证：scoped rustfmt/static scans 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 gameplay host 内部 mutation/nav 错误的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader script VM Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 script scene hook typed errors | runtime_15_script_scene_hook_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`script/vm/scene_hook/error.rs` 新增 `ScriptSceneHookError` / `ScriptSceneHookResult` typed-error owner，`scene_hook.rs` 的 `script.bindings` JSON parse、`VmPluginManager::call_package_export(...)` failure 与 manager resolve source 已从内部 `Result<_, String>`、`Err(format!(...))` 与 `.map_err(|error| error.to_string())` 收敛到 typed result。`CoreError::Initialization` 字符串诊断只保留在 `SceneRuntimeHook::run(...)` 的 CoreError 边界，不改变 hook id、phase filtering、`onStart`/`onFixedUpdate`/`onUpdate` 调用顺序或 `script.bindings` schema。新增 `review_f5_script_scene_hook_uses_typed_errors_before_core_boundary` 锁定 error owner、scene hook owner、status/docs anchors 和无 String-error 回流。验证：scoped rustfmt/static scans 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 script scene hook lifecycle binding 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader script VM Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 export CLI typed errors | runtime_15_export_cli_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`bin/zircon_export_pack/error.rs` 新增 `ExportPackError` / `ExportPackResult`，`bin/zircon_export_validate/error.rs` 新增 `ExportValidateError` / `ExportValidateResult`，并把两个导出命令内部的 argument parsing、JSON encode/decode、pack/delta verification 与 report/pack file IO failure 从 `Result<_, String>`、`Err(format!(...))` 与 `.map_err(|error| format!(...))` 收敛到 typed result。`main.rs` 保持最终 CLI display boundary，只在退出前 `eprintln!("{error}")`；Pack report diagnostics 和 Validate fatal report diagnostics 保持 JSON 人类可读字符串，不改变 stage report schema、exit code `2` 语义、delta apply self-check 或 determinism check。新增 `review_f5_export_cli_uses_typed_errors_before_cli_boundary` 锁定 pack/validate error owner、run/args/manifest 无 String-error 回流、status/docs anchors 和 30 个 code-review guards。验证：scoped rustfmt/static scans 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 export pack/validate CLI 内部 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader export CLI Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 host reflection docs CLI typed errors | runtime_15_host_reflection_docs_cli_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 + root-entry follow-up：`bin/zircon_host_reflection_docs.rs` 已收缩为只挂载 `args`、`error`、`run` 并调用 `run::run(...)` 的 thin entry shell；新增 `bin/zircon_host_reflection_docs/{args,error,run}.rs`，其中 `HostReflectionDocsError` / `HostReflectionDocsResult` 保留 usage、`VmError` built-in host module collection source 和 host interface Markdown write `std::io::Error` source。旧 root 内部 `Result<(), String>`、`Err(format!(...))` 与 `.map_err(|error| format!(...))` 已清除，`main.rs` 仍是最终 CLI display boundary。新增 `review_f5_host_reflection_docs_cli_uses_typed_errors_before_cli_boundary` 锁定 folder-backed bin owner、typed-error owner、status/docs anchors 和无 String-error 回流；code-review findings 计数更新到 31 guards。验证：scoped rustfmt/static scans 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 host reflection docs writer CLI 的 String-error/root-entry 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader script VM Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 dynamic API session typed errors | runtime_15_dynamic_api_session_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`dynamic_api/session/error.rs` 新增 `RuntimeDynamicSessionError` / `RuntimeDynamicSessionResult` 与 `RuntimeProjectError` / `RuntimeProjectResult`，并把 dynamic session 构造、project root parsing、project asset/default scene/navmesh/script loading、scene hook install、frame tick、render bridge submit/present/bind 和 accessibility encode failure 从内部 `Result<_, String>` / `Err(format!(...))` / `.map_err(|error| error.to_string())` 收敛到 typed result。`dynamic_api/session/status.rs::error_status(...)` 保持唯一 C ABI `ZrStatus` 字符串出口，`ZrByteSlice` 生命周期策略不变。新增 `review_f5_dynamic_api_session_uses_typed_errors_before_abi_status_boundary` 锁定 error owner、project owner、ABI status boundary、status/docs anchors 和无 production String-error 回流；code-review findings 计数更新到 32 guards。验证：scoped rustfmt/static scans 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 dynamic API session/project 内部 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader dynamic API Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 VM plugin management policy typed errors | runtime_15_vm_plugin_management_policy_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`script/vm/plugin/management_policy/error.rs` 新增 `VmPluginManagementPolicyError` / `VmPluginManagementPolicyResult`，并把 `garbage_collection.rs`、`memory.rs` 与 `policy.rs` 的 validation path 从内部 `Result<(), String>`、`Err(format!(...))` 与 `.to_string()` 收敛到 typed variants。错误 variants 覆盖 disabled GC interval、zero GC interval、zero memory soft/hard limits 和 soft limit exceeding hard limit；`script/vm/mod.rs` 与 `script/mod.rs` 同步导出 typed validation surface。management TOML schema、hot reload policy、GC policy vocabulary、memory policy fields、slot lifecycle 状态投影和 display 文案不变。新增 `review_f5_vm_plugin_management_policy_uses_typed_validation_errors` 锁定 error owner、validate signatures、facade exports、status/docs anchors 和无 production String-error 回流；code-review findings 计数更新到 34 guards。验证：scoped rustfmt/static scans、docs/status/session anchor scan、conflict marker scan 与 scoped git diff --check 通过，diff-check 仅报告 LF/CRLF normalization warnings；Cargo 因 active external cargo/rustc lane（cargo PIDs 63444, 65076；rustc PID 61568）deferred，不计 Cargo 通过。该记录关闭 VM plugin management policy validation 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader script VM Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 UI surface input effect typed errors | runtime_15_ui_surface_input_effect_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`ui/surface/input/error.rs` 新增 `UiSurfaceInputEffectError` / `UiSurfaceInputEffectResult`，并把 `ui/surface/input/effect.rs`、`effect/{component_event,drag_drop,focus_pointer,navigation,node,popup_tooltip,redraw,text_services}.rs`、`validation.rs` 与 `state/drag_drop.rs` 从内部 `Result<_, String>`、`Result<(), String>`、`Err(format!(...))` / `Err("...".to_string())` 收敛到 typed variants。错误分支覆盖 input owner、missing node/dirty target、focus/pointer capture/lock/high-precision、drag/drop session、navigation route/target/focus、input method surrounding text 与 clipboard payload contract；`UiDispatchRejectedEffect.reason` 是唯一 `error.to_string()` 字符串出口，dispatch rejected reason schema 和既有文案不变。新增 `review_f5_ui_surface_input_effects_use_typed_errors_before_rejected_reason_boundary` 并同步 status-output expectations。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active deferred，不计通过。该记录关闭 UI surface input effect dispatcher 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader UI input Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 UI input surrounding-text error source | runtime_15_ui_input_surrounding_text_error_source_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`zircon_runtime_interface/src/ui/dispatch/input/effect.rs` 的 `UiInputMethodSurroundingTextError` 现在实现 `std::error::Error`，让 `ui/surface/input/error.rs::UiSurfaceInputEffectError::InvalidInputMethodSurroundingText` 能把 validation payload 标成 `#[source]` 并满足 `thiserror` source 组合约束；`zircon_runtime_interface/src/tests/ui_dispatch_error_contracts.rs::ui_input_method_surrounding_text_error_is_std_error` 用编译期 trait bound 锁定接口错误可作为 `StdError + Send + Sync + 'static` 使用，`review_f5_ui_input_surrounding_text_error_implements_std_error` 同步锁定接口 source、runtime payload、docs/status anchors 和 code-review guard count 38。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因外部 cargo/rustc 编译车道 active deferred，不计通过。该记录关闭 UI input surrounding-text typed validation error 的 source-contract 编译阻塞；完整 typed-error sweep、`module_convention_gate` 与 broader UI input Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 UI template resource resolver typed errors | runtime_15_ui_template_resource_resolver_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`ui/template/asset/resource_ref/resolver.rs` 的 UI scheme 到 runtime `ResourceLocator` lookup helper 已从 `Result<RuntimeResourceLookup, String>`、`Result<(String, Option<String>), String>` 与 `.map_err(|error| error.to_string())` 收敛到 `UiResourceLookupError` / `UiResourceLookupResult`。`ResourceLocatorError` source 现在通过 transparent typed error 保留，`ResourceLocatorError::EmptyLabel` 覆盖 mapped `asset://...#` / `project://...#` 空 label，外层 `UiResourceResolveDiagnostic` 仍是唯一面向 editor/report 的字符串诊断边界，因此 `InvalidUri` 文案、fallback policy、cache invalidation 与 registry lookup 语义不变。新增 `ui_resource_resolver_reports_invalid_mapped_ui_scheme_empty_label` 与 `review_f5_ui_template_resource_resolver_uses_typed_lookup_errors_before_diagnostics_boundary`，code-review findings 当前守卫数为 36，并同步 status-output expectations。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因外部 cargo/rustc 编译车道 active deferred，不计通过。该记录关闭 UI template resource resolver lookup 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader UI asset/resource Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 UI asset document typed errors | runtime_15_ui_asset_document_typed_errors_static_passed_cargo_deferred | 2026-06-27 | E1/E2/F5 follow-up：`asset/assets/ui.rs` 的 v1/v2 UI document wrappers 已从 `Parse(String)` 与 `.map_err(|error| error.to_string())` 收敛到 `UiAssetDocumentError::Parse(#[from] UiAssetError)`、`UiV2AssetDocumentError::Parse(#[from] UiV2AssetError)`，theme/icon TOML parse 保留 `toml::de::Error` source；`UiIconAssetDocumentError` 用 `InvalidDefaultSize`、`EmptySemanticId`、`EmptyInlineSvgSource`、`EmptyExternalSourceUri` 与 `InvalidSourceUri { source: ResourceLocatorError }` 表达 validation failures。`asset/importer/error.rs` 新增 `AssetImportError::UiThemeDocument` / `AssetImportError::UiIconDocument`，`import_ui_asset.rs`、`import_ui_v2_asset.rs`、`import_ui_zui_asset.rs`、`import_ui_theme_asset.rs` 与 `import_ui_icon_asset.rs` 不再把 UI document failures 降级成 `Parse(String)`；`.v2.ui.toml` component 仍通过 `UiV2AssetDocumentError::ComponentRequiresZui` 拒绝。新增 `ui_asset_wrappers_preserve_typed_parse_sources`、`ui_icon_asset_reports_typed_validation_errors`、`importer_preserves_typed_ui_asset_document_parse_sources`、`importer_preserves_typed_theme_and_icon_document_sources` 与 `review_f5_ui_asset_documents_use_typed_errors_before_import_boundary`，code-review findings 当前守卫数为 37，并同步 status-output expectations。验证：scoped rustfmt/static scans、docs/status/session anchor scan 通过；Cargo 因外部 cargo/rustc 编译车道 active deferred，不计通过。该记录关闭 UI asset document wrapper/importer 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader UI asset Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 typed API residual typed errors | runtime_15_typed_api_residual_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`scene/world/typed_api.rs` 的 `insert_dynamic_component_presence(...)` / `remove_dynamic_component_presence(...)` 和 `scene/world/identity.rs::register_stable_entity(...)` 已从内部 `Result<_, String>` / `error.to_string()` 残留收敛到 `SceneResult`。`SceneError` 现在通过 `EntityRegistry(#[from] EntityRegistryError)` 保留 stable entity registry source，dynamic component presence helper 通过 `Storage(#[from] StorageError)` 保留 component storage source；`review_f5_world_spawn_bundle_surface_uses_scene_error` 同步锁定 typed API、identity owner、status/docs anchors 不回退到 String 错误。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 F5 原证据文件 typed API 内部残留 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader scene/ECS Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 fixed world mutation typed errors | runtime_15_fixed_world_mutation_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`scene/world/{component_access,hierarchy,query,records}.rs` 的固定 World mutation helper 已从内部 `Err(format!(...)).into()` / `to_string().into()` 残留收敛到显式 `SceneError` 变体。缺失实体继续使用 `SceneError::MissingEntity`，固定组件缺失使用 `SceneError::MissingRequiredComponent`，joint 自连接、层级自父级/环、静态 mobility/transform/reparent 约束、重复实体与空名称分别由 typed variants 表达；`review_f5_fixed_world_mutation_uses_scene_error_variants` 锁定这些 owner 不回退到 String 错误。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭固定 World mutation façade 的 `SceneError::Message(String)` 回流子切片；完整 typed-error sweep、`module_convention_gate` 与 broader scene/ECS Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 asset authoring typed errors | runtime_15_asset_authoring_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`asset/assets/authoring.rs` 的 Terrain/TileMap/MaterialGraph authoring 校验已从 `Result<(), String>` / `Err(format!(...))` 收敛到 `AssetAuthoringError` / `AssetAuthoringResult`。`asset/importer/ingest/import_authoring_asset.rs` 仅在 `AssetImportError::Parse` 边界 stringify，`asset/tests/assets/authoring.rs::authoring_asset_validation_reports_typed_errors` 匹配三个 typed variants；`review_f5_asset_authoring_uses_typed_error` 锁定 authoring owner、facade exports、import boundary 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 authoring asset validation 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 navigation asset typed errors | runtime_15_navigation_asset_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`asset/assets/navigation.rs` 的 `NavMeshAsset::to_bytes(...)` / `from_bytes(...)` 已从 `Result<_, String>` 与 `error.to_string()` 收敛到 `NavigationAssetError` / `NavigationAssetResult`，并用 `Serialize` / `Deserialize` variants 保留 bincode source。`asset/assets/mod.rs` 与 `asset/mod.rs` 公开导出新 typed-error surface，`asset/tests/assets/navigation.rs::navmesh_binary_roundtrip_reports_typed_errors` 覆盖 roundtrip 与无效字节 typed failure；`review_f5_navigation_asset_uses_typed_error` 锁定 navigation owner、facade exports、test mount、module doc 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 navmesh binary helpers 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 font asset typed errors | runtime_15_font_asset_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`asset/assets/font.rs` 的 `FontAsset::from_toml_str(...)` 已从 `FontAssetError::Parse(String)` / `error.to_string()` 收敛到 `FontAssetError::Parse(#[source] toml::de::Error)` / `FontAssetResult`，保留 TOML parser source。`asset/assets/mod.rs` 与 `asset/mod.rs` 公开导出 typed surface，`asset/importer/ingest/import_font_asset.rs` 只在 `AssetImportError::Parse` 边界格式化错误，`asset/tests/assets/font.rs::font_asset_parse_reports_typed_toml_error_source` 覆盖 invalid TOML typed failure；`review_f5_font_asset_uses_typed_error_source` 锁定 font owner、facade exports、import boundary、module doc 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 font TOML parsing 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 sound asset typed errors | runtime_15_sound_asset_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`asset/assets/sound.rs` 的 `SoundAsset::from_wav_bytes(...)` 与 WAV parser helpers 已从 `Result<_, String>` / `Err(format!(...))` / `.to_string()` 收敛到 `SoundAssetError` / `SoundAssetResult`。错误 variants 覆盖 RIFF/WAVE container、fmt/data chunk、WAVE_FORMAT_EXTENSIBLE subformat/valid-bits/channel-mask、unsupported bits/format、block-align 与 sample/frame alignment failures；`asset/assets/mod.rs` 与 `asset/mod.rs` 公开导出 typed surface，`asset/importer/ingest/import_sound.rs` 只在 `AssetImportError::Parse` 边界格式化错误，`asset/tests/assets/sound.rs::{sound_asset_wav_parse_reports_typed_error_variants,sound_asset_rejects_wav_extensible_unsupported_speaker_mask_bits}` 直接匹配 typed variants；`review_f5_sound_asset_uses_typed_error` 锁定 sound owner、facade exports、import boundary、module doc 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 WAV asset parsing 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset/audio Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 zshader definition typed errors | runtime_15_zshader_definition_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`asset/assets/shader/zshader.rs` 的 `.zshader` `shader_def_values` 转换已从 `Result<_, String>` / `format!` 字符串错误收敛到 `ZShaderDefinitionError` / `ZShaderDefinitionResult`。`BoolValue`、`IntValue`、`UintValue` 和 `UnsupportedKind { name, kind }` 分别表达 bool/int/uint value 类型不匹配和 unsupported kind；`asset/assets/shader/mod.rs` 与 `asset/mod.rs` 公开导出 typed surface，`asset/importer/ingest/import_shader_package.rs` 只在 `AssetImportError::Parse` 边界格式化错误，`asset/tests/assets/shader_readiness.rs::zshader_definition_values_report_typed_authoring_errors` 直接匹配 typed variants；`review_f5_zshader_definition_values_use_typed_error` 锁定 owner、facade exports、import boundary、shader/material doc 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 `.zshader` definition value conversion 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F5 asset meta typed errors | runtime_15_asset_meta_typed_errors_static_passed_cargo_deferred | 2026-06-25 | E1/E2/F5 follow-up：`.zmeta` `asset/project/meta.rs::migrate_to_current(...)` 已从 `Result<(), String>` / `Err(format!(...))` 收敛到 `AssetMetaError` / `AssetMetaResult`；`AssetMetaError::UnsupportedFormatVersion { found, supported }` 保留未来格式版本与当前支持版本，`AssetMetaDocument::load(...)` 只在 `std::io::ErrorKind::InvalidData` 边界经 `invalid_data(...)` stringify。新增 inline `asset_meta_migration_reports_typed_future_version_error`，`review_f5_asset_meta_uses_typed_error` 锁定 meta owner、facade exports、importer doc 和 status/docs anchors。验证：scoped rustfmt/static scans 与 docs/status/session anchor scan 通过；Cargo 因并行 cargo/rustc 编译车道 active 暂缓，不计 Cargo 通过。该记录关闭 `.zmeta` migration future-version 的 String-error 子切片；完整 typed-error sweep、`module_convention_gate` 与 broader asset Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F13 provider registration shared owner | runtime_15_provider_registration_shared_owner_coremin_check_passed | 2026-06-22 | E5/S11/F13：新增 `graphics/runtime_provider/{mod,registration}.rs`，`RuntimeProviderRegistration<P: ?Sized>` 统一 provider ID、priority、provider trait object 与 debug name 存储，`define_runtime_provider_registration!` 生成 HGI、Virtual Geometry、Solari 三套 public registration wrapper 的 `new` / `provider_id` / `priority` / `with_priority` / `provider` / `Debug`。`HybridGiRuntimeProviderRegistration`、`VirtualGeometryRuntimeProviderRegistration` 与 `SolariRuntimeProviderRegistration` 的 public API 名称不变，但 provider-specific owner 不再各自复制字段、builder 和 debug 实现；新增 `docs/zircon_runtime/graphics/runtime_provider/registration.md`。守卫：`runtime_15_provider_registration_uses_shared_owner`，状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：standalone guard red->green；scoped rustfmt、structure guard、status-output 与 core-min `cargo check` 通过（既有 warnings）。本记录只关闭 registration 样板，update/feedback 子切片已由后续 shared-owner 记录收束，provider prepare-input duplication 仍 pending。 |
| M5 | Runtime 15 F13 provider update shared stats owner | runtime_15_provider_update_shared_stats_owner_coremin_check_passed | 2026-06-22 | E5/S11/F13：新增 `graphics/runtime_provider/update.rs`，`RuntimeProviderUpdate<S>` 统一 provider update stats payload，`define_runtime_provider_update!` 生成 `HybridGiRuntimeUpdate` 与 `VirtualGeometryRuntimeUpdate`。HGI 继续以 `stats() -> HybridGiRuntimeStats` 返回 copy，Virtual Geometry 继续以 `stats() -> &VirtualGeometryRuntimeStats` 返回引用，现有 record-submission 和测试 fixture API 不变；两套 provider-specific update 文件不再各自复制 stats 字段、constructor 和 getter 样板。新增 `docs/zircon_runtime/graphics/runtime_provider/update.md`。守卫：`runtime_15_provider_update_uses_shared_stats_owner`，状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：缺失 `graphics/runtime_provider/update.rs` 的红态静态证据已记录；scoped rustfmt 与 core-min `cargo check` 通过（既有 warnings）；standalone structure guard/status-output test binary 启动被 Windows `ResourceUnavailable` / 用户取消阻断，focused Cargo test 904s 超时无结果，不计为通过。本记录只关闭 update stats 样板，feedback 子切片已由后续 shared-owner 记录收束，provider prepare-input duplication 仍 pending。 |
| M5 | Runtime 15 F13 provider feedback shared payload owner | runtime_15_provider_feedback_shared_payload_owner_coremin_check_passed | 2026-06-22 | E5/S11/F13：新增 `graphics/runtime_provider/feedback.rs`，`RuntimeProviderFeedback<G, V>` 统一 provider feedback 的 `gpu_completion` 与 `visibility_feedback` 共同 payload，HGI 与 Virtual Geometry public feedback wrapper 的类型名、constructor、getter surface 不变。HGI 的 `evictable_probe_ids` 与 Virtual Geometry 的 `node_and_cluster_cull_page_requests`、`evictable_page_ids`、`generation` 继续留在各自 provider-specific owner，避免把不同生命周期字段伪装成共同模型。新增 `docs/zircon_runtime/graphics/runtime_provider/feedback.md`。守卫：`runtime_15_provider_feedback_uses_shared_payload_owner`，状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：缺失 `graphics/runtime_provider/feedback.rs` 的红态静态证据已记录；scoped rustfmt、standalone structure guard 1/1、standalone status-output 2/2 与 core-min `cargo check` 通过（既有 warnings）。本记录只关闭 feedback 共同 payload 样板，provider prepare-input duplication 仍 pending。 |
| M5 | Runtime 15 F13 provider prepare input shared frame owner | runtime_15_provider_prepare_input_shared_frame_owner_coremin_check_passed | 2026-06-22 | E5/S11/F13：新增 `graphics/runtime_provider/prepare_input.rs`，`RuntimeProviderPrepareInput<'a, E>` 统一 provider prepare 阶段共同的 optional extract 与 frame generation。`HybridGiRuntimePrepareInput` 与 `VirtualGeometryRuntimePrepareInput` 的 public 类型名、constructor 参数和 getter surface 不变；HGI 的 mesh/light/update-plan 输入与 Virtual Geometry 的 page-upload/cluster/draw-segment 输入继续留在各自 provider-specific owner。新增 `docs/zircon_runtime/graphics/runtime_provider/prepare_input.md`。守卫：`runtime_15_provider_prepare_input_uses_shared_extract_generation_owner` 放在 `structure_convention/provider_boilerplate.rs`，与后续 registration/update/feedback/provider 总审计守卫同 owner；状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：scoped rustfmt、standalone structure guard 1/1、standalone status-output all-subplans guard 1/1 与 core-min `cargo check` 通过（既有 warnings）；broader `status_output` 批次仍有非本切片 Runtime 06 F8 row-drift 失败。full `runtime_15_no_duplicated_provider_boilerplate` / `module_convention_gate` 仍 pending。 |
| M5 | Runtime 15 F13 full provider boilerplate audit | runtime_15_provider_boilerplate_full_audit_coremin_check_passed | 2026-06-22 | E5/S11/F13：`structure_convention/provider_boilerplate.rs` 新增 `runtime_15_no_duplicated_provider_boilerplate` 总守卫，把 registration / update / feedback / prepare-input 四个 shared-owner 子切片收束成一个完整 provider boilerplate 审计。守卫验证 `graphics/runtime_provider/{registration,update,feedback,prepare_input}.rs` 均由共享 owner 挂载；HGI、Virtual Geometry、Solari registration wrapper 只经 `define_runtime_provider_registration!` 生成，不再复制 provider id / priority / trait-object / debug 样板；HGI/VG update wrapper 只经 `define_runtime_provider_update!` 生成；HGI/VG feedback wrapper 委托 `RuntimeProviderFeedback<G, V>`，不再复制共同 GPU completion / visibility feedback 字段；HGI/VG prepare-input wrapper 委托 `RuntimeProviderPrepareInput<'a, E>`，不再复制 optional extract / generation；Particle feedback 因只有 `ParticleGpuFeedback`、无 visibility payload，被明确记录为 feature-specific 例外而非双 payload owner。状态输出期望行同步到 `expected_status_row_data.rs` / `expected_slices/{status,date}.rs`。验证：scoped rustfmt --check 通过；standalone `provider_boilerplate::runtime_15_no_duplicated_provider_boilerplate` 1/1 通过；standalone status-output all-subplans guard 1/1 通过；core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-provider-boilerplate-full-coremin-0622` 通过（既有 141 warnings）。完整 `module_convention_gate`、全量 dead-code sweep 与测试组织拆分仍 pending。 |
| M2 | Runtime 15 F19 construction owner rename | render_scene_renderer_construct_modules_coremin_passed | 2026-06-22 | R2.5/S12：render F19 已将 scene renderer construction owner 从 `_new` 后缀硬切到 `scene_renderer_core_construct` / `scene_renderer_construct`，无旧目录、compat re-export 或 shim。守卫：`review_f19_scene_renderer_construction_modules_use_construct_names`；验证：scoped rustfmt、旧 owner 名称扫描、focused guard 1/1、core-min `cargo check` 通过（既有 warnings）。 |
| M3 | Runtime 15 M3 facade surface guard module split | runtime_15_facade_surface_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：prelude coverage 与 graphics façade visibility 两个结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/facade_surface.rs`，把 crate prelude / subsystem prelude coverage 和 graphics façade 可见性说明放在同一 façade surface owner。新增 `runtime_15_facade_surface_guard_is_folder_backed`，验证顶层聚合文件只挂载 façade 子模块、不再直接持有 `runtime_15_prelude_covers_required_types` 与 `runtime_15_mixed_visibility_has_facade_note`，`structure_convention.rs` 回落到 500 行以下，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 façade/prelude guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 runtime dead-code guard module split | runtime_15_runtime_dead_code_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：runtime UI dead-code support、runtime-owned dead-code cleanup 与 script host value descriptor 三个非 graphics dead-code 结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/runtime_dead_code.rs`，让 F10/F12 runtime dead-code surface 的结构守卫同 owner。新增 `runtime_15_runtime_dead_code_guard_is_folder_backed`，验证顶层聚合文件只挂载 runtime dead-code 子模块、不再直接持有三段 dead-code guard，`structure_convention.rs` 回落到 180 行以下，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 runtime dead-code guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 runtime dead-code guard forbidden attribute literal cleanup | runtime_15_runtime_dead_code_guard_literal_cleanup_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3 + F12：`structure_convention/runtime_dead_code.rs` 不再把禁止项 `#[allow(dead_code)]` 作为测试源字面量散落在断言里，而是集中为 `DEAD_CODE_ALLOW_ATTRIBUTE = concat!("#[allow(", "dead_code", ")]")` 后复用；新增 `runtime_15_runtime_dead_code_guard_forbidden_attribute_literal_is_constant_backed`，验证 runtime dead-code guard 源文件本身不再命中该 forbidden attribute literal，避免简单源扫描把守卫测试误报为残留 suppression，同时继续检查 UI root、asset worker/core module-entry、script host descriptor 与 reflection fixture 的生产/测试 owner 不使用 dead-code 抑制。同步 Runtime 15/status/docs 锚点（含 `structure_convention/runtime_dead_code.rs`、`DEAD_CODE_ALLOW_ATTRIBUTE` 与新守卫名）。验证：scoped rustfmt/static scans、literal scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 runtime dead-code guard forbidden-literal 噪声子切片；完整 `runtime_15_no_dead_code_suppression_in_production`、`module_convention_gate` 与 full Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 lock poison policy guard folder split | runtime_15_lock_poison_policy_guard_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`structure_convention/lock_poison_policy.rs` 不再承载 21 个 E9/F2 lock-poison 回归守卫的全部断言，改为 folder-backed 父模块加 `core_runtime.rs`、`runtime_services.rs`、`asset_render_input.rs` 三个 child owner。父文件从 1583 行减压为 202 行，只保留子模块挂载、共享 helper 和新布局守卫；`core_runtime.rs` 为 649 行，承接 scene/EventBus、config、devtools、CoreHandle diagnostics/time/states/task/profiling/registry 守卫；`runtime_services.rs` 为 452 行，承接 plugin bridge、navigation、dynamic API/session/spawn、ECS parallel executor、resource manager 守卫；`asset_render_input.rs` 为 460 行，承接 asset project/worker、WGPU framework、animation/input、script VM registry 守卫。新增 `runtime_15_lock_poison_policy_guard_is_folder_backed`，验证父/子模块挂载、代表性 moved guard 不回流、21 个既有守卫加 1 个布局守卫数量保持为 22、所有 owner 低于 800 行预算，并同步状态输出期望行（含 `structure_convention/lock_poison_policy/core_runtime.rs`、`structure_convention/lock_poison_policy/runtime_services.rs`、`structure_convention/lock_poison_policy/asset_render_input.rs`）。验证：scoped rustfmt/static scans、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 19964、34648、59276、70536；`rustc` PIDs 15088、29672）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 lock poison policy guard owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 lock-poison Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core runtime lock poison guard child-owner split | runtime_15_core_runtime_lock_poison_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`structure_convention/lock_poison_policy/core_runtime.rs` 不再承载 10 个 core runtime lock-poison 守卫的全部断言，改为 folder-backed 父模块挂载 `core_runtime/scene_eventbus.rs`、`core_runtime/global_gate.rs`、`core_runtime/config_devtools.rs`、`core_runtime/handle_accessors.rs` 与 `core_runtime/task_profiling.rs`。父文件只保留 child mount 和 `runtime_15_core_runtime_lock_poison_guard_child_owner_split` 布局守卫；既有 scene/EventBus、production direct-lock 全局 gate、config/devtools、CoreHandle diagnostics/time/states/registry、task/profiling 守卫全部保留在子 owner。同步状态输出期望行（含 `structure_convention/lock_poison_policy/core_runtime.rs`、`structure_convention/lock_poison_policy/core_runtime/handle_accessors.rs` 与新守卫名）。验证：scoped rustfmt/static scans、核心子 owner 行数预算扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 core runtime lock-poison guard owner 的 M3 child-owner 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 lock-poison Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 F2 lock poison recovery guard | runtime_15_f2_lock_poison_recovery_guard_core_min_cargo_passed_full_sweep_pending | 2026-06-27 | E9/F2：新增 `structure_convention/lock_poison_policy.rs`，把 2026-06-22 已完成的 scene/EventBus poison-safe lock 修复固化为 Runtime 15 防回退守卫。`runtime_15_f2_lock_poison_recovery_guard_covers_scene_and_eventbus` 确认 `scene/level_system.rs` 保留 `lock_poison_recovered(...)` 并覆盖 world/runtime_state/metadata/lifecycle/subsystems accessors，`scene/module/default_level_manager.rs` 保留 `lock_levels()`，lifecycle owner 通过该 helper 访问 level map；`core/runtime/events.rs` 保留 `lock_subscribers()` / `lock_delivery()`，publish/subscribe/prune 子 owner 只通过 helper 开锁。新增 `code_review_findings/p0_robustness.rs::review_f2_scene_eventbus_locks_recover_after_poison`，把审查主表同步为 `scene/EventBus poison-safe lock recovery complete`，并锁定状态不回退。验证：先以新增 F2 review guard 触发 RED，失败在缺少 `core_min_cargo_passed_full_sweep_pending` 状态锚；随后 `level_system_accessors_recover_poisoned_state_locks`、`review_f2_scene_eventbus_locks_recover_after_poison`、`runtime_15_f2_lock_poison_recovery_guard_covers_scene_and_eventbus`、`runtime_15_code_review_findings_tests_are_folder_backed` 与 `status_output_tables` 均在 core-min 聚焦 Cargo 通过。完整 Runtime 15 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 production direct lock unwrap global gate | runtime_15_production_direct_lock_unwrap_global_gate_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`structure_convention/lock_poison_policy/core_runtime.rs::runtime_15_production_sources_do_not_directly_unwrap_mutex_locks` 新增全局回归闸口，递归扫描 `zircon_runtime/src` 下非 `tests/`、非 `tests.rs`、非 `*_tests.rs` 的生产 Rust 源文件，并只读取 `#[cfg(test)]` 之前的生产段，拒绝生产源码重新出现 `LOCK_UNWRAP_CALL` 所代表的 direct `.lock().unwrap()`。该守卫不改变生产 runtime 行为，只把已完成的 lock poison recovery 清理提升为全 crate 生产段规则；测试段中用于制造 poisoned lock 的 fixture 仍允许存在。同步 Runtime 15/status/docs 锚点（含 `structure_convention/lock_poison_policy/core_runtime.rs`、`LOCK_UNWRAP_CALL` 与 `runtime_15_production_sources_do_not_directly_unwrap_mutex_locks`）。验证：全量生产段 direct lock unwrap scan、scoped rustfmt/static scans、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 config store lock poison recovery | runtime_15_config_store_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/config_store.rs` 的 `values` mutex 从 direct lock unwrap 收敛为私有 `lock_values()` poison recovery helper，`store_value`、`load_value` 与 `snapshot_values` 统一走该 helper；新增 module-local `config_store_accessors_recover_poisoned_values_lock` 覆盖锁中毒后 store/load/snapshot 仍可恢复。`structure_convention/lock_poison_policy.rs::runtime_15_config_store_lock_poison_recovery_guard_covers_runtime_config_store` 锁定生产段不回到 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `core/runtime/config_store.rs`、`docs/zircon_runtime/core/runtime/config_store.md` 与新守卫名）。验证：scoped rustfmt/static scans、生产段 direct lock unwrap scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core runtime devtools lock poison recovery | runtime_15_core_runtime_devtools_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/diagnostics/devtools.rs` 的 module/service/scene hook registry 读取从 direct lock unwrap 收敛为私有泛型 `lock_poison_recovered(...)` helper，`collect_runtime_devtools_snapshot(...)` 的只读 DTO 投影不再因 poisoned runtime registry mutex 崩溃。新增 module-local `devtools_snapshot_recovers_poisoned_runtime_registry_locks` 覆盖 modules、services 与 scene_hooks locks 中毒后仍可收集空 snapshot；`structure_convention/lock_poison_policy.rs::runtime_15_core_runtime_devtools_lock_poison_recovery_guard_covers_devtools_snapshot` 锁定生产路径不回到 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `core/runtime/diagnostics/devtools.rs`、`docs/zircon_runtime/core/diagnostics.md` 与新守卫名）。验证：scoped rustfmt/static scans、devtools direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core diagnostics Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core handle diagnostics lock poison recovery | runtime_15_core_handle_diagnostics_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/handle/diagnostics.rs` 的 `CoreHandle` diagnostics store 访问从 direct lock unwrap 收敛为私有 `lock_diagnostics()` poison recovery helper，`diagnostic_store()`、`diagnostic_store_snapshot()` 与 `record_diagnostic(...)` 不再因 poisoned diagnostics mutex 崩溃。新增 module-local `core_handle_diagnostic_accessors_recover_poisoned_store_lock` 覆盖 diagnostics lock 中毒后仍可 record/snapshot；`structure_convention/lock_poison_policy.rs::runtime_15_core_handle_diagnostics_lock_poison_recovery_guard_covers_diagnostic_store` 锁定生产路径不回到 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `core/runtime/handle/diagnostics.rs`、`docs/zircon_runtime/core/diagnostics.md` 与新守卫名）。验证：scoped rustfmt/static scans、CoreHandle diagnostics direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core diagnostics Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core handle time lock poison recovery | runtime_15_core_handle_time_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/handle/time.rs` 的 runtime clocks、frame clock 与时间诊断写入入口从 direct lock unwrap 收敛为 `lock_time()` / `lock_frame_clock()` helpers，并让 `record_time_diagnostics(...)` 复用 `CoreHandle::record_diagnostic(...)` 的 poison-safe diagnostics store 入口；`time_clocks()`、`advance_time_by(...)`、`tick_time(...)` 与虚拟/固定时钟配置不再因 poisoned mutex 崩溃。新增 module-local `core_handle_time_accessors_recover_poisoned_runtime_clocks` 覆盖 time、frame_clock 与 diagnostics locks 中毒后仍可 pause/unpause、advance/tick 和写入时间诊断；`structure_convention/lock_poison_policy.rs::runtime_15_core_handle_time_lock_poison_recovery_guard_covers_runtime_clocks` 锁定生产路径不回到 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `core/runtime/handle/time.rs`、`docs/zircon_runtime/core/diagnostics.md` 与新守卫名）。验证：scoped rustfmt/static scans、CoreHandle time direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core diagnostics Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core handle states lock poison recovery | runtime_15_core_handle_states_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/handle/states.rs` 的 `CoreHandle` state registry 访问从 direct lock unwrap 收敛为私有 `lock_states()` poison recovery helper，`init_state`、`insert_state`、state/next-state 读取、pending transition 写入、transition apply/event 查询和 hook 注册都统一走该 helper。新增 module-local `core_handle_state_accessors_recover_poisoned_state_registry_lock` 覆盖 states lock 中毒后仍可 init、set/apply transition、读取当前 state 与 event history；`structure_convention/lock_poison_policy.rs::runtime_15_core_handle_states_lock_poison_recovery_guard_covers_state_registry` 锁定生产路径不回到 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `core/runtime/handle/states.rs`、`docs/zircon_runtime/core/state.md` 与新守卫名）。验证：scoped rustfmt/static scans、CoreHandle states direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core state Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core runtime task lock poison recovery | runtime_15_core_runtime_task_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/tasks/job_handle.rs` 与 `core/runtime/tasks/job_scheduler.rs` 的 JobHandle state、condvar wait 和 pending scheduled task mutex 从 direct lock expect 收敛为 `JobState::lock_inner()` / `wait_inner(...)` / `wait_inner_timeout(...)` 与 `PendingScheduledJob::lock_task()` poison recovery helpers。新增 module-local `job_handle_accessors_recover_poisoned_state_lock`、`job_handle_wait_recovers_poisoned_state_lock` 与 `pending_scheduled_job_recovers_poisoned_task_lock`，覆盖 job state 和 pending task lock 中毒后仍可 dependent callback、wait、mark complete 和 launch scheduled task；`structure_convention/lock_poison_policy.rs::runtime_15_core_runtime_task_lock_poison_recovery_guard_covers_job_handles` 锁定生产路径不回到 direct lock expect/unwrap，并同步 Runtime 15/status/docs 锚点（含 `docs/zircon_runtime/core/tasks.md` 与新守卫名）。验证：scoped rustfmt/static scans、core runtime task direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core tasks Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core runtime profiling lock poison recovery | runtime_15_core_runtime_profiling_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/diagnostics/profiling/mod.rs` 的全局 `ProfileRecorder` mutex 从 direct lock unwrap 收敛为私有 `lock_recorder()` poison recovery helper，`start_capture`、`stop_capture`、`reset_capture`、`snapshot` 与 `with_recorder(...)` 不再因 poisoned recorder mutex 崩溃。新增 module-local `profile_recorder_accessors_recover_poisoned_global_lock` 覆盖全局 recorder lock 中毒后仍可 snapshot 与 reset；`structure_convention/lock_poison_policy.rs::runtime_15_core_runtime_profiling_lock_poison_recovery_guard_covers_global_recorder` 锁定生产路径不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `docs/zircon_runtime/core/diagnostics.md` 与新守卫名）。验证：scoped rustfmt/static scans、profiling recorder direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core diagnostics/profiling Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core handle registry lock poison recovery | runtime_15_core_handle_registry_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/runtime/handle/core_handle.rs` 新增 `lock_modules()`、`lock_services()`、`lock_scene_hooks()`、`lock_world_extensions()` 与 `lock_plugin_bridge_lifecycle()` poison recovery helpers，`activation.rs`、`registration/register_module.rs`、`resolution.rs` 与 `runtime_extensions.rs` 的 modules/services/world-extension/scene-hook/plugin-lifecycle 生产入口不再 direct lock unwrap。新增 module-local `core_handle_registry_accessors_recover_poisoned_runtime_locks` 覆盖这些 runtime registry locks 中毒后 helper 仍可恢复；`core/runtime/tests/registration/structure.rs` 的事务锁结构哨兵改为检查 helper commit boundary；`structure_convention/lock_poison_policy.rs::runtime_15_core_handle_registry_lock_poison_recovery_guard_covers_registry_accessors` 锁定生产路径不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `docs/zircon_runtime/core/runtime/lifecycle.md` 与新守卫名）。验证：scoped rustfmt/static scans、CoreHandle registry direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core runtime handle Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core runtime registration structure behavior layout split | runtime_15_core_runtime_registration_structure_behavior_layout_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`core/runtime/tests/registration/structure.rs` 不再承载 registration behavior 子模块布局与热点 registration source 结构的全部断言，behavior folder wiring 断言迁入 `core/runtime/tests/registration/structure/behavior_layout.rs`。父文件从 805 行减压为 763 行，继续拥有 registration hot-path、service-list cache、dependency-name materialization 与 duplicate helper boundary 守卫；`behavior_layout.rs` 为 71 行，验证 `behavior.rs` 只挂载 `validation/cache_lists/commit/canonical_keys` 子 owner，不直接持有 `#[test]`/`use`，并确认关键 behavior tests 仍在子文件。新增 `registration_behavior_tests_stay_folder_backed`，同步状态输出期望行。验证：scoped rustfmt/static scans、父子行数预算扫描、剩余 oversized-test scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 4820、8680、8844、20052、32036、33512、50960、56884；`rustc` PIDs 27412、53344、54144、62068）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 registration structure test owner 的 M3 folder-backed 子切片；完整 `module_convention_gate` 与 full core runtime registration Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 plugin bridge table lock poison recovery | runtime_15_plugin_bridge_table_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`plugin/bridge/table.rs` 的 `BridgeEntry` provider slot mutex 从 direct lock unwrap 收敛为私有 `lock_provider()` poison recovery helper，`provider_installed()`、typed provider resolve、deactivate、replace/reload 与 restore provider 入口都统一走 helper。新增 module-local `bridge_entry_provider_accessors_recover_poisoned_provider_lock` 覆盖 provider slot 被 poison 后仍可 snapshot/status、typed resolve、deactivate、restore 与 replace；`structure_convention/lock_poison_policy.rs::runtime_15_plugin_bridge_table_lock_poison_recovery_guard_covers_provider_slot` 锁定生产段不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `plugin/bridge/table.rs`、`docs/zircon_runtime/plugin/bridge.md` 与新守卫名）。验证：scoped rustfmt/static scans、plugin bridge table direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该切片不改变 `FrozenBridgeTable` / `BridgeEntry` public surface、generation parity、strong/weak bridge resolution、owner lifecycle reports 或 bridge diagnostics matrix；完整 `module_convention_gate` 与 full plugin bridge Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 native live-host bridge methods lock poison recovery | runtime_15_native_live_host_bridge_methods_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`plugin/native_plugin_loader/native_plugin_live_host/bridge_methods.rs` 的 runtime bridge method binding registry 从 `lock poisoned` error path 收敛为私有 `lock_runtime_bridge_method_bindings()` poison recovery helper，`clear_runtime_bridge_method_bindings`、installed binding reads 与 replace/install/discovery teardown 写入路径都统一走该 helper。新增 module-local `native_live_host_bridge_method_bindings_recover_poisoned_lock` 覆盖 binding table 被 poison 后仍可 read、clear、replace；新增独立结构守卫 `structure_convention/native_live_host_lock_poison.rs::runtime_15_native_live_host_bridge_methods_lock_poison_recovery_guard_covers_binding_registry`，避免继续扩大超大 `lock_poison_policy.rs`，并锁定生产段不回到 direct lock unwrap 或 `lock poisoned` panic。同步 Runtime 15/status/docs 锚点（含 `plugin/native_plugin_loader/native_plugin_live_host/bridge_methods.rs`、`docs/zircon_runtime/plugin/bridge.md` 与新守卫名）。验证：scoped rustfmt/static scans、native live-host bridge methods direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该切片不改变 ABI v3 bridge method table、manifest descriptor validation、reload scope rebuild 或 lifecycle report surface；完整 `module_convention_gate` 与 full plugin bridge Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 navigation lock poison recovery | runtime_15_navigation_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`navigation/runtime.rs` 的 `BuiltinNavigationManager` 状态 mutex 从 `expect("navigation state lock poisoned")` 收敛为私有 `lock_state()` poison recovery helper，`load_nav_mesh`、`load_navigation_settings`、`find_path`、`sample_position`、`raycast`、agent tick stats 更新、`tick_agent` 内部 path/sample 查询与 `stats()` 都统一走该 helper；新增 module-local `navigation_manager_accessors_recover_poisoned_state_lock` 覆盖锁中毒后 load/settings/sample/stats 仍可恢复。`structure_convention/lock_poison_policy.rs::runtime_15_navigation_lock_poison_recovery_guard_covers_builtin_navigation_manager` 锁定生产路径不回到 navigation state lock panic 或 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `navigation/runtime.rs`、`navigation/runtime/tests.rs`、`docs/zircon_runtime/navigation/runtime.md` 与新守卫名）。验证：scoped rustfmt/static scans、navigation direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 dynamic API session lock poison recovery | runtime_15_dynamic_api_session_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`dynamic_api/session.rs` 的全局 session registry 与单 session execution mutex 从 direct lock unwrap 收敛为私有 `lock_registry()` / `lock_session()` poison recovery helpers，`destroy_session`、`insert_session` 与 `with_session` 的 registry lookup/action dispatch 都统一走 helper。新增 `dynamic_api/session/tests/lock_poison.rs::dynamic_api_session_registry_accessors_recover_poisoned_locks`，先后 poison registry 与 session locks，再验证 `with_session` 和 `destroy_session` 仍返回 `Ok`；`structure_convention/lock_poison_policy.rs::runtime_15_dynamic_api_session_lock_poison_recovery_guard_covers_session_registry` 锁定生产路径不回到 `registry().lock().unwrap()` 或 `session.lock().unwrap()`，并同步 Runtime 15/status/docs 锚点（含 `dynamic_api/session.rs`、`dynamic_api/session/tests/lock_poison.rs`、`docs/zircon_runtime/dynamic_api/session.md` 与新守卫名）。验证：scoped rustfmt/static scans、dynamic API session direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full dynamic API Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 dynamic scene spawn task lock poison recovery | runtime_15_dynamic_scene_spawn_task_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`scene/dynamic_scene/spawn_task/task.rs` 的 async spawn status/result mutex 从 direct lock expect 收敛为 `lock_spawn_status(...)` 与 `lock_spawn_result(...)` poison recovery helpers，`status`、`status_snapshot`、`take_ready` 与 `wait_ready` 都统一走 helper。`scene/dynamic_scene/spawn_task/loader.rs` 的 background job running/completion/result write 也改为消费同一组 helper，不再保留 `dynamic scene spawn task ... lock poisoned` panic。新增 module-local `dynamic_scene_spawn_task_accessors_recover_poisoned_locks` 覆盖 status/result locks 中毒后仍可记录 poll、标记 running 并取回准备结果；`structure_convention/lock_poison_policy.rs::runtime_15_dynamic_scene_spawn_task_lock_poison_recovery_guard_covers_spawn_task` 锁定生产段不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `scene/dynamic_scene/spawn_task/task.rs`、`scene/dynamic_scene/spawn_task/loader.rs`、`docs/zircon_runtime/scene/dynamic_scene.md` 与新守卫名）。验证：scoped rustfmt/static scans、dynamic scene spawn task direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该切片不改变 `DynamicSceneSpawnTask`、`JobScheduler`、caller-thread world mutation、scene-asset reload queue 或 prepared spawn apply 语义；完整 `module_convention_gate` 与 full dynamic scene Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene ECS parallel executor lock poison recovery | runtime_15_scene_ecs_parallel_executor_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`scene/ecs/schedule_parallel_executor.rs` 的 scheduled batch result slot 从 direct `Mutex::lock().expect("scheduled batch result lock poisoned")` 收敛为私有 `lock_batch_result(...)` poison recovery helper，scheduled closure 写入 batch result 与 tail wait 后按 batch order 取回 result 都统一走 helper。新增 module-local `schedule_parallel_executor_batch_result_slot_recovers_poisoned_lock` 覆盖 batch result slot 被 poison 后仍可取回 `Ok(())` 与写入/取回 missing-task result；`structure_convention/lock_poison_policy.rs::runtime_15_scene_ecs_parallel_executor_lock_poison_recovery_guard_covers_batch_result_slots` 锁定生产段不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `scene/ecs/schedule_parallel_executor.rs`、`docs/zircon_runtime/scene/ecs.md` 与新守卫名）。验证：scoped rustfmt/static scans、ECS parallel executor direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该切片不改变 `ScheduleParallelExecutor` / `ScheduleParallelTaskRegistry` public surface、batch dependency chaining、abort flag semantics、batch-order result replay 或 diagnostics counters；完整 `module_convention_gate` 与 full ECS schedule Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 core resource manager lock poison recovery | runtime_15_core_resource_manager_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`core/resource/manager/resource_manager.rs` 把 `ResourceManager` 的 registry、payload、runtime slot 与 subscribers 锁入口收敛为 `lock_registry_read()`、`lock_registry_write()`、`lock_payloads_read()`、`lock_payloads_write()`、`lock_runtime_read()`、`lock_runtime_write()` 与 `lock_subscribers()` poison recovery helpers；`registry_ops.rs`、`payload_ops.rs`、`lease_ops.rs` 与 `events.rs` 不再直接对资源管理状态执行 lock expect/unwrap。新增 `resource_manager_accessors_recover_poisoned_state_locks` 覆盖 subscribers、registry、payloads 与 runtime locks 中毒后仍可 subscribe、register_ready、get/acquire、ref_count 与 runtime_state；`structure_convention/lock_poison_policy.rs::runtime_15_core_resource_manager_lock_poison_recovery_guard_covers_resource_manager` 锁定生产路径不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `core/resource/manager/resource_manager.rs`、`core/resource/manager/registry_ops.rs`、`docs/zircon_runtime/core/resource.md` 与新守卫名）。验证：scoped rustfmt/static scans、core resource manager direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full core resource Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset project manager lock poison recovery | runtime_15_asset_project_manager_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`asset/pipeline/manager/project_asset_manager/runtime.rs` 把 project RwLock、pending importer registry RwLock、change subscribers、watch-error subscribers 与 watcher mutex 入口收敛为 poison recovery helpers；`asset/pipeline/manager/project_asset_manager/construction.rs` 的 importer registry 写入/读取改为消费 `importer_registry_write()` / `importer_registry_read()`。新增 module-local `project_asset_manager_runtime_accessors_recover_poisoned_locks` 覆盖 project/importer/subscriber/watcher locks 被 poison 后仍可恢复；`structure_convention/lock_poison_policy.rs::runtime_15_asset_project_manager_lock_poison_recovery_guard_covers_project_asset_manager` 锁定生产路径不回到 direct lock unwrap、RwLock expect 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `asset/pipeline/manager/project_asset_manager/runtime.rs`、`asset/pipeline/manager/project_asset_manager/construction.rs`、`docs/zircon_runtime/asset/importer.md` 与新守卫名）。验证：scoped rustfmt/static scans、project asset manager direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full asset pipeline Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset worker pool lock poison recovery | runtime_15_asset_worker_pool_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`asset/pipeline/worker_pool.rs` 把 in-flight request table 与 worker diagnostics mutex 入口收敛为 `lock_in_flight()` / `lock_diagnostics()` 以及 free helper `lock_in_flight_map()` / `lock_worker_diagnostics()`；request de-duplication、queue-full rollback、diagnostics snapshot、frame diagnostics 与 completion publishing 不再因 poisoned worker locks panic。`asset/pipeline/manager/service_contracts/asset_manager_contract.rs` 改为消费 `ProjectAssetManager` 既有 importer/subscriber helpers，`project_asset_manager/runtime.rs` 将 subscriber helpers 暴露到 manager owner 内。新增 module-local `asset_worker_pool_accessors_recover_poisoned_locks` 覆盖 worker locks 中毒后仍可 request、diagnostics 与 completion publish；`structure_convention/lock_poison_policy.rs::runtime_15_asset_worker_pool_lock_poison_recovery_guard_covers_asset_worker_pool` 锁定生产路径不回到 direct lock unwrap、RwLock expect 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `asset/pipeline/worker_pool.rs`、`asset/pipeline/manager/service_contracts/asset_manager_contract.rs` 与 `docs/zircon_runtime/asset/worker_pool.md`）。验证：scoped rustfmt/static scans、asset worker pool direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full asset pipeline Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 WGPU render framework lock poison recovery | runtime_15_wgpu_render_framework_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`graphics/runtime/render_framework/wgpu_render_framework/wgpu_render_framework.rs` 的 `WgpuRenderFramework::lock_operation()` 与 `lock_state()` 从 direct `.lock().unwrap()` 收敛为 poison recovery helpers；create/destroy viewport、pipeline asset set/reload/register、frame submit、capture、viewport surface、stats/debugger query 等生产路径继续只通过这两个入口访问 operation/state locks。新增 module-local `wgpu_render_framework_accessors_recover_poisoned_locks` 覆盖 operation lock 与 render framework state lock 中毒后仍可恢复；`structure_convention/lock_poison_policy.rs::runtime_15_wgpu_render_framework_lock_poison_recovery_guard_covers_wgpu_framework` 锁定生产路径不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `graphics/runtime/render_framework/wgpu_render_framework/wgpu_render_framework.rs`、`docs/zircon_runtime/graphics/render-product-submit.md` 与新守卫名）。验证：scoped rustfmt/static scans、WGPU render framework direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full graphics/render Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 RHI WGPU render device lock poison recovery | runtime_15_rhi_wgpu_render_device_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`rhi_wgpu/device.rs` 的 `WgpuRenderDevice` internal state mutex 从多处 direct `.lock().unwrap()` 收敛为私有 `lock_state()` poison recovery helper，buffer/texture/sampler/bind group/shader/pipeline/fence/submit/transient stats/readback/writeback 生产路径统一通过该入口访问 headless WGPU device state。新增 module-local `wgpu_render_device_state_accessors_recover_poisoned_lock` 覆盖 state lock 中毒后仍可读取 transient allocator stats、创建 staging buffer、写入并读回数据；新增独立结构守卫 `structure_convention/rhi_wgpu_lock_poison.rs::runtime_15_rhi_wgpu_render_device_lock_poison_recovery_guard_covers_device_state`，避免继续扩大超大 `lock_poison_policy.rs`，并同步 Runtime 15/status/docs 锚点（含 `rhi_wgpu/device.rs` 与 `docs/zircon_runtime/rhi/descriptors.md`）。验证：scoped rustfmt/static scans、RHI WGPU device production direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` passed with LF-to-CRLF warnings only；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 15540、30624、41464、42000、61116、69224；`rustc` PIDs 3600、6012、6488、9080、10692、15256、23412、29408、38788、52576、54436、57988、58796、63940）按 Runtime 15 实施切片节奏 deferred，不计通过。该切片不改变 `RenderDevice` trait、neutral RHI descriptors、headless command-list validation、resource handle semantics 或 fence completion behavior；完整 `module_convention_gate` 与 full RHI Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 animation manager lock poison recovery | runtime_15_animation_manager_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`animation/manager/mod.rs` 的 `DefaultAnimationManager` playback settings mutex 从 `expect("animation playback mutex poisoned")` 收敛为私有 `lock_playback_settings()` poison recovery helper，`store_playback_settings` 与 `playback_settings()` 都通过该 helper 访问 `AnimationPlaybackSettings`。新增 module-local `animation_manager_playback_settings_recover_poisoned_lock` 覆盖播放设置锁中毒后仍可 store/read；`structure_convention/lock_poison_policy.rs::runtime_15_animation_manager_lock_poison_recovery_guard_covers_playback_settings` 锁定生产路径不回到 direct lock unwrap 或 playback mutex panic，并同步 Runtime 15/status/docs 锚点（含 `animation/manager/mod.rs`、`docs/zircon_runtime/animation/runtime.md` 与新守卫名）。验证：scoped rustfmt/static scans、animation manager direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full animation Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 input runtime manager lock poison recovery | runtime_15_input_runtime_manager_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`input/runtime/default_input_manager.rs` 的 `DefaultInputManager` state mutex 与 `input/runtime/default_input_action_manager.rs` 的 action evaluator mutex 收敛为私有 `lock_state()` / `lock_evaluator()` poison recovery helpers；begin-frame、submit-event、snapshot/drain 路径和 action-map/evaluation 路径不再 direct lock unwrap。新增 module-local `input_manager_accessors_recover_poisoned_state_lock` 与 `input_action_manager_accessors_recover_poisoned_evaluator_lock` 覆盖中毒后 input state 仍可 submit/snapshot/drain，action manager 仍可 set/get/evaluate；`structure_convention/lock_poison_policy.rs::runtime_15_input_runtime_manager_lock_poison_recovery_guard_covers_input_state` 锁定生产路径不回到 direct lock unwrap，并同步 Runtime 15/status/docs 锚点（含 `input/runtime/default_input_manager.rs`、`input/runtime/default_input_action_manager.rs`、`docs/zircon_runtime/input/input_state.md` 与新守卫名）。验证：scoped rustfmt/static scans、input runtime manager direct-lock scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full input Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 script VM registry lock poison recovery | runtime_15_script_vm_registry_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`script/vm/backend/backend_registry.rs`、`script/vm/host/host_registry.rs`、`script/vm/host/host_export_registry.rs` 与 `script/vm/runtime/hot_reload_coordinator.rs` 的内部 registry/slot locks 收敛为 `lock_families()`、`lock_handles()`、`lock_modules()` 与 `lock_slots()` poison recovery helpers，backend family lookup、host capability records、host export descriptors/call table 与 VM plugin hot-reload slot projection 生产路径不再 direct lock unwrap。新增 module-local `vm_backend_registry_accessors_recover_poisoned_family_lock`、`host_registry_accessors_recover_poisoned_handle_lock`、`host_export_registry_accessors_recover_poisoned_module_lock` 与 `hot_reload_coordinator_accessors_recover_poisoned_slot_table_lock` 覆盖中毒后注册、解析、调用表与 slot lifecycle 仍可恢复；`structure_convention/lock_poison_policy.rs::runtime_15_script_vm_registry_lock_poison_recovery_guard_covers_vm_registries` 锁定生产路径不回到 direct lock unwrap 或 `lock poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `script/vm/backend/backend_registry.rs`、`script/vm/host/host_export_registry.rs` 与 `docs/zircon_runtime/script/vm/zr_vm_host_reflection.md`）。验证：scoped rustfmt/static scans、script VM registry direct-lock/direct-panic scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full script VM Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 ZrVM real backend runtime lock poison recovery | runtime_15_zr_vm_real_backend_runtime_lock_poison_recovery_static_passed_cargo_timeout_no_result | 2026-06-27 | E9/F2 follow-up：`script/vm/backend/zr_vm_project_backend/real_backend/lock.rs` 的全局 real-backend runtime mutex 从 `expect("zr_vm runtime lock should not be poisoned")` 收敛为 `unwrap_or_else(|poisoned| poisoned.into_inner())` poison recovery helper，`package.rs` 的项目加载与 `instance.rs` 的 activate/deactivate/save/restore/export 调用继续通过 `acquire_zr_vm_lock()` 串行化 ZrVM runtime/session 访问但不再因 poisoned lock panic。新增 module-local `zr_vm_real_backend_runtime_lock_recovers_after_poison` 覆盖中毒后 helper 仍可恢复；`structure_convention/lock_poison_policy/asset_render_input.rs::runtime_15_zr_vm_real_backend_runtime_lock_poison_recovery_guard_covers_global_runtime_lock` 锁定生产段不回到 direct lock unwrap 或 `lock should not be poisoned` panic，并同步 Runtime 15/status/docs 锚点（含 `script/vm/backend/zr_vm_project_backend/real_backend/lock.rs`、`script/vm/backend/zr_vm_project_backend/real_backend/package.rs` 与 `docs/zircon_runtime/script/vm/zr_vm_project_backend.md`）。验证：scoped rustfmt/static scans、ZrVM runtime lock direct-panic rollback scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo gate 首次因 `ZR_VM_RUST_BINDING_LIB_DIR` 未设置被 build script 拦截，补 `E:\Git\zr_vm\build-msvc\lib\Release` / `bin\Release` 后 120s timeout_no_result，已终止本轮 cargo/rustc 子进程，不计通过。该切片不改变 real backend selector、host callback table、project compile/session lifecycle 或 lifecycle export ABI；完整 `module_convention_gate` 与 full script VM/ZrVM Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 VM plugin manager selected-backend lock poison recovery | runtime_15_vm_plugin_manager_selected_backend_lock_poison_recovery_static_passed_cargo_deferred | 2026-06-24 | E9/F2 follow-up：`script/vm/runtime/vm_plugin_manager.rs` 的 selected backend `RwLock<String>` 从 direct read/write unwrap 收敛为 `selected_backend_read()` / `selected_backend_write()` poison recovery helpers，`selected_backend_name()` 与 `select_default_backend()` 不再因 poisoned selected-backend lock panic。新增 module-local `vm_plugin_manager_selected_backend_accessors_recover_poisoned_lock` 覆盖 selected-backend lock 中毒后仍可读取默认 selector 并切换到 `builtin:mock`；新增独立结构守卫 `structure_convention/script_vm_lock_poison.rs::runtime_15_vm_plugin_manager_selected_backend_lock_poison_recovery_guard_covers_manager_selector`，避免继续扩大超大 `lock_poison_policy.rs`，并同步 Runtime 15/status/docs 锚点（含 `script/vm/runtime/vm_plugin_manager.rs` 与 `docs/zircon_runtime/script/vm/zr_vm_host_reflection.md`）。验证：scoped rustfmt/static scans、VM plugin manager direct RwLock unwrap scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该切片不改变 backend registry、plugin package discovery、slot lifecycle、host export dispatch 或 default backend selector values；完整 `module_convention_gate` 与 full script VM Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 diagnostics guard module split | runtime_15_diagnostics_guard_module_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：diagnostics frame trait / ECS wrapper cleanup 结构守卫已从顶层 `structure_convention.rs` 迁入 `structure_convention/diagnostics_surface.rs`，让 F14 diagnostics normalization 守卫和 diagnostics 文档锚同 owner。新增 `runtime_15_diagnostics_guard_is_folder_backed`，验证顶层聚合文件只挂载 diagnostics 子模块、不再直接持有 `runtime_15_diagnostics_use_frame_trait_without_world_wrapper`，`structure_convention.rs` 回落到 80 行以下，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 diagnostics guard 测试组织子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 core framework test folder split | runtime_15_core_framework_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`core/framework/tests.rs` 底部 framework surface 与 render product/camera 合约用例已迁入 folder-backed `core/framework/tests/framework_surfaces.rs` 与 `core/framework/tests/render_product_surface.rs`，父文件从 1848 行降到 653 行，两个新增子文件分别保持 800 行以下，既有 `phase_queue_summary.rs` 保持子 owner。新增 `structure_convention/test_file_budget.rs::runtime_15_core_framework_tests_are_folder_backed`，验证父/子模块挂载、 moved guard 不回流、所有 core framework test owner 行数低于 800，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录关闭 `core/framework/tests.rs` 的 M3 folder-backed 子切片；`ui/tests/v2_asset.rs`、`ui/tests/shared_core.rs`、完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 core runtime deactivation blocked test folder split | runtime_15_core_runtime_deactivation_blocked_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`core/runtime/tests/activation/behavior/deactivation/blocked.rs` 已拆成 folder-backed `core/runtime/tests/activation/behavior/deactivation/blocked/{external_dependents,exact_two_three_dependency_matcher,shutdown_order,exact_four_dependency_matcher,exact_five_without_index_map}.rs` 五个新增 deactivation-blocked 测试 owner，并保留既有 `exact_five_dependency_matcher.rs` 子 owner。父文件从 869 行降到 7 行，只保留子模块挂载；10 个 blocked 测试全部保留在子模块，最大子文件 `exact_two_three_dependency_matcher.rs` 为 280 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/core_runtime_deactivation.rs::runtime_15_core_runtime_deactivation_blocked_tests_are_folder_backed`，验证父/子模块挂载、代表性 external dependent / exact matcher / shutdown-order moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `blocked.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI v2 asset test folder split | runtime_15_ui_v2_asset_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`ui/tests/v2_asset.rs` 已拆成 folder-backed `ui/tests/v2_asset/{asset_loading,style_runtime,default_controls,range_controls,demo_and_builder,composite_components,file_cache}.rs` 七个行为 owner，父文件只保留共享导入、子模块挂载和 helper，行数从 3806 降到 331，最大子文件 `style_runtime.rs` 为 718 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget.rs::runtime_15_ui_v2_asset_tests_are_folder_backed`，验证父/子模块挂载、代表性 moved guard 不回流、全部 UI v2 asset test owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录关闭 `ui/tests/v2_asset.rs` 的 M3 folder-backed 子切片；`ui/tests/shared_core.rs`、完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI shared core test folder split | runtime_15_ui_shared_core_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`ui/tests/shared_core.rs` 已拆成 folder-backed `ui/tests/shared_core/{layout_surface,box_flow,input_visibility,navigation,scroll_mutation}.rs` 五个行为 owner，父文件只保留共享导入、模块挂载和 constraint / pointer helper，行数从 3145 降到 77，最大子文件 `input_visibility.rs` 为 771 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget.rs::runtime_15_ui_shared_core_tests_are_folder_backed`，验证父/子模块挂载、代表性 moved guard 不回流、全部 UI shared core test owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录关闭 `ui/tests/shared_core.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI shared core guard child-owner split | runtime_15_ui_shared_core_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`tests/runtime_absorption/structure_convention/test_file_budget/ui_shared_core.rs` 从 641 行减压为路由父文件，只挂载 `ui_shared_core/{root,layout_surface,input_visibility,scroll_mutation}.rs` 四个守护 child owner；新增 `structure_convention/test_file_budget/ui_shared_core/layout_surface.rs` 等 focused owner 承接既有 UI shared-core root、layout-surface、input-visibility 与 scroll-mutation 预算守护，最大 child 为 227 行，父子 owner 均低于 400 行 focused 预算。新增 `runtime_15_ui_shared_core_guard_child_owners_are_folder_backed`，锁定父文件不再定义四个已移动守护、子 owner 保留原守护、状态输出期望行与 Runtime 15/status/module/UI/session docs 锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved guard 扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo 进程 active 按支撑切片节奏 deferred，不计 Cargo 通过。完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 test-file-budget guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI accessibility test folder split | runtime_15_ui_accessibility_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/accessibility.rs` 已拆成 folder-backed `ui/tests/accessibility/{extraction,naming_relations,focus_diagnostics,description_references,activation_actions,value_actions}.rs` 六个 accessibility 行为 owner，父文件只保留共享导入、子模块挂载和 accessibility tree/surface helper，行数从 2251 降到 125；最大子文件 `ui/tests/accessibility/value_actions.rs` 低于 800 行，49 个原有测试全部保留在子模块。新增 `structure_convention/test_file_budget/ui_accessibility.rs::runtime_15_ui_accessibility_tests_are_folder_backed`，验证父/子模块挂载、代表性 extraction/value action moved guard 不回流、迁移测试数量和 UI accessibility test owner 行数预算，并同步状态输出期望行（包含 `ui/tests/accessibility.rs`、`ui/tests/accessibility/extraction.rs` 与 `ui/tests/accessibility/value_actions.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/accessibility.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI accessibility widget actions test folder split | runtime_15_ui_accessibility_widget_actions_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/accessibility_widget_actions.rs` 已拆成 folder-backed parent + `ui/tests/accessibility_widget_actions/{disclosure_actions,popup_actions,tooltip_menu}.rs` 三个 widget action 行为 owner。父文件从 811 行降到 250 行，只保留共享 surface、dispatch、binding report 和 runtime widget/popup/tooltip fixture helper；11 个原有测试全部保留在子模块，子文件分别承接 3/6/2 个测试，最大子文件 `ui/tests/accessibility_widget_actions/popup_actions.rs` 为 291 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_accessibility_widget_actions.rs::runtime_15_ui_accessibility_widget_actions_tests_are_folder_backed`，验证父/子模块挂载、disclosure/popup/tooltip-menu moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/accessibility_widget_actions.rs`、`ui/tests/accessibility_widget_actions/popup_actions.rs` 与 `ui/tests/accessibility_widget_actions/tooltip_menu.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/accessibility_widget_actions.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI layout slots test folder split | runtime_15_ui_layout_slots_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/layout_slots.rs` 已拆成 folder-backed parent + `ui/tests/layout_slots/{linear_free,overlay_scroll,flow_grid_masonry}.rs` 三个 layout slot 行为 owner。父文件从 867 行降到 100 行，只保留共享导入、子模块挂载、constraint/pointer/render/hit helper；10 个原有测试全部保留在子模块，子文件分别承接 3/3/4 个测试，最大子文件 `ui/tests/layout_slots/flow_grid_masonry.rs` 为 315 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_layout_slots.rs::runtime_15_ui_layout_slots_tests_are_folder_backed`，验证父/子模块挂载、linear/free、overlay/scroll、flow/grid/masonry moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/layout_slots.rs`、`ui/tests/layout_slots/linear_free.rs` 与 `ui/tests/layout_slots/flow_grid_masonry.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/layout_slots.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI surface-frame authority test folder split | runtime_15_ui_surface_frame_authority_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/surface_frame_authority.rs` 已拆成 folder-backed parent + `ui/tests/surface_frame_authority/{arranged_authority,taffy_flex,taffy_wrap_grid,zircon_fallback}.rs` 四个 surface-frame authority 行为 owner。父文件从 922 行降到 409 行，只保留共享导入、常量、surface fixture、layout button helper、Taffy/Grid/SizeBox fixture 和子模块挂载；9 个原有测试全部保留在子模块，子文件分别承接 2/4/2/1 个测试，最大子文件 `ui/tests/surface_frame_authority/taffy_flex.rs` 为 297 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_surface_frame_authority.rs::runtime_15_ui_surface_frame_authority_tests_are_folder_backed`，验证父/子模块挂载、arranged/focus、Taffy flex、Taffy wrap/grid 与 Zircon fallback moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/surface_frame_authority.rs`、`ui/tests/surface_frame_authority/arranged_authority.rs` 与 `ui/tests/surface_frame_authority/taffy_wrap_grid.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/surface_frame_authority.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI surface dirty domains test folder split | runtime_15_ui_surface_dirty_domains_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/surface_dirty_domains.rs` 已拆成 folder-backed parent + `ui/tests/surface_dirty_domains/{rebuild_domains,incremental_layout,render_domains,mutation_state}.rs` 四个 dirty-domain 行为 owner。父文件从 1021 行降到 297 行，只保留共享 surface fixture、dirty/assert helper、layout route helper、keyboard event helper 和子模块挂载；13 个原有测试全部保留在子模块，子文件分别承接 2/5/4/2 个测试，最大子文件 `ui/tests/surface_dirty_domains/incremental_layout.rs` 为 261 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_surface_dirty_domains.rs::runtime_15_ui_surface_dirty_domains_tests_are_folder_backed`，验证父/子模块挂载、rebuild、incremental layout、render-only 与 mutation/state moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/surface_dirty_domains.rs`、`ui/tests/surface_dirty_domains/rebuild_domains.rs` 与 `ui/tests/surface_dirty_domains/incremental_layout.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/surface_dirty_domains.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI material layout test folder split | runtime_15_ui_material_layout_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/material_layout.rs` 已拆成 folder-backed parent + `ui/tests/material_layout/{button_icon_metrics,row_label_metrics,field_values,asset_icon_roles,constraints_children}.rs` 五个 material-layout 行为 owner。父文件从 813 行降到 111 行，只保留共享 material leaf measurement/render command helper、intrinsic constraint helper 和子模块挂载；23 个原有测试全部保留在子模块，子文件分别承接 7/5/5/3/3 个测试，最大子文件 `ui/tests/material_layout/field_values.rs` 为 238 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_material_layout.rs::runtime_15_ui_material_layout_tests_are_folder_backed`，验证父/子模块挂载、button/icon、row/label、field values、asset/icon roles 与 constraints/children moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/material_layout.rs`、`ui/tests/material_layout/button_icon_metrics.rs` 与 `ui/tests/material_layout/field_values.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/material_layout.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI template test folder split | runtime_15_ui_template_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/template.rs` 已拆成 folder-backed parent + `ui/tests/template/{loader_instance_validation,interaction_bindings,surface_containers,slot_contracts,layout_compute}.rs` 五个 template 行为 owner。父文件从 884 行降到 154 行，只保留共享 template TOML fixture、tree helper、root helper 和子模块挂载；22 个原有测试全部保留在子模块，子文件分别承接 3/9/4/5/1 个测试，最大子文件 `ui/tests/template/surface_containers.rs` 为 204 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_template.rs::runtime_15_ui_template_tests_are_folder_backed`，验证父/子模块挂载、loader/instance、interaction binding、surface/container、slot contract 与 layout compute moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/template.rs`、`ui/tests/template/interaction_bindings.rs` 与 `ui/tests/template/slot_contracts.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/template.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI component catalog test folder split | runtime_15_ui_component_catalog_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/component_catalog.rs` 已拆成 folder-backed parent + `ui/tests/component_catalog/{catalog_inventory,descriptor_contracts,registry_queries}.rs` 三个 component catalog 行为 owner。父文件从 934 行降到 136 行，只保留 component catalog 子模块挂载、共享 descriptor/registry helper 与 schema/assert helper；7 个原父文件测试全部迁出，子文件分别承接 1/3/3 个测试，最大子文件 `ui/tests/component_catalog/catalog_inventory.rs` 为 446 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_component_catalog.rs::runtime_15_ui_component_catalog_tests_are_folder_backed`，验证父/子模块挂载、catalog inventory、descriptor contracts 与 registry query/revision moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/component_catalog.rs`、`ui/tests/component_catalog/catalog_inventory.rs` 与 `ui/tests/component_catalog/descriptor_contracts.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/component_catalog.rs` 父文件的 M3 folder-backed 子切片；既有 `ui/tests/component_catalog/component_state.rs` 仍超过 800 行，完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI boundary test folder split | runtime_15_ui_boundary_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/boundary.rs` 已拆成 folder-backed parent + `ui/tests/boundary/{template_namespace,layout_tree_surface,binding_event_roots,asset_fixture_projection}.rs` 四个 UI boundary 行为 owner。父文件从 1210 行降到 62 行，只保留文件系统/路径 helper 和子模块挂载；32 个原有测试全部保留在子模块，子文件分别承接 14/8/4/6 个测试，最大子文件 `ui/tests/boundary/template_namespace.rs` 为 438 行，所有 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_boundary.rs::runtime_15_ui_boundary_tests_are_folder_backed`，验证父/子模块挂载、template namespace、layout/tree/surface、binding/event root 与 asset fixture/projection moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/boundary.rs`、`ui/tests/boundary/template_namespace.rs` 与 `ui/tests/boundary/asset_fixture_projection.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/boundary.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI component state test folder split | runtime_15_ui_component_catalog_component_state_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/component_catalog/component_state.rs` 已拆成 folder-backed parent + `ui/tests/component_catalog/component_state/{retained_events,collection_mutation,reference_sources,interaction_numeric}.rs` 四个 component-state 行为 owner。父文件从 965 行降到 26 行，只保留现有 component-state 子模块挂载和共享导入；18 个原父文件测试全部保留在子模块，子文件分别承接 4/3/6/5 个测试，最大子文件 `ui/tests/component_catalog/component_state/reference_sources.rs` 为 300 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_component_catalog_component_state.rs::runtime_15_ui_component_catalog_component_state_tests_are_folder_backed`，验证父/子模块挂载、retained events、collection mutation、reference source 与 interaction/numeric moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/component_catalog/component_state.rs`、`ui/tests/component_catalog/component_state/reference_sources.rs` 与 `ui/tests/component_catalog/component_state/interaction_numeric.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/component_catalog/component_state.rs` 父文件的 M3 folder-backed 子切片；后续键盘子 owner 已由相邻记录继续拆分，完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI component state keyboard test folder split | runtime_15_ui_component_catalog_component_state_keyboard_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/component_catalog/component_state/keyboard.rs` 已拆成 folder-backed parent + `ui/tests/component_catalog/component_state/keyboard/{action_selection,menu_navigation,text_inputs,numeric_controls}.rs` 四个 component-state keyboard 行为 owner。父文件从 1282 行降到 20 行，只保留共享 component-state keyboard 导入、`menu_option` helper 和子模块挂载；14 个原父文件测试全部迁入子模块，子文件分别承接 5/5/2/2 个测试，最大子文件 `ui/tests/component_catalog/component_state/keyboard/menu_navigation.rs` 为 586 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_component_catalog_component_state_keyboard.rs::runtime_15_ui_component_catalog_component_state_keyboard_tests_are_folder_backed`，验证父/子模块挂载、action selection、menu/tree/table navigation、text input editing 与 numeric/range control moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/component_catalog/component_state/keyboard.rs`、`ui/tests/component_catalog/component_state/keyboard/action_selection.rs` 与 `ui/tests/component_catalog/component_state/keyboard/menu_navigation.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/component_catalog/component_state/keyboard.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI Material foundation test folder split | runtime_15_ui_component_catalog_material_foundation_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/component_catalog/material_foundation/mod.rs` 已拆成 folder-backed parent + `ui/tests/component_catalog/material_foundation/{planned_layers,editor_components,mui_surface_overlay,mui_x_runtime,folder_structure}.rs` 五个 Material foundation 行为 owner。父文件从 1081 行降到 149 行，只保留 Material foundation 子模块挂载和共享 schema/helper；原超长 catalog 覆盖被拆成 4 个行为测试，原 folder-backed-family 守卫下沉到 child，5 个测试全部保留在子模块，最大子文件 `ui/tests/component_catalog/material_foundation/editor_components.rs` 为 324 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_component_catalog_material_foundation.rs::runtime_15_ui_component_catalog_material_foundation_tests_are_folder_backed`，验证父/子模块挂载、planned layers、editor descriptors、MUI surface/overlay、MUI X/runtime visibility 与 folder structure moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/component_catalog/material_foundation/mod.rs`、`ui/tests/component_catalog/material_foundation/planned_layers.rs` 与 `ui/tests/component_catalog/material_foundation/editor_components.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/component_catalog/material_foundation/mod.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI event routing test folder split | runtime_15_ui_event_routing_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/event_routing.rs` 已拆成 folder-backed `ui/tests/event_routing/{pointer_state,component_events,dispatch_effects,shared_input}.rs` 四个 routing 行为 owner，父文件只保留共享导入、子模块挂载和 button/scroll/editable/input helper，行数从 1676 降到 341；最大子文件 `ui/tests/event_routing/shared_input.rs` 低于 800 行，27 个原有测试全部保留在子模块。新增 `structure_convention/test_file_budget/ui_event_routing.rs::runtime_15_ui_event_routing_tests_are_folder_backed`，验证父/子模块挂载、代表性 pointer/component/dispatch/shared-input moved guard 不回流、迁移测试数量和 UI event routing test owner 行数预算，并同步状态输出期望行（包含 `ui/tests/event_routing.rs`、`ui/tests/event_routing/pointer_state.rs` 与 `ui/tests/event_routing/shared_input.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/event_routing.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime input reply routes test folder split | runtime_15_ui_runtime_input_reply_routes_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/runtime_input_reply_routes.rs` 已将父文件剩余 13 个统一 reply route 测试迁入 folder-backed `ui/tests/runtime_input_reply_routes/route_trace_routes.rs`、`ui/tests/runtime_input_reply_routes/pointer_bubble_routes.rs` 与 `ui/tests/runtime_input_reply_routes/focus_text_accessibility_routes.rs` 三个行为 owner，父文件只保留已有子模块挂载、共享 route surface/input helper 和 assertion helper，行数从 1558 降到 500；三个新子文件全部低于 800 行。新增 `structure_convention/test_file_budget/ui_runtime_input_reply_routes.rs::runtime_15_ui_runtime_input_reply_routes_tests_are_folder_backed`，验证父/子模块挂载、代表性 route-trace/pointer-bubble/focus-text/accessibility moved guard 不回流、迁移测试数量和本次新增 UI runtime input reply route owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/runtime_input_reply_routes.rs` 父文件的 M3 folder-backed 子切片；既有 `keyboard_navigation_routes.rs` 与 `tree_view_pointer_routes.rs` 仍超过 800 行，完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime input reply route child folder split | runtime_15_ui_runtime_input_reply_route_children_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/runtime_input_reply_routes/keyboard_navigation_routes.rs` 已从 980 行降到 152 行，并把 keyboard focus path、semantic action route、timers-disabled 与 directional navigation 测试迁入 folder-backed `keyboard_navigation_routes/{focus_path,semantic_actions,timers_disabled,directional}.rs` 四个 owner，15 个原有测试全部保留；`ui/tests/runtime_input_reply_routes/tree_view_pointer_routes.rs` 已从 810 行降到 418 行，并把 tree selection、drag/reorder 与 virtualization 测试迁入 `tree_view_pointer_routes/{selection,drag_reorder,virtualization}.rs` 三个 owner，9 个原有测试全部保留。新增 `structure_convention/test_file_budget/ui_runtime_input_reply_routes.rs::runtime_15_ui_runtime_input_reply_route_children_are_folder_backed`，验证两个 child parent 的模块挂载、helper ownership、代表性 moved guard 不回流、keyboard/tree 子 owner 测试数量与行数预算，并同步 status-output 期望锚点。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭本轮两个 oversized reply-route 子 owner；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime input reply route guard child-owner split | runtime_15_ui_runtime_input_reply_route_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`tests/runtime_absorption/structure_convention/test_file_budget/ui_runtime_input_reply_routes.rs` 从 539 行减压为路由父文件，只挂载 `ui_runtime_input_reply_routes/{root,route_children,table_pointer}.rs` 三个守护 child owner；新增 `structure_convention/test_file_budget/ui_runtime_input_reply_routes/route_children.rs` 承接既有 keyboard/tree child-route 守护，`root.rs` 承接基础 reply routes 守护，`table_pointer.rs` 承接 table pointer 守护，最大 child 为 243 行，父子 owner 均低于 400 行 focused 预算。新增 `runtime_15_ui_runtime_input_reply_route_guard_child_owners_are_folder_backed`，锁定父文件不再定义三个已移动守护、子 owner 保留原守护、状态输出期望行与 Runtime 15/status/module/UI/session docs 锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved guard 扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 进程 active 按支撑切片节奏 deferred，不计 Cargo 通过。完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 test-file-budget guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 runtime diagnostics test folder split | runtime_15_runtime_diagnostics_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`tests/runtime_diagnostics/mod.rs` 中单个超长 render diagnostics 断言体已迁入 folder-backed `tests/runtime_diagnostics/{capability_history_visibility,hzb_light_camera_capture,graph_resources,graph_execution,post_process_material_mesh,gpu_sprite_ui_advanced}.rs` 六个诊断领域 owner；父文件只保留两个测试入口、runtime/devtools 装配和子模块调用，行数从 2098 降到 89，最大子文件 `tests/runtime_diagnostics/graph_resources.rs` 为 445 行，既有 `support.rs` 为 703 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget.rs::runtime_15_runtime_diagnostics_tests_are_folder_backed`，验证父/子模块挂载、代表性 diagnostics series moved anchor 不回流、所有 runtime diagnostics test owner 行数预算，并同步状态输出期望行（包含 `tests/runtime_diagnostics/graph_resources.rs` 与 `tests/runtime_diagnostics/gpu_sprite_ui_advanced.rs`）。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录关闭 `tests/runtime_diagnostics/mod.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 RHI command list test folder split | runtime_15_rhi_command_list_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`rhi/tests/command_list.rs` 已拆成 folder-backed `rhi/tests/command_list/{basic_commands,bind_groups,raster_draws,vertex_index_state}.rs` 四个 command-list 行为 owner，父文件只保留共享导入、RHI test fixture helpers 和子模块挂载，行数从 1034 降到 214；四个子文件分别为 152、233、222、222 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget.rs::runtime_15_rhi_command_list_tests_are_folder_backed`，验证父/子模块挂载、代表性 moved guard 不回流、所有 RHI command-list test owner 行数预算，并同步状态输出期望行（包含 `rhi/tests/command_list/basic_commands.rs` 与 `rhi/tests/command_list/vertex_index_state.rs`）。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录关闭 `rhi/tests/command_list.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 RHI device contract test folder split | runtime_15_rhi_device_contract_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`rhi/tests/device_contract.rs` 已拆成 folder-backed `rhi/tests/device_contract/{basic_resources,texture_sampler_descriptors,bind_groups,invalid_descriptors,transfer_and_fences,framework_boundary}.rs` 六个 device-contract 行为 owner，父文件只保留共享 RHI 导入、bind-group layout/pipeline layout helpers 和子模块挂载，行数从 987 降到 40；最大子文件 `rhi/tests/device_contract/bind_groups.rs` 为 308 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget.rs::runtime_15_rhi_device_contract_tests_are_folder_backed`，验证父/子模块挂载、代表性 moved guard 不回流、所有 RHI device-contract test owner 行数预算，并同步状态输出期望行（包含 `rhi/tests/device_contract/basic_resources.rs` 与 `rhi/tests/device_contract/framework_boundary.rs`）。验证：scoped rustfmt/static checks 通过；带锁 core-min cargo check 明确返回当前工作区 `Cargo.lock` / `Cargo.toml` 不一致，focused structure guard 与 status-output Cargo 尝试在 active Cargo lanes 下超时且无测试结果，不计通过。该记录关闭 `rhi/tests/device_contract.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset pack test folder split | runtime_15_asset_pack_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/pack.rs` 已拆成 folder-backed `asset/tests/pack/{basic,reader_validation,delta_reader_validation,delta_pack,delta_installer,trim}.rs` 六个资产打包行为 owner，父文件只保留共享 pack manifest/helper、子模块挂载和临时目录 helper，行数从 1288 降到 154；最大子文件 `asset/tests/pack/delta_installer.rs` 为 402 行，42 个原有测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_tests.rs::runtime_15_asset_pack_tests_are_folder_backed`，验证父/子模块挂载、代表性 pack/delta/installer/trim moved guard 不回流、迁移测试数量和所有 asset pack test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/pack.rs` 与 `asset/tests/pack/delta_installer.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均 120s 超时无结果，当前工作区 Cargo.lock/Cargo.toml 漂移仍未解决，不计通过。该记录关闭 `asset/tests/pack.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset facade test folder split | runtime_15_asset_facade_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/facade.rs` 已拆成 folder-backed `asset/tests/facade/{handle_events,load_state_roots,project_facade,recursive_dependencies,dependency_failures}.rs` 五个资产 façade 行为 owner，并保留既有 `failure_reason.rs`、`handle_lifecycle.rs` 与 `hot_reload.rs` 子 owner；父文件只保留共享 asset/resource helper、`ui_v2_view_asset()` 和模块挂载，行数从 1017 降到 111。新增子文件最大 `recursive_dependencies.rs` 为 292 行，20 个原父文件测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_tests.rs::runtime_15_asset_facade_tests_are_folder_backed`，验证父/子模块挂载、代表性 typed handle/load-state/project/dependency moved guard 不回流、迁移测试数量和 asset facade test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/facade.rs` 与 `asset/tests/facade/recursive_dependencies.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描与 docs/status 锚点扫描通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/facade.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset project zmeta test folder split | runtime_15_asset_project_zmeta_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/project/zmeta.rs` 已拆成 folder-backed `asset/tests/project/zmeta/{metadata_lifecycle,package_roots,compound_shader,shader_diagnostics_fixture}.rs` 四个 zmeta 行为 owner，父文件只保留共享 importer/material/import outcome helper 和子模块挂载，行数从 996 降到 104。最大子文件 `compound_shader.rs` 为 283 行，9 个原有测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_tests.rs::runtime_15_asset_project_zmeta_tests_are_folder_backed`，验证父/子模块挂载、代表性 zmeta schema/package/compound shader/fixture moved guard 不回流、迁移测试数量和 asset project zmeta test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/project/zmeta.rs` 与 `asset/tests/project/zmeta/compound_shader.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均 120s 超时无结果，未留下匹配 target-dir 进程，不计通过。该记录关闭 `asset/tests/project/zmeta.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset project manager test folder split | runtime_15_asset_project_manager_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/project/manager.rs` 已拆成 folder-backed `asset/tests/project/manager/{library_imports,restore_failure_migration,subassets_errors}.rs` 三个 project-manager 行为 owner，父文件只保留共享 first-wave importer fixture、counted importer、dependency/subasset importer helper 和子模块挂载，行数从 940 降到 181。最大子文件 `restore_failure_migration.rs` 为 292 行，11 个原有测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_tests.rs::runtime_15_asset_project_manager_tests_are_folder_backed`，验证父/子模块挂载、代表性 first-wave import / restore / migration / subasset moved guard 不回流、迁移测试数量和 asset project manager test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/project/manager.rs` 与 `asset/tests/project/manager/restore_failure_migration.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/project/manager.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset project flow sample test folder split | runtime_15_asset_project_flow_sample_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/project/asset_flow_sample.rs` 已拆成 folder-backed `asset/tests/project/asset_flow_sample/{end_to_end,importers,fixtures,assertions}.rs` 四个 sample owner，父文件只保留共享导入和子模块挂载，行数从 975 降到 28。最大子文件 `asset/tests/project/asset_flow_sample/end_to_end.rs` 为 448 行，原有 1 个端到端测试保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_project_flow_sample.rs::runtime_15_asset_project_flow_sample_tests_are_folder_backed`，验证父/子模块挂载、代表性 end-to-end/importer/fixture/assertion helper 不回流、端到端测试数量和 asset project flow sample owner 行数预算，并同步状态输出期望行（包含 `asset/tests/project/asset_flow_sample.rs` 与 `asset/tests/project/asset_flow_sample/end_to_end.rs`）。验证：scoped rustfmt/static checks、端到端测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/project/asset_flow_sample.rs` 的 M3 folder-backed 子切片；asset 测试域当前已无超过 800 行测试文件，完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset material test folder split | runtime_15_asset_material_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/assets/material.rs` 已拆成 folder-backed `asset/tests/assets/material/{asset_serialization,owned_descriptor,override_validation,shader_readiness,management_records}.rs` 五个 material 行为 owner，父文件只保留共享 shader/material helper 和子模块挂载，行数从 1228 降到 69。最大子文件 `asset/tests/assets/material/owned_descriptor.rs` 为 361 行，23 个原有测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_tests.rs::runtime_15_asset_material_tests_are_folder_backed`，验证父/子模块挂载、代表性 serialization/descriptor/validation/shader/management moved guard 不回流、迁移测试数量和 asset material test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/assets/material.rs` 与 `asset/tests/assets/material/owned_descriptor.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/assets/material.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset glTF importer test folder split | runtime_15_asset_gltf_importer_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/assets/gltf_importer.rs` 已拆成 folder-backed `asset/tests/assets/gltf_importer/{basic_import,labeled_subassets,multi_primitive,external_inputs,vertex_channels,material_transforms,multi_scene}.rs` 七个 glTF importer 行为 owner，父文件只保留共享 importer/locator/assertion helper 和子模块挂载，行数从 989 降到 129。最大子文件 `asset/tests/assets/gltf_importer/labeled_subassets.rs` 为 259 行，13 个原有测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_gltf_importer.rs::runtime_15_asset_gltf_importer_tests_are_folder_backed`，验证父/子模块挂载、代表性 root import/subasset/material label/external input/vertex channel/material transform/multi-scene moved guard 不回流、迁移测试数量和 asset glTF importer test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/assets/gltf_importer.rs` 与 `asset/tests/assets/gltf_importer/labeled_subassets.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/assets/gltf_importer.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset glTF primitive fixture folder split | runtime_15_asset_gltf_primitive_fixtures_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/assets/gltf_primitive_fixtures.rs` 已拆成 folder-backed `asset/tests/assets/gltf_primitive_fixtures/{basic,vertex_channels,materials,animation}.rs` 四个 glTF primitive fixture owner，父文件只保留模块挂载和受限 `pub(super) use` 测试夹具导出，行数从 876 降到 11。最大子文件 `asset/tests/assets/gltf_primitive_fixtures/vertex_channels.rs` 为 307 行，8 个 fixture writer 全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_gltf_primitive_fixtures.rs::runtime_15_asset_gltf_primitive_fixtures_are_folder_backed`，验证父/子模块挂载、代表性 triangle/line/vertex/material/animation fixture writer 不回流、fixture writer 数量和 glTF primitive fixture owner 行数预算，并同步状态输出期望行（包含 `asset/tests/assets/gltf_primitive_fixtures.rs` 与 `asset/tests/assets/gltf_primitive_fixtures/vertex_channels.rs`）。验证：scoped rustfmt/static checks、fixture writer 数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/assets/gltf_primitive_fixtures.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset importer test folder split | runtime_15_asset_importer_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/assets/importer.rs` 已拆成 folder-backed `asset/tests/assets/importer/{structure,typed_toml_ui,builtin_data,registry_priority,registry_errors,shader_model,physics_animation}.rs` 七个 importer 行为 owner，父文件只保留共享 importer/typed-TOML/WGSL/virtual-geometry helper 和子模块挂载，行数从 857 降到 105。最大子文件 `asset/tests/assets/importer/typed_toml_ui.rs` 为 242 行，23 个原有测试全部保留在子模块，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_importer.rs::runtime_15_asset_importer_tests_are_folder_backed`，验证父/子模块挂载、代表性 structure/typed-TOML/builtin data/registry/error/shader-model/physics-animation moved guard 不回流、迁移测试数量和 asset importer test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/assets/importer.rs` 与 `asset/tests/assets/importer/typed_toml_ui.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；复跑带锁 focused guard 与 core-min cargo check 均在编译/测试前被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/assets/importer.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset scene test folder split | runtime_15_asset_scene_tests_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`asset/tests/assets/scene.rs` 已拆成 folder-backed `asset/tests/assets/scene/{foundation,camera,post_process,physics_animation,lights,script_bindings}.rs` 六个 scene 行为 owner，并保留既有 `asset/tests/assets/scene/management.rs` 管理概览 owner；父文件只保留 SceneAsset/组件类型共享导入和子模块挂载，行数从 841 降到 25。最大子文件 `asset/tests/assets/scene/management.rs` 为 413 行，本次迁出的 10 个原父文件测试全部保留在六个子模块，scene 测试族合计 13 个测试，全部低于 800 行预算。新增 `structure_convention/test_file_budget/asset_scene.rs::runtime_15_asset_scene_tests_are_folder_backed`，验证父/子模块挂载、代表性 foundation/camera/post-process/physics-animation/light/script moved guard 不回流、迁移测试数量和 asset scene test owner 行数预算，并同步状态输出期望行（包含 `asset/tests/assets/scene.rs` 与 `asset/tests/assets/scene/foundation.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描与 scoped `git diff --check` 通过；带锁 focused guard 与 core-min cargo check 均被当前工作区 Cargo.lock/Cargo.toml 不一致阻塞，不计通过。该记录关闭 `asset/tests/assets/scene.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 test file budget guard folder split | runtime_15_test_file_budget_guard_folder_split_static_passed_cargo_lock_blocked | 2026-06-23 | R4.1/M3：`structure_convention/test_file_budget.rs` 已拆出 folder-backed `structure_convention/test_file_budget/{runtime_diagnostics,rhi_command_list,rhi_device_contract,asset_gltf_importer,asset_gltf_primitive_fixtures,asset_importer,asset_project_flow_sample,asset_scene}.rs` 八个 guard owner，父文件只保留 core framework / UI v2 asset / UI shared core guard、子模块挂载、自守卫和共享 read helpers，当前行数为 701；八个子文件全部低于 800 行预算。新增 `structure_convention/test_file_budget.rs::runtime_15_test_file_budget_guard_is_folder_backed`，验证父/子模块挂载、moved guard 不回流、父子行数预算，并同步状态输出期望行（包含 `structure_convention/test_file_budget/runtime_diagnostics.rs`、`structure_convention/test_file_budget/rhi_device_contract.rs`、`structure_convention/test_file_budget/asset_gltf_primitive_fixtures.rs` 与 `structure_convention/test_file_budget/asset_project_flow_sample.rs`）。验证：scoped rustfmt/static checks 通过；带锁 Cargo 验证仍受当前工作区 Cargo.lock/Cargo.toml 不一致与 active Cargo lanes 影响，不计通过。该记录关闭 `structure_convention/test_file_budget.rs` 自身接近 1000 行的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 Runtime 07 performance hotspot guard folder split | runtime_15_runtime_07_performance_hotspots_guard_folder_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/performance_hotspots.rs` 已拆成 folder-backed `tests/runtime_absorption/performance_hotspots/{submit_context,submit_error_paths,hotspot_inventory,scene_project_splits,artifact_render_diagnostics_splits}.rs` 五个热点守卫 owner，并保留既有 `performance_hotspots/owner_budget.rs`；其中 submit-context 守卫的精确 owner 为 `tests/runtime_absorption/performance_hotspots/submit_context.rs`。父文件从 1394 行降到 12 行，最大子文件 `hotspot_inventory.rs` 为 425 行，8 个原父文件测试全部保留在子模块，所有 owner 低于 800 行预算。`performance_hotpath_source_inventory.py` 同步把 Runtime 07 test inventory 从 6 个文件更新为 11 个文件。新增 `performance_hotspots/owner_budget.rs::runtime_15_runtime_07_performance_hotspots_guard_is_folder_backed`，验证父/子模块挂载、代表性 hotspot/submission/split guard 不回流、owner 行数预算、Runtime 07 `expected_test_file_count = 11` 镜像和状态输出期望行。验证：scoped rustfmt --check、Python py_compile、standalone exact guard 1/1、Runtime 07 test inventory scan 与 docs/status 锚点扫描通过；带锁 focused guard 与 core-min cargo check 均 120s 超时无结果，未留下本切片 target-dir 进程，不计通过。该记录关闭 `tests/runtime_absorption/performance_hotspots.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 script VM test folder split | runtime_15_script_vm_tests_folder_split_static_passed_cargo_timeout_no_result | 2026-06-23 | R4.1/M3：`script/vm/tests.rs` 已拆成 folder-backed `script/vm/tests/{host_exports,bridge_host,reflection_docs,plugin_runtime,module_surface,support}.rs` 六个脚本 VM 测试/夹具 owner，并保留既有 `script/vm/tests/lifecycle_failures.rs`。父文件从 1456 行降到 41 行，最大子文件 `reflection_docs.rs` 为 314 行，32 个原测试（含 lifecycle failures）全部保留在子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/script_vm_tests.rs::runtime_15_script_vm_tests_are_folder_backed`，验证父/子模块挂载、代表性 host/bridge/reflection/plugin/module moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `script/vm/tests.rs`、`script/vm/tests/host_exports.rs`、`script/vm/tests/reflection_docs.rs`、`structure_convention/test_file_budget/script_vm_tests.rs` 与 `runtime_15_script_vm_tests_are_folder_backed`）。验证：scoped rustfmt --check 已通过；带锁 focused guard 与 core-min cargo check 均在冷构建阶段超时，未得到编译/测试结果，不计通过。该记录关闭 `script/vm/tests.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 script VM hot-reload coordinator test folder split | runtime_15_script_vm_hot_reload_coordinator_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`script/vm/runtime/hot_reload_coordinator.rs` 不再内嵌 hot-reload policy/lifecycle/poison recovery 测试，父文件只保留 slot table、load/hot-reload/unload/call/list 生产逻辑和 `#[cfg(test)] mod tests;` 挂载，从 770 行降到 282 行；5 个原 module-local 测试与 `PolicyRecordingBackend`、`LifecycleQueryBackend`、`CoordinatorSlotLifecycle` 等 fixture 迁入 `script/vm/runtime/hot_reload_coordinator/tests.rs`（407 行）。新增 `structure_convention/test_file_budget/script_vm_tests.rs::runtime_15_script_vm_hot_reload_coordinator_tests_are_folder_backed`，验证父/子模块挂载、5 个 moved test 不回流、测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `script/vm/runtime/hot_reload_coordinator.rs`、`script/vm/runtime/hot_reload_coordinator/tests.rs`）。验证：scoped rustfmt/static scans、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 hot-reload coordinator module-local test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与 full script VM Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 native live-host tests folder split | runtime_15_native_live_host_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`plugin/native_plugin_loader/native_plugin_live_host/tests.rs` 已拆成 folder-backed `plugin/native_plugin_loader/native_plugin_live_host/tests/{runtime_behavior,bridge_bindings,hot_reload_state}.rs` 三个新增 child owner，并保留既有 `hot_reload_failures.rs`、`hot_update_application.rs` 与 `registration_replay.rs`。父文件从 1297 行降到 541 行；新增子文件分别为 138、284、254 行，既有子文件也均低于 800 行预算。本次迁出的 18 个测试与父文件剩余 9 个测试合计保留原父文件 27 个测试。新增 `structure_convention/test_file_budget/native_live_host_tests.rs::runtime_15_native_live_host_tests_are_folder_backed`，验证父/子模块挂载、代表性 runtime descriptor、bridge binding、hot reload snapshot moved guard 不回流、迁移测试数量和 owner 行数预算，并同步状态输出期望行（包含 `plugin/native_plugin_loader/native_plugin_live_host/tests.rs`、`plugin/native_plugin_loader/native_plugin_live_host/tests/bridge_bindings.rs` 与 `plugin/native_plugin_loader/native_plugin_live_host/tests/hot_reload_state.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 3472、21776、47696、55676、63860、64772；`rustc` PIDs 30312、52648、57156）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 native live-host parent test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin native live-host Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 extension registry bridge test folder split | runtime_15_extension_registry_bridge_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/plugin_extensions/extension_registry_bridge.rs` 已拆成 folder-backed `tests/plugin_extensions/extension_registry_bridge/{basics,diagnostics,lifecycle}.rs` 三个 child owner。父文件从 oversized bridge 合约测试根收缩为 114 行，只保留共享 interface fixture、provider fixture、snapshot/owner-report helper 与子模块挂载；`basics.rs` 为 199 行，承接 duplicate export、absent/strong/weak bridge、generation parity、pin guard、debug diagnostics 和 finish context；`diagnostics.rs` 为 376 行，承接 interface status、snapshot、summary、editor matrix projection 与 owner filter；`lifecycle.rs` 为 268 行，承接 hot reload、owner reload、owner enable/disable/deactivate transition。20 个原测试全部保留在子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/extension_registry_bridge.rs::runtime_15_extension_registry_bridge_tests_are_folder_backed`，验证父/子模块挂载、代表性 moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `tests/plugin_extensions/extension_registry_bridge.rs`、`tests/plugin_extensions/extension_registry_bridge/basics.rs` 与 `tests/plugin_extensions/extension_registry_bridge/diagnostics.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 23868、34676、47696、48080、50600、64772；`rustc` PIDs 8940、42604、51932）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 extension registry bridge test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin extension Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 manifest contributions test folder split | runtime_15_manifest_contributions_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/plugin_extensions/manifest_contributions.rs` 已拆成 folder-backed `tests/plugin_extensions/manifest_contributions/{editor_only,net}.rs` 两个 child owner。父文件降到 640 行，只保留 runtime/catalog manifest 贡献、shared plugin manifest 读取 helper 与 child owner 挂载；`editor_only.rs` 为 130 行，承接 editor-only package targets/capabilities 与 explicit experimental maturity；`net.rs` 为 159 行，承接 net plugin TOML content-download dependency 与 layered optional feature catalog。13 个原测试全部保留，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/manifest_contributions.rs::runtime_15_manifest_contributions_tests_are_folder_backed`，验证父/子模块挂载、代表性 moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `tests/plugin_extensions/manifest_contributions.rs`、`tests/plugin_extensions/manifest_contributions/editor_only.rs` 与 `tests/plugin_extensions/manifest_contributions/net.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 34676、48080；`rustc` PID 56948）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 manifest contributions test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin extension Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 runtime plugin package manifest test folder split | runtime_15_runtime_plugin_package_manifest_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/plugin_extensions/runtime_plugin_package_manifest.rs` 已拆出 folder-backed `tests/plugin_extensions/runtime_plugin_package_manifest/feature_modules.rs` child owner。父文件降到 793 行，只保留 package identity/public metadata、bridge interface/method/dependency、packaging/capability/dependency/asset-importer/capability-status validation、shared runtime plugin fixture 与 child owner 挂载；`feature_modules.rs` 为 308 行，承接 optional feature、feature extension 与 package module identity/capability/system/target/duplicate-name validation。35 个原测试全部保留，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/runtime_plugin_package_manifest.rs::runtime_15_runtime_plugin_package_manifest_tests_are_folder_backed`，验证父/子模块挂载、representative optional-feature/feature-extension/module-validation moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `tests/plugin_extensions/runtime_plugin_package_manifest.rs`、`tests/plugin_extensions/runtime_plugin_package_manifest/feature_modules.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 2908、3092、8528、35188；`rustc` PIDs 6392、47316）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 runtime plugin package manifest test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin extension Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 export build plan test folder split | runtime_15_export_build_plan_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/plugin_extensions/export_build_plan.rs` 已拆出 folder-backed `tests/plugin_extensions/export_build_plan/catalog_projection.rs` child owner。父文件从 933 行降到 723 行，只保留 export profile projection、required-provider diagnostics、SourceTemplate/LibraryEmbed shared build-validation plan、profile feature matrix 与 shared helper；`catalog_projection.rs` 为 263 行，承接 builtin catalog completion、target-mode projection、rendering default feature providers、advanced render plugin links 与 SourceTemplate+NativeDynamic merge assertions。16 个原测试全部保留，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/export_build_plan.rs::runtime_15_export_build_plan_tests_are_folder_backed`，验证父/子模块挂载、catalog/render/native moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `tests/plugin_extensions/export_build_plan.rs`、`tests/plugin_extensions/export_build_plan/catalog_projection.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 28208、40420、60772、66220；`rustc` PIDs 13984、29744）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 export build plan test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin extension Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 export build plan platform test folder split | runtime_15_export_build_plan_platform_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/plugin_extensions/export_build_plan_platform.rs` 已拆出 folder-backed `tests/plugin_extensions/export_build_plan_platform/browser_hosts.rs` child owner。父文件从 819 行降到 780 行，只保留 target platform policy、native-dynamic rejection、headless/mobile/browser host scaffold、mobile/browser package manifest、signing/CDN release contracts、platform callback adapters、release adapter gates 与 mobile binding/resource glue；`browser_hosts.rs` 为 69 行，承接 WebGPU/WASM host WebAssembly exports 与 allowed asset origin gate assertions。10 个原测试全部保留，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/export_build_plan_platform.rs::runtime_15_export_build_plan_platform_tests_are_folder_backed`，验证父/子模块挂载、browser host moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `tests/plugin_extensions/export_build_plan_platform.rs`、`tests/plugin_extensions/export_build_plan_platform/browser_hosts.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 22876、28208、40420、61976、63160、66380、70136、70160；`rustc` PIDs 32424、42100、52768、68332）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 export build plan platform test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin extension Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 gameplay host test folder split | runtime_15_gameplay_host_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`script/vm/gameplay_host/tests.rs` 已拆成 folder-backed `script/vm/gameplay_host/tests/{spawn_transform,component_state,combat_lifecycle,property_animation}.rs` 四个玩法宿主测试 owner。父文件从 891 行降到 46 行，只保留共享导入、`mod` 挂载与 `assert_vec3_close` / `assert_quat_close` helper；最大子文件 `property_animation.rs` 为 289 行，9 个原测试全部保留在子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/script_vm_tests.rs::runtime_15_gameplay_host_tests_are_folder_backed`，验证父/子模块挂载、代表性 transform/spawn/component/combat/bindings/HUD moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `script/vm/gameplay_host/tests.rs`、`script/vm/gameplay_host/tests/spawn_transform.rs`、`script/vm/gameplay_host/tests/property_animation.rs` 与 `runtime_15_gameplay_host_tests_are_folder_backed`）。验证：scoped rustfmt/static checks、父子行数预算扫描、moved test scan、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `script/vm/gameplay_host/tests.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 shader prewarm manifest test folder split | runtime_15_shader_prewarm_manifest_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`bin/zircon_shader_prewarm/manifest.rs` 已将内联测试模块拆成 folder-backed `bin/zircon_shader_prewarm/manifest/tests.rs`。父文件从 810 行降到 672 行，只保留 `#[cfg(test)] mod tests;` 挂载；子文件 137 行，承接 `shader_prewarm_asset_root_manifest_reads_compound_zshader_package` 资产扫描预热 manifest 合约，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/shader_prewarm_manifest.rs::runtime_15_shader_prewarm_manifest_tests_are_folder_backed`，验证父/子模块挂载、内联测试不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `bin/zircon_shader_prewarm/manifest.rs`、`bin/zircon_shader_prewarm/manifest/tests.rs` 与 `runtime_15_shader_prewarm_manifest_tests_are_folder_backed`）。验证：scoped rustfmt/static checks、父子行数预算扫描、moved test scan、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 shader prewarm manifest 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 shader prewarm manifest guard child-owner split | runtime_15_shader_prewarm_manifest_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3 follow-up：`structure_convention/test_file_budget/shader_prewarm_manifest.rs` 在后续 Plan 08 staged-cache 守卫持续追加后达到 785 行，虽然尚未越过 800 行预算，但已同时承接 manifest folder-backed、geometry-source、custom shading-model、builtin template source、builtin fallback、product staged prewarm 与 asset revision export 七组独立断言。本切片把父守卫降为 219 行，只保留 child owner 挂载和新增 `runtime_15_shader_prewarm_manifest_guard_children_are_folder_backed`。七个原守卫分别迁入 `structure_convention/test_file_budget/shader_prewarm_manifest/manifest_contract.rs`（103 行）、`structure_convention/test_file_budget/shader_prewarm_manifest/geometry_source.rs`（98 行）、`structure_convention/test_file_budget/shader_prewarm_manifest/custom_shading_model.rs`（103 行）、`structure_convention/test_file_budget/shader_prewarm_manifest/builtin_template_source.rs`（130 行）、`structure_convention/test_file_budget/shader_prewarm_manifest/builtin_fallback.rs`（142 行）、`structure_convention/test_file_budget/shader_prewarm_manifest/product_staged_prewarm.rs`（118 行）和 `structure_convention/test_file_budget/shader_prewarm_manifest/asset_revision.rs`（97 行）。新增布局守卫锁定 child mount、moved guard 不回流、父/子 owner 行数预算，以及 Runtime 15/status/module/session docs 锚点。验证已通过：scoped rustfmt/static scans、父子行数预算扫描、moved guard parent scan、docs/status/session anchor scan、status/date expected-slice map scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo deferred，不计通过。 |
| M3 | Runtime 15 M3 scene ECS schedule test folder split | runtime_15_scene_ecs_schedule_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/ecs_schedule.rs` 已拆成 folder-backed `scene/tests/ecs_schedule/{resources_events,schedule_plan,render_extract,world_driver}.rs` 四个新增 schedule 测试 owner，并保留既有 `conflict_graph.rs`、`fixed_update.rs` 与 `parallel_executor.rs`。父文件从 1430 行降到 32 行，最大 owner 为既有 `conflict_graph.rs` 768 行，新增最大子文件 `render_extract.rs` 为 413 行；37 个原父文件测试全部迁入新增子模块，`ecs_schedule/` 测试族合计 57 个测试，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_ecs_schedule.rs::runtime_15_scene_ecs_schedule_tests_are_folder_backed`，验证父/子模块挂载、资源/事件、调度计划、render extract、WorldDriver moved guard 不回流、迁移测试数量、全族测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描和 docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/ecs_schedule.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene ECS systems test folder split | runtime_15_scene_ecs_systems_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/ecs_systems.rs` 已拆成 folder-backed `scene/tests/ecs_systems/{commands,state_params,events,run_window_filters,many_single_queries,removal_local}.rs` 六个 ECS systems 测试 owner。父文件从约 1000+ 行降到 53 行，只保留共享组件/资源/事件 fixture 和子模块挂载；最大子文件 `run_window_filters.rs` 为 330 行，`state_params.rs` 为 286 行，24 个原父文件测试全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_ecs_systems.rs::runtime_15_scene_ecs_systems_tests_are_folder_backed`，验证父/子模块挂载、命令队列、SystemState/ParamSet、事件、run-window 过滤、many/single query behavior、removed-components/local-state moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描和 docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/ecs_systems.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene ECS query test folder split | runtime_15_scene_ecs_query_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/ecs_query.rs` 已拆成 folder-backed `scene/tests/ecs_query/{read_items,mutation_access,fixed_ticks,iter_many,cached_queries}.rs` 五个 ECS query 测试 owner。父文件从 938 行降到 60 行，只保留共享 query fixture、错误 helper、cached component location helper 和子模块挂载；最大子文件 `cached_queries.rs` 为 555 行，19 个原父文件测试全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_ecs_query.rs::runtime_15_scene_ecs_query_tests_are_folder_backed`，验证父/子模块挂载、query data reads、mutation/access、fixed components/change ticks、iter-many、cached queries moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描和 docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/ecs_query.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene ECS query structure test folder split | runtime_15_scene_ecs_query_structure_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/ecs_query_structure.rs` 已拆成 folder-backed `scene/tests/ecs_query_structure/{query_state_layout,mutable_iterators,cached_iterators,cache_rebuild,archetype_access,combinations}.rs` 六个 ECS query 结构守卫 owner，其中状态锚显式覆盖 `scene/tests/ecs_query_structure/cache_rebuild.rs` 与 `scene/tests/ecs_query_structure/cached_iterators.rs`。父文件从 1041 行降到 33 行，只保留共享 path/source helper、QueryState 结构预算常量和子模块挂载；最大子文件 `cached_iterators.rs` 为 233 行，11 个原父文件结构守卫全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_ecs_query_structure.rs::runtime_15_scene_ecs_query_structure_tests_are_folder_backed`，验证父/子模块挂载、QueryState layout、mutable iterator、cached iterator、cache rebuild、archetype/access、combination moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描和 docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/ecs_query_structure.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene derived-state test folder split | runtime_15_scene_derived_state_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/derived_state.rs` 已拆成 folder-backed `scene/tests/derived_state/{spawn_paths,hierarchy_rebuild,projected_reads,runtime_freshness,hierarchy_behavior}.rs` 五个 derived-state 测试 owner，其中状态锚显式覆盖 `scene/tests/derived_state/projected_reads.rs` 与 `scene/tests/derived_state/runtime_freshness.rs`。父文件从 943 行降到 68 行，只保留共享导入、`LARGE_HIERARCHY_NODE_COUNT`、`detached_node_record(...)` / `pending_reparented_world(...)` / source helper 和子模块挂载；最大子文件 `projected_reads.rs` 为 291 行，23 个原父文件测试全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_derived_state.rs::runtime_15_scene_derived_state_tests_are_folder_backed`，验证父/子模块挂载、spawn path、hierarchy rebuild、projected reads、runtime freshness、hierarchy behavior moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/derived_state.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 dynamic scene session path-management test folder split | runtime_15_dynamic_scene_session_path_management_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/dynamic_scene_session/path_management.rs` 已拆成 folder-backed `scene/tests/dynamic_scene_session/path_management/{slot_mutations,mutation_previews,slot_copy,single_slot_import,single_slot_save,archive_merge}.rs` 六个 runtime session path-management 测试 owner，其中状态锚显式覆盖 `scene/tests/dynamic_scene_session/path_management/single_slot_import.rs` 与 `scene/tests/dynamic_scene_session/path_management/archive_merge.rs`。父文件从 972 行降到 14 行，只保留共享导入、session helper imports 和子模块挂载；最大子文件 `single_slot_import.rs` 为 249 行，19 个原父文件测试全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_dynamic_session.rs::runtime_15_dynamic_scene_session_path_management_tests_are_folder_backed`，验证父/子模块挂载、slot mutation、preview、copy、single-slot import/save、archive merge moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/dynamic_scene_session/path_management.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene component-structure test folder split | runtime_15_scene_component_structure_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/component_structure.rs` 已拆成 folder-backed `scene/tests/component_structure/{component_registry,component_storage_dispatch,component_storage_indexing,dynamic_scene_owner_tree,project_serialization,runtime_world_domains}.rs` 六个新增 component-structure 测试 owner，并保留既有 `scene/tests/component_structure/runtime_08_owner_tree.rs`。父文件从 842 行降到 9 行，最大 owner 为既有 `runtime_08_owner_tree.rs` 301 行，新增最大子文件 `runtime_world_domains.rs` 为 199 行；原父文件 20 个测试全部迁入新增子模块，component-structure 测试族合计 23 个测试，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_component_structure.rs::runtime_15_scene_component_structure_tests_are_folder_backed`，验证父/子模块挂载、registry/storage/dynamic-scene/project-serialization/runtime-domain moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（显式覆盖 `scene/tests/component_structure/component_storage_indexing.rs` 与 `scene/tests/component_structure/dynamic_scene_owner_tree.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/component_structure.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene ECS reflect foundation test folder split | runtime_15_scene_ecs_reflect_foundation_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/ecs_reflect/foundation.rs` 已拆成 folder-backed `scene/tests/ecs_reflect/foundation/{registry,value_conversion,address_routing,fixed_registry,fixed_lights_name,fixed_transform_active,fixed_render_physics}.rs` 七个 ECS reflect foundation 测试 owner。父文件从 1122 行降到 158 行，只保留共享导入、dummy adapter/helper 和子模块挂载；最大子文件 `fixed_render_physics.rs` 为 236 行，`value_conversion.rs` 为 191 行，20 个原父文件测试全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_ecs_reflect_foundation.rs::runtime_15_scene_ecs_reflect_foundation_tests_are_folder_backed`，验证父/子模块挂载、registry/value conversion/address routing/fixed component moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（显式覆盖 `scene/tests/ecs_reflect/foundation/value_conversion.rs` 与 `scene/tests/ecs_reflect/foundation/fixed_render_physics.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/ecs_reflect/foundation.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 dynamic scene root test folder split | runtime_15_dynamic_scene_root_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`scene/tests/dynamic_scene.rs` 已拆成 folder-backed `scene/tests/dynamic_scene/{scene_patch_document,archive_core,archive_mutation,archive_manifest,level_apply}.rs` 五个 dynamic-scene root 测试 owner。父文件从 1548 行降到 181 行，只保留共享导入、FrameCounter resource helper、cloud-layer descriptor helper 和子模块挂载；最大子文件 `archive_mutation.rs` 为 376 行，`archive_manifest.rs` 为 355 行，27 个原父文件测试全部迁入子模块，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/scene_dynamic_scene_root.rs::runtime_15_dynamic_scene_root_tests_are_folder_backed`，验证父/子模块挂载、scene patch/document、archive core/mutation/manifest、level apply moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（显式覆盖 `scene/tests/dynamic_scene/archive_manifest.rs` 与 `scene/tests/dynamic_scene/scene_patch_document.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/dynamic_scene.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files` 仍需避开活跃 render 会话处理 `scene/tests/render_extract.rs`，`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 test file budget root-layout child split | runtime_15_test_file_budget_root_layout_child_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`structure_convention/test_file_budget.rs` 在新增 scene query structure 守卫后接近 800 行预算，已将根布局自守卫 `runtime_15_test_file_budget_guard_is_folder_backed` 迁入 folder-backed `structure_convention/test_file_budget/root_layout.rs`。父文件从 793 行降到 428 行，只保留子模块挂载、core framework/UI v2/UI shared core 预算守卫和共享 read helper；`root_layout.rs` 为 367 行并继续验证所有 test-budget 子守卫 owner、moved guard 不回流、父子行数预算，以及本次 root-layout 状态锚。验证：scoped rustfmt/static checks、父子行数预算扫描、root guard moved scan、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `structure_convention/test_file_budget.rs` 的根布局减压子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 test file budget root-layout status scan child split | runtime_15_test_file_budget_root_layout_status_scan_child_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/structure_convention/test_file_budget/root_layout.rs` 从 739 行减压为 532 行，只保留 test-file-budget folder-backed parent/child layout 与 moved guard ownership 检查；新增 folder-backed 子 owner `tests/runtime_absorption/structure_convention/test_file_budget/root_layout/status_scan.rs`（149 行）承接所有 test-file-budget child owner 行数预算扫描、root-layout 历史状态锚扫描和本切片自守护。新增 `runtime_15_test_file_budget_root_layout_status_scan_is_child_owner`，锁定 `root_layout.rs` 子模块挂载、status/line-budget scan 不回流、所有 test-file-budget guard owner 低于 800 行预算，并要求 Runtime 15/status/module docs 镜像锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved root-layout status scan 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 root-layout status/line-budget scan 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 test-file-budget guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 test file budget root-layout folder-backed guard child split | runtime_15_test_file_budget_root_layout_folder_backed_guard_child_split_static_passed_cargo_timeout_no_result | 2026-06-24 | R4.1/M3：6 月 24 日已把 `runtime_15_test_file_budget_guard_is_folder_backed` 从 `structure_convention/test_file_budget/root_layout.rs` 迁入 `structure_convention/test_file_budget/root_layout/folder_backed.rs`，并新增 `root_layout/module_layout.rs::runtime_15_test_file_budget_root_layout_folder_backed_guard_is_child_owner` 锁定 root-layout 父模块挂载、旧 guard 不回流、`folder_backed.rs`/`module_layout.rs` 低于预算和 status/docs 镜像锚。Cargo 当时 focused run 超时无结果，不计 Cargo 通过；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 test-file-budget guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 test file budget root-layout folder-backed support child-owner split | runtime_15_test_file_budget_root_layout_folder_backed_support_child_owner_split_static_passed_cargo_deferred | 2026-06-25 | R4.1/M3：`structure_convention/test_file_budget/root_layout/folder_backed.rs` 从 558 行守卫体减压为 154 行父文件，只挂载 `structure_convention/test_file_budget/root_layout/folder_backed/assertions.rs`、`structure_convention/test_file_budget/root_layout/folder_backed/guard_names.rs` 与 `structure_convention/test_file_budget/root_layout/folder_backed/sources.rs` 三个 support child owner；`assertions.rs` 承接 test-budget 子 owner 断言组，`guard_names.rs` 承接 moved guard name 构造，`sources.rs` 承接批量 source reads，最大 child 为 354 行，父子 owner 均低于 400 行 focused 预算。新增 `runtime_15_test_file_budget_root_layout_folder_backed_support_child_owners_are_folder_backed`，锁定 support owner 挂载、父文件不再承接支撑结构、状态输出期望行与 Runtime 15/status/module/session docs 锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved support anchor 扫描、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。 |
| M3 | Runtime 15 M3 test file budget root-layout UI child split | runtime_15_test_file_budget_root_layout_ui_child_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`tests/runtime_absorption/structure_convention/test_file_budget/root_layout.rs` 从 780 行继续减压为 499 行，只保留非 UI test-budget parent/child layout 与 moved guard ownership 检查；新增 folder-backed 子 owner `tests/runtime_absorption/structure_convention/test_file_budget/root_layout/ui_children.rs`（225 行）承接 24 个 UI test-budget child guard 的父模块挂载、moved guard 不回流、子 owner 归属和 docs/status 锚点扫描。新增 `runtime_15_test_file_budget_root_layout_ui_child_scan_is_child_owner`，锁定 `root_layout.rs` 子模块挂载、UI guard ownership 不回流、`root_layout/status_scan.rs` 行数预算列表包含新子 owner，并要求 Runtime 15/status/module docs 镜像锚同步。验证：scoped rustfmt/static scans、父子行数预算扫描、moved UI guard ownership 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录关闭 root-layout UI child scan 的 M3 减压支撑切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 test-file-budget guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 historical oversized test roots closeout | runtime_15_historical_oversized_test_roots_closeout_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：历史 S6 超大测试根 `core/framework/tests.rs`、`ui/tests/v2_asset.rs` 与 `ui/tests/shared_core.rs` 已统一收口为 folder-backed parent/child 布局；当前父文件分别为 653、331、77 行，代表子 owner `core/framework/tests/framework_surfaces.rs`、`core/framework/tests/render_product_surface.rs`、`ui/tests/v2_asset/style_runtime.rs`、`ui/tests/v2_asset/file_cache.rs`、`ui/tests/shared_core/input_visibility.rs` 与 `ui/tests/shared_core/scroll_mutation.rs` 均低于 800 行，当前最大子 owner 为 771 行。新增 `structure_convention/test_file_budget/historical_oversized_roots.rs::runtime_15_historical_oversized_test_roots_are_folder_backed`，锁定三组历史根的子模块挂载、代表性 moved guard 不回流、父子行数预算、Runtime 15/status/module docs 锚点和 status-output expectation。验证：scoped rustfmt/static scans、历史父子行数预算扫描、moved guard ownership 扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按支撑切片节奏 deferred，不计 Cargo 通过。该记录只关闭 `core/framework/tests.rs`、`ui/tests/v2_asset.rs`、`ui/tests/shared_core.rs` 三个历史超大测试根的收口证明；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 test-file-budget guard sweep 仍 pending。 |
| M3 | Runtime 15 M3 no oversized test files global gate | runtime_15_no_oversized_test_files_global_gate_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3：新增 `structure_convention/test_file_budget/global_budget.rs::runtime_15_no_oversized_test_files` 作为全局测试文件预算闸口，递归扫描 `zircon_runtime/src` 下所有 Rust 测试文件（`tests/` 目录、`tests.rs` 与 `*_tests.rs`）并要求每个文件低于 `TEST_FILE_LINE_BUDGET = 800` 行。当前静态扫描无 `>= 800` 行测试文件，最高测试 owner 为 `tests/plugin_extensions/runtime_plugin_package_manifest.rs` 763 行；本守卫同时锁定 `test_file_budget/mod.rs` 挂载、Runtime 15 子计划、runtime index、review findings、structure convention、module-convention docs、session note 与 status-output expectations 均包含本切片锚。验证：scoped rustfmt/static line-budget scans、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `runtime_15_no_oversized_test_files` 结构闸；`module_convention_gate` 与全量 Runtime 15 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset test-budget guard child-owner split | runtime_15_asset_test_budget_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`structure_convention/test_file_budget/asset_tests.rs` 从 759 行减压为 161 行父模块，只保留 `pack`/`facade`/`project`/`material` 子 owner 挂载和自检；原 pack/facade/project zmeta/project manager/material 五个预算守卫迁入 `structure_convention/test_file_budget/asset_tests/{pack,facade,project,material}.rs`，代表路径包含 `structure_convention/test_file_budget/asset_tests/pack.rs` 与 `structure_convention/test_file_budget/asset_tests/project.rs`，最大 child `project.rs` 为 275 行，`root_layout.rs` 同步纳入四个新 child owner 与状态锚。验证：scoped rustfmt/static checks、父子行数预算扫描、moved guard scan、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `asset_tests.rs` 二级 child-owner 减压；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |

| M3 | Runtime 15 M3 UI asset test folder split | runtime_15_ui_asset_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/asset.rs` 已拆成 folder-backed `ui/tests/asset/{style_rule_ids,style_write_apis,loader_validation,document_compiler,fixture_migration,component_schema}.rs` 六个 UI asset 测试 owner。父文件从 1359 行降到 251 行，只保留共享 asset/template/style TOML fixture 和子模块挂载；32 个原父文件测试全部迁入子模块，最大子文件 `style_write_apis.rs` 为 261 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/ui_asset.rs::runtime_15_ui_asset_tests_are_folder_backed`，验证父/子模块挂载、style id/write API、loader validation、document compiler、fixture migration、component schema moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、父子行数预算扫描、docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/asset.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI asset MUI X web style test folder split | runtime_15_ui_asset_mui_web_mui_x_style_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/asset_mui_web_mui_x_style.rs` 已拆成 folder-backed `ui/tests/asset_mui_web_mui_x_style/{data_grid,tree_view,date_time_pickers,charts,agent_chat}.rs` 五个 MUI X web style 测试 owner。父文件从 1347 行降到 685 行，只保留 MUI X style/layout TOML fixture、模块挂载和 shared find/attr/class helper；原综合测试按组件族拆为 5 个子测试，最大子文件 `data_grid.rs` 为 221 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/ui_asset_mui_web_mui_x_style.rs::runtime_15_ui_asset_mui_web_mui_x_style_tests_are_folder_backed`，验证父/子模块挂载、DataGrid、TreeView、Date/Time Pickers、Charts/Gauge、Agent Chat moved guard 不回流、组件族覆盖数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、父子行数预算扫描、docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/asset_mui_web_mui_x_style.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI asset MUI web style test folder split | runtime_15_ui_asset_mui_web_style_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/asset_mui_web_style.rs` 已拆成 folder-backed `ui/tests/asset_mui_web_style/{state_icons,slots_native,feedback,surface,data_display}.rs` 五个 MUI web style 测试 owner。父文件从 1296 行降到 648 行，只保留 MUI web style TOML fixture、模块挂载和共享 attr/class assertion helper；9 个原父文件测试全部迁入子模块，最大子文件 `feedback.rs` 为 162 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/ui_asset_mui_web_style.rs::runtime_15_ui_asset_mui_web_style_tests_are_folder_backed`，验证父/子模块挂载、state/icon、slot/native、feedback、surface、data-display moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、父子行数预算扫描、docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/asset_mui_web_style.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |

| M3 | Runtime 15 M3 UI taffy layout pass test folder split | runtime_15_ui_taffy_layout_pass_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/taffy_layout_pass.rs` 已拆成 folder-backed `ui/tests/taffy_layout_pass/{routing_diagnostics,arrangement,linear_slots,fallback_policy,grid_slots}.rs` 五个 Taffy layout pass 测试 owner。父文件从 1230 行降到 168 行，只保留共享 layout/tree helper、selection/fallback assertion helper 和子模块挂载；35 个原父文件测试全部迁入子模块，最大子文件 `routing_diagnostics.rs` 为 347 行，全部低于 800 行预算。新增 `structure_convention/test_file_budget/ui_taffy_layout_pass.rs::runtime_15_ui_taffy_layout_pass_tests_are_folder_backed`，验证父/子模块挂载、routing diagnostics、native arrangement、linear/wrap slot、fallback policy、grid slot moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、父子行数预算扫描、docs/status 锚点扫描通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/taffy_layout_pass.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime window input pump test folder split | runtime_15_ui_runtime_window_input_pump_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/runtime_window_input_pump.rs` 已拆成 folder-backed parent + `ui/tests/runtime_window_input_pump/{lifecycle,pointer_routes,metrics_dirty}.rs` 三个 runtime window input pump 行为 owner。父文件从 882 行降到 184 行，只保留共享 window input pump fixture、dispatch/window metadata helper、popup/tooltip helper 和子模块挂载；14 个原父文件测试全部迁入子模块，子文件分别承接 6/4/4 个测试，最大子文件 `ui/tests/runtime_window_input_pump/lifecycle.rs` 为 328 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_runtime_window_input_pump.rs::runtime_15_ui_runtime_window_input_pump_tests_are_folder_backed`，验证父/子模块挂载、lifecycle、pointer route 与 metrics/dirty-domain moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/runtime_window_input_pump.rs`、`ui/tests/runtime_window_input_pump/lifecycle.rs` 与 `ui/tests/runtime_window_input_pump/pointer_routes.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/runtime_window_input_pump.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime window event ABI child folder split | runtime_15_ui_runtime_window_event_abi_children_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/runtime_ui_window_event_routes/abi.rs` 已拆成 folder-backed parent + `ui/tests/runtime_ui_window_event_routes/abi/{batch_adapter,pointer_window_routes,keyboard_gamepad_routes}.rs` 三个 runtime window event ABI 行为 owner。父文件从 1078 行降到 5 行，只保留 `use super::*` 和子模块挂载；13 个原父文件测试全部迁入子模块，子文件分别承接 5/5/3 个测试，最大子文件 `ui/tests/runtime_ui_window_event_routes/abi/pointer_window_routes.rs` 为 445 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_runtime_window_event_abi.rs::runtime_15_ui_runtime_window_event_abi_children_are_folder_backed`，验证父/子模块挂载、batch/adapter、pointer/window route 与 keyboard/gamepad route moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `ui/tests/runtime_ui_window_event_routes/abi.rs`、`ui/tests/runtime_ui_window_event_routes/abi/batch_adapter.rs` 与 `ui/tests/runtime_ui_window_event_routes/abi/pointer_window_routes.rs`）。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/runtime_ui_window_event_routes/abi.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI widget text input keyboard test folder split | runtime_15_ui_widget_text_input_keyboard_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/widget_text_input_keyboard.rs` 已拆成 folder-backed parent + `ui/tests/widget_text_input_keyboard/{basic_editing,selection_navigation,word_shortcuts,clipboard_newline,text_ime}.rs` 五个 text-input keyboard 行为 owner。父文件从 1362 行降到 318 行，只保留共享 dispatch/text/IME/surface fixture helper 和子模块挂载；52 个原父文件测试全部迁入子模块，子文件分别承接 7/19/8/10/8 个测试，最大子文件 `selection_navigation.rs` 为 323 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_widget_text_input_keyboard.rs::runtime_15_ui_widget_text_input_keyboard_tests_are_folder_backed`，验证父/子模块挂载、basic editing、selection/navigation、word shortcuts、clipboard/newline 与 text/IME moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/widget_text_input_keyboard.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI focus navigation test folder split | runtime_15_ui_focus_navigation_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/focus_navigation.rs` 已拆成 folder-backed parent + `ui/tests/focus_navigation/{focus_state,property_mutation,tab_directional,modal_popup}.rs` 四个 focus/navigation 行为 owner。父文件从 913 行降到 346 行，只保留共享 focus/modal/popup/navigation fixture helper 和子模块挂载；16 个原父文件测试全部迁入子模块，子文件分别承接 4/4/4/4 个测试，最大子文件 `modal_popup.rs` 为 222 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_focus_navigation.rs::runtime_15_ui_focus_navigation_tests_are_folder_backed`，验证父/子模块挂载、focus state、property mutation、tab/directional navigation 与 modal/popup focus moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/focus_navigation.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime input manager test folder split | runtime_15_ui_runtime_input_manager_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/runtime_input_manager.rs` 已拆成 folder-backed parent + `ui/tests/runtime_input_manager/{window_timer,route_order,route_matrix,touch_pointer}.rs` 四个 runtime input manager 行为 owner，显式锚点包含 `ui/tests/runtime_input_manager/route_matrix.rs` 与 `ui/tests/runtime_input_manager/touch_pointer.rs`。父文件从 1006 行降到 295 行，只保留共享 route matrix、double-click、popup、input metadata、pointer/touch/keyboard/popup event 与 window metadata helper 和子模块挂载；15 个原父文件测试全部迁入子模块，子文件分别承接 2/2/5/6 个测试，最大子文件 `touch_pointer.rs` 为 400 行，所有本轮 owner 低于 800 行。新增 `structure_convention/test_file_budget/ui_runtime_input_manager.rs::runtime_15_ui_runtime_input_manager_tests_are_folder_backed`，验证父/子模块挂载、window/timer、route order、route matrix、touch/multi-pointer moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/runtime_input_manager.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |
| M3 | Runtime 15 M3 UI runtime input ownership test folder split | runtime_15_ui_runtime_input_ownership_tests_folder_split_static_passed_cargo_deferred | 2026-06-23 | R4.1/M3：`ui/tests/runtime_input_ownership.rs` 已拆成 folder-backed parent + `ui/tests/runtime_input_ownership/input_method.rs`、`ui/tests/runtime_input_ownership/owner_validation.rs`、`ui/tests/runtime_input_ownership/high_precision_dispatch.rs`、`ui/tests/runtime_input_ownership/drag_drop.rs`、`ui/tests/runtime_input_ownership/popup_tooltip.rs` 与 `ui/tests/runtime_input_ownership/route_trace.rs` 六个 runtime input ownership 行为 owner。父文件从 1152 行降到 203 行，只保留共享 pointer capture、input metadata、keyboard/analog/pointer/popup/tooltip/drag-drop event 与 input-method fixture helper 和子模块挂载；16 个原父文件测试全部迁入子模块，子文件分别承接 4/3/2/2/2/3 个测试，最大子文件 `popup_tooltip.rs` 为 213 行，所有本轮 owner 低于 800 行。本轮保留既有 pointer-capture API 更新，测试继续通过 `set_pointer_capture_for_id` / `active_pointer_capture` / `pointer_capture_owner` 断言每 pointer capture。新增 `structure_convention/test_file_budget/ui_runtime_input_ownership.rs::runtime_15_ui_runtime_input_ownership_tests_are_folder_backed`，验证父/子模块挂载、input-method、owner validation、high-precision dispatch、drag/drop、popup/tooltip 与 route trace moved guard 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行。验证：scoped rustfmt/static checks、迁移测试数量扫描、父子行数预算扫描、docs/status 锚点扫描和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `ui/tests/runtime_input_ownership.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 dead-code sweep 仍 pending。 |

| M3 | Runtime 15 M3 picking test folder split | runtime_15_picking_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/picking/mod.rs` 已拆成 folder-backed `tests/picking/{rays,hits_and_hover,diagnostics,pipeline,pointer_events}.rs` 五个 picking 行为 owner。父文件降到 41 行，只保留共享 imports、子模块挂载和 `hit` / `pointer_location` / `event_labels` / `test_camera` fixture；20 个原父文件测试全部迁入子模块，所有 picking test owner 低于 800 行。新增 `structure_convention/test_file_budget/picking.rs::runtime_15_picking_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、迁移测试数量、owner 行数预算，并同步 `docs/zircon_runtime/core/framework/picking.md` 与 status-output expectations。验证：scoped rustfmt --check、父 moved-test scan、child test count scan、line-budget scan、docs/status/date anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 按实施切片节奏 deferred，不计通过。该记录关闭 `tests/picking/mod.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 picking Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset mesh test root split | runtime_15_asset_mesh_tests_root_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`asset/tests/assets/mesh.rs` 已从执行测试根收缩为 89 行父模块，只保留共享 mesh fixture/helper、既有 `morph_targets`/`normal_generation`/`tangent_generation` owner 挂载，以及新增 `document_roundtrip`、`validation`、`summaries`、`conversion_import` 四个 mesh 行为 owner。原父文件中的 19 个测试按 `.zmesh` roundtrip、严格 validation、summary/management 和 model/import conversion 分别迁入 `asset/tests/assets/mesh/document_roundtrip.rs`、`asset/tests/assets/mesh/validation.rs`、`asset/tests/assets/mesh/summaries.rs` 与 `asset/tests/assets/mesh/conversion_import.rs`，最大新增子文件为 `summaries.rs` 241 行，所有 mesh test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/asset_mesh.rs::runtime_15_asset_mesh_tests_are_folder_backed`，验证父/子模块挂载、19 个 moved test 不回流、迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `asset/tests/assets/mesh.rs`、`asset/tests/assets/mesh/document_roundtrip.rs`、`asset/tests/assets/mesh/conversion_import.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `asset/tests/assets/mesh.rs` 的 M3 root-test folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 asset test sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset project example vampire test folder split | runtime_15_asset_project_example_vampire_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`asset/tests/project/example_vampire.rs` 已从 783 行执行测试根收缩为 10 行父模块，只保留 `manifest_scene_imports` 与 `third_person_render_extract` 子 owner 挂载和共享 `vampire_root()` fixture。原有两个 vampire example 测试分别迁入 `asset/tests/project/example_vampire/manifest_scene_imports.rs` 与 `asset/tests/project/example_vampire/third_person_render_extract.rs`，覆盖 project manifest、scene/script import、shader/animation/asset import records，以及 third-person runtime render extract/static grass batch/camera post-process contract；最大子文件 `manifest_scene_imports.rs` 为 703 行，所有 example vampire test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/asset_project_example_vampire.rs::runtime_15_asset_project_example_vampire_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、两个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `asset/tests/project/example_vampire.rs`、`asset/tests/project/example_vampire/manifest_scene_imports.rs`、`asset/tests/project/example_vampire/third_person_render_extract.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `asset/tests/project/example_vampire.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 vampire example Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset artifact store test folder split | runtime_15_asset_artifact_store_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`asset/tests/assets/artifact_store.rs` 已从 791 行执行测试根收缩为 50 行父模块，只保留共享 artifact payload/asset-reference helper 和 `material_data`、`scene_script`、`scene_components`、`binary_payloads`、`library_assets` 五个 child owner 挂载。原父文件 15 个测试分别迁入 `asset/tests/assets/artifact_store/material_data.rs`、`asset/tests/assets/artifact_store/scene_script.rs`、`asset/tests/assets/artifact_store/scene_components.rs`、`asset/tests/assets/artifact_store/binary_payloads.rs` 与 `asset/tests/assets/artifact_store/library_assets.rs`，覆盖 material/data dynamic payload、scene script binding JSON、scene mesh/camera/physics references、bincode/binary payloads 和 physics/shader/animation/text-reject library artifact contracts；最大子文件 `scene_components.rs` 为 222 行，所有 artifact-store test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/asset_artifact_store.rs::runtime_15_asset_artifact_store_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、15 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `asset/tests/assets/artifact_store.rs`、`asset/tests/assets/artifact_store/binary_payloads.rs`、`asset/tests/assets/artifact_store/library_assets.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `asset/tests/assets/artifact_store.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 artifact store Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset UI test folder split | runtime_15_asset_ui_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`asset/tests/assets/ui.rs` 已从 742 行执行测试根收缩为 255 行父模块，只保留 UI TOML/ZUI fixtures、fixture importer helper、legacy component TOML helper 和 `wrappers`、`references`、`importer`、`project_manager`、`fixture_validation` 五个 child owner 挂载。原父文件 16 个测试分别迁入 `asset/tests/assets/ui/wrappers.rs`、`asset/tests/assets/ui/references.rs`、`asset/tests/assets/ui/importer.rs`、`asset/tests/assets/ui/project_manager.rs` 与 `asset/tests/assets/ui/fixture_validation.rs`，覆盖 wrapper/facade labels、direct dependency collection、direct importer decoding、ProjectManager scan/restore 和 legacy `.v2.ui.toml` component rejection；最大子文件 `project_manager.rs` 为 188 行，所有 UI asset test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/asset_ui.rs::runtime_15_asset_ui_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、16 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `asset/tests/assets/ui.rs`、`asset/tests/assets/ui/importer.rs`、`asset/tests/assets/ui/project_manager.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `asset/tests/assets/ui.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 UI asset Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 asset pipeline manager test folder split | runtime_15_asset_pipeline_manager_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`asset/tests/pipeline/manager.rs` 已从 733 行多职责执行测试根收缩为 31 行父模块，只保留共享 first-wave plugin fixture helper、imports 和 `project_open`、`model_import`、`watcher`、`resource_records`、`service_capabilities`、`resource_revisions`、`runtime_leases` 七个 child owner 挂载。原父文件 10 个测试分别迁入 `asset/tests/pipeline/manager/project_open.rs`、`asset/tests/pipeline/manager/model_import.rs`、`asset/tests/pipeline/manager/watcher.rs`、`asset/tests/pipeline/manager/resource_records.rs`、`asset/tests/pipeline/manager/service_capabilities.rs`、`asset/tests/pipeline/manager/resource_revisions.rs` 与 `asset/tests/pipeline/manager/runtime_leases.rs`，覆盖 project open/scan、virtual-geometry model import、watch reimport/sidecar suppression/single-event loop、ResourceManager record surface、importer capability service、resource revision update/isolation 和 runtime acquire/release lease lifecycle；最大子文件 `watcher.rs` 为 203 行，所有 pipeline manager test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/asset_pipeline_manager.rs::runtime_15_asset_pipeline_manager_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、10 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `asset/tests/pipeline/manager.rs`、`asset/tests/pipeline/manager/model_import.rs`、`asset/tests/pipeline/manager/watcher.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `asset/tests/pipeline/manager.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 asset pipeline manager Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene asset integration test folder split | runtime_15_scene_asset_integration_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`scene/tests/asset_scene.rs` 已从 751 行执行测试根收缩为 59 行父模块，只保留共享 scene asset reference、project IO source/section、authoring-token guard helper 和 `hierarchy_sources`、`mesh_bindings`、`product_fields` 三个 child owner 挂载。原父文件 9 个测试分别迁入 `scene/tests/asset_scene/mesh_bindings.rs`、`scene/tests/asset_scene/hierarchy_sources.rs` 与 `scene/tests/asset_scene/product_fields.rs`，覆盖 asset-bound mesh instantiation/render extract/primitive material binding、script-only and transform-only hierarchy preservation、asset-preserving source guards、physics/animation/camera/ambient/rect-light product field roundtrips；最大子文件 `product_fields.rs` 为 276 行，所有 scene asset integration test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/scene_asset_integration.rs::runtime_15_scene_asset_integration_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、9 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `scene/tests/asset_scene.rs`、`scene/tests/asset_scene/mesh_bindings.rs`、`scene/tests/asset_scene/product_fields.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/asset_scene.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 scene asset Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene world basics test folder split | runtime_15_scene_world_basics_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`scene/tests/world_basics.rs` 已从 717 行执行测试根收缩为 24 行父模块，只保留共享 imports 和 `world_state`、`render_extract`、`sprites` 三个 child owner 挂载。原父文件 15 个测试分别迁入 `scene/tests/world_basics/world_state.rs`、`scene/tests/world_basics/render_extract.rs` 与 `scene/tests/world_basics/sprites.rs`，覆盖 world bootstrap/spawn/hierarchy/project record roundtrip、transform/mesh phase/light render extraction、2D sprite extraction/layer filtering/mesh2d exclusion；最大子文件 `render_extract.rs` 为 349 行，所有 scene world-basics test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/scene_world_basics.rs::runtime_15_scene_world_basics_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、15 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `scene/tests/world_basics.rs`、`scene/tests/world_basics/render_extract.rs`、`scene/tests/world_basics/sprites.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/world_basics.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 scene world-basics Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 scene property paths test folder split | runtime_15_scene_property_paths_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`scene/tests/property_paths.rs` 已从 701 行混合测试根收缩为 11 行父模块，只保留共享 imports 和 `read_paths`、`runtime_mutation`、`write_validation` 三个 child owner 挂载。原父文件 18 个测试分别迁入 `scene/tests/property_paths/read_paths.rs`、`scene/tests/property_paths/runtime_mutation.rs` 与 `scene/tests/property_paths/write_validation.rs`，覆盖 property read/path source guards、entity/path runtime mutation behavior、write/value-conversion validation guards；最大子文件 `read_paths.rs` 为 286 行，所有 scene property-path test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/scene_property_paths.rs::runtime_15_scene_property_paths_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、18 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `scene/tests/property_paths.rs`、`scene/tests/property_paths/read_paths.rs`、`scene/tests/property_paths/write_validation.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `scene/tests/property_paths.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 scene property-path Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 input manager test folder split | runtime_15_input_manager_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`input/tests/input_manager.rs` 已从 731 行测试根收缩为 14 行父模块，只保留共享 imports 和 `frame_state`、`touch_gamepad`、`host_requests` 三个 child owner 挂载。原父文件 14 个测试分别迁入 `input/tests/input_manager/frame_state.rs`、`input/tests/input_manager/touch_gamepad.rs` 与 `input/tests/input_manager/host_requests.rs`，覆盖基础输入状态/帧清理/IME、touch/gamepad state 与 event-log harness、gamepad/cursor host request 队列；最大子文件 `touch_gamepad.rs` 为 347 行，所有 input manager test owner 均低于 800 行预算。新增 `structure_convention/test_file_budget/input_manager.rs::runtime_15_input_manager_tests_are_folder_backed`，验证父/子模块挂载、moved test 不回流、14 个迁移测试数量、owner 行数预算，并同步 Runtime 15/status/docs 锚点（含 `input/tests/input_manager.rs`、`input/tests/input_manager/frame_state.rs`、`input/tests/input_manager/touch_gamepad.rs`）。验证：scoped rustfmt/static scans、父 moved-test 扫描、子测试数量扫描、父子行数预算扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 `input/tests/input_manager.rs` 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 input Cargo sweep 仍 pending。 |
| M4 | Runtime 15 M4 scene component lighting/post-process owner split | runtime_15_scene_component_light_postprocess_owner_split_static_passed_cargo_deferred | 2026-06-24 | R1.4/M4：`scene/components/scene.rs` 从 711 行减压为 481 行，继续拥有 `NodeKind`、基础 active/transform/render-layer、`CameraComponent`、`MeshRenderer`、physics、animation、`SceneNode`/`NodeRecord` 与 serde default helpers；新增 folder-backed 子 owner `scene/components/scene/lighting.rs`（85 行）承接 `AmbientLight`、`DirectionalLight`、`PointLight`、`RectLight`、`SpotLight` 与 defaults，`scene/components/scene/post_process.rs`（84 行）承接 `PostProcessSettingsComponent`、`PostProcessVolumeComponent`、defaults、`global`/`local`/`with_weight` builders。父模块通过 `mod lighting;`、`mod post_process;` 与 `pub use self::...` 保持 `scene::components` public type paths 不变，不新增兼容 facade，也不改变 scene asset/render extract/post-process authoring semantics。新增 `structure_convention/production_file_budget/scene_components.rs::runtime_15_scene_components_light_postprocess_are_child_owners`，验证 light/post-process declarations 不回流父文件、三侧低于 800 行预算，并同步 Runtime 15/status/docs 锚点（含 `scene/components/scene.rs`、`scene/components/scene/lighting.rs`、`scene/components/scene/post_process.rs`）。验证：scoped rustfmt/static scans、父子行数预算扫描、moved owner 扫描、docs/status/session 锚点扫描、trailing-whitespace 扫描和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 scene component light/post-process owner 的 M4 减压子切片；完整 `large_file_ownership_gate`、`module_convention_gate` 与全量 scene Cargo sweep 仍 pending。 |
| M5 | Runtime 15 UI boundary runtime-host forbidden attribute literal cleanup | runtime_15_ui_boundary_runtime_host_literal_cleanup_static_passed_cargo_deferred | 2026-06-24 | F12/S10：`tests/ui_boundary/runtime_host.rs` 不再把 forbidden dead-code allow attribute 作为测试源直接字面量匹配，而是复用 `DEAD_CODE_ALLOW_ATTRIBUTE = concat!("#[allow(", "dead_code", ")]")`。既有 `runtime_ui_host_surface_splits_production_frame_from_test_support` 继续验证 `ui/mod.rs` 只暴露生产 `PublicRuntimeFrame` 与 test-only runtime UI support 分离，不改变生产 UI 行为；该清理只避免简单源码扫描把 UI boundary 守卫自身误报为 suppression 残留。同步 Runtime 15/status/docs 锚点（含 `tests/ui_boundary/runtime_host.rs`、`DEAD_CODE_ALLOW_ATTRIBUTE` 与 `runtime_ui_host_surface_splits_production_frame_from_test_support`）。验证：scoped rustfmt/static scans、literal scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `runtime_15_no_dead_code_suppression_in_production`、`module_convention_gate` 与 full Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F1 native host callback panic guard | runtime_15_native_host_callback_panic_guard_static_passed_cargo_deferred | 2026-06-27 | P0/F1 + E7：`plugin/native_plugin_loader/ffi_panic_guard.rs` 继续作为 native host FFI panic 边界 owner，保留 `catch_unwind(AssertUnwindSafe(call))`。`plugin/native_plugin_loader/host_api_adapter.rs` 的 9 个 public `ZrHostApiV3` callbacks 均先进入 `catch_native_host_api_panic` 并把 panic 映射为 `ZrStatusCode::Panic`；`plugin/native_plugin_loader/host_callbacks.rs` 的 4 个 private native host callbacks 均先进入 `catch_native_plugin_host_callback_panic` 并映射为 `ZIRCON_NATIVE_PLUGIN_STATUS_PANIC`。新增 `tests/runtime_absorption/code_review_findings/p0_robustness.rs::review_f1_native_host_callbacks_catch_unwind_before_crossing_ffi`，锁定 13/13 callback guard、native plugin loader docs、review findings、Runtime 15/index、structure convention、module-convention docs 与 status-output expectations；code-review findings guard 计数更新到 33。验证：scoped rustfmt/static scans、13/13 callback guard scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与 full Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 native plugin loader real fixture test folder split | runtime_15_native_plugin_loader_real_fixture_tests_folder_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/plugin_extensions/native_plugin_loader.rs` 已拆出 folder-backed `tests/plugin_extensions/native_plugin_loader/real_fixture.rs` child owner。父文件从 933 行减压为 470 行，只保留 native load-manifest discovery、package-id mismatch/dedup、editor-only discovery、feature extension package 与 split native package loading 测试；`real_fixture.rs` 为 564 行，承接真实 native dynamic fixture build/load、descriptor/entry/status-code coverage、unknown ABI rejection 与 `NativeAssetImporterHandler` data asset import coverage。11 个原测试全部保留，所有 owner 低于 800 行预算。新增 `structure_convention/test_file_budget/native_plugin_loader.rs::runtime_15_native_plugin_loader_real_fixture_tests_are_folder_backed`，验证父/子模块挂载、real fixture moved test 不回流、迁移测试数量、owner 行数预算，并同步状态输出期望行（包含 `tests/plugin_extensions/native_plugin_loader.rs`、`tests/plugin_extensions/native_plugin_loader/real_fixture.rs`）。验证：scoped rustfmt/static scans、迁移测试数量扫描、父子行数预算扫描、moved-test parent scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 19972、44764、49624、54336；`rustc` PIDs 29320、54940）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 native plugin loader real fixture test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 plugin extension/native loader Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 render shader template assembly guard WGSL contracts split | runtime_15_render_shader_template_assembly_guard_wgsl_contracts_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3 + R4.3：`structure_convention/production_file_budget/render_shader_template_assembly.rs` 不再把 Rust template assembly/cache guard 与 WGSL ABI contract guard 放在同一 845 行测试 owner 中，WGSL include/template/shading contract 断言迁入 `structure_convention/production_file_budget/render_shader_template_assembly/wgsl_contracts.rs`。父文件从 845 行减压为 610 行，继续验证 shader/template module wiring、assembler/include registry/material surface/pass specialization、mesh pipeline cache 和跨文档 Plan 08 anchors；`wgsl_contracts.rs` 为 258 行，验证 scene uniform、GPU scene transform/palette、surface interpolation、static/skinned geometry、Forward/GBuffer/Depth/Shadow/Velocity/TAA template 与 Standard PBR light-grid/shadow contracts。新增 `runtime_15_render_shader_template_wgsl_contracts_are_child_owner`，同步状态输出期望行。验证：scoped rustfmt/static scans、父子行数预算扫描、剩余 oversized-test scan、docs/status/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active（`cargo` PIDs 4820、8680、8844、20052、32036、33512、50960、56884；`rustc` PIDs 27412、53344、54144、62068）按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 render shader template assembly guard 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、Plan 08 focused Cargo/Naga/WGPU sweep 与 `module_convention_gate` 仍 pending。 |
| M3 | Runtime 15 M3 render shader template assembly guard support child-owner split | runtime_15_render_shader_template_assembly_guard_support_child_owner_split_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3 + R4.3 follow-up：`structure_convention/production_file_budget/render_shader_template_assembly.rs` 在 WGSL contract split 后又因继续承接批量 source reads、Rust/template/cache 断言和 Plan 08 文档锚点增长到 839 行；本切片把父文件减压为 168 行，只保留模块挂载、原 `runtime_15_render_shader_template_assembly_is_folder_backed` 测试入口和新增 support 布局守卫。新增 child owner `structure_convention/production_file_budget/render_shader_template_assembly/assembly_assertions.rs`（677 行）承接 Rust template/cache/shadow execution/line-budget 断言，`structure_convention/production_file_budget/render_shader_template_assembly/docs_anchors.rs`（133 行）承接 Plan 08/render index/shader docs/review/structure/session 锚点断言，`structure_convention/production_file_budget/render_shader_template_assembly/sources.rs`（98 行）承接批量 `read_runtime_src(...)`，既有 `depth_prepass_cache.rs`、`gbuffer_cache.rs` 与 `wgsl_contracts.rs` 继续独立。新增 `runtime_15_render_shader_template_assembly_support_children_are_folder_backed`，锁定 support child mount、moved support anchors 不回流、父/子 owner 均低于 800 行预算，并同步 Runtime 15/status/module/session docs 锚。验证已通过：scoped rustfmt/static scans、父子行数预算扫描、moved support anchor 扫描、docs/status/session anchor scan、status/date expected-slice map scan、trailing-whitespace scan 和 scoped `git diff --check`；Cargo deferred，不计 Cargo 通过。 |
| M3 | Runtime 15 M3 core runtime registration structure owner split | runtime_15_core_runtime_registration_structure_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`core/runtime/tests/registration/structure.rs` 已退役，registration 结构守卫改为 folder-backed `core/runtime/tests/registration/structure/mod.rs`，并拆成 `module_layout.rs`、`service_count_paths.rs`、`service_list_caches.rs`、`dependency_fast_paths.rs`、`duplicate_detection.rs`、`cleanup.rs` 与既有 `behavior_layout.rs`。新父模块只挂载 child owner 和共享 `registration_sources()` fixture；service-count child 继续锁定 registration hot-path、helper commit boundary 与 `lock_modules()` precheck/commit 顺序；service-list child 继续锁定 cached service/startup/shutdown list direct paths；dependency、duplicate 与 cleanup child 分别承接 dependency-name materialization、six-or-more duplicate helper 和 legacy cleanup 哨兵。新增 `structure_convention/test_file_budget/core_runtime_registration.rs::runtime_15_core_runtime_registration_structure_tests_are_folder_backed`，验证旧平铺文件不回流、父/子挂载、关键子 owner 锚点、所有 registration structure owner 低于 800 行，并同步 Runtime 15/status/docs 锚点（含 `core/runtime/tests/registration/structure/mod.rs`、`core/runtime/tests/registration/structure/service_count_paths.rs`、`core/runtime/tests/registration/structure/service_list_caches.rs`）。验证：scoped rustfmt/static scans、父子行数预算扫描、docs/status/date/session 锚点扫描、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 core runtime registration structure test owner 的 M3 folder-backed 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 core runtime registration Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 root entries guard child-owner split | runtime_15_root_entries_guard_child_owner_split_static_passed_cargo_deferred | 2026-06-24 | R4.1/M3：`tests/runtime_absorption/root_entries.rs` 从 618 行混合守卫压缩为 6 行结构入口，只挂载 `root_entries/core_spine.rs`、`root_entries/module_families.rs` 与 `root_entries/runtime_root.rs`。`core_spine.rs` 承接 Runtime 02 core root/spine 五条守卫，`runtime_root.rs` 承接 runtime crate/builtin root 三条守卫，`module_families.rs` 承接 Runtime 14 navigation/animation/module-family 五条守卫。`core_spine_root_generated.rs` 与 `core_spine_root_generated_boundary.py` 改为聚合读取三份 child owner 以保持 `root_entries guard tests 13` 与 `guard_test_anchor_count = 26` 语义；`module_family_boundary.py` 同步聚合读取 child owner，避免 Runtime 14 guard anchor 计数失真。新增 `structure_convention/test_file_budget/root_entries.rs::runtime_15_root_entries_guard_child_owners_are_folder_backed`，锁定父入口只挂载、moved guard 不回流、父/子低于 800 行预算、Rust/Python 审计聚合路径和 Runtime 15/status/docs 镜像锚。验证：scoped rustfmt/static scans、父子行数预算扫描、审计聚合路径扫描、docs/status/date/session 锚点扫描、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 root_entries guard 的 M3 child-owner 子切片；完整 `runtime_15_no_oversized_test_files`、`module_convention_gate` 与全量 Runtime 02/14 guard Cargo sweep 仍 pending。 |
| M5 | Runtime 15 M5 production dead-code suppression global gate | runtime_15_production_dead_code_suppression_global_gate_static_passed_cargo_deferred | 2026-06-24 | F12/M5：新增 `structure_convention/runtime_dead_code.rs::runtime_15_production_sources_do_not_allow_dead_code_suppression` 作为生产 dead-code suppression 全局闸口，递归扫描 `zircon_runtime/src` 下非 `tests/`、非 `tests.rs`、非 `*_tests.rs` 的生产 Rust 源文件，验证 `DEAD_CODE_ALLOW_ATTRIBUTE` 不再出现在生产源码中。该守卫复用常量拼装的 forbidden attribute 字符串，避免测试源码自身被简单 literal scan 误报；同时锁定 Runtime 15 子计划、runtime index、review findings、structure convention、module-convention docs、session note 与 status-output expectations 都包含本切片锚。验证：生产源码 dead-code suppression 扫描、scoped rustfmt/static scans、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。该记录关闭 M5/F12 的生产源码全局 suppression 闸口；完整 `module_convention_gate` 与全量 Runtime 15 Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F12 UI text edit-state dead-code suppression cleanup | runtime_15_ui_text_edit_state_dead_code_suppression_cleanup_static_passed_cargo_deferred | 2026-06-27 | F12/M5：`ui/text/mod.rs` 已删除遗漏的 `#[cfg_attr(not(test), allow(dead_code))]`，保留 `mod edit_state;` 与 `pub(crate) use edit_state::apply_text_edit_action;` 作为生产文本编辑入口。`ui/text/edit_state.rs` 当前由 TextInput reducer、editable text input、keyboard clipboard 与 text pointer 生产路径读取，因此本切片不把 edit-state owner 降为 test-only，而是让 live consumer 保持可见并新增 `runtime_15_ui_text_edit_state_dead_code_suppression_cleanup`。全局 `runtime_15_production_sources_do_not_allow_dead_code_suppression` 同步从 exact `#[allow(dead_code)]` 字符串扩展为按属性行扫描 `allow(dead_code)`，覆盖 `cfg_attr(not(test), allow(dead_code))` 这类漏网形态。验证：scoped rustfmt/static scans、生产源码 dead-code suppression 扫描、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。完整 `module_convention_gate` 与全量 Runtime 15 Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F12 dead-code review status sync | runtime_15_f12_dead_code_review_status_sync_static_passed_cargo_deferred | 2026-06-28 | F12/M5：新增 `tests/runtime_absorption/code_review_findings/f12_dead_code.rs::review_f12_runtime_production_dead_code_suppression_is_globally_gated`，把已落地的生产源码全局闸口同步到评审顶层行和 Runtime 15 状态输出。守卫递归扫描 `zircon_runtime/src` 生产 Rust 源码并确认 `allow(dead_code)` 属性零命中，同时锁定 `engine-code-review-findings-2026-06.md` 的 F12 行不再把历史 `全 crate 200 命中` 当作当前 runtime 状态，且保留 editor `template_runtime/component_adapter/registry.rs` 残项归 Editor UI 10。同步 status-output expectations、runtime index、structure convention、module-convention docs 与 session note。验证：scoped rustfmt/static scans、生产源码 dead-code suppression scan、focused review guard、code-review folder guard、status-output 直接测试和 scoped `git diff --check`；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。完整 `module_convention_gate` 与全量 Runtime 15 Cargo sweep 仍 pending。 |
| M5 | Runtime 15 F12 dead-code runtime/editor boundary status guard | runtime_15_f12_dead_code_runtime_editor_boundary_status_static_passed_cargo_deferred | 2026-06-28 | F12/M5：在生产源码全局闸口和 F12 review status sync 之后，本切片只锁定顶层评审行的边界状态，不改 runtime/editor production。`tests/runtime_absorption/code_review_findings/f12_dead_code.rs::review_f12_runtime_production_dead_code_suppression_is_globally_gated` 现在要求 F12 行同时包含本状态锚、`runtime_15_production_sources_do_not_allow_dead_code_suppression` 与 `Editor UI 10`，并且以 `Runtime 15 + Editor UI 10 + convention` 结尾；这表示 runtime production `allow(dead_code)` 已零命中，但 editor `template_runtime/component_adapter/registry.rs` 残项仍委派给 Editor UI 10，不把 F12 误标为 runtime 全量 review closed。同步 review findings、runtime index、structure convention、module-convention docs、session note 与 status-output expectations；Cargo 按本切片节奏 deferred，不计通过。完整 `module_convention_gate` 与全量 Runtime 15 Cargo sweep 仍 pending。 |

| M3 | Runtime 15 M3 graphics dead-code guard forbidden attribute literal cleanup | runtime_15_graphics_dead_code_guard_literal_cleanup_static_passed_cargo_deferred | 2026-06-27 | R4.1/M3 + F12 支撑：`structure_convention/graphics_dead_code/mod.rs` 新增 `DEAD_CODE_ALLOW_ATTRIBUTE = concat!("#[allow(", "dead_code", ")]")` 常量，`graphics_dead_code/{backend_owners,gpu_resource_owners,renderer_output_accessors,resource_streamer_cleanup}.rs` 不再直接写入 forbidden attribute literal，而是统一使用该常量检查对应生产源码。新增 `runtime_15_graphics_dead_code_guard_forbidden_attribute_literal_is_constant_backed`，锁定父常量、四个 child owner 不嵌入直接字面量、状态输出期望和跨文档锚点；该切片不改变任何 graphics production owner 或 render behavior，只为后续全 crate dead-code suppression 扫描去除测试守卫噪声。验证：scoped rustfmt/static scans、literal scan、docs/status/date/session anchor scan、trailing-whitespace scan 和 scoped `git diff --check` 通过；Cargo 因外部 cargo/rustc 通道 active 按 Runtime 15 实施切片节奏 deferred，不计通过。 |
| M3 | Runtime 15 M3 module convention gate output contract | runtime_15_module_convention_gate_output_contract_static_passed_cargo_deferred | 2026-06-27 | R4.3/M3：`.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_runtime_structure.py` 现在输出 `module_convention_gate` 聚合字段，并由 `runtime_structure_audits/module_convention_gate.py` 汇总 `module_classification`、`large_file_ownership_gate`、`runtime_naming_boundary`、`hard_cutover_migration_smells` 与 `non_network_server_references`，显式提供计划要求的 `m1_gate_status`、`classification_counts`、`migration_debt_count` 和 `exempt`。新增 `module_convention_gate_markdown.py` 与 `structure_convention/module_convention_gate.rs::runtime_15_module_convention_gate_output_contract_is_backed_by_structure_audit`，锁定 JSON/Markdown 输出契约和跨文档状态锚；该切片不声明完整结构债清零，当前 `module_convention_gate` 仍可报告 migration debt，后续 M1/M2/M4 切片继续消债。验证按本轮静态检查执行，Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。 |
| M3 | Runtime 15 M3 module convention non-render debt guard | runtime_15_module_convention_non_render_debt_guard_static_passed_cargo_deferred | 2026-06-27 | R4.3/M3 follow-up：`module_convention_gate.py` 现在把聚合后的 `migration_debt` 分区为 `render_scoped_migration_debt` 与 `non_render_migration_debt`，并输出对应 count；当前 render-scoped whitelist 只保留尚可能由 runtime graphics owner 关闭的通用 render 前缀，Hybrid GI 专用 debt bucket 已退役，当前审计为 render-scoped migration debt 0、non-render migration debt 0。`module_convention_gate_markdown.py` 同步显示 render-scoped / non-render debt count，新增 `structure_convention/module_convention_gate.rs::runtime_15_module_convention_gate_reports_non_render_debt_boundary` 锁定字段、白名单和跨文档状态锚。验证：scoped rustfmt/Python compile/audit JSON/static anchor/diff-check；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。当前 `module_convention_gate classified-and-clear`，全量 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 render-scoped migration debt handoff gate | runtime_15_render_scoped_migration_debt_handoff_static_passed_cargo_deferred | 2026-06-27 | R4.3/M3 follow-up：在 `module_convention_gate` 已拆分 render-scoped / non-render debt 后，本切片把当前审计事实写入状态闭环：render-scoped migration debt 0、non-render migration debt 0，`module_convention_gate classified-and-clear`。新增 `structure_convention/module_convention_gate.rs::runtime_15_render_scoped_migration_debt_handoff_is_status_locked`，锁定 Runtime 15 子计划、runtime index、结构规范、review findings、module-convention docs 与 session note 都保留该交接状态。Hybrid GI render plugin hard-cutover wording 已由后续 extract scene-source 切片关闭；完整全量 Cargo sweep 仍 pending。 |
| M3 | Runtime 15 M3 hard-cutover allowed Hyper policy risk cleanup | runtime_15_hard_cutover_allowed_hyper_policy_risk_cleanup_static_passed_cargo_deferred | 2026-06-27 | R4.3/M3 follow-up：`hard_cutover_migration_smells.py` 不再把已允许的 `external-hyper-http1-client-policy` 第三方 Hyper HTTP/1 `legacy` API path 计入 generic legacy risk；该 path 仍由 `zircon_plugins/net/features/http/runtime/src/backend/http1_client_policy.rs` 和 `runtime_15_net_http_hyper_http1_client_policy_is_isolated` 隔离，`legacy_reference_count=1`、`classification_counts.external-hyper-http1-client-policy=1` 保持可见，`hard_cutover_migration_debt_count=0`、`risk_count=0`、`risks=[]`。新增 `structure_convention/module_convention_gate.rs::runtime_15_hard_cutover_allowed_hyper_policy_does_not_report_risk`，锁定审计逻辑、module-convention risk aggregation 和跨文档状态锚。验证：scoped Python compile/audit JSON/rustfmt/static anchor/diff-check；Cargo 按 Runtime 15 implementation-slice cadence deferred，不计通过。 |
| F8 | Runtime 15 F8 RuntimePluginDescriptor status mirror cleanup | runtime_15_runtime_plugin_descriptor_status_mirror_cleanup_static_passed_cargo_deferred | 2026-06-27 | E3/F8 follow-up：实现层已由 `runtime_plugin_descriptor_public_field_convergence_coremin_check_passed` 与 `runtime_plugin_descriptor_public_constructor_retired_coremin_check_passed` 完成 RuntimePluginDescriptor private fields 15/15、15 个访问器、RuntimePluginDescriptor public-field convergence complete 与 RuntimePluginDescriptor::new retired；本切片把 Runtime 06/index、review findings、结构规范、package-manifest docs 与 status-output 期望中的完成态镜像同步，并新增 `code_review_findings/f8_api_convergence.rs::review_f8_runtime_plugin_descriptor_status_mirrors_do_not_claim_public_field_pending`，禁止优先级文档继续保留旧 pending 状态。该切片不改生产 runtime/plugin descriptor 行为；Cargo 因外部 cargo/rustc 通道 active deferred，不计通过。 |
| M2 | Runtime 15 M2 Net HTTP backend Hyper HTTP/1 client policy hard cutover | runtime_15_net_http_hyper_http1_client_policy_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + hard-cutover：`zircon_plugins/net/features/http/runtime/src/backend/client.rs` 不再直接导入 `hyper_util::client::legacy::Client`，只通过 `http1_client_policy::plain_http_client()` 取得 plain HTTP client；新增 `zircon_plugins/net/features/http/runtime/src/backend/http1_client_policy.rs` 作为 Net HTTP backend dependency owner，集中拥有第三方 Hyper HTTP/1 legacy API path 与 `PlainHttpClient` type alias。`hard_cutover_migration_smells.py` 将该 owner 分类为允许的 `external-hyper-http1-client-policy`，`legacy-net-hyper-client-api-debt` 不再计入迁移债；新增 `naming_boundary/runtime_15_m2/net.rs::runtime_15_net_http_hyper_http1_client_policy_is_isolated` 锁定 client/policy/audit/docs/status 锚点。该切片不改变 HTTP retry、timeout、plain HTTP Hyper path、HTTPS reqwest/rustls/pinning 行为，不新增 compat alias/re-export；Cargo 因外部 cargo/rustc 通道 active deferred，完整 `module_convention_gate` 与 full Net HTTP Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 Hub message raw text policy hard cutover | runtime_15_hub_message_raw_text_policy_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + hard-cutover：`zircon_hub/src/state/hub_message/message.rs` 已把 unstructured message branch 从 generic legacy 命名硬切为 `RawText(String)` 与 `HubMessage::raw_text(...)`，反序列化旧字符串 wire payload 由 `ArchivedRawText(String)` 表达 archived raw-text policy；`zircon_hub/src/tauri_app/runtime_state/build_actions.rs` 等 Hub 调用方只消费 `HubMessage::raw_text`，`zircon_hub/src` 下旧 `legacy` 词汇、`HubMessage::legacy`、`Self::Legacy` 与 `HubMessageRepr::Legacy` 均已清零。新增 `naming_boundary/runtime_15_m2/hub.rs::runtime_15_hub_message_raw_text_policy_uses_current_names` 锁定 Hub source、hard-cutover audit、docs/status/date anchors。`hard_cutover_migration_smells` 实测 `legacy-hub-message-archived-text-debt` 清零，`hard_cutover_migration_debt_count` 从 4 降至 3；Cargo 因外部 cargo/rustc 通道 active deferred，完整 `module_convention_gate` 与 full Hub Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 editor Workbench archived fixture naming hard cutover | runtime_15_editor_workbench_archived_fixture_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + hard-cutover：editor retained-host fixture debt 已从 generic legacy/deprecated naming 硬切为 archived/current owner language。`zircon_editor/src/ui/retained_host/host_contract/paint_workbench_renderer/legacy.rs` 已退役为 `zircon_editor/src/ui/retained_host/host_contract/paint_workbench_renderer/host_window.rs`，入口改为 `draw_host_workbench_window(...)` / `draw_host_workbench_window_profiled(...)`；table-row fallback parser 改为 `split_archived_table_text(...)`；Particle/Icon extension fixture row、ZUI authored assets、builtin template bindings、preview action list 与 feedback dispatch 均改为 `ArchivedRow` / `archived_table_row` / `icon-archive`。新增 `runtime_15_editor_workbench_archived_fixtures_use_current_names` 锁定旧 module/function/route/control id/text 不回流。`hard_cutover_migration_smells` 实测 `legacy-editor-ui-fixture-debt` 清零，`hard_cutover_migration_debt_count` 从 3 降至 2；Cargo 因外部 cargo/rustc 通道 active deferred，完整 `module_convention_gate` 与 full editor retained-host Cargo sweep 仍 pending。 |
| M2 | Runtime 15 M2 scene render layer schema-v1 mask naming hard cutover | runtime_15_scene_render_layer_schema_v1_mask_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render-scoped debt follow-up：`RenderLayerSet` 新增 `from_scene_schema_v1_mask(...)`、`to_scene_schema_v1_mask_lossy(...)` 与 `intersects_scene_schema_v1_mask(...)`；`scene/world/render.rs`、`scene/world/render/lights.rs`、`scene/world/render_particles.rs`、`scene/world/render_post_process.rs` 与 `scene/world/render_visibility.rs` 已把 scene schema v1 的 `u32` layer mask 显式投影到该 API，scene render caller 不再使用旧 mask helper 或 generic legacy naming。新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_scene_render_layer_schema_v1_masks_use_versioned_names` 锁定 API、docs、status-output 与 date/status map 锚点；runtime naming audit 中 `legacy-scene-schema-render-debt` 已清零，render-scoped migration debt 从 4 降至 3。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics/hybrid-GI render debt 仍 pending。 |
| M2 | Runtime 15 M2 render shader definition bare-flag naming hard cutover | runtime_15_render_shader_definition_bare_flag_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：`core/framework/render/shader/definition_value.rs` 的私有 untagged serde 分支从 `LegacyFlag(String)` 硬切为 `BareFlag(String)`，表达裸字符串 shader definition flag 输入，而不是 runtime 内部 legacy owner。该改动不改变 `RenderShaderDefinitionValue` tagged `bool`/`int`/`uint` schema、不改变 `.zshader`/material import 行为，也不新增 compat alias/re-export。新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_render_shader_definition_uses_bare_flag_names`，锁定 `BareFlag`、旧 `LegacyFlag` 不回流，以及 Runtime 15、runtime index、结构规范、review findings、render-assets/zmeta docs 与 status-output expectations。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics render naming debt 仍 pending。 |
| M2 | Runtime 15 M2 GPU model embedded primitive naming hard cutover | runtime_15_gpu_model_embedded_primitive_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：`graphics/scene/resources/gpu_model/gpu_model_resource_from_asset.rs` 的 inline regression helper 和 test name 已从 generic legacy primitive/payload wording 硬切为 `embedded_primitive` / embedded payload，表达模型根内嵌 primitive 在 mesh reference 未解析时继续作为 fallback payload。该切片不改变 `model_primitives_preferring_mesh_assets(...)` 的加载优先级、不改变 `MeshAsset::to_model_primitive()` conversion、不新增 compat alias/re-export；新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_gpu_model_embedded_primitive_uses_current_names` 锁定新命名、Runtime 15/status/date maps、runtime index、结构规范、review findings、module-convention docs 与 render-assets docs。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics render naming debt 仍 pending。 |
| M2 | Runtime 15 M2 frame extract snapshot adapter naming hard cutover | runtime_15_frame_extract_snapshot_adapter_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：`core/framework/render/frame_extract.rs` 的 `RenderFrameExtract::from_snapshot(...)` 注释从 generic legacy viewport packet wording 硬切为 `scene viewport snapshot packet`，明确该路径是 preview/roundtrip/synthetic validation 的 snapshot adapter，而不是 production render-frame extraction owner。该切片不改变 `SceneViewportRenderPacket` 输入、不改变 `RenderSceneSnapshot` 转换、不新增 compat alias/re-export；新增 `naming_boundary/runtime_15_m2/core_scene.rs::runtime_15_frame_extract_snapshot_adapter_uses_current_names` 锁定新命名、旧 `legacy viewport packet` 不回流，以及 Runtime 15/status/date maps、runtime index、结构规范、review findings、module-convention docs 与 scene render-extract docs。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics render naming debt 仍 pending。 |
| M2 | Runtime 15 M2 core framework render fixture naming hard cutover | runtime_15_core_framework_render_fixture_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：core framework render fixtures 已从 generic legacy wording 硬切到 current names：`core/framework/render/core_pipeline/render_queue.rs` 使用 `authored_queue_offsets_are_clamped_to_material_window`，`core/framework/render/post_process/effect_stack_settings.rs` 使用 `without_retired_fields`，`core/framework/render/relevance.rs` 使用 `scene_schema_v1_mask_width`，`core/framework/render/light/readiness.rs` 与 `core/framework/render/scene_extract.rs` 的 test fixture mask wrapper 改为 `RenderLayerSet::from_scene_schema_v1_mask(...)`。该切片不改变 render queue clamp、effect-stack enablement、primitive relevance、light readiness 或 scene extract 行为；新增 `naming_boundary/runtime_15_m2/core_framework.rs::runtime_15_core_framework_render_fixtures_use_current_names` 锁定新命名、Runtime 15/status/date maps、runtime index、结构规范、review findings、module-convention docs 与 common render API docs。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics render naming debt 仍 pending。 |
| M2 | Runtime 15 M2 render feature fallback capability naming hard cutover | runtime_15_render_feature_fallback_capability_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：`graphics/runtime/render_framework/submit_frame_extract/build_frame_submission_context/resolve_enabled_features.rs` 与 `graphics/scene/scene_renderer/core/runtime_features/runtime_features_from_pipeline.rs` 的 no-capability fixture IDs 从 `legacy-*`/`legacy.*` 硬切为 `fallback-*`/`fallback.*`，覆盖 `fallback-virtual-geometry-without-capability`、Hybrid GI、`without-capability` 和 `without-submission-capability` 两组诊断 ID。该切片不改变 capability metadata 判定、advanced runtime flags、pipeline compile 或 submit 行为，也不新增 compat alias/re-export；新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_render_feature_fallback_capability_fixtures_use_current_names` 锁定新 ID、旧 ID 不回流、Runtime 15/status/date maps、runtime index、结构规范、review findings、module-convention docs 与 render-product-submit docs。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics render naming debt 仍 pending。 |
| M2 | Runtime 15 M2 render material stale texture fixture naming hard cutover | runtime_15_render_material_stale_texture_fixture_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：`graphics/scene/render_product_streamer_tests/material_runtime.rs` 的 shader standard alias fixture 已从 legacy texture wording 硬切为 stale schema-v1 texture wording：`legacy_texture_id` 改为 `stale_texture_id`，`legacy-base.png` / `missing-legacy-base.png` 改为 `stale-base.png` / `missing-stale-base.png`，测试名改为 `render_product_streamer_shader_standard_alias_shadows_unresolved_stale_texture`。该切片不改变 material descriptor promotion、shader texture alias readiness、runtime material preparation 或 dependency rebuild 行为，也不新增 compat alias/re-export；新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_render_material_stale_texture_fixtures_use_current_names` 锁定新命名、Runtime 15/status/date maps、runtime index、结构规范、review findings、render-assets/zmeta docs 与 module-convention docs。Cargo 因外部 cargo/rustc 通道 active deferred，完整 graphics render naming debt 仍 pending。 |
| M2 | Runtime 15 M2 render graph fallback fixture naming hard cutover | runtime_15_render_graph_fallback_fixture_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：最后一组 hard-cutover runtime graphics fixture wording 已硬切：`graphics/scene/scene_renderer/core/scene_renderer_core/advanced_plugin_resources/scene_renderer_advanced_plugin_resources.rs` 使用 `fallback-virtual-geometry-without-resource-capability`，`graphics/scene/scene_renderer/graph_execution/render_graph_execution_record/compute_workload.rs` 使用 `unexpected-compute` / `unexpected.executor` / `unexpected-pipeline`。该切片不改变 advanced plugin resource capability scanning、compute workload audit、unexpected dispatch counting 或 graph execution behavior，也不新增 compat alias/re-export；新增 `naming_boundary/runtime_15_m2/graphics.rs::runtime_15_render_graph_fallback_fixtures_use_current_names` 锁定新命名、Runtime 15/status/date maps、runtime index、结构规范、review findings、render-product-submit/render_graph docs 与 module-convention docs。Cargo 按 Runtime 15 implementation-slice cadence deferred；hard-cutover runtime graphics debt 已清零，hybrid-GI render plugin debt 仍 pending。 |
| M2 | Runtime 15 M2 render layer schema-v1 mask API naming hard cutover | runtime_15_render_layer_schema_v1_mask_api_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + render naming follow-up：`RenderLayerSet` 不再保留 `from_legacy_mask(...)`、`to_legacy_mask_lossy(...)` 或 `intersects_legacy_mask(...)` helper；所有 ordinary runtime source/test callers 已硬切到 `from_scene_schema_v1_mask(...)`、`to_scene_schema_v1_mask_lossy(...)` 与 `intersects_scene_schema_v1_mask(...)`，覆盖 core camera contract、render submit tests、light buffer packing、camera history keys、post-process computed resources、mesh/particle/shadow/visibility callers。新增 `naming_boundary/runtime_15_m2/core_framework.rs::runtime_15_render_layer_schema_v1_mask_api_uses_current_names` 锁定 API、ordinary Rust source scan、Runtime 15/status/date maps、runtime index、结构规范、review findings、render camera docs、render-product-submit docs 与 module-convention docs。该切片不改变 scene schema v1 `u32` storage，也不改变 shader/light/visibility 32-bit ABI，只移除 legacy helper API；runtime naming graphics debt 已清零，module-convention 当前只剩 Hybrid GI render plugin debt 与全量 Cargo sweep pending。 |
| M2 | Runtime 15 M2 Hybrid GI extract scene-source naming hard cutover | runtime_15_hybrid_gi_extract_scene_source_naming_hard_cutover_static_passed_cargo_deferred | 2026-06-27 | R2.3/M2 + hard-cutover follow-up：`zircon_plugins/hybrid_gi/runtime/src/hybrid_gi` 已把 Hybrid GI renderer/test source 中的 legacy extract/trace/probe wording 硬切为 `extract`、`extract-backed` 与 `extract-sourced` terminology，覆盖 scene-prepare collect inputs、probe quantization、trace-region inputs、hierarchy resolve weight、runtime parent chain、irradiance/RT-lighting hierarchy tests、scene representation、visibility/runtime test sources。新增 child owner `naming_boundary/runtime_15_m2/graphics/hybrid_gi.rs::runtime_15_hybrid_gi_extract_scene_source_uses_current_names`，父 `graphics.rs` 仅挂载 `mod hybrid_gi`，扫描 Hybrid GI runtime Rust sources，锁定 `extract_trace_region_ids`、`extract-backed`、`extract-sourced RenderHybridGiProbe`、Runtime 15/status/date maps、runtime index、结构规范、review findings、render-product-submit docs 与 module-convention docs。该切片不改变 Hybrid GI scene-prepare ownership、runtime scene-truth filtering、probe/trace budgets、parent-chain fallback、visibility feedback 或 shader ABI；`hard_cutover_migration_debt_count` 已清零，`module_convention_gate classified-and-clear`，Cargo 按 Runtime 15 implementation-slice cadence deferred。 |

## 8. 与既有计划 / guard 的联动

- 本计划加入后，runtime 总索引 §3 子计划地图新增第 15 行——`runtime_index_subplan_map_covers_existing_plan_files_without_stale_rows`、`runtime_plan_status_boundary`（"runtime subplans 14/14"等计数）的期望值须在 M1 同步更新为含 15，否则 guard 红灯。
- M3 / M4 与 `large_file_ownership_gate` 共用 hotspot 清单，不重复拆分；large-file owner 类（`runtime-other` 等）拆分进度计入本计划 M4。
- graphics 大文件热点（render 子计划 §状态总览所列）由本计划 + large-file gate 共同治理，render index「代码结构规范」节引用本计划。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 picking test folder split` / `runtime_15_picking_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/picking/mod.rs`、`tests/picking/pipeline.rs`、`tests/picking/pointer_events.rs` 与 `runtime_15_picking_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 asset mesh test root split` / `runtime_15_asset_mesh_tests_root_split_static_passed_cargo_deferred` 精确锚点包括 `asset/tests/assets/mesh.rs`、`asset/tests/assets/mesh/document_roundtrip.rs`、`asset/tests/assets/mesh/conversion_import.rs` 与 `runtime_15_asset_mesh_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 asset pipeline manager test folder split` / `runtime_15_asset_pipeline_manager_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `asset/tests/pipeline/manager.rs`、`asset/tests/pipeline/manager/model_import.rs`、`asset/tests/pipeline/manager/watcher.rs` 与 `runtime_15_asset_pipeline_manager_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 scene asset integration test folder split` / `runtime_15_scene_asset_integration_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `scene/tests/asset_scene.rs`、`scene/tests/asset_scene/mesh_bindings.rs`、`scene/tests/asset_scene/product_fields.rs` 与 `runtime_15_scene_asset_integration_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 scene world basics test folder split` / `runtime_15_scene_world_basics_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `scene/tests/world_basics.rs`、`scene/tests/world_basics/render_extract.rs`、`scene/tests/world_basics/sprites.rs` 与 `runtime_15_scene_world_basics_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 scene property paths test folder split` / `runtime_15_scene_property_paths_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `scene/tests/property_paths.rs`、`scene/tests/property_paths/read_paths.rs`、`scene/tests/property_paths/write_validation.rs` 与 `runtime_15_scene_property_paths_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 input manager test folder split` / `runtime_15_input_manager_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `input/tests/input_manager.rs`、`input/tests/input_manager/frame_state.rs`、`input/tests/input_manager/touch_gamepad.rs` 与 `runtime_15_input_manager_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M4 scene component lighting/post-process owner split` / `runtime_15_scene_component_light_postprocess_owner_split_static_passed_cargo_deferred` 精确锚点包括 `scene/components/scene.rs`、`scene/components/scene/lighting.rs`、`scene/components/scene/post_process.rs` 与 `runtime_15_scene_components_light_postprocess_are_child_owners`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 script VM registry lock poison recovery` / `runtime_15_script_vm_registry_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `script/vm/backend/backend_registry.rs`、`script/vm/host/host_export_registry.rs`、`script/vm/runtime/hot_reload_coordinator.rs` 与 `runtime_15_script_vm_registry_lock_poison_recovery_guard_covers_vm_registries`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 VM plugin manager selected-backend lock poison recovery` / `runtime_15_vm_plugin_manager_selected_backend_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `script/vm/runtime/vm_plugin_manager.rs`、`docs/zircon_runtime/script/vm/zr_vm_host_reflection.md`、`vm_plugin_manager_selected_backend_accessors_recover_poisoned_lock` 与 `runtime_15_vm_plugin_manager_selected_backend_lock_poison_recovery_guard_covers_manager_selector`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 native live-host tests folder split` / `runtime_15_native_live_host_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `plugin/native_plugin_loader/native_plugin_live_host/tests.rs`、`plugin/native_plugin_loader/native_plugin_live_host/tests/bridge_bindings.rs`、`plugin/native_plugin_loader/native_plugin_live_host/tests/hot_reload_state.rs` 与 `runtime_15_native_live_host_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 native plugin loader real fixture test folder split` / `runtime_15_native_plugin_loader_real_fixture_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/plugin_extensions/native_plugin_loader.rs`、`tests/plugin_extensions/native_plugin_loader/real_fixture.rs` 与 `runtime_15_native_plugin_loader_real_fixture_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 script VM hot-reload coordinator test folder split` / `runtime_15_script_vm_hot_reload_coordinator_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `script/vm/runtime/hot_reload_coordinator.rs`、`script/vm/runtime/hot_reload_coordinator/tests.rs` 与 `runtime_15_script_vm_hot_reload_coordinator_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 lock poison policy guard folder split` / `runtime_15_lock_poison_policy_guard_folder_split_static_passed_cargo_deferred` 精确锚点包括 `structure_convention/lock_poison_policy.rs`、`structure_convention/lock_poison_policy/core_runtime.rs`、`structure_convention/lock_poison_policy/runtime_services.rs`、`structure_convention/lock_poison_policy/asset_render_input.rs` 与 `runtime_15_lock_poison_policy_guard_is_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 core runtime registration structure behavior layout split` / `runtime_15_core_runtime_registration_structure_behavior_layout_split_static_passed_cargo_deferred` 精确锚点包括 `core/runtime/tests/registration/structure.rs`、`core/runtime/tests/registration/structure/behavior_layout.rs` 与 `registration_behavior_tests_stay_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 render shader template assembly guard WGSL contracts split` / `runtime_15_render_shader_template_assembly_guard_wgsl_contracts_split_static_passed_cargo_deferred` 精确锚点包括 `structure_convention/production_file_budget/render_shader_template_assembly.rs`、`structure_convention/production_file_budget/render_shader_template_assembly/wgsl_contracts.rs` 与 `runtime_15_render_shader_template_wgsl_contracts_are_child_owner`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 extension registry bridge test folder split` / `runtime_15_extension_registry_bridge_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/plugin_extensions/extension_registry_bridge.rs`、`tests/plugin_extensions/extension_registry_bridge/basics.rs`、`tests/plugin_extensions/extension_registry_bridge/diagnostics.rs` 与 `runtime_15_extension_registry_bridge_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 manifest contributions test folder split` / `runtime_15_manifest_contributions_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/plugin_extensions/manifest_contributions.rs`、`tests/plugin_extensions/manifest_contributions/editor_only.rs`、`tests/plugin_extensions/manifest_contributions/net.rs` 与 `runtime_15_manifest_contributions_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 runtime plugin package manifest test folder split` / `runtime_15_runtime_plugin_package_manifest_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/plugin_extensions/runtime_plugin_package_manifest.rs`、`tests/plugin_extensions/runtime_plugin_package_manifest/feature_modules.rs` 与 `runtime_15_runtime_plugin_package_manifest_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 export build plan test folder split` / `runtime_15_export_build_plan_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/plugin_extensions/export_build_plan.rs`、`tests/plugin_extensions/export_build_plan/catalog_projection.rs` 与 `runtime_15_export_build_plan_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 export build plan platform test folder split` / `runtime_15_export_build_plan_platform_tests_folder_split_static_passed_cargo_deferred` 精确锚点包括 `tests/plugin_extensions/export_build_plan_platform.rs`、`tests/plugin_extensions/export_build_plan_platform/browser_hosts.rs` 与 `runtime_15_export_build_plan_platform_tests_are_folder_backed`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 asset project manager lock poison recovery` / `runtime_15_asset_project_manager_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `asset/pipeline/manager/project_asset_manager/runtime.rs`、`asset/pipeline/manager/project_asset_manager/construction.rs`、`project_asset_manager_runtime_accessors_recover_poisoned_locks` 与 `runtime_15_asset_project_manager_lock_poison_recovery_guard_covers_project_asset_manager`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 asset worker pool lock poison recovery` / `runtime_15_asset_worker_pool_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `asset/pipeline/worker_pool.rs`、`asset/pipeline/manager/service_contracts/asset_manager_contract.rs`、`asset_worker_pool_accessors_recover_poisoned_locks` 与 `runtime_15_asset_worker_pool_lock_poison_recovery_guard_covers_asset_worker_pool`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 WGPU render framework lock poison recovery` / `runtime_15_wgpu_render_framework_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `graphics/runtime/render_framework/wgpu_render_framework/wgpu_render_framework.rs`、`docs/zircon_runtime/graphics/render-product-submit.md`、`wgpu_render_framework_accessors_recover_poisoned_locks` 与 `runtime_15_wgpu_render_framework_lock_poison_recovery_guard_covers_wgpu_framework`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 RHI WGPU render device lock poison recovery` / `runtime_15_rhi_wgpu_render_device_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `rhi_wgpu/device.rs`、`docs/zircon_runtime/rhi/descriptors.md`、`wgpu_render_device_state_accessors_recover_poisoned_lock` 与 `runtime_15_rhi_wgpu_render_device_lock_poison_recovery_guard_covers_device_state`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 dynamic scene spawn task lock poison recovery` / `runtime_15_dynamic_scene_spawn_task_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `scene/dynamic_scene/spawn_task/task.rs`、`scene/dynamic_scene/spawn_task/loader.rs`、`dynamic_scene_spawn_task_accessors_recover_poisoned_locks` 与 `runtime_15_dynamic_scene_spawn_task_lock_poison_recovery_guard_covers_spawn_task`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 scene ECS parallel executor lock poison recovery` / `runtime_15_scene_ecs_parallel_executor_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `scene/ecs/schedule_parallel_executor.rs`、`docs/zircon_runtime/scene/ecs.md`、`schedule_parallel_executor_batch_result_slot_recovers_poisoned_lock` 与 `runtime_15_scene_ecs_parallel_executor_lock_poison_recovery_guard_covers_batch_result_slots`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 plugin bridge table lock poison recovery` / `runtime_15_plugin_bridge_table_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `plugin/bridge/table.rs`、`docs/zircon_runtime/plugin/bridge.md`、`bridge_entry_provider_accessors_recover_poisoned_provider_lock` 与 `runtime_15_plugin_bridge_table_lock_poison_recovery_guard_covers_provider_slot`。

Runtime 15 精确锚点补记 2026-06-24：`Runtime 15 M3 native live-host bridge methods lock poison recovery` / `runtime_15_native_live_host_bridge_methods_lock_poison_recovery_static_passed_cargo_deferred` 精确锚点包括 `plugin/native_plugin_loader/native_plugin_live_host/bridge_methods.rs`、`docs/zircon_runtime/plugin/bridge.md`、`native_live_host_bridge_method_bindings_recover_poisoned_lock` 与 `runtime_15_native_live_host_bridge_methods_lock_poison_recovery_guard_covers_binding_registry`。
