# 12-input-stack-and-action-mapping 产出记录归档

> 来源：[`12-input-stack-and-action-mapping.md`](../12-input-stack-and-action-mapping.md) 的 `## 状态与产出记录`。

## 2026-07-10 Runtime12 输入栈当前守卫 owner 对账

状态锚：`runtime_12_input_stack_current_guard_owner_reconciled_fresh_filter_pending`。`input_stack_boundary` 当前事实为 `expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`expected_guard_file_count = 6`、`missing_guard_files = []`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`behavior_test_anchor_count = 15`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；镜像守卫为 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts`。本次只把守卫读取面硬切到 Runtime12/Runtime15 编号产出，并补齐 6 个当前守卫文件的镜像事实，不修改输入生产行为；fresh `input` Cargo filter 仍 pending。

## 状态与产出记录

| 里程碑 | 切片 | 状态 | 完成日期 | 证据 |
|---|---|---|---|---|
| M0 | 0.1 链路与帧语义 | input_frame_contract_static_passed_cargo_pending | 2026-06-13 | 已读锚点：`dev/bevy/crates/bevy_input/src/button_input.rs`（`just_pressed` / `just_released` 一帧语义）、`dev/godot/core/input/` 目录（Input/InputMap 后续动作映射与仲裁参照）。复用镜像文档 `docs/zircon_runtime/input/input_state.md`，新增 "Frame Input Contract"：platform/app 事件源 → `DefaultInputManager::submit_event(...)` → `InputFrameSnapshot` / `InputSnapshot` → gameplay/UI consumer，并明确 dynamic session 中 level tick 先于 `input_manager.begin_frame()` 的清帧边界。新增测试锚 `input_snapshot_just_pressed_is_true_for_exactly_one_frame` 与 `frame_input_clears_after_level_tick_not_before`（`zircon_runtime/src/input/tests/input_manager.rs`）；新增 `runtime_absorption::input_stack::runtime_12_input_stack_contracts_stay_documented_and_exported` 锁定文档/导出面；`rustfmt --edition 2021 --check` 已覆盖 Runtime 12 输入契约、runtime、测试挂载与输入测试文件，冲突标记/尾随空白扫描为空，`git diff --check` 仅有既有 LF/CRLF 提示。Cargo 未跑：当前有 active HZB/Cargo lane，待空闲后运行 `cargo test -p zircon_runtime --lib input --locked -- --nocapture`。 |
| M0 | 0.2 仲裁判词 | arbitration_judgement_documented_static_passed | 2026-06-13 | 裁决已写入本计划的 "M0.2 输入仲裁判词"：底层 `zircon_runtime::input` 只维护原始输入与帧状态；UI surface/pointer capture/popup/focus 先经 09 的 `interaction_gate`/dispatch 权威链；玩法/action mapping 只消费 UI 未处理或无 UI/headless 输入流。09 计划已加交叉引用；本切片不改 UI 源码、不运行 Cargo。 |
| M1 | 1.1 动作映射设计 | action_contract_static_passed_cargo_pending | 2026-06-13 | 新增 `InputAction`、`InputBinding`、`InputActionMap`、`InputActionState` 于 `zircon_runtime/src/core/framework/input/`，均为 serde 数据契约，action id 使用字符串键，binding 使用可序列化 `InputButton` chord。`InputActionMap::clear_bindings(...)` 支持运行时重绑定；`zircon_runtime/src/prelude.rs` 与 `zircon_runtime/src/input/mod.rs` 已导出公共面。静态证据同 M1.2：`rustfmt --edition 2021 --check` 覆盖 `core/framework/input` 契约文件、runtime evaluator、测试挂载与输入测试；冲突标记/尾随空白扫描为空，`git diff --check` 仅有 LF/CRLF 提示；Cargo 待 active Cargo lane 清空后随 M1.2 运行 `action_map` 与 `input` filters。 |
| M1 | 1.2 最小实现 | action_evaluator_static_passed_cargo_pending | 2026-06-13 | 新增 `zircon_runtime/src/input/runtime/action_evaluator.rs`，从 `InputFrameSnapshot` 求值 pressed/just_activated/just_deactivated；`evaluate_with_consumed_buttons(...)` 承接 M0.2 UI-first arbitration，只消费 UI 未处理输入。新增测试锚 `action_map_resolves_chords_and_reports_just_activated`、`rebinding_action_does_not_require_recompilation` 于独立 `zircon_runtime/src/input/tests/action_mapping.rs`；新增 `runtime_absorption::input_stack::runtime_12_action_mapping_keeps_ui_filtered_evaluation_path` 锁 action contract/evaluator/test anchors；`rustfmt --edition 2021 --check` 已覆盖新输入代码/测试/prelude，`git diff --check` 仅有既有 CRLF 提示，冲突/尾随空白扫描为空。Cargo 待 active Cargo lane 清空后运行 `cargo test -p zircon_runtime --lib action_map --locked -- --nocapture`。 |
| M1 | 1.3 action context routing | action_context_static_passed_cargo_pending | 2026-06-17 | 新增 `InputActionContext` 于 `zircon_runtime/src/core/framework/input/input_action_context.rs`，`InputActionMap` 现在保存 context table 并以 `#[serde(default)]` 兼容旧 action map 数据；`InputActionEvaluator` 新增 `evaluate_with_active_contexts(...)` 与 `evaluate_with_active_contexts_and_consumed_buttons(...)`，global action 继续无 context 常驻，gameplay/menu 等 context 只在启用并被 active context 选中时求值。新增行为锚 `action_contexts_filter_gameplay_and_menu_maps_without_rebinding`，并同步 `runtime_absorption::input_stack` 与 `input_stack_boundary`：`expected_runtime_module_count = 10`、`expected_framework_module_count = 18`、`expected_test_module_count = 5`、`public_surface_anchors = 11/11`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 7`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。Cargo input/action_map/gamepad/app gates 仍 pending。 |
| M1 | 1.4 gamepad axis action value | action_axis_value_static_passed_cargo_deferred | 2026-06-17 | 新增 `InputAxisBinding` 与 `InputAxisDirection`，`InputBinding` 现在可保存按钮 chord、gamepad axis 或 button-gated axis，旧 action map 通过 `#[serde(default)]` 继续读取为空轴列表；`InputActionState` 新增 `value(...)` / `valued_actions(...)`，按钮 action 继续输出 `1.0`，轴 action 从 `InputFrameSnapshot::gamepad_axes` 输出 `Full`/`Positive`/`Negative` 连续值；`InputActionEvaluator` 新增 `binding_axis_value` 与 `dominant_action_value`，仍先应用 context 和 UI-consumed button 过滤。新增行为锚 `gamepad_axis_binding_reports_continuous_action_value`，并同步 `runtime_absorption::input_stack` 与 `input_stack_boundary`：`expected_runtime_module_count = 10`、`expected_framework_module_count = 18`、`expected_test_module_count = 5`、`public_surface_anchors = 13/13`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 8`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：`rustfmt --edition 2021 --check` 通过；Python py_compile 通过；direct `input_stack_boundary_audit` 报告 `public_surface_anchor_count = 13`、`behavior_test_anchor_count = 8`、missing anchors empty、`risks = []`；standalone `input_stack.rs` 4/4 passed；standalone `plan_status.rs` 32/32 passed；direct `runtime_plan_status_boundary_audit` 报告 `status_output_table_guard_present = true`、`missing_status_output_table_guard_anchors = []`、`risks = []`。Focused Cargo `cargo test -p zircon_runtime --lib gamepad_axis_binding_reports_continuous_action_value --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-axis-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| M1 | 1.7 gamepad axis transition edges | action_axis_transition_static_passed_cargo_deferred | 2026-06-17 | `GamepadAxisTransition` 已加入 `core/framework/input`、`input/mod.rs` 与 prelude 公共输入面；`InputFrameSnapshot::gamepad_axis_transitions` 记录本帧处理后的轴变化，`DefaultInputManager` 在轴值跨过死区或手柄断开时写入 transition，并将同一帧多次轴事件折叠为“帧起始值 -> 帧最终值”。`InputActionEvaluator` 新增 `binding_axis_transition`，让 axis-only action 在轴从 0 到非 0 时报告 `just_activated`，从非 0 到 0 或 disconnect 时报告 `just_deactivated`；button-gated axis 继续服从 UI-consumed button 与 active context 过滤。新增独立测试 owner `zircon_runtime/src/input/tests/action_axis_transitions.rs`，行为锚 `gamepad_axis_action_reports_deadzone_transition_edges` 与 `gamepad_disconnect_deactivates_axis_action`；`runtime_absorption::input_stack` 与 `input_stack_boundary` 当前同步为 `expected_runtime_module_count = 11`、`expected_framework_module_count = 19`、`expected_test_module_count = 6`、`public_surface_anchors = 19/19`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 12`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：`rustfmt --edition 2021 --check`、Python py_compile、direct `input_stack_boundary_audit`、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32 与 direct `runtime_plan_status_boundary_audit` 已作为本切片轻量 gate；focused Cargo `cargo test -p zircon_runtime --lib gamepad_axis_action_reports_deadzone_transition_edges --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-axis-transition-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| M1 | 1.5 action map config source | action_config_static_passed_cargo_deferred | 2026-06-17 | `InputConfig` 现在是输入模块级 action-map 数据源，序列化 `enabled` 与 `action_map`，保留旧默认 `enabled = false`，并提供 `with_enabled(...)`、`with_action_map(...)`、`action_evaluator()` 从反序列化配置直接构造 `InputActionEvaluator`。新增行为锚 `input_config_builds_action_evaluator_from_serialized_action_map` 覆盖 JSON roundtrip、config-driven evaluator、运行时重绑定与 active context 求值；`runtime_absorption::input_stack` 与 `input_stack_boundary` 同步为 `expected_runtime_module_count = 10`、`expected_framework_module_count = 18`、`expected_test_module_count = 5`、`public_surface_anchors = 14/14`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 9`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：`rustfmt --edition 2021 --check` 通过；Python py_compile 通过；direct `input_stack_boundary_audit` 报告 `public_surface_anchor_count = 14`、`behavior_test_anchor_count = 9`、missing anchors empty、`risks = []`；standalone `input_stack.rs` 4/4 passed；standalone `plan_status.rs` 32/32 passed；direct `runtime_plan_status_boundary_audit` 报告 `status_output_table_guard_present = true`、`missing_status_output_table_guard_anchors = []`、`risks = []`。Focused Cargo `cargo test -p zircon_runtime --lib input_config_builds_action_evaluator_from_serialized_action_map --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-config-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| M1 | 1.6 action manager runtime registration | action_manager_registration_static_passed_cargo_deferred | 2026-06-17 | `InputActionManager` 已加入 `core/framework/input` 作为 runtime manager contract，`DefaultInputActionManager` 在 `input/runtime` 内以 `InputActionEvaluator` 为实现内核并支持 `set_action_map(...)`、active context 与 UI-consumed button 求值；`InputConfig::effective_action_map()` 让 disabled config 注册为空 action map，`InputConfig::action_manager()` 生成 manager 实例。`InputModule` 新增 `module_descriptor_with_config(InputConfig)`，注册 `InputModule.Manager.InputActionManager`，`core::manager` 新增 `InputActionManagerHandle`、`INPUT_ACTION_MANAGER_NAME` 与 `resolve_input_action_manager(...)`。新增行为锚 `input_action_manager_resolves_from_runtime_module_descriptor` 覆盖 `CoreRuntime` 注册、manager resolve、配置驱动求值与运行时重绑定；`runtime_absorption::input_stack` 与 `input_stack_boundary` 同步为 `expected_runtime_module_count = 11`、`expected_framework_module_count = 19`、`expected_test_module_count = 5`、`public_surface_anchors = 17/17`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 10`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：`rustfmt --edition 2021 --check`、Python py_compile、direct `input_stack_boundary_audit`、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32 与 direct `runtime_plan_status_boundary_audit` 已作为本切片轻量 gate；focused Cargo `cargo test -p zircon_runtime --lib input_action_manager_resolves_from_runtime_module_descriptor --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-action-manager-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| M1 | 1.8 consumed gamepad axis arbitration | action_axis_consumption_static_passed_cargo_deferred | 2026-06-17 | `GamepadAxisInput` 已加入公共输入协议，用于描述 UI 09 已消费的物理手柄轴；`InputActionEvaluator` 新增 `evaluate_with_consumed_input(...)` 与 `evaluate_with_active_contexts_and_consumed_input(...)`，并通过 `binding_axis_consumed(...)` 在轴值和轴 transition 求值前过滤 consumed axis。旧 `evaluate_with_consumed_buttons(...)` 与 `evaluate_with_active_contexts_and_consumed_buttons(...)` 继续委托到新入口以保持兼容；`InputActionManager` / `DefaultInputActionManager` 同步暴露 consumed-input manager 方法。新增行为锚 `consumed_gamepad_axis_does_not_activate_gameplay_action`，并修正 `gamepad_axis_binding_reports_continuous_action_value` 对 axis-only `just_activated` 的断言，匹配 M1.7 轴边沿语义；`runtime_absorption::input_stack` 与 `input_stack_boundary` 当前同步为 `expected_runtime_module_count = 11`、`expected_framework_module_count = 19`、`expected_test_module_count = 6`、`public_surface_anchors = 19/19`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 12`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。Focused Cargo `cargo test -p zircon_runtime --lib consumed_gamepad_axis_does_not_activate_gameplay_action --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-axis-consumption-0617 --message-format short --color never -- --nocapture --test-threads=1` 按“先实现功能”要求暂缓；input/action_map/gamepad/app broader gates 仍 pending。 |
| M4 | 4.1 input recording/replay | input_recording_replay_static_passed_cargo_deferred | 2026-06-17 | 新增 `zircon_runtime/src/input/runtime/recording.rs`，提供 `InputRecording`、`InputRecordingFrame`、`InputReplayCursor`、`InputReplayFrameReport`；录制通过 `InputRecordingFrame::capture_from_manager(...)` 复用现有 `InputManager::drain_event_records()`，回放通过 `InputReplayCursor::replay_next_frame(...)` 逐帧调用 `begin_frame()` 后按记录顺序重新提交 `InputEvent`，并返回 `InputFrameSnapshot` 作为同帧读取结果。`zircon_runtime::input` 与 prelude 已导出新 helper；新增测试 owner `zircon_runtime/src/input/tests/recording_replay.rs`，行为锚 `input_recording_captures_drainable_event_records_by_frame` 与 `input_replay_restores_frame_snapshots_in_recorded_order`。`runtime_absorption::input_stack` 与 `input_stack_boundary` 当前同步为 `expected_runtime_module_count = 12`、`expected_framework_module_count = 19`、`expected_test_module_count = 7`、`public_surface_anchors = 23/23`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 14`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：rustfmt、Python py_compile、direct `input_stack_boundary_audit` risks=[]、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32、aggregate `audit_runtime_structure.py --json` Runtime 12/plan-status risks=[]、scoped `git diff --check` 通过（仅 LF/CRLF warnings）。`cargo check -p zircon_runtime --lib --no-default-features --features core-min ...` 180s timeout no result；进程扫描仅发现其他会话 `zircon_editor --lib` 编译通道；input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | cursor host requests | cursor_host_request_static_passed_cargo_deferred | 2026-06-20 | 新增并公开 `CursorGrabMode`、`CursorPosition` 与 `CursorHostRequest`，把 cursor visible、grab mode、hit-test 与 host cursor position 统一为 runtime-to-host 请求值；`InputEvent::CursorHostRequest`、`InputFrameSnapshot::cursor_host_requests`、`InputManager::drain_cursor_host_requests()` 与 `DefaultInputManager` queue/drain 路径保持请求只在当前帧可见，`begin_frame()` 清空未 drain 请求，drain 不影响普通输入事件流。`RuntimeDynamicSession::drain_host_requests()` 现在把该队列编码为 `ZrRuntimeHostRequestV1::Cursor`，`zircon_app` runtime preview host 通过 `apply_runtime_cursor_host_request(...)` 调用 winit `set_cursor_visible`、`set_cursor_grab`、`set_cursor_hittest` 与 `set_cursor_position`，并把 desktop `platform.cursor_options` 提升为 `supported:winit_window_options`。新增行为锚 `cursor_host_requests_are_frame_local_and_drainable`，并同步 `runtime_absorption::input_stack` 与 `input_stack_boundary`：`expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 15`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。Cargo input/action_map/gamepad/app broader gates 仍 pending。 |
| 横切 | input validation window recheck | cargo_recheck_timeout_static_guards_passed | 2026-06-20 | `cargo test -p zircon_runtime --lib input --no-default-features --features core-min --locked --jobs 1 --target-dir D:\cargo-targets\zircon-runtime-12-input-validation-0620 --message-format short --color never -- --test-threads=1 --nocapture` 在 1200s 窗口内仍处于 `zircon_runtime` lib-test 编译，未产出测试二进制或测试结果；匹配该 target-dir 的残留 cargo/rustc 进程已停止，不声明 Cargo 通过。轻量验证通过：`input_stack_boundary.py` py_compile，direct `input_stack_boundary_audit` risks=[] 且 runtime/framework/test 12/20/7、public 26、behavior 15、missing anchors empty；standalone `input_stack.rs` 4/4；rustfmt check over Runtime 12 input/cursor guard files。input/action_map/gamepad/app Cargo gates pending。 |
| 横切 | Input stack current audit recheck | input_stack_current_audit_static_passed_cargo_pending | 2026-06-20 | 状态锚 `input_stack_current_audit_static_passed_cargo_pending`；本轮只复核 Runtime 12 当前输入栈/动作映射/cursor host request 边界事实，生产代码未改：`input_stack_boundary_audit` 报告 runtime/framework/test owner 12/20/7、public surface anchors 26/26、Runtime 12 guard anchors 5/5、behavior-test anchors 15/15、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `input_stack_boundary_audit` risks=[]、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 32/32；input/action_map/gamepad/app Cargo gates 仍 pending。 |
| 横切 | Input stack 2026-07-01 current audit recheck | input_stack_20260701_current_audit_static_passed_cargo_deferred | 2026-07-01 | 状态锚 `input_stack_20260701_current_audit_static_passed_cargo_deferred`；复核当前 Runtime 12 输入栈/动作映射/cursor host request 边界事实，生产代码未改：`input_stack_boundary_audit` 报告 runtime/framework/test owner 12/20/7、public surface anchors 26/26、Runtime 12 guard anchors 5/5、behavior-test anchors 15/15、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。同轮 full `audit_runtime_structure.py --json` 风险汇总为 `{}`；standalone `plan_status.rs` 41/41 通过。input/action_map/gamepad/app package Cargo gates 仍 deferred，因为外部 Cargo/rustc 通道 active。 |
| 横切 | Runtime 12 Input stack inventory split | input_stack_inventory_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `input_stack_inventory_split_static_passed_cargo_deferred_tests_deferred`；`input_stack_source_inventory.py` 现在持有 Runtime 12 runtime/framework/test owner module 清单和 line-budget 常量，`input_stack_anchor_inventory.py` 现在持有 module declarations、public surface、action evaluator、gamepad ABI、cursor host-request、guard/doc/test/behavior/Cargo gate anchors；`input_stack_boundary.py` 只保留审计读取、缺失计算与风险聚合，当前 337 行；`input_stack_markdown.py` 承接 Markdown 渲染，当前 108 行。direct audit 仍报告 runtime/framework/test owner 12/20/7、public surface anchors 26/26、Runtime 12 guard anchors 5/5、behavior-test anchors 15/15、cursor host-request anchors 12/12、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：Python py_compile、direct `input_stack_boundary_audit` risks=[]、rustfmt touched plan-status guards、standalone `input_stack.rs` 4/4、standalone `plan_status.rs` 33/33；外部 render/editor Cargo lanes 活跃，本切片不启动 package-level Cargo，不提升 input/action_map/gamepad/app gates。 |
| 横切 | Runtime 12 Input stack Markdown renderer split | input_stack_markdown_split_static_passed_cargo_deferred_tests_deferred | 2026-06-21 | 状态锚 `input_stack_markdown_split_static_passed_cargo_deferred_tests_deferred`；`input_stack_markdown.py` now owns `render_input_stack_boundary_markdown`, and `audit_runtime_structure.py` imports the renderer from that Markdown owner instead of `input_stack_boundary.py`; `input_stack_boundary.py` now owns audit read, missing-anchor calculation, and risk aggregation at 337 lines, while the Markdown owner is 108 lines. Direct audit reports runtime/framework/test owner 12/20/7, public surface anchors 26/26, Runtime 12 guard anchors 5/5, behavior-test anchors 15/15, cursor host-request anchors 12/12, missing doc/test/behavior/Cargo anchors [], oversized modules [], `mirror_docs_guard_present = true`, and `risks = []`. Validation: Python py_compile and direct `input_stack_boundary_audit`; standalone `input_stack.rs` 4/4 and standalone `plan_status.rs` 33/33; broader input/action_map/gamepad/app Cargo gates remain deferred while external compile lanes are active. |
| 横切 | input boundary grouped manager import guard repair | input_boundary_grouped_manager_import_guard_passed_broader_input_failures_pending | 2026-06-21 | 状态锚 `input_boundary_grouped_manager_import_guard_passed_broader_input_failures_pending`；默认 full-lib broader failure 中的 `input_protocol_types_live_in_runtime_input_surface` 是测试守卫漂移：`core::manager::resolver` 当前已通过 `crate::core::framework::{ input::{InputActionManager, InputManager}, ... }` grouped import 来源取得 `InputManager`，没有回退到 `core::manager` 协议类型；`zircon_runtime/src/input/tests/boundary.rs` 现在同时接受 grouped import 与旧单项 import 形态，并继续禁止 manager re-export input protocol types。验证：`rustfmt --edition 2021 --check zircon_runtime\src\input\tests\boundary.rs` 通过；`cargo test -p zircon_runtime --lib input_protocol_types_live_in_runtime_input_surface --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture` 日志 `target\codex-runtime12-logs\input_boundary_grouped_manager_import_default_20260621.log` 记录 `1 passed; 0 failed; 4704 filtered out`。随后 Runtime 10 修复 dynamic input mouse-wheel event owner guard 并把 Vampire W 输入真实后端测试改为默认配置 ignored gate 后，`cargo test -p zircon_runtime --lib input --locked --jobs 1 --target-dir target\codex-runtime11-default-tasks-0621 --message-format short --color never -- --test-threads=1 --nocapture` 在 `target\codex-runtime12-logs\input_default_after_vampire_real_backend_gate_20260621.log` 记录 `342 passed; 11 failed; 1 ignored; 4353 filtered out`，剩余失败集中在 UI input ownership/routing、MUI text input/IME/pointer 等上层输入消费路径；因此本行只关闭 boundary 守卫并记录最新 broader input 剩余风险，不提升 Runtime 12 input/action_map/gamepad/app Cargo gates。 |
| 横切 | Runtime 12 input_manager child test owner audit sync | runtime_12_input_manager_child_test_owner_audit_sync_static_passed_cargo_deferred | 2026-07-01 | `input_manager` 测试拆到 `input/tests/input_manager/{frame_state,host_requests}.rs` 后，本切片把这两个子 owner 纳入 `input_stack_source_inventory.py` 的行为锚来源，并让 `input_stack_boundary.py` 与 `runtime_absorption::input_stack` 同步读取子文件；顶层 `expected_test_module_count = 7` 不变，`behavior_test_anchor_count = 15`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_doc_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`risks = []` 继续作为 Runtime 12 镜像事实。验证：Python py_compile、direct `input_stack_boundary_audit`、rustfmt check、standalone `input_stack.rs` 4/4、targeted `plan_status.rs` status-table/index/last_refined checks 3/3 通过；完整 `plan_status.rs` 31/41 通过，剩余 10 个既有跨计划失败集中在 Runtime 02/03/04/05/06/07/14 与架构评审锚，不提升 package-level input/action_map/gamepad/app Cargo gates。 |
| M2 | 2.1 gamepad 桥接 | gamepad_bridge_static_passed_cargo_pending | 2026-06-13 | 现有 app 侧 `zircon_app/src/entry/runtime_entry_app/gamepad/{events,polling}.rs` 已通过 `ZrRuntimeEventV1::gamepad_connection_with_ids` / `gamepad_button` / `gamepad_axis` 向 runtime ABI 发送 gilrs 连接、按钮、轴事件，`RuntimeDynamicSession` 入口经 `zircon_runtime/src/dynamic_api/session/events.rs` 归约为 `InputEvent::GamepadConnection` / `GamepadButton` / `GamepadAxis`。新增 `zircon_runtime/src/input/tests/gamepad_bridge.rs`，含 `gamepad_disconnect_clears_held_state_without_panic` 与 `gamepad_host_bridge_uses_runtime_gamepad_abi_constructors` 两个锚；新增 `runtime_absorption::input_stack::runtime_12_gamepad_bridge_keeps_runtime_abi_path` 锁 gamepad contract/app ABI/session event reducers；`rustfmt --edition 2021 --check` 已覆盖 gamepad 测试与输入测试挂载，冲突标记/尾随空白扫描为空，`git diff --check` 仅有 LF/CRLF 提示。Cargo 待 active Cargo lane 清空后运行 `cargo test -p zircon_runtime --lib gamepad --locked -- --nocapture`，app 包回归仍待后续空闲窗口。 |
| M3 | 验证门守卫 | cargo_validation_pending_guarded | 2026-06-13 | 新增 `runtime_absorption::plan_status::runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation`，要求本计划保持 `in_progress`，M0/M1/M2 状态行继续保留 `*_cargo_pending` 与 Cargo gate，直到 `cargo test -p zircon_runtime --lib input --locked -- --nocapture`、`cargo test -p zircon_runtime --lib action_map --locked -- --nocapture`、`cargo test -p zircon_runtime --lib gamepad --locked -- --nocapture` 以及 `cargo test -p zircon_app --locked` 有真实通过记录。 |
| 横切 | Input stack 结构审计 owner | structure_audit_static_passed_cargo_pending | 2026-06-14 | 新增 `.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/runtime_structure_audits/input_stack_boundary.py` 并接入 `audit_runtime_structure.py`，把 Runtime 12 的 `input/` runtime owner、`core/framework/input` 契约、输入测试 owner、`InputActionEvaluator::evaluate_with_consumed_buttons(...)`、app gilrs → runtime ABI → `session/events.rs` → `InputEvent::Gamepad*` 路径、三条 `runtime_absorption::input_stack` 守卫、镜像文档守卫与 pending Cargo gate 统一纳入结构审计。Targeted audit facts：`expected_runtime_module_count = 10`、`expected_framework_module_count = 17`、`expected_test_module_count = 5`、`public_surface_anchors = 10/10`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 6`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。Cargo 仍按 M3 gate 保持 pending。 |
| 横切 | Input stack 镜像文档守卫 | mirror_docs_static_passed_cargo_pending | 2026-06-14 | 新增 `runtime_absorption::input_stack::runtime_12_input_stack_mirror_docs_match_structure_audit_counts`，锁定 input module doc、本计划、runtime index、M0 review 与 runtime-interface convergence 均同步记录 `input_stack_boundary` 的同一组结构事实：`expected_runtime_module_count = 10`、`expected_framework_module_count = 17`、`expected_test_module_count = 5`、`public_surface_anchors = 10/10`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 6`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`。验证：rustfmt check 通过；Python py_compile 与 direct `input_stack_boundary_audit` 断言通过；standalone `rustc --edition 2021 --test zircon_runtime/src/tests/runtime_absorption/input_stack.rs` 4/4 passed；stale Runtime 12 4/4 scan、scoped diff/conflict checks clean（仅 LF-to-CRLF warning）。该守卫只封住文档漂移；input/action_map/gamepad/app Cargo gates 仍保持 pending。 |
| 横切 | gamepad dynamic-session event-owner 漂移同步 | input_boundary_static_passed_cargo_pending | 2026-06-15 | Runtime 10 Dynamic Session Event Split 后，gamepad reducer owner 已从 `zircon_runtime/src/dynamic_api/session.rs` 拆到 `zircon_runtime/src/dynamic_api/session/events.rs`；本切片同步 `input_stack_boundary.py` 与 `runtime_absorption::input_stack::runtime_12_gamepad_bridge_keeps_runtime_abi_path`，把 app gilrs → runtime ABI → `session/events.rs` → `InputEvent::Gamepad*` 纳入同一静态边界，并将镜像文档增加 `missing_gamepad_abi_anchors = []`。验证：Python py_compile 通过；direct `input_stack_boundary_audit` 报告 `risks = []`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_cargo_gate_anchors = []`；aggregate `audit_runtime_structure.py --json` 报告 `input_stack_boundary.risks = []` 且 `runtime_plan_status_boundary.risks = []`；`rustfmt --edition 2021 --check zircon_runtime/src/tests/runtime_absorption/input_stack.rs` 通过；standalone `rustc --edition 2021 --test zircon_runtime/src/tests/runtime_absorption/input_stack.rs` 4/4 passed；冲突标记与尾随空白扫描为空，`git diff --check` 仅 LF-to-CRLF warning。Cargo input/action_map/gamepad/app gates 仍 pending。 |
| 横切 | gamepad bridge source guard event-owner sync | gamepad_bridge_source_guard_static_passed_cargo_timeout | 2026-06-17 | `zircon_runtime/src/input/tests/gamepad_bridge.rs` 的 `gamepad_host_bridge_uses_runtime_gamepad_abi_constructors` 现在同时读取 `zircon_runtime/src/dynamic_api/session.rs` 与 `zircon_runtime/src/dynamic_api/session/events.rs`，允许 reducer owner 位于拆分后的 `session/events.rs`，并继续锁定 app gilrs → `ZrRuntimeEventV1::{gamepad_connection_with_ids,gamepad_button,gamepad_axis}` → `InputEvent::Gamepad*`。验证：`rustfmt --edition 2021 --check` 覆盖 Runtime 12 输入契约/runtime/tests；Python py_compile 通过；direct `input_stack_boundary_audit` 报告 `expected_runtime_module_count = 10`、`expected_framework_module_count = 18`、`expected_test_module_count = 5`、`public_surface_anchor_count = 11`、`guard_anchor_count = 5`、`behavior_test_anchor_count = 7`、`missing_gamepad_abi_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`；standalone `input_stack.rs` 4/4 passed。Focused Cargo `cargo test -p zircon_runtime --lib gamepad_host_bridge_uses_runtime_gamepad_abi_constructors --locked --jobs 1 --target-dir E:\cargo-targets\zircon-runtime-12-input-0617 --message-format short --color never -- --nocapture --test-threads=1` 605s timeout no result；残留 cargo/rustc/rustdoc 扫描为空，input/action_map/gamepad/app gates 仍 pending。 |
| 横切 | Input stack 行为测试锚审计同步 | mirror_docs_static_passed_cargo_pending | 2026-06-15 | `input_stack_boundary` 与 `runtime_12_input_stack_mirror_docs_match_structure_audit_counts` 现在显式锁定 Runtime 12 M0/M1/M2 的 6 个输入行为测试锚：`input_snapshot_just_pressed_is_true_for_exactly_one_frame`、`frame_input_clears_after_level_tick_not_before`、`action_map_resolves_chords_and_reports_just_activated`、`rebinding_action_does_not_require_recompilation`、`gamepad_disconnect_clears_held_state_without_panic`、`gamepad_host_bridge_uses_runtime_gamepad_abi_constructors`；当前 `behavior_test_anchor_count = 6`、`missing_behavior_test_anchors = []`。验证：rustfmt check、Python py_compile、direct `input_stack_boundary_audit`、aggregate Runtime 12 + plan-status assertions、standalone input_stack 4/4、standalone status-output 2/2；input/action_map/gamepad/app Cargo gates pending。 |
| 横切 | Runtime 12 current child guard/status owner sync | runtime_12_input_stack_guard_doc_owner_sync_static_passed_cargo_deferred | 2026-07-10 | `input_stack_source_inventory.py` 现显式登记 route parent、contracts/action/gamepad children、inventory mirror child 与 `cargo_gates/late/runtime_12.rs` 共 6 个真实 guard owners；`input_stack_boundary.py` 聚合这些 owner 并由 `tools/tests/test_runtime_input_stack_audit.py` 锁定 runtime/framework/test 12/20/7、guards 6/6、guard anchors 5/5、behavior anchors 15/15、missing guard/doc/test/behavior/Cargo anchors 全空、mirror present、`risks = []`。同步 Runtime 12、runtime index、input module doc、M0 review 与 convergence 当前计数、状态/Cargo 锚，并恢复 `玩法/action mapping 只消费 UI 未处理` 硬仲裁判词。验证：Python regression 1/1、py_compile、direct audit、scoped rustfmt、standalone Rust Runtime 12 guards 9/9 通过；package gates 未重跑，Runtime 12 保持 `in_progress`。 |

基线：`input/` 模块树（mod/module/runtime/tests）；gamepad：app 侧 gilrs optional 且已有 ABI bridge，M2.1 已补断开清理与 bridge source guard 锚；M0.1/M1/M2/M4 的 `rustfmt --edition 2021 --check`、锚点扫描、冲突标记扫描、尾随空白扫描与 `git diff --check` 静态证据已记录，`runtime_absorption::input_stack` 守卫已锁定文档/导出、UI-filtered action evaluator、gamepad ABI path、axis action value、axis transition edges、InputConfig action-map 数据源、action manager runtime registration、input recording/replay helper、cursor host request frame-local queue、dynamic host-request ABI/app host application 与镜像文档计数；`input_stack_boundary.py` 进一步锁定 `expected_runtime_module_count = 12`、`expected_framework_module_count = 20`、`expected_test_module_count = 7`、`public_surface_anchors = 26/26`、`runtime_12_guard_anchors = 5/5`、`behavior_test_anchor_count = 15`、`missing_gamepad_abi_anchors = []`、`missing_cursor_host_request_anchors = []`、`missing_doc_anchors = []`、`missing_test_anchors = []`、`missing_behavior_test_anchors = []`、`missing_cargo_gate_anchors = []`、`oversized_modules = []`、`mirror_docs_guard_present = true`、`risks = []`，并作为 337 行审计/风险 owner 配合 108 行 `input_stack_markdown.py`；Cargo 通过数仍待 active HZB/active Cargo lane 清空后记录，并由 `runtime_12_input_stack_cargo_pending_gate_stays_explicit_until_input_validation` 锁定 input/action_map/gamepad/app filters 的待验证状态；M0.2 已定 UI-first arbitration 边界，M1 已落最小 action/context/binding/map/state 契约、gamepad axis 连续值、axis deadzone activation/deactivation edges、config-driven evaluator 与 manager/resolver 注册路径，M4 已落基于 `InputEventRecord` 的确定性录制/回放 helper，cursor host requests 已提供 runtime → ABI → app host handoff 请求值并让 desktop `platform.cursor_options` 报告 supported，后续不得绕过 09 路由权威直接把 UI 命中事件映射成玩法动作。

## 风险与协调

- 与 09（UI 路由）的交接面已由 0.2 判词定为双向互引：12 只定义全局输入消费边界，09 继续拥有 UI 内部路由权威，禁止各写一套。
- 动作映射的配置文件格式与 04（asset/config）的 config_store 归属衔接；序列化形状过 05 的 authoring token 守卫（不得引入 editor 语义词）。
- 03 帧序图（frame_schedule.md）若已定稿，0.1 的清帧时机直接标注其上；未定稿则本计划先行并回写。
