# Zircon 插件生态完善总体计划（docs/plans/zircon_plugins）

> 状态：工程化细化版 v2（2026-06）——全部分计划已按代码实查校准基线，并细化到"模块文件树 + API 签名 + 里程碑任务表（含测试名）"的可开工粒度
> 范围：以插件架构核心升级为前置，系统性补齐 Sound / Physics / Animation / Navigation / AI / Net / VM / 跨平台发行 八大周边设施插件，并确立编辑器集成横切规范。
> 原则（全计划共同遵守）：硬切不留兼容层；中立契约在 `zircon_runtime::core::framework::*`，实现在 `zircon_plugins/*`；root 接线文件保持薄；注册期重、运行期零开销；插件分 runtime/editor 两部分注册。
> **实现纪律**：每份计划文末附录给出 `dev/` 参考源码对位表（路径均已实地核验）——任务开工前必读对应条目，复杂算法/协议/语义（调度拓扑、DSP 数值、Jolt 映射、行为树打断、复制协议、导出流程等）一律对照参考实现核对，**禁止凭记忆或凭空实现**。

## 1. 文档索引

| 编号                                       | 计划               | 优先级       | 核心交付                                                                                                                                                                                                                                                                                                 |
| ------------------------------------------ | ------------------ | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [01](01-plugin-architecture-core.md)          | 插件架构核心升级   | **P0** | 9 阶段调度 + SystemSet 偏序、register_system/resource/event、TypedExtensionPoint/FrozenExtensionTable、四阶段生命周期、Editor capability 对称、Native ABI v3                                                                                                                                             |
| [02](02-sound.md)                             | Sound              | P1           | **kira 为执行核心**（自研混音/输出栈退役）、契约 → kira track/effect 映射编译、缺口效果与 HRTF 以 kira Effect 迁移现有数值、3D 策略计算 + Timeline→Tween/Clock                                                                                                                                   |
| [03](03-physics.md)                           | Physics            | P1           | PhysicsBackend trait + joltc-sys 后端、形状全集、六约束、ragdoll 三模式、QueryMode/过滤                                                                                                                                                                                                                  |
| [04](04-animation.md)                         | Animation          | P1           | AnimationTargetId dense 化 + PoseBuffer SoA 零分配求值、avatar mask 编译、多层状态机、GPU skinning、IK                                                                                                                                                                                                   |
| [05](05-navigation.md)                        | Navigation         | P1           | TiledBake 异步、DetourCrowd 接入、TileCache carving、off-mesh traverse 状态机（vendored recastnavigation 已在）                                                                                                                                                                                          |
| [06](06-ai.md)                                | AI                 | P2           | 行为树 dense 编译 + 执行内核重写（observer aborts）、类型化 blackboard、perception 预算、集成节点                                                                                                                                                                                                        |
| [07](07-net.md)                               | Net                | P2           | NetWorker 线程模型纠偏（替代 block_on）、TLS、Session/RPC、replication 闭环、可靠 UDP、内容下载                                                                                                                                                                                                          |
| [08](08-zr-vm.md)                             | ZrVM               | P2           | zircon_reflect_derive 宏、双向反射统一模型、dense call site、四回调通道、GC 协约、热替换迁移                                                                                                                                                                                                             |
| [09](09-export-publishing.md)                 | 跨平台发行         | P2           | 导出 profile、三路径闭环、平台模板、zrpack 字节格式与裁剪、`zircon export` CLI 阶段状态机                                                                                                                                                                                                              |
| [10](10-editor-integration.md)                | 编辑器集成规范     | P1（横切）   | 扩展点签名级约定、AI Workbench 对位表、三项调试设施、E1 反射默认 drawer、E2 OperationStack 收口                                                                                                                                                                                                          |
| [11](11-plugin-call-bridge.md)                | 插件调用桥         | P1（横切）   | 强/弱依赖接口直调（StrongBridge 零检查 / WeakBridge 未启用返回 NotEnabled）、FrozenBridgeTable 世代模型、事件 dense 通道 + dormant 订阅                                                                                                                                                                  |
| [12](12-plugin-dx-and-structure-framework.md) | 插件 DX 与结构框架 | P1（横切）   | 统一 `plugin.toml` schema + 唯一 crate 骨架 + 注册单入口 + capability 单源（四源一致）+ `plugin_sdk` builder；执行 [`engine-code-structure-convention`](../engine-code-structure-convention.md) §6                                                                                                   |
| [13](13-standalone-plugin-build.md)           | 插件独立构建与分发 | P1（横切）   | 双形态单源（embed `rlib` / dist `cdylib`）+ 依赖边界硬约束（dist 闭包禁 `zircon_runtime`）+ 注册跨 ABI 编组 + `zircon plugin build <id>` 每插件独立构建 + per-plugin zrpack 资产子包 + ABI/引擎兼容性协商 + 签名/hash sidecar + load manifest 汇编；规范见 [`plugin-standalone-build.md`](../../zircon_plugins/plugin-standalone-build.md) |

## 2. 依赖图与推进顺序

```
01 插件架构核心 (P0)
 ├─ M1 调度/系统注册 ──→ 03-M1 / 04-M1（首批验证者，同窗口迁移 scene hook）
 ├─ M2 资源/事件/类型化扩展点 ──→ 02 / 05 / 06 / 07 全部
 ├─ M3 生命周期/可选功能 ──→ 07（feature 包）、可选依赖探测（02 occlusion、05 几何收集、06 sight）
 ├─ M4 Editor 对称化 ──→ 10-E2、各插件 Editor 里程碑
 └─ M5 Native ABI v3 ──→ 09-M5（NativeDynamic 发行）

08-M1 统一反射（zircon_reflect_derive） ──→ 10-E1（默认 drawer）、07-M3/M4（RPC/replication schema）、06-M3（ScriptTask）
01-M2/M3 ──→ 11-M1（调用桥核心）；11-M1 ──→ 02 occlusion / 05 几何收集 / 06 sight 的运行期弱依赖调用（WeakBridge<physics.query.v1>，physics 于 03-M2 后导出）
11-M2 事件优化 ──→ 与 01-M2 register_event 同窗口落地（EventStore dense 化一次迁移）
04-M2 骨骼姿态通道（SkeletalPoseTargets/SimulatedPoseFeed） ──→ 03-M5（ragdoll）
05-M3 agent ──→ 06-M3（MoveTo 节点）
07-M6 content_download ↔ 09-M2 zrpack（共享 ZrPackManifest/ZrChunkEntry DTO，定义于 framework/net/download.rs，先定 DTO 再各自实现）
13 插件独立构建（双形态/依赖边界/ABI 编组/per-plugin build）依赖 01-M2/M4（register 通道+访问集）、09-M1/M2（NativeDynamic+zrpack 底座）、11-M1/M2（bridge dense 通道）、12-M1..M4（manifest/骨架/SDK/capability）；其 M1/M2 与 12 同窗口（双形态骨架是 12 骨架的发行维扩展），M3/M4 与 09-M1/M2 共享 zrpack/CLI 底座，M5 随各能力波次 touch-it-conform-it
```

推荐执行波次（同波内可并行，互不依赖）：

1. **波次零（结构前置）**：12-M1/M2/M3/M4（统一 `plugin.toml` schema、唯一 crate 骨架 + `plugin_sdk`、注册单入口、capability 单源）+ **13-M1/M2（双形态骨架 + 依赖边界 + 注册跨 ABI 编组）**——先于能力波次，定义所有插件共用契约；12-M5 / 13-M5 存量硬切随各插件能力波次 touch-it-conform-it（见计划 12 / 13）。2026-06-22 至 2026-06-23 已推进 12-M1 manifest/schema/generated parity、12-M2 `plugin_sdk` builder/native/editor/test fixture 骨架，以及 12-M3 registration builder + animation 代表迁移、`asset_importers/{data,model,shader}` family 与 split importers（`gltf_importer` / `obj_importer` / `texture_importer` / `audio_importer` / `opus_importer` / `shader_wgsl_importer` / `ui_document_importer`）公开注册自由函数清零，并新增 D12 `runtime_plugin_exports!` SDK helper，完成 ai、animation、hybrid_gi、navigation、net、particles、physics、prefab_tools、rendering、solari、terrain、texture、tilemap_2d、virtual_geometry、zr_vm_language 共 15 个 trait-backed runtime 插件的 helper 全量 rollout。当前结构状态包括 `plugins_12_runtime_capability_single_source_guard_passed`、`plugins_12_importer_capability_owner_rollout_reduces_migration_debt`、`plugins_12_runtime_only_skeleton_owner_rollout_reduces_migration_debt`、`plugins_12_editor_only_skeleton_owner_rollout_reduces_migration_debt`、`plugins_12_authoring_runtime_editor_skeleton_owner_rollout_reduces_migration_debt` 与 `plugins_12_particles_physics_texture_skeleton_owner_rollout_reduces_migration_debt`：上述 15 个 trait-backed first-party runtime 根的 runtime package capability 已迁入 `runtime/src/capability.rs`，7 个 split importer 与 `asset_importers/{data,model,shader}` 也已迁入各自 `runtime/src/capability.rs`；`ai`、`solari`、`zr_vm_language` 与 `asset_importers/{audio,texture}` 已迁入 runtime-only `plugin.rs`/`capability.rs` owner 骨架；`native_window_hosting`、`runtime_diagnostics`、`ui_asset_authoring` 已迁入 editor-only `capability.rs`/`extension_ids.rs`/`plugin.rs`/`tests.rs` owner 骨架；`prefab_tools`、`terrain`、`tilemap_2d` 已迁入 authoring runtime/editor `plugin.rs`/`authoring.rs`/`tests.rs` owner 骨架；`particles`、`physics`、`texture` 已迁入 runtime/editor owner 骨架。`tools/audit_plugin_structure.py --json` 报告 `missing_plugin_toml = 0`、`manifest_schema_violations = 0`、`m1_gate_status = classified-and-clear`、`skeleton_conformance.sample_conformance_status = sample-clean`、`sample_workspace_dependency_status = sample-workspace-deps-clean`、`skeleton_conformance.migration_debt_count = 11`、`plugin_skeleton_gate.migration_debt_count = 11`、`registration_conformance.m3_t1_gate_status = family-single-entry-clean`、`registration_conformance.m3_split_importer_gate_status = split-importer-single-entry-clean`、`asset_importer_family_free_function_registration_sites = 0`、`split_importer_free_function_registration_sites = 0`、aggregate `m3_importer_gate_status = importer-single-entry-clean`、`capability_conformance.audited_runtime_root_count = 15`、`capability_conformance.capability_source_mismatches = 0`、`capability_conformance.m4_runtime_capability_gate_status = runtime-capability-single-source-clean`。native-only SDK check/test、native fixture check、SDK editor feature check、editor sample check、default SDK test-runtime check、`test_runtime_builder` SDK self-test 2/2、registration builder SDK self-test 1/1、animation registration focused test 1/1、SDK+animation locked check、data/model/shader importer family locked check、split importer focused `cargo check --locked`、D12 full rollout rustfmt、16-package D12 offline/locked check、M4 capability py_compile/audit JSON/scoped rustfmt、16-package M4 focused `cargo check --locked`、catalog `plugins_12_capability_single_source_conformance` focused test 1/1、importer M5/T1 10 包逐包 locked check、runtime-only M5/T1 5 包 offline/locked check、authoring runtime/editor M5/T1 三包 runtime offline check，以及 particles/physics/texture M5/T1 三包 runtime 与三包 editor offline check 已通过（仅既有 runtime/large-plugin warnings）；editor-only M5/T1 scoped rustfmt、py_compile 与 audit JSON 已通过，三包 editor `cargo check` 被无关 `zircon_editor` retained-host compile drift 阻塞，未计通过；authoring 三包 editor `cargo check --offline` 两次依赖构建超时，未计通过。editor feature/sample focused Cargo tests 仍为编译期超时、不计通过；model focused lib-test 被既有 `MaterialCaptureSeed` / `MaterialRuntime::capture_seed` runtime lib-test drift 阻断；sound/剩余 editor-runtime roots 与 M5 存量迁移仍未关闭。
   2026-06-23 更新：Plugins 12/13 已关闭 SDK builder/editor mirror guard、13-M1 dist 依赖边界 seed、13-M2 registration manifest ABI schema 与 system bridge replay 代码路径、13-M2/T3 dist entry helper、13-M3 单插件构建/carrier 判定/可重复包输出、13-M4 per-plugin zrpack focused guard、loader distribution compatibility gate、签名/hash + load manifest 汇编、13-M5/T2 CI matrix seed、12/13 RuntimePluginId string-newtype，以及 importer/runtime-only/editor-only/authoring skeleton owner 子切片。该段为历史快照；当前 skeleton debt 为 0，dist-capable 计数已由 Solari、AI、ZrVM Language、Navigation、Physics、Texture、Particles、Animation、Hybrid GI、Terrain、Tilemap 2D、Prefab Tools、Net、Rendering、Virtual Geometry、`asset_importer.data`、`asset_importer.model`、`asset_importer.shader`、`asset_importer.audio`、`asset_importer.texture`、`audio_importer`、`obj_importer`、`gltf_importer`、`texture_importer`、`opus_importer`、`shader_wgsl_importer`、`ui_document_importer`、`sound`、`runtime_diagnostics`、`native_window_hosting`、`ui_asset_authoring`、`material_editor`、`animation_graph`、`timeline_sequence`、`editor_build_export_desktop` 与 `plugin_sdk_examples` rollout 更新为 37；`plugin.toml` distribution 缺口为 0，仍需复跑更广 runtime loader/editor/export/full regression 阻塞测试。
   注：上方波次零长段与后续长段中的 `migration_debt_count = 11`、`migration_debt_count = 10`、`sound/剩余 ... pending` 均为对应子切片结束时的历史快照；当前权威计数见后续最新更新，skeleton debt 已清零，dist-capable 计数已由 `plugin_sdk_examples` editor-only rollout 更新为 37，distribution section 缺口为 0。
   2026-06-25 最新更新（Runtime Diagnostics editor-only dist rollout）：`plugins_13_m5_t1_runtime_diagnostics_editor_dist_rollout` 已把 editor-only `runtime_diagnostics` 扩成独立 dist 形态：新增 `runtime_diagnostics/dist` cdylib crate `zircon_plugin_runtime_diagnostics_dist`，`runtime_diagnostics/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；`plugin_sdk::dist` 同步新增 `native_dist_editor_plugin_v3!`，dependency-boundary audit 改为要求 `runtime_entry` / `editor_entry` 至少一个存在。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 30`、`dist_build_matrix_count = 30`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增 `runtime_diagnostics` editor-only dist code/CI/build rollout；editor focused manifest rerun 与其它非 dist 插件族仍未关闭。
   2026-06-25 最新更新（Native Window Hosting editor-only dist rollout）：`plugins_13_m5_t1_native_window_hosting_editor_dist_rollout` 已把 editor-only `native_window_hosting` 扩成独立 dist 形态：新增 `native_window_hosting/dist` cdylib crate `zircon_plugin_native_window_hosting_dist`，`native_window_hosting/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `native_window_hosting`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 31`、`dist_build_matrix_count = 31`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增 `native_window_hosting` editor-only dist code/CI/build rollout；editor focused manifest rerun 与其它非 dist 插件族仍未关闭。
   2026-06-25 最新更新（UI Asset Authoring editor-only dist rollout）：`plugins_13_m5_t1_ui_asset_authoring_editor_dist_rollout` 已把 editor-only `ui_asset_authoring` 扩成独立 dist 形态：新增 `ui_asset_authoring/dist` cdylib crate `zircon_plugin_ui_asset_authoring_dist`，`ui_asset_authoring/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `ui_asset_authoring`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 32`、`dist_build_matrix_count = 32`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增 `ui_asset_authoring` editor-only dist code/CI/build rollout；editor focused manifest rerun 与当时剩余 5 个非 dist 插件（`animation_graph`、`editor_build_export_desktop`、`material_editor`、`plugin_sdk_examples`、`timeline_sequence`）仍未关闭。
   2026-06-25 最新更新（Material Editor editor-only dist rollout）：`plugins_13_m5_t1_material_editor_editor_dist_rollout` 已把 editor-only `material_editor` 扩成独立 dist 形态：新增 `material_editor/dist` cdylib crate `zircon_plugin_material_editor_dist`，`material_editor/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `material_editor`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 33`、`dist_build_matrix_count = 33`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增 `material_editor` editor-only dist code/CI/build rollout；editor focused manifest test 604s 未到目标结果且无残留进程，未计通过；剩余 4 个非 dist 插件为 `animation_graph`、`editor_build_export_desktop`、`plugin_sdk_examples`、`timeline_sequence`。
   2026-06-25 最新更新（Animation Graph editor-only dist rollout）：`plugins_13_m5_t1_animation_graph_editor_dist_rollout` 已把 editor-only `animation_graph` 扩成独立 dist 形态：新增 `animation_graph/dist` cdylib crate `zircon_plugin_animation_graph_dist`，`animation_graph/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `animation_graph`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 34`、`dist_build_matrix_count = 34`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增 `animation_graph` editor-only dist code/CI/build rollout；editor focused manifest test 605s 未到目标结果，残留 target-dir cargo/rustc 已停止，未计通过；剩余 3 个非 dist 插件为 `editor_build_export_desktop`、`plugin_sdk_examples`、`timeline_sequence`。
   2026-06-25 最新更新（Timeline Sequence editor-only dist rollout）：`plugins_13_m5_t1_timeline_sequence_editor_dist_rollout` 已把 editor-only `timeline_sequence` 扩成独立 dist 形态：新增 `timeline_sequence/dist` cdylib crate `zircon_plugin_timeline_sequence_dist`，`timeline_sequence/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `timeline_sequence`。该切片当时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 35`、`dist_build_matrix_count = 35`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。后续 `editor_build_export_desktop` 与 `plugin_sdk_examples` rollouts 已把当前计数推进到 37/37，插件根 distribution 缺口为 0；Timeline Sequence editor focused manifest test 609s 未到目标结果且无残留 target-dir cargo/rustc，未计通过。
   2026-06-25 最新更新（Desktop Build Export editor-only dist rollout）：`plugins_13_m5_t1_editor_build_export_desktop_editor_dist_rollout` 已把 editor-only `editor_build_export_desktop` 扩成独立 dist 形态：新增 `editor_build_export_desktop/dist` cdylib crate `zircon_plugin_editor_build_export_desktop_dist`，`editor_build_export_desktop/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `editor_build_export_desktop`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 36`、`dist_build_matrix_count = 36`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增 Desktop Export editor-only dist code/CI/build rollout；editor focused manifest test 1/1 通过；剩余 1 个非 dist 插件为 `plugin_sdk_examples`。
   2026-06-25 最新更新（Plugin SDK Examples editor-only dist rollout）：`plugins_13_m5_t1_plugin_sdk_examples_editor_dist_rollout` 已把 editor-only `plugin_sdk_examples` 扩成独立 dist 形态：新增 `plugin_sdk_examples/dist` cdylib crate `zircon_plugin_sdk_examples_dist`，`plugin_sdk_examples/plugin.toml` 与 editor package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 editor entry，不伪造 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `plugin_sdk_examples`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 37`、`dist_build_matrix_count = 37`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`；`plugin.toml` distribution scan 报告 `count=0`。本切片关闭当前插件根 distribution 缺口；editor focused manifest test 904s 未到目标结果，残留 target-dir cargo/rustc 已停止，未计通过；sound optional feature dist 拆分与更广 runtime/editor/export/full regression 仍未关闭。
2026-06-23 历史更新：`plugins_12_particles_physics_texture_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_particles_physics_texture_skeleton_owner_rollout` 已覆盖 `particles`、`physics`、`texture` 的 runtime/editor owner 骨架，并把 skeleton debt 从 14 降到 11；随后 `plugins_12_editor_build_export_desktop_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_editor_build_export_desktop_skeleton_owner_rollout` 已覆盖 `editor_build_export_desktop` editor owner 骨架，将 skeleton debt 从 11 降到 10；`plugins_12_sound_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_sound_skeleton_owner_rollout` 覆盖 `sound` main runtime/editor 与两个 feature runtime/editor crate，将 skeleton debt 从 10 降到 9；本轮 `plugins_12_timeline_sequence_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_timeline_sequence_skeleton_owner_rollout` 覆盖 `timeline_sequence` editor owner 骨架，将当时 skeleton debt 从 9 降到 8；`python tools\audit_plugin_structure.py --json` 报告 `capability_source_mismatches = 0`、`skeleton_conformance.migration_debt_count = 8`、`plugin_skeleton_gate.migration_debt_count = 8`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`。本切片不新增 dist-capable 插件，也不关闭 13-M5/T1 full dual-form rollout；剩余 roots 为 `animation`、`animation_graph`、`hybrid_gi`、`material_editor`、`navigation`、`net`、`rendering`、`virtual_geometry`。
   2026-06-23 最新更新（Solari dist rollout）：`plugins_13_m5_t1_solari_dist_rollout` 已把首个真实一方 runtime 插件 `solari` 扩成独立 dist 形态：新增 `solari/dist` cdylib crate，`solari/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `solari`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 2`、`dist_build_matrix_count = 2`、`dist_capable_plugins = ["native_dynamic_fixture", "solari"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-23 最新更新（AI dist rollout）：`plugins_13_m5_t1_ai_dist_rollout` 已把第二个真实一方 runtime 插件 `ai` 扩成独立 dist 形态：新增 `ai/dist` cdylib crate，`ai/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `ai`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 3`、`dist_build_matrix_count = 3`、`dist_capable_plugins = ["ai", "native_dynamic_fixture", "solari"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-23 最新更新（ZrVM Language dist rollout）：`plugins_13_m5_t1_zr_vm_language_dist_rollout` 已把第三个真实一方 runtime 插件 `zr_vm_language` 扩成独立 dist 形态：新增 `zr_vm_language/dist` cdylib crate，`zr_vm_language/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `zr_vm_language`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 4`、`dist_build_matrix_count = 4`、`dist_capable_plugins = ["ai", "native_dynamic_fixture", "solari", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Navigation dist rollout）：`plugins_13_m5_t1_navigation_dist_rollout` 已把第四个真实一方 runtime 插件 `navigation` 扩成独立 dist 形态：新增 `navigation/dist` cdylib crate，`navigation/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `navigation`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 5`、`dist_build_matrix_count = 5`、`dist_capable_plugins = ["ai", "native_dynamic_fixture", "navigation", "solari", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Physics dist rollout）：`plugins_13_m5_t1_physics_dist_rollout` 已把第五个真实一方 runtime 插件 `physics` 扩成独立 dist 形态：新增 `physics/dist` cdylib crate，`physics/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `physics`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 6`、`dist_build_matrix_count = 6`、`dist_capable_plugins = ["ai", "native_dynamic_fixture", "navigation", "physics", "solari", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Texture dist rollout）：`plugins_13_m5_t1_texture_dist_rollout` 已把第六个真实一方 runtime 插件 `texture` 扩成独立 dist 形态：新增 `texture/dist` cdylib crate，`texture/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `texture`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 7`、`dist_build_matrix_count = 7`、`dist_capable_plugins = ["ai", "native_dynamic_fixture", "navigation", "physics", "solari", "texture", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Particles dist rollout）：`plugins_13_m5_t1_particles_dist_rollout` 已把第七个真实一方 runtime 插件 `particles` 扩成独立 dist 形态：新增 `particles/dist` cdylib crate，`particles/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `particles`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 8`、`dist_build_matrix_count = 8`、`dist_capable_plugins = ["ai", "native_dynamic_fixture", "navigation", "particles", "physics", "solari", "texture", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Animation dist rollout）：`plugins_13_m5_t1_animation_dist_rollout` 已把第八个真实一方 runtime 插件 `animation` 扩成独立 dist 形态：新增 `animation/dist` cdylib crate，`animation/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `animation`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 9`、`dist_build_matrix_count = 9`、`dist_capable_plugins = ["ai", "animation", "native_dynamic_fixture", "navigation", "particles", "physics", "solari", "texture", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Hybrid GI dist rollout）：`plugins_13_m5_t1_hybrid_gi_dist_rollout` 已把第九个真实一方 runtime 插件 `hybrid_gi` 扩成独立 dist 形态：新增 `hybrid_gi/dist` cdylib crate，`hybrid_gi/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `hybrid_gi`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 10`、`dist_build_matrix_count = 10`、`dist_capable_plugins = ["ai", "animation", "hybrid_gi", "native_dynamic_fixture", "navigation", "particles", "physics", "solari", "texture", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Terrain dist rollout）：`plugins_13_m5_t1_terrain_dist_rollout` 已把第十个真实一方 runtime 插件 `terrain` 扩成独立 dist 形态：新增 `terrain/dist` cdylib crate，`terrain/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `terrain`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 11`、`dist_build_matrix_count = 11`、`dist_capable_plugins = ["ai", "animation", "hybrid_gi", "native_dynamic_fixture", "navigation", "particles", "physics", "solari", "terrain", "texture", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Tilemap 2D dist rollout）：`plugins_13_m5_t1_tilemap_2d_dist_rollout` 已把第十一个真实一方 runtime 插件 `tilemap_2d` 扩成独立 dist 形态：新增 `tilemap_2d/dist` cdylib crate，`tilemap_2d/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `tilemap_2d`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 12`、`dist_build_matrix_count = 12`、`dist_capable_plugins = ["ai", "animation", "hybrid_gi", "native_dynamic_fixture", "navigation", "particles", "physics", "solari", "terrain", "texture", "tilemap_2d", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Prefab Tools dist rollout）：`plugins_13_m5_t1_prefab_tools_dist_rollout` 已把第十二个真实一方 runtime 插件 `prefab_tools` 扩成独立 dist 形态：新增 `prefab_tools/dist` cdylib crate，`prefab_tools/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `prefab_tools`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 13`、`dist_build_matrix_count = 13`、`dist_capable_plugins = ["ai", "animation", "hybrid_gi", "native_dynamic_fixture", "navigation", "particles", "physics", "prefab_tools", "solari", "terrain", "texture", "tilemap_2d", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Net dist rollout）：`plugins_13_m5_t1_net_dist_rollout` 已把第十三个真实一方 runtime 插件 `net` 扩成独立 dist 形态：新增 `net/dist` cdylib crate，`net/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `net`。当前 `python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.migration_debt_count = 0`、`standalone_distribution_conformance.dist_capable_plugin_count = 14`、`dist_build_matrix_count = 14`、`dist_capable_plugins = ["ai", "animation", "hybrid_gi", "native_dynamic_fixture", "navigation", "net", "particles", "physics", "prefab_tools", "solari", "terrain", "texture", "tilemap_2d", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍未关闭。
   2026-06-24 最新更新（Rendering dist rollout）：`plugins_13_m5_t1_rendering_dist_rollout` 已把第十四个真实一方 runtime 插件 `rendering` 扩成独立 dist 形态：新增 `rendering/dist` cdylib crate，`rendering/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；CI `plugin-standalone-dist` matrix 同步覆盖 `rendering`。该切片后 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 15`、`dist_build_matrix_count = 15`、`dist_dependency_boundary_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍需更广阻塞测试复跑。
   2026-06-24 最新更新（Virtual Geometry dist rollout）：`plugins_13_m5_t1_virtual_geometry_dist_rollout` 已把第十五个真实一方 runtime 插件 `virtual_geometry` 扩成独立 dist 形态：新增 `virtual_geometry/dist` cdylib crate，`virtual_geometry/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility 和 runtime entry；registration manifest 同步声明 runtime module 与三个渲染扩展贡献，CI `plugin-standalone-dist` matrix 同步覆盖 `virtual_geometry`。该切片后 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 16`、`dist_build_matrix_count = 16`、`dist_capable_plugins = ["ai", "animation", "hybrid_gi", "native_dynamic_fixture", "navigation", "net", "particles", "physics", "prefab_tools", "rendering", "solari", "terrain", "texture", "tilemap_2d", "virtual_geometry", "zr_vm_language"]`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 仍需更广阻塞测试复跑。
   2026-06-24 最新更新（Data Asset Importer dist rollout）：`plugins_13_m5_t1_asset_importer_data_dist_rollout` 已把 `asset_importer.data` family runtime 插件扩成独立 dist 形态：新增 `asset_importers/data/dist` cdylib crate，`asset_importers/data/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 TOML/JSON/YAML/XML data importer capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `asset_importer.data`。该切片后 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 17`、`dist_build_matrix_count = 17`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 最新更新（Model Asset Importer dist rollout）：`plugins_13_m5_t1_asset_importer_model_dist_rollout` 已把 `asset_importer.model` family runtime 插件扩成独立 dist 形态：新增 `asset_importers/model/dist` cdylib crate，`asset_importers/model/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 Mesh/CAD model importer capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `asset_importer.model`。该切片后 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 18`、`dist_build_matrix_count = 18`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 最新更新（Shader Asset Importer dist rollout）：`plugins_13_m5_t1_asset_importer_shader_dist_rollout` 已把 `asset_importer.shader` family runtime 插件扩成独立 dist 形态：新增 `asset_importers/shader/dist` cdylib crate，`asset_importers/shader/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 WGSL/Naga shader importer capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `asset_importer.shader`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 19`、`dist_build_matrix_count = 19`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 最新更新（Audio Asset Importer dist rollout）：`plugins_13_m5_t1_asset_importer_audio_dist_rollout` 已把 `asset_importer.audio` family runtime 插件扩成独立 dist 形态：新增 `asset_importers/audio/dist` cdylib crate，`asset_importers/audio/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 audio importer root/codec capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `asset_importer.audio`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 20`、`dist_build_matrix_count = 20`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 更新（Texture Asset Importer dist rollout）：`plugins_13_m5_t1_asset_importer_texture_dist_rollout` 已把 `asset_importer.texture` family runtime 插件扩成独立 dist 形态：新增 `asset_importers/texture/dist` cdylib crate，`asset_importers/texture/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 texture importer root/container/PSD capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `asset_importer.texture`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 21`、`dist_build_matrix_count = 21`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 最新更新（Audio Importer split importer dist rollout）：`plugins_13_m5_t1_audio_importer_dist_rollout` 已把 `audio_importer` split importer 扩成独立 dist 形态：新增 `audio_importer/dist` cdylib crate，`audio_importer/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 audio importer root/WAV/codec capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `audio_importer`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 22`、`dist_build_matrix_count = 22`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。本切片新增真实 dist-capable 插件，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 最新更新（OBJ Importer split importer dist rollout）：`plugins_13_m5_t1_obj_importer_dist_rollout` 已把 `obj_importer` split importer 扩成独立 dist 形态：新增 `obj_importer/dist` cdylib crate，`obj_importer/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 OBJ model importer root capability；CI `plugin-standalone-dist` matrix 同步覆盖 `obj_importer`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 23`、`dist_build_matrix_count = 23`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。真实 `plugin build obj_importer --form dist` 已输出 loader manifests 与 `obj_importer.sig`，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-24 最新更新（glTF Importer split importer dist rollout）：`plugins_13_m5_t1_gltf_importer_dist_rollout` 已把 `gltf_importer` split importer 扩成独立 dist 形态：新增 `gltf_importer/dist` cdylib crate，`gltf_importer/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 glTF model importer root capability；CI `plugin-standalone-dist` matrix 同步覆盖 `gltf_importer`。当前 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 24`、`dist_build_matrix_count = 24`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。真实 `plugin build gltf_importer --form dist` 已输出 loader manifests 与 `gltf_importer.sig`，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-25 最新更新（Texture Importer split importer dist rollout）：`plugins_13_m5_t1_texture_importer_dist_rollout` 已把 `texture_importer` split importer 扩成独立 dist 形态：新增 `texture_importer/dist` cdylib crate，`texture_importer/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 texture importer root/image/container/PSD capabilities；CI `plugin-standalone-dist` matrix 同步覆盖 `texture_importer`。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 25`、`dist_build_matrix_count = 25`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。真实 `plugin build texture_importer --form dist` 已输出 loader manifests 与 `texture_importer.sig`，但 13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-25 更新（Opus Importer split importer dist rollout）：`plugins_13_m5_t1_opus_importer_dist_rollout` 已把 `opus_importer` split importer 扩成独立 dist 形态：新增 `opus_importer/dist` cdylib crate，`opus_importer/plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和 audio.opus importer capability；CI `plugin-standalone-dist` matrix 同步覆盖 `opus_importer`。当时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 26`、`dist_build_matrix_count = 26`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。真实 `plugin build opus_importer --form dist` 已输出 loader manifests 与 `opus_importer.sig`；runtime package-manifest focused test 受外部并发 `zircon_runtime` Cargo/rustc lane 阻断，未计通过；13-M5/T1 full dual-form rollout 对其他非 dist 插件族仍未关闭。
   2026-06-25 前序更新（Shader WGSL Importer / UI Document Importer split importer dist rollout）：`plugins_13_m5_t1_shader_wgsl_importer_dist_rollout` 与 `plugins_13_m5_t1_ui_document_importer_dist_rollout` 已把两个剩余 split importer 扩成独立 dist 形态：新增 `shader_wgsl_importer/dist` 与 `ui_document_importer/dist` cdylib crate，两者 `plugin.toml` 与 runtime package manifest 声明 `[distribution]`、native module、NativeDynamic default packaging、ABI v3、engine compatibility、runtime entry 和各自 importer capability；CI `plugin-standalone-dist` matrix 同步覆盖两者。该切片结束时 `python tools\audit_plugin_structure.py --json` 报告 `standalone_distribution_conformance.dist_capable_plugin_count = 28`、`dist_build_matrix_count = 28`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`；sound rollout 后的当前权威状态见上方最新更新。真实 `plugin build shader_wgsl_importer --form dist` 与 `plugin build ui_document_importer --form dist` 已输出 loader manifests 与 `.sig`；runtime package-manifest focused test 仍需待外部 Cargo lanes 空闲后复跑。
2. **波次一**：01-M1/M2 + 03-M1 + 04-M1（架构核心与两个首批验证插件）
3. **波次二**：01-M3/M4 + 11-M1/M2 + 02-M1/M2 + 05-M1/M2 + 08-M1
4. **波次三**：03-M2..M4 + 04-M2..M4 + 05-M3..M5 + 06-M1/M2 + 07-M1..M3 + 11-M3
5. **波次四**：03-M5 + 04-M5 + 06-M3/M4 + 07-M4..M6 + 08-M2..M4 + 09-M1/M2 + **13-M3/M4（每插件独立构建命令 + 产物包/兼容性协商，与 09 共享 zrpack/CLI 底座）** + 01-M5 + 11-M4
6. **波次五**：全部 Editor 里程碑（按 10 规范，含 10-E1/E2）+ 09-M3..M6 + 08-M5 + **13-M5（双形态全量 rollout + 每插件 CI 构建矩阵）**

## 3. 现状基线摘要（2026-06 代码实查结论，v2 校准）

- **架构**：四层插件系统（package_manifest / runtime_plugin / extension_registry / native_plugin_loader）成型；**访问集（`SystemParamAccess`）、冲突图、并行批次、7 阶段 `SystemStage` 已存在**——真缺口是 SystemSet 偏序、registry 的 system/resource/event 注册通道、类型化扩展点冻结、owner 追踪、ABI 分域函数表（01 §2 缺口表 G1–G8）。
- **插件成熟度（实查比早期调研乐观）**：sound 自研执行栈功能面完整但热路径有硬伤，**已裁决以 kira 为执行核心整体替代自研混音/输出栈（02 v2.1），自研 DSP 数值迁移为 kira 自定义 Effect**；navigation 已 vendored upstream recastnavigation 且单块烘焙 bridge 在（缺 tiled/crowd/carving 闭环）；net 六 feature crate 已建（缺 worker 线程模型与深度实现）；physics/animation 契约 DTO 完整、核心算法缺（Jolt、约束、ragdoll、GPU skinning、avatar mask 执行）；ai 框架在、节点库/观察者中断/感知缺；zr_vm 为 wrapper 但**反射 DTO 家族与宿主注册表已在**（缺 derive/反向/性能层）；export 的 profile/验证/物化模板**已在**（缺 zrpack/CLI/平台模板）；terrain/tilemap_2d 为 stub（不在本轮范围）。
- **既有计划冲突已裁决**：Physics 后端以"Jolt（joltc-sys）唯一必交付 + builtin 降级"为准（03）；animation 归属以"已迁入 zircon_plugins"现实为准（04）；Tokio 不进 runtime 本体依赖、由 net 插件自建 worker（07）。

## 4. 全局验收

每波次收口必须全绿：

```bash
cargo build --workspace --locked
cargo test --workspace --locked
cargo check --manifest-path zircon_plugins/Cargo.toml --workspace --all-targets --locked
cargo test --manifest-path zircon_plugins/Cargo.toml --workspace --locked
cargo fmt --all --check
```

并保持四源一致性测试（plugin.toml / runtime descriptor / 派生 builtin catalog / workspace member）覆盖每个新增 feature 与模块；01-M4-T3 起新增 `system_anchors` 声明与实际注册的契约核对。

## 5. 与既有计划的关系

本目录是对 `.codex/plans/` 中相关计划（周边设施、Sound 核心、Physics+Animation、Net、导航、ZrVM、全量插件化收敛、多插件组合可选功能、插件注册与 EditorOperation、最小本体与发行导出）的**收敛与细化**：既定架构决策全部继承，冲突项在各分计划中显式裁决并标注；后续以本目录为插件生态的执行基线，`.codex/plans` 对应文档转为历史背景材料。

v2 较 v1 的实质变化：各计划的"现状基线"按代码实查重写（多处早期调研结论被修正，见各文档 §2 缺口表的证据列）；新增模块文件树、Rust 签名级 API、里程碑任务表（任务 id/改动文件/依赖/测试函数名）；跨文档接口（03↔04 ragdoll、05↔06 MoveTo、07↔09 zrpack、08↔07 schema、08↔10 drawer）双侧签名对齐。


## 6. 插件形态验收记录

本总览只汇总各子计划 `## 状态与产出记录` 的当前事实,不替代子计划状态表。完成判定以每个子计划最后一列的后续项和验证证据为准。

| 日期 | 子计划 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_runtime_diagnostics_editor_dist_rollout` | `runtime_diagnostics` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_runtime_diagnostics_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 `editor.extension.runtime_diagnostics` capability，且 runtime entry 为空。`plugin build runtime_diagnostics --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar。验证：scoped rustfmt、lockfile refresh、dependency_boundary py_compile、Runtime Diagnostics dist tests 2/2、SDK existing dist helper test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；该切片结束时 audit 报告 `dist_capable_plugin_count = 30`、`dist_build_matrix_count = 30`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | runtime_diagnostics editor-only dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 editor focused manifest test，并继续推进其它非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_native_window_hosting_editor_dist_rollout` | `native_window_hosting` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_native_window_hosting_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 `editor.extension.native_window_hosting` capability，且 runtime entry 为空。`plugin build native_window_hosting --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar。验证：scoped rustfmt、lockfile refresh、Native Window Hosting dist tests 2/2、CI matrix unittest、audit JSON 与真实 build smoke 均通过；该切片结束时 audit 报告 `dist_capable_plugin_count = 31`、`dist_build_matrix_count = 31`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | native_window_hosting editor-only dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 editor focused manifest test，并继续推进其它非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_ui_asset_authoring_editor_dist_rollout` | `ui_asset_authoring` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_ui_asset_authoring_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 `editor.extension.ui_asset_authoring` capability，且 runtime entry 为空。`plugin build ui_asset_authoring --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar。验证：scoped rustfmt、lockfile refresh、UI Asset Authoring dist tests 2/2、CI matrix unittest、audit JSON 与真实 build smoke 均通过；该切片结束时 audit `dist_capable_plugin_count = 32`、`dist_build_matrix_count = 32`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | ui_asset_authoring editor-only dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 editor focused manifest test，当时剩余 5 个非 dist 插件继续由后续切片推进。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_material_editor_editor_dist_rollout` | `material_editor` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_material_editor_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 `editor.extension.material_editor_authoring` capability，且 runtime entry 为空。`plugin build material_editor --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar；material graph authoring/compile/validation 仍留在 editor owner。验证：scoped rustfmt、lockfile refresh、Material Editor dist tests 2/2、CI matrix unittest、audit JSON 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 33`、`dist_build_matrix_count = 33`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`；editor focused manifest test 604s 未到目标结果，未计通过。 | material_editor editor-only dist code/CI/build rollout 已覆盖；继续推进剩余 4 个非 dist 插件（`animation_graph`、`editor_build_export_desktop`、`plugin_sdk_examples`、`timeline_sequence`）与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_animation_graph_editor_dist_rollout` | `animation_graph` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_animation_graph_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 `editor.extension.animation_graph_authoring` capability，且 runtime entry 为空。`plugin build animation_graph --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar；animation graph/state-machine authoring、node palette、asset editor、compile/validation 仍留在 editor owner。验证：scoped rustfmt、lockfile refresh、Animation Graph dist tests 2/2、CI matrix unittest、audit JSON 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 34`、`dist_build_matrix_count = 34`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`；editor focused manifest test 605s 未到目标结果，残留 target-dir cargo/rustc 已停止，未计通过。 | animation_graph editor-only dist code/CI/build rollout 已覆盖；继续推进剩余 3 个非 dist 插件（`editor_build_export_desktop`、`plugin_sdk_examples`、`timeline_sequence`）与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_timeline_sequence_editor_dist_rollout` | `timeline_sequence` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_timeline_sequence_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 `editor.extension.timeline_sequence_authoring` capability，且 runtime entry 为空。`plugin build timeline_sequence --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar；timeline authoring、track descriptors、keyframe move、event marker payload validation 与 sequence validation 仍留在 editor owner。验证：scoped rustfmt、lockfile refresh、Timeline Sequence dist tests 2/2、CI matrix unittest、audit JSON 与真实 build smoke 均通过；audit 当时 `dist_capable_plugin_count = 35`、`dist_build_matrix_count = 35`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`；editor focused manifest test 609s 未到目标结果且无残留 target-dir cargo/rustc，未计通过。 | timeline_sequence editor-only dist code/CI/build rollout 已覆盖；后续 `editor_build_export_desktop` 与 `plugin_sdk_examples` rollouts 已把当前计数推进到 37/37，插件根 distribution 缺口为 0；继续推进更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_editor_build_export_desktop_editor_dist_rollout` | `editor_build_export_desktop` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_editor_build_export_desktop_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 Desktop Export editor capabilities，且 runtime entry 为空。`plugin build editor_build_export_desktop --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar；Desktop Export panel、导出 operation/menu、NativeDynamic report templates 与 export profile authoring 仍留在 editor owner。验证：scoped rustfmt、lockfile refresh、Desktop Export dist tests 2/2、editor focused manifest test 1/1、CI matrix unittest、audit JSON 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 36`、`dist_build_matrix_count = 36`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | editor_build_export_desktop editor-only dist code/CI/build rollout 已覆盖；继续推进剩余 1 个非 dist 插件（`plugin_sdk_examples`）、sound optional feature dist 拆分与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_plugin_sdk_examples_editor_dist_rollout` | `plugin_sdk_examples` 已新增 editor-only fallback `<plugin>/dist/` cdylib crate `zircon_plugin_sdk_examples_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 editor package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/editor entry 与 SDK example editor capabilities，且 runtime entry 为空。`plugin build plugin_sdk_examples --form dist` 可物化根 loadable、native loader-compatible copy、loader manifest、native package report 与 `.sig` sidecar；example window、model importer、asset inspector 与 SDK fixture registration 仍留在 editor owner。验证：scoped rustfmt、lockfile refresh、Plugin SDK Examples dist tests 2/2、CI matrix unittest、audit JSON、`plugin.toml` distribution scan `count=0` 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 37`、`dist_build_matrix_count = 37`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`；editor focused manifest test 904s 未到目标结果，残留 target-dir cargo/rustc 已停止，未计通过。 | 当前插件根 distribution 缺口已关闭；sound optional feature dist 拆分与更广 runtime/editor/export/full regression 仍待后续长窗口复跑。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_sound_dist_rollout` | `sound` 主插件已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_sound_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 `runtime.plugin.sound` capability。`plugin build sound --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifest 与 `.sig` sidecar。验证：scoped rustfmt、dist test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 与 `cargo check --lib` 受外部 Cargo/rustc lanes 阻断，未计通过；该切片当时 audit 报告 `dist_capable_plugin_count = 29`、`dist_build_matrix_count = 29`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`，后续 editor-only rollout 已把当前计数更新为 37。 | sound main plugin dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 runtime focused test，sound optional feature dist 拆分与更广回归仍待推进。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_shader_wgsl_importer_dist_rollout` | `shader_wgsl_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_shader_wgsl_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 shader.wgsl importer capability。`plugin build shader_wgsl_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 受外部 editor Cargo/rustc lanes 阻断，未计通过；该切片 audit 报告 `dist_capable_plugin_count = 28`、`dist_build_matrix_count = 28`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | shader_wgsl_importer split importer dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 runtime focused test，并继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_ui_document_importer_dist_rollout` | `ui_document_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_ui_document_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 ui_document importer capability。`plugin build ui_document_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 尚未在外部 Cargo lanes 空闲后复跑；该切片 audit 报告 `dist_capable_plugin_count = 28`、`dist_build_matrix_count = 28`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | ui_document_importer split importer dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 runtime focused test，并继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_gltf_importer_dist_rollout` | `gltf_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_gltf_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 glTF model importer root capability。`plugin build gltf_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、runtime package-manifest focused test、CI matrix unittest、cargo metadata、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 首次长编译未产出目标结果，修正枚举断言后 no-debug target 复跑 1/1 通过；audit 当前 `dist_capable_plugin_count = 24`、`dist_build_matrix_count = 24`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | gltf_importer split importer dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_texture_importer_dist_rollout` | `texture_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_texture_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 texture importer root/image/container/PSD capabilities。`plugin build texture_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、runtime package-manifest focused test、CI matrix unittest、cargo metadata、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 前两次 305s 编译未产出目标结果，随后以 20 分钟 timeout 和 no-debug target 复跑 1/1 通过；该切片结束时 audit `dist_capable_plugin_count = 25`、`dist_build_matrix_count = 25`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | texture_importer split importer dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-25 | 13-M5/T1 | `plugins_13_m5_t1_opus_importer_dist_rollout` | `opus_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_opus_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 audio.opus importer capability。`plugin build opus_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、CI matrix unittest、cargo metadata、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 受外部并发 `zircon_runtime` Cargo/rustc lane 阻断，未计通过；audit 当时 `dist_capable_plugin_count = 26`、`dist_build_matrix_count = 26`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | opus_importer split importer dist code/CI/build rollout 已覆盖；待编译通道空闲后复跑 runtime focused test，并继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_asset_importer_texture_dist_rollout` | `asset_importer.texture` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_asset_importer_texture_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 texture importer root/container/PSD capabilities。`plugin build asset_importer.texture --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist check/test、runtime package-manifest focused test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 首次并行编译 1204s 超时未计通过，no-debug target 复跑 1/1 通过；该切片结束时 audit `dist_capable_plugin_count = 21`、`dist_build_matrix_count = 21`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | texture importer family dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_obj_importer_dist_rollout` | `obj_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_obj_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 OBJ model importer root capability。`plugin build obj_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、runtime package-manifest focused test、CI matrix unittest、cargo metadata、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 前两次 cold compile 未产出目标测试结果，残留同 target-dir cargo 进程自然结束后 no-debug target 复跑 1/1 通过；audit 该切片结束时 `dist_capable_plugin_count = 23`、`dist_build_matrix_count = 23`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | obj_importer split importer dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_audio_importer_dist_rollout` | `audio_importer` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_audio_importer_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 audio importer root/WAV/codec capabilities。`plugin build audio_importer --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist test、runtime package-manifest focused test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 首次 cold compile 未产出目标测试结果，清理同 target-dir 进程后 no-debug target 复跑 1/1 通过；该切片结束时 audit `dist_capable_plugin_count = 22`、`dist_build_matrix_count = 22`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | audio_importer split importer dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_asset_importer_audio_dist_rollout` | `asset_importer.audio` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_asset_importer_audio_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 audio importer root/codec capabilities。`plugin build asset_importer.audio --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist check/test、runtime package-manifest focused test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 首次 604s 编译超时未计通过，停止残留后以 no-debug target 复跑 1/1 通过；audit 当前 `dist_capable_plugin_count = 20`、`dist_build_matrix_count = 20`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | audio importer family dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_asset_importer_shader_dist_rollout` | `asset_importer.shader` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_asset_importer_shader_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 WGSL/Naga shader importer capabilities。`plugin build asset_importer.shader --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist check/test、runtime package-manifest focused test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 首次 904s 编译超时未计通过，停止残留后复跑 1/1 通过；audit 当前 `dist_capable_plugin_count = 19`、`dist_build_matrix_count = 19`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | shader importer family dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_asset_importer_model_dist_rollout` | `asset_importer.model` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_asset_importer_model_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 Mesh/CAD model importer capabilities。`plugin build asset_importer.model --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist check/test、runtime package-manifest focused test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；runtime package-manifest focused test 首次编译超时未计通过，复跑 1/1 通过；audit 该切片后 `dist_capable_plugin_count = 18`、`dist_build_matrix_count = 18`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | model importer family dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_asset_importer_data_dist_rollout` | `asset_importer.data` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_asset_importer_data_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility、descriptor/runtime entry 与 TOML/JSON/YAML/XML capabilities。`plugin build asset_importer.data --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、`native_plugins.toml` manifests 与 `.sig` sidecar。验证：scoped rustfmt、dist check/test、runtime package-manifest focused test、CI matrix unittest、audit JSON 与真实 build smoke 均通过；audit 该切片后 `dist_capable_plugin_count = 17`、`dist_build_matrix_count = 17`、`dist_dependency_boundary_violations = 0`、`distribution_section_violations = 0`。 | data importer family dist rollout 已覆盖；继续推进其他非 dist 插件族与更广 runtime/editor/export 回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_virtual_geometry_dist_rollout` | `virtual_geometry` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_virtual_geometry_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry，registration manifest 声明 runtime module 与三个渲染扩展贡献。`plugin build virtual_geometry --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Virtual Geometry dist check/test、production runtime check 与真实 build smoke 均通过；runtime focused unit test 被无关渲染测试夹具漂移阻塞；audit 当前 `dist_capable_plugin_count = 16`、`dist_build_matrix_count = 16`、`dist_dependency_boundary_violations = 0`。 | 当前审计集的一方 runtime-root dist rollout 已覆盖；继续复跑更广 runtime/editor/export 阻塞测试与全量回归。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_rendering_dist_rollout` | `rendering` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_rendering_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build rendering --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Rendering dist check/test、Rendering runtime focused test 与真实 build smoke 均通过；audit 该切片后 `dist_capable_plugin_count = 15`、`dist_build_matrix_count = 15`、`dist_dependency_boundary_violations = 0`。 | 继续推进 Virtual Geometry dist 形态并复跑更广阻塞测试。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_net_dist_rollout` | `net` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_net_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build net --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Net dist check/test、Net runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 14`、`dist_build_matrix_count = 14`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_prefab_tools_dist_rollout` | `prefab_tools` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_prefab_tools_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build prefab_tools --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Prefab Tools dist check/test、Prefab Tools runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 13`、`dist_build_matrix_count = 13`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_tilemap_2d_dist_rollout` | `tilemap_2d` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_tilemap_2d_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build tilemap_2d --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Tilemap 2D dist check/test、Tilemap 2D runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 12`、`dist_build_matrix_count = 12`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_terrain_dist_rollout` | `terrain` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_terrain_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build terrain --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Terrain dist check/test、Terrain runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 11`、`dist_build_matrix_count = 11`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_hybrid_gi_dist_rollout` | `hybrid_gi` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_hybrid_gi_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build hybrid_gi --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Hybrid GI dist check/test 与真实 build smoke 均通过；runtime focused unit test 被无关 `zircon_runtime` render handle/value semantics 编译漂移阻塞，未计通过；audit 当前 `dist_capable_plugin_count = 10`、`dist_build_matrix_count = 10`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_animation_dist_rollout` | `animation` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_animation_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build animation --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Animation dist check/test、Animation runtime scoped check、Animation runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 9`、`dist_build_matrix_count = 9`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_particles_dist_rollout` | `particles` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_particles_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build particles --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Particles dist check/test、Particles runtime scoped check、Particles runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 8`、`dist_build_matrix_count = 8`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_texture_dist_rollout` | `texture` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_texture_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build texture --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Texture dist check/test、Texture runtime scoped check、Texture runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 7`、`dist_build_matrix_count = 7`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_physics_dist_rollout` | `physics` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_physics_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build physics --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Physics dist check/test、Physics runtime scoped check、Physics runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 6`、`dist_build_matrix_count = 6`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-24 | 13-M5/T1 | `plugins_13_m5_t1_navigation_dist_rollout` | `navigation` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_navigation_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build navigation --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Navigation dist check/test、Navigation runtime scoped check 与真实 build smoke 均通过；runtime focused unit test 被无关 `zircon_runtime` UI surface private method / trait import 编译漂移阻塞，未计通过；audit 当前 `dist_capable_plugin_count = 5`、`dist_build_matrix_count = 5`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-23 | 13-M5/T1 | `plugins_13_m5_t1_zr_vm_language_dist_rollout` | `zr_vm_language` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_zr_vm_language_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build zr_vm_language --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、ZrVM Language dist check/test、ZrVM Language runtime scoped check 与真实 build smoke 均通过；runtime focused unit test 的 manifest 入口断言已修正，但复跑被无关 `zircon_runtime/src/ui/component/catalog/editor_showcase.rs` `numeric` 未定义编译漂移阻塞，未计通过；audit 当前 `dist_capable_plugin_count = 4`、`dist_build_matrix_count = 4`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-23 | 13-M5/T1 | `plugins_13_m5_t1_ai_dist_rollout` | `ai` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_ai_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build ai --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml`、`plugins/native_plugins.toml` 与 `.sig` sidecar。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、AI dist check/test、AI runtime focused test 与真实 build smoke 均通过；audit 当前 `dist_capable_plugin_count = 3`、`dist_build_matrix_count = 3`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；全量双形态 rollout 仍 pending。 |
| 2026-06-23 | 13-M5/T1 | `plugins_13_m5_t1_solari_dist_rollout` | `solari` 已新增 fallback `<plugin>/dist/` cdylib crate `zircon_plugin_solari_dist`，只依赖 `zircon_plugin_sdk` native ABI；`plugin.toml` 与 runtime package manifest 均声明 `[distribution]`、native module、NativeDynamic packaging、ABI v3、engine compatibility 与 runtime entry。`plugin build solari --form dist` 可物化根 loadable、`native/<dist_crate>` loader 兼容副本、根 `native_plugins.toml` 与 `plugins/native_plugins.toml`。验证：scoped rustfmt、py_compile、`test_plugin_build` 4/4、CI matrix unittest、audit JSON、Solari dist check/test、Solari runtime focused test、真实 build smoke 与插件 workspace locked check 均通过；该切片结束时 audit 报告 `dist_capable_plugin_count = 2`、`dist_build_matrix_count = 2`、`dist_dependency_boundary_violations = 0`。 | 继续按能力波次扩展其它一方插件的 dist 形态；根 `zircon_runtime` focused locked check 被 root `Cargo.lock` 更新需求阻塞，待根锁文件状态收束后复跑。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_final_skeleton_owner_rollout_clears_migration_debt` / `plugins_13_m5_t1_final_skeleton_owner_rollout` | 最后 8 个 skeleton debt roots（`animation`、`animation_graph`、`hybrid_gi`、`material_editor`、`navigation`、`net`、`rendering`、`virtual_geometry`）已迁入 owner 骨架：runtime/editor crate roots 均退为薄 façade，feature crate 同步拥有 feature-local `capability.rs` / `plugin.rs` owner。`python tools\audit_plugin_structure.py --json` 报告 `plugin_skeleton_gate.m2_gate_status = sample-clean-migration-debt-clear`、`skeleton_conformance.migration_debt_count = 0`、`plugin_skeleton_gate.migration_debt_count = 0`、`migration_debt_roots = []`、`capability_conformance.capability_source_mismatches = 0`、`missing_capability_owner_files = 0`、`missing_runtime_capability_exports = 0`；139 个 touched owner/façade 文件 rustfmt check 通过；`cargo check --manifest-path zircon_plugins\Cargo.toml --workspace --offline --locked` 在补齐 `hybrid_gi` placeholder mesh 的 `RenderLayerSet::from_legacy_mask(...)` 后通过（仅既有 warning 噪声）。 | Plugins 12 M5/T1 skeleton migration debt 已清零；该历史行完成时 Plugins 13 仍只有 `native_dynamic_fixture` seed，当前 dist-capable 计数已由后续 rollout 更新为 37，最新见上方 `plugins_13_m5_t1_plugin_sdk_examples_editor_dist_rollout`。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_timeline_sequence_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_timeline_sequence_skeleton_owner_rollout` | `timeline_sequence/editor` 已迁入 owner 骨架：`capability.rs` 拥有插件 id/editor capability 与 animation timeline event track capability，`extension_ids.rs` 拥有 view/drawer/template ids，`plugin.rs` 拥有 editor plugin descriptor/manifest/registration、operation/menu 与 timeline authoring batch helper，`tests.rs` 承接 focused package/registration/validation/keyframe/event marker 断言；`lib.rs` 退为薄 façade 并保留 timeline authoring 领域 helper。`python tools\audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 8`、`plugin_skeleton_gate.migration_debt_count = 8`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`、`capability_conformance.capability_source_mismatches = 0`；scoped rustfmt 通过。`cargo check` 被当前无关 `zircon_runtime` render 编译漂移挡在目标 timeline_sequence crate 前，未计通过。 | 剩余 8 个 skeleton debt roots：`animation`、`animation_graph`、`hybrid_gi`、`material_editor`、`navigation`、`net`、`rendering`、`virtual_geometry`；13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_sound_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_sound_skeleton_owner_rollout` | `sound` main runtime/editor 与 `ray_traced_convolution_reverb`、`timeline_animation_track` feature runtime/editor crate 已迁入 owner 骨架：runtime/editor `plugin.rs` 承接 trait owner 和 descriptor/manifest/registration/helper，`capability.rs` 承接 capability 单源，editor `extension_ids.rs` 承接 authoring ids，`tests.rs` 承接 focused package/registration 断言；`lib.rs` 均退为薄 façade。`python tools\audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 9`、`plugin_skeleton_gate.migration_debt_count = 9`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`、`capability_conformance.capability_source_mismatches = 0`；scoped rustfmt 通过。`cargo check` 被当前无关 `zircon_runtime` render 编译漂移挡在目标 sound crate 前，未计通过。 | 剩余 9 个 skeleton debt roots：`animation`、`animation_graph`、`hybrid_gi`、`material_editor`、`navigation`、`net`、`rendering`、`timeline_sequence`、`virtual_geometry`；13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_editor_build_export_desktop_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_editor_build_export_desktop_skeleton_owner_rollout` | `editor_build_export_desktop` editor crate 已迁入 owner 骨架：`capability.rs` 拥有插件 id/editor capabilities，`extension_ids.rs` 拥有 view/drawer/template/report/operation ids 与私有模板路径，`plugin.rs` 拥有 editor plugin descriptor/manifest/registration、导出 operation/menu、NativeDynamic report templates 与 export profile authoring 注册，`tests.rs` 承接 package/registration/template asset 断言；`lib.rs` 退为 74 行薄 façade。`python tools\audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 10`、`plugin_skeleton_gate.migration_debt_count = 10`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`、`capability_conformance.capability_source_mismatches = 0`；scoped rustfmt 与该 editor crate `cargo check --all-targets --offline` 均通过。 | 剩余 10 个 skeleton debt roots、sound/剩余 runtime/editor rollout 与 13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_particles_physics_texture_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_particles_physics_texture_skeleton_owner_rollout` | `particles`、`physics`、`texture` 的 runtime/editor crate 已迁入 owner 骨架：runtime `plugin.rs` + 薄 `lib.rs` façade，editor `capability.rs`、`extension_ids.rs`、`plugin.rs`、`tests.rs`；`texture` 另拆 `manager.rs`/`module.rs`，`particles` CPU 快照构造补齐 `render_layer_mask`。`python tools/audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 11`、`plugin_skeleton_gate.migration_debt_count = 11`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`、`capability_conformance.capability_source_mismatches = 0`；scoped rustfmt、三包 runtime `cargo check --offline` 与三包 editor `cargo check --offline` 均通过。 | 剩余 11 个 skeleton debt roots、sound/剩余 editor-runtime rollout 与 13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_authoring_runtime_editor_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_authoring_runtime_editor_skeleton_owner_rollout` | `prefab_tools`、`terrain`、`tilemap_2d` 的 runtime crate 已拆成 `plugin.rs`、`capability.rs`、`tests.rs` owner，editor crate 已拆成 `authoring.rs`、`capability.rs`、`extension_ids.rs`、`plugin.rs`、`tests.rs` owner，两个 `lib.rs` 退为薄 façade。`python tools/audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 14`、`plugin_skeleton_gate.migration_debt_count = 14`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`、`capability_conformance.capability_source_mismatches = 0`；scoped rustfmt 与三包 runtime `cargo check --offline` 通过，三包 editor `cargo check --offline` 两次依赖构建超时。 | 该快照 skeleton debt roots = 14；sound/剩余 editor-runtime rollout 与 13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_editor_only_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_editor_only_skeleton_owner_rollout` | `native_window_hosting`、`runtime_diagnostics`、`ui_asset_authoring` 的 editor crate 已拆成 `capability.rs`、`extension_ids.rs`、`plugin.rs`、`tests.rs` owner，`lib.rs` 退为薄 façade。`python tools/audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 17`、`plugin_skeleton_gate.migration_debt_count = 17`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`、`capability_conformance.capability_source_mismatches = 0`；scoped rustfmt 与 py_compile 通过，三包 editor `cargo check` 被无关 `zircon_editor` retained-host compile drift 阻塞。 | 剩余 17 个 skeleton debt roots、sound/剩余 editor-runtime rollout 与 13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_runtime_only_skeleton_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_runtime_only_skeleton_owner_rollout` | `ai`、`solari`、`zr_vm_language` 与 `asset_importers/{audio,texture}` 已迁入 runtime-only `plugin.rs`/`capability.rs` owner 骨架；`asset_importers/audio` 与 `asset_importers/texture` 补 SDK workspace dependency，从 legacy declaration-only `package_manifest()` 收口为 trait-backed runtime plugin。`python tools/audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 20`、`plugin_skeleton_gate.migration_debt_count = 20`、`standalone_distribution_conformance.dist_capable_plugin_count = 1`；scoped rustfmt、offline cargo check 与 locked cargo check 通过。 | 剩余 20 个 skeleton debt roots、editor/sound 更广 rollout 与 13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T1 / 13-M5/T1 | `plugins_12_importer_capability_owner_rollout_reduces_migration_debt` / `plugins_13_m5_t1_importer_capability_owner_rollout` | 7 个 split importer 与 `asset_importers/{data,model,shader}` 已新增 `runtime/src/capability.rs`，`lib.rs` 退为薄 façade + re-export；`python tools/audit_plugin_structure.py --json` 报告 `skeleton_conformance.migration_debt_count = 25`、`plugin_skeleton_gate.migration_debt_count = 25`，较上一状态减少 10 个 importer roots。10 个 importer runtime package 逐包 locked `cargo check` 均 exit=0；`standalone_distribution_conformance.dist_capable_plugin_count = 1` 未变。 | 剩余 25 个 skeleton debt roots、`asset_importers/audio` / `asset_importers/texture` legacy declaration-only roots、sound/editor 更广 rollout 与 13-M5/T1 full dual-form rollout 仍 pending。 |
| 2026-06-23 | 12-M5/T2 / 13-M5/T3 | `plugins_12_runtime_plugin_id_string_newtype_accepts_external_ids` / `plugins_13_runtime_plugin_id_string_newtype_accepts_external_ids` | `RuntimePluginId` 已从封闭 enum 改为开放 string-newtype；内建插件 id 保留关联常量，合法第三方 key 可由 `parse_key(...)` / `new(...)` 承载并稳定序列化为字符串；loader 对未知合法 runtime plugin id 走 externalized diagnostic fallback，插件 id 不再要求 engine core 新增 match arm。scoped rustfmt、runtime `core-min` cargo check 与 plugin SDK locked check 通过；focused runtime lib-test 被无关 test-tree 编译漂移阻塞，未计通过。 | 13-M5/T1 全量双形态 rollout 仍 pending；runtime focused test 待无关 lib-test 编译漂移清理后复跑。 |
| 2026-06-23 | 13-M5/T2 | `plugins_13_standalone_dist_ci_matrix_covers_dist_capable_plugins` | `.github/workflows/ci.yml` 已新增 `plugin-standalone-dist` job，当时 matrix 覆盖 `native_dynamic_fixture`；`standalone_distribution_conformance.dist_build_matrix_entries` / `dist_build_matrix_count` 由 dependency-boundary audit 输出，`test_plugin_standalone_ci_matrix.py` 锁定 CI matrix 与审计集合一致。py_compile、focused unittest 1/1、audit JSON 与真实 fixture locked dist build 均通过。 | 当前 matrix 已由后续 rollout 扩到 37，并包含 editor-only `runtime_diagnostics`、`native_window_hosting`、`ui_asset_authoring`、`material_editor`、`animation_graph`、`timeline_sequence`、`editor_build_export_desktop` 与 `plugin_sdk_examples`；distribution 缺口为 0，13-M5/T1 仍需 sound optional feature dist 拆分和更广阻塞测试复跑。 |
| 2026-06-23 | 13-M4/T3 | `plugins_13_native_plugin_load_manifest_assembles_signed_entries` | `plugin_build.py` 已生成包内 `<id>.sig` hash sidecar，支持外部 signer 在 package report 前执行，并在 `<out>/native_plugins.toml` 汇编 `NativePluginLoadManifest` ABI v3 条目；`native_dynamic_package.toml` payload 收录签名后的 loadable artifact 与 `.sig`。`python -m unittest tools.zircon_export.tests.test_plugin_build` 4/4、py_compile 与 carrier focused unittest 通过。 | 13-M5 全量 rollout 仍 pending；M4/T1 真实 Cargo pack smoke 604s 超时仍未计通过。 |
| 2026-06-23 | 13-M4/T2 | `plugins_13_loader_distribution_compatibility_gate` | `PluginPackageManifest` 已承载 typed `[distribution]`；runtime native loader 在探测库路径前校验 `forms`、`abi_version` 与 `engine_compat`，不匹配时输出诊断并跳过插件，不再继续报缺库。scoped rustfmt 与 `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked` 通过；focused lib-test 被无关 runtime test-tree 编译漂移阻塞，未计通过。 | 13-M4/T3 已由上方记录关闭；13-M5 全量 rollout 仍 pending。 |
| 2026-06-23 | 13-M4/T1 | `plugins_13_native_dynamic_package_includes_plugin_zrpack` | `plugin_build.py` 已读取 `[distribution].assets`，生成 Pack 阶段输入清单并调用 `zircon_export_pack` / `--packer` 写出 `<id>.zrpack`；`native_dynamic_package.toml` payload manifest 会收录 `.zrpack`。`native_dynamic_fixture` 新增 `assets/shader.wgsl` 并声明 `assets = ["assets/**"]`。Python focused `test_plugin_build` 3/3 通过；真实 Cargo pack smoke 604s 超时，未计通过。 | 13-M4/T2 已由上方记录关闭；13-M4/T3 签名/hash/load manifest 汇编与 13-M5 全量 rollout 仍 pending。 |
| 2026-06-23 | 13-M3/T3 | `plugins_13_plugin_dist_build_is_byte_reproducible` | `tools/zircon_export/tests/test_plugin_build.py::test_plugin_dist_build_is_byte_reproducible` 双跑同一插件 dist package 并比较包目录 bytes 映射；真实 `native_dynamic_fixture` 复用同一 target dir 双跑到两个 out 目录，`native_dynamic_fixture.dll`、`plugin.toml` 与 `native_dynamic_package.toml` sha256 一致。py_compile、`python -m unittest tools.zircon_export.tests.test_plugin_build` 2/2 与真实包比较 `MATCH` 均通过。 | 13-M4/T1 已由上方记录关闭；13-M4/T3 与 13-M5 全量 rollout 仍 pending。 |
| 2026-06-23 | 13-M3/T2 | `plugins_13_zircon_build_classifies_forms_from_manifest` | `tools/zircon_build.py` 已记录 `distribution_forms` / `dist_crate_name`，并在 manifest 声明 `[distribution].forms` 时按 `dist`→`native_dynamic`、`embed`→`rlib_static` 判定 carrier；无 forms legacy manifest 仍走 crate-type 回退。`tools/tests/test_zircon_build_plugin_carriers.py` 覆盖 dist-only、embed-only 与 legacy cdylib 分类；py_compile 与 focused unittest 1/1 通过。 | 13-M3/T3 与 13-M4/T1 已由上方记录关闭；13-M4/T3 与 13-M5 全量 rollout 仍 pending。 |
| 2026-06-23 | 13-M3/T1 | `plugins_13_plugin_build_emits_isolated_package_dir` | `tools/zircon_export/plugin_build.py` 与 CLI 分派已支持 `python -m tools.zircon_export plugin build <id>`；当前 `native_dynamic_fixture` 可用独立 target dir 编译 `--no-default-features --features dist --locked`，并物化 `<out>/native_dynamic_fixture/{native_dynamic_fixture.dll, plugin.toml, native_dynamic_package.toml}`。Python focused test 1/1 与真实 fixture 构建/打包 smoke 通过。 | 13-M3/T2/T3 与 13-M4/T1 已由上方记录关闭；13-M4/T3 与 13-M5 全量 rollout 仍 pending。 |
| 2026-06-23 | 13-M2/T3 | `plugins_13_dist_entry_helper_compiles` | `zircon_plugin_sdk::dist` 已提供 `native_dist_plugin_v3!` / `native_dist_runtime_plugin_v3!`，一份声明生成 ABI descriptor、runtime/editor entry report、behavior schema/manifest 指针、bridge method table 与 symbol export；`native_dynamic_fixture` 已改用该 helper 并声明 `native_dynamic_fixture.runtime.tick` method slot。SDK focused `dist_plugin_one_file_export_compiles` 1/1 通过，fixture `--no-default-features --features dist` check 通过。 | 13-M3/T1/T2/T3 与 13-M4/T1 已由上方记录关闭；13-M4/T3 与 13-M5 全量 rollout 仍 pending。 |
| 2026-06-23 | 12-M4/T2 | `plugins_12_sdk_builder_mirror_guard_passed` | `PluginFeatureBundleBuilder` 与 `EditorPluginDeclaration::mirrors_runtime(...)` 已接入 SDK；审计报告 `sdk_builder_mirror_violations = 0`、`m4_t2_builder_mirror_gate_status = sdk-builder-mirror-clean`；默认 SDK check、feature bundle focused test、SDK editor feature check 通过。 | sound/importer/editor 更广 rollout 仍 pending；M5 open/custom `RuntimePluginId` 已由上方记录关闭。 |
| 2026-06-23 | 13-M2/T2 | `plugins_13_registration_bridge_replay_implemented` | runtime loader/live host 已解析 registration manifest 并把 system anchor 回放进 `RuntimeExtensionRegistry`，宿主 system tick 通过 dense bridge scope 调插件 method；focused test `dist_system_plugin_loads_and_ticks_via_bridge` 已新增到独立 test owner；scoped rustfmt 通过。 | focused cargo test 被无关 `runtime_absorption/structure_convention/test_file_budget.rs` missing modules 阻塞，未计通过；13-M2/T3、13-M3/T1/T2/T3 与 13-M4/T1 见上方记录，13-M4/T3 与 13-M5 仍 pending。 |
| 2026-06-23 | 13-M2/T1 | `plugins_13_registration_manifest_schema_round_trips` | SDK/runtime loader ABI v3 已承载 `registration_manifest_schema` 与 `registration_manifest`；SDK TOML DTO 覆盖 module/system/resource/event/extension/capability registration manifest；`native_dynamic_fixture` runtime entry 已导出 `zircon.native.registration-manifest/3` runtime registration manifest；SDK native-only check、focused round-trip test、native fixture dist check 与 audit/rustfmt 通过。 | 13-M2/T2 replay focused test 仍需在 runtime lib-test compile drift 清理后重跑；M2/T3、13-M3/T1/T2/T3 与 13-M4/T1 见上方记录，13-M4/T3 与 13-M5 仍 pending。 |
| 2026-06-23 | 13-M1 | `plugins_13_dist_dependency_boundary_clean` | `native_dynamic_fixture` 已作为首个独立 dist 样例纳入 `[distribution]` + `cdylib` + `dist` feature + dependency-boundary audit；审计报告 `dist_capable_plugin_count = 1`、`distribution_section_violations = 0`、`dist_dependency_boundary_violations = 0`、`m1_dist_dependency_boundary_gate_status = dist-boundary-clean`；native fixture dist check 与 catalog guard test 通过。 | 13-M2 ABI 编组、13-M3/T1/T2/T3 与 13-M4/T1 已由上方记录推进；13-M4/T3 与 13-M5 全量 rollout 仍 pending。 |
