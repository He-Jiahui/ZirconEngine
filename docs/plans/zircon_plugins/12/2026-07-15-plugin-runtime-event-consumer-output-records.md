# Plugins 12 runtime event consumer 产出记录

> 来源：[`12-plugin-dx-and-structure-framework.md`](../12-plugin-dx-and-structure-framework.md) 的 Plugin Editor/Runtime mirror Failure 优先修复切片。

## 产出记录与时间

| 日期 | 里程碑 | 切片 | 状态 | 证据 |
|---|---|---|---|---|
| 2026-07-15 | M4 exact manifest 独立复核 | runtime event consumer 16-file 精确提交可重放性与所有权边界 | `plugins_12_exact_manifest_review_p0_0_p1_3_p2_1_submission_blocked` | 独立复核结论为 P0=0、P1=3、P2=1，当前 exact manifest 不可提交。P1-1：旧 `failure-2026-07-13-plugin-structure-audit-report-fixture-drift.md` 只删除旧端，但计划链接的 Editor09 `fixed-2026-07-15-plugin-structure-audit-report-fixture-drift.md` 仍未跟踪且不在 manifest，必须由 Editor09 先落地或在合法 lifecycle commit 同步提交。P1-2：共享 `zircon_runtime_interface/src/{lib.rs,runtime_api.rs,runtime_api/api_table.rs}` 已包含 Editor03 operation 模块/V2 tail，但未跟踪的 `runtime_api/operation.rs` 被排除，exact commit 无法编译且会错误吸收 Editor03 hunks。P1-3：event mirror 缺少必要的 `scene/event_mirror/mod.rs`、`scene/mod.rs`、`scene/world/{mod,world,bootstrap}.rs` 与 `dynamic_api/tests/mod.rs` 接线；当前 `scene/tests/mod.rs` 已启用测试，单独重放无法解析公开 mirror/World 状态。P2：公开 `RuntimeEventMirrorSubscription` 在 connected 状态直接 drop 时没有 world-owned cleanup，直接 World 消费者的 reader/callback 计数可能保留到 World 销毁；产品 session 路径已有显式退订/失败重试，暂不阻断 M4。代码语义本身 V2-only 与 refcount rollback 无 P0/P1；Python report tests 4/4、handoff validator 141/141、exact paths `git diff --check` 通过，本轮未新增 Cargo 运行。 |
| 2026-07-15 | M4 capability 单源 / D9 editor-runtime mirror | typed ECS event mirror、V2-only 动态 ABI、PIE consumer host 与 Navigation 实际产品链 | `plugins_12_runtime_event_consumer_focused_gates_passed_shared_commit_pending` | Runtime 新增按稳定 event-id 独立计数的 opt-in serde mirror；普通 ECS reader 不再影响 mirror callback，Navigation `NavigationDebugCapture` 严格随 mirror 订阅数 0→1→N→0 切换。动态宿主只消费完整 19-field `ZrRuntimeApiV2`，V1 table/export/loader fallback 已按 Runtime10 hard cutover 清零；mirror/operation required group 缺失会显式拒绝装载。`RuntimePluginRegistrationReport` 经 linked plan 安装 module、scene hook 与 extension 到真实 `LevelSystem` World，app/editor 产品路径实际推进 runtime tick。`first_party_editor_catalog` 根文件已收束为薄门面，项目 manifest 由 `catalog.rs` 投影为 Navigation editor registration；PIE host 使用独立 generation、逐帧 capability reconcile、跨 session/schema/sequence 校验，并在 unsubscribe false/error 时保留本地 ownership 供后续重试。公共 linked-session 与 editor consumer 边界使用 typed error/source chain。原 644 行 `session.rs` 已拆成约 52 行门面、305 行 `ffi.rs`、263 行 `state.rs`、56 行 `hooks.rs` 与 285 行 `construction.rs`，Runtime03/07/10 审计和 Rust 镜像守卫同步跟随真实 owner。当前 Python 组合单测 24/24 通过；真实插件结构报告 `missing_plugin_toml = 0`、schema/capability/mirror/registration/skeleton violations 全 0；动态 API 结构审计 48/48、loader failure anchors 13/13、legacy V1 hits `[]` 且 `risks = []`，schedule/input/performance 旁路审计均 `risks = []`。Windows 受管 focused Cargo 已全部通过，Navigation app 产品门禁见 job `188a8df88f10431c8240845ad440dd05`（1/1），代码语义独立复审 0 Critical / 0 Important；exact manifest 的 3 组 P1 另见本表首行。共享 `zircon_app/src/entry/runtime_library/tests.rs` 同时包含 Editor03 ABI/operation 改动，本计划不吸收该外部 blob，等待其 owner 提交后再完成精确提交边界。 |
| 2026-07-15 | M4 lowest-layer event generation | schedule stage 收尾不再重放无条件 internal systems | `plugins_12_event_generation_single_rotation_completed` | Fresh linked-session gate 证明第二 tick 仍为空，最低层定位为 `SceneScheduleRunner` 已在有序步骤中执行 `UpdateEvents`，stage 收尾又通过 `run_internal_scene_systems_for_stage` 重放一次，导致 `current` 当帧被清空。收尾现改为 `flush_pending_scene_systems_for_stage`，仅补跑 `DerivedStateDirty::should_run` 为真的派生系统；`UpdateEvents` / `ApplyDeferred` 不再重放。world-driver 行为测试锁定首 tick 空、次 tick 得到上一帧事件，结构守卫禁止旧调用回流；Windows 受管验证依次获得 1/1、3/3、3/3、6/6 通过，独立复核 0 Critical / 0 Important。该 7-file 精确里程碑已通过协调器提交为 `663537c7`；完整证据见 [`2026-07-15-m4-event-generation-single-rotation-closeout.md`](2026-07-15-m4-event-generation-single-rotation-closeout.md)。 |
| 2026-07-15 | M4 linked runtime modules | linked plugin module descriptor 进入 CoreRuntime 注册与依赖序激活 | `plugins_12_linked_module_registration_completed_product_gate_passed` | `RuntimeExtensionRegistry::modules()` 现与 core modules 一样先完整注册，再由 `activate_registered_modules()` 按依赖顺序统一激活；linked-session 测试用 lazy driver 强制证明模块服务可在 runtime system tick 中解析。Windows 受管 job `af8e3d41fb514d0f960f8d8a4de56546`：3 passed / 0 failed；独立复核 0 Critical / 0 Important，动态 API 审计 `risks = []`。Frameworks05 text owner hard-cut 修复回传后，app Navigation 产品门禁首次进入目标测试并暴露首帧期望漂移；最低层 linked-session 测试已规定双 generation 语义为首帧 drain 空、次帧取得上一帧事件，因此产品测试改为首帧 `pump = 0`、次帧 `pump = 1`。重跑 Windows 受管 job `188a8df88f10431c8240845ad440dd05`：1 passed / 0 failed；独立只读复核 0 Critical / 0 Important，确认 `UpdateEvents` 位于 First、Navigation 发事件位于 Update，stage 收尾不会再次旋转 generation。 |
| 2026-07-15 | 优先结构 / 审查发现复核 | `engine-code-structure-convention` 与 2026-06 review findings 的 Plugins12 owner 项 | `plugins_12_priority_structure_review_findings_live_audit_completed` | Fresh 插件结构审计继续报告 manifest、native identity、capability、editor/runtime mirror、registration builder、compatibility shim、skeleton migration debt、distribution boundary 违规全部为 0；D-S7/D3、D-S8、D1、D5、D7–D13 的完成证据仍由 2026-07-09 编号归档持有。当前新增 owner 未堆叠大文件：`dynamic_api/session.rs` 51 行，子 owner 最大 304 行；app runtime session 480 行，editor consumer host 393 行；无生产文件达到约 1000 行阈值。root façade、typed error/source chain、V2 hard cut 和 `registration_compatibility_shim_sites = 0` 共同证明本切片未新增兼容层、root 行为或多职责大文件。此结论只关闭 Plugins12 对两份总纲的承接项，不把仍为 `in_progress` 的全引擎 review findings 总计划标完成。 |
