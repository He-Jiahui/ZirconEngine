# 06 · AI 插件完善计划（行为树 / Blackboard / Perception）

```zircon-workflow
{
  "schema": 1,
  "workflow_id": "zircon-plugins-ai",
  "goal": "完成 AI 插件的行为树、Blackboard、跨子系统任务、Perception 与 Editor 产品闭环",
  "milestones": [
    {"id": "M1", "title": "BT 编译与标准节点库", "depends_on": []},
    {"id": "M2", "title": "Blackboard 与观察者中断", "depends_on": ["M1"]},
    {"id": "M3", "title": "Navigation、Animation 与 ZrVM 集成节点", "depends_on": ["M1", "M2"]},
    {"id": "M4", "title": "Perception", "depends_on": ["M1", "M2"]},
    {"id": "M5", "title": "Editor 与调试", "depends_on": ["M3", "M4"]}
  ]
}
```

> 状态：工程化细化版 v2 · 优先级：P2 · 前置：[01 插件架构核心](01-plugin-architecture-core.md) M1–M2；[05 Navigation](05-navigation.md) M3（MoveTo 节点）
> 现状文档：`docs/zircon_plugins/ai/runtime.md`
> 参考实现：Unreal BehaviorTree（Composite/Decorator/Service/Task 四分类 + Blackboard key 观察者 + 条件中断）、Unreal AIPerception（Sight/Hearing 刺激模型）
> 当前失败交接：[AI runtime root lockfile drift](06/failure-2026-07-15-ai-runtime-root-lockfile-drift.md)

## 1. 目标

把 `zircon_plugins/ai` 从 Experimental 推进到可生产使用：完整行为树节点库与资产化、类型安全 blackboard、观察者驱动的条件中断、视觉/听觉感知系统、与 navigation/animation 的标准交接节点、行为树可视化编辑与运行时调试。

## 2. 现状基线（实查）

成熟度 Experimental / Partial。

- **契约层** `zircon_runtime/src/core/framework/ai/`：`behavior_tree.rs`（`AiBehaviorNodeKind`：Selector/Sequence/Parallel/Decorator/Service/Task/Subtree 粗分类、`AiBehaviorNodeDescriptor`/`AiBehaviorTreeDescriptor`/参数 DTO）、`blackboard.rs`（**类型化 schema DTO 已在**：`AiBlackboardValueType`/`AiBlackboardValue`/`AiBlackboardEntry`/`AiBlackboardKeyDescriptor`/`AiBlackboardSchemaDescriptor`）、`perception.rs`、`snapshot.rs`、`tick.rs`、`ids.rs`、`manager.rs`（`AiManager` trait）。
- **runtime** `zircon_plugins/ai/runtime/src/manager/`：behavior_tree/blackboard/execution/parameters/perception/service/snapshot/state/tick/validation（validation 较厚，校验规则成体系）；注册经 `register_module`。无 editor crate。

缺口（按严重度）：

| # | 缺口 | 证据 |
|---|------|------|
| B1 | 节点库：`AiBehaviorNodeKind` 只有粗分类，具体节点（Cooldown/TimeLimit/RandomSelector/Wait/MoveTo…）靠参数字符串区分，无注册化节点目录 | `framework/ai/behavior_tree.rs:7` |
| B2 | 执行内核：轮询式逐 tick 遍历，无 observer aborts（任意子树打断 + 状态清理）——需要换内核 | `manager/execution.rs` |
| B3 | Blackboard：schema DTO 在，运行期无 dense slot 存储、无世代号、无 key 观察者 | `manager/blackboard.rs` |
| B4 | Perception 近乎占位（无视锥/遮挡/遗忘/预算） | `manager/perception.rs` |
| B5 | 无 navigation/animation/VM 标准集成节点 | — |
| B6 | 无 `ai.behavior_tick` 系统锚点（Module 轮询）；无 editor | `module.rs` |

## 3. 架构设计

中立契约维持在 `zircon_runtime::core::framework::ai`；执行实现在 `zircon_plugins/ai/runtime`。

### 3.1 行为树资产与编译（解决 B1，`runtime/src/behavior_tree/` [manager/behavior_tree.rs 收编]）

- `.btree.toml` 资产 → 契约 `AiBehaviorTreeDescriptor`（现 DTO）→ 加载期编译：

```rust
/// dense 节点数组，索引寻址，树结构全 agent 共享；每 agent 仅持轻量运行态。
pub struct CompiledBehaviorTree {
    pub nodes: Box<[CompiledNode]>,          // 前序排列，子节点区间 [first_child, last_child]
    pub services: Box<[CompiledService]>,    // 按宿主 composite 分组
    pub blackboard_schema: BlackboardLayout, // §3.2
}
pub struct CompiledNode {
    pub node: NodeImplId,                    // 节点目录 dense id（见下）
    pub params: ParamBlockRef,               // 预解析参数块（含 blackboard key → slot）
    pub children: core::ops::Range<u32>,
    pub observer: Option<ObserverDecl>,      // decorator 的 abort 声明
}
pub struct TreeInstanceState { /* 每节点 1 字节状态 + 活跃路径栈 + cooldown 计时数组 */ }
```

- 节点目录：`register_bt_node(BtNodeDescriptor)` 走 01 的 `TypedExtensionPoint` 实例（key = 节点名，value = 工厂 + 语义元数据），Rust 插件与 VM（[08 ZrVM](08-zr-vm.md) §3.2 `register_bt_node` 通道）都可注册；标准库节点同样经此目录注册（dogfooding）。
- 标准节点库（v1 必交付，Unreal 形态四分类）：
  - **Composite**：`Selector`、`Sequence`、`Parallel { policy: AnySucceeds|AllSucceed }`、`RandomSelector`（权重参数）。
  - **Decorator**：`BlackboardCondition`（key 条件 + observer abort）、`Cooldown`、`TimeLimit`、`Loop { count|infinite }`、`Inverter`、`ForceResult`。
  - **Service**：`UpdateBlackboardDistance` 等周期回调（间隔 + 抖动参数）。
  - **Task**：`Wait`、`MoveTo`、`PlayAnimation`、`SetBlackboard`、`EmitEvent`、`RunSubTree`（契约 Subtree kind）、`ScriptTask`（VM 回调）。

### 3.2 Blackboard 运行期（解决 B3，`runtime/src/blackboard/` [收编]）

```rust
/// 编译期由 AiBlackboardSchemaDescriptor 排布：按类型分区的 dense slot。
pub struct BlackboardLayout { /* key 名 → BlackboardSlot { type, offset }，加载期一次解析 */ }
#[derive(Clone, Copy)] pub struct BlackboardSlot(u32);
pub struct BlackboardStore {
    bools: Box<[bool]>, ints: Box<[i64]>, floats: Box<[f32]>,
    vectors: Box<[Vec3]>, entities: Box<[Option<EntityId>]>, strings: Box<[String]>, assets: Box<[Option<AssetRef>]>,
    generations: Box<[u32]>,                 // 每 slot 写入世代号
    observers: Box<[SmallVec<ObserverRef>]>, // slot → 订阅的 decorator
}
impl BlackboardStore {
    pub fn write<T: BlackboardValueKind>(&mut self, slot: BlackboardSlot, value: T) -> WriteOutcome; // 值变更才 bump 世代并通知
}
```

- 观察者中断（解决 B2 的内核需求）：key 写入 → 通知订阅 decorator → 按 `AbortPolicy { None, Self_, LowerPriority, Both }` 打断——执行内核（`execution.rs` **重写**为 `runtime/src/behavior_tree/executor.rs`）改为显式状态机：活跃路径栈 + 打断请求队列，tick 入口先处理打断（自高优先级向下清理被打断子树的运行态：cooldown 保留、Wait/MoveTo 等 Task 收到 `on_abort` 回调），再继续正常推进。`manager/validation.rs` 既有校验规则全部迁移保留。

### 3.3 Perception（解决 B4，`runtime/src/perception/` [收编]）

- 组件：`AiPerceptionSource { channels: SIGHT|HEARING 位集, strength }`、`AiPerceptionReceiver { sight_fov_degrees, sight_range, hearing_radius, forget_seconds }`。
- `ai.perception_tick` ∈ Update（before `ai.behavior_tick`，显式约束）：分帧预算扫描（`AiTickBudget` 资源：每帧最多 N 对 receiver×source）；sight 遮挡走 physics raycast 可选依赖（`finish` 探测 `runtime.capability.physics.raycast`，无 physics 退化为距离+视锥）；结果写 `PerceivedStimuli` 快照（契约 `snapshot.rs` 扩展），blackboard service 消费。
- 事件刺激适配器：`AnimationEvent`/`SoundPlaybackEvent` 等总线事件可注册为 hearing 刺激源。

### 3.4 ECS 集成（解决 B6，对接 01 定稿 API）

- `ai.behavior_tick` ∈ Update（01 锚点表；`navigation.agent_tick` 在其后由 navigation 侧声明，MoveTo 在下一次 behavior tick 消费导航上报）；`ai.perception_tick` ∈ Update before `ai.behavior_tick`。
- agent tick LOD：按距相机距离分档降频（每 agent 帧间隔 1/2/4），档位计算在 behavior_tick 内联。
- `register_event::<BtNodeResultEvent>`（调试流，M5 编辑器消费）；`register_resource::<AiTickBudget>`。

### 3.5 与 Navigation 的交接（与 [05 Navigation](05-navigation.md) §3.4 对偶，双方逐字一致）

`MoveTo` Task 只写 `NavMeshAgent.destination` 并消费目标限定的 `NavAgentTickReport` 生产结果，不直接调用 NavWorld 查询；首次建立目标时忽略同目标旧结果，后续三态映射为：`arrived_agents` → Success，`no_path_agents` → Failure，其余 → Running。`OffMeshTraverseEvent` 仍由 navigation 子系统维护，不作为任务完成条件。

## 4. 模块文件树

```
zircon_plugins/ai/runtime/src/
  behavior_tree/compile.rs      [新增] descriptor → CompiledBehaviorTree
  behavior_tree/executor.rs     [新增] 重写执行内核（打断队列 + 活跃路径栈），替换 manager/execution.rs
  behavior_tree/catalog.rs      [新增] BtNodeDescriptor 节点目录（TypedExtensionPoint 实例）
  behavior_tree/nodes/{composite,decorator,service,task}.rs  [新增] 标准库
  behavior_tree/nodes/integration.rs  [新增] MoveTo/PlayAnimation/ScriptTask
  blackboard/{layout,store,observer}.rs  [新增] 收编 manager/blackboard.rs
  perception/{components,scan,stimuli,adapter}.rs  [新增] 收编 manager/perception.rs
  manager/*                     [改造] Manager API 对接新内核；execution.rs 删除
  lib.rs                        [改造] register_system/resource/event + 节点目录注册
zircon_plugins/ai/editor/       [新增 crate] M5
```

## 5. 里程碑与任务分解

### M1 BT 编译与标准节点库

| 任务 | 内容 | 改动文件 | 依赖 | 新增测试 |
|------|------|---------|------|---------|
| M1-T1 | CompiledBehaviorTree + .btree.toml 加载编译 | behavior_tree/compile.rs | — | `compiled_tree_preorder_ranges_are_consistent` |
| M1-T2 | 节点目录（TypedExtensionPoint 实例）+ 标准库注册 | catalog.rs、nodes/* | 01-M2、M1-T1 | `standard_node_catalog_snapshot` |
| M1-T3 | 执行内核重写（无打断版先行）+ validation 规则迁移 | executor.rs | M1-T1 | 节点语义矩阵测试（每节点 Success/Failure/Running 三态）`node_semantics_matrix` |
| M1-T4 | ai.behavior_tick 系统注册 + LOD 降频 | lib.rs | 01-M1、M1-T3 | `behavior_tick_anchor_in_update`、`lod_tiers_reduce_tick_rate` |

#### M1 完成状态（2026-07-13）

- [x] M1-T1：版本化 `.btree.toml` 经完整 DTO 校验后编译为前序 dense node 与独立 direct-child index table。
- [x] M1-T2：18 个标准节点统一经 `TypedExtensionPoint` 目录注册，并通过 `ai.behavior_node_registry.v1` 强类型运行时接口接受 Rust 插件工厂贡献。
- [x] M1-T3：`manager/execution.rs` 已硬切换到 `behavior_tree/executor.rs`；标准节点 Success/Failure/Running 语义矩阵、运行分支续跑和每 agent 状态均通过。
- [x] M1-T4：真实 `ai.behavior_tick` Update 系统、事件目录与 Full/Half/Quarter LOD 已注册，跳帧累计时间保持计时节点的墙钟语义。
- [x] M1 Testing：Windows 受管 target 上 44/44 tests 通过，标准验证矩阵通过；产出与边界证据见 [2026-07-13-ai-m1-output-records.md](06/2026-07-13-ai-m1-output-records.md)。

> 当前总计划仍为 `Experimental / Partial`：M2 Blackboard/observer abort、M3 真实跨子系统任务、M4 Perception 与 M5 Editor 均未完成，不计入本次 M1 验收。
>
> 外部编译失败已修复回传：[UiResourceRef hard-cutover 遗留导入](06/fixed-2026-07-13-runtime-interface-ui-resource-ref-hard-cutover.md)。owner 已硬切到 `value::UiResourceRef`，M1 owner-revoke barrier 验证恢复并通过。

### M2 Blackboard 与观察者中断

| 任务 | 内容 | 改动文件 | 依赖 | 新增测试 |
|------|------|---------|------|---------|
| M2-T1 | BlackboardLayout/Store（dense + 世代号） | blackboard/* | M1-T1 | `schema_layout_round_trip`、`write_same_value_does_not_bump_generation` |
| M2-T2 | 观察者 + AbortPolicy 进内核（打断队列、on_abort 清理） | executor.rs、observer.rs | M2-T1、M1-T3 | `lower_priority_abort_timing_contract`、`abort_preserves_cooldown_state` |

### M3 集成节点（前置：05-M3、08-M2）

- [x] **M3.1 MoveTo、PlayAnimation 与 ScriptTask 真实集成节点。** M3-T1–T3 已完成；MoveTo 使用目标限定的导航生产结果，ScriptTask 通过最终合并后的弱生命周期 bridge import 调用 ZrVM provider。
- [ ] **M3.2 巡逻-发现-追逐端到端场景。** 对应 M3-T4，依赖 M4 Perception，当前明确 deferred 至 M4 完成后执行。

| 任务 | 内容 | 改动文件 | 依赖 | 新增测试 |
|------|------|---------|------|---------|
| M3-T1 | MoveTo（NavMeshAgent.target + 事件订阅三态映射） | nodes/integration.rs | 05-M3 | `move_to_maps_arrival_and_failure_to_node_result` |
| M3-T2 | PlayAnimation（状态机参数/trigger 写入） | nodes/integration.rs | 04-M3 | `play_animation_sets_parameter_and_completes` |
| M3-T3 | ScriptTask（VM 回调经 host handle） | nodes/integration.rs | 08-M2 | `script_task_round_trips_through_mock_vm` |
| M3-T4 | 端到端：巡逻-发现-追逐场景 | tests | M3-T1..T3、M4 | `patrol_detect_chase_scenario` |

### M4 Perception

| 任务 | 内容 | 改动文件 | 依赖 | 新增测试 |
|------|------|---------|------|---------|
| M4-T1 | 组件 + 分帧预算扫描 + 遗忘 | perception/* | M1-T4 | `scan_budget_caps_pairs_per_frame`、`stimulus_forgotten_after_timeout` |
| M4-T2 | sight 遮挡（physics 可选依赖 + 退化） | perception/scan.rs | 01-M3 | `sight_blocked_by_raycast`、`no_physics_falls_back_to_cone_test` |
| M4-T3 | 事件刺激适配器 | perception/adapter.rs | M4-T1 | `sound_event_registers_hearing_stimulus` |

### M5 Editor 与调试

| 任务 | 内容 | 改动文件 | 依赖 | 新增测试 |
|------|------|---------|------|---------|
| M5-T1 | 行为树图编辑器（register_graph_editor + node palette 来自节点目录，布局 `ai-behavior-tree-layout.png`） | ai/editor | M1-T2、[10 规范](10-editor-integration.md) | editor 契约测试 |
| M5-T2 | 运行时节点状态高亮（BtNodeResultEvent 只读镜像）+ blackboard 监视面板 | ai/editor | M2、M5-T1 | 镜像通道契约测试 |
| M5-T3 | perception 调试 overlay（视锥/刺激，布局 `ai-ai-perception-layout.png`） | ai/editor | M4 | overlay 注册快照测试 |

## 6. 验收命令

```bash
cargo test --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_ai_runtime --locked
```

## 7. 风险

- observer aborts 要求"任意子树打断 + 状态清理"，`execution.rs` 直接换内核而非增补（激进重构原则）；`validation.rs` 既有校验规则全部迁移保留，作为重写回归网。
- ScriptTask 依赖 [08](08-zr-vm.md) M2 回调通道，M3-T3 排期在其后；M3-T1/T2 不受影响。
- 打断语义的边界（Parallel 分支中断、Service 在打断后的停止时机）按 Unreal 语义裁决并写节点语义矩阵测试固化，避免后续编辑器联调期重谈语义。

## 8. 附录 · dev 参考源码对位

实现各任务前**必须先读对应参考实现**，节点语义与打断时序对照真实代码核对，禁止凭空实现：

| 设计点 | 参考源码（已核验存在） | 看什么 |
|--------|----------------------|--------|
| 行为树四分类节点库 + observer aborts（最重要） | `dev/UnrealEngine/Engine/Source/Runtime/AIModule/Classes/BehaviorTree/`（Composites/Decorators/Services/Tasks 子目录） | 每个标准节点的三态语义、abort 请求的传播与活跃分支清理顺序、Service 的 tick 间隔抖动——M1/M2 的节点语义矩阵直接以此为判例 |
| Blackboard key 观察者 | `dev/UnrealEngine/Engine/Source/Runtime/AIModule/`（Classes/BehaviorTree 下 Blackboard 相关类型） | key 观察者通知 → decorator 重求值 → abort 的完整链路 |
| Perception 刺激模型 | `dev/UnrealEngine/Engine/Source/Runtime/AIModule/Classes/Perception/` | sight 视锥+遮挡判定顺序、刺激 age/遗忘、listener 更新预算 |
