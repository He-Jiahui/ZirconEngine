# 10 · 插件编辑器集成规范（Editor Plugin 约定与 AI Workbench 风格）

> 状态：工程化细化版 v2 · 优先级：P1（横切规范，随各插件 Editor 里程碑落地）
> 关联：`zircon_editor/src/core/editor_plugin.rs`（`EditorPlugin` trait）、`zircon_editor/src/core/editor_extension.rs`（`EditorExtensionRegistry`）
> UI 参考：`docs/ui-and-layout/ai-workbench-style/`（STYLE-NOTES.md + 62 张 workbench 布局图）

## 1. 目标

为所有插件的 Editor 部分确立统一的注册约定、面板布局语言与调试设施规范，使每个 runtime 插件的编辑器体验一致、可被 capability gate 管理，并为"editor 部分多为骨架"的现状给出共同验收标准。

## 2. 现状基线（实查）

- `EditorPlugin` trait（`editor_plugin.rs:226`）：`descriptor()` / `package_manifest()` / `editor_capabilities()` / `register_editor_extensions(&mut EditorExtensionRegistry)` / `on_lifecycle_event(&EditorPluginLifecycleEvent)`——对接 01 §3.4 后与 RuntimePlugin 四阶段对称（lifecycle event 即对称化挂点，01-M4 范围）。
- `EditorExtensionRegistry`（`editor_extension.rs`）**15 个扩展点已存在**：`register_view` / `register_drawer` / `register_menu_item` / `register_component_drawer` / `register_ui_template` / `register_asset_importer` / `register_asset_editor` / `register_asset_creation_template` / `register_viewport_tool_mode` / `register_graph_editor` / `register_graph_node_palette` / `register_timeline_editor` / `register_timeline_track_type` / `register_operation`（进 `EditorOperationRegistry`）。
- capability gate 原语已在：`EditorExtensionRegistration::with_required_capabilities` / `is_enabled_by`（`editor_extension.rs:271-316`）；缺统一验证管线（01-M4 交付）。
- undo/redo 现状：`core/editing/history.rs`（EditorHistory）+ `ui/host/editor_event_execution/undo_policy.rs` + workbench `editor_state.rs` 各持局部历史——E2 的收口对象；`core/editor_operation.rs` 已有 operation stack 雏形。
- 诊断：`zircon_plugins/runtime_diagnostics` 插件存在（rolling store 宿主）。

## 3. 扩展点使用约定（签名级）

插件 Editor 部分一律实现 `EditorPlugin` trait，经 `EditorExtensionRegistry` 注册，禁止旁路接线编辑器内部状态。各扩展点的使用规则（描述符均为 `editor_extension.rs` 现有类型）：

| 扩展点（现有签名） | 用途 | 命名规则 |
|--------|------|---------|
| `register_view(ViewDescriptor::new(id, display_name, category))` | 插件主面板（Mixer Console、Navigation Bake、BT Debugger 等） | `view.<plugin>.<name>` |
| `register_component_drawer(ComponentDrawerDescriptor::new(component_type, ui_document, controller))` | 插件组件 Inspector 绘制器 | 组件全名 `<Plugin>.Component.<Type>` |
| `register_asset_editor` / `register_asset_creation_template` | 插件资产（.btree.toml、.ragdoll.toml、.avatar_mask.toml、.znavmesh…）打开/新建 | 按资产扩展名路由 |
| `register_graph_editor` / `register_graph_node_palette` | 节点图类编辑（动画状态机、行为树、未来 shader graph） | 共享图编辑基座，插件只提供 node palette + 语义校验 |
| `register_timeline_editor` / `register_timeline_track_type(TimelineTrackDescriptor)` | 时间轴类（animation sequence、sound automation） | track type 全名 `<plugin>.track.<name>` |
| `register_viewport_tool_mode(ViewportToolModeDescriptor)` | 视口工具（navmesh 烘焙范围、碰撞体编辑手柄） | overlay 绘制走 gizmos 通道 |
| `register_operation` → `EditorOperationRegistry` | 一切可撤销编辑动作 | `XXX.YYY.ZZZ` 命名（既定规则），进统一 EditorOperationStack（E2） |
| `register_menu_item(EditorMenuItemDescriptor::new(path, operation))` | 菜单入口 | Unity 风格路径 `Tools/<Plugin>/<Action>`；debug overlay 一律 `View/Debug Overlays/<Plugin>` |

对接 [01 计划](01-plugin-architecture-core.md) M4 的新增要求：

- Editor 注册同样走 capability gate：缺失 capability 产出 `RegistrationDiagnostic`（`zircon_runtime_interface/src/plugin_diagnostics.rs`，01-M4-T1）而非静默禁用；`EditorPluginDescriptor` 的 capability 从 `plugin.toml` 单源派生（01-M4-T2）。
- 组件 drawer 默认实现由反射描述自动生成（E1，见 §6），插件只为需定制的字段注册覆盖绘制器。

## 4. AI Workbench 风格对位表

各插件主面板以 `docs/ui-and-layout/ai-workbench-style/` 对应布局图为版式基准（三栏 workbench：左结构树 / 中主编辑区 / 右属性检查器，遵循 STYLE-NOTES.md 间距与层级 token）：

| 插件面板 | 参考布局图 | 交付里程碑 |
|----------|-----------|-----------|
| Sound Mixer Console | 沿用现有 `mixer_console.v2.ui.toml`，token 对齐 STYLE-NOTES | [02](02-sound.md) M5 |
| Physics 碰撞调试 / Ragdoll 编辑 | `ai-physics-collision-layout.png` | [03](03-physics.md) M6 |
| Animation 状态机 / Blend Space / Sequencer | `ai-blend-space-layout.png`、`ai-sequencer-layout.png`、`ai-montage-editor-layout.png` | [04](04-animation.md) M6 |
| Navigation 烘焙与 Agent 调试 | `ai-navmesh-ai-layout.png` | [05](05-navigation.md) M6 |
| AI 行为树编辑/调试、Perception | `ai-behavior-tree-layout.png`、`ai-ai-perception-layout.png` | [06](06-ai.md) M5 |
| Net 诊断 / Replication 配置 | `ai-console-diagnostics-layout.png` | [07](07-net.md) M7 |
| 导出向导 | `ai-build-export-layout.png` | [09](09-export-publishing.md) M6 |

面板实现走 retained host 的 `.ui.toml` 模板体系（与 `docs/zircon_editor/ui/retained_host/` 契约一致），经 `register_ui_template(EditorUiTemplateDescriptor)` 注册；不引入 Slint 旁路（zircon_hub 独占 Slint 的边界维持）。

## 5. 运行时调试设施规范（横切，每插件 Editor 里程碑必交付）

1. **Viewport overlay**：空间型插件（physics/navigation/sound volume/ai perception）提供可开关视口叠加——gizmos 通道绘制，菜单统一 `View/Debug Overlays/<Plugin>`（`register_menu_item` + overlay 开关 operation）；overlay 注册清单进契约快照测试。
2. **诊断接入**：运行时指标（physics step 耗时、GC 暂停、net 带宽、audio underrun）进 rolling diagnostics store——插件在 runtime 侧注册 `DiagnosticPath`（`runtime_diagnostics` 插件的路径目录），editor 侧面板自动呈现，插件 editor 不写自有图表。
3. **Play-in-editor 状态镜像**：行为树节点状态、状态机当前状态、agent 路径等运行态经 editor 运行时客户端**只读镜像**到对应面板——数据走类型化事件（`BtNodeResultEvent` 等）或反射读请求（`ReflectReadRequest`，`zircon_runtime_interface::reflect` 现有 DTO），编辑器不直接持有 runtime world 引用（维持 ABI 边界）。

## 6. 前置基建两项（单列工程项）

### E1 反射驱动默认 drawer（前置：[08 ZrVM](08-zr-vm.md) M1 derive 宏）

- 裁决：默认 drawer 采用**运行期构建**（由 `ReflectTypeInfo` + `ReflectEditorHint` 即时生成控件树），不走 .ui.toml 静态投影——理由：组件 schema 随插件加载动态变化，静态模板生成会引入第二份需同步的产物；定制 drawer 仍走 `.ui.toml`（`register_component_drawer` 覆盖）。
- 落点：`zircon_editor/src/ui/inspector/reflect_drawer.rs` [新增]——`ReflectTypeKind` → 控件映射表（数值带 `ReflectNumericRange` 滑条、枚举带 `ReflectEnumOption` 下拉、嵌套 struct 折叠组）；写回经 `ReflectWriteRequest` + `register_operation` 包装（自动可撤销）。
- 验收测试：`reflect_drawer_renders_all_reflect_type_kinds`、`reflect_drawer_write_is_undoable`。

### E2 EditorOperationStack 收口（前置：01-M4）

- 统一 undo/redo 栈替代分散历史。迁移表（实查）：

| 现状 | 动作 |
|------|------|
| `core/editing/history.rs`（EditorHistory） | 收编为 `EditorOperationStack` 内核，迁移至 `core/editing/operation_stack.rs` |
| `ui/host/editor_event_execution/undo_policy.rs` | 改为消费统一栈的 policy 层 |
| `ui/workbench/state/editor_state.rs` / `editor_state_play_mode.rs` 局部历史 | 删除局部栈，经 operation 提交 |
| `core/editor_operation.rs` 雏形 | 扩展为栈宿主：`push(op)` / `undo()` / `redo()` / `transaction(label)`（合并复合编辑） |

- 验收测试：`all_registered_operations_are_undoable`（契约：注册表内每个 operation 必须实现 inverse 或显式声明 non-undoable 并给理由）、`transaction_undoes_atomically`。

## 7. 验收标准（每插件 Editor 里程碑通用）

- `cargo test -p zircon_editor --test integration_contracts --features integration-contracts --locked` 全绿。
- 插件 editor crate 契约测试覆盖：扩展点注册清单快照、operation 可撤销性、capability 缺失时的降级行为（`RegistrationDiagnostic` 产出断言）。
- §5 三项调试设施各有对应测试（overlay 快照 / DiagnosticPath 注册 / 镜像通道契约）。
- `docs/zircon_plugins/<plugin>/editor.md` 与实现同步更新。

## 8. 里程碑

本文档为规范而非独立工程项；落地节奏跟随各插件计划的 Editor 里程碑（02-M5、03-M6、04-M6、05-M6、06-M5、07-M7、09-M6）。前置基建单列：

| 项 | 内容 | 前置 | 新增测试 |
|----|------|------|---------|
| E1 | 反射驱动默认 drawer（运行期构建） | 08-M1 | `reflect_drawer_renders_all_reflect_type_kinds`、`reflect_drawer_write_is_undoable` |
| E2 | EditorOperationStack 收口（迁移表见 §6） | 01-M4 | `all_registered_operations_are_undoable`、`transaction_undoes_atomically` |

## 9. 验收命令

```bash
cargo test -p zircon_editor --lib --locked
cargo test -p zircon_editor --test integration_contracts --features integration-contracts --locked
```

## 状态与产出记录

| 日期 | 任务 | 状态 | 产出 | 验证 |
|------|------|------|------|------|
| 2026-06-14 | 09-M6 导出向导契约 | 已实现数据契约；真实 UI 渲染未声明 | `zircon_plugins/editor_build_export_desktop/editor/src/export_wizard.rs` 声明导出向导的 `ai-build-export-layout.png` 版式引用、Profiles/Pipeline/Report 三栏区域、Validate/CompileHost/SourceTemplate/CookAssets/Pack/PlatformBundle/Report 阶段流、每阶段 `report.json` 路径、Pending/Running/Passed/Fatal 进度状态，以及 SourceTemplate/LibraryEmbed/NativeDynamic 报告视图 template 映射。 | `rustfmt --edition 2021 --check` 通过；冲突标记/尾随空白扫描和 `git diff --check` 通过；新增 `export_wizard_descriptor_covers_build_layout_stages_and_reports`；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 超时 244 秒且无 Rust 诊断，锁文件保持未修改。 |
| 2026-06-14 | 09-M6 导出向导流式进度解析 | 已实现 parser 契约；真实 retained UI/process 接入未声明 | `zircon_plugins/editor_build_export_desktop/editor/src/progress.rs` 新增 `ExportWizardProgressState`，从 `python -m zircon_export` stdout 的 stage banner、`key=value` 产物路径和顶层 JSON `fatal` 字段投影阶段 Running/Passed/Fatal snapshot；共享 `ExportPipelineStage` 补齐 `SourceTemplate`、`CookAssets`、`Report`，避免 editor 向导维护私有阶段枚举。 | 新增 `export_wizard_progress_parses_cli_stream_into_stage_snapshots`、`export_wizard_progress_marks_fatal_stage_reports`、`export_pipeline_stage_parser_accepts_cli_and_report_stage_names`；`rustfmt --edition 2021 --check` 通过；冲突标记/尾随空白扫描和 `git diff --check` 通过（仅 LF/CRLF 提示）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；focused `cargo test ... export_wizard ...` 604 秒超时，未取得目标测试结果。 |
| 2026-06-14 | 09-M6 导出向导 pipeline command-plan | 已实现 host 可消费命令契约；真实 retained UI/process 接入未声明 | `zircon_plugins/editor_build_export_desktop/editor/src/pipeline/` 新增 `ExportWizardPipelineOptions`、`ExportWizardPipelinePlan` 和阶段命令 DTO，为七阶段生成 `python -m zircon_export` 参数、默认 report/artifact 路径、stdout key 预期和缺失输入诊断；命令 banner 与 `ExportWizardProgressState` 对齐。 | 新增 `export_wizard_pipeline_plan_builds_stage_commands_in_cli_order`、`export_wizard_pipeline_plan_threads_stage_artifact_inputs`、`export_wizard_pipeline_plan_reports_missing_execution_inputs`、`export_wizard_pipeline_banners_drive_progress_parser`；`rustfmt --edition 2021` 已应用；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）。 |
| 2026-06-14 | 09-M6 导出向导 process adapter | 已实现同步 process adapter 契约；真实 retained UI/process 接入未声明 | `zircon_plugins/editor_build_export_desktop/editor/src/pipeline/execution.rs` 将 pipeline command-plan 与 progress parser 接成可替换 runner：`ExportWizardCommandRunner` 便于 host/test 注入，`ProcessCommandRunner` 提供默认同步进程捕获，stage/pipeline execution 结果携带 argv、exit code、stdout/stderr、diagnostics、fatal 和 progress snapshot。 | 新增 `export_wizard_stage_execution_feeds_stdout_into_progress`、`export_wizard_pipeline_execution_stops_on_missing_inputs_before_process_run`、`export_wizard_pipeline_execution_stops_on_plan_diagnostics_before_process_run`、`export_wizard_pipeline_execution_stops_on_process_failure`；`rustfmt --edition 2021` 已应用；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；focused `cargo test ... export_wizard_ ...` 244 秒超时，未取得目标测试结果。 |
| 2026-06-14 | 09-M6 导出向导 host-owned wizard 契约 | 已完成 hard cutover；真实 retained UI 线程/按钮接入未声明 | 原插件内的 stream parser、pipeline command-plan 和 process adapter 迁入 `zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/`，并新增 `ExportWizardJobState/Status/Snapshot` 作业快照模型；`editor_build_export_desktop` 插件只保留 `export_wizard.rs` 面板描述器并 re-export host-owned wizard API，避免 editor host 反向依赖插件 crate。 | `rustfmt --edition 2021` 已应用；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；新增 job 状态聚合测试；`cargo test -p zircon_editor --lib export_wizard_job_state --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614 --no-run` 304 秒超时且无 Rust 诊断，已清理该 target-dir 专属 cargo/rustc 进程；focused test execution 尚未声明通过；锁文件保持未修改。 |
| 2026-06-14 | 09-M6 导出向导 host job runner events | 已实现同步事件契约；真实 retained UI 线程/按钮接入未声明 | `zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/run.rs` 新增 `run_export_wizard_job`、`ExportWizardJobEvent/Kind`、`ExportWizardCancelSignal` 和 `ExportWizardNeverCancel`，按阶段向 retained host 发送 Created/Started/StageStarted/StageFinished/Finished/Failed/Cancelled snapshot event，并在阶段边界支持取消。插件 crate 继续 re-export host-owned API。 | 新增 `export_wizard_job_runner_emits_successful_snapshot_events`、`export_wizard_job_runner_stops_after_fatal_stage_event`、`export_wizard_job_runner_cancels_after_stage_boundary`；`rustfmt --edition 2021` 已应用；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；focused test execution 尚未声明通过；锁文件保持未修改。 |
| 2026-06-14 | 09-M6 导出向导 host background controller | 已实现后台作业控制契约；真实 retained UI 按钮/渲染接入未声明 | `zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/controller.rs` 新增 `ExportWizardJobController` 与 `ExportWizardJobHandle`，将 host-owned job runner 放入 worker thread，向 retained host 暴露 event receiver、共享 cancel token 和 `finish()` join/final snapshot 契约；插件 crate 继续 re-export host-owned API。 | 新增 `export_wizard_job_controller_streams_events_and_finishes_worker`、`export_wizard_job_controller_handle_requests_stage_boundary_cancel`；`rustfmt --edition 2021` 已应用；首次 plugin `cargo check` 244 秒超时且无 Rust 诊断，随后 `cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning），plugin scoped `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 重跑通过（既有 warning）；`cargo test -p zircon_editor --lib export_wizard_job_controller --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614 --no-run` 312 秒后以退出码 `1073807364` 停在依赖编译阶段且无 Rust 诊断，未取得目标测试结果；focused test execution 尚未声明通过；锁文件保持未修改。 |
| 2026-06-14 | 09-M6 导出向导 retained UI view model | 已实现 UI 可绑定状态契约；真实 retained UI 模板绑定/按钮路由未声明 | `zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/view_model.rs` 新增 `ExportWizardPanelViewModel`、`ExportWizardControlState` 和 stage row DTO，从 pipeline plan 与 job events 投影 start/cancel/close 控制状态、planned report/artifact 路径、missing inputs、最新 snapshot 和阶段行；插件 crate 继续 re-export host-owned API。 | 新增 `export_wizard_view_model_projects_plan_stage_rows_and_controls`、`export_wizard_view_model_reports_missing_inputs_before_start`、`export_wizard_view_model_drains_job_events_into_terminal_rows`；`rustfmt --edition 2021` 已应用；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；plugin scoped `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；`cargo test -p zircon_editor --lib export_wizard_view_model --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614 --no-run` 604 秒超时且无 Rust 诊断，已停止该 target-dir 残留 cargo/rustc 进程；focused test execution 尚未声明通过；锁文件保持未修改。 |
| 2026-06-14 | 09-M6 导出向导私有模板资产 | 已实现插件私有模板资产与注册格式契约；真实按钮路由/渲染未声明 | `editor_build_export_desktop` 新增主面板、SourceTemplate/LibraryEmbed/NativeDynamic 报告 `.v2.ui.toml` view 模板、profile drawer `.zui` component 模板和默认 profile TOML；`EditorExtensionRegistry` 将 UI template 文档扩展为 `.zui` 或 `.v2.ui.toml`，component drawer 继续拒绝非 `.zui` 文档；插件注册常量和测试锁定资源 URI。 | 新增 `desktop_export_private_template_assets_match_registered_documents`、`authoring_registry_accepts_view_templates_but_keeps_drawers_component_only`；`rustfmt --edition 2021` 已应用；Python `tomllib` 解析新增资源通过；最终 `rustfmt --edition 2021 --check`、`git diff --check`、冲突标记/尾随空白扫描通过（`git diff --check` 仅 LF/CRLF 提示）；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；plugin scoped `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；focused plugin test 604 秒超时，未取得测试结果。 |
| 2026-06-14 | 09-M6 导出向导 retained UI 按钮会话 | 已实现按钮动作/会话契约；真实 retained 绘制未声明 | `zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/session.rs` 新增面板按钮绑定常量、`ExportWizardPanelAction`、`ExportWizardPanelSession`、custom `DesktopExportWizard` call 和 runtime binding 注册 helper；`editor_build_export_desktop/editor/panel.v2.ui.toml` 为 Generate Plan/Start/Cancel 接入 Click binding；插件 crate 继续 re-export host-owned API，并把导出 operation id 修正为合法小写 `build_export.desktop.*`。 | 新增 `export_wizard_panel_bindings_project_template_button_events`、`export_wizard_panel_session_rejects_unready_start_until_plan_regenerates`、`export_wizard_panel_session_starts_polls_and_cancels_job`；触及文件 `rustfmt --check` 通过；Python `tomllib` 按钮事件检查通过；较早 `cargo check -p zircon_editor --lib` 对新增 wizard/session 代码通过（既有 warning），但最新 rerun 被无关 `pane_option_projection.rs` partial move 编译错误阻塞；`cargo test -p zircon_editor --lib export_wizard_panel -- --nocapture` 和插件模板 focused test 均未执行到目标测试，不声明 focused pass。 |
| 2026-06-14 | 09-M6 retained UI 投影编译恢复 | 已修复 editor retained option 投影阻塞；真实 retained 绘制未声明 | `zircon_editor/src/ui/retained_host/ui/pane_data_conversion/pane_option_projection.rs` 在构造 `TemplatePaneOptionData` 前先计算所有 option 状态字段，再移动 `id/label`，避免按钮会话验证被共享投影 E0382 阻塞。 | 复现后修复 `cargo check -p zircon_editor --lib` 的 E0382；`rustfmt --edition 2021 --check`、`git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 通过（既有 warning）；focused editor/plugin tests 均 364 秒超时且无 Rust 诊断，已清理本 target-dir 残留进程，不声明 focused pass。 |
| 2026-06-14 | 09-M6 导出面板 host 安装入口 | 已实现 retained host 模板/绑定标准接线；真实 retained 绘制未声明 | `register_export_wizard_panel_template(...)` 将导出面板模板文件和 DesktopExportWizard 三个 Click binding 一次注册到 `EditorUiHostRuntime`；`project_export_wizard_panel(...)` 固定投影标准模板文档；`EXPORT_WIZARD_TEMPLATE_DOCUMENT_ID` 作为 host/plugin 共用模板 id，插件 crate 不再重复维护 `editor_build_export_desktop.panel` 字符串。 | `rustfmt --edition 2021 --check` 与 `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-0614` 600 秒窗口通过（既有 warning）；`cargo test -p zircon_editor --lib export_wizard_panel_bindings_project_template_button_events --no-run ...` 184 秒超时且无 Rust 诊断；插件 scoped check 当前被无关 `zircon_runtime/src/dynamic_api/session.rs` 活跃 runtime 改动阻塞，未声明插件 check pass。 |
| 2026-06-14 | 09-M6 导出面板 request/update 分发入口 | 已实现 retained host 按钮分发契约；真实 retained 绘制未声明 | `ExportWizardPanelRequest` 将 GeneratePlan 的 options 输入与 Start/Cancel/Poll 简单动作分开，`ExportWizardPanelUpdate` 将事件 drain 数、active job、latest event 和 snapshot 回传给 host；`ExportWizardPanelSession::handle_request(...)` 与 `handle_action_call(...)` 让 retained host 可统一路由按钮事件，同时避免 UI 侧散落 controller 生命周期分支。 | `rustfmt --edition 2021 --check` 通过；`git diff --check` 与冲突标记扫描通过（仅 LF/CRLF 提示）；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；新增 focused tests 编译/执行仍因 `cargo test -p zircon_editor --lib export_wizard_panel_session ...` 904 秒超时未取得目标测试结果。 |
| 2026-06-14 | 09-M6 导出面板模板槽位状态投影 | 已实现 retained host 槽位数据契约；真实 retained 绘制未声明 | `ExportWizardPanelTemplateState` 将导出 view model 投影为 MissingInputs、StageRows、TerminalOutput、ArtifactPaths、ReportBody 五个模板槽位；每个 entry 保留稳定 key、显示 label、detail、stage 与 severity，供后续 retained host 绑定现有 `panel.v2.ui.toml` 锚点而不让模板直接理解 job/controller 生命周期。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；新增 `export_wizard_panel_template_state_projects_template_slots`、`export_wizard_panel_template_state_reports_missing_inputs`；focused `cargo test -p zircon_editor --lib export_wizard_panel_template_state ...` 被无关 `RenderQualityProfile::with_history_resolve` 缺失编译错误阻塞，未运行目标测试。 |
| 2026-06-14 | 09-M6 导出面板 Poll 终态收口 | 已实现 retained host 轮询收口契约；真实 retained 绘制未声明 | `ExportWizardPanelSession::handle_request(Poll)` drain 到 terminal snapshot 后会自动 join worker、清空 active controller，并返回 `active_job_id = None` 的 update；GeneratePlan 可在终态 Poll 后直接替换下一轮 plan，避免 UI 模板或按钮回调额外管理 `finish_job()` 顺序。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；新增 `export_wizard_panel_session_poll_finishes_terminal_job`；focused `cargo test -p zircon_editor --lib export_wizard_panel_session_poll_finishes_terminal_job ...` 304 秒超时且无 Rust 诊断，已清理本 target-dir 残留进程，不声明 focused test pass。 |
| 2026-06-15 | 09-M6 导出面板按钮控制状态投影 | 已实现 retained host 按钮 enablement 契约；真实 retained 绘制未声明 | `ExportWizardPanelTemplateState.control_bindings` 暴露 GeneratePlan/Start/Cancel 三个稳定按钮 id 的 enabled 状态，`control(...)` 允许 host 按 id 查询；状态来自 view model 的 `can_close/can_start/can_cancel`，让后续 retained 绘制只消费投影数据，不直接分支 job/controller 生命周期。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；扩展 `export_wizard_panel_template_state_projects_template_slots` 和 `export_wizard_panel_template_state_reports_missing_inputs` 覆盖按钮 enablement；本切片未单独声明 focused test pass。 |
| 2026-06-15 | 09-M6 导出面板 retained host 投影 | 已实现 retained host projection 契约；全局应用状态接线未声明 | `export_wizard_panel_retained_projection(...)` 从标准导出面板模板构建 shared surface 并生成 `RetainedUiHostProjection`；`apply_export_wizard_panel_template_state(...)` 将按钮 disabled 状态和 MissingInputs/StageRows/TerminalOutput/ArtifactPaths/ReportBody 槽位 entry 写入 projection synthetic label 节点，供 native retained host 直接消费。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；新增 `export_wizard_panel_retained_projection_applies_controls_and_slot_entries`、`export_wizard_panel_retained_projection_disables_start_for_missing_inputs`；focused `cargo test -p zircon_editor --lib export_wizard_panel_retained_projection --no-run ...` 被无关 `RenderQualityProfile::with_history_resolve` 缺失编译错误阻塞，未运行目标测试。 |
| 2026-06-15 | 09-M6 导出面板 retained pane 接线 | 已接入 BuildExport pane native projection；真实按钮执行/逐帧会话状态未声明 | `build_export_wizard_panel.rs` 将标准 retained wizard panel 投影适配为现有 `TemplatePaneNodeData`，保留按钮 binding/action 元数据和五个槽位 synthetic entry；`build_export.rs` 在 `BuildExportV1` pane presentation 下优先走该投影，projection 不可用时回退旧 BuildExport target rows。 | `rustfmt --edition 2021` 已应用；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；新增 `build_export_wizard_panel_nodes_project_retained_export_wizard_panel`；focused `cargo test -p zircon_editor --lib build_export_wizard_panel_nodes_project_retained_export_wizard_panel --no-run ...` 首次被 target-dir dep-info 写入 `os error 3` 阻塞，关闭增量重跑 1204 秒超时，已清理残留进程，不声明 focused test pass。 |

| 2026-06-15 | 09-M6 导出面板 retained 按钮分发接线 | 已接入 retained wizard 按钮到 BuildExport action 分发；逐帧 wizard session 状态未声明 | `build_export_wizard_panel.rs` 为 GeneratePlan/Start/Cancel 生成 `workbench.build_export.plan/execute/cancel.<profile>` action id，同时保留 `DesktopExportWizard/...` binding id；`template_activation_semantics.rs` 对 `export_wizard_panel` 使用 action 优先激活；`pane_surface_actions.rs` 通过 `parse_build_export_action(...)` 接收插件面板按钮；`build_export_actions.rs` 新增 GeneratePlan 并拆出 `build_export_actions/tests.rs`。 | `rustfmt --edition 2021` 已应用；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；新增/更新 `build_export_actions_parse_execute_profile`、`plugin_export_wizard_action_id_routes_to_build_export_dispatch`、`export_wizard_panel_route_prefers_action_over_binding` 与 pane projection action 断言；focused `cargo test -p zircon_editor --lib export_wizard_panel_route_prefers_action_over_binding --no-run ...` 被无关 `RenderQualityProfile::with_history_resolve` 缺失编译错误阻塞，未运行目标测试。 |
| 2026-06-15 | 09-M6 导出面板 app-owned wizard session 接线 | 已接入 retained app 会话状态；真实 CLI 端到端执行未声明 | `retained_host/app/build_export_wizard_session.rs` 以 profile 为键持有 `ExportWizardPanelSession`，将 GeneratePlan/Start/Cancel wizard controls 映射为 session request，组装当前项目 manifest/output 的 `ExportWizardPipelineOptions`，并在 retained host tick 中 Poll；`BuildExportPaneViewData.wizard_view_model` 把 live view model 交给 `build_export_wizard_panel.rs`，没有 session 时保留 synthetic fallback；旧 BuildExport target-row actions 仍走旧队列。 | `rustfmt --edition 2021 --check` 通过；冲突标记扫描和 `git diff --check` 通过（仅 LF/CRLF 提示）；新增 `build_export_wizard_surface_action_maps_panel_buttons_to_session_actions`、`desktop_export_wizard_sessions_project_view_model_after_generate_plan`；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 被无关 `zircon_runtime/src/ui/surface/render/command_palette.rs:311` 的 `E0282` type annotations needed 阻塞，未到达 editor 代码。 |
| 2026-06-15 | 09-M6 app-owned session 验证恢复 | 已恢复 editor/plugin scoped check；focused lib-test 仍超时未通过 | 对当前工作区新增的 `zircon_runtime/src/ui/surface/render/command_palette.rs` 补显式 `Vec<CommandPaletteRow>` 类型注解，移除阻塞导出面板 app-owned session 验证的 `E0282`；导出面板 retained app/session 架构保持不变。 | `cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo test -p zircon_editor --lib build_export_wizard_session ...` 904 秒超时且无目标测试输出，已停止同 target-dir 残留 cargo/rustc 进程，不声明 focused session test pass。 |
| 2026-06-15 | 09-M6 导出向导 Start/Cancel 当帧控制状态 | 已实现 view model 活动作业状态；focused lib-test 被无关 editor/render 编译漂移阻塞 | `ExportWizardPanelViewModel.active_job` 明确表达 worker 生命周期；`ExportWizardPanelSession` 在 Start 后立即调用 `mark_job_started`，在 Cancel 后立即调用 `mark_cancel_requested`，在 worker join 后调用 `mark_job_finished`，使真实 retained panel 的 Start/Cancel/Close/progress 状态不再等待下一次 worker event。新增 `session_control_tests.rs` 覆盖当帧 Start/Cancel 控制状态。 | `rustfmt --edition 2021` 已应用；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；focused `cargo test -p zircon_editor --lib export_wizard_panel_session_start_updates_controls_before_worker_poll --exact ...` 被无关 `RenderQualityProfile::with_history_resolve` 缺失与 `notification_center.rs` partial move 编译错误阻塞，未运行目标测试；同 target-dir 无残留 cargo/rustc/rustdoc 进程。 |
| 2026-06-15 | 09-M6 导出向导阶段 stdout/stderr 投影 | 已实现 TerminalOutput 槽位输出投影；focused lib-test 被无关 render profile 测试漂移阻塞 | `ExportWizardStageViewRow.stdout_lines/stderr_lines` 将阶段执行输出带到 retained 面板数据；`panel_projection.rs` 将输出行写入 `TerminalOutput` 槽位并生成稳定 key/severity；新增 `panel_output_tests.rs` 覆盖 Pack stdout/stderr 映射。 | `rustfmt --edition 2021` 已应用；editor scoped check passed；plugin scoped check passed；focused exact test blocked by `RenderQualityProfile::with_history_resolve`；no residual processes. |
| 2026-06-15 | 09-M6 导出向导 staged CompileHost host handoff | 已实现 retained app 到 PlatformBundle 的 host 产物路径接续；focused lib-test 编译超时未声明通过 | runtime `LibraryEmbedCompileHostPlan` 暴露 binary/profile 规则；wizard `plan.rs` 新增 CompileHost target/executable 路径 helper；`build_export_wizard_session.rs` 生成 profile options 时使用 `<out>/stages/compile_host/target/<cargo-profile>/<binary>[.exe]`，替代旧 `host/zircon_game.exe` 占位路径；新增 `pipeline_handoff_tests.rs` 与 retained app 默认 host 测试。 | `rustfmt --edition 2021` 已应用；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` passed with existing warnings；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` passed with existing warnings；focused `cargo test -p zircon_editor export_wizard_compile_host_path --lib ...` timed out twice during lib-test compilation with no target output, leftovers stopped. |
| 2026-06-15 | 09-M6 导出向导 CLI 工作目录接续 | 已实现 repo-root 启动目录契约；真实 CLI 端到端执行未声明 | `ExportWizardPipelineStageCommand` 新增 `working_dir`；`wizard/plan.rs` 将 `repo_root` 同步为每阶段 `python -m zircon_export` 的工作目录；`ProcessCommandRunner` 在启动子进程前设置 `current_dir`；retained app session 默认填入 engine repo root 并守卫 `zircon_export/__main__.py`。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；focused `cargo test -p zircon_editor --lib export_wizard_pipeline_commands_use_repo_root_as_working_dir --exact ...` 被无关 `RenderQualityProfile::with_history_resolve` 缺失编译错误阻塞，未运行目标测试。 |
| 2026-06-15 | 09-M6 Report 阶段 SourceTemplate 报告 handoff | 已实现 editor command-plan 与 CLI report 聚合对齐；真实 CLI 端到端执行未声明 | `wizard/plan.rs` 的 Report 命令 consumed artifacts 包含 SourceTemplate stage report，和 CLI `pipeline_report.py` 的可选 SourceTemplate 聚合规则对齐；新增 `pipeline_report_tests.rs` 覆盖 6 个 stage report 输入。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；focused editor lib-test 未单独重跑，因为同一轮 exact lib-test 已确认仍被无关 `RenderQualityProfile::with_history_resolve` 编译漂移阻塞。 |
| 2026-06-15 | 09-M6 导出向导 Start 刷新计划 | 已实现 retained app Start latest-options 契约；真实 CLI 端到端执行未声明 | `DesktopExportWizardSessions` 的 Start 分发现在先用当帧 `ExportWizardPipelineOptions` 重新生成 profile plan，再注入默认 `ProcessCommandRunner` 启动作业；测试专用 runner 注入避免真实 CLI。新增 `desktop_export_wizard_sessions_start_refreshes_existing_plan_options`，覆盖 GeneratePlan 旧输出后 Start 新输出时 CookAssets/PlatformBundle 阶段输入均刷新。 | `rustfmt --edition 2021 --check` 通过；`git diff --check`、冲突标记和尾随空白扫描通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；focused `cargo test -p zircon_editor --lib desktop_export_wizard_sessions_start_refreshes_existing_plan_options --exact ...` 604 秒超时且无目标输出；同 target-dir 无残留 cargo/rustc/rustdoc 进程。 |
| 2026-06-15 | 09-M6 ReportBody pipeline report 投影 | 已实现 retained 报告槽位最终报告入口；真实 CLI 端到端执行未声明 | `ExportWizardPanelTemplateState` 的 ReportBody 槽位新增 `report.pipeline_report` entry，planned 状态展示 `<out>/report.json`，执行后优先采用 Report stage stdout 的 `pipeline_report=` 路径，并按 Report 阶段状态投影 severity；新增 `panel_report_body_tests.rs`。 | `rustfmt --edition 2021 --check` 通过；`git diff --check`、冲突标记和尾随空白扫描通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-editor-dispatch-0614 --message-format short --color never` 通过（既有 warning）；focused `cargo test -p zircon_editor --lib export_wizard_panel_template_state_projects_pipeline_report_body_entry --exact ...` 604 秒超时且无目标输出，已停止同 target-dir cargo/rustc 残留。 |
| 2026-06-15 | 09-M6 CLI 输出流式 job 事件 | 已实现 retained host 逐行输出事件契约；真实 CLI 端到端执行未声明 | `ExportWizardCommandRunner::run_with_output(...)` 为 runner 暴露输出行回调；默认 `ProcessCommandRunner` spawn 子进程后并行读取 stdout/stderr；`ExportWizardJobEventKind::StageOutput` 在 `StageFinished` 前携带最新 snapshot；`ExportWizardJobSnapshot.live_stage_outputs` 让 `ExportWizardPanelViewModel.stage_rows()` 在阶段运行中投影 stdout/stderr。新增 `streaming_output_tests.rs`。 | `rustfmt --edition 2021` 已应用；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-streaming-0615 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-streaming-0615 --message-format short --color never` 通过（既有 warning）；focused `cargo test -p zircon_editor --lib export_wizard_job_runner_streams_stage_output_before_stage_finished --exact ...` 904 秒超时且无目标输出，已停止同 target-dir cargo/rustc 残留，不声明 focused pass。 |
| 2026-06-15 | 09-M6 导出向导运行中取消 | 已实现活动阶段 Cancel 语义；真实 CLI 端到端执行未声明 | `ExportWizardCommandRunner::run_with_output_and_cancel(...)` 将取消轮询传入 runner；默认 `ProcessCommandRunner` 在 child 运行期间轮询取消、终止进程并 drain stdout/stderr reader；stage execution 新增 `cancelled`，job 记录阶段后发出 `Cancelled` terminal snapshot，view model 避免把 cancelled stage 显示为 Passed。新增 `cancellation_tests.rs`。 | `rustfmt --edition 2021 --check` 通过；`cargo check -p zircon_editor --lib --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-cancel-0615 --message-format short --color never` 通过（既有 warning）；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_editor_build_export_desktop_editor --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-export-m6-cancel-0615 --message-format short --color never` 通过（既有 warning）；focused `cargo test -p zircon_editor --lib export_wizard_job_runner_cancels_during_active_stage_without_failing --exact ...` 908 秒超时且无目标输出，同 target-dir 进程审计为空，不声明 focused pass。 |

## 10. 风险

- E2 触及 workbench 状态层的局部历史，与编辑器既有事件执行路径（editor_event_execution）耦合深；迁移按"先栈后策略"两步走，undo_policy 行为以现有测试为回归网。
- E1 的运行期控件树在大组件（几十字段）上的布局性能需要 retained host 虚拟化支持；首版以折叠组惰性展开规避。

## 11. 附录 · dev 参考源码对位

实现各项前**必须先读对应参考实现**，交互语义与视觉基准对照真实代码核对，禁止凭空实现：

| 设计点 | 参考源码（已核验存在） | 看什么 |
|--------|----------------------|--------|
| 反射驱动 Inspector（E1） | `dev/godot/editor/`（inspector 相关：editor_inspector/property editor 族）与 `dev/Fyrox/editor/`（reflect-based Inspector） | 类型 → 控件映射表、嵌套折叠/数组项编辑、撤销集成点 |
| undo/redo 栈（E2） | `dev/godot/`（core/object 下 undo_redo 实现 + editor 侧封装） | action 合并（merge mode）、对象生命期安全引用、transaction 嵌套 |
| 编辑器控件视觉/交互基准 | `dev/material-ui/`、`dev/material-components/`、`dev/bevy/crates/bevy_feathers/`、`bevy_ui_widgets/` | 组件状态层（hover/focus/disabled）、密度 token——与 `ai-workbench-style/STYLE-NOTES.md` 互补 |
| 时间轴/sequencer 交互 | `dev/theatre/`（Theatre.js studio 的 sequence 编辑） | 关键帧选择/拖拽/吸附、轨道折叠交互形态 |
| 声明式 UI 模板体系 | `dev/slint/` | 模板编译与属性绑定（zircon_hub 一侧的边界参照，editor 不引入） |
