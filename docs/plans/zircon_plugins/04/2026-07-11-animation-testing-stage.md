# Plugins 04 Animation 正式测试阶段记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 验收边界：本记录覆盖 `zircon_plugin_animation_runtime` 当前已实现的全部 unit/integration test target，不将尚未接线的 Runtime/Render/Physics/Editor 外部所有权项计为完成。
> 路由说明：本文件保留前 8 条测试阶段记录；后续 M6/WSL/IK 与 post-hard-cut 验收迁入 [Plugins 04 post-hard-cut 产出记录](2026-07-12-animation-post-hard-cut-output-records.md)，不在两处复制具体事实。

## 状态与产出记录

| 日期 | 阶段 | 状态锚点 | 验证事实 | 未纳入项 |
|---|---|---|---|---|
| 2026-07-11 | M1 reusable final pose allocation 门 | `plugins_04_m1_final_pose_reuse_allocation_2_of_2_passed` | Windows nightly locked/offline `animation_pose_buffer_allocation_contract` 2/2 通过。`pooled_pose_buffer_blend_performs_zero_allocations` 在 256 次稳定循环内为 0 分配；`final_pose_output_clone_reuses_bone_and_name_storage` 同时断言 final-pose copy 为 0 分配且 bone Vec/name String 指针保持稳定。Graph base blend 的首姿态不再 clone。 | 该 focused 门关闭已知 final owned handoff churn，不推算整个 production Tick 的所有临时事件/扫描容器。post-hard-cut Windows/WSL 全套仍待复验。 |
| 2026-07-11 | M2/M3/M5 production Tick 可执行验收 | `plugins_04_production_tick_windows_34_of_34_passed` | 在受管允许 target `D:\cargo-targets\zircon-plugin-architecture-plugin-checks` 执行 nightly locked/offline `runtime_physics_animation_tick_contract`，9m22s 完成构建、0.33s 完成执行，34/34 通过。新增关键场景全部取得 executable：`simulated_pose_blends_under_ragdoll_mask`、masked layer、SubMachine parent transition、layer interruption continuity、queued TwoBoneIK、queued LookAt；既有 pose target bridge、transition/event、BlendSpace、Clip/Graph/Sequence 路径同步保持全绿。 | 本行关闭此前仅 `cargo check` 的 production Tick 行为缺口；不据此推算全 `--tests` 新总数。下一正式门仍为 post-hard-cut Windows/WSL 全套、M1 reusable final-pose owner、M4 Render、M6 Editor。 |
| 2026-07-11 | M5 Manager/dense IK postprocess 静态门 | `plugins_04_m5_t1_manager_dense_ik_postprocess_check_passed` | Windows nightly locked/offline 定向检查 `animation_ik_contract` 退出 0（145.4 秒），随后 `runtime_physics_animation_tick_contract` 退出 0（86.9 秒）。两条命令均完整检查 Runtime、SDK、Physics、Animation 依赖链；新增中立 Manager contract、逐 World 队列、dense target 编译、TwoBone/LookAt model-space 后处理、IK diagnostic 注册及两条生产 tick 场景均成功编译。输出仅含共享 Runtime 既有 warning，没有 Animation/IK 新 warning。 | 本行只证明编译闭环。当前仍有多条外部 Runtime/Editor/Hub 链接 lane，尚未运行新增 3 条行为测试，也不更新 78/78 Windows 或 75/75 WSL 的旧全套计数。 |
| 2026-07-11 | M5 focused executable attempt | `plugins_04_m5_t1_executable_deferred_after_604s_runtime_link_timeout` | `cargo +nightly test --locked --offline -p zircon_plugin_animation_runtime --test animation_ik_contract --jobs 1 -- --nocapture` 在 Runtime test link 阶段运行至 604 秒外层硬超时；未输出 OOM、编译错误或测试断言，超时清理后确认没有遗留本会话 Cargo/rustc。 | 没有 executable 就没有行为结果；继续保留 4 条 IK contract（数学 3 + Manager 1）与两条 production tick 为“已编译、待执行”。 |
| 2026-07-11 | M5 code-structure/review 优先门 | `plugins_04_m5_t1_structure_scoped_clear_global_naming_debt_preserved` | scoped rustfmt check、`git diff --check` 与生产 `allow(dead_code)`/TODO/FIXME 扫描均退出 0；新增生产 owner 最大为 `ik/postprocess.rs` 303 行，其余关键 owner 为 85/191/190 行。全 Runtime `audit_runtime_structure.py --json` 退出 0，并报告 root/core-spine/hard-cutover/large-file gate 均 `classified-and-clear`、≥1000 行热点 0、三大 crate module classification 均 converged。 | 审计总门仍为 `migration-debt-present`：存在本 slice 之外的 runtime naming debt 2 类（4 个 editor 未分类命名引用、1 个 graphics legacy 命名）。本 M5 slice 没有新增 debt，也不越权修改活跃 Editor/Graphics owner。 |
| 2026-07-11 | M3 Layer interruption continuity 静态门 | `plugins_04_m3_t3_layer_interruption_continuity_check_passed` | 新增 layer A→B 被 B→C 打断的 5.0→12.5 连续姿态合同；Layer evaluator 复用主状态机 interruption selection、持久化 interrupted source，并分离旧/新迁移事件时间窗。Windows nightly locked/offline `cargo check -p zircon_plugin_animation_runtime --test runtime_physics_animation_tick_contract --jobs 1` 退出 0（50.1 秒），只有 Runtime 既有 warning。 | production 合同尚未执行；与 masked layer、SubMachine parent-transition 一并等待 Runtime test link 资源窗口。 |
| 2026-07-11 | M2 SimulatedPoseFeed Animation 侧静态门 | `plugins_04_m2_t3_simulated_feed_animation_side_check_passed` | 新增逐骨 ragdoll feed 混合和 `simulated_pose_blends_under_ragdoll_mask` 生产合同；最终 feed pose 在 IK 前合入，并在同帧作为下一 Physics target 发布。Windows nightly locked/offline production tick `cargo check` 退出 0（28.8 秒），只有 Runtime 既有 warning。 | Physics 真实 ragdoll writer 与新增合同 executable 尚未完成；M2-T3 保持部分完成。 |
| 2026-07-11 | M3 Layer typed diagnostic 完整静态门 | `plugins_04_m3_t3_layer_typed_diagnostic_tests_check_passed` | Layer blend 静默失败改为 typed diagnostic event，并新增骨名错位私有合同及注册快照断言。Windows nightly locked/offline `cargo check -p zircon_plugin_animation_runtime --tests --jobs 1` 完整退出 0（25.3 秒），只有 Runtime 既有 warning。 | `--tests` 为编译门，不更新 executable test 计数。 |
