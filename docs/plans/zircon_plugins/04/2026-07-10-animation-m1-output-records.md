# Plugins 04 Animation M1 产出记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 记录边界：本文件只记录 M1 求值管线重构的具体产出、验证和未完成项。

## 状态与产出记录

| 日期 | 里程碑 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-07-11 | 04-M1/T3 reusable final pose 收口 | `plugins_04_m1_final_pose_reuse_allocation_2_of_2_passed` | Runtime 中立 `AnimationPoseOutput::clone_from_reusing_storage` 按稳定骨架原位更新 source/state/bone transform，并复用既有 bone Vec 与逐骨 name String 容量；`LevelSystem::record_animation_poses` 不再整包替换 BTreeMap，而是保留稳定 entity、移除消失 entity、新 entity 直接转移 owned pose。Graph base blend 同步消费首个 owned weighted pose，删除整包 clone。全 allocation contract 首次复跑暴露测试计数器把并行测试线程分配误计入当前 case，改为 thread-local scoped allocator probe 后，Windows nightly locked/offline 2/2 通过：PosePool 256 次循环与 final-pose copy 均为零分配。scoped rustfmt/diff hygiene 通过。 | M1 实现闭环；仍不把上游事件/扫描临时容器或跨线程后台分配描述为整个 Animation tick 的绝对零分配。post-hard-cut Windows/WSL 全 `--tests` 留作最终里程碑门。 |
| 2026-07-11 | 04-M1/T3 dense state 生产接入阶段 | `plugins_04_m1_t3_dense_state_pipeline_runtime_tick_20_of_20_passed` | 新增 `CompiledAnimationStateMachine`，把 entry/current/from/to/condition parameter 在资源 revision 边界编译为 `StateSlot`/`ParameterSlot`，拒绝重复状态与缺失边；focused contract 2/2 通过。`AnimationEvaluationPipeline` 新增有界 state-machine revision cache，生产 `state_machine_step` 不再调用 Runtime 的逐帧字符串 evaluator，graph/state transition 统一消费 dense graph path。完整生产 tick 首次运行 6/20，通过的 14 个失败均由测试 harness 未应用声明式 scene resource 暴露；在 tick 边界补幂等 resource 安装后，完整 `runtime_physics_animation_tick_contract` 20/20 通过。fresh WSL `cargo check --tests` 退出 0；生产 evaluation panic/allow 扫描仍为 0，compiled state/cache/step 分别 229/87/290 行，均低于结构预算。 | 最终 `AnimationPoseOutput`/LevelSystem handoff 的 owned Vec/BTreeMap 仍是稳定态分配边界；该 API 属于 active Runtime owner，需协调 handback 后才能完成可回收 pose owner，再进入 M1 全包 testing stage。 |
| 2026-07-11 | 04-M1/T3 dense graph/mask 生产接入阶段 | `plugins_04_m1_t3_dense_graph_mask_pipeline_passed_focused_2_of_2` | 新增 folder-backed `evaluation/compiled_graph/{compile,error,evaluate,types}.rs`：资产字符串只在编译边界解析为 `GraphNodeSlot`/`ParameterSlot`、预验证拓扑环/缺边/重复节点/参数，并把 skeleton target mask 编译为 dense row mask。`AnimationEvaluationPipeline` 以 `{graph_id,skeleton_id,revision}` 缓存有界 compiled graph，生产 `resolve_graph_pose_requests` 已切到该 owner；PoseBlend 对 compiled path 按 bone row 读取 mask，不再逐骨骼扫描 target string。新增 `animation_compiled_graph_contract` 覆盖 source mutation 后 dense 拓扑/参数/mask 不变，以及 missing edge、cycle、duplicate-leaf mask 歧义。首次 RED 因测试 URI 使用未支持的 `project://` 方案失败；改为仓库资源方案后 WSL focused 2/2 通过。随后 `cargo fmt --check` 与 `cargo +nightly check -p zircon_plugin_animation_runtime --tests --locked --offline --jobs 1` 退出 0。 | M1-T3 尚未关闭：state machine dense slot 属于 M3-T1，但生产 M1 state lookup 仍需避免逐帧字符串扫描；最终 `AnimationPoseOutput`/LevelSystem handoff 仍分配，需与 Runtime owner 协调可回收 owner 后进入完整 M1 testing stage。 |
| 2026-07-11 | 04-M1/T3 pipeline owner 与有界 evaluator cache 实现阶段 | `plugins_04_m1_t3_pipeline_owner_hard_cut_bounded_evaluator_cache_implemented_static_passed` | `animation.evaluate` 的生产实现已从旧 `src/scene_hook/**` 硬迁到 `evaluation/pipeline/**`，旧目录物理删除且 `runtime_system.rs` 不再用 path attribute 隐藏行为；`parameter_apply`、`state_machine_step`、`graph_evaluate`、`pose_blend`、`pose_apply` 为具名 phase owner，`evaluation/mod.rs`/`pipeline/mod.rs` 分别保持 33/14 行结构 façade。声明式 ECS resource 从 clip-only evaluator 提升为 `AnimationEvaluationPipeline`，内部持有 revision-aware evaluator。skeleton/clip/diagnostic cache 新增确定性访问序列、2/3 类可配置上限、LRU 淘汰计数和资源失效同步；默认 64/256/1,024 策略只在 `cache_policy.rs` 私有 owner 声明。生产 evaluation panic/allow 扫描为 0，原缓存 `expect!/unreachable!` 已换 typed invariant error；scoped rustfmt、结构断言、old-path 扫描和 diff check 通过。按 milestone-first 规则，本行只记录实现/静态证据，新增 cache/structure Cargo 测试留到 M1 testing stage，不提前宣称行为通过。 | 下一切片把 graph topology、mask target 和 state name 编译为 dense node/row/state slot，并让生产 PoseBlend/PoseApply 复用最终姿态 owner；随后统一进入 M1 Cargo testing stage。 |
| 2026-07-10 | 04-M1/T3 生产接线与审查修正阶段 | `plugins_04_m1_t3_production_compiled_evaluator_review_corrections_in_progress` | 生产 `scene_hook` 的 clip/graph/state-machine 姿态采样已改走 revision-aware `AnimationClipEvaluator` 与 `CompiledAnimationClip`，不再调用旧 `sample_clip_pose`；clip 缓存前一次性验证 key 时间严格递增/有限、TRS value/tangent 类型与有限性、rotation 非零，并返回 track/channel/key/role 类型化错误。资源层新增原子 `ResourceSnapshot<T>`，`register_ready` 以 registry→payload 固定锁序提交，动画 evaluator 消费 `{payload, revision}` 快照并监听 Added/Updated/Removed 失效 skeleton/clip 缓存，覆盖 remove/re-add revision 重置。Additive 已按 skeleton bind pose 转成 translation/rotation/scale 参考差分；零/负/非有限历史 transition duration 定义为立即切到目标姿态。`clip_evaluator`、`compiled_animation_clip`、`pose_buffer` 均硬切为薄 façade + 具名声明子文件。正式 WSL 证据：target table 10/10、weighted pose 4/4、compiled evaluator 10/10、原子 snapshot 并发契约 1/1；最新生产 tick 二进制完整 19/19（含原 16 条基线与 remove/re-add、非法 transition、additive bind-reference 三条新增边界）通过；`cargo check -p zircon_plugin_animation_runtime` 退出 0。生产输出仍会构造 `AnimationPoseOutput`/clone bone name，graph/state 仍含 name/target string 扫描，因此本行不宣称“全生产链零分配/零字符串”，M1-T3 仍进行中。 | 下一步把 graph/mask/state 的 string lookup 编译为 dense row/state slot，并把 production output 改为复用/借用式 pose owner；补错误可观测诊断、cache 上限与插件声明式 ECS resource 注册后，再关闭 M1-T3。 |
| 2026-07-10 | 04-M1/T2 实现与验证阶段 | `plugins_04_m1_t2_weighted_pose_standalone_4_of_4_cargo_blocked_external_runtime_compile_drift` | `PoseBuffer` 已按计划补齐 translation/rotation/scale/**weight** 四路 SoA，`PosePool` 预分配并复用四路容量；override/additive 以 source row weight 缩放，非法权重和 shape mismatch 为结构化错误，零分配测试覆盖 256 次稳定态循环。WSL `cargo check -p zircon_plugin_animation_runtime --tests` 退出 0；直接 owner harness 编译当前生产源码并 include 原 `animation_pose_buffer_contract.rs`，4/4 通过、0 失败，临时 harness 已删除。正式 Cargo 重试仍在插件编译前被共享 runtime 漂移阻断，最新为 `bind_group_layouts::hzb_msaa` façade 缺失，因此未计 Cargo 通过。 | M1-T2 touched algorithm 的 standalone 行为证据已闭环；待 shared runtime 收敛后补正式 Cargo 行。M1-T3 前还需把 buffer/pool 接入生产四段求值器。 |
| 2026-07-10 | 04-M1/T1 审查修正阶段 | `plugins_04_m1_t1_review_corrections_standalone_10_of_10_cargo_blocked_external_runtime_compile_drift` | 审查后将目标表改为 skeleton 加载期一次构建的 `SkeletonTargetTable`，compiled clip 以 `Arc` 共享；`TargetSlot`/`TargetTable<T>` 收为 crate-private，外部只查询安全 bone index。新增 canonical bone/path、黄金哈希、segment matrix、立即坏 parent、cycle、duplicate skeleton target 与 `DuplicateTrackTarget` 类型化边界，并把模块硬切为 `target_id.rs` 与目录 façade。WSL 全 animation test target type check 退出 0；直接 owner harness 编译真实 `target_id.rs`/`EntityPath`/evaluation owners并 include 原目标表 integration test，10/10 通过、0 失败，临时 harness 已删除。正式 Cargo 仍受上述外部 runtime 漂移阻断，未计通过。 | 待外部 runtime 基线收敛后补正式 Cargo 行；随后把 compiled target 真正接入 production sampler/`sequence/conversion.rs`。本行明确不宣称生产 tick 已“运行期零字符串”。 |
| 2026-07-10 | 04-M1/T1 测试阶段 | `plugins_04_m1_t1_target_identity_focused_tests_passed` | 当前源码在外部 IBL importer export 瞬态收敛后，`CARGO_PROFILE_DEV_DEBUG=0 cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_animation_runtime --test animation_target_table_contract --locked --jobs 1 --target-dir E:\cargo-targets\zircon-plugin-typed-extension-freeze-0710` 退出 0（2分25秒）；随后 `CARGO_PROFILE_TEST_DEBUG=0 cargo test` 同一 package/test/target 退出 0（8分34秒），`target_id_stable_across_reimport`、`channel_resolution_has_no_string_lookup_at_runtime`、dense slot/冲突重绑与重复叶名歧义共 4/4 通过、0 失败。输出只有仓库既有 runtime/physics warning。 | M1-T1 的稳定身份与预解析 focused slice 已取得类型和行为证据；继续 M1-T2 PoseBuffer/PosePool 与 blend 算子，并在四段求值接线时验证生产主循环消费 `CompiledAnimationClip`，不以本行替代整个 M1 或插件 workspace 全量回归。 |
| 2026-07-10 | 04-M1/T1 | `plugins_04_m1_t1_target_identity_and_dense_compile_path_implemented` | `zircon_runtime::core::framework::animation` 新增命名空间化、分段长度参与哈希的 128-bit `AnimationTargetId`；`zircon_plugin_animation_runtime::evaluation` 新增泛型 `TargetTable<T>`、dense `TargetSlot`、结构化绑定错误，以及把 skeleton 完整骨骼路径和 clip track 预解析为 slot 的 `CompiledAnimationClip`。边界覆盖包含重导入稳定性、路径分段防碰撞、同 ID 冲突绑定、重复骨骼叶名歧义与源码字符串在编译后变化不影响已解析 slot。`rustfmt --check` 通过；首次聚焦 Cargo 编译发现并修正 `animation_target_id.rs` 的 E0502。修正后的聚焦测试在 604 秒仍停留于默认 `zircon_runtime` 链接，随后 `cargo check --test` 在 304 秒内也未取得退出结果；两次均未计通过，超时命令的本会话进程已清理。 | 待共享 Cargo/rustc 通道稳定后，复用缓存重跑 `animation_target_table_contract`；通过后追加独立测试阶段记录。随后将 compiled clip 接入 M1-T2 PoseBuffer/PosePool 求值路径，确保主循环不回退到逐帧字符串扫描。 |
