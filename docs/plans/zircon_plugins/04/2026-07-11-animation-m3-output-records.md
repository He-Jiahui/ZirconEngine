# Plugins 04 Animation M3 产出记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 记录边界：本文件只记录 M3 状态机完整化的具体产出、验证和未完成项。

## 状态与产出记录

| 日期 | 里程碑 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-07-11 | 04-M3/T3 production executable 收口 | `plugins_04_m3_production_tick_34_of_34_passed` | Windows nightly locked/offline 完整 production Tick executable 34/34 通过；其中 `layered_state_machine_blends_production_pose_with_dense_mask`、`parent_transition_can_leave_sub_machine_state`、`layered_state_machine_interruption_preserves_blended_pose_continuity` 分别关闭 masked layer、父 SubMachine 迁出与打断连续性的“只编译未执行”缺口。既有 BlendSpace、Direct Clip、nested transition、exit-time 和 base interruption 场景同时保持绿灯。 | M3 实现与 Windows 定向行为闭环；post-hard-cut Windows/WSL 全 `--tests` 仍是最终里程碑门，typed diagnostic 的订阅行为可在该全套内一并复验。 |
| 2026-07-11 | 04-M3/T3 直接 Clip state 生产接线 | `plugins_04_m3_t3_direct_clip_state_production_8_of_8_passed` | 先新增生产 RED，状态机因 `UnsupportedStateKind(clip)` 无姿态而失败；随后将 Clip reference 编入 dense `CompiledStateKind`，通过现有 revision-aware `AnimationClipEvaluator` 直接采样 pose，并在状态时钟窗口发布 clip event，不伪造临时 graph。compiled state source-mutation 合同 5/5；集中复跑直接 Clip + 1D/2D BlendSpace 三种生产 state 3/3，共 8/8 通过。transition pose/events/exit time 也统一改为 state-kind dispatch，不再暗含 graph-only 路径。 | `SubMachine` 仍是唯一已建模但 typed 拒绝的 StateKind；直接 Clip 当前按 non-looping 状态采样，未来若资产需要 loop/speed 配置应扩展 Clip state DTO，不在运行期写死隐藏策略。 |
| 2026-07-11 | 04-M3/T3 SubMachine nested production | `plugins_04_m3_t3_sub_machine_nested_state_8_of_8_passed` | `SubMachine` 从 typed reject 改为 compiled dense reference；生产 pipeline 沿资产引用解析嵌套 machine，按 `{entity, machine lineage}` 隔离 active state、transition 与 interruption source，最大深度 8 且拒绝循环引用。嵌套叶 machine 可继续使用 GraphRef/Clip/BlendSpace；父组件 active state 保留父层名称，子层状态与迁移跨帧持久化。compiled state-machine 6/6，生产 nested delegate 1/1、nested transition 两帧连续性 1/1，共 8/8 通过。 | M3 的五种 StateKind 均已进入 compiled/production；计划中的“多层”仍缺显式 layer asset/runtime stack（权重、mask、override/additive）在单个 player 内并行求值。 |
| 2026-07-11 | 04-M3/T3 layer 资产硬切与生产接线 | `plugins_04_m3_t3_layer_asset_hard_cut_roundtrip_4_of_4_passed` | 删除重复的 `AnimationLayeredStateMachineAsset` wrapper，把 `layers` 直接并入唯一 `AnimationStateMachineAsset`；当前 `.zranim` 负载持久化 layer，current/v3/v2/v1 typed fallback 将旧 StateKind/旧 graph 状态机迁移为空层，不留 facade。`state_machine/layer/**` 编译权重、Override/Additive 与 dense mask；revision-aware machine cache 同时持有 compiled base/layers。生产 tick 为每层维护独立 `{entity,parent machine,layer name,machine lineage}` 状态与迁移，采样后通过既有 `PoseLayer`/`PoseBuffer::blend_layers` 折叠回 base pose。Animation manifest 显式启用 neutral `physics-contracts`，避免库目标只在 dev-dependency 特性统一时偶然编译。layer 合同 4/4、Windows `cargo check --lib` 和 `cargo check --tests` 均退出 0。 | 新增 production masked override 场景已编译但因共享机器链接资源拥塞未取得执行结果；完成前不把 M3-T3 标记为最终完成。 |
| 2026-07-11 | 04-M3/T3 SubMachine 边界收束 | `plugins_04_m3_t3_sub_machine_parent_transition_compiled_pending_run` | 将 resolver 从 `state_machine_step.rs` 拆到 `nested_machine_resolve.rs`，递归 pose/event/time 采样归 `nested_machine_sample.rs`；实例键加入父状态 owner，避免同一 child machine 被兄弟父状态引用时共享运行态。resolver 仅在父请求通过 normalized exit gate 或父迁移已活动时停止下钻，使父层可以从 SubMachine 状态迁出，同时未到 exit-time 时子机继续推进；迁移两端可递归采样嵌套姿态。新增父层迁出生产合同并通过 `cargo check --tests` 编译。 | 父层迁出与 layer 生产测试的可执行绿灯仍待机器负载下降后取得；当前只记录编译证据。 |
| 2026-07-11 | 04-M3/T3 Layer interruption/continuity 收束 | `plugins_04_m3_t3_layer_interruption_continuity_check_passed` | 每个 layer machine 现调用与 base machine 相同的 `select_interruption_candidate`，遵守 None/CurrentToNext/NextToNext/Both；打断 active A→B 时先采样包含既有 interruption source 的已混合姿态，再将它登记为 B→C 的固定 source，旧迁移与新迁移分别按自身 from/to 时钟发布事件。迁移完成或退回静态 state 时同步清理 source。新增生产合同从 5.0 的 Idle/Run 混合姿态打断到 Sprint，并要求下一半秒为 12.5 而非旧迁移的 10.0。Windows nightly locked/offline `runtime_physics_animation_tick_contract` 定向 `cargo check` 退出 0（50.1 秒），scoped rustfmt/diff hygiene 通过。 | 新合同与此前 masked layer、parent-transition 一样仍待 executable 行为绿灯；不把静态检查写成行为通过。 |
| 2026-07-11 | 04-M3/T3 Layer typed diagnostic 收束 | `plugins_04_m3_t3_layer_typed_diagnostic_tests_check_passed` | `blend_layer_pose` 从静默 bool 改为 `Result<(), AnimationStateMachineLayerError>`，分类 bone-count/name mismatch、base/layer `PoseBufferError` 与 `PoseBlendError`；失败层不发布其 clip events，而是发布带 entity/layer name 的 `AnimationStateMachineLayerDiagnostic`，并注册 `animation.events.layer_diagnostic` / `animation.layer_diagnostic.v1` catalog。私有合同锁定 Hand/Foot row 错位返回 typed error。Windows nightly locked/offline Animation 完整 `cargo check --tests` 退出 0（25.3 秒），scoped rustfmt/diff hygiene 通过。 | diagnostic 行为订阅与完整 executable 套件仍待链接窗口；实现和静态合同完成。 |
