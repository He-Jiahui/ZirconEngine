# Plugins 04 Animation M5 产出记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 记录边界：本文件只记录 M5 IK 与动画事件总线的具体产出、验证与未接线边界。

## 状态与产出记录

| 日期 | 里程碑 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-07-11 | 04-M5/T1 production IK executable 收口 | `plugins_04_m5_production_tick_34_of_34_passed` | Windows nightly locked/offline 完整 production Tick executable 34/34 通过；`queued_two_bone_ik_runs_after_pose_blend_before_pose_apply` 与 `queued_look_at_uses_model_space_target_and_clamps_final_pose` 均真实执行并通过，证明 stable ID→dense slot、model-space solve、最终 local rotation、one-shot drain，以及 IK 位于最终混合/feed 后、Physics target/PoseApply 前。结合独立 `animation_ik_contract` 4/4，M5-T1 的数学、Manager 与生产路径行为均有 executable 证据。 | M5 实现与 Windows 定向行为闭环；post-hard-cut Windows/WSL 全 `--tests` 仍是最终里程碑门，不用本行替代跨平台全套。 |
| 2026-07-11 | 04-M5/T1 IK 数学内核 | `plugins_04_m5_t1_two_bone_look_at_math_3_of_3_passed` | 对照 Unreal AnimationCore `TwoBoneIK.cpp` 实现 plugin-local `TwoBoneIkJob` 与 `LookAtJob`。Two-bone 保持上/下肢长、使用 pole 定义弯曲平面、对不可达目标做最大伸展夹取；LookAt 对旋转弧做角度 clamp 与 weight 插值。TDD RED 在类型缺失时编译失败；Windows nightly locked/offline focused 3/3 通过，覆盖可达目标、不可达夹取和 LookAt 30° 上限。 | 还缺 Manager 命令/组件到 dense TargetSlot 的资产输入契约，因此未将数学内核假装成生产 `PoseBlend → IK → PoseApply` 完整接线，M5-T1 仍为进行中。 |
| 2026-07-11 | 04-M5/T2 AnimationClipEvent 注册 | `plugins_04_m5_t2_clip_event_registration_1_of_1_passed` | `register_runtime_system` 现在通过 SDK builder 正式注册 `AnimationClipEvent`，并产生 `animation.events.clip` / `animation.clip_event.v1` catalog 元数据；既有 clip/graph/state-machine/transition 发送路径仍共用 dormant event-store 语义。注册 focused 测试 1/1 通过；本次前的完整生产 tick 套件已证明四类 clip event 可观测且无重复。 | 正式 Animation 全套测试阶段再复跑事件行为与注册快照；M5-T2 实现项完成。 |
| 2026-07-11 | 04-M5/T1 Manager → dense IK 生产接线 | `plugins_04_m5_t1_manager_dense_ik_postprocess_check_passed` | Runtime framework 新增可序列化 `AnimationIkCommand::{TwoBone,LookAt}`、typed validation error 与 `AnimationManager::{queue,drain}_ik_commands`；Runtime 吸收层的默认 Manager 按 World 持有 4096 条上限的 poison-recovering 队列。插件在最终 clip/graph/state-machine/layer pose 合并后消费命令，复用 revision-aware skeleton target table 将稳定 ID 编译为私有 `TargetSlot`，按骨架层级在 model space 求解，再把 local rotation 写回最终 pose；结果随后才发布给 Physics 与 PoseApply。无 pose/骨架、目标不存在、shape/chain 错误与 solver 错误通过已注册的 `AnimationIkDiagnostic` event 上报。Manager contract 与真实 TwoBone/LookAt tick 场景分别通过 Windows nightly locked/offline `cargo check`，且 scoped format/diff hygiene 通过。 | 行为测试 `animation_ik_contract` 在 Runtime test link 阶段达到 604 秒外层硬超时，没有产出 executable、OOM、Rust 错误或断言结果；超时期间其他 Runtime/Editor lane 重新出现。故本行不把 `cargo check` 或无断言超时写成行为通过。待资源释放后运行 Manager/TwoBone/LookAt 三条新增行为，再复跑 Windows/WSL 全套。 |
| 2026-07-11 | 04-M5/T1 IK contract 低内存可执行复验 | `plugins_04_m5_t1_lowmem_ik_contract_4_of_4_passed` | 在独立 `F:\cargo-targets\zircon-animation-m3-lowmem`、`CARGO_INCREMENTAL=0`、`RUSTFLAGS=-C debuginfo=0`、单 job 下执行 nightly locked/offline `animation_ik_contract`，457.8 秒后退出 0，数学 3 条与逐 World Manager 队列 1 条合计 4/4 通过。该结果取代上一行 604 秒无 executable 的暂存结论，并实证输入校验、world 隔离、drain one-shot 语义及 TwoBone/LookAt 数学边界。 | 两条生产 Tick 的稳定 ID→dense slot→最终 pose 后处理行为仍待 executable；M5-T1 在取得该 2/2 以及 post-hard-cut Windows/WSL 全套前不标记正式完成。 |
