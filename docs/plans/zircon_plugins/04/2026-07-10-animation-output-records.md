# Plugins 04 Animation 产出记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 迁移说明：Plugins 04 的具体实现、测试和阻塞事实只追加到本编号归档；父计划仅保留当前概述。

## 状态与产出记录

| 日期 | 里程碑 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-07-11 | 04-M2/T3 与 03-M5 对偶 Ragdoll 生产接线 | `plugins_04_m2_t3_physics_ragdoll_focused_5_of_5_passed` | Physics 新增 folder-backed `skeletal/{profile,runtime}.rs`：严格 `.ragdoll.toml` profile 在场景变更前验证 id、唯一骨路径、父存在/环、质量、shape、body offset 与 0..1 blend weight；两阶段 `spawn_configured` 按父拓扑生成 rigid body、collider、Generic6Dof joint 和 `PhysicsSkeletonJointBinding`，失败时回滚。`RagdollRuntime` 以 Arc-backed 配置保存 Animated/Simulated/Blended 模式、逐骨权重与 offset；FixedUpdate 用前一 Animation pose 驱动 Animated kinematic body，并从连续动画目标计算有限线/角释放速度；切到 Simulated/Blended 时保持 body transform、一次性注入速度，后续物理帧不覆盖求解器速度。FixedPostUpdate 从同步 body 反算 parent-local bone pose，写入 mode × profile mask × interpolation alpha 的 `SimulatedPoseFeed`。生产库 `cargo check --lib` 通过；计划精确命名的 `ragdoll_drop_golden_snapshot` 与 profile spawn/rollback、无跳变速度继承、Blended 权重合同在 Windows nightly locked/offline 聚焦复跑 5/5 通过。scoped rustfmt、diff hygiene、production allow/TODO/unwrap 扫描通过，`runtime.rs` 500 行、`profile.rs` 低于 500 行。 | Physics producer 行为已闭环；M2-T3 仍需 Animation `simulated_pose_blends_under_ragdoll_mask` 和相关 production Tick 场景取得 executable，随后再做跨插件收口与 post-hard-cut Windows/WSL 全套。 |
| 2026-07-11 | 04-M2/T2 分层 PoseBlend 阶段 | `plugins_04_m2_t2_layered_masked_additive_pose_blend_4_of_4_passed` | `PoseBuffer` 新增 `PoseLayer`/`PoseLayerBlendMode` 与 `blend_layers`，按 layer 顺序组合 override/additive，并将 `MaskWeights` 逐 dense row 乘入 source row weight；mask/pose shape mismatch 返回 typed `PoseBlendError::MaskShapeMismatch`，无新字符串或热路径分配。新增计划锚点 `upper_lower_body_split_blend_scenario`，与既有 golden/partial/error 合同组成 focused 4/4 通过；随后 fresh locked/offline Animation `cargo check --tests` 与 rustfmt check 退出 0。`pose_buffer/blend.rs` 仍为单一 137 行算子 owner。 | M2-T3 的 ragdoll `SkeletalPoseTargets`/`SimulatedPoseFeed` 依赖 Physics 03-M5-T1，当前不越过该依赖。继续等待 Runtime handback 完成 M1 final pose owner，或转入 M3-T1 条件表达式完整化。 |
| 2026-07-11 | 04-M2/T3 目标姿态桥阶段 | `plugins_04_m2_t3_pose_target_bridge_windows_78_and_physics_43_passed` | 按 03/04 逐字对偶边界新增 `core/framework/physics/skeletal_pose.rs`，定义 entity→骨骼行的 `SkeletalPoseTargets` 与 `SimulatedPoseFeed`；行数据明确使用 `local_transform`，不把局部姿态误称为 world transform。Physics 插件拥有双资源注册，Animation 仅在 Physics 已安装资源时于 `animation.evaluate` 发布当前最终姿态，不直接依赖 backend。首次定向测试暴露 `WorldRuntimeExtensionSet` 只合并 runtime system、丢失 resource/event/native system/component 的底层缺口；修复为 clone/merge/finalize/commit 的全世界扩展族合并，并新增核心单元/公开集成回归入口。生产桥定向 1/1、Animation Windows nightly/offline 全量 78/78、Physics 默认全量 43/43、rustfmt/diff hygiene 通过。 | Physics 尚未在 `physics.sync_to_scene` 写入 `SimulatedPoseFeed`，Animation 尚未在 PoseBlend 前按 ragdoll mask 消费该 feed，计划测试 `simulated_pose_blends_under_ragdoll_mask` 未实现；因此 M2-T3 只记录桥契约与目标姿态发布完成，不宣称 ragdoll 闭环。独立 Runtime 集成 check 被并行 UI `rich_link` 8 个范围外错误阻断。 |
| 2026-07-11 | 04-M2/T3 SimulatedPoseFeed Animation 侧消费 | `plugins_04_m2_t3_simulated_feed_animation_side_check_passed` | 新增 `pipeline/simulated_pose_blend.rs`，在 base/graph/state-machine/layer 最终合并后、IK 之前读取 Physics-owned feed；`normalized_weight` 明确定义为 Physics 已合成的 mode × mask × interpolation alpha，逐骨混合 translation/scale 与 quaternion slerp。`SkeletonTargetTable` 在加载边界编译唯一骨名索引，运行期不构造字符串 map；歧义骨名、非有限/零四元数变换和非法权重均不污染 pose。新增计划同名生产合同以 Hand=10、weight=0.25 验证最终 pose 及随后发布的 target 均为 2.5。Windows nightly locked/offline production tick `cargo check` 退出 0（28.8 秒），scoped rustfmt/diff hygiene 通过。 | Physics 侧尚未从真实 ragdoll body 写 feed，新增合同也尚未取得 executable 行为绿灯；因此只关闭 Animation 消费实现，不宣称跨插件 ragdoll 运行闭环。 |
| 2026-07-11 | 04-M2/T1 Avatar Mask 编译阶段 | `plugins_04_m2_t1_avatar_mask_toml_dense_weights_2_of_2_passed` | 新增 `mask/{asset,compile,error}.rs` folder owner：`.avatar_mask.toml` 以 deny-unknown-fields 的 typed asset 解析，规则支持 subtree inheritance、后置规则覆写与逐深度 `boundary_weights`；`MaskWeights` 在 skeleton `TargetTable` 上编译为与 pose 同序的 `Box<[f32]>`，验证 id/target/0..1 finite weight，并保留 `AnimationAvatarMask` editor view。`SkeletonTargetTable` 现在同时保存加载期 canonical bone path，热路径不回退字符串树遍历。TDD focused 首轮 1/2：gradient 通过，inherit/override fixture 因错误省略 `Chest` 路径而返回正确的 typed `UnresolvedTarget`；修正 fixture 后 `subtree_weight_inherits_and_overrides` 与 `mask_boundary_gradient_values` 2/2 通过。新增 serde/toml 直接依赖并离线同步插件 lock；lock 中并发 Reflection Probe 的 `thiserror` 条目原样保留。 | M2-T2 将以 `MaskWeights` 实现 layers × masks × additive 的分层 PoseBlend；M2-T3 仍依赖 Physics 03-M5-T1。M1 最终 pose handoff 继续等待 Runtime owner handback。 |
