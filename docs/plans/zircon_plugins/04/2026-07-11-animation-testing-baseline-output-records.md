# Plugins 04 Animation 测试基线归档

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 验收边界：本文件归档早期 Windows/WSL 全套与 M3 增量验证事实。

## 状态与产出记录

| 日期 | 阶段 | 状态锚点 | 验证事实 | 未纳入项 |
|---|---|---|---|---|
| 2026-07-11 | Plugins 04 Runtime 正式测试 | `plugins_04_runtime_formal_windows_wsl_75_of_75_passed` | Windows nightly locked/offline `cargo check -p zircon_plugin_animation_runtime --tests` 退出 0；Windows nightly locked/offline `cargo test --tests` 75/75 通过，退出 0；WSL nightly locked/offline 同一 `cargo test --tests` 75/75 通过，退出 0。套件包含 runtime unit 4、Avatar Mask 2、BlendSpace 4、compiled clip 13、compiled graph 2、compiled state/condition 3、GPU skinning 4、IK 3、pipeline structure 2、PosePool 零分配 1、PoseBlend 4、transition 3、target table 10 与生产 physics+animation tick 20。rustfmt check、`git diff --check`、旧 `scene_hook`/单体 compiled-state 路径缺席、≥1000 行新文件检查、production `allow/unwrap/expect` 扫描 0 均通过。 | M1 final-pose reusable ownership；M2 ragdoll DTO；M3 exit-time/interruption/StateKind 资产 DTO；M4 Render storage/shader/1000 实例基准；M5 dense IK job 输入；M6 Editor Cargo 行为复跑。这些仍依赖其他 owner 或共享边界，本行不缩小该未完成列表。 |
| 2026-07-11 | M3 transition hard-cut 增量复验 | `plugins_04_m3_transition_incremental_28_of_28_passed` | 在 transition asset 硬切及生产中断接线后，Windows nightly/offline `cargo check --lib` 退出 0；随后集中复跑 `animation_compiled_state_machine_contract` 3/3、`animation_state_transition_contract` 3/3、`runtime_physics_animation_tick_contract` 22/22，共 28/28 通过。期间曾由 `state_machine_transition_emits_from_and_to_graph_clip_events` 精确捕获事件时间窗回归（0/2），修复后同组全绿。 | 该行是 hard-cut 后增量证据，不能替代上一行 75/75 的全套 Windows/WSL 基线；全套 post-hard-cut Windows/WSL 复验仍需在共享工作树稳定后执行。未完成项更新为 M1 final-pose ownership、M2 ragdoll、M3 StateKind/BlendSpace 资产与 duration cache、M4 Render upload/shader/benchmark、M5 IK job、M6 Editor Cargo。 |
| 2026-07-11 | M3 transition hard-cut Windows 全套复验 | `plugins_04_runtime_post_transition_windows_77_of_77_passed` | 在把 interruption source 与 transition selection 拆分为单一职责 owner 后，Windows nightly/offline `cargo test -p zircon_plugin_animation_runtime --tests` 完整通过 77/77，退出 0。计数为 lib 4、Avatar Mask 2、BlendSpace 4、compiled clip 13、compiled graph 2、compiled state 3、GPU skinning 4、IK 3、pipeline 2、allocation 1、PoseBlend 4、transition 3、target table 10、production tick 22。最长生产 owner `state_machine_step.rs` 409 行；新增 `state_machine_transition.rs` 116 行、`interrupted_transition_source.rs` 54 行。 | WSL post-hard-cut 全套尚未复跑；共享 Runtime 当前仍输出既有 warning，本 slice 没有 Animation warning 或测试失败。 |
| 2026-07-11 | M2 ragdoll 目标姿态桥 Windows 全套复验 | `plugins_04_runtime_pose_bridge_windows_78_of_78_passed` | `pose_targets_visible_to_physics_step` 定向 1/1 通过；随后 Windows nightly/offline `cargo test -p zircon_plugin_animation_runtime --tests` 完整通过 78/78，退出 0，production tick 从 22 增至 23，其余计数与上一行一致。Physics 资源所有权调整后，默认 `cargo test -p zircon_plugin_physics_runtime --tests` 同步通过 43/43（unit 10 + contract 33）。Scoped rustfmt 与 `git diff --check` 通过，新增/修改 owner 均远低于 1000 行。 | 默认特性 Runtime `--lib` focused 测试和公开集成测试各在 Windows 链接 604 秒超时，均未取得测试结果；`--no-default-features` lib-test 被现有测试无条件引用 Graphics/UI/Script 的 84 个错误阻断；最终公开集成 `cargo check` 又被并行 UI `rich_link` 8 个范围外错误阻断。因此只把真实通过的 Animation 78/78、Physics 43/43 和桥定向 1/1 计为本行证据。WSL post-hard-cut 仍待复跑。 |
| 2026-07-11 | M3 StateKind/BlendSpace 生产增量复验 | `plugins_04_m3_t3_state_kind_blend_space_production_7_of_7_passed` | Windows nightly/offline `animation_compiled_state_machine_contract` 4/4、`animation_state_kind_asset_contract` 1/1、生产 `blend_space_1d_state_blends_production_graph_poses` 1/1、生产 `blend_space_2d_state_blends_production_graph_poses` 1/1 通过，合计 7/7。生产 1D 输出 2.5，2D Delaunay 权重输出 7.5，证明资产 DTO→dense compiled state→真实 graph/clip pose→world pose 全链闭环。scoped rustfmt check 通过，`state_machine_step.rs` 从 438 行拆至 325 行，新增单一职责 `state_graph_sample.rs`。修复同轮暴露的 project manifest 反序列化目标类型后，Windows nightly/offline `cargo check --tests` 退出 0。 | 本行是增量证据，不替代 78/78 Windows 全套基线；本行时尚未接线的 Clip 已由下一行闭环，SubMachine、完整多层 machine 与 WSL 全套仍待完成。 |
| 2026-07-11 | M3 direct Clip state RED→GREEN | `plugins_04_m3_t3_direct_clip_state_production_8_of_8_passed` | 生产 RED 1/1 精确失败于缺少 state pose；实现后 `animation_compiled_state_machine_contract` 5/5，集中复跑 `blend_space_state` 3/3（direct Clip、1D、2D），共 8/8 通过；真实 clip 输出 10.0 hand translation，既有 BlendSpace 输出继续为 2.5/7.5。 | 本行是定向增量证据；SubMachine、完整多层 machine、Windows/WSL 全套复验仍待完成。 |
| 2026-07-11 | M3 SubMachine nested state | `plugins_04_m3_t3_sub_machine_nested_state_8_of_8_passed` | Windows nightly/offline compiled state 6/6；生产 nested delegate 1/1；nested child Idle→Run 迁移在两个 tick 中持久化并输出 5.0→10.0，1/1 通过，共 8/8。 | 显式 layer stack 与 Windows/WSL 全套复验仍待完成。 |
| 2026-07-11 | M3 layer hard-cut / production compile | `plugins_04_m3_t3_layer_asset_hard_cut_roundtrip_4_of_4_passed` | Windows nightly/offline `animation_state_machine_layer_contract` 4/4，覆盖唯一状态机资产的 layer binary roundtrip、blend mode/mask 编译、非法权重/掩码 typed reject 与 direct-reference 次序。补齐 Animation 正式依赖的 `physics-contracts` feature 后，`cargo check -p zircon_plugin_animation_runtime --lib` 与 `--tests` 均退出 0；后者包含 production masked layer 与 SubMachine parent-transition 新合同。 | 两条新增 production 可执行测试和 post-hard-cut Windows/WSL 全套仍待取得运行绿灯；共享机器链接阶段持续超时，不把超时写成通过。 |
| 2026-07-11 | M3 production executable retry | `plugins_04_m3_production_retry_blocked_by_rustc_resource_pressure` | 第一次 focused 运行在 `zircon_runtime` 编译期由 nightly rustc OOM/`STATUS_STACK_BUFFER_OVERRUN` 退出；随后使用独立 F: target、`CARGO_INCREMENTAL=0`、单 job 与无 debuginfo 重试。Runtime fingerprint 最终完成链接，但外层 10 分钟门禁先超时，后续复跑又因共享源码持续变化重新进入 Runtime 编译，均未产出 production test executable。这属于当前共享机器资源/源码抖动门禁，不是测试断言失败。 | 保留生产合同为“已编译、待执行”；待其他构建 lane 释放后优先复跑 parent-transition、masked layer 两条 focused，再跑 Windows/WSL 全套。 |
