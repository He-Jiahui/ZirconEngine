# Plugins 04 Animation M4 产出记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 记录边界：本文件只记录 M4 GPU Skinning 的具体产出、验证和外部接线边界。

## 状态与产出记录

| 日期 | 里程碑 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-07-11 | 04-M4/T1-T2 palette/双缓冲/回退 | `plugins_04_m4_palette_double_buffer_fallback_parity_4_of_4_passed` | 新增 `gpu_skinning/**` folder-backed owner；`SkinningPalette` 从 skeleton bind pose 与当前 local pose 构建 world-space joint matrices，定额限制 `MAX_SKIN_JOINTS=256`；`SkinningPaletteDoubleBuffer` 保留 current/previous 帧 palette；`AnimationGpuSkinningDecision` 严格消费现有 readiness 契约，对资源未就绪和超 256 骨返回 CPU fallback 及可诊断原因。TDD RED 在缺失四个公开契约时编译失败；GREEN 后 Windows nightly locked/offline focused 测试 4/4 通过，包含 palette 双缓冲交换、not-ready 回退、over-256 回退和 GPU/CPU matrix 金样同精度。rustfmt/diff hygiene 通过，最大 owner 99 行。 | M4-T3 继续接入真实 Render storage/shader 路径。 |
| 2026-07-11 | 04-M4/T3 Render storage hard cut | `plugins_04_m4_render_storage_shader_runtime_lib_check_passed` | Render 继续只消费中立 `RenderSkeletalPoseExtract`，不依赖 Animation 插件类型。固定 256-joint ABI 从 uniform 硬切为 `SkinnedMeshJointPaletteStorage`；group3 bindings 3/4 与 WGSL 均为 vertex-visible read-only storage，旧类型、文件、uniform 声明及调用点已全局清零。fallback 边界修正后的 Windows nightly locked/offline `cargo check -p zircon_runtime --lib` 在禁用 incremental 的共享 D 盘 target 上通过，耗时 3 分 06 秒；同一 Runtime test binary 的 storage bind-group layout 精确测试 1/1 通过、7611 filtered。 | 最终整套仍随 post-hard-cut 跨平台门复跑。 |
| 2026-07-11 | 04-M4/T1 persistent WGPU 双缓冲 | `plugins_04_m4_wgpu_palette_cpu_fallback_1_of_1_passed` | GPUScene 现按 stable instance 持有两个 `STORAGE | COPY_DST` buffer；draw build 始终写 non-committed slot，兼容 previous 只绑定 committed slot，成功 submit 后才交换，第三帧复用第一槽。source 未就绪但 palette 合法的 CPU fallback 帧仍 stage/commit 当帧 palette、只是不绑定 draw，因此 GPU 恢复帧的 previous 指向紧邻 CPU 帧；palette 缺失时显式丢弃失败帧 staged slot。无 GPU slots owner 测试 3/3 通过；Windows nightly locked/offline 真实 `Arc::ptr_eq` WGPU filtered test 1/1 通过、7611 filtered、2.55 秒，日志无 adapter skip。 | 首轮 15 分 22 秒构建结束时协调器因会话心跳过期把已通过任务记为 orphaned；随后复跑因共享资产迁移再次变化，被接口导出与 `AssetReference::guid()` 测试漂移抢先阻断，未运行 M4 测试，不覆盖首轮实际通过证据。 |
| 2026-07-11 | 04-M4/T3 WGSL storage 一致性 | `plugins_04_m4_fallback_shader_storage_validation_passed` | 使用构建缓存中的正式 Naga 对生产拼接 `fallback mesh + GPUScene + lighting/shadow/volumetric/environment` 源做 parse + full validation，通过；同时断言 bindings 3/4 为 storage、current/previous joint count helper 存在、旧 `SkinnedJointPaletteUniform` 不存在。同一 Runtime test binary 的生产 fallback WGSL 精确测试 1/1 通过、7611 filtered。 | 随最终 Runtime 全套复验同一内建 shader test。 |
| 2026-07-11 | 04-M4/T3 1000 实例 WGPU 基准 | `plugins_04_m4_gpu_storage_1000_instances_2000_buffers_123884us` | 真实 offscreen WGPU adapter/device 创建 1000 实例 × current/previous 两槽共 2000 个 16400-byte storage/copy-dst buffers，逐槽 `queue.write_buffer` 并 submit；总 payload 32,800,000 bytes，实测 123,884 µs。生产 ignored benchmark已同步为相同双槽负载；同一 Runtime test binary 的 1000 实例双槽内存预算精确测试 1/1 通过、7611 filtered。 | 最终性能门可在固定 GPU/驱动环境重复采样；本记录只声明本机可执行基准，不声明跨设备 FPS。 |
| 2026-07-11 | 04-M4 结构规范/6 月审查聚焦门 | `plugins_04_m4_structure_review_focused_audit_passed` | 触及生产 owner 最大 766 行，低于 800 行软上限；新增 slots/storage owner 为 74/128 行；M4 对 `mod.rs` 只增加声明或精选重导出，未增加行为；storage 固定 ABI 已移除生产 `expect`，独立真实依赖类型编译通过；新增 panic/expect 仅位于测试断言。尾随空白、冲突标记为 0，旧 uniform 类型/文件/生产 WGSL 声明清零。全局结构审计的 Render scoped migration debt 为 0，现有风险仅为范围外 asset migration legacy/unclassified 债；产出记录审计仍为既有 Editor/Render 19 条，Plugins 04 未新增违规。 | 最终整库结构/Cargo 门仍随共享迁移恢复后复跑；不把范围外全局债提升为 M4 通过。 |
