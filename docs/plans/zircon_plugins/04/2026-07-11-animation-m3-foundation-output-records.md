# Plugins 04 Animation M3 基础产出记录

> 来源文件：`docs/plans/zircon_plugins/04-animation.md`
> 记录边界：本文件归档 M3 条件、迁移、BlendSpace 与 StateKind 基础阶段事实。

## 状态与产出记录

| 日期 | 里程碑 | 状态锚点 | 当前事实 | 后续项 |
|---|---|---|---|---|
| 2026-07-11 | 04-M3/T1 条件表达式编译 | `plugins_04_m3_t1_compiled_condition_expression_matrix_3_of_3_passed` | 将原单文件 compiled state machine 硬切为 `state_machine/compiled/**` 与 `state_machine/condition_expression/**` 两个单一职责目录；`ConditionExpression` 支持 Condition/All/Any/Not，加载期编译为后缀指令和 dense parameter slot，并限制最大深度。已编译结果不受源表达式后续变更影响，数值比较对非有限值返回 false；旧平面迁移条件统一通过隐式 All 程序进入生产 compiled state machine。Windows nightly locked/offline focused 套件 3/3 通过，完整生产 `runtime_physics_animation_tick_contract` 20/20 通过，rustfmt check 通过。Cargo 输出中的 53 条 warning 来自当前共享 `zircon_runtime`，本 slice 无新增 Animation warning。 | 继续 M3-T2 duration/exit time/中断策略/交叉淡化；不修改当前被 Runtime 会话持有的中立 DTO，先实现 plugin-local 编译与运行时边界。 |
| 2026-07-11 | 04-M3/T2 迁移时序基础 slice | `plugins_04_m3_t2_transition_timing_policy_crossfade_3_of_3_passed` | 新增 `state_machine/transition/**` folder-backed owner，实现有限且非负的 duration/elapsed 归一化、normalized exit time gate、None/CurrentToNext/NextToNext/Both 中断策略矩阵与和为 1 的单调 crossfade weights。TDD RED 在缺失五个公开类型时编译失败；GREEN 后 Windows nightly locked/offline focused 测试 3/3 通过，rustfmt/diff hygiene 通过，每个新 owner 不超过 62 行。生产 `state_machine_step` 的交叉淡化进度已统一调用该内核，接入后完整生产 tick 20/20 仍通过。 | 当前 Runtime-owned 中立 asset/scene transition DTO 只有 duration，因此 exit-time 和 interruption 尚无法从资产流入生产 compiled transition；待该所有权交回后一次硬切，不添加兼容旁路。 |
| 2026-07-11 | 04-M3/T3 BlendSpace slice | `plugins_04_m3_t3_blend_space_delaunay_weights_4_of_4_passed` | 新增 `state_machine/blend_space/**` folder-backed owner；1D 编译器排序样本并做有界线段插值，2D 编译器构建确定性 Delaunay 三角形、使用重心权重，超出 convex hull 时投影到最近边。发现并修正四点共圆时所有候选三角形同时保留的重叠问题，通过共边同侧/严格包含判定做确定性去重。Windows nightly locked/offline focused 测试 4/4 通过；独立实源 harness 也曾 4/4 通过并已删除。rustfmt/diff hygiene 通过，最大 owner `blend_space_2d.rs` 156 行。 | 当前中立 Animation state asset 只有 GraphRef 形态，无 StateKind/BlendSpace DTO；本 slice 完成 plugin-local 编译/采样内核。多层 pose 组合已由 M2-T2 `PoseLayer`/mask/additive 语义覆盖；资产接线待 Runtime owner 交回后硬切。 |
| 2026-07-11 | 04-M3/T2 transition asset 硬切 | `plugins_04_m3_t2_transition_asset_exit_time_check_tests_passed` | 在确认无活跃 Runtime 所有者且取得路径租约后，中立 `AnimationStateTransitionAsset` 一次性新增 serde-defaulted `exit_time` 与 `AnimationTransitionInterruptionPolicyAsset`，所有 Rust 构造点同步迁移，不保留旧 DTO/shim。`CompiledTransition` 现在保存完整 `TransitionDesc`，compiled contract 覆盖 0.75 exit time 和 Both 策略；生产 state-machine step 根据当前 graph 最长 clip 时长计算 normalized state time，在 exit gate 通过后才启动迁移。Windows nightly locked/offline Animation `cargo check --tests` 退出 0。 | 中断策略资产/编译表示已完成；后续生产选择与连续性见下一行。StateKind/BlendSpace 资产形态仍属 M3-T3 未完成项。 |
| 2026-07-11 | 04-M3/T2 生产中断与连续性 | `plugins_04_m3_t2_production_exit_interruption_continuity_28_of_28_passed` | 按 TDD 增加生产级 exit-time gate 与 active-transition 中断场景；首次组合测试暴露新迁移事件窗口错误（2 个事件实际为 0），修正为新迁移仍以当前帧 `pending.from_time_seconds` 和目标 0 秒作为事件起点。`AnimationEvaluationPipeline` 现持久化被打断时的已混合源姿态；`Both` 策略可从 active A→B 的 next 状态选择 B→C，首帧保持中断前姿态，随后从该姿态连续交叉淡化到 C，过期 entity 状态会被回收。duration、0.75 normalized exit time、None/CurrentToNext/NextToNext/Both 矩阵与生产事件/姿态路径集中复跑：compiled 3/3、transition kernel 3/3、production tick 22/22，共 28/28 通过；独立 `cargo check --lib` 退出 0，rustfmt/diff hygiene 通过。 | `normalized_graph_time` 当前会读取已编译 graph 并按最长有效 clip 推导时长；后续性能收束需把依赖 clip revision 的 state duration 编译进有界 cache，避免 exit gate 每帧重复资产读取。M3-T3 的 StateKind/BlendSpace 资产接线仍未完成。 |
| 2026-07-11 | 04-M3/T2 graph timing cache 收束 | `plugins_04_m3_t2_graph_timing_revision_cache_3_of_3_passed` | 新增 `pipeline/graph_timing_cache.rs` 单一 owner，按 `{graph_id, skeleton_id}` 保持 128 项有界 LRU；cache signature 包含每个 clip 的 id、resource revision 和 playback-speed bits，duration 使用最长有效 clip 的速度修正值。已有 `ResourceSnapshot` 时不触发磁盘加载，依赖 clip revision 或速度改变会重算。`cargo check --lib` 退出 0；三个生产 transition 场景（duration、exit gate、A→B→C continuity）3/3 通过。 | 当前 signature 为保证 revision-aware 仍逐 clip 获取 resource snapshot；后续若 profiling 证明需要，可把 duration metadata 并入 compiled graph dependency table。M3-T3 StateKind/BlendSpace 资产仍未接线。 |
| 2026-07-11 | 04-M3/T3 StateKind/BlendSpace 资产与生产求值 | `plugins_04_m3_t3_state_kind_blend_space_production_7_of_7_passed` | 中立 `AnimationStateAsset` 已从单一 `graph` 字段硬切为 `AnimationStateKindAsset`，提供 `Clip`、`BlendSpace1D`、`BlendSpace2D`、`SubMachine`、`GraphRef` 五种显式形态；当前/v2/v1 二进制 DTO 可把旧 graph 状态迁移为 `GraphRef`，direct-reference 读取覆盖全部变体。compiled machine 将 BlendSpace 参数编译为 dense slot，并用固定三个 sample 槽返回 graph/weight，源资产后续变更不影响编译结果。生产管线按全部非零样本计算最长 duration、采样事件、求值姿态并使用现有 `PoseBlend` 折叠；迁移两端也消费完整样本集。TDD 与定向验收通过：compiled 4/4、StateKind binary/direct-reference 1/1、生产 1D 1/1、生产 2D 1/1，共 7/7；2D 三角形在 `(0.25, 0.25)` 得到 0.5/0.25/0.25 并输出 7.5 的真实骨骼位移。为遵守结构规范，graph sample/duration/event/pose 逻辑从 438 行 `state_machine_step.rs` 拆入独立 `state_graph_sample.rs`，前者降至 325 行。随后修复同轮暴露的 project manifest 反序列化类型标注，Windows nightly/offline `cargo check --tests` 退出 0。 | `Clip` 与 `SubMachine` 已进入资产/序列化/引用合同；本行记录时尚未接线的 Clip 已由下一行闭环。完整多层 machine owner 仍待后续切片，post-hard-cut WSL 全套仍待复跑。 |
