---
title: Editor retained tab drag borrowed arranged anchor protected plan routing
date: 2026-08-23
status: routing_requested
owner_record: 2026-08-23-editor-retained-tab-drag-borrowed-arranged-anchor-architecture-review.md
---

# Protected plan updates

This routing record requests owner-plan changes without overwriting shared ledgers or numbered
plans.

## `docs/plans/performance/pending.md`

Keep one concise module entry:

`zircon_editor/src/ui/retained_host/tab_drag.rs + tab_drag` - 9/9 Rust files source-reviewed;
release-row borrowed M0, committed arranged-frame authority, instance-to-host index, managed tests,
WPR/allocation/power and real-window parity remain pending.

Do not add the module to `review.md` until M0-M3 and the dynamic gates in the owner record pass.

## `docs/plans/performance/01-mvp-performance-audit-and-optimization.md`

Make final editor tab drop an MVP-P0 interaction item. Apply borrowed-row M0 without waiting for
dynamic tooling, then require zero drop-time title measurement and one committed arranged-frame
generation before acceptance.

## `docs/plans/performance/02-unreal-aligned-engine-system-hard-cutover.md`

Route the final cutover through one Unreal-aligned tab-well authority: retained typed tabs, committed
arranged frames and one insertion lookup. Remove production width reconstruction only after every
document/drawer/floating consumer has migrated; keep no compatibility geometry facade.

## `docs/plans/zircon_editor/editor_ui/08`

Own the host-scene/drop integration. Extend the Workbench committed generation with stable tab-frame
or insertion-boundary access; drop must not rebuild a parallel `TabStripHitBox` from titles.

## `docs/plans/zircon_editor/editor_layout/15-component-standardization-from-primitives.md`

Keep Runtime Text sizing policy pure. Authored templates and fallback chrome must publish the same
arranged tab artifact, including close and insertion frames, rather than exporting only a broad
strip rectangle.

## Existing performance owner records

Merge M1 with
`2026-08-22-editor-ui-workbench-document-tabs-committed-frame-authority-architecture-review.md`.
Merge shell receipt/generation counters with
`2026-08-23-editor-retained-shell-pointer-single-release-receipt-architecture-review.md`.
Do not create a second cache or generation for tab drag.

## `docs/plans/zircon_runtime/text/03-line-breaking-measure-and-layout.md`

Expose the existing Runtime Text metrics generation to the committed strip invalidation key.
Measure exactly once on invalidation and zero times during drop; no editor-only width algorithm.

## Acceptance handoff

The plan owner should receive the 9/9 fingerprint, focused RED/GREEN result, managed Rust tests,
1/8/32/100/1000-tab counters, same-build WPR/allocation/power evidence, real-window parity and any
render-impact RenderDoc capture. Only an accepted dynamic milestone may be committed and sent to
WeCom with quantified data.
