---
title: Virtual Geometry Product Topology Current-Source Protected Plan Routing
date: 2026-08-24
status: routing_record_only
source_review: docs/plans/performance/01/2026-08-24-virtual-geometry-product-topology-current-source-algorithm-performance-review.md
---

# Virtual Geometry Product Topology Current-Source Protected Plan Routing

## Review ledger status

Virtual geometry's **200-file non-validation set** and **65-file validation set** completed current-worktree static review at repository revision `79f64878f3b9526517644c055ad3bf5cadfccd0f`. Their fingerprints are `629052491ad2a22cb33a07e7b4060956b3c61223e53da765ca0a388a5c84d9a0` and `981677bdce5a2557902dacd5331e278df0e6c912c987222cca54b8f63518d2f4`; the **265-file** composite fingerprint is `6a04da8a30e9a93b78f8c4e90e4128e9988b7d40452b78e11f67bc8883220396`. Dynamic validation is unavailable, so protected `review.md` and `pending.md` remain unchanged.

## Canonical owner notifications

| Finding | Canonical plan owner | Required update |
|---|---|---|
| Five registered executors only validate graph metadata and return success | Plugins17 + Runtime09a | Make capability unavailable until executors record real prepare/cull/feedback/raster/overlay commands and timestamps. |
| GPU resources/render frame are constructed only by tests | Plugins17 + Runtime09a | Hard-cutover dead renderer scaffolding or wire one production owner; forbid duplicate hidden render paths. |
| Runtime prepare uploads already prepared CPU request IDs and returns empty outputs | Plugins17 + Runtime09d | Replace with delayed compact GPU feedback ring owned by the render/streaming boundary. |
| ZVG0 page metadata needs original mesh arrays and expands indexed triangles | Plugins17 + Runtime93 mesh owner | Cook self-contained compressed cluster/page payloads with hierarchy, dependencies, fixups and fallback. |
| CPU performs hierarchy traversal, visibility, fallback, page requests and record packing | Plugins17 + Runtime09a | Move bounded work queues and culling to real GPU passes; retain CPU path only as oracle. |
| Per-frame extraction/readback clones overlapping DTOs and creates many buffers | Plugins17 + Runtime09d | Introduce stable artifact/resource generations, persistent GPU pools and opt-in bounded debug snapshots. |
| Count budgets and nominal 4 KiB math do not represent encoded/resident bytes | Plugins17 + Runtime09d | Admit, upload and evict by actual bytes plus platform reserve and dependency constraints. |
| Nine renderer validation sources containing 88 tests are explicitly unwired | Plugins17 + Runtime09a | Migrate them to plugin-local/public neutral contracts, compile them, and require real submission/readback before treating them as coverage. |
| Eight default tests hard-assert local wall-clock superiority | Plugins17 | Replace noisy elapsed-time gates with deterministic operation/allocation counters; keep release timing as non-default evidence. |
| Editor descriptor points to an authoring asset without a completed runtime product | Plugins17 + Editor asset/cook owner | Expose authoring only after import/cook/status/reload/runtime generation is functional and measurable. |

## Acceptance routing

Implementation order is truthful capability -> cooked artifact -> byte-budgeted async streaming -> persistent GPU pools/uploader -> GPU-driven cull -> real visbuffer/depth raster -> opt-in debug/editor -> dynamic qualification. Local allocation microbenchmarks cannot close any earlier gate while production remains unwired.

Dynamic acceptance records exact source/build/config/device/scene identity, import/cook cost, artifact size, instance/cluster/page counts, main/render/worker CPU, scheduler wait, allocations/bytes, IO/upload/evict/readback bytes, page misses, dispatch/draw counts, GPU timestamps, barriers, frame p50/p95/p99, RSS/VRAM, wakeups, power and pixel parity.

WPR/WPAExporter are present, but there is no launchable current-source product executable; RenderDoc CLI is absent. No Git milestone commit or quantified WeCom message is warranted by this static routing record.
