---
related_code:
  - zircon_editor/src/ui/activity.rs
  - zircon_editor/src/ui/control.rs
  - zircon_editor/src/ui/asset_editor/mod.rs
  - zircon_editor/src/ui/retained_host/activity_rail_pointer/mod.rs
  - zircon_editor/src/ui/retained_host/ui/pane_data_conversion/mod.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection/menu_chrome.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/host_data.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/pane_presentation.rs
  - zircon_editor/assets/ui/editor/components/workbench/shell/workbench_viewport_panel.zui
  - zircon_editor/src/ui/retained_host/callback_dispatch/template_bridge/workbench/data_sync.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_template_nodes/style_selector/workbench_tree_row/selection.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_template_nodes/style_selector/workbench_tree_row/surface.rs
  - zircon_runtime/src/ui/surface/render/collection_rows/tree.rs
  - zircon_editor/src/ui/template_runtime/showcase_demo_state.rs
  - zircon_editor/src/ui/template_runtime/showcase_demo_state/events.rs
  - zircon_editor/src/ui/host/editor_manager_asset_editor.rs
  - zircon_editor/src/ui/host/editor_manager_asset_editor/binding.rs
  - zircon_editor/src/ui/host/editor_manager_asset_editor/designer.rs
  - zircon_editor/src/ui/host/editor_manager_asset_editor/document.rs
  - zircon_editor/src/ui/host/editor_manager_asset_editor/presentation.rs
  - zircon_editor/src/ui/host/editor_manager_asset_editor/style.rs
  - zircon_editor/src/ui/animation_editor/session.rs
  - zircon_editor/src/ui/animation_editor/session/graph.rs
  - zircon_editor/src/ui/animation_editor/session/lifecycle.rs
  - zircon_editor/src/ui/animation_editor/session/parameters.rs
  - zircon_editor/src/ui/animation_editor/session/presentation.rs
  - zircon_editor/src/ui/animation_editor/session/sequence.rs
  - zircon_editor/src/ui/animation_editor/session/state_machine.rs
  - zircon_editor/src/ui/animation_editor/session/support.rs
  - zircon_editor/src/ui/asset_editor/preview/preview_mock.rs
  - zircon_editor/src/ui/asset_editor/preview/preview_mock/entries.rs
  - zircon_editor/src/ui/asset_editor/session/lifecycle.rs
  - zircon_editor/src/ui/asset_editor/session/lifecycle/v2_projection.rs
  - zircon_editor/src/ui/asset_editor/binding/binding_inspector.rs
  - zircon_editor/src/ui/asset_editor/binding/binding_inspector/payload_editing.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/execution.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/run.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/session.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/mod.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/support.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/pipeline_plan.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/pipeline_execution.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/job.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/panel_session.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/view_model.rs
  - zircon_editor/src/ui/template_runtime/runtime/runtime_host.rs
  - zircon_editor/src/ui/retained_host/app/tests/mod.rs
  - zircon_editor/src/ui/retained_host/app/tests/support.rs
  - zircon_editor/src/ui/retained_host/app/tests/command_palette.rs
  - zircon_editor/src/ui/retained_host/app/tests/child_window_focus.rs
  - zircon_editor/src/ui/retained_host/app/tests/menu_pointer.rs
  - zircon_editor/src/ui/retained_host/app/tests/projection_geometry.rs
  - zircon_editor/src/ui/retained_host/app/tests/viewport_template_bridge.rs
  - zircon_editor/src/ui/retained_host/app/tests/root_pointer_fallbacks.rs
  - zircon_editor/src/ui/retained_host/app/tests/child_window_tabs.rs
  - zircon_editor/src/ui/retained_host/ui/tests/mod.rs
  - zircon_editor/src/ui/retained_host/ui/tests/support.rs
  - zircon_editor/src/ui/retained_host/ui/tests/host_scene_projection.rs
  - zircon_editor/src/ui/retained_host/ui/tests/host_scene_projection/assertions.rs
  - zircon_editor/src/ui/retained_host/ui/tests/apply_presentation_shell.rs
  - zircon_editor/src/ui/retained_host/ui/tests/workbench_layout_frames.rs
  - zircon_editor/src/ui/retained_host/ui/tests/welcome_presentation.rs
  - zircon_editor/src/ui/retained_host/ui/tests/scene_document_pane.rs
  - zircon_editor/src/ui/retained_host/ui/tests/floating_windows.rs
  - zircon_editor/src/ui/retained_host/ui/tests/component_showcase.rs
  - zircon_editor/src/tests/host/retained_callback_dispatch/template_bridge/workbench_projection.rs
  - zircon_editor/src/tests/host/template_runtime/mod.rs
  - zircon_editor/src/tests/host/template_runtime/scene_viewport_toolbar_runtime_projection.rs
  - zircon_editor/src/core/editor_event/runtime/editor_event_runtime_state.rs
  - zircon_editor/src/ui/host/module.rs
  - zircon_editor/src/ui/asset_editor/style/theme_authoring.rs
  - zircon_editor/src/ui/asset_editor/style/theme_authoring/action_projection.rs
  - zircon_editor/src/ui/asset_editor/style/theme_authoring/merge.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection/activity_rail.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection/dock_header.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection/status_bar.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection/tests.rs
  - zircon_editor/src/ui/retained_host/ui/apply_presentation/pane_conversion.rs
  - zircon_editor/src/ui/retained_host/ui/apply_presentation/scene_conversion.rs
plan_sources:
  - docs/plans/engine-code-structure-convention.md
  - docs/engine-architecture/large-file-ownership-m1.md
implementation_files:
  - docs/zircon_editor/structure/module-convention.md
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/editor_structure_audits/module_convention_boundary.py
  - .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_editor_structure.py
  - zircon_editor/src/tests/structure_convention/mod.rs
tests:
  - cargo check -p zircon_editor --lib --locked
  - cargo test -p zircon_editor --lib structure_convention --locked
  - cargo test -p zircon_editor --test integration_contracts --features integration-contracts --locked
  - python .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_editor_structure.py --json
  - cargo fmt --all --check
doc_type: structure-plan
status: active
---

# 10 · Editor UI 代码结构与模块规范收束计划

> 状态：active · 范围：`zircon_editor/src`（`core/` `scene/` `ui/`）
> 上游权威：[`docs/plans/engine-code-structure-convention.md`](../../engine-code-structure-convention.md)
> 本计划承接 editor UI 计划 01–09 之外的**结构治理横切**：把 retained-host / workbench / asset-editor 的 owner 边界、超大投影文件、重复测试树收敛到规范，并以 editor `module_convention_gate` 防回归。

## 1. 目标

editor 是结构债最重的 crate（探索报告评分最低）：`ui/` 臃肿、`retained_host` 子目录无限细化、超大投影 / 转换文件、`src/tests/**` 与 `src/ui/**` 重复测试树。本计划让 editor 达到与 runtime 同口径的结构友好度。

## 2. 现状缺口（按代码实查，带路径证据）

| # | 缺口 | 规范条目 | 证据路径 |
|---|------|---------|---------|
| S1 | `ui/` 顶层单文件与目录模块混排 | R1.1 / R1.2 | `ui/activity.rs`、`ui/control.rs`（单文件）vs `ui/asset_editor/`、`ui/animation_editor/`（目录） |
| S2 | 碎片化过度（细节文件名 + 36 行 mod.rs） | R1.3 / R3.1 | `ui/retained_host/activity_rail_pointer/`（25 个 100–300 行私有模块） |
| S3 | 超大投影 / 转换文件、职责过载 | R1.3 / R1.4 | `template_bridge/workbench_projection.rs`(3619)、`workbench_host_window/chrome_template_projection.rs`(1741)、`retained_host/ui/pane_data_conversion/`（mod 1559 + projection 1126 + tests 1288） |
| S4 | 重复测试树（无单一 source of truth） | R4.3 | `src/tests/host/retained_callback_dispatch/**`(23 文件) ↔ `src/ui/retained_host/callback_dispatch/**` |
| S5 | 层级不清 / 无主名 | R2.3 | `core/editor_event/runtime/editor_event_runtime_state.rs` 已完成 `_inner` 禁名硬切；`editor_runtime_play_mode_backend.rs` 仍需继续评估 runtime 目录职责 |
| S6 | 巨型测试文件 | R4.3 | `src/tests/.../workbench_projection.rs`(3619)、`src/tests/editor_event/runtime.rs`(3591)；`retained_host/ui/tests.rs` 已拆为 `retained_host/ui/tests/{mod,support,host_scene_projection,apply_presentation_shell,workbench_layout_frames,welcome_presentation,scene_document_pane,floating_windows,component_showcase}.rs` |
| S7 | **投影函数样板复制 + 超长函数**（`to_host_contract_pane` ~228 行、`animation_template_projection` ~326 行共用 view→映射→model_rc 骨架） | R1.3 / 规范 E5 | `retained_host/ui/pane_data_conversion/mod.rs:74,245,316,468,760,1082` |
| S8 | **死代码抑制聚集，疑半成品**（component_adapter 注册 5 处 `#[allow(dead_code)]`、workbench world slot 4 处） | 规范 E6 | `ui/template_runtime/component_adapter/registry.rs:14,50,83,116,198`、`ui/workbench/state/editor_world_slot.rs:10,23,35,47` |

> S7-S8 来自 [`engine-code-review-findings-2026-06.md`](../../../engine-code-review-findings-2026-06.md)（F15/F12）。

## 3. 目标结构（收敛后形态）

- `ui/` 顶层单文件归入对应 owner 目录；每个 owner 目录 `mod.rs` 是分组精选 façade。
- 超大投影按 authoring 工作流 + template-runtime owner 拆叶子，root 留薄 façade（见 §范式）。
- 测试单一 owner：`src/ui/**` 的行为测试就近 folder-backed，`src/tests/**` 仅保留跨模块集成测试，消除双写。
- `core/editor_event/runtime/` 去 `_inner`，按职责（dispatcher / runtime / play_mode_backend）显式分名。

### 范式：超大投影文件 owner 拆分
```
workbench_projection.rs(3619)  →  workbench_projection/
                                    mod.rs            # 薄 façade
                                    layout.rs         # 区域布局投影
                                    event_routing.rs  # 事件 → host 路由投影
                                    data_conversion.rs# DTO 转换
                                    template_bridge.rs# 模板桥接
```
对齐 `large-file-ownership-m1.md`：按 ownership 拆、不按等行数切；root 不为避免改调用方而留行为。

## 4. 里程碑（任务级执行蓝本）

切片期 `cargo check -p zircon_editor --lib --locked`；里程碑末进测试。

| 里程碑 | 任务 | 改动文件（代表） | 依赖 | 验收命令 / 测试函数 |
|---|---|---|---|---|
| **M1 `ui/` owner 边界** | T1 顶层单文件归入 owner 目录 | `ui/activity.rs`、`ui/control.rs` → owner 目录 | — | `cargo check -p zircon_editor --lib`；`editor_ui_10_ui_module_owner_boundaries` |
| | T2 碎片化收口 + façade 预算 | `ui/retained_host/activity_rail_pointer/` | T1 | `editor_ui_10_facade_reexport_within_budget` |
| **M2 拆超大文件** | T1 投影桥拆 owner | `template_bridge/workbench_projection.rs`(3619) → `workbench_projection/{layout,event_routing,data_conversion,template_bridge}.rs` | — | `editor_ui_10_no_oversized_production_files` |
| | T2 chrome 投影拆 owner | `workbench_host_window/chrome_template_projection.rs`(1741) | — | 同上 |
| | T3 pane 转换拆 owner | `retained_host/ui/pane_data_conversion/**` | — | 同上 |
| **M3 测试树去重** | T1 单一 owner | `src/tests/host/**` ↔ `src/ui/retained_host/**` 去双写；拆 > 800 行测试 | — | `editor_ui_10_no_duplicate_test_trees`、`editor_ui_10_no_oversized_test_files` |
| **M4 命名 + prelude** | T1 去 `_inner` 按职责分名 | `core/editor_event/runtime/editor_event_runtime_state.rs` 等 | — | `editor_ui_10_no_banned_name_modules` |
| | T2 子系统 prelude | `ui/`、`scene/`、`core/` 各 `prelude.rs` | T1 | `editor_ui_10_prelude_within_budget` |
| **M5 投影去样板 + 死代码清除** | T1 抽 `project_nodes<T>()` 泛型 helper + 拆超长投影函数 | `retained_host/ui/pane_data_conversion/mod.rs`、`apply_presentation.rs` | M2 | `editor_ui_10_no_oversized_production_functions`（F15） |
| | T2 `#[allow(dead_code)]` sweep（核查 component_adapter 5 处是否半成品） | `ui/template_runtime/component_adapter/registry.rs`、`ui/workbench/state/editor_world_slot.rs` | — | `editor_ui_10_no_dead_code_suppression_in_production`（F12） |

## 状态与产出记录

| 日期 | 切片 | 状态 | 产出 | 验证 | 后续 |
|---|---|---|---|---|---|
| 2026-06-25 | Editor UI 08/10 Workbench projection focused execution closeout | 完成（结构门、editor check、WSL focused tests 与截图复核通过；完整执行矩阵未声明通过） | 按 R1/R4.1/R6 继续处理 Workbench projection 验收缺口：viewport gizmo 轴文字使用 ZUI owner 内显式 `style.self.foreground_color`，不引入 `style_override_foreground_color` sentinel；`workbench_window_projection.rs` 拆出 authored text/value-display 语义，避免命令控件 label 被 value 覆盖；Inspector 动态选中行改用 `background`/`border` 覆盖静态 style，清空属性槽会清掉 stale surface style；TreeRow selected background 在 retained style selector 与 runtime collection row renderer 两层消费声明的 `background_color`；`chrome_template_projection.rs` 恢复 `DOCK_HEADER_ASSET`，并将 menu chrome/popup 投影抽到 `chrome_template_projection/menu_chrome.rs`。没有新增 root façade、compat shim、旧路径 re-export、production `#[allow(dead_code)]`、panic fallback 或临时诊断输出。 | `cargo fmt --all --verbose` 通过；WSL `cargo test -p zircon_runtime --lib render_extract_expands_collection_row_primitives ...` 1/1 通过；WSL `cargo test -p zircon_editor --lib componentized_workbench_window_template_bridge_updates_scene_tree_selection_state ...` 1/1 通过；WSL `cargo test -p zircon_editor --lib workbench_projection ...` 41/41 通过；`audit_editor_structure.py --json` 复跑为 `classified-and-clear`，migration debt、oversized production file、production dead-code suppression、duplicate test tree 与 UI owner boundary violation 均为 0；WSL `cargo check -p zircon_editor --lib --locked ...` 通过；截图证据在 `docs/tests/editor`，人工检查 2026-06-25 09:35 +08 的 900x620、640x420、1260x780 与 popup 布局合理。 | Windows focused lane 早前的 rustc 1.94.1 ICE/exit `-1` 仍作为本机风险记录；当前 WSL focused lane 已产出目标 test result。完整 editor lib-test / integration package matrix 继续作为 clean long-window 或 CI-parity 验证项。 |
| 2026-06-24 | Editor UI 08/10 final architecture validation and event closeout | 完成（结构门清零；editor check/no-run 通过；定向事件与截图通过） | 本轮没有新增根 façade、compat shim、dead-code suppression 或生产 panic；runtime popup focus 修复落在 `zircon_runtime::ui::surface::focus` owner，Workbench mutation 错误语境落在 retained-host template bridge owner，截图断言和参考布局证据留在 editor test owner 与 `docs/tests/editor`。`drawer_layout.rs` 继续按 compact side width limit 约束 900x620 左/右抽屉，防止中心 document/viewport 被压扁。 | `audit_editor_structure.py --json` 通过，`m1_gate_status = classified-and-clear`，`migration_debt_count = 0`、`oversized_production_file_count = 0`、`production_dead_code_suppression_count = 0`、`duplicate_test_tree_count = 0`、`ui_module_owner_boundary_violation_count = 0`。`CARGO_INCREMENTAL=0 cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --quiet` 通过；`cargo test -p zircon_editor --lib --locked --no-run --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --quiet` 通过；直接二进制 `widget_popup` 3/3、`declared_workbench_module_events_dispatch_preview_actions` 1/1、`capture_m3_gui_acceptance_visual_artifacts` 1/1 通过并刷新 `docs/tests/editor` 8 张截图。 | 完整执行版 editor lib-test / integration package matrix 尚未作为通过项记录；既有 warning 噪声后续单独治理。 |
| 2026-06-25 | Editor UI 08/10 ZUI/layout-data governance follow-up | 部分完成（结构治理、focused gates、Workbench projection execution 通过；`menu_popup_nodes_` 与完整执行矩阵未关闭） | 按 R1/R4.1/R6 处理本轮发现：builtin host bridge 过滤 componentized drawer shell/header frame，不再把组合壳层导出为业务 frame；`page_templates`、`presets`、`shell_regions` 从 `.v2.ui.toml` 硬切为 `.toml`，让 `.v2.ui.toml` 只承载 ZUI schema；floating workbench assets 改为 owner-prefixed `workbench_*` 文件并同步 locator；ZUI governance 更新到 `workbench/floating/`、DocumentTabs、tokenized axis/material tokens 与当前 L4 topology；`asset_editor/node_projection.rs` 补齐 `ViewTemplateNodeData.options`；shared runtime `depth_prepass.rs` 只修正编译阻塞参数名；`chrome_template_projection.rs` 将菜单 chrome/popup 投影抽到 `chrome_template_projection/menu_chrome.rs`，父 owner 从 1038 行降到 716 行。未新增 compat shim、旧路径 façade、生产 `#[allow(dead_code)]`、根模块行为或临时诊断输出。 | `cargo test -p zircon_editor --lib zui_asset_governance ...` 通过 66/66；直接 `editor_layout_contracts` 12/12；`cargo test -p zircon_editor --lib page_layout_templates ...` 4/4；`audit_editor_structure.py --json` 复跑为 `classified-and-clear`，migration debt、oversized production file、production dead-code suppression、duplicate test tree 与 UI owner boundary violation 均为 0；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --message-format short --color never` 通过；后续 WSL `cargo test -p zircon_editor --lib workbench_projection ...` 41/41 通过。截图证据仍在 `docs/tests/editor`，人工检查 900x620、640x420、1260x780 与 popup 布局未见空白中心或越界重叠。 | `menu_popup_nodes_` focused execution 早前一次 rustc ICE、一次 no-incremental isolated target 非零退出且无目标 test result，不能计为通过；完整 editor lib-test / integration package matrix 继续作为 clean long-window 验证项。 |
| 2026-06-23 | Editor UI 10 retained-host apply-presentation test helper visibility unblock | 完成（定向 native input 验证期间暴露并修复） | `ui/retained_host/ui/apply_presentation.rs` 的 test-only `to_host_contract_host_scene_data` re-export 改为 `pub(in crate::ui::retained_host::ui)`；`apply_presentation/scene_conversion.rs` 的同名 helper 同步扩大到同一测试域，恢复 `ui/tests/support.rs` 对 host-scene projection helper 的正常导入。生产路径仍通过 `apply_presentation(...)` 与 `to_host_contract_host_scene_data_with_runtime(...)`，未新增兼容模块或双路径。 | 首次 `cargo test -p zircon_editor --lib native_input_translation --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui01-native-input-0623 --message-format short --color never -- --test-threads=1 --nocapture` 复跑进入 editor lib-test 编译后暴露 E0364/E0603；修复后 `cargo fmt -p zircon_editor --check` 通过，scoped `git diff --check` 通过（仅既有 LF/CRLF warning），同一 focused native-input test 通过 7/7。 | 继续 01.M1.S2 platform-input 单实现；后续 Editor UI 10 focused test window 可复用该可见性修复。 |
| 2026-06-22 | Editor UI 10 M2 retained-host apply-presentation conversion owner split | 完成（最终结构门清零，编辑器库检查通过） | `ui/retained_host/ui/apply_presentation.rs` 保留 apply 流程、layout frame resolution、welcome/floating/basic DTO helper；新增 `apply_presentation/pane_conversion.rs`，承接 host pane payload gate、pane-family DTO selection 与 body surface frame refresh；新增 `apply_presentation/scene_conversion.rs`，承接 host shell/layout/project overview、menu/page/status/resize/drag/dock/floating layer 与 native floating-surface conversion；显式 `#[path]` 保持 path-mounted module 的子 owner 归档在 `ui/apply_presentation/` tree 下。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`m1_gate_status = classified-and-clear`，`oversized_production_file_count = 0`，`migration_debt_count = 0`，`production_dead_code_suppression_count = 0`；行数：`apply_presentation.rs` 724、`apply_presentation/pane_conversion.rs` 227、`apply_presentation/scene_conversion.rs` 401；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-architecture-0622 --message-format short --color never` 通过，仅有既有 warning。 | Editor UI 10 的 M1 结构门当前清零；后续可进入 focused test matrix / workspace validation。 |
| 2026-06-22 | Editor UI 10 M2 workbench chrome template projection owner split | 完成（结构拆分、格式与脚本审计通过；随后编辑器库检查通过） | `ui/layouts/windows/workbench_host_window/chrome_template_projection.rs` 保留 source-contract 可见入口、菜单 popup 投影、page chrome 与通用 template/frame helper；新增 `chrome_template_projection/{activity_rail,dock_header,status_bar,tests}.rs`，分别承接 activity rail projection、dock header projection/status、status-bar projection 和 module-local regression tests；保留 root wrappers 以满足现有 source-contract guard 对入口名的扫描。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 当时降至 `oversized_production_file_count = 1`，最终 apply-presentation split 后清零；行数：`chrome_template_projection.rs` 878、`activity_rail.rs` 169、`dock_header.rs` 139、`status_bar.rs` 144、`tests.rs` 371；后续 editor package check 通过。 | `chrome_template_projection.rs` 退出 oversized 清单；剩余 apply-presentation 已在下一切片清零。 |
| 2026-06-22 | Editor UI 10 M2 asset-editor theme authoring owner split | 完成（结构拆分、格式与脚本审计通过；随后编辑器库检查通过） | `ui/asset_editor/style/theme_authoring.rs` 保留 theme authoring DTO、公开 promote/adopt/refactor API 与 local override cleanup；新增 `theme_authoring/action_projection.rs`，承接 active cascade、imported-theme compare/action projection 和 clone-detection helper；新增 `theme_authoring/merge.rs`，承接 imported theme detach/clone merge、stylesheet/token rewrite、identifier normalization 与 theme draft naming helper。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 当时降至 `oversized_production_file_count = 2`，最终 apply-presentation split 后清零；行数：`theme_authoring.rs` 770、`theme_authoring/action_projection.rs` 315、`theme_authoring/merge.rs` 334；后续 editor package check 通过。 | `theme_authoring.rs` 退出 oversized 清单；继续 chrome/apply-presentation owner split。 |
| 2026-06-22 | Editor UI 10 M2 asset-editor binding inspector payload owner split | 完成（结构拆分、格式与脚本审计通过；Cargo package check 未重启） | `ui/asset_editor/binding/binding_inspector.rs` 保留 binding inspector DTO、公开 selection/editing 入口、event/action/route/schema projection helper；新增 `binding_inspector/payload_editing.rs`，承接 binding payload suggestion、schema payload entries、route/action suggestions、payload path resolution、payload action-state application、identifier/path normalization 和 binding item formatting helper；现有 `payload_suggestions.rs` / `schema_projection.rs` 仍作为 sibling owner 使用。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 3`，`migration_debt_count = 3`；行数：`binding_inspector.rs` 713、`binding_inspector/payload_editing.rs` 698；当前本机仍有 plugin/runtime Cargo lanes 活跃，本切片未重启 editor package check，未声明编译通过。 | `binding_inspector.rs` 退出 oversized 清单；继续剩余 3 个 production oversized owner。 |
| 2026-06-22 | Editor UI 10 M2 asset-editor session lifecycle v2 projection owner split | 完成（结构拆分、格式与脚本审计通过；Cargo package check 未通过确认） | `ui/asset_editor/session/lifecycle.rs` 保留 `UiAssetEditorSession` lifecycle/load/save/recompile/source-buffer 方法；新增 `lifecycle/v2_projection.rs`，承接 `parse_ui_asset_source`、v2→legacy projection、legacy→v2 serialization projection、compiled resource report、structured diagnostics 与 palette-index reconciliation helper；session public behavior 和 schema serialization 调用点保持原名导入。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 4`，`migration_debt_count = 4`；行数：`lifecycle.rs` 819、`lifecycle/v2_projection.rs` 393；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-asset-lifecycle-0622 --message-format short --color never` 304 秒超时且无诊断输出，匹配该 target 的残留进程已检查/清理，未声明编译通过。 | `session/lifecycle.rs` 退出 oversized 清单；继续剩余 4 个 production oversized owner。 |
| 2026-06-22 | Editor UI 10 M2 asset-editor preview mock entries owner split | 完成（结构拆分、格式与脚本审计通过；Cargo package check 未通过确认） | `ui/asset_editor/preview/preview_mock.rs` 保留 preview mock state/field DTO、公开编辑入口、override 应用入口和现有 expression/suggestion/value-resolution wiring；新增 `preview_mock/entries.rs`，承接 mock subject/property/nested entry selection、expression resolution、literal/inline formatting、override value mutation、nested path normalization 与 collection/object entry collection helper；公开函数名和调用路径保持不变，未添加兼容 shim。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 5`，`migration_debt_count = 5`；行数：`preview_mock.rs` 583、`preview_mock/entries.rs` 615；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-preview-mock-0622 --message-format short --color never` 304 秒超时且无诊断输出，匹配该 target 的残留进程已检查/清理，未声明编译通过。 | `preview_mock.rs` 退出 oversized 清单；继续剩余 5 个 production oversized owner。 |
| 2026-06-22 | Editor UI 10 M2 retained-host ui test owner split | 完成（结构拆分、格式与脚本审计通过；Cargo 定向执行未通过确认） | 删除 `ui/retained_host/ui/tests.rs` 旧巨型 root 测试文件，新增 `ui/tests/mod.rs` 作为 11 行本地测试入口；共享 retained-host fixture / host-scene helper / apply-presentation wrapper 迁入 `ui/tests/support.rs`；host-scene pane conversion、root apply-presentation shell、workbench layout frames、welcome mount projection、scene document pane、floating window projection 回归分别落到 `host_scene_projection.rs`、`apply_presentation_shell.rs`、`workbench_layout_frames.rs`、`welcome_presentation.rs`、`scene_document_pane.rs`、`floating_windows.rs`；host-scene projection 断言段继续下沉到 `host_scene_projection/assertions.rs`；既有 component showcase owner 保持在 `component_showcase.rs` 并改由新 support owner 提供 helper。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 6`，`migration_debt_count = 6`；旧 `ui/tests.rs` `Test-Path` 为 false；新 test owner 行数：`mod.rs` 11、`support.rs` 401、`apply_presentation_shell.rs` 99、`workbench_layout_frames.rs` 642、`welcome_presentation.rs` 108、`scene_document_pane.rs` 54、`floating_windows.rs` 381、`host_scene_projection.rs` 982、`host_scene_projection/assertions.rs` 353、既有 `component_showcase.rs` 1167；`cargo test -p zircon_editor --lib ui::retained_host::ui::tests::scene_document_pane::scene_document_pane_uses_viewport_dimensions_and_enables_toolbar --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-retained-ui-tests-0622 --message-format short --color never -- --exact --test-threads=1` 两次分别约 305 秒 / 304 秒超时且无诊断输出；匹配本次 target 的残留进程已检查/清理，未声明执行通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换。 | `retained_host/ui/tests.rs` 退出 oversized 清单；继续剩余 6 个 production oversized owner。 |
| 2026-06-22 | Editor UI 10 M2 retained-host app test owner split | 完成（结构拆分、格式与脚本审计通过；Cargo 定向执行未通过确认） | 删除 `ui/retained_host/app/tests.rs` 旧巨型 root 测试文件，新增 `app/tests/mod.rs` 作为 13 行本地测试入口，并将 shared retained-host harness / env lock / workbench control lookup 迁入 `app/tests/support.rs`；root command palette、child-window focus、menu pointer、projection geometry、viewport/template bridge、root pointer fallback、child-window tab/header 回归分别落到 `command_palette.rs`、`child_window_focus.rs`、`menu_pointer.rs`、`projection_geometry.rs`、`viewport_template_bridge.rs`、`root_pointer_fallbacks.rs`、`child_window_tabs.rs`；既有 `close_prompt`、`componentized_workbench`、`drag_sources`、`floating_window_projection`、`runtime_diagnostics_drawer` 子测试 owner 保持原职责。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 7`，`migration_debt_count = 7`；旧 `app/tests.rs` `Test-Path` 为 false；新 root test owner 行数：`mod.rs` 13、`command_palette.rs` 92、`child_window_tabs.rs` 94、`menu_pointer.rs` 115、`child_window_focus.rs` 178、`viewport_template_bridge.rs` 187、`support.rs` 308、`root_pointer_fallbacks.rs` 350、`projection_geometry.rs` 356；首次 focused Cargo run 暴露测试模块可见性缺口，已修正 `tests/mod.rs`/`support.rs` wiring；随后 `cargo test -p zircon_editor --lib native_unhandled_ctrl_shift_p_opens_workbench_command_palette --locked --target-dir E:\cargo-targets\zircon-editor-retained-app-tests-0622 --message-format short --color never -- --exact --test-threads=1` 完成 lib-test 编译但 exact filter 匹配 0 tests（2069 filtered out）；非 exact 复跑 184 秒超时且无诊断输出，残留 cargo/rustc 进程已停止，未声明执行通过。 | `app/tests.rs` 退出 oversized 清单；继续剩余 7 个 oversized owner。 |
| 2026-06-22 | Editor UI 10 M2 export wizard test owner split | 完成（结构拆分、格式与脚本审计通过；Cargo 定向测试未通过确认） | 删除 `ui/host/editor_manager_plugins_export/export_build/wizard/tests.rs` 旧巨型测试文件，新增 `wizard/tests/{mod,support,pipeline_plan,pipeline_execution,job,panel_session,view_model}.rs`；共享 runner/helper 迁入 `support.rs`，pipeline plan/execution、job、panel session、view-model/template-state 测试按 owner 分组；同步修正阶段取消分类，区分运行中取消与阶段结束后取消，并让 export wizard v2 面板注册同时加载 `panel.v2.ui.toml` 和 `editor_base.v2.ui.toml` 导入源。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 8`，`migration_debt_count = 8`；旧 `wizard/tests.rs` `Test-Path` 为 false；新测试 owner 行数：`mod.rs` 6、`support.rs` 156、`pipeline_execution.rs` 165、`view_model.rs` 233、`panel_session.rs` 242、`pipeline_plan.rs` 276、`job.rs` 281；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换；`cargo test -p zircon_editor --lib export_wizard --locked --target-dir E:\cargo-targets\zircon-editor-export-wizard-0622 --message-format short --color never -- --test-threads=1` 在修复后曾被 runtime 侧 `GpuMeshResource::indirect_order_signature` 私有字段访问错误阻断于 `zircon_runtime` 编译阶段，文档同步后的最新复跑 304 秒超时且无诊断输出，残留 cargo/rustc 进程已停止，未声明通过。 | `wizard/tests.rs` 退出 oversized 清单；继续剩余 8 个 oversized owner，并在 runtime GPU mesh blocker / Cargo 超时窗口清除后复跑 export_wizard 定向测试。 |
| 2026-06-22 | Editor UI 10 M2 host `editor_manager_asset_editor` façade owner split | 完成（代码与文档记录；结构审计通过，后续同轮 package check 通过） | 将 `ui/host/editor_manager_asset_editor.rs` 从 1023 行单文件 façade 收束为 5 行 wiring 文件，新增 `editor_manager_asset_editor/{binding,designer,document,presentation,style}.rs`，按 binding authoring、designer/detail authoring、document/tree/palette/source authoring、theme/style authoring、presentation/open/preview/save façade 分 owner；117 个 `EditorManager` 转发方法保持原有公开名称与调用路径，未保留重复实现或兼容模块。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 10`，`migration_debt_count = 10`，`editor_manager_asset_editor.rs` 退出 oversized 清单；子文件行数分别为 132/209/223/155/209；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换；同轮后续 `cargo check -p zircon_editor --lib --locked --message-format short --color never` 已通过，仅有既有 warning。 | 已进入下一项 oversized owner；继续按 M2 拆剩余 production hotspots。 |
| 2026-06-22 | Editor UI 10 M2 animation editor session owner split | 完成（代码、测试规则与文档记录；package check 与定向测试通过） | 将 `ui/animation_editor/session.rs` 从单文件 session owner 收束为 48 行根 wiring/type owner，新增 `session/{lifecycle,presentation,sequence,graph,state_machine,parameters,support}.rs`；加载/保存、pane presentation、sequence timeline/track/key/playback、graph node/parameter/connection、state-machine state/transition/condition、parameter literal parsing 与共享文档/timing/path helper 分别落到具名 child owner，未保留兼容模块。 | `cargo fmt -p zircon_editor --check` 通过；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，仅有既有 warning；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 9`，`migration_debt_count = 9`；行数采样：root 48，graph 238，lifecycle 72，parameters 88，presentation 121，sequence 278，state_machine 167，support 114，tests 617；`cargo test -p zircon_editor --lib animation_editor --locked --target-dir E:\cargo-targets\zircon-editor-ui-animation-session-0622 --message-format short --color never -- --test-threads=1` 通过（24 passed，2045 filtered out）；`cargo test -p zircon_editor --lib structure_convention --locked --target-dir E:\cargo-targets\zircon-editor-structure-0622 --message-format short --color never -- --test-threads=1` 通过（3 passed，2066 filtered out）；同步修正 `host_cutover` 结构测试，让 thin asset wrapper 断言读取 folder-backed child modules。 | `session.rs` 退出 oversized 清单；继续拆剩余 9 个 oversized production owner。 |
| 2026-06-22 | Editor UI 10 M2 oversized template-runtime showcase events owner split | 完成（代码与文档记录；package check 通过） | 将 `ui/template_runtime/showcase_demo_state.rs` 中事件输入、错误、binding action 解析和 `component_event_for_action(...)` 迁入 `showcase_demo_state/events.rs`；根文件从 1004 行降到 486 行，保留 demo state、host-model 投影和状态属性同步职责；新事件 owner 528 行，未保留重复实现。 | `cargo fmt -p zircon_editor --check` 通过；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，仅有既有 warning；`audit_editor_structure.py --json` 通过，`oversized_production_file_count = 11`，`migration_debt_count = 11`；旧根文件事件解析符号扫描清零；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换。 | `showcase_demo_state.rs` 退出 oversized 清单；继续拆剩余 11 个 oversized production owner。 |
| 2026-06-22 | Editor UI 10 M3.T1 viewport-toolbar duplicate test-tree owner rename | 完成（代码与文档记录；focused test 通过） | 删除 `src/tests/host/template_runtime/viewport_toolbar.rs` 旧宽泛测试文件名，新增 `scene_viewport_toolbar_runtime_projection.rs` 并更新 `template_runtime/mod.rs`；crate-level 测试继续表达内建 viewport toolbar 模板运行时投影，`ui/retained_host/host_contract/surface_hit_test/viewport_toolbar_tests.rs` 保持私有 hit-test owner，未保留双路径。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`duplicate_test_tree_count = 0`，`migration_debt_count = 12`；旧测试文件 `Test-Path` 为 false，新测试文件为 true；`cargo test -p zircon_editor --lib scene_viewport_toolbar_runtime_projection --locked --message-format short --color never -- --test-threads=1` 通过（2 passed，2067 filtered out）；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换。 | M3.T1 重复测试树清零；剩余债务为 12 个 oversized production owner，继续 M2。 |
| 2026-06-22 | Editor UI 10 M5.T2 production `dead_code` suppression final sweep | 完成（代码与文档记录；package check 与 focused guard 通过） | 补齐 M5.T2 剩余生产 `#[allow(dead_code)]` 清理：上一轮移除 `asset_editor/palette/entry.rs`、`presenter/softbuffer.rs`、`viewport_state.rs`、`world_space_ui.rs`、`template/catalog.rs`、`component_drawer.rs` 的 6 个抑制点；本轮删除未使用 `DOCK_HEADER_ASSET`，并移除 `host_data.rs`、`pane_presentation.rs`、`showcase_demo_state.rs` 的剩余审计命中；未引入兼容路径。 | `cargo fmt -p zircon_editor --check` 通过；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，仅有既有 warning；`audit_editor_structure.py --json` 通过，`production_dead_code_suppression_count = 0`，`migration_debt_count = 13`；4 个剩余命中文件的 `#[allow(dead_code)]`/`DOCK_HEADER_ASSET` 扫描清零；`cargo test -p zircon_editor --lib structure_convention --locked --message-format short --color never -- --test-threads=1` 通过（3 passed，2066 filtered out）；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换。 | M5.T2 生产 dead-code 抑制清零；继续 M3/M2：处理 `src/tests <-> ui::viewport_toolbar` 重复测试树和 12 个 oversized owner。 |
| 2026-06-22 | Editor UI 10 M4.T1 Workbench renderer banned-name hard cutover | 完成（代码与文档记录；focused audit/package check 通过） | 删除 `ui/retained_host/host_contract/paint_workbench_impl.rs` 与 `paint_workbench_impl/` 目录，新增 `paint_workbench_renderer.rs` 与 `paint_workbench_renderer/`，并更新 host-contract module wiring、Workbench paint façade 调用点、owner 文档和活跃计划记录；未保留兼容模块。 | `cargo fmt -p zircon_editor --check` 通过；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，仅有既有 warning；`audit_editor_structure.py --json` 通过，`banned_name_module_count = 0`，`migration_debt_count = 23`；源码旧 `paint_workbench_impl` 引用扫描清零；旧源文件/目录 `Test-Path` 为 false，新 renderer 文件/目录为 true；owner 文档已改向 renderer，计划/镜像/会话仅保留旧名作为删除完成记录；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换。 | M4.T1 禁名模块清零；继续 M3/M5：处理重复测试树、生产 `dead_code` 抑制和 oversized owner。 |
| 2026-06-22 | Editor UI 10 M4.T1 editor event runtime state banned-name hard cutover | 完成（代码与文档记录；focused audit/package check 通过） | 删除 `core/editor_event/runtime/editor_event_runtime_inner.rs`，新增 `editor_event_runtime_state.rs` 并将 `EditorEventRuntimeInner` 硬切为 `EditorEventRuntimeState`；更新 runtime wiring、host execution/dispatch/reflection 调用点、边界测试与架构文档；未保留兼容模块。 | `cargo fmt -p zircon_editor --check` 通过；`cargo check -p zircon_editor --lib --locked --message-format short --color never` 通过，仅有既有 warning；`audit_editor_structure.py --json` 通过，`banned_name_module_count = 2`，`migration_debt_count = 25`；源码旧 runtime-inner 引用扫描清零；旧源文件 `Test-Path` 为 false、新 state 文件为 true；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换；focused `structure_convention` guard 本次 300s 超时无诊断，已停止本次 editor Cargo/Rustc，未声明通过。 | 后续已完成 Workbench renderer 禁名硬切；继续清理重复测试树、生产 `dead_code` 抑制和 oversized owner。 |
| 2026-06-22 | Editor UI 10 M4.T1 local helper banned-name hard cutover | 完成（代码与文档记录；focused audit 通过） | 删除 `ui/workbench/project/runtime_asset_helpers.rs` 与 `ui/retained_host/app/host_lifecycle/invalidation_bridge/mark_helpers.rs`，新增 `runtime_asset_resolution.rs` 与 `dirty_marking.rs` 并更新调用方与相关文档；未保留兼容模块。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`banned_name_module_count = 3`，`migration_debt_count = 26`，`ui_module_owner_boundary_violation_count = 0`；旧 helper 源文件不存在，源码旧 helper 引用扫描清零；相关 owner/asset 文档旧路径扫描清零；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 转换；`cargo test -p zircon_editor --lib structure_convention --locked --message-format short --color never -- --test-threads=1` 本次 240s 超时无诊断，已停止本次 editor Cargo/Rustc，未声明通过。 | 后续已完成 runtime state 禁名硬切；继续 M4.T1 剩余 paint-workbench owner 命名债。 |
| 2026-06-22 | Editor UI 10 M4.T1 viewport handle `_impl` banned-name hard cutover | 完成（代码与文档记录；focused audit 通过） | 删除 `scene/viewport/handles/{move,rotate,scale}_handle_tool_impl.rs`，新增 `*_handle_tool_behavior.rs` 并更新 `handles/mod.rs`；同步更新 runtime/editor boundary 文档中的 handle owner 路径，未保留兼容模块。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`banned_name_module_count = 5`，`migration_debt_count = 28`；旧 `_tool_impl` 路径扫描清零。 | 后续已完成 helper 禁名硬切；继续 M4.T1 剩余 `_inner` 与 paint-workbench owner 命名债。 |
| 2026-06-22 | Editor UI 10 M1.T1 `ui/` top-level owner hard cutover | 完成（代码与文档记录；focused audit 通过） | 删除 `ui/activity.rs`、`ui/control.rs`、`ui/reflection.rs` 顶层单文件 owner；新增 `ui/activity/{mod,slot,view,window}.rs`、`ui/control/{mod,error,service}.rs`、`ui/reflection/{mod,activity,adapter,builder,model,state_flags,value_type}.rs`，root 仅保留结构声明和精选导出；调用方继续通过 owner 模块路径访问，未添加兼容 shim；同步更新 docs 中旧 editor owner 路径。 | `cargo fmt -p zircon_editor --check` 通过；`audit_editor_structure.py --json` 通过，`ui_module_owner_boundary_violation_count = 0`，`migration_debt_count = 31`；旧路径 `Test-Path` 全为 false；旧 editor 路径扫描清零（剩余命中仅为 `zircon_runtime_interface/src/ui/event_ui/{control,reflection}.rs`）；post-cutover `cargo test -p zircon_editor --lib structure_convention --locked --message-format short --color never -- --test-threads=1` 重新尝试时被 runtime 侧 `RuntimePluginDescriptor` 私有字段访问错误阻断，尚未进入 editor 编译。 | 继续 M1/M2：处理 banned name modules、重复测试树、生产 `dead_code` 抑制和剩余 oversized owner；待 runtime/plugin 工作流修复后复跑 structure guard 与 package check。 |
| 2026-06-22 | Editor UI 10 M5.T2 component-adapter `dead_code` suppression sweep | 完成（代码与文档记录；package-level Cargo check 未重启） | 移除 `ui/template_runtime/component_adapter/registry.rs` 中 5 处陈旧生产 `#[allow(dead_code)]`；`data_sources()` 与 registry helper 已确认为 runtime access 和 component adapter 测试真实使用；新增 `docs/zircon_editor/ui/template_runtime/component_adapter/index.md` 作为模块说明入口。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/template_runtime/component_adapter/registry.rs` 通过；registry `#[allow(dead_code)]` 扫描清零；`editor_event_runtime_access.rs` 与 `src/tests/ui/component_adapter.rs` 使用点扫描通过；S8 剩余扫描仅命中 `ui/workbench/state/editor_world_slot.rs` 4 处抑制。 | 继续 M5.T2：单独审计 `editor_world_slot.rs` 的 `loaded/snapshot/with_world/with_world_mut` 抑制，决定生产接线、`cfg(test)` 收口或删除。 |
| 2026-06-22 | Editor UI 10 M5.T2 workbench world-slot `dead_code` suppression sweep | 完成（代码与文档记录；package-level Cargo check 未重启） | 移除 `ui/workbench/state/editor_world_slot.rs` 中 4 处陈旧生产 `#[allow(dead_code)]`；`loaded(world)` 保持生产构造入口；`snapshot/with_world/with_world_mut` 收口为 `cfg(test)` 测试便捷入口；新增 `docs/zircon_editor/ui/workbench/state/editor_world_slot.md` 说明生产 `try_*` 契约。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/workbench/state/editor_world_slot.rs` 通过；`registry.rs` + `editor_world_slot.rs` `#[allow(dead_code)]` 扫描清零；生产 `ui/**` 中 `world.snapshot/with_world/with_world_mut` 扫描零命中；生产 `try_*` 与 `EditorWorldSlot::loaded` 使用点扫描通过；测试树保留 57 个便捷方法调用点并由 `cfg(test)` 编译。 | S8 两个证据路径已清零；后续在 runtime lanes 空闲时补跑 `editor_ui_10_no_dead_code_suppression_in_production`/package check，并继续处理 S7/S3 超大投影与函数样板。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` template-node projection helper | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/template_node_projection.rs`，提供 `project_nodes<T, F>` 与 `project_node_vec<T, F>`；替换 hierarchy、inspector、console、generated bottom fallback、assets activity、animation editor、asset browser、project overview 的重复 legacy template-node 映射；新增 `docs/zircon_editor/ui/retained_host/pane_data_conversion.md`。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `template_node_projection.rs` 通过；`map_model_rc(&data.nodes, ...)` template-node 旧样板扫描清零；helper 定义与 8 个调用点扫描通过。 | 继续 M5.T1/S7：拆 `inspector_template_projection(...)` 与 `animation_template_projection(...)` 的 header/body/fields 主体，逐步缩短 `mod.rs`。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` inspector fields owner split | 完成（代码与文档记录；package-level Cargo check 600s 超时无诊断） | 新增 `ui/retained_host/ui/pane_data_conversion/inspector_fields.rs`，迁出 inspector fallback 字段模型、字段 frame/height 计算、plugin component header/diagnostic/property 节点、fallback message 与文本/数字/按钮节点 helper；`mod.rs` 只保留 `inspector_template_projection(...)` runtime orchestration 与 `InspectorPaneData` 装配。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs`、`inspector_fields.rs`、`template_node_projection.rs` 通过；父 `mod.rs` 中 inspector 字段 helper 残留扫描清零，仅保留 `mod/use` 与 2 个调用点；行数采样：`mod.rs` 910 行、`inspector_fields.rs` 547 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-pane-data-s7-0622 --message-format short --color never` 600s 超时无 actionable diagnostics，超时后未发现 editor Cargo/Rustc 残留，runtime/plugin 进程保留。 | 继续 M5.T1/S7：拆 `animation_template_projection(...)` 的 sequence/graph body 装配，或继续把 `inspector_template_projection(...)` payload extraction 收成更薄 helper。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` animation projection owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/animation_projection.rs`，迁出 `animation_template_projection(...)`、sequence/graph payload 分支与 `SharedString` list 转换；`mod.rs` 仅保留 animation pane façade/native fallback，并通过子模块调用 template projection。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `animation_projection.rs` 通过；父 `mod.rs` 中 `AnimationSequenceV1`/`AnimationGraphV1`/`shared_string_list` 残留扫描清零，仅保留 animation façade 的 2 个调用点与 native fallback 字段；行数采样：`mod.rs` 851 行、`animation_projection.rs` 55 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 console/hierarchy projection orchestration 或其它 pane payload extraction，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` hierarchy projection owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/hierarchy_projection.rs`，迁出 `hierarchy_template_projection(...)`、`HierarchyV1` scene-node DTO 转换，以及 `HierarchyListPanel`/`SelectRoot` template state 标记；`mod.rs` 仅保留 hierarchy pane façade/native fallback。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `hierarchy_projection.rs` 通过；父 `mod.rs` 中 `HierarchyV1`/`apply_hierarchy_template_state`/`hierarchy_template_projection` 实现残留扫描清零，仅保留 2 个 façade 调用点；私有 payload 子类型 import 扫描清零；行数采样：`mod.rs` 777 行、`hierarchy_projection.rs` 78 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 console projection orchestration 或其它 pane payload extraction，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` console projection owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/console_projection.rs`，迁出 `console_template_projection(...)`、`ConsoleV1` payload status fallback、runtime surface layout 与 host-model node projection；`mod.rs` 仅保留 console pane façade/native fallback。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `console_projection.rs` 通过；父 `mod.rs` 中 `ConsoleV1`/`payload_status_text`/`console_template_projection` 实现残留扫描清零；私有 `ConsolePanePayload` import 扫描清零；行数采样：`mod.rs` 796 行、`console_projection.rs` 56 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 inspector/generated-bottom/template-node runtime orchestration 或其它 pane payload extraction，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` inspector projection owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/inspector_projection.rs`，迁出 `inspector_template_projection(...)`、`InspectorV1` payload 字段读取、plugin component payload 到 view-data 的转换、runtime surface layout 与 host-model node projection；`mod.rs` 仅保留 inspector pane façade/native fallback。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `inspector_projection.rs` 通过；父 `mod.rs` 中 `InspectorV1`/payload name-parent-translation-delete 字段读取/`inspector_template_projection` 实现残留扫描清零，剩余 `InspectorPluginComponentViewData` 命中仅在测试 fixture；私有 inspector payload 子类型 import 扫描清零；行数采样：`mod.rs` 678 行、`inspector_projection.rs` 126 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 generated-bottom/template-node runtime orchestration 或其它 pane payload extraction，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` generated-bottom projection owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/generated_bottom_projection.rs`，迁出 `to_host_contract_generated_bottom_pane_from_host_pane(...)`、template-runtime node projection 调用、native generated-bottom fallback 和 status 装配；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `generated_bottom_projection.rs` 通过；父 `mod.rs` 中 `GeneratedBottomPaneData`/`native_body.generated_bottom`/函数实现残留扫描清零；行数采样：`mod.rs` 658 行、`generated_bottom_projection.rs` 30 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 component showcase 或共享 template-node runtime helper，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` component-showcase projection owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/component_showcase_projection.rs`，迁出 `to_host_contract_component_showcase_pane_from_host_pane*`、`UiComponentShowcaseV1` payload-kind gating、builtin/runtime projection 分支、surface layout 与 host-model node projection；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `component_showcase_projection.rs` 通过；父 `mod.rs` 中 `UiComponentShowcaseV1`/`component_showcase_template_projection`/component showcase entry 实现残留扫描清零，剩余 `build_host_model_with_surface` 命中仅为共享 template-node helper；行数采样：`mod.rs` 608 行、`component_showcase_projection.rs` 62 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中共享 template-node runtime helper 或 remaining fallback façade，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` template-runtime projection helper owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/template_runtime_projection.rs`，迁出 `project_pane_template_nodes*`、`pane_template_runtime(...)`、`builtin_host_runtime()`、surface layout、host-model node projection 与 content-size fallback 规则；`mod.rs` 仅保留私有导入供子模块继续通过 `super::...` 调用。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `template_runtime_projection.rs` 通过；父 `mod.rs` 中 `OnceLock`/`UiSize`/`RetainedUiHostNodeProjection`/template-runtime helper 函数/`build_host_model_with_surface` 残留扫描清零；子模块调用点仍命中 `super::project_pane_template_nodes*`/`pane_template_runtime`/`builtin_host_runtime`；行数采样：`mod.rs` 529 行、`template_runtime_projection.rs` 90 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 scene-node/legacy fallback façade 或 project-overview/assets/console native conversion owner，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` hierarchy native façade owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 将 `to_host_contract_hierarchy_pane_from_host_pane*`、native `HierarchyPaneViewData` fallback、legacy `SceneNodeData` DTO 转换补迁到 `ui/retained_host/ui/pane_data_conversion/hierarchy_projection.rs`；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `hierarchy_projection.rs` 通过；父 `mod.rs` 中 `to_host_contract_scene_node(s)`/`to_host_contract_hierarchy_pane`/`hierarchy_template_projection`/`SceneNodeData`/`HierarchyPaneData` 残留扫描清零；行数采样：`mod.rs` 489 行、`hierarchy_projection.rs` 129 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 project-overview/assets/console native conversion 或泛型 model projection helper，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` console native façade owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 将 `to_host_contract_console_pane_from_host_pane*`、native `ConsolePaneViewData` fallback、legacy `ConsoleTextPanel`→`ConsoleBodySection` id 修正补迁到 `ui/retained_host/ui/pane_data_conversion/console_projection.rs`；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `console_projection.rs` 通过；父 `mod.rs` 中 `ConsolePaneData`/`ConsolePaneViewData`/`ConsoleTextPanel`/`ConsoleBodySection`/console native façade 实现残留扫描清零；行数采样：`mod.rs` 456 行、`console_projection.rs` 94 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 project-overview/assets/animation native conversion 或泛型 model projection helper，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` inspector pane test owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/inspector_pane_tests.rs`，迁出 `mod.rs` 内联 inspector pane projection 行为测试、fixture 与 node lookup helper；父 `mod.rs` 只保留 `#[cfg(test)] mod inspector_pane_tests;` 挂载点。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `inspector_pane_tests.rs` 通过；父 `mod.rs` 中 inspector 测试函数、fixture、plugin component fixture 类型与 `blank_viewport_chrome` 残留扫描清零；行数采样：`mod.rs` 186 行、`inspector_pane_tests.rs` 260 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 project-overview/assets/animation native conversion 或泛型 model projection helper，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` animation native façade owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 将 `to_host_contract_animation_editor_pane_from_host_pane*` 与 native `AnimationEditorPaneViewData` fallback 补迁到 `ui/retained_host/ui/pane_data_conversion/animation_projection.rs`；子模块同时拥有已迁出的 `AnimationSequenceV1`/`AnimationGraphV1` template projection，`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `animation_projection.rs` 通过；父 `mod.rs` 中 `AnimationEditorPaneViewData`/`AnimationEditorPaneData`/animation native façade 实现/`native_body.animation` 残留扫描清零；行数采样：`mod.rs` 149 行、`animation_projection.rs` 103 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 inspector façade 或 project-overview/assets native conversion，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` inspector native façade owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 将 `to_host_contract_inspector_pane_from_host_pane*` 与 native `InspectorPaneViewData` fallback 补迁到 `ui/retained_host/ui/pane_data_conversion/inspector_projection.rs`；子模块同时拥有已迁出的 `InspectorV1` template projection 与 inspector field 节点追加，`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs`、`inspector_projection.rs` 与 `inspector_pane_tests.rs` 通过；父 `mod.rs` 中 `InspectorPaneViewData`/`InspectorPaneData`/inspector native façade 实现/`native_body.inspector`/`InspectorVisualFields`/`inspector_field_nodes` 残留扫描清零；行数采样：`mod.rs` 109 行、`inspector_projection.rs` 168 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 root 中 project-overview/assets native conversion 或共享 model projection helper，保持 `mod.rs` 只做 façade。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` native template-node panes owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/native_template_node_panes.rs`，迁出 assets activity、asset browser、project overview 三个 native template-node pane DTO 转换；父 `mod.rs` 通过 `pub(super) use` 保持既有入口。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs` 与 `native_template_node_panes.rs` 通过；父 `mod.rs` 中三类 pane view/data DTO、三个 `to_host_contract_*_pane(...)` 实现、`to_host_contract_template_node_owned` 与 `project_nodes` 残留扫描清零；行数采样：`mod.rs` 86 行、`native_template_node_panes.rs` 31 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估 `map_model_rc` 是否继续作为子模块共享 helper 留在 façade，或迁入专门 model projection owner。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` model projection helper owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/model_projection.rs`，迁出共享 `map_model_rc(...)`；build export、`module_plugins.rs`、`performance_timeline.rs` 改为直接引用该 owner，父 `mod.rs` 不再持有转换 helper。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs`、`model_projection.rs`、build export owner、`module_plugins.rs`、`performance_timeline.rs` 通过；旧 `super::map_model_rc` 扫描清零；父 `mod.rs` 中 `model_rc`/`ModelRc`/`map_model_rc` 残留扫描清零；行数采样：`mod.rs` 70 行、`model_projection.rs` 16 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：转向其它仍超长 pane owner 的内部 owner 拆分，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` UI Asset detail fields owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 UI Asset detail field 单文件 owner，新增 `ui/retained_host/ui/pane_data_conversion/ui_asset_detail_fields/{mod,binding,layout,row_model,section_nodes,sections,slot,widget}.rs`；保留 `to_host_contract_ui_asset_template_nodes(...)` 入口；拆出 Widget/Slot/Layout/Binding 行构建、row DTO/helper、section 聚合和 retained section node 注入。 | `rustfmt --edition 2021 --check` 覆盖 `ui_asset_detail_fields/*.rs` 通过；旧 owner 文件 `Test-Path` 为 false；父级 flat files 中旧内部函数名残留扫描清零；行数采样：`binding.rs` 55 行、`layout.rs` 100 行、`mod.rs` 31 行、`row_model.rs` 44 行、`section_nodes.rs` 133 行、`sections.rs` 35 行、`slot.rs` 172 行、`widget.rs` 91 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 M5.T1/S7：拆 `pane_ui_asset_conversion.rs`、Build/Export 或其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` build export owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 Build/Export pane 单文件 owner，新增 `ui/retained_host/ui/pane_data_conversion/build_export/{mod,target_rows,tests}.rs`；保留 `to_host_contract_build_export_pane_from_host_pane(...)` 入口；拆出 fallback target-row/action button projection，并把原 Build/Export pane 行为测试挂到 owner tree。 | `rustfmt --edition 2021 --check` 覆盖 `build_export/*.rs` 通过；旧 owner 文件 `Test-Path` 为 false；父级 flat files 中 target-row/action helper 残留扫描清零；行数采样：`mod.rs` 73 行、`target_rows.rs` 340 行、`tests.rs` 399 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：拆 `pane_ui_asset_conversion.rs` 或其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` UI Asset pane conversion test owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion_tests.rs`，迁出 `pane_ui_asset_conversion.rs` 内联 UI Asset pane conversion 行为测试和 fixture imports；父 `mod.rs` 只保留 `cfg(test)` 测试 owner 挂载，生产 owner 继续持有 UI Asset pane host-contract conversion。 | `rustfmt --edition 2021 --check` 覆盖 `mod.rs`、`pane_ui_asset_conversion.rs` 与 `pane_ui_asset_conversion_tests.rs` 通过；生产 owner 中 `#[cfg(test)]`/`mod tests`/测试函数/`ViewTemplateFrameData`/`ViewTemplateNodeData` 残留扫描清零；行数采样：`pane_ui_asset_conversion.rs` 466 行、`pane_ui_asset_conversion_tests.rs` 247 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：拆 UI Asset production conversion 的 preview/style/inspector 数据子域或其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` UI Asset production owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 UI Asset production conversion 单文件 owner，新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion/{mod,preview,style,inspector,string_selection}.rs`；`mod.rs` 保留 `to_host_contract_ui_asset_pane(...)` façade 与顶层装配，Preview/Style/Inspector 数据转换和 shared-string helper 分别下沉到子 owner，同时补回原父级缺失的 `to_host_contract_shared_string_list(...)`。 | `rustfmt --edition 2021 --check` 覆盖 `pane_ui_asset_conversion/*.rs`、父 `mod.rs` 与 `pane_ui_asset_conversion_tests.rs` 通过；旧 owner 文件 `Test-Path` 为 false；新 owner tree 中 stale `super::to_host_contract_shared_string_list` 扫描清零；行数采样：`mod.rs` 160 行、`preview.rs` 103 行、`style.rs` 112 行、`inspector.rs` 176 行、`string_selection.rs` 17 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：拆 UI Asset runtime-report/designer/palette-drag 装配或其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` UI Asset runtime-report/designer/palette owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion/{runtime_report,designer_tools,palette_drag}.rs`；迁出 runtime report、designer tool state、palette drag 与 slot target DTO 转换，`preview.rs` 不再持有 palette drag slot target helper，`mod.rs` 保留 UI Asset pane façade 与 header/actions/collections/source 顶层装配。 | `rustfmt --edition 2021 --check` 覆盖 `pane_ui_asset_conversion/*.rs`、父 `mod.rs` 与 `pane_ui_asset_conversion_tests.rs` 通过；`mod.rs` 中 runtime-report/designer/palette raw fields 和 DTO struct 残留扫描清零；`preview.rs` 中 `CanvasSlotTarget`/palette drag helper 残留扫描清零；行数采样：`mod.rs` 119 行、`preview.rs` 84 行、`runtime_report.rs` 46 行、`designer_tools.rs` 13 行、`palette_drag.rs` 43 行、`style.rs` 112 行、`inspector.rs` 176 行、`string_selection.rs` 17 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：拆 UI Asset header/actions/collections/source 或其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` UI Asset header/actions/collections/source owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion/{header,actions,collections,source}.rs`；迁出 UI Asset header/action/collection/source DTO 转换，`mod.rs` 只保留 template-node detail field 调用、子 owner 调用和最终 `UiAssetEditorPaneData` 总装。 | `rustfmt --edition 2021 --check` 覆盖 `pane_ui_asset_conversion/*.rs`、父 `mod.rs` 与 `pane_ui_asset_conversion_tests.rs` 通过；`mod.rs` 中 header/actions/collections/source raw fields 和 DTO struct 残留扫描清零；行数采样：`mod.rs` 71 行、`actions.rs` 34 行、`collections.rs` 23 行、`header.rs` 15 行、`source.rs` 23 行、`preview.rs` 84 行、`runtime_report.rs` 46 行、`designer_tools.rs` 13 行、`palette_drag.rs` 43 行、`style.rs` 112 行、`inspector.rs` 176 行、`string_selection.rs` 17 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：转向其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` Build/Export target rows owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 `build_export/target_rows.rs`，新增 `build_export/target_rows/{mod,actions,identity,metrics,node,row}.rs`；保留 `build_export_target_row_nodes(...)` 入口，拆出 target id/key、布局常量、基础 node 构造、row action/button projection 和单 row 节点组装。 | `rustfmt --edition 2021 --check` 覆盖 `build_export/mod.rs`、`build_export/tests.rs` 与 `build_export/target_rows/*.rs` 通过；旧 owner 文件 `Test-Path` 为 false；`build_export/mod.rs` 中 target-row 内部 helper/constant 残留扫描清零；行数采样：`mod.rs` 92 行、`actions.rs` 120 行、`identity.rs` 31 行、`metrics.rs` 7 行、`node.rs` 18 行、`row.rs` 124 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：拆其它仍偏大的 pane owner，保持 root façade 只做模块出口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection helper owner split | 完成（代码与文档记录；package-level Cargo check 未重启；binding/dialog 后续已目录化） | 新增 `pane_component_projection/{attribute_values,badge,binding_actions,button_style,collection_window,dialog,string_lists,value_color}.rs`；迁出 root `mod.rs` 中的属性读取、Badge/Dialog 投影、binding action 派生、button-style 别名、virtual collection window、shared string list 与 value-color helper，测试改为显式导入 `UiEventKind`。该 binding action helper 后续已硬切为 `pane_component_projection/binding_actions/` tree，dialog helper 后续已硬切为 `pane_component_projection/dialog/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、新增 helper owner 与 `tests.rs` 通过；`mod.rs` 中旧 helper 函数定义残留扫描清零；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 binding action 与 dialog owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection surface metrics owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 先新增 surface metrics projection owner；迁出 `surface_defaults.rs` 中的 corner radius、border width、elevation、z-index、MUI z-index 常量与默认 helper，`surface_defaults.rs` 保留 variant/surface/text/validation role-token 逻辑。该 owner 后续已硬切为 `pane_component_projection/surface_metrics/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`surface_defaults.rs` 与 surface metrics owner 通过；`surface_defaults.rs` 中 surface metric/z-index 函数与 MUI z-index 常量残留扫描清零；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 surface metrics owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection surface defaults owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 `pane_component_projection/surface_defaults.rs`，新增 `surface_defaults/{mod,alert_dialog,badge,chip,component_variant,shared,skeleton,surface,text_tone,validation}.rs`；`mod.rs` 保留四个上层投影 wrapper，子 owner 分别持有 component role token、Badge/Alert/Dialog/Chip/Skeleton 变体、surface/text/validation 推导与共享 TOML 属性解析。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、surface metrics owner 与 `surface_defaults/*.rs` 通过；旧 owner 文件 `Test-Path` 为 false；`pane_component_projection/*.rs` 中 `surface_defaults.rs` 引用扫描清零；行数采样：`surface_defaults/mod.rs` 43 行、`component_variant.rs` 91 行、`badge.rs` 114 行、`alert_dialog.rs` 86 行、`chip.rs` 93 行、`skeleton.rs` 55 行、`shared.rs` 77 行、`surface.rs` 54 行、`text_tone.rs` 27 行、`validation.rs` 31 行、`pane_component_projection/mod.rs` 847 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：继续拆 `host_template_node(...)` 主体或其它仍偏大的 pane component projection owner，保持 root owner 收束。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection world-space owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 world-space projection owner；迁出 `host_template_node(...)` 中 world-space 属性读取、默认值、vector component 展开和 retained-host world field 中间 projection，父函数只保留 `projected_world_space(...)` 调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/world_space/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 world-space owner 通过；父 `mod.rs` 中 `world_position/world_rotation/world_scale/world_size/pixels_per_meter/camera_target/vec_component` 解析残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 800 行、world-space owner 82 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 world-space owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection world-space owner-tree split | 完成（代码与文档记录；package-level Cargo check 未通过确认，已修复投影 owner 可见性诊断） | 删除旧单文件 world-space owner，新增 `pane_component_projection/world_space/{mod,activation,model,rendering,surface,transform}.rs`；world-space 总装、DTO、enabled gate、position/rotation/scale transform、world size/pixels 与 rendering metadata 分别落到具名叶子；复验暴露的 sibling/parent owner re-export 边界已统一收敛为 `pub(in super::super)`。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`template_node_data/{spatial,parts}.rs`、`tests/world_space.rs`、`world_space/{mod,activation,model,rendering,surface,transform}.rs` 以及本次可见性修正的 DTO/helper 文件通过；旧 owner `Test-Path` 为 false；旧生产路径扫描清零；行数采样：`transform.rs` 41 行、`mod.rs` 34 行、`rendering.rs` 31 行、`surface.rs` 24 行、`model.rs` 19 行、`activation.rs` 11 行；`cargo check -p zircon_editor --locked` 到达 editor 编译层但未通过，首批 pane-component 诊断为 E0364/E0365/E0603 private re-export/import，可见性修正后因独立 runtime cargo/rustc lanes 活跃暂未复跑；该 check 仍含其它 editor 层既有错误。 | 继续 M5.T1/S7：等编译通道空闲后复跑 package check，或继续评估剩余中型 projection owner。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection collection/pagination owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 collection/pagination projection owner；迁出 collection items 读取、collection field projection 调用、virtualization window 过滤、item extent/overscan/total/visible counters 和 pagination page/total counters，父函数只保留 `projected_collection(...)` 调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/collection_projection/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 collection/pagination owner 通过；父 `mod.rs` 中 collection/virtualization/pagination 属性解析残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 736 行、collection/pagination owner 87 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 collection projection owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection collection projection owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 collection projection owner，新增 `pane_component_projection/collection_projection/{mod,fields,items,model,pagination,virtualization}.rs`；collection 总装、中间 DTO、items/window、fields handoff、pagination 与 virtualization projection 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`template_node_data/{options_collection,parts}.rs` 与 `collection_projection/{mod,fields,items,model,pagination,virtualization}.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`virtualization.rs` 45 行、`mod.rs` 35 行、`pagination.rs` 33 行、`items.rs` 26 行、`model.rs` 16 行、`fields.rs` 14 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估剩余中型 projection owner 或进入测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection value/media owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 value/media projection owner；迁出 `host_template_node(...)` 中 value text/number/percent/color、media source、icon name、preview image 加载和 vector component projection，父函数只保留 `projected_value_media(...)` 调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/value_media/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与 value/media owner 通过；父 `mod.rs` 中 `UiValue`、value/media preview helper、value percent/color 与 vector component 解析残留扫描清零，仅保留 `layout_icon_size` 的布局字段读取和 DTO 字段赋值；旧单文件 owner 当时 121 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 value/media owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection popup/action owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 popup/action projection owner；迁出 menu items/structured menu items、popup open/anchor/frame、accepted drag payloads、showcase/binding action ids、dialog action fallback 与 dispatch kind projection，父函数只保留 `projected_popup_actions(...)` 调用、transition popup-open 读取和 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/popup_actions/` tree。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与当时 popup/action owner 通过；父 `mod.rs` 中 primary/preferred action helper、dialog action、popup frame/menu 解析残留扫描清零，仅保留 `structured_menu_items` DTO 字段赋值；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 popup/action owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection text/layout owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 text/layout projection owner；迁出 text/label、label color/brightness、layout offsets、selected segment border/underline、font size/weight、text align 与 overflow projection，父函数只保留 `projected_text_layout(...)` 调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/text_layout/` tree。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与当时 text/layout owner 通过；父 `mod.rs` 中 text/layout/selected segment/font/text align/overflow 属性解析残留扫描清零；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 text/layout owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection visual-state owner split | 完成（代码与文档记录；package-level Cargo check 未通过确认；后续已目录化） | 新增 visual-state projection owner；迁出 checked/expanded/focused/hovered/pressed/dragging/drop-hover/active-drag-target、state-layer、ripple 与 icon-state projection，父函数只保留 `projected_visual_state(...)` 调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/visual_state/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与 visual-state owner 通过；父 `mod.rs` 中 state-layer/ripple/icon-state/drop/active-drag 直接属性解析残留扫描清零，仅保留 DTO 字段赋值；旧单文件 owner 当时 91 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 尝试运行但 120 秒超时，未得到通过结论。 | 后续已完成 visual-state owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection selection/options owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 selection/options projection owner；迁出 selection state、search query、selected flag、tree depth/indent、options text/list、command-palette/notification-center structured options 与 generic structured option fallback，父函数只保留 `projected_selection_options(...)` 调用、host 模型转换和 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/selection_options/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 selection/options owner 通过；父 `mod.rs` 中 selection/search/tree/options/structured options 直接属性解析残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 316 行、selection/options owner 81 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 selection/options owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection selection/options owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 selection/options owner，新增 `pane_component_projection/selection_options/{mod,model,options,search,selection,structured,tree}.rs`；selection state、selected flag、query、tree depth/indent、options list、structured-option handoff 和 fallback 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`template_node_data/{options_collection,parts}.rs` 与 `selection_options/{mod,model,options,search,selection,structured,tree}.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 33 行、`tree.rs` 24 行、`selection.rs` 23 行、`structured.rs` 17 行、`options.rs` 15 行、`model.rs` 12 行、`search.rs` 10 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估剩余中型 projection owner 或进入测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection visual-style owner split | 完成（代码与文档记录；package-level Cargo check 未重启；后续已目录化） | 新增 visual-style projection owner；迁出 component category/layout token、component variant、surface variant、text tone、button variant/style、corner/border/elevation/z-index 与 transition metadata projection，父函数只保留 `projected_visual_style(...)` 调用和 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/visual_style/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与 pre-tree visual-style owner 通过；父 `mod.rs` 中 button/surface/transition helper 调用和局部解析残留扫描清零，仅保留 DTO 字段赋值；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 visual-style owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection validation/drop/clip owner split | 完成（代码与文档记录；package-level Cargo check 未重启；drag-overlay 后续已目录化） | 新增 `pane_component_projection/validation_state.rs` 与 `clip_frame.rs`，并把 drag/drop 投影交由当时的 drag-overlay owner 承接 `drop_source_summary`；迁出 disabled/validation level/message、drop source summary 和 runtime clip frame 到 retained-host frame DTO 的转换，父函数只保留 owner 调用和 DTO 字段赋值。该 drag-overlay owner 后续已硬切为 `pane_component_projection/drag_overlay/` tree。 | 当时 `rustfmt --edition 2021 --check` 覆盖 validation/drop/clip owner 通过；父 `mod.rs` 中 disabled/enabled/validation_message/drop_source_summary 直接属性解析和 inline clip frame DTO 转换残留扫描清零，仅保留 DTO 字段赋值与 `projected_clip_frame(...)` 调用；行数采样：`mod.rs` 256 行、`validation_state.rs` 32 行、`clip_frame.rs` 19 行、drag-overlay owner 111 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 drag-overlay owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection drag-overlay owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 drag-overlay owner，新增 `pane_component_projection/drag_overlay/{mod,attributes,cursor,drop_target,indicator,model,payload}.rs`；drag-overlay 总装、中间 DTO、attribute conversion、payload、cursor/preview/offset、drop target 和 drop indicator projection 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`drag_overlay_tests.rs`、`template_node_data/{interaction,parts}.rs` 与 `drag_overlay/{mod,attributes,cursor,drop_target,indicator,model,payload}.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 59 行、`attributes.rs` 34 行、`cursor.rs` 30 行、`drop_target.rs` 30 行、`model.rs` 25 行、`payload.rs` 23 行、`indicator.rs` 20 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估剩余中型 projection owner 或进入测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection root-entry hard split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 `pane_component_projection/host_template_node.rs`；将 `host_template_node(...)` 主投影入口与最终 DTO 装配整体迁出，`pane_component_projection/mod.rs` 只保留 child module 声明、`host_template_node` re-export 和测试模块挂载。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与 `host_template_node.rs` 通过；`mod.rs` 中 `fn`、局部变量、crate/super imports、`TemplatePaneNodeData`、属性读取等实现形态扫描清零；行数采样：`mod.rs` 39 行、`host_template_node.rs` 224 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估 `host_template_node.rs` 的最终 DTO 装配是否还需要按 field-family 继续拆分；后续测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection template-node DTO assembly owner split | 完成（代码与文档记录；package-level Cargo check 未重启） | 新增 template-node DTO assembly owner，迁出最终 `TemplatePaneNodeData` 字段装配、host `ModelRc` 转换与 shared-string list 转换，`host_template_node.rs` 只负责 control/component role 解析、投影片段收集和 `ProjectedTemplateNodeParts` 交接；后续该 owner 已硬切为 `template_node_data/` tree。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs` 与 DTO assembly owner 通过；`host_template_node.rs` 中 `TemplatePaneNodeData {`、`model_rc`、shared-string list 转换和 DTO 字段族残留扫描清零；`mod.rs` implementation-shape 扫描仍清零；行数采样：`mod.rs` 40 行、`host_template_node.rs` 90 行、DTO assembly owner 206 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 `template_node_data/` field-family owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1/M3 `pane_data_conversion` pane component projection test owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 module-local test owner，新增 `tests/{mod,actions,collection,mui_surface_defaults,mui_variants,visual_metadata,world_space}.rs`；30 个 module-local projection 行为测试按 action/binding、collection/virtualization、MUI defaults、MUI variants、visual metadata 与 world-space field family 分层，共享 fixture/import 收束到 `tests/mod.rs`。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs` 与 `tests/*.rs` 通过；旧测试 owner `Test-Path` 为 false；旧路径残留扫描清零；行数采样：`tests/mod.rs` 63 行、`actions.rs` 71 行、`collection.rs` 116 行、`mui_surface_defaults.rs` 303 行、`mui_variants.rs` 234 行、`visual_metadata.rs` 349 行、`world_space.rs` 36 行。 | 后续已完成 `template_node_data/` field-family owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection collection-fields owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 collection-fields owner，新增 `collection_fields/{mod,array,map,empty,roles,type_tokens,validation,tests}.rs`；ArrayField row、MapField row、empty warning row、role mapping、declared type 分类、key/value validation 与私有 owner 行为测试分别落到具名叶子，父 `mod.rs` 只保留 `collection_fields_for_component(...)` dispatch。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、collection projection owner 与 `collection_fields/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`validation.rs` 111 行、`array.rs` 85 行、`map.rs` 70 行、`tests.rs` 60 行、`roles.rs` 40 行、`empty.rs` 31 行、`mod.rs` 22 行、`type_tokens.rs` 19 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `collection_projection/` 与 `template_node_data/` field-family owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection showcase-actions owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 showcase-actions owner，新增 `showcase_actions/{mod,action_buttons,binding_ids,commit_action,drag_actions,edit_action,primary_action,tests}.rs`；binding path/action id 归一化、primary/drag/edit/commit action selection、action-button DTO projection 与私有 owner 行为测试分别落到具名叶子，父 `mod.rs` 只保留受限导出。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`popup_actions/*.rs`、`collection_fields/{array,map}.rs` 与 `showcase_actions/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`primary_action.rs` 68 行、`binding_ids.rs` 57 行、`tests.rs` 54 行、`action_buttons.rs` 50 行、`drag_actions.rs` 35 行、`edit_action.rs` 24 行、`commit_action.rs` 19 行、`mod.rs` 16 行；`verify-showcase-state-matrix.mjs` 已改为读取新 owner tree，但仍因既有 deterministic demo-input 覆盖缺口返回非零；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `popup_frame/`、`command_palette/` 与 `template_node_data/` owner-tree split；测试窗口仍补跑 package/workspace validation，并补齐 showcase demo-input 覆盖缺口。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection popup-frame owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 popup-frame owner，新增 `popup_frame/{mod,attributes,origin,overlay,placement,tests}.rs`；anchor-positioned overlay gate、numeric attribute read、anchor/transform origin alignment、Popper placement geometry 与私有 owner 行为测试分别落到具名叶子，父 `mod.rs` 只保留 `projected_popup_frame(...)` façade 和 frame 总装。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`popup_actions/*.rs` 与 `popup_frame/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 98 行、`tests.rs` 70 行、`placement.rs` 60 行、`origin.rs` 36 行、`overlay.rs` 26 行、`attributes.rs` 9 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `command_palette/` 与 `template_node_data/` owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection notification-center owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 notification-center 单文件 owner 与旧单文件测试 owner，新增 `notification_center/{mod,attributes,entries,entry,options,parse,tests}.rs`；notification row model、TOML/pipe-string parsing、attribute/tone conversion、visible-limit filtering、structured-option DTO 映射与私有 owner 行为测试分别落到具名叶子，父 `mod.rs` 恢复/保持 command-palette 与 notification-center 投影入口的受限导出。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、selection/options owner、value/media 当前 owner 入口与 `notification_center/*.rs` 通过；旧 owner 与旧测试 `Test-Path` 均为 false；行数采样：`tests.rs` 187 行、`options.rs` 70 行、`parse.rs` 70 行、`attributes.rs` 58 行、`entry.rs` 26 行、`entries.rs` 26 行、`mod.rs` 13 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `selection_options/`、`command_palette/` 与 `template_node_data/` owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection command-palette owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 command-palette 单文件 owner，新增 `command_palette/{mod,attributes,entries,entry,ids,options,parse,tests}.rs`；command row model、TOML/pipe-string parsing、attribute/query conversion、filtered/recent/disabled id projection、structured-option DTO 映射与私有 owner 行为测试分别落到具名叶子，父 `mod.rs` 保持 command-palette 投影入口的受限导出。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、selection/options owner 与 `command_palette/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`tests.rs` 127 行、`options.rs` 67 行、`parse.rs` 53 行、`attributes.rs` 42 行、`entries.rs` 33 行、`ids.rs` 30 行、`entry.rs` 24 行、`mod.rs` 13 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `selection_options/` 与 `template_node_data/` field-family owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection template-node DTO assembly owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 template-node DTO assembly 单文件 owner，新增 `template_node_data/{mod,assembly,content,identity,interaction,options_collection,parts,spatial,visual}.rs`；`parts.rs` 拥有投影片段 DTO，`assembly.rs` 拥有默认 `TemplatePaneNodeData` 创建与装配顺序，identity/content/options-collection/spatial/interaction/visual 字段族赋值分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs` 与 `template_node_data/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`visual.rs` 53 行、`interaction.rs` 50 行、`content.rs` 44 行、`assembly.rs` 40 行、`options_collection.rs` 34 行、`spatial.rs` 31 行、`parts.rs` 29 行、`identity.rs` 14 行、`mod.rs` 11 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估其它 pane component projection 中型 owner 或进入测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection popup/action owner-tree split | 完成（代码与文档记录；package-level Cargo check 未通过确认） | 删除旧 popup/action 单文件 owner，新增 `popup_actions/{mod,action_buttons,action_ids,drag_payloads,menu,model,popup_state}.rs`；总装入口、dialog/showcase action buttons、action id selection、accepted drag payload serialization、menu projection、intermediate model 与 popup open/anchor/frame state 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs` 与 `popup_actions/*.rs` 通过；旧 owner `Test-Path` 为 false；旧路径扫描清零；行数采样：`mod.rs` 72 行、`action_ids.rs` 63 行、`popup_state.rs` 55 行、`action_buttons.rs` 22 行、`menu.rs` 21 行、`model.rs` 20 行、`drag_payloads.rs` 16 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 尝试运行但 300 秒超时，未得到通过结论，超时残留的 cargo/rustc 进程已停止。 | 后续已完成 text/layout owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection text/layout owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧 text/layout 单文件 owner，新增 `text_layout/{mod,attributes,label,model,offsets,selected_segment,text,typography}.rs`；总装入口、文本来源选择、label fields、layout offsets、selected segment metrics、typography/overflow 和中间模型分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs` 与 `text_layout/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`offsets.rs` 56 行、`mod.rs` 55 行、`typography.rs` 37 行、`label.rs` 29 行、`text.rs` 29 行、`selected_segment.rs` 28 行、`model.rs` 23 行、`attributes.rs` 12 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 surface metrics owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection surface metrics owner-tree split | 完成（代码与文档记录；package-level Cargo check 300s 超时无通过结论） | 删除旧单文件 surface metrics owner，新增 `surface_metrics/{mod,border,corner,elevation,values,z_index}.rs`；corner/radius/square、outlined/dialog border width、MUI surface elevation、MUI z-index/default fallback、TOML int conversion 与 variant helper 分别落到具名叶子，`mod.rs` 仅保留受限导出。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、visual-style owner 与 `surface_metrics/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`elevation.rs` 44 行、`z_index.rs` 39 行、`border.rs` 28 行、`corner.rs` 27 行、`values.rs` 13 行、`mod.rs` 9 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 300 秒超时，未得到通过结论，超时后 cargo/rustc 进程未残留。 | 后续已完成 transition metadata owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection transition metadata owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 transition metadata owner，新增 `transition_metadata/{mod,direction,kind,model,state,timing}.rs`；总装入口、DTO、transition kind、state/progress/entered、duration/easing 默认值和 slide direction fallback 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、visual-style owner 与 `transition_metadata/*.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`state.rs` 72 行、`timing.rs` 48 行、`mod.rs` 33 行、`kind.rs` 24 行、`direction.rs` 19 行、`model.rs` 9 行；package-level Cargo check 未重启，因为上一 surface metrics 切片刚以 300 秒超时结束，后续测试窗口统一补跑。 | 后续已完成 value/media owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection value/media owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 value/media owner，新增 `value_media/{mod,icon,media,model,number,text,vector}.rs`；总装入口、value/media DTO、value text fallback、numeric alias、media source、icon name 与 vector component extraction 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`template_node_data/content.rs` 与 `value_media/{mod,icon,media,model,number,text,vector}.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 46 行、`text.rs` 27 行、`media.rs` 20 行、`icon.rs` 18 行、`number.rs` 14 行、`model.rs` 13 行、`vector.rs` 8 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估其它 pane component projection 中型 owner 或进入测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection visual-state owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 visual-state owner，新增 `visual_state/{mod,flags,icon,model,ripple,state_layer}.rs`；总装入口、visual-state DTO、interaction flags、state-layer、ripple 与 icon-state projection 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`template_node_data/visual.rs` 与 `visual_state/{mod,flags,icon,model,ripple,state_layer}.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`flags.rs` 38 行、`mod.rs` 34 行、`ripple.rs` 34 行、`icon.rs` 25 行、`state_layer.rs` 25 行、`model.rs` 21 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 M5.T1/S7：评估其它 pane component projection 中型 owner 或进入测试窗口补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection dialog owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 dialog owner，新增 `dialog/{mod,actions,message}.rs`；plain dialog value text、plain dialog action、confirm/alert cancel/confirm action DTO projection 分别落到具名叶子，`mod.rs` 只保留受限导出。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`host_template_node.rs`、`value_media/text.rs`、`popup_actions/action_buttons.rs` 与 `dialog/{mod,actions,message}.rs` 通过；旧 owner `Test-Path` 为 false；旧生产路径扫描清零；行数采样：`actions.rs` 75 行、`message.rs` 11 行、`mod.rs` 4 行；package-level Cargo check 未重启，因为独立 runtime cargo/rustc lanes 正在占用编译通道。 | 后续已完成 binding-actions 与 visual-style owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection binding-actions owner-tree split | 完成（代码与文档记录；package-level Cargo check 未重启） | 删除旧单文件 binding-actions owner，新增 `binding_actions/{mod,path,primary}.rs`；Click/Change/Submit primary binding/action selection 与 binding path segment normalization 分别落到具名叶子，`mod.rs` 只保留受限导出。 | `rustfmt --edition 2021 --check` 覆盖 `pane_component_projection/mod.rs`、`popup_actions/action_ids.rs` 与 `binding_actions/{mod,path,primary}.rs` 通过；旧 owner `Test-Path` 为 false；行数采样：`primary.rs` 35 行、`path.rs` 25 行、`mod.rs` 6 行；package-level Cargo check 未重启，因为独立 runtime cargo/rustc lanes 正在占用编译通道。 | 后续已完成 visual-style owner-tree split；测试窗口仍补跑 package/workspace validation。 |
| 2026-06-22 | Editor UI 10 M5.T1 `pane_data_conversion` pane component projection visual-style owner-tree split | 完成（代码与文档记录；package-level Cargo check 通过） | 删除旧单文件 visual-style owner，新增 `visual_style/{mod,button,component,model,surface}.rs`；总装入口、style DTO、component token/variant、button style alias、surface metrics 与 transition metadata handoff 分别落到具名叶子。验证期同步修复 retained-host/native-pointer、paint-template-node 与 editor AssetManager handle/trait 接口收束后暴露的最低层可见性和类型边界。 | `cargo fmt -p zircon_editor --check` 通过；旧 owner `Test-Path` 为 false；旧生产路径与冲突标记扫描清零；行数采样：`mod.rs` 41 行、`surface.rs` 28 行、`button.rs` 21 行、`component.rs` 20 行、`model.rs` 16 行；`cargo check -p zircon_editor --locked --message-format short --color never` 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换。 | 继续 M5.T1/S7：评估剩余中型 projection owner 或进入测试窗口补跑 full Cargo test/workspace validation。 |
| 2026-06-22 | Editor UI 10 M1/M2 structure audit gate scaffold | 完成（审计脚本、guard 与镜像文档记录；Cargo test 入口已修复并通过） | 新增 `.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_editor_structure.py` 与 `editor_structure_audits/module_convention_boundary.py`，输出 editor `module_convention_gate`；新增 `zircon_editor/src/tests/structure_convention/mod.rs` 并接入 `src/tests/mod.rs`。当前审计实测：`m1_gate_status = migration-debt-present`、`migration_debt_count = 28`、超大生产文件 12、生产 `dead_code` 抑制 10、禁名模块 5、UI 顶层 owner 违规 0、重复测试树 1；visual-style 旧路径为 false 且 owner tree 完整。修复 retained-host/test-owner 编译漂移：模板节点测试 import、build-export 测试导入、runtime-interface `UiSurfaceFrame` 路径、chrome command stream 测试 helper、pane UI asset include 路径与 retained tab-drag 测试 helper。 | `python .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_editor_structure.py --json` 通过；`python -m py_compile ...audit_editor_structure.py ...module_convention_boundary.py` 通过；新增 guard 名称：`editor_ui_10_module_convention_audit_report_has_expected_shape`、`editor_ui_10_visual_style_owner_tree_is_hard_cut_over`、`editor_ui_10_module_convention_mirror_docs_track_audit_entry`；`cargo test -p zircon_editor --lib structure_convention --locked --message-format short --color never -- --test-threads=1` 通过（M1.T1 前，3 passed，2066 filtered out；仍有既有 warning noise）；`cargo check -p zircon_editor --locked --message-format short --color never` 通过（M1.T1 前，既有 warning noise）。 | 继续 M1/M2/M4：按审计输出处理剩余 banned name modules、重复测试树，以及 retained-host/editor-ui 超大文件；runtime/plugin `RuntimePluginDescriptor` 私有字段 blocker 修复后复跑 editor package/full-test validation。 |

| 2026-06-24 | Editor UI 10 structure gate and screenshot validation closeout | 完成（结构审计清零；截图 harness 通过；新投影断言 Cargo 复跑超时未计通过） | 按本计划 R1/R4.1/R6 复核 editor UI owner 边界：未新增兼容 shim、旧路径 façade、生产 `#[allow(dead_code)]` 或大根模块。`drawer_layout.rs` 只在 workbench drawer layout owner 内补侧抽屉 compact 输入，`template_buttons/identity.rs` 只补普通 editor button variant 的分类 gate，新增断言分别落在既有 visual screenshot/project overview/template button 测试 owner。 | `python .codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/audit_editor_structure.py --json` 通过，`m1_gate_status=classified-and-clear`、`migration_debt_count=0`、`oversized_production_file_count=0`、`production_dead_code_suppression_count=0`、`duplicate_test_tree_count=0`、`ui_module_owner_boundary_violation_count=0`、visual-style owner tree 完整。Scoped `rustfmt --edition 2021 --check` 覆盖本轮 touched Rust 文件通过；`capture_m3_gui_acceptance_visual_artifacts` 直接二进制通过并生成 8 张截图。`cargo test -p zircon_editor --lib project_overview_projection_maps_bootstrap_asset_into_template_nodes` 600s 超时，残留本轮 cargo/rustc 已停止，不计通过。 | 结构门当前保持清零；后续空闲编译窗口再补完整 `cargo check -p zircon_editor --lib --locked` 与 project-overview focused Cargo 结果。 |
| 2026-06-24 | Editor UI 10 Project Overview readable-button owner closeout | 完成（结构门保持清零；截图、直接二进制聚焦验证和 editor package check 通过；lib-test matrix 未跑完） | 继续按 R1/R4.1/R6 收束上一条视觉发现：component-owned `Button` projection 逻辑保留在 `view_projection.rs`，按钮分类保留在 `template_buttons/identity.rs`，fallback 字体宽度测量保留在 `paint_text/font.rs` 并由 `template_buttons/content/{metrics,entry}.rs` 使用；Project Overview DTO 断言保留在 `src/tests/ui/project_overview/bootstrap_assets.rs`，按钮 paint/identity 断言保留在 `template_buttons_tests/{paint,identity}.rs`。未新增 compatibility shim、旧路径 façade、生产 dead-code suppression、重复测试树或根模块行为。 | 直接二进制验证：`editor_variant_button_uses_centered_button_text_path` 1/1 通过；`project_overview_projection_maps_bootstrap_asset_into_template_nodes` + `workbench_button_matches_button_nodes_without_icon_or_tab_nodes` 2/2 通过；ignored `capture_m3_gui_acceptance_visual_artifacts` 1/1 通过并刷新 8 张截图到 2026-06-24 09:43 +08。截图裁剪确认 `Open Assets` 与 `Asset Browser` 在 900x620 Project Overview 左栏完整可读。结构审计 `audit_editor_structure.py --json` 仍为 `classified-and-clear` 且各违规计数为 0；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --message-format short --color never` 于 2026-06-24 09:59 +08 通过，仅有既有 warning 噪声。`cargo test -p zircon_editor --lib --locked ... -- --test-threads=1` 20 分钟编译期超时，残留本轮 editor Cargo/Rustc 已停止，不计通过。 | 结构治理闭环保持完成；后续只补 full editor lib-test / integration package matrix。 |

## 5. 审计与 guard 契约（本计划新增）

- **审计脚本**：新建 owner 域同级目录 `.codex/skills/zircon-project-skills/zr-runtime-interface-convergence/scripts/editor_structure_audits/`，含 `module_convention_boundary.py` + `_markdown.py` 与聚合器 `audit_editor_structure.py`。字段同 Runtime 15 的 `module_convention_*`，加 editor owner 分类（对齐 `large-file-ownership` 的 `editor-retained-host` / `editor-ui`）。
- **guard 测试**：`zircon_editor/src/tests/structure_convention/`，含 `editor_ui_10_module_convention_mirror_docs_match_structure_audit_counts`。
- **镜像文档**：`docs/zircon_editor/structure/module-convention.md`。

## 6. 硬切 checklist

- [ ] 旧路径文件 / 模块已物理删除
- [ ] 全部调用方已迁移到新 owner / façade（grep 零命中）
- [ ] 无重复测试树残留（一个行为一个 owner）
- [ ] 无兼容 re-export / 双路径
- [ ] 删除清单写进提交说明

## 7. 完成定义

editor `module_convention_gate.m1_gate_status = classified-and-clear`、各 violation 字段 = 0、镜像守卫绿；`cargo test -p zircon_editor --lib`、`cargo test -p zircon_editor --test integration_contracts --features integration-contracts`、`cargo fmt --all --check`、`audit_editor_structure.py --json` 无 risk。

## 8. 联动

- 与 `large-file-ownership-m1.md` 的 `editor-retained-host`(1) / `editor-ui`(2) 全局 owner 类共享生产 hotspot 清单；本计划自身 editor 审计还跟踪 `editor-retained-host`(1) / `editor-ui`(2) 的 retained-host/editor-ui production-owner debt，拆分进度计入 M2。
- editor UI 计划 08/09（workbench shell、editor modules）在其切片触及上述超大文件时，按本计划 owner 边界落地，不在旧巨型文件上叠加。
- 阶段 gate：本计划与 editor UI 计划阶段 A/B/C/D 并行，但**触及同一文件的能力切片须先过本计划的 owner 拆分**（避免在待拆文件上做能力深化被返工）。
