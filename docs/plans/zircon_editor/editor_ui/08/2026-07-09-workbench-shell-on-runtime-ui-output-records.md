# 08-workbench-shell-on-runtime-ui 产出记录归档

> 来源：[`08-workbench-shell-on-runtime-ui.md`](../08-workbench-shell-on-runtime-ui.md) 的 `## 状态与产出记录`。

## 状态与产出记录

> 迁入产出记录：editor_ui index 迁入记录 已拆入 [`08/2026-07-09-index-output-records.md`](2026-07-09-index-output-records.md)；本子计划状态表保留当前执行行，长记录在子目录文件中维护。


| 日期 | 切片 | 状态 | 产出 | 验证 | 后续 |
|------|------|------|------|------|------|
| 2026-06-29 | Plan 11 M5 Workbench shell `.zui` layout authority guard | 完成（文档当前权威与静态守卫补齐；不改生产代码） | §3.1 明确 Workbench shell layout 描述只以 `.zui` 为当前权威，`.ui.toml` / `.v2.ui.toml` 后缀已退役，不再作为当前壳层、抽屉、浮窗或插件 editor view/layout 入口；历史状态表中的旧后缀只保留为迁移证据。`tools/tests/test_zui_docs_suffix_convergence.py` 新增 Plan 08 当前设计段守卫，防止旧后缀重新进入当前架构口径。 | RED：focused unittest 先失败并列出 §3 缺少 `.zui-only` 当前事实；GREEN：`python -m unittest tools.tests.test_zui_docs_suffix_convergence.ZuiDocsSuffixConvergenceTests.test_workbench_shell_plan_design_declares_zui_only_layout_authority` 通过 1/1。 | 不运行 Cargo；完整 editor lib-test / integration package matrix 仍属于清洁长窗口验证项。 |
| 2026-06-25 | Editor UI 08/10 Workbench projection focused execution closeout | 完成（WSL focused execution、结构门、editor check 与截图复核通过；完整 lib/integration matrix 仍作为广域长窗口项） | 关闭本轮 `workbench_projection` 暴露的最低层问题：viewport gizmo X/Y/Z 轴文字通过 `workbench_viewport_panel.zui` 的显式 `style.self.foreground_color` 保留 authored axis 色；`workbench_window_projection.rs` 分离 authored text 与 value-display 语义，Button/Tab/Segmented 等命令控件不再被 value 渲染文本覆盖；Inspector 动态选中行改用 `background`/`border` 覆盖静态 style override，清空的属性槽会清掉旧 surface style；TreeRow 选中背景在 retained painter 与 runtime collection-row renderer 两层都消费模板声明的 `background_color`；`chrome_template_projection.rs` 恢复 `DOCK_HEADER_ASSET` source-contract 并把 menu chrome/popup 投影交给 `chrome_template_projection/menu_chrome.rs`；reflection source contract 改为扫描 folder-backed `ui/reflection/`。未新增 compatibility path、旧 façade、生产 `#[allow(dead_code)]`、panic fallback、`style_override_foreground_color` sentinel 或临时诊断输出。 | `cargo fmt --all --verbose` 通过；WSL `cargo test -p zircon_runtime --lib render_extract_expands_collection_row_primitives ...` 1/1 通过；WSL `cargo test -p zircon_editor --lib componentized_workbench_window_template_bridge_updates_scene_tree_selection_state ...` 1/1 通过；WSL `cargo test -p zircon_editor --lib workbench_projection ...` 41/41 通过；`python .codex\skills\zircon-project-skills\zr-runtime-interface-convergence\scripts\audit_editor_structure.py --json` 通过，`m1_gate_status=classified-and-clear` 且 migration debt、oversized production file、dead-code suppression、duplicate test tree、UI owner boundary violation 均为 0；WSL `cargo check -p zircon_editor --lib --locked ...` 通过。人工复核 `docs/tests/editor` 的 8 张 2026-06-25 09:35 +08 截图：900x620 Workbench/menu popup、640x420 compact、1260x780 large 均非空，主 document、viewport、侧栏、底栏和 popup 均可见，未见空白中心或越界重叠。 | Windows focused Cargo 早前的 rustc 1.94.1 ICE/exit `-1` 仍作为本机 Windows lane 风险记录；当前 WSL CI-parity focused lane 已关闭 `workbench_projection` 目标结果。完整 `cargo test -p zircon_editor --lib --locked` 与 `integration_contracts` 仍需独立长窗口补跑，本行不声明广域矩阵通过。 |
| 2026-06-15 | 08 M4.S1/M4.S2 EditorCommandRegistry + default keymap baseline | 完成（代码、资产、模块服务、文档、轻量检查）；focused Cargo 被运行时渲染层编译错误阻断 | 新增 `ui::host::commands` 模块族，分离 command descriptor/action/enablement、default keymap、key chord normalization、CommandPalette `UiValue` projection；`EditorModule` 懒加载注册 `EditorCommandRegistry` 与 `EditorKeymap`；新增 `assets/ui/editor/keymap/default.keymap.toml` 覆盖 `Ctrl+Shift+P`、`Ctrl+S`、`Ctrl+Z`、`Delete`、`F5` 等默认键位；默认命令集覆盖现有 File/Edit/Selection/Runtime/View/Window/Help 菜单动作和 CommandPalette 入口 | `rustfmt --edition 2021 --check` 覆盖新增 command 模块与 host/lib re-export 已通过；default keymap `tomllib` 解析通过；`git diff --check`、冲突标记与尾随空白扫描通过（仅既有 LF/CRLF warning）；`cargo test -p zircon_editor --lib commands --locked ...` 首轮暴露 `default_chord` 局部命名遮蔽辅助函数的编译错误并已修正，复跑在进入 `zircon_editor` 前被 `zircon_runtime` post_process/SSR 编译错误阻断：`EXPOSURE_HISTOGRAM_WORKGROUP_SIZE` 私有重导出/私有导入，以及多处 SSR execute 调用参数数目不匹配 | M4.S3 接真实菜单树、CommandPalette committed id 到 editor action dispatcher、以及 runtime focus-path 未消费键到 `EditorKeymap::resolve(...)` 的输入接线；运行时 post_process/SSR owner 清理编译错误后补跑 focused `commands` Cargo；快速按钮旧实现删除必须等菜单树接通后执行 |
| 2026-06-15 | 08 M4.S3 menu bar source convergence baseline | 完成（代码、文档、轻量检查）；focused Cargo 超时且仍需等运行时渲染层编译阻塞清理后补跑 | `EditorCommandRegistry` 新增 `menu_bar_model(...)` / `menu_model(...)` 投影，默认菜单栏从 `EditorChromeSnapshot` 派生 `EditorCommandContext` 后由 registry 生成；菜单项保留现有 `MenuAction` binding 与 `operation_path`，启用态统一走 command enablement；View/Window 命令的 CommandPalette 标签继续为 `Open ...`，菜单路径改为自然标签；删除旧 `file_menu/edit_menu/selection_menu/play_menu/view_menu/window_menu/help_menu` 静态 builder 模块，只保留扩展菜单追加入口 | `rustfmt --edition 2021 --check` 覆盖 registry 与 menu 入口通过；旧 builder 引用扫描通过；`git diff --check` 通过（仅既有 LF/CRLF warning）；新增 `menu_bar_projects_registry_commands_with_contextual_enablement` 覆盖保存/撤销/重做/选择/播放启用态、View 自然标签、CommandPalette 非 MenuAction 不进可点击菜单；focused Cargo 两次验证分别 120s/304s 超时且无新 Rust 诊断，残留 cargo/rustc 进程已停止 | 接 `editor.command_palette` committed id 到 editor action dispatcher；接 runtime focus-path 未消费键到 `EditorKeymap::resolve(...)`；运行时 post_process/SSR 编译阻塞清理后补跑 `cargo test -p zircon_editor --lib commands ...` 和菜单投影 focused test；再做实机菜单/快捷键验收 |
| 2026-06-15 | 08 M4.S3 command id dispatch baseline | 完成（代码、文档、轻量检查）；未启动新的 Cargo 编译以避免叠加当前活跃 runtime 编译通道 | 新增 `EditorUiBindingPayload::EditorCommand { command_id }` 及 codec/constructor；`normalize_editor_event_binding(...)` 现在通过 `EditorCommandRegistry::event_for_command(...)` 把菜单型 command id 归一化为 `EditorEvent::WorkbenchMenu(...)`；当时 `EditorCommandDispatchError` 显式区分 unknown command、operation dispatch 未接线、CommandPalette 打开命令未接线；host/lib re-export 补齐错误类型；menu/native/template projection 的 action-id helper 能保留 `EditorCommand` id，后续 palette committed id 与 keymap resolve 可以直接产出同一 payload | `rustfmt --edition 2021 --check` 覆盖 command registry、binding payload/codec/constructor、event normalization、host/lib re-export 与 projection helpers；`EditorUiBindingPayload` match site 扫描确认生产入口已覆盖或保持明确菜单/操作专用；新增 registry tests 覆盖 command id -> `WorkbenchMenu` event 与 `EditorCommand` binding normalization；未启动 focused Cargo，因为当前 workspace 有独立 `zircon_runtime --features core-min` 编译通道活跃，且此前 editor focused Cargo 已被 runtime post_process/SSR 编译阻塞或长时间超时 | Runtime CommandPalette、未消费键与 overlay open 后续切片已接通；`EditorCommandAction::Operation` 已由后续 operation-backed command dispatch convergence 接入 operation registry |
| 2026-06-15 | 08 M4.S3 CommandPalette commit adapter baseline | 完成（代码、测试、文档、轻量检查）；focused Cargo 已到 editor 编译但最终超时 | 新增 `template_runtime::component_adapter::command`，验证 `command` domain 的 `UiComponentEvent::Commit` envelope，要求 `committed_command_id`/`selected_command_id`/`command_id` 提交值为非空字符串；`EditorEventRuntime::dispatch_ui_component_adapter_event(...)` 对 command domain 特判，构造 `EditorUiBindingPayload::EditorCommand` 并通过既有 `dispatch_binding(..., RetainedHost)` 进入统一事件归一化；新增 component_adapter tests 覆盖 committed command id -> `WorkbenchMenu(OpenProject)`、非字符串值拒绝、`editor.command_palette` 未接 overlay-open 路由时返回 HostMutation | `rustfmt --edition 2021 --check` 覆盖 command adapter、adapter mod、runtime access 与 component_adapter tests；冲突标记、尾随空白扫描通过；scoped `git diff --check` 通过（仅 LF/CRLF warning）；`cargo test -p zircon_editor --lib command_component_adapter_dispatches_committed_command_id_through_editor_events ...` 首次到 `zircon_editor` 编译层并暴露本地类型错误：`dispatch_binding` 期望 `EditorUiBinding`，代码传入了 `UiEventBinding`，已修正；复跑在 editor lib-test 编译/链接阶段 304s 超时且无新 Rust 诊断，匹配 `E:\cargo-targets\zircon-editor-ui-command-registry-0615` 的 cargo/rustc 进程已停止 | 把实际可见 CommandPalette surface 的 `committed_command_id` 变更/提交路由成 `command` domain envelope；接 runtime focus-path 未消费键到 `EditorKeymap::resolve(...)` 并复用同一 `EditorCommand` binding；编译通道清理后补跑 `cargo test -p zircon_editor --lib component_adapter ...` 与 command focused tests |
| 2026-06-15 | 08 M4.S2/M4.S3 unconsumed keymap dispatch bridge | 完成（代码、测试、文档、focused Cargo 通过） | `EditorKeyChord::from_keyboard_input(...)` 从 runtime `UiKeyboardInputEvent` 提取 pressed chord、保留修饰键、忽略释放/修饰键本身/dead key，并对 unidentified Delete/F-key 等常用键走 key_code fallback；`EditorKeymap::resolve_keyboard_input(...)` 直接把 keyboard event 解析到 command id；`EditorEventRuntime::dispatch_unhandled_input_keymap_command(...)` 只接受 `UiDispatchReply::Unhandled` 的键盘 dispatch result，解析 keymap 后构造 `EditorCommand` binding，并复用 retained-host 事件归一化路径；同时把 default keymap 解析从 `Value::from_str` 修为 TOML document `Table` 解析 | `rustfmt --edition 2021 --check` 覆盖 key_chord/keymap/registry/runtime access/component_adapter tests；冲突标记、尾随空白和 scoped `git diff --check` 通过（仅 LF/CRLF warning）；`cargo test -p zircon_editor --lib keymap_dispatches_unhandled_keyboard_result_through_editor_command_binding --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615 ...` 首轮暴露 default keymap TOML 解析方式错误并修正，复跑暴露 keymap 局部变量重名并修正，最终通过；`cargo test -p zircon_editor --lib keymap_dispatch ...` 2/0 通过；`cargo test -p zircon_editor --lib key_chord_normalizes_runtime_keyboard_input ...` 1/0 通过；`cargo test -p zircon_editor --lib keymap_resolves_unconsumed_chord_to_command ...` 1/0 通过；未见残留 cargo/rustc 进程 | 仍需把实际可见 runtime/native surface 的键盘 pump 在 `UiSurface` 返回 unhandled 后调用该桥 |
| 2026-06-15 | 08 M4.S3 visible CommandPalette committed-id route | 完成（代码、测试、文档、focused Cargo 通过） | `workbench_window.zui` 在 Workbench root 下挂载 collapsed `WorkbenchCommandPalette` 并声明 `CommandPalette/Commit` Submit route；`workbench_window_template_bindings.rs` 注册 `EditorCommand("editor.command_palette")` 占位 binding；新增 `callback_dispatch/workbench/command_palette.rs`，把 native edit/commit value 转成 `command` domain `committed_command_id` envelope，经 command adapter 转为 `EditorCommand` binding 并通过 retained-host event path 分发；`RetainedEditorHost` 的 Workbench surface edit path 优先识别该 route；新增 route/projection/dispatch focused tests | `rustfmt --edition 2021 --check` 覆盖 Workbench CommandPalette route 文件通过；`python -c "tomllib.loads(...workbench_window.zui...)"` 通过；`cargo test -p zircon_editor --lib workbench_command_palette --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 3/0/2008；`cargo test -p zircon_editor --lib componentized_workbench_inspector_property_edit_updates_row_preview --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2010，确认普通 Workbench surface edit route 未被命令面板分支劫持；首轮 Cargo 暴露测试临时日志对象生命周期问题 `E0716` 并已修正；残留超时 cargo/rustc 进程已停止 | retained native keyboard pump 已由后续切片接通；剩余实机 `Ctrl+Shift+P`/Enter 执行验收与后续全 runtime `UiSurface` hard cutover |
| 2026-06-15 | 08 M4.S3 CommandPalette open/effect route | 完成（代码、测试、文档、focused Cargo 通过） | `editor.command_palette` 现在由 `EditorCommandRegistry::event_for_command(...)` 归一化为 `EditorEvent::Transient(OpenCommandPalette)`；事件执行层新增 `CommandPaletteOpenRequested` effect，`UiHostEventEffects` 携带 `open_command_palette_requested`，`RetainedEditorHost::open_workbench_command_palette(...)` 从当前 `EditorChromeSnapshot` 构造 `EditorCommandContext`，把 registry 命令列表投影为 `commands`/`filtered_commands`/`disabled_commands` 并交给 `BuiltinWorkbenchWindowTemplateSurfaceBridge::open_command_palette(...)` 展开 Workbench overlay；模板桥新增 `close_command_palette(...)`，用于收束 popup open/visibility 状态 | `rustfmt --edition 2021 --check` 覆盖 command/event/effect/template bridge/app/tests 文件通过；`cargo test -p zircon_editor --lib command_palette --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 9/0/2005，覆盖 Workbench palette route、open/close state、palette-open commit 与 native painter/projection；`cargo test -p zircon_editor --lib command_palette_command_requests_open_effect --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2013；`cargo test -p zircon_editor --lib command_component_adapter_dispatches_palette_open_command --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2013；`cargo test -p zircon_editor --lib command_registry_maps_menu_command_ids_to_editor_events --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2013；`cargo test -p zircon_editor --lib componentized_workbench_inspector_property_edit_updates_row_preview --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2013 | retained native keyboard pump 已由后续切片接通；剩余实机 `Ctrl+Shift+P`/Enter 执行验收与后续全 runtime `UiSurface` hard cutover |
| 2026-06-15 | 08 M4.S3 retained native keymap pump | 完成（代码、测试、文档、focused Cargo 通过） | `UiHostContext` 新增 `unhandled_keyboard_input` callback；retained native window 记录修饰键与输入序号，文本/弹窗/focus path 未消费 pressed `KeyEvent` 时翻译成 `UiKeyboardInputEvent`；`RetainedEditorHost::dispatch_unhandled_native_keyboard_input(...)` 调用 `EditorEventRuntime::dispatch_keyboard_keymap_command(...)` 并应用 command record effects；原生 `Ctrl+Shift+P` 现在通过 keymap 打开 Workbench CommandPalette overlay | `rustfmt --edition 2021 --check` 覆盖 native keymap pump touched Rust 通过；`cargo test -p zircon_editor --lib native_unhandled_ctrl_shift_p_opens_workbench_command_palette --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2014；`cargo test -p zircon_editor --lib command_palette --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 10/0/2005；`cargo test -p zircon_editor --lib command_registry_maps_menu_command_ids_to_editor_events --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2014 | 可见 palette Enter/primary activation 已由后续 native activation 切片覆盖；operation-backed commands 已由后续 convergence 切片接入；后续全 runtime `UiSurface` hard cutover pump 收束 |
| 2026-06-15 | 08 M4.S3 visible CommandPalette native activation | 完成（代码、测试、文档、focused Cargo 通过） | `WorkbenchCommandPalette` Mount 现在在 workbench window projection 中复用 command-palette row 投影，导出真实 `structured_options` 与初始 focused/selected/disabled 状态；native popup `Accept` 对该 control 走 `CommandPalette/Commit` surface edit route 而不是 showcase option route；primary pointer activation 同样识别 command-palette option 并提交 command id；提交后复用既有 command adapter/EditorCommand 归一化路径，落到 `EditorEvent::WorkbenchMenu(...)` | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；`cargo test -p zircon_editor --lib native_command_palette_enter_commits_focused_workbench_command --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2017；`cargo test -p zircon_editor --lib apply_presentation_projects_open_workbench_command_palette_rows_for_native_input --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2017；`cargo test -p zircon_editor --lib native_unhandled_ctrl_shift_p_opens_workbench_command_palette --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2017；`cargo test -p zircon_editor --lib command_palette --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 13/0/2005；`cargo test -p zircon_editor --lib command_palette_option_routes_to_commit_activation --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615` 通过 1/0/2017 | live/manual real-window `Ctrl+Shift+P` + Enter 验收仍需补；后续 full runtime `UiSurface` hard cutover pump 收束 |
| 2026-06-15 | 08 M4.S3 operation-backed command dispatch convergence | 完成（代码、测试、文档、focused Cargo 通过） | 默认命令集中凡能映射到 `EditorOperationPath` 的菜单命令现在声明为 `EditorCommandAction::Operation`，稳定 command id 不变；菜单栏投影对操作型命令输出 `EditorOperation` binding 与 `operation_path`，非操作命令仍保留 `MenuAction`；`EditorEventRuntime::dispatch_binding(...)` 识别 `EditorCommand`/`EditorOperation` payload，操作型命令统一调用 `invoke_operation(...)`，从而保留 operation metadata、undo stack、failure journal、参数与 source 映射；`EditorCommandRegistry::event_for_command(...)` 对内置操作型命令仍可解析静态 event shape；retained menu-pointer 在没有投影菜单模型时的 fallback 也切到相同内置 operation id，layout preset save/load 保留非 operation 特例 | `rustfmt --edition 2021 --check zircon_editor/src/core/commands/registry.rs zircon_editor/src/ui/host/editor_event_dispatch.rs zircon_editor/src/tests/editor_event/runtime.rs` 通过；menu-pointer fallback touched Rust rustfmt 通过；`cargo test -p zircon_editor --lib menu_commands_project_operation_backed_bindings_when_operation_paths_exist ...` 首轮 184s/424s 在编译期超时无诊断，最终长超时通过 1/0/2019；`editor_command_operation_action_invokes_operation_registry` 通过 1/0/2019；`commands` 通过 37/0/1983；`command_palette` 通过 13/0/2007；`menu_binding` 通过 6/0/2014；`shared_menu_pointer_click_dispatches_reset_layout_through_runtime_dispatcher` 通过 1/0/2019；`retained_menu_pointer` 通过 22/0/1994（4 个截图用例 ignored）；warning 噪声为既有未清理 warning | live/manual real-window `Ctrl+Shift+P` + Enter + 操作型命令执行验收仍需补；后续 full runtime `UiSurface` hard cutover pump 收束；扩展命令注册若要进入 CommandPalette 需后续把 extension operation catalog 并入 command registry |
| 2026-06-15 | 08 M5.S1 floating window surface ownership baseline | 完成（代码、测试、文档、focused Cargo 通过） | `WindowHostManager` 的每个 `NativeWindowRecord` 现在拥有独立 runtime `UiSurface`，并通过 `NativeWindowHostState.surface_tree_id` 暴露稳定树 id；`sync_layout_windows(...)` 在 bounds 更新时保留既有 surface，在 floating window 从布局移除时同步释放宿主记录；功能编辑器窗口启动路径断言 `window:{instance_id}` 对应的 native surface tree id，例如 `zircon.editor.native_window.window:editor.material_editor_window#1` | `cargo test -p zircon_editor --lib native_window_hosts_allocate_independent_surfaces_per_floating_window ...` 通过 1/0/2022；`cargo test -p zircon_editor --lib window_host_manager ...` 通过 3/0/2020；`cargo test -p zircon_editor --lib opening_functional_editor_window_creates_instance_scoped_floating_window ...` 通过 1/0/2022；`cargo test -p zircon_editor --lib floating_window ...` 通过 47/0/1976；`rustfmt --edition 2021 --check` 覆盖 M5.S1 touched Rust 通过；`git diff --check` 通过（仅既有 LF/CRLF 提示）；冲突标记与尾随空白扫描通过；当前无 cargo/rustc 残留进程 | 真实 child-window 输入/渲染泵仍由 retained `UiHostWindow` 承载；后续 full runtime `UiSurface` hard cutover 必须把浮动子窗口输入、layout、render extract 路由到这些 window-owned surfaces；`floating_window_projection.rs` 物理删除要等 M5.S1 完整原生窗口迁移闭环 |
| 2026-06-16 | 08 M5.S2 project workspace restore diagnostics + fallback | 完成（代码、测试、文档、focused Cargo 通过） | `.zircon/editor-workspace.json` 现在作为可恢复布局源读取：`EditorProjectDocument` 新增 `workspace_restore_diagnostics`，workspace JSON 损坏、不可读或 `format_version` 不支持时不再阻断项目/场景打开，而是置空 `editor_workspace` 并记录诊断；`open_project_and_remember(...)` 在启动状态中报告“Project opened with default layout”，随后 `apply_project_workspace(None)` 继续走全局默认布局/default workbench 链；同时修正 M5.S1 已入基线的 native target `Option::filter_map` 编译错误与 floating-window projection 测试缺少 `UiTreeId` 导入 | `cargo test -p zircon_editor --lib project_open_with_corrupt_workspace_falls_back_to_global_layout_with_diagnostic ...` 通过 1/0/2023；`editor_project_document_ignores_unknown_workspace_format_with_diagnostic` 通过 1/0/2024；`editor_project_document_roundtrips_world_and_workspace` 通过 1/0/2024；`explicit_project_open_session_bypasses_component_showcase_builtin_view` 通过 1/0/2023；`save_and_load_preset_roundtrip_through_manager_commands` 和 `save_and_load_preset_roundtrip_through_project_asset_files` 均通过 1/0/2023；`native_floating_window_targets_fall_back_to_shared_projection_when_host_bounds_are_empty` 通过 1/0/2023；`build_floating_window_projection_bundle_prefers_native_host_bounds_and_splits_frames` 通过 1/0/2023；warning 噪声为既有未清理 warning | 仍需把自动“最近项目启动恢复”策略接到 `resolve_startup_session(...)`，并做真实重启实机验收；full runtime `UiSurface` hard cutover pump 仍待后续 |
| 2026-06-16 | 08 M5.S2 recent-project startup restore policy | 完成（代码、测试、文档、focused Cargo 通过） | `resolve_startup_session(...)` 现在读取 `StoredStartupSession.last_project_path`，有效时自动 `open_project(...)` 并返回 `Project` 启动会话；无记录、缺失、无效或打开失败时回退到 `editor.ui_component_showcase`，同时保留带校验状态的 recent-project 列表和可见状态文案；workspace 诊断在自动恢复路径中显示为 “Restored recent project with default layout”，继续让 `apply_project_workspace(None)` 使用全局默认布局/default workbench 链 | `rustfmt --edition 2021 --check zircon_editor/src/ui/host/startup/resolve_session.rs zircon_editor/src/tests/host/manager/bootstrap_and_startup.rs` 通过；`cargo test -p zircon_editor --lib startup_session --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615 ...` 通过 2/0/2024；`create_project_and_open_persists_recent_project_and_returns_project_session` 通过 1/0/2025；`explicit_project_open_session_bypasses_component_showcase_builtin_view` 通过 1/0/2025；warning 噪声为既有未清理 warning | 仍需真实重启实机验收；full runtime `UiSurface` hard cutover pump 仍待后续；M5.S2 代码链已覆盖 preset/project workspace/global default/builtin fallback 的自动启动入口 |
| 2026-06-16 | 08 M6.S1 context-menu provider route | 完成（代码、测试、文档、focused Cargo 通过） | Workbench secondary press now routes through the retained host-contract hit path: `workbench_context_menu_request_for_hit(...)` classifies scene rows, module rows, and generic Workbench controls, excludes existing popup rows, and emits `WorkbenchContextMenuRequestData` with target metadata, semantic path, pointer anchor, and provider menu rows. `RetainedEditorHost::dispatch_workbench_context_menu_requested(...)` opens the hidden root `WorkbenchContextMenu` in `workbench_window.zui` through the template bridge, which writes popup visibility, target/path, anchor, and structured menu rows while reusing shared popup cancel cleanup. A lower-layer native-plugin namespace compile repair was required during validation: editor/runtime call sites now import native loader/live-host types from `zircon_runtime::plugin::native` instead of stale root paths, and `plugin::native` no longer re-exports removed V1/V2 entry ABI symbols. | `cargo test -p zircon_editor --lib workbench_context_menu_open_state_populates_visible_overlay --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615 ...` passed 1/0/2029 after tightening the close-state assertion to the observable popup state; `cargo test -p zircon_editor --lib workbench_context_menu --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615 ...` passed 3/0/2027; `cargo test -p zircon_editor --lib native_workbench_secondary_press_requests_scene_context_menu --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-command-registry-0615 ...` passed 1/0/2029 after moving the test pointer to the scene row's uncovered right side. Existing warning noise remains in `zircon_runtime`/`zircon_editor`. | Live/manual right-click acceptance in the real editor window still needs to be captured; later full runtime `UiSurface` hard-cutover must route the same provider contract through the final shell surface owner. |
| 2026-06-16 | 08 M6.S2 toast trigger + NotificationCenter history | 完成（代码、测试、文档、focused Cargo 通过） | 新增 retained-host `WorkbenchNotification` DTO 与 record/error/import 结果映射；`UiHostEventEffects` 携带并合并 `workbench_notifications`；Workbench window template 挂载 `WorkbenchToast` 与 closed-but-projectable `WorkbenchNotificationCenter` overlay；`BuiltinWorkbenchWindowTemplateSurfaceBridge::push_workbench_notifications(...)` 写入 runtime 可解析 `toast_queue` 与 `notifications` 历史；app side-effect 链在 editor event、import 成功/失败、dispatch error 中发布 toast/history；Workbench projection 复用 NotificationCenter row projection 以导出 native structured rows。 | `cargo test -p zircon_editor --lib toast --locked` 通过 6/0/2026；`workbench_toast_queue_and_notification_history_receive_editor_notifications` 通过 1/0/2031；`save_and_import_records_queue_workbench_notifications_for_retained_host` 通过 1/0/2031；`cargo test -p zircon_editor --lib workbench_notifications --locked` 通过 1/0/2031；`cargo test -p zircon_editor --lib notification_center --locked` 通过 3/0/2029；warning 噪声为既有未清理 warning。 | Live/manual save/import/error toast acceptance in the real editor window still needs capture; later full runtime `UiSurface` hard-cutover must keep this retained notification contract routed through final shell surface owner. |
| 2026-06-16 | 08 M6.S3 status bar realtime task slot | 完成（代码、测试、文档、focused Cargo 通过） | 新增 `StatusTaskProgressSnapshot` 并贯通 `EditorState`、`EditorDataSnapshot`、`EditorChromeSnapshot`、`StatusBarModel` 与 Workbench status bar template bridge；状态栏从 chrome 同步 ready/errors/warnings/messages/grid/snap/zoom，新增可折叠 `WorkbenchStatusTaskProgress`、`WorkbenchStatusTaskLabel`、`WorkbenchStatusTaskBar`；桌面导出队列和运行中 job 的阶段/百分比映射为状态栏任务进度；补充 `status_bar.md` 模块文档与 focused bridge/view-model/export tests。 | `cargo fmt -p zircon_editor -- --check` 通过；`cargo test -p zircon_editor --lib status_bar --locked -- --nocapture` 通过 5/0/2031；`cargo test -p zircon_editor --lib workbench_view_model_projects_status_task_progress_slot --locked -- --nocapture` 通过 1/0/2035；`cargo test -p zircon_editor --lib desktop_export_job_snapshot_projects_status_bar_task_progress --locked -- --nocapture` 通过 1/0/2035；warning 噪声为既有未清理 warning。 | Live/manual desktop export progress acceptance in the real editor window still needs capture; later full runtime `UiSurface` hard-cutover must keep the same chrome-to-status-bar binding routed through final shell surface owner. |
| 2026-06-16 | 08 M1.S3 status bar legacy painter cutover | 完成（代码、测试、文档、focused Cargo 通过） | componentized Workbench 存在时旧 root skeleton 不再绘制 status bar 背板或旧 `status_secondary` 文本标记，状态栏像素来源收束到 `WorkbenchStatusBar` 模板节点；新增 command-stream 回归测试锁定 legacy `STATUS_BAR` quad 不再出现，保留非 componentized retained fallback。 | `cargo test -p zircon_editor --lib componentized_workbench_status_bar_skips_legacy_skeleton_fill --locked -- --nocapture` 通过 1/0/2036；`cargo test -p zircon_editor --lib status_bar --locked -- --nocapture` 通过 6/0/2031；`cargo fmt -p zircon_editor -- --check` 通过；warning 噪声为既有未清理 warning。 | activity rail/main tabs/drawers/root shell 旧 skeleton 与 pointer bridge 仍需后续按区域硬切；full runtime `UiSurface` hard-cutover 和 real-window capture 仍待。 |
| 2026-06-16 | 08 M2.S1 activity rail legacy painter cutover baseline | 完成（代码、测试、文档、focused Cargo 通过；pointer bridge 未删除） | componentized Workbench 暴露 `ActivityRailRoot`/`WorkbenchWindowActivityRail`/`WorkbenchMainBandActivityRail` 时，旧 root skeleton 的左侧 `SIDE_PANEL` 背景会避开 activity rail 矩形，rail 像素来源收束到 `WorkbenchActivityRail` 模板节点；新增 command-stream 回归测试先红后绿，锁定旧 left-region fill 不再覆盖组件化 rail，同时保留左 drawer 区域旧 fallback。 | `cargo test -p zircon_editor --lib componentized_workbench_activity_rail_skips_legacy_left_region_fill --locked -- --nocapture` 首轮按预期失败后复跑通过 1/0/2037；`cargo test -p zircon_editor --lib activity_rail --locked -- --nocapture` 通过 15/0/2023；`cargo test -p zircon_editor --lib componentized_workbench_status_bar_skips_legacy_skeleton_fill --locked -- --nocapture` 通过 1/0/2037；`cargo test -p zircon_editor --lib status_bar --locked -- --nocapture` 通过 6/0/2032；`cargo fmt -p zircon_editor -- --check` 通过；warning 噪声为既有未清理 warning。 | `activity_rail_pointer` 桥仍需与 01 M5 输入路由协同删除；main tabs/drawers/root shell 旧 skeleton 仍需按区域硬切；real-window activity rail capture 与 full runtime `UiSurface` hard-cutover 仍待。 |
| 2026-06-16 | 08 M2.S2 main band center skeleton cutover baseline | 完成（代码、测试、文档、focused Cargo 通过；tab/pointer bridge 未删除） | componentized Workbench 暴露 `WorkbenchWindowMainBandRegion`/`WorkbenchMainBand` 时，旧 root skeleton 的 `CENTER_BAND` 背景会避开 main band 矩形，主带中心底色来源开始收束到 runtime Workbench 模板；新增 command-stream 回归测试锁定旧 center-band fill 不再覆盖组件化 main band，同时保留 document/viewport/drawer/tab-drag 与 pointer fallback 等待后续硬切。 | `cargo test -p zircon_editor --lib componentized_workbench_main_band_skips_legacy_center_band_fill --locked -- --nocapture` 首轮因并行 Cargo 锁 124s 超时，复跑在修复前按预期失败 0/1/2038，修复后通过 1/0/2038；`cargo test -p zircon_editor --lib document_tab --locked -- --nocapture` 通过 17/0/2022；`cargo test -p zircon_editor --lib activity_rail --locked -- --nocapture` 通过 15/0/2024；`cargo test -p zircon_editor --lib status_bar --locked -- --nocapture` 通过 6/0/2033；`cargo fmt -p zircon_editor -- --check` 通过；warning 噪声为既有未清理 warning。 | `document_tab_pointer`、tab drag 命中态和 main tabs runtime 交互仍需后续协同删除；document/viewport/drawer/root shell 旧 skeleton 仍需按区域硬切；real-window main-band capture 与 full runtime `UiSurface` hard-cutover 仍待。 |
| 2026-06-16 | 08 M2.S2 document tabs + Workbench component projection baseline | 完成（代码、测试、文档、focused Cargo 通过；tab/pointer bridge 未删除） | `workbench_viewport_panel.zui` 在 viewport toolbar/surface 前挂载 `DocumentTabsRoot` 并暴露 activate/close dock routes；runtime 旧模板编译器与 v2 instancer 统一支持实例 props/style 覆盖 expanded root，surface-backed retained projection 把 `style_overrides` 合并为有效属性；`WorkbenchTabStrip` 声明 default slot 并转发 Labs tab 子节点，component drawer 补齐 `WorkbenchInputDisabled`，Labs tab routes 对齐 `component_lab.labs_tab_*.select`；WorkBench projection 辅助读取同一 control 的首个非空背景/边框命令，避免 Group owner command 掩盖 TableRow/ListRow 渲染。 | `cargo test -p zircon_runtime --lib ui_document_compiler_applies_reference_instance_props_to_expanded_root --locked -- --nocapture` 通过；`prototype_compiler_applies_reference_instance_props_to_expanded_root` 通过；`ui_document_compiler_applies_reference_instance_style_overrides_after_stylesheets` 通过；`prototype_compiler_applies_reference_instance_style_overrides_after_stylesheets` 通过；`ui_v2_imported_component_instance_style_patches_expanded_root` 通过 1/0/4279；`surface_backed_retained_projection_exposes_style_overrides_as_effective_properties` 通过 1/0/2040；`ui_v2_composite_component_forwards_default_slot_children` 首轮 124s 超时但后台完成，长窗口复跑通过 1/0/4284；`componentized_workbench_window_template_bridge_exports_surface_projection_frames_and_routes` 通过 1/0/2040；`componentized_workbench_window_template_bridge_exposes_document_tab_runtime_routes` 通过 1/0/2040；warning 噪声为既有未清理 warning。 | `document_tab_pointer`、tab drag 命中态、drawer resize/bridge 和 root shell 旧 skeleton 仍需后续协同删除；real-window document-tab/Labs-tab capture 与 full runtime `UiSurface` hard-cutover 仍待。 |
| 2026-06-16 | 08 M2.S3 component drawer bottom skeleton cutover baseline | 完成（代码、测试、文档、focused Cargo 通过；drawer resize/bridge 未删除） | componentized Workbench 暴露 `WorkbenchWindowComponentDrawerRegion`/`WorkbenchComponentDrawer` 时，旧 root skeleton 的底部 `SIDE_PANEL` 背景会避开 component drawer 矩形，component drawer 像素来源开始收束到 runtime `WorkbenchComponentDrawer` 模板；新增 command-stream 回归测试锁定旧 bottom-region fill 不再覆盖组件化 drawer，同时修复 lower-layer post-process `upscale_pipeline.rs` shader source 导入路径以解除 editor focused test 编译阻断。 | 首次 focused Cargo 被 `upscale_pipeline.rs` 误导入不存在的 `post_process::shader_sources` 阻断，已改为同级 pipeline builder 使用的 `resources::shader_sources` 相对所有权路径；`cargo test -p zircon_editor --lib componentized_workbench_component_drawer_skips_legacy_bottom_region_fill --locked -- --nocapture` 通过 1/0/2041；`cargo test -p zircon_runtime --lib upscale_shader_parses --locked -- --nocapture` 通过 1/0/4289；`cargo fmt` 通过；scoped `git diff --check` 仅报告既有 LF/CRLF 提示，冲突标记扫描为空；warning 噪声为既有未清理 warning。 | `LeftDrawerShellRoot`/`RightDrawerShellRoot`/`BottomDrawerShellRoot` host shell、drawer resize/header pointer bridge 和 root shell 旧 skeleton 仍需后续硬切；`workbench.rs` 已超过模块化阈值，后续 skeleton/region guard 逻辑应拆出专门模块；real-window drawer capture 与 full runtime `UiSurface` hard-cutover 仍待。 |
| 2026-06-16 | 08 M2.S3 Workbench skeleton region guard extraction | 完成（代码、文档、scoped 检查通过；行为保持不变） | 新增 `workbench_skeleton_regions.rs`，把 componentized Workbench 的 activity rail/main band/component drawer control-id 识别、visible frame 过滤、legacy rect exclusion helper 和 status-bar fallback 标记从 2170+ 行 `workbench.rs` 拆出；`workbench.rs` 保留 root skeleton 绘制顺序与 fallback orchestration，后续 drawer/root-shell hard-cutover 不再继续在巨型 painter 文件中堆区域 guard。 | `cargo fmt` 通过；`rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件通过；`cargo test -p zircon_editor --lib skips_legacy --locked -- --nocapture --test-threads=1` 通过 4/0/2038；scoped `git diff --check` 仅报告既有 LF/CRLF 提示，冲突标记扫描为空；计划执行时复核 `workbench.rs` 从 2267 行降到 2174 行，新模块 115 行。较宽的 `componentized_workbench_` 过滤暴露既有 Workbench projection/input 失败（例如 `apply_presentation_carries_componentized_workbench_window_nodes_separately`、`componentized_workbench_pointer_focuses_input_fields_without_authored_binding`），不在本 painter guard 移动路径内，转入后续 08 收敛切片。 | 本切片不删除 `drawer_header_pointer`/`drawer_resize`/`LeftDrawerShellRoot` 等宿主 drawer bridge；下一步仍需把 drawer header/resize/input 路径接到 componentized Workbench surface 后再物理删除旧桥接和 retained drawer shell source，同时处理宽过滤暴露的 Workbench projection/input 漂移。 |
| 2026-06-16 | 08 M2.S3 Workbench projection/input convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | Workbench pointer bridge 现在把 primary press 导致的 runtime focus 变化转为 paint-only feedback，不再要求输入框带 authored activation binding；`visible_control_frame(...)` 改用 arranged frame 与有效 clip 的交集，避免 clipped controls 的 synthetic pointer center 落到不可命中区域；组件抽屉 demo popup 声明 `disable_enforce_focus = true`，防止常开展示菜单抢占兄弟输入控件焦点；WorkBench projection 测试同步 action/binding identity 分离，Primary 按钮 action id 固定为 `component_lab.button.primary`，binding id 保留 `ComponentLab/...` 源信息。 | 已完成必要聚焦验证：`componentized_workbench_pointer_focuses_input_fields_without_authored_binding` 通过 1/0/2041，`apply_presentation_carries_componentized_workbench_window_nodes_separately` 通过 1/0/2041；`rustfmt` 已覆盖 touched Rust。按用户“先实现功能，测试暂时延后”要求，暂不扩展完整 `componentized_workbench_`/全量 Cargo 矩阵。 | 继续实现 drawer header/resize/runtime surface 路由硬切；后续测试窗口补跑 broader `componentized_workbench_`、drawer-focused 和 real-window capture。 |
| 2026-06-16 | 08 M2.S3 Workbench drawer projection baseline | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench_window.zui` 在组件化 Workbench 根 Overlay 内挂载透明 drawer projection source，暴露 `LeftDrawerShellRoot`、`RightDrawerShellRoot`、`BottomDrawerShellRoot` 及 header/content control frames；新增组件化窗口桥 `recompute_layout_with_workbench_model(...)`，从 top toolbar/status bar frame 推导 body anchors，并复用旧 `workbench_drawer_source::layout` 把 `WorkbenchViewModel` 中的左/右/底 drawer extents 写入 runtime `UiSurface`；宿主 dirty layout 刷新阶段改为调用 model-aware componentized Workbench recompute。 | `rustfmt --edition 2021` 覆盖 touched Rust 通过；Python `tomllib` 解析 `workbench_window.zui` 并确认 drawer projection 节点通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 unused warning 噪声。按用户“先实现功能，测试暂时延后”要求，未扩展完整 drawer/componentized/full Cargo 测试矩阵。 | 下一切片把 drawer header pointer 与 drawer resize 命中/状态更新改为优先消费 componentized Workbench surface frames，再删除旧 retained drawer source/bridge 残余；后续补跑 drawer-focused、broader `componentized_workbench_` 和 real-window capture。 |
| 2026-06-16 | 08 M2.S3 Workbench drawer header/resize frame routing | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `merge_drawer_frames_into_root_shell_frames(...)`，保留旧宿主 root shell 的 document/menu/body/status 等非 drawer frame，但用 componentized Workbench surface 的 drawer shell/header/content frame 覆盖旧 drawer source frame；覆盖逻辑把 collapsed/不可见 drawer 的 `None` 当成最终结果，避免 stale legacy frame 复活隐藏 splitters；宿主 dirty layout 重算现在把 merged frames 传给 `HostShellPointerBridge::update_layout_with_root_shell_frames(...)` 与 drawer-header pointer layout，使 resize hitbox 和 drawer header 点击区域优先消费 runtime Workbench surface frame。 | `rustfmt --edition 2021 --check` 覆盖 touched Rust 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声；新增 focused 断言 `componentized_workbench_drawer_frames_override_legacy_root_shell_frames` 已写入，但本地 `cargo test -p zircon_editor --lib componentized_workbench_drawer_frames_override_legacy_root_shell_frames --locked -- --nocapture --test-threads=1` 在 180s 编译窗口超时，已停止残留 cargo/rustc 进程。按用户要求未继续扩展完整测试矩阵。 | 下一步删除或旁路旧 retained drawer source/bridge 的实际 frame 依赖，补跑 drawer header/resize focused tests、broader `componentized_workbench_`，并做 real-window drawer resize/header capture。 |
| 2026-06-16 | 08 M2.S3 Workbench drawer consumer frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 host 级 `root_shell_frames_with_componentized_drawers()`，把旧 root shell frame 与 componentized Workbench drawer frame overlay 的关系集中到 `RetainedEditorHost`；drawer-backed callback surface 尺寸、activity rail pointer strip、tab drag/drop 的 drawer strip 解析、drawer resize 捕获基准和 drawer host viewport toolbar 宽度改为消费同一套 componentized drawer frames；document/menu/host-page/document-tab 消费仍保留旧 root frames，等待各自区域切片。 | `rustfmt --edition 2021 --check` 覆盖本切片 Rust 文件通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused drawer/full Cargo 测试矩阵。 | 继续削减旧 retained drawer source 的实际消费者；补跑 drawer resize/header/drop focused tests、broader `componentized_workbench_` 和 real-window drawer capture 后再物理删除旧 bridge/source。 |
| 2026-06-16 | 08 M2.S3 Workbench legacy drawer-source bypass | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 生产 `BuiltinHostWindowTemplateBridge` 不再持有、构建或重算独立 `BuiltinHostDrawerSourceTemplateBridge`，`control_frame(...)` 只读取 host shell projection；startup built-in runtime 不再加载 `workbench.drawer_source`；旧 drawer-source bridge、source-frame extractor、文档 id 和独立 surface 构建函数收窄为 `#[cfg(test)]`，生产路径仅保留可复用的 `workbench_drawer_source::layout` 给 componentized Workbench `UiSurface` 直接写入 drawer constraints。 | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声，且本切片移除了旧 drawer-source 残余 warning。按用户“先实现功能，测试暂时延后”要求，不扩展 focused drawer/full Cargo 测试矩阵。 | 后续测试窗口补跑 drawer resize/header/drop focused tests、broader `componentized_workbench_` 和 real-window drawer capture；下一步按区域处理 document/menu/host-page/document-tab 的旧 root-shell frame 与 legacy skeleton 删除。 |
| 2026-06-16 | 08 M2.S3 Workbench drawer projection frame-only paint guard | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench_window.zui` 的 drawer projection 子树 26 个节点全部声明 `surface_variant = "frame_only"`；retained `template_nodes.rs` 在通用模板绘制入口把 `frame_only` token 作为 no-paint 契约提前返回，防止 drawer projection source 因未来样式/背景 token 成为第二套 drawer 像素来源；projection 仍保留 arranged frames 和命中框，供 drawer header/resize 路由继续消费。 | `rustfmt --edition 2021 --check` 覆盖 `template_nodes.rs` 通过；Python `tomllib` 解析 `workbench_window.zui` 并确认 26 个 drawer projection 节点无缺失 `frame_only` 标记；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。 | 后续仍需按区域把真实 drawer 像素从 legacy skeleton 迁到 componentized Workbench drawer 区域，再补 drawer resize/header/drop focused tests、broader `componentized_workbench_` 和 real-window drawer capture。 |
| 2026-06-16 | 08 M2.S3 Workbench document tabs frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowTemplateSurfaceBridge::document_tabs_frame()` 暴露 componentized Workbench surface 的可见 `DocumentTabsRoot` frame；`RetainedEditorHost::sync_document_tab_pointer_layout(...)` 通过新增 `build_host_document_tab_pointer_layout_with_root_strip_frame(...)` 让主文档标签栏 hitbox 优先消费该 runtime Workbench frame，旧 root-shell resolver 仅保留 fallback；浮动窗口 tab strip 仍由 `FloatingWindowProjectionBundle` 提供，等待多窗口 surface 切片。 | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；冲突标记扫描为空；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 document-tab focused/full Cargo 测试矩阵。 | 后续继续按区域处理 host-page/menu/document viewport/tab drag 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 document-tab focused tests、broader `componentized_workbench_` 与 real-window 标签栏 capture。 |
| 2026-06-16 | 08 M2.S3 Workbench viewport toolbar width frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `EditorWorkbenchTemplateControlIds::VIEWPORT_TOOLBAR` 固化 `WorkbenchViewportToolbar` control id，`BuiltinWorkbenchWindowTemplateSurfaceBridge::viewport_toolbar_frame()` 暴露 componentized Workbench 的可见 toolbar frame；主文档 `viewport_toolbar_surface_size(...)` 和 `attach_viewport_toolbar_surface_frames_to_ui(...)` 现在优先用该 frame 宽度，旧 root-shell document/pane width 仅作为 fallback；drawer-host toolbar 宽度继续消费 componentized drawer overlay，floating-window toolbar 宽度仍走现有 floating-window content frame，`SceneViewportToolbarRoot` 28px 高度契约不变。 | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；冲突标记扫描为空；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 viewport-toolbar focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu/tab-drag 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 viewport-toolbar/document-tab focused tests、broader `componentized_workbench_` 与 real-window viewport toolbar capture。 |
| 2026-06-16 | 08 M2.S3 Workbench viewport content frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `EditorWorkbenchTemplateControlIds::VIEWPORT_SURFACE` 固化 `WorkbenchViewportSurface` control id，`BuiltinWorkbenchWindowTemplateSurfaceBridge::viewport_surface_frame()` 暴露 componentized Workbench 的可见 content frame；主文档 viewport resize、viewport pointer bounds 和 `HostWindowLayoutData.viewport_content_frame` 现在优先消费同一个 runtime Workbench surface frame，旧 `resolve_root_viewport_content_frame(...)` 仅作 fallback；host-page strip、menu pointer 与 floating-window viewport content 保持各自旧语义，等待独立 hard-cutover。 | `rustfmt --edition 2021` 覆盖本切片 touched Rust 通过；冲突标记扫描为空；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 viewport/document-tab/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu/tab-drag 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 viewport focused tests、broader `componentized_workbench_` 与 real-window viewport content capture。 |
| 2026-06-16 | 08 M2.S3 Workbench scene/game callback surface size cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::resolve_host_frame_backed_size_for_kind(...)` 对 active main-page `Scene`/`Game` callback surface 尺寸优先使用 `BuiltinWorkbenchWindowTemplateSurfaceBridge::viewport_surface_frame()`，让 callback surface 回退尺寸与主文档 viewport resize、pointer bounds、`HostWindowLayoutData.viewport_content_frame` 共享同一个 `WorkbenchViewportSurface`；非视口主文档内容、drawer callback source 与 floating-window source 保持既有 frame owner。 | `rustfmt --edition 2021` 覆盖 `helpers.rs` 通过；完整 scoped 检查随后与本轮 touched 文件一起执行。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu/tab-drag 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 callback-surface/viewport focused tests 与 real-window viewport capture。 |
| 2026-06-16 | 08 M2.S3 Workbench activity rail pointer frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowTemplateSurfaceBridge::activity_rail_frame()` 暴露 componentized Workbench 的 `WorkbenchMainBandActivityRail` 可见 frame；生产 `RetainedEditorHost::sync_activity_rail_pointer_layout(...)` 通过 `build_host_activity_rail_pointer_layout_with_left_strip_frame(...)` 让左侧主 activity rail hit strip 优先消费该 runtime frame，旧 `resolve_root_activity_rail_frame(...)` 仅保留 fallback；旧三参数 builder 收窄为 test-only，右侧 drawer rail 仍走 drawer/root-shell frame owner。 | `rustfmt --edition 2021` 覆盖 touched Rust 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 activity-rail focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu/tab-drag 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 activity-rail pointer focused tests 与 real-window activity rail capture。 |
| 2026-06-16 | 08 M2.S3 Workbench document tab drag frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 生产 `RetainedEditorHost::dispatch_drag_drop_from_pointer(...)` 改为调用 `resolve_host_tab_drop_route_with_document_tabs_frame(...)` 并传入 `BuiltinWorkbenchWindowTemplateSurfaceBridge::document_tabs_frame()`；tab drag 的 route/drop/strip hitbox 链路现在让主文档标签插入命中优先消费 componentized Workbench 的 `DocumentTabsRoot` 可见 frame，旧 root-shell document bounds 仅保留 fallback；旧 root-frame route/drop/precise helper 收窄为 test-only，drawer 与 floating-window tab strip owner 保持原路径。 | `rustfmt --edition 2021` 覆盖本切片 touched Rust 通过；冲突标记扫描为空；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 tab-drag focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 tab-drag focused tests、broader `componentized_workbench_` 与 real-window tab drag capture。 |
| 2026-06-16 | 08 M2.S3 Workbench shell drag document target frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowTemplateSurfaceBridge::document_workspace_frame()` 暴露 componentized Workbench 的 `WorkbenchMainBandViewportPanel` 可见 frame；生产 `RetainedEditorHost::recompute_if_dirty(...)` 通过 `HostShellPointerBridge::update_layout_with_document_workspace_frame(...)` 把该 frame 传入 shell drag surface；`build_drag_surface(...)` 现在让 root document drop target 与 document-edge split target 优先消费 componentized document workspace frame，旧 `document_host_frame`/`resolve_root_document_region_frame(...)` 仅保留 fallback；left/right/bottom drawer drop targets 与 floating-window targets 保持原 owner。 | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；冲突标记扫描为空；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 shell-pointer/tab-drag/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu 的旧 root-shell frame 消费和 legacy skeleton 删除；补跑 shell-pointer/tab-drag focused tests、broader `componentized_workbench_` 与 real-window drag target capture。 |
| 2026-06-16 | 08 M2.S3 Workbench viewport skeleton paint guard | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench_skeleton_regions.rs` 新增 componentized document workspace 与 viewport surface guard，识别 `WorkbenchMainBandViewportPanel`/`WorkbenchViewportPanel` 和 `WorkbenchViewportSurface`；`draw_root_skeleton(...)` 现在把旧 `DOCUMENT_PANEL`/`VIEWPORT_PANEL` 填充切到这些 componentized frame 外侧，并在对应 componentized owner 存在时停止绘制旧 document border、viewport border 和 viewport label marker，避免旧 root skeleton 覆盖 runtime Workbench 文档/视口像素。 | `rustfmt --edition 2021 --check` 覆盖 `workbench.rs` 与 `workbench_skeleton_regions.rs` 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu 的旧 root-shell frame 消费和 remaining legacy skeleton 删除；补跑 skeleton guard focused tests、broader `componentized_workbench_` 与 real-window viewport capture。 |
| 2026-06-16 | 08 M2.S3 Workbench side-panel skeleton paint guard | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench_skeleton_regions.rs` 新增 scene tree 与 inspector panel guard，识别 `WorkbenchMainBandSceneTreePanel`/`WorkbenchSceneTreePanel` 和 `WorkbenchMainBandInspectorPanel`/`WorkbenchInspectorPanel`；`draw_root_skeleton(...)` 现在把旧 left/right `SIDE_PANEL` 填充切到这些 componentized frame 外侧，并在对应 runtime owner 存在时停止绘制旧 left/right root border；左侧 activity rail 与 scene tree 共用旧 left-region fallback，所以新增 `draw_rect_excluding_many(...)` 避免多个已迁移 frame 互相被旧 segment 覆盖。 | `rustfmt --edition 2021 --check` 覆盖 `workbench.rs` 与 `workbench_skeleton_regions.rs` 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 top toolbar、host-page/menu 的旧 root-shell frame 消费和 remaining legacy skeleton 删除；补跑 side-panel skeleton focused tests、broader `componentized_workbench_` 与 real-window scene tree/inspector capture。 |
| 2026-06-16 | 08 M2.S3 Workbench top-toolbar skeleton paint guard | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench_skeleton_regions.rs` 新增 top toolbar guard，识别 `WorkbenchWindowTopToolbarRegion`/`WorkbenchWindowTopToolbar`；`draw_root_skeleton(...)` 现在把旧 `TOP_BAR` 填充切到 componentized toolbar frame 外侧，并在 runtime toolbar owner 存在时停止绘制旧 project marker、debug refresh-rate marker 和 top-bar separator；toolbar popup overlay 与菜单语义保持 componentized/command 后续切片 owner，本切片只收束 legacy painter 像素。 | `rustfmt --edition 2021 --check` 覆盖 `workbench.rs` 与 `workbench_skeleton_regions.rs` 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu 的旧 root-shell frame 消费和 remaining legacy skeleton 删除；补跑 top-toolbar skeleton focused tests、broader `componentized_workbench_` 与 real-window toolbar capture。 |
| 2026-06-16 | 08 M2.S3 Workbench bottom-drawer skeleton border guard | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `draw_root_skeleton(...)` 在 `component_drawer_frame` 存在时不再绘制旧 `bottom_region` border；旧 bottom `SIDE_PANEL` fill 已经通过 `draw_rect_excluding(...)` 避开 `WorkbenchWindowComponentDrawerRegion`/`WorkbenchComponentDrawer`，本切片补齐边框层 single-owner 规则，让 componentized drawer 独占自己的 chrome/header/content/resize 像素。 | `rustfmt --edition 2021 --check` 覆盖 `workbench.rs` 与 `workbench_skeleton_regions.rs` 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 host-page/menu 的旧 root-shell frame 消费和 remaining legacy skeleton 删除；补跑 bottom-drawer skeleton focused tests、broader `componentized_workbench_` 与 real-window drawer capture。 |
| 2026-06-16 | 08 M2.S3 Workbench document callback workspace fallback | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::resolve_host_frame_backed_size_for_kind(...)` 现在区分 Workbench 主页面与 Exclusive 页面：active Workbench document tab 的 callback surface fallback 优先使用 `BuiltinWorkbenchWindowTemplateSurfaceBridge::document_workspace_frame()` 暴露的 `WorkbenchMainBandViewportPanel` 尺寸；Scene/Game 仍优先 `WorkbenchViewportSurface`；旧 `pane_surface_frame`/`document_host_frame` 只保留 fallback。drawer callback source、floating-window source 与 Exclusive page 继续走原 owner，避免误把外层 host shell 页面迁入 Workbench window。 | `rustfmt --edition 2021 --check` 覆盖 `helpers.rs` 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 remaining legacy root-shell frame fallback 的删除条件、host-page/menu 的外层 shell owner 验证，以及补跑 callback-surface focused tests、broader `componentized_workbench_` 与 real-window document workspace capture。 |
| 2026-06-16 | 08 M2.S3 Workbench status-bar layout frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowTemplateSurfaceBridge::layout_frames()` 通过 `BuiltinWorkbenchWindowLayoutFrames.status_bar_frame` 暴露 componentized Workbench 的 `WorkbenchWindowStatusBarRegion` 可见 frame；`apply_presentation(...)` 与 `host_window_layout(...)` 现在优先使用该 frame 生成 `HostWindowLayoutData.status_bar_frame`，旧 `StatusBarRoot` 只保留 fallback；主窗口 native damage/menu bounds/status layout 与 runtime Workbench 状态栏 owner 对齐，status 文案/chip/task-slot 数据同步仍走已有 status bar bridge，native floating-window 调用继续传空保持原 owner。 | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声；按用户“先实现功能，测试暂时延后”要求，不扩展 status-bar focused/full Cargo 测试矩阵。 | 后续继续处理 remaining legacy root-shell frame fallback 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 status-bar layout/damage focused tests、broader `componentized_workbench_` 与 real-window status-bar capture。 |
| 2026-06-16 | 08 M2.S3 Workbench main layout frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `BuiltinWorkbenchWindowLayoutFrames` 和 `BuiltinWorkbenchWindowTemplateSurfaceBridge::layout_frames()`，把 componentized Workbench 的 `WorkbenchWindowMainBandRegion`、activity rail + scene tree 合并左区、`WorkbenchMainBandViewportPanel`、`WorkbenchMainBandInspectorPanel`、`WorkbenchWindowComponentDrawerRegion`、`WorkbenchWindowStatusBarRegion`、`WorkbenchViewportSurface` 作为单一 layout frame contract；`apply_presentation(...)`/`host_window_layout(...)` 优先消费该结构生成 `HostWindowLayoutData` 的 center/left/document/right/bottom/status/viewport frames，旧 root-shell frames 仅作 fallback；host-page/menu/root overlay、drawer header/resize frame-only projection 与 floating-window 仍保留原 owner。 | `rustfmt --edition 2021 --check` 覆盖本切片 touched Rust 通过；冲突标记扫描为空；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 remaining legacy root-shell frame fallback 删除条件、drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 layout/damage/menu-bound focused tests、broader `componentized_workbench_` 与 real-window full-shell capture。 |
| 2026-06-16 | 08 M2.S3 Workbench viewport toolbar fallback frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::viewport_toolbar_surface_size(...)` 新增 `componentized_document_viewport_toolbar_width()`，让主文档 viewport toolbar 尺寸在普通 Document 路径和无 current instance 的 startup fallback 路径都先使用 `WorkbenchViewportToolbar`，再使用 `BuiltinWorkbenchWindowLayoutFrames.viewport_content_frame`，最后才退回 legacy root-shell document/viewport content frame；drawer、ExclusivePage 与 floating-window toolbar owner 保持原路径。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/app/viewport.rs` 通过；冲突标记扫描为空；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 viewport-toolbar focused/full Cargo 测试矩阵。 | 后续继续处理 remaining legacy root-shell frame fallback 删除条件、drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 viewport-toolbar focused tests 与 real-window toolbar capture。 |
| 2026-06-16 | 08 M2.S3 Workbench shell drag layout frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `HostShellPointerBridge` 的生产入口改为 `update_layout_with_workbench_layout_frames(...)`，并把 `BuiltinWorkbenchWindowLayoutFrames` 传入 `build_drag_surface(...)`；root shell 拖拽命中面现在优先使用 componentized Workbench 的 center band、status bar、document workspace、left-region union、right/inspector region、bottom/component-drawer region 来计算 document drop、document-edge split 与 left/right/bottom drop extents，旧 root-shell resolvers 仅作 fallback；resize splitter、drawer header/resize、host-page/menu overlay 与 floating-window owner 保持原路径。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/shell_pointer/bridge.rs zircon_editor/src/ui/retained_host/shell_pointer/drag_surface.rs zircon_editor/src/ui/retained_host/app/host_lifecycle.rs` 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 shell-pointer focused/full Cargo 测试矩阵。 | 后续继续处理 resize splitter 与 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 shell-pointer/tab-drag focused tests 与 real-window drag/drop capture。 |
| 2026-06-16 | 08 M2.S3 Workbench tab-drag layout frame cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowLayoutFrames` 新增 `document_tabs_frame` 并由 `layout_frames()` 填充 `DocumentTabsRoot`；生产 `dispatch_drag_drop_from_pointer(...)` 改为调用 `resolve_host_tab_drop_route_with_workbench_layout_frames(...)`，tab-drag route/drop/strip hitbox 链路现在通过同一个 frame contract 优先消费 componentized Workbench 的 document tab strip、left/right/bottom/document region frames，旧 root-shell resolvers 仅作 fallback；floating-window strip 与 drawer resize owner 保持原路径。 | `rustfmt --edition 2021 --check` 覆盖 layout_frames、componentized_window、workspace_docking 与 tab_drag touched Rust 通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 tab-drag focused/full Cargo 测试矩阵。 | 后续继续处理 resize splitter 与 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 tab-drag focused tests 与 real-window tab/drop capture。 |

| 2026-06-16 | 08 M2.S3 Workbench document-tab pointer layout frame contract convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::sync_document_tab_pointer_layout(...)` 不再直接读取 `document_tabs_frame()`，改为消费 `BuiltinWorkbenchWindowTemplateSurfaceBridge::layout_frames().document_tabs_frame`，使文档标签 pointer layout 与 host layout、shell drag、tab drag 共用同一个 componentized Workbench frame contract；旧 root-shell document-tab resolver 仍只作为 builder fallback，floating-window tab strips 仍由 `FloatingWindowProjectionBundle` 拥有。 | `rustfmt --edition 2021 --check zircon_editor/src/ui/retained_host/app/pointer_layout.rs` 通过；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 document-tab focused/full Cargo 测试矩阵。 | 后续继续处理 resize splitter 与 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 document-tab pointer focused tests 与 real-window tab hover/click capture。 |
| 2026-06-16 | 08 M2.S3 Workbench viewport/document layout frame contract convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowLayoutFrames` 新增 `viewport_toolbar_frame`，并把 viewport toolbar/content/document workspace 三类生产消费统一到 `layout_frames()`：viewport resize、viewport pointer bounds、callback-surface size fallback、host presentation toolbar frame attachment、document toolbar pointer width 都消费同一个 componentized Workbench frame contract；源码契约测试同步到 `resolve_host_tab_drop_route_with_workbench_layout_frames(...)` 与 `update_layout_with_workbench_layout_frames(...)`。验证时还发现 `RenderMaterialValidationError::UnregisteredShadingModel` 新变体未被 editor 材质投影覆盖，已在 `material_editor/projection.rs` 与 `renderer_data_projection.rs` 做最小 match arm 补齐以解除编译阻断。 | `rustfmt --edition 2021 --check` 覆盖 layout_frames、componentized_window、host_lifecycle、helpers、viewport、retained_tab_drag surface contract 以及 material_editor 两个投影文件通过；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 viewport/document focused/full Cargo 测试矩阵。 | 后续继续处理 activity rail 精确 frame helper 的去留边界、resize splitter 与 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 viewport/document/tab-drag focused tests 与 real-window capture。 |
| 2026-06-17 | 08 M2.S3 Workbench activity rail layout frame contract convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowLayoutFrames` 新增 `activity_rail_frame`，把左侧主 activity rail 的精确 hit strip 纳入同一个 `layout_frames()` 合同；`RetainedEditorHost::sync_activity_rail_pointer_layout(...)` 现在消费 `layout_frames().activity_rail_frame`，而不是直接读取 Workbench bridge 单帧 helper；`left_region_frame` 继续作为 activity rail + scene tree 的 broad shell union 给 shell drag/host layout 使用，避免 rail hitbox 扩大到 scene tree；旧 `document_tabs_frame()`、`activity_rail_frame()`、viewport/document 单帧 helper 从 Workbench bridge 公共面移除。编译检查同时暴露 editor 侧 debug reflector/layout-route 测试仍引用旧 `legacy_selected_count`/`LegacyZircon` 命名，已同步到 runtime interface 的 `zircon_selected_count`/`Zircon`。 | `rustfmt --edition 2021 --check` 覆盖 layout_frames、componentized_window、pointer_layout、activity-rail pointer surface contract、layout route contract 与 debug_reflector model 通过；旧 Workbench bridge 单帧 helper 生产调用扫描为空；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 activity-rail focused/full Cargo 测试矩阵。 | 后续继续处理 resize splitter 与 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 activity-rail focused tests 与 real-window rail capture。 |
| 2026-06-17 | 08 M2.S3 Workbench resize splitter layout frame contract convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowLayoutFrames` 新增 `left_resize_splitter_frame`、`right_resize_splitter_frame` 与 `bottom_resize_splitter_frame`，并由 componentized drawer shell frames 推导显式 splitter hit strip；`HostShellPointerBridge::update_layout_with_workbench_layout_frames(...)` 把同一 frame contract 传入 resize surface，`resize_surface.rs` 现在优先消费 componentized splitter frame，再回退 legacy root-shell splitter resolver；源码契约测试同步到 `componentized_workbench_layout_frames` 路径，并新增 focused resize route 断言覆盖 componentized splitter 优先级。验证时还暴露 runtime shader `variant_key.rs` 对 `GeometrySourceId` 的 owner import 漂移，已同步为从 `geometry_source` owner 模块导入以解除编译阻断。 | `rustfmt --edition 2021 --check` 覆盖 layout_frames、drawer_layout、componentized_window、resize_surface、shell_pointer bridge、drawer_resize contract/target 与 runtime shader variant key 通过；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；focused `cargo test -p zircon_editor --lib resize_target_route_prefers_componentized_workbench_splitter_frame --locked --jobs 1 --message-format short --color never` 在 244s 编译窗口超时且无 Rust 诊断，超时后未发现残留 cargo/rustc/rustdoc 进程；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 后续继续处理 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 resize focused tests、broader `componentized_workbench_` 与 real-window drawer resize capture。 |
| 2026-06-17 | 08 M2.S3 Workbench drawer header layout frame contract convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowLayoutFrames` 新增 left/right/bottom drawer shell/header frames，并由 `layout_frames()` 直接暴露 componentized drawer projection frames；生产 `RetainedEditorHost` 改为调用 `build_host_drawer_header_pointer_layout_with_workbench_layout_frames(...)`，drawer header pointer layout 优先用 componentized drawer shell 作为区域基准、用 componentized header frame 作为 tab strip 命中条，旧 root-shell frame 仅作 fallback；旧三参数 builder 收窄为 `#[cfg(test)]` fallback 覆盖，源码契约测试同步到新的 layout-frame contract。 | `rustfmt --edition 2021 --check` 覆盖 layout_frames、drawer_layout、componentized_window、drawer_header_pointer builder/mod、app.rs、pointer_layout、host_lifecycle 与 drawer-header contract/target 测试通过；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声且未新增旧 builder 生产 unused warning。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer-header focused/full Cargo 测试矩阵。 | 后续继续处理 drawer header/resize frame-only projection 删除条件、host-page/menu 外层 shell owner 验证，以及补跑 drawer-header focused tests、broader `componentized_workbench_` 与 real-window drawer header capture。 |
| 2026-06-17 | 08 M2.S3 host-page/menu outer-shell owner contract | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `BuiltinHostOuterShellFrames` 和 `BuiltinHostWindowTemplateBridge::outer_shell_frames()`，只暴露 `shell_frame`、`menu_bar_frame`、`host_page_strip_frame` 三类外层宿主壳 frame；`sync_menu_pointer_layout(...)` 与 `sync_host_page_pointer_layout(...)` 现在传入该窄合同，`build_host_menu_pointer_layout(...)` / `build_host_page_pointer_layout(...)` 不再接收 broad `BuiltinHostRootShellFrames`，也不会消费 `BuiltinWorkbenchWindowLayoutFrames`；源码契约测试固定 menu/host-page 使用 `outer_shell_frames()`，并断言 componentized Workbench layout frame contract 不增加 menu-bar/host-page-strip owner 字段。 | `rustfmt --edition 2021 --check` 覆盖 outer_shell_frames、template bridge 导出、menu/host-page pointer builders、pointer_layout 与相关 source-contract/layout 测试通过；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 未进入 Rust 编译阶段：当前工作区清单已有 `blake3.workspace = true`，但 `Cargo.lock` 没有 `name = "blake3"`，`--locked` 拒绝更新锁文件。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵，也未改动无关锁文件。 | 后续继续处理 drawer header/resize frame-only projection 删除条件、remaining legacy root-shell frame fallback 删除条件，并在锁文件漂移解决后补跑 host-page/menu focused tests、broader `componentized_workbench_` 与 real-window menu/host-page capture。 |
| 2026-06-17 | 08 M2.S3 Workbench drawer content layout-frame contract convergence | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinWorkbenchWindowLayoutFrames` 新增 left/right/bottom drawer content frames 和 `drawer_content_frame(region)`；`layout_frames()` 直接从 componentized Workbench frame-only drawer projection 填充 content frame；drawer callback surface 尺寸、drawer viewport-toolbar 宽度与 drawer resize capture base extent 现在优先消费 `layout_frames()` 的 content/shell frame，旧 root-shell drawer content helper 只保留为过渡 fallback。源码契约测试固定这些消费者使用 componentized Workbench layout frame contract；编译 warning 显示 `BuiltinHostRootShellFrames::drawer_content_frame` 已无生产调用，可在下一切片删除。 | `rustfmt --edition 2021 --check` 覆盖 drawer_layout、layout_frames、componentized_window、helpers、viewport、workspace_docking、drawer_resize contract 与 drawer_header_pointer contract 通过；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer focused/full Cargo 测试矩阵。 | 后续删除 unused `BuiltinHostRootShellFrames::drawer_content_frame`，继续收缩 `root_shell_frames_with_componentized_drawers()`，并处理 remaining activity rail/shell pointer/drag fallback 与 frame-only projection 删除条件。 |
| 2026-06-17 | 08 M2.S3 root-shell drawer content owner removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinHostRootShellFrames` 物理删除 left/right/bottom drawer content frame 字段与 `drawer_content_frame(...)` helper；`BuiltinHostWindowTemplateBridge::root_shell_frames()` 和 `BuiltinWorkbenchWindowTemplateSurfaceBridge::merge_drawer_frames_into_root_shell_frames(...)` 不再把 drawer content 写入 root-shell frame bag；componentized Workbench layout frame 测试新增 `componentized_workbench_layout_frames_own_drawer_content_frames`，源码契约测试断言 root-shell 不再声明 drawer content owner。drawer shell/header 仍保留在 root-shell 作为 header/resize 过渡 fallback。 | `rustfmt --edition 2021 --check` 覆盖 root_shell_frames、bridge、drawer_layout、workbench_projection 与 drawer_header_pointer surface contract 通过；root-shell content 字段扫描仅剩 Workbench layout frame 与 test-only drawer-source 结构；冲突标记扫描为空；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer focused/full Cargo 测试矩阵。 | 继续收缩 `root_shell_frames_with_componentized_drawers()`，优先处理 activity rail、shell pointer/tab-drag 的 merged-root fallback；后续再进入 drawer frame-only projection 删除条件与 real-window drawer capture。 |
| 2026-06-17 | 08 M2.S3 activity rail merged-root fallback removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::sync_activity_rail_pointer_layout(...)` 不再调用 `root_shell_frames_with_componentized_drawers()`，改为传入普通 `template_bridge.root_shell_frames()` 和 `workbench_window_bridge.layout_frames()`；`build_host_activity_rail_pointer_layout_with_workbench_layout_frames(...)` 现在从 `BuiltinWorkbenchWindowLayoutFrames.activity_rail_frame` 解析左侧主 rail hit strip，并从 `drawer_shell_frame(ShellRegionId::Right)` 解析右侧抽屉 rail hit strip，普通 root-shell resolver 只保留缺帧 fallback；源码契约测试固定活动栏生产路径不再依赖 merged-root overlay。 | `rustfmt --edition 2021 --check` 覆盖 activity_rail_pointer builder/mod、app.rs、pointer_layout 与 activity-rail surface contract 通过；活动栏路径扫描确认旧 `build_host_activity_rail_pointer_layout_with_left_strip_frame(...)` 与 `root_shell_frames_with_componentized_drawers()` 调用已移除；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 activity-rail focused/full Cargo 测试矩阵。 | 继续收缩 `root_shell_frames_with_componentized_drawers()` 的 shell pointer、tab-drag、viewport/drawer transition fallback；后续再删除 drawer frame-only projection 与补跑 real-window rail/drawer capture。 |
| 2026-06-17 | 08 M2.S3 drawer viewport-toolbar merged-root fallback removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::viewport_toolbar_surface_size(...)` 的 drawer host 分支不再构造 `root_shell_frames_with_componentized_drawers()`；drawer toolbar 宽度现在优先使用 `BuiltinWorkbenchWindowLayoutFrames.drawer_content_frame(region)`，再使用 `drawer_shell_frame(region)`，最后只回退普通 `template_bridge.root_shell_frames()` 的 root-shell region resolver；源码契约测试固定 viewport 路径使用 componentized drawer frames 且不调用 merged-root helper。 | `rustfmt --edition 2021 --check` 覆盖 viewport.rs 与 drawer-header surface contract 通过；viewport 路径扫描确认 `root_shell_frames_with_componentized_drawers()` 已移除且保留 content/shell/root-shell fallback 顺序；冲突标记扫描为空；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 viewport-toolbar focused/full Cargo 测试矩阵。 | 继续收缩 `root_shell_frames_with_componentized_drawers()` 的 shell pointer、tab-drag 与 drawer resize capture fallback；后续再删除 drawer frame-only projection 与补跑 real-window drawer toolbar capture。 |
| 2026-06-17 | 08 M2.S3 drawer resize capture merged-root fallback removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::begin_drawer_resize_capture(...)` 的 fallback 不再调用 `root_shell_frames_with_componentized_drawers()`；drawer resize capture 继续优先用 `BuiltinWorkbenchWindowLayoutFrames.drawer_shell_frame(region)` 作为当前抽屉尺寸基准，缺失时只退回普通 `template_bridge.root_shell_frames()` 的 root-shell region resolver；drawer resize source contract 将 no-merged-root 断言收窄到 capture 函数片段，避免与待处理 tab-drag merged-root fallback 混淆。 | `rustfmt --edition 2021 --check` 覆盖 workspace_docking.rs 与 drawer_resize surface contract 通过；resize capture 路径扫描确认保留 `drawer_shell_frame(region)` 优先级并改用普通 root-shell fallback；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer-resize focused/full Cargo 测试矩阵。 | 继续收缩 `root_shell_frames_with_componentized_drawers()` 的 shell pointer 与 tab-drag/drop fallback；后续再删除 drawer frame-only projection 与补跑 real-window drawer resize capture。 |
| 2026-06-17 | 08 M2.S3 tab-drag/drop merged-root fallback removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::dispatch_drag_drop_from_pointer(...)` 不再为 tab-drag/drop 构造 `root_shell_frames_with_componentized_drawers()`；drop route 继续调用 `resolve_host_tab_drop_route_with_workbench_layout_frames(...)` 并传入 `workbench_window_bridge.layout_frames()`，缺失时只使用普通 `template_bridge.root_shell_frames()` 作为 root-shell fallback；tab-drag source contract 固定 docking 路径使用 workbench layout frame contract 且不再依赖 merged-root helper。 | `rustfmt --edition 2021 --check` 覆盖 workspace_docking.rs 与 tab-drag surface contract 通过；workspace_docking 路径扫描确认 tab-drag/drop 和 drawer resize 均改用普通 root-shell fallback，`root_shell_frames_with_componentized_drawers()` 已无 workspace_docking 调用；冲突标记扫描为空；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 tab-drag focused/full Cargo 测试矩阵。 | 继续收缩 `root_shell_frames_with_componentized_drawers()` 的最后 shell pointer sync 调用；后续再删除 helper/merge API、drawer frame-only projection，并补跑 real-window tab drag/drop capture。 |
| 2026-06-17 | 08 M2.S3 shell pointer merged-root helper removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `RetainedEditorHost::recompute_if_dirty(...)` 的 shell pointer layout sync 不再构造 merged drawer/root shell，改为把普通 `root_shell_frames` 与 `BuiltinWorkbenchWindowLayoutFrames` 同时传入 `HostShellPointerBridge::update_layout_with_workbench_layout_frames(...)`；`RetainedEditorHost::root_shell_frames_with_componentized_drawers(...)`、`BuiltinWorkbenchWindowTemplateSurfaceBridge::merge_drawer_frames_into_root_shell_frames(...)` 与私有 `componentized_drawer_frame(...)` 物理删除；原 merge 行为测试改为验证 `layout_frames()` 拥有 drawer shell/header frames，source contract 固定 helper/API 不再存在。 | `rustfmt --edition 2021 --check` 覆盖 host_lifecycle、helpers、drawer_layout、workbench_projection 与 tab_drag surface contract 通过；merged-root helper/API 生产扫描为空，retained_host 源码只剩 source-contract 负向断言；冲突标记扫描为空；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 首次 244s 超时但无 Rust 诊断且无残留 cargo/rustc/rustdoc 进程，600s 重跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 shell-pointer focused/full Cargo 测试矩阵。 | 继续处理 drawer frame-only projection 删除条件与 retained drawer bridge hard-cutover；后续补跑 shell-pointer/tab-drag/drawer focused tests 与 real-window drag/resize capture。 |
| 2026-06-17 | 08 M2.S3 retained drawer source hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench/drawer_layout.rs` 现在在 Workbench window owner 内直接维护 frame-only drawer projection constraints，生产路径不再依赖独立 `workbench_drawer_source` layout；独立 `workbench_drawer_source` bridge/source-frame/error/control-id/layout 模块、`workbench.drawer_source` builtin document id、`workbench_drawer_source.v2.ui.toml` 资产和 drawer-source 专用测试已物理删除；边界测试改为固定 `workbench_window.zui` 内嵌 `surface_variant = "frame_only"` drawer projection，builtin registry 固定为不再注册独立 drawer source 文档。真实 SceneTree/Inspector/ComponentDrawer 组件尚未暴露旧 header/content owner，所以内嵌 frame-only projection 仍保留为过渡 frame owner。 | `rustfmt --edition 2021` 覆盖本切片 touched Rust 通过；源码/资产扫描确认旧 drawer source 生产入口无残留，源码中仅剩边界测试的删除断言；文档扫描确认 `docs/zircon_editor` 中只保留历史/删除说明；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer focused/full Cargo 测试矩阵。 | 后续让真实 drawer panel components 暴露 shell/header/content owner frames 后删除 Workbench window 内嵌 frame-only projection；补跑 drawer-header/resize/drop focused tests、broader `componentized_workbench_` 与 real-window drawer capture。 |
| 2026-06-17 | 08 M2.S3 real drawer panel frame owner cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `workbench_window.zui` 删除内嵌 `drawer_projection_source`/`surface_variant = "frame_only"` drawer projection；`workbench_main_band.zui` 真实承载 `LeftDrawerShellRoot`/`RightDrawerShellRoot`，SceneTree/Inspector 组件承载左右 header/content owner，Workbench window 用真实 `BottomDrawerShellRoot` 包住 ComponentDrawer，ComponentDrawer 组件承载底部 header/content owner；`workbench/drawer_layout.rs` 只把 model-driven side panel width 和 bottom shell height 写到真实 owner 节点，不再维护 projection-only panel/separator/top/status placeholder constraints；边界测试固定 projection 不得回归且 owner id 必须在真实组件资产中。 | `tomllib` 解析 Workbench window/MainBand/SceneTree/Inspector/ComponentDrawer 5 个 touched 资源通过；资源扫描确认真实资产无 `drawer_projection`、`WorkbenchDrawerSourceRoot`、`frame_only` 和旧 Panel/OuterSeparator owner；owner id 扫描确认 left/right/bottom shell/header/content 全部落在真实资产；`rustfmt --edition 2021 --check` 覆盖 touched Rust 通过；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer-focused/full Cargo 测试矩阵。 | 下一步处理真实抽屉 owner 后的视觉/命中细节和残余 root-shell fallback，补跑 drawer-header/resize/drop focused tests、broader `componentized_workbench_` 与 real-window drawer capture。 |
| 2026-06-17 | 08 M2.S3 root-shell drawer shell/header owner removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `BuiltinHostRootShellFrames` 删除 left/right/bottom drawer shell/header 字段和 helper；`BuiltinHostWindowTemplateBridge::root_shell_frames()` 不再采样 `LeftDrawerShellRoot`/`RightDrawerShellRoot`/`BottomDrawerShellRoot` 及 header root；`root_shell_projection.rs` 不再从 root drawer shell 推导 side/bottom/document frame；drawer header、activity rail、tab drag/drop、drawer resize、host presentation splitter 与相关测试 helper 统一优先走 `BuiltinWorkbenchWindowLayoutFrames`，root-shell 只保留非 drawer 通用 fallback。 | root-shell drawer owner scan 确认 `root_shell_frames.rs`/`bridge.rs`/`root_shell_projection.rs` 无 drawer shell/header owner 名称残留；scoped `rustfmt --edition 2021 --check` 通过；scoped `git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer-focused/full Cargo 测试矩阵。 | 下一步补视觉/命中细节、focused drawer-header/resize/drop/tab-drag 测试和 real-window capture 证据。 |
| 2026-06-17 | 08 M2.S3 drawer root fallback hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `root_shell_projection.rs` 物理删除 root left/right/bottom drawer region 与 drawer splitter resolver；drawer header pointer、右侧 activity rail、shell drag/drop、resize surface、drawer resize capture、tab strip hitbox、drawer toolbar sizing 与 host presentation side/bottom/splitter 布局均只消费 `BuiltinWorkbenchWindowLayoutFrames` 的 drawer frames，缺 frame 时返回空/default 或不启动 resize capture，不再回退 root-shell drawer geometry；source-contract 测试同步固定旧 resolver/helper 不得回归。 | `rustfmt --edition 2021` 覆盖本切片 touched Rust 通过；旧 drawer region/splitter resolver 扫描只剩测试负向断言；merged drawer/root helper 扫描只剩测试负向断言；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 drawer-focused/full Cargo 测试矩阵。 | 下一步进入真实抽屉 owner 后的视觉/命中细节，补跑 drawer-header/resize/drop/tab-drag focused tests 与 real-window drawer capture。 |
| 2026-06-17 | 08 M2.S3 document-tab pointer root fallback hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 生产 `RetainedEditorHost::sync_document_tab_pointer_layout(...)` 不再读取 `template_bridge.root_shell_frames()`，改为把 `workbench_window_bridge.layout_frames()` 传给 `build_host_document_tab_pointer_layout_with_workbench_layout_frames(...)`；主文档 tab hit strip 只消费 `BuiltinWorkbenchWindowLayoutFrames.document_tabs_frame`，缺 frame 时为空/default，不再从 root-shell document frame 推导；`root_shell_projection.rs` 删除 `resolve_root_document_tabs_frame(...)`，旧 root 推导只保留在 test-only 兼容 builder 内。 | `rustfmt --edition 2021` 覆盖 document-tab pointer/app/root projection/source-contract touched Rust 通过；旧 `resolve_root_document_tabs_frame` 与旧 `build_host_document_tab_pointer_layout_with_root_strip_frame` 扫描无残留；`cargo check -p zircon_editor --lib --locked` 首次 125s 超时但后台编译自然结束且无残留后，600s 重跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 document-tab focused/full Cargo 测试矩阵。 | 下一步继续收缩 `root_shell_projection.rs` 的 document/viewport/center/status fallback，进入 M3.S1 剩余区域 hard-cutover；后续补跑 document-tab focused tests 与 real-window tab hover/click capture。 |
| 2026-06-17 | 08 M2.S3 activity rail root fallback hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 生产 `RetainedEditorHost::sync_activity_rail_pointer_layout(...)` 不再读取 `template_bridge.root_shell_frames()`，`build_host_activity_rail_pointer_layout_with_workbench_layout_frames(...)` 也不再接收 root-shell frames；左侧主 rail 只消费 `BuiltinWorkbenchWindowLayoutFrames.activity_rail_frame`，右侧 drawer rail 只消费 `drawer_shell_frame(ShellRegionId::Right)`，缺 frame 时为空/default；`root_shell_projection.rs` 删除 `resolve_root_activity_rail_frame(...)`，旧 root activity rail 推导只保留在 test-only 兼容 builder 内。 | `rustfmt --edition 2021` 覆盖 activity-rail pointer/app/root projection/source-contract touched Rust 通过；旧 `resolve_root_activity_rail_frame` 扫描只剩 source-contract 负向断言；activity rail sync 源码契约固定 Workbench layout frame contract 且禁止 `template_bridge.root_shell_frames()`；按用户“先实现功能，测试暂时延后”要求，本切片先不扩展 activity-rail focused/full Cargo 测试矩阵。 | 下一步继续收缩 `root_shell_projection.rs` 的 document/viewport/center/status fallback，进入 M3.S1 剩余区域 hard-cutover；后续补跑 activity-rail focused tests 与 real-window rail hover/click capture。 |
| 2026-06-17 | 08 M3.S1 tab-drag document/center root fallback hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 生产 `RetainedEditorHost::dispatch_drag_drop_from_pointer(...)` 不再读取 `template_bridge.root_shell_frames()`，`resolve_host_tab_drop_route_with_workbench_layout_frames(...)` 与 `resolve_tab_drop_with_workbench_layout_frames(...)` 也不再接收 root-shell frames；`strip_hitbox.rs` 的 document tab 插入命中只消费 `BuiltinWorkbenchWindowLayoutFrames.document_tabs_frame`，缺 tab strip 时只退到同一 Workbench contract 的 `document_region_frame`/`center_band_frame`，不再调用 `resolve_root_document_region_frame(...)` 或 `resolve_root_center_band_frame(...)`；旧 root-frame route/drop 包装仅保留为 `#[cfg(test)]` 兼容路径并合成临时 Workbench layout frames。 | `rustfmt --edition 2021 --check` 覆盖 workspace_docking/tab_drag/source-contract touched Rust 通过；tab-drag hitbox/root fallback 扫描确认 `resolve_root_center_band_frame`、`resolve_root_document_region_frame`、`resolve_direct_document_host_frame` 不在 `tab_drag` hitbox/drop route 生产路径；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 tab-drag focused/full Cargo 测试矩阵。 | 继续收缩 shell pointer、host presentation、viewport 里的 center/document/status/viewport root fallback；后续补跑 tab-drag focused tests 与 real-window drag/drop capture。 |
| 2026-06-17 | 08 M3.S1 shell pointer center/document/status root fallback hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 生产 `RetainedEditorHost::recompute_if_dirty(...)` 调用 `HostShellPointerBridge::update_layout_with_workbench_layout_frames(...)` 时不再传 root-shell frames；`shell_pointer/drag_surface.rs` 只从 `BuiltinWorkbenchWindowLayoutFrames.center_band_frame`、`document_region_frame`、`status_bar_frame` 与 drawer frames 构建 drag/drop targets，缺 Workbench center/status/document frame 时生成空/default targets，不再调用 `resolve_root_center_band_frame(...)`、`resolve_root_document_region_frame(...)`、`resolve_root_status_bar_frame(...)` 或 direct document-host fallback；旧 root-frame shell/tab drag helper 仅保留为 `#[cfg(test)]` 兼容路径并合成临时 Workbench layout frames。 | `rustfmt --edition 2021 --check` 覆盖 shell_pointer/app/drawer_resize/tab_drag/source-contract touched Rust 通过；shell pointer drag surface root fallback 扫描为空；生产 `host_lifecycle` shell pointer 调用确认不再传 `Some(&root_shell_frames)`；`cargo check -p zircon_editor --lib --locked` 被当前工作区既有 dirty `zircon_runtime/src/scene/ecs/query/query_state/read_only.rs` 生命周期错误 E0309 阻塞，editor UI 代码未进入诊断阶段。按用户“先实现功能，测试暂时延后”要求，不扩展 shell-pointer focused/full Cargo 测试矩阵。 | 继续收缩 host presentation、viewport 与 root-shell projection 中的 center/document/status/viewport fallback；待 runtime ECS 查询生命周期错误处理后补跑 `cargo check`、shell-pointer focused tests 与 real-window drag/drop capture。 |
| 2026-06-17 | 08 M3.S1 host presentation/viewport root fallback hard-cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `apply_presentation(...)` 不再接收 `BuiltinHostRootShellFrames`，`HostWindowLayoutData` 的 center/document/viewport/status 与 drawer/splitter frame 全部只由 `BuiltinWorkbenchWindowLayoutFrames` 生成，缺 Workbench frame 时为空/default；`RetainedEditorHost::recompute_if_dirty(...)` 的 viewport resize/pointer bounds 不再调用 `resolve_root_viewport_content_frame(...)`；`viewport_toolbar_surface_size(...)` 和 active Workbench main-page callback surface sizing 也不再从 root-shell pane/document/viewport frames 兜底，Exclusive pages、menu/host-page/root-template overlay 与 native/floating window 继续保留各自 owner contract。 | `rustfmt --edition 2021 --check` 覆盖 host_lifecycle、viewport、helpers、apply_presentation、相关测试辅助与 source-contract 通过；`apply_presentation.rs` 扫描确认无 `shared_root_frames`、`BuiltinHostRootShellFrames` 与 `resolve_root_*`；host_lifecycle/viewport 扫描确认无 `resolve_root_viewport_content_frame` 或 `active_document_shows_viewport_toolbar`；`cargo check -p zircon_editor --lib --locked` 仍被当前工作区既有 dirty `zircon_runtime/src/scene/ecs/query/query_state/read_only.rs` 生命周期错误 E0309 阻塞，editor UI 代码未进入诊断阶段。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续处理 `root_shell_projection.rs` 剩余 center/document/status/viewport resolver 的删除条件、root-template/painter 外层骨架边界，并在 runtime ECS 查询生命周期错误处理后补跑 `cargo check` 与 host-presentation/viewport focused tests。 |
| 2026-06-17 | 08 M3.S1 root-shell projection module removal | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `zircon_editor/src/ui/retained_host/ui/workbench_window_projection.rs` 与 `retained_host/mod.rs` 模块导出物理删除；`ui/tests.rs` 旧 presentation 兼容测试改为验证 Workbench layout frames 缺失时 center/document/status/viewport 保持空/default，显式 Workbench frame 存在时只消费 `BuiltinWorkbenchWindowLayoutFrames`；`workbench_projection_cutover.rs` 改为删除契约，固定旧 resolver 模块不得回归。 | `rustfmt --edition 2021 --check` 覆盖 retained_host mod、ui/tests 与 workbench_projection_cutover 通过；retained_host 源码扫描确认 `root_shell_projection` 与 `resolve_root_center_band_frame`/`resolve_root_status_bar_frame`/`resolve_root_document_region_frame`/`resolve_root_viewport_content_frame` 无残留；全 src 扫描只剩 source-contract 负向断言；`cargo check -p zircon_editor --lib --locked` 仍被当前工作区既有 dirty `zircon_runtime/src/scene/ecs/query/query_state/read_only.rs` 生命周期错误 E0309 阻塞，editor UI 代码未进入诊断阶段。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步进入 root-template/painter 外层骨架与 presentation cache/painter 投影族删除条件；待 runtime ECS 查询生命周期错误处理后补跑 `cargo check` 与 M3 focused tests。 |
| 2026-06-17 | 08 M3.S2 componentized Workbench root-skeleton hard cutover | 完成（功能代码、回归约束、文档记录；完整测试矩阵按用户要求暂缓） | `host_contract/painter/workbench.rs` 的 full-frame paint、recorded command stream 与 damage repaint 入口现在在 root frame 解析前分支：存在 `workbench_window_nodes` 时只绘制 `draw_componentized_workbench_window(...)`，非组件化 retained fallback 才绘制旧 `draw_root_skeleton(...)` + `draw_host_scene(...)`；删除不再使用的 `workbench_skeleton_regions.rs` 模块声明/文件，旧“root skeleton 局部挖洞”混合路径不再作为生产分支存在；`command_stream/tests.rs` 新增 `componentized_workbench_command_stream_skips_legacy_root_skeleton_quads`，用 frame-only Workbench owner 节点约束旧 center/document/viewport root-skeleton 颜色不得进入组件化 command stream。 | `rustfmt --edition 2021 --check` 覆盖 painter mod、workbench painter 与 command_stream tests 通过；painter 入口扫描确认 `paint_host_frame`/`record_host_frame_commands`/`repaint_host_frame_region`/`draw_root_skeleton`/`draw_host_scene` 各只有一个定义且无 `workbench_skeleton_regions`、`componentized_workbench_skeleton_regions`、`draw_rect_excluding*` 残留；全 `zircon_editor/src` 扫描确认已删除 helper 无引用；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续 M3.S2：梳理 host presentation cache/presenter 旧路径删除条件，逐步把 retained host command stream/presentation cache 缩到最终 runtime UI shell 承载所需的过渡面。 |
| 2026-06-17 | 08 M3.S2 presentation cache hard cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `app/presentation_cache.rs` 删除，`RetainedEditorHost` 不再保存 `HostPresentationCache`；活动 activity-window template gate、floating-window surface focus、welcome recent pointer 数据和 console 滚动范围改由 `workbench_snapshot_access.rs` 从当前 `EditorEventRuntime::chrome_snapshot()` / `status_line()` 按需读取；recompute、viewport resize、tab-drag/docking 等路径不再更新重复 presentation cache。 | `rustfmt --edition 2021 --check` 覆盖 retained host app cache-cutover touched Rust 通过；retained_host 扫描确认无 `presentation_cache`/`HostPresentationCache`/删除文件/`recompute_update_presentation_cache` 残留，`Test-Path` 确认 `app/presentation_cache.rs` 不存在；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2 presenter 残余旧路径收缩，随后梳理 painter/presenter 删除条件与 focused/real-window 验收。 |
| 2026-06-17 | 08 M3.S2 presenter snapshot hard cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/presenter/snapshot.rs`，用 `build_chrome_command_stream(...)` + `paint_chrome_command_stream_to_frame(...)` 生成完整 RGBA 快照；`UiHostWindow::take_snapshot(...)` 与 profiling `screenshot_reference.png` 输出不再直接调用 `paint_host_frame(...)`，统一经过 presenter command stream；`painter/mod.rs` 中 `paint_host_frame` 导出收为 `#[cfg(test)]`，生产只继续导出 command stream 所需的 `record_host_frame_commands`。 | `rustfmt --edition 2021 --check` 覆盖 presenter snapshot、presenter mod、painter mod、window 与 profiling artifacts 通过；retained_host 扫描确认生产调用面无 `use super::painter::paint_host_frame` / `paint_host_frame(...)`，只剩 painter 定义和 command_stream 测试对照；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声且上一轮新出现的 `paint_host_frame` 生产未使用 warning 已消失。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：盘点 `record_host_frame_commands` 与 painter template_* 投影族删除条件，收缩 presenter 对 painter recorded-command 的最后依赖。 |
| 2026-06-17 | 08 M3.S2 direct pixel painter test-only cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `host_contract/painter/workbench.rs` 中直接 RGBA full-frame `paint_host_frame(...)` 和 region `repaint_host_frame_region(...)` 定义改为 `#[cfg(test)]`；生产 build 的 Workbench painter 可见入口只剩 `record_host_frame_commands(...)`，用于 presenter command stream extraction；command_stream 对照测试仍可使用 direct pixel painter 验证 replay parity。 | `rustfmt --edition 2021 --check` 覆盖 painter workbench/mod 通过；retained_host 扫描确认 `paint_host_frame(...)` / `repaint_host_frame_region(...)` 只剩测试专用定义与 command_stream 测试引用；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步将 `record_host_frame_commands(...)` 从 painter 命名空间剥离为 presenter/command-stream extraction 接口，并继续分批删除不再需要的 painter template_* 族。 |
| 2026-06-17 | 08 M3.S2 presenter extraction boundary split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/presenter/extraction.rs`，把 `record_host_frame_commands(...)` 返回的 painter recorded commands 转换为 `ChromeCommand` 的逻辑从 `command_stream.rs` 中剥离；`command_stream.rs` 现在只调用 `extract_chrome_commands(...)` 并组合 damage clip/command list，不再直接导入 `HostRecordedPaintCommand` / `HostRecordedPaintKind` 或实现 `push_recorded_command(...)`。 | `rustfmt --edition 2021 --check` 覆盖 presenter command_stream/extraction/mod 通过；扫描确认 `command_stream.rs` 无 `record_host_frame_commands`、`HostRecordedPaintCommand`、`HostRecordedPaintKind`、`push_recorded_command`；`git diff --check` 仅报告既有 LF/CRLF 提示；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步评估是否把 `record_host_frame_commands(...)` 的录制入口从 `painter/workbench.rs` 迁出到 presenter extraction，随后继续删除/隔离 painter template_* 族。 |
| 2026-06-17 | 08 M3.S2 host-contract paint-frame neutralization | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `HostRgbaFrame`、`HostRecordedPaintCommand`、`HostRecordedPaintKind`、`HostPaintAtlasImage` 与 `HostPaintImageUvRect` 从 `host_contract/painter/frame.rs` 迁入中立 `host_contract/paint_frame.rs`，旧 `painter/frame.rs` 物理删除；`painter/mod.rs` 不再 re-export 基础帧/recorded-command 类型，`window.rs`、presenter snapshot/softbuffer/command stream/extraction 直接依赖 `paint_frame`；command stream 单元测试通过 `extraction` 的 test-only 转换 helper 构造 ChromeCommand，不再保留 `push_recorded_command(...)` 入口。 | `cargo fmt -p zircon_editor --check` 通过；host_contract 源码扫描确认无 `painter::HostRgbaFrame`、`painter::HostRecordedPaint*`、`sprite_atlas::HostPaint*`、`push_recorded_command`、`mod frame;` 残留，`Test-Path host_contract/painter/frame.rs` 为 false；`cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续把 `record_host_frame_commands(...)` 的 Workbench 命令录制入口从 painter 命名空间剥离，或按区域删除剩余 painter template_* 投影族。 |
| 2026-06-17 | 08 M3.S2 paint-recording extraction entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_recording.rs` 作为中立 recorded-command 入口，负责 recording-only `HostRgbaFrame`、damage clip 和背景 clear；`host_contract/painter/workbench.rs` 删除 `record_host_frame_commands(...)`，只保留 `draw_host_presentation_commands(...)` 作为 painter 内部绘制 adapter；`presenter/extraction.rs` 改为依赖 `paint_recording::record_host_frame_commands(...)`，不再从 painter 命名空间导入录制入口。 | `cargo fmt -p zircon_editor --check` 通过；host_contract/presenter 扫描确认 recorded-command 入口只在 `paint_recording.rs` 定义，presenter extraction 只导入 `paint_recording::record_host_frame_commands`，painter/mod 不再 re-export `record_host_frame_commands`；首次 `cargo check` 暴露 `SHELL_BACKGROUND` 可见性不足并已修正，复跑 `cargo check -p zircon_editor --lib --locked` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续把 presenter command-stream software replay 对 painter primitive helpers 的依赖剥离，或进入剩余 painter template_* 投影族删除。 |
| 2026-06-17 | 08 M3.S2 paint software replay neutralization | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增中立 `host_contract/paint_diagnostics.rs`、`paint_geometry.rs`、`paint_primitives.rs`、`paint_text.rs`，并物理删除旧 `host_contract/painter/diagnostics_overlay.rs`、`painter/geometry.rs`、`painter/primitives.rs`、`painter/text.rs`；`presenter/command_stream.rs` 的软件回放现在直接依赖 `paint_primitives`/`paint_text`，`presenter/softbuffer.rs` 的诊断损伤扩展直接依赖 `paint_diagnostics`，`surface_hit_test/template_node.rs` 也改为从 `paint_geometry` 取模板 frame。 | `cargo fmt -p zircon_editor --check` 通过；host_contract 源码扫描确认无 `painter::frame_from_template`、`super::geometry`、`super::primitives`、`super::text`、`super::super::painter::{...}`、`diagnostics_overlay`、`mod geometry;`、`mod primitives;`、`mod text;` 残留；`Test-Path` 确认四个旧 painter 基础文件已删除、四个中立 paint 文件存在；首次 `cargo check -p zircon_editor --lib --locked` 304s 超时且无诊断，复跑 `cargo check -p zircon_editor --lib --locked --jobs 1` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续 M3.S2：收缩 `painter/workbench.rs` 作为 `paint_recording` 的临时 draw adapter，并盘点剩余 template_* 投影族删除条件。 |
| 2026-06-17 | 08 M3.S2 paint theme neutralization | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `host_contract/painter/theme.rs` 迁入中立 `host_contract/paint_theme.rs`，`painter/mod.rs` 删除 `mod theme;` 与 `SHELL_BACKGROUND` re-export；所有 painter/template/style_selector/material primitive 直接导入 `paint_theme::PALETTE`；`paint_recording.rs` 现在直接用 `PALETTE.shell_background` 定义本地录制背景，不再通过 painter 命名空间获取 shell 背景色。 | `cargo fmt -p zircon_editor` 完成机械 import 排序；源码扫描确认无 `mod theme;`、`super::theme::PALETTE`、`super::super::theme::PALETTE`、`super::super::super::theme::PALETTE`、`painter/theme`、`painter\\theme` 或 `painter::{..., SHELL_BACKGROUND}` 残留；`Test-Path` 确认旧 `painter/theme.rs` 已删除、新 `paint_theme.rs` 存在；`cargo check -p zircon_editor --lib --locked --jobs 1` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续把 `draw_host_presentation_commands(...)` 从 painter re-export 收缩到中立 Workbench paint entry，或按 template_* 投影族删除条件继续拆除。 |
| 2026-06-17 | 08 M3.S2 Workbench paint entry neutralization | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_workbench.rs` 作为中立 Workbench 绘制入口；`paint_recording.rs` 现在调用 `draw_workbench_presentation_commands(...)`，test-only `paint_host_frame(...)` 与 `repaint_host_frame_region(...)` 也迁入该入口模块；`painter/mod.rs` 不再 re-export Workbench draw/test painter 入口；`painter/workbench.rs` 删除旧 `draw_host_presentation_commands(...)` 入口，只对中立入口暴露 `draw_componentized_workbench_window(...)`、`draw_legacy_workbench_window(...)` 与 profiled legacy helper，root-frame、root-skeleton、host-scene 细节重新收回为 painter 内部实现。 | `cargo fmt -p zircon_editor --check` 通过；host_contract 源码扫描确认无旧 painter Workbench entry re-export/import、无 `painter::draw_host_presentation_commands` 或旧 `draw_host_presentation_commands(...)` 调用残留，`Test-Path host_contract/paint_workbench.rs` 为 true；`cargo check -p zircon_editor --lib --locked --jobs 1` 复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续拆小 `painter/workbench.rs` 的绘制实现或按 template_* 投影族删除条件继续收缩 painter 命名空间。 |
| 2026-06-17 | 08 M3.S2 Workbench painter module hard cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 旧 `host_contract/painter/workbench.rs` 物理迁出为中立 `host_contract/paint_workbench_renderer.rs`，`painter/mod.rs` 删除 `mod workbench`；`paint_workbench.rs` 只依赖中立 impl 模块；painter 命名空间仅向该实现显式开放 `draw_close_prompt(...)`、`draw_debug_reflector_overlay(...)`、`draw_template_nodes(...)` 与 `has_template_nodes(...)` 四个既有模板绘制 helper，避免重新开放 Workbench painter 子模块。 | `cargo fmt -p zircon_editor --check` 通过；`Test-Path host_contract/painter/workbench.rs` 为 false、`Test-Path host_contract/paint_workbench_renderer.rs` 为 true；源码扫描确认无 `painter::workbench`、`mod workbench;`、`painter/workbench` 生产路径残留；`cargo check -p zircon_editor --lib --locked --jobs 1` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续把 remaining template-node painter helper 迁往中立 paint/template 命名空间，或按 template_* 投影族删除条件继续收缩 painter。 |
| 2026-06-17 | 08 M3.S2 paint helper neutralization | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | `host_contract/painter/close_prompt.rs` 迁出为 `host_contract/paint_close_prompt.rs`，`host_contract/painter/debug_reflector_overlay.rs` 迁出为 `host_contract/paint_debug_reflector_overlay.rs`；`painter/mod.rs` 删除对应模块声明/re-export；`paint_workbench_renderer.rs` 直接依赖中立 close-prompt/debug-overlay helper，对 painter 命名空间的剩余依赖压缩到 `draw_template_nodes(...)` 与 `has_template_nodes(...)`。 | `cargo fmt -p zircon_editor` 应用格式化；文件探针确认两个旧 painter helper 文件为 false、两个新中立 helper 文件为 true；源码扫描确认无 painter close-prompt/debug-overlay module/import/path 残留（除 `data/mod.rs` 的同名数据模块）；`cargo check -p zircon_editor --lib --locked --jobs 1` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步继续把 `template_nodes` 及其 template_* painter helper 迁往中立命名空间，或按投影族删除条件继续收缩 painter。 |
| 2026-06-17 | 08 M3.S2 template-node paint entry neutralization | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes.rs` 作为中立模板节点绘制入口，接管 `draw_template_nodes(...)`、`has_template_nodes(...)` 与 test-only template-node RGBA helper；`paint_workbench_renderer.rs` 改为只导入该中立入口，不再直接依赖 `super::painter`；`painter/mod.rs` 不再 re-export template-node entry/test functions，仅把具体 `template_nodes` 模块限制在 `host_contract` 内部作为下一轮整体迁移对象。 | `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `paint_workbench_renderer.rs` 无 `super::painter`/`painter::`/旧 `draw_host_presentation_commands(...)`，`painter/mod.rs` 无旧 template-node re-export，`paint_template_nodes.rs` 为 true 且旧 Workbench/close-prompt/debug-overlay painter 文件仍为 false；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 下一步以单独硬切片迁移整组 `painter/template_*` 绘制族和 `render_commands`/style-selector 依赖，避免只搬 1021 行 `template_nodes.rs` 造成可见性补丁和迁移壳。 |
| 2026-06-17 | 08 M3.S2 template paint family hard cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 临时 `host_contract/paint_template_nodes.rs` 升级为 folder-backed `host_contract/paint_template_nodes/mod.rs`；整组旧 `host_contract/painter/` 目录（`template_nodes.rs`、`template_*`、`render_commands`、material/style-selector/MUI/visual-assets 族）硬迁入中立 `paint_template_nodes/` 命名空间；`host_contract/mod.rs` 不再声明 `mod painter`，test-only template-node RGBA/render-command helper 从新模块导出；visual-assets 路径契约更新到新目录；为打通本次 editor 验证，补齐 `zircon_runtime` post_process 常量模块对已定义 `SMAA_STAGE_FORMAT` 的 re-export。 | `cargo fmt -p zircon_runtime -p zircon_editor`、`cargo fmt -p zircon_editor --check`、`cargo fmt -p zircon_runtime --check` 通过；文件探针确认旧 `host_contract/painter/` 为 false、新 `host_contract/paint_template_nodes/mod.rs` 为 true；源码扫描确认无 `host_contract::painter` / `super::painter` / `host_contract/painter` / `mod painter;` 残留；首轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 runtime `SMAA_STAGE_FORMAT` 导出缺口，最小补齐后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：在不恢复旧 `painter` 命名空间的前提下，把中立 `paint_template_nodes/` 软件绘制族与 presenter command stream 的剩余过渡责任继续收束到 runtime extract/GPU command stream 边界。 |
| 2026-06-17 | 08 M3.S2 chrome command-stream ownership hard cutover | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增中立 `host_contract/chrome_command_stream/`，接管 `ChromeCommandStream`、recorded-command extraction、software replay、command-stream parity tests 与 atlas tests；旧 `presenter/command_stream.rs`、`presenter/command_stream/`、`presenter/extraction.rs` 物理删除；`presenter/mod.rs` 不再声明 command-stream/extraction 模块，snapshot、softbuffer、gpu 后端改为消费中立 stream。 | `cargo fmt -p zircon_editor` 通过；文件探针确认旧 presenter command-stream/extraction 路径为 false、新 `host_contract/chrome_command_stream/{mod.rs,extraction.rs}` 为 true；host_contract 源码扫描确认无旧 `presenter::command_stream` / `super::command_stream` / `presenter::extraction` / `super::extraction` / presenter `mod command_stream` 残留，仅保留新模块内部 `mod extraction;`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：收缩中立 `chrome_command_stream/` 与 `paint_template_nodes/` 软件桥，逐步把 shell extract/replay 责任并入 runtime extract / GPU command stream 边界；如模块继续增长，拆分 `chrome_command_stream/` 子模块。 |
| 2026-06-17 | 08 M3.S2 runtime draw-list projection ownership split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/runtime_draw_list.rs`，把 `ChromeCommandStream` → runtime `UiSurfaceDrawList` / `UiSurfaceCommandKind` 投影从 `presenter/gpu.rs` 迁入中立 command-stream 模块；rounded quad/border、text style、image upload payload、atlas UV、clip、damage 与 surface size 的转换规则现在由 stream owner 统一维护；`GpuChromePresenter` 只保留 surface submit、cache bootstrap、damage diagnostics 与 profiling counter。 | `cargo fmt -p zircon_editor` 通过；presenter/chrome_command_stream 源码扫描确认 `ui_surface_command_from_chrome`、`UiSurfaceCommandKind`、`UiSurfaceImagePayload`、`UiSurfaceImageUvRect`、`UiSurfaceTextStyle`、`UiTextRunPaintStyle` 等 runtime draw-list 投影只在 `chrome_command_stream/runtime_draw_list.rs` 中出现，`presenter/gpu.rs` 仅调用 `ui_surface_draw_list_from_stream(...)`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：把 `chrome_command_stream/mod.rs` 中 stats/software replay/atlas subimage 等剩余职责继续拆到专属子模块，并继续收缩 `paint_template_nodes/` 软件桥向 runtime extract/GPU command stream 边界。 |
| 2026-06-17 | 08 M3.S2 chrome command-stream stats responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/stats.rs`，接管 `ChromeCommandStreamStats` 与 `ChromeCommandStream::stats()`；`mod.rs` 不再导入 `BTreeSet` 或承载 image upload/draw-call 聚合，统计仍通过现有 `stream.stats()` 调用面暴露，行为保持不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `ChromeCommandStreamStats`、`fn stats`、`image_upload_bytes`、`draw_call_count` 聚合只在 `chrome_command_stream/stats.rs` 定义，测试断言仍在 `tests.rs`，`mod.rs` 无 `BTreeSet`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：拆分 `chrome_command_stream/mod.rs` 中 software replay/atlas subimage 等剩余职责，或继续收缩 `paint_template_nodes/` 软件桥向 runtime extract/GPU command stream 边界。 |
| 2026-06-17 | 08 M3.S2 chrome command-stream software replay responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/replay.rs`，接管 `paint_chrome_command_stream_to_frame(...)`、`repaint_chrome_command_stream_region(...)`、command ordering replay、border fallback、image fallback 与 atlas UV 子图采样；`mod.rs` 只 re-export 原函数名并保留 stream 类型/构建职责，外部 presenter/snapshot/softbuffer 调用面不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `paint_chrome_command_stream_into_frame`、`paint_chrome_command(...)`、`paint_atlas_image_payload`、`atlas_subimage_rgba`、`paint_border_command`、`FALLBACK_IMAGE_COLOR` 和软件绘制 helper imports 只在 `chrome_command_stream/replay.rs`，`mod.rs` 只保留 re-export；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：如需进一步拆分，把 atlas subimage 采样从 `replay.rs` 下沉到 atlas 专属子模块；否则继续收缩 `paint_template_nodes/` 软件桥向 runtime extract/GPU command stream 边界。 |
| 2026-06-17 | 08 M3.S2 chrome command-stream atlas sampling responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/atlas.rs`，接管 atlas UV 校验、UV→像素矩形转换和 RGBA 子图采样；`replay.rs` 只在绘制 atlas image payload 时调用 `atlas_subimage_rgba(...)`，不再承载采样规则。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `atlas_subimage_rgba` 与 `atlas_uv_pixel_rect` 定义只在 `chrome_command_stream/atlas.rs`，`replay.rs` 只导入并调用采样 helper；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：`chrome_command_stream/` 主模块已收束到 stream 数据与构建入口，可继续收缩 `paint_template_nodes/` 软件桥或进入 runtime extract/GPU command stream 边界的下一批切换。 |
| 2026-06-17 | 08 M3.S2 chrome command-stream stream model responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/stream.rs`，接管 `ChromeCommandStream`、`ChromeCommand`、command layer/kind、image payload/UV 类型、surface size clamp 与 push/accessor 方法；`mod.rs` 现在只声明/转发子模块并保留 `build_chrome_command_stream(...)` 组装入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 stream 类型定义、`full_rebuild(...)`、`push_*`、`extend_commands(...)` 只在 `chrome_command_stream/stream.rs`，`mod.rs` 只保留 `use stream::...` 和 `mod stream;`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：`chrome_command_stream/mod.rs` 已降为组装入口，下一步应转向 `paint_template_nodes/` 软件桥收缩，或推进 runtime extract/GPU command stream 边界切换。 |
| 2026-06-17 | 08 M3.S2 template-node pipeline responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_node_pipeline.rs`，接管模板节点列表遍历、有效裁剪、command 收集、`draw_host_paint_commands(...)` 提交以及 test-only RGBA 入口；`template_nodes.rs` 只保留单节点命令展开、图像/材质反馈/text fallback 等节点绘制规则，并把行数从 1021 降到 755。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `draw_host_paint_commands`、`effective_template_clip`、入口测试与 `HostRgbaFrame` 绘制入口只在 `template_node_pipeline.rs`，`template_nodes.rs` 不再承载入口/全局裁剪/测试块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续把 `template_nodes.rs` 中图像 payload、material feedback progress/backdrop 或 text fallback 等软件绘制责任拆入专属模块，并逐步向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template material-feedback responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_material_feedback.rs`，接管 Material backdrop、linear/circular progress、indeterminate progress、progress 色彩解析与 circular progress 像素生成；`template_nodes.rs` 仅调用 `push_material_feedback_primitive_commands(...)`，并从 755 行降到 482 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `progress_*`、`circular_progress_*`、`MUI_BACKDROP_SCRIM`、`MATERIAL_PROGRESS_TRACK` 与 Material feedback public entry 只在 `template_material_feedback.rs`，`template_nodes.rs` 只保留单次调用；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_nodes.rs` 中图像 payload 或文本 fallback/表面 fallback 责任，之后再推进这些软件桥向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template image-payload responsibility split | 完成（功能代码、支持层编译阻塞修复、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_node_images.rs`，接管模板节点 image/icon payload 选择、目标矩形拟合、icon-only/leading-icon 判定、tint 解析与 `HostPaintCommand::image_pixels(...)` 生成；`template_nodes.rs` 只保留单节点调度、surface/text fallback 和对 image 模块的调用，并从 482 行降到 357 行。为打通本轮 editor 编译，修正 `zircon_runtime` render framework 对 `CompiledGraphCache`/`CompiledGraphCacheKey` 的旧 crate-root 导入，改为真实 owner `graphics::pipeline`，并修复 cache hit 分支中 `entries` 可变借用期间读取 `len()` 的借用冲突。 | `cargo fmt -p zircon_runtime -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `template_image_pixels`、`template_image_tint`、`raster_size_from_frame`、`image_rect_for_node`、`fitted_image_rect` 与 `template_node_has_image_source` 只在 `template_node_images.rs`，`template_nodes.rs` 只保留 image 调用和 text 布局所需 icon helper 引用；runtime cache 扫描确认无旧 `crate::CompiledGraphCache*` 导入残留；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 先暴露 runtime cache 导出/借用阻塞，最小修复后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_nodes.rs` 中 text fallback 或 surface fallback 责任，并逐步把这些软件桥向 runtime extract/GPU command stream 边界收束；后续 milestone testing stage 再扩展 focused/full 测试矩阵。 |
| 2026-06-17 | 08 M3.S2 template text-fallback responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_node_text.rs`，接管模板节点通用 text fallback 的 label 过滤、文本矩形布局、leading icon 文字避让、字体尺寸夹取、`UiTextRunPaintStyle` 默认样式与 `HostPaintCommand::text(...)` 生成；`template_nodes.rs` 只保留单节点调度、surface fallback 与 image/property/table/text fallback 调用顺序，并从 357 行降到 304 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `UiTextRunPaintStyle`、`text_color`、`text_rect_for_node`、`node_font_size`、`template_node_label` 与 icon/text helper 引用不再留在 `template_nodes.rs`，文本 fallback 入口和 helper 只在 `template_node_text.rs`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：拆出 `template_nodes.rs` 中 surface fallback/状态层 fallback 责任，或推进该软件桥到 runtime extract/GPU command stream 边界；后续 milestone testing stage 再扩展 focused/full 测试矩阵。 |
| 2026-06-17 | 08 M3.S2 template surface-fallback responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_node_surface.rs`，接管通用模板节点 surface fallback 的 frame-only 判定、surface 绘制资格、elevation shadow、border 判定、surface quad、state-layer/ripple 调用与 `HostPaintCommand::quad(...)` 生成；`template_nodes.rs` 只保留单节点调度和各专属 primitive/image/text/popup 调用顺序，并从 304 行降到 231 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `push_state_layer_commands`、`MATERIAL_ELEVATION_SHADOW_OPACITY`、`draws_surface(...)`、`draws_border(...)`、shadow/border/surface style helper 调用不再留在 `template_nodes.rs`，surface fallback 入口和 helper 只在 `template_node_surface.rs`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：`template_nodes.rs` 已收缩到单节点调度入口；下一步应推进该软件桥向 runtime extract/GPU command stream 边界，或拆分剩余专属 primitive 族中仍混合的职责。 |
| 2026-06-17 | 08 M3.S2 template status-glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_status_glyphs.rs`，接管 Workbench status signal/icon/chevron glyph 绘制、warning/check/info mark segment、icon 居中矩形与 mark width 归一化；`template_status_controls.rs` 保留 status 控件识别、布局、文本和按钮/chip/signal 调度，并从约 984 行降到 647 行，新 glyph 模块为 347 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `push_status_signal_icon`、`push_status_icon_glyph`、`push_down_chevron`、`push_snap_icon`、`push_world_icon`、`push_target_icon`、`push_warning_triangle`、`warning_mark_segments`、`push_check_mark`、`push_segments`、`centered_rect`、`local_rect*` 与 `normalized_status_mark_width` 定义只在 `template_status_glyphs.rs`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：优先推进 `paint_template_nodes/` 中剩余大型专属 primitive 文件的职责收束，或进入 runtime extract/GPU command stream 边界切换。 |
| 2026-06-17 | 08 M3.S2 template popup-row adornment responsibility split | 完成（功能代码、支持层编译阻塞修复、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_popup_row_adornments.rs`，接管 popup row 右侧勾选/箭头/add/open/save/delete glyph 绘制、dropdown 选中 adornment 选择、菜单 raw flag 解析和默认 icon 映射；`template_popup_rows.rs` 保留 popup option/menu row 调度、背景/surface/separator/label/shortcut/style 选择，当前为 670 行，新 adornment 模块为 323 行。为打通本轮 editor 编译，修正 `zircon_runtime/src/core/runtime/diagnostics/render_stats_store/product.rs` 对 `record_bool`/`record_bytes`/`record_count` 的父模块导入，使 product 子模块的 `super::record_*` 导入解析到真实 owner。 | `cargo fmt -p zircon_runtime -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo fmt -p zircon_runtime --check` 通过；源码扫描确认 `PopupRowAdornmentKind`、`push_popup_row_adornment`、`menu_item_has_flag`、`menu_item_flag_value`、`popup_row_adornment_from_icon`、`menu_item_default_icon`、各 glyph push helper、`push_segments`、`local_rect` 与 `menu_item_flags` 定义只在 `template_popup_row_adornments.rs`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆分 `paint_template_nodes/` 中剩余大型专属 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 visual asset SVG responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/visual_assets/svg.rs`，接管 SVG tree cache key、文件解析、font db 懒加载、font 需求扫描、SVG tree→pixels/image raster 化与 tint 应用；`visual_assets.rs` 保留 retained/template visual asset 调度、候选路径、像素 cache key、missing-icon fallback、tint 选择与 `HostPaintImagePixels`/target-size 模型，行数从 960 降到 777，新 SVG 模块为 199 行；`visual_assets/mui_icons.rs` 继续独立解析 MUI module，并复用 SVG 子模块的 tree raster 入口。参考边界来自 `dev/slint/internal/core/graphics/image/svg.rs` 的 SVG 子模块形态与 `dev/Fyrox/fyrox-ui/src/resources/` 的 UI 资源独立目录。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `render_svg_file_pixels`、`render_svg_tree_pixels`、`render_svg_file_image`、`render_svg_tree_image`、`parse_svg_tree_data`、`load_svg_tree`、`SvgTreeCacheKey`、`svg_tree_cache`、`cached_svg_font_db` 与 `svg_may_need_fonts` 定义只在 `visual_assets/svg.rs`，父模块只导入/调用文件级 SVG 入口，MUI icon 模块只复用 tree 入口；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆分 `paint_template_nodes/` 中真实混合职责的专属 primitive/asset 文件，或推进 retained visual asset/software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template inspector-row glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_inspector_row_glyphs.rs`，接管 inspector resource swatch、mesh cube、down-chevron 与 checked tick 像素 glyph 绘制；`template_inspector_rows.rs` 保留 inspector 行分类、resource/disclosure/shadow 行布局、文本、样式与状态调度，行数从 956 降到 784，新 glyph 模块为 187 行。参考边界来自 Fyrox inspector 框架/编辑器模块拆分形态：行语义与具体 editor/glyph 实现分开。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `push_inspector_swatch`、`push_inspector_cube_icon`、`push_inspector_down_chevron`、`push_inspector_check_tick`、`MATERIAL_SWATCH` 与 `INSPECTOR_SWATCH_SIZE` 只在 `template_inspector_row_glyphs.rs` 定义，`template_inspector_rows.rs` 只导入/调用 glyph 入口；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆分 `paint_template_nodes/` 中仍混合布局/语义/像素绘制的专属 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template icon-button glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_icon_button_glyphs.rs`，接管 Workbench icon-button glyph-kind 选择与 menu/file/folder/save/cursor/move/rotate/scale/snap/play/chevron/grid/sun/more/plus/trash/filter/cube/graph/image/audio/code/eye/eye-off/lock 像素 segment 绘制；`template_icon_buttons.rs` 保留 icon-button 识别、Workbench context、layout、surface 与 style 调度，行数从 919 降到 134，新 glyph 模块为 788 行。参考边界来自 Bevy stroke font/gizmos 的调用层与 glyph primitive 数据分离形态。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `IconGlyphKind`、`icon_glyph_kind(...)`、各 icon glyph push helper 与 `ICON_GRID` 定义只在 `template_icon_button_glyphs.rs`，`template_icon_buttons.rs` 只导入/调用 `push_icon_button_glyph(...)`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆分剩余 oversized specialized primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template button glyph and test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_button_glyphs.rs`，接管 Workbench button plus/trash/chevron glyph 选择与 segment 绘制；新增 `template_buttons_tests.rs` 承载原内联按钮绘制/样式/布局测试；`template_buttons.rs` 保留按钮识别、kind、layout offset、surface/text 内容排布、style/opacity/radius 调度，行数从 962 降到 246，新 glyph 模块为 109 行，测试模块为 614 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `push_segments(...)`、`scaled_rect(...)`、glyph segment geometry 与 `BUTTON_ICON_SIZE` 只在 `template_button_glyphs.rs`，`template_buttons.rs` 只导入/调用 `push_button_glyph(...)` 并通过 `#[path = "template_buttons_tests.rs"] mod tests;` 接入外置测试；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆分 `paint_template_nodes/` 中仍接近阈值的控件 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template tree-row glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_tree_row_glyphs.rs`，接管 Workbench scene tree disclosure/object/action glyph 选择与 cube/player-start/audio/eye/lock/kebab/chevron segment 绘制；`template_tree_rows.rs` 保留 tree-row 识别、surface、indent guides、label/actions layout、style/state 调度，行数从 755 降到 475，新 glyph 模块为 323 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `TreeIconKind`、`tree_icon_kind(...)`、object/action/chevron glyph push helper、`push_segments(...)` 与 `local_rect(...)` 只在 `template_tree_row_glyphs.rs`，`template_tree_rows.rs` 只导入/调用 tree-row glyph 入口；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆分 `paint_template_nodes/` 中仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template slider geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_slider_geometry.rs`，接管 Workbench slider track/value/range/tick/thumb rect、百分比/值文本/range min 解析、fill span 与像素对齐；`template_sliders.rs` 保留 slider 识别、绘制顺序、style/state、track/fill/tick/thumb/value/label paint command 发射，行数从 803 降到 651，新 geometry 模块为 163 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 slider geometry/value helper 定义只在 `template_slider_geometry.rs`，`template_sliders.rs` 只导入/调用 geometry 入口和少量布局常量；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 `SLIDER_HORIZONTAL_INSET`/`SLIDER_LABEL_WIDTH` 导入缺口，补齐后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：优先拆 `template_selection_controls.rs`、`template_segmented_controls.rs` 等仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template selection-control geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_selection_control_geometry.rs`，接管 checkbox/radio/toggle 的 leading mark、label rect、toggle track/thumb、radio dot、centered square 与尺寸/间距解析；`template_selection_controls.rs` 保留 checkbox/radio/toggle 识别、paint command 发射、text label、style/state/tone 调度，行数从 839 降到 722，新 geometry 模块为 125 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 selection-control geometry helper 与尺寸常量只在 `template_selection_control_geometry.rs`，`template_selection_controls.rs` 只导入/调用 geometry 入口；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 test-only geometry 常量导入 warning，改为 `#[cfg(test)]` 导入后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_segmented_controls.rs`、`template_viewport_scene*` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template selection-control test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_selection_controls_tests.rs` 承载原内联 checkbox/radio/toggle kind、geometry、style、painting 回归测试；`template_selection_controls.rs` 仅通过 `#[path = "template_selection_controls_tests.rs"] mod tests;` 接入测试，生产文件从 722 降到 323 行，测试模块为 398 行，geometry 模块保持 125 行。 | `cargo fmt -p zircon_editor` 通过；首次并行 `cargo fmt -p zircon_editor --check` 与格式化重叠报告测试文件格式差异，复跑 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认测试 helper 与像素断言只在 `template_selection_controls_tests.rs`，`template_selection_controls.rs` 只保留外置测试模块声明；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_segmented_controls.rs`、`template_viewport_scene*` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template segmented-control geometry and test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_segmented_control_geometry.rs`，接管 segmented/tab group label、body、segment、divider、selected inset/underline、tab underline、label rect 与 line-height 几何；新增 `template_segmented_controls_tests.rs` 承载原内联 segmented/tab option、geometry、style、painting 回归测试；`template_segmented_controls.rs` 保留控件识别、选项/选中值解析、style/state、文本和 quad command 发射，生产文件从 696 降到 354 行，新 geometry 模块为 137 行，测试模块为 259 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `segmented_body_rect`、`tab_paint_rect`、`segment_rect` 与 `inset_rect` 等几何 helper 定义只在 `template_segmented_control_geometry.rs`，`template_segmented_controls.rs` 只保留外置测试模块声明并导入/调用 geometry 入口；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_status_controls.rs`、`template_inspector_rows.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template status-control geometry and test responsibility split | 完成（功能代码、支持层编译阻塞修复、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_status_control_geometry.rs`，接管 status signal/chip/icon-button offset rect、icon slot、paint rect、text rect、chip chevron rect、glyph rect 与 line-height 几何；新增 `template_status_controls_tests.rs` 承载原内联 status kind、geometry、style、painting 回归测试；`template_status_controls.rs` 保留 status 控件识别、style/state、文本与 glyph/quad command 发射，生产文件从 709 降到 226 行，新 geometry 模块为 133 行，测试模块为 399 行。为打通本轮 editor 编译，`zircon_runtime/src/render_graph/mod.rs` 重新公开 `RenderGraphExternalResourceBinding`、`RenderGraphExternalResourceRequirement`、`RenderGraphExternalResourceType`，使 render feature descriptor 回到正常 `crate::render_graph` 导入路径。 | `cargo fmt -p zircon_runtime -p zircon_editor`、`cargo fmt -p zircon_runtime -p zircon_editor --check` 通过；源码扫描确认 status 几何 helper 定义只在 `template_status_control_geometry.rs`，`template_status_controls.rs` 只保留外置测试模块声明并导入/调用 geometry 入口；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 runtime render_graph 外部资源绑定类型 re-export 缺口，补齐后一次 184s 检查窗口超时且残留进程已清理，600s 复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_inspector_rows.rs`、`template_alerts.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template alert/toast glyph and test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_alert_glyphs.rs`，接管 inline alert 与 toast 的 info/success/warning/error mark、close glyph、segment scaling 与共享 icon-size 基线；新增 `template_alerts_tests.rs` 承载原内联 alert/toast kind、style、painting 回归测试；`template_alerts.rs` 保留 alert/toast 识别、standalone toast 判定、tone 解析、surface/text/action command sequencing 与 toast close/action layout，生产文件从 676 降到 264 行，新 glyph 模块为 164 行，测试模块为 252 行。 | `cargo fmt -p zircon_runtime -p zircon_editor` 与 `cargo fmt -p zircon_runtime -p zircon_editor --check` 通过；源码扫描确认 `push_alert_mark`、`push_close_mark`、`push_segments`、`scaled_rect` 与 `ALERT_ICON_SIZE` 只在 `template_alert_glyphs.rs` 定义，`template_alerts.rs` 只保留外置测试模块声明并导入/调用 glyph 入口；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_inspector_rows.rs`、`template_popup_rows.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template inspector-row geometry and test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_inspector_row_geometry.rs`，接管 Inspector row field、nested label、nested select、leading affordance、chevron 与 shadow-check rect 几何；新增 `template_inspector_rows_tests.rs` 承载原内联 inspector row kind、geometry、style、painting 回归测试；`template_inspector_rows.rs` 保留 Inspector row 分类、style/color 选择、文本、field surface、glyph dispatch 与 paint-command sequencing，生产文件从 722 降到 442 行，新 geometry 模块为 75 行，测试模块为 213 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `shadow_check_rect`、`shadow_check_content_offset_x`、`nested_label_rect`、`nested_select_field_rect`、`field_rect`、`leading_affordance_rect` 与 `chevron_rect` 只在 `template_inspector_row_geometry.rs` 定义，`template_inspector_rows.rs` 只保留外置测试模块声明并导入/调用 geometry 入口；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_popup_rows.rs`、`template_sliders.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes inspector-row geometry field/label/metric/shadow subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_inspector_row_geometry.rs` 中 Inspector row shared metrics、field/select/affordance/chevron rect helpers、nested label rect helper 与 shadow-check rect/content-offset helpers 分别拆入 `template_inspector_row_geometry/metrics.rs`、`fields.rs`、`labels.rs` 和 `shadows.rs`：根 `template_inspector_row_geometry.rs` 从 75 行降为 14 行 structural export entry；新增子文件分别为 16、40、14、21 行，并保持 `template_inspector_rows.rs` 与测试对 geometry helpers/constants 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes inspector-row geometry field/label/metric/shadow ownership scan 确认根 template_inspector_row_geometry 文件不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、raw geometry constants、field/label/chevron/shadow rect bodies 或 shadow content-offset body，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-17 | 08 M3.S2 template popup-row test responsibility split | 完成（功能代码、支持层编译阻塞修复、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_popup_rows_tests.rs`，承载原内联 popup menu/dropdown flag、style、painting 回归测试；`template_popup_rows.rs` 保留 popup option/menu dispatch、background/surface/separator drawing、label/shortcut text、adornment dispatch 与 row style selection，生产文件从 629 降到 348 行，新测试模块为 273 行。为打通本轮 editor 编译，`zircon_runtime/src/scene/ecs/query/query_state/mod.rs` 补回 root `QueryState` cache fields 需要的 `EntityId` 导入；`render_pipeline_asset/compile.rs` 仅由 rustfmt 整理 import 顺序。 | `cargo fmt -p zircon_runtime -p zircon_editor` 与 `cargo fmt -p zircon_runtime -p zircon_editor --check` 通过；源码扫描确认 `template_popup_rows.rs` 不再定义内联 `mod tests {`，也不再保留 `menu_item_flag_value` / `WORKBENCH_POPUP_ROW_DANGER_TEXT` 等 test-only imports，只通过 `#[path = "template_popup_rows_tests.rs"] mod tests;` 接入外置测试；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_sliders.rs`、`template_table_rows.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template slider test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_sliders_tests.rs`，承载原内联 Workbench slider recognition、declared style/color、geometry 与 painting 回归测试；`template_sliders.rs` 保留 slider recognition、style lookup、track/fill/tick/thumb/value/label command sequencing，生产文件从 600 降到 335 行，新测试模块为 248 行，`template_slider_geometry.rs` 继续作为 geometry/value helper owner（146 行）。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_runtime -p zircon_editor --check` 通过；源码扫描确认 `template_sliders.rs` 不再定义内联 `mod tests {`，也不再保留 `slider_label_color`、`slider_visual_state`、`changed_pixel_count`、`pixel_at`、`WORKBENCH_SLIDER_*`、`PALETTE` 等 test-only helper/import；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_table_rows.rs`、`template_fields.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template table-row test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_table_rows_tests.rs`，承载原内联 Workbench table row/header/tail cell parsing、declared offsets、style 与 painting 回归测试；`template_table_rows.rs` 保留 table row/header/tail 识别、cell text layout、surface/action glyph command sequencing 与 table-row style selection，生产文件从 571 降到 349 行，新测试模块为 216 行；拆分后移除生产文件中不再使用的 `PALETTE` import。 | `cargo fmt -p zircon_editor --check` 通过；源码扫描确认 `template_table_rows.rs` 不再定义内联 `mod tests {`，也不再保留 `WORKBENCH_TABLE_*`、`TABLE_HEADER_BG`、`TABLE_SELECTED_BG`、`TABLE_TAIL_BG`、`different_pixel_count`、`pixel_at`、`SharedString`、`UiPainterResolvedState`、`paint_theme::PALETTE` 等 test-only/helper import，只通过 `#[path = "template_table_rows_tests.rs"] mod tests;` 接入外置测试；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；scoped `git diff --check` 通过（仅 LF/CRLF working-tree warning）。`cargo fmt -p zircon_runtime -p zircon_editor --check` 被范围外 `zircon_runtime/src/graphics/feature/builtin_render_feature_descriptor/feature_descriptors/shadows.rs` rustfmt 差异阻断，未修改该文件。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene*`、`template_fields.rs`、`render_commands.rs`、`visual_assets.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template viewport-scene test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_viewport_scene_tests.rs`，承载原内联 Workbench viewport scene floor/grate/lighting/wall/prop/gizmo/container painting 回归测试；`template_viewport_scene.rs` 保留 viewport control-id 分类、pixel alignment、scene-kind dispatch 与对 floor/light/architecture/surfaces/structure 子模块的调用，生产文件从 642 降到 271 行，新测试模块为 374 行。 | `cargo fmt -p zircon_editor --check` 通过；viewport-scene ownership scan 为 clean，确认 `template_viewport_scene.rs` 不再保留内联 `mod tests {` 或 `TemplateNodeFrameData`、`paint_template_nodes_for_test*`、`UiRgbaColor`、`UiStyleColor`、`styled_node`、`pixel_at`、`fn luma` 等 test-only helper/import；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；scoped `git diff --check` 通过，仅 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene_structure.rs`、`render_commands.rs`、`visual_assets.rs`、`template_fields.rs` 或推进 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 viewport-scene gizmo responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_viewport_scene_gizmos.rs`，接管 Workbench viewport selection glow、axis line/origin、axis color/glow/cap 与 gizmo center 立方体/rod 绘制；`template_viewport_scene_structure.rs` 保留 base surface、floor grate、cargo/prop/rack/handrail 结构细节，生产文件从 689 降到 469 行，新 gizmo 模块为 223 行；`template_viewport_scene.rs` 改从 gizmo 模块导入 overlay/axis/gizmo 命令。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；viewport-scene gizmo ownership scan 为 clean，确认 `template_viewport_scene_structure.rs` 不再保留 `CYAN_GLOW`、`AXIS_*`、`GIZMO_*`、`push_selection_glow`、`push_axis_line`、`push_axis_origin`、`push_gizmo_center`、`axis_color`、`declared_surface_color`、`axis_glow`、`push_axis_cap`、`resolved_style_color`、`text_color` 等 overlay/gizmo helper/import；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；scoped `git diff --check` 通过，仅 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：继续拆 `template_viewport_scene_structure.rs` 的 floor grate/cargo/prop/rack/handrail 细节，或推进 `render_commands.rs`、`visual_assets.rs`、`template_fields.rs` 与 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 viewport-scene floor/prop responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_viewport_scene_props.rs`，接管 cargo detail、cargo inner frame、prop top/body、rack 与 handrail 结构细节；`push_floor_grate_slots(...)` 迁入已有 `template_viewport_scene_floor.rs`，与 floor grid/panel/seam 同属 floor 细节；`template_viewport_scene_structure.rs` 现在只保留共享 `push_base_surface(...)`，从 469 行降到 25 行，新 props 模块为 369 行，floor 模块为 282 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；viewport-scene floor/prop ownership scan 为 clean，确认 `template_viewport_scene_structure.rs` 不再保留 `GRATE_*`、`CARGO_*`、`PROP_*`、`HANDRAIL_*`、`push_floor_grate_slots`、`push_cargo_detail`、`push_cargo_inner_frame`、`push_prop_top_detail`、`push_prop_body_detail`、`push_rack_detail`、`push_handrail`、`template_corner_radius_from_rect`、`push_rect_line` 等 floor/prop helper；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；scoped `git diff --check` 通过，仅 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `render_commands.rs`、`visual_assets.rs`、`template_fields.rs`，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 render-command conversion responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/render_command_conversion.rs`，接管 runtime `UiRenderCommand` 到 retained `HostPaintCommand` 的转换，包括 runtime paint-element traversal、brush fill/border 解析、image resource 到 visual asset lookup、runtime text run/decorator 展开、UI frame conversion、text alignment fallback 与 style-color parsing；`render_commands.rs` 保留 `HostPaintCommand` 数据模型、排序回放、quad/text/image 软件绘制、placeholder image fallback、opacity 应用与测试 RGBA 入口，生产文件从 776 行降到 404 行，新转换模块为 345 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；render-command conversion ownership scan 为 clean，确认 `render_commands.rs` 不再保留 `push_runtime_command`、`push_runtime_paint_element`、`push_text_paint_commands`、`push_text_decorations`、`push_image_resource_command`、`brush_fill_color`、`brush_border`、`image_brush_resource`、`visual_asset_from_resource`、`resource_image_key`、`frame_from_ui`、`runtime_foreground_color`、`aligned_text_x`、`parse_style_color`、`parse_hex_pair`、`parse_nibble` 与 runtime conversion-only imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `visual_assets.rs`、`template_fields.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template list-row glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_list_row_glyphs.rs`，接管 Workbench list-row 右侧 adornment 选择、disabled/loading adornment 状态判断、adornment rect 几何、check mark、right chevron 与 disabled-diamond 像素段绘制；`template_list_rows.rs` 保留 list-row 识别、surface/background/border 选择、label text layout、style lookup 与带 style color 的 adornment dispatch，生产文件从 443 行降到 261 行，新 glyph 模块为 186 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；list-row glyph ownership scan 为 clean，确认 `template_list_rows.rs` 不再定义 `ListRowAdornmentKind`、`list_row_adornment_kind`、`is_unavailable_list_row_state`、`list_row_adornment_rect`、`push_check_mark`、`push_right_chevron`、`push_disabled_diamond`、`LIST_ROW_RIGHT_INSET` 与 `LIST_ROW_ADORNMENT_SIZE` 等 glyph/adornment helper（仅测试导入与断言保留）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `visual_assets.rs`、`template_fields.rs`、`template_dropdowns.rs`、`template_tooltips.rs` 或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template list-row test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_list_rows_tests.rs`，承载 list-row adornment-kind priority、selected/disabled painter coverage、declared surface/text/adornment style、shared-state priority、list node builders 与 pixel helper 回归测试；`template_list_rows.rs` 保留 list-row 识别、surface/background/border 选择、label text layout、style lookup 与通过 glyph owner 的 adornment dispatch，生产文件从 289 行降到 124 行，新测试模块为 161 行，`template_list_row_glyphs.rs` 继续作为 list-row adornment glyph owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；list-row test ownership scan 为 clean，确认 `template_list_rows.rs` 不再定义内联 `mod tests {`、`list_node`、`changed_pixel_count`、`pixel_at` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` / `UiPainterResolvedState` / `ListRowAdornmentKind` / `list_row_adornment_kind` / `PALETTE` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template field-stepper responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_field_stepper.rs`，接管 Workbench input stepper 宽度 reserve、divider line、up/down arrow segment 绘制、segment scaling 与 stepper 专属 paint-command 发射；`template_fields.rs` 保留 field 识别、surface/background/border 选择、value/label text layout、style lookup、stepper 宽度引用与 half-pixel 高度保留，生产文件从 446 行降到 370 行，新 stepper 模块为 82 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；field-stepper ownership scan 为 clean，确认 `template_fields.rs` 不再定义 `push_stepper`、`push_segments`、`scaled_rect`、`STEPPER_DIVIDER` 与 `STEPPER_WIDTH`，这些 helper/constant 只由 `template_field_stepper.rs` 接管；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `visual_assets.rs`、`template_dropdowns.rs`、`template_tooltips.rs` 或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 visual asset loading responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/visual_assets/loading.rs`，接管 first-existing-path 选择、pixel cache key/cache lookup/store、MUI module/SVG/raster image 加载、sprite-atlas attachment、intrinsic image loading、missing-icon fallback 像素生成；`visual_assets.rs` 保留 retained/template asset public entry、candidate path generation、tint priority、target-size model 与 `HostPaintImagePixels` / `RasterTargetSize` 数据形态，生产文件从 705 行降到 557 行，新 loading 模块为 155 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；visual-asset loading ownership scan 为 clean，确认 `visual_assets.rs` 不再定义 `load_pixels_from_candidates`、`load_image_from_candidates`、`missing_icon_pixels`、`load_image_from_path`、`image_pixels_cache_key` 与 `VISUAL_ASSET_CACHE`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；拆分后的 `load_image_from_candidates` 改为 test-only 导入，避免新增 production unused-import warning。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_dropdowns.rs`、`template_tooltips.rs`、`material_primitives.rs`、`mui_x_primitives.rs` 或继续把 retained visual asset/software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template dropdown glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_dropdown_glyphs.rs`，接管 Workbench dropdown chevron size/right/reserve、chevron rect、segment drawing 与 segment scaling；`template_dropdowns.rs` 保留 dropdown 识别、paint rect、surface/border/text command sequencing、label fallback、placeholder 判定与 style selection，生产文件从 395 行降到 370 行，新 glyph 模块为 71 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；dropdown glyph ownership scan 为 clean，确认 `template_dropdowns.rs` 不再定义 `push_dropdown_chevron`、`push_segments`、`scaled_rect`、`DROPDOWN_CHEVRON_SIZE`、`DROPDOWN_CHEVRON_RIGHT` 与 `DROPDOWN_CHEVRON_RESERVE`，这些 helper/constant 只由 `template_dropdown_glyphs.rs` 接管；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_tooltips.rs`、`material_primitives.rs`、`mui_x_primitives.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes dropdown glyph metric/chevron/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_dropdown_glyphs.rs` 中 Workbench dropdown chevron reserve/size/right metrics、chevron rect/style-color command dispatch 与 14px segment scaling 分别拆入 `template_dropdown_glyphs/metrics.rs`、`chevron.rs` 和 `segments.rs`：根 `template_dropdown_glyphs.rs` 从 67 行降为 6 行 structural export entry；新增子文件分别为 4、31、35 行，并保持 `template_dropdowns.rs` 与 `template_dropdowns/text.rs` 对 `push_dropdown_chevron` 和 `DROPDOWN_CHEVRON_RESERVE` 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes dropdown glyph metric/chevron/segment ownership scan 确认根 template_dropdown_glyphs 文件不再拥有 `FrameRect`/`HostPaintCommand`/`WorkbenchDropdownStyle` imports、chevron metrics constants、chevron rect body、segment loop 或 scaled-rect helper，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-17 | 08 M3.S2 template tooltip glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_tooltip_glyphs.rs`，接管 Workbench tooltip arrow size、icon size、diamond arrow drawing、info-icon placement 与 info mark segment drawing；`template_tooltips.rs` 保留 tooltip 识别、bubble rect、shadow/surface command sequencing、title/body text、style selection 与既有 tooltip painter/style 回归测试，生产文件从 448 行降到 305 行，新 glyph 模块为 149 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；tooltip glyph ownership scan 为 clean，确认 `template_tooltips.rs` 不再定义 `push_tooltip_arrow`、`push_diamond`、`push_tooltip_info_icon`、`tooltip_arrow_size`、`tooltip_icon_size`、`TOOLTIP_ARROW_SIZE` 与 `TOOLTIP_ICON_SIZE`，这些 helper/constant 只由 `template_tooltip_glyphs.rs` 接管；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `material_primitives.rs`、`mui_x_primitives.rs`、`template_shell_panels.rs`、`template_section_titles.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template section-title glyph responsibility split | 完成（功能代码、支持层编译阻塞修复、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_section_title_glyphs.rs`，接管 Workbench section title 的 icon kind 选择、title glyph color、Inspector cube / Transform cross / Mesh grid segment 绘制与 segment scaling；`template_section_titles.rs` 保留 section-title 识别、icon/text placement、label command sequencing、declared/disabled/text-tone color selection 与既有 painter/style 回归测试，生产文件从 434 行降到 263 行，新 glyph 模块为 148 行。本轮后续支持层清理确认 runtime DTO 调用方走 `crate::builtin` / `zircon_runtime::builtin`，`zircon_runtime/src/lib.rs` 未恢复 root DTO facade。 | `cargo fmt -p zircon_runtime -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；section-title glyph ownership scan 为 clean，确认 `template_section_titles.rs` 不再定义 `SectionTitleIcon`、`section_title_icon`、`push_section_icon`、`section_icon_color`、`push_cube_icon`、`push_transform_icon`、`push_mesh_icon`、`push_segments`、`scaled_rect`、`SECTION_ICON_SIZE`、`SECTION_ICON_GAP`、`SECTION_GLYPH` 与 `SECTION_TRANSFORM_GLYPH`，这些 helper/constant 只由 `template_section_title_glyphs.rs` 接管；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 stale root DTO consumer，迁到 builtin owner/确认旧路径扫描为 clean 后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `material_primitives.rs`、`mui_x_primitives.rs`、`template_shell_panels.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 material divider responsibility split | 完成（功能代码、支持层 root DTO 旧路径清理、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/divider.rs`，接管 MUI Divider role/surface-variant 识别、horizontal/vertical line extent、label placement、text align、divider/text color、font-size clamp 与 pixel alignment；`material_primitives.rs` 保留 Material primitive dispatch 与 shared Material style helper，生产文件从 524 行降到 101 行，新 divider 模块为 422 行。支持层确认 `RuntimePluginId`/`RuntimeTargetMode` 调用方走 builtin owner，`zircon_runtime/src/lib.rs` 不恢复 root DTO facade。 | `cargo fmt -p zircon_runtime -p zircon_editor` 与 `cargo fmt -p zircon_runtime -p zircon_editor --check` 通过；material divider ownership scan 为 clean，确认 `material_primitives.rs` 不再定义 `DIVIDER_*`、`MaterialPrimitiveKind`、`material_primitive_kind`、`push_horizontal_divider`、`push_vertical_divider`、`divider_is_vertical`、`divider_text_align`、`divider_color` 或 `pixel_aligned`；stale root DTO path scan 为 clean，只剩 `builtin::RuntimeTargetMode` owner 路径；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `mui_x_primitives.rs`、`template_shell_panels.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 Material Chip style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/chip/style.rs`，接管 MUI Chip palette 常量、filled/outlined background/foreground/border resolution、avatar/delete-icon color 与 color-token parsing；`material_primitives/chip.rs` 保留 Chip root/slot 识别、compact frame/label/delete/icon/avatar 几何、slot margin、label sizing 与 command sequencing。拆分后父文件为 460 行，新 style 模块为 139 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chip style ownership scan 为 clean，确认 `MUI_*` 常量与 `chip_*_color`/`chip_color_token`/`chip_palette_main` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 Material `chip.rs` 的几何/slot owner，或转向 `material_primitives/alert.rs`、`paint_primitives.rs`、retained software bridge/runtime extract 边界。 |
| 2026-06-18 | 08 M3.S2 Material Chip geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/chip/geometry.rs`，接管 MUI Chip compact frame、高度/半径、font fallback、label padding/width、avatar/icon/delete slot frame 与 negative slot margin；`material_primitives/chip.rs` 保留 Chip root/slot 识别、surface/avatar/icon/label/delete glyph command sequencing 与 delete-dot glyph emission。拆分后父文件为 284 行，`chip/geometry.rs` 为 216 行，`chip/style.rs` 为 139 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chip geometry ownership scan 为 clean，确认 `CHIP_*` geometry 常量、`chip_frame`、`chip_corner_radius`、label padding/font/slot edge/margin helper 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 Material `alert.rs`、retained software bridge/runtime extract 边界，或处理其他仍偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 Material Alert style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/alert/style.rs`，接管 MUI Alert severity palette 常量、standard/filled/outlined surface resolution、border color/width、corner radius、foreground/icon/action/cutout color 与 severity/color-token parsing；`material_primitives/alert.rs` 保留 Alert root/slot 识别、message/action layout、icon/close glyph emission 与 command sequencing。拆分后父文件为 366 行，新 style 模块为 160 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；alert style ownership scan 为 clean，确认 `MUI_*` 常量、`ALERT_DEFAULT_RADIUS`、`alert_*_color`、`alert_border_width`、`alert_corner_radius`、`alert_color_token`、`alert_is_filled`、`alert_is_outlined` 与 `pascal_case` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 Material `alert.rs` 的 geometry/action owner，或转向 retained software bridge/runtime extract 边界和其他大 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 Material Alert geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/alert/geometry.rs`，接管 MUI Alert pixel-aligned paint rect、message left/right bounds、action width/frame、icon frame、icon mark frame、font fallback 与 text width estimation；`material_primitives/alert.rs` 保留 Alert root/slot 识别、surface/icon/message/action/close command sequencing 与 close-dot glyph emission。拆分后父文件为 315 行，`alert/geometry.rs` 为 104 行，`alert/style.rs` 为 160 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；alert geometry ownership scan 为 clean，确认 `ALERT_PADDING_X`、`ALERT_ICON_EDGE/GAP/MARK_EDGE`、`ALERT_ACTION_EDGE/GAP/TRAILING`、`ALERT_FONT_SIZE`、`ALERT_TEXT_WIDTH_RATIO`、`alert_action_width`、`alert_font_size` 与 pixel alignment helper 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：转向 retained software bridge/runtime extract 边界，或处理 Material `divider.rs`/`avatar.rs` 等仍偏大的 primitive owner。 |
| 2026-06-18 | 08 M3.S2 Material Divider style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/divider/style.rs`，接管 Divider line/text color、disabled color routing、explicit style-color lookup 与 text-tone palette fallback；`material_primitives/divider.rs` 保留 Divider root 识别、horizontal/vertical extent、label placement、line command emission 与 text command sequencing。拆分后父文件为 427 行，新 style 模块为 29 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；divider style ownership scan 为 clean，确认 `divider_color`、`divider_text_color`、`PALETTE` 与 `resolved_style_color` 不再由父模块定义/导入；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 Divider geometry/label owner，或处理 Avatar/paint_primitives 等仍偏大的 retained host owner。 |
| 2026-06-18 | 08 M3.S2 Material Divider geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/divider/geometry.rs`，接管 Divider line thickness、middle/inset extent、wrapper padding、font-size clamp、horizontal/vertical orientation、line frame、label bounds/text frame、text alignment 与 pixel alignment；`material_primitives/divider.rs` 保留 Divider root 识别、label extraction、line/text command emission 与 command sequencing。拆分后父文件为 273 行，`divider/geometry.rs` 为 228 行，`divider/style.rs` 为 29 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；divider geometry ownership scan 为 clean，确认 `DIVIDER_*` geometry 常量、`horizontal_divider_extent`、`vertical_divider_extent`、label extent/font/text-align helper、`divider_is_vertical` 与 `pixel_aligned` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：转向 Avatar/Badge 等仍偏大的 Material primitive，或 retained software bridge/runtime extract 边界。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive dispatch/shared subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives.rs` 中 Material primitive child dispatch、text-field surface dispatch、component-variant token matching 与 first-non-empty helper 分别拆入 `material_primitives/dispatch.rs` 和 `shared.rs`：根 `material_primitives.rs` 从 71 行降为 16 行 structural export entry；新增子文件分别为 47、16 行，并保持 `template_nodes/fallback.rs` 与 material primitive child modules 对 `push_material_*`、`component_variant_contains`、`first_non_empty` 和 `resolved_style_color` 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes Material primitive dispatch/shared ownership scan 确认根 material_primitives 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、primitive dispatch chain、text-field dispatch body、component-variant token split body 或 first-non-empty helper body，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 Material Skeleton style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/skeleton/style.rs`，接管 Skeleton default background、wave highlight color、disabled opacity、resolved background/border lookup 与 style border width calculation；`material_primitives/skeleton.rs` 保留 Skeleton root/child recognition、text/circular frame calculation、corner-radius calculation、wave-frame placement 与 command sequencing。拆分后父文件为 123 行，`skeleton/style.rs` 为 41 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；skeleton style ownership scan 为 clean，确认 `resolved_style_color`、`SKELETON_DEFAULT_BG`、`SKELETON_WAVE`、`SKELETON_DISABLED_OPACITY`、`skeleton_color`、`skeleton_wave_color`、`skeleton_border_color`、`skeleton_border_width` 与 `skeleton_opacity` 不再由父模块定义/导入；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：拆分 Skeleton geometry owner 或转向 retained software bridge/runtime extract 边界。 |
| 2026-06-18 | 08 M3.S2 Material Skeleton geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/skeleton/geometry.rs`，接管 Skeleton text/circular frame calculation、rectangular/circular/rounded corner-radius resolution、wave-frame placement 与 configured radius lookup；`material_primitives/skeleton.rs` 保留 Skeleton root/child recognition、wave-state detection、surface/wave command sequencing 与 style/geometry dispatch。拆分后父文件为 73 行，`skeleton/geometry.rs` 为 61 行，`skeleton/style.rs` 为 41 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；skeleton geometry ownership scan 为 clean，确认 `SKELETON_TEXT_SCALE_Y`、`SKELETON_DEFAULT_RADIUS`、`SKELETON_WAVE_X_RATIO`、`SKELETON_WAVE_WIDTH_RATIO`、`skeleton_frame_for_variant`、`skeleton_corner_radius`、`skeleton_wave_frame` 与 `configured_corner_radius` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：Material primitive 子模块化已覆盖 Alert/Avatar/Badge/Chip/Divider/Skeleton/Timeline，下一步转向 retained software bridge/runtime extract 边界或其他仍混合职责的 retained-host 文件。 |
| 2026-06-18 | 08 M3.S2 Material Timeline style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/timeline/style.rs`，接管 Timeline outlined-state lookup、dot background/border/tone resolution、connector color fallback、dot border width、local MUI grey/secondary constants 与 timeline dot color-token parsing；`material_primitives/timeline.rs` 保留 Timeline primitive kind routing、separator consumption、dot/connector command emission、centered dot geometry 与 connector strip geometry。拆分后父文件为 135 行，`timeline/style.rs` 为 109 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；timeline style ownership scan 为 clean，确认 `PALETTE`、`resolved_style_color`、`MUI_*` constants、`TIMELINE_DOT_BORDER_WIDTH`、`timeline_dot_is_outlined`、`timeline_dot_background_color`、`timeline_dot_border_color`、`timeline_dot_border_width`、`timeline_connector_color`、`timeline_dot_tone_color` 与 `timeline_dot_color_token` 不再由父模块定义/导入；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：处理 Skeleton 或 retained software bridge/runtime extract 边界。 |
| 2026-06-18 | 08 M3.S2 Material Badge style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/badge/style.rs`，接管 Badge root background、root border、root text color、overlay palette token resolution、overlay contrast text、overlay border color 与 badge style border widths；`material_primitives/badge.rs` 保留 Badge root/slot 识别、root label extraction、invisible/dot/anchor state checks、overlay frame placement、text frame construction 与 command sequencing。拆分后父文件为 298 行，`badge/style.rs` 为 109 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；badge style ownership scan 为 clean，确认 `PALETTE`、`resolved_style_color`、`MUI_*` constants、`badge_overlay_background_color`、`badge_overlay_text_color`、`badge_overlay_border_color`、`badge_overlay_border_width`、`badge_root_background_color`、`badge_root_border_color`、`badge_root_border_width`、`badge_root_text_color` 与 `badge_color_token` 不再由父模块定义/导入；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：拆分 Badge geometry owner，或转向 retained software bridge/runtime extract 边界。 |
| 2026-06-18 | 08 M3.S2 Material Badge geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/badge/geometry.rs`，接管 Badge standard/dot sizing constants、root label frame calculation、overlay frame placement、standard/dot radius selection、overlay text frame calculation 与 anchor-origin overlap calculations；`material_primitives/badge.rs` 保留 Badge root/slot 识别、root label extraction、invisible/dot state checks、root/overlay/text command sequencing 与 root surface radius resolution。拆分后父文件为 198 行，`badge/geometry.rs` 为 138 行，`badge/style.rs` 为 109 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；badge geometry ownership scan 为 clean，确认 `BADGE_*` sizing constants、`badge_overlay_frame`、`badge_overlay_radius`、`badge_overlay_text_frame`、`badge_root_text_frame`、`badge_anchor_point`、`badge_is_left_anchored` 与 `badge_is_bottom_anchored` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：处理 Skeleton/Timeline 等仍未子模块化的 Material primitive，或转向 retained software bridge/runtime extract 边界。 |
| 2026-06-18 | 08 M3.S2 Material Avatar style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/avatar/style.rs`，接管 Avatar color-default fallback tone、disabled surface/text routing、explicit style-color lookup、text-tone palette fallback、border color 与 border width；`material_primitives/avatar.rs` 保留 Avatar root 识别、square/rounded/circular geometry、image/icon rasterization、alpha mask、fallback glyph/text/image/border command sequencing。拆分后父文件为 359 行，新 style 模块为 50 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；avatar style ownership scan 为 clean，确认 `MUI_GREY_600`、`avatar_background_color`、`avatar_foreground_color`、`avatar_border_color`、`avatar_border_width`、`PALETTE` 与 `resolved_style_color` 不再由父模块定义/导入；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 Avatar geometry/image-mask owner，或处理 Badge 等仍偏大的 Material primitive。 |
| 2026-06-18 | 08 M3.S2 Material Avatar geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/material_primitives/avatar/geometry.rs`，接管 Avatar default frame sizing、square/rounded/circular radius、fallback child centering、text font fallback 与 text frame sizing；`material_primitives/avatar.rs` 保留 Avatar root 识别、image/icon rasterization、alpha mask、fallback glyph/text/image/border command sequencing。拆分后父文件为 299 行，`avatar/geometry.rs` 为 78 行，`avatar/style.rs` 为 50 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；avatar geometry ownership scan 为 clean，确认 `AVATAR_DEFAULT_EDGE`、`AVATAR_ROUNDED_RADIUS`、`AVATAR_TEXT_FONT_RATIO`、`AVATAR_MIN_FONT_SIZE`、`AVATAR_TEXT_WIDTH_RATIO`、`avatar_frame`、`avatar_corner_radius`、`avatar_text_frame`、`avatar_font_size` 与 `centered_child_rect` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：处理 Badge 等仍偏大的 Material primitive，或转向 retained software bridge/runtime extract 边界。 |
| 2026-06-17 | 08 M3.S2 MUI X primitive responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/mui_x_primitives/{tree_view.rs,data_grid.rs,pickers.rs,chat.rs}`，分别接管 MUI X TreeView row state/indent marker、DataGrid header/row、Date/Time picker field/popup preview、AgentChat/ChatComposer bubble/composer 绘制；既有 `mui_x_primitives/charts.rs` 继续拥有 chart kind、plot、bar 与 raster chart，并接管 chart surface fallback；`mui_x_primitives.rs` 保留 MUI X primitive dispatch、role matching、shared quad/style helpers，生产文件从 505 行降到 120 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；MUI X primitive ownership definition scan 为 clean，确认 `mui_x_primitives.rs` 不再定义 `MUI_X_*` 常量、TreeView/DataGrid/Picker/Chat 具体 push helper 或具体 surface/row/chat color helper，根文件只保留对 child module 的 dispatch 调用；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 在当前 runtime temporal post-process 子模块落盘前暴露下层 transient E0583/E0599，复核后工作区已有 `gpu/post_process/temporal.rs` 且包含缺失 TAA/motion-vector 方法，复跑 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_shell_panels.rs`、`visual_assets.rs`、`template_dialogs.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template shell-panel separator responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_shell_panels/separators.rs`，接管 Workbench shell chrome pixel alignment、top/bottom/left/right separator line emission 与 drawer-column gap separator placement；`template_shell_panels.rs` 保留 shell panel control-id 识别、chrome style selection、surface fill 与 separator dispatch，生产文件从 407 行降到 244 行，新 separators 模块为 173 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；shell-panel separator ownership scan 为 clean，确认 `template_shell_panels.rs` 不再定义 `DRAWER_COLUMN_SEPARATOR_OFFSET`、`push_shell_panel_separators`、`push_top_line`、`push_bottom_line`、`push_left_line`、`push_right_line`、`push_horizontal_line`、`push_vertical_line` 或 `pixel_aligned_rect`，这些 helper/constant 只由 `template_shell_panels/separators.rs` 接管；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `visual_assets.rs`、`template_dialogs.rs`、`template_inspector_rows.rs`、`template_tree_rows.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template dialog action/style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_dialogs/{actions.rs,style.rs}`，分别接管 Dialog action label extraction、action width、confirm/cancel/default action layout、action text command emission，以及 unavailable state、severity parsing、surface/border/title/body/action color selection、confirm-enabled color rules；`template_dialogs.rs` 保留 Dialog/ConfirmDialog recognition、popup-open gating、surface/title/body command sequencing、severity mark dispatch、pixel alignment 与文本提取 helper，生产文件从 379 行降到 152 行，新 actions/style 模块分别为 98/148 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；dialog action/style ownership scan 为 clean，确认 `template_dialogs.rs` 不再定义 `DIALOG_ACTION_*`、dialog surface/border/title/body/action 色值常量、`DialogSeverity`、`push_dialog_actions`、`action_label`、`dialog_surface_color`、`dialog_border_color`、`dialog_title_color`、`dialog_body_color`、`dialog_action_color`、`cancel_action_color`、`confirm_action_color`、`confirm_enabled`、`severity`、`severity_mark_color`、`severity_border_color`、`action_width` 或 `variant_contains_any`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `visual_assets.rs`、`template_inspector_rows.rs`、`template_tree_rows.rs`、`template_style.rs` 或其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 visual asset candidate responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/visual_assets/candidates.rs`，接管 image/icon/template asset candidate generation、absolute direct path handling、asset URI normalization、SVG variant expansion、dedup、first-existing-path lookup 与 SVG path checks；`visual_assets.rs` 保留 retained/template asset public entry、tint priority、target-size model 与 `HostPaintImagePixels` / `RasterTargetSize` 数据形态，生产文件从 557 行降到 521 行，新 candidates 模块为 105 行，`visual_assets/loading.rs` 改通过候选路径 helper 读取 first-existing/SVG 判定。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；visual-asset candidate ownership scan 为 clean，确认 `visual_assets.rs` 不再定义 `image_candidates`、`template_image_candidates`、`icon_candidates`、`editor_dev_asset_root`、`workspace_root`、`push_direct_candidate`、`push_svg_variants`、`push_candidate`、`first_existing_path`、`is_svg_path` 或 `normalized_asset_relative_path`；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_inspector_rows.rs`、`template_tree_rows.rs`、`template_style.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained visual asset/software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template tree-row geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_tree_row_geometry.rs`，接管 Workbench tree-row font/row radius/guide color、disclosure/icon/action rect、indent guide x、label rect 与 line-height；`template_tree_rows.rs` 保留 tree-row 识别、surface/indent/text/action command sequencing、style/state 调度与既有回归测试，生产文件从 475 行降到 424 行，新 geometry 模块为 69 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；tree-row geometry ownership scan 为 clean，确认 `template_tree_rows.rs` 不再定义 `TREE_BASE_INSET_X`、`TREE_DISCLOSURE_SIZE`、`TREE_ICON_SIZE`、`TREE_TEXT_GAP`、`TREE_RIGHT_INSET`、`TREE_ACTION_SIZE`、`TREE_ACTION_GAP`、`TREE_ROW_RADIUS`、`TREE_GUIDE_STEP`、`TREE_GUIDE_COLOR`、`tree_disclosure_rect`、`tree_icon_rect` 或 `tree_action_rect` 等 geometry constant/helper；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_inspector_rows.rs`、`template_style.rs`、`template_property_rows.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template inspector-row kind responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_inspector_row_kind.rs`，接管 Inspector property-row control-id recognition、resource/disclosure/shadow row kind classification、resource kind enum、material/mesh row ids 与 boolean display/value parsing；`template_inspector_rows.rs` 保留 resource/disclosure/shadow paint-command sequencing、field surface/text emission、style/color selection 与 glyph/geometry dispatch，生产文件从 473 行降到 399 行，新 kind 模块为 82 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；inspector-row kind ownership scan 为 clean，确认 `template_inspector_rows.rs` 不再定义 `InspectorRowKind`、`InspectorResourceKind`、`inspector_row_kind`、`is_inspector_property_row`、`bool_display_value`、`bool_value`、`matches_ignore_ascii_case`、`COMPONENT_PROPERTY_VIRTUAL_PREFIX`、`COMPONENT_PROPERTY_SLOT_04` 或 `MESH_PROPERTY_ROW`；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；窄 `cargo test -p zircon_editor --lib template_inspector_rows --locked --jobs 1 --no-run --message-format short --color never` 304 秒超时，无通过/失败结果且无残留进程，未作为通过证据。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_style.rs`、`template_property_rows.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes inspector-row kind enum/classifier/value subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_inspector_row_kind.rs` 中 Inspector row/resource enum、public row constants、row-kind classification、case-insensitive material matching 与 boolean display/value parsing 分别拆入 `template_inspector_row_kind/kind.rs`、`constants.rs`、`classifier.rs`、`matching.rs` 和 `bool_values.rs`：根 `template_inspector_row_kind.rs` 从 72 行降为 10 行 structural export entry；新增子文件分别为 12、6、43、5、13 行，并保持 `template_inspector_rows.rs` 与测试对 `InspectorRowKind`、`InspectorResourceKind`、`inspector_row_kind`、`bool_*` 和 row constants 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes inspector-row kind enum/classifier/value ownership scan 确认根 template_inspector_row_kind 文件不再拥有 `TemplatePaneNodeData` import、enum bodies、row-id constant bodies、classification branch body、bool parser body 或 case-insensitive matcher body，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-17 | 08 M3.S2 template property-row axis parsing responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_property_axis_values.rs`，接管 Workbench PropertyRow axis-token parsing、axis/value grouping、`PropertyAxisValue` 数据形态与 X/Y/Z/W token 判定；`template_property_rows.rs` 保留 interim PropertyRow recognition、label/scalar/axis field paint-command sequencing、label width、value field rect 与 style/color selection，生产文件从 339 行降到 298 行，新 axis 模块为 41 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；property-row axis ownership scan 为 clean，确认 `template_property_rows.rs` 不再定义 `PropertyAxisValue`、`property_axis_values`、`push_current_axis_value` 或 `is_axis_token`；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_style.rs`、其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template style color responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_style_color.rs`，接管 shared `UiStyleColor`/material-role resolution、MUI overlay/on-dark constants、typed button container/tone color、primary-contained button 判定；`template_style.rs` 保留 disabled/elevation/border/radius 与 surface/border/text style sequencing，生产文件从 404 行降到 306 行，新 style-color 模块为 104 行。`material_primitives.rs`、`mui_x_primitives.rs`、style selector 子模块以及 image/material/viewport/inspector consumers 改为直接依赖 style-color owner，避免通过 `template_style.rs` 暴露颜色解析。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template style-color ownership scan 为 clean，确认 `template_style.rs`、`material_primitives.rs`、`mui_x_primitives.rs` 不再定义 `resolved_style_color`、`typed_button_variant_background`、`typed_button_tone_color`、`button_container_color`、`is_primary_button_color`、`material_role_color`、`is_primary_contained_button` 或 `MUI_*` 色值常量；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 style selector 旧导入，迁到 `template_style_color` 后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-17 | 08 M3.S2 template chip glyph responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_chip_glyphs.rs`，接管 Workbench chip chevron reserve/size/right constants、chevron availability 判定、chevron rect、segment drawing 与 segment scaling；`template_chips.rs` 保留 chip 识别、surface/border/text/glyph color selection、label text command sequencing 与 pixel-aligned chip frame，生产文件从 315 行降到 248 行，新 glyph 模块为 78 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chip glyph ownership scan 为 clean，确认 `template_chips.rs` 不再定义 `push_chip_chevron`、`chip_has_chevron`、`push_segments`、`scaled_rect`、`CHIP_CHEVRON_SIZE`、`CHIP_CHEVRON_RIGHT` 或 `CHIP_CHEVRON_RESERVE`；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes chip glyph identity/metrics/chevron/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_chip_glyphs.rs` 中 Workbench chip chevron availability、reserve/size/right metrics、chevron rect/segment command emission 与 12px segment scaling 分别拆入 `template_chip_glyphs/identity.rs`、`metrics.rs`、`chevron.rs` 和 `segments.rs`：根 `template_chip_glyphs.rs` 从 73 行降为 8 行 structural export entry；新增子文件分别为 9、3、34、35 行，并保持 `template_chips.rs` 和 `template_chips/text.rs` 对 `chip_has_chevron`/`push_chip_chevron`/`CHIP_CHEVRON_RESERVE` 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes chip glyph identity/metrics/chevron/segment ownership scan 确认根 template_chip_glyphs 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、chevron constants、availability match body、chevron rect body、segment loop 或 scaled-rect helper，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 template chip test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_chips_tests.rs`，承载 Workbench chip recognition、viewport chip surface/border/text/chevron painter coverage、focused-border coverage、chip node builders 与 pixel helper 回归测试；`template_chips.rs` 保留 chip 识别、surface/border/text/glyph color selection、label text command sequencing、pixel-aligned chip frame 与 glyph child-module dispatch，生产文件从 248 行降到 162 行，新测试模块为 85 行，`template_chip_glyphs.rs` 继续作为 chevron glyph owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chip test ownership scan 为 clean，确认 `template_chips.rs` 不再定义内联 `mod tests {`、`chip_node`、`changed_pixel_count`、`pixel_at` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template axis-value-field style responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_axis_value_field_style.rs`，接管 Workbench axis value field surface/background/border/text color constants、hover/pressed/disabled/invalid/focused border rules、border width 与 declared value-color text override；`template_axis_value_fields.rs` 保留 Transform axis-value field recognition、compact field geometry、value text fallback、text command sequencing 与既有测试，生产文件从 322 行降到 264 行，新 style 模块为 70 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；axis-value-field style ownership scan 为 clean，确认 `template_axis_value_fields.rs` 不再定义 `axis_field_background`、`axis_field_border`、`axis_field_border_width`、`axis_field_text_color` 或 `AXIS_FIELD_*` style constants；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template axis-value-field test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_axis_value_fields_tests.rs`，承载 Transform axis-value field recognition、compact field painter coverage、focused/disabled style coverage、declared value-color coverage、node builders 与 pixel helper 回归测试；`template_axis_value_fields.rs` 保留 axis-value field 识别、compact geometry、value fallback text、text command sequencing 与仅 test-build 可见的外置测试模块入口，生产文件从 264 行降到 130 行，新测试模块为 131 行，`template_axis_value_field_style.rs` 继续作为 style owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；axis-value-field test ownership scan 为 clean，确认 `template_axis_value_fields.rs` 不再定义内联 `mod tests {`、`axis_node`、`label_node`、`changed_pixel_count`、`pixel_at` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` / `PALETTE` / `AXIS_FIELD_*` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template axis-label test responsibility split | 完成（功能代码、支持层编译阻塞修复、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_axis_labels_tests.rs`，承载 Transform axis-label recognition、scale-link recognition、compact axis-text painter coverage、audited axis-tone coverage、scale-link offset coverage、node builders、frame rect helper 与 pixel helper 回归测试；`template_axis_labels.rs` 保留 axis-label/scale-link 识别、text/quad command sequencing、color selection、scale-link glyph placement 与仅 test-build 可见的外置测试模块入口，生产文件从 301 行降到 176 行，新测试模块为 103 行。验证阶段暴露 lower-layer runtime final-target alias iterator 类型阻塞，已在 `bind_frame_graph_resources.rs` 中改为按 `for &alias` / `for &resource` 迭代 `FINAL_TARGET_ALIASES`，保持最终目标别名作为 `&str` 进入 graph resource binding。 | `cargo fmt -p zircon_runtime -p zircon_editor` 与 `cargo fmt -p zircon_runtime -p zircon_editor --check` 通过；axis-label test ownership scan 为 clean，确认 `template_axis_labels.rs` 不再定义内联 `mod tests {`、`label_node`、`frame_rect`、`changed_pixel_count` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template field test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_fields_tests.rs`，承载 Workbench field recognition、surface/border/text、focused/disabled state、stepper glyph、layout-offset 与 half-pixel height 回归测试；`template_fields.rs` 保留 field 识别、surface/text command sequencing、label fallback、style lookup、stepper dispatch 与 half-pixel paint rect，生产文件从 414 行降到 151 行，新测试模块为 257 行，`template_field_stepper.rs` 继续作为 stepper glyph owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；field test ownership scan 为 clean，确认 `template_fields.rs` 不再定义内联 `mod tests {`、`changed_pixel_count`、`pixel_at`、test node builders、`field_surface`、`field_border`、`field_text_color` 或 `field_visual_state`；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template dropdown test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_dropdowns_tests.rs`，承载 Workbench dropdown recognition、surface/border/text/chevron color、popup-row paint、layout-offset、brightness 与 half-pixel height 回归测试；`template_dropdowns.rs` 保留 dropdown 识别、paint rect、surface/text command sequencing、label fallback、placeholder 判定、style lookup 与 chevron dispatch，生产文件从 370 行降到 120 行，新测试模块为 244 行，`template_dropdown_glyphs.rs` 继续作为 chevron glyph owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；dropdown test ownership scan 为 clean，确认 `template_dropdowns.rs` 不再定义内联 `mod tests {`、`changed_pixel_count`、`pixel_at`、test node builders、`dropdown_surface`、`dropdown_border`、`dropdown_text_color`、`dropdown_chevron_color` 或 `dropdown_visual_state`；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template tooltip test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_tooltips_tests.rs`，承载 Workbench tooltip bubble/arrow/info-icon painter coverage 与 shared-state priority style 回归测试；`template_tooltips.rs` 保留 tooltip 识别、bubble rect/layout、shadow/surface command sequencing、title/body text、style selection 与 glyph dispatch，生产文件从 305 行降到 200 行，新测试模块为 104 行，`template_tooltip_glyphs.rs` 继续作为 arrow/info-icon glyph owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；tooltip test ownership scan 为 clean，确认 `template_tooltips.rs` 不再定义内联 `mod tests {`、`tooltip_node`、`changed_pixel_count`、`pixel_at` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` / `UiPainterResolvedState` / `WORKBENCH_TOOLTIP_*` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template section-title test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_section_titles_tests.rs`，承载 Workbench section-title recognition、bold-label paint、leading-icon paint、audited mesh title tone、declared label color、transform-icon opacity 与 pixel-painter 回归测试；`template_section_titles.rs` 保留 section-title 识别、icon/text placement、label command sequencing、declared/disabled/text-tone color selection、pixel alignment 与 glyph dispatch，生产文件从 286 行降到 166 行，新测试模块为 118 行，`template_section_title_glyphs.rs` 继续作为 section-title icon glyph owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；section-title test ownership scan 为 clean，确认 `template_section_titles.rs` 不再定义内联 `mod tests {`、`title_node`、`changed_pixel_count`、`pixel_at` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` / `SectionTitleIcon` / `SECTION_TRANSFORM_GLYPH` / `section_icon_color` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 template tree-row test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/template_tree_rows_tests.rs`，承载 Workbench tree-row recognition、selected/nested/collapsed/loading painter coverage、shared-state priority style 与 pixel helper 回归测试；`template_tree_rows.rs` 保留 tree-row 识别、surface/indent/text/action command sequencing、style/color lookup、glyph dispatch、lock-action 判定与 geometry/glyph child-module dispatch，生产文件从 425 行降到 223 行，新测试模块为 200 行，`template_tree_row_geometry.rs` / `template_tree_row_glyphs.rs` 继续作为 geometry/glyph owners。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；tree-row test ownership scan 为 clean，确认 `template_tree_rows.rs` 不再定义内联 `mod tests {`、`tree_node`、`changed_pixel_count`、`pixel_at` 或测试专用 `TemplateNodeFrameData` / `paint_template_nodes_for_test` / `model_rc` / `UiPainterResolvedState` / `WORKBENCH_TREE_ROW_TEXT_SELECTED` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进其他仍混合控件语义/布局/像素绘制的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 visual-asset test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_template_nodes/visual_assets_tests.rs`，承载 runtime/template SVG target-size coverage、editor-pages 16px icon readability coverage、MUI icon module/ligature coverage、missing-icon fallback、SVG font-scan guard、projected preview image authority、tint-priority coverage 与 pixel helper；`visual_assets.rs` 保留 retained/template visual asset public lookup、target sizing、tint selection、resource cache key 与 `HostPaintImagePixels` / `RasterTargetSize` 数据形态，生产文件从 470 行降到 235 行，新测试模块为 284 行，`visual_assets/{candidates,loading,svg,mui_icons}.rs` 继续作为候选路径、加载、SVG 和 MUI module owners。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；visual-asset test ownership scan 为 clean，确认 `visual_assets.rs` 不再定义内联 `mod tests {`、`has_visible_pixel`、`EDITOR_PAGES_WIRED_TEMPLATE_ICONS`、`IconReadabilityFootprint`、`icon_readability_footprint`、`solid_preview_image` 或测试专用 `load_image_from_candidates` 导入；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 focused/full Cargo 测试矩阵。 | 继续 M3.S2：推进 `template_node_pipeline.rs`、`template_style.rs`、`template_property_rows.rs`、`template_shell_panels.rs` 或其他仍含内联测试/混合职责的 primitive 文件，或继续把 retained software bridge 向 runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 remaining inline-test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `material_state_layer_tests.rs`、`sprite_atlas_tests.rs`、`template_node_labels_tests.rs`、`template_node_pipeline_tests.rs`、`template_property_rows_tests.rs`、`template_shell_panels_tests.rs`、`template_style_tests.rs`，分别承载 Material state-layer/ripple、sprite atlas resolver、template label、template pipeline clip/popup、PropertyRow scalar/axis、Workbench shell-panel chrome 与 template style priority 回归测试；父文件只保留生产逻辑和 test-build 外置入口，行数分别为 `material_state_layer.rs` 117、`sprite_atlas.rs` 143、`template_node_labels.rs` 63、`template_node_pipeline.rs` 82、`template_property_rows.rs` 233、`template_shell_panels.rs` 87、`template_style.rs` 204。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；remaining inline-test ownership scan 为 clean，确认 `paint_template_nodes/` 下不再存在内联 `mod tests {`，上述父文件不再保留被移动的测试函数/helper；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：剩余工作转向 retained software bridge/runtime extract/GPU command stream 边界收束，或继续拆分仍偏大的具体 primitive/glyph owner（例如 `template_icon_button_glyphs.rs`）。 |
| 2026-06-18 | 08 M3.S2 template icon-button glyph catalog responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `template_icon_button_glyph_kind.rs`、`template_icon_button_glyph_segments.rs`、`template_icon_button_glyph_shapes.rs`，分别承载 Workbench icon-button glyph-kind mapping、16x16 segment scaling/quad emission、以及 menu/file/folder/save/cursor/move/rotate/scale/snap/play/chevron/grid/sun/more/plus/trash/filter/cube/graph/image/audio/code/eye/eye-off/lock segment lists；`template_icon_button_glyphs.rs` 现在只保留 icon-button glyph entry 和 pressed-state overlay dispatch，生产文件从 788 行降到 39 行，新 kind/segments/shapes owner 分别为 90/46/659 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；icon-button glyph catalog ownership scan 为 clean，确认 `template_icon_button_glyphs.rs` 不再定义 glyph-kind enum/mapping、`ICON_GRID`、segment scaling 或具体 glyph shape 函数；remaining inline-test scan 仍为 clean；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 runtime support-layer 的 `SceneRendererAdvancedPluginReadbacks::from_outputs_and_external_buffer_bindings(...)` 缺口，确认该 support owner 已补齐后复跑同一命令通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：优先把 retained software bridge/runtime extract/GPU command stream 边界继续收束，或继续拆分仍偏大的 primitive/glyph owner（例如新 `template_icon_button_glyph_shapes.rs`）。 |
| 2026-06-18 | 08 M3.S2 template icon-button glyph shape-family responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 删除单文件 `template_icon_button_glyph_shapes.rs`，新增 `template_icon_button_glyph_shapes/mod.rs` 作为 glyph shape dispatch；新增 `actions.rs`、`assets.rs`、`chrome.rs`、`files.rs`、`tools.rs`、`visibility.rs`，分别承载 plus/trash/filter、cube/graph/image/audio/code、play/chevron/grid/sun/more、menu/file/folder/save、cursor/move/rotate/scale/snap、eye/eye-off/lock segment lists。拆分后行数为 `mod.rs` 96、`actions.rs` 67、`assets.rs` 129、`chrome.rs` 117、`files.rs` 100、`tools.rs` 127、`visibility.rs` 69。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；shape-family ownership scan 为 clean，确认旧 `template_icon_button_glyph_shapes.rs` 已删除，父 `mod.rs` 只做 kind dispatch，各 child module 拥有对应 glyph push helper；root paint_template_nodes/shape-family inline-test scan 仍为 clean；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 通过，仅输出既有 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或拆分其他仍偏大的具体 primitive/glyph owner。 |
| 2026-06-18 | 08 M3.S2 paint primitive image responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_primitives/image.rs`，接管 retained software image draw entry、resource-key/atlas recording metadata、content-scoped RGBA key hashing、opaque identity-row fast path、scaled RGBA sampling、alpha blending 和 image-specific regression tests；`paint_primitives.rs` 保留 rect/rounded-rect/border/separator/text-marker/pixel helper，生产/测试混合文件从 918 行降到 510 行，新 image 子模块为 420 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；paint primitive image ownership scan 为 clean，确认 `ImageRecordingMetadata`、`rgba_resource_key`、`draw_scaled_rgba_image_pixels`、`try_copy_opaque_identity_image_rows`、`write_rgba_pixel` 与 `HostPaintAtlasImage` 只在 `paint_primitives/image.rs`，父模块只声明 `mod image;` 并转发 resource-key/atlas image draw entry。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或拆分 `paint_workbench_renderer.rs`/`native_pointer.rs` 等仍明显偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 workbench welcome paint responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_workbench_renderer/welcome.rs`，接管 Welcome pane native content paint、welcome 专属颜色/布局常量、recent-project list、new-project form、status/validation/action button 绘制、template node frame lookup 与 fallback frame 约束；`paint_workbench_renderer.rs` 保留 workbench window paint 调度、dock/pane/viewport/menu/fallback 等其他区域，父文件从约 1957 行降到 1428 行，新 welcome 子模块为 564 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；workbench welcome ownership scan 为 clean，确认 `WELCOME_*`、`draw_welcome_*`、`welcome_node_frame`、`inset_frame` 与 `constrain_welcome_content` 只在 `paint_workbench_renderer/welcome.rs`，父模块只保留 `mod welcome;` 与 `welcome::draw_welcome_native_content(...)` 调用。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 `paint_workbench_renderer.rs` 的菜单/窗格/层级或 `native_pointer.rs` 等仍偏大的 host_contract owner，或推进 retained software bridge 到 runtime extract/GPU command stream 边界。 |
| 2026-06-18 | 08 M3.S2 workbench menu paint responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_workbench_renderer/menus.rs`，接管 top-bar menu labels、open menu popup、submenu popup chain、popup row text/shortcut/hover paint、menu popup row geometry、menu/submenu viewport constraint 与 menu-bar scroll frame；`paint_workbench_renderer.rs` 保留 workbench window paint 调度与 `menus::draw_menu_bar_labels(...)` / `menus::draw_open_menu_popup(...)` 调用。拆分后父文件为 1159 行，`menus.rs` 287 行，`welcome.rs` 564 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；workbench menu ownership scan 为 clean，确认 `HostMenuChromeItemData`、`draw_menu_bar_labels`、`draw_open_menu_popup`、`draw_open_submenu_popups`、`draw_menu_popup_rows`、`menu_popup_row_frame`、`menu_popup_height`、`constrained_submenu_popup_frame`、`constrained_menu_popup_frame` 与 `scrolled_menu_frame` 只在 `paint_workbench_renderer/menus.rs`。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 `paint_workbench_renderer.rs` 的 dock/pane/hierarchy/asset/floating/debug marker 区域或 `native_pointer.rs` 等仍偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 workbench native pane paint responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_workbench_renderer/native_panes.rs`，接管 viewport image paint、RuntimeDiagnostics overlay、Welcome/Hierarchy/Assets/AssetBrowser native pane dispatch、hierarchy row paint 与 viewport frame lookup、asset tree hover row frame/paint；`paint_workbench_renderer.rs` 保留 pane orchestration 并调用 `native_panes::draw_viewport_image(...)`、`native_panes::draw_native_pane_content(...)` 与 `native_panes::draw_pane_debug_overlay(...)`。拆分后父文件为 894 行，`native_panes.rs` 222 行，`menus.rs` 287 行，`welcome.rs` 564 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；workbench native pane ownership scan 为 clean，确认 viewport/native pane/debug overlay、hierarchy row 与 asset tree hover helper 不再由父模块定义。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：`paint_workbench_renderer.rs` 已降至 1000 行以下，后续可继续拆分 dock/floating/window chrome 区域，或回到 retained software bridge/runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 native pointer routing/redraw responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/native_pointer/routing.rs` 与 `host_contract/native_pointer/redraw_result.rs`；routing 接管 top-level chrome、pane/floating-window/template-node/viewport-toolbar hit route 与 `ChromePointerRoute`/`PanePointerRoute`/`PanePointerTarget` route payload；redraw_result 接管 pointer-move hover、Workbench template-node hover、resize、chrome press、tab-drag release 与 hierarchy row damage 到 `NativePointerDispatchResult` 的包装。`native_pointer.rs` 保留 root move/button/scroll dispatch、resize/tab-drag transient state、text-input focus timing 与 host callback 调用，父文件从 1577 行降到 907 行，新 `routing.rs` 497 行、`redraw_result.rs` 122 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；native pointer routing/redraw ownership scan 为 clean，确认 `route_*`/route payload/geometry helper 与 redraw helper 不再由父模块定义。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained host software bridge/runtime extract/GPU command stream 边界，或拆分其他仍偏大的 host_contract owner（如 `window.rs`、`profiling_artifacts.rs`）。 |
| 2026-06-18 | 08 M3.S2 host window event-loop/template-hover responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/window/event_loop.rs` 与 `host_contract/window/template_hover.rs`；event_loop 接管 winit `ApplicationHandler`、native window creation、GPU/softbuffer presenter selection、winit pointer/keyboard/IME/wheel dispatch、external redraw draining、present scenario attribution 与 profiling artifact export；template_hover 接管 cloned presentation 上的 Workbench template-node hover、structured dropdown option hover 与 popup menu row hover 覆盖。`window.rs` 保留 `UiHostWindow`/`HostWindowHandle` 状态 API、presentation 聚合、text-input focus editing、unhandled keyboard forwarding、external redraw queue 与 snapshot entry，父文件从 1294 行降到 801 行，新 `event_loop.rs` 361 行、`template_hover.rs` 159 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、window event-loop/template-hover ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；扫描确认 winit event-loop helper 与 template-hover mutation helper 不再由 `window.rs` 定义。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：下一个大 owner 可处理 `profiling_artifacts.rs`，或转回 retained software bridge/runtime extract/GPU command stream 边界收束。 |
| 2026-06-18 | 08 M3.S2 profiling artifact test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/profiling_artifacts/tests.rs`，承载 profile geometry 的 absolute splitter/tab frame、template clip、viewport-toolbar clip 与 top-hit 回归测试；`profiling_artifacts.rs` 保留 profiling artifact export、geometry model、hit sample construction、surface-frame control collection 与 profile output path helper，父文件从约 1035 行降到 824 行，新测试模块为 211 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling artifact ownership scan 为 clean，确认父文件不再定义内联 `mod tests {`、profile geometry 测试函数或 `viewport_toolbar_surface_frame_for_test` helper；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：转向 retained software bridge/runtime extract/GPU command stream 边界收束，或继续拆分仍偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 workbench dock/pane paint responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_workbench_renderer/docks.rs`，接管 side/document/bottom dock shell、panel header、pane orchestration、viewport toolbar、template-node pane dispatch、pane fallback label、activity-rail active marker 与 floating-window body paint；`paint_workbench_renderer.rs` 保留 window-level root skeleton、host-scene paint order、componentized draw、resize layer、top/status marker、root overlay 与 root-frame fallback。拆分后父文件为 434 行，新 `docks.rs` 516 行，`native_panes.rs` 233 行、`menus.rs` 287 行、`welcome.rs` 564 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench dock/pane ownership scan 为 clean，确认 `draw_side_dock`、`draw_document_dock`、`draw_bottom_dock`、`draw_panel_header`、`draw_pane`、`draw_pane_template_nodes`、`draw_viewport_toolbar`、`draw_pane_fallback`、`draw_active_rail_marker`、`draw_floating_layer`、`draw_floating_window` 与 `SaturatingAddF32` 不再由父模块定义；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或拆分 `window.rs`/`native_pointer.rs` 等仍偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 host window test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/window/tests.rs`，接管 refresh diagnostics overlay、close-request callback reentrant-borrow safety、frame-update redraw region 和 completed frame-update scenario one-shot 回归；`window.rs` 保留 host state API、presentation 聚合、text-input focus editing、unhandled keyboard forwarding、external redraw queue 与 snapshot entry，只保留 `#[cfg(test)] mod tests;` 声明。拆分后 `window.rs` 为 817 行，`window/tests.rs` 83 行，`window/event_loop.rs` 381 行，`window/template_hover.rs` 168 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window test ownership scan 为 clean，确认四个窗口回归测试只在 `window/tests.rs` 定义，父模块不再包含内联 `mod tests {`；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或继续拆分仍偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 native pointer drag/resize responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/native_pointer/drag_resize.rs`，接管 splitter resize capture 的 arm/move/release、host resize callback 转发、tab drag arm/move/release、drag-start threshold、tab payload/source-group 解析与 drag state 清理；`native_pointer.rs` 保留 root move/button/scroll dispatch、菜单/close prompt/workbench/template/pane callback ordering、text-input focus timing 与 viewport/button id 转换。拆分后 `native_pointer.rs` 为 732 行，`drag_resize.rs` 242 行，`routing.rs` 521 行，`redraw_result.rs` 132 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer drag/resize ownership scan 为 clean，确认 resize/tab-drag lifecycle helper、`HostResizeStateData`/`HostDragStateData` 和 `TAB_DRAG_START_DISTANCE_PX` 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或拆分剩余较大的 host_contract owner（如 `profiling_artifacts.rs`、`window.rs`、surface hit-test）。 |
| 2026-06-18 | 08 M3.S2 profiling artifact geometry responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/profiling_artifacts/geometry.rs`，接管 `UiProfileGeometry::from_presentation(...)`、resize/tab/activity/template/viewport-toolbar frame 收集、surface-frame top-hit filtering、clip intersection、hit sample 生成与 route-hit 对照；`profiling_artifacts.rs` 保留 export env gate、profile output path、reference screenshot export 与序列化 DTO。拆分后父文件为 212 行，`geometry.rs` 651 行，`tests.rs` 211 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling artifact geometry ownership scan 为 clean，确认 geometry collect/hit-sample/helper 不再由父模块定义；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或拆分 `surface_hit_test/template_node.rs`、`window.rs` 等剩余较大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 template-node hit-test test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/surface_hit_test/template_node_tests.rs`，接管 Workbench dropdown option row、TextInput component family、decorative viewport scene layer、bounded dropdown popup、popup menu row 与 separator blocking 回归；`surface_hit_test/template_node.rs` 保留 pane/workbench template-node hit-test、popup row synthesis、dispatchable filtering 与 surface-frame construction，只保留外部 test-module 声明。拆分后父文件为 392 行，新测试文件为 289 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template-node hit-test ownership scan 为 clean，确认父模块不再包含内联 `mod tests {` 或回归测试函数，测试文件接管全部七个用例；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 retained software bridge/runtime extract/GPU command stream 边界，或拆分其他仍偏大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 softbuffer presenter responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/presenter/softbuffer/diagnostics.rs` 与 `softbuffer/surface_io.rs`；diagnostics 接管 present 诊断计划、region/full repaint 计数、debug refresh overlay damage 扩展、command-stream counter 与 verbose summary，surface_io 接管窗口尺寸 clamp、softbuffer resize、damage pixel bounds/count、damage rect 与 RGBA→softbuffer copy；`softbuffer.rs` 保留 fallback presenter 生命周期、backbuffer 管理、chrome command stream replay 调度与 `HostChromePresenter` 实现。拆分后父文件为 443 行，`diagnostics.rs` 185 行，`surface_io.rs` 81 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；softbuffer production responsibility scan 为 clean，确认 diagnostics/surface I/O helper 不再由父模块定义；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露子模块 `super::super` 路径越界并已修正，复跑通过，只有既有 warning 噪声；touched-file whitespace scan 为 clean，scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续把 fallback/software bridge 责任收束到 runtime extract/GPU command stream 边界，或拆分其他仍偏大的生产 owner。 |
| 2026-06-18 | 08 M3.S2 paint primitive pixel responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_primitives/pixels.rs`，接管 low-level rectangular span fill、rounded fill、rounded border fill、alpha blending、per-pixel writes、rounded-rect containment、corner-radius clamping 与 frame inset helper；`paint_primitives.rs` 保留 rectangle/rounded rectangle/border/separator/text-marker draw entries、clip/recording coordination、`PixelRect` target selection 与 image child re-export。拆分后父文件为 371 行，`paint_primitives/pixels.rs` 为 151 行，`paint_primitives/image.rs` 为 420 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint primitive pixel ownership scan 为 clean，确认 low-level pixel fill/blend/radius helper 不再由父模块定义并集中在 `paint_primitives/pixels.rs`；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 `paint_frame.rs`/`chrome_command_stream`/runtime extract 或 GPU command stream 边界。 |
| 2026-06-18 | 08 M3.S2 paint-frame recording responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/paint_frame/recording.rs`，接管 `HostRecordedPaintCommand`、`HostRecordedPaintKind`、`HostPaintAtlasImage`、`HostPaintImageUvRect` 与 internal `HostPaintRecording` command accumulator；`paint_frame.rs` 保留 `HostRgbaFrame` frame-buffer ownership、paint clip state、record-only frame construction、rectangle fill、byte accessors、pixel-rect conversion 与 local software fill helpers。拆分后父文件为 332 行，新 recording 模块为 86 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-frame recording ownership scan 为 clean，确认 recorded paint DTO/accumulator 不再由 `paint_frame.rs` 定义并集中在 `paint_frame/recording.rs`；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露父模块 `record_text(...)` 缺少 `UiTextRunPaintStyle` import，已修正并复跑通过，只有既有 warning 噪声；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 `chrome_command_stream`/runtime draw-list/GPU command stream 边界，或拆分仍混合记录与像素职责的 retained host owner。 |
| 2026-06-18 | 08 M3.S2 chrome command DTO responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/command.rs`，接管 `ChromeCommand`、`ChromeCommandKind`、`ChromeCommandLayer`、`ChromeImagePayload` 与 `ChromeImageUvRect`；`chrome_command_stream/stream.rs` 保留 `ChromeCommandStream` state、surface-size clamp、full/patch construction、push/accessor 方法与 visible-frame filtering。拆分后 `stream.rs` 为 172 行，`command.rs` 为 54 行，`mod.rs` 为 44 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chrome command DTO ownership scan 为 clean，确认 command/layer/kind/image payload DTO 不再由 `stream.rs` 定义并集中在 `command.rs`；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 `push_text(...)` 缺少 `UiTextRunPaintStyle` import，已修正并复跑通过，只有既有 warning 噪声；touched-file whitespace scan 为 clean；scoped `git diff --check` 仅输出既有 LF/CRLF working-tree warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 runtime draw-list/GPU command stream 边界，或拆分 `chrome_command_stream/tests.rs`/remaining host_contract owners。 |

| 2026-06-18 | 08 M3.S2 runtime draw-list/GPU presenter boundary test responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/chrome_command_stream/runtime_draw_list/tests.rs` 与 `host_contract/presenter/gpu/tests.rs`；前者接管 runtime draw-list rounded quad/border 与 atlas UV payload 投影回归，后者接管 GPU presenter RHI failure propagation、upload/draw-call diagnostics、damage patch after cache bootstrap 与 resize invalidation 回归。`runtime_draw_list.rs` 与 `presenter/gpu.rs` 只保留生产映射/提交逻辑和 `#[cfg(test)] mod tests;` 声明。拆分后 `runtime_draw_list.rs` 为 95 行、`runtime_draw_list/tests.rs` 为 94 行、`presenter/gpu.rs` 为 166 行、`presenter/gpu/tests.rs` 为 209 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；runtime draw-list/GPU presenter test ownership scan 为 clean，确认父模块不再包含内联 `mod tests {`、测试 helper 或测试函数；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；`cargo check -p zircon_editor --lib --tests --locked --jobs 1 --message-format short --color never` 额外探测被既有无关 lib-test 错误阻塞，失败点在 `tests/host/render_framework_boundary` 的旧 command_stream 路径、`paint_template_nodes/*_tests` 的 stale paint_theme/PALETTE 导入、`profiling_artifacts/tests.rs` 的 `UiSurfaceFrame` 引用，以及 `retained_tab_drag` 的旧 resolver 函数名。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续收束 `chrome_command_stream/tests.rs`、retained software bridge/runtime extract/GPU command stream 边界，或拆分剩余较大的 host_contract owner。 |
| 2026-06-18 | 08 M3.S2 chrome command-stream test tree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将原 648 行 `host_contract/chrome_command_stream/tests.rs` 收束为 folder-backed 测试树：根 `tests.rs` 仅声明 `extraction`、`replay`、`stream_model`、`support` 四个子模块；`tests/support.rs` 接管 shared presentation fixtures、root overlay image fixture、pixel diff/helper 与 recorded-command test insertion；`tests/stream_model.rs` 接管 full/patch stream、stats、legacy skeleton skip、viewport upload 和 atlas payload 回归；`tests/extraction.rs` 接管 recorded paint 到 chrome command 的 corner-radius 回归；`tests/replay.rs` 接管 close prompt、full-frame parity、root overlay image 和 damage repaint parity。拆分后 `tests.rs` 为 4 行、`support.rs` 为 280 行、`stream_model.rs` 为 141 行、`extraction.rs` 为 64 行、`replay.rs` 为 191 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chrome command-stream test-tree ownership scan 为 clean，确认根 `tests.rs` 不再包含 `#[test]`、fixture/helper 函数或 production imports，关键回归函数分别落入 `tests/stream_model.rs`、`tests/extraction.rs` 与 `tests/replay.rs`；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续把 retained software bridge/runtime extract/GPU command stream 边界收束，或拆分剩余较大的 host_contract owner（如 `window.rs`、`native_pointer.rs`、`paint_workbench_renderer/welcome.rs` 等）。 |
| 2026-06-18 | 08 M3.S2 workbench Welcome subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_workbench_renderer/welcome.rs` 从 564 行 Welcome 大 owner 收束为 folder-backed 子树：根 `welcome.rs` 只保留 draw entry、outer/recent/main panel resolution 与 panel fill/border；新增 `welcome/layout.rs` 接管 template-node frame lookup、fallback inset/content constraints 与 row metrics，`welcome/style.rs` 接管 Welcome palette/action colors，`welcome/main_column.rs` 接管 hero/status/new-project form/preview/validation/action button column，`welcome/recent_projects.rs` 接管 recent header、empty state、project rows、invalid border 与 status label。拆分后 `welcome.rs` 为 60 行、`welcome/layout.rs` 为 36 行、`welcome/style.rs` 为 13 行、`welcome/main_column.rs` 为 360 行、`welcome/recent_projects.rs` 为 135 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Welcome subtree ownership scan 为 clean，确认根 `welcome.rs` 不再定义 `draw_welcome_field`、`draw_welcome_validation`、`draw_welcome_button`、`draw_welcome_recent_projects`、`constrain_welcome_content`、`inset_frame`、style constants 或 row metrics，生产职责分别落入 layout/style/main-column/recent-project 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（如 `paint_workbench_renderer/docks.rs`、`window.rs`、`native_pointer.rs`），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 workbench dock subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_workbench_renderer/docks.rs` 从 516 行 Dock 大 owner 收束为 folder-backed 子树：根 `docks.rs` 保留 side/document/bottom dock shell entry、region background、rail/panel frame resolution 与 floating layer public entry；新增 `docks/panel_header.rs` 接管 panel header fill/template/separator，`docks/pane.rs` 接管 pane background/body orchestration、template/native/debug/fallback dispatch，`docks/viewport_toolbar.rs` 接管 Scene/Game viewport toolbar chrome，`docks/rail.rs` 接管 active rail marker，`docks/floating_windows.rs` 接管 floating layer/window shell/header/body layout。拆分后 `docks.rs` 为 185 行、`panel_header.rs` 为 34 行、`pane.rs` 为 182 行、`viewport_toolbar.rs` 为 37 行、`rail.rs` 为 28 行、`floating_windows.rs` 为 86 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；dock subtree ownership scan 为 clean，确认根 `docks.rs` 不再定义 `draw_panel_header`、`draw_pane`、`draw_pane_template_nodes`、`draw_if_present`、`draw_viewport_toolbar`、`draw_pane_fallback`、`draw_active_rail_marker`、`draw_floating_window` 或 `SaturatingAddF32`，生产职责分别落入 panel-header/pane/viewport-toolbar/rail/floating-window 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner，或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 profiling geometry subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/profiling_artifacts/geometry.rs` 从 614 行 profiling geometry 大 owner 收束为 folder-backed 子树：根 `geometry.rs` 保留 `UiProfileGeometry::from_presentation(...)` assembly、resize splitter collection、profile layout assembly 与 child collector orchestration；新增 `geometry/tabs.rs` 接管 document/drawer/floating/host-page tab frame collection，`geometry/pane_frames.rs` 接管 activity rail、pane body、viewport toolbar、template-node 和 surface-frame top-hit control collection，`geometry/hit_samples.rs` 接管 clickable frame route sample construction，`geometry/frame_math.rs` 接管 visibility、translation、intersection、center-point 与 profile-frame push helpers。拆分后 `geometry.rs` 为 155 行、`tabs.rs` 为 84 行、`pane_frames.rs` 为 235 行、`hit_samples.rs` 为 61 行、`frame_math.rs` 为 112 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling geometry subtree ownership scan 为 clean，确认根 `geometry.rs` 不再定义 tab collectors、pane/template/surface-frame collectors、hit sample/route-hit helpers、visibility/intersection/translation helpers、center helpers 或 `push_named_*` helpers，生产职责分别落入 tabs/pane-frames/hit-samples/frame-math 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（如 `window.rs`、`native_pointer.rs`、surface hit-test、template primitive owner），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 host window text-input/test-support responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/window.rs` 的 focused text edit/native keyboard dispatch 与 `#[cfg(test)]` native input helpers 移入 window 子树：新增 `window/text_input.rs` 接管 text focus active、insert/backspace/commit、focused key dispatch、popup keyboard/text-search fallback、unhandled keyboard forwarding、Welcome/showcase/inspector/asset/surface edit callback 路由与 text-focus redraw；新增 `window/test_support.rs` 接管 native key/pointer/text/popup test helper methods 与 deterministic keyboard metadata。`window.rs` 保留 host state API、presentation aggregation、external redraw queue、event-loop metadata、snapshot entry 与 module declarations。拆分后 `window.rs` 为 382 行、`text_input.rs` 为 199 行、`test_support.rs` 为 148 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window text-input/test-support ownership scan 为 clean，确认根 `window.rs` 不再定义 `dispatch_focused_text_insert`、`dispatch_focused_text_backspace`、`dispatch_focused_key_event`、`dispatch_native_keyboard_event`、`dispatch_unhandled_keyboard_input`、`dispatch_focused_text_commit`、`dispatch_text_focus_value`、`asset_dispatch_source`、`native_keyboard_event_consumed`、`text_focus_consumes_keyboard_event`、`text_input_focus_redraw` 或 `dispatch_native_*_for_test` helpers，生产职责分别落入 text-input/test-support 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（如 `native_pointer.rs`、surface hit-test、template primitive owner），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer move-dispatch responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/native_pointer/move_dispatch.rs`，接管 root pointer-move flow、resize/tab-drag capture move short-circuit、menu hover dispatch、Workbench template-node hover dispatch、pane hover/native move dispatch、viewport move event、hover clear damage 与 structured Workbench option/menu row hover mapping；`native_pointer.rs` 保留 button/scroll event flow、text-input focus timing、close prompt、chrome press、pane click/context-menu、viewport scroll 与 callback ordering。拆分后 `native_pointer.rs` 为 580 行、`move_dispatch.rs` 为 133 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer move-dispatch ownership scan 为 clean，确认根 `native_pointer.rs` 不再定义 `dispatch_native_pointer_move` 或 `set_hovered_workbench_template_hit`，move child owns pointer-move routing and hover mapping；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 child re-export visibility 太窄，已将 `dispatch_native_pointer_move` 调整为 host_contract 可见并复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（surface hit-test、template primitive owner、native pointer button/scroll 子路径），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer button/scroll responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 新增 `host_contract/native_pointer/button_dispatch.rs` 与 `host_contract/native_pointer/scroll_dispatch.rs`。button child 接管 root pointer press/release、resize/tab-drag release finish、close prompt/menu/outside-popup/chrome press ordering、Workbench context-menu request、template text focus、template primary press、pane button callbacks、viewport button events、chrome press callback fanout 与 viewport button id mapping；scroll child 接管 menu scroll、pane scroll 与 viewport scroll forwarding。`native_pointer.rs` 只保留 subtree declarations、dispatch re-export、shared pointer constants 与 `NativePointerButtonState`。拆分后 `native_pointer.rs` 为 33 行、`button_dispatch.rs` 为 460 行、`scroll_dispatch.rs` 为 109 行、`move_dispatch.rs` 为 133 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer button/scroll ownership scan 为 clean，确认父 `native_pointer.rs` 不再定义 `dispatch_native_pointer_button`、`dispatch_native_pointer_scroll`、`dispatch_chrome_press`、`focus_template_node_text_input`、`close_prompt_action_at` 或 `viewport_button_id`。首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露旧父级私有 alias 被移除后 `chrome_damage.rs`/`menu_geometry.rs` 仍从父模块导入 `ChromePointerRoute`/`contains`，已改为直接从 `routing` 导入并复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：下一步优先拆分 `button_dispatch.rs` 内部 pane callback/text focus/chrome press 子路径，或继续推进 surface hit-test/template primitive owner 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer button subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/native_pointer/button_dispatch.rs` 收束为 folder-backed button 子树：根 `button_dispatch.rs` 保留 root pointer press/release ordering、resize/tab-drag release finish、close prompt overlay/menu/outside-popup/chrome/workbench/pane route sequencing 与 damage union；新增 `button_dispatch/chrome_press.rs` 接管 top-level chrome callback fanout 与 resize arm delegation，`button_dispatch/pane_callbacks.rs` 接管 pane body callbacks、viewport toolbar press、viewport button event、template-node primary press 与 pane press damage fallback，`button_dispatch/text_focus.rs` 接管 template text-input focus activation，`button_dispatch/close_prompt_hit.rs` 接管 close-prompt action hit testing，`button_dispatch/viewport_button.rs` 接管 viewport button id mapping。拆分后 `button_dispatch.rs` 为 173 行、`pane_callbacks.rs` 为 191 行、`chrome_press.rs` 为 75 行、`text_focus.rs` 为 25 行、`close_prompt_hit.rs` 为 23 行、`viewport_button.rs` 为 12 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer button subtree ownership scan 为 clean，确认根 `button_dispatch.rs` 不再定义 `dispatch_chrome_press`、`focus_template_node_text_input`、`close_prompt_action_at`、`viewport_button_id` 或 `match pointer.target` pane callback fanout；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（例如 `button_dispatch/pane_callbacks.rs` 的各 pane family、surface hit-test、template primitive owner），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer pane callback subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks.rs` 收束为 folder-backed pane callback 子树：根保留 pane target routing、button/host/viewport event kind resolution、passive pane text-focus damage 与 final pane-press damage fallback；新增 `pane_callbacks/native_panes.rs` 接管 Hierarchy/Welcome callback fanout，`pane_callbacks/asset_panes.rs` 接管 AssetTree/AssetContent/AssetReference callback fanout，`pane_callbacks/viewport.rs` 接管 viewport toolbar press 与 viewport body button forwarding，`pane_callbacks/template_nodes.rs` 接管 template-node primary press 与 text-input focus activation handoff。拆分后 `pane_callbacks.rs` 为 126 行、`asset_panes.rs` 为 85 行、`native_panes.rs` 为 47 行、`template_nodes.rs` 为 35 行、`viewport.rs` 为 57 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer pane callback subtree ownership scan 为 clean，确认根 `pane_callbacks.rs` 不再直接调用 hierarchy/welcome/assets/viewport/template-node callback fanout，具体 callback 分别落入 native-panes/asset-panes/viewport/template-nodes 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（surface hit-test、template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer routing subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/native_pointer/routing.rs` 收束为 folder-backed routing 子树：根保留 `ChromePointerRoute`、`PanePointerRoute`、`PanePointerTarget` route payload contract 与 public route entry re-export；新增 `routing/chrome.rs` 接管 resize splitter、document tab、drawer header、activity rail、host page tab 与 floating-window header hit routing，`routing/panes.rs` 接管 floating/local pane body routing、viewport toolbar priority、template-node pane hits 与 pane-kind target mapping，`routing/geometry.rs` 接管 containment/translation/side-dock/floating-window content geometry，`routing/workbench.rs` 接管 componentized Workbench template-node route entry。拆分后 `routing.rs` 为 88 行、`chrome.rs` 为 241 行、`panes.rs` 为 170 行、`geometry.rs` 为 41 行、`workbench.rs` 为 10 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer routing subtree ownership scan 为 clean，确认根 `routing.rs` 不再定义 document/drawer/activity-rail/pane routing helpers、pane route mode、viewport-toolbar/template hit calls 或 geometry helpers，具体职责分别落入 chrome/panes/geometry/workbench 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（surface hit-test、template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer menu geometry subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/native_pointer/menu_geometry.rs` 收束为 folder-backed menu geometry 子树：根只保留 `menu_handles_point`、`menu_popup_handles_point`、`menu_damage_frame` 与 `menu_damage_frame_with_state` 的 dispatch-facing re-export；新增 `menu_geometry/bar.rs` 接管 top-bar/scrolled menu-control containment，`menu_geometry/popup.rs` 接管 open-menu popup containment、nested submenu containment 与 blocking frame，`menu_geometry/damage.rs` 接管当前/显式 menu state 的 damage rectangle，`menu_geometry/frames.rs` 接管 menu chrome fallback、shell extent、popup constraint、row frame、submenu frame 与 popup height math。拆分后 `menu_geometry.rs` 为 8 行、`bar.rs` 为 26 行、`popup.rs` 为 87 行、`damage.rs` 为 91 行、`frames.rs` 为 163 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer menu geometry subtree ownership scan 为 clean，确认根 `menu_geometry.rs` 不再定义 containment、popup stack、damage stack 或 frame math helpers，具体职责分别落入 bar/popup/damage/frames 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（surface hit-test、template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native pointer drag/resize capture subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/native_pointer/drag_resize.rs` 收束为 folder-backed capture 子树：根只保留 resize 与 tab-drag dispatch-facing re-export；新增 `drag_resize/resize_capture.rs` 接管 splitter resize arm/move/release、`HostResizeStateData` 更新、DrawerResize perf scenario 与 bounded resize redraw；新增 `drag_resize/tab_drag.rs` 接管 document/drawer/floating tab payload resolution、drag-start threshold、drag down/move/up forwarding、`HostDragStateData` 清理与 tab-drag release redraw。拆分后 `drag_resize.rs` 为 9 行、`resize_capture.rs` 为 64 行、`tab_drag.rs` 为 190 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pointer drag/resize capture subtree ownership scan 为 clean，确认根 `drag_resize.rs` 不再定义 resize/tab-drag state mutation、perf scenario、payload resolution 或 redraw helper calls，具体职责分别落入 resize-capture/tab-drag 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（surface hit-test、template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 template-node hit-test subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/surface_hit_test/template_node.rs` 收束为 folder-backed template-node hit-test 子树：根保留 `TemplateNodePointerHit` DTO、pane/workbench hit-test entry、pane surface-frame build entry 与 ordinary surface-frame hit assembly；新增 `template_node/pane_nodes.rs` 接管 pane kind 到 `TemplatePaneNodeData` 的映射，`template_node/popup_rows.rs` 接管 open popup row priority、option/menu row hit synthesis、popup body blocking 与 menu action id normalization，`template_node/surface_frame_builder.rs` 接管 dispatchable-node filtering、temporary runtime `UiSurfaceFrame` assembly、template metadata、clip-frame preservation 与 surface rebuild。拆分后 `template_node.rs` 为 105 行、`pane_nodes.rs` 为 23 行、`popup_rows.rs` 为 178 行、`surface_frame_builder.rs` 为 114 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；surface hit-test template-node subtree ownership scan 为 clean，确认根 `template_node.rs` 不再定义 popup row helpers、menu action id normalization、dispatchable filtering、runtime `UiSurface` construction 或 pane-kind mapping，具体职责分别落入 pane-nodes/popup-rows/surface-frame-builder 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |

| 2026-06-18 | 08 M3.S2 paint primitive image subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_primitives/image.rs` 收束为 folder-backed image 子树：根保留 RGBA image clipped draw entry variants、clip/recording/raster orchestration 与 image regression tests；新增 `paint_primitives/image/recording.rs` 接管 image recording metadata、explicit resource-key recording、atlas recording、content-scoped fallback key hashing 与 atlas payload validation；新增 `paint_primitives/image/raster.rs` 接管 opaque identity-row copy、scaled RGBA sampling、identity mapping detection 与 per-pixel alpha writes。拆分后 `image.rs` 为 249 行、`raster.rs` 为 113 行、`recording.rs` 为 67 行。 | `cargo fmt -p zircon_editor --check` 通过；paint primitive image subtree ownership scan 为 clean，确认根 `image.rs` 不再定义 fallback resource key hashing、recording metadata implementation、identity-row copy、scaled raster loop 或 RGBA pixel blending，具体职责分别落入 recording/raster 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template primitive owner、render command conversion、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |

| 2026-06-18 | 08 M3.S2 template render-command subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/render_commands.rs` 收束为 folder-backed render-command 子树：根只保留 subtree declarations、`HostPaintCommand`/`draw_host_paint_commands` 可见性保持、runtime `UiRenderCommand` 转换入口与 test helper；新增 `render_commands/command.rs` 接管 `HostPaintCommand` DTO、internal kind、constructors、z-index/opacity/clip/frame/text/image payload storage 与 image placeholder fallback border；新增 `render_commands/draw.rs` 接管 stable z-order playback、quad/text/image per-kind dispatch、rounded/flat border/fill emission、atlas/resource-key image fallback、placeholder color generation 与 opacity application。拆分后 `render_commands.rs` 为 26 行、`command.rs` 为 180 行、`draw.rs` 为 210 行。 | `cargo fmt -p zircon_editor --check` 通过；render command subtree ownership scan 为 clean，确认根 `render_commands.rs` 不再定义 `HostPaintCommand` DTO、command kind、ordered playback、per-kind draw helpers、image placeholder/opacity helpers 或 border expansion；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 `template_node_pipeline.rs` 仍按旧可见性导入 `draw_host_paint_commands`，已将 re-export 与 child function visibility 调整到既有 paint-template 可见性并复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（render_command_conversion、template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |

| 2026-06-18 | 08 M3.S2 render-command conversion subtree responsibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/render_command_conversion.rs` 收束为 folder-backed conversion 子树：根保留 runtime command traversal、clip inheritance、invisible/transparent element filtering 与 payload dispatch；新增 `render_command_conversion/brush.rs` 接管 brush fill/border color extraction 与 image/vector/box resource extraction，`image.rs` 接管 visual asset resource resolution、raster-size request、image-pixels command construction 与 resource-key fallback，`text.rs` 接管 run/shaped/source text fallback conversion、selection/caret/composition/outline decoration layering 与 text command construction，`style.rs` 接管 `UiFrame` 到 `FrameRect` 转换、foreground fallback、text alignment 与 hex color parsing。拆分后 `render_command_conversion.rs` 为 86 行、`brush.rs` 为 29 行、`image.rs` 为 64 行、`style.rs` 为 69 行、`text.rs` 为 116 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；render command conversion subtree ownership scan 为 clean，确认根 `render_command_conversion.rs` 不再定义 brush helpers、image resource fallback、text/decorations conversion、style color parsing、alignment 或 frame conversion；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template primitive owner、remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |

| 2026-06-18 | 08 M3.S2 Workbench painter root orchestration subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_workbench_renderer.rs` 收束为 folder-backed root orchestration 子树：根保留 module declarations、legacy/profiled public draw entries、componentized Workbench draw re-export、shared Workbench painter palette constants 与 `first_non_empty` helper；新增 `paint_workbench_renderer/root_frames.rs` 接管 `RootFrames`、top/status/center/left/right/bottom/document/viewport fallback geometry、layout source selection 与 `zero_origin`；新增 `skeleton.rs` 接管 legacy root skeleton fills/borders/separator、project marker、debug refresh marker、viewport label 与 status secondary label；新增 `scene_layers.rs` 接管 host-scene paint order、componentized Workbench window drawing、root template overlay、resize layer、close prompt/menu/dock/floating/status template-node layers。拆分后 `paint_workbench_renderer.rs` 为 66 行、`root_frames.rs` 为 99 行、`skeleton.rs` 为 94 行、`scene_layers.rs` 为 177 行。 | `cargo fmt -p zircon_editor --check` 通过；Workbench root orchestration subtree ownership scan 为 clean，确认根 `paint_workbench_renderer.rs` 不再定义 `RootFrames`、root frame fallback, legacy skeleton markers, host scene layer, resize layer, componentized template drawing 或 root overlay helpers；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（menus/native panes/template primitive owner/remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 Workbench menu painter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_workbench_renderer/menus.rs` 收束为 folder-backed menu painter 子树：根只保留 `draw_menu_bar_labels(...)` 与 `draw_open_menu_popup(...)` 的 host-scene 入口委托；新增 `menus/bar.rs` 接管 top-bar label paint、active-menu color、clipped label/separator 与 menu-bar scroll adjustment；`menus/popup.rs` 接管 open menu popup、custom template popup dispatch、submenu branch traversal 与 popup chrome paint；`menus/rows.rs` 接管 fallback popup row label/shortcut/disabled/hover paint；`menus/geometry.rs` 接管 popup row frame、popup height、top-level popup viewport constraint、submenu side placement 与 scrolled menu frame math。拆分后 `menus.rs` 为 18 行、`geometry.rs` 为 110 行、`popup.rs` 为 88 行、`rows.rs` 为 48 行、`bar.rs` 为 44 行。 | `cargo fmt -p zircon_editor --check` 通过；workbench menu subtree ownership scan 为 clean，确认根 `menus.rs` 不再定义 submenu traversal、row paint、popup geometry、popup height 或 scrolled menu frame helpers，具体职责落入 bar/popup/rows/geometry 子模块；touched-file trailing whitespace scan 为 clean；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（native panes/template primitive owner/remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 Workbench native-pane painter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_workbench_renderer/native_panes.rs` 收束为 folder-backed native pane painter 子树：根只保留 dock-facing `draw_viewport_image(...)`、`draw_native_pane_content(...)` 与 `draw_pane_debug_overlay(...)` 入口委托；新增 `native_panes/viewport.rs` 接管 Scene/Game viewport image validation 与 RGBA image draw dispatch；`native_panes/diagnostics.rs` 接管 RuntimeDiagnostics overlay primitive collection 与 debug-reflector overlay dispatch；`native_panes/content.rs` 接管 Welcome/Hierarchy/Assets/AssetBrowser native pane kind routing；`native_panes/hierarchy.rs` 接管 Hierarchy row paint、selection/hover color、indent、scroll 与 template-derived viewport frame；`native_panes/assets.rs` 接管 Assets/AssetBrowser tree hover overlay、row-control matching、row frame translation、scroll offset 与 hover border paint。拆分后 `native_panes.rs` 为 35 行、`assets.rs` 为 109 行、`hierarchy.rs` 为 85 行、`viewport.rs` 为 26 行、`content.rs` 为 23 行、`diagnostics.rs` 为 17 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench native pane subtree ownership scan 为 clean，确认根 `native_panes.rs` 不再定义 hierarchy row paint、asset tree hover overlay、viewport image draw、diagnostics overlay 或 pane-kind routing 细节，具体职责落入 assets/content/diagnostics/hierarchy/viewport 子模块；touched-file trailing whitespace scan 为 clean；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template primitive owner/remaining painter owners），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 paint primitive root subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_primitives.rs` 收束为 folder-backed primitive API 入口：根保留 image draw re-export 以及 rectangle/rounded-rectangle/border/separator/text-marker 稳定 wrapper，调用方 API 不变；新增 `paint_primitives/clip.rs` 接管 active paint clip 与 explicit clip intersection；`shapes.rs` 接管 rectangle/rounded rectangle/border/rounded border、recording-aware quad/border emission、`PixelRect` target conversion 与 pixel-fill dispatch；`lines.rs` 接管 separator line clip/record/span write；`text_markers.rs` 接管 fallback text bars 与 label markers；`tests.rs` 接管原根文件内 clipped rectangle regression。拆分后 `paint_primitives.rs` 为 90 行、`shapes.rs` 为 184 行、`text_markers.rs` 为 56 行、`lines.rs` 为 51 行、`clip.rs` 为 14 行、`tests.rs` 为 74 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint primitive root subtree ownership scan 为 clean，确认根 `paint_primitives.rs` 不再定义 `effective_clip`、shape fill/recording helpers、separator span write、text marker body 或 inline tests；scoped `git diff --check` 为 clean；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 `image.rs` 仍从 root 导入 `effective_clip`，已改为 `clip::effective_clip` 并复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template primitive owner、presenter/window/native-keyboard 等），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 MUI X chart painter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/charts.rs` 收束为 folder-backed chart painter 子树：根保留 `ChartKind`、role detection、chart surface/plot frame drawing 与 bar/raster dispatch；新增 `charts/bars.rs` 接管 bar chart command emission；`charts/raster_commands.rs` 接管 raster dimensions、line/pie/sparkline/gauge raster sequencing、chart value normalization 与 image resource-key naming；`charts/raster.rs` 接管 `ChartRaster`、polyline/disc/arc/pie pixel drawing、pixel-range clamp、segment distance 与 angle normalization。拆分后 `charts.rs` 为 90 行、`raster.rs` 为 141 行、`raster_commands.rs` 为 123 行、`bars.rs` 为 69 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；MUI X chart subtree ownership scan 为 clean，确认根 `charts.rs` 不再定义 bar helper、raster command sequencing、`ChartRaster` 或 low-level pixel math；scoped `git diff --check` 为 clean；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template inspector/table/popup/status/viewport scene owner、presenter/window/native-keyboard 等），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |

| 2026-06-18 | 08 M3.S2 softbuffer presenter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/presenter/softbuffer.rs` 收束为 62 行的 fallback presenter 状态与 trait-facing 委托入口：根保留 softbuffer context/surface/backbuffer/diagnostics/log cache 状态、`new`/`resize`/`present`/`diagnostics_snapshot` 委托和 `HostChromePresenter` 实现；新增 `softbuffer/lifecycle.rs` 接管 context/surface 创建、尺寸 clamp、surface resize、backbuffer/overlay reset；`softbuffer/present.rs` 接管 present orchestration、diagnostics planning、command stream build、perf counter、softbuffer buffer copy/present 与 verbose diagnostic log；`softbuffer/backbuffer.rs` 接管 regional repaint eligibility、command-stream region replay、full repaint fallback 与 `RepaintOutcome`；`softbuffer/tests.rs` 接管原 inline softbuffer regressions。拆分后 `softbuffer.rs` 为 62 行、`present.rs` 为 120 行、`diagnostics.rs` 为 173 行、`surface_io.rs` 为 71 行、`backbuffer.rs` 为 49 行、`lifecycle.rs` 为 35 行、`tests.rs` 为 180 行。 | `cargo fmt -p zircon_editor` 已执行；softbuffer presenter subtree ownership scan 为 clean，确认根 `softbuffer.rs` 不再定义 diagnostic log、command-stream build/repaint、softbuffer copy/present damage、regional repaint helper、`RepaintOutcome` 或 inline tests，具体职责落入 lifecycle/present/backbuffer/diagnostics/surface_io/tests 子模块；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（window/native_keyboard/template inspector/table/popup/status/viewport scene owner 等），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 native keyboard subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/native_keyboard.rs` 收束为 11 行的 popup keyboard dispatch-facing 入口：根只保留 subtree declarations、test module declaration 与 `window/text_input.rs` / test-support 所需的稳定 re-export；新增 `native_keyboard/commands.rs` 接管 winit key 到 `WorkbenchPopupKeyboardCommand` 的边界键映射；`native_keyboard/dispatch.rs` 接管 popup keyboard command execution、hover row damage、accept/cancel callback fanout、command palette commit 与 popup cancel hover clear；`native_keyboard/target.rs` 接管 active popup target discovery、option/menu row construction、popup/row frame resolution、current-row identity matching 与 text-search matching；`native_keyboard/tests.rs` 接管原 inline key mapping regression。拆分后 `native_keyboard.rs` 为 11 行、`target.rs` 为 267 行、`dispatch.rs` 为 101 行、`commands.rs` 为 23 行、`tests.rs` 为 13 行。 | `cargo fmt -p zircon_editor` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声；native keyboard root ownership scan 为 clean，确认根 `native_keyboard.rs` 不再定义 popup target/row DTO、popup target discovery、row geometry、callback dispatch、text search 或 inline tests，具体职责落入 commands/dispatch/target/tests 子模块。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（window/template inspector/table/popup/status/viewport scene owner 等），或继续把 retained software bridge 向 runtime extract/GPU command stream 收束。 |
| 2026-06-18 | 08 M3.S2 host window handle/snapshot subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/window.rs` 中的 `HostWindowHandle` 与 `HostWindowSnapshot` 移入 `window/handle.rs`：根继续保留 `UiHostWindow` 状态边界、presentation aggregation、redraw queue、native input metadata 与 handle construction；新增 `window/handle.rs` 接管 native window position/size/visibility/maximized mutation、close-request callback registration、snapshot command-stream capture 与 RGBA byte packaging。拆分后 `window.rs` 为 322 行，`window/handle.rs` 为 69 行，既有 `event_loop.rs` 361 行、`text_input.rs` 199 行、`template_hover.rs` 159 行、`test_support.rs` 148 行、`tests.rs` 71 行保持职责不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window handle/snapshot ownership scan 为 clean，确认根 `window.rs` 不再定义 `HostWindowHandle`、`HostWindowSnapshot`、snapshot byte packaging、handle mutation methods 或 presenter snapshot invocation；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 当前在进入 editor 分析前被 unrelated dirty `zircon_runtime` render graph 变更阻塞，现有错误包括 `ParticleGpuTransparentDrawContext` re-export drift、`render_scene_passes.rs` 对 `execute_graph_stage` 的 24/27 参数调用漂移与类型错位，以及 `gpu/particle.rs` encoder borrow E0502。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：待 runtime render graph 变更收敛后补跑 scoped cargo check；继续拆分仍偏大的 retained host owner（template inspector/table/popup/status/viewport scene owner 等）。 |
| 2026-06-18 | 08 M3.S2 template inspector rows subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_inspector_rows.rs` 收束为 42 行 row-kind dispatch entry：根只保留 Inspector row kind 分类、resource/disclosure/shadow 分发和测试模块声明；新增 `template_inspector_rows/resource.rs` 接管 resource row field/text/glyph command sequencing，`disclosure.rs` 接管 disclosure row command sequencing，`shadow.rs` 接管 shadow select/check row command sequencing，`primitives.rs` 接管 shared label/field/text emission，`style.rs` 接管 inspector row font/radius/color constants and declared color routing。拆分后 `template_inspector_rows.rs` 为 42 行、`resource.rs` 为 99 行、`shadow.rs` 为 83 行、`primitives.rs` 为 104 行、`style.rs` 为 64 行、`disclosure.rs` 为 44 行；`template_inspector_rows_tests.rs` 改为显式导入新 owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template inspector rows subtree ownership scan 为 clean，确认根 `template_inspector_rows.rs` 不再定义 resource/disclosure/shadow row painter、field/text primitives、style constants/color helpers 或 inline tests；touched-file trailing whitespace scan 为 clean；scoped `git diff --check` 返回成功，仅提示计划文档 CRLF warning。`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 当前在进入 editor 分析前被 unrelated dirty `zircon_runtime` render graph 变更阻塞，现有错误包括 `ParticleGpuTransparentDrawContext` re-export drift、`render_scene_passes.rs` 对 `execute_graph_stage` 的 24/27 参数调用漂移与类型错位，以及 `gpu/particle.rs` encoder borrow E0502。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：待 runtime render graph 变更收敛后补跑 scoped cargo check；继续拆分仍偏大的 retained host owner（profiling_hit_routes、globals、template_viewport_scene_props、event_loop 等）。 |
| 2026-06-18 | 08 M3.S2 globals callback registry subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/globals.rs` 的 callback registry DTO 与 `Callback0`-`Callback8` arity aliases 移入 `globals/callbacks.rs`：根继续保留 `HostContractState`、`UiHostContext`、`PaneSurfaceHostContext`、state mutation API 与 callback registration/invocation methods；新增 child module 接管 `UiHostCallbacks`、`PaneSurfaceCallbacks` 以及所有 callback storage aliases。拆分后 `globals.rs` 为 320 行、`globals/callbacks.rs` 为 98 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；globals callback ownership scan 为 clean，确认根 `globals.rs` 不再定义 callback arity aliases、`UiHostCallbacks` 或 `PaneSurfaceCallbacks`，child module 拥有 callback DTO/storage；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（profiling_hit_routes、data/ui_asset、window/event_loop、template_viewport_scene_props 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 profiling hit-route subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/profiling_hit_routes.rs` 收束为 29 行 route-kind dispatch entry：根只保留 serialized profile route kind/id/surface 分发；新增 `profiling_hit_routes/chrome.rs` 接管 resize splitter 与 activity-rail route hit，`tabs.rs` 接管 document/drawer/floating/host-page tab route hit，`viewport_toolbar.rs` 接管 Scene/Game viewport toolbar hit，`template.rs` 接管 runtime surface-frame template-control hit，`geometry.rs` 接管 containment、translation、side-dock content frame 与 floating-window content frame math。拆分后 `profiling_hit_routes.rs` 为 29 行、`tabs.rs` 为 128 行、`template.rs` 为 100 行、`viewport_toolbar.rs` 为 98 行、`chrome.rs` 为 54 行、`geometry.rs` 为 37 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling hit-route subtree ownership scan 为 clean，确认根 `profiling_hit_routes.rs` 不再定义 tab/activity-rail/viewport-toolbar/template/floating-window/geometry helper 或 runtime hit-test imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（data/ui_asset、window/event_loop、template_viewport_scene_props、template_table_rows、paint_frame 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset DTO subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/data/ui_asset.rs` 从 UI Asset editor pane DTO 大聚合收束为 15 行 structural re-export entry：新增 `ui_asset/common.rs` 接管 shared string selection，`canvas.rs` 接管 canvas node/slot target/palette drag/preview canvas DTO，`source.rs` 接管 source detail/source panel DTO，`preview.rs` 接管 preview mock/panel DTO，`style.rs` 接管 theme/style rule/token/state/style panel DTO，`inspector.rs` 接管 inspector semantic/slot/layout/binding/widget/panel DTO，`pane.rs` 接管 pane header/action/designer/collection/runtime report 与 aggregate `UiAssetEditorPaneData`。拆分后 `common.rs` 为 7 行、`canvas.rs` 为 44 行、`source.rs` 为 19 行、`preview.rs` 为 36 行、`style.rs` 为 90 行、`inspector.rs` 为 111 行、`pane.rs` 为 98 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；UI Asset DTO subtree ownership scan 为 clean，确认根 `ui_asset.rs` 不再直接定义 DTO、`ModelRc`/`SharedString` imports 或 `TemplatePaneNodeData` imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（window/event_loop、template_viewport_scene_props、template_table_rows、paint_frame、window.rs 等），并在 milestone testing stage 补跑完整测试矩阵。 |

| 2026-06-18 | 08 M3.S2 viewport scene prop subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene_props.rs` 从 370 行 prop detail owner 收束为 8 行 dispatch/re-export entry：新增 `template_viewport_scene_props/cargo.rs` 接管 cargo highlight/inset/inner-frame/detail line 绘制，`property.rs` 接管 prop top/body base-surface overlay，`rails.rs` 接管 rack/handrail detail，`primitives.rs` 接管 shared rect-line 与 corner-radius helper。拆分后 `cargo.rs` 为 151 行、`property.rs` 为 108 行、`rails.rs` 为 98 行、`primitives.rs` 为 34 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；viewport scene prop root ownership scan 为 clean，确认根 `template_viewport_scene_props.rs` 不再定义 paint helper、常量、`FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports 或 `push_base_surface` 调用；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露子模块导入层级和 re-export 可见性漂移，修正后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（template_table_rows、window/event_loop、paint_frame、window.rs 等），并在 milestone testing stage 补跑完整测试矩阵。 |

| 2026-06-18 | 08 M3.S2 template table rows subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_table_rows.rs` 从 380 行 table row painter 收束为 61 行 dispatch entry：新增 `template_table_rows/identity.rs` 接管 Table/TableRow family 与 header/tail/selected control-id 识别，`cells.rs` 接管 declared option/legacy text cell extraction、column layout、content offsets 与 text command emission，`surface.rs` 接管 row paint rect、background/border/separator，`actions.rs` 接管 gear/kebab action glyph，`style.rs` 接管 Workbench table row style accessors；`template_table_rows_tests.rs` 改为显式导入新 owner。拆分后 `actions.rs` 为 108 行、`cells.rs` 为 127 行、`identity.rs` 为 37 行、`style.rs` 为 22 行、`surface.rs` 为 55 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template table rows root ownership scan 为 clean，确认根 `template_table_rows.rs` 不再定义 surface/cell/action/style/identity helper 或 `UiTextRunPaintStyle`/`TemplateComponentFamily`/`WorkbenchTableRowStyle` imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（window/event_loop、paint_frame、window.rs 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes table-row command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_table_rows.rs` 中 Workbench table-row recognition gate、cell-empty guard、row paint-rect lookup、surface/cell/action command ordering 与 fallback table text sequencing 下沉到 `template_table_rows/commands.rs`：根 `template_table_rows.rs` 从 53 行降为 12 行 module wiring/test-module entry；新增 `commands.rs` 为 51 行，并保持 `template_nodes/specialized.rs` 对 `push_table_row_commands`、`template_nodes/fallback.rs` 对 `push_table_row_text_commands` 以及 `template_table_rows_tests.rs` 对子 owner 的既有导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes table-row command sequencing ownership scan 确认根 `template_table_rows.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、table row recognition imports、cell/surface/action child-owner imports 或 command sequencing bodies，仅保留 module wiring、command re-export 和 test-module attachment；recursive use-site search 确认 `push_table_row_text_commands` 仍通过 root re-export 供 `template_nodes/fallback.rs` 使用；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-18 | 08 M3.S2 paint-frame subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_frame.rs` 从 370 行 mixed frame owner 收束为 94 行 neutral frame state entry：根保留 `HostRgbaFrame` storage、constructors、paint-clip accessor、dimension/byte accessors、recorded-command re-export 与 child declarations；新增 `paint_frame/geometry.rs` 接管 visible-frame/clip intersection，`pixels.rs` 接管 `fill_rect`、pixel-rect conversion 与 span/channel writes，`recording_frame.rs` 接管 `HostRgbaFrame` recording-facing methods，`tests.rs` 接管原 inline fill/clip/recording regressions，既有 `recording.rs` 继续接管 recorded paint DTO/accumulator。拆分后 `geometry.rs` 为 32 行、`pixels.rs` 为 65 行、`recording.rs` 为 96 行、`recording_frame.rs` 为 120 行、`tests.rs` 为 74 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-frame root ownership scan 为 clean，确认根 `paint_frame.rs` 不再定义 `fill_rect`/`pixel_rect`、visible/clip/intersection helper、pixel span/channel writes、recording methods 或 `UiTextRunPaintStyle` import；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（window/event_loop、window.rs 等），并在 milestone testing stage 补跑完整测试矩阵。 |

| 2026-06-18 | 08 M3.S2 window event-loop subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/window/event_loop.rs` 从 381 行 winit event-loop owner 收束为 67 行 `ApplicationHandler` state/委托 entry：新增 `event_loop/lifecycle.rs` 接管 native window creation、GPU/softbuffer presenter fallback、native state sync 与 about-to-wait drain；`events.rs` 接管 `WindowEvent` match 与 close/resize/move/pointer/keyboard/IME/wheel/redraw branch dispatch；`input.rs` 接管 pointer button/state、scroll delta、IME enablement 与 input metadata sequence；`redraw.rs` 接管 pending/external redraw merge/drain、pointer dispatch redraw fanout、frame-update scenario attribution、presenter present、profiling artifact export 与 diagnostics overlay update。拆分后 `lifecycle.rs` 为 115 行、`events.rs` 为 117 行、`redraw.rs` 为 89 行、`input.rs` 为 54 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window event-loop root ownership scan 为 clean，确认根 `event_loop.rs` 不再直接拥有 `WindowAttributes`/presenter creation、native pointer dispatch、mouse/scroll/input helper、redraw/present/export artifact 或 IME helper 细节；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（window.rs、host_lifecycle/app/ui_asset_editor 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 window presentation/diagnostics/redraw subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `host_contract/window.rs` 从 322 行 mixed host-window owner 收束为 117 行窗口状态/lifecycle/root entry：新增 `window/presentation.rs` 接管 host presentation mutation、presentation snapshot aggregation、close-prompt damage redraw、menu/pane snapshot、template hover state writes 与 bootstrap geometry；新增 `window/diagnostics.rs` 接管 refresh invalidation diagnostics 与 refresh diagnostics overlay mutation；新增 `window/redraw.rs` 接管 frame-update request、completed scenario handoff、region redraw request、frame-update region redraw 与 external redraw queue coalescing/drain accounting；`window/handle.rs` 改为从 presentation owner 复用 snapshot aggregation helper。拆分后 `presentation.rs` 为 171 行、`redraw.rs` 为 66 行、`diagnostics.rs` 为 26 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window root presentation/diagnostics/redraw ownership scan 为 clean，确认根 `window.rs` 不再直接定义 presentation mutation/clone helper、refresh diagnostics overlay、frame-update/redraw queue helper 或相关 DTO/diagnostic/perf imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（host_lifecycle/app/ui_asset_editor 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 host_contract redraw request/dispatch/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/redraw.rs` 中 `HostRedrawRequest` 请求模型/构造/merge/scenario 逻辑、`NativePointerDispatchResult` 指针派发结果包装，以及内联 redraw 回归测试分别拆入 `redraw/request.rs`、`redraw/dispatch_result.rs` 和 `redraw_tests.rs`：根 `redraw.rs` 从 345 行降为 9 行 structural export/test hook entry；新增子文件分别为 158、51、138 行，并保持 native pointer、native keyboard、window event-loop 和 window redraw 的原 `host_contract::redraw::{...}` 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract redraw request/dispatch/test ownership scan 确认根 `redraw.rs` 不再拥有 `FrameRect`/perf imports、request enum body、merge/coalescing body、dispatch-result constructors/query body 或 inline tests，仅保留 child module declarations、type re-exports 和 external test-module hook；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle module-plugin projection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 module plugin 面板投影 helper 与内联测试移入 `app/module_plugin_projection.rs`：新模块接管 primary action label/id、optional feature action label/id、feature dependency summary、target mode label、packaging label 与 fallback project manifest；`host_lifecycle.rs` 保留 `module_plugins_pane_data(...)` 的 editor-manager/chrome 读取和生命周期编排。拆分后 `host_lifecycle.rs` 从 1987 行降为 1739 行，`module_plugin_projection.rs` 为 257 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle module-plugin projection ownership scan 为 clean，确认根 `host_lifecycle.rs` 不再定义 module plugin label/action/summary/fallback helper 或 module-plugin inline tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（host_lifecycle 其他纯投影 helper、ui/apply_presentation、ui_asset_editor 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle viewport-toolbar projection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 viewport toolbar surface-frame 附着与 projection-control action-id 映射移入 `app/viewport_toolbar_projection.rs`：新模块接管 docked Scene/Game panes 与 floating Scene/Game windows 的 toolbar surface frame attachment、toolbar size 推导、floating window row 重建、host presentation 回写，以及 `SetTool`/`SetTransformSpace`/`SetProjectionMode`/`AlignView` 等 projection control 到稳定 action id 的映射；`host_lifecycle.rs` 保留 recompute/native-window sync 调用点。拆分后 `host_lifecycle.rs` 降为 1523 行，`viewport_toolbar_projection.rs` 为 220 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle viewport-toolbar projection ownership scan 为 clean，确认根 `host_lifecycle.rs` 不再定义 toolbar attachment helper、toolbar pane attachment helper、projection control action-id helper 或 viewport toolbar action mapping helper；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（host_lifecycle 其他纯投影 helper、ui/apply_presentation、ui_asset_editor 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle module-plugin pane-data projection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `RetainedEditorHost::module_plugins_pane_data(...)` 从 `app/host_lifecycle.rs` 移入 `app/module_plugin_projection.rs`：新 owner 现在完整接管 module plugin 面板数据投影，包括 active project plugin status 读取、project manifest fallback、`ModulePluginsPaneViewData` 构造、`ModulePluginStatusViewData` row mapping、primary/feature/packaging/target-mode/unload/hot-reload action label/id 构造与 diagnostics 汇总；`host_lifecycle.rs` 仅保留 recompute 调用点。拆分后 `host_lifecycle.rs` 降为 1411 行，`module_plugin_projection.rs` 为 367 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle module-plugin pane-data ownership scan 为 clean，确认根 `host_lifecycle.rs` 不再定义 `module_plugins_pane_data(...)`、module plugin row mapping、catalog fallback、primary/feature helper 或 helper import；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（host_lifecycle build export pane projection、ui/apply_presentation、ui_asset_editor 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle build-export projection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `RetainedEditorHost::build_export_pane_data(...)` 从 `app/host_lifecycle.rs` 移入 `app/build_export_projection.rs`：新 owner 接管 Build/Export 面板数据投影，包括 project manifest 读取、desktop export profiles 合并、native-aware export plan 生成、`BuildExportTargetViewData` row 构造、output diagnostic 前缀、completed report overlay、queued/running job overlay 与 wizard view-model 选择；`host_lifecycle.rs` 仅保留 recompute 调用点。拆分后 `host_lifecycle.rs` 降为 1218 行，`build_export_projection.rs` 为 197 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle build-export projection ownership scan 为 clean，确认根 `host_lifecycle.rs` 不再定义 `build_export_pane_data(...)`、output diagnostic helper、target row construction、manifest load projection、summary/job overlay 或相关 helper imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（host_lifecycle 剩余 runtime/apply/presentation orchestration、ui/apply_presentation、ui_asset_editor 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset editor detail-event/style subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor.rs` 中 UI Asset detail/collection 事件分发、component adapter commit、semantic action-path helpers 与相关内联测试移入 `app/ui_asset_editor_detail_events.rs`，再将 style/theme 相关 detail handler 移入 `app/ui_asset_editor_detail_events/style.rs`；`ui_asset_editor.rs` 现在只保留顶层 UI Asset editor action routing。拆分后 `ui_asset_editor.rs` 从 1348 行降为 287 行，`ui_asset_editor_detail_events.rs` 为 872 行，`style.rs` 为 204 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset editor detail-event/style ownership scan 为 clean，确认根 `ui_asset_editor.rs` 不再定义 detail dispatch、detail handler、component adapter commit、semantic action-path helper、detail route import 或 inline semantic tests，且 detail dispatcher 不再定义 style/theme handler；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（UI Asset detail events 的 binding/preview/collection 子域、helpers、build_export_actions、module_plugin_actions 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 app helpers animation-assets subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers.rs` 中 glTF/glb derived animation asset 工作流移入 `app/helpers/animation_assets.rs`：新模块接管 `derive_animation_assets_from_model_source(...)`、derived skeleton/clip DTO、gltf skin/animation traversal、channel/interpolation mapping、generated animation asset 写入，以及稳定 sibling 文件与 reimport asset-id 保持的回归 fixture；`helpers.rs` 保留 callback source、surface visibility、shell region、model staging 与 resource locator normalization helper，并 re-export animation derivation entry。拆分后 `helpers.rs` 从 1061 行降为 453 行，`animation_assets.rs` 为 627 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers animation-assets ownership scan 为 clean，确认根 `helpers.rs` 不再定义 derived skeleton/clip DTO、gltf animation derivation helpers、generated animation asset writes、animation regression fixture 或 animation asset imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（build_export_actions、module_plugin_actions、pane_surface_actions、callback_wiring、showcase_event_inputs、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export job queue subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions.rs` 中 desktop export async job queue 移入 `app/build_export_actions/job_queue.rs`：新模块接管 `DesktopExportJobQueue`、job/progress snapshot、cancellation result、pending/active job DTO、worker thread spawn、progress/result polling、status task projection、pane job overlay helper 与 result-to-summary conversion；`build_export_actions.rs` 保留 action id parsing、profile definitions、output override/reveal/pick side effects 与 `RetainedEditorHost` action methods。拆分后 `build_export_actions.rs` 从 896 行降为 498 行，`job_queue.rs` 为 413 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export job-queue ownership scan 为 clean，确认父模块不再定义 job queue DTO、pending/active/result/progress/message structs、queue impl、worker spawn、queue status projection 或 result conversion；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（module_plugin_actions、pane_surface_actions、callback_wiring、showcase_event_inputs、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 module-plugin live-host backend subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions.rs` 中 live plugin host 后端契约、命令 DTO、native adapter、卸载/热重载 dispatch validation、成功消息格式化与后端相关测试移入 `app/module_plugin_actions/live_host.rs`；父模块保留 Plugin Manager action-id parsing、project manifest policy mutation 与 `RetainedEditorHost::dispatch_module_plugin_action(...)`。拆分后 `module_plugin_actions.rs` 从 768 行降为 528 行，`live_host.rs` 为 253 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin live-host ownership scan 为 clean，确认父模块不再定义 live-host trait/request/outcome/command/backend adapter/dispatch helper/success formatter/unavailable formatter 或 `NativePluginLiveHost` import；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露 backend trait re-export 可见性过窄，修正为 app 边界后复跑通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（pane_surface_actions、callback_wiring、showcase_event_inputs、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 pane-surface showcase subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pane_surface_actions.rs` 中 Component Showcase 与 Material Lab host dispatch 移入 `app/pane_surface_actions/component_showcase.rs`：新模块接管 showcase action-id 到 runtime binding 的解析、demo event input 选择、drag/context/option/edit dispatch、Material Lab feedback、virtual-list/paged-list demo 状态推进与 presentation invalidation；父模块保留通用 pane surface click/edit routing、componentized Workbench dispatch、UI Asset detail edit binding parse、module-plugin/build-export/performance action forwarding。拆分后 `pane_surface_actions.rs` 从 587 行降为 290 行，`component_showcase.rs` 为 301 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pane-surface showcase ownership scan 为 clean，确认父模块不再定义 showcase dispatch methods、showcase action-id helper、demo input host-state helper、Material Lab handler、showcase constants/imports 或 camel-case normalization helper；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring、showcase_event_inputs、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 callback-wiring pane-surface subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring.rs` 中 `PaneSurfaceHostContext` callback registration 移入 `app/callback_wiring/pane_surface.rs`：新模块接管 Welcome/Hierarchy/Console/Inspector/pane surface controls/Workbench context menu/Component Showcase/mesh import/asset panes/viewport/UI Asset editor callbacks；根模块保留 `UiHostContext` frame/keyboard/menu/activity/tab/header/drag/resize callbacks、`dispatch_with_callback_source(...)` 与顶层 `wire_callbacks(...)` 装配入口。拆分后 `callback_wiring.rs` 从 669 行降为 153 行，`pane_surface.rs` 为 527 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring pane-surface ownership scan 为 clean，确认根模块不再导入 `PaneSurfaceHostContext`/`SharedString`，也不再注册 pane-surface/welcome/hierarchy/inspector/surface/component-showcase/asset/viewport/UI Asset callbacks；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring/pane_surface 子分组、showcase_event_inputs、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 callback-wiring asset callbacks subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring/pane_surface.rs` 中 mesh import 与 asset pane callback registration 移入 `app/callback_wiring/pane_surface/assets.rs`：新模块接管 mesh import path edit、asset control change/click、asset tree/content/reference pointer callbacks 与 browser asset details scroll；`pane_surface.rs` 保留 Welcome/Hierarchy/Console/Inspector/surface controls/Workbench context menu/Component Showcase/viewport/UI Asset callbacks 以及 asset 子分组调用。拆分后 `pane_surface.rs` 从 527 行降为 324 行，`assets.rs` 为 216 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring asset callback ownership scan 为 clean，确认 `pane_surface.rs` 不再直接注册 mesh import、asset control、asset tree/content/reference 或 browser asset details callbacks；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring/pane_surface viewport/UI Asset 子分组、showcase_event_inputs、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 showcase edit-input subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/showcase_event_inputs.rs` 中 edit string payload parsing 与 edit-focused regressions 移入 `app/showcase_event_inputs/edit.rs`：新模块接管 context menu anchor、virtual-list range、paged-list request、array/map row edit、number/range edit 与 fallback string value parsing；根模块保留 action click/activation demo mapping、action id normalization、shared demo collection values 与 `select_option(...)` re-export surface。拆分后 `showcase_event_inputs.rs` 从 645 行降为 349 行，`edit.rs` 为 306 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app showcase edit-input ownership scan 为 clean，确认根模块不再定义 edit parser entry、edit row payload parser、popup/list/page request parser 或 edit payload tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring/pane_surface viewport/UI Asset 子分组、showcase action mapping 子域、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset collection-event subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events.rs` 中 UI Asset collection row selection/activation dispatch 移入 `app/ui_asset_editor_detail_events/collection.rs`：新模块接管 matched style rule、palette、hierarchy、preview、source outline、preview mock、binding、slot semantic 与 layout semantic collection ids 到 `EditorManager` selection/activation 的映射；detail dispatcher 保留 detail-id routing、component-adapter commit 与非 style/detail mutation handlers。拆分后 `ui_asset_editor_detail_events.rs` 从 872 行降为 780 行，`collection.rs` 为 97 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset collection-event ownership scan 为 clean，确认 detail dispatcher 不再定义 collection dispatch method、collection id match table 或 collection selection/activation manager calls；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（UI Asset detail preview/binding 子域、callback_wiring/pane_surface viewport/UI Asset 子分组、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset binding-detail subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events.rs` 中 UI Asset binding detail、binding payload 与 binding suggestion handlers 移入 `app/ui_asset_editor_detail_events/binding.rs`：新模块接管 binding add/delete、binding id/event/route/target component-adapter commits、binding payload upsert/delete、binding payload suggestion、binding route suggestion 与 binding action suggestion；detail dispatcher 保留 detail-id routing、component-adapter commit 与 widget/slot/layout/source/palette/preview mock handlers。拆分后 `ui_asset_editor_detail_events.rs` 从 780 行降为 588 行，`binding.rs` 为 202 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset binding-detail ownership scan 为 clean，确认 detail dispatcher 不再定义 binding detail/payload/suggestion handler、binding manager mutation calls 或 binding-specific unknown-action status branches；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（UI Asset detail preview/source/palette 子域、callback_wiring/pane_surface viewport/UI Asset 子分组、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset preview-detail subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events.rs` 中 UI Asset preview mock value、nested entry 与 suggestion handlers 移入 `app/ui_asset_editor_detail_events/preview.rs`：新模块接管 preview mock value set/clear、nested value/upsert/delete 与 suggestion apply；detail dispatcher 保留 detail-id routing、component-adapter commit 与 widget/slot/layout/source/palette handlers。拆分后 `ui_asset_editor_detail_events.rs` 从 588 行降为 490 行，`preview.rs` 为 103 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset preview-detail ownership scan 为 clean，确认 detail dispatcher 不再定义 preview mock handler、preview mock manager mutation calls 或 preview-specific unknown-action status branches；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（UI Asset detail source/palette 子域、callback_wiring/pane_surface viewport/UI Asset 子分组、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset source/palette-detail subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events.rs` 中 UI Asset source text/cursor 与 palette drag hover handlers 移入 `app/ui_asset_editor_detail_events/source.rs` 和 `app/ui_asset_editor_detail_events/palette.rs`：source 子模块接管 source text replacement 与 source cursor byte-offset selection；palette 子模块接管 palette drag hover coordinate parsing 与 selected drag target 更新；detail dispatcher 保留 detail-id routing、component-adapter commit 与 widget/slot/layout handlers。拆分后 `ui_asset_editor_detail_events.rs` 从 490 行降为 416 行，`source.rs` 为 38 行，`palette.rs` 为 47 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset source/palette-detail ownership scan 为 clean，确认 detail dispatcher 不再定义 source/palette handler、source/palette manager mutation calls、coordinate parsing 或 source/palette-specific unknown-action status branches；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（UI Asset detail widget/slot/layout 子域、callback_wiring/pane_surface viewport/UI Asset 子分组、tests、host_lifecycle 剩余 orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset widget/structure/component-adapter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events.rs` 中 component-adapter commit、widget/promote-widget handlers、slot/layout handlers 与 semantic action-path tests 移入 `component_adapter.rs`、`widget.rs`、`structure.rs`：component-adapter 子模块接管 `UiComponentEventEnvelope` 构造、runtime adapter dispatch、status text 与 presentation dirty 传播；widget 子模块接管 widget direct/generic prop-state commits 与 promote metadata edits；structure 子模块接管 slot/layout detail commits、semantic delete 与 semantic action-path mapping regressions。拆分后 `ui_asset_editor_detail_events.rs` 从 416 行降为 104 行，`component_adapter.rs` 为 38 行，`widget.rs` 为 84 行，`structure.rs` 为 217 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset widget/structure/component-adapter ownership scan 为 clean，确认 detail dispatcher 不再定义 widget/slot/layout/component-adapter handler、semantic helper/tests、component-adapter imports、widget route helper import 或 ViewInstanceId import；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring/pane_surface viewport/UI Asset 子分组、tests、host_lifecycle 剩余 orchestration、module/build/export/action owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 callback-wiring viewport/UI Asset callbacks subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring/pane_surface.rs` 中 viewport pointer/toolbar 与 UI Asset editor action/detail/collection callback registration 移入 `app/callback_wiring/pane_surface/viewport.rs` 和 `app/callback_wiring/pane_surface/ui_asset.rs`：viewport 子模块接管 viewport pointer event 与 toolbar pointer clicked source-window forwarding；UI Asset 子模块接管 `on_ui_asset_action`、`on_ui_asset_detail_event`、`on_ui_asset_collection_event` 到 retained host UI Asset action owners 的 forwarding；父模块保留 Welcome/Hierarchy/Console/Inspector/surface controls/Workbench context menu/Component Showcase callback 装配并委托 asset/viewport/UI Asset 子分组。拆分后 `pane_surface.rs` 从 324 行降为 245 行，`viewport.rs` 为 43 行，`ui_asset.rs` 为 59 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring viewport/UI Asset ownership scan 为 clean，确认父 `pane_surface.rs` 不再直接注册 viewport 或 UI Asset callbacks，只保留 `viewport::wire_viewport_callbacks(...)` 与 `ui_asset::wire_ui_asset_callbacks(...)` 委托；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring/pane_surface general/showcase/welcome 子分组、host_lifecycle 剩余 orchestration、module/build/export/action owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 callback-wiring Component Showcase callbacks subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring/pane_surface.rs` 中 Component Showcase activate、drag-delta、edit、context request 与 option selected callback registration 移入 `app/callback_wiring/pane_surface/component_showcase.rs`：新模块接管 `on_component_showcase_control_activated`、`on_component_showcase_control_drag_delta`、`on_component_showcase_control_edited`、`on_component_showcase_control_context_requested`、`on_component_showcase_option_selected` 到 retained host Component Showcase action owners 的 source-window forwarding；父模块保留 Welcome/Hierarchy/Console/Inspector/surface controls/Workbench context menu callback 装配并委托 component_showcase/asset/viewport/UI Asset 子分组。拆分后 `pane_surface.rs` 从 245 行降为 177 行，`component_showcase.rs` 为 79 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring Component Showcase ownership scan 为 clean，确认父 `pane_surface.rs` 不再直接注册 Component Showcase callbacks，只保留 `component_showcase::wire_component_showcase_callbacks(...)` 委托；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（callback_wiring/pane_surface welcome/hierarchy/inspector/control 子分组、host_lifecycle 剩余 orchestration、module/build/export/action owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle native-window presenter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `RetainedEditorHost::sync_native_window_presenters(...)` 从 `app/host_lifecycle.rs` 移入 `app/host_lifecycle/native_window_presenters.rs`：新子模块接管 native floating-window target 收集/清理、new native window callback wiring、close callback、module-plugin/build-export per-window pane payload projection、native window presentation application、viewport-toolbar surface-frame attachment 与 native floating-window metadata application；`host_lifecycle.rs` 保留 recompute 末尾调用点，`app.rs` 将 native window target/configure 测试 re-export 收窄到 `#[cfg(test)]`。拆分后 `host_lifecycle.rs` 从 1218 行降为 1124 行，`native_window_presenters.rs` 为 117 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle native-window presenter ownership scan 为 clean，确认主 `host_lifecycle.rs` 不再定义 native-window presenter sync、target collection、native-window metadata apply 或 native close fallback branch；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 `zircon_editor` 仍为 62 条既有 warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（host_lifecycle 剩余 startup/recompute/dispatch orchestration、tests、assets/module/build action owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle effects/invalidation bridge subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 status mutation、`UiHostEventEffects` application、active-recompute viewport resize effect handling、dispatch side-effect fanout、dirty-mask perf counter、pending UI perf scenario capture、paint-only invalidation 与 mark helper 移入 `app/host_lifecycle/dispatch_effects.rs` 和 `app/host_lifecycle/invalidation_bridge.rs`：新 `dispatch_effects` 子模块接管 status line/task progress、dispatch result/effect application、Workbench notifications、asset refresh side effects、model import、command palette 与 welcome surface side effects；新 `invalidation_bridge` 子模块接管 `HostInvalidationRoot` mutation、legacy dirty flag bridging、paint-only diagnostic publish 与 layout/presentation/render mark helpers。拆分后 `host_lifecycle.rs` 从 1124 行降为 952 行，`dispatch_effects.rs` 为 122 行，`invalidation_bridge.rs` 为 78 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle effects/invalidation ownership scan 为 clean，确认主 `host_lifecycle.rs` 不再定义 `record_ui_dirty_mask(...)`、dispatch side-effect helper、pending UI perf scenario capture、status mutation、invalidator 或 mark helpers；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 `zircon_editor` 仍为 62 条既有 warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（tests、assets/module/build action owners、remaining recompute/startup orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 module-plugin action-id/project-policy subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions.rs` 中 `workbench.plugin.*` action-id grammar、`ModulePluginAction` enum、feature action parser、project plugin policy helper、packaging/target-mode cycle、status label formatter、feature dependency enablement message formatter 与相关 regressions 移入 `app/module_plugin_actions/action_ids.rs` 和 `app/module_plugin_actions/project_policy.rs`：父模块保留 active project manifest load/save、`EditorManager` policy mutation calls、live-host unload/hot-reload dispatch 与 status-line propagation。拆分后 `module_plugin_actions.rs` 从 528 行降为 163 行，`action_ids.rs` 为 192 行，`project_policy.rs` 为 185 行，`live_host.rs` 保持 253 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin action-id/project-policy ownership scan 为 clean，确认父 `module_plugin_actions.rs` 不再定义 action enum/parser、feature parser、policy cycle helper、status label formatter、dependency enablement formatter 或 inline tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 `zircon_editor` 仍为 62 条既有 warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（tests、assets/build action owners、remaining app helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export action/profile subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions.rs` 中 `BuildExportAction` enum、`workbench.build_export.*` action-id parser、desktop/mobile/browser/headless export profile catalog、profile lookup、platform label formatter 与默认输出路径约定移入 `app/build_export_actions/action_ids.rs` 和 `app/build_export_actions/profiles.rs`：父模块保留 execution summary projection、queue/cancel/output/reveal host side effects、output override mutation、status-line propagation 与 `RetainedEditorHost` action methods；`build_export_actions/tests.rs` 显式导入测试所需 runtime profile enums。拆分后 `build_export_actions.rs` 从 498 行降为 360 行，`action_ids.rs` 为 78 行，`profiles.rs` 为 74 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export action/profile ownership scan 为 clean，确认父 `build_export_actions.rs` 不再定义 action enum/parser、profile catalog、profile lookup、platform label formatter、default output-root helper 或 runtime profile imports；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露新子模块 re-export 可见性过窄，改为 app 边界后复跑通过，只有既有 warning 噪声且 `zircon_editor` 仍为 62 条既有 warning。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（tests、assets、remaining build-export execution-summary/action side effects 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export execution-summary subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions.rs` 中 completed export summary DTO、exported/failed/cancelled result construction、status-line message formatter、pane diagnostics、fatal-state detection 与 completed-summary target-row projection 移入 `app/build_export_actions/execution_summary.rs`：父模块保留 queue/cancel/output/reveal host side effects、output override mutation、status-line propagation 与 `RetainedEditorHost` action methods；`job_queue.rs` 仅依赖 summary DTO constructor 生成完成/失败/取消结果。拆分后 `build_export_actions.rs` 从 360 行降为 218 行，`execution_summary.rs` 为 130 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export execution-summary ownership scan 为 clean，确认父 `build_export_actions.rs` 不再定义 execution state/summary、summary constructors、status message、pane diagnostics 或 completed-summary target-row helper；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（tests、assets、build-export remaining action side effects、helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 asset refresh subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/assets.rs` 中 asset/resource/editor-asset event drain、refresh-plan application、catalog/resource snapshot sync、selected asset detail refresh、visible asset preview refresh、startup refresh-event drain、paint-only preview redraw 与 refresh plan profiling counters 移入 `app/assets/refresh.rs`：父模块保留 asset surface bridge creation、model import、default material resolution、asset workspace sync entry 与 asset surface control dispatch；`backend_refresh.rs` 继续保留纯刷新策略判定。拆分后 `assets.rs` 从 438 行降为 174 行，`assets/refresh.rs` 为 270 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app assets refresh ownership scan 为 clean，确认父 `assets.rs` 不再定义 refresh execution、startup drain、catalog/resource snapshot helper、selected-detail/visible-preview refresh、paint-only redraw 或 refresh plan counters；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分仍偏大的 retained host owner（tests、helpers、pointer/layout owners、asset pointer/content owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle startup subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 retained host construction、test construction、manager/resource/event receiver resolution、startup session resolution、startup state fallback、builtin template runtime/bridge creation、native plugin live-host/play-mode backend setup、initial pointer/scroll/native window/invalidation state 与 bootstrap asset sync 移入 `app/host_lifecycle/startup.rs`：`host_lifecycle.rs` 保留 tick、refresh_ui、shell size sync、dirty recompute orchestration、render submission、viewport image polling 与 pane-payload collection。拆分后 `host_lifecycle.rs` 从 952 行降为 587 行，`startup.rs` 为 343 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup ownership scan 为 clean，确认主 `host_lifecycle.rs` 不再定义 retained-host constructors、startup session/state helpers、manager/resource resolution、startup bridge construction 或 startup fallback；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（pointer/layout、helpers、projection owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 pointer-layout asset-surface subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pointer_layout.rs` 中 activity/browser asset surface 的 tree/content/references/used-by layout sync、pane-surface hover/scroll state writeback、asset surface pointer-state accessors、asset workspace snapshot selection 与 reference-list layout helper 移入 `app/pointer_layout/asset_surfaces.rs`：父模块保留 menu/activity rail/host page/document tab/drawer header/welcome/hierarchy/detail scroll pointer layout sync，asset tree/content/reference pointer event dispatch 继续由各自 action owner 调用 app-internal asset surface helper。拆分后 `pointer_layout.rs` 从 403 行降为 256 行，`asset_surfaces.rs` 为 154 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pointer-layout asset-surface ownership scan 为 clean，确认父 `pointer_layout.rs` 不再定义 asset surface layout sync、asset surface state accessors、asset workspace snapshot helper、asset reference layout helper 或 asset tree/content/reference layout imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（helpers、module/build projection owners、asset pointer dispatch owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 app helpers callback-surface subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers.rs` 中 callback source window id 解析、callback source scoping、floating-window focus tracking、docked/floating/native callback surface fallback sizing、asset-surface kind mapping 与 callback source window tests 移入 `app/helpers/callback_surface.rs`：父模块保留 asset-surface visibility、window menu popup height、shell region key、model source staging 与 resource locator normalization。拆分后 `helpers.rs` 从 402 行降为 139 行，`callback_surface.rs` 为 274 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers callback-surface ownership scan 为 clean，确认父 `helpers.rs` 不再定义 callback source/focus/size fallback helper、floating-window content-size helper、asset-surface callback kind helper 或 callback source tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（module/plugin projection、asset pointer dispatch、showcase/action mapping、wizard session owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 module-plugin projection rows subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_projection.rs` 中 Plugin Manager row action label/id、optional feature action/summary、target-mode label、packaging label、fallback project manifest 与 projection helper tests 移入 `app/module_plugin_projection/rows.rs`：父模块保留 active project plugin status lookup、manifest load fallback diagnostics、`ModulePluginsPaneViewData` construction 与 row DTO assembly。拆分后 `module_plugin_projection.rs` 从 349 行降为 114 行，`rows.rs` 为 242 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin projection rows ownership scan 为 clean，确认父 `module_plugin_projection.rs` 不再定义 row action/label helper、feature summary/action helper、target/packaging label helper、fallback manifest helper 或 inline helper tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（asset pointer dispatch、showcase/action mapping、build-export wizard session、invalidation owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 showcase action-input subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/showcase_event_inputs.rs` 中 Component Showcase action id 到 demo input 的映射、asset field binding suffix 处理、reference drop/collection/list/world-surface action demo values 与 action-input regressions 移入 `app/showcase_event_inputs/action.rs`：父模块保留 action/edit 子模块 re-export、shared action-id normalization、virtual/paged list defaults 与 `select_option(...)`，`edit.rs` 改为显式导入共享 helper。拆分后 `showcase_event_inputs.rs` 从 333 行降为 44 行，`action.rs` 为 296 行，`edit.rs` 为 298 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app showcase action-input ownership scan 为 clean，确认父 `showcase_event_inputs.rs` 不再定义 action-input mapping、asset field binding suffix constants、collection demo value helpers 或 inline action-input tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（asset reference/content pointer dispatch、build-export wizard session、invalidation/pane-surface action owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 asset-reference pointer target subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_reference_pointer.rs` 中 reference/used-by list 的 target preparation、callback-size resolution、list layout sync、prepared bridge dispatch、target-error drag clearing 与 pointer state writeback 移入 `app/asset_reference_pointer/target.rs`：父模块保留 press/click/move/scroll retained-host action entry、runtime Asset Surface bridge click forwarding、dispatch effects application 与 reference-row drag payload creation。拆分后 `asset_reference_pointer.rs` 从 336 行降为 235 行，`target.rs` 为 124 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-reference pointer target ownership scan 为 clean，确认父 `asset_reference_pointer.rs` 不再重复定义 workspace/list-size target preparation、reference-list bridge sync block 或 pointer-state writeback block；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（asset content pointer dispatch、build-export wizard session、invalidation/pane-surface action owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 asset-content pointer target subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_content_pointer.rs` 中 content list 的 target preparation、callback-size resolution、content layout sync、prepared bridge dispatch、target-error drag clearing 与 pointer state writeback 移入 `app/asset_content_pointer/target.rs`：父模块保留 press/click/move/scroll retained-host action entry、runtime Asset Surface bridge click forwarding、dispatch effects application 与 content-row drag payload creation。拆分后 `asset_content_pointer.rs` 从 269 行降为 181 行，`target.rs` 为 98 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-content pointer target ownership scan 为 clean，确认父 `asset_content_pointer.rs` 不再重复定义 workspace/content-size target preparation、content bridge sync block 或 pointer-state writeback block；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（build-export wizard session、invalidation/pane-surface action、host lifecycle remaining orchestration owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export wizard surface-actions subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_wizard_session.rs` 中 desktop export wizard surface button/action-id mapping、wizard job id construction、required pipeline option validation 与 wizard status-line message formatting 移入 `app/build_export_wizard_session/surface_actions.rs`：父模块保留 per-profile `ExportWizardPanelSession` state、view-model lookup、plan regeneration、process-runner dispatch、polling、host option construction 与 `RetainedEditorHost` action entry。拆分后 `build_export_wizard_session.rs` 从 307 行降为 242 行，`surface_actions.rs` 为 79 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export wizard surface-actions ownership scan 为 clean，确认父 `build_export_wizard_session.rs` 不再定义 surface action DTO/parser、wizard job-id prefix/helper、required-options helper 或 wizard status message formatter；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（invalidation、pane-surface action、host lifecycle remaining orchestration、welcome/session owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 invalidation mask subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/invalidation.rs` 中 `HostInvalidationMask` dirty-domain constants、bit operations、dirty-flag conversion、recompute requirement predicates 与 summary formatting 移入 `app/invalidation/mask.rs`：父模块保留 `HostInvalidationRoot` pending recompute reasons、request counters、slow/render path rebuild counters 与 diagnostics snapshot。拆分后 `invalidation.rs` 从 287 行降为 174 行，`mask.rs` 为 115 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app invalidation mask ownership scan 为 clean，确认父 `invalidation.rs` 不再定义 mask bitset、dirty-domain constants、bitwise helpers、dirty-flag conversion、requires_* predicates 或 summary formatter；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（pane-surface action、host lifecycle remaining orchestration、welcome/session owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 pane-surface Workbench forwarding subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pane_surface_actions.rs` 中 componentized Workbench active-document guard、action-id to binding-id resolution、popup cancellation、menu item selection、control click dispatch、option selection、edit dispatch 与 command palette commit forwarding 移入 `app/pane_surface_actions/workbench_surface.rs`：父模块保留 pane-surface clicked/edited entry、Module Plugins/Build Export/performance forwarding、builtin pane/template fallback、UI Asset detail binding parsing 与 Build/Export route detection。拆分后 `pane_surface_actions.rs` 从 276 行降为 165 行，`workbench_surface.rs` 为 115 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pane-surface Workbench ownership scan 为 clean，确认父 `pane_surface_actions.rs` 不再定义 componentized Workbench guard、binding-id resolution 或 componentized Workbench click/option/edit forwarding body；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、welcome/session、viewport/projection owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 welcome-session actions subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/welcome_session.rs` 中 Welcome draft project field updates、create/open project actions、recent project open/remove handling、startup view opening 与 `WelcomeHostEvent` routing 移入 `app/welcome_session/actions.rs`：父模块保留 Welcome surface bridge creation、runtime snapshot refresh、Welcome page presentation、startup-session application 与 builtin Welcome control dispatch；recent-project pointer 继续通过 app 内可见事件入口复用动作逻辑。拆分后 `welcome_session.rs` 从 288 行降为 143 行，`actions.rs` 为 149 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app welcome-session actions ownership scan 为 clean，确认父 `welcome_session.rs` 不再定义 Welcome draft/project/recent/startup-view action helpers 或 event routing body；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、viewport/projection/workbench pointer owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle render-submission subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 render-dirty invalidation reason consumption、render rebuild diagnostics/perf counter、runtime render extract/UI submission、viewport backend-not-ready retry 与 viewport-content frame update scheduling 移入 `app/host_lifecycle/render_submission.rs`：父模块保留 `tick` sequencing、asset/job polling、shell-size sync、dirty recompute、viewport image polling 与 viewport error handling。拆分后 `host_lifecycle.rs` 从 587 行降为 536 行，`render_submission.rs` 为 59 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle render-submission ownership scan 为 clean，确认主 `host_lifecycle.rs` 不再定义 render-dirty submission block、viewport extract submit retry branch 或 render-path diagnostic logging body；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（remaining recompute/pane payload orchestration、viewport/projection/workbench pointer owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle pane-payload subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 recompute pane payload collection、preset name snapshot、UI Asset pane presentation collection、Animation Editor pane presentation collection、runtime diagnostics snapshot、Module Plugins pane data 与 Build/Export pane data gathering 移入 `app/host_lifecycle/pane_payloads.rs`：父模块保留 recompute sequencing、presentation assembly、viewport toolbar attachment、native-window sync 与 lifecycle orchestration。拆分后 `host_lifecycle.rs` 从 536 行降为 442 行，`pane_payloads.rs` 为 128 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle pane-payload ownership scan 为 clean，确认主 `host_lifecycle.rs` 不再定义 pane payload collection blocks、UI Asset/Animation pane collectors、Module Plugins/Build Export default pane data fallback 或 runtime diagnostics collection body；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（viewport/projection/workbench pointer owners、tests、remaining app orchestration helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 UI Asset action-routing subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor.rs` 中 top-level UI Asset action-id mapping、source-window focus、status-line message handling、post-save asset workspace sync 与 presentation invalidation 移入 `app/ui_asset_editor/actions.rs`：父模块保留 app-visible retained-host UI Asset action entry 并委托 action 子模块，外部 retained host 回调入口不变。拆分后 `ui_asset_editor.rs` 从 287 行降为 9 行，`actions.rs` 为 287 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset action routing ownership scan 为 clean，确认根 `ui_asset_editor.rs` 不再定义 action match table、EditorManager UI Asset action calls、source-window focus、post-save sync 或 presentation invalidation body；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（workspace docking、pointer layout、build/export wizard、viewport/workbench pointer owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 workspace docking drag/drop-resize subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/workspace_docking.rs` 中 tab drag target sync、drop-route resolution、detached-window fallback target construction、runtime tab-drop dispatch、drawer resize capture、transient preferred-size updates、runtime resize dispatch 与 resize invalidation 移入 `app/workspace_docking/drag_drop.rs` 和 `app/workspace_docking/drawer_resize.rs`：父模块保留 host drag/resize pointer event kind normalization、committed pointer-layout refresh 与子模块委托。拆分后 `workspace_docking.rs` 从 256 行降为 61 行，`drag_drop.rs` 为 103 行，`drawer_resize.rs` 为 101 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app workspace-docking drag/drop and drawer-resize ownership scan 为 clean，确认根 `workspace_docking.rs` 不再定义 tab-drop route resolution、detached-window id construction、runtime tab-drop dispatch、drawer resize capture body、ShellFrame conversion 或 runtime resize dispatch body；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（pointer layout、build/export wizard、viewport/workbench pointer owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 pointer-layout welcome/detail-scroll subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pointer_layout.rs` 中 Welcome recent-project list pane-size resolution/layout sync/hover-action state writeback，以及 Console、Inspector、browser asset details scroll layout sync/scroll-offset writeback 移入 `app/pointer_layout/welcome_recent.rs` 和 `app/pointer_layout/detail_scrolls.rs`：父模块保留 menu、activity rail、host page、document tab、drawer header、hierarchy shell-level layout sync 与 pane-surface host accessor。拆分后 `pointer_layout.rs` 从 284 行降为 146 行，`welcome_recent.rs` 为 74 行，`detail_scrolls.rs` 为 77 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pointer-layout welcome/detail-scroll ownership scan 为 clean，确认根 `pointer_layout.rs` 不再定义 Welcome recent sizing/layout/writeback、Console/Inspector/browser asset detail scroll sync 或 detail scroll state writeback；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（build/export wizard、viewport/workbench pointer owners、native-window close/invalidation/detail owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export wizard options subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_wizard_session.rs` 中 active-project export wizard option construction、project manifest/output-root resolution、desktop export profile lookup、source asset manifest defaulting、host executable path construction、target-platform option filling 与 engine repo-root derivation 移入 `app/build_export_wizard_session/options.rs`：父模块保留 per-profile session state、view-model lookup、plan regeneration、process-runner dispatch、polling 与 host action dispatch。拆分后 `build_export_wizard_session.rs` 从 261 行降为 211 行，`options.rs` 为 59 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export wizard options ownership scan 为 clean，确认父 `build_export_wizard_session.rs` 不再定义 wizard option construction、default source asset manifest constant、default host executable helper、engine repo-root helper、project_root_path import 或 ExportProfile/PathBuf imports；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（viewport/workbench pointer owners、native-window close/invalidation/detail owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 asset pointer press-drag subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_content_pointer.rs` 与 `app/asset_reference_pointer.rs` 中 press dispatch、incompatible scene/object drag clearing、prepared press bridge dispatch、active asset drag payload creation/clearing 与 drag-source status text 移入 `app/asset_content_pointer/press.rs` 和 `app/asset_reference_pointer/press.rs`：父模块保留 press/release gate、click runtime Asset Surface bridge forwarding、move/scroll dispatch entry、dispatch effects application 与 target-preparation 委托。拆分后 `asset_content_pointer.rs` 从 191 行降为 149 行，`asset_content_pointer/press.rs` 为 57 行；`asset_reference_pointer.rs` 从 245 行降为 196 行，`asset_reference_pointer/press.rs` 为 65 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-reference/content pointer press ownership scan 为 clean，确认父 content/reference pointer 模块不再定义 incompatible drag clearing、press prepared dispatch body、active asset drag payload creation/clearing 或 drag-source summary status text；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（viewport/workbench pointer owners、native-window close/invalidation/detail owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 host lifecycle recompute viewport subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 recompute viewport content frame derivation、viewport resize dispatch、post-resize chrome/model rebuild、viewport pointer frame update、shell pointer bridge layout update 与 activity rail/host page/document tab/drawer header pointer layout sync 移入 `app/host_lifecycle/recompute_viewport.rs`：父模块保留 recompute sequencing、layout/model/chrome construction、pane payload collection、presentation publication、viewport surface submission 与 native-window sync。拆分后 `host_lifecycle.rs` 从 442 行降为 393 行，`recompute_viewport.rs` 为 64 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute viewport ownership scan 为 clean，确认主 `host_lifecycle.rs` 不再定义 viewport content frame extraction、viewport resize dispatch block、viewport pointer frame update、shell pointer bridge layout update 或 shell pointer layout sync block；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（workbench pointer、native-window close/invalidation/detail owners、tests 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 viewport pointer/toolbar subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/viewport.rs` 中 native viewport pointer kind/button mapping、world-space UI pointer routing、callback-source focus、runtime viewport pointer dispatch、viewport toolbar click dispatch、toolbar surface-size resolution 与 document/drawer/floating-window width fallback 分别移入 `app/viewport/pointer_event.rs` 和 `app/viewport/toolbar_pointer.rs`：根 `viewport.rs` 变为 2 行结构入口，`pointer_event.rs` 为 97 行，`toolbar_pointer.rs` 为 117 行；`app.rs` 清理已下沉的 toolbar layout builder 导入。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app viewport pointer/toolbar ownership scan 为 clean，确认根 `viewport.rs` 不再定义 pointer event、toolbar click/size、mapping/status 或 frame-width helper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，清理 transient unused import 后 editor warning 数回到既有 62 条。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（native-window close、build/export projection/action、workbench pointer、host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 native-window close request/action subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/native_window_close.rs` 中 close prompt action handling、prompt show/clear/window targeting、automatic dirty document save、prompted close completion、floating-window document instance collection、`LayoutCommand::CloseView` dispatch 与 no-prompt floating-window close confirmation 分别移入 `app/native_window_close/prompt_actions.rs` 和 `app/native_window_close/floating_window.rs`：根 `native_window_close.rs` 从 187 行降为 45 行，仅保留 main/floating native close-request entry；`prompt_actions.rs` 为 101 行，`floating_window.rs` 为 60 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app native-window close ownership scan 为 clean，确认根文件只保留 request entry 和对子模块 helper 的调用，不再定义 prompt action/save/show/finish 或 floating-window traversal/close implementation；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 首次二次归档因 244s 超时中断，确认无残留编译进程后以更长超时重跑通过，只有既有 warning 噪声且 editor warning 数保持 62 条。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（build/export projection/action、workbench pointer、host lifecycle remaining orchestration、projection owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export projection target subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_projection.rs` 中 active project root/manifest resolution、per-profile export plan generation、target-row DTO construction、output-root diagnostic prepending、completed-summary overlay 与 queued/running job overlay 移入 `app/build_export_projection/targets.rs`：根 `build_export_projection.rs` 从 191 行降为 24 行，仅保留 pane data assembly、diagnostics vector 和 wizard view-model attachment；`targets.rs` 为 164 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export projection-target ownership scan 为 clean，确认根 `build_export_projection.rs` 不再定义 project manifest/root resolution、target-row DTO construction、export plan generation、diagnostic prepending 或 summary/job overlay logic；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（build/export action side effects、workbench pointer、host lifecycle remaining orchestration、projection owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 Workbench pointer shell/document/floating subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/workbench_pointer.rs` 中 activity rail、host page tab、drawer header tab、document tab activation/close 与 floating-window header focus routing 拆入 `app/workbench_pointer/shell_chrome.rs`、`app/workbench_pointer/document_tabs.rs` 和 `app/workbench_pointer/floating_window.rs`：根 `workbench_pointer.rs` 从 177 行降为 3 行结构入口；`shell_chrome.rs` 为 95 行，`document_tabs.rs` 为 70 行，`floating_window.rs` 为 30 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app Workbench pointer ownership scan 为 clean，确认根 `workbench_pointer.rs` 不再定义 activity/host-page/document-tab/floating-window/drawer-header callback bodies、shared dispatch calls 或 shell route matching；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，只有既有 warning 噪声且 editor warning 数保持 62 条。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（build/export action side effects、host lifecycle remaining orchestration、projection owners、inspector/profiling owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-18 | 08 M3.S2 build-export host-action subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；当前二次 Cargo check 被下层运行时渲染改动阻断） | 将 `app/build_export_actions.rs` 中 desktop export job polling/start-next/status-task sync、parsed Build/Export action dispatch、enqueue/cancel/output override mutation、folder picker/reveal side effects 与 effective output-root resolution 移入 `app/build_export_actions/host_actions.rs`：根 `build_export_actions.rs` 从 218 行降为 21 行结构入口；`host_actions.rs` 为 209 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export host-action ownership scan 为 clean，确认根 `build_export_actions.rs` 不再定义 retained-host action impl、job polling、enqueue/cancel/output/reveal side effects、project manifest/root resolution 或 effective output-root helper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。初始 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 曾通过并确认 app-local 可见性；本轮二次归档同命令在进入 `zircon_editor` 前被当前 `zircon_runtime` 后处理渲染改动阻断：`execute_*` 新文件缺少 `UVec2` 导入，`gpu/post_process.rs` 的 `execute_post_process` 调用实参数量不匹配。按用户“先实现功能，测试暂时延后”要求，不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、projection owners、inspector/profiling owners、asset pointer owners 等）；milestone testing stage 前需先恢复 `zircon_runtime` 当前渲染编译错误再补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 viewport-toolbar projection hit-control subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被下层运行时渲染改动阻断） | 将 `app/viewport_toolbar_projection.rs` 中 projection control id 到 stable viewport toolbar hit-control id 的映射（tool、transform-space、projection-mode、align-view、display/grid/snap、preview toggles、frame-selection、play-mode）移入 `app/viewport_toolbar_projection/hit_controls.rs`：根 `viewport_toolbar_projection.rs` 从 207 行降为 151 行，仅保留 presentation-time toolbar surface-frame projection；`hit_controls.rs` 为 59 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app viewport toolbar projection hit-control ownership scan 为 clean，确认根 `viewport_toolbar_projection.rs` 不再定义 toolbar action-id mapping helpers 或 stable action id literals；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 与更窄的 `cargo check -p zircon_editor --lib --no-default-features --locked --jobs 1 --message-format short --color never` 均在进入 editor 代码前被当前 `zircon_runtime` 后处理渲染改动阻断：多个 `execute_*` 模块缺少 `UVec2` 导入，`gpu/post_process.rs` 的 `execute_post_process` 调用实参数量不匹配。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、inspector/profiling owners、asset pointer owners 等）；milestone testing stage 前需先恢复 `zircon_runtime` 当前渲染编译错误再补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 inspector drag/control subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被下层运行时渲染改动阻断） | 将 `app/inspector.rs` 中 inspector reference pointer object-drag payload construction、selected inspector source metadata、field-id mapping、batch apply argument construction、component-adapter value change dispatch、click dispatch 与 builtin inspector surface control dispatch 分别移入 `app/inspector/drag_source.rs` 和 `app/inspector/surface_controls.rs`：根 `inspector.rs` 从 174 行降为 2 行结构入口；`drag_source.rs` 为 50 行，`surface_controls.rs` 为 141 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app inspector drag/control ownership scan 为 clean，确认根 `inspector.rs` 不再定义 retained-host impl、drag payload helper、field-id/apply-argument helper、component event envelope 或 inspector dispatch methods；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 仍在进入 editor 代码前被当前 `zircon_runtime` 后处理渲染改动阻断。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、profiling owner、asset pointer/tree owners 等）；milestone testing stage 前需先恢复 `zircon_runtime` 当前渲染编译错误再补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 profiling action/diagnostics subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被下层运行时渲染改动阻断） | 将 `app/profiling.rs` 中 performance timeline action-id mapping/dispatch/status formatting、runtime diagnostics profile enrichment、editor/runtime `ProfileSnapshot` merge/span id remap/session-id merge 分别移入 `app/profiling/actions.rs`、`app/profiling/diagnostics.rs` 和 `app/profiling/snapshot_merge.rs`：根 `profiling.rs` 从 172 行降为 5 行结构入口；`actions.rs` 为 101 行，`diagnostics.rs` 为 36 行，`snapshot_merge.rs` 为 43 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app profiling action/diagnostics ownership scan 为 clean，确认根 `profiling.rs` 不再定义 retained-host impl、performance timeline action constants/mapping/status helper、diagnostics profile merge entry 或 snapshot merge helpers；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 仍在进入 editor 代码前被当前 `zircon_runtime` 后处理渲染改动阻断：多个 `execute_*` 模块缺少 `UVec2` 导入，`gpu/post_process.rs` 的 `execute_post_process` 调用实参数量不匹配。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、asset pointer/tree owners、native/window/pane owners 等）；milestone testing stage 前需先恢复 `zircon_runtime` 当前渲染编译错误再补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-tree pointer target/dispatch subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被下层运行时渲染改动阻断） | 将 `app/asset_tree_pointer.rs` 中 committed pointer layout reuse、callback-source focus、asset workspace snapshot lookup、surface-size fallback、tree bridge layout sync、Activity/Browser tree click/move/scroll dispatch 与 pointer-state/effects application 分别移入 `app/asset_tree_pointer/target.rs` 和 `app/asset_tree_pointer/dispatch.rs`：根 `asset_tree_pointer.rs` 从 183 行降为 2 行结构入口；`dispatch.rs` 为 112 行，`target.rs` 为 41 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-tree pointer target/dispatch ownership scan 为 clean，确认根 `asset_tree_pointer.rs` 不再定义 retained-host impl、tree target prep、tree bridge sync、shared asset tree click dispatch 或 move/scroll handlers；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 仍在进入 editor 代码前被当前 `zircon_runtime` 后处理渲染改动阻断：多个 `execute_*` 模块缺少 `UVec2` 导入，`gpu/post_process.rs` 的 `execute_post_process` 调用实参数量不匹配。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（asset pointer owners、host lifecycle remaining orchestration、native/window/pane owners 等）；milestone testing stage 前需先恢复 `zircon_runtime` 当前渲染编译错误再补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 close-prompt model/presentation/action subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/close_prompt.rs` 中 close prompt action-id parsing、dirty view model/save eligibility、pending prompt target state、`HostClosePromptData` construction、dialog/button geometry 与 prompt title/message/details 投影分别移入 `app/close_prompt/actions.rs`、`app/close_prompt/model.rs` 和 `app/close_prompt/presentation.rs`：根 `close_prompt.rs` 从 158 行降为 8 行结构入口；`actions.rs` 为 8 行，`model.rs` 为 67 行，`presentation.rs` 为 88 行。验证期间同步修复 `scene/viewport/render_packet.rs`，把 editor `ViewportCameraSnapshot` 转换为 runtime `CameraRenderDescriptor` 后再写入 `SceneViewportExtractRequest::camera`，对齐当前运行时相机顺序契约。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app close-prompt model/presentation ownership scan 为 clean，确认根 `close_prompt.rs` 不再定义 prompt DTO、button geometry、dirty-view filtering、save eligibility、action parsing 或 host presentation helper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（asset pointer owners、host lifecycle remaining orchestration、native/window/pane owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 hierarchy-pointer target/events/drag-source subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/hierarchy_pointer.rs` 中 hierarchy pointer surface-size resolution、committed pointer-layout reuse、scene-entry snapshot retrieval、hierarchy bridge layout sync、press/click/move/scroll dispatch 与 scene-node drag payload construction 分别移入 `app/hierarchy_pointer/target.rs`、`app/hierarchy_pointer/events.rs` 和 `app/hierarchy_pointer/drag_source.rs`：根 `hierarchy_pointer.rs` 从 158 行降为 3 行结构入口；`events.rs` 为 109 行，`target.rs` 为 23 行，`drag_source.rs` 为 27 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app hierarchy-pointer target/events/drag-source ownership scan 为 clean，确认根 `hierarchy_pointer.rs` 不再定义 retained-host impl、callback dispatch、surface target prep、drag payload helper 或 scene-entry drag metadata；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（asset pointer owners、host lifecycle remaining orchestration、native/window/pane owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset content/reference pointer events subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_content_pointer.rs` 与 `app/asset_reference_pointer.rs` 中 content/reference pointer press-release gates、click forwarding、move hover updates、scroll updates、shared runtime Asset Surface click dispatch、effect application 与 pointer-state writeback 分别移入 `app/asset_content_pointer/events.rs` 和 `app/asset_reference_pointer/events.rs`：两个根文件都降为 3 行结构入口；content events 为 138 行，reference events 为 185 行。既有 `press.rs` 继续拥有拖拽 payload 创建，`target.rs` 继续拥有 target prep/bridge sync。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset content/reference pointer events ownership scan 为 clean，确认两个根文件不再定义 retained-host impl、callback dispatch、click/move/scroll body、event gate 或 pointer-state writeback；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、native/window/pane owners、remaining app helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export wizard host-actions subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_wizard_session.rs` 中 retained-host wizard surface action dispatch、active-project options lookup for plan/start、wizard update application、poll status propagation 与 layout/status mutation 移入 `app/build_export_wizard_session/host_actions.rs`：根 session 文件保留 per-profile `ExportWizardPanelSession` state、view-model lookup、plan regeneration、process-runner dispatch 与 changed-update polling；`build_export_wizard_session.rs` 从 194 行降为 130 行，`host_actions.rs` 为 65 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export wizard host-actions ownership scan 为 clean，确认根 session 文件不再定义 retained-host action impl、surface action dispatch body、wizard update apply helper、poll status propagation 或 layout/status mutation；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、native/window/pane owners、remaining app helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin host-actions subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions.rs` 中 retained-host Plugin Manager action routing、active project manifest load/save、`EditorManager` policy mutation calls、live-host unload/hot-reload dispatch、status-line updates 与 layout invalidation 移入 `app/module_plugin_actions/host_actions.rs`：根 `module_plugin_actions.rs` 从 160 行降为 5 行结构入口；`host_actions.rs` 为 159 行；既有 `action_ids.rs`、`project_policy.rs`、`live_host.rs` 继续拥有 parser、deterministic policy helpers 与 live-host backend contracts。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin host-actions ownership scan 为 clean，确认根 `module_plugin_actions.rs` 不再定义 retained-host impl、manifest load/save、action parser consumption、EditorManager mutation dispatch、live-host dispatch 或 status/layout side effects；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、native/window/pane owners、remaining app helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 pane-surface generic click/edit/routing subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pane_surface_actions.rs` 中 generic pane surface click routing、feature action forwarding、built-in pane surface fallback、template binding fallback、dispatch result application、generic edit routing、UI Asset detail edit binding parser 与 Build/Export route predicate 分别移入 `app/pane_surface_actions/click.rs`、`edit.rs`、`ui_asset_detail.rs` 和 `routing.rs`：根 `pane_surface_actions.rs` 从 165 行降为 7 行结构入口；`click.rs` 为 53 行，`edit.rs` 为 55 行，`routing.rs` 为 28 行，`ui_asset_detail.rs` 为 33 行；既有 `component_showcase.rs` 和 `workbench_surface.rs` 继续拥有各自 feature dispatch。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pane-surface generic click/edit ownership scan 为 clean，确认根 `pane_surface_actions.rs` 不再定义 generic dispatch methods、UI Asset detail binding parser、Build/Export route predicate、callback dispatch 或 `UiBindingValue` 处理；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、native/window/pane owners、remaining app helpers 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 native-window target/presentation/store subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/native_windows.rs` 中 native floating-window target DTO/filtering、host presentation/window geometry writes、presenter map synchronization、stale window hiding、creation hook dispatch 与 window lookup 分别移入 `app/native_windows/target.rs`、`presentation.rs` 和 `store.rs`：根 `native_windows.rs` 从 131 行降为 8 行结构入口；`target.rs` 为 36 行，`presentation.rs` 为 37 行，`store.rs` 为 61 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app native-window target/presentation/store ownership scan 为 clean，确认根 `native_windows.rs` 不再定义 target collection、platform presentation writes、presenter map sync 或 stale-window lifecycle；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration、assets/invalidation/pointer layout 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 assets bridge/control/workspace subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/assets.rs` 中 lazy asset surface bridge creation、runtime asset surface callback dispatch、asset UI action-id mapping、click/change argument construction、default-scene reload、project model import、derived animation asset import、default material resolution 与 asset workspace sync entry 分别移入 `app/assets/bridge.rs`、`controls.rs` 和 `workspace.rs`：根 `assets.rs` 从 174 行降为 4 行结构入口；`bridge.rs` 为 45 行，`controls.rs` 为 60 行，`workspace.rs` 为 78 行；既有 `assets/refresh.rs` 继续拥有 refresh execution path。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app assets bridge/control/workspace ownership scan 为 clean，确认根 `assets.rs` 不再定义 retained-host impl、asset surface bridge/control dispatch、project import/default-scene reload、workspace sync 或 action-id mapper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区既有 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（invalidation/pointer layout/host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 pointer-layout menu/chrome/hierarchy subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pointer_layout.rs` 中 menu pointer layout sync/state writeback、activity rail/host page/document tab/drawer header layout sync、hierarchy layout sync/state writeback 与 shared pane-surface host accessor 分别移入 `app/pointer_layout/menu.rs`、`shell_chrome.rs`、`hierarchy.rs` 和 `pane_surface_context.rs`：根 `pointer_layout.rs` 从 135 行降为 7 行结构入口；`menu.rs` 为 51 行，`shell_chrome.rs` 为 56 行，`hierarchy.rs` 为 34 行，`pane_surface_context.rs` 为 9 行；既有 `asset_surfaces.rs`、`welcome_recent.rs` 和 `detail_scrolls.rs` 继续拥有各自 layout families。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pointer-layout menu/chrome/hierarchy ownership scan 为 clean，确认根 `pointer_layout.rs` 不再定义 menu/chrome/hierarchy sync methods 或 pane-surface host accessor；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露从根移出后兄弟模块调用的可见性问题，已将相关 methods 收窄为 `pub(in crate::ui::retained_host::app)` 并复跑通过，Cargo 仍输出当前工作区 warning 噪声（runtime 143 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（invalidation/host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 invalidation root/tests subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/invalidation.rs` 中 `HostInvalidationRoot` scheduling state、pending recompute reasons、request counters、slow/render rebuild counters、stats summary、diagnostics snapshot 与 root scheduling regressions 分别移入 `app/invalidation/root.rs` 和 `app/invalidation/root/tests.rs`：根 `invalidation.rs` 从 174 行降为 4 行结构入口；`root.rs` 为 103 行，`root/tests.rs` 为 67 行；既有 `mask.rs` 继续拥有 dirty-domain bitmask vocabulary。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app invalidation root/tests ownership scan 为 clean，确认根 `invalidation.rs` 不再定义 scheduling state、diagnostics snapshot 或 root regressions；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露从根移出后 sibling lifecycle callers 的可见性问题，已将 root type/methods 收窄为 `pub(in crate::ui::retained_host::app)` 并复跑通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 welcome-session bridge/session subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/welcome_session.rs` 中 lazy Welcome surface bridge creation、Welcome control-id mapping、builtin Welcome control dispatch、Welcome snapshot refresh、page presentation、startup-session application、startup builtin view opening、project workspace application、runtime world replacement 与 invalidation 分别移入 `app/welcome_session/bridge.rs` 和 `app/welcome_session/session.rs`：根 `welcome_session.rs` 从 143 行降为 3 行结构入口；`bridge.rs` 为 59 行，`session.rs` 为 86 行；既有 `actions.rs` 继续拥有 Welcome user action execution。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app welcome-session bridge/session ownership scan 为 clean，确认根 `welcome_session.rs` 不再定义 bridge creation/control dispatch/session application/snapshot refresh 或 control-id mapper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 viewport-toolbar projection surface-frame subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/viewport_toolbar_projection.rs` 中 document/left/right/bottom/floating-window toolbar surface-frame attachment、toolbar size derivation、pane eligibility、layout recompute 与 host presentation writeback 移入 `app/viewport_toolbar_projection/surface_frames.rs`：根 `viewport_toolbar_projection.rs` 从 151 行降为 3 行结构入口；`surface_frames.rs` 为 150 行；既有 `hit_controls.rs` 继续拥有 stable hit-control id mapping。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app viewport toolbar projection surface-frame ownership scan 为 clean，确认根 `viewport_toolbar_projection.rs` 不再定义 pane-data projection、toolbar size derivation、layout recompute 或 hit-control mapping；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露从根移出后 sibling lifecycle callers 的可见性问题，已将 `attach_viewport_toolbar_surface_frames_to_ui(...)` 收窄为 `pub(in crate::ui::retained_host::app)` 并复跑通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-drag payload content/reference subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_drag_payload.rs` 中 visible asset content row payload construction、activity/browser content source control ids、reference/used-by list selection、known-project filtering、reference source control ids 与 locator extension fallback 分别移入 `app/asset_drag_payload/content.rs` 和 `reference.rs`：根 `asset_drag_payload.rs` 从 113 行降为 4 行结构入口；`content.rs` 为 34 行，`reference.rs` 为 81 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-drag payload content/reference ownership scan 为 clean，确认根 `asset_drag_payload.rs` 不再定义 snapshot/reference payload construction、source control id mapping、known-project filtering 或 extension fallback；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 暴露从根移出后 pointer press sibling callers 的可见性问题，已将 exported payload functions 收窄为 `pub(in crate::ui::retained_host::app)` 并复跑通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle remaining orchestration 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 callback-wiring host-shell subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring.rs` 中 `UiHostContext` frame tick、unhandled keyboard、close prompt、menu pointer、activity rail、host page/document tab/floating header/drawer header、host drag 与 host resize callback registration 移入 `app/callback_wiring/host_shell.rs`：根 `callback_wiring.rs` 从 136 行降为 18 行结构入口，保留 `dispatch_with_callback_source(...)` 和总 `wire_callbacks(...)`；`host_shell.rs` 为 126 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring host-shell callback ownership scan 为 clean，确认根 `callback_wiring.rs` 不再定义 `UiHostContext` callback registration 或 host-shell pointer/keyboard/frame handlers；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 63 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 非测试 owner（host lifecycle/helpers/build export/module plugin subowners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 callback-wiring pane-surface region callback subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring/pane_surface.rs` 中 Welcome recent/control、Hierarchy pointer、Console scroll、Inspector pointer/control、generic pane surface control 与 Workbench context menu callback registration 拆入 `welcome.rs`、`hierarchy.rs`、`console.rs`、`inspector.rs`、`pane_controls.rs`：根 `pane_surface.rs` 从 177 行降为 30 行结构入口，仅解析 `PaneSurfaceHostContext` 并按区域委派；新增子文件行数分别为 61、40、16、45、41。既有 asset、Component Showcase、viewport、UI Asset callback 子 owner 保持不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring pane-surface region callback ownership scan 确认具体 `on_welcome_*`、`on_hierarchy_*`、`on_console_*`、`on_inspector_*`、`on_surface_control_*`、`on_workbench_context_*` callback registration 已迁入区域子文件；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 已尝试但在并发 `zircon_runtime` Cargo 任务活跃时超时，本切片不声明新的完整编译通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 callback-wiring asset callback subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring/pane_surface/assets.rs` 中 mesh import、asset control、asset tree、asset content、asset reference/used-by、browser asset detail callback registration 拆入 `assets/mesh_import.rs`、`controls.rs`、`tree.rs`、`content.rs`、`references.rs`、`details.rs`：根 `assets.rs` 从 216 行降为 22 行结构入口；新增子文件行数分别为 18、33、39、64、87、18。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring asset callback ownership scan 确认具体 `on_mesh_import_*`、`on_asset_control_*`、`on_asset_tree_*`、`on_asset_content_*`、`on_asset_reference_*`、`on_browser_asset_details_*` callback registration 已迁入资产子文件；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。由于并发 `zircon_runtime` Cargo 任务仍在运行，本切片不声明新的完整 `cargo check` 通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle recompute orchestration subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle.rs` 中 dirty recompute orchestration 整体迁入 `app/host_lifecycle/recompute.rs`：新子模块负责 invalidation reason 消费、paint-only fast path、slow-path diagnostics、layout/descriptors/chrome/model 构建、shell geometry、template/workbench/floating bridge recompute、floating-window projection bounds、viewport/pointer recompute、presentation application、world-space UI surface submission、native-window presenter sync、pointer-surface sync 与 dirty flag 清理。根 `host_lifecycle.rs` 从 375 行降为 96 行，保留 tick/refresh、committed pointer layout、chrome snapshot、shell-size sync 和 refresh diagnostics；`recompute.rs` 为 303 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute ownership scan 确认根文件不再保留 `compute_workbench_shell_geometry`、`apply_presentation`、floating-window projection、viewport surface submission 和 native-window presenter sync 的重算细节；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。由于并发 `zircon_runtime` Cargo test 任务仍在运行，本切片不声明新的完整 `cargo check` 通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 helpers visibility/geometry/model-staging subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers.rs` 中 asset-surface visibility、viewport/window-menu/shell-region geometry helpers、model source staging 与 resource locator normalization 拆入 `helpers/asset_visibility.rs`、`helpers/geometry.rs`、`helpers/model_staging.rs`：根 `helpers.rs` 从 152 行降为 13 行结构 re-export 入口；新增子文件行数分别为 51、32、68。`asset_uri_from_relative_path(...)` 作为 helpers 家族内部函数保留给 `animation_assets.rs` 复用。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers visibility/geometry/model-staging ownership scan 确认根文件不再定义 `asset_surface_visible`、`active_workspace_tab`、`viewport_size_from_frame`、`compute_window_menu_popup_height`、`shell_region_group_key`、`stage_model_source`、`asset_uri_from_relative_path`；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。由于并发 `zircon_runtime` Cargo 任务仍在运行，本切片不声明新的完整 `cargo check` 通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export wizard session-state subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_wizard_session.rs` 中 `DesktopExportWizardSessions`、per-profile session/last-update storage、view-model lookup、action dispatch with runner、plan regeneration、changed-update polling 与 missing-job error 构造迁入 `app/build_export_wizard_session/session_state.rs`：根文件从 140 行降为 8 行结构入口；`session_state.rs` 为 133 行。测试需要访问的 `sessions` 字段和 runner 注入方法保持父模块可见。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export wizard session-state ownership scan 确认根文件不再定义 `ExportWizardPanelSession` state/action/poll 逻辑；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。由于并发 `zircon_runtime` Cargo 任务仍在运行，本切片不声明新的完整 `cargo check` 通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin projection pane-data subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_projection.rs` 中 `RetainedEditorHost::module_plugins_pane_data(...)`、active project manifest lookup、native plugin status report 选择、builtin fallback diagnostics、`ModulePluginsPaneViewData` 构造和 row helper consumption 迁入 `app/module_plugin_projection/pane_data.rs`：根文件从 119 行降为 2 行结构入口；`pane_data.rs` 为 116 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin projection pane-data ownership scan 确认根文件不再保留 `module_plugins_pane_data`、`ProjectManifest`、`project_root_path`、plugin status report 或 pane view-model 构造逻辑；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。由于并发 `zircon_runtime` Cargo 任务仍在运行，本切片不声明新的完整 `cargo check` 通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset detail-dispatch subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events.rs` 中 `RetainedEditorHost::dispatch_ui_asset_detail_event(...)`、stable detail-id routing 和 unknown detail status fallback 迁入 `app/ui_asset_editor_detail_events/detail_dispatch.rs`：根文件从 104 行降为 12 行结构入口；`detail_dispatch.rs` 为 94 行。既有 binding/collection/component-adapter/palette/preview/source/style/structure/widget 子 owner 保持具体 detail mutation 归属。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset detail dispatch ownership scan 确认根文件不再保留 detail-id match、preview/binding suggestion route 或 unknown-detail fallback；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。由于并发 `zircon_runtime` Cargo 任务仍在运行，本切片不声明新的完整 `cargo check` 通过；完整 Cargo test/full validation matrix 按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 非测试 owner，并在并发构建空闲后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 retained-host owner split batch compile recovery | 完成（可见性修复、批量编译验证；完整测试矩阵按用户要求暂缓） | 针对本轮 callback-wiring、assets、host-lifecycle、helpers、build-export wizard、module-plugin projection、UI Asset detail-dispatch owner split 做批量编译恢复：`cargo check` 首次暴露 `DesktopExportWizardSessions` 从根移出后的 app 内可见性不足、`view_model/dispatch_profile_action/poll_all` 方法可见性不足，以及 `helpers/callback_surface.rs` 仍导入旧根级 `active_workspace_tab(...)`。已将 wizard session store 与 app 调用方法收窄为 `pub(in crate::ui::retained_host::app)`，保留测试所需 `sessions` 字段和 runner 注入方法父模块可见；将 `active_workspace_tab(...)` 作为 `asset_visibility.rs` 的 helpers-family 内部 helper，并让 callback surface 显式导入该路径。 | `cargo fmt -p zircon_editor` 通过；`cargo fmt -p zircon_editor --check` 通过；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。完整 Cargo test/full validation matrix 仍按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：可继续拆分 retained host 深层大 owner，或转入 milestone testing stage 前的剩余编译/集成验证清单。 |
| 2026-06-19 | 08 M3.S2 animation asset helper subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers/animation_assets.rs` 中 glTF derived animation workflow 的 skeleton derivation、clip/channel derivation、path/write helpers 与 regression fixtures 拆入 `app/helpers/animation_assets/skeleton.rs`、`clip.rs`、`paths.rs` 和 `tests.rs`：根 `animation_assets.rs` 从 627 行降为 74 行，仅保留 glTF import、derived skeleton/clip orchestration 与 generated locator ordering；新增子文件行数分别为 83、168、37、224。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers animation-assets subowner ownership scan 确认根文件不再定义 `DerivedSkeletonAsset` / `DerivedClipTrack`、channel builder、skeleton traversal、path sanitizer、file-write helper 或 glTF fixture；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle startup/recompute、build-export job queue、showcase event inputs 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export job-queue snapshot subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/job_queue.rs` 中 desktop export job/progress snapshot DTO、pane diagnostics、Build/Export target overlay application 与 status-task projection 移入 `app/build_export_actions/job_queue/snapshot.rs`：根 `job_queue.rs` 从 413 行降为 285 行，继续拥有 pending/active internal job state、enqueue/cancel/poll/start-next、worker-thread spawning 与 result-to-summary conversion；新 `snapshot.rs` 为 106 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export job-queue snapshot ownership scan 确认根文件不再定义 `status_label`、`pane_diagnostics`、`apply_job_snapshot_to_target`、`desktop_export_status_task_from_queue` 或 `StatusTaskProgressSnapshot` 投影；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle startup/recompute、showcase event inputs、build-export host actions 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 showcase event-input test subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/showcase_event_inputs/action.rs` 与 `edit.rs` 中剩余 inline action/edit regression tests 移入 `app/showcase_event_inputs/action_tests.rs` 和 `edit_tests.rs`：`action.rs` 从 296 行降为 243 行，`edit.rs` 从 298 行降为 159 行；新增测试子文件分别为 50 行和 133 行。生产 action/edit 文件继续拥有 action-id demo input mapping 与 edit payload parsing。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app showcase event-input test ownership scan 确认 action/edit 生产文件只保留 `#[path = ...]` 测试接入，不再承载 inline `mod tests` / `#[test]` 回归体；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle startup/recompute、build-export host actions、UI Asset action/detail owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup session/template-bridge subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/startup.rs` 中 startup request interpretation、startup session/state resolution、test-only startup-state fallback、shared builtin template runtime loading、host/window/floating/toolbar/inspector/pane startup bridge construction 与 Component Showcase runtime creation 拆入 `app/host_lifecycle/startup/session.rs` 和 `template_bridges.rs`：根 `startup.rs` 从 343 行降为 230 行，继续拥有 retained-host construction、manager/event receiver resolution、native plugin live-host/play-mode backend setup、host-state assembly、initial asset sync 与 refresh diagnostics；新子文件分别为 54 行和 85 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup session/template-bridge ownership scan 确认根 startup 文件不再定义 startup session/state helper、test fallback、`load_startup_builtin_template_runtime` 或具体 `Builtin*TemplateBridge::new_with_runtime` 初始化；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、build-export host actions、UI Asset action/detail owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset action domain subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor/actions.rs` 中 UI Asset stable action-id mapping 按领域拆入 `actions/workspace.rs`、`theme.rs`、`palette.rs`、`canvas.rs` 和 `mode_preview.rs`：根 `actions.rs` 从 287 行降为 53 行，只保留 callback-source focus、domain dispatch chain、unknown-action reporting、post-save workspace sync 与 presentation invalidation；新子文件分别为 75、86、45、53、89 行，分别拥有 workspace/emergency/undo/reference、theme/style、palette、canvas 和 mode/preview/locale actions。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset action domain ownership scan 确认根 action 文件不再直接导入 `UiAssetEditorMode`、`UiAssetPreviewPreset`、`UiDesignerToolMode`、`ViewDescriptorId` 或承载具体 action-id command table；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、build-export host actions、component-showcase pane actions、assets refresh 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 pane-surface component-showcase subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pane_surface_actions/component_showcase.rs` 中 Component Showcase runtime binding-id 解析、runtime event 应用、host-state-aware demo 输入推进与 Material Lab feedback 分别拆入 `component_showcase/bindings.rs`、`events.rs`、`inputs.rs` 和 `material_lab.rs`：根 `component_showcase.rs` 从 283 行降为 97 行，只保留 activate/drag/edit/context/option callback entry；新增子文件分别为 65、52、66、28 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pane-surface component-showcase subowner ownership scan 确认根文件不再定义 action-id normalization、runtime binding lookup、demo runtime event apply、virtual-list/page progression 或 Material Lab feedback；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、build-export host actions、assets refresh、callback surface 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 assets refresh event/snapshot/counter subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/assets/refresh.rs` 中 refresh/startup event draining、catalog/resource/detail/preview snapshot sync、paint-only shell-frame redraw 与 refresh-plan profiling counters 分别拆入 `assets/refresh/events.rs`、`snapshots.rs` 和 `counters.rs`：根 `refresh.rs` 从 270 行降为 97 行，继续拥有 refresh-plan application、default-scene reload dispatch、invalidation 和 paint-only orchestration；新增子文件分别为 88、71、45 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app assets refresh event/snapshot/counter ownership scan 确认根 refresh 文件不再定义 event receiver loops、startup drain loops、catalog/resource/detail/preview sync helpers、visible-preview collection 或 plan counter emission；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、callback surface、module/plugin live host、UI Asset detail event 子 owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 helper callback-surface source/size subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers/callback_surface.rs` 中 callback source-window resolution/focus tracking 与 callback surface size fallback 拆入 `helpers/callback_surface/source_window.rs` 和 `surface_size.rs`：根 `callback_surface.rs` 从 274 行降为 3 行结构入口；`source_window.rs` 为 94 行，拥有 callback source-window id、source scoping、floating focus tracking 与测试；`surface_size.rs` 为 184 行，拥有 floating/native/drawer/document/viewport/asset surface fallback size resolution。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers callback-surface source/size ownership scan 确认根文件不再定义 callback source-window logic、focus tracking、host-frame fallback sizing、drawer/main-page matching 或 asset surface mode mapper；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、module/plugin live host、UI Asset detail event 子 owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin projection row subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_projection/rows.rs` 中 optional feature summaries/action ids、primary action/target/packaging labels、fallback manifest construction 与 row-helper regressions 分别拆入 `rows/features.rs`、`labels.rs`、`manifest.rs` 和 `tests.rs`：根 `rows.rs` 从 242 行降为 12 行结构入口；新增子文件分别为 92、46、10、98 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin projection row subowner ownership scan 确认根 rows 文件不再定义 optional feature summarizer/action chooser、label/action-id helpers、fallback manifest 或 inline tests；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、module/plugin live host、UI Asset detail event 子 owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin live-host subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions/live_host.rs` 中 live-host command/request/outcome/backend contract、NativePluginLiveHost adapter、dispatch validation/success message 与 live-host regressions 分别拆入 `live_host/types.rs`、`native_backend.rs`、`dispatch.rs` 和 `tests.rs`：根 `live_host.rs` 降为 12 行结构入口；新增子文件分别为 37、32、36、132 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin live-host subowner ownership scan 确认根 live_host 文件不再定义 DTO/backend trait、native adapter、dispatch helper、success-message formatter 或 inline tests；首次 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 暴露 dispatch helper re-export 可见性偏窄，已将 dispatch helpers 收窄到 module-plugin action family 可见并复跑通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、UI Asset detail event 子 owner、build-export action 子 owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export host-action job/output subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/host_actions.rs` 中 desktop export poll/start/enqueue/cancel/status-task sync 与 output picker/reveal/effective-root 逻辑分别拆入 `host_actions/jobs.rs` 和 `host_actions/output.rs`：根 `host_actions.rs` 从 209 行降为 52 行，只保留 Build/Export action dispatch、plan refresh status 和 direct output override mutation；新增子文件分别为 98、69 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export host-action job/output ownership scan 确认根 host_action 文件不再定义 queue polling/start-next/enqueue/cancel/status-task sync、output picker/reveal 或 effective output-root lookup。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 已尝试但在进入 editor 前被当前 `zircon_runtime` 工作区错误阻断：`submit_runtime_frame.rs` 引用缺失的 `ViewportCameraStackAttachmentPolicy`，且 `submission.frame` move 后再借用；无默认特性 editor check 也超时，本切片不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset structure-detail subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/structure.rs` 中 slot/layout detail mutation、slot/layout semantic action-path mapping 与 semantic mapping regressions 分别拆入 `structure/slot.rs`、`layout.rs`、`semantic_paths.rs` 和 `tests.rs`：根 `structure.rs` 从 208 行降为 6 行结构入口；新增子文件分别为 83、65、36、27 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset structure-detail slot/layout ownership scan 确认根 structure 文件不再定义 slot/layout handlers 或 semantic path tables；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、binding/style detail owner、build-export output/action owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset binding-detail subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/binding.rs` 中 binding direct field commits、payload-map mutation 与 payload/route/action suggestion application 分别拆入 `binding/entry.rs`、`payload.rs` 和 `suggestions.rs`：根 `binding.rs` 从 202 行降为 4 行结构入口；新增子文件分别为 76、36、88 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset binding-detail entry/payload/suggestion ownership scan 确认根 binding 文件不再定义 binding handlers 或 `ViewInstanceId` 处理；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、style detail owner、build-export output/action owner 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset style-detail subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/style.rs` 中 style class membership、theme-source helper actions、stylesheet rule/declaration mutation 与 style token mutation 分别拆入 `style/class.rs`、`theme_source.rs`、`rules.rs` 和 `tokens.rs`：根 `style.rs` 从 199 行降为 5 行结构入口；新增子文件分别为 31、47、86、37 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset style-detail class/theme/rule/token ownership scan 确认根 style 文件不再定义 style/theme handlers 或 `ViewInstanceId` 处理；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（host lifecycle recompute、build-export output/action owner、asset reference/content pointer owners 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle recompute invalidation subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/recompute.rs` 中 recompute invalidation reason consumption、paint-only fast-path completion、slow-path rebuild diagnostics 和 UI perf counter emission 拆入 `recompute/invalidation.rs`：根 `recompute.rs` 从 303 行降为 237 行，继续拥有 ordered recompute flow、model/chrome/geometry build、bridge sync、presentation apply、viewport/native-window/pointer sync 和 dirty flag finalization；新增 `invalidation.rs` 为 84 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute invalidation ownership scan 确认根 recompute 文件不再直接消费 `take_recompute_reasons` / `HostInvalidationMask::from_dirty_flags`，也不再直接写 diagnostic log；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。本切片按用户“先实现功能，测试暂时延后”要求不扩展 Cargo test/full validation matrix。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（build-export output/action owner、asset reference/content pointer owners、host lifecycle bridge/apply substeps 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export output-folder picker/reveal subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/output_folder.rs` 中 native folder picker command execution/parse、file-browser reveal command construction 和 output-folder regressions 分别拆入 `output_folder/picker.rs`、`reveal.rs` 和 `tests.rs`：根 `output_folder.rs` 从 216 行降为 6 行结构入口；新增子文件分别为 116、35、49 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export output-folder picker/reveal ownership scan 确认根 output_folder 文件不再定义 OS command construction、selected-folder parsing 或 inline tests；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 已尝试但在进入 editor 前被当前 `zircon_runtime` 工作区错误阻断：`dynamic_api/session/project.rs` 对 `Arc<dyn AssetManager>` 调用缺失的 `current_project_manager`，并把 `&dyn AssetManager` 传给期望 `&ProjectAssetManager` 的接口；本切片不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin action-id test subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions/action_ids.rs` 中 inline parser regressions 移入 `action_ids/tests.rs`：根 `action_ids.rs` 从 192 行降为 108 行，继续拥有 `ModulePluginAction`、stable `workbench.plugin.*` parser 和 feature action path split；新增 `tests.rs` 为 79 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin action-id test ownership scan 确认根 action_ids 文件不再承载 inline `#[test]` 或 parser case body，子测试文件保留 feature dependency parser case；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用上一行刚确认的当前 `zircon_runtime` 工作区阻断（`dynamic_api/session/project.rs` AssetManager/ProjectAssetManager 接口不匹配）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-reference pointer event subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_reference_pointer/events.rs` 中 reference/used-by click forwarding、effect writeback、move hover updates 和 scroll updates 分别拆入 `events/click.rs` 和 `events/motion.rs`：根 `events.rs` 从 193 行降为 25 行，只保留 primary-button press/release gate 和 drag payload release clear；新增子文件分别为 90、78 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-reference pointer event subowner ownership scan 确认根 events 文件不再定义 click dispatch、move dispatch 或 scroll dispatch；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用当前 `zircon_runtime` 工作区阻断（`dynamic_api/session/project.rs` AssetManager/ProjectAssetManager 接口不匹配）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-content pointer event subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_content_pointer/events.rs` 中 content-list click forwarding、effect writeback、move hover updates 和 scroll updates 分别拆入 `events/click.rs` 和 `events/motion.rs`：根 `events.rs` 从 146 行降为 24 行，只保留 primary-button press/release gate 和 drag payload release clear；新增子文件分别为 58、64 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-content pointer event subowner ownership scan 确认根 events 文件不再定义 click dispatch、move dispatch 或 scroll dispatch；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用当前 `zircon_runtime` 工作区阻断（`dynamic_api/session/project.rs` AssetManager/ProjectAssetManager 接口不匹配）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export job-queue worker subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/job_queue.rs` 中 desktop export worker-thread spawning、worker progress/result message DTOs 和 result-to-summary conversion 拆入 `job_queue/worker.rs`：根 `job_queue.rs` 从 285 行降为 225 行，继续拥有 pending/active queue state、enqueue、busy check、snapshot assembly、cancellation、message polling 和 start-next state transition；新增 `worker.rs` 为 77 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export job-queue worker ownership scan 确认根 job_queue 文件不再直接调用 `thread::spawn`、不再定义 worker result/progress DTO，worker 子文件拥有 export runner dispatch；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用当前 `zircon_runtime` 工作区阻断（`dynamic_api/session/project.rs` AssetManager/ProjectAssetManager 接口不匹配）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 showcase action-input subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/showcase_event_inputs/action.rs` 中 field/toggle/slider payloads、selection/menu payloads、reference drop/binding suffix handling、collection/container payloads、list/page payloads 与 world-surface payloads 分别拆入 `action/fields.rs`、`selection.rs`、`references.rs`、`collections.rs`、`lists.rs` 和 `world_surface.rs`：根 `action.rs` 从 243 行降为 47 行，只保留 stable action input dispatcher 与 ComponentShowcase show fallback；新增子文件分别为 72、37、48、74、30、26 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app showcase action subowner ownership scan 确认根 action 文件不再拥有 `UiDragPayload`、`BTreeMap`、virtual/paged list defaults 或大 action match table；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用当前 `zircon_runtime` 工作区阻断（`dynamic_api/session/project.rs` AssetManager/ProjectAssetManager 接口不匹配）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 runtime 阻断修复后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup resources/state split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/startup.rs` 中 startup manager/resource resolution、asset/editor/resource change subscriptions 和一次性 `RetainedEditorHost` 字段组装分别拆入 `startup/resources.rs` 与 `startup/state.rs`：根 `startup.rs` 从 230 行降为 111 行，只保留 retained host 构造顺序、startup session/state/template bridge/runtime backend 串联与初始 asset refresh 触发；新增子文件分别为 64、132 行。同步修正上一轮 `output_folder` 子文件 helper 可见性为 Build/Export action 家族可见。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup resources/state ownership scan 确认根 startup 文件不再直接解析 asset/editor/resource manager 或写完整 host 字段初始化；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 142 条、editor 65 条）。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 callback-surface surface-size subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers/callback_surface/surface_size.rs` 中 frame size validation/conversion、drawer/main-page `ViewContentKind` matching、docked/floating/native/root-pane host-frame fallback lookup 与 asset-surface mode mapping 分别拆入 `surface_size/frame.rs`、`workbench_regions.rs`、`host_frames.rs` 和 `asset_surface.rs`：根 `surface_size.rs` 从 184 行降为 39 行，只保留 callback/cached/host-frame fallback dispatch；新增子文件分别为 13、65、87、8 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers callback-surface surface-size subowner ownership scan 确认根 surface_size 文件不再拥有 drawer/main-page matching、floating/native host-frame lookup 或 frame clamp conversion；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 143 条、editor 65 条）。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin project-policy subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions/project_policy.rs` 中 dependency enablement status message、current native-aware project selection lookup、packaging/target-mode transition policy、status label formatting 和 policy regressions 分别拆入 `project_policy/dependencies.rs`、`selection.rs`、`transitions.rs`、`status.rs` 和 `tests.rs`：根 `project_policy.rs` 从 173 行降为 10 行，只保留结构入口与 re-export；新增子文件分别为 35、15、24、23、76 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin project-policy subowner ownership scan 确认根 project_policy 文件不再拥有 runtime policy imports、policy logic 或 inline tests；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo checks 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export projection-target subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_projection/targets.rs` 中 active project manifest loading、per-profile target-row construction/report+job overlay、output-root diagnostic prefixing 分别拆入 `targets/project.rs`、`rows.rs` 和 `diagnostics.rs`：根 `targets.rs` 从 164 行降为 33 行，只保留 project load、job snapshot collection、profile iteration 和 error collection；新增子文件分别为 12、116、12 行。`build_export_actions.rs` 追加 app-boundary `DesktopExportJobSnapshot` re-export 供 projection row child 显式接收 job snapshot。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export projection-target subowner ownership scan 确认根 targets 文件不再拥有 `ProjectManifest`/`project_root_path`、`BuildExportTargetViewData` 初始化、summary/job overlay 或 output diagnostic 拼接；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo checks 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 showcase edit-input subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/showcase_event_inputs/edit.rs` 中 context menu popup-anchor parsing、virtual/paged list edit request parsing、array/map row edit payload parsing、typed collection value 与 number/range/fallback committed value parsing 分别拆入 `edit/menu.rs`、`lists.rs`、`collections.rs` 和 `values.rs`：根 `edit.rs` 从 159 行降为 23 行，只保留 stable edit-input dispatcher；新增子文件分别为 17、70、64、27 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app showcase edit-input subowner ownership scan 确认根 edit 文件不再拥有 collection row parsing、list/page parsing、popup-anchor parsing 或 typed value parsing；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo checks 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 viewport toolbar surface-frame subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/viewport_toolbar_projection/surface_frames.rs` 中 docked region toolbar frame attachment、floating-window row projection/writeback 和 single-pane eligibility/layout/frame creation 分别拆入 `surface_frames/docked.rs`、`floating.rs` 和 `pane_frame.rs`：根 `surface_frames.rs` 从已记录 150 行降为 22 行，只保留 host presentation 取出、子 owner 调用和写回；新增子文件分别为 79、34、38 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app viewport toolbar projection surface-frame subowner ownership scan 确认根 surface_frames 文件不再拥有 dock 字段展开、floating row rebuild 或 single-pane frame creation；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 welcome action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/welcome_session/actions.rs` 中 Welcome draft mutation、create/open-existing project flows、recent-project open/remove flows 和 startup workbench/view opening 分别拆入 `actions/draft.rs`、`project.rs`、`recent.rs` 和 `startup_views.rs`：根 `actions.rs` 从 152 行降为 49 行，只保留 `WelcomeHostEvent` dispatcher；新增子文件分别为 11、36、41、34 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app welcome-session action subowner ownership scan 确认根 actions 文件不再直接改 draft、不再直接 open/create/forget project，也不再直接 dismiss/open startup view；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 animation clip subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers/animation_assets/clip.rs` 中 channel interpolation/constant/sample key conversion 和 skeleton-derived track seeding/final bone-track conversion 分别拆入 `clip/channels.rs` 与 `clip/tracks.rs`：根 `clip.rs` 从 168 行降为 64 行，只保留 glTF animation channel traversal、target-joint lookup、duration accumulation 和 final `AnimationClipAsset` assembly；新增子文件分别为 82、42 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers animation clip subowner ownership scan 确认根 clip 文件不再定义 constant channel builders、sample builders、`DerivedClipTrack` 或 `BTreeMap` track assembly；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 pointer-layout asset-surface subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pointer_layout/asset_surfaces.rs` 中 activity/browser asset layout sync、pane-surface hover/scroll UI writeback、asset surface state/snapshot/reference-layout helpers 分别拆入 `asset_surfaces/sync.rs`、`ui_writeback.rs` 和 `state.rs`：根 `asset_surfaces.rs` 从 154 行降为 3 行结构入口；新增子文件分别为 43、51、51 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pointer-layout asset-surface subowner ownership scan 确认根 asset_surfaces 文件不再拥有 layout sync、UI writeback 或 state/reference layout helper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin host-action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions/host_actions.rs` 中 active project manifest context load/save、live backend unload/hot-reload dispatch、manifest-backed project plugin mutations 分别拆入 `host_actions/manifest.rs`、`live_actions.rs` 和 `project_actions.rs`：根 `host_actions.rs` 从 159 行降为 49 行，只保留 action-id parse、project context 调用、project/live action 分流、context save、status line 和 layout dirty 收尾；新增子文件分别为 27、22、120 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin host-actions subowner ownership scan 确认根 host_actions 文件不再直接 `ProjectManifest::load/save`、不再直接调用 policy helpers 或 live backend dispatch helpers；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests/checks 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export job-queue operation subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/job_queue.rs` 中 pending/active cancellation、worker message polling/result summary collection、start-next active transition/worker spawn 分别拆入 `job_queue/cancellation.rs`、`updates.rs` 和 `start.rs`：根 `job_queue.rs` 从 225 行降为 118 行，保留 queued/active DTO、queue storage、default construction、enqueue、busy check 和 queue snapshot assembly；新增子文件分别为 49、37、40 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export job-queue subowner ownership scan 确认根 job_queue 文件不再拥有 `cancel_profile`、`poll_updates` 或 `start_next` 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests/checks 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 inspector surface-control subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/inspector/surface_controls.rs` 中 inspector control field-id mapping、batch apply arguments、component-adapter value changes、Apply/Delete click routing 和 builtin surface dispatch 分别拆入 `surface_controls/field_ids.rs`、`apply_arguments.rs`、`value_change.rs`、`click.rs` 和 `dispatch.rs`：根 `surface_controls.rs` 从 141 行降为 5 行结构入口；新增子文件分别为 13、49、44、24、23 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app inspector surface-control subowner ownership scan 确认根 surface_controls 文件不再拥有 field mapping、batch argument construction、adapter dispatch 或 builtin dispatch；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为当前工作区已有其他 runtime Cargo tests/checks 正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 owner-split batch compile correction | 完成（功能代码可编译；完整测试矩阵按用户要求暂缓） | 修正前两轮拆分后的编译接缝：`module_plugin_actions/project_policy/*` helper 可见性扩大到 module-plugin action 家族，供 `host_actions/project_actions.rs` 使用但不越过 app action 边界；`build_export_actions/job_queue/worker.rs` 的 `DesktopExportExecutionSummary` 导入改到 Build/Export action 父模块。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；module-plugin project-policy 与 build-export job-queue scoped `git diff --check` 通过；ownership scan 确认 project-policy 根为 12 行结构入口，job_queue 根为 127 行、worker 为 84 行且 summary DTO 仍归 execution_summary owner；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 仍输出当前工作区 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner（execution_summary、asset-reference target、callback host shell 等），并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export execution-summary subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/execution_summary.rs` 中 export summary 构造、status/diagnostic/fatal formatting 和 completed-summary target-row application 分别拆入 `execution_summary/constructors.rs`、`status.rs` 和 `target.rs`：根 `execution_summary.rs` 从 130 行降为 26 行结构入口；新增子文件分别为 55、60、13 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export execution-summary subowner ownership scan 确认根 execution_summary 文件不再拥有 constructor、status-message 或 target apply 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 已尝试但在进入 editor 前被当前 `zircon_runtime` 工作区错误阻断：`scene/dynamic_scene/session/path_capture.rs` 中 `capture_*_to_path*` 与 `preview_*_to_path*` 方法重复定义；本切片不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；runtime 阻断解除后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-reference target subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_reference_pointer/target.rs` 中 prepared target snapshot/list-size preparation、prepared reference-list bridge sync/dispatch 和 pointer-state writeback 分别拆入 `target/prepare.rs`、`dispatch.rs` 和 `state.rs`：根 `target.rs` 从 124 行降为 11 行结构入口；新增子文件分别为 51、68、18 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-reference pointer target subowner ownership scan 确认根 target 文件不再拥有 preparation、dispatch/sync 或 state writeback 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用上一行刚确认的当前 `zircon_runtime` 工作区阻断（`scene/dynamic_scene/session/path_capture.rs` 重复定义 capture/preview path 方法）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；runtime 阻断解除后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-shell callback subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/callback_wiring/host_shell.rs` 中 runtime/keyboard/close-prompt 回调、menu/activity-rail 回调、page/tab/header 回调、floating/drag/resize 回调分别拆入 `host_shell/runtime.rs`、`menu.rs`、`chrome.rs` 和 `drag_resize.rs`：根 `host_shell.rs` 从 126 行降为 15 行结构入口；新增子文件分别为 29、35、64、28 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app callback-wiring host-shell subowner ownership scan 确认根 host_shell 文件不再直接注册 `UiHostContext` 回调，只负责调用四个 callback group；scoped `git diff --check` 通过。本切片未复跑 `cargo check`，沿用当前 `zircon_runtime` 工作区阻断（`scene/dynamic_scene/session/path_capture.rs` 重复定义 capture/preview path 方法）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；runtime 阻断解除后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-content target subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_content_pointer/target.rs` 中 prepared target snapshot/content-size preparation、prepared content-list bridge sync/dispatch 和 pointer-state writeback 分别拆入 `target/prepare.rs`、`dispatch.rs` 和 `state.rs`：根 `target.rs` 从 98 行降为 11 行结构入口；新增子文件分别为 47、48、15 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-content pointer target subowner ownership scan 确认根 target 文件不再拥有 preparation、dispatch/sync 或 state writeback 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，沿用当前 `zircon_runtime` 工作区阻断（`scene/dynamic_scene/session/path_capture.rs` 重复定义 capture/preview path 方法）；不声明新的编译通过。完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；runtime 阻断解除后补跑 editor lib check / milestone testing stage 完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 owner-split batch compile validation | 完成（focused lib check 通过；完整测试矩阵按用户要求暂缓） | 对 build-export execution-summary、asset reference/content pointer target、host-shell callback 等本轮 owner split 做收束性编译验证，确认新子模块导入、可见性和 retained-host impl 分布能通过 Rust type check。 | `cargo fmt -p zircon_editor --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 viewport toolbar pointer click/size split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/viewport/toolbar_pointer.rs` 中 toolbar pointer bridge sync/click dispatch 与 document/drawer/floating/exclusive toolbar surface-size resolution 分别拆入 `toolbar_pointer/click.rs` 和 `size.rs`：根 `toolbar_pointer.rs` 从 124 行降为 2 行结构入口；新增子文件分别为 46、82 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app viewport toolbar pointer click/size ownership scan 确认根 toolbar_pointer 文件不再拥有 click dispatch 或 surface-size 推导；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为独立 `zircon_runtime` Cargo test 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 invalidation mask requirement/summary split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/invalidation/mask.rs` 中 recompute requirement predicates 和 dirty-domain summary formatting 分别拆入 `mask/requirements.rs` 与 `mask/summary.rs`：根 `mask.rs` 从 132 行降为 68 行，继续拥有 `HostInvalidationMask`、dirty-domain 常量、bit 操作和 dirty-flag conversion；新增子文件分别为 34、39 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app invalidation mask requirement/summary ownership scan 确认根 mask 文件不再拥有 `requires_*` 或 `summary()` 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为独立 `zircon_runtime` Cargo test 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 output-folder picker command/selection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/output_folder/picker.rs` 中 platform-specific folder picker command construction、stable initial-directory selection 与 selected-folder parsing 分别拆入 `picker/commands.rs` 和 `picker/selection.rs`：根 `picker.rs` 从 131 行降为 45 行，只保留命令执行、进程结果处理和错误汇总；新增子文件分别为 78、17 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export output-folder picker command/selection ownership scan 确认根 picker 文件不再拥有 `folder_picker_commands`、`stable_picker_initial_dir` 或 `parse_selected_folder` 行为实现；scoped `git diff --check` 通过。本切片未复跑 `cargo check`，因为独立 `zircon_runtime` Cargo test 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 build-export job snapshot projection subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/job_queue/snapshot.rs` 中 progress report conversion、queued/running/cancelled status formatting、Build/Export target-row overlay application 和 desktop export status-task projection 分别拆入 `snapshot/progress.rs`、`status.rs`、`target.rs` 与 `status_task.rs`：根 `snapshot.rs` 从 106 行降为 31 行，只保留 snapshot DTO 与窄重导出；新增子文件分别为 12、44、10、30 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export job-queue snapshot subowner ownership scan 确认根 snapshot 文件不再拥有 `from_report`、status diagnostic、target apply 或 status-task projection 行为实现；scoped `git diff --check` 通过。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 recompute late-phase subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/recompute.rs` 后半段的 main retained-host presentation application、viewport toolbar/world-space surface submission 和 final pointer-surface publication 分别拆入 `recompute/presentation.rs`、`viewport_surfaces.rs` 与 `pointer_surfaces.rs`：根 `recompute.rs` 从 237 行降为 186 行，只保留 recompute 顺序编排；新增子文件分别为 47、25、18 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute late-phase subowner ownership scan 确认根 recompute 文件不再直接 `apply_presentation`、提交 world-space UI surfaces 或同步 menu/welcome/hierarchy/detail/asset pointer surfaces；scoped `git diff --check` 通过。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 module-plugin action-id parser subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions/action_ids.rs` 中 `workbench.plugin.*` prefix parser chain 与 feature-action splitting 拆入 `action_ids/parser.rs`：根 `action_ids.rs` 从 108 行降为 32 行，只保留 `ModulePluginAction` 声明、parser 重导出和 parser tests 挂载；新增 parser 子文件 79 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin action-id parser ownership scan 确认根 action_ids 文件不再拥有 `strip_prefix` parser chain、`split_once` feature parser 或 feature-action helper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 pane-payload subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/pane_payloads.rs` 中 UI Asset/Animation Editor pane presentation gathering 与 runtime diagnostics/Module Plugins/Build Export visibility-gated payload gathering 分别拆入 `pane_payloads/editor_panes.rs` 和 `workbench_panes.rs`：根 `pane_payloads.rs` 从 122 行降为 71 行，只保留 payload DTO 与收集顺序；新增子文件分别为 39、49 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle pane-payload subowner ownership scan 确认根 pane_payloads 文件不再拥有 view-instance descriptor filtering、runtime diagnostics visibility policy 或 Module Plugins/Build Export default payload policy；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 module-plugin project-actions subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_actions/host_actions/project_actions.rs` 中 plugin enablement/packaging/target-mode mutations 与 feature/dependency mutations 分别拆入 `project_actions/plugin.rs` 和 `feature.rs`：根 `project_actions.rs` 从 120 行降为 49 行，只保留 manifest-backed action dispatch 与 live-action misroute protection；新增子文件分别为 80、50 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin project-actions subowner ownership scan 确认根 project_actions 文件不再直接调用 `EditorManager` native-aware plugin mutation APIs、不再拥有 policy helper/status formatting；scoped `git diff --check` 通过。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 dispatch-effects subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/dispatch_effects.rs` 中 status line/status task mutation 与 `UiHostEventEffects` side effects 分别拆入 `dispatch_effects/status.rs` 和 `side_effects.rs`：根 `dispatch_effects.rs` 从 116 行降为 56 行，只保留 dispatch effect dirty-domain flow、active-recompute viewport resize filtering 和 dispatch-result error wrapping；新增子文件分别为 25、44 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle dispatch-effects subowner ownership scan 确认根 dispatch_effects 文件不再实现 status mutation、不再拥有 import/asset refresh/command palette/welcome surface side effects；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 build-export target rows subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_projection/targets/rows.rs` 中 ready/blocked Build Export target-row construction 与 completed-summary/queued-running job overlays 分别拆入 `rows/constructors.rs` 和 `rows/overlays.rs`：根 `rows.rs` 从 116 行降为 36 行，只保留 per-profile manifest clone、native-aware export-plan request 和 target construction/overlay 调度；新增子文件分别为 72、18 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export target rows subowner ownership scan 确认根 rows 文件不再拥有 target DTO literal、diagnostic prefixing、summary overlay 或 job overlay 行为实现；scoped `git diff --check` 通过。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 pane-surface Workbench subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pane_surface_actions/workbench_surface.rs` 中 componentized Workbench control click forwarding、option-selected forwarding 和 edit/command-palette commit forwarding 分别拆入 `workbench_surface/control.rs`、`option.rs` 与 `edit.rs`：根 `workbench_surface.rs` 从 115 行降为 3 行结构入口；新增子文件分别为 63、25、35 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pane-surface Workbench subowner ownership scan 确认根 workbench_surface 文件不再拥有 active document check、binding-id resolution、popup/menu/control dispatch、option dispatch 或 edit dispatch 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片未复跑 `cargo check`，因为独立 Cargo/rustc 进程正在运行；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 native-window presenter subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/native_window_presenters.rs` 中 per-native-window Module Plugins/Build Export/Component Showcase payload preparation、native presenter callback wiring/close forwarding、per-window retained presentation application/viewport-toolbar attachment/native metadata configuration 分别拆入 `native_window_presenters/payloads.rs`、`callbacks.rs` 与 `presentation.rs`：根 `native_window_presenters.rs` 从 113 行降为 76 行，只保留 native target collection、empty-target cleanup 和 `NativeWindowPresenterStore::sync_targets(...)` orchestration；新增子文件分别为 42、27、55 行；`app/native_windows.rs` 将 `NativeFloatingWindowTarget` 从 test-only re-export 提升为 app-internal re-export，供子 owner typed helper 使用。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle native-window presenter subowner ownership scan 确认根 native_window_presenters 文件不再拥有 pane payload visibility policy、callback wiring/close handler、`apply_presentation`、viewport toolbar attachment 或 native metadata configuration；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）。本切片 focused `cargo check` 等待无并发编译窗口补跑；完整测试矩阵按用户“先实现功能，测试暂时延后”要求暂缓。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在下一次无并发编译窗口补跑 editor lib check。 |
| 2026-06-19 | 08 M3.S2 module-plugin pane-data report/view-row subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_projection/pane_data.rs` 中 active project plugin status lookup/fallback diagnostics 与 `ModulePluginStatusViewData` row construction 分别拆入 `pane_data/report.rs` 和 `pane_data/view_rows.rs`：根 `pane_data.rs` 从 113 行降为 23 行，只保留 `module_plugins_pane_data(...)` assembly；新增子文件分别为 45、66 行。focused check 还修正了上一批 owner split 暴露的可见性接缝：`action_ids/parser.rs` parser re-export、`output_folder/picker/{commands,selection}.rs` picker re-export、`workbench_surface/{control,option,edit}.rs` sibling dispatch 调用均收窄到对应 app/internal owner 边界。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin projection pane-data subowner ownership scan 确认根 pane_data 文件不再拥有 project manifest IO、native/builtin status lookup、row DTO literal、action-id/label construction 或 diagnostics join 行为实现；app retained-host owner visibility compile-boundary scan 通过；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export wizard session-state subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_wizard_session/session_state.rs` 中 profile action dispatch/plan regeneration、view-model/session lookup、changed-update polling 分别拆入 `session_state/actions.rs`、`lookup.rs` 与 `polling.rs`：根 `session_state.rs` 从 136 行降为 15 行，只保留 retained per-profile session/update maps；新增子文件分别为 70、28、45 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export wizard session-state subowner ownership scan 确认根 session_state 文件不再拥有 `dispatch_profile_action`、injectable runner dispatch、plan regeneration、`view_model`、`session_mut` 或 `poll_all` 行为实现；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 invalidation root requests/reasons/diagnostics subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/invalidation/root.rs` 中 initial full rebuild/request accounting、recompute reason consumption/rebuild counters、stats summary/diagnostics snapshot 分别拆入 `root/requests.rs`、`root/reasons.rs` 与 `root/diagnostics.rs`：根 `root.rs` 从 103 行降为 22 行，只保留 `HostInvalidationRoot` state shape 和子模块边界；新增子文件分别为 49、31、30 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app invalidation root requests/reasons/diagnostics ownership scan 确认根 root 文件不再拥有 invalidation mutation、reason drain、rebuild counter、stats formatting 或 diagnostics snapshot 行为实现；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-tree pointer dispatch subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_tree_pointer/dispatch.rs` 中 Activity/Browser asset tree click、move、scroll dispatch 分别拆入 `dispatch/click.rs`、`dispatch/motion.rs` 与 `dispatch/scroll.rs`：根 `dispatch.rs` 从 112 行降为 3 行结构入口；新增子文件分别为 55、32、33 行。click 子 owner 保留 runtime asset surface bridge forwarding 与 dispatch effects 应用，motion/scroll 子 owner 只处理已同步 tree bridge 的本地 pointer state。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-tree pointer dispatch subowner ownership scan 确认根 dispatch 文件不再拥有 click/move/scroll 方法、runtime bridge dispatch、local bridge movement 或 status-line 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 hierarchy pointer events subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/hierarchy_pointer/events.rs` 中 scene drag probing、shared click dispatch、hover movement 和 scroll movement 分别拆入 `events/drag.rs`、`events/click.rs`、`events/motion.rs` 与 `events/scroll.rs`：根 `events.rs` 从 109 行降为 4 行结构入口；新增子文件分别为 47、26、22、23 行。drag 子 owner 保留 release 清理、互斥拖拽 payload 清理和 scene drag payload 激活，click 子 owner 保留 shared callback dispatch/effects pipeline。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app hierarchy-pointer events subowner ownership scan 确认根 events 文件不再拥有 drag/click/move/scroll 方法、shared callback dispatch、drag payload construction、bridge movement 或 status-line 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 native-window close prompt-actions subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/native_window_close/prompt_actions.rs` 中 close prompt save/discard/cancel action dispatch、prompt UI begin/show/clear/target lookup、prompted close completion 和 dirty document auto-save policy 分别拆入 `prompt_actions/actions.rs`、`presentation.rs`、`completion.rs` 与 `saving.rs`：根 `prompt_actions.rs` 从 101 行降为 4 行结构入口；新增子文件分别为 34、40、18、29 行。shared helpers 仅放宽到 `native_window_close` 家族内部，避免 prompt UI、save policy、close completion 泄露到 app 其他 owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app native-window close prompt-actions subowner ownership scan 确认根 prompt_actions 文件不再拥有 close action dispatch、prompt presentation、UI target lookup、prompt completion、dirty-view saving 或 status-line 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 viewport pointer-event mapping/world-space subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/viewport/pointer_event.rs` 中 native integer pointer kind/button decoding 与 world-space UI pointer status text 分别拆入 `pointer_event/mapping.rs` 与 `pointer_event/world_space.rs`：根 `pointer_event.rs` 从 97 行降为 46 行，只保留 committed pointer layout reuse、non-move focus transfer、world-space UI routing 和 regular viewport pointer dispatch flow；新增子文件分别为 47、13 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app viewport pointer-event mapping/world-space ownership scan 确认根 pointer_event 文件不再拥有 integer kind/button constants、button mapping、`UiPointerEvent` construction、scroll delta mutation 或 world-space status text；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 pane-surface Component Showcase callback subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/pane_surface_actions/component_showcase.rs` 中 activate、drag-delta、edit、context-request 和 option-selected callback flow 分别拆入 `component_showcase/activation.rs`、`drag.rs`、`edit.rs`、`context.rs` 与 `option.rs`：根 `component_showcase.rs` 从 97 行降为 10 行结构入口；新增子文件分别为 15、21、17、25、26 行。既有 `bindings.rs`、`events.rs`、`inputs.rs`、`material_lab.rs` 继续分别负责 binding lookup、runtime event application、host-state-aware input 和 Material Lab feedback。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app pane-surface component-showcase callback subowner ownership scan 确认根 component_showcase 文件不再拥有 activate/drag/edit/context/option 方法、focus handling、input construction、Workbench option forwarding 或 runtime event forwarding 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 profiling action command/dispatch/status subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/profiling/actions.rs` 中 performance timeline action-id grammar/command mapping、profiling-enabled command dispatch/non-profiling fallback、combined editor/runtime status-line formatting 分别拆入 `actions/commands.rs`、`actions/dispatch.rs` 与 `actions/status.rs`：根 `actions.rs` 从 101 行降为 3 行结构入口；新增子文件分别为 50、42、13 行。feature-gated mapping 测试跟随 command grammar owner。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app profiling action subowner ownership scan 确认根 actions 文件不再拥有 action id constants、command mapping、dispatch side effects、status formatting、profile control request 或 mapping tests；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export job-queue state/enqueue/query subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/job_queue.rs` 中 pending/active job structs、queue channel state/default init、queued job creation、busy-profile checks 和 active/pending snapshot assembly 分别拆入 `job_queue/state.rs`、`job_queue/enqueue.rs` 与 `job_queue/queries.rs`：根 `job_queue.rs` 从 118 行降为 16 行结构入口；新增子文件分别为 45、35、37 行。既有 cancellation/start/updates/worker/snapshot 子 owner 继续承接各自长操作。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export job-queue state/enqueue/query ownership scan 确认根 job_queue 文件不再拥有 queue structs/default init、enqueue、busy check、snapshot assembly、atomic/mpsc/manifest imports 或 pending/active storage behavior；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle recompute floating-projection subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/recompute.rs` 中 floating-window source bridge recompute、shared source frame resolution、native projection bounds sync、native host snapshot read 和 `FloatingWindowProjectionBundle` construction 拆入 `recompute/floating_projection.rs`：根 `recompute.rs` 从 186 行降为 132 行；新增子文件为 65 行。根 owner 继续保留 dirty recompute sequencing、model/chrome rebuild、viewport/pointer recompute、late-phase presentation/surface publication、native presenter sync 和 dirty-flag clearing。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute floating-projection ownership scan 确认根 recompute 文件不再拥有 floating source bridge recompute、shared-source resolution、native projection bounds sync、native host lookup 或 floating projection bundle construction；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 build-export host-action job subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/build_export_actions/host_actions/jobs.rs` 中 desktop export poll/update/start-next、enqueue preparation/manifest snapshotting、pending/active cancellation handling 和 status-task sync 分别拆入 `jobs/polling.rs`、`jobs/enqueue.rs`、`jobs/cancellation.rs` 与 `jobs/status_task.rs`：根 `jobs.rs` 从 98 行降为 4 行结构入口；新增子文件分别为 28、50、32、11 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app build-export host-action job subowner ownership scan 确认根 jobs 文件不再拥有 poll/enqueue/cancel/status-task 方法、ProjectManifest/project-root/profile imports、DesktopExportCancellation 或 queue-to-status-task projection；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 assets refresh apply subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/assets/refresh.rs` 中 refresh-plan side-effect application 拆入 `refresh/apply.rs`：新子 owner 接管 catalog/resource/detail/preview sync、default-scene reload、render/presentation invalidation、paint-only invalidation 和 asset preview repaint request；根 `refresh.rs` 从 97 行降为 49 行，只保留 refresh event collection、runtime-project editor asset refresh、refresh-plan construction 和 counter recording；新增子文件为 67 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app assets refresh apply subowner ownership scan 确认根 refresh 文件不再拥有 snapshot sync、default-scene reload、HostInvalidationMask、paint-only invalidation 或 asset preview repaint request 行为实现；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 workspace docking drag-drop route subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/workspace_docking/drag_drop.rs` 中 workbench model rebuild、pointer route lookup、drop-route resolution、detached-window fallback route construction 和 detached window id sanitization 拆入 `drag_drop/route.rs`：根 `drag_drop.rs` 从 103 行降为 50 行，只保留 drag target group sync、runtime tab-drop dispatch 和 status-line update；新增子文件为 72 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app workspace-docking drag-drop route ownership scan 确认根 drag_drop 文件不再拥有 route resolver imports、WorkbenchViewModel build、UI perf counter、detached route helpers 或 detached id sanitization；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset preview-detail value/nested/suggestion subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/preview.rs` 中 preview mock value set/clear、nested preview mock value/upsert/delete 与 preview mock suggestion apply 分别拆入 `preview/value.rs`、`preview/nested.rs` 与 `preview/suggestions.rs`：根 `preview.rs` 从 103 行降为 3 行结构入口；新增子文件分别为 33、44、34 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset preview-detail value/nested/suggestion ownership scan 确认根 preview 文件不再拥有 preview mock handler 方法、ViewInstanceId import、EditorManager mutation calls、presentation dirty propagation 或 unknown-action status branches；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 workspace docking drawer-resize capture/movement subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/workspace_docking/drawer_resize.rs` 中 resize-region capture、shell-frame visibility/conversion、starting preferred-size resolution、transient preferred-size updates、runtime resize dispatch 与 resize-driven invalidation 分别拆入 `drawer_resize/capture.rs` 和 `drawer_resize/movement.rs`：根 `drawer_resize.rs` 从 101 行降为 2 行结构入口；新增子文件分别为 55、61 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app workspace-docking drawer-resize capture/movement ownership scan 确认根 drawer_resize 文件不再拥有 begin/update/finish 方法、ShellFrame conversion、HostShellPointerRoute routing、dispatch_resize_to_group、HostInvalidationMask 或 frame visibility helper；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 workbench pointer shell-chrome subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/workbench_pointer/shell_chrome.rs` 中 activity rail、host page tab、drawer header tab pointer callbacks 分别拆入 `shell_chrome/activity.rs`、`shell_chrome/host_page.rs` 与 `shell_chrome/drawer_header.rs`：根 `shell_chrome.rs` 从 98 行降为 3 行结构入口；新增子文件分别为 34、35、37 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app Workbench pointer shell-chrome subowner ownership scan 确认根 shell_chrome 文件不再拥有 activity/host-page/drawer-header callback methods、callback_dispatch imports、UiPoint construction、side/tab validation、status-line or dispatch-effect handling；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 close-prompt presentation data/layout/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/close_prompt/presentation.rs` 中 `HostClosePromptData` construction、dialog/button geometry、target/title/message/details text 分别拆入 `presentation/data.rs`、`presentation/layout.rs` 与 `presentation/text.rs`：根 `presentation.rs` 从 97 行降为 17 行，只保留 show/clear helpers 和 child declarations；新增子文件分别为 29、46、36 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app close-prompt presentation data/layout/text ownership scan 确认根 presentation 文件不再拥有 HostClosePromptData/FrameRect construction、button constants, geometry helpers、prompt text helpers、target window id selection 或 can-save projection；scoped `git diff --check` 通过（仅提示既有 CRLF 工作区转换 warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 assets refresh events runtime/startup subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/assets/refresh/events.rs` 中 runtime refresh event receiver draining/event-count counters 与 startup bootstrap event draining/startup drained counters 分别拆入 `events/runtime.rs` 和 `events/startup.rs`：根 `events.rs` 从 95 行降为 18 行，只保留 `AssetRefreshEvents` DTO 与结构入口；新增子文件分别为 43、42 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app assets refresh events runtime/startup ownership scan 确认根 events 文件不再拥有 drain 方法、profile scope/counter 或 `try_recv` 行为实现；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle recompute shell-snapshot subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/recompute.rs` 中 runtime layout/descriptors reads、chrome/model construction、model-build counter recording、shell geometry computation、root/workbench template bridge recompute 和 componentized layout-frame capture 拆入 `recompute/shell.rs`：根 `recompute.rs` 从 137 行降为 75 行，只保留 invalidation decision 后的 ordered recompute orchestration；新增子文件为 87 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute shell-snapshot ownership scan 确认根 recompute 文件不再拥有 layout/descriptors reads、Workbench model build、shell geometry computation、template bridge recompute 或 layout-frame capture；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset collection-event editor/binding subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/collection.rs` 中 editor-facing collection selection/activation calls 与 binding/semantic collection selection calls 分别拆入 `collection/editor.rs` 和 `collection/binding.rs`：根 `collection.rs` 从 97 行降为 50 行，只保留 source-window focus、input normalization、known-dispatch lookup、presentation invalidation 和 unknown-event reporting；新增子文件分别为 68、43 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset collection-event editor/binding subowner ownership scan 确认根 collection 文件不再拥有具体 EditorManager selection/activation call mapping；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 module-plugin feature action/summary subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/module_plugin_projection/rows/features.rs` 中 optional feature action label/id selection 与 optional feature availability/dependency summary formatting 分别拆入 `features/action.rs` 和 `features/summary.rs`：根 `features.rs` 从 95 行降为 5 行结构入口；新增子文件分别为 50、54 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app module-plugin projection feature action/summary subowner ownership scan 确认根 features 文件不再拥有 feature-state/dependency summary formatting 或 feature action-id selection；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 helpers callback-source window focus/resolution subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers/callback_surface/source_window.rs` 中 native floating-window source-id resolution/tests 与 callback-source scoping/floating-window focus tracking 分别拆入 `source_window/resolution.rs` 和 `source_window/focus.rs`：根 `source_window.rs` 从 107 行降为 4 行结构入口；新增子文件分别为 43、65 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers callback-source window focus/resolution subowner ownership scan 确认根 source_window 文件不再拥有 source-id resolution、focus dispatch、last-focused tracking 或 tests；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 helpers callback-surface host-frame source/workbench subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/helpers/callback_surface/surface_size/host_frames.rs` 中 callback-source floating/native window fallback size lookup 与 workbench drawer/document/viewport/root-pane fallback lookup 分别拆入 `host_frames/source_window.rs` 和 `host_frames/workbench.rs`：根 `host_frames.rs` 从 95 行降为 19 行结构入口；新增子文件分别为 42、55 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app helpers callback-surface host-frame source/workbench subowner ownership scan 确认根 host_frames 文件不再拥有 callback-source frame lookup、native-window bounds sizing、chrome snapshot read、drawer/document/viewport/root-pane fallback matching；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup state interaction subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/host_lifecycle/startup/state.rs` 中 initial pointer bridges、pointer state、scroll surfaces 和 asset pointer state initialization 拆入 `state/interaction.rs`，并将 one-time `RetainedEditorHost` field assembly 移入 `state/construction.rs`：根 `state.rs` 从 136 行降为 6 行结构入口；新增子文件分别为 132、64 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup state interaction subowner ownership scan 确认根 state 文件不再拥有 `RetainedEditorHost` field literal、pointer bridge initialization、scroll surface construction 或 asset pointer state initialization；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset mode-preview action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor/actions/mode_preview.rs` 中 preview preset、editor mode、designer tool 与 locale preview action-id mapping 分别拆入 `mode_preview/preview_preset.rs`、`mode_preview/editor_mode.rs`、`mode_preview/designer_tool.rs` 和 `mode_preview/locale.rs`：根 `mode_preview.rs` 从 89 行降为 28 行结构入口；新增子文件分别为 40、32、36、30 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset mode-preview action subowner ownership scan 确认根 mode_preview 文件不再拥有 preview preset、editor mode、designer tool、locale preview enum/string mapping 或 EditorManager setter calls；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset binding suggestion action/payload/route subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/binding/suggestions.rs` 中 binding payload、route 与 action suggestion application 分别拆入 `suggestions/payload.rs`、`suggestions/route.rs` 和 `suggestions/action.rs`：根 `suggestions.rs` 从 94 行降为 3 行结构入口；新增子文件均为 34 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset binding suggestion action/payload/route subowner ownership scan 确认根 suggestions 文件不再拥有 suggestion action ids、EditorManager apply calls、source-window focus、dirty marking 或 unknown-action status text；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 detail-scroll pointer console/inspector/asset-browser subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/detail_scroll_pointer.rs` 中 Console、Inspector 与 Asset Browser selected-asset detail scroll callbacks 分别拆入 `detail_scroll_pointer/console.rs`、`detail_scroll_pointer/inspector.rs` 和 `detail_scroll_pointer/asset_browser.rs`：根 `detail_scroll_pointer.rs` 从 93 行降为 3 行结构入口；新增子文件分别为 33、32、34 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app detail-scroll pointer console/inspector/asset-browser subowner ownership scan 确认根 detail_scroll_pointer 文件不再拥有 surface-size resolution、scroll layout sync、ScrollSurface dispatch、UiPoint construction、runtime/editor snapshot reads、pointer state UI writeback 或 status-line error handling；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 Welcome recent pointer click/motion/scroll subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/welcome_recent_pointer.rs` 中 recent-project click、hover movement 与 scroll callbacks 分别拆入 `welcome_recent_pointer/click.rs`、`welcome_recent_pointer/motion.rs` 和 `welcome_recent_pointer/scroll.rs`：根 `welcome_recent_pointer.rs` 从 91 行降为 3 行结构入口；新增子文件分别为 42、30、31 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app welcome recent pointer click/motion/scroll subowner ownership scan 确认根 welcome_recent_pointer 文件不再拥有 callback surface sizing、chrome snapshot reads、Welcome bridge dispatch、pointer move/scroll dispatch、returned event forwarding、pointer-state UI writeback 或 status-line error handling；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 asset-reference pointer click dispatch subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/asset_reference_pointer/events/click.rs` 中 asset-surface bridge availability checks、activity/browser references/used-by bridge selection、shared click dispatch、returned pointer-state writeback、returned host effects 和 click-target diagnostics 拆入 `events/click/dispatch.rs`：根 `click.rs` 从 93 行降为 37 行 click-preparation owner；新增子文件为 73 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app asset-reference pointer click dispatch subowner ownership scan 确认根 click 文件不再拥有 `ensure_asset_surface_bridge`、shared click callback dispatch、reference/used-by bridge matching、returned effects application、pointer-state writeback 或 target diagnostic status text；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset style rule/declaration subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓） | 将 `app/ui_asset_editor_detail_events/style/rules.rs` 中 stylesheet rule selection/rename/delete/reorder 与 style declaration select/upsert/delete 分别拆入 `style/rules/rule.rs` 和 `style/rules/declaration.rs`：根 `rules.rs` 从 90 行降为 2 行结构入口；新增子文件分别为 46、48 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset style rule/declaration subowner ownership scan 确认根 rules 文件不再拥有 `ViewInstanceId` import、style rule/declaration action ids、EditorManager mutation calls、presentation dirty propagation 或 unknown-action status branches；scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，Cargo 输出当前 warning 噪声（runtime 141 条、editor 65 条）但无 error。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner，并在 milestone testing stage 补跑完整测试矩阵。 |

| 2026-06-19 | 08 M3.S2 UI Asset theme action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 活跃切片阻塞） | 将 `app/ui_asset_editor/actions/theme.rs` 中 theme-source operations、style rule operations、pseudo-state toggles 与 indexed theme source selection 分别拆入 `theme/source.rs`、`theme/style_rule.rs`、`theme/pseudo_state.rs` 和 `theme/selection.rs`：根 `theme.rs` 从 86 行降为 25 行调度入口；新增子文件分别为 33、21、34、28 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset theme action subowner ownership scan 确认根 theme 文件不再拥有具体 theme/style action ids、EditorManager mutation calls、pseudo-state helper、selection parser 或 invalid-selection status text。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 当前在进入 editor 编译前被活跃 runtime session 阻塞：`zircon_runtime::scene::dynamic_scene::session::io` re-export 私有 IO helper，产生 E0364/E0603 可见性错误。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；待 runtime session IO 可见性切片恢复后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset workspace action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 活跃切片阻塞） | 将 `app/ui_asset_editor/actions/workspace.rs` 中 save/reload/keep-local、local-copy status、conflict diff snapshot、emergency recovery/Asset Browser open、undo/redo/reference-open 分别拆入 `workspace/save.rs`、`workspace/local_copy.rs`、`workspace/diff.rs`、`workspace/emergency.rs` 和 `workspace/history_reference.rs`：根 `workspace.rs` 从 75 行降为 30 行调度入口；新增子文件分别为 26、23、27、21、23 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset workspace action subowner ownership scan 确认根 workspace 文件不再拥有具体 workspace/emergency/undo/reference action ids、EditorManager mutation calls、local-copy/diff status text 或 Asset Browser descriptor construction。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 当前在进入 editor 编译前仍被活跃 runtime session 阻塞：`zircon_runtime::scene::dynamic_scene::session::io` re-export 私有 IO helper，产生 E0364/E0603 可见性错误。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；待 runtime session IO 可见性切片恢复后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset detail dispatch group subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 活跃切片阻塞） | 将 `app/ui_asset_editor_detail_events/detail_dispatch.rs` 中 stable detail-id routing 按 surface、style、preview、binding 四组拆入 `detail_dispatch/surface.rs`、`detail_dispatch/style.rs`、`detail_dispatch/preview.rs` 和 `detail_dispatch/binding.rs`：根 `detail_dispatch.rs` 从 93 行降为 52 行 group orchestration owner；新增子文件分别为 29、43、31、39 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset detail dispatch group subowner ownership scan 确认根 detail_dispatch 文件不再拥有具体 detail-id match table 或 handler calls，只保留分组调用顺序和 unknown-detail status fallback。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 当前在进入 editor 编译前仍被活跃 runtime session 阻塞：`zircon_runtime::scene::dynamic_scene::session::io` re-export 私有 IO helper，产生 E0364/E0603 可见性错误。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；待 runtime session IO 可见性切片恢复后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset widget commit/promote subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 活跃切片阻塞） | 将 `app/ui_asset_editor_detail_events/widget.rs` 中 widget prop/state component-adapter commit dispatch 与 promote-widget metadata commit dispatch 分别拆入 `widget/commit.rs` 和 `widget/promote.rs`：根 `widget.rs` 从 81 行降为 2 行结构入口；新增子文件分别为 49、35 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset widget commit/promote subowner ownership scan 确认根 widget 文件不再拥有 widget/promote action ids、component-adapter target paths、route helper import、ViewInstanceId construction、EditorManager promote calls、dirty propagation 或 unknown-action status text。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 当前在进入 editor 编译前仍被活跃 runtime session 阻塞：`zircon_runtime::scene::dynamic_scene::session::io` re-export 私有 IO helper，产生 E0364/E0603 可见性错误。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；待 runtime session IO 可见性切片恢复后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 Welcome session snapshot/present/apply subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 活跃切片阻塞） | 将 `app/welcome_session/session.rs` 中 runtime Welcome snapshot refresh、Welcome page presentation/recent-project refresh、startup-session document application 分别拆入 `session/snapshot.rs`、`session/present.rs` 和 `session/apply.rs`：根 `session.rs` 从 86 行降为 3 行结构入口；新增子文件分别为 8、21、63 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app welcome-session session subowner ownership scan 确认根 session 文件不再拥有 runtime snapshot writes、recent project refresh、show/dismiss Welcome page calls、startup builtin view open、project workspace application、runtime world replacement、asset workspace sync 或 invalidation/status writes。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 当前在进入 editor 编译前仍被活跃 runtime session 阻塞：`zircon_runtime::scene::dynamic_scene::session::io` re-export 私有 IO helper，产生 E0364/E0603 可见性错误。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；待 runtime session IO 可见性切片恢复后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup constructors/with-viewport subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 活跃切片阻塞） | 将 `app/host_lifecycle/startup.rs` 中 public/test construction entry points 与 viewport-ready retained-host construction flow 分别拆入 `startup/constructors.rs` 和 `startup/with_viewport.rs`：根 `startup.rs` 从 111 行降为 7 行结构入口；新增子文件分别为 30、81 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup constructors/with-viewport subowner ownership scan 确认根 startup 文件不再拥有 `RetainedEditorHost::new` / test constructor bodies、viewport controller creation、manager/session/state resolution、template bridge creation、native plugin live-host/play-mode backend construction、host field construction、startup asset sync 或 bootstrap refresh diagnostics。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 当前在进入 editor 编译前仍被活跃 runtime session 阻塞：`zircon_runtime::scene::dynamic_scene::session::io` re-export 私有 IO helper，产生 E0364/E0603 可见性错误。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；待 runtime session IO 可见性切片恢复后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup template-bridge bundle/factory subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/startup/template_bridges.rs` 中 `StartupTemplateBridges` bundle shape 与 startup template bridge factory 分别拆入 `template_bridges/bundle.rs` 和 `template_bridges/factory.rs`：根 `template_bridges.rs` 从 85 行降为 4 行结构入口；新增子文件分别为 20、74 行；补齐 bundle/factory 的 startup-family visibility，使 `with_viewport.rs` 与 `state/construction.rs` 能通过父模块结构入口消费。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup template-bridge bundle/factory subowner ownership scan 确认根 template_bridges 文件不再拥有 bridge bundle fields、builtin template runtime loading、bridge construction calls、component showcase runtime creation 或 startup bridge result assembly。第一次 compile-boundary check 暴露 E0365/E0364/E0603，原因是 `StartupTemplateBridges` 与 `create_startup_template_bridges(...)` 在 child module 内仅 `pub(super)`；已收窄修正为 `pub(in crate::ui::retained_host::app::host_lifecycle::startup)` 并重新运行 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset structure slot commit/semantic subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/ui_asset_editor_detail_events/structure/slot.rs` 中 slot mount/padding/preferred-size component-adapter commit dispatch 与 slot semantic value/delete/field edits 分别拆入 `structure/slot/commit.rs` 和 `structure/slot/semantic.rs`：根 `slot.rs` 从 83 行降为 21 行结构入口；新增子文件分别为 25、44 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset structure slot commit/semantic subowner ownership scan 确认根 slot 文件不再拥有 slot action ids、semantic action-path helper import、component-adapter commit calls、EditorManager semantic delete、dirty propagation 或具体 mutation routing；`git diff --check` scoped 检查仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset structure layout commit/semantic subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/ui_asset_editor_detail_events/structure/layout.rs` 中 layout preferred-size component-adapter commit dispatch 与 layout semantic value/delete/field edits 分别拆入 `structure/layout/commit.rs` 和 `structure/layout/semantic.rs`：根 `layout.rs` 从 61 行降为 21 行结构入口；新增子文件分别为 23、44 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset structure layout commit/semantic subowner ownership scan 确认根 layout 文件不再拥有 layout action ids、semantic action-path helper import、component-adapter commit calls、EditorManager semantic delete、dirty propagation 或具体 mutation routing；`git diff --check` scoped 检查仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle recompute invalidation decision/fast/slow subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/recompute/invalidation.rs` 中 recompute invalidation reason consumption/decision、paint-only fast-path dirty completion 与 slow-path rebuild diagnostics 分别拆入 `invalidation/decision.rs`、`invalidation/fast_path.rs` 和 `invalidation/slow_path.rs`：根 `invalidation.rs` 从 84 行降为 3 行结构入口；新增子文件分别为 40、31、31 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute invalidation decision/fast/slow subowner ownership scan 确认根 invalidation 文件不再拥有 decision DTO、reason consumption、paint-only/slow-path diagnostics、UI perf counter calls 或 dirty-flag mutation；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup state construction input/assembly subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/startup/state/construction.rs` 中 startup construction input DTO 与 one-time `RetainedEditorHost` field assembly 分别拆入 `construction/input.rs` 和 `construction/assembly.rs`：根 `construction.rs` 从 128 行降为 4 行结构入口；新增子文件分别为 23、108 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup state construction input/assembly subowner ownership scan 确认根 construction 文件不再拥有 field assembly、manager destructuring、interaction construction、dirty flag defaults 或 retained-host field assignments；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle recompute shell snapshot/builder/template subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/recompute/shell.rs` 中 recompute shell DTO、runtime layout/descriptors/chrome/model/geometry snapshot construction、root/workbench template-bridge recompute 分别拆入 `shell/snapshot.rs`、`shell/builder.rs` 和 `shell/template_bridges.rs`：根 `shell.rs` 从 83 行降为 3 行结构入口；新增子文件分别为 12、56、34 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle recompute shell snapshot/builder/template subowner ownership scan 确认根 shell 文件不再拥有 shell DTO、layout/descriptors reads、model-build perf counter、geometry computation、template bridge recompute 或 layout-frame capture；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 UI Asset binding entry lifecycle/field subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/ui_asset_editor_detail_events/binding/entry.rs` 中 binding add/delete mutation 与 binding id/event/route/target component-adapter commit dispatch 分别拆入 `binding/entry/lifecycle.rs` 和 `binding/entry/fields.rs`：根 `entry.rs` 从 76 行降为 24 行结构入口；新增子文件均为 28 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app UI Asset binding entry lifecycle/field subowner ownership scan 确认根 entry 文件不再拥有 binding action ids、EditorManager add/delete calls、component-adapter commit calls 或具体 target path mapping；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle tick/shell-metrics subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle.rs` 中 retained-host tick scheduling、refresh/committed pointer layout publication、shell-size synchronization、chrome snapshot building、frame visibility check 与 refresh invalidation diagnostics publication 分别拆入 `host_lifecycle/tick.rs` 和 `host_lifecycle/shell_metrics.rs`：根 `host_lifecycle.rs` 从 96 行降为 10 行结构入口；新增子文件分别为 54、39 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle tick/shell-metrics subowner ownership scan 确认根 host_lifecycle 文件不再拥有 tick/refresh bodies、UI perf scenario imports、shell-size mutation、chrome snapshot perf counter、frame visibility helper 或 diagnostics publication body；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle invalidation bridge dirty-flags/dirty-marking subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过；2026-06-22 路径已随 Editor UI 10 M4.T1 禁名切片更新） | 将 `app/host_lifecycle/invalidation_bridge.rs` 中 dirty-mask UI perf counter recording、pending UI perf scenario capture、invalidation-root mutation、legacy dirty-flag mutation、paint-only diagnostics publication 与 layout/presentation/render dirty-marking helpers 分别拆入 `invalidation_bridge/dirty_flags.rs` 和 `invalidation_bridge/dirty_marking.rs`：根 `invalidation_bridge.rs` 从 78 行降为 2 行结构入口；新增子文件分别为 64、17 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle invalidation bridge dirty-flags/dirty-marking subowner ownership scan 确认根 invalidation_bridge 文件不再拥有 dirty-mask perf counter calls、pending scenario capture、HostInvalidationRoot mutation、legacy dirty flag mutation、paint-only diagnostics publication 或 mark helper bodies；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup resources bundle/events/managers subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/startup/resources.rs` 中 `StartupManagers` bundle shape、asset/editor/resource change event subscriptions、manager/resource resolution 与 final startup bundle assembly 分别拆入 `startup/resources/bundle.rs`、`startup/resources/events.rs` 和 `startup/resources/managers.rs`：根 `resources.rs` 从 68 行降为 6 行结构入口；新增子文件分别为 20、38、45 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup resources bundle/events/managers subowner ownership scan 确认根 resources 文件不再拥有 manager/resource resolver imports、StartupManagers field declarations、event subscription calls、profile scope bodies 或 final bundle assembly；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle native-window presenter sync/target subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/native_window_presenters.rs` 中 native floating-window target collection、empty-target presenter cleanup、`NativeWindowPresenterStore::sync_targets(...)` orchestration、callback/presentation closure wiring 与 pane-template runtime selection 分别拆入 `native_window_presenters/targets.rs` 和 `native_window_presenters/sync.rs`：根 `native_window_presenters.rs` 从 82 行降为 5 行结构入口；新增子文件分别为 32、70 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle native-window presenter sync/target subowner ownership scan 确认根 native_window_presenters 文件不再拥有 target collection、empty cleanup、sync_targets closure wiring、pane-template runtime selection 或 native-window sync status errors；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 host-lifecycle startup with-viewport session/runtime/bootstrap/finalize subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `app/host_lifecycle/startup/with_viewport.rs` 中 startup session/state resolution、host-window bootstrap shell size、native live host/runtime/play-mode backend setup 与 startup asset sync/bootstrap refresh diagnostics 分别拆入 `with_viewport/session_state.rs`、`with_viewport/shell_bootstrap.rs`、`with_viewport/runtime_backend.rs` 和 `with_viewport/finalize.rs`：根 `with_viewport.rs` 从 84 行降为 60 行 construction-order owner；新增子文件分别为 30、12、33、7 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；app host-lifecycle startup with-viewport session/runtime/bootstrap/finalize subowner ownership scan 确认根 with_viewport 文件不再拥有 startup session/state resolver calls、host-window bootstrap read、native live host/runtime setup、play-mode backend setup 或 startup finalize calls；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 workbench Welcome main-column hero/form/action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_workbench_renderer/welcome/main_column.rs` 中 Welcome hero/status、new-project header/fields/path preview/validation 与 action buttons 分别拆入 `welcome/main_column/hero.rs`、`welcome/main_column/form.rs` 和 `welcome/main_column/actions.rs`：根 `main_column.rs` 从 345 行降为 126 行 layout-order owner；新增子文件分别为 64、117、72 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench welcome main-column hero/form/action subowner ownership scan 确认根 main_column 文件不再拥有 primitive draw calls、Welcome style constants、hero/status/form/validation/action concrete paint bodies 或 button paint body；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes popup-row menu/option/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_popup_rows.rs` 中 popup menu row sequencing、dropdown option row sequencing、shared popup surface/separator command emission 与 row label/shortcut text command emission 分别拆入 `template_popup_rows/menu.rs`、`template_popup_rows/options.rs`、`template_popup_rows/surface.rs` 和 `template_popup_rows/text.rs`：根 `template_popup_rows.rs` 从 348 行降为 35 行入口分派；新增子文件分别为 92、85、96、76 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes popup-row menu/option/surface/text subowner ownership scan 确认根 popup_rows 文件不再拥有 style selection、quad/text command emission、popup frame/row geometry、adornment dispatch 或 text-run paint style；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes status glyph signal/icon/geometry subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_status_glyphs.rs` 中 status signal ready/success/warning/info mark drawing、Snap/World/Target icon-button glyph drawing、shared segment/chevron emission 与 centered/local/scaled rect geometry 分别拆入 `template_status_glyphs/signals.rs`、`template_status_glyphs/icon_glyphs.rs`、`template_status_glyphs/segments.rs` 和 `template_status_glyphs/geometry.rs`：根 `template_status_glyphs.rs` 从 347 行降为 12 行结构入口；新增子文件分别为 139、122、45、61 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes status glyph signal/icon/geometry subowner ownership scan 确认根 status_glyphs 文件不再拥有具体 quad emission、signal/icon glyph bodies、shared segment helper 或 local rect helpers；scoped `git diff --check` 仅提示既有 docs/LF CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes popup-row adornment flag/selection/geometry/glyph subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_popup_row_adornments.rs` 中 raw menu flag parsing、option/menu adornment selection/default icon mapping、adornment reserved-width/rect geometry 与 right-side check/chevron/plus/folder/save/trash glyph command emission 分别拆入 `template_popup_row_adornments/flags.rs`、`template_popup_row_adornments/selection.rs`、`template_popup_row_adornments/geometry.rs` 和 `template_popup_row_adornments/glyphs.rs`：根 `template_popup_row_adornments.rs` 从 303 行降为 10 行结构入口；新增子文件分别为 29、51、21、214 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes popup-row adornment flag/selection/geometry/glyph subowner ownership scan 确认根 adornment 文件不再拥有 menu-item data parsing、adornment kind mapping、paint geometry、quad emission、glyph helper 或 segment helper；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes tree-row glyph disclosure/object/action/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_tree_row_glyphs.rs` 中 expanded/collapsed disclosure chevrons、scene-object kind selection plus cube/player-start/audio icons、eye/lock/kebab action glyphs 与 shared segment/local rect helpers 分别拆入 `template_tree_row_glyphs/disclosure.rs`、`template_tree_row_glyphs/object.rs`、`template_tree_row_glyphs/actions.rs` 和 `template_tree_row_glyphs/segments.rs`：根 `template_tree_row_glyphs.rs` 从 304 行降为 9 行结构入口；新增子文件分别为 60、123、99、31 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tree-row glyph disclosure/object/action/segment subowner ownership scan 确认根 tree_row_glyphs 文件不再拥有 concrete glyph bodies、state/icon classification、segment emission 或 local rect helpers；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes slider identity/track/thumb/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_sliders.rs` 中 Workbench slider recognition/style lookup、track/fill/tick command emission、thumb/halo command emission 与 label/value/range-min text command emission 分别拆入 `template_sliders/identity.rs`、`template_sliders/track.rs`、`template_sliders/thumb.rs` 和 `template_sliders/text.rs`：根 `template_sliders.rs` 从 335 行降为 113 行 sequencing entry；新增子文件分别为 12、77、42、112 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes slider identity/track/thumb/text subowner ownership scan 确认根 sliders 文件不再拥有 component-family recognition、style selector call、quad/text emission、thumb geometry, fill-span, or value-label command bodies；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes segmented-control identity/option/style/segment/tab subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_segmented_controls.rs` 中 segmented/tab component-family recognition、declared option/value parsing、style accessors、group/segment text emission、segmented surface/divider/selected segment drawing 与 tab surface/underline/text drawing 分别拆入 `template_segmented_controls/identity.rs`、`template_segmented_controls/options.rs`、`template_segmented_controls/style.rs`、`template_segmented_controls/labels.rs`、`template_segmented_controls/segments.rs` 和 `template_segmented_controls/tabs.rs`：根 `template_segmented_controls.rs` 从 353 行降为 58 行 dispatch entry；新增子文件分别为 12、37、75、59、113、59 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes segmented-control identity/option/style/segment/tab subowner ownership scan 确认根 segmented_controls 文件不再拥有 component-family recognition、option/value parsing、style selector accessors、quad/text command emission 或 segmented/tab concrete paint bodies；scoped `git diff --check` 仅提示既有 docs LF/CRLF 转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes selection-control checkbox/radio/toggle/style/label/identity subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_selection_controls.rs` 中 checkbox/radio/toggle component-family classification、checkbox mark/tick command emission、radio mark/dot command emission、toggle label/track/thumb command emission、shared label text emission 与 Workbench selection-control style/color/state accessors 分别拆入 `template_selection_controls/identity.rs`、`template_selection_controls/checkbox.rs`、`template_selection_controls/radio.rs`、`template_selection_controls/toggle.rs`、`template_selection_controls/labels.rs` 和 `template_selection_controls/style.rs`：根 `template_selection_controls.rs` 从 323 行降为 68 行 dispatch entry；新增子文件分别为 20、82、53、58、32、65 行。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、`cargo fmt -p zircon_runtime --check` 通过；paint_template_nodes selection-control checkbox/radio/toggle/style/label/identity subowner ownership scan 确认根 selection_controls 文件不再拥有 component-family classification、style selector calls、quad/text command emission、checkbox/radio/toggle concrete paint bodies 或 label body；首轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 暴露既有 runtime `pass_authoring.rs` 缺少 `RenderFeaturePassDescriptor` 导入，按下层编译错误补 import 后重跑通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes viewport-scene architecture doors/panels/stairs/walls/primitives subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_viewport_scene_architecture.rs` 中 viewport back-door/door-core command emission、side-panel detail lines、side-stair surface/steps、wall detail lines/column edges 与共享 inset/alpha helper 分别拆入 `template_viewport_scene_architecture/doors.rs`、`panels.rs`、`stairs.rs`、`walls.rs` 和 `primitives.rs`：根 `template_viewport_scene_architecture.rs` 从 341 行降为 9 行 dispatch/re-export entry；新增子文件分别为 89、33、45、101、80 行。 | `cargo fmt -p zircon_editor` 通过；paint_template_nodes viewport-scene architecture doors/panels/stairs/walls/primitives subowner ownership scan 确认根 architecture 文件不再拥有 `HostPaintCommand::quad`、architecture helper bodies、style helper calls、base-surface calls 或 shared inset/alpha helper bodies；首轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 暴露 child-module re-export visibility E0364/E0603，修正 child entry visibility 后重跑通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes viewport-scene surfaces backdrop/ceiling/back-wall/floor/primitives subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 已补跑通过） | 将 `host_contract/paint_template_nodes/template_viewport_scene_surfaces.rs` 中 viewport backdrop haze/side-shadow、ceiling rib/glint/shadow、back-wall panel grid/haze、floor depth lines/warm sheen/bottom shadow 与共享 layer/alpha helper 分别拆入 `template_viewport_scene_surfaces/backdrop.rs`、`ceiling.rs`、`back_wall.rs`、`floor.rs` 和 `primitives.rs`：根 `template_viewport_scene_surfaces.rs` 从 300 行降为 9 行 dispatch/re-export entry；新增子文件分别为 59、61、77、92、26 行。 | `cargo fmt -p zircon_editor` 通过；paint_template_nodes viewport-scene surfaces backdrop/ceiling/back-wall/floor/primitives subowner ownership scan 确认根 surfaces 文件不再拥有 `HostPaintCommand::quad`、surface helper bodies、base-surface calls、surface color constants 或 shared layer/alpha helper bodies；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 通过，仅保留既有 warning 噪声（runtime 141 条、editor 65 条）。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口补跑完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes viewport-scene light ambient/fixture/primitives subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_viewport_scene_light.rs` 中 soft light、soft shadow、floor reflection command emission、wall-light/beacon fixture command emission 与共享 expanded/inset layer/alpha helper 分别拆入 `template_viewport_scene_light/ambient.rs`、`fixtures.rs` 和 `primitives.rs`：根 `template_viewport_scene_light.rs` 从 293 行降为 5 行 dispatch/re-export entry；新增子文件分别为 149、91、60 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes viewport-scene light ambient/fixture/primitives subowner ownership scan 确认根 light 文件不再拥有 `HostPaintCommand::quad`、light helper bodies、surface style calls、base-surface calls、fixture constants 或 shared layer/alpha helper bodies；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-owner-split-0619 --message-format short --color never` 连续三次超时且无 compiler diagnostics，默认 target 缓存检查也超时，残留 cargo/rustc 进程已清理。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes viewport-scene floor grate/grid/panel/seam/primitives subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_viewport_scene_floor.rs` 中 major/minor grid line glow、repeating grate slot stripes、floor panel base/inset/shadow、floor seam glow/core 与共享 inset/alpha helper 分别拆入 `template_viewport_scene_floor/grid.rs`、`grate.rs`、`panels.rs`、`seams.rs` 和 `primitives.rs`：根 `template_viewport_scene_floor.rs` 从 282 行降为 9 行 dispatch/re-export entry；新增子文件分别为 54、80、40、39、80 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes viewport-scene floor grate/grid/panel/seam/primitives subowner ownership scan 确认根 floor 文件不再拥有 `HostPaintCommand::quad`、floor helper bodies、surface style calls、base-surface calls、grid/grate/panel constants 或 shared inset/alpha helper bodies；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未再启动 editor Cargo check，因为 light 切片后 Cargo check lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes viewport-scene identity/geometry subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_viewport_scene.rs` 中 viewport control-id classification、chrome/exclusion checks、scene-kind enum 与 pixel-aligned rect normalization 分别拆入 `template_viewport_scene/identity.rs` 和 `template_viewport_scene/geometry.rs`：根 `template_viewport_scene.rs` 从 274 行降为 144 行 scene-kind dispatch owner；新增子文件分别为 127、9 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes viewport-scene identity/geometry subowner ownership scan 确认根 viewport_scene 文件不再拥有 `ViewportSceneKind` enum body、control-id classification、chrome-node checks、viewport prefix 或 pixel-alignment helper body；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes material feedback backdrop/progress/state/circular subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_material_feedback.rs` 中 backdrop recognition/scrim emission、linear progress command emission、circular progress raster/image command generation、Material progress recognition/percent/mode/tone/color helpers 分别拆入 `template_material_feedback/backdrop.rs`、`progress.rs`、`circular_progress.rs` 和 `state.rs`：根 `template_material_feedback.rs` 从 273 行降为 27 行 dispatch entry；新增子文件分别为 41、85、83、61 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes material feedback backdrop/progress/state/circular subowner ownership scan 确认根 material_feedback 文件不再拥有 concrete `HostPaintCommand` bodies、backdrop constants、progress state helpers、linear progress bodies 或 circular raster/image generation；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes notification-center identity/layout/panel/row/style subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_notification_center.rs` 中 NotificationCenter recognition/header/empty text、panel/row geometry、panel/header/empty-message command emission、notification row command emission、row state/severity colors 分别拆入 `template_notification_center/identity.rs`、`layout.rs`、`panel.rs`、`row.rs` 和 `style.rs`：根 `template_notification_center.rs` 从 267 行降为 49 行 dispatch entry；新增子文件分别为 30、86、59、68、47 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes notification-center identity/layout/panel/row/style subowner ownership scan 确认根 notification_center 文件不再拥有 concrete `HostPaintCommand` bodies、panel/row constants、style helpers、text-state helpers 或 row command body；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes notification-center command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_notification_center.rs` 中 NotificationCenter recognition gate、popup-open gate、pixel-aligned rect gate、panel command sequencing、empty-message routing、row iteration、row rect lookup 与 row command fan-out 下沉到 `template_notification_center/commands.rs`：根 `template_notification_center.rs` 从 56 行降为 8 行 structural export entry；新增 `commands.rs` 为 51 行，并恢复根文件对既有 `style.rs` 子 owner 的显式 module wiring，保持 `template_nodes/specialized.rs` 对 `push_notification_center_commands` 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes notification-center command sequencing ownership scan 确认根 notification_center 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、popup/rect gating、panel/empty-message routing、row iteration 或 `push_notification_center_commands` body，仅保留 module wiring 与 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes alert identity/inline/layout/toast subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_alerts.rs` 中 alert/toast recognition、tone parsing、inline alert surface/icon/text command emission、alert/toast geometry、toast surface/status/action/close command emission 与 status mark sizing 分别拆入 `template_alerts/identity.rs`、`inline.rs`、`layout.rs` 和 `toast.rs`：根 `template_alerts.rs` 从 264 行降为 44 行 dispatch entry；新增子文件分别为 55、51、91、81 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes alert identity/inline/layout/toast subowner ownership scan 确认根 alerts 文件不再拥有 concrete `HostPaintCommand` bodies、alert/toast layout constants、tone parsing body、inline/toast command bodies 或 toast geometry helpers；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes alert command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_alerts.rs` 中 alert/toast recognition gating、pixel-aligned rect validation、inline-versus-toast dispatch 与 child-owner fan-out 拆入 `template_alerts/commands.rs`：根 `template_alerts.rs` 从 44 行降为 22 行 module wiring/test-import entry；新增 `commands.rs` 为 33 行，并保持 `template_nodes/specialized.rs` 对 `push_alert_commands` 的原 root 导入路径以及 `template_alerts_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes alert command sequencing ownership scan 确认根 `template_alerts.rs` 不再拥有 `HostPaintCommand` import、inline/toast child-owner imports、pixel-aligned rect gate、kind dispatch match 或 `push_alert_commands` body，仅保留 module wiring、test imports 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes property-row identity/layout/label/field/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_property_rows.rs` 中 PropertyRow/component-row recognition、label/value/axis rectangle math、label text command emission、scalar/axis field surface/value text command emission 与 shared text command construction 分别拆入 `template_property_rows/identity.rs`、`layout.rs`、`labels.rs`、`fields.rs` 和 `text.rs`：根 `template_property_rows.rs` 从 233 行降为 75 行 dispatch entry；新增子文件分别为 23、88、24、83、24 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes property-row identity/layout/label/field/text subowner ownership scan 确认根 property_rows 文件不再拥有 concrete `HostPaintCommand` bodies、label/value/axis layout constants、component row recognition body、field border helper 或 shared text command body；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes property-row command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_property_rows.rs` 中 PropertyRow recognition gate、label/value trim gate、label width routing、label command sequencing、scalar-versus-axis value selection 与 field/text command fan-out 拆入 `template_property_rows/commands.rs`：根 `template_property_rows.rs` 从 75 行降为 18 行 structural export entry；新增 `commands.rs` 为 56 行，并保持 `template_property_rows_tests.rs` 的 `super::*` 可见测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes property-row command sequencing ownership scan 确认根 template_property_rows 文件不再拥有 `HostPaintCommand` import、value-area routing、axis/scalar dispatch body、label command fan-out 或 `push_property_row_text_commands` body，仅保留 module wiring、command re-export 与 test-only imports；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes command-palette identity/layout/panel/rows subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_command_palette.rs` 中 CommandPalette recognition、panel/search/row geometry、panel/search/empty-message command emission、command row style/surface/selection mark/label command emission 分别拆入 `template_command_palette/identity.rs`、`layout.rs`、`panel.rs` 和 `rows.rs`：根 `template_command_palette.rs` 从 229 行降为 49 行 dispatch entry；新增子文件分别为 4、75、76、98 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes command-palette identity/layout/panel/rows subowner ownership scan 确认根 command_palette 文件不再拥有 concrete `HostPaintCommand` bodies、panel/search/row layout constants、row style helper、row surface body、row label body 或 pixel-align helper；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes command-palette command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_command_palette.rs` 中 CommandPalette recognition gate、popup-open gate、pixel-aligned rect gate、panel command sequencing、empty-message routing、row iteration、row rect lookup 与 row command fan-out 下沉到 `template_command_palette/commands.rs`：根 `template_command_palette.rs` 从 56 行降为 7 行 structural export entry；新增 `commands.rs` 为 51 行，并保持 `template_nodes/specialized.rs` 对 `push_command_palette_commands` 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes command-palette command sequencing ownership scan 确认根 command_palette 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、popup/rect gating、panel/empty-message routing、row iteration 或 `push_command_palette_commands` body，仅保留 module wiring 与 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes button identity/geometry/style/surface/content subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_buttons.rs` 中 Workbench button recognition/kind/key/add-component classification、paint rect/radius/pixel alignment、style/opacity resolution、surface command emission 与 glyph/text content command emission 分别拆入 `template_buttons/identity.rs`、`geometry.rs`、`style.rs`、`surface.rs` 和 `content.rs`：根 `template_buttons.rs` 从 227 行降为 48 行 dispatch entry；新增子文件分别为 48、33、21、27、108 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes button identity/geometry/style/surface/content subowner ownership scan 确认根 buttons 文件不再拥有 concrete `HostPaintCommand` bodies、button constants、key/add-component helper、pixel-align helper、surface/content bodies 或 style/radius bodies；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；`cargo check -p zircon_editor --lib` 3 分钟超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes viewport-scene gizmo axes/center/selection subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_viewport_scene_gizmos.rs` 中 axis line/origin color/glow/cap command emission、gizmo center rod/cube command emission 与 selection glow command emission 分别拆入 `template_viewport_scene_gizmos/axes.rs`、`center.rs` 和 `selection.rs`：根 `template_viewport_scene_gizmos.rs` 从 233 行降为 7 行 structural export entry；新增子文件分别为 140、71、28 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes viewport-scene gizmo axes/center/selection subowner ownership scan 确认根 gizmos 文件不再拥有 concrete `HostPaintCommand` bodies、gizmo/axis color constants、axis color helper、declared surface helper、axis glow helper 或 cap helper；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes status-control identity/signal/chip/icon subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_status_controls.rs` 中 status ID classification/control kind、status signal icon/text command emission、status chip surface/text/chevron command emission 与 status icon-button surface/glyph command emission 分别拆入 `template_status_controls/identity.rs`、`signals.rs`、`chips.rs` 和 `icons.rs`：根 `template_status_controls.rs` 从 226 行降为 57 行 dispatch entry；新增子文件分别为 26、89、55、40 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes status-control identity/signal/chip/icon subowner ownership scan 确认根 status_controls 文件不再拥有 concrete `HostPaintCommand` bodies、StatusControlKind enum、status ID classifier、signal/chip/icon command bodies、text-label command helpers 或 style/test color helper bodies；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes tree-row identity/style/surface/label/action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_tree_rows.rs` 中 TreeRow recognition、shared style/color/state accessors、row surface/indent-guide command emission、label text command emission 与 eye/kebab/lock action command emission 分别拆入 `template_tree_rows/identity.rs`、`style.rs`、`surface.rs`、`labels.rs` 和 `actions.rs`：根 `template_tree_rows.rs` 从 223 行降为 57 行 sequencing entry；新增子文件分别为 8、39、58、36、55 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tree-row identity/style/surface/label/action subowner ownership scan 确认根 tree_rows 文件不再拥有 concrete `HostPaintCommand` bodies、TreeRow recognition body、surface/indent guide bodies、label text body、action body、style/color helper bodies 或 lock-action visibility helper；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes tooltip identity/layout/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_tooltips.rs` 中 Workbench tooltip recognition、bubble rect/pixel alignment、shadow/surface command emission 与 title/body fallback/text command emission 分别拆入 `template_tooltips/identity.rs`、`layout.rs`、`surface.rs` 和 `text.rs`：根 `template_tooltips.rs` 从 200 行降为 78 行 sequencing entry；新增子文件分别为 8、22、43、81 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tooltip identity/layout/surface/text subowner ownership scan 确认根 tooltips 文件不再拥有 concrete `HostPaintCommand` bodies、tooltip recognition body、bubble/pixel-align helpers、surface/shadow body、title/body fallback helpers、text command body 或 tooltip constants；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；scoped whitespace scan 覆盖新增子文件并通过；未再启动 editor Cargo check，因为 active Cargo lane 已连续超时且无 compiler diagnostics，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes drag-overlay identity/layout/style/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_drag_overlay.rs` 中 DragOverlay role recognition、preview/icon/text/drop-indicator geometry、allowed/blocked color selection、preview surface/icon/drop-indicator quad emission 与 drag label fallback/text command emission 分别拆入 `template_drag_overlay/identity.rs`、`layout.rs`、`style.rs`、`surface.rs` 和 `text.rs`：根 `template_drag_overlay.rs` 从 194 行降为 40 行 sequencing entry；新增子文件分别为 5、97、27、63、41 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes drag-overlay identity/layout/style/surface/text subowner ownership scan 确认根 drag_overlay 文件不再拥有 concrete `HostPaintCommand::quad/text` bodies、preview/drop-indicator constants、overlay recognition body、preview/drop-indicator geometry helpers、label fallback body 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 600 秒超时且无 compiler diagnostics，残留进程检查仅见无关 `zircon_runtime` 测试通道，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes list-row glyph geometry/selection/segment/shape subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_list_row_glyphs.rs` 中 list-row adornment kind declaration/disabled-selected-chevron selection、right-adornment geometry、shared segment-to-quad emission 与 check/right-chevron/disabled-diamond pixel shapes 分别拆入 `template_list_row_glyphs/selection.rs`、`geometry.rs`、`segments.rs` 和 `shapes.rs`：根 `template_list_row_glyphs.rs` 从 196 行降为 32 行 adornment dispatch entry；新增子文件分别为 30、13、24、122 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes list-row glyph geometry/selection/segment/shape subowner ownership scan 确认根 list_row_glyphs 文件不再拥有 concrete `HostPaintCommand::quad` bodies、style-state selection body、right-adornment constants/geometry、specific glyph shape bodies、disabled diamond palette access 或 `UiPainterResolvedState` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未再启动 editor Cargo check，因为无关 `zircon_runtime` Cargo 通道仍在活跃且上一轮 editor check 已 600 秒超时无诊断，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes inspector-row glyph swatch/cube/chevron/check/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_inspector_row_glyphs.rs` 中 material swatch rect/color/command emission、mesh cube segments、down-chevron segment selection、check-tick segments 与 shared glyph segment-to-quad emission 分别拆入 `template_inspector_row_glyphs/swatches.rs`、`cubes.rs`、`chevrons.rs`、`checks.rs` 和 `segments.rs`：根 `template_inspector_row_glyphs.rs` 从 187 行降为 10 行 structural export entry；新增子文件分别为 31、41、60、41、25 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes inspector-row glyph swatch/cube/chevron/check/segment subowner ownership scan 确认根 inspector_row_glyphs 文件不再拥有 concrete `HostPaintCommand::quad` bodies、swatch constants、segment helper、inline `FrameRect` shape bodies、palette access 或 rect-width/height shape math；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未再启动 editor Cargo check，因为无关 `zircon_runtime` Cargo 通道仍在活跃且上一轮 editor check 已 600 秒超时无诊断，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes axis-label identity/style/text/scale-link subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_axis_labels.rs` 中 Label/Icon/SvgIcon recognition、transform-axis token mapping、scale-link classification、axis/link color/font constants、axis text command emission 与 scale-link glyph geometry/quad emission 分别拆入 `template_axis_labels/identity.rs`、`style.rs`、`text.rs` 和 `scale_link.rs`：根 `template_axis_labels.rs` 从 176 行降为 36 行 dispatch entry；新增子文件分别为 28、34、36、64 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes axis-label identity/style/text/scale-link subowner ownership scan 确认根 axis_labels 文件不再拥有 concrete `HostPaintCommand::text/quad` bodies、axis/link constants、axis-label classification body、axis color selection body、scale-link origin/body 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未再启动 editor Cargo check，因为无关 `zircon_runtime` Cargo 通道仍在活跃且上一轮 editor check 已 600 秒超时无诊断，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes alert glyph close/mark/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_alert_glyphs.rs` 中 info/success/warning/error status mark drawing、close-X segment drawing、shared 18px coordinate scaling、`ALERT_ICON_SIZE` 与 segment-to-quad emission 分别拆入 `template_alert_glyphs/marks.rs`、`close.rs` 和 `segments.rs`：根 `template_alert_glyphs.rs` 从 164 行降为 6 行 structural export entry；新增子文件分别为 120、30、36 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes alert glyph close/mark/segment subowner ownership scan 确认根 alert_glyphs 文件不再拥有 concrete `HostPaintCommand::quad` bodies、alert glyph constants、status-mark match body、close mark body、segment scaling helper 或 `FrameRect`/`AlertTone` imports；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未再启动 editor Cargo check，因为无关 `zircon_runtime` Cargo 通道仍在活跃且上一轮 editor check 已 600 秒超时无诊断，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes section-title identity/geometry/style/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_section_titles.rs` 中 Workbench section-title control-id recognition、pixel alignment、icon/label rect geometry、section title text colors/line metrics、label lookup、bold overdraw 与 text command emission 分别拆入 `template_section_titles/identity.rs`、`geometry.rs`、`style.rs` 和 `text.rs`：根 `template_section_titles.rs` 从 156 行降为 46 行 sequencing entry；新增子文件分别为 8、33、23、65 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes section-title identity/geometry/style/text subowner ownership scan 确认根 section_titles 文件不再拥有 concrete `HostPaintCommand::text` bodies、section title constants、section recognition body、text color selection body、label/text command body、pixel-align helper 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未再启动 editor Cargo check，因为无关 `zircon_runtime` Cargo 通道仍在活跃且上一轮 editor check 已 600 秒超时无诊断，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes section-title command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_section_titles.rs` 中 Workbench section-title recognition gate、pixel-aligned rect gate、icon lookup、icon command dispatch 与 label command ordering 下沉到 `template_section_titles/commands.rs`：根 `template_section_titles.rs` 从 46 行降为 16 行 module wiring/test-import entry；新增 `commands.rs` 为 39 行，并保持 `template_nodes/specialized.rs` 对 `push_section_title_commands` 的原 root 导入路径以及 `template_section_titles_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes section-title command sequencing ownership scan 确认根 section_titles 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、glyph child-owner call、icon/text command sequencing 或 `push_section_title_commands` body，仅保留 module wiring、test imports 与 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes chip identity/geometry/style/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_chips.rs` 中 Workbench chip recognition/status-chip exclusion、pixel alignment、label rect geometry、chip surface/border/text/glyph colors、surface command emission、label lookup、chevron reserve handling 与 text command emission 分别拆入 `template_chips/identity.rs`、`geometry.rs`、`style.rs`、`surface.rs` 和 `text.rs`：根 `template_chips.rs` 从 152 行降为 49 行 sequencing entry；新增子文件分别为 16、20、48、22、36 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes chip identity/geometry/style/surface/text subowner ownership scan 确认根 chips 文件不再拥有 concrete `HostPaintCommand::quad/text` bodies、chip constants、chip recognition body、surface/border/text/glyph color helper bodies、label command body、pixel-align helper 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 在本切片后尝试运行并于 300 秒超时，无 compiler diagnostics；残留进程检查仅见无关 `zircon_runtime` Cargo 测试通道，按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes chip command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_chips.rs` 中 Workbench chip recognition gate、pixel-aligned rect gate、surface/label command ordering、chevron availability check、glyph-color lookup 与 chevron child-owner dispatch 下沉到 `template_chips/commands.rs`：根 `template_chips.rs` 从 55 行降为 19 行 module wiring/test-import entry；新增 `commands.rs` 为 39 行，并保持 `template_nodes/specialized.rs` 对 `push_chip_commands` 的原 root 导入路径以及 `template_chips_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes chip command sequencing ownership scan 确认根 chips 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、chevron availability/color lookup、pixel-aligned rect gating 或 chip surface/text/chevron command sequencing，仅保留 module wiring、test imports 与 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-19 | 08 M3.S2 paint_template_nodes dialog identity/layout/content/surface/style/action subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前超时待测试阶段补跑） | 将 `host_contract/paint_template_nodes/template_dialogs.rs` 中 Dialog/ConfirmDialog recognition、popup-open paint state、pixel alignment、visible-area gating、title/body/action/severity-mark geometry、dialog surface/severity-mark command emission、title/body fallback/text command emission 分别拆入 `template_dialogs/identity.rs`、`layout.rs`、`surface.rs` 和 `content.rs`，并将 action 右边界统一改由 `layout.rs` 提供；根 `template_dialogs.rs` 从 152 行降为 63 行 sequencing entry；新增子文件分别为 35、60、42、72 行，既有 `actions.rs`/`style.rs` 保留为 action/state-color owners。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes dialog identity/layout/content/surface/style/action subowner ownership scan 确认根 dialogs 文件不再拥有 dialog-kind body、popup-open state body、title/body fallback helpers、pixel-align helper、dialog constants、concrete `HostPaintCommand::quad/text` bodies 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增/更新子文件并通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 在本切片后尝试运行并于 184 秒超时，无 compiler diagnostics，超时进程随后退出；按计划留到 milestone testing stage 补跑。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口优先补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes dialog command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_dialogs.rs` 中 Dialog/ConfirmDialog paint-state gating、pixel-aligned rect gating、unavailable-state resolution、chrome/content/action ordering 与 child-owner fan-out 下沉到 `template_dialogs/commands.rs`：根 `template_dialogs.rs` 从 58 行降为 8 行 structural export entry；新增 `commands.rs` 为 53 行，并保持 `template_nodes/specialized.rs` 对 `push_dialog_commands` 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes dialog command sequencing ownership scan 确认根 dialog 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、`DialogPaintState` match、visible-area gating、unavailable-state resolution 或 dialog chrome/content/action command sequencing，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-19 | 08 M3.S2 paint_template_nodes section-title glyph identity/metrics/style/shape/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_section_title_glyphs.rs` 中 section-title icon-kind selection、icon size/gap metrics、icon color/transform opacity、cube/transform/mesh shape dispatch 与 shared 14px segment scaling/quad emission 分别拆入 `template_section_title_glyphs/identity.rs`、`metrics.rs`、`style.rs`、`shapes.rs` 和 `segments.rs`：根 `template_section_title_glyphs.rs` 从 158 行降为 10 行 structural export entry；新增子文件分别为 17、2、11、98、36 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes section-title glyph identity/metrics/style/shape/segment subowner ownership scan 确认根 section_title_glyphs 文件不再拥有 `TemplatePaneNodeData`/`FrameRect`/`HostPaintCommand` imports、icon-kind enum body、control-id mapping body、icon color body、shape bodies、segment helper、scaled-rect helper 或 `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 在本切片后运行但被 `zircon_runtime` 当前 render-history 编译错误阻塞（`VisibilityStaticIndex` 未导出、`ViewportRecord::into_history` 缺失、history API 参数数量不匹配），未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes slider geometry metrics/rect/label/value subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_slider_geometry.rs` 中 slider metrics constants、track/value/range-min/centered/pixel-aligned rect helpers、label/value/range-min text formatting、percent/range/tick/fill-span/thumb-size helpers 分别拆入 `template_slider_geometry/metrics.rs`、`rects.rs`、`labels.rs` 和 `values.rs`：根 `template_slider_geometry.rs` 从 163 行降为 19 行 structural export entry；新增子文件分别为 22、92、26、66 行，并保持 `template_sliders.rs`、`template_sliders/text.rs`、`thumb.rs`、`track.rs` 与现有测试的导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes slider geometry metrics/rect/label/value subowner ownership scan 确认根 slider_geometry 文件不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、`SLIDER_*` constant bodies、slider rect/value/label helper bodies、declared-percent helper 或 pixel-align helper body；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为上一轮已被 `zircon_runtime` 当前 render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes tooltip glyph metrics/arrow/icon subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_tooltip_glyphs.rs` 中 tooltip arrow/icon size resolution、bubble arrow diamond rasterization/command emission、info-icon rect placement/outline/stem/dot command emission 分别拆入 `template_tooltip_glyphs/metrics.rs`、`arrows.rs` 和 `icons.rs`：根 `template_tooltip_glyphs.rs` 从 149 行降为 7 行 structural export entry；新增子文件分别为 26、63、67 行。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tooltip glyph metrics/arrow/icon subowner ownership scan 确认根 tooltip_glyphs 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、tooltip size constants、size helper bodies、arrow/icon command bodies、diamond raster body 或 `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes button glyph identity/metrics/shape/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_button_glyphs.rs` 中 button key to glyph selection、button icon size、plus/trash/chevron shape dispatch 与 shared 14px segment scaling/quad emission 分别拆入 `template_button_glyphs/identity.rs`、`metrics.rs`、`shapes.rs` 和 `segments.rs`：根 `template_button_glyphs.rs` 从 109 行降为 8 行 structural export entry；新增子文件分别为 21、2、55、37 行，并保持 `template_buttons/content.rs` 与 `template_buttons_tests.rs` 的导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes button glyph identity/metrics/shape/segment subowner ownership scan 确认根 button_glyphs 文件不再拥有 `FrameRect`/`HostPaintCommand` imports、glyph enum body、key mapping body、shape bodies、segment helper、scaled-rect helper、`BUTTON_ICON_SIZE` constant body 或 `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes dropdown identity/geometry/style/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_dropdowns.rs` 中 Workbench dropdown recognition、paint rect/pixel alignment、placeholder-aware style lookup、surface command emission、label fallback/text command emission 分别拆入 `template_dropdowns/identity.rs`、`geometry.rs`、`style.rs`、`surface.rs` 和 `text.rs`：根 `template_dropdowns.rs` 从 120 行降为 51 行 sequencing entry；新增子文件分别为 9、17、7、25、56 行，并保持 `template_dropdowns_tests.rs` 的 `super::*` 可见测试入口与 chevron glyph dispatch 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes dropdown identity/geometry/style/surface/text subowner ownership scan 确认根 dropdowns 文件不再拥有 dropdown recognition body、pixel-align helper body、placeholder fallback helper、style helper body、surface/text command bodies、dropdown constants 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes dropdown command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_dropdowns.rs` 中 Workbench dropdown recognition gate、paint-rect gate、style lookup orchestration、surface/text command ordering 与 chevron glyph dispatch 下沉到 `template_dropdowns/commands.rs`：根 `template_dropdowns.rs` 从 51 行降为 22 行 module wiring/test-import entry；新增 `commands.rs` 为 37 行，并保持 `template_nodes/specialized.rs` 对 `push_dropdown_commands` 与 `dropdown_paint_rect` 的原 root 导入路径以及 `template_dropdowns_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes dropdown command sequencing ownership scan 确认根 `template_dropdowns.rs` 不再拥有 `HostPaintCommand` import、production identity/style/surface/text imports、chevron child-owner call 或 `push_dropdown_commands` body，仅保留 module wiring、test imports、`dropdown_paint_rect` re-export 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes field identity/geometry/style/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_fields.rs` 中 Workbench field recognition、stepper classification、paint rect/pixel alignment、opacity/style lookup、surface command emission、label fallback/text command emission 分别拆入 `template_fields/identity.rs`、`geometry.rs`、`style.rs`、`surface.rs` 和 `text.rs`：根 `template_fields.rs` 从 151 行降为 64 行 sequencing entry；新增子文件分别为 14、17、11、25、64 行，并保持 `template_fields_tests.rs` 的 `super::*` 可见测试入口和 stepper glyph dispatch 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes field identity/geometry/style/surface/text subowner ownership scan 确认根 fields 文件不再拥有 Workbench field recognition body、stepper body、pixel-align helper body、field opacity/style helper body、surface/text command bodies、field constants、text label fallback 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes field command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_fields.rs` 中 Workbench field recognition gate、paint-rect gate、opacity/style lookup orchestration、surface/stepper/text command ordering 与 stepper child-owner dispatch 下沉到 `template_fields/commands.rs`：根 `template_fields.rs` 从 64 行降为 21 行 module wiring/test-import entry；新增 `commands.rs` 为 50 行，并保持 `template_nodes/specialized.rs` 对 `push_field_commands` 的原 root 导入路径以及 `template_fields_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes field command sequencing ownership scan 确认根 `template_fields.rs` 不再拥有 `HostPaintCommand` import、production geometry/identity/style/surface/text imports、stepper child-owner call 或 `push_field_commands` body，仅保留 module wiring、test imports 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes template style color/dimension/elevation/overlay/state subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_style.rs` 中 shared surface/border/text color sequencing、border width/radius resolution、elevation shadow gating/geometry、MUI overlay component-role recognition 与 button interaction/disabled state helpers 分别拆入 `template_style/colors.rs`、`dimensions.rs`、`elevation.rs`、`overlay.rs` 和 `state.rs`：根 `template_style.rs` 从 217 行降为 18 行 structural export entry；新增子文件分别为 147、26、18、15、16 行；`template_style_tests.rs` 改为直接导入 test-only state helper 和 shared fixtures。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes template style color/dimension/elevation/overlay/state subowner ownership scan 确认根 style 文件不再拥有 color/geometry/elevation/overlay/state helper bodies、`PALETTE`/`ButtonInteractionState`/`ButtonVariant`/`FrameRect`/`TemplatePaneNodeData` imports 或 elevation shadow constants；scoped whitespace scan 覆盖 root、新增子文件和 test import update 并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes icon button identity/geometry/style/surface subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_icon_buttons.rs` 中 Workbench icon-button recognition/status exclusion、paint/glyph rect geometry、context/style selection、surface command emission 分别拆入 `template_icon_buttons/identity.rs`、`geometry.rs`、`style.rs` 和 `surface.rs`：根 `template_icon_buttons.rs` 从 134 行降为 52 行 sequencing entry；新增子文件分别为 11、37、29、26 行，并保持 `template_icon_buttons_tests.rs` 的 `super::*` 可见测试入口、panel radius 测试导入与 glyph dispatch 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes icon button identity/geometry/style/surface subowner ownership scan 确认根 icon_buttons 文件不再拥有 Workbench icon-button recognition body、context/style helper bodies、paint/glyph rect helper bodies、surface command body、style selector imports 或 `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_icon_buttons.rs` 中 Workbench icon-button recognition gate、paint/context/style lookup orchestration、surface command ordering、glyph rect construction 与 glyph child-owner dispatch 下沉到 `template_icon_buttons/commands.rs`：根 `template_icon_buttons.rs` 从 52 行降为 20 行 module wiring/test-import entry；新增 `commands.rs` 为 37 行，并保持 `template_nodes/specialized.rs` 对 `push_icon_button_commands` 的原 root 导入路径以及 `template_icon_buttons_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes icon-button command sequencing ownership scan 确认根 icon_buttons 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、glyph child-owner call、surface command ordering 或 `push_icon_button_commands` body，仅保留 module wiring、test imports 与 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes axis-value field identity/geometry/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_axis_value_fields.rs` 中 Transform axis-value field recognition、text-input/component-role classification、compact rect geometry、field surface command emission、value fallback/text command emission 分别拆入 `template_axis_value_fields/identity.rs`、`geometry.rs`、`surface.rs` 和 `text.rs`：根 `template_axis_value_fields.rs` 从 130 行降为 38 行 sequencing entry；新增子文件分别为 45、13、27、48 行，并保持 `template_axis_value_fields_tests.rs` 的 `super::*` 可见测试入口和 `template_axis_value_field_style.rs` style owner 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes axis-value field identity/geometry/surface/text subowner ownership scan 确认根 axis_value_fields 文件不再拥有 Transform axis recognition body、text-input classifier、compact rect helper、surface/text command bodies、axis field constants、value fallback helper 或 `UiTextRunPaintStyle` import；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes axis-value field style color/background/border/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_axis_value_field_style.rs` 中 axis-value field palette constants、background state selection、border color/width selection 与 text color selection 分别拆入 `template_axis_value_field_style/colors.rs`、`background.rs`、`border.rs` 和 `text.rs`：根 `template_axis_value_field_style.rs` 从 65 行降为 11 行 structural export entry；新增子文件分别为 7、16、32、18 行，并保持 `template_axis_value_fields/surface.rs`、`text.rs` 与 `template_axis_value_fields_tests.rs` 对原 style root 的导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes axis-value field style color/background/border/text ownership scan 确认根 style 文件不再拥有 `TemplatePaneNodeData`/`PALETTE` imports、disabled/validation/value-color state bodies 或 `axis_field_*` function bodies，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-19 | 08 M3.S2 paint_template_nodes template-node image identity/geometry/command subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_node_images.rs` 中 image-source recognition、icon/icon-only classification、leading-icon/image/fitted rect geometry、tint/raster lookup 与 image-pixels command emission 分别拆入 `template_node_images/identity.rs`、`geometry.rs` 和 `command.rs`：根 `template_node_images.rs` 从 143 行降为 7 行 structural export entry；新增子文件分别为 13、68、69 行，并保持 `template_node_text.rs` 与 `template_nodes.rs` 的 root 导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes template-node image identity/geometry/command subowner ownership scan 确认根 image 文件不再拥有 image command body、icon/source classification bodies、leading/icon/fitted rect helper bodies、`ICON_TEXT_*` constants、raster/tint/image lookup imports、`profile_scope` 或 `HostPaintCommand::image_pixels` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes segmented-control geometry metrics/segmented/tabs subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_segmented_control_geometry.rs` 中 segmented/tab shared metrics、line-height helpers、segmented group/body/segment/divider/selected rect helpers、tab paint/underline/label rect helpers 分别拆入 `template_segmented_control_geometry/metrics.rs`、`segmented.rs` 和 `tabs.rs`：根 `template_segmented_control_geometry.rs` 从 137 行降为 12 行 structural export entry；新增子文件分别为 34、103、37 行，并保持 `template_segmented_controls.rs`、`template_segmented_controls/labels.rs`、`segments.rs`、`tabs.rs` 与 `template_segmented_controls_tests.rs` 的既有导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes segmented-control geometry metrics/segmented/tabs subowner ownership scan 确认根 geometry 文件不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、segmented/tab private metric constants、segmented rect helper bodies、tab rect helper bodies 或 inset helper body；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes segmented-control command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_segmented_controls.rs` 中 segmented-control recognition gate、empty-option guard、segmented control concrete paint dispatch 与 Workbench tab dispatch 下沉到 `template_segmented_controls/commands.rs`：根 `template_segmented_controls.rs` 从 51 行降为 32 行 module wiring/test-import entry；新增 `commands.rs` 为 31 行，并保持 `template_nodes/specialized.rs` 对 `push_segmented_control_commands` 的原 root 导入路径以及 `template_segmented_controls_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes segmented-control command sequencing ownership scan 确认根 `template_segmented_controls.rs` 不再拥有 `HostPaintCommand` import、production identity/options/segments/tabs imports 或 `push_segmented_control_commands` body，仅保留 module wiring、test imports 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes status-control geometry common/metrics/signal/chip/icon subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_status_control_geometry.rs` 中 status shared offset rect、font/radius/line-height metrics、status signal icon/text rect placement、chip text/chevron rect placement、icon-button glyph rect placement 分别拆入 `template_status_control_geometry/common.rs`、`metrics.rs`、`signals.rs`、`chips.rs` 和 `icons.rs`：根 `template_status_control_geometry.rs` 从 133 行降为 16 行 structural export entry；新增子文件分别为 13、11、91、28、8 行，并保持 `template_status_controls.rs`、`signals.rs`、`chips.rs`、`icons.rs` 与 `template_status_controls_tests.rs` 的既有导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes status-control geometry common/metrics/signal/chip/icon subowner ownership scan 确认根 status_control_geometry 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`StatusSignalKind` imports、status private metric constants、status offset/signal/chip/icon rect helper bodies、signal size helpers、`centered_rect`/status glyph size imports 或 test-only text-gap body；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes status-control command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_status_controls.rs` 中 Signal/Chip/Icon command dispatch 下沉到 `template_status_controls/commands.rs`：根 `template_status_controls.rs` 从 60 行降为 32 行 module wiring/test-import entry；新增 `commands.rs` 为 30 行，并保持 `paint_template_nodes` 对 `push_status_control_commands` 的原 root 导入路径以及 `template_status_controls_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes status-control command sequencing ownership scan 确认根 status_controls 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` production imports、status-control kind dispatch imports、signal/chip/icon painter imports、`match status_control_kind` body 或 `push_status_control_commands` function body，仅保留 module wiring、root re-export 和 test-only imports；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes selection-control geometry marks/metrics/radio/toggle subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_selection_control_geometry.rs` 中 selection/radio/toggle metric constants、leading mark 与 label-after-mark geometry、label-gap/centered-square helpers、radio dot sizing、toggle track/thumb placement 和 declared size fallback 分别拆入 `template_selection_control_geometry/metrics.rs`、`marks.rs`、`radio.rs` 和 `toggle.rs`：根 `template_selection_control_geometry.rs` 从 125 行降为 12 行 structural export entry；新增子文件分别为 14、62、12、66 行，并保持 `template_selection_controls.rs`、`checkbox.rs`、`radio.rs`、`toggle.rs` 与 `template_selection_controls_tests.rs` 的既有导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes selection-control geometry marks/metrics/radio/toggle subowner ownership scan 确认根 selection_control_geometry 文件不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、private selection/toggle metric constants、leading mark/label/toggle/radio geometry bodies、selection mark-size helper 或 toggle declared size fallback helpers；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes selection-control command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_selection_controls.rs` 中 Checkbox/Radio/Toggle command dispatch 下沉到 `template_selection_controls/commands.rs`：根 `template_selection_controls.rs` 从 63 行降为 35 行 module wiring/test-import entry；新增 `commands.rs` 为 30 行，并保持 `paint_template_nodes` 对 `push_selection_control_commands` 的原 root 导入路径以及 `template_selection_controls_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes selection-control command sequencing ownership scan 确认根 selection_controls 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` production imports、kind dispatch imports、checkbox/radio/toggle painter imports、`match selection_control_kind` body 或 `push_selection_control_commands` function body，仅保留 module wiring、root re-export 和 test-only imports；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes tree-row geometry metrics/rows/actions/labels subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_tree_row_geometry.rs` 中 tree-row font/radius/guide/action/spacing metrics、line-height calculation、disclosure/icon rect placement、indent-guide x placement、right-side action rect placement 与 label rect placement 分别拆入 `template_tree_row_geometry/metrics.rs`、`rows.rs`、`actions.rs` 和 `labels.rs`：根 `template_tree_row_geometry.rs` 从 69 行降为 9 行 structural export entry；新增子文件分别为 19、39、18、17 行，并保持 `template_tree_rows.rs`、`surface.rs`、`labels.rs`、`actions.rs` 与 `template_tree_rows_tests.rs` 的既有导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tree-row geometry metrics/rows/actions/labels subowner ownership scan 确认根 tree_row_geometry 文件不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、private tree metric constants、disclosure/icon/action/guide/label helper bodies 或 line-height body；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes tree-row command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_tree_rows.rs` 中 TreeRow recognition gating、surface/indent/disclosure/icon/label/action command ordering、style-color lookup 与 glyph fan-out 下沉到 `template_tree_rows/commands.rs`：根 `template_tree_rows.rs` 从 57 行降为 14 行 module wiring/test-import entry；新增 `commands.rs` 为 49 行，并保持 `template_nodes/specialized.rs` 对 `push_tree_row_commands` 的原 root 导入路径以及 `template_tree_rows_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tree-row command sequencing subowner ownership scan 确认根 `template_tree_rows.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、glyph geometry imports、surface/label/action child-owner imports、style lookup body、TreeRow gating body 或 command sequencing body，仅保留 module wiring、test imports 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes list-row identity/style/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_list_rows.rs` 中 ListRow recognition、shared style/color accessors、row surface command emission、label layout/text command emission 分别拆入 `template_list_rows/identity.rs`、`style.rs`、`surface.rs` 和 `text.rs`：根 `template_list_rows.rs` 从 124 行降为 42 行 sequencing entry；新增子文件分别为 9、26、28、40 行，并保持 `template_list_rows_tests.rs` 的 `super::*` 可见测试入口和 `template_list_row_glyphs.rs` adornment owner 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes list-row identity/style/surface/text subowner ownership scan 确认根 list_rows 文件不再拥有 component-family recognition body、style selector imports、text command body、surface command body、list-row constants、`UiTextRunPaintStyle` import 或 direct `HostPaintCommand::text/quad` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes shell-panel identity/surface/separator subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_shell_panels.rs` 中 Workbench shell-panel control-id recognition、chrome style selection、surface fill command emission 与 separator dispatch 分别拆入 `template_shell_panels/identity.rs` 和 `surface.rs`，保留既有 `template_shell_panels/separators.rs` 作为 separator owner：根 `template_shell_panels.rs` 从 92 行降为 34 行 sequencing entry；新增子文件分别为 44、30 行，并保持 `template_shell_panels_tests.rs` 的 `super::*` 可见测试入口和 `ShellPanelKind` test import 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes shell-panel identity/surface/separator subowner ownership scan 确认根 shell_panels 文件不再拥有 shell-panel kind mapping、chrome style selector import、surface fill command body、direct `HostPaintCommand::quad` emission 或 concrete Workbench shell-panel ID match arms；scoped whitespace scan 覆盖 root、新增子文件和 separator owner 并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes Material state-layer state/ripple subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_state_layer.rs` 中 Material state-layer overlay color/opacity selection 与 ripple enablement/pressed-origin geometry/clip/radius/command emission 分别拆入 `material_state_layer/state.rs` 和 `ripple.rs`：根 `material_state_layer.rs` 从 83 行降为 45 行 sequencing entry；新增子文件分别为 43、59 行，并通过 root 的 test-only import 保持 `material_state_layer_tests.rs` 的 `super::*` ripple helper 可见入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes Material state-layer state/ripple subowner ownership scan 确认根 material_state_layer 文件不再拥有 state opacity/color body、ripple rect/clip/diameter/radius body、interaction opacity constants、paint-theme/intersect/style-disabled imports 或 direct ripple `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes generic template-node surface eligibility/command subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_node_surface.rs` 中 generic surface fallback 的 frame-only filtering、surface/border eligibility、elevation shadow、border/surface quad emission 与 Material state-layer/ripple dispatch 分别拆入 `template_node_surface/eligibility.rs` 和 `commands.rs`：根 `template_node_surface.rs` 从 90 行降为 26 行 fallback gating entry；新增子文件分别为 46、54 行，并保持 `template_nodes.rs` 通过 root re-export 消费 `is_frame_only_node` 和 `push_template_surface_fallback_commands` 的导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes generic template-node surface eligibility/command subowner ownership scan 确认根 template_node_surface 文件不再拥有 frame-only matcher、surface/border eligibility bodies、elevation shadow constant/body、template_style color/border/elevation imports、paint-theme import、state-layer command import 或 direct `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes generic template-node text eligibility/geometry/metric/command subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_node_text.rs` 中 fallback text skip rules、text rect geometry、font metrics 与 retained text command emission 分别拆入 `template_node_text/eligibility.rs`、`geometry.rs`、`metrics.rs` 和 `command.rs`：根 `template_node_text.rs` 从 77 行降为 43 行 sequencing entry；新增子文件分别为 14、30、16、31 行，并保持 `template_nodes.rs` 的 `push_template_text_fallback_command` 调用入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes generic template-node text eligibility/geometry/metric/command subowner ownership scan 确认根 template_node_text 文件不再拥有 text skip predicate、leading-icon geometry、text inset/font constants、font-size clamp body、`UiTextRunPaintStyle` import 或 direct `HostPaintCommand::text` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes generic template-node label focus/property/value subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_node_labels.rs` 中 focused text-input override、PropertyRow label/value composition 与 text-input-aware fallback value ordering 分别拆入 `template_node_labels/focus.rs`、`property.rs` 和 `values.rs`：根 `template_node_labels.rs` 从 63 行降为 26 行 label precedence entry；新增子文件分别为 10、15、33 行，并保持 `template_node_labels_tests.rs` 的 `super::*` 可见 `template_node_label` 入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes generic template-node label focus/property/value subowner ownership scan 确认根 template_node_labels 文件不再拥有 focused value override body、PropertyRow label composition body、text-input classifier、fallback first-non-empty ordering helper 或 direct fallback value array construction；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes field-stepper command/metrics/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_field_stepper.rs` 中 input-stepper width/divider/arrow segment constants、divider/glyph command sequencing 与 shared segment scaling 分别拆入 `template_field_stepper/metrics.rs`、`command.rs` 和 `segments.rs`：根 `template_field_stepper.rs` 从 82 行降为 5 行 structural export entry；新增子文件分别为 9、46、36 行，并保持 `template_fields.rs` 的 `push_field_stepper` 导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes field-stepper command/metrics/segment subowner ownership scan 确认根 template_field_stepper 文件不再拥有 stepper constants、divider command body、glyph rect construction、segment loop、scaled-rect helper 或 direct `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X primitive kind/shared subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/mui_x_primitives.rs` 中 TreeView/DataGrid/Picker/Chart/Chat primitive kind selection 与 shared role/radius/background/quad/component-variant helpers 分别拆入 `mui_x_primitives/kind.rs` 和 `shared.rs`：根 `mui_x_primitives.rs` 从 87 行降为 43 行 dispatch entry；新增子文件分别为 26、48 行，并保持各 MUI X child module 通过 root re-export 使用 `super::matches_any_role` / `push_quad` / `node_radius` 等共享 helper 的调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes MUI X primitive kind/shared ownership scan 确认根 mui_x_primitives 文件不再拥有 kind enum/body、role matching helper、quad helper、radius/background/component-variant helper、paint-theme/style-color imports 或 direct `HostPaintCommand::quad` emission；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes template-node dispatch geometry/ordering/specialized/fallback subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_nodes.rs` 中 template-node frame translation/clip/visibility gating、paint order/transition opacity、specialized component painter dispatch、generic fallback surface/image/text/popup sequencing 分别拆入 `template_nodes/geometry.rs`、`ordering.rs`、`specialized.rs` 和 `fallback.rs`：根 `template_nodes.rs` 从 273 行降为 77 行 top-level sequencing entry；新增子文件分别为 29、17、82、64 行，并保持 `template_node_pipeline.rs` 的 `push_template_node_commands` 调用入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes template-node dispatch geometry/ordering/specialized/fallback ownership scan 确认根 template_nodes 文件不再拥有 frame translation/clip/intersection body、order stride constants、transition opacity body、specialized painter dispatch list、fallback Material/MUI X surface/image/text/popup sequencing 或 direct specialized imports；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层大 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes template-node command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_nodes.rs` 中 per-node frame/clip gate、frame-only suppression、paint order/transition opacity、specialized painter handoff 与 generic fallback handoff 拆入 `template_nodes/commands.rs`：根 `template_nodes.rs` 从 77 行降为 34 行 module wiring/test-entry export file；新增 `commands.rs` 为 47 行，并保持 `template_node_pipeline.rs` 对 `push_template_node_commands` 的原 root 导入路径以及 `paint_template_nodes_for_test*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes template-node command sequencing ownership scan 确认根 `template_nodes.rs` 不再拥有 `FrameRect`/`HostTextInputFocusData`/`HostPaintCommand` imports、frame/clip gate、frame-only suppression、order/opacity calculation、specialized/fallback handoff 或 `push_template_node_commands` body，仅保留 module wiring、test entry re-exports 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes template-node pipeline draw/clip/test-support subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_node_pipeline.rs` 中 active damage/template clip resolution、template-node traversal/command collection/command replay submission 与 test-only RGBA helper entry points 分别拆入 `template_node_pipeline/clip.rs`、`draw.rs` 和 `test_support.rs`：根 `template_node_pipeline.rs` 从 90 行降为 23 行 module wiring/test-entry file；新增子文件分别为 14、51、30 行，并保持 `paint_template_nodes` root 对 `draw_template_nodes(...)`、`has_template_nodes(...)` 和 `paint_template_nodes_for_test*` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes template-node pipeline draw/clip/test-support ownership scan 确认根 `template_node_pipeline.rs` 不再拥有 command replay body、effective clip body、template-node row iteration、draw command imports 或 RGBA helper body，仅保留 module wiring、public pipeline/test helper re-exports 与 external test-module hook；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes sprite-atlas key/discovery/cache/image/uv subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/sprite_atlas.rs` 中 retained source-key entry-name normalization、project/library atlas manifest discovery、atlas manifest cache key/metadata invalidation/TOML parse/validation、atlas texture path/RGBA loading 与 runtime UV conversion 分别拆入 `sprite_atlas/keys.rs`、`discovery.rs`、`cache.rs`、`image.rs` 和 `uv.rs`：根 `sprite_atlas.rs` 从 159 行降为 43 行 resolver entry；新增子文件分别为 7、43、57、18、10 行，并保持 `sprite_atlas_tests.rs` 的 `super::*` 可见测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes sprite-atlas key/discovery/cache/image/uv ownership scan 确认根 sprite_atlas 文件不再拥有 cache key struct/body、manifest discovery body、manifest load/cache body、texture path/RGBA load body、UV conversion body、`BTreeMap`/`Mutex`/`OnceLock`/`UNIX_EPOCH` imports 或 runtime atlas validation imports；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes visual-asset model/target/tint/key/retained/template/asset subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/visual_assets.rs` 中 `HostPaintImagePixels` 数据模型、`RasterTargetSize`/目标尺寸、icon tint/state rules、visual/template/retained cache keys、retained image pixel conversion、template image source/preview/fallback routing、`UiVisualAssetRef` runtime asset lookup 分别拆入 `visual_assets/pixels.rs`、`target.rs`、`tint.rs`、`keys.rs`、`retained.rs`、`template.rs` 和 `asset.rs`：根 `visual_assets.rs` 从 235 行降为 25 行 structural export entry；新增子文件分别为 25、26、45、22、21、38、41 行，并保持 `visual_assets_tests.rs` 的 `super::*` 可见测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes visual-asset model/target/tint/key/retained/template/asset ownership scan 确认根 visual_assets 文件不再拥有 hasher/import/data-struct body、raster target body、tint body、retained/template/runtime asset load body、cache-key body 或 retained-image conversion body，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene command dispatch subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene.rs` 中 viewport scene-kind command dispatch、pixel-aligned rect gate、scene child painter routing、base-surface sequencing 与 overlay/axis/floor/light/prop/architecture/surface command fan-out 拆入 `template_viewport_scene/commands.rs`：根 `template_viewport_scene.rs` 从 144 行降为 7 行 structural export entry；新增 `commands.rs` 为 139 行，并保持既有 `identity.rs` 127 行、`geometry.rs` 9 行和 `template_viewport_scene_tests.rs` 的 `super::*` 可见测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes viewport-scene command dispatch ownership scan 确认根 template_viewport_scene 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、child painter imports、`ViewportSceneKind` match body、pixel-aligned guard 或 concrete command fan-out；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes slider command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_sliders.rs` 中 Workbench slider command sequencing、geometry gate、label/value/track/thumb/tick/range-min ordering 与 style/geometry helper orchestration 拆入 `template_sliders/commands.rs`：根 `template_sliders.rs` 从 113 行降为 11 行 structural export entry；新增 `commands.rs` 为 106 行，并保持既有 `identity.rs` 12 行、`track.rs` 77 行、`thumb.rs` 42 行、`text.rs` 112 行和 `template_sliders_tests.rs` 的 `super::*` 可见测试入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes slider command sequencing ownership scan 确认根 template_sliders 文件不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、template_slider_geometry imports、slider label/value/track/thumb/tick/range-min command body 或 `push_slider_commands` body，仅保留 module wiring、public command re-export 与 test-only identity imports；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes template style-color role/button/constant subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_style_color.rs` 中 MUI overlay/on-dark constants、`UiStyleColor`/material role resolution、typed button container/tone/primary-contained rules 分别拆入 `template_style_color/constants.rs`、`roles.rs` 和 `buttons.rs`：根 `template_style_color.rs` 从 96 行降为 8 行 structural export entry；新增子文件分别为 3、36、60 行，并保持 `resolved_style_color`、`typed_button_variant_background`、`typed_button_tone_color`、`is_primary_contained_button` 与 MUI 常量的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes template style-color role/button/constant ownership scan 确认根 template_style_color 文件不再拥有 `TemplatePaneNodeData`/`PALETTE`/`ButtonColor`/`ButtonVariant`/`UiStyleColor` imports、MUI constant bodies、style-color match body、button color helpers 或 material role body，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button glyph-kind enum/mapping subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_kind.rs` 中 `IconButtonGlyphKind` enum 与 Workbench icon-button control/icon keyword mapping 分别拆入 `template_icon_button_glyph_kind/kind.rs` 和 `mapping.rs`：根 `template_icon_button_glyph_kind.rs` 从 88 行降为 4 行 structural export entry；新增子文件分别为 28、61 行，并保持 `template_icon_button_glyphs.rs` 对 `icon_button_glyph_kind(...)` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes icon-button glyph-kind enum/mapping ownership scan 确认根 glyph-kind 文件不再拥有 `TemplatePaneNodeData` import、enum body、keyword `format!`/`contains(...)` mapping body 或具体 icon kind match branches，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command conversion command/payload subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/render_command_conversion.rs` 中 runtime `UiRenderCommand` traversal、clip inheritance、paint-element visibility gate、empty group/brush/image/text payload dispatch、brush border/fill orchestration 与 text/image child owner fan-out 拆入 `render_command_conversion/commands.rs`：根 `render_command_conversion.rs` 从 86 行降为 6 行 structural export entry；新增 `commands.rs` 为 82 行，并保持 `render_commands.rs` 对 `runtime_render_commands_to_host(...)` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes render-command conversion command/payload ownership scan 确认根 render_command_conversion 文件不再拥有 `UiPaintElement`/`UiPaintPayload`/`UiRenderCommand`/`FrameRect`/`HostPaintCommand` imports、visibility gate、payload match body、brush/image/text child dispatch 或 `push_runtime_*` bodies，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes tooltip command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_tooltips.rs` 中 Workbench tooltip recognition gate、pixel-aligned rect gate、style selection、bubble/arrow/icon metrics、surface/text/arrow/info-icon command ordering 与 glyph fan-out 拆入 `template_tooltips/commands.rs`：根 `template_tooltips.rs` 从 78 行降为 15 行 structural export entry；新增 `commands.rs` 为 66 行，并保持既有 `identity.rs`、`layout.rs`、`surface.rs`、`text.rs` 和 `template_tooltips_tests.rs` 的 `super::*` 可见测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes tooltip command sequencing ownership scan 确认根 template_tooltips 文件不再拥有 `FrameRect`/`HostPaintCommand` imports、style selector imports、glyph dispatch imports、pixel-aligned gate 或 `push_tooltip_commands` body，仅保留 module wiring、command re-export 与 test-only imports；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes button command sequencing subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_buttons.rs` 中 Workbench button recognition gating、pixel-aligned paint-rect validation、kind/opacity orchestration、surface command ordering 与 content/glyph child-owner dispatch 拆入 `template_buttons/commands.rs`：根 `template_buttons.rs` 从 48 行降为 29 行 module wiring/test-import entry；新增 `commands.rs` 为 30 行，并保持 `template_nodes/specialized.rs` 对 `push_button_commands` 的原 root 导入路径以及 `template_buttons_tests.rs` 的 `super::*` 测试入口。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes button command sequencing ownership scan 确认根 `template_buttons.rs` 不再拥有 `HostPaintCommand` import、surface/content child-owner imports、button paint-rect gate、kind/opacity orchestration 或 `push_button_commands` body，仅保留 module wiring、test imports 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract workbench-context-menu request/provider/classifier/path/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/workbench_context_menu.rs` 中 context-menu request assembly、scene/module/generic provider catalog、hit classifier predicates、target value/path segment normalization 和 inline regressions 分别拆入 `workbench_context_menu/request.rs`、`provider.rs`、`classifier.rs`、`path.rs` 和 `workbench_context_menu_tests.rs`：根 `workbench_context_menu.rs` 从 201 行降为 10 行 structural re-export/test-hook entry；新增子文件分别为 33、51、40、28 行，外置测试文件为 55 行，并保持 native pointer secondary-press dispatch 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host_contract workbench-context-menu request/provider/classifier/path/test ownership scan 确认根 `workbench_context_menu.rs` 不再拥有 `WorkbenchContextMenuRequestData`/`TemplateNodePointerHit`/`SharedString` imports、request assembly body、provider enum/menu rows、scene/module/generic classifier predicates、target value/path normalization 或 inline tests，仅保留 module wiring、request re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract template-popup-layout dropdown/template/row/metric/bounds/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/template_popup_layout.rs` 中 dropdown option popup placement、bounded popup flip/clamp、dropdown row frame、projected DropdownPopup frame/row behavior、menu item row geometry、shared row-gap/min-row-height metrics、bounds validation 和 inline regressions 分别拆入 `template_popup_layout/dropdown.rs`、`template.rs`、`rows.rs`、`metrics.rs`、`bounds.rs` 和 `template_popup_layout_tests.rs`：根 `template_popup_layout.rs` 从 229 行降为 19 行 structural re-export/test-hook entry；新增子文件分别为 75、44、16、13、10 行，外置测试文件为 77 行，并保持 native popup dismiss 与 popup hit-test 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host_contract template-popup-layout dropdown/template/row/metric/bounds/test ownership scan 确认根 `template_popup_layout.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、popup metric constants、dropdown placement/flip/clamp bodies、row frame bodies、projected popup classifier、bounds validator 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract native-input keyboard/ime/wheel/key/modifier subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/native_input_translation.rs` 中 winit keyboard event 转 shared input、IME event 转 shared input、mouse wheel 转 pointer input、legacy key-code/scan-code/name helpers 与 modifier conversion 分别拆入 `native_input_translation/keyboard.rs`、`ime.rs`、`wheel.rs`、`keys.rs` 和 `modifiers.rs`：根 `native_input_translation.rs` 从 190 行降为 9 行 structural re-export entry；新增子文件分别为 31、36、37、90、13 行，并保持 `host_contract/mod.rs` test-only re-export 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host_contract native-input keyboard/ime/wheel/key/modifier ownership scan 确认根 `native_input_translation.rs` 不再拥有 winit/runtime input imports、keyboard/IME/wheel conversion bodies、pixel scroll scale、modifier conversion、keyboard state/name/key-code/scan-code helpers 或 byte-index clamp helper，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract template-activation route/asset/dispatch/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/template_activation_semantics.rs` 中 primary activation route enum/classifier、asset pane activation source/change classification、callback fan-out dispatch、action/control-id fallback helper 和 inline regressions 分别拆入 `template_activation_semantics/route.rs`、`asset.rs`、`dispatch.rs`、`helpers.rs` 和 `template_activation_semantics_tests.rs`：根 `template_activation_semantics.rs` 从 264 行降为 10 行 structural re-export/test-hook entry；新增子文件分别为 40、73、46、10 行，外置测试文件为 108 行，并保持 native pointer primary-press dispatch 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host_contract template-activation route/asset/dispatch/test ownership scan 确认根 `template_activation_semantics.rs` 不再拥有 `PaneSurfaceHostContext`/`TemplateNodePointerHit`/callback id imports、route enum、asset activation structs、route classifier、asset source/change classifier、callback fan-out body、action/control-id helper 或 inline tests，仅保留 module wiring、dispatch re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract template-component-family enum/classify/role/layout/workbench/visual/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/template_component_family.rs` 中 `TemplateComponentFamily` enum/string label、declared role/host role match tables、category/layout fallback、Workbench control-id heuristics、semantic classification helpers、Workbench visual-language detection 和 inline regressions 分别拆入 `template_component_family/family.rs`、`roles.rs`、`layout.rs`、`workbench.rs`、`classify.rs`、`visual_language.rs` 和 `template_component_family_tests.rs`：根 `template_component_family.rs` 从 291 行降为 16 行 structural re-export/test-hook entry；新增子文件分别为 52、67、16、51、36、10 行，外置测试文件为 66 行，并保持 activation/input semantics 与 surface hit-test 的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host_contract template-component-family enum/classify/role/layout/workbench/visual/test ownership scan 确认根 `template_component_family.rs` 不再拥有 `TemplatePaneNodeData` import、enum body、`as_str` body、role/host-role/layout/workbench match bodies、semantic helper bodies、visual-language body 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract globals state/ui-context/pane-context/macro subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/globals.rs` 中 shared state container/global trait、host-level context state+callback API、pane-level context state+callback API 与 callback registration/invocation macro 分别拆入 `globals/state.rs`、`ui_context.rs`、`pane_context.rs` 和 `callback_methods.rs`，保留既有 `globals/callbacks.rs` 的 callback DTO/storage owner：根 `globals.rs` 从 320 行降为 10 行 structural re-export entry；新增/保留子文件分别为 73、84、161、14、98 行，并保持 `window.rs`、`host_contract/mod.rs` 与外部调用方的原 root 导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host_contract globals state/ui-context/pane-context/macro ownership scan 确认根 `globals.rs` 不再拥有 `Rc`/`RefCell`/state data imports、`HostContractState` body、`HostContractGlobal` trait body、`UiHostContext`/`PaneSurfaceHostContext` bodies、callback macro body 或 concrete callback/state mutation methods，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root、新增子文件、已有 callbacks 文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-text draw/clip/font/raster/blend/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_text.rs` 中 retained-host text entry/layout/recording、active/explicit clip resolution、embedded fallback font、glyph raster cache、pixel alpha blending 和 inline regressions 分别拆入 `paint_text/draw.rs`、`clip.rs`、`font.rs`、`raster.rs`、`blend.rs` 和 `paint_text_tests.rs`：根 `paint_text.rs` 从 309 行降为 12 行 structural export/test-hook entry；新增子文件分别为 152、15、16、62、30 行，外置测试文件为 54 行，并保持 chrome command replay、template render commands 与 primitive text markers 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-text draw/clip/font/raster/blend/test ownership scan 确认根 `paint_text.rs` 不再拥有 `fontdue`/cache/font/frame/clip/geometry imports、font bytes、default metrics、draw/layout bodies、font loader、raster cache、pixel blend、clip body 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract profiling-artifacts export/environment/schema subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/profiling_artifacts.rs` 中 artifact export workflow、profiling environment/path resolution、serialized profile DTO schema 与 test-only import surface 分别拆入 `profiling_artifacts/export.rs`、`environment.rs`、`schema.rs` 和 `profiling_artifacts/tests.rs` 显式导入：根 `profiling_artifacts.rs` 从 186 行降为 13 行 structural export/test-hook entry；新增子文件分别为 42、39、109 行，并保持 window event-loop profiling export 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract profiling-artifacts export/environment/schema ownership scan 确认根 `profiling_artifacts.rs` 不再拥有 `fs`/`PathBuf`/`serde::Serialize`/presentation/screenshot imports、artifact file constants、export function body、profile DTO structs、environment helpers、path sanitizer 或 test-only data imports，仅保留 module wiring、export re-export、schema re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract diagnostics invalidation/overlay/refresh/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/diagnostics.rs` 中 invalidation counter snapshot、startup overlay text、refresh/present/FPS aggregation、overlay formatting 和 inline regressions 分别拆入 `diagnostics/invalidation.rs`、`overlay.rs`、`refresh.rs` 和 `diagnostics_tests.rs`：根 `diagnostics.rs` 从 150 行降为 9 行 structural re-export/test-hook entry；新增子文件分别为 7、2、102 行，外置测试文件为 55 行，并保持 presenter/window diagnostics 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract diagnostics invalidation/overlay/refresh/test ownership scan 确认根 `diagnostics.rs` 不再拥有 `Instant` import、startup overlay string body、diagnostic struct bodies、refresh impl/default/overlay formatting bodies 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-debug-reflector-overlay color/draw/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_debug_reflector_overlay.rs` 中 debug overlay palette/alpha rules、primitive draw sequencing、clip/translation/label emission 和 inline regression 分别拆入 `paint_debug_reflector_overlay/colors.rs`、`draw.rs` 和 `paint_debug_reflector_overlay_tests.rs`：根 `paint_debug_reflector_overlay.rs` 从 142 行降为 6 行 structural re-export/test-hook entry；新增子文件分别为 33、65 行，外置测试文件为 48 行，并保持 native pane diagnostics painter 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-debug-reflector-overlay color/draw/test ownership scan 确认根 `paint_debug_reflector_overlay.rs` 不再拥有 `FrameRect`/`HostRgbaFrame`/primitive/geometry imports、debug overlay color constants、overlay draw body、primitive draw match、solid border alpha helper 或 inline tests，仅保留 module wiring、draw re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-diagnostics marker/top-bar/union/visibility/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_diagnostics.rs` 中 debug refresh overlay marker geometry、presentation top-bar selection/fallback、diagnostic frame union、visible-frame predicate 和 inline regressions 分别拆入 `paint_diagnostics/marker.rs`、`top_bar.rs`、`union.rs`、`visibility.rs` 和 `paint_diagnostics_tests.rs`：根 `paint_diagnostics.rs` 从 118 行降为 10 行 structural re-export/test-hook entry；新增子文件分别为 26、37、16、9 行，外置测试文件为 33 行，并保持 Workbench root frame 与 skeleton diagnostic overlay 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-diagnostics marker/top-bar/union/visibility/test ownership scan 确认根 `paint_diagnostics.rs` 不再拥有 `FrameRect`/layout/presentation imports、marker metrics constants、refresh marker geometry body、top-bar selection/fallback body、union body、visible-frame predicate 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件、测试文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract native-popup-dismiss dispatch/target subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/native_popup_dismiss.rs` 中 outside-primary-press popup cancel dispatch、active popup bounds/damage resolution、pane callback invocation、hover clear、dismiss target DTO/containment、reverse-order active target scan 和 per-node dropdown/menu target construction 分别拆入 `native_popup_dismiss/dispatch.rs` 和 `target.rs`：根 `native_popup_dismiss.rs` 从 101 行降为 3 行 structural re-export entry；新增子文件分别为 35、70 行，并保持 native pointer button dispatch 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract native-popup-dismiss dispatch/target ownership scan 确认根 `native_popup_dismiss.rs` 不再拥有 presentation/template/global/redraw imports、outside-click cancel body、popup target struct/impl、active-target scan、per-node popup geometry construction 或 damage union behavior，仅保留 module wiring 与 dispatch re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-workbench command/test-frame subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_workbench.rs` 中 production Workbench command drawing adapter、test-only full-frame direct RGBA painter 和 test-only regional repaint helper 分别拆入 `paint_workbench/commands.rs` 和 `test_frame.rs`：根 `paint_workbench.rs` 从 99 行降为 8 行 structural re-export entry；新增子文件分别为 13、84 行，并保持 paint recording 与 command-stream replay parity tests 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-workbench command/test-frame ownership scan 确认根 `paint_workbench.rs` 不再拥有 presentation/frame/theme/impl imports、shell background constant、production draw body、test-only full-frame painter、regional repaint helper、clip intersection 或 profile scope body，仅保留 module wiring 与 production/test-only re-export；scoped whitespace scan 覆盖 root、新增子文件和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract template-geometry frame/bounds/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/template_geometry.rs` 中 template-node frame conversion、native-window/template-node popup bounds fallback、template-node visible union 和 inline regressions 分别拆入 `template_geometry/frame.rs`、`bounds.rs` 和 `template_geometry_tests.rs`：根 `template_geometry.rs` 从 90 行降为 7 行 structural re-export/test-hook entry；新增子文件分别为 11、37 行，外置测试文件为 45 行，并保持 native popup dismiss 与 popup layout 调用的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract template-geometry frame/bounds/test ownership scan 确认根 `template_geometry.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`ModelRc` imports、frame conversion body、popup bounds fallback body、template-node union body 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件和测试文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-geometry frame/rect/pixel subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_geometry.rs` 中 visible-frame/fallback/template-frame helpers、translation/intersection/inset rect math 和 `PixelRect` pixel-clip conversion 分别拆入 `paint_geometry/frame.rs`、`rect_ops.rs` 和 `pixel_rect.rs`：根 `paint_geometry.rs` 从 88 行降为 8 行 structural re-export entry；新增子文件分别为 30、44、33 行，并保持 paint primitives、paint text、debug overlay、Workbench impl 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-geometry frame/rect/pixel ownership scan 确认根 `paint_geometry.rs` 不再拥有 `FrameRect`/`TemplateNodeFrameData` imports、visible-frame/fallback/template conversion bodies、translated/intersect/inset bodies、`PixelRect` struct 或 pixel conversion impl，仅保留 module wiring 与 re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-close-prompt color/layout/button/draw subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_close_prompt.rs` 中 close prompt palette constants、details-area layout、button drawing 和 prompt overlay/dialog/details/action draw sequencing 分别拆入 `paint_close_prompt/colors.rs`、`layout.rs`、`button.rs` 和 `draw.rs`：根 `paint_close_prompt.rs` 从 83 行降为 5 行 structural re-export entry；新增子文件分别为 10、9、25、47 行，并保持 Workbench scene layer close-prompt painter 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-close-prompt color/layout/button/draw ownership scan 确认根 `paint_close_prompt.rs` 不再拥有 presentation/frame/primitive/theme imports、palette constants、prompt draw body、button draw body 或 details layout helper，仅保留 module wiring 与 draw re-export；scoped whitespace scan 覆盖 root 与新增子文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract template-input-semantics classification/target/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/template_input_semantics.rs` 中 pointer-hit text-input classification、component text-input semantics predicate、edit-target id resolution 和 inline regressions 分别拆入 `template_input_semantics/classification.rs`、`target.rs` 和 `template_input_semantics_tests.rs`：根 `template_input_semantics.rs` 从 67 行降为 7 行 structural re-export/test-hook entry；新增子文件分别为 15、14 行，外置测试文件为 38 行，并保持 native pointer text-input focus routing 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract template-input-semantics classification/target/test ownership scan 确认根 `template_input_semantics.rs` 不再拥有 `TemplateNodePointerHit`/`TemplateComponentFamily`/`SharedString` imports、text-input classification body、edit-target id body、component semantics helper 或 inline tests，仅保留 module wiring、re-export 和 external test hook；scoped whitespace scan 覆盖 root、新增子文件和测试文件并通过；scoped `git diff --check` 通过，仅提示既有 Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract window lifecycle/constants/metadata subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/window.rs` 中 default native host window constants、`UiHostWindow` construction/show/hide/run/handle/global/close/exit lifecycle 和 native input metadata/platform-error conversion 分别拆入 `window/constants.rs`、`lifecycle.rs` 和 `metadata.rs`：根 `window.rs` 从 98 行降为 22 行 structural module entry；新增子文件分别为 5、62、21 行，并保持 host handle re-export、event-loop input metadata sequencing 和 test-support deterministic window id 的原行为。 | `cargo fmt -p zircon_editor --check` 通过；host_contract window lifecycle/constants/metadata ownership scan 确认根 `window.rs` 不再拥有 `SystemTime`/winit event loop/platform primitive/dispatch metadata imports、default window constants、lifecycle method bodies、close/global/exit method bodies、native metadata body 或 platform error conversion，仅保留 shared state field、module wiring 和 handle re-export；scoped whitespace scan 覆盖 root、新增子文件、event-loop input/test-support 调整和同步文档并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-primitives root re-export split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_primitives.rs` 中 shape/line/text-marker draw wrapper bodies 收束为 child-owner direct re-export：`paint_primitives/shapes.rs` 直接暴露 rect/rounded/border draw entries，`lines.rs` 直接暴露 separator line draw entry，`text_markers.rs` 直接暴露 fallback text marker draw entries；根 `paint_primitives.rs` 从 90 行降为 19 行 structural module entry，并保持既有 host_contract-scoped调用路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-primitives root re-export ownership scan 确认根 `paint_primitives.rs` 不再拥有 `FrameRect`/`HostRgbaFrame` imports 或 rect/border/separator/text wrapper bodies，仅保留 module wiring、image re-export 和 child-owner draw re-export；scoped whitespace scan 覆盖 root、shape/line/text-marker 子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-frame frame-storage subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_frame.rs` 中 `HostRgbaFrame` storage fields、constructors、paint clip accessor/replacement、dimension/byte accessors 和 byte extraction 拆入 `paint_frame/frame.rs`：根 `paint_frame.rs` 从 80 行降为 11 行 structural module entry；新增 `frame.rs` 为 72 行，并保持 `HostRgbaFrame`、recorded command DTO 与 existing paint/replay callers 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-frame frame-storage ownership scan 确认根 `paint_frame.rs` 不再拥有 `FrameRect`/`HostPaintRecording` imports、`HostRgbaFrame` struct fields、constructor bodies、paint-clip/dimension/byte accessor bodies 或 byte extraction body，仅保留 module wiring 与 root re-export；scoped whitespace scan 覆盖 root、新增 frame 子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-workbench-impl legacy/style/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_workbench_renderer.rs` 中 legacy fallback draw orchestration、profiled legacy draw scopes、shared Workbench fallback palette constants 和 shared first-non-empty text helper 分别拆入 `paint_workbench_renderer/legacy.rs`、`style.rs` 和 `text.rs`：根 `paint_workbench_renderer.rs` 从 66 行降为 21 行 structural module/re-export entry；新增子文件分别为 35、14、7 行，并保持 `draw_legacy_workbench_window(...)`、profiled draw entry、componentized draw entry 和 child-owner palette/text 调用路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-workbench-impl legacy/style/text ownership scan 确认根 `paint_workbench_renderer.rs` 不再拥有 presentation/frame/theme imports、fallback palette constants、legacy draw bodies、profile scopes 或 text fallback body，仅保留 module wiring、public draw re-export、shared style re-export 和 text helper re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract frame-geometry hit/union/visibility/test subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/frame_geometry.rs` 中 visible-frame predicate、point containment、frame union/optional union 和 inline regressions 分别拆入 `frame_geometry/visibility.rs`、`hit.rs`、`union.rs` 和 `tests.rs`：根 `frame_geometry.rs` 从 63 行降为 8 行 structural module/re-export entry；新增子文件分别为 9、4、23、27 行，并保持 native pointer damage、redraw coalescing、popup dismiss 和 template bounds 的原 root helper 调用路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract frame-geometry hit/union/visibility/test ownership scan 确认根 `frame_geometry.rs` 不再拥有 `FrameRect` import、visible predicate body、contains body、union bodies 或 inline tests，仅保留 module wiring 与 child-owner re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-theme model/token subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_theme.rs` 中 `HostMaterialPalette` palette schema 与 concrete retained-host RGBA tokens 分别拆入 `paint_theme/model.rs` 和 `tokens.rs`：根 `paint_theme.rs` 从 57 行降为 4 行 structural module/re-export entry；新增子文件分别为 28、30 行，并保持 `PALETTE` 和 `HostMaterialPalette` 的原 root 导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-theme model/token ownership scan 确认根 `paint_theme.rs` 不再拥有 palette struct fields 或 concrete RGBA token body，仅保留 module wiring 与 child-owner re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract paint-recording record/damage subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_recording.rs` 中 record-only frame command capture workflow、shell background fill、Workbench command emission、frame bounds construction、damage clipping/intersection 和 visible-frame helper 分别拆入 `paint_recording/record.rs` 与 `damage.rs`：根 `paint_recording.rs` 从 52 行降为 3 行 structural re-export entry；新增子文件分别为 27、36 行，并保持 chrome command-stream extraction 的原 root 入口导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract paint-recording record/damage ownership scan 确认根 `paint_recording.rs` 不再拥有 data/frame/theme/workbench imports、shell-background constant、record body、frame bounds construction、damage intersection 或 visible-frame helper，仅保留 module wiring 与 record entry re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract native-pointer constants/state subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/native_pointer.rs` 中 shared host pointer event ids、viewport pointer event ids、viewport button ids 和 `NativePointerButtonState` 分别拆入 `native_pointer/constants.rs` 与 `state.rs`：根 `native_pointer.rs` 从 33 行降为 25 行 structural module/re-export entry；新增子文件分别为 11、5 行，并保持 button/move/scroll/drag-resize 子树的原常量和状态导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract native-pointer constants/state ownership scan 确认根 `native_pointer.rs` 不再拥有 raw pointer constants 或 `NativePointerButtonState` enum body，仅保留 module wiring、dispatch re-export、constants re-export 和 state re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 host_contract profiling-hit-routes dispatch subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/profiling_hit_routes.rs` 中 profiling route-kind dispatch table、presentation scene extraction 和 concrete route helper fan-out 拆入 `profiling_hit_routes/routes.rs`：根 `profiling_hit_routes.rs` 从 27 行降为 7 行 structural module/re-export entry；新增 `routes.rs` 为 23 行，并保持 profiling artifact geometry 的原 root hit-route 入口导入路径。 | `cargo fmt -p zircon_editor --check` 通过；host_contract profiling-hit-routes dispatch ownership scan 确认根 `profiling_hit_routes.rs` 不再拥有 `HostWindowPresentationData` import、route-kind match、scene extraction 或 route fan-out body，仅保留 route-family module wiring 与 dispatch re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive alert subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/alert.rs` 中 Alert root/slot gating、command ordering、identity classification、layout geometry、style/tone/color selection、surface/icon/message/action command emission 分别拆入 `alert/commands.rs`、`identity.rs`、`geometry.rs`、`style.rs`、`surface.rs`、`icon.rs`、`message.rs` 和 `action.rs`：根 `alert.rs` 从 295 行降为 9 行 module wiring/re-export entry；新增子文件分别为 48、38、94、143、24、60、46、95 行，并保持 Material primitive dispatch 对 `push_alert_primitive_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes Material primitive alert subtree ownership scan 确认根 `alert.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand`/`UiTextRunPaintStyle` imports、Alert metric/style constants、classification/geometry/style helper bodies 或 concrete surface/icon/message/action command emission bodies，仅保留 module wiring 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive avatar subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/avatar.rs` 中 Avatar root gating、command ordering、identity classification、geometry/style helpers、image/icon raster lookup、text/fallback glyph command emission 和 rounded alpha mask 分别拆入 `avatar/commands.rs`、`identity.rs`、`geometry.rs`、`style.rs`、`image.rs`、`text.rs`、`glyph.rs` 和 `mask.rs`：根 `avatar.rs` 从 279 行降为 9 行 module wiring/re-export entry；新增子文件分别为 78、7、76、45、68、40、45、54 行，并保持 Material primitive dispatch 对 `push_avatar_primitive_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes Material primitive avatar subtree ownership scan 确认根 `avatar.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand`/`HostPaintImagePixels`/`UiTextRunPaintStyle` imports、Avatar constants、classification/body/image/text/glyph/mask helper bodies 或 concrete command emission bodies，仅保留 module wiring 和 command entry re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive chip subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/chip.rs` 中 Chip root/slot gating、command ordering、identity/state classification、surface/avatar/icon/label/delete command emission 分别拆入 `chip/commands.rs`、`identity.rs`、`surface.rs`、`leading.rs`、`text.rs` 和 `delete.rs`，并把既有 geometry/style 子 owner 调整为从 identity owner 读取分类：根 `chip.rs` 从 263 行降为 9 行 module wiring/re-export entry；新增子文件分别为 40、42、23、67、36、71 行，既有 `geometry.rs` 为 198 行、`style.rs` 为 130 行，并保持 Material primitive dispatch 对 `push_chip_primitive_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes Material primitive chip subtree ownership scan 确认根 `chip.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand`/`UiTextRunPaintStyle` imports、Chip delete constants、classification helper bodies、label helper body、surface/avatar/icon/label/delete command emission bodies 或 component-variant token parser，仅保留 module wiring 和 command entry re-export；scoped whitespace scan 覆盖 root、新增/调整子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive divider subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/divider.rs` 中 Divider role recognition、orientation dispatch、horizontal/vertical line-gap sequencing、line quad emission、label text command emission 和 label fallback 分别拆入 `divider/identity.rs`、`commands.rs`、`horizontal.rs`、`vertical.rs`、`lines.rs` 和 `labels.rs`，既有 `geometry.rs`/`style.rs` 继续作为 geometry/color owner：根 `divider.rs` 从 256 行降为 9 行 module wiring/re-export entry；新增子文件分别为 13、26、48、55、53、76 行，并保持 Material primitive dispatch 对 `push_divider_primitive_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；paint_template_nodes Material primitive divider subtree ownership scan 确认根 `divider.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand`/`UiTextRunPaintStyle` imports、role matcher、orientation/line/label sequencing bodies、quad/text command emission bodies 或 divider label helper，仅保留 module wiring 和 command entry re-export；scoped whitespace scan 覆盖 root、新增/既有子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 Rust/docs LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 native_keyboard target discovery/model/row subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/native_keyboard/target.rs` 中 active popup discovery、option row construction、menu row construction、target/row DTO behavior、current row selection 和 text-search helpers 分别拆入 `target/discovery.rs`、`options.rs`、`menu.rs`、`model.rs`、`selection.rs` 和 `search.rs`：根 `target.rs` 从 267 行降为 8 行 module wiring/re-export entry；新增子文件分别为 55、72、35、70、54、12 行，并保持 dispatch 对 `active_popup_keyboard_target_for_ui`、`PopupKeyboardTarget`、`PopupKeyboardRow` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；native keyboard target discovery/model/row ownership scan 确认根 `target.rs` 不再拥有 `UiHostWindow`/presentation/template geometry/popup layout/model imports、target/row struct bodies、active popup scan、option/menu row construction、current-row resolution 或 text-search helper bodies，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench button style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_button.rs` 中 button kind/style DTO、palette tokens、normal/hover/pressed/focused/unavailable state construction、declared style-color overrides、add-component specialization、brightness scaling 和 inline loading regression 分别拆入 `workbench_button/model.rs`、`palette.rs`、`states.rs`、`selection.rs`、`brightness.rs` 和 `tests.rs`：根 `workbench_button.rs` 从 258 行降为 15 行 structural export entry；新增子文件分别为 17、23、136、45、29、28 行，并保持 style selector root 对 `select_workbench_button_style`、`WorkbenchButtonKind`、`WorkbenchButtonStyle` 和测试色 token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench button style-selector ownership scan 确认根 `workbench_button.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiStyleColor` imports、button DTO bodies、palette constants、state-style construction bodies、declared color helper、brightness scaling helpers 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench chrome style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_chrome.rs` 中 chrome kind/style DTO、shell chrome palette tokens、normal/interactive fill selection、separator state colors、style assembly 和 inline normal/loading/active regressions 分别拆入 `workbench_chrome/model.rs`、`palette.rs`、`fill.rs`、`separators.rs`、`selection.rs` 和 `tests.rs`：根 `workbench_chrome.rs` 从 189 行降为 19 行 structural export entry；新增子文件分别为 27、19、51、19、24、65 行，并保持 style selector root 对 `select_workbench_chrome_style`、`WorkbenchChromeKind`、`WorkbenchChromeStyle` 和 test-visible chrome token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench chrome style-selector ownership scan 确认根 `workbench_chrome.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiPainter*` imports、chrome DTO bodies、palette constants、fill/separator helper bodies、style assembly body 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench dropdown style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_dropdown.rs` 中 dropdown style DTO、surface/border/text/chevron palette tokens、surface/border/text/chevron color selection、declared style-color helpers、label brightness scaling、unavailable-state helper 和 inline loading regression 分别拆入 `workbench_dropdown/model.rs`、`palette.rs`、`colors.rs`、`brightness.rs`、`selection.rs`、`state.rs` 和 `tests.rs`：根 `workbench_dropdown.rs` 从 211 行降为 16 行 structural export entry；新增子文件分别为 11、12、112、35、21、8、31 行，并保持 style selector root 对 `select_workbench_dropdown_style`、`WorkbenchDropdownStyle` 和 dropdown token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench dropdown style-selector ownership scan 确认根 `workbench_dropdown.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`Color`/`UiStyleColor`/`UiPainter*` imports、dropdown DTO body、palette constants、surface/border/text/chevron helper bodies、declared color helpers、brightness helpers 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench selection-control style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_selection_control.rs` 中 selection kind/style DTO、checkbox/radio/toggle palette tokens、surface/border state selection、thumb/accent/text/label color rules、declared style-color helpers、hot/unavailable/family state helpers 和 inline loading regression 分别拆入 `workbench_selection_control/model.rs`、`palette.rs`、`selection.rs`、`colors.rs`、`state.rs` 和 `tests.rs`：根 `workbench_selection_control.rs` 从 253 行降为 17 行 structural export entry；新增子文件分别为 19、12、106、72、30、37 行，并保持 style selector root 对 `select_workbench_selection_control_style`、`WorkbenchSelectionControlKind`、`WorkbenchSelectionControlStyle` 和测试色 token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench selection-control style-selector ownership scan 确认根 `workbench_selection_control.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiStyleColor`/`UiPainter*` imports、selection DTO bodies、palette constants、surface/border/color/state helper bodies、declared style-color helpers 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench icon-button style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_icon_button.rs` 中 icon-button context/style DTO、toolbar/rail/panel palette tokens、background/border/radius/glyph style selection、declared style-color helpers、danger-icon classification、unavailable-state helper 和 inline loading regression 分别拆入 `workbench_icon_button/model.rs`、`palette.rs`、`selection.rs`、`state.rs` 和 `tests.rs`：根 `workbench_icon_button.rs` 从 258 行降为 13 行 structural export entry；新增子文件分别为 20、8、190、8、41 行，并保持 style selector root 对 `select_workbench_icon_button_style`、`WorkbenchIconButtonContext`、`WorkbenchIconButtonStyle` 和 panel radius token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench icon-button style-selector ownership scan 确认根 `workbench_icon_button.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiPainter*` imports、icon-button DTO bodies、palette constants、style selection bodies、declared color helpers、danger classifier、unavailable helper 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench popup-row style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_popup_row.rs` 中 popup row semantic state DTO、painter-state projection、marked helper、style DTO、danger text token、background/selection-mark/text/shortcut/adornment rules、hot/unavailable helpers 和 inline regressions 分别拆入 `workbench_popup_row/model.rs`、`palette.rs`、`selection.rs`、`state.rs` 和 `tests.rs`：根 `workbench_popup_row.rs` 从 202 行降为 13 行 structural export entry；新增子文件分别为 50、2、82、20、49 行，并保持 style selector root 对 `select_workbench_popup_row_style`、`WorkbenchPopupRowState`、`WorkbenchPopupRowStyle` 和 danger token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench popup-row style-selector ownership scan 确认根 `workbench_popup_row.rs` 不再拥有 `PALETTE`/`UiPainter*` imports、row/style DTO bodies、painter-state projection body、marked helper、palette constants、popup row style helper bodies 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench segmented-control style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_segmented_control.rs` 中 segmented/tab kind/style DTO、group/idle palette tokens、outer control background/border rules、selected segment surface/border/underline metrics、selected/idle/group label colors、brightness scaling、unavailable-state helper 和 inline loading regression 分别拆入 `workbench_segmented_control/model.rs`、`palette.rs`、`control.rs`、`segments.rs`、`selection.rs`、`text.rs`、`state.rs` 和 `tests.rs`：根 `workbench_segmented_control.rs` 从 263 行降为 16 行 structural export entry；新增子文件分别为 25、3、65、54、31、53、8、47 行，并保持 style selector root 对 `select_workbench_segmented_control_style`、`WorkbenchSegmentedControlKind`、`WorkbenchSegmentedControlStyle` 和 idle background token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench segmented-control style-selector ownership scan 确认根 `workbench_segmented_control.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiStyleColor`/`UiPainter*` imports、segmented DTO bodies、palette constants、control/segment/text/state helper bodies、brightness helper 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench slider style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_slider.rs` 中 slider style DTO、palette tokens、declared color conversion、track/fill/tick rules、thumb/outline/halo rules、value surface/border rules、label/value text rules、hot/unavailable state helpers 和 inline loading regression 分别拆入 `workbench_slider/model.rs`、`palette.rs`、`colors.rs`、`track.rs`、`thumb.rs`、`value.rs`、`text.rs`、`state.rs` 和 `tests.rs`：根 `workbench_slider.rs` 从 210 行降为 20 行 structural export entry；新增子文件分别为 17、15、5、38、39、34、20、21、39 行，并保持 style selector root 对 `select_workbench_slider_style`、`is_workbench_slider_state_hot`、`WorkbenchSliderStyle` 和 slider token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench slider style-selector ownership scan 确认根 `workbench_slider.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`Color`/`UiPainterResolvedState` imports、slider DTO body、palette constants、track/fill/thumb/value/text helper bodies、declared color helper 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench status-control style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_status_control.rs` 中 status signal/chip/icon-button style DTO、palette tokens、signal fill/text/mark selection、status chip state colors、status icon-button state colors、declared color/unavailable-state helpers 和 inline unavailable-state regression 分别拆入 `workbench_status_control/model.rs`、`palette.rs`、`signals.rs`、`chips.rs`、`icons.rs`、`helpers.rs` 和 `tests.rs`：根 `workbench_status_control.rs` 从 254 行降为 18 行 structural export entry；新增子文件分别为 33、10、62、59、68、11、32 行，并保持 style selector root 对 `select_workbench_status_*`、`WorkbenchStatusSignalKind`、`WorkbenchStatusSignalStyle` 和测试色 token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench status-control style-selector ownership scan 确认根 `workbench_status_control.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`Color`/`UiPainter*` imports、status DTO bodies、palette constants、signal/chip/icon-button state-style helper bodies、declared color/unavailable helper 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench table-row style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_table_row.rs` 中 table-row style DTO、row/header/tail/selection/separator/action/text palette tokens、header/tail classification、background/border/action/declared-color rules、style assembly、hot/unavailable helpers、cell text color resolution 和 inline loading regression 分别拆入 `workbench_table_row/model.rs`、`palette.rs`、`identity.rs`、`colors.rs`、`selection.rs`、`state.rs`、`text.rs` 和 `tests.rs`：根 `workbench_table_row.rs` 从 207 行降为 18 行 structural export entry；新增子文件分别为 17、16、9、80、34、19、23、27 行，并保持 style selector root 对 `select_workbench_table_row_style`、`WorkbenchTableRowStyle` 和 table token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench table-row style-selector ownership scan 确认根 `workbench_table_row.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiStyleColor`/`UiPainter*` imports、table-row DTO body、palette constants、`text_for_cell` body、header/tail classification、style assembly、background/border/action/color helper bodies 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench list-row style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_list_row.rs` 中 list-row style DTO、surface/background/border/declared-background rules、text/adornment/declared-color rules、style assembly、unavailable-state helper 和 inline loading regression 分别拆入 `workbench_list_row/model.rs`、`surface.rs`、`colors.rs`、`selection.rs`、`state.rs` 和 `tests.rs`：根 `workbench_list_row.rs` 从 150 行降为 11 行 structural export entry；新增子文件分别为 11、54、41、21、8、26 行，并保持 style selector root 对 `select_workbench_list_row_style` 和 `WorkbenchListRowStyle` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench list-row style-selector ownership scan 确认根 `workbench_list_row.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiStyleColor`/`UiPainter*` imports、list-row DTO body、surface/background/border/color helper bodies、declared color helpers、unavailable-state helper 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench alert style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_alert.rs` 中 alert tone/style DTO、info/success/warning/error palette tokens、shared painter-state style selection、declared style-color overrides、style assembly 和 inline loading regression 分别拆入 `workbench_alert/model.rs`、`palette.rs`、`state.rs`、`colors.rs`、`selection.rs` 和 `tests.rs`：根 `workbench_alert.rs` 从 170 行降为 16 行 structural export entry；新增子文件分别为 18、49、38、33、18、30 行，并保持 style selector root 对 `select_workbench_alert_style`、`WorkbenchAlertStyle`、`WorkbenchAlertTone` 和 test-visible alert tone tokens 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench alert style-selector ownership scan 确认根 `workbench_alert.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`resolved_style_color`/`Color`/`UiPainter*` imports、alert DTO bodies、palette constants、state-style construction、declared color helper、style assembly body 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench text-field style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_text_field.rs` 中 text-field style DTO、surface/border/text/placeholder/stepper palette tokens、focused sample state resolution、surface/border rules、text/placeholder/stepper color rules、declared style-color conversion、style assembly 和 inline loading regression 分别拆入 `workbench_text_field/model.rs`、`palette.rs`、`state.rs`、`surface.rs`、`text.rs`、`colors.rs`、`selection.rs` 和 `tests.rs`：根 `workbench_text_field.rs` 从 164 行降为 19 行 structural export entry；新增子文件分别为 11、20、18、68、27、7、19、26 行，并保持 style selector root 对 `select_workbench_text_field_style`、`WorkbenchTextFieldStyle` 和 test-visible text-field tokens 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench text-field style-selector ownership scan 确认根 `workbench_text_field.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`resolved_style_color`/`UiPainter*` imports、text-field DTO body、palette constants、state resolution、surface/border/text/stepper helper bodies、declared color helper 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench tree-row style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_tree_row.rs` 中 tree-row style DTO、row text/icon/action palette tokens、background/border/border-width rules、primary/icon/secondary/action color rules、hot/focus/unavailable helpers、style assembly 和 inline loading regression 分别拆入 `workbench_tree_row/model.rs`、`palette.rs`、`surface.rs`、`colors.rs`、`state.rs`、`selection.rs` 和 `tests.rs`：根 `workbench_tree_row.rs` 从 164 行降为 13 行 structural export entry；新增子文件分别为 13、8、30、47、29、26、25 行，并保持 style selector root 对 `select_workbench_tree_row_style`、`WorkbenchTreeRowStyle` 和 test-visible selected text token 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench tree-row style-selector ownership scan 确认根 `workbench_tree_row.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`UiPainter*` imports、tree-row DTO body、palette constants、surface/color/state helper bodies、style assembly body 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench tooltip style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_tooltip.rs` 中 tooltip style DTO、surface/border/title/body/icon/shadow palette tokens、normal style construction、shared painter-state style selection、declared style-color overrides、style assembly 和 inline loading regression 分别拆入 `workbench_tooltip/model.rs`、`palette.rs`、`state.rs`、`colors.rs`、`selection.rs` 和 `tests.rs`：根 `workbench_tooltip.rs` 从 163 行降为 14 行 structural export entry；新增子文件分别为 13、27、47、41、17、33 行，并保持 style selector root 对 `select_workbench_tooltip_style` 和 test-visible tooltip tokens 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench tooltip style-selector ownership scan 确认根 `workbench_tooltip.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`resolved_style_color`/`Color`/`UiPainter*` imports、tooltip DTO body、palette constants、state-style construction、declared color helper、style assembly body 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench toast style-selector subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_toast.rs` 中 toast style DTO、surface/border/text/action/hover/pressed palette tokens、normal style construction、shared painter-state style selection、declared style-color overrides、style assembly 和 inline loading regression 分别拆入 `workbench_toast/model.rs`、`palette.rs`、`state.rs`、`colors.rs`、`selection.rs` 和 `tests.rs`：根 `workbench_toast.rs` 从 155 行降为 13 行 structural export entry；新增子文件分别为 12、26、51、35、17、32 行，并保持 style selector root 对 `select_workbench_toast_style` 和 test-visible toast tokens 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Workbench toast style-selector ownership scan 确认根 `workbench_toast.rs` 不再拥有 `TemplatePaneNodeData`/`PALETTE`/`resolved_style_color`/`Color`/`UiPainter*` imports、toast DTO body、palette constants、state-style construction、declared color helper、style assembly body 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes visual-asset MUI icon candidate/name/parser/rendering/svg-document subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/visual_assets/mui_icons.rs` 中 MUI module candidate generation、module path detection、Material UI module-name normalization、Pascal-case ligature fallback、JS module path/opacity extraction、SVG document assembly、rendering handoff 和 inline regressions 分别拆入 `visual_assets/mui_icons/candidates.rs`、`names.rs`、`parser.rs`、`svg_document.rs`、`rendering.rs` 和 `tests.rs`：根 `visual_assets/mui_icons.rs` 从 256 行降为 9 行 structural export entry；新增子文件分别为 20、60、65、30、24、40 行，并保持 visual-assets root 对 `module_candidates`、`is_module_path`、`render_module_pixels` 和 `render_module_image` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；visual-asset MUI icon candidate/name/parser/rendering/svg-document ownership scan 确认根 `mui_icons.rs` 不再拥有 `fs`/`Path`/`PathBuf`/render payload imports、candidate/path/name/parser/SVG/render helper bodies 或 inline test body，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes popup-row adornment glyph dispatch/symbol/assets/segment subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/template_popup_row_adornments/glyphs.rs` 中 popup-row adornment row clipping、`PopupRowAdornmentKind` dispatch、check/chevron/plus/trash symbolic segment layouts、folder/save asset-style quad composition 和 shared segment emission 分别拆入 `glyphs/dispatch.rs`、`glyphs/symbols.rs`、`glyphs/assets.rs` 和 `glyphs/segments.rs`：根 `glyphs.rs` 从 214 行降为 5 行 structural export entry；新增子文件分别为 45、89、72、23 行，并保持 `template_popup_row_adornments.rs` 对 `push_popup_row_adornment` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；popup-row adornment glyph dispatch/symbol/assets/segment ownership scan 确认根 `glyphs.rs` 不再拥有 `FrameRect`/`HostPaintCommand`/`PALETTE` imports、clip gating、kind dispatch、concrete glyph helper bodies 或 segment push body，仅保留 module wiring 和 stable re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command draw ordering/dispatch/quad/text/image/helper subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/render_commands/draw.rs` 中 stable z-order replay、single-command visibility/opacity gating、per-kind dispatch、quad fill/border replay、text replay、image atlas/resource replay、placeholder fallback、expanded border replay 和 opacity application 分别拆入 `render_commands/draw/ordering.rs`、`dispatch.rs`、`quad.rs`、`text.rs`、`image.rs`、`border.rs` 和 `color.rs`：根 `draw.rs` 从 210 行降为 8 行 structural export entry；新增子文件分别为 24、27、48、26、86、16、5 行，并保持 `render_commands.rs` 对 `draw_host_paint_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；render-command draw ordering/dispatch/quad/text/image/helper ownership scan 确认根 `draw.rs` 不再拥有 `HostRgbaFrame`/`HostPaintCommand`/`HostPaintCommandKind`/`HostPaintImagePixels`/`PALETTE` imports、ordered playback body、per-kind draw helpers、placeholder/opacity helpers 或 border expansion body，仅保留 module wiring 和 stable replay re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command command model/kind/constructor subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/render_commands/command.rs` 中 `HostPaintCommand` storage、`HostPaintCommandKind`、quad/text/group/image/image_pixels constructors、fallback image border token 和 retained-image payload construction 分别拆入 `render_commands/command/model.rs`、`kind.rs`、`quad.rs`、`text.rs`、`group.rs` 和 `image.rs`：根 `command.rs` 从 180 行降为 8 行 structural export entry；新增子文件分别为 39、7、33、35、29、70 行，并保持 `render_commands.rs` 对 `HostPaintCommand` 以及 draw 子模块对 `HostPaintCommandKind` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；render-command command model/kind/constructor ownership scan 确认根 `command.rs` 不再拥有 `UiTextRunPaintStyle`/`FrameRect`/`HostPaintAtlasImage`/`HostPaintImagePixels`/`PALETTE` imports、command kind enum body、command DTO body、constructor bodies 或 fallback image border token，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive divider geometry align/metric/orientation/extent/line/label/text-frame subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/divider/geometry.rs` 中 pixel alignment、divider metrics/font-size clamping、vertical orientation detection、horizontal/vertical extents、line coordinate/frame construction、label bounds/text alignment 和 label text-frame layout 分别拆入 `divider/geometry/align.rs`、`metrics.rs`、`orientation.rs`、`extents.rs`、`line_frames.rs`、`label_bounds.rs` 和 `text_frame.rs`：根 `geometry.rs` 从 209 行降为 14 行 structural export entry；新增子文件分别为 3、19、7、42、27、77、54 行，并保持 `divider/{horizontal,vertical,lines,labels,commands}.rs` 对几何 helper 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Material primitive divider geometry align/metric/orientation/extent/line/label/text-frame ownership scan 确认根 `geometry.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`component_variant_contains` imports、divider metric constants、orientation/extents/line/label/text-frame helper bodies、font-size helper 或 alignment enum，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive text-field command/identity/geometry/style/variant subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/text_field.rs` 中 TextField recognition、pixel-aligned frame conversion、outlined/filled/standard variant selection、surface command sequencing、underline command emission、fill/stroke/radius rules 和 MUI field constants 分别拆入 `text_field/identity.rs`、`geometry.rs`、`variant.rs`、`commands.rs` 和 `style.rs`：根 `text_field.rs` 从 201 行降为 6 行 structural export entry；新增子文件分别为 10、9、17、113、64 行，并保持 `material_primitives/dispatch.rs` 对 `push_text_field_surface_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Material primitive text-field command/identity/geometry/style/variant ownership scan 确认根 `text_field.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand`/`PALETTE` imports、MUI field constants、TextField recognition、variant enum/selection、field command helpers、style helper bodies 或 pixel alignment body，仅保留 module wiring 和 stable re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive chip geometry frame/leading/delete/label/metric subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/chip/geometry.rs` 中 chip frame/radius geometry、avatar/icon leading slot geometry、delete icon frame/edge geometry、label frame/font/padding rules 和 chip geometry constants 分别拆入 `chip/geometry/frame.rs`、`leading.rs`、`delete.rs`、`label.rs` 和 `metrics.rs`：根 `geometry.rs` 从 198 行降为 9 行 structural export entry；新增子文件分别为 30、74、23、73、15 行，并保持 `chip/{commands,leading,text,delete,surface}.rs` 对 chip geometry helper 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Material primitive chip geometry frame/leading/delete/label/metric ownership scan 确认根 `geometry.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData` imports、chip identity helper imports、CHIP metric constants、frame/radius/slot/label/delete helper bodies 或 edge/margin helper bodies，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive paper command/identity/geometry/shadow/style subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/paper.rs` 中 Paper role recognition、pixel-aligned paper rectangles、Paper command sequencing、elevation shadow layer construction、expanded shadow rects、outlined/elevation/radius/background/border/overlay rules 分别拆入 `paper/identity.rs`、`geometry.rs`、`commands.rs`、`shadow.rs` 和 `style.rs`：根 `paper.rs` 从 190 行降为 6 行 structural export entry；新增子文件分别为 7、9、62、61、62 行，并保持 `material_primitives/dispatch.rs` 对 `push_paper_primitive_commands` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；Material primitive paper command/identity/geometry/shadow/style ownership scan 确认根 `paper.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand` imports、MUI paper/shadow constants、ShadowLayer、Paper role recognition、command sequencing、shadow construction、style helpers 或 pixel alignment body，仅保留 module wiring 和 stable re-export；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive badge command/identity/label subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/badge.rs` 中 Badge root/slot recognition、dot/invisible state recognition、root/overlay text extraction、root/overlay command sequencing、overlay text command emission 和 root radius helper 分别拆入 `badge/identity.rs`、`labels.rs` 和 `commands.rs`：根 `badge.rs` 从 184 行降为 6 行 structural export entry；新增子文件分别为 21、12、154 行，并保持 `material_primitives/dispatch.rs` 对 `push_badge_primitive_commands` 的原导入路径；既有 `badge/geometry.rs` 与 `badge/style.rs` 继续保留 badge frame/text geometry 和 color rules。 | `cargo fmt -p zircon_editor --check` 通过；Material primitive badge command/identity/label ownership scan 确认根 `badge.rs` 不再拥有 `FrameRect`/`TemplatePaneNodeData`/`HostPaintCommand`/`UiTextRunPaintStyle` imports、Badge recognition、label extraction、root/overlay command helper bodies、dot/invisible helpers 或 root radius helper，仅保留 module wiring 和 stable re-export；scoped whitespace scan 覆盖 root、新增/既有子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes visual-asset SVG cache/font/image/parse/pixel subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 被 runtime 当前错误阻塞） | 将 `host_contract/paint_template_nodes/visual_assets/svg.rs` 中 SVG tree cache key/cache lookup/store、SVG file reading/resource-dir resolution、system font database lazy init、font-needs scan、`usvg` tree parsing、intrinsic SVG image rendering、target-sized pixel rasterization、tint application 和 retained pixel payload construction 分别拆入 `svg/cache.rs`、`font.rs`、`parse.rs`、`image.rs` 和 `pixels.rs`：根 `svg.rs` 从 182 行降为 9 行 structural export entry；新增子文件分别为 76、23、16、36、46 行，并保持 loading/MUI icon renderers 对 `render_svg_*` 与 `parse_svg_tree_data` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 通过；visual-asset SVG cache/font/image/parse/pixel ownership scan 确认根 `svg.rs` 不再拥有 std/resvg imports、cache key/cache body、font DB body、parse body、tree/image/pixel render bodies、font-scan helper 或 retained pixel construction，仅保留 module wiring 和 stable re-exports；scoped whitespace scan 覆盖 root、新增子文件、同步文档、计划和 session 记录并通过；scoped `git diff --check` 通过，仅提示既有 docs/Rust LF→CRLF 工作树转换；未重复启动 Cargo check，因为当前 package checks 已被 `zircon_runtime` render-history 编译错误阻塞且未到达 editor diagnostics。未运行 Cargo test/full validation matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes visual-asset loading cache/image/key/missing/pixels subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 超时未出诊断） | 将 `host_contract/paint_template_nodes/visual_assets/loading.rs` 中 visual asset pixel cache lookup/store、candidate image path loading、pixel cache key formatting、missing-icon fallback raster generation、candidate pixel loading、SVG/MUI/raster dispatch、sprite-atlas attachment 和 tint-aware resource-key handling 分别拆入 `loading/cache.rs`、`image.rs`、`key.rs`、`missing.rs` 和 `pixels.rs`：根 `loading.rs` 从 167 行降为 9 行 structural export entry；新增子文件分别为 30、34、23、38、66 行，并保持 asset/template/tests 对 `load_pixels_from_candidates`、`load_image_from_candidates` 和 `missing_icon_pixels` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；visual-asset loading cache/image/key/missing/pixels ownership scan 确认根 `loading.rs` 不再拥有 std/cache/path/SVG/MUI/retained-pixel imports、cache/key/missing-image bodies、image-path loading body 或 pixel dispatch body，仅保留 module wiring 和 stable re-exports；当前 `cargo check -p zircon_editor --lib` 在 184 秒后超时且未输出编译诊断，按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench icon-button selection background/border/radius/glyph/declared/danger subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 暂未复跑） | 将 `host_contract/paint_template_nodes/style_selector/workbench_icon_button/selection.rs` 中 icon-button background state rules、border color/width rules、declared radius fallback、glyph color rules、declared style/icon-color extraction 和 delete/trash/danger/error semantic detection 分别拆入 `selection/background.rs`、`border.rs`、`radius.rs`、`glyph.rs`、`declared.rs` 和 `danger.rs`：根 `selection.rs` 从 190 行降为 34 行 structural style assembly entry；新增子文件分别为 42、52、22、41、19、15 行，并保持 `workbench_icon_button.rs` 对 `select_workbench_icon_button_style` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Workbench icon-button selection ownership scan 确认根 `selection.rs` 不再拥有 PALETTE/token imports、declared style-color extraction、state-specific background/border/glyph/radius helper bodies 或 danger-key matching body，仅保留 module wiring、state/context/danger assembly 和 stable style return；按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes shell-panel separator command/geometry/line/metric subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 暂未复跑） | 将 `host_contract/paint_template_nodes/template_shell_panels/separators.rs` 中 shell-panel separator routing、pixel-aligned panel rects、top/bottom/left/right/vertical line quad emission 和 drawer-column separator offset 分别拆入 `separators/commands.rs`、`geometry.rs`、`lines.rs` 和 `metrics.rs`：根 `separators.rs` 从 182 行降为 7 行 structural export entry；新增子文件分别为 54、10、125、1 行，并保持 `template_shell_panels.rs` 与 `surface.rs` 对 `pixel_aligned_rect` / `push_shell_panel_separators` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；shell-panel separator ownership scan 确认根 `separators.rs` 不再拥有 FrameRect/HostPaintCommand/style imports、line helper bodies、drawer offset 或 pixel-alignment body，仅保留 module wiring 和 stable re-exports；按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive badge command sequencing/root/overlay subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 暂未复跑） | 将 `host_contract/paint_template_nodes/material_primitives/badge/commands.rs` 中 Badge root/slot gating、root surface command emission、root label text command emission、overlay/dot quad emission、overlay text command emission 和 root corner-radius helper 分别拆入 `commands/sequencing.rs`、`root_surface.rs`、`root_label.rs` 和 `overlay.rs`：根 `commands.rs` 从 161 行降为 6 行 structural export entry；新增子文件分别为 27、37、32、79 行，并保持 `badge.rs` 对 `push_badge_primitive_commands` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Material primitive badge command ownership scan 确认根 `commands.rs` 不再拥有 FrameRect/TemplatePaneNodeData/HostPaintCommand/UiTextRunPaintStyle imports、root/overlay command bodies、root radius helper 或 Badge recognition/label/style/geometry imports，仅保留 module wiring 和 stable command export；按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes dialog style color/severity/state/token/variant subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 暂未复跑） | 将 `host_contract/paint_template_nodes/template_dialogs/style.rs` 中 dialog palette constants、severity parsing/mark colors、unavailable/confirm-enabled state、variant token matching 和 surface/border/title/body/action color selection 分别拆入 `style/tokens.rs`、`severity.rs`、`state.rs`、`variants.rs` 和 `colors.rs`：根 `style.rs` 从 163 行降为 12 行 structural style export entry；新增子文件分别为 15、39、18、18、87 行，并保持 surface/content/actions 对 `dialog_*`、`confirm_*` 和 `severity_mark_color` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；dialog style ownership scan 确认根 `style.rs` 不再拥有 TemplatePaneNodeData/DialogKind imports、DIALOG_* constants、DialogSeverity、variant matching、severity parsing、state helper 或 concrete color-selection bodies，仅保留 module wiring 和 stable style re-exports；按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive alert style palette/radius/surface/text/token/variant subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 暂未复跑） | 将 `host_contract/paint_template_nodes/material_primitives/alert/style.rs` 中 Alert MUI style constants、severity main/container/fill text colors、corner radius clamping、surface background/border/border-width rules、text/icon/action/cutout colors 和 color-token/variant parsing 分别拆入 `style/tokens.rs`、`palette.rs`、`radius.rs`、`surface.rs`、`text.rs` 和 `variants.rs`：根 `style.rs` 从 160 行降为 12 行 structural style export entry；新增子文件分别为 8、34、15、42、44、35 行，并保持 surface/icon/message/action 对 `alert_*` style helper 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Material primitive alert style ownership scan 确认根 `style.rs` 不再拥有 FrameRect/TemplatePaneNodeData/PALETTE/shared-style imports、MUI/ALERT constants、surface/text/icon/action/radius helper bodies、variant parsing 或 PascalCase helper，仅保留 module wiring 和 stable style re-exports；按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X Chat identity/command/agent/composer/metric/style subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 暂未复跑） | 将 `host_contract/paint_template_nodes/mui_x_primitives/chat.rs` 中 AgentChat/Composer role classification、chat kind dispatch、AgentChat surface/bubble/streaming command emission、composer surface/action command emission、chat metrics 和 chat surface color selection 分别拆入 `chat/identity.rs`、`commands.rs`、`agent.rs`、`composer.rs`、`metrics.rs` 和 `style.rs`：根 `chat.rs` 从 159 行降为 9 行 structural export entry；新增子文件分别为 19、20、78、39、3、12 行，并保持 `kind.rs` 与 `mui_x_primitives.rs` 对 `chat_kind` / `push_chat` / `ChatKind` 的原导入路径。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；MUI X Chat ownership scan 确认根 `chat.rs` 不再拥有 FrameRect/TemplatePaneNodeData/HostPaintCommand/PALETTE imports、chat metric constants、ChatKind body、role matching body、AgentChat/Composer command bodies 或 surface-color helper，仅保留 module wiring 和 stable exports；按用户“先实现功能，测试暂时延后”要求不扩展 full Cargo test matrix。 | 继续 M3.S2：执行 scoped whitespace/diff check 后继续拆分 retained host 深层 owner；后续测试窗口需先等 runtime 编译错误收敛，再补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 retained-host owner visibility convergence and package validation | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 统一拆分后 `host_contract` 与 `paint_template_nodes` 内部 owner re-export 可见性：将仍停留在叶子 `pub(super)` 或子 owner `pub(in ...::child)` 的协作入口收敛到对应 owner 根，修复 Material primitive、Dialog style、visual asset、MUI X Chat、shell-panel separator、native pointer/keyboard routing 等拆分后暴露的 E0364/E0365/E0603 边界错误；同时修正 style-selector 深层文件对 `TemplatePaneNodeData` / `PALETTE` 的旧相对路径，恢复 `template_popup_bounds` 的 `template_geometry` 引用，并从 owner 根导出 `STEPPER_WIDTH` / `INSPECTOR_CHEVRON_SIZE`。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 均通过；`git diff --check` 通过，仅输出既有 LF→CRLF 工作树转换提示；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：在 editor 包级检查已恢复的基础上继续拆分 retained-host 深层 owner，并把 full tests 留到阶段性测试窗口。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X chart raster subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/charts/raster.rs` 中 chart raster pixel buffer、normalized coordinate conversion、line/point/disc rasterization、arc/gauge rasterization、pie/donut fill rasterization 和 math helpers 分别拆入 `raster/model.rs`、`math.rs`、`line.rs`、`shape.rs`、`arc.rs` 和 `pie.rs`：根 `raster.rs` 从 166 行降为 8 行 structural export entry，并保持 `raster_commands.rs` 对 `ChartRaster` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes template style color surface/border/text subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_style/colors.rs` 中通用 surface/background、border/tone、text/contrast 颜色选择分别拆入 `colors/surface.rs`、`border.rs` 和 `text.rs`：根 `colors.rs` 从 149 行降为 7 行 structural export entry，并保持 `template_style.rs` 对 `surface_color`、`border_color` 和 `text_color` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、template style color surface/border/text ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF→CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene ambient light subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene_light/ambient.rs` 中 soft light、soft shadow 和 floor reflection 命令输出分别拆入 `ambient/soft_light.rs`、`soft_shadow.rs` 和 `floor_reflection.rs`：根 `ambient.rs` 从 149 行降为 7 行 structural export entry，并保持 `template_viewport_scene_light.rs` 对 `push_soft_light`、`push_soft_shadow` 和 `push_floor_reflection` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene ambient light ownership scan、scoped whitespace scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene light fixture beacon/palette/wall-light subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_light/fixtures.rs` 中 wall-light command emission、beacon command emission 和 fixture palette constants 分别拆入 `fixtures/wall_light.rs`、`beacon.rs` 和 `palette.rs`：根 `fixtures.rs` 从 94 行降为 5 行 structural export entry，并保持 `template_viewport_scene_light.rs` 对 `push_wall_light` 和 `push_beacon` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene light fixture beacon/palette/wall-light ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene cargo prop subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene_props/cargo.rs` 中 cargo surface highlight/inset shadow 与 inner-frame edge/grid lines 分别拆入 `cargo/detail.rs` 和 `cargo/inner_frame.rs`：根 `cargo.rs` 从 146 行降为 5 行 structural export entry，并保持 `template_viewport_scene_props.rs` 对 `push_cargo_detail` 和 `push_cargo_inner_frame` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene cargo prop ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；默认 target 先出现 stale fingerprint/proc-macro cache 错误，因此用专用 target 目录完成包级验证；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive chip style subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/chip/style.rs` 中 MUI palette/token matching、background、foreground、border、avatar background 和 delete icon color rules 分别拆入 `style/palette.rs`、`background.rs`、`foreground.rs`、`border.rs`、`avatar.rs` 和 `delete.rs`：根 `style.rs` 从 142 行降为 13 行 structural export entry，并保持 surface/leading/text/delete 对 `chip_*` style helper 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive chip style ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene command domain routing split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene/commands.rs` 中 gizmo/floor/surface/prop/architecture/lighting scene-kind routing 分别拆入 `commands/gizmos.rs`、`floor.rs`、`surfaces.rs`、`props.rs`、`architecture.rs` 和 `lighting.rs`：根 `commands.rs` 从 139 行降为 77 行 top-level router，并保持 `template_viewport_scene.rs` 对 `push_viewport_scene_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene command domain routing ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes status signal icon base/success/info/warning subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_status_glyphs/signals.rs` 中 Ready circle、Success check mark、Info mark、Warning triangle/mark 分别拆入 `signals/base.rs`、`success.rs`、`info.rs` 和 `warning.rs`：根 `signals.rs` 从 139 行降为 structural dispatch entry，并保持 `template_status_glyphs.rs` 对 `push_status_signal_icon` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、status signal icon base/success/info/warning ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes status glyph icon kind/snap/world/target subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_status_glyphs/icon_glyphs.rs` 中 `StatusIconKind`、Snap、World、Target glyph command emission 分别拆入 `icon_glyphs/kind.rs`、`snap.rs`、`world.rs` 和 `target.rs`：根 `icon_glyphs.rs` 从 122 行降为 structural dispatch entry，并保持 `template_status_glyphs.rs` 对 `push_status_icon_glyph` 和 `StatusIconKind` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、status glyph icon kind/snap/world/target ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive badge geometry anchor/metrics/model/overlay/root-text subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/badge/geometry.rs` 中 badge sizing metrics、`BadgeTextFrame`、root text frame、overlay/dot frame/radius/text frame 和 anchor-origin/circular-overlap offset resolution 分别拆入 `geometry/metrics.rs`、`model.rs`、`root_text.rs`、`overlay.rs` 和 `anchor.rs`：根 `geometry.rs` 从 134 行降为 9 行 structural export entry，并保持 Badge command 子模块对 `badge_root_text_frame`、`badge_overlay_frame`、`badge_overlay_radius` 和 `badge_overlay_text_frame` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive badge geometry anchor/metrics/model/overlay/root-text ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive timeline identity/dot/connector/geometry subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/timeline.rs` 中 Timeline role classification、Dot command emission、Connector command emission、centered dot-square geometry 和 connector width 分别拆入 `timeline/identity.rs`、`dot.rs`、`connector.rs` 和 `geometry.rs`：根 `timeline.rs` 从 125 行降为 30 行 dispatch entry，并保持 Material primitive dispatch 对 `push_timeline_primitive_commands` 的原导入路径；既有 `timeline/style.rs` 继续拥有 color/variant/border rules。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive timeline identity/dot/connector/geometry ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene gizmo axes color/cap/line/origin subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene_gizmos/axes.rs` 中 axis color/glow selection、axis cap geometry/command emission、axis line/glow command emission 和 axis origin glow/base-surface command emission 分别拆入 `axes/colors.rs`、`cap.rs`、`line.rs` 和 `origin.rs`：根 `axes.rs` 从 133 行降为 6 行 structural export entry，并保持 `template_viewport_scene_gizmos.rs` 对 `push_axis_line` 和 `push_axis_origin` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene gizmo axes color/cap/line/origin ownership scan 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes table-row cell command/geometry/metric/text subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_table_rows/cells.rs` 中 declared option/legacy table text extraction、cell rect/content offset geometry、text command emission 和 table cell metrics 分别拆入 `cells/text.rs`、`geometry.rs`、`commands.rs` 和 `metrics.rs`：根 `cells.rs` 从 126 行降为 12 行 structural export entry，并保持 `template_table_rows/commands.rs`、`actions.rs` 和 `template_table_rows_tests.rs` 对 table cell helpers 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、table-row cell command/geometry/metric/text ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF→CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench button state normal/interactive/unavailable subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/style_selector/workbench_button/states.rs` 中 normal、hover、pressed、focused、disabled/loading style construction 分别拆入 `states/normal.rs`、`interactive.rs` 和 `unavailable.rs`：根 `states.rs` 从 138 行降为 24 行 state dispatch entry，并保持 `selection.rs` 对 `base_button_style` 和 `is_unavailable_button_interaction` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Workbench button state normal/interactive/unavailable ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button asset glyph data/media/code subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/assets.rs` 中 Cube/Graph、Image/Audio、Code glyph segment lists 分别拆入 `assets/data.rs`、`media.rs` 和 `code.rs`：根 `assets.rs` 从 129 行降为 10 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `assets::push_cube_icon`、`push_graph_icon`、`push_image_icon`、`push_audio_icon` 和 `push_code_icon` 的原调用路径。 | `cargo fmt -p zircon_editor --check`、icon-button asset glyph data/media/code ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF→CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button tool glyph pointer/transform/snap subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/tools.rs` 中 Cursor、Move/Rotate/Scale、Snap glyph segment lists 分别拆入 `tools/pointer.rs`、`transform.rs` 和 `snap.rs`：根 `tools.rs` 从 127 行降为 8 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `tools::push_cursor_icon`、`push_move_icon`、`push_rotate_icon`、`push_scale_icon` 和 `push_snap_icon` 的原调用路径。 | `cargo fmt -p zircon_editor --check`、icon-button tool glyph pointer/transform/snap ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF→CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene identity kind/chrome/classify subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene/identity.rs` 中 `ViewportSceneKind` enum、viewport chrome-node exclusion、WorkbenchViewport control-id classification 分别拆入 `identity/kind.rs`、`chrome.rs` 和 `classify.rs`：根 `identity.rs` 从 129 行降为 6 行 structural export entry，并保持 `template_viewport_scene/commands.rs` 对 `identity::{viewport_scene_kind, ViewportSceneKind}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene identity kind/chrome/classify ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes tree-row object glyph dispatch/kind/icon subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_tree_row_glyphs/object.rs` 中 object glyph public dispatch、TreeIconKind 选择、unavailable state classification 与 cube/player-start/audio segment lists 分别拆入 `object/dispatch.rs`、`kind.rs` 和 `icons.rs`：根 `object.rs` 从 123 行降为 5 行 structural export entry，并保持 `template_tree_row_glyphs.rs` 对 `object::push_tree_object_icon_glyph` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、tree-row object glyph dispatch/kind/icon ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes alert mark dispatch/palette/round/warning subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_alert_glyphs/marks.rs` 中 alert-tone dispatch、shared dark glyph color、info/success round-surface mark emission 与 warning triangle/mark emission 分别拆入 `marks/dispatch.rs`、`palette.rs`、`round.rs` 和 `warning.rs`：根 `marks.rs` 从 120 行降为 6 行 structural export entry，并保持 `template_alert_glyphs.rs` 对 `marks::push_alert_mark` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、alert mark dispatch/palette/round/warning ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button chrome glyph display/navigation/playback subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/chrome.rs` 中 Grid/Sun display glyphs、ChevronDown/More navigation glyphs 和 Play playback glyph 分别拆入 `chrome/display.rs`、`navigation.rs` 和 `playback.rs`：根 `chrome.rs` 从 117 行降为 11 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `chrome::push_play_icon`、`push_chevron_down_icon`、`push_grid_icon`、`push_sun_icon` 和 `push_more_icon` 的原调用路径。 | `cargo fmt -p zircon_editor --check`、icon-button chrome glyph display/navigation/playback ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF→CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes list-row glyph shape check/chevron/disabled subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_list_row_glyphs/shapes.rs` 中 check mark、right chevron 与 disabled diamond pixel shapes 分别拆入 `shapes/check.rs`、`chevron.rs` 和 `disabled.rs`：根 `shapes.rs` 从 119 行降为 7 行 structural export entry，并保持 `template_list_row_glyphs.rs` 对 `shapes::push_check_mark`、`push_right_chevron` 和 `push_disabled_diamond` 的原调用路径。 | `cargo fmt -p zircon_editor --check`、list-row glyph shape check/chevron/disabled ownership scan 与 scoped `git diff --check` 通过；首轮 `cargo check` 暴露 `shapes/disabled.rs` 中 `paint_theme::PALETTE` 相对路径少上溯一层，已修正并重跑 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 通过，仅保留既有 warning 噪声；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button file glyph document/folder/menu/save subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/files.rs` 中 File、Folder、Menu 与 Save glyph segment lists 分别拆入 `files/document.rs`、`folder.rs`、`menu.rs` 和 `save.rs`：根 `files.rs` 从 100 行降为 9 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `files::push_menu_icon`、`push_file_icon`、`push_folder_icon` 和 `push_save_icon` 的原调用路径。 | `cargo fmt -p zircon_editor --check`、icon-button file glyph document/folder/menu/save ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF/CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button action glyph plus/trash/filter subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/actions.rs` 中 Plus、Trash 与 Filter glyph segment lists 分别拆入 `actions/plus.rs`、`trash.rs` 和 `filter.rs`：根 `actions.rs` 从 70 行降为 7 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `actions::push_plus_icon`、`push_trash_icon` 和 `push_filter_icon` 的原调用路径。 | `cargo fmt -p zircon_editor --check`、icon-button action glyph plus/trash/filter ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF/CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button visibility glyph eye/eye-off/lock subowner split | 完成（功能代码、包级检查通过；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/visibility.rs` 中 Eye、EyeOff 与 Lock glyph segment lists 分别拆入 `visibility/eye.rs`、`eye_off.rs` 和 `lock.rs`：根 `visibility.rs` 从 72 行降为 7 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `visibility::push_eye_icon`、`push_eye_off_icon` 和 `push_lock_icon` 的原调用路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、icon-button visibility glyph eye/eye-off/lock ownership scan、scoped `git diff --check` 与 `cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 均通过；`git diff --check` 仅提示既有 LF/CRLF 工作树转换；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button glyph shape dispatch subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/mod.rs` 中 `IconButtonGlyphKind` 到 action/file/tool/chrome/asset/visibility family owner 的路由移入 `template_icon_button_glyph_shapes/dispatch.rs`：父 `mod.rs` 从 98 行降为 9 行 structural export owner，并保持 `template_icon_button_glyph_shapes::push_icon_button_glyph_shape` 的原导出路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、icon-button glyph shape dispatch ownership scan 与 scoped `git diff --check` 均通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\Git\ZirconEngine\.codex\targets\zircon-editor-ui-cargo-split-0620 --message-format short --color never` 在进入 editor 前被并行 Runtime 08 ECS `component_storage` 工作树阻断，报 `RawRemoveResult`、`StoredComponent`、`SparseComponentStorage`、`TableComponentStorage`、`downcast_component`、`sort_component_ids_if_needed` 私有 re-export；未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes shell-panel separator line horizontal/vertical subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_shell_panels/separators/lines.rs` 中 top/bottom horizontal line emission 与 left/right/direct vertical line emission 分别拆入 `lines/horizontal.rs` 和 `lines/vertical.rs`：父 `lines.rs` 从 125 行降为 8 行 structural export entry，并保持 `separators/commands.rs` 对 `push_top_line`、`push_bottom_line`、`push_left_line`、`push_right_line` 和 `push_vertical_line` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 与 shell-panel separator line horizontal/vertical ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive badge style root/overlay/palette/token subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/badge/style.rs` 中 Badge root background/border/text color rules、overlay background/text/border rules、MUI badge palette constants 和 badge color-token resolution 分别拆入 `style/root.rs`、`overlay.rs`、`palette.rs` 和 `tokens.rs`：父 `style.rs` 从 111 行降为 12 行 structural export entry，并保持 Badge command 子模块对 `badge_root_*` 与 `badge_overlay_*` helper 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 Material primitive badge style root/overlay/palette/token ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X Picker identity/command/field/popup/geometry/metric subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/pickers.rs` 中 Date/Time picker role matching、field/root command emission、popup preview command emission、picker frame geometry 和 picker constants 分别拆入 `pickers/identity.rs`、`commands.rs`、`field.rs`、`popup.rs`、`geometry.rs` 和 `metrics.rs`：父 `pickers.rs` 从 123 行降为 8 行 structural export entry，并保持 `kind.rs` 对 `is_date_time_picker` 与 `mui_x_primitives.rs` 对 `push_date_time_picker` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 MUI X Picker identity/command/field/popup/geometry/metric ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes alert layout common/inline/metric/toast subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_alerts/layout.rs` 中 alert/toast font/radius/line-height metrics、pixel alignment、centered rect helper、inline alert icon/text frames 和 toast icon/close/action/text frames 分别拆入 `layout/metrics.rs`、`common.rs`、`inline.rs` 和 `toast.rs`：父 `layout.rs` 从 115 行降为 15 行 structural export entry，并保持 inline/toast/commands 子模块对 alert/toast layout helper 与 constants 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 alert layout common/inline/metric/toast ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes notification-center layout common/metric/panel/row subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_notification_center/layout.rs` 中 NotificationCenter pixel alignment、panel/header/row/text metrics、header/empty-message rectangles 与 row/mark/title/message/text-width rectangles 分别拆入 `layout/common.rs`、`metrics.rs`、`panel.rs` 和 `row.rs`：父 `layout.rs` 从 113 行降为 15 行 structural export entry，并保持 commands/panel/row 子模块对 notification layout helper 与 constants 的原导入路径。 | `cargo fmt -p zircon_editor --check` 与 notification-center layout common/metric/panel/row ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command conversion text command/decoration subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/render_command_conversion/text.rs` 中 runtime text color resolution、run/shaped/fallback text command emission、aligned fallback placement 与 selection/caret/composition/outline decoration quad emission 分别拆入 `text/commands.rs` 和 `text/decorations.rs`：父 `text.rs` 从 116 行降为 3 行 structural export entry，并保持 `render_command_conversion/commands.rs` 对 `push_text_paint_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 render-command conversion text command/decoration ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command conversion style color/frame/text subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/render_command_conversion/style.rs` 中 runtime foreground fallback、hex style color parsing、`UiFrame` 到 `FrameRect` 转换和 aligned text fallback x-placement 分别拆入 `style/colors.rs`、`frame.rs` 和 `text.rs`：父 `style.rs` 从 87 行降为 9 行 structural export entry，并保持 brush/commands/text 子模块对 `parse_style_color`、`runtime_foreground_color`、`frame_from_ui` 和 `aligned_text_x` 的原导入路径。 | `cargo fmt -p zircon_editor --check` 与 render-command conversion style color/frame/text ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X chart raster command/metric/kind/shape subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/charts/raster_commands.rs` 中 raster dimensions、chart-kind resource key naming、line/sparkline/pie/gauge raster drawing、gauge value normalization 和 `HostPaintCommand::image_pixels(...)` sequencing 分别拆入 `raster_commands/dimensions.rs`、`identity.rs`、`line.rs`、`sparkline.rs`、`pie.rs`、`gauge.rs` 和 `commands.rs`：父 `raster_commands.rs` 从 133 行降为 9 行 structural export entry，并保持 `charts.rs` 对 `push_chart_raster` 的原调用路径。 | `cargo fmt -p zircon_editor --check` 与 MUI X chart raster command/metric/kind/shape ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes property-row layout label/value/axis/metric subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_property_rows/layout.rs` 中 component-aware label width、label/value/scalar field rectangles、axis group rectangles 和 property-row layout constants 分别拆入 `layout/labels.rs`、`values.rs`、`axis.rs` 和 `metrics.rs`：父 `layout.rs` 从 120 行降为 16 行 structural export entry，并保持 commands/labels/fields/root test imports 对 property-row layout helper 与 constants 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 property-row layout label/value/axis/metric ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive alert geometry root/icon/message/action/metric subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/alert/geometry.rs` 中 alert root pixel alignment、icon/mark frames、message left/right/frame placement、font fallback、action width/frame placement 和 geometry constants 分别拆入 `geometry/root.rs`、`icon.rs`、`message.rs`、`action.rs` 和 `metrics.rs`：父 `geometry.rs` 从 120 行降为 16 行 structural export entry，并保持 commands/icon/message/action 子模块对 alert geometry helper 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 Material primitive alert geometry root/icon/message/action/metric ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive text-field command entry/surface/underline subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/text_field/commands.rs` 中 TextField role gating、field rect validation、filled/standard/outlined variant dispatch、surface command emission 和 underline command emission 分别拆入 `commands/entry.rs`、`surface.rs` 和 `underline.rs`：父 `commands.rs` 从 113 行降为 4 行 structural export entry，并保持 `text_field.rs` 对 `push_text_field_surface_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 Material primitive text-field command entry/surface/underline ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes visual-asset candidate path/query/resolution/variant subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/visual_assets/candidates.rs` 中 image/icon/template candidate assembly、editor/workspace asset root resolution、asset URI/path normalization、direct/SVG variant insertion、deduplication、first-existing lookup 和 SVG extension checks 分别拆入 `candidates/query.rs`、`paths.rs`、`variants.rs` 和 `resolution.rs`：父 `candidates.rs` 从 108 行降为 9 行 structural export entry，并保持 asset/template/loading/test 模块对 candidate helper 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 visual-asset candidate path/query/resolution/variant ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive timeline style connector/dot/token subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/timeline/style.rs` 中 connector color fallback、dot outlined/background/border rules、border-width fallback、timeline tone-token classification 和 MUI grey/secondary palette constants 分别拆入 `style/connector.rs`、`dot.rs` 和 `tokens.rs`：父 `style.rs` 从 107 行降为 10 行 structural export entry，并保持 dot/connector 子模块对 timeline style helper 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 Material primitive timeline style connector/dot/token ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes slider text label/range-min/value subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_sliders/text.rs` 中 slider label text placement、range-min value surface/text command emission 和 current value surface/text command emission 分别拆入 `text/label.rs`、`range_min.rs` 和 `value.rs`：父 `text.rs` 从 112 行降为 7 行 structural export entry，并保持 commands 模块对 `push_slider_label`、`push_slider_range_min_value` 和 `push_slider_value` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 slider text label/range-min/value ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes segmented-control segment body/divider/selected subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_segmented_controls/segments.rs` 中 group/body surface sequencing、option iteration、segment divider command emission 和 selected segment surface/underline command emission 分别拆入 `segments/body.rs`、`divider.rs` 和 `selected.rs`：父 `segments.rs` 从 108 行降为 4 行 structural export entry，并保持 commands 模块对 `push_segmented_control` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 segmented-control segment body/divider/selected ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench dropdown color surface/border/content/declared subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/style_selector/workbench_dropdown/colors.rs` 中 surface state/declared background override、validation/unavailable/state border、text/chevron colors 和 declared native/style color extraction 分别拆入 `colors/surface.rs`、`border.rs`、`content.rs`、`declared.rs`：父 `colors.rs` 从 106 行降为 9 行 structural export entry，并保持 `selection.rs` 对 `dropdown_surface`、`dropdown_border`、`dropdown_text` 和 `dropdown_chevron` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 Workbench dropdown color surface/border/content/declared ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench selection-control selection entry/surface/border subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/style_selector/workbench_selection_control/selection.rs` 中 resolved-state/checked-style assembly、checkbox/radio/toggle surface state selection 和 checkbox/radio/toggle border state selection 分别拆入 `selection/entry.rs`、`surface.rs` 和 `border.rs`：父 `selection.rs` 从 106 行降为 4 行 structural export entry，并保持根 `workbench_selection_control.rs` 对 `select_workbench_selection_control_style` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 Workbench selection-control selection entry/surface/border ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes table-row action entry/geometry/glyph subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_table_rows/actions.rs` 中 table-row action rect placement、header/non-header action glyph dispatch、gear glyph segments 和 kebab glyph segments 分别拆入 `actions/geometry.rs`、`entry.rs` 和 `glyphs.rs`：父 `actions.rs` 从 105 行降为 4 行 structural export entry，并保持 commands 模块对 `push_table_action` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 table-row action entry/geometry/glyph ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes button content entry/glyph/text/metric subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_buttons/content.rs` 中 content width/leading/trailing glyph sequencing、button glyph lookup/placement/emission、label text command emission 和 content metrics 分别拆入 `content/entry.rs`、`glyph.rs`、`text.rs` 和 `metrics.rs`：父 `content.rs` 从 104 行降为 6 行 structural export entry，并保持 commands/test 模块对 `push_button_content` 与 `button_glyph` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 button content entry/glyph/text/metric ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene prop property top/body/palette subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene_props/property.rs` 中 prop top overlay command emission、prop body overlay command emission 和 prop highlight/edge/shadow palette constants 分别拆入 `property/top.rs`、`body.rs` 和 `palette.rs`：父 `property.rs` 从 105 行降为 5 行 structural export entry，并保持 `template_viewport_scene_props.rs` 对 `push_prop_top_detail` 与 `push_prop_body_detail` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 viewport-scene prop property top/body/palette ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes inspector-row primitive label/field/text subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_inspector_rows/primitives.rs` 中 inspector label/nested-label text rect routing、resource field surface command emission 和 shared inspector text command emission 分别拆入 `primitives/labels.rs`、`field.rs` 和 `text.rs`：父 `primitives.rs` 从 100 行降为 8 行 structural export entry，并保持 resource/disclosure/shadow 模块对 `push_label`、`push_nested_label`、`push_field` 和 `push_text` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 inspector-row primitive label/field/text ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes tree-row action glyph eye/lock/kebab subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_tree_row_glyphs/actions.rs` 中 eye、lock 和 kebab action glyph segment lists 分别拆入 `actions/eye.rs`、`lock.rs` 和 `kebab.rs`：父 `actions.rs` 从 99 行降为 6 行 structural export entry，并保持根 `template_tree_row_glyphs.rs` 对 `push_tree_eye_action_glyph`、`push_tree_lock_action_glyph` 和 `push_tree_kebab_action_glyph` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 tree-row action glyph eye/lock/kebab ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes command-palette row entry/style/surface/label subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_command_palette/rows.rs` 中 command row sequencing、popup-row style projection、row background/selection-mark emission 和 row label text emission 分别拆入 `rows/entry.rs`、`style.rs`、`surface.rs` 和 `label.rs`：父 `rows.rs` 从 98 行降为 5 行 structural export entry，并保持 commands 模块对 `push_command_row_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 command-palette row entry/style/surface/label ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes command-palette layout common/metric/panel/row/search subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_command_palette/layout.rs` 中 pixel alignment、panel/search/row radii、font metrics、spacing offsets、empty-message geometry、row/label/selection-mark geometry 和 search/search-text geometry 分别拆入 `layout/common.rs`、`metrics.rs`、`panel.rs`、`rows.rs` 和 `search.rs`：父 `layout.rs` 从 100 行降为 15 行 structural export entry，并保持 panel/rows/commands 模块对 `layout::...` helper 和 constants 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 command-palette layout common/metric/panel/row/search ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes dialog action command/label/metric/text subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_dialogs/actions.rs` 中 ConfirmDialog confirm/cancel/default action sequencing、action label extraction、width calculation、action layout/font metrics 和 retained text command emission 分别拆入 `actions/commands.rs`、`labels.rs`、`metrics.rs` 和 `text.rs`：父 `actions.rs` 从 99 行降为 6 行 structural export entry，并保持 dialog commands 模块对 `push_dialog_actions` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 dialog action command/label/metric/text ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes drag-overlay layout preview/indicator/metric subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_drag_overlay/layout.rs` 中 drag-preview fallback/cursor placement、icon/text rectangles、drop-target indicator rectangles 和 preview/icon/text/indicator constants 分别拆入 `layout/preview.rs`、`indicator.rs` 和 `metrics.rs`：父 `layout.rs` 从 107 行降为 11 行 structural export entry，并保持 surface/text/root 模块对 `layout::...` helper 和 constants 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 drag-overlay layout preview/indicator/metric ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes popup-row surface background/metric/row/separator subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_popup_rows/surface.rs` 中 popup background emission、row background/selection-mark emission、separator geometry/emission 和 popup row order/mark constants 分别拆入 `surface/background.rs`、`row.rs`、`separator.rs` 和 `metrics.rs`：父 `surface.rs` 从 101 行降为 8 行 structural export entry，并保持 menu/options/text 模块对 `surface::...` helper 和 `POPUP_ROW_ORDER_OFFSET` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 popup-row surface background/metric/row/separator ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes segmented-control geometry group/body/item/selected subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_segmented_control_geometry/segmented.rs` 中 group-label rectangles、labeled body placement、segment/divider/label rectangles、selected segment inset 和 underline geometry 分别拆入 `segmented/group.rs`、`body.rs`、`item.rs` 和 `selected.rs`：父 `segmented.rs` 从 103 行降为 12 行 structural export entry，并保持 geometry root 对 segmented helper 的原 re-export 路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 segmented-control geometry group/body/item/selected ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene architecture wall column/detail/palette subowner split | 完成（功能代码、轻量检查通过；包级检查被并行 Runtime 08 ECS 工作树阻断；完整测试矩阵按用户要求暂缓） | 将 `host_contract/paint_template_nodes/template_viewport_scene_architecture/walls.rs` 中 wall surface/detail line emission、wall-column base surface/edge overlay emission 和 wall-column overlay palette constants 分别拆入 `walls/detail.rs`、`column.rs` 和 `palette.rs`：父 `walls.rs` 从 104 行降为 6 行 structural export entry，并保持 architecture root 对 `push_wall_column` 与 `push_wall_detail_lines` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check` 与 viewport-scene architecture wall column/detail/palette ownership scan 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换；未重复启动 package Cargo check，因为上一轮 editor check 已被并行 Runtime 08 ECS `component_storage` 私有 re-export 错误阻断且 runtime session 仍 owns 该工作树。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：等待 Runtime 08 ECS 工作树恢复后补跑 editor 包级检查；继续拆分 retained-host 深层 owner。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene prop rail rack/handrail/palette subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_props/rails.rs` 中 rack detail command emission、handrail command emission 和 rack/handrail palette constants 分别拆入 `rails/rack.rs`、`handrail.rs` 和 `palette.rs`：父 `rails.rs` 从 95 行降为 5 行 structural export entry，并保持 `template_viewport_scene_props.rs` 对 `push_rack_detail` 与 `push_handrail` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene prop rail rack/handrail/palette ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene surface floor depth/shadow/sheen/palette subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_surfaces/floor.rs` 中 floor base-surface sequencing、depth-line command emission、top/bottom shadow layers、warm sheen layer 和 floor overlay constants 分别拆入 `floor/surface.rs`、`depth.rs`、`shadows.rs`、`sheen.rs` 和 `palette.rs`：父 `floor.rs` 从 92 行降为 6 行 structural export entry，并保持 `template_viewport_scene_surfaces.rs` 对 `push_floor_surface` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene surface floor depth/shadow/sheen/palette ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene surface back-wall haze/palette/panel/shadow subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_surfaces/back_wall.rs` 中 back-wall base-surface sequencing、panel-line command emission、top shadow layer、inner haze layer 和 overlay constants 分别拆入 `back_wall/surface.rs`、`panels.rs`、`shadow.rs`、`haze.rs` 和 `palette.rs`：父 `back_wall.rs` 从 77 行降为 6 行 structural export entry，并保持 `template_viewport_scene_surfaces.rs` 对 `push_back_wall_surface` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene surface back-wall haze/palette/panel/shadow ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes slider command context/entry/sequence subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_sliders/commands.rs` 中 Workbench slider recognition consumption、pixel-aligned geometry gating、label/value/track rect collection、percent/range-min/tick/style context construction 和 label/value/track/thumb/tick/range-min command ordering 分别拆入 `commands/context.rs`、`entry.rs` 和 `sequence.rs`：父 `commands.rs` 从 106 行降为 5 行 structural export entry，并保持 `template_sliders.rs` 对 `push_slider_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、slider command context/entry/sequence ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes style-selector root state projection subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/style_selector/mod.rs` 中 template-node to `UiPainterState` projection、button interaction-state overrides 和 retained node flags merge 拆入 `style_selector/state.rs`：父 `style_selector/mod.rs` 从 125 行降为 101 行 child-module/re-export surface，新增 `state.rs` 25 行，并保持所有 style-selector child modules 对 `resolved_state_for_node` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、style-selector root state projection ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X TreeView identity/row/style subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/mui_x_primitives/tree_view.rs` 中 TreeView role recognition、surface/row/marker command emission、row geometry 和 surface/row/marker color rules 分别拆入 `tree_view/identity.rs`、`rows.rs` 和 `style.rs`：父 `tree_view.rs` 从 101 行降为 6 行 structural export entry，并保持 `kind.rs` 对 `is_tree_view` 与 `mui_x_primitives.rs` 对 `push_tree_view` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、MUI X TreeView identity/row/style ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes MUI X chart identity/surface subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/mui_x_primitives/charts.rs` 中 chart kind role matching、`ChartKind` enum、chart surface/plot command emission、fallback surface color 和 bar-vs-raster dispatch 分别拆入 `charts/identity.rs` 与 `surface.rs`：父 `charts.rs` 从 101 行降为 10 行 structural export entry，并保持 `kind.rs` 对 `chart_kind` / `ChartKind` 与 `mui_x_primitives.rs` 对 `push_chart` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、MUI X chart identity/surface ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive alert action entry/close/line/metric subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/alert/action.rs` 中 alert action frame/color lookup、close-vs-line dispatch、close mark dot emission、fallback action-line emission 和 close-dot sizing 分别拆入 `action/entry.rs`、`close.rs`、`line.rs` 和 `metrics.rs`：父 `action.rs` 从 99 行降为 6 行 structural export entry，并保持 `alert/commands.rs` 对 `push_alert_action` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive alert action entry/close/line/metric ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes inspector-row resource entry/count/field subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_inspector_rows/resource.rs` 中 resource-row label/count/field sequencing、material resource count width/text emission、resource field surface、leading mesh/material glyph、value text 和 chevron command emission 分别拆入 `resource/entry.rs`、`count.rs` 和 `field.rs`：父 `resource.rs` 从 99 行降为 5 行 structural export entry，并保持 `template_inspector_rows.rs` 对 `push_resource_row` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、inspector-row resource entry/count/field ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes section-title glyph shape entry/cube/mesh/transform subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_section_title_glyphs/shapes.rs` 中 section icon color dispatch、cube glyph segment list、mesh glyph segment list 和 transform glyph segment list 分别拆入 `shapes/entry.rs`、`cube.rs`、`mesh.rs` 和 `transform.rs`：父 `shapes.rs` 从 98 行降为 6 行 structural export entry，并保持 `template_section_title_glyphs.rs` 对 `push_section_icon` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、section-title glyph shape entry/cube/mesh/transform ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes popup-row menu entry/style subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_popup_rows/menu.rs` 中 menu row count/background iteration、separator/surface/label/shortcut/adornment command ordering 和 menu item `WorkbenchPopupRowState` style projection 分别拆入 `menu/entry.rs` 与 `menu/style.rs`：父 `menu.rs` 从 97 行降为 5 行 structural export entry，并保持 `template_popup_rows.rs` 对 `push_menu_row_commands` 与测试对 `popup_menu_row_style` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、popup-row menu entry/style ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes popup-row option entry/style subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_popup_rows/options.rs` 中 option popup placement、row iteration、surface/label/check-adornment command ordering 和 option selected/special `WorkbenchPopupRowState` style projection 分别拆入 `options/entry.rs` 与 `options/style.rs`：父 `options.rs` 从 87 行降为 5 行 structural export entry，并保持 `template_popup_rows.rs` 对 `push_option_row_commands` 与测试对 `popup_option_row_style` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、popup-row option entry/style ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes popup-row adornment glyph symbol check/chevron/plus/trash subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_popup_row_adornments/glyphs/symbols.rs` 中 check、chevron、plus、trash 四类 popup adornment glyph segment layouts 分别拆入 `symbols/check.rs`、`chevron.rs`、`plus.rs` 与 `trash.rs`：父 `symbols.rs` 从 89 行降为 9 行 structural export entry，并保持 `dispatch.rs` 对 `push_*_adornment` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、popup-row adornment glyph symbol check/chevron/plus/trash ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene architecture door back/core/palette subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_architecture/doors.rs` 中 back-door base/inset/crossbar command emission、door-core base/inset/spine command emission 和 door inset light/shadow palette constants 分别拆入 `doors/back.rs`、`core.rs` 与 `palette.rs`：父 `doors.rs` 从 89 行降为 6 行 structural export entry，并保持 `template_viewport_scene_architecture.rs` 对 `push_back_door` 与 `push_door_core` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene architecture door back/core/palette ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command draw image entry/pixel/placeholder subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/render_commands/draw/image.rs` 中 atlas/resource image replay、opacity-adjusted RGBA pixel generation 和 placeholder fallback fill/highlight/border replay 分别拆入 `image/entry.rs`、`pixels.rs` 与 `placeholder.rs`：父 `image.rs` 从 89 行降为 5 行 structural export entry，并保持 `draw/dispatch.rs` 对 `image::draw_image_command` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、render-command draw image entry/pixel/placeholder ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes material feedback progress entry/linear subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_material_feedback/progress.rs` 中 circular-vs-linear routing 和 linear progress track/fill/indeterminate bar command emission 分别拆入 `progress/entry.rs` 与 `progress/linear.rs`：父 `progress.rs` 从 85 行降为 4 行 structural export entry，并保持 `template_material_feedback.rs` 对 `progress::push_material_progress_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、material feedback progress entry/linear ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes slider geometry rect alignment/range/track/value subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_slider_geometry/rects.rs` 中 centered/pixel-aligned helpers、range-min value rect、track rect 和 current value rect 分别拆入 `rects/alignment.rs`、`range.rs`、`track.rs` 与 `value.rs`：父 `rects.rs` 从 92 行降为 10 行 structural export entry，并保持 `template_slider_geometry.rs` 对 `rects::{centered_rect, pixel_aligned_rect, slider_range_min_value_rect, slider_track_rect, slider_value_rect}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、slider geometry rect alignment/range/track/value ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes dialog style color action/border/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_dialogs/style/colors.rs` 中 action color、border color、surface color 和 title/body text color rules 分别拆入 `colors/actions.rs`、`border.rs`、`surface.rs` 与 `text.rs`：父 `colors.rs` 从 87 行降为 12 行 structural export entry，并保持 `style.rs` 对 `colors::{cancel_action_color, confirm_action_color, dialog_action_color, dialog_body_color, dialog_border_color, dialog_surface_color, dialog_title_color}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、dialog style color action/border/surface/text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes status-control signal command/metric/test-accessor subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_status_controls/signals.rs` 中 status signal command emission、mark-width projection 和 test-only color accessors 分别拆入 `signals/commands.rs`、`metrics.rs` 与 `test_accessors.rs`：父 `signals.rs` 从 89 行降为 11 行 structural export entry，并保持 `template_status_controls.rs` 与 `template_status_controls/commands.rs` 对 `signals::{push_status_signal_item, status_signal_mark_width, status_signal_icon_fill, status_signal_text_color, status_signal_mark_color}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、status-control signal command/metric/test-accessor ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes property-row field axis/scalar/surface subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_property_rows/fields.rs` 中 scalar field surface/text sequencing、axis label/field/value sequencing 和 shared value-field surface quad emission 分别拆入 `fields/scalar.rs`、`axis.rs` 与 `surface.rs`：父 `fields.rs` 从 83 行降为 6 行 structural export entry，并保持 `template_property_rows/commands.rs` 对 `fields::{push_scalar_value_commands, push_axis_value_commands}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、property-row field axis/scalar/surface ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes status-control geometry signal constant/icon/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_status_control_geometry/signals.rs` 中 signal geometry constants、icon rect/paint rect 计算和 text rect/text gap 计算分别拆入 `signals/constants.rs`、`icon.rs` 与 `text.rs`：父 `signals.rs` 从 91 行降为 10 行 structural export entry，并保持 `template_status_control_geometry.rs` 对 `signals::{status_signal_icon_paint_rect, status_signal_icon_rect, status_signal_text_rect, status_signal_text_gap}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、status-control geometry signal constant/icon/text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes segmented-control style segmented/tab subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_segmented_controls/style.rs` 中 segmented-control style accessors/test helpers 和 Workbench tab style accessors 分别拆入 `style/segmented.rs` 与 `style/tabs.rs`：父 `style.rs` 从 96 行降为 16 行 structural export entry，并保持 `template_segmented_controls.rs`、`labels.rs` 与 `tabs.rs` 对 `style::{segment_text_color, segmented_group_label_color, segmented_control_style, tab_style, tab_text_color, ...}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、segmented-control style segmented/tab ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes slider command sequence entry/label/thumb/track/value subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_sliders/commands/sequence.rs` 中 ready-context fan-out、label command、track/tick command、range/current thumb command 和 range/current value command sequencing 分别拆入 `sequence/entry.rs`、`label.rs`、`track.rs`、`thumbs.rs` 与 `values.rs`：父 `sequence.rs` 从 97 行降为 6 行 structural export entry，并保持 `commands/entry.rs` 对 `sequence::push_ready_slider_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、slider command sequence entry/label/thumb/track/value ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes alert toast entry/action/icon/surface/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_alerts/toast.rs` 中 Toast style lookup/command ordering、surface quad、success mark/mark-size、label text 和 UNDO action/close mark emission 分别拆入 `toast/entry.rs`、`surface.rs`、`icon.rs`、`text.rs` 与 `action.rs`：父 `toast.rs` 从 90 行降为 8 行 structural export entry，并保持 `template_alerts/commands.rs` 对 `toast::push_toast` 与测试对 `toast::toast_status_mark_size` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、alert toast entry/action/icon/surface/text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes icon-button glyph dispatch entry/family subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_icon_button_glyph_shapes/dispatch.rs` 中 `IconButtonGlyphKind` 顶层 family grouping 和 file/tool/chrome/action/asset/visibility family fan-out 分别拆入 `dispatch/entry.rs`、`files.rs`、`tools.rs`、`chrome.rs`、`actions.rs`、`assets.rs` 与 `visibility.rs`：父 `dispatch.rs` 从 91 行降为 8 行 structural export entry，并保持 `template_icon_button_glyph_shapes/mod.rs` 对 `dispatch::push_icon_button_glyph_shape` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、icon-button glyph dispatch entry/family ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene cargo inner-frame edge/entry/grid/palette subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_props/cargo/inner_frame.rs` 中 inner-frame edge/grid sequencing、edge line emission、vertical divider/horizontal grid emission 和 inner-frame line/corner palette constants 分别拆入 `inner_frame/entry.rs`、`edges.rs`、`grid.rs` 与 `palette.rs`：父 `inner_frame.rs` 从 87 行降为 5 行 structural export entry，并保持 `cargo.rs` 对 `inner_frame::push_cargo_inner_frame` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene cargo inner-frame edge/entry/grid/palette ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive avatar geometry child/frame/metric/radius/text subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/avatar/geometry.rs` 中 Avatar root frame placement、corner-radius rules、text frame/font-size calculation、fallback child centering 和 geometry constants 分别拆入 `geometry/frame.rs`、`radius.rs`、`text.rs`、`child.rs` 与 `metrics.rs`：父 `geometry.rs` 从 86 行降为 9 行 structural export entry，并保持 `avatar/commands.rs`、`image.rs` 与 `text.rs` 对 `geometry::{avatar_frame, avatar_corner_radius, avatar_fallback_child_frame, avatar_text_frame}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive avatar geometry child/frame/metric/radius/text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive chip leading edge/frame/margin subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/chip/geometry/leading.rs` 中 avatar/icon edge sizing、avatar/icon slot frame placement、leading margin 和 negative slot margin rules 分别拆入 `leading/edge.rs`、`frame.rs` 与 `margin.rs`：父 `leading.rs` 从 86 行降为 10 行 structural export entry，并保持 `geometry.rs`、`geometry/label.rs` 与 `chip/leading.rs` 对 `leading::{chip_avatar_frame, chip_icon_frame, chip_leading_edge, chip_leading_margin, chip_negative_slot_margin}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive chip leading edge/frame/margin ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene identity classify domain subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene/identity/classify.rs` 中 WorkbenchViewport classification sequencing、prefix/chrome/axis-label exclusion、lighting/gizmo/surface/floor/prop/architecture control-id mapping 分别拆入 `classify/entry.rs`、`gate.rs`、`lighting.rs`、`gizmo.rs`、`surfaces.rs`、`floor.rs`、`props.rs` 与 `architecture.rs`：父 `classify.rs` 从 85 行降为 9 行 structural export entry，并保持 `identity.rs` 对 `classify::viewport_scene_kind` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene identity classify domain ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |

| 2026-06-20 | 08 M3.S2 paint_template_nodes material feedback circular entry/geometry/key/pixel subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_material_feedback/circular_progress.rs` 中 raster-size lookup/progress state projection/image command sequencing、square image frame placement、retained image-key formatting 和 ring raster pixel generation 分别拆入 `circular_progress/entry.rs`、`geometry.rs`、`key.rs` 与 `pixels.rs`：父 `circular_progress.rs` 从 83 行降为 5 行 structural export entry，并保持 `progress/entry.rs` 对 `circular_progress::push_circular_progress_command` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、material feedback circular entry/geometry/key/pixel ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes Workbench table-row color action/background/border/declared subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/style_selector/workbench_table_row/colors.rs` 中 row/header/tail/selected/hover background selection、focus border/border-width rules、action color selection 和 declared background/value color extraction 分别拆入 `colors/background.rs`、`border.rs`、`action.rs` 与 `declared.rs`：父 `colors.rs` 从 84 行降为 10 行 structural export entry，并保持 `selection.rs` 对 `colors::{declared_value_color, table_row_action_color, table_row_background, table_row_border, table_row_border_width}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Workbench table-row color action/background/border/declared ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene architecture primitives inset/alpha subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_architecture/primitives.rs` 中 shared inset-rect edge command emission 与 alpha-factor color adjustment 分别拆入 `primitives/inset_rect.rs` 与 `primitives/alpha.rs`：父 `primitives.rs` 从 83 行降为 5 行 structural export entry，并保持 door/stair/panel/wall owners 对 `super::primitives::{color_with_alpha_factor, push_inset_rect}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene architecture primitives inset/alpha ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene floor primitives inset/alpha subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_floor/primitives.rs` 中 shared inset-rect edge command emission 与 alpha-factor color adjustment 分别拆入 `primitives/inset_rect.rs` 与 `primitives/alpha.rs`：父 `primitives.rs` 从 83 行降为 5 行 structural export entry，并保持 floor panel/seam owners 对 `super::primitives::{color_with_alpha_factor, push_inset_rect}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene floor primitives inset/alpha ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes inspector-row shadow select/check subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_inspector_rows/shadow.rs` 中 shadow select label/field/value/chevron command sequencing 与 shadow checkbox label/surface/tick command sequencing 分别拆入 `shadow/select.rs` 与 `shadow/check.rs`：父 `shadow.rs` 从 81 行降为 5 行 structural export entry，并保持 root Inspector row dispatch 对 `shadow::{push_shadow_check_row, push_shadow_select_row}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、inspector-row shadow select/check ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes viewport-scene floor grate entry/palette subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_viewport_scene_floor/grate.rs` 中 repeated grate slot command emission 与 grate stripe/edge color constants 分别拆入 `grate/entry.rs` 与 `grate/palette.rs`：父 `grate.rs` 从 80 行降为 4 行 structural export entry，并保持 floor root 对 `grate::push_floor_grate_slots` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、viewport-scene floor grate entry/palette ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes table-row action glyph gear/kebab subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/template_table_rows/actions/glyphs.rs` 中 gear frame segment emission 与 vertical kebab dot segment emission 分别拆入 `glyphs/gear.rs` 与 `glyphs/kebab.rs`：父 `glyphs.rs` 从 80 行降为 4 行 structural export entry，并保持 action entry 对 `glyphs::{push_table_gear, push_table_kebab}` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、table-row action glyph gear/kebab ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command conversion text entry/runs/shaped/fallback subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/render_command_conversion/text/commands.rs` 中 text color fallback/decorator bracketing/routing、runtime text-run emission、shaped-line emission 和 aligned fallback text emission 分别拆入 `commands/entry.rs`、`runs.rs`、`shaped.rs` 与 `fallback.rs`：父 `commands.rs` 从 79 行降为 6 行 structural export entry，并保持 `render_command_conversion/text.rs` 对 `commands::push_text_paint_commands` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、render-command conversion text entry/runs/shaped/fallback ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes render-command conversion command traversal/element/brush-payload subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/render_command_conversion/commands.rs` 中 host-command vector assembly、runtime command traversal/clip inheritance、paint-element visibility/payload fan-out 和 image-brush handoff/brush quad fallback 分别拆入 `commands/entry.rs`、`command.rs`、`element.rs` 与 `brush_payload.rs`：父 `commands.rs` 从 82 行降为 6 行 structural export entry，并保持 `render_command_conversion.rs` 对 `commands::runtime_render_commands_to_host` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、render-command conversion command traversal/element/brush-payload ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-20 | 08 M3.S2 paint_template_nodes Material primitive paper style color/overlay/shape/state subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；Cargo check 当前被 Runtime 08 ECS 阻塞） | 将 `host_contract/paint_template_nodes/material_primitives/paper/style.rs` 中 background/border color/width rules、dark overlay alpha projection、corner radius rules 和 outlined/elevation state projection 分别拆入 `style/colors.rs`、`overlay.rs`、`shape.rs` 与 `state.rs`：父 `style.rs` 从 79 行降为 12 行 structural export entry，并保持 `paper/commands.rs` 对 `paper_background_color` / `paper_border_color` / `paper_border_width` / `paper_corner_radius` / `paper_dark_overlay` / `paper_elevation` / `paper_is_outlined` 的原导入路径。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、Material primitive paper style color/overlay/shape/state ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level editor Cargo check 未重启，因为当前包级检查在 unrelated Runtime 08 ECS `component_storage` private re-export errors 处被阻塞且未到达 editor diagnostics。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口需等 Runtime 08 ECS 编译阻塞收敛后补跑 editor Cargo check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-primitives image test-owner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/paint_primitives/image.rs` 中 image replay/recording 回归移入 `paint_primitives/image/tests.rs`：父 `image.rs` 从 249 行降为 115 行，只保留生产 draw entry、clip/scale/orchestration 与 paint recording 交接；新增 `tests.rs` 133 行专管 image replay、alpha blend、recorded resource key 和 clip skip 回归，生产 API 不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、paint primitive image test-owner ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 已尝试，300 秒超时且未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome command-stream replay command/image/shape split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/chrome_command_stream/replay.rs` 中 command-kind dispatch、image fallback/atlas draw 和 quad/border draw 分别拆入 `replay/commands.rs`、`replay/commands/images.rs` 与 `replay/commands/shapes.rs`：父 `replay.rs` 从 203 行降为 31 行，只保留 stable z ordering、full replay 与 damage-region repaint orchestration；子 owner 分别为 38、64、110 行，保持 software replay 行为与外部入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、chrome command-stream replay command/image/shape ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 已尝试，300 秒超时且未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract data world-space submission builder/model/test split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/data/world_space_submission.rs` 中 world-space submission DTO、pane/scene traversal builder、extent projection helper 和 inline tests 分别拆入 `world_space_submission/model.rs`、`builder.rs` 与 `tests.rs`：父 `world_space_submission.rs` 从 273 行降为 11 行 structural export/test hook entry；子 owner 分别为 29、148、94 行，保持 `data::*` 现有 re-export 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、host_contract data world-space submission builder/model/test ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 已在本轮尝试，300 秒超时且未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract data pane DTO subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/data/panes.rs` 中各类 Pane DTO 分到 `panes/{pane,viewport,hierarchy,inspector,animation,runtime_diagnostics,performance_timeline,module_plugins,build_export,basic}.rs`：父 `panes.rs` 从 284 行降为 21 行 structural export entry；最大子 owner 为 57 行，保持 `data::*` re-export 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、host_contract data panes DTO subtree ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 已在本轮尝试，300 秒超时且未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract data host-components DTO subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/data/host_components.rs` 中 window shell/layout、tab/frame common、dock surface、floating window、menu/page/status chrome 和 shell metrics DTO 分到 `host_components/{common,window,docks,floating,menus,metrics}.rs`：父 `host_components.rs` 从 280 行降为 13 行 structural export entry；最大子 owner 为 83 行，保持 `data::*` re-export 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、host_contract data host-components DTO subtree ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 已在本轮尝试，300 秒超时且未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-primitives shape rect/border split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/paint_primitives/shapes.rs` 中 rect/rounded-rect draw、quad recording、border segment emission 和 rounded-border recording/fill 分到 `shapes/rects.rs` 与 `shapes/borders.rs`：父 `shapes.rs` 从 202 行降为 9 行 structural export entry；子 owner 分别为 89、114 行，保持 `paint_primitives` 对外 draw 函数入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、paint primitive shape rect/border ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 沿用本轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 的 300 秒超时结果，未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-primitives pixel span/fill/border/geometry/test split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/paint_primitives/pixels.rs` 中 span fill、alpha blend、rect/rounded fill、rounded border fill、rounded-rect containment/clamp/inset helper 与 inline regression 分到 `pixels/{span,fill,border,geometry,tests}.rs`：父 `pixels.rs` 从 180 行降为 14 行 structural export entry；子 owner 分别为 53、35、28、63、6 行，保持 `paint_primitives`/shape caller 对 pixel helper 的导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、paint primitive pixel span/fill/border/geometry/test ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 沿用本轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 的 300 秒超时结果，未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome-command-stream stream model/push/geometry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/chrome_command_stream/stream.rs` 中 stream 状态/访问器、push helper/command construction、surface/frame validation 分到 `stream/{model,push,geometry}.rs`：父 `stream.rs` 从 172 行降为 5 行 structural export entry；子 owner 分别为 45、128、14 行，保持 `ChromeCommandStream` 与 `clamp_surface_size` 的原模块导出入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、chrome command-stream stream model/push/geometry ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 沿用本轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 的 300 秒超时结果，未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome-command-stream extraction entry/command/image/model/visibility split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/chrome_command_stream/extraction.rs` 中 recorded-command extraction、layer/kind conversion、image payload/atlas conversion、DTO 和 visible-frame filter 分到 `extraction/{entry,command,image,model,visibility}.rs`：父 `extraction.rs` 从 146 行降为 9 行 structural export entry；子 owner 分别为 24、94、47、6、9 行，保持 `extract_chrome_commands`、`ChromeCommandExtraction` 和 test hook 的原模块导出入口不变。 | `cargo fmt -p zircon_editor --check`、chrome command-stream extraction entry/command/image/model/visibility ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片首次 `cargo fmt -p zircon_editor` 在 120 秒窗口超时但 touched files 已格式化，package-level Cargo check 沿用本轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 的 300 秒超时结果，未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract data template-node DTO subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 超时） | 将 `host_contract/data/template_nodes.rs` 中 aggregate template node、frame、action、option、collection field、menu item 和 context-menu request DTO 分到 `template_nodes/{node,frame,actions,options,collection,menu,context_menu}.rs`：父 `template_nodes.rs` 从 230 行降为 14 行 structural export entry；最大子 owner 为 158 行，保持 `data::*` re-export 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、data template-node DTO subtree ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 沿用本轮 `cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 的 300 秒超时结果，未产生可执行 editor 诊断。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window text-input edit/keyboard split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/window/text_input.rs` 中 focused text edit/value dispatch 与 keyboard/popup fallback/unhandled input forwarding 分别拆入 `text_input/edit.rs` 与 `text_input/keyboard.rs`：父 `text_input.rs` 从 201 行降为 2 行 structural entry；子 owner 各 103 行，保持 `UiHostWindow` 现有 text input/keyboard 方法入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、window text-input edit/keyboard ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling artifact pane-frame subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/profiling_artifacts/geometry/pane_frames.rs` 中 activity rail button frame collection、side/floating content frame math、pane-level toolbar/template sequencing、surface-frame top-hit filtering 和 dispatchable template-node projection 分到 `pane_frames/{activity_rail,content,pane,surface_frame,template_nodes}.rs`：父 `pane_frames.rs` 从 252 行降为 12 行 structural entry；最大子 owner 为 79 行，保持 `geometry.rs` 对 pane-frame helper 的原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、profiling artifact pane-frame activity/content/pane/surface/template ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome routing subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome.rs` 中 splitter/root route ordering、document/drawer/host-page tab routing、activity-rail containment 和 floating-window header/tab routing 分到 `chrome/{top_level,tabs,rails,floating}.rs`：父 `chrome.rs` 从 232 行降为 5 行 structural entry；最大子 owner 为 94 行，保持 `route_top_level_chrome` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome routing top-level/tabs/rails/floating ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome floating header split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/floating.rs` 中单个 floating-window header containment、document-tab priority 和 fallback route construction 分到 `floating/header.rs`：父 `floating.rs` 从 36 行降为 19 行 floating-window iteration owner；新增子 owner 为 28 行，保持 `route_floating_window_header` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome floating header ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome rail split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/rails.rs` 中 activity-rail frame containment 与 translated button hit testing 分到 `rails/{frame,hit}.rs`：父 `rails.rs` 从 46 行降为 26 行 route entry；最大新子 owner 为 31 行，保持 `route_activity_rail` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome rail frame/hit ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane routing subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes.rs` 中 floating/local pane route iteration、pane body/toolbar/template hit construction、pointer-move template-hit gating 和 pane-kind target mapping 分到 `panes/{entry,pane,mode,target}.rs`：父 `panes.rs` 从 164 行降为 7 行 structural entry；最大子 owner 为 80 行，保持 `route_pointer_to_pane` 与 `route_pointer_move_to_pane` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane routing entry/mode/pane/target ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane routing entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes/entry.rs` 中 public default/pointer-move route entry points、floating-window pane priority 和 document/side/bottom dock pane sequence 分到 `entry/{route,floating,local}.rs`：父 `entry.rs` 从 84 行降为 8 行 structural entry；最大新子 owner 为 50 行，保持 `route_pointer_to_pane` 与 `route_pointer_move_to_pane` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane routing entry route/floating/local ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane routing local dock split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes/entry/local.rs` 中 document/side/bottom dock pane routing 分到 `local/{document,side,bottom}.rs`：`local.rs` 从 50 行降为 33 行，只保留本地 dock 命中顺序；三个新子 owner 均为 23 行，保持 `route_local_dock_pane` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane routing local dock document/side/bottom ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes/pane.rs` 中 pane body/template-node route construction 与 viewport toolbar route construction 分到 `pane/{entry,toolbar}.rs`：父 `pane.rs` 从 67 行降为 4 行 structural entry；最大新子 owner 为 47 行，保持 `pane_route_from_pane` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane route entry/toolbar ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane route viewport-body split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes/pane/entry.rs` 中 Scene/Game viewport toolbar priority 与 body-frame adjustment 拆入 `entry/viewport_body.rs`：`entry.rs` 从 45 行降为 37 行，新增子 owner 为 42 行，保持 `pane_route_from_pane` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane route viewport body ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane route viewport-body child split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes/pane/entry/viewport_body.rs` 中 Scene/Game toolbar eligibility/frame/hit routing、body-frame adjustment 和 `ViewportBodyRoute` DTO 分到 `viewport_body/{toolbar,body,model}.rs`：父 `viewport_body.rs` 从 42 行降为 22 行 structural entry；最大新子 owner 为 26 行，保持 `viewport_body_route` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane route viewport body toolbar/body/model ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane route entry template-hit split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/panes/pane/entry.rs` 中 pointer-move template-node eligibility/hit-test/route construction 分到 `entry/template.rs`：父 `entry.rs` 从 37 行降为 29 行 pane route sequencing owner；新增子 owner 为 22 行，保持 `pane_route_from_pane` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane route entry template-hit ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag lifecycle/payload split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/drag_resize/tab_drag.rs` 中 tab-drag arming/move/release lifecycle 与 route-to-tab/source-group payload resolution 分别拆入 `tab_drag/lifecycle.rs` 与 `tab_drag/payload.rs`：父 `tab_drag.rs` 从 182 行降为 5 行 structural entry；子 owner 分别为 89、98 行，保持 `arm_native_tab_drag`、`dispatch_native_tab_drag_move`、`finish_native_tab_drag` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag lifecycle/payload ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer resize capture split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/drag_resize/resize_capture.rs` 中 resize capture arming、active move forwarding/redraw 和 pointer-up cleanup/redraw 分到 `resize_capture/{arm,move_event,finish}.rs`：父 `resize_capture.rs` 从 64 行降为 7 行 structural entry；最大新子 owner 为 30 行，保持 `arm_native_resize`、`dispatch_native_resize_move` 与 `finish_native_resize` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer resize capture arm/move/finish ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag lifecycle split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/drag_resize/tab_drag/lifecycle.rs` 中 tab-drag arming、drag-start threshold/down-move forwarding 和 release redraw/cleanup 分到 `lifecycle/{arm,move_event,finish}.rs`：父 `lifecycle.rs` 从 96 行降为 7 行 structural entry；最大新子 owner 为 44 行，保持 `arm_native_tab_drag`、`dispatch_native_tab_drag_move` 与 `finish_native_tab_drag` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag lifecycle arm/move/finish ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag move event split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/drag_resize/tab_drag/lifecycle/move_event.rs` 中 drag-start threshold gating/initial down callback 和 active drag pointer move forwarding 分到 `move_event/{start,active}.rs`：父 `move_event.rs` 从 44 行降为 31 行 move-event entry；最大新子 owner 为 28 行，保持 `dispatch_native_tab_drag_move` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag move event active/start ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag payload split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/drag_resize/tab_drag/payload.rs` 中 chrome-route matching、document/floating tab lookup 和 drawer tab lookup 分到 `payload/{route,document,drawer}.rs`：父 `payload.rs` 从 100 行降为 5 行 structural entry；最大新子 owner 为 45 行，保持 `tab_drag_payload_for_route` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag payload route/document/drawer ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag payload child split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/drag_resize/tab_drag/payload/document.rs` 中 root/floating document tab lookup 分到 `document/{root,floating}.rs`，并将 `payload/drawer.rs` 中 left/right/bottom drawer tab lookup 分到 `drawer/{left,right,bottom}.rs`：`document.rs` 从 45 行降为 16 行 structural entry，`drawer.rs` 从 45 行降为 20 行 structural entry；最大新子 owner 为 27 行，保持 `document_tab_drag_payload` 与 `drawer_tab_drag_payload` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag payload document root/floating and drawer side ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer routing route payload split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing.rs` 中 `ChromePointerRoute`、`PanePointerRoute` 与 `PanePointerTarget` payload declarations 分到 `routing/{chrome_route,pane_route}.rs`：父 `routing.rs` 从 95 行降为 17 行 structural entry；最大新子 owner 为 48 行，保持 route helper 与 route type 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer routing route payload ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer routing pane route payload split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/pane_route.rs` 中 `PanePointerRoute` DTO 与 `PanePointerTarget` enum 拆入 `pane_route/{route,target}.rs`：`pane_route.rs` 从 45 行降为 4 行 structural entry；最大新子 owner 为 27 行，保持 `routing.rs` 对 `PanePointerRoute` 与 `PanePointerTarget` 的原 re-export 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer routing pane route route/target ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer routing geometry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/geometry.rs` 中 point-in-frame containment、translated frame、side-dock content frame 与 floating-window content frame 计算分到 `geometry/{containment,frames}.rs`：父 `geometry.rs` 从 50 行降为 7 行 structural entry；最大新子 owner 为 37 行，保持 `contains`、`translated`、`side_dock_content_frame` 与 `floating_window_content_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer routing geometry containment/frames ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome top-level route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/top_level.rs` 中 root chrome route ordering、drawer-header route sequencing 和 resize splitter hit routing 分到 `top_level/{entry,drawers,resize}.rs`：父 `top_level.rs` 从 98 行降为 5 行 structural entry；最大新子 owner 为 64 行，保持 `route_top_level_chrome` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome top-level entry/drawer/resize ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome top-level drawer split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/top_level/drawers.rs` 中 side-dock drawer header routing 与 bottom-dock drawer header routing 分到 `drawers/{side,bottom}.rs`：父 `drawers.rs` 从 39 行降为 20 行 drawer-header sequence owner；最大新子 owner 为 21 行，保持 `route_drawer_headers` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome top-level drawer side/bottom ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome top-level docked/side-rail split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/top_level/entry.rs` 中 document-dock tab routing 与左右 activity-rail routing 分到 `top_level/{docked,side_rails}.rs`：`entry.rs` 从 64 行降为 38 行，只保留 root chrome route ordering；最大新子 owner 为 30 行，保持 `route_top_level_chrome` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome top-level docked/side-rail ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome tabs route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/tabs.rs` 中 document tab/close route construction、drawer-header tab route construction 和 host-page tab route construction 分到 `tabs/{document,drawer,host_page}.rs`：父 `tabs.rs` 从 90 行降为 7 行 structural entry；最大新子 owner 为 44 行，保持 `route_document_tabs`、`route_drawer_header` 与 `route_host_page_tabs` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome tabs document/drawer/host-page ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome document tab split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/routing/chrome/tabs/document.rs` 中 document close-button hit route 与 tab-body hit route 分到 `document/{close,body}.rs`：父 `document.rs` 从 44 行降为 28 行 route-order entry；两个新子 owner 均为 29 行，保持 `route_document_tabs` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome document tab body/close ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch.rs` 中 root button event ordering、capture release、close-prompt blocking/dispatch、menu primary-click damage、top-level chrome press sequencing 和 Workbench template/context-menu routing 分到 `button_dispatch/{entry,release,close_prompt,menu_press,chrome_route,workbench}.rs`，并扩展 `text_focus.rs` 负责 primary press text-focus cleanup：父 `button_dispatch.rs` 从 173 行降为 12 行 structural entry；最大新子 owner 为 92 行，保持 `dispatch_native_pointer_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch entry/release/menu/chrome/workbench ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer close prompt action dispatch split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/close_prompt.rs` 中 primary-press gating、UiHostContext close-prompt callback invocation 和 close-prompt damage result mapping 分到 `close_prompt/action.rs`：父 `close_prompt.rs` 从 36 行降为 35 行 hit-blocking/public dispatch owner；新增子 owner 为 27 行，保持 `dispatch_close_prompt_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer close prompt action dispatch ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer text focus split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/text_focus.rs` 中 template text-input focus activation 与 primary-press focused-text cleanup 分到 `text_focus/{activation,clear}.rs`：父 `text_focus.rs` 从 44 行降为 5 行 structural entry；最大新子 owner 为 26 行，保持 `focus_template_node_text_input` 与 `clear_focused_text_input_on_primary_press` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer text focus activation/clear ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer Workbench button split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/workbench.rs` 中 Workbench hit routing/button-state ordering、secondary context-menu request/damage、primary text-input focus/template activation 和 release damage 分到 `workbench/{entry,secondary,primary,release}.rs`：父 `workbench.rs` 从 59 行降为 6 行 structural entry；最大新子 owner 为 42 行，保持 `dispatch_workbench_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer workbench button entry/primary/secondary/release ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer Workbench button entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/workbench/entry.rs` 中 primary/secondary pressed dispatch selection 与 release dispatch handoff 分到 `entry/{pressed,released}.rs`：父 `entry.rs` 从 42 行降为 31 行 hit/state entry；最大新子 owner 为 33 行，保持 `dispatch_workbench_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer workbench button entry pressed/released ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer Workbench primary split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/workbench/primary.rs` 中 text-input focus detection/result mapping、template primary activation 和 hit damage union 分到 `primary/{text_input,activation,damage}.rs`：父 `primary.rs` 从 35 行降为 21 行 primary-press entry；最大新子 owner 为 21 行，保持 `dispatch_workbench_primary_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer workbench primary activation/text-input/damage ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome press split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/chrome_press.rs` 中 chrome route-family dispatch、activity rail callback fanout、tab/header callbacks、floating header callback 和 resize arming 分到 `chrome_press/{entry,rails,tabs,floating,resize}.rs`：父 `chrome_press.rs` 从 82 行降为 7 行 structural entry；最大新子 owner 为 71 行，保持 `dispatch_chrome_press` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome press entry/rails/tabs/floating/resize ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome press entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/chrome_press/entry.rs` 中 host-page/document/drawer tab route dispatch 与 activity-rail/floating-header/resize shell route dispatch 分到 `entry/{tabs,shell}.rs`：父 `entry.rs` 从 71 行降为 18 行 structural entry；最大新子 owner 为 62 行，保持 `dispatch_chrome_press` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome press entry tabs/shell ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome press tab route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/chrome_press/entry/tabs.rs` 中 host-page、document tab 与 drawer header tab route dispatch 分到 `entry/tabs/{host_page,document,drawer}.rs`：父 `entry/tabs.rs` 从 62 行降为 13 行 structural entry；最大新子 owner 为 32 行，保持 `dispatch_chrome_tab_press` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome press tab route host/document/drawer ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome press tabs split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/chrome_press/tabs.rs` 中 host-page、document tab/close 与 drawer header tab callback fanout 分到 `tabs/{host_page,document,drawer}.rs`：父 `tabs.rs` 从 68 行降为 7 行 structural entry；最大新子 owner 为 36 行，保持 `dispatch_host_page_tab_press`、`dispatch_document_tab_press` 与 `dispatch_drawer_header_tab_press` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome press tabs host/document/drawer ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome press document tab callback split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/chrome_press/tabs/document.rs` 中 document tab body callback 与 document tab close callback fanout 分到 `tabs/document/{body,close}.rs`：父 `document.rs` 从 35 行降为 25 行 callback-selector entry；两个新子 owner 均为 23 行，保持 `dispatch_document_tab_press` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome press document tab body/close callback ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch root-entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry.rs` 中 primary-press menu/outside-popup/chrome ordering 和 pane hit-routing handoff 分到 `button_dispatch/{primary_press,pane_route}.rs`：父 `entry.rs` 从 102 行降为 86 行；最大新子 owner 为 31 行，保持 `dispatch_native_pointer_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch root entry primary/pane-route ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch body route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry.rs` 中 Workbench body routing、pane route handoff 和 cleared-text fallback damage 分到 `entry/body_routes.rs`：`entry.rs` 从 86 行降为 68 行，新子 owner 为 49 行，保持 `dispatch_native_pointer_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch body route ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch root-entry input/release/primary-overlay split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry.rs` 中 button defaulting/presentation capture/viewport button-id lookup、primary-release resize/tab-drag completion gating 和 primary-press overlay dispatch gating 分到 `entry/{input,release_capture,primary_overlays}.rs`：`entry.rs` 现为 70 行 route sequence；最大新子 owner 为 22 行，保持 `dispatch_native_pointer_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch root entry input/release/primary-overlay ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch root-entry sequence split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry.rs` 中 root button event ordering 与 perf scope setup 下沉到 `entry/sequence.rs`：父 `entry.rs` 从 70 行降为 6 行 structural entry；新子 owner 为 57 行，保持 `dispatch_native_pointer_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch root entry sequence ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch root-entry sequence child split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry/sequence.rs` 中 perf scope/root input capture 与 release/close-prompt/text-focus/primary-overlay/body-route ordered dispatch 分到 `sequence/{entry,steps}.rs`：父 `sequence.rs` 从 57 行降为 3 行 structural entry；最大新子 owner 为 48 行，保持 `dispatch_native_pointer_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch root entry sequence entry/steps ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch root-entry sequence steps split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry/sequence/steps.rs` 中 close-prompt blocking dispatch、focused-text cleanup dispatch 和 final body-route handoff 分到 `steps/{close_prompt,text_focus,body}.rs`：父 `steps.rs` 从 50 行降为 42 行 ordering entry；最大新子 owner 为 27 行，保持 `dispatch_button_steps` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch root entry sequence steps body/close-prompt/text-focus ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button dispatch root-entry sequence release/primary-overlay split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry/sequence/steps.rs` 中 release-capture completion 与 primary-overlay dispatch handoff 分到 `steps/{release,primary_overlays}.rs`：父 `steps.rs` 从 42 行降为 38 行 ordering entry；最大新子 owner 为 26 行，保持 `dispatch_button_steps` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button dispatch root entry sequence steps release/primary-overlay ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer button body route subtree split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/entry/body_routes.rs` 中 public input capture、shared route input model、Workbench-first sequence、Workbench handoff、pane-route handoff 和 cleared-text fallback damage 分到 `body_routes/{entry,input,sequence,workbench,pane,fallback}.rs`：父 `body_routes.rs` 从 49 行降为 8 行 structural entry；最大新子 owner 为 30 行，保持 `dispatch_body_button_routes` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer button body route entry/input/sequence/workbench/pane/fallback ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry frame split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/frames.rs` 中 menu chrome/top-bar fallback、shell content extents/popup blocking、root popup constraints、menu row metrics 和 submenu constraints 分到 `frames/{chrome,content,popup,row,submenu}.rs`：父 `frames.rs` 从 167 行降为 17 行 structural entry；最大新子 owner 为 49 行，保持 `menu_geometry::{bar,popup,damage}` 对 frame helper 的原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry frame chrome/content/popup/row/submenu ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry content split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/frames/content.rs` 中 popup blocking frame construction 和 shell content dimensions 分到 `content/{blocking,dimensions}.rs`：`content.rs` 从 46 行降为 6 行 structural entry；最大新子 owner 为 22 行，保持 `frames.rs` 对 `popup_blocking_frame`、`shell_content_width`、`shell_content_height` 的原 re-export 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry content blocking/dimensions ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/tab_drag_damage.rs` 中 release damage group routing、floating-window target lookup、local/document-edge group classification、same-group/center-status region assembly 和 visible-frame union math 分到 `tab_drag_damage/{entry,floating,groups,regions,union}.rs`：父 `tab_drag_damage.rs` 从 145 行降为 7 行 structural entry；最大新子 owner 为 45 行，保持 `tab_drag_release_damage_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag damage entry/floating/groups/regions/union ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag damage entry target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/tab_drag_damage/entry.rs` 中 cross-group local/local、document-edge、floating-target 和 floating-to-local release-damage classification 分到 `entry/target.rs`：父 `entry.rs` 从 35 行降为 16 行 source/target capture + same-group owner；新增子 owner 为 29 行，保持 `tab_drag_release_damage_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag damage entry cross-group target ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag damage floating split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/tab_drag_damage/floating.rs` 中 floating-window target-group/edge-group frame lookup 与 floating-target damage assembly 分到 `floating/{group,target}.rs`：父 `floating.rs` 从 42 行降为 5 行 structural entry；最大新子 owner 为 24 行，保持 `floating_group_frame` 与 `floating_target_damage_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag damage floating group/target ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag damage regions split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/tab_drag_damage/regions.rs` 中 same-group release damage 与 center-band/status damage 分到 `regions/{same_group,center_status}.rs`：父 `regions.rs` 从 45 行降为 6 行 structural entry；最大新子 owner 为 26 行，保持 region helper 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag damage regions same-group/center-status ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer tab-drag damage union visible split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/tab_drag_damage/union.rs` 中 visible-frame filtering 与 union accumulation 分到 `union/visible.rs` 并复用共享 `frame_geometry` helper：父 `union.rs` 从 36 行降为 3 行 structural entry；新增子 owner 为 21 行，保持 `union_visible_frame` 与 `visible_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer tab-drag damage union visible ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/chrome_damage.rs` 中 chrome route damage family selection、floating document-tab/header damage、host-page tab/project/template/status union damage 和 visible-frame union math 分到 `chrome_damage/{entry,route,floating,host_page,union}.rs`：父 `chrome_damage.rs` 从 141 行降为 7 行 structural entry；最大新子 owner 为 47 行，保持 `chrome_press_damage_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome damage entry/route/floating/host-page/union ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome damage route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/chrome_damage/route.rs` 中 activity-rail/document-dock/drawer-dock frame selection、root-document/floating-document tab routing、floating-window-header routing 和 host-page tab routing 分到 `route/{dock,document,floating,host_page}.rs`：父 `route.rs` 从 41 行降为 29 行 route-kind dispatch entry；最大新子 owner 为 34 行，保持 `route_chrome_press_damage_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome damage route dock/document/floating/host-page ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome damage dock drawer split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/chrome_damage/route/dock.rs` 中 drawer surface-key 到 dock frame 的映射分到 `dock/drawer.rs`：父 `dock.rs` 从 39 行降为 35 行 activity/document dock damage owner；新增子 owner 为 17 行，保持 `route_drawer_header_damage_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome damage dock drawer ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome damage union visible split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/chrome_damage/union.rs` 中 visible-frame filtering 与 union accumulation 分到 `union/visible.rs` 并复用共享 `frame_geometry` helper：父 `union.rs` 从 36 行降为 3 行 structural entry；新增子 owner 为 21 行，保持 `union_visible_frame` 与 `visible_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome damage union visible ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer chrome host-page damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/chrome_damage/host_page.rs` 中 host-page tab/project damage、page chrome template-node damage conversion 和 center-band/status damage accumulation 分到 `host_page/{tabs,template_nodes,status}.rs`：父 `host_page.rs` 从 47 行降为 19 行 structural entry；最大新子 owner 为 28 行，保持 `host_page_tab_damage_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer chrome host-page tabs/template/status ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer move dispatch split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/move_dispatch.rs` 中 resize/tab-drag capture ordering、menu hover dispatch、Workbench template hover/structured-row mapping、pane/asset/template/viewport move callback fanout 和 final hover cleanup 分到 `move_dispatch/{entry,menu,workbench,pane,clear}.rs`：父 `move_dispatch.rs` 从 141 行降为 7 行 structural entry；最大新子 owner 为 82 行，保持 `dispatch_native_pointer_move` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer move dispatch entry/menu/workbench/pane/clear ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer move dispatch entry body/capture split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/move_dispatch/entry.rs` 中 resize/tab-drag capture priority 与 menu/Workbench/pane/final-clear hover route order 分到 `entry/{capture,body}.rs`：父 `entry.rs` 从 35 行降为 21 行 perf-scope/top-level ordering entry；最大新子 owner 为 23 行，保持 `dispatch_native_pointer_move` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer move dispatch entry body/capture ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer Workbench move hover split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/move_dispatch/workbench.rs` 中 structured-row hover state writes 与 ordinary template-node hover state writes 分到 `workbench/hover.rs`：父 `workbench.rs` 从 40 行降为 25 行 Workbench move route/redraw owner；新增子 owner 为 21 行，保持 `dispatch_workbench_template_pointer_move` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer move dispatch workbench hover ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane move target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/move_dispatch/pane.rs` 中 pane pointer-move route/redraw sequencing、native pane move callbacks、asset pane move callbacks、template hover state、viewport move forwarding 和 inert hover cleanup 分到 `pane/{entry,native,asset,template,viewport,passive}.rs`：父 `pane.rs` 从 82 行降为 8 行 structural entry；最大新子 owner 为 44 行，保持 `dispatch_pane_pointer_move` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane move target native/asset/template/viewport/passive ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane move entry target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/move_dispatch/pane/entry.rs` 中 pane target-family dispatch 分到 `entry/target.rs`：父 `entry.rs` 从 44 行降为 25 行 route/redraw entry；新子 owner 为 29 行，保持 `dispatch_pane_pointer_move` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane move entry target dispatch ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer viewport toolbar damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/viewport_toolbar_damage.rs` 中 toolbar damage public routing、viewport/status-affecting control classification、center-band/status damage assembly 和 frame visibility/union math 分到 `viewport_toolbar_damage/{entry,target,status,union}.rs`：父 `viewport_toolbar_damage.rs` 从 76 行降为 6 行 structural entry；最大新子 owner 为 47 行，保持 `viewport_toolbar_press_damage_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer viewport toolbar damage entry/status/target/union ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer viewport toolbar union split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/viewport_toolbar_damage/union.rs` 中 optional damage merge、visible-frame predicate 和 visible-frame accumulation 分到 `union/{optional,visible}.rs`：父 `union.rs` 从 47 行降为 5 行 structural entry；最大新子 owner 为 21 行，保持 `union_optional_frames`、`union_visible_frame` 与 `visible_frame` 原导入入口不变，并复用共享 `frame_geometry` union/visibility helper。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer viewport toolbar union optional/visible ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane button damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/pane_button_damage.rs` 中 pane press damage assembly 与 frame visibility/union math 分到 `pane_button_damage/{entry,union}.rs`：父 `pane_button_damage.rs` 从 61 行降为 4 行 structural entry；最大新子 owner 为 47 行，保持 `pane_pointer_press_damage_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane button damage entry/union ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane button union split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/pane_button_damage/union.rs` 中 optional damage merge 与 visible-frame accumulation 分到 `union/{optional,visible}.rs`：父 `union.rs` 从 47 行降为 5 行 structural entry；最大新子 owner 为 15 行，保持 `union_optional_frames` 与 `union_visible_frame` 原导入入口不变，并复用共享 `frame_geometry` union/visibility helper。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane button union optional/visible ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer close prompt damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/close_prompt_damage.rs` 中 close-prompt overlay/dialog damage assembly 与 visible-frame/union math 分到 `close_prompt_damage/{entry,union}.rs`：父 `close_prompt_damage.rs` 从 46 行降为 4 行 structural entry；最大新子 owner 为 36 行，保持 `close_prompt_action_damage_frame` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer close prompt damage entry/union ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer close prompt damage union visible split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/close_prompt_damage/union.rs` 中 visible-frame filtering 与 union accumulation 分到 `union/visible.rs` 并复用共享 `frame_geometry` helper：父 `union.rs` 从 36 行降为 3 行 structural entry；新增子 owner 为 21 行，保持 `union_visible_frame` 与 `visible_frame` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer close prompt damage union visible ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer redraw result split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/redraw_result.rs` 中 pane pointer-move redraw、Workbench template hover redraw、resize redraw、chrome press redraw、tab-drag release redraw 和 hierarchy row damage 分到 `redraw_result/{pointer_move,workbench,resize,chrome,tab_drag,hierarchy}.rs`：父 `redraw_result.rs` 从 132 行降为 12 行 structural entry；最大新子 owner 为 63 行，保持 `pointer_move_redraw`、`workbench_template_node_move_redraw`、`resize_pointer_redraw`、`chrome_press_redraw` 与 `tab_drag_release_redraw` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer redraw result pointer/workbench/resize/chrome/tab-drag/hierarchy ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pointer-move redraw split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/redraw_result/pointer_move.rs` 中 generic pointer-move redraw short-circuit、Hierarchy scroll/row hover damage 和 non-hierarchy template-hover damage merging 分到 `pointer_move/{entry,hierarchy,template}.rs`：父 `pointer_move.rs` 从 63 行降为 5 行 structural entry；最大新子 owner 为 38 行，保持 `pointer_move_redraw` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pointer-move redraw entry/hierarchy/template ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pointer-move hierarchy redraw split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/redraw_result/pointer_move/hierarchy.rs` 中 hierarchy scroll-change damage 与 hierarchy row-hover damage union 分到 `hierarchy/{scroll,row}.rs`：父 `hierarchy.rs` 从 35 行降为 20 行 hierarchy-redraw selector；最大新子 owner 为 27 行，保持 `hierarchy_pointer_move_redraw` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pointer-move hierarchy redraw row/scroll ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer scroll dispatch split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/scroll_dispatch.rs` 中 root scroll ordering、menu scroll callback/damage、native pane scroll callback fanout 和 viewport scroll event forwarding 分到 `scroll_dispatch/{entry,menu,pane,viewport}.rs`：父 `scroll_dispatch.rs` 从 114 行降为 6 行 structural entry；最大新子 owner 为 96 行，保持 `dispatch_native_pointer_scroll` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer scroll dispatch entry/menu/pane/viewport ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer scroll pane target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/scroll_dispatch/pane.rs` 中 pane scroll routing/result ordering、native pane scroll callback fanout、asset pane scroll callback fanout、viewport scroll handoff 和 inert target classification 分到 `pane/{entry,native,asset,viewport_target,passive}.rs`：父 `pane.rs` 从 96 行降为 7 行 structural entry；最大新子 owner 为 63 行，保持 `dispatch_pane_pointer_scroll` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer scroll pane target ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer asset pane scroll split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/scroll_dispatch/pane/asset.rs` 中 AssetTree、AssetContent 与 AssetReference scroll callback fanout 分到 `asset/{tree,content,reference}.rs`：`asset.rs` 从 45 行降为 17 行 structural entry；最大新子 owner 为 21 行，保持 `dispatch_asset_pane_scroll` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer asset pane scroll tree/content/reference ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer native pane scroll split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/scroll_dispatch/pane/native.rs` 中 native pane scroll target selection 与 hierarchy、welcome、console/inspector、browser asset details callback fanout 分到 `native/{entry,hierarchy,welcome,panels,browser}.rs`：父 `native.rs` 从 63 行降为 7 行 structural entry；最大新子 owner 为 33 行，保持 `dispatch_native_pane_scroll` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer native pane scroll hierarchy/welcome/panels/browser ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane callback target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks.rs` 中 pane-button target routing、native/viewport event-kind mapping 和 passive/final pane damage fallback 分到 `pane_callbacks/{target,kind,fallback}.rs`：父 `pane_callbacks.rs` 从 126 行降为 9 行 structural entry；最大新子 owner 为 106 行，保持 `dispatch_pane_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane callback target/kind/fallback ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer native pane button split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/native_panes.rs` 中 hierarchy pointer event/click callback 与 welcome recent click callback 分到 `native_panes/{hierarchy,welcome}.rs`：父 `native_panes.rs` 从 47 行降为 5 行 structural entry；最大新子 owner 为 31 行，保持 `dispatch_hierarchy_button` 与 `dispatch_welcome_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer native pane button hierarchy/welcome ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane callback target-domain split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target.rs` 中 pane-button target ordering、native/asset/viewport/template/passive target selection 分到 `target/{entry,native,asset,viewport,template,passive}.rs`：父 `target.rs` 从 106 行降为 8 行 structural entry；最大新子 owner 为 74 行，保持 `dispatch_pane_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane callback target domain ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane callback viewport target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/viewport.rs` 中 viewport-toolbar target handoff 与 viewport-body target handoff 分到 `target/viewport/{toolbar,body}.rs`：父 `viewport.rs` 从 37 行变为 39 行 viewport-target dispatch entry；最大新子 owner 为 30 行，保持 `dispatch_viewport_pane_target_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane callback viewport target toolbar/body ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane callback target entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry.rs` 中 native/asset callback-only target dispatch 与 viewport/template/passive result-producing target dispatch 分到 `entry/{callback_targets,result_targets}.rs`：`entry.rs` 从 74 行降为 54 行；最大新子 owner 为 49 行，保持 `dispatch_pane_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane callback target entry callback/result ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane callback target entry sequence split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry.rs` 中 pane-button dispatch input capture、public input assembly 与 callback/result/fallback ordering 分到 `entry/{input,sequence}.rs` 和 `entry/sequence/{entry,run}.rs`：`target/entry.rs` 从 54 行降为 5 行 structural entry；最大新子 owner 为 48 行，保持 `dispatch_pane_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane callback target entry input/sequence/run ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer pane callback target sequence-run split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/sequence/run.rs` 中 callback-only fallback damage、result-producing dispatch 和 final fallback damage 分到 `run/{callback,result,fallback}.rs`：父 `run.rs` 从 48 行降为 24 行 ordering entry；最大新子 owner 为 27 行，保持 `dispatch_pane_button_sequence` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer pane callback target sequence run callback/result/fallback ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer result target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/entry/result_targets.rs` 中 public input capture、shared route input model、viewport/template/passive result ordering 与三类 result target handoff 分到 `result_targets/{entry,input,sequence,viewport,template,passive}.rs`：父 `result_targets.rs` 从 49 行降为 8 行 structural entry；最大新子 owner 为 34 行，保持 `dispatch_result_pane_targets` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer result target entry/input/sequence/viewport/template/passive ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer asset pane target split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset.rs` 中 AssetTree/AssetContent/AssetReference target matching 与具体 asset callback handoff 分到 `asset/{entry,tree,content,reference}.rs`：父 `asset.rs` 从 50 行降为 6 行 structural entry；最大新子 owner 为 50 行，保持 `dispatch_asset_pane_target_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer asset pane target entry/tree/content/reference ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer asset pane target route split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/target/asset/entry.rs` 中 AssetTree/AssetContent/AssetReference target matching 下沉到 `asset/{tree,content,reference}.rs`：`entry.rs` 从 50 行降为 25 行，只保留 tree/content/reference route sequencing；最大子 owner 为 30 行，保持 `dispatch_asset_pane_target_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer asset pane target tree/content/reference route ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer viewport pane callback split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/viewport.rs` 中 viewport toolbar button clicks/damage 与 viewport body pointer event forwarding 分到 `viewport/{toolbar,viewport}.rs`：父 `viewport.rs` 从 57 行降为 5 行 structural entry；最大新子 owner 为 43 行，保持 `dispatch_viewport_toolbar_button` 与 `dispatch_viewport_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer viewport pane callback toolbar/body ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer viewport toolbar callback split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/viewport/toolbar.rs` 中 primary-press gating、viewport-toolbar click callback fanout 和 toolbar damage result mapping 分到 `toolbar/{entry,click,damage}.rs`：父 `toolbar.rs` 从 43 行降为 5 行 structural entry；最大新子 owner 为 31 行，保持 `dispatch_viewport_toolbar_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer viewport toolbar entry/click/damage ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer asset pane callback split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes.rs` 中 asset tree click fanout、asset content event/click fanout 和 asset reference event/click fanout 分到 `asset_panes/{tree,content,reference}.rs`：父 `asset_panes.rs` 从 85 行降为 7 行 structural entry；最大新子 owner 为 38 行，保持 `dispatch_asset_tree_button`、`dispatch_asset_content_button` 与 `dispatch_asset_reference_button` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer asset pane callback tree/content/reference ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer asset pane reference callback click split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/button_dispatch/pane_callbacks/asset_panes/reference.rs` 中 AssetReference primary-click gating 与 click callback fanout 分到 `reference/click.rs`：父 `reference.rs` 从 38 行降为 33 行 pointer-event fanout owner；新增子 owner 为 27 行，保持 `dispatch_asset_reference_button` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer asset pane reference callback click ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry damage split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/damage.rs` 中 menu damage rectangle assembly 和 submenu stack-bottom traversal 分到 `damage/{entry,stack}.rs`：父 `damage.rs` 从 91 行降为 6 行 structural entry；最大新子 owner 为 54 行，保持 `menu_damage_frame` 与 `menu_damage_frame_with_state` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry damage entry/stack ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry damage stack split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/damage/stack.rs` 中 single-level submenu branch validation、scroll offset selection、anchor row construction、constrained popup frame construction 和 child item handoff 分到 `damage/stack/level.rs`：父 `stack.rs` 从 41 行降为 30 行 stack-bottom accumulation owner；新增子 owner 为 40 行，保持 `menu_popup_stack_bottom` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry damage stack level ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry damage stack level frame split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/damage/stack/level.rs` 中 scroll-offset selection、anchor row construction、constrained submenu popup-frame construction 和 child-count height conversion 分到 `level/frame.rs`：父 `level.rs` 从 43 行降为 40 行 branch/child handoff owner；新增子 owner 为 29 行，保持 `next_menu_popup_stack_level` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry damage stack level frame ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry damage popup-bottom split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/damage/entry.rs` 中打开 root menu、构造 root popup frame、转交 submenu stack-bottom 的计算拆入 `damage/popup_bottom.rs`：`entry.rs` 从 54 行降为 25 行，新增子 owner 为 39 行，保持 `menu_damage_frame` 与 `menu_damage_frame_with_state` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry damage popup-bottom ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry damage popup-bottom root split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/damage/popup_bottom.rs` 中 opened root-menu lookup、root popup frame construction 和 submenu stack-bottom handoff 分到 `popup_bottom/root.rs`：父 `popup_bottom.rs` 从 37 行降为 13 行 open-menu guard/fallback owner；新增子 owner 为 30 行，保持 `open_menu_popup_bottom` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry damage popup-bottom root ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry popup split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/popup.rs` 中 root popup/blocking-region containment 和 nested submenu hit traversal 分到 `popup/{entry,nested}.rs`：父 `popup.rs` 从 87 行降为 4 行 structural entry；最大新子 owner 为 50 行，保持 `menu_popup_handles_point` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry popup entry/nested ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry popup entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/popup/entry.rs` 中 opened root-menu lookup/root popup frame construction 与 popup blocking-region containment 分到 `entry/{root,blocking}.rs`：父 `entry.rs` 从 44 行降为 21 行 hit-order entry；最大新子 owner 为 42 行，保持 `menu_popup_handles_point` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry popup entry root/blocking ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry popup root split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/popup/entry/root.rs` 中 opened root-menu source lookup 与 constrained root popup frame construction 分到 `root/{source,frame}.rs`：父 `root.rs` 从 42 行降为 22 行 root popup result assembly owner；最大新子 owner 为 34 行，保持 `opened_root_menu_popup` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry popup root source/frame ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu popup submenu-frame split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/popup/nested.rs` 中 nested submenu scroll/anchor/height/constrained popup frame construction 拆入 `popup/submenu_frame.rs`：`nested.rs` 从 50 行降为 41 行，新增子 owner 为 24 行，保持 `menu_popup_handles_point` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry popup submenu-frame ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry nested level split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/popup/nested.rs` 中 single-level submenu branch advancement、nested popup frame construction 和 point containment classification 分到 `nested/level.rs`：父 `nested.rs` 保留 42 行 path traversal owner；新子 owner 为 40 行，保持 `menu_popup_handles_point` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry nested popup level ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-pointer menu geometry nested hit payload split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 沿用超时记录） | 将 `host_contract/native_pointer/menu_geometry/popup/nested/level.rs` 中 `NestedMenuLevelHit` per-depth traversal result DTO 分到 `nested/hit.rs`：`level.rs` 从 40 行降为 35 行 branch/frame/containment owner；新增子 owner 为 8 行，保持 `nested_menu_level_hit` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、native pointer menu geometry nested hit payload ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；package-level Cargo check 未重启，沿用本轮 editor package check 300 秒超时且未产生可执行 editor 诊断的记录。未运行 full Cargo test matrix，按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench dock shell entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/docks.rs` 中 side/document/bottom dock shell fill、header paint、content-frame translation 和 pane handoff 分到 `docks/{side,document,bottom}.rs`：父 `docks.rs` 从 193 行降为 13 行 structural entry；新增子 owner 分别为 87/51/51 行，保持 `docks::draw_side_dock`、`draw_document_dock`、`draw_bottom_dock` 与 `draw_floating_layer` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、workbench dock shell entry ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench dock pane template/fallback split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/docks/pane.rs` 中 pane-kind template-node model selection/draw 与 fallback label selection/clipped text drawing 分到 `pane/{template_nodes,fallback}.rs`：父 `pane.rs` 从 189 行降为 81 行 pane body ordering owner；新增子 owner 分别为 95/26 行，保持 `draw_pane` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、workbench dock pane template/fallback ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench scene layer split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/scene_layers.rs` 中 top/status chrome layers、dock/floating layer dispatch、menu/close-prompt/root overlay 和 resize splitter paint 分到 `scene_layers/{chrome,dock_layer,overlay,resize}.rs`：父 `scene_layers.rs` 从 185 行降为 26 行 host-scene paint-order entry；新增子 owner 分别为 57/65/76/21 行，保持 `draw_host_scene`、`draw_componentized_workbench_window` 与 `draws_componentized_workbench_window` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、workbench scene layer chrome/dock/overlay/resize ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench Welcome main-column frames split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/main_column.rs` 中 hero/status/header/name/location/preview/validation/action frame fallback chain 与 content-width constraint resolution 分到 `welcome/main_column/frames.rs`：父 `main_column.rs` 从 136 行降为 53 行 paint-order owner；新增子 owner 为 149 行，保持 `draw_welcome_main_column` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、workbench welcome main-column frames ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window presentation close-prompt/snapshot/template-hover split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/presentation.rs` 中 close-prompt state mutation/redraw damage、transient hovered template node/row writes/clearing、bootstrap geometry 和 cloned presentation aggregation 分到 `presentation/{close_prompt,template_hover_state,snapshot}.rs`：父 `presentation.rs` 从 171 行降为 63 行 presentation API/rebuild diagnostics owner；新增子 owner 分别为 24/65/37 行，保持 `UiHostWindow` 原方法入口和 `host_presentation_from_state` 导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、window presentation close-prompt/snapshot/template-hover ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window template-hover pane/node/row split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/template_hover.rs` 中 dock/floating pane traversal、pane-kind node-list resolution、matching template-node hover mutation 和 structured option/menu row hover flag rewriting 分到 `template_hover/{panes,nodes,rows}.rs`：父 `template_hover.rs` 从 168 行降为 19 行 clone-overlay entry；新增子 owner 分别为 82/31/68 行，保持 `apply_template_hover_to_presentation` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、window template-hover pane/node/row ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract surface hit-test popup-row menu/option/hit/action split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/surface_hit_test/template_node/popup_rows.rs` 中 structured menu-row hit testing、structured option-row hit testing、popup-row hit payload assembly 和 menu action-id normalization 分到 `popup_rows/{menu,option,hit,action_id}.rs`：父 `popup_rows.rs` 从 154 行降为 35 行 open-popup priority entry；新增子 owner 分别为 46/63/31/30 行，保持 `hit_test_template_popup_rows` 与 `TemplatePopupRowHit` 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、surface hit-test popup-row menu/option/hit/action ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract softbuffer diagnostics counters/overlay/planned-present/summary split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/presenter/softbuffer/diagnostics.rs` 中 present diagnostics planning/repaint-outcome accounting、overlay damage expansion、chrome command-stream perf counters 和 verbose frame/presentation summary formatting 分到 `diagnostics/{planned_present,overlay,counters,summary}.rs`：父 `diagnostics.rs` 从 188 行降为 11 行 structural re-export entry；新增子 owner 分别为 112/22/22/38 行，保持 `plan_present_for_diagnostics`、`damage_with_debug_overlay`、`record_chrome_command_stream_counters` 与 summary helper 原导入入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、softbuffer diagnostics counters/overlay/planned-present/summary ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract GPU presenter lifecycle/present/stats/geometry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/presenter/gpu.rs` 中 construction/resize/cache invalidation、GPU present orchestration、RHI stat/perf-counter fanout 和 clamp/pixel accounting helpers 分到 `gpu/{lifecycle,present,stats,geometry}.rs`：父 `gpu.rs` 从 171 行降为 44 行 state/trait bridge；新增子 owner 分别为 44/62/39/23 行，保持 `GpuChromePresenter` 原构造、present、present_stream、diagnostics/accessor 入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、GPU presenter lifecycle/present/stats/geometry ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract redraw request constructors/query/merge split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/redraw/request.rs` 中 full/region constructor + perf-counter + visible-frame filtering、request predicates/damage/scenario accessors、request coalescing 分到 `request/{constructors,query,merge}.rs`：父 `request.rs` 从 158 行降为 21 行 request model entry；新增子 owner 分别为 54/37/65 行，保持 `HostRedrawRequest` 原导入入口和方法 API 不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、redraw request constructors/query/merge ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract globals pane-context setters/callbacks split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/globals/pane_context.rs` 中 pane state setters/placeholders、direct retained state mutation 和 pane callback registration/invocation macro expansion 分到 `pane_context/{setters,callbacks}.rs`：父 `pane_context.rs` 从 161 行降为 22 行 context wrapper entry；新增子 owner 分别为 114/51 行，保持 `PaneSurfaceHostContext` 原方法 API 不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、globals pane-context setters/callbacks ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling-artifacts geometry assembly split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_artifacts/geometry.rs` 中 resize splitter capture、drawer/floating tab aggregation、activity-rail button aggregation、pane toolbar/template-control traversal、clickable-frame aggregation、layout DTO projection 和 hit-sample batch aggregation 分到 `geometry/{resize_splitters,drawer_tabs,activity_rail_buttons,pane_profile_controls,clickable_frames,layout}.rs`，并在 `hit_samples.rs` 增加 `collect_hit_samples`：父 `geometry.rs` 从 164 行降为 65 行 retained profile assembly entry，保持 `UiProfileGeometry::from_presentation` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、profiling-artifacts geometry assembly ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling-artifacts geometry frame-math split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_artifacts/geometry/frame_math.rs` 中 visible-frame gating、frame/profile intersection、profile sample centers、frame translation 和 named-frame push helpers 分到 `frame_math/{visibility,intersection,center,translation,named}.rs`：父 `frame_math.rs` 从 138 行降为 20 行 structural re-export entry，保持 `frame_math::{...}` 原 helper 导入路径不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、profiling-artifacts geometry frame-math ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract surface hit-test viewport-toolbar production/test split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/surface_hit_test/viewport_toolbar.rs` 中 runtime `UiSurface` 测试夹具和 toolbar 命中回归移动到 `viewport_toolbar_tests.rs`：父 `viewport_toolbar.rs` 从 125 行降为 36 行 production hit-test entry，新增测试 owner 为 89 行，保持 `hit_test_viewport_toolbar` 与 `ViewportToolbarPointerHit` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、surface hit-test viewport-toolbar production/test ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome command-stream push command-family split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/chrome_command_stream/stream/push.rs` 中 quad/border、text、image、clip、bulk extend 和 visible-frame gated command insertion 分到 `push/{shape,text,image,clip,extend,command}.rs`：父 `push.rs` 从 137 行降为 6 行 structural child declaration entry，保持 `ChromeCommandStream::push_*` 原方法 API 不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、chrome command-stream stream push command-family ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract softbuffer present log/submit split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/presenter/softbuffer/present.rs` 中 verbose present diagnostics/log cache mutation 和 softbuffer backbuffer copy/present-with-damage handoff 分到 `present/{log,submit}.rs`：父 `present.rs` 从 126 行降为 62 行 present orchestration entry，新增子 owner 分别为 47/28 行，保持 `SoftbufferHostPresenter::present` 原行为和错误传播不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、softbuffer present log/submit ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling hit-route tab-family split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_hit_routes/tabs.rs` 中 document/drawer/floating/host-page tab route hit checks 和 shared tab row containment 分到 `tabs/{document,drawer,floating,host_page,shared}.rs`：父 `tabs.rs` 从 128 行降为 9 行 structural re-export entry，保持 `document_tab_route_hit`、`drawer_tab_route_hit`、`floating_tab_route_hit` 和 `host_page_tab_route_hit` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、profiling hit-route tab-family ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract world-space submission builder node/pane/scene split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/data/world_space_submission/builder.rs` 中 node projection/extents/sorting、pane-family traversal 和 host-scene dock/floating aggregation 分到 `builder/{node,pane,scene}.rs`：父 `builder.rs` 从 146 行降为 5 行 structural builder entry，保持 `build_world_space_ui_surface_submissions` 与 `build_world_space_ui_surface_submissions_from_host_scene` 原入口不变。 | `cargo fmt -p zircon_editor`、`cargo fmt -p zircon_editor --check`、world-space submission builder node/pane/scene ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract paint-text draw layout/glyph split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_text/draw.rs` 中 fontdue layout setup 与 glyph traversal/pixel loop 分别拆入 `draw/{layout,glyphs}.rs`：父 `draw.rs` 现在只保留 text draw entry、clip/pixel conversion、font-size/line-height clamp、recording handoff 与 draw-order orchestration，根 `paint_text.rs` 的外部导入路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-text draw layout/glyph ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract Welcome main-column frame fallback/model/resolve/sequence split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/main_column/frames.rs` 中 frame payload、fallback geometry/content metrics、template-frame resolve/content constraint 与 resolved-frame sequencing 分别拆入 `frames/{model,fallback,resolve,sequence}.rs`：父 `frames.rs` 从 149 行降为 5 行 structural frame entry，`welcome_main_column_frames(...)` 调用路径不变，后续 fallback 仍基于前一个已解析 frame。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Welcome main-column frame fallback/model/resolve/sequence ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract Welcome recent-projects header/list/empty/rows split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/recent_projects.rs` 中 header frame/copy、list frame/shell、empty state、visible row iteration/status/invalid styling 分别拆入 `recent_projects/{header,list,empty,rows}.rs`：父 `recent_projects.rs` 从 135 行降为 31 行 structural recent-project paint entry，`draw_welcome_recent_projects(...)` 外部入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Welcome recent-projects header/list/empty/rows ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract paint-primitives image draw/pipeline split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_primitives/image.rs` 中 image draw entry variants 与 validation/clip/record/raster orchestration 分别拆入 `image/draw.rs` 和 `image/draw/pipeline.rs`：父 `image.rs` 从 124 行降为 10 行 structural image entry，根 `paint_primitives.rs` 对 image draw API 的 re-export 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint primitive image draw/pipeline ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract paint-primitives image raster identity/scaled/pixel split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_primitives/image/raster.rs` 中 identity mapping/opaque row copy、scaled sampling loop 和 alpha-aware pixel writes 分别拆入 `raster/{identity,scaled,pixel}.rs`：父 `raster.rs` 从 122 行降为 5 行 structural raster entry，draw pipeline 对 raster API 的调用不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint primitive image raster identity/scaled/pixel ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract Workbench menu geometry row/popup/submenu/scroll split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/menus/geometry.rs` 中 popup row frame、popup height/top-level placement、submenu placement、scrolled menu-frame adjustment 和 shared shell bounds 分别拆入 `geometry/{row,popup,submenu,scroll,shell}.rs`：父 `geometry.rs` 从 119 行降为 10 行 structural geometry entry，menu bar 与 popup 调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Workbench menu geometry row/popup/submenu/scroll ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract surface hit-test template-node surface-frame builder dispatch/node/surface split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/surface_hit_test/template_node/surface_frame_builder.rs` 中 dispatchable filtering/component classification、runtime `UiSurface` root/traversal/rebuild 和 per-node `UiTreeNode` metadata/state/clip assembly 分别拆入 `surface_frame_builder/{dispatch,surface,node}.rs`：父 `surface_frame_builder.rs` 从 114 行降为 7 行 structural builder entry，`build_template_surface_frame(...)` 与 `template_nodes_surface_frame(...)` 调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template-node surface-frame builder dispatch/node/surface ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract paint-primitives shape border rect/rounded split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_primitives/shapes/borders.rs` 中 plain border edge frame math 与 rounded-border validation/record/fill/square fallback 分别拆入 `borders/{rect,rounded}.rs`：父 `borders.rs` 从 115 行降为 7 行 structural border entry，`draw_border*` 与 `draw_rounded_border_clipped(...)` re-export 路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint primitive shape border rect/rounded ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract Workbench native pane asset hover overlay/frame split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/native_panes/assets.rs` 中 Asset activity/AssetBrowser hover overlay entry/paint 与 asset tree row matching/frame translation/scroll offset 分别拆入 `assets/{overlay,frame}.rs`：父 `assets.rs` 从 116 行降为 6 行 structural asset-pane overlay entry，两个公开 hover overlay 入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native pane asset hover overlay/frame ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract globals callback host/pane/type subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/globals/callbacks.rs` 中 host/chrome callback storage、pane surface callback storage 与 callback arity aliases 分别拆入 `callbacks/{host,pane,types}.rs`：父 `callbacks.rs` 从 130 行降为 5 行 structural callback registry entry，`HostContractState` 继续通过原 `UiHostCallbacks`/`PaneSurfaceCallbacks` 路径初始化回调注册表。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；globals callback host/pane/type ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract Workbench Welcome main-column form header/field/preview/validation split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/main_column/form.rs` 中 new-project header、labeled field、project-path preview 与 validation message/color paint 分别拆入 `form/{header,field,preview,validation}.rs`：父 `form.rs` 从 117 行降为 8 行 structural form-paint entry，`draw_welcome_*` 调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；welcome main-column form header/field/preview/validation ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract paint-frame recording-frame command/state split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_frame/recording_frame.rs` 中 `HostRgbaFrame` recording command emission 与 recording state/extraction 分别拆入 `recording_frame/{commands,state}.rs`：父 `recording_frame.rs` 从 111 行降为 2 行 structural recording extension entry，`record_*`、`is_recording`、`record_only`、`into_recorded_commands` 方法仍挂在 `HostRgbaFrame`。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-frame recording-frame command/state ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract Workbench Welcome main-column fallback-frame metrics/rects split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/main_column/frames/fallback.rs` 中 content x/width metrics 与 hero/status/header/name/location/preview/validation/action fallback rectangle chain 分别拆入 `fallback/{metrics,rects}.rs`：父 `fallback.rs` 从 112 行降为 7 行 structural fallback-frame entry，frame resolution 顺序和 fallback geometry 数值不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；welcome main-column frame fallback metrics/rects ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract chrome command-stream replay shape quad/border split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/chrome_command_stream/replay/commands/shapes.rs` 中 quad replay 与 rounded/rectangular border replay 分别拆入 `shapes/{quad,border}.rs`：父 `shapes.rs` 从 110 行降为 4 行 structural shape replay entry，`paint_quad_command` 与 `paint_border_command` 调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chrome command-stream replay shape quad/border ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract data host-interaction menu/drag/text-focus/pane/resize split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/data/host_interaction.rs` 中 menu state、tab drag state、focused text-input state、pane hover/scroll state 与 resize capture state 分别拆入 `host_interaction/{menu,drag,text_focus,pane,resize}.rs`：父 `host_interaction.rs` 从 109 行降为 10 行 structural DTO re-export entry，`data::*` 对外 DTO 名称不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；data host-interaction menu/drag/text-focus/pane/resize ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |

| 2026-06-21 | 08 M3.S2 host_contract profiling-artifacts schema frame/geometry/hit/named/tab split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_artifacts/schema.rs` 中 frame/point/size/layout DTO、top-level geometry document、hit samples、named frames 与 tab frames 分别拆入 `schema/{frame,geometry,hit,named,tab}.rs`：父 `schema.rs` 从 106 行降为 12 行 structural schema re-export entry，profile JSON 字段名和导出类型名不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling artifact schema frame/geometry/hit/named/tab ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-template-nodes root re-export split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/mod.rs` 中 `draw_template_nodes`、`has_template_nodes`、`paint_template_nodes_for_test` 与 `paint_template_nodes_for_test_with_background` 的薄 wrapper 改为直接 re-export `template_node_pipeline` 的稳定入口：父 `mod.rs` 从 118 行降为 85 行 structural declaration/re-export entry，保持 Workbench painter 与测试 helper 调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-template-nodes root re-export ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-keyboard dispatch action split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/native_keyboard/dispatch.rs` 中 popup accept/cancel/hover row effect、PaneSurfaceHostContext callback 调用和 hover damage union 拆入 `dispatch/actions.rs`：父 `dispatch.rs` 从 107 行降为 43 行 command dispatcher，保留 active popup target resolution 与 keyboard command/text-search 分派入口。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native keyboard dispatch action subowner ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract UI Asset inspector DTO subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/data/ui_asset/inspector.rs` 中 semantic、slot、layout、binding、widget prop-state、widget 与 inspector panel DTO 分别拆入 `inspector/{semantic,slot,layout,binding,widget,panel}.rs`：父 `inspector.rs` 从 111 行降为 13 行 structural re-export entry，保持 `UiAssetInspector*Data` 类型名与字段语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；UI Asset inspector DTO subowner ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract UI Asset pane DTO subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/data/ui_asset/pane.rs` 中 pane header、action state、designer tool state、collection panels、runtime report 与 aggregate editor pane DTO 分别拆入 `pane/{header,actions,tools,collections,runtime_report,editor}.rs`：父 `pane.rs` 从 98 行降为 13 行 structural re-export entry，保持 `UiAsset*Data` pane 类型名与字段语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；UI Asset pane DTO subowner ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract UI Asset style DTO subowner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/data/ui_asset/style.rs` 中 theme-source、style rule、matched rule、declaration、token、state 与 aggregate style panel DTO 分别拆入 `style/{theme_source,rule,matched_rule,declaration,token,state,panel}.rs`：父 `style.rs` 从 90 行降为 15 行 structural re-export entry，保持 `UiAssetStyle*Data` 类型名与字段语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；UI Asset style DTO subowner ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract softbuffer planned-present model/outcome split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/presenter/softbuffer/diagnostics/planned_present.rs` 中 `PlannedPresent` result DTO/presentation overlay update 与 repaint outcome accounting 分别拆入 `planned_present/{model,outcome}.rs`：父 `planned_present.rs` 从 112 行降为 76 行 planning loop entry，保留 damage eligibility、overlay expansion convergence 和 diagnostics record sequencing。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；softbuffer planned-present model/outcome ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract softbuffer surface-io copy/damage/size split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/presenter/softbuffer/surface_io.rs` 中 RGBA pixel copy、damage pixel bounds/count/softbuffer rect conversion 与 window-size clamp/surface resize 分别拆入 `surface_io/{copy,damage,size}.rs`：父 `surface_io.rs` 从 92 行降为 11 行 structural re-export entry，保持 softbuffer presenter 调用入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；softbuffer surface-io copy/damage/size ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Welcome recent-project rows frame/surface/text split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/recent_projects/rows.rs` 中 per-row frame math、invalid-row surface/border styling 与 display/path/status text paint 分别拆入 `rows/{frame,surface,text}.rs`：父 `rows.rs` 从 106 行降为 42 行 row iteration entry，保持 visible row traversal 和 recent project status label selection 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench welcome recent-project rows frame/surface/text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench root-frame model/selection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/root_frames.rs` 中 `RootFrames`/zero-origin model 与 scene-layout vs host-layout selection 分别拆入 `root_frames/{model,selection}.rs`：父 `root_frames.rs` 从 103 行降为 76 行 fallback geometry assembly entry，保持 root fallback frame formulas 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench root-frame model/selection ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-text draw recording/metrics split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_text/draw.rs` 中 font-size/line-height clamp 与 retained recording handoff 拆入 `draw/recording.rs`：父 `draw.rs` 从 105 行降为 101 行 draw entry，保留公共 draw 函数链、clip resolution、layout invocation、record-only early return 与 glyph dispatch。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-text draw recording/metrics ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract diagnostics refresh model/timing/overlay-text split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/diagnostics/refresh.rs` 中 `HostRefreshDiagnostics` record/counter mutation、present timestamp/FPS helpers 与 startup/live overlay text formatting 分别拆入 `refresh/{model,timing,overlay_text}.rs`：父 `refresh.rs` 从 104 行降为 5 行 structural re-export entry，保持 diagnostics public type 和 overlay text 输出格式不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；diagnostics refresh model/timing/overlay-text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling-hit-routes template pane/floating/route-check split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_hit_routes/template.rs` 中 pane route prefix/content/body-toolbar checks、floating-window iteration 与 runtime surface-frame top-hit/control-id comparison 分别拆入 `template/{pane,floating,route_check}.rs`：父 `template.rs` 从 105 行降为 53 行 template route orchestration entry，保持 document/side/bottom/floating route hit behavior 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling-hit-routes template pane/floating/route-check ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling-hit-routes viewport-toolbar pane/floating/route-check split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_hit_routes/viewport_toolbar.rs` 中 Scene/Game toolbar eligibility、prefix/content checks、floating-window iteration 与 viewport-toolbar hit/control-id comparison 分别拆入 `viewport_toolbar/{pane,floating,route_check}.rs`：父 `viewport_toolbar.rs` 从 98 行降为 53 行 viewport-toolbar route orchestration entry，保持 document/side/bottom/floating toolbar route hit behavior 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling-hit-routes viewport-toolbar pane/floating/route-check ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-frame recording command shape/text/image/record split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_frame/recording_frame/commands.rs` 中 quad/border/text/image recording methods 与 shared record handoff helper 分别拆入 `commands/{shapes,text,image,record}.rs`：父 `commands.rs` 从 100 行降为 4 行 structural command entry，保持 `HostRgbaFrame` recording method API 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-frame recording-frame command shape/text/image/record ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-workbench skeleton marker split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/skeleton.rs` 中 project-path marker 与 debug refresh-rate marker drawing 拆入 `skeleton/markers.rs`：父 `skeleton.rs` 从 99 行降为 64 行 root skeleton paint-order entry，保持 root fill/border/separator、viewport label 与 status label draw ordering 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench skeleton marker ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-frame recording model/state split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_frame/recording.rs` 中 image UV/atlas DTO、recorded paint kind/command DTO 与 recording accumulator/state 分别拆入 `recording/{model,state}.rs`：父 `recording.rs` 从 98 行降为 7 行 structural re-export entry，保持 `paint_frame` 对外 recorded paint type API 与 `HostPaintRecording` 内部调用不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-frame recording model/state ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome-command-stream runtime draw-list command/text-style/geometry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/chrome_command_stream/runtime_draw_list.rs` 中 command-kind projection、text-style mapping、frame rect/image UV conversion 分别拆入 `runtime_draw_list/{command,text_style,geometry}.rs`：父 `runtime_draw_list.rs` 从 95 行降为 26 行 stream-to-list orchestration entry，保持 `UiSurfaceDrawList` 输出语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chrome command-stream runtime draw-list command/text-style/geometry ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract native-input key helper legacy/name/scan/state split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/native_input_translation/keys.rs` 中 keyboard state conversion、physical/logical key name formatting、legacy web-style key-code mapping 和 native scan-code extraction 分别拆入 `keys/{state,names,legacy,scan}.rs`：父 `keys.rs` 从 97 行降为 11 行 structural key-helper re-export entry，保持 native keyboard event conversion 的 helper 调用名不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；native-input key helper legacy/name/scan/state ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Welcome fallback-rect top/fields/frame split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/main_column/frames/fallback/rects.rs` 中 hero/status/header fallback geometry、name/location/preview/validation/action fallback geometry 与 shared `FrameRect` constructor 分别拆入 `rects/{top,fields,frame}.rs`：父 `rects.rs` 从 107 行降为 8 行 structural fallback-rect entry，保持 fallback geometry 数值和顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；welcome fallback-rect top/fields/frame ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract globals pane-context setter family split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/globals/pane_context/setters.rs` 中 Welcome/project overview、viewport image/mesh import、asset-pane projected-data placeholders 与 retained pane interaction state mutation 分别拆入 `setters/{welcome,viewport,asset_data,interaction}.rs`：父 `setters.rs` 从 114 行降为 4 行 structural setter-family entry，保持 `PaneSurfaceHostContext` setter 方法名与调用面不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；globals pane-context setter family ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 paint_template_nodes style-selector export split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/style_selector/mod.rs` 中 state projection、Workbench family selector 与 test-only token re-export surface 拆入 `style_selector/exports.rs`：父 `mod.rs` 从 101 行降为 20 行 structural declaration/re-export entry，保持 selector restricted visibility 与调用路径不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；style-selector export ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window text-input edit dispatch/redraw split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/text_input/edit.rs` 中 edited-value callback fanout、asset dispatch source selection 与 text-focus redraw region fallback 分别拆入 `edit/{dispatch,redraw}.rs`：父 `edit.rs` 从 110 行降为 53 行 focused edit entry，保持 text insert/backspace/commit 行为和 callback routing 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window text-input edit dispatch/redraw ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract surface-hit-test template-node hit/model split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/surface_hit_test/template_node.rs` 中 `TemplateNodePointerHit` DTO、popup-priority delegation、runtime surface-frame hit traversal 与 host-space frame translation 拆入 `template_node/{model,hit}.rs`：父 `template_node.rs` 从 105 行降为 54 行 public hit-test entry，保持 pane/window template-node hit-test 与 surface-frame build API 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；surface-hit-test template-node hit/model ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window text-input keyboard popup/consumed/unhandled split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/text_input/keyboard.rs` 中 popup keyboard command/text-search fallback、text-focus keyboard consumption checks 与 unhandled runtime keyboard forwarding 分别拆入 `keyboard/{popup,consumed,unhandled}.rs`：父 `keyboard.rs` 从 109 行降为 55 行 focused keyboard orchestration entry，保持 focused key dispatch、popup fallback 和 unhandled forwarding 语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window text-input keyboard popup/consumed/unhandled ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-text draw clip-rect split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_text/draw.rs` 中 effective text clip resolution 与 frame-to-pixel clip conversion 拆入 `draw/clip_rect.rs`：父 `draw.rs` 从 101 行降为 93 行 draw entry，保持 public draw entry chain、recording、layout 和 glyph dispatch 顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-text draw clip-rect ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome command-stream extraction command kind/layer split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/chrome_command_stream/extraction/command.rs` 中 recorded-paint kind to `ChromeCommandKind` conversion 与 recorded-paint kind to `ChromeCommandLayer` selection 分别拆入 `command/{kind,layer}.rs`：父 `command.rs` 从 98 行降为 36 行 structural recorded-command conversion entry，保持 extraction output、layer policy 与 image payload delegation 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chrome command-stream extraction command kind/layer ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract chrome command-stream replay border rect split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/chrome_command_stream/replay/commands/shapes/border.rs` 中 rectangular border width normalization、segment iteration、segment frame geometry 与 clipped rect paint 拆入 `border/rect.rs`：父 `border.rs` 从 97 行降为 25 行 focused rounded/rect dispatch entry，保持 rounded border handoff 与 rectangular border paint order 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；chrome command-stream replay border rect ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench dock pane template-node selection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/docks/pane/template_nodes.rs` 中 pane-kind to template-node model selection 拆入 `template_nodes/selection.rs`：父 `template_nodes.rs` 从 95 行降为 28 行 retained template-node draw entry，保持 Hierarchy/Inspector/Assets/Welcome/UI Asset/Animation pane routing 与 draw gating 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench dock pane template-node selection ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window event-loop lifecycle native-window/presenter split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/event_loop/lifecycle.rs` 中 native window attribute/creation failure handling 与 GPU presenter creation/softbuffer fallback/failure logging 分别拆入 `lifecycle/{native_window,presenter}.rs`：父 `lifecycle.rs` 从 118 行降为 73 行 lifecycle orchestration entry，保持创建窗口、同步宿主状态、创建 presenter、请求首帧 redraw 的顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window event-loop lifecycle native-window/presenter ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window event-loop event keyboard/pointer/resize split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/event_loop/events.rs` 中 keyboard/IME dispatch、pointer move/button/scroll dispatch 与 surface resize/native move/presenter resize handling 分别拆入 `events/{keyboard,pointer,resize}.rs`：父 `events.rs` 从 115 行降为 57 行 event dispatch entry，保持 winit WindowEvent match ordering、close handling 和 redraw request branch 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window event-loop event keyboard/pointer/resize ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench menu popup submenu chain split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/menus/popup.rs` 中 submenu path traversal、anchor row selection、submenu popup constraints、submenu body paint 与 nested item advancement 拆入 `popup/submenus.rs`：父 `popup.rs` 从 88 行降为 45 行 open-menu popup entry，保持一级菜单 popup、模板 popup 与 fallback row paint 顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench menu popup submenu chain ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling artifact geometry tabs collection split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_artifacts/geometry/tabs.rs` 中 shared tab row loop、visible-frame filtering、origin/close-frame translation 与 `UiProfileTabFrame` construction 拆入 `tabs/collect.rs`：父 `tabs.rs` 从 88 行降为 63 行 tab source-selection entry，保持 document/drawer/floating/host-page tab capture 输出不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling artifact geometry tabs collection ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-text glyph row pixel split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_text/draw/glyphs.rs` 中 row/column clipping、emphasis offset、strong-text overdraw、coverage alpha 与 blend dispatch 拆入 `glyphs/row.rs`：父 `glyphs.rs` 从 88 行降为 52 行 glyph traversal entry，保持 cached raster lookup、per-row clip gating 与 glyph draw ordering 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-text glyph row pixel ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window event-loop redraw present split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/event_loop/redraw.rs` 中 presenter present、profiling artifact export、refresh diagnostics overlay update 与 presenter failure exit 拆入 `redraw/present.rs`：父 `redraw.rs` 从 87 行降为 59 行 redraw queue/scenario entry，保持 pending redraw merge、frame-update scenario attribution 与 redraw request scheduling 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window event-loop redraw present ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench dock floating-window shell split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/docks/floating_windows.rs` 中单个浮窗 visibility gate、shadow/panel/border/header paint、body frame construction 与 pane handoff 拆入 `floating_windows/window.rs`：父 `floating_windows.rs` 从 86 行降为 29 行 floating-layer iteration entry，保持浮窗列表遍历和绘制顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench dock floating-window shell ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench native pane hierarchy viewport split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/native_panes/hierarchy.rs` 中 template-node viewport frame discovery、host-space translation、visible-frame filtering 与 body fallback 拆入 `hierarchy/viewport.rs`：父 `hierarchy.rs` 从 85 行降为 68 行 hierarchy row-paint entry，保持 Hierarchy 行绘制、hover/selection、scroll clipping 与文字缩进不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench native pane hierarchy viewport ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint primitive shape rect solid split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_primitives/shapes/rects.rs` 中 effective clip resolution、`PixelRect` target conversion、recording-aware quad emission、record-only short-circuit 与 solid/rounded fill dispatch 拆入 `rects/solid.rs`：父 `rects.rs` 从 84 行降为 33 行 public rect entry，保持 rect/rounded-rect public draw API 不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint primitive shape rect solid ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract paint-text draw entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_text/draw.rs` 中 public draw entry chain、default font metrics 与 default text-style selection 拆入 `draw/entry.rs`：父 `draw.rs` 从 85 行降为 50 行 text pipeline owner，保持 clip resolution、metric clamping、recording handoff、layout 与 glyph dispatch 顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；paint-text draw entry ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench dock side frame split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/docks/side.rs` 中 rail-before-panel rail/panel frame resolution 拆入 `side/frames.rs`：父 `side.rs` 从 83 行降为 59 行 side-dock paint-order entry，保持 side shell、rail template、active marker、header 与 pane handoff 绘制顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench dock side frame ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract diagnostics refresh counter method split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/diagnostics/refresh/model.rs` 中 present counter mutation、invalidation counter merging、FPS forwarding 与 overlay text forwarding 拆入 `model/counters.rs`：父 `model.rs` 从 80 行降为 30 行 diagnostics record owner，保持 `HostRefreshDiagnostics` 对外方法语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；diagnostics refresh counter method ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench native pane asset hover row overlay split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/native_panes/assets/overlay.rs` 中 hovered-row index gating、row-frame lookup、clip intersection、hover fill 与 hover border paint 拆入 `overlay/row.rs`：父 `overlay.rs` 从 80 行降为 56 行 asset/tree entry owner，保持 Activity Assets 与 Asset Browser hover overlay 调用面不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench native pane asset hover row overlay ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench Welcome main-column frame sequence top/form split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/welcome/main_column/frames/sequence.rs` 中 hero/status/header resolution order 与 name/location/preview/validation/action resolution order 分别拆入 `sequence/{top,form}.rs`：父 `sequence.rs` 从 80 行降为 26 行 final frame assembly entry，保持 Welcome 主列 frame 依赖顺序和 fallback 链不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench welcome main-column frame sequence top/form ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling artifact pane-frame surface controls split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_artifacts/geometry/pane_frames/surface_frame.rs` 中 pointer-capable node traversal、host-space frame/clip translation、center-point top-hit verification 与 named profile-frame emission 拆入 `surface_frame/controls.rs`：父 `surface_frame.rs` 从 79 行降为 33 行 optional surface-frame entry，保持 viewport-toolbar profiling controls 的 clipping/top-hit 语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling artifact pane-frame surface controls ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract profiling artifact pane-frame template-node source/dispatchable split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/profiling_artifacts/geometry/pane_frames/template_nodes.rs` 中 pane-kind to template-node model source selection 与 dispatchable-template-control predicate 分别拆入 `template_nodes/{source,dispatchable}.rs`：父 `template_nodes.rs` 从 77 行降为 55 行 template-control collection entry，保持 template profile frame projection 与 clipping 语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；profiling artifact pane-frame template-node source/dispatchable ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench dock pane shell/content split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/docks/pane.rs` 中 pane background/viewport toolbar/body layout 与 viewport/template/native/debug/fallback content sequencing 分别拆入 `pane/{body,content}.rs`：父 `pane.rs` 从 81 行降为 36 行 visible-pane entry，保持 pane visible guard、toolbar、viewport image、template nodes、native content、debug overlay 与 fallback 顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench dock pane shell/content ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench scene overlay componentized/modal/root-template split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/scene_layers/overlay.rs` 中 componentized Workbench template detection/draw handoff、open-menu popup/close-prompt ordering、root-template overlay profiling/frame bounds 分别拆入 `overlay/{componentized,modal,root_template}.rs`：父 `overlay.rs` 从 76 行降为 9 行 structural re-export entry，保持 host-scene overlay 绘制顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench scene overlay componentized/modal/root-template ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench native pane hierarchy row split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/native_panes/hierarchy.rs` 中 per-row frame construction、visible-row gating、hover/selected color selection、selected border paint 与 depth-based text indentation 拆入 `hierarchy/row.rs`：父 `hierarchy.rs` 从 77 行降为 42 行 Hierarchy row iteration entry，保持 Hierarchy viewport/clip/scroll/row 绘制语义不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench native pane hierarchy row ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Workbench native pane hierarchy row frame/style/text split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_workbench_renderer/native_panes/hierarchy/row.rs` 中 row frame construction、hover/selected/default row color selection、row label positioning 与 depth indentation 分别拆入 `row/{frame,style,text}.rs`：父 `row.rs` 从 79 行降为 38 行 single-row paint-order entry，保持 Hierarchy 单行绘制顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；workbench native pane hierarchy row frame/style/text ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract window handle snapshot split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/window/handle.rs` 中 `HostWindowSnapshot` payload、frame-to-byte conversion、snapshot width/height/bytes accessors 拆入 `handle/snapshot.rs`：父 `handle.rs` 从 85 行降为 59 行 handle API entry，保持窗口位置/大小/最大化/关闭回调与截图入口不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；window handle snapshot ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract template selection-control style checkbox/radio/toggle split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_selection_controls/style.rs` 中 checkbox、radio、toggle style accessor 与 shared selector handoff 分别拆入 `style/{checkbox,radio,toggle,selector}.rs`：父 `style.rs` 从 89 行降为 15 行 structural re-export entry，保持 checkbox/radio/toggle painter helper 名称与视觉状态解析不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template selection controls style checkbox/radio/toggle ownership scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract template selection-control checkbox tick split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_selection_controls/checkbox.rs` 中 checked-tick segment geometry 与 tick quad emission 拆入 `checkbox/tick.rs`：父 `checkbox.rs` 从 78 行降为 39 行 checkbox paint-order entry，保持 checkbox surface、checked-state gating、label placement 与绘制顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；selection-control checkbox tick ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract template-node specialized primary/dropdown/secondary split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_nodes/specialized.rs` 中 early component dispatch、dropdown popup-row handoff、remaining specialized component chain 分别拆入 `specialized/{primary,dropdown,secondary}.rs`：父 `specialized.rs` 从 79 行降为 26 行 routing entry，保持 specialized painter 优先级与 dropdown popup-row 顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；template-node specialized primary/dropdown/secondary ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 当前实现先收口；后续 M3.S2 继续拆分 retained-host 深层 owner，并在测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract presenter trait test-owner split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/presenter/host_chrome_presenter.rs` 中 boxed-backend trait-object regression 拆入 `host_chrome_presenter/tests.rs`：父 `host_chrome_presenter.rs` 从 77 行降为 15 行 trait boundary，保持 `HostChromePresenter` resize/present/diagnostics_snapshot 合约不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；host chrome presenter trait/tests ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive divider label horizontal/vertical/text split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/divider/labels.rs` 中 horizontal label text command emission、vertical label text command emission 与 divider label fallback 分别拆入 `labels/{horizontal,vertical,text}.rs`：父 `labels.rs` 从 78 行降为 7 行 structural export entry，保持 Divider horizontal/vertical label placement、fallback 顺序与 retained text command 参数不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Material primitive divider label horizontal/vertical/text ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract style-selector exports chrome/feedback/form-controls/rows split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/style_selector/exports.rs` 中 chrome/status、alert/toast/tooltip、button/dropdown/icon-button/segmented/selection/slider/text-field、list/popup/table/tree row selector re-export families 分别拆入 `exports/{chrome,feedback,form_controls,rows}.rs`：父 `exports.rs` 从 93 行降为 9 行 structural export entry，保持 `style_selector/mod.rs` 的 `exports::*` 对外受限导出面不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；style-selector exports chrome/feedback/form-controls/rows ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive avatar command surface/content split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/avatar/commands.rs` 中 background/border quad emission 与 image/text/icon/fallback content selection 分别拆入 `commands/{surface,content}.rs`：父 `commands.rs` 从 84 行降为 65 行 Avatar root gating 与 draw-order entry，保持 Avatar surface、image、fallback text、icon、glyph、border 绘制顺序不变。 | `cargo fmt -p zircon_editor` 首次尝试 120 秒超时但已格式化 touched files；`cargo fmt -p zircon_editor --check` 通过；Material primitive avatar command surface/content ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive avatar image command/icon/pixels split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/avatar/image.rs` 中 avatar image command emission、fallback icon raster lookup、preview/media raster lookup 与 rounded alpha masking 分别拆入 `image/{command,icon,pixels}.rs`：父 `image.rs` 从 71 行降为 7 行 structural export entry，保持 Avatar image/icon 解析参数和 retained image command 参数不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Material primitive avatar image command/icon/pixels ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive badge overlay surface/text split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/badge/commands/overlay.rs` 中 overlay/dot quad emission 与 overlay text command emission 分别拆入 `overlay/{surface,text}.rs`：父 `overlay.rs` 从 79 行降为 52 行 overlay visibility/frame ordering entry，保持 invisible/dot/display gating、overlay frame 和 text command 顺序不变。 | `cargo fmt -p zircon_editor` 与 `cargo fmt -p zircon_editor --check` 通过；Material primitive badge overlay surface/text ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive chip style palette constants/lookup/token split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/chip/style/palette.rs` 中 MUI chip color constants、`chip_palette_main` 和 component-variant color token classification 分别拆入 `palette/{constants,lookup,token}.rs`：父 `palette.rs` 从 82 行降为 7 行 structural export entry，保持 chip style callers 的 palette/token 导入面不变。 | `cargo fmt -p zircon_editor` 通过；后续 `cargo fmt -p zircon_editor --check` 因 unrelated `zircon_runtime` Cargo test 占用 Cargo 队列而 120 秒超时，改用 touched files `rustfmt --edition 2021 --check` 通过；Material primitive chip style palette constants/lookup/token ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive text-field style stroke/fill/radius split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/text_field/style.rs` 中 TextField stroke color/width、filled background/radius、configured corner-radius fallback 分别拆入 `style/{stroke,fill,radius}.rs`：父 `style.rs` 从 78 行降为 7 行 structural export entry，保持 outlined/filled/standard callers 的 style 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；Material primitive text-field style stroke/fill/radius ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract MUI X AgentChat command/surface/bubbles/streaming split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/chat/agent.rs` 中 AgentChat paint order、shell surface、two-bubble preview 与 streaming indicator 分别拆入 `agent/{commands,surface,bubbles,streaming}.rs`：父 `agent.rs` 从 78 行降为 7 行 structural export entry，保持 chat command dispatch 的 `push_agent_chat` 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；MUI X AgentChat command/surface/bubbles/streaming ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract popup-row text label/shortcut/metrics split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_popup_rows/text.rs` 中 popup-row label text command、shortcut text command 与 shared text metrics 分别拆入 `text/{label,shortcut,metrics}.rs`：父 `text.rs` 从 79 行降为 6 行 structural export entry，保持 menu/option row callers 的 label/shortcut 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；popup-row text label/shortcut/metrics ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract template style surface color entry/severity/interaction/variant split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_style/colors/surface.rs` 中 surface color priority、severity tones、interaction-state colors 与 variant fallback colors 分别拆入 `surface/{entry,severity,interaction,variants}.rs`：父 `surface.rs` 从 79 行降为 6 行 structural export entry，保持 `template_style/colors.rs` 的 `surface_color` 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；template style surface color entry/severity/interaction/variant ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract tree-row object icon cube/audio/player-start split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_tree_row_glyphs/object/icons.rs` 中 cube、audio、PlayerStart icon segment lists 分别拆入 `icons/{cube,audio,player_start}.rs`：父 `icons.rs` 从 78 行降为 7 行 structural export entry，保持 object glyph dispatch 的 `push_*_icon` 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；tree-row object icon cube/audio/player-start ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract command-palette panel entry/surface/search/empty split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_command_palette/panel.rs` 中 panel paint order、panel surface、search field/text 与 empty message 分别拆入 `panel/{entry,surface,search,empty}.rs`：父 `panel.rs` 从 76 行降为 7 行 structural export entry，保持 command-palette command sequencing 的 panel/empty 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；command-palette panel entry/surface/search/empty ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract tooltip text title/body/metrics/entry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_tooltips/text.rs` 中 tooltip title fallback/emission、body fallback/emission、shared metrics 与 title/body order 分别拆入 `text/{title,body,metrics,entry}.rs`：父 `text.rs` 从 76 行降为 6 行 structural export entry，保持 tooltip command sequencing 的 `push_tooltip_text` 导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；tooltip text title/body/metrics/entry ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive divider label-bounds horizontal/vertical/align split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/divider/geometry/label_bounds.rs` 中 horizontal label width/left placement、vertical label height/top placement 与 text-align classification 分别拆入 `label_bounds/{horizontal,vertical,align}.rs`：父 `label_bounds.rs` 从 77 行降为 5 行 structural export entry，保持 `geometry.rs` 对 `horizontal_label_bounds` / `vertical_label_bounds` 的 re-export 与调用方导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；Material primitive divider label-bounds horizontal/vertical/align ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract slider track rail/ticks split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/template_sliders/track.rs` 中 slider base/fill track command emission 与 tick mark command emission 分别拆入 `track/{rail,ticks}.rs`：父 `track.rs` 从 77 行降为 4 行 structural export entry，保持 command sequence 对 `push_slider_track` / `push_slider_ticks` 的导入面和绘制顺序不变。 | touched files `rustfmt --edition 2021 --check` 通过；slider track rail/ticks ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract MUI X TreeView row entry/metric/row split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/tree_view/rows.rs` 中 TreeView surface command + row fan-out、row metrics constants 与 row/marker command emission 分别拆入 `rows/{entry,metrics,row}.rs`：父 `rows.rs` 从 77 行降为 4 行 structural export entry，保持 `tree_view.rs` 对 `push_tree_view` 的 re-export 和 MUI X primitive dispatch 入口不变。 | touched files `rustfmt --edition 2021 --check` 通过；MUI X TreeView row entry/metric/row ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract MUI X DataGrid identity/command/surface/row/metric split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/mui_x_primitives/data_grid.rs` 中 DataGrid role recognition、draw-order orchestration、header/row metrics、surface/header command emission 与 selected/default row command emission 分别拆入 `data_grid/{identity,commands,metrics,surface,rows}.rs`：父 `data_grid.rs` 从 70 行降为 7 行 structural export entry，保持 MUI X primitive dispatch 对 `is_data_grid` / `push_data_grid` 的导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；MUI X DataGrid identity/command/surface/row/metric ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material primitive chip delete entry/dot/metrics split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_primitives/chip/delete.rs` 中 delete icon frame/color lookup + diagonal dot iteration、single-dot quad emission 与 delete metrics constants 分别拆入 `delete/{entry,dot,metrics}.rs`：父 `delete.rs` 从 71 行降为 4 行 structural export entry，保持 chip command sequence 对 `push_chip_delete_icon` 的导入面和 delete glyph 绘制顺序不变。 | touched files `rustfmt --edition 2021 --check` 通过；Material primitive chip delete entry/dot/metrics ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-21 | 08 M3.S2 host_contract Material state-layer ripple command/geometry split | 完成（功能代码、文档记录；完整测试矩阵按用户要求暂缓；package-level Cargo check 暂缓） | 将 `host_contract/paint_template_nodes/material_state_layer/ripple.rs` 中 ripple enablement/command emission 与 pressed-origin geometry/clip/radius/diameter helpers 分别拆入 `ripple/{commands,geometry}.rs`：父 `ripple.rs` 从 70 行降为 6 行 structural export entry，保持 `material_state_layer.rs` 对 `push_ripple_commands` 与 test-only ripple helper 的导入面不变。 | touched files `rustfmt --edition 2021 --check` 通过；Material state-layer ripple command/geometry ownership scan、docs marker scan、scoped whitespace scan 与 scoped `git diff --check` 通过；新增文件 no-index diff-check 仅提示 LF/CRLF 转换；本切片未重启 package-level Cargo check，完整测试矩阵按用户“先实现功能，测试暂时延后”要求留到 milestone testing stage。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract template button test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 300s 超时无 editor 诊断） | 删除单文件 `host_contract/paint_template_nodes/template_buttons_tests.rs`，改为 `template_buttons_tests/{mod,identity,paint,style,geometry,support}.rs`：父 `mod.rs` 只声明测试子模块，`identity.rs` 覆盖按钮识别排除，`paint.rs` 覆盖像素绘制断言，`style.rs` 覆盖 declared style/状态优先级，`geometry.rs` 覆盖布局偏移，`support.rs` 集中 fixture、resolved-style builder、frame helper 与 pixel probe；`template_buttons.rs` 只改测试挂载路径并保持 26 行结构入口。 | `cargo fmt -p zircon_editor --check` 通过；button test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-template-button-tests-0622 --message-format short --color never` 300s 超时且未产出 editor 诊断，期间存在 unrelated runtime Cargo/Rustc 测试编译车道。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract viewport-scene test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_viewport_scene_tests.rs`，改为 `template_viewport_scene_tests/{mod,architecture,floor,gizmo,light,props,surfaces,support}.rs`：父 `mod.rs` 只声明测试域，domain 文件分别覆盖 architecture/floor/gizmo/light/props/surfaces 的像素回归，`support.rs` 集中 styled-node、render helper、pixel probe 与 luminance helper；`template_viewport_scene.rs` 只改测试挂载路径并保持 7 行结构入口。 | `cargo fmt -p zircon_editor --check` 通过；viewport-scene test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为本会话前一个 editor package check 已在 unrelated runtime Cargo/Rustc 活跃车道下 300s 超时且无 editor 诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract selection-control test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；editor test compile 180s 超时无诊断） | 删除单文件 `host_contract/paint_template_nodes/template_selection_controls_tests.rs`，改为 `template_selection_controls_tests/{mod,identity,marks,paint,state,support,toggle}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 role/control-id 分类，`marks.rs` 覆盖 checkbox/radio mark 几何与 declared style，`toggle.rs` 覆盖 track/thumb 几何与 tone，`state.rs` 覆盖 pressed/loading visual state，`paint.rs` 覆盖像素输出，`support.rs` 集中 fixture、resolved-style builder 与 pixel probe；`template_selection_controls.rs` 只改测试挂载路径并保持小型结构入口。 | `cargo fmt -p zircon_editor --check` 通过；selection-control test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；`cargo test -p zircon_editor --lib --locked --no-run --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-selection-tests-0622 --message-format short --color never` 180s 超时且未产出诊断，本次 editor 检查残留进程已停止，unrelated runtime Cargo/Rustc 车道未干扰。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract status-control test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_status_controls_tests.rs`，改为 `template_status_controls_tests/{mod,chips,icons,identity,signals,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 status ID 分类，`signals.rs` 覆盖 signal 像素、几何和 declared style，`chips.rs` 覆盖 chip 像素、布局和状态优先级，`icons.rs` 覆盖 icon-button 像素、布局和状态优先级，`support.rs` 集中 status node fixture 与 pixel probe；`template_status_controls.rs` 只改测试挂载路径并保持小型结构入口。 | touched Rust files `rustfmt --edition 2021 --check` 通过；status-control test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract icon-button test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_icon_buttons_tests.rs`，改为 `template_icon_buttons_tests/{mod,geometry,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Workbench icon-button 识别与 status-control 排除，`paint.rs` 覆盖 toolbar/rail/panel 像素输出，`geometry.rs` 覆盖 declared offset/icon-size 布局，`style.rs` 覆盖 declared glyph/surface/border/radius/danger/state priority，`support.rs` 集中 icon-node fixture、resolved-style builder、frame helper 与 pixel probe；`template_icon_buttons.rs` 只改测试挂载路径并保持小型结构入口。 | touched Rust files `rustfmt --edition 2021 --check` 通过；icon-button test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract visual-asset test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/visual_assets_tests.rs`，改为 `visual_assets_tests/{mod,editor_pages,mui,runtime,svg,template,tint,support}.rs`：父 `mod.rs` 只声明测试域，`runtime.rs` 覆盖 runtime SVG target-size，`template.rs` 覆盖 template SVG、missing fallback、projected preview authority，`editor_pages.rs` 覆盖 editor-page 16px icon readability，`mui.rs` 覆盖 MUI module/ligature，`svg.rs` 覆盖 font-scan guard，`tint.rs` 覆盖 tint priority，`support.rs` 集中 pixel visibility/readability helper 与 preview fixture；`visual_assets.rs` 只改测试挂载路径并保持小型结构入口。 | touched Rust files `rustfmt --edition 2021 --check` 通过；visual-asset test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract popup-row test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_popup_rows_tests.rs`，改为 `template_popup_rows_tests/{mod,adornment,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`adornment.rs` 覆盖 menu item adornment kind、danger、submenu 与 icon flag，`style.rs` 覆盖 popup row style selector 状态优先级和 runtime state projection，`paint.rs` 覆盖 dropdown/menu 像素输出，`support.rs` 集中 popup menu/dropdown option fixture 与 pixel probe；`template_popup_rows.rs` 只改测试挂载路径并保持小型结构入口。 | touched Rust files `rustfmt --edition 2021 --check` 通过；popup-row test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract slider test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_sliders_tests.rs`，改为 `template_sliders_tests/{mod,geometry,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Workbench slider ID/role/variant 识别，`paint.rs` 覆盖 track/fill/thumb/value 与 hover halo 像素输出，`style.rs` 覆盖 shared selector、declared fill/thumb/color 与 halo state，`geometry.rs` 覆盖 label/value/track proportion、range-min 与 tick-count，`support.rs` 集中 slider fixture、style accessor、resolved-style builder 与 pixel probe；`template_sliders.rs` 只改测试挂载路径并保持小型结构入口。 | touched Rust files `rustfmt --edition 2021 --check` 通过；slider test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract inspector-row test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_inspector_rows_tests.rs`，改为 `template_inspector_rows_tests/{mod,disclosure,geometry,identity,resource,shadow,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 inspector row kind 分类，`resource.rs` 覆盖 resource row 像素与 declared style，`shadow.rs` 覆盖 shadow checkbox/select，`geometry.rs` 覆盖 nested select geometry，`disclosure.rs` 覆盖 disclosure row 像素，`support.rs` 集中 inspector fixture、resolved-style builder 与 pixel probe；`template_inspector_rows.rs` 只改测试挂载路径并保持小型结构入口，同时符合 Editor UI 10 的测试 owner 规则。 | touched Rust files `rustfmt --edition 2021 --check` 通过；inspector-row test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2 / Editor UI 10：继续拆分 retained-host 深层 owner，并把后续涉及 `pane_data_conversion` 的工作优先对齐 F15 owner 拆分要求。 |
| 2026-06-22 | 08 M3.S2 host_contract alert test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_alerts_tests.rs`，改为 `template_alerts_tests/{mod,identity,inline,style,support,toast}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Workbench alert/toast 分类，`inline.rs` 覆盖 inline alert 像素输出，`toast.rs` 覆盖 Toast 像素与 declared status/action style，`style.rs` 覆盖 alert/toast state priority，`support.rs` 集中 fixture、pixel probe 与 alpha blend helper；`template_alerts.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；alert test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract table-row test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_table_rows_tests.rs`，改为 `template_table_rows_tests/{mod,cells,geometry,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`cells.rs` 覆盖 declared/legacy cell parsing，`paint.rs` 覆盖 selected/header/tail 像素输出，`geometry.rs` 覆盖 row/content/cell offset，`style.rs` 覆盖 state priority，`support.rs` 集中 table fixture 与 pixel probe；`template_table_rows.rs` 只改测试挂载路径并保持小型结构入口。 | touched Rust files `rustfmt --edition 2021 --check` 通过；table-row test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract dropdown test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_dropdowns_tests.rs`，改为 `template_dropdowns_tests/{mod,geometry,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Workbench dropdown 分类，`paint.rs` 覆盖 closed/open/offset/brightness 像素输出，`geometry.rs` 覆盖 half-pixel height 对齐，`style.rs` 覆盖 declared style 与 shared state priority，`support.rs` 集中 dropdown fixture、popup option fixture、resolved-style builder、brightness helper 与 pixel probe；`template_dropdowns.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；dropdown test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract field test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_fields_tests.rs`，改为 `template_fields_tests/{mod,geometry,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Workbench field 分类，`paint.rs` 覆盖 field/stepper surface、border、text 与 layout-offset 像素输出，`geometry.rs` 覆盖 half-pixel height 对齐，`style.rs` 覆盖 declared focus border、opacity 与 shared state priority，`support.rs` 集中 field fixture 与 pixel probe；`template_fields.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；field test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract template-node pipeline test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_node_pipeline_tests.rs`，改为 `template_node_pipeline_tests/{mod,clip,dropdown_popup,menu_popup,support}.rs`：父 `mod.rs` 只声明测试域，`clip.rs` 覆盖 active damage/template clip 跳过与相交绘制，`dropdown_popup.rs` 覆盖 dropdown option rows 下方/上方定位与 Workbench layout offset anchoring，`menu_popup.rs` 覆盖 popup menu frame 像素输出，`support.rs` 集中 panel/dropdown/menu fixture、rect helper 与 pixel probe；`template_node_pipeline.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；template-node pipeline test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract tree-row test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_tree_rows_tests.rs`，改为 `template_tree_rows_tests/{mod,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 TreeRow role/control-id/component-role 分类，`paint.rs` 覆盖 selected/nested/collapsed/loading 像素输出，`style.rs` 覆盖 shared state priority，`support.rs` 集中 tree-row fixture 与 pixel probe；`template_tree_rows.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；tree-row test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract list-row test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_list_rows_tests.rs`，改为 `template_list_rows_tests/{mod,adornment,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`adornment.rs` 覆盖 disabled/loading/selected/chevron adornment priority，`paint.rs` 覆盖 selected/disabled 像素输出，`style.rs` 覆盖 declared surface/text/adornment color 与 shared state priority，`support.rs` 集中 list-row fixture 与 pixel probe；`template_list_rows.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；list-row test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract shell-panel test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_shell_panels_tests.rs`，改为 `template_shell_panels_tests/{mod,identity,paint,state,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 shell-panel container ID 分类，`paint.rs` 覆盖 toolbar/side-panel/drawer/status/drawer-column surface 与 separator 像素输出，`state.rs` 覆盖 chrome selector loading/focused 状态绘制，`support.rs` 集中 panel fixture 与 pixel probe；`template_shell_panels.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；shell-panel test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract axis-value-field test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_axis_value_fields_tests.rs`，改为 `template_axis_value_fields_tests/{mod,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Transform axis-value field 分类，`paint.rs` 覆盖 compact/focused/disabled 像素输出，`style.rs` 覆盖 declared value-color text，`support.rs` 集中 axis/label fixture 与 pixel probe；`template_axis_value_fields.rs` 只改测试挂载路径。 | touched Rust files `rustfmt --edition 2021 --check` 通过；axis-value-field test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract tooltip test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_tooltips_tests.rs`，改为 `template_tooltips_tests/{mod,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`paint.rs` 覆盖 tooltip bubble/arrow/border/icon/title/body 像素输出，`style.rs` 覆盖 shared state priority，`support.rs` 集中 tooltip fixture 与 pixel probe；`template_tooltips.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；tooltip test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract section-title test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_section_titles_tests.rs`，改为 `template_section_titles_tests/{mod,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 section-title 分类，`paint.rs` 覆盖 bold-label 与 leading-icon 像素输出，`style.rs` 覆盖 audited mesh tone、declared title color 与 transform glyph opacity，`support.rs` 集中 title fixture 与 pixel probe；`template_section_titles.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；section-title test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract axis-label test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_axis_labels_tests.rs`，改为 `template_axis_labels_tests/{mod,identity,paint,style,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 axis-label/scale-link 分类，`paint.rs` 覆盖 scale-link glyph 与 compact axis text 像素输出，`style.rs` 覆盖 audited axis tone 与 scale-link origin，`support.rs` 集中 label fixture、FrameRect 转换与 pixel probe；`template_axis_labels.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；axis-label test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract template-style test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_style_tests.rs`，改为 `template_style_tests/{mod,state,surface,support}.rs`：父 `mod.rs` 只声明测试域，`state.rs` 覆盖 shared painter priority 与 style-state values，`surface.rs` 覆盖 pressed/disabled surface priority 与 declared background fallback，`support.rs` 集中 button fixture 与 resolved background style 构造；`template_style.rs` 只改测试挂载路径。 | touched Rust files `rustfmt --edition 2021 --check` 通过；template-style test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract sprite-atlas test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/sprite_atlas_tests.rs`，改为 `sprite_atlas_tests/{mod,resolver,support}.rs`：父 `mod.rs` 只声明测试域，`resolver.rs` 覆盖 project/library atlas manifest、texture 与 UV resolution，`support.rs` 集中 unique temp root helper；`sprite_atlas.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；sprite-atlas test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract chip test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_chips_tests.rs`，改为 `template_chips_tests/{mod,identity,paint,support}.rs`：父 `mod.rs` 只声明测试域，`identity.rs` 覆盖 Workbench/status chip 分类，`paint.rs` 覆盖 viewport chip surface/border/text/chevron 与 focused border 像素输出，`support.rs` 集中 chip fixture 与 pixel probe；`template_chips.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；chip test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract Material state-layer test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/material_state_layer_tests.rs`，改为 `material_state_layer_tests/{mod,commands,ripple,support}.rs`：父 `mod.rs` 只声明测试域，`commands.rs` 覆盖 ripple-enabled state-layer command sequencing，`ripple.rs` 覆盖 ripple diameter 与 zero-press origin geometry contract，`support.rs` 集中 frame fixture；`material_state_layer.rs` 只改测试挂载路径。 | touched Rust files `rustfmt --edition 2021 --check` 通过；Material state-layer test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract property-row test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 暂缓） | 删除单文件 `host_contract/paint_template_nodes/template_property_rows_tests.rs`，改为 `template_property_rows_tests/{mod,axis,component,support}.rs`：父 `mod.rs` 只声明测试域，`axis.rs` 覆盖 axis/value/unit parsing，`component.rs` 覆盖 component property row identity 与 label-width routing，`support.rs` 集中 component row fixture 与 frame helper；`template_property_rows.rs` 只改测试挂载路径并移除旧测试入口专用导入。 | touched Rust files `rustfmt --edition 2021 --check` 通过；property-row test owner-tree ownership scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；未重启 package-level Cargo check，因为 active runtime compile lanes 正在占用验证车道且本会话前序 editor package/test-compile 尝试已超时无诊断。 | 继续 M3.S2：继续拆分 retained-host 深层 owner；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | 08 M3.S2 host_contract generic label test owner-tree split | 完成（测试代码、文档记录、轻量检查通过；package-level Cargo check 600s 超时无诊断） | 删除单文件 `host_contract/paint_template_nodes/template_node_labels_tests.rs`，改为 `template_node_labels_tests/{mod,property,support}.rs`：父 `mod.rs` 只声明测试域，`property.rs` 覆盖 PropertyRow label/value visibility，`support.rs` 集中 property-row label fixture；`template_node_labels.rs` 只改测试挂载路径。 | touched Rust files `rustfmt --edition 2021 --check` 通过；generic template-node label test owner-tree ownership scan、root single-test-file scan、broad related-code stale-path scan、冲突标记扫描、scoped `git diff --check` 通过（仅既有 LF/CRLF warning）；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-test-owner-cleanup-0622 --message-format short --color never` 600s 超时且未产出 actionable diagnostics，超时后已停止本会话启动的 editor Cargo/Rustc 进程，runtime 测试通道保留不动。 | 继续 M3.S2：`paint_template_nodes/` 下单文件 `*_tests.rs` 已清零；后续测试窗口补跑 editor package check 与完整测试矩阵。 |
| 2026-06-22 | Editor UI 10 S8 component-adapter `dead_code` suppression sweep（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 按 `engine-code-structure-convention.md` E6 / Editor UI 10 M5.T2 移除 `ui/template_runtime/component_adapter/registry.rs` 5 处陈旧生产 `#[allow(dead_code)]`，并新增 `docs/zircon_editor/ui/template_runtime/component_adapter/index.md` 说明 registry 数据源与 envelope 分发契约。 | `rustfmt --edition 2021 --check` 通过；registry `dead_code` 抑制扫描清零；runtime access 与 component adapter 测试使用点扫描通过；S8 剩余扫描仅命中 `ui/workbench/state/editor_world_slot.rs` 4 处；未重启 package-level Cargo check。 | 继续 Editor UI 10 S8：审计 `editor_world_slot.rs` 抑制；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S8 workbench world-slot `dead_code` suppression sweep（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 按 `engine-code-structure-convention.md` E6 / Editor UI 10 M5.T2 移除 `ui/workbench/state/editor_world_slot.rs` 4 处陈旧生产 `#[allow(dead_code)]`；`loaded(world)` 保持生产构造入口，`snapshot/with_world/with_world_mut` 改为 `cfg(test)` 测试便捷入口；新增 `docs/zircon_editor/ui/workbench/state/editor_world_slot.md`。 | `rustfmt --edition 2021 --check` 通过；`registry.rs` + `editor_world_slot.rs` dead_code 抑制扫描清零；生产 `ui/**` 中测试专用 world 访问方法扫描零命中；测试树保留 57 个便捷调用点；未重启 package-level Cargo check。 | Editor UI 10 S8 两个证据路径已清零；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` template-node projection helper（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/template_node_projection.rs`，把 legacy `ModelRc<T>` 到 `TemplatePaneNodeData` 的重复映射收束为 `project_nodes<T, F>` / `project_node_vec<T, F>`；更新 hierarchy、inspector、console、generated bottom fallback、assets activity、animation editor、asset browser、project overview 调用点；新增 `docs/zircon_editor/ui/retained_host/pane_data_conversion.md`。 | `rustfmt --edition 2021 --check` 通过；旧 `map_model_rc(&data.nodes, ...)` template-node 样板扫描清零；helper 与调用点扫描通过；未重启 package-level Cargo check。 | 继续 Editor UI 10 S7：拆 `inspector_template_projection(...)` 与 `animation_template_projection(...)` 大函数；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` inspector fields owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 600s 超时无诊断） | 新增 `ui/retained_host/ui/pane_data_conversion/inspector_fields.rs`，迁出 inspector fallback 字段模型、frame/height 计算、plugin component header/diagnostic/property 节点、fallback message 与文本/数字/按钮节点 helper；`mod.rs` 保留 projection orchestration。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` inspector 字段 helper 残留扫描清零，仅保留模块声明/use 与 2 个调用点；行数采样：`mod.rs` 910 行、`inspector_fields.rs` 547 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir E:\cargo-targets\zircon-editor-ui-pane-data-s7-0622 --message-format short --color never` 600s 超时无 actionable diagnostics，超时后未发现 editor Cargo/Rustc 残留，runtime/plugin 进程保留。 | 继续 Editor UI 10 S7：拆 `animation_template_projection(...)` 或继续收薄 inspector payload extraction；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` animation projection owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/animation_projection.rs`，迁出 `animation_template_projection(...)`、AnimationSequence/AnimationGraph payload 分支与 `SharedString` list 转换；`mod.rs` 仅保留 animation pane façade/native fallback。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 sequence/graph payload 分支与 string-list helper 残留扫描清零，仅保留 animation façade 2 个调用点与 native fallback 字段；行数采样：`mod.rs` 851 行、`animation_projection.rs` 55 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 console/hierarchy projection orchestration 或其它 pane payload extraction；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` hierarchy projection owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/hierarchy_projection.rs`，迁出 `hierarchy_template_projection(...)`、HierarchyV1 scene-node DTO 转换，以及 `HierarchyListPanel`/`SelectRoot` template state 标记；`mod.rs` 仅保留 hierarchy pane façade/native fallback。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 hierarchy payload 分支与 state helper 残留扫描清零，仅保留 hierarchy façade 2 个调用点；私有 payload 子类型 import 扫描清零；行数采样：`mod.rs` 777 行、`hierarchy_projection.rs` 78 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 console projection orchestration 或其它 pane payload extraction；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` console projection owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/console_projection.rs`，迁出 `console_template_projection(...)`、ConsoleV1 payload status fallback、runtime surface layout 与 host-model node projection；`mod.rs` 仅保留 console pane façade/native fallback。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 console payload 分支与 `payload_status_text` fallback 残留扫描清零，仅保留 console façade 2 个调用点；私有 payload 子类型 import 扫描清零；行数采样：`mod.rs` 796 行、`console_projection.rs` 56 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 retained-host pane conversion 中剩余 root orchestration；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` inspector projection owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/inspector_projection.rs`，迁出 `inspector_template_projection(...)`、InspectorV1 payload 字段读取、plugin component payload 到 view-data 的转换、runtime surface layout 与 host-model node projection；`mod.rs` 仅保留 inspector pane façade/native fallback。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 inspector payload 分支与 payload 字段读取残留扫描清零，仅保留 inspector façade 2 个调用点与测试 fixture；私有 inspector payload 子类型 import 扫描清零；行数采样：`mod.rs` 678 行、`inspector_projection.rs` 126 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 generated-bottom/template-node runtime orchestration 或其它 pane payload extraction；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` generated-bottom projection owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/generated_bottom_projection.rs`，迁出 generated-bottom pane conversion、template-runtime node projection 调用、native fallback 和 status 装配；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 generated-bottom DTO/status/fallback 组装实现残留扫描清零；行数采样：`mod.rs` 658 行、`generated_bottom_projection.rs` 30 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 component showcase 或共享 template-node runtime helper；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` component-showcase projection owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/component_showcase_projection.rs`，迁出 component showcase pane conversion、UiComponentShowcase payload-kind gating、builtin/runtime projection 分支、surface layout 与 host-model node projection；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 component-showcase payload gating 与 template projection 实现残留扫描清零，剩余 host-model projection 命中仅为共享 helper；行数采样：`mod.rs` 608 行、`component_showcase_projection.rs` 62 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆共享 template-node runtime helper 或 remaining fallback façade；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` template-runtime projection helper owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/template_runtime_projection.rs`，迁出 shared pane template-runtime node projection、builtin runtime fallback、surface layout 与 content-size fallback 规则；`mod.rs` 仅保留私有导入供现有子模块调用。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 shared template-runtime helper 实现、`OnceLock`、`UiSize` 与 host-model projection 残留扫描清零；子模块调用点保持 `super::project_pane_template_nodes*`/`pane_template_runtime`/`builtin_host_runtime`；行数采样：`mod.rs` 529 行、`template_runtime_projection.rs` 90 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 remaining fallback façade / native conversion owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` hierarchy native façade owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 将 hierarchy pane façade/native fallback、legacy SceneNodeData DTO 转换补迁到 `ui/retained_host/ui/pane_data_conversion/hierarchy_projection.rs`；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 hierarchy native/template projection 实现与 scene-node DTO 转换残留扫描清零；行数采样：`mod.rs` 489 行、`hierarchy_projection.rs` 129 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 project-overview/assets/console native conversion 或泛型 model projection helper；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` console native façade owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 将 console pane façade/native fallback、legacy `ConsoleTextPanel`→`ConsoleBodySection` id 修正补迁到 `ui/retained_host/ui/pane_data_conversion/console_projection.rs`；`mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 console native façade 与 legacy id 修正实现残留扫描清零；行数采样：`mod.rs` 456 行、`console_projection.rs` 94 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 project-overview/assets/animation native conversion 或泛型 model projection helper；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` inspector pane test owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/inspector_pane_tests.rs`，迁出父 `mod.rs` 内联 inspector pane projection 行为测试、fixture 与 node lookup helper；父 `mod.rs` 只保留 `#[cfg(test)]` 测试模块挂载。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 inspector 测试函数、fixture、plugin component fixture 类型与 `blank_viewport_chrome` 残留扫描清零；行数采样：`mod.rs` 186 行、`inspector_pane_tests.rs` 260 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 project-overview/assets/animation native conversion 或泛型 model projection helper；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` animation native façade owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 将 animation pane façade/native fallback 补迁到 `ui/retained_host/ui/pane_data_conversion/animation_projection.rs`；子模块同时拥有 `AnimationSequenceV1`/`AnimationGraphV1` template projection，父 `mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 animation native façade、`AnimationEditorPaneViewData`/`AnimationEditorPaneData` 与 `native_body.animation` 残留扫描清零；行数采样：`mod.rs` 149 行、`animation_projection.rs` 103 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 inspector façade 或 project-overview/assets native conversion；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` inspector native façade owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 将 inspector pane façade/native fallback 补迁到 `ui/retained_host/ui/pane_data_conversion/inspector_projection.rs`；子模块同时拥有 `InspectorV1` template projection 与 inspector field 节点追加，父 `mod.rs` 通过 re-export 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 inspector native façade、`InspectorPaneViewData`/`InspectorPaneData`、`native_body.inspector`、`InspectorVisualFields` 与 `inspector_field_nodes` 残留扫描清零；行数采样：`mod.rs` 109 行、`inspector_projection.rs` 168 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：继续拆 project-overview/assets native conversion 或共享 model projection helper；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` native template-node panes owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/native_template_node_panes.rs`，迁出 assets activity、asset browser、project overview 三个 native template-node pane DTO 转换；父 `mod.rs` 通过 `pub(super) use` 保持既有入口。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中三类 pane view/data DTO、三个 native template-node pane 转换实现、`to_host_contract_template_node_owned` 与 `project_nodes` 残留扫描清零；行数采样：`mod.rs` 86 行、`native_template_node_panes.rs` 31 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估共享 model projection helper owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` model projection helper owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/model_projection.rs`，迁出共享 `map_model_rc(...)`；build export、module plugins、performance timeline owner 直接引用该 helper，父 `mod.rs` 不再持有转换行为。 | `rustfmt --edition 2021 --check` 通过；旧 `super::map_model_rc` 扫描清零；父 `mod.rs` 中 `model_rc`/`ModelRc`/`map_model_rc` 残留扫描清零；行数采样：`mod.rs` 70 行、`model_projection.rs` 16 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：转向其它仍超长 pane owner 的内部 owner 拆分；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` UI Asset detail fields owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 删除旧 UI Asset detail field 单文件 owner，新增 `ui/retained_host/ui/pane_data_conversion/ui_asset_detail_fields/{mod,binding,layout,row_model,section_nodes,sections,slot,widget}.rs`；保留 template-node conversion 入口，并拆出 Widget/Slot/Layout/Binding 行构建、row DTO/helper、section 聚合和 retained section node 注入。 | `rustfmt --edition 2021 --check` 通过；旧 owner 文件 `Test-Path` 为 false；父级 flat files 中旧内部函数名残留扫描清零；行数采样：`binding.rs` 55 行、`layout.rs` 100 行、`mod.rs` 31 行、`row_model.rs` 44 行、`section_nodes.rs` 133 行、`sections.rs` 35 行、`slot.rs` 172 行、`widget.rs` 91 行；未重启 package-level Cargo check，因为 runtime lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 `pane_ui_asset_conversion.rs`、Build/Export 或其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` build export owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 删除旧 Build/Export pane 单文件 owner，新增 `ui/retained_host/ui/pane_data_conversion/build_export/{mod,target_rows,tests}.rs`；保留 Build/Export pane conversion 入口，并拆出 fallback target-row/action button projection 与原行为测试 owner。 | `rustfmt --edition 2021 --check` 通过；旧 owner 文件 `Test-Path` 为 false；父级 flat files 中 target-row/action helper 残留扫描清零；行数采样：`mod.rs` 73 行、`target_rows.rs` 340 行、`tests.rs` 399 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 `pane_ui_asset_conversion.rs` 或其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` UI Asset pane conversion test owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion_tests.rs`，迁出 `pane_ui_asset_conversion.rs` 内联 UI Asset pane conversion 行为测试和 fixture imports；父 `mod.rs` 只保留 `cfg(test)` 测试 owner 挂载，生产 owner 继续持有 UI Asset pane host-contract conversion。 | `rustfmt --edition 2021 --check` 通过；生产 owner 中 `#[cfg(test)]`/`mod tests`/测试函数/`ViewTemplateFrameData`/`ViewTemplateNodeData` 残留扫描清零；行数采样：`pane_ui_asset_conversion.rs` 466 行、`pane_ui_asset_conversion_tests.rs` 247 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 UI Asset production conversion 的 preview/style/inspector 数据子域或其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` UI Asset production owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 删除旧 UI Asset production conversion 单文件 owner，新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion/{mod,preview,style,inspector,string_selection}.rs`；`mod.rs` 保留 `to_host_contract_ui_asset_pane(...)` façade 与顶层装配，Preview/Style/Inspector 数据转换和 shared-string helper 分别下沉到子 owner。 | `rustfmt --edition 2021 --check` 通过；旧 owner 文件 `Test-Path` 为 false；新 owner tree 中 stale `super::to_host_contract_shared_string_list` 扫描清零；行数采样：`mod.rs` 160 行、`preview.rs` 103 行、`style.rs` 112 行、`inspector.rs` 176 行、`string_selection.rs` 17 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 UI Asset runtime-report/designer/palette-drag 装配或其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` UI Asset runtime-report/designer/palette owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion/{runtime_report,designer_tools,palette_drag}.rs`；迁出 runtime report、designer tool state、palette drag 与 slot target DTO 转换，`preview.rs` 不再持有 palette drag slot target helper，`mod.rs` 保留 UI Asset pane façade 与 header/actions/collections/source 顶层装配。 | `rustfmt --edition 2021 --check` 通过；`mod.rs` 中 runtime-report/designer/palette raw fields 和 DTO struct 残留扫描清零；`preview.rs` 中 `CanvasSlotTarget`/palette drag helper 残留扫描清零；行数采样：`mod.rs` 119 行、`preview.rs` 84 行、`runtime_report.rs` 46 行、`designer_tools.rs` 13 行、`palette_drag.rs` 43 行、`style.rs` 112 行、`inspector.rs` 176 行、`string_selection.rs` 17 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 UI Asset header/actions/collections/source 或其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` UI Asset header/actions/collections/source owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 新增 `ui/retained_host/ui/pane_data_conversion/pane_ui_asset_conversion/{header,actions,collections,source}.rs`；迁出 UI Asset header/action/collection/source DTO 转换，`mod.rs` 只保留 template-node detail field 调用、子 owner 调用和最终 `UiAssetEditorPaneData` 总装。 | `rustfmt --edition 2021 --check` 通过；`mod.rs` 中 header/actions/collections/source raw fields 和 DTO struct 残留扫描清零；行数采样：`mod.rs` 71 行、`actions.rs` 34 行、`collections.rs` 23 行、`header.rs` 15 行、`source.rs` 23 行、`preview.rs` 84 行、`runtime_report.rs` 46 行、`designer_tools.rs` 13 行、`palette_drag.rs` 43 行、`style.rs` 112 行、`inspector.rs` 176 行、`string_selection.rs` 17 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：转向其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` Build/Export target rows owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 删除旧 `build_export/target_rows.rs`，新增 `build_export/target_rows/{mod,actions,identity,metrics,node,row}.rs`；保留 `build_export_target_row_nodes(...)` 入口，拆出 target id/key、布局常量、基础 node 构造、row action/button projection 和单 row 节点组装。 | `rustfmt --edition 2021 --check` 通过；旧 owner 文件 `Test-Path` 为 false；`build_export/mod.rs` 中 target-row 内部 helper/constant 残留扫描清零；行数采样：`mod.rs` 92 行、`actions.rs` 120 行、`identity.rs` 31 行、`metrics.rs` 7 行、`node.rs` 18 行、`row.rs` 124 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆其它仍偏大的 pane owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection helper owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；binding/dialog 后续已目录化） | 新增 `pane_component_projection/{attribute_values,badge,binding_actions,button_style,collection_window,dialog,string_lists,value_color}.rs`；迁出 root `mod.rs` 中的属性读取、Badge/Dialog 投影、binding action 派生、button-style 别名、virtual collection window、shared string list 与 value-color helper，测试改为显式导入 `UiEventKind`。该 binding action helper 后续已硬切为 `pane_component_projection/binding_actions/` tree，dialog helper 后续已硬切为 `pane_component_projection/dialog/` tree。 | 当时 `rustfmt --edition 2021 --check` 通过；`mod.rs` 中旧 helper 函数定义残留扫描清零；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 binding action 与 dialog owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection surface metrics owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；后续已目录化） | 先新增 surface metrics projection owner；迁出 `surface_defaults.rs` 中的 corner radius、border width、elevation、z-index、MUI z-index 常量与默认 helper，`surface_defaults.rs` 保留 variant/surface/text/validation role-token 逻辑。该 owner 后续已硬切为 `pane_component_projection/surface_metrics/` tree。 | `rustfmt --edition 2021 --check` 通过；`surface_defaults.rs` 中 surface metric/z-index 函数与 MUI z-index 常量残留扫描清零；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 surface metrics owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection surface defaults owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项） | 删除旧 `pane_component_projection/surface_defaults.rs`，新增 `surface_defaults/{mod,alert_dialog,badge,chip,component_variant,shared,skeleton,surface,text_tone,validation}.rs`；保留四个上层投影入口，组件变体、MUI role token、surface/text/validation 和共享属性解析分别由子 owner 持有。 | `rustfmt --edition 2021 --check` 通过；旧 owner 文件 `Test-Path` 为 false；`pane_component_projection/*.rs` 中 `surface_defaults.rs` 引用扫描清零；行数采样：`surface_defaults/mod.rs` 43 行、`component_variant.rs` 91 行、`badge.rs` 114 行、`alert_dialog.rs` 86 行、`chip.rs` 93 行、`skeleton.rs` 55 行、`shared.rs` 77 行、`surface.rs` 54 行、`text_tone.rs` 27 行、`validation.rs` 31 行、`mod.rs` 847 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：拆 `host_template_node(...)` 主体或其它偏大的 pane component projection owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection world-space owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；后续已目录化） | 新增 world-space projection owner；迁出 world-space 属性读取、默认值、vector component 展开和 retained-host world field 中间 projection，父 `host_template_node(...)` 只保留调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/world_space/` tree。 | 当时 `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 world-space 属性解析残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 800 行、world-space owner 82 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 world-space owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection world-space owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未通过确认，已修复投影 owner 可见性诊断） | 删除旧单文件 world-space owner，新增 `pane_component_projection/world_space/{mod,activation,model,rendering,surface,transform}.rs`；world-space 总装、DTO、enabled gate、position/rotation/scale transform、world size/pixels 与 rendering metadata 分别落到具名叶子；复验暴露的 sibling/parent owner re-export 边界已统一收敛为 `pub(in super::super)`。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；旧生产路径扫描清零；行数采样：`transform.rs` 41 行、`mod.rs` 34 行、`rendering.rs` 31 行、`surface.rs` 24 行、`model.rs` 19 行、`activation.rs` 11 行；`cargo check -p zircon_editor --locked` 到达 editor 编译层但未通过，首批 pane-component 诊断为 E0364/E0365/E0603 private re-export/import，可见性修正后因独立 runtime cargo/rustc lanes 活跃暂未复跑；该 check 仍含其它 editor 层既有错误。 | 继续 Editor UI 10 S7：等编译通道空闲后复跑 package check，或继续评估剩余中型 projection owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection collection/pagination owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；后续已目录化） | 新增 collection/pagination projection owner；迁出 collection items、collection fields、virtualization window 和 pagination counter projection，父 `host_template_node(...)` 只保留调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/collection_projection/` tree。 | 当时 `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 collection/virtualization/pagination 属性解析残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 736 行、collection/pagination owner 87 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 collection projection owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection collection projection owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 collection projection owner，新增 `pane_component_projection/collection_projection/{mod,fields,items,model,pagination,virtualization}.rs`；collection 总装、中间 DTO、items/window、fields handoff、pagination 与 virtualization projection 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`virtualization.rs` 45 行、`mod.rs` 35 行、`pagination.rs` 33 行、`items.rs` 26 行、`model.rs` 16 行、`fields.rs` 14 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估剩余中型 projection owner 或进入测试窗口补跑 package/workspace validation；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection value/media owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；后续已目录化） | 新增 value/media projection owner；迁出 value text/number/percent/color、media source、icon name、preview image 加载和 vector component projection，父 `host_template_node(...)` 只保留调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/value_media/` tree。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 value/media 解析残留扫描清零，仅保留 `layout_icon_size` 的布局字段读取和 DTO 字段赋值；旧单文件 owner 当时 121 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 value/media owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection popup/action owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；后续已目录化） | 新增 popup/action projection owner；迁出 menu items/structured menu items、popup open/anchor/frame、accepted drag payloads、showcase/binding action ids、dialog action fallback 与 dispatch kind projection，父 `host_template_node(...)` 只保留调用、transition popup-open 读取和 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/popup_actions/` tree。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 popup/action/menu 解析残留扫描清零，仅保留 `structured_menu_items` DTO 字段赋值；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 popup/action owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection text/layout owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；后续已目录化） | 新增 text/layout projection owner；迁出 text/label、label color/brightness、layout offsets、selected segment border/underline、font size/weight、text align 与 overflow projection，父 `host_template_node(...)` 只保留调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/text_layout/` tree。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 text/layout/selected segment/font/text align/overflow 属性解析残留扫描清零；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 text/layout owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection visual-state owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未通过确认；后续已目录化） | 新增 visual-state projection owner；迁出 checked/expanded/focused/hovered/pressed/dragging/drop-hover/active-drag-target、state-layer、ripple 与 icon-state projection，父 `host_template_node(...)` 只保留调用与 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/visual_state/` tree。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 visual-state 直接属性解析残留扫描清零，仅保留 DTO 字段赋值；旧单文件 owner 当时 91 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 尝试运行但 120 秒超时，未得到通过结论。 | 后续已完成 visual-state owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection selection/options owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启；后续已目录化） | 新增 selection/options projection owner；迁出 selection state、search query、selected flag、tree depth/indent、options text/list、command-palette/notification-center structured options 与 generic structured option fallback，父 `host_template_node(...)` 只保留调用、host 模型转换和 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/selection_options/` tree。 | 当时 `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 selection/options 直接属性解析残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 316 行、selection/options owner 81 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 selection/options owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection selection/options owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 selection/options owner，新增 `pane_component_projection/selection_options/{mod,model,options,search,selection,structured,tree}.rs`；selection state、selected flag、query、tree depth/indent、options list、structured-option handoff 和 fallback 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 33 行、`tree.rs` 24 行、`selection.rs` 23 行、`structured.rs` 17 行、`options.rs` 15 行、`model.rs` 12 行、`search.rs` 10 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估剩余中型 projection owner 或进入测试窗口补跑 package/workspace validation；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection visual-style owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启；后续已目录化） | 新增 visual-style projection owner；迁出 component category/layout token、component variant、surface variant、text tone、button variant/style、corner/border/elevation/z-index 与 transition metadata projection，父 `host_template_node(...)` 只保留调用和 DTO 字段赋值。该 owner 后续已硬切为 `pane_component_projection/visual_style/` tree。 | `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 visual-style helper 调用和局部解析残留扫描清零，仅保留 DTO 字段赋值；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 visual-style owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection validation/drop/clip owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启；drag-overlay 后续已目录化） | 新增 `pane_component_projection/validation_state.rs` 与 `clip_frame.rs`，并把 drag/drop 投影交由当时的 drag-overlay owner 承接 `drop_source_summary`；迁出 disabled/validation level/message、drop source summary 和 clip frame DTO 转换，父 `host_template_node(...)` 只保留调用和 DTO 字段赋值。该 drag-overlay owner 后续已硬切为 `pane_component_projection/drag_overlay/` tree。 | 当时 `rustfmt --edition 2021 --check` 通过；父 `mod.rs` 中 validation/drop/clip 直接属性解析和 inline frame DTO 转换残留扫描清零，仅保留 DTO 字段赋值；行数采样：`mod.rs` 256 行、`validation_state.rs` 32 行、`clip_frame.rs` 19 行、drag-overlay owner 111 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 drag-overlay owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection drag-overlay owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 drag-overlay owner，新增 `pane_component_projection/drag_overlay/{mod,attributes,cursor,drop_target,indicator,model,payload}.rs`；drag-overlay 总装、中间 DTO、attribute conversion、payload、cursor/preview/offset、drop target 与 drop indicator projection 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 59 行、`attributes.rs` 34 行、`cursor.rs` 30 行、`drop_target.rs` 30 行、`model.rs` 25 行、`payload.rs` 23 行、`indicator.rs` 20 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估剩余中型 projection owner 或进入测试窗口补跑 package/workspace validation；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection root-entry hard split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 新增 `pane_component_projection/host_template_node.rs`；将 `host_template_node(...)` 主投影入口与最终 DTO 装配整体迁出，`pane_component_projection/mod.rs` 只保留 child module 声明、`host_template_node` re-export 和测试模块挂载。 | `rustfmt --edition 2021 --check` 通过；`mod.rs` implementation-shape 扫描清零；行数采样：`mod.rs` 39 行、`host_template_node.rs` 224 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估 `host_template_node.rs` 的最终 DTO 装配是否还需要按 field-family 继续拆分；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection template-node DTO assembly owner split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 新增 template-node DTO assembly owner，迁出最终 `TemplatePaneNodeData` 字段装配、host `ModelRc` 转换与 shared-string list 转换，`host_template_node.rs` 只负责投影片段收集和 `ProjectedTemplateNodeParts` 交接；后续该 owner 已硬切为 `template_node_data/` tree。 | `rustfmt --edition 2021 --check` 通过；`host_template_node.rs` 最终 DTO 装配残留扫描清零，`mod.rs` implementation-shape 扫描仍清零；行数采样：`mod.rs` 40 行、`host_template_node.rs` 90 行、DTO assembly owner 206 行；未重启 package-level Cargo check，因为 runtime/plugin lanes 正在占用编译通道。 | 后续已完成 `template_node_data/` field-family owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection test owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 module-local test owner，新增 `tests/{mod,actions,collection,mui_surface_defaults,mui_variants,visual_metadata,world_space}.rs`；module-local projection 行为测试按 action/binding、collection/virtualization、MUI defaults、MUI variants、visual metadata 与 world-space field family 分层。 | `rustfmt --edition 2021 --check` 通过；旧测试 owner `Test-Path` 为 false；旧路径残留扫描清零；行数采样：`tests/mod.rs` 63 行、`actions.rs` 71 行、`collection.rs` 116 行、`mui_surface_defaults.rs` 303 行、`mui_variants.rs` 234 行、`visual_metadata.rs` 349 行、`world_space.rs` 36 行。 | 后续已完成 `template_node_data/` field-family owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection collection-fields owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 collection-fields owner，新增 `collection_fields/{mod,array,map,empty,roles,type_tokens,validation,tests}.rs`；ArrayField、MapField、empty row、role mapping、type token、validation 与私有 owner 测试分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`validation.rs` 111 行、`array.rs` 85 行、`map.rs` 70 行、`tests.rs` 60 行、`roles.rs` 40 行、`empty.rs` 31 行、`mod.rs` 22 行、`type_tokens.rs` 19 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `template_node_data/` field-family owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection showcase-actions owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 showcase-actions owner，新增 `showcase_actions/{mod,action_buttons,binding_ids,commit_action,drag_actions,edit_action,primary_action,tests}.rs`；showcase binding-id 归一化、primary/drag/edit/commit action selection、action-button projection 与私有 owner 测试分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`primary_action.rs` 68 行、`binding_ids.rs` 57 行、`tests.rs` 54 行、`action_buttons.rs` 50 行、`drag_actions.rs` 35 行、`edit_action.rs` 24 行、`commit_action.rs` 19 行、`mod.rs` 16 行；`verify-showcase-state-matrix.mjs` 已改为读取新 owner tree，但仍因既有 deterministic demo-input 覆盖缺口返回非零；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `popup_frame/`、`command_palette/` 与 `template_node_data/` owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection popup-frame owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 popup-frame owner，新增 `popup_frame/{mod,attributes,origin,overlay,placement,tests}.rs`；anchor overlay 判定、attribute read、origin alignment、Popper placement geometry 与私有 owner 测试分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 98 行、`tests.rs` 70 行、`placement.rs` 60 行、`origin.rs` 36 行、`overlay.rs` 26 行、`attributes.rs` 9 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `command_palette/` 与 `template_node_data/` owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection notification-center owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧 notification-center 单文件 owner 与旧单文件测试 owner，新增 `notification_center/{mod,attributes,entries,entry,options,parse,tests}.rs`；notification entry model、attribute/tone conversion、entry parsing、visible-limit filtering、structured option projection 与私有 owner 测试分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner 与旧测试 `Test-Path` 均为 false；行数采样：`tests.rs` 187 行、`options.rs` 70 行、`parse.rs` 70 行、`attributes.rs` 58 行、`entry.rs` 26 行、`entries.rs` 26 行、`mod.rs` 13 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `command_palette/` 与 `template_node_data/` owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection command-palette owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧 command-palette 单文件 owner，新增 `command_palette/{mod,attributes,entries,entry,ids,options,parse,tests}.rs`；command entry model、attribute/query conversion、entry parsing、filtered/recent/disabled id projection、structured option projection 与私有 owner 测试分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`tests.rs` 127 行、`options.rs` 67 行、`parse.rs` 53 行、`attributes.rs` 42 行、`entries.rs` 33 行、`ids.rs` 30 行、`entry.rs` 24 行、`mod.rs` 13 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 后续已完成 `template_node_data/` field-family owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection template-node DTO assembly owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧 template-node DTO assembly 单文件 owner，新增 `template_node_data/{mod,assembly,content,identity,interaction,options_collection,parts,spatial,visual}.rs`；投影片段 DTO、装配顺序、identity/content/options-collection/spatial/interaction/visual 字段族赋值分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`visual.rs` 53 行、`interaction.rs` 50 行、`content.rs` 44 行、`assembly.rs` 40 行、`options_collection.rs` 34 行、`spatial.rs` 31 行、`parts.rs` 29 行、`identity.rs` 14 行、`mod.rs` 11 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估其它 pane component projection 中型 owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection popup/action owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未通过确认） | 删除旧 popup/action 单文件 owner，新增 `popup_actions/{mod,action_buttons,action_ids,drag_payloads,menu,model,popup_state}.rs`；总装入口、dialog/showcase action buttons、action id selection、accepted drag payload serialization、menu projection、intermediate model 与 popup open/anchor/frame state 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；旧路径扫描清零；行数采样：`mod.rs` 72 行、`action_ids.rs` 63 行、`popup_state.rs` 55 行、`action_buttons.rs` 22 行、`menu.rs` 21 行、`model.rs` 20 行、`drag_payloads.rs` 16 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 尝试运行但 300 秒超时，未得到通过结论，超时残留的 cargo/rustc 进程已停止。 | 后续已完成 text/layout owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection text/layout owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧 text/layout 单文件 owner，新增 `text_layout/{mod,attributes,label,model,offsets,selected_segment,text,typography}.rs`；总装入口、文本来源选择、label fields、layout offsets、selected segment metrics、typography/overflow 和中间模型分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`offsets.rs` 56 行、`mod.rs` 55 行、`typography.rs` 37 行、`label.rs` 29 行、`text.rs` 29 行、`selected_segment.rs` 28 行、`model.rs` 23 行、`attributes.rs` 12 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估其它 pane component projection 中型 owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection surface metrics owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 300s 超时无通过结论） | 删除旧单文件 surface metrics owner，新增 `surface_metrics/{mod,border,corner,elevation,values,z_index}.rs`；corner/radius/square、outlined/dialog border width、MUI surface elevation、MUI z-index/default fallback、TOML int conversion 与 variant helper 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`elevation.rs` 44 行、`z_index.rs` 39 行、`border.rs` 28 行、`corner.rs` 27 行、`values.rs` 13 行、`mod.rs` 9 行；`cargo check -p zircon_editor --lib --locked --jobs 1 --message-format short --color never` 300 秒超时，未得到通过结论，超时后 cargo/rustc 进程未残留。 | 后续已完成 transition metadata owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection transition metadata owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 transition metadata owner，新增 `transition_metadata/{mod,direction,kind,model,state,timing}.rs`；总装入口、DTO、transition kind、state/progress/entered、duration/easing 默认值和 slide direction fallback 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`state.rs` 72 行、`timing.rs` 48 行、`mod.rs` 33 行、`kind.rs` 24 行、`direction.rs` 19 行、`model.rs` 9 行；package-level Cargo check 未重启，因为上一 surface metrics 切片刚以 300 秒超时结束，后续测试窗口统一补跑。 | 后续已完成 value/media owner-tree split；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection dialog owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 dialog owner，新增 `dialog/{mod,actions,message}.rs`；plain dialog value text、plain dialog action、confirm/alert cancel/confirm action DTO projection 分别落到具名叶子，`mod.rs` 只保留受限导出。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；旧生产路径扫描清零；行数采样：`actions.rs` 75 行、`message.rs` 11 行、`mod.rs` 4 行；package-level Cargo check 未重启，因为独立 runtime cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估剩余中型 projection owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection binding-actions owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 binding-actions owner，新增 `binding_actions/{mod,path,primary}.rs`；Click/Change/Submit primary binding/action selection 与 binding path segment normalization 分别落到具名叶子，`mod.rs` 只保留受限导出。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`primary.rs` 35 行、`path.rs` 25 行、`mod.rs` 6 行；package-level Cargo check 未重启，因为独立 runtime cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估剩余中型 projection owner；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection visual-style owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 通过） | 删除旧单文件 visual-style owner，新增 `visual_style/{mod,button,component,model,surface}.rs`；总装入口、style DTO、component token/variant、button style alias、surface metrics 与 transition metadata handoff 分别落到具名叶子。验证期同步修复 retained-host/native-pointer、paint-template-node 与 editor AssetManager handle/trait 接口收束后暴露的最低层可见性和类型边界。 | `cargo fmt -p zircon_editor --check` 通过；旧 owner `Test-Path` 为 false；旧生产路径与冲突标记扫描清零；行数采样：`mod.rs` 41 行、`surface.rs` 28 行、`button.rs` 21 行、`component.rs` 20 行、`model.rs` 16 行；`cargo check -p zircon_editor --locked --message-format short --color never` 通过；scoped `git diff --check` 通过，仅提示既有 LF/CRLF 工作树转换。 | 继续 Editor UI 10 S7：评估其它 pane component projection 中型 owner 或进入测试窗口；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection value/media owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 value/media owner，新增 `value_media/{mod,icon,media,model,number,text,vector}.rs`；value/media 总装、DTO、value text fallback、numeric alias、media source、icon name 和 vector component extraction 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`mod.rs` 46 行、`text.rs` 27 行、`media.rs` 20 行、`icon.rs` 18 行、`number.rs` 14 行、`model.rs` 13 行、`vector.rs` 8 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估其它 pane component projection 中型 owner 或进入测试窗口；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |
| 2026-06-22 | Editor UI 10 S7 `pane_data_conversion` pane component projection visual-state owner-tree split（08 cross-plan） | 完成（结构治理代码与文档记录；非 08 主里程碑收尾项；package-level Cargo check 未重启） | 删除旧单文件 visual-state owner，新增 `visual_state/{mod,flags,icon,model,ripple,state_layer}.rs`；visual-state 总装、DTO、interaction flags、state-layer、ripple 与 icon-state projection 分别落到具名叶子。 | `rustfmt --edition 2021 --check` 通过；旧 owner `Test-Path` 为 false；行数采样：`flags.rs` 38 行、`mod.rs` 34 行、`ripple.rs` 34 行、`icon.rs` 25 行、`state_layer.rs` 25 行、`model.rs` 21 行；package-level Cargo check 未重启，因为 cargo/rustc lanes 正在占用编译通道。 | 继续 Editor UI 10 S7：评估其它 pane component projection 中型 owner 或进入测试窗口；08 M3.S2 后续仍以 retained-host owner 收束和测试窗口补跑为准。 |

| 2026-06-24 | Editor UI 08 M2/M3 retained-host screenshot layout validation | 完成（截图 harness 通过；外部 profile capture 被本机杀毒拦截；Cargo package check 仍受并行编译通道影响未重跑完成） | 修复 `retained_host/callback_dispatch/template_bridge/workbench/drawer_layout.rs` 中 componentized Workbench 左/右抽屉未套用 compact side width limit 的问题；窄宽 900x620 下 document/viewport 不再被左右抽屉压缩到 16px。同步收紧 `visual_screenshot.rs` 的 viewport containment/status-bar bottom-edge 断言，并补充 legacy editor button variant 分类断言，防止 Project Overview 普通 `Button` 掉回不一致的 fallback 分类。 | `capture_m3_gui_acceptance_visual_artifacts` 直接运行测试二进制通过 1/1，生成 `target/visual-layout/editor-window-m3-{workbench,welcome-input,asset-browser,assets-drawer,menu-popup-svg-icons,drag-after-release,svg-icon-scale-small,svg-icon-scale-large}.png`（2026-06-24 06:26）；人工查看 900x620、640x420、1260x780 与菜单 popup 截图，主带、侧栏、底栏、弹出菜单和响应式尺寸均可见且无 document collapse。`tools/ui-profile-capture.ps1` 被杀毒提示 malicious content 拦截，未计通过。 | 后续若继续做像素级设计收敛，优先处理 Project Overview 左栏按钮文字偏紧与顶部工具条截断观感；本轮不把该视觉观察记为失败，因为几何 containment 与截图 harness 已通过。 |
| 2026-06-24 | Editor UI 08 M2/M3 Project Overview button readability closeout | 完成（截图 harness 通过；Project Overview 按钮文字完整可读；editor package check 通过；完整 lib-test matrix 未跑完） | 关闭上一条截图观察项：`view_projection.rs` 对 component-owned `Button` 使用文本 carrier + 最大非文本 frame，避免普通按钮文本丢失或重复；`template_buttons/identity.rs` 接受 legacy editor `primary/secondary/...` variant；`paint_text/font.rs` 暴露 fallback 字体实际 advance 测量，`template_buttons/content/metrics.rs`/`entry.rs` 使用真实 label width，避免 128px Project Overview `Asset Browser` 按钮换行裁切。侧抽屉 compact layout、viewport containment/status bottom-edge 断言和按钮投影断言仍保持在既有 owner/test tree 内。 | 直接二进制验证：`editor_variant_button_uses_centered_button_text_path` 1/1 通过；`project_overview_projection_maps_bootstrap_asset_into_template_nodes` + `workbench_button_matches_button_nodes_without_icon_or_tab_nodes` 2/2 通过；ignored `capture_m3_gui_acceptance_visual_artifacts` 1/1 通过并在 2026-06-24 09:43 +08 刷新 8 张 `target/visual-layout/editor-window-m3-*.png`。人工检查 `editor-window-m3-workbench-900x620.png` 和 `editor-window-m3-workbench-900x620-project-buttons-crop.png`：左抽屉、Scene viewport、右 Inspector、底部 Console/status 均可见，`Open Assets`/`Asset Browser` 完整可读。`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --message-format short --color never` 于 2026-06-24 09:59 +08 通过，仅有既有 warning 噪声；随后 `cargo test -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --message-format short --color never -- --test-threads=1` 20 分钟编译期超时，残留本轮 editor Cargo/Rustc 已停止，不计通过；`tools/ui-profile-capture.ps1` 仍被本机杀毒拦截。 | 当前 08 布局/截图目标已完成且 editor package check 已补绿；后续只剩空闲编译窗口补完整 `cargo test -p zircon_editor --lib --locked` / integration package matrix，不再保留 Project Overview 按钮文字专项 follow-up。 |
| 2026-06-24 | Editor UI 08 M2/M3 final workbench event and screenshot validation | 完成（结构、编译、定向事件、截图均通过；完整执行矩阵未声明通过） | 修复 runtime UI popup/modal focus scope 的最低层输入 owner 校验：popup 打开时只自动聚焦仍属于有效输入 owner 的可聚焦后代，隐藏/折叠祖先下的控件不再偷焦点，也不会把 Workbench 模块动作误报为 `MissingNode`。`componentized_window.rs` 的节点布尔 mutation 错误补 `LayoutMutation { node_id, property, source }` 语境；截图 harness 输出迁到 `docs/tests/editor`，并加入 900x620 reference Workbench document 宽度/viewport containment/status bottom 断言。 | `audit_editor_structure.py --json` 通过且 `m1_gate_status = classified-and-clear`，migration debt、oversized production file、production dead-code suppression、duplicate test tree、ui module owner boundary violation 均为 0。`CARGO_INCREMENTAL=0 cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --quiet` 通过；同 target 的 `cargo test -p zircon_editor --lib --locked --no-run --jobs 1 --quiet` 通过；直接二进制 `widget_popup` 3/3 通过，`declared_workbench_module_events_dispatch_preview_actions` 1/1 通过，ignored `capture_m3_gui_acceptance_visual_artifacts` 1/1 通过并刷新 8 张 `docs/tests/editor/editor-window-m3-*.png`。人工核验 900x620 Workbench/Asset Browser/Assets drawer：主 document、Scene viewport、左右侧栏、底 Console、status bar 与 popup 均在视口内，未出现中心内容坍缩。 | 完整执行版 `cargo test -p zircon_editor --lib --locked` 与 integration package matrix 仍需独立长窗口补跑；既有 warning 噪声不在本轮收口范围。 |
| 2026-06-25 | Editor UI 08/10 ZUI governance and layout-data hard-cutover follow-up | 部分完成（ZUI/layout/check/screenshot 与 Workbench projection focused execution 通过；`menu_popup_nodes_` 与完整 lib/integration matrix 未关闭） | 按 08/10 与全局结构规范继续收束：builtin host bridge 不再导出 componentized drawer shell/header frames；editor layout custom data 从 `.v2.ui.toml` 硬切到 owner-specific `.toml`，避免 ZUI 扫描器误解析非 ZUI schema；floating workbench asset 改为 `workbench_command_palette.zui` / `workbench_preferences.zui` 并同步 locator；ZUI governance 允许 `workbench/floating/` owner、DocumentTabs namespace、tokenized axis/material tokens 与新版 L4 topology；`asset_editor/node_projection.rs` 补齐 `ViewTemplateNodeData.options` 投影；`depth_prepass.rs` 只做 shared runtime 编译阻塞的最小 owner 修正；`chrome_template_projection.rs` 将菜单 chrome/popup 投影抽到 `chrome_template_projection/menu_chrome.rs`，父文件降到 716 行、子 owner 344 行。 | `cargo test -p zircon_editor --lib zui_asset_governance --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --message-format short --color never -- --test-threads=1 --nocapture` 通过 66/66；直接 `editor_layout_contracts` 二进制通过 12/12；`cargo test -p zircon_editor --lib page_layout_templates ...` 通过 4/4；`audit_editor_structure.py --json` 复跑为 `classified-and-clear` 且 migration debt/oversized/dead-code suppression 均为 0；`cargo check -p zircon_editor --lib --locked --jobs 1 --target-dir D:\cargo-targets\zircon-editor-ui-completion-audit-0624 --message-format short --color never` 通过；本轮后续 WSL `workbench_projection` focused execution 41/41 通过。人工复核 `docs/tests/editor` 的 900x620 Workbench/menu popup、640x420 compact 与 1260x780 large 截图：主 document、viewport、侧栏、底栏和 popup 均可见，未见空白中心或控件重叠；640x420 Inspector 标题有边界内截断但不越界。 | `menu_popup_nodes_` focused execution 早前一次触发 `zircon_runtime_interface` rustc ICE、一次 no-incremental target 非零退出且无目标 test result，本轮未把它声明为通过；完整 `cargo test -p zircon_editor --lib --locked` 与 `integration_contracts` 仍需清洁长窗口或 CI-parity lane 补跑。 |

## 8. 测试矩阵（代表性用例）

- **M1**：`status_bar_renders_via_runtime_extract`、`status_bar_chip_click_routes_intent`
- **M2**：`drawer_resize_reflows_content_via_taffy`、`tab_drag_reorders_documents`、`rail_button_toggles_drawer`
- **M3**：`no_painter_projection_paths_remain`（编译期：旧模块不存在）、集成契约全量回归
- **M4**：`keymap_resolves_unconsumed_chord_to_command`、`menu_item_enabled_predicate_follows_state`、`palette_filters_commands_by_query`
- **M5**：`floating_window_owns_independent_surface`、`layout_restores_from_project_workspace_over_global`
- **M6**：`right_click_opens_nearest_provider_menu`、`toast_fires_on_import_failure_event`

落点：editor `src/tests/` 既有结构（host/、workbench/ 子目录惯例）+ 模块内 `#[cfg(test)]`。

## 9. 风险与对策

| 风险 | 对策 / 探测信号 |
|------|----------------|
| 区域双路径过渡期视觉/交互不一致 | 区域硬切：同区域单帧只有一个来源；切前后截图对比纳入切片验收 |
| 旧 painter 删除牵连未盘点调用方 | M3.S2 出删除确认清单；编译期断言（模块不存在即红）|
| 浮窗多 surface 的输入焦点/IME 归属混乱 | 每窗口独立 batch 与 focus path；跨窗口焦点切换测试 |
| 布局持久化格式将随 docking 演进破坏兼容 | snapshot 带版本号；不可读时按四级链降级并记录诊断 |
| keymap 与文本编辑焦点冲突（输入框里按 Delete） | 严格按 01 路由次序：focus-path 未消费才进 keymap；测试覆盖输入框场景 |

## 10. 里程碑级依赖表

| 里程碑 | 前置 | 被依赖 |
|--------|------|--------|
| M1 | 01 M3、02 M1、04 M4、05 M2（shell 资产热重载）、06 M1 | 08 M2 |
| M2 | 08 M1、01 M5（对应区域桥收编）、02 M4（docking 接缝）、06 M4（L4 组合） | 08 M3 |
| M3 | 08 M2 | 09 全部（E0 门槛）、08 M4–M6 |
| M4 | 08 M3、06 M3（CommandPalette 骨架） | 09 M1（编辑命令入口） |
| M5 | 08 M3 | 09 批次 2/3（浮窗承载编辑器） |
| M6 | 08 M3、06 M3（toast/context menu 组件）、07 M2（过渡动画，弱） | 09 M1（console/状态反馈） |

## 11. 完成定义

- editor 主窗口像素全部来自 runtime render extract → GPU command stream；painter 投影族物理删除。
- 实机全壳交互：tabs/drawer/rail/status bar/菜单/快捷键/浮窗/布局恢复全部可用。
- `cargo test -p zircon_editor --lib --locked` 与集成契约全绿；software_fallback_count=0 持续成立。
- 验收命令组：`cargo test -p zircon_editor --lib --locked`、`cargo test -p zircon_editor --test integration_contracts --features integration-contracts --locked`、实机 `cargo run -p zircon_app --no-default-features --features target-editor-host --bin zircon_editor`。

## 12. 边界约束

- 切换以区域为单位硬切，禁止同区域双路径长期并存；GPU command stream 验收标准（software_fallback_count=0）持续有效。
- workbench/docking/windowing 语义不下沉 runtime；runtime 不出现 editor page/drawer/window 概念。
- 视觉验收对照 `ai-workbench-web-framework.png` 的结构与配色方向：近黑 chrome、teal 激活态、左窄 activity rail、左 scene/assets 树、右 inspector、底 console/timeline、薄 status bar——结构正确即可，不逐像素。
- 命令 id 是稳定契约：改名走废弃别名表，不直接断链。

## 13. 参考实现对照（dev/ 源码锚点）

实现切片前先读对应锚点，不确定的行为语义以参考实现为准（在 PR 说明中注明出处）；禁止凭印象实现、禁止引用未核实路径。

| 设计点 | 主参考 | 次参考 | 参考什么 |
|--------|--------|--------|---------|
| docking/tab manager | `dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Docking`（FTabManager/SDockTab） | `dev/Fyrox/fyrox-ui/src/dock/{tile.rs, config.rs}` | tab 拖出/合回、layout 序列化恢复（FTabManager::PersistLayout 对应布局持久化四级链）；Fyrox dock config 是 Rust 端序列化样板 |
| 菜单栏/工具栏装配 | `dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/MultiBox` | — | MultiBox：菜单项 = command + 可扩展插槽的装配模式（菜单树声明的架构参照） |
| 命令注册表/快捷键 | `dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Commands`（UICommandList/InputBindingManager） | — | command id、默认键位、上下文绑定链、按键未消费时的命令解析（EditorKeymap.resolve 对照） |
| 通知/Toast | `dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Notifications` | — | NotificationManager 的队列与生命周期 |
| 编辑器壳整体组织 | `dev/Fyrox/editor` | `dev/godot/editor`（editor_node 等） | Rust 引擎编辑器的 docking 壳 + 面板注册组织；Godot 的 dock 槽位/主题化壳（结构参考） |
| 多窗口/浮窗 | `dev/bevy/crates/bevy_winit`（多窗口生命周期） | `dev/Fyrox/fyrox-ui/src/window.rs` | 每窗口独立 surface 的事件归属与关闭语义；Fyrox 的 UI 内浮动窗口（拖动/缩放/置顶） |
