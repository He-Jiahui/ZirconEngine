---
related_code:
  - zircon_runtime_interface/src/ui/design_tokens.rs
  - zircon_runtime_interface/src/ui/style.rs
  - zircon_runtime_interface/src/ui/template/document.rs
  - zircon_runtime_interface/src/ui/template/asset/document.rs
  - zircon_runtime/src/ui/v2/style/runtime_state.rs
  - zircon_runtime/src/ui/surface/focus.rs
  - zircon_runtime/src/ui/surface/popup_stack.rs
  - zircon_runtime/src/ui/surface/render/popup_position.rs
  - zircon_runtime/crates/zr_rhi/src/ui_surface.rs
  - zircon_runtime/crates/zr_rhi_wgpu/src/ui_surface/batching.rs
  - zircon_runtime/crates/zr_rhi_wgpu/src/ui_surface/geometry.rs
  - zircon_runtime/crates/zr_rhi_wgpu/src/ui_surface/shaders/ui_material.wgsl
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/render.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/render/geometry.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/render/tests.rs
  - zircon_runtime/src/graphics/scene/scene_renderer/ui/shaders/screen_space_ui.wgsl
  - zircon_editor/src/ui/retained_host/host_contract/paint_template_nodes/template_settings_window/commands.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_template_nodes/template_section_titles/commands.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_template_nodes/template_section_titles/geometry.rs
  - tools/ui-profile-workbench-pointer-evidence.ps1
  - tools/tests/ui-profile-capture-output-contract.Tests.ps1
  - zircon_editor/assets/ui/editor/theme/editor_tokens.zui
  - zircon_editor/assets/ui/theme/editor_workbench_strict.zui
  - zircon_editor/assets/ui/theme/editor_workbench_spatial.zui
  - zircon_editor/assets/ui/editor/windows/workbench_window.zui
  - zircon_editor/assets/ui/editor/components/workbench/shell/workbench_top_toolbar.zui
  - zircon_editor/assets/ui/editor/components/workbench/shell/workbench_main_band.zui
  - zircon_editor/src/core/commands/mod.rs
  - zircon_editor/src/ui/workbench/autolayout/layout_tier.rs
  - zircon_editor/src/ui/workbench/layout_preset.rs
  - zircon_editor/src/ui/retained_host/popup_anchor_metrics.rs
  - zircon_editor/src/ui/retained_host/host_contract/paint_primitives/shapes/borders/rounded.rs
  - zircon_editor/src/ui/template_runtime/builtin/workbench_window_template_bindings.rs
  - zircon_editor/src/ui/retained_host/callback_dispatch/template_bridge/workbench/window_menu_state.rs
plan_sources:
  - user: 2026-08-11 参考 UnrealEngine 与 MagicaVoxel 收敛 .zui 设计思想、样式、触发和布局逻辑
  - docs/plans/zircon_editor/editor_ui/04-style-theme-and-painter-selector.md
  - docs/plans/zircon_editor/editor_ui/08-workbench-shell-on-runtime-ui.md
  - docs/plans/zircon_editor/editor_ui/09-editor-modules-and-design-parity.md
  - docs/plans/zircon_editor/editor_ui/11-zui-suffix-convergence-and-ui-toml-retirement.md
tests:
  - zircon_runtime/src/ui/tests/v2_asset/style_runtime/runtime_pseudo_state.rs
  - zircon_runtime/src/ui/tests/focus_navigation/modal_popup.rs
  - zircon_runtime/src/ui/tests/render_popup_menu.rs
  - zircon_runtime/src/ui/tests/render_popup_options.rs
  - zircon_editor/src/tests/ui/boundary/template_assets.rs
  - zircon_editor/src/tests/workbench/layout/editor_design_token_contracts/chrome_theme.rs
  - zircon_editor/src/tests/host/retained_callback_dispatch/template_bridge/workbench_toolbar_breakpoints
  - zircon_runtime/crates/zr_rhi_wgpu/src/ui_surface/geometry/tests.rs
  - zircon_runtime/crates/zr_rhi_wgpu/src/ui_surface/batching/tests/scale_and_cache.rs
  - zircon_runtime/crates/zr_rhi_wgpu/src/ui_surface/tests/native_submission.rs
  - tools/tests/test_editor_ui_device_pixel_aa_contract.py
  - tools/tests/capture-editor-ui-visual.Tests.ps1
  - tools/tests/ui-profile-capture-output-contract.Tests.ps1
doc_type: plan
---

# 计划 12：Unreal / MagicaVoxel `.zui` 设计与工作台收敛

> 状态：执行中（2026-08-11）
> 目标：以 Unreal Slate/Starship 为架构主标准，以 MagicaVoxel 为高密度视觉与工作流次标准，收敛 Zircon 编辑器 `.zui` 的描述思想、语义样式、触发逻辑、停靠布局与自适应策略。
> 约束：本计划编排展示层收敛，不复制计划 01/02/04/06/08/09/11 已拥有的输入、布局、主题、组件、工作台和后缀契约；需要改变基础契约时回到既有 owner，并以硬切方式更新 live caller。

## 1. 执行边界与优先级例外

- 所有编辑器作者态 UI、工作台布局选择、面板开合和命令投影仍归 `zircon_editor::ui`；`zircon_runtime_interface::ui` 只持有可复用的样式、组件状态和布局描述契约，不把编辑器状态写入 runtime world。
- `.zui` 是声明式展示与交互映射资产，不成为业务状态机、命令执行器或绝对坐标脚本。业务命令和 `can_execute / checked / visibility` 由 Rust command/action owner 决定。
- 当前 MVP 计划仍处于推进期；本计划属于用户在 2026-08-11 明确要求的 UI 收敛例外。执行只改 UI/Editor 展示链和必要的通用 UI 契约，不借机扩展未完成的运行时功能。
- 当前共享工作区存在大量并行 Session。执行前按精确路径获取 lease；`ui_asset_editor.zui`、其 workspace projection、UI Asset Editor Rust owner 和 action bar 在本计划全部里程碑中永久为 scan-only，即使其原 Session 后续释放 lease，本计划也不得编辑或迁移这些路径。
- 每个里程碑完成 testing stage、独立审查和计划状态记录后，使用 coordinator 的 milestone commit；该提交作为同一里程碑的企业微信通知来源，不重复发送同一 SHA。

## 2. 当前基线与主要问题

2026-08-11 对当前工作树的只读盘点：

| 项目 | 当前事实 | 收敛要求 |
|---|---|---|
| `.zui` 规模 | `zircon_editor/assets/ui/editor` 下 248 个 `.zui` | 通过共享 token、组件 recipe 和治理测试改变整体风格，不逐页复制样式 |
| 样式主表 | `editor_workbench_strict.zui` 1411 行、169 条 rule、83 个十六进制颜色 | 拆清语义 token、组件 recipe、壳层 selector；允许 viewport 场景色作为明确例外 |
| 顶部工具栏 | 383 行、31 条 route、75 个固定尺寸声明 | 按命令优先级分组，图标命令进入 overflow；稳定高度但不固定整组总宽 |
| 主工作区 | activity rail / 左抽屉 / viewport / 右抽屉单层横排 | 以 dock region 和 splitter contract 表达；viewport 保持最小可用尺寸，侧栏按优先级收缩或折叠 |
| 工作台窗口 | 资产仍有固定 `position`，runtime 已有 popup stack/placement/focus，editor retained host 又维护 open-time anchor/clamp | runtime 成为 generic popup 几何、stack 与 focus 的唯一 owner；editor 只提交 trigger/policy 并投影 open state |
| 视觉值 | 268 个 hex，其中 token owner 外 239 个；5262 个 `Fixed` stretch | 原始颜色只允许 token owner/图像式 viewport；固定尺寸只允许图标、chrome、行高等稳定格式控件 |
| 状态 | hover/pressed/selected/focused 在 stylesheet 与 props 中重复 | 状态优先级集中到 selector/painter；focus-visible 是正交轮廓，不冒充 selected fill |
| 触发 | `UiBindingRef.id/event/route/action` 与 `UiActionRef.route/action` 可并存，且页面 route 与 Rust action id 有同义分叉 | editor command 只以 `UiActionRef.action` 为身份并解析到既有 `EditorCommandRegistry`；local binding id 与 native event 保留各自职责 |
| live root | `workbench_shell.zui` 与 `workbench_window.zui` 同时注册，组件化窗口是当前新主链 | 只在 live componentized projection 上推进，保留 host shell 仅承担宿主接线职责 |

### 2.1 根因

1. token、组件 recipe、页面布局和业务状态混在同一层，导致同一个按钮状态在 token、primitive props 和全局 stylesheet 重复定义。
2. `.zui` 大量使用 `min = preferred = max + Fixed` 模拟截图，失去 measure/arrange 与窗口缩放能力。
3. route 只描述“点击后发字符串”，缺少 Unreal `FUICommandInfo + FUIAction + FUICommandList` 那样的命令描述、可执行性和层级路由统一入口。
4. popup 的打开状态、绝对坐标和可见性在资产中并列，触发控件几何、屏幕边界、focus scope 没有形成单一链路。
5. 全局风格表按具体页面类名持续追加，产生大量近似色和状态分支，无法形成稳定的工具型桌面产品语言。

### 2.2 基线扫描口径

- 范围固定为 `zircon_editor/assets/ui/editor/**/*.zui`，2026-08-11 共 248 个文件；`editor_workbench_strict.zui` 位于该目录外，单独统计为 1411 行、169 条 rule、83 个 hex。
- `hex` 是正则 `#[0-9A-Fa-f]{6}(?:[0-9A-Fa-f]{2})?` 的匹配数，不是去重色值数；268 个匹配减去唯一 token owner `editor/theme/editor_tokens.zui` 的 29 个匹配，得到页面/组件侧 239 个。原先未定义口径的“219 个内联颜色”不作为治理基线。
- `Fixed` 是同一文件集上 `\bFixed\b` 的词法匹配数，当前为 5262；它用于发现候选点，不等同于 5262 个违规，因为图标、chrome、row height 等稳定格式控件可以进入 family allowlist。
- 2026-08-23 current-source 触发复扫解析全部 249 个 `.zui` 文档中的实际字段，而不是只计命中行：`route = "..."` 为 1473 个字段 / 1418 个唯一字符串，`action = { action = "..." }` 为 18 个字段 / 17 个唯一 action / 6 个文件；按路径分类，route 中 1228 个位于 live/workbench、91 个位于 fixture/reference、154 个位于其他 editor 资产。M4/M5 必须保持这套分类，editor-command route 的目标是零；fixture 或明确登记的 runtime navigation route 只能进入 owner/用途明确的 allowlist。
- 同一扫描得到 1228 个 `selected`、991 个 `checked`，并有 980 条声明行 / 138 个文件同时写入两者；其中 251 行位于 live/workbench、5 行位于 fixture/reference、724 行位于其他 editor 资产。该数字是重复权威候选而不是全局清零目标：selection 与 boolean check 可以语义独立，但 toolbar/module/play/layout 等同一业务状态不得再由资产同时硬编码 `selected/checked/value`。
- 2026-08-23 current-source popup 复扫在 `zircon_editor/assets/ui/editor/**/*.zui` 中得到 17 条包含 `popup_anchor_*` 的声明行、分布于 11 个文件；其中 live `windows/workbench_window.zui` 仍有 7 条，另外 10 条属于 showcase、primitive/reference 或 component-drawer 资产。Editor callback 已会在部分打开路径覆盖这些值，但资产默认绝对坐标与 runtime trigger frame 仍构成双权威，因此该计数只在 M4 删除 open-time `popup_anchor_*` 写入路径后归零，不把“运行时恰好覆盖”视为收敛。
- M2/M5 的 guard 必须复用上述范围与表达式并输出逐文件清单，使数量变化可以审计；最终违规判定仍由 token/viewport/data-visualization/brand allowlist 进行结构化分类。

## 3. 参考证据与采用结论

### 3.1 Unreal Engine：主标准

| 参考文件 | 证据 | Zircon 采用 |
|---|---|---|
| `dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Styling/SlateStyle.h` | `FSlateStyleSet` 提供命名、类型化 style/property registry 与 parent fallback | `.zui` token 保持语义命名；组件按 family recipe 解析，不由页面直接拼颜色和 padding |
| `dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Styling/SlateStyleRegistry.h`、`dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Styling/AppStyle.h` | 中央 style registry + application-wide singleton accessor | 单一 editor workbench theme owner；页面只引用 token/recipe |
| `dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Styling/StyleColors.h` | Background/Panel/Header/Hover/Primary/Select 等语义色与 theme change 广播 | palette 改为中性层级 + 少量语义角色，避免青蓝色值在 selector 中扩散 |
| `dev/UnrealEngine/Engine/Source/Runtime/SlateCore/Public/Styling/ToolBarStyle.h` | toolbar 把 button/toggle/separator/icon size/padding/overflow 作为成组 recipe | 顶栏/面板头/viewport toolbar 各有明确 chrome recipe 和 overflow 规则 |
| `dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Commands/UICommandInfo.h`、`dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Commands/UIAction.h`、`dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Commands/UICommandList.h` | descriptor 与 execute/can-execute/check/visibility 分离，并支持 command list hierarchy | `.zui` 只绑定 action id；Rust 统一 command catalog、状态与快捷键；同一命令不在多处复制逻辑 |
| `dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Docking/LayoutExtender.h`、`dev/UnrealEngine/Engine/Source/Runtime/Slate/Public/Framework/Docking/LayoutService.h` | 以 area/splitter/stack/tab 扩展点构造并版本化保存布局 | 工作台从固定三列改为 region/splitter/preset；坏布局回退到版本化默认值 |
| `dev/UnrealEngine/Engine/Source/Editor/EditorStyle/Private/StarshipStyle.cpp` | Recessed/Header/Input 语义面、按控件类型分级的圆角、低 padding、选中 Primary、明确 tab/dock 状态 | 采用安静的中性深灰、细分隔线、紧凑行高、单一蓝色 selection/focus 信号；圆角采用 Zircon 当前 6/8/10/12 逻辑像素层级，不机械复制旧 Starship 小半径数值 |
| `dev/UnrealEngine/Engine/Source/Runtime/Slate/Private/Test/SlateUserCursorLockTest.cpp` | 提供明确几何与边界组合的测试先例；它不是 popup 或 docking 的直接测试 | Zircon 采用该测试组织方式，并由自身 popup/focus、autolayout 与 splitter tests 直接覆盖相应行为 |

### 3.2 MagicaVoxel：次标准

| 参考文件 | 证据 | Zircon 采用 |
|---|---|---|
| `dev/MagicaVoxel-0.99.7.2-win64/config/ui/editor.sty` | 独立 style 文件、`-h/-s/-d` 状态后缀、30-70 中性灰表面、极少蓝/橙强调；常用控件半径主要为 1-4，较大容器/面板约为 4-6 | 用少量可辨识状态替代装饰性卡片和同色系层层描边；危险/警告使用暖色语义色。其源码半径并不大于 Zircon 的 6/8/10/12 层级，因此“更圆滑”的目标落实到物理像素、边缘覆盖和一致几何，不再全局放大半径 |
| `dev/MagicaVoxel-0.99.7.2-win64/config/ui/m_opt.ui` | 提供显式 `HDPI` 开关并通过 `ui dpi` 切换 | Zircon 产品路径始终使用窗口当前物理 client extent 与 per-monitor scale，不提供会退回低于设备分辨率的默认模式；验证记录实际窗口 DPI、surface extent 和 presenter backend |
| `dev/MagicaVoxel-0.99.7.2-win64/config/ui/top.ui` | 32px 顶部行、左 mode、中项目名、右常用图标命令和 shortcut hint | Zircon 顶栏收敛为低高度 command strip；主项目上下文与运行控制保持稳定位置 |
| `dev/MagicaVoxel-0.99.7.2-win64/config/ui/editor.ui` | Layer/VBox/HBox 组合、左/中/右区、可折叠/resize panel、popup 独立层 | `.zui` 用容器、region、splitter 和 overlay 描述结构；不把 popup 混进普通流布局 |
| `dev/MagicaVoxel-0.99.7.2-win64/ui_reimplementation_guide.md` | retained view tree、文本 DSL、behavior/render delegate 分离、measure/arrange、事件到命令字符串 | 保留 `.zui` 文档 + Rust runtime 的分层，但把命令字符串提升为有 catalog 约束的稳定 action id |

实际运行参考确认了其核心不是“像素复刻”，而是信息密度、持续可见的 viewport、低装饰分区和按任务展开的工具面板。本计划不复制 MagicaVoxel 的专用 voxel 工具或私有二进制实现。

### 3.3 Slint：声明式实现交叉校验

- `dev/slint/ui-libraries/material/src/ui/styling/material_style_metrics.slint` 证明尺寸应由集中 metrics 提供。
- `dev/slint/ui-libraries/material/src/ui/components/state_layer.slint` 证明 hover/press/focus 应由共享状态层处理，键盘激活和 pointer 状态共用组件行为。
- `dev/slint/ui-libraries/material/src/ui/components/menu.slint` 把 popup、menu item、selected 和 close policy 分层；`dev/slint/tests/cases/elements/popupwindow_position.slint` 与 `dev/slint/tests/cases/focus/popupwindow_focus.slint` 覆盖动态位置和焦点恢复。
- Zircon 不采用 Material 的大圆角、48-56px 移动端密度和 ripple；只采用其声明式属性、组件行为、测试分层方式。

## 4. 目标设计契约

### 4.1 `.zui` 描述职责

`.zui` 可以声明：

- 组件树、稳定 `control_id`、语义 class、slot 和 import；
- min/preferred/max、stretch、alignment、dock region、splitter、overlay anchor；
- token/recipe 引用和有限的初始展示状态；
- `event -> action_id` 映射、tooltip 文案与可访问名称。

`.zui` 不可以拥有：

- 文件保存、编译、播放等业务分支；
- `can_execute`、checked、visibility 的重复权威状态；
- 屏幕绝对 popup 坐标、手工碰撞修正或焦点恢复算法；
- 页面私有的 hover/pressed/disabled 色值；
- 为截图而固定的整组宽度和窗口主区域高度。

### 4.2 样式层级

```text
editor theme tokens
  -> semantic surface/text/accent/status roles
    -> component family recipe (toolbar, panel header, row, field, popup)
      -> pseudo-state overlay (hover, press, selected/open, focus-visible, disabled)
        -> page class only for domain-specific composition
```

- 表面角色：`window / chrome / panel / recessed / input / popup / hover / selection`。
- 文本角色：`primary / secondary / disabled / inverse`。
- 强调角色：selection/focus 使用单一冷蓝；success/info/warning/error 各自保留语义色，不能用 accent 冒充。
- 形状：连续相接的工作台 chrome band、splitter 和 viewport 边界可以为
  0px；紧凑控件、默认控件、大型控件、panel/popup 分别使用
  6/8/10/12 logical px 的共享层级，并随有效 DPI 投影到物理像素。
  `999px` 仅表示真实 pill、chip 或圆形 thumb，不得作为普通卡片的默认值。
  最终数值必须通过 M6 的真实 WGPU 截图复核，不允许用整窗固定倍数
  supersampling 掩盖错误的半径、DPI 或像素覆盖实现。
- 栅格：surface extent 不低于窗口物理 client extent，logical-to-physical 只投影一次；圆角盒在每个物理目标像素内执行局部 4x4 coverage。相邻且同 frame/clip/radius 的 fill + border 必须在 backend 合并为一个 analytic item，并以 `inner + (outer - inner)` 分割一次 outer coverage；CPU fallback 使用相同覆盖分区，禁止两个 primitive 在同一外沿重复 source-over。源命令统计仍保持 fill + border 两条，但首次编译与 retained cache-hit damage 统计都必须报告一个 draw item / draw call，且缓存命中不得重新扫描 arranged/render geometry。该策略对应 Unreal Slate 的单 RoundedBox shader，而不是整窗 TSR 或固定倍数离屏放大。
- 状态输出是“一个 primary visual state + 若干独立 overlay”，不能压成单个互斥枚举后丢失信息。
- primary fill/foreground 的确定顺序固定为：`disabled > loading > drop-target > dragging > pressed > open > checked > selected > hovered > normal`；同一控件多状态并存时取最左者，因而 open/checked/selected 的平局也有确定结果。
- `focus-visible` 是独立 outline overlay，在 selected/checked/open fill 之上仍必须可见；普通 pointer focus 不画该 outline。drop indicator、validation/error indicator 也作为独立 overlay 合成，不得覆盖 primary fill 的身份。
- `selected/checked/open` 默认使用中性 selection surface、前景色或一条局部 accent indicator 表达，不得把完整 accent outline 当作通用选中态；完整 outline 只属于 `focus-visible`、drop target 或显式 validation。实心 accent fill 只保留给当前主操作，避免旧产品图中“所有选中控件同时发青”的层级噪声。
- 历史 2026-07-11 的 1672x941 产品图仅作为缺陷发现基线，不是 current-source oracle；它暴露的文字互相覆盖、窄字段截断、低对比小字号和状态描边过量，必须在 M5/M6 的当前源码截图中逐项判定，不能因 2026-08-16 token 已更新而默认视为修复。

### 4.3 命令与触发

```text
.zui event
  -> stable action id
    -> scoped command list
      -> descriptor(label/icon/chord/type)
      -> action(execute/can_execute/check_state/visibility)
        -> state projection back to retained UI
```

- pointer、keyboard、menu、command palette 调用同一个 action id。
- menu item 不再用页面私有字符串决定 enabled/checked；由 command projection 注入。
- tooltip 由共享 delayed-open policy 管理；disabled command 仍可显示原因，但不触发 execute。
- 现有 schema 的 survivor contract 固定为：`UiBindingRef.id` 只做资产内 binding identity、诊断和 target assignment；`UiBindingRef.event` 只表示 native `UiEventKind`；`UiBindingRef.action.action` 是唯一 editor command identity，并解析到既有 `zircon_editor::core::commands::EditorCommandRegistry`。
- M4 完成迁移后，editor-command binding 禁止 `UiBindingRef.route` 和 `UiActionRef.route`，也不新建 UI-local command catalog；非 editor-command 的 runtime navigation route 只有在 owner 明确且 guard allowlist 登记后才可保留。

### 4.4 Popup 与 focus 唯一所有权

- `zircon_runtime::ui::surface::popup_stack`、`surface::render::popup_position` 和 runtime focus/input routing 是 generic popup 的唯一实现 owner：依据实时 trigger frame 定位，执行 flip/clamp，维护 nested stack、outside click、Escape、focus trap/restore。
- editor command/menu/palette owner 只请求 `open/close + placement policy + trigger identity`，并把 runtime 返回的 open/checked/focus projection 写回 retained UI；不得再计算 generic popup 的绝对 x/y。
- M4 硬切时删除 `.zui` 的 open-time absolute `position/popup_anchor_*` 写入路径，并删除或缩减 `zircon_editor/src/ui/retained_host/popup_anchor_metrics.rs` 及 workbench callback 中重复的 generic anchor/clamp/dismiss 逻辑。editor 只可保留确有领域含义的策略常量，不能保留第二套几何算法。
- runtime-interface 只承载可序列化 placement/trigger/policy contract，不承载 editor command 或窗口状态；popup 关闭后的 focus target 由 runtime stack 记录的触发控件决定。

### 4.5 布局与自适应

- 固定格式控件可以使用稳定尺寸：图标按钮、chrome 高度、row height、separator、status item。
- 工具组、抽屉、面板和文档区必须使用 min/preferred/max + stretch，禁止三值相等后再固定整组。
- 主区优先级固定为：viewport/document > active inspector/scene tree > auxiliary drawer > low-priority toolbar labels。
- 宽度不足时顺序为：缩短文本 -> 图标化低优先命令 -> 收入 overflow -> 折叠辅助 drawer；不得压缩 viewport 到不可交互尺寸。
- popup、toast、command palette 使用 overlay/portal，不参与主 VBox/HBox 的测量。
- splitter 的视觉线可为 1px，但 pointer hit target 不小于 8px；layout preset 版本化持久化并支持 default fallback。
- 响应式判定唯一复用 `zircon_editor::ui::workbench::autolayout::WorkbenchLayoutTier`。输入先按 `ResolutionContext::logical_extent(physical_width, scale_factor)` 转为 logical px，再用 `EditorDensityTokens` 的 `breakpoint_ultra_width=480`、`breakpoint_narrow_width=640`、`breakpoint_wide_width=1260` 产生 Ultra/Narrow/Regular/Wide；测试同时覆盖 1.0/1.25/1.5/2.0 DPI 的等价 logical-width 结果。
- 当前 shell drawer 验收 oracle 固定为：普通窗口最小 640x420 logical px、Ultra 最小 420x360；document/viewport 宽度不得低于可用主区的 `minimum_document_width_fraction=0.5`；Ultra/Narrow 自动折叠 right drawer，Regular/Wide 不自动折叠。模块 overlay 的内部侧栏使用独立但同源的 viewport-first 优先级：`rail_gap` 与 left panel 从 Narrow 起可见，right panel 只在 Wide 可见，center 始终保留；不得把 shell drawer 的持久化规则误套为模块内 right panel 的 Regular 可见性。若 token 值在 M2 经审查调整，测试读取 token 字段而不复制常量，但断言上述 tier 关系与 document 优先级不变。
- collapse/restore 状态由 editor workbench layout model 和 `LayoutPresetPersistenceStore` 唯一拥有；以 `LAYOUT_PRESET_PERSISTENCE_VERSION` 版本化，缺失或版本不匹配回退 `Authoring` preset。窗口跨 tier 后，自动折叠不能覆盖用户持久化的 drawer/preset，恢复到足够宽度时按持久化状态恢复。

## 5. 依赖顺序与里程碑

### M1 — 参考基线、架构和治理范围

- 固化本计划的 Unreal 主参考、MagicaVoxel 次参考、Slint 交叉证据和当前 `.zui` 量化基线。
- 明确计划 04/08/09/11 的 owner 关系、MVP 例外、并行 Session 避让和每里程碑提交/企业微信规则。
- Testing stage：计划 frontmatter/path/链接审计、plan output audit、`git diff --check`；独立 reviewer 核对证据与范围未越过 runtime/editor 边界。
- Exit evidence：coordinator validation batch + M1 milestone commit。

### M2 — 语义 token、chrome metrics 与状态 recipe

- 稳定 `EditorDesignTokens` 的 palette/chrome/density/state role 分层，收束当前进行中的 chrome/density 拆分，消除同义值和 editor/runtime 两份默认值漂移；圆角权威固定为 small/control/large/panel = 6/8/10/12 logical px。
- 把工作台主色改为中性深灰层级、蓝色 selection/focus、暖色 warning/error；`editor_workbench_strict.zui` 只导入 `editor_tokens.zui`、只持有通用壳层语义 alias/recipe 且不得出现 raw color，不把 Material/MUI showcase theme 带入产品图；`editor_workbench_spatial.zui` 作为 strict 的后置扩展，唯一持有显式 allowlist 内的 viewport/axis 场景色与空间域 selector。
- 为 toolbar/panel-header/row/field/popup 建立共享 family recipe；focus-visible 与 selected 状态分离。
- 添加 token round-trip、cascade alias、state priority、focus-visible 正交性、raw color allowlist 的 focused tests/guards。
- Testing stage：runtime-interface token/style focused tests、runtime v2 pseudo-state tests、editor theme contract tests；随后做 scoped Cargo check/test batch。
- Exit evidence：M2 validation batch、独立 review、计划状态行、milestone commit/企业微信。

2026-08-27 palette reference correction：本地 Unreal `StyleColors.cpp::InitializeDefaults()` 的 `Background/Panel/Header/Dropdown` 明确为 `#151515/#242424/#2F2F2F/#383838`，MagicaVoxel 参考截图的高频工作面实测为 `(30,30,30)/(38,38,38)/(60,60,60)`；两者都用中性灰建立层级，把彩色留给交互与语义状态。Zircon 先前的 `#111416/#171A1D/#1B1F23/#252B31` 面层与 `#3CC7D6` 青色 accent 仍让产品呈现偏暗、偏蓝，未满足本计划 3.1/4.2 的设计目标。唯一 `.zui` token owner 现采用 Unreal 四级面值，hover/border/track 同步收为中性灰；selection/accent/focus 使用 `#243F5A/#60AEFF/#66B2FF` 冷蓝，success/info/warning/error 保留独立语义色；accent 作为 selected 正文时的最小对比度为 4.65:1。`EditorPaletteTokens` 首帧常量与资产逐字段一致，Welcome 背景和 success 也从页面私有数组改为 `PALETTE.shell_background/success`。通用 retained painter 复扫进一步把旧 `CENTER_BAND/DOCUMENT_PANEL/VIEWPORT_PANEL/FLOATING_SHADOW` 数组映射回中央 palette，并让 splitter 的 1px visual rule 每帧只解析一次 `separator_strong`；8px hit target、damage clip 和 scene/viewport 图像域 allowlist 不变。对比度复审发现普通 Primary 的最终 retained 状态机曾把浅色正文画在 accent/focus fill 上；现与 Unreal `ForegroundInverted` 思路一致，rest/hover 使用 `shell_background` 深色前景（对 `accent/focus_ring` 分别为 7.82:1/8.15:1），pressed 的深色 selected surface 使用 `text_primary`（8.86:1），文本与 glyph 共用同一状态 owner；Primary icon command 原本已采用相同反色规则。聚焦 palette 结构、parity 与 contrast/state-source 契约为 3/3 GREEN；该结果仅证明 owner/默认值/语义分工，最终亮度、对比度和状态辨识仍待 current-source WGPU 三档截图验收。

### M3 — 工作台壳层与高密度布局

- 重构 live `workbench_window.zui` 组件链：25-32px chrome、紧凑 panel header/status bar、低装饰分区、viewport-first 主区。
- 顶栏按 project/context、transform tools、run、layout 分组；移除固定整组总宽，低优先 command 进入 overflow。
- M3 只改变 command 的分组、可见性和 overflow 投影，必须保留当前 binding 行为与 action identity；不得在 M3 提前改写 route/action 字符串，命令身份硬切只在 M4 完成。
- `workbench_main_band.zui` 改为可持久化 region/splitter 约束，统一 activity rail/left/right/bottom drawer 的 min/preferred/max 和 collapse priority。
- 迁移 scene tree、inspector、viewport、console/status 等高频壳层；通过共享 primitive 影响全部模块，但不改其他 Session 持有的 UI Asset Editor 专属资产。
- 添加窗口宽度档、稳定 chrome 尺寸、viewport 最小面积、drawer collapse/restore 和文本不溢出的布局测试。
- Testing stage：editor componentized workbench、toolbar breakpoint、autolayout/geometry 和 template asset focused tests；运行产品启动 smoke。
- Exit evidence：M3 validation batch、独立 review、桌面/窄窗口截图、milestone commit/企业微信。

### M4 — 命令、popup、focus 与触发一致性

- 统一 toolbar/menu/context-menu/command-palette 的 action id 与既有 `EditorCommandRegistry` descriptor；按 4.3 survivor contract 硬切 editor-command binding，移除 `UiBindingRef.route`/`UiActionRef.route` 及所有同义 caller，不保留兼容 shim 或第二 catalog。
- 按 command owner 而不是文件数量推进迁移：先收口 `workbench_top_toolbar.zui`（31 route）、`workbench_component_drawer.zui`（33）、core asset workspace（38）与 generated bottom panel（48）的共享入口，再由同一 registry mapping 机械迁移 `workbench.extension.*`（当前 888）和 `workbench.module.*`（当前 189）；禁止为批量资产另造临时 route-to-action 兼容表。
- 把 popup 从窗口绝对位置改为 trigger-frame anchor + flip/clamp；统一 outside click、Escape、nested popup、disabled item、focus trap/restore。
- 将 play/stop、tool mode、layout、module overflow 的 checked/visibility 投影回组件状态；不由资产硬编码选中态。
- 为 pointer/keyboard/menu/command palette 同命令、边缘 popup、无 focusable popup、nested modal 和 disabled command 增加测试。
- Testing stage：runtime focus/popup/event routing、editor workbench menu/command callback、command palette focused tests和产品交互 smoke。
- Exit evidence：M4 validation batch、独立 review、交互验收截图/记录、milestone commit/企业微信。

2026-08-26 current-source execution note：结构化解析当前存在的 248 个 editor `.zui`，得到 1473 个 `route` 字段 / 136 个文件以及 53 个 `action` 字段 / 11 个文件。该原始 route 数量主要是 module、tool、dock 和页面状态导航，不能机械改成 editor command。已硬切产品资产中最后两个 `workbench.project.open/save` 命令别名；顶部工具栏与 host shell 的 Open、Save、Play、Stop、Asset Browser 均使用注册表中的 `UiActionRef.action` 身份，产品 `.zui` 对已删除命令 route 的命中为零。治理测试保留 1464 条非命令 navigation route 的 owner/identity 哈希，同时把五个已删除命令 route 设为永久拒绝项。Notification Center 已从 window/primitive 的 `popup_anchor_*` 与 resize/open-time 坐标回写硬切到 `WorkbenchWindowTopToolbarRegion` control anchor；icon tooltip 使用 input-time `UiSurface::set_popup_control_anchor`，只更新稳定控件身份与 popup-stack owner；context menu primitive 与 live window instance 均使用 `UiPopupAnchor::Pointer { owner_property = "context_target" }` 与 transient `set_popup_pointer_anchor`，打开事务先写 content/owner/point 再切 open；Command Palette、Dialog 与 ConfirmDialog 使用 `UiPopupAnchor::Surface`，runtime 从 arranged parent chain 解析当前 surface 根框架，`placement = "center"` 让 modal 以根框架中心定位，editor 已删除这些 overlay 的数值锚点和 open/resize 时 `popup_anchor_x/y/width` 回写。上述路径均不再读取/回写 `UiFrame`，render extract 与 projected hit-test 从同一 runtime anchor state 解析几何，retarget 只标记 render/input dirty。live window 的绝对 anchor owner 从 3 个降为 0，产品可达图的数值 anchor owner 已清零；全资产目录仅保留 2 个 Showcase 示例与 1 个 catalog-only WorkbenchDropdownPopup 样例，因此全目录口径为 3，不能与产品可达口径混用。未实例化的 WorkbenchDropdownPopup import 已从产品窗口移除，产品 Dropdown 继续由 `WorkbenchDropdown` 运行时组件自身定位选项弹层。window/primitive 的关闭态 `focused_index` 均为 `-1`，popup gap 统一消费 `$editor.density.gap.small`。当前 Python `.zui` 套件 96/96、248/248 TOML 解析通过。以 HEAD `601472078e848164d2221967c55a77fea2452928` 加当前 overlay 发起的 managed Editor build 在 `cargo.acquire` 前 `command_post_timeout`（request `ab0ac2cb6865438dbab1c1a75f7b0e9d`），因此没有 rustc 诊断，旧 27-error copy 不计 current-source 证据。M4 仍待 Cargo focused tests、popup/focus 产品交互验证和真实 WGPU 产品证据，不能据此标记完成。

2026-08-27 tooltip-trigger audit：Runtime `UiInputManager` 已实现统一的 tooltip `arm -> elapsed -> show` / leave-cancel 定时链，默认延迟 500ms，并拒绝 stale timer；但 live Workbench pointer 事务仍直接调用 `refresh_workbench_icon_tooltip()`，在首个 hover route 中立即写入全局 tooltip 的 anchor/text/open/visibility，未经过该 timer。当前已有 Editor 回归也明确期待首次 move 后立即 visible，因此这不是“已有延迟但测试遗漏”，而是产品路径分裂。拒绝在 Editor 再加本地 deadline，因为那会形成第三套 tooltip 状态机；M4 接受前必须把 Workbench pointer 事务接入 Runtime `UiInputManager` 的 tooltip timer event flow，并把现有 immediate-open 回归改为 arm/elapsed/leave-cancel。当前 shared runtime timer/input-manager 文件存在其他 owner 的大范围未提交变更，本轮只登记缺口，不编辑该 owner，也不把 icon hint 的“文案可解析”误报为 delayed-open 完成。

同日 tooltip-trigger source correction：live Winit move/button/wheel/leave 现保留标准 `UiPointerInputEvent` 的 timestamp、sequence、source、modifiers 与 window metadata，并在既有 native action 路由之前只把事件交给 Workbench tooltip observer；点击、拖拽、滚动和 action dispatch 仍由原 owner 执行一次。Editor 只用当前 projected `UiSurface::hit_test` 解析 icon/rail 的显式 tooltip 或 authored label，候选、取消、默认延迟、stale elapsed 拒绝与 retained visible state 全部由 Runtime `UiInputManager` 拥有；generic surface 与 Editor host 共同消费公开的 `DEFAULT_TOOLTIP_DELAY_MS = 500`，资产可通过非负 `tooltip_delay_ms` 显式覆盖，包括 `0ms`。本地 Unreal `FSlateApplication::TickPlatform` 同样集中调用 `FSlateUser::UpdateTooltip`，其路径以 `bIgnoreEnabledStatus = true` 查询提供者，并在 mouse-down、drag、inactive/window/menu 状态下阻止或关闭 tooltip；Workbench 因此允许 disabled command 继续显示显式原因或 authored label，但 execute/can-execute 路径不变。到期唤醒使用独立的 `input_timer_frame_wake_deadline` 参与现有 `ControlFlow::WaitUntil`，不借用会被 asset refresh 清理的 maintenance slot，也不创建线程或 Editor 私有时钟；tick 看到 Runtime retained tooltip 已 visible 后才打开 control-anchor `.zui` popup。pointer down/up/scroll/leave、keyboard、IME、focus loss 统一取消；宿主 menu/page-overflow 打开时，observer 与到期 tick 两端都阻止底层 Workbench tooltip。聚焦 source contract 为 5/5，既有 Rust 行为回归已改为 499ms hidden / 500ms visible / leave-cancel，另有低层 disabled-reason 回归，但本轮未运行 Cargo，故该链只称 source-ready，M4 仍待 current-source 编译和真实产品交互验收。

Tooltip transition 审计确认 retained painter 仅在 `fade/grow/zoom` 时把已经投影的 `transition_progress` 乘入 opacity，而 Workbench 当前没有按帧推进该 progress 的 owner；只在 `.zui` 声明 transition 会直接投影为 `entered/1.0`，不会产生真实入场动画。因此本轮不写无效 fade 配置。后续若加入动画，必须由 Runtime input/timeline owner 依据同一 tooltip 生命周期推进 progress，并用 current-source WGPU 连续帧验证，而不能在 Editor host 再造计时状态机。

2026-08-27 tooltip timing reference correction：以本地 Unreal `FSlateUser::UpdateTooltip` 的 `TooltipSummonDelay = 0.15f` 为主参考，将 Runtime/Editor 共享 `DEFAULT_TOOLTIP_DELAY_MS` 从早期接线时的 500ms 收敛为 150ms；默认边界回归相应改为 149.999ms hidden / 150ms visible。资产显式 `tooltip_delay_ms` 仍具有优先级，既不改写明确的 500ms，也保留 `0ms` 的即时提示语义。Unreal 的 100ms intro 依赖连续帧更新；Zircon 当前没有 tooltip transition-progress 的逐帧 owner，因此此次只修正 summon delay，不伪造静态 fade。

同轮 tooltip extent correction：产品 Workbench Tooltip 不再永久使用 `280px` 外框承载所有文案。`.zui` 显式声明 `layout_min_width = 96` 与对齐 Unreal `Slate.ToolTipWrapWidth` 的 `tooltip_wrap_width = 1000`；Runtime 到期发布 popup 时测量当前字体下的真实标题宽度，加上共享 gap/clip guard 后收敛到紧凑下限、窗口可用宽度和 wrap 上限之间，并只在宽度变化时标记节点 layout dirty。同一可见候选经历 shell-size 或 device-scale reflow 后也会重新测量，不再因文本与 anchor 未变而提前返回旧 extent。Host painter 原有 `row_height * 10` 的 320px 气泡上限同步提升为 `row_height * 31.25`，使 1x 默认值为 1000px 且随设备 DPI 等比扩展。行为回归要求短 `Move` 保持 96px，`Compile Current Module` 按内容扩宽并在 420px 窗口内收口；将该可见长提示重排到 120px shell 后，宽度必须收敛到 96..104px。上述静态修复仍待 current-source Cargo 与 WGPU tooltip 截图验证。

同轮 tooltip intro correction：前述“只有 progress 消费端、没有逐帧 owner”的审计缺口已由 Runtime `UiInputTimerState` 收口。成功的 delayed Show 在同一 manager 内启动 100ms intro，`next_frame_visible_delay` 复用 input-timer deadline 以最多 16ms 间隔唤醒，`tooltip_intro_progress` 从原输入时间域输出 0..1；新候选、任意取消活动或 dismiss 均清理 timeline。Editor 不持有时间状态，只把 Runtime progress 投影为 `.zui` 的 `fade`/`linear`、`entering -> entered` 属性。`transition_progress`/`transition_status` 对 Tooltip 被低层回归锁为 render-only invalidation，不重建 layout、hit-test、text 或 input。回归边界为显示瞬间 0.0、50ms 为 0.5、100ms 为 1.0；当前仍待 Cargo 与连续 WGPU 帧证明真实透明度变化。

同轮 current-source build evidence：在 HEAD `301685cfe38d2ed7ee2c3a03f38b737ef2c794d8` 加当前 overlay 上仅提交一次 `tools/build-editor.ps1 -Ephemeral`。协调器接受 `cargo.acquire` 请求 `a6f74582554e476890dc3cd66dbcfbfd`，但在 reconciliation 后返回 `command_post_timeout`，没有 terminal result 或 rustc 诊断。按“不实时跟踪协调器”的约束不查询 recovery URL、不重复提交；因此该请求不计编译、WGPU 或视觉通过证据。

### M5 — `.zui` 家族迁移、治理闸口与产品验收

- 按 primitives -> shell -> core modules -> secondary modules 的顺序迁移仍绕过 token/recipe 的资产；viewport 场景颜色、数据可视化色和插件品牌色必须进入显式 allowlist。
- 添加治理闸口：禁止页面新增原始交互色、重复状态 recipe、主区域绝对 popup anchor、无理由固定整组尺寸、未注册 action id，以及 editor-command binding 中出现 `UiBindingRef.route`/`UiActionRef.route`。
- UI Asset Editor 的 `ui_asset_editor.zui`、workspace projection、Rust owner 与 action bar 永久 scan-only：M5 可以报告其治理结果，但无论 lease 是否释放都不得编辑、自动迁移或纳入本计划提交。
- 清理 `editor_workbench_strict.zui` / `editor_workbench_spatial.zui` 中失效或跨域 selector，验证当前 Git 可见且实际存在的 repository `.zui` 可解析、import 可达、control id/action id 无冲突。产品可达性必须同时包含 Workbench 静态 import 图和 Rust 动态加载的 view roots；动态 view projection 以根 `.zui` 的递归 widget/style import 图为权威，除显式测试 fixture 外不得再次注入 legacy theme source。
- 使用真实 `zircon_editor` Windows 产品窗口和 GPU presenter 做 640x520、900x620、1672x941 物理客户区截图与像素非空检查；每档记录 `GetDpiForWindow`，核对无重叠、文本不裁切、selected/focus 职责分离、popup 不出屏、viewport 始终可用。CPU first-present snapshot 与 softbuffer fallback 只能作为交叉检查，不能替代 WGPU 产品证据。
- Testing stage：`.zui` 全量静态治理、runtime/editor focused suites、Windows-native 产品构建/启动和最终 scoped regression batch。
- Exit evidence：M5 validation batch、独立 review、产品截图、计划终态记录、milestone commit/企业微信。

2026-08-26 current-source density follow-up：在 HEAD `d5d41037e080ecc948a3b13f3e8bab38b4cd708a` 加当前共享 overlay 上，核心模块 53 个正 container gap 已从私有 `6/8/10` 数值收敛到共享 density token，10 个 module overlay rail gap 已从旧 `72px` 改为 `$editor.chrome.activity_rail.width`；除永久 scan-only 的 UI Asset Editor workspace 外，43 个可写 extension rail gap 使用同一 chrome token，212 个正 container gap 收敛到 density/separator token。Rust 产品桥接测试不再复制 `72.0`，而是读取 `EditorDesignTokens::workbench_dark().chrome.activity_rail_width`。UI Asset Editor 文件本轮未编辑，其当前 diff 仍只有另一 owner 的 action-state 修复。当前 editor `.zui` Python 矩阵 110/110、248/248 TOML 解析通过，Rustfmt 与 scoped diff check 通过。实际启动并截取 MagicaVoxel 0.99.7.2 的 `1084x600` 物理客户区只用于参考复审，确认其紧凑层级、连续覆盖圆角和克制状态面；它不是 Zircon 产品验收截图。随后执行 `tools/build-editor.ps1 -Ephemeral`，在 Cargo/rustc 前被 coordinator 的 `unmanaged_artifacts_detected` 拒绝，当前 paths 为 `D:\ZirconBuilds\tooling15-wave103-runtime-20260826-214953` 与 `D:\ZirconBuilds\tooling15-wave104-runtime-20260826-220711`；未获得 Editor EXE、编译诊断、WGPU 产品截图或 profile，因此 M5 acceptance 继续 pending。

2026-08-26 current-source responsive follow-up：53 个可写 module workspace 共 159 个侧边几何节点现统一复用 `WorkbenchLayoutTier` metadata：53 个 `rail_gap` 与 53 个 left panel 的 `responsive_min_tier` 为 `narrow`，53 个 right panel 为 `wide`。因此 Ultra 只保留 stretch center，Narrow/Regular 恢复 rail gap 与主上下文栏，Wide 才恢复辅助 inspector；不再由 270px + 320px 的固定侧栏在 480/900 档抢占中央画布。新增核心与 extension 全量治理契约，其中 extension 永久排除 scan-only UI Asset Editor；该文件修改前后 SHA-256 一致。当前 editor `.zui` Python 矩阵 112/112、静态后缀/生成矩阵 22/22、248/248 TOML 解析和 scoped diff check 通过。该证据只接受布局声明层，不替代 current-source Editor Cargo、WGPU 产品截图或 profile，M5/M6 acceptance 继续 pending。

同轮 header/content-budget follow-up：结构化测量发现 52 个可写 module center header 中有 30 个在 Ultra 投影前的固定最小内容超过 420 logical px，HUD 因 10 个 extension 跳转与 Preview 同排达到 1304px。现由 29 个 over-budget 双操作 header 把 outlined 次操作设为 `responsive_min_tier = "narrow"`，始终保留 filled primary；HUD header 只保留标题与 Preview，10 个既有 extension control ID/route 原样迁入可接收滚动输入且裁剪的 horizontal `ScrollableBox` 工具条。全量契约按 token 中的 `ultra_minimum_window_width` 计算可见子项和 gap，52/52 均在预算内。进一步从 54 个可写 workspace root 递归传播 Ultra 的 `420x238` 可用 frame，覆盖 209 个 Ultra 可见 HorizontalBox/VerticalBox 容器：横向预算问题为 0；唯一纵向问题是 Gameplay Effect 的 9 个固定行需要 312px，现已改为裁剪、接收滚动输入、自动滚动且 `Receive` 输入的 vertical `ScrollableBox`，子项顺序、control identity、事件和 route 不变，修复后纵向问题为 0。当前 editor `.zui` Python 矩阵 117/117、静态后缀/生成矩阵 22/22、253/253 TOML 解析通过。真实排版、滚动输入与文本裁切仍必须由 current-source WGPU 产品截图验证。

同轮 icon-hint follow-up：共享 hover tooltip resolver 现把 `workbench-icon-button` 与 `workbench-rail-button` 作为同一类紧凑图标入口，显式 `tooltip` 仍优先，否则使用非默认 authored `label`；6 个 activity-rail 动作因此复用既有 Scene/Entities/Graph/Assets/Audio/Code label，不复制第二套提示文案。Component Drawer 中不属于 icon class 的纯图标 Delete 动作补充显式 tooltip，control ID、route、尺寸和危险态不变。结构化审计排除永久 scan-only UI Asset Editor 后，产品可达纯图标 Click 动作的 unresolved hint 数为 0。当前 editor `.zui` Python 矩阵 119/119、静态后缀/生成矩阵 22/22、253/253 TOML 解析、Rustfmt 和 scoped diff check 通过。随后唯一一次 `tools/build-editor.ps1 -Ephemeral` 已进入 Cargo，但在 rustc 前因当前共享 manifests 与 `Cargo.lock` 尚不一致而被 `--locked` 拒绝；临时 target 为 `F:\cargo-targets\zircon-engine\ephemeral\check\eabc667e94744fe5b7d1e052356b17e0`。本轮未覆盖其他 owner 的 lock/manifests 改动，也未获得 Editor EXE、WGPU 产品截图或 profile。

同轮 scroll-trigger follow-up：Runtime 的 `input_interactive = true` 是 broad capability，会同时推导 clickable、hoverable 与 focusable，不是“可滚动”的窄标志。HUD extension strip、Gameplay Effect canvas 与 module-tab strip 现与既有 scene tree/inspector scroll 容器一致，只声明 `input_hoverable = true` 并由 layout 明确 `input_policy = "Receive"`；滚轮与 pointer hit 仍进入容器，但容器本身不再制造伪 Click target 或额外 Tab stop。结构化审计覆盖 6 个可写 Workbench ScrollableBox，broad-interactive 数为 0；相关 focused Python 契约 23/23 通过。

同轮 component-drawer state-authority follow-up：live `WorkbenchDrawerTabComponents` / `WorkbenchDrawerTabConsole` 原先在 `.zui` 同时声明 `value/checked/selected`，Console body 另声明初始 `visibility`，但点击后的互斥选择与 body 显隐已经由 Rust `apply_reference_menu_action` 更新，形成初始化与后续状态的双权威。现在两个 tab 资产只保留文案、几何和事件；`initialize_panel_live_control_state()` 在同一事务中选择 Components、显示 component body 并折叠 Console body。组件画廊内部用于展示 checkbox/radio/list/table on/off 外观的 sample state 不在本次范围内。新增静态契约先以 `drawer_tab_components duplicates drawer state` RED，再完成专项 1/1、live-state 25/25、完整 Editor `.zui` Python 133/133 GREEN；当前 repository `.zui` 318/318 parse，Rustfmt、Python compile 与 scoped diff check 通过。既有 Rust 交互回归仍覆盖默认 tab/body 状态及切换到 Console 后的互斥状态，但本轮未运行 Cargo，因此只称 source-ready。

同轮 Core-module tab state-authority 与 family-boundary follow-up：10 个 Core workspace 的 30 个 `WorkbenchTab` 原先各自声明 `selected/checked/value`，而点击后已经由 Rust `workbench_module_panel_tab_control_id()` / `workbench_module_panel_tab_group()` 和 `select_exclusive()` 更新，形成与 Drawer 相同的初始态双权威。现在 30 个资产节点只保留文案、几何、control identity 与 route；`initialize_panel_live_control_state()` 以 10 个默认 action id 复用既有导航映射，在同一初始化事务内投影每组默认 Tab，不复制第二份 control-id 表。产品可达图另增加原生交互组件旁路门禁：除 Workbench primitive 定义层外，123 个可达文档加 Welcome 对 `Button/InputField/Tab/ListRow/...` 的直接消费者为 0，页面必须经共享 Workbench family；永久 scan-only 的 UI Asset Editor workspace 仅参与读取审计，文件未编辑。专项契约先以 30/30 authored live state RED，再完成 Core 1/1、live-state 26/26、完整 Editor `.zui` Python 135/135 GREEN；Git 可见且实际存在的 repository `.zui` 318/318 parse，Rustfmt 与 Python compile 通过。未运行 Cargo、未获得 current-source WGPU 产品截图或 profile，因此 M5/M6 acceptance 继续 pending。

同轮 extension-module tab state-authority follow-up：43 个可写 extension workspace 的 129 个 `WorkbenchTab` 同样删除 authored `selected/checked/value`，文案、布局、control identity 与 route 均保持不变。默认项不另建 43 条 action/control 表；`workbench_extension_default_panel_tabs()` 直接遍历既有 `EXTENSION_MODULE_NAVIGATION_SPECS`，把每份 spec 的首个 `tab_action` 作为该组默认项，并在初始化事务中复用同一 `select_exclusive()`。函数显式排除 `WorkbenchExtensionUiAssetEditorWorkspace`，其资产前后 SHA-256 均为 `5DEFDAD04BE2837AF3CD012C63D6801D0136F8B1CAC36B492E6799C921381F09`，本轮未改变该 owner 的资产或行为；低层 Rust 回归另锁定 43 个默认项、默认 control 必须属于自己的 group 且不得包含 UI Asset Editor。专项契约先以 129/129 authored live state RED，再完成 extension 1/1、live-state 27/27、完整 Editor `.zui` Python 136/136 GREEN；45/45 extension 与 repository 318/318 `.zui` parse，Rustfmt、Python compile 和 scoped diff check 通过。Rust 单测尚未由 Cargo 执行，current-source WGPU 产品截图/profile 仍缺，因此 M5/M6 acceptance 继续 pending。

同轮 row-selection scope correction：Core 与 extension 导航 catalog 的 row group 是候选索引，不等于一个视觉面板的互斥域；Animation Compression 等 workspace 同时需要保留左侧资源选择与中心表格选择，旧 `select_exclusive_selected()` 却会在点击任一行时清空整个 8-row spec group。现在 Editor bridge 通过 control index 取得目标节点的实际父容器，只在同父候选间更新 `selected`；动态或 legacy control 无法解析父节点时仍回退旧的全组语义。该路径没有全树扫描，仍只遍历既有小型候选 slice。Rust 行为回归要求点击 `WorkbenchExtensionAnimationCompressionAttackClipTableRow` 后左侧 Locomotion 保持选中、表格 Run 切换为 Attack；Core Material 回归同步要求图节点选择不再清掉左栏 Base Color。source contract 先以缺失 `control_parent_id(selected_control_id)` RED，再完成专项 1/1、live-state 28/28、完整 Editor `.zui` Python 137/137 GREEN；Rustfmt 与 scoped diff check 通过。上述 Rust 行为测试尚未由 Cargo 执行，故只称 source-ready。

同轮 Ultra overlay follow-up：标准 Runtime popup 继续由 control/pointer/surface anchor 的 flip/clamp 权威约束，不在 Editor host 重复定位；不走 portal 的 Preferences 与 toast 则由 Workbench 根 Overlay slot 提供 `$editor.density.gap.large` 安全边距并使用可缩小的 Stretch 约束，因此 `420x360` logical Ultra surface 不再被 `720x480` / `390px + 24px offset` 固定几何挤出边界。Preferences 新增 retained `settings_category_scroll_offset` 与 `settings_scroll_offset`；open 同时重置两者，category switch 只重置右侧内容，原生滚轮通过一次双参数 host callback 原子更新。projection、paint、hit-test 都消费同一个 `SettingsWindowLayout` 的钳制偏移与行投影，插件贡献让 category 数量超过侧栏高度时也能滚动到达。左右列表溢出时均使用 token-derived scrollbar thickness/min-thumb/radius 生成可见轨道与滑块，并分别裁剪到自己的内容列表；枚举/颜色浮层仍使用窗口裁剪。Ultra 下 category sidebar 可收缩到约 120px，值列按可用宽度使用 52%/62%/72% 三档分栏，标签绘制宽度从同一布局权威推导；带 Reset 的值控件不再从旧 72% 固定起点塌缩到约 1px。当前 Editor `.zui` Python 矩阵 121/121、253/253 TOML 解析、触及 Rust 文件的 rustfmt 与 scoped diff check 通过；尚无 Cargo focused test 或 WGPU 产品视觉证据。

2026-08-27 compatibility visual follow-up：为避免把静态合同当成视觉验收，使用现存 2026-08-10 profiling `zircon_editor.exe`/`zircon_runtime.dll` 与当前 runtime/editor assets 组装隔离 bundle，并通过产品自带的 first-present WGPU capture 生成前后帧 [`zircon-legacy-current-assets-before-toolbar-density.png`](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-before-toolbar-density.png) 与 [`zircon-legacy-current-assets-after-toolbar-density.png`](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-after-toolbar-density.png)。旧宿主不认识当前 Blend Space validation callbacks；仅在隔离 bundle 中清空这组 callback 以完成模板加载，源资产未作该兼容修改。首帧暴露模块 `Save` 文本被画成 `Sa...`，其根因是 388px 模块命令组中重复的 Save/Browse 文字按钮；当前 `.zui` 已把二者改为带显式 label/tooltip 的 34px 图标按钮，Compile 继续作为文字主命令，组预算收敛到 300px，后帧确认截断消失且命令身份不变。当前 Rust toolbar projection 同样把 Save/Browse 固定为 34px icon-only 命令，Compile 保留 104px 标签，并在 Ultra/Regular/Wide 间保持同一展示语义，避免运行时重建覆盖 `.zui`。欢迎侧栏副标题也从说明句收敛为 `Recent projects`；宽屏 Welcome 外层内容增加 1000 logical px 上限，左右安全 spacer 使用等权 Stretch，[居中帧](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-welcome-centered.png)确认约 1000px 的 recent/main 内容块居中、两侧余量对称且列内无裁切。[900x620](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-900x620-layout.png) 与 [640x520](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-640x520-layout.png) 物理客户区兼容帧确认 Welcome 两列仍在边界内。随后 Welcome 删除页面私有的 showcase/material 状态规则，改为只导入严格 Workbench theme，并用共享 `WorkbenchButton`/`WorkbenchField` 承担 6 个动作与 2 个输入框；control ID、route、尺寸及两列结构不变。组件节点现在直接持有 80px/118px 动作宽度，不再依赖会在组件展开时丢失的 parent-slot override；1440x900 [共享配方兼容帧](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-welcome-shared-workbench-recipes.png) 确认主/次动作恢复预期宽度、字段与 shell 接线可绘制且两列布局未塌缩。该旧 EXE 仍让禁用 `Open` 的 80px 按钮轮廓可见但不绘制标签；当前资产保留 `text = "Open"`，`WorkbenchButton` 明确提供 disabled foreground，Rust 只投影 disabled flag 与 route，因此登记为旧组件展开差异，不以兼容补丁污染当前资产。旧宿主在 resize 后出现空 toolbar 带且把 host tabs 画成 `page:*` 内部 ID；当前源码的 breakpoint tests 保持各命令区可达，而 `HostPageTabModel.title -> TabData.title` 明确消费 `MainPageSnapshot.title`，因此这些只登记为旧宿主差异，不驱动当前源码回退。聚焦 Welcome `.zui` 合同 4/4、完整 Python 矩阵 127/127、253/253 Editor TOML 解析与 Rustfmt 通过。六张图只接受资产布局和共享样式接线变化：旧 EXE 不包含本轮 analytic rounded SDF、linear-light coverage 或新的 Rust 投影，因此不得用作 M5/M6 current-source AA/触发 acceptance。隔离快照成功在不修改共享锁文件的情况下生成自洽 `Cargo.lock`，但 managed build 被 coordinator 的 `repository_mismatch` 拒绝；主根的唯一一次 managed build 又在 Cargo/rustc 前被 `unmanaged_artifacts_detected` 拒绝，残留 reservation 指向 `D:\ZirconBuilds\tooling15-wave105-runtime-20260826-231249`。本任务未释放他人 reservation、未删除共享产物，也未绕过 managed Cargo。

2026-08-27 short-height compatibility correction：后续逐图复核否定了上段早期 `640x520` / `900x620` 的“仅在边界内即通过”口径：四个 startup chooser 动作在短客户区不可达，1440x900 帧也只露出其顶部边缘。Welcome 现让 `main_panel_top_space`、`actions_optional_gap`、hero/status/preview 区及相邻 gap、`main_panel_bottom_space` 保留 preferred/max，但把 minimum height 收敛为 0，并由主列统一 clip；高度不足时先压缩八个非交互展示区，项目字段、Open/Create 与 startup chooser 的触发节点、顺序、control ID 和 route 均不变。第一轮精确客户区复抓让底部行恢复可见，同时暴露 `Default Workbench / Component Showcase / Asset Window / UI Layout Editor` 在 640px 下产生省略号；最终呈现文本收敛为 `Default / Showcase / Assets / UI Layout`，完整语义通过 tooltip 保留，事件 route 不变。96 DPI 的最终 [640x520](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-640x520-height-priority.png) 与 [900x620](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-900x620-height-priority.png) 客户区兼容帧确认四个入口全部完整、无重叠且主操作可达；其 SHA-256 分别为 `85DE09B87CDAA70F26987CD60994D3C1D1AB8148A247ECEE4B43C5975BB3BCC4` 与 `7D074DEB30382C4ADA9C5F3EAA5474D970662F23CBFC181F6C9F91E1C0B4B69A`。本记录以 Welcome 6/6、完整 Editor `.zui` Python 129/129、当前存在的 repository `.zui` 318/318 parse 和 Rustfmt 为最新静态结果，取代上段 4/4 与 127/127 数字；两张新增图仍只证明旧 EXE 对当前资产布局的兼容消费，不证明 current-source rounded SDF、SVG 超采样、popup 或触发路径。

同日 current-source geometry-authority correction：Welcome 的八个可收缩节点引出了 retained painter 的旧 fallback 风险——layout capture 会把 0 高度帧表示为缺失，而 `.unwrap_or(fallback)` 会在绘制阶段重新生成 hero/status/preview，破坏 `.zui` arrange 的唯一几何权威。`resolve_welcome_frame` 现显式区分“无资产布局的 legacy caller”与“当前资产布局已存在”：只有前者允许 fallback；后者的缺失/折叠帧保持不可见，主列五个语义绘制入口在提交命令前统一检查 frame visibility。原 control ID、文案、事件和 `.zui` 几何不变。聚焦合同完成 RED -> 7/7 GREEN，完整 Editor `.zui` Python 矩阵为 130/130，四个 touched Rust owner 通过 Rustfmt 与 scoped diff check；未运行 Cargo，因此 Rust 单元回归仍只称 source-ready，current-source 产品视觉 acceptance 继续 pending。

同日 shared-radius authority correction：产品 Welcome 已通过 `WorkbenchField` / `WorkbenchButton` 间接消费 `$editor.control.radius.control`，页面没有私有圆角；但其 Rust bootstrap 回归仍期待原生 `TextField` / `Button` 和旧 `4.0` 数值，形成会把实现拉回小圆角的反向权威。该回归现改为验证页面组件身份、页面不得声明 `corner_radius`、共享组件根必须持有 control-radius token；测试不复制当前 `8.0` token 值，因此未来层级调整仍保持单一权威。聚焦 Welcome 合同为 8/8，半径专项为 25/25，完整 Editor `.zui` Python 矩阵为 131/131，当前存在的 repository `.zui` 为 318/318 parse，Rustfmt 与 scoped diff check 通过。生产 `.zui` 几何、事件和绘制实现未变，未运行 Cargo，current-source 产品视觉 acceptance 仍 pending。

同日 task-first density correction（后续 wide-screen correction 之前的兼容记录）：逐帧对照 MagicaVoxel 的工具型布局后，Welcome 不再为重复的 `Open or Create` hero 与 `Ready` 展示区保留常态高度；`hero_panel`、`hero_status_gap`、`status_panel`、`actions_optional_gap` 固定为 0，项目摘要从 Create 动作之后前移到 location 之后，并将 preferred height 从 72 收敛到 52，使字段、摘要、验证与动作按任务顺序连续出现。`preview_bottom_fill` 在该轮仍位于动作之后，四个 startup chooser 当时保持底部锚定；control ID、事件 route、字段和动作语义均未改变。隔离旧宿主只在临时合并资产根中移除其不认识的五个 Blend Space validation callback，源资产未作兼容删减。该轮任务顺序在 [1672x941](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-welcome-task-first-1672x941.png) 宽屏帧可直接检查，[精确 640x520 客户区](../../../../artifacts/ui12-reference/zircon-legacy-current-assets-welcome-task-first-exact-640x520.png) 仍完整呈现项目字段、Create 与四个 startup 入口，无重叠或裁切；SHA-256 分别为 `45ED544CA732F850239A423621295EAA858E417E2A457B01EE7FB9DD67AEC683` 与 `53E9AB0AF8F63B82FA84B5C4C3EF4B07110F45A06841235C0449B58451DA3D43`。旧宿主仍不绘制 disabled Open 标签，继续按既有兼容差异处理。该轮聚焦 Welcome 合同为 9/9，完整 Editor `.zui` Python 矩阵为 132/132，设备像素/AA 静态门禁为 10/10，当前 repository `.zui` 为 318/318 parse，scoped diff check 通过。两张图仍只证明旧 EXE 对当时资产布局和共享样式接线的兼容消费，不证明 current-source rounded SDF、SVG 超采样、popup 或触发路径；current-source 产品视觉 acceptance 继续 pending。

同日 wide-screen task-cluster correction：上段旧兼容帧进一步暴露 `preview_bottom_fill` 位于 startup chooser 之前，会在宽屏把四个次级启动入口推到面板底部，在 Create/Open 与入口之间制造约 300px 的无意义空洞。Welcome 现新增可收缩的 `$editor.density.gap.large` `startup_chooser_gap`，让 chooser 紧接主动作；弹性 fill 改到 chooser 之后，只吸收任务簇下方的剩余高度。短窗口仍可优先压缩该 gap，按钮 control ID、焦点顺序、事件 route 与四入口顺序均保持不变。聚焦契约以缺少 gap 的 RED 起步并完成 9/9 GREEN；完整 Editor `.zui` Python 矩阵 178/178、设备像素/AA 11/11、Tooltip 5/5、当前实际存在的 repository `.zui` 318/318 parse 与 scoped diff check 均通过。永久 scan-only 的 UI Asset Editor workspace SHA-256 仍为 `5DEFDAD04BE2837AF3CD012C63D6801D0136F8B1CAC36B492E6799C921381F09`。旧 1672x941/640x520 兼容截图先于本修正，不作为新布局的视觉通过证据，仍需 current-source WGPU 三档截图验收。左侧 recent 区不新增重复资产占位：当前 retained painter 已在空列表中绘制 `No recent projects` 状态，旧图的空白属于旧宿主差异。

2026-08-28 product-interaction authority follow-up：产品可达 Workbench 组件现递归解析到 native interactive primitive，并拒绝任何未显式 inert 且无 event 的 consumer。审计发现 Scene Tree search 以及 Component Lab 的 number/slider/range/steps 共五个缺口；前者以 `Workbench/SceneSearchEdit|Commit` binding identity 进入既有 hierarchy-filter authority，不在 host 新增 control-name 特判，后四者通过 retained local-state bridge 同步 numeric value、显示文本与 normalized position。普通文本与 Search 保留用户原始空白，trim 只服务数值解析。Welcome 两个无页面 event 的字段必须同时在 Rust `welcome_text` native-dispatch projection 中声明 control 与 action，不能成为无文档例外。模板 binding、preview action allowlist 与 `.zui` route 现一一对应，focused contract 4/4、完整 Editor `.zui` 矩阵 180/180、AA/physical-sampling/visual-oracle 聚合 95/95、Editor `.zui` parse 248/248，保护资产 SHA-256 仍为 `5DEFDAD04BE2837AF3CD012C63D6801D0136F8B1CAC36B492E6799C921381F09`。共享 `zircon_app/Cargo.toml` 已增加 Windows `windows-sys`，但 `Cargo.lock` 的 `zircon_app` 依赖列表仍缺该项；本轮未修改 foreign manifest/lock、未启动 Cargo，因此 current-source WGPU 截图和产品 profile 仍待完成。

同日 managed-build follow-up：后续 HEAD `9201304352ed466ef7b5256b2c0ca02d133af4d2` 已由共享 lock owner 把 `windows-sys 0.61.2` 写入 `zircon_app` package dependency list，前段 mismatch 不再成立。UI12 等待外部 `zircon_runtime --lib --offline` Cargo/rustc 自然退出后，只向 `D:\ZirconBuilds\ui12-current-editor-20260828-interaction-aa` 提交一次 managed ephemeral 产品构建；coordinator 接受 `cargo.acquire` request `224344288ed141b59e9b6b6c97bac055`，但 reconciliation 后以 `command_post_timeout` 返回且没有 terminal result。失败后无 Cargo/rustc、发布目录或 staging 残留，本任务未查询 recovery URL、未重试。profiling/capture Pester 最新为 128/128；仍未得到 current-source EXE、WGPU 三档截图或 1000/1000/200 profile，M5/M6 acceptance 保持 pending。

同日 dynamic-product trigger hard-cut：实际产品 `hierarchy.zui` 的 Search 不再以 `edit_action_id = "HierarchySearchQuery"` 驱动 host control-name 特判，而是与 componentized Scene Tree 共用显式 `Workbench/SceneSearchEdit|Commit` Change/Submit binding；control id 只保留为稳定展示身份，输入中间态和提交态均进入既有 hierarchy-filter authority。Project Overview 的 `Open Assets` / `Asset Browser` 则经只读追踪确认属于有意的 native-template projection，前者由 `workbench.view.open.editor.assets` 打开实际 Assets view，后者进入 canonical `OpenAssetBrowser` asset command，因此没有叠加第二套 `.zui` event。focused 交互 contract 5/5、完整 Editor `.zui` 矩阵 181/181、Rustfmt 与 scoped diff check 均通过；保护资产 SHA-256 仍为 `5DEFDAD04BE2837AF3CD012C63D6801D0136F8B1CAC36B492E6799C921381F09`。current-source 产品构建、三档 WGPU 截图和 1000/1000/200 profile 仍待完成。

同日 current-source managed compile：在 HEAD `a2d8d811c4a3a1fc1db6f5375c491e7e4502533f` 上，managed Editor 构建已真实进入 Cargo，并在隔离 target `F:\cargo-targets\zircon-engine\ephemeral\check\a8d8c56b523a4d08a9028d491b676e42` 编译 UI/WGPU 依赖图；最终唯一 error 是 shared `zircon_runtime_host/src/foreign_output/item_count.rs:80` 的 E0004，尚未覆盖 `WorldQueryResult::TransformSnapshot`。该 current-source mixed-tree 缺陷已由 open RuntimeInterface01 handoff `docs/plans/optimize/zircon_runtime_interface/01/failure-2026-08-27-world-query-transform-snapshot-item-count.md` 明确拥有，并禁止上层计划单独补丁；没有诊断指向 UI12。Cargo/rustc 已退出且没有发布产品目录，本计划不修改 foreign world-query migration、不重复构建，current-source EXE、WGPU 三档截图和 profile 仍待 lower-layer 修复后执行。

同日 adaptive SVG sampling follow-up：设备物理目标中 fitted 最大边不超过 32px 的 SVG 图标从统一 2x 提升为 4x 局部源栅格，常规向量仍为 2x，任何会越过 4096px 栅格边界的目标降级为 1x。解析器不再写死 `downsample_rgba_2x`，而是按实际 sample axis 对预乘 alpha 的线性光样本求平均后重新编码 sRGB。小图标在 pixel-cache lookup 前同时取消 2px 上分桶，17x19 等目标保持精确物理尺寸，避免 4x resolve 后又把 18x20 纹理线性缩回原 frame；33px 以上仍使用 4/8/16px resize bucket，1..512 方形目标集合只从 64 增到 80。缓存仍只持有最终设备像素 RGBA，32x32 图标的 128x128 临时源不会放大 resident cache。相关 AA/性能/GPU-residency source contract 为 39/39，Rustfmt 与 scoped diff check 通过。该记录只接受受控倍率、色彩与缓存边界，不替代 current-source WGPU 三档截图的 1:1 视觉验收。

同日 dynamic-pane density authority：Hierarchy/Console 的 shell gutter 统一消费 `$editor.density.gap.medium`，Inspector 消费 `$editor.density.gap.large`，三者内部 section gap 统一消费 `$editor.density.gap.small`，不再复制当前 4/8/12 的数值。Console 独立的 10px 左右边距收敛为 8px medium tier，与正文 inset 对齐并为重复输出多释放 4px 横向空间。focused token-map 1/1、完整 Editor `.zui` 182/182、scoped diff check 与保护 hash 均通过。

同日 dynamic asset-surface breakpoint correction：Assets Activity 不再把 340px 的 `compact_left_drawer_max_width`（抽屉自身宽度上限）误作整个视口的 compact 断点，而是与 Workbench `Narrow <= 640` 的共享 breakpoint authority 对齐。其静态双栏仍在 Regular 档保留 188px Tree + 320px Content 的可读下限；640/420 由 Rust projector 折叠辅助 Tree 并让 Content 铺满 root，900 保持双栏。Asset Browser 的既有 column budget 则在 640 保留 152px Sources、先折叠 Details，更窄时再折叠 Sources。静态断点回归完成 RED/GREEN，完整 Editor `.zui` 矩阵 185/185、Rustfmt、scoped diff check 与保护 hash 均通过；640/420/900 Rust product-projection 回归已 source-ready，等待 current-source managed Editor compile。

同日 fractional square-border correction：software fallback 的直角边框不再在 render-command 分支对物理宽度执行 `ceil()`，因此 125%/150% DPI 的 1.5px 直角边缘不会跳成两层全不透明像素。radius-zero 与 rounded 现共用 border primitive 和分数宽度 record authority；fallback 使用解析的 outer-minus-inner rectangle coverage 并在线性光合成。扫描只覆盖 top/bottom 完整边带与中段 left/right 边带，维持 `O(perimeter * border_width)`，没有把大面板退化为全面积 8x 超采样。后续审计又收敛了 root skeleton、native dock、Welcome、close/blocker dialog 共用的直接 `draw_border` 路径：旧实现以四个 `floor`/`ceil` 实心矩形拼边，现在委托同一 radius-zero 解析 coverage authority，半像素位置不再把名义 1px 边缘扩成 2px 实边。静态回归完成 RED/GREEN，设备像素/AA 23/23、AA + SVG resize bucket + GPU residency 45/45、完整 Editor `.zui` 185/185、Rustfmt 与保护 hash 均通过；Rust pixel 回归 source-ready，等待 current-source managed Editor compile。

同日 WGPU square-fill geometry correction：software helper 过去在录制 radius-zero quad 前把原始 `f32` frame 通过 `PixelRect floor/ceil` 改写为整数 frame，导致滚动、缩放与动画中的方形 surface 逐像素跳动，并绕过已经存在的 WGPU SDF/fwidth fractional coverage。现在 square 与 rounded fill 都发布原始设备空间 frame；静态 1px separator 继续使用独立整数 API，明确 snap 而不把全局 UI 几何整数化。source-ready Rust 回归锁定 `4.25/6.5/24.5/18.75` frame 不变；AA 21/21、组合门禁 43/43 与 Rustfmt 均通过，等待 current-source managed Editor compile 和真实窗口截图。

同日 software square-fill coverage correction：radius-zero fallback 不再把整个 `floor/ceil` envelope 当成全覆盖。整数对齐内部继续使用连续 row-span 快路径，只有 fractional top/bottom 行和 left/right 边缘逐像素求解析 rectangle coverage 并在线性光合成；没有引入整面板超采样。source-ready 0.5px 回归要求 1x1 白色方块在四个邻接像素各得到约 sRGB 137 的 25% 覆盖。AA 22/22、组合门禁 44/44 与 Rustfmt 均通过，Rust 执行仍等待 current-source managed Editor compile。

同日 chrome replay border hard-cut：software command replay 删除了独立的 radius-zero `width.ceil()` 四边循环，square/rounded border 现在无条件委托同一解析 primitive，避免 1.5px 录制宽度在 replay 时重新量化为 2px。runtime WGPU draw-list 转换继续原样传递 `f32` frame/width/radius；chrome stream 复扫已无 `width.ceil`、`PixelRectExt` 或 frame 重量化残留。AA 23/23、组合门禁 45/45、Rustfmt 与 scoped diff check 均通过。

同日 CPU scaled-image linear sampling correction：WGPU image cache 已在 admission 时把 straight sRGB 转为 linear-premultiplied texel 并使用 Linear sampler，但 software fallback 过去直接对 sRGB bytes 做 alpha-aware bilinear，50% 覆盖落在错误的 128 灰阶。现在四邻域样本先解码 sRGB、在线性光预乘插值，再与解码后的目标像素合成并编码回 sRGB；50% 白色或透明蓝覆盖的 Rust 期望均为约 188。256-entry decode LUT 与 4097-entry encode LUT 把 transfer `powf` 留在一次性初始化，opaque identity-size row copy 快路径不变。设备像素/AA 20/20、物理采样 2/2、组合门禁 42/42 与 Rustfmt 均通过；Rust 回归 source-ready，等待 current-source managed Editor compile。

同日 Component Lab trigger authority hard-cut：字段 Change/Submit 仍只从 `.zui` authored binding 进入 bridge，但搜索输入不再通过 `WorkbenchInputSearch` 控件名特判写入 `query`。bridge 现从控件声明的 property schema 识别 `query` state target，其余 numeric/text 字段继续使用各自声明的 `value`/`value_text`，control ID 只保留稳定身份，不携带隐藏行为。产品交互 contract 6/6、完整 Editor `.zui` 185/185、Rustfmt 与保护 hash 均通过；Rust 执行等待 current-source managed Editor compile。

同日 software linear source-over authority：software fallback 不再只有 fractional geometry edge 使用线性光，而让半透明 shape span、CPU image、software text 与透明 viewport gizmo overlay 共用 retained-host 的 sRGB decode / linear source-over / sRGB encode authority。256-entry decode LUT 与 4097-entry encode LUT 只在首次使用时建立；逐像素循环不执行 transfer `powf`，opaque span 与 identity-size image 仍保留直接写入/整行复制快路。共享函数同时计算透明目标的 destination alpha，overlay 不再把首个半透明样本错误强制为 opaque。source-ready Rust 数值回归锁定 50% white-over-black 约为 sRGB 188、半透明 image/text 的线性结果及透明 overlay alpha；当前 Rust 执行仍被 foreign world-query E0004 截断。设备像素/AA 25/25、physical sampling 2/2、AA + sampling + SVG resize bucket + GPU residency 49/49、完整 Editor `.zui` 185/185、Rustfmt 与保护 hash 均通过；这仍不替代 current-source WGPU 产品截图。

同日 local-resolve LUT convergence：adaptive SVG 2x/4x resolve、MUI chart 4x local raster 与 native-resize frozen snapshot 的 bilinear sampler 不再在每个样本/像素重复自己的 transfer `powf`。SVG 与 resize sampler 直接查共享 decode/encode LUT；chart 在样本聚合后把 linear-premultiplied source 与 source alpha 交给同一透明 source-over authority。没有改变 SVG supersample 倍率、cache bucket、chart 4x coverage 或 resize physical-pixel-center 策略。设备像素/AA 25/25、physical sampling 2/2、组合门禁 49/49 与 direct rustfmt 通过；真实性能收益仍待 1000/1000/200 产品 profile 测量。

2026-08-29 WGPU square-edge analytic coverage correction：本地 Unreal 源码复核确认 Slate 常规 UI render target 在 `SlateRHIRenderingPolicy.cpp` 使用 `NumSamples = 1`，圆角由 `SlateShaderCommon.ush` 的 analytic signed distance、`fwidth` 与 `smoothstep` 覆盖；因此产品质量基线不是全帧 2x/4x backbuffer，而是设备物理分辨率、局部向量超采样和像素域解析覆盖的组合。Zircon 的 rounded WGPU path 已符合该模型，但 radius-zero quad 过去无条件进入 flat instance shader，fractional frame 会得到硬边；square border 还拆成四个实例并把宽度强制为至少 1px。现在只有物理像素边缘全部对齐的普通 square fill 保留实例快路，fractional square fill 进入同一 SDF coverage；square/rounded border 均作为单个 analytic outline 保留原始 `f32` 宽度和 frame，旧 `border_rects` 路径已删除。source-ready Rust 回归锁定 fractional square 的 1px coverage fringe、0.625px square border 和整数 fill 快路；原生离屏回读另比较 0.625px 与 1px outline 的 alpha 能量，防止宽度重新被钳到 1px。设备像素/AA 静态门禁为 27/27、physical sampling 2/2，Rustfmt、scoped diff check 与保护资产 hash 通过。focused `zr_rhi_wgpu` managed gate 在 Cargo 前被 coordinator `cargo.acquire` 的 `command_post_timeout` 截断，无 Cargo/rustc 启动；没有 native WGPU readback 或产品窗口证据，M6 acceptance 继续 pending。

同日 native acceptance preflight：产品截图脚本及其 source-binding helper 通过 PowerShell parser，focused Pester 8/8，强制三档精确物理客户区、GPU presenter、当前 UI source fingerprint、bundle asset-set parity 与 Editor/Runtime receipt hash；interaction latency focused Pester 11/11，包含每个 resize extent 的 sequence-bound input-to-present 门禁，并要求 1000 click / 1000 pointer move / 200 resize 交互证据携带 CPU core/system utilization、working set 与 private bytes。完整 profile-output contract 48/48，确认不完整计数、缺 provenance、process metric 不一致、full invalidation 与 visual/GPU cache churn 均 fail closed；当前完整 Editor `.zui` contract 为 185/185。已有 `.codex/state/ui12-wgpu-visual-proof-*.png` 继续只作为 frozen-snapshot lower-layer proof，不升级为产品 acceptance。foreign `zircon_runtime_host/src/foreign_output/item_count.rs` 仍缺 `WorldQueryResult::TransformSnapshot` 分支，因此没有重复提交 managed Editor build，也没有 current-source EXE、产品截图或 profile。

同日 trigger/focus reference convergence：本地 Unreal `SButton` 的默认 pointer/touch/keyboard 触发均为 `DownAndUp`，并把 `MouseDown`、`MouseUp`、`PreciseClick` 留作显式高级策略；MagicaVoxel `top.ui` 的 `repeated = '1'` 只用于 Undo/Redo。Zircon 继续以现有 Press/Release/Click、多鼠标键和 double-click route 覆盖通用按钮，不把长按重复扩散为所有命令的隐式默认；在没有 duration、repeat interval、pointer-leave/capture cancel、history boundary 和 accessibility 契约前也不为 Undo/Redo 添加局部计时旁路。状态侧发现 Runtime 已发布 `focus_visible_known/focus_visible`，但 Editor retained painter 的公共 selector 仍把所有 semantic `focused` 画成 keyboard outline。公共入口现以 Runtime modality 为 live authority，只在未知 modality 的静态/legacy preview 回退到 authored focus；主命令按钮删除重复判断并消费同一入口。source-ready lower regressions锁定 pointer focus 保留语义但不画 outline、keyboard focus 与静态 preview 仍可见；direct rustfmt、scoped diff check 与 5 项 source guard 通过，Cargo 执行继续等待 current-source Editor 验证通道。

同次 current-source 触发清单确认，显式 `event = "Press"` 只存在于 Component Showcase 的 list-row press 与按指针位置打开 context menu 两个演示契约；产品工作台命令没有从 `Click` 退化为 pointer-down 激活。焦点契约 4/4、完整 `test_editor_ui*.py` 71/71 与 `test_editor_zui*.py` 185/185 均通过。人工查看的 1672x941 Zircon 兼容帧生成于当前 `welcome.zui` 之前约五小时；当前资产和 Welcome density contract 都已锁定 startup chooser 位于 `preview_bottom_fill` 之前，因此该旧帧只解释历史空洞，不作为当前源码回归或 current-source 视觉验收证据。

同日 specialized-painter focus hard-cut：公共 selector 之外仍直接用 `node.focused` 绘制高亮的 Material TextField、Material state-layer、Asset thumbnail、Axis value field、Chip、Command Palette search、Inspector resource field 与 Property scalar 共八条路径，现全部消费共享 `focus_visible_for_node`。`focused` 仍保留给语义状态与 surface paint eligibility；pressed/open/selected/checked、validation 和未知 modality 的静态预览优先级不变。生产 painter 复扫不再发现绕过该 helper 的 `node.focused` 视觉分支；聚焦契约 5/5、完整 Editor UI Python 矩阵 72/72、完整 Editor `.zui` 矩阵 185/185、direct rustfmt、scoped diff check 与保护资产 hash 均通过，Rust/Cargo 执行与原生视觉验收仍待 current-source 产品通道。

同日 focus/accent role separation：后续 painter 复扫发现 Material state-layer、通用 border、panel/status icon、Chip、Dropdown、Toast、Tooltip、Alert、Dialog、TextField、语义正文和通用 active asset tint 仍有 selected/checked/pressed/open/active 内容或 shell chrome 映射到 `focus_ring`，使静态选择态与普通指针交互持续呈现为键盘焦点。现在 focus-visible/drop-target outline 继续使用 `focus_ring`，selected/pressed/open/dragging chrome 改用局部 neutral surface/border，active glyph/text/action 改用 `accent`；Alert 的 pressed 保留 status tone，Dialog 的 pressed/open 保留中性 shell border，TextField 的 pressed/open 使用 hover interaction border，Tooltip 普通与 pressed icon 使用 accent 且仅 Focused 改 outline。typed button 的 content tone 与 border tone 也分离，contained Primary 正文保持 inverted foreground，边框才使用 accent。真实启动的 MagicaVoxel 0.99.7.2 窗口再次确认其清晰度来自物理像素输出、约 6–8px 的紧凑控件圆角、局部状态填充和克制的彩色信号，而不是全屏大圆角或持续高亮轮廓；仓库既有参考图 `artifacts/ui12-reference/magicavoxel-0.99.7.2-client.png` 仍是参考证据，不属于 Zircon acceptance。焦点/选择静态契约 14/14、Editor UI Python 81/81、repository ZUI 219/219、采集链 Pester 60/60、52 个显式 Rust 文件 direct rustfmt、scoped diff check 与 scan-only 资产 SHA-256 均通过；Rust 行为回归 source-ready，等待 current-source 产品编译。

同日 Asset filter-bar convergence：本地 Unreal Content Browser 的 `SFilterList`/`SAssetFilterBar` 以一个 add-filter/menu authority 管理 active/pinned filters，MagicaVoxel `top.ui` 同样只把高频动作常驻在紧凑工具条；Zircon `asset_browser.zui` 过去却把 12 个 kind tab 作为两排资产节点，并在产品投影中按宽度逐个隐藏，当前选中项之外的类型在窄窗不可发现。Asset Browser 现与 Assets Activity 共用 `views/asset_kind_filter.rs` 的 16 项稳定 ID、显示文案、支持集合和 unsupported-current 投影；产品 toolbar 只挂一个原生 `WorkbenchDropdown`，由其 Runtime `popup_open`、选项几何、dismiss 与键盘导航构成唯一 popup authority，Change 选项继续进入既有 `AssetCommand::SetKindFilter`，不再并行打开 Workbench context menu。Editor popup-row 与 keyboard-target 只对资产下拉保留 `asset` / `asset:browser` 来源，普通下拉仍使用 `workbench_option`；根 Dropdown 的无 action 点击仅交给 Runtime 开关弹层，选项的 `AssetSurface/SetKindFilter` 规范化为资产 Change。生产 `asset_browser.zui` 已物理删除 12 个旧 Chip 和不可达 secondary row，投影不再构造它们的 text/state override，重排也不再扫描缺失的 secondary control；旧 chip 选择算法只留在无 Dropdown 的兼容/性能测试中，不进入当前产品树。响应式 row 在 640px 下先为 Search + 124px filter trigger 保留预算，再依次收起低优先级 view/locate 动作，Import 主动作仍可达；unsupported kind 以 selected+disabled 第 17 项可见，不静默回退 All。最新专项 contract 7/7、ZUI 聚合 223/223、当前资产集 248/248 TOML parse、direct rustfmt、scoped diff check 与永久 scan-only 资产 SHA-256 `5DEFDAD04BE2837AF3CD012C63D6801D0136F8B1CAC36B492E6799C921381F09` 通过。真实启动的 bundled MagicaVoxel 另产生 900x620 物理客户区参考图和 18x18 dropdown corner crop（33 个 distinct ARGB），确认约六像素紧凑圆角的清晰边缘来自输出像素上的连续覆盖，而非全窗超采样或全局 pill radius。此前单次 current-source `build-editor.ps1 -Ephemeral` 在 Cargo 前被 coordinator `cargo.acquire` request `ce363d0d866542c99b3810a7d0de1dce` 的 `command_post_timeout` 截断；未查询 recovery URL，没有从该请求推断 rustc、EXE、WGPU 截图或 profile，M5/M6 acceptance 继续 pending。

同日 host asset-filter value-domain correction：产品 page template 与 shell region 实际挂载的 `host/asset_surface_controls.zui` 仍让 ComboBox 发出 `all/scene/mesh/material/ui`，而唯一 `parse_asset_kind_filter()` 只接受大小写精确的 `All/Scene/Mesh/Material/Ui*` 稳定 ID，因此旧选择会进入 `AssetKindFilterError::Unknown`，且只能发现 5 类。该 host control 保留自身 ComboBox Change authority 和原 `AssetSurface/SetKindFilter` binding，不叠加 context-menu popup；其值域硬切为与两个产品 pane 相同的 16 个 canonical ID/可读 label，默认显示 `All Types`，宽度收敛为 124/168/220 logical px。静态契约逐项要求每个 option ID 都存在于执行解析器，完成 RED（`all != All`）到 5/5 GREEN；完整 Editor `.zui` 187/187、Editor UI 81/81、ZUI 聚合 221/221、248/248 TOML parse 与保护资产 hash 通过。Rust/Cargo 与 current-source 产品视觉证据仍未取得，前述 coordinator timeout 不因此升级为成功。

同日 asset-filter painter identity correction：旧 Workbench button selector 以宽泛的 `AssetBrowserKind*` / `AssetsActivityKind*` 前缀识别 toolbar chip，使新 `AssetBrowserKindFilterDropdown` 也会被 tab-like/chip style 分类器捕获。Kind 控件身份现仅接受明确的 `...Chip` / `...Button` 后缀，ViewMode 家族和旧 `workbench.asset.kind_filter.set` action 兼容路径不变；新控件保持 Dropdown painter 与原生 Change/popup trigger 语义。专项契约先以宽泛前缀命中完成 RED，再以 Python source guard 和 Rust identity regression 收口；最新完整矩阵见上段。Rust 回归仍为 source-ready，current-source 产品 build、原生 WGPU 三档截图和 profile 继续 pending。

同日 final-size raster edge correction：产品 `CircularProgress` 仍在最终物理尺寸以像素中心二值裁剪圆环，外缘/内缘 alpha 只有 0/255，任意角度的 percent 端点也在 fill/track 间二选一；缺失图标 fallback 同样直接以整数像素绘制边框和 X 形斜线；sample-grid 与 timeline 还分别以 `2r+1` 条 1px quad 扫描线拼菱形点。圆环现把解析 annulus coverage 与 angular arc-length derivative 缓存在既有 topology 中，轮廓按 coverage 缩放直通 alpha，起止端点在约一个物理像素弧长内以共享 LUT 做 alpha-weighted linear-light 混色；24px 数值复核得到 108 个 fractional silhouette pixels、11 个 fractional alpha level，37.5% 非轴对齐端点另有 2 个完全不透明的混色像素。缺失图标不创建扩大位图，而在不超过 32px 时按每像素 4x4、较大目标按 2x2 局部采样覆盖率，并以保守包围判定跳过远离稀疏 stroke 的像素，保留既有图片缓存。两处 diamond consumer 收敛到 `template_diamond_glyph.rs` 的 4x4 local coverage 与 32-entry `(edge,color)` LRU，每层只提交一个可进入现有 icon atlas 的 image command；radius 3 的 7x7 raster 有 24 个 fractional pixels，外层从 7 commands 降为 1，odd-edge image frame 按 `edge / 2` 而不是整数 radius 定位，确保采样中心落在 authored point，避免整个 glyph 向右下偏移 0.5 device pixel。`.zui` 产品 radius token 复核仍为 6/8/10/12px，普通按钮和下拉使用 8px，与实测 MagicaVoxel 约 6px 紧凑圆角相符，因此不把全局控件改成 pill。三条 Python 契约均先 RED 后 GREEN，设备像素/AA 静态门禁现为 30/30；Rust 像素/缓存/单命令/中心对齐/早退穷举回归 source-ready，未伪称已由 Cargo 执行。

同日 workbench chrome shape correction：样式优先级复核确认 stylesheet 的 `radius` 会覆盖组件节点的 `corner_radius`，导致 `WorkbenchPanelHeader` 明明声明了形状 token 仍被严格主题的 `radius = 0.0` 抹平。面板标题组件与 `.workbench-panel-header` 现统一使用 `$editor.control.radius.small`（6px），`.workbench-section-title` 同样使用该 token；视口场景、属性行、dock 外壳仍保持方角，避免把结构边界和数据密集行误做成卡片。现有设计 token 契约同步检查组件与主题的一致性；相关 UI 形状/布局/AA Python 契约 77/77、资产读取与 scoped diff check 通过。该修正改善控件圆角语义，但仍不能替代 current-source Editor EXE 的三档 WGPU 截图与 profile 验收。

### M6 — 设备像素 AA、局部超采样与真实产品视觉

- 详细 owner 与验收矩阵见 [`12/2026-08-15-m6-device-pixel-aa-and-local-supersampling.md`](12/2026-08-15-m6-device-pixel-aa-and-local-supersampling.md)。原生 surface 的质量下限是当前物理 client extent，不允许低于设备输出分辨率；布局与命中仍保持 logical/device transform 单一权威。
- Windows per-monitor `ScaleFactorChanged` 先更新 retained scale，再与 native resize 共用 frame-bound transaction；若同轮收到新的 surface metrics，presenter 只接收其中的 physical extent。每个尺寸/DPI 事件最多保留最新 extent，并排一个 interactive full-frame；redraw 阶段先完成 presenter resize，再提交 retained layout/frame update 与 present，避免以 debounce deadline 结束在 frozen frame 或 stale logical scale。设备像素/AA 静态门禁现为 40/40，覆盖跨显示器 DPI 链、direct/template/replay 方形边框的统一解析 coverage authority、CPU/WGPU scaled-image 线性光采样一致性、software shape/image/text/transparent-overlay 的统一线性 source-over、WGPU/software square-fill 分数几何不被隐式整数化，以及 CircularProgress、缺失图标 fallback、sample-grid/timeline diamond 的 fractional alpha。
- WGPU 圆角 fill/border 使用像素域 rounded-box SDF、`fwidth` 与 `smoothstep` fractional coverage；外轮廓与内轮廓分别从自己的 signed distance 求导数宽度，避免缩放后的细边框复用错误的外缘过渡宽度，并在 linear-light premultiplied-alpha 路径合成。匹配的 fill + border 在 geometry compile 阶段融合为一个顶点项，CPU direct/template/replay 使用同一覆盖分区；低层回归分别锁定同色外沿不得重复覆盖、异色中心/边环/外缘颜色分区，以及 frame/clip/radius 不一致时禁止融合。retained batch plan 另缓存被融合的 border command bit-vector，使 damage cache-hit 以 O(1) 标志读取维持 2 source commands / 1 draw item / 1 draw call。静态分隔线可显式 pixel snap，圆角、滚动、变换和动画不得被全局整数化。profile v4 还从同一绘制命令流发布实际物理圆角 frame/radius/border，视觉 oracle 按声明几何评估曲线，不再用固定 8px 经验值代替产品像素证据。
- SVG 与 fallback glyph 使用受目标物理尺寸约束的局部超采样和缓存；不得默认超采样完整编辑器 backbuffer。非方形 SVG 先按物理目标等比拟合：最大内容边不超过 32px 时使用 4x 临时源，其他向量使用 2x，越过 4096px 边界前降级为 1x；随后以 linear-light resolve 居中写入完整物理目标的透明画布，保证上传 texture 与最终 frame 同尺寸，避免被 linear sampler 二次拉伸。缺失图标 fallback 以相同的 32px 阈值执行每像素 4x4/2x2 局部 coverage resolve，不分配超采样 bitmap；CircularProgress 则直接使用解析 annulus coverage。该边界与本地 Unreal Slate 的 output-resolution raster + analytic coverage + vector-local filtering 一致，避免把 scene TSR/MSAA 误当成 Slate UI 方案。
- 低层 RHI/WGPU 回归和离屏 fractional-edge readback 是必要证据，但不能替代产品验收。最终仍须用 current-source `zircon_editor` 在 640x520、900x620、1672x941 三档物理客户区完成真实 WGPU 截图、像素检查、DPI 记录，以及 1000 click / 1000 pointer move / 200 resize profile。
- 当前 acceptance 仍为 pending：较早的 managed Editor 尝试分别被 foreign world-query E0004、coordinator timeout 或 Cargo 前的空等待截断。2026-08-31 在 HEAD `14c89f9776bed828cc85e05e4b9914b3f8d1e784` 加当前 overlay 上执行 `tools/build-editor.ps1 -Ephemeral -StorageMode diagnostic`；managed job `8577b2fd6f5c4ab8be04dfc8283146ad` 已真实进入 `cargo build -p zircon_app --bin zircon_editor --no-default-features --features target-editor-host --locked`，编译到 Editor/WGPU/Runtime 依赖图后在共享 `zircon_runtime` 以 102 个 current-source errors 停止。代表性诊断包括 `graphics/feature/builtin_render_feature_descriptor/feature_descriptors/mod.rs:32` 的 E0015、`graphics/scene/gpu_scene/prev_transform.rs:34` 的 E0502、`resource_streamer/resource_streamer_ensure_material.rs:683` 的 E0594 与 `platform/host/service.rs:56` 的 E0382；错误横跨 mesh、transient texture、ECS、font 和 resource streamer 等 foreign owner，不构成 UI12 的批量修复授权。job 已以 exit code 1 自然结束、released，隔离 target 与发布 staging 均已清理，未生成 Editor EXE 或 bundle。本计划不修改这些 foreign Runtime owner 路径，也不会把静态检查、低层离屏 readback 或旧兼容帧升级成产品视觉通过；当前仍没有 current-source Zircon 产品窗口、三档 WGPU 截图或 1000/1000/200 profile，不能接受 M6。

## 6. 风险与拒绝条件

- 若基础 style/popup/focus failure 已由其他编号计划拥有，创建 cross-plan failure handoff；不在 editor 资产中添加兼容分支或视觉掩盖。
- 若某个 fixed size 属于真实稳定格式控件，保留并在 guard allowlist 中按组件 family 说明；治理不得机械禁止所有 `Fixed`。
- 若 token 变更导致 viewport 图像、数据可视化或插件品牌辨识度下降，回退该具体语义映射，不回退整体中性工作台层级。
- 若视觉截图正常但 keyboard/popup/focus 测试不完整，里程碑不接受。
- 若测试通过但 live componentized projection 未使用改动资产，里程碑不接受。
- 若当前工作树的未归属改动无法证明来源，不把它静默纳入 milestone commit；先通过 coordinator attribution/lease 确认归属。

## 7. 状态与产出记录

每个里程碑 testing stage、独立审查和 coordinator acceptance 完成后记录一次；实现切片不单独写产出记录。

子计划 current-source manifest 的 `Status: completed` 只表示该份 source output record 已达到 coordinator pre-bind 所需的结构完整性，不表示里程碑已接受。里程碑 acceptance 只以当前 immutable manifest 的 managed validation、独立 reviewer `0 Critical / 0 Important`、coordinator gate 和 milestone commit 为准；在这些条件齐备前，manifest 必须另列 `Acceptance: pending_independent_review`，本表状态也不得写“完成”。

| 里程碑 | 范围 | 状态 | 完成日期 | 验证批次 / 残余风险 |
|---|---|---|---|---|

2026-08-29 painter role correction：MUI X 共享 `push_quad` 曾把所有正宽度几何边框映射为 `focus_ring`，导致普通 picker/chat/demo surface 被误显示为键盘焦点。当前 helper 改用中性 `palette.border`，focus-visible 仍由状态感知 selector 独立绘制；scoped rustfmt、diff check 和静态 UI 矩阵通过。该修正不替代尚未取得的 current-source Editor/WGPU 三档产品截图与交互 profile。

2026-08-29 vector aspect correction：矢量缓存仅对正方形目标做 resize 分桶；非正方形 SVG 保留精确物理像素尺寸，避免 `41x43 -> 44x44 -> 41x43` 这类缓存量化造成比例拉伸。缓存仍受 4096 项/64 MiB LRU 硬上限约束；刷新后的 device-pixel AA + visual oracle 54/54、Editor UI 85/85、repository ZUI 223/223 均通过，current-source Editor/WGPU 产品截图仍待完成。

2026-08-29 visual population gate：原生截图 oracle 不再允许靠单个平滑圆角或单个平滑图标掩盖其余二值锯齿。对至少两个角具备有效前景/背景对比的圆角控件，至少一半必须四角都具有连续的分数覆盖，并且横纵曲线跨度与预期物理半径相称，避免把平滑但退化成 3px 的小圆角误判为合格；可分析的 activity-rail 矢量图标也至少一半必须具备分数边缘覆盖。报告输出候选数、半径合格数、通过数和比例，合成的 binary edge、稀疏噪声、3px radius 及 one-of-three token 回归 23/23 通过；其中 one-of-three 圆角控件用例从 capture manifest 入口重算 geometry fingerprint 并走完整产品分析链，证明总体覆盖门槛不是孤立 helper。PowerShell 原生采集链 8/8，锁定三档各自独立 GPU 进程、物理客户区、DPI、source/binary/bundle fingerprint 并默认调用 oracle。该门禁仍必须由 640x520、900x620、1672x941 三档 current-source WGPU 产品截图实际触发，不能用静态测试替代视觉验收。

2026-08-29 product icon routing re-audit：产品 `.zui` 字面 icon 全部通过 packaged SVG 解析合同，popup semantic icon 进入同一 vector candidate path，toolbar 的 icon-only 命令保留 label/tooltip；旧的 Plus/Trash/Chevron identity 推断也已映射到 packaged SVG，不再绘制 pixel-grid segments。SVG raster bucket、popup icon 和 toolbar hint 专项 26/26 通过；抗锯齿 missing-icon raster 仅是最后兜底，不是产品正常绘制路径。

2026-08-29 text and rounded-geometry evidence：产品 profile geometry 继续使用 schema v4，并从用于参考帧绘制的同一个 `ChromeCommandStream` 发布可见 text run 与实际圆角 Quad/Border 的 frame、clip、RGBA/字号、行高、命令序号、字符数、物理 corner radius 和 border width；证据不序列化实际界面文案。原生截图 oracle 以声明几何匹配控件，按 text run 的声明颜色和截图主背景为线性光端点，统计 glyph ink 与圆角边缘的分数覆盖、coverage bins 及跨行/跨列连续性，并要求至少一半可分析 text run 和圆角控件通过；圆角和矢量图标即使平滑，二值化文字或低半径控件仍会 fail closed。当前 AA + visual oracle 59/59、Editor UI 87/87、repository ZUI 223/223、native capture Pester 8/8、Python compile、Rustfmt 与 scoped diff check 均通过，受保护 UI Asset Editor 资产 SHA-256 仍为 `5DEFDAD04BE2837AF3CD012C63D6801D0136F8B1CAC36B492E6799C921381F09`。`docs/plans/optimize/zircon_runtime_interface/01/failure-2026-08-27-world-query-transform-snapshot-item-count.md` 在 HEAD `f2df7ed2100a771881a3b7222b726789b0b40abd` 仍为 open，故本轮未启动 Cargo，也未取得 current-source Editor EXE、三档 WGPU 文字/圆角像素或 1000/1000/200 profile；M6 acceptance 继续 pending。

2026-08-29 Welcome compact-width density correction：历史 Zircon 兼容帧虽然不能充当 current-source 视觉证据，但明确暴露了普通表单控件过高、最近项目列抢占主任务宽度的问题。当前 `welcome.zui` 把两个普通 `WorkbenchField` 从 48px large tier 收敛到共享 32px default tier；最近项目列使用 `136/184/240`，新建项目列使用 `280/560/760`，在 1000px 内容上限内保持总 max 精确闭合，并在 560–900px 窗口优先保证项目表单与动作宽度。MagicaVoxel `editor.ui` 的折叠标题为 25px、`file.ui` 分区标题为 28px；对应地，Recent Projects 从私有 46/48px 双行标题硬切到共享 `$editor.chrome.panel_header.height` 30px 单行栏头，删除无动作语义且在 136px 最小列宽会截断的 `Pinned startup workspace`，空列表的两行说明继续保留。最近项目滚动、pointer viewport、action route 和共享 8px control radius 均未改写。专项静态契约两次完成预期 RED 后为 12/12 GREEN；完整 Editor UI 87/87、Editor `.zui` 191/191、通用 ZUI 32/32、当前 253 个 Editor `.zui` TOML 解析、Python compile、direct rustfmt 与 scoped diff check 通过。永久 scan-only 的 `ui_asset_editor.zui` 对当前 HEAD 无 worktree diff，准确字节 SHA-256 为 `2B43835B860C82AB0CA71B8B10A5CFC451ADBFC5F1C6C9A00FD7DD78CA017C81`；此前 `5DE...` 记录不再作为 current baseline。Rust 布局回归已更新为 source-ready，但因 RuntimeInterface 集成 SHA 尚未返回，本轮没有启动 Cargo，也没有把历史帧或静态契约升级为 current-source WGPU 视觉通过。

2026-08-29 Generated Bottom single-mode authority：底部生成面板原先同时提供 Output/Build/Validation/Runtime/Review 五个 `WorkbenchTab` 和内容完全相同的 `WorkbenchDropdown`，但后者的 edit/commit 只改状态文案，Rust 还要把 Tab 选择镜像回 dropdown 的 `value/text`，形成重复状态和重复触发权威。当前产品树物理删除 dropdown、ModeEdit/ModeCommit bindings、preview action、navigation 与 feedback 分支；五个 Tab 继续通过既有 select action、互斥状态与 route table 构成唯一模式入口。默认态也进入与 Drawer/Core/Extension tabs 相同的 live-state 初始化事务：五个 Tab 删除 authored `selected/checked/value`，初始化复用 `select_generated_bottom_mode(Output)`，并把静态摘要 fallback 从矛盾的 Runtime 修正为 Output。过滤字段从固定 260px 改为 `160/260/340 + weight 1 + Stretch`，选中路由行继续 Stretch，因此窄底栏先压缩搜索、宽底栏再利用余量，不再由 160px 重复选择器挤占输出摘要。37 条 route 从只裁剪的 `VerticalGroup` 改为显示自动滚动条的纵向 `ScrollableBox`；五个固定宽模式 Tab 改由隐藏滚动条的横向 `ScrollableBox` 承载，二者都只声明 `input_hoverable + Receive`，不制造容器级 Click target 或 Tab stop。340px 辅助详情栏增加 `responsive_min_tier = narrow`，Ultra 时把宽度完整让给 route 列，从 Narrow 起恢复。专项契约先对旧 dropdown、旧路由、固定宽度、authored 默认态和两类不可达布局完成 6 项 RED，再为 6/6 GREEN；原生 generated-bottom verifier 为 37 routes / 46 events / 46 bindings / 46 preview actions，总 web-native handoff verifier 为 native 11 / extension 44 / component families 15。完整 Editor `.zui` 197/197、Editor UI 87/87、通用 ZUI 32/32、当前 253 个 Editor `.zui` TOML 解析、Node syntax、direct rustfmt、stale-authority source guard 与 scoped diff check 均通过。为使原型证据继续验证当前源码，两个 verifier 同步跟随 asset-backed component catalog、`WorkbenchSlot::BottomDrawer`、reference-menu dispatcher、world-building navigation 分片和 quality/observability preview-action 分片；没有放宽动作数量或身份一致性。RuntimeInterface 的 TransformSnapshot producer/consumer/test 联合仍只存在于未提交工作树而未进入 HEAD，故本任务没有启动 Editor Cargo、current-source WGPU 三档截图或 1000/1000/200 profile，本段只接受 source-ready UI 逻辑与布局。

2026-08-30 side-panel scroll reachability：结构化产品树审计发现 Assets Wide 详情栏有 26 个固定高子项、Blend Space 左栏有 14 个子项、Blend Space details composite 有 16 个子项，三者都只用 `clip + VerticalBox`，在 520/620 档会永久裁掉后半入口。Unreal `SDetailsView` 把固定筛选 chrome 放在 FillHeight 内容之外，并由 `SDetailTree.ExternalScrollbar` 管理可变详情树；`SAssetView` 的 tile/list 也显式设置 scrollbar visibility。MagicaVoxel `file.ui` 的 I/O 面板和 `editor.ui` 的 brush/options 侧栏同样在外层 layer 声明纵向 `scroll`，而非靠 clip 丢弃内容。当前三处长侧栏均改为 `ScrollableBox/Vertical/Auto`，只声明 `input_hoverable + Receive`，保留原 responsive tier、宽度、子项顺序、control ID 和 action route。固定 176–204px 的 Validation Log 进一步把 header/filter/footer 留在普通根布局，只将四条诊断放入独立滚动体，并删除无语义的 stretch fill，避免 Clear 命令随诊断一起被裁掉；Validation All 也移除 authored selected/checked，改由 `select_exclusive` 在 live 初始化阶段统一设定。随后 Assets 详情栏的 22 个低频全宽扩展入口收敛为 World/Gameplay & Simulation/Production 三个普通按钮触发器，窗口 overlay 持有三个 `WorkbenchPopupMenu`，菜单项通过 `asset_editor_menu.rs` 显式映射回原扩展动作，保持菜单命中、dismiss、键盘导航和 action dispatch 的单一权威。专项契约按四项 RED 收口为 4/4 GREEN；完整 Editor `.zui` 201/201、Editor UI 87/87、通用 ZUI 32/32、当前 253 个 Editor `.zui` TOML 解析、Python compile、两套 handoff verifier 与 scoped diff check 通过。当前 HEAD `a6cc2b0ca091014944b1a0ce0b48e1a8c3101eb6` 仍不含 RuntimeInterface TransformSnapshot arm；本任务没有启动或接管工作区中其他 owner 的 Cargo，因此仍无 current-source Editor/WGPU 视觉与 profile 证据。
补充回归：菜单化新增 4 个 Editor ZUI 静态契约测试后，当前完整 Editor `.zui` 矩阵为 205/205，实际可解析 Editor 资产为 248/248；前段记录中的 201/201、253 个资产数字对应菜单改造前的同一收口阶段。

2026-08-30 gameplay details scroll reachability：继续按 Unreal `SDetailsView` 的固定 chrome + 可滚动详情树和 MagicaVoxel 侧栏 layer scroll 组织核心 Gameplay 模块。Ability 左侧任务列表、Ability 右侧详情/动画工具、Effect 左侧筛选/资源列表、Effect 右侧属性详情均保留原标题、control ID、事件 route 和 responsive tier，只把长 body 移入独立 `ScrollableBox/Vertical/Auto`；标题不随滚动消失，窄高面板不会永久裁掉后半入口，也不会引入容器级输入目标。新增专项契约后完整 Editor `.zui` 静态矩阵为 206/206，Editor 资产 TOML 解析保持 248/248，Python compile、Rustfmt 和 scoped diff check 通过。RuntimeInterface `TransformSnapshot` 联合修复仍未进入当前 HEAD，因此没有把这次 source-ready 布局改造升级为 current-source Editor/WGPU 截图或 profile 验收。

2026-08-30 viewport toolbar overflow：视口左侧工具条原先直接横向排列 14 个 28px 图标按钮，窄窗口时会挤压中央 viewport 并遮蔽右侧 Camera/Align 控件。参考 Unreal viewport toolbar 的稳定右侧工具组和 MagicaVoxel 的紧凑可滚动工具层，`scene_viewport_toolbar.zui` 现把左侧组改为无滚动条的横向 `ScrollableBox`，保留每个工具的 control ID、icon、tooltip、Change/Click route；右侧相机组仍固定在 root 末端。布局专项新增溢出契约后完整 Editor `.zui` 静态矩阵为 207/207，Editor 资产 TOML 解析保持 248/248，handoff verifiers、Python compile、Rustfmt 和 scoped diff check 通过。该结构改造仍待 RuntimeInterface 进入 HEAD 后由 current-source WGPU 运行帧确认实际物理客户区表现。

2026-08-30 Ability animation tools menu：Ability 详情栏原先把 Sequencer、Montage Editor、Blend Space、Pose Library、Retarget、Control Rig、Motion Matching、Animation Compression 八个低频扩展入口作为连续全宽按钮堆叠，造成详情字段和命令层级混杂。当前改为单一 `WorkbenchAbilityAnimationTools` 触发器，由窗口 Overlay 的 `WorkbenchAbilityAnimationToolsMenu` 承载八个菜单项；`ability_editor_menu.rs` 将每项显式映射回原有 `workbench.extension.*.open`，并复用参考菜单的 MenuAction binding、窗口菜单状态和 popup dismiss 逻辑。扩展导航 spec 统一使用菜单 control identity，旧八个按钮和对应 native extension opener binding 已删除，preview action 仍完整保留。新增 Ability 菜单契约后完整 Editor `.zui` 静态矩阵为 211/211，Editor 资产 TOML 解析保持 248/248，native extension verifier 为 858 events / 858 routed / 858 unique / 858 bindings / 888 preview actions，web-native handoff 为 native 11 / extension 44 / component families 15 / nativeExtensionEvidence 44。RuntimeInterface TransformSnapshot 联合修复尚未进入 HEAD，因此本段仍是 source-ready UI 逻辑收口，不宣称 current-source Editor/WGPU 像素或交互 profile 验收。

2026-08-30 core module tab hierarchy：Behavior、Perception、Assets、Ability、Effect、Tags、Material、Render、VFX、HUD 十个核心模块原先都把三个同级模式 Tab 作为全宽纵向按钮直接挂在左栏，与资源行、任务行和命令混成同一视觉层级。参考 Unreal 工具面板的固定 tab chrome，以及 MagicaVoxel `editor.ui` 的 `sw-title -> tab-l -> scroll layer` 结构，当前统一为 `固定 SectionTitle -> 34px 横向等分 TabStrip -> 内容行`；Ability/Effect 进一步将 TabStrip 提升到滚动体外，仅让任务/资源内容滚动。新增 strip 容器没有 events、selected/checked 或 input policy，30 个原始 Tab 的 control ID、event ID、route 与 Rust live-state 互斥选择 authority 均未改变。专项契约 3/3、完整 Editor `.zui` 214/214、Editor UI 87/87、通用 ZUI 32/32、248 个 Editor 资产解析、native/web handoff、Python compile 与 scoped diff check 通过；结构化扫描确认核心模块 `vertical_tab_clusters=0`。底层复核同时确认产品 surface 使用物理窗口像素、圆角命令进入 analytic SDF/fwidth/smoothstep 路径，CPU fallback 使用 8x8 覆盖率与线性光混合；因此本段没有用继续放大 radius token 掩盖尚待 current-source WGPU 截图确认的真实视觉问题。

2026-08-30 core Details name/value convergence：Behavior、Perception、Assets、Ability、Effect、Tags、Material、Render、VFX、HUD 的右侧 Details 原先直接纵向堆叠 31 个 Field/Dropdown，Blackboard、Cooldown、Magnitude、DPI 等属性有时只有值、有时又把属性名重复编码进值字符串，扫描时必须逐项猜测语义。Unreal `FDetailWidgetRow` 明确拆分 `NameContent`/`ValueContent`，`SDetailSingleItemRow` 通过共享 `FDetailColumnSizeData` 和 splitter 维持稳定名称列/值列；MagicaVoxel `editor.sty` 也以紧凑 label/textField 和统一 margin/radius 组织工具属性。当前十个核心模块统一复用既有 `WorkbenchPropertyEditorRow`，把稳定属性名放入共享 105px 逻辑名称列，把原 WorkbenchField/WorkbenchDropdown 原样插入 `value` slot；值只保留实际数据，Tags 搜索从伪值改为真实空值 + placeholder。31 个值控件的 control ID、事件 ID、Change/Submit route、Dropdown `value/value_text` authority 均未改写，外层属性行不拥有事件或第二状态。专项契约按共享行缺失和标签重复完成预期 RED 后为 3/3 GREEN；完整 Editor `.zui` 217/217、Editor UI 87/87、通用 ZUI 32/32、当前 253 个 Editor `.zui` TOML 解析、Python compile 与 scoped diff check 通过。RuntimeInterface `TransformSnapshot` producer/RuntimeHost consumer/全变体回归仍未进入当前 HEAD，因此本段仍是 source-ready 的信息架构与布局收口，没有启动 Cargo，也不宣称 current-source Editor/WGPU 三档截图或交互 profile 已完成。

2026-08-30 Render Tools anchored menu：Render 右侧 Details 原先把 Shader Editor、Lighting Bake、Post Process 三个低频扩展入口直接堆在属性字段下方，和可编辑的 Pipeline/Platform/Frame 值混成一个命令层。当前收敛为一个 `WorkbenchRenderTools` 普通按钮，窗口 Overlay 持有控制锚定的 `WorkbenchRenderToolsMenu`，菜单项以 `menu.item.render.*` 保留三项动作身份；新增 `render_editor_menu.rs` 以显式 `menu_action_id -> workbench.extension.*.open` 表映射并复用参考菜单的 `EditorUiBindingPayload::menu_action`、dismiss、键盘导航与 retained dispatch。扩展导航 spec 统一使用 `WorkbenchRenderToolsMenu`，旧三个 Render opener control/binding 已删除，preview action 仍保留，避免业务动作注册与入口布局耦合。原型交接验证器同步按当前结构计数：44 个扩展工作区的 888 个 preview actions 中，855 个保持直接 ZUI/native binding，Assets/Ability/Render 菜单分别承接 22/8/3 个 opener，菜单映射 33 个，合计 native evidence 888/888；核心模块验证为 194/194 events 与 bindings，5 个面板菜单状态动作由窗口菜单状态单独校验。专项 Render 契约 4/4、完整 Editor ZUI 221/221、Editor UI 87/87、通用 ZUI 32/32、253 个 Editor `.zui` TOML 解析、三个 Node verifier、8 个受影响 Rust 文件 direct rustfmt、Python compile 与 scoped diff check 均通过。一次误触发的本地 `cargo fmt --all -- --check` 在超时后仅终止本次命令自有进程，未触碰既有 foreign Cargo job；本段没有启动编译、Editor EXE、current-source WGPU 三档截图或 1000/1000/200 profile，RuntimeInterface `TransformSnapshot` 联合修复仍未进入 HEAD，M6 acceptance 继续 pending。

2026-08-30 HUD Tools anchored menu：HUD 中心区原先在 Preview 主动作下方常驻十个低频诊断/工具扩展按钮，并依赖横向 `ScrollableBox` 在窄宽度下隐藏溢出入口；这会额外占用 Canvas 高度，也把主预览动作与偶发诊断跳转放在同一视觉权重。当前删除整条快捷按钮带，在固定 header 中保留独立 Preview，并新增单一 `WorkbenchHudTools` 普通按钮；窗口 Overlay 持有控制锚定的 `WorkbenchHudToolsMenu`，十个 `menu.item.hud.*` 菜单项由新增 `hud_editor_menu.rs` 显式映射回原 `workbench.extension.*.open`。窗口菜单状态、MenuAction payload、dismiss、键盘导航与 retained dispatch 继续复用既有单一 authority；十个扩展 navigation spec 的 opener identity 统一改为菜单 control，旧 HUD button control 和直接 native opener binding 均删除，扩展页内部字段、preview action 与业务 route 不变。全局验证器按当前结构计数：44 个扩展工作区仍有 888 个 preview actions，其中 845 个保持直接 ZUI/native binding，Assets/Ability/Render/HUD 四类菜单承接 43 个 opener，合计 native evidence 888/888；核心模块验证为 195/195 events 与 bindings，6 个面板菜单状态动作单独校验。HUD 专项契约 4/4、完整 Editor ZUI 225/225、Editor UI 87/87、通用 ZUI 32/32、253 个 Editor `.zui` TOML 解析、三个 Node verifier、9 个受影响 Rust 文件 direct rustfmt、Python compile 与 scoped diff check 均通过。当前 HEAD `6d65bb33742bea089260f73f1faacb28df8c76f5` 仍不含 `WorldQueryResult::TransformSnapshot` item-count arm，仅共享未提交工作树包含该 arm；因此本段未启动 Cargo、Editor EXE、current-source WGPU 三档截图或 1000/1000/200 profile，M6 acceptance 继续 pending。

同轮 extension-menu icon semantics：Assets、Ability、Render、HUD 四组 43 个扩展菜单入口虽然已完成分组和单一触发权威，但此前全部复用 `grid` 图标，无法通过轮廓快速区分地形、物理、动画、渲染和诊断工具。当前每个菜单 action 显式使用对应语义名称，共 42 种图标身份；`shell_icon_alias` 只补充到已有打包 SVG 的确定映射，例如 terrain/prefab/physics/navmesh、sequence/skeleton/constraint、shader/rendering、diagnostics/profiler/frame-time/font，不增加手绘 glyph、运行时路径猜测或第二套图标 loader。Ionicons 的 terminal/network 与既有 eye/image/link/light 等别名继续走同一 `push_icon_asset_pixels`、设备物理像素和局部超采样缓存路径。新增契约先对 43 个 `grid` 命中 RED，修复后 popup/icon 专项 4/4、打包 SVG 解析守卫 17/17、完整 Editor ZUI 226/226、Editor UI 87/87、通用 ZUI 32/32、253/253 Editor `.zui` TOML、Rustfmt、Python compile 与 scoped diff check 均通过。该证据证明资源与路由闭合，但实际图标锐度、对比度和菜单行节奏仍待 current-source WGPU 产品截图验收。

同轮 Tags action-row density：Tags 左栏的 `Add Tag` 与 `Rename` 原先作为两个纵向按钮占用两行，和标题、TabStrip、源文件行混在同一垂直命令流；参考 Unreal Details 的 inline action row 以及 MagicaVoxel 紧凑横向工具层，当前新增无状态 `WorkbenchTagsActions` 横向容器，把两个既有按钮并列放入单一 30–32px 行。按钮 control ID、event ID、route、secondary visual variant 和 Rust dispatch 均未改写，容器不声明 events、selection 或 input policy。专项 core tab/layout 契约扩展为 4/4、完整 Editor ZUI 227/227、Editor UI 87/87、通用 ZUI 32/32、253/253 TOML、Rustfmt、Python compile 与 scoped diff check 通过；这是 source-ready 密度改进，仍待 current-source WGPU 窄窗口截图确认文字和按钮边缘没有裁切。

同轮 legacy screen-space AA convergence：复核发现 `zircon_runtime/src/graphics/scene/scene_renderer/ui` 的旧 screen-space fallback 仍以 24-byte `position + color` 顶点和纯色 fragment 绘制 quad，完全不消费 `UiResolvedStyle.corner_radius`，也没有对 fractional physical frame 提供边缘覆盖率；这与当前产品 `zr_rhi_wgpu/ui_surface` 已采用的物理 target、rounded-box SDF、`fwidth`/`smoothstep` 路径不一致。该 fallback 现在携带本地坐标、半尺寸、圆角半径和边框宽度，圆角、分数物理边缘与分数边框统一进入解析 coverage，物理像素对齐的普通方形仍保留原有简单几何；旧绘制路径的 pipeline ABI 与 WGSL fragment 已同步更新，并增加 source-ready geometry/shader regression。此改动只修复绘制底层的视觉一致性，不改变 `.zui` 布局、事件、popup 或 hit-test authority。direct rustfmt 与 scoped diff check 通过；由于当前 HEAD 仍未合入 `WorldQueryResult::TransformSnapshot` producer/consumer/test 联合修复，本轮没有启动 Cargo、Editor EXE 或 current-source WGPU 视觉截图，M6 acceptance 继续 pending。

同轮 legacy batch publication regression：前一轮几何测试只验证了 `push_rect_with_radius` / `push_border_with_radius` 的局部构造，未直接证明真实 `plan_screen_space_ui_batches` 会把 `UiResolvedStyle` 的圆角和分数边框送入最终顶点批次。新增 quad command 回归覆盖 8px radius、0.625px border、fractional physical frame，要求最终计划同时包含 6 个填充顶点与 6 个解析边框顶点，并逐顶点检查 radius、非负 border width 与有限 local coordinates；WGSL coverage helper 改用 `1.0 - smoothstep(-half_width, half_width, distance)` 的正向边界表达，避免反向 edge 参数在不同后端上的实现差异。该记录仍是 source-ready 底层证据，未替代 current-source WGPU 三档截图和交互 profile。

同轮 analytic fringe scissor correction：旧 fallback 已生成外扩 1px 的解析覆盖几何，但批处理原先仍以未经扩展的 `command.frame` 计算 GPU scissor，导致圆角/分数边缘可能在最后一道裁剪时被截掉。现在 `coverage_frame` 与几何路径共享同一判定（圆角、非整数物理边界或分数边框宽度），scissor 先覆盖 fringe，再与 viewport/父级 `clip_frame` 求交；新增计划级回归锁定 8px radius 的 `(x,y,w,h) = (7,11,122,38)` 物理裁剪范围。该修正只扩大解析边缘的可见窗口，不放宽父级 clip，也不影响对齐方形的旧 fast path。

同轮 parent-clip guard：为防止 fringe 扩张穿透组件边界，新增父级裁剪回归，使用同一 8px analytic quad 与 `20,15,40,20` 的 `clip_frame`，要求最终 scissor 严格回到父级矩形 `(20,15,40,20)`；因此解析覆盖只补充组件自身边缘，不改变现有 viewport/父级裁剪权威。设备像素、AA、screen-space plan/cache 与 Workbench layout/status 静态矩阵合计 104/104 通过，仍待 RuntimeInterface 联合修复进入 HEAD 后执行 current-source WGPU 和真实产品截图。

同轮 popup shadow coverage：复核发现 Runtime popup 已有主题 `shadow` 色和圆角解析覆盖，却将 popup 背景直接贴在相邻面板上，`elevation = 0.0` 也不会进入 `UiResolvedStyle`，因此仅调整 `.zui` elevation 不会产生任何视觉结果。现在 `popup_rows.rs` 在精确的 popup background z 之前发布两层轻量 shadow quad：一层外扩 2px 并下移 2px，一层外扩 1px 并下移 1px；两层均消费主题 shadow 色、相同 panel radius 加扩散量和现有 clip/opacity。命中投影仍只匹配 `popup_base_z` 的 Open background，不会把阴影扩散区域变成可点击区域。新增 Runtime shadow 几何/顺序回归和静态契约；本地 Python contract 与 rustfmt/scoped diff check 通过，当前 HEAD 仍缺 RuntimeInterface `TransformSnapshot` 联合修复，未启动 Cargo 或产品截图。

同轮低层验证边界：popup shadow 的三份 Runtime 源文件 direct `rustfmt --check` 与 34 项 popup/菜单/性能 Python 契约通过；随后真实执行 `cargo test -p zr_rhi --lib ui_surface --locked`，UI surface 20/20 通过。`cargo check -p zr_rhi_wgpu --lib --locked` 也通过，说明圆角 SDF、`fwidth` coverage、物理 projection 和 WGPU pipeline 的生产库可编译；其 `--lib ui_surface` 测试目标未能启动，先在既有测试源导入漂移处停止：`production/diagnostics/readback/tests.rs` 缺少 `texture_row_alignment` 导入，`ui_surface/tests/native_submission.rs` 缺少 `SolidVertex` 导入。该测试入口失败不归因于 popup/AA 切片；当前仍未取得 `zircon_editor` current-source EXE、三档 WGPU 产品截图或 1000/1000/200 profile，因此 M6 acceptance 继续 pending。

同轮 Asset Browser 顶层详情滚动：窄高审计发现根 `asset_browser.zui` 的 `AssetBrowserDetailsContentPanel` 固定 538px 高，却由普通 `HorizontalBox` 承载在固定 header 下方；520/620 物理客户区会裁掉底部 Diagnostics，且没有滚轮入口。参考 Unreal Details 的固定 header + 可滚动 detail tree 以及 MagicaVoxel 侧栏的 scroll layer，现将原 `AssetBrowserDetailsScrollBody` 保持 control ID 不变，改为 `ScrollableBox` 的 Vertical/Auto 容器并声明 `input_hoverable + Receive`；header 仍留在 `AssetBrowserDetailsPanel` 外层，左右 gutter、内容面板的字段顺序/高度、所有 action 与选择路由均不变。新增 2 项静态契约后该资产 TOML 解析和专项测试 2/2 通过；当前仅完成 source-ready 布局修正，仍待 current-source Editor/WGPU 窄高截图确认滚动条、文本和圆角实际呈现。

同轮 physical-fragment 2x2 coverage：现有 WGPU 与 legacy screen-space UI 已在物理 client extent 上用 rounded-box SDF + `fwidth`/`smoothstep` 做解析覆盖，但单一 fragment center sample 在小圆角和分数边界仍会暴露方向性阶梯。两条 shader 的 fill/border coverage 现统一通过四个物理像素子样本（`sample_offsets`）平均，并在每个子样本内分别计算 outer/inner signed-distance coverage；布局坐标、DPI presentation scale、target/projection size 与命中权威均未改写。WGPU 生产库 `cargo check -p zr_rhi_wgpu --lib --locked` 通过；本地 Naga WGSL parser 对两份 shader 实际解析通过；设备像素与 legacy AA Python 契约 36/36 通过，scoped diff check 通过。该改动仍是 source-ready 的边缘质量提升，不代表 current-source Editor EXE、三档 WGPU 产品截图或 1000/1000/200 profile 已完成。

同轮 physical-target projection contract：针对“UI 中间目标不得低于当前屏幕物理分辨率”的底层不变量，`UiSurfaceDrawList` 新增 Rust 回归，覆盖零尺寸归一化以及普通、generation、compact 三种构造路径，要求 `projection_size == surface_size` 且不误标记 target-only resize；Editor GPU presenter 的静态契约同步锁定 native resize 在 X/Y 两轴均以 `max(previous, current, requested)` 保持投影不低于当前物理客户区，并确认 resize 绘制实际消费冻结 projection。`test_editor_ui_device_pixel_aa_contract` 现为 35/35，`cargo fmt --manifest-path zircon_runtime/crates/zr_rhi/Cargo.toml -- --check` 与 scoped `git diff --check` 通过。该收口只固化既有物理 target/projection 设计，没有引入整窗 TSR 或改变 native resize 的保帧语义；由于当前 HEAD 仍缺 RuntimeInterface `TransformSnapshot` 联合修复，本轮未启动 Editor Cargo、EXE、WGPU 产品截图或交互 profile，M6 acceptance 继续 pending。

2026-08-30 reference implementation traceability：针对“不能低于屏幕分辨率、需要抗锯齿/超分辨率”的底层设计要求，补齐 Unreal 与 MagicaVoxel 的逐文件对照。Unreal `Engine/Source/Runtime/Slate/Private/Framework/Application/SlateApplication.cpp:1030` 在窗口创建前启用 High-DPI，约 `:1394` 的 Slate prepass 将应用缩放与原生 DPI 相乘；`Engine/Source/Runtime/SlateRHIRenderer/Private/SlateRHIRenderer.cpp:1603` 从 viewport extent/override 计算 UI scale 并携带 viewport extent/rect，post-buffer 的下采样是渲染完成后的可选路径，不是 UI target 的最低分辨率；`Engine/Source/Runtime/SlateCore/Public/Styling/SlateBrush.h:101` 说明 vector brush 按请求尺寸与 scale 渲染/缓存。MagicaVoxel `config/ui/editor.ui` 则以固定标题/Tab chrome 加 `flex=1` 的 scroll layer 组织窄面板，`config/ui/editor.sty` 使用约 3–6px 的紧凑 layer/control radius，`ui_reimplementation_guide.md:104-107` 与 `:381-423` 明确 style radius、hbox/vbox measure、scroll/clipping 与 popup layer 的实现边界。

同轮 WGPU coverage predicate correction：复核发现 WGPU `rounded_box_alpha` 曾把 `smoothstep` 的 edge0/edge1 反置，导致 signed-distance coverage 依赖未定义的反向边界行为；现与 legacy screen-space shader 统一为 `1.0 - smoothstep(-0.5 * width, 0.5 * width, distance)`，负距离内部为 1、正距离外部为 0，边缘在一个解析宽度内连续过渡。新增/更新静态契约锁定正向边界表达，避免后端差异再次把圆角退化为硬切；这不改变物理 target、局部 2x2 coverage、border outer-minus-inner 或布局/命中 authority。

同轮 lower-layer validation boundary：修正后 `cargo check -p zr_rhi_wgpu --lib --locked` 真实通过，Python device-pixel/AA contract 为 35/35，`cargo fmt --check` 与 scoped `git diff --check` 通过；尝试执行精确的 `wgpu_ui_rounded_solid_readback_contains_fractional_edge_coverage` 离屏目标时，测试编译入口在现存的非本切片测试源导入漂移处未完成（此前已知 `texture_row_alignment` 与 `SolidVertex` 缺失），本次没有把该超时升级为 shader failure，也没有生成 current-source Editor EXE。已有 `.codex/state/ui12-wgpu-visual-proof-*.png` 已人工查看并确认非空，但它们是旧 frozen proof，不能替代修正后 current-source 三档产品截图；M6 仍需等 RuntimeInterface `TransformSnapshot` 联合修复进入 HEAD 后重编、截图和 1000/1000/200 profile。

同轮 WGSL tool validation：使用缓存的 Naga validator 对当前 `ui_material.wgsl` 与 `screen_space_ui.wgsl` 实际执行 parse + validation，两份均输出 `WGSL_VALID`；该验证只确认 shader module 结构/类型/入口合法，不冒充 GPU 离屏像素回读。当前可信边界是：WGPU 生产库编译通过、两份 shader Naga 验证通过、静态 AA 契约通过；GPU fractional coverage 的离屏测试仍被测试目标中既有的两个缺失导入阻断，Editor 产品截图仍等待 RuntimeInterface 联合修复进入 HEAD。

同轮 offline visual-oracle validation：coverage predicate 修正后，`test_editor_ui_device_pixel_aa_contract` 与 `test_zircon_editor_ui_visual_oracle` 合计 61/61 通过；这覆盖 rounded-control fractional population、icon/text AA、fractional frame、required layout frames、profile geometry 与 capture manifest fail-closed 规则。该结果是离线 fixture/source contract 证据，不等同于 current-source Editor 窗口截图；后者仍需 TransformSnapshot 联合修复进入 HEAD 后重新构建。

本项目的适配矩阵据此固定为五条：一，`.zui` 继续使用 logical layout，presenter 以物理 client extent 作为 target/projection 下限，输入通过同一 logical/device transform 逆变换；二，圆角和分数边缘使用解析 SDF、`fwidth`、`smoothstep`，再做 16 个物理像素子样本的 4x4 局部 coverage，border 使用 outer-minus-inner coverage，fringe/scissor 只扩展 1px 并与父级 clip 求交；三，普通 draw list 与 native resize 均禁止生成低于当前物理 target 的中间 projection；四，“超分辨率”限定为局部矢量/图标/SVG 的 2x/4x 或 fragment 4x4 coverage，不引入会模糊文字、菜单和细线的整窗 TSR，Slate 的正常 UI 路径也不是该模型；五，工作台继续采用固定 chrome + 可滚动 body，popup 通过内容测量与可用边界 fitting 得到最终尺寸，6/8/12px token 分别承担紧凑控件、普通控件和面板层级，避免用全局大圆角掩盖像素质量问题。以上参考已转化为当前 WGPU/legacy shader、physical projection、popup shadow、滚动布局和 radius token 的 source-ready 实现；仍必须在 RuntimeInterface `TransformSnapshot` 联合修复进入 HEAD 后，执行 current-source Editor 三档窗口截图、DPI/GPU presenter 检查及 1000 click / 1000 pointer move / 200 resize profile，才能把 M6 从 pending 提升为 acceptance。

2026-08-30 workbench popup extent convergence：工作台窗口的 10 个产品菜单原先以固定高度声明，但共享 popup 行公式为 `padding 6px * 2 + row 28px * count + gap 2px * (count - 1)`，导致 4/5/7/8/10 行菜单分别短缺 6/6/12/12/12px，Render 三行也短缺 2px，首次绘制会压缩行高或裁掉底部。当前 `window_menu_state.rs` 在每次菜单打开时读取最新 `menu_items`，复用内容测量宽度、presentation scale、物理 client extent 与 `menu_popup_content_height` 更新节点约束，并在模块溢出菜单动态刷新后再测量；实例仍由同一 popup frame/hit authority 负责，未引入第二套布局或输入状态。`workbench_window.zui` 的初始高度同步为 160/130/130/160/250/220/220/250/100/310px，作为动态测量前的可用 floor；新增契约要求每个静态菜单的 authored 行数与高度至少覆盖共享公式，Run Mode native test 锁定布局缓存 130px。菜单契约 8/8、workbench layout/popup/action/interaction 相关契约 37/37、`zr_rhi` UI surface 21/21、两个本次 Rust 文件 direct rustfmt 与 scoped diff check 通过。仓库级 `cargo fmt` 仍被既有 Rust 2024 `let` chain 语法阻断，未启动 Editor Cargo；TransformSnapshot 联合修复尚未进入 HEAD，因此没有将该布局切片升级为 current-source WGPU 截图或 M6 acceptance。

2026-08-30 native fractional-control coverage：复核 host retained painter 后确认按钮、下拉和字段的几何入口仍使用 `inward_pixel_aligned_rect`，搜索字段还对居中位置和高度执行 `round()`；这会在已经完成 DPI/物理投影后的控件上二次量化，使圆角边缘和 fractional border 在 125%/150% DPI、滚动及动画中跳变。当前三类 geometry 与 search field 均保留 post-DPI `f32` frame，只应用显式 layout offset/紧凑搜索高度约束；最终 `PixelRect` 仍把扫描范围裁到物理 target，`rect_pixel_coverage`/rounded coverage 负责边缘透明度，分隔线等明确静态 snap 路径不变。新增 Rust geometry 回归锁定 fractional frame 与 offset，搜索回归锁定 fractional 居中；新增 Python pixel-snapping policy 检查 native control geometry 不得重新调用 inward snap/round。Python 6/6、受影响 8 个文件 direct rustfmt 通过；按 milestone policy 未启动 Editor Cargo，RuntimeInterface `TransformSnapshot` producer/consumer/test 联合仍未进入 HEAD，因此未宣称 current-source WGPU/Editor 截图、GPU 离屏回读或 M6 acceptance。

同轮 native rounded-surface parity：Material text field 与 Workbench filter chip 也移除 host painter 的整框 inward snap，改为保留 fractional frame，避免相邻输入/筛选控件在同一 DPI 下出现不同边缘质量；其显式 `paint_rect`/`chip_paint_rect` 仍保留有限面积检查，最终 surface/border 继续使用现有 rounded coverage。Python pixel-snapping policy 扩展至 6 个 native geometry 文件，Python 6/6、受影响 Rust 文件 direct rustfmt 与 scoped diff check 通过；静态 divider/timeline/preview snap 不变，未启动 Editor Cargo 或 current-source 截图。

2026-08-30 physical raster floor：参考 Unreal Slate 的 High-DPI 初始化、viewport extent 与可选 post-buffer downsample 后，补齐 UI 资源层的最低分辨率不变量。`UiRenderExtract::normalized_raster_scale` 与 screen-space renderer 的实际消费点现在都把任何有限正值钳制到至少 `1.0`：允许 `1.25/1.5/2.0` 的文字、SVG 和图片超采样，但禁止 `0.75` 等下采样值把分辨率相关资源降到物理客户区以下；逻辑布局、DPI presentation scale、普通 UI target 与命中坐标均保持原 authority。新增 interface regression 覆盖 undersample clamp 与 supersample preservation，runtime render regression 覆盖 native text 的实际 raster scale，device-pixel Python contract 同步锁定两层实现。该改动只影响资源光栅化输入，不引入整窗 TSR；`UiRenderFrameExtract` 的 owner-managed 归一化路径未改写，避免跨 owner 改写未提交的 frame publication union。当前仍未启动 Editor Cargo 或 current-source 截图，M6 acceptance pending。

同轮 lower-layer pixel readback validation：为解锁真实的视觉证据，修正 WGPU 测试目标中的两个缺失可见性导入：diagnostic readback tests 通过同级 `layout::texture_row_alignment`，UI native submission tests 显式导入 `SolidVertex`。随后精确执行圆角、分数方框边框、子像素平移、sRGB 线性光覆盖、透明边缘和提交顺序共 16 个 WGPU UI tests，结果 16/16 通过；其中圆角读回确认存在 `0 < alpha < 255` 的边缘像素，分数边框保持小于 1px 的覆盖，sRGB 用例隔离 1x1 导数边界后验证线性光编码。第一次 sRGB 失败 `[165,165,165,96]` 被追溯为 1x1 target 与 coverage derivative 叠加的测试构造问题，修正为覆盖范围大于 target 的实心 quad 后单测和整组均通过。新增物理 raster floor 的 interface/runtime source regression 与 device-pixel Python contract 通过；共享 `zircon_runtime_interface` Cargo 测试在另一个 owner 的长期 `zr_resource` 任务期间未取得回传，不把超时升级为失败。当前 HEAD 仍不含 RuntimeInterface `TransformSnapshot` 联合修复，因此没有启动 Editor Cargo、EXE 或三档产品截图，M6 acceptance 继续 pending。

同轮 shader-contract follow-up：WGSL 的 `rounded_box_alpha` 已统一为正向 `1.0 - smoothstep(-w/2, +w/2, distance)`，但 WGPU `pipeline.rs` 的几何 ABI 回归仍断言旧的反向 `smoothstep` 字符串；这会把正确的圆角 coverage 实现误报为失败。现已将回归断言改为匹配实际多行正向表达，且 `rustfmt --check`、scoped `git diff --check` 与设备像素/AA Python 契约 36/36 通过。该修正只同步测试与 shader 的既有契约，不改变绘制、布局、命中或颜色空间行为；没有重新启动共享 Cargo，也不把 source-level 通过升级为 current-source 产品截图验收。

同轮 SVG/icon raster-floor follow-up：文字资源已由 `UiRenderExtract`/screen-space consumer 禁止低于物理客户区的 raster scale，但 SVG/icon atlas 的 `icon_pixel_size` 仍会把 `dpi_scale = 0.75` 直接乘到默认尺寸，窄 DPI 下会生成低于目标分辨率的图标位图。现将有效 DPI scale 统一钳制到 `max(1.0)`，保留 `1.5/2.0` 等显式超采样；新增 icon-atlas 回归同时覆盖 undersample floor 与 supersample preservation，设备像素 Python 契约扩展检查该实现。Rustfmt、scoped diff check 与 Python 36/36 通过；当前 Runtime Cargo 正由其他 owner 占用，未启动竞争构建，Editor current-source EXE/截图/profile 仍等待 HEAD 合入 `TransformSnapshot` 联合修复。

同轮窄验证回传：WGPU `ui_surface::pipeline::tests` 在当前源码中实际执行并通过 5/5，覆盖 analytic shape ABI、linear/byte target entry-point 选择、Material helper 路由和 rounded coverage shader 入口。随后在共享 Cargo 空闲后尝试执行 Runtime icon-atlas 单测；命令受 1 秒调度窗口限制而超时，且未留下 cargo/rustc 进程，没有产生可归属的通过或失败结果。因此 icon floor 仍按 source-ready + 静态证据记录，不冒充 Runtime Cargo 通过。工作树 HEAD 仍缺 `WorldQueryResult::TransformSnapshot` 联合修复，Editor EXE、current-source 三档截图和交互 profile 继续 pending。

同轮 popup layout-authority correction：完整 Editor `.zui` 静态矩阵首次执行为 228/229，唯一 RED 是窗口菜单状态层在完成内容尺寸测量后直接修改节点约束并调用 `mark_layout_dirty`，使菜单状态再次成为布局失效的第二 authority。当前保留 `window_menu_state.rs` 对最新 `menu_items`、presentation scale 和可用 client extent 的内容测量，但把精确 fixed width/height 写入及 layout dirty 统一收敛到 `componentized_window/refresh_layout.rs::set_fixed_control_extent`；状态层只提交 `UiSize`，Runtime control-anchor 的定位、边界 fitting、frame publication 和 hit authority 不变。静态契约同时要求状态层不出现 retired geometry/dirty owner，并要求布局模块实际提供固定尺寸事务与 dirty 标记。聚焦回归 1/1、完整 Editor `.zui` 契约 229/229、两个 Rust 文件 direct `rustfmt --check` 与 scoped `git diff --check` 通过。当前 HEAD 仍未包含 RuntimeInterface 的 `TransformSnapshot` producer/consumer/all-variant regression 联合提交，因此本轮未启动 Editor Cargo，也未把 source-ready 修正升级为 current-source WGPU 产品视觉验收。

同轮 Inspector fixed-chrome/property-filter convergence：当前 `workbench_inspector_panel.zui` 原先把选中对象标题放进详情滚动体，且没有属性筛选入口；长属性列表滚动后对象身份会离开视野，也无法按字段语义缩小结果。参考 Unreal `SDetailsView.cpp` 的 selected-object/name area、property filter 与独立 scrollable detail tree，以及 MagicaVoxel `config/ui/editor.ui` 的固定标题/Tab chrome + scroll layer，现将 `WorkbenchInspectorTitle` 与新增 `WorkbenchInspectorFilter` 固定在滚动体上方，只有 metadata、Transform、组件属性、Add Component 与空结果提示进入纵向滚动内容。原 `Inspector/History` 双 Tab 只有互斥选中样式，没有 History 内容节点、可见性切换或数据投影；当前将其资产节点、binding、preview action、初始化 state 与 Rust 回归完整 hard cut，同时保留 `RightDrawerHeaderRoot` 作为无状态对象身份 header，避免伪入口并维持右抽屉布局 authority。搜索字段复用 `WorkbenchSearchInput`，Change/Submit 分别保留显式 binding/action identity；键盘编辑通过 `edit_inspector_filter`，清除按钮产生的 Change 通过 reference-menu action state，两者最终共享 `apply_inspector_filter`。bridge 新增完整 `inspector_source_properties` 快照，既有 `component_properties/component_property_keys` 只保存筛选后的虚拟行投影，因此查询切换不破坏原始反射数据、稳定 item key、滚动刷新或属性编辑 metadata。空查询恢复 metadata/Transform/组件/Add Component；非空查询按静态分组名、组件名及 field id/name/label/value kind 做 ASCII 大小写无关匹配，无结果时显示单一空态。专项契约完成预期 5 项 RED 后为 5/5 GREEN，完整 Editor `.zui` Python 契约为 234/234；9 个受影响 Rust 文件 direct `rustfmt --check`、专项 Python compile、退役 Inspector Tab 标识全仓 0 命中与 scoped `git diff --check` 通过。当前 HEAD 仍未包含 RuntimeInterface `TransformSnapshot` producer/consumer/all-variant regression 联合提交，因此没有启动 Editor Cargo、current-source 三档 WGPU 截图或 1000/1000/200 profile，本切片仍为 source-ready，M6 acceptance、milestone commit 与企业微信推送保持 pending。

同轮 Scene Tree fake-mode hard cut：`workbench_scene_tree_panel.zui` 的 `Scene/Layers` 双 Tab 与 Inspector 旧 Tab 一样，只在 `reference_menu_actions.rs` 中切换 selected/checked，下面始终是同一 search row 与同一 hierarchy virtual tree，没有 Layers 内容节点、数据源或可见性切换。Unreal `SceneOutliner/Private/SSceneOutliner.cpp:62-73` 将搜索框直接绑定 filter change/commit，`:193-250` 把搜索与工具动作放入固定 AutoHeight toolbar，`:273-305` 再以 FillHeight 承载真正的 tree view；MagicaVoxel `config/ui/editor.ui` 也只在存在真实模式内容时使用 `tab-l`，并把可变主体放入 `flex=1` scroll layer。因此当前以无状态 `scene_header` + `WorkbenchSectionTitle("Scene")` 替换伪 Tab 条，保留 `LeftDrawerHeaderRoot`、`LeftDrawerContentRoot`、Scene 搜索、Filter、Add、virtual row、选择 route 与滚动 authority；两个 Tab 的资产节点、binding、preview action、初始化/切换 state 和仅验证选中样式的 Rust 回归全部 hard cut。Scene 专项完成预期 2 项 RED 后为 2/2 GREEN，相关 live-state/layout 契约 52/52，完整 Editor `.zui` Python 契约为 236/236；Inspector + Scene 两组退役标识全仓 0 命中，Python compile、9 个受影响 Rust 文件 direct `rustfmt --check` 与 scoped `git diff --check` 通过。当前仍因 RuntimeInterface 联合修复未进入 HEAD 而不启动 Editor Cargo，也不宣称 current-source WGPU 视觉验收、profile、milestone commit 或企业微信推送完成。

同轮 core workspace fake-mode hard cut：10 个核心工作区原先各自声明 3 个内嵌 `WorkbenchTab`，共 30 个 `Rules/Tags/Simulation`、`Graph/Preview/Shader`、`Tree/Blackboard/Debugger` 等入口；但 Runtime 的唯一行为只是 `select_exclusive` 改写 selected/checked，所有 Tab 下方始终呈现同一列表、画布、字段和命令，没有独立 content stack、数据源或 visibility 投影。Unreal `MaterialEditor/Private/MaterialEditor.cpp:559-621` 的每个 `RegisterTabSpawner` 都绑定一个具体 `SpawnTab_*` 内容构造器；MagicaVoxel `config/ui/editor.ui:56-60`、`:128-132`、`:224-238` 的 `tab-l` 则通过 `view = [[...]]` 切换真实 view 集合，二者都不把 Tab 当作装饰性选中状态。当前 10 份核心 `.zui` 全部保留固定 `WorkbenchSectionTitle`，其后直接进入真实列表或 `ScrollableBox`；Gameplay 的长侧栏仍保持 title 固定、scroll body 可达。同步 hard cut `module_navigation` 的 10 组 panel-tab 常量/映射、初始化默认值、dispatch 分支、30 个 template binding、preview registry 项及只验证伪 Tab 的 Rust/Python 回归，顶层模块切换、行选择、字段编辑和命令 route 不变。专项契约先得到 15 个预期 RED，修正后 2/2 GREEN；相关结构/live-state/scroll 契约 39/39，完整 Editor `.zui` Python 契约 237/237，退役 core panel-tab action/control 标识为 0，Python compile、7 个 Rust 文件 direct `rustfmt --check` 与 scoped `git diff --check` 通过。当前仍不启动 Editor Cargo 或宣称 current-source WGPU 视觉/profile acceptance，milestone commit 与企业微信推送继续等待统一 M6 产品验收。

同轮 extension workspace fake-mode hard cut：44 个扩展工作区各自声明 3 个 `WorkbenchTab`，共 132 个标签，但 43 个工作区只把标签直接混排在同一侧栏内容前，Blend Space 仅多一层纯标签容器；所有工作区都没有按标签拆分的 content stack、数据源或 visibility 投影，Runtime 行为也只有 `select_exclusive` 改写 selected/checked。该结构不满足 Unreal `MaterialEditor/Private/MaterialEditor.cpp:559-621` 的“每个 Tab spawner 对应具体 `SpawnTab_*` 内容”关系，也不满足 MagicaVoxel `config/ui/editor.ui:56-60`、`:128-132`、`:224-238` 中 `tab-l` 通过 `view = [[...]]` 切换真实 view 集合的语义。当前 44 份扩展 `.zui` 已 hard cut 132 个伪标签及唯一空 `blend_space_tabs` 容器，保留搜索、列表、画布、字段、命令和滚动填充；同步移除 44 组 navigation spec tab metadata、132 个 template binding、132 个 preview action、默认标签初始化与 dispatch 分支，扩展工作区打开、行选择、字段编辑和命令反馈不变。专项契约先得到 69 个预期 RED，修正后 2/2 GREEN；live-state 契约 29/29，完整 Editor `.zui` Python 契约 239/239，退役扩展 tab asset/import/action/control 标识全为 0，preview registry 从 888 精确收敛为 756，Python compile、受影响 Rust 文件 direct `rustfmt --check` 与 scoped `git diff --check` 通过。RuntimeInterface `TransformSnapshot` producer/consumer/all-variant regression 联合仍只存在于共享脏工作树而未进入当前 HEAD，因此本轮未启动 Editor Cargo、current-source WGPU 产品截图或 1000/1000/200 profile，也未提交 milestone 或推送企业微信。

同轮 remaining-tab authority audit：移除核心与扩展伪标签后，Editor 资产仅剩 27 个 `WorkbenchTab`，分布在 5 个文档。顶栏 11 个标签映射到 11 个独立模块工作区；Generated Bottom 5 个模式映射到各自 route/content projection；Component Drawer 的 Components/Console 两个标签显式切换两个独立 body；Asset Browser/Assets Activity 的 6 个 utility 标签进入 typed `AssetUtilityTab` 状态与对应内容投影。Component Lab 的 3 个 `Tab 1/2/3` 位于明确的组件样例卡内，并由 tab painter、geometry 与选择交互回归作为 canonical specimen，而不是产品内容导航，因此保留。该审计拒绝机械清零所有 Tab，只 hard cut 缺少内容或样例语义的入口；同一轮对非滚动纵向容器复扫后，Welcome/Inspector/Assets preview/Material Component Lab 候选的固定内容高度与 stretch fill 均可达，没有再发现新的产品裁切路径。

同轮 Dropdown display/state authority convergence：产品 Workbench 资产中最后 79 个 extension Dropdown 与 Blend Space 的 2 个 Dropdown 已从 painter fallback `text` 硬切到 `value_text`，全产品扫描不再存在静态 Dropdown `text`。为避免选择后把人类可读标签退回机器 ID，retained host 新增唯一结构化 option parser，投影与 popup selection 共同消费 `id|label=...`；选择事务只把 machine ID 写入 `value`/`special_options`，把 display label 写入 `value_text`。17 个原先 `value != value_text` 的产品 Dropdown 已补齐结构化 options，Perception config 同步补齐真实候选。Ability 的 Name/Net Policy/Cooldown 从 profile navigation 重置中分离，Playtest 反馈读取三个 live 值；Render compile、HUD preview 与 Material compile 分别读取当前 Platform、Screen、Domain/Blend，不再回显固定 fixture。专项契约先覆盖 RED，再以完整 Editor `.zui` Python 矩阵 287/287、Python compile、10 个受影响 Rust 文件 direct `rustfmt --check` 与 scoped `git diff --check` 收口。RuntimeInterface `TransformSnapshot` producer/consumer/all-variant 联合仍未进入 HEAD；依照外部 owner 门禁，本轮没有启动 Editor Cargo，也没有把 source-ready 的状态逻辑升级为 current-source WGPU 截图、1000/1000/200 profile 或 M6 acceptance。

同轮 core editable-command convergence：结构化解析十个核心 workspace 中所有带 Change/Submit 的 `WorkbenchField`、`WorkbenchDropdown` 与 `WorkbenchSearchInput`，确认旧实现仍存在“可编辑但执行命令只读取 profile fixture”的分裂。当前 Effect Apply 读取 Name/Tag/Magnitude/Policy/Stack，并把对象身份、属性预览和执行摘要分层投影；Render Compile 读取 Pipeline/Platform/Frame，Material Compile 读取 Domain/Blend/Preview Mesh，VFX Simulate 读取 System/Bounds/Sort，HUD Preview 读取 Screen/DPI/Locale。Behavior Validate 改为读取 Blackboard/Controller/State，Assets Import 读取 Type/Path/Owner，Tags Add/Rename 读取 Owner/Redirect，Perception Simulate 读取 Config/LOS/Team；Ability Playtest 延续上一段的 Name/Net Policy/Cooldown live-state 读取。固定 `profile.applied_output`、Behavior `profile.validation` 与仅服务旧导入反馈的 `selected_asset()` 已删除，搜索和 Dropdown 仍通过既有常量 owner 读取，不建立第二套 command state。执行反馈按 Unreal 式对象标题、参数视图与状态摘要分区，没有把所有值拼进单个窄状态栏。九组专项契约先获得预期 RED，最终核心可编辑控件复扫无未归属 control ID，完整 Editor `.zui` Python 矩阵 296/296、Python compile 与 10 个 workspace state 文件 direct `rustfmt --check` 通过；Cargo、current-source WGPU 截图和 profile 仍受 RuntimeInterface HEAD 联合门禁约束，M6 acceptance 保持 pending。

2026-08-30 extension live-state and Details convergence：42 个可写 extension workspace 的 command feedback 不再只返回静态 fixture；低频命令路径按 canonical `.edit` route 从 retained host projection 读取当前 `value_text`，并保留静态反馈作为无输入、导航与只读模块的 fallback。对应 126 个 Field/Dropdown 全部由现有 `WorkbenchPropertyEditorRow` 承载，形成 Unreal Details 风格的稳定 Name/Value 双栏；标签从唯一 `.edit` 字段段派生，原 control ID、状态、选项、Change/Submit route 与尺寸约束保持不变。属性名进入左列后，Pose Library、Lighting Bake、Post Process 与 Shader Editor 的 6 个 Dropdown 删除右列的同名重复前缀，当前 79 个 extension Dropdown 的最长选项为 17 个字符，适合约 200px 的 value column。Blend Space 搜索栏保留其专用搜索布局，UI Asset Editor workspace 继续执行永久 scan-only 边界；迁移前后两份排除资产 SHA-256 均未变化。触发链静态复核覆盖 637 个 authored event，未发现缺失 Rust template binding；297 个可点击行均同时注册 row action 与 exclusive group，86 个命令按钮全部归一为 `.invoke` 并有 feedback owner，Blend Space Preview/Apply 继续由其 contextual owner 处理。专项契约先获得预期 RED，随后验证恰好 42 个 workspace / 126 个控件、原控件只由对应 value slot 承载且 value/option 不重复左列标签；248 个 Editor `.zui` 全部 TOML 解析通过，完整 Editor `.zui` Python 矩阵 299/299、Python compile、受影响 Rust 文件 direct `rustfmt --check` 与 scoped `git diff --check` 通过。RuntimeInterface `TransformSnapshot` 联合仍未进入 HEAD，本批没有启动 Cargo；current-source WGPU 三档产品截图、实际抗锯齿视觉判断与 1000/1000/200 profile 仍是 M6 的硬门禁。

同轮 compact Module Details layout convergence：900px 物理客户区属于 Regular tier，旧契约却把 53 个可写 module workspace 的右侧 Details 全部限定为 Wide，造成属性虽然已收敛为 Name/Value 双栏，却在标准窗口宽度下完全不可达；直接把 320px Details 恢复进普通横排又会把中心编辑区压缩到不可用宽度。参考 `dev/UnrealEngine/Engine/Source/Editor/LevelEditor/Private/SLevelEditor.cpp::SummonDetailsPanel` 以 `SDockTab`/drawer close lifecycle 承载 Details，以及 `SLevelViewport.cpp::OnToggleSidebarTabs` 通过 TabManager 切换 sidebar 的做法，当前 53 个非保护 workspace 根节点统一改为 Overlay：Wide 时主横排中的等宽 reserve 保留原三栏中心预算，真实 Details 作为右对齐 overlay 精确覆盖 reserve；Narrow/Regular 时 reserve 折叠，工具栏只在模块工作区激活时显示 `WorkbenchModuleDetailsDrawerToggle`，同一 retained action 在右侧打开/关闭 Details overlay，不把其重新塞回中心布局；Ultra 不暴露该入口，Scene workspace 也不会显示无目标按钮。响应式投影新增 min/max tier 与 compact drawer role，状态只由 bridge 的 `compact_module_details_drawer_open` 持有；toolbar priority 在计算内容宽度前消费最终可见性，避免第三个 layout 图标溢出。UI Asset Editor workspace 继续永久 scan-only，当前 SHA-256 保持 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。专项契约 3/3、完整 Editor `.zui` Python 矩阵 302/302、当前 253 个 Editor `.zui` TOML 解析、Python compile、受影响 Rust direct `rustfmt --check` 与 scoped `git diff --check` 均通过；Rust 行为回归已覆盖 Regular 初始隐藏、点击开关、Scene/Effect 往返、Wide 常驻和 Ultra 隐藏，但尚未由 Cargo 执行。RuntimeInterface `TransformSnapshot` producer/consumer/all-variant 联合仍未进入当前 HEAD，因此没有启动 Editor Cargo，也未把本段 source-ready 结果升级为 current-source WGPU 三档截图、真实视觉判断或 1000/1000/200 profile acceptance。

同轮 native visual interaction acceptance preparation：`capture-editor-ui-visual.ps1` 在原 640x520、900x620、1672x941 三档 GPU 主截图之外，为 900x620 Regular 工作区增加三组 source-bound 交互补图。脚本只使用同一进程首次发布的 profile geometry 定位稳定控件：真实 Win32 pointer move/down/up 点击 `WorkbenchToolbarMenu`，保存主菜单打开态并以同一控件二次点击关闭，锚点下方局部像素必须显著变化后恢复；hover `WorkbenchModuleDetailsDrawerToggle` 350ms 跨过共享 150ms summon delay 与 100ms intro，截图时保留指针位置，随后移到无 tooltip 的客户区原点并要求局部像素恢复；最后真实点击同一 Details 控件并保存右侧抽屉打开态。对应文件为 `editor-900x620-main-menu.png`、`editor-900x620-main-menu-dismissed.png`、`editor-900x620-module-details-tooltip.png`、`editor-900x620-module-details-tooltip-dismissed.png` 与 `editor-900x620-module-details.png`，每条可见/关闭变化不足都会 fail closed。补图记录明确标注 `source_geometry_scope = pre_interaction_trigger_only`，不会把交互前 geometry 冒充交互后布局证据，也不进入三张主图的像素 oracle。交互 helper 已纳入 source fingerprint；focused Pester 10/10、视觉 oracle 26/26、Python compile、`pwsh` 与 Windows PowerShell 5.1 parser/helper、scoped `git diff --check` 均通过。以上只是 current-source 产品验收基础设施；当前 HEAD `cc5cadbd597c3707954ebd6109fad0fd5643a152` 的 `item_count.rs` 仍为 0 个 `TransformSnapshot` arm，因此尚未启动 Editor Cargo、生成这些产品截图或执行 1000/1000/200 profile，M6 继续 pending。

2026-08-31 dock stencil completeness correction：`workbench_dock_header.zui` 只提供 3 个 authored tab slot，旧 fallback 判定却只检查 `DockTab0`，因此第 4 个及后续 live document/floating tab 会被模板静默丢弃，既没有绘制也没有 hit frame。当前判定先为投影建立一次 `ControlFrameIndex`，再检查 `0..tabs.row_count()` 的每个 live slot；任一缺失即切换既有动态 dock 投影。回归使用 4 个 closeable document tabs，锁定 `DockTab3` 与 `DockTabClose3` 均存在、宽度可读且 close frame 位于 tab 内。focused host-chrome source contract 7/7、Rustfmt、Python compile 与 scoped diff check 通过；Rust 行为回归因共享 `zircon_runtime` 102-error build blocker 尚未由 Cargo 执行。该修复只消除 authored stencil 的静默截断，不伪造无行为的 dock overflow glyph；窄宽隐藏列表必须在后续完整复用 popup、keyboard、scroll、hit-routing authority 后才能接受。

同轮 dock compact-before-overflow correction：authored stencil 即使包含 live tab，也可能把其 frame 投影到标题栏横向边界之外；fallback document/bottom/floating 布局又始终按完整标题宽度线性排布，典型 420px document lane 的第 3/4 个标签仍会越界。当前 fallback 判定在同一个 `ControlFrameIndex` 上同时检查 slot 存在性与 bar 横向包含关系；一旦缺失或越界，document/bottom/floating 与 side dock 共用一个自适应槽位分配器。活动标签优先获得测量后的完整标题宽度，非活动标签先保留 shared dense control height 对应的图标点击区，再按剩余空间逐个恢复完整标题与 close target；图标态不投影文字或悬空 close frame。420px / 4 个长标题回归锁定活动标题可读、三个非活动标签为稳定图标宽度、所有 frame 位于右侧 inset 内，另有 synthetic authored-frame 越界回归。focused host-chrome 8/8、完整 Editor `.zui` 静态矩阵 323/323、direct Rustfmt 与 scoped diff check 通过；Rust 回归仍因共享 Runtime 编译错误只达到 source-ready。该切片覆盖常见窄栏的四标签可达性，但不宣称解决任意数量标签；真正隐藏的标签仍须后续复用 popup、keyboard、scroll 与 hit-routing authority 的完整 dock overflow 行为。

同轮 native rounded-surface fractional geometry convergence：Workbench 的 Button/Dropdown/Field/Chip 已保留 post-DPI `f32` frame，但 Badge overlay、Axis value field、Command Palette、Dialog、Notification Center、Alert 与 Tooltip 仍在进入共享 rounded fill/border/shadow primitive 前对整个物理 frame 执行 `round/ceil/floor`。该二次量化不会增加曲线采样质量，反而会让 125%/150% DPI、滚动或过渡中的圆角控件按整像素跳动，并造成同一 coverage authority 接收两种尺度语义。当前七类生产入口保留分数位置和尺寸，只继续执行既有有限值、最小尺寸、可见性与 clip 检查；误导性的 `pixel_aligned_rect` 入口改名为 `paint_rect`，文字原点和明确静态 divider 的像素对齐不变。Rust source-ready 回归分别锁定 Badge、Axis、Command Palette、Dialog、Notification、Alert 与 Tooltip 的 fractional frame；设备像素/AA contract 新增全路径禁用 geometry quantizer 的守卫。验证结果为 device-pixel/AA 37/37、完整 Editor UI 104/104、完整 Editor `.zui` 323/323、direct Rustfmt 与 scoped diff check 通过；受保护 UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。共享 `zircon_runtime` 的 102-error current-source build 仍未清零，因此没有把本段 source-ready 结果冒充当前产品截图、WGPU 窗口验收或 M6 acceptance。

同轮 native text raster-scale read-only audit：当前 ScreenSpace text request 明确以 `logical_px * raster_scale` 生成 Swash 物理字号桶，并保留水平/垂直 subpixel phase，因此它不是把固定 1x 字形位图直接放大。后续 source projection 又以字形 bitmap 的 `width/height` 直接生成 atlas `screen_rect`；与此同时 interface contract 声明 render command 坐标保持 logical，而 Editor Workbench host 会在 mount 时先把 frame/font metrics 投影为 physical。由于 `native_glyph_run.rs` 当前未跟踪，`native_bitmap_atlas/source_image.rs`、`frame.rs` 与 `UiSurface::current_raster_scale` 也属于共享未提交 Text/Runtime union，本计划不凭静态歧义修改其尺度公式。M6 在取得 current-source EXE 后必须用 100%/125%/150%/200% 的同一文本样本核对声明 frame、glyph bitmap bucket、最终屏幕 bounding box 与像素覆盖；在该证据前只确认“按 DPI 重栅格化存在”，不宣称 text scale/锐度链已完成视觉验收。

同轮 native dock compact-tab tooltip single-authority closure：fallback Dock header 的紧凑标签仍属于宿主原生 chrome projection，但 pointer move 现在复用一次 `route_top_level_chrome` 的既有 `ChromePointerRoute` 命中结果，再以 `DocumentTab/DrawerHeaderTab` 的 `surface_key + index` 从同一批已发布 `tab_frames` 做 O(1) overlay-aware lookup，生成稳定 identity、完整标题与物理 absolute frame；tooltip 解析模块本身不包含第二份 `contains`/hit-test。事件回调由裸 `Option<UiNodeId>` 收敛为 `SurfaceNode | HostChrome` 显式目标，组件化 surface 路径只包裹其原节点，native capture、菜单与 overflow occlusion 继续返回无候选。`workbench_window.zui` 新增一个真实、visible 但完全不可命中的 `WorkbenchHostChromeTooltipAnchor`，bridge 按 mount origin 与 presentation scale 将原生物理 frame 逆投影一次并更新该节点的 position/fixed extent；popup 仍通过 `set_popup_control_anchor` 消费 Runtime arranged geometry，candidate owner/anchor control 与每个 tab 的 identity 分离，切换同 anchor 下的不同标签会重新触发共享 150ms summon delay 与 100ms intro。没有新增 host timer/deadline，leave/press/scroll/focus-loss/menu occlusion 继续走既有 `UiInputManager`/Workbench lifecycle。静态契约先对缺失模块与旧 `Option<UiNodeId>` 获得预期 RED，修正后聚焦 6/6、完整 Editor `.zui` 342/342、Editor UI 106/106、253/253 `.zui` TOML、候选分配性能 5/5、direct Rustfmt 与 scoped diff check 通过；source-ready Rust 回归覆盖 document/side drawer frame、物理到逻辑只转换一次，以及真实 ZUI anchor 在 149.999ms 隐藏、150ms 由 Runtime manager 显示。一次 current-source `build-editor.ps1 -Ephemeral` 在 Cargo 前被协调器 `request_overloaded` 拒绝，没有 Rust 诊断、EXE 或 artifact；因此本段只完成 source-ready 触发/布局 authority，仍不得替代 current-source 三档 WGPU 产品截图与 1000/1000/200 profile acceptance。

同轮 SVG search-glyph fractional-origin convergence：图标资源已经按目标物理尺寸执行 4x/2x 局部矢量超采样，GPU command stream 保留 `f32` image frame，软件回放也按像素中心到分数矩形做双线性映射；但 Search field 的唯一图标几何入口仍把 post-DPI `x/y` 四舍五入，导致 125%/150% DPI、分数滚动或动画时图标以整像素跳动。当前仅移除该入口的两次 `.round()`，保持 SVG 尺寸、clip、field hit geometry、clear action 和文本基线不变，让现有超采样位图在 GPU/软件路径中使用同一分数目标。专项契约先按预期 RED，修正后 focused 1/1、完整 device-pixel/AA 38/38、direct Rustfmt 与 scoped diff check 通过；Rust 回归锁定分数 field frame 产生分数 icon origin，尚未由 Cargo 执行。

同轮 current-source Editor retry gate：上次 managed Editor build 的四个代表性 shared Runtime blocker 在本次只读复核中仍保持相同源码形态：`final_output_resource_schema` 的 const bitflags `|`、`roll_prev_transforms_after_success` 在多处可变借用实参期间读取 `self.stats()`、material prepare 对非 `mut` runtime 写 `readiness_report`，以及 platform host install 将 `snapshot` 移入 state 后再次返回。四个路径均为外部修改/未跟踪 owner，本计划不改写；由于失败指纹没有实质变化，本轮不重复占用 managed Cargo lane，也不把 source-ready UI 契约升级为 current-source EXE、截图或 profile 证据。

同轮 fragment coverage current-state correction：计划中保留的 `physical-fragment 2x2 coverage` 段落描述的是首次从单中心样本升级到四子样本的历史阶段，不再代表当前 shader。现有 `ui_material.wgsl` 与 legacy `screen_space_ui.wgsl` 均声明 16 个 `sample_offsets`，以 `0.0625` 汇总为 4x4 物理局部 coverage；device-pixel/AA 契约同时锁定两条实现。最终适配矩阵已据此改为 fragment 4x4，并继续区分它与 SVG/图标临时源的 2x/4x raster supersampling；两者都不是整窗 TSR。

同轮 Dock overflow single-authority closure：fallback Dock allocator 现在仅在确有零宽隐藏标签时为专用 `DockTabOverflow` 控件预留稳定槽位，并由 document、left、right、bottom 与 floating 五类 scene projection 各自发布一个 `overflow_frame`；隐藏集合只从同一批已发布 `tab_frames` 推导，不扫描模板树，也不复制第二份 tab 列表。宿主状态只保存 open、`surface_key`、hovered index 与 bounded scroll offset；统一的 `HostDockOverflowProjection` 负责解析 owner、全局 anchor、隐藏行、document/drawer activation kind 和 popup geometry，pointer press/move/scroll、keyboard navigation/accept/escape、paint、outside dismiss 与 focus-loss cleanup 均消费这一权威。打开另一 Dock/Page overflow trigger 时允许单击切换，打开 Dock popup 会请求完整帧重绘，行激活继续复用既有 document-tab 或 drawer-header action，不复制业务选择逻辑；left Dock route 同时修正为包含 activity rail 的全局原点。Rust source regressions锁定专用槽位、发布 anchor、隐藏索引、左轨偏移、滚动上界与 routing；Python source contract 4/4、宿主 chrome contract 合计 13/13、完整 Editor `.zui` 328/328、Editor UI 105/105、direct Rustfmt 与 scoped diff check 均通过，永久 scan-only UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。共享 `zircon_runtime` 的 102-error current-source build 指纹未发生实质变化，因此这些 Rust 行为回归尚未由 Cargo 执行；当前也没有 Editor EXE、640x520/900x620/1672x941 WGPU 产品截图或 1000/1000/200 profile，本段只接受 source-ready 的布局与交互权威收口，M6 继续 pending。

同轮 Blend Space short-height reachability：结构化扫描确认 writable module 中仍有一个长纵向 clip-only 容器：Blend Space 的 wide preview card 在固定 header 之外直接堆叠 164–216px preview viewport、divider、heatmap header、64–86px heatmap 与 Asset 行；同一 center 又常驻 176–204px bottom composite row，因此 `900x620` 目标档存在后半内容被永久裁掉的确定风险。Unreal `SAnimationBlendSpace.cpp` 将主 BlendSpace grid 放在 `NonScrollEditorPanels` 的 `FillHeight` 区，故本次不滚动主 sample canvas；参考其主工作区稳定原则及 MagicaVoxel side layer 的独立 vertical scroll，只把 preview header 之外的次级 viewport/heatmap/asset 内容放入 `ScrollableBox/Vertical/Auto`，并删除滚动内容中的无界 stretch fill。原 control ID、preview/heatmap 数据与命令 route 不变。新契约先准确 RED 于旧七子项直排，修正后 side-panel 7/7、extension density/state/passive-output 7/7、完整 Editor `.zui` 329/329、Editor UI 105/105、248 个 Editor 资产 TOML 解析、Python compile 与 scoped diff check 通过；永久 scan-only UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。该切片尚未由 current-source `900x620` WGPU 产品帧确认，M6 视觉与 profile 验收继续 pending。

同轮 active-task status budget：状态栏在任务隐藏时可满足 900px 目标，但任务进度展开后，Ready、Errors、Warnings、Messages、task、Grid/Snap、三个 24px 图标、Zoom 与左右 8px padding 的最小宽度合计为 906px，必然在中档物理客户区产生挤压或裁剪。当前仅把可伸缩 `WorkbenchStatusReady` 的 min 从 160px 收敛为 128px，保留 160px preferred 与 Stretch；active-task 最小预算降为 874px，留下 26px 余量，所有右侧状态/命令 identity、responsive tier、任务进度宽度和触发 route 不变。预算测试直接解析 `editor_tokens.zui` 的 status height 与 padding token，先在旧 min 上 RED，修正后状态栏 5/5、Workbench layout 21/21、完整 Editor `.zui` 330/330、Editor UI 105/105、Python compile 与 scoped diff check 通过；永久 scan-only UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。该 26px 余量仍须由 current-source `900x620` WGPU 帧和 active-task profile 实际确认。

同轮 Welcome high-DPI reachability correction：此前 `WelcomeMainPanel` 只以 `VerticalBox + clip` 压缩展示区；在 `640x520` 物理客户区与 150% DPI 下，可用 logical 高度不足以容纳项目字段、验证、Create/Open 和四个 startup 入口，底部交互会被永久裁掉且没有滚轮到达路径。当前主任务列改为接收输入的纵向 `ScrollableBox`，使用 `Auto` scrollbar 和同一 clip authority；删除滚动内容中的无界 `preview_bottom_fill`，保留已有宽度权重、任务顺序、control ID、字段绑定和 route。常规高度不产生滚动条，短高度或高 DPI 下则优先保持控件尺寸并提供滚动可达性。新契约先在旧 `VerticalBox` 上准确 RED，修正后 focused 1/1、Welcome 12/12、产品交互 9/9、完整 Editor `.zui` 330/330、Editor UI 105/105、253/253 Editor `.zui` TOML 解析、Python compile 与 scoped diff check 均通过；永久 scan-only UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。该切片修复的是结构性可达性，仍须由 current-source `640x520`、`900x620` 与 150% DPI WGPU 产品帧确认滚动条、绘制裁剪和滚轮命中体验。

同轮 Welcome Ultra-width authority：`640px / 150% DPI = 426.7 logical px`，而 recent 136px、main 280px 与两侧 16px 安全边距的最小预算为 448px，因此旧双栏即使纵向可滚动仍会横向溢出。当前 recent history 通过 `responsive_min_tier = "narrow"` 在 Ultra 档折叠，项目创建主任务保留至少 280px；这与 Unreal 在窄 viewport 优先主任务、将次级 history/sidebar 折叠的布局策略一致。修复同时收口 native authority：权威资产布局缺少 recent frame 时，painter 不再用 legacy 220–320px fallback 复活侧栏；recent pointer/profile viewport 返回零面积，避免不可见列继续命中。只有无当前资产布局的 legacy caller 保留 fallback。专项契约先在缺失 responsive props 上 RED，修正后 focused 1/1、Welcome 13/13、完整 Editor `.zui` 331/331、Editor UI 105/105、253/253 Editor `.zui` TOML 解析、direct Rustfmt、Python compile 与 scoped diff check 均通过；永久 scan-only UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。Rust 行为回归已覆盖 authoritative collapsed 与 legacy fallback 两类语义，但尚未由 Cargo 执行；current-source 150% DPI 产品截图仍是最终验收门槛。

同轮 Generated Bottom Ultra command budget：通用 bottom header 的标题 160px、模式 strip、Filter 120px、Open 76px、Pin 68px 与 4 个 8px gap 形成 456px 最小预算；在 `640px / 150% DPI` 扣除 34px activity rail 后只有约 392.7 logical px。当前仅让非交互 `Generated Bottom Panels` 上下文标题在 Ultra 档折叠，五个可横向滚动的模式、Filter、Open 与 Pin 全部保留；Ultra 最小预算降为 288px，命令 control ID、event route、header 顺序和内层 mode-scroll authority 不变。预算契约先在缺失 responsive boundary 上 RED，修正后专项 8/8、完整 Editor `.zui` 331/331、Editor UI 105/105、253/253 Editor `.zui` TOML 解析、Python compile 与 scoped diff check 均通过；永久 scan-only UI Asset Editor workspace SHA-256 仍为 `1F092C929EB6D7DE66E3684A4A2BD8A3335FD1358E37E073657FD487F4523335`。该 source-ready 预算仍需 current-source 150% DPI WGPU 产品帧确认。

同轮 Asset Browser utility-tab Ultra budget：底部工具行的 Preview、References、Metadata、Plugins 四个命令标签最小宽度为 316px，右侧非交互 selection locator 另占 156px，加上 5 个 4px gap 后 authored 最小预算为 492px，超过 `640px / 150% DPI` 扣除 34px activity rail 后约 392.7px 的可用逻辑宽度。当前仅让 locator 在 Ultra 档折叠，四个可交互标签、节点顺序、control ID、Change route 与 spacer authority 全部保留；Ultra 最小预算降为 332px。预算契约先准确 RED 于缺失 `responsive_min_tier`，修正后 Asset Browser 专项 8/8、完整 Editor `.zui` 331/331、Editor UI 105/105、当前 248/248 Editor `.zui` TOML 解析通过。该结果只证明 source-ready 的命令可达预算；locator 折叠、四标签绘制与实际命中仍须由 current-source `640x520`、150% DPI WGPU 产品帧确认。

同轮 Material Component Lab responsive shell：Lab 的 AppBar 四个固定 chip 最小预算为 480px，Drawer 220px 与 Interaction Contract side panel 280px 使 body 最小预算达到 524px，均超过 420px Ultra 工作区。当前 Ultra 保留可伸缩产品名、MUI 范围、prototype 数量和完整原型内容，折叠两个纯状态 chip 及两块静态辅助栏；Narrow 的 640px 预算恢复完整 AppBar 与三栏。AppBar Ultra 最小预算为 228px，body 内容区最小预算为 0px。纵向原型 `ScrollableBox` 同时补齐 `input_hoverable + Receive + clip`，让滚轮命中、裁剪和 Auto scrollbar 共用显式 authority。专项先对响应式边界与滚轮输入得到 2 项 RED，修正后 2/2、完整 Editor `.zui` 333/333、Editor UI 105/105、当前 248/248 Editor `.zui` TOML 解析通过。该 Lab 改造仍需 current-source WGPU 产品帧确认实际 chip 折叠、滚轮和 150% DPI 曲线质量。

同轮 workbench preview token parity：设计预览此前仍以旧蓝黑 surface 和 teal 强调色作为独立视觉权威，与当前 `.zui` 的 `#151515/#242424/#2f2f2f/#383838` 中性层级、`#60aeff` 蓝色强调以及 6/8/12px 圆角层级发生漂移。当前 CSS foundation 直接投影 `editor_tokens.zui`，交互文案、fixture token 与内联状态色同步硬切到蓝色/中性灰语义；新增回归拒绝旧 teal 与旧蓝黑 foundation 重新进入预览，manifest 汇总图也复用同一 surface/text/accent 变量。视觉 verifier 也从 `tealRatio` 改为可识别 dimmed overlay 状态的 `blueAccentRatio`，并把暗色工作台亮度上限收敛为 average luma 60、bright pixel ratio 0.12；dark pixel ratio 0.82 与色彩多样性门槛继续保留，避免通过提升可读性退化成亮色或单色界面。完整导出后 271/271 张 1672x941 PNG 通过 provenance、尺寸、视觉密度、蓝色强调与输出清单校验；token parity 5/5、Editor `.zui` 333/333、Editor UI 105/105、248/248 Editor `.zui` TOML 解析通过。本段 PNG 是 HTML 设计基线，用于让后续产品截图有明确比较对象，不替代 current-source Editor EXE、WGPU 三档帧或 1000/1000/200 profile 验收。

同轮 Material Lab palette authority convergence：`material_component_lab.zui` 的响应式壳虽已收口，但其 40 个 shell/card/nav/meta/state stylesheet rule 仍维护一套独立蓝黑/teal hex 调色板，导致同一 `.zui` 产品在 Workbench token 更新后继续显示旧 surface、focus 与 selected 语义。当前 Lab stylesheet 已删除全部私有 hex 色值，统一引用 `$editor.surface.*`、`$editor.text.*`、`$editor.accent*`、`$editor.focus.ring`、separator/border 与 semantic container；共享 `editor_material.zui` 的 pressed、selected、accent-soft、popup、track、outline-variant 和 on-primary 角色也与 Editor palette 对齐，其中可直接表达的角色使用 `$theme.palette.*`，accent-soft/selected/track 由 parity 回归锁定到当前 authored token。新契约先对旧私有色和旧 material interaction roles 准确 RED，修正后聚焦 7/7、完整 Editor `.zui` 335/335、248/248 Editor `.zui` 加共享 `editor_material.zui` TOML 解析与 scoped diff check 通过。该切片消除了资产层双重颜色权威；真实 Material Lab 的 focus ring、圆角边缘和滚动体验仍须 current-source WGPU 产品帧验收。

同轮 Component Drawer Ultra gallery budget：底部组件抽屉虽然已有纵向 `ScrollableBox`，但两列 Masonry 的七张 sample card 仍以 168–260px 固定宽度参与布局，最坏两列预算为 `2*260+8=528px`；Table 卡固定 590px，两个 feedback 列固定 390px，均超过 420px Ultra 窗口扣除 34px activity rail 后 386px 的逻辑内容宽度。当前七张卡统一允许收缩到不高于 188px 并保持原 preferred/max，两列预算精确闭合为 384px；Table 改为 `320/590/590 + Stretch`；alerts/toast 列复用共享 notification panel 的 `280/320/380 + Stretch` token。表格四列数据、反馈内容、全部 control/event/route、Masonry 两列和纵向滚动 authority 均未改变。预算回归分别在旧 590px Table 与旧 260px sample card 上准确 RED，修正后完整 Workbench layout 21/21；全仓 writable Editor 产品 `.zui` 数字最小宽度扫描仅余 Viewport 场景内部的 440px lightwash 装饰节点，不再发现超过 Ultra 内容宽度的 UI chrome/control 容器。该 source-ready 预算仍需 current-source 640x520、150% DPI WGPU 帧确认表格文字和双列卡片在收缩后的真实可读性。

同轮 Material Lab micro-typography correction：74 个 prototype 文件仍各自保留元数据与状态胶囊的 `font_size = 8.0` authored fallback，共 888 个标签；结构化扫描确认它们全部属于 `.material-lab-meta-chip` 或 `.material-lab-state-pill`。产品 Lab 根 stylesheet 现在由这两个共享 class 统一投影 `$editor.typography.caption.size`，当前 caption 为 10.666667 logical px，结合 1.2 line-height 得到约 12.8px 行框，仍完整落在所有相关标签不小于 16px 的高度内。pill 几何继续由 `$editor.control.radius.pill` 覆盖，不批量改写 74 份原型或制造第二份字号常量。专项合同先在两个 class 缺失 `font_size` 时准确 RED，修正后 focused 5/5、完整 Editor `.zui` 343/343、Editor UI 106/106、248/248 Editor `.zui` TOML 解析与 scoped diff check 通过。该结果修复 Lab 可读性权威，但仍须由 current-source WGPU 产品帧确认 glyph 清晰度、胶囊内边距与 100%/125%/150%/200% DPI 的真实像素覆盖。

同轮 raw-radius semantic audit：全量生产 `.zui` 的 `corner_radius = 0..5` 命中仅来自两类合法语义。`workbench_viewport_panel.zui` 的 64 处命中全部属于墙面、门框、货箱、灯带、选择轮廓、坐标轴与 gizmo 的场景示意几何，不是 Editor chrome；Material Lab prototype 的剩余命中属于 segmented control 中间段和 standard underline text field 的显式直角样例。真实 Workbench 按钮、输入、面板、列表与 pill 均消费 `editor.control.radius.small/control/large/panel/pill = 6/8/10/12/999`。因此不对场景几何或语义直角执行全局半径放大；用户感知的硬边继续由物理像素覆盖与一次混合修复，而不是用 16/20px 圆角掩盖底层锯齿。

同轮 rounded fill-border single-coverage closure：`zr_rhi_wgpu` 只在最终排序后的 fill/border 互为 draw-order 邻居且 frame/clip/radius 完全一致、颜色可见、border width 有限且为正时，才把两条源命令融合为一个 analytic item。新增回归明确允许邻接命令跨不同 z 值融合，同时在中间插入任意绘制项后要求保留三项，禁止未来退化成按源索引跨层查找。shader 在一个 fragment 输出中以 `inner coverage` 和 `outer-inner coverage` 分配 fill/border 预乘颜色；retained damage cache 通过按源命令索引发布的 bit-vector 在 O(1) 路径恢复一 draw-item/draw-call 统计，不重新扫描 geometry。不同 fill/border 颜色的离屏读回、相同颜色不重复覆盖、geometry mismatch 与 cache-hit 统计回归均已 source-ready；device-pixel/AA 静态合同当前 40/40、单文件 Rustfmt 与 scoped diff check 通过，仍待受管 WGPU 测试及 current-source Editor 产品帧确认真实边缘。

同轮 retained Host text raster authority correction：并行 Text union 已把 Editor painter 内固定 `TEXT_RASTER_SUPERSAMPLE = 8` 的旧 owner 删除，字体栅格改为 `logical_px * surface_scale_factor` 量化到物理 ppem，按物理字号选择 Swash hinting，并让每个 `CachedGlyphRaster` 自带 `sample_scale`；普通 Swash/fontdue Host raster 明确使用 `NATIVE_RASTER_SAMPLE_SCALE = 1.0`，避免 8x 缩小对紧凑笔画的额外模糊，而 runtime artifact 仍可携带自身采样比例。UI12 不改写该 foreign Text union，只把自己的 AA 静态合同从旧常量改为锁定物理 ppem、hinting、native sample scale 与 painter 消费 `raster.sample_scale`。更新后 AA 合同 40/40、Python compile、单文件 Rustfmt 与 scoped diff check 通过；M6 仍须以 100%/125%/150%/200% current-source WGPU 帧验证 glyph bucket、屏幕 bounding box 与实际清晰度。

同轮 legacy screen-space rounded-box single-coverage convergence：场景渲染器此前对同一 `UiRenderCommand` 先生成圆角 fill 六顶点，再生成 border 六顶点，二者在完全相同的外轮廓上分别执行 alpha blending；这会让抗锯齿边缘重复 source-over，形成比 fill 或 border 本身更亮、更硬的像素环。当前 planner 在 fill 与 border 同时存在时改为调用一次 `push_rounded_box`，以一张六顶点 analytic geometry 同时携带 border color 与 fill color；`screen_space_ui.wgsl` 只计算一次 outer/inner 4x4 coverage，再以 `inner` 和 `outer-inner` 分区。该 legacy 管线仍使用 straight-alpha blending，因此 shader 先按 coverage 形成加权 premultiplied RGB，再除以合成 alpha 输出 straight RGB；fill-only 与 border-only 路径继续复用原有 primitive。计划级回归锁定组合面板只有六顶点、分数 border width/radius/颜色 ABI，device-pixel/AA 静态合同同时锁定 location 6、单次 geometry 与 straight-alpha 还原；WGSL 已通过本地 Naga parse/validate，尚待受管 Rust 测试及 current-source Editor 产品帧确认真实边缘。

同轮 Settings/Section Title post-DPI geometry convergence：全量量化扫描进一步发现两个仍在共享 painter 之前改变整个产品控件 frame 的入口。Settings Window 根面板把 `x/y/width/height` 全部 `round()` 后才生成面板、内部布局和 popup bounds；Workbench Section Title 则先以 `ceil/floor` 向内收缩整行，再生成 header surface、图标和文字。二者都不是 1px divider、虚拟列表索引或最终像素循环，而会让 125%/150% DPI 下同一 arranged geometry 被二次量化。当前两条路径直接消费已验证的 post-DPI `FrameRect`，最终 raster/clip 继续由共享 primitive 离散化；Settings 的 12px panel radius、Section Title 的 command-capacity 优化及 control identity 均保留。静态合同先在旧量化入口准确 RED，修复后增加两条 Rust source-ready 回归，以 `.25/.5/.75` frame 断言首个 surface command 四个分量原样保留；AA 40/40、Editor UI 109/109、Editor `.zui` 343/343、离线视觉 oracle 26/26、Rustfmt 与 scoped diff check 通过，仍待受管 Cargo 与 current-source WGPU 产品帧确认。

同轮 current-source visual/profile acceptance correction：native visual capture 已经以受管构建 receipt 的 Editor/Runtime/source SHA-256、产品 bundle 资产指纹、Windows per-monitor DPI 和实际物理 client extent 为准，并会在 `640x520`、`900x620`、`1672x941` 三个物理尺寸各启动一个 GPU presenter 进程；捕获合约 10/10、受控输出路径 3/3 通过。完整 profile 合约同时暴露旧验收仍读取 `ui.workbench.pointer.feedback_refresh_count`，而当前产品已把事件回调改为只登记 dirty、帧提交统一重建并发布 `feedback_deferred_count`。验收工具现硬切到后者，与 `activation_coalesced_refresh_count` 互补计算每事务至多一次 commit，既不恢复事件阶段即时 rebuild，也不让 1000 次 pointer 输入因废弃计数器误判；PowerShell parse、scoped diff check 与完整 profile 合约 48/48 通过。上述结果只证明当前 EXE 到位后可执行 source-bound 截图和 1000 clicks / 1000 pointer moves / 200 resizes 验收，不替代受管 Cargo、真实 WGPU 像素和性能结果。
