# Component Standardization Output / Performance Handoffs

- 来源计划：[`../15-component-standardization-from-primitives.md`](../15-component-standardization-from-primitives.md)
- 归档说明：本文件无损保存父计划迁出的 28 条具体产出、验证与跨计划交接记录；failure/fixed lifecycle 文件仍以各自原路径为唯一事实来源。

## 迁入记录
- `implemented_static / validation_pending`（2026-08-13，S15.5 visual-test owner convergence）：按结构规范 R1.4/R4.2-R4.3，将资产占位视觉测试从生产 root 迁入 `template_asset_placeholder_visuals/tests.rs`，root 保留生产绘制与唯一 `#[cfg(test)] mod tests` 挂载；将 Button 的 primary/danger/outlined/state-layer/pressed-selected 视觉测试迁入 `template_buttons_tests/paint/surface_state.rs`，其余 paint owner 保留同一 `mod surface_state` 发现边界。资产 root/test leaf 现为 339/453 行，Button paint root/state leaf 为 661/215 行；与 `HEAD` 的测试函数集合静态等价（资产 39、Button 29），child discovery、目标 Rust 2024 rustfmt 与自有 scoped diff 均通过。独立静态复审 P0-P2 为 0。未运行受管 Cargo，本记录不把源码等价检查当作测试执行结果。
- `implemented_static / validation_pending`（2026-08-13，S15.5 Sample Grid theme/metric refinement）：Unreal 11px Key、共享 palette/metrics、Runtime Text 标签测量、紧凑 plot clip 与顶部轴文字布局的当前事实和复核结果统一记录在 [`2026-08-13-sample-grid-theme-metrics-output-record.md`](2026-08-13-sample-grid-theme-metrics-output-record.md)。早期把 21px `PreviewSize` 误作 selected halo 的描述已作废；本切片未运行受管 Cargo、未生成或宣称新截图，仍为 `validation_pending`。
- 已修复交接（`fixed / 2026-07-11`）：[`Editor 01/fixed-2026-07-11-editor-m1-zui-governance.md`](../../editor/01/fixed-2026-07-11-editor-m1-zui-governance.md)；审查后 ZUI governance 为 72/72，24 个 builtin registry 键及直接消费者已硬切 locator，alias 机制已删除，功能计划只保留回链与摘要。
- 待修复交接（`open / 2026-07-12`）：[`Physics 03/failure-2026-07-12-collider-shape-consumer-exhaustiveness.md`](../../editor/01/fixed-2026-07-12-collider-shape-consumer-exhaustiveness.md)；Physics 03 M2-T1 新增五个 `ColliderShape` 变体后，Runtime project I/O、property access 与 render post-process 四处 match 尚未穷尽，当前源码 Editor Windows 构建在截图测试前被 E0004 阻断。
- accepted 当前源验收：[material-state-priority-convergence](2026-07-16-material-state-priority-convergence.md)
- `implemented / validation_pending`（2026-07-17，S15.5）：Workbench `Play` 与 `Run Mode` 已从 full-only 控件改为 640/900/1260 三档常驻，RunGroup 宽度由实际可见子项 preferred width 与容器 gap 推导；`Layout`/`Theme` 只在完整命令内容满足可用宽度预算时显示，不再依赖固定 1440px 阈值。focused Cargo、640 截图和独立复审等待 FIFO 与 Runtime Text01 双 lock 漂移关闭后执行。
- `implemented_static / validation_pending`（2026-07-19，S15.5q）：顶部运行区已随真实 `EditorSessionMode` 在 success Play 与 error Stop 间互斥切换；Stop 复用 canonical ExitPlayMode 绑定并保持与 Play 相同槽位几何，focused 契约测试源码已落盘。当前静态 rustfmt/diff/模板语义/重复 ID/≤800 owner 门通过；受管 Cargo、运行中截图与独立复审仍未计通过。
- `implemented_static / validation_pending`（2026-07-19，S15.5r）：顶部 30px preview-only New 槽已硬切为真实 Assets 入口，复用现有 Asset Browser 打开事件/视图链并保持紧凑档内容预算不变；focused 契约测试源码已落盘。当前静态 rustfmt/diff/旧 control 零残留/≤800 owner 门通过；受管 Cargo、当前源码截图与独立复审仍未计通过。
- `implemented_static / validation_pending`（2026-07-19，S15.4pr/S15.6oq）：状态栏任务进度已从模板/运行时四套局部 RGB 收敛为 semantic tone → 中央 Material Feedback palette；Warning tone 与无颜色 override 契约源码已落盘。当前静态 rustfmt/diff/任务 RGB 零残留/≤800 owner 门通过；受管 Cargo、当前源码截图与独立复审仍未计通过。
- `implemented_static / validation_pending`（2026-07-19，S15.5s）：Workbench 主菜单 Asset Browser/Open/Save/Command Palette 已接入真实业务 binding，旧 New Asset preview 文本删除；Asset Browser 端到端 journal/effects 契约和四项 binding 契约源码已落盘。当前静态 rustfmt/diff/旧菜单文本零残留/≤800 owner 门通过；受管 Cargo、当前源码截图与独立复审仍未计通过。
- `implemented_static / validation_pending`（2026-07-19，S15.5t）：Workbench 主菜单已从权威 `creation_templates` 投影动态创建行,点击时读取最新选择目录并调用既有资产 creation operation；菜单高度由共享 popup 内容度量与实际窗口可用高度推导,同名模板 action 可确定性消歧。当前静态 rustfmt/diff/显式 VM 构造点/≤800 owner 门通过,投影与端到端 operation 契约源码已落盘；受管 Cargo、当前源码菜单截图与独立复审仍未计通过。
- `implemented_static / validation_pending`（2026-08-01，S15.5y）：Workbench Run Mode 的 Play In Editor/Simulate 已接入 typed menu action、事件日志、`PlaySessionController` 下一次运行偏好与真实 `PlayStartRequest`;choice popup 会移动唯一 checked 标记，未实现的 Standalone/Network Preview 明确 disabled。当前目标 rustfmt/scoped diff/旧宿主硬编码/owner 规模静态门通过，端到端 Simulate 会话契约源码已落盘；受管 Cargo、当前源码截图与 accepted closeout 未计通过，本切片未生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5z）：Workbench Layout popup 的 Reset 行已接入 canonical layout reset 业务链，Gameplay/Rendering 在注册预设投影接入前明确 disabled，Default Layout 保持状态指示；端到端源码契约锁住 disabled 零 journal mutation、真实 reset effects，以及 surface/host projection 双层 command 状态恢复。目标 rustfmt/scoped diff/owner 规模门通过，独立二次审查 P0-P2 为 0；受管 Cargo 与当前源码截图尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5aa）：Console 空 body 已替换为稳定 id 的真实 Runtime Text code `Label` 行；共享 18px 行模型同时拥有逻辑行拆分、内容 extent 与 painter 可见行区间，`console_scroll_px` 进入 retained interaction state 并只平移可见输出行，overflow 复用共享 Starship 垂直滚动条且 fit 内容不画轨道，旧字符宽度/猜测换行高度 owner 已硬删除。第一次独立审查发现滚轮回调曾以外层 pane 高度计算上限；已前向修复为滚动命中、callback local frame、裁剪与 scrollbar 共用 `ConsoleOutputPaintMetadata.viewport`，近 overflow 和长输出都能到达精确末端。二次审查又发现真实 Focus 节点未进入 Workbench IconButton 专用 painter、浮动 Console 非输出区域滚轮可能穿透到底层 dock；已分别通过真实 workbench 组件身份和浮动路由 `Miss/Occluded/Routed` 三态前向修复，限域复核 P0-P2 为 0 findings。目标 Rust 2024 rustfmt/scoped diff/旧 owner 零残留/owner 规模与模块路径静态门通过；受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.4ps/S15.6or）：模板 `icon_placement` 已贯穿 runtime projection、retained DTO 与 Workbench Button 内容 painter；`icon_only` 保留 authored label 供语义/交互使用，但不绘制可见标签，也不再为不存在的标签保留图标 gap。Console Focus 使用右侧弹性 spacer、28px icon-only 槽和相对输出 inset，不依赖绝对 x 定位，并显式声明 workbench component variant；真实投影回归经完整 command dispatcher 锁住 28px 槽内居中的 16px asset icon、零文本命令和保留的语义标签。Rust 2024 rustfmt、673 个 `TemplatePaneNodeData` 构造点穷尽审计、scoped diff 与二次复核门通过；受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ab）：Console 已从单条 `status_line` 升级为 `EditorState` 单一写入口拥有的 256 条有界真实状态历史；空白和连续重复消息不入队，预计算 `Arc<str>` 输出随 `EditorDataSnapshot`/`EditorChromeSnapshot` 传播到模板 payload、native projection、scene projection、滚动 extent 和 callback 重算，状态栏仍只消费最新状态。owner 单元回归锁住容量、顺序与无效更新零重建，状态级回归锁住最新状态和历史尾部同步；所有手工快照夹具已补齐内部一致性。目标 Rust 2024 rustfmt、全树 14 个 `EditorDataSnapshot` + 8 个 `EditorChromeSnapshot` 构造穷尽检查、中央 setter 静态门与 scoped diff 通过，二次独立审查 P0-P2 为 0 findings；受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ac）：Console header 已加入 28px workbench icon-only Clear 按钮，复用现有 `clear-logs.svg` 并保留 `Clear Console` 语义标签；`ConsolePaneBody/ClearConsole` 通过内建 `view.console.clear` operation、typed `MenuAction::ClearConsole` 与非 undoable execution 清空中央 history owner，不替换状态栏最新文本。重复清空为 no-op，清空后相同状态可重新入队；真实模板投影回归锁住 28px 槽内 16px asset icon、零文本命令和 binding id，端到端绑定回归锁住 presentation invalidation 且不请求 layout。二次独立审查只发现 `runtime/mod.rs` 两组 import 未满足 Rust 2024 排序的 P2 门禁，已限域前向修复并以 `skip_children=true` 复核通过，`runtime/integration.rs` 保持零差异；最终功能范围无剩余 P0-P2。目标 Rust 2024 rustfmt、命令/operation/binding/枚举穷尽静态门、scoped diff 与 target/docs screenshot 零写入检查通过；受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ad）：Console 滚动宿主已加入显式 follow-tail 策略：初次溢出和原本位于尾部时随真实历史增长移动到新末端，用户上滚后保持阅读位置，重新滚回末端后恢复跟随；Clear 或内容收缩时从 pointer bridge 回读夹取后的状态，不再把过期偏移写回 pane surface。最大偏移由 `ScrollSurfacePointerLayout` 单源计算并同时供 bridge clamp 与宿主策略使用，Inspector/Asset Browser 保持原有 position-preserving 同步。二次独立审查发现 production recompute 在首次 scroll callback 前没有初始化 Console surface 的 P1，已前向修复为：已应用 presentation 后通过窄借用 host query 从 `ConsoleOutputPaintMetadata.viewport` 初始化尺寸，优先 callback source floating/native window，再回退 local docks/其他 floating surface，全程不克隆整份 presentation；真实 `ChildWindowHostHarness` 回归锁住“零指针输入的首次溢出即跟随末端”。源码回归覆盖跟随、脱离、恢复、收缩夹取和首屏初始化；目标 Rust 2024 rustfmt、所有权/调用点扫描、scoped diff 与 target/docs screenshot 零写入检查通过，窄范围独立复审确认重算顺序、窗口选择、零分配借用和首屏回归均成立，P0-P2 为 0 findings。受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ae）：Console logical-line owner 已从 `Vec<&str>` 物化切为借用 iterator，真实 Runtime Text 行 projection 直接消费该 iterator；独立 `console_output_line_count(...)` 以换行字节扫描提供 extent/metadata 行数，空输出、CRLF、尾随换行语义保持，不再在 pointer layout 同步或 retained 节点 projection 前额外分配引用表。源码回归覆盖空/单行/尾随换行行数和既有逻辑行顺序；目标 Rust 2024 rustfmt、旧 Vec 签名零残留、调用点穷尽和 scoped diff 静态门通过，二次独立审查确认 iterator 生命周期、`Vec::splice` 消费边界、空输出/CRLF/尾随换行计数均正确，P0-P2 为 0 findings。受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5af，已被 S15.5ao 前向替代）：该阶段曾把 Console-local `Focus Console` 从误表达日志信息的 `log-info.svg` 切到 Unreal-style `focus-selection.svg` 并保持原响应链；S15.5ao 对照 Validation Log 复合控件后已移除该局部按钮，pane focus 回归工作台 chrome owner，本段仅保留历史记录，不再描述当前模板。
- `implemented_static / validation_pending`（2026-08-01，S15.5ag）：Console 输出已从裸 `Arc<str>` 收敛为单一 `ConsoleOutputSnapshot`，同一对象共享持有预计算文本和逐逻辑行 `Info/Warning/Error`，避免可错位的外部平行字段；`EditorConsoleHistory` 在容量/去重/clear 的同一 owner 内同步重建两者，多行消息、尾随换行与同文不同级别保持一一对应。默认状态入口保持 Info，权威 EditorEvent 执行失败写 Error、被容忍的缺失动画目标写 Warning；`ConsolePanePayload` 复用文本与级别两个 `Arc`，Runtime Text projection 将三档映射为中央 `secondary/warning/error` semantic tone，空输出仍为 muted。独立二次审查先后发现 builder/TOML 双重全文物化、retained `ConsolePaneData.status_text: String` 再复制全文、scene/pane projection 在真实模板主路径前 eager 构造 legacy 字符串和节点三项 P2；已前向修复为 builder 仅 `text_arc()`、只在 TOML `Value::String` owned 边界物化一次、`ConsolePaneViewData`/`ConsolePaneData` 全链共享同一 `Arc<str>`，并从 scene projection 删除 eager legacy nodes，仅在 template runtime 确实失败的 `unwrap_or_else` 中按 content size 惰性构造。源码契约锁住 Arc allocation identity、主路径零 legacy `console_pane_nodes`、builder 零 `to_string()` 与真实 template-runtime count attributes。源码回归覆盖 string snapshot 行数、history 多行级别、EditorState 传播、真实模板行 tone 和 legacy fallback；目标 Rust 2024 rustfmt、19 个快照构造/3 个权威传播点穷尽、scoped diff 与 target/docs screenshot 零写入检查通过；P2 前向修复窄复审确认 P0-P2 为 0 findings。受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ah）：`ConsoleOutputSnapshot` 已在唯一构造边界预计算 `ConsoleOutputLevelCounts`，不在每次 pane projection 重扫级别；Console header 新增稳定 id 的 142px 相对 HorizontalGroup，投影 `All` 与 Error/Warning/Info 三个真实计数，分别消费中央 accent/error/warning/info Runtime Text tone。控件宽度为 40/30/30/30、内部 4px gap，header 剩余宽度继续由弹性 spacer 吸收，Clear/Focus 28px 槽及 binding 不变。独立二次审查发现 TOML count attributes 缺少真实 runtime projection 断言的 P2，已前向补齐 info/warning/error 三项 `Value::Integer` 回归；目标 Rust 2024 rustfmt、唯一 payload 构造、template id、scoped diff 与 target/docs screenshot 零写入检查通过。受管 Cargo、当前源码截图、P2 修复窄复审与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ai）：Console header 的 All/Error/Warning/Info 已从被动徽标升级为真实分段 Button，并注册 `view.console.filter.*` canonical operations、typed `MenuAction::SetConsoleMessageFilter`、非 undoable host execution 和四个 builtin template bindings。筛选状态由拥有 256 条全量历史的 `EditorConsoleHistory` 单源持有：可见 Runtime Text 正文、逻辑行 metadata、滚动 extent 与 selected/checked 视觉态同步切换，总计数始终反映全量历史；重复选择同一筛选为 no-op 且保留文本 Arc，Clear 保留筛选条件，切回 All 可无损恢复历史。源码回归覆盖 owner 筛选/clear/计数、EditorState snapshot、四档 menu action roundtrip、真实 template Button/binding/选中态和 builtin 点击恢复；目标 Rust 2024 rustfmt、四个模板事件/四个 binding/四个稳定 control id、命令/枚举穷尽、scoped diff 与 target/docs screenshot 零写入检查通过；独立二次审查确认完整命令链 P0-P2 为 0 findings。受管 Cargo、当前源码截图与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5aj）：对照 `ai-workbench-style/ai-blend-space-layout.png` 的 Validation Log，Console 四档筛选从纯文字/数字段深化为语义图标 + `All`/数量：复用仓库现有 `filter-logs.svg`、`log-error.svg`、`log-warning.svg`、`log-info.svg`，统一 16px leading icon、caption Runtime Text、4px radius/1px border 和中央 accent/error/warning/info tone。组宽由 142px 调整为 214px（58 + 48×3 + 4×3）；320px header 仍保留弹性 spacer，不使用绝对定位。独立二次审查发现通用 Button 12px 横向 padding + 7px icon gap 会让 48px 段只剩 1px 数字空间、58px `All` 只剩 11px 文本空间的 P1；已前向修复为可复用 `compact_icon_text` authored 组件变体，并由共享宿主度量统一消费 `gap_s` 作为 4px 横向 padding 与 4px icon gap，不按 Console control id 特判。窄复审确认文本度量与实际绘制 frame 回归成立，又发现通用变体分类器暂置于 `tab_like.rs` 的职责归属 P2；已继续前向迁入独立 `workbench_button/component_variant.rs` owner，`tab_like.rs` 零通用变体残留。真实模板回归提升到绘制命令层，锁住资源 locator、leading placement、16px 实际 icon、每段恰好一个实际 text command、至少 8/16px 可读文本 frame、4px 图文间距、按钮内边界与 320px 无重叠；资源存在、目标 Rust 2024 rustfmt、静态变体/owner 契约、scoped diff 与 target/docs screenshot 零写入检查通过。受管 Cargo、当前源码截图、P2 owner 修复最终窄复审与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ak）：继续对照同一 Validation Log 参考，`Clear` 从顶部筛选行左侧的 28px icon-only 槽移到日志内容下方的 28px footer 右侧；该阶段顶部保留 214px 筛选组、弹性 spacer 与 28px Focus，随后 S15.5ao 已将顶部收敛为唯一自适应筛选组。footer/body/header 由根 VerticalGroup 与两个 HorizontalGroup 相对分配，未引入绝对坐标；真实模板回归锁住 footer 节点、右对齐/纵向顺序与唯一 clear binding。该切片最初落地的 64px 局部 compact icon+text 叶样式随后已由 S15.5am 硬切为共享 WorkbenchButton，不作为当前合同保留。目标 Rust 2024 rustfmt、模板节点/绑定唯一性、scoped diff 与 target/docs screenshot 零写入检查通过。受管 Cargo、当前源码截图、独立二次审查与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5al）：Console 非空日志行已从“整行仅换 tone”深化为参考图同类的结构化 Runtime Text 复合行：固定 64px severity `Label` 显示 `[Info]`/`[Warning]`/`[Error]` 并消费 secondary/warning/error tone，右侧弹性 message `Label` 保持 secondary 正文；空态仍为单一全宽 `No output yet`，不伪造级别。`ConsoleOutputPaintMetadata` 显式拥有 `nodes_per_line`，content extent 继续按逻辑行而非节点数计算，可见区一次返回同一逻辑行的 severity/message 两节点，projector 对二者施加相同滚动与 clip，header/footer 保持固定；零节点组被拒绝，截断模型只消费完整节点组。源码回归覆盖单节点兼容、双节点可见区、截断夹取、复合 projector 同步滚动、真实 severity/message DTO 与两条实际 Runtime Text command 的无重叠 frame；目标 Rust 2024 rustfmt、复合行静态契约、scoped diff 与 target/docs screenshot 零写入检查通过。受管 Cargo、当前源码截图、独立二次审查与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5am）：Console 四个筛选段与 footer Clear 已从 host view 的裸 Button + 局部 surface/radius/border 复刻硬切为导入共享 `WorkbenchButton` 原子。筛选段只覆写 compact icon+text、caption Runtime Text、semantic tone/validation level、计数与选中态；Clear 与现有 `WorkbenchValidationLog` 组合规范一致，使用 text-only outlined secondary 和 50/58/66 响应宽度，不再维护 `clear-logs.svg`、局部 64px 槽或独立交互 chrome。真实模板回归锁住五个展开后的 Button role、四档 validation level/text variant、Clear outlined/58px preferred frame、唯一 binding、零 image command 与可读 Runtime Text frame；静态门锁住一个 canonical import、五个 WorkbenchButton 实例和旧局部 button styling 零残留。目标 Rust 2024 rustfmt、scoped diff 与 target/docs screenshot 零写入检查通过。受管 Cargo、当前源码截图、独立二次审查与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5an）：参考图实际消费的共享 `WorkbenchValidationLog` 也已从 `E 0/W 1/I 2` 文字占位切到 `filter-logs/log-error/log-warning/log-info` 四个真实语义图标、`code compact_icon_text` 共享内容度量、caption Runtime Text 与 accent/error/warning/info tone。为适配 Blend Space 最窄 212px 面板，All 使用 48/54/58、三档计数使用 42/46/48 min/preferred/max，四个 4px gap 后 preferred 总量为 208px，剩余与宽屏增量由 stretch fill 吸收；Clear 继续复用 text-only outlined secondary 50/58/66 合同。契约回归位于现有 `blend_space_workspace/composite_contracts.rs` owner，主截图编排文件未新增复合控件责任；静态门锁住四个变体/资源、响应宽度、旧字母占位零残留和资源存在。二次审查发现四处实例级 `font_size` 与该复合层既有零字体覆写契约确定冲突的 P1，已移除局部声明，并让共享 `compact_icon_text` 按钮内容度量消费中央 `HostControlMetrics.font_small`；Console 同类四段也同步回归单源。目标 Rust 2024 rustfmt、scoped diff 与 target/docs screenshot 零写入检查通过。受管 Cargo、当前源码截图、P1 修复最终窄复审与 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented_static / validation_pending`（2026-08-01，S15.5ao）：二次审查发现 Console header 在小于 254px 时会因固定筛选组 + spacer + Focus 溢出，且 256 条消息上限可被单条 8,000 行消息绕过并物化约 16,000 个 Runtime Text 节点。已按最低 owner 前向修复：模板移除参考复合控件中不存在的局部 Focus/Spacer，筛选组和按钮改为 186/204/214、48/54/58、42/46/48 min/preferred/max，相同 186px 下限进入 Console autolayout constraint；severity 最大 64px 并为 message 保留至少 32px。容量常量提升到 `ConsoleOutputSnapshot` 数据契约，history 按最近 256 个逻辑行精确裁剪，快照构造边界再次防御直入来源，因此投影上限稳定为 256 个 message + 256 个 severity 节点。追加审查发现 partial trim 恰好留下空尾行时旧修复会额外丢掉该行和级别的 P2；当前改为保留空 message entry，快照用 `levels` presence 区分空历史/一条空行，projection 对后者仍生成 severity + 空 message 两节点。触及的 1082 行 `pane_body_documents.rs` 已把四个独立资产契约迁到 203 行 folder-backed leaf，根 owner 降至 884 行；迁移时补齐 Clear binding 的 `EditorOperation` namespace 分支，测试函数名保持不变。最终 hard-cut 扫描删除 legacy fallback 中已无对应节点的 `FocusConsole` no-op 标记和 orphan builtin binding，并用源契约锁住生产 `rs/zui` 零残留。回归覆盖 220/240px 真实模板边界、跨消息裁剪/级别计数、空尾行 presence、300 行单消息和 8,000 行快照直入；目标 Rust 2024 rustfmt、静态模板/旧常量/旧 Focus、测试 owner 规模、scoped diff 与 target/docs screenshot 零写入检查通过。P1/P2 前向修复独立复审已确认 P0-P2 为 0；最终 residual 窄复审已发起且不等待。受管 Cargo、当前源码截图和 accepted closeout 尚未计通过，本切片不生成截图。
- `implemented / validation_pending`（2026-07-17，S15.4/S15.5）：Welcome 已将 Project name → Location → Validation → Open/Create 业务链前置，将 Hero/Status/Preview/开发启动入口后置；TextField 收敛为 44 高/4px 圆角，字段标签改为 9px muted `Label` 并走共享 Runtime Text，按钮收敛为 32 高/4px 圆角，内容采用 28 slot padding + 680 max width。Recent 列改为 220/320/320 弹性约束、项目主列声明 280 最小宽，补充 560×384 实际 640 窗口内容区回归并优先主业务栏；Recent 顶部节奏收敛为 18/46/8，native fallback 同步且 list 高度改为相对 panel bottom。Welcome 专属层序硬切为 native foundation → shared template controls，重复的 native `actions.rs`、`form/field.rs` 及 action-only 色常量已删除，标准控件绘制/状态/命中回到通用模板 owner；560×384、640×420、900×520、1260×780 回归、独立 29 行 640 截图 route 和模块文档已落盘，focused Cargo 与 `docs/tests/editor` 截图仍待执行，不声明 accepted。
