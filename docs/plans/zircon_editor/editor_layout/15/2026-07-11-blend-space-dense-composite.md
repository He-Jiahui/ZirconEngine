---
record_kind: milestone_slice
plan: docs/plans/zircon_editor/editor_layout/15-component-standardization-from-primitives.md
milestone: S15.5
slice: blend-space-dense-adaptive-composite
status: workbench-status-bar-validation-blocked-external
date: 2026-07-11
---

# Layout 15：Blend Space 密集自适应复合界面

## 状态

`Blend Space 当前源码原生绘制/三档响应式几何/真实窗口截图基线已通过，视觉逼近继续进行`。Editor M1 ZUI 治理交接已修复并迁移为 [`fixed-2026-07-11-editor-m1-zui-governance.md`](../../editor/01/fixed-2026-07-11-editor-m1-zui-governance.md)；共享注册表、SearchField DTO、父链投影、唯一活动 extension workspace painter、通用 `responsive_min_tier` 和细分隔线绘制均有最低层回归。640×520、900×620 与 1260×780 当前源码截图只写入 `docs/tests/editor`。这代表 S15.5 的响应式复合基线完成，不代表 Blend Space 已与 Unreal/参考图视觉等价；整体 Editor Layout Goal 继续 active。

## 已完成实现

- 以 `workbench_extension_blend_space_workspace.zui` 为唯一生产资产；保留扩展模块契约要求的横向 rail/left/center/right 根结构，并在 center 内组合编辑区与底部 Blend Output。
- 保留 72 px activity rail 关系和既有 20 个交互事件；不新增兼容 shim，不扩散 action/control owner。
- 左侧使用标准 tab、search、list、tree、divider；中心使用相对伸缩 sample grid 与 preview card；右侧使用 property row、field、dropdown；底部使用标准 table rows 组合输出日志。
- 左/右栏、预览卡和固定内容按 `min/preferred/max + Fixed` 声明稳定尺寸，中心、填充槽与等分 tab 只声明 `Stretch` 并接收剩余空间；生产资产没有绝对坐标或截图专用像素定位。
- 视觉节奏收敛为 28-32 px 控件、4/6 px gap、1 px border、4 px corner radius，并移除旧 6 px 大圆角与按钮局部颜色覆写。
- 新增叶子测试 `visual_screenshot/blend_space_workspace.rs`，避免继续扩大约 2200 行的父测试文件；测试覆盖组件结构、900/1260 自适应几何及真实生产窗口截图导出路径。

## TDD 与验证证据

1. RED 源码契约先确认缺少 `WorkbenchSearchInput`、body、sample canvas 与 output panel。
2. 实现后同一源码契约转绿；staged `.zui` validator 通过，要求的标准组件与相对 stretch 结构均存在。
3. 原型/生产扩展模块契约验证通过：44 个模块、880 个事件均 routed/unique/bound，Blend Space 保持 rail/left/center/right 根结构与既有事件身份。`verify-web-native-handoff.mjs` 仍有 2 个既有 generated-bottom-panel 失败，均不涉及本切片。
4. 首轮 Windows 受管聚焦测试完成新 editor test binary 构建并执行 3 项：源码结构 `1 passed`，截图测试 `1 ignored`，自适应测试因测试 harness 通过 action-id 反查 opener 失败而 `1 failed`。随后已把 harness 修正为点击真实 `WorkbenchAbilityBlendSpaceButton` + `UiEventKind::Click`。
5. 修正后的受管复跑在共享 Runtime 当前源码编译阶段失败，最低失败层为 `zircon_runtime/src/graphics/scene/gpu_scene/prev_skinned_palette.rs` 调用仅对 `scene_renderer::mesh` 可见的 `JointPaletteStorage::create_buffer`，报告 `E0624 x2`。该路径不属于 Layout 15 租约，不能在编辑器布局切片中越层修复。
6. Editor M1 fixed handoff 向上复验首次发现本资产违反两项当前治理合同：27 个 `Stretch` 轴同时携带显式尺寸约束，以及预览字符文本首尾空白不满足 Inspector trim-stable 序列化。修复后，既有 Editor test binary 完整执行 `zui_asset_governance` 为 `72 passed / 0 failed / 0 ignored`，耗时 129.48s；没有改宽治理规则或增加单资产例外。
7. 修复后的第二次 Windows 受管构建未再出现 `JointPaletteStorage` 或 Editor fixture 的旧错误，但在共享 Asset owner 正在迁移的 `project_document` 边界失败：`codec/material/model` 子模块函数可见性与父模块 re-export 发生 `E0364/E0603 x16`。该路径由 Frameworks 03 活跃会话持有，本切片不越租约修改；owner 收束后下一次构建成功越过该错误。
8. 向上构建又暴露两项 Editor 测试 fixture 漂移：旧 `SceneAsset::to_toml_string` 与 `MaterialAsset::from_toml_str` 已被生产项目文档接口硬切。fixture 已迁移到 `to_project_toml_string`、`ReferenceResolutionError` 与 `MaterialAsset::from_zmaterial_document`，没有恢复兼容 API。首轮迁移的 resolver 错误类型不匹配产生单一 `E0271`，修正后最新受管构建已越过 Editor lib-test typecheck。
9. E 盘失败任务已通过协调器标记失败并释放；随后使用独立本地维护能力，只删除协调器复核批准的 `D:\cargo-targets\zircon-plugin-architecture-plugin-checks` 陈旧通道，D 盘可用空间从约 25 GiB 恢复到约 45 GiB，未删除其他会话的 retention/lease 目录。
10. D 盘新受管通道首次完整构建成功越过 Runtime 与 Editor fixture，暴露 `zircon_editor/src/core/editing/engine/history.rs` 的非穷尽 `Option<usize>` 匹配 `E0004`。保持既有历史语义，把 guarded `Some(saved)` 两分支收束为 `Some(saved) => saved.checked_sub(1)`；格式与 diff 检查通过。
11. 修复后第二次受管构建已越过该 `E0004` 并进入 Runtime 最终 rlib/cdylib 链接；命令超时后仍由原 Cargo/rustc 子进程继续运行至结束，但未产出 `zircon_editor-*.exe`。残留受管 job 已按 `exit 124` 标记失败并释放。随后的其他共享构建继续占用磁盘，最终复核为 D/E/F 可用约 `4.84 / 32.88 / 1.08 GiB`；当前最低阻塞是 Windows 构建存储容量，不是 Blend Space 源码或 ZUI 治理失败。
12. 本记录不宣称自适应测试或截图通过；预定截图 `editor-window-m3-blend-space-workbench-{900x620,1260x780}.png` 仍未生成，也未向仓库 `target` 写入验证图。
13. 复查参考图后移除了 sample canvas 底部依赖空格撑开的伪定位文本，把 `0 / SPEED / 620` 拆成 5 个相对布局子节点（固定端点与标题、两侧 `Stretch` 间隔）。生产资产现为 94 个节点、12 个 widget imports，`Stretch + size` 违规为零；随后治理复验仍为 `72 passed / 0 failed / 0 ignored`（149.34s）。
14. 协调器后续又识别并安全删除 3 个过期 E 盘受管通道，其中包括第一次失败的 Blend Space 通道；未删除当前 D 盘 retention 通道。尝试在 E 盘以关闭 incremental/debug info 的紧凑配置获取新通道，连续 3 次均在创建目标目录前被共享 coordinator 离线中断，因此没有产生新的 target 内容，也没有生成截图。
15. 共享 coordinator closeout 完成后，D 盘紧凑配置受管构建实际执行 `cargo clean`（回收 8.3 GiB）并重新进入 Runtime 编译；它没有再报告 Editor `history.rs` 的 `E0004`，但被 Physics 03 活跃 owner 新增的 `ColliderShape::{Cylinder, ConvexHull, TriangleMesh, HeightField, Compound}` 暂态切断阻塞：`project_io/physics.rs`、`property_access/entries/physics.rs` 两处和 `render_post_process.rs` 共 `E0004 x4`。`scene.rs` 在 05:29 仍由 `plugins-03-physics-m2-20260712` 活跃会话编辑，本切片不越租约用 wildcard 掩盖这些新形状的序列化/属性/渲染语义；权威 `open` 交接为 [`Physics 03 / ColliderShape consumer exhaustiveness`](../../../zircon_plugins/03/failure-2026-07-12-collider-shape-consumer-exhaustiveness.md)，协调器已能按 fixing plan 查询该节点。
16. 继续按“原子组件先行”复查参考图时，移除 Blend Space 三个 L4 容器的局部 `background_color/border_color/corner_radius` 覆写，改为完全消费共享 `workbench-panel/workbench-surface-inset` token；预览卡中的 ASCII 假角色替换为标准 `WorkbenchIcon` + `zircon_engine_style/scene/skeleton.svg` 图像组件。生产资产仍为 94 个节点，imports 由 12 增至 13，`Stretch + size` 违规与四类局部视觉覆写均为 0。当前源码读取型治理精确验证 `workbench_primitive_component_assets_keep_native_component_contract` 与 `l4_surfaces_contain_no_inline_primitive_structures` 各 1/1；完整治理组为 71/72，唯一失败是并发修改的 `workbench_command_palette.zui` 缺 `disabled_commands` popup-shell prop，与本 Blend Space 资产无关，因此不把 71/72 记成本切片退化或全绿。
17. Physics 03 owner 已在 project I/O、property projection/capacity、property write 与 render post-process 显式补齐五个新形状，旧 `E0004 x4` 不再出现。复用 D 盘紧凑受管 target 的当前源码 `zircon_editor blend_space_workspace --no-run` 构建继续推进至 Shader 06，但被其活跃未跟踪 `realtime_ibl_graph_plan.rs` 的返回借用生命周期 `E0106` 与 `realtime_ibl_wgpu_recorder.rs` 的 `Option ?`/`Result` 边界 `E0277` 阻断。新建权威 `open` 交接 [`Shader 06 / Realtime IBL graph recorder type errors`](../../../../zircon_runtime/shader/06/failure-2026-07-12-realtime-ibl-graph-recorder-type-errors.md) 并导入协调器；本切片不越 Shader 06 租约修复。截图仍未生成。
18. 文本原子继续按 Runtime text/Editor typography 边界收敛：新增共享 `primitives/data/workbench_caption.zui#WorkbenchCaption`，集中拥有 Unreal 8pt（10.67 logical px）、600 weight、secondary tone 与 18-22px caption 行高。Blend Space 的 13 个分组标题/坐标标注全部由 `WorkbenchLabel + 局部 font_size/font_weight` 迁到该原子，L4 复合资产的字号和字重覆写归零；TOML 解析、94 节点/14 imports、13 caption consumers、`Stretch + size = 0`、局部 surface/typography override = 0 的静态审计通过。相应 typography 模块文档已更新；当前 binary governance 与两档截图仍随 Shader 06 构建交接保持 pending。
19. 2026-07-12 当前源码复验确认 13 个 `WorkbenchCaption` 消费点、111 个相对 `Stretch` 声明、局部 surface override = 0、局部 typography override = 0；Blend Space 截图测试源码通过 `rustfmt --check`，本切片文件通过 `git diff --check`。
20. 修正 900 px 窗口的相对布局退化：左/预览/右侧栏从不可收缩 `Fixed` 改为有界 `min/preferred/max + weight=0 Stretch`，中心编辑栏与采样画布同时获得 240 px 最小宽度并由 `weight=1` 接收剩余空间。当前 Windows Editor binary 的源码结构、caption 合同与 900/1260 几何组为 `3 passed / 0 failed / 1 ignored`；采样画布在两档窗口均大于预览卡 1.35 倍，中心栏在宽屏获得显著增量。
21. 原生 retained painter 增加 extension workspace 专用绘制阶段，并把 retained `parent_id` 投影到 `TemplatePaneNodeData::parent_node_id`。子树过滤不再依赖 node-id 字符串前缀，而是按父链闭包选择；同时跳过没有 control-id、不会进入 native DTO 的组件展开节点，保留最近可绘制父节点。最低层回归为 painter `2/2`、parent projection `2/2`，覆盖非前缀 node id、跨不可绘制 wrapper 的父链和 SceneWorkspace 重叠兄弟不越界覆盖。
22. ignored screenshot route 已成功执行 `1 passed / 0 failed`，生成 [`900x620`](../../../tests/editor/editor-window-m3-blend-space-workbench-900x620.png) 与 [`1260x780`](../../../tests/editor/editor-window-m3-blend-space-workbench-1260x780.png) 两张真实窗口图；没有把验证图写入 Cargo target。人工复核仍看到空白三栏而非 94 节点的密集复合内容，所以截图只作为“当前失败视觉证据”，不能作为视觉验收完成证据。
23. 最新一次继续增强 screenshot parent-chain 诊断的增量构建被其他活跃 Runtime text 工作区漂移阻断：`zircon_runtime/src/graphics/text/layout/rich.rs` 初始化 `RichParseResult` 缺 `tables`，另有同次输出的 `E0432`。该失败发生在已取得上一次全绿 Editor binary 与截图证据之后，不回退本切片的 2+2+3+1 已通过结果；也不越权修改 Runtime text owner 文件。
24. 修复前的真实 native screenshot 诊断已证明问题不在窗口截图 API：900px retained parent chain 能从 workspace/search/canvas/apply 回溯到 `WorkbenchExtensionModuleWorkspacesHost`，但 native frame 中 Search/Canvas/Apply 分别只有 `2/1/2` 个 distinct colors；直接 template paint 也仅 `1/1/2`，因此空白三栏的最低原因位于 retained descriptor/projection/painter 支撑层，而非 `.zui` 自适应几何或截图保存路径。
25. 最低共享根因进一步收敛为注册表分裂：`RetainedUiHostAdapter` 只消费 `editor_showcase()`，而 `SearchField` 由 `material_editor_foundation()` 拥有，导致它在 retained projection 丢失 `search-field/input/leaf` typed metadata，随后 native DTO 把它降级为 `Mount + panel + 空文本`。新增 `ui/component_registry.rs` 作为 retained projection/contract metadata 的单一合并 owner；注册行为按 component id 替换，material foundation 描述符覆盖同 id 的旧展示描述符，不引入字符串兼容 shim。
26. SearchField native DTO 已显式保留 Runtime text 输入身份：placeholder 在无 value/text 时投影为 authored text，role 为 `SearchField`，surface 为 `inset`，默认 radius/border 为 `5/1`，host component family 归入 `TextInput`。新增 typed registry、SearchField projection 和 extension-workspace SearchField 像素回归；断言要求 surface、border、搜索 glyph 与 Runtime text placeholder 至少形成 4 种颜色。
27. 修复 Editor14 回传暴露的两个本 owner 编译问题：typed category/layout-role 断言改为各自 `as_str()` 消费新版唯一类型真源；`paint_componentized_extension_workspace_for_test` 补齐 `overlay -> scene_layers -> renderer -> host_contract` 的 `#[cfg(test)]` 单一可见性链。相关 Rust 已执行 scoped `rustfmt`，owned diff 已通过 `git diff --check`；在新的当前源码 binary 生成前不把这些项目记为 Cargo 通过。
28. 两次受管 Windows `zircon_editor --lib --no-run` 复验曾在进入 Editor 当前改动前由 Render 18 阻断。第一次为平面反射类型/函数/纹理尺寸父模块导出 E0432/E0425 及级联 E0282，第二次只剩 `environment::PLANAR_REFLECTION_TEXTURE_SIZE` E0425。Render 18 owner 已补齐唯一父模块导出并回传为 [`fixed-2026-07-12-planar-reflection-export-boundary.md`](fixed-2026-07-12-planar-reflection-export-boundary.md)；Layout 15 未修改 Runtime Render 文件、未添加调用点常量、未弱化测试。
29. 当前 `docs/tests/editor/editor-window-m3-blend-space-workbench-{900x620,1260x780}.png` 和 `diagnostic-native-blend-space-900x620.png` 仍是修复前旧 binary 产生的失败证据；没有在 Cargo `target` 放置截图。必须等待当前源码二进制构建通过后重新生成、人工复核并删除临时 diagnostic 图，才允许升级本记录状态。
30. Render 18 回传后的受管 Windows job `94f7c5732c7a4c128ad82cc5079f6eb1` 已完整编译 `zircon_runtime` 并进入 `zircon_editor`；先前本切片的 component registry E0308 与 componentized test-export E0432 均未再出现，证明当前源码至少通过到 Editor 作业系统边界。构建随后仅在 `zircon_editor/src/core/jobs/system/mod.rs:247` 因 `PendingJob.cancel_task` 字段误作方法调用产生 E0599；该文件归属活跃 Editor14 owner。因 Editor14 已向 Layout15 持有未回传交接，协调器审计禁止创建反向交接环；本记录保留直接阻断证据，且未把“编译推进”记作 focused test 或视觉通过。
31. Editor14 owner 复用同一受管 target 执行 focused job `026db73e687d465ea11a2cac5d5cd927`，当前源码于 13:13 再次在同一 `system/mod.rs:247` E0599 结束；fingerprint 记录明确给出字段闭包应以 `(pending.cancel_task)(...)` 语法调用。该复验仍未进入任何测试体，也未产生新的 Editor test binary，因此 Layout15 不使用 11:49 的旧 binary 重跑或覆盖截图。
32. Editor14 owner 已把该行修正为 `(pending.cancel_task)(...)`，并于 13:14 启动 `editor14-m3-focused-rerun.log` 对应的当前源码 focused 重建；截至本状态更新仍在 Runtime/Editor 编译阶段。此项只记“阻断源码已修、验证运行中”，不能提前记为 jobs、Layout15 或视觉通过。
33. Editor14 当前源码重建随后完成：`core::jobs::tests` 为 `36 passed / 0 failed`；同一新 binary 的 typed registry 为 `1/1`、SearchField DTO 为 `1/1`、parent projection 为 `2/2`、原 componentized painter 为 `3/3`，Blend Space 非 ignored 组为 `3 passed / 1 ignored`。registry typed 与 componentized test-export 两项交接已通过协调器迁移回 Editor14 的 `fixed-2026-07-12-*` 记录。
34. ignored 当前源码截图仍以 Search/Canvas/Apply `2/1/2` 色失败；DTO 已是 `SearchField/search-field/inset/Search samples`，workspace/search/canvas 父链均到达 extension module host，且 isolated subtree painter 仍为 `1/1/2`。最低根因因此从注册表/父链继续下沉到子树选择：`WorkbenchExtensionModuleWorkspacesHost` 的 Overlay 同时保留所有 `*WorkspaceHost`，空 sibling host 在活动 Blend Space 后以相同 frame 生成 panel 命令并覆盖内容。
35. 生产修复不再以 module-workspaces host 作为绘制根，而是在 host 的可见 DTO 中选择父节点为 `WorkbenchExtension*WorkspaceHost` 的具体 `WorkbenchExtension*Workspace` 根，再按既有 parent-chain closure 绘制其后代；module host 只拥有自适应 clip。新增多 workspace 重叠回归，要求空 sibling host 存在与否逐像素一致。scoped `rustfmt --check` 与 `git diff --check` 通过。首轮重编撞上并发 typed `JobFailure` 修改快照的 14 个错误；owner 修复后第二轮已把阻断收敛为 Editor15 typed-export 的 `ExportBuildPlanError` 新导出路径 E0432×2 和一个 runner 旧返回类型。runner 已在构建结束后修复，剩余 E0432 仍由 Editor15 owner 持有，故本项状态为“实现完成、当前源码验证阻断”，不是通过。
36. Editor15 已恢复 `ExportBuildPlanError` 的唯一父模块导出，并在后续 Windows lib-test 中通过 Editor 当前源码编译。该向上复验在链接阶段发现本切片的 ignored screenshot 仍占用同一复用 target 的 `zircon_editor-*.exe`，触发 `LNK1104`；具体失败、进程证据和修复回传已归档为 [`fixed-2026-07-12-blend-space-visual-test-target-lock.md`](../../editor/15/fixed-2026-07-12-blend-space-visual-test-target-lock.md)。这不是 Editor15 功能错误；Layout15 已在后续视觉验收中收束截图进程与验证通道生命周期。
37. 14:24 的后续进程审计发现 Layout15 又直接对同一协调器复用 pool 启动 Blend Space `--no-run` Cargo；此时 pool 已独占登记给 Editor15，导致 Editor15 focused wizard 测试等待 artifact lock。该项已追加到同一 failure 记录，后续必须先获取独立/互斥受管通道，不能继续把 `--target-dir` 视为并发授权。
38. Editor15 owner 补齐 `zircon_runtime::plugin::ExportBuildPlanError` 唯一根导出后，同一 retained target 生成 14:12 当前源码 binary。Painter 聚焦组为 `4 passed / 0 failed`，新增 inactive sibling host 回归证明未激活 workspace 不再覆盖可见 Blend Space；Blend Space 非 ignored 组为 `3 passed / 0 failed / 1 ignored`。
39. 人工检查首轮修复图发现 900px 的中心内部 preview card 越过 center/right 边界。按相对布局修正 center lane 的内部总最小宽度为 canvas `240` + gap `4` + preview `150`，并新增 canvas/preview 必须包含于 center work-area 的几何断言；宽屏增量门禁改为中心获得至少 45% 的 workspace 新增宽度。当前源码复验再次为 `3 passed / 0 failed / 1 ignored`，900px canvas/preview 均保持在中心栏内。
40. ignored 当前源码截图为 `1 passed / 0 failed`。[`900×620`](../../../tests/editor/editor-window-m3-blend-space-workbench-900x620.png) SHA-256 `EF7CF80A17708B47CFBAFEED85F2893C41CCCF1F9D7712ED26AAF9A94C751D52`；[`1260×780`](../../../tests/editor/editor-window-m3-blend-space-workbench-1260x780.png) SHA-256 `188A9A8BF493800C81732F0EB0F1A3BEBA712950B6BAB4FAD110D741AC612461`。两图人工复核显示 Blend Space 左资产栏、中心采样画布/预览/输出表和右属性栏；900px 为有界压缩、1260px 中心获得额外空间。临时 diagnostic PNG 已删除，仓库 `target` 与 D/E/F Cargo target 扫描没有 `*blend-space*.png`。
41. screenshot route 已移除临时 diagnostic 写图逻辑；执行结束后目标 Editor test exe 可独占读写打开，证明当前进程/文件句柄已释放。原 target-lock failure 已回传 fixed；后续布局截图仍必须先取得协调器 lease，不能绕过协调器并发链接共享 pool。
42. 最终当前源码受管构建 job `bc8f993d48094deea5e75fed278f315c` 成功（exit 0，`cargo start/finish/release` 生命周期完整）；该 binary 再次通过 painter `4/4` 与 Blend Space `3 passed / 0 failed / 1 ignored`。Scoped `rustfmt --check`、owned `git diff --check` 均通过。
43. 后续视觉复核未接受 900px 图中仍显粗大的裸 SectionTitle。共享原子已按 Unreal Starship `DetailsView.CategoryFontStyle = SmallBold` 与平直分类标题思路收敛为 Runtime `body_size = 13.333333`、`strong_weight = 600`、28–30px 高度、零圆角 raised surface 和 1px separator；原生 painter 改用 `component_variant = "section-title"`，删除宽泛 `Workbench*Title` 后缀识别，并把标题图标从 `font_large` 收敛到 `font_body`。当前测试资产增加 source/semantic identity/Runtime metrics/palette/native pixel 覆盖。
44. 当前源码聚焦重建首先暴露 Editor15 测试内部调用兄弟模块私有 cleanup 构造器的 `E0624 x2`，已以公开枚举变体构造真实 typed source，不扩大生产可见性。随后 Runtime Text07 新表格常量只定义在 `text::rich`，但两个消费者从 `core::framework::render` 根导入，产生 `MAX_RICH_TABLE_ROW_SPAN` 的 `E0432 x2`；已补齐 `rich -> text -> render` 唯一导出链，未在调用点复制常量。当前源码 binary 的 SectionTitle 组已推进到 `9/10`，剩余失败只证明旧断言把底边通用 border 当作 separator；断言已改为校验实际顶层通用 border，同时 palette 单元测试继续单独证明 separator token。最终 current-source focused rerun、640/900/1260 组合和新截图仍为 `pending`，不得把本条记作视觉通过。
45. 最终当前源码 SectionTitle 聚焦组为 `10 passed / 0 failed`；源码、语义 identity、Runtime body/strong typography、palette、icon 尺寸和 native pixel 断言均通过。SectionTitle 继续以 `component_variant = "section-title"` 进入原生 painter，不恢复宽泛 control-id 后缀匹配。
46. 新增通用 `responsive_min_tier` 资产合同，由 Workbench bridge 按逻辑宽度和既有 `Ultra/Narrow/Regular/Wide` tier 在第二次布局前统一切换 `Visible/Collapsed`。Blend Space 不包含按截图 control-id 写死的坐标分支：640 折叠右详情、预览卡、预览按钮、资产摘要和固定输出区；900 保留主画布与输出区；1260 展开预览和右详情。tier 单元测试 `1/1`，Blend Space 非 ignored 组 `4 passed / 0 failed / 1 ignored`。
47. 真实截图首次揭示视觉粗条不是 L4 gap，而是原生 resize layer 把 8px 指针命中区直接涂成 `[79,92,112]`。最低修复保留 8px hit frame，只把可见规则线居中画成 1px shared border tone `[42,50,56]`；垂直/水平 splitter visual-frame 回归 `1/1`。这同时消除了 640/900/1260 主区和底栏的粗蓝灰条。
48. ignored 当前源码截图 route 为 `1 passed / 0 failed`。三图 SHA-256：[`640×520`](../../../tests/editor/editor-window-m3-blend-space-workbench-640x520.png) `4AB29D0FAB4A056176FFE3EC811D2E0B6007C0073C65207310C9A8734C7FDEC1`；[`900×620`](../../../tests/editor/editor-window-m3-blend-space-workbench-900x620.png) `37AC7B2BA9CC09697D74A32EF1AB613AF0BA2D7362A4B80892E42306FB66C8BE`；[`1260×780`](../../../tests/editor/editor-window-m3-blend-space-workbench-1260x780.png) `67C55782D73A81CC7077046D671F3C8BDAE852B5818007FC37940EBA682F97D4`。三图人工复核确认响应式降级、细分隔线、SectionTitle 紧凑标题和画布包含关系；Cargo target 扫描为 `NO_BLEND_SPACE_SCREENSHOTS_IN_TARGETS`。
49. 为使当前源码 test binary 可重建，本轮按失败最低层补齐并发 Text07/Editor15 已进入主编译图的真实合同缺口：Rich inline 两个 layout 初始化 `boxes`；RichTable box/padding 与 ProjectManifest 唯一父模块导出；Editor export pipeline 错误报告借用/clone 边界、typed ExportStage 路径、preset/tests 借用，以及 text table-cell decoration exhaustive paint。它们均是当前生产类型的显式收束，没有恢复旧 API、wildcard 掩盖或调用点常量复制。
50. 按参考图启动下一原子切片：新增共享 `WorkbenchSampleGrid` 组件资产，Blend Space 中心画布从三组 `WorkbenchListRow` 伪采样行硬切为一个连续 Canvas；L4 只声明轴范围、刻度和采样点，不再用列表宽度或空白填充模拟二维坐标。
51. Sample Grid 的 retained DTO 采用显式 `TemplatePaneSampleGridData/PointData`；资产属性经过 typed TOML array/table projection 后才进入 painter。投影测试覆盖双轴、5+5 刻度、选中点和错误条目过滤，避免把采样数据压成私有分隔字符串或 control-id 特例。
52. 原生 painter 拆分为 identity/geometry/metrics/palette/surface/text/points 七个职责叶子：相对 frame 计算 plot gutter，绘制深色连续曲面、虚线网格、零轴、菱形采样点和 cyan 选中 halo；刻度、轴名和 `Run_Fwd` 标签全部发出 Runtime Text `HostPaintCommand::text`，没有回退为伪文本条。
53. 当前资产上的完整 `zui_asset_governance` 独立复验为 `72 passed / 0 failed / 0 ignored`（88.49s）；新增 Canvas 组件满足 file-derived locator、单组件文档、已知 props/可序列化值、可达 import 与 L4 bottom-up 组合合同。旧 binary 的 Blend Space 源码结构、900/1260 自适应几何及两个 primitive-governance exact 也分别为 `1/1`、`1/1`、`2/2`，证明资产硬切未退化既有几何。
54. 当前源码 `cargo test -p zircon_editor sample_grid --no-run` 已进入 Runtime 编译，但被 Navigation 05 活跃工作树的 `NavQueryFilter` 阻断：`zircon_runtime/src/core/framework/navigation/query.rs` 去掉 `Serialize/Deserialize` derive 后仍保留 `#[serde(default)]`，报告 `cannot find attribute serde in this scope`。该 owner 已发布 `failure-2026-07-12-navigation-query-filter-serde-array.md`；Layout 15 未越租约修复导航 wire contract，也不把未执行的 2 个投影测试与 3 个 painter 测试记为通过。
55. 因当前源码 binary 尚未生成，本轮没有覆盖 19:44 的三档截图；它们仍是上一响应式基线，不代表 Sample Grid 视觉验收。验证图 target 扫描保持 0，后续必须在 Navigation 05 恢复编译后运行原子测试并刷新 `docs/tests/editor` 的 640/900/1260 三图。
56. Navigation 05 恢复编译后，当前源码 Windows Editor lib-test 已完成重建。Sample Grid 的 3 个原生像素测试与 2 个 typed projection 测试为 `5 passed / 0 failed`；Blend Space 非 ignored 组为 `4 passed / 0 failed / 1 ignored`，其 native DTO 新增硬断言，确认 `component_variant=sample-grid`、9/6 个双轴刻度、8 个采样点和选中的 `Run_Fwd` 均穿过真实 Workbench projection。
57. 新截图首次把“资产有数据但画布只有边框”的原因定位到第二条 DTO 适配路径：pane body 使用 `host_template_node` 并已携带 typed Sample Grid，而全窗口 `to_host_contract_workbench_window_node` 只投影通用字段，遗漏 `TemplatePaneSampleGridData`。修复把已有 typed TOML projector 提升为两条 retained adapter 共用的唯一数据入口；没有增加 control-id 分支、私有字符串协议或截图坐标。
58. ignored 当前源码截图 route 为 `1 passed / 0 failed`，刷新 640×520、900×620、1260×780 三张真实窗口图。人工对比参考图确认连续深色网格、虚线刻度、零轴、菱形样本、cyan 选中 halo 与 Runtime Text `Run_Fwd` 标签均可见，三档降级关系保持；尚未实现参考图的真实 3D preview、weight heatmap 与高密度 samples/details 内容，所以视觉状态仍是 `refinement-active`，不是 Unreal 等价完成。
59. 刷新后完整 ZUI 治理为 `72 passed / 0 failed`。三图 SHA-256 分别为 640 `AC7CF47B82CA4DCEF25006D75579F2CBDE0B8426005F532B53D9E61114B17AB3`、900 `8C5C84954519E7903E09F7CC23FEC07EDE0B0E9F9B2EE284DA87312DC2AD77DE`、1260 `2EF203D87092E4E9E788BDF181DCF2769EB9500966735313D42375C851592B6D`（Sample Grid 阶段，后续第 63 项已由 Weight Heatmap Wide 图替换）；repo `target` 与 D/E/F Cargo targets 扫描为 `NO_BLEND_SPACE_OR_SAMPLE_GRID_SCREENSHOTS_IN_TARGETS`。
60. 继续按参考图向下一个共享原子推进：新增 `WorkbenchWeightHeatmap` Canvas 资产及显式 `TemplatePaneWeightHeatmapData/SourceData`。typed projector 同时服务 pane body 与全窗口 retained adapter，限定 4-32×3-24 网格分辨率并把 source x/y/weight 归一到 0..1；4 个 projection/painter 测试覆盖错误条目过滤、clamp、多色热场、采样 marker 与相对 frame 缩放。
61. Weight Heatmap native painter 按 field/geometry/identity/markers/palette/text 六个叶子拆分：以相对 frame 计算 plot/legend，按采样源径向影响生成蓝-青-绿-黄-红热场，复用菱形 sample 语言标记源点，并通过 Runtime Text 命令绘制 0/1 legend。没有引入位图截图资产、控制 ID 坐标或 L4 局部 RGB。
62. 热力图只组合到 `Wide` preview card；640/900 的既有降级截图逐字节 SHA 保持不变，1260 展开图增加可见 `WEIGHT HEATMAP`、彩色 field、legend 与 markers。为保持密度，预览栏删除 3 个重复 speed/direction/weight 属性行，只保留 preview subject、heatmap 与选中 asset；轴/采样细节仍由右侧详情 owner 展示。
63. 当前源码 Weight Heatmap 聚焦组为 `4 passed / 0 failed`，Blend Space 为 `4 passed / 0 failed / 1 ignored`，ignored screenshot route 为 `1/1`，ZUI governance 为 `72/72`。截图 SHA：640/900 仍为 `AC7CF47B...` / `8C5C8495...`，1260 更新为 `6311312B80E41EC3D1674ED980EA89D137B3E43E0BA57D9907E456DFCB9EA4E2`；target 扫描为 `NO_BLEND_SPACE_OR_WEIGHT_HEATMAP_SCREENSHOTS_IN_TARGETS`。
64. 2026-07-13 进入 Preview Viewport 原子切片：先写入共享真实图像原子、Runtime Text 叠加和 Wide samples 表密度的 RED 合同，再新增 `WorkbenchPreviewViewport` 与无内嵌文字的 `animation_mannequin_viewport.svg`。Blend Space 已删除 skeleton icon 占位，Wide 预览卡改为 `Perspective/Lit` 工具条、真实 Image 视口和 `Run_Fwd | Previewing` 状态行；右详情增加 4 个四列 `WorkbenchTableRow` 样本行。所有 L4 尺寸仍是相对 `min/preferred/max + Stretch/Fixed`，未增加局部 RGB、字体、截图坐标、control-id painter 分支或 Runtime Text 旁路。本项只记录实现完成，Windows 测试、三档截图、人工复核和 target 扫描仍为 `pending`。
65. Windows 当前源码测试阶段完成：受管 `zircon_editor --lib --no-run` job `3689686ab23d4c40a976836644d543ec` exit 0；新 binary 的 Blend Space 组为 `6 passed / 0 failed / 1 ignored`，其中 image atom/Runtime Text overlay 与 dense sample rows 两个新增合同均通过；完整 ZUI governance 为 `72 passed / 0 failed`。ignored 截图 route 为 `1/1`，刷新后的 640/900/1260 三图只位于 `docs/tests/editor`；SHA-256 依次为 `35F47D77E88E1B7B415548A9656A7B96EA26E6A232E61DDDDAA9577726EC83CB`、`8B81CD305E2E08479BC07C5E1C69387B7607D6B01CA14455F231BCC9A1A8764B`、`FB62D0AE07F6D71C5CEDDAEEC8DF0FD8E5920F07AAAC44D8CE4965E52A369191`；repo 与 D/E/F Cargo targets 的 `*blend-space*.png` 扫描为 0。
66. 人工复核确认 640 保持左资产栏+主网格降级，900 保持主网格+输出表，1260 展开真实 mannequin preview、Perspective/Lit/preview status、Weight Heatmap、SAMPLES(8) 四行表和 axes/sample detail；预览卡/右详情没有越过 center/right 边界。视觉密度和预览信息量相对上一版明显提升，但顶栏、底部诊断复合区和全参考图的曲线/时间轴密度仍属于后续切片，因此整体状态继续 `visual-refinement-active`，不声明 Unreal 等价或 Layout 15 完成。
67. 继续按参考图底部复合区启动 Preview Timeline 原子切片：测试合同先定义 typed duration/playhead/ticks/track/keys、错误条目过滤、共享 host palette 投影、Runtime Text 与自适应几何；生产实现新增 `WorkbenchTimelineStrip`、`TemplatePaneTimelineStripData/KeyData`、pane/full-window 双投影与按 identity/geometry/metrics/palette/surface/text/keys 拆分的 native painter。Blend Space 的重复 `Blend Output` 四行伪表已替换为共享按钮工具条 + typed timeline Canvas；原 output 与 idle/walk/run/diagonal route 迁移到 timeline/右侧 sample rows，交互合同不删除。新 painter 的尺寸与颜色只从 `HostControlMetrics` / `HostMaterialPalette` 投影，L4 未增加 RGB、字体或绝对坐标。本项只记录实现完成；Windows compile/test、三档截图、人工复核和 target 扫描仍为 `pending`。
68. 2026-07-14 按组件优先级继续修复共享 Button content 原子。真实 native painter RED 证明 top-toolbar `Compile` 已投影 `layout_spacing=4`，但 painter 仍无条件使用 host fallback 7，最终 icon/text frame 的实际间距为 7。最低层修复让有限正值的 `layout_content_offset_x` 成为已声明 spacing，未声明节点继续回退共享 host metric；Runtime Text 测量、SVG icon、relative frame、palette 和 interaction 路径不变，没有增加 toolbar control-id 特例或 L4 像素坐标。focused GREEN 为 `1 passed / 0 failed`，完整 retained Button painter 组为 `53 passed / 0 failed`，Blend Space 当前源码为 `17 passed / 0 failed / 1 ignored`，截图 route 为 `1/1` 并且只刷新 `docs/tests/editor` 三档图。人工复核确认 Save/Browse/Compile 图标与完整 Runtime Text 标签间距一致、没有重叠，三档响应式边界保持；当前 SHA-256 为 640 `94F01E59B0BB9B62287142A0BB650694D9DB9B0838CA016A3FC2DEF2393A2553`、900 `517834FEE9EBC5B47F58A8395B4BB85ECBF82E311259F842330E96AC4164FD58`、1260 `FEBF16B153C281DDAC2C5D3D2287ACE6E4F0C13BACDE16F763BD70925989CA93`，repo 与 D/E/F target 的 `*blend-space*.png` 扫描为 0。本项状态为 `completed-primitive / visual-refinement-active`，不据此声明整体视觉完成。
69. 2026-07-14 继续修复共享 WorkbenchStatusItem 原子。生产截图中 1260 宽仍把 `No Errors / 2 Warnings / 1 Message` 裁成省略文本；最低层检查确认 native geometry 在 72–100 宽状态项内先消耗 `gap_l*2=24` 左空隙，再消耗 8 marker + 8 gap。Runtime Text RED 首先报告 Ready marker x=`24`，收敛到单个 `gap_m=8` 后又准确报告 Warnings 可用 `68` 小于 Runtime Text+clip guard 所需 `70.79818`；最终单个 compact inset 让四个 authored status frame 均容纳完整 Runtime Text 墨迹。focused `1/1`、完整 status controls `29/29`、局部视觉 route/导出各 `1/1`、完整 ZUI governance `75/75`、Blend Space `17/0/1` 与截图 route `1/1`。人工确认三档 `No Errors / 2 Warnings / 1 Message` 完整且 right-side Grid/Snap/Zoom 无位移；primary 动态消息仍按既有固定 slot 省略，留给后续 status-bar composite 语义重构。当前 SHA-256：component `3681AED16CD5D336842F44DB6414B6B9963715008E2B0550BBA1320D1ACDEBBF`；640 `94D41B95126EDCC6EB40174144F73EF3357F05B36141DFE663D6B55AEDBA7BA7`、900 `837EB502F8B26B2A391DDD62FC6E184D3D55C42E930A904ECE32C19F237A4F5F`、1260 `342B53D6FB90A493AE5B7598AE4D444E384B67E5930BCC3ABA2857D398FE4EDC`；repo 与 D/E/F target 的 `*blend-space*.png` / `*status-controls*.png` 扫描为 0。本项状态为 `completed-primitive / visual-refinement-active`，不宣称整窗 Unreal 等价。
70. 2026-07-14 进入 WorkbenchStatusBar 自适应 Runtime Text 复合切片。Unreal `SCurveEditorTreeFilterStatusBar` 的 `SHorizontalBox` remaining-width 结构作为依据：删除匿名 `status_fill`，把 `WorkbenchStatusReady` 从固定 72 硬切为状态栏唯一 `Stretch` child，visible diagnostics/tools 保留 authored fixed width；L4 不新增坐标计算、局部字体/RGB 或 control-id breakpoint。首轮独立审查发现 active task 的固定 224 与其余固定 582 合计 806，会使 640 合同不可达；第二轮又证明只验 420/640/900/1260 代表点会遗漏 481 与 641 tier 下界的溢出。最终最低组合层修复复用共享 `responsive_min_tier`：Ultra/Narrow 只保留 primary+task，Regular 加 diagnostics，Wide 才恢复 Grid/Snap/Zoom 与三个 icon actions；task/label/progress 分别获得 160–224、100–132、52–84 的一致压缩边界，primary 保留 160 minimum。idle/active production-bridge 合同直接消费共享 tier classifier，覆盖 420/480/481/640/641/900/1259/1260，均用 Runtime Text 实测 `Blend space opened`，并要求 visible fixed controls 不缩小、不重叠、右缘贴齐；active 合同还检查 task label/progress 的范围、8px 顺序与 containment。源码 RED 明确缺少 tier/min 合同，最终 TOML/source GREEN 与下界容量审计为 420 `320<=420`、481 `320<=481`、641 `608<=641`、1260 `902<=1260`。ZUI TOML 解析、唯一 Stretch/source topology 与 scoped format/diff checks 已通过。共享工作树的受管 Windows job `e6693467f3464dde865ca6cbf56d7205` 已编译到 `zircon_editor`，但被 Editor UI 03 已登记 handoff [`failure-2026-07-11-retained-text-family-and-subpixel-contracts.md`](../../editor_ui/03/failure-2026-07-11-retained-text-family-and-subpixel-contracts.md) 的 `paint_text/draw/layout/tests.rs:741` 阻断：`ShapedGlyph` fixture 缺少已定稿 `font_instance_id`，测试体与截图 route 尚未执行。本切片当前状态为 `implemented / validation-blocked-external`；已另建 pinned-HEAD validation copy 只覆盖本切片三文件，待最新审查修复重新物化并执行，不得把旧 screenshot binary 或 primitive 阶段哈希冒充 composite GREEN。
71. 2026-07-14 强制复审最终为 `Ready`，无 Critical/Important；确认 production classifier 边界、完整 tier 容量、idle/active 八宽矩阵、任务 parent/label/bar 范围与 containment 一致。为继续向上验证，按 Editor UI 03 交接中已写明的 canonical 语义把非变量 host glyph fixture 补成 `font_instance_id: None`；受管 job `c1abbe55243c4b1689e30bc79256c81a` 已越过原 E0063，证明该编译子项收口。构建随后在活跃 Runtime Text Session 租约内的 `zircon_runtime/src/graphics/scene/scene_renderer/ui/text.rs` 报 E0616：仍直接写已私有化的 `ResolvedScreenSpaceUiTextBatches.native_texts/sdf_texts`。Layout15 不越租约修改该 Runtime owner；focused test body、governance、Blend Space 与新截图仍未执行，所以状态继续 `implemented / validation-blocked-external`。完整 pinned-HEAD validation copy 因协调器逐文件物化仍停在 `planned`，不作为验证证据。
72. 2026-07-14 复查确认前述两项 Runtime Text 编译阻断已由各自 owner 在当前共享源码解除；本轮受管 Windows 验证成功生成 3,173-test Editor binary，但整组执行超过 60 分钟测试时限，没有可作为整组 GREEN 的终态摘要。随后 exact composite 合同真实执行并暴露测试自身的最低断点：断言把组件内部 `WorkbenchWindowStatusBar` 当成 public status region，而 production bridge 的可见/裁剪 authority 是 `EditorWorkbenchTemplateFrames::status_bar`（`WorkbenchWindowStatusBarRegion`）。测试已改为消费该 canonical frame owner，不改变组件布局政策。结构审查同时修复两个 Important：generic host status projection 删除恒真 `FAST_PROCEDURAL_CHROME_NODES` 的像素 fallback，始终走 authored `workbench_status_bar.zui`；resize/DPI 两个 template bridge recompute `Result` 不再静默丢弃，而是写入 typed diagnostic。新增 authored-root 与 diagnostic source contracts；`cargo fmt -p zircon_editor -- --check`、scoped `git diff --check` 与静态 hard-cut/diagnostic 检查均 GREEN。当前共享工作树仍被外部 Editor owner 的 `assets.rs` 缺失 imports、`pane_payload` 私有测试路径等 10 个编译错误阻断，故 current-source exact body、Blend Space、governance 和截图 route 尚未复验；三档 composite 截图仍不得刷新或冒充验收，状态保持 `implemented / validation-blocked-external`。

## 后续验收

- S15.5 响应式复合基线已通过；后续 Layout 切片继续以当前共享原子组件和 active-workspace painter 为基线，不在 Blend Space L4 资产恢复局部颜色、字体或绝对坐标。
- 如继续逼近参考图，优先扩展共享原子（网格绘制、视口图像/热力图、折叠标题栏）和对应 native painter，而不是在本复合资产内添加截图专用像素。
- 当前视觉仍是结构化工具面板基线，并非 Unreal/参考图等价验收；下一优先级是通用 sample-grid/canvas primitive、图标与正文层级、宽屏详情密度，而不是继续堆 L4 文本占位。
- Sample Grid 的生产架构、双 retained projection、原生 painter、原子测试和三档真实截图均已通过；下一优先级是继续以共享原子实现真实 preview image/heatmap 和更接近参考图的 samples/details 密度，不在 L4 恢复伪行、局部像素定位或控制 ID 特例。
- Preview Viewport、Wide samples 与 Preview Timeline 已通过；当前切片把参考图的 Sample Weights / Validation Log 拆为共享状态、进度与诊断复合组件，下一步进入 Windows 测试与三档截图阶段，不恢复 Blend Output 伪表。
- Sample Weights / Validation Log 三档验证已完成；下一原子切片把 Timeline 的 `Rec/Play/Pause/Prev/Next/Loop` 文本按钮替换为共享 `WorkbenchTransportControls` 图标按钮组，先对齐 Unreal `Animation.PlayControlsButton` 的 20px glyph、2px content padding、透明 normal surface 与低强调 foreground，再复验三档相对宽度。

## 产出记录与时间

| 里程碑 | 切片 | 状态 | 完成日期 | 证据 |
|---|---|---|---|---|
| S15.5 | Blend Space 密集自适应复合基线 | `completed-baseline / visual-refinement-active` | 2026-07-12 | SectionTitle `10/10`；responsive tier `1/1`；splitter visual `1/1`；Blend Space `4 passed / 1 ignored`；ignored screenshot `1/1`；640/900/1260 三档图仅位于 `docs/tests/editor`；target 扫描为 0。 |
| S15.5 | Typed Sample Grid 原子与真实视觉复验 | `completed-atom / visual-refinement-active` | 2026-07-12 | 当前源码构建通过；Sample Grid projection/painter `5/5`；Blend Space `4 passed / 1 ignored`；ignored screenshot `1/1`；ZUI governance `72/72`；三档截图仅位于 `docs/tests/editor`；Cargo target 截图扫描为 0。 |
| S15.5 | Typed Weight Heatmap 原子与 Wide 组合 | `completed-atom / visual-refinement-active` | 2026-07-12 | Weight Heatmap projection/painter `4/4`；Blend Space `4 passed / 1 ignored`；ignored screenshot `1/1`；ZUI governance `72/72`；640/900 响应式图 hash 未变、1260 Wide 图出现真实热场；Cargo target 截图扫描为 0。 |
| S15.5 | Preview Viewport 图像原子与 Wide samples 密度 | `completed-atom / visual-refinement-active` | 2026-07-13 | 当前源码构建 exit 0；Blend Space `6 passed / 1 ignored`；ZUI governance `72/72`；ignored screenshot `1/1`；三档图只位于 `docs/tests/editor` 且 target 扫描为 0；人工确认 Wide 展开真实 mannequin viewport 与四行 samples 表。 |
| S15.5 | Typed Preview Timeline 原子与输出区替换 | `completed-atom / visual-refinement-active` | 2026-07-13 | RED 合同、typed DTO、双投影、共享 host token painter、Runtime Text、Blend Space toolbar/timeline 组合和既有 action route 迁移已完成。Windows 当前源编译曾 exit 0；Timeline `5/5`；Blend Space `7 passed / 1 ignored`；截图导出 `1/1`，三档图仅写入 `docs/tests/editor`。ZUI 相关筛选 `92/93`，唯一失败为既有 `runtime_host.rs` 文件缓存源码标记与测试期待漂移，不属于本原子；所有 ZUI governance 子项均通过。随后结构拆分复编译被并发 Runtime owner 的 `SceneRendererCore.deferred_scene_resources` 字段漂移阻断，最后可用 Editor 二进制的 focused tests 仍保持通过。 |
| S15.5 | Sample Weights / Validation Log 共享复合组件与 Wide 三列组合 | `completed-composites / visual-refinement-active` | 2026-07-13 | RED 合同先确认两个共享 composite 资产缺失；实现 `WorkbenchSampleWeights`（Caption + ProgressBar + Checkbox）与 `WorkbenchValidationLog`（Caption + StatusItem + Button），全部相对 stretch/min/max 布局、无局部 RGB/font override；Windows 当前源码 lib-test 构建 exit 0，Blend Space `8 passed / 1 ignored`，截图导出 `1/1`；640 保持输出区折叠、900 保持 Timeline-only、1260 展开 Timeline/Weights/Log；三档图仅位于 `docs/tests/editor`，repo 与 D/E/F Cargo target 截图扫描为 0。 |
| S15.5 | Unreal-density Transport Controls 共享复合组件 | `completed-composite / visual-refinement-active` | 2026-07-13 | 先增加源码合同与 Wide timeline 最小相对宽度门禁；实现六个 `WorkbenchIconButton` 原子的共享 `WorkbenchTransportControls`，以 Zircon 自有 Record/Play/Pause/Previous/Next/Loop SVG 对齐 Unreal Starship 20×20 playback brush、2px padding 和 28px 紧凑 lane；Blend Space 删除六个易裁切文本按钮并消费共享 composite，Sample Weights/Validation Log 宽度改为有界压缩以把剩余宽度返还 Timeline。Windows 当前源码 lib-test 构建 exit 0；Blend Space `9 passed / 1 ignored`，ZUI governance `73/73`，ignored screenshot `1/1`；三档图只位于 `docs/tests/editor`，target 扫描 0。 |
| S15.5 | Blend Space Details 领域复合组件与 L4 结构收敛 | `completed-composite / visual-refinement-active` | 2026-07-13 | 先把 Wide Details 的共享资产、三个 SectionTitle 分类、十条既有 route 和无局部 RGB/font 合同写入测试；新增 `WorkbenchBlendSpaceDetails`，用 SectionTitle/TableRow/PropertyRow/Field/Dropdown 原子组合 Samples、Axis Settings、Sample，保留 sample click 与 edit/commit routes；L4 workspace 从 579 行收敛到 415 行，只挂载一个 stretch Details composite，消除十六个内联领域节点与 Caption/divider 伪分类。Windows 当前源码构建 exit 0；Blend Space `9 passed / 1 ignored`，ZUI governance `73/73`，截图导出 `1/1`；640/900 hash 保持、1260 显示三段平直 Details 分类，target 扫描 0。 |
| S15.5 | Transport Controls 真实 action route 与 retained 状态反馈 | `completed-interaction / visual-refinement-active` | 2026-07-13 | 六个 IconButton 分别接入 `AnimationTransport{Record,Play,Pause,Previous,Next,Loop}` Click event、唯一 binding 与已注册 preview action；Workbench bridge 负责 Record/Loop 独立 toggle、Play/Pause 互斥、Previous/Next timeline 首尾跳转，以及 Runtime Text preview/status 反馈，不引入局部 callback 或 control-id painter 特例。当前源码 Windows lib-test 构建 exit 0，两项交互测试 `2 passed / 0 failed`；原生扩展契约为 `44 modules / 886 events / 886 routed / 886 unique / 886 bindings / 886 actions`。本切片不改变默认视觉像素，三档基线图继续只位于 `docs/tests/editor`。 |
| S15.5 | PropertyRow value projection 与共享 PropertyEditorRow | `completed-composite / visual-refinement-active` | 2026-07-13 | RED 先复现 v2 根布局的 `container.kind = None`；组件根布局和 Slot mount 均改为递归合并，显式 instance/caller 叶子值保持最高优先级。Runtime frame 回归 `1/1`、legacy Slot 回归 `1/1`；Editor 当前源码构建 exit 0，严格几何 `1/1`，三行 260px PropertyRow 的 editor 均从 x=1109 开始并取得 151px value 宽度；ZUI governance `74/74`，完整 Blend Space `13 passed / 0 failed / 1 ignored`，截图导出 `1/1`。三档图只位于 `docs/tests/editor`，repo 与 D/E/F Cargo target 扫描均为 0；整体继续视觉优化。 |
| S15.5 | PropertyEditorRow 强制审查补强 | `completed-review-fix / visual-refinement-active` | 2026-07-13 | 旧二进制 RED 证明 legacy Slot placeholder 仍保留字面量 `$value_slot_stretch`；最低层修复先组合 component token、解析 component parameter，再通过共享 `resolve_value_map` 捕获 placeholder layout，并让 caller `weight=2` 叶子最后覆盖。Text 05 补齐 `UiTextDistanceFieldEffects` canonical import 后，Windows 独立集成测试 `1/1`。Frameworks05 收束 bridge 后又真实暴露 Editor M1 测试的 11 处退役 `builtin::RuntimeTargetMode`；消费端硬切 canonical platform owner 后，当前源码 Editor `--no-run` exit 0，新二进制通过 provider `2/2`、PropertyRow projection/geometry `2/2`、Blend Space `13/0/1`、governance `74/74`、截图路由 `1/1`。 |
| S15.5 | WorkbenchPanelHeader 双槽复合控件与底部三面板迁移 | `completed-review-fix / visual-refinement-active` | 2026-07-13 | Unreal Starship `Header/Panel`、SlimToolbar 与 trailing action 结构作为依据；共享 `composites/chrome/WorkbenchPanelHeader` 单独拥有 2px gap、28–30px 相对 HorizontalBox、stretch title slot 与 multiple actions slot，不拥有文字/图标/事件，也无 RGB/font/position 覆写。强制审查删除三消费者局部 gap/height 覆写，并以真实 production bridge 验证三个 title 与 Timeline action 的父子/frame 合同。Windows 当前源码生成 3,156-test binary；新回归 `1/1`、Blend Space `15/0/1`、ZUI governance `75/75`、截图 `1/1`、原生扩展 `44 modules / 886 routes` 全绿。三档图只位于 `docs/tests/editor`，target 扫描为 0；整体继续视觉优化。 |
| S15.5 | WorkbenchPanelHeader 中心三面板迁移 | `completed-composite / visual-refinement-active` | 2026-07-13 | 沿用已验收的共享双槽 header，把 Sample Grid 局部 HorizontalGroup、Blend Preview 独立 SectionTitle、Weight Heatmap 独立 Caption 迁到 `WorkbenchPanelHeader`；Canvas status 进入 actions slot，title slot 接管伸缩并删除纯 fill 节点。三处消费者只保留 stretch width，不覆写 2px gap 或 28–30px 高度；内部 SampleGrid/PreviewViewport/WeightHeatmap 数据与事件不变。RED missing `4`，实现后 source GREEN `0`；Windows 当前源码 build exit 0，源码/production bridge 精确回归 `2/2`，完整 Blend Space `16 passed / 0 failed / 1 ignored`，ZUI governance `75/75`，截图路由 `1/1`。三档图人工确认相对布局、折叠和 28px 标题带无重叠，SHA-256 为 640 `B75F08CA...C832`、900 `6CF6A252...A2EF`、1260 `F448EFB7...4BDB`；图只位于 `docs/tests/editor`，repo 与 D/E/F Cargo target 扫描为 0。整体继续视觉优化，不宣称整窗 Unreal 像素等价。 |
| S15.5 | WorkbenchPreviewViewport mannequin subject fidelity | `completed-primitive-visual / visual-refinement-active` | 2026-07-13 | 按参考图优先修共享预览原子，不放大 L4 面板或增加截图专用坐标。真实 1260x780 native painter RED 量得中央中性亮色 subject pixels=`430`；共享 SVG 将 mannequin+shadow 组成同中心的 `preview-subject` 并作 1.15 倍矢量缩放，同一测试达到 `>=520` 并转绿。完整 Blend Space `17/0/1`、ZUI governance `75/75`、截图路由 `1/1`；人工确认 Wide mannequin 更清晰且不裁切，640/900 因响应式隐藏预览而哈希不变。三图只在 `docs/tests/editor`，当前哈希为 640 `B75F08CA...C832`、900 `6CF6A252...A2EF`、1260 `7720A63E...D928`；整体仍不宣称 Unreal 像素等价。 |
| S15.5 | Workbench toolbar Button authored icon-label spacing | `completed-primitive / visual-refinement-active` | 2026-07-14 | Unreal Starship SlimToolbar 作为共享 spacing/padding 依据；native RED 记录 authored `4` 被错误替换成 actual `7`，最低层 painter 改为优先消费 typed layout projection、无值时回退 host token。focused `1/1`、完整 retained Button painter `53/53`、Blend Space `17/0/1`、截图 route `1/1`；Runtime Text ink width与右侧 inset 同时受测，人工确认顶栏三命令无重叠，三档图仅刷新到 `docs/tests/editor`，target 扫描 0。整体视觉优化继续 active。 |
| S15.5 | WorkbenchStatusItem compact marker and Runtime Text width | `completed-primitive / visual-refinement-active` | 2026-07-14 | Unreal Slate status-strip compact padding作为依据；native RED 记录 marker x=`24` 与 Warnings 可用 `68 < 70.79818`，最低层 metric 改为单个 `gap_m=8` inset。focused `1/1`、status controls `29/29`、局部截图 `1/1`、governance `75/75`、Blend Space `17/0/1`、三档截图 `1/1`；非 primary 三状态文字完整，primary 动态摘要继续作为后续 composite 切片，target 扫描 0。 |
| S15.5 | WorkbenchStatusBar adaptive primary Runtime Text slot | `implemented / validation-blocked-external` | 2026-07-14 | 两轮独立审查先后捕获 active-task 640 overflow 与 tier 下界 481/641 overflow；最终 tier priority 与 task 160–224 bounded compression 覆盖 420/480/481/640/641/900/1259/1260，静态下界容量、唯一 Stretch/TOML/source checks 通过，最终复审 Ready。`ShapedGlyph.font_instance_id=None` 已让共享构建越过原 E0063；最新受管 gate 被活跃 Runtime Text owner 的 private batch fields E0616 阻断，尚无 focused body/new screenshot，不覆盖上一切片哈希。 |

### 2026-07-13 Preview Timeline 验证边界

- `cargo fmt -p zircon_editor -- --check` 与 scoped `git diff --check` 均 exit 0。
- `workbench_window_projection.rs` 已按职责拆出 `host_value_toml.rs`，父 owner 为 961 行；对应跨计划 failure 已回迁 Frameworks 06 的 `fixed-2026-07-13-workbench-projection-file-budget-regression.md`。
- Preview Timeline 切片的三档真实截图已人工检查：640 保持主 Sample Grid，900 展开 Preview Timeline，1260 同时展开 mannequin Preview、Weight Heatmap、右侧 Samples/Details 与 Preview Timeline；当前 Sample Weights / Validation Log 实现后的新截图仍待本轮 testing stage 刷新。
- target 截图扫描为 0；最终计划记录审计有 6 个其他 owner 的存量 violation（原 5 项之外新增 Editor09 直接记录超限），本切片新增 0 项且未越权修改。

### 2026-07-13 Sample Weights / Validation Log 验证

- 受管 Windows 当前源码 `cargo test -p zircon_editor --lib --no-run --locked --jobs 1` 构建 exit 0；生成的 Editor test binary 执行 Blend Space 组为 `8 passed / 0 failed / 1 ignored`，新 composite 合同测试通过。
- ignored screenshot route 为 `1 passed / 0 failed`，刷新 [`640×520`](../../../tests/editor/editor-window-m3-blend-space-workbench-640x520.png)、[`900×620`](../../../tests/editor/editor-window-m3-blend-space-workbench-900x620.png)、[`1260×780`](../../../tests/editor/editor-window-m3-blend-space-workbench-1260x780.png)。人工复核确认响应式关系：Narrow 不显示底部输出区，Regular 只显示 Preview Timeline，Wide 显示 Timeline / Sample Weights / Validation Log 三列；图像均位于 `docs/tests/editor`。
- 完整 ZUI governance 首轮发现测试目录白名单尚未登记新的 `composites/{animation,feedback}` 分类（其余 `71/72` 通过）；已在治理测试中加入这两个明确功能目录。该源码级修复随当前修改保留，需在下一次当前源码 binary 中复验为 `72/72`，不把旧 binary 的同一失败误记成仍未修复。
- `cargo fmt -p zircon_editor -- --check`、scoped `git diff --check`、三份 ZUI TOML 解析、composite 局部 RGB/font override 扫描均通过；测试 owner 从 879 行拆分为 747 行主文件 + 135 行 composite 合同子模块，保持结构预算。
- repo `target` 与 `D:/E:/F:/cargo-targets` 的 `*blend-space*.png` 扫描为 0。当前视觉已显著逼近参考图底部诊断密度，但仍处于 `visual-refinement-active`，后续继续收敛正文裁切、按钮文字完整度与交互 route，不宣称 Unreal 像素等价。

### 2026-07-13 Transport Controls 验证

- 受管 Windows 当前源码 `cargo test -p zircon_editor --lib --no-run --locked --jobs 1` 构建 exit 0；聚焦 composite 合同 `1/1`、Unreal scrub-density 治理 `1/1`、Blend Space 组 `9 passed / 0 failed / 1 ignored`。
- 完整 `zui_asset_governance` 为 `73 passed / 0 failed / 0 ignored`；新的 `composites/animation` owner、六个 IconButton 原子、20px glyph、四边 2px padding 与所有 import/reachability/layout 合同均通过。
- ignored screenshot route 为 `1 passed / 0 failed`。640 图 SHA-256 `35F47D77E88E1B7B415548A9656A7B96EA26E6A232E61DDDDAA9577726EC83CB` 保持不变；900 更新为 `E737AD33E1AFE7A8A25601369D15E4AFA44B1765D4AD67B776FBB3FD974DC88F`；1260 更新为 `4607215031DC3946BD5C7D36766B90ED9856E41B24EF619FC5E6F828F50D5A57`。人工复核确认文本缩写已被六个清晰 transport glyph 替换，900 Timeline 不再裁按钮文字，1260 Timeline/Weights/Log 三列仍保持相对布局。
- `cargo fmt -p zircon_editor -- --check`、两份 ZUI TOML parse、六份 SVG XML parse、scoped `git diff --check` 均 exit 0；repo `target` 与 D/E/F Cargo targets 的 `*blend-space*.png` 扫描为 0。
- 当前复刻仍未达到 Unreal 整窗等价：下一优先级应继续把右侧 Details 从平铺 property rows 收敛为共享 SectionTitle + field/dropdown 组合，并补齐 transport actions 的业务 route；本切片不以 L4 control-id 特例或绝对坐标掩盖这些后续项。

### 2026-07-13 Blend Space Details 验证

- 受管 Windows 当前源码 `cargo test -p zircon_editor --lib --no-run --locked --jobs 1` 构建 exit 0；首轮旧断言因仍把 L4 文件当作 TableRow 与 sample route owner 而 2/10 失败，断言改为跨 workspace + Details composite 检查真实所有权后，Blend Space 组为 `9 passed / 0 failed / 1 ignored`。
- 完整 `zui_asset_governance` 为 `73 passed / 0 failed / 0 ignored`；新资产通过可达 import、单根树、functional folder、事件 id/route 唯一、相对轴与无局部 style override 合同。
- ignored screenshot route 为 `1 passed / 0 failed`。640/900 SHA-256 保持 `35F47D77...` / `E737AD33...`，证明 Narrow/Regular 响应式输出未被 Wide Details 重构影响；1260 更新为 `4FFE29C9CF9FBF029D56D308FB8F490C9FE23C572335F6BBB29EABC5732C6DBB`，人工复核显示 `SAMPLES (8)`、`AXIS SETTINGS`、`SAMPLE` 三个平直共享 SectionTitle，字段仍在右栏边界内。
- `cargo fmt -p zircon_editor -- --check`、两份 ZUI TOML parse、scoped `git diff --check` 均 exit 0；L4 workspace 415 行、Details composite 144 行，符合 structure convention 的职责和文件预算。
- repo `target` 与 D/E/F Cargo target 的 `*blend-space*.png` 扫描为 0；三张验证图仍只在 `docs/tests/editor`。下一优先级是把 Details 的 Asset/X Axis/Interpolation 改成带 label/value 双列的共享 property editor row，并为 transport buttons 接回真实 action routes。

### 2026-07-13 Transport Actions 验证

- RED 合同先确认六个 Transport Click event 与六个 dotted action id 均不存在；实现后静态 route 清单 `12/12`，原生扩展模块验证为 `modules=44 events=886 routed=886 unique=886 bindings=886 extensionPreviewActions=886`。
- 受管 Windows 当前源码 `zircon_editor --lib --no-run` 构建 exit 0；新二进制中的 `blend_space_transport_buttons_dispatch_unique_registered_actions` 与 `blend_space_transport_actions_update_independent_preview_state` 均为 `1 passed / 0 failed`，验证六路唯一派发、Record/Loop 独立状态、Play/Pause 互斥、Previous/Next 时间轴首尾跳转及 Runtime Text 状态反馈。
- `cargo fmt -p zircon_editor -- --check`、Node verifier syntax、scoped `git diff --check` 和 ZUI governance `73/73` 均通过。此动作切片没有改动默认静态布局，因此不伪造新的视觉差异；既有 640/900/1260 基线仍只位于 `docs/tests/editor`，整体状态继续 `visual-refinement-active`。

### 2026-07-13 Property Editor Row 实现边界

- 上层症状是 Wide Details 的 Horizontal、Vertical、Grid divisions 等 PropertyRow 只显示标签。候选底层包括 ZUI authored props、component expansion、retained host value extraction、full-window projection 与 property-row painter；检查确认前三者已有值且 painter 已支持双列，最低断点位于 `projected_workbench_value_text` 未把 `property-row`/`PropertyRow` 视为 display-value owner。
- 修复保持共享正常路径：projection 增加 property-row 身份；新 `WorkbenchPropertyEditorRow` 只提供有界 name column 与 stretch value slot，不复制 Field/Dropdown 绘制或事件。Blend Space Details 用三行复合组件承载 Asset、X Axis、Interpolation，字段 route 身份不变。
- RED missing `4`，实现后 source GREEN `4/4`；定向 `rustfmt --check` 与 scoped `git diff --check` exit 0。当前源码 Editor lib-test 构建 exit 0，projection、composite folder governance 与严格几何聚焦测试均通过。
- 第一次当前源码 Editor 严格几何测试复现三组 Field/Dropdown 与整行 frame 完全相同，证明问题不在 Blend Space L4 组合。沿 production bridge 向下追踪后确认实际编译路径为 Runtime UI v2；其 `push_slot_children` 在删除 `Slot` 占位节点时只保留调用方 child mount，丢失占位节点 `layout`。
- 修复位于两个共享编译层：legacy `component_instance_expander` 与 production v2 `component_instancer` 都先把占位布局转成 mounted-child slot metadata，再递归合并调用方 slot，调用方叶子值保持最高优先级。继续向下的 Runtime frame RED 表明 v2 instance 的局部 width/height 又整表替换了 prototype root layout，`container.kind` 实际为 `None`；生产修复改为根布局递归合并，保留原型 `HorizontalBox` 结构并让 instance 显式叶子覆盖。
- Runtime 当前 Windows focused 证据为 legacy Slot `1/1`、v2 metadata + 真实 surface frame `1/1`。Editor 严格几何 `1/1`：三行 row 均为 `x=1000,width=260`，Dropdown/Field 均为 `x=1109,width=151`，固定标签列、4px gap 与剩余 value column 精确成立。
- PropertyRow projection `1/1`、composite functional folder governance `1/1`、完整 `zui_asset_governance` `74 passed / 0 failed`。完整 Blend Space 组首次暴露 Wide 底部诊断列挤压时间轴到 223px；保持相对 HorizontalBox 结构，把 Sample Weights/Validation Log 的有界固定宽度收敛为 178/212 后，时间轴恢复至少 246px，完整复验为 `13 passed / 0 failed / 1 ignored`，没有放宽测试或加入绝对坐标。
- ignored screenshot route `1 passed / 0 failed`，刷新 [`640×520`](../../../tests/editor/editor-window-m3-blend-space-workbench-640x520.png)、[`900×620`](../../../tests/editor/editor-window-m3-blend-space-workbench-900x620.png)、[`1260×780`](../../../tests/editor/editor-window-m3-blend-space-workbench-1260x780.png)。SHA-256 依次为 `CA9BD4C1938DB9F96BDD35EF1507F393FAAD115BA7C86D068FD6C69EC2534BEA`、`5B0221F0311BF82F0A3D818CC3BC4FDEEE550499A3EDF68375FC53F28CE8CDFF`、`C499E211792DBFF5AE94BD9B39C2901C7666E39E600074CE0035CF3BB002FBE6`。
- 人工复核确认 1260 Wide 的属性标签/编辑器双列分离、Preview/Heatmap/Details 边界与六按钮 Timeline；900/640 按 tier 收起右栏和底部复合区，没有重叠或越界。repo `target` 与 D/E/F Cargo target 的 `*blend-space*.png` 扫描均为 0。当前只完成 Property Editor Row 故障切片验收，预览资产精细度、顶栏和整体参考图等价仍属于后续 `visual-refinement-active`。
- 强制代码审查没有发现 Critical；确认 UI v2 根 layout 递归合并、真实 surface frame、Runtime Text projection 与 route 保留正确。审查发现 legacy Slot placeholder layout 复制发生在 component token/参数解析之前。独立集成测试的旧实现 RED 为 `Some("$value_slot_stretch") != Some("Stretch")`；生产修复把 component token composition、component param resolution 前置，并在捕获 placeholder layout 时调用共享 `resolve_value_map`，caller width `weight=2` 叶子继续最后合并。
- Text 05 在 canonical Runtime Interface import 链补齐 `UiTextDistanceFieldEffects` 后，E 盘受管紧凑通道执行 `ui_component_slot_layout_contract` 为 `1 passed / 0 failed`。Frameworks05 收束 plugin bridge 后，完整 Editor 构建继续暴露 M1 native-provider 测试 11 处退役 `builtin::RuntimeTargetMode`；该测试消费端硬切 `core::framework::platform::RuntimeTargetMode`，没有恢复 re-export 或 alias。最终受管 `zircon_editor --lib --no-run` exit 0；3153-test 新二进制的 builtin/native provider `2/2`、PropertyRow projection/严格几何 `2/2`、Blend Space `13/0/1`、governance `74/74`、三档截图 `1/1` 全部通过。
- 最终截图 SHA-256 保持 640 `CA9BD4C1938DB9F96BDD35EF1507F393FAAD115BA7C86D068FD6C69EC2534BEA`、900 `5B0221F0311BF82F0A3D818CC3BC4FDEEE550499A3EDF68375FC53F28CE8CDFF`、1260 `C499E211792DBFF5AE94BD9B39C2901C7666E39E600074CE0035CF3BB002FBE6`；人工复核仍接受三档响应式边界但不宣称整窗 Unreal 等价。图只位于 `docs/tests/editor`，repo 与 D/E/F Cargo target 命中均为 0。

### 2026-07-13 Workbench Panel Header 验证

- Windows 受管当前源码 `zircon_editor --lib --no-run` 构建 exit 0；新二进制的 `workbench_panel_header_exposes_compact_title_and_action_slots` 与 `blend_space_bottom_panels_compose_shared_panel_headers` 为 `2 passed / 0 failed`，完整 Blend Space 组为 `14 passed / 0 failed / 1 ignored`，完整 ZUI governance 为 `75 passed / 0 failed`。
- ignored screenshot route 为 `1 passed / 0 failed`，刷新 [`640×520`](../../../tests/editor/editor-window-m3-blend-space-workbench-640x520.png)、[`900×620`](../../../tests/editor/editor-window-m3-blend-space-workbench-900x620.png)、[`1260×780`](../../../tests/editor/editor-window-m3-blend-space-workbench-1260x780.png)。SHA-256 依次为 `CA9BD4C1938DB9F96BDD35EF1507F393FAAD115BA7C86D068FD6C69EC2534BEA`、`DEB10C0798C16D2F8BA686BDDADF75435FE85701455D53C37C7A986A6A348F0A`、`32FA428CA45E0B896CF609CCA4B32C838A6F10A3051EC206886C2E13B3A2C32A`。
- 人工复核确认 900 的 Preview Timeline 标题与 `1.0x` trailing action 保持在同一紧凑标题带，1260 的 Timeline / Sample Weights / Validation Log 三个标题带顶部对齐且无重叠；640 继续按 tier 收起底部输出区。该提升是可见但局部的，不宣称顶栏、预览资产或整窗已达到 Unreal 像素等价。
- 原生扩展合同为 `44 modules / 886 events / 886 routed / 886 unique / 886 bindings / 886 actions`；定向 rustfmt、scoped `git diff --check` 与四份 ZUI TOML 解析均 exit 0。repo `target` 与 D/E/F Cargo targets 的 `*blend-space*.png` 扫描为 0，验证图只位于 `docs/tests/editor`。
- 强制审查发现 Preview Timeline 实例仍把共享 `gap=2 / height=28–30` 覆写成 `gap=4 / height=30–32`，且原源码字符串合同不能证明 Slot 真实展开。修复后三个消费者都只保留 stretch width，由 `WorkbenchPanelHeader` 单独拥有 gap/height；新增 `blend_space_panel_header_slots_expand_with_one_shared_layout_contract`，通过 production bridge 检查三个 header/title 与 Timeline action 的父子关系和最终 frame containment。
- Runtime Text/SDF owner 补齐 `Pod/Zeroable/UiPaintPayload` 后，Frameworks 05 又把 `RuntimeProfileId` 消费端硬切到 canonical `core::framework::project`，当前源码随后在 Windows 受管 retained pool 完成 8m58s lib-test 重建并生成 3,156-test binary。新增 production-bridge 回归 `1/1`，完整 Blend Space `15 passed / 0 failed / 1 ignored`，ZUI governance `75/75`，ignored screenshot route `1/1`。
- 审查后截图 SHA-256 为 640 `CA9BD4C1938DB9F96BDD35EF1507F393FAAD115BA7C86D068FD6C69EC2534BEA`、900 `A4094F00C5E05C9A022DE8419053ABF24795BB822CF1ABA0C45767BAC59EE524`、1260 `D164F50B59C10A28F01321EDFCB205A55E5835120C3C208C6B8F9D974E1AB75F`。人工复核确认 640 正确折叠底部区，900 Timeline 标题与 `1.0x` 同行且无重叠，1260 Timeline / Sample Weights / Validation Log 三个 28px 标题带顶部对齐。原生扩展合同仍为 `44 modules / 886 events / 886 routed / 886 unique / 886 bindings / 886 actions`；repo 与 D/E/F Cargo target 截图扫描为 0。
- 中心三面板迁移在当前源码 3,157-test binary 上完成 testing stage：`blend_space_center_panels_compose_shared_panel_headers` 与 production-bridge `blend_space_panel_header_slots_expand_with_one_shared_layout_contract` 各为 `1/1`；完整 Blend Space 为 `16 passed / 0 failed / 1 ignored`，完整 ZUI governance 为 `75 passed / 0 failed`。Sample Grid、Blend Preview、Weight Heatmap 的 title 均展开在共享 28px header frame 内，Canvas status actions slot 与 title 不重叠。
- ignored screenshot route 为 `1 passed / 0 failed`，最终 SHA-256 为 640 `B75F08CA3555DBA99D75939E20CDE478B19096EF94D4867406DC1521701CC832`、900 `6CF6A2524B4DB068E4333FE29180F43041EF7095A811248D3C91568CB9F0A2EF`、1260 `F448EFB78808836ACC10530A55E292CCF2CC94738EC348C31D37B3880BEA4BDB`。人工复核确认 640 只保留主 Sample Grid，900 展开 28px Preview Timeline header 且 controls/timeline 不重叠，1260 同时展开统一的 Sample Grid / Blend Preview / Weight Heatmap / bottom diagnostic headers；仍不宣称顶栏、预览模型或整窗达到 Unreal 像素等价。三张图只位于 `docs/tests/editor`，repo 与 D/E/F Cargo target 的 `*blend-space*.png` 命中为 0。
- 当前源码首次向上构建准确捕获 ZrVM `HostRegistry` 已硬切为 `register_capability -> Result` 与 `resolve(handle)`，而 Editor M1 消费端仍使用 infallible register/退役 `capability(handle)`。Editor bridge 现把注册错误转成 capability-specific diagnostic 并停止继续注册，测试消费端改用 canonical `resolve`，没有恢复 alias、fallback 或 compatibility shim。重建 exit 0；HostRegistry handle、builtin provider、native provider、Runtime Text HUD framebuffer 四个 handoff 精确门禁均为 `1 passed / 0 failed`。
- 强制代码审查无 Critical，发现一个 Important：中心三面板源码合同把 `LF` 写死在跨行 substring，在 Windows `core.autocrlf=true` 的 fresh checkout 会因 `CRLF` 假失败。测试已改为解析 `toml::Value`，逐 header 验证 `component`、child node、`slot.name`，并从 nodes table 验证旧 fill 不存在；不再依赖行结束符或松散字符串匹配。修复后 current-source exact `1/1`，完整 Blend Space `16/0/1`、governance `75/75`；独立复审最终 `Ready`，无 Critical/Important。

### 2026-07-13 Preview Viewport 人物可读性验证

- 真实生产窗口与 native painter 的 RED 先量得 1260x780 preview frame `(791,164,208,86)` 内中央 subject pixels=`430`，低于 `520` 最小合同；测试不读取 SVG 源码或伪造像素。
- 生产修复只调整共享 `animation_mannequin_viewport.svg`：mannequin 与投影进入同一个 `preview-subject` 矢量组，以保持中心的变换放大到 1.15 倍；floor grid、lighting panels、axis gizmo、Runtime Text overlay 和 responsive frame 均不变，没有增加 feature-local painter、绝对窗口定位或 SVG text。
- 当前 Windows Editor binary 的新 exact 为 `1/1`，完整 Blend Space 为 `17 passed / 0 failed / 1 ignored`，完整 ZUI governance 为 `75/75`；Editor M1 builtin/native provider 为 `2/2`，原始 Runtime Text HUD framebuffer exact 为 `1/1`。
- ignored screenshot route 为 `1/1`。人工复核确认 1260 Wide mannequin 更醒目且四肢/投影未裁切；640/900 因 tier 隐藏该预览而保持原哈希。SHA-256 为 640 `B75F08CA3555DBA99D75939E20CDE478B19096EF94D4867406DC1521701CC832`、900 `6CF6A2524B4DB068E4333FE29180F43041EF7095A811248D3C91568CB9F0A2EF`、1260 `7720A63EB291FB2D1E0166EE69EAB242409D23D936705DAA3D91FC61FE16D928`。
- 三张验证图只位于 `docs/tests/editor`；本切片没有把图写入 Cargo target。该原子视觉切片完成，但顶栏、Preview 真实渲染、宽屏细节密度与整窗 Unreal 等价继续保持 `visual-refinement-active`。
- 强制审查无 Critical，发现一个 Important：`blend_space_workspace.rs` 已增长到 883 行，新增 preview 产品像素测试继续扩大超过 800 行的测试 root。按结构规范把 PreviewViewport 像素合同/helper 与同职责的 narrow secondary-pane 折叠合同提取到 `blend_space_workspace/preview_viewport.rs`；root 实际降至 786 行、child 为 112 行。`cargo fmt --check` 与 scoped `git diff --check` 通过；审查同时量得最终主体命中 551 pixels 且 bbox 集中在 mannequin，排除 floor/background 假绿。
- 审查后 current-source Cargo 复验在共享 Runtime 全体重链阶段持续约 20 分钟，只完成新的 `zircon_runtime` rlib/cdylib 并输出外部 unused-import warnings，未产出新的 Editor test binary，也未执行测试体；因此结构迁移后的 Cargo 结果保持 `pending`，不把旧 3,158-test binary 冒充 post-split green。该迁移只移动测试/helper 与收窄 `pub(super)` fixture 可见性，生产 SVG、ZUI 和三档截图未变化。
