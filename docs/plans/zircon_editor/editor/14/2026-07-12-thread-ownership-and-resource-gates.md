---
related_code:
  - zircon_editor/src/core/jobs
  - zircon_editor/src/core/context/builder.rs
  - zircon_editor/src/ui/host/editor_manager.rs
  - zircon_editor/src/ui/host/export_cargo_process.rs
  - zircon_editor/src/ui/host/editor_manager_plugins_export/export_build/wizard/tests/panel_session.rs
plan_sources:
  - docs/plans/zircon_editor/editor/14-threading-and-job-scheduling.md
tests:
  - cargo test -p zircon_editor --lib --locked core::jobs::tests -- --test-threads=1
  - cargo test -p zircon_runtime --lib --locked core::runtime::tasks -- --test-threads=1
  - cargo test -p zircon_editor --lib ui::host::editor_manager_plugins_export::export_build::wizard::tests::panel_session::export_wizard_panel_session_poll_finishes_queued_prestart_cancellation --locked -- --exact --test-threads=1
  - cargo test -p zircon_editor --lib ui::host::export_cargo_process::tests::cargo_capture_and_poll_complete_on_a_single_runtime_worker --locked -- --exact --test-threads=1
doc_type: milestone-detail
---

# Editor 14 线程所有权与资源门禁产出归档

## 产出记录与时间

| 里程碑 | 切片 | 状态 | 完成日期 | 完成项目与证据（命令输出 / 文件 / 测试名） |
|---|---|---|---|---|
| M1 | 前置 API 与优先级事实复核 | `计划纠偏完成-已进入实现` | 2026-07-11 | 实现前确认 `JobScheduler::{schedule,schedule_after,wait_all}` 与 `JobHandle` 依赖回调可直接支撑 Editor 门面；当时 `EditorContext` 仅持 bus/events，`EditorMessagePayload` 仅有 Document/Transaction/Mode/Focus/Custom。复核 `TaskPoolThreadAssignmentPolicy` 仅含 min/max/percent，原计划“三级优先级直接映射现枚举”失实，现硬切为 Editor 准入队列优先级，不新增 runtime 兼容 API、不自建线程池；后续 1.1/1.2 已按该结论实现。 |
| M1 | 1.1 调度门面与取消协议 | `完成-局部门禁通过` | 2026-07-11 | 新建 `zircon_editor/src/core/jobs/` 文件夹模块：`EditorJobSystem/Spec/Ticket`、typed `JobId/JobError`、`CancellationToken/JobContext`；直接包装 runtime `JobScheduler::schedule_after`，类别配额默认 Thumbnail=2、Export=1、Import=runtime parallelism，互斥组以前序 `JobHandle` 链式串行，优先级按 Interactive→Normal→Background 准入；无 Editor 自建线程池、无旧 API shim。TDD RED 为计划符号缺失；最终 focused GREEN 与 1.2 合并执行 12/12，其中调度合同 10 项覆盖错误依赖完成顺序且 ticket 不悬挂（日志 `E:/cargo-targets/zircon-editor-assets-content-scroll-hover-validator-0710/editor-jobs-m1-final-focused.log`）。 |
| M1 | 1.2 回流泵、Context 服务位与双态 ticket | `完成-局部门禁通过` | 2026-07-11 | 增加 `JobEvent/JobEventKind`、`TOPIC_JOB`、`EditorMessagePayload::Job`，worker 仅写无界 MPSC，`pump_events()` 由主线程显式发布到 01 bus；`EditorContextBuilder` 以同一 bus 创建并持有 jobs 服务；`JobTicket::wait/try_take` 共用唯一结果且拉取至多一次。TDD RED 7 个预期缺失符号；最终 focused GREEN 12/12，其中 pump/Context 合同 2 项（`editor-jobs-m1-final-focused.log`）。`EditorJob: Send + 'static` 且 `Output: Send + 'static` 为公开类型边界；编译器在 `submit` 处拒绝非 Send 捕获，不引 trybuild 依赖。 |
| M1 | 外部功能阻塞交接 | `已归档-不在本计划兼容修复` | 2026-07-11 | 首次 GREEN 被 Runtime rich-text E0432 阻塞，已按归属写入 `docs/plans/zircon_editor/editor_ui/03/failure-2026-07-11-retained-text-family-and-subpixel-contracts.md`；UI 03 owner 完成唯一新导出后，上述局部门禁已转绿。Plan 14 未恢复旧路径或增加兼容导出。 |
| M1 | Runtime tasks 向下回归 | `完成-3/3通过` | 2026-07-11 | `cargo test -p zircon_runtime --lib --locked --offline core::runtime::tasks -- --test-threads=1` 为 3 passed / 0 failed / 7489 filtered out（0.24s）；日志 `E:/cargo-targets/zircon-editor-assets-content-scroll-hover-validator-0710/editor-jobs-m1-runtime-tasks.log`。证明 Editor 门面消费未改坏 Runtime scheduler/handle 的 poison recovery 合同。完整 `zircon_editor --lib` 门禁仍沿用 Plan 01 已归档的 133 项 UI03/05/06/08 功能失败，不据此宣称 Plan 14 M1 总门禁完成。 |
| M2 | 当前源码裸线程复核与切片纠偏 | `完成-识别5个owner` | 2026-07-11 | M2 前置 `git grep` 发现计划旧基线“仅 1 处”已漂移：`wizard/controller.rs`、`wizard/execution.rs`、`ui/host/export_cargo_process.rs`、`retained_host/app/build_export_actions/job_queue/worker.rs` 共 4 个直接 `thread::spawn` 调用点；M2.2 守卫审查进一步发现 `retained_host/viewport/viewport_state.rs` 的 `Builder::spawn` 隐藏 owner。M2.1 迁 controller + panel session；M2.2 收编其余四类并把 guard 扩为 direct/Builder/单项与别名 import 全覆盖，白名单为空。 |
| M2 | 2.1 ExportWizard controller / session 硬切 | `实现完成-测试阶段待执行` | 2026-07-11 | `ExportWizardJobController::submit(&EditorJobSystem,...)` 取代旧 spawn/JoinHandle/AtomicBool/JobHandle；新增窄 `controller/{job,completion,poll}.rs`，使用 `JobCategory::Export`、共享 `CancellationToken`、typed `JobTicket` 与业务 `ExportWizardJobEvent`。Panel/retained session 显式注入 Context-owned jobs，retained 主线程 tick 在 wizard poll 前执行 `pump_events()`；插件旧 re-export 同步删除。`EditorJobSystem::cancel(JobId)` 对 pending job 锁外同步发布 Cancelled/ticket 后才公开 completed dependency handle，既不占 worker/类别也不悬挂 after；poll/direct finish 均补 drain 业务终态事件并把取消/失败投影为一致 snapshot。规格审查与质量审查最终均 approved；旧 controller spawn/handle、wizard AtomicBool、生产 default jobs 均 0 命中，M2.2 三文件未改，rustfmt/diff-check clean。Cargo 按 milestone-first 留待 M2 测试阶段。 |
| M2 | 2.2 其余线程 owner 与取消链硬切 | `实现完成-进入测试阶段` | 2026-07-11 | Wizard/Cargo process 与 retained export queue 全部改用 Context-owned `EditorJobSystem`，`ProcessCommandRunner::new(jobs)`、queue typed `JobTicket`、`JobCategory::Export` 与 `CancellationToken` 取代 pipe reader/worker/AtomicBool 旧协议；viewport `Builder/JoinHandle` 同步迁为注入 jobs + typed ticket。共享 `export_process_support/` 以临时文件、每流每轮 64KiB 有界读取、single-worker `join`、双 EOF final drain、跨平台进程树终止和 armed/disarm child RAII 收口阻塞进程；终态 ticket 为真源并在 terminal 前后二次 drain progress。零裸线程 guard 覆盖 direct/Builder/brace/单项/module alias，白名单为空；新增真实 child-tree ready/survivor、viewport typed success/failure/pending-drop-cancel 行为合同。静态扫描旧 owner/API 0 命中，rustfmt/diff-check clean，规格与质量复审最终均 approved；Cargo 与手验由紧随其后的 M2 测试阶段执行。 |
| M2 | 当前 Editor 全量门裸线程守卫与资源收尾 | `未通过-已归档到本功能计划` | 2026-07-12 | 受管 job `520d85713df249afae31661a7697ad07` 中 `editor_production_sources_do_not_create_bare_threads` 失败；全量观察 178 个失败名后长时间无日志且 45 秒 CPU 增量不足 1 秒，test 进程约 3.5 GiB 工作集，最终人工终止并以 coordinator exit 101 归档。责任与验收（`open / 待修复`）见 [`failure-2026-07-12-editor-full-gate-thread-exhaustion.md`](failure-2026-07-12-editor-full-gate-thread-exhaustion.md)；禁止给 guard 加白名单或用 ignore 隐藏泄漏。 |
| M2 | 裸线程守卫范围与调度器所有权最低层修复 | `实现完成-向上门禁被UI03编译阻断` | 2026-07-12 | 查明 guard 误扫 `src/tests/editing/transaction_engine/locking.rs` 中合法锁序并发测试；现仅枚举生产 Rust 源码，规范 `tests/`、`tests.rs`、`*_test.rs`、`*_tests.rs` 明确排除且无白名单，并新增范围回归。同步查明每个 `EditorContextBuilder`/测试 fixture 隐式 `JobScheduler::default()` 会按实例创建 Rayon pool，是全量并行测试线程/内存倍增的最低资源原因；Builder 现强制注入 scheduler，`EditorManager` 复用 `CoreHandle::scheduler()`，删除 `EditorJobSystem::{Default,with_limits,with_bus}`，测试夹具以 `OnceLock` 共享一份 scheduler。静态扫描四个旧构造模式均 0 命中；本计划所触及 Rust 文件逐文件 `rustfmt --check` 通过。定向 Cargo 复验被 UI03 `RichTable*` 唯一导出 E0432/E0282 阻断，已追加到其现有 failure；因此本行不宣称线程合同测试或全量自然结束已绿。 |
| M2 | Scope/Builder 裸线程守卫与终态记录资源收敛 | `实现完成-局部门禁22/22通过` | 2026-07-12 | 守卫由纯子串改为轻量 Rust tokenizer + provenance 扫描，剥离注释/字符串/字符/字节字符，覆盖 direct、leading `::`、module/function/root alias、`Builder::{spawn,spawn_scoped}`、`Scope::spawn`、typed receiver 与 UFCS；本地同名模块、无关 executor、未调用 alias 均有负例，白名单仍为空。`EditorJobRecord` 改为 Awaiting/Scheduled/Terminal，完成、失败、取消立即释放 runtime handle；终态依赖历史以 state 私有常量限制为 256，被 accepted pending 引用时固定，解除后重新收敛；淘汰 ID 返回 typed `ExpiredDependency`，未知/未来 ID 仍 `UnknownDependency`，mutex tail 在终态释放。生命周期测试覆盖 pending pin/release、8-job mutex 链尾归零、64 个快速任务 handle/类别许可归零。受管 job `a51c2a0d5b83461f82c6bbfd73b4b2cd`：22 passed / 0 failed / 3028 filtered out；日志 `D:/cargo-targets/editor14-focused-rerun-20260712.{log,err.log}`。最终一次重编译受同时变化的 Editor UI owner E0432/E0308 阻断，见本计划 failure；不降低本行已取得的局部行为证据。 |
| M3 | 3.2 关停 deadline 与未竟清单后端 | `实现完成-测试阶段待执行` | 2026-07-12 | `EditorJobSystem::shutdown(Instant)` 已硬切为共享 Context job 门面的唯一收尾协议：共享 clone 原子停收，`submit` 返回 typed `ShuttingDown`；广播取消全部 active token，pending ticket 不等类别许可立即 `Cancelled`；condition variable 等待时释放 state mutex，deadline 后按 `JobId` 稳定返回 `{id,label,category}` 未竟清单。合同源码覆盖全完成、协作取消、不可中断超时、submit/shutdown、双 shutdown、cancel/shutdown 三组并发线性化；最终检查 pending/category/scheduled/mutex-tail 全归零。规格与质量复审均 approved，触及文件 `rustfmt --check`、`git diff --check` 通过；按 milestone-first 未运行 Cargo，留待 M3 测试阶段，且现有 UI/Layout 编译失败继续由其功能计划处理。 |
| M3 | 3.1 唯一进度事实源与 retained UI 接线 | `实现完成-测试阶段待执行` | 2026-07-12 | 新增 `core/jobs/progress.rs`，以单一 `BTreeMap<JobId, ActiveJobEntry>` 同时拥有 active 元数据、取消 token、terminal 可见位与进度；`system/state.rs` 的第二份 active map 已硬删除。terminal event 先隐藏 UI，Runtime handle/类别/mutex/state 清理后才物理移除，避免 shutdown 提前返回；pending、scheduled/running cancel 均走同一 token。`EditorHostEventController::job_progress_snapshot()` 提供公开只读任务面板入口，retained 状态栏按最小 JobId 投影且安全处理 0-total/溢出；Export 私有 status-task 事实源及文件硬删除。合同覆盖 pending、共享/排序、progress、完成/失败/取消、running cancel、controller 入口、shutdown 生命周期和并发竞态；复核 P2 后全部 channel/ticket 等待均带 5 秒 deadline。规格与质量复审最终 approved，rustfmt/diff 静态门通过；按 milestone-first 未运行 Cargo。 |
| M3 | 3.3 1000 个后台缩略图任务风暴基线夹具 | `实现完成-测试阶段待执行` | 2026-07-12 | 新增 `background_storm_contract.rs`：1000 个 `Background/Thumbnail` job 在 gate 释放前固定 `scheduled=2/pending=998`，无自建线程/async/pool；逐 JobId 验证严格 `Started -> Progress(1/1,"thumbnail ready") -> Completed`、事件 ID 与 ticket ID 集完全相等、ticket 单次成功，并要求 pending/running/scheduled/mutex-tail 最终全零。夹具记录 submit、pump+delivery tick、nonempty tick 与 batch 的 P50/P95/max，输出 `EDITOR_JOB_STORM_BASELINE ... numeric_budget=undefined`；runtime/03 无数值预算，runtime/07 `<20%` 仅保留给测试阶段两次同命令可重复性比较，不伪造 SLA。1ms 节拍在计时区外并将 watchdog 样本量限制在约 60000；规格与质量复审 approved，rustfmt/diff 静态门通过，未运行 Cargo。 |
| M3 | 统一 Windows 测试阶段与风暴双样本 | `完成-局部门禁36/36通过` | 2026-07-12 | coordinator job `026db73e687d465ea11a2cac5d5cd927` 首次编译发现本功能 `PendingJob.cancel_task` 是 trait-object 字段而非方法（E0599）；在 Editor14 owner 内硬修为 `(pending.cancel_task)(...)`，未建 wrapper/shim。job `b5f3feb25bd148e198795b550a872435` 复跑 `cargo test -p zircon_editor --lib --locked core::jobs::tests -- --test-threads=1 --nocapture`：36 passed / 0 failed / 3035 filtered out（7.68s）。精确风暴 jobs `62d0aaaadeb6414daf81164774b7a206`、`1904971dedce4fb398d051e7fbc15aba` 连续各 1/1 通过；`release_elapsed_ns=278322000/260519800`，均值相对偏差 6.607579%；`submit_total_ns=104629400/126985600`，偏差 19.304622%。两次均 `max_active=2`、3000 events、1000 event IDs、四项 final state 为 0、`numeric_budget=undefined`。日志：`D:/cargo-targets/zircon-engine/pool/525d696af7ff7754af95fa549668e728f5cc05bc36657acf1040f23e37a9cd34/editor14-m3-focused-rerun.log` 与 `editor14-m33-baseline-run{2,3}.log`。外部 UI/Layout 与 Editor15 typed error failure 仍按归属开放，不降低本局部门禁结论。 |
| M2/M3 | queued Export 取消测试一次性门收尾 | `完成-精确1/1通过` | 2026-07-13 | 原 full-lib 精确停在 `export_wizard_panel_session_poll_finishes_queued_prestart_cancellation`；该测试隔离运行 34.5s 仍不返回，而下层 `queued_cancel_completes_without_waiting_for_category_capacity` 1/1 通过。最低原因是测试的 `BlockingRunner` 每个 pipeline stage 都等待同一 channel，但夹具只发送一次 release 且在 `first.finish()` 前仍持有 sender；后续 stage 永久等待。测试在发送第一阶段 release 后显式 drop sender，表达“一次性 gate”语义；当前 3157-test 程序 exact 1/1、0.01s。未改生产 `EditorJobSystem`、配额或取消协议。 |
| M2/M3 | Windows Cargo capture 高输出夹具边界 | `完成-精确1/1通过` | 2026-07-13 | 全量越过 queued-cancel 后继续停在 `cargo_capture_and_poll_complete_on_a_single_runtime_worker`。真实 child 命令显示两个 Windows `for` 直接以 `&` 相连，`cmd` 将第二循环纳入第一循环体；3×3 探针产生 3 stdout + 9 stderr，原 5000 配置会产生 5000 + 25,000,000 行，而非计划的 5000 + 5000。用括号把两段循环分组后，当前源码 exact 1/1、9.40s，断言两流均包含 `*-line-5000`；既有 output-capture single-worker/byte-budget 合同 2/2、0.01s，证明无需改生产捕获/调度逻辑。 |
| M2/M3 | 当前 Editor full-lib 自然结束门 | `完成-资源停滞关闭-功能失败独立保留` | 2026-07-14 | 同一 fresh 3157-test 程序以 `--nocapture --test-threads=1` 自然结束并打印 `2975 passed / 144 failed / 38 ignored`，耗时 2833.59s，exit 101 仅由已列出的功能断言失败产生；不再人工终止。50ms 外部监控 first/min/peak/last threads=`1/1/1657/59`，观测工作集峰值约 1.12 GiB；旧 4091/5547 threads 持续累积、queued-cancel 永久等待和 2500 万行夹具慢运行均未复现。高瞬时峰值继续由 Runtime11 task-budget failure 独立审计，144 项 UI/Editor 功能失败按各自计划处理，不回灌 Editor14 资源 failure。日志 `.codex/tmp/runtime02_editor_full_after_export_fixtures.{stdout,stderr}.log`。 |
