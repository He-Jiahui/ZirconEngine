# Editor03 Camera-Subtree Invariant Follow-up

This follow-up extends `failure-2026-08-13-detached-entity-batch-editor-inverse-delta.md` while its existing file handle is unavailable for an in-place update.

## 产出记录与时间

| 时间 | 完成项目 | 状态 | 证据与后续 |
| --- | --- | --- | --- |
| 2026-08-23 | 相机子树不变量 | `implementation_complete / static_validation_complete / managed_validation_blocked` | Runtime adds `World::subtree_component_count<T>` and walks hierarchy ownership with actual ECS component storage. `DeleteNodeCommand` now rejects deleting a subtree when its `CameraComponent` count equals the global camera count, retaining the existing zero-camera rejection behavior. No `NodeRecord` projection is used for this decision. |
| 2026-08-23 | move-only batch redo/undo coverage | `implementation_complete / static_validation_complete / managed_validation_blocked` | Added editor regressions for zero cameras, two cameras under one parent, and `delete -> undo -> redo -> undo` restoring the original active camera, plus a Runtime component-scope regression. Independent `rustfmt --check` and scoped `git diff --check` pass. |
| 2026-08-23 | restore callback 异常路径收敛 | `implementation_complete / static_validation_complete / managed_validation_blocked` | `DeleteNodeCommand::revert` no longer uses a production `expect` to consume its detached batch. Gateway non-invocation and rejected restore now retain the move-only batch through typed invariant/scene errors; the existing rejected-restore retry regression remains the behavioral owner. Exact `rustfmt --check`, scoped diff check, and command source invariants pass. |
| 2026-08-23 | 回调后网关失败的历史补偿 | `implementation_complete / static_validation_complete / managed_validation_orphaned` | Gateway contract does not guarantee that a failed `with_world_mut` call skipped the callback. `CoreEditContext` now returns one explicit callback outcome, separating no invocation from a completed scene write plus a transport/protocol error. Create/Delete/Update/reflected-field commands uniformly map the latter to `CommandEffect::Applied`; delete restore preserves its move-only batch so history recovery can reapply and rebuild the inverse delta. New callback-then-error regressions cover create compensation, delete compensation/retry, and duplicate callback reporting after the first callback. Static `rustfmt --check` and scoped diff check pass. Managed `zircon_editor` gateway validation job `99610203af564d1492fb70f8af737315` became `orphaned` with no exit code, so it is not acceptance evidence. |
| 2026-08-23 | 受管定向验证 | `blocked_by_coordinator_artifact_governance` | Earlier `deleting_parent_of_all_cameras_is_rejected` validation was rejected before Cargo by reservation `3bba2c9f4999490aacce9ef80df1f9fd`. A later `-Ephemeral` run for `create_apply_is_applied_when_gateway_fails_after_the_callback` successfully allocated a temporary F: lane but was rejected before Cargo by `unmanaged_artifacts_detected` for pre-existing, unregistered `F:\cargo-targets\zircon-engine` artifacts. No shared artifact was deleted or altered; no Cargo success or performance measurement is claimed. |
