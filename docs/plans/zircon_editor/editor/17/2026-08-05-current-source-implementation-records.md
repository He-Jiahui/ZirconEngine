# Editor17 Current-Source Implementation Records

This archive is the canonical child-plan owner for the concrete implementation records migrated from `17-editor-services-and-recovery.md` after that numbered plan exceeded the ten-record limit. It preserves their original dates, status anchors and evidence; open failure artifacts remain in their respective `failure-*.md` files.

## 产出记录与时间

| 日期 | 切片 | 状态 | 完成项目与验证证据 |
| --- | --- | --- | --- |
| 2026-08-26 | M3.3 Settings Workbench 产品投影、动态分类、值批次、基础编辑与统一 mutation owner | `source_complete_static_green / primitive_edit_controls_complete / generic_editor_state_complete / schema_enum_pointer_control_complete / string_commit_control_complete / typed_chord_capture_complete / typed_color_editor_complete / open_window_hot_refresh_complete / context_mutation_coordinator_complete / change_driven_health_authority_complete / operator_retry_ui_complete / file_generation_identity_complete / immutable_projection_pending / managed_validation_pending` | `editor.settings.open` 已贯通 command→event effect→retained side effect，并从唯一 `SettingsAuthority` snapshot、同一 contribution/capability snapshot 和 active i18n locale 捕获 `SettingsWindowProjection`。Workbench 删除固定 General/Layout 子树，硬切为动态内建/插件分类；新增注册期 category→keys 索引和一次锁同代际 `ResolvedSettingsBatch`，打开/分类切换只克隆当前 builtin 分类 K 项的 typed value/source，frame/paint authority 与 filesystem 访问为 0。写路径由 Context `SettingsMutationCoordinator` 统一 `set/clear/MRU`、Project binding、两文档有界 pending、typed retry 与 shutdown admission guard；Runtime11 lane 按 scope+physical path 合并不同 key。新增 bool/numeric/schema-enum/string/chord/color/reset 基础控件，row/option hit 为索引访问，数值、enum 与 RGBA 单通道步进算法归 Settings schema；string 复用通用 Host text focus，逐字符只更新 commit-only 草稿，Enter 才复验 schema 并提交 coordinator。Chord payload 硬切为 `EditorKeyChord`，key selector 复用宿主唯一焦点并进入独占 `chord_capture`：IME 关闭、全局 keymap 不接收 capture 事件，Escape 取消、modifier-only 等待、有效 chord 一次提交后退出。枚举专用 open state 已硬切为 `{kind,key,row}` 通用活动 editor；Color resolved value 结构化携带 RGBA，row 使用 alpha checkerboard swatch，弹层四通道 stepper 通过 typed action 提交同一 coordinator，不解析展示字符串。Context coordinator 以固定 User/Project persistence-health snapshot、document identity + observation token 拒绝迟到终态；PendingAdmission/Failed 经锁外 subscriber 进入唯一 notification authority，Settings 标题栏直接 scope hit 只调用 typed retry，稳定通知帧不读 health。Persistence request/ticket/health/retry receipt 现硬切为 `(scope, physical target, SettingsFileGeneration)` 身份，进程单调代次避免 project path 重绑倒退；authority revision 仅诊断，Request 与 deferred admission 自带原 Store，`retry(ticket)` 不能替换目标。file-generation 4/4、Settings 窗口 18/18、Editor17 48/48、限定 rustfmt 通过；新增 Rust 行为未执行，默认 registry 尚无独立 Chord/Color definition，immutable file projection/digest、锁外编码、keymap 冲突/解绑、其余结构化 editor、真实窗口、managed Cargo、动态性能/功耗或 1/1k/100k 规模仍待完成。详见 [2026-08-26-settings-visible-value-batch.md](2026-08-26-settings-visible-value-batch.md)、[2026-08-26-settings-persistence-health-foundation.md](2026-08-26-settings-persistence-health-foundation.md) 与 [Settings file-generation review](../../../performance/01/2026-08-15-editor-core-settings-file-generation-current-architecture-review.md)。 |
| 2026-08-04 | M3.1/M3.3 独立审查前向修复 | `source_complete_static_green / independent_second_review_green / managed_validation_pending` | 独立审查先后暴露日志的文件/总线顺序、sink 可重入、CWD 根推断、项目回滚解绑、无界 FIFO 事件保留、canonical bus 回执未进入重同步，以及 i18n 的 locale 事件反序、无界事件保留与回执丢失。未回滚已集成代码：日志改为 emission 内 FIFO 入队、锁外有序派发，并以条目/字节预算拒绝溢出 record；发生本地或 bus 断裂时，以 `through_sequence` 事实要求订阅者重建 store snapshot，不伪造不可证明的精确丢失区间。i18n 改为 transition 内 FIFO 入队、锁外派发，并在条目/字节预算溢出后合并为最新 locale resync。两者都会把 sink 的 `Rejected/Backpressured` 吸收为待派发 resync，失败的 resync 保留至下一事实事件重试，完成计数只在送达后增加；canonical bounded inbox 仅在每个被淘汰订阅者也收到 marker 时确认完成，否则继续重试。i18n failure absorb/retry 与普通 locale transition 共用线性化锁；并发回归接受“旧 locale resync 后新 change”或“直接合并到最新 locale resync”两种收敛序列。项目成功打开才绑定真实根并在关闭/失败回滚后解绑。受控多线程交错、慢 sink 饱和、字节预算、失败重试和 bounded inbox 交错回归已加入。两位独立审查者最终均为 `Critical/Important/Minor = 0/0/0`；限定格式、diff、FIFO/生命周期/回退链和无 panic 静态守卫通过，未运行 Cargo，托管验证待回执。 |
| 2026-08-04 | M3.1 logging aggregation core | `source_complete_static_green / independent_second_review_green / managed_validation_pending` | 新建 `core/logging/`：类型化 `LogSource`/`LogJump` 构造边界、条目与频道/severity 过滤、有条目数和字节数双上限的内存环、按日滚动并在进程重启后续写下一段的文件 sink；日志 record 以 sequence 通知接入 canonical bus，但实体仍只从 store 查询。成功打开项目后以真实 `<project>/.zircon/logs` 绑定 sink，关闭项目后解绑。限定 rustfmt、diff、无 panic 路径、单行落盘、受控并发顺序和可重入事件静态守卫通过；两位独立审查最终 `0/0/0`。未运行 Cargo，六来源生产者、CLI diagnostics 路径和活动面板仍未接线。 |
| 2026-08-04 | M3.3 i18n core | `source_complete_static_green / independent_second_review_green / managed_validation_pending` | 新建 `core/i18n/` 和 en/zh-CN 内嵌 TOML 包；`EditorI18nService` 以显式服务引用实现语言切换、当前语言→en→raw key 回退和 `tr!` 宏，语言变更只以 `editor.i18n` 事实事件通知订阅者，并挂入 `EditorContext`。限定 rustfmt、diff、无 panic 路径和双包/回退链静态守卫通过；两位独立审查最终 `0/0/0`。未运行 Cargo，08 命令、设置页、`ui/activity/` 消费端与插件词条尚未迁移。 |
| 2026-08-04 | M3.3 notification i18n presentation | `source_complete_static_green / independent_second_review_green / managed_validation_pending` | 新增 `core/notifications/presentation.rs`，将 Decision、Toast、Progress 的不可变快照投影为本地化读模型；翻译发生在消费边界，ticket、option id、通知来源、expiry、severity、resolved receipt 与权威 job snapshot 原样保留，不引入第二状态或动作入口。补入现有 play-pending-edit 与启动 Play 词条的 en/zh-CN 资源；回归覆盖语言热切换后的 decision identity、raw key 回退 toast 和 progress snapshot 保真。二审前向修复为单次捕获并公开 locale、以该快照完成全部 key 回退解析，及完整比较 resolved receipt 的 sequence/ticket/option；两位独立审查最终均为 `Critical/Important/Minor = 0/0/0`。限定 rustfmt、diff、五个生产 key 双包一致性、只读边界、locale 快照、receipt identity 和模块导出静态守卫通过；未运行 Cargo，托管验证待回执。 |
| 2026-08-04 | M3.2 notification core forward repair | `source_complete_static_green / independent_second_review_green / managed_validation_pending` | 新建共享 identity、Toast、Progress 子域并挂入 `EditorNotificationService`；Toast 以显式时钟管理 expiry、容量和 duplicate，Progress 从 `JobTicket` 仅提取 `JobId` 并随 active progress snapshot 清理陈旧绑定。Decision 迁至共享 identity，保留式 pending-edit 用例已走真实 option callback 和 receipt 链路。局部格式、diff 与静态契约守卫通过，两轮独立审查最终 `0/0/0`；未运行 Cargo，生产者及 activity 迁移未完成。 |
| 2026-08-04/05 | M2 session guard / restore flow 前向修复 | `source_complete_static_green / independent_second_review_green / managed_validation_pending` | 前向修复 residual takeover 的 read-then-replace 竞态：`SessionGuard` 在验证前取得且整个 guard 生命周期持有项目 OS 租约（Windows `Global\` 命名 mutex / Unix `.zircon` 非阻塞 `flock`），避免 takeover、heartbeat、release 之间覆盖 successor；记录写入先 stage+flush 再原子发布，Windows 因本层没有 parent-directory fsync 等价物、Unix 因发布后目录同步失败时均以 `PublishedWithDurabilityUncertainty` 保留已生效 guard，而不伪装为未变更。重复 key 的锁记录改为拒绝；`RestoreFlow` 对残锁但无较新 autosave 返回 `ResidualTakeoverRequired`，并拒绝无候选时的额外决议。按模块边界拆为 `record`、`mutation`、`ownership_lease`、`guard`，根模块仅导出。2026-08-05 新增 `autosave_catalog/`，以不可改绑 `recovery.json` 把快照映射到项目内源文档，并以文件系统 create-new marker 把每文档/序号写入收敛为跨实例、跨进程的单飞；marker 残留只阻止该编号，候选发现与轮转不猜测删除。独立审查先发现独立 store 的序号竞态（`0/1/0`），以并发 barrier 回归和 marker 前向修复后复审为 `0/0/0`；另有崩溃遗留 marker 的回归，证明其拒绝同序号重用而不影响 recovery candidate 发现。限定 rustfmt、diff、所有权/发布/解析/恢复静态守卫通过；未运行 Cargo。项目启动 liveness 裁决、prepared-project 接线、恢复 UI 与托管验证仍待 Editor16 owner/协调器，故 M2 不得视为产品闭环或 accepted。 |
| 2026-08-05 | M3.1 Play backend log-source and Activity projection forward connection | `source_complete_static_green / independent_second_review_pending / managed_validation_rebind_required` | Play 后端 stdout/stderr 不再无条件写入 Runtime：控制器仅在实际附着 `WorldDomain::Play(instance)` 时发出 `LogSource::play(instance)`，没有附着 world 则明确退化为 `LogSource::runtime()`，不以 UI generation 或裸整数伪造实例身份。`ui/activity` 同时新增只读 `ActivityLogView`，直接持有 immutable `LogRecord`，保留 sequence/source/severity/message/frame/typed JumpToken；过滤、保留和跳转 authority 不复制到 UI。前向移除了 severity/frame accessor 对非 const `LogRecord::entry()` 的错误 `const` 标记，并以静态 guard 固化该类型边界。新增 attached/unattached 与 typed log-record/jump 定向回归和静态守卫；`rustfmt --check`、三份 Python 静态契约共 10/10、`py_compile`、范围 `git diff --check`、failure/产出记录审计均通过。`ActivityLogView` 当前仅是 core read-model，尚无生产 host/retained 控件消费；该缺口已转 Editor06 failure，不能把 projection helper 误报为面板接线。未运行本地 Cargo；其余 Runtime、Plugin、CLI、Import、ScriptBuild 与 retained-host 过滤/跳转控件仍为未完成 M3.1 工作，本记录不宣称 M3 完成。 |
| 2026-08-05 | M1 Settings registry/snapshot/authority owner hard cut | `source_complete_static_green / independent_second_review_pending / managed_validation_rebind_required` | 865 行的 `settings/registry.rs` 已按责任拆为 265 行 registry（definitions/layers/precedence/change log）、275 行 snapshot（built-in slots/immutable typed projection）和 339 行 authority（mutex/ArcSwap publish/subscriber/project-layer lifecycle）；`mod.rs` 仍只提供既有精选 public surface，不保留旧模块路径 alias 或兼容 wrapper。TDD 结构守卫先确认缺少 owner 文件和超预算，拆分后所有 owner 均不超过 800 行；新增 public entry-point location guard 后，全部 Editor17 Python static contracts 14/14、`py_compile` 和限定 `rustfmt --check` 通过。未运行本地 Cargo；独立二审与 current-source managed validation 仍待 coordinator 流程，本记录不宣称 accepted。 |
| 2026-08-05/26 | M2 recovery test owner progressive hard cut | `source_complete_static_green / directory_owner_complete / managed_validation_rebind_required` | 初次切分把 autosave job-admission/atomic-rejection/write-failure 生命周期回归及其 `GateJob`、`CountingSnapshotSource`、completion pump 从根迁入命名 owner。2026-08-26 current-source 已继续删除旧 `tests/autosave_adapter.rs`，硬切为 `tests/autosave_adapter/{admission,completion,outcomes,scheduling,shutdown,support}.rs`；同日把 12 项 SessionGuard/lease/takeover 回归及平台 alias fixture 迁入 `tests/session_guard.rs`。根 `tests.rs` 现为 591 行、SessionGuard owner 375 行、autosave-adapter 最大 owner 408 行，不保留旧文件或兼容 wrapper。TDD 所有权契约 4/4、全部 Editor17 发现集 34/34、Settings UI 12/12、9 个 Recovery owner 限定 `rustfmt --check` 与 scoped diff 通过；未运行 Cargo，独立二审与 managed validation 仍待 coordinator，本记录不宣称 accepted。 |
| 2026-08-05 | M3.1 remaining source-producer handoff routing | `open_handoffs_recorded / core_source_complete_static_green` | 已按当前 `LogChannel::{Editor,Runtime,Play,Plugin,Import,ScriptBuild}` 六值模型完成逐生产者归属：Play 已接通；任务 diagnostics→Runtime11、插件 diagnostics→Editor12、导入→Editor09、脚本编译→Editor13、CLI→Editor16。五份 failure record 固化“中立事实/现有 host boundary→唯一 `EditorLogService`”和有界 cursor/dedup 验收，明确禁止 Runtime 反向依赖 Editor、私有 history/global logger、`LogChannel::Cli` 或第七频道。仅新增计划交接，未修改外部生产代码、未运行 Cargo，也不宣称 M3.1 全链完成。 |
| 2026-08-05 | M3.1 Activity host-consumer gap handoff | `open_handoff_recorded / read_model_static_green` | 搜索确认 `activity_log_views` 的生产消费点为零：现有 `ActivityLogView` 只提供 immutable `LogRecord` 的 typed projection，不会自行进入 Workbench reflection 或 retained host。已向 Editor06 写入当前-snapshot/filter/JumpToken 单一路由 handoff，要求仅消费 `EditorLogService` + `LogFilter`，刷新时替换陈旧行，不建立第二 history。未修改 retained-host 源码、未运行 Cargo；M3.1 继续保持未全链完成。 |
| 2026-08-24 CST | M2 recovery autosave production owner hard cut | `source_complete_static_green / independent_second_review_pending / managed_validation_pending` | 为收敛 `engine-code-structure-convention.md` 的生产 owner 预算，844 行 `core/recovery/autosave.rs` 已物理硬切为 `autosave/identity.rs`（稳定 document/extension identity）、`policy.rs`（dirty projection 与 job policy）、`scheduler.rs`（单飞轮询计划）、`store.rs`（sequence reservation、snapshot rotation、atomic publish）和薄 `mod.rs` facade。`autosave_catalog` 仍只能经唯一 `write_new_atomically` leaf 发布不可改绑元数据；`AutosaveJobAdapter` 仍为 admission/worker owner，未改 autosave interval、job category/priority、原子写入、保留数或 public recovery surface，未保留旧实现或兼容路径。static inventory 确认旧 owner 不存在、所有既有 public symbols 可达、atomic publisher 唯一、生产 owner 最大 420 行；限定 `rustfmt --edition 2024 --config skip_children=true --check`、scoped diff whitespace 与旧路径文档引用检查通过。未运行 Cargo、WPR 或产品 recovery 流程，未产生性能或产品闭环结论。 |
