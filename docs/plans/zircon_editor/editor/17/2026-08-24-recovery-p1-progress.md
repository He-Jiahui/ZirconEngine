# Editor17 Recovery P1 Progress

## 产出记录与时间

> P1-10 追加状态（2026-08-24 21:23 +08:00）：活动场景事务路由已完成硬切。`DocumentLifecycleAuthority` 提交启动场景或 picker 场景后，`EditorState` 只接收该 `DocumentId` 绑定；场景命令、gizmo release、undo/redo、保存 `saved_top`、关闭工程脏检查、snapshot 与事件追踪均使用 `HistoryContextId::Document(document)`，无 `Global` fallback，未绑定场景拒绝编辑。世界替换/关闭只清理已绑定文档历史，避免把下一场景混入旧上下文。此前 P1-10 记录中“scene-facing `Global` history”的描述已被本切片取代；durable `DocumentJournalCoordinator::append_committed`、autosave checkpoint compaction 和启动 discovery/replay 仍未接入，不能将本路由切片视为恢复闭环或提交里程碑。

> 验证补充（2026-08-24 21:32 +08:00）：受管 D 盘 lane `f9fef644bf8e441a49ad1c139495499657f126cd246ffca80d13868db535561d` 已实际启动 `cargo build -p zircon_editor --locked` 与 `cargo test -p zircon_editor --locked --lib history`，两者均在 `zr_rhi/src/surface.rs:233-234` 的外部 E0499 前终止，未到达 `zircon_editor` 编译或本轮测试执行；无本切片 Cargo 成功声明。局部 Rustfmt、受限 diff 检查和 workbench `HistoryContextId::Global` 零命中仍通过。

> P1-10 生命周期绑定补充（2026-08-24 21:55 +08:00）：项目会话现在唯一持有 `DocumentJournalCoordinator`；它只从自身 project root 下的物理 scene source 推导 `JournalDocumentKey`，拒绝工程外路径。启动场景在 lifecycle reservation 后、发布前绑定；picker 打开/创建场景在 authoring world 安装前绑定，创建路径在 source publish 前绑定。后续 publish/catalog/install 失败会释放未提交 binding，场景切换会释放已关闭 document 的会话 binding，避免按历史打开数量增长。绑定阶段不创建 writer、不读写 journal；`append_committed`、有界后台写入、autosave checkpoint compaction、startup discovery/replay 仍未接通。E 盘 1 KiB/200 次强制 flush 微采样为 p50 `0.5926 ms`、p95 `0.8938 ms`、p99 `2.1235 ms`、max `21.4729 ms`，只作为“禁止 UI 帧内 fsync”的 pre-implementation evidence，非 engine/WPR/功耗或 Unreal 对比验收。

> 验证补充（2026-08-24 22:07 +08:00）：同一受管 D 盘 target 的 `cargo build -p zircon_editor --locked` 得到退出码 `1`；Cargo 已调度 `zircon_editor`，但 `zr_rhi/src/surface.rs:233-234` 的两个外部 E0499 在其依赖图完成前失败。因而没有 editor crate 成功、也没有本轮 `document_journal_coordinator|scene_document` 定向测试执行；本轮只可声明 Rustfmt/scoped diff/static routing 检查通过。

> P1-10 会话所有权补充（2026-08-24 22:18 +08:00）：重审 project 启动调用栈后，`DocumentJournalCoordinator` 改为 session 内不可替换的 project-root authority。`publish_document_startup_session` 与 `complete_project_open` 对同根重复初始化只复用原 coordinator，异根请求返回 typed `ProjectRootConflict` 并沿既有激活 rollback 清理，防止未来 writer 接通后丢失 binding 或文件句柄。此项不接通 `append_committed`，不改变 autosave/checkpoint/replay 的未完成状态；本轮仍无 Cargo 成功结果，不能提交或发送企微。

> 验证补充（2026-08-24 22:25 +08:00）：受管 D 盘 `validate-matrix.ps1 -Package zircon_editor -LibTests -TestFilter 'document_journal_coordinator|scene_document'` 已完整结束。`Cargo build` 与 `Cargo test` 均为 exit `101`，各自在 `zr_rhi/src/surface.rs:233-234` 的两个 E0499 停止；没有 `zircon_editor` 编译成功，也没有定向测试实际执行。外部 failure handoff 仍为 `docs/plans/optimize/zircon_runtime/90/failure-2026-08-24-rhi-surface-allocator-borrow-compile.md`，本切片不得提交或发送企微。

- 2026-08-24 21:04 +08:00 | P1-10 durable journal foundation | `进行中，未验收`：hard cut `core/editing/engine/journal.rs` 为 folder-backed `journal/{payload,transaction,codec,durable}/`；无旧路径、re-export shim 或兼容实现。新增 `JournalDocumentKey`，只从跨宿主一致的 project-relative UTF-8 source path 推导 BLAKE3 durable identity，禁止 session-local `DocumentId` 作为 journal 目录键。`DurableJournal` 在 `<project>/.zircon/journal/<key>/transactions.zjr` 写 versioned header、单调 sequence、length-delimited BLAKE3 frame 与 `sync_data`；reader 对 header/file/record/record-count/checksum/schema/sequence 做有界校验，返回有效前缀和 typed tail fault，坏尾拒绝继续 append，写失败 writer 进入终止状态。新增显式 snapshot-covered prefix compaction：只保留覆盖点后的连续帧并以同目录 replace 发布；只读 discovery 以 header-derived key 校验目录 identity，隔离单条坏目录/非普通文件，返回正常 journal 和 typed issue。Recovery `DocumentJournalCoordinator` 只在内存绑定 `DocumentId -> JournalDocumentKey`，每 document 独立 writer slot，显式 commit owner append、读/写/compact 在该 slot 串行且 compact 先丢弃旧句柄；不使用可丢失 message bus。`core/editing/journal_codecs/` 现注册 scene create/delete/update/reflected-field 的 v1 codec：owned DTO 在打开 scope 前校验 create intent/retained record、normalized node edit、no-op reflected write 与必需 field；delete payload 继续只存 forward descriptor，不落 move-only inverse batch。新增 matching-baseline 下四种 scene journal replay 与无效 payload 不进入目标 scene/history 的回归；autosave checkpoint 链接、startup discovery selection/replay，以及活动场景 `Document` history commit/append owner 仍未接入，现有 scene-facing `Global` history 不得写入 durable journal。局部 Rustfmt、`git diff --check`、production unwrap/expect/compat 扫描和文件预算静态检查通过。2026-08-24 21:04 受管 `zircon_editor` journal 验证在 Cargo 启动前被 coordinator `unmanaged_artifacts_detected` 拒绝（外部 `F:\cargo-targets\zircon-engine\ephemeral`）；此前非本计划 `zr_rhi/src/surface.rs:233-234` E0499 handoff 仍开放。真实启动 recovery selection/replay、故障注入与受管 Cargo 均未完成，故不得提交或发送企微。

- 2026-08-24 19:37 +08:00 | P1-17 profiling precondition | `调研完成，性能基线待执行`：重审 `editor_document_autosave.rs`、`EditorUiHost::capture_document_autosave`、toolkit `SaveLease`、UI asset/animation capture 与 `AutosaveSourceDigest` 后确认，当前 request 仅持有 `Weak<EditorManager>`，后台 worker 才获取 toolkit save lease、读取 dirty generation、序列化可变 session，并流式 BLAKE3 读取 source。直接把 payload freeze 挪到 UI 会改变 mutex 时序，并将序列化/复制/磁盘摘要成本移入帧线程，不能在未测量时作为“优化”提交。参考 Unreal `PackageAutoSaver` 的 dirty/saved/undo 事实同步，后续设计应先定义 generation-bound immutable checkpoint，再让 worker 只编码/持久化。WPR/WPA 已安装；`E:\Git\ZirconEngine\target\profiling\zircon_editor.exe` 与 PDB 最后构建于 2026-08-10，早于当前源码，不能作为数据基线。待 coordinator 在 E 盘重建 profiling target 后，以 1/10/100 MiB UI asset 与 animation 文档分别执行 interval autosave、foreground-save overlap、final shutdown；WPR 采集 CPU sampling、thread scheduling、file I/O 与 Energy Estimation，报告 UI frame p50/p95/p99、UI capture/lock 时间、worker encode/write/digest 时间、in-flight bytes、allocation/commit、I/O bytes 与能耗，再决定 immutable checkpoint 的 COW/stream/budget 方案。当前没有 P1-17 性能数据或代码改动，故不得声称瓶颈、功耗或最优规模结论。

- 2026-08-24 19:29 +08:00 | P1-26 | `进行中，未验收`：修复 P1-20 退出失败保留 session guard 的跨层矛盾。`EditorManager::Drop` 不再 best-effort 调用 `SessionGuard::release()`；只有 `release_project_session_guard()` 的显式成功事务删除持久化 `session.lock` 并清理 heartbeat。因 final autosave、startup、release I/O 或其他异常路径退出时，guard 仅释放 OS lease 而保留 residual，下一次启动必须进入既有 recovery/admission 裁决。参考 Unreal `FEngineLoop::Exit` 的显式 PreExit/Exit 阶段，不将可失败的持久化状态变化藏入 destructor；既有 `session_guard_claim_holds_a_residual_record_until_explicit_takeover` 覆盖 guard drop 的 residual 语义。局部 `rustfmt --check`、scoped `git diff --check`、`EditorManager` Drop/隐式 release 为 0 和唯一显式 release owner 静态检查均通过；未运行 Cargo、真实异常退出/锁 I/O、Windows 双进程或 restart recovery 验收，故不得提交或发送企微。

- 2026-08-24 19:23 +08:00 | P1-20 / test-budget recovery | `进行中，未验收`：补齐退出前 project 未绑定 autosave adapter 的防守边界：final request 现在返回 typed retryable `JobLifecycle` cancellation，不启动 worker，host 因 failure outcome 保留 session guard，禁止静默丢失脏文档。新增该边界与 single-entry admission 下跨多个 final window 全部 drain 的覆盖；当前 adapter 测试为 21 项（基线 15）。本轮局部 `rustfmt --check` 与 scoped `git diff --check` 均通过；旧 `autosave().shutdown(...)` 调用、旧 flat adapter/test 路径、`#[path]`/`include!`/compat 指令及超 800 行模块均为 0。仍未运行 Cargo 或 Windows/真实 host/故障注入验收，未回传 failure handoff，故不得提交或发送企微。

- 2026-08-24 19:20 +08:00 | P1-20 / test-budget recovery | `进行中，未验收`：按 `docs/plans/zircon_editor/editor/17` 的 shutdown 顺序重审并实现 final autosave drain。hard cut 平铺 `autosave_adapter.rs` 为 `autosave_adapter/{model,adapter,write_job,mod}.rs`，并将超预算 `tests/autosave_adapter.rs` 拆为 folder-backed admission/scheduling/completion/outcomes/shutdown/support 模块；旧路径不存在，所有新模块均小于 800 行，当前 adapter 测试为 20 项。scheduler 新增不等待 interval 的 final window，adapter 在退出时 fence 普通 admission；`EditorAutosaveService` 在 shared `EditorJobSystem` 仍存活时排空已有工作、按 entry/byte admission 分批处理 final 请求、为 deadline 未达 terminal 的文档生成 typed lifecycle cancellation 并走 durable diagnostics fallback，之后才执行全局 job shutdown。retained host 从 dirty toolkit 生成 request（snapshot 仍在 worker capture），且仅在 final outcome、diagnostics、job deadline、settings、event loop 与帧捕获均成功后才 `commit_project_close` 释放 session guard；历史周期失败不会误判为本次 final shutdown 失败。补充 `docs/zircon_editor/core/recovery.md` 的跨模块 shutdown contract。已通过局部 Rustfmt、`git diff --check`、旧 shutdown 调用点为 0、旧 flat 路径为 0、无 `#[path]`/`include!`/compat 残留、模块行数和测试计数静态检查；未运行 Cargo、Windows retained-host、进程 kill、磁盘满/权限、deadline/cancel 竞态、跨进程或大项目性能验证，也尚未向 failure handoff 回传，故不得提交或发送企微。

- 2026-08-24 16:59 +08:00 | P1-23 | `进行中，未验收`：新增 `core/recovery/restore_executor.rs`，仅消费既有 `RestoreFlow` 的完整决策计划。`RestoreAutosave` 与 `OpenComparison` 均用 no-replace atomic write 将 snapshot 物化到 `<project>/.zircon/recovered/{restore|comparison}/`，不覆盖 authoritative source；`DiscardAutosave` 只删除已验证归属的单个 autosave document 目录。执行器验证 candidate 文档映射和 autosave-path containment，返回 typed per-document report，并有 source 不变与 discard 范围覆盖。项目 admission 仍把 residual 压成错误文本，retained host 没有 Inspect/Take Over/Restore/Open Copy/Discard/Cancel 决策 owner，report 也尚未驱动 recovered-copy 打开、import/hydrate 或错误重试；只完成 Rustfmt/`git diff --check` 静态检查，未运行 Cargo、真实进程或 kill/recovery 故障注入，故不得提交或发送企微。

- 2026-08-24 17:17 +08:00 | P1-25 | `进行中，未验收`：`EditorManager` 以单调时钟持有 5 秒 session-heartbeat cadence；retained-host tick 只在 deadline 到期时调用 `SessionGuard::refresh_heartbeat`，正常刷新后重新调度，首次失败转入 terminal `Degraded` 并停止后续续租。host contract 新增独立 lifecycle wake slot，native event loop 将其并入 `WaitUntil` 的最早 deadline，因此空闲窗口也会触发 heartbeat；它不与 asset refresh 的 maintenance slot 互相清除，并以 `ui.session_heartbeat` 归因。失败写入 editor error log 与可见 status line，避免静默所有权假象。新增 cadence/degrade、lifecycle/asset wake 隔离、tick 接线覆盖；只完成 Rustfmt/`git diff --check`/定向静态扫描，未运行 Cargo、真实窗口、双进程、锁 I/O 失败或 kill/recovery 故障注入。residual admission 仍为错误文本、没有恢复决策产品 owner，Drop/release 失败收束仍属 P1-26，故不得提交或发送企微。

- 2026-08-24 17:24 +08:00 | P1-24 | `进行中，未验收`：hard cut `AutosaveStore::recovery_candidates()`，改为 `recovery_catalog()` 返回 `AutosaveRecoveryCatalogReport { candidates, diagnostics }`。catalog root 不可枚举仍为唯一全局错误；根目录可枚举后，entry 枚举、file-type、document identity、snapshot sequence、metadata 与时间读取错误均生成 typed `AutosaveRecoveryCatalogDiagnostic`，隔离该目录并继续返回其他有效 candidate。新增坏 metadata 不阻断有效 document 与重复 sequence quarantine 覆盖；定向静态扫描确认旧 API 调用点为 0、root 缺失返回 empty report。只完成 Rustfmt/`git diff --check`/定向静态扫描，未运行 Cargo、真实启动恢复、损坏目录权限故障或 kill/recovery 注入；report 也还没有产品 recovery coordinator 消费，故不得提交或发送企微。

- 2026-08-24 18:02 +08:00 | P1-22 | `进行中，未验收`：完成恢复 freshness 的结构性重审并落地新提交协议。参考 `dev/UnrealEngine` 的 `PackageAutoSaver` 将恢复清单与 dirty/save/undo 生命周期分离，结合现有 Editor17 的 per-document history；Zircon 将 owner 固定在 `core/recovery`，不向 runtime world 或 UI authoring state 泄漏恢复事实。`recovery.json` v2 只固定 project-relative source identity；每个 `N.<extension>` payload 都必须有 no-replace 发布的 `N.snapshot.json` commit record，记录 source content digest、dirty-registry base generation、明确的 journal unavailable range、`zircon_editor.autosave.snapshot` schema id/version 与 committed BLAKE3 checksum。Catalog 只消费已提交 metadata，流式摘要校验 snapshot 和当前 source，按 `SourceMissing`/`SnapshotAheadOfSource`/`SourceDiverged`/`SnapshotAlreadyCommitted` 判定，删除所有 mtime freshness；已提交到 source 的 snapshot 不再提示恢复，分叉 source 仍只生成可恢复副本候选。旧单文件 snapshot 格式与 `recovery_candidates()` 不保留兼容路径；checksum 损坏保持 per-entry quarantine。新增 source-ahead/diverged/already-committed/checksum mismatch 覆盖，并迁移 autosave/restore 测试夹具。已通过本次拥有文件的 Rustfmt、adapter 测试夹具只读 Rustfmt 解析、`git diff --check` 和旧 mtime/API 静态扫描；未运行 Cargo、真实 retained-host 启动、进程 kill、跨进程写入、断电/磁盘满或大文件恢复测量，故不得提交或发送企微。P1-10 durable journal、P1-17 UI-thread immutable capture、P1-19/P1-21 durable diagnostics 仍未闭合。

- 2026-08-24 18:39 +08:00 | P1-19 / P1-21 | `进行中，未验收`：参考 Unreal `PackageAutoSaver` 的 dirty/saved/undo 事实保留、Godot 的可配置 autosave timer 与 Fyrox 的显式保存决策边界，hard cut `AutosaveCompletion` 的“失败数量即诊断”模型。`AutosaveSnapshotSource` 现必须在 admission 前提供 project-relative source；adapter 队列改为 document-bound ticket，poll 返回本次新增的 typed outcome（document/source/stage/retryability/bounded error chain/usable snapshot/durable flag）及 generation health telemetry。正常 worker 在 `.zircon/autosave/diagnostics/` no-replace 原子发布每个 terminal record，128 条 retention 且坏记录隔离读取，`AutosaveDiagnosticStore` 提供 Welcome/Hub 查询与 per-document autosave folder 路径；retained host 对失败/取消写 Editor log/activity 投影和 status line。项目切换不再直接丢弃 adapter：旧项目进入受全局 completion budget 限制的 retired queue；已启动 worker 自行落盘，未启动取消由 teardown fallback 持久化，失败则保留 retired owner 在后续 poll 重试。新增 failure outcome、retired cancellation 与 retention-after-write usable snapshot 契约，更新 `docs/zircon_editor/core/recovery.md` 的 catalog/diagnostics 事实。已通过 Rustfmt、`git diff --check`、旧 mtime/API/纯计数路径静态扫描；未运行 Cargo、真实 retained host、Welcome/Hub consumer、per-document Retry/Open-folder 控件、进程 kill、磁盘满/权限/断电、跨进程或大项目性能测试。P1-20 shutdown final snapshot/drain 仍是独立未闭合 owner，故不得提交或发送企微。
