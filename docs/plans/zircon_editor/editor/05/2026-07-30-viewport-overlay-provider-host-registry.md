# Editor05: viewport overlay provider host registry

## 产出记录与时间

| 时间 | 切片 | 状态 | 完成项目与验证证据 |
| --- | --- | --- | --- |
| 2026-07-30 | plugin viewport overlay shared host contract | 源码与RED已落盘 / 动态验收待上游 frame source | `EditorExtensionRegistry` 新增非序列化 provider factory registration；host 在 extension registration 时验证每个 tool-mode provider id，并在任何宿主可变登记前预检重复 provider ID 后安装到 `SceneViewportController`，避免半注册。provider 默认关闭，toggle 走 `EditorViewportEvent -> ViewportCommand`，`refresh_reflection` 将 capability snapshot 同步给 viewport，render packet 合并 interaction 与 provider 的 `scene_gizmos`，capability 禁用后下一帧无旧 extract。host integration RED 覆盖注册、菜单 operation、toggle、packet 合并、capability clear、未注册 provider 引用拒绝及重复 ID 预检；retained callback 局部测试覆盖 provider id 保真。定点静态扫描确认唯一 `apply_interaction_overlays` caller 已传 provider channel，全部受管 Rust 文件 `rustfmt --check` 及 `git diff --check` 通过。共享树仍含外部会话编译输入变动，且 Navigation 没有真实 frame source；尚未声明 Cargo GREEN。 |
| 2026-07-30 | Navigation overlay frame source dependency | 已回传 Plugins05 / open | 现有 Navigation overlay 仅能消费传入的 `NavigationGizmoSnapshot` 与 `NavigationPieFrame`，仓库内没有 NavMesh snapshot 的生产发布者。新增 [failure handoff](failure-2026-07-30-navigation-overlay-frame-publication.md) 已导入协调器 lifecycle（request `7686915c3d994cc0bb382c344122e98c`，response SHA-256 `373d8037bc98b60b15c56f2cbe25b0d10a7768cf83956a094a12c385e49696fd`），要求 Plugins05 发布 lifecycle-owned canonical frame；禁止 empty/fabricated snapshot、global cache 或 test sink。原始 `plugin-viewport-overlay-provider-runtime-wiring` failure 保持 open，不能将本条部分 host 实现标为 fixed。 |
| 2026-07-30 22:45 +08:00 | provider registry focused immutable-copy gate | `resolving_failure` / Coordinator01 validation-copy attribution | 受管请求 `49612f22fdba4b62a4a450fc4a990170` 创建 job `67bbfb22c0a9403894fd6b7d523a892f` 后，在 materialization 的 `overlay_ownership` 阶段以 `validation_copy_overlay_not_owned` 终止，路径为 `zircon_editor/src/core/editor_event/types.rs`。copy 没有 input manifest、外部源挂载、Cargo run、PID 或测试计数，故这不是 Rust 测试失败，也不能宣称 GREEN。该 current-attribution 与 live lease 不一致的问题复用 Coordinator01 [failure handoff](../../../zircon_tooling/session_coordinator/01/failure-2026-07-26-live-lease-attribution-validation-copy-divergence.md)；等待其返回后重建本 focused gate。 |
| 2026-07-30 22:53 +08:00 | provider registry attribution reproducibility | `resolving_failure` / Coordinator01 handoff confirmed | R4 对相同 18 个源文件重新执行 `lease claim`，request `8068e844665b4a169ac1e4c807bf6943` 返回 `acquired: true` 且零冲突；随后 heartbeat `f89194e4ec754cbf8e0649cf035ae52f` 续到 19 条 lease。新受管请求 `87fe26a3935949f79d844bc13dce98b9` 的 job `66906dec0bb845948aa44403b41b5e21` 仍在同一 `types.rs`、同一 `overlay_ownership` 阶段终止，仍无 manifest、Cargo、PID 或测试计数。这将问题限定为已存在的 Coordinator01 live-lease -> attribution -> validation-copy 原子链，而非 lease 过期或 R4 代码；本切片在该依赖 fixed 后重试。 |
