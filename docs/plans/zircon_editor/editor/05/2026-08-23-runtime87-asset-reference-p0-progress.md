# Runtime87 Asset Reference P0 Progress

Plan: `docs/plans/optimize/zircon_runtime/87-runtime-asset-reference-identity-locator-guid-subasset-redirector-rename-move-resolution-repair-migration-product-integration-current-source-review.md`

Status: `in_progress / implementation_and_static_validation_complete / managed_runtime_validation_running`

## 产出记录与时间

| 日期 | 项目 | 状态 | 证据与后续 |
| --- | --- | --- | --- |
| 2026-08-23 | GUID-authoritative resolution hard cut | `implementation_complete / static_validation_complete` | `resolve_project_reference` 对缺失 GUID 且已占用 path hint 返回 `PathOccupiedCandidate`，GUID/subasset 不一致返回 `Conflict`；registry 的 `resolve_asset_id_for_reference` 删除 UUID 失败后的 path fallback。path 仅能作为同一 GUID 的安全 hint 更新，不能替换 stable identity。新增 registry path-reuse 回归测试与 Runtime87 source contract。 |
| 2026-08-23 | generation-bound safe repair observation publication | `implementation_complete / static_validation_complete` | full、targeted import 将 `ReferenceRepair` 保存在不可变 `ProjectCatalogInputRecord`；Editor asset catalog 从同一 generation 投影诊断。该链路只发布 `PathHint` 观察，不写 authoring source、registry identity 或 artifact。 |
| 2026-08-23 | native importer response protocol hard cut | `implementation_complete / static_validation_complete` | 响应 magic 从 `ZRIMO001` 升为 `ZRIMO002`，`reference_repairs` 为必填 typed 字段；host 原样传入 `AssetImportOutcome`。native fixture 与其 bounded-response baseline 同步发出空观察列表，旧 payload 对比语义已移除。当前 runtime importer/registry 文档已收敛为新协议；历史 `docs/superpowers` 计划保留为当时证据。插件仅报告观察，没有 GUID 替换或文档写入权限。 |
| 2026-08-23 | Editor10 asset delete preflight | `implementation_complete / static_validation_complete / managed_test_pending` | 新增 `core/asset/refactor` 的 runtime-registry-backed 删除准入：未知 UUID、只读/非项目 source、带 label 的 subasset、存在 referencer 都 fail closed；referencer 以 canonical locator 稳定排序供确认界面消费。该服务不触碰 filesystem、watcher、redirector 或 source 文档，真实删除必须由后续 mutation transaction 在提交 generation 重新校验。`test_editor10_asset_refactor_preflight_contract.py` 为 2/2；Rust 单测已加入但受 managed Cargo 终态缺失限制未计为通过。 |
| 2026-08-23 | Editor asset manager delete-preflight gateway | `implementation_complete / static_validation_complete / managed_test_pending` | `EditorAssetManager` 新增 `asset_delete_preflight(uuid, write_policy)`；唯一实现 `DefaultEditorAssetManager` 从活动 `ProjectManager::asset_registry()` 生成视图，活动工程缺失则返回 host initialization error。UI 不持有 registry copy，调用方必须显式传入资产类型的 write policy。gateway 的状态锁使用 poison recovery，不以 production `expect` 终止只读 preflight。gateway source contract 加入后 `test_editor10_asset_refactor_preflight_contract.py` 为 2/2。 |
| 2026-08-23 | Runtime-owned mutation delete topology hard cut | `implementation_complete / static_validation_complete / managed_test_pending` | 新增 folder-backed `asset/mutation/`，`AssetMutationDeletePreflight` 在 Runtime 侧从单一 `AssetRegistryIndex` 快照投影 target/referencers，拒绝 missing UUID 与 labeled subasset，并按 canonical locator/UUID 稳定排序。Editor 删除预检硬切为仅映射 Runtime topology 加 `AssetSourceWritePolicy`，不再遍历 referencer 图或持有重复 asset projection；`AssetRefactorAsset` 旧 editor owner 已删除且无 re-export。该预检不触碰 filesystem 或 generation state，真实 transaction 仍须在 commit generation 重验。 |
| 2026-08-23 | Runtime relocation transaction input | `implementation_complete / static_validation_complete / managed_test_pending` | `AssetMutationRelocationPreflight` 只接受 unlabeled `res://` source/target，fail closed 覆盖 missing UUID、subasset、非项目 source/target、occupied target 与 no-op；在单一 registry snapshot 中产出 root+subasset companion set，并去重外部 referencer closure，内部 companion 边不重复纳入重写计划。该输入没有 filesystem I/O、watcher 推断或 registry mutation，下一切片将其接入 Runtime commit generation 的 source/meta/registry/resource/catalog 原子事务。 |
| 2026-08-23 | Runtime full-generation resource rename reconciliation | `implementation_complete / static_validation_complete / managed_test_pending` | 新增 `resource_sync/reconcile_project_resources.rs`：watcher full reconciliation 与 `reimport_all` 仅保存 `{ResourceId, ResourceLocator}` identity snapshot，再按 stable resource ID 比较 previous/candidate generation；locator 变化先显式提交 `ResourceMutationBatch::rename` 再 upsert，candidate 不含该 ID 才 remove。修复了 ResourceManager 的 `ExplicitRenameRequired` 合同与 GUID-preserving rename 的发布矛盾；跨 project activation 继续沿用 remove-only，不会把不同工程的 UUID 碰撞伪造为 rename。 |
| 2026-08-23 | Offline project registry explicit rename staging | `implementation_complete / static_validation_complete / managed_test_pending` | `ResourceRegistryStaging::stage_rename_locator` 作为 Runtime core 的候选 generation 能力，严格拒绝缺失 source 和其他 ID 占用 target，保持 `ResourceId`/kind，并将新的 authorized locator 覆盖到随后 `stage_record` 的 identity 校验。该 API 不接触 filesystem、不发布 runtime event；真实可见 rename 仍必须通过 `ResourceManager` batch 和后续 project transaction 同步提交。新增 Rust 行为测试及 Runtime87 static contract。 |
| 2026-08-23 | Asset registry identity-preserving relocation candidate | `implementation_complete / static_validation_complete / managed_test_pending` | 新增 `asset/registry/relocation.rs` 的 `prepare_source_relocation_generation`：只接受 old source 与 relocated meta 的一一 UUID、label、kind、source-digest 对应，拒绝 no-op、entry topology drift 与 target path collision；candidate 删除 old source 后以相同 UUID 注册 target entries，并只对 companion 与外部 referencer closure 重写 dependency paths、刷新反向 UUID edges。算法为 `O(S + R + D)`，不扫描全索引；尚未写 source/meta 文件、artifact、live ResourceManager 或 watcher。 |
| 2026-08-23 | Runtime project-source relocation durable generation | `implementation_complete / static_validation_complete / managed_test_running / performance_architecture_open` | folder-backed `project/manager/relocation.rs` 只构造受限候选；唯一公开 mutation 命令是 Runtime pipeline 的 `ProjectAssetManager::relocate_project_source`。它在同一 generation 内复制 target source、退休旧 source，写入并退休 `.zmeta`，持久化 registry candidate；候选 `ResourceRegistryStaging` 对 companion locator 执行显式 rename，再刷新 companion/external referencer 的 dependency closure，并以 targeted catalog generation 发布。Pipeline 在资源批次预留后提交文件、安装 live project/source-path，最后发布单个 `Renamed` change。source UUID、subasset UUID、artifact locator 与 artifact bytes 不重铸也不重写。通用事务增加 `retiring_with_expected_digest`，阻止准备后被外部改写的 source 被静默覆盖；恢复策略只允许 asset roots 内同类 source→source 或 `.zmeta`→`.zmeta` retirement，仍拒绝 registry/artifact 或非 retirement 的 authoring source 写入。集成测试已覆盖成功搬迁、source 退休后受控中断的 restart rollback，以及 pipeline live-resource rename/change event；动态 Rust 终态待受管 receipt。当前没有资格声称 targeted complexity：`active_project.clone()` 和 `AssetRegistryIndex::clone()` 仍复制完整 generation，实际 prepare 至少为 `O(N + B + S + R + D)` 且有 `O(N)` peak memory；`N` 是 project/resource/asset registry 与 catalog input generation 的总规模。single-file/当前 digest 限制不变；compound member move 必须由后续完整 membership transaction 实现，未伪装为支持。 |
| 2026-08-23 | Runtime mutation owner re-review | `implementation_complete / watcher_fence_pending` | `ProjectAssetManager` 已有 candidate prepare、`project_generation_write`、resource batch reservation/apply、文件 commit 和 live project/source-path installation 的单 generation 结构；本切片将 relocation 接入该唯一 Runtime pipeline owner。Editor 仍无 project clone、filesystem 或 registry mutation 入口，watcher refresh 仍以 generation precondition retry。后续 delete、compound move、redirect/tombstone、receipt 与 watcher self-write suppression 必须复用同一 owner，不得引入 Editor 旁路或以 watcher 推断替代正式 mutation。 |
| 2026-08-23 | Editor asset relocation gateway | `implementation_complete / static_validation_complete / managed_test_pending` | `EditorAssetManager::relocate_project_source` 仅解析 UUID 并通过受管 `ProjectAssetManager` 委托 Runtime public command；成功状态非空时再刷新 editor-only catalog projection。Editor 不获得 filesystem、registry 或 `ProjectManager` mutation API，runtime 未配置时 typed initialization error fail closed。新增无 Runtime owner 的回归测试和 Runtime87 source contract；动态 Editor 测试将与 Runtime relocation filter 分别由受管 Cargo receipt 计数。 |
| 2026-08-23 | AssetMutationService structural research | `architecture_review_complete / first_relocation_slice_complete` | 已复核 Godot `ResourceUID` 的稳定 ID 到 path 映射、Editor10 的 UE 离线 referencer 约束，以及当前 Runtime source。首个 mutation 切片已经扩展 Runtime 的受限 project-source recovery allowlist，并在同一 generation 安装 source/meta/registry/resource/catalog；未额外建立会掩盖 owner 的 facade。不得以 `std::fs`、Editor project clone 或 watcher 回扫替代。后续按 `asset/mutation/{request,preflight,prepare,commit,receipt,watcher_fence}` 的 folder-backed owner 收束 delete、compound source 与 watcher fence。吞吐优化尚未开始：先记录 closure cardinality、planned source/meta/artifact bytes、prepare/reimport/commit elapsed 与 watcher-suppression counts，再用 1/1k/100k source 的 move/rename/delete 采样，未采集前不声明规模、功耗或引擎对标数据。 |
| 2026-08-23 | Watcher self-write fence design review | `architecture_review_complete / implementation_deferred` | 已逐调用链复核 notify map、event folding、activation coalesce 与 `process_watch_batch_in_generation`。`.zmeta` 被 watcher 入口过滤；同一 source relocation 在不同平台可表现为 `Renamed`，也可分裂为 target `Added` 加 source `Removed`。因此禁止用 wall-clock window、单纯 URI match 或“mutation 后丢弃下一批”压制回环。后续 fence 由 Runtime mutation receipt 持有 expected generation、source absent 与 target BLAKE3 digest；watch worker 仅在当前文件状态完全匹配 receipt 时消费对应 source 事件，任何 digest 不同、unknown path、overflow/reconciliation 或 generation 不匹配事件都走既有 candidate refresh。该设计保持 external write 可见、无稳定帧 I/O，成本为每个匹配事件一次 bounded affected-file stat/digest（`O(B)`，只在 mutation 后到达的有限事件），并记录 matched/mismatched/expired/suppressed counters 后再采集 1/1k/100k move 数据。 |
| 2026-08-23 | Relocation prepare structural cost audit | `bottleneck_confirmed_by_source_review / optimization_not_started` | `ProjectAssetManager::relocate_project_source` 先 `active_project.clone()`，`prepare_source_relocation_generation` 再 `self.clone()`，当前 `ResourceRegistryStaging` 同样以 full registry candidate 工作；虽然 entry/referencer rewrite 是 closure-bounded，整体仍是 whole-generation clone，不符合大工程 rename/move 的设计目标。优化不得先微调 HashMap 或排序：先把 project/asset/resource/catalog state 分成 immutable generation handle 与 typed targeted overlay，prepare 仅保存 changed/removed entries、reverse-edge deltas、source-path delta 和 expected fingerprints，commit 时在 generation gate compare-and-apply；rollback/recovery receipt 保持同一 mutation identity。采样计划：在实现前后分别记录 `N/S/R/D/B`、clone bytes、allocator peak、prepare/commit/publish elapsed、watcher suppression 与 p50/p95；规模为 1/1k/100k assets 且分别覆盖 no-referencer、fanout 和 compound fixture。未完成 overlay、受管 benchmark 与 hardware/corpus receipt 前，不声明性能、功耗或 Unreal 对标。 |
| 2026-08-23 | static acceptance | `passed_with_external_format_lock` | `py -3 tools/tests/test_runtime87_reference_resolution_contract.py` 为 17/17（新增 source/meta/registry 同 generation relocation、expected retired digest、ProjectManager internal-candidate hard cut、Pipeline owner、Editor gateway 与 explicit resource rename guard）；`py -3 tools/tests/test_editor10_asset_refactor_preflight_contract.py` 为 2/2。此次 core transaction、project manager relocation、pipeline relocation、Editor gateway、recovery policy 与集成测试的 scoped `rustfmt --check`、scoped `git diff --check` 通过（仅 LF/CRLF 提示）；`core/resource/registry.rs` 最后一行格式检查仍被外部、无当前权限终止的 orphan `rustfmt` PID 48644 映射锁阻止，未宣称其格式通过。未采集 CPU、GPU、功耗或规模性能数据。 |
| 2026-08-23 | managed Cargo validation | `first_attempt_failed / owned_compile_fixes_applied / rerun_running` | jobs `7337418614d64cefa9907510a40e44a7`、`67ab0952d25047dc84e574b7bf4bf683` 与 `03aeb414cf824fbe8286d2fb5eb20fa8` 都在首次依赖下载/编译期被 coordinator 标为 `finish_blocked/cargo_process_tree_alive`；没有 Rust 编译诊断或测试摘要。job `cc6609336cf443eca4981c469f1d402d` 已给出终态 `101`，但在编译前因共享 `Cargo.lock` 与 `--locked` 不一致而拒绝更新 lockfile，未执行测试。job `7eeebade3dd94d30b6fcc5cf3e81bb0a` 首次进入 lib-test 后以 `101` 结束：本切片已修正 relocation helper 可见性、candidate re-export、HashSet 类型、`thiserror` 的非 error `source` 字段和 catalog 测试 move；同次仍有 33 项 UI/graphics 等外部 lib-test 编译错误，定向 Rust 测试未执行，不计通过。当前 job `bc8ec3d68f934f5685d076bc65980950` 在受管 F 盘 target 运行 `cargo check -p zircon_runtime --lib --locked --jobs 1`，以隔离 test-module 噪声；收到 terminal receipt 前不计为通过或失败。 |

## 未关闭边界

- Runtime87 P0 不因本记录关闭：动态 Rust 测试尚无可采信终态。
- `ReferenceRepairPlanner`、同事务 source/meta/artifact/registry/catalog commit、restore parity、redirect/tombstone 和 Editor save preflight 仍属于 Runtime87 M3-M7/Editor10 的后续 owner，不能由 resolver 或 native adapter 自动补偿。
- 性能资格、百万引用、批量 mutation、fault/crash 和真实工程 rename/move 尚未开始；在获得正确性终态前不声明与 Unreal 等参考引擎的性能或功耗可比性。
