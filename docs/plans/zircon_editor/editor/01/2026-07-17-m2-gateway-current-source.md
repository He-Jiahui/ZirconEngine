---
plan_source: docs/plans/zircon_editor/editor/01-editor-kernel-and-runtime-interaction.md
related_code:
  - zircon_editor/src/core/gateway/contract.rs
  - zircon_editor/src/core/gateway/capabilities.rs
  - zircon_editor/src/core/gateway/error.rs
  - zircon_editor/src/core/gateway/handle.rs
  - zircon_editor/src/core/gateway/in_process.rs
  - zircon_editor/src/core/gateway/mod.rs
  - zircon_editor/src/core/gateway/session.rs
  - zircon_editor/src/tests/gateway/in_process.rs
  - zircon_editor/src/tests/gateway/session.rs
  - zircon_editor/src/tests/ui/boundary/editor_event_cutover.rs
  - zircon_app/src/entry/runtime_library/loaded_runtime.rs
  - zircon_app/src/entry/runtime_library/runtime_session.rs
  - zircon_app/src/entry/runtime_library/tests.rs
  - zircon_app/src/entry/entry_runner/editor.rs
  - zircon_app/src/entry/entry_runner/editor/tests/runtime_loading.rs
status: m2.2-review-hardening-current-source-revalidation-pending
---

# Editor01 M2 Gateway 当前源进度记录

## 产出记录与时间

| 时间 | 里程碑/切片 | 状态 | 完成项目与证据 | 后续门禁 |
| --- | --- | --- | --- | --- |
| 2026-07-17 | M2.1 `InProcessGateway` 借用式访问基础 | `in_progress` | `EditorRuntimeGateway` 新增对象安全的 `with_world`/`with_world_mut`；`InProcessGateway` 持有 `CoreHandle + LevelSystem` 并直连同一 world；稳定 handle 转发当前 transport；detached transport 定型返回 `RequiresSerializedAccess`；crate 根导出新实现；新增 3 项合同测试与模块文档。TDD RED：reservation `6f1a0f2bb6394912b9f418881e292530`、job `509976e304b74589b9caed5d440ebcf4`、run `2db3ffe3005d42989490ba07725bda8d`，得到预期 E0432/E0599。GREEN：reservation `660d315f4a83421db6fc698953acd806`、job `4828ff6ea53644bdb2fa6dbaf1c3a9db`、run `744043094ded46d68efab2c63acf2c1b`，Windows 执行 `cargo test -p zircon_editor --lib gateway::in_process --locked --jobs 1 -- --test-threads=1`，3 passed / 0 failed，exit 0。 | M2.1 尚未完成：仍需迁入 `EditorRuntimeClient`、清除 `src/ui/**` 对 runtime scene/core 深路径的直接依赖并补消费端测试；M2.2 的 `SessionGateway`/`RuntimeCapabilities`、M2.3 的 `selected_node` 删除及 M2.4 守卫测试均不得由本基础切片提前标记完成。 |
| 2026-07-17 | M2.1 借用回调死锁与旁路审查整改 | `current_source_review_accepted-validation_blocked` | 独立首审 0/2/0 定位同线程重入 world mutex 死锁及公开 `core()/level()` 旁路；TDD RED reservation `3a742c4e4bd24c0bae1b40ccb35acadc`、job `69df9e6978444e04afc4067e2fe5c739`、run `58a0c583bf95480ab55d033de062715e` 得到预期 `ReentrantBorrowedWorldAccess` E0599×2。实现线程局部 RAII 叶节点守卫、类型化 fail-fast 错误并删除两个 raw accessor；合同扩为 7 项，覆盖连续 nested Err、panic unwind/poison recovery、跨线程 TLS 隔离。最终 current-source 独立复审 Critical/Important/Minor = 0/0/0。最新 GREEN reservation `7b441e22b36342e1b0632b6dd9aaa38c`、job `a93d2db3edd9446b95a102fc3da07efc`、run `9ba1d618f83b41ffbb900e3a8ade7d71` 在执行测试前被 Runtime Text01 `FontDatabase::has_same_render_inputs` E0624 阻断，exit 101；gateway 7 项尚未在该源码上执行。 | Text01 failure 当时已导入；后续修复与回传见 [fixed-2026-07-17-font-database-render-input-equivalence-visibility](fixed-2026-07-17-font-database-render-input-equivalence-visibility.md)。该行保留阻断时点证据，不代表当前仍 open。 |
| 2026-07-17 | M2.1 current-source 最终聚焦验收 | `verified-foundation` | Text01 最低可见性修复进入共享当前源后，首轮最终门 reservation `a524c3d6ee1641e79510ffbca65ea3e7`、job `3582157c450541f3a2c612c667885a0e`、run `24b668d8fe1345e6962aaa6d4a5868fc` 暴露本测试夹具 E0507：`FnMut` 回调错误 move 捕获的 `thread_gateway`。夹具改为回调内 clone 后 move，并通过独立 current-source 复审 Critical/Important/Minor = 0/0/0。最终 reservation `b157e6c8f4c645149f4dca36066edc41`、job `37b0965d5e7647bb8952c3adb523145d`、run `6b173cb849884a49b827961fdfcb6667` 在 Windows 执行同一命令，7 passed / 0 failed / 3235 filtered，exit 0；覆盖同 world 读写、稳定 handle、连续读写重入拒绝、panic 后恢复、跨线程 TLS 隔离及 detached 拒绝。 | 本行只验收 `InProcessGateway` 借用访问基础；M2.1 的 runtime client 迁移、UI 深路径清理及 M2.4 守卫仍未完成，父计划继续 `in_progress`。Text01 visibility lifecycle 已 fixed 回传；后续 Runtime15 font-id report 漂移也已由 [fixed-2026-07-17-screen-space-ui-text-font-id-report-mount-drift](../../../zircon_runtime/text/01/fixed-2026-07-17-screen-space-ui-text-font-id-report-mount-drift.md) 以受管 `text_font` 47/47、独立复审 0/0/0 回传。 |
| 2026-07-17 | M2.2 `SessionGateway`、capabilities 与 app transport cutover | `current_source_landed-review_findings_open-validation_blocked` | 当前源码已新增 `SessionGateway`、owned `RuntimeCapabilities`、宿主所有权 `EditorRuntimeFrame`，并覆盖 tick/event/frame/profile/plugin-event/operation；`profile_control` 缺指针返回 `Ok(None)`，其余必需入口类型化报 `CapabilityMissing`；owned output 在成功、空批次、invalid JSON、malformed buffer、runtime error 与 frame-copy 路径恰好释放一次；app 已删除 `RuntimeSession` 的 gateway trait/profile bridge并由 stable handle 注入 `SessionGateway`。独立复审 Critical/Important/Minor = 0/2/0：一项要求 editor-normalized API table 删除 create/destroy lifecycle authority，另一项即本 M2.2 子计划记录缺失。test-first 已新增 `editor_gateway_api_table_strips_session_lifecycle_authority`；首轮 managed reservation `8e2b49cdf7ac4ad886bcf0ad8d311d2e`、job `5134b3bff56c478da5607b04d8bd4495`、run `c6085185488f45b4ae825127483a5238` 在测试前被 Runtime12 active owner 的 `event_buffer/frame.rs` Windows `os error 5` 阻断，exit 101，不能冒充 RED。对应最低共享问题由 [Runtime12 input-event-buffer visibility failure](../../../zircon_runtime/runtime/12/failure-2026-07-17-input-event-buffer-visibility.md) 处理；随后为重试创建的 reservation `ed5b17866c734f68a4cc0517b0fae55c` 因 Render01 E0425 仍会在断言前阻断而由本会话主动释放，状态 `released`、`jobId=null`，没有运行 Cargo。 | Render01 owner 修复 E0425 后，以相同 canonical compatibility 新建精确预约并执行 lifecycle GREEN；随后执行 `cargo test -p zircon_editor --lib gateway:: --locked --jobs 1 -- --test-threads=1` 与 app runtime-library gate。M2.2 在 final review 0/0/0 和 managed gates 通过前不得标记 verified；M2.4 UI 深路径守卫仍独立 pending。 |
| 2026-07-17 | M2.2 session lifecycle authority hard cut | `code_static_review_passed-cargo_pending` | test-first guard 已先加入，静态 RED 前置条件确认旧 `editor_gateway_api_table` 同时复制 create/destroy；真实 RED 尝试被 Runtime12 active rewrite 窗口阻断，未伪造测试结果。随后最小硬切删除 normalized editor table 的两条 lifecycle 赋值；`RuntimeSession::create_with_profile_and_project` 与 `Drop` 继续是唯一 create/destroy owner，SessionGateway 只收到实际消费的调用入口。`rustfmt --check`、scoped `git diff --check` 与 source guard 通过：editor table lifecycle assignments = 0，`RuntimeSession::drop` destroy call = 1。最终 current-source 独立复审 Critical/Important/Minor = 0/0/0，并确认 M2.4 基线为 19 production files / 47 hits（16 explicit + 3 wildcard），没有遗漏 raw-type consumer。 | 旧重试 reservation `ed5b17866c734f68a4cc0517b0fae55c` 已无 job 释放，避免已知外部编译失败长期占据 FIFO；待 Render01 active owner 修复 E0425 后新建预约执行 managed GREEN。当前只声明静态实现与复审通过，不声明 lifecycle test、gateway matrix 或 app gate 通过。 |
| 2026-07-17 | M2.2 lifecycle managed GREEN | `verified-lifecycle-gateway-matrix-pending` | Render01 编译阻断关闭后，新 reservation `fcf1058bc3054e2abf4d1766f540f990` 原子消费为 job `18d3e80c10094fe09357ae25892bc2b8`，run `2feb43d1b0944a389cbc7cc4b3a7a0e7` 在 Windows 执行 `cargo test -p zircon_app --lib --no-default-features --features target-editor-host editor_gateway_api_table_strips_session_lifecycle_authority --locked --jobs 1 -- --test-threads=1`；结果 1 passed / 0 failed / 175 filtered，exit 0。该门同时断言 editor-normalized table 的 create/destroy 为 `None`，且 `RuntimeSession::Drop` 仍取得并调用 destroy 指针。 | 首个 gateway matrix reservation `5351365f78e24e4ba572e2ca40339e13` 在 foreign cold-target FIFO 等待期间因 heartbeat helper 退出而随 Session stale 原子变为 `expired/jobId=null`，没有运行 Cargo；Session 与 29 条租约已恢复，相同 command fingerprint/compatibility 的新 reservation 为 `d7870672e0b0472584092e2202e97d40`。app runtime-library 上行门仍需在 matrix 通过后新建精确预约。 |
| 2026-07-17 | M2.2 gateway contract matrix managed GREEN | `verified-prior-fingerprint-review-hardening-superseded` | 首次实际执行的精确 reservation `5ef7b68c61e840e18a9166111b01615c`、job `a4ff529bd2d64b4984fd09783e31af68`、run `5636833c80374faf974920f821287952` 在测试前被 Performance01 所属 `text_input/edit.rs` `SharedString` 转换 E0282 阻断，exit 101；该失败按 lifecycle key 回传并由 [fixed-2026-07-17-editor-text-input-shared-string-type-inference](../../editor_layout/19/fixed-2026-07-17-editor-text-input-shared-string-type-inference.md) 关闭，受管提交为 `43a1957e929739e229fcd34ab0ef1c36f0f156c3`。随后 reservation `5f75415a397c4b74a5edc3e8dde1920d` 原子消费 job `13392907003549dbac31e080da7ab7aa`；run `ff0b13f008754335abe011470ad59f75` 在 Windows 执行 `cargo test -p zircon_editor --lib gateway:: --locked --jobs 1 -- --test-threads=1`，24 passed / 0 failed / 3334 filtered，exit 0。 | 该 24/24 只证明评审整改前的精确源码指纹。本轮协议加固已改变 gateway/app 源码，原 app reservation `1b02c0fbbda6495c9385c057654310a6` 已在 `jobId=null` 下释放，必须为新指纹重跑 gateway matrix 与 app runtime-library 门。 |
| 2026-07-17 | M2.2 provider 输出与响应身份评审加固 | `source_landed-static-green-managed-revalidation-pending` | 独立评审得到 Critical/Important/Minor = 0/2/4。test-first 增加 oversized owned-buffer、error-status callback-less storage、截断 RGBA、跨 subscription delivery、poll/harvest handle 交叉响应；实现现已在成功和非 OK status 两路先验证 `len <= capacity <= isize::MAX`、null/free 合同并统一释放，非空 frame 强制 `width*height*4`，plugin delivery 与 operation response 强制匹配请求身份。app normalized table 进一步删除未被 `SessionGateway` 消费的 viewport bind/unbind/present 与 host-request drain 指针；source guard 固定这些字段均为 `None`。`entry_runner/editor.rs` 的 367 行内联测试硬切到 `editor/tests.rs`，生产 owner 从 862 行降为 496 行。文件级 `rustfmt --check` 与 scoped `git diff --check` 已通过。 | 当前只声明源码与静态检查；独立复审、`gateway::` 新指纹、app runtime-library 新指纹及上行门均待受管执行。未完成前不得把本行提升为 verified。 |
| 2026-07-18 | M2.2 generation-bound gateway/capability snapshot | `source_landed-static-green-coordinator-validation-blocked` | Performance01 priority-0 failure 已按 TDD 硬切：workspace 引入 `arc-swap 1.9.2`；stable handle 发布 immutable `{generation, gateway, capabilities}`，数据面只持原子 guard，低频 replace 由可 poison-recovery 的 writer mutex 串行；`RuntimeCapabilities` trait/API 改为 generation-bound `Arc`，删除逐调用深 clone。测试覆盖同 generation Arc 身份、replace generation、旧 transport 并发寿命、失败 replace 原子性/恢复及 `RwLock` 零命中。文件级 `rustfmt --check`、gateway static contract 6/6、dependency guard 3/3、test inventory 4/4 与 scoped `git diff --check` 通过。旧 exact-11 reservation `a604598586b74e0e8e6b4d63fe948347` 因租约已过期且 Coordinator01 的绝对过期 pending reservation 未推进 FIFO，由本会话在 `jobId=null/startedAt=null` 下主动释放，未执行 Cargo。 | 待 `pending-cpu-reservation-absolute-expiry-not-enforced` fixed return 后，在重新取得的 exact scope 上生成 fresh source manifest/reservation，执行 `gateway::handle` locked gate；GREEN 后跑 gateway matrix、独立复审与 WPR/计数证据，再生成 fixed return。 |
| 2026-07-29 | M2.4 `core/ -> ui` 单向边界守卫 | `in_progress / static_guard_green / managed_validation_pending` | TDD 先在 `src/tests/ui/boundary/editor_event_cutover.rs` 新增 `core_remains_independent_from_the_ui_layer`，递归检查全部 `src/core/**/*.rs`，拒绝 `use crate::ui` 与 `crate::ui::` 两类真实反向依赖；源码库存结果为 0，`rustfmt --check` 与 scoped `git diff --check` 通过。Coordinator snapshot `1241` 冻结该测试文件为 `9882798831756e924acbea06bc84010ae1af78adec36433107c0935ffda0b69b`。该行只冻结已达成的 core 单向依赖，不把尚存 UI runtime 深路径迁移计为完成；未启动 Cargo。当前受管 immutable copy 在 `validation_copy_overlay_not_owned` 终止，归入 [Coordinator01 live lease attribution divergence](../../../zircon_tooling/session_coordinator/01/failure-2026-07-26-live-lease-attribution-validation-copy-divergence.md)，不能用共享工作树测试替代。 | 继续按 facade owner 拆迁 `src/ui/**` 的 raw runtime consumers；当前工作树以 `CoreHandle`/`CoreWeak`/`LevelSystem` 全词库存重算为 20 个生产文件、56 处文本命中（排除 `*_tests.rs`），且 `EditorWorldSlot` 仍持有 `LevelSystem`，因此 M2.4、双实现矩阵和 M2.1 均保持 `in_progress`。其中 authoring gateway 被 runtime session 覆盖的生命周期设计已交接 [Editor04 failure](../../04/failure-2026-07-29-authoring-runtime-gateway-ownership-conflation.md)。Coordinator01 修复并支持当前精确 overlay 后，建立 fresh source-bound manifest 再执行受管 Cargo 与独立评审。 |

## 架构边界

- 借用式 world 访问只属于同进程实现；detached/session-backed 实现必须通过序列化合同，不增加兼容旁路。
- `EditorRuntimeGatewayHandle` 是消费者持有的稳定门面；UI 不应缓存 gateway 内部的 `LevelSystem` 或 `World`。
- Stable handle generation 通过 `ArcSwap` 原子发布；数据面不得恢复 `RwLock`、transport Arc clone 或 owned capability 深复制。replace 构建失败必须保持上一 generation，可恢复 writer poison 后再发布。
- 借用回调是持 world mutex 的叶节点：回调内再次进入任一 gateway 借用 API 会得到 `ReentrantBorrowedWorldAccess`，不得等待自锁；panic 后 RAII 恢复访问状态。
- 回调内只做有界读取或原子编辑，不执行资产 IO、全量投影或等待任务，避免把 UI/渲染帧阻塞在 world mutex 上。
- 当前操作 API 的 capability-missing 行为保持不变。本切片没有伪造 operation/session 能力，也没有新增旧架构兼容层。
- `RuntimeSession` 是 create/destroy 的唯一生命周期 owner；`SessionGateway` 只接收调用所需的 normalized entries。当前源码已删除 normalized table 的 lifecycle pointers，最终 current-source 复审为 0/0/0；lifecycle 1/1 与 gateway matrix 24/24 已受管 GREEN，app runtime-library 上行门仍 pending。

## 未完成项目

### 2026-08-15 M2.4 typed host runtime access（current source，未验收）

`EditorManager` composition root 当前构造 `EditorHostRuntimeServices`，`EditorUiHost` 不再保存或导出 `CoreWeak`/`CoreHandle`/`runtime_core()`；project asset/config、VM bridge、runtime diagnostics 和 foreground document-save job 分别通过该 typed access 的具名操作取得所需服务。capability enablement 使用短生命周期 `EditorCapabilityConfiguration` 持有 runtime 至 config store、report apply 和失败 rollback 完成，避免旧 `CoreHandle` 事务在 plugin state publish 中途释放。`editor_capability_report.rs` 只应用已解析的 `EditorSubsystemReport`，不重新查询 runtime。静态 source contract、`rustfmt --check --config skip_children=true` 和 scoped diff-check 已通过；完整 recursive rustfmt 仍受已有 Rust-2024 let-chain sibling 与 workspace 2021 edition 不匹配阻断，未运行 Cargo。`DefaultEditorAssetManager` project access 继续由其专属 owner 处理；retained host 已改为 composition-owned runtime lease、viewport typed weak access 与 asset typed access leaf。本行不宣称 M2.4 全局清零、性能数据、独立复审、managed validation 或 accepted milestone。

- `EditorRuntimeClient` 当前源码 occurrence 已为 0，旧 client owner 不再存在；不得为迁移名义恢复兼容 wrapper。
- `editor_subsystems` 的 runtime-backed report/sandbox helpers 已从 `pub(crate)` 收紧为 `pub(super)`；仅 host 内的 `EditorHostRuntimeServices` 可调用，静态 RED/GREEN guard、scoped rustfmt 与 diff-check 已通过。该收敛不改变残余 raw-type consumer 的职责归属，也不替代受管 Cargo。
- `EditorUiHost::refresh_capabilities` 只负责从 typed services 取得一次 report，随后委托 `editor_capability_report` 的唯一 apply owner；不再复制 snapshot/lock/view-registration 逻辑。M2.4 guard 已先验证旧重复体为 RED、再验证委托和唯一 projection 为 GREEN。
- `editor_event_runtime_access.rs` 已物理硬切为同名 folder-backed owner：薄 `mod.rs` 只接线 asset/component/event/input/extension/snapshot/status 具名叶子，最大 239 行；asset access 只消费 `core::resource::ResourceManagementGeneration`，不恢复 `framework::asset::ResourceManagement*`、raw Core/ManagerResolver/LevelSystem。M2.4 source guard 锁定旧文件删除、facade 挂载、资源 owner 与每叶 800 行预算；尚未运行 Cargo。
- retained viewport 的 `ViewportState` 与 `RenderFrameworkResolveJob` 已不再保存或导入 `CoreHandle`，controller constructor 也只接收 `ViewportRenderFrameworkAccess`。`RetainedEditorHost` composition root 的 25 行 `RetainedHostRuntimeLease` 是唯一强 runtime owner：它在 startup 前持有 core、仅派生 render-framework weak access，随后 host 完整生命周期保活 runtime；startup 仅借用该 lease 的 bootstrap core。45 行 access 只异步取得 `RenderFramework` stable handle，并在既有 UI-thread 时机解析该 handle，不能暴露 generic resolver 或改变 ticket/cancel/resolve 线程模型。结构 guard 先以 state=4/job=3 个 `CoreHandle` 命中和缺失 composition lease 复现 RED，hard cut 后 state/job/controller 均为 0；弱 provider 在 runtime 已释放时的 fail-closed ticket 回归已加入。静态 `rustfmt`、source contract 与 scoped diff-check 通过，尚未运行 Cargo。
- retained host 的 asset 相关 runtime access 已由 host/state、`StartupManagers` 与 `app/assets.rs` 中散落的三类 `ManagerServiceHandle` 和 `ManagerResolver` 硬切为 45 行 `RetainedHostAssetRuntimeAccess` 具名叶子。它只提供 `asset_manager`、`editor_asset_manager` 与 `resource_manager` 三项已命名服务，startup 仅以短生命周期解析服务完成变更订阅后移交 leaf；不暴露 generic `resolve`，不改写 Editor09 的 asset-manager implementation、catalog 或 asset-workspace projection。TDD source guard 已先确认旧 state/manager resolver 布局为 RED，hard cut 后 typed-access contract、scoped `rustfmt` 与 diff-check 为 GREEN；尚未运行 Cargo。
- `EditorManager::create_runtime_level` 已硬删除为对 caller 的 API，替换为 `prepare_authoring_world(Scene) -> AuthoringWorldSeed`。场景 picker install、默认场景重载、startup restore 与 welcome-session apply 四条 workflow 只传递 core-owned opaque seed 给既有 `EditorState::replace_world`/construction；`EditorUiHost::create_runtime_level` 也已删除，`create_level` 与 `LevelMetadata` 仅保留在 `EditorHostRuntimeServices` typed leaf。source guard 先以旧 raw call 为 RED，后验证零 production caller、typed seed signature 与 runtime leaf 唯一 construction 为 GREEN；该收敛不改变 edit/play gateway 分域、scene transition 或 Editor04 的 P2 lifecycle，尚未运行 Cargo。
- `ForegroundDocumentSaveJob` 已不再携带 `CoreHandle` 在 worker 期反查 manager；`EditorHostRuntimeServices` 在 admission 时以既有 typed access 解析一次 `EditorManager` 并只传入 `Weak<EditorManager>`。job 运行时 manager 已释放即 fail-closed，既不延长 runtime 生命周期也不开放 generic service resolver；interactive save mutex、cancel-before-run 与 canonical save/dirty-token 语义未变。source guard 先确认 raw core job 为 RED，随后验证 weak owner、无 job-side resolver 与 fail-closed upgrade 为 GREEN；尚未运行 Cargo。
- `EditorHostRuntimeServices::config_manager` 已从临时 `ManagerResolver` 改为与 asset access 相同的 `config_manager_handle` + `resolve_manager_service` stable-handle 路径；仍在请求时升级 `CoreWeak`，不前移服务可用性检查、不新增 service locator 或 fallback。scoped rustfmt 与 source contract 确认 host runtime service 已无 generic resolver；尚未运行 Cargo。
- `editor_subsystems` 已从 runtime-core helper 收敛为纯配置投影：`EditorHostRuntimeServices` 与短生命周期 capability transaction 各自在 typed owner 内加载 `Option<Vec<String>>`/`Option<bool>`，再传给 report/sandbox helper。缺失或解码失败仍按既有默认 capability/sandbox=true 语义处理；新增 unit cases 覆盖 known/custom capability、默认集合与 sandbox default。该拆分移除 helper 层的 raw `CoreHandle`，不创建第二配置权威；尚未运行 Cargo。
- `EditorManager::save_project(&Scene)` 当前仅由 host menu action 触发，已由 `pub` 收紧为 `pub(crate)`；project persistence 后的 document `Saved` publish、asset refresh 与失败语义未改变。独立 host test 锁定 raw `Scene` persistence 不得重新成为跨 crate API，本项同样不替代 Editor04 的 authoring-domain 迁移。
- `EditorHostEventController::project_scene_snapshot` 仅由 retained-host automation 在 crate 内消费，已由 `pub` 收紧为 `pub(crate)`；对外仍保留 typed `EditorDataSnapshot`、`RenderSceneSnapshot` 与 frame extract 投影。folder test 固定 raw `Scene` 不得再次跨 crate 泄露，本项不改变 Editor04 的 authoring-world 生命周期。
- `core/gateway/in_process.rs` 的 inline `DefaultLevelManager` fixture 已迁入 canonical `src/tests/gateway/in_process.rs`；production owner 不再含 `#[cfg(test)]` 或 concrete manager token，viewport latest-generation behavior 保留在外部 test。当前 production/test owner 分别为 129/332 行，均满足结构预算；此迁移不改变 gateway ABI、highlight 或 world-sync 行为。
- `core/gateway/detached.rs` 与 `core/gateway/highlight_set.rs` 的 inline behavior tests 也分别迁入 canonical `tests/gateway/handle.rs` 与 `tests/gateway/highlight_set.rs`；三项 production leaf 均不再混入 test module。`handle.rs` 的公开 highlight/viewport forwarding 行为也已迁入同一 canonical test owner，production/test owner 目前为 379/456 行；仅保留无法经公开状态构造的 `next_generation(u64::MAX)` 私有 overflow unit，不为迁移测试增加 test-only public API。`session/tests.rs` 的 9 项 private protocol/surface-lifecycle 用例与外部 session suite 不重复，仍须按私有 fixture ownership 单独设计迁移，当前不删除或降级覆盖。
- 清点并迁移 `zircon_editor/src/ui/**` 内 `zircon_runtime::scene/core` raw-type consumers。2026-08-15 以 `CoreHandle`/`CoreWeak`/`LevelSystem`/`ManagerResolver` 全词、排除 `src/tests/**` 和 `*_tests.rs` 重算为 11 个生产文件 / 37 hits：typed host composition/service 2 文件，Editor09 asset-manager access 2 文件，retained-host bootstrap/composition、asset access leaf 与 viewport typed access 7 文件。该机械库存包含允许的 typed owner（不等同于 pane/workspace 旁路）；asset access leaf 将 generic resolver 收敛到唯一具名 owner，viewport state/job/controller、manager/host-facing raw level API、foreground autosave job、host config generic resolver 和 core-bound subsystem helper 已退出，composition lease 是唯一强 owner。历史 19/47、20/56、16/42 与 16/47 是不同 worktree 或查询口径，不再作为当前分类依据。M2.4 guard 必须同时拒绝显式 import 与 wildcard 类型旁路；workbench world access 与 host/bootstrap services 需按不同 façade owner 分切，不能塞进同一个 session gateway。
- 完成双实现合同矩阵及 `core/`/`ui/` 边界守卫后，才能关闭 M2.1/M2.4。
- 依照总目标，后续实现优先服从 Runtime 启动、基础场景、渲染输入退出、保存重开、Editor 选择修改保存的 MVP 依赖顺序。
