---
related_code:
  - zircon_runtime/src/lib.rs
  - zircon_runtime/src/asset/mod.rs
  - zircon_runtime/src/scene/mod.rs
  - zircon_runtime/src/graphics/mod.rs
  - zircon_runtime/src/animation/module.rs
  - zircon_runtime/src/graphics/hybrid_gi_runtime_provider/mod.rs
  - zircon_runtime/src/dynamic_api/session.rs
  - zircon_runtime/src/scene/world/property_access/path_resolution.rs
  - zircon_runtime/src/animation/sequence/apply.rs
  - zircon_runtime/src/animation/sequence/target.rs
  - zircon_runtime/src/asset/assets/texture/descriptor.rs
  - zircon_runtime/src/asset/assets/texture/texture_asset.rs
  - zircon_runtime/src/asset/importer/ingest/import_texture.rs
  - zircon_editor/src/ui/layouts/windows/workbench_host_window/chrome_template_projection.rs
  - zircon_plugins/gltf_importer/plugin.toml
  - zircon_plugins/gltf_importer/runtime/src/plugin.rs
  - zircon_plugins/obj_importer/runtime/src/plugin.rs
  - zircon_plugins/texture_importer/runtime/src/plugin.rs
  - zircon_plugins/audio_importer/runtime/src/plugin.rs
  - zircon_plugins/opus_importer/runtime/src/plugin.rs
  - zircon_plugins/shader_wgsl_importer/runtime/src/plugin.rs
  - zircon_plugins/ui_document_importer/runtime/src/plugin.rs
  - zircon_plugins/asset_importers/data/runtime/src/plugin.rs
  - zircon_plugins/asset_importers/model/runtime/src/plugin.rs
  - zircon_plugins/asset_importers/shader/runtime/src/plugin.rs
  - zircon_plugins/plugin_sdk/src/runtime_exports.rs
  - tools/audit_plugin_structure.py
  - tools/plugin_structure_audits/capability.py
  - zircon_plugins/first_party_runtime_catalog/src/lib.rs
  - zircon_plugins/ai/runtime/Cargo.toml
  - zircon_plugins/ai/runtime/src/lib.rs
  - zircon_plugins/ai/runtime/src/capability.rs
  - zircon_plugins/animation/runtime/src/lib.rs
  - zircon_plugins/animation/runtime/src/capability.rs
  - zircon_plugins/hybrid_gi/runtime/Cargo.toml
  - zircon_plugins/hybrid_gi/runtime/src/lib.rs
  - zircon_plugins/hybrid_gi/runtime/src/capability.rs
  - zircon_plugins/navigation/runtime/Cargo.toml
  - zircon_plugins/navigation/runtime/src/lib.rs
  - zircon_plugins/navigation/runtime/src/capability.rs
  - zircon_plugins/physics/runtime/src/lib.rs
  - zircon_plugins/physics/runtime/src/capability.rs
  - zircon_plugins/particles/runtime/Cargo.toml
  - zircon_plugins/particles/runtime/src/lib.rs
  - zircon_plugins/particles/runtime/src/capability.rs
  - zircon_plugins/prefab_tools/runtime/Cargo.toml
  - zircon_plugins/prefab_tools/runtime/src/lib.rs
  - zircon_plugins/prefab_tools/runtime/src/capability.rs
  - zircon_plugins/rendering/runtime/Cargo.toml
  - zircon_plugins/rendering/runtime/src/lib.rs
  - zircon_plugins/rendering/runtime/src/capability.rs
  - zircon_plugins/solari/runtime/Cargo.toml
  - zircon_plugins/solari/runtime/src/lib.rs
  - zircon_plugins/solari/runtime/src/capability.rs
  - zircon_plugins/terrain/runtime/Cargo.toml
  - zircon_plugins/terrain/runtime/src/lib.rs
  - zircon_plugins/terrain/runtime/src/capability.rs
  - zircon_plugins/texture/runtime/Cargo.toml
  - zircon_plugins/texture/runtime/src/lib.rs
  - zircon_plugins/texture/runtime/src/capability.rs
  - zircon_plugins/tilemap_2d/runtime/Cargo.toml
  - zircon_plugins/tilemap_2d/runtime/src/lib.rs
  - zircon_plugins/tilemap_2d/runtime/src/capability.rs
  - zircon_plugins/virtual_geometry/runtime/Cargo.toml
  - zircon_plugins/virtual_geometry/runtime/src/lib.rs
  - zircon_plugins/virtual_geometry/runtime/src/capability.rs
  - zircon_plugins/zr_vm_language/runtime/Cargo.toml
  - zircon_plugins/zr_vm_language/runtime/src/lib.rs
  - zircon_plugins/zr_vm_language/runtime/src/capability.rs
  - zircon_plugins/net/runtime/src/lib.rs
  - zircon_plugins/net/runtime/src/capability.rs
  - zircon_runtime/src/ui/surface/input/editable_text.rs
  - zircon_plugins/animation/runtime/src/sequence/apply.rs
  - zircon_plugins/animation/runtime/src/sequence/target.rs
  - zircon_plugins/texture_importer/runtime/src/importers.rs
  - zircon_runtime/src/tests/runtime_absorption/code_review_findings.rs
implementation_files:
  - docs/engine-architecture/large-file-ownership-m1.md
  - docs/plans/zircon_runtime/runtime/15-code-structure-and-module-conventions.md
  - docs/plans/zircon_editor/editor_ui/10-code-structure-and-module-conventions.md
  - docs/plans/zircon_plugins/12-plugin-dx-and-structure-framework.md
plan_sources:
  - user: 2026-06-22 优化 docs/plans editor/runtime/plugins 计划，统一代码结构与插件接口开发体验框架
  - docs/engine-architecture/large-file-ownership-m1.md
  - docs/engine-architecture/runtime-interface-convergence.md
tests:
  - rustfmt --edition 2021 --check zircon_plugins/plugin_sdk/src/lib.rs zircon_plugins/plugin_sdk/src/runtime_exports.rs zircon_plugins/animation/runtime/src/lib.rs zircon_plugins/physics/runtime/src/lib.rs zircon_plugins/net/runtime/src/lib.rs: passed 2026-06-23
  - cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_sdk -p zircon_plugin_animation_runtime -p zircon_plugin_physics_runtime -p zircon_plugin_net_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-d12-export-macro-0623 --message-format short --color never: passed 2026-06-23 with existing zircon_runtime/net/physics warnings
  - cargo test --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_sdk --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-d12-export-macro-0623 --message-format short --color never runtime_plugin_exports -- --test-threads=1 --nocapture: 1 passed, 0 failed on 2026-06-23 with existing zircon_runtime warnings
  - rustfmt --edition 2021 --check zircon_plugins/plugin_sdk/src/lib.rs zircon_plugins/plugin_sdk/src/runtime_exports.rs zircon_plugins/ai/runtime/src/lib.rs zircon_plugins/animation/runtime/src/lib.rs zircon_plugins/hybrid_gi/runtime/src/lib.rs zircon_plugins/navigation/runtime/src/lib.rs zircon_plugins/net/runtime/src/lib.rs zircon_plugins/particles/runtime/src/lib.rs zircon_plugins/physics/runtime/src/lib.rs zircon_plugins/prefab_tools/runtime/src/lib.rs zircon_plugins/rendering/runtime/src/lib.rs zircon_plugins/solari/runtime/src/lib.rs zircon_plugins/terrain/runtime/src/lib.rs zircon_plugins/texture/runtime/src/lib.rs zircon_plugins/tilemap_2d/runtime/src/lib.rs zircon_plugins/virtual_geometry/runtime/src/lib.rs zircon_plugins/zr_vm_language/runtime/src/lib.rs: passed 2026-06-23
  - cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_plugin_sdk -p zircon_plugin_ai_runtime -p zircon_plugin_animation_runtime -p zircon_plugin_hybrid_gi_runtime -p zircon_plugin_navigation_runtime -p zircon_plugin_net_runtime -p zircon_plugin_particles_runtime -p zircon_plugin_physics_runtime -p zircon_plugin_prefab_tools_runtime -p zircon_plugin_rendering_runtime -p zircon_plugin_solari_runtime -p zircon_plugin_terrain_runtime -p zircon_plugin_texture_runtime -p zircon_plugin_tilemap_2d_runtime -p zircon_plugin_virtual_geometry_runtime -p zircon_plugin_zr_vm_language_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-d12-export-macro-0623 --message-format short --color never: passed 2026-06-23 with existing zircon_runtime and large-plugin warning noise
  - cargo metadata --manifest-path zircon_plugins/Cargo.toml --format-version 1 --no-deps --locked: passed 2026-06-23
  - python tools/audit_plugin_structure.py --json: capability_conformance.m4_runtime_capability_gate_status=runtime-capability-single-source-clean, audited_runtime_root_count=15, capability_source_mismatches=0 on 2026-06-23
  - cargo check --manifest-path zircon_plugins/Cargo.toml -p zircon_first_party_runtime_catalog -p zircon_plugin_ai_runtime -p zircon_plugin_animation_runtime -p zircon_plugin_hybrid_gi_runtime -p zircon_plugin_navigation_runtime -p zircon_plugin_net_runtime -p zircon_plugin_particles_runtime -p zircon_plugin_physics_runtime -p zircon_plugin_prefab_tools_runtime -p zircon_plugin_rendering_runtime -p zircon_plugin_solari_runtime -p zircon_plugin_terrain_runtime -p zircon_plugin_texture_runtime -p zircon_plugin_tilemap_2d_runtime -p zircon_plugin_virtual_geometry_runtime -p zircon_plugin_zr_vm_language_runtime --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-m4-capability-0623 --message-format short --color never: passed 2026-06-23 with existing zircon_runtime and large-plugin warning noise
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test --manifest-path zircon_plugins/Cargo.toml -p zircon_first_party_runtime_catalog --no-default-features --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-m4-capability-test-nodebug-0623 --message-format short --color never plugins_12_capability_single_source_conformance -- --test-threads=1 --nocapture: 1 passed, 0 failed on 2026-06-23
doc_type: convention-authority
status: planned
---

# 引擎级代码结构与模块接口规范（Engine Code Structure & Module Interface Convention）

> 本文是 ZirconEngine **代码结构与模块接口约定的唯一权威**。`docs/plans/**` 各计划只**引用**本文，不重定义其规则；与任何子计划正文冲突时以本文为准。
>
> 落地子计划：Runtime 15（`zircon_runtime/runtime/15-...`）、Editor UI 10（`zircon_editor/editor_ui/10-...`）、Plugins 12（`zircon_plugins/12-...`）；render / hub index 增节引用。强制机制见 §7。

## §0 适用范围与背景

适用 `zircon_runtime` / `zircon_editor` / `zircon_plugins` / `zircon_runtime_interface` / `zircon_app` / `zircon_hub`，含 render 子计划覆盖的 `graphics/**`。

引擎已非常庞大，分层方向合理，但模块内细节散乱，直接损害用户 code review / inspect 体验，集中表现在六类结构债：模块布局不统一、命名失序、巨型文件、公共 API 不友好、测试组织三套并行、插件 DX 割裂（证据见各落地子计划"现状缺口表"）。

本规范复用仓库既有的结构治理范式——`audit_runtime_structure.py` + `runtime_structure_audits/*.py` 审计脚本族、`tests/runtime_absorption/**` guard 测试、`docs/**` 镜像文档、`large_file_ownership_gate`（`m1_gate_status` / owner 分类 / 迁移债）——把"统一模块规范 + 插件 DX 框架"做成同形门禁，对**后续开发**与**存量重构**均可机器化强制、可验收、可防回归。

## §1 模块文件布局标准（owner-module 模式）

| 规则 | 定稿 | 反例（实证） |
|---|---|---|
| R1.1 根接线薄 façade | `lib.rs` / `mod.rs` 仅含 `mod` 声明 + 精选 `pub use` + 模块 doc，**零行为** | `plugin/mod.rs`（74 行扁平导出） |
| R1.2 `mod.rs` vs `module.rs` | 目录统一用 `mod.rs` 作 façade；引擎子系统的 `ModuleDescriptor` 单独放 `module.rs`，**且仅注册子系统**才有 `module.rs` | `input/mod.rs` 直塞注册 / `graphics/` 注册分散无 `module.rs` |
| R1.3 行为落具名 owner 叶子 | 行为进 `lifecycle.rs` / `dispatch.rs` / `validation.rs` / `diagnostics.rs` / `conversion.rs` / `extract.rs` 等具名模块，不堆胖单文件 | `dynamic_api/session.rs`（773 行协调 17 子模块） |
| R1.4 行数预算 | 生产文件软上限 800（review 警告）、硬上限 1000（gate 拦截）；测试文件 > 800 必须 folder-backed 拆分；豁免（vendored upstream、fixture、`@generated`）须在 gate `exempt` 字段登记 | `core/framework/tests.rs`（1848） |
| R1.5 嵌套深度 | 同一分类维度 ≤ 3 层；域重叠（如 `asset/assets/scene/animation/`）须拍平 | `asset/assets/scene/` 深嵌 |

**`module.rs` 存在判据**：当且仅当该目录对应一个会向 runtime/editor 注册的 `*Module`（拥有 `module_descriptor()`）。否则不得出现 `module.rs`。

owner 拆分纪律继承 `large-file-ownership-m1.md`：**按 ownership 拆，不按等行数切**；root 文件可作结构 façade，但不得为避免改调用方而保留行为；拆分时不留兼容 wrapper，消费方直接调用新 owner 路径或精选 façade。

## §2 命名规范

- **R2.1 复数 / 单数判定**：目录名是"其下每个文件都是它的一个实例"的**种类** → 复数（`components/`、`assets/`、`importers/`、`systems/`、`effects/`）；目录是"单一内聚子系统 / owner" → 单数（`manager/`、`dispatch/`、`pipeline/`、`backend/`、`layout/`）。判定测问句："该目录名是不是一类东西、其下每个文件是它的一个？"是则复数，否则单数。
- **R2.2 前缀允许词表**（其余前缀视为命名债）：
  - `runtime_`：**仅**当与 authoring/descriptor 孪生对比时（`runtime_asset_path` vs `asset_path`）。**禁止**当通用命名空间标签——已在 runtime crate / `*_runtime_provider` 目录内的模块不得再冠 `runtime_`（`hybrid_gi_runtime_provider/runtime_state.rs` → `state.rs`）。
  - `default_`（默认实现）、`builtin_`（内置目录 / 枚举）、`compiled_`（编译后产物）、`frozen_`（冻结表）。
- **R2.3 禁用无主名**：模块名禁用 `_inner` / `_impl` / `_helper` / `util(s)` / `misc` / `common`；改成描述其 owns 什么的名字（例如旧 `editor_event_runtime_inner.rs` 已按职责硬切为 `editor_event_runtime_state.rs`）。
- **R2.4 文件名 snake_case**（已普遍满足，纳入审计兜底）。
- **R2.5 构造目录命名**：放构造逻辑的目录用 `construct` / `builder`，**禁用 `*_new` 后缀**——`_new` 与 hard-cutover 的迁移气味后缀冲突，读如 v2 僵尸路径。2026-06-22 原实证 `graphics/scene/scene_renderer/core/` 下 scene-renderer construction owner 已硬切为 `scene_renderer_core_construct` / `scene_renderer_construct`，由 `review_f19_scene_renderer_construction_modules_use_construct_names` 守卫防回退。

## §3 公共 API 与"用户友好的模块化接口"

- **R3.1 精选 façade**：子系统 `mod.rs` 只 re-export 小而有意的公共集，**分组 + 每组注释**；façade 的 `pub use` 行数纳入审计（软阈值，超阈值要求改 prelude / 分组），禁止 100 符号扁平 dump。
- **R3.2 禁 glob 出口**：子系统 / crate façade 禁止 `pub use x::*`（隐藏 surface）；owner 组内小范围分组显式 re-export 允许。
- **R3.3 prelude 分层**：**子系统级 prelude 为主**——`<crate>::<subsystem>::prelude`（如 `zircon_runtime::asset::prelude`）是消费者主入口；**crate 级 `prelude` 仅聚合**——只 re-export 各子系统 prelude 的跨子系统高频集，不直接列符号。分工：façade(`mod.rs`)=完整公共面、prelude=高频常用面；prelude 也设符号预算，防退化成第二个 dump。插件 crate 经 `lib.rs` 暴露公共 API，体量大时才加 prelude。
- **R3.4 可见性纪律**：模块非 `pub`（公共 API）即 `pub(crate)` / `pub(super)`（实现）；同一 `mod.rs` 不得无规则混排 `pub mod` / `pub(crate) mod`——若混排，façade 注释须显式标注公共集边界；稳定公共项带 doc 注释。

### 范式：巨型扁平 façade → 分组 façade + prelude（`asset/mod.rs`）
**前**：`pub use assets::{ ...100+ 符号一坨... };`
**后**：
```rust
// asset/mod.rs —— 精选 façade（完整公共面，分组）
pub mod prelude;

// —— 资产类型 ——
pub use assets::{MeshAsset, MaterialAsset, TextureAsset, SceneAsset, ModelAsset,
    UiWidgetAsset, VirtualGeometryAsset /* … */};
// —— 导入 / 校验 ——
pub use assets::{asset_kind_for_imported_asset, validate_sprite_atlas_asset,
    validate_wgsl_captures /* … */};
```
```rust
// asset/prelude.rs —— 高频常用面（设符号预算，不得扩成第二个 dump）
pub use super::{Assets, AssetManager, ProjectAssetManager,
    MeshAsset, MaterialAsset, TextureAsset, SceneAsset};
```
crate 级 `zircon_runtime::prelude` 收窄为 `pub use crate::{asset::prelude::*, scene::prelude::*, ui::prelude::*, ...}`，不再直接列子系统符号。

## §4 测试组织（单一规则）

- **R4.1**：单文件小测（< ~150 行测试）→ 内联 `#[cfg(test)] mod tests`。
- **R4.2**：更大 / 行为测试 → folder-backed `tests/` 镜像源树、按行为族分文件。
- **R4.3**：禁止 > 800 行 `tests.rs`；禁止重复测试树（如 editor `src/tests/**` 镜像 `src/ui/**` 双写）——一个行为一个 owner。
- **R4.4**：跨 crate 集成测试归 crate `tests/`；测试命名按所属子系统过滤词前缀（沿用 `render_*`、`runtime_*` guard 命名惯例），便于 milestone 末按过滤词收窄。

## §5 资源 / 描述 / manifest 放置

- **R5.1**：出货资产 → crate `assets/`（staged build 已合并 editor / runtime 两 `assets/`）；测试 fixture → owner 模块 `tests/fixtures/` 或 `<module>/tests/assets/`，禁散落。
- **R5.2**：插件 manifest → crate 根 `plugin.toml`（强制、统一 schema，见 §6.2）。
- **R5.3**：每个 descriptor 家族（`.zui` / `.ui.toml` / `.zmaterial` / `.zasset` / `plugin.toml`）有唯一 schema owner 文件，reviewer 可定位 schema 权威。

## §6 统一插件接口开发体验框架（Plugin DX）

### §6.1 唯一插件 crate 骨架（template）
```
<plugin>/
  plugin.toml          # 统一 schema（强制，见 §6.2）
  runtime/
    Cargo.toml
    src/
      lib.rs             # 薄：pub use 公共 API + 导出 Plugin struct + 常量
      plugin.rs          # 唯一注册 owner：impl RuntimePlugin + descriptor()
      capability.rs      # capability id pub const —— 单一来源
      contract/          # 该插件 ABI-safe DTO（纯消费 interface 则省略）
      backend/           # 实际算法 / 协议实现 owner（按 §1 拆叶子）
      systems/           # 注册进调度图的 ECS 系统
      tests/
  editor/                # 镜像同骨架（能力对称：plugin.rs / capability.rs / ...）
    Cargo.toml
    src/{lib.rs, plugin.rs, capability.rs, ...}
```
- 导入器类插件：`backend/` 即 importer 实现，`plugin.rs` 的 `register` 同时 `register_module` + 注册 importer descriptor；退役 `asset_importers/*/registration.rs` 的自由函数分离。
- native-dynamic 插件：`plugin.toml` 显式区分 runtime / editor 两 `[[modules]]` 的 `crate_name`，禁止两 module 指向同名 crate 却不以 `kind` 区分。

### §6.2 统一 `plugin.toml` schema（canonical）
唯一 schema owner：`docs/zircon_plugins/plugin-manifest-schema.md`（含校验器契约）。必选 / 可选段固定形状，使 30 行与 105 行插件共享骨架：

```toml
# —— 必选头 ——
id = "<plugin>"                       # 与 capability 前缀一致
version = "0.1.0"
sdk_api_version = "0.1.0"
display_name = "..."
category = "runtime|asset_importer|editor|..."
description = "..."
supported_targets = ["client_runtime", "editor_host", ...]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["runtime.plugin.<plugin>", ...]   # 与 capability.rs 单源核对
maturity = "stable|beta|experimental"

# —— 必选模块声明（每个 crate 一条）——
[[modules]]
name = "<plugin>.runtime"             # <plugin>.{runtime|editor}
kind = "runtime|editor"
crate_name = "zircon_plugin_<plugin>_runtime"
target_modes = [...]
capabilities = [...]
system_anchors = [...]                # 与实际注册的 system 源核对

# —— 可选段（按需，schema 固定形状）——
[[capability_statuses]]   capability = "..."  status = "partial|stable"
[[asset_importers]]       id = "..."  source_extensions = [...]  output_kind = "..."  ...
[[optional_features]]     id = "..."
[[dependencies]]          id = "..."
[[options]]               key = "..."  ...
[[event_catalogs]]        ...
```
规则：① 所有插件（含 `asset_importers/*`、native）必须有 `plugin.toml`；② `capabilities` 与 `capability.rs` 常量集双向一致（审计）；③ 可选段缺省即省略，不得改变必选段形状。

### §6.3 唯一注册入口
仅经 `impl RuntimePlugin::register(&self, registry)`（Plugins 01 已硬切运行时插件 trait 到唯一 `register`）；editor 侧经对称 editor plugin trait。自由函数注册（`asset_importers` 的 `package_manifest()` / `plugin_registration()`）收编进 `plugin.rs` 的 `register`。运行时模块与 system 注册优先走 `plugin_sdk::registration::RuntimePluginRegistrationBuilder`，由 SDK 封装 `intern_plugin_module -> register_module -> register_runtime_scene_system` 顺序和 owner token 传递；2026-06-22 已以 `zircon_plugin_animation_runtime` 作为代表迁移样例，2026-06-23 已把 `asset_importers/{data,model,shader}` family 与 root-level split importers 注册入口迁入 `src/plugin.rs` trait owner，并由 `registration_conformance.m3_t1_gate_status`、`registration_conformance.m3_split_importer_gate_status` 和 aggregate `m3_importer_gate_status` 锁定 importer 自由注册函数清零。标准 runtime crate helper（`runtime_plugin()` / `package_manifest()` / `runtime_selection()` / `plugin_registration()`）不得逐插件手写复制；新代码使用 `zircon_plugin_sdk::runtime_plugin_exports!(PluginType)`。2026-06-23 D12 rollout 已把 ai、animation、hybrid_gi、navigation、net、particles、physics、prefab_tools、rendering、solari、terrain、texture、tilemap_2d、virtual_geometry、zr_vm_language 的 trait-backed runtime helper 全部迁到该宏；`asset_importers/audio` 与 `asset_importers/texture` 的 legacy `package_manifest()` 属 importer owner 收口，不属于 D12 标准 helper 块。

### §6.4 capability 单源 + 四源一致性
capability id 为 `capability.rs` 的 `pub const`；guard 测试交叉核对四源：`capability.rs` 常量 ↔ `plugin.toml capabilities` ↔ runtime descriptor ↔ workspace member。扩展现有 `declared_system_anchors_are_registered` 同款模式到 capability。

2026-06-23 首批落地范围：`ai`、`animation`、`hybrid_gi`、`navigation`、`net`、`particles`、`physics`、`prefab_tools`、`rendering`、`solari`、`terrain`、`texture`、`tilemap_2d`、`virtual_geometry`、`zr_vm_language` 这 15 个 trait-backed first-party runtime 根已用 `runtime/src/capability.rs` 拥有 runtime package capability，并由 `tools/plugin_structure_audits/capability.py` 输出 `capability_source_mismatches = 0` 与 `m4_runtime_capability_gate_status = runtime-capability-single-source-clean`。editor `mirrors_runtime(...)`、optional feature capability builder 与 sound/importer/editor 更广迁移仍属于 Plugins 12 M4/T2/M5。

### §6.5 `plugin_sdk` builder（祝福路径）
把 `plugin_sdk_examples` 固化为模板与 builder/test fixture API，新插件 ≈ 一文件声明（descriptor + capability + systems 注册），runtime system 注册通过 `plugin_sdk::registration` 隐藏 owner token 样板，runtime helper exports 通过 `plugin_sdk::runtime_plugin_exports!` 投影 trait-backed manifest/selection/registration，跨插件测试通过 `plugin_sdk::test::TestRuntime::builder()` 复用 runtime/scene/fixed-step 启动样板。

### §6.6 双形态独立构建（发行维扩展）
由 [Plugins 13](zircon_plugins/13-standalone-plugin-build.md) 落地、规范权威 [`docs/zircon_plugins/plugin-standalone-build.md`](../zircon_plugins/plugin-standalone-build.md)。在 §6.1 骨架上扩"发行维"：每个插件一份声明投影两形态——`embed`（`rlib`，静态链接、`impl RuntimePlugin::register`）与 `dist`（`cdylib`，ABI-only、`zircon_native_plugin_descriptor_v3` 导出），二者共享 `backend/` 纯逻辑不复制。**依赖边界铁律**：`dist` 产物依赖闭包禁含 `zircon_runtime`（与 §7.5 E8 同源），`backend/`/`capability.rs` 禁 `use zircon_runtime::*`，触碰 `zircon_runtime` 的代码一律 `#[cfg(feature = "embed")]`；由 `tools/plugin_structure_audits/dependency_boundary.py` 的 `dist_dependency_boundary_violations` 守卫。`zircon plugin build <id>` 单插件独立构建产出可分发动态包，注册跨 ABI 编组依赖 Plugins 01（register 通道/访问集）+ 11（bridge dense 通道）。

### 范式：插件 crate 骨架化（`asset_importers/model`）
**前**（无 `plugin.toml`，注册在自由函数）：`src/{lib.rs(re-export), registration.rs(161 行: descriptors + manifest + plugin_registration 自由函数), mesh_importer.rs, cad.rs, tests/}`
**后**：
```
asset_importers/model/runtime/
  plugin.toml          # 新增，统一 schema
  src/
    lib.rs             # 薄：pub use + ModelImporterRuntimePlugin
    plugin.rs          # impl RuntimePlugin::register（自由函数收编于此）
    capability.rs      # pub const RUNTIME_CAPABILITY = "runtime.plugin.asset_importer.model";
    backend/ mesh_importer.rs  cad.rs
    tests/
```

## §7 强制机制（对后续开发与存量重构同时生效）

1. **审计脚本族**：runtime 进现有 `runtime_structure_audits/`（`module_convention_boundary.py` + `_markdown.py`，由 `audit_runtime_structure.py` 聚合）；editor / plugins 各新建 owner 域同级目录 `editor_structure_audits/` / `tools/plugin_structure_audits/`（与 `runtime_structure_audits/` 平级），各带 `audit_editor_structure.py` / `tools/audit_plugin_structure.py` 聚合器。
2. **guard 测试**：runtime `tests/runtime_absorption/structure_convention.rs`、editor `zircon_editor/src/tests/structure_convention/`、plugins workspace guard——断言审计字段与镜像文档计数一致。
3. **owner-class gate**：`module_convention_gate` / `plugin_skeleton_gate` 报告 `m1_gate_status` ∈ {`migration-debt-present`, `classified-and-clear`}，含 `classification_counts` 与 `migration_debt_count`（目标 → 0），`exempt` 字段登记豁免。
4. **镜像文档**：`docs/**/structure/*.md` 计数须与审计一致，由 `*_mirror_docs_match_structure_audit_counts` 守卫锁定。
5. **硬切纪律**：新 owner 路径落地的同一变更内迁移调用方并删除旧路径，不留 re-export / shim / 双轨；grep 旧符号零命中。
6. **milestone-first 验收**：切片期轻量 `cargo check`，里程碑末进测试 + `cargo fmt --all --check` + 运行对应 `audit_*_structure.py --json`。

每条规则都映射到某审计字段（façade 行数→`oversized_facade_files`、前缀→`prefix_vocabulary_violations`、骨架→`skeleton_conformance`、capability 单源→`capability_source_mismatches`…），确保"可验收"非空话。

## §7.5 错误处理与反重复约定（2026-06 审查并入）

来自 [`engine-code-review-findings-2026-06.md`](engine-code-review-findings-2026-06.md) 的规范级结论：

- **E1 统一 typed error**：子系统公共入口禁返 `Result<_, String>` / `Err(format!())`；用 thiserror enum（每子系统一个 `XxxError`），跨子系统转换保留 source 不 lossy 压扁（实证反例：`scene/world/typed_api.rs:11`、`core/resource/registry.rs:46`、`asset/artifact/cache_payload.rs`）。
- **E2 Result 别名**：每个 owner 模块导出 `pub type Result<T> = std::result::Result<T, XxxError>`，调用点不写全 `Result<T, XxxError>`。
- **E3 builder infallible**：链式 `with_*` 一律返 `Self` 不返 `Result`，校验移到 `build()` / `finish()`，或把可失败应用/解析入口命名为非 builder 动词（如 `apply_*`）；> ~6 字段的 descriptor 必须提供 builder，禁裸字段字面量构造（实证反例：`asset/assets/texture/descriptor.rs:105`、`plugin/runtime_plugin/descriptor.rs:13`）。2026-06-22 已把 texture import settings 子切片收敛到 `apply_import_settings(...) -> Result<_, String>`，由 `review_f8_texture_import_settings_use_fallible_apply_not_with` 守卫；`RuntimePluginDescriptorBuilder` scaffold 已由 `review_f8_runtime_plugin_descriptor_exposes_builder_scaffold` 守卫，first-party runtime plugin descriptor production files 16/16 已由 `review_f8_first_party_runtime_plugin_descriptors_use_builder` 迁到 builder，plugin extension RuntimePluginDescriptor test fixtures 14/14 已由 `review_f8_runtime_plugin_descriptor_test_fixtures_use_builder` 迁到 builder，RuntimePluginDescriptor private fields 15/15 与 15 个访问器由 `review_f8_runtime_plugin_descriptor_fields_are_private_with_accessors` 锁定，旧 `RuntimePluginDescriptor::new` / descriptor fluent owner 由 `review_f8_runtime_plugin_descriptor_public_constructor_is_retired` 锁定退役。RuntimePluginDescriptor public-field convergence complete；RuntimePluginDescriptor::new retired。
- **E4 查找动词规则**：`get_*` → `Option`、`resolve_*` → `Result`；同概念用单一查找动词，禁 `get_`/`fetch_`/`find_`/`lookup_` 混用。`resolve_*_manager` helper 必须保持与 generic `resolve_manager<T>() -> Result<Arc<T>, CoreError>` 同形，特定 object-safe trait 转换放在调用边界显式执行。
- **E5 反重复样板**：近 99% 复制粘贴的并列模块（如 4 套 `*_runtime_provider` 的 registration/prepare_input）必须抽泛型 + 宏/derive；并列 `*Diagnostics` 结构统一 trait + 子域组合，禁 `World*` 纯包装 `Ecs*`。2026-06-22 Runtime 15 F13 registration 子切片已抽 `RuntimeProviderRegistration<P: ?Sized>` + `define_runtime_provider_registration!`，HGI/VG/Solari 三套 public registration 不再复制字段和 debug 样板；Runtime 15 F13 update stats 子切片已抽 `RuntimeProviderUpdate<S>` + `define_runtime_provider_update!`，HGI/VG update wrapper 不再复制 stats 字段/constructor/getter 样板；Runtime 15 F13 feedback shared payload 子切片已抽 `RuntimeProviderFeedback<G, V>`，HGI/VG feedback wrapper 不再复制共同的 GPU completion / visibility feedback payload；Runtime 15 F13 prepare input 子切片已抽 `RuntimeProviderPrepareInput<'a, E>`，HGI/VG prepare input wrapper 不再复制共同的 optional extract / generation frame context；full F13 provider boilerplate audit 已由 `runtime_15_no_duplicated_provider_boilerplate` 总守卫闭合。
- **E6 `#[allow(dead_code)]` 限制**：生产代码禁止用 `#[allow(dead_code)]` 长期掩盖未接线脚手架或僵尸；要么接线、要么删除。审计统计其计数，纳入 gate（实证：全 crate 200 处掩盖 `runtime_ui`、shading-model registry 等僵尸）。2026-06-22 至 2026-06-23 Runtime 15 F12 已分批清理 runtime-owned suppression、script host value descriptor suppression、OffscreenTarget texture owner suppression、RenderBackend state owner suppression、GpuTextureResource owner suppression、GpuMaterialUniformResource owner suppression、GpuMeshResource order-signature suppression、GpuModelResource identity suppression、PostProcessLutTextureResource owner suppression、OutputTargetTextureResource owner suppression、MaterialRuntime capture seed/test texture sampling suppression、ResourceStreamer diagnostics accessor suppression、ResourceStreamer resolve texture id helper suppression、particle GPU readback output accessor suppression 与 advanced plugin output test accessor suppression；全量 sweep 仍未完成。
- **E7 FFI panic 边界**：所有 `extern "C"` 边界（出站导出 + 入站 host 回调）必须包裹 panic guard，panic 转状态码不跨 FFI（实证 P0 缺口：`plugin/native_plugin_loader/host_api_adapter.rs` 9 个回调 0 catch_unwind）。
- **E8 边界依赖白名单**：`zircon_runtime_interface` 禁 wgpu/slint/winit/tokio（已 PASS，作守卫基线）；`zircon_editor`/`zircon_app` 允许 winit + softbuffer、禁 wgpu/slint，消除 "authoring-only" 标签歧义。
- **E9 生产锁 poison 处理**：运行时生产路径的共享状态 `Mutex` 不得直接 `.lock().unwrap()`；infallible owner API 使用集中 helper 做 poison 恢复，fallible API 返回 typed error。测试中为制造 poison 可局部使用 unwrap，但结构守卫必须只扫描生产段（实证 P0 缺口：`scene/level_system.rs` 与 `core/runtime/events/prune.rs`）。

## §8 各计划集落地索引

| 计划集 | 落地 | 范围 |
|---|---|---|
| Runtime | `zircon_runtime/runtime/15-code-structure-and-module-conventions.md` | runtime 全模块 + graphics（render 子计划引用本文 §1/§5） |
| Editor UI | `zircon_editor/editor_ui/10-code-structure-and-module-conventions.md` | editor `core/scene/ui` |
| Plugins | `zircon_plugins/12-plugin-dx-and-structure-framework.md` | 全插件 DX + manifest/骨架/注册/capability |
| Plugins 发行 | `zircon_plugins/13-standalone-plugin-build.md` | 双形态独立构建 + 依赖边界 + 注册跨 ABI 编组 + per-plugin 动态包 |
| Render | `zircon_runtime/render/index.md`「代码结构规范」节 | graphics 热点纳入 Runtime 15 + `large_file_ownership_gate` |
| Hub | `zircon_hub/index.md`「代码结构规范」节 | Hub 巨型文件 + 前端组件化纳入本文 §1/§3/§4 |
| 审查发现目录 | `engine-code-review-findings-2026-06.md` | F1–F19 + D 系列 DX 发现，分派到各计划 |

## 9. 审查和修复记录

| 日期 | 规则 | 状态 | 记录 |
|---|---|---|---|
| 2026-06-23 | §6.4 capability 单源 + 四源一致性 | plugins_12_runtime_capability_single_source_guard_passed | Plugins 12 M4/T1 首批已把 15 个 trait-backed first-party runtime 根的 runtime package capability 迁入 `runtime/src/capability.rs`，crate root 通过 `RUNTIME_CAPABILITIES` 投影 descriptor/status/importer requirement/runtime_capabilities。`tools/audit_plugin_structure.py --json` 现包含 `capability_conformance`，报告 `audited_runtime_root_count = 15`、`capability_source_mismatches = 0`、`m4_runtime_capability_gate_status = runtime-capability-single-source-clean`；`zircon_first_party_runtime_catalog::tests::plugins_12_capability_single_source_conformance` 消费该报告。验证：py_compile、plugin audit JSON、scoped rustfmt、16-package focused `cargo check --locked` 与 catalog focused test 1/1 通过（仅既有 warning）。为解除该 test 构建阻塞，`zircon_runtime/src/ui/surface/input/editable_text.rs` 补齐 `UiImeInputEventKind::DeleteSurrounding` 的不可达 match 分支，保持现有提前返回语义。M4/T2 editor 对称与 optional feature builder 仍 pending。 |
| 2026-06-22 | E3 builder infallible | texture_import_settings_apply_api_coremin_check_passed | Runtime 04 F8 texture import settings apply API 已把 `TextureAssetDescriptor` 与 `TextureAsset` 的可失败导入设置入口改为 `apply_import_settings(...) -> Result<_, String>`，runtime texture ingest 与 first-party texture importer plugin 同步调用新入口，不保留旧 fallible with-entry shim、alias 或转发。新增 `review_f8_texture_import_settings_use_fallible_apply_not_with`，同步 Runtime 04、runtime index、asset importer/render-assets docs、review findings 与 status-output expectations。验证：scoped rustfmt、F8 structure guard、status-output 与 runtime core-min `cargo check` 通过（既有 warnings）；`zircon_plugin_texture_importer_runtime` package `cargo check` 当前被既有 `graphics/runtime_provider/registration.rs` trait-object Clone drift 阻塞，未计通过。RuntimePluginDescriptor public-field convergence remains pending。 |
| 2026-06-22 | E3 RuntimePluginDescriptor builder scaffold | runtime_plugin_descriptor_builder_scaffold_coremin_check_passed | Runtime 06 F8 builder scaffold 已新增 `RuntimePluginDescriptorBuilder` 与 `RuntimePluginDescriptor::builder(...).build()`，并从 `runtime_plugin` 与 `plugin` facade 公开；builder 覆盖 RuntimePluginDescriptor 的主要声明字段，`runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 证明 builder 输出与既有 fluent descriptor projection 等价，`review_f8_runtime_plugin_descriptor_exposes_builder_scaffold` 锁定 API、re-export、文档和 status-output 锚点。验证：scoped rustfmt、F8 structure guard 2/2、status-output 2/2、builder behavior filter 1/1 与 runtime core-min `cargo check` 通过（既有 warnings）。RuntimePluginDescriptor public-field convergence remains pending：裸字段访问收束和字段私有化仍归后续 Runtime 06/结构规范切片。 |
| 2026-06-22 | E3 first-party descriptor builder migration | runtime_plugin_descriptor_first_party_builder_migration_coremin_check_passed | Runtime 06 F8 production call-site migration 已把 16 个 first-party runtime plugin descriptor 入口迁到 `RuntimePluginDescriptor::builder(...).build()`，包括 rendering optional-feature loop 的 builder 累加形态。新增 `review_f8_first_party_runtime_plugin_descriptors_use_builder`，锁定 first-party runtime plugin descriptor production files 16/16 使用 builder 且旧 `RuntimePluginDescriptor::new(` absent。验证：scoped rustfmt --check、F8 structure guard 3/3、status-output 2/2、runtime core-min `cargo check` 与 16-package `zircon_plugins` focused `cargo check` 均通过（既有 warnings）。RuntimePluginDescriptor public-field convergence remains pending，字段私有化仍归后续切片。 |
| 2026-06-22 | E3 test fixture descriptor builder migration | runtime_plugin_descriptor_test_fixture_builder_migration_coremin_check_passed | Runtime 06 F8 test fixture migration 已把 `zircon_runtime/src/tests/plugin_extensions` 下 14 个 runtime/plugin extension 测试文件迁到 `RuntimePluginDescriptor::builder(...).build()`，旧 `RuntimePluginDescriptor::new(` absent。新增 `review_f8_runtime_plugin_descriptor_test_fixtures_use_builder`，锁定 plugin extension RuntimePluginDescriptor test fixtures 14/14 与 builder call count 64；`runtime_plugin_descriptor_builder_matches_fluent_descriptor_projection` 保留 builder 类型入口覆盖但不再依赖旧 constructor 对照。验证：scoped rustfmt --check 通过；F8 structure guard 4/4、status-output 2/2、runtime core-min `cargo check` 与 focused descriptor behavior filter 1/1 通过（既有 warnings）；broad plugin_extensions cargo filter 在 1200s timeout 后无测试结果，残留 cargo/rustc 已清理，不计通过。RuntimePluginDescriptor public-field convergence remains pending，字段私有化仍归后续切片。 |
| 2026-06-22 | E3 descriptor field privatization | runtime_plugin_descriptor_public_field_convergence_coremin_check_passed | Runtime 06 F8 public-field convergence 已把 `RuntimePluginDescriptor` 15 个 public 字段硬切为私有字段，并新增 `descriptor/access.rs` 中的 15 个只读 accessor。生产 catalog augmentation、registration validation 与 plugin extension 测试调用点改走 accessor；`review_f8_runtime_plugin_descriptor_fields_are_private_with_accessors` 锁定 RuntimePluginDescriptor private fields 15/15、访问器签名和 RuntimePluginDescriptor public-field convergence complete。验证：runtime core-min `cargo check` 通过（既有 warnings）；lib-test 编译通过，`plugin_workspace_shape` 过滤器运行暴露两个既有 manifest 断言失败，不计为本切片失败。 |
| 2026-06-22 | E3 descriptor public constructor retirement | runtime_plugin_descriptor_public_constructor_retired_coremin_check_passed | Runtime 06 F8 public constructor retirement 已删除 `descriptor/builder/construction.rs` 与 `descriptor/builder/fluent.rs`，`RuntimePluginDescriptorBuilder` 直接组装私有字段，builtin catalog 子树改为传递 `BuiltinCatalogDescriptorBuilder` 并在 catalog root 统一 `build()`，`zircon_plugins/plugin_sdk` 的声明封装改为持有 builder。新增 `review_f8_runtime_plugin_descriptor_public_constructor_is_retired`，锁定 RuntimePluginDescriptor::new retired、retired owner 不复活与 builtin catalog child modules 不直接使用 descriptor。验证：runtime core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime06-f8-public-constructor-0622` 通过（既有 144 warnings）；plugin SDK focused `cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_sdk --locked --jobs 1 --target-dir target\codex-runtime06-f8-public-constructor-0622` 通过（既有 runtime warnings）。 |
| 2026-06-22 | E4 查找动词规则 | runtime_08_entity_path_lookup_getter_rename_coremin_check_passed | Runtime 08 F17 entity path Option lookup verb rename 已把 scene/world entity path lookup 收敛到 `World::get_entity_by_path(&EntityPath) -> Option<EntityId>`，使 Option-returning 查询遵守 `get_*` 规则；runtime animation 与 first-party animation plugin 调用点同步迁移，没有保留旧 resolve-verb method shim、alias 或转发。新增 `review_f17_entity_path_option_lookup_uses_get_verb`，同步 Runtime 08、runtime index、ECS/animation/editor boundary docs、review findings 与 status-output expectations。验证：scoped rustfmt、F17 structure guard、status-output、runtime core-min `cargo check` 与 `zircon_plugin_animation_runtime` package `cargo check` 均通过（既有 warnings）。 |
| 2026-06-22 | E4 查找动词/manager 解析返回形态 | runtime_10_asset_manager_resolution_handle_shape_coremin_check_passed | Runtime 10 F18 asset manager resolution return shape 已把资产专用 resolver 收敛到 generic manager 解析同形：`resolve_asset_manager(core)` 返回 `Result<Arc<AssetManagerHandle>, CoreError>`，不再直接返回 `Arc<dyn AssetManager>`；`dynamic_api/session/project.rs` 在项目启动边界调用 `.shared()` 后再使用 `AssetManager::open_project(...)`，使 trait-object 转换位置可见。新增 `review_f18_asset_manager_resolution_returns_registered_handle`，同步 Runtime 10、runtime index、dynamic session/asset facade docs、review findings 与 status-output expectations。验证：scoped rustfmt --check、F18 structure guard、status-output 与 core-min `cargo check` 均通过（既有 141 warnings）。 |
| 2026-06-22 | §6.2/§6.4 descriptor/manifest parity | plugins_12_feature_descriptor_parity_guard_passed | Plugins 12 已新增 feature-enabled runtime descriptor/static manifest parity guard，覆盖 linked first-party runtime catalog providers 的 category、maturity、targets、platforms、capabilities、default packaging 与 runtime modules。已修复 glTF/rendering/texture maturity、Hybrid GI/Virtual Geometry category、Navigation Recast capability 漂移；`tools/audit_plugin_structure.py --json` 仍保留 `capability_source_mismatches = "pending"`，完整四源一致性归 M4。验证：feature parity 1/1、默认 `plugins_12` 3/3、Navigation runtime test 1/1、scoped rustfmt check 通过。 |
| 2026-06-22 | R3.4 可见性纪律 | runtime_15_graphics_facade_visibility_note_static_passed_cargo_blocked_graphics_drift | Runtime 15 graphics facade visibility note：Runtime 15 M1 已给 `graphics/mod.rs` 的混合可见性 façade 补明确边界，crate-private implementation owners、public module entries、public façade exports、crate-visible bridge 与 test-only access 分区可见，`backend`/`scene`/`types` 等实现模块保持 `pub(crate)`，公共入口仍通过精选 façade 和 `graphics::prelude`。新增 `runtime_15_mixed_visibility_has_facade_note` 守卫，并把该切片同步到 Runtime 15 与 runtime index 状态表；Cargo 聚焦验证当前被既有 graphics 编译漂移阻塞，未计通过。 |
| 2026-06-22 | R3.3 prelude 分层 | runtime_15_prelude_required_types_coremin_check_passed | Runtime 15 F9 runtime prelude required type coverage 已把 crate 级 `zircon_runtime::prelude` 收窄为聚合子系统 prelude：`asset/prelude.rs`、`scene/prelude.rs`、`ui/prelude.rs`、`graphics/prelude.rs` 各自维护高频类型清单，crate prelude 只 `pub use crate::{asset,scene,ui,graphics}::prelude::*`。新增 `runtime_prelude_exports_asset_scene_ui_and_graphics_contracts` 和 `runtime_15_prelude_covers_required_types`，锁定 gameplay/authoring 所需的 asset、scene/ECS、UI、graphics 高频类型不会再漏出；scoped rustfmt、focused behavior、Runtime 15 structure guard、status-output 与 core-min check 已通过（既有 warnings）。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_runtime_ui_dead_code_support_split_coremin_check_passed | Runtime 15 runtime UI dead-code support split 已把 `runtime_ui` 的生产死代码面拆清：`PublicRuntimeFrame` 迁到生产 `ui/public_runtime_frame.rs`，`RuntimeUiManager` / `RuntimeUiFixture` / input-router / window-event support 迁到 test-only `ui/tests/runtime_ui_support`，`ui/mod.rs` 只在 `#[cfg(test)]` 下挂载该 support 并删除 `#[allow(dead_code)] mod runtime_ui;`，旧 `ui/runtime_ui/` 无 shim 删除。新增 `runtime_15_runtime_ui_dead_code_surface_is_test_support` 守卫。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、ui_architecture 3/3、status-output 2/2 通过；direct ui_architecture_boundary_audit risks=[]（ui entries 18/18、taffy hits/files 175/175 与 10/10）；core-min focused cargo test `runtime_15_runtime_ui_dead_code_surface_is_test_support` 1/1 通过；core-min `cargo check` 通过（既有 warnings）。 该记录只关闭 F10 runtime UI 子切片；F12 全量 dead-code suppression sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_runtime_owned_dead_code_suppression_cleanup_coremin_check_passed | Runtime 15 F12 runtime-owned dead-code suppression cleanup 已清理两个非 render/plugin/editor 的 runtime-owned 抑制点：`asset/pipeline/worker_pool.rs` 的 test-only `request_rx_guard` 改由 `request_channel_guard_is_alive_for_test()` 和 worker-pool 测试显式读取，`core/runtime/state/module_entry.rs` 的 descriptor 字段改由 `ModuleEntry::descriptor()` 被 runtime devtools 消费。新增 `runtime_15_runtime_owned_dead_code_suppression_cleanup` 守卫，禁止这两处回退到 `#[allow(dead_code)]`。验证：scoped rustfmt --check、standalone structure guard、status-output 与 core-min `cargo check` 已通过（既有 warnings）。该记录只关闭 runtime-owned 两处 suppression 子切片；graphics resources、script value descriptors 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_script_host_value_descriptors_coremin_check_passed | Runtime 15 F12 script host value descriptor dead-code cleanup 已清理 `script/vm/host/builtin_host_modules.rs` 的两个 descriptor-only 抑制点：`Vec3` 与 `ColorRgba` 不再使用 `#[allow(dead_code)]`，改由字段布局哨兵构造并读取所有字段，保持反射描述器布局 live。该切片不新增 VM host call，不改变 Runtime 13 ledger 的 6 模块 / 52 固定函数 / 2 类型描述器基线。新增 `runtime_15_script_host_value_descriptors_do_not_suppress_dead_code`，禁止脚本值描述器回退到 dead-code suppression。验证：scoped rustfmt --check、standalone structure guard、status-output 与 core-min `cargo check` 已通过（既有 warnings）。该记录只关闭 script host value descriptor 子切片；graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_offscreen_target_texture_owner_cleanup_static_passed_cargo_timeout_no_result | Runtime 15 F12 offscreen target texture owner cleanup 已清理 `graphics/backend/render_backend/offscreen_target.rs` 的固定帧 WGPU texture owner 抑制点：`OffscreenTarget::RETAINED_FRAME_TEXTURE_COUNT` 与 `retained_frame_texture_count()` 显式读取 final color、GI、scene color、bloom、G-buffer、normal、AO 与 depth 9 个 owner，compiled-scene `bind_frame_graph_resources(...)` 通过 debug assertion 消费该生产保活契约。新增 `runtime_15_offscreen_target_texture_owner_cleanup`，禁止 OffscreenTarget texture owner 回退到 dead-code suppression。验证：scoped rustfmt --check 通过；standalone structure guard 1/1、status-output all-subplans guard 1/1 通过；core-min Cargo check 在独立 target 目录 10 分钟超时无编译结果，残留本切片 cargo/rustc 进程已停止，不计通过。该记录只关闭 OffscreenTarget 固定帧 texture owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_render_backend_state_owner_cleanup_coremin_check_passed | Runtime 15 F12 render backend state owner cleanup 已清理 `graphics/backend/render_backend/render_backend.rs` 的 WGPU backend state owner 抑制点：`RenderBackend::RETAINED_STATE_OWNER_COUNT` 与 `retained_state_owner_count()` 显式读取 `instance`、`adapter`、`config` 3 个 owner，`RenderBackend::caps()` 通过 debug assertion 消费该生产保活契约。新增 `runtime_15_render_backend_state_owner_cleanup`，禁止 RenderBackend state owner 回退到 dead-code suppression。验证：scoped rustfmt --check、standalone structure guard、status-output 与 core-min `cargo check` 已通过（既有 warnings）。该记录只关闭 RenderBackend state owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_gpu_texture_resource_owner_cleanup_coremin_check_passed | Runtime 15 F12 gpu texture resource owner cleanup 已清理 `graphics/scene/resources/gpu_texture/gpu_texture_resource.rs` 的材质纹理资源 owner 抑制点：`GpuTextureResource::RETAINED_TEXTURE_BINDING_OWNER_COUNT` 与 `retained_texture_binding_owner_count()` 显式读取 `id`、`texture`、`view`、`sampler` 4 个 owner，`view()` / `sampler()` 通过 debug assertion 消费该生产保活契约。新增 `runtime_15_gpu_texture_resource_owner_cleanup`，禁止 GpuTextureResource owner 回退到 dead-code suppression。验证：scoped rustfmt --check、standalone structure guard、status-output 与 core-min `cargo check` 已通过（既有 warnings）。该记录只关闭 GpuTextureResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_gpu_material_uniform_owner_cleanup_coremin_check_passed | Runtime 15 F12 gpu material uniform owner cleanup 已清理 `graphics/scene/resources/gpu_material_uniform/gpu_material_uniform_resource.rs` 的材质 uniform buffer / byte-length diagnostics owner 抑制点：`GpuMaterialUniformResource::RETAINED_MATERIAL_UNIFORM_OWNER_COUNT` 与 `retained_material_uniform_owner_count()` 显式读取 `buffer`、`payload_byte_len`、`buffer_byte_len` 3 个 owner，`binding_resource()` 通过 debug assertion 消费该生产保活契约，`resource_streamer_accessors.rs` 通过 owner accessor 读取诊断长度。新增 `runtime_15_gpu_material_uniform_owner_cleanup`，禁止 GpuMaterialUniformResource owner 回退到 dead-code suppression。验证：scoped rustfmt --check、standalone structure guard、status-output 与 core-min `cargo check` 均通过（既有 144 warnings）。该记录只关闭 GpuMaterialUniformResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_gpu_mesh_order_signature_cleanup_coremin_check_passed | Runtime 15 F12 gpu mesh order signature cleanup 已清理 `graphics/scene/resources/gpu_mesh/gpu_mesh_resource.rs` 的 `indirect_order_signature` 抑制点：字段收窄为 `gpu_mesh` owner 内部字段并由 `GpuMeshResource::indirect_order_signature()` 读取，`graphics/scene/resources/gpu_mesh/gpu_mesh_resource_from_asset.rs` 保持完整 vertex/index payload 签名派生，`graphics/scene/scene_renderer/mesh/build_mesh_draws/build/extend_pending_draws_for_mesh_instance.rs` 的 prepared mesh draw tie-breaker 通过 `mesh_order_command_sort_tie_breaker(...)` 混入该签名。新增 `runtime_15_gpu_mesh_order_signature_cleanup`，禁止 GpuMeshResource order signature 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 GpuMeshResource order-signature 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_gpu_model_identity_cleanup_coremin_check_passed | Runtime 15 F12 gpu model identity cleanup 已清理 `graphics/scene/resources/gpu_model/gpu_model_resource.rs` 的 `id` 抑制点：字段收窄为 `gpu_model` owner 内部字段并由 `GpuModelResource::id()` 读取，`graphics/scene/resources/gpu_model/gpu_model_resource_from_asset.rs` 保持构造时记录资源身份，`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs::model(...)` 通过 debug assertion 校验缓存 key 与资源身份一致。新增 `runtime_15_gpu_model_identity_cleanup`，禁止 GpuModelResource identity 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 GpuModelResource identity 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_post_process_lut_texture_owner_cleanup_coremin_check_passed | Runtime 15 F12 post-process LUT texture owner cleanup 已清理 `graphics/scene/resources/post_process_lut_texture/post_process_lut_texture_resource.rs` 的 `texture` 抑制点：`PostProcessLutTextureResource::RETAINED_LUT_TEXTURE_OWNER_COUNT` 与 `retained_lut_texture_owner_count()` 显式读取 texture/view owner，`view()` 通过 debug assertion 消费该保活契约，`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs::prepared_post_process_lut_3d_view(...)` 改为通过 accessor 暴露 3D LUT binding view。新增 `runtime_15_post_process_lut_texture_owner_cleanup`，禁止 PostProcessLutTextureResource owner 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 PostProcessLutTextureResource owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_output_target_texture_owner_cleanup_coremin_check_passed | Runtime 15 F12 output target texture owner cleanup 已清理 `graphics/scene/resources/output_target_texture/output_target_texture_resource.rs` 的 descriptor/texture/view/sampler 抑制点和 `graphics/scene/resources/prepared/prepared_output_target_texture.rs` 的 prepared resource 抑制点：`OutputTargetTextureResource::RETAINED_OUTPUT_TARGET_TEXTURE_OWNER_COUNT` 显式读取 output target descriptor 与 WGPU texture/view/sampler owner，`PreparedOutputTargetTexture::RETAINED_OUTPUT_TARGET_CACHE_OWNER_COUNT` 显式读取 cached Arc owner；graph import、writeback 与 material sampling 路径通过 owner accessor clone/borrow prepared resource。新增 `runtime_15_output_target_texture_owner_cleanup`，禁止 output target resource/cache owner 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 OutputTargetTextureResource / PreparedOutputTargetTexture owner 子切片；更宽 graphics resources 与全量 F12 sweep 仍 pending。 |
| 2026-06-22 | E6 `#[allow(dead_code)]` 限制 | runtime_15_material_runtime_capture_seed_cleanup_coremin_check_passed | Runtime 15 F12 material runtime capture seed cleanup 已清理 `graphics/scene/resources/runtime/material_runtime.rs` 的 `MaterialCaptureSeed`、`MaterialRuntime` 与 `capture_seed()` 抑制点：`MaterialRuntime` 继续作为生产材质运行态 DTO，`MaterialCaptureSeed` 与 `MaterialRuntime::capture_seed()` 只在 `#[cfg(test)]` 下供 render product streamer 测试读取。`graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs` 的 `material_capture_seed(...)`、`sample_texture_rgba(...)`、`sample_texture_asset_rgba(...)` 与 `wrap01(...)` 同步收进 test cfg，生产路径不再暴露历史 capture helper。新增 `runtime_15_material_runtime_capture_seed_cleanup`，禁止 material capture seed/test texture sampling 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 MaterialRuntime capture seed/test texture sampling 子切片；resource_streamer 其他 diagnostics accessor suppression 与全量 F12 sweep 仍 pending。 |
| 2026-06-23 | E6 `#[allow(dead_code)]` 限制 | runtime_15_resource_streamer_diagnostics_accessor_cleanup_static_passed_cargo_lock_blocked | Runtime 15 F12 resource streamer diagnostics accessor cleanup 已清理 `graphics/scene/resources/resource_streamer/resource_streamer_accessors.rs` 的剩余 diagnostics accessor suppression：测试诊断 helper 收进 `#[cfg(test)]`，生产仍消费的 `material_readiness_report(...)` / `material_readiness_summary(...)` 保持正常构建并由 `resource_streamer_ensure_scene_resources.rs` 消费。新增 `runtime_15_resource_streamer_diagnostics_accessor_cleanup`，禁止 ResourceStreamer 诊断查询面继续用 dead-code suppression 保存。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 ResourceStreamer diagnostics accessor 子切片；全量 F12 sweep 仍 pending。 |
| 2026-06-23 | E6 `#[allow(dead_code)]` 限制 | runtime_15_resource_streamer_resolve_texture_id_cleanup_static_passed_cargo_lock_blocked | Runtime 15 F12 resource streamer resolve texture id cleanup 已删除 `graphics/scene/resources/resource_streamer/resource_streamer_resolve_texture_id.rs` 中未使用的 `ResourceStreamer::resolve_texture_id(...)` helper 和对应 `#[allow(dead_code)]`。生产材质贴图解析仍通过 `resolve_texture_reference(...)` / `resolve_texture_reference_with_support(...)` 和 `ResolvedTextureReference::id()` 暴露，保留 unresolved/not-upload-ready validation 与 texture-slot fallback。新增 `runtime_15_resource_streamer_resolve_texture_id_cleanup`，禁止这个僵尸 helper 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 ResourceStreamer resolve_texture_id helper 子切片；全量 F12 sweep 仍 pending。 |
| 2026-06-23 | E6 `#[allow(dead_code)]` 限制 | runtime_15_particle_gpu_readback_output_accessor_cleanup_static_passed_cargo_lock_blocked | Runtime 15 F12 particle GPU readback output accessor cleanup 已删除 `graphics/scene/scene_renderer/core/scene_renderer_runtime_outputs/take_last_particle_gpu_readback_outputs.rs` 中的 stale `#[allow(dead_code)]`。该 accessor 已由 `graphics/runtime/render_framework/submit_frame_extract/submit/collect_runtime_feedback.rs::collect_particle_feedback(...)` 生产路径调用，和 prepared sideband particle readback outputs 合并后投递到 `ParticleGpuFeedback::new(...)`，说明它是 renderer output drain 的 live feedback bridge。新增 `runtime_15_particle_gpu_readback_output_accessor_cleanup`，禁止该 live accessor 回退到 dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 particle GPU readback accessor 子切片；全量 F12 sweep 仍 pending。 |
| 2026-06-23 | E6 `#[allow(dead_code)]` 限制 | runtime_15_advanced_plugin_output_test_accessor_cleanup_static_passed_cargo_lock_blocked | Runtime 15 F12 advanced plugin output test accessor cleanup 已删除 `graphics/scene/scene_renderer/core/scene_renderer/advanced_plugin_outputs/output_access.rs` 中三个 stale `#[allow(dead_code)]`。`has_virtual_geometry_gpu_readback(...)`、`plugin_renderer_outputs(...)` 与 `has_particle_gpu_readback(...)` 只由 inline mailbox/readback tests 观察，现改为 `#[cfg(test)]`；生产路径继续保留 `take_hybrid_gi_readback_outputs(...)`、`take_particle_gpu_readback_outputs(...)` 与 `take_virtual_geometry_readback_outputs(...)` 作为 renderer plugin-output drain。新增 `runtime_15_advanced_plugin_output_test_accessor_cleanup`，禁止 advanced plugin output observation helpers 回退到 production dead-code suppression。验证：scoped rustfmt --check 与静态 dead-code suppression 扫描通过；standalone structure guard、status-output 与 core-min `cargo check` 的带锁 Cargo 执行被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。该记录只关闭 advanced plugin output test accessor 子切片；全量 F12 sweep 仍 pending。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_graphics_dead_code_guard_module_split_static_passed_cargo_lock_blocked | Runtime 15 M3 graphics dead-code guard module split 已将 903 行的 graphics dead-code 单文件 guard 硬切为 `structure_convention/graphics_dead_code/mod.rs`、`structure_convention/graphics_dead_code/module_layout.rs` 与 `structure_convention/graphics_dead_code/renderer_output_accessors.rs`。layout 守卫和 renderer output accessor 两个守卫迁入子模块，父模块保留共享读取 helper 与其余 graphics dead-code 守卫。新增 `runtime_15_graphics_dead_code_guard_is_folder_backed`，禁止旧单文件路径复活并要求父模块保持低于近大文件阈值。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_provider_boilerplate_guard_module_split_static_passed_cargo_lock_blocked | Runtime 15 M3 provider boilerplate guard module split 已将 provider registration/update/feedback 三个结构守卫从顶层 `structure_convention.rs` 迁入 `structure_convention/provider_boilerplate.rs`，与 provider prepare-input 和 full boilerplate audit 守卫同 owner。新增 `runtime_15_provider_boilerplate_guard_is_folder_backed`，禁止 provider 守卫回流到顶层聚合文件，并要求 `structure_convention.rs` 保持小型聚合器。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_facade_surface_guard_module_split_static_passed_cargo_lock_blocked | Runtime 15 M3 facade surface guard module split 已将 prelude coverage 与 graphics façade visibility 两个结构守卫从顶层 `structure_convention.rs` 迁入 `structure_convention/facade_surface.rs`，让 façade/prelude surface guard 与其文档锚同 owner。新增 `runtime_15_facade_surface_guard_is_folder_backed`，禁止 `runtime_15_prelude_covers_required_types` 和 `runtime_15_mixed_visibility_has_facade_note` 回流到顶层聚合文件，并要求 `structure_convention.rs` 保持小型聚合器。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_runtime_dead_code_guard_module_split_static_passed_cargo_lock_blocked | Runtime 15 M3 runtime dead-code guard module split 已将 runtime UI dead-code support、runtime-owned dead-code cleanup 与 script host value descriptor 三个结构守卫从顶层 `structure_convention.rs` 迁入 `structure_convention/runtime_dead_code.rs`，让 F10/F12 runtime dead-code surface guard 同 owner。新增 `runtime_15_runtime_dead_code_guard_is_folder_backed`，禁止这些 dead-code guard 回流到顶层聚合文件，并要求 `structure_convention.rs` 保持小型聚合器。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_diagnostics_guard_module_split_static_passed_cargo_lock_blocked | Runtime 15 M3 diagnostics guard module split 已将 diagnostics frame trait / ECS wrapper cleanup 结构守卫从顶层 `structure_convention.rs` 迁入 `structure_convention/diagnostics_surface.rs`，让 F14 diagnostics normalization guard 与 diagnostics 文档锚同 owner。新增 `runtime_15_diagnostics_guard_is_folder_backed`，禁止 diagnostics guard 回流到顶层聚合文件，并要求 `structure_convention.rs` 保持纯挂载/共享 helper 聚合器。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_core_framework_tests_folder_split_static_passed_cargo_lock_blocked | Runtime 15 M3 core framework test folder split 已将 `core/framework/tests.rs` 底部 framework surface 与 render product/camera 合约用例迁入 `core/framework/tests/framework_surfaces.rs` 与 `core/framework/tests/render_product_surface.rs`，父文件从 1848 行降到 653 行，新增子文件均低于 800 行。新增 `runtime_15_core_framework_tests_are_folder_backed`，锁定父/子模块挂载、moved guard 不回流和 core framework test owner 行数预算。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。`ui/tests/v2_asset.rs` 与 `ui/tests/shared_core.rs` 仍待后续拆分。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_ui_v2_asset_tests_folder_split_static_passed_cargo_lock_blocked | Runtime 15 M3 UI v2 asset test folder split 已将 `ui/tests/v2_asset.rs` 拆成 `ui/tests/v2_asset/asset_loading.rs`、`ui/tests/v2_asset/style_runtime.rs`、`ui/tests/v2_asset/default_controls.rs`、`ui/tests/v2_asset/range_controls.rs`、`ui/tests/v2_asset/demo_and_builder.rs`、`ui/tests/v2_asset/composite_components.rs` 与 `ui/tests/v2_asset/file_cache.rs` 七个行为 owner；父文件从 3806 行降到 331 行，最大子文件 `ui/tests/v2_asset/style_runtime.rs` 为 718 行，全部低于 800 行。新增 `runtime_15_ui_v2_asset_tests_are_folder_backed`，锁定父/子模块挂载、代表性 moved guard 不回流和 UI v2 asset test owner 行数预算。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。`ui/tests/shared_core.rs` 仍待后续拆分。 |
| 2026-06-23 | R4.1 测试组织 folder-backed | runtime_15_ui_shared_core_tests_folder_split_static_passed_cargo_lock_blocked | Runtime 15 M3 UI shared core test folder split 已将 `ui/tests/shared_core.rs` 拆成 `ui/tests/shared_core/layout_surface.rs`、`ui/tests/shared_core/box_flow.rs`、`ui/tests/shared_core/input_visibility.rs`、`ui/tests/shared_core/navigation.rs` 与 `ui/tests/shared_core/scroll_mutation.rs` 五个行为 owner；父文件从 3145 行降到 77 行，最大子文件 `ui/tests/shared_core/input_visibility.rs` 为 771 行，全部低于 800 行。新增 `runtime_15_ui_shared_core_tests_are_folder_backed`，锁定父/子模块挂载、代表性 moved guard 不回流和 UI shared core test owner 行数预算。验证：scoped rustfmt/static checks 通过；带锁 focused structure guard/status-output/core-min cargo check 均在进入测试前被当前工作区 `Cargo.lock` / `Cargo.toml` 不一致阻塞，不计通过。 |
| 2026-06-22 | E5 diagnostics trait + 去纯包装层 | runtime_15_diagnostics_frame_trait_wrapper_removed_coremin_check_passed | Runtime 15 F14 diagnostics normalization 已新增共享 `FrameDiagnostics` / `FrameDiagnosticsStatus` 契约，render/physics/animation diagnostics 与 `EcsFramePerformanceDiagnostics` 统一暴露 domain/available/error 状态；`RuntimeDiagnosticsSnapshot::frame_diagnostics_statuses()` 组合 runtime 子域，不改变现有 `DiagnosticStore` 计数路径。`World` 直接保存 `EcsFramePerformanceDiagnostics`，`scene/world/performance_diagnostics.rs` 删除 `WorldEcsFramePerformanceDiagnostics` 纯包装和 `.0` 转发。新增 `runtime_snapshot_frame_diagnostics_statuses_preserve_subdomains`、`ecs_frame_performance_diagnostics_uses_scene_ecs_frame_domain` 与 `runtime_15_diagnostics_use_frame_trait_without_world_wrapper`；scoped rustfmt --check、standalone guard 1/1、status-output 2/2 与 core-min `cargo check` 已通过（既有 warnings）。focused behavior Cargo test 1200s 超时无测试结果，不计通过。F13 registration/update stats/feedback/prepare-input shared-owner 子切片与 full provider boilerplate audit 已由后续记录闭合。 |
| 2026-06-22 | E5 provider registration 反重复样板 | runtime_15_provider_registration_shared_owner_coremin_check_passed | Runtime 15 F13 provider registration shared owner 已新增 `graphics/runtime_provider/registration.rs`，`RuntimeProviderRegistration<P: ?Sized>` 统一 provider ID、priority、provider trait object 与 debug name 存储，`define_runtime_provider_registration!` 生成 `HybridGiRuntimeProviderRegistration`、`VirtualGeometryRuntimeProviderRegistration`、`SolariRuntimeProviderRegistration` 的 public wrapper。原 public API 名称和方法不变；feature-specific registration 文件不再复制 `provider_id` / `priority` / `Arc<dyn ...>` / `Debug` 样板。新增 `runtime_15_provider_registration_uses_shared_owner`，同步 Runtime 15、runtime index、review findings、module-convention docs 与新 `docs/zircon_runtime/graphics/runtime_provider/registration.md`。验证：standalone guard red->green；scoped rustfmt、structure guard、status-output 与 core-min `cargo check` 通过（既有 warnings）。本记录只关闭 registration 子切片；update/feedback 子切片已由后续 shared-owner 记录收束，provider prepare-input duplication 仍 pending。 |
| 2026-06-22 | E5 provider update stats 反重复样板 | runtime_15_provider_update_shared_stats_owner_coremin_check_passed | Runtime 15 F13 provider update shared stats owner 已新增 `graphics/runtime_provider/update.rs`，`RuntimeProviderUpdate<S>` 统一 update stats payload，`define_runtime_provider_update!` 生成 `HybridGiRuntimeUpdate` 与 `VirtualGeometryRuntimeUpdate` 的 public wrapper。原 public API 名称和 `stats()` 返回形状不变；feature-specific update 文件不再复制 `stats` 字段、constructor 和 getter 样板。新增 `runtime_15_provider_update_uses_shared_stats_owner`，同步 Runtime 15、runtime index、review findings、module-convention docs 与新 `docs/zircon_runtime/graphics/runtime_provider/update.md`。验证：缺失 `graphics/runtime_provider/update.rs` 的红态静态证据已记录；scoped rustfmt 与 core-min `cargo check` 通过（既有 warnings）；standalone structure guard/status-output test binary 启动被 Windows `ResourceUnavailable` / 用户取消阻断，focused Cargo test 904s 超时无结果，不计为通过。本记录只关闭 update stats 子切片；feedback 子切片已由后续 shared-owner 记录收束，provider prepare-input duplication 仍 pending。 |
| 2026-06-22 | E5 provider feedback 反重复样板 | runtime_15_provider_feedback_shared_payload_owner_coremin_check_passed | Runtime 15 F13 provider feedback shared payload owner 已新增 `graphics/runtime_provider/feedback.rs`，`RuntimeProviderFeedback<G, V>` 统一 feedback 的 `gpu_completion` 与 `visibility_feedback` 共同 payload。`HybridGiRuntimeFeedback` 与 `VirtualGeometryRuntimeFeedback` 的 public API 名称、constructor 和 getter surface 不变；feature-specific feedback 文件不再复制共同 payload 字段，HGI evictable probe ids 与 VG page-request/generation 字段继续留在各自 owner。新增 `runtime_15_provider_feedback_uses_shared_payload_owner`，同步 Runtime 15、runtime index、review findings、module-convention docs 与新 `docs/zircon_runtime/graphics/runtime_provider/feedback.md`。验证：缺失 `graphics/runtime_provider/feedback.rs` 的红态静态证据已记录；scoped rustfmt、standalone structure guard 1/1、standalone status-output 2/2 与 core-min `cargo check` 通过（既有 warnings）。本记录只关闭 feedback 共同 payload 子切片；provider prepare-input duplication 仍 pending。 |
| 2026-06-22 | E5 provider prepare input 反重复样板 | runtime_15_provider_prepare_input_shared_frame_owner_coremin_check_passed | Runtime 15 F13 provider prepare input shared frame owner 已新增 `graphics/runtime_provider/prepare_input.rs`，`RuntimeProviderPrepareInput<'a, E>` 统一 prepare input 的 optional extract 与 generation frame context。`HybridGiRuntimePrepareInput` 与 `VirtualGeometryRuntimePrepareInput` 的 public API 名称、constructor 和 getter surface 不变；feature-specific prepare input 文件不再复制共同 `extract` / `generation` 字段，HGI mesh/light/update-plan 输入与 VG page-upload/cluster/draw-segment 输入继续留在各自 owner。新增 `runtime_15_provider_prepare_input_uses_shared_extract_generation_owner`，同步 Runtime 15、runtime index、render index、review findings、module-convention docs 与新 `docs/zircon_runtime/graphics/runtime_provider/prepare_input.md`。验证：scoped rustfmt、standalone structure guard 1/1、standalone status-output all-subplans guard 1/1 与 core-min `cargo check` 通过（既有 warnings）；broader `status_output` 批次仍有非本切片 Runtime 06 F8 row-drift 失败。full F13 provider boilerplate audit 仍 pending。 |
| 2026-06-22 | E5 provider boilerplate 总审计 | runtime_15_provider_boilerplate_full_audit_coremin_check_passed | Runtime 15 F13 full provider boilerplate audit 已把 registration / update / feedback / prepare-input 四个 shared-owner 子切片收束到 `runtime_15_no_duplicated_provider_boilerplate` 总守卫：`graphics/runtime_provider/{registration,update,feedback,prepare_input}.rs` 保持共享 owner；HGI/VG/Solari registration 不再复制 provider id / priority / trait-object / debug 样板；HGI/VG update 不再手写 constructor/getter；HGI/VG feedback 不再复制共同 GPU completion / visibility feedback 字段；HGI/VG prepare input 不再复制共同 optional extract / generation 字段；Particle feedback 作为单 payload feature-specific 例外记录。状态同步 Runtime 15、runtime index、render index、review findings、module-convention docs 与 status-output expectations。验证：scoped rustfmt --check、standalone full provider boilerplate guard 1/1、standalone status-output all-subplans guard 1/1 与 core-min `cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --jobs 1 --target-dir target\codex-runtime15-provider-boilerplate-full-coremin-0622` 均通过（既有 141 warnings）。 |
| 2026-06-22 | E1/E2 typed error + Result 别名 | asset_artifact_importer_typed_errors_coremin_passed | Runtime 04 F7 asset artifact/importer typed errors 已把 importer registry 和 artifact cache conversion 错误源收束到 `AssetImportError`：`AssetImportError::Registry(#[from] AssetImporterRegistryError)` 保留 registry source，artifact cache payload/TOML conversion 不再返回裸 `Result<_, String>`，TOML serialize/deserialize、cached datetime、UI/UI v2 document 与 bincode conversion 均通过 typed source 变体表达。新增 `review_f7_asset_artifact_errors_use_asset_import_error_sources` 与 `asset_import_error_preserves_registry_error_source`；broader Runtime 04 `asset::` / `worker_pool` Cargo gates 仍 pending。 |
| 2026-06-22 | E1/E2 typed error + Result 别名 | core_resource_registry_typed_errors_coremin_check_passed | Runtime 02 F6 core resource registry typed errors 已把 resource registry rename 公共错误面收束到 `CoreError` / `CoreResult`：`ResourceRegistry::rename(...)` 与 `ResourceManager::rename(...)` 不再返回裸 `Result<_, String>`，缺失 locator/id 通过 `CoreError::MissingResourceRecordForLocator` / `MissingResourceRecordForId` 表达，`Err(format!())` 不再存在于该子系统路径。新增 `review_f6_core_resource_registry_rename_uses_core_error` 与 `registry_rename_reports_missing_locator_with_core_error`；Runtime 02 broader Cargo gates 仍 pending。 |
| 2026-06-22 | E1/E2 typed error + Result 别名 | world_typed_mutation_errors_coremin_check_passed_partial | Runtime 08 F5 world typed mutation errors 已把 ECS typed world mutation 公共入口收敛到 `SceneError` / `SceneResult`：`World::spawn`、`spawn_at`、`insert_bundle`、`insert`、`remove` 与 `Bundle::insert_into(...)` 不再返回裸 `Result<_, String>`；缺失实体为 `SceneError::MissingEntity`，storage error 保留 typed source，deferred report 只在边界 stringify。本轮固定 world mutation façade 已继续覆盖 `component_access`、`hierarchy`、`query` 与 `records`，外层 property/dynamic-scene/reflection/script 字符串合同显式降级。新增 `review_f5_world_spawn_bundle_surface_uses_scene_error` 与 `world_typed_mutation_errors_report_missing_entities_as_scene_errors`。`dynamic_components` String-error family 与 `DynamicSceneError::WorldMutation(String)` 已由 `dynamic_component_typed_errors_coremin_check_passed` 关闭。 |
| 2026-06-22 | E1/E2 typed error + Result 别名 | dynamic_component_typed_errors_coremin_check_passed | Runtime 08 F5 dynamic component typed errors 已把 plugin component type registry、dynamic component attach/remove/property write、plugin unload guard 与 DynamicScene world mutation 收束到 `SceneError` / `SceneResult`：world owner 内部不再 `Err(format!())` 或 `error.to_string()`，`DynamicSceneError::WorldMutation` 保留 `SceneError` source，反射/插件 registry/脚本宿主只在外部合同边界 stringify 或分类。新增 `review_f5_dynamic_component_errors_preserve_scene_error_sources`、`dynamic_component_mutation_errors_report_scene_error_variants` 与 `dynamic_scene_world_mutation_preserves_scene_error_source`；scoped rustfmt、F5 dynamic 结构守卫、status-output 守卫与 core-min `cargo check` 通过，聚焦 behavior `cargo test` 构建 15m 超时无测试结果、不计通过；Runtime 08 broader ECS Cargo gates 仍 pending。 |
| 2026-06-22 | E7 FFI panic 边界 | native_host_ffi_panic_guard_coremin_passed | Runtime native host FFI 回调已补边界保护：`ffi_panic_guard.rs` 承接 `catch_unwind`，`host_api_adapter.rs` 的 9 个 public host API callbacks 与 `host_callbacks.rs` 的 4 个 private host callbacks 均在 `extern "C"` 边界内把 panic 映射为状态码。验证：scoped rustfmt 通过；13/13 静态 guard 覆盖扫描通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-f1-ffi-guard-0622 --message-format short --color never` 通过（既有 warning 噪声）；`cargo test ... ffi_panic_guard` 通过 2 tests；`cargo test ... native_host_bridge_call_catches_plugin_method_panic` 通过 1 test。 |
| 2026-06-22 | E9 生产锁 poison 处理 | scene_level_poison_recovery_coremin_passed_eventbus_guard_timeout | Runtime 07 F2 已收敛 scene/EventBus 生产锁入口：`LevelSystem` 与 `DefaultLevelManager` 使用 poison-safe helper，`EventBus` 使用私有 `lock_subscribers()` / `lock_delivery()`，publish/subscribe/prune 不再直接展开锁。验证：scoped rustfmt/check 通过；`cargo check -p zircon_runtime --lib --no-default-features --features core-min --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-f2-lock-poison-0622 --message-format short --color never` 通过（既有 warning 噪声）；`cargo test ... level_system_accessors_recover_poisoned_state_locks` 通过 1 test；`cargo test ... level_system_recovers_world_lock_after_writer_panic` 通过 1 test；EventBus root 结构守卫超时且不计通过，残留编译进程已清理。 |
| 2026-06-22 | §6.2 插件 manifest 单源/生成标记 | plugins_12_static_manifest_generated_marker_static_passed_cargo_timeout | Plugins 12 M1/T5 已给 30 个非 native 根 `plugin.toml` 加 `@generated from Rust descriptor package_manifest()` 头，`native_dynamic_fixture` 删除 Rust 内嵌 TOML 副本并通过 `include_str!("../../plugin.toml")` 嵌入唯一手写根 manifest；`zircon_first_party_runtime_catalog` 新增 `plugins_12_static_plugin_manifest_is_generated` guard 覆盖生成头、feature-enabled runtime descriptor 子集 parity 与 native 单源嵌入。验证：scoped rustfmt/check 通过，静态生成头扫描 30/30 通过，scoped `git diff --check` 仅 LF/CRLF 提示；focused Cargo guard 1200s 超时且不计通过，残留已清理。完整 manifest 生成器、缺失 importer manifest、schema uniform/capability 四源一致性仍属 Plugins 12 后续切片。 |
| 2026-06-22 | §6.2 插件 manifest 缺失覆盖 | plugins_12_missing_importer_manifests_guard_passed | Plugins 12 M1/T3 已补齐 `asset_importers/{audio,data,model,shader,texture}/plugin.toml` 与 `opus_importer/plugin.toml`，所有非 native `zircon_plugins/**/plugin.toml` 生成头扫描扩到 36/36；`asset_importers/audio` 与 `asset_importers/texture` 的 descriptor manifest 派生补平台和 runtime module 明细，`asset_importers/model` 去除重复 runtime module。`plugins_12_static_plugin_manifest_is_generated` guard 已支持多行 TOML 数组并覆盖新增 manifest。验证：scoped rustfmt 通过；`cargo metadata --manifest-path zircon_plugins/Cargo.toml --format-version 1 --no-deps --locked` 通过；`cargo check` 三个 asset importer 聚焦包通过（仅既有 runtime warnings）；`cargo test -p zircon_first_party_runtime_catalog plugins_12_static_plugin_manifest_is_generated` 通过 1/1。完整 schema uniform 审计、骨架迁移、capability 四源一致性仍属 Plugins 12 后续切片。 |
| 2026-06-22 | §6.2 插件 manifest 必选字段 uniform | plugins_12_manifest_schema_uniform_supported_platforms_guard_passed | Plugins 12 M1/T4 已把 `supported_platforms` 写入 37 份 manifest 必选形状并让 `RuntimePluginDescriptor::package_manifest()` 默认派生同一平台集合；`plugins_12_manifest_schema_uniform` 覆盖 36 个 generated 非 native manifest + native fixture 手写 manifest 的固定必选字段与 `[[modules]]` 基础字段，required-field 扫描 37/37 通过。验证：scoped rustfmt/check 通过；`plugins_12_static_plugin_manifest_is_generated` 与 `plugins_12_manifest_schema_uniform` 均通过 1/1（仅既有 `zircon_runtime` warnings）。完整 `tools/plugin_structure_audits/*` 聚合脚本、骨架迁移与 capability 四源一致性仍属 Plugins 12 后续切片。 |
| 2026-06-22 | §7 插件结构审计入口 | plugins_12_manifest_structure_audit_guard_passed | Plugins 12 M1/T4 已把 manifest uniform 规则沉入仓库根 `tools/audit_plugin_structure.py` + `tools/plugin_structure_audits/manifest_schema.py`：审计从 plugin workspace members 推导 37 个插件根，报告 `missing_plugin_toml = 0`、`manifest_schema_violations = 0`、`generated_manifest_header_violations = 0` 与 `m1_gate_status = classified-and-clear`；`first_party_runtime_catalog` 的 workspace guard 消费该 JSON。验证：脚本 JSON、py_compile、scoped rustfmt/check 与 `plugins_12_` 三个 catalog guard 全部通过（仅既有 runtime warnings）。骨架迁移、registration 单入口与 capability 四源一致性仍属后续切片。 |
| 2026-06-22 | §6.5 `plugin_sdk` builder 祝福路径 | plugins_12_plugin_sdk_builder_baseline_passed | Plugins 12 M2/T1 已新增 `zircon_plugins/plugin_sdk` support crate，按 R1.1 薄 façade 拆为 `manifest`、`runtime` 与 `prelude`。`PluginManifestBuilder` 固化 SDK/API 版本、默认平台与默认 packaging，`PluginModuleBuilder` 标准化 runtime/editor/native/vm module 声明，`RuntimePluginDeclaration` 从单源声明投影 runtime descriptor 与 package manifest；`plugin_structure_audits` 显式跳过该 support crate，避免把 SDK 自身当插件 root。验证：scoped rustfmt check、`cargo check -p zircon_plugin_sdk --locked`、`cargo test -p zircon_plugin_sdk` 3/3 与 `tools/audit_plugin_structure.py --json` 通过（仅既有 `zircon_runtime` warnings）。该记录只关闭 §6.5/M2-T1 baseline；editor 宏、test runtime fixture、骨架符合度 guard 已由后续记录关闭，capability 四源一致性仍 pending。 |
| 2026-06-22 | §6.1 插件骨架样例与 guard | plugins_12_crate_skeleton_sample_conformance_passed | Plugins 12 M2/T2 已把 `zircon_plugins/plugin_sdk_examples/editor` 切成首个 clean skeleton sample：`lib.rs` 只保留 façade，`capability.rs` 单源能力常量，`plugin.rs` 拥有 editor plugin/SDK manifest projection/registration report，`extensions.rs` 拥有 editor extension 注册，`extension_ids.rs` 拥有扩展 id，`tests.rs` 拥有样例断言。新增 `tools/plugin_structure_audits/skeleton.py` 并由 `plugins_12_crate_skeleton_conformance` 消费 `tools/audit_plugin_structure.py --json`，锁定 `sample_conformance_status = sample-clean`、`sample_violation_count = 0`、`migration_debt_count = 35`。验证：审计 JSON、py_compile、scoped rustfmt、`cargo metadata --manifest-path zircon_plugins/Cargo.toml --format-version 1 --no-deps --locked` 与 focused `cargo check -p zircon_plugin_sdk_examples_editor -p zircon_first_party_runtime_catalog --locked` 通过；两个 focused Cargo test 分别 1200s/900s 超时且不计通过。该记录只关闭 §6.1/M2-T2 首个样例和 audit/check guard；M2/T5 test runtime fixture 已由后续记录关闭，M5 存量迁移、M4 capability 四源一致性仍 pending。 |
| 2026-06-22 | §6.5 editor authoring macro + workspace deps | plugins_12_editor_authoring_macro_workspace_deps_passed | Plugins 12 M2/T4 已新增 `plugin_sdk::editor` feature，`EditorPluginDeclaration` 统一 editor descriptor/base manifest/capabilities/asset roots/content roots，`authoring_plugin!` 宏生成 editor plugin struct、`Default`/`new`、manifest/capability/registration helpers 与 `EditorPlugin` impl。`plugin_sdk_examples/editor` 主插件改用宏，不再手写主 descriptor/trait impl/base manifest builder；`zircon_plugins/Cargo.toml` 新增 `[workspace.dependencies]`，样例 editor crate、SDK crate 与 native fixture 改用 workspace dependency inheritance。`tools/plugin_structure_audits/skeleton.py` 新增 sample workspace-dependency guard，`sample_workspace_dependency_status = sample-workspace-deps-clean`。验证：审计 JSON、py_compile、scoped rustfmt、cargo metadata、SDK editor feature check、editor sample check、默认 SDK check、native fixture check 均通过（仅既有 runtime/editor warnings）；editor macro/sample focused tests 300s 超时且不计通过。M2/T5 test runtime fixture 已由后续记录关闭，M4 capability 四源一致性仍 pending。 |
| 2026-06-22 | §6.5 test runtime fixture | plugins_12_test_runtime_fixture_passed | Plugins 12 M2/T5 已新增 `zircon_plugin_sdk::test` runtime fixture owner：`TestRuntime::builder()` 默认注册并激活 foundation/asset/scene 基础模块，合并 runtime plugin registration reports、注册插件 runtime modules、安装 scene/world runtime extensions，并暴露 `handle()`、`runtime()`、`extension_report()`、`activated_modules()`、typed manager 解析、默认 level 创建与 fixed-step/tick helpers。SDK 自测用 fake runtime plugin 验证基础模块和插件 module 注册激活、manager 解析、默认 level extension 安装与 tick 流程。验证：scoped rustfmt 通过；`cargo check --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_sdk --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-test-runtime-m2-0622 --message-format short --color never` 通过（仅既有 runtime warnings）；`cargo test --manifest-path zircon_plugins\Cargo.toml -p zircon_plugin_sdk --locked --jobs 1 --target-dir D:\cargo-targets\zircon-plugin-test-runtime-m2-0622 --message-format short --color never test_runtime_builder -- --test-threads=1 --nocapture` 复跑通过 2/2，首次 600s 超时未计通过。 |
| 2026-06-22 | §6.3 runtime registration builder | plugins_12_registration_builder_animation_passed | Plugins 12 M3/T2 已新增 `zircon_plugin_sdk::registration`：`RuntimePluginRegistrationBuilder` 与 `RuntimePluginModuleRegistration` 封装 runtime module owner intern、module registration、runtime scene system registration、system set、order 与 before/after constraint 写入，插件作者不再直接传 `PluginModuleId`。`zircon_plugin_animation_runtime` 已作为代表插件迁移，`AnimationRuntimePlugin::register` 改用 SDK builder，`runtime_system.rs` 只接收 module registration handle。验证：SDK+animation locked check 通过；SDK `runtime_registration_builder` focused test 1/1 通过；animation `animation_registration_contributes_runtime_module` focused test 1/1 通过（仅既有 runtime warnings）。该记录只关闭 D8 的 SDK 封装和 animation 代表迁移；`asset_importers/*` family 自由注册函数已由后续 M3/T1 记录关闭，D12 转发自由函数 helper、split importer D13 骨架化与 importer selection 派生仍 pending。 |
| 2026-06-23 | §6.3 importer family 注册单入口 | plugins_12_asset_importer_family_single_registration_entry_check_passed | Plugins 12 M3/T1 已把 `asset_importers/{data,model,shader}` family 注册入口收编到 `src/plugin.rs` 的 `RuntimePlugin` owner：`lib.rs` 不再承载公开 `pub fn register(...)`，model 旧 `registration.rs` 删除，`plugin_registration()` 统一走 `RuntimePluginRegistrationReport::from_plugin`，`runtime_selection()` 由 descriptor projection 派生。新增 `tools/plugin_structure_audits/registration.py`，`tools/audit_plugin_structure.py --json` 报告 `registration_conformance.m3_t1_gate_status = family-single-entry-clean`、`asset_importer_family_free_function_registration_sites = 0`、`asset_importer_family_registration_owner_files = 0`。验证：scoped rustfmt check、py_compile、plugin audit JSON 与 data/model/shader importer family `cargo check --locked` 均通过（仅既有 runtime warnings）；model focused lib-test 被既有 `MaterialCaptureSeed` / `MaterialRuntime::capture_seed` runtime lib-test drift 阻断，不计通过。split importer 注册自由函数已由后续 §6.3 split 行关闭；D6 `RuntimePluginId` string-newtype、M4 capability 单源与 M5 存量迁移仍 pending。 |
| 2026-06-23 | §6.3 split importer 注册单入口 | plugins_12_split_importer_single_registration_entry_check_passed | Plugins 12 M3/T1 已把 `gltf_importer`、`obj_importer`、`texture_importer`、`audio_importer`、`opus_importer`、`shader_wgsl_importer`、`ui_document_importer` 的注册入口收编到各自 `runtime/src/plugin.rs` 的 `RuntimePlugin` owner，`lib.rs` 退为薄 façade，旧 `texture_importer/runtime/src/registration.rs` 删除，非 test 源码不再出现 importer `pub fn register(...)`。`tools/audit_plugin_structure.py --json` 报告 `registration_conformance.m3_split_importer_gate_status = split-importer-single-entry-clean`、`split_importer_free_function_registration_sites = 0`、`split_importer_registration_owner_files = 0`、aggregate `m3_importer_gate_status = importer-single-entry-clean`。验证：scoped rustfmt check、py_compile、plugin audit JSON、`cargo metadata --manifest-path zircon_plugins/Cargo.toml --format-version 1 --no-deps --locked` 与 split importer focused `cargo check --locked` 均通过（仅既有 `zircon_runtime` warnings）。D12 转发自由函数 helper/blanket、M4 capability 单源与 M5 存量迁移仍 pending。 |
| 2026-06-23 | §6.3 runtime helper export 宏 | plugins_12_runtime_export_macro_representative_check_passed | Plugins 12 M3/D12 已新增 `zircon_plugin_sdk::runtime_plugin_exports!`，生成 runtime plugin crates 反复手写的 `runtime_plugin()`、`package_manifest()`、`runtime_selection()` 与 `plugin_registration()` helper，并通过 `RuntimePlugin` trait 方法投影，保留 Net 这类自定义 manifest override。`zircon_plugin_animation_runtime`、`zircon_plugin_physics_runtime`、`zircon_plugin_net_runtime` 已作为评审证据路径代表迁移，physics/net runtime crates 改用 workspace `zircon_plugin_sdk` 依赖。验证：scoped rustfmt、SDK+animation/physics/net offline check、同包 locked check 与 SDK `runtime_plugin_exports` focused test 1/1 均通过（仅既有 `zircon_runtime`、net、physics warnings）。剩余 trait-backed runtime helper rollout 已由下一行关闭；M4 capability 单源与 M5 存量迁移仍 pending。 |
| 2026-06-23 | §6.3 runtime helper export 全量 rollout | plugins_12_runtime_export_macro_rollout_check_passed | Plugins 12 M3/D12 已把 first-party trait-backed runtime helper rollout 扩到 ai、animation、hybrid_gi、navigation、net、particles、physics、prefab_tools、rendering、solari、terrain、texture、tilemap_2d、virtual_geometry、zr_vm_language。上述 runtime crate root 不再手写标准 helper 块，统一调用 `zircon_plugin_sdk::runtime_plugin_exports!(...)`，且自定义 package manifest override 仍由 `RuntimePlugin::package_manifest()` 保留。全量扫描只剩 `asset_importers/audio` 与 `asset_importers/texture` 的 legacy `package_manifest()`，不属于 D12 trait-backed helper 块。验证：scoped rustfmt、16-package offline check、16-package locked check、`cargo metadata --locked` 与 SDK `runtime_plugin_exports` focused test 1/1 均通过（仅既有 warning 噪声）。M4 capability 单源与 M5 `RuntimePluginId` open/custom id 仍 pending。 |
| 2026-06-22 | §6.5 native ABI helper | plugins_12_native_sdk_helper_passed | Plugins 12 M2/T3 已新增 `plugin_sdk::native` feature，作者侧 native ABI v3 类型和 helper 宏不再由每个 cdylib 手写：SDK 提供 `NativePluginAbiV3`/entry report/behavior/host table/bridge DTO/status constants、`NativePluginStatic<T>`、entry capability gate、SDK-owned byte buffers/free callback、panic boundary 和 `export_native_plugin_descriptor_v3!` / `export_native_plugin_entry_v3!` / `native_command_plugin_v3!`。`native` feature 仅依赖 `zircon_runtime_interface`，不拉完整 runtime crate；`native_dynamic_fixture` 已删除本地 ABI/free/panic/capability/export 样板并改依赖 SDK native。验证：native-only SDK check、native SDK tests 4/4、native fixture check、默认 SDK check 均通过；默认 SDK check 仅保留既有 `zircon_runtime` warnings。 |
| 2026-06-22 | R2.5 construction directory naming | render_scene_renderer_construct_modules_coremin_passed | Render F19 已把 scene renderer 的 construction owner 从 `*_new` 后缀硬切为 `scene_renderer_core_construct` / `scene_renderer_construct`，所有源码、结构测试和文档路径直接指向新 owner；未保留兼容 re-export、shim、facade 或旧目录。新增 `review_f19_scene_renderer_construction_modules_use_construct_names` 守卫，并以 core-min `cargo check` 验证。 |
