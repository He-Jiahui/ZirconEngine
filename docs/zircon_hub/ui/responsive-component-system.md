---
related_code:
  - zircon_hub/Cargo.toml
  - zircon_hub/build.rs
  - zircon_hub/ui/components.slint
  - zircon_hub/ui/tokens.slint
  - zircon_hub/ui/material_bridge.slint
  - zircon_hub/ui/theme.slint
  - zircon_hub/ui/layout.slint
  - zircon_hub/ui/surfaces.slint
  - zircon_hub/ui/inputs.slint
  - zircon_hub/ui/shell_header_components.slint
  - zircon_hub/ui/shell_header_popup_components.slint
  - zircon_hub/ui/shell_sidebar_components.slint
  - zircon_hub/ui/shell_page_components.slint
  - zircon_hub/ui/navigation.slint
  - zircon_hub/ui/data_display.slint
  - zircon_hub/ui/table_view_components.slint
  - zircon_hub/ui/operation_timeline_components.slint
  - zircon_hub/ui/overlays.slint
  - zircon_hub/ui/shared.slint
  - zircon_hub/ui/app.slint
  - zircon_hub/ui/projects.slint
  - zircon_hub/ui/project_dashboard.slint
  - zircon_hub/ui/project_dashboard_components.slint
  - zircon_hub/ui/project_pages.slint
  - zircon_hub/ui/project_new_page.slint
  - zircon_hub/ui/project_browser_page.slint
  - zircon_hub/ui/project_detail_page.slint
  - zircon_hub/ui/project_detail_components.slint
  - zircon_hub/ui/project_browser_components.slint
  - zircon_hub/ui/project_page_components.slint
  - zircon_hub/ui/editor.slint
  - zircon_hub/ui/editor_page_components.slint
  - zircon_hub/ui/builds.slint
  - zircon_hub/ui/builds_page_components.slint
  - zircon_hub/ui/assets.slint
  - zircon_hub/ui/catalog_page_components.slint
  - zircon_hub/ui/cloud.slint
  - zircon_hub/ui/cloud_page_components.slint
  - zircon_hub/ui/learn.slint
  - zircon_hub/ui/plugins.slint
  - zircon_hub/ui/team.slint
  - zircon_hub/ui/team_page_components.slint
  - zircon_hub/ui/settings.slint
  - zircon_hub/ui/settings_page_components.slint
  - zircon_hub/src/app/runtime.rs
  - zircon_hub/src/app/runtime/folder_picker.rs
  - zircon_hub/src/app/runtime/project_workspace.rs
  - zircon_hub/src/app/runtime/root_paths.rs
  - zircon_hub/src/app/runtime/source_scoped_views.rs
  - zircon_hub/src/app/runtime/source_engine_paths.rs
  - zircon_hub/src/app/binding.rs
  - zircon_hub/src/app/view_model.rs
  - zircon_hub/src/app/view_model/media.rs
  - zircon_hub/src/app/view_model/projects.rs
  - zircon_hub/src/app/view_model/plugins.rs
  - zircon_hub/src/app/view_model/quick_actions.rs
  - zircon_hub/src/app/view_model/team.rs
  - zircon_hub/src/assets/catalog.rs
  - zircon_hub/src/learn/catalog.rs
  - zircon_hub/src/plugins/catalog.rs
  - zircon_hub/src/projects/mod.rs
  - zircon_hub/src/projects/editor_recent_sync.rs
  - zircon_hub/src/projects/metadata.rs
  - zircon_hub/src/state/hub_snapshot.rs
  - zircon_hub/src/team/local_git.rs
  - zircon_hub/src/app/localization.rs
  - zircon_hub/tests/project_management_contract.rs
  - zircon_hub/tests/project_workflow_contract.rs
  - zircon_hub/tests/project_path_scope_contract.rs
  - zircon_hub/tests/project_source_engine_contract.rs
  - zircon_hub/tests/project_page_copy_contract.rs
  - zircon_hub/tests/project_quick_actions_contract.rs
  - zircon_hub/tests/ui_contract.rs
  - zircon_hub/tests/ui_global_rules_contract.rs
  - zircon_hub/tests/ui_material_usage_contract.rs
  - zircon_hub/tests/ui_taffy_layout_contract.rs
  - zircon_hub/tests/ui_workspace_layout_contract.rs
  - zircon_hub/tests/ui_project_layout_contract.rs
  - zircon_hub/tests/ui_page_surface_coverage_contract.rs
  - zircon_hub/tests/ui_project_scope_contract.rs
  - zircon_hub/tests/ui_project_navigation_contract.rs
  - zircon_hub/tests/ui_project_browser_table_contract.rs
  - zircon_hub/tests/ui_selected_project_catalog_contract.rs
  - zircon_hub/tests/ui_selected_project_runtime_contract.rs
  - zircon_hub/tests/ui_foundation_contract.rs
  - zircon_hub/tests/ui_data_display_contract.rs
  - zircon_hub/tests/ui_inputs_contract.rs
  - zircon_hub/tests/ui_navigation_contract.rs
  - zircon_hub/tests/ui_input_navigation_api_contract.rs
  - zircon_hub/tests/ui_input_primitives_contract.rs
  - zircon_hub/tests/ui_overlay_primitives_contract.rs
  - zircon_hub/tests/ui_data_container_primitives_contract.rs
  - zircon_hub/tests/ui_shell_contract.rs
  - zircon_hub/tests/ui_shell_header_contract.rs
  - zircon_hub/tests/ui_shell_composition_contract.rs
  - zircon_hub/tests/ui_shell_navigation_contract.rs
  - zircon_hub/tests/ui_shell_window_contract.rs
  - zircon_hub/tests/ui_panel_slot_contract.rs
  - zircon_hub/tests/ui_typography_contract.rs
  - zircon_hub/tests/ui_visual_standard_contract.rs
  - docs/ui-and-layout/hub.png
  - docs/ui-and-layout/hub-ai-reference-manifest.json
  - docs/ui-and-layout/hub-ai-reference-manifest.schema.json
  - docs/ui-and-layout/hub-web-reference/index.html
  - docs/ui-and-layout/hub-web-reference/app.js
  - docs/ui-and-layout/hub-web-reference/styles.css
  - docs/ui-and-layout/hub-web-reference/export-pages.mjs
  - docs/ui-and-layout/hub-web-reference/validate-visuals.mjs
  - docs/ui-and-layout/hub-web-reference/validate-interactions.mjs
  - docs/ui-and-layout/hub-web-reference/ACCEPTANCE_EVIDENCE.md
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/SKILL.md
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1
  - dev/material-rust-template/README.md
  - dev/material-rust-template/material-1.0/material.slint
implementation_files:
  - zircon_hub/Cargo.toml
  - zircon_hub/build.rs
  - zircon_hub/ui/components.slint
  - zircon_hub/ui/tokens.slint
  - zircon_hub/ui/material_bridge.slint
  - zircon_hub/ui/theme.slint
  - zircon_hub/ui/layout.slint
  - zircon_hub/ui/surfaces.slint
  - zircon_hub/ui/inputs.slint
  - zircon_hub/ui/shell_header_components.slint
  - zircon_hub/ui/shell_header_popup_components.slint
  - zircon_hub/ui/shell_sidebar_components.slint
  - zircon_hub/ui/shell_page_components.slint
  - zircon_hub/ui/navigation.slint
  - zircon_hub/ui/data_display.slint
  - zircon_hub/ui/table_view_components.slint
  - zircon_hub/ui/operation_timeline_components.slint
  - zircon_hub/ui/overlays.slint
  - zircon_hub/ui/shared.slint
  - zircon_hub/ui/app.slint
  - zircon_hub/ui/projects.slint
  - zircon_hub/ui/project_dashboard.slint
  - zircon_hub/ui/project_dashboard_components.slint
  - zircon_hub/ui/project_pages.slint
  - zircon_hub/ui/project_new_page.slint
  - zircon_hub/ui/project_browser_page.slint
  - zircon_hub/ui/project_detail_page.slint
  - zircon_hub/ui/project_detail_components.slint
  - zircon_hub/ui/project_browser_components.slint
  - zircon_hub/ui/project_page_components.slint
  - zircon_hub/ui/editor.slint
  - zircon_hub/ui/editor_page_components.slint
  - zircon_hub/ui/builds.slint
  - zircon_hub/ui/builds_page_components.slint
  - zircon_hub/ui/cloud.slint
  - zircon_hub/ui/cloud_page_components.slint
  - zircon_hub/ui/catalog_page_components.slint
  - zircon_hub/ui/team.slint
  - zircon_hub/ui/team_page_components.slint
  - zircon_hub/ui/settings.slint
  - zircon_hub/ui/settings_page_components.slint
  - zircon_hub/src/app/runtime.rs
  - zircon_hub/src/app/runtime/folder_picker.rs
  - zircon_hub/src/app/runtime/project_workspace.rs
  - zircon_hub/src/app/runtime/root_paths.rs
  - zircon_hub/src/app/runtime/source_scoped_views.rs
  - zircon_hub/src/app/runtime/source_engine_paths.rs
  - zircon_hub/src/app/binding.rs
  - zircon_hub/src/app/view_model.rs
  - zircon_hub/src/app/view_model/media.rs
  - zircon_hub/src/app/view_model/projects.rs
  - zircon_hub/src/app/view_model/plugins.rs
  - zircon_hub/src/app/view_model/quick_actions.rs
  - zircon_hub/src/app/view_model/team.rs
  - zircon_hub/src/assets/catalog.rs
  - zircon_hub/src/learn/catalog.rs
  - zircon_hub/src/plugins/catalog.rs
  - zircon_hub/src/projects/mod.rs
  - zircon_hub/src/projects/editor_recent_sync.rs
  - zircon_hub/src/projects/metadata.rs
  - zircon_hub/src/state/hub_snapshot.rs
  - zircon_hub/src/team/local_git.rs
  - zircon_hub/src/app/localization.rs
  - zircon_hub/tests/project_management_contract.rs
  - zircon_hub/tests/project_workflow_contract.rs
  - zircon_hub/tests/project_path_scope_contract.rs
  - zircon_hub/tests/project_source_engine_contract.rs
  - zircon_hub/tests/project_page_copy_contract.rs
  - zircon_hub/tests/project_quick_actions_contract.rs
  - zircon_hub/tests/ui_contract.rs
  - zircon_hub/tests/ui_global_rules_contract.rs
  - zircon_hub/tests/ui_material_usage_contract.rs
  - zircon_hub/tests/ui_taffy_layout_contract.rs
  - zircon_hub/tests/ui_workspace_layout_contract.rs
  - zircon_hub/tests/ui_project_layout_contract.rs
  - zircon_hub/tests/ui_page_surface_coverage_contract.rs
  - zircon_hub/tests/ui_project_scope_contract.rs
  - zircon_hub/tests/ui_project_navigation_contract.rs
  - zircon_hub/tests/ui_project_browser_table_contract.rs
  - zircon_hub/tests/ui_selected_project_catalog_contract.rs
  - zircon_hub/tests/ui_selected_project_runtime_contract.rs
  - zircon_hub/tests/ui_foundation_contract.rs
  - zircon_hub/tests/ui_data_display_contract.rs
  - zircon_hub/tests/ui_inputs_contract.rs
  - zircon_hub/tests/ui_navigation_contract.rs
  - zircon_hub/tests/ui_input_navigation_api_contract.rs
  - zircon_hub/tests/ui_input_primitives_contract.rs
  - zircon_hub/tests/ui_overlay_primitives_contract.rs
  - zircon_hub/tests/ui_data_container_primitives_contract.rs
  - zircon_hub/tests/ui_shell_contract.rs
  - zircon_hub/tests/ui_shell_header_contract.rs
  - zircon_hub/tests/ui_shell_composition_contract.rs
  - zircon_hub/tests/ui_shell_navigation_contract.rs
  - zircon_hub/tests/ui_shell_window_contract.rs
  - zircon_hub/tests/ui_panel_slot_contract.rs
  - zircon_hub/tests/ui_typography_contract.rs
  - zircon_hub/tests/ui_visual_standard_contract.rs
  - docs/ui-and-layout/hub.png
  - docs/ui-and-layout/hub-ai-reference-manifest.json
  - docs/ui-and-layout/hub-ai-reference-manifest.schema.json
  - docs/ui-and-layout/hub-web-reference/index.html
  - docs/ui-and-layout/hub-web-reference/app.js
  - docs/ui-and-layout/hub-web-reference/styles.css
  - docs/ui-and-layout/hub-web-reference/export-pages.mjs
  - docs/ui-and-layout/hub-web-reference/validate-visuals.mjs
  - docs/ui-and-layout/hub-web-reference/validate-interactions.mjs
  - docs/ui-and-layout/hub-web-reference/ACCEPTANCE_EVIDENCE.md
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/SKILL.md
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1
plan_sources:
  - user: 2026-05-19 implement Zircon Hub responsive componentization plan
  - user: 2026-05-19 directly introduce the Slint Material UI template instead of designing a custom clone
  - user: 2026-05-28 continue Hub responsive UI milestone
  - user: 2026-05-28 optimize Hub from Zircon Hub 响应式组件化重构计划
  - .opencode/workflows/wfl_e6f2a3e7a001bwlTGNP366ii1f/main-plan.md
  - .opencode/workflows/wfl_e6f2a3e7a001bwlTGNP366ii1f/hub-responsive-baseline/plan.md
  - .opencode/workflows/wfl_e6f2a3e7a001bwlTGNP366ii1f/workflow.xml
  - .opencode/workflows/wfl_e6cbe9691001e6Yupnwkg7OjJS/hub-responsive-ui/plan.md
  - .opencode/workflows/wfl_e6cbe9691001e6Yupnwkg7OjJS/hub-docs-contracts/plan.md
  - .opencode/workflows/wfl_e6e3e18b9001tnrMvsVdCpGp5v/hub-baseline/plan.md
  - https://mui.com/material-ui/all-components/
  - dev/material-ui/packages/mui-system
  - dev/material-rust-template/README.md
  - dev/material-rust-template/material-1.0/material.slint
  - .codex/plans/Zircon Hub 全页面视觉一致性与设计图补齐计划.md
  - user: 2026-05-29 implement Zircon Hub full-page visual consistency and design artifact plan
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-token-theme/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-input-states/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-input-states/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-button-states/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-button-states/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-nav-states/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-nav-states/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-contracts-docs/plan.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-inputs-navigation-contracts-docs/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish/decomposition.md
  - .opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations-surfaces-layout-layout-flow/plan.md
  - .opencode/workflows/20260528_231820_026_优化hub[image Zircon Hub 响应式组件化重构计划.md]/hub-runtime-state-integration/decomposition.md
  - .opencode/workflows/20260528_231820_026_优化hub[image Zircon Hub 响应式组件化重构计划.md]/hub-runtime-state-integration-contract-docs/plan.md
  - .opencode/workflows/20260528_231820_026_优化hub[image Zircon Hub 响应式组件化重构计划.md]/hub-runtime-state-integration-contract-docs/review-surface.md
  - .opencode/workflows/20260528_231820_026_优化hub[image Zircon Hub 响应式组件化重构计划.md]/hub-docs-contract-refresh/plan.md
  - .opencode/workflows/20260528_231820_026_优化hub[image Zircon Hub 响应式组件化重构计划.md]/hub-docs-contract-refresh/review-surface.md
tests:
  - cargo test -p zircon_hub --test ui_foundation_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_global_rules_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_material_usage_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_typography_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_inputs_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_navigation_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_input_navigation_api_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_input_primitives_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_overlay_primitives_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_data_container_primitives_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_shell_navigation_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_data_display_contract --locked -- --nocapture
  - cargo test -p zircon_hub --test ui_panel_slot_contract --locked -- --nocapture
  - cargo check -p zircon_hub --locked
  - node docs\ui-and-layout\hub-web-reference\export-pages.mjs
  - node docs/ui-and-layout/hub-web-reference/validate-interactions.mjs
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-visual-standard-0529 --test ui_visual_standard_contract --message-format short --color never
  - cargo test -p zircon_hub --test project_workflow_contract --locked --jobs 1 -- --nocapture
  - cargo test -p zircon_hub --test ui_selected_project_runtime_contract --locked --jobs 1 -- --nocapture
  - cargo test -p zircon_hub --test ui_selected_project_catalog_contract --locked --jobs 1 -- --nocapture
  - zircon_hub/tests/hub_docs_contract.rs
  - cargo test -p zircon_hub --test hub_docs_contract --locked --jobs 1 -- --nocapture
  - cargo fmt --manifest-path zircon_hub\Cargo.toml --check
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_project_layout_contract.rs zircon_hub\tests\ui_panel_slot_contract.rs zircon_hub\tests\ui_inputs_contract.rs zircon_hub\tests\ui_material_usage_contract.rs zircon_hub\tests\ui_page_surface_coverage_contract.rs zircon_hub\tests\ui_typography_contract.rs zircon_hub\tests\ui_project_navigation_contract.rs zircon_hub\tests\ui_global_rules_contract.rs
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_project_browser_table_contract.rs
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_project_browser_table_contract.rs -o target\codex-contracts\ui_project_browser_table_contract.exe; target\codex-contracts\ui_project_browser_table_contract.exe
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-e2e-0528 --test ui_project_layout_contract --test ui_panel_slot_contract --test ui_inputs_contract --test ui_material_usage_contract --test ui_page_surface_coverage_contract --test ui_typography_contract --test ui_project_navigation_contract --test ui_global_rules_contract --message-format short --color never
  - cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-e2e-0528 --message-format short --color never
  - cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-e2e-0528 --message-format short --color never
  - git diff --check -- zircon_hub\ui\project_dashboard.slint zircon_hub\ui\project_dashboard_components.slint zircon_hub\ui\project_new_page.slint zircon_hub\ui\project_browser_page.slint zircon_hub\ui\project_detail_page.slint zircon_hub\ui\project_page_components.slint zircon_hub\ui\project_browser_components.slint zircon_hub\ui\project_detail_components.slint zircon_hub\tests\ui_project_layout_contract.rs docs\zircon_hub\ui\responsive-component-system.md .codex\sessions\20260529-0106-hub-projects-responsive.md
  - cargo fmt --manifest-path zircon_hub\Cargo.toml --check
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_foundation_contract.rs
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --test ui_foundation_contract --message-format short --color never
  - cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --message-format short --color never
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --message-format short --color never
  - cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --test ui_data_container_primitives_contract --message-format short --color never
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --test ui_foundation_contract --test ui_project_layout_contract --test ui_material_usage_contract --test ui_typography_contract --test ui_page_surface_coverage_contract --message-format short --color never
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --test ui_project_layout_contract --test ui_material_usage_contract --test ui_inputs_contract --test ui_typography_contract --test ui_page_surface_coverage_contract --message-format short --color never
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-project-pages.ps1 -BinaryPath target\debug\zircon_hub.exe -OutputDir target\hub-visual-check\project-engine-choice-list-direct-0531 -WindowWidth 1568 -WindowHeight 1003 -DetailClickX 1487 -DetailClickY 398
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-project-pages.ps1 -BinaryPath target\debug\zircon_hub.exe -OutputDir target\hub-visual-check\project-detail-hubsection-0531 -WindowWidth 1568 -WindowHeight 1003 -DetailClickX 1487 -DetailClickY 398
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-project-pages.ps1 -BinaryPath target\debug\zircon_hub.exe -OutputDir target\hub-visual-check-final -WindowWidth 1568 -WindowHeight 1003 -BrowserClickX 360 -BrowserClickY 808 -DetailClickX 1476 -DetailClickY 338 -BrowserFilterMenuClickX 1050 -BrowserFilterMenuClickY 227 -BrowserSortMenuClickX 1375 -BrowserSortMenuClickY 227 -CapturePendingDelete -CaptureBrowserMenus
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-project-pages.ps1 -BinaryPath target\debug\zircon_hub.exe -OutputDir target\hub-visual-check-responsive-0529\1600x1024 -WindowWidth 1600 -WindowHeight 1024 -CapturePendingDelete -CaptureBrowserMenus
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-project-pages.ps1 -BinaryPath target\debug\zircon_hub.exe -OutputDir target\hub-visual-check-responsive-0529\1280x900 -WindowWidth 1280 -WindowHeight 900 -DeleteClickX 1030 -DeleteClickY 449 -CapturePendingDelete -CaptureBrowserMenus
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-project-pages.ps1 -BinaryPath target\debug\zircon_hub.exe -OutputDir target\hub-visual-check-responsive-0529\1024x720 -WindowWidth 1024 -WindowHeight 720 -CapturePendingDelete -CaptureBrowserMenus
  - .codex\skills\zircon-project-skills\capture-hub-window-screenshot\scripts\capture-hub-window.ps1 with the final project-pages-config and each responsive project-pages-config for Editor, Assets, Builds, Plugins, Packages, Team, Learn, Settings, Source Engine popup, and user menu captures
  - Python Pillow dimension/content/delta check over target\hub-visual-check-final and target\hub-visual-check-responsive-0529\{1600x1024,1280x900,1024x720}
  - PowerShell recent_projects/project_metadata path-existence check over target\hub-visual-check-responsive-0529\{1600x1024,1280x900,1024x720}\project-pages-config
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_project_layout_contract.rs zircon_hub\tests\ui_panel_slot_contract.rs zircon_hub\tests\ui_page_surface_coverage_contract.rs zircon_hub\tests\ui_material_usage_contract.rs
  - cargo fmt --manifest-path zircon_hub\Cargo.toml --check
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-e2e-0528 --test ui_project_layout_contract --test ui_panel_slot_contract --test ui_inputs_contract --test ui_material_usage_contract --test ui_page_surface_coverage_contract --test ui_typography_contract --test ui_project_navigation_contract --test ui_global_rules_contract --message-format short --color never
  - cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-e2e-0528 --message-format short --color never
  - target/hub-visual-check/hub-project-template-rail-panel-0528-1280x900/hub-projects-new-project.png
  - target/hub-visual-check/hub-project-template-rail-panel-0528-1024x720/hub-projects-new-project.png
  - target/hub-visual-check/hub-project-template-rail-panel-0528-1024x1024-single/hub-projects-new-project.png
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-responsive-ui-0528 --test ui_global_rules_contract --test ui_page_surface_coverage_contract --message-format short --color never
  - cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-responsive-ui-0528 --message-format short --color never
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-responsive-ui-0528 --message-format short --color never
  - cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-responsive-ui-0528 --message-format short --color never
  - target/hub-visual-check/hub-responsive-ui-0528-1024x768.png
  - cargo fmt -p zircon_hub --check
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --message-format short --color never
  - cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --message-format short --color never
  - cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --message-format short --color never
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_taffy_layout_contract.rs zircon_hub\tests\ui_workspace_layout_contract.rs zircon_hub\tests\ui_project_layout_contract.rs zircon_hub\tests\ui_page_surface_coverage_contract.rs
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --test ui_workspace_layout_contract --test ui_project_layout_contract --test ui_page_surface_coverage_contract --test ui_taffy_layout_contract --message-format short --color never
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --test ui_shell_header_contract --test ui_shell_composition_contract --test ui_shell_navigation_contract --test ui_shell_window_contract --test ui_shell_contract --message-format short --color never
  - rustfmt --edition 2021 --check zircon_hub\tests\project_workflow_contract.rs zircon_hub\tests\project_path_scope_contract.rs zircon_hub\tests\project_source_engine_contract.rs zircon_hub\tests\project_page_copy_contract.rs zircon_hub\tests\project_quick_actions_contract.rs
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_path_scope_contract.rs -o target\hub-material-check\project_path_scope_contract_0525.exe; target\hub-material-check\project_path_scope_contract_0525.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_source_engine_contract.rs -o target\hub-material-check\project_source_engine_contract_0525.exe; target\hub-material-check\project_source_engine_contract_0525.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_page_copy_contract.rs -o target\hub-material-check\project_page_copy_contract_0525.exe; target\hub-material-check\project_page_copy_contract_0525.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_quick_actions_contract.rs -o target\hub-material-check\project_quick_actions_contract_0525.exe; target\hub-material-check\project_quick_actions_contract_0525.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_page_surface_coverage_contract.rs -o target\hub-material-check\ui_page_surface_coverage_contract_settings_0525.exe; target\hub-material-check\ui_page_surface_coverage_contract_settings_0525.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_data_display_contract.rs -o target\hub-material-check\ui_data_display_contract_0524.exe; target\hub-material-check\ui_data_display_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_data_display_split_0524.exe; target\hub-material-check\ui_contract_data_display_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_inputs_contract.rs -o target\hub-material-check\ui_inputs_contract_0524.exe; target\hub-material-check\ui_inputs_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_inputs_split_0524.exe; target\hub-material-check\ui_contract_inputs_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_navigation_contract.rs -o target\hub-material-check\ui_navigation_contract_0524.exe; target\hub-material-check\ui_navigation_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_navigation_split_0524.exe; target\hub-material-check\ui_contract_navigation_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_typography_contract.rs -o target\hub-material-check\ui_typography_contract_0524.exe; target\hub-material-check\ui_typography_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition 2021 --test zircon_hub\tests\ui_typography_contract.rs -o F:\cargo-targets\zircon-platform-m5-workspace\manual-static-contracts\ui_typography_contract.exe; F:\cargo-targets\zircon-platform-m5-workspace\manual-static-contracts\ui_typography_contract.exe --test-threads=1
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition 2021 --test zircon_hub\tests\ui_workspace_layout_contract.rs -o F:\cargo-targets\zircon-platform-m5-workspace\manual-static-contracts\ui_workspace_layout_contract.exe; F:\cargo-targets\zircon-platform-m5-workspace\manual-static-contracts\ui_workspace_layout_contract.exe --test-threads=1
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_typography_split_0524.exe; target\hub-material-check\ui_contract_typography_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_foundation_contract.rs -o target\hub-material-check\ui_foundation_contract_0524.exe; target\hub-material-check\ui_foundation_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_foundation_split_0524.exe; target\hub-material-check\ui_contract_foundation_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_global_rules_contract.rs -o target\hub-material-check\ui_global_rules_contract_0524.exe; target\hub-material-check\ui_global_rules_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_global_rules_split_0524.exe; target\hub-material-check\ui_contract_global_rules_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_material_usage_contract.rs -o target\hub-material-check\ui_material_usage_contract_0524.exe; target\hub-material-check\ui_material_usage_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_taffy_layout_contract.rs -o target\hub-material-check\ui_taffy_layout_contract_0524.exe; target\hub-material-check\ui_taffy_layout_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_material_taffy_split_0524.exe; target\hub-material-check\ui_contract_material_taffy_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_project_scope_contract.rs -o target\hub-material-check\ui_project_scope_contract_0524.exe; target\hub-material-check\ui_project_scope_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_project_navigation_contract.rs -o target\hub-material-check\ui_project_navigation_contract_0524.exe; target\hub-material-check\ui_project_navigation_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_retired_0524.exe; target\hub-material-check\ui_contract_retired_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_selected_project_catalog_contract.rs -o target\hub-material-check\ui_selected_project_catalog_contract_0524.exe; target\hub-material-check\ui_selected_project_catalog_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_selected_project_runtime_contract.rs -o target\hub-material-check\ui_selected_project_runtime_contract_0524.exe; target\hub-material-check\ui_selected_project_runtime_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_project_scope_contract.rs -o target\hub-material-check\ui_project_scope_contract_split_0524.exe; target\hub-material-check\ui_project_scope_contract_split_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_shell_contract.rs -o target\hub-material-check\ui_shell_contract_0524.exe; target\hub-material-check\ui_shell_contract_0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_shell_split_0524.exe; target\hub-material-check\ui_contract_shell_split_0524.exe --nocapture
  - git diff --check -- zircon_hub\tests\ui_contract.rs zircon_hub\tests\ui_shell_contract.rs docs\zircon_hub\index.md docs\zircon_hub\ui\responsive-component-system.md .codex\sessions\20260522-0348-hub-pageslot-editor-height.md
  - git diff --check -- zircon_hub/ui/inputs.slint zircon_hub/tests/ui_contract.rs docs/zircon_hub/ui/responsive-component-system.md
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-toolbar-select-chevron-0524.exe; target\hub-toolbar-select-chevron-0524.exe --nocapture
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_contract.rs
  - cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-detail-delete-0524-c --message-format short --color never
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1 -BinaryPath D:\cargo-targets\zircon-hub-detail-delete-0524-c\debug\zircon_hub.exe -WindowWidth 1280 -WindowHeight 900 -OutputDir target\hub-visual-check\hub-projects-toolbar-chevron-1280x900-0524 -CaptureBrowserMenus
  - target/hub-visual-check/hub-projects-toolbar-chevron-1280x900-0524/hub-projects-{browser,browser-filter-menu,browser-sort-menu}.png
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1 -BinaryPath D:\cargo-targets\zircon-hub-detail-delete-0524-c\debug\zircon_hub.exe -WindowWidth 1600 -WindowHeight 1024 -OutputDir target\hub-visual-check\hub-projects-toolbar-chevron-1600x1024-0524 -CaptureBrowserMenus
  - target/hub-visual-check/hub-projects-toolbar-chevron-1600x1024-0524/hub-projects-{dashboard,new-project,browser,detail,browser-filter-menu,browser-sort-menu}.png
  - git diff --check -- zircon_hub/ui/project_pages.slint zircon_hub/tests/ui_contract.rs .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1 .codex/skills/zircon-project-skills/capture-hub-window-screenshot/SKILL.md docs/zircon_hub/ui/responsive-component-system.md
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-delete-confirm-0524\ui_contract_delete_confirm.exe; target\hub-delete-confirm-0524\ui_contract_delete_confirm.exe --nocapture
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1 -WindowWidth 1600 -WindowHeight 1024 -OutputDir target/hub-visual-check/hub-projects-1600x1024-delete-confirm -CapturePendingDelete
  - target/hub-visual-check/hub-projects-1600x1024-delete-confirm/hub-projects-{dashboard,new-project,browser,detail,detail-delete-confirm}.png
  - cargo fmt -p zircon_hub --check
  - git diff --check -- zircon_hub/ui/surfaces.slint zircon_hub/ui/project_pages.slint zircon_hub/tests/ui_contract.rs docs/zircon_hub/ui/responsive-component-system.md
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-compact-delete-0524\ui_contract_compact_delete.exe; target\hub-compact-delete-0524\ui_contract_compact_delete.exe --nocapture
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - cargo test -p zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1 -WindowWidth 1024 -WindowHeight 720 -OutputDir target/hub-visual-check/hub-projects-1024x720-statusbanner-final-0524 -CapturePendingDelete
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1 -WindowWidth 1600 -WindowHeight 1024 -OutputDir target/hub-visual-check/hub-projects-1600x1024-statusbanner-final-0524 -CapturePendingDelete
  - target/hub-visual-check/hub-projects-1024x720-statusbanner-final-0524/hub-projects-{dashboard,new-project,browser,detail,detail-delete-ready,detail-delete-confirm}.png
  - target/hub-visual-check/hub-projects-1600x1024-statusbanner-final-0524/hub-projects-{dashboard,new-project,browser,detail,detail-delete-confirm}.png
  - cargo fmt -p zircon_hub --check
  - git diff --check -- zircon_hub/ui/app.slint zircon_hub/tests/ui_contract.rs docs/zircon_hub/ui/responsive-component-system.md
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-subpage-header-0524\ui_contract_subpage_header.exe; target\hub-subpage-header-0524\ui_contract_subpage_header.exe --nocapture
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - target/hub-visual-check/hub-projects-1024x720-subpage-header-hidden/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/hub-projects-1280x900-subpage-header-hidden/hub-projects-{dashboard,new-project,browser,detail}.png
  - cargo fmt -p zircon_hub --check
  - git diff --check -- zircon_hub/ui/app.slint zircon_hub/tests/ui_contract.rs docs/zircon_hub/ui/responsive-component-system.md
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-project-actions-0524\ui_contract_project_actions.exe; target\hub-project-actions-0524\ui_contract_project_actions.exe --nocapture
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - target/hub-visual-check/hub-projects-1024x720-project-actions-hidden/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/hub-projects-1280x900-project-actions-hidden/hub-projects-{dashboard,new-project,browser,detail}.png
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test --manifest-path zircon_hub\Cargo.toml --test ui_contract --locked --offline --jobs 1 --message-format short --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never
  - target/hub-visual-check/hub-projects-responsive-1600x1024-current/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/hub-projects-responsive-1280x900-current/hub-projects-{dashboard,new-project,browser,detail}.png
  - cargo fmt -p zircon_hub --check
  - cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-new-project-location-0522 --message-format short --color never
  - cargo test -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-new-project-location-0522 --message-format short --color never
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-new-project-location-0522 --message-format short --color never
  - cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-continue-0522 --message-format short --color never
  - cargo test -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-continue-0522 --message-format short --color never
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-continue-0522\ui_contract_direct.exe; target\hub-continue-0522\ui_contract_direct.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-continue-0523\ui_contract_direct.exe; target\hub-continue-0523\ui_contract_direct.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-continue-0523-c\ui_contract_direct.exe; target\hub-continue-0523-c\ui_contract_direct.exe --nocapture
  - CARGO_PROFILE_DEV_DEBUG=0 cargo build --manifest-path target\hub-standalone-0523\zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-standalone-0523 --message-format short --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo check --manifest-path target\hub-standalone-0523\zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-standalone-0523 --message-format short --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test --manifest-path target\hub-standalone-0523\zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-standalone-0523 --message-format short --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test --manifest-path target\hub-standalone-0523\zircon_hub\Cargo.toml --test project_management_contract --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-standalone-0523 --message-format short --color never
  - target/hub-visual-check/hub-projects-continue-0523-latest-1600x1024/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/hub-projects-continue-0523-latest-1280x900/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/new-project-location-1280x900-0522-final/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/new-project-location-1024x720-0522-final/hub-projects-{dashboard,new-project,browser,detail}.png
  - cargo check -p zircon_hub --locked --offline --jobs 4 --target-dir target\hub-width-check --message-format short --color never
  - cargo test -p zircon_hub --test ui_contract --locked --offline --jobs 4 --target-dir target\hub-width-check --message-format short --color never
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_projects_height_direct.exe; target\hub-material-check\ui_contract_projects_height_direct.exe --nocapture
  - cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir target\hub-material-check --color never
  - cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir target\hub-material-check --message-format short --color never
  - cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-info-row-0520 --message-format short --color never
  - cargo test -p zircon_hub --test ui_contract --locked --offline --jobs 1 --target-dir target\hub-material-check --message-format short --color never
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_windowbutton_direct.exe; target\hub-material-check\ui_contract_windowbutton_direct.exe --nocapture
  - cargo test -p zircon_hub --locked --offline --jobs 1 --target-dir target\hub-material-check --color never
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --target-dir target\hub-material-check --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-ui-contract-nodebug-0520 --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-ui-contract-nodebug-0520 --message-format short --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test -p zircon_hub --test ui_contract --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-ui-contract-nodebug-0520 --message-format short --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-ui-contract-nodebug-0520 --message-format short --color never
  - rustfmt --edition 2021 --check zircon_hub\tests\ui_contract.rs
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_project_browser_detail_direct.exe; target\hub-material-check\ui_contract_project_browser_detail_direct.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_component_samples_direct.exe; target\hub-material-check\ui_contract_component_samples_direct.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-selected-build-0522.exe; target\hub-ui-contract-selected-build-0522.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-retry-b-0522.exe; target\hub-ui-contract-retry-b-0522.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-retry-d-0522.exe; target\hub-ui-contract-retry-d-0522.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_page_surface_roots_0523.exe; target\hub-material-check\ui_contract_page_surface_roots_0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-team-repo-match-0523.exe; target\hub-ui-contract-team-repo-match-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_workflow_contract.rs -o target\hub-project-workflow-contract-0524.exe; target\hub-project-workflow-contract-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_workflow_contract.rs -o target\hub-project-workflow-contract-source-scoped-0524.exe; target\hub-project-workflow-contract-source-scoped-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_workflow_contract.rs -o target\hub-project-workflow-contract-build-source-refresh-0524.exe; target\hub-project-workflow-contract-build-source-refresh-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-selected-path-match-0523.exe; target\hub-ui-contract-selected-path-match-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-startup-selected-project-0523.exe; target\hub-ui-contract-startup-selected-project-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-cloud-project-path-match-0523.exe; target\hub-ui-contract-cloud-project-path-match-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-project-metadata-key-0523.exe; target\hub-ui-contract-project-metadata-key-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-quick-action-target-label-0523.exe; target\hub-ui-contract-quick-action-target-label-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-build-bound-engine-0523.exe; target\hub-ui-contract-build-bound-engine-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-build-engine-state-copy-0523.exe; target\hub-ui-contract-build-engine-state-copy-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-remember-project-engine-0523.exe; target\hub-ui-contract-remember-project-engine-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-project-engine-unavailable-0523.exe; target\hub-ui-contract-project-engine-unavailable-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-new-project-engine-required-0523.exe; target\hub-ui-contract-new-project-engine-required-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-open-preserves-binding-0523.exe; target\hub-ui-contract-open-preserves-binding-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-builds-can-build-row-final-0523.exe; target\hub-ui-contract-builds-can-build-row-final-0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_panel_slot_contract.rs -o target\hub-material-check\ui_panel_slot_contract_0523.exe; target\hub-material-check\ui_panel_slot_contract_0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_panel_slot_0523.exe; target\hub-material-check\ui_contract_panel_slot_0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_panel_slot_contract.rs -o target\hub-material-check\ui_panel_slot_contract_0523_editor.exe; target\hub-material-check\ui_panel_slot_contract_0523_editor.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_panel_slot_editor_0523.exe; target\hub-material-check\ui_contract_panel_slot_editor_0523.exe --nocapture
  - target\hub-material-check\hub_slint_compile_probe_0523.exe
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_panel_slot_contract.rs -o target\hub-material-check\ui_panel_slot_contract_0523_settings.exe; target\hub-material-check\ui_panel_slot_contract_0523_settings.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_panel_slot_settings_0523.exe; target\hub-material-check\ui_contract_panel_slot_settings_0523.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-material-check\ui_contract_metric_card_0523.exe; target\hub-material-check\ui_contract_metric_card_0523.exe cloud_and_team_workspace_typography_uses_material_text material_and_taffy_coverage_uses_real_hub_surfaces --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-source-scoped-0524.exe; target\hub-ui-contract-source-scoped-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-build-source-refresh-0524.exe; target\hub-ui-contract-build-source-refresh-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_workflow_contract.rs -o target\hub-project-workflow-contract-new-project-engine-sync-0524.exe; target\hub-project-workflow-contract-new-project-engine-sync-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_workflow_contract.rs -o target\hub-project-workflow-contract-output-refresh-0524.exe; target\hub-project-workflow-contract-output-refresh-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\project_workflow_contract.rs -o target\hub-project-workflow-contract-cloud-output-config-0524.exe; target\hub-project-workflow-contract-cloud-output-config-0524.exe --nocapture
  - CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub rustc --edition=2021 --test zircon_hub\tests\ui_contract.rs -o target\hub-ui-contract-new-project-engine-sync-0524.exe; target\hub-ui-contract-new-project-engine-sync-0524.exe --nocapture
  - target\hub-material-check\hub_slint_compile_probe_0523_settings.exe
  - cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-panel-slot-settings-0523 --message-format short --color never
  - OUT_DIR=E:\Git\ZirconEngine\target\hub-ui-check-selected-build-0522 CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub CARGO_PKG_NAME=zircon_hub D:\cargo-targets\zircon-hub-continue-0522\debug\build\zircon_hub-7fc227b3e5306fb7\build-script-build.exe
  - OUT_DIR=target\hub-ui-check-open-detail-callback CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub CARGO_PKG_NAME=zircon_hub target\hub-project-scroll-check\debug\build\zircon_hub-7fc227b3e5306fb7\build-script-build.exe
  - CARGO_PROFILE_DEV_DEBUG=0 cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-project-browser-detail-0520 --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo test -p zircon_hub --test ui_contract --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-project-browser-detail-0520 --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo check -p zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-project-browser-detail-0520 --color never
  - CARGO_PROFILE_DEV_DEBUG=0 cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-project-browser-detail-0520 --color never
  - target/hub-visual-check/material-open-detail-iconbutton-default-1280x900/hub-projects-dashboard.png
  - target/hub-visual-check/material-open-detail-iconbutton-default-1280x900/hub-projects-new-project.png
  - target/hub-visual-check/material-open-detail-iconbutton-default-1280x900/hub-projects-browser.png
  - target/hub-visual-check/material-open-detail-iconbutton-default-1280x900/hub-projects-detail.png
  - target/hub-visual-check/material-detail-hit-target-1280x900/hub-projects-dashboard.png
  - target/hub-visual-check/material-detail-hit-target-1280x900/hub-projects-new-project.png
  - target/hub-visual-check/material-detail-hit-target-1280x900/hub-projects-browser.png
  - target/hub-visual-check/material-detail-hit-target-1280x900/hub-projects-detail.png
  - target/hub-visual-check/material-browser-detail-current-1280x900/hub-projects-dashboard.png
  - target/hub-visual-check/material-browser-detail-current-1280x900/hub-projects-new-project.png
  - target/hub-visual-check/material-browser-detail-current-1280x900/hub-projects-browser.png
  - target/hub-visual-check/material-browser-detail-current-1280x900/hub-projects-detail.png
  - target/hub-visual-check/material-browser-viewall-detail-1280x900/hub-projects-dashboard.png
  - target/hub-visual-check/material-browser-viewall-detail-1280x900/hub-projects-new-project.png
  - target/hub-visual-check/material-browser-viewall-detail-1280x900/hub-projects-browser.png
  - target/hub-visual-check/material-browser-viewall-detail-1280x900/hub-projects-detail.png
  - cargo build -p zircon_hub --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-browser-hit-area-0521 --message-format short --color never
  - target/hub-visual-check/material-popupmenu-local-nav-1280x900-asserted-0521-v2/hub-projects-dashboard.png
  - target/hub-visual-check/material-popupmenu-local-nav-1280x900-asserted-0521-v2/hub-projects-new-project.png
  - target/hub-visual-check/material-popupmenu-local-nav-1280x900-asserted-0521-v2/hub-projects-browser.png
  - target/hub-visual-check/material-popupmenu-local-nav-1280x900-asserted-0521-v2/hub-projects-detail.png
  - target/hub-visual-check/material-popupmenu-local-nav-1024x720-asserted-0521-v2/hub-projects-dashboard.png
  - target/hub-visual-check/material-popupmenu-local-nav-1024x720-asserted-0521-v2/hub-projects-new-project.png
  - target/hub-visual-check/material-popupmenu-local-nav-1024x720-asserted-0521-v2/hub-projects-browser.png
  - target/hub-visual-check/material-popupmenu-local-nav-1024x720-asserted-0521-v2/hub-projects-detail.png
  - OUT_DIR=target\hub-ui-check-project-browser-detail-button CARGO_MANIFEST_DIR=E:\Git\ZirconEngine\zircon_hub CARGO_PKG_NAME=zircon_hub target\hub-project-scroll-check\debug\build\zircon_hub-7fc227b3e5306fb7\build-script-build.exe
  - target/hub-ui-check-project-browser-detail-button/app.rs
  - zircon_hub/tests/ui_contract.rs
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-window.ps1
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1
  - target/hub-visual-check/projects-taffy-1600x1024-0520/hub-projects-dashboard.png
  - target/hub-visual-check/projects-taffy-1600x1024-0520/hub-projects-new-project.png
  - target/hub-visual-check/projects-taffy-1600x1024-0520/hub-projects-browser.png
  - target/hub-visual-check/projects-taffy-1600x1024-0520/hub-projects-detail.png
  - target/hub-visual-check/projects-taffy-1280x900-0520-v2/hub-projects-dashboard.png
  - target/hub-visual-check/projects-taffy-1280x900-0520-v2/hub-projects-new-project.png
  - target/hub-visual-check/projects-taffy-1280x900-0520-v2/hub-projects-browser.png
  - target/hub-visual-check/projects-taffy-1280x900-0520-v2/hub-projects-detail.png
  - target/hub-visual-check/projects-taffy-1024x768-0520-v2/hub-projects-dashboard.png
  - target/hub-visual-check/projects-taffy-1024x768-0520-v2/hub-projects-new-project.png
  - target/hub-visual-check/projects-taffy-1024x768-0520-v2/hub-projects-browser.png
  - target/hub-visual-check/projects-taffy-1024x768-0520-v2/hub-projects-detail.png
  - target/hub-visual-check/material-card-final-project-pages-v8/hub-projects-dashboard.png
  - target/hub-visual-check/material-card-final-project-pages-v8/hub-projects-new-project.png
  - target/hub-visual-check/material-card-final-project-pages-v8/hub-projects-browser.png
  - target/hub-visual-check/material-card-final-project-pages-v8/hub-projects-detail.png
  - target/hub-visual-check/material-card-final-project-pages-v8-1366b/hub-projects-detail.png
  - target/hub-visual-check/material-card-final-responsive-1100x760.png
  - target/hub-visual-check/material-card-final-responsive-960x640.png
  - target/hub-visual-check/material-buttons-dashboard-fixed-960x640.png
  - target/hub-visual-check/material-textfield-settings-960x640.png
  - target/hub-visual-check/material-textfield-editor-960x640.png
  - target/hub-visual-check/material-textfield-editor-fields-960x640.png
  - target/hub-visual-check/material-textfield-project-pages/hub-projects-new-project.png
  - target/hub-visual-check/material-toolbar-popup-960x640.png
  - target/hub-visual-check/material-segment-settings-960x640.png
  - target/hub-visual-check/material-dropdown-header-fixed-960x640.png
  - target/hub-visual-check/material-dropdown-popup-960x640.png
  - target/hub-visual-check/material-searchbox-1600x1024-v3/hub-projects-dashboard.png
  - target/hub-visual-check/material-searchbox-1600x1024-v3/hub-projects-new-project.png
  - target/hub-visual-check/material-searchbox-1600x1024-v3/hub-projects-browser.png
  - target/hub-visual-check/material-searchbox-1600x1024-v3/hub-projects-detail.png
  - target/hub-visual-check/material-searchbox-1280x900-v5/hub-projects-dashboard.png
  - target/hub-visual-check/material-searchbox-1280x900-v5/hub-projects-browser.png
  - target/hub-visual-check/material-searchbox-1280x900-v5/hub-projects-detail.png
  - target/hub-visual-check/material-searchbox-1024x768-v2/hub-projects-new-project.png
  - target/hub-visual-check/material-searchbox-1024x768-v2/hub-projects-browser.png
  - target/hub-visual-check/material-searchbox-1024x768-v2/hub-projects-detail.png
  - target/hub-visual-check/zircon-theme-material-listtile-1280x900-v3/hub-projects-dashboard.png
  - target/hub-visual-check/zircon-theme-material-listtile-1280x900-v3/hub-projects-new-project.png
  - target/hub-visual-check/zircon-theme-material-listtile-1280x900-v3/hub-projects-browser.png
  - target/hub-visual-check/zircon-theme-material-listtile-1280x900-v3/hub-projects-detail.png
  - target/hub-visual-check/zircon-theme-material-listtile-1024x768-v1/hub-projects-dashboard.png
  - target/hub-visual-check/zircon-theme-material-listtile-1024x768-v1/hub-projects-new-project.png
  - target/hub-visual-check/zircon-theme-material-listtile-1024x768-v1/hub-projects-browser.png
  - target/hub-visual-check/zircon-theme-material-listtile-1024x768-v1/hub-projects-detail.png
  - target/hub-visual-check/material-searchbar-1024x768-v2/hub-projects-dashboard.png
  - target/hub-visual-check/material-searchbar-1024x768-v2/hub-projects-new-project.png
  - target/hub-visual-check/material-searchbar-1024x768-v2/hub-projects-browser.png
  - target/hub-visual-check/material-searchbar-1024x768-v2/hub-projects-detail.png
  - target/hub-visual-check/material-navigationrail-baseline-nosize-main-984x640.png
  - target/hub-visual-check/material-navigationrail-collapsed-nosize-1100x820.png
  - target/hub-visual-check/material-navbutton-listtile-expanded-nosize-1100x820.png
  - target/hub-visual-check/material-statuspill-actionchip-1600x1024.png
  - target/hub-visual-check/material-actionrow-1600x1024-v1/hub-projects-dashboard.png
  - target/hub-visual-check/material-actionrow-1600x1024-v1/hub-projects-browser.png
  - target/hub-visual-check/material-actionrow-1600x1024-v1/hub-projects-detail.png
  - target/hub-visual-check/material-actionrow-1024x768-v1/hub-projects-dashboard.png
  - target/hub-visual-check/material-actionrow-1024x768-v1/hub-projects-browser.png
  - target/hub-visual-check/material-actionrow-1024x768-v1/hub-projects-detail.png
  - target/hub-visual-check/material-header-engine-listtile-984x640-v1.png
  - target/hub-visual-check/material-inforow-editor-984x640-v1.png
  - target/hub-visual-check/material-inforow-builds-984x640-v2.png
  - target/hub-visual-check/material-inforow-settings-1280x900-v3.png
  - target/hub-visual-check/material-windowbutton-984x640-v1.png
  - target/hub-visual-check/material-divider-1600x1024.png
  - target/hub-visual-check/material-datadisplay-scrollview-1100x820.png
  - target/hub-visual-check/material-project-scrollview-1100x820/hub-projects-dashboard.png
  - target/hub-visual-check/material-project-scrollview-1100x820/hub-projects-new-project.png
  - target/hub-visual-check/material-project-scrollview-1100x820/hub-projects-browser.png
  - target/hub-visual-check/material-project-scrollview-1100x820/hub-projects-detail.png
  - target/hub-visual-check/material-badge-materialtext-1100x820-software.png
  - target/hub-visual-check/material-badge-materialtext-pages-1100x820/hub-projects-dashboard.png
  - target/hub-visual-check/material-badge-materialtext-pages-1100x820/hub-projects-new-project.png
  - target/hub-visual-check/material-badge-materialtext-pages-1100x820/hub-projects-browser.png
  - target/hub-visual-check/material-badge-materialtext-pages-1100x820/hub-projects-detail.png
  - target/hub-visual-check/shared-materialtext-typography-1100x820.png
  - target/hub-visual-check/surface-title-materialtext-1100x820.png
  - target/hub-visual-check/data-table-materialtext-1100x820.png
  - target/hub-visual-check/catalog-list-materialtext-1100x820-assets.png
  - target/hub-visual-check/catalog-list-materialtext-1100x820-plugins.png
  - target/hub-visual-check/shell-page-header-materialtext-1100x820-assets.png
  - target/hub-visual-check/shell-chrome-materialtext-1100x820-assets.png
  - target/hub-visual-check/dashboard-card-materialtext-1100x820.png
  - target/hub-visual-check/project-workflow-materialtext-1100x820/hub-projects-{dashboard,new-project,browser,detail}.png
  - target/hub-visual-check/cloud-team-materialtext-1100x820/{cloud,team}-materialtext.png
  - target/hub-visual-check/builds-materialtext-1100x820/builds-materialtext.png
  - target/hub-visual-check/builds-materialtext-1600x1024/builds-materialtext.png
  - target/hub-material-check/ui_contract_project_browser_state_direct.exe --nocapture
  - target/hub-material-check/ui_contract_project_selector_state_direct.exe --nocapture
  - target/hub-material-check/ui_contract_sidebar_collapse_state_direct.exe --nocapture
  - target/hub-material-check/ui_contract_project_browser_select_direct.exe --nocapture
  - target/hub-material-check/ui_contract_global_typography_direct.exe --nocapture
  - target/hub-material-check/ui_contract_character_icons_direct.exe --nocapture
  - target/hub-material-check/ui_contract_toucharea_direct.exe --nocapture
  - target/hub-material-check/ui_contract_percentage_sizing_direct.exe --nocapture
  - target/hub-material-check/ui_contract_workspace_slot_direct.exe --nocapture
  - target/hub-ui-check-project-browser-state/app.rs
  - target/hub-ui-check-project-selector-state/app.rs
  - target/hub-ui-check-sidebar-collapse-state/app.rs
  - target/hub-ui-check-project-browser-select/app.rs
  - target/hub-ui-layout-gen/app.rs
  - .codex/skills/zircon-project-skills/capture-hub-window-screenshot/scripts/capture-hub-project-pages.ps1
  - rustfmt --edition 2021 --check zircon_hub\tests\project_workflow_contract.rs zircon_hub\tests\project_path_scope_contract.rs zircon_hub\tests\project_source_engine_contract.rs zircon_hub\tests\ui_project_scope_contract.rs zircon_hub\tests\ui_project_navigation_contract.rs zircon_hub\tests\ui_selected_project_runtime_contract.rs
  - cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-docs-contracts-0528 --test project_workflow_contract --test project_path_scope_contract --test project_source_engine_contract --test ui_project_scope_contract --test ui_project_navigation_contract --test ui_selected_project_runtime_contract --message-format short --color never
  - cargo test -p zircon_hub --test ui_foundation_contract --locked
  - cargo test -p zircon_hub --test ui_workspace_layout_contract --locked
  - cargo test -p zircon_hub --test ui_taffy_layout_contract --locked
  - cargo check -p zircon_hub --locked
doc_type: module-detail
---

# Zircon Hub Responsive Component System

Zircon Hub keeps Slint as the rendering and interaction layer. It does not embed HTML, CSS, WebView, or a browser layout engine. The Hub UI now exposes a Slint-native component layer that borrows the useful parts of Material UI's component taxonomy: tokens first, layout primitives second, then surfaces, inputs, navigation, data display, and overlays.

## Hub Visual Consistency Contract

### `hub-ui-polish-foundations` Milestone Boundary

The workflow milestone `.opencode/workflows/20260528_190023_866_继续完善hub/hub-ui-polish-foundations/decomposition.md` owns only the lower Hub design-system foundation. Its owner files are `tokens.slint`, `theme.slint`, `material_bridge.slint`, `layout.slint`, `surfaces.slint`, `inputs.slint`, `navigation.slint`, `data_display.slint`, `table_view_components.slint`, `operation_timeline_components.slint`, `overlays.slint`, `shared.slint`, `components.slint`, and the focused static UI contracts named in the document header. This milestone intentionally does not polish shell placement or individual page layouts; those remain downstream work for `hub-ui-polish-shell-responsive`, `hub-ui-polish-project-pages`, and `hub-ui-polish-workspace-pages` after the primitive APIs are stable.

Foundation primitives must expose semantic state instead of page-local visual forks. Tokens define spacing, page padding, panel/toolbar gaps, breakpoints, row/control heights, radii, borders, focus-ring width/color/offset, disabled opacity, Material typography usage, and neutral/info/success/warning/error status colors. Inputs, buttons, navigation rows, and data rows expose shared enabled/disabled, selected or active, focus, status/tone, density, basis/min-width, overflow, and scroll-budget APIs; hover and pressed behavior stays delegated to Material controls or `StateLayerArea` instead of direct `TouchArea` layers. `components.slint` remains a thin import surface, and `shared.slint` is limited to cross-page DTOs plus genuinely shared compatibility wrappers.

Acceptance for this milestone is the focused command set recorded in the document header: `ui_foundation_contract`, `ui_global_rules_contract`, `ui_material_usage_contract`, `ui_typography_contract`, `ui_inputs_contract`, `ui_navigation_contract`, `ui_data_display_contract`, `ui_panel_slot_contract`, and `cargo check -p zircon_hub --locked`. Downstream shell/page polish may consume these token and primitive contracts, but should not rename or bypass them without updating the same owner files and contract tests.

`docs/ui-and-layout/hub.png` is the Projects Dashboard pixel reference. `HubVisualSpec` in `zircon_hub/ui/tokens.slint` defines the shared reference canvas, radius, density, panel, outline, and state-color tokens that pages must consume through shared Hub components.

The companion `1568x1003` design PNG set under `docs/ui-and-layout/` is now AI-directed, HTML/CSS-finalized. `docs/ui-and-layout/hub-ai-reference-manifest.json` records `docs/ui-and-layout/hub.png` as the source reference, the prompt family, selected page draft role, AI structure-layout direction drafts, and final export filenames. `docs/ui-and-layout/hub-ai-reference-manifest.schema.json` is the manifest schema contract, and `docs/ui-and-layout/hub-web-reference/validate-visuals.mjs` runs schema subset validation plus negative schema self-tests for extra fields, missing reference output, short reference inventory, malformed AI draft paths, and canvas drift. Those AI drafts are for reviewing the overall interaction structure first: navigation, page regions, toolbars, card/table grids, side panels, dropdowns, modals, and state overlays. local functional-content callouts are secondary and only clarify page intent. `docs/ui-and-layout/hub-web-reference/export-pages.mjs` captures the static web reference with Playwright/Edge, so the final reference text, icons, spacing, menus, overlays, and state panels are authored in HTML/CSS. The exporter starts on port `5198` by default, falls back to a free local port when the default is occupied, and keeps explicit `ZIRCON_HUB_WEB_REFERENCE_PORT` strict. `docs/ui-and-layout/hub-web-reference/validate-interactions.mjs` then locks browser-openable page selection: it rejects unknown `data-route` and `href="?page=..."` targets, verifies exported PNG filename replay, verifies every `EXPORTS.md` replay path, and checks representative header, project, toolbar, and Quick Actions click routes. AI drafts are design direction only and are not acceptance evidence; real Hub captures remain runtime evidence and must still be collected with the Hub screenshot scripts before claiming runtime visual acceptance.

The supplemental design-mode sheets `hub-design-structure-layout.png`, `hub-design-structure-supplement.png`, and `hub-design-functional-details.png` stay outside the final `hub-*.png` acceptance inventory. They exist for overall structure layout review and local functional-content detail discussion only; the accepted page/state references remain the manifest outputs plus `hub.png`. Their reproducible source is `docs/ui-and-layout/hub-design-board/index.html`, exported with `node docs/ui-and-layout/hub-design-board/export-design-board.mjs` and checked with `node docs/ui-and-layout/hub-design-board/validate-design-board.mjs`, including the structure-review checklist and coverage matrix.

`ui_visual_standard_contract` locks the exported visual spec, required design PNG list, reference canvas size, AI manifest coverage, web exporter coverage, documentation links, and the `docs/ui-and-layout/index.md` artifact matrix that maps each design PNG to its page/state and runtime screenshot path.

The same contract locks root page composition. `project_dashboard.slint`, Projects secondary pages, `editor.slint`, `builds.slint`, `assets.slint`, `plugins.slint`, `cloud.slint`, `team.slint`, `learn.slint`, and `settings.slint` must each expose one root page component, keep local code to layout/data/callback forwarding, and compose shared wrappers such as `DashboardToolbar`, `ProjectBrowserResultsPanel`, `EditorActionsPanel`, `BuildTaskHistoryPanel`, `CatalogPage`, `CloudServicesPanel`, `TeamMembersPanel`, `SettingsToolchainPanel`, and `OperationTimelinePanel`. New visual helpers belong in page component modules or shared token/component layers, not in root pages.

The final Hub visual audit also keeps raw page colors centralized: `theme.slint` and `tokens.slint` are the only UI files allowed to define direct color literals, and page/shell components route status colors through `HubVisualSpec.success-stroke`, `accent-stroke`, `warning-stroke`, `error-stroke`, and outline tokens instead of private `rgb()` branches.

The 2026-05-29 takeover capture set at `target/hub-visual-check-final/` validates the same `1568x1003` runtime canvas against the new visual standard. Root captures cover `hub-projects-dashboard.png`, `hub-projects-new-project.png`, `hub-projects-browser.png`, `hub-projects-detail.png`, `hub-projects-detail-delete-confirm.png`, `hub-projects-browser-filter-menu.png`, and `hub-projects-browser-sort-menu.png`; `target/hub-visual-check-final/main-pages/` covers `hub-editor.png`, `hub-builds.png`, `hub-assets.png`, `hub-plugins.png`, `hub-cloud.png`, `hub-team.png`, `hub-learn.png`, and `hub-settings.png`; `target/hub-visual-check-final/popups/` covers `hub-source-engine-popup.png` and `hub-user-menu.png`; `target/hub-visual-check-final/states/` covers `hub-state-empty.png`, `hub-state-loading.png`, and `hub-state-error.png`. The final screenshot check verified every PNG in this set is `1568x1003` and non-empty; `hub-learn.stderr.log` contains only repeated ICU4X segmentation-model warnings and did not block image output.

The 2026-05-29 responsive acceptance capture set at `target/hub-visual-check-responsive-0529/` extends that evidence to the planned `1600x1024`, `1280x900`, and `1024x720` breakpoints. Each breakpoint folder contains Projects Dashboard/New/Browser/Detail, browser filter/sort menus, and delete-confirm captures at the root, plus main navigation and top-header popup screenshots under `main-pages/`. The project-page script now seeds `recent_projects.path` and `project_metadata` keys with the real isolated project directories under `project-pages-config`, so guarded delete captures validate the actual confirmation state instead of a missing-path error. The final responsive check verified all required PNGs match their breakpoint dimensions, are non-empty, and have a visible Detail-to-delete-confirm delta; it also verified every seeded project path exists. `hub-learn.stderr.log` again contains only the known repeated ICU4X segmentation-model warning.

Manual visual acceptance remains stricter than file existence. A Hub runtime capture is accepted only when it preserves component density against the matching design PNG, has no overlapping UI, no clipped text or controls, consistent button and badge states, stable panel hierarchy, and the shared empty/loading/error styling. `ui_visual_standard_contract` keeps this checklist present in the visual docs so future capture updates cannot reduce acceptance to dimensions and non-empty PNG checks.

The global-state design PNGs (`hub-state-empty.png`, `hub-state-loading.png`, and `hub-state-error.png`) are HTML/CSS-finalized baselines and now have matching `1568x1003` runtime captures in `target/hub-visual-check-final/states/`. The empty capture uses an isolated Hub profile with no recent projects and validates the shared `EmptyStateBlock`/`EmptyStatePanel` treatment. The loading and error captures use an isolated selected project on the Builds page: loading captures `TaskStatus::running_operation` while a background source build is still running, and error captures the recoverable Source Engine missing-directory path through `TaskStatus::error`/`StatusBanner`. They remain in the design matrix so page implementations can compare state visuals without inventing page-local empty, loading, or error styling.

The Hub now imports the local Slint Material UI template directly. `zircon_hub/build.rs` registers `dev/material-rust-template/material-1.0/material.slint` as the Slint library path `@material`, and `material_bridge.slint` re-exports the template's layout, card, button, input, navigation, list, progress, tab, tooltip, and style primitives for Hub pages that need the real template components. `theme.slint` installs a Zircon teal/dark `MaterialPalette.schemes` override at UI initialization time, so Material-owned controls keep the template behavior without inheriting the template's default blue palette. This is a direct template dependency, not a hand-written Material clone and not an HTML/CSS engine.

Hub surfaces keep the Zircon dark tool UI contract, but they are now backed by real Material template cards where the template provides the matching primitive. `HubPanel` selects `ElevatedCard` for elevated variants and `OutlinedCard` for standard panel variants, then hosts Hub-owned border, tint, and children content so existing page APIs continue to work while the base surface comes from the imported template. Panel wrappers do not add direct pointer layers above children; nested list rows, buttons, inputs, and detail affordances own their interaction through Material controls, `ListTile`, or `StateLayerArea`.

Shared Hub button APIs also delegate to the imported Material template instead of reimplementing pointer behavior locally. `PillButton` wraps `FilledButton` for primary actions and `TonalButton` for secondary actions, while Hub `IconButton` wraps `FilledIconButton` for active icon actions and `OutlineIconButton` for inactive icon actions. `WindowButton` wraps the template `IconButton` with the compact inline icon metric and the template error color path for the close action, so title-bar minimize/maximize/close controls use the same centered icon layout as other Material icon controls instead of a custom `TouchArea`. `PillButton` owns a stable atom-level preferred width and clip boundary so Material text buttons cannot overpaint adjacent responsive header actions at compact window sizes; Hub `IconButton` clips the Material icon button to the requested atom size for dense rows. All three shared button wrappers now expose explicit `fallback-icon-image` inputs, token-derived focus radii, Hub focus overlays, shared disabled opacity, and enabled-gated callbacks. The Hub-facing properties remain stable (`text`, `icon-image`, `fallback-icon-image`, `has-icon-image`, `primary`, `active`, `enabled`, `focused`, and `clicked`), so existing pages keep semantic button usage while hover, disabled, ripple, accessibility, and icon button behavior come from the direct Material template import.

Hub text input fields now follow the same compatibility-wrapper pattern. `HubTextField` lives in `inputs.slint`, exposes Hub-facing `label`, `placeholder`, `supporting-text`, `text`, `enabled`, derived `focused`, and edit/accept callbacks, and renders the imported Material template `TextField` internally. The wrapper owns the Hub disabled opacity and tokenized focus-ring overlay (`HubVisualSpec.focus-ring-width` / `focus-ring-color`) while the Material field owns text editing, label, placeholder, supporting text, and icon layout. Settings uses this wrapper for build jobs and routes toolchain/default-path rows through exported Settings-specific wrappers in `settings_page_components.slint`; Editor uses it for active engine name, source checkout, and staged output fields; Projects New Project routes project name and the editable project creation location through `ProjectCreateSettingsPanel`, which owns the exported `ProjectCreateField` rows from `project_page_components.slint`, so both fields share the same `HubTextField` binding shape while the location row alone enables the Browse button and forwards `browse-folder("new-project-location")`. Projects New Project and Project Detail also route registered Source Engine choices through exported `ProjectEngineChoiceList`; the wrapper owns the shared `PanelListViewport`, `EngineChoiceRow`, empty-state block, scroll binding, and selected-engine callback forwarding, while each page or enclosing panel supplies its own list height, row metrics, empty-state copy, compact-label rule, and target selection callback. New Project template choices route through exported `ProjectTemplateRailPanel`; the wrapper inherits `PanelSlot`, owns the template rail `PanelHeader`, Material `PanelListViewport`, repeated `TemplateChoiceRow` delegates, scroll binding, and selected-template callback forwarding, while `ProjectNewPage` keeps only the rail's responsive basis/grow/min-width/height, list height, row metrics, localized title/availability copy, projected template data, and compact-label rule. `TemplateChoiceRow` now renders the selected template through the shared `HubCheckBox` primitive plus `StateLayerArea`, `MaterialText`, and `StatusBadge`, so template selection exercises the same Material checkbox atom as future binary rows without reintroducing page-local pointer handling. Project Detail's project information list now routes through exported `ProjectDetailInfoSection` from `project_detail_components.slint`, which owns the shared `PanelHeader` plus the five `ProjectSettingSummaryRow` bindings for status, root path, Source Engine, engine version, and last modified while the page supplies only section height, row metrics, compact subtitle state, projected detail data, and localized copy. Project Detail action buttons now route through exported `ProjectDetailActionButton` from `project_detail_components.slint`, which centralizes the Material-backed `PillButton` height, stretch, SVG icon binding, and icon-presence flag for confirm delete, cancel, open, pin/unpin, remove from Hub, and delete while leaving each call site responsible for visible copy, enablement, primary emphasis, and the existing project callbacks. That New Project location is stored as `HubRuntime::new_project_location`, projected through `HubSnapshot`, and bound to `HubWindow.new-project-location`; it intentionally does not share the Settings `project-location` property that persists `HubSettings.default_project_dir`, so selecting a template, selecting an engine, or refreshing a snapshot cannot overwrite the address currently being edited on the New Project page. The New Project create action and Create Summary badge now require a non-empty project name, a non-empty project location, an enabled template, and a selected Source Engine before showing the ready state; the runtime still performs the authoritative trimmed-name/path validation before launching the Editor create command. Hub editable form pages should not import `std-widgets` `LineEdit` directly, while `HubTokens.input-field` owns the Material field atom height and `HubTokens.input-width` gives Material text fields a stable preferred width before row stretch is available. Toolbar search remains exposed as the Hub-facing `SearchBox` API, but it intentionally uses the reference Hub outlined shell plus a compact `TextInput` and search SVG so dashboard/browser toolbars match the `hub.png` density instead of returning to the earlier Material capsule/search-field branch.

Projects New Project's settings panel now routes through exported `ProjectCreateSettingsPanel` from `project_page_components.slint`. The typed panel inherits `PanelSlot` and owns the settings `PanelHeader`, project-name and location `ProjectCreateField` rows, the Source Engine label plus `ProjectEngineChoiceList`, the `ProjectCreateActionRow`, and the desktop create summary. The compact stacked summary routes through `ProjectCreateCompactSummaryPanel`, which owns the separate compact panel shell and delegates visible rows to `ProjectCreateSummary`. The page supplies only responsive basis/grow/min-width/height mirrors, form-ready state, localized `UiTextData`, row/list/summary metrics, scroll bindings, project data, and callback forwarding. The same component module owns `ProjectTemplateRailPanel`, so the template rail is no longer an inline `PanelSlot`/`PanelHeader`/`PanelListViewport` block inside `project_new_page.slint`, and `ProjectNewPage` no longer directly imports or calls `PanelSlot`. `ui_project_layout_contract.rs`, `ui_inputs_contract.rs`, `ui_panel_slot_contract.rs`, `ui_material_usage_contract.rs`, `ui_page_surface_coverage_contract.rs`, `ui_project_navigation_contract.rs`, and `ui_typography_contract.rs` lock these rows and panel boundaries so the form panel does not return to an inline `HorizontalLayout`/`PillButton` action cluster, the Source Engine list does not return to page-local scroll wiring, the compact summary does not return to a direct page `PanelSlot`, and the template rail does not return to a page-local list viewport. The 2026-05-28 `hub-docs-contracts` refresh also locks the Rust-side create workflow in `project_workflow_contract.rs`: the selected `new_project_engine_id` must be validated, written as the active Source Engine, stored in Hub project metadata with the selected template, and only then passed to `remember_project` so the next Slint snapshot sees selected-project, Source Engine, Builds, Cloud/package, Assets, Plugins, Learn, and Team scope in one refresh.

Project Detail's header metadata strip now routes through exported `ProjectDetailStatusStrip` from `project_detail_components.slint`. The wrapper owns the version badge, pinned/not-pinned badge, modified-time `MaterialText`, and status-row spacing, while `ProjectDetailPage` only supplies row metrics, badge widths, projected detail data, and localized copy. `ui_project_layout_contract.rs`, `ui_panel_slot_contract.rs`, `ui_material_usage_contract.rs`, and `ui_typography_contract.rs` lock this status-strip shape so the version/pin/modified metadata does not return to loose page-body bindings.

Project Detail's Change Source Engine block now routes through exported `ProjectDetailEngineSection` from `project_detail_components.slint`. The wrapper owns the local `PanelHeader`, current bound-engine subtitle, shared `ProjectEngineChoiceList`, scroll binding, row metrics, empty-state copy, and selected-engine callback forwarding, while the actions panel only decides whether the section is visible outside pending delete. `ui_project_layout_contract.rs`, `ui_panel_slot_contract.rs`, `ui_material_usage_contract.rs`, and `ui_typography_contract.rs` lock this section so the actions panel does not re-expand the engine-change list as a loose `VerticalLayout`.

The Projects secondary-page wrapper components now live outside the page roots instead of at the top of `project_pages.slint`. `project_page_components.slint` exports shared secondary-page components (`PageHeader`, `ProjectCreateSettingsPanel`, `ProjectCreateCompactSummaryPanel`, `ProjectCreateField`, `ProjectCreateActionRow`, `ProjectEngineChoiceList`, `ProjectTemplateRailPanel`, engine/template rows, and summary rows), `project_detail_components.slint` owns Detail-specific action/status/info/engine-section wrappers, and `project_browser_components.slint` owns Browser-specific `ProjectFilterSelect`, `ProjectSortSelect`, and `ProjectBrowserRow` while remaining re-exported through `project_page_components.slint` for the existing browser import surface. `project_new_page.slint` owns the New Project page shell, typed settings/compact-summary/template panel call sites, metric calculations, scroll bindings, data forwarding, and callback forwarding; `ProjectCreateSettingsPanel` owns the settings panel's `PanelSlot` body and create-form internals, `ProjectCreateCompactSummaryPanel` owns the compact summary `PanelSlot` body, and `ProjectTemplateRailPanel` owns the template rail's `PanelSlot` body, `PanelHeader`, Material `PanelListViewport`, and row repetition. `project_browser_page.slint` owns the Browser page shell, toolbar, explicit list-panel call site, and inner `browser-scroll-y` forwarding; `project_detail_page.slint` owns the Detail page shell, detail/action `PanelSlot` columns, cover section, guarded delete confirmation, and Detail wrapper call sites. `project_pages.slint` is now only the stable secondary-page aggregate that re-exports `ProjectNewPage`, `ProjectBrowserPage`, and `ProjectDetailPage` so `projects.slint` keeps one import source. The project layout, inputs, Material usage, navigation, page-surface, panel-slot, and typography contracts check both sides of that boundary: shared component definitions stay in `project_page_components.slint`, Browser definitions stay in `project_browser_components.slint`, Detail definitions stay in `project_detail_components.slint`, New page call sites stay in `project_new_page.slint`, Browser page call sites stay in `project_browser_page.slint`, Detail call sites stay in `project_detail_page.slint`, and `project_pages.slint` stays implementation-free.

Hub segmented controls also use the imported template now. The Hub-facing `SegmentButton` wrapper keeps the existing `text`, `active`, and `clicked` API for toolbar and compact Hub tool surfaces, but its internal state is a one-item Material `SegmentedButton` rather than a hand-painted rectangle plus `TouchArea`. The wrapper uses the Material component's 40px minimum control height through `HubTokens.control-md`, so future segmented rows do not clip the template's state layer or selected check affordance. Settings build-profile and language choices now use the `HubComboBox` menu wrapper instead, keeping those configuration choices on the same Material `DropDownMenu` path as other menu-backed selectors.

Toolbar dropdowns now follow the same rule. `ToolbarSelect` keeps the Hub-facing label, icon, legacy option id, and callback contract, but its trigger is the imported Material `OutlineButton` and its popup list is the imported Material `PopupMenu` fed by `MenuItem` data. Projects dashboard and browser pages pass both Material `menu-items` and the existing id-bearing `options`, so the visual/menu layer comes from the local template while filter and sort actions still dispatch the stable Hub ids.

The generic Hub `DropDownButton` wrapper is also backed by Material buttons. It now renders an imported `OutlineButton` for inactive dropdown triggers and `TonalButton` for active triggers, using the existing Hub `text`, `icon-image`, `has-icon-image`, `active`, and `clicked` properties. The top Source Engine selector still owns its custom `PopupWindow` engine list because that list is not a simple `MenuItem` projection, but the trigger itself no longer paints local borders/backgrounds or owns a separate pointer `TouchArea`.

Collapsed Hub navigation now delegates to the imported Material template `NavigationRail`. Rust still projects the stable Hub `NavItemData` model for page ids, titles, and active state, and `view_model.rs` derives a parallel `NavigationItem` model from the same data for the Material rail. `NavRail` uses that Material model only in collapsed mode and dispatches clicks back through the original Hub page ids by index, so page routing stays unchanged. Expanded navigation remains the Hub-owned left-aligned `NavButton` row list because the template `NavigationRail` is an 80px icon rail and the template `NavigationDrawer` has a wider drawer contract than the Hub's 200px sidebar; each expanded `NavButton` keeps the Hub row shell, tokenized active/focus border, disabled opacity, Material `StateLayerArea` interaction body, icon slot, and MaterialText label. The sidebar collapse control delegates hover, press, and click state to Material `StateLayerArea`, leaving the shell's direct `TouchArea` usage for the frameless title-bar drag region only. Both manual collapse and compact auto-collapse feed the same `nav-effective-collapsed` state for rail width, padding, and `NavRail` mode, so compact windows do not keep expanded-sidebar padding around the 80px Material rail. `HubTokens.nav-width-collapsed-*` therefore uses the Material `size_80` rail width instead of the old 64/72px custom icon strip.

`components.slint` is intentionally only a public entrypoint. Existing pages can continue importing from it, but implementation belongs in the category files below:

- `tokens.slint` owns spacing, breakpoints, page padding, panel gaps, toolbar gaps, control heights, icon sizes, list/table row heights, workspace panel row heights, borders, panel minimums, and shared dark Hub colors. The current shared scale is fixed at spacing `0/4/8/12/16/24/28`, panel gap `14px`, toolbar gap `10px`, icon sizes `16/18/20/28/34px`, control heights `34/38/42px`, table rows `28px`, and list rows `56/72/86px`; those values are expressed through `MaterialStyleMetrics` and `HubTokens` rather than raw page literals.
- `material_bridge.slint` owns no Hub styling. It is the only Hub UI file allowed to import `@material` directly, then re-exports the local Material template components that Hub wrappers are allowed to consume through `components.slint` or the bridge itself. Conflicting public names such as Hub `Badge` stay under Hub ownership; badge labels can still use bridged Material typography primitives such as `MaterialText`.
- `layout.slint` owns CSS-like composition primitives: `Stack`, `Row`, `Column`, `Flow`, `Page`, `PanelGrid`, `ResponsiveSlot`, `ResponsiveState`, `Fill`, `Divider`, `PageScrollSurface`, `ResponsivePanelFlow`, and `WorkspacePanelSection`. `Page` uses the Material template `ScrollView`, exposes padded `content-width`, usable `viewport-height`, and padded `content-height`, and delegates `Divider` to Material `HorizontalDivider`/`VerticalDivider`; its default padding is compact-aware through `HubTokens.page-padding-compact` so small routed pages do not need to override page chrome just to reduce edge gutters. `ResponsiveState` centralizes viewport breakpoint booleans (`compact`, `medium`, `wide`, and `short`) so app chrome and page shells consume semantic state instead of scattering `root.width < ...` checks. `Fill` is the shell/page-slot fill primitive and owns the internal child `VerticalLayout`, including the selected-page slot in `app.slint`, so page instances stretch into the allocated area instead of repeating `width: parent.width` and `height: parent.height`. `PanelGrid` and `WorkspacePanelSection` now expose token-backed padding slots for rare container inset needs while remaining geometry-only; they must not absorb panel chrome, data rows, buttons, typography, or page-specific business presentation. Content width is still constrained by the Hub shell so the page body only sees the viewport left after the navigation rail.
- `theme.slint` owns the Hub-level Material palette override. It is installed by `HubWindow` through a zero-size `ZirconMaterialTheme` component so every imported Material control uses the same Zircon teal/dark scheme.
- `shared.slint` owns shared DTO structs plus compatibility primitives such as `PillButton`, Hub `IconButton`, header `StatusPill`, the expanded-navigation `NavButton`, `FieldLabel`, and `MutedText`. Those wrappers preserve the Hub API and route their internals through Material `FilledButton`, `OutlineButton`, `FilledIconButton`, `OutlineIconButton`, `ActionChip`, `StateLayerArea`, and `MaterialText` where the local template owns the atom behavior, text metrics, interaction surface, and accessibility surface.
- `surfaces.slint` owns panel/card/header/badge/banner surfaces. `HubPanel` is the compatibility wrapper around Material `ElevatedCard` and `OutlinedCard`; Hub-specific variants only add the Zircon border/tint layer above that template surface. `PanelSlot` inherits `ResponsiveSlot` and centralizes the standard workspace-panel shell: Material-backed `HubPanel`, padded stretch body, and child slot forwarding. Page use sites keep the semantic basis/grow/shrink/order inputs next to the matching Slint `flex-basis`/`flex-grow`/`flex-shrink`/`flex-order` properties because Slint 1.16.1 only accepts those flex properties on direct `FlexboxLayout` children. `OverviewPanel` centralizes the top summary-card shell used by Cloud and Team, reusing `HubPanel` plus `PanelHeader` instead of repeating page-local padding/layout boilerplate. `EmptyStateBlock` owns the Material `MaterialText`/`MutedText` body for empty states, and `EmptyStatePanel` wraps that block in `HubPanel` for dashboard and typed-list surfaces, so page files pass title/detail/extra-detail and padding instead of repeating `HubPanel`, `VerticalLayout`, `MaterialText`, and `MutedText` blocks. `PanelHeader` keeps the Hub right-action API, but the action itself is an imported Material `OutlineButton`, and its title typography now goes through Material `MaterialText`. `StatusBanner` titles use the same `MaterialText` path. `Badge` and `StatusBadge` keep the Hub tone shell and public name while delegating label typography and alignment to Material `MaterialText`.
- `inputs.slint` owns search, select, dropdown, segmented, checkbox, switch, toggle, and combo controls, the Hub-outlined `SearchBox` wrapper with compact `TextInput`, the Material `TextField`-backed `HubTextField` wrapper, the Material `SegmentedButton`-backed Hub `SegmentButton` wrapper, the Material `OutlineButton`/shared `HubPopupMenu`-backed Hub `ToolbarSelect` wrapper, the Material `OutlineButton`/`TonalButton`-backed Hub `DropDownButton` wrapper, and the shared `HubDropDownSurface`-backed `HubComboBox` wrapper. `HubComboBox` exposes `current-index` over the imported template-backed overlay wrapper, while page-specific adapters such as `SettingsComboChoice` can drive that index after initialization when persisted values need an initial visible selection.

The `hub-ui-polish-foundations-inputs-navigation` child tightened that primitive-state boundary. `PillButton`, Hub `IconButton`, `WindowButton`, `DropDownButton`, `SegmentButton`, `SearchBox`, `HubTextField`, and `ToolbarSelect` all expose semantic enabled/focused/active state at the Hub wrapper layer while hover, pressed, ripple, text, and icon behavior stays inside the imported Material controls or `StateLayerArea`. Focus rings are intentionally overlaid by the Hub wrapper because the local Material template does not expose one uniform focus-ring API across filled, tonal, outlined, icon, segmented, text-field, and title-bar button variants; those overlays use `HubVisualSpec.focus-ring-width` and `HubVisualSpec.focus-ring-color` rather than page-local colors. The `hub-ui-polish-foundations-inputs-navigation-button-states` child locks the shared button half of that boundary in `shared.slint`: `PillButton`, Hub `IconButton`, and `WindowButton` all gate `clicked` callbacks when disabled, use `HubVisualSpec.disabled-opacity`, derive focus radius from Material/token metrics, and expose a real fallback image property so missing caller icons produce a visible alert or close glyph instead of a blank control. Hub `IconButton` delegates active/inactive icon controls to Material `FilledIconButton` and `OutlineIconButton`, and `WindowButton` keeps the title-bar controls on Material `IconButton` while honoring the same enabled/focus state contract. `ui_inputs_contract.rs` and `ui_foundation_contract.rs` are the focused acceptance gates for this shared-button primitive slice. `NavRail` keeps expanded navigation on `NavButton` rows and collapsed navigation on Material `NavigationRail`; the collapsed rail receives an adapter model derived from the same Rust `NavItemData` projection, and the wrapper gates disabled collapsed interactions by presenting an empty Material item model while expanded rows forward `enabled` into each `NavButton`.

The `hub-ui-polish-foundations-inputs-navigation-input-states` child keeps the input half of that boundary in `inputs.slint`. `SearchBox` now derives its shell background, border, and placeholder tone from private state properties instead of page-level colors: disabled fields use the shared Material disabled alpha, focused fields raise to `surface_container_high` with the Hub focus ring, non-empty fields keep the stronger outline, and empty placeholders remain visible even while the inner `TextInput` is focused. `HubTextField` keeps Material `TextField` as the editing atom but centralizes its Hub focus-border and disabled-opacity values as wrapper state. `ToolbarSelect` now has explicit `menu-ready`, `select-background`, `select-border`, and `select-foreground` state so enabled, disabled, populated, empty, focused, and compact toolbar menu triggers use one token-driven visual contract; the transparent Material `OutlineButton` remains the trigger while the visible Hub shell owns the independent chevron, foreground, and `StateLayerArea` menu affordance. `DropDownButton` and `SegmentButton` now expose a shared `button-height` density input and derive focus-ring radius from that height; `SegmentButton` continues to gate click callbacks at the wrapper layer because the current imported template segmented control does not expose an `enabled` property, and its disabled `index_changed` branch immediately restores `selected-index` from the wrapper `active` model so disabled pointer interaction cannot leave a toggled Material visual state behind. `ui_inputs_contract.rs` locks these private state helpers, tokenized density hooks, disabled SegmentButton reset behavior, and Material-backed control boundaries so downstream button and navigation milestones can depend on a stable input primitive API.
- `data_display.slint` owns shared data rows plus generic list/tree primitives, while `table_view_components.slint` owns the table shell layer. `InfoRow`, `ActionRow`, and `BuildHistoryRow` now wrap the imported Material `ListTile` for icon/avatar, title/detail text, disabled handling, and state-layer interaction while preserving existing Hub row contracts and trailing badge/chevron affordances. `InfoRow` exposes idle/selected border widths, idle/selected backgrounds, and enabled avatar colors, so popup selector rows can reuse the shared information-row primitive while keeping transparent inactive rows and accent selected rows. `ActionRow` also exposes `show-trailing`, disabled-shell opacity, and enabled/disabled avatar color overrides, so compact popup rows can reuse the same Material action-row primitive without inheriting Dashboard/Quick Actions chevrons. `BuildHistoryRow` is shared by Editor and Builds so recent source-build records use one data-display implementation instead of a page-local Editor row. `OperationTimelineRowData` stays defined in `shared.slint`, while `operation_timeline_components.slint` owns `OperationTimelineRow` and `OperationTimelinePanel`; the row projects action record detail text by preferring recovery guidance, then `log_excerpt`, then command line, then plain detail, so persisted build/process errors surface the most actionable text first. `components.slint` re-exports the timeline module so Builds and Settings render the same persisted action-history panel without importing each other. `MetricCard` is the shared Material-text metric/summary card body used by Cloud readiness metrics and Team identity/repository summaries, replacing page-local `CloudMetric`, `TeamSummaryItem`, and `TeamSummaryCard` implementations. `TableColumnHeader`, `ProjectTableRow`, `HubTableBody`, `DataTable`, and `HubTableView` now live together in `table_view_components.slint`, and the module imports only the generic `PanelListViewport` from `data_display.slint`. `CatalogListPanel`, `PanelListViewport`, and `HubTableBody` use the Material `ScrollView` surface instead of `std-widgets.slint`, so shared table/list scrolling follows the same template path as page-level `PageScrollSurface`. `CatalogPage` now inherits `PageScrollSurface` directly and consumes its `content-height` for the list panel budget, while `CatalogListPanel` derives minimum panel and empty-state heights from the semantic `row-height` parameter. Assets, Plugins, and Learn now inherit `CatalogPage` as their page root instead of wrapping it in a local `Rectangle`, so those pages only set catalog title/badge/empty-state/row semantics and row call sites; the shared page owns geometry, page scroll, and internal list-scroll state. `catalog_page_components.slint` owns the exported catalog row wrappers `AssetRow`, `PluginRow`, and `LearnRow`, keeping per-row `InfoRow` metadata, icons, trailing badges, compact label collapse, and Learn's arrow/open-resource affordance out of the page roots. This keeps catalog pages away from page-local window-height minus padding formulas and row-construction boilerplate while letting taller plugin/learn rows reserve matching space. `MetricCard`, `TableColumnHeader`, `ProjectTableRow`, and the shared `EmptyStateBlock` used by `DataTable` and `CatalogListPanel` now use Material `MaterialText`, keeping proportional metric/column/catalog layout in Hub code while delegating typography metrics to the template. Editor source-engine rows follow the same Material `ListTile` rule while keeping their Hub-specific trailing status and remove controls.
- App chrome implementation lives in focused shell component modules, while `components.slint` remains the public import surface for `HubTopHeader`, `HubNavSidebar`, `HubPageHeader`, and `HubStatusBar`. The former `shell.slint` compatibility note has been deleted so no migration-only shell file remains between callers and the focused owners. Source-engine popup heading/count text, top-header brand and user text, nav current-project forwarding, page-title text, non-Projects task labels, and bottom-status task detail now use Material `MaterialText` typography through those shell component files. Projects page-level Import/New Project actions are gated by `project-actions-visible` from `app.slint`, so only the Dashboard shows them; Projects secondary pages rely on their local `PageHeader` back affordance and page-local actions. `app.slint` now mounts each selected page inside a shared `Fill` slot and gives the page stretch constraints only, keeping page hosting semantic instead of assigning fixed parent width/height on every route.
- `shell_header_components.slint` owns the top-header implementation, frameless-window drag regions, header control-slot centering helper, and `HubTopHeader` orchestration. `HubTopHeader` receives the selected project and active Source Engine context from `app.slint`, showing the selected project in the brand subtitle when available while the imported Source Engine selector stays beside it. `app.slint` forwards all shell chrome metrics from the shared `ResponsiveState`: compact windows use the shorter shell row height, compact shell gaps, compact horizontal padding, and the medium control height for header/page actions. `components.slint` exports `HubTopHeader` from this module, and `ui_shell_composition_contract.rs`, `ui_shell_header_contract.rs`, and `ui_shell_window_contract.rs` check that the top-header body, drag `TouchArea`, responsive metric forwarding, control-slot helper, and popup import remain in the focused component files while the deleted `shell.slint` path stays absent.
- `shell_header_popup_components.slint` owns the top-header Source Engine popup components and user menu popup. `HeaderEngineSelector` keeps the Hub-owned popup/card shell, anchor `x`/`y`, and shared `PanelListViewport`, while `HeaderEngineOption` now inherits shared `InfoRow` for each engine option, preserving the same title/status/last-build text, active row, transparent inactive row, and avatar color bindings without a local `ListTile` row shell. `HeaderUserMenuAction` now inherits shared `ActionRow`, hides its trailing affordance through `show-trailing: false`, and overrides avatar/disabled-shell colors to preserve the compact user-menu look while removing the local `ListTile` row shell from the popup module. The popup module is intentionally imported by `shell_header_components.slint` rather than publicly re-exported through `components.slint`, so the public shell surface stays limited to `HubTopHeader`. The user-menu popup avatar uses token-derived square sizing instead of `root.width` so it cannot expand to the popup width and clip the menu header row.
- `shell_sidebar_components.slint` owns the full left-sidebar implementation. `HubNavSidebar` keeps expanded/collapsed navigation, Material `NavRail` hosting, sidebar collapse `StateLayerArea`, and selected-project/Source Engine state forwarding. `app.slint` derives an effective collapsed state from the user's `nav-collapsed` setting plus `ResponsiveState.compact`, so 1024px-class windows automatically use the Material rail even before the user toggles collapse; wider windows still honor the manual expanded/collapsed preference. `NavStatusPanel` keeps the Material `HubPanel`, selected-project label, engine status badge, update row, token-derived height, and Material text bindings. `components.slint` exports `HubNavSidebar` from this module, and `ui_shell_composition_contract.rs`, `ui_shell_navigation_contract.rs`, and `ui_shell_window_contract.rs` check that sidebar implementation and effective collapsed-state forwarding stay in the focused shell files while the deleted `shell.slint` path stays absent.
- `shell_page_components.slint` owns `HubPageHeader` and `HubStatusBar`. `HubPageHeader` keeps the page title/subtitle band plus Dashboard-only Import/New Project action row, while `HubStatusBar` keeps selected-project and active Source Engine context badges and remains mounted on the Projects dashboard so that selected-project context stays visible in shell chrome instead of being available only through the user popup. Both receive compact state, responsive page-title/action/status heights, compact page padding, and tokenized context widths from `app.slint` instead of deriving responsive layout from their own resolved width. `components.slint` exports these two components from the page chrome module, and `ui_shell_composition_contract.rs` plus `ui_shell_window_contract.rs` check that they stay in this focused owner while the deleted `shell.slint` path stays absent.
- Projects secondary pages also pass `header-visible: false` into `HubPageHeader`, so the outer Projects title/action band belongs only to the Dashboard. New Project, Project Browser, and Project Detail start from their local `PageHeader` back affordance and page-local controls instead of reserving an empty parent header row.
- `navigation.slint` owns Hub navigation composition. Its collapsed branch wraps the imported Material `NavigationRail`; its expanded branch keeps the wider Hub row-style `NavButton` list, with each row body using Hub token geometry plus a Material `StateLayerArea` and `MaterialText` label.
- `data_display.slint` owns reusable rows, metric cards, `CatalogPage`, catalog list panels, tree/list wrappers, and the generic Material-backed panel scroll viewport. `table_view_components.slint` owns table headers, rows, bodies, and table panel shells. Development-only component sample surfaces stay removed; contracts validate real Hub pages and wrappers instead.
- `overlays.slint` owns popup-style reusable surfaces.
- `project_dashboard.slint` owns the Projects dashboard composition: toolbar sizing inputs, card-section inputs, lower `PanelGrid` sizing, and callbacks. `project_dashboard_components.slint` owns the exported dashboard-specific components `ProjectCover`, `ProjectCard`, `ProjectFlow`, `DashboardProjectCardsSection`, `DashboardToolbar`, `DashboardQuickActionRow`, `DashboardRecentProjectsPanel`, and `DashboardQuickActionsPanel`, keeping toolbar `ResponsiveSlot` internals, search/filter/sort controls, grid/list mode buttons, card visuals, card scrolling, empty-card state, Show More button, recent-project `DataTable`, quick-action `PanelListViewport`, empty quick-action state, and repeated quick-action `ActionRow` wiring out of the page root. `ProjectDashboardPage` now inherits `PageScrollSurface` directly for page overflow and inherited content-width/viewport-height sizing, while project card titles use Material `MaterialText` typography and dashboard empty states route through shared `EmptyStatePanel`. The lower Recent Projects and Quick Actions panels are typed `PanelSlot`-backed components, so their basis/grow/min-width semantics stay in the page while the Material-backed panel surface, padded body, table/list bodies, and empty states live in the dashboard component module. Quick Actions route repeated rows through the exported `DashboardQuickActionRow` wrapper; the wrapper owns the `ActionRow` action binding and trigger forwarding, while `DashboardQuickActionsPanel` owns the shared scroll viewport and forwards only the dashboard `quick-action(id)` callback. Project card flow keeps tokenized min/preferred card sizing on Slint built-in layout properties; it deliberately does not pass page-derived `content-width` or card-height values through `ProjectFlow` custom properties into the repeated `ProjectCard` delegate, because Slint 1.16.1 can panic in binding analysis for that cross-component repeater path. Repeated card labels (`modified-label`, `pinned-label`, `missing-label`) are projected into `ProjectCardData` by Rust so repeated card delegates do not bind page-level `UiTextData` through custom child properties, another shape the Slint 1.16.1 Rust generator rejects in debug binding analysis. `project_page_components.slint` owns shared Projects secondary-page building blocks; `PageHeader` and `ProjectSettingSummaryRow` use Material `MaterialText` for visible title and label typography, while `project_browser_components.slint` owns Browser-specific select wrappers and `ProjectBrowserRow` title/meta typography. `project_detail_components.slint` owns the Detail-specific action buttons, status strip, information section, and change-engine section. `project_new_page.slint` owns New Project page flow and its settings/summary/template `PanelSlot` call sites, `project_browser_page.slint` owns Browser page flow and the custom inner `ScrollView` row-width viewport, `project_detail_page.slint` owns Project Detail page flow and its detail/action `PanelSlot` call sites, and `project_pages.slint` now only re-exports those three secondary pages. New Project, Browser, and Detail page roots derive content width from `PageScrollSurface`; New Project and Detail standard two-column panels use `PanelSlot` for the Material-backed surface and padded body while preserving their page-level basis/grow/min-width semantics. Project Detail's pending delete state uses the shared `StatusBanner` at error tone inside the Actions panel flow, so the guarded Recycle Bin confirmation remains part of the Material-backed panel composition instead of becoming a page-local loose text block. `projects.slint` remains a subpage router, inherits the shared `Fill` slot behavior instead of binding each subpage to `parent.width`/`parent.height`, and binds New Project to `new-project-location` while Settings keeps the default-project-directory editor on `project-location`. It also remembers the Project Detail entry subpage, so dashboard card/table detail opens return to the dashboard, while Project Browser row detail opens return to the browser selection panel. Dashboard and Browser no longer share the removed `project-entry-mode`, `project-list-rows`, or `recent-project-rows` staging inputs: Dashboard consumes `dashboard-project-rows`, Browser consumes `project-browser-rows`, and View All Projects routes only through the browser subpage.
- Project secondary-page compact page padding/gap state is forwarded from `HubWindow`'s shared `ResponsiveState`: `app.slint` passes `responsive-state.compact` as `viewport-compact`, `projects.slint` forwards that property to New Project, Project Browser, and Project Detail, and those page roots derive their local `compact-page` value from the forwarded state. This keeps global viewport breakpoints centralized in `layout.slint` while still allowing Projects secondary pages to use compact page padding/gap budgets.
- `app.slint` is now only the public `HubWindow` binding surface plus shell/page routing. It delegates chrome to `HubTopHeader`, `HubNavSidebar`, `HubPageHeader`, and `HubStatusBar` instead of directly drawing header/nav/status layout, consumes `ResponsiveState` for viewport breakpoint state, and forwards the selected project, active Source Engine, compact state, and localized shell copy into the top header, nav sidebar, page header, and status bar.

The component-contract milestone treats root/page `.slint` files as structural entrypoints. `app.slint` may own the `HubWindow` property/callback surface and route selected pages, but it must not grow page-specific helper components. Page roots such as `project_dashboard.slint`, `project_new_page.slint`, `project_browser_page.slint`, `project_detail_page.slint`, `editor.slint`, `builds.slint`, `settings.slint`, `cloud.slint`, `team.slint`, and the catalog pages expose one page component each; helper rows, typed panels, card sections, shell fragments, and list bodies belong in their focused `*_components.slint` owner modules. `project_pages.slint` remains a pure aggregate re-export for the three Projects secondary pages, and the old `shell.slint` migration note remains deleted. `ui_foundation_contract.rs` now mechanically checks those entrypoint and component-module ownership rules in addition to the public `components.slint` thin-entrypoint check.
- `editor.slint`, `builds.slint`, `settings.slint`, `cloud.slint`, and `team.slint` use `WorkspacePanelSection` so workspace panel rows share tokenized heights and compact behavior instead of repeating `flow-height` breakpoint formulas in each page. These workspace pages now derive compact thresholds from `PageScrollSurface.content-width`, inherit `PageScrollSurface` directly as their page roots, and let the shared surface plus stretch layout fill rows instead of assigning `width: root.content-width`. `editor.slint`, `builds.slint`, and `settings.slint` route `WorkspacePanelSection` child sizing through `ResponsiveSlot` semantics plus direct paired Slint flex properties at the child use site; Builds, Settings, and Editor's standard workspace panels use `PanelSlot`, which preserves that `ResponsiveSlot` boundary while moving repeated `HubPanel` and padded body layout into `surfaces.slint`. Settings now routes Toolchain, Build Defaults, Default Paths, and Configuration Health through typed `PanelSlot`-backed components in `settings_page_components.slint`; `settings.slint` keeps only the section sizing, flex mirrors, scroll state, data forwarding, and browse/save callback forwarding at those use sites. Editor keeps one explicit `ResponsiveSlot` for the split source-engine/build-history side column because that slot contains two stacked nested panels rather than one standard panel body. Editor, Builds, and the Settings detail row now give their main slots a larger token basis with grow weight 2 and their side slots a side-panel token basis with grow weight 1, so pages no longer precompute overview/actions/config/detail widths from a remaining-width formula. Settings default-path rows keep a token preferred width and stretch inside `PanelListViewport` instead of deriving row width from the panel's resolved width. `project_dashboard.slint`, `project_new_page.slint`, `project_browser_page.slint`, and `project_detail_page.slint` now inherit `PageScrollSurface` directly; Projects toolbar slots still use `ResponsiveSlot`, while New Project and Detail standard panel columns use `PanelSlot` so their Material-backed surfaces share the same shell as workspace panels. Browser keeps a separate `browser-scroll-y` and explicit list panel for its inner Material list viewport, so list scrolling does not reuse the inherited page-scroll state and row width can follow the visible viewport. Dashboard toolbar selects, lower Recent/Quick panels, token card basis, New/Detail main-side panel bases, and Browser toolbar select bases now avoid width-minus-gap formulas, while dashboard repeat cards use preferred/min widths because Slint 1.16.1 rejects `ResponsiveSlot` wrappers and page-derived sizing properties inside that repeated card path. `builds.slint` consumes selected project detail so build/package rows describe the current project context instead of only the active engine, sizes its summary row token for five Build/Open/Launch/Package/Install action rows, uses `PanelSlot` for the five source/control/pipeline/status-history/operation-timeline panels, and uses Material `MaterialText` for the current-task status headline. `cloud.slint` sets `compact-stack: false`, drives `compact-rows` for wrapped metric cards, routes metric card basis/min-width/grow through tokenized `ResponsiveSlot` inputs, and uses four-column/two-column/one-column Taffy growth instead of page-local content-width-minus-gap division for multi-column metric rows. Shared `CatalogPage`, Cloud service rows, and Team member rows consume `PageScrollSurface.content-height` for their list panel viewport budget rather than recomputing `root.height - page-padding - bottom-status` in page code. `team.slint` keeps member empty states on `EmptyStatePanel`, while the other workspace pages use the default compact stacking behavior.

Cloud and Team top summary cards now share `OverviewPanel` from `surfaces.slint`, while Cloud metric cards and Team summary cards share `MetricCard` from `data_display.slint`. Those pages provide title/status and label/primary/secondary values plus responsive slot sizing instead of owning panel chrome. Cloud and Team lower lists now route through typed `PanelSlot` components: `CloudServicesPanel` owns the Services header, badge, `PanelListViewport`, repeated `CloudServiceRow` bindings, and service empty state in `cloud_page_components.slint`; `TeamMembersPanel` owns the Members header, badge, `PanelListViewport`, repeated `TeamMemberRow` bindings, and no-members empty state in `team_page_components.slint`. The page roots keep only list height, row-height where needed, data/copy forwarding, scroll binding, and compact-label state. A generic `ListPanel` shell was removed because Slint 1.16.1 binding analysis panics when page-authored dynamic row children are forwarded through that reusable child slot.

The UI contract now blocks the specific unsafe dashboard card path as well: `ProjectFlow` must not reintroduce `available-content`, `card-height-token`, or dashboard card-height/basis properties sourced from page geometry and then consumed by the repeated card delegate. Card flow responsiveness stays with tokenized min/preferred widths plus the surrounding `Flow` layout.

The `hub-responsive-baseline` milestone also records the standalone launcher boundary as part of the responsive baseline, not as a separate product decision. `zircon_hub` remains the only root package with the real Slint desktop Hub dependency, `build.rs` compiles `zircon_hub/ui/app.slint` with Slint 1.16.1 experimental flexbox and the local Material template bridge, and the Hub manifest must not depend on `zircon_runtime`, `zircon_editor`, `libloading`, WebView, HTML, or CSS stacks. Editor integration is deliberately process-oriented: `EditorLaunchCommand` builds `--project` and `--create-project` command lines and `launch_editor` spawns `zircon_editor` as a child process. Hub runtime code may orchestrate configs, recent projects, source engines, builds, package/install actions, and child-process status, but it must not register runtime lifecycle modules, dynamically load runtime libraries, or route Hub UI ownership into editor-owned Slint paths. `ui_foundation_contract.rs` now locks that boundary together with the component-system foundation contracts so future responsive work cannot accidentally solve layout issues by moving Hub into an editor/runtime/WebView surface.

## Runtime-State Data Flow

The runtime-state integration split is the UI data contract for selected-project and Source Engine scope: `HubRuntime -> HubSnapshot -> HubScope -> view-model builders -> binding::apply_snapshot() -> Slint DTO props`. Slint pages consume projected DTOs; they may forward `ProjectDetailData`, `QuickActionData`, `SourceEngineData`, `WorkspaceActionReadinessData`, catalog row data, and `ActionRecordData`, but they must not rebuild selected-project, latest-recent, or active-engine fallback rules.

`shared.slint` remains the DTO boundary. It can expose fields such as `has-selected-project`, `selected-project-label`, `source-engine-label`, `can-build`, disabled-reason copy, and row source labels. It cannot introduce callback-local filesystem scanning or selected-project lookup. Page roots and component wrappers forward those fields into shared wrappers such as `DashboardQuickActionsPanel`, `BuildControlsPanel`, `CatalogPage`, `CloudPackageActionsPanel`, `TeamMembersPanel`, `ProjectDetailPage`, and `OperationTimelinePanel`.

Runtime owners for the flow are intentionally narrow. `HubSnapshot::scope()` resolves project and Source Engine state; `source_scoped_views.rs` owns Assets/Plugins/Learn/Team catalog roots; command handlers record success/failure through `record_action_and_persist()`; and restart-visible state lives in `HubConfig.runtime`. Dashboard Quick Actions are the only visible surface that may use latest-recent fallback when no selected project exists. Builds, Project Detail, catalog pages, and source-engine scoped panels consume selected-project or active-engine DTO copy from the snapshot instead of querying config or recent lists directly.

The consolidation gate for this data flow is `hub-runtime-state-integration-contract-docs`. It ties `ui_selected_project_runtime_contract.rs`, `ui_selected_project_catalog_contract.rs`, `project_quick_actions_contract.rs`, `project_workflow_contract.rs`, `project_source_engine_contract.rs`, and `project_path_scope_contract.rs` together so the responsive component system can stay visual/layout-focused while the Rust contracts protect scope ownership.

## Docs/Contracts Refresh Gate

The docs refresh gate treats the component stack as a contract matrix: `tokens -> layout primitives -> surfaces -> inputs/navigation/data display/overlays -> shell and page-specific wrappers -> page roots`. Future edits should update the owner module and its focused contract instead of adding root-page exceptions or another broad scanner. `hub_docs_contract.rs` keeps this document, the Hub index, state foundations, project lifecycle, actionable pages, and Settings status docs aligned with that matrix.

The current static contract split is intentional. `ui_foundation_contract.rs` owns thin entrypoints and component-module ownership, `ui_global_rules_contract.rs` owns no raw text/icons/touch/percent/page-geometry drift, `ui_material_usage_contract.rs` owns Material-template consumption, `ui_taffy_layout_contract.rs` owns Taffy/flex pairing, `ui_workspace_layout_contract.rs` owns workspace section layout, `ui_project_layout_contract.rs` owns Projects layout, `ui_selected_project_runtime_contract.rs` and `ui_selected_project_catalog_contract.rs` own selected-project runtime/catalog scope, `ui_visual_standard_contract.rs` owns visual artifact handoff, and `hub_docs_contract.rs` owns this documentation matrix.

The known Slint 1.16.1 constraints remain part of the design: repeated card delegates avoid page-derived custom geometry, root pages inherit `PageScrollSurface` or `CatalogPage` directly, direct `flex-*` properties stay beside matching semantic slot inputs, and generic dynamic child wrappers are avoided where Slint binding analysis panics. These constraints are documented so later acceptance work can verify screenshots and generated UI without "simplifying" back to page-local layout formulas.

## Layout Contract

Pages should describe semantic regions instead of pixel-placement details: shell, header, nav rail, toolbar, card flow, panel grid, list/table, detail, and action regions. Page-level code may choose `min-width`, `preferred-width`, row-height parameters, and breakpoint tokens, while workspace rows, Projects toolbar items, and Projects secondary-page columns should express Taffy basis/grow/shrink through `ResponsiveSlot` or `PanelSlot` semantic inputs, then mirror those inputs immediately to Slint `flex-*` properties at the direct Flexbox child use site. It should not place business regions with `x`/`y`, nor introduce literal page breakpoints such as `root.width < 980px`.

Workspace-style pages should use `WorkspacePanelSection` for repeated two-panel rows and metric groups that need shared height math. The primitive is still Slint-native `FlexboxLayout`, but it centralizes compact height calculation, exposes `compact-stack` for rows that must wrap instead of stack, exposes `compact-rows` for wrapped metric groups that need more than two rows, and keeps row heights in `HubTokens`. It also exposes `section-padding*` inputs; those insets are included in both compact and regular section-height calculations before being forwarded to the internal `FlexboxLayout`, so a page that needs extra row breathing room does not have to wrap a section in another `Rectangle` or duplicate `FlexboxLayout` padding. Workspace pages derive their semantic content width from the surrounding `PageScrollSurface`, while `WorkspacePanelSection` itself keeps `preferred-width: 0px` so it does not derive preferred width from its own layout result. The migrated workspace page roots inherit `PageScrollSurface` directly and do not rebind a nested page child to `root.width` or `root.height`. This makes `editor`, `builds`, `settings`, `cloud`, and `team` easier to scan and keeps future breakpoint changes in one place. The shell lets Slint/Taffy allocate the page host as the horizontally and vertically stretched sibling after the navigation rail and lets the main body stretch below the top header; `app.slint` must not hand-write `parent.width - root.nav-width` or `root.height - root.shell-header-height` content formulas, because those formulas create layout-info dependency loops when the shell, page header, and routed page all query each other.

Percent sizing is intentionally avoided in Hub-owned Slint files. Shared stretch components such as `InfoRow`, `ActionRow`, `PanelGrid`, and `PanelHeader` use `horizontal-stretch`, `min-width`, parent/content-width inputs, and `preferred-width: 0px` instead of `preferred-width: 100%`, so the component does not ask Slint to derive its preferred size from the same layout result it participates in.

Fixed sizes remain allowed at the atom layer where they carry real interaction meaning: icon sizes, button/control heights, Material text-field height, table/list row heights, 1px borders/dividers, popup anchors, and window drag/resize math. Those values should come from `HubTokens` whenever they are part of the common Hub scale, and the foundation contract locks the token formulas so page files cannot drift back to local `34px`/`38px`/`42px` controls, `14px`/`10px` gaps, or ad hoc list/table row heights.

Visible Hub typography follows the same component contract. Page and component files should use Material `MaterialText` or Hub wrappers such as `FieldLabel` and `MutedText` instead of raw Slint `Text`, `inherits Text`, or direct `font-size`/`font-weight` bindings. Read-only `MaterialTypography.*.font_size` references remain allowed when a layout needs to derive row or section height from the template's text metrics.

Icon-like affordances follow the same rule. Toolbar, row, status, popup, and window controls should use bundled SVG assets or Material icon slots instead of character literals such as `+`, `>`, `v`, `!`, `?`, `[]`, `::`, `==`, or `...` in `text` and `fallback-text` bindings.

Pointer interaction follows the imported Material/template path. Buttons, list rows, cards, toolbars, selectors, and row action zones should use Material button primitives, `ListTile`, `PopupMenu`, or `StateLayerArea`; direct Slint `TouchArea` usage is reserved for the frameless shell drag region in `shell_header_components.slint`.

The current responsive breakpoints are:

- compact: widths below `HubTokens.breakpoint-compact` (`1040px`)
- medium: widths below `HubTokens.breakpoint-medium` (`1280px`)
- wide: widths at or above `HubTokens.breakpoint-wide` (`1536px`)

`layout.slint` now owns both window-wide and content-width responsive predicates. `ResponsiveState` remains the only place that evaluates viewport-level compact/medium/wide/short flags from `root.width` and `root.height`, while the zero-size `ResponsiveCollapse` helper evaluates a caller-provided `content-width` against a tokenized collapse threshold. Catalog-style pages and lower workspace lists use that helper for trailing-label collapse instead of repeating `root.content-width < HubTokens.breakpoint-medium` at each row call site. This keeps label-collapse behavior on the same shared responsive primitive as shell breakpoints while still allowing page-specific flow decisions such as Cloud metric row count or Projects column stacking to stay content-relative.

The shell treats widths below the wide breakpoint as compact for the top title bar. At 1280px, the title bar has enough room for the brand, source-engine selector, user controls, and window buttons, but not enough room for the full task/success/warning/error status-pill group without overlap. `app.slint` therefore passes `compact: true` to `HubTopHeader` below `HubTokens.breakpoint-wide`; the status pills remain visible at the 1600px reference width and are hidden at 1280px and 1024px so the chrome stays aligned while page bodies enter their own responsive layouts.

The shell also treats heights below `HubTokens.breakpoint-short` as compact for the left rail. The threshold is intentionally above the 900px and 768px validation sizes so expanded Material `ListTile` navigation rows have enough vertical room; the lower engine/project status card and collapse action are hidden before they can compress the navigation list into the bottom status bar. When visible, the lower card uses token-derived shell row height to reserve room for both active Source Engine status and current selected-project context.

The page header and bottom status bar treat widths below `HubTokens.breakpoint-medium` as compact, but that breakpoint is evaluated by `ResponsiveState` and passed into the shell components from `app.slint`. At wide sizes `HubPageHeader` places selected-project plus active Source Engine badges beside non-Projects page title actions, while `HubStatusBar` keeps the current task text flexible through Material `MaterialText` and places the same badges after it using `HubTokens.status-badge-width`; at compact sizes those badges are hidden, the page header uses the base shell-header height, the status bar uses the medium control height, and the page title padding switches to `HubTokens.page-padding-compact` so the title band and task detail can elide cleanly instead of overlapping shell chrome. These shell components must not calculate compact state, page padding, action height, status height, or badge width from their own resolved width because those values participate in Slint layout info. `app.slint` also derives `header-tight`, `header-minimal`, compact shell row height, compact shell gap/padding, and automatic compact navigation collapse from `ResponsiveState`; the header no longer instantiates its own `ResponsiveState` or derives breakpoints from its resolved width. It still sizes the Source Engine selector from `nav-width`, header button size, and token clamps instead of dividing `root.width`, so compact chrome width remains a shell input contract rather than page-local geometry math.

Projects secondary pages use content-relative breakpoints rather than a single shell width. `ProjectNewPage` keeps project name, location, Source Engine choice, create action, and summary in the main panel, with templates in a side rail only when inherited `PageScrollSurface.content-width` can hold both columns; both panels use `PanelSlot`/`ResponsiveSlot` semantics as direct children of the page `FlexboxLayout` and mirror those semantics to paired Slint flex properties, and the page root inherits the shared Material-backed `PageScrollSurface`. Its main/side columns use tokenized panel bases and minimums instead of subtracting gaps from `content-width`, and its form flow height consumes inherited `PageScrollSurface.content-height` instead of subtracting page padding from the viewport in page code. At 1024px, that rail wraps below the settings panel and remains reachable through the page scroll. `ProjectBrowserPage` wraps search, filter, and sort controls into a second toolbar row when the content width cannot hold the wide toolbar, with each toolbar control sized through `ResponsiveSlot` and paired Slint flex properties; filter and sort selects use a shared toolbar-select basis/grow path rather than a half-width subtraction, and the browser page uses a separate inner Material `ScrollView` only for list rows with `browser-scroll-y` while the list panel budget comes from inherited `PageScrollSurface.content-height`. `ProjectDetailPage` uses the same content-driven flow: the main cover/info panel remains first, the Actions/Bound Source Engine panel stacks below it at 1024px, both detail columns use tokenized `PanelSlot`/`ResponsiveSlot` bases with paired Slint flex properties, and the detail page inherits `PageScrollSurface` directly so `content-height` drives the detail/action panel budget while short windows scroll instead of clipping project-info rows. Workspace pages use `WorkspacePanelSection` for wrapped summary/metric rows; Builds uses the section's compact height override so its Source Build summary can stay content-sized while the selected-project actions panel keeps the full five-action list height, Editor uses the same semantic row-budget pattern for its active-engine overview plus action row, and Cloud passes a compact row count plus tokenized metric slot basis/min-width/grow for the four-card metric set. Cloud keeps two-column cards on the same minimum card width used by four-column mode and lets Taffy grow them across the row, so one-column, two-column, and wide four-column layouts expand by row token and Taffy growth instead of relying on page-local height, half-width formulas, or a medium-panel basis that would force compact layouts back to one column.

## Validation

Learn now follows the same selected-project snapshot contract as Assets, Plugins, and Team: project selection refreshes selected-project documentation before projection, while Source Engine changes still rebuild the source-scoped documentation set. The Learn row metadata carries Selected Project or Source Engine source labels, and the page title plus empty-state detail switch based on whether a project is selected.

Quick-action fallback is part of the responsive workflow contract. When Package, Install, or Open Editor promotes the latest recent project because no project is selected, the runtime refreshes selected-project scoped Assets, Plugins, and Team data before projecting the next Hub snapshot; if that action activates a different bound Source Engine, it refreshes the full Assets/Learn/Plugins/Team source-scoped set. If `selected_project_path` is set but no longer resolves to a recent project, Quick Action projection and runtime helpers show selected-project-unavailable copy and treat it as no usable project target instead of promoting the newest recent project. Build can use the same selected/newest-recent target, but it is enabled only when that project has a Hub metadata binding to an installed Source Engine. Build target copy distinguishes a project with no binding from a stale binding whose Source Engine record is unavailable. Normal project selection uses the stricter selected-project path: when Hub metadata binds that project to a Source Engine, selection makes the bound engine current and refreshes project/engine scoped views before Builds, Cloud, Assets, Plugins, Team, Learn, or shell context render again. New Project uses the same engine-validity rule: create readiness requires the selected Source Engine id to resolve to an installed engine, runtime creation revalidates the id before activating the engine or launching the Editor, and the default engine tracks the active Source context only when the current default is empty, invalid, or was following the previous active engine. A valid explicit New Project engine choice remains stable across unrelated active-engine switches. Existing-project open only remembers the recent/selected project path; it does not write the current active Source Engine into Hub metadata, so unbound and stale binding states remain visible until New Project creation or an explicit Project Detail Source Engine choice updates them. The opened/created project remember path uses the same bound-engine activation before persisting the new `last_project_path`, so newly created project bindings are current in the next projected snapshot. Startup uses the Editor session's `last_project_path` only when it still matches a merged recent project, then restores `selected_project_path` before the first scoped catalogs are refreshed. Runtime selection, Quick Action target resolution, Quick Action visible target labels, Project Detail projection, remove-from-Hub, pending-delete cleanup, and selected-project scoped refresh change detection all use the shared `project_paths_match` rule, which reuses Hub project metadata key normalization instead of direct `PathBuf` equality and treats Windows drive-letter paths case-insensitively even when those persisted strings are validated on non-Windows hosts. Assets, Plugins, Learn, and Team scanner de-duplication use `project_filesystem_path_key`, which canonicalizes existing roots/manifests and then applies the same metadata-key rule; Source Engine registration now hashes and de-duplicates source checkout paths through that same filesystem key before migrating existing engine metadata. Startup, settings save, Source Engine selection/removal, build startup after registering settings, project actions that activate a bound engine, and Source/output folder browsing all call `source_scoped_views.rs` for the full Assets/Learn/Plugins/Team refresh set, so page data follows one source-scoped refresh contract instead of callback-local four-call chains. Team repository-scope projection falls back to `project_metadata_key` for non-canonical string comparisons. Project card, browser row, and detail projections also use only that per-project metadata for the visible engine/version binding; an unbound project stays localized as `Unbound`/`No Source Engine` even when Hub has another active Source Engine, while a stale stored engine id stays visible as metadata but renders localized `Source Engine unavailable`/`Unavailable` instead of a valid engine version. Project Detail status is localized as `Missing`, `Ready`, or `Invalid` from the same Rust projection before Builds and Projects pages render it. Builds page Build, Open Editor, Package, and Install rows route through selected-project-only callbacks that reuse the same build/package/install helpers without allowing the Dashboard quick-action fallback; Open Editor, Package, and Install stay disabled until `ProjectDetailData` has an actual selected project, while Build consumes the separate row/detail `can_build` flag so an openable but unbound or stale-bound project can still be opened, packaged, or installed without showing Build as available. Runtime also re-checks the valid bound Source Engine before building and refreshes source-scoped page data before projecting the building snapshot, so the Builds workflow cannot silently build or display stale context from the previously active engine. Removing or confirming deletion of the selected project clears those scoped views through the same refresh path; the shared remove helper also clears pending-delete state for the removed path after normalized matching. Confirm Delete waits for the Windows Recycle Bin command to finish successfully before removing Hub recent and project metadata, so a PowerShell/FileSystem deletion failure leaves the Hub entry intact and reports the command error instead of silently removing the project from Hub. Project-scoped actions also activate the project's bound Source Engine when Hub metadata has one, while `ProjectDetailData` remains an explicit no-selection row until `selected_project_path` is set.

Plugins follows that same scoped snapshot rule: selected-project plugin manifests keep Project scope, engine manifests keep Source Engine scope, and the final `PluginData` projection repeats the Project-before-Engine ordering before rows reach Slint. Plugin manifest de-duplication now uses the same canonicalized filesystem path key as selected-project asset roots and Team Git root discovery, so scoped catalog rows keep one project/engine classification even when the same root is reached through an alias.

Learn follows the scoped catalog rule as documentation instead of manifests: selected-project docs are discovered and projected before Source Engine docs, duplicate docs roots are removed through `project_filesystem_path_key`, and Slint receives selected-project empty-state copy plus per-row source metadata.

Builds page project actions are deliberately stricter than Dashboard Quick Actions, and the page subtitle plus visible actions-panel title use selected-project wording so the page context matches those callbacks. The Builds callbacks use selected-project-only runtime entrypoints for Build, Open Editor, Package, and Install, so a disabled or stale Builds row cannot silently promote the newest recent project or build whatever Source Engine happened to be active. Dashboard Quick Actions remain the only place where no-selection fallback to the newest recent project is part of the visible contract, and Build still requires that fallback target to have a valid bound Source Engine before it can run; a stale selected project path suppresses that fallback instead of targeting another project. Runtime errors call out missing bindings and unavailable bound-engine records separately, and the action-history layer persists those package/install selection failures as failed recoverable records instead of only surfacing transient bottom-bar status. Builds source summary and source history follow the same workflow scope by resolving the selected project's bound Source Engine first, using active-engine fallback only when no project is selected, and suppressing fallback for explicitly selected unbound or stale-bound projects; the no-history copy names selected-project or active Source Engine context instead of the old active-engine-only wording. Build history rows now receive a bounded build-log excerpt plus the exact `tools/zircon_build.py` command line from the Rust build runner, while the persistent Hub action history records build/open/package/install outcomes, recovery hints, output directories, and Editor child-process ids. Project create/open launch successes are recorded after recent-project metadata persistence, so the UI will not reload with a successful launch history row when the project state write failed. The compact Builds summary row also keeps a `controls-first` height check so short stacked layouts put selected-project actions before the Source Build summary through paired `order`/`flex-order` values while preserving the same Taffy slot sizing.

Focused companion tests such as `zircon_hub/tests/ui_selected_project_catalog_contract.rs`, `zircon_hub/tests/ui_selected_project_runtime_contract.rs`, `zircon_hub/tests/ui_project_scope_contract.rs`, `zircon_hub/tests/ui_project_navigation_contract.rs`, and `zircon_hub/tests/ui_panel_slot_contract.rs` lock the important structural rules as integration tests so the file scanner does not force the full Slint-generated Hub library into a Rust unit-test harness: the `components.slint` entrypoint must stay thin, the direct `@material` bridge must stay available, `Page` must use Material `ScrollView`, `Divider` must use Material `HorizontalDivider`/`VerticalDivider`, data-display table/list surfaces and Projects dashboard/secondary scroll surfaces must use Material `ScrollView`, data-display table headers, `ProjectTableRow` cells, `CatalogListPanel` empty-state titles, Projects dashboard card/empty titles, Projects secondary-page shared header/summary/browser labels, Projects page section/status labels, Builds current-task status headline, Cloud metric status labels, and Team summary/empty-state titles must use Material `MaterialText`, the Hub window must install `ZirconMaterialTheme`, `HubPanel` must remain backed by Material card surfaces without placing a panel-level `TouchArea` above `@children`, `PanelSlot` must inherit `ResponsiveSlot`, re-export from `components.slint`, and own the standard Material panel body wrapper, `PanelHeader` right actions must stay on Material `OutlineButton`, Hub `Badge`/`StatusBadge` labels plus `PanelHeader`, `StatusBanner`, and shared `FieldLabel`/`MutedText` title wrappers must remain backed by Material `MaterialText` without returning to raw `Text` font bindings, and every Hub UI `.slint` file is globally scanned so raw `Text`, `inherits Text`, or direct `font-size`/`font-weight` bindings cannot return outside those focused component checks. The same global scan blocks character-icon `text`/`fallback-text` literals for icon-like symbols so controls keep using SVG assets or Material icon slots, reserves direct `TouchArea` for only the shell window-drag layer, and forbids percent sizing in width/height/preferred/min/max bindings. Hub `PillButton`/`IconButton` must remain backed by Material button primitives without local `TouchArea` pointer layers, header `StatusPill` must remain backed by Material `ActionChip`, Hub form pages must use the Material-backed `HubTextField` wrapper instead of `LineEdit`, Hub `SearchBox` must keep the reference Hub outlined shell with compact `TextInput` instead of returning to the earlier Material `SearchBar` capsule branch, collapsed Hub navigation must remain backed by Material `NavigationRail`, expanded Hub `NavButton` rows, Hub `InfoRow`, `ActionRow`, shared `BuildHistoryRow`, Editor source-engine rows, and source-engine popup option rows must remain backed by Material `ListTile`, dashboard `ProjectCard`, dashboard `ProjectTableRow`, `ProjectBrowserRow`, and the sidebar collapse control must use Material `StateLayerArea` instead of custom card/row/thumbnail/body/collapse hit targets for hover/press feedback, while `ProjectBrowserRow` keeps row selection and the trailing detail transition in separate tokenized Material `StateLayerArea` zones with a centered SVG detail icon and a fallback branch for captured trailing clicks. Hub `SegmentButton` must remain backed by Material `SegmentedButton`, Hub `ToolbarSelect` must remain backed by Material `OutlineButton` and `PopupMenu`, Hub `DropDownButton` must remain backed by Material `OutlineButton`/`TonalButton`, Projects engine/template choice rows must remain backed by Material `ListTile`, `HubPageHeader` and `HubStatusBar` must receive selected-project and active Source Engine context from `HubWindow` and hide those badges on compact widths, Builds must receive selected project detail and selected-project build-context source history rows, Assets must prioritize selected-project assets and pass selected-project empty-state copy into the page, `ProjectDetailData` must remain an explicit no-selection state until `selected_project_path` resolves, selected-project matching must reuse `project_paths_match` and the metadata-key normalization, Plugins must surface selected-project/Source Engine scope and pass selected-project empty-state copy into the page, Team must pass selected-project empty-state copy into its local Git view, Quick Actions must derive their selected/latest project targeting from `HubSnapshot`, the component modules and tokens must exist, workspace pages must route panel rows through `WorkspacePanelSection`, Editor/Settings/Builds workspace rows plus Projects dashboard and secondary pages must keep direct `flex-basis`/`flex-grow`/`flex-shrink`/`flex-order` paired immediately after the matching semantic `basis`/`grow`/`shrink`/`order` lines, viewport breakpoint comparisons must stay centralized in `layout.slint` `ResponsiveState`, business pages may not reintroduce `x`/`y` layout positioning, the app shell may not return to hand-written remaining-width subtraction, and Hub UI files may not introduce raw pixel literals above `1px` outside the `MaterialStyleMetrics`/`HubTokens` scale. The global-rules companion also blocks nested `page-surface := PageScrollSurface` wrappers and page-local forwarding of `page-surface.content-width`, `page-surface.content-height`, or `page-surface.viewport-height`, so migrated page roots must inherit `PageScrollSurface` or `CatalogPage` directly. The focused companions also assert that the removed `ButtonStates` development surface does not return, `ComponentSamples` stays internal-only for Material/Taffy coverage, and the old `PlaceholderPage` file stays removed so user-facing routes cannot fall back to a reserved placeholder card. `zircon_hub/Cargo.toml` sets the library target's default unit-test harness to `test = false`; Hub package tests still run through integration tests, while normal `cargo check`/`cargo build` continue compiling the Slint UI library and binary. Compile and visual validation still remain required because Slint layout behavior is ultimately checked through generated UI and real Hub screenshots.

The global guardrail subset now lives in `zircon_hub/tests/ui_global_rules_contract.rs`, covering the full-file scans for MaterialText routing, character-icon literals, direct `TouchArea`, percentage sizing, page-local root-width arithmetic, nested page surfaces, scattered viewport breakpoints, business-page `x`/`y`, and raw `px` layout literals. The Material-usage subset lives in `zircon_hub/tests/ui_material_usage_contract.rs`, covering StatusPill ActionChip delegation, source-engine and Projects choice `ListTile` rows, Material `ScrollView` page/list surfaces, dashboard `StateLayerArea` selectors, and the Projects dashboard router. The Taffy layout subset lives in `zircon_hub/tests/ui_taffy_layout_contract.rs`, covering `WorkspacePanelSection`, `ResponsiveSlot`/`PanelSlot` semantic flex pairing, and real Hub surface consumption of the Material/Taffy wrappers. The selected-project catalog subset lives in `zircon_hub/tests/ui_selected_project_catalog_contract.rs`, covering Assets/Plugins/Learn/Team/Cloud scoped page copy and catalog projection ordering. The selected-project runtime subset lives in `zircon_hub/tests/ui_selected_project_runtime_contract.rs`, covering Builds, Quick Actions, project-detail, path matching, startup, package/install, and delete scoped runtime invariants. The remaining project-scope subset lives in `zircon_hub/tests/ui_project_scope_contract.rs`, covering Cloud package manifest scoping plus dashboard card label projection. The Projects navigation subset lives in `zircon_hub/tests/ui_project_navigation_contract.rs`, covering dashboard/import/browser/detail routing and New Project location-state invariants. The data-display subset lives in `zircon_hub/tests/ui_data_display_contract.rs`, covering Material `ScrollView` usage, Dashboard `DataTable` consumption of `HubTableBody`, table/list/catelog empty-state typography, shared `InfoRow`/`ActionRow` `ListTile` backing, and the shared build-history row path used by Editor and Builds. The inputs subset lives in `zircon_hub/tests/ui_inputs_contract.rs`, covering `PillButton`, `IconButton`, `WindowButton`, `HubTextField`, `SearchBox`, `ToolbarSelect`, `DropDownButton`, `SegmentButton`, and the Projects filter/sort select wrappers. The navigation subset lives in `zircon_hub/tests/ui_navigation_contract.rs`, covering expanded `NavButton` Material `ListTile` backing and collapsed `NavRail` Material `NavigationRail` adapter wiring through `app.slint`, `shell_sidebar_components.slint`, binding, and view-model projection. The typography subset lives in `zircon_hub/tests/ui_typography_contract.rs`, covering shared text wrappers, Builds current-task status, Cloud/Team metric and empty-state text, dashboard project/empty titles, and Projects workflow page/component labels. The shell-specific checks live in `zircon_hub/tests/ui_shell_header_contract.rs`, `zircon_hub/tests/ui_shell_composition_contract.rs`, `zircon_hub/tests/ui_shell_navigation_contract.rs`, and `zircon_hub/tests/ui_shell_window_contract.rs`, covering `HubTopHeader`, `HubNavSidebar`, `NavStatusPanel`, shell routing from `app.slint`, sidebar collapse state layers, preferred/minimum window sizing, and the selected-page `Fill` host. The retired `ui_contract.rs` and `ui_shell_contract.rs` entrypoints now stay as discoverability stubs, while focused companion contracts own component-family and workflow checks by responsibility.

The same structural contract now requires Project Detail's pending-delete confirmation to render as a shared `StatusBanner` with error tone and the localized confirm/delete detail copy before the confirm/cancel actions. `StatusBanner` keeps a token-derived row height and zero vertical stretch so wide action columns do not expand the warning into a blank card. While that confirmation is pending, the normal Open, pin, remove-from-Hub, delete, and Change Source Engine controls stay hidden so compact viewports show a complete confirmation cluster instead of clipping the lower action rows. This keeps the destructive-action warning on the same Material typography and panel surface path as other Hub status content.

The same contract now requires Cloud and Team to reuse `MetricCard` for metric/summary cards while keeping their typed row viewport on the shared `PanelListViewport` primitive. It forbids `CloudMetric`, `TeamSummaryItem`, and `TeamSummaryCard` from returning as page-local card components.

The shell context contracts in `zircon_hub/tests/ui_shell_header_contract.rs`, `zircon_hub/tests/ui_shell_composition_contract.rs`, and `zircon_hub/tests/ui_shell_navigation_contract.rs` cover `HubTopHeader`, `NavStatusPanel`, and `HubNavSidebar`: `app.slint` must pass `ProjectDetailData`, `SourceEngineData`, and `UiTextData` into the exported shell components, the top-header brand subtitle must derive from the selected project when one is selected and fall back to `UiTextData.game-engine` in the empty-selection dashboard state, and the expanded lower sidebar status card must live in `NavStatusPanel` inside `shell_sidebar_components.slint`. `HubNavSidebar` now only decides when that status panel is visible and forwards semantic project/source-engine/task state; the panel itself owns the Material `MaterialText`, selected-project-only `Badge`, refresh icon row, and token-derived height so sidebar layout does not accumulate another inline card body or show a synthetic "no project" badge. The Slint `UiTextData.current-project` field name remains stable for those shell components, but Rust localization initializes it as `Selected Project` / `选中项目` so visible chrome follows the selected-project workflow when a project badge is actually present instead of implying a separate current-project concept.

`ToolbarSelect` popup behavior is also part of the static contract: the wrapper root must stay unclipped, the Material `PopupMenu` must use a tokenized minimum width, and its offset anchor must keep the menu visible under compact filter/sort toolbar buttons. The trigger remains a Material `OutlineButton`, while the select chevron is an independent token-sized `Icon` anchored to the right side and vertically centered so filter/sort popup affordances do not drift with label text.

The status-bar contract also forbids deriving compact state or badge width from `HubStatusBar`'s own resolved width, because that creates a Slint layout-info binding loop. The selected-page host contract similarly requires every `app.slint` route to fill the shared `Fill` slot with stretch constraints and forbids returning to per-route `width: parent.width` / `height: parent.height` bindings.

The Projects height contract now treats inherited `PageScrollSurface.content-height` as the source for dashboard lower panel budgets plus New Project, Browser, and Detail row-height ratios. `ProjectDashboardPage`, `ProjectNewPage`, `ProjectBrowserPage`, and `ProjectDetailPage` consume inherited `content-height` directly; dashboard lower-grid fallback height includes `root.content-height / 18` so it remains tied to the shared page surface instead of the surrounding viewport. Projects pages should not return to page-root `root.height / ...` or `root.viewport-height` formulas for those vertical budgets.

The Cloud tab is surfaced as local package/install tooling in navigation copy. Runtime view-model copy, page subtitles, and contract checks forbid account-service wording so responsive shell context does not imply an online Cloud account surface.

The Projects ratio contract keeps proportional toolbar, card, panel, and stacked-detail ratios in `HubTokens` instead of writing numeric `content-width` or `content-height` fractions directly in page files. Dashboard search/select/card/table/lower-panel ratios, Browser toolbar search/select ratios, New Project stack ratios, and Detail cover/badge/main-side/action stack ratios are now named tokens, so page code reads as semantic layout intent instead of a local set of percent formulas. Dashboard project cards now derive their basis from `ProjectFlow.flow-width` plus the `project-dashboard-card-ratio` token, so the row follows its actual Flow container instead of fixed column counts or page-root width arithmetic.

The selected-project workflow contract also covers Builds copy. Build history projection already chooses the selected project's bound Source Engine before falling back to the active engine when no project is selected, and the localized no-history text now says selected project or active Source Engine instead of naming only the active source engine.

The selected-project workflow contract also covers Assets route copy. The catalog already scopes selected-project assets ahead of recent-project and Source Engine asset groups, and the `HubPage::Assets` fallback subtitle plus localized page subtitle now describe selected-project and Source Engine assets rather than a future integration placeholder or vague discovered-assets bucket. The Assets empty-state title and details are also scope-neutral, saying checked project folders plus Source Engine asset roots were empty and explaining selected-project/recent-project/Source Engine roots without returning to active-checkout wording.

Plugins follows the same route-copy rule: selected-project `plugin.toml` manifests are projected before Source Engine plugin manifests, and `HubPage::Plugins` now describes those two scopes directly instead of using generic plugin-discovery/project-extension wording. The Plugins empty-state title and details mirror the same scope-neutral rule by naming checked project folders plus Source Engine plugin roots, or Source Engine plugin roots plus repository fallback roots, instead of only the active source checkout/tree.

The same selected-project workflow contract now includes Cloud: when a project is selected, the Cloud page receives selected-project package/install/output title and detail copy instead of presenting the local readiness panel as a generic cloud account view. The stable `cloud` route now presents as Packages in navigation and localized page-title copy, keeping the user-facing label aligned with the local package workflow while preserving the existing route id. Package and local device-install metrics both read `zircon-package.toml` `source_project` metadata and compare it through `project_paths_match`, so unrelated packages or installs do not satisfy the selected project's readiness state and path spelling differences follow the same metadata-key normalization as the rest of the selected-project workflow. If the build output folder is empty, Cloud keeps the package path and package status at `Not configured` rather than resolving a relative `packages` directory from the current working directory. The visible Cloud metrics describe local mode plus reserved slots rather than account sign-in, bind through `local-mode-status` / `cloud-local-mode` while retaining older account-named fields only as additive aliases, the `HubPage::Cloud` fallback subtitle uses local package/install/service-slot wording instead of account connections, and `CloudServiceRow` no longer keeps a stale `Not connected` tone branch.

Team follows the same workspace-page row budget. Its page root inherits `PageScrollSurface` directly, the identity/repository summary uses `WorkspacePanelSection`, and the typed members panel derives from inherited `PageScrollSurface.content-height` plus a visible-row cap before handing overflow to its internal `PanelListViewport`, so selected-project Git repositories with no members or only a few recent authors do not leave a large blank list area while larger repositories remain scrollable. Its empty member explanation is passed through shared `EmptyStateBlock.detail`, so the local Git empty state still tells the user whether Hub checked the selected project first or Source Engine plus local fallback repositories without reintroducing page-local raw text or a nested panel surface. Its `HubPage::Team` fallback subtitle also stays on local Git identity/recent contributors instead of membership/collaboration settings. Cloud uses the same typed `PanelSlot` pattern for service rows, keeping lower-list headers, badges, and scroll bodies aligned with Team and catalog-style pages without reintroducing the removed generic `ListPanel`.

The 2026-05-24 Cloud/Team PanelSlot visual check captured the manifest-scoped Hub binary at 1280x900 and 960x640. The 1280 captures validate the full lower `PanelSlot` list bodies for Cloud service slots and Team contributors, while the 960 captures validate compact page clipping/scrolling without title, toolbar, or status-bar overlap.

The later 2026-05-24 OverviewPanel check captured Cloud and Team again after their top summary cards moved to the shared surface wrapper. `hub-overview-panel-1280x900-cloud.png`, `hub-overview-panel-1280x900-team-v2.png`, `hub-overview-panel-960x640-cloud.png`, and `hub-overview-panel-960x640-team.png` confirm the top summary cards, metric/summary panels, lower list panels, and bottom status bar remain aligned after the extraction.

The same 2026-05-24 componentization pass added `EmptyStateBlock` and `EmptyStatePanel` under `surfaces.slint`. Projects dashboard no-project content, Team no-members content, `DataTable`, Builds source-build history, and catalog list empty states now route through those shared primitives instead of page-local text/panel blocks. The focused validation for this pass was `cargo fmt -p zircon_hub --check`, direct static runs of `ui_contract.rs` and `ui_panel_slot_contract.rs`, `cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-empty-state-0524 --message-format short --color never`, and `cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-empty-state-0524 --message-format short --color never`.

The 2026-05-24 shell follow-up extracted the expanded navigation status card into local `NavStatusPanel` and added a static contract that keeps `HubNavSidebar` limited to visibility and state forwarding. The same validation exposed a Rust/Slint field-sync gap for `UiTextData.cloud-local-mode`; `localization.rs` now initializes that label and `ui_contract.rs` asserts both the Slint field and Rust initializer. The detailed shell-family checks were then moved into `zircon_hub/tests/ui_shell_contract.rs`, which now runs 4/4 while the broad `ui_contract.rs` runs 49/49 after the split. The focused evidence for this slice was `cargo fmt -p zircon_hub --check`, direct `ui_contract.rs` static execution at `target\hub-material-check\ui_contract_nav_status_panel_0524.exe` -> 53/53, direct `ui_shell_contract.rs` execution at `target\hub-material-check\ui_shell_contract_0524.exe` -> 4/4, direct `ui_contract.rs` execution at `target\hub-material-check\ui_contract_shell_split_0524.exe` -> 49/49, `cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-nav-status-0524 --message-format short --color never`, `cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-nav-status-0524 --message-format short --color never`, and `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-nav-status-0524 --message-format short --color never`.

The 2026-05-25 shell contract split retired `ui_shell_contract.rs` as a stub and moved those assertions into `ui_shell_header_contract.rs`, `ui_shell_composition_contract.rs`, `ui_shell_navigation_contract.rs`, and `ui_shell_window_contract.rs`. The split keeps top-header hit regions, shell composition/context routing, sidebar collapse Material state, and window/page-slot sizing separate while preserving the original static rules; targeted Cargo integration tests for those five shell entries passed against `D:\cargo-targets\zircon-hub-current-visual-0524`.

The later contract modularization moved foundation checks into `zircon_hub/tests/ui_foundation_contract.rs`, global guardrail checks into `zircon_hub/tests/ui_global_rules_contract.rs`, Material-usage checks into `zircon_hub/tests/ui_material_usage_contract.rs`, Taffy layout checks into `zircon_hub/tests/ui_taffy_layout_contract.rs`, selected-project catalog checks into `zircon_hub/tests/ui_selected_project_catalog_contract.rs`, selected-project runtime checks into `zircon_hub/tests/ui_selected_project_runtime_contract.rs`, remaining project-scope checks into `zircon_hub/tests/ui_project_scope_contract.rs`, Projects navigation checks into `zircon_hub/tests/ui_project_navigation_contract.rs`, data-display component-family checks into `zircon_hub/tests/ui_data_display_contract.rs`, input component-family checks into `zircon_hub/tests/ui_inputs_contract.rs`, navigation component-family checks into `zircon_hub/tests/ui_navigation_contract.rs`, and typography component-family checks into `zircon_hub/tests/ui_typography_contract.rs`. The same modularization path split `project_workflow_contract.rs` into `project_path_scope_contract.rs`, `project_source_engine_contract.rs`, `project_page_copy_contract.rs`, and `project_quick_actions_contract.rs`, so path normalization, Source Engine behavior, page-copy assertions, and quick-action fallbacks can evolve independently while the original file remains a stub entrypoint. The latest focused evidence for this split is direct execution of `target\hub-material-check\ui_data_display_contract_0524.exe` -> 6/6, `target\hub-material-check\ui_contract_data_display_split_0524.exe` -> 43/43, `target\hub-material-check\ui_inputs_contract_0524.exe` -> 6/6, `target\hub-material-check\ui_contract_inputs_split_0524.exe` -> 37/37, `target\hub-material-check\ui_navigation_contract_0524.exe` -> 2/2, `target\hub-material-check\ui_contract_navigation_split_0524.exe` -> 35/35, `target\hub-material-check\ui_typography_contract_0524.exe` -> 5/5, `target\hub-material-check\ui_contract_typography_split_0524.exe` -> 30/30, `target\hub-material-check\ui_foundation_contract_0524.exe` -> 5/5, `target\hub-material-check\ui_contract_foundation_split_0524.exe` -> 25/25, `target\hub-material-check\ui_global_rules_contract_0524.exe` -> 9/9, `target\hub-material-check\ui_contract_global_rules_split_0524.exe` -> 16/16, `target\hub-material-check\ui_material_usage_contract_0524.exe` -> 6/6, `target\hub-material-check\ui_taffy_layout_contract_0524.exe` -> 2/2, and `target\hub-material-check\ui_contract_material_taffy_split_0524.exe` -> 8/8, `target\hub-material-check\ui_project_scope_contract_0524.exe` -> 3/3, `target\hub-material-check\ui_project_navigation_contract_0524.exe` -> 5/5, and `target\hub-material-check\ui_contract_retired_0524.exe` -> 0/0, `target\hub-material-check\ui_selected_project_catalog_contract_0524.exe` -> 1/1, `target\hub-material-check\ui_selected_project_runtime_contract_0524.exe` -> 1/1, and `target\hub-material-check\ui_project_scope_contract_split_0524.exe` -> 2/2, plus `cargo fmt -p zircon_hub --check`. This retires the broad contract as a stub while the focused files own specific Material bridge/theme/token/surface, global layout/static guardrails, real Hub Material/Taffy surface coverage, selected-project catalog copy/projection, selected-project runtime action rules, Cloud package/dashboard scope checks, Projects navigation workflow, Material button, form, table/list, navigation, typography, shell, panel-slot, and project workflow contracts.

The 2026-05-25 package-level validation reran the manifest-scoped Hub package against the warmed `D:\cargo-targets\zircon-hub-current-visual-0524` target directory after the contract split. `cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --message-format short --color never` passed across the focused integration files, `cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --message-format short --color never` passed, and `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --target-dir D:\cargo-targets\zircon-hub-current-visual-0524 --message-format short --color never` rebuilt the Hub binary. The Learn selected-project documentation slice passed direct static executions of `ui_selected_project_catalog_contract.rs`, `ui_selected_project_runtime_contract.rs`, and `ui_page_surface_coverage_contract.rs`; locked/offline `cargo check`, focused Cargo integration tests for those three contracts, and `cargo build --bin zircon_hub` all passed under `D:\cargo-targets\zircon-hub-learn-scope-0525`. A full manifest-scoped `cargo test` against that target directory was stopped after the 20-minute command timeout and replaced with the focused Cargo contract run. The only observed contention was waiting for Cargo package/build-directory locks from parallel sessions; no Hub diagnostic failed.

The 2026-05-25 Material bridge hardening records the direct-template import rule in `ui_material_usage_contract.rs` and `ui_foundation_contract.rs`. `zircon_hub/build.rs` must map `@material` to `dev/material-rust-template/material-1.0/material.slint`, `material_bridge.slint` must remain the single direct `@material` import site, and Hub wrappers must compose `MaterialWindow`, `TextField`, `OutlineButton`, `PopupMenu`, `SegmentedButton`, `ScrollView`, and Material dividers through that bridge rather than local stand-ins or page-level template imports. Toolbar search is the exception that keeps the Hub reference outlined `SearchBox`/`TextInput` density while still using the shared Material palette and tokens. `HubWindow` inherits the imported template `MaterialWindow` via `components.slint`; the Hub-specific frameless chrome and responsive shell live in `app.slint` plus the focused `shell_*_components.slint` modules. The 2026-05-28 hard cutover deleted the old `shell.slint` compatibility-note path, and the shell contracts now assert that the retired path stays absent. The same 2026-05-25 slice passed `cargo check`, `cargo test`, and `cargo build --bin zircon_hub` with `--locked --offline --jobs 1` in `D:\cargo-targets\zircon-hub-material-window-0525`.

Team repository status also follows the selected-project context contract at the Rust projection layer. The projection compares the selected project path and discovered Git root through direct component checks, canonical paths, and `project_metadata_key` slash/case keys before labeling the row as `Selected project repository`; when a selected path exists but the discovered Git root is a fallback root, the row says the selected project repository is unavailable before showing the Source Engine repository. Only no-selection fallback roots are labeled as `Source Engine repository`, keeping the visible Source Engine name consistent across shell, Assets, Plugins, Builds, and Team. Git-root discovery itself de-duplicates candidate roots through `project_filesystem_path_key`, sharing the canonicalization path used by Assets and Plugins.

The latest Material-card validation captured Projects dashboard, new-project, project-browser, and project-detail pages at 1280x900 and 1024x768 after `ZirconMaterialTheme` moved Material controls back to the teal/dark Hub palette and Projects engine/template choices moved to Material `ListTile`. The Projects Taffy validation also captured those four pages at 1600x1024, 1280x900, and 1024x768 after dashboard toolbar and Projects secondary-page flex items moved to `ResponsiveSlot`; all three capture runs wrote empty stdout/stderr logs. Later focused captures and static contracts verify the Hub outlined `SearchBox`, NavigationRail, NavButton, StatusPill, Divider, ScrollView, Badge/MaterialText, PanelHeader/StatusBanner title MaterialText, `PanelSlot` surface composition, shared FieldLabel/MutedText MaterialText typography, DataTable/ProjectTableRow MaterialText typography, dashboard ProjectCard/empty-state MaterialText typography, Projects secondary-page PageHeader/summary/browser/status MaterialText typography, Builds current-task status MaterialText typography, Cloud/Team metric and empty-state MaterialText typography, `CatalogListPanel` empty-state MaterialText typography, CatalogListPanel list rendering on Assets/Plugins at 1100x820, shell chrome/page-header/status MaterialText typography, the global raw Text/direct font-binding scan, the character-icon literal scan, the direct TouchArea reservation scan, the percent-sizing scan, the Editor/Builds workspace slot grow scan, ActionRow, InfoRow, source-engine popup, Cloud/Team workspace rows, project selector state layers, status-bar context badges without self-width binding loops, and the Project Browser trailing detail hit zone through real Hub pages. The current asserted captures at `target/hub-visual-check/material-popupmenu-local-nav-1280x900-postbuild-0521` and `target/hub-visual-check/material-popupmenu-local-nav-1024x720-postbuild-0521` were run against the rebuilt Hub binary through the dashboard secondary-page path after `ToolbarSelect` was restored to Material `PopupMenu`; the capture script now uses a harmless focus click before business navigation, retries the Project Detail trailing-button click once inside the same hit zone when Windows frame/client-coordinate drift prevents navigation, and compares each post-click screenshot against the previous page so stale Dashboard/Browser images fail instead of being accepted under the wrong label. The 2026-05-23 Project routing captures at `target/hub-visual-check/hub-projects-continue-0523-latest-1600x1024` and `target/hub-visual-check/hub-projects-continue-0523-latest-1280x900` were produced from an isolated Hub-only locked build at `target/hub-standalone-0523\zircon_hub\Cargo.toml`, because the main workspace `cargo check/build -p zircon_hub --locked` path was blocked by an unrelated `zircon_app/Cargo.toml` workspace lockfile change. Those captures validate dashboard, new-project, project-browser, and project-detail navigation after Projects detail gained a return-subpage route, top-header interactive regions moved to stretch alignment, and the obsolete inline entry/list row properties were removed. The static contract now checks Material/Taffy coverage through bridge exports, shared wrappers, Projects, Editor, Builds, Settings, Cloud four-card metrics, PanelSlot workspace-panel shells, and data-display rows, while also asserting that the removed `ButtonStates` development surface does not return, that internal `ComponentSamples` stays out of user-facing pages, that dashboard `Show More` remains separate from `View All Projects`/list-mode Project Browser navigation, that Import Project remains a picker-backed import action instead of a plain Project Browser route, and that New Project engine choices expose both engine version and source path through the Material ListTile supporting text. The `project_management_contract` integration test keeps the secondary Projects workflow backed by executable behavior checks for metadata persistence, Editor recent isolation, enabled-template gating, recycle-bin command construction, and recent-project filtering, while the focused project workflow companion contracts keep selected-project scanner path normalization shared across Assets, Plugins, Learn, Team, Source Engine registration, source-scoped refresh routing, and project metadata. Earlier captures still cover the 1600x1024 search/filter/sort toolbar row, the 1366x820 detail layout, 1100x760 and 960x640 dashboard layouts, the Material-button-backed dashboard header, the Material text-field wrappers in Settings/Editor/Projects, the Material `PopupMenu` selector surface, the Material `SegmentedButton` wrapper in Settings, and the Material `DropDownButton` trigger plus Hub-owned engine picker.
The current responsive capture set at `target/hub-visual-check/hub-projects-responsive-1600x1024-current` and `target/hub-visual-check/hub-projects-responsive-1280x900-current` validates the Projects Dashboard, New Project, Project Browser, and Project Detail pages after ProjectFlow moved card density to container-driven `flow-width`. Dashboard project cards render as a four-card row at both checked widths, New Project keeps settings primary and templates to the right, Project Browser keeps the relaxed pinned-project list, and Project Detail keeps actions plus bound Source Engine selection visible. Project Detail now labels the Source Engine selector as `Change Source Engine`, while the current binding remains in the header subtitle, so changing engine version is presented as an explicit detail-page operation without adding another fixed-size control. The same detail action panel labels `Remove from Hub` as a non-destructive Hub-entry removal so users can distinguish it from the guarded Recycle Bin delete flow.

The 2026-05-26 Projects toolbar follow-up keeps the Dashboard grid/list segmented affordance aligned with the internal Projects view mode. The grid button remains active for `project-view-mode == "grid"`, and the list button is now active for `project-view-mode == "list"` while still routing to the Project Browser secondary page. `ui_project_navigation_contract.rs` asserts the list trigger owns both the active-state binding and the Project Browser route, so future toolbar edits cannot make the list affordance look inactive while it is the selected mode.

The same follow-up also tightens Dashboard card expansion. `ProjectFlow` now derives its column count from the actual flow width and card basis, computes the row count from `project-card-count`, keeps the collapsed state to one row, and caps expanded visible height to three rows while the inner Material `ScrollView` exposes any remaining projected cards. This preserves the reference dashboard's compact card strip, but prevents the 12-card projection limit from silently clipping cards after only two rows on wide and narrow layouts. `ui_project_layout_contract.rs` locks the column, row, visible-height, content-height, and `card-scroll-y` viewport bindings.

Project Browser now receives the same `viewport-compact` state as New Project and Project Detail. Its secondary-page `page-padding` and `page-gap` switch to compact Hub tokens when the shared responsive state reports a compact viewport, so the relaxed project list keeps proportional spacing on narrower windows instead of retaining desktop gutters. `ProjectsPage` forwards the compact state into all three Projects secondary pages, and `ui_project_layout_contract.rs` locks the Browser-local `compact-page`, compact padding, and compact gap bindings.

Project Browser result rendering now follows the same page/component split as the other Hub panels. `ProjectBrowserResultsPanel` lives in `project_browser_components.slint` and owns the result `HubPanel`, the explicit `HubVisualSpec.panel-radius` surface token, `PanelHeader`, Material-backed `PanelListViewport`, repeated `ProjectBrowserRow` delegates, and centered `EmptyStateBlock` for no matches. `ProjectBrowserPage` keeps only page/header/toolbar layout, result-panel and list-height calculations, `browser-scroll-y`, row metrics, data forwarding, and select/detail callbacks. `ui_project_layout_contract.rs`, `ui_material_usage_contract.rs`, `ui_typography_contract.rs`, and `ui_page_surface_coverage_contract.rs` check that boundary so the Browser page does not reintroduce inline result-list internals.

Project Browser's list-first result panel now uses an explicit `ProjectBrowserTableHeader` plus a row model that shares the same `table-padding-x`, `table-gap`, thumbnail, engine, modified, status, and detail column widths with every `ProjectBrowserRow`. The selected-row background spans the same row width as the header, the trailing detail affordance owns the same detail column slot as the header spacer, and compact viewports hide engine/modified/status columns through one `compact-table` switch instead of letting header and rows collapse independently. `RecentProjectRowData.status` is projected in `projects.rs` from the same localized detail-status helper used by Project Detail, so the Browser status column can render Ready/Missing/Invalid without page-local text branching. `ui_project_browser_table_contract.rs` locks the shared column properties, page header-height reservation, panel pass-throughs, and localized row-status projection so the All Projects browser cannot regress to misaligned header labels, partial selected-row highlights, or page-owned table geometry.

The 2026-05-31 native Browser alignment capture set is `target/hub-visual-check/hub-project-browser-table-0531-v3`. It includes `hub-projects-browser.png`, `hub-projects-detail.png`, and the focused selected-row check `hub-projects-browser-selected.png`, where the Browser table header, selected row fill, status badge column, and trailing detail arrow share the same inner row width. This slice also repaired the overlay wrapper default width from the nonexistent `MaterialStyleMetrics.size_320` to `HubTokens.panel-min-sm - HubTokens.space-6`, allowing `cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1` and `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1` to pass before the real-window capture.

New Project template rail rendering follows the same page/component split. `ProjectTemplateRailPanel` lives in `project_page_components.slint` and owns the template rail's `PanelSlot` shell, `PanelHeader` count badge, shared `PanelListViewport`, repeated `TemplateChoiceRow` delegates, and selected-template callback forwarding. `TemplateChoiceRow` now uses `HubCheckBox` for the selected/unselected affordance, with a row-level `StateLayerArea` forwarding clicks to the same selected-template callback and `StatusBadge` preserving category/soon metadata. `ProjectNewPage` keeps the responsive flex mirrors, list-height calculation, `template-scroll-y`, row metrics, template data, localized title/soon label, and compact trailing-label state. `ui_project_layout_contract.rs`, `ui_panel_slot_contract.rs`, `ui_material_usage_contract.rs`, `ui_input_primitives_contract.rs`, and `ui_page_surface_coverage_contract.rs` check that boundary so the page does not reintroduce an inline template list viewport or raw checkbox/pointer handling.

New Project settings rendering follows the same split. `ProjectCreateSettingsPanel` lives in `project_page_components.slint` and owns the settings `PanelSlot`, header, `ProjectCreateField` rows, Source Engine list, create action row, and desktop summary. `ProjectNewPage` keeps the flex sizing, height calculations, `new-engine-scroll-y`, form-ready state, localized copy, and create/browse/select callback forwarding. `ui_project_layout_contract.rs`, `ui_panel_slot_contract.rs`, `ui_inputs_contract.rs`, `ui_material_usage_contract.rs`, `ui_project_navigation_contract.rs`, and `ui_typography_contract.rs` check the boundary so the page does not reintroduce inline settings-panel body wiring.

The same continuation keeps the non-Projects catalog pages visually aligned. Assets now uses the same relaxed `CatalogPage` row height as Plugins and Learn (`HubTokens.list-row-lg + HubTokens.space-6`), giving long asset paths plus source/size metadata the same breathing room as plugin and learning-resource rows while preserving the shared `InfoRow` and medium-breakpoint trailing-label collapse behavior. `ui_page_surface_coverage_contract.rs` locks the catalog-page row-height alignment.

Catalog page-specific row wrappers now live in `catalog_page_components.slint` instead of at the top of `assets.slint`, `plugins.slint`, and `learn.slint`. `AssetRow`, `PluginRow`, and `LearnRow` are exported from that component module, leaving each catalog page focused on selected-project-aware title/empty-state copy, row count, row height, and the Learn open-resource callback. `ui_data_display_contract.rs`, `ui_page_surface_coverage_contract.rs`, and `ui_selected_project_catalog_contract.rs` check both sides of that boundary.

The 2026-05-28 Hub responsive milestone adds `ResponsiveCollapse` as the shared content-width helper for catalog and lower workspace row labels. Assets, Plugins, Learn, Cloud service rows, and Team member rows now instantiate the helper once per page and pass `label-collapse.collapsed` into their row wrappers. `ui_global_rules_contract.rs` asserts the helper remains in `layout.slint`, and `ui_page_surface_coverage_contract.rs` locks those representative pages to the shared helper so future fixed-layout drift cannot reintroduce scattered label-collapse formulas. Validation for this slice passed focused Hub static contracts, manifest-scoped `cargo check`, full manifest-scoped `cargo test`, manifest-scoped `cargo build --bin zircon_hub`, and a 1024x768 screenshot capture at `target/hub-visual-check/hub-responsive-ui-0528-1024x768.png`.

Cloud and Team page-specific wrappers now follow the same page/component split. `cloud_page_components.slint` exports `CloudMetricSlot`, `CloudPackageActionRow`, `CloudPackageActionsPanel`, `CloudServiceRow`, and `CloudServicesPanel`, and `team_page_components.slint` exports `TeamSummarySlot`, `TeamMemberRow`, and `TeamMembersPanel`; the page roots keep responsive metric/member budgets, `WorkspacePanelSection`, list/action heights, data/copy forwarding, callbacks, and scroll bindings while the component modules own lower `PanelSlot`/`HubListPanelSlot`, `PanelHeader`, shared `ActionRow` construction, `PanelListViewport`, repeated rows, and empty-state wiring. Cloud action rows no longer import `BuildControlAction` from the Builds component module; `CloudPackageActionRow` preserves the same shared `ActionRow` behavior inside the Cloud component module, and `CloudPackageActionsPanel` now inherits `HubListPanelSlot` so package/install actions use the same title/list shell as other fixed action panels. `ui_workspace_layout_contract.rs`, `ui_page_surface_coverage_contract.rs`, `ui_panel_slot_contract.rs`, `ui_data_container_primitives_contract.rs`, and `ui_typography_contract.rs` check the combined page/component surfaces while also preventing those wrapper definitions from returning to `cloud.slint` or `team.slint`.

Team member empty state now uses `EmptyStateBlock` inside `TeamMembersPanel` instead of nesting another `EmptyStatePanel` card inside the list panel. This keeps the workspace page on one visible panel surface while still centering the no-members copy and repository path in the list viewport. `ui_workspace_layout_contract.rs` and `ui_panel_slot_contract.rs` lock the typed panel plus in-panel empty-state block.

Editor's Source Engines and Build History lists now use the same in-panel `EmptyStateBlock` treatment when empty. The page keeps its existing two-list side panel, but the empty rows are token-sized, centered, and Material-text-backed instead of plain muted text floating in the list viewport. `ui_workspace_layout_contract.rs` and `ui_page_surface_coverage_contract.rs` lock this Editor empty-state alignment.

Cloud service slots now use the same in-panel empty-state treatment inside `CloudServicesPanel`. When no service rows are projected, the Services typed panel keeps its computed complete-row height and renders a centered `EmptyStateBlock` inside the list viewport instead of leaving a blank panel body. `ui_workspace_layout_contract.rs` locks the Cloud service empty-state block alongside the existing metric-card and complete-row list budget checks.

The 2026-05-27 M5 workspace-gate follow-up keeps Cloud metrics on `CloudMetricSlot` as the page-local `ResponsiveSlot` wrapper while `MetricCard` remains the only metric card body. `ui_workspace_layout_contract.rs` is responsible for requiring the four wrapped slots and their semantic flex sizing; `ui_typography_contract.rs` now bans only the old `component CloudMetric inherits ...` local card implementation plus the retired Team local card names, so typography extraction no longer conflicts with the layout wrapper requirement. Direct static evidence passed `ui_typography_contract.rs` at 7/7 and `ui_workspace_layout_contract.rs` at 1/1 through `rustc --edition 2021 --test`; the Cargo integration entry for `ui_typography_contract` was stopped after timing out inside full `zircon_hub` Slint library compilation before any test diagnostics.

Builds Build History now follows the same in-panel empty-state rule. Its no-history block uses the larger list-empty token height, Material body padding, and centered content inside the status/history `PanelSlot`, so an empty build history no longer collapses to a single compact row. `ui_workspace_layout_contract.rs` locks the Builds no-history block alongside the existing slot sizing checks.

Settings Configuration Health now follows the same in-panel empty-state rule through `SettingsConfigurationHealthPanel`. `SettingStatusRow` supports compact trailing-label collapse, while the typed panel owns the `PanelHeader`, `PanelListViewport`, status-row repetition, and centered `EmptyStateBlock` with localized `no-configuration-checks` and `configuration-health-empty-detail` copy when no checks are projected. `settings.slint` forwards only the status data/count, compact-label flag, empty-height token, and health scroll binding. `ui_workspace_layout_contract.rs`, `ui_page_surface_coverage_contract.rs`, `ui_panel_slot_contract.rs`, and `ui_typography_contract.rs` lock the Settings empty-state and compact-label behavior.

Settings build-profile and language choices now route through `SettingsBuildDefaultsPanel` in `settings_page_components.slint`. The typed panel owns the `PanelHeader`, both exported `SettingsComboChoice` rows, and the build-jobs `HubTextField`, while each combo row projects its two Hub values into Material `MenuItem` data consumed by the shared `HubComboBox`. `settings.slint` forwards only localized copy through `UiTextData` plus `build-profile`, `build-jobs`, and `language` bindings. `ui_workspace_layout_contract.rs` and `ui_input_primitives_contract.rs` lock the wrapper instances and shared combo consumption so future Settings edits cannot reintroduce duplicate segment-button groups.

Settings toolchain path fields now route through `SettingsToolchainPanel` in `settings_page_components.slint`. The typed panel owns the `PanelHeader` and three exported `SettingsToolchainField` rows; each field inherits `HubTextField` and owns the shared label/placeholder/text binding shape for Python, Cargo, and Rustup executable paths. `settings.slint` forwards only localized copy through `UiTextData` plus `python-path`, `cargo-path`, and `rustup-path` bindings. `ui_inputs_contract.rs` locks the three wrapper instances and the original bindings.

Settings page-specific wrappers now live in `settings_page_components.slint` instead of at the top of `settings.slint`. `SettingsToolchainPanel`, `SettingsBuildDefaultsPanel`, `SettingsDefaultPathsPanel`, and `SettingsConfigurationHealthPanel` are typed `PanelSlot`-backed wrappers, and the same module exports their lower-level `PathSettingRow`, `SettingStatusRow`, `SettingsToolchainField`, `SettingsComboChoice`, and `SettingsSaveActionRow` helpers. This keeps `SettingsPage` focused on workspace-section layout, typed panel sizing, data/scroll forwarding, the save action call site, and existing browse/save callbacks while centralizing repeated `HubTextField`, `HubComboBox`, `PillButton`, `InfoRow`, `PanelHeader`, `PanelListViewport`, and `EmptyStateBlock` construction. The save action wrapper owns the right-aligned primary `PillButton`, icon binding, and callback forwarding while the page supplies only the responsive button width and localized label. `ui_workspace_layout_contract.rs`, `ui_inputs_contract.rs`, `ui_input_primitives_contract.rs`, `ui_panel_slot_contract.rs`, and `ui_page_surface_coverage_contract.rs` check both sides of that boundary.

Dashboard page-specific wrappers now live in `project_dashboard_components.slint` instead of at the top of `project_dashboard.slint`. `ProjectCover`, `ProjectCard`, `ProjectFlow`, `DashboardProjectCardsSection`, `DashboardToolbar`, `DashboardQuickActionRow`, `DashboardRecentProjectsPanel`, and `DashboardQuickActionsPanel` are exported from that component module, keeping Dashboard page code focused on toolbar basis/height inputs, card-section inputs, lower `PanelGrid` sizing, row-height inputs, and callback/data forwarding while centralizing the repeated toolbar `ResponsiveSlot` flow, `SearchBox`, Project filter/sort selectors, grid/list view buttons, card visuals, Material state layer, Material scroll view, empty card panel, Show More `PillButton`, recent-project `DataTable`, quick-action `PanelListViewport`, quick-action empty state, and quick-action row construction. `ui_project_layout_contract.rs`, `ui_project_navigation_contract.rs`, `ui_inputs_contract.rs`, `ui_panel_slot_contract.rs`, `ui_material_usage_contract.rs`, `ui_project_scope_contract.rs`, and `ui_typography_contract.rs` check both sides of that boundary.

The 2026-05-29 reference-dashboard alignment pass keeps `DashboardButtonStatesStrip` as a reference-only overlay for matching `docs/ui-and-layout/hub.png`. `HubWindow` places that overlay at `height - dashboard-bottom-strip-height`, with the strip height including the extra panel gap so the bottom Button States band occupies the same final viewport zone as the reference and no page background bleeds below it. The strip's state colors now live in `HubVisualSpec` beside the neutral reference-gray page/chrome/panel tokens, so the Dashboard sample band stays deterministic without adding private color literals to page components. Presentation-edge fixture normalization stays in the UI layer: project cards render `Modified 1d ago` as `Modified yesterday`, the recent-project table renders it as `Yesterday`, and the Sands of Time card shows `Linux` without changing persisted project metadata. Project covers now render the seeded PNGs without an extra dark overlay, the Projects filter/sort `ToolbarSelect` trigger keeps Material `OutlineButton`/`PopupMenu` behavior while drawing a neutral Hub shell, top-header status pills use reference-matched Running/standard/Error widths, Dashboard Quick Actions use the reference header/list rhythm, and Dashboard cards use the 22px reference horizontal gap. `view_model/media.rs` also maps the seeded Neon Streets fixture to the reference PNG cover so the Recent Projects thumbnail remains inspectable in screenshot comparison. The latest validation capture is `target/hub-visual-check/hub-reference-aligned-1568x1003-v65/hub-projects-dashboard.png`; pixel comparison against the provided reference improved from v56 full-frame mean/RMS `14.01`/`31.13` to v65 `10.77`/`29.53`, with toolbar selects at `10.95`/`29.96`, Quick Actions at `13.39`/`37.22`, card covers at `15.08`/`26.05`, and the bottom strip at `8.02`/`22.86`. Focused acceptance for this visual slice includes `ui_shell_header_contract.rs`, `ui_inputs_contract.rs`, `ui_project_layout_contract.rs`, `ui_data_display_contract.rs`, `ui_panel_slot_contract.rs`, `ui_foundation_contract.rs`, `ui_page_surface_coverage_contract.rs`, `ui_visual_standard_contract.rs`, a manifest-scoped `zircon_hub` build, and real Windows Hub captures through `capture-hub-project-pages.ps1`.

The 2026-05-30 AI-directed web-reference closeout records its final evidence in `docs/ui-and-layout/hub-web-reference/ACCEPTANCE_EVIDENCE.md`. That ledger lists the HTML/CSS export command, the 19 generated final `hub-*.png` files, `validate-visuals.mjs`, `validate-interactions.mjs`, `validate-design-board.mjs`, and the focused `cargo test --manifest-path zircon_hub/Cargo.toml --locked --offline --jobs 1 --test ui_visual_standard_contract` gate. It also records the Export port policy, manifest schema, schema subset validation, negative schema self-tests, strict `ZIRCON_HUB_WEB_REFERENCE_PORT` behavior, interaction temporary profile cleanup with no `zircon-hub-cdp-*` temp profile left behind, AI drafts and the three design-board PNGs as review support rather than final acceptance evidence, and that an optional `cargo check` attempt timed out under concurrent Hub library compilation without replacing the focused visual contract result.

The follow-up Project Browser web-reference alignment fix keeps the design PNG's `All Projects` table on one shared column model. `styles.css` now applies `display: grid`, the same six-column template, and cell overflow rules to both `.browser-head` and `.browser-row`, so the Name, Engine, Platform, Modified, and Status header labels line up with the row cells in `docs/ui-and-layout/hub-projects-browser.png`. `validate-responsive.mjs` checks the header edge, row edge, selected-row edge, per-column left, and per-column width deltas for Browser pages, while `ui_visual_standard_contract.rs` statically locks the CSS snippets and responsive validator checks.

Builds page-specific wrappers now live in `builds_page_components.slint` instead of at the top of `builds.slint`. `BuildSourceSummaryPanel` and `BuildTaskHistoryPanel` remain Builds-owned `PanelSlot` components, while `BuildControlsPanel` and `BuildPipelinePanel` now inherit `HubListPanelSlot` so their title plus fixed action/pipeline rows consume the shared list-panel shell instead of owning a local `PanelHeader` plus repeated row stack. `BuildControlAction`, `BuildPipelineStep`, and `BuildSourceSummaryRow` stay in that component module as row-level helpers. The shared `OperationTimelinePanel` now lives in `operation_timeline_components.slint` and is re-exported by `components.slint`, so Builds and Settings consume one Material `ListTile` row and empty-state treatment for persisted action history without a Settings-to-Builds dependency. The Builds component module owns the Source Build header, source/output summary rows, profile/jobs badges, selected-project Build/Open Output/Open Editor/Package/Install rows, pipeline steps, current-task status, build-history viewport, and build-history empty state. `BuildsPage` now keeps only `WorkspacePanelSection` composition, semantic `basis`/`grow`/`order` and paired Slint flex properties, selected-project/source-engine/status data forwarding, build-history and operation-timeline scroll bindings, compact label collapse, and callback forwarding. `ui_workspace_layout_contract.rs`, `ui_panel_slot_contract.rs`, `ui_data_container_primitives_contract.rs`, `ui_selected_project_runtime_contract.rs`, `ui_data_display_contract.rs`, and `ui_typography_contract.rs` check both sides of that boundary: Builds component definitions and row internals stay in `builds_page_components.slint`, shared timeline internals stay in `operation_timeline_components.slint`, and `builds.slint` keeps one call each to the five typed panels plus original source path, output path, last-build, profile, status, job-count, selected-project, action callback, build-history, and operation-timeline bindings.

Editor page-specific wrappers now live in `editor_page_components.slint` instead of at the top of `editor.slint`. `SourceEngineRow`, `EditorSideListPanel`, `EditorSourceEngineListPanel`, `EditorBuildHistoryPanel`, `EditorPathFieldRow`, `EditorActionRow`, `EditorActionsPanel`, `EditorSourceSummaryPanel`, `EditorSourceSettingsPanel`, and `EditorSourceSummaryRow` are exported from that component module, keeping the Editor page responsible for panel layout, typed panel call sites, scroll bindings, data forwarding, and Source Engine selection/removal callbacks. The component module owns the repeated Material `ListTile`, `HubPanel` side-list shell, typed Source Engine and Build History list bodies, shared `PanelListViewport` rows, empty-state blocks, `HubTextField` plus browse/rename button rows, the `HubListPanelSlot`-backed Editor Actions panel, the `PanelSlot`-backed source summary and source settings panels, `ActionRow`, and `InfoRow` construction.

The Editor contracts check both sides of that boundary. `ui_workspace_layout_contract.rs`, `ui_inputs_contract.rs`, `ui_panel_slot_contract.rs`, `ui_material_usage_contract.rs`, `ui_typography_contract.rs`, and `ui_data_display_contract.rs` require the exported component definitions in `editor_page_components.slint`; `editor.slint` must keep one typed source summary panel call, one typed actions panel call, one typed source settings panel call, one typed Source Engine list panel call, one typed Build History panel call, original source path, output path, active engine, status, job-count, callback bindings, and the `actions-first` compact ordering rule that places the actions panel above the overview when the first viewport is too short for both. It must not reintroduce inline `EditorActionRow`, `EditorSourceSummaryRow`, `EditorPathFieldRow`, `SourceEngineRow`, or `BuildHistoryRow` loops.

The 2026-05-26 Hub layout continuation reran package-level validation with the warmed target directory at `D:\cargo-targets\zircon-hub-projects-0526-final`: `cargo fmt -p zircon_hub --check`, `cargo check --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1`, `cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1`, and `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1` all passed. The same slice captured Projects Dashboard, New Project, Project Browser, and Project Detail at `target\hub-visual-check\hub-layout-continue-0526-1280x900`, `target\hub-visual-check\hub-layout-continue-0526-1024x720-detail-fixed`, and `target\hub-visual-check\hub-layout-continue-0526-1024x720-default-fixed`; the final compact run verifies the multi-page screenshot script's Project Detail default click after it moved to the first-row trailing affordance. Focused workspace captures for Packages/Cloud, Team, and Editor were written beside each Projects set and visually checked for obvious overlap, black bands, bottom status-bar obstruction, and stale-page navigation.

The 2026-05-24 delete-confirm capture set at `target/hub-visual-check/hub-projects-1600x1024-delete-confirm` validates Dashboard, New Project, Project Browser, Project Detail, and the Project Detail pending-delete state after the guarded warning moved to `StatusBanner`. The final focused capture sets at `target/hub-visual-check/hub-projects-1024x720-statusbanner-final-0524` and `target/hub-visual-check/hub-projects-1600x1024-statusbanner-final-0524` validate the same flow after the pending confirmation moved to the top of the Actions panel, the normal action/change-engine controls hide while pending, and `StatusBanner` stopped stretching into a tall empty block on wide Detail layouts. The multi-page screenshot skill now exposes `-CapturePendingDelete`, `-DeleteClickX/-DeleteClickY`, and `-DeleteScrollNotches`, records those click defaults in `hub-project-pages-clicks.json`, captures `hub-projects-detail-delete-ready.png` after compact Detail scrolling, and fails the run if `hub-projects-detail-delete-confirm.png` does not differ enough from that ready baseline after clicking Delete Project.

The 2026-05-24 toolbar-chevron captures at `target/hub-visual-check/hub-projects-toolbar-chevron-1280x900-0524` and `target/hub-visual-check/hub-projects-toolbar-chevron-1600x1024-0524` validate Project Browser plus the open filter and sort `PopupMenu` states after `ToolbarSelect` gained the independent right-aligned Material chevron icon. The captured toolbars keep the chevrons on the far right of the select triggers, vertically centered against the button height, while menu anchors remain under the correct trigger. The 1600 capture also keeps the wide top status pills visible while Dashboard, New Project, Browser, and Detail stay inside the Projects secondary-page structure.

The 2026-05-25 Settings bridge captures reused the existing rebuilt Hub binary at `D:\cargo-targets\zircon-hub-settings-0525\debug\zircon_hub.exe` and wrote `target/hub-visual-check/settings-bridge-1600/hub-settings.png`, `target/hub-visual-check/settings-bridge-1366/hub-settings.png`, `target/hub-visual-check/settings-bridge-1100/hub-settings.png`, and `target/hub-visual-check/settings-bridge-960/hub-settings-header-click2.png`. These captures cover the planned 1600x1024, 1366x820, 1100x760, and 960x640 Settings breakpoints after the direct Material template bridge contract was hardened; text fields, the Settings build-default combo rows, path rows, and configuration-health rows remain inside shared page/panel/list surfaces without visible text collision or control overflow. The follow-up MaterialWindow capture at `target/hub-visual-check/material-window-bridge-1280/hub-dashboard.png` verifies the imported template window wrapper still renders the Hub frameless shell, background, sidebar, dashboard, and bottom status bar without black bands or chrome overlap.

The `hub-ui-polish-foundations-inputs-navigation-contracts-docs` lock-down milestone adds a focused API-level contract over the input/navigation primitive family. `components.slint` explicitly re-exports the stable input primitives (`SelectOptionData`, `SearchBox`, `HubTextField`, `ToolbarSelect`, `DropDownButton`, and `SegmentButton`), the shared navigation DTO `NavItemData`, `NavRail`, and the Material adapter DTOs (`NavigationItem`, `MenuItem`, and Material style objects) that callers need to build or adapt toolbar and rail data without importing lower-level owner files directly. `ui_input_navigation_api_contract.rs` is intentionally narrower than the existing visual-state tests: it checks public property, callback, DTO, and adapter-routing shapes, while `ui_inputs_contract.rs` and `ui_navigation_contract.rs` continue to check internal state-layer, Material-control, and shell forwarding details. Downstream shell and page polish should therefore consume the wrappers through `components.slint` and route collapsed navigation through the Rust `NavItemData` to `NavigationItem` adapter rather than constructing a second page-routing model.

The 2026-05-31 bottom-up Material component pass fills the remaining low-level control gap before more page assembly. `inputs.slint` now owns `HubCheckBox`, `HubCheckBoxRow`, `HubSwitch`, `HubToggleRow`, and `HubComboBox` over the local Material template's `CheckBox`, `CheckBoxTile`, `Switch`, and shared `HubDropDownSurface`; `navigation.slint` owns `HubTabs` over `TabBar` and `SecondaryTabBar`. These wrappers are re-exported through `components.slint` so higher-level lists, settings panels, drawers, and window surfaces can compose checkbox/toggle/tabs/combo behavior without page-local `TouchArea` or text-input clones. `HubToggleRow` keeps its text column left aligned and its `HubSwitch` in the trailing slot through Slint stretch layout rather than root-width arithmetic, avoiding layout recursion while preserving the expected row shape. Settings build defaults now exercise that path through `SettingsComboChoice`, which maps two stored values to `MenuItem` rows, initializes the Material `current-index` from the stored value, and binds later selections back to `build-profile` and `language`. New Project template choices now exercise the checkbox path through `TemplateChoiceRow`, where `HubCheckBox` mirrors `ProjectTemplateData.selected` and the row-level Material state layer forwards enabled template clicks. Project Detail now exercises the toggle path through `ProjectDetailPinToggleRow`, which inherits `HubToggleRow`, mirrors `ProjectDetailData.pinned`, and forwards the existing `toggle-pin` callback while leaving Open, Remove, and Delete as command buttons. `ui_inputs_contract.rs`, `ui_input_primitives_contract.rs`, `ui_project_layout_contract.rs`, `ui_material_usage_contract.rs`, and `ui_navigation_contract.rs` lock the new wrapper layer and reject hand-rolled interaction internals.

The same bottom-up pass now promotes the next container layer. `overlays.slint` owns `HubPopupMenu`, `HubDropDownSurface`, `HubDrawer`, `HubModalDrawer`, `HubWindowView`, `PopupPanel`, and `HubPopupWindow` as wrappers over the local Material `PopupMenu`, `DropDownMenu`, `Drawer`, `ModalDrawer`, `MaterialWindow`, `PopupWindow`, and the Hub popup panel chrome; `HubPopupWindow` centralizes close-on-click-outside behavior plus popup `x`/`y`/width/height forwarding, while `app.slint` inherits `HubWindowView` so window sizing and frameless chrome constraints no longer live in the page shell. `ToolbarSelect` now consumes `HubPopupMenu` for Project Browser filter/sort menus, keeping the Material popup behavior behind the shared overlay wrapper while leaving the toolbar trigger geometry in `inputs.slint`; `HubComboBox` now consumes `HubDropDownSurface` for Settings build-profile and language choices, keeping the public `items`/`current-index`/`selected` adapter API while removing the direct `DropDownMenu` import from `inputs.slint`; top-header Source Engine and user popups now consume `HubPopupWindow` plus `PopupPanel` instead of owning raw `PopupWindow`/`HubPanel` directly, so close policy and anchor geometry stay in `overlays.slint` while the body chrome stays in `PopupPanel`. `data_display.slint` owns generic `HubListView`, `HubListPanelSlot`, `TreeItemData`, `HubTreeRow`, and `HubTreeView` on top of `PanelListViewport`, `ScrollView`, `StateLayerArea`, and `ListTile`, while `table_view_components.slint` owns `HubTableView`, `HubTableBody`, `TableColumnHeader`, `ProjectTableRow`, and `DataTable` on top of the shared viewport. This gives later pages a Material-style list/table/tree layer before page-specific rows are assembled without letting one file collect every container type. `CatalogListPanel` now inherits `HubListView` instead of hand-building a second scroll shell, while preserving the catalog header, badge tone, vertical padding, empty-state height, and centered empty copy used by Assets, Plugins, and Learn. `HubListPanelSlot` fills the same role for responsive `PanelSlot` children: Cloud Services, Team Members, Settings Default Paths, Settings Configuration Health, the New Project template rail, and Dashboard Quick Actions inherit it, keep their existing page-level height and scroll bindings, and no longer duplicate `PanelHeader` plus `PanelListViewport` shells in their page component modules. The wrapper exposes a `header-height` override so dense reference panels such as Quick Actions can keep the shorter Dashboard header without forking the shared shell. Editor's Source Engines and Build History side-list panels now inherit `HubListView` through `EditorSideListPanel`, keeping their existing title/badge and scroll bindings while removing another page-local `HubPanel`/`PanelListViewport` shell. `ProjectBrowserResultsPanel` now inherits `HubTableView` and places `ProjectBrowserTableHeader` plus `HubTableBody` inside that shared table shell, so the Browser keeps its stable header-above-rows geometry while consuming the generic table viewport for scroll, row spacing, and empty-state behavior. Dashboard Recent Projects now also reaches the same table body layer: `DataTable` keeps its transparent in-panel table header/divider and `ProjectTableRow` column model, but its scrollable row body is `HubTableBody` instead of a page-local `ScrollView`/content-height implementation. `ui_overlay_primitives_contract.rs`, `ui_data_display_contract.rs`, `ui_data_container_primitives_contract.rs`, `ui_panel_slot_contract.rs`, `ui_workspace_layout_contract.rs`, `ui_page_surface_coverage_contract.rs`, `ui_typography_contract.rs`, `ui_project_browser_table_contract.rs`, `ui_shell_composition_contract.rs`, `ui_shell_header_contract.rs`, `ui_visual_standard_contract.rs`, and `ui_shell_window_contract.rs` lock these boundaries. The Cloud/Team panel-slot slice was captured through the native Windows Hub window at `target/hub-visual-check/cloud-team-hublistpanelslot-0531/hub-cloud.png` and `target/hub-visual-check/cloud-team-hublistpanelslot-0531/hub-team.png`; the Settings slice was captured at `target/hub-visual-check/settings-hublistpanelslot-0531/hub-settings.png`; the New Project template rail slice was captured at `target/hub-visual-check/template-rail-hublistpanelslot-0531/hub-new-project.png`; the Quick Actions slice was captured at `target/hub-visual-check/dashboard-quickactions-hublistpanelslot-0531/hub-projects-dashboard.png` and with seeded reference projects at `target/hub-visual-check/dashboard-quickactions-hublistpanelslot-0531/seeded/hub-projects-dashboard.png`; the toolbar-select overlay slice was captured at `target/hub-visual-check/toolbarselect-hubpopupmenu-0531/hub-projects-browser-filter-menu.png` and `target/hub-visual-check/toolbarselect-hubpopupmenu-0531/hub-projects-browser-sort-menu.png`; the combo overlay slice was captured at `target/hub-visual-check/settings-combo-hubdropdownsurface-0531/hub-settings.png` and `target/hub-visual-check/settings-combo-hubdropdownsurface-0531/hub-settings-build-profile-menu.png`; the top-header popup panel slice was captured at `target/hub-visual-check/top-header-popuppanel-0531/hub-source-engine-popup.png` and `target/hub-visual-check/top-header-popuppanel-0531/hub-user-menu.png`; the top-header popup-window shell slice was captured at `target/hub-visual-check/top-header-hubpopupwindow-0531/hub-source-engine-popup.png` and `target/hub-visual-check/top-header-hubpopupwindow-0531/hub-user-menu.png`; the top-header user-menu `ActionRow` slice was captured at `target/hub-visual-check/top-header-actionrow-0531/hub-user-menu.png`; the top-header Source Engine `InfoRow` slice was captured at `target/hub-visual-check/top-header-engine-inforow-0531/hub-source-engine-popup-final.png`. These captures validate that the shared panel, row, and overlay shells keep page structure, popup/menu anchors, badges, row rhythm, selector/action-row affordances, and fixed bottom status chrome stable at `1568x1003`.
The Builds list-panel continuation extends that `HubListPanelSlot` usage to `BuildControlsPanel` and `BuildPipelinePanel`: both panels keep their page-level height and callback/data bindings, but their title, row count, row spacing, and repeated `BuildControlAction`/`BuildPipelineStep` delegates now live inside the shared list-panel shell instead of local `PanelHeader` plus hand-stacked rows. The same action-panel slice moves `EditorActionsPanel` and `CloudPackageActionsPanel` to `HubListPanelSlot`, so Editor actions, Cloud package/install actions, Builds controls, and Builds pipeline all share the same fixed-row list-panel wrapper. The native Builds capture at `target/hub-visual-check/builds-hublistpanelslot-0531/hub-builds.png` validates that the Selected Project Actions and Build Pipeline panels keep their row rhythm, badge alignment, disabled action treatment, and bottom status-bar clearance at `1568x1003`; the follow-up Editor/Cloud captures at `target/hub-visual-check/editor-cloud-actions-hublistpanelslot-0531/hub-editor.png` and `target/hub-visual-check/editor-cloud-actions-hublistpanelslot-0531/hub-cloud.png` validate the same shared action-panel shell on Editor Actions and Cloud Packages.

The Dashboard table-shell continuation moves `DashboardRecentProjectsPanel` from a local `PanelSlot` header/body stack to `HubTableView`. `HubTableView` now exposes the same semantic `basis`/`grow`/`shrink`/`order` inputs used by responsive panel call sites plus a `header-height` override, so the Dashboard Recent Projects panel keeps the reference compact header while sharing the same Material table shell as Project Browser. `DataTable` still owns the transparent in-panel table header, divider, `ProjectTableRow` column model, and `HubTableBody` scroll body; the page root only supplies lower-grid flex geometry, data, labels, and callbacks. `ui_data_container_primitives_contract.rs`, `ui_panel_slot_contract.rs`, `ui_project_layout_contract.rs`, `ui_material_usage_contract.rs`, and `ui_page_surface_coverage_contract.rs` lock the new table-panel boundary. The native Dashboard capture at `target/hub-visual-check/dashboard-recent-hubtableview-0531/hub-projects-dashboard-single.png` validates the seeded Recent Projects table, compact header, footer action, Quick Actions neighbor panel, and bottom status chrome at `1568x1003`.

The Project source-engine list continuation moves `ProjectEngineChoiceList` itself onto the shared list viewport layer: it now directly inherits `PanelListViewport` instead of owning a transparent `Rectangle` plus local `engine-list` child. New Project and Project Detail still pass their own `row-height`, `row-spacing`, `empty-height`, selected/registered labels, scroll binding, and compact-label inputs into that shared inherited surface, while `EngineChoiceRow` remains the row-level Material `ListTile` delegate. `ui_data_container_primitives_contract.rs`, `ui_project_layout_contract.rs`, `ui_material_usage_contract.rs`, `ui_inputs_contract.rs`, `ui_typography_contract.rs`, and `ui_page_surface_coverage_contract.rs` lock the direct viewport boundary and the call-site bindings. The native Projects capture at `target/hub-visual-check/project-engine-choice-list-direct-0531` validates Dashboard, New Project, Browser, and Detail after the wrapper removal; the accepted New Project and Detail screenshots are `hub-projects-new-project.png` and `hub-projects-detail.png`.

The Project Detail section continuation adds `HubSection` in `surfaces.slint` as a lightweight in-panel section primitive: it centralizes `PanelHeader` plus arbitrary child content without `HubPanel` or `PanelSlot` chrome, so it can be used inside an existing card without creating nested cards. `ProjectDetailInfoSection` and `ProjectDetailEngineSection` now inherit `HubSection`; the page still supplies the section heights, spacing, project data, row metrics, engine list metrics, and callbacks, while the component module owns only the section title/subtitle and row/list delegates. `ui_foundation_contract.rs`, `ui_project_layout_contract.rs`, `ui_material_usage_contract.rs`, `ui_typography_contract.rs`, and `ui_page_surface_coverage_contract.rs` lock the new primitive and both Project Detail consumers. The native Project Detail capture at `target/hub-visual-check/project-detail-hubsection-0531/hub-projects-detail.png` validates that the Actions and Project Information panels remain single-card structures with stable section spacing, source-engine rows, and bottom status chrome at `1568x1003`.

Builds now consumes the same in-panel section primitive for its current task and build-history stack. `BuildTaskHistoryPanel` remains the owning `PanelSlot`, but its Current Task and Build History subsections are `HubSection` blocks instead of duplicated local `PanelHeader` stacks. The component intentionally keeps explicit `current-task-section-height`, `history-list-height`, and `history-section-height` calculations rather than stretching both sections equally; the stretched prototype cropped the no-history empty state near the fixed bottom status bar. `ui_data_container_primitives_contract.rs`, `ui_workspace_layout_contract.rs`, and `ui_typography_contract.rs` lock the section-height model, the `PanelListViewport` history body, and the MaterialText status title. The 2026-05-31 verification reran `cargo fmt --manifest-path zircon_hub\Cargo.toml --check`, the focused Hub UI contract set (`ui_data_container_primitives_contract`, `ui_foundation_contract`, `ui_workspace_layout_contract`, `ui_panel_slot_contract`, `ui_typography_contract`, and `ui_page_surface_coverage_contract`), `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1`, and the native screenshot capture `target/hub-visual-check/buildtaskhistory-hubsection-0531/hub-builds-final.png` at `1568x1003`. The final capture shows the Current Task and Build History sections in one right-hand panel with the no-history empty text visible above the bottom status bar.

The same 2026-05-31 design-reference pass regenerated `docs/ui-and-layout/hub-projects-browser.png` with `node docs\ui-and-layout\hub-web-reference\export-pages.mjs --only=hub-projects-browser` and reran `node docs\ui-and-layout\hub-web-reference\validate-responsive.mjs` plus `node docs\ui-and-layout\hub-web-reference\validate-visuals.mjs`. The responsive validator checks the All Projects table, header, every browser row, selected-row fill, left/right edges, and column widths across the `1915x508` design-review crop plus the standard responsive viewport set, preventing the previously reported Browser table header/row misalignment from returning in the HTML/CSS reference artifacts. The visual validator keeps the exported PNG inventory, export index, spot-check ledger, docs matrix, AI manifest coverage, and dashboard pixel comparison synchronized with the regenerated Browser reference.

The operation timeline continuation extracts the shared timeline row and panel from the oversized `data_display.slint` into `operation_timeline_components.slint`. `data_display.slint` now stays focused on generic list/tree primitives plus catalog/data rows, while `OperationTimelineRow` and `OperationTimelinePanel` own the action-history-specific `OperationTimelineRowData` binding, status icon, `PanelSlot`, timeline `PanelHeader`, `PanelListViewport`, and centered empty state. `components.slint` remains the public import surface, so Builds, Cloud, and Settings still consume `OperationTimelinePanel` through the same shared component entry point. `ui_data_display_contract.rs`, `ui_foundation_contract.rs`, `ui_panel_slot_contract.rs`, `ui_workspace_layout_contract.rs`, and `ui_page_surface_coverage_contract.rs` lock the new module boundary, including the rule that `data_display.slint` must not regain operation-timeline ownership. The 2026-05-31 verification reran `cargo fmt --manifest-path zircon_hub\Cargo.toml --check`, focused Hub UI contracts (`ui_data_display_contract`, `ui_foundation_contract`, `ui_panel_slot_contract`, `ui_workspace_layout_contract`, and `ui_page_surface_coverage_contract`), `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1`, and native captures at `target/hub-visual-check/operation-timeline-module-0531/hub-builds-1568x1003.png` plus the supplemental tall timeline check `target/hub-visual-check/operation-timeline-module-0531/hub-builds-operation-timeline-1568x1200.png`.

The table-view continuation extracts `TableColumnHeader`, `ProjectTableRow`, `HubTableBody`, `DataTable`, and `HubTableView` into `table_view_components.slint`. The module remains below the public `components.slint` entrypoint, imports only the shared `PanelListViewport` from `data_display.slint`, and keeps the Dashboard Recent Projects and Project Browser table APIs unchanged. `data_display.slint` is now 539 lines and cannot regain table component ownership under `ui_data_display_contract.rs` and `ui_data_container_primitives_contract.rs`; `ui_foundation_contract.rs`, `ui_material_usage_contract.rs`, `ui_page_surface_coverage_contract.rs`, and `ui_visual_standard_contract.rs` also include the new module in the focused component and visual-token checks. The 2026-05-31 focused static verification ran `cargo test --manifest-path zircon_hub\Cargo.toml --locked --offline --jobs 1 --test ui_data_display_contract --test ui_data_container_primitives_contract --test ui_foundation_contract --test ui_material_usage_contract --test ui_page_surface_coverage_contract --test ui_visual_standard_contract --message-format short --color never`, covering the table module split before the final build and native screenshot check.

The 2026-05-31 Dashboard pixel-reference continuation narrows the Projects landing page toward `docs/ui-and-layout/hub.png` without claiming pixel parity. `SearchBox` now exposes a `prominent` state so the Dashboard search field can hold the cyan reference outline even before keyboard focus. Dashboard toolbar select controls use the reduced `project-dashboard-toolbar-select-ratio` token, center their compact button height, and route the grid/list affordances through the local `DashboardViewToggleButton` shape so the toolbar tracks the reference icon-button proportions. `ProjectCover` owns absolute cover overlays for the card menu and Zircon mark plate, and `ProjectFlowNextButton` adds the collapsed-card carousel affordance at the right edge of the card row. Dashboard Quick Actions now use a Dashboard-specific compact `DashboardQuickActionRow` backed by Material `StateLayerArea` and the existing quick-action ids/callbacks, while preserving the reference copy for Build Project, Install to Device, Package Project, and Open in Editor. The top header notification, help, and settings actions use a transparent `HeaderPlainIconButton` instead of framed Hub icon buttons so the shell chrome is closer to the supplied image. The verification capture set is `target/hub-visual-check/pixel-match-0531-j`; its Dashboard screenshot is `hub-projects-dashboard.png` at `1568x1003`. The reference comparison against `docs/ui-and-layout/hub.png` currently measures full-frame mean `10.06` and RMS `28.83`; the largest remaining regional gaps are project cards mean `14.07`, top bar mean `12.09`, sidebar mean `11.92`, and quick panel mean `9.20`. This is an improvement over the prior full-frame mean `10.48`/RMS `29.81`, but exact pixel-level parity still requires another pass on card imagery, shell/sidebar tone, and font/rendering offsets. The same slice passed `cargo build --manifest-path zircon_hub\Cargo.toml --bin zircon_hub --locked --offline --jobs 1 --message-format short --color never`, the focused contract set covering dashboard layout, panel slots, foundations, data display, Material usage, page-surface coverage, and visual-standard checks, plus `cargo fmt --manifest-path zircon_hub\Cargo.toml --check`.

The follow-up Dashboard pixel-reference pass keeps the target as `docs/ui-and-layout/hub.png` and validates only through native Windows Hub captures. The capture script now moves the cursor out of the Dashboard before the first save and uses a wide-layout New Project click ratio that works without manual override. Layout/color convergence tightened the sidebar rail inner width, top-header brand spacing, Projects header vertical offset, New Project split-button geometry, header action icon vertical centering, non-running Engine Status success color, and chrome background tone. The accepted capture set is `target/hub-visual-check/pixel-match-0531-ad`; `hub-projects-dashboard.png` is `1568x1003` and compares against the reference at full-frame mean `9.30` / RMS `27.87`. Regional means are topbar `10.66`, sidebar `9.54`, workspace `9.37`, header actions `13.97`, project cards `12.75`, recent panel `9.79`, quick panel `9.12`, and bottom strip `8.01`. Exact pixel-level parity is still not met; the remaining highest gaps are the project cover/card raster content, header action font/icon antialiasing, and text-rendering offsets inside the table/action panels.

The 2026-06-01 bottom-up icon-button continuation adds `HubIconButton` as the shared Hub-level icon button primitive beside the Material-template-backed `IconButton`. `IconButton` remains the generic Filled/Outlined Material wrapper, while `HubIconButton` centralizes the reference-tuned square/circle chrome used by transparent titlebar icons, Dashboard grid/list toggles, and the project-card flow next affordance: tokenized width/height/radius, active/idle backgrounds, borders, foreground colors, disabled opacity, focus ring, and `StateLayerArea` interaction are now one primitive instead of repeated page-local `Rectangle` plus `Image` blocks. `HubTopHeader` consumes `HubIconButton` directly for bell/help/settings, and `DashboardViewToggleButton` plus `ProjectFlowNextButton` inherit it inside `project_dashboard_components.slint`. Focused static contracts now lock the primitive API and prevent the header from reintroducing `HeaderPlainIconButton`. The native capture set `target/hub-visual-check/hubiconbutton-0601` preserved the previous Dashboard comparison exactly at full-frame mean `9.30` / RMS `27.87`, so this slice improves component layering without regressing the current pixel-alignment baseline.

The same 2026-06-01 command-button continuation adds `HubCommandButton` as the shared text-plus-icon command primitive for reference Projects header actions. It preserves the existing Import Project and New Project geometry while moving the local `HeaderActionButton` chrome out of `shell_page_components.slint`: tokenized width/height, focused border, primary/secondary fills, Material `StateLayerArea`, centered leading icon, Material label, and optional split-menu divider/chevron are now centralized in `shared.slint`. `HubPageHeader` consumes `HubCommandButton` for both Projects header actions, and the shell composition contract rejects reintroducing a local header action button. The native capture set `target/hub-visual-check/hubcommandbutton-0601` preserved the Dashboard comparison at full-frame mean `9.30` / RMS `27.87`, with header actions still at mean `13.97`, confirming this structural extraction did not move the current pixel baseline.
