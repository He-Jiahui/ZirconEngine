[package]
name = "zircon_plugin_ui_document_importer_dist"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "UI document importer standalone native plugin distribution for Zircon."

[lib]
crate-type = ["cdylib"]

[features]
default = ["dist"]
dist = ["zircon_plugin_sdk/native"]

[dependencies]
zircon_plugin_ui_document_importer_runtime = { path = "../runtime" }
zircon_plugin_sdk = { workspace = true, default-features = false }
