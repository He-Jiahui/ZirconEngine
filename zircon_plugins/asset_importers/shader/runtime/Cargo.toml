[package]
name = "zircon_plugin_asset_importer_shader_runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shader asset importer runtime plugin package for Zircon."

[dependencies]
naga = { version = "29.0.1", features = ["glsl-in", "spv-in", "wgsl-in", "wgsl-out"] }
zircon_runtime = { path = "../../../../zircon_runtime", default-features = false }

[dev-dependencies]
toml = "1.1.2"
