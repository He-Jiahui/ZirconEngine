# @generated from Rust PluginDeclaration; do not edit by hand.
id = "texture"
version = "0.1.0"
display_name = "Texture"
description = "Optional texture processing runtime and editor package."
sdk_api_version = "0.2.0"
category = "runtime"
maturity = "stable"
supported_targets = ["client_runtime", "editor_host"]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["runtime.plugin.texture"]
default_packaging = ["source_template", "library_embed", "native_dynamic"]

[distribution]
forms = ["dist"]
default_packaging = ["native_dynamic"]
abi_version = 3
engine_compat = ">=0.1, <0.2"
dist_crate = "zircon_plugin_texture_dist"
descriptor_symbol = "zircon_native_plugin_descriptor_v3"
runtime_entry = "zircon_plugin_texture_runtime_entry_v3"

[[capability_statuses]]
capability = "runtime.plugin.texture"
status = "complete"

[[modules]]
name = "texture.runtime"
kind = "runtime"
crate_name = "zircon_plugin_texture_runtime"
target_modes = ["client_runtime", "editor_host"]
capabilities = ["runtime.plugin.texture"]

[[modules]]
name = "texture.editor"
kind = "editor"
crate_name = "zircon_plugin_texture_editor"
target_modes = ["editor_host"]
capabilities = ["editor.extension.texture_authoring"]

[[modules]]
name = "texture.dist"
kind = "native"
crate_name = "zircon_plugin_texture_dist"
target_modes = ["client_runtime", "editor_host"]
capabilities = ["runtime.plugin.texture"]
