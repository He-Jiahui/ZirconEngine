# @generated from Rust descriptor package_manifest(); do not edit by hand.
id = "native_window_hosting"
version = "0.1.0"
sdk_api_version = "0.2.0"
display_name = "Native Window Hosting"
description = "Optional editor integration for native floating window surfaces."
category = "platform"
maturity = "experimental"
supported_targets = ["editor_host"]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["editor.extension.native_window_hosting"]
default_packaging = ["source_template", "library_embed", "native_dynamic"]

[distribution]
forms = ["dist"]
default_packaging = ["native_dynamic"]
abi_version = 3
engine_compat = ">=0.1, <0.2"
dist_crate = "zircon_plugin_native_window_hosting_dist"
descriptor_symbol = "zircon_native_plugin_descriptor_v3"
editor_entry = "zircon_plugin_native_window_hosting_editor_entry_v3"

[[modules]]
name = "native_window_hosting.editor"
kind = "editor"
crate_name = "zircon_plugin_native_window_hosting_editor"
target_modes = ["editor_host"]
capabilities = ["editor.extension.native_window_hosting"]

[[modules]]
name = "native_window_hosting.dist"
kind = "native"
crate_name = "zircon_plugin_native_window_hosting_dist"
target_modes = ["editor_host"]
capabilities = ["editor.extension.native_window_hosting"]
