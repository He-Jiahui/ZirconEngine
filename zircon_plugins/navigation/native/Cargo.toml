[package]
name = "zircon_plugin_navigation_recast"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Recast/Detour navigation backend boundary for Zircon navigation."

[dependencies]
zircon_runtime = { path = "../../../zircon_runtime", default-features = false }

[build-dependencies]
cc = "1.2"
