# @generated from Rust PluginDeclaration; do not edit by hand.
id = "navigation"
version = "0.1.0"
sdk_api_version = "0.2.0"
package_prefix = "com"
package_company = "zircon"
package_name = "navigation"
display_name = "Navigation"
category = "runtime"
description = "Optional navigation runtime and editor package."
maturity = "beta"
supported_targets = ["client_runtime", "server_runtime", "editor_host"]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["runtime.plugin.navigation", "runtime.plugin.navigation.recast"]
default_packaging = ["source_template", "library_embed", "native_dynamic"]

[distribution]
forms = ["dist"]
default_packaging = ["native_dynamic"]
abi_version = 3
engine_compat = ">=0.1, <0.2"
dist_crate = "zircon_plugin_navigation_dist"
descriptor_symbol = "zircon_native_plugin_descriptor_v3"
runtime_entry = "zircon_plugin_navigation_runtime_entry_v3"

[[capability_statuses]]
capability = "runtime.plugin.navigation"
status = "partial"
note = "Gameplay navmesh/pathfinding is optional; UI navigation parity is separate."

[[modules]]
name = "navigation.runtime"
kind = "runtime"
crate_name = "zircon_plugin_navigation_runtime"
target_modes = ["client_runtime", "server_runtime", "editor_host"]
capabilities = ["runtime.plugin.navigation", "runtime.plugin.navigation.recast"]
system_sets = ["navigation.main"]
system_anchors = ["navigation.agent_tick"]

[[modules]]
name = "navigation.dist"
kind = "native"
crate_name = "zircon_plugin_navigation_dist"
target_modes = ["client_runtime", "server_runtime", "editor_host"]
capabilities = ["runtime.plugin.navigation", "runtime.plugin.navigation.recast"]

[[modules]]
name = "navigation.editor"
kind = "editor"
crate_name = "zircon_plugin_navigation_editor"
target_modes = ["editor_host"]
capabilities = ["editor.extension.navigation_authoring", "editor.extension.navigation_gizmos"]

[[modules.event_consumers]]
consumer_id = "navigation.editor.agent_tick"
event_id = "navigation.events.agent_tick_completed"
payload_schema = "navigation.events.nav_agent_tick_report.v1"
required_capability = "editor.extension.navigation_gizmos"
