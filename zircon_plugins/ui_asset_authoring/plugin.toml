# @generated from Rust descriptor package_manifest(); do not edit by hand.
id = "ui_asset_authoring"
version = "0.1.0"
sdk_api_version = "0.2.0"
display_name = "UI Asset Authoring"
category = "authoring"
description = "Optional editor tools for authoring retained UI asset documents."
maturity = "experimental"
supported_targets = ["editor_host"]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["editor.extension.ui_asset_authoring"]
default_packaging = ["source_template", "library_embed", "native_dynamic"]

[distribution]
forms = ["dist"]
default_packaging = ["native_dynamic"]
abi_version = 3
engine_compat = ">=0.1, <0.2"
dist_crate = "zircon_plugin_ui_asset_authoring_dist"
descriptor_symbol = "zircon_native_plugin_descriptor_v3"
editor_entry = "zircon_plugin_ui_asset_authoring_editor_entry_v3"

[[modules]]
name = "ui_asset_authoring.editor"
kind = "editor"
crate_name = "zircon_plugin_ui_asset_authoring_editor"
target_modes = ["editor_host"]
capabilities = ["editor.extension.ui_asset_authoring"]

[[modules]]
name = "ui_asset_authoring.dist"
kind = "native"
crate_name = "zircon_plugin_ui_asset_authoring_dist"
target_modes = ["editor_host"]
capabilities = ["editor.extension.ui_asset_authoring"]
