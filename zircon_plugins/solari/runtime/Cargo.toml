[package]
name = "zircon_plugin_solari_runtime"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Solari experimental runtime provider contract for Zircon."

[dependencies]
zircon_plugin_sdk = { workspace = true, features = ["runtime"] }
zircon_runtime = { path = "../../../zircon_runtime", default-features = false }
