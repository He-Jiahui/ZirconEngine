[package]
name = "zircon_plugin_animation_graph_dist"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Animation graph editor standalone distribution package for Zircon."

[lib]
crate-type = ["cdylib"]

[features]
default = ["dist"]
dist = []

[dependencies]
zircon_plugin_sdk = { workspace = true, default-features = false, features = ["native"] }
