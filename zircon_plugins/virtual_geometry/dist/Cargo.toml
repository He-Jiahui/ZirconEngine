[package]
name = "zircon_plugin_virtual_geometry_dist"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Standalone native dynamic Virtual Geometry plugin wrapper for Zircon."

[lib]
crate-type = ["cdylib"]

[features]
default = ["dist"]
dist = []

[dependencies]
zircon_plugin_sdk = { workspace = true, default-features = false, features = ["native"] }
