# @generated from Rust descriptor package_manifest(); do not edit by hand.
id = "terrain"
version = "0.1.0"
sdk_api_version = "0.2.0"
display_name = "Terrain"
category = "authoring"
description = "Optional runtime and editor package for terrain heightfield authoring."
maturity = "beta"
supported_targets = ["client_runtime", "editor_host"]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["runtime.plugin.terrain"]
default_packaging = ["source_template", "library_embed", "native_dynamic"]

[distribution]
forms = ["dist"]
default_packaging = ["native_dynamic"]
abi_version = 3
engine_compat = ">=0.1, <0.2"
dist_crate = "zircon_plugin_terrain_dist"
descriptor_symbol = "zircon_native_plugin_descriptor_v3"
runtime_entry = "zircon_plugin_terrain_runtime_entry_v3"

[[capability_statuses]]
capability = "runtime.plugin.terrain"
status = "partial"

[[modules]]
name = "terrain.runtime"
kind = "runtime"
crate_name = "zircon_plugin_terrain_runtime"
target_modes = ["client_runtime", "editor_host"]
capabilities = ["runtime.plugin.terrain"]

[[modules]]
name = "terrain.editor"
kind = "editor"
crate_name = "zircon_plugin_terrain_editor"
target_modes = ["editor_host"]
capabilities = ["editor.extension.terrain_authoring"]

[[modules]]
name = "terrain.dist"
kind = "native"
crate_name = "zircon_plugin_terrain_dist"
target_modes = ["client_runtime", "editor_host"]
capabilities = ["runtime.plugin.terrain"]
