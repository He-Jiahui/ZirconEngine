# @generated from Rust PluginDeclaration; do not edit by hand.
id = "runtime_diagnostics"
version = "0.1.0"
sdk_api_version = "0.2.0"
display_name = "Runtime Diagnostics"
description = "Optional editor diagnostics views for inspecting embedded runtime state."
category = "diagnostics"
maturity = "experimental"
supported_targets = ["editor_host"]
supported_platforms = ["windows", "linux", "macos"]
capabilities = ["editor.extension.runtime_diagnostics"]
default_packaging = ["source_template", "library_embed", "native_dynamic"]

[distribution]
forms = ["dist"]
default_packaging = ["native_dynamic"]
abi_version = 3
engine_compat = ">=0.1, <0.2"
dist_crate = "zircon_plugin_runtime_diagnostics_dist"
descriptor_symbol = "zircon_native_plugin_descriptor_v3"
editor_entry = "zircon_plugin_runtime_diagnostics_editor_entry_v3"

[[modules]]
name = "runtime_diagnostics.editor"
kind = "editor"
crate_name = "zircon_plugin_runtime_diagnostics_editor"
target_modes = ["editor_host"]
capabilities = ["editor.extension.runtime_diagnostics"]

[[modules]]
name = "runtime_diagnostics.dist"
kind = "native"
crate_name = "zircon_plugin_runtime_diagnostics_dist"
target_modes = ["editor_host"]
capabilities = ["editor.extension.runtime_diagnostics"]
