pub(in crate::plugin::runtime_plugin) fn is_lowercase_runtime_plugin_token(value: &str) -> bool {
    !value.is_empty()
        && value
            .bytes()
            .all(|byte| byte.is_ascii_lowercase() || byte.is_ascii_digit() || byte == b'_')
}
