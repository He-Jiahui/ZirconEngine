mod record;
mod record_capture;
mod record_history;
mod update_hybrid_gi_runtime;
mod update_virtual_geometry_runtime;

pub(in crate::runtime::server::submit_frame_extract) use record::record_submission;
