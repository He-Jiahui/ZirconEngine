mod prepared_material;
mod prepared_model;
mod prepared_shader;
mod prepared_texture;

pub(in crate::scene::resources) use prepared_material::PreparedMaterial;
pub(in crate::scene::resources) use prepared_model::PreparedModel;
pub(in crate::scene::resources) use prepared_shader::PreparedShader;
pub(in crate::scene::resources) use prepared_texture::PreparedTexture;
