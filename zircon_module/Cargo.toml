[package]
name = "zircon_module"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Native extensions and engine module loading (Godot modules/ parallel)."

[dependencies]
libloading = "0.9.0"
zircon_core = { path = "../zircon_core" }
