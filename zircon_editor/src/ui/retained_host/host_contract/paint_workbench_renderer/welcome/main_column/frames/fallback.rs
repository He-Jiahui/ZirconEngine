mod metrics;
mod rects;

pub(super) use metrics::{welcome_main_column_frame_metrics, WelcomeMainColumnFrameMetrics};
pub(super) use rects::{
    header_fallback, hero_fallback, location_fallback, name_fallback, preview_fallback,
    status_fallback, validation_fallback,
};
