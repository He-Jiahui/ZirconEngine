//! Math primitives: vectors, transforms, rays, and collision helpers.
//!
//! Conceptual counterpart: Fyrox `fyrox-math`; Godot math in `core/math`.

/// Returns the workspace engine name.
pub fn engine_name() -> &'static str {
    "ZirconEngine"
}
