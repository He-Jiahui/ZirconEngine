var console = %import("zr.system.console");
var equipmentRouting = %import("progression/equipment_routing_state");

console.printLine("m5-equipment-routing-state-test");
var result = equipmentRouting.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
