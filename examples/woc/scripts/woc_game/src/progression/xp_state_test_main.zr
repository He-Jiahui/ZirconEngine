var console = %import("zr.system.console");
var progression = %import("progression/xp_state");

console.printLine("m5-xp-progression-state-test");
var result = progression.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
