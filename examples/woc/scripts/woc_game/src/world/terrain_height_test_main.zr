var console = %import("zr.system.console");
var terrainHeight = %import("world/terrain_height");

console.printLine("m3-terrain-height-test");
var result = terrainHeight.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
