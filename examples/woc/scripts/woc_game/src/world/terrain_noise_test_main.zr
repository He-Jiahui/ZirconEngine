var console = %import("zr.system.console");
var terrainNoise = %import("world/terrain_noise");

console.printLine("m3-terrain-noise-test");
var result = terrainNoise.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
