var console = %import("zr.system.console");
var motionWorld = %import("world/player_motion_world");

console.printLine("m3-player-motion-world-test");
var result = motionWorld.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
