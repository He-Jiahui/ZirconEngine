var console = %import("zr.system.console");
var collisionStatic = %import("world/delve_collision_static");

console.printLine("m3-delve-collision-static-test");
var result = collisionStatic.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
