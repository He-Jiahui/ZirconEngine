var console = %import("zr.system.console");
var decorationCandidate = %import("world/decoration_candidate");

console.printLine("m3-decoration-candidate-test");
var result = decorationCandidate.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
