var console = %import("zr.system.console");
var waterJet = %import("instances/pet_water_jet_state");

console.printLine("m7-pet-water-jet-state-test");
var result = waterJet.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
