// Generated by examples/woc/tools/m3_dungeon_entrance_codegen.mjs.
// Source 5ef9f7cb21cd8875b6d2c49701015dfcd78de35a; DUNGEON_LIST index order; do not edit.

dungeonIndexIsValid(index: int): bool {
    return index >= 0 && index < 6;
}

pub catalogSha(required: bool): string {
    if (!required) { throw "woc dungeon entrance catalog is required"; }
    return "a8c64e4832a082ff1edfee640656b1f8159ea67bd4150562f2ab69f95a076499";
}

pub dungeonCount(required: bool): int {
    if (!required) { throw "woc dungeon entrance count is required"; }
    return 6;
}

pub instanceSlotCount(required: bool): int {
    if (!required) { throw "woc instance slot count is required"; }
    return 24;
}

pub dungeonId(index: int, required: bool): string {
    if (!required || !dungeonIndexIsValid(index)) { throw "woc dungeon index is invalid"; }
    if (index == 0) { return "hollow_crypt"; }
    if (index == 1) { return "sunken_bastion"; }
    if (index == 2) { return "gravewyrm_sanctum"; }
    if (index == 3) { return "drowned_temple"; }
    if (index == 4) { return "nythraxis_crypt"; }
    if (index == 5) { return "nythraxis_boss_arena"; }
    throw "woc dungeon index is invalid";
}

pub dungeonName(index: int, required: bool): string {
    if (!required || !dungeonIndexIsValid(index)) { throw "woc dungeon index is invalid"; }
    if (index == 0) { return "The Hollow Crypt"; }
    if (index == 1) { return "The Sunken Bastion"; }
    if (index == 2) { return "Gravewyrm Sanctum"; }
    if (index == 3) { return "The Drowned Temple"; }
    if (index == 4) { return "Abandoned Crypt"; }
    if (index == 5) { return "Nythraxis Raid Arena"; }
    throw "woc dungeon index is invalid";
}

pub dungeonIndex(index: int, required: bool): int {
    if (!required || !dungeonIndexIsValid(index)) { throw "woc dungeon index is invalid"; }
    if (index == 0) { return 0; }
    if (index == 1) { return 1; }
    if (index == 2) { return 2; }
    if (index == 3) { return 3; }
    if (index == 4) { return 4; }
    if (index == 5) { return 5; }
    throw "woc dungeon index is invalid";
}

pub hasOverworldDoor(index: int, required: bool): bool {
    if (!required || !dungeonIndexIsValid(index)) { throw "woc dungeon index is invalid"; }
    if (index == 0) { return true; }
    if (index == 1) { return true; }
    if (index == 2) { return true; }
    if (index == 3) { return true; }
    if (index == 4) { return true; }
    if (index == 5) { return false; }
    throw "woc dungeon index is invalid";
}

pub doorCoordinate(index: int, axis: int, required: bool): float {
    if (!required || !dungeonIndexIsValid(index) || (axis != 1 && axis != 2)) {
        throw "woc dungeon door coordinate is invalid";
    }
    if (index == 0) { return axis == 1 ? 80 : 90; }
    if (index == 1) { return axis == 1 ? 45 : 515; }
    if (index == 2) { return axis == 1 ? 0 : 858; }
    if (index == 3) { return axis == 1 ? -70 : 792; }
    if (index == 4) { return axis == 1 ? -152 : 610; }
    if (index == 5) { return axis == 1 ? -152 : 610; }
    throw "woc dungeon door coordinate is invalid";
}

pub contractTest(): int {
    if (dungeonCount(true) != 6 || instanceSlotCount(true) != 24 ||
        dungeonId(0, true) != "hollow_crypt" || dungeonId(5, true) != "nythraxis_boss_arena" ||
        !hasOverworldDoor(4, true) || hasOverworldDoor(5, true) ||
        doorCoordinate(3, 1, true) != -70.0 || doorCoordinate(4, 2, true) != 610.0) {
        return -1;
    }
    return 1;
}
