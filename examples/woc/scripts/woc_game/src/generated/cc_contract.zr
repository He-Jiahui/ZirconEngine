// Generated from 5ef9f7cb21cd8875b6d2c49701015dfcd78de35a; do not edit by hand.
pub isStunnedKind(kind: string): bool { return kind == "stun" || kind == "stasis" || kind == "incapacitate" || kind == "polymorph"; }
pub isRootKind(kind: string): bool { return kind == "root"; }
pub isSlowKind(kind: string): bool { return kind == "slow"; }
pub isSilenceKind(kind: string): bool { return kind == "silence"; }
pub isBlindKind(kind: string): bool { return kind == "blind"; }
pub isDisarmKind(kind: string): bool { return kind == "disarm"; }
pub isLockoutKind(kind: string): bool { return kind == "lockout"; }
pub isTonguesKind(kind: string): bool { return kind == "tongues"; }
pub motionAuraKindCode(kind: string): uint {
    if (kind == "stun") { return <uint>1; }
    if (kind == "stasis") { return <uint>2; }
    if (kind == "incapacitate") { return <uint>3; }
    if (kind == "polymorph") { return <uint>4; }
    if (kind == "root") { return <uint>5; }
    if (kind == "ice_floes") { return <uint>6; }
    if (kind == "slow") { return <uint>7; }
    if (kind == "buff_speed") { return <uint>8; }
    if (kind == "buff_armor") { return <uint>9; }
    if (kind == "buff_dodge") { return <uint>10; }
    if (kind == "faerie_fire") { return <uint>11; }
    if (kind == "buff_ap") { return <uint>12; }
    if (kind == "lockout") { return <uint>13; }
    if (kind == "buff_rage_gen") { return <uint>14; }
    if (kind == "buff_reckless") { return <uint>15; }
    if (kind == "battle_stance") { return <uint>16; }
    if (kind == "next_attack_crit") { return <uint>17; }
    if (kind == "buff_haste") { return <uint>18; }
    if (kind == "bleed_vuln") { return <uint>19; }
    if (kind == "buff_dr") { return <uint>20; }
    return <uint>0;
}
pub isMotionAuraKindCode(code: uint): bool { return code >= <uint>1 && code <= <uint>20; }
pub isMotionStunnedKindCode(code: uint): bool {
    return code == <uint>1 || code == <uint>2 ||
        code == <uint>3 || code == <uint>4;
}
pub isMotionRootedKindCode(code: uint): bool {
    return isMotionStunnedKindCode(code) || code == <uint>5;
}
pub isIceFloesKindCode(code: uint): bool { return code == <uint>6; }
pub isMotionSlowKindCode(code: uint): bool { return code == <uint>7; }
pub isMotionSpeedBuffKindCode(code: uint): bool { return code == <uint>8; }
pub faerieFireArmorReduction(): float { return 0.1; }
