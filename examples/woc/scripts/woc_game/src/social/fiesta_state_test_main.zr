var console = %import("zr.system.console");
var fiesta = %import("social/fiesta_state");

console.printLine("m6-fiesta-state-test");
var result = fiesta.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
