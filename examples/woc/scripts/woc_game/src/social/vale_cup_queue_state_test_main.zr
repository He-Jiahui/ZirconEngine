var console = %import("zr.system.console");
var queue = %import("social/vale_cup_queue_state");

console.printLine("m6-vale-cup-queue-state-test");
var result = queue.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
