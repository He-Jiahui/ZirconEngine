var console = %import("zr.system.console");
var spell = %import("combat/spell_combat_state");

console.printLine("m4-spell-combat-state-test");
var result = spell.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
