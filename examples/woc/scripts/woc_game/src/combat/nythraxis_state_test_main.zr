var console = %import("zr.system.console");
var nythraxis = %import("combat/nythraxis_state");

console.printLine("m4-nythraxis-state-test");
var result = nythraxis.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
