var console = %import("zr.system.console");
var history = %import("combat/damage_history_state");
console.printLine("m4-damage-history-state-test");
var result = history.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
