var console = %import("zr.system.console");
var frost = %import("combat/frost_frozen_cast_state");

console.printLine("m4-frost-frozen-cast-state-test");
var result = frost.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
