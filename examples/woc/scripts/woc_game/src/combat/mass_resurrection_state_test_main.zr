var console = %import("zr.system.console");
var resurrection = %import("combat/mass_resurrection_state");
console.printLine("m4-mass-resurrection-state-test");
var result = resurrection.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
