var console = %import("zr.system.console");
var castingState = %import("combat/casting_state");

console.printLine("m4-casting-state-test");
var result = castingState.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
