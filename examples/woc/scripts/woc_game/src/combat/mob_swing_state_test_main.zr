var console = %import("zr.system.console");
var mobSwing = %import("combat/mob_swing_state");

console.printLine("m4-mob-swing-state-test");
var result = mobSwing.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
