var console = %import("zr.system.console");
var dispatchState = %import("combat/effect_world_dispatch_state");

console.printLine("m4-effect-world-dispatch-state-test");
var result = dispatchState.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
