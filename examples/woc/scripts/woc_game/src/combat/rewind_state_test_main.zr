var console = %import("zr.system.console");
var rewind = %import("combat/rewind_state");
console.printLine("m4-rewind-state-test");
var result = rewind.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
