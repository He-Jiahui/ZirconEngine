var console = %import("zr.system.console");
var equipProcs = %import("combat/equip_procs_state");

console.printLine("m4-equip-procs-state-test");
var result = equipProcs.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
