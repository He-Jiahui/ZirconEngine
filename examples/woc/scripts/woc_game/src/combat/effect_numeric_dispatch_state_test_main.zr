var console = %import("zr.system.console");
var dispatchState = %import("combat/effect_numeric_dispatch_state");

console.printLine("m4-effect-numeric-dispatch-state-test");
var result = dispatchState.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
