var console = %import("zr.system.console");
var surge = %import("combat/aether_surge_state");

console.printLine("m4-aether-surge-state-test");
var result = surge.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
