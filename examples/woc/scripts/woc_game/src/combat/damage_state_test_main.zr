var console = %import("zr.system.console");
var damage = %import("combat/damage_state");

console.printLine("m4-damage-state-test");
var result = damage.contractTest();
console.printLine(result);
return result == 1 ? 0 : result;
