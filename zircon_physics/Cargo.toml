[package]
name = "zircon_physics"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Physics world and queries (Godot PhysicsServer-adjacent)."

[features]
default = []
jolt = ["dep:rolt"]

[dependencies]
rolt = { version = "0.3.1", optional = true }
